import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"12. Plugin IPC and Host Services","description":"","frontmatter":{},"headers":[],"relativePath":"spec/07-plugins/12-plugin-ipc-and-host-services.md","filePath":"spec/07-plugins/12-plugin-ipc-and-host-services.md","lastUpdated":1789885187000}');
const _sfc_main = { name: "spec/07-plugins/12-plugin-ipc-and-host-services.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="_12-plugin-ipc-and-host-services" tabindex="-1">12. Plugin IPC and Host Services <a class="header-anchor" href="#_12-plugin-ipc-and-host-services" aria-label="Permalink to &quot;12. Plugin IPC and Host Services&quot;">​</a></h1><h2 id="_1-goals" tabindex="-1">1. Goals <a class="header-anchor" href="#_1-goals" aria-label="Permalink to &quot;1. Goals&quot;">​</a></h2><p>Complete the plugin-related host services and UI IPC so implementation does not rely on ad-hoc conventions.</p><h2 id="_2-main-process-services" tabindex="-1">2. Main-process services <a class="header-anchor" href="#_2-main-process-services" aria-label="Permalink to &quot;2. Main-process services&quot;">​</a></h2><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>PluginManager</span></span>
<span class="line"><span> ├─ PluginRegistryStore</span></span>
<span class="line"><span> ├─ ManifestValidator</span></span>
<span class="line"><span> ├─ PackageInstaller</span></span>
<span class="line"><span> ├─ PermissionGateway</span></span>
<span class="line"><span> ├─ ContributionRegistry</span></span>
<span class="line"><span> │ ├─ CommandRegistry</span></span>
<span class="line"><span> │ ├─ AgentToolRegistry</span></span>
<span class="line"><span> │ ├─ SkillRegistry</span></span>
<span class="line"><span> │ ├─ ThemeRegistry</span></span>
<span class="line"><span> │ └─ McpServerRegistry</span></span>
<span class="line"><span> ├─ PluginRuntimeBroker</span></span>
<span class="line"><span> │ ├─ ServiceSupervisor</span></span>
<span class="line"><span> │ └─ MessageBus</span></span>
<span class="line"><span> ├─ PluginPanelHostService</span></span>
<span class="line"><span> └─ MarketClient</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br></div></div><h2 id="_3-ui-ipc-additions" tabindex="-1">3. UI IPC (additions) <a class="header-anchor" href="#_3-ui-ipc-additions" aria-label="Permalink to &quot;3. UI IPC (additions)&quot;">​</a></h2><h3 id="plugin-domain" tabindex="-1">plugin domain <a class="header-anchor" href="#plugin-domain" aria-label="Permalink to &quot;plugin domain&quot;">​</a></h3><ul><li><code>plugin/list</code></li><li><code>plugin/detail</code></li><li><code>plugin/loadDev</code> — open the folder picker and return what the folder <em>declares</em> as a permission review; nothing is registered yet</li><li><code>plugin/loadDevConfirm</code> — the answer to that review: register the folder as a development plugin and load it with the accepted permissions, which become the ceiling every later hot reload is measured against</li><li><code>plugin/reload</code> — resolve the registered plugin path, compare the manifest against the recorded approval, and either reload in Electron main or return a review when the manifest now asks for more</li><li><code>plugin/reloadConfirm</code> — the answer to that review: reload under the accepted permissions and refresh the development-plugin permission ceiling</li><li><code>plugin/installFromPath</code> ✅</li><li><code>plugin/installFromPackage</code> ✅</li><li><code>plugin/enable</code></li><li><code>plugin/disable</code></li><li><code>plugin/uninstall</code></li><li><code>plugin/reload</code></li><li><code>plugin/getLogs</code></li><li><code>plugin/getPermissions</code></li><li><code>plugin/grantPermissions</code></li><li><code>plugin/revokePermissions</code></li><li><code>plugin/openDataDir</code></li><li><code>plugin/openInstallDir</code></li><li><code>plugin/openPanel</code></li><li><code>plugin/setAutoUpdate</code></li><li><code>plugin/themes</code> ✅ — every loaded plugin&#39;s sanitized theme CSS, for the theme picker and the injected <code>&lt;style&gt;</code> element</li><li><code>plugin/services</code> ✅ — resident service status (<code>starting</code> | <code>running</code> | <code>stopped</code> | <code>failed</code>) plus the restart count, for the Plugins page chips</li></ul><h3 id="commandpalette-domain" tabindex="-1">commandPalette domain <a class="header-anchor" href="#commandpalette-domain" aria-label="Permalink to &quot;commandPalette domain&quot;">​</a></h3><ul><li><code>commandPalette/search</code></li><li><code>commandPalette/execute</code></li><li><code>commandPalette/listRecent</code></li></ul><h3 id="market-domain-implemented" tabindex="-1">market domain (implemented) <a class="header-anchor" href="#market-domain-implemented" aria-label="Permalink to &quot;market domain (implemented)&quot;">​</a></h3><ul><li><code>market/search</code></li><li><code>market/getDetail</code></li><li><code>market/install</code></li><li><code>market/checkUpdates</code></li><li><code>market/applyUpdates</code></li><li><code>market/listProviders</code> (single official provider for now)</li></ul><h2 id="_4-events-main-→-renderer" tabindex="-1">4. Events (main → renderer) <a class="header-anchor" href="#_4-events-main-→-renderer" aria-label="Permalink to &quot;4. Events (main → renderer)&quot;">​</a></h2><ul><li><code>plugin/event/changed</code> (installed/enabled change)</li><li><code>plugin/event/loadError</code></li><li><code>plugin/event/permissionRequired</code></li><li><code>market/event/updateAvailable</code></li></ul><p>The shipped <code>pluginChanged</code> event carries a <code>reason</code> so the renderer can decide what to refetch: <code>install</code>, <code>loadDev</code>, <code>enable</code>, <code>disable</code>, <code>uninstall</code>, <code>crash</code>, <code>service</code>, <code>market.install</code>, <code>market.applyUpdates</code>, <code>themes</code> (runtime <code>themes.upsert</code> / <code>themes.remove</code>). <code>service</code> fires on every supervision transition and is the cheapest of them — only the service list needs a reload.</p><p><code>settingsChanged</code> (<code>pi-desktop/app/event/settingsChanged</code>) carries a settings patch when the <strong>host</strong> writes app settings outside the renderer path — today only plugin <code>app.setTheme</code> (<code>{ theme }</code>). The renderer merges the patch into its store so the shell paints the new preference.</p><p>Panel bridge fixed channels also include <code>app.setTheme</code>, <code>themes.upsert</code>, <code>themes.remove</code>, and <code>themes.list</code> (all require <code>ui.theme</code>).</p><h2 id="_4-1-events-host-→-plugin-process" tabindex="-1">4.1 Events (host → plugin process) <a class="header-anchor" href="#_4-1-events-host-→-plugin-process" aria-label="Permalink to &quot;4.1 Events (host → plugin process)&quot;">​</a></h2><p>The broker also pushes one-way frames down to a plugin&#39;s <code>utilityProcess</code>:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>{ t: &quot;event&quot;, event: &quot;bus.message&quot;, subscriptionId, message }</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>There is no reply frame and no backpressure: delivery is fire-and-forget so a wedged subscriber cannot stall the publisher. The child dispatches to the handler registered for <code>subscriptionId</code> and to any <code>pi.events.on</code> listener; a throwing handler is logged, never fatal. This is the same channel that finally makes <code>pi.events.on</code> / <code>off</code> real (see <a href="./03-plugin-api">03-plugin-api.md</a> §5).</p><h2 id="_5-contributionregistry-behavior" tabindex="-1">5. ContributionRegistry behavior <a class="header-anchor" href="#_5-contributionregistry-behavior" aria-label="Permalink to &quot;5. ContributionRegistry behavior&quot;">​</a></h2><h3 id="registration" tabindex="-1">Registration <a class="header-anchor" href="#registration" aria-label="Permalink to &quot;Registration&quot;">​</a></h3><ul><li>key must be unique</li><li>Plugin commands share the prefix: <code>plugin.&lt;pluginId&gt;.&lt;commandId&gt;</code></li><li>Plugin tool prefix policy: <code>plugin_&lt;pluginIdSafe&gt;_&lt;toolName&gt;</code> (fixed in the implementation)</li></ul><h3 id="query" tabindex="-1">Query <a class="header-anchor" href="#query" aria-label="Permalink to &quot;Query&quot;">​</a></h3><ul><li>The command palette only queries contributions that are enabled + loaded successfully</li><li>Agent sees registered tools; Plan receives no plugin tools regardless of risk or grant state</li></ul><h3 id="deregistration" tabindex="-1">Deregistration <a class="header-anchor" href="#deregistration" aria-label="Permalink to &quot;Deregistration&quot;">​</a></h3><ul><li>Remove everything on disable/unload/uninstall, including stopping resident services, disconnecting MCP servers, dropping bus subscriptions, and removing the plugin&#39;s themes from the picker</li></ul><h2 id="_6-runtimebroker-call-chain" tabindex="-1">6. RuntimeBroker call chain <a class="header-anchor" href="#_6-runtimebroker-call-chain" aria-label="Permalink to &quot;6. RuntimeBroker call chain&quot;">​</a></h2><p>Plugin API call:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>plugin runtime</span></span>
<span class="line"><span> → RPC to PluginRuntimeBroker</span></span>
<span class="line"><span> → PermissionGateway.check</span></span>
<span class="line"><span> → Host service execute</span></span>
<span class="line"><span> → audit log</span></span>
<span class="line"><span> → response</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><h3 id="_6-1-allowlist-names-and-audit-operations-for-the-real-time-capabilities" tabindex="-1">6.1 Allowlist names and audit operations for the real-time capabilities <a class="header-anchor" href="#_6-1-allowlist-names-and-audit-operations-for-the-real-time-capabilities" aria-label="Permalink to &quot;6.1 Allowlist names and audit operations for the real-time capabilities&quot;">​</a></h3><p>The broker&#39;s <code>HOST_API_ALLOWLIST</code> gains three implemented entries, all gated on <code>keyboard.globalShortcut</code>:</p><ul><li><code>keyboard.registerGlobalShortcut</code></li><li><code>keyboard.unregisterGlobalShortcut</code></li><li><code>keyboard.listGlobalShortcuts</code></li></ul><p>Their audit operations are <code>keyboard.globalShortcut.register</code>, <code>keyboard.globalShortcut.unregister</code>, and <code>keyboard.globalShortcut.trigger</code> (a shortcut that fired). Register and trigger entries record the accelerator and command; no key events and no input text are ever recorded.</p><p>The socket capability is implemented: <code>net.websocket.connect</code> / <code>net.websocket.send</code> / <code>net.websocket.close</code> are registered in the same allowlist, gated on <code>net.websocket</code>. Its audit operations are <code>net.websocket.connect</code> and <code>net.websocket.close</code> (plugin id and result, never payloads, headers, or keys), plus a refused <code>net.websocket.send</code>; a successful send is not audited. Frames travel back to the owning plugin only, as the host events <code>net:websocket:open</code>, <code>net:websocket:message</code>, <code>net:websocket:close</code>, and <code>net:websocket:error</code>.</p><p>The audio names are registered in the same allowlist: <code>audio.getInputDevices</code>, <code>audio.openInput</code>, <code>audio.closeInput</code>, <code>audio.getCaptureState</code>, <code>audio.onInputFrame</code>, <code>audio.offInputFrame</code>, <code>audio.openOutput</code>, <code>audio.writeOutput</code>, <code>audio.stopOutput</code>, and <code>audio.closeOutput</code>. The permission gate runs first, so an ungranted call is refused with <code>PERMISSION_DENIED</code> under <code>audio.capture.background</code> / <code>audio.playback.background</code>, exactly like any other gated API. This host has no device backend yet, so every call that passes the gate is audited as an <code>UNSUPPORTED</code> refusal — <code>{ api: &quot;audio.&lt;method&gt;&quot;, ok: false, errorCode: &quot;UNSUPPORTED&quot; }</code> — and rejected with that code; the two synchronous registration helpers <code>audio.onInputFrame</code> / <code>audio.offInputFrame</code> throw it synchronously. Reserved audit-operation names for the device service: <code>audio.input.open</code> / <code>audio.input.close</code>, <code>audio.output.open</code> / <code>audio.output.stop</code> / <code>audio.output.close</code> (ADR 0257).</p><h2 id="_7-panelhost-interaction" tabindex="-1">7. PanelHost interaction <a class="header-anchor" href="#_7-panelhost-interaction" aria-label="Permalink to &quot;7. PanelHost interaction&quot;">​</a></h2><ul><li>Create an isolated view when opening a panel</li><li>Pass in pluginId / theme tokens</li><li>Destroy the view and message subscriptions on close. Cleanup that needs a <code>webContents</code> identity copies that id before the window is destroyed; the <code>closed</code> handler must not read <code>webContents</code> on a destroyed window, or the host surfaces an uncaught <code>TypeError: Object has been destroyed</code>.</li><li>The preload exposes <code>pluginBridge.getDroppedFilePath(file)</code> without exposing Node to the page. A panel may call <code>fs.registerDropped</code> with that path; the host consumes a sender-bound recent drop record once and issues a one-file read grant for <code>fs.stat</code> / <code>fs.readRange</code>.</li></ul><p>Panel bridge file channels are permission-gated as follows:</p><table tabindex="0"><thead><tr><th>Channel</th><th>Required permission</th></tr></thead><tbody><tr><td><code>fs.readText</code>, <code>fs.stat</code>, <code>fs.readRange</code>, <code>fs.readPreview</code>, <code>fs.openDefault</code>, <code>fs.reveal</code>, <code>fs.glob</code>, <code>fs.list</code></td><td><code>fs.read</code></td></tr><tr><td><code>fs.registerDropped</code></td><td><code>fs.read</code> plus a real drop gesture</td></tr><tr><td><code>fs.writeText</code></td><td><code>fs.write</code></td></tr></tbody></table><h2 id="_8-failure-isolation" tabindex="-1">8. Failure isolation <a class="header-anchor" href="#_8-failure-isolation" aria-label="Permalink to &quot;8. Failure isolation&quot;">​</a></h2><ul><li>Plugin API timeout: return TIMEOUT</li><li>runtime crash: mark load_error, clean up contributions</li><li>panel crash: only close the panel, do not unload the plugin (can prompt to reload)</li></ul><p><strong>Implemented (2026-07-29, ADR 0008):</strong> the broker lives in <code>electron/main/plugin-runtime.ts</code> and every plugin call is a request to the plugin&#39;s own <code>utilityProcess</code>. Budgets: load 15s, lifecycle hook 5s, command 30s, tool 110s (under host-core&#39;s 150s dispatch budget). On process exit the broker rejects pending calls with <code>PLUGIN_CRASHED</code>, deregisters that plugin&#39;s commands and tools, closes its panel, writes a <code>plugin.crash</code> audit entry, and emits a toast plus <code>pluginChanged</code> to the renderer.</p><h2 id="_9-acceptance" tabindex="-1">9. Acceptance <a class="header-anchor" href="#_9-acceptance" aria-label="Permalink to &quot;9. Acceptance&quot;">​</a></h2><ol><li>The plugin list IPC works</li><li>The command palette IPC can execute plugin commands</li><li>Start/stop triggers contribution registration/deregistration</li><li>The market IPC runs end-to-end under a mock provider (later milestone)</li></ol><h2 id="appendix-agent-tool-dispatch-protocol-implemented-m5" tabindex="-1">Appendix: agent-tool dispatch protocol (implemented M5) <a class="header-anchor" href="#appendix-agent-tool-dispatch-protocol-implemented-m5" aria-label="Permalink to &quot;Appendix: agent-tool dispatch protocol (implemented M5)&quot;">​</a></h2><p>Plugin agent tools execute in the desktop runner (Electron main), while the permission gate and result envelope stay in host-core:</p><ol><li>Model calls <code>plugin_&lt;pluginIdSafe&gt;_&lt;toolName&gt;</code>; the sidecar forwards it to host <code>tools.execute</code> like any built-in tool.</li><li>host-core resolves the durable operating mode first. In Agent it runs the normal permission flow (risk, session grants, 120s timeout), then emits notification <code>plugins.execute</code><code>{ executionId, sessionId, toolCallId, toolName, args, turnId }</code>. <code>turnId</code> is the runtime turn identity, forwarded unchanged so the plugin tool context can be matched against the <code>session:turnEnded</code> event.</li><li>Plan calls fail at the host policy step with <code>PLUGIN_DISABLED_IN_PLAN</code>; they never reach Electron or the plugin runtime. Agent calls continue with Electron main executing the registered plugin tool JS and answering via RPC <code>plugins.resolveExecution</code> <code>{ executionId, ok, content, errorCode? }</code>.</li><li>host-core resolves the pending execution and returns a standard <code>ToolsExecuteResult</code> to the sidecar. Dispatch waits up to 150s (<code>DESKTOP_TOOL_DISPATCH_TIMEOUT_MS</code>, above both the 110s plugin tool budget and the widest MCP leg — a 10s lazy handshake, a 30s <code>tools/list</code> traversal, then the 100s call) and then maps to <code>TOOL_TIMEOUT</code>; an unknown/unloaded tool maps to <code>TOOL_NOT_FOUND</code>. The transport deadline for these calls covers the 120s permission wait, the 30s admission queue wait, that dispatch, and 10s of slack (<code>rpcTimeoutMs</code>), so no outer layer gives up before host-core reports the outcome.</li></ol><p>The model-facing registry gains plugin tools per prompt: main passes registered defs (<code>fullName</code>, description, JSON-schema parameters) to <code>agent.prompt</code>, and the runtime keeps them in a deferred catalog instead of serializing every schema into the first request. The model loads a matching plugin tool through the local <code>ToolSearch</code> tool; the next turn receives the selected schema and then uses the same host permission/dispatch path above. Covered by protocol smoke scenario E2E-024 and the runtime-loading scenario E2E-008a.</p><p>Tools discovered from a plugin&#39;s MCP servers enter the same registry under <code>plugin_&lt;pluginIdSafe&gt;_&lt;serverId&gt;_&lt;toolName&gt;</code>, so steps 1–4 above are unchanged; only step 3 differs internally, forwarding to the MCP client instead of plugin JS.</p><p>Skills use a separate, simpler path. The catalog (id, name, description) is part of the base system prompt, the <code>Skill</code> schema is itself deferred behind <code>ToolSearch</code>, and its body is fetched by a local <code>Skill</code> tool that Electron main serves directly — the sidecar never holds skill text, and a skill document reaches the model only when it asks for it (D174/D185).</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("spec/07-plugins/12-plugin-ipc-and-host-services.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _12PluginIpcAndHostServices = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _12PluginIpcAndHostServices as default
};
