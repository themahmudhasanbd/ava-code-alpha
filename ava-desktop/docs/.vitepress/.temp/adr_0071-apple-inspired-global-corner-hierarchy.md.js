import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0071: Adopt an Apple-Inspired Global Corner Hierarchy","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0071-apple-inspired-global-corner-hierarchy.md","filePath":"adr/0071-apple-inspired-global-corner-hierarchy.md","lastUpdated":1786723993000}');
const _sfc_main = { name: "adr/0071-apple-inspired-global-corner-hierarchy.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0071-adopt-an-apple-inspired-global-corner-hierarchy" tabindex="-1">ADR 0071: Adopt an Apple-Inspired Global Corner Hierarchy <a class="header-anchor" href="#adr-0071-adopt-an-apple-inspired-global-corner-hierarchy" aria-label="Permalink to &quot;ADR 0071: Adopt an Apple-Inspired Global Corner Hierarchy&quot;">​</a></h1><ul><li>Status: Accepted for implementation</li><li>Date: 2026-08-11</li><li>Deciders: PI-Desktop core</li><li>Related: D072, D210</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>PI-Desktop enforced a shared radius-token scale, but that scale preserved a collection of historical 5/6/7/8/10/12/14/16/18/22px values. The one-pixel steps at the compact end were difficult to distinguish, and the shared button primitive used a capsule even though most of its 28–32px actions belong to a dense desktop interface.</p><p>Apple&#39;s current design guidance separates fixed rounded rectangles, capsules, and concentric shapes. It continues to use rounded rectangles for Mini, Small, and Medium macOS controls, reserves capsules for emphasis and appropriate component families, and defines a concentric child radius from its containing radius and inset. Applying that geometry is more important than copying one radius across every component.</p><p>The renderer is pinned to Electron 37 / Chromium 138. CSS <code>corner-shape</code> and its <code>squircle</code> value ship in Chromium 139, so relying on them would silently fall back in the packaged application.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>Replace the fixed-radius ladder with 4/6/8/10/12/14/16/18/20/24px values. Keep <code>--radius-full</code> for capsules and <code>--radius-round</code> for circles.</li><li>Standard compact and medium controls use fixed rounded rectangles. The shared button and field primitives use <code>--radius-sm</code> (10px).</li><li>Pills, badges, segmented selections, switches, tracks, status dots, and equal-width circular icon controls keep their explicit capsule or circle geometry.</li><li>Surfaces increase radius with size and elevation. When a child corner sits near and parallel to a rounded container corner, use the concentric relation <code>outer radius = inner radius + inset</code>.</li><li>Full-width structural shell surfaces remain square at the window edge.</li><li>The composer aliases its established 20px radius to <code>--radius-xl</code>, keeping the visible composer geometry while bringing it into the global ladder.</li><li>D072&#39;s token-only lint enforcement remains unchanged; only the frozen pixel values and shared primitive assignments are amended.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>The interface has a more legible small-to-large corner hierarchy, while compact macOS controls no longer look uniformly pill-shaped.</li><li>Existing token consumers receive the revised values globally without component-local literals.</li><li>Designers and implementers must choose pill or circle tokens intentionally instead of treating them as a default radius.</li><li>True continuous-corner rendering remains unavailable until the packaged Chromium runtime supports <code>corner-shape</code>; the compatible fallback is the standardized fixed-radius ladder.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><h3 id="make-every-component-a-capsule" tabindex="-1">Make every component a capsule <a class="header-anchor" href="#make-every-component-a-capsule" aria-label="Permalink to &quot;Make every component a capsule&quot;">​</a></h3><p>Rejected because Apple retains rounded rectangles for compact macOS controls and uses capsules selectively for emphasis and specific control families.</p><h3 id="add-corner-shape-squircle-now" tabindex="-1">Add <code>corner-shape: squircle</code> now <a class="header-anchor" href="#add-corner-shape-squircle-now" aria-label="Permalink to &quot;Add \`corner-shape: squircle\` now&quot;">​</a></h3><p>Rejected because the packaged Chromium 138 runtime does not support it. A declaration that only works in newer development browsers would make visual verification and shipped behavior diverge.</p><h3 id="keep-the-historical-values-and-change-individual-components" tabindex="-1">Keep the historical values and change individual components <a class="header-anchor" href="#keep-the-historical-values-and-change-individual-components" aria-label="Permalink to &quot;Keep the historical values and change individual components&quot;">​</a></h3><p>Rejected because the request is global, and retaining closely spaced 5/6/7/8px steps would continue to make the hierarchy hard to perceive and maintain.</p><h2 id="addendum-2026-08-14-the-corner-shape-blocker-is-gone" tabindex="-1">Addendum (2026-08-14): the <code>corner-shape</code> blocker is gone <a class="header-anchor" href="#addendum-2026-08-14-the-corner-shape-blocker-is-gone" aria-label="Permalink to &quot;Addendum (2026-08-14): the \`corner-shape\` blocker is gone&quot;">​</a></h2><p>The Context and Alternatives above are left as recorded. Their runtime premise no longer holds: the shell moved to Electron 43, which bundles Chromium 150, and <code>CSS.supports(&quot;corner-shape&quot;, &quot;squircle&quot;)</code> returns <code>true</code> in the packaged renderer. The deferral in Consequences and the &quot;Add <code>corner-shape: squircle</code> now&quot; rejection were both about availability alone, so nothing stands in the way of revisiting them.</p><p>This addendum does not change the decision. The fixed-radius ladder stays as specified; adopting continuous corners is a design change that needs its own decision, not a side effect of a dependency bump.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0071-apple-inspired-global-corner-hierarchy.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0071AppleInspiredGlobalCornerHierarchy = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0071AppleInspiredGlobalCornerHierarchy as default
};
