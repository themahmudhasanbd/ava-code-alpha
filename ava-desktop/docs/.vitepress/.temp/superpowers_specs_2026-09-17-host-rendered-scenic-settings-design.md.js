import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Host-rendered Scenic Settings Design","description":"","frontmatter":{},"headers":[],"relativePath":"superpowers/specs/2026-09-17-host-rendered-scenic-settings-design.md","filePath":"superpowers/specs/2026-09-17-host-rendered-scenic-settings-design.md","lastUpdated":1789642367000}');
const _sfc_main = { name: "superpowers/specs/2026-09-17-host-rendered-scenic-settings-design.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="host-rendered-scenic-settings-design" tabindex="-1">Host-rendered Scenic Settings Design <a class="header-anchor" href="#host-rendered-scenic-settings-design" aria-label="Permalink to &quot;Host-rendered Scenic Settings Design&quot;">​</a></h1><h2 id="status" tabindex="-1">Status <a class="header-anchor" href="#status" aria-label="Permalink to &quot;Status&quot;">​</a></h2><p>Approved for implementation on 2026-09-17.</p><h2 id="problem" tabindex="-1">Problem <a class="header-anchor" href="#problem" aria-label="Permalink to &quot;Problem&quot;">​</a></h2><p>Plugin-provided HTML Settings pages cannot meet the scenic compositing requirement. An Electron <code>WebContentsView</code> is an opaque native child surface; a sandboxed iframe is a separate Chromium document surface. Both introduce a solid rectangle between the host-owned scenic backdrop and the Settings controls.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Replace generic plugin Settings HTML destinations with a host-rendered declarative <code>scenicThemes</code> destination. The host renders all DOM, styling, layout, focus behavior, selection state, slider, and Apply action in the normal React Settings tree. A plugin contributes only validated metadata and declared theme assets.</p><p>The first implementation supports the Nexus Scenic Themes pack. It is a deliberately narrow contract, not a general plugin Settings UI language.</p><h2 id="manifest-contract" tabindex="-1">Manifest contract <a class="header-anchor" href="#manifest-contract" aria-label="Permalink to &quot;Manifest contract&quot;">​</a></h2><p><code>contributes.scenicThemes</code> is an optional object requiring a stable id, localized label, description, keywords, a host-supported icon token, and one to twelve theme cards in presentation order. Every card carries a plugin-owned declared theme id, localized name and description, and one preview asset declared by that theme.</p><p>The host verifies <code>ui.settings</code>, <code>ui.theme</code>, card ids, localization values, asset containment, ownership, and the declared typed <code>--nexus-backdrop-blur</code> length variable with whole-number <code>0..20</code> bounds. Other variables and plugin settings are not editable by this destination.</p><p>Generic <code>settingsDestinations</code> HTML entries are retired from the appearance extension capability. The plugin&#39;s <code>settings/</code> directory is removed; there is no iframe, custom page protocol, bridge, or second document canvas.</p><h2 id="host-surface-and-visual-ownership" tabindex="-1">Host surface and visual ownership <a class="header-anchor" href="#host-surface-and-visual-ownership" aria-label="Permalink to &quot;Host surface and visual ownership&quot;">​</a></h2><p>The host places the scenic destination under Extensions and renders it directly inside <code>.settings-content-inner</code>. The host canvas, content wrapper, and scenic destination wrapper are transparent under a scenic plugin theme so the one root backdrop remains visible throughout Settings.</p><ul><li>Preview cards use declared local <code>plugin-asset://</code> images, readable overlays, selected outline/checkmark, and host-owned focus behavior.</li><li>Only individual cards and the blur control own material surfaces; the destination has no outer panel or full-page background.</li><li>The host owns responsive grid layout, narrow-window reflow, native titlebar/control protection, drag regions, and reduced-transparency fallbacks.</li></ul><h2 id="behavior-and-data-flow" tabindex="-1">Behavior and data flow <a class="header-anchor" href="#behavior-and-data-flow" aria-label="Permalink to &quot;Behavior and data flow&quot;">​</a></h2><ol><li>Main validates scenic metadata from each loaded plugin.</li><li>The host renders cards and resolves the selected theme&#39;s persisted blur value.</li><li>Selecting a card immediately invokes the restricted plugin-owned theme selection API.</li><li>The range input accepts integer values from 0 to 20 and changes only local draft state plus its <code>npx</code> output.</li><li>Apply invokes the existing typed variable API for only <code>--nexus-backdrop-blur</code> on the selected owned theme.</li><li>The host refreshes active theme CSS after successful save; failures restore confirmed value and show a host toast.</li><li>Switching cards restores each theme&#39;s confirmed blur; base/other plugin themes follow existing theme cleanup.</li></ol><h2 id="security-and-lifecycle" tabindex="-1">Security and lifecycle <a class="header-anchor" href="#security-and-lifecycle" aria-label="Permalink to &quot;Security and lifecycle&quot;">​</a></h2><p>Plugins cannot provide HTML, CSS, JavaScript, selectors, components, event handlers, arbitrary actions, or iframe content. They cannot change Settings geometry, navigation, drag regions, native controls, or the work panel. Unknown paths, URLs, variables, theme ids, and cross-plugin references fail closed. Disable, uninstall, or invalid metadata removes the destination and returns the active Settings view to General.</p><h2 id="tests-and-validation" tabindex="-1">Tests and validation <a class="header-anchor" href="#tests-and-validation" aria-label="Permalink to &quot;Tests and validation&quot;">​</a></h2><ul><li>SDK validation rejects malformed objects, unknown/cross-plugin theme ids, undeclared assets, duplicates, non-localized strings, unsupported variables, and card counts outside 1–12.</li><li>Main/IPC tests prove eligible loaded plugins only, declared asset rewriting, typed variable ownership, and lifecycle cleanup.</li><li>Renderer tests prove the host renders cards/range/Apply, transparent outer ownership, responsive layout, selected accessibility, no iframe, and no outer panel.</li><li>Interaction tests prove immediate selection, draft-only slider updates, Apply-only persistence, restoration, failures, base/plugin isolation, reduced transparency, and native controls.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("superpowers/specs/2026-09-17-host-rendered-scenic-settings-design.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _20260917HostRenderedScenicSettingsDesign = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _20260917HostRenderedScenicSettingsDesign as default
};
