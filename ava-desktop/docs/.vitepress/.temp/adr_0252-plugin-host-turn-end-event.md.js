import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0252: Host turn-end event for plugins","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0252-plugin-host-turn-end-event.md","filePath":"adr/0252-plugin-host-turn-end-event.md","lastUpdated":1789397975000}');
const _sfc_main = { name: "adr/0252-plugin-host-turn-end-event.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0252-host-turn-end-event-for-plugins" tabindex="-1">ADR 0252: Host turn-end event for plugins <a class="header-anchor" href="#adr-0252-host-turn-end-event-for-plugins" aria-label="Permalink to &quot;ADR 0252: Host turn-end event for plugins&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-13</li><li>Deciders: PI-Desktop core</li><li>Related: <a href="./../spec/08-meta/decisions-log">D422</a> · <a href="./0005-user-installable-plugin-system">ADR 0005</a> · <a href="./0040-plugin-resident-services-and-message-bus">ADR 0040</a> · <a href="./0213-persist-host-owned-turn-queue">ADR 0213</a> · <a href="./0235-domain-facades-and-architecture-budgets">ADR 0235</a> · <code>07-plugins/03-plugin-api.md</code> · <code>07-plugins/13-plugin-permissions-matrix.md</code></li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>A plugin that drives a GUI — a notification surface, a launcher, a work-panel view — can already observe <code>workspace:changed</code>, <code>plugin:settingsChanged</code>, and <code>session:modelChanged</code>, but nothing told it when the host turn it was reacting to had actually finished. Plugins fell back to idle timers, which are wrong in both directions: they fire during a long tool call, and they fire again for a turn that ended moments earlier.</p><p>The host already owns this fact. One turn teardown path resolves the durable <code>session.endTurn</code> row with <code>completed</code>, <code>aborted</code>, or <code>error</code> for the turn created by <code>session.beginTurn</code>. What was missing was a published identity and a published boundary: the plugin tool context declared <code>turnId</code>, but the host never populated it, so a plugin could not correlate a late tool result with the turn that requested it.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><p><strong><code>session:turnEnded</code> is a host event.</strong> Payload is <code>{ sessionId: string; turnId: string; reason: &quot;completed&quot; | &quot;aborted&quot; | &quot;error&quot; }</code>, delivered to plugin processes exactly like <code>workspace:changed</code> and <code>session:modelChanged</code>, and (like <code>workspace:changed</code>) to plugin panel pages and docked views.</p></li><li><p><strong>Once per turn actually started.</strong> The event is emitted at the end of turn teardown, after the durable <code>session.endTurn</code> attempt, and only for a turn created by <code>session.beginTurn</code>: a user submission, an approved plan execution, or a scheduled run. A queued item that never started produces no event. The announcement is claimed once per <code>(sessionId, turnId)</code>, so a second terminal event for the same turn joins the first claim rather than announcing again.</p></li><li><p><strong>Turn identity is authoritative.</strong> The finalizer is called with an explicit <code>turnId</code> and never infers one from whichever turn happens to be active, so a late terminal event from an earlier turn cannot settle a newer one, and a terminal event carrying no identity settles nothing at all. Plugin tool contexts are populated with the same <code>turnId</code>, forwarded unchanged from the host.</p></li><li><p><strong>No new permission.</strong> The event travels on the existing plugin event channel to every loaded plugin, and subscribing to an unknown event name does not error.</p></li></ol><p>Explicit non-promises: there is no ack and no replay. The host broadcasts once per started turn; a plugin that is alive and subscribed receives it once. Delivery that races a plugin crash, reload, or host quit is not guaranteed, and receiving the event does not mean that every in-flight tool of that turn has exited — late results can still arrive, so cleanup must be serialised or scoped by <code>turnId</code>. A helper that only releases its own resources must not fabricate a host turn end, and a graceful stop keeps the existing <code>completed</code> boundary.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Plugins settle turn-scoped UI, notifications, and bookkeeping from the host&#39;s own terminal state instead of guessing with idle timers.</li><li>The event bounds the turn, not the individual tool calls, so a plugin must still treat late tool results as valid.</li><li>The announcement is one call made from one finalizer that validates turn identity, requires no <code>await</code> before it claims the turn, and runs after the local teardown. Each of the three delivery surfaces isolates its own senders, so one unreachable recipient cannot suppress the others, and a failed or timed-out durable write does not suppress the announcement.</li><li>Turn-scoped cleanup that ignores <code>turnId</code> can settle the wrong turn; the tool context&#39;s <code>turnId</code> now makes correct scoping possible.</li><li>No published release emits this event yet: the first release that contains it has not shipped, and 0.14.8 does not include it. A plugin that depends on it must require the release that actually ships it, and must not assume 0.14.7 or 0.14.8.</li></ul><h2 id="alternatives-rejected" tabindex="-1">Alternatives rejected <a class="header-anchor" href="#alternatives-rejected" aria-label="Permalink to &quot;Alternatives rejected&quot;">​</a></h2><h3 id="poll-a-session-api-for-the-active-turn" tabindex="-1">Poll a session API for the active turn <a class="header-anchor" href="#poll-a-session-api-for-the-active-turn" aria-label="Permalink to &quot;Poll a session API for the active turn&quot;">​</a></h3><p>Reading session state on a timer would need <code>session.read</code>, would hand a plugin the conversation it does not otherwise see, and would still leave the end boundary to guesswork — the event carries no more than the identity and reason the host already owns.</p><h3 id="release-the-plugin-per-invocation" tabindex="-1">Release the plugin per invocation <a class="header-anchor" href="#release-the-plugin-per-invocation" aria-label="Permalink to &quot;Release the plugin per invocation&quot;">​</a></h3><p>Unloading a plugin when its turn ends ties turn end to a lifecycle event that also has to survive reload, crash, and quit, and would drop the resident services and panel state a plugin holds across turns.</p><h3 id="raise-the-idle-timeout" tabindex="-1">Raise the idle timeout <a class="header-anchor" href="#raise-the-idle-timeout" aria-label="Permalink to &quot;Raise the idle timeout&quot;">​</a></h3><p>A longer silence threshold detects turn end later and still cannot distinguish &quot;a tool is thinking&quot; from &quot;the turn is over&quot;; it also delays exactly the completion signal a GUI plugin exists to deliver.</p><h2 id="references" tabindex="-1">References <a class="header-anchor" href="#references" aria-label="Permalink to &quot;References&quot;">​</a></h2><ul><li><code>apps/desktop/electron/main/runtime/plans.ts</code> — <code>finishTurn</code>, the single teardown entry every terminal path funnels through</li><li><code>apps/desktop/electron/main/runtime/session-coordination.ts</code> — turn identity, the abort lock, and the shared busy check</li><li><code>apps/desktop/electron/main/services/plugin-services.ts</code> — <code>announceTurnEnded</code></li><li><code>apps/desktop/electron/main/plugin-runtime.ts</code> — <code>broadcastEvent</code></li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0252-plugin-host-turn-end-event.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0252PluginHostTurnEndEvent = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0252PluginHostTurnEndEvent as default
};
