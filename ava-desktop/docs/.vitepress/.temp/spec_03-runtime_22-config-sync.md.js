import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Portable configuration sync","description":"","frontmatter":{},"headers":[],"relativePath":"spec/03-runtime/22-config-sync.md","filePath":"spec/03-runtime/22-config-sync.md","lastUpdated":1790065649000}');
const _sfc_main = { name: "spec/03-runtime/22-config-sync.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="portable-configuration-sync" tabindex="-1">Portable configuration sync <a class="header-anchor" href="#portable-configuration-sync" aria-label="Permalink to &quot;Portable configuration sync&quot;">​</a></h1><p>Status: implemented baseline for the approved WebDAV configuration-sync slice.</p><p>Portable configuration sync is a host-owned, encrypted WebDAV workflow. It does not synchronize conversation history, project source files, attachments, runtime state, or model weights. The Renderer only receives redacted status, preview counts, conflicts, and activation summaries.</p><h2 id="_1-ownership-and-boundaries" tabindex="-1">1. Ownership and boundaries <a class="header-anchor" href="#_1-ownership-and-boundaries" aria-label="Permalink to &quot;1. Ownership and boundaries&quot;">​</a></h2><p>The process path remains:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Renderer → Preload IPC → Electron Main → host-core → WebDAV</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Host-core owns the vault key, WebDAV transport, revision state, merge base, pending approvals, local encrypted staging, and import application. Electron Main only registers the IPC handlers and forwards <code>configSync.changed</code>. Agent Runtime has no sync implementation.</p><p>The export path is an explicit per-domain allowlist in <code>crates/host-core/src/config_sync/domains.rs</code>. It never serializes the database, the secrets directory, or an arbitrary filesystem tree. Local paths, window state, readiness flags, OAuth sessions, plugin binaries, and device approvals are local overlays or excluded data. The same module exposes an adapter registry declaring schema version, portable/secret fields, local overlays, identity and references, merge granularity, activation, and recovery policy for every supported domain.</p><h2 id="_2-portable-domains" tabindex="-1">2. Portable domains <a class="header-anchor" href="#_2-portable-domains" aria-label="Permalink to &quot;2. Portable domains&quot;">​</a></h2><p>The current adapter set covers application preferences, user-owned providers, MCP definitions, user skills, global subagents, app-managed global/project instructions and projects, plugin installation intent, scheduled automations, and optional project memory. Credentials are opt-in. Provider API keys and MCP environment variables/headers are included only when the credential category is selected; OAuth access/refresh tokens and cookies are never exported. The instruction adapter reads only the fixed global <code>~/.pi/agent/AGENTS.md</code> and each registered project&#39;s root <code>AGENTS.md</code>; it does not scan nested repositories or arbitrary files. Imported instruction files are written only after their scope is explicitly selected, mapped where required, and approved. Directory-shaped skills carry bounded sibling resources as authenticated objects. Package paths, symlinks, collisions, file counts, and total size are validated by Host before approved resources are written: at most 256 resources per package, 2 MiB per resource, and 16 MiB per package, while the skill document itself stays capped at 128 KiB because it can reach a prompt. Scripts are stored as bytes and are never executed by import.</p><p>Project and workspace bindings are represented by opaque logical identifiers. Host-core assigns a persistent logical identity to each registered standalone project; project-group roots derive identities from the group identity and ordered root position. A device mapping overlays the received logical ID onto its local folder, so different absolute paths do not create duplicate entities. Incoming project-scoped executable content stays staged until a local folder mapping and approval exist. No local path is written into the encrypted portable entity payload. Plugin data that has no supported adapter is reported as unsupported rather than treated as synchronized. Approved plugin installation intent is kept in a Host-owned local overlay; it never pulls plugin bytes or permission grants.</p><h2 id="_3-vault-and-webdav-protocol" tabindex="-1">3. Vault and WebDAV protocol <a class="header-anchor" href="#_3-vault-and-webdav-protocol" aria-label="Permalink to &quot;3. Vault and WebDAV protocol&quot;">​</a></h2><p>Each vault has a random data key. The backup password is processed with bounded Argon2id parameters, and the data key is wrapped with AES-256-GCM. Every head, revision manifest, and resource object is separately authenticated with a purpose/vault-derived object key, domain-separated associated data, and a fresh nonce. Resource object IDs are keyed content identifiers, so plaintext global hashes are not required. The vault envelope version is independent from the SQLite schema and is bumped when cryptographic derivation changes.</p><p>The remote layout is an opaque header plus immutable revision/resource objects. Strict mode also has one mutable encrypted head:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&lt;selected-directory&gt;/header</span></span>
<span class="line"><span>&lt;selected-directory&gt;/vault/&lt;opaque-vault-id&gt;/head</span></span>
<span class="line"><span>&lt;selected-directory&gt;/vault/&lt;opaque-vault-id&gt;/revisions/&lt;revision-id&gt;</span></span>
<span class="line"><span>&lt;selected-directory&gt;/vault/&lt;opaque-vault-id&gt;/objects/&lt;object-id&gt;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>Strict initialization uses <code>If-None-Match: *</code>. Existing heads require a strong ETag and are published with <code>If-Match</code>. A failed precondition restarts reconciliation from the newly read head; it never overwrites blindly. The capability probe writes a temporary object with conditional creation twice, obtains a strong ETag, verifies a matching <code>If-Match</code> update, and verifies a stale <code>If-Match</code> is rejected before the object is removed.</p><p>The settings page also offers an explicit append-only compatibility mode for a server that ignores conditional headers. The probe must still prove bounded <code>PROPFIND</code> directory listing. Compatibility mode adds an opaque per-device pointer collection:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&lt;selected-directory&gt;/vault/&lt;opaque-vault-id&gt;/heads/&lt;device-id&gt;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Each device overwrites only its own encrypted pointer and verifies it by reading it back. A sync lists all pointers, includes a legacy strict head when present, walks the bounded parent graph, removes ancestor pointers, and merges the remaining tips against the acknowledged base. New revisions and resources are unique immutable objects; an existing resource is read and authenticated before reuse. Compatibility mode retains immutable history and does not run remote cleanup because the server cannot provide a safe cross-device acknowledgement protocol. All devices sharing a vault must use the same mode. This mode is never selected silently and does not claim strict CAS guarantees.</p><p>Some WebDAV gateways report a missing object as <code>502 Bad Gateway</code> instead of <code>404 Not Found</code>. The capability probe records this behavior for the selected endpoint after deleting its temporary object; subsequent reads treat only that observed status as absence. Other non-success responses remain errors. This does not relax strict-mode conditional-write requirements: a server that ignores <code>If-None-Match</code> or <code>If-Match</code> is usable for bidirectional sync only after the user selects append-only compatibility mode and the directory-listing probe succeeds.</p><p>HTTPS is required by default. The settings page may expose an explicit LAN-risk acknowledgement for HTTP, but Host accepts that exception only for localhost, <code>.local</code> names, or private/link-local IP addresses. Public HTTP endpoints remain rejected. The warning explains that HTTP does not protect WebDAV credentials in transit. Redirects, endpoint userinfo, path traversal, unsafe remote names, oversized objects, weak ETags, and unbounded KDF parameters are rejected.</p><h2 id="_4-merge-and-activation" tabindex="-1">4. Merge and activation <a class="header-anchor" href="#_4-merge-and-activation" aria-label="Permalink to &quot;4. Merge and activation&quot;">​</a></h2><p>The local encrypted base is the last acknowledged common revision. Capture, remote read, three-way merge, immutable object upload, head publication, and local application are serialized per vault. Strict mode uses CAS head publication; compatibility mode publishes a per-device pointer and merges discovered tips. Scalar settings merge by declared entity unit; provider records, MCP records, skill packages, and automation definitions are not merged as arbitrary JSON arrays. Tombstones represent explicit deletion; an unselected category is not deletion.</p><p>Same edits converge and disjoint edits continue. Same-unit edits and delete-versus-edit retain both candidates as conflicts. Imported MCP, skills, subagents, plugins, and automations require local activation approval; an approval is bound to the entity digest. A changed command, endpoint, script, instruction, or credential destination therefore invalidates the old approval. Missing provider references are retained as dependency approvals instead of being written as unusable defaults; unrelated entities can continue applying. The host never activates staged executable content merely because a UI flag is set. Newly imported automation definitions are also disabled until this device explicitly takes execution ownership; an existing local task keeps its device-local enabled state.</p><p>The import path rechecks the captured local generation before applying each entity. Changes made after capture are left in place and are reconciled on the next run. A durable encrypted pending bundle records the captured local manifest and an <code>applying</code> marker; restart recovery finishes the approved steps or leaves the bundle visible for retry before the configuration is considered converged. Existing direct local MCP creation behavior is unchanged.</p><h2 id="_5-settings-workflow" tabindex="-1">5. Settings workflow <a class="header-anchor" href="#_5-settings-workflow" aria-label="Permalink to &quot;5. Settings workflow&quot;">​</a></h2><p>Settings → Cloud sync provides WebDAV endpoint credentials, vault password, device label, server compatibility mode, category selection, a capability test, sync-now, unlock, pause, folder mapping, approval/rejection, revision history/restore, vault-password rewrap, and disconnect controls. Strict CAS is the default. Selecting append-only compatibility mode shows a persistent risk warning and requires confirmation before configuration is saved; its test success reports directory-listing support rather than conditional-write support. The renderer displays <code>notConfigured</code>, <code>locked</code>, <code>upToDate</code>, <code>localChangesPending</code>, <code>syncing</code>, <code>offline</code>, <code>unsupportedServer</code>, <code>conflict</code>, <code>awaitingActivation</code>, <code>paused</code>, and <code>error</code> as distinct states. Disconnect keeps local data and does not delete remote data.</p><p>A manual sync reports what it is doing while it runs. <code>configSync.progress</code> carries the current phase — <code>capture</code>, <code>download</code>, <code>merge</code>, <code>upload</code>, <code>apply</code>, or <code>cleanup</code> — the units finished in that phase, and the bytes when they are known: <code>done</code>/<code>total</code> count resource objects while transferring — the device tips being read, in append-only mode — and entities otherwise, a <code>total</code> of 0 means the phase cannot know its size, and a <code>bytesTotal</code> of 0 means the byte size is unknown, which is the normal case for a download. Reports are throttled, a phase change is never dropped, and the state event plus the call&#39;s return value stay the terminal signal. Background polls report nothing: only the manual path has a caller watching.</p><p>Credentials and memory are unchecked by default. The setup preview reports supported, excluded, secret-bearing, mapping-required, and pending-activation counts. Raw secret values, vault keys, and backup passwords never cross the renderer boundary.</p><p>The host owns an immediate startup check, a 30-second local-change debounce, and a bounded five-minute remote poll when automatic sync is enabled. Failed network attempts use bounded exponential retry with jitter; paused, locked, incompatible, authentication, and wrong-password states do not spin retry loops. The worker is cancelled with the host; there is no extra daemon after the app exits.</p><h2 id="_6-durability-and-compatibility" tabindex="-1">6. Durability and compatibility <a class="header-anchor" href="#_6-durability-and-compatibility" aria-label="Permalink to &quot;6. Durability and compatibility&quot;">​</a></h2><p>Sync configuration is stored in the host <code>kv</code> namespace. The vault key and WebDAV password use the existing host secret store. Local base and pending bundles are encrypted and replaced through temp-file rename. A malformed or unauthenticated local bundle is an error, not an empty state. Backup format versioning is independent from the SQLite schema; a newer format is rejected without truncating the local representation.</p><p>Strict remote history retains the newest 30 reachable logical revisions and protects the current head, merge base, pending conflict references, and recovery points; unreferenced objects are deleted only after a grace period and successful publication. Compatibility mode retains immutable history because there is no safe cross-device cleanup acknowledgement. Restore publishes a new revision after a pre-restore encrypted local recovery point is written. A vault-password change CAS-updates the wrapped-key header in strict mode; in compatibility mode it uses an unconditional write followed by authenticated readback. Neither mode changes the vault data key; copied old keys are not cryptographically revoked, so device removal is not treated as revocation.</p><p>Project-group mappings accept ordered multi-root bindings and preserve the primary root invariant. WebDAV tests use an in-process fixture for conditional creation, strong-ETag updates, stale-writer rejection, empty-vault races, and weak-ETag rejection. The remaining availability limitation is inherent: a fresh device without trusted head history cannot prove that a malicious server returned the newest valid backup.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("spec/03-runtime/22-config-sync.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _22ConfigSync = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _22ConfigSync as default
};
