import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0240: Independent session discovery and navigable collaboration projections","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0240-independent-session-discovery-and-navigable-projections.md","filePath":"adr/0240-independent-session-discovery-and-navigable-projections.md","lastUpdated":1789314340000}');
const _sfc_main = { name: "adr/0240-independent-session-discovery-and-navigable-projections.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0240-independent-session-discovery-and-navigable-collaboration-projections" tabindex="-1">ADR 0240: Independent session discovery and navigable collaboration projections <a class="header-anchor" href="#adr-0240-independent-session-discovery-and-navigable-collaboration-projections" aria-label="Permalink to &quot;ADR 0240: Independent session discovery and navigable collaboration projections&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-13</li><li>Decision: D410</li><li>Amends: ADR 0239</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The host-owned collaboration ledger already permits a delivery between any two existing Agent Session IDs, but Session Orchestrator&#39;s private recent-reference store is not an authority for discovering those IDs. A session created through the normal New Task flow can therefore be a valid communication target while remaining invisible to <code>SessionTask.list</code>.</p><p>The sidebar collaboration projection also needs to explain durable creation provenance without exposing complete transcripts, and the existing hover card must let the user move between related sessions.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>The reviewed <code>desktop.control</code> catalog gains the additive read operation <code>session/collaboration/list</code>. It returns at most 100 non-deleted Agent sessions with their real Session IDs, titles, status, updated time, readable provider and model labels, and bounded creation links. It does not return project paths, credentials, transcripts, or message previews. <code>send</code> remains the existing authenticated mutation path and accepts any existing Agent Session ID; it does not create a worker relationship or replace the target session.</p><p>The host sidebar projection adds readable <code>providerName</code> and <code>modelName</code> fields plus at most eight <code>createdSessions</code> references. <code>createdBySession</code> and <code>createdSessions</code> describe only durable Session Orchestrator creation links; ordinary independently created sessions do not receive a fabricated creator. The renderer renders these references as native keyboard-focusable buttons. An activation opens the referenced durable session through the existing store selection path and focuses the Composer.</p><p>The hover card remains on-demand and bounded. Its interactive portal owns the pointer/focus grace period so moving from the row to a related-session button does not dismiss the card before the click. Renderer code remains read-only; Rust remains authoritative for persistence and provenance, and Electron remains the thin projection/orchestration layer.</p><h2 id="compatibility-and-validation" tabindex="-1">Compatibility and validation <a class="header-anchor" href="#compatibility-and-validation" aria-label="Permalink to &quot;Compatibility and validation&quot;">​</a></h2><p>The new operation and projection fields are additive. Existing <code>status</code>, <code>send</code>, <code>spawn</code>, result, cancellation, private references, and session data are unchanged. Session Orchestrator retains the legacy <code>workers</code> field in <code>list</code> while adding the host-backed <code>sessions</code> directory, so callers that used the old recent-reference compatibility behavior continue to work.</p><p>The list is restricted to Agent sessions because the host already rejects delivery to Plan and Goal sessions. Host-core projection tests cover readable provider/model names, creator/created-session links, and independent session discovery. Plugin tests cover list discovery and bidirectional delivery. The relevant live journeys are tracked by <code>E2E-SESSION-independent-top-level-communication</code> and <code>E2E-SESSION-hover-card-model-and-links</code>.</p><h2 id="amendment-2026-09-13-reference-availability" tabindex="-1">Amendment (2026-09-13): reference availability <a class="header-anchor" href="#amendment-2026-09-13-reference-availability" aria-label="Permalink to &quot;Amendment (2026-09-13): reference availability&quot;">​</a></h2><p><code>SessionReference</code> gained an optional <code>available</code> field. The host reports <code>available: false</code> for a <code>createdBySession</code>, <code>createdSessions</code>, <code>currentTask.senderSession</code>, or <code>recentExchanges[].peer</code> reference whose session row is deleted or absent, and falls back to the Session ID as the title. The renderer renders an unavailable reference as text and offers no navigation, and activating a reference whose session no longer exists reports a visible error instead of committing an empty selection. The field is additive and optional, so existing callers and persisted data are unaffected.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0240-independent-session-discovery-and-navigable-projections.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0240IndependentSessionDiscoveryAndNavigableProjections = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0240IndependentSessionDiscoveryAndNavigableProjections as default
};
