import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0041: Bound host runtime resources and decouple message persistence","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0041-bounded-host-runtime-and-persistence-outbox.md","filePath":"adr/0041-bounded-host-runtime-and-persistence-outbox.md","lastUpdated":1789800041000}');
const _sfc_main = { name: "adr/0041-bounded-host-runtime-and-persistence-outbox.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0041-bound-host-runtime-resources-and-decouple-message-persistence" tabindex="-1">ADR 0041: Bound host runtime resources and decouple message persistence <a class="header-anchor" href="#adr-0041-bound-host-runtime-resources-and-decouple-message-persistence" aria-label="Permalink to &quot;ADR 0041: Bound host runtime resources and decouple message persistence&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-08-01</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The Rust host previously spawned an unbounded Tokio task for every RPC and could start an unbounded number of shell processes. During a local burst this produced <code>Resource temporarily unavailable</code>, followed by concurrent host restarts and writes to a destroyed stdio pipe. The resulting persistence errors obscured the original resource failure.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Host-core owns a bounded admission budget for RPC and tool execution. Tool classes have independent global limits, every session has a limit, and the queue is finite. Transient process-spawn resource failures receive bounded backoff; started commands are never automatically retried. Timed-out children are reaped before their permits are released.</p><p>Electron host supervision is single-flight and generation-aware. A stale host generation cannot issue notifications or accept new RPC writes. Assistant and tool message appends pass through an Electron-main-owned, file-backed outbox and are flushed sequentially after a successful host handshake. The handshake <strong>awaits</strong> that drain before the host is advertised ready, so a cold <code>session.get</code> cannot race a queued assistant/tool row (D327). Host-side message append is idempotent by message id. A colliding id that already belongs to another session is remapped to <code>{sessionId}:{id}</code> before the JSONL write (D444). The outbox treats <code>UNIQUE constraint failed: messages.id</code> as an ack, not a pause. A permanently rejected append (<code>PERMISSION_DENIED:</code> provenance or permission on that row) is dropped the same way so one poison head cannot fill the 1024-entry cap and discard every later row (D597).</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A burst is rejected or backpressured instead of exhausting process resources.</li><li>One failed host does not create a restart storm or hundreds of stale-pipe errors.</li><li>SQLite ownership remains exclusively in host-core.</li><li>The application data directory gains one small recovery outbox file.</li><li>A missing sessions row is restored from the live JSONL (or created as a stub under the same id) so a queued outbox can drain; <code>session.delete</code> drops that session&#39;s outbox entries so a stub cannot resurrect a deleted conversation (D318).</li><li>Tool capacity becomes observable through <code>app.health</code> and structured logs.</li></ul><h2 id="alternatives-rejected" tabindex="-1">Alternatives rejected <a class="header-anchor" href="#alternatives-rejected" aria-label="Permalink to &quot;Alternatives rejected&quot;">​</a></h2><ul><li>Raising the restart count: masks the resource leak and amplifies the crash loop.</li><li>A semaphore only around <code>Bash</code>: leaves RPC, plugin, read, and persistence fan-out unbounded.</li><li>In-memory-only persistence buffering: loses messages if Electron exits before host recovery.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0041-bounded-host-runtime-and-persistence-outbox.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0041BoundedHostRuntimeAndPersistenceOutbox = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0041BoundedHostRuntimeAndPersistenceOutbox as default
};
