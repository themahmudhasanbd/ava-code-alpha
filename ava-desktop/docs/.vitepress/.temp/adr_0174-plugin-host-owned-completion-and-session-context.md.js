import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0174: Host-owned plugin completions and session context","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0174-plugin-host-owned-completion-and-session-context.md","filePath":"adr/0174-plugin-host-owned-completion-and-session-context.md","lastUpdated":1789136575000}');
const _sfc_main = { name: "adr/0174-plugin-host-owned-completion-and-session-context.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0174-host-owned-plugin-completions-and-session-context" tabindex="-1">ADR 0174: Host-owned plugin completions and session context <a class="header-anchor" href="#adr-0174-host-owned-plugin-completions-and-session-context" aria-label="Permalink to &quot;ADR 0174: Host-owned plugin completions and session context&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-07</li><li>Deciders: PI-Desktop core</li><li>Related: D019, D015, D018, D336, ADR 0005, ADR 0008, ADR 0121, ADR 0170, <code>07-plugins/03-plugin-api.md</code>, <code>07-plugins/13-plugin-permissions-matrix.md</code></li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Pi CLI extensions can let an executor model call a zero-parameter reviewer tool. The host serializes the resolved LLM context and runs a one-shot completion against a stronger reviewer using the user&#39;s existing credentials.</p><p>PI-Desktop plugins can already register tools, commands, settings, and skills, but they cannot:</p><ul><li>list the models the user has authenticated</li><li>read the current session&#39;s model-facing transcript</li><li>spend the user&#39;s provider quota on a side completion</li></ul><p>A plugin that called providers itself would need secrets (forbidden, D018) or <code>net.fetch</code> plus a pasted key. D019 denied session-summary access by default. Composer <code>prompt/enhance</code> already proves a host-owned one-shot path that never hands a secret to the renderer.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><p><strong>Three public plugin APIs</strong>, gated by new permissions:</p><ul><li><code>models.list</code> (medium) — <code>pi.models.list()</code> returns ready <code>(providerId, modelId)</code> rows. No secrets.</li><li><code>session.read</code> (high) — <code>pi.session.getLlmContext()</code> returns a bounded, compaction-aware projection of the <strong>in-flight tool session</strong>. The plugin cannot pass a session id; identity comes from <code>plugins.execute</code> (same rule as D333).</li><li><code>agent.complete</code> (high) — <code>pi.agent.complete({ modelKey, thinkingLevel, system, messages, includeSessionContext })</code> runs a host-owned one-shot completion with <code>tools: []</code> through the same credential resolver as agent turns and prompt enhancement. <code>includeSessionContext: true</code> also requires <code>session.read</code> and an in-flight tool session.</li></ul></li><li><p><strong>Host owns credentials and the wire call.</strong> The plugin process never receives an API key, OAuth refresh token, or short-lived <code>ModelAuth</code>. Completions reuse <code>resolveAgentRuntimeLaunch</code> plus the runtime one-shot helper. Secrets stay in Electron main.</p></li><li><p><strong>Session context is a projection, not a dump.</strong> Subagent rows are omitted. An in-flight call of the plugin&#39;s own tool is stripped from the tail. Compaction summaries replace pre-checkpoint history. Tool results are truncated. The payload is capped. Audit logs record plugin id, model key, sizes, and usage — never transcript text.</p></li><li><p><strong>Rate and size brakes.</strong> Eight <code>agent.complete</code> calls per plugin per rolling 60s. System prompt ≤ 32 KiB. Combined messages ≤ 200k characters. Completion budget 90s, under the 110s plugin-tool timeout.</p></li><li><p><strong><code>session:modelChanged</code> is a host event</strong> pushed after a successful <code>session.configure</code> that changes provider, model, or thinking level.</p></li><li><p><strong>D019 is amended, not withdrawn.</strong> Session content remains denied until <code>session.read</code> is declared and granted. Tool-exec context may include <code>modelKey</code> / <code>thinkingLevel</code> without that permission because those are session configuration, not transcript.</p></li><li><p><strong>Bundled Advisor is temporarily not shipped.</strong> The public APIs remain available to explicitly installed plugins, but PI-Desktop does not bundle a first-party reviewer command, panel, skill, or agent tool for now.</p></li></ol><p>No host-protocol or storage schema bump. Completions are Electron-local, like <code>prompt/enhance</code>.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Third-party plugins can build reviewer / second-opinion tools without holding keys.</li><li><code>session.read</code> + <code>agent.complete</code> is a legitimate conversation-exfil channel to another model the user already pays for. Install UI must show both permissions and their help text.</li><li>Plugin tools keep the D015 prefix. Plan and Goal still hard-deny plugin tools.</li><li>One-shot completion spend is audited on the plugin call and is not merged into the parent assistant usage chip (ADR 0171).</li></ul><h2 id="alternatives-rejected" tabindex="-1">Alternatives rejected <a class="header-anchor" href="#alternatives-rejected" aria-label="Permalink to &quot;Alternatives rejected&quot;">​</a></h2><h3 id="load-the-npm-pi-cli-extension-in-the-sidecar" tabindex="-1">Load the npm Pi CLI extension in the sidecar <a class="header-anchor" href="#load-the-npm-pi-cli-extension-in-the-sidecar" aria-label="Permalink to &quot;Load the npm Pi CLI extension in the sidecar&quot;">​</a></h3><p>The sidecar is not the Pi TUI extension host. The CLI reviewer command requires a TTY and fails under RPC.</p><h3 id="let-the-plugin-call-providers-through-net-fetch" tabindex="-1">Let the plugin call providers through <code>net.fetch</code> <a class="header-anchor" href="#let-the-plugin-call-providers-through-net-fetch" aria-label="Permalink to &quot;Let the plugin call providers through \`net.fetch\`&quot;">​</a></h3><p>Would require secrets in plugin settings (D018) or a user-pasted key, and would skip host retry, OAuth, and audit.</p><h3 id="first-party-reviewer-tool-instead-of-a-plugin" tabindex="-1">First-party reviewer tool instead of a plugin <a class="header-anchor" href="#first-party-reviewer-tool-instead-of-a-plugin" aria-label="Permalink to &quot;First-party reviewer tool instead of a plugin&quot;">​</a></h3><p>Deferred for now: the bundled reviewer experience is temporarily removed while the public host APIs remain available for explicitly installed plugins.</p><h3 id="give-plugins-an-arbitrary-sessionid-argument" tabindex="-1">Give plugins an arbitrary <code>sessionId</code> argument <a class="header-anchor" href="#give-plugins-an-arbitrary-sessionid-argument" aria-label="Permalink to &quot;Give plugins an arbitrary \`sessionId\` argument&quot;">​</a></h3><p>Rejected: a plugin could read every open conversation. In-flight tool identity is the same bound used for <code>pi.browser.*</code>.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0174-plugin-host-owned-completion-and-session-context.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0174PluginHostOwnedCompletionAndSessionContext = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0174PluginHostOwnedCompletionAndSessionContext as default
};
