import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0044: Session-bound project instruction preflight","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0044-session-bound-project-instruction-preflight.md","filePath":"adr/0044-session-bound-project-instruction-preflight.md","lastUpdated":1785652476000}');
const _sfc_main = { name: "adr/0044-session-bound-project-instruction-preflight.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0044-session-bound-project-instruction-preflight" tabindex="-1">ADR 0044: Session-bound project instruction preflight <a class="header-anchor" href="#adr-0044-session-bound-project-instruction-preflight" aria-label="Permalink to &quot;ADR 0044: Session-bound project instruction preflight&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-08-02</li><li>Related: <a href="./0037-project-instruction-chain">ADR 0037</a> · <a href="./../spec/03-runtime/02-agent-runtime">Agent runtime</a> · <a href="./../spec/03-runtime/09-logging-and-observability">Logging</a></li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Path-scoped instruction loading ran before every file tool. The resolver lived in Electron main, but it first called host-core <code>session.get</code> to rediscover the project root. A slow or congested host therefore delayed an otherwise local file operation and made the delay appear in the generic host round-trip measurement. Repeating reads in one prompt also repeated the same instruction resolution.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>Electron main derives the project root from the host-owned session record during runtime launch and passes it as launch metadata.</li><li>Electron main registers that root on the sidecar wrapper before each prompt or compaction request. A reverse <code>project.instructions.resolve</code> request uses this binding; any root supplied by sidecar payload is ignored.</li><li>The sidecar keeps a per-prompt claim map keyed by the registered root and target directory. A successful result and a best-effort fallback are both reused for later file tools in that prompt. The map is cleared before the next prompt, so instruction edits are visible across messages.</li><li>Tool timing reports instruction preflight separately from <code>hostRttMs</code>, with cache-hit and base-fallback markers.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>File-tool preflight no longer adds a host-core <code>session.get</code> round trip.</li><li>Repeated file tools in one directory avoid duplicate resolver IPC and file discovery while preserving cross-message freshness.</li><li>The main-process containment boundary from ADR 0037 remains explicit: the sidecar selects only a target path, while Electron main owns the root.</li><li>A resolver timeout or host failure remains best-effort and falls back to the base chain without carrying a sibling directory&#39;s instructions.</li></ul><h2 id="alternatives-rejected" tabindex="-1">Alternatives rejected <a class="header-anchor" href="#alternatives-rejected" aria-label="Permalink to &quot;Alternatives rejected&quot;">​</a></h2><ul><li><strong>Cache instruction chains across all prompts:</strong> risks stale rules after an instruction file changes and requires file-watch or stat invalidation.</li><li><strong>Trust a project root sent by the sidecar:</strong> weakens the session-bound containment boundary and lets model-directed input choose the scan root.</li><li><strong>Keep the per-file <code>session.get</code>:</strong> preserves an avoidable host dependency on the hottest file-tool preflight path.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0044-session-bound-project-instruction-preflight.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0044SessionBoundProjectInstructionPreflight = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0044SessionBoundProjectInstructionPreflight as default
};
