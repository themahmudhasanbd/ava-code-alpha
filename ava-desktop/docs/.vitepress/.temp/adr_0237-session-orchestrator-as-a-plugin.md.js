import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0237: Keep Session Orchestration in an Official Plugin","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0237-session-orchestrator-as-a-plugin.md","filePath":"adr/0237-session-orchestrator-as-a-plugin.md","lastUpdated":1789292061000}');
const _sfc_main = { name: "adr/0237-session-orchestrator-as-a-plugin.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0237-keep-session-orchestration-in-an-official-plugin" tabindex="-1">ADR 0237: Keep Session Orchestration in an Official Plugin <a class="header-anchor" href="#adr-0237-keep-session-orchestration-in-an-official-plugin" aria-label="Permalink to &quot;ADR 0237: Keep Session Orchestration in an Official Plugin&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-13</li><li>Updated: 2026-09-13</li><li>Decision: D391</li><li>Related: ADR 0200, ADR 0203, ADR 0208, ADR 0062, ADR 0089</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Some tasks benefit from several independent Agents working at the same time, but the existing <code>Task</code> family is intentionally a bounded in-process subagent system. A new orchestration feature must not create a second session store, bypass plugin permissions, or make the renderer an Agent runtime.</p><p>The public plugin SDK already exposes Agent tools and the reviewed <code>desktop.control</code> gateway. It does not expose live session creation or Agent lifecycle events, so the first implementation needs a small composition layer without moving orchestration into host-core.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Ship <code>pi.session-orchestrator</code> as an ordinary marketplace plugin. It registers the <code>SessionTask</code> Agent Tool with <code>spawn</code>, <code>send</code>, <code>supervise</code>, <code>status</code>, <code>wait</code>, <code>result</code>, <code>accept</code>, <code>cancel</code>, and <code>list</code> actions.</p><ul><li><code>spawn</code> uses <code>session/create</code> followed by <code>agent/prompt</code>, creating a new durable session with no copied transcript. It inherits the parent project, provider/model, thinking level, and permission mode.</li><li><code>send</code> reuses the recorded worker session. <code>cancel</code> uses <code>agent/abort</code> and never deletes the session.</li><li>The plugin persists only the parent/worker relationship, task, status, timestamp, and bounded report in its private data directory. The host-owned worker transcript remains the source of truth. The canonical worker identity in the tool response and relationship store is the original durable <code>sessionId</code> returned by <code>session/create</code>; 0.2.x <code>workerSessionId</code> settings are migrated on load and no second Work ID is created.</li><li>A parent may control only Worker Session IDs recorded under its own session id. A <code>send</code> or <code>supervise</code> follow-up addresses those same Session IDs, so a child retains its own context without creating a replacement session. Worker sessions cannot use the tool to create or control more workers. Active work is capped at four workers per parent and sixteen across the plugin.</li><li>Until a lifecycle subscription exists, <code>wait</code> performs one-second lightweight status polling with a 25-second default and 45-second maximum timeout. Each desktop read is bounded at five seconds, report transcript reads are deferred until a worker is no longer running, and a timeout returns <code>timedOut</code> so the plugin tool does not occupy the host deadline indefinitely. The Agents panel uses the same status-only refresh path.</li><li>A simple <code>Agents</code> work-panel view lists workers and offers status refresh, Open Session, and Stop.</li></ul><p>Two additive host primitives support these boundaries:</p><ol><li><code>session/create</code> accepts optional <code>inheritPermissionFromSessionId</code>. For a plugin-originated call, the desktop gateway binds that id to the current Agent tool session. The host then copies the existing parent&#39;s persisted permission mode while holding the state lock; callers never submit an arbitrary worker permission mode, and omission retains the existing <code>inherit</code> default.</li><li>The reviewed desktop catalog gains <code>session/open</code>, which validates and selects an existing durable session. Plugin-originated create/prompt calls refresh the renderer without stealing the parent&#39;s active session; explicit <code>session/open</code> is the only navigation action.</li></ol><p>No Rust schema migration, MCP self-call, MCP token access, A2A channel, message bus, DAG, or change to <code>Task</code>, <code>TaskWait</code>, <code>TaskList</code>, or <code>TaskStop</code> is added.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><p>The parent can fan out real durable workers while every worker stays visible and inspectable in the normal session UI. Existing session and subagent behavior remains intact, and plugin data can restore the relationship list after a plugin or app restart.</p><p>The first version does not receive lifecycle events and therefore has bounded polling latency. The plugin also has a four-worker limit and does not provide unscoped arbitrary-session control or worker-to-worker messaging. A dangerous host operation is never used for orchestration; any future extension must preserve the existing desktop-control permission and native-consent boundary.</p><h2 id="verification" tabindex="-1">Verification <a class="header-anchor" href="#verification" aria-label="Permalink to &quot;Verification&quot;">​</a></h2><p>The plugin runtime integration test covers three parallel real-session requests, parent scoping, persistence across reload, canonical Session ID routing, same-session context reuse, bounded report extraction, timeout-safe status polling, legacy settings migration, and cancellation without deletion. Host-core tests cover the optional permission-inheritance input. The E2E plan records the live-provider journey as <code>E2E-PLUGIN-session-orchestrator-real-workers</code>.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0237-session-orchestrator-as-a-plugin.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0237SessionOrchestratorAsAPlugin = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0237SessionOrchestratorAsAPlugin as default
};
