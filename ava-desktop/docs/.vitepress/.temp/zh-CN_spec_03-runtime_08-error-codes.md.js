import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"08. 错误代码","description":"","frontmatter":{},"headers":[],"relativePath":"zh-CN/spec/03-runtime/08-error-codes.md","filePath":"zh-CN/spec/03-runtime/08-error-codes.md","lastUpdated":1790043493000}');
const _sfc_main = { name: "zh-CN/spec/03-runtime/08-error-codes.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="_08-错误代码" tabindex="-1">08. 错误代码 <a class="header-anchor" href="#_08-错误代码" aria-label="Permalink to &quot;08. 错误代码&quot;">​</a></h1><blockquote><p><strong>翻译说明：</strong> 本页是与 <a href="/spec/03-runtime/08-error-codes">英文源规格</a> 一一对应的机器辅助翻译。代码、协议字段和标识符保持原文；如翻译与英文源事实有歧义，以英文版本为准。</p></blockquote><blockquote><p>事实来源：<code>packages/shared/src/errors.ts</code> (<code>ErrorCodes</code>)。代码在 §3.7 被保留（在发布之前记录）；其他一切都是实时的。</p></blockquote><h2 id="_1-goal" tabindex="-1">1. Goal <a class="header-anchor" href="#_1-goal" aria-label="Permalink to &quot;1. Goal&quot;">​</a></h2><p>提供一种稳定的错误词汇表：</p><ul><li>Renderer 用户界面</li><li>Electron IPC</li><li>Rust 主机 RPC</li><li>Node pi sidecar 桥</li></ul><h2 id="_2-错误对象" tabindex="-1">2. 错误对象 <a class="header-anchor" href="#_2-错误对象" aria-label="Permalink to &quot;2. 错误对象&quot;">​</a></h2><div class="language-ts vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">type</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> AppError</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  code</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">            // stable machine code, e.g. TOOL_DENIED</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  message</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">         // English UI/default message</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  details</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> unknown</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  retriable</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> boolean</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  source</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;renderer&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;electron&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;host&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;agent&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;plugin&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  causeCode</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">      // nested/transport code if mapped</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  traceId</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br></div></div><p>规则：</p><ol><li><code>code</code> 一旦发布就不可更改</li><li><code>message</code> 为英文源文本（i18n 键可单独映射）</li><li>UI 应该更喜欢从 <code>code</code> 派生的 i18n 密钥（如果可用）</li></ol><p>桌面测试套件（<code>apps/desktop/test/error-code-registry.test.mjs</code>）会验证 <code>ErrorCodes</code> 的每一项都出现在本文档中，并且 host-core 从其 RPC 调度器和原生 工具发出的每一个 <code>errorCode</code> 都已注册；§3.7 中的保留代码在有实现发出它们 之前，刻意不出现在 <code>ErrorCodes</code> 里。</p><h2 id="_3-代码注册" tabindex="-1">3. 代码注册 <a class="header-anchor" href="#_3-代码注册" aria-label="Permalink to &quot;3. 代码注册&quot;">​</a></h2><h3 id="_3-1-应用程序-协议" tabindex="-1">3. 1 应用程序/协议 <a class="header-anchor" href="#_3-1-应用程序-协议" aria-label="Permalink to &quot;3. 1 应用程序/协议&quot;">​</a></h3><table tabindex="0"><thead><tr><th>代码</th><th>可重审的</th><th>意义</th></tr></thead><tbody><tr><td><code>PROTOCOL_MISMATCH</code></td><td>不</td><td>handshake/protocol 版本不匹配</td></tr><tr><td><code>HOST_UNAVAILABLE</code></td><td>是的</td><td>Rust 主机不是 running/reachable</td></tr><tr><td><code>HOST_OVERLOADED</code></td><td>是的</td><td>绑定主机 RPC/tool 容量已满；背压后重试</td></tr><tr><td><code>AGENT_UNAVAILABLE</code></td><td>是的</td><td>pi sidecar 不是 running/reachable</td></tr><tr><td><code>APP_DEGRADED</code></td><td>是的</td><td>应用程序以有限的功能运行</td></tr><tr><td><code>INTERNAL</code></td><td>也许</td><td>意外的内部故障</td></tr><tr><td><code>INVALID_ARGUMENT</code></td><td>不</td><td>请求 schema/args 无效，包括错误 file/directory 类型的本机工具路径</td></tr><tr><td><code>INVALID_PARAMS</code></td><td>不</td><td>host-core RPC 参数校验失败（数字码 <code>1002</code>）；sidecar 和渲染器原样透传</td></tr><tr><td><code>UNAUTHORIZED</code></td><td>不</td><td>capability/auth 边界拒绝呼叫</td></tr><tr><td><code>NOT_FOUND</code></td><td>不</td><td>未找到实体</td></tr><tr><td><code>SESSION_NOT_FOUND</code></td><td>不</td><td>会话作用域的 RPC（包括 <code>tools.execute</code>）点名了主机没有的会话；未知 id 永远不会继承全局工作区</td></tr><tr><td><code>CONFLICT</code></td><td>也许</td><td>状态冲突/资源繁忙</td></tr><tr><td><code>UNSUPPORTED</code></td><td>不</td><td>该操作在此界面上没有实现，例如没有桌面窗口时的受信任扩展提示（规格 16 §9）</td></tr><tr><td><code>FORBIDDEN</code></td><td>不</td><td>RACP：主体的角色不允许该方法（规格 19 §13）</td></tr><tr><td><code>METHOD_NOT_FOUND</code></td><td>不</td><td>RACP：此 Host 上未知的方法，例如没有 Gateway 时的 <code>host/list</code></td></tr><tr><td><code>IDEMPOTENCY_CONFLICT</code></td><td>不</td><td>队列或回合的幂等 key 以不同输入被重用（D386）</td></tr><tr><td><code>CURSOR_EXPIRED</code></td><td>不</td><td>RACP：回放游标早于保留窗口；从快照重新订阅</td></tr><tr><td><code>CLIENT_TOO_SLOW</code></td><td>是的</td><td>RACP：客户端落后于事件流并被断开</td></tr><tr><td><code>APPROVAL_EXPIRED</code></td><td>不</td><td>RACP：审批期限在回答到达前已过</td></tr><tr><td><code>APPROVAL_STALE</code></td><td>不</td><td>RACP：审批已被处理或属于更早的回合</td></tr><tr><td><code>PAYLOAD_TOO_LARGE</code></td><td>不</td><td>RACP：帧超过协商的大小上限</td></tr><tr><td><code>TIMEOUT</code></td><td>是的</td><td>通用超时</td></tr><tr><td><code>NETWORK_POLICY_BLOCKED</code></td><td>不</td><td>主进程公网策略守卫拒绝了一次抓取,因为它<strong>判定了</strong>目标：URL 未通过公网 HTTPS 语法检查,或本地 DNS 解析返回了策略判定为非公网的地址——其中包括本地代理生成的 fake-IP 占位地址（ADR 0243）。仅桌面端使用；拒绝即判定,因此在地址改变前重试不会成功。本地解析完全没有返回答案时改用 <code>NETWORK_RESOLVE_FAILED</code>（issue #419）。</td></tr><tr><td><code>NETWORK_RESOLVE_FAILED</code></td><td>是的</td><td>主进程公网策略守卫无法判定目标主机：本地 DNS 解析没有返回答案,或在返回前抛错。请求仍与策略拒绝一样被拒,但没有判定任何地址,因此任何界面或日志都不得把它描述成地址校验的判定结果。与 <code>NETWORK_ERROR</code> 不同,后者是请求本身的失败。可重试：当解析器或代理开始应答同一主机时,同一请求即可成功（ADR 0243,issue #419）。</td></tr><tr><td><code>HOST_SHUTTING_DOWN</code></td><td>是的</td><td>主机收到 EOF 正在排空；调用被拒绝而不是被启动</td></tr><tr><td><code>RATE_LIMITED</code></td><td>是的</td><td>某个按调用方计的主机预算（插件会话导入、批量操作）在其窗口内被超出</td></tr><tr><td><code>LIMIT_EXCEEDED</code></td><td>不</td><td>载荷超过了固定的主机上限（条目数、字节数）并被拒绝</td></tr><tr><td><code>CONFIG_SYNC_INVALID</code></td><td>不</td><td>同步配置、密码、路径、请求或审批输入无效</td></tr><tr><td><code>CONFIG_SYNC_LOCKED</code></td><td>不</td><td>本地加密同步 vault 尚未解锁</td></tr><tr><td><code>CONFIG_SYNC_UNSUPPORTED</code></td><td>不</td><td>vault 格式或 WebDAV 服务器能力不受支持</td></tr><tr><td><code>CONFIG_SYNC_REMOTE</code></td><td>也许</td><td>远端 WebDAV 对象、认证、配额或可用性失败</td></tr><tr><td><code>CONFIG_SYNC_CONFLICT</code></td><td>也许</td><td>远端 head、vault 身份或审批 digest 冲突</td></tr><tr><td><code>CONFIG_SYNC_CRYPTO</code></td><td>不</td><td>认证加密、对象身份或密文校验失败</td></tr><tr><td><code>CONFIG_SYNC_MAPPING_REQUIRED</code></td><td>不</td><td>导入的项目作用域配置需要明确的本地文件夹/项目组映射</td></tr><tr><td><code>CONFIG_SYNC_LIMIT_EXCEEDED</code></td><td>不</td><td>加密同步状态超过实体、对象、资源、归档或解压上限</td></tr></tbody></table><p><code>HOST_UNAVAILABLE</code> 是为丢失或损坏的主机 process/transport 保留的， 不是普通的入学压力。 RPC 容量返回 <code>HOST_OVERLOADED</code>，并且 由于操作系统暂时无法启动而无法启动的 shell 进程资源返回 <code>PROCESS_RESOURCE_EXHAUSTED</code>。主机核的控制 stdio 与 Tokio 的动态阻塞池隔离，因此后一种情况 不会将临时线程压力转变为主机进程退出。</p><h3 id="_3-2-agent-会话" tabindex="-1">3. 2 Agent/会话 <a class="header-anchor" href="#_3-2-agent-会话" aria-label="Permalink to &quot;3. 2 Agent/会话&quot;">​</a></h3><table tabindex="0"><thead><tr><th>代码</th><th>可重审的</th><th>意义</th></tr></thead><tbody><tr><td><code>AGENT_BUSY</code></td><td>不</td><td>会话已经有活动轮次；父级终态错误后的残留子智能体不会让会话保持忙碌（D352）</td></tr><tr><td><code>AGENT_NOT_FOUND</code></td><td>不</td><td>会话丢失</td></tr><tr><td><code>TURN_NOT_FOUND</code></td><td>不</td><td>使 id 无效</td></tr><tr><td><code>TURN_ABORTED</code></td><td>不</td><td>回合被 user/system 中止</td></tr><tr><td><code>MODEL_NOT_CONFIGURED</code></td><td>不</td><td>未选择可用模型，或提供商因未知而拒绝所选模型</td></tr><tr><td><code>PROVIDER_ERROR</code></td><td>是的</td><td>上游提供商故障；可重试的故障（5xx 网关）最多获得四次同回合重试，格式错误的 400/422 请求是终止的</td></tr><tr><td><code>PROVIDER_UNAUTHORIZED</code></td><td>不</td><td>bad/missing 提供商凭证</td></tr><tr><td><code>PROVIDER_RATE_LIMITED</code></td><td>是的</td><td>供应商费率有限</td></tr><tr><td><code>CONTEXT_TOO_LARGE</code></td><td>不</td><td>恢复后 prompt/context 仍超出安全模型预算、发生第二个提供程序溢出或禁用自动恢复</td></tr><tr><td><code>CONTEXT_COMPACTION_FAILED</code></td><td>不</td><td>自动保留尾部恢复无法准备、持久或适合检查点，或手动检查点摘要生成/持久追加失败；受保护的下一个提供程序请求不会启动</td></tr><tr><td><code>STREAM_FAILED</code></td><td>是的</td><td>提供程序流在完整响应之前终止、提前关闭或以其他方式结束；最多四次同回合重试可能会在终止事件之前发生</td></tr><tr><td><code>EMPTY_MODEL_RESPONSE</code></td><td>是的</td><td>模型在没有工具调用且没有可见文本的情况下结束了两次：一次是流式传输，一次是在自动重新运行后；对 Host 账本完成通知的第一条回复除外（规范 02-agent-runtime §5e、D446）</td></tr><tr><td><code>PROMPT_ENHANCEMENT_EMPTY</code></td><td>不</td><td>一次性增强模型没有返回任何文本</td></tr><tr><td><code>SPEECH_NOT_CONFIGURED</code></td><td>不</td><td>设置里没有绑定转写或朗读</td></tr><tr><td><code>SPEECH_PROTOCOL_UNSUPPORTED</code></td><td>不</td><td>语音协议未知或不支持该角色</td></tr><tr><td><code>SPEECH_INPUT_TOO_LARGE</code></td><td>不</td><td>语音输入超过 25 MB</td></tr><tr><td><code>SUBAGENT_IDLE_TIMEOUT</code></td><td>不</td><td>已撤回（D328）：空闲看门狗不再武装；代码仅为已存储结果保留</td></tr><tr><td><code>SUBAGENT_DURATION_TIMEOUT</code></td><td>不</td><td>已撤回（D328）：时长看门狗不再武装；代码仅为已存储结果保留</td></tr><tr><td><code>SUBAGENT_CONTEXT_OVERFLOW</code></td><td>不</td><td>委派自身的模型上下文超出其安全预算，自动的回合边界压缩与仅保留任务简报和最近消息的降级重试都没能把它带回限制以内；该失败给出可执行的恢复方式，而不是提供商的溢出文本</td></tr></tbody></table><h3 id="_3-3-工作空间-工具-权限" tabindex="-1">3. 3 工作空间/工具/权限 <a class="header-anchor" href="#_3-3-工作空间-工具-权限" aria-label="Permalink to &quot;3. 3 工作空间/工具/权限&quot;">​</a></h3><table tabindex="0"><thead><tr><th>代码</th><th>可重审的</th><th>意义</th></tr></thead><tbody><tr><td><code>WORKSPACE_REQUIRED</code></td><td>不</td><td>无工作空间限制</td></tr><tr><td><code>PATH_OUTSIDE_WORKSPACE</code></td><td>不</td><td>在显式外部路径权限决策之前路径逃逸沙箱，或提示词附件位于其会话 scratch/project/attachment 根目录之外</td></tr><tr><td><code>WORKSPACE_PATH_DENIED</code></td><td>不</td><td>显式的 <code>Read</code>/<code>Write</code>/<code>Edit</code> 路径命中了始终开启的安全拒绝名单（私钥、<code>.env</code> 文件、凭证包、<code>.git/objects</code>）；外部路径授权不会解除它（规格 15 §3）</td></tr><tr><td><code>READ_PATH_IS_DIRECTORY</code></td><td>不</td><td><code>Read</code> 拿到的是目录；结果附带一条 <code>Glob</code> 建议</td></tr><tr><td><code>TOOL_BINARY_CONTENT</code></td><td>不</td><td><code>Read</code> 拒绝把二进制文件倾倒进模型上下文</td></tr><tr><td><code>TOOL_NOT_FOUND</code></td><td>不</td><td>未知工具</td></tr><tr><td><code>TOOL_DENIED</code></td><td>不</td><td>权限被拒绝/模式被禁止</td></tr><tr><td><code>TOOL_TIMEOUT</code></td><td>是的</td><td>工具执行超时</td></tr><tr><td><code>TOOL_FAILED</code></td><td>也许</td><td>工具已执行但失败</td></tr><tr><td><code>TOOL_ABORTED</code></td><td>不</td><td>工具在完成前被用户停止或回合中止取消</td></tr><tr><td><code>MUTATION_RETRY_BUDGET_EXHAUSTED</code></td><td>是</td><td>重复保护在同路径 <code>Edit</code> 或 shell patch 反复失败后终止了本轮；携带 <code>details.kind</code>（<code>edit</code> 或 <code>patch-command</code>）与最后一个工具错误代码</td></tr><tr><td><code>PROCESS_RESOURCE_EXHAUSTED</code></td><td>是的</td><td>shell 进程无法启动，因为操作系统暂时耗尽了进程资源</td></tr><tr><td><code>SHELL_NOT_FOUND</code></td><td>不</td><td>目录回退后没有有效的平台 shell 可用；消息承载指引</td></tr><tr><td><code>COMMAND_SHELL_CHANGED</code></td><td>不</td><td>固定的 shell ID 或方言在执行前已更改</td></tr><tr><td><code>COMMAND_SHELL_INVALID</code></td><td>不</td><td>设置提供了未知、不可用或错误的平台 shell ID</td></tr><tr><td><code>PERMISSION_TIMEOUT</code></td><td>不</td><td>权限提示超时（映射为拒绝）</td></tr><tr><td><code>PERMISSION_REQUIRED</code></td><td>不</td><td>等待用户决定</td></tr><tr><td><code>WRITE_DISABLED_IN_PLAN</code></td><td>不</td><td>Write 的契约模式硬拒绝</td></tr><tr><td><code>EDIT_DISABLED_IN_PLAN</code></td><td>不</td><td>编辑的契约模式硬拒绝</td></tr><tr><td><code>PLUGIN_DISABLED_IN_PLAN</code></td><td>不</td><td>每个插件工具的契约模式硬拒绝</td></tr><tr><td><code>TOOL_DISABLED_IN_PLAN</code></td><td>不</td><td>unknown/unlisted 工具的契约模式硬拒绝</td></tr><tr><td><code>PLAN_NOT_ACTIVE</code></td><td>不</td><td>在没有协商合同的情况下运行了提交工具</td></tr><tr><td><code>PLAN_KIND_MISMATCH</code></td><td>不</td><td>Goal 模式下的 <code>SubmitPlan</code>，或 Plan 模式下的 <code>SubmitGoal</code></td></tr><tr><td><code>PLAN_APPROVAL_REQUIRED</code></td><td>不</td><td>SubmitPlan/SubmitGoal 正在等待单独的批准</td></tr><tr><td><code>PLAN_APPROVAL_TIMEOUT</code></td><td>不</td><td>绝对 30 分钟计划批准期限已过</td></tr><tr><td><code>PLAN_APPROVAL_STALE</code></td><td>不</td><td>响应与实时 proposal/session/turn/tool-call/version 不匹配</td></tr><tr><td><code>PLAN_APPROVAL_INTERRUPTED</code></td><td>不</td><td>待批准在中止、崩溃或持久性失败期间关闭</td></tr><tr><td><code>PLAN_ARTIFACT_WRITE_FAILED</code></td><td>不</td><td>主机无法将确切的字节写入新的 <code>.pi/&lt;kind&gt;/*.md</code> 工件</td></tr><tr><td><code>PLAN_EXECUTION_INTERRUPTED</code></td><td>不</td><td>已批准的 queued/running Plan 或 Goal 执行已停止且不重播</td></tr><tr><td><code>PLAN_REQUIRES_INTERACTIVE_SESSION</code></td><td>不</td><td>unattended/scheduled Plan 或 Goal 运行无法请求批准</td></tr><tr><td><code>PLAN_NOT_FOUND</code></td><td>不</td><td>没有审批记录与该提案 id 匹配</td></tr><tr><td><code>PLAN_SESSION_NOT_FOUND</code></td><td>不</td><td>Plan/Goal RPC 点名了主机没有的会话</td></tr><tr><td><code>PLAN_WORKSPACE_REQUIRED</code></td><td>不</td><td>会话没有持久化的项目；临时会话无法进入 Plan 或 Goal</td></tr><tr><td><code>PLAN_ALREADY_ACTIVE</code></td><td>不</td><td>会话已经有一份正在协商的契约</td></tr><tr><td><code>PLAN_ALREADY_PENDING</code></td><td>不</td><td>同一回合的审批仍在等待时又收到了一次提交</td></tr><tr><td><code>PLAN_ALREADY_RESOLVED</code></td><td>不</td><td>第二次 approve/reject 到达了一个已经裁决的审批</td></tr><tr><td><code>PLAN_APPROVAL_CONFLICT</code></td><td>不</td><td>审批记录在版本守卫更新底下被改动</td></tr><tr><td><code>PLAN_INVALID_ACTION</code></td><td>不</td><td>审批响应既不是 <code>approve</code> 也不是 <code>reject</code></td></tr><tr><td><code>PLAN_INVALID_ARGUMENT</code></td><td>不</td><td>submit/resolve 参数校验失败</td></tr><tr><td><code>PLAN_PERMISSION_MODE_REQUIRED</code></td><td>不</td><td>approve 没有选择 <code>ask</code>、<code>accept-edits</code> 或 <code>auto</code></td></tr><tr><td><code>PLAN_PERMISSION_MODE_INVALID</code></td><td>不</td><td>选定的权限模式不是这三者之一</td></tr><tr><td><code>PLAN_MARKDOWN_TOO_LARGE</code></td><td>不</td><td>提交的 Markdown 超出工件大小上限</td></tr><tr><td><code>PLAN_REJECTED</code></td><td>不</td><td>用户拒绝了提案；本轮结束且不执行</td></tr><tr><td><code>PLAN_SUBMIT_FAILED</code></td><td>也许</td><td>主机无法记录提案</td></tr><tr><td><code>PLAN_CONFIGURATION_BLOCKED</code></td><td>不</td><td>提案或执行进行中时 <code>session.configure</code> 被拒绝</td></tr><tr><td><code>PLAN_ARTIFACT_INVALID</code></td><td>不</td><td>检查点工件在执行前未通过校验</td></tr><tr><td><code>PLAN_ARTIFACT_NOT_READY</code></td><td>不</td><td>工件尚未持久写入就被认领执行</td></tr><tr><td><code>PLAN_ARTIFACT_PATH_UNSAFE</code></td><td>不</td><td>工件路径逃逸了 <code>&lt;workspaceRoot&gt;/.pi/&lt;kind&gt;/</code></td></tr><tr><td><code>PLAN_ARTIFACT_COLLISION_LIMIT</code></td><td>不</td><td>主机用尽了唯一的工件名</td></tr><tr><td><code>PLAN_ARTIFACT_HASH_MISMATCH</code></td><td>不</td><td>执行时工件字节与记录的哈希不再一致</td></tr><tr><td><code>PLAN_EXECUTION_ACTIVE</code></td><td>不</td><td>该会话已有一个已批准的执行在运行</td></tr><tr><td><code>PLAN_EXECUTION_NOT_FOUND</code></td><td>不</td><td>没有排队中的执行与认领匹配</td></tr><tr><td><code>PLAN_EXECUTION_ALREADY_CLAIMED</code></td><td>不</td><td>另一个认领者抢先拿走了排队中的执行</td></tr><tr><td><code>PLAN_EXECUTION_STALE</code></td><td>不</td><td>执行 epoch 与当前会话不再匹配</td></tr><tr><td><code>PLAN_EXECUTION_STATUS_INVALID</code></td><td>不</td><td>当前状态不允许该状态迁移</td></tr><tr><td><code>PLAN_EXECUTION_CONFLICT</code></td><td>不</td><td>执行记录在版本守卫更新底下被改动</td></tr><tr><td><code>PLAN_EXECUTION_FAILED</code></td><td>也许</td><td>已批准的执行以错误结束</td></tr><tr><td><code>PLAN_INTERNAL</code></td><td>也许</td><td>没有更细分类的 Plan/Goal 主机失败</td></tr><tr><td><code>WRITE_DISABLED_IN_CHAT</code></td><td>不</td><td>历史遗留（D188 之前的 Chat profile）；为已存储的转录本保留注册，不再发出</td></tr><tr><td><code>BASH_DISABLED_IN_CHAT</code></td><td>不</td><td>历史遗留（D188 之前的 Chat profile）；为已存储的转录本保留注册，不再发出</td></tr></tbody></table><p><code>_IN_PLAN</code> 后缀和 <code>PLAN_</code> 前缀是历史性的：两种合约模式 （Plan 和 Goal）共享这些代码，而不是复制 <code>_IN_GOAL</code> 集 (<strong>D198</strong>)。渲染器从提案的 <code>kind</code> 中选择其措辞，因此 代码可以显示为“Plan”或“Goal”副本。</p><h3 id="_3-4-edit-契约-adr-0087" tabindex="-1">3. 4 Edit 契约（ADR 0087） <a class="header-anchor" href="#_3-4-edit-契约-adr-0087" aria-label="Permalink to &quot;3. 4 Edit 契约（ADR 0087）&quot;">​</a></h3><p>仅由 <code>Edit</code> 发出。版本与来源失败拥有各自的代码，因为每一个都指向不同的 下一步动作；把它们报告为 <code>TOOL_FAILED</code> 会丢失这一信息。见 <a href="./18-line-anchored-edit-contract">18-line-anchored-edit-contract</a> §11。</p><table tabindex="0"><thead><tr><th>代码</th><th>可重试</th><th>含义</th></tr></thead><tbody><tr><td><code>EDIT_TAG_REQUIRED</code></td><td>否</td><td><code>tag</code> 缺失或不是 4 位十六进制</td></tr><tr><td><code>EDIT_TAG_MISMATCH</code></td><td><code>Read</code> 之后可以</td><td>tag 无法哈希出实时文件且漂移恢复拒绝；携带实时 tag 与锚点处的当前内容</td></tr><tr><td><code>EDIT_TAG_UNKNOWN</code></td><td><code>Read</code> 之后可以</td><td>tag 格式正确，但本会话没有为该路径记录过对应内容</td></tr><tr><td><code>EDIT_LINES_UNSEEN</code></td><td>是</td><td>锚点引用了会话从未显示过的行；携带被揭示的内容</td></tr><tr><td><code>EDIT_PARSE_FAILED</code></td><td>否</td><td>操作头格式错误、无冒号头下出现正文行、缺少正文，或出现 <code>-</code>/上下文行</td></tr><tr><td><code>EDIT_RANGE_INVALID</code></td><td>否</td><td>范围反向、行号越界、操作重叠，或锚点重复</td></tr><tr><td><code>EDIT_BLOCK_UNRESOLVED</code></td><td>否</td><td><code>N*</code> 定位符无法解析；消息给出纯范围替代方案</td></tr><tr><td><code>EDIT_REGISTER_EMPTY</code></td><td>否</td><td>从未设置的寄存器粘贴</td></tr><tr><td><code>EDIT_REGISTER_AMBIGUOUS</code></td><td>否</td><td>存在多个待粘贴的匿名捕获时进行匿名粘贴</td></tr><tr><td><code>EDIT_REPAIR_AMBIGUOUS</code></td><td>否</td><td>边界修复候选在最小代价上并列</td></tr><tr><td><code>EDIT_NO_CHANGE</code></td><td>否</td><td>应用产生了与输入完全相同的文本</td></tr><tr><td><code>EDIT_AMPLIFICATION_LIMIT</code></td><td>否</td><td>下降展开超过膨胀上限</td></tr></tbody></table><p>当消息报告 reveal 完整时，<code>EDIT_LINES_UNSEEN</code> <strong>无需</strong>再次 <code>Read</code> 即可重试： 被揭示的行已并入会话来源集，因此原样重试同一个 <code>tag</code> 即可应用。被截断的 reveal 不并入任何行，必须重新读取。</p><p><code>EDIT_TAG_MISMATCH</code>、<code>EDIT_TAG_UNKNOWN</code> 与 <code>EDIT_LINES_UNSEEN</code> 在重复保护开始计数 之前，各自在每条路径上有一次免费尝试，因为每一个都已经携带了重试所需的东西。其余代码 在第一次出现时就计数，而耗尽额度的那次失败会以 §3.3 的 <code>MUTATION_RETRY_BUDGET_EXHAUSTED</code> 出现在 assistant 行上 （<a href="/zh-CN/spec/03-runtime/18-line-anchored-edit-contract">18-line-anchored-edit-contract</a> §9.3）。</p><h3 id="_3-5-秘密-设置" tabindex="-1">3. 5 秘密/设置 <a class="header-anchor" href="#_3-5-秘密-设置" aria-label="Permalink to &quot;3. 5 秘密/设置&quot;">​</a></h3><table tabindex="0"><thead><tr><th>代码</th><th>可重审的</th><th>意义</th></tr></thead><tbody><tr><td><code>PROVIDER_SECRET_MISSING</code></td><td>不</td><td>启用的提供程序需要 API 密钥</td></tr><tr><td><code>MODEL_ALIAS_TOO_LONG</code></td><td>不</td><td>已配置模型别名超过 60 个 Unicode 字符</td></tr><tr><td><code>MODEL_BINDINGS_DEGRADED</code></td><td>不</td><td>存储模型绑定已降级；为防止数据丢失，拒绝显式替换模型数组</td></tr><tr><td><code>SECRET_STORE_UNAVAILABLE</code></td><td>也许</td><td>操作系统安全存储不可用（保留）</td></tr><tr><td><code>SETTINGS_INVALID</code></td><td>不</td><td>设置有效负载无效（保留）</td></tr></tbody></table><h3 id="_3-6-插件" tabindex="-1">3. 6 插件 <a class="header-anchor" href="#_3-6-插件" aria-label="Permalink to &quot;3. 6 插件&quot;">​</a></h3><table tabindex="0"><thead><tr><th>代码</th><th>可重审的</th><th>意义</th></tr></thead><tbody><tr><td><code>PLUGIN_NOT_FOUND</code></td><td>不</td><td>插件 ID 缺失</td></tr><tr><td><code>PLUGIN_INVALID</code></td><td>不</td><td>manifest/package 无效</td></tr><tr><td><code>PLUGIN_LOAD_FAILED</code></td><td>也许</td><td>enable/load 失败</td></tr><tr><td><code>PLUGIN_DISABLED</code></td><td>不</td><td>插件已禁用（保留）</td></tr><tr><td><code>PLUGIN_PERMISSION_DENIED</code></td><td>不</td><td>插件缺少该调用所需的已声明且已授予的权限</td></tr><tr><td><code>PLUGIN_INTEGRITY</code></td><td>不</td><td>包校验和或签名与目录条目不匹配</td></tr><tr><td><code>PLUGIN_NETWORK</code></td><td>是的</td><td>市场下载或目录拉取失败</td></tr><tr><td><code>PLUGIN_HOST_TOO_OLD</code></td><td>不</td><td>包的 <code>engines.piDesktop</code> 范围排除了当前宿主</td></tr><tr><td><code>PLUGIN_MARKET_INVALID</code></td><td>不</td><td>市场目录格式错误或缺少必需的发布字段</td></tr><tr><td><code>PLUGIN_MARKET_UNTRUSTED_HOST</code></td><td>不</td><td>目录或包 URL 不在可信市场主机之内</td></tr><tr><td><code>PLUGIN_MARKET_YANKED</code></td><td>不</td><td>请求的发布版本已从目录中撤回</td></tr><tr><td><code>PLUGIN_MARKET_NOT_PUBLISHED</code></td><td>不</td><td>平台有该版本但尚未对外提供</td></tr><tr><td><code>PLUGIN_MARKET_ARCHIVED</code></td><td>不</td><td>插件已被平台下架</td></tr><tr><td><code>PLUGIN_MARKET_NOT_FOUND</code></td><td>不</td><td>平台没有该插件或该版本</td></tr><tr><td><code>PLUGIN_MARKET_RATE_LIMITED</code></td><td>是</td><td>下载接口要求客户端等待后重试</td></tr><tr><td><code>PLUGIN_MARKET_NO_SOURCE</code></td><td>也许</td><td>没有任何分发目标能提供该包</td></tr><tr><td><code>PLUGIN_CANCELLED</code></td><td>不</td><td>用户在下载过程中取消了安装</td></tr><tr><td><code>MCP_INVALID</code></td><td>不</td><td>用户的 MCP 服务器定义校验失败</td></tr><tr><td><code>SKILL_INVALID</code></td><td>不</td><td>用户的技能文档校验失败</td></tr><tr><td><code>SUBAGENT_INVALID</code></td><td>不</td><td>用户的子代理文档校验失败</td></tr><tr><td><code>CAPABILITY_INVALID</code></td><td>不</td><td>Agent 能力根目录或作用域设置校验失败</td></tr><tr><td><code>PLUGIN_COMMAND_NOT_FOUND</code></td><td>不</td><td>命令 ID 丢失（保留）</td></tr><tr><td><code>PLUGIN_CRASHED</code></td><td>是的</td><td>插件运行时崩溃（保留）</td></tr><tr><td><code>PLUGIN_CONTRACT_MISMATCH</code></td><td>不</td><td>不支持的 manifest/api 版本（保留）</td></tr></tbody></table><h3 id="_3-7-保留的详细代码-尚未发布" tabindex="-1">3. 7 保留的详细代码（尚未发布） <a class="header-anchor" href="#_3-7-保留的详细代码-尚未发布" aria-label="Permalink to &quot;3. 7 保留的详细代码（尚未发布）&quot;">​</a></h3><p>记录了更细粒度的 provider/tool 区别，以供将来映射。 在发布之前，实现使用所示的规范父代码。</p><table tabindex="0"><thead><tr><th>保留代码</th><th>今天的规范父母</th><th>笔记</th></tr></thead><tbody><tr><td><code>PROVIDER_BASE_URL_INVALID</code></td><td><code>PROVIDER_ERROR</code></td><td>端点无效（400）</td></tr><tr><td><code>PROVIDER_PROTOCOL_MISMATCH</code></td><td><code>PROVIDER_ERROR</code></td><td>错误的协议配置文件</td></tr><tr><td><code>PROVIDER_MODEL_NOT_FOUND</code></td><td><code>MODEL_NOT_CONFIGURED</code></td><td>未知模型 ID (404)</td></tr><tr><td><code>PROVIDER_TIMEOUT</code></td><td><code>TIMEOUT</code></td><td>network/server 超时（可重试）</td></tr><tr><td><code>PROVIDER_UNSUPPORTED_CAPABILITY</code></td><td><code>PROVIDER_ERROR</code></td><td>tools/vision 不支持</td></tr><tr><td><code>PROVIDER_DISABLED</code></td><td><code>MODEL_NOT_CONFIGURED</code></td><td>提供商已禁用</td></tr></tbody></table><p><code>WORKSPACE_PATH_DENIED</code> 和 <code>TOOL_BINARY_CONTENT</code> 在主机开始发出它们时已从 本表移出（§3.3）。</p><p>历史别名（切勿在新代码中使用）：<code>PROVIDER_AUTH_FAILED</code> → <code>PROVIDER_UNAUTHORIZED</code>； <code>PROVIDER_STREAM_INTERRUPTED</code> → <code>STREAM_FAILED</code>； <code>WORKSPACE_OUTSIDE_ROOT</code> → <code>PATH_OUTSIDE_WORKSPACE</code>； <code>SECRET_MISSING</code> → <code>PROVIDER_SECRET_MISSING</code>； <code>SHELL_UNAVAILABLE</code> → <code>SHELL_NOT_FOUND</code>； <code>SHELL_IDENTITY_STALE</code> → <code>COMMAND_SHELL_CHANGED</code>； <code>PLAN_APPROVAL_EXPIRED</code> → <code>PLAN_APPROVAL_TIMEOUT</code>。截断不是错误：有界工具结果 带有一个标记，命名哪一端幸存以及其余部分在哪里，或者报告 同级结果字段中的有界窗口 （请参阅 <a href="/zh-CN/spec/03-runtime/16-tool-result-limits">16-工具-结果-限制</a>）。</p><h3 id="_3-8-远程控制-racp-ws-ssh-引导" tabindex="-1">3.8 远程控制（RACP-WS / SSH 引导） <a class="header-anchor" href="#_3-8-远程控制-racp-ws-ssh-引导" aria-label="Permalink to &quot;3.8 远程控制（RACP-WS / SSH 引导）&quot;">​</a></h3><p>当会话位于经 <code>RACP-WS</code> 驱动的已配对远程主机上时，由桌面端的远程主机客户端与 <code>pi-host</code> 服务端发出（参见 <a href="/zh-CN/spec/03-runtime/19-remote-agent-control-protocol">19-远程代理控制协议</a>、 <a href="/zh-CN/spec/05-security/02-remote-control-security">../05-security/02-remote-control-security</a>、 ADR 0285）。渲染进程除了一个标识徽章外看不到本地/远程之分；这些码通过与其他调用 相同的错误对象浮现。</p><table tabindex="0"><thead><tr><th>码</th><th>可重试</th><th>含义</th></tr></thead><tbody><tr><td><code>HOST_DISCONNECTED</code></td><td>是</td><td>远程主机连接断开；进行中的调用被拒绝，客户端按游标重连并重新订阅</td></tr><tr><td><code>HOST_BOOTSTRAP_FAILED</code></td><td>否</td><td>经 SSH 配置远程 <code>pi-host</code> 失败（下载、校验和不匹配或 <code>install.sh</code>）；<code>details.reason</code> 指明阶段</td></tr><tr><td><code>HOST_VERSION_MISMATCH</code></td><td>否</td><td>远程 <code>pi-host</code> 版本与桌面不匹配；桌面拒绝驱动不兼容的主机</td></tr><tr><td><code>REMOTE_AUTH_FAILED</code></td><td>否</td><td>设备或配对令牌在 RACP-WS 升级时被拒</td></tr><tr><td><code>REMOTE_CONNECTION_FAILED</code></td><td>是</td><td>RACP-WS 传输无法连接（非回环 URL、套接字被拒）</td></tr><tr><td><code>REMOTE_FORWARD_FAILED</code></td><td>是</td><td>无法建立 SSH 回环端口转发</td></tr><tr><td><code>REMOTE_PATH_NOT_FOUND</code></td><td>否</td><td>远程项目/工作区路径在主机上不存在</td></tr><tr><td><code>REMOTE_PATH_FORBIDDEN</code></td><td>否</td><td>远程路径在主机允许的根之外</td></tr><tr><td><code>PAIRING_FAILED</code></td><td>否</td><td><code>connection/pair</code> 无法铸造设备凭据</td></tr><tr><td><code>PAIRING_TOKEN_EXPIRED</code></td><td>否</td><td>一次性配对令牌在配对完成前已过期</td></tr><tr><td><code>CAPABILITY_UNAVAILABLE</code></td><td>否</td><td>请求的操作对应主机声明为不可用的能力（如附件、工具中继）</td></tr></tbody></table><h2 id="_4-映射规则" tabindex="-1">4. 映射规则 <a class="header-anchor" href="#_4-映射规则" aria-label="Permalink to &quot;4. 映射规则&quot;">​</a></h2><h3 id="主机-rpc-数字-→-apperror-code" tabindex="-1">主机 RPC 数字 → AppError.code <a class="header-anchor" href="#主机-rpc-数字-→-apperror-code" aria-label="Permalink to &quot;主机 RPC 数字 → AppError.code&quot;">​</a></h3><p>请参阅 <code>06-host-rpc-protocol.md</code> 数值表。 示例：主机 <code>1004</code> → <code>TOOL_DENIED</code>。</p><h3 id="提供商例外" tabindex="-1">提供商例外 <a class="header-anchor" href="#提供商例外" aria-label="Permalink to &quot;提供商例外&quot;">​</a></h3><p>Node sidecar 将提供商 SDK 错误映射到：</p><ul><li><code>PROVIDER_UNAUTHORIZED</code></li><li><code>PROVIDER_RATE_LIMITED</code></li><li><code>MODEL_NOT_CONFIGURED</code>（提供商拒绝选择的模型并返回 404）</li><li><code>PROVIDER_ERROR</code></li><li><code>NETWORK_ERROR</code></li><li><code>STREAM_FAILED</code></li></ul><p>精确的 <code>terminated</code> 提供商消息和等效的过早流关闭 消息映射到 <code>STREAM_FAILED</code>。请求设置阶段或响应后的 <code>PROVIDER_RATE_LIMITED</code> 使用共享的运行时预算：初始尝试之后最多 10 次重试， 且设置和流式传输失败一起计数。非 429 瞬时故障——<code>STREAM_FAILED</code>、 <code>NETWORK_ERROR</code>、<code>TIMEOUT</code> 以及可重试的 <code>PROVIDER_ERROR</code>（例如上游网关 502/503/504）——共享它们自己的有界预算：初始尝试之后最多 10 次重试，同样 跨请求设置和流式传输一起计数，并且与 429 预算相互独立。两个预算都是 可中止的。429 路径在客户端退避之前先遵循 <code>retry-after-ms</code>、<code>retry-after</code> 秒和 HTTP 日期标头，并将等待上限设为 30 秒；非 429 路径应用相同的优先级， 上限为 8 秒，在其他情况下依次等待 1 秒、2 秒、4 秒，然后是 8 秒。只有失败 的请求会被重放；会话及其工具状态保持不变。来自格式错误的 400/422 请求的 不可重试 <code>PROVIDER_ERROR</code> 永远不会进入任何预算。预算耗尽后的失败仍然是 致命的。设置 <code>infiniteProviderRetry</code> 默认关闭；开启后只移除上述可重试网络/瞬时类别的次数上限， 不会改变退避、<code>Retry-After</code>、取消或终止分类，并可能在用户停止回合前持续消耗 API 用量。</p><p><strong>Synchronized update (#699):</strong> A complete successful model response resets both budgets, including a tool-call response, in the main session and builtin subagents. Headers, partial output, and phase changes do not replenish them. Exhaustion reports <code>retryAttempt: 10</code> from the relevant budget counter.</p><p><code>NETWORK_ERROR</code> 以有界的 <code>details</code> 携带真正失败的传输层： <code>networkCategory</code>（<code>dns</code>、<code>tls</code>、<code>timeout</code>、<code>refused</code>、<code>unreachable</code>、 <code>reset</code>、<code>proxy</code>，或在没有留下任何线索时为 <code>unknown</code>）、<code>networkCode</code> （errno，例如 <code>ENOTFOUND</code>、<code>ECONNRESET</code>、<code>EPROTO</code>、<code>UND_ERR_SOCKET</code>）， 以及传输层提供时的 <code>networkSyscall</code> 和 <code>networkHost</code>。只保留裸主机名—— 绝不包含 URL、端口、路径、查询串或凭据——当 <code>providerCode</code> 会重复 <code>networkCode</code> 时省略它。刻意不引入按层划分的错误码（<code>DNS_ERROR</code>、 <code>TLS_ERROR</code>、<code>SOCKET_RESET</code> 等）：分类字段已能区分这些层，而无需为每一层 增加用户可见的错误码与本地化文案。</p><p>诊断来自 fetch 边界处的实时 cause 链，而不只是提供程序消息：pi-ai 会把被 拒绝的请求摊平成 <code>errorMessage</code>，等到分类运行时 undici 存放在 <code>error.cause</code> 里的 errno 已经消失，裸 <code>fetch failed</code> 只能被记为 <code>networkCategory: unknown</code>； 而 fetch 包装层仍持有原始 Error，并从它给出同一组经过校验的字段。捕获到的 cause 同时确定了阶段：没有任何响应到达时故障记为 <code>phase: request</code>，这正是它 与「响应中途断流」的区别。<code>networkRoute</code>（<code>direct</code>、<code>environment-proxy</code>、 <code>http-proxy</code>、<code>socks5-proxy</code>）指出请求实际走的链路，代理这一跳失败无需再从 errno 猜测。</p><p>同一来源在一轮内连续两次这样失败（完全没有响应）时，下一次尝试前会重建提供 程序传输，而不是继续复用同一个 undici 连接池。重建是进程级的、且有明确边界： 每个连续失败序列只重建一次，每 30 秒最多一次，且 <code>dns</code> 永不触发重建（新连接池 无法改变名字解析结果）。替换在关闭旧 dispatcher 之前安装，旧的 dispatcher 采 用优雅关闭，因此其他会话已派发的请求仍会在它原本使用的连接池上完成。生效的 链路会被原样复现，绝不会悄悄降级为直连。</p><h3 id="权限超时" tabindex="-1">权限超时 <a class="header-anchor" href="#权限超时" aria-label="Permalink to &quot;权限超时&quot;">​</a></h3><p>UI/host 超时在内部发出 <code>PERMISSION_TIMEOUT</code>，工具结果向代理显示为拒绝 (<code>TOOL_DENIED</code>)。</p><h3 id="shell-和-plan-goal-检查点失败" tabindex="-1">Shell 和 Plan/Goal 检查点失败 <a class="header-anchor" href="#shell-和-plan-goal-检查点失败" aria-label="Permalink to &quot;Shell 和 Plan/Goal 检查点失败&quot;">​</a></h3><p>仅当目录回退发现不可用时才返回 <code>SHELL_NOT_FOUND</code> 平台外壳。 <code>COMMAND_SHELL_CHANGED</code> 永远不会使用不同的 shell 重试； 该回合必须获得新的有效 ID/dialect。 <code>PLAN_ARTIFACT_WRITE_FAILED</code> 从不创建批准行。 <code>PLAN_APPROVAL_TIMEOUT</code> 仅适用于 绝对待决期限； <code>PLAN_EXECUTION_INTERRUPTED</code> 标识已批准的 queued/running 执行因中止或主机恢复而中断。 <code>PLAN_KIND_MISMATCH</code> 是 终止工具错误，如 <code>PLAN_NOT_ACTIVE</code>：提交工具运行于 错误的合同，因此没有编写任何工件，也没有创建批准行。</p><h2 id="_5-ui-处理指南" tabindex="-1">5. UI 处理指南 <a class="header-anchor" href="#_5-ui-处理指南" aria-label="Permalink to &quot;5. UI 处理指南&quot;">​</a></h2><table tabindex="0"><thead><tr><th>类</th><th>用户界面行为</th></tr></thead><tbody><tr><td>auth/config（<code>PROVIDER_SECRET_MISSING</code>、<code>MODEL_NOT_CONFIGURED</code>）</td><td>带有设置 CTA 的助理错误消息</td></tr><tr><td>拒绝许可</td><td>内联工具卡状态</td></tr><tr><td>可重试的 provider/network</td><td>带有重试操作的助理错误消息</td></tr><tr><td>internal/host 不可用</td><td>降级横幅 + 恢复提示</td></tr></tbody></table><p>消息绑定提供程序故障从不使用 toast 或浮动全局横幅。 助手错误消息显示本地化摘要和稳定代码，并带有 包含经过编辑的提供商响应的可访问详细信息披露， 提供商 ID 和模型 ID。提供商详细信息上限为 600 个字符，并且 公共 credential/header 值在事件发射或持久化之前进行编辑。 详细信息披露也可能显示有界的 <code>phase</code>、<code>providerStatus</code>、<code>providerCode</code>、 <code>providerWaitMs</code>、<code>streamMs</code>、<code>retryAttempt</code>、<code>networkCategory</code>、 <code>networkCode</code>、<code>networkSyscall</code>、<code>networkHost</code>、<code>networkRoute</code>、<code>requestMessages</code>、 <code>requestBytes</code> 和 <code>compactionGeneration</code> 字段。请求字段只有计数与字节大小， 压缩字段是检查点世代计数器，均不携带消息内容。当瞬时提供商故障正在重试时， 活动指示器的原因气泡会显示本地化摘要、稳定错误码，并在网络故障时显示传输层 errno（<code>NETWORK_ERROR · ENOTFOUND</code>），因此失败层级在重试期间与日志记录中 同样可见。</p><h2 id="_6-i18n-按键约定" tabindex="-1">6. i18n 按键约定 <a class="header-anchor" href="#_6-i18n-按键约定" aria-label="Permalink to &quot;6. i18n 按键约定&quot;">​</a></h2><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>errors.&lt;code&gt;</span></span>
<span class="line"><span>errors.&lt;code&gt;.action</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>示例：</p><ul><li><code>errors.PROVIDER_SECRET_MISSING</code></li><li><code>errors.PROVIDER_SECRET_MISSING.action</code></li><li><code>errors.HOST_UNAVAILABLE</code></li></ul><h2 id="_7-验收" tabindex="-1">7. 验收 <a class="header-anchor" href="#_7-验收" aria-label="Permalink to &quot;7. 验收&quot;">​</a></h2><ol><li>每次 IPC 失败都会返回 <code>AppError.code</code></li><li>主路径上没有原始非类型化字符串故障</li><li>Plan/Goal 硬否认使用显式特定于工具的代码； Bash 从未被否认 仅仅因为运营模式而采用任一合同模式，而不是 遵循权限策略</li><li>主机数字代码映射到稳定的字符串代码</li><li>无效的 shell 设置、no-effective-shell/stale-pin、artifact-write、 到期、计划拒绝和重新启动中断路径映射到稳定 代码；仅允许记录的预转目录后备，并且不进行任何工作 正在重播</li></ol><h3 id="证书校验失败-issue-714" tabindex="-1">证书校验失败（issue #714） <a class="header-anchor" href="#证书校验失败-issue-714" aria-label="Permalink to &quot;证书校验失败（issue #714）&quot;">​</a></h3><p>当 <code>details.networkCode</code> 是已识别的证书校验错误时，<code>NETWORK_ERROR</code> 不可重试， 包括不受信任或自签名链、证书已过期或尚未生效，以及 <code>ERR_TLS_CERT_ALTNAME_INVALID</code>。具体证书原因优先于通用 socket/proxy 包装错误。 即使 adapter 已将错误扁平化，捕获的 fetch 原因仍会应用这条策略。未知 TLS 错误和 非证书协议错误继续使用原有恢复行为。</p><p>transcript 保留稳定错误码、传输 errno 和原始 details，但使用本地化的证书指引， 而不是通用连接错误摘要。它会提示用户检查证书、系统时间以及安全软件或代理使用的 信任根，并在修改信任设置后重启。文案不会断言一定是流量拦截，也不会提供关闭 TLS 校验的绕过方式。修复原因后，用户仍可手动继续。</p><h2 id="本地请求准备错误" tabindex="-1">本地请求准备错误 <a class="header-anchor" href="#本地请求准备错误" aria-label="Permalink to &quot;本地请求准备错误&quot;">​</a></h2><p>上下文校验、估算或请求准备阶段产生的结构化 <code>LOCAL_REQUEST_ERROR</code>，映射为既有 <code>INTERNAL</code> 且 <code>retriable: false</code>。在适配器把异常压成文字前保留本地来源与阶段； 诊断可以保留原因类型，不向界面复制请求正文、搜索结果、凭据或任意底层异常文字。 不能靠匹配异常句子或统一禁用所有 <code>TypeError</code> 重试来分类；网络故障和取消维持原有策略。</p><p>历史恢复校验可能在运行时流创建前失败。此时使用既有 RPC 错误 <code>data</code>，携带 <code>errorCode</code>、<code>retriable: false</code> 及安全的 <code>details</code>（来源、阶段、可选原因类型）。 不得发出模型请求，sidecar 保持可用，也不改写存储记录；容器不是存储块列表时仍按此失败。</p><p>单个存储块无法回放属于另一种情况：网关丢弃 id 时本应用本身就会存下仅供展示的块， 因此该消息的整条 replay 降级为“没有 replay”，而不是让之后每一轮请求都失败。 回合继续执行，展示轮次不变；诊断只记录块数与阶段，不复制搜索内容、结果或凭据。</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("zh-CN/spec/03-runtime/08-error-codes.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _08ErrorCodes = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _08ErrorCodes as default
};
