import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"18. 行锚定编辑契约","description":"","frontmatter":{},"headers":[],"relativePath":"zh-CN/spec/03-runtime/18-line-anchored-edit-contract.md","filePath":"zh-CN/spec/03-runtime/18-line-anchored-edit-contract.md","lastUpdated":1788986066000}');
const _sfc_main = { name: "zh-CN/spec/03-runtime/18-line-anchored-edit-contract.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="_18-行锚定编辑契约" tabindex="-1">18. 行锚定编辑契约 <a class="header-anchor" href="#_18-行锚定编辑契约" aria-label="Permalink to &quot;18. 行锚定编辑契约&quot;">​</a></h1><blockquote><p><strong>翻译说明：</strong> 本页是与 <a href="/spec/03-runtime/18-line-anchored-edit-contract">英文源规格</a> 一一对应的机器辅助翻译。代码、协议字段和标识符保持原文；如翻译与英文源事实有歧义，以英文版本为准。</p></blockquote><blockquote><p>应用的决定：ADR 0087。修订 D186、ADR 0069 §2、ADR 0043 §1。 实现的事实来源：<code>crates/host-core/src/tools/hashline/</code>（新增）、 <code>crates/host-core/src/tools/mod.rs</code>、<code>packages/agent-runtime/src/runtime.ts</code>。</p></blockquote><h2 id="_1-目的" tabindex="-1">1. 目的 <a class="header-anchor" href="#_1-目的" aria-label="Permalink to &quot;1. 目的&quot;">​</a></h2><p><code>Edit</code> 命名文件中的<strong>位置</strong>，并且只提供<strong>新内容</strong>。它从不要求模型复述已有字节。 版本正确性与读取来源由主机证明，而不是由模型断言。</p><p>在写入任何字节之前，按以下顺序强制三条性质：</p><ol><li><strong>版本。</strong> 模型提供的 <code>tag</code> 必须能哈希出实时文件。</li><li><strong>来源。</strong> 编辑所锚定的每一行都必须曾向本会话显示过。</li><li><strong>确定性。</strong> 操作必须解析为恰好一个跨度。有歧义即失败；绝不按偏好消解歧义。</li></ol><h2 id="_2-冻结政策总结" tabindex="-1">2. 冻结政策总结 <a class="header-anchor" href="#_2-冻结政策总结" aria-label="Permalink to &quot;2. 冻结政策总结&quot;">​</a></h2><table tabindex="0"><thead><tr><th>主题</th><th>决定</th></tr></thead><tbody><tr><td><code>Edit</code> 参数</td><td><code>path</code>、<code>tag</code>、<code>ops</code></td></tr><tr><td>已移除</td><td><code>old_string</code>、<code>new_string</code>，以及两种契约之间的任何模式开关</td></tr><tr><td>Tag</td><td>4 位大写十六进制，由整个规范化文件导出</td></tr><tr><td>Tag 的生成者</td><td><code>Read</code>、<code>Grep</code>、<code>Write</code>，以及每一次成功的 <code>Edit</code></td></tr><tr><td>锚定单位</td><td>被 tag 命名快照的 1 起始原始行号</td></tr><tr><td>正文行</td><td>仅 <code>+</code> 前缀的最终内容；没有 <code>-old</code>，没有上下文行</td></tr><tr><td>每次调用的 section 数</td><td>恰好一个（args 中的 <code>path</code>）</td></tr><tr><td>命名寄存器</td><td>会话作用域，跨 <code>Edit</code> 调用存活</td></tr><tr><td>匿名寄存器</td><td>调用内局部</td></tr><tr><td>块操作</td><td>tree-sitter；无法解析时拒绝，绝不近似</td></tr><tr><td>过时 tag</td><td>仅在可证明唯一且安全时恢复；否则拒绝</td></tr><tr><td>空操作应用</td><td>错误</td></tr><tr><td>快照存储</td><td>内存中、按会话、有界、随会话一起丢弃</td></tr></tbody></table><h2 id="_3-快照-tag" tabindex="-1">3. 快照 tag <a class="header-anchor" href="#_3-快照-tag" aria-label="Permalink to &quot;3. 快照 tag&quot;">​</a></h2><h3 id="_3-1-规范化" tabindex="-1">3.1 规范化 <a class="header-anchor" href="#_3-1-规范化" aria-label="Permalink to &quot;3.1 规范化&quot;">​</a></h3><p>在哈希之前、以及在寻址任何行之前，文件文本会被规范化：</p><ol><li>去掉开头的 UTF-8 BOM，并保留它以便写入时恢复。</li><li>检测行尾并规范化为 <code>LF</code>；保留原本占主导的行尾以便写入时恢复。</li><li>去掉每一行末尾的 <code>[ \\t\\r]</code>，包括最后一行。</li></ol><p>第 3 步<strong>只</strong>作用于哈希输入，绝不作用于文件所保留的内容。它的存在是为了让一个 <code>CRLF</code> 文件、以及一次去掉了行尾空白的显示，仍然生成与磁盘字节相同的 tag。</p><p>行寻址按 <code>LF</code> 切分规范化文本。末尾换行终结它前面的一行，本身不是可寻址内容；切分 产生的尾部空元素会被丢弃。</p><h3 id="_3-2-计算" tabindex="-1">3.2 计算 <a class="header-anchor" href="#_3-2-计算" aria-label="Permalink to &quot;3.2 计算&quot;">​</a></h3><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>tag(text) = uppercase_hex_4( low_16_bits( digest( normalize_for_hash(text) ) ) )</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p><code>digest</code> 是 <code>crates/host-core/src/review.rs</code> 已在使用的 SHA-256 原语；tag 取其低 16 位。复用已有原语让 host-core 只有一套哈希叙事，也避免新增依赖。代价是每次读取与每次 写入各做一次整文件摘要，其上界由文件大小决定，对 <code>Read</code> 所服务的文件规模而言是个位数 毫秒。</p><h3 id="_3-3-碰撞政策" tabindex="-1">3.3 碰撞政策 <a class="header-anchor" href="#_3-3-碰撞政策" aria-label="Permalink to &quot;3.3 碰撞政策&quot;">​</a></h3><p>tag 有 65 536 个取值。碰撞是预期之内的，而不是例外。tag 是<strong>索引，绝不是身份</strong>：</p><ul><li>快照去重要求<strong>全文相等</strong>，而不是 tag 相等。共享同一个 tag 的两段不同文本就是两个快照。</li><li>按 tag 查找返回最近记录的匹配版本。</li><li>每一个可能损坏文件的决定——来源校验、漂移恢复、路径恢复——都额外比较全文或校验周围 上下文。仅 tag 匹配从不授权写入。</li></ul><p>把两段文本融合到一个 tag 之下，会把其中一段显示过的行挂到另一段的内容上，而这正是 16 位 tag 唯一能造成真实损害的方式。</p><h2 id="_4-会话快照存储" tabindex="-1">4. 会话快照存储 <a class="header-anchor" href="#_4-会话快照存储" aria-label="Permalink to &quot;4. 会话快照存储&quot;">​</a></h2><h3 id="_4-1-形状" tabindex="-1">4.1 形状 <a class="header-anchor" href="#_4-1-形状" aria-label="Permalink to &quot;4.1 形状&quot;">​</a></h3><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>session_id → canonical_path → [Snapshot]   (newest first)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Snapshot {</span></span>
<span class="line"><span>  text:        String,        // full normalized text, LF, no BOM</span></span>
<span class="line"><span>  tag:         String,        // 4 hex, uppercase</span></span>
<span class="line"><span>  recorded_at: Instant,</span></span>
<span class="line"><span>  seen_lines:  Option&lt;BTreeSet&lt;u32&gt;&gt;,   // 1-indexed lines actually displayed</span></span>
<span class="line"><span>}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><p><code>canonical_path</code> 是 <code>workspace::resolve_tool_path</code> 已经产出的解析后路径，因此工作区、 scratch 与被批准的外部根共享一个键空间，同一路径的两种写法解析到同一条目。</p><p><code>seen_lines == None</code> 意味着“没有记录来源”——此时跳过来源闸门而不是失败关闭，因此一个 外部生成或已被淘汰的 tag 会降级为只做版本校验。</p><h3 id="_4-2-记录与读取融合" tabindex="-1">4.2 记录与读取融合 <a class="header-anchor" href="#_4-2-记录与读取融合" aria-label="Permalink to &quot;4.2 记录与读取融合&quot;">​</a></h3><p><code>record(path, text, seen_lines)</code> 返回 tag，并且：</p><ul><li>如果某个保留版本的 tag 相同<strong>且</strong>全文相同，该版本被提升到头部、刷新其最近使用时间， 并把 <code>seen_lines</code> 并入。tag 被复用。</li><li>否则把一个新版本插到该路径历史的最前面。</li></ul><p>读取融合正是分页阅读能够工作的原因：读取一个未变文件的第 1–2000 行，再读取第 1800–3600 行，会产生一个 <code>seen_lines</code> 覆盖 1–3600 的 tag，因此之后落在该范围内任何位置的 <code>Edit</code> 都无需第三次读取即可校验通过。</p><h3 id="_4-3-哪些行算作已显示" tabindex="-1">4.3 哪些行算作已显示 <a class="header-anchor" href="#_4-3-哪些行算作已显示" aria-label="Permalink to &quot;4.3 哪些行算作已显示&quot;">​</a></h3><p>一行只有在生产者<strong>完整</strong>输出它时才加入 <code>seen_lines</code>：</p><ul><li><code>Read</code> 记录它返回的每一行，但<strong>排除</strong>在 <code>MAX_LINE_CHARS</code>（16,384）处被剪辑的行。被剪辑 的行没有被显示；它的尾部恰恰是盲写造成损害的地方。</li><li><code>content</code> 模式下的 <code>Grep</code> 只记录其命中行。<code>filesWithMatches</code> 与 <code>count</code> 模式什么都不 记录，并生成一个 <code>seen_lines</code> 为空的 tag——这与 <code>None</code> 不同：该 tag 可用于版本校验， 而在有东西显示这些行之前，每个锚点都会被拒绝。</li><li><code>Write</code> 与成功的 <code>Edit</code> 记录写入后的内容，并把<strong>所有</strong>行标记为已显示：是会话写出了它们。</li></ul><p>空的 <code>seen_lines</code> 集被当作“什么都没显示过”，因此每个锚点都被拒绝。<code>None</code> 被当作“未知”， 因此没有锚点会被拒绝。这一区分是刻意的，不得被合并。</p><h3 id="_4-4-界限与生命周期" tabindex="-1">4.4 界限与生命周期 <a class="header-anchor" href="#_4-4-界限与生命周期" aria-label="Permalink to &quot;4.4 界限与生命周期&quot;">​</a></h3><table tabindex="0"><thead><tr><th>界限</th><th>取值</th><th>达到上限时的行为</th></tr></thead><tbody><tr><td>每会话路径数</td><td>64</td><td>LRU 淘汰最冷的路径历史</td></tr><tr><td>每路径版本数</td><td>4</td><td>先丢弃最旧的</td></tr><tr><td>每会话保留文本</td><td>8 MiB</td><td>LRU 淘汰路径直到低于上限</td></tr><tr><td>单文件保留</td><td>2 MiB</td><td>该文件仍被哈希并生成 tag，但不保留其文本；该文件不可用恢复</td></tr></tbody></table><p>该存储位于 host-core 进程内存中，处于已有的会话状态锁之下。它在会话被删除、工作区被 切换以及进程退出时被丢弃。它从不持久化：来自上一次运行的 tag 无法被识别，这会正确地 强制一次新的 <code>Read</code>。</p><p>淘汰在构造上是安全的。丢失一个快照会丢失从漂移中<em>恢复</em>的能力，以及<em>校验</em>来源的能力； 两者都降级为一次拒绝或一次警告，而绝不会降级为一次错误写入。</p><h2 id="_5-生产者输出形状" tabindex="-1">5. 生产者输出形状 <a class="header-anchor" href="#_5-生产者输出形状" aria-label="Permalink to &quot;5. 生产者输出形状&quot;">​</a></h2><h3 id="_5-1-read" tabindex="-1">5.1 <code>Read</code> <a class="header-anchor" href="#_5-1-read" aria-label="Permalink to &quot;5.1 \`Read\`&quot;">​</a></h3><p><code>content</code> 变为带行号并携带 section 头。这反转了 ADR 0069 §2 的字节忠实 <code>content</code>，其 唯一目的曾是 <code>old_string</code> 匹配。</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[src/main.rs#A1B2]</span></span>
<span class="line"><span>41:fn main() {</span></span>
<span class="line"><span>42:    println!(&quot;hi&quot;);</span></span>
<span class="line"><span>43:}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><ul><li>该头是<strong>整个文件</strong>的 tag，不是所返回窗口的 tag。</li><li>行号是绝对文件行号，因此 <code>offset</code> 不会移动它们。</li><li><code>N:</code> 前缀不计入 <code>MAX_LINE_CHARS</code>；剪辑仍然作用于行文本，并且仍然把该行排除在 <code>seen_lines</code> 之外。</li><li>已有的兄弟字段（<code>path</code>、<code>root</code>、<code>offset</code>、<code>lineCount</code>、<code>totalLines</code>、<code>truncated</code>、 <code>fileBytes</code>、<code>notice</code>）保持不变。<code>tag</code> 作为一等字段新增，因此消费者无需解析该头。</li></ul><h3 id="_5-2-grep" tabindex="-1">5.2 <code>Grep</code> <a class="header-anchor" href="#_5-2-grep" aria-label="Permalink to &quot;5.2 \`Grep\`&quot;">​</a></h3><p>每个 <code>matches[]</code> 条目保持 <code>{path, line, text}</code>，结果新增一个 <code>tags: { &lt;path&gt;: &lt;tag&gt; }</code> 映射，并在渲染形式中为每个文件加一行头。因此一个 grep 命中只能作为其命中行的可用编辑锚点。</p><h3 id="_5-3-write" tabindex="-1">5.3 <code>Write</code> <a class="header-anchor" href="#_5-3-write" aria-label="Permalink to &quot;5.3 \`Write\`&quot;">​</a></h3><p>返回 <code>tag</code> 以及实际落盘内容的 <code>[path#TAG]</code> 头，因此紧随其后的 <code>Edit</code> 无需 <code>Read</code>。</p><p><code>Write</code> 继续从传入 <code>content</code> 中剥离被粘贴的 <code>[path#TAG]</code> 头与 <code>N:</code> 行前缀；在 <code>Read</code> 输出带行号之后，这不再只是便利，而是必需。</p><h3 id="_5-4-写入后漂移" tabindex="-1">5.4 写入后漂移 <a class="header-anchor" href="#_5-4-写入后漂移" aria-label="Permalink to &quot;5.4 写入后漂移&quot;">​</a></h3><p>写入之后记录的 tag 由写入路径报告为已落盘的字节计算，而不是由工具本意的字节计算。当 格式化器或外部写穿在保存时改变内容时，记录本意文本会为并不存在的内容发布一个 tag，而 下一次 <code>Edit</code> 会针对文件已经离开的基线解析。</p><p>本意文本仍然是审核记录与模型可见摘要所描述的内容，因此一次漂移写入报告一行警告，而不是 整文件差异。</p><h2 id="_6-edit-请求形状" tabindex="-1">6. <code>Edit</code> 请求形状 <a class="header-anchor" href="#_6-edit-请求形状" aria-label="Permalink to &quot;6. \`Edit\` 请求形状&quot;">​</a></h2><table tabindex="0"><thead><tr><th>字段</th><th>类型</th><th>必填</th><th>描述</th></tr></thead><tbody><tr><td><code>path</code></td><td>string</td><td>是</td><td>已存在的常规文件，工作区相对路径或被批准的绝对路径。<code>Edit</code> 从不创建文件；请用 <code>Write</code>。</td></tr><tr><td><code>tag</code></td><td>string</td><td>是</td><td>该路径最近一次 <code>Read</code> / <code>Grep</code> / <code>Write</code> / <code>Edit</code> 结果中的 4 位大写十六进制。</td></tr><tr><td><code>ops</code></td><td>string</td><td>是</td><td>一个或多个操作头及其正文行，以换行分隔。</td></tr></tbody></table><p><code>ops</code> 可以以一行 <code>[path#TAG]</code> 头开始。存在时它必须与 <code>path</code> 和 <code>tag</code> 一致；不一致返回 <code>INVALID_ARGUMENT</code> 而不是静默偏向其中之一，因为两种写法不一致意味着模型已经不知道自己 在编辑哪个文件。</p><h2 id="_7-补丁语言" tabindex="-1">7. 补丁语言 <a class="header-anchor" href="#_7-补丁语言" aria-label="Permalink to &quot;7. 补丁语言&quot;">​</a></h2><h3 id="_7-1-语法" tabindex="-1">7.1 语法 <a class="header-anchor" href="#_7-1-语法" aria-label="Permalink to &quot;7.1 语法&quot;">​</a></h3><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ops        := header_block+</span></span>
<span class="line"><span>header_block := op_header NEWLINE body_row*        // colon-terminated headers</span></span>
<span class="line"><span>              | op_line                            // colonless headers</span></span>
<span class="line"><span>op_header  := &quot;PUT&quot; SP locator &quot;:&quot;</span></span>
<span class="line"><span>op_line    := &quot;PUT&quot; SP locator SP register</span></span>
<span class="line"><span>            | &quot;CUT&quot; SP locator [SP register]</span></span>
<span class="line"><span>            | &quot;REM&quot;</span></span>
<span class="line"><span>            | &quot;MV&quot; SP dest</span></span>
<span class="line"><span>locator    := line &quot;.=&quot; line        // inclusive range</span></span>
<span class="line"><span>            | line &quot;*&quot;              // syntactic block opening at line</span></span>
<span class="line"><span>            | &quot;&lt;&quot; line              // gap before line</span></span>
<span class="line"><span>            | &quot;&gt;&quot; line              // gap after line</span></span>
<span class="line"><span>            | &quot;&gt;&quot; &quot;$&quot;               // end of file</span></span>
<span class="line"><span>            | &quot;&gt;&quot; line &quot;*&quot;          // gap after the block opening at line</span></span>
<span class="line"><span>line       := [1-9][0-9]*</span></span>
<span class="line"><span>register   := &quot;@&quot; [A-Za-z0-9_-]{1,32}</span></span>
<span class="line"><span>body_row   := &quot;+&quot; TEXT</span></span>
<span class="line"><span>dest       := path | quoted_path</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br></div></div><h3 id="_7-2-操作" tabindex="-1">7.2 操作 <a class="header-anchor" href="#_7-2-操作" aria-label="Permalink to &quot;7.2 操作&quot;">​</a></h3><table tabindex="0"><thead><tr><th>头</th><th>正文</th><th>含义</th></tr></thead><tbody><tr><td><code>PUT N.=M:</code></td><td>必需</td><td>用正文替换原始的闭区间行 <code>N</code>–<code>M</code>。单行用 <code>N.=N</code>。</td></tr><tr><td><code>PUT N*:</code></td><td>必需</td><td>替换在第 <code>N</code> 行开启的语法块。</td></tr><tr><td><code>PUT &lt;N:</code></td><td>必需</td><td>在第 <code>N</code> 行之前插入正文。<code>PUT &lt;1:</code> 是文件头。</td></tr><tr><td><code>PUT &gt;N:</code></td><td>必需</td><td>在第 <code>N</code> 行之后插入正文。</td></tr><tr><td><code>PUT &gt;$:</code></td><td>必需</td><td>在文件末尾追加。</td></tr><tr><td><code>PUT &gt;N*:</code></td><td>必需</td><td>在第 <code>N</code> 行开启的块结束之后、以兄弟深度插入。</td></tr><tr><td><code>CUT N.=M</code></td><td>无</td><td>删除第 <code>N</code>–<code>M</code> 行并捕获它们。</td></tr><tr><td><code>CUT N*</code></td><td>无</td><td>删除在第 <code>N</code> 行开启的块并捕获它。</td></tr><tr><td><code>CUT … @name</code></td><td>无</td><td>捕获到命名寄存器而不是匿名寄存器。</td></tr><tr><td><code>PUT &lt;N @name</code> / <code>PUT &gt;N @name</code></td><td>无</td><td>在间隙处粘贴一个寄存器。</td></tr><tr><td><code>PUT N.=M @name</code> / <code>PUT N* @name</code></td><td>无</td><td>用寄存器覆盖该范围或已解析的块。在跨度形式中 <code>@name</code> 是必需的。</td></tr><tr><td><code>REM</code></td><td>无</td><td>删除由 <code>path</code> 命名的文件。</td></tr><tr><td><code>MV DEST</code></td><td>无</td><td>在把其他所有操作应用到源之后，移动/重命名到 <code>DEST</code>。</td></tr></tbody></table><h3 id="_7-3-锚定规则" tabindex="-1">7.3 锚定规则 <a class="header-anchor" href="#_7-3-锚定规则" aria-label="Permalink to &quot;7.3 锚定规则&quot;">​</a></h3><ol><li>所有行号都指<strong>被 tag 命名的快照</strong>。它们绝不会被同一次调用中较早的操作移动。</li><li>范围是闭区间且有序（<code>N &lt;= M</code>）。</li><li>范围只命名<strong>被改动的</strong>行。纯插入使用间隙定位符；一个重述了幸存行的加宽 <code>PUT</code> 是最 具破坏性的单一写法错误，也正是 §7.4 的仅 <code>+</code> 正文规则要防止的对象。</li><li>两个操作不得重叠，两个操作也不得针对同一个原始锚点。</li><li>范围不得起止于折叠或被剪辑的区域内部。被排除在 <code>seen_lines</code> 之外的行会被 §9 的来源 闸门机械地拒绝，而不是靠约定。</li></ol><h3 id="_7-4-正文行" tabindex="-1">7.4 正文行 <a class="header-anchor" href="#_7-4-正文行" aria-label="Permalink to &quot;7.4 正文行&quot;">​</a></h3><p>正文行是 <code>+</code> 后跟该行的最终文本，保留前导空白。单独的 <code>+</code> 是一个空行。</p><p>没有 <code>-old</code> 行，也没有上下文行。范围已经表达了删除；正文只是文件将要包含的内容。内容中 字面的前导 <code>-</code> 与 <code>+</code> 分别写作 <code>+- item</code> 与 <code>++ item</code>。</p><p>无冒号的头<strong>不</strong>带正文行。寄存器粘贴之下的正文行返回 <code>INVALID_ARGUMENT</code>，而不是被静默 丢弃。</p><h3 id="_7-5-寄存器" tabindex="-1">7.5 寄存器 <a class="header-anchor" href="#_7-5-寄存器" aria-label="Permalink to &quot;7.5 寄存器&quot;">​</a></h3><table tabindex="0"><thead><tr><th>寄存器</th><th>作用域</th><th>生命周期</th></tr></thead><tbody><tr><td>匿名</td><td>一次 <code>Edit</code> 调用</td><td>调用返回时清除</td></tr><tr><td><code>@name</code></td><td>会话</td><td>直到被覆盖、会话结束或存储被丢弃</td></tr></tbody></table><p>跨文件搬移使用两次调用：先对源执行 <code>CUT 1* @fn</code>，再对目标执行 <code>PUT &lt;1 @fn</code>。这是被认可 的形式；根据 §13.2，<code>Edit</code> 是单路径的。</p><p>同一次调用中出现两个或更多未被粘贴的匿名 <code>CUT</code>，会让下一次匿名粘贴产生歧义。它会失败并 在消息中给出寄存器语法，而不是挑选最近一次捕获。</p><p>命名寄存器持有的是被捕获的内容，而不是活引用。之后删除或移动源文件不会使它失效。</p><h2 id="_8-下降与应用" tabindex="-1">8. 下降与应用 <a class="header-anchor" href="#_8-下降与应用" aria-label="Permalink to &quot;8. 下降与应用&quot;">​</a></h2><h3 id="_8-1-中间表示" tabindex="-1">8.1 中间表示 <a class="header-anchor" href="#_8-1-中间表示" aria-label="Permalink to &quot;8.1 中间表示&quot;">​</a></h3><p>解析器把每个操作下降为一个由五种编辑种类组成的扁平列表，全部锚定在原始行号上：</p><table tabindex="0"><thead><tr><th>种类</th><th>由谁产生</th></tr></thead><tbody><tr><td><code>insert{cursor, text, mode?}</code></td><td>任何 <code>PUT</code> 的正文行</td></tr><tr><td><code>delete{anchor}</code></td><td>被范围或块消耗的每一行</td></tr><tr><td><code>cut{range, register?}</code></td><td><code>CUT</code></td></tr><tr><td><code>paste{target, register?}</code></td><td>寄存器 <code>PUT</code></td></tr><tr><td><code>block{anchor, payloads, mode?, register?}</code></td><td>任何 <code>N*</code> 定位符，在解析之前</td></tr></tbody></table><p>一次多行替换分解为每个正文行一个 <code>insert</code> 加每个被消耗行一个 <code>delete</code>。由于每个锚点都 索引原始快照，所有位置只计算一次；没有操作会观察到另一个操作的效果。</p><h3 id="_8-2-块解析" tabindex="-1">8.2 块解析 <a class="header-anchor" href="#_8-2-块解析" aria-label="Permalink to &quot;8.2 块解析&quot;">​</a></h3><p><code>block</code> 编辑在解析时不带跨度。<code>resolve_blocks(text, path, line)</code> 在校验之前运行，返回 一个闭区间 <code>{start, end}</code> 或 <code>None</code>。</p><p>在以下任一情况下解析返回 <code>None</code>——并且该操作以一条可操作的消息被拒绝：</p><ul><li>扩展名不在受支持的语法集合内；</li><li>行号超出文件范围；</li><li>该行没有开启任何多行构造（一条裸语句、一个闭合定界符，或该构造的最后一行）；</li><li>文件无法干净解析。</li></ul><p>首个实现中受支持的语法：</p><p><code>rust</code>、<code>typescript</code>、<code>tsx</code>、<code>javascript</code>、<code>jsx</code>、<code>json</code>、<code>markdown</code>、<code>css</code>、 <code>python</code>、<code>toml</code>、<code>yaml</code>、<code>html</code>、<code>bash</code>。</p><p>新增语法是增量的，不需要改动契约。不受支持的语言只失去块操作；每个范围与间隙操作仍然 可用，而且拒绝消息会明确这样说。</p><p>有两条解析规则属于契约，而不是实现细节：</p><ul><li><strong>前置附着节点是独立的。</strong> 声明上方的装饰器、属性或文档注释是它自己的节点。锚定声明行 会遗弃它们；锚定第一个装饰器则同时包含两者。独立的行注释绝不会被卷入某个块。</li><li><strong>Markdown 标题是块的开启者。</strong> 对 <code>##</code> 的块操作覆盖该小节，直到下一个同级或更高级 标题之前的更深标题为止。</li></ul><p>每一次解析都在工具结果中以 <code>{anchorLine, start, end, op}</code> 回显，因此锚错开启行是可见的， 而不是被静默应用。</p><p>块解析针对 tag 所命名的文本运行：tag 匹配时是实时文件，文件已漂移时是被 tag 命名的快照 （以便 §10 重映射所得跨度）。当需要该快照而它不可用时，跨度无法定位，调用被拒绝。</p><h3 id="_8-3-剪贴板前置处理" tabindex="-1">8.3 剪贴板前置处理 <a class="header-anchor" href="#_8-3-剪贴板前置处理" aria-label="Permalink to &quot;8.3 剪贴板前置处理&quot;">​</a></h3><p>在普通应用之前，<code>cut</code> 与 <code>paste</code> 被展开：</p><ol><li><code>cut</code> 把该范围的当前行捕获到它的寄存器，并下降为每行一个 <code>delete</code>。</li><li><code>paste</code> 展开为每个被捕获行一个普通 <code>insert</code>。跨度目标额外展开为逐行 <code>delete</code>——<strong>只在 寄存器读取成功之后</strong>，因此从空寄存器粘贴不会留下孤立的删除。</li></ol><p>前置处理先校验时序，并用它自己的消息报告：在任何捕获之前粘贴、以及覆盖未被粘贴的匿名 内容，都是与 tag 不匹配不同的错误，不得被报告为同一件事。</p><h3 id="_8-4-替换边界修复" tabindex="-1">8.4 替换边界修复 <a class="header-anchor" href="#_8-4-替换边界修复" aria-label="Permalink to &quot;8.4 替换边界修复&quot;">​</a></h3><p>一个吞掉了一行未改动边界的范围，或一个重述了刚好在范围之外幸存行的正文，会由一次有界的 候选搜索修复。该修复刻意保守：</p><ul><li>对紧邻范围之外一行的精确重述，仅凭行相等即被规范化掉。</li><li>如果写出的结果仍然无法解析，搜索可以保留该范围的第一行或有效最后一行，并且可以把这种 保留与回声移除结合起来。</li><li>保留绝不仅凭解析成功：在一个原本可解析的基线上，删掉该行本身必须破坏语法，并且候选还 必须满足源范围结构与缩进证据。</li><li><strong>在最小修复代价上并列的不同候选文本被拒绝，而不是排序。</strong> 一个通常正确的猜测正是整份 规格要消除的失败模式。</li></ul><p>每次修复都发出一条说明它改动了什么的警告。</p><h2 id="_9-校验闸门" tabindex="-1">9. 校验闸门 <a class="header-anchor" href="#_9-校验闸门" aria-label="Permalink to &quot;9. 校验闸门&quot;">​</a></h2><p><code>Edit</code> 恰好解析为四种结果之一。设 <code>expected</code> 为提供的 tag，<code>live</code> 为当前文件的 tag。</p><table tabindex="0"><thead><tr><th>分支</th><th>条件</th><th>行为</th></tr></thead><tbody><tr><td><strong>A. 直接</strong></td><td><code>live == expected</code></td><td>运行来源闸门，然后应用。锚点行号与实时文件 1:1 对应，因此已解析的块跨度会被回显。</td></tr><tr><td><strong>B. 位置稳定</strong></td><td><code>live != expected</code>，且每个操作只针对文件头或文件尾</td><td>带漂移警告应用到实时内容。<code>&gt;$</code> 与 <code>&lt;1</code> 不会被内容漂移移动，因此过时 tag 在此不是致命的。</td></tr><tr><td><strong>C. 恢复</strong></td><td><code>live != expected</code>，且存在带锚点的操作</td><td>尝试 §10。成功则带恢复警告应用；已解析的跨度<strong>不</strong>回显，因为行号已经移动。</td></tr><tr><td><strong>D. 拒绝</strong></td><td>恢复拒绝</td><td>带实时 tag、锚点行以及其附近当前内容的 <code>EDIT_TAG_MISMATCH</code>。</td></tr></tbody></table><h3 id="_9-1-来源闸门" tabindex="-1">9.1 来源闸门 <a class="header-anchor" href="#_9-1-来源闸门" aria-label="Permalink to &quot;9.1 来源闸门&quot;">​</a></h3><p>仅在分支 A 上，每个锚点行都会与其文本等于实时内容的那个快照的 <code>seen_lines</code> 比对：</p><ul><li><code>seen_lines</code> 缺失（<code>None</code>）→ 跳过闸门。</li><li>所有锚点已显示 → 应用。</li><li>否则 → 拒绝，并内联未显示锚点行的真实内容。</li></ul><p>该拒绝的设计使得一次诚实的重试无需再次 <code>Read</code> 就能成功：</p><ul><li>最多揭示 <strong>40</strong> 行未显示内容，每行在 <strong>512</strong> 个字符处剪辑。</li><li>当揭示以<strong>全宽</strong>覆盖了<strong>每一个</strong>未显示锚点行时，这些行被并入 <code>seen_lines</code>。错误中的 内容本身就是模型现在已经看过它们的证明，因此重试同一个 <code>tag</code> 即可应用。</li><li>当揭示被任一上限截断时，<strong>不</strong>并入任何行，消息保留其“重新读取该范围”的指示。</li></ul><p>这种不对称是承重的。并入被截断的揭示会让一次盲目的宽范围编辑被拆成若干低于上限的重试， 每次揭示一个切片，最终在没有任何读取的情况下应用；也会让一行被压缩的内容在其大部分宽度 仍未被显示的情况下被洗进 <code>seen_lines</code>。</p><h3 id="_9-2-路径恢复" tabindex="-1">9.2 路径恢复 <a class="header-anchor" href="#_9-2-路径恢复" aria-label="Permalink to &quot;9.2 路径恢复&quot;">​</a></h3><p>当 <code>path</code> 在磁盘上不存在，但其<strong>基名与 tag</strong> 一起恰好匹配会话记录过的唯一一个文件时， 编辑被重新绑定到该文件并发出一条警告。要求：</p><ul><li>基名与 tag 都必须匹配；</li><li>恰好一个候选——并列则拒绝；</li><li>排除写出路径自身记录的快照，因此被删除的文件不能恢复到它自己的历史上；</li><li>重新绑定发生在写权限闸门<strong>之前</strong>，因此拼错的路径会解析到它真实且可写的位置，而不是对着 模型从未指向的路径失败。</li></ul><h3 id="_9-3-空操作与重复保护" tabindex="-1">9.3 空操作与重复保护 <a class="header-anchor" href="#_9-3-空操作与重复保护" aria-label="Permalink to &quot;9.3 空操作与重复保护&quot;">​</a></h3><p>一次产生与输入完全相同文本的应用返回 <code>EDIT_NO_CHANGE</code>，而不是成功。</p><p>§4d 的重复保护仍然会终止一个在同一路径上反复失败的提示，但它不再对每种失败一视同仁。 三个可恢复代码——<code>EDIT_TAG_MISMATCH</code>、<code>EDIT_TAG_UNKNOWN</code>、<code>EDIT_LINES_UNSEEN</code>——各自都 交回了重试所需的东西：实时 tag，或主机拒绝盲写的那些行的内容（§9.1）。一次诚实的重试正是 针对它们的设计中动作，因此<strong>每个代码在每条路径上有一次免费尝试</strong>，之后才开始计数。其他每 一个代码——格式错误的操作、无效范围、空操作应用——在第一次出现时就计数，因为重复其中之一 意味着模型在猜。</p><table tabindex="0"><thead><tr><th>一个提示内同一路径上的序列</th><th>结果</th></tr></thead><tbody><tr><td><code>EDIT_TAG_MISMATCH</code>，然后 <code>EDIT_LINES_UNSEEN</code></td><td>两者都不计数：两次不同的诚实失败，各有自己的宽限</td></tr><tr><td>两次 <code>EDIT_TAG_MISMATCH</code></td><td>第二次计为尝试 1</td></tr><tr><td><code>EDIT_PARSE_FAILED</code> 三次</td><td>第 3 次尝试——本轮停止</td></tr><tr><td>一次失败，然后一次成功 <code>Edit</code>，然后一次失败</td><td>尝试 1——落盘的写入清除该路径的历史</td></tr></tbody></table><p>宽限按代码计数，而不是按调用计数，因此过时 tag 后跟未显示行是两次不同的诚实失败，而同一个 代码两次不是。</p><p>当计数确实达到上限时，工具结果携带 <code>terminate: true</code>，代理循环在该批次之后停止。停在那里 不得留下一个只是结束了的 turn：runtime 用 <code>MUTATION_RETRY_BUDGET_EXHAUSTED</code> 敲定该 assistant 行——可重试，<code>details.kind</code> 为 <code>edit</code> 或 <code>patch-command</code>，并带上最后一个错误代码——并发出一 条对应的错误事件，因此用户看得到代理是有意停止的，并保留继续的入口。一个没有消息的被终止 turn 与一个选择什么都不说的模型无法区分。</p><h2 id="_10-漂移恢复" tabindex="-1">10. 漂移恢复 <a class="header-anchor" href="#_10-漂移恢复" aria-label="Permalink to &quot;10. 漂移恢复&quot;">​</a></h2><p>恢复先证明每个锚点仍然映射到唯一一个未改动区域，然后针对实时内容重放操作。它绝不会编辑 被 tag 命名的快照并把它写出去。</p><ol><li>查找 tag 等于 <code>expected</code> 的快照。缺失 → 拒绝。</li><li>对快照文本与实时文本做差分，只用<strong>未改动</strong>的行构建映射：<code>old_line → new_line</code>。</li><li>每个锚点都必须存在于映射中。缺失 → 拒绝。 对 <code>cut</code> 与跨度 <code>paste</code>，<strong>每一个被捕获的内部行</strong>都是锚点：被改动的内部内容无法安全搬移。</li><li>校验每个锚点的周围上下文： <ul><li>如果锚点文本在旧文件与新文件中都唯一，至少一个相邻的非锚点行必须以相同偏移量映射；</li><li>如果该文本在任一侧重复出现，<strong>两个</strong>相邻的非锚点行都必须以相同偏移量映射。</li></ul></li><li>所有锚点必须移动<strong>同一个一致的偏移量</strong>。任何分歧都导致拒绝。</li><li>针对实时内容重放。应用失败，或重放什么都没改变，都导致拒绝。</li></ol><p>恢复警告区分成因，因为纠正动作不同：</p><table tabindex="0"><thead><tr><th>警告</th><th>条件</th></tr></thead><tbody><tr><td>外部变更</td><td>被 tag 命名的快照仍是头部——会话之外的东西写了该文件</td></tr><tr><td>会话链</td><td>tag 命名了一个更旧的版本——模型复用了会话中较早的过时 tag</td></tr><tr><td>行重映射</td><td>锚点以非零偏移量移动</td></tr></tbody></table><h2 id="_11-错误" tabindex="-1">11. 错误 <a class="header-anchor" href="#_11-错误" aria-label="Permalink to &quot;11. 错误&quot;">​</a></h2><table tabindex="0"><thead><tr><th>代码</th><th>可重试</th><th>含义</th></tr></thead><tbody><tr><td><code>EDIT_TAG_REQUIRED</code></td><td>否</td><td><code>tag</code> 缺失或不是 4 位十六进制</td></tr><tr><td><code>EDIT_TAG_MISMATCH</code></td><td><code>Read</code> 之后可以</td><td>tag 无法哈希出实时文件且恢复拒绝；携带实时 tag 与锚点处的当前内容</td></tr><tr><td><code>EDIT_TAG_UNKNOWN</code></td><td><code>Read</code> 之后可以</td><td>tag 格式正确，但本会话没有为该路径记录过对应内容</td></tr><tr><td><code>EDIT_LINES_UNSEEN</code></td><td>是</td><td>锚点引用了从未显示过的行；携带被揭示的内容</td></tr><tr><td><code>EDIT_PARSE_FAILED</code></td><td>否</td><td>头格式错误、无冒号头下出现正文行、缺少正文、出现 <code>-</code>/上下文行</td></tr><tr><td><code>EDIT_RANGE_INVALID</code></td><td>否</td><td>范围反向、行号越界、操作重叠、锚点重复</td></tr><tr><td><code>EDIT_BLOCK_UNRESOLVED</code></td><td>否</td><td><code>N*</code> 无法解析；给出纯范围替代方案</td></tr><tr><td><code>EDIT_REGISTER_EMPTY</code></td><td>否</td><td>从未设置的寄存器粘贴</td></tr><tr><td><code>EDIT_REGISTER_AMBIGUOUS</code></td><td>否</td><td>存在多个待粘贴匿名捕获时进行匿名粘贴</td></tr><tr><td><code>EDIT_REPAIR_AMBIGUOUS</code></td><td>否</td><td>边界修复候选在最小代价上并列</td></tr><tr><td><code>EDIT_NO_CHANGE</code></td><td>否</td><td>应用产生了完全相同的文本</td></tr><tr><td><code>EDIT_AMPLIFICATION_LIMIT</code></td><td>否</td><td>下降展开超过膨胀上限</td></tr></tbody></table><p>这些全部属于 <code>Edit</code> 作用域，并且是对 <a href="/zh-CN/spec/03-runtime/08-error-codes">08-error-codes</a> §3.4 的增量。已有的 <code>INVALID_ARGUMENT</code>、<code>TOOL_DENIED</code>、<code>PATH_OUTSIDE_WORKSPACE</code> 与 <code>TOOL_FAILED</code> 语义不变；<code>Edit</code> 不再把版本或来源问题报告为通用的 <code>TOOL_FAILED</code>，因为两者 都能以一个具体的下一步动作恢复。</p><p><code>MUTATION_RETRY_BUDGET_EXHAUSTED</code> 不在这张表里，因为它不是一个 <code>Edit</code> 结果：工具调用已经 以上面某个代码失败了，而 runtime 在重复保护终止本轮时，把该代码加到它写出的 assistant 行上 （§9.3、<a href="/zh-CN/spec/03-runtime/08-error-codes">08-error-codes</a> §3.3）。</p><p>每条错误消息都给出一个具体的下一步。当主机已经知道范围时，只说“重新读取并重试”而不给出 范围是不可接受的消息。</p><h2 id="_12-限制" tabindex="-1">12. 限制 <a class="header-anchor" href="#_12-限制" aria-label="Permalink to &quot;12. 限制&quot;">​</a></h2><table tabindex="0"><thead><tr><th>限制</th><th>取值</th><th>达到上限时的行为</th></tr></thead><tbody><tr><td><code>ops</code> 负载</td><td>256 KB</td><td><code>INVALID_ARGUMENT</code></td></tr><tr><td>每次调用操作数</td><td>200</td><td><code>INVALID_ARGUMENT</code></td></tr><tr><td>展开后的 IR 行数</td><td>100 000</td><td><code>EDIT_AMPLIFICATION_LIMIT</code></td></tr><tr><td>未显示行揭示</td><td>40 行</td><td>截断揭示，不并入任何行</td></tr><tr><td>揭示行宽</td><td>512 字符</td><td>剪辑、标记为截断、不并入</td></tr><tr><td>寄存器捕获</td><td>4096 行 / 1 MiB</td><td>在该 <code>CUT</code> 上返回 <code>INVALID_ARGUMENT</code></td></tr><tr><td>每会话命名寄存器数</td><td>16</td><td>LRU 淘汰最冷的</td></tr><tr><td>块解析文件上限</td><td>每文件 4 MiB</td><td>拒绝该块操作</td></tr><tr><td>快照存储</td><td>§4.4</td><td>LRU 淘汰</td></tr></tbody></table><h2 id="_13-与既有子系统的交互" tabindex="-1">13. 与既有子系统的交互 <a class="header-anchor" href="#_13-与既有子系统的交互" aria-label="Permalink to &quot;13. 与既有子系统的交互&quot;">​</a></h2><h3 id="_13-1-审核快照与回滚-adr-0043" tabindex="-1">13.1 审核快照与回滚（ADR 0043） <a class="header-anchor" href="#_13-1-审核快照与回滚-adr-0043" aria-label="Permalink to &quot;13.1 审核快照与回滚（ADR 0043）&quot;">​</a></h3><p><code>review::prepare_change</code> 只以 <code>tool_name</code> 与 <code>args.path</code> 为键，两者都保持不变，因此常见 情形下的工具前捕获无需修改。需要两处新增：</p><ul><li><code>MV DEST</code> 产生一次源删除与一次目标创建。它在一次工具调用下记录为两条审核条目，回滚 要么同时恢复两者，要么都不恢复。</li><li><code>REM</code> 记录为一次删除，其回滚恢复被捕获的字节。</li></ul><p>回滚的工具后哈希检查不受影响：它使用完整摘要，而不是 16 位 tag。回滚之后必须使该路径的 会话快照存储失效，否则模型会持有一个描述被回滚替换内容的 tag。</p><h3 id="_13-2-单路径-edit" tabindex="-1">13.2 单路径 <code>Edit</code> <a class="header-anchor" href="#_13-2-单路径-edit" aria-label="Permalink to &quot;13.2 单路径 \`Edit\`&quot;">​</a></h3><p>权限闸门、审核快照、工件行（<code>crates/host-core/src/rpc/mod.rs:2550</code>）与按会话的突变许可 全部以单个 <code>args.path</code> 为键。因此 <code>Edit</code> 保持单路径，跨文件搬移使用跨两次调用的会话作用域 命名寄存器（§7.5）。</p><h3 id="_13-3-突变排序" tabindex="-1">13.3 突变排序 <a class="header-anchor" href="#_13-3-突变排序" aria-label="Permalink to &quot;13.3 突变排序&quot;">​</a></h3><p>§4d 的序列化不变，并且变得更加重要：快照存储会被生产者与 <code>Edit</code> 同时修改，而同一会话上的 两次并发突变可能把一次记录插到校验与写入之间。既有的按会话突变许可已经排除了这一点。</p><h3 id="_13-4-渲染器" tabindex="-1">13.4 渲染器 <a class="header-anchor" href="#_13-4-渲染器" aria-label="Permalink to &quot;13.4 渲染器&quot;">​</a></h3><p>Edit 行改为从审核记录的 hunks 渲染——ADR 0043 已经在产出它们——并原样展示操作头作为 模型陈述的意图。已解析的块跨度以及 §8.4、§9.2、§10 的每一条警告都在该行上呈现，而不是 被吞掉。</p><h3 id="_13-5-子代理" tabindex="-1">13.5 子代理 <a class="header-anchor" href="#_13-5-子代理" aria-label="Permalink to &quot;13.5 子代理&quot;">​</a></h3><p>子代理是独立会话，因此拥有独立的快照存储与独立的寄存器。父代理不能把 tag 交给子代理， 子代理在编辑之前必须 <code>Read</code>。这是正确的默认：来源按读取者隔离。</p><h3 id="_13-6-工具描述与提示词" tabindex="-1">13.6 工具描述与提示词 <a class="header-anchor" href="#_13-6-工具描述与提示词" aria-label="Permalink to &quot;13.6 工具描述与提示词&quot;">​</a></h3><p>host-core 中的 <code>builtin_tool_defs()</code> 与 <code>packages/agent-runtime/src/runtime.ts</code> 中的 <code>buildToolDefinitions()</code> 必须携带同一张操作表、同一套正文行规则与同一批反模式。面向模型 的指引是契约的一部分：操作表、仅 <code>+</code> 正文规则、“范围只命名被改动的行”、“每次编辑之后 重新定位”，以及成文的反模式（把空 <code>PUT</code> 当作删除、按编辑后内容确定范围大小、<code>-</code>/上下文 行、为纯插入使用加宽的 <code>PUT</code>、把 <code>&gt;N*</code> 锚定在闭合行上、在寄存器粘贴之下写正文行）。</p><h2 id="_14-与-oh-my-pi-的-hashline-的刻意偏离" tabindex="-1">14. 与 <code>oh-my-pi</code> 的 <code>hashline</code> 的刻意偏离 <a class="header-anchor" href="#_14-与-oh-my-pi-的-hashline-的刻意偏离" aria-label="Permalink to &quot;14. 与 \`oh-my-pi\` 的 \`hashline\` 的刻意偏离&quot;">​</a></h2><table tabindex="0"><thead><tr><th>偏离</th><th>原因</th></tr></thead><tbody><tr><td>每次 <code>Edit</code> 调用一个 section，而不是多 section 补丁</td><td>PI-Desktop 有四个子系统以单个 <code>args.path</code> 为键（§13.2）；命名寄存器已经覆盖跨文件搬移</td></tr><tr><td>使用 SHA-256 低 16 位而不是 <code>xxHash32</code></td><td>复用 host-core 已有原语；不新增依赖</td></tr><tr><td>快照存储仅在内存中，从不持久化</td><td>跨重启存活的 tag 会比证明它的那些读取活得更久</td></tr><tr><td>没有 <code>apply_patch</code> / <code>replace</code> 回退模式，也没有按模型的排除名单</td><td>ADR 0087 §1：只有一套写入契约</td></tr><tr><td>内部 URL 方案（<code>artifact://</code>、<code>xd://</code>……）不作为 <code>Edit</code> 目标</td><td>超出范围；PI-Desktop 的路径规则仍按 03-tools-and-permissions §4 的规定</td></tr><tr><td>存储界限为 64 路径 / 8 MiB，而不是 30 / 64 MiB</td><td>host-core 是持有许多会话的长生命周期桌面进程，而不是每次调用一个的 CLI</td></tr></tbody></table><h2 id="_15-分期" tabindex="-1">15. 分期 <a class="header-anchor" href="#_15-分期" aria-label="Permalink to &quot;15. 分期&quot;">​</a></h2><p>每一期都可独立发布，并让契约保持自洽。</p><table tabindex="0"><thead><tr><th>期</th><th>内容</th><th>退出标准</th></tr></thead><tbody><tr><td>1</td><td>快照存储、把 <code>session_id</code> 贯穿到 <code>execute_tool_with_path_access</code>、为 <code>Read</code>/<code>Grep</code>/<code>Write</code> 加 tag、带行号的 <code>Read</code></td><td><strong>已上线。</strong> tag 往返可用</td></tr><tr><td>2</td><td>范围与间隙（<code>PUT N.=M:</code>、<code>PUT &lt;N:</code>、<code>PUT &gt;N:</code>、<code>PUT &gt;$:</code>、<code>CUT N.=M</code>）、tag 校验、来源闸门、<code>REM</code>/<code>MV</code></td><td><strong>已上线。</strong> 移除 <code>old_string</code>；§9 的分支 A/B/D 上线</td></tr><tr><td>3</td><td>漂移恢复（§10）与路径恢复（§9.2）</td><td>分支 C 上线</td></tr><tr><td>4</td><td>tree-sitter 块操作（<code>N*</code>、<code>&gt;N*</code>）及其解析回显</td><td>块操作在不受支持的语言上干净拒绝（今天是 <code>EDIT_BLOCK_UNRESOLVED</code>）</td></tr><tr><td>5</td><td>寄存器（§7.5）与边界修复（§8.4）</td><td>命名 <code>CUT</code>/<code>PUT @name</code> 捕获；边界修复并列被拒绝</td></tr></tbody></table><p>第 2 期是旧契约的不可回头点，必须与 §13.4 的渲染器改动以及 §13.6 的提示词改动在同一个 版本中发布。</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("zh-CN/spec/03-runtime/18-line-anchored-edit-contract.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _18LineAnchoredEditContract = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _18LineAnchoredEditContract as default
};
