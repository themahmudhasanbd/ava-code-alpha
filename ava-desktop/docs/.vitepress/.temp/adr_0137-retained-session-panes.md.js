import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0137: Retained Session Panes","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0137-retained-session-panes.md","filePath":"adr/0137-retained-session-panes.md","lastUpdated":1788621504000}');
const _sfc_main = { name: "adr/0137-retained-session-panes.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0137-retained-session-panes" tabindex="-1">ADR 0137: Retained Session Panes <a class="header-anchor" href="#adr-0137-retained-session-panes" aria-label="Permalink to &quot;ADR 0137: Retained Session Panes&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-08-30</li><li>Deciders: PI-Desktop core</li><li>Related: D162, D261, D317, D324, ADR 0120, ADR 0127, ADR 0130, D142, <code>04-ux/08-component-spec.md</code> §3.5 / §7, <code>04-ux/09-interaction-patterns.md</code> §5, E2E-011, E2E-071d, E2E-071g, E2E-177, E2E-183</li><li>Amends: D162 (the dimmed keep-alive frame), ADR 0130 clause 4 and clause 5 (per-session window reset and first-commit hydration), D162&#39;s &quot;activation resets manual-scroll state&quot; rule for revisits</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>D162 already avoided the worst session-switch artifact: it kept the last settled transcript mounted instead of tearing it down to a skeleton. The switch still flashed, because keeping <em>a</em> transcript mounted is not the same as keeping <em>the destination&#39;s</em> transcript mounted.</p><p>The store held one <code>messages: UiMessage[]</code> for the active session, and a switch replaced that array wholesale. One <code>ChatTranscript</code> instance served every session, so on a <code>sessionId</code> change it had to reset its scroll position, its mounted-row window, and its hydration state by hand, and every row remounted because row keys are message-scoped. Markdown was reparsed and Shiki retokenized for a transcript the user had read a moment earlier.</p><p>Three visible costs followed:</p><ul><li>The kept frame was <strong>dimmed</strong> (<code>opacity: .82</code>, <code>pointer-events: none</code>) under a thin progress track. On a warm revisit the destination content was already in renderer memory, so the dim was a wait animation for work that did not need to happen.</li><li><strong>Scroll position was not the pane&#39;s</strong>. A single scroller cannot hold two reading positions, so returning to a session the user had scrolled up in restarted at the bottom, and the reset had to be explicit to avoid inheriting the <em>previous</em> session&#39;s offset instead.</li><li>The <strong>two-stage progressive hydration</strong> of ADR 0130 clause 5 ran on every switch: mount the trailing 15 rows for one frame behind a <code>100vh</code> spacer, then expand in a rAF and re-anchor. It exists only because the component instance is shared, and it is the machinery most able to produce the jump it is there to prevent.</li></ul><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><code>ChatSurface</code> mounts one <strong><code>SessionPane</code> per retained session id</strong>, keyed by session id. A pane owns its transcript DOM, its scroll position, and its mounted-row window for its whole lifetime, so a switch is a visibility swap, not a rebuild.</li><li>Retention is a <strong>bounded LRU of 3 panes</strong>: the visible one plus the two most recently visited. <code>retainedSessionIds</code> holds that order in the store and eviction unmounts the oldest pane.</li><li>An inactive pane stays mounted, hidden with <code>visibility: hidden</code> + <code>content-visibility: hidden</code>, <code>aria-hidden</code>, and non-interactive. It is explicitly <strong>not</strong> <code>display: none</code>: that destroys the layout box, which is what holds <code>scrollTop</code>, so hiding by display would reset every retained reading position and defeat the whole decision.</li><li><code>messages</code> remains the <strong>live projection of the store-active session</strong>. The store adds a bounded per-session snapshot record, <code>retainedTranscripts</code>. A pane renders <code>messages</code> when its session is the store-active one, and its own snapshot otherwise, so no pane ever renders another session&#39;s rows.</li><li>A <strong>warm switch</strong> — the destination already has a retained pane — reveals that pane immediately from its retained content. The first frame is already correct: no dim, no skeleton, no remount. The revalidated transcript lands in the same pane afterwards with no visible change. If the destination is still running, or still holds a completed reply the durable page has not caught up to, that revalidation stitches the bounded durable page onto the live snapshot in chronological order (older live rows before the page, in-flight or not-yet-flushed tail after it) so D261&#39;s trailing mounted window still shows the newest turn (D317, D324). Live provenance is cleared only once that page already contains every live row.</li><li>A <strong>cold switch</strong> — no retained pane — keeps the currently visible pane showing its own session until the destination commits. Only the thin progress track marks the wait. The opacity dim is removed entirely.</li><li><strong>Scroll retention is per pane.</strong> A pane restores its own position when it becomes visible again: a pane still pinned re-anchors to the bottom, a pane the user had scrolled up in returns to that offset. First activation of a session still settles at its newest turn.</li><li>The <strong>first commit is per pane and local</strong>. Because the component instance belongs to one session, &quot;the first commit of this pane&quot; is plain mount state; the cross-session hydration spacer, its rAF expansion, and its re-anchor are deleted. ADR 0130&#39;s trailing window, two-stage escalation, and prepend anchor are unchanged inside a pane.</li><li>The <strong>composer is mounted once</strong> for the whole chat surface rather than once per branch — it already carries a per-session draft cache. It stays non-interactive while the visible pane is not yet the store-active session, so a prompt cannot be sent to the session being left.</li></ol><p>No IPC, storage, host protocol, or pagination change. Transcript reads, the latest-wins navigation generation, and the five-snapshot transcript cache of D162 are untouched; panes are a renderer-side presentation bound, not a second cache.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Retained renderer memory grows by at most two extra mounted transcripts, each itself bounded by ADR 0130&#39;s window. The ceiling is the pane budget times the window, not the number of sessions visited.</li><li>A revisit within the budget costs no Markdown parse, no Shiki tokenization, and no scroll correction — the frame the user left is the frame they return to.</li><li>Deleted: the <code>session-switching</code> opacity dim, the session-loading skeleton path, the cross-session scroll/window/hydration resets inside <code>ChatTranscript</code>, the <code>100vh</code> hydration spacer, and the per-branch composer mount.</li><li>Four invariants callers must keep: <ul><li><strong>Cross-session events must not write the visible projection.</strong> Background message, tool, and completion events update their own session&#39;s snapshot only; writing <code>messages</code> from a non-store-active session would paint another session&#39;s rows into the visible pane. This is the same boundary D142 draws for work-panel contexts.</li><li><strong>Deleting a session must release its pane.</strong> Removing a session drops its <code>retainedTranscripts</code> entry and its <code>retainedSessionIds</code> slot, so no pane outlives its session or holds a snapshot of deleted history.</li><li><strong>Leaving the chat with no active session must drop every pane.</strong> Switching or clearing the project makes the retained sessions unreachable. Because the visible pane is the head of the retained order, a pane left behind would keep the previous project&#39;s conversation on screen and suppress the empty state.</li><li><strong>A hidden pane performs no reading work.</strong> Follow scrolling, history pagination, and row-position measurement all depend on a rendered scroller; a hidden pane&#39;s scroller reports <code>scrollTop === 0</code>, which reads as &quot;at the top&quot; and would page history for a session nobody is looking at. Hidden panes keep their DOM and their offset, and nothing else.</li></ul></li><li>Per-session scroll retention <strong>supersedes</strong> the older rule that activation resets inherited manual-scroll state, for revisits: a revisited pane is expected to return to where the user left it. First activation is unchanged and still settles at the newest turn, and no pane may ever show another session&#39;s offset.</li><li>An evicted session behaves like a cold open. This is intentional and must stay indistinguishable from a first visit rather than surfacing an error or an empty frame.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Replace <code>messages</code> with a per-session message map:</strong> rejected. It moves the same wholesale swap one level down — a single transcript instance still reads one entry at a time, so the row remount, the Markdown rebuild, and the shared scroller all survive. The flash is a component-identity problem, not a store shape problem.</li><li><strong>Deep-equality reuse of the cached array:</strong> rejected. Making a revalidated read reference-equal to the cached array suppresses one re-render but leaves the destination sharing one instance with the session being left, so the first frame after a switch is still built from scratch. It also pays a deep walk over the transcript on every revalidation to save work the pane already saves structurally.</li><li><strong>Keep the dim, only skip it when the cache is warm:</strong> rejected. The dim was never the affordance for a warm switch; a correct destination frame is. Keeping it as a conditional leaves two switch appearances to reason about and two states to test, for a wait that the retained pane has already removed. The thin progress track is enough for the cold case.</li><li><strong>Retain every visited session&#39;s pane:</strong> rejected. Unbounded panes reintroduce exactly the unbounded retention ADR 0130 removed, on a low-memory machine that reported the chat area degrading. Three panes cover the observed switch-and-return pattern; the fourth is a cold open.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0137-retained-session-panes.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0137RetainedSessionPanes = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0137RetainedSessionPanes as default
};
