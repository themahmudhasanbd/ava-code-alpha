import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0063: A Managed Surface for Global Subagent Definitions","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0063-subagent-management-ui.md","filePath":"adr/0063-subagent-management-ui.md","lastUpdated":1787243067000}');
const _sfc_main = { name: "adr/0063-subagent-management-ui.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0063-a-managed-surface-for-global-subagent-definitions" tabindex="-1">ADR 0063: A Managed Surface for Global Subagent Definitions <a class="header-anchor" href="#adr-0063-a-managed-surface-for-global-subagent-definitions" aria-label="Permalink to &quot;ADR 0063: A Managed Surface for Global Subagent Definitions&quot;">​</a></h1><ul><li>Status: Accepted (amended by ADR 0112)</li><li>Date: 2026-08-06</li><li>Deciders: PI-Desktop core</li><li>Related: D202, ADR 0062 (bounded subagents), ADR 0112</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>ADR 0062 shipped subagents without a renderer surface. A user could create a personal delegate only by placing a Markdown document in an implementation- specific location, and the shipped catalog was not visible in Settings. The management surface must not write tracked project files or silently introduce a project-level source that differs from the runtime contract.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><h3 id="_1-global-documents-are-the-only-user-managed-subagent-source" tabindex="-1">1. Global documents are the only user-managed subagent source <a class="header-anchor" href="#_1-global-documents-are-the-only-user-managed-subagent-source" aria-label="Permalink to &quot;1. Global documents are the only user-managed subagent source&quot;">​</a></h3><p>User-owned definitions are Markdown files in:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>~/.agents/subagents/&lt;id&gt;.md</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>There is no project-level subagent directory. The application does not scan or write <code>.pi/agents</code> for capability management. <code>id == name</code> remains the model&#39;s <code>Task</code> handle, duplicate names are rejected, and the runtime&#39;s global user catalog is combined with the builtins without a project capability layer.</p><h3 id="_2-activation-is-app-local" tabindex="-1">2. Activation is app-local <a class="header-anchor" href="#_2-activation-is-app-local" aria-label="Permalink to &quot;2. Activation is app-local&quot;">​</a></h3><p>The document owns prompt metadata (<code>name</code>, <code>description</code>, <code>tools</code>, <code>model</code>, <code>thinkingLevel</code>, and <code>maxTurns</code>) but never owns <code>enabled</code>. The enabled state is stored in <code>&lt;data&gt;/agent-capabilities/subagents.json</code>. Scanning the global folder removes state for deleted documents, so a missing file never leaves a visible pending row or an orphaned override.</p><h3 id="_3-the-settings-page-is-global-only" tabindex="-1">3. The Settings page is global-only <a class="header-anchor" href="#_3-the-settings-page-is-global-only" aria-label="Permalink to &quot;3. The Settings page is global-only&quot;">​</a></h3><p>Settings &gt; Agent &gt; Subagents is one fixed-height global list. It has no project picker, project column, or add/import action in this surface. Each row shows the frontmatter summary and a local enable switch; toggling it writes only the app-local state file and takes effect on the next runtime catalog load.</p><p>Skills and MCP have separate Settings destinations and are not tabs inside Extensions. Extensions itself contains only Installed and Marketplace.</p><h3 id="_4-the-runtime-remains-the-source-of-truth" tabindex="-1">4. The runtime remains the source of truth <a class="header-anchor" href="#_4-the-runtime-remains-the-source-of-truth" aria-label="Permalink to &quot;4. The runtime remains the source of truth&quot;">​</a></h3><p>Electron supplies the global user documents to <code>loadSubagentDefinitions</code> on the next prompt. The renderer does not reimplement catalog precedence or invent a project source. Builtins and malformed documents are handled by the runtime loader; the management list is the scanned global user-document list.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Personal delegates follow the user across projects without modifying a repository.</li><li>Teams that need a repository-specific prompt must use the project&#39;s normal instruction mechanism; <code>.pi/agents</code> is not a capability source.</li><li>The UI can show and persist local enablement without changing Markdown files.</li><li>There is no project-scoped subagent toggle, so project precedence applies to Skills and MCP only.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0063-subagent-management-ui.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0063SubagentManagementUi = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0063SubagentManagementUi as default
};
