import { ssrRenderAttrs, ssrInterpolate } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Composer Prompt Enhancement","description":"","frontmatter":{},"headers":[],"relativePath":"spec/04-ux/12-prompt-enhancement.md","filePath":"spec/04-ux/12-prompt-enhancement.md","lastUpdated":1789795215000}');
const _sfc_main = { name: "spec/04-ux/12-prompt-enhancement.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="composer-prompt-enhancement" tabindex="-1">Composer Prompt Enhancement <a class="header-anchor" href="#composer-prompt-enhancement" aria-label="Permalink to &quot;Composer Prompt Enhancement&quot;">​</a></h1><h2 id="_1-scope" tabindex="-1">1. Scope <a class="header-anchor" href="#_1-scope" aria-label="Permalink to &quot;1. Scope&quot;">​</a></h2><p>The prompt-enhancement capability supports a one-shot <code>Enhance prompt</code> request for a non-empty draft. The Composer renders it as a standalone Sparkles action between the combined model × reasoning selector and the single Stop/Send submit slot. When invoked, the request rewrites only the draft text with the model currently displayed in the Composer. Inline file-reference chips, including pasted image chips, remain unchanged and do not disable the action.</p><p>This is a v1 utility action, not an agent turn: it does not append a message, read session history, run tools, or persist a transcript row.</p><h2 id="_2-availability-and-interaction" tabindex="-1">2. Availability and interaction <a class="header-anchor" href="#_2-availability-and-interaction" aria-label="Permalink to &quot;2. Availability and interaction&quot;">​</a></h2><p>The Sparkles action is enabled only when all of the following are true:</p><ul><li>the draft is non-empty after trimming;</li><li>the effective displayed provider/model is enabled and authenticated, using the same readiness predicate as Send; and</li><li>the trimmed draft does not start with <code>/</code>.</li></ul><p>While the request is running, the action is disabled and shows the shared <code>.tool-spinner</code> plus the localized <code>Enhancing…</code> label. Sending remains allowed. The Composer sends <code>providerId</code>, <code>modelId</code>, and <code>thinkingLevel</code> from the currently displayed model selector; main validates those values and falls back through session, draft, global, and provider defaults when a snapshot is missing or stale.</p><p>On success, the trimmed result replaces the text, the caret moves to the end, and a single <code>Undo enhancement</code> action restores the exact pre-enhancement text. Any user edit, send, or Composer session switch clears the undo action. There is no multi-level history, keyboard shortcut, or cancel action.</p><h2 id="_3-request-and-provider-boundary" tabindex="-1">3. Request and provider boundary <a class="header-anchor" href="#_3-request-and-provider-boundary" aria-label="Permalink to &quot;3. Request and provider boundary&quot;">​</a></h2><p>Renderer requests use the allowlisted <code>pi-desktop/prompt/enhance</code> invoke channel. Electron main resolves the effective provider/model through the same runtime launch resolver used for agent turns, reads API credentials only in main, and invokes agent-runtime&#39;s one-shot completion helper. Vendor OAuth providers receive a short-lived <code>ModelAuth</code> through the existing main-owned resolver; no key or refresh token crosses into the renderer.</p><p>The completion context is a built-in system prompt plus one user message built from a user template. The template defaults to <code>packages/shared/src/prompt-enhancement.ts</code> and can be overridden in Settings (see §5). It carries a <code>${ssrInterpolate(_ctx.draft)}</code> placeholder; every occurrence is replaced with the draft text, and the default template keeps the draft inside <code>&lt;draft&gt;</code> tags so draft text reads as content to improve rather than as instructions. The system prompt is not user-editable and states the role, the rewrite principles, an explicit do-not list (including leaving code, commands, file paths, identifiers, and other proper nouns exactly as written), language-following rules that forbid language meta notes, a length brake (do not expand beyond roughly twice the draft&#39;s length; a long draft may stay long), and the output contract.</p><p>No prior conversation, tools, attachments, or session state are included. The renderer removes its inline file-reference chip tokens before the request and restores those chips in their original order and relative position after the text response; the model is not trusted to preserve opaque renderer sentinels. The selected thinking level is passed to pi-ai, and provider setup retries use the existing bounded retry controller. When the resolved provider is OpenCode Go (or another <code>opencode.ai</code> host), the one-shot forwards the Composer session id as <code>x-opencode-session</code>; a request with no session gets a per-call id. Model output is consumed as plain text, has one matching pair of wrapping quotation marks removed, has a leading rewrite label such as <code>Enhanced:</code> stripped, and is trimmed. Empty or whitespace-only output is a <code>PROMPT_ENHANCEMENT_EMPTY</code> failure.</p><p>The handler bounds one request with a 60-second ceiling. On expiry it aborts the in-flight request (best-effort: the transport consults the signal between provider retries) and races the promise so the caller is released. The action fails with <code>TIMEOUT</code>. It does not silently retry on the session model: the user chose the pinned model, and a hidden second attempt would double the wait.</p><h2 id="_4-failure-and-race-handling" tabindex="-1">4. Failure and race handling <a class="header-anchor" href="#_4-failure-and-race-handling" aria-label="Permalink to &quot;4. Failure and race handling&quot;">​</a></h2><p>Failures preserve the current draft and render a dismissible Composer error bar containing the classified error message and code. Existing provider codes such as <code>PROVIDER_UNAUTHORIZED</code>, <code>NETWORK_ERROR</code>, and <code>TIMEOUT</code> are reused.</p><p>The renderer captures the draft key and an edit generation when starting a request. If the draft changes, is sent/cleared, or the user switches sessions before the response arrives, the response is discarded and cannot overwrite the newer draft. File chips are not included in the rewrite and are not removed by success or failure.</p><h2 id="_5-configurable-user-template-model-and-reasoning" tabindex="-1">5. Configurable user template, model, and reasoning <a class="header-anchor" href="#_5-configurable-user-template-model-and-reasoning" aria-label="Permalink to &quot;5. Configurable user template, model, and reasoning&quot;">​</a></h2><p>Settings -&gt; AI hosts a Prompt enhancement card. One row carries a switch — <code>Use a custom template</code> — and the settings icon button the subagent rows use for editing, which opens an editor sheet. The sheet holds the user template and saves on <code>Save</code>, so closing it abandons the edit; <code>Cancel</code> and <code>Escape</code> close it, and a click on the sheet backdrop closes it only when no save is in flight.</p><table tabindex="0"><thead><tr><th>Field</th><th>Effect when off or empty</th></tr></thead><tbody><tr><td><code>promptEnhancementCustomTemplate</code></td><td>the built-in user template applies</td></tr><tr><td><code>promptEnhancementUserTemplate</code></td><td>the built-in user template</td></tr></tbody></table><p>The switch is the gate, not the text, and it is enabled only once a usable custom template exists. With no saved template it renders disabled with a hint that saving one unlocks it, because it would otherwise choose between two identical states. Saving a template turns the switch on, since the user just wrote one.</p><p>Turning the switch off keeps <code>promptEnhancementUserTemplate</code>, so turning it back on restores the user&#39;s text instead of discarding it. A stored template with the switch off, or an enabled switch whose template cannot be found, both resolve to the built-in template.</p><p>The system prompt is not editable and exposes no field. It is part of the feature contract (proper-noun preservation, language following without meta notes, the length brake, the output contract), so changing it is a source change that updates this spec. host-core drops a stored system-prompt override written by an earlier build, so the store cannot hold a value nothing reads.</p><p>The template field shows the built-in default text when no override is stored, so the editor opens on the value in force. Editing the field back to the exact default text clears the override rather than storing a frozen copy, so later improvements to the default still reach users who never customized it. Never persisting the default text is deliberate.</p><p>The field offers an insert action that writes the draft variable at the caret, and a save that would leave the template without it is refused locally with a message. host-core enforces the same rules for any writer: a non-blank <code>promptEnhancementUserTemplate</code> must contain <code>${ssrInterpolate(_ctx.draft)}</code>, the value must be a string within <code>PROMPT_ENHANCEMENT_TEMPLATE_MAX_LENGTH</code>, and a blank value is stored as absent rather than as an empty string.</p><h3 id="enhancement-model-and-reasoning" tabindex="-1">Enhancement model and reasoning <a class="header-anchor" href="#enhancement-model-and-reasoning" aria-label="Permalink to &quot;Enhancement model and reasoning&quot;">​</a></h3><p>Which model runs the rewrite, and with how much reasoning, live on the same Settings -&gt; AI Prompt enhancement card as the template, as two rows below the custom-template switch. The model row is titled <code>Default model</code> and uses the same anchored, searchable menu as Settings -&gt; Models&#39; default-model row.</p><table tabindex="0"><thead><tr><th>Field</th><th>Effect when empty</th></tr></thead><tbody><tr><td><code>promptEnhancementProviderId</code> + <code>promptEnhancementModelId</code></td><td>follow the Composer&#39;s current model</td></tr><tr><td><code>promptEnhancementThinkingLevel</code></td><td><code>off</code></td></tr></tbody></table><p>Both destinations therefore offer one kind of model picker. Both rows read <code>Default model</code>; the card heading (Prompt enhancement vs Models Defaults) is what separates the conversation&#39;s default from the enhancement&#39;s. When <code>promptEnhancementProviderId</code> is set, the row still shows that pin even if the provider is gone or disabled, with an unavailable hint. Main prefers the pin and logs a warning plus falls back to the Composer&#39;s current model if it cannot be resolved: a stale pin is a preference that cannot be honoured, never a failure that disables the action.</p><p>The reasoning row lists the levels the selected model actually supports, using the same resolution the Composer applies to a turn: the model binding&#39;s <code>thinkingLevels</code>, then the live catalog, then the provider default. A model without reasoning therefore offers only <code>Off (no reasoning)</code> and disables the row, rather than presenting a ladder it cannot run. With no model pinned the request follows the conversation&#39;s model, whose ladder is not knowable here, so every canonical level is offered.</p><p>The row defaults to <code>Off</code>, with no follow-the-session option: the enhancement never inherits the conversation&#39;s effort, because a rewrite rarely benefits from reasoning and reasoning is the slow path. Changing the model re-clamps the stored level onto the new model&#39;s ladder, and the value written is the clamped one, so a stored level is always one the model can run. Round-tripping the displayed value through the same resolver keeps the row and the store in step.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("spec/04-ux/12-prompt-enhancement.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _12PromptEnhancement = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _12PromptEnhancement as default
};
