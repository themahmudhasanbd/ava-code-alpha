import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0084: Defer new-task session creation until the first message","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0084-deferred-new-task-session-creation.md","filePath":"adr/0084-deferred-new-task-session-creation.md","lastUpdated":1786678930000}');
const _sfc_main = { name: "adr/0084-deferred-new-task-session-creation.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0084-defer-new-task-session-creation-until-the-first-message" tabindex="-1">ADR 0084: Defer new-task session creation until the first message <a class="header-anchor" href="#adr-0084-defer-new-task-session-creation-until-the-first-message" aria-label="Permalink to &quot;ADR 0084: Defer new-task session creation until the first message&quot;">​</a></h1><ul><li><strong>Status:</strong> Accepted</li><li><strong>Date:</strong> 2026-08-14</li><li><strong>Related:</strong> D220 · D088 · D093 · E2E-011b · E2E-011d</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Clicking New Task created a session immediately, so the sidebar history gained a default-titled <code>新建任务</code> / &quot;New task&quot; row even when the user never typed a message. A reuse rule (D088/D093) kept at most one empty draft per project or temporary scope, but the first empty row still appeared and remained visible until the user sent a prompt that renamed it. Empty draft sessions also left rows on disk in the host database.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>New Task opens an unpersisted draft instead of creating a session:</p><ul><li><code>newSession()</code> resets the renderer to the home empty state (keeping the requested project scope) and no longer calls <code>session.create</code> nor reuses a legacy empty draft. <code>activeSessionId</code> stays unset, which the renderer already supports as its initial and project-switching state.</li><li>The first message (<code>sendPrompt</code>) or pasted files (which must attach to a session) materialize the draft through a shared <code>materializeDraftSession</code> helper that creates the session, applies any retained toolbar configuration, and commits the same navigation/run state the eager path used.</li><li>Composer and top-bar model toolbar choices made on the draft are retained in a new <code>draftConfiguration</code> store slice and applied at materialization time instead of materializing a session on a toolbar-only interaction.</li><li>The sidebar history filter now drops sessions whose title is still a default untitled value, hiding legacy empty drafts and any accidentally empty session as a defense in depth.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>The sidebar history only records tasks that actually carry input; abandoned New Task drafts leave no row and no persisted session.</li><li>No new empty sessions accumulate in the host database; legacy empty drafts remain on disk but are hidden from history.</li><li>The renderer store, Composer, and Sidebar change; protocol v9, host RPC, and storage schema v11 are untouched, so older renderers remain compatible.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li>Keep eager creation and only hide empty drafts in the sidebar: smaller change, but leaves invisible sessions on disk and keeps the reuse machinery alive for rows the user should never see.</li><li>Create the session on the first draft keystroke: would still create rows for messages that are never sent, which is what the request asks to avoid.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0084-deferred-new-task-session-creation.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0084DeferredNewTaskSessionCreation = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0084DeferredNewTaskSessionCreation as default
};
