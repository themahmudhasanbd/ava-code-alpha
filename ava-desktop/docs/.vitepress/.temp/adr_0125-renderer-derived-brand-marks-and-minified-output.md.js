import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0125: Renderer Ships Derived Brand Marks and Minified Output","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0125-renderer-derived-brand-marks-and-minified-output.md","filePath":"adr/0125-renderer-derived-brand-marks-and-minified-output.md","lastUpdated":1789839347000}');
const _sfc_main = { name: "adr/0125-renderer-derived-brand-marks-and-minified-output.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0125-renderer-ships-derived-brand-marks-and-minified-output" tabindex="-1">ADR 0125: Renderer Ships Derived Brand Marks and Minified Output <a class="header-anchor" href="#adr-0125-renderer-ships-derived-brand-marks-and-minified-output" aria-label="Permalink to &quot;ADR 0125: Renderer Ships Derived Brand Marks and Minified Output&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-08-26</li><li>Related: ADR 0083, D079, D094</li><li>Baseline: <code>0.10.8</code></li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The renderer output was 31 MiB, and three of its costs were accidental rather than product decisions:</p><ol><li><code>electron-vite</code> hard-defaults its renderer preset to <code>minify: false</code>, unlike plain Vite. <code>apps/desktop/electron.vite.config.ts</code> did not override it, so every production build shipped unminified chunks; the entry chunk alone was 108,273 lines.</li><li>KaTeX&#39;s stylesheet declares <code>woff2</code>, <code>woff</code>, and <code>truetype</code> sources for each of its faces. The bundled Chromium supports <code>woff2</code> universally, so 40 <code>.woff</code>/<code>.ttf</code> files (0.78 MiB) were emitted and never served.</li><li>D079 and D094 named <code>build/icon_1024.png</code> as the canonical brand mark, and <code>BrandLogo</code> imported it directly. That file is the 1024x1024 master <code>scripts/make-icon.py</code> derives <code>build/icon.icns</code> from. <code>BrandLogo</code> renders at 16, 20, and 64 CSS px, so the renderer carried 1.04 MiB of PNG to draw a 64 px mark.</li></ol><p>None of this is Electron&#39;s own footprint. The unpacked application is about 279 MB, but a bare Electron 43.4.0 shell with no application code is already about 277 MB, so the renderer is the only part worth tuning.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>The renderer build sets <code>minify: &quot;esbuild&quot;</code> explicitly. The setting is asserted by <code>packaging-footprint.test.mjs</code> because the framework default would silently undo it.</li><li>A <code>pi-drop-legacy-font-fallbacks</code> Vite plugin removes <code>woff</code> and <code>truetype</code><code>src</code> entries from CSS with <code>enforce: &quot;pre&quot;</code>, before Vite registers <code>url()</code> values as assets. Stripping them later leaves the assets emitted. Only comma-prefixed fallback entries are removed, so a face whose single source is <code>woff</code> or <code>truetype</code> keeps it.</li><li><code>build/icon_1024.png</code> and <code>build/logo_dark.png</code> remain the canonical brand masters and the source of truth for installer icons. This amends D079 and D094 on one point: the renderer imports <strong>derived</strong> marks at <code>apps/desktop/src/assets/brand/logo-light.png</code> and <code>logo-dark.png</code> instead of the masters. The derived files are 192x192, covering a 64 px render at 3x device pixel ratio. Regenerate them from the masters with <code>sips -Z 192</code> whenever the canonical marks change; the visual identity is unchanged.</li></ol><p><em>(Superseded by <a href="./0298-remove-bundled-fonts">ADR 0298</a>: the app ships no fonts, so there is nothing to subset.)</em> The two bundled CJK faces stayed unsubset while they existed. ADR 0083 section 2 appended <code>Noto Sans SC</code> to every font stack so Chinese text stayed readable offline, and subsetting would have dropped glyphs from user-supplied content; reducing those 15 MiB required revising ADR 0083, not a build-config change. ADR 0298 removed the faces instead, and <code>out/renderer</code> now keeps only KaTeX&#39;s <code>woff2</code> glyphs.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li><code>out/renderer</code> drops from 31 MiB to 24 MiB: JavaScript 12.53 to 7.72 MiB, legacy font fallbacks 0.78 MiB to 0, PNG brand assets 1.23 to 0.06 MiB. Measured before <a href="./0298-remove-bundled-fonts">ADR 0298</a>; the bundled <code>woff2</code> payload is no longer part of the renderer.</li><li>Minified renderer stack traces need the devtools source view; no sourcemaps are emitted, which matches the previous release behavior.</li><li>The brand marks now exist in two places. <code>packaging-footprint.test.mjs</code> asserts the renderer does not import from <code>build/</code>, so a future contributor reintroducing the master path fails the suite rather than silently regaining 1 MiB.</li><li>Stripping legacy font formats is safe only because the app runs exclusively in the bundled Chromium. Plugin panels sanitize their own theme CSS to <code>data:</code> URIs and cannot reference renderer font assets, and the docs site uses an independent theme.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0125-renderer-derived-brand-marks-and-minified-output.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0125RendererDerivedBrandMarksAndMinifiedOutput = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0125RendererDerivedBrandMarksAndMinifiedOutput as default
};
