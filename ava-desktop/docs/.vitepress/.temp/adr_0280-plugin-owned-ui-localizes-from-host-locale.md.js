import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0280: Plugin-Owned UI Localizes from the Host Locale","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0280-plugin-owned-ui-localizes-from-host-locale.md","filePath":"adr/0280-plugin-owned-ui-localizes-from-host-locale.md","lastUpdated":1789657556000}');
const _sfc_main = { name: "adr/0280-plugin-owned-ui-localizes-from-host-locale.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0280-plugin-owned-ui-localizes-from-the-host-locale" tabindex="-1">ADR 0280: Plugin-Owned UI Localizes from the Host Locale <a class="header-anchor" href="#adr-0280-plugin-owned-ui-localizes-from-the-host-locale" aria-label="Permalink to &quot;ADR 0280: Plugin-Owned UI Localizes from the Host Locale&quot;">​</a></h1><ul><li><strong>Status</strong>: Accepted</li><li><strong>Date</strong>: 2026-09-17</li><li><strong>Related</strong>: <a href="./0009-english-first-globalization">ADR 0009</a> · <a href="./0082-localized-plugin-panel-chrome">ADR 0082</a> · <a href="./0159-plugin-generated-settings-and-local-shortcuts">ADR 0159</a> · <a href="./0160-shipped-locale-registry-and-language-picker">ADR 0160</a> · <a href="./0267-plugin-labels-follow-the-app-language">ADR 0267</a> · <a href="./../spec/07-plugins/02-plugin-manifest-schema">07-plugins/02-plugin-manifest-schema</a> · <a href="./../spec/07-plugins/03-plugin-api">07-plugins/03-plugin-api</a></li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The host already resolves two kinds of plugin copy it renders itself: <code>manifest.i18n</code> for identity (<code>name</code> / <code>description</code> / <code>safetyNotes</code>), and inline <code>{ en, &quot;zh-CN&quot; }</code> on a few chrome labels (<code>ui.title</code>, view titles, settings destinations, session sources). Putting the same maps on generated <code>contributes.settings</code> (and then on commands, tools, themes) would grow a second incomplete i18n system inside the manifest: only two contract locales, two declaration shapes, and still no way for a panel or widget to relabel its own HTML.</p><p>A plugin process can already read the app language (<code>pi.app.getLocale</code>, <code>pi.app.getAppearance().locale</code>). Open panels already receive <code>appearance:changed</code>. The plugin process did not.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>For <strong>plugin-owned</strong> copy the host publishes the active language and nothing else: <code>pi.app.getLocale()</code>, <code>getAppearance().locale</code>, and <code>appearance:changed</code> to open panels <strong>and</strong> loaded plugin processes.</li><li>Plugin-owned UI (panels, views, widgets, settings destinations, toasts, runtime command titles) localizes itself from that tag. Authors pick any catalog they want; the host does not translate it.</li><li>Do not add more <code>PluginLocalizedString</code> maps to contribution fields. Generated <code>contributes.settings</code> <code>title</code> / <code>description</code> / <code>enum[].label</code> stay author-language plain strings. A plugin that needs a localized settings surface ships <code>settingsDestinations</code>.</li><li>Host-owned surfaces that render without plugin code keep the contracts they already have: <code>manifest.i18n</code> (ADR 0267) and the shipped chrome labels in (2) of the Context.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A language switch restyles plugin-owned UI live, without a reload and without rewriting the registry.</li><li>Authors are not required to maintain bilingual maps for every setting.</li><li>Existing host chrome localization is unchanged.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><ul><li><strong>Host-resolved locale maps on every contribution field</strong>: two models in one manifest, still only <code>en</code> / <code>zh-CN</code>, and plugin HTML remains unsolved.</li><li><strong>Resolve plugin copy in the renderer</strong>: rejected in ADR 0267; the renderer still must not parse a manifest.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0280-plugin-owned-ui-localizes-from-host-locale.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0280PluginOwnedUiLocalizesFromHostLocale = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0280PluginOwnedUiLocalizesFromHostLocale as default
};
