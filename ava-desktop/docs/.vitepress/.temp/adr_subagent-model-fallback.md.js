import { ssrRenderAttrs, ssrRenderAttr } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const _imports_0 = "/assets/subagent-model-fallback.CfebzcMQ.png";
const __pageData = JSON.parse('{"title":"ADR: Ordered subagent model fallback","description":"","frontmatter":{},"headers":[],"relativePath":"adr/subagent-model-fallback.md","filePath":"adr/subagent-model-fallback.md","lastUpdated":1789447116000}');
const _sfc_main = { name: "adr/subagent-model-fallback.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-ordered-subagent-model-fallback" tabindex="-1">ADR: Ordered subagent model fallback <a class="header-anchor" href="#adr-ordered-subagent-model-fallback" aria-label="Permalink to &quot;ADR: Ordered subagent model fallback&quot;">​</a></h1><ul><li>Status: Accepted for implementation</li><li>Date: 2026-09-14</li><li>Related: ADR 0062, ADR 0063, ADR 0202, ADR subagent-model-opt-in</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>A subagent can select only one model. Its existing bounded provider retries cannot recover when that model remains unavailable. Restarting the delegated task on another agent could repeat tools that already changed files.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Add optional <code>fallbackModels</code> to a definition, the user-subagent record/input, and the existing internal launch definition. It is an ordered list of <code>provider/model</code> strings in managed Markdown and host RPC, parsed into model pins in Shared. The existing primary <code>model</code> and Task override priority stay unchanged. Missing lists preserve single-model behavior. An omitted update preserves the list; <code>[]</code> clears it. No database or protocol version changes are needed for these additive fields; old documents remain valid.</p><p>Electron resolves alternatives through the same credential, provider-count, and model-capability boundaries as primary pins. These bindings are scoped to their owning definition, even when Task explicitly overrides its primary. They do not grant <code>Task.model</code> access to another definition, or enter the opted-in model summary. A primary pin that cannot resolve still fails before launch; an unresolved alternative is reported when reached and skipped.</p><p>After the current model&#39;s existing retries are exhausted (or a provider error is non-retryable), the runtime tries the next configured alternative. Each resolved provider-id/model-id pair is tried at most once per delegation, excluding the existing retry budget within each model. The same Agent keeps its system prompt, original task, successful assistant/tool history, tool scope, counters, and usage. Only the failed assistant request is removed from model context before <code>continue()</code>. It is retained in the visible transcript. No task or completed tool is replayed by the fallback controller.</p><p>The model, adapter, credentials, headers, output cap, and thinking level change together. Thinking is re-clamped from the definition or original parent selection for each model; <code>omit</code> remains omission. The original abort signal owns the whole chain. Host/tool failures, missing reports, unexpected thrown errors, and cancellation do not initiate fallback.</p><p>Failed attempts remain visible in child transcript diagnostics and in the bounded parent report. Lifecycle snapshots retain the final effective model, thinking, and additive <code>modelFailures</code> diagnostics. Exhausting the chain returns <code>failed</code> with the final provider error; it never selects an unlisted model or silently inherits the parent as recovery.</p><h2 id="settings-example" tabindex="-1">Settings example <a class="header-anchor" href="#settings-example" aria-label="Permalink to &quot;Settings example&quot;">​</a></h2><p>The Advanced section keeps one primary model and lists fallback models in priority order. Each fallback can be moved up/down or removed.</p><p><img${ssrRenderAttr("src", _imports_0)} alt="Subagent editor showing the primary model and two ordered fallback models"></p><h2 id="validation" tabindex="-1">Validation <a class="header-anchor" href="#validation" aria-label="Permalink to &quot;Validation&quot;">​</a></h2><p>Shared parser and provider-resolution tests, host registry round trips, real local HTTP tests for continuation/credentials/cancellation, and the sidecar model suite cover the contract. Required integration suites are <code>test:e2e</code>, <code>test:e2e:subagents</code>, and <code>test:e2e:subagent-models</code>. The editor journey separately covers adding, reordering, removing, and retaining unavailable pins across save/reopen.</p><h3 id="local-verification-and-remaining-integration-gates" tabindex="-1">Local verification and remaining integration gates <a class="header-anchor" href="#local-verification-and-remaining-integration-gates" aria-label="Permalink to &quot;Local verification and remaining integration gates&quot;">​</a></h3><ul><li>Runtime: 200 focused tests passed, including the existing parent-runtime suite; Shared: 35 definition tests; Desktop: 100 subagent tests; i18n: 24 tests; host-core: 13 subagent tests. Typecheck, lint, Rust formatting and clippy passed.</li><li><code>test:e2e:subagent-models</code>: passed on the request branch with a real sidecar and local HTTP transport, including ordered fallback and live/settled model metadata. This is pre-integration evidence.</li><li><code>test:e2e</code>: 19 passed, two project-deletion assertions failed, and two live provider scenarios were skipped for missing credentials. An independently built, unchanged <code>a9053531</code> host reproduced the same two failures.</li><li>E2E: <strong>NOT RUN</strong> to completion for <code>test:e2e:subagents</code> on Windows. Rust&#39;s known-folder lookup ignores the fixture HOME, so the suite now fails before registry writes. Alternative validation is the host record/document tests and Shared/launch-loader tests. The remaining risk is the complete managed registry RPC/save/reopen journey; run this suite under Linux/macOS isolation.</li><li>Full editor interaction/reload and post-integration <code>main</code> E2E remain outstanding. This request retains its branch and worktree for integration.</li></ul><h3 id="wsl-branch-verification-2026-09-14" tabindex="-1">WSL branch verification (2026-09-14) <a class="header-anchor" href="#wsl-branch-verification-2026-09-14" aria-label="Permalink to &quot;WSL branch verification (2026-09-14)&quot;">​</a></h3><p>The implementation commit <code>82dfb3353ab071b4b0479ac401797cc6ab3c178f</code> was exported to an isolated Linux filesystem directory and built under WSL2 Ubuntu 24.04, using Linux Node 24.14.1, pnpm 11.18.0, Rust 1.96.0, and Electron 43.6.0. No Windows <code>node_modules</code> or executable binaries were reused. The locked dependency install, JavaScript build, and Linux host build passed.</p><ul><li><code>pnpm test:e2e:subagents</code>: <strong>31/31 passed</strong>, including ordered fallback persistence, omitted-update preservation, invalid-pin rejection, loader propagation, and clearing the list.</li><li><code>pnpm test:e2e:subagent-models</code>: <strong>13/13 passed</strong>, including fallback and the existing model opt-in/override isolation scenarios.</li><li><code>pnpm test:e2e</code>: <strong>21/21 non-live checks passed</strong>. The two real-provider cases (<code>E2E-008-live-model</code>, <code>E2E-009-stream</code>) were <strong>SKIPPED</strong> because no test API key was configured. Both project-deletion cases that failed on Windows passed on Linux; this does not fix or invalidate the Windows results.</li><li>An isolated Electron/Xvfb configuration-editor check using CDP passed four journeys: add/reorder/save, preserve across another-field edit and renderer reload, retain unavailable-provider pins, and remove all/save/reopen. The UI exercised the real renderer, preload, Main, and Linux host registry; persisted records were checked through IPC. It used synthetic providers without credentials and removed its temporary HOME/data/profile afterward.</li></ul><p>The test environment set <code>PI_DESKTOP_PLUGIN_MARKET_URL=http://127.0.0.1:9/e2e-offline-catalog</code> so startup used the existing bundled marketplace catalog. With the default external market URL, WSL network connectivity caused host-handshake timeouts before the tested behavior. These runs do not qualify online marketplace connectivity. The UI run also logged an unrelated bundled <code>pi.file-manager</code> module-load error; its successful subagent assertions are not a full plugin health claim.</p><p>This closes the earlier Linux registry and configuration-editor validation gaps. Full task-transcript reload acceptance after fallback, real-provider cases, and required post-integration <code>main</code> E2E remain outstanding. No main integration or remote publication was performed during this validation.</p><h3 id="expanded-failure-chain-verification-2026-09-14" tabindex="-1">Expanded failure-chain verification (2026-09-14) <a class="header-anchor" href="#expanded-failure-chain-verification-2026-09-14" aria-label="Permalink to &quot;Expanded failure-chain verification (2026-09-14)&quot;">​</a></h3><p>The existing implementation at <code>82dfb335</code> was unchanged. Additional test sources were copied into the isolated WSL source snapshot; these results apply to that implementation with the expanded tests in this change.</p><table tabindex="0"><thead><tr><th>Local provider responses in configured order</th><th>Expected and observed result</th></tr></thead><tbody><tr><td>Success, unused alternative</td><td>Primary completes; no alternative request</td></tr><tr><td>HTTP 404, success, unused alternative</td><td>Second model completes</td></tr><tr><td>HTTP 404, HTTP 404, success, unused alternative</td><td>Third model completes</td></tr><tr><td>HTTP 404, HTTP 404, HTTP 404, success, unused alternative</td><td>Fourth model completes</td></tr><tr><td>Four HTTP 404 responses</td><td>Child fails after four requests; no wraparound</td></tr></tbody></table><p>Both the real-transport runtime tests and the separate sidecar process passed this matrix. The sidecar assertions inspect actual request order, the original task, every live model change, the settled model/status, ordered failure codes, and the final child report. A successful parent response alone cannot pass the child-failure assertion.</p><p>Additional real-transport tests passed for mixed HTTP 401/403/404 failures, HTTP 429 and HTTP 503 exhausting independent retry budgets on two consecutive models, and preserving one completed tool call across one, two, or three failed models. The retry fixture sends <code>Retry-After: 0</code>; production retry logic and retry counts remain active, with no fake timers or retry bypass. Cancellation, unresolved/duplicate bindings, credentials, thinking selection, and host-tool error boundaries remain covered.</p><ul><li>Windows: <code>pnpm --filter @pi-desktop/agent-runtime exec vitest run src/subagent.test.ts src/subagent-fallback.test.ts src/subagent-definitions.test.ts src/runtime.test.ts</code> — <strong>210/210 passed</strong>.</li><li>Windows: <code>pnpm --filter @pi-desktop/agent-runtime typecheck</code> — <strong>passed</strong>.</li><li>Windows: <code>pnpm lint</code> and <code>git diff --check</code> — <strong>passed</strong>.</li><li>WSL: <code>pnpm --filter @pi-desktop/agent-runtime exec vitest run src/subagent-fallback.test.ts --reporter=verbose</code> — <strong>17/17 passed</strong>.</li><li>WSL: <code>pnpm test:e2e:subagent-models</code> — <strong>17/17 checks passed</strong>, including five failure-chain/control scenarios and the existing model-isolation checks.</li></ul><p>These are deterministic local HTTP providers driving the real runtime and sidecar, not live commercial-model availability tests. No application code changed, so the previously built portable executable contains the same tested fallback implementation. Main integration, post-integration E2E, live-provider coverage, and task-transcript reload acceptance remain outside this result.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/subagent-model-fallback.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const subagentModelFallback = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  subagentModelFallback as default
};
