import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0132: Attribute cross-display window moves to the user","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0132-attribute-cross-display-window-moves-to-the-user.md","filePath":"adr/0132-attribute-cross-display-window-moves-to-the-user.md","lastUpdated":1787908248000}');
const _sfc_main = { name: "adr/0132-attribute-cross-display-window-moves-to-the-user.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0132-attribute-cross-display-window-moves-to-the-user" tabindex="-1">ADR 0132: Attribute cross-display window moves to the user <a class="header-anchor" href="#adr-0132-attribute-cross-display-window-moves-to-the-user" aria-label="Permalink to &quot;ADR 0132: Attribute cross-display window moves to the user&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-08-28</li><li>Related: <a href="./../spec/03-runtime/01-ipc-protocol">01-ipc-protocol</a> · <a href="./../spec/04-ux/09-interaction-patterns">09-interaction-patterns §8</a> · decision D263 · issue #18</li><li>Amends: ADR 0122 (and the reservation behavior it restored from ADR 0032)</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>ADR 0122 keeps a target-state, display-aware native reservation for the docked work panel. Main plans the reservation from remembered <em>base bounds</em> — the window rect without the reservation — and reconciles later native deltas against the last rect it actually applied.</p><p>That reconciliation collapsed two very different situations into one <code>displayChanged</code> boolean. When the window ended up on another display, the remembered base bounds were reused verbatim, then clamped into the new display&#39;s work area. That is correct when the OS re-fitted bounds we asked for: the user&#39;s intent survives on a constrained display and is restored on a roomy one. It is wrong when the user dragged the window there, because the previous display&#39;s coordinates are no longer the intent.</p><p>Worse, the reconcile was wired to every native <code>move</code> event, so it ran while the drag was still in progress. Reported in issue #18 and reproducible on every release from 0.10.0 onward: dragging the window to a second display made it jump on pointer release, because the deferred <code>setBounds</code> planned from the origin display&#39;s base bounds and landed at the target display&#39;s clamped edge. The window&#39;s y coordinate came from the old display too. The same misattribution then leaked into <code>window-state.json</code>, so relaunch reopened the window on the display the user had left.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>Replace the <code>displayChanged</code> boolean with an explicit <code>DisplayTransition</code> of <code>none</code>, <code>os-adjusted</code>, or <code>user-moved</code>.</li><li><code>os-adjusted</code> keeps ADR 0122&#39;s behavior unchanged: the remembered base bounds survive an OS re-fit or a display topology change.</li><li><code>user-moved</code> derives new base bounds from the window&#39;s current position, so the drop position becomes the intent. Only the origin is normalized into the target display&#39;s work area; the size is preserved even when the target work area is smaller, because base bounds are the restorable intent under ADR 0122 and a shrink here would be persisted permanently.</li><li>Classify the transition by whether an unaccounted native <code>move</code> stream precedes it. A drag is the only cross-display transition that follows one. The marker is a flag, not a deadline, so attribution never depends on how long the main process took to reach the classification, and it survives the deferred geometry of a maximized or fullscreen window.</li><li>Never re-plan bounds mid-drag. The <code>move</code> handler only marks the user-move window and defers display reconciliation until the move stream goes quiet.</li><li>Persist base bounds for both <code>none</code> and <code>user-moved</code>, and advance the remembered display key on <code>user-moved</code>, so relaunch restores the display the user actually left the window on. <code>os-adjusted</code> still refuses to persist. The persistence path normalizes a dragged-in base the same way, because a maximized or fullscreen window defers reservation geometry and leaves this as the only consumer of the drag.</li><li>Retire an unconsumed marker on a deadline. The deadline bounds how long a marker can linger when no consumer runs; it never decides attribution.</li><li>A forced bounds recovery (the Stage Manager path) clears the pending user-move attribution, because it is not user intent.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Dragging the window between displays keeps the position where it was dropped, with no jump on pointer release.</li><li>Relaunch reopens the window on the display it was last used on.</li><li>ADR 0122&#39;s constrained-display reservation behavior is preserved verbatim for OS-owned adjustments, including the restore path back to a roomy display.</li><li>Display topology events clear the pending marker before reconciling, so a display added, removed, or rescaled right after a drag is still classified as OS-owned.</li><li>A window dropped onto a display whose work area cannot hold it keeps its size and is pinned to the work area&#39;s top-left. <code>planWorkPanelReservation</code> still caps the width it adds, so the reservation shrinks instead of the window.</li><li>No IPC, storage, or host protocol change. <code>window/setWorkPanelReservation</code> keeps its request/response shape.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><h3 id="clamp-the-remembered-base-bounds-into-the-new-display" tabindex="-1">Clamp the remembered base bounds into the new display <a class="header-anchor" href="#clamp-the-remembered-base-bounds-into-the-new-display" aria-label="Permalink to &quot;Clamp the remembered base bounds into the new display&quot;">​</a></h3><p>Rejected. It keeps the window on the target display but discards the drop position, which is the reported symptom rather than a fix for it.</p><h3 id="skip-reservation-planning-entirely-on-a-display-change" tabindex="-1">Skip reservation planning entirely on a display change <a class="header-anchor" href="#skip-reservation-planning-entirely-on-a-display-change" aria-label="Permalink to &quot;Skip reservation planning entirely on a display change&quot;">​</a></h3><p>Rejected. The reservation would then be sized for the previous display&#39;s work area, so a narrower target display would leave the window wider than its work area.</p><h3 id="detect-drags-from-pointer-state-instead-of-the-move-stream" tabindex="-1">Detect drags from pointer state instead of the move stream <a class="header-anchor" href="#detect-drags-from-pointer-state-instead-of-the-move-stream" aria-label="Permalink to &quot;Detect drags from pointer state instead of the move stream&quot;">​</a></h3><p>Rejected for now. Electron exposes no drag-begin/drag-end pair for native window moves, and polling the pointer during a drag costs more than the pending-move marker it would replace.</p><h3 id="attribute-the-drag-with-a-time-deadline" tabindex="-1">Attribute the drag with a time deadline <a class="header-anchor" href="#attribute-the-drag-with-a-time-deadline" aria-label="Permalink to &quot;Attribute the drag with a time deadline&quot;">​</a></h3><p>Rejected. A deadline makes correctness depend on main-process scheduling: a busy main process, or a maximized window that defers its geometry past the deadline, would silently fall back to the buggy OS-adjusted path.</p><h2 id="references" tabindex="-1">References <a class="header-anchor" href="#references" aria-label="Permalink to &quot;References&quot;">​</a></h2><ul><li><code>apps/desktop/electron/main/work-panel-window.ts</code></li><li><code>apps/desktop/electron/main/index.ts</code></li><li><code>apps/desktop/test/work-panel-window.test.mjs</code></li><li><code>docs/adr/0122-reserve-native-width-while-work-panel-visible.md</code></li><li><code>docs/spec/06-delivery/04-e2e-test-plan.md</code> (E2E-160)</li><li><code>docs/spec/08-meta/decisions-log.md</code> (D263)</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0132-attribute-cross-display-window-moves-to-the-user.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0132AttributeCrossDisplayWindowMovesToTheUser = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0132AttributeCrossDisplayWindowMovesToTheUser as default
};
