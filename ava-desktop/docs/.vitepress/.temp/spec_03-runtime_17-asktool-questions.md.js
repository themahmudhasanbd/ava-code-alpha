import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"17. asktool Interactive Questions","description":"","frontmatter":{},"headers":[],"relativePath":"spec/03-runtime/17-asktool-questions.md","filePath":"spec/03-runtime/17-asktool-questions.md","lastUpdated":1786527725000}');
const _sfc_main = { name: "spec/03-runtime/17-asktool-questions.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="_17-asktool-interactive-questions" tabindex="-1">17. asktool Interactive Questions <a class="header-anchor" href="#_17-asktool-interactive-questions" aria-label="Permalink to &quot;17. asktool Interactive Questions&quot;">​</a></h1><h2 id="_1-purpose" tabindex="-1">1. Purpose <a class="header-anchor" href="#_1-purpose" aria-label="Permalink to &quot;1. Purpose&quot;">​</a></h2><p><code>asktool</code> lets the model pause a turn and ask the user one or more bounded questions. It is available in Agent, Plan, and Goal mode and is separate from permission approval: it does not authorize an operation and has no validity deadline.</p><h2 id="_2-request-shape" tabindex="-1">2. Request shape <a class="header-anchor" href="#_2-request-shape" aria-label="Permalink to &quot;2. Request shape&quot;">​</a></h2><p>The tool accepts a non-empty <code>questions</code> array. Each question contains:</p><ul><li><code>question</code>: the prompt text;</li><li><code>options</code>: one or more selectable answer labels;</li><li><code>multiSelect</code>: optional; when true, more than one selectable answer is allowed.</li></ul><p>The desktop card always adds one extra <code>Enter another answer</code> option with a text field. The model does not need to add a special free-text choice to the tool arguments.</p><h2 id="_3-card-interaction" tabindex="-1">3. Card interaction <a class="header-anchor" href="#_3-card-interaction" aria-label="Permalink to &quot;3. Card interaction&quot;">​</a></h2><p>Only one question is shown at a time. A small indicator is rendered for every question and uses three states: unanswered, answered, and skipped. Selecting an indicator revisits that question. <code>Next</code> records an answer when one exists; otherwise it records a skip. <code>Skip</code> explicitly records a skip and advances. <code>Decline all</code> resolves every question as skipped.</p><p>There is no timer, countdown, or automatic expiration. The card remains pending until the user submits or the turn is stopped. If the turn is stopped while a card is open, the runtime resolves every remaining question as skipped.</p><h2 id="_4-tool-output" tabindex="-1">4. Tool output <a class="header-anchor" href="#_4-tool-output" aria-label="Permalink to &quot;4. Tool output&quot;">​</a></h2><p>The response is the normal tool result returned to the model and persisted with the tool row. For each question, the content is serialized as:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>question text：answer 1、answer 2</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Multiple questions are separated by <code>\\n---\\n</code>. A skipped or unanswered question keeps the question text and uses an empty placeholder:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>question text：</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>This format is deterministic, preserves question order, and makes multi-select answers distinguishable without exposing a renderer-only state object to the model.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("spec/03-runtime/17-asktool-questions.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _17AsktoolQuestions = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _17AsktoolQuestions as default
};
