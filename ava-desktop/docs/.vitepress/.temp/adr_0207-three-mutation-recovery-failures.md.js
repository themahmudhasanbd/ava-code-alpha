import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0207: Allow Three Same-Path Mutation Recovery Failures","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0207-three-mutation-recovery-failures.md","filePath":"adr/0207-three-mutation-recovery-failures.md","lastUpdated":1788986066000}');
const _sfc_main = { name: "adr/0207-three-mutation-recovery-failures.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0207-allow-three-same-path-mutation-recovery-failures" tabindex="-1">ADR 0207: Allow Three Same-Path Mutation Recovery Failures <a class="header-anchor" href="#adr-0207-allow-three-same-path-mutation-recovery-failures" aria-label="Permalink to &quot;ADR 0207: Allow Three Same-Path Mutation Recovery Failures&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-10</li><li>Related: D186, D379, ADR 0087, E2E-140, E2E-141</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The line-anchored mutation guard previously terminated a prompt after two counted failures for the same path. The first failure often exposes a syntax, range, or provenance correction, while the second failure can still be the agent applying that correction. Stopping at that point makes a bounded editing mistake harder to recover from than necessary.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Amend D186 and ADR 0087: a prompt may make three counted failed <code>Edit</code> calls on one path before the repeat guard terminates the turn. A recognized shell patch command uses the same three-failure limit under its patch-command key. The first and second counted failures return their normal error-specific recovery hints and leave the turn running; the third carries <code>terminate: true</code> and finalizes the assistant row with <code>MUTATION_RETRY_BUDGET_EXHAUSTED</code>.</p><p>The existing rules remain unchanged: recoverable error codes each receive one per-code grace, counters are scoped to the prompt and path, and a successful mutation clears that path&#39;s failure history. Provider retry budgets and other tool concurrency limits are unaffected.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>The model gets one additional bounded opportunity to correct an Edit or recognized shell patch failure.</li><li>Persistent or guessing mutation loops still terminate deterministically.</li><li>The runtime error message and every shipped locale must say three failures.</li><li>No IPC, storage, host protocol, or tool result shape changes.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li>Keep the two-failure limit: rejected because the second correction attempt can still be an honest recovery after a distinct first error.</li><li>Remove the limit: rejected because malformed or blind mutation loops must not consume an unbounded turn.</li></ul><h2 id="verification" tabindex="-1">Verification <a class="header-anchor" href="#verification" aria-label="Permalink to &quot;Verification&quot;">​</a></h2><ul><li>Runtime unit tests cover Edit, shell patch, per-code grace, reset after a successful mutation, and the visible terminating error row.</li><li>E2E-140 and E2E-141 document the third-failure boundary.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0207-three-mutation-recovery-failures.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0207ThreeMutationRecoveryFailures = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0207ThreeMutationRecoveryFailures as default
};
