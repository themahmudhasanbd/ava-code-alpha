import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0061: Imperceptible background context compaction","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0061-imperceptible-background-context-compaction.md","filePath":"adr/0061-imperceptible-background-context-compaction.md","lastUpdated":1786032712000}');
const _sfc_main = { name: "adr/0061-imperceptible-background-context-compaction.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0061-imperceptible-background-context-compaction" tabindex="-1">ADR 0061: Imperceptible background context compaction <a class="header-anchor" href="#adr-0061-imperceptible-background-context-compaction" aria-label="Permalink to &quot;ADR 0061: Imperceptible background context compaction&quot;">​</a></h1><ul><li>Status: Accepted (clauses 2, 4, 6, 7, 8 amended by ADR 0064)</li><li>Date: 2026-08-06</li><li>Deciders: PI-Desktop core</li><li>Amends: ADR 0030 / ADR 0049 / D158</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The turn-boundary guard from ADR 0030 is correct: no provider request is issued above the hard budget, and ADR 0049 gives every automatic failure a durable retained-tail recovery. What it is not is invisible. Compaction announced itself to the user in five ways:</p><ul><li>every successful automatic compaction raised an info toast;</li><li><code>compaction_start</code> set <code>isRunning</code>, so the run state and spinner jumped while nothing the user asked for was running;</li><li>the soft boundary injected a transient instruction asking the model to call <code>CompactContext</code>, which spent a model turn and left a tool-activity row in the transcript;</li><li>compaction ran only at <code>tokens &gt;= hardLimit</code>, so it always happened at the moment the user was waiting for a reply, with a summary input close to the whole window in the worst case;</li><li>Settings exposed <code>reserveTokens</code>, <code>keepRecentTokens</code>, and an enable switch, making the user responsible for tuning a safety mechanism.</li></ul><p>Codex (<code>codex-rs/core/src/session/context_window.rs</code>, <code>state/auto_compact_window.rs</code>) shows two ideas worth adopting: grade the trigger instead of using one hard edge, and measure the trigger over the <em>increment</em> since the current context prefix rather than the total (<code>AutoCompactTokenLimitScope::BodyAfterPrefix</code>).</p><blockquote><p>Corrected by ADR 0064: this section originally added &quot;Codex also has no model-side compaction tool at all — the host decides and executes.&quot; That is wrong. Codex has <code>new_context</code> (<code>tools/handlers/new_context_window_spec.rs</code>), gated by <code>Feature::TokenBudget</code>, and neither of the two ideas above is Codex&#39;s default: <code>BodyAfterPrefix</code> is opt-in and Codex has no pre-computation to grade a trigger for.</p></blockquote><p>One property of the existing implementation makes background work cheap: <code>entriesWithCompaction()</code> locates a checkpoint by <code>throughMessageId</code> and splices it into the entry list, leaving everything after the anchor intact. A checkpoint computed early therefore stays installable as the tail keeps growing, so pre-computation needs no new invariant.</p><p>The fixed <code>reserveTokens: 16_384</code> / <code>keepRecentTokens: 20_000</code> defaults were also a real defect independent of visibility: they applied the same absolute numbers to a 32k window and a 1M window.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Compaction becomes a host-owned background activity that the user cannot perceive. The blocking hard boundary is unchanged and remains the safety net.</p><ol><li><strong>Tiered budget, derived from the model window.</strong> <code>contextBudget()</code> keeps <code>hardLimit</code> and <code>requestHeadroom</code> exactly as ADR 0030 defined them, and adds <code>backgroundLimit = floor(hardLimit * 0.7)</code> as the pre-computation trigger. <code>keepRecentTokens</code> is derived as <code>clamp(hardLimit * 0.2, 8k, 64k)</code>, still capped at half the hard budget. The soft boundary and its <code>softGap</code> are deleted.</li><li><strong>Incremental trigger scope.</strong> Background pre-computation requires both <code>tokens &gt;= backgroundLimit</code> and growth of at least <code>keepRecentTokens</code> since the baseline recorded when the newest checkpoint was installed. Without the increment test a large retained tail sitting above the background limit would request a fresh summary every turn while reducing nothing. The hard boundary keeps measuring the total, because that is the provider&#39;s actual constraint.</li><li><strong>Generation is separated from installation.</strong> <code>buildCheckpoint()</code> runs the preparation, budget preflight, and summary request without persisting anything or touching <code>activeCompaction</code>. <code>installCheckpoint()</code> re-estimates, appends through host-core, updates <code>activeCompaction</code>, and emits <code>compaction_end</code>. The blocking path is the two composed back to back, so threshold, overflow, and manual behavior are unchanged.</li><li><strong>Provider-idle windows only.</strong> Background summary requests are started from exactly two places: <code>tool_execution_start</code>, where the model stream has ended and the next request has not been issued, and the <code>finally</code> of <code>prompt()</code>, where the user is reading the result. A background summary never shares the provider connection with a streaming turn: <code>prepareNextTurn()</code> awaits any in-flight build before the next request. Background work deliberately does not set <code>compactionInProgress</code>, because that flag feeds <code>getStatus().isRunning</code>.</li><li><strong>Staleness is checked at install, and failure is silent.</strong> A pre-computed checkpoint is consumed at the next turn boundary or user prompt only if the checkpoint it was based on is still active, its <code>throughMessageId</code> anchor is still present in <code>fullEntries</code>, and it still fits the <em>current</em> model&#39;s budget. Any miss discards it and falls through to the existing blocking path. A failed background build is discarded without persisting, without an event, and without the ADR 0049 fallback: the retained tail belongs to the hard boundary, which is still there to catch whatever background work misses.</li><li><strong>No model-facing compaction.</strong> <code>CompactContext</code>, the <code>&lt;context_management&gt;</code> nudge, and the <code>&quot;CompactContext&quot;</code> entry in the host-core no-confirmation allowlist are removed. Triggering is entirely deterministic and host-driven.</li><li><strong>Silence.</strong> <code>compaction_start</code> and <code>compaction_end</code> carry an optional <code>phase?: &quot;background&quot; | &quot;blocking&quot;</code> (absent means <code>blocking</code>; additive per ADR 0047, so the protocol version does not change). A successful automatic compaction — background or blocking — notifies nobody: no toast, no run state change, no transcript row. Three toasts remain, each following something the user already saw: a <code>retained_tail</code> fallback (warning), an overflow retry (warning), and a manual <code>/compact</code> result.</li><li><strong>The context inspector is the only visible trace.</strong> <code>compaction_end</code> carries <code>status: { generation, summaryTokens }</code>, and the durable <code>SessionDetail.compaction</code> provides the same on session open or fork. The inspector renders one line — <code>Compacted N× · summary ≈X</code> — and nothing when there is no checkpoint. The generation counter rides inside the checkpoint&#39;s opaque <code>details</code> value, which host-core persists verbatim, so no record schema change is needed.</li><li><strong>No settings.</strong> The Settings compaction card, its search keywords, its i18n keys, and the main-process passthrough are removed. Persisted <code>contextCompaction</code> values are ignored, deliberately: a user who once turned compaction off would otherwise have no switch left to turn it back on. The <code>ContextCompactionSettings</code> type survives as the runtime&#39;s construction-time override so tests can build a compaction-disabled session.</li></ol><p>Manual <code>/compact</code> is unchanged and remains fail-fast.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>In the common case the user never waits for compaction. The summary request overlaps a tool execution or an idle session, and the turn boundary only installs an already-finished checkpoint.</li><li>Summary inputs are smaller and cheaper, because 0.7 of the hard limit is a much smaller history than the hard limit itself.</li><li>A session that crosses the background limit pays for a summary it might not have needed. 0.7 is the cost trade: past that point reaching the hard limit is close to inevitable, so the tokens are rarely wasted, while a lower ratio would bill short sessions for summaries they never use.</li><li>Small-window and large-window models now get proportionate budgets instead of one pair of absolute token counts.</li><li>Losing the model-side tool removes a class of wasted turns and a transcript artifact, and removes the possibility of the model ignoring, deferring, or repeating the request. Nothing is lost that the deterministic trigger did not already cover.</li><li>Compaction is no longer auditable from the transcript. The inspector line, the durable checkpoint record, and the lifecycle events remain, so diagnosis is possible; casual observation is not. This is an accepted reversal of ADR 0030&#39;s &quot;remain visible/durable as a tool activity row&quot;.</li><li>Users can no longer disable automatic compaction from the UI. Since a disabled guard means an oversized provider request, that is the intended outcome.</li><li>Two provider-idle windows are not all of them. A session that never runs a tool and never goes idle between prompts still compacts synchronously at the hard boundary, exactly as before.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><h3 id="compact-concurrently-with-the-streaming-turn" tabindex="-1">Compact concurrently with the streaming turn <a class="header-anchor" href="#compact-concurrently-with-the-streaming-turn" aria-label="Permalink to &quot;Compact concurrently with the streaming turn&quot;">​</a></h3><p>Rejected. It would remove the last of the latency, but two simultaneous requests on one provider connection invite rate limiting (observed on Bedrock) and double the visible cost of a single user action.</p><h3 id="keep-the-soft-boundary-nudge-alongside-background-compaction" tabindex="-1">Keep the soft-boundary nudge alongside background compaction <a class="header-anchor" href="#keep-the-soft-boundary-nudge-alongside-background-compaction" aria-label="Permalink to &quot;Keep the soft-boundary nudge alongside background compaction&quot;">​</a></h3><p>Rejected. With deterministic pre-computation the nudge adds only the failure modes it always had — a spent turn, a transcript row, and a model free to ignore it.</p><h3 id="keep-the-settings-knobs-but-hide-them-behind-a-developer-toggle" tabindex="-1">Keep the settings knobs but hide them behind a developer toggle <a class="header-anchor" href="#keep-the-settings-knobs-but-hide-them-behind-a-developer-toggle" aria-label="Permalink to &quot;Keep the settings knobs but hide them behind a developer toggle&quot;">​</a></h3><p>Rejected. The values are now derived from the model window; an override would be a way to reintroduce the small-window/large-window defect, and a hidden switch to disable a safety guard is worse than no switch.</p><h3 id="show-a-subtle-inline-indicator-while-compacting" tabindex="-1">Show a subtle inline indicator while compacting <a class="header-anchor" href="#show-a-subtle-inline-indicator-while-compacting" aria-label="Permalink to &quot;Show a subtle inline indicator while compacting&quot;">​</a></h3><p>Rejected. Any persistent indicator makes the user aware of a mechanism they cannot act on. The inspector already answers the question for anyone who thinks to ask it.</p><h2 id="references" tabindex="-1">References <a class="header-anchor" href="#references" aria-label="Permalink to &quot;References&quot;">​</a></h2><ul><li><code>docs/spec/03-runtime/01-ipc-protocol.md</code></li><li><code>docs/spec/03-runtime/02-agent-runtime.md</code></li><li><code>docs/spec/03-runtime/03-tools-and-permissions.md</code></li><li><code>docs/spec/04-ux/06-settings-ia.md</code></li><li><code>docs/spec/06-delivery/04-e2e-test-plan.md</code></li><li><code>docs/spec/08-meta/decisions-log.md</code> (D158, D200)</li><li><code>codex-rs/core/src/session/context_window.rs</code> (behavioral reference)</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0061-imperceptible-background-context-compaction.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0061ImperceptibleBackgroundContextCompaction = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0061ImperceptibleBackgroundContextCompaction as default
};
