import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0127: Transcript Layout Index and Identity-Based Truncation","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0127-transcript-layout-index-and-identity-truncation.md","filePath":"adr/0127-transcript-layout-index-and-identity-truncation.md","lastUpdated":1787836182000}');
const _sfc_main = { name: "adr/0127-transcript-layout-index-and-identity-truncation.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0127-transcript-layout-index-and-identity-based-truncation" tabindex="-1">ADR 0127: Transcript Layout Index and Identity-Based Truncation <a class="header-anchor" href="#adr-0127-transcript-layout-index-and-identity-based-truncation" aria-label="Permalink to &quot;ADR 0127: Transcript Layout Index and Identity-Based Truncation&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-08-26</li><li>Deciders: PI-Desktop core</li><li>Related: ADR 0120, D119, D122, D139, <code>03-runtime/04-data-storage.md</code>, <code>03-runtime/06-host-rpc-protocol.md</code>, <code>03-runtime/01-ipc-protocol.md</code></li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>ADR 0120 bounded what a session open sends to the renderer, but explicitly kept &quot;the sequential scan needed to locate the requested page&quot;. That residue is what users experience as the remaining problem: opening a session is still linear in the whole conversation, and it degrades as the conversation grows, because the newest 100 messages can only be found by parsing every line before them. The scan also deserialized a <code>type</code> discriminator per line, so a single large tool result was walked in full merely to be classified and skipped.</p><p>Two further defects were found in the same path, both coordinate-space confusions rather than performance issues:</p><ul><li>The window clamped <code>messageBefore</code> against <code>SessionSummary.message_count</code>, which is <code>sessions.last_seq</code> — a deduplicated logical count. Transcript positions are physical file lines. A message whose durable file append succeeded while its index commit did not (a documented crash window) leaves the counter permanently below the line count, and the tail window then cut off the newest messages: they existed on disk and never reached the renderer.</li><li>Regenerate and edit-resend sent <code>truncateBefore</code> as <code>sessionHistory[sessionId].messageStart + userIndex</code>, adding a physical file offset to an index into the renderer&#39;s deduplicated, display-filtered array. The sum only addresses the intended message when the renderer holds the entire history, so regenerating from a paged-back transcript could truncate at the wrong point and archive the wrong tail as a revision.</li></ul><p>Separately, a session fork committed on the host could vanish from the UI: both fork actions returned early when their navigation intent (D139) was superseded, after <code>api.forkSession</code> had already created the child. The branch stayed on disk and was absent from the sidebar until a manual refresh.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><strong>Layout index.</strong> host-core maintains a per-session <em>transcript layout</em>: the byte offset of every message and compaction line plus the <code>file_len</code> those offsets were recorded against. A bounded window seeks directly to its first selected line, so serving it costs the window and not the history in front of it. The layout is derived data cached in memory: <code>file_len</code> detects growth (scan the tail only), shrink or replacement (rescan), and every rewrite and delete path drops the entry because a rewrite can reproduce the same length. A torn trailing line is excluded from both offsets and <code>file_len</code> so a later refresh adopts it once the writer completes it.</li><li><strong>Cheap classification.</strong> Transcript lines are classified by one depth-aware scan for the <em>top-level</em> <code>type</code> key, never by parsing the line into a value. Depth matters: tool results and checkpoint details are open-ended JSON and can nest an object whose own <code>type</code> names a line kind, which a positional or first-match check misreads. <code>type</code> is written first on every new line so the scan normally stops at the first key, while lines written under the previous ordering carry it after their payload and are read by the same scan, so no migration is required.</li><li><strong>One coordinate space.</strong> Read-window offsets are physical message-line positions, clamped against the layout. <code>last_seq</code> is never used for this purpose. The compaction chain is still returned whole with any window, because the newest checkpoint drives model context.</li><li><strong>Identity-based truncation.</strong> <code>agent/prompt</code> accepts <code>truncateFromMessageId</code>, and the host resolves that identity against its own transcript; an unresolvable id is rejected with <code>NOT_FOUND</code> instead of truncating at a guessed position. <code>truncateBefore</code> remains accepted for older callers with its correctness condition stated.</li><li><strong>Durable fork commit.</strong> Recording a forked child (session list, cached transcript, checkpoint marks) is unconditional, because it is already durable on the host when the call returns. Only the visible switch — active session, transcript, work panel, history entry — remains gated on the navigation intent still being current.</li></ol><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Persist the layout in SQLite:</strong> rejected. It is cheap to rebuild, and a durable copy adds a schema migration plus a second source of truth that can disagree with the file after a crash.</li><li><strong>Fix the clamp by making <code>last_seq</code> authoritative for file positions:</strong> rejected. It is a deduplicated counter by design; making the file conform to it would mean rewriting transcripts to repair a derived index.</li><li><strong>Keep index-based truncation and always rehydrate the full history first:</strong> rejected. It reintroduces exactly the full-history read this change removes, on the most latency-sensitive action in the product.</li><li><strong>Refresh the session list after a superseded fork:</strong> rejected as a remedy for the lost branch. It relies on an extra round trip to repair state the renderer already had in hand.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Opening a session, and each older page, costs the requested window rather than the conversation length; a large tool result is no longer parsed to be skipped. Measured on synthetic transcripts of 3,000 messages / 32 MB and 12,000 / 129 MB, a warm tail window stays around 0.7-0.8 ms while the previous sequential read grew from 6 ms to 25 ms, so the cost stops tracking history length. The first open of a session still pays one index scan.</li><li>The layout is a cache with an explicit validity token, so correctness after a crash or rewrite does not depend on it being present or fresh.</li><li>The newest messages of a session with an unindexed file line are visible again, and paging backwards reaches the true first message.</li><li>Regenerating from a paged-back transcript replaces the turn the user selected.</li><li>A branch created while the user navigates away is still listed and openable.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0127-transcript-layout-index-and-identity-truncation.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0127TranscriptLayoutIndexAndIdentityTruncation = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0127TranscriptLayoutIndexAndIdentityTruncation as default
};
