import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0204: Explicit Unsigned macOS First-Launch Helper","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0204-unsigned-macos-first-launch-helper.md","filePath":"adr/0204-unsigned-macos-first-launch-helper.md","lastUpdated":1789813349000}');
const _sfc_main = { name: "adr/0204-unsigned-macos-first-launch-helper.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0204-explicit-unsigned-macos-first-launch-helper" tabindex="-1">ADR 0204: Explicit Unsigned macOS First-Launch Helper <a class="header-anchor" href="#adr-0204-explicit-unsigned-macos-first-launch-helper" aria-label="Permalink to &quot;ADR 0204: Explicit Unsigned macOS First-Launch Helper&quot;">​</a></h1><ul><li>Status: Accepted (amended by D406 / ADR 0232, D450 / ADR 0289, D457 / ADR 0296)</li><li>Date: 2026-09-09</li><li>Deciders: PI-Desktop core</li><li>Related: D078, D371, D406, D450, D457, E2E-196b, ADR 0289, ADR 0296</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Local and debug macOS packaging remain unsigned so contributors and release operators can build without Developer ID credentials. A trusted download of that artifact can still carry Apple&#39;s quarantine attribute, which may make Gatekeeper report that PI-Desktop is damaged. The existing guidance required users to open Terminal and run <code>xattr -cr</code>, which is broader than the single attribute that causes this launch failure.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>Every macOS distribution includes an executable <code>PI-Desktop-macOS-open.command</code> alongside the opening-help note. The DMG places the helper in a visible first-launch row below the install gesture.</li><li>The helper searches only <code>/Applications/PI-Desktop.app</code> and <code>~/Applications/PI-Desktop.app</code>. It verifies the bundle identifier is <code>net.aiuo.pi-desktop</code>, and the user must move the app into one of those directories before running it.</li><li>When the verified app carries <code>com.apple.quarantine</code>, the helper recursively removes only that attribute and then opens PI-Desktop. It never uses <code>sudo</code>, accepts no arbitrary path argument, removes no other extended attribute, and does not claim that an unsigned app passed Gatekeeper qualification.</li><li>The DMG labels the package as unsigned and the note continues to state that signed and notarized builds do not need the helper.</li><li>Developer ID signing and notarization remain the release path for normal Gatekeeper launches; this helper is an explicit user action for trusted unsigned artifacts, not a replacement for signing.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A user can read the documented unsigned first-launch fallback from the DMG; the ZIP retains the one-Finder-double-click helper after the normal drag-to-Applications step.</li><li>Other extended attributes remain intact, reducing the scope of the quarantine workaround.</li><li>The helper cannot repair an app installed outside the two standard Applications directories; the opening note provides the supported Terminal fallback for <code>/Applications</code>.</li><li>The same helper is harmless in signed packages but is not needed there.</li></ul><h2 id="amendment-d406-adr-0232" tabindex="-1">Amendment (D406 / ADR 0232) <a class="header-anchor" href="#amendment-d406-adr-0232" aria-label="Permalink to &quot;Amendment (D406 / ADR 0232)&quot;">​</a></h2><p>The DMG-specific helper placement is replaced. DMGs now expose only the opening-help note, displayed as <code>If app won&#39;t open, read this.txt</code>; the executable helper remains in the macOS ZIP package. The note is the DMG fallback and no longer describes a helper that is present in the DMG.</p><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li>Keep the Terminal-only <code>xattr -cr</code> instruction: rejected because it is not a one-click flow and clears more metadata than necessary.</li><li>Use <code>sudo</code> or target a user-selected arbitrary path: rejected because it would expand the privilege and application scope of an installer workaround.</li><li>Require signing for every local build: rejected because D078 intentionally keeps local packaging available without Developer ID credentials.</li></ul><h2 id="amendment-d450-adr-0289" tabindex="-1">Amendment (D450 / ADR 0289) <a class="header-anchor" href="#amendment-d450-adr-0289" aria-label="Permalink to &quot;Amendment (D450 / ADR 0289)&quot;">​</a></h2><p>Official GitHub tag releases are Developer ID-signed and notarized. The first-launch helper and opening note remain for trusted unsigned local or debug artifacts only. Official Release DMGs must not claim they are unsigned.</p><h2 id="amendment-d457-adr-0296" tabindex="-1">Amendment (D457 / ADR 0296) <a class="header-anchor" href="#amendment-d457-adr-0296" aria-label="Permalink to &quot;Amendment (D457 / ADR 0296)&quot;">​</a></h2><p>The DMG no longer shows the opening-help note. ZIP retains the helper and note for trusted unsigned local or debug artifacts.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0204-unsigned-macos-first-launch-helper.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0204UnsignedMacosFirstLaunchHelper = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0204UnsignedMacosFirstLaunchHelper as default
};
