import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0016: Organize the sidebar around retained multi-project tabs","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0016-sidebar-organization-and-multi-project-tabs.md","filePath":"adr/0016-sidebar-organization-and-multi-project-tabs.md","lastUpdated":1785040740000}');
const _sfc_main = { name: "adr/0016-sidebar-organization-and-multi-project-tabs.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0016-organize-the-sidebar-around-retained-multi-project-tabs" tabindex="-1">ADR 0016: Organize the sidebar around retained multi-project tabs <a class="header-anchor" href="#adr-0016-organize-the-sidebar-around-retained-multi-project-tabs" aria-label="Permalink to &quot;ADR 0016: Organize the sidebar around retained multi-project tabs&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-07-26</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>D088 replaced the mixed Recents list with one current-project group plus path-less Temporary sessions. That made the workspace boundary visible, but it also forced every project switch to replace the only project group and removed basic organization controls. A user working across several repositories needs to retain those projects in the sidebar, collapse inactive groups, and pin or archive project and conversation rows without deleting durable data.</p><p>The host currently exposes one selected workspace through <code>workspace.set</code> (<code>project.set</code> at the renderer bridge). Agent turns, permissions, and transcripts are already keyed by session. Replacing the host workspace with a multi-workspace singleton would enlarge the privileged-state surface and make background tool execution depend on whichever tab the renderer selected most recently.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><h3 id="retained-project-tabs" tabindex="-1">Retained project tabs <a class="header-anchor" href="#retained-project-tabs" aria-label="Permalink to &quot;Retained project tabs&quot;">​</a></h3><ul><li>The renderer retains an ordered set of normalized project paths. Every retained path appears as an independently collapsible project group in the sidebar. A path-less Temporary group remains separate.</li><li>Opening a project adds its path without closing other tabs. Selecting a tab activates that path through the existing <code>project.set</code> operation. The host still has exactly one active workspace for visible shell context.</li><li>Closing a tab removes only its retained sidebar entry. It does not archive the project, delete sessions, or remove the host-owned <code>projects</code> row. When the active tab closes, the renderer activates a remaining tab or clears the selected workspace.</li></ul><h3 id="renderer-owned-organization-metadata" tabindex="-1">Renderer-owned organization metadata <a class="header-anchor" href="#renderer-owned-organization-metadata" aria-label="Permalink to &quot;Renderer-owned organization metadata&quot;">​</a></h3><ul><li>Project metadata is keyed by normalized full path and contains <code>pinned</code>, <code>archived</code>, <code>collapsed</code>, and optional manual <code>order</code>.</li><li>Conversation metadata is keyed by durable session id and contains <code>pinned</code>, <code>archived</code>, and optional manual <code>order</code>.</li><li>User-facing sidebar ordering supports <code>recent</code>, <code>created</code>, <code>oldest</code>, and <code>name</code>. Pinned rows always precede unpinned rows; the selected sort is the secondary order. A persisted <code>manual</code> value and optional <code>order</code> fields are retained for compatibility, but this decision does not introduce drag or other manual-reorder interaction; rows without an explicit prior order use the stable recent-order fallback.</li><li>Archive is non-destructive. Archived rows are omitted by default and recoverable through the explicit archived view. Delete remains a separate host operation.</li><li>The presentation record is best-effort renderer local storage. It contains no transcript, tool argument, provider secret, or other privileged data.</li></ul><h3 id="session-workspace-isolation" tabindex="-1">Session workspace isolation <a class="header-anchor" href="#session-workspace-isolation" aria-label="Permalink to &quot;Session workspace isolation&quot;">​</a></h3><ul><li>The project associated with a session is the authority for that session&#39;s tool root. <code>tools.execute</code> resolves the workspace from the persisted <code>session.projectPath</code>/<code>project_id</code>, not from the mutable active tab.</li><li>Selecting another project or conversation never aborts a running turn. Per-session run state and grants remain independent, and background tool calls remain sandboxed to the originating session&#39;s project.</li><li>Temporary sessions have no project root. Switching to a Temporary conversation clears the visible active workspace and does not inherit the previously selected project&#39;s tool access.</li></ul><p>This supersedes D088&#39;s one-current-project and no-multi-project-tree limitations, and restores scoped project/conversation row actions superseded with D068. D088&#39;s exact-path grouping, scoped draft reuse, and Temporary session boundary remain in force. No new renderer IPC channel is required.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>The sidebar can show several project groups in one independently scrollable region while the main pane still shows one destination/transcript.</li><li>Project/session pin, archive, collapse, sort, and open paths survive app restart when renderer preferences are available. Clearing those preferences resets presentation only; host-owned projects and sessions stay intact.</li><li>Activating a missing or inaccessible retained path reports the existing workspace error without silently deleting the tab or its sessions.</li><li>Host tool execution must load the session before resolving its workspace. Legacy calls without a durable session may fall back to the selected host workspace during the compatibility window.</li><li>The Projects index remains the durable project directory and the recovery path for closed or archived sidebar tabs.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><h3 id="keep-exactly-one-project-group" tabindex="-1">Keep exactly one project group <a class="header-anchor" href="#keep-exactly-one-project-group" aria-label="Permalink to &quot;Keep exactly one project group&quot;">​</a></h3><p>Rejected because it discards the working set on every project switch and makes parallel session work cumbersome.</p><h3 id="store-archive-and-pin-flags-in-the-host-schema" tabindex="-1">Store archive and pin flags in the host schema <a class="header-anchor" href="#store-archive-and-pin-flags-in-the-host-schema" aria-label="Permalink to &quot;Store archive and pin flags in the host schema&quot;">​</a></h3><p>Rejected for this iteration because the flags affect only local presentation. Durable project/session identity and deletion continue to belong to the host.</p><h3 id="add-one-host-workspace-per-open-tab" tabindex="-1">Add one host workspace per open tab <a class="header-anchor" href="#add-one-host-workspace-per-open-tab" aria-label="Permalink to &quot;Add one host workspace per open tab&quot;">​</a></h3><p>Rejected because only tool execution needs a workspace root, and the durable session already identifies it. A second host workspace registry would duplicate project/session state.</p><h3 id="open-each-project-in-a-separate-native-window" tabindex="-1">Open each project in a separate native window <a class="header-anchor" href="#open-each-project-in-a-separate-native-window" aria-label="Permalink to &quot;Open each project in a separate native window&quot;">​</a></h3><p>Rejected because it multiplies window/process state and does not solve conversation organization in the primary workbench.</p><h2 id="references" tabindex="-1">References <a class="header-anchor" href="#references" aria-label="Permalink to &quot;References&quot;">​</a></h2><ul><li><code>docs/spec/03-runtime/03-tools-and-permissions.md</code></li><li><code>docs/spec/03-runtime/04-data-storage.md</code></li><li><code>docs/spec/03-runtime/06-host-rpc-protocol.md</code></li><li><code>docs/spec/04-ux/01-ui-ia.md</code></li><li><code>docs/spec/04-ux/08-component-spec.md</code></li><li><code>docs/spec/04-ux/09-interaction-patterns.md</code></li><li><code>docs/spec/06-delivery/04-e2e-test-plan.md</code></li><li><code>docs/spec/08-meta/decisions-log.md</code> (D093)</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0016-sidebar-organization-and-multi-project-tabs.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0016SidebarOrganizationAndMultiProjectTabs = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0016SidebarOrganizationAndMultiProjectTabs as default
};
