import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0155: Add Zhipu / Z.AI Named Endpoint Presets","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0155-zhipu-endpoint-presets.md","filePath":"adr/0155-zhipu-endpoint-presets.md","lastUpdated":1788566402000}');
const _sfc_main = { name: "adr/0155-zhipu-endpoint-presets.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0155-add-zhipu-z-ai-named-endpoint-presets" tabindex="-1">ADR 0155: Add Zhipu / Z.AI Named Endpoint Presets <a class="header-anchor" href="#adr-0155-add-zhipu-z-ai-named-endpoint-presets" aria-label="Permalink to &quot;ADR 0155: Add Zhipu / Z.AI Named Endpoint Presets&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-05</li><li>Deciders: PI-Desktop core</li><li>Updates ADR 0012, ADR 0020, and ADR 0116</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Zhipu AI (智谱) exposes two product modes and two regions:</p><ul><li>Standard API vs GLM Coding Plan</li><li>China (<code>open.bigmodel.cn</code>) vs international (<code>api.z.ai</code>)</li></ul><p>pi-ai already ships Completions transports for the two Coding Plan URLs (<code>zai</code>, <code>zai-coding-cn</code>) and detects <code>thinkingFormat: &quot;zai&quot;</code> from those hosts. models.dev already publishes all four endpoints. PI-Desktop&#39;s add-provider dialog was a generic OpenAI-compatible form, so users had to know which URL to paste, and a saved row used <code>vendorKey: &quot;custom&quot;</code>.</p><p>Issue #35 asked for first-class configuration of API vs Coding Plan and China vs international, without a new vendor SDK.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Expose four named endpoint presets in the add-provider <strong>Service</strong> select. They stay on the existing OpenAI-compatible path:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>vendorKey              baseUrl</span></span>
<span class="line"><span>zhipuai                https://open.bigmodel.cn/api/paas/v4</span></span>
<span class="line"><span>zhipuai-coding-plan    https://open.bigmodel.cn/api/coding/paas/v4</span></span>
<span class="line"><span>zai                    https://api.z.ai/api/paas/v4</span></span>
<span class="line"><span>zai-coding-plan        https://api.z.ai/api/coding/paas/v4</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>Selecting a preset fills the name, locks the Base URL, keeps <code>apiStyle: &quot;chat_completions&quot;</code>, and persists the models.dev <code>vendorKey</code>. The display name stays editable. Coding Plan shows a one-line API-key hint. This is a compact select, not a restored vendor-card grid.</p><p>pi-ai&#39;s <code>zai</code> transport is the international Coding Plan, while models.dev <code>zai</code> is the standard API. PI-Desktop stores the models.dev key plus the exact URL so catalog matching cannot confuse them. <code>zai-coding-cn</code> remains an alias of <code>zhipuai-coding-plan</code>.</p><p>When the configured URL or <code>vendorKey</code> matches a preset, the sidecar Completions model record receives <code>thinkingFormat: &quot;zai&quot;</code> and <code>zaiToolStream: true</code>. No new wire adapter, secret table, or host protocol is introduced.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>China and international Zhipu users can pick API vs Coding Plan without copying URLs from external docs.</li><li>Catalog enrichment follows the selected endpoint, not a generic custom row.</li><li>Generic OpenAI-compatible configuration remains the Custom endpoint option.</li><li>OpenCode Go stays an API-style preset; Zhipu does not add four more apiStyle values.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0155-zhipu-endpoint-presets.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0155ZhipuEndpointPresets = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0155ZhipuEndpointPresets as default
};
