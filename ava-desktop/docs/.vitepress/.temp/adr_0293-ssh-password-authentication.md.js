import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0293: SSH password authentication for the remote-host bootstrap","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0293-ssh-password-authentication.md","filePath":"adr/0293-ssh-password-authentication.md","lastUpdated":1789795544000}');
const _sfc_main = { name: "adr/0293-ssh-password-authentication.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0293-ssh-password-authentication-for-the-remote-host-bootstrap" tabindex="-1">ADR 0293: SSH password authentication for the remote-host bootstrap <a class="header-anchor" href="#adr-0293-ssh-password-authentication-for-the-remote-host-bootstrap" aria-label="Permalink to &quot;ADR 0293: SSH password authentication for the remote-host bootstrap&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-19</li><li>Decision: D454</li><li>Amends: <a href="./0292-ssh-remote-host-bootstrap">ADR 0292</a> — its &quot;no SSH secret is</li><li>Related: <a href="./0286-remote-host-desktop-kernel">ADR 0286</a> (D449), ADR 0205 (D373), <code>02-architecture/05-remote-agent-control.md</code> §5.2, <code>05-security/02-remote-control-security.md</code> §3.4</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>ADR 0292 bootstraps a <code>pi-host</code> on a machine the user already reaches over SSH by shelling out to the system <code>ssh</code> client. That choice was made deliberately and its main argument was that the app holds no SSH secret at all: <code>~/.ssh/config</code>, the agent, and <code>known_hosts</code> decide the login, so there is nothing for the app to leak. <code>BatchMode=yes</code> enforced the claim — a host that needed an interactive password failed immediately with a typed error instead of hanging behind an invisible prompt.</p><p>That is the right default and it stays. It is not, however, sufficient for the machines this feature exists for. A fresh cloud instance, a NAS, a container host, or any box provisioned with a password and no key yet is reachable from a terminal after one <code>ssh</code> prompt, while the desktop refuses it. The user&#39;s only recourse is to install a key from a terminal first — which is exactly the &quot;use your terminal instead&quot; detour the bootstrap was built to remove.</p><p>There is no way to support that case while keeping the invariant literally true. Accepting a password means the app holds a secret, so the decision is what shape that takes, not whether it exists. Three constraints bound the answer:</p><ul><li>The transport spawns <code>ssh</code> with no terminal, so a password cannot simply be handed over; OpenSSH must be given a way to ask for one.</li><li>A secret that must survive a restart to keep reconnecting is a secret at rest, and the app already has exactly one encrypted-at-rest store it trusts.</li><li>The transport is a port (<code>SshTransport</code>) with the bootstrap and the tunnel manager on the other side of it, so whatever carries the secret has to be expressible through that seam.</li></ul><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><p><strong>Password auth is opt-in, and a key or agent stays the default.</strong> Absent credentials, the argv is byte-for-byte what ADR 0292 produced, <code>BatchMode=yes</code> included. A target that carries a password is the only one that differs.</p></li><li><p><strong>The secret reaches <code>ssh</code> through OpenSSH&#39;s own askpass helper</strong> (<code>remote/ssh-askpass.ts</code>). <code>SSH_ASKPASS</code> names a generated <code>/bin/sh</code> script and <code>SSH_ASKPASS_REQUIRE=force</code> makes <code>ssh</code> use it without a terminal; the script <code>cat</code>s the secret from a file and terminates the line, because <code>ssh</code> reads the helper&#39;s answer only up to the first line break. The secret is therefore never an <code>ssh</code> argument and never an environment variable — it is a path in the environment and bytes in a file, so <code>ps</code> output and argv dumps stay clean. Its confidentiality rests on modes rather than convention: a <code>0700</code> directory created by <code>mkdtemp</code>, a <code>0600</code> secret, a <code>0700</code> helper.</p></li><li><p><strong><code>BatchMode=yes</code> becomes <code>BatchMode=no</code> only when a password is supplied, together with <code>NumberOfPasswordPrompts=1</code> and <code>PubkeyAuthentication=no</code>.</strong> The helper answers every prompt with the same secret, so a retry could only repeat a wrong password, and repeated failures are what trip a server&#39;s own lockout. One prompt, one answer. Default identities are skipped: an encrypted <code>~/.ssh/id_rsa</code> would consume that single prompt as a key passphrase and the login password would never be tried. Password mode in Settings replaces a key; a user with both picks key mode.</p></li><li><p><strong>The credential is short-lived.</strong> Material is written when a child is about to authenticate and deleted once it cannot still be prompting: after an <code>exec</code> / <code>execWithInput</code> child has closed, after a forward&#39;s local port is up, or on <code>dispose</code>. A reference count keeps a shared copy alive across concurrent children and drops it when the last one is done, so the ordinary case leaves one file on disk for the duration of one authentication and nothing afterwards.</p></li><li><p><strong>The password is persisted encrypted, in a new record field.</strong><code>RemoteHostRecord.sshSecret</code> is written to <code>remote-hosts.json</code> as <code>encryptedSshSecret</code> through the same <code>safeStorage</code>-backed <code>EncryptionPort</code> the device token already uses, and read back the same way. The descriptor in <code>metadata.ssh</code> gains <code>auth: &quot;password&quot;</code> — and only that value is written, so a key descriptor and every record written before this change keep exactly the shape they had. A record whose SSH password will not decrypt (keychain moved, different OS user) loses the secret but keeps the paired host, because the device token is independent of it.</p></li><li><p><strong>The secret never reaches the renderer.</strong> <code>RemoteHostSshMetadata</code> stays secret-free by construction: it is plaintext metadata, it is stored in <code>metadata</code>, and it is echoed to the renderer inside <code>RemoteHostBootstrapResult</code>. The password travels beside it — from the IPC request into <code>SshTarget.password</code>, and back out of the bootstrap as <code>SshBootstrapOutcome.sshSecret</code> for the caller to encrypt — and <code>RemoteHostSummary</code> is unchanged.</p></li><li><p><strong>A password that cannot work is refused before anything runs.</strong> A line break cannot survive the askpass round trip, so <code>assertSshPassword</code> rejects one with <code>INVALID_ARGUMENT</code> in the bootstrap rather than letting it become a failed authentication. Windows is refused with <code>HOST_BOOTSTRAP_FAILED</code> naming the remedy, because Windows OpenSSH cannot execute a shell-script askpass helper and shipping a helper executable is a project of its own. A host recorded as <code>auth: &quot;password&quot;</code> with no usable secret is not opened at all, which is reported as the host being disconnected instead of as a rejected login.</p></li><li><p><strong>The Settings surface says what it does.</strong> The SSH form gains an authentication mode with a masked password field and a reveal toggle, and its description states that the login password is saved in the OS keychain so the host reconnects after a restart. The copy that promised no password was ever stored is replaced in all eight shipped locales.</p></li></ol><h2 id="invariants" tabindex="-1">Invariants <a class="header-anchor" href="#invariants" aria-label="Permalink to &quot;Invariants&quot;">​</a></h2><ul><li>With no password supplied, nothing changes: same argv, same <code>BatchMode=yes</code>, no credential file created, no askpass variables set.</li><li>The password is never an <code>ssh</code> argument, never an environment variable value, never in a URL, never in a log line, and never in a renderer payload.</li><li>Credential files exist only inside a <code>0700</code> directory, only for the interval in which an <code>ssh</code> child can still prompt, and are removed on every path — including <code>dispose</code> and a failed <code>forward</code>.</li><li>Nothing but the system <code>ssh</code> client consumes the secret; no library, no second credential store, and no keychain entry of its own.</li><li>A malformed or absent descriptor still degrades to &quot;not an SSH host&quot; rather than to a spawn with junk arguments.</li></ul><h2 id="out-of-scope" tabindex="-1">Out of scope <a class="header-anchor" href="#out-of-scope" aria-label="Permalink to &quot;Out of scope&quot;">​</a></h2><ul><li><strong>Interactive (non-persisted) prompting.</strong> The password is supplied once by the user in Settings and either remembered encrypted or not at all; there is no &quot;ask me each time&quot; mode, which would need a main-process prompt surface.</li><li><strong>Keyboard-interactive auth as a separate mode.</strong> <code>ssh</code> treats it as the same prompt, so the helper answers both with one secret and there is no second switch to expose.</li><li><strong>Windows support</strong>, as above, and <strong>non-Linux remote targets</strong>, unchanged from ADR 0292.</li><li><strong>Key passphrase storage.</strong> A passphrase unlocks a local key; the askpass helper answers that prompt only as a side effect of the same secret being supplied for the login.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong><code>sshpass</code>, or <code>SSH_ASKPASS</code> with the secret in the environment.</strong> Rejected: <code>sshpass</code> is a third-party dependency that must be installed, and either form exposes the secret to anything that can read the process environment or arguments — including other users on a shared machine.</li><li><strong>A PTY and a scripted <code>expect</code>-style dialog.</strong> Rejected: it reintroduces a pseudo-terminal whose prompts and timing the desktop would have to parse, and it makes an interactive prompt possible again on a path that was designed to fail closed.</li><li><strong>Store the password unencrypted in <code>metadata</code>.</strong> Rejected: <code>metadata</code> is plaintext, is user-editable, and is not covered by the keychain, so it would put a reusable credential into a file the app cannot protect.</li><li><strong>Keep nothing at rest and ask on every launch.</strong> Rejected for now: it would make an SSH host the only paired host that needs a human at startup, so a restart would silently leave it offline. Encrypting it in the same store as the device token keeps one credential model instead of two.</li><li><strong><code>ssh -o PreferredAuthentications=password</code>.</strong> Rejected as a <em>key-path</em> default: forcing the method would turn a working agent login into a failure. Password mode (a secret supplied) instead sets <code>PubkeyAuthentication=no</code>, which is the exclusive Settings choice rather than a fall-through.</li></ul><h2 id="testing" tabindex="-1">Testing <a class="header-anchor" href="#testing" aria-label="Permalink to &quot;Testing&quot;">​</a></h2><p><code>apps/desktop/test/remote-host-ssh-password.test.mjs</code> drives the whole path with fixture executables and a fake <code>EncryptionPort</code>, so nothing touches a network or a real keychain. It asserts the argv of a password spawn carries no secret and the argv of a key spawn is unchanged, that the helper answers exactly what the secret file holds, that the file, the helper, and their directory carry <code>0600</code> / <code>0700</code> / <code>0700</code>, that material is gone once its child is done and after <code>dispose</code>, that a round trip through the registry leaves no cleartext in <code>remote-hosts.json</code>, and that a pre-existing key record reads back with no added field.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>The desktop can now be asked to hold an SSH password, which is a real widening of what the app is trusted with. It is bounded by being opt-in per host, encrypted at rest through the store that already exists, and never exposed to the renderer or the process table.</li><li><code>remote-hosts.json</code> gains an optional <code>encryptedSshSecret</code> and <code>metadata.ssh.auth</code>. Both are additive: the file&#39;s <code>version</code> stays <code>1</code>, and a key-authenticated record is written exactly as before.</li><li>Users can now bootstrap a host whose only credential is a password, which removes the terminal detour that ADR 0292&#39;s <code>BatchMode=yes</code> left in place.</li><li>Server-side lockout policy becomes reachable through the app: one wrong saved password produces one failed attempt per connect. <code>NumberOfPasswordPrompts=1</code> keeps that to the minimum the user asked for.</li><li>The <code>settings.remoteHosts</code> copy that claimed no password is ever stored had to change in eight locales, and the claim in ADR 0292 needed an explicit amendment rather than a quiet edit.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0293-ssh-password-authentication.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0293SshPasswordAuthentication = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0293SshPasswordAuthentication as default
};
