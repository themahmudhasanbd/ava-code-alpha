import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0113: Persist the New Task empty slot immediately and deduplicate it by message count","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0113-immediate-empty-session-slot-and-deduplication.md","filePath":"adr/0113-immediate-empty-session-slot-and-deduplication.md","lastUpdated":1788557630000}');
const _sfc_main = { name: "adr/0113-immediate-empty-session-slot-and-deduplication.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0113-persist-the-new-task-empty-slot-immediately-and-deduplicate-it-by-message-count" tabindex="-1">ADR 0113: Persist the New Task empty slot immediately and deduplicate it by message count <a class="header-anchor" href="#adr-0113-persist-the-new-task-empty-slot-immediately-and-deduplicate-it-by-message-count" aria-label="Permalink to &quot;ADR 0113: Persist the New Task empty slot immediately and deduplicate it by message count&quot;">​</a></h1><ul><li><strong>Status:</strong> Accepted</li><li><strong>Date:</strong> 2026-08-21</li><li><strong>Supersedes:</strong> ADR 0084 / D220</li><li><strong>Related:</strong> D088 · D093 · D305 · E2E-011b · E2E-011d · E2E-011e · E2E-011g</li><li><strong>Amended by:</strong> ADR 0154 (first-frame empty reveal; reuse without a blocking list refresh)</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>ADR 0084 deferred session creation until the first prompt. That removed abandoned rows, but it also made the New Task action invisible in the sidebar and removed the scoped empty-session reuse behavior. The product now requires a real, immediately visible empty session while preserving one empty slot per project or temporary sidebar group.</p><p>Titles cannot define emptiness because users may rename an empty session and the first message can produce a title independently. The host already stores the current transcript ordinal in <code>sessions.last_seq</code>, which is the current message count after append and rewrite operations.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>When New Task is invoked for a project or the path-less Temporary group:</p><ol><li>Sort that group&#39;s non-archived sessions by <code>updatedAt</code> descending.</li><li>If the most recent session is empty (<code>messageCount = 0</code> and the renderer has no live rows, running turn, or submitted draft), select it and reveal its empty transcript on the first frame. Selecting the already active row is a no-op.</li><li>Otherwise reveal the empty home on the first frame, create a real empty session through <code>session.create</code>, and insert the returned summary into the sidebar. Do not block on <code>session.list</code> or <code>session.get</code> (ADR 0154).</li></ol><p>Renderer requests for the same group are serialized. This makes rapid repeated clicks observe the first completed empty slot instead of racing multiple <code>session.create</code> calls. Project paths remain normalized through the existing grouping helper, and path-less sessions use the same rule rather than a separate session type.</p><p><code>SessionSummary.messageCount</code> is added to the shared IPC/session contract. The Rust host derives it from <code>sessions.last_seq</code>; no storage migration is needed and protocol v9 remains compatible because this is an additive session-summary field. Title heuristics remain presentation-only: the sidebar renders empty rows and the reuse decision never calls <code>isDefaultSessionTitle</code>.</p><p>The app&#39;s startup home remains an unpersisted renderer draft so simply opening the application does not create a slot. Explicit New Task actions create or reuse the durable slot. A startup draft still materializes when a first prompt or pasted attachment needs a session.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>New sessions are visible and selectable immediately, before the first user message.</li><li>Repeated New Task clicks in one group are idempotent while different project groups remain independent.</li><li>An older empty session can remain when a newer non-empty session is the group&#39;s latest; the rule intentionally considers only that latest session.</li><li>Empty sessions persist until the user deletes or archives them.</li><li>Protocol v9 gains one additive summary field; storage schema v11 and host ownership remain unchanged.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li>Keep the unpersisted draft from ADR 0084: rejected because the action has no immediate sidebar presence and cannot provide a durable ready slot.</li><li>Continue using a default title as the empty predicate: rejected because a manual rename changes presentation, not transcript state.</li><li>Add a host-side special <code>session.createOrReuse</code> RPC: rejected because the existing renderer navigation boundary can enforce the requested grouping rule without expanding the host method catalog.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0113-immediate-empty-session-slot-and-deduplication.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0113ImmediateEmptySessionSlotAndDeduplication = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0113ImmediateEmptySessionSlotAndDeduplication as default
};
