import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0030: Turn-boundary context checkpoint compaction","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0030-turn-boundary-context-checkpoint-compaction.md","filePath":"adr/0030-turn-boundary-context-checkpoint-compaction.md","lastUpdated":1786032712000}');
const _sfc_main = { name: "adr/0030-turn-boundary-context-checkpoint-compaction.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0030-turn-boundary-context-checkpoint-compaction" tabindex="-1">ADR 0030: Turn-boundary context checkpoint compaction <a class="header-anchor" href="#adr-0030-turn-boundary-context-checkpoint-compaction" aria-label="Permalink to &quot;ADR 0030: Turn-boundary context checkpoint compaction&quot;">​</a></h1><ul><li>Status: Accepted; soft-boundary, model-tool, and visibility clauses superseded in part by ADR 0061, then partly restored by ADR 0064</li><li>Date: 2026-07-28</li><li>Last amended: 2026-08-06</li><li>Amended by: ADR 0049 (failure recovery), ADR 0061 (background compaction; removes the soft-boundary nudge, the <code>CompactContext</code> tool, and the visibility of compaction activity), ADR 0064 (Codex parity; compaction is inline again, the model-facing tool and budget reminders return, and every compaction is visible)</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>A long pi agent run can contain many model/tool turns before <code>agent_end</code>. Compacting only between user prompts or after the run is already terminal does not protect the next provider request inside that loop. The observed Bedrock failure reached 1,077,172 tokens against a 1,000,000-token provider maximum, so the provider rejected the request before PI-Desktop had a recovery point.</p><p>pi-agent-core already supplies session-context reconstruction, token estimation, cut-point selection, summary generation, retained-tail handling, and compaction records. It does not define PI-Desktop&#39;s renderer lifecycle, Rust-owned persistence, provider-headroom policy, or long-loop guard.</p><p>OpenCode Dynamic Context Pruning (DCP), inspected at commit <code>85b6f5ceba144fee9e65eb28dc36cab1b960e418</code>, demonstrates useful behavioral patterns: evaluate context each turn, inject deduplicated guidance before the emergency boundary, and let the model request context management. It is an OpenCode plugin under AGPL-3.0, so directly linking or copying it would add an incompatible runtime and licensing boundary to PI-Desktop.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>PI-Desktop uses pi-agent-core&#39;s public compaction primitives and independently implements the desktop-specific controller.</p><ul><li>The controller runs after every <code>turn_end</code> and before pi starts another provider request. <code>turn_end</code> is a model/tool-turn boundary; only <code>agent_end</code> or <code>error</code> ends the overall desktop run.</li><li>Budgeting uses the pi-ai model context/output limits. Request headroom is at least the configured reserve, the model output allowance capped at 25% of context, or 5% of context. A configured reserve cannot consume more than half the window. The retained-tail target cannot exceed half the remaining hard budget, so small-window models remain compactable. <em>(ADR 0061 keeps this headroom rule and derives the reserve and retention targets from the model window instead of settings. ADR 0064 removes ADR 0061&#39;s background limit and replaces the retained tail with recent user messages only, capped at 20,000 tokens.)</em></li><li>At the soft boundary, a transient system instruction asks the model to call <code>CompactContext</code> with the active-task focus. It is eligible only after tool turns and repeats at most once every three qualifying turns. The instruction is not persisted and does not mutate the durable system prompt. <em>(Superseded by ADR 0061: the soft boundary and this instruction are removed.)</em></li><li><code>CompactContext</code> bypasses the Rust tool/permission bridge because it changes model context, not the workspace. Its call and result use normal tool events and remain visible/durable as a tool activity row. The requested checkpoint is generated only after that tool turn finishes, preserving provider-valid call/result pairing. <em>(ADR 0061 removed the tool; ADR 0064 restores it as <code>new_context</code> with these same semantics, and restores a transcript row — a divider owned by the checkpoint rather than the tool&#39;s activity row.)</em></li><li>At the hard boundary, the runtime generates a checkpoint deterministically. The candidate context is re-estimated before persistence and again before continuation. Automatic summary failure or an ineffective checkpoint first attempts the retained-tail recovery defined by ADR 0049; if that recovery cannot be prepared, durably appended, or brought below the safe budget, the loop stops before another provider request with <code>CONTEXT_COMPACTION_FAILED</code>. <em>(ADR 0061 keeps this path unchanged as the safety net; under ADR 0064 it is again the only path.)</em></li><li>A final tool-result batch is kept with its assistant tool-call carrier. If that atomic batch exceeds the normal retained-tail target, the runtime lets pi move the cut point to the carrier. If the batch itself reaches half the hard budget, PI-Desktop creates a checkpoint-only bounded copy: each result keeps its tool identity/error envelope and a fair share of head/tail text with an explicit checkpoint-truncation marker, while duplicate diagnostic details are omitted. The original durable messages and visible transcript remain complete. The bounded copy is re-estimated before persistence and can never authorize an oversized provider request.</li><li>An exact provider overflow is a final safety net. The failed assistant stays visible for diagnosis but is removed from model context; the runtime creates one checkpoint and retries the model request once. A second overflow is terminal.</li><li>Automatic protection is enabled by default. Disabling it removes the model tool and disables threshold and overflow recovery. Manual <code>/compact</code> remains available for an idle session. <em>(ADR 0061 removes the user-facing switch: protection is always on and persisted <code>enabled: false</code> values are ignored.)</em></li><li>Protocol v6 adds a host-owned <code>session.appendCompaction</code> operation and compaction lifecycle events. The host appends a typed checkpoint line to the session JSONL file. Visible messages are never deleted, rewritten, or hidden by context compaction.</li><li>Only the newest valid checkpoint rebuilds model context. Transcript rewrites preserve it only while its boundary remains present. Forks copy and remap it only when the fork includes that boundary.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Long tool loops receive context checks at the only boundary that can protect the next request without interrupting an active model or tool call.</li><li>The deterministic hard guard does not depend on the model obeying a reminder.</li><li>Restart and model changes retain compacted working context without changing the human-readable conversation.</li><li>Compaction itself is one extra provider request and can fail. Automatic failures have a deterministic, durable retained-tail recovery, while manual compaction remains fail-fast; the lifecycle is explicit, abortable, and surfaced through stable events/error codes.</li><li>A checkpoint may retain a shortened model-facing copy of an oversized atomic tool-result batch. This is a loss of future model detail under pressure, but it is explicit in context, preserves every call/result pair, and does not alter the user-visible or diagnostic transcript.</li><li>Provider token accounting on retained assistant messages is cleared while reconstructing a checkpoint, because that usage described the pre-compacted request and would otherwise overcount the restored context.</li><li>OpenCode DCP updates do not flow into PI-Desktop automatically. Any future behavioral adoption requires a fresh independent implementation review.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><h3 id="integrate-opencode-dcp-directly" tabindex="-1">Integrate OpenCode DCP directly <a class="header-anchor" href="#integrate-opencode-dcp-directly" aria-label="Permalink to &quot;Integrate OpenCode DCP directly&quot;">​</a></h3><p>Rejected because it targets OpenCode&#39;s plugin hooks and persistence model and is AGPL-3.0. PI-Desktop needs a Rust-host durability contract and pi-specific turn lifecycle, so an adapter would retain most of the implementation cost while adding license and upgrade coupling.</p><h3 id="use-pi-compaction-only-at-agent-end" tabindex="-1">Use pi compaction only at <code>agent_end</code> <a class="header-anchor" href="#use-pi-compaction-only-at-agent-end" aria-label="Permalink to &quot;Use pi compaction only at \`agent_end\`&quot;">​</a></h3><p>Rejected because no <code>agent_end</code> occurs between tool turns in one long run. The next provider request can already be oversized.</p><h3 id="rely-only-on-a-model-reminder" tabindex="-1">Rely only on a model reminder <a class="header-anchor" href="#rely-only-on-a-model-reminder" aria-label="Permalink to &quot;Rely only on a model reminder&quot;">​</a></h3><p>Rejected because the model may ignore, postpone, or repeat the request. Soft guidance is useful for summary focus but cannot enforce the provider limit. <em>(ADR 0061 goes further and removes the reminder entirely: with pre-computed checkpoints its only remaining effect was a spent turn and a transcript row. ADR 0064 brings reminders back in a different shape — two budget notices tied to the remaining token count rather than a request to call a tool — and keeps the hard boundary as the enforcement point, exactly as this rejection requires.)</em></p><h3 id="delete-older-visible-transcript-messages" tabindex="-1">Delete older visible transcript messages <a class="header-anchor" href="#delete-older-visible-transcript-messages" aria-label="Permalink to &quot;Delete older visible transcript messages&quot;">​</a></h3><p>Rejected because context management must not destroy user history, revision families, fork inputs, searchability, or diagnostics.</p><h2 id="references" tabindex="-1">References <a class="header-anchor" href="#references" aria-label="Permalink to &quot;References&quot;">​</a></h2><ul><li><code>docs/spec/03-runtime/01-ipc-protocol.md</code></li><li><code>docs/spec/03-runtime/02-agent-runtime.md</code></li><li><code>docs/spec/03-runtime/04-data-storage.md</code></li><li><code>docs/spec/03-runtime/06-host-rpc-protocol.md</code></li><li><code>docs/spec/04-ux/04-builtin-commands.md</code></li><li><code>docs/spec/04-ux/06-settings-ia.md</code></li><li><code>docs/spec/06-delivery/04-e2e-test-plan.md</code></li><li><code>docs/spec/08-meta/decisions-log.md</code> (D158)</li><li><code>https://github.com/Opencode-DCP/opencode-dynamic-context-pruning</code></li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0030-turn-boundary-context-checkpoint-compaction.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0030TurnBoundaryContextCheckpointCompaction = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0030TurnBoundaryContextCheckpointCompaction as default
};
