import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"02. Agent Runtime","description":"","frontmatter":{},"headers":[],"relativePath":"spec/03-runtime/02-agent-runtime.md","filePath":"spec/03-runtime/02-agent-runtime.md","lastUpdated":1790075939000}');
const _sfc_main = { name: "spec/03-runtime/02-agent-runtime.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="_02-agent-runtime" tabindex="-1">02. Agent Runtime <a class="header-anchor" href="#_02-agent-runtime" aria-label="Permalink to &quot;02. Agent Runtime&quot;">​</a></h1><h2 id="_1-goal" tabindex="-1">1. Goal <a class="header-anchor" href="#_1-goal" aria-label="Permalink to &quot;1. Goal&quot;">​</a></h2><p>Applied decisions: <strong>D002/D003/D008/D158/D189/D190/D193/D194/D278/D378</strong>.</p><p>Wrap pi into a product runtime that desktop layers can consume safely.</p><p>Core packages:</p><ul><li><code>@earendil-works/pi-ai</code></li><li><code>@earendil-works/pi-agent-core</code></li></ul><h2 id="_2-runtime-placement" tabindex="-1">2. Runtime placement <a class="header-anchor" href="#_2-runtime-placement" aria-label="Permalink to &quot;2. Runtime placement&quot;">​</a></h2><p>Agent loop runs in a <strong>Node/TypeScript pi sidecar</strong>, not in renderer.</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>packages/agent-runtime/*</span></span>
<span class="line"><span>apps/desktop/electron/* (supervisor)</span></span>
<span class="line"><span>crates/host-core (tool execution + permissions)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><h2 id="_3-core-objects" tabindex="-1">3. Core objects <a class="header-anchor" href="#_3-core-objects" aria-label="Permalink to &quot;3. Core objects&quot;">​</a></h2><h3 id="_3-1-piruntime-node" tabindex="-1">3.1 PiRuntime (Node) <a class="header-anchor" href="#_3-1-piruntime-node" aria-label="Permalink to &quot;3.1 PiRuntime (Node)&quot;">​</a></h3><ul><li>init models/providers</li><li>create Agent</li><li>bind tool bridge</li><li>subscribe/normalize pi events</li></ul><h3 id="_3-2-agenthostfacade-electron-main" tabindex="-1">3.2 AgentHostFacade (Electron main) <a class="header-anchor" href="#_3-2-agenthostfacade-electron-main" aria-label="Permalink to &quot;3.2 AgentHostFacade (Electron main)&quot;">​</a></h3><ul><li>session routing</li><li>process supervision</li><li>IPC translation</li></ul><h3 id="_3-3-host-tool-bridge-rust" tabindex="-1">3.3 Host Tool Bridge (Rust) <a class="header-anchor" href="#_3-3-host-tool-bridge-rust" aria-label="Permalink to &quot;3.3 Host Tool Bridge (Rust)&quot;">​</a></h3><ul><li>receives tool call requests</li><li>applies permission policy</li><li>executes builtin/plugin tools</li><li>returns normalized tool results</li></ul><h2 id="_4-runtime-api-package-level" tabindex="-1">4. Runtime API (package-level) <a class="header-anchor" href="#_4-runtime-api-package-level" aria-label="Permalink to &quot;4. Runtime API (package-level)&quot;">​</a></h2><div class="language-ts vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">interface</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> AgentRuntime</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> prompt</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">input</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> PromptInput</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> Promise</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&lt;{ </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">turnId</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> }&gt;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> steer</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">input</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> RuntimePrompt</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">expectedTurnId</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">message</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> UiMessage</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">accepted</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> boolean</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">turnId</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> requestGracefulStop</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">()</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">requested</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> boolean</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> abort</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">turnId</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> Promise</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">void</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&gt;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> getStatus</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">()</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> RuntimeStatus</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> dispose</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">()</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> Promise</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">void</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&gt;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> subscribe</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">handler</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> (</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">event</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> NormalizedAgentEvent</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> void</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> () </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> void</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br></div></div><p><code>requestGracefulStop()</code> is a one-shot request for the active runtime. The pi loop evaluates it after <code>turn_end</code>, once the current assistant response and tool batch have completed, and emits a normal <code>agent_end</code> before another model request. It does not cancel an active provider stream or running tool. An idle runtime returns <code>{ requested: false }</code>; immediate <code>abort()</code> remains the separate cancellation path.</p><h3 id="_4-0-active-turn-steering" tabindex="-1">4.0 Active-turn steering <a class="header-anchor" href="#_4-0-active-turn-steering" aria-label="Permalink to &quot;4.0 Active-turn steering&quot;">​</a></h3><p><code>steer</code> validates the current turn identity before changing any state, then queues user input through pi-agent-core&#39;s native steering queue in <code>all</code> mode. The current provider request and any started tool batch finish first; all accepted input is included at the next model-request boundary within the same durable turn. A live provider request is not rewritten or aborted. Main owns attachment validation and transcript persistence as for ordinary prompts.</p><p>Queued input retains its renderer message id when pi consumes it, including when another input arrives before the initial user message has been consumed. An admission after pi&#39;s last queue poll suppresses the terminal event and continues once pi has released the run, with the same turn identity and without a second public <code>agent_start</code>. Existing context/provider recovery takes precedence over that continuation. Steering also wakes a parent that is idle waiting for background delegates; it does not cancel those delegates.</p><p>Abort, graceful stop, fatal errors and terminal settlement close admission. Accepted but unconsumed input remains transcript/context history and is removed from pi&#39;s steering queue so it cannot execute independently on a later turn. An ordinary follow-up stays in the separate Host-owned FIFO until durable turn finalization. A steering failure must not terminate the active run.</p><h3 id="_4-1-session-title-summarization" tabindex="-1">4.1 Session title summarization <a class="header-anchor" href="#_4-1-session-title-summarization" aria-label="Permalink to &quot;4.1 Session title summarization&quot;">​</a></h3><p>The renderer applies a short first-prompt fallback immediately so sending a prompt never waits on title generation. After the first turn emits <code>agent_end</code>, Electron main resolves the session&#39;s effective provider/model and invokes the runtime&#39;s <code>summarizeSessionTitle</code> one-shot path with thinking disabled. The runtime supplies the initial user prompt and an optional assistant reply, returns only sanitized title text, and treats an empty/failing completion as a non-fatal result. The renderer persists a successful title through the existing <code>session.rename</code> path.</p><p>The renderer also persists a <code>manualTitle</code> marker in its local session metadata. Automatic summarization is skipped for that marker and for any persisted title that is neither a recognized default nor the deterministic first-prompt fallback, which protects manual and already-summarized titles after restart. No host RPC or storage schema change is required.</p><h2 id="_5-prompt-flow" tabindex="-1">5. Prompt flow <a class="header-anchor" href="#_5-prompt-flow" aria-label="Permalink to &quot;5. Prompt flow&quot;">​</a></h2><ol><li>load the durable session and reject a missing session</li><li>resolve that session&#39;s mode/provider/model and project binding (app/current workspace defaults are legacy fallback only)</li><li>resolve the complete models.dev metadata record for the exact provider/API URL and model and clamp the durable session thinking level to its nearest supported value; an ID absent from the snapshot uses the explicit generic fallback</li><li>validate model/secret availability</li><li>reject if session busy; the renderer queues a user-facing next prompt and does not call this path until the current session reaches <code>agent_end</code></li><li>validate structured attachments at Electron main&#39;s session-bound path boundary, persist image bytes by SHA-256, and retain only attachment refs in the durable user message. Only an image that is within the 10 MB inline bound for a vision model is read into memory; larger images use streamed hashing/copying and the existing safe path fallback</li><li>snapshot the effective shell ID and dialect for the turn</li><li>start pi turn with the resolved session configuration and effective thinking level; HTTP 429 setup and stream failures use the runtime-owned silent ten-retry budget, while other transient transport/provider failures share a runtime-owned bounded ten-retry budget across the setup and stream phases (D127, D186, D245, D258, D378)</li><li>stream normalized answer and thinking events to UI</li><li>on tool calls, delegate to Rust host bridge with the durable <code>sessionId</code>; host resolves the session-bound workspace root</li><li>if pi finishes a message with <code>stopReason: &quot;error&quot;</code>, finalize any partial assistant bubble with a structured <code>UiMessage.error</code>, persist it in the transcript, and emit a normalized lifecycle <code>error</code> event carrying the same provider <code>AppError</code>; even a failure with no answer text remains a visible assistant error message</li><li>finalize and persist successful answer/thinking blocks independently</li></ol><p>While an active turn has no new transcript row, the runtime emits a normalized <code>status</code> event that names the quiet interval: <code>starting</code> for the prompt handoff, <code>waiting-model</code> while a provider request waits for its first assistant event, <code>preparing</code> after a tool batch and before the next request, <code>compacting</code> during a context checkpoint, <code>recovering</code> during a silent-turn re-run, <code>retrying</code> during a bounded provider backoff, and <code>waiting-subagents</code> while the parent waits for delegated work (including each running target&#39;s latest coarse child action). The renderer keeps the phase scoped to the session and clears it when assistant or tool activity starts, or when the turn terminates. This is observability only; it does not add a second agent loop or a completion percentage.</p><p>The runtime constructs exactly one pi <code>Agent</code> per durable session. Plan does not select a second model, planner service, permission implementation, or runtime. The same Agent changes its planning state and tool registry after a host-confirmed transition.</p><h3 id="_5d-bounded-provider-recovery-and-diagnostics-d186-d245-d259-d378-adr-0091-adr-0128-adr-0206" tabindex="-1">5d. Bounded provider recovery and diagnostics (D186, D245, D259, D378, ADR 0091, ADR 0128, ADR 0206) <a class="header-anchor" href="#_5d-bounded-provider-recovery-and-diagnostics-d186-d245-d259-d378-adr-0091-adr-0128-adr-0206" aria-label="Permalink to &quot;5d. Bounded provider recovery and diagnostics (D186, D245, D259, D378, ADR 0091, ADR 0128, ADR 0206)&quot;">​</a></h3><p>Provider request setup and stream delivery are separate failure phases, but HTTP 429 handling is one response-recovery policy. pi-ai&#39;s nested adapter retry is disabled for this path so the runtime can share one budget across both phases.</p><p><code>PROVIDER_RATE_LIMITED</code> receives at most ten retries after the initial attempt, for eleven provider attempts total. A setup 429 is retried inside the provider stream adapter. A mid-stream 429 removes the failed assistant from the next model context and calls <code>continue()</code> in the same turn. Both phases claim the same counter, so a setup 429 followed by a stream 429 cannot reset or multiply the budget. The captured response status is applied before classifying the provider message in both phases, so a generic 429 body still enters the 429 budget while known non-retryable classifications remain terminal. The main session and builtin subagents use the same controller and policy.</p><p>429 retries are silent at the transcript lifecycle: no intermediate assistant error, lifecycle <code>error</code>, <code>turn_end</code>, <code>agent_end</code>, or duplicate assistant bubble reaches the UI. A normalized <code>status</code> event identifies the retry backoff so the user can tell that the turn is still active. The visible assistant message id is reused when a retry starts, replacing any partial content in one bubble. End events are emitted once by the final successful or exhausted attempt. An abort during the wait cancels the timer and prevents the next provider request.</p><p>The delay follows the OpenCode-style order <code>retry-after-ms</code>, <code>retry-after</code> seconds, <code>retry-after</code> HTTP-date, then exponential backoff. The fallback starts at 2 seconds and grows exponentially with up to 25% positive jitter; every server or calculated value is capped at 30 seconds. The runtime captures the failed response status and headers from fetch because pi-ai&#39;s ordinary response callback only covers an established response.</p><p>Non-429 transient failures share their own bounded response-recovery budget of ten retries after the initial attempt, for eleven provider attempts total. The budget is shared by request setup and stream delivery, so a fault that moves between phases cannot reset or multiply it, and it is separate from the 429 budget. It admits exactly <code>NETWORK_ERROR</code>, <code>TIMEOUT</code>, <code>STREAM_FAILED</code>, and retryable <code>PROVIDER_ERROR</code> — including a gateway <code>502</code>/<code>503</code>/<code>504</code> that arrives before headers or mid-stream. Authentication, model-selection, malformed-request, context, and other non-retryable errors do not enter either provider replay path, and a non-retryable <code>PROVIDER_ERROR</code> from a malformed 400/422 request stays terminal.</p><p>Both budgets reset after a complete, non-error, non-aborted model response, including a response that requests tools. The next model request starts with fresh counters and backoff, even within the same user turn. Receiving HTTP headers, partial text, or changing failure phase does not reset either budget. This rule applies to the main session and builtin subagents: a long task with independent recovered outages must not eventually stop because earlier tool rounds consumed the budget. One-shot completions still use one bounded budget for their single response. Persistent failures remain bounded and abortable.</p><p>Before surfacing a pre-stream <code>PROVIDER_ERROR</code> for HTTP 400/422 whose message ends in <code>(no body)</code>, the runtime makes at most one silent repair attempt with the generated output-limit fields removed: <code>max_tokens</code>, <code>max_completion_tokens</code>, and <code>max_output_tokens</code>. This repair does not consume the transient retry budget or add backoff, and the caller&#39;s <code>onPayload</code> rewrite remains active. A second opaque failure is terminal, and an abort before the repair starts prevents the repair request.</p><p>The non-429 delay honors the server first: <code>retry-after-ms</code>, <code>retry-after</code> seconds, then <code>retry-after</code> HTTP-date, capped at 8 seconds. Captured headers are retained for every status that can carry a usable delay (429, 408, 409, and 5xx), not for 429 alone. Without a usable header the wait is a plain doubling schedule of 1, 2, 4, then remains at the 8-second cap for later retries. The schedule is identical in the request and stream phases so a fault that moves between them keeps one predictable rhythm. It is deterministic — no jitter — because it paces one failed request rather than a synchronized rate-limit burst. A server-stated delay wins outright, including one shorter than the scheduled wait.</p><p>Only the failed request is replayed. The session, its transcript, and its tool state are untouched: the failed assistant is removed from the next model context and the same visible message id is reused, so a retry never restarts the turn or re-runs a completed tool call.</p><p>The application setting <code>infiniteProviderRetry</code> is off by default. When enabled, the main session and its builtin subagents skip only the ten-retry ceiling for <code>NETWORK_ERROR</code>, <code>TIMEOUT</code>, <code>STREAM_FAILED</code>, retryable <code>PROVIDER_ERROR</code> (including 5xx gateway failures), and <code>PROVIDER_RATE_LIMITED</code>. The same backoff, <code>Retry-After</code> precedence, visible retry status, and abort/Stop path remain in force. Non-retryable errors, context recovery, compaction, tool execution, and one-shot completions are unchanged. The setting can keep billing requests alive indefinitely until the user stops the turn. Settings reads expose an explicit boolean for this flag: absent or disabled values normalize to <code>false</code>. Read-modify-write operations on unrelated settings must remain valid without enabling retries; non-boolean writes stay invalid. Each retry is abortable and reports its current backoff through the normalized status event. The <code>retrying</code> activity carries the classified error code, the bounded/redacted provider message, and the HTTP status when known. The main session, builtin subagents, and one-shot composer enhancement use the same codes, budget size, and precedence.</p><p>When the retry budget is exhausted, the final assistant error and lifecycle <code>error</code> are emitted once. Provider failures carry bounded diagnostics in <code>AppError.details</code> when available: <code>phase</code> (<code>request</code> or <code>stream</code>), <code>providerStatus</code>, <code>providerCode</code>, <code>providerWaitMs</code>, <code>streamMs</code>, <code>retryAttempt</code>, the network diagnosis (<code>networkCategory</code>, <code>networkCode</code>, <code>networkSyscall</code>, <code>networkHost</code>, <code>networkRoute</code>) and the request correlation (<code>requestMessages</code>, <code>requestBytes</code>, <code>compactionGeneration</code>). For a persistent 429 or non-429 transient failure, <code>retryAttempt</code> is <code>10</code>, derived from the exhausted error class&#39;s budget rather than temporary retry activity state. Credentials and unrestricted response bodies never enter the event or log. The active-turn status shows the remaining backoff and the retry budget as <code>Retrying in 0s · attempt 9/10</code> in English.</p><p>Each retry builds a new request, stream, and <code>AbortController</code>; the one piece of state a retry shares is the process-wide undici dispatcher. When the same origin fails twice in a row without any response and no fresh attempt reaches it, the transport is rebuilt once (throttled to one rebuild every 30 seconds, never for <code>dns</code>) before the next attempt, so the retry does not replay into a pool whose connection is already dead. The rebuild installs the replacement before closing the previous dispatcher and closes it gracefully, and it reproduces the configured route, so another session&#39;s in-flight request finishes on the pool it started on and a proxy is never silently dropped.</p><h3 id="_5e-silent-turn-recovery" tabindex="-1">5e. Silent-turn recovery <a class="header-anchor" href="#_5e-silent-turn-recovery" aria-label="Permalink to &quot;5e. Silent-turn recovery&quot;">​</a></h3><p>An ordinary turn that ends with no tool call and no visible assistant text is invisible to the user: reasoning is never rendered, so a conclusion written only there did not arrive. 15 of 255 recorded sessions ended a turn that way, and the user&#39;s only recourse was typing &quot;继续&quot;.</p><p>The runtime detects it at <code>message_end</code>: the stop was neither an error nor an abort, the message requested no tools (no <code>toolCall</code> content part), and the visible text is blank after trimming. Reasoning content does not exempt a turn — a thinking-only turn is exactly the case that needs recovery.</p><p>Recovery mirrors §5d and is bounded the same way: at most one re-run per run. The silent assistant is dropped from the model context (<code>continue()</code> refuses a transcript ending in an assistant message, and an empty one is not worth resending), a short no-output instruction is appended to the system prompt for that one continuation, and the bubble id is reused so a recovered turn leaves no empty row behind. The silent attempt&#39;s <code>turn_end</code> and <code>agent_end</code> are suppressed; the re-run emits the single terminal lifecycle.</p><p>Recovery is armed inside <code>message_end</code> and carried out once the loop is idle, so it belongs to every entry point that drives the loop — a user prompt and an approved plan or goal execution alike. Each entry point clears the recovery state before it starts and runs the pending recovery after <code>waitForIdle</code>, through one shared implementation of each half. The shared drain also handles recoveries armed by a recovery attempt before it returns, so a chained failure cannot leave lifecycle suppression active with no recovery or terminal event. Skipping either half ends the run with its lifecycle still suppressed and no recovery attempted, which reaches the user as a session that stopped mid-work with no error and no retry action. §5d overflow and provider-stream retry ride the same contract, and a suppression flag left behind would swallow the <em>next</em> run&#39;s terminal events.</p><p>The one-shot instruction rides on the agent&#39;s system prompt rather than the <code>prepareNextTurn</code> hook, because that hook only shapes turns inside a live run and this run has already ended. It is removed afterwards unless a path-scoped instruction reload rewrote the prompt meanwhile, in which case the newer rebuild wins.</p><p>If the re-run is silent too, the turn ends as a visible assistant error with retriable <code>EMPTY_MODEL_RESPONSE</code>, which gives the transcript its normal retry action. No empty assistant message is persisted in either case.</p><p>A Host-ledger completion notice (ADR 0239, D446) is the narrow exception: its prompt already permits no acknowledgement. Main resolves the queued message by ID, verifies its target session, and constructs provenance from the ledger. The runtime accepts silence only for <code>kind: completion</code> targeting the current session with nonempty message and reply-to IDs. A silent notice reply emits its normal completed message and terminal lifecycle without a recovery request or <code>EMPTY_MODEL_RESPONSE</code>. Provider errors and aborts retain their normal handling, and a provider retry of the same attempt keeps the exception. The original task/result is not rewritten, and completion notices never request another callback.</p><p>The exception covers exactly the notice&#39;s own reply: the first settled assistant response of the run spends it, whether that response is silent, textual, or a tool batch. A reply that follows tool results is therefore ordinary work under this section, and the exception is revoked as soon as an accepted steering message enters the model context, so a reply to the user keeps its full re-run and error path. Every new run recomputes it from the prompt&#39;s provenance. Ordinary user input, task/message deliveries, copied source framing, and restored history cannot enable it.</p><p>An accepted silent reply is still not worth resending. Main persists it as an empty completed row (the transcript hides it and the host stores no text), but the runtime keeps it out of its entries and out of pi&#39;s transcript state, and the context projection drops any assistant with no content blocks, exactly as a restored transcript already did. The next provider request therefore carries no empty assistant message.</p><p>Decision D193 and D446 (ADR 0239 amendment); see E2E-146 and E2E-SESSION-completion-notice-allows-silence.</p><h3 id="_5e-1-progress-only-recovery-for-approved-plan-goal-execution" tabindex="-1">5e.1. Progress-only recovery for approved Plan/Goal execution <a class="header-anchor" href="#_5e-1-progress-only-recovery-for-approved-plan-goal-execution" aria-label="Permalink to &quot;5e.1. Progress-only recovery for approved Plan/Goal execution&quot;">​</a></h3><p>An approved Plan or Goal can still stop after a visible progress update when the model puts its narration in a text-only assistant message and emits the tool call in a later message. Ordinary Agent prompts do not use this recovery. The runtime only arms it for a successful, non-aborted message with no tool call whose visible text has a clear forward-looking action signal such as &quot;Writing the remaining note&quot; or &quot;the next step is ...&quot;. A normal completion report such as &quot;Implemented the approved plan&quot; is terminal and is not nudged.</p><p>The recovery is bounded to one attempt per approved execution. The progress text remains in the current assistant bubble, while its assistant message is removed from model context before <code>continue()</code> so the provider never receives an invalid assistant-to-assistant transcript. The first attempt&#39;s <code>agent_start</code>, <code>turn_start</code>, <code>turn_end</code>, and <code>agent_end</code> are suppressed; the continuation reuses the same bubble id and emits the single terminal lifecycle. The progress nudge is attached to the system prompt for that continuation and removed afterwards. If the continuation produces a tool call, the normal autonomous loop proceeds; if it produces another text-only response, that response is terminal and cannot trigger a second progress nudge. A silent recovery that already ran in the same execution also prevents the recovered final report from being misclassified as progress.</p><p>See E2E-146a.</p><h3 id="_5-1-context-checkpoint-protection-d158-d203-adr-0030-0049-0061-0064" tabindex="-1">5.1 Context checkpoint protection (D158/D203, ADR 0030/0049/0061/0064) <a class="header-anchor" href="#_5-1-context-checkpoint-protection-d158-d203-adr-0030-0049-0061-0064" aria-label="Permalink to &quot;5.1 Context checkpoint protection (D158/D203, ADR 0030/0049/0061/0064)&quot;">​</a></h3><p>The complete visible transcript and the model context are separate views of the same session. A durable checkpoint summarizes older model context while the renderer continues to show every original user, assistant, and tool row.</p><p>PI-Desktop reuses pi-agent-core&#39;s <code>convertToLlm</code>, <code>estimateContextTokens</code>, <code>prepareCompaction</code>, and <code>compact</code> primitives, and applies the same session context projection pi used to export as <code>buildSessionContext</code> (slice from the newest compaction, then <code>compactionSummary</code> before the retained tail). pi 0.85 moved that helper off the public package export and made the remaining internal builder async for custom-entry projectors; the desktop runtime keeps a synchronous local copy because it synthesizes only message and compaction entries. The desktop runtime owns when they run and how the result crosses the Rust storage boundary; OpenCode DCP is an AGPL-3.0 behavioral reference only, not a linked or copied dependency.</p><p>Compaction follows Codex&#39;s mechanism (ADR 0064): it always happens inline at a turn boundary, the model can request it through <code>new_context</code>, every compaction adds a transcript row and raises one warning toast, and there is no pre-computation anywhere.</p><p>pi 0.84.4+ invokes <code>prepareNextTurn</code> only when the loop will start another assistant turn in the same run — including between a completed tool batch and the follow-up model request. A new user prompt still compacts before its first provider request through <code>automaticCompactionNeeded</code> in <code>prompt()</code>.</p><p>For every pi loop turn:</p><ol><li>pi emits and awaits <code>turn_end</code> after the assistant message and all tool results for that turn are complete</li><li>PI-Desktop rebuilds the context from the full transcript plus the newest valid checkpoint and estimates the next request budget</li><li>below the hard boundary, and with no pending model request, the next turn proceeds unchanged</li><li>at or above the hard boundary, or when the model called <code>new_context</code>, compaction runs synchronously before the next provider request. In the summary family, generation is mandatory; the runtime preflights the summary input against the model window, reduces it once, and then splits a range that still does not fit into chunks that each do, so a prompt too large for one request is summarized by several (ADR 0302). An automatic summary failure first attempts a deterministic retained-tail checkpoint, while manual compaction still reports <code>CONTEXT_COMPACTION_FAILED</code></li><li>successful generation or deterministic recovery first appends the checkpoint through host-core, then installs its summary plus the applicable retained tail as the runtime context for the next provider request; a hard-boundary checkpoint is re-estimated before it is persisted and again before continuation, and cannot authorize the next request unless it is below the hard budget</li></ol><p>Checkpoint generation and installation are separate operations. <code>buildCheckpoint</code> runs the preparation, budget preflight, and summary request without persisting anything or changing the active checkpoint; installation re-estimates, appends through host-core, updates the active checkpoint, and emits <code>compaction_end</code>. The blocking path composes the two back to back.</p><p><strong>What survives a checkpoint.</strong> A successful checkpoint leaves the model context as the summary plus, at most, one <strong>user</strong> message; assistant and tool messages are dropped from model context and remain in the visible transcript. pi&#39;s <code>prepareCompaction</code> still chooses the cut point, so its turn-boundary and split-turn handling are preserved, but the runtime then folds the split-turn prefix and the recent tail back into the summary input, so the summary covers the whole compacted range and nothing crosses the boundary uncovered.</p><p>A <strong>retained-tail fallback</strong> is the exception, because no summary covers its range: it keeps the real recent window — the newest contiguous messages of the compacted range, every role, bounded by the keep-recent target and by what the safe budget leaves once the carried-forward summary and the recovery notice are paid for — and records <code>details.retainedTailShape</code> as <code>recent_window</code>, so a rebuild replays that window instead of narrowing it back to one user message (ADR 0302, issue #827). It drops the assistant messages pi drops from the rebuilt context anyway (error, aborted, deferred) and any tool result whose tool call is not in the window, because a provider rejects a result whose call is missing. An <code>active_turn</code> fallback also keeps the active task&#39;s user message ahead of the window when the window itself cannot hold it, so the continuation never loses its goal.</p><p>The retention mode is selected from the lifecycle that requested compaction. It decides what a <em>successful</em> checkpoint leaves behind:</p><ul><li>An <code>active_turn</code> checkpoint is created while the provider must continue the current task after a tool result, a <code>toolUse</code> turn, or overflow recovery. It carries only the latest user message from the compacted range, up to the retention limit below; if that message crosses the limit it is truncated rather than dropped (<code>[checkpoint truncated: this message crossed the retained context budget]</code>).</li><li>A <code>completed_turn</code> checkpoint is created at a terminal turn boundary, before a new user prompt, or for manual compaction. It carries no naked historical user messages. The summary is authoritative for completed work, and the next user prompt is the only new task after the checkpoint.</li><li>A retained-tail fallback carries its recent window in both modes, because the alternative is an empty context; the mode still decides whether it has to keep the active task&#39;s user message as well.</li><li>The <code>fresh_window</code> family remains the deliberate no-summary exception from ADR 0064 and always carries an empty tail.</li></ul><p>The retained-tail mode and shape are stored in the checkpoint&#39;s opaque <code>details</code> field, so a restart preserves the same task boundary and the same treatment of the tail. A checkpoint without the shape marker — every successful checkpoint, and every record written before the marker existed — is normalized to its latest user message only, so a restart cannot restore a sequence of executable-looking old requests. Dropping an assistant message also drops its tool calls, and the fallback drops the results of calls it did not keep, so no orphaned tool call or unmatched result can reach the provider. The retained tail is re-estimated with the summary before persistence and before continuation, so an oversized request still cannot pass the guard. A fallback also records <code>details.failureReason</code> — <code>no_new_history</code>, <code>summary_budget</code>, <code>summary_provider</code>, or <code>checkpoint_oversized</code> — a closed vocabulary rather than provider error text, which can carry endpoint details.</p><p><strong>Two compaction families.</strong> Both run the same lifecycle — budget re-estimation, host-core append, <code>compaction_end</code>, transcript row, warning:</p><ul><li><code>summary</code> (the default) requests a summary from the model;</li><li><code>fresh_window</code> requests nothing and installs a checkpoint with an empty retained tail and a fixed marker text saying the history was reset without being summarized.</li></ul><p>The family is resolved from a construction option, then <code>PI_DESKTOP_COMPACTION_STRATEGY</code>. It is not a setting, is absent from <code>AppSettings</code> and i18n, and exists so the no-summary mechanism is implemented and testable.</p><p><strong>Model-facing surface.</strong> <code>new_context</code> takes no parameters and starts a new context window at the next turn boundary; it never clears or resets environment state. Two budget reminders are appended to the current turn&#39;s system prompt, each at most once per checkpoint window and reset when a checkpoint is installed: one when the remaining budget falls to <code>clamp(hardLimit * 0.15, 8k, 32k)</code>, asking the model to start closing out, and one at 2,000 tokens remaining, telling it to write down whatever must survive. Neither reminder is persisted or shown in the transcript.</p><p>The hard boundary is the model context window minus request headroom. Headroom is the maximum of a 16,384-token reserve floor, model maximum output capped at 25% of the context window, and a 5% safety margin. The reserve floor is itself capped at half the window. The cut-point target passed to pi is derived from the model window as 20% of the hard budget clamped to 8,000–64,000 tokens, then capped at half the hard budget; it decides where the boundary falls, not what survives it. The active-user retention limit is 20,000 tokens, capped at half the hard budget so retention alone cannot fill a small window and leave the summary no room. A fallback&#39;s window is capped by 25% of the hard budget and by the room the carried-forward summary and the recovery notice leave, so recovery cannot install a checkpoint the guard rejects. None of these values are configurable.</p><p><strong>Estimate calibration (D606).</strong> Every threshold above is compared against one number, and that number is corrected against what requests actually cost. pi&#39;s <code>estimateContextTokens</code> anchors on the last assistant usage and estimates everything after it as <code>chars / 4</code>: that constant under-counts CJK text, and with no anchor left it omits the system prompt and the tool schemas, which the next request still pays for. The two errors are measured and applied separately — the per-character bias as a scale-free ratio over the guessed tail, and an unanchored residual as a ratio only for observations taken at a comparable scale (0.5×–2× of the estimate), otherwise as the observed overhead capped at 32,000 tokens.</p><p>The correction is asymmetric because this number gates compaction: upward applies once three observations exist, downward needs three agreeing samples, is capped at 15 % per step and can never take the value below 85 % of the raw estimate, so a projection at 1.18× the hard limit (<code>1 / 0.85</code>) still compacts. A report outside 0.5×–3× of what the calibration predicted is treated as a misreport, and two consecutive misreports freeze the downward direction until a usable report arrives.</p><p>The provider request layer also caps the concrete output budget before every parent, subagent, and one-shot request. It estimates the serialized input with the pi-ai chars/4 baseline plus a CJK correction, then reserves the larger of 4,096 tokens or 1% of the effective window. The effective window is the smaller of the configured value and the published catalog window when both are known; this prevents an oversized user override from bypassing compaction and output protection. Unknown windows preserve the configured output budget.</p><p>The incoming user prompt participates in budgeting before the first provider request. The automatic summary request retries transient provider failures under a bounded pi-ai retry policy (3 retries, 2s/4s/8s backoff, cancelled by Stop); deterministic failures such as quota or auth return at once. The preflight guard sizes the prompt pi actually serializes — tool results already capped — rather than the raw messages, and when that prompt still exceeds the window it tries exactly one reduced input (tool results cut to a short prefix, thinking dropped, no message removed). If even that input does not fit, the range is split into contiguous chunks that each do: at most 16 requests, each carrying the previous chunk&#39;s summary through pi&#39;s update-the-summary prompt, with the range&#39;s file list appended on the last one, and the checkpoint reports the summed usage of the requests that produced it. A budget therefore decides how many requests a summary takes, not whether the model is asked at all (ADR 0302); only an empty range, or one past that request bound, still gives up on the summary (ADR 0282). If normal compaction still fails during an automatic threshold or overflow recovery, the runtime persists a short recovery checkpoint with the previous summary (when available) and the bounded recent window described above. The complete transcript remains durable and visible, while the next model request receives only that recovery checkpoint and applicable tail. The lifecycle event marks this as <code>fallback: &quot;retained_tail&quot;</code>, and the checkpoint&#39;s mark carries the same <code>fallback</code>, so the renderer shows a warning and labels the transcript row as a failed summary rather than a false success. If the fallback cannot be prepared, persisted, or kept below the safe budget, the user row and an assistant error remain durable and no provider request starts. Provider-reported context overflow is the last recovery layer: omit the failed assistant from model context, compact once, and retry once. A second overflow remains terminal. Bedrock&#39;s <code>prompt is too long: N tokens &gt; M maximum</code> form maps to this path.</p><p>Automatic protection is always enabled and is not user-configurable. The runtime still accepts a construction-time override that disables it, used by tests; persisted <code>contextCompaction</code> settings are ignored so a session cannot be left with the guard off and no way to restore it. Manual <code>/compact</code> remains available while the session is idle. Checkpoint generation is abortable and counts as running state until durable persistence completes.</p><p>The file list a checkpoint carries is read out of the summarized range by pi&#39;s own collector, which recognizes the lowercase spellings <code>read</code> / <code>write</code> / <code>edit</code> — the names pi&#39;s tools carry. PI-Desktop registers <code>Read</code> / <code>Write</code> / <code>Edit</code>, so the runtime converts exactly those three on the way into pi&#39;s preparation (<code>withPiFileOpToolNames</code>): nothing stored changes, and every other tool name is left spelled the way we register it. Without that conversion a checkpoint&#39;s <code>readFiles</code> / <code>modifiedFiles</code> and the <code>&lt;read-files&gt;</code> section of a summary were always empty (D618).</p><p>A delegate (§5f) runs the same derivation against its own resolved model and compacts at its own turn boundaries, without a durable checkpoint chain of its own (ADR 0299).</p><h2 id="_5b-operating-mode-and-planning-state" tabindex="-1">5b. Operating mode and planning state <a class="header-anchor" href="#_5b-operating-mode-and-planning-state" aria-label="Permalink to &quot;5b. Operating mode and planning state&quot;">​</a></h2><ul><li>Default product mode: <strong>Agent</strong></li><li>The product selector is <strong>Agent | Plan | Goal</strong>; the internal conversation page may still use <code>page = &quot;chat&quot;</code></li><li>Mode is session-scoped and persisted with session metadata</li><li>Thinking level is session-scoped and persisted with session metadata</li><li>Host configuration is mutable only while the session is idle. The renderer keeps mode/provider/model/thinking/permission controls editable during a run, treats the latest selection as next-turn state, and flushes one full configuration after the terminal event.</li><li>Changing mode/provider/model/thinking level applies to the next turn and recreates the pi runtime when any runtime-affecting configuration changes; no in-flight runtime observes a queued renderer choice.</li><li>The live planning indicator follows the turn that is actually running. A mode choice staged while a turn runs never flips the projected <code>planning</code>/<code>inactive</code> state mid-turn: the renderer keeps the state of the in-flight turn and only moves it to the staged contract mode after the terminal event flushes the configuration, so <code>Plan / planning</code> surfaces when the next prompt starts under the new mode. The Composer mode chip pulse and the compact transcript Planning row are that same live projection: the chip label may already show the staged mode, but it does not pulse until the in-flight turn projects <code>planning</code>.</li></ul><p>The live planning state is derived and projected as:</p><div class="language-ts vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">type</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> OperatingMode</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;agent&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;plan&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;goal&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">type</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> ProposalKind</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;plan&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;goal&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">type</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> PlanningState</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;inactive&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;planning&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;awaiting_approval&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">type</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> PlanExecutionState</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;queued&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;running&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;completed&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;interrupted&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p>Plan and Goal are the two <strong>contract modes</strong> (D198). They share one durable approval table, one projected <code>PlanningState</code>, one approval surface, and one execution queue; a <code>kind</code> discriminator (<code>plan</code> | <code>goal</code>) on the proposal selects the prompt, the artifact directory, and the user-facing copy. <code>Agent</code> is the only mode with no kind, and is the only mode that executes freely. Because the projection is shared, <code>planning</code> and <code>awaiting_approval</code> are always read together with the kind to know which durable mode a session is in.</p><p><code>Agent / inactive</code> enters <code>Plan / planning</code> either when the user selects Plan while idle or when the Agent calls <code>EnterPlanMode</code>. In Plan, the Agent can inspect, use context controls, run Bash through the selected permission mode, and call <code>SubmitPlan(title, markdown, question)</code>. Host-core preserves the submitted Markdown bytes in a new immutable <code>.pi/plan/&lt;unique-name&gt;.md</code> artifact, records its relative path/hash/size and structured title/question in <code>plan_approvals</code>, and moves the live state to <code>awaiting_approval</code>.</p><p>Approval has only <code>approve</code> and <code>reject</code>. Approval commits <code>mode = agent</code>, the explicit permission mode, an execution ID, and <code>execution_state = queued</code> on the same <code>plan_approvals</code> row in one host transaction. The same Agent then receives a fresh model turn with the Agent tool set. Reject, absolute expiry, a pending interruption, stale response, or persistence failure closes the approval row and returns the live state to editable <code>Plan / planning</code> without granting execution tools. A later accepted Plan prompt is a new turn: earlier <code>SubmitPlan</code> calls remain historical immutable checkpoints, and the Agent must call <code>SubmitPlan</code> once with a new complete Markdown snapshot to create a new artifact. If approval already committed and a queued/running execution is interrupted, durable mode remains Agent and the execution is not replayed.</p><p>Manual mode and configuration selection may be staged by the renderer while a turn runs, but host persistence remains idle-only. Selecting Agent is an intentional user override and does not synthesize a plan or approval. Each session has one active turn, one pending approval, and one queued/running execution; a second prompt or execution is rejected, while staged configuration is submitted only after the session is idle.</p><p><code>Agent / inactive</code> enters <code>Goal / planning</code> the same two ways, by user selection while idle or by the Agent calling <code>EnterGoalMode</code>. Goal has the identical tool surface as Plan, except that its submit tool is <code>SubmitGoal(title, markdown, question)</code> and its artifact is written to <code>.pi/goal/&lt;unique-name&gt;.md</code>. The submitted Markdown is a <strong>goal contract</strong> — the outcome to reach, the acceptance criteria that prove it was reached, and the boundaries that must not be crossed — not a list of implementation steps. A submit tool is rejected with <code>PLAN_KIND_MISMATCH</code> when the session&#39;s active kind is the other one, and with <code>PLAN_NOT_ACTIVE</code> when no contract is active.</p><p>Goal approval commits exactly what Plan approval commits: <code>mode = agent</code>, the explicit permission mode, an execution ID, and <code>execution_state = queued</code> on the same row. The queued execution instruction differs by kind. An approved plan is replayed as steps to follow; an approved goal instructs the Agent to choose its own approach, verify every acceptance criterion by running the checks the contract names, keep working while a criterion is unmet and an untried approach remains, stop early only when a boundary blocks it, and close with a criterion-by-criterion report of what was met and the evidence observed.</p><h2 id="_5c-thinking-capability-and-stream-contract" tabindex="-1">5c. Thinking capability and stream contract <a class="header-anchor" href="#_5c-thinking-capability-and-stream-contract" aria-label="Permalink to &quot;5c. Thinking capability and stream contract&quot;">​</a></h2><ul><li>Canonical levels are <code>off</code>, <code>minimal</code>, <code>low</code>, <code>medium</code>, <code>high</code>, <code>xhigh</code>, and <code>max</code>. Session (and subagent) selectors also accept <code>omit</code>, which is not a catalog/binding capability: the runtime keeps agent bookkeeping at <code>off</code> and uses the low-level provider stream so no thinking override is synthesized (ADR 0194 / ADR 0295).</li><li>The bundled models.dev release snapshot is authoritative for published reasoning support, thinking-level mapping, limits, input/output modalities, pricing, and other model metadata. pi-ai remains responsible for request serialization and adapter compatibility.</li><li>Provider configuration cannot override published reasoning, thinking, limits, or other model metadata. The explicit attachment capability fields are the exception: <code>supportsImages</code> and <code>supportsDocuments</code> are effective binding overrides for the endpoint.</li><li>Unsupported requested levels use the selected models.dev model&#39;s nearest-supported-level rule: scan upward first, then downward. A non-reasoning provider always resolves to <code>off</code>.</li><li>Vision support starts from the same published model record. An absent or <code>null</code> <code>supportsImages</code> follows its image input; <code>true</code> or <code>false</code> explicitly enables or disables image transport for the configured binding. Unknown or custom ids remain conservative text/path models unless their binding explicitly enables image input.</li><li>The effective level is passed to the pi <code>Agent</code>; provider-specific request serialization remains pi-ai&#39;s responsibility.</li><li>Pi <code>thinking</code> blocks become <code>UiMessage.thinking</code> and <code>message_update.deltaThinking</code>. They never append to <code>content</code> or <code>deltaText</code>.</li><li>Append-only <code>message_update</code> events set <code>stream: \\&quot;delta\\&quot;</code> and carry only the new chunk. The runtime keeps the full <code>currentAssistant</code> in memory, coalesces deltas every 16ms, and flushes before tool/terminal/abort/error/retry boundaries. <code>message_end</code> is the authoritative snapshot (D412).</li><li>Restored assistant history reconstructs separate text and thinking blocks before the next turn.</li><li>Restored history also reconstructs tool call/result pairs from persisted tool rows (<code>toolCallId</code>/<code>toolArgs</code>/<code>toolResult</code>), so a recreated runtime keeps its full working context — file contents read, command output — instead of collapsing to bare chat text (D127). An interrupted tool row restores as an errored result; a tool row whose assistant row was lost gets a synthesized call-only assistant carrier so call/result pairs stay well-formed for every provider API.</li><li>Vision runtimes hydrate persisted image refs only from the session-bound attachment, scratch, and project roots. Images within the 10 MB inline safety bound become transient pi-ai image blocks; oversized or unavailable images become safe <code>@path</code> fallbacks. Oversized history hydration copies files without first loading their contents into memory. Base64 is never restored into durable UI messages or transcript records.</li><li>Failed assistant messages remain durable diagnostic transcript entries but are never restored into pi model context on a later turn.</li><li>A tool-call id is unique in every request. The transcript is an append-only snapshot stream that tolerates a retried append, so the same call can reach the assembled context twice — under one row id, which the host&#39;s keep-last read already collapses, or under two, which it cannot. The last view before the wire therefore keeps the first occurrence of each <code>toolCall</code> id and drops a later call or a later result for it, so the pair the provider validates stays well-formed; a request with no duplicates is returned unchanged. A drop is reported once on the <code>agent</code> log channel with the session and the ids (D608). Anthropic-family endpoints, including DeepSeek&#39;s, reject the whole turn with <code>tool_use ids must be unique</code> (issue #718), which leaves the session unable to continue.</li><li>The guard compares the wire-visible call id — the part before the <code>|</code> that separates the Responses item id — and drops an assistant message whole once every tool call in it was already claimed, because keeping its residual text would leave a message sitting between a call and the result answering it. A message that replays a claimed call while carrying a new one keeps its new call in place; no writer produces that partial replay, and the guard does not reorder messages to close the gap it leaves (D620).</li><li>The agent loop owns its context array. <code>prepareNextTurn</code> hands pi a copy of the live state — for a delegate turn boundary too — exactly as pi&#39;s own <code>createContextSnapshot()</code> does for <code>prompt()</code> and <code>continue()</code>: pi&#39;s loop appends each streamed assistant message and each tool result to the array it was given, while its <code>message_end</code> listener appends the same message to <code>state.messages</code>, so handing over the live array stored every message of a run&#39;s later iterations twice. The next turn&#39;s first request was built from that array, and the duplicate of an assistant message carrying text plus a tool call survived as a text-only clone between the call and its result — pi-ai then closed the still-pending call with a synthesized output next to the real one, and the endpoint rejected the turn with <code>Duplicate tool output for call_id</code>. In a delegate the same doubling doubled the estimate at the following boundary and left the trailing row a fallback carries onto a model it cannot resume from (D620).</li><li>Restored checkpoints clear provider usage from retained assistant messages for budgeting. That usage measured the pre-compacted request and must not make the summary + tail appear as large as the discarded context.</li><li>Runtime recreation and model changes restore the newest valid checkpoint. Truncation keeps it only when its boundary remains in the live transcript; a fork copies/remaps it only when the child includes that boundary.</li><li>A forked session receives a new session id and no shared runtime. Its first prompt creates a fresh pi runtime and restores context only from the child transcript, including the remapped tool call/result pairs.</li><li>Message-scoped assistant Fork/Edit follows the same rule: the child transcript may stop at or replace the selected assistant response, but its next prompt cannot reuse the source session&#39;s runtime/provider cache because the session id and remapped transcript identities are independent (D134).</li></ul><h2 id="_5f-subagent-delegation-d201-adr-0062-adr-0089" tabindex="-1">5f. Subagent delegation (D201, ADR 0062, ADR 0089) <a class="header-anchor" href="#_5f-subagent-delegation-d201-adr-0062-adr-0089" aria-label="Permalink to &quot;5f. Subagent delegation (D201, ADR 0062, ADR 0089)&quot;">​</a></h2><p>The session Agent can hand separable pieces of work to delegates that run in their own context, in the background, and report back on demand.</p><p><strong>Catalog.</strong> Definitions are Markdown documents from two sources: the five builtins shipped inline in <code>agent-runtime</code> (<code>explorer</code>, <code>code-reviewer</code>, <code>test-runner</code>, <code>fixer</code>, <code>ui-designer</code>) and the global user documents under <code>~/.agents/subagents/*.md</code>. There is no project-level subagent directory and <code>.pi/agents</code> is not scanned for capabilities. User documents are filtered by the app-local enabled state before they reach the loader, and the shipped builtins are filtered by that same app-local state inside it (ADR 0270). Electron main loads <code>subagentProviders</code> in the sidecar params, so editing a definition takes effect on the next prompt. The catalog is capped at <code>MAX_SUBAGENT_DEFINITIONS</code> (16); a malformed or unreadable document becomes a launch diagnostic and never fails the launch.</p><p>Frontmatter adds <code>permission: inherit | ask | accept-edits | auto</code> (default <code>inherit</code>), which controls the scope the delegate&#39;s tool calls resolve under instead of the session mode (§5f.1). <code>tools: inherit</code> (alone or with assignable extras) opts a definition into the parent session&#39;s live tool catalog minus a deny list (ADR 0246 / D415); builtins stay on today&#39;s whitelist. <code>idle-timeout</code> and <code>max-duration</code> still parse for compatibility but no longer kill a run (D328). Only builtin and user definitions may declare a permission scope — both express a choice the user already made, whereas a project definition arrives with the repository, so honoring its scope would let cloned code grant itself <code>auto</code>. A project document that declares a non-<code>inherit</code> scope keeps loading with a warning and its delegates run under the session&#39;s effective mode; a user who wants the scope copies the document into their own agents directory. Builtins, including the write-capable <code>fixer</code> and <code>ui-designer</code>, do not override the parent session by default: they follow <code>auto</code> completely (including explicit external paths) while <code>ask</code> and <code>accept-edits</code> retain their normal approval behavior. An explicit builtin or user scope remains an intentional override.</p><p><strong>Tools (ADR 0089).</strong> Delegation is a four-tool lifecycle, built only in Agent mode and only when the catalog is non-empty, and all four belong to the Agent core set rather than the on-demand catalog of §7.1:</p><ul><li><p><code>Task(agent, task, description?, model?, resume?)</code> — validates its arguments (an unknown <code>agent</code>, an empty <code>task</code>, an unresolvable model pin and a definition whose tools are all unavailable each return a tool error explaining the failure rather than throwing), starts the delegate <strong>in the background</strong>, and returns immediately with a <code>delegationId</code>. Starting fails with a tool error when the session already runs <code>MAX_SUBAGENT_CONCURRENCY</code> (10) delegates.</p><p>The <code>Task</code> tool accepts an optional <code>model</code> parameter (<code>&quot;provider/modelId&quot;</code>) that overrides the delegate&#39;s model for that run. Resolution priority: Task.model parameter → definition frontmatter pin → session model. The parent agent sees a model summary in the system prompt listing all models marked <code>availableForSubagents</code> in provider settings. If the delegation catalog is empty, the prompt tells the model to omit <code>model</code> and use the definition pin, or inherit the session model when unpinned; an explicit key that exactly names the current session provider/model is treated as the same inheritance case. Other explicit model keys must be configured and enabled for delegation. Electron sends <code>subagentModelKeys</code> separately from <code>subagentProviders</code>: the latter may include definition-only pins, while only the former authorizes cached overrides and the model summary. Missing keys default to an empty list; successful on-demand resolution is cached separately from launch opt-in and does not rewrite definition pins or runtime reuse matching. Grants expire at each new parent prompt or approved plan/goal execution; late responses from an older turn cannot repopulate the cache. On-demand provider matching uses the same unique id/vendor/name rule as pin resolution. A changed opt-in list retires the idle runtime on the next launch. Pins remain usable by their own definitions when <code>model</code> is omitted or when <code>Task.model</code> repeats that definition&#39;s own pin key, even without an opt-in. The Task definition catalog displays each default model and treats omitting or repeating that key as keeping the default. See <a href="./../../adr/subagent-model-opt-in">ADR subagent-model-opt-in</a>. When a model key is not pre-resolved, the runtime asks Electron main to resolve it on-demand via the <code>provider.resolveSubagentModel</code> RPC. The started <code>Task</code> result details record the effective <code>modelId</code> and resolved <code>thinkingLevel</code> used for that run. The level is resolved after inheritance and target-model capability clamping; <code>omit</code> records that no provider thinking override was sent.</p></li><li><p><code>TaskWait(delegationIds?, mode?, minCompleted?, timeoutSeconds?)</code> — converges on running delegations (defaults to all of them) and returns their reports; <code>mode: &quot;any&quot;</code> with <code>minCompleted</code> converges as soon as the first N settle. Settled delegations return immediately, so re-reading a report by id is cheap. The joined result is bounded to <code>MAX_TASKWAIT_RESULT_CHARS</code> (50k); if the bound omits finished reports, those reports remain undelivered and the runtime sends them on the idle resume (or they can be re-read by id). <code>timeoutSeconds</code> defaults to 600 and is clamped to 900: the wait blocks the turn, so the ceiling is what bounds how long a session can look hung. Expiry is not a failure and does not stop the delegates (D328) — the wait returns a heartbeat (agent, status, elapsed, turns, last tool) plus any finished reports. The runtime keeps the parent turn open and delivers remaining reports when they finish, even if the parent already stopped calling tools. Only <code>TaskStop</code> or user Stop aborts a delegate.</p></li><li><p><code>TaskList()</code> — reports every delegation of the session with status and a running heartbeat.</p></li><li><p><code>TaskStop(delegationIds?)</code> — stops running delegations (defaults to all); waits for each abort to settle, then persists <code>status: &quot;stopped&quot;</code> with <code>completedAt</code> on <code>details.stopped[]</code>. Stopped delegations read as <code>stopped</code>.</p></li></ul><p><strong>Live settlement.</strong> When a delegate settles, the runtime refreshes its original <code>Task</code> transcript row with the terminal delegation summary (<code>status</code>, <code>completedAt</code>, counters, and failure details when present), using the existing full <code>message_end</code> snapshot. This does not depend on the parent calling <code>TaskWait</code> / <code>TaskList</code> / <code>TaskStop</code> or on other delegates finishing. The snapshot retains the Task call&#39;s identity, arguments, tool timing, and token usage; it does not execute the tool again or add usage to the parent turn. The initial Task result is emitted first even if the delegate settles before that result arrives. Electron persists the refreshed row through the normal message outbox so session switching and history reload preserve the outcome. An outbox write acknowledges only the snapshot sent to the host; a newer snapshot replacing the same message ID during that write remains queued. No new event type or storage schema is required.</p><p><strong>Delegate loop.</strong> A <code>SubagentRun</code> is a second pi <code>Agent</code> in the same sidecar process with the definition&#39;s system prompt, its (possibly pinned) provider/model, its declared tools, and the same host connection. A pinned or explicitly selected delegation model uses the exact provider/model binding saved in Settings for its effective thinking capability; models.dev supplies the baseline only. It runs under the same bounded provider retry policy as the parent. A delegate has no turn limit: it ends when it finishes, when the parent calls <code>TaskStop</code>, when the user Stops, or when a terminal parent error aborts it (ADR 0253). A document that still declares <code>maxTurns</code> loads normally and the key is ignored like any other unrecognized frontmatter key. <code>maxTokens</code> is an optional per-definition output cap (maximum 200000); omitted, <code>none</code>, or <code>0</code> follows the model&#39;s published limit. It overrides <code>maxTokens</code> on the model built for that delegate, so the adapter&#39;s derived <code>max_tokens</code> / <code>max_completion_tokens</code> / <code>max_output_tokens</code> carry it, and it binds that delegate&#39;s own responses only — the session&#39;s requests keep the model binding. A value past the ceiling is a typo and is clamped rather than forwarded to the provider. The built-in <code>explorer</code> declares <code>Read</code>, <code>Glob</code>, <code>Grep</code>, and <code>Bash</code>, while <code>code-reviewer</code> remains read-only; <code>fixer</code> and <code>ui-designer</code> write inside the workspace, and <code>ui-designer</code> adds <code>BrowserPreview</code> so it can open and inspect its rendered result before reporting. <code>BrowserPreview</code> only opens a live-reloading workspace HTML page; responsive, keyboard-focus, and reduced-motion checks require project-provided browser tests or other tooling. Its statuses are <code>completed</code>, <code>failed</code>, <code>aborted</code>, <code>timed_out</code> and the registry-only <code>stopped</code>; the terminal ones surface through <code>TaskWait</code>, whose text is the report (bounded to <code>MAX_SUBAGENT_REPORT_CHARS</code>, 12k) and whose details carry <code>delegationId</code>, <code>agent</code>, <code>modelId</code>, <code>thinkingLevel</code>, <code>status</code>, <code>startedAt</code>, <code>completedAt</code> when settled, <code>turns</code>, <code>toolCalls</code> and, on failure or timeout, <code>error</code>. The same effective model and thinking fields are included in the immediate <code>Task</code> result and in lifecycle snapshots so live and restored delegation views do not re-derive them from definitions or parent settings. <code>startedAt</code> and <code>completedAt</code> are runtime timestamps in milliseconds and are the source of truth for renderer delegation duration; the immediate <code>Task</code> tool-call duration only covers starting the background work.</p><p><strong>Delegate lifetime (D328).</strong> The runtime does not idle-timeout or duration-timeout a delegate. <code>idle-timeout</code> / <code>max-duration</code> frontmatter still parses so old documents load, but those values are not armed. A delegate runs until it finishes, fails, is <code>TaskStop</code>&#39;d, or the user Stops / the runtime is disposed. The parent agent judges whether to cancel via <code>TaskStop</code>; a one-line heartbeat (who, status, elapsed, turns, last tool) is what it has to go on while the delegate is running.</p><p>When the parent stops calling tools while delegates are still running, the runtime swallows that <code>agent_end</code>, keeps the durable turn open, waits for the delegates, and prompts the parent with their reports. Ending the parent loop does not abort them.</p><p>Fatal provider/stream errors (including exhausted HTTP 429) and parent aborts retain their existing <code>failed</code> and <code>aborted</code> outcomes. A terminal parent error also aborts leftover delegates, skips the resume prompt, and returns the session to idle so Continue is not <code>AGENT_BUSY</code> (D352).</p><p><strong>Resumable delegations (ADR 0279).</strong> <code>Task</code> accepts an optional <code>resume</code> parameter carrying the <code>delegationId</code> of a settled delegation in the same conversation. The resumed delegate is a new <code>SubagentRun</code> seeded with the chain&#39;s prior messages — the original <code>task</code> brief plus every row the chain produced — and then prompted with the new <code>task</code>, so a delegate that already read or changed a file continues from that context instead of starting cold. Seeding is transcript-backed: the chain&#39;s rows are exactly those carrying <code>parentToolCallId</code> for one of the chain&#39;s <code>Task</code> calls, and they are converted into provider messages with the delegate&#39;s own binding (a pinned delegation model is not the session model). Nothing is kept warm in memory and no new event type, storage schema, or tool parameter is introduced.</p><p>A chain is the sequence of <code>Task</code> calls that share one delegate session: the first call, plus every later call that passed the earlier <code>delegationId</code> as <code>resume</code>. The runtime rebuilds the chain index from the persisted transcript at launch — each <code>Task</code> row carries its own <code>delegationId</code> and settled status in <code>toolResult.details</code> and the resumed id in <code>toolArgs.resume</code>, with the agent name normalized on rebuild — so resumability survives a sidecar restart. Chain identity (<code>delegateSessionId</code>) stays internal; the parent only ever passes a <code>delegationId</code>, and the reverse map resolves it.</p><p>Only <code>completed</code> and <code>failed</code> chains are resumable; <code>stopped</code> and <code>aborted</code> runs are terminal and revive only by starting a new delegation, and a run the app closed while it still worked rebuilds as <code>interrupted</code>, which is not resumable either. A chain whose read-only tool output exceeds <code>MAX_RESUMABLE_READ_LINES</code> (50000) leaves the reusable list without an in-chain trim, so a resume never silently drops history. The registry keeps at most <code>MAX_RESUMABLE_CHAINS_PER_AGENT</code> (2) reusable chains per definition name and evicts least-recently-active ones whenever a delegation settles; a chain that is still working is never evicted, so the bound counts reusable chains and a live chain may sit above it until it settles.</p><p>Resume is strictly same-session and never queues: resuming a running delegation is a tool error telling the parent to converge with <code>TaskWait</code> first, and a chain has at most one live record at a time. <code>model</code> and <code>resume</code> together are rejected, and a resumed run keeps the chain&#39;s recorded binding: the <code>providerId/modelId</code> key it resolved is preferred and reauthorized on demand when its turn grant has expired. A chain rebuilt from the transcript is matched by model id only among the current definition pin/fallbacks, the session binding, and currently authorized overrides. Other definitions&#39; private pins are excluded. A known key never falls through to another account just because its model id matches. When nothing authorized resolves it the run continues on the definition&#39;s current binding and records the previous model id as <code>modelChangedFrom</code> in its lifecycle details. Changing models on purpose means starting a new delegation. An unknown id, an id belonging to another definition, a non-resumable status, an over-budget chain, and a chain whose rows are gone each return a tool error naming the reason and, for an unknown id, the reusable ids when there are any.</p><p>The parent discovers reusable chains through the system prompt, which lists each chain&#39;s latest <code>delegationId</code>, its objective, and up to <code>MAX_RESUMABLE_LISTED_FILES</code> (8) of the files it read (with a <code>(+N more)</code> suffix past that). The list is recomposed when a delegation settles, around the existing prompt sections. A resumed run is an ordinary delegation for <code>MAX_SUBAGENT_CONCURRENCY</code>, <code>TaskWait</code>, <code>TaskList</code>, <code>TaskStop</code>, and lifecycle snapshots. The immediate <code>Task</code> result and the lifecycle details add <code>resumedFrom</code> for audit; the transcript renders a chain as one continuous multi-turn conversation under its latest <code>Task</code> card, with no separate &quot;resumed&quot; marker.</p><p><strong>Model pins.</strong> <code>model: &lt;provider&gt;/&lt;model&gt;</code> in the frontmatter is resolved once per launch in Electron main, where credentials and the models.dev snapshot live, against provider id, vendor key or display name, and capped at <code>MAX_SUBAGENT_PROVIDERS</code> (8) distinct providers. An unresolvable pin is omitted from the binding map on purpose; the runtime turns the missing entry into a tool error naming the pin, and never falls back to the session model. A definition&#39;s <code>thinkingLevel</code> is clamped against the resolved model with the same nearest-supported rule as §5c, except that the special <code>omit</code> value deliberately sends no thinking override and leaves the provider adapter&#39;s own default in control. <code>agents.create</code> and <code>agents.update</code> accept the pin only in that <code>&lt;provider&gt;/&lt;model&gt;</code> shape; a value without a provider half is rejected with <code>SUBAGENT_INVALID</code> rather than written, because the runtime could never resolve it. Only the slash is structural — the provider half is matched by a normalized alias, so a display name containing spaces is valid.</p><p><strong>Ordered model fallback.</strong> A definition may declare <code>fallbackModels</code> as an inline list (<code>fallbackModels: [provider/model, other/model]</code>) or a block list. The managed host <code>agents.create</code> / <code>agents.update</code> inputs and records expose <code>fallbackModels?: string[]</code>; omission preserves a list on update and <code>[]</code> clears it. Existing <code>model</code> pins and Task override priority remain unchanged. A missing primary pin still fails before launch. Alternatives are resolved in Electron with the definition pins and count toward the existing eight-provider ceiling. They authorize only that definition, including when Task overrides its primary, and do not enter the independent <code>Task.model</code> opt-in catalog.</p><p>After a provider failure exhausts that model&#39;s retries, or is non-retryable, the same child Agent advances through these alternatives in order. Actual provider-id/model-id duplicates are skipped; an unresolved alternative is reported and skipped. The runtime removes only the terminal failed assistant from model context, preserving the original user task and completed tools, then continues with the next provider&#39;s adapter, credentials, request headers, output cap, and re-clamped original thinking selection. <code>omit</code> stays omitted. The completed tool history is never replayed by the fallback controller. Host/tool failures, unexpected thrown errors, no-report outcomes, and Stop do not trigger fallback. The run&#39;s original owner and abort signal govern every attempt, and usage/counters include all attempts. No configured alternatives means the existing single-model behavior.</p><p>Failure diagnostics remain in the child transcript, bounded parent report, and additive <code>modelFailures</code> lifecycle detail. Lifecycle model/thinking fields track the effective alternative, including after settlement and reload. If all alternatives fail, the result remains <code>failed</code> with the final provider error. See <a href="./../../adr/subagent-model-fallback">ADR subagent-model-fallback</a>.</p><p><strong>Context budget and compaction (ADR 0299).</strong> A delegate has the same window protection the session has, derived the same way. The budget comes from the model the run actually resolved — a <code>Task.model</code> override, a definition pin, or the inherited session model — through the shared derivation of §5.1, so <code>hardLimit</code> is that window minus the same request headroom and a per-definition <code>maxTokens</code> cap participates as the output budget. At a delegate turn boundary the run re-estimates its own context and, at or above <code>hardLimit</code>, compacts synchronously before the next provider request, with the retention mode chosen from the same lifecycle rule as the session: a boundary that still has pending tool results retains as an active turn, a completed one as a completed turn. There is no pre-computation and no second threshold.</p><p>When a summary cannot be generated, or the compacted context still exceeds the budget, the run degrades: it keeps the original task brief plus the most recent message(s), discards the rest of its history, continues, and records that it was degraded, so the report and the lifecycle details say the delegate lost history rather than presenting a complete answer. When even that does not fit, the run fails with <code>SUBAGENT_CONTEXT_OVERFLOW</code> (not retriable) naming what the parent can change — narrow the task, delegate to a model with a larger window, read less at once — instead of forwarding the provider&#39;s overflow text.</p><p>An ordered alternative (<strong>Ordered model fallback</strong>, above) is re-evaluated against its own window before it is attempted: an alternative whose budget cannot hold the carried context is skipped and recorded in <code>modelFailures</code> with that reason rather than retried into the same failure. A resumed chain is seeded within the budget — <code>seedDelegateMessages</code> preserves the original task brief and the recent turns and drops the oldest tool results first — so a resume starts below <code>hardLimit</code> instead of overflowing on its first request.</p><p>Delegate compaction affects the delegate&#39;s model context only. It rewrites no persisted transcript row, writes no host-core checkpoint, and adds no compaction row, warning toast, or context-inspector line; the delegate keeps its complete visible rows and the parent still sees only reports. Compaction is entirely automatic: <code>new_context</code> remains denied to delegates, because executing it would set the parent runtime&#39;s pending-compaction flag.</p><p><strong>Events and context.</strong> Every event a delegate emits carries <code>parentToolCallId</code> and <code>agentName</code> on its envelope, and Electron main copies both onto the persisted row. When the runtime rebuilds model context it skips every row with <code>parentToolCallId</code>: the parent only ever saw reports through <code>TaskWait</code> or the runtime&#39;s completion prompt (D328), and replaying delegate rows would both contradict that and reintroduce the context cost delegation exists to avoid.</p><p><strong>Turn ownership.</strong> A delegate&#39;s lifecycle never reaches Electron main&#39;s turn handling. The parent may keep working or talk to the user after <code>Task</code>. If it stops calling tools while delegates still run, the runtime keeps the durable turn open and delivers the reports when they finish. User Stop, <code>TaskStop</code>, runtime dispose, or a parent fatal error (D352) abort a still-running delegate.</p><h3 id="_5f-1-delegate-permission-scope-adr-0089" tabindex="-1">5f.1 Delegate permission scope (ADR 0089) <a class="header-anchor" href="#_5f-1-delegate-permission-scope-adr-0089" aria-label="Permalink to &quot;5f.1 Delegate permission scope (ADR 0089)&quot;">​</a></h3><p>A delegate&#39;s tool calls flow through the same host <code>tools.execute</code> path as the parent&#39;s. When the definition declares a non-<code>inherit</code> <code>permission</code> <strong>and its source is <code>builtin</code> or <code>user</code></strong>, the sidecar attaches the scope to the delegate&#39;s tool RPCs and host-core resolves the call under that mode instead of the session&#39;s effective permission mode. With no declared scope — the default, and the policy used by the builtins — no override is attached, so the delegate inherits the parent&#39;s effective permission mode, including <code>auto</code> for explicit external paths. A <code>project</code> definition&#39;s declared scope is dropped at parse time with a warning, so opening an untrusted repository cannot escalate its own delegates past the session mode. Two gates stay above any explicit scope, exactly as they stay above the session mode: the contract modes&#39; hard deny (delegation only exists in Agent mode) and the external-path gate. An explicit <code>accept-edits</code> still means &quot;Write/Edit inside the workspace resolve without a prompt; external paths and other tools keep their normal approval behavior&quot;; an explicit scope remains an intentional override.</p><p>The surrounding contracts live in <code>03-tools-and-permissions.md</code> §10.2 (what a delegate may call), <code>04-data-storage.md</code> §4.7a (persisted attribution), <code>04-ux/03-permission-ux.md</code> §6a (more than one pending request) and <code>04-ux/08-component-spec.md</code> §9.9 (how a delegation reads).</p><h3 id="_5f-2-no-in-process-sibling-or-parent-to-parent-task-channel-d326-adr-0165" tabindex="-1">5f.2 No in-process sibling or parent-to-parent Task channel (D326, ADR 0165) <a class="header-anchor" href="#_5f-2-no-in-process-sibling-or-parent-to-parent-task-channel-d326-adr-0165" aria-label="Permalink to &quot;5f.2 No in-process sibling or parent-to-parent Task channel (D326, ADR 0165)&quot;">​</a></h3><p>Concurrent <code>Task</code> delegates do not message each other, and the parent Task runtime does not address other conversations. The in-process <code>Peer</code> mailbox (ADR 0138 / ADR 0140) and the host-core A2A broker (ADR 0147 / ADR 0162 / ADR 0164) are withdrawn.</p><p>Coordination stays on the existing delegation contract: the parent writes independent briefs, starts <code>Task</code>s, and collects self-contained reports through <code>TaskWait</code> / <code>TaskList</code> / <code>TaskStop</code>. A later round of work, if needed, is a new <code>Task</code> whose brief includes earlier reports. <code>A2A</code> and <code>Peer</code> are not assignable tools; a definition that names either is treated as an unknown tool name and dropped with a parse warning.</p><h3 id="_5f-3-plugin-mediated-session-collaboration-d409-adr-0239" tabindex="-1">5f.3 Plugin-mediated session collaboration (D409, ADR 0239) <a class="header-anchor" href="#_5f-3-plugin-mediated-session-collaboration-d409-adr-0239" aria-label="Permalink to &quot;5f.3 Plugin-mediated session collaboration (D409, ADR 0239)&quot;">​</a></h3><p>The official <code>pi.session-orchestrator</code> plugin is the reviewed exception for durable cross-session communication. Its <code>desktop.control</code> calls are composed outside the <code>Task</code> runtime and are admitted only from the plugin&#39;s active Agent tool invocation. Host-core owns the source/target Session IDs, durable delivery ledger, permission ceiling, actual target turn, completion callback, cancellation, restart fence, and transcript provenance. Existing sessions keep their own project, model, context, and permission configuration; a newly spawned worker inherits the initiating session&#39;s project and permission ceiling.</p><p>Session messages are framed as agent-provided task data and never become new human authorization. A completion callback is durable and at-most-once, and does not automatically trigger another callback. This path does not restore the withdrawn <code>A2A</code> or <code>Peer</code> tools and does not change <code>Task</code>, <code>TaskWait</code>, <code>TaskList</code>, or <code>TaskStop</code> semantics.</p><h2 id="_6-providers-models" tabindex="-1">6. Providers &amp; models <a class="header-anchor" href="#_6-providers-models" aria-label="Permalink to &quot;6. Providers &amp; models&quot;">​</a></h2><blockquote><p>Full policy: <code>11-provider-model-system.md</code>, <code>12-provider-config-schema.md</code>, <code>13-model-catalog-and-selection.md</code>.</p></blockquote><p>Coverage strategy:</p><ol><li><strong>Native providers</strong> exposed by pi-ai (OpenAI, Anthropic, Google, and others available at pin version)</li><li><strong>OpenAI-compatible</strong> first-class path for gateways and long-tail vendors</li><li><strong>Custom providers</strong> with protocol profiles</li><li><strong>Refreshable model catalog</strong> + <strong>free-form model IDs</strong> (no closed allowlist)</li></ol><p>MVP UI always includes at least:</p><ul><li>OpenAI</li><li>Anthropic</li><li>Google Gemini</li><li>OpenAI-Compatible (generic)</li><li>Custom provider entry</li></ul><p>Runtime responsibilities:</p><ul><li>resolve <code>(providerId, modelId)</code></li><li>resolve and serialize the complete models.dev record, or label an absent ID with the unknown generic fallback</li><li>resolve model reasoning capability and effective thinking level from the models.dev record</li><li>fetch secrets via host (never cache raw secrets in logs)</li><li>translate vendor failures into provider AppError codes</li><li>stream tokens/events to orchestrator</li><li>support abort/cancel mid-stream</li></ul><p>Local models are supported through OpenAI-compatible endpoints (Ollama, LM Studio, vLLM, etc.).</p><h3 id="_6-1-one-shot-composer-enhancement" tabindex="-1">6.1 One-shot Composer enhancement <a class="header-anchor" href="#_6-1-one-shot-composer-enhancement" aria-label="Permalink to &quot;6.1 One-shot Composer enhancement&quot;">​</a></h3><p>Composer enhancement uses the same resolved provider binding and retry classification as an agent request, but creates a separate completion context with exactly one user message and the static enhancement system prompt. It does not instantiate a session agent, include transcript history, expose tools, or persist a turn. The renderer receives only the trimmed text result; API keys and vendor refresh credentials remain in Electron main. OpenCode Go one-shots reuse the conversation id as <code>x-opencode-session</code> when a session is present; otherwise the runtime synthesizes a per-call id so the gateway accepts the request.</p><h3 id="_6-2-opencode-session-routing-headers" tabindex="-1">6.2 OpenCode session routing headers <a class="header-anchor" href="#_6-2-opencode-session-routing-headers" aria-label="Permalink to &quot;6.2 OpenCode session routing headers&quot;">​</a></h3><p>Chat, subagent, context-compaction summary, prompt-enhancement, and plugin one-shot completions whose provider is <code>apiStyle: opencode_go</code>, whose <code>vendorKey</code> is <code>opencode</code> or <code>opencode-go</code>, whose pi-ai provider id is one of those values, or whose base URL host is <code>opencode.ai</code> send:</p><ul><li><code>x-opencode-session</code>: the durable conversation id, or a per-call UUID when the caller has no session</li><li><code>x-opencode-client: pi-desktop</code></li><li><code>User-Agent: pi-desktop/&lt;APP_VERSION&gt;</code></li></ul><p>Caller-supplied headers override the client and User-Agent defaults. An empty session header is restored from the conversation id so OpenCode Go cannot return <code>MissingSessionID</code>. A provider-row <code>headers</code> map is applied after this merge (headers plus a fetch wrapper) so custom values win over the OpenCode default and over adapter last-writes. Reserved keys cannot smash <code>x-opencode-session</code>. This is an agent-runtime concern, matching the official Pi coding-agent attribution layer; pi-ai&#39;s <code>sessionId</code> stream option does not emit <code>x-opencode-session</code>.</p><p>The context-compaction summary is a provider request of the same kind, but the harness assembles its own stream options and never passes the session&#39;s stream function, so agent-runtime applies the header merge to the model collection it hands to compaction. That request carries the session&#39;s conversation id rather than the per-call id the harness would otherwise mint, so a summary reaches the same gateway backend as the conversation it summarizes.</p><h2 id="_7-system-prompt-composition" tabindex="-1">7. System prompt composition <a class="header-anchor" href="#_7-system-prompt-composition" aria-label="Permalink to &quot;7. System prompt composition&quot;">​</a></h2><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[base product prompt in English]</span></span>
<span class="line"><span>+ [operating-state prompt: agent/plan/goal]</span></span>
<span class="line"><span>+ [workspace info]</span></span>
<span class="line"><span>+ [tool instructions]</span></span>
<span class="line"><span>+ [project instruction chain, when present]</span></span>
<span class="line"><span>+ [optional user custom instructions]</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><h3 id="_7-0-1-user-custom-system-prompt-files-issue-542" tabindex="-1">7.0.1 User custom system prompt files (issue #542) <a class="header-anchor" href="#_7-0-1-user-custom-system-prompt-files-issue-542" aria-label="Permalink to &quot;7.0.1 User custom system prompt files (issue #542)&quot;">​</a></h3><p>The <code>[optional user custom instructions]</code> layer is the pi-compatible file pair <code>SYSTEM.md</code> / <code>APPEND_SYSTEM.md</code>, discovered per session launch from <code>&lt;workspace&gt;/.pi/</code> (project) and <code>~/.pi/agent/</code> (global), each kind picking a single winner with project over global, exactly like pi CLI. A change to the resolved content retires the runtime through the reuse match, so the next prompt recomposes; the files are not re-read per tool call like the project instruction chain. Native-pi sessions keep resolving them through the upstream <code>DefaultResourceLoader</code> as before.</p><p>Two deliberate deviations from pi CLI&#39;s semantics:</p><ul><li><code>SYSTEM.md</code> replaces only the base product persona line, not the whole prompt: the operational rules below (collaboration, search, edit contract, scratch, delegation, skills) are desktop mechanics a persona file must not remove.</li><li><code>APPEND_SYSTEM.md</code> is appended after the composed base prompt and before the project instruction chain, matching pi&#39;s ordering, so the user&#39;s own <code>AGENTS.md</code> keeps the last word.</li></ul><p>Both files are capped at 64 KiB, and a whitespace-only file counts as absent. Native <code>SYSTEM.md</code> / <code>APPEND_SYSTEM.md</code> resolution in a native-pi session is unaffected: it stays with the upstream loader.</p><p>The base prompt states collaboration rules explicitly, because omitting them is what produced silent sessions: &quot;prefer concise, actionable answers&quot; was the only relevant line, and a reasoning model executed it as saying nothing at all. Required behaviours, each one an observed failure inverted:</p><ul><li>answer in the language the user writes in</li><li>one sentence before each tool batch in the same assistant message as the tool calls, and no silence longer than one tool batch or 60 seconds of work</li><li>anything the user asked is answered in visible text; reasoning is not shown to them and does not count as an answer</li><li>the final message is self-contained</li><li>work is carried through end to end rather than stopping at analysis</li><li>tool calls go through the native tool-call interface; a call written as prose (notably an OpenAI-style <code>multi_tool_use.parallel</code> wrapper) does not run, and the runtime logs it when a model emits one</li></ul><p>It also states a search preference that matches the host-side budgets in <a href="./16-tool-result-limits">16-tool-result-limits</a>: scope <code>Read</code>, <code>Grep</code>, and <code>Glob</code> with their own parameters instead of hand-rolling <code>cat</code>/<code>sed</code>/<code>grep</code>/ <code>find</code>. <code>Read</code> accepts only an existing regular text file. When a file name is uncertain or a directory must be listed, an Agent activates <code>Glob</code> for the current prompt through <code>ToolSearch</code> instead of guessing a name or reading the directory. <code>Glob.path</code> is a directory, while <code>Grep.path</code> may be one file or a directory tree. Calls use <code>Read.offset/limit</code>, <code>Glob.path/limit</code>, and <code>Grep.path/include/outputMode/headLimit</code>; <code>filesWithMatches</code> or <code>count</code> avoids unneeded content. Workspace-relative paths remain the portable default, with a bounded command in the active shell only when native tools are insufficient. Grep uses a system <code>rg</code> when one is installed and an in-process searcher otherwise; the agent calls Grep rather than shelling out to <code>rg</code>. Bash must still not assume <code>rg</code> is present. The agent must not repeat a search whose answer is already in context.</p><p>The edit-discipline block carries the line-anchored <code>Edit</code> contract of <a href="./18-line-anchored-edit-contract">18-line-anchored-edit-contract</a>: the op table, the <code>+</code>-only body rule, &quot;ranges name changed lines only&quot;, &quot;re-ground on the tag returned by every successful write&quot;, and the worked anti-patterns. The sidecar&#39;s <code>Edit</code> schema is <code>{ path, tag, ops }</code>; <code>old_string</code> and <code>new_string</code> no longer exist, and the sidecar&#39;s tool description must stay byte-identical in substance to host-core&#39;s <code>builtin_tool_defs()</code> entry, because a model taught one grammar and validated against another fails every call.</p><h3 id="_7-1-active-tool-context-and-on-demand-loading-d185-adr-0048" tabindex="-1">7.1 Active tool context and on-demand loading (D185, ADR 0048) <a class="header-anchor" href="#_7-1-active-tool-context-and-on-demand-loading-d185-adr-0048" aria-label="Permalink to &quot;7.1 Active tool context and on-demand loading (D185, ADR 0048)&quot;">​</a></h3><p>The sidecar builds one complete tool registry, but it does not serialize every registered schema into every provider request. Each new user prompt starts with the mode&#39;s core set plus any deferred tools that can be restored from successful activation evidence still present in the effective session context:</p><ul><li>Agent: <code>Read</code>, <code>Bash</code>, <code>Edit</code>, and <code>Write</code> (matching pi&#39;s coding-agent core)</li><li>Agent: <code>Skill</code> whenever the skill catalog is non-empty (D404, ADR 0230) — the <code># Skills</code> section and a user-typed <code>/skill-id</code> both ask the model to call it, and a tool that is missing from the schema cannot be called at all</li><li>Agent: <code>Task</code>, <code>TaskWait</code>, <code>TaskList</code>, and <code>TaskStop</code> as well, whenever the subagent catalog is non-empty (§5f) — a capability the model has to go looking for is one it will not use, and the delegation lifecycle is worth the extra schemas per request</li><li>Plan: <code>Read</code>, <code>Glob</code>, <code>Grep</code>, <code>BrowserPreview</code>, and <code>Bash</code></li><li>both modes: <code>ToolSearch</code> when at least one deferred capability exists</li></ul><p>In Agent mode, <code>Glob</code> and <code>Grep</code> join <code>BrowserPreview</code>, plugin tools, and plugin-development helpers in the deferred set. Both contract modes keep their read/inspection core available, while the kind&#39;s submit tool (<code>SubmitPlan</code> or <code>SubmitGoal</code>) is exposed only during the planning state, and only for the active kind. Deferred tools are registered but their names and compact one-line descriptions appear in an <code># On-demand tools</code> catalog; parameter schemas do not. The catalog is bounded so a plugin with many tools cannot recreate the original prompt bloat. The model calls <code>ToolSearch</code> with an exact name or a short capability query. The sidecar activates up to four matches, returns their names through pi-agent-core&#39;s <code>addedToolNames</code>, and rebuilds the next-turn context with those schemas. Providers with native deferred-tool search receive the definitions at that load point; other providers receive the active definitions normally.</p><p>At the start of each new user prompt, the sidecar clears the in-memory deferred activation set and rebuilds it from the effective context. Successful <code>ToolSearch</code> results contribute their <code>addedToolNames</code>; successful results from deferred tools contribute that tool&#39;s name. Only names still present in the current mode&#39;s deferred catalog are restored. Failed rows, interrupted or missing-result placeholders, and assistant/user prose never activate a tool. The tool registry, host permission path, tool timeout, and workspace containment rules remain unchanged. <code>ToolSearch</code> is local to the sidecar and does not cross the host RPC boundary. Its activation marker is retained in the persisted tool result, so a runtime restart or a new prompt can reuse an eligible capability while that evidence remains in the effective context; a fresh search is still required after the evidence is compacted away or otherwise absent.</p><p>For user-visible HTML deliverables, the default system prompt asks the agent to activate <code>BrowserPreview</code> once after creating the page or making its first meaningful visual edit, using a workspace-relative path. The agent reuses the live-reloading preview while iterating instead of issuing repeated preview calls. Generated, test-only, and non-visual HTML files are excluded. When the tool is deferred, <code>ToolSearch</code> must activate it before the preview call.</p><h3 id="_7-2-plan-prompt-requirements" tabindex="-1">7.2 Plan prompt requirements <a class="header-anchor" href="#_7-2-plan-prompt-requirements" aria-label="Permalink to &quot;7.2 Plan prompt requirements&quot;">​</a></h3><p>The Plan prompt tells the same Agent to understand the request, inspect the relevant repository/specification/test context, identify impacted files and risks, include focused validation and migration/recovery implications, surface open questions. When any initial or revised plan is ready, it must call <code>SubmitPlan</code> immediately exactly once in the current turn with one complete Markdown snapshot. An accepted new Plan prompt has no prior pending approval; earlier submissions in the transcript are historical immutable checkpoints. After reject, expiry, or interruption, the Agent may revise in the new turn and must follow the same one-SubmitPlan rule. It must not claim that changes were made. The host writes the immutable <code>.pi/plan/*.md</code> artifact; the Agent does not write or edit it itself and does not receive a request-changes flow.</p><p>The prompt may describe Bash as permission-gated and potentially mutating. It must not describe Plan as a strict read-only security boundary.</p><h3 id="_7-2a-goal-prompt-requirements" tabindex="-1">7.2a Goal prompt requirements <a class="header-anchor" href="#_7-2a-goal-prompt-requirements" aria-label="Permalink to &quot;7.2a Goal prompt requirements&quot;">​</a></h3><p>The Goal prompt tells the same Agent to negotiate a goal contract before any autonomous work. It asks for what to achieve rather than how: the outcome, the acceptance criteria, and the boundaries. It must not enumerate implementation steps, because the Agent decides those itself after approval. Every acceptance criterion must be objectively checkable by the Agent after execution — a command that must pass, or an observable behavior. The Agent inspects the workspace and asks about anything ambiguous first, then calls <code>SubmitGoal</code> immediately exactly once in the current turn with one complete Markdown snapshot.</p><p>The one-submit rule, the historical-checkpoint rule, the revise-after-close rule, the no-chat-confirmation rule, and the host-writes-the-artifact rule are the same as Plan&#39;s, with <code>SubmitGoal</code> and <code>.pi/goal/*.md</code> in place of their Plan equivalents. The prompt additionally states that once approved, the contract is the standard the Agent works against: it pursues the goal autonomously, chooses its own approach, and stops only when every acceptance criterion is verified or a boundary blocks it.</p><h3 id="_7-2b-subagent-prompt-composition-d201-adr-0062" tabindex="-1">7.2b Subagent prompt composition (D201, ADR 0062) <a class="header-anchor" href="#_7-2b-subagent-prompt-composition-d201-adr-0062" aria-label="Permalink to &quot;7.2b Subagent prompt composition (D201, ADR 0062)&quot;">​</a></h3><p>A delegate&#39;s system prompt is composed in the sidecar from three parts, in this order: the delegation framing, the definition&#39;s Markdown body, and the tool guidance its resolved tools earn. The body sits ahead of the workspace guidance so a project&#39;s own instructions still have the last word.</p><p>The framing states the shape of the delegate&#39;s situation, which is not inferable from the body: it is one delegated task, the delegate cannot see the user, ask a question, or delegate further, it has exactly the listed tools, and its final message is the only thing the main agent receives. Listed names are the spawn-time resolved set when <code>tools: inherit</code> is on. A read-only definition is additionally told never to report an edit it could not have made; a write-capable one is told to touch only the files the task is about. Mutation framing uses the resolved set, not the raw frontmatter extras.</p><p>Guidance blocks are the same text the session prompt uses, included only when the resolved tools include the matching name: search/read scoping for Read/Grep/Glob, edit discipline for Edit/Write, the command shell contract for Bash, the <code># Skills</code> catalog when <code>Skill</code> is present, and the scratch-directory rule when the session has a scratch directory and the delegate can write. The project instruction chain (§7.3) is appended last, so a delegate follows the same project rules as its session.</p><h3 id="_7-3-project-instruction-chain" tabindex="-1">7.3 Project instruction chain <a class="header-anchor" href="#_7-3-project-instruction-chain" aria-label="Permalink to &quot;7.3 Project instruction chain&quot;">​</a></h3><p>The Electron main process first resolves the global <code>~/.pi/agent/AGENTS.md</code>, then project instruction files inside the session-bound project root when a runtime starts. For each project directory it uses at most one non-empty file in this order: <code>AGENTS.override.md</code>, <code>AGENTS.md</code>, <code>CLAUDE.md</code>, then <code>.claude/CLAUDE.md</code>. Entries are concatenated from project root to the target directory, so the closest file appears last and takes precedence. The initial chain targets the project root. Before a <code>Read</code>, <code>Write</code>, <code>Edit</code>, or <code>BrowserPreview</code> call, the sidecar asks Electron main to resolve the target path and replaces the active instruction section with that path&#39;s complete chain before the tool executes. This keeps rules lazy and prevents sibling-directory rules from persisting after the agent moves to a different file tree.</p><p>The session-bound project root is passed with the runtime launch metadata and registered by Electron main before each prompt or compaction request. The sidecar cannot select a different root. During one prompt, path-resolution claims are cached by project root and target directory, so repeated file tools in the same directory do not perform another IPC request. Claims are discarded at the next prompt, allowing edits and newly created instruction files to take effect without a stale cross-message cache.</p><p>Path-specific resolution is best-effort and has a 2-second deadline. If the resolver or its host RPC is unavailable or exceeds that deadline, the file tool continues with the runtime&#39;s base/root chain rather than waiting for the general host RPC timeout. A failed resolution never leaves a previously resolved sibling-directory chain active.</p><p>All discovery stays within the session project root. Empty, unreadable, and out-of-root files are skipped. The combined UTF-8 content is capped at 32 KiB and source paths are labelled under <code># Project instructions</code>. The sidecar never reads workspace instructions directly. A changed root chain recreates an idle runtime on its next prompt; nested instructions are resolved again when a relevant file tool runs. The resolver&#39;s timeout and fallback are operational safeguards; they do not emit a separate timing log record.</p><p>Settings provides dedicated management for the fixed global path. The Projects view project-list menu provides an <code>AGENTS.md</code> editor for its corresponding registered project root. Its IPC does not accept arbitrary renderer file paths. Saves affect the next prompt without restarting the application.</p><h2 id="_8-concurrency" tabindex="-1">8. Concurrency <a class="header-anchor" href="#_8-concurrency" aria-label="Permalink to &quot;8. Concurrency&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Scope</th><th>MVP policy</th></tr></thead><tbody><tr><td>same session</td><td>single turn serial</td></tr><tr><td>different sessions</td><td>limited parallel</td></tr><tr><td>tools</td><td>sequential by default</td></tr><tr><td><code>Task</code> calls in one assistant message</td><td>parallel; 10 running delegates per session (ADR 0089)</td></tr></tbody></table><p>Tool concurrency is expressed through pi execution modes: every catalog tool is <code>sequential</code> and <code>Task</code> alone is <code>parallel</code>, and pi runs a batch sequentially as soon as it contains one sequential tool. So an all-<code>Task</code> batch is the only batch that fans out, and every other ordering guarantee is unchanged. Delegates issue host calls independently, and host-core&#39;s one-mutation-per-session admission keeps writes from tearing but leaves two same-path mutations unordered, so the sidecar serializes <code>Write</code>/<code>Edit</code> calls that target the same normalized path before they reach the host; calls on different paths never wait on each other. This is what keeps the per-path edit-recovery rules of <code>03-tools-and-permissions.md</code> §4d meaningful under fan-out.</p><p>Selecting another project tab affects only the visible shell workspace. It does not dispose, abort, or re-root a runtime belonging to another session.</p><h2 id="_9-abort-semantics" tabindex="-1">9. Abort semantics <a class="header-anchor" href="#_9-abort-semantics" aria-label="Permalink to &quot;9. Abort semantics&quot;">​</a></h2><ul><li>stop model stream</li><li>attempt cancel interruptible tools</li><li>do not auto-rollback completed writes</li><li>mark turn aborted in UI/storage</li><li>preserve elapsed response duration; when provider final usage is unavailable, estimate visible thinking plus answer output at four Unicode code points per token and persist it as <code>responseOutputTokens</code> so stopped-turn throughput is still available and visibly approximate</li><li>renderer smart-stop transcript undo and structured Composer restoration are reconciliation after abort; they do not change runtime cancellation or roll back completed tool effects</li></ul><h2 id="_10-explicit-non-goals" tabindex="-1">10. Explicit non-goals <a class="header-anchor" href="#_10-explicit-non-goals" aria-label="Permalink to &quot;10. Explicit non-goals&quot;">​</a></h2><ul><li>no DOM knowledge</li><li>no direct FS access bypassing Rust host</li><li>no secret leakage into events/logs</li></ul><h2 id="_11-implementation-status-m5" tabindex="-1">11. Implementation status (M5) <a class="header-anchor" href="#_11-implementation-status-m5" aria-label="Permalink to &quot;11. Implementation status (M5)&quot;">​</a></h2><p>Implemented: streaming turns over the OpenAI-compatible protocol path (universal escape hatch, D024); one active turn per session enforced with <code>AGENT_BUSY</code>; real <code>turnId</code> returned per accepted prompt; provider failures mapped to <code>PROVIDER_UNAUTHORIZED</code> / <code>PROVIDER_RATE_LIMITED</code> / <code>MODEL_NOT_CONFIGURED</code> / <code>STREAM_FAILED</code> / <code>TURN_ABORTED</code> where detectable. The desktop development lifecycle rebuilds <code>packages/agent-runtime/dist</code> before Electron starts so the spawned sidecar always executes the current normalization and error-mapping source.</p><p>Tracked gaps (post-MVP backlog): richer system prompt composition (§7) and provider/model catalog discovery beyond the currently wired paths.</p><h2 id="_12-native-pi-continuation-runtime-adr-0254" tabindex="-1">12. Native Pi continuation runtime (ADR 0254) <a class="header-anchor" href="#_12-native-pi-continuation-runtime-adr-0254" aria-label="Permalink to &quot;12. Native Pi continuation runtime (ADR 0254)&quot;">​</a></h2><p>The sidecar selects runtime by session source. Desktop-owned ids continue to use <code>DesktopAgentRuntime</code>, host turn rows, and the host persistence outbox unchanged. Opaque <code>native-pi:</code> ids use a dedicated coding-agent <code>AgentSession</code> constructed with the original v3 <code>SessionManager</code>, Pi <code>ModelRuntime</code>, <code>SettingsManager</code>, and <code>DefaultResourceLoader</code>. Native context is built by the SDK from the active tree leaf, compaction, model/thinking changes, and context-bearing custom messages; it is never reconstructed from renderer <code>UiMessage</code> rows.</p><p>The first native slice supports text prompt <strong>without model tools</strong>, stop/abort, and explicit refresh. <code>createAgentSession</code> receives <code>noTools: &quot;all&quot;</code>; neither built-in nor extension tools may bypass Desktop permissions. Tool parity awaits an explicit permission bridge. Native extension startup/resource discovery binds with the SDK headless UI and unsupported session-control actions; guards and listeners are installed before startup appends. Binding failure disposes the session and releases ownership. Pi extensions remain trusted local code, not Desktop plugins.</p><p>A native fork (<code>native.session.fork</code>) branches the parent snapshot in memory: the SDK extracts the anchored branch using its native label/compaction re-chaining rules, drops later and sibling entries, and never touches the parent file or its manager. When the anchored branch recorded no model, the child records the parent session&#39;s saved provider/model; when it recorded no thinking-level change, it records the parent&#39;s saved level, while an explicit branch value (including off) wins. The child is published as a complete new file whose staged and published bytes must still match the captured device/inode/size/hash before it is projected or registered; an altered file fails closed without returning a child. A fork is a data-only copy: it executes no model and loads no project resources, so it stays available while the parent is provider-unavailable or project-untrusted, without granting prompt</p><p>ModelRuntime performs its public offline initialization to restore the local catalog and auth snapshot. Native Composer readiness uses native <code>canPrompt</code>, not Desktop provider or secret availability. Owned idle leases remain usable; active turns stay stoppable through retries/compaction, reject overlap without disposal, and publish exactly one terminal event after SDK prompt settlement and final persistence (including <code>agent_settled</code> hooks). An error rejection uses the error terminal path instead.</p><p>The native dispatcher carries the optimistic <code>userMessageId</code>; a <code>user_message_persisted</code> event acknowledges the SDK-assigned durable entry ID after the guarded append. No caller ID is written into native JSONL. The renderer reconciles active, retained, and cached rows by identity, never text. Native abort refreshes the original transcript instead of smart-stop rewrite.</p><p>The saved provider/model and configured Pi auth must resolve exactly; there is no Desktop provider fallback. Missing cwd, required project trust, unsupported format, repair-needing newline, unavailable provider/auth, active lease, or external byte change makes continuation fail closed while detail remains browseable.</p><h3 id="provider-certificate-trust-issue-714" tabindex="-1">Provider certificate trust (issue #714) <a class="header-anchor" href="#provider-certificate-trust-issue-714" aria-label="Permalink to &quot;Provider certificate trust (issue #714)&quot;">​</a></h3><p>The desktop sidecar starts with Node&#39;s <code>--use-system-ca</code>, retaining bundled roots and inherited <code>NODE_EXTRA_CA_CERTS</code>. It uses the OS trust store without turning off chain or hostname validation. Restart after updating local trust or the extra-CA startup environment. Headless pi-host launch behavior and System/Direct/Custom proxy routing are unchanged.</p><p>Explicit certificate verification errors are terminal for both setup and stream recovery in main sessions and built-in delegates. Their structured cause survives adapter message flattening, remains on the final error row, and never triggers a provider transport rebuild. Protocol errors such as <code>EPROTO</code> keep their existing retry behavior. See <a href="./../../adr/provider-system-certificates">certificate trust ADR</a>.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("spec/03-runtime/02-agent-runtime.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _02AgentRuntime = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _02AgentRuntime as default
};
