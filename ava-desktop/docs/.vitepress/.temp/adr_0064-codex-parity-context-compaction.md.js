import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0064: Codex-parity context compaction","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0064-codex-parity-context-compaction.md","filePath":"adr/0064-codex-parity-context-compaction.md","lastUpdated":1788143508000}');
const _sfc_main = { name: "adr/0064-codex-parity-context-compaction.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0064-codex-parity-context-compaction" tabindex="-1">ADR 0064: Codex-parity context compaction <a class="header-anchor" href="#adr-0064-codex-parity-context-compaction" aria-label="Permalink to &quot;ADR 0064: Codex-parity context compaction&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-08-06</li><li>Deciders: PI-Desktop core</li><li>Amends: ADR 0061 / ADR 0030 / D158 / D200; amended by ADR 0136</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>ADR 0061 made compaction imperceptible and cited Codex as the reference for doing so. Reading the Codex sources afterwards showed that four of its clauses are the opposite of what Codex actually does, and that one factual claim in its Context section is wrong:</p><table tabindex="0"><thead><tr><th>Codex</th><th>ADR 0061</th></tr></thead><tbody><tr><td>Compaction is synchronous only; there is no pre-computation anywhere</td><td>Checkpoints are pre-computed in provider-idle windows (clause 4)</td></tr><tr><td>The auto-compact threshold scope defaults to the whole context; <code>BodyAfterPrefix</code> is opt-in</td><td>The background trigger measures the increment since the last checkpoint (clause 2)</td></tr><tr><td>Every compaction emits a <code>ContextCompaction</code> turn item <strong>and</strong> an <code>EventMsg::Warning</code></td><td>A successful compaction notifies nobody and leaves no row (clauses 7, 8)</td></tr><tr><td><code>new_context</code> is a real model-facing tool, gated by <code>Feature::TokenBudget</code></td><td>&quot;Codex also has no model-side compaction tool at all&quot; (line 31); the tool is removed (clause 6)</td></tr><tr><td>After compaction the model context keeps recent <strong>user</strong> messages (≤20k tokens) plus the summary</td><td>The retained tail keeps whole recent turns, assistant and tool messages included</td></tr><tr><td>Two families: LLM-summary compaction and a no-summary rollover into a fresh window</td><td>One family, always summarized</td></tr><tr><td>Two-tier budget reminders (<code>TokenBudgetReminder</code>, then <code>AutoCompactFallbackPrompt</code>), each once per window</td><td>No model-facing nudge at all (clause 6)</td></tr></tbody></table><p>Source anchors for each of the above:</p><ul><li><code>core/src/compact.rs</code> — <code>COMPACT_USER_MESSAGE_MAX_TOKENS = 20_000</code>; <code>build_compacted_history_with_limit()</code> walks history newest-first keeping only user messages, truncates the one that crosses the limit, <code>reverse()</code>s back into order, then appends the summary; <code>:384</code> raises <code>EventMsg::Warning</code> after every compaction.</li><li><code>core/src/compact_token_budget.rs</code> — the no-summary path runs the same compaction lifecycle, emits the same <code>ContextCompaction</code> turn item, and then calls <code>start_new_context_window()</code>.</li><li><code>session/mod.rs:3665 start_new_context_window()</code> — history is cleared to the initial context plus the turn context item, with <code>message: String::new()</code>.</li><li><code>session/turn.rs:423</code> — <code>should_roll_over = needs_follow_up &amp;&amp; (take_new_context_window_request() || token_limit_reached)</code>: the trigger is evaluated at a turn boundary, inline.</li><li><code>session/token_budget.rs:66 maybe_record()</code> — tier one at <code>remaining &lt;= reminder_threshold_tokens</code>, tier two at <code>remaining == 0 &amp;&amp; allow_fallback</code>, each claimed once per window.</li><li><code>tools/spec_plan.rs:994</code> and <code>tools/handlers/new_context_window_spec.rs</code> — <code>new_context</code>, parameterless, described as &quot;Start a new context window. Does not clear, reset, or otherwise affect environment state.&quot;</li></ul><p>The user asked for Codex parity explicitly, was told it reverses the imperceptibility goal they had asked for one round earlier, and reaffirmed it. Only one ADR 0061 clause survives on its own merits <em>and</em> matches Codex: hiding the tuning knobs. Codex reads reserve and retention values from model metadata and never asks the user either.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Compaction follows Codex&#39;s mechanism. ADR 0030&#39;s hard boundary, ADR 0049&#39;s retained-tail recovery, ADR 0061&#39;s model-window-derived budgets, and ADR 0061&#39;s <code>buildCheckpoint</code> / <code>installCheckpoint</code> split all stay; ADR 0061 clauses 2, 4, 6, 7, and 8 are replaced.</p><ol><li><strong>Inline only.</strong> All background pre-computation is deleted: <code>pendingBackgroundCheckpoint</code>, <code>backgroundCompaction</code>, <code>backgroundAbort</code>, <code>checkpointBaselineTokens</code>, <code>backgroundLimit</code>, and the three call sites in <code>tool_execution_start</code>, <code>prompt()</code>, and the run <code>finally</code>. <code>prepareNextTurn()</code> re-estimates the budget and compacts synchronously when the total crosses <code>hardLimit</code>. The increment-scoped trigger goes with it — there is no longer a second threshold for it to guard.</li><li><strong>Codex&#39;s retained shape.</strong> <code>codexShapedPreparation()</code> keeps <code>prepareCompaction()</code>&#39;s cut point (and therefore its turn-boundary and split-turn handling), then folds <code>turnPrefixMessages</code> and <code>retainedTail</code> back into <code>messagesToSummarize</code> so the summary covers the entire compacted range, and rebuilds the retained tail as user messages only: <code>selectRetainedUserMessages()</code> walks the compacted range plus the previous checkpoint&#39;s retained users newest-first up to <code>COMPACTION_RETAINED_USER_MESSAGE_MAX_TOKENS = 20_000</code> (capped at half the hard budget so retention alone cannot fill a small window), truncates the message that crosses the limit instead of dropping it, and reverses back into chronological order. Folding the tail into the summary is not optional: <code>prepareCompaction()</code> only summarizes below the cut point, so filtering the tail without doing this would silently lose assistant and tool content that nothing summarized. Dropping assistant messages also drops their tool calls, so no orphaned <code>tool_use</code> can reach the provider.</li><li><strong>Two families behind an internal switch.</strong><code>CompactionStrategy = &quot;summary&quot; | &quot;fresh_window&quot;</code> resolves from a construction option, then <code>PI_DESKTOP_COMPACTION_STRATEGY</code>, defaulting to <code>&quot;summary&quot;</code>. <code>fresh_window</code> issues no summary request: it installs a checkpoint with an empty retained tail and the fixed <code>CONTEXT_ROLLOVER_SUMMARY</code> marker text, then continues through the identical lifecycle — budget re-estimation, <code>session.appendCompaction</code>, <code>compaction_end</code>, transcript row, warning — mirroring Codex modelling its token-budget rollover as an ordinary compaction. The switch reaches neither <code>AppSettings</code>, Settings, nor i18n.</li><li><strong><code>new_context</code> is back.</strong> The tool is parameterless and carries Codex&#39;s description verbatim; executing it only sets <code>pendingModelCompaction</code> and returns the family&#39;s rollover message. <code>prepareNextTurn()</code> compacts when <code>pendingModelCompaction || tokens &gt;= hardLimit</code>, matching Codex&#39;s <code>should_roll_over</code>. The name is synchronized across <code>activeTools()</code>, <code>isCoreTool()</code>, the contract-mode allowlists, and the host-core no-confirmation allowlist (<code>crates/host-core/src/permissions.rs</code>), where it replaces <code>&quot;CompactContext&quot;</code>.</li><li><strong>Two-tier reminders, once each per window.</strong> With <code>remaining = hardLimit - tokens</code>: tier one at <code>remaining &lt;= clamp(hardLimit * 0.15, 8k, 32k)</code> states the remaining budget and asks the model to start closing out; tier two at <code>remaining &lt;= 2_000</code> tells it to write down now whatever must survive. Each is claimed once and both claims reset when a checkpoint is installed. The reminder is appended to the current turn&#39;s system prompt and restored in <code>finally</code> — the same mechanism as <code>SILENT_TURN_NUDGE</code> — so it never enters the transcript or the durable history.</li><li><strong>The whole checkpoint chain is durable.</strong> <code>read_compactions()</code> and <code>write_transcript_with_compactions()</code> replace their latest-only predecessors; <code>sessions.rs</code> validates, forks, and remaps every record and adds <code>compactions: Vec&lt;CompactionRecord&gt;</code> to the session detail payload, keeping <code>compaction</code> as its last element. One transcript row per compaction requires the chain to survive restart, late truncation, and forks.</li><li><strong>One transcript row per compaction, and a warning every time.</strong><code>compaction_end</code> carries <code>mark?: ContextCompactionMark</code> (<code>id</code>, <code>throughMessageId</code>, <code>generation</code>, <code>summaryTokens</code>, <code>summarized</code>) instead of ADR 0061&#39;s <code>status</code>, and drops <code>phase</code> entirely; the durable records supply the same marks on session open or fork. <code>buildTranscriptEntries()</code> inserts a <code>kind: &quot;compaction&quot;</code> entry after the anchor message, ending whatever assistant turn contains it, and discards a mark whose anchor no longer exists. A successful compaction also raises an unconditional warning toast; the fallback, overflow, and manual toasts stay, because each says something more specific. The context inspector keeps its line, now reading the newest mark.</li></ol><p>Manual <code>/compact</code> is unchanged and remains fail-fast. Settings still exposes no compaction controls and still ignores persisted <code>contextCompaction</code> values (ADR 0061 clause 9).</p><h3 id="deliberate-deviations-from-codex" tabindex="-1">Deliberate deviations from Codex <a class="header-anchor" href="#deliberate-deviations-from-codex" aria-label="Permalink to &quot;Deliberate deviations from Codex&quot;">​</a></h3><ul><li><strong>Summary position.</strong> Codex appends the summary after the retained user messages. <code>buildSessionContext()</code> in pi-agent-core emits <code>createCompactionSummaryMessage(summary)</code> before <code>entry.retainedTail</code>, and that order is not ours to choose. Both orders present the same content.</li><li><strong>Task boundary.</strong> ADR 0136 narrows the retained tail: an active turn may retain only its latest user message, while a completed-turn checkpoint has an empty tail so the next prompt cannot be mistaken for a continuation of completed work.</li><li><strong><code>hardLimit</code> derivation.</strong> Codex compacts at 90% of the window; we keep ADR 0030&#39;s &quot;window − output reserve&quot;. Codex can afford the looser number because it has a separate full-window guard; we do not, and an oversized request is a hard provider error.</li><li><strong>Tool registration.</strong> Codex gates <code>new_context</code> behind <code>Feature::TokenBudget</code>, so it exists only for the no-summary family. We register it in both, because the requirement was that the model has the tool. <code>get_context_remaining</code> is not implemented; the reminders carry the number instead.</li><li><strong>Reminder thresholds and text.</strong> Codex reads both from per-model metadata. We have no such feed, so the thresholds derive from the same <code>hardLimit</code> the guard uses and the wording is ours.</li><li><strong>Reminder delivery.</strong> Codex injects synthetic history items. We have no channel for a history entry that stays out of the transcript, so the reminder is a per-turn system-prompt append instead — equivalent in effect and with no persistence risk.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Compaction is paid for at the moment the user is waiting again. That is the cost of parity; Codex pays it too. The zero-wait property from ADR 0061 is gone.</li><li>Model context after a compaction is much smaller and much lossier: no assistant reasoning and no tool output survives except through the summary. The visible transcript is untouched, so nothing is lost to the user.</li><li>The summary input is now the entire compacted range rather than the range minus the tail, so each summary request is larger than under ADR 0061 — but there are fewer of them, because the trigger is one hard edge again.</li><li>Compaction is auditable from the transcript again, restoring ADR 0030&#39;s visibility property that ADR 0061 reversed.</li><li>Every compaction interrupts the user with a warning. This is intended: only the user can decide to start a fresh session instead, and after several compactions that is usually the better answer.</li><li>The model can compact early and deliberately, and can also ignore the reminders — the hard boundary is still there when it does.</li><li>The no-summary family is implemented but unreachable in a shipped build. It exists so the mechanism is complete and testable, not as a product option.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><h3 id="keep-background-pre-computation-and-add-the-visible-row-and-warning" tabindex="-1">Keep background pre-computation and add the visible row and warning <a class="header-anchor" href="#keep-background-pre-computation-and-add-the-visible-row-and-warning" aria-label="Permalink to &quot;Keep background pre-computation and add the visible row and warning&quot;">​</a></h3><p>Rejected. The user asked for Codex&#39;s mechanism, not a superset of it, and a pre-computed checkpoint that installs at an unrelated turn boundary makes the row&#39;s position arbitrary.</p><h3 id="filter-the-retained-tail-to-user-messages-without-folding-it-into-the-summary" tabindex="-1">Filter the retained tail to user messages without folding it into the summary <a class="header-anchor" href="#filter-the-retained-tail-to-user-messages-without-folding-it-into-the-summary" aria-label="Permalink to &quot;Filter the retained tail to user messages without folding it into the summary&quot;">​</a></h3><p>Rejected as a correctness bug: <code>prepareCompaction()</code> summarizes only below the cut point, so the dropped assistant and tool messages inside the tail would be covered by nothing.</p><h3 id="set-keeprecenttokens-0-and-let-preparecompaction-produce-an-empty-tail" tabindex="-1">Set <code>keepRecentTokens: 0</code> and let <code>prepareCompaction()</code> produce an empty tail <a class="header-anchor" href="#set-keeprecenttokens-0-and-let-preparecompaction-produce-an-empty-tail" aria-label="Permalink to &quot;Set \`keepRecentTokens: 0\` and let \`prepareCompaction()\` produce an empty tail&quot;">​</a></h3><p>Rejected. When the last entry is a tool result, <code>findCutPoint()</code> falls back to the earliest valid cut point, which discards far more than intended.</p><h3 id="expose-the-family-switch-in-settings" tabindex="-1">Expose the family switch in Settings <a class="header-anchor" href="#expose-the-family-switch-in-settings" aria-label="Permalink to &quot;Expose the family switch in Settings&quot;">​</a></h3><p>Rejected. Codex does not expose it, and no user can judge &quot;no summary at all&quot; against &quot;one summary request&quot; from a settings row.</p><h2 id="references" tabindex="-1">References <a class="header-anchor" href="#references" aria-label="Permalink to &quot;References&quot;">​</a></h2><ul><li><code>docs/spec/03-runtime/01-ipc-protocol.md</code></li><li><code>docs/spec/03-runtime/02-agent-runtime.md</code></li><li><code>docs/spec/03-runtime/03-tools-and-permissions.md</code></li><li><code>docs/spec/03-runtime/04-data-storage.md</code></li><li><code>docs/spec/04-ux/08-component-spec.md</code></li><li><code>docs/spec/04-ux/09-interaction-patterns.md</code></li><li><code>docs/spec/06-delivery/04-e2e-test-plan.md</code></li><li><code>docs/spec/08-meta/decisions-log.md</code> (D158, D200, D203)</li><li><code>codex-rs/core/src/compact.rs</code>, <code>compact_token_budget.rs</code>, <code>session/token_budget.rs</code>, <code>tools/handlers/new_context_window_spec.rs</code> (behavioral reference)</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0064-codex-parity-context-compaction.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0064CodexParityContextCompaction = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0064CodexParityContextCompaction as default
};
