import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0298: The app ships no fonts","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0298-remove-bundled-fonts.md","filePath":"adr/0298-remove-bundled-fonts.md","lastUpdated":1789839347000}');
const _sfc_main = { name: "adr/0298-remove-bundled-fonts.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0298-the-app-ships-no-fonts" tabindex="-1">ADR 0298: The app ships no fonts <a class="header-anchor" href="#adr-0298-the-app-ships-no-fonts" aria-label="Permalink to &quot;ADR 0298: The app ships no fonts&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-20</li><li>Deciders: PI-Desktop UX and desktop maintainers</li><li>Baseline: <code>0.15.1-beta.7</code> (frozen baseline <code>0.4.18</code>)</li><li>Protocol: v11 (unchanged; the <code>pi-desktop/app/systemFonts</code> channel is untouched)</li><li>Storage schema: v19 (unchanged; <code>AppSettings.fontFamily</code> keeps its shape and semantics)</li><li>Amends: <a href="./0083-custom-global-ui-font">ADR 0083</a> — its §2 bundled families, its <code>@font-face</code>/offline-rendering clauses, and its installer-size consequence — and D232</li><li>Related: D598, E2E-126, E2E-092, <a href="./../spec/04-ux/06-settings-ia">04-ux/06-settings-ia.md</a> · <a href="./../spec/04-ux/07-ui-design-system">04-ux/07-ui-design-system.md</a> · <a href="./../spec/06-delivery/06-release-runbook">06-delivery/06-release-runbook.md</a></li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>ADR 0083 shipped the global UI font picker together with four bundled <code>woff2</code> families — Geist, Inter, Noto Sans SC, and LXGW WenKai — all under the SIL Open Font License 1.1, with their license texts under <code>apps/desktop/src/assets/fonts/licenses/</code> and a <code>@font-face</code> layer in <code>apps/desktop/src/styles/fonts.css</code> imported before the token layer. Every custom stack was prefixed with <code>Noto Sans SC</code> so Chinese text stayed readable offline, and the picker gained a <strong>Bundled</strong> group whose rows carried the license badge.</p><p>Those files are the bulk of the renderer&#39;s remaining weight: ADR 0125 measured <code>woff2</code> at 15.71 MiB in <code>out/renderer</code>, 7.6 MiB of it <code>lxgw-wenkai.woff2</code> and 7.4 MiB <code>noto-sans-sc.woff2</code> (<a href="./../spec/06-delivery/06-release-runbook">06-delivery/06-release-runbook.md</a>). Every platform the desktop targets already resolves the same coverage from its own fonts — PingFang SC on macOS, Microsoft YaHei on Windows, and fontconfig CJK families on Linux — so the app paid for a duplicate copy in every installer and carried the OFL notice obligations that come with redistributing font files, while offering only four faces next to the hundreds the OS already installs. The product wants the picker, not the payload.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><p><strong>Nothing is bundled.</strong> <code>apps/desktop/src/assets/fonts/</code> (the four <code>woff2</code> files and their <code>licenses/</code> texts), <code>apps/desktop/src/styles/fonts.css</code>, and the <code>@import &quot;./fonts.css&quot;;</code> line in <code>apps/desktop/src/styles/globals.css</code> are deleted. <code>BUNDLED_FONTS</code>, the <code>BundledFont</code> type, the <code>license</code> field on <code>FontOption</code>, the <code>settings-font-item-license</code> badge, and the <code>settings.fontBundled</code> key in all eight shipped locale catalogs are deleted with them.</p></li><li><p><strong>The picker keeps two kinds of option.</strong> <code>buildFontOptions</code> returns <strong>System default</strong> — persisted as the empty string <code>&quot;&quot;</code>, which every consumer treats as the built-in <code>--font-sans</code> token stack — followed by installed system families. Enumeration is exactly ADR 0083 §3 and is unchanged: Electron main uses platform tooling only (<code>osascript</code> JXA CoreText on macOS with <code>system_profiler</code> as a fallback, PowerShell on Windows, <code>fc-list</code> on Linux), deduplicated, filtered, sorted, cached 60 s, and reached through the additive allowlisted channel <code>pi-desktop/app/systemFonts</code>. A stored stack that matches no option is still re-added first under the existing <strong>Saved</strong> (<code>custom</code>) group.</p></li><li><p><strong>The CJK fallback tier is system-only.</strong> Every stack generated by the picker appends <code>&quot;PingFang SC&quot;, &quot;Hiragino Sans GB&quot;, &quot;Microsoft YaHei&quot;, sans-serif</code>. No bundled family precedes it, so Chinese text resolves to fonts the operating system provides and no selection can depend on a file the app no longer ships.</p></li><li><p><strong>Stored stacks are preserved, not migrated.</strong> A <code>AppSettings.fontFamily</code> written while a bundled option existed (for example <code>&quot;Geist&quot;, &quot;PingFang SC&quot;, &quot;Hiragino Sans GB&quot;, &quot;Microsoft YaHei&quot;, sans-serif</code>) stays byte for byte. It matches no known option any more, so the picker shows the stored value first under <strong>Saved</strong>, and at render time the first family is simply unknown to the platform and the cascade falls through to the system fallback tier. No storage migration runs, no stored value changes shape, and <code>settings.set</code> semantics, the protocol version, and the schema version are untouched.</p></li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li><code>out/renderer</code> no longer emits an application font face; the only <code>woff2</code> files left are KaTeX&#39;s math glyphs. Measured from a clean <code>pnpm install --frozen-lockfile</code>, the <code>woff2</code> group fell from 23 files / 15.71 MiB to 19 files / 0.24 MiB and the <code>out/renderer</code> total from 25.36 MiB to 9.89 MiB — a 15.47 MiB drop that is the four deleted faces (<a href="./../spec/06-delivery/06-release-runbook">06-delivery/06-release-runbook.md</a>).</li><li>A face the app used to supply now comes from the operating system. A user who wants Geist, Inter, or LXGW WenKai installs it on the OS, after which the picker lists it like any other installed family; LXGW WenKai ships on no platform by default, so that face is no longer available out of the box.</li><li>Chinese text still renders on every supported platform through the fallback tier, and no part of it reads a file the app ships.</li><li>The app no longer redistributes OFL-licensed font files, so the four shipped license texts and their notice obligations leave the bundle with the faces.</li><li>The picker&#39;s option list becomes &quot;System default plus whatever this machine has&quot;, which is the same list every other System-style picker in the app shows; the license badge and the <strong>Bundled</strong> group heading go away.</li><li>E2E-126 describes the two-group picker and the saved-stack fallback, and E2E-092&#39;s packaging checks expect no application <code>woff2</code> beyond KaTeX.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><h3 id="keep-the-bundled-families-and-subset-them" tabindex="-1">Keep the bundled families and subset them <a class="header-anchor" href="#keep-the-bundled-families-and-subset-them" aria-label="Permalink to &quot;Keep the bundled families and subset them&quot;">​</a></h3><p>Rejected: subsetting would drop glyphs from user-supplied content, which is why ADR 0125 kept the CJK faces unsubset in the first place, and it leaves the <code>@font-face</code> layer, the license tree, and the build wiring in place for a saving the platform fonts already provide.</p><h3 id="keep-the-bundled-families-behind-a-build-flag-or-setting" tabindex="-1">Keep the bundled families behind a build flag or setting <a class="header-anchor" href="#keep-the-bundled-families-behind-a-build-flag-or-setting" aria-label="Permalink to &quot;Keep the bundled families behind a build flag or setting&quot;">​</a></h3><p>Rejected: the files would still be built and shipped, so the installer cost and the notice obligations stay, and the app would gain a second font configuration surface for a four-item list.</p><h3 id="keep-noto-sans-sc-at-the-head-of-the-cjk-fallback-tier" tabindex="-1">Keep <code>Noto Sans SC</code> at the head of the CJK fallback tier <a class="header-anchor" href="#keep-noto-sans-sc-at-the-head-of-the-cjk-fallback-tier" aria-label="Permalink to &quot;Keep \`Noto Sans SC\` at the head of the CJK fallback tier&quot;">​</a></h3><p>Rejected: the tier must not name a family the app pretends to provide. It would only take effect on machines where the user installed Noto Sans SC separately, and it would silently change which face a Chinese glyph resolves to on exactly those machines.</p><h3 id="migrate-stored-bundled-stacks-to-a-system-equivalent" tabindex="-1">Migrate stored bundled stacks to a system equivalent <a class="header-anchor" href="#migrate-stored-bundled-stacks-to-a-system-equivalent" aria-label="Permalink to &quot;Migrate stored bundled stacks to a system equivalent&quot;">​</a></h3><p>Rejected: there is no honest mapping from a removed face to a system face, and rewriting the value would change the user&#39;s rendering without their action. Keeping the stored stack lets the picker show what was chosen under <strong>Saved</strong> and lets the cascade resolve it, and the user can pick a replacement at any time.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0298-remove-bundled-fonts.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0298RemoveBundledFonts = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0298RemoveBundledFonts as default
};
