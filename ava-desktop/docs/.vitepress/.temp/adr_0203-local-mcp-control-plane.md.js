import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0203: Local MCP Control Plane for Desktop Operations","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0203-local-mcp-control-plane.md","filePath":"adr/0203-local-mcp-control-plane.md","lastUpdated":1788969330000}');
const _sfc_main = { name: "adr/0203-local-mcp-control-plane.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0203-local-mcp-control-plane-for-desktop-operations" tabindex="-1">ADR 0203: Local MCP Control Plane for Desktop Operations <a class="header-anchor" href="#adr-0203-local-mcp-control-plane-for-desktop-operations" aria-label="Permalink to &quot;ADR 0203: Local MCP Control Plane for Desktop Operations&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-09</li><li>Decision: D370 (amended by D372)</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>PI-Desktop already has typed Electron-main IPC for projects, sessions, the pi Agent, workspace views, plugins, settings, and other desktop operations. That surface is available to the renderer only, so an external Agent cannot drive a running desktop without a second application-specific integration. The remote Gateway / WebUI control boundary remains out of scope under baseline decision #20 and ADR 0004.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ul><li>Add an optional Streamable HTTP MCP server inside Electron Main. It starts only when <code>PI_DESKTOP_MCP_CONTROL=1</code> is set and binds to <code>127.0.0.1</code>; an optional <code>PI_DESKTOP_MCP_PORT</code> selects the local port and defaults to 37123.</li><li>Validate any supplied <code>Origin</code> against loopback hostnames to prevent DNS rebinding from remote web content. Non-browser MCP clients may omit <code>Origin</code>.</li><li>Persist a random 256-bit bearer token in the Electron user-data directory and publish the current URL, token, PID, and active state in <code>mcp-control.json</code>. Write the token and manifest with mode <code>0600</code> where the platform supports POSIX permissions.</li><li>Delegate every exposed call to the existing registered main-process IPC handler. Provide named tools for the common project/session/Agent/workspace flow and a generic <code>pi_desktop_invoke</code> over an explicit, risk-tagged catalog. <code>pi_control_describe</code> is the discovery surface for that catalog.</li><li>Require <code>confirm: true</code> for dangerous generic operations and destructive named tools, including <code>session/configure</code> (permission mode). <code>confirm</code> is an agent acknowledgement, not a desktop user prompt. Exclude secret get/set/delete channels, provider/OAuth/MCP secret-write paths, settings writes, and renderer-only native picker/dialog channels (including <code>plugin/loadDev</code>). Strip secret-shaped fields from accepted arguments. A new IPC handler is not exposed automatically.</li><li>Bind only loopback addresses; <code>initialize</code> negotiates <code>2025-06-18</code> or the compatible <code>2025-03-26</code> value and never echoes an unsupported version. Bound both the text payload and <code>structuredContent</code>. Stop the server with <code>closeAllConnections</code> before host shutdown and mark the connection manifest inactive.</li><li>After successful <strong>mutating</strong> project/session/Agent calls, reuse the existing <code>pi-desktop/session/event/changed</code> renderer event with additive project and selection fields. Reads such as <code>session/get</code> do not refresh the visible desktop.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><p>An external local Agent can open a project, create or inspect sessions, send a prompt, observe status, and invoke reviewed desktop operations through a standard MCP client. The renderer and external Agent converge through the same IPC handlers and session refresh path, so there is no duplicated authorization or persistence logic.</p><p>The endpoint grants a local client the authority of the running desktop for the operations it invokes. Loopback binding, opt-in startup, bearer authentication, bounded requests/results, explicit operation review, and confirmation gates limit accidental exposure but do not protect against an untrusted process that can read the user&#39;s application-data directory or token. Remote clients, secret material, native pickers, and remote Gateway semantics remain excluded.</p><h2 id="amendment-d372" tabindex="-1">Amendment (D372) <a class="header-anchor" href="#amendment-d372" aria-label="Permalink to &quot;Amendment (D372)&quot;">​</a></h2><p>Narrow the first-version catalog to the project/session/Agent/workspace flow plus reviewed reads. <code>session/configure</code> is dangerous because it can set <code>permissionMode: &quot;auto&quot;</code>. Secret material is stripped from arguments even when a remaining operation accepts a generic object. The listen address is asserted to be loopback after bind. Renderer session refresh is mutation-only.</p><h2 id="verification" tabindex="-1">Verification <a class="header-anchor" href="#verification" aria-label="Permalink to &quot;Verification&quot;">​</a></h2><p><code>apps/desktop/test/mcp-control.test.mjs</code> and <code>apps/desktop/test/mcp-control-wiring.test.mjs</code> cover token authentication, protocol negotiation, tool discovery, project/session dispatch, dangerous-operation confirmation (including session configure), secret stripping, catalog exclusions, loopback bind refusal, mutation-only renderer refresh, and inactive shutdown manifests. E2E-220 records the full Electron journey; full local desktop E2E remains deferred by repository policy.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0203-local-mcp-control-plane.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0203LocalMcpControlPlane = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0203LocalMcpControlPlane as default
};
