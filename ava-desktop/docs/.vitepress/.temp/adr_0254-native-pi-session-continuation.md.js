import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0254: Continue native Pi sessions in their canonical JSONL","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0254-native-pi-session-continuation.md","filePath":"adr/0254-native-pi-session-continuation.md","lastUpdated":1789547461000}');
const _sfc_main = { name: "adr/0254-native-pi-session-continuation.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0254-continue-native-pi-sessions-in-their-canonical-jsonl" tabindex="-1">ADR 0254: Continue native Pi sessions in their canonical JSONL <a class="header-anchor" href="#adr-0254-continue-native-pi-sessions-in-their-canonical-jsonl" aria-label="Permalink to &quot;ADR 0254: Continue native Pi sessions in their canonical JSONL&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-14</li><li>Decision: D421</li><li>Supersedes: baseline D007 only for session discovery; one-shot import remains available</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>PI-Desktop historically imports Pi sessions by flattening them into Desktop-owned transcripts. That copy cannot preserve the native tree, compaction, custom entries, or receive turns subsequently sent from either application. The coding-agent SDK already owns the v3 session format and native model/auth/resource lifecycle.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>The Node agent sidecar exposes a source-discriminated <code>pi-native</code> adapter. It discovers files below Pi&#39;s agent session directory, projects immutable byte snapshots through <code>SessionManager.inMemory</code>, and opens a persistent <code>SessionManager</code> only when a prompt is accepted.</li><li>Native identity is an opaque composite derived from canonical file path plus native header id. Paths never cross the preload boundary. Electron merges native summaries with Rust-owned Desktop summaries; Rust remains the exclusive SQLite and Desktop-transcript owner.</li><li>Native continuation uses <code>createAgentSession</code> with Pi <code>ModelRuntime</code>, <code>SettingsManager</code>, <code>DefaultResourceLoader</code>, saved provider/model/thinking, and the original <code>SessionManager</code>. There is no Desktop provider fallback and no <code>UiMessage</code> reconstruction for model context.</li><li>The first slice supports list, detail, refresh, text-only Agent prompt with no tools (<code>noTools: &quot;all&quot;</code>), stop, and renderer-local pin/archive. Built-in and extension model tools stay disabled until an explicit Desktop permission bridge is designed; this is not full Pi tool parity. Rename, delete, move, revision, Plan/Goal, queue, collaboration, attachment, and transcript rewrite operations are rejected; fork and side chat are added by the amendment below.</li><li>A cooperative sidecar lease serializes PI-Desktop writers. Before each SDK append, the adapter verifies canonical file identity and full-byte fingerprint; after append it verifies exactly one line with the expected entry and parent was added. Divergence fails closed. A stale lease is reclaimed only for a dead local PID when the target is unchanged or a complete append-only extension with the same file identity, original byte prefix, and continuous parent chain. Capability refresh exposes reclaimable dead-owner leases without deleting them; only acquisition reclaims. Live, remote, malformed, or uncertain ownership is never stolen. An owned idle runtime remains promptable, while an active runtime is stoppable and rejects overlap until settlement. This cannot make an uncooperative Pi Web/CLI process honor the lease, so optimistic validation remains mandatory.</li><li>Version other than exactly v3, a missing trailing newline, missing cwd, unavailable saved provider/auth, untrusted project resources, corrupt identity, or another live lease keeps the session browseable and read-only with an explicit reason. Projection never repairs or migrates the source file.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Desktop sessions and their host-owned persistence are unchanged.</li><li>Native branches, compaction data (including unknown fields such as <code>retainedTail</code>), custom/context messages, and future unknown entries remain untouched because reads are in-memory and writes are SDK append-only.</li><li>An uncooperative writer can still race within the OS append operation. The adapter detects prefix/suffix divergence after the append, preserves bytes, disposes the runtime, and requires reload; full mutual exclusion requires Pi clients to adopt a shared lease protocol.</li><li><code>@earendil-works/pi-coding-agent</code> is a runtime dependency of the bundled sidecar and its session format behavior is version-pinned.</li><li><code>bindExtensions</code> runs native startup/resource discovery with the SDK headless UI (<code>mode: &quot;print&quot;</code>, <code>hasUI: false</code>), error ownership, and explicit unsupported session controls. Guards and listeners precede startup; failure disposes the session and lease. Native extensions are trusted local code, not Desktop plugins or sandboxed tools.</li><li>ModelRuntime initializes the local catalog/auth snapshot without model network refresh; the exact saved provider/model/auth is required. Native Composer readiness uses <code>canPrompt</code>, independently of Desktop provider secrets.</li><li>Desktop terminal completion follows SDK prompt settlement (after <code>agent_settled</code> hooks and final persistence), not intermediate <code>agent_end</code>. Durable user acknowledgements reconcile caller optimistic IDs to SDK IDs without changing native entry IDs or deduplicating text. Abort refreshes native detail and never rewrites/restores a durable user row.</li><li>Native compact and session-addressed queue push/list fail with <code>NATIVE_PI_UNSUPPORTED</code> before host access. Queue remove/prioritize retain the existing opaque <strong>host turnId</strong> contract, not a session/source contract. Native paths never create host queue entries; future native queue support requires an explicit source/session protocol extension.</li><li>Listing still synchronously parses complete changed and unchanged files. Projection caching and bounded async scanning are deferred; no responsiveness bound is claimed for large catalogs in this slice.</li></ul><h2 id="amendment-native-side-chat-forks-2026-09-14" tabindex="-1">Amendment: native side-chat forks (2026-09-14) <a class="header-anchor" href="#amendment-native-side-chat-forks-2026-09-14" aria-label="Permalink to &quot;Amendment: native side-chat forks (2026-09-14)&quot;">​</a></h2><p>The <code>sidechat:</code> panel and tab described in this amendment were removed by ADR 0268; the <code>session.fork</code> capability they routed to remains in place and is used by the Fork action.</p><p><code>session/fork</code> is now source-discriminated. Desktop sources keep the existing Rust <code>session.fork</code> contract; <code>native-pi:</code> sources route to a sidecar <code>native.session.fork</code>, which returns the child as an ordinary <code>SessionDetail</code>. The renderer never supplies or receives a file path.</p><p>A native fork branches the parent snapshot through the SDK (<code>SessionManager.inMemory</code> + <code>createBranchedSession</code>), so ancestry, label re-chaining, compaction re-parenting, and unknown entries follow the SDK&#39;s own rules. The parent file and its live manager are never mutated; the child is a new v3 JSONL in the parent&#39;s session directory with a header <code>parentSession</code> pointing at the canonical source path. Child metadata (title, and a fallback model/thinking) is appended in memory. Publication writes a complete temporary file (device/inode plus content hash captured from the exact payload), then hardlinks it to the final name after verifying that the staged file is still that inode with those exact bytes; the published child is verified against the same identity/hash before any projection or registration. A failed fork never exposes a partial child, never clobbers or renames an existing file, and removes only files whose device/inode and content still match what this operation itself wrote. An altered staging or published file fails closed with <code>NATIVE_PI_SESSION_CHANGED</code> and its uncertain bytes are preserved rather than deleted. The source bytes and our own running/opening state are re-checked immediately before publication; drift fails closed. As with the rest of the window, an uncooperative external writer that acts between verification and link is outside the cooperative-lease contract; the snapshot checks catch what they can and never touch the parent.</p><p>When the selected branch saved no model, the child records the <strong>parent session&#39;s saved</strong> provider/model; when the branch saved no thinking-level change at all, it records the parent&#39;s saved level. An explicit branch value, including &quot;off&quot;, always wins. There is no Desktop provider/auth fallback. Forking is a data-only copy: it executes no model and loads no project resources, so it remains allowed while the parent is provider-unavailable or project-untrusted. That does not grant prompt readiness - the child reports the same read-only reason and keeps the existing auth/trust gate until it is satisfied.</p><p>The side-chat panel streams native replies: <code>NativePiRuntime</code> projects <code>message_start</code>/<code>message_update</code> under a provisional row id and the terminal <code>message_end</code> names that exact id in the additive <code>replacesMessageId</code> field, so the renderer re-keys only that row through <code>projectMessageEnd</code> (active, cached/retained, and side-chat projections share the same seam); a generic Desktop completion carries no field and never touches another row. Durable user acknowledgements also reconcile the side-chat projection. Closing the panel removes only the renderer registration and tab; the child remains a native session in the sidebar and in title/project search, and reopens as an ordinary conversation. A send while a native turn is running fails before the Desktop queue with a visible message and a preserved draft; native sessions still cannot enqueue.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0254-native-pi-session-continuation.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0254NativePiSessionContinuation = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0254NativePiSessionContinuation as default
};
