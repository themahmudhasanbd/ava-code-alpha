import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Documentation site","description":"","frontmatter":{},"headers":[],"relativePath":"spec/02-architecture/04-documentation-site.md","filePath":"spec/02-architecture/04-documentation-site.md","lastUpdated":1789483292000}');
const _sfc_main = { name: "spec/02-architecture/04-documentation-site.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="documentation-site" tabindex="-1">Documentation site <a class="header-anchor" href="#documentation-site" aria-label="Permalink to &quot;Documentation site&quot;">​</a></h1><h2 id="status" tabindex="-1">Status <a class="header-anchor" href="#status" aria-label="Permalink to &quot;Status&quot;">​</a></h2><p>Accepted. See <a href="./../../adr/0079-vitepress-documentation-site">ADR 0079</a>.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>The <code>docs/</code> directory is a standalone VitePress project inside the pnpm workspace. Markdown remains the source of truth; VitePress supplies the local development server, static build, local search index, code highlighting, and versioned navigation shell.</p><p>The site exposes two locale entry points:</p><ul><li><code>/</code> — English-first complete documentation navigation.</li><li><code>/zh-CN/</code> — Simplified Chinese orientation plus a path-for-path companion page for every document under <code>docs/spec/</code>.</li></ul><p>For Vercel deployments whose Root Directory is <code>docs</code>, <code>docs/vercel.json</code> declares the VitePress build output as <code>.vitepress/dist</code> and enables Vercel&#39;s <code>cleanUrls</code> routing. This keeps extensionless links such as <code>/spec/README</code> and <code>/adr/README</code> working after a direct page refresh instead of becoming static hosting 404s.</p><p>Existing <code>spec/</code>, <code>adr/</code>, <code>project/</code>, and guide Markdown files remain in place so repository links and review history stay stable. Chinese specification pages live under <code>docs/zh-CN/spec/</code> with the same relative paths as their English sources. Every translated page links to the canonical English page and keeps code, protocol fields, and identifiers unchanged.</p><h2 id="local-commands" tabindex="-1">Local commands <a class="header-anchor" href="#local-commands" aria-label="Permalink to &quot;Local commands&quot;">​</a></h2><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> docs:dev</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> docs:build</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> docs:preview</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> docs:check</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>The production build is static and does not require a runtime service. The site may load Google Fonts during development or deployment, but the content and search index are generated locally by VitePress.</p><p><code>pnpm docs:check</code> verifies that every English specification has a matching Chinese Markdown file, and that the companion satisfies all of:</p><ol><li>a top-level heading,</li><li>Chinese characters somewhere in the body,</li><li>the <code>[英文源规格](/spec/&lt;path&gt;)</code> canonical-source link,</li><li>no leftover untranslated-placeholder token (the gate greps for it as a bare substring, so this page cannot quote it verbatim),</li><li>the same table shape as the English page — the count of <code>|</code> cells, row by row,</li><li>the same number of fenced code blocks as the English page.</li></ol><p>Conditions 5 and 6 make the gate structural rather than cosmetic: a Chinese page that drops a table row or a code block is reported even when its prose reads complete. The gate runs in <code>.github/workflows/docs-check.yml</code>, which covers the documentation paths intentionally ignored by the application CI workflow. Treat any failure as a list of mirrors to finish, and do not add a new English specification without its companion.</p><p><code>pnpm docs:check</code> also runs <code>scripts/check-docs.mjs</code>, a second gate over every Markdown page under <code>docs/</code>:</p><ol><li>exactly one H1, except that a <code>layout: home</code> page renders its hero instead,</li><li>balanced code fences, and tables whose rows agree on their column count,</li><li>the ADR catalog: an <code>NNNN-slug.md</code> (or plain slug) file name, one owner per decision id, an H1 that declares that id, and the Status, Context, and Decision sections,</li><li><code>adr/README.md</code>, which must list every record exactly once, and may only link a row to the record that owns its id,</li><li>every <code>ADR NNNN</code> citation under <code>docs/</code>, which must resolve to a record, or to a retired id that <code>08-meta/decisions-log.md</code> names,</li><li>every Chinese page, which must mirror an English page at the same relative path, with <code>index.md</code> and <code>README.md</code> read as the same page,</li><li><code>NAV.md</code>, which must list every page of its own tree, and the two <code>spec/</code> trees, whose section directories must match and stay numbered like <code>01-product</code>.</li></ol><p>The same workflow then runs the VitePress production build, which validates the rendered routes and every internal link.</p><h2 id="content-rules" tabindex="-1">Content rules <a class="header-anchor" href="#content-rules" aria-label="Permalink to &quot;Content rules&quot;">​</a></h2><ol><li>English remains the canonical source language for specs, ADRs, code identifiers, and protocol terms.</li><li>Every English specification has a Simplified Chinese companion at the same relative path under <code>/zh-CN/spec/</code>; both locales expose the same sections and reading order.</li><li>Chinese pages translate the complete prose, link back to the English source, and preserve code, protocol fields, and identifiers verbatim. When wording differs, the English contract remains authoritative.</li><li>The sidebar is derived from the Markdown tree so a new specification cannot be omitted from deep navigation by accident.</li><li>User-visible or protocol-visible documentation behavior belongs in the E2E test plan.</li><li>Navigation should expose the shortest useful path; deep files remain searchable and directly linkable.</li></ol></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("spec/02-architecture/04-documentation-site.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _04DocumentationSite = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _04DocumentationSite as default
};
