import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0223: Context Usage Display Preference","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0223-context-usage-display-preference.md","filePath":"adr/0223-context-usage-display-preference.md","lastUpdated":1789112320000}');
const _sfc_main = { name: "adr/0223-context-usage-display-preference.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0223-context-usage-display-preference" tabindex="-1">ADR 0223: Context Usage Display Preference <a class="header-anchor" href="#adr-0223-context-usage-display-preference" aria-label="Permalink to &quot;ADR 0223: Context Usage Display Preference&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-11</li><li>Deciders: PI-Desktop desktop UI maintainers</li><li>Amends: 0184</li><li>Related: <a href="./../spec/04-ux/06-settings-ia">04-ux/06-settings-ia</a> · <a href="./../spec/04-ux/08-component-spec">04-ux/08-component-spec</a> · <a href="./../spec/08-meta/decisions-log">08-meta/decisions-log</a> (D398) · E2E-250</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The composer toolbar&#39;s context usage inspector (ADR 0184 / D347) always leads with the remaining-capacity figure: the trigger ring, popover heading, tooltip, and <code>aria-label</code> all show the remaining token count and percentage. Some users find the used-capacity figure more intuitive — especially when context is lightly loaded and the remaining number is close to the total window, which provides little signal at a glance.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>A new setting <code>AppSettings.contextUsageDisplay</code> (<code>ContextUsageDisplay = &quot;remaining&quot; | &quot;used&quot;</code>) lets the user choose which figure the context inspector leads with. The default (and fallback for absent or unrecognised values) is <code>&quot;remaining&quot;</code>, preserving the existing behaviour.</li><li>When <code>contextUsageDisplay</code> is <code>&quot;used&quot;</code>, the composer toolbar ring&#39;s arc length (<code>strokeDashoffset</code>), the trigger percentage and token label, the popover heading, the tooltip, and the <code>aria-label</code> all switch to the used-capacity pair instead of the remaining pair. The ring fills proportionally to <code>usedRatio</code> rather than <code>remainingRatio</code>.</li><li>Warning and critical color thresholds remain based on <strong>remaining</strong> capacity (remaining ≤ 25 % → warning, ≤ 10 % → critical) regardless of the display mode. A display reading &quot;used 78 %&quot; still turns warning colour because only 22 % remains.</li><li>Settings → AI → Defaults gains a <code>ContextUsageDisplayRow</code> (segmented control: Remaining / Used) placed after the Link open destination row and before the Enter-to-send row.</li><li>The change is renderer-only: no protocol, storage schema, host-side migration, or IPC change. The host-core settings merge preserves unknown keys, so persisted <code>contextUsageDisplay</code> values survive across upgrades without a schema bump.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Users who prefer a &quot;how much have I spent&quot; mental model get a consistent display; users who prefer the original &quot;how much is left&quot; model see no change by default.</li><li>The ring arc direction flips visually when switching to <code>&quot;used&quot;</code>, which is the correct correspondence: a fuller ring means more context consumed.</li><li>Color semantics stay stable across modes, so the warning/critical signal is never ambiguous regardless of the chosen display direction.</li><li>No host or storage change means no migration risk and no protocol version bump.</li></ul><h2 id="rejected-alternatives" tabindex="-1">Rejected alternatives <a class="header-anchor" href="#rejected-alternatives" aria-label="Permalink to &quot;Rejected alternatives&quot;">​</a></h2><ul><li><strong>Boolean toggle (show-used: true/false):</strong> a two-value segmented control reads clearer than a checkbox for mutually exclusive display modes, and the <code>ContextUsageDisplay</code> union type leaves room for future modes without a type rename.</li><li><strong>Color thresholds also follow display mode:</strong> rejected; it would make a &quot;used 90 %&quot; ring green despite only 10 % remaining, which is dangerously misleading. Remaining capacity is the safety signal and must stay authoritative for color.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0223-context-usage-display-preference.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0223ContextUsageDisplayPreference = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0223ContextUsageDisplayPreference as default
};
