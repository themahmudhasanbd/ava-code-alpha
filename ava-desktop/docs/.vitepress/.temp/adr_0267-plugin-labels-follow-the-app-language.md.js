import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0267: Plugin Labels Follow the App Language","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0267-plugin-labels-follow-the-app-language.md","filePath":"adr/0267-plugin-labels-follow-the-app-language.md","lastUpdated":1789657556000}');
const _sfc_main = { name: "adr/0267-plugin-labels-follow-the-app-language.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0267-plugin-labels-follow-the-app-language" tabindex="-1">ADR 0267: Plugin Labels Follow the App Language <a class="header-anchor" href="#adr-0267-plugin-labels-follow-the-app-language" aria-label="Permalink to &quot;ADR 0267: Plugin Labels Follow the App Language&quot;">​</a></h1><ul><li><strong>Status</strong>: Accepted</li><li><strong>Date</strong>: 2026-09-16</li><li><strong>Related</strong>: <a href="./0009-english-first-globalization">ADR 0009</a> · <a href="./0160-shipped-locale-registry-and-language-picker">ADR 0160</a> · <a href="./0182-traditional-chinese-shell-locale">ADR 0182</a> · <a href="./0280-plugin-owned-ui-localizes-from-host-locale">ADR 0280</a> · <a href="./../spec/07-plugins/02-plugin-manifest-schema">07-plugins/02-plugin-manifest-schema</a> · <a href="./../spec/07-plugins/07-plugin-marketplace">07-plugins/07-plugin-marketplace</a></li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>A plugin&#39;s display name and description come from its manifest, and a marketplace card&#39;s from its catalog entry. Both are single strings written in whatever language the author used, so the Extensions page, the plugin launcher, and the marketplace drew Chinese names under an English shell and English names under a Chinese one. The plugin repository&#39;s own validator had been requiring a top-level <code>i18n</code> block — <code>{ &quot;en&quot;: { name, description, safetyNotes }, &quot;zh-CN&quot;: { … } }</code> — since its first release, and the catalog ships the same block on every entry. The desktop shell read none of it: rows rendered <code>name</code> and <code>description</code> verbatim, so a plugin translated by its author into the user&#39;s language still arrived in the wrong one.</p><p>The shell already has every piece the fix needs: <code>@pi-desktop/i18n</code> owns the shipped locale registry, the app language is resolved in the main process (<code>settings.language</code>, or the OS locale while it is <code>auto</code>), and localized manifest fields (<code>ui.title</code>, view titles, command and destination labels) are already resolved by the host through <code>resolvePluginLocalizedString</code> with English fallback.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><code>manifest.i18n</code> and the same block on a marketplace catalog entry become part of the plugin contract, with <code>name</code>, <code>description</code>, and <code>safetyNotes</code>. <code>en</code> and <code>zh-CN</code> are the contract locales; a plugin is not required to translate itself into the other shipped shell locales.</li><li>Every Chinese shell locale reads <code>zh-CN</code>; every other locale reads <code>en</code>. <code>zh-TW</code> therefore reads English rather than a half-shared <code>zh-CN</code> guess, matching <code>resolvePluginLocalizedString</code> (ADR 0182).</li><li>Resolution happens in the <strong>host</strong>, not the renderer: rows leave the process</li><li>Resolution happens in the <strong>host</strong>, not the renderer: rows leave the process as finished strings, exactly like a plugin view title. A missing locale, a missing field, or an empty string falls back per field to the other contract locale, and from there to the author&#39;s flat <code>name</code> / <code>description</code>, so a partial translation never blanks a row.</li><li>The desktop shell pushes the app language down (<code>plugins.setLocale</code>) whenever it changes and then emits <code>pluginChanged</code>, so the surfaces re-read. The registry keeps the author&#39;s own strings: a language change never rewrites persisted rows, and the <code>i18n</code> block is neither persisted nor sent over RPC.</li><li>A malformed block (not an object of locale → object, or a non-string <code>name</code>/<code>description</code>/<code>safetyNotes</code>) fails <code>validateManifest</code>. Unknown locales and unknown fields inside an entry are ignored, so a publisher may carry more than the contract requires.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A translated plugin now reads in the user&#39;s language on the Extensions page, the plugin launcher, and the marketplace, and its <code>safetyNotes</code> — the text that explains what an install can touch — reads in that language too.</li><li>Catalog search matches every locale&#39;s name and description, so a user who types what they saw in one language still finds the entry after switching.</li><li>Plugin authors keep shipping a single-language manifest if they want to: the flat fields remain the fallback, and no field of the block is mandatory to load.</li><li>The host holds one more piece of state (the display locale). It is pushed by the shell rather than read per request, so a row does not depend on which RPC happens to carry a locale.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><ul><li><strong>Resolve in the renderer</strong>: the renderer knows <code>i18n.language</code> and would switch instantly, but it would have to receive every manifest&#39;s <code>i18n</code> block, which contradicts the rule that the renderer never reads a manifest, and it would duplicate the fallback rules the host already owns.</li><li><strong>Per-request locale parameter on every plugin RPC</strong>: works for <code>plugins.list</code> but leaves the summaries the host itself returns (install results, permission changes, marketplace update checks) unlocalized, and adds a locale to every call site.</li><li><strong>Leave names to the author&#39;s language</strong>: the plugin repository already requires the block, so the shell would keep ignoring data its ecosystem publishes — the reported bug.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0267-plugin-labels-follow-the-app-language.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0267PluginLabelsFollowTheAppLanguage = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0267PluginLabelsFollowTheAppLanguage as default
};
