import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR: Desktop-owned automation dispatch with Host-owned schedules","description":"","frontmatter":{},"headers":[],"relativePath":"adr/scheduled-desktop-automations.md","filePath":"adr/scheduled-desktop-automations.md","lastUpdated":1790009940000}');
const _sfc_main = { name: "adr/scheduled-desktop-automations.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-desktop-owned-automation-dispatch-with-host-owned-schedules" tabindex="-1">ADR: Desktop-owned automation dispatch with Host-owned schedules <a class="header-anchor" href="#adr-desktop-owned-automation-dispatch-with-host-owned-schedules" aria-label="Permalink to &quot;ADR: Desktop-owned automation dispatch with Host-owned schedules&quot;">​</a></h1><ul><li>Status: Accepted for implementation</li><li>Date: 2026-09-20</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The shipped Scheduled page stores a cadence but never dispatches work when that cadence becomes due. The pinned pi-ai, pi-agent-core and pi-coding-agent 0.86.1 packages supply agent execution, not a persistent desktop wall-clock scheduler. The existing Host-owned task and run tables already provide the appropriate storage boundary.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Keep schedule calculation, persistence, duplicate admission and restart recovery in Rust host-core. A small Electron lifecycle service polls every 30 seconds while the app runs and dispatches admitted work through the existing agentPrompt handler. It never asks the renderer to execute a background task. The existing turn finalizer closes the task run. The service stops when the app quits.</p><p>Use the existing config_json extension boundary for a validated schedule {hour, minute, weekday, weekdays?}, nextRunAt in epoch milliseconds and workspacePath. The workspace is captured when a schedule is first configured. No new tables, columns, schema migration or change to existing task identifiers is needed. Old cadence-only records remain unarmed until explicitly configured. Existing scheduled.run retains its prepare-only response; additive scheduledExecute IPC dispatches a turn, and scheduledListRuns exposes the existing run ledger.</p><p>Automatic runs use Ask permission mode. They may wait for a user in their result conversation; this feature never grants approval. Plan/Goal rejection remains. The default provider/model are resolved at execution time. Retain the task&#39;s project even when the foreground workspace changes.</p><p>Expose Scheduled through a footer clock action as well as global search. The page has task and run-history views, editing, pause/resume, Run now and conversation links. This amends the earlier omission of Scheduled from the home sidebar; other destinations and navigation ownership remain unchanged.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><p>The app must remain running. A due occurrence more than 90 seconds late is skipped, never replayed in a burst. An active run suppresses an overlapping occurrence. Startup interrupts orphaned runs and arms only future occurrences. Daily/weekly times follow the host&#39;s local timezone. Weekly schedules accept a nonempty unique selection of weekdays, falling back to the legacy single weekday when absent. Hourly tasks instead wait one elapsed hour after saving, enabling, startup or the preceding admission. This avoids a minute selector for a fixed interval while preserving calendar semantics for daily/weekly tasks. No cloud execution, OS service, arbitrary cron or sub-hourly interval is added. Downgrading keeps the existing records readable but removes automatic execution.</p><h2 id="calendar-provenance-compatibility" tabindex="-1">Calendar provenance compatibility <a class="header-anchor" href="#calendar-provenance-compatibility" aria-label="Permalink to &quot;Calendar provenance compatibility&quot;">​</a></h2><p>The optional <code>calendarConfigured</code> configuration key records explicit Daily or Weekly calendar intent separately from Hourly&#39;s required internal placeholder. Legacy Daily/Weekly rows infer intent from their saved cadence. Legacy Hourly rows preserve saved fields but require an explicit schedule on calendar conversion, because their origin cannot be recovered reliably. Known calendar intent survives an Hourly round trip and restart. This uses the existing JSON extension boundary with no physical schema migration or new wire field. Downgrades retain readable task data but cannot enforce the conversion guard.</p><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><p>A plugin would duplicate lifecycle/storage ownership and make baseline automation depend on extension availability. A second agent loop would bypass existing admission, persistence and permissions. Both are rejected. The pi agent implementation remains the execution engine.</p><h2 id="conversation-management" tabindex="-1">Conversation management <a class="header-anchor" href="#conversation-management" aria-label="Permalink to &quot;Conversation management&quot;">​</a></h2><p>Expose project-scoped CRUD as on-demand Agent tools using the existing <code>tools.execute</code> permission and audit pipeline. A thin Host adapter reuses the scheduled RPC handlers with the calling session&#39;s project, so background conversations cannot retarget tasks when the foreground workspace changes. List is read-only; mutations keep normal approvals. Plan/Goal stay denied.</p><p>The UI offers four time periods (09:00, 14:00, 19:00, 22:00) to keep setup simple. AI tools retain precise local time configuration, and the form preserves those custom times. Direct database access or renderer-mediated tool mutation would duplicate ownership or bypass permission gates and is rejected.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/scheduled-desktop-automations.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const scheduledDesktopAutomations = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  scheduledDesktopAutomations as default
};
