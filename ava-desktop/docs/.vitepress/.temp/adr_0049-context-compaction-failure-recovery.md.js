import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0049: Recover automatic context compaction failures with a retained tail","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0049-context-compaction-failure-recovery.md","filePath":"adr/0049-context-compaction-failure-recovery.md","lastUpdated":1790057120000}');
const _sfc_main = { name: "adr/0049-context-compaction-failure-recovery.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0049-recover-automatic-context-compaction-failures-with-a-retained-tail" tabindex="-1">ADR 0049: Recover automatic context compaction failures with a retained tail <a class="header-anchor" href="#adr-0049-recover-automatic-context-compaction-failures-with-a-retained-tail" aria-label="Permalink to &quot;ADR 0049: Recover automatic context compaction failures with a retained tail&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-08-04</li><li>Amends: ADR 0030 / D158</li><li>Amended by: ADR 0061 (the fallback stays reserved for the blocking hard boundary; a failed background build is discarded silently) / ADR 0064 (there is no background build left to discard, and the <code>fresh_window</code> family issues no summary request, so this path cannot trigger there) / ADR 0282 (the summary request retries transient failures and the preflight guard sizes the serialized prompt, with one reduced pass, before this fallback runs) / ADR 0302 (the fallback keeps the real recent window rather than one user</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The turn-boundary context guard correctly prevents an oversized provider request, but an LLM summary request can fail after the session has already reached the hard boundary. Treating every automatic summary failure as a terminal turn leaves the complete transcript durable but forces the next user message to recover the session manually. It also makes a transient or provider-specific summarizer failure indistinguishable from an inability to persist a checkpoint.</p><p>The visible transcript and the model-facing context are already separate. A checkpoint can therefore preserve the recent provider-valid message tail even when no new natural-language summary is available. The checkpoint must still be durable and must pass the same hard-budget recheck; otherwise the provider request remains blocked.</p><p>This also covers the boundary case where the newest checkpoint is the current transcript leaf. pi reports no new history to summarize in that shape, but a new prompt can still make the retained tail too large. The full transcript is the source for rebuilding a smaller tail while the existing summary is carried forward.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>For automatic <code>threshold</code> and <code>overflow</code> compaction only, PI-Desktop uses a three-outcome controller:</p><ol><li>Preflight the summary input against the provider model window. If the serialized history plus the summary output allowance cannot fit, skip the doomed summary request; otherwise run the normal pi-agent-core summary compaction.</li><li>If summary generation fails, or a generated checkpoint remains above the safe budget, prepare a deterministic fallback checkpoint. It reuses the previous checkpoint summary when available, adds a short recovery marker, keeps an aggressively bounded recent tail, and records <code>details.fallback = &quot;retained_tail&quot;</code>.</li><li>Append the fallback through host-core and re-estimate the model context. A successful fallback emits <code>compaction_end</code> with <code>fallback: &quot;retained_tail&quot;</code>; the active run continues, and the renderer shows a warning rather than a normal success toast.</li></ol><p>Manual <code>/compact</code> never silently falls back. Durable append failures, missing transcript boundaries, and fallback checkpoints that remain oversized emit <code>CONTEXT_COMPACTION_FAILED</code> and block the guarded provider request.</p><p>The complete transcript is never deleted, rewritten, or replaced by the fallback. The fallback is only the model-context view rebuilt after restart.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Automatic compaction failures no longer terminate a run when a safe durable retained tail can be installed.</li><li>The model may lose older task details when summary generation fails; the warning and checkpoint metadata make that degradation explicit.</li><li>Host persistence and hard-budget validation remain mandatory, so recovery cannot trade a better UX for an unsafe provider request.</li><li>Manual compaction remains an explicit user action with fail-fast semantics.</li><li>The event and checkpoint metadata provide a stable diagnostic signal without persisting provider error text that may contain sensitive endpoint details.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><h3 id="continue-without-a-checkpoint" tabindex="-1">Continue without a checkpoint <a class="header-anchor" href="#continue-without-a-checkpoint" aria-label="Permalink to &quot;Continue without a checkpoint&quot;">​</a></h3><p>Rejected because the next provider request would bypass the hard guard and could reproduce the provider context-limit failure.</p><h3 id="delete-visible-transcript-history" tabindex="-1">Delete visible transcript history <a class="header-anchor" href="#delete-visible-transcript-history" aria-label="Permalink to &quot;Delete visible transcript history&quot;">​</a></h3><p>Rejected because it destroys searchability, diagnostics, fork inputs, and the user&#39;s complete conversation.</p><h3 id="retry-the-summary-indefinitely" tabindex="-1">Retry the summary indefinitely <a class="header-anchor" href="#retry-the-summary-indefinitely" aria-label="Permalink to &quot;Retry the summary indefinitely&quot;">​</a></h3><p>Rejected because a long or incompatible provider failure can hold the session busy indefinitely. The bounded deterministic fallback is predictable and keeps the provider request gate intact.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0049-context-compaction-failure-recovery.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0049ContextCompactionFailureRecovery = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0049ContextCompactionFailureRecovery as default
};
