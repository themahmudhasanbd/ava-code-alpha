import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"远程 Agent 控制安全规格","description":"","frontmatter":{},"headers":[],"relativePath":"zh-CN/spec/05-security/02-remote-control-security.md","filePath":"zh-CN/spec/05-security/02-remote-control-security.md","lastUpdated":1789036160000}');
const _sfc_main = { name: "zh-CN/spec/05-security/02-remote-control-security.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="远程-agent-控制安全规格" tabindex="-1">远程 Agent 控制安全规格 <a class="header-anchor" href="#远程-agent-控制安全规格" aria-label="Permalink to &quot;远程 Agent 控制安全规格&quot;">​</a></h1><ul><li>状态：目标规格，属于 MVP 之后</li><li>决策：D373 / ADR 0205，经 D374 与 D375 修订</li><li>英文源规格：<a href="/spec/05-security/02-remote-control-security">英文源规格</a></li></ul><h2 id="_1-安全目标" tabindex="-1">1. 安全目标 <a class="header-anchor" href="#_1-安全目标" aria-label="Permalink to &quot;1. 安全目标&quot;">​</a></h2><p>远程控制必须提供身份认证、会话级授权、TLS、撤销、审计、资源上限、 无重放保证，以及远程权限上限，使远程 controller 无法把会话变成无人值守 执行。prompt、tool result、附件和模型输出均是不可信数据，不能凭自身内容 提升权限。</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Remote Client ── HTTPS/WSS ── Gateway</span></span>
<span class="line"><span>                                  │ outbound mTLS Host link</span></span>
<span class="line"><span>                                  ▼</span></span>
<span class="line"><span>                             Agent Host</span></span>
<span class="line"><span>                             ├── pi sidecar</span></span>
<span class="line"><span>                             └── Rust host-core</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><h2 id="_2-信任区" tabindex="-1">2. 信任区 <a class="header-anchor" href="#_2-信任区" aria-label="Permalink to &quot;2. 信任区&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Zone</th><th>Trust assumption</th><th>Required boundary</th></tr></thead><tbody><tr><td>Remote Client</td><td>已认证但 UI 和 prompt 不可信</td><td>有范围的 session 或 bearer 凭据，不默认给 secret 权限</td></tr><tr><td>Gateway</td><td>用户自托管的中继，暴露在网络边界</td><td>auth、授权、限流、审计、仅瞬态缓冲，不访问 raw host RPC</td></tr><tr><td>Agent Host</td><td>工作区旁的本地权威</td><td>mTLS/device identity、签名 route context、Host policy、远程权限上限</td></tr><tr><td>Node sidecar</td><td>Agent runtime，不拥有 policy</td><td>Main/Host proxy allowlist</td></tr><tr><td>Rust host-core</td><td>工作区、存储、工具、权限、secret 权威</td><td>仅 stdio，无公网监听</td></tr></tbody></table><div class="language-ts vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">type</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> HostRouteContext</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">tenantId</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">hostId</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">subject</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">clientConnectionId</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">sessionScopes</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">[]; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">roles</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">[]; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">expiresAt</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> }</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><h2 id="_3-身份、授权与边界" tabindex="-1">3. 身份、授权与边界 <a class="header-anchor" href="#_3-身份、授权与边界" aria-label="Permalink to &quot;3. 身份、授权与边界&quot;">​</a></h2><p>D385 使远程控制从结构上就是用户本地的：链路中没有任何项目方运营的身份或账号服务， 客户端持有的唯一凭据是用户自己的 Host 在配对时签发的设备 token；Gateway 若由用户 自托管，也只以这些 Host 签发的设备凭据准入，并在路由前校验其 Host id、有效期与撤销 状态。OIDC 联合与 pi-backend 账号服务不在远程控制范围内，该模型中不存在 refresh token。 浏览器无法在 WebSocket/EventSource 上设置请求头，因此 Gateway 提供两种认证 profile：非浏览器客户端用 <code>Authorization</code> 头（header profile）；浏览器客户端用 HttpOnly、Secure、SameSite cookie + 按租户的 Origin 白名单 + 每次 mutation 的 CSRF token（cookie profile），也可改用带头部认证的 <code>fetch</code> 流式读取。两种 profile 都禁止把 token 放进 URL。Gateway 与 cookie profile 属于不排期的 Gateway 与浏览器里程碑（D375）；首个远程拓扑使用 header profile，设备 token 来自 SSH 引导配对。Host 通过出站 mTLS Host link 连接 Gateway，link 只认证 Gateway，用户权限只来自每个逻辑连接的签名 route context；一次性 enrollment credential 必须短期、单次使用。</p><p>SSH 引导配对（首个远程拓扑）：桌面经用户自己的 SSH 会话在远端启动 <code>pi-host</code>， SSH 登录已证明对该机器的 shell 访问，配对只是把一个桌面设备绑定到它启动的 Host。配对 token 由 Host 启动时生成，单次使用，在引导窗口内过期，只经 SSH 通道 传递，绝不写入可读文件或 URL；桌面在转发的 loopback 端口上用它一次性换取设备 token 并存入安全存储，Host 把该设备记为 <code>owner</code>。Host 只绑定 loopback，只接受 loopback 对端出示的设备 token；非 loopback 绑定要求 TLS 与同样的设备 token。引导脚本经 SSH 上传后，从 GitHub Releases 下载与桌面同版本、对应远端平台的 <code>pi-host</code> 包，校验随发布公布的 SHA-256 后 安装到用户主目录；桌面自身从不上传可执行字节。首版无法引导没有 GitHub 出网能力的 机器。 在 Host 上撤销设备 token 或在桌面移除该 Host 即结束配对，重新配对需要重新 经 SSH 引导。远端 Host 的 provider 配置由引导步骤经 SSH 通道写入为 Host 本地 配置，绝不经过 RACP。</p><table tabindex="0"><thead><tr><th>Operation</th><th style="${ssrRenderStyle({ "text-align": "right" })}">Viewer</th><th style="${ssrRenderStyle({ "text-align": "right" })}">Controller</th><th style="${ssrRenderStyle({ "text-align": "right" })}">Approver</th><th style="${ssrRenderStyle({ "text-align": "right" })}">Owner</th></tr></thead><tbody><tr><td>List/get/subscribe/history</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td></tr><tr><td>Create/attach as viewer</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td></tr><tr><td>Start or queue a turn</td><td style="${ssrRenderStyle({ "text-align": "right" })}">no</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td><td style="${ssrRenderStyle({ "text-align": "right" })}">optional</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td></tr><tr><td>Stop/interrupt/cancel a turn</td><td style="${ssrRenderStyle({ "text-align": "right" })}">no</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td><td style="${ssrRenderStyle({ "text-align": "right" })}">optional</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td></tr><tr><td>Resolve tool approval (<code>allow-once</code>, <code>deny</code>)</td><td style="${ssrRenderStyle({ "text-align": "right" })}">no</td><td style="${ssrRenderStyle({ "text-align": "right" })}">no by default</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td></tr><tr><td>Resolve tool approval with <code>allow-session</code></td><td style="${ssrRenderStyle({ "text-align": "right" })}">no</td><td style="${ssrRenderStyle({ "text-align": "right" })}">no</td><td style="${ssrRenderStyle({ "text-align": "right" })}">policy</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td></tr><tr><td>Resolve Plan/Goal approval with permission mode</td><td style="${ssrRenderStyle({ "text-align": "right" })}">no</td><td style="${ssrRenderStyle({ "text-align": "right" })}">no by default</td><td style="${ssrRenderStyle({ "text-align": "right" })}">explicit policy</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td></tr><tr><td>Answer an input request</td><td style="${ssrRenderStyle({ "text-align": "right" })}">no</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td><td style="${ssrRenderStyle({ "text-align": "right" })}">optional</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td></tr><tr><td>Upload attachment</td><td style="${ssrRenderStyle({ "text-align": "right" })}">no</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td><td style="${ssrRenderStyle({ "text-align": "right" })}">optional</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td></tr><tr><td>Revoke membership</td><td style="${ssrRenderStyle({ "text-align": "right" })}">no</td><td style="${ssrRenderStyle({ "text-align": "right" })}">no</td><td style="${ssrRenderStyle({ "text-align": "right" })}">no</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td></tr><tr><td>Archive session</td><td style="${ssrRenderStyle({ "text-align": "right" })}">no</td><td style="${ssrRenderStyle({ "text-align": "right" })}">no</td><td style="${ssrRenderStyle({ "text-align": "right" })}">no</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td></tr><tr><td>Provider secrets</td><td style="${ssrRenderStyle({ "text-align": "right" })}">no</td><td style="${ssrRenderStyle({ "text-align": "right" })}">no</td><td style="${ssrRenderStyle({ "text-align": "right" })}">no</td><td style="${ssrRenderStyle({ "text-align": "right" })}">no</td></tr><tr><td>Raise the remote permission ceiling</td><td style="${ssrRenderStyle({ "text-align": "right" })}">no</td><td style="${ssrRenderStyle({ "text-align": "right" })}">no</td><td style="${ssrRenderStyle({ "text-align": "right" })}">no</td><td style="${ssrRenderStyle({ "text-align": "right" })}">no</td></tr><tr><td>Open or use a session terminal</td><td style="${ssrRenderStyle({ "text-align": "right" })}">no</td><td style="${ssrRenderStyle({ "text-align": "right" })}">policy</td><td style="${ssrRenderStyle({ "text-align": "right" })}">policy</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td></tr><tr><td>Advertise relayed tools</td><td style="${ssrRenderStyle({ "text-align": "right" })}">no</td><td style="${ssrRenderStyle({ "text-align": "right" })}">no</td><td style="${ssrRenderStyle({ "text-align": "right" })}">no</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td></tr></tbody></table><p>客户端不能通过字段指定 workspaceRoot、permissionMode（Plan/Goal <code>approve</code> 上的显式选择除外，且须在 <code>allowedPermissionModes</code> 内）、toolName、provider secret 或其他 principal / clientConnectionId；<code>admission: &quot;queue&quot;</code> 只能进入有界、 可取消的 Host 队列。远程主体发起的回合运行在会话模式与 Host <code>remoteMaxPermissionMode</code>（默认 <code>ask</code>，顺序 <code>ask</code> &lt; <code>accept-edits</code> &lt; <code>auto</code>）中 较低者之下，只有持有 <code>approver</code> 且策略允许时才可超出。上限只作用于经 Gateway 路由的主体：经 SSH 引导配对的桌面设备持有该 Host 的 <code>owner</code> 并豁免，因为 SSH 登录已授予该机器的 shell 访问，上限无可限制；Host 策略 <code>applyCeilingToPairedDevices</code> （默认关闭）可对配对设备重新施加上限。<code>allow-session</code> 只在 Host 策略允许远程会话授权时出现在 <code>allowedDecisions</code> 中。所有工具继续走 Host 的 workspace、permission、secret 和 approval 边界；不得暴露 <code>host.proxy</code>、 raw IPC 或任意命令执行。</p><h2 id="_4-网络、附件和多租户" tabindex="-1">4. 网络、附件和多租户 <a class="header-anchor" href="#_4-网络、附件和多租户" aria-label="Permalink to &quot;4. 网络、附件和多租户&quot;">​</a></h2><p>公网 HTTP、SSE 和 WebSocket 必须使用 TLS，保留的 gRPC 同样适用；绑定 loopback 并经 SSH 端口转发访问的 <code>pi-host</code>，在绑定地址与对端地址都是 loopback 且出示 有效设备 token 时可接受明文 <code>ws://</code>，SSH 通道提供机密性，与 ADR 0203 的 loopback 规则一致，任何非 loopback 绑定都要求 TLS；生产 Host link 必须双向认证。Origin、CORS、CSRF、cookie、WebSocket upgrade、token URL 和 SSRF 都要在边界处校验。附件使用大小、hash、MIME 和过期时间校验，不能接受 本地路径；经 Gateway 时 Gateway 只校验大小、分块中继到 Host，并在 <code>attachment/complete</code> 成功或过期后删除副本。首个部署为单租户，路由已携带 <code>tenantId</code>，跨租户隔离测试在多租户 harness 就绪后执行。</p><table tabindex="0"><thead><tr><th>Resource</th><th style="${ssrRenderStyle({ "text-align": "right" })}">Initial target</th></tr></thead><tbody><tr><td>Control requests per principal</td><td style="${ssrRenderStyle({ "text-align": "right" })}">120/minute</td></tr><tr><td>Turn starts per Session</td><td style="${ssrRenderStyle({ "text-align": "right" })}">20/minute</td></tr><tr><td>Queued turns per Session</td><td style="${ssrRenderStyle({ "text-align": "right" })}">8</td></tr><tr><td>Concurrent clients per Host</td><td style="${ssrRenderStyle({ "text-align": "right" })}">16</td></tr><tr><td>Concurrent subscriptions per connection</td><td style="${ssrRenderStyle({ "text-align": "right" })}">8</td></tr><tr><td>Request/event frame</td><td style="${ssrRenderStyle({ "text-align": "right" })}">1 MiB</td></tr><tr><td>Prompt payload</td><td style="${ssrRenderStyle({ "text-align": "right" })}">256 KiB</td></tr><tr><td>Attachment</td><td style="${ssrRenderStyle({ "text-align": "right" })}">50 MiB</td></tr><tr><td>In-flight attachment uploads per principal</td><td style="${ssrRenderStyle({ "text-align": "right" })}">4</td></tr><tr><td>Event send queue</td><td style="${ssrRenderStyle({ "text-align": "right" })}">4 MiB or 1,000 durable events</td></tr><tr><td>Open terminals per Session</td><td style="${ssrRenderStyle({ "text-align": "right" })}">2</td></tr></tbody></table><h2 id="_5-审计、撤销和验收" tabindex="-1">5. 审计、撤销和验收 <a class="header-anchor" href="#_5-审计、撤销和验收" aria-label="Permalink to &quot;5. 审计、撤销和验收&quot;">​</a></h2><p>待处理审批是 Host 状态：host-core 保有待处理权限表和计时器，Agent Host 通过 <code>permissions.pending</code> 读取并脱敏，晚接入的客户端能看到已打开的请求；首个有效 决定生效。远程会话的目录包含远端 Host 的工具以及配对桌面通过中继公布的工具，即用户配置的 MCP 服务器和不需要会话工作区的插件工具；中继工具在桌面自身的插件权限与确认规则下 执行，绝不在 Host 运行、绝不针对远程工作区，Host 的权限决定先于中继请求，Host 只传 Agent 的参数不传 secret，中继连接丢失则工具失败而回合继续。会话终端是以 <code>pi-host</code> 用户身份在 Host 机器上运行的 shell，工作目录为会话根，只有 SSH 配对的 owner 设备或 持有显式 <code>terminal</code> scope 的主体可以打开。provider secret 在任何方向都不经过 RACP。审批寿命是 Host 策略：本地默认仍是 120 秒后拒绝，有远程订阅者接入时默认 30 分钟（D375），Host 可在上限内调整，被阻塞的工具在本地或远程任一决定先到之前 一直等待，断线不会延长它。</p><p>审计记录包含 principal、tenant、Host、clientConnectionId、Session、Turn、 operation、准入模式与 <code>effectivePermissionMode</code>、授权决定、epoch 与序号范围， 但默认不记录 provider secret、原始 prompt、tool 参数、tool 输出或附件字节。 撤销用户、Host、连接、成员或附件后，新的 mutation 必须被拒绝，被撤销主体的 排队回合被取消，已完成回合不能因撤销而重放。</p><p>安全验收必须覆盖 TLS、角色矩阵、过期 token、重复 mutation、游标回放、慢客户端、 上传边界、日志脱敏、Host/Gateway 重启、远程权限上限、浏览器 cookie/header profile 与 URL token 拒绝（浏览器里程碑排期后适用）、中继审批请求只应答一次 （Gateway 里程碑排期后适用）、绑定 loopback 的 <code>pi-host</code> 只接受出示有效设备 token 的 loopback 对端且无 TLS 的非 loopback 绑定无法启动、配对 token 单次使用且 只经 SSH 通道传递、远程会话只暴露远端 Host 的工具目录与配对桌面公布的中继工具、中继工具绝不在 Host 执行且 Host 审批先于中继请求、会话终端只对 SSH 配对 owner 或持有 <code>terminal</code> scope 的主体开放，以及多租户 harness 就绪后的跨租户隔离。</p><h2 id="_6-修订记录" tabindex="-1">6. 修订记录 <a class="header-anchor" href="#_6-修订记录" aria-label="Permalink to &quot;6. 修订记录&quot;">​</a></h2><p>D374（2026-09-10）新增浏览器 cookie/header 认证 profile、两种可接受的身份源、 远程权限上限、本地决策词汇、远程审批寿命策略、Host link 与 Gateway 附件中继 规则、单租户优先条款以及验收门 13–15。</p><p>D375（2026-09-10）新增 SSH 引导配对、经 SSH 端口转发的 <code>pi-host</code> loopback 规则、 SSH 配对 owner 设备的上限豁免、远程工具目录与 provider 配置规则、验收门 16–18， 并把 Gateway 与 cookie profile 条款标记为属于不排期的里程碑。</p><p>D375 同日记录的设计决定把身份源定为 PI 账号服务，<code>pi-host</code> 从 GitHub Releases 下载， 补充中继与终端规则及验收门 19–20、远程审批 30 分钟默认寿命和 <code>applyCeilingToPairedDevices</code> 策略。</p><p>D385（2026-09-10）撤回第一方身份源：远程控制从结构上就是用户本地的，所有凭据由用户 自己的 Host 签发，Gateway 只能是用户自托管的中继。</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("zh-CN/spec/05-security/02-remote-control-security.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _02RemoteControlSecurity = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _02RemoteControlSecurity as default
};
