import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Renderer Plugin Settings Surfaces Design","description":"","frontmatter":{},"headers":[],"relativePath":"superpowers/specs/2026-09-15-renderer-plugin-settings-surfaces-design.md","filePath":"superpowers/specs/2026-09-15-renderer-plugin-settings-surfaces-design.md","lastUpdated":1789642367000}');
const _sfc_main = { name: "superpowers/specs/2026-09-15-renderer-plugin-settings-surfaces-design.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="renderer-plugin-settings-surfaces-design" tabindex="-1">Renderer Plugin Settings Surfaces Design <a class="header-anchor" href="#renderer-plugin-settings-surfaces-design" aria-label="Permalink to &quot;Renderer Plugin Settings Surfaces Design&quot;">​</a></h1><h2 id="problem" tabindex="-1">Problem <a class="header-anchor" href="#problem" aria-label="Permalink to &quot;Problem&quot;">​</a></h2><p><code>contributes.settingsDestinations</code> currently opens each plugin page in an Electron <code>WebContentsView</code>. On Windows and Linux that child native surface is always composited above the main renderer and has an opaque backing canvas. Even when the plugin document and its CSS are transparent, it cannot reveal a scenic backdrop painted by the main renderer. The result is the black rectangle visible behind Nexus Scenic Themes.</p><p>The rectangle cannot be corrected with plugin theme CSS: it is outside the document&#39;s compositor. Making the BrowserWindow transparent would risk native resize, drag, and Windows/Linux window-control behaviour, and duplicating the backdrop inside the child view would create an unsynchronised second canvas.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Settings destinations will become renderer-composited sandboxed iframe surfaces. The host renderer owns their location in the existing Settings layout; plugins supply only their page files and communicate through a host-controlled message bridge.</p><p>The existing native <code>PluginViewHost</code> remains the implementation for work-panel views. It is not used for Settings destinations.</p><h2 id="resource-model" tabindex="-1">Resource model <a class="header-anchor" href="#resource-model" aria-label="Permalink to &quot;Resource model&quot;">​</a></h2><p>The main process will register a secure, standard <code>plugin-settings:</code> protocol before <code>app.whenReady()</code>. A URL has the shape:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>plugin-settings://&lt;plugin-id&gt;/&lt;plugin-relative-entry-or-resource&gt;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>The handler resolves a resource only when all of the following hold:</p><ol><li><code>&lt;plugin-id&gt;</code> is currently loaded and has the granted <code>ui.settings</code> permission.</li><li>The requested path remains inside that plugin&#39;s package.</li><li>The package has a declared Settings destination whose resolved entry is in the same package.</li><li>The resource extension is one of the host&#39;s static page formats (HTML, CSS, JavaScript, image, font, or media) and has a known MIME type.</li></ol><p>The handler answers with <code>no-store</code>, <code>nosniff</code>, and a host-authored CSP. The CSP permits only same-origin scripts/styles/images/fonts plus declared <code>plugin-asset:</code> images; it denies network connection, framing of other pages, objects, workers, form submission, and navigation. This preserves the no-egress contract even though an iframe shares the app BrowserWindow&#39;s Electron session.</p><p>The HTML entry receives one host-authored bridge script before plugin scripts. It does not receive Node, Electron, the main renderer&#39;s preload bridge, or a same-origin relationship with the host page.</p><h2 id="bridge-model" tabindex="-1">Bridge model <a class="header-anchor" href="#bridge-model" aria-label="Permalink to &quot;Bridge model&quot;">​</a></h2><p>The embedded page receives <code>window.pluginBridge</code> with the same public shape as today&#39;s panel bridge:</p><div class="language-ts vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">window.pluginBridge.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">invoke</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(channel, payload</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">window.pluginBridge.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">on</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(event, handler)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>The bridge is implemented by an injected host script and <code>postMessage</code>, not by a preload. The host React component accepts messages only from its own iframe window, validates the message envelope and generated request id, and forwards the call through a new main-process IPC endpoint. Main validates the active plugin/destination, then calls the existing <code>PluginRuntime.invokePanelBridge</code>. Consequently the existing fixed channel, permission, auditing, and plugin identity checks remain authoritative. A plugin cannot invoke renderer IPC, touch the Settings React tree, choose another plugin/destination, or send a cross-plugin request.</p><p>Host appearance and locale arrive in a one-way initial bridge message. Existing sanctioned bridge events, including <code>appearance:changed</code>, are forwarded only to the matching active destination.</p><h2 id="lifecycle-and-layout" tabindex="-1">Lifecycle and layout <a class="header-anchor" href="#lifecycle-and-layout" aria-label="Permalink to &quot;Lifecycle and layout&quot;">​</a></h2><p><code>PluginSettingsDestination</code> renders a single iframe in the existing <code>.settings-content-inner</code> flow. It has no native bounds IPC and cannot cover the titlebar, Windows/Linux controls, resize edges, sidebar, or Settings rail. The iframe expands with its normal Settings content region; the outer renderer keeps scroll ownership and normal Settings geometry.</p><p>Core navigation, plugin disable/uninstall/reload, an absent entry, and a failed load remove the iframe and return the user to General. No <code>WebContentsView</code> is created, cached, or attached for a Settings destination. The Settings page therefore stays on the one renderer compositing plane as <code>.app-scenic-backdrop</code>.</p><h2 id="security-and-compatibility" tabindex="-1">Security and compatibility <a class="header-anchor" href="#security-and-compatibility" aria-label="Permalink to &quot;Security and compatibility&quot;">​</a></h2><ul><li><code>sandbox</code> is present on every Settings iframe. It includes only <code>allow-scripts</code>; it deliberately excludes <code>allow-same-origin</code>, popups, downloads, top navigation, forms, and pointer-lock.</li><li>Plugin destination pages remain package-local. Remote requests, dynamic resource URLs, and arbitrary CSS/DOM injection remain unavailable.</li><li>Plugin panels and work-panel views retain their isolated <code>WebContentsView</code> architecture; their geometry and bridge contracts do not change.</li><li>Ordinary Settings and all non-plugin themes retain their existing DOM, layout, native control handling, and styling.</li><li>The host does not make the BrowserWindow transparent and does not add a second scenic background to a plugin surface.</li></ul><h2 id="verification" tabindex="-1">Verification <a class="header-anchor" href="#verification" aria-label="Permalink to &quot;Verification&quot;">​</a></h2><p>Contract tests will demonstrate protocol registration and strict CSP, sandboxed iframe ownership, absence of Settings <code>WebContentsView</code> calls, message source and destination validation, lifecycle cleanup, and protected native geometry. Focused build/type checks will accompany existing plugin-theme tests. Manual verification will cover switching between core Settings and Nexus Scenic Themes while each scenic theme is active, plus native title controls on Windows/Linux.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("superpowers/specs/2026-09-15-renderer-plugin-settings-surfaces-design.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _20260915RendererPluginSettingsSurfacesDesign = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _20260915RendererPluginSettingsSurfacesDesign as default
};
