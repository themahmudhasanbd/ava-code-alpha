import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0179: Import model configuration from local agent stores","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0179-import-model-configuration.md","filePath":"adr/0179-import-model-configuration.md","lastUpdated":1788882099000}');
const _sfc_main = { name: "adr/0179-import-model-configuration.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0179-import-model-configuration-from-local-agent-stores" tabindex="-1">ADR 0179: Import model configuration from local agent stores <a class="header-anchor" href="#adr-0179-import-model-configuration-from-local-agent-stores" aria-label="Permalink to &quot;ADR 0179: Import model configuration from local agent stores&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-08</li><li>Deciders: PI-Desktop core</li><li>Related: D007, D342, ADR 0012, ADR 0188, <code>04-ux/06-settings-ia.md</code>, <code>04-ux/08-component-spec.md</code> §18.5, <code>03-runtime/01-ipc-protocol.md</code>, <code>03-runtime/11-provider-model-system.md</code></li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Settings → Import already scans Claude Code, Codex, OpenCode, and Pi session stores. The same tools keep provider URLs, model ids, and often API keys in well-known files. Users who switch to PI-Desktop otherwise retype those endpoints on Settings → Models.</p><p>D007 forbids auto-import of <code>~/.pi</code> so PI-Desktop owns <code>~/.pi-desktop</code>. That must stay true for model configuration: a scan is an explicit action, and nothing is written until Import selected.</p><p>Secrets cannot cross the renderer. Session import already keeps <code>filePath</code> in the main-process scan cache; model import must keep keys the same way.</p><p>OAuth/subscription grants from those tools (Codex ChatGPT login, Claude subscription, OpenCode <code>type: oauth</code>) are not PI-Desktop vendor-account credentials. Copying refresh tokens would be the wrong security boundary.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><p><strong>Settings → Import</strong> gains a second card, Model configuration, with its own Scan / selection / Import selected. Session import is unchanged.</p></li><li><p><strong>Sources</strong> (same family as session import):</p><ul><li>Claude Code: <code>~/.claude/settings.json</code> plus <code>settings.local.json</code> overlay (<code>env.ANTHROPIC_*</code>, <code>model</code>)</li><li>Codex: <code>~/.codex/config.toml</code> <code>[model_providers.&lt;id&gt;]</code></li><li>OpenCode: <code>~/.config/opencode/opencode.json</code> <code>provider</code> map plus <code>~/.local/share/opencode/auth.json</code> API keys</li><li>Pi: <code>~/.pi/agent/models.json</code> (fallback <code>~/.pi/models.json</code>)</li><li>CC Switch: <code>~/.cc-switch/cc-switch.db</code> <code>providers</code> table (legacy <code>config.json</code>). Each row&#39;s <code>settings_config</code> is converted by app type. Empty official seeds and OAuth-only rows are omitted. A live Claude / Codex / OpenCode / Pi file that matches a CC Switch endpoint and credential is not listed twice; a different credential remains visible.</li></ul></li><li><p><strong>IPC</strong> (Electron only, no host protocol bump): <code>pi-desktop/modelConfig/importScan</code> returns public drafts (<code>source</code>, <code>externalId</code>, <code>name</code>, <code>baseUrl</code>, <code>apiStyle</code>, <code>modelIds</code>, <code>hasSecret</code>). <code>importRun</code> looks the selection up in the latest scan cache and calls host <code>providers.create</code>.</p></li><li><p><strong>Secrets.</strong> A stored API key, <code>env:</code> / <code>env_key</code> resolution, or <code>Authorization: Bearer</code> header is copied into the host secret store. Placeholder values (<code>YOUR_API_KEY</code>, <code>\${VAR}</code>) are treated as missing. OAuth auth.json entries and Codex <code>requires_openai_auth</code> tables without a key are omitted or imported without a secret.</p></li><li><p><strong>Idempotence.</strong> A candidate whose normalized base URL, API style, and credential match an existing provider is skipped. Different credentials remain independent provider rows; the credential comparison stays in Electron main and never reaches the renderer. Named presets may set <code>vendorKey</code>; a custom URL stays <code>custom</code>. ADR 0188 amends this rule.</p></li><li><p><strong>Default model.</strong> If <code>settings.defaultProviderId</code> is empty after the first successful create in that run, that provider and its first model become the global default. An existing default is never overwritten.</p></li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Switching from another local agent can bring both transcripts and the models those transcripts used, without pasting keys into Settings.</li><li>PI-Desktop still does not silently ingest <code>~/.pi</code>.</li><li>Users with only a ChatGPT/Claude subscription still sign in through Vendor accounts; import cannot impersonate that grant.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><ul><li>Auto-import on first launch: rejected by D007.</li><li>Merge keys into an existing same-URL provider: would overwrite a working row. Skip is safer; the user can edit the Models page.</li><li>Renderer-side <code>providers.create</code> with the key in the IPC payload: the scan cache already exists for sessions; keep secrets off the renderer.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0179-import-model-configuration.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0179ImportModelConfiguration = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0179ImportModelConfiguration as default
};
