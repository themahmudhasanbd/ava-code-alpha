import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0094: Admit one desktop instance per data directory","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0094-single-instance-per-data-directory.md","filePath":"adr/0094-single-instance-per-data-directory.md","lastUpdated":1789837117000}');
const _sfc_main = { name: "adr/0094-single-instance-per-data-directory.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0094-admit-one-desktop-instance-per-data-directory" tabindex="-1">ADR 0094: Admit one desktop instance per data directory <a class="header-anchor" href="#adr-0094-admit-one-desktop-instance-per-data-directory" aria-label="Permalink to &quot;ADR 0094: Admit one desktop instance per data directory&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-08-18</li><li>Deciders: PI-Desktop core</li><li>Related: D236, D002, D216, ADR 0078, ADR 0090</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Nothing stopped a second PI-Desktop process from starting. Launching the app again while it was already running — a double click on Windows or Linux, a <code>open -n</code> or a packaged app started next to a development host on macOS, or a tray-resident session the user assumed had exited — booted a complete second copy: another host-core over the same <code>pi.sqlite</code>, another persistence outbox and log tree in the same data directory, another agent sidecar, another tray icon, another global launcher shortcut registration, and another updater.</p><p>host-core owns SQLite exclusively (D002) precisely so there is a single writer. A second app process defeats that: the two shells show divergent session lists over one database, the second registration of the launcher chord silently loses, and quitting one of them tears down state the other is still using. The user&#39;s mental model is one desktop app; the process model was the only thing that disagreed.</p><p>The data directory is what cannot be shared, but Electron&#39;s single-instance lock is scoped to <code>userData</code>, not to <code>PI_DESKTOP_DATA_DIR</code>. Runs that point at their own data directory — the E2E harnesses, the capture rig, a deliberate side-by-side profile — share no state with the default installation and must stay launchable while one is running.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>Electron main takes <code>app.requestSingleInstanceLock()</code> during module evaluation, after <code>app.setName</code> (the lock file lives under the name-derived <code>userData</code> path) and before anything reads or writes the data directory.</li><li>A launch that does not get the lock calls <code>app.quit()</code> immediately and boots nothing. The readiness handler and the shutdown handler both return early for it, so it never creates a window, a tray, a child process, or a log line in the running instance&#39;s data directory.</li><li>The instance that holds the lock handles <code>second-instance</code> by restoring and focusing its main window through the same path as the tray&#39;s Show action, which recreates a window that was closed or hidden into the tray.</li><li>The lock is requested only when <code>PI_DESKTOP_DATA_DIR</code> is unset. A run given its own data directory keeps the current start-anytime behavior.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>One installation is one process: one host-core, one SQLite writer, one outbox, one tray, one launcher shortcut binding, one updater.</li><li>Relaunching the app is a reliable way back to a tray-hidden or closed window, alongside the tray, the Dock, and <code>did-become-active</code> (ADR 0078, ADR 0086).</li><li>E2E harnesses, the capture rig, and side-by-side profiles are unaffected because they set <code>PI_DESKTOP_DATA_DIR</code>.</li><li>Two runs that are pointed at the <em>same</em> explicit data directory are still admitted. That combination is a deliberate act, not an accidental relaunch, and scoping the lock to it would mean relocating <code>userData</code> under the data directory and discarding renderer-local state for every profile user.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><h3 id="relocate-userdata-under-pi-desktop-data-dir" tabindex="-1">Relocate <code>userData</code> under <code>PI_DESKTOP_DATA_DIR</code> <a class="header-anchor" href="#relocate-userdata-under-pi-desktop-data-dir" aria-label="Permalink to &quot;Relocate \`userData\` under \`PI_DESKTOP_DATA_DIR\`&quot;">​</a></h3><p>This would scope the lock to the resource that actually cannot be shared. Rejected because <code>userData</code> also holds renderer <code>localStorage</code> (retained project tabs and sidebar presentation, D093), plugin panel partitions, and browser pane cookies; moving it would silently discard that state for existing profile users to close a rare edge case.</p><h3 id="keep-a-lock-file-in-the-data-directory" tabindex="-1">Keep a lock file in the data directory <a class="header-anchor" href="#keep-a-lock-file-in-the-data-directory" aria-label="Permalink to &quot;Keep a lock file in the data directory&quot;">​</a></h3><p>Rejected because it re-implements what Electron already provides and still has no channel to raise the running window; a stale lock file after a crash would also block the app with no recovery path.</p><h3 id="let-the-second-launch-focus-the-first-and-stay-alive" tabindex="-1">Let the second launch focus the first and stay alive <a class="header-anchor" href="#let-the-second-launch-focus-the-first-and-stay-alive" aria-label="Permalink to &quot;Let the second launch focus the first and stay alive&quot;">​</a></h3><p>Rejected because a resident duplicate keeps a second Electron process, tray, and shortcut registration for no purpose. Quitting immediately is the same visible result with none of the cost.</p><h3 id="do-nothing-and-document-the-hazard" tabindex="-1">Do nothing and document the hazard <a class="header-anchor" href="#do-nothing-and-document-the-hazard" aria-label="Permalink to &quot;Do nothing and document the hazard&quot;">​</a></h3><p>Rejected because the failure is silent data divergence over a single-writer database, and the user has no signal that two shells are open on one workspace.</p><h2 id="amendment-d599" tabindex="-1">Amendment (D599) <a class="header-anchor" href="#amendment-d599" aria-label="Permalink to &quot;Amendment (D599)&quot;">​</a></h2><p>The alternatives below still stand: the lock is not scoped to <code>PI_DESKTOP_DATA_DIR</code>, and <code>userData</code> is not relocated under it.</p><p>What changed is that a development build is no longer the same installation as the packaged app. It takes <code>PI-Desktop Dev</code> in the OS application-data root and <code>~/.pi-desktop-dev</code>, so <code>pnpm dev</code> starts while the packaged app holds its lock and the two never share <code>pi.sqlite</code>, the outbox, or the log tree. An explicit <code>--user-data-dir</code> still wins, because the E2E harnesses point a build at a throwaway profile with it. Only the development side moved: a shipped installation keeps <code>PI-Desktop</code> and <code>~/.pi-desktop</code>, so no existing profile is relocated. See D599.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0094-single-instance-per-data-directory.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0094SingleInstancePerDataDirectory = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0094SingleInstancePerDataDirectory as default
};
