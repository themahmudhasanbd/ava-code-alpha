import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0080: Prewarm the global plugin launcher after boot","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0080-prewarm-global-plugin-launcher.md","filePath":"adr/0080-prewarm-global-plugin-launcher.md","lastUpdated":1786603479000}');
const _sfc_main = { name: "adr/0080-prewarm-global-plugin-launcher.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0080-prewarm-the-global-plugin-launcher-after-boot" tabindex="-1">ADR 0080: Prewarm the global plugin launcher after boot <a class="header-anchor" href="#adr-0080-prewarm-the-global-plugin-launcher-after-boot" aria-label="Permalink to &quot;ADR 0080: Prewarm the global plugin launcher after boot&quot;">​</a></h1><ul><li><strong>Status:</strong> Accepted</li><li><strong>Date:</strong> 2026-08-13</li><li><strong>Related:</strong> D211 · D217 · ADR 0072 · E2E-120</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The global plugin launcher was created lazily on the first Option/Alt+Space invocation. That path had to allocate a second Electron <code>BrowserWindow</code>, start its sandboxed renderer, load the shared renderer bundle, mount React, and only then reveal the launcher. The first invocation therefore felt substantially slower than later invocations even though the window was retained after use.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>As soon as Electron is ready, before backend/plugin boot completes, Electron starts creating and loading the launcher window in the background while keeping it hidden. This work runs in parallel with primary startup. Shortcut invocations continue to set the current display bounds, show, and focus the same retained window.</p><p>Window creation is represented by one shared in-flight promise. A shortcut received while warm-up is still loading joins that promise, preventing a blank window, a lost shown event, or duplicate renderer creation. A failed warm-up is logged, destroys the incomplete window, clears the promise, and leaves the normal shortcut path able to retry creation.</p><p>The launcher still refreshes the plugin catalog whenever it is shown, so preloading changes presentation latency without making plugin availability stale or changing IPC, host RPC, permissions, protocol v9, or storage schema v11.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>The first post-boot shortcut no longer pays BrowserWindow and renderer load latency on its visible path; warm-up starts early enough to overlap backend startup.</li><li>One hidden sandboxed renderer remains resident after boot, trading a bounded memory cost for consistent launch latency.</li><li>Application boot is not blocked on launcher warm-up, and warm-up failure does not prevent a later shortcut from retrying.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li>Keep first-use lazy creation: preserves the smallest idle footprint but keeps the reported first-launch delay.</li><li>Await warm-up as part of application boot: guarantees readiness before the app is marked booted but lengthens the primary window&#39;s critical startup path.</li><li>Reuse the main renderer: avoids a second renderer but cannot provide the independent system-wide utility window while PI-Desktop is unfocused.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0080-prewarm-global-plugin-launcher.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0080PrewarmGlobalPluginLauncher = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0080PrewarmGlobalPluginLauncher as default
};
