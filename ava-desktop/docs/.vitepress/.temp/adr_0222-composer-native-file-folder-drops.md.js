import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0222: Native File and Folder Drops in the Composer","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0222-composer-native-file-folder-drops.md","filePath":"adr/0222-composer-native-file-folder-drops.md","lastUpdated":1789111350000}');
const _sfc_main = { name: "adr/0222-composer-native-file-folder-drops.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0222-native-file-and-folder-drops-in-the-composer" tabindex="-1">ADR 0222: Native File and Folder Drops in the Composer <a class="header-anchor" href="#adr-0222-native-file-and-folder-drops-in-the-composer" aria-label="Permalink to &quot;ADR 0222: Native File and Folder Drops in the Composer&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-11</li><li>Deciders: PI-Desktop core</li><li>Amends: ADR 0101 (drag/drop scope)</li><li>Related: D397, ADR 0059, ADR 0070</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The Composer already handles clipboard files and picker-selected files through the session-scoped scratch flow. Native file-system drag-and-drop was still left to the browser default, which could open or navigate away from the app. Folders need different treatment from regular files: showing the source path is useful for <code>@</code> completion, but traversing or copying a folder would be unbounded and would violate scratch ownership.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>The Composer shell accepts native file-system drops and prevents the browser default for transfers containing file items. The whole shell gets an accent outline during drag-over; the outline does not change layout.</li><li>The Electron preload exposes only <code>webUtils.getPathForFile</code> for the user-originated dropped <code>File</code> object. The renderer uses the existing <code>webkitGetAsEntry()</code> signal to classify folders and keeps the OS item order.</li><li>Regular dropped files are transferred as bounded bytes through the existing <code>composer/pasteFiles</code> bridge. They are written below the active session&#39;s scratch directory and rendered as the existing removable leaf-name chips.</li><li>Dropped folders are never read, traversed, or copied. Their complete native path is inserted at the caret as the literal <code>@&lt;path&gt;/</code> directory form. Mixed file/folder drops preserve order, surrounding draft text, focus, and the caret across the asynchronous file save.</li><li>This is a renderer/preload interaction change only. It adds no host RPC method, protocol message, workspace write, or durable storage schema.</li></ol><h2 id="security-and-boundary-notes" tabindex="-1">Security and boundary notes <a class="header-anchor" href="#security-and-boundary-notes" aria-label="Permalink to &quot;Security and boundary notes&quot;">​</a></h2><ul><li>The renderer receives a source path only for a native user drop and uses it as visible prompt text; it does not gain arbitrary filesystem read or write access.</li><li>Folder drops do not enumerate entries or transfer bytes, so a large folder cannot turn into an unbounded scratch operation.</li><li>Regular files retain the existing main-process size, name, session, and scratch-root validation from ADR 0059.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Traverse and copy dropped folders:</strong> rejected because it is unbounded and would make a path-display gesture mutate session scratch contents.</li><li><strong>Insert only the folder leaf name:</strong> rejected because it loses the path needed to disambiguate and continue <code>@</code> completion.</li><li><strong>Add a new main-process folder IPC:</strong> rejected because the preload path bridge supplies the source path for this user-originated gesture without widening filesystem capabilities or adding a protocol surface.</li><li><strong>Keep the browser default:</strong> rejected because dropping a file or folder could navigate or open the dropped resource instead of editing the prompt.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Users can drag files and folders directly into the chat input.</li><li>Files have the same bounded scratch lifecycle and attachment metadata as pasted files; folders remain visible references without being copied.</li><li>The Composer&#39;s existing draft and chip persistence logic remains the single source of truth for mixed asynchronous drops.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0222-composer-native-file-folder-drops.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0222ComposerNativeFileFolderDrops = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0222ComposerNativeFileFolderDrops as default
};
