import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0278: Canonical Application ID net.aiuo.pi-desktop","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0278-canonical-application-id.md","filePath":"adr/0278-canonical-application-id.md","lastUpdated":1789648214000}');
const _sfc_main = { name: "adr/0278-canonical-application-id.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0278-canonical-application-id-net-aiuo-pi-desktop" tabindex="-1">ADR 0278: Canonical Application ID <code>net.aiuo.pi-desktop</code> <a class="header-anchor" href="#adr-0278-canonical-application-id-net-aiuo-pi-desktop" aria-label="Permalink to &quot;ADR 0278: Canonical Application ID \`net.aiuo.pi-desktop\`&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-17</li><li>Deciders: PI-Desktop packaging maintainers</li><li>Amends: D141, D371, ADR 0204</li><li>Related: issue #524 · E2E-196b · <a href="./../spec/01-product/01-product-scope">01-product/01-product-scope</a> · <a href="./../spec/06-delivery/06-release-runbook">06-delivery/06-release-runbook</a></li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The product advertised <code>com.pi-desktop.app</code> as the application ID, but the owner domain is <code>net.aiuo.pi-desktop</code>. Default unsigned macOS packs also left Electron&#39;s adhoc signature on <code>PI-Desktop.app</code> (<code>Identifier=Electron</code>, <code>Info.plist=not bound</code>). <code>usernotificationsd</code> then required the private <code>com.apple.private.usernotifications.bundle-identifiers</code> entitlement and refused every request for the product bundle ID (issue #524). Adhoc signing itself is not the defect: the identifier must equal <code>CFBundleIdentifier</code>.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>The canonical application ID is <strong><code>net.aiuo.pi-desktop</code></strong>. It is <code>APP_ID</code>, electron-builder <code>appId</code>, macOS <code>CFBundleIdentifier</code>, the unsigned-helper expected bundle ID, and the Windows AppUserModelID. Development macOS hosts use <code>net.aiuo.pi-desktop.dev</code>.</li><li>NSIS/AppUserModelID follow the same ID. Existing <code>com.pi-desktop.app</code> installs are a new identity after this cut.</li><li>Unsigned GitHub macOS packs do <strong>not</strong> re-sign the outer bundle. An <code>afterPack</code> adhoc <code>codesign</code> fails because nested Electron helpers are still unsigned (<code>code object is not signed at all</code>). Issue #524 remains open on the default unsigned lane until a later signed or helper-safe pack path.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Packaged unsigned macOS artifacts keep Electron&#39;s outer identifier.</li><li>Signed/notarized builds use Developer ID with the product bundle ID.</li><li>Windows upgrades from a <code>com.pi-desktop.app</code> NSIS install are a new product identity; users may see a parallel shortcut until the old install is removed.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li>Keep <code>com.pi-desktop.app</code> and only re-sign: rejected because the owner domain is <code>net.aiuo.pi-desktop</code>.</li><li>Change only the macOS bundle ID: rejected because D141 requires one ID across runtime and packaging.</li><li>Adhoc-sign in <code>afterPack</code>/<code>afterSign</code>: rejected; nested helpers are not signed yet and the pack fails.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0278-canonical-application-id.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0278CanonicalApplicationId = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0278CanonicalApplicationId as default
};
