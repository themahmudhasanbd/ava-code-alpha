import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"09. Logging and Observability","description":"","frontmatter":{},"headers":[],"relativePath":"spec/03-runtime/09-logging-and-observability.md","filePath":"spec/03-runtime/09-logging-and-observability.md","lastUpdated":1789891299000}');
const _sfc_main = { name: "spec/03-runtime/09-logging-and-observability.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="_09-logging-and-observability" tabindex="-1">09. Logging and Observability <a class="header-anchor" href="#_09-logging-and-observability" aria-label="Permalink to &quot;09. Logging and Observability&quot;">​</a></h1><h2 id="_1-goals" tabindex="-1">1. Goals <a class="header-anchor" href="#_1-goals" aria-label="Permalink to &quot;1. Goals&quot;">​</a></h2><ol><li>Diagnose failures quickly.</li><li>Audit sensitive tool and plugin actions.</li><li>Avoid leaking secrets.</li><li>Keep the MVP local-first and quiet during normal operation.</li></ol><h2 id="_2-log-levels" tabindex="-1">2. Log levels <a class="header-anchor" href="#_2-log-levels" aria-label="Permalink to &quot;2. Log levels&quot;">​</a></h2><ul><li><code>debug</code></li><li><code>info</code></li><li><code>warn</code></li><li><code>error</code></li></ul><p>Default runtime level:</p><ul><li>development: <code>debug</code></li><li>release: <code>info</code></li></ul><h2 id="_3-channels" tabindex="-1">3. Channels <a class="header-anchor" href="#_3-channels" aria-label="Permalink to &quot;3. Channels&quot;">​</a></h2><table tabindex="0"><thead><tr><th>channel</th><th>content</th><th>location</th></tr></thead><tbody><tr><td>app</td><td>boot, IPC, window, process supervision</td><td><code>~/.pi-desktop/logs/app/&lt;category&gt;.log</code></td></tr><tr><td>host</td><td>Rust host-core events (stderr capture)</td><td><code>~/.pi-desktop/logs/host/&lt;category&gt;.log</code></td></tr><tr><td>agent</td><td>pi sidecar events (stderr capture)</td><td><code>~/.pi-desktop/logs/agent/&lt;category&gt;.log</code></td></tr><tr><td>audit</td><td>sensitive permission, tool, and plugin actions</td><td>host-core SQLite <code>audit_log</code> table</td></tr><tr><td>plugin</td><td>per-plugin logs</td><td><code>~/.pi-desktop/plugins/logs/&lt;id&gt;.log</code></td></tr></tbody></table><p>The <code>~/.pi-desktop</code> paths above are the packaged installation&#39;s. A development build writes the same tree under <code>~/.pi-desktop-dev</code>, and <code>PI_DESKTOP_DATA_DIR</code> replaces either root (D599). <code>app</code>, <code>host</code>, and <code>agent</code> are NDJSON files written by the Electron main <code>Logger</code> (<code>apps/desktop/electron/main/logger.ts</code>). Host and agent stderr lines are wrapped into records on their channel. The audit channel is stored in SQLite, owned exclusively by host-core, so it remains queryable independently of rotating diagnostic files.</p><h3 id="_3a-category-routing" tabindex="-1">3a. Category routing <a class="header-anchor" href="#_3a-category-routing" aria-label="Permalink to &quot;3a. Category routing&quot;">​</a></h3><p>The three process channels are directories, not aggregate files. Main-process call sites choose a category. Host and agent stderr uses marker-based classification; unknown child output uses <code>runtime</code>.</p><p>The application categories are:</p><ul><li><p><code>lifecycle</code> — boot, shutdown, and application supervision</p></li><li><p><code>session</code> — prompts, turns, session lifecycle, and compaction</p></li><li><p><code>tool</code> — tool execution outcomes and interruptions</p></li><li><p><code>permission</code> — permission requests and decisions</p></li><li><p><code>plugin</code> — plugin loading, services, and plugin tool execution</p></li><li><p><code>provider</code> — provider/model discovery, retries, and cache failures</p></li><li><p><code>persistence</code> — transcript and outbox persistence failures</p></li><li><p><code>updater</code> — updater diagnostics and errors</p></li><li><p><code>diagnostics</code> — blocked navigation, menu, template, and outbound-fetch diagnostics. The skill market&#39;s two channels record one <code>skillMarket.sourceFailed</code> / <code>skillMarket.documentFailed</code> record per source or document that produced nothing, with <code>source</code>, <code>host</code>, <code>kind</code>, <code>address</code> and — for a guard refusal — <code>reason</code>, <code>addressKind</code> and <code>route</code> in <code>data</code>. <code>kind</code> is <code>policy</code> when the guard judged the target&#39;s own address, <code>fake-ip</code> when it judged a placeholder the local proxy invented for the name (Clash&#39;s <code>198.18.0.0/15</code>), <code>unresolved</code> when the local resolver returned no answer, and <code>network</code> otherwise; <code>code</code> is <code>NETWORK_POLICY_BLOCKED</code> for the first two and <code>NETWORK_RESOLVE_FAILED</code> for the third, so one log line separates &quot;the address is not public&quot; from &quot;a proxy answered with a fake-IP&quot; from &quot;the resolver answered nothing&quot;. <code>reason</code> names the guard&#39;s own branch (<code>url-syntax</code>, <code>resolve-failed</code>, <code>non-public-address</code>, <code>redirect-limit</code>), <code>addressKind</code> the class of the refused address (<code>benchmark</code> for a TUN fake-IP, <code>private</code> for RFC1918), and <code>route</code> the route that address was judged on (<code>proxied</code>, <code>direct</code>, or <code>unknown</code> when the transport reported no readable route), so a fake-IP refusal on a direct route reads apart from one on a route nobody could read (ADR 0272). The record carries the host name, the address it resolved to, that class and that route — never the URL, its path, query or credentials — because a catalog source URL is user-supplied and the refused host and address are the whole diagnostic value (issue #419). After a Chromium-process crash, the next launch writes one <code>crashDumpsFound</code> record (<code>error</code> if any new dump is the browser/main process, <code>warn</code> otherwise) with <code>count</code>, <code>total</code>, <code>byProcessType</code>, <code>directory</code>, and <code>newestMtimeMs</code>. A recovered renderer crash is therefore not reported as the previous app run aborting. A scan failure is one <code>crashDumpReportFailed</code> warn and never blocks the first window (D602).</p></li><li><p><code>runtime</code> — host/sidecar lifecycle, uncategorized child output, and main-process <code>uncaughtException</code> / <code>unhandledRejection</code> records</p></li></ul><p>There is no dedicated <code>timing</code> category. Timing files from older application runs are left untouched, but current code does not create or append to them. Flat legacy <code>app.log</code>, <code>host.log</code>, and <code>agent.log</code> files are also left untouched.</p><h2 id="_4-required-fields" tabindex="-1">4. Required fields <a class="header-anchor" href="#_4-required-fields" aria-label="Permalink to &quot;4. Required fields&quot;">​</a></h2><p>Every structured log line should include:</p><div class="language-ts vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">type</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> LogRecord</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  ts</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  level</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;debug&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;info&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;warn&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;error&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  channel</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  category</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  event</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">              // stable dot-separated machine-readable name</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  message</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  traceId</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  requestId</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  sessionId</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  turnId</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  toolCallId</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  parentToolCallId</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  agentName</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  pluginId</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  executionId</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  code</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  data</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> unknown</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br><span class="line-number">19</span><br></div></div><p>Format: NDJSON files.</p><p><code>event</code> is the stable query key; <code>message</code> is a short human-readable summary. Correlation fields are emitted at the top level so a failed tool, its permission request, and its parent/child agent can be joined without parsing free-form text. <code>data</code> is diagnostic metadata, not a transcript or command output: it is redacted, depth/collection bounded, and capped at 8 KiB per record.</p><h2 id="_5-what-must-be-logged" tabindex="-1">5. What must be logged <a class="header-anchor" href="#_5-what-must-be-logged" aria-label="Permalink to &quot;5. What must be logged&quot;">​</a></h2><h3 id="always" tabindex="-1">Always <a class="header-anchor" href="#always" aria-label="Permalink to &quot;Always&quot;">​</a></h3><ul><li>app boot and shutdown;</li><li>host/agent spawn, handshake, and unexpected exit;</li><li>session create/delete;</li><li>prompt accepted/aborted;</li><li>tool completion/failure/interruption and permission request/decision/timeout;</li><li>Plan artifact creation, approval, expiry, rejection, execution transition, and startup interruption;</li><li>shell identity, timeout, abort, and process-tree shutdown;</li><li>plugin enable/disable/load/error;</li><li>tool admission rejection, queue/resource exhaustion, and updater errors.</li></ul><p>These records should identify the relevant session, turn, tool call, plugin, or stable error code when available. A normal tool call emits one completion or failure record after <code>tool_end</code>; an unexpected sidecar exit emits one interruption record for each still-active tool. The sidecar protocol still uses <code>tool_start</code> and <code>tool_end</code> unchanged for execution and transcript correctness. Normal successful operations should not emit per-phase or per-request latency records.</p><h3 id="never" tabindex="-1">Never <a class="header-anchor" href="#never" aria-label="Permalink to &quot;Never&quot;">​</a></h3><ul><li>API keys or raw secrets;</li><li>full secure-storage payloads; or</li><li>unnecessary full file contents for large reads in audit records.</li></ul><h2 id="_6-redaction-rules" tabindex="-1">6. Redaction rules <a class="header-anchor" href="#_6-redaction-rules" aria-label="Permalink to &quot;6. Redaction rules&quot;">​</a></h2><ol><li>Keys matching token, secret, password, API key, authorization, cookie, credential, private key, or client secret are redacted.</li><li>Bearer/Basic credentials, URL user-info, and common provider token formats are redacted even when they occur inside a string.</li><li>Home, application-data, and log-directory prefixes are normalized to placeholders; raw local paths are not retained in diagnostic records.</li><li>Arbitrary strings are bounded. Structured data is bounded by depth and collection size and then capped at 8 KiB per record, including host-core audit payloads after shaping.</li><li>Tool arguments and results are never copied wholesale into normal logs. Tool results retain only safe metadata such as outcome, error code, duration, field names, content-block count, and stdout/stderr sizes. Long command output is counted or truncated in audit records.</li><li>Child stderr is stored as a bounded, ANSI-free <code>data.output</code> field under a stable <code>child.process.stderr</code> event; it is not used as the record message.</li></ol><h2 id="_7-trace-correlation" tabindex="-1">7. Trace correlation <a class="header-anchor" href="#_7-trace-correlation" aria-label="Permalink to &quot;7. Trace correlation&quot;">​</a></h2><p>Use one <code>traceId</code> per user-visible action when possible:</p><ul><li>prompt → <code>turnId</code>;</li><li>tool call → <code>toolCallId</code>; and</li><li>permission flow → <code>toolCallId</code> / <code>requestId</code>.</li></ul><p>Renderer, Electron, host, and agent should propagate these identifiers.</p><h2 id="_7a-functional-duration-metadata" tabindex="-1">7a. Functional duration metadata <a class="header-anchor" href="#_7a-functional-duration-metadata" aria-label="Permalink to &quot;7a. Functional duration metadata&quot;">​</a></h2><p>The application still preserves bounded duration metadata needed by product features: <code>ToolsExecuteResult.duration_ms</code>, transcript <code>toolDurationMs</code> and <code>responseDurationMs</code>, delegation start/completion timestamps, and bounded provider diagnostics. These values support the transcript, context inspector, throughput display, and audit records; they do not create timing log lines.</p><p>Host-core audit rows may retain the existing permission and execution timing fields for forensic inspection. They are structured audit data, not a separate <code>timing.log</code> stream.</p><h2 id="_8-user-facing-diagnostics" tabindex="-1">8. User-facing diagnostics <a class="header-anchor" href="#_8-user-facing-diagnostics" aria-label="Permalink to &quot;8. User-facing diagnostics&quot;">​</a></h2><p>MVP provides:</p><ol><li>in-app error text with a stable code;</li><li>an “Open logs folder” command; and</li><li>optional copy of error details (code and <code>traceId</code>).</li></ol><p>There is no remote telemetry pipeline or cloud crash analytics in the MVP.</p><h2 id="_9-retention" tabindex="-1">9. Retention <a class="header-anchor" href="#_9-retention" aria-label="Permalink to &quot;9. Retention&quot;">​</a></h2><ul><li>app/host/agent category logs: rotate each category file at 5 MB and keep two rotated files beside it (<code>&lt;category&gt;.1.log</code>, <code>&lt;category&gt;.2.log</code>);</li><li>audit log (SQLite): retained with the database and pruned according to the host retention policy;</li><li>Crashpad minidumps in <code>&lt;data_dir&gt;/crash-dumps</code> are not rotated by the logger; they remain until the user deletes them; and</li><li>rotation and logging failures must never fail the caller.</li></ul><p>Session transcripts are user data and are not deleted by log rotation.</p><h2 id="_10-acceptance" tabindex="-1">10. Acceptance <a class="header-anchor" href="#_10-acceptance" aria-label="Permalink to &quot;10. Acceptance&quot;">​</a></h2><ol><li>Failed and interrupted tool calls can be traced by <code>toolCallId</code> across key logs, with one outcome record per normal execution.</li><li>Secrets never appear in log files during normal flows.</li><li>The logs folder can be opened from the app/command palette.</li><li>Boot, host, sidecar, plugin, updater, and renderer paths emit only their lifecycle, state-change, error, or security-relevant records; no timing category files are created for normal operation.</li><li>Logging or console mirroring never crashes the main process when stdout is a broken pipe.</li><li>Host restart, permission failure, shell timeout, and process abort remain diagnosable from stable lifecycle records and error codes.</li></ol></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("spec/03-runtime/09-logging-and-observability.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _09LoggingAndObservability = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _09LoggingAndObservability as default
};
