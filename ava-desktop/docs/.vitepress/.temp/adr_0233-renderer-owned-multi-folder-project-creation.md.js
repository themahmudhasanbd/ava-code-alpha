import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0233: Renderer-Owned Multi-Folder Project Creation","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0233-renderer-owned-multi-folder-project-creation.md","filePath":"adr/0233-renderer-owned-multi-folder-project-creation.md","lastUpdated":1789382919000}');
const _sfc_main = { name: "adr/0233-renderer-owned-multi-folder-project-creation.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0233-renderer-owned-multi-folder-project-creation" tabindex="-1">ADR 0233: Renderer-Owned Multi-Folder Project Creation <a class="header-anchor" href="#adr-0233-renderer-owned-multi-folder-project-creation" aria-label="Permalink to &quot;ADR 0233: Renderer-Owned Multi-Folder Project Creation&quot;">​</a></h1><ul><li>Status: Accepted; behavior superseded in part by ADR 0249</li><li>Date: 2026-09-12</li><li>Deciders: PI-Desktop desktop UI maintainers</li><li>Amends: ADR 0011, ADR 0016</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The existing project entry point opens a native directory picker and activates the single directory returned by it. The redesigned Create project flow needs to collect a project name and support selecting multiple local folders in one operation, while preserving the host-core boundary that exposes one current visible workspace.</p><p>Treating the selected folders as one new host workspace would expand the runtime ownership model and make session, instruction, and file-access semantics ambiguous. Treating all but the first folder as temporary UI state would also lose the user&#39;s explicit project choices.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>The renderer owns the Create project dialog, including the project name, selected-folder list, remove actions, primary marker, validation, focus handling, and creation state.</li><li>The main process exposes <code>project/pickFolders</code> as a narrow native capability. It may open a multi-selection directory picker and returns absolute paths; it does not change the active host workspace.</li><li>The first selected folder is the primary root of one logical project group. Creation activates it and applies the entered display name to the group; every other selected folder is retained as a group root, not as an independent project tab. The host still exposes only the primary root as the currently active workspace.</li><li>The sidebar and Settings project entry points use the same dialog and creation action.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Users can name a project and add multiple folders without repeating the project-entry flow.</li><li>Additional folders remain available as durable roots of the same named project group.</li><li>The host RPC and security boundary remain unchanged apart from the additive directory-picker capability; switching tabs still changes the one active host workspace through the existing project activation path.</li><li>The first folder&#39;s position is meaningful. Reordering or choosing a different Primary folder is intentionally out of scope for this iteration.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Add multi-root execution to the host workspace:</strong> rejected because it changes the frozen one-visible-workspace model and requires a separate containment and permission design. ADR 0249 adds logical group ownership without broadening builtin tool roots.</li><li><strong>Keep the native picker as the entire UI:</strong> rejected because it cannot collect a project name, expose selected-folder removal, or provide a consistent ChatGPT-like creation surface.</li><li><strong>Open each folder in a separate user action:</strong> rejected because it makes a multi-folder setup slow and hides the relationship between the selected folders during creation.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0233-renderer-owned-multi-folder-project-creation.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0233RendererOwnedMultiFolderProjectCreation = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0233RendererOwnedMultiFolderProjectCreation as default
};
