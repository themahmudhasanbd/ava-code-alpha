import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0060: Archive the Regenerate Branch Under the RPC Lock","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0060-regenerate-branch-archive-under-the-rpc-lock.md","filePath":"adr/0060-regenerate-branch-archive-under-the-rpc-lock.md","lastUpdated":1786014746000}');
const _sfc_main = { name: "adr/0060-regenerate-branch-archive-under-the-rpc-lock.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0060-archive-the-regenerate-branch-under-the-rpc-lock" tabindex="-1">ADR 0060: Archive the Regenerate Branch Under the RPC Lock <a class="header-anchor" href="#adr-0060-archive-the-regenerate-branch-under-the-rpc-lock" aria-label="Permalink to &quot;ADR 0060: Archive the Regenerate Branch Under the RPC Lock&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-08-06</li><li>Deciders: PI-Desktop core</li><li>Related: D119 (transcript file store), D109 (regenerate history pager), ADR 0041 (decoupled message persistence)</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Turn completion in Electron main archived the finished regenerate branch with a read-modify-write across four host calls: <code>session.get</code>, <code>session.listRevisions</code>, <code>session.saveRevision</code>, then <code>session.replaceMessages</code> to stamp pager metadata (<code>revisionRootId</code> / <code>revisionCount</code> / <code>activeRevision</code>) on the user root.</p><p>Assistant and tool messages do not take that path. They travel through the app-owned <code>PersistenceOutbox</code> (ADR 0041) and reach SQLite asynchronously through <code>session.appendMessage</code>. The two paths therefore race, and <code>session.get</code> can observe a transcript that is one message short of the turn it is archiving.</p><p><code>session.replaceMessages</code> is a whole-transcript rewrite: it deletes every index row for the session and rewrites the transcript file from the caller&#39;s array. A snapshot taken before the outbox drained is written back as the truth, so the message appended in between is deleted from both the transcript file and the index. One observed session lost its final assistant message exactly this way — the model call succeeded (<code>outcome=ok</code>, <code>providerStatus=200</code>), the append succeeded, and the stamp-only rewrite landed 62 ms later from a stale snapshot. The rewrite also dropped <code>turn_id</code> on every row, because the reinserted index rows carried no turn attribution.</p><p>The metadata being written was a no-op in that case: the root already carried the intended <code>revisionCount</code> / <code>activeRevision</code>. A rewrite whose only purpose is an idempotent stamp destroyed a message.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>Turn completion calls one new host method, <code>session.saveActiveRevision</code>, which performs read, archive, and stamp inside a single RPC while holding the host state lock. No transcript snapshot crosses a process boundary and no window exists for a concurrent <code>session.appendMessage</code> to be overwritten.</li><li>The pager stamp rewrites exactly one transcript line (<code>transcripts::update_message</code>), copying every other line through verbatim. The file is re-read inside that function, so a line appended after any caller&#39;s own read survives. A metadata stamp can no longer cost the transcript its newest messages.</li><li>Electron main drains the persistence outbox before calling, and skips the archive (with a warning) when the outbox cannot be drained. An incomplete branch archive is silently wrong forever once a user pages back to it, so not archiving is the better failure.</li><li><code>session.replaceMessages</code> keeps each surviving message&#39;s owning <code>turn_id</code> across the rewrite. Remaining callers (regenerate truncation, tool review state) no longer strip turn attribution from the index.</li><li><code>session.replaceMessages</code> is documented as safe only for a caller that owns the whole transcript for the duration of the call. Read-modify-write over it from Electron main is not a supported pattern.</li><li>The new method is additive and the protocol version stays at 9. Host and Electron ship in one artifact, and a host without the method fails the call, which the caller already logs as a skipped archive rather than treating as data loss. Nothing a v9 client relies on changes.</li></ol><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Await the outbox and keep the four-call rewrite:</strong> narrows the window but does not close it. Any future writer on the append path reopens it, and the destructive primitive stays in the turn-completion path. Rejected.</li><li><strong>Stamp through a targeted <code>session.updateMessage</code> RPC and keep the archive in Electron:</strong> still two host calls with the archive reading a snapshot from outside the lock, so the archived branch can miss the final message even though the live transcript survives. Rejected.</li><li><strong>Make <code>session.replaceMessages</code> merge unknown newer messages:</strong> an implicit merge in a method whose contract is &quot;this is the transcript&quot; would make regenerate truncation unable to delete anything. Rejected.</li><li><strong>Bump the protocol version to 10:</strong> buys no negotiation because handshake already requires exact equality between components that ship together, and forces edits to five unrelated frozen-contract guards. Rejected.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A turn that completes after a regenerate can no longer lose its final assistant message, and archived branches contain the whole turn.</li><li><code>turn_id</code> survives transcript rewrites, so per-turn queries and diagnostics stay accurate after regenerate and tool review updates.</li><li>Electron main holds less transcript logic: the branch root search, the revision index arithmetic, and the stamp now live in host-core with unit tests, including one that appends a message after the archive&#39;s read.</li><li>Data already lost to the previous behavior is not recoverable; the message is absent from both the rewritten transcript and the archived revision payload.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0060-regenerate-branch-archive-under-the-rpc-lock.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0060RegenerateBranchArchiveUnderTheRpcLock = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0060RegenerateBranchArchiveUnderTheRpcLock as default
};
