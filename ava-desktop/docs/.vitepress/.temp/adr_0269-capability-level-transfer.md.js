import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0269: Move capability documents between the global and project levels","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0269-capability-level-transfer.md","filePath":"adr/0269-capability-level-transfer.md","lastUpdated":1789548812000}');
const _sfc_main = { name: "adr/0269-capability-level-transfer.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0269-move-capability-documents-between-the-global-and-project-levels" tabindex="-1">ADR 0269: Move capability documents between the global and project levels <a class="header-anchor" href="#adr-0269-move-capability-documents-between-the-global-and-project-levels" aria-label="Permalink to &quot;ADR 0269: Move capability documents between the global and project levels&quot;">​</a></h1><ul><li>Status: Accepted for implementation</li><li>Date: 2026-09-16</li><li>Amends: ADR 0112 (agent capability management roots and Settings IA — adds a cross-level move to the capability surfaces)</li><li>Related: ADR 0056 (user-owned MCP servers and skills), D193, D194</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>ADR 0112 made <code>.agents</code> the only capability root and separated file ownership from app-local activation state: an MCP server or skill lives globally (<code>~/.agents/servers</code>, <code>~/.agents/skills</code>) or inside a project (<code>&lt;project&gt;/.agents/...</code>), while <code>&lt;data&gt;/agent-capabilities/*.json</code> records whether it is enabled, with a per-project override for global documents. The two levels were reachable only as two independent lists. Changing a capability&#39;s level therefore meant deleting the file at one level and recreating it at the other: the user had to retype the document, the enablement decision was lost, and a mistake destroyed the only copy. Nothing in the Settings IA expressed the switch as one action.</p><p>Users do legitimately change their mind about ownership — a scratch server graduates from one project to every project, or a global experiment turns out to belong to one repository. This ADR records the move that the MCP and Skill pages now expose (<code>mcp.transfer</code> / <code>skills.transfer</code>).</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><h3 id="_1-a-level-switch-moves-the-document-it-does-not-copy-it" tabindex="-1">1. A level switch moves the document; it does not copy it <a class="header-anchor" href="#_1-a-level-switch-moves-the-document-it-does-not-copy-it" aria-label="Permalink to &quot;1. A level switch moves the document; it does not copy it&quot;">​</a></h3><p>The file&#39;s level <em>is</em> its ownership. A copy would leave the source level holding a definition the user just said belongs elsewhere, so the same capability would appear twice with two names until the user cleaned up the old one — and the two would drift. The source level stops listing the entry, and the destination holds the only file.</p><p>The move is one unit per capability: <code>McpServerRegistry::transfer</code> relocates the server&#39;s JSON file, and <code>UserSkillRegistry::transfer</code> relocates either the single Markdown document or, for the conventional <code>&lt;skill&gt;/SKILL.md</code> shape, the whole skill directory with the sibling resources a <code>Skill</code> invocation resolves (<code>directory_skill_root</code>, <code>copy_directory_contents</code>). <code>move_capability_file</code> tries <code>fs::rename</code> first so a same-filesystem move is atomic and cheap, and falls back to copy-then-remove for a project on another mount. When the document must be rewritten (the rename case below), the destination is written before the source is removed, so an interrupted move leaves a shadowed duplicate rather than no capability at all.</p><h3 id="_2-a-destination-collision-renames-the-arriving-document-it-never-blocks-the-move" tabindex="-1">2. A destination collision renames the arriving document; it never blocks the move <a class="header-anchor" href="#_2-a-destination-collision-renames-the-arriving-document-it-never-blocks-the-move" aria-label="Permalink to &quot;2. A destination collision renames the arriving document; it never blocks the move&quot;">​</a></h3><p>The destination level may already own the same id, or the same display name / label compared case-insensitively, the way project-over-global shadowing compares it. Overwriting an existing document would be data loss, and refusing the move would make the level switch useless whenever both sides happen to share a name. The arriving document is therefore disambiguated the way a file manager disambiguates a copy: a colliding id takes a <code>-2</code>/<code>-3</code>… suffix (within the 64-character id budget, <code>suffixed_capability_id</code>), and a colliding display name takes a <code> (2)</code>/<code> (3)</code>… suffix (<code>suffixed_display_name</code>). The existing entry and its file stay untouched, and the RPC response returns the record under the id it landed on so the UI can follow the rename.</p><p>Renaming a skill rewrites only the frontmatter <code>name</code> line (<code>rewrite_document_name</code>); every other frontmatter field, the whitespace, the line endings, and the body are copied byte for byte, because the user asked to relocate a document, not to reformat it. A document with no parsable frontmatter block gets a fresh one so the renamed file is still scannable.</p><p>A skill&#39;s identity is not its file name, and the move has to plan against the scanner rather than the filesystem. The catalog derives a skill&#39;s id with <code>capability_id</code>: the slug of the frontmatter <code>name</code> when that slug is usable, otherwise the slug of the path stem, and a hash as a last resort. A Chinese name, or a name whose slug already belongs to another document, therefore lands on an id the file name does not predict. Choosing a file name first and reading the id back from it produced exactly the failure this decision avoids: the source file was gone while the arrival was reported missing, and a same-id document silently lost the scan&#39;s de-duplication and vanished from Settings. <code>plan_skill_placement</code> now replays the real <code>capability_id</code> for the exact path each candidate would occupy, accepts only a placement the scan lists as its own record, and <code>transfer</code> resolves the arrived record by path — never by the stem it chose — so the state entry is written under the id the catalog will read back. A landing the scan still refuses (an empty body, an oversized document) is rolled back to the source with its original bytes, because no state has been written yet and a loud error beats a stranded file.</p><p>The same reasoning makes id collisions case-insensitive: macOS and Windows both hand back one file for <code>MyServer.json</code> and <code>myserver.json</code>, so a move that compared ids literally would write over a definition it never listed.</p><p>One artifact of that pre-existing <code>capability_id</code> rule is accepted rather than changed here: a non-ASCII name that has to be suffixed gains ASCII characters, and those characters alone become its slug. A Chinese-named skill arriving beside a same-named one therefore lands on the id <code>2</code>, not on a readable stem. Making the slug ignore a purely numeric remainder would change how every imported document is identified, which is a wider decision than a level move.</p><h3 id="_3-activation-state-follows-the-document" tabindex="-1">3. Activation state follows the document <a class="header-anchor" href="#_3-activation-state-follows-the-document" aria-label="Permalink to &quot;3. Activation state follows the document&quot;">​</a></h3><p>State is app-local and ownership-free, so a move that left the old enablement behind would silently change whether the capability runs, and a move that left entries at the source level would leave pointers to an id that no longer exists there. <code>set_moved_capability_state</code> therefore drops the source level&#39;s state for the old id — via <code>CapabilityState::forget</code>, including every project override a moved global document had — and writes the value the source row was showing at the destination:</p><ul><li>moving into a project stores that project&#39;s own state;</li><li>moving into global stores that value as the new global default, never another project&#39;s override, because the move says nothing about the projects the document left behind.</li></ul><p>A global source read may carry a <code>projectPath</code>, which is exactly the context the visible value is resolved in; <code>CapabilityTarget</code> keeps that field so the two meanings — &quot;owner&quot; and &quot;state context&quot; — are never confused.</p><h3 id="_4-the-two-ends-are-explicit-and-a-same-directory-move-is-a-no-op" tabindex="-1">4. The two ends are explicit and a same-directory move is a no-op <a class="header-anchor" href="#_4-the-two-ends-are-explicit-and-a-same-directory-move-is-a-no-op" aria-label="Permalink to &quot;4. The two ends are explicit and a same-directory move is a no-op&quot;">​</a></h3><p><code>mcp.transfer</code> / <code>skills.transfer</code> take <code>{ id, from, to }</code>, each end a <code>{ level, projectPath? }</code> target, instead of a single level and an implicit &quot;other side&quot;: the two ends can never be derived from one another wrongly, and a project-level end without <code>projectPath</code> fails with <code>CAPABILITY_INVALID</code>. <code>CapabilityTarget::same_directory</code> makes a move whose ends name the same directory a no-op that returns the current record, so a UI that offers the action redundantly can never rewrite the file.</p><h3 id="_5-the-global-agents-root-has-a-redirect-seam" tabindex="-1">5. The global <code>.agents</code> root has a redirect seam <a class="header-anchor" href="#_5-the-global-agents-root-has-a-redirect-seam" aria-label="Permalink to &quot;5. The global \`.agents\` root has a redirect seam&quot;">​</a></h3><p><code>PI_DESKTOP_AGENTS_DIR</code> repoints the global capability root, the way <code>PI_DESKTOP_DATA_DIR</code> repoints the app-local data directory; an empty value falls back to the home directory (<code>AGENTS_DIR_ENV</code>, <code>global_agents_dir</code>). A cross-level move spans the real home directory and a project, so without the seam the global half of the feature can only be tested against the developer&#39;s own <code>~/.agents</code>, and an isolated or portable install cannot place its global capabilities anywhere but the user&#39;s home. It is a test and installation seam, not a user setting, and it is not exposed in Settings.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A capability has exactly one home. The Settings row disappears from the source group and appears in the destination group, which is the confirmation the move happened; a same-level duplicate is impossible by construction.</li><li>A directory skill keeps working after the move because its resources travel with the document; a renamed directory skill is written under its new id.</li><li>Collisions stay productive: both definitions survive under names the user can tell apart, at the cost of the user having to recognize the <code>-2</code> name as the one they just moved.</li><li>A moved global document drops its other projects&#39; overrides. Those entries no longer name any global document, so keeping them would be state for a capability that does not exist; the projects simply fall back to no entry.</li><li>The move is host-owned and does not touch the renderer&#39;s level queries: <code>mcp.list</code> / <code>mcp.active</code> / <code>skills.list</code> / <code>skills.active</code> report the result after the scan, and the document itself never carries <code>enabled</code>.</li><li>Validation: transfer, rename, state, and directory-resource paths are covered by host-core registry tests, and the move-menu destination rule has a source-contract test in <code>apps/desktop/test/agent-capability-settings.test.mjs</code>. The rendered journey is specified as E2E-CAPABILITY-move-across-levels and remains Draft until a rendered E2E run is performed.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0269-capability-level-transfer.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0269CapabilityLevelTransfer = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0269CapabilityLevelTransfer as default
};
