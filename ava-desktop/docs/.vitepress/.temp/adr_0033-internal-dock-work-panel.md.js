import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0033: Internal-dock work panel (no native window expansion)","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0033-internal-dock-work-panel.md","filePath":"adr/0033-internal-dock-work-panel.md","lastUpdated":1789282569000}');
const _sfc_main = { name: "adr/0033-internal-dock-work-panel.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0033-internal-dock-work-panel-no-native-window-expansion" tabindex="-1">ADR 0033: Internal-dock work panel (no native window expansion) <a class="header-anchor" href="#adr-0033-internal-dock-work-panel-no-native-window-expansion" aria-label="Permalink to &quot;ADR 0033: Internal-dock work panel (no native window expansion)&quot;">​</a></h1><ul><li>Status: Superseded by ADR 0122; internal-dock direction restored by ADR 0151</li><li>Date: 2026-07-30</li><li>Related: <a href="./../spec/04-ux/01-ui-ia">01-ui-ia</a> · <a href="./../spec/04-ux/08-component-spec">08-component-spec §5</a> · <a href="./../spec/04-ux/09-interaction-patterns">09-interaction-patterns §8</a> · <a href="./../spec/03-runtime/01-ipc-protocol">01-ipc-protocol</a> · decision D163</li><li>Supersedes in part: ADR 0032</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>ADR 0032 made the work panel a docked flex column but, to keep the chat width stable, reserved <em>native window width</em> equal to the panel&#39;s committed width. Main expanded the OS window (within the display work area) by that width and positioned the native browser <code>WebContentsView</code> in the extended region. The user-visible result is that opening the panel <strong>grows the whole application window</strong> — it reads as the panel &quot;expanding out separately&quot; rather than occupying space inside the existing window.</p><p>ChatGPT and WorkBuddy keep the window fixed and let the side panel take space from inside the client area, pushing the conversation left. That is the requested behavior.</p><p>Investigation showed the window expansion was only a <em>room-making</em> mechanism:</p><ul><li>The former interactive terminal tab did not use a native view; ADR 0108 removes that surface entirely.</li><li>Native <code>WebContentsView</code>s (the host browser guest and plugin views) are positioned from the <strong>renderer-measured</strong> panel rect via <code>setBounds</code> (see <code>BrowserPane.setBounds</code> and <code>PluginViewTab</code>). The view composites above renderer content, so it must be told where to sit — but that rect is measured wherever the panel visually is, independent of whether the window grew.</li></ul><p>Therefore the native width reservation is unnecessary for correctness: if the panel becomes an in-flow column of a <em>fixed</em> window, the measured rect already lands inside the window, and the browser view follows it without any window expansion.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>The work panel is a fixed-width in-flow flex column inside the <strong>fixed</strong> client area. Opening it reflows <code>MainChat</code> to the left; it never expands the OS window.</li><li>The renderer always requests a native reservation width of <code>0</code> (<code>api.setWorkPanelReservation(0)</code>). Main no longer changes window bounds for the panel. The <code>window/setWorkPanelReservation</code> IPC is retained as a stable seam; Main returns an empty reservation (<code>{ requested: 0, reserved: 0 }</code>).</li><li>The native browser <code>WebContentsView</code> continues to be positioned from the renderer-measured panel rect via <code>browserSetBounds</code>. No window expansion is required for it to sit correctly.</li><li>The default committed width is <strong>420px</strong> (the established baseline), within the unchanged <code>364..720px</code> clamp. <em>(Superseded by decision D167: the default is 280px inside a <code>244..720px</code> clamp; every other clause here stands.)</em><em>(Amended by ADR 0238: the <code>244..720px</code> clamp becomes the live three-column budget — 244px minimum, no fixed maximum, MainChat floor 360px.)</em></li><li>Native window-edge resize changes <code>MainChat</code> only, now by plain reflow (the panel is internal and stays at its committed width).</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>The OS window size is stable across open / collapse / divider commit; only <code>MainChat</code> reflows. This matches ChatGPT / WorkBuddy.</li><li>On small windows <code>MainChat</code> can be squeezed below its 360px readability target when the panel is open at a wide width — the same trade-off ChatGPT accepts, and acceptable here.</li><li>Geometry logic is simpler: the reservation machinery is retained but inert, so the renderer→Main IPC surface and its tests stay valid.</li><li>Persisted normal bounds already exclude reservation width; with reservation always <code>0</code> they are simply the user&#39;s window size, which is the desired relaunch behavior.</li><li>The exit animation and native view detach-before-exit logic are unchanged.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><h3 id="keep-adr-0032-reserve-native-width-expand-the-window" tabindex="-1">Keep ADR 0032 (reserve native width, expand the window) <a class="header-anchor" href="#keep-adr-0032-reserve-native-width-expand-the-window" aria-label="Permalink to &quot;Keep ADR 0032 (reserve native width, expand the window)&quot;">​</a></h3><p>Rejected: expanding the whole window to make room for the panel is exactly the &quot;expands out separately&quot; behavior the user wants to remove. It also makes the panel feel disconnected from the in-window layout.</p><h3 id="clip-the-native-view-with-a-separate-overlay-window" tabindex="-1">Clip the native view with a separate overlay window <a class="header-anchor" href="#clip-the-native-view-with-a-separate-overlay-window" aria-label="Permalink to &quot;Clip the native view with a separate overlay window&quot;">​</a></h3><p>Rejected: adds a second window, focus/occlusion complexity, and a different failure mode, for no benefit over measuring the in-flow rect.</p><h2 id="references" tabindex="-1">References <a class="header-anchor" href="#references" aria-label="Permalink to &quot;References&quot;">​</a></h2><ul><li><code>docs/adr/0032-reserve-native-width-for-the-docked-work-panel.md</code> (superseded in part)</li><li><code>apps/desktop/src/App.tsx</code> (reservation target set to <code>0</code>)</li><li><code>apps/desktop/src/lib/work-panel-resize.ts</code> (<code>WORK_PANEL_DEFAULT_WIDTH</code>)</li><li><code>apps/desktop/src/components/workpanel/PluginViewTab.tsx</code> (<code>pluginViewSetBounds</code> from measured rect)</li><li><code>apps/desktop/electron/main/browser-view.ts</code> (<code>BrowserPane.setBounds</code>)</li><li><code>docs/spec/06-delivery/04-e2e-test-plan.md</code> (E2E-056)</li><li><code>docs/spec/08-meta/decisions-log.md</code> (D163)</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0033-internal-dock-work-panel.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0033InternalDockWorkPanel = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0033InternalDockWorkPanel as default
};
