import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0251: Deleting a Project Removes Its Owned Sessions","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0251-project-delete-with-owned-sessions.md","filePath":"adr/0251-project-delete-with-owned-sessions.md","lastUpdated":1789397881000}');
const _sfc_main = { name: "adr/0251-project-delete-with-owned-sessions.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0251-deleting-a-project-removes-its-owned-sessions" tabindex="-1">ADR 0251: Deleting a Project Removes Its Owned Sessions <a class="header-anchor" href="#adr-0251-deleting-a-project-removes-its-owned-sessions" aria-label="Permalink to &quot;ADR 0251: Deleting a Project Removes Its Owned Sessions&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-15</li><li>Deciders: PI-Desktop maintainers</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The Projects index has listed durable project records since ADR 0026 moved it into Settings, and ADR 0016 made the sidebar a retained-tab surface. Archive, pin, close, and ordering are deliberately renderer-local metadata: the component contract states that &quot;Project close removes retained tab only; durable project/sessions remain&quot;, and <code>US-UI-47</code> states that renderer metadata never hides or deletes a durable Project-archive row.</p><p>That left no user-facing way to remove a durable project record. A user who archived a project that should no longer exist could only edit host-core&#39;s SQLite file directly and clear renderer storage afterwards, because the Projects index is a union of four sources: project-group projections of the durable <code>projects</code> rows, <code>pi.desktop.recentProjects</code>, projects re-derived from live sessions, and the active workspace. Deleting only the database row leaves the other three sources in place, so the row reappears.</p><p>Two integrity facts constrain the fix:</p><ul><li><code>PRAGMA foreign_keys = ON</code> is enforced. <code>sessions.project_id</code> and <code>scheduled_tasks.project_id</code> are <code>ON DELETE SET NULL</code>, so deleting a project row alone orphans its sessions rather than removing them.</li><li>A stored (non-legacy) project group owns an ordered root list and a primary root. Removing the primary root underneath the group would leave the group without a valid primary.</li></ul><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>Host-core gains the additive RPC <code>projects.remove({ path })</code>, returning <code>{ removed, sessionsRemoved }</code>. It is the only supported way to remove a durable project record.</li><li>The RPC deletes the project row together with every session whose <code>project_id</code> points at it, reusing the existing <code>sessions::delete_session</code> path so transcript, scratch, and review files are removed exactly as they are for a single conversation delete, and it clears the project&#39;s durable memory. It is idempotent for an unknown path, and it is refused while any attached session has a running turn (see 7).</li><li>The RPC never touches the project&#39;s folder on disk. PI-Desktop deletes application records, never user files, and a project whose folder was moved or deleted is still removable.</li><li>A path that is a root of a stored multi-folder project group is refused with a structured error. The user removes the folder from the group first, which keeps the group&#39;s primary root valid; legacy single-root projections are not affected and delete normally.</li><li>Deleting is a renderer action behind a second confirmation that names the project and the number of sessions it owns. On success the renderer drops the matching renderer-local record in the same operation: the archived/pinned metadata, the recent-project entry, the retained tab, and, when the deleted project was active, the workspace falls back to another open project or to Temporary. A path the host has no durable row for is not a failure: the renderer-local record is removed anyway, because a stale recent-project entry is the only thing that can keep such a row visible.</li><li>The action stays renderer-only. It is deliberately absent from <code>CONTROL_OPERATION_SPECS</code>, so local MCP control cannot delete projects.</li><li>The bulk delete is refused while any attached session has a running turn (1008 / <code>CONFLICT</code>). A live turn still owns its tools and working directory and is still appending to the transcript the delete would remove, so the operation is deliberately stricter than single-conversation delete, which keeps its current semantics. The renderer blocks the same action up front, so users normally meet a message instead of an error, and a refusal that reaches the dialog is shown with the same localized copy. The check and the deletes run in one host RPC under the host&#39;s single state lock, so no turn can start between them.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A durable project row can finally be removed from the UI, and the row no longer reappears after a reload, because the durable row and the renderer-local records are removed by one action.</li><li>Sessions and transcripts of a deleted project are gone permanently. A project delete is therefore not recoverable from the application; the folder on disk and any file-level history outside PI-Desktop are unaffected.</li><li>Group structure is never silently rewritten. Users with multi-folder projects see an explicit refusal instead of a corrupted group.</li><li>Composer/sidebar code that lists projects must treat the four index sources consistently; a future index source has to be cleaned by the same action, or the row will reappear.</li><li>A project with a running task must be stopped before it can be deleted, which is one extra step in that case; the alternative was deleting under a live writer, which can resurrect a session row for the transcript the user just removed.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><ul><li><strong>Add a renderer-only &quot;hide&quot; flag.</strong> Rejected: it keeps the durable row, the sessions, and the disk usage, which is the complaint that motivated the change, and it contradicts <code>US-UI-47</code>&#39;s promise about renderer metadata.</li><li><strong>Delete the project row and let <code>ON DELETE SET NULL</code> orphan the sessions.</strong> Rejected: it produces unreachable conversations that still occupy the transcript directory and still look like a project in the index.</li><li><strong>Delete the project&#39;s folder on disk too.</strong> Rejected: an application action must not remove user files.</li><li><strong>Detach the root from a multi-folder group inside the same call.</strong> Rejected for now: <code>project.group.update</code> refuses to detach a root that has chats, and silently rewriting group structure during a delete makes the outcome hard to predict. The refusal path can be relaxed by a later ADR.</li><li><strong>Abort the running turns inside the same call.</strong> Rejected: host-core can settle a turn&#39;s own bookkeeping but cannot stop the agent runtime that is still streaming into that session, so an aborted-then-deleted session can still be re-created as a stub by the next append (D318). Refusing the delete keeps the timing decision with the user and leaves no window for that.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0251-project-delete-with-owned-sessions.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0251ProjectDeleteWithOwnedSessions = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0251ProjectDeleteWithOwnedSessions as default
};
