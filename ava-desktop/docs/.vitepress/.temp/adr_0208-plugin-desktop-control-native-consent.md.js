import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0208: Plugin Desktop Control Requires Native User Consent","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0208-plugin-desktop-control-native-consent.md","filePath":"adr/0208-plugin-desktop-control-native-consent.md","lastUpdated":1788992254000}');
const _sfc_main = { name: "adr/0208-plugin-desktop-control-native-consent.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0208-plugin-desktop-control-requires-native-user-consent" tabindex="-1">ADR 0208: Plugin Desktop Control Requires Native User Consent <a class="header-anchor" href="#adr-0208-plugin-desktop-control-requires-native-user-consent" aria-label="Permalink to &quot;ADR 0208: Plugin Desktop Control Requires Native User Consent&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-10</li><li>Decision: D377</li><li>Related: ADR 0203 (local MCP control plane), D370, D372, <a href="./../spec/07-plugins/03-plugin-api">07-plugins/03-plugin-api.md</a>, <a href="./../spec/07-plugins/04-plugin-security">07-plugins/04-plugin-security.md</a> §8.2, <a href="./../spec/07-plugins/13-plugin-permissions-matrix">07-plugins/13-plugin-permissions-matrix.md</a></li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p><code>desktop.control</code> gives a plugin the reviewed operation catalog that the local MCP control plane exposes: project, session, Agent, and workspace operations, each tagged <code>read</code>, <code>write</code>, or <code>dangerous</code>. The catalog and the invocation path are shared with MCP so there is one permission and persistence implementation.</p><p>The shared controller requires <code>confirm: true</code> for a <code>dangerous</code> operation. For an external MCP agent that flag is the documented contract: it is an acknowledgement by the agent, not a desktop prompt (D372). When the same controller is reached from plugin code, the flag is set by the plugin, so a plugin with <code>desktop.control</code> could delete sessions, switch a session to <code>auto</code> tool approval, or resolve a pending tool permission with no human in the loop. A conversational plugin that shows its own confirmation card is self-policing, and the card can only show what the plugin (or a model behind it) chooses to display.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>A <code>dangerous</code> desktop operation from a plugin needs two answers. The plugin&#39;s <code>confirm: true</code> remains required first (<code>CONFIRMATION_REQUIRED</code> otherwise) so an unacknowledged call never reaches the user.</li><li>After that, Electron main asks the user in a native, blocking dialog (<code>plugin-desktop-consent.ts</code>) attached to the main window. The dialog shows the catalog operation id, the catalog description, and a bounded preview of the arguments. It never shows plugin- or model-authored text, so a prompt-injected transcript cannot relabel <code>session/delete</code> as something benign.</li><li>Escape, dismissal, and Deny are refusals (<code>PERMISSION_DENIED</code>). The answer is per call; there is no &quot;allow until quit&quot; for dangerous operations.</li><li>A host that supplies no dialog service (headless or test runtime) refuses every dangerous operation from plugins. <code>read</code> and <code>write</code> operations are unchanged.</li><li>Every invocation is audited with plugin id, operation, and risk; a refusal is audited as <code>PERMISSION_DENIED</code>.</li><li><code>ui.microphone</code> stays a separate, narrower grant: audio capture only, inside the plugin&#39;s isolated panel session; camera and other device permissions remain denied.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Plugin-originated destructive desktop operations gain the same human-in-the-loop guarantee that plugin file access outside the manifest scope already has (<code>confirmFsAccess</code>).</li><li>The MCP contract is unchanged: an external agent still acknowledges with <code>confirm: true</code> and the desktop user is the one who enabled the loopback control plane.</li><li>Plugin authors must expect a native prompt and should not promise unattended dangerous operations.</li><li>Covered by <code>apps/desktop/test/plugin-desktop-control.test.mjs</code> and E2E-236.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0208-plugin-desktop-control-native-consent.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0208PluginDesktopControlNativeConsent = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0208PluginDesktopControlNativeConsent as default
};
