import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"13. Plugin Permissions Matrix","description":"","frontmatter":{},"headers":[],"relativePath":"spec/07-plugins/13-plugin-permissions-matrix.md","filePath":"spec/07-plugins/13-plugin-permissions-matrix.md","lastUpdated":1789739317000}');
const _sfc_main = { name: "spec/07-plugins/13-plugin-permissions-matrix.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="_13-plugin-permissions-matrix" tabindex="-1">13. Plugin Permissions Matrix <a class="header-anchor" href="#_13-plugin-permissions-matrix" aria-label="Permalink to &quot;13. Plugin Permissions Matrix&quot;">​</a></h1><h2 id="_1-goals" tabindex="-1">1. Goals <a class="header-anchor" href="#_1-goals" aria-label="Permalink to &quot;1. Goals&quot;">​</a></h2><p>Provide a permission–capability–risk–default-policy reference table for reuse by UI copy and validation.</p><h2 id="_2-matrix" tabindex="-1">2. Matrix <a class="header-anchor" href="#_2-matrix" aria-label="Permalink to &quot;2. Matrix&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Permission</th><th>Risk</th><th>Allowed API / capability</th><th>Default policy</th><th>Notes</th></tr></thead><tbody><tr><td><code>ui.panel</code></td><td>low</td><td>Open the plugin panel</td><td>Granted at install</td><td>Needed by almost all UI plugins</td></tr><tr><td><code>ui.view</code></td><td>low</td><td><code>contributes.views</code> are listed in the work panel and may be opened</td><td>Granted at install</td><td>Same isolation as a panel window: sandboxed page, per-plugin partition, <code>net.domains</code> egress. Filtered by activation scope</td></tr><tr><td><code>ui.theme</code></td><td>low</td><td><code>contributes.themes</code> CSS is loaded and offered in Settings; runtime <code>pi.themes.upsert</code> / <code>remove</code> / <code>list</code> and <code>pi.app.setTheme</code> (ADR 0260)</td><td>Granted at install</td><td>CSS is sanitized by the host; it cannot script. Declared <code>assets</code> are served over the host&#39;s read-only <code>plugin-asset:</code> scheme. <code>setTheme</code> may only select a built-in preference or a currently registered plugin theme. There is no per-plugin theme count cap</td></tr><tr><td><code>ui.window.appearance</code></td><td>low</td><td><code>contributes.windowAppearance</code> sets the native window background while one of the plugin&#39;s themes is selected</td><td>Granted at install</td><td><code>#rrggbb</code> / <code>#rrggbbaa</code> only; applied per resolved palette and back to the host default once the theme is gone. macOS keeps vibrancy</td></tr><tr><td><code>clipboard.read</code></td><td>medium</td><td><code>clipboard.readText</code>, <code>clipboard.getHistory</code></td><td>Confirm on first use</td><td>May read sensitive information and retained clipboard history</td></tr><tr><td><code>clipboard.write</code></td><td>medium</td><td><code>clipboard.writeText</code></td><td>Confirm on first use</td><td>Prevents clipboard pollution</td></tr><tr><td><code>notify</code></td><td>low</td><td><code>ui.notify</code>, <code>ui.getNotificationPermission</code>, <code>ui.requestNotificationPermission</code>, <code>ui.showNativeNotification</code></td><td>Can be granted by default</td><td>Native delivery is OS-controlled; avoid notification-spam abuse</td></tr><tr><td><code>fs.read</code></td><td>medium</td><td><code>fs.readText</code> / <code>fs.stat</code> / <code>fs.readRange</code> / <code>fs.readPreview</code> / <code>fs.openDefault</code> / <code>fs.reveal</code> / <code>fs.glob</code> / <code>fs.list</code> / <code>fs.requestDirectory</code> / dropped-file grant</td><td>Granted at install, bounded by <code>manifest.fs.read</code></td><td><code>fs.stat</code> and <code>fs.readRange</code> share the read gate; a dropped-file grant is one-file, read-only, memory-only, and gesture-bound; all file calls remain root- and deny-list-checked</td></tr><tr><td><code>fs.write</code></td><td>high</td><td><code>fs.writeText</code></td><td>Granted at install, bounded by <code>manifest.fs.write</code></td><td>Scope is required; a whole-tree pattern fails validation. Out of scope asks the user</td></tr><tr><td><code>fs.delete</code></td><td>high</td><td><code>fs.remove</code></td><td>Granted at install, bounded by <code>manifest.fs.delete</code></td><td>Two tiers (<code>own</code> / <code>scope</code>), always via the OS trash, non-recursive, rate-braked (§2B)</td></tr><tr><td><code>fs.read.workspace</code></td><td>medium</td><td>—</td><td>Downgraded on load to <code>fs.read</code> with a whole-tree scope</td><td>Legacy name; predates scopes</td></tr><tr><td><code>fs.write.workspace</code></td><td>high</td><td>—</td><td>Downgraded on load to <code>fs.write</code> with <strong>no</strong> scope</td><td>Legacy name; every write asks the user until the manifest declares scope</td></tr><tr><td><code>fs.delete.workspace</code></td><td>high</td><td>—</td><td>Downgraded on load to <code>fs.delete</code> with <code>own: true</code></td><td>Legacy name; only the plugin&#39;s own output goes without asking</td></tr><tr><td><code>agent.tool.register</code></td><td>high</td><td>Register an agent tool</td><td>Confirm at install</td><td>Tool execution is audited separately</td></tr><tr><td><code>agent.prompt.inject</code></td><td>high</td><td>Inject a system prompt; activates <code>contributes.skills</code></td><td>Deny by default / strong confirmation</td><td>Easily leads to behavior hijacking</td></tr><tr><td><code>agent.extension</code></td><td>high</td><td>Run <code>contributes.agentExtensions</code> modules inside the agent process</td><td>Explicit confirmation; local imports and development plugins only in v1.1</td><td>Same access as the agent&#39;s own tools; the plugin sandbox does not apply (spec 16)</td></tr><tr><td><code>provider.register</code></td><td>high</td><td><code>contributes.providers</code> become rows in the native provider list, owned by the plugin and refreshed from the manifest on load</td><td>Explicit confirmation; local imports and development plugins only in v1.1, matching <code>agent.extension</code></td><td>The user path refuses the row (<code>PROVIDER_OWNED_BY_PLUGIN</code>); credentials stay in the Host secret store under the usual provider refs; <code>oauth</code> declarations are not enabled yet</td></tr><tr><td><code>net.fetch</code></td><td>high</td><td><code>net.fetch</code></td><td>Deny by default</td><td>Confined to <code>manifest.net.domains</code>; an empty or malformed list means no egress (§2A)</td></tr><tr><td><code>net.websocket</code></td><td>high</td><td><code>pi.net.websocket.connect</code> / <code>send</code> / <code>close</code> (host-owned sockets; at most 4 per plugin, 1 MiB frames)</td><td>Deny by default</td><td>Confined to <code>manifest.net.domains</code> like <code>net.fetch</code>; a refused host never reaches the transport, and every socket is closed when the plugin unloads, is disabled, or crashes</td></tr><tr><td><code>shell.openExternal</code></td><td>medium</td><td>Open external link</td><td>Confirm on first use</td><td>Prevents phishing links</td></tr><tr><td><code>mcp.server.local</code></td><td>high</td><td>Spawn a <code>transport: &quot;stdio&quot;</code> MCP server declared in the manifest</td><td>Deny by default</td><td>Runs a local executable; its tools reach the agent</td></tr><tr><td><code>mcp.server.remote</code></td><td>high</td><td>Connect a <code>transport: &quot;http&quot;</code> MCP server</td><td>Deny by default</td><td>Sends tool arguments to a third-party endpoint; non-loopback HTTP is unencrypted</td></tr><tr><td><code>background.service</code></td><td>medium</td><td>Start <code>contributes.services</code> and keep the plugin process resident</td><td>Confirm at install</td><td>Supervised with backoff; visible on the Plugins page</td></tr><tr><td><code>bus.publish</code></td><td>medium</td><td><code>bus.publish</code> to declared topics</td><td>Confirm at install</td><td>Other plugins can act on the message</td></tr><tr><td><code>bus.subscribe</code></td><td>medium</td><td><code>bus.subscribe</code> to declared patterns</td><td>Confirm at install</td><td>Can observe another plugin&#39;s messages</td></tr><tr><td><code>browser.cdp</code></td><td>high</td><td><code>pi.browser.*</code> against the host work-panel guest</td><td>Confirm at install</td><td>Guest bounds are clamped to the calling plugin view; CDP is allowlisted</td></tr><tr><td><code>desktop.control</code></td><td>high</td><td><code>pi.desktop.listOperations</code>, <code>pi.desktop.invoke</code>, including the reviewed <code>session/collaboration/*</code> operations</td><td>Confirm at install</td><td>Shared with the local MCP control plane&#39;s reviewed operation catalog, except for operations marked plugin-only: the six <code>session/collaboration/*</code> operations reach the plugin gateway but are deliberately absent from the MCP-visible catalog and have no renderer mutation channel; collaboration <code>spawn</code>/<code>send</code> additionally require an active plugin Agent tool invocation, whose source Session/turn/invocation identity is injected by the host; panel cancellation is limited to that plugin&#39;s own deliveries; a <code>dangerous</code> operation needs <code>confirm: true</code> from the plugin <strong>and</strong> the user&#39;s answer to a host-owned native dialog that names the catalog operation; the MCP bearer token and Electron channel names are never exposed</td></tr><tr><td><code>ui.microphone</code></td><td>medium</td><td><code>navigator.mediaDevices.getUserMedia({ audio: true })</code> inside the plugin&#39;s isolated panel</td><td>Confirm at install</td><td>Audio only; camera and every other device permission stay denied; no native handle or host secret reaches the plugin</td></tr><tr><td><code>audio.capture.background</code></td><td>high</td><td><code>pi.audio.getInputDevices</code>, <code>openInput</code>, <code>closeInput</code>, <code>getCaptureState</code>, <code>onInputFrame</code> / <code>offInputFrame</code> (registered in the plugin API and gated by this permission; the two synchronous registration helpers throw the coded refusal)</td><td>Deny by default</td><td>Host owns the device; PCM16 frames only, no device handle or <code>MediaStream</code>. The host has no device backend yet, so an authorized call is refused with a coded <code>UNSUPPORTED</code> (audited); no device is opened</td></tr><tr><td><code>audio.playback.background</code></td><td>medium</td><td><code>pi.audio.openOutput</code>, <code>writeOutput</code>, <code>stopOutput</code>, <code>closeOutput</code> (registered in the plugin API and gated by this permission)</td><td>Confirm at install</td><td>Host-owned playback queue, PCM16 only. The host has no device backend yet, so an authorized call is refused with a coded <code>UNSUPPORTED</code> (audited); no device is opened</td></tr><tr><td><code>keyboard.globalShortcut</code></td><td>medium</td><td><code>pi.keyboard.registerGlobalShortcut</code>, <code>unregisterGlobalShortcut</code>, <code>listGlobalShortcuts</code>; <code>contributes.globalShortcuts</code></td><td>Confirm at install</td><td>Host owns Electron <code>globalShortcut</code>; a shortcut only runs the plugin&#39;s own command; conflicts are refused (<code>SHORTCUT_CONFLICT</code> / <code>SHORTCUT_UNAVAILABLE</code> / <code>INVALID_ACCELERATOR</code> / <code>LIMIT_EXCEEDED</code>, max 8 per plugin); released on unload/disable/crash</td></tr><tr><td><code>models.list</code></td><td>medium</td><td><code>pi.models.list</code></td><td>Confirm at install</td><td>Ready provider/model rows only; no secrets</td></tr><tr><td><code>project.create</code></td><td>high</td><td><code>pi.project.create</code> and explicit <code>projectId</code> on session import</td><td>Confirm at install</td><td>Creates or reuses a durable project row without activating the workspace; imported sessions remain unbound unless the id is supplied</td></tr><tr><td><code>session.read</code></td><td>high</td><td><code>pi.session.getLlmContext</code></td><td>Confirm at install</td><td>In-flight tool session only; compaction-aware projection (D019 / D336)</td></tr><tr><td><code>session.import</code></td><td>high</td><td><code>pi.session.import</code>, <code>pi.session.importBatch</code></td><td>Confirm at install</td><td>Imports only into the calling plugin&#39;s declared session sources; bounded and rate-limited</td></tr><tr><td><code>session.read.own</code></td><td>medium</td><td><code>pi.session.list</code>, <code>pi.session.get</code>, <code>pi.session.listMessages</code></td><td>Confirm at install</td><td>Reads only sessions imported by the calling plugin; no cross-plugin access</td></tr><tr><td><code>session.update.own</code></td><td>medium</td><td><code>pi.session.rename</code></td><td>Confirm at install</td><td>Renames only the calling plugin&#39;s active imported sessions</td></tr><tr><td><code>session.delete.own</code></td><td>high</td><td><code>pi.session.delete</code></td><td>Confirm at install</td><td>Trash/purge only the calling plugin&#39;s imported sessions; rate-limited</td></tr><tr><td><code>usage.read</code></td><td>medium</td><td><code>pi.usage.listTurns</code></td><td>Confirm at install</td><td>Read-only listing of completed-turn facts (per-turn token counters and identifiers, keyset-paginated); no message body and no write path</td></tr><tr><td><code>agent.complete</code></td><td>high</td><td><code>pi.agent.complete</code></td><td>Confirm at install</td><td>Host-owned one-shot; spends user quota; <code>includeSessionContext</code> also needs <code>session.read</code></td></tr><tr><td><code>speech.adapter.register</code></td><td>high</td><td><code>pi.speech.registerAdapter</code> / <code>unregisterAdapter</code></td><td>Confirm at install</td><td>Registers a speech protocol. Handles stay in the guest; HTTP plans are executed by the host with the bound provider key and must stay on that origin. Built-in protocol ids are reserved</td></tr></tbody></table><h2 id="_2a-a-permission-is-the-switch-the-manifest-carries-the-range" tabindex="-1">2A. A permission is the switch; the manifest carries the range <a class="header-anchor" href="#_2a-a-permission-is-the-switch-the-manifest-carries-the-range" aria-label="Permalink to &quot;2A. A permission is the switch; the manifest carries the range&quot;">​</a></h2><p>Two capabilities are too coarse to be answered by a name alone, so the name says whether the plugin may act and a manifest field says how far. Both fields are enforced by the host, shown to the user next to the permissions, and validated at install time.</p><table tabindex="0"><thead><tr><th>Field</th><th>Bounds</th><th>Absent or empty means</th></tr></thead><tbody><tr><td><code>net.domains</code></td><td>Every host-owned egress path: the panel session, <code>pi.net.fetch</code>, remote HTTP MCP endpoints</td><td>No egress at all, whatever <code>net.fetch</code> says</td></tr><tr><td><code>fs.read</code> / <code>fs.write</code> / <code>fs.delete</code></td><td>Which paths that file mode may touch</td><td>No standing reach; every access falls to a confirmation</td></tr></tbody></table><p>Failing closed on an absent field is what makes the two safe to omit: a manifest that says nothing grants nothing. See <a href="./04-plugin-security">04-plugin-security.md</a> §6 and §8.1, and ADR 0088.</p><p>The two are also linked. <code>fs.read</code> may declare the whole tree because a read only becomes a leak when the bytes can leave, and <code>net.domains</code> closes that half. <code>fs.write</code> and <code>fs.delete</code> are dangerous on their own, so a whole-tree pattern (<code>**</code>, <code>**/*</code>, <code>*/**</code>, <code>./*</code>) fails manifest validation for those modes.</p><h2 id="_2b-deletion" tabindex="-1">2B. Deletion <a class="header-anchor" href="#_2b-deletion" aria-label="Permalink to &quot;2B. Deletion&quot;">​</a></h2><p><code>fs.delete</code> is the one file mode whose damage is not undoable by re-running the plugin, so it carries three bounds the other modes do not:</p><ol><li><strong>Two tiers.</strong> <code>own: true</code> lets a plugin remove files it wrote itself — the host keeps a write ledger in the plugin&#39;s data directory — with no scope and no prompt; a file the user has modified since drops out of the ledger. Deleting anything else needs a declared <code>scope</code>, and out-of-scope paths ask the user.</li><li><strong>The OS trash.</strong> Removal goes through <code>shell.trashItem</code>, never <code>rm</code>, and never recursively: a non-empty directory is refused rather than emptied. The host keeps no copy of the user&#39;s data to provide this.</li><li><strong>A rate brake.</strong> 50 deletes per rolling 60s per plugin. Past it the user is asked once with the reason given as rate rather than path, because <code>recursive: false</code> bounds one call and not a <code>glob</code> plus a loop.</li></ol><h2 id="_3-permission-dependencies" tabindex="-1">3. Permission dependencies <a class="header-anchor" href="#_3-permission-dependencies" aria-label="Permalink to &quot;3. Permission dependencies&quot;">​</a></h2><ul><li><code>ui.panel</code> is required to load a panel entry</li><li><code>ui.view</code> is required to contribute work panel views; it is independent of <code>ui.panel</code>, so a plugin may ship docked views without a detached window</li><li><code>agent.tool.register</code> is required to contribute agentTools</li><li>When <code>fs.write</code> is present, it is recommended to also declare <code>fs.read</code></li><li><code>manifest.fs.&lt;mode&gt;</code> requires the matching <code>fs.&lt;mode&gt;</code> permission; a scope nobody can use fails validation rather than being silently ignored</li><li><code>fs.requestDirectory</code> (the <code>userSelected</code> root) is gated on <code>fs.read</code>; writing or deleting inside the chosen directory still needs <code>fs.write</code> / <code>fs.delete</code></li><li>A contribution whose permission is missing fails manifest validation (<code>themes</code>, <code>mcpServers</code>, <code>services</code>, <code>bus</code>); <code>skills</code> is the exception and is skipped at load time instead (see <a href="./02-plugin-manifest-schema">02-plugin-manifest-schema.md</a> §7)</li><li>Lifecycle and state events need no permission: <code>workspace:changed</code>, <code>session:modelChanged</code>, <code>session:turnEnded</code>, and <code>plugin:settingsChanged</code> arrive on the existing plugin event channel, and subscribing to an unknown event name does not error</li></ul><h2 id="_3a-plan-operating-state-rule" tabindex="-1">3A. Plan operating-state rule <a class="header-anchor" href="#_3a-plan-operating-state-rule" aria-label="Permalink to &quot;3A. Plan operating-state rule&quot;">​</a></h2><p>Every <code>agentTools</code> contribution is denied in Plan, regardless of this matrix&#39;s risk or default policy. <code>agent.tool.register</code> authorizes registration for Agent, not visibility in Plan. The host returns <code>PLUGIN_DISABLED_IN_PLAN</code> for a direct Plan call and records the denial. Plugin tools become eligible only after the same Agent is approved into Agent mode.</p><h2 id="_4-permission-display-copy" tabindex="-1">4. Permission display copy <a class="header-anchor" href="#_4-permission-display-copy" aria-label="Permalink to &quot;4. Permission display copy&quot;">​</a></h2><p>English is the primary copy. The zh-CN column holds the localized example strings. A file permission is never shown alone: the declared scope is rendered beside it, so &quot;Modify the files it lists&quot; is followed by the list.</p><table tabindex="0"><thead><tr><th>Permission</th><th>English copy</th><th>zh-CN example</th></tr></thead><tbody><tr><td><code>fs.read</code></td><td>Read the files it lists</td><td>读取它列出的文件</td></tr><tr><td><code>clipboard.read</code></td><td>Read the current clipboard and retained history</td><td>读取当前剪贴板和保留的历史</td></tr><tr><td><code>fs.write</code></td><td>Modify the files it lists</td><td>修改它列出的文件</td></tr><tr><td><code>fs.delete</code></td><td>Delete the files it lists, to the trash</td><td>删除它列出的文件（进回收站）</td></tr><tr><td><code>notify</code></td><td>Show in-app and native notifications</td><td>显示应用内和系统通知</td></tr><tr><td><code>agent.tool.register</code></td><td>Provide executable tools to the AI Agent</td><td>向 AI Agent 提供可执行工具</td></tr><tr><td><code>agent.prompt.inject</code></td><td>Adjust agent instructions</td><td>调整智能体指令</td></tr><tr><td><code>agent.extension</code></td><td>Run code inside the agent</td><td>在 agent 内运行代码</td></tr><tr><td><code>net.fetch</code></td><td>Access the network</td><td>访问网络</td></tr><tr><td><code>shell.openExternal</code></td><td>Open external links</td><td>打开外部链接</td></tr><tr><td><code>ui.theme</code></td><td>Provide a theme</td><td>提供主题</td></tr><tr><td><code>ui.settings</code></td><td>Add a sandboxed Settings entry in Extensions</td><td>在“扩展”中添加沙盒设置项</td></tr><tr><td><code>ui.window.appearance</code></td><td>Set the window background</td><td>设置窗口背景</td></tr><tr><td><code>mcp.server.local</code></td><td>Run a local MCP server</td><td>运行本地 MCP 服务</td></tr><tr><td><code>mcp.server.remote</code></td><td>Reach a remote MCP server</td><td>连接远端 MCP 服务</td></tr><tr><td><code>background.service</code></td><td>Keep a background service running</td><td>保持后台服务运行</td></tr><tr><td><code>bus.publish</code></td><td>Send messages to other plugins</td><td>向其他插件发送消息</td></tr><tr><td><code>bus.subscribe</code></td><td>Receive messages from other plugins</td><td>接收其他插件的消息</td></tr><tr><td><code>browser.cdp</code></td><td>Control the work-panel browser</td><td>控制工作面板浏览器</td></tr><tr><td><code>models.list</code></td><td>List authenticated models</td><td>列出已登录的模型</td></tr><tr><td><code>session.read</code></td><td>Read the current conversation sent to the model</td><td>读取当前发给模型的对话</td></tr><tr><td><code>session.import</code></td><td>Import bounded session history into your declared sources</td><td>导入受限的会话历史到已声明的数据源</td></tr><tr><td><code>session.read.own</code></td><td>Read sessions imported by this plugin</td><td>读取此插件导入的会话</td></tr><tr><td><code>session.update.own</code></td><td>Rename sessions imported by this plugin</td><td>重命名此插件导入的会话</td></tr><tr><td><code>session.delete.own</code></td><td>Trash or purge sessions imported by this plugin</td><td>将此插件导入的会话移入回收站或清除</td></tr><tr><td><code>usage.read</code></td><td>Read usage statistics</td><td>读取用量统计</td></tr><tr><td><code>agent.complete</code></td><td>Run a one-shot completion with your models</td><td>用你的模型发起一次补全</td></tr><tr><td><code>speech.adapter.register</code></td><td>Register a speech adapter</td><td>注册语音适配器</td></tr><tr><td><code>audio.capture.background</code></td><td>Use the microphone in the background</td><td>后台使用麦克风</td></tr><tr><td><code>audio.playback.background</code></td><td>Play audio in the background</td><td>后台播放声音</td></tr><tr><td><code>keyboard.globalShortcut</code></td><td>Register system-wide shortcuts</td><td>注册系统级快捷键</td></tr><tr><td><code>net.websocket</code></td><td>Open real-time connections</td><td>建立实时双向连接</td></tr></tbody></table><h2 id="_5-adding-permissions-on-upgrade" tabindex="-1">5. Adding permissions on upgrade <a class="header-anchor" href="#_5-adding-permissions-on-upgrade" aria-label="Permalink to &quot;5. Adding permissions on upgrade&quot;">​</a></h2><p>If new permissions appear on upgrade:</p><ol><li>Compute the diff</li><li>Force user confirmation</li><li>If not confirmed, cancel the upgrade or disable the new capabilities (canceling the upgrade is recommended)</li></ol><h2 id="_6-runtime-check-pseudocode" tabindex="-1">6. Runtime check pseudocode <a class="header-anchor" href="#_6-runtime-check-pseudocode" aria-label="Permalink to &quot;6. Runtime check pseudocode&quot;">​</a></h2><div class="language-ts vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">assertPermission</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(pluginId, perm) {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> if</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> (</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">!</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">granted</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(pluginId, perm)) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">throw</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> ERROR_PERMISSION_DENIED</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>Every Host API entry point must assert first. A file entry point then passes three more gates, in this order — a later gate can only refuse, never widen:</p><div class="language-ts vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">assertFsAccess</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(pluginId, mode, requestedPath, sessionId) {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> assertPermission</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(pluginId, </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">\`fs.\${</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">mode</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">}\`</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)              </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// declared AND granted</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> full </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> realpathWithinRoot</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">root</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(pluginId, mode, sessionId), requestedPath)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> if</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> (</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">!</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">full) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">throw</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> NOT_FOUND</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> INVALID_ARGUMENT</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">         // symlinks resolved first</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> if</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> (</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">isDenied</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(full) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">||</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> isHostReserved</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(full)) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">throw</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> ERROR_PERMISSION_DENIED</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> if</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> (</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">!</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">inScope</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(full, </span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">declaredScope</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(pluginId, mode))) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">await</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> confirmWithUser</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">...</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p>The <code>workspace</code> root is the invoking tool session&#39;s project, falling back to the visible workspace for a panel call (ADR 0266).</p><h2 id="_7-acceptance" tabindex="-1">7. Acceptance <a class="header-anchor" href="#_7-acceptance" aria-label="Permalink to &quot;7. Acceptance&quot;">​</a></h2><ol><li>Unauthorized API calls fail</li><li>Permission copy is visible in the install UI, and a file permission shows its declared scope alongside</li><li>Upgrades that add permissions prompt the user</li><li>A write or delete outside the declared scope prompts, and a denial is audited as <code>PERMISSION_DENIED</code></li><li><code>.env</code> and <code>.git/**</code> stay unreadable under a whole-tree read scope, and do not appear in <code>fs.glob</code> results either</li><li>A symlink inside the root pointing outside it does not carry an access out</li><li>A delete lands in the OS trash, refuses a non-empty directory, and is interrupted past 50 removals in a rolling minute</li><li>A plugin declaring only the legacy <code>fs.*.workspace</code> names loses write and delete reach, and the Plugins page says so</li></ol></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("spec/07-plugins/13-plugin-permissions-matrix.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _13PluginPermissionsMatrix = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _13PluginPermissionsMatrix as default
};
