import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0284: Headless runtime boundary in packages/host-runtime","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0284-headless-runtime-boundary.md","filePath":"adr/0284-headless-runtime-boundary.md","lastUpdated":1789728753000}');
const _sfc_main = { name: "adr/0284-headless-runtime-boundary.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0284-headless-runtime-boundary-in-packages-host-runtime" tabindex="-1">ADR 0284: Headless runtime boundary in <code>packages/host-runtime</code> <a class="header-anchor" href="#adr-0284-headless-runtime-boundary-in-packages-host-runtime" aria-label="Permalink to &quot;ADR 0284: Headless runtime boundary in \`packages/host-runtime\`&quot;">​</a></h1><ul><li>Status: Accepted for implementation</li><li>Date: 2026-09-18</li><li>Decision: D447</li><li>Related: ADR 0205 (D373 / D374 / D375), ADR 0213 (D386), <code>02-architecture/05-remote-agent-control.md</code> §4 and §11, <code>03-runtime/07-process-model.md</code> §4, <code>06-delivery/07-remote-control-rollout.md</code> R2</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Rollout R1 delivered the headless Agent Host module (<code>packages/agent-host</code>): admission, the turn queue, the approval broker, the event log, and the snapshot builder run without Electron. Everything underneath it did not. The stdio transports to host-core and the agent sidecar, the restart supervisor, the durable turn lifecycle (<code>session.beginTurn</code> → prompt → <code>session.endTurn</code>), transcript persistence, the in-flight reply checkpoint, and approved Plan/Goal execution all lived in <code>apps/desktop/electron/main</code>, and the Electron <code>AgentHost</code> bridge reached them by invoking the desktop&#39;s own IPC handlers. <code>HostProcess</code> resolved the binary from <code>process.resourcesPath</code>, and <code>AgentSidecar</code> could only start the sidecar as <code>process.execPath</code> + <code>ELECTRON_RUN_AS_NODE</code>.</p><p>R2 needs the same runtime on a machine that has no Electron: the <code>pi-host</code> bundle (<code>02-architecture/05-remote-agent-control.md</code> §5.2) must run the Agent Host module, the sidecar, and host-core behind a RACP server. Re-implementing the turn lifecycle in a second place would create a second source of truth for exactly the invariants the desktop spent a year fixing (abort locks, stale terminal events, single-flight finalization, queue release after durable settlement).</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><strong>A new workspace package, <code>packages/host-runtime</code>, owns the Electron-independent runtime layer.</strong> It depends on <code>shared</code>, <code>agent-host</code>, and <code>agent-runtime</code> only, and never on <code>electron</code> or on <code>apps/desktop</code>. Its modules are: <ul><li><code>HostProcess</code> and <code>AgentSidecar</code>: the stdio NDJSON JSON-RPC transports, moved from Electron main unchanged in behavior. Both take their launch as options — <code>binaryPath</code> / <code>dataDir</code> / extra <code>env</code> for host-core, <code>{ command, args, env }</code> for the sidecar — and an <code>onStderr</code> sink. The sidecar&#39;s <code>host.proxy</code> allowlist, local-tool short circuit, Plan-mode gate, vendor-auth binding check, and trusted-extension bridge are unchanged. <code>HostProcess</code> accepts an optional <code>diagnoseFailure</code> hook so an embedding host can name a boot refusal it can phrase.</li><li><code>RuntimeSupervisor</code>: the restart policy of process-model §4 (0.5s → 1s → 2s capped at 4s, three restarts per two-minute window, single flight per child, never during shutdown, stop on the first unrecoverable failure). The embedding host supplies <code>start</code>, <code>afterRestart</code>, <code>isUnrecoverable</code>, and an event sink.</li><li><code>RuntimeService</code>: <code>RuntimePort</code> for the Agent Host module on top of the two transports — prompt admission with the durable turn row and the user message appended before the sidecar starts, steer, stop, abort with the abort lock, asktool answers, manual compaction, and <code>finishTurn</code> with the same claim-before-await rule, stale-terminal guard, and settlement announcement as Electron main.</li><li><code>TurnEventPipeline</code> and <code>TurnPersistence</code>: the per-event pass (tool-call tracking, D299 checkpoints, terminal events, completed rows) and an in-memory ordered append queue with a bounded retry while host-core restarts. A <code>pi-host</code> process ends only with its supervisor, so the desktop&#39;s file-backed outbox is not duplicated.</li><li><code>createHeadlessLaunchResolver</code>: launch resolution from host-core&#39;s own registries (providers and secrets, the effective command shell, project instructions and memory, user skills, subagent definitions). It refuses vendor OAuth accounts and plugin agents, which need the desktop.</li><li><code>PlanExecutionDispatcher</code>: approved Plan/Goal execution (D189) with the same claim, no-replay, and finalization rules.</li><li>Host-core adapters for the module&#39;s <code>SessionPort</code>, <code>QueueStore</code> (schema v15), and <code>permissions.pending</code>, plus the session-message ledger lookup, the inflight checkpointer, and the plan-execution decoders, moved out of Electron main.</li></ul></li><li><strong>Electron main keeps thin adapters.</strong> <code>electron/main/host-process.ts</code> and <code>agent-sidecar.ts</code> subclass the package classes and add only what Electron knows: the packaged binary and bundled-plugin locations, the <code>ELECTRON_RUN_AS_NODE</code> launch, redacted stderr logging, and the schema-too-new / glibc diagnoses. <code>runtime/lifecycle.ts</code> drives the shared supervisor and keeps the renderer status pushes, plugin locale resync, and approved-plan drain. The Agent Host bridge uses the shared host-core ports instead of its own copies. The local prompt path (<code>agent-ipc.ts</code>), with its attachments, slash expansion, plugin tools, MCP relay, vendor accounts, and notifications, is unchanged and still owned by the desktop.</li><li><strong><code>TurnStartRequest</code> gains an optional <code>userMessageId</code></strong> so a client can keep its optimistic user row id through the headless runtime, exactly as the renderer does through IPC (D288). Additive; no caller is required to send it.</li><li><strong>No wire contract changes.</strong> Electron IPC, the sidecar JSON-RPC, host-core RPC, and the Plugin SDK are untouched; the desktop&#39;s observable behavior, defaults, and persisted data are unchanged.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li><code>pi-host</code> (R2) can compose <code>HostProcess + AgentSidecar + RuntimeService + AgentHost + RuntimeSupervisor</code> in plain Node; the RACP server binds to the module, never to Electron IPC.</li><li>The restart policy, the turn lifecycle, and the transports now have unit tests that run without Electron (<code>packages/host-runtime/src/*.test.ts</code>).</li><li>The desktop&#39;s source-contract tests that pinned <code>host-process.ts</code> and <code>agent-sidecar.ts</code> now read the package sources; the renderer-facing status and diagnosis assertions still read the Electron adapters.</li><li>Local Electron main still runs its own prompt path through IPC. Moving the desktop onto <code>RuntimeService</code> for local sessions is a later, behavior-preserving step; it is not required for R2 and is not done here.</li><li>The headless resolver deliberately supports less than the desktop: no prompt attachments, no plugin tools, no desktop MCP relay, no vendor OAuth. Those return typed errors rather than degrading silently.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Extract only the transports and re-implement the turn lifecycle in <code>pi-host</code>.</strong> Rejected: the lifecycle invariants are the hard part, and a second copy would drift from the desktop&#39;s.</li><li><strong>Keep the modules in <code>apps/desktop/electron/main</code> and import them from <code>pi-host</code>.</strong> Rejected: <code>packages/*</code> must not depend on desktop implementation code, and the modules would keep growing Electron imports.</li><li><strong>Put the runtime into <code>packages/agent-host</code>.</strong> Rejected: the module is the transport-free semantic core with no process or filesystem knowledge, and the RACP server and integrations depend on it staying that way.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0284-headless-runtime-boundary.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0284HeadlessRuntimeBoundary = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0284HeadlessRuntimeBoundary as default
};
