import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0052: Plan operating state and approval boundary","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0052-plan-operating-state-and-approval-boundary.md","filePath":"adr/0052-plan-operating-state-and-approval-boundary.md","lastUpdated":1786032712000}');
const _sfc_main = { name: "adr/0052-plan-operating-state-and-approval-boundary.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0052-plan-operating-state-and-approval-boundary" tabindex="-1">ADR 0052: Plan operating state and approval boundary <a class="header-anchor" href="#adr-0052-plan-operating-state-and-approval-boundary" aria-label="Permalink to &quot;ADR 0052: Plan operating state and approval boundary&quot;">​</a></h1><ul><li>Status: Superseded by ADR 0053</li><li>Date: 2026-07-30</li><li>Baseline: <code>0.4.13</code></li><li>Protocol: v7</li><li>Storage schema: v8</li></ul><p>ADR 0053 replaces this checkpoint with immutable unique host-written <code>.pi/plan/*.md</code> artifacts, approve/reject-only resolution, the existing <code>plan_approvals</code> artifact/execution fields, and a startup interruption fence with no replay. The structured proposal/request-changes details below remain historical context for the superseded design.</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>PI-Desktop&#39;s former product selector treated Chat and Agent as two tool profiles. That vocabulary made a planning workflow ambiguous: a plan could be described as another agent, a planner model, or a read-only permission mode. Those interpretations would duplicate the pi runtime or put authorization in the renderer.</p><p>The required workflow is one pi Agent that can inspect a task, submit a structured plan, wait for a separate user decision, revise from feedback, and continue execution only after approval. The workflow crosses the durable session model, pi tool composition, Rust authorization, host RPC, renderer IPC, plugin registration, storage recovery, and scheduled execution.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><h3 id="_1-one-agent-two-operating-states" tabindex="-1">1. One Agent, two operating states <a class="header-anchor" href="#_1-one-agent-two-operating-states" aria-label="Permalink to &quot;1. One Agent, two operating states&quot;">​</a></h3><p>The product selector is exactly <code>Agent | Plan</code>. There is one pi Agent per session. Plan is that Agent after it enters planning state; it is not a second Agent, planner service, planner model, or permission mode. Agent remains the default for new sessions and new scheduled tasks.</p><p>The durable session mode is <code>agent | plan</code>. The live planning state is a projection of the durable mode, runtime, and host approval record:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Agent / inactive</span></span>
<span class="line"><span>  -&gt; Plan / planning</span></span>
<span class="line"><span>  -&gt; Plan / awaiting_approval</span></span>
<span class="line"><span>  -&gt; Agent / inactive after approval, same Agent continues</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>The user may select Plan while idle. The same Agent may also call <code>EnterPlanMode</code> while executing. Both paths converge on the same host-validated Plan state. <code>ExitPlanMode</code> submits a structured plan and is the only assistant tool call allowed in its batch.</p><p>The internal renderer <code>page = &quot;chat&quot;</code> value may remain as the conversation surface route. It is not an operating mode and must not appear in mode selectors, mode commands, or authorization decisions.</p><h3 id="_2-host-owned-durable-authority" tabindex="-1">2. Host-owned durable authority <a class="header-anchor" href="#_2-host-owned-durable-authority" aria-label="Permalink to &quot;2. Host-owned durable authority&quot;">​</a></h3><p>Rust host-core is authoritative for:</p><ul><li>resolving <code>sessions.mode</code> from the durable <code>sessionId</code> on every tool call;</li><li>enforcing the Plan/Agent tool policy before permission modes and grants;</li><li>creating and resolving durable plan approval records;</li><li>committing the Plan → Agent transition with the selected permission mode;</li><li>emitting normalized approval/state events and applying timeout/recovery;</li><li>scheduled/unattended policy and stable error codes.</li></ul><p>Renderer state and sidecar mode fields are projections or diagnostic context. A conflicting mode supplied by Electron or the sidecar cannot authorize a tool. A stale renderer cannot clear Plan or grant execution.</p><h3 id="_3-plan-tool-and-permission-policy" tabindex="-1">3. Plan tool and permission policy <a class="header-anchor" href="#_3-plan-tool-and-permission-policy" aria-label="Permalink to &quot;3. Plan tool and permission policy&quot;">​</a></h3><p>Plan exposes:</p><ul><li><code>Read</code>, <code>Glob</code>, <code>Grep</code>, and <code>BrowserPreview</code>;</li><li><code>Bash</code>, governed by the durable permission mode;</li><li><code>ExitPlanMode</code> (this list also carried <code>CompactContext</code>, removed by ADR 0061 and restored by ADR 0064 as <code>new_context</code>).</li></ul><p>Plan denies <code>Write</code>, <code>Edit</code>, every plugin tool, and unknown tools, regardless of permission mode, session grant, manifest risk, or stale IPC state. Agent keeps the existing <code>Read</code>, <code>Glob</code>, <code>Grep</code>, <code>Write</code>, <code>Edit</code>, <code>Bash</code>, and registered plugin policy.</p><p>Plan retains permission-mode selection. Bash prompts under <code>ask</code> and <code>accept-edits</code>; Bash under <code>auto</code> runs without confirmation and may mutate the workspace or scratch directory. BrowserPreview is the explicit read-only UI inspection exception. Plan therefore expresses planning intent, not a strict read-only security profile. The UI must state this tradeoff.</p><h3 id="_4-separate-plan-approval-transaction" tabindex="-1">4. Separate plan approval transaction <a class="header-anchor" href="#_4-separate-plan-approval-transaction" aria-label="Permalink to &quot;4. Separate plan approval transaction&quot;">​</a></h3><p><code>ExitPlanMode</code> creates a host-owned <code>plan_approvals</code> row with request, session, turn, tool-call, structured plan, deadline, and pending status. The host emits the request and waits on an in-memory one-shot channel; the row preserves the proposal and final outcome but does not make a dead Agent resumable.</p><p>Approval is not a generic tool permission. <code>plans.resolve</code> accepts only a matching live request/session/turn. Approval requires an explicit target permission mode, defaults to <code>ask</code> in the UI, and commits atomically:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>BEGIN</span></span>
<span class="line"><span>  plan_approvals: pending -&gt; approved</span></span>
<span class="line"><span>  sessions.mode: plan -&gt; agent</span></span>
<span class="line"><span>  sessions.permission_mode: selected explicit mode</span></span>
<span class="line"><span>  append audit record</span></span>
<span class="line"><span>COMMIT</span></span>
<span class="line"><span>wake ExitPlanMode</span></span>
<span class="line"><span>start a new model turn with Agent tools</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><p>Requesting changes requires non-empty feedback, records the outcome, returns the feedback to the same Agent as a Plan tool result, and leaves the session in Plan. Reject records the outcome, stops the run, and leaves Plan active.</p><p>Timeout, abort, persistence failure, host crash, sidecar crash, and stale responses fail closed. Full process restart marks pending approvals <code>interrupted</code>, aborts associated turns, keeps sessions in Plan, and rejects old responses. Renderer reload may restore only a request backed by a live host waiter.</p><h3 id="_5-migration-and-protocol" tabindex="-1">5. Migration and protocol <a class="header-anchor" href="#_5-migration-and-protocol" aria-label="Permalink to &quot;5. Migration and protocol&quot;">​</a></h3><p>Schema v8 is a transactional v7 migration. It maps persisted session mode, app default mode, and scheduled task mode values from <code>chat</code> to <code>plan</code>, keeps transcripts/turns/permissions, adds <code>plan_approvals</code>, and leaves schema v7 authoritative on failure. New defaults remain Agent. Protocol v7 carries the <code>plan | agent</code> union, plan state events, structured approval events, and <code>plans.pending</code> / <code>plans.resolve</code> RPCs.</p><h3 id="_6-scheduled-and-plugin-policy" tabindex="-1">6. Scheduled and plugin policy <a class="header-anchor" href="#_6-scheduled-and-plugin-policy" aria-label="Permalink to &quot;6. Scheduled and plugin policy&quot;">​</a></h3><p>Plan is interactive-only in this release. A scheduled or unattended Plan run fails before the provider request with <code>PLAN_REQUIRES_INTERACTIVE_SESSION</code>; no background process displays or auto-approves a plan. Existing scheduled Chat values migrate to Plan and require an explicit switch to Agent before running unattended.</p><p>Plugin agent tools are Agent-only contributions. Plan hides and denies them at the host boundary even when their manifest risk is low or permission is granted. Plugin commands and panels remain explicit user UI contributions, but cannot become model-callable Plan tools.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><h3 id="positive" tabindex="-1">Positive <a class="header-anchor" href="#positive" aria-label="Permalink to &quot;Positive&quot;">​</a></h3><ul><li>Planning preserves one Agent context and avoids a second planner lifecycle.</li><li>Durable host authorization cannot be bypassed by renderer or sidecar state.</li><li>Users can choose the post-approval permission posture explicitly.</li><li>Feedback, recovery, migration, plugin denial, and scheduled behavior are observable and testable through stable protocol/storage contracts.</li></ul><h3 id="tradeoffs" tabindex="-1">Tradeoffs <a class="header-anchor" href="#tradeoffs" aria-label="Permalink to &quot;Tradeoffs&quot;">​</a></h3><ul><li>Plan is not a strict mutation-free mode because Auto Bash can mutate. This is intentional and must be visible in the product copy and approval UX.</li><li>A full process crash discards an in-flight Agent wait; the proposal is kept as an interrupted record, but the user must submit a new plan.</li><li>Protocol and schema version bumps require synchronized host, sidecar, main, renderer, migration, and compatibility work.</li></ul><h2 id="alternatives-rejected" tabindex="-1">Alternatives rejected <a class="header-anchor" href="#alternatives-rejected" aria-label="Permalink to &quot;Alternatives rejected&quot;">​</a></h2><h3 id="a-second-planner-agent-or-model" tabindex="-1">A second planner Agent or model <a class="header-anchor" href="#a-second-planner-agent-or-model" aria-label="Permalink to &quot;A second planner Agent or model&quot;">​</a></h3><p>Rejected because it duplicates context, introduces a second approval/runtime boundary, and conflicts with the requirement that feedback and approval return to the same Agent.</p><h3 id="plan-as-a-permission-mode-or-strict-read-only-profile" tabindex="-1">Plan as a permission mode or strict read-only profile <a class="header-anchor" href="#plan-as-a-permission-mode-or-strict-read-only-profile" aria-label="Permalink to &quot;Plan as a permission mode or strict read-only profile&quot;">​</a></h3><p>Rejected because planning intent and authorization posture are distinct. Plan must retain permission selection and Bash behavior, including Auto&#39;s explicit mutation tradeoff.</p><h3 id="renderer-owned-mode-or-approval-state" tabindex="-1">Renderer-owned mode or approval state <a class="header-anchor" href="#renderer-owned-mode-or-approval-state" aria-label="Permalink to &quot;Renderer-owned mode or approval state&quot;">​</a></h3><p>Rejected because stale or forged IPC could grant execution and renderer reload would lose the authoritative transition. Rust owns durable mode, policy, and approval identity.</p><h3 id="command-text-classification-to-permit-bash-in-plan" tabindex="-1">Command-text classification to permit Bash in Plan <a class="header-anchor" href="#command-text-classification-to-permit-bash-in-plan" aria-label="Permalink to &quot;Command-text classification to permit Bash in Plan&quot;">​</a></h3><p>Rejected because a general shell command cannot be proven read-only reliably. The existing permission mode is the explicit control; Plan&#39;s Write/Edit/plugin denials remain exact tool policy.</p><h2 id="related-docs" tabindex="-1">Related docs <a class="header-anchor" href="#related-docs" aria-label="Permalink to &quot;Related docs&quot;">​</a></h2><ul><li><code>docs/spec/00-baseline.md</code></li><li><code>docs/spec/03-runtime/01-ipc-protocol.md</code></li><li><code>docs/spec/03-runtime/02-agent-runtime.md</code></li><li><code>docs/spec/03-runtime/03-tools-and-permissions.md</code></li><li><code>docs/spec/03-runtime/04-data-storage.md</code></li><li><code>docs/spec/03-runtime/05-host-core-rust.md</code></li><li><code>docs/spec/03-runtime/06-host-rpc-protocol.md</code></li><li><code>docs/spec/03-runtime/07-process-model.md</code></li><li><code>docs/spec/03-runtime/08-error-codes.md</code></li><li><code>docs/spec/03-runtime/10-session-state-machine.md</code></li><li><code>docs/spec/04-ux/03-permission-ux.md</code></li><li><code>docs/spec/04-ux/04-builtin-commands.md</code></li><li><code>docs/spec/04-ux/06-settings-ia.md</code></li><li><code>docs/spec/04-ux/08-component-spec.md</code></li><li><code>docs/spec/04-ux/09-interaction-patterns.md</code></li><li><code>docs/spec/05-security/01-security.md</code></li><li><code>docs/spec/07-plugins/04-plugin-security.md</code></li><li><code>docs/spec/06-delivery/04-e2e-test-plan.md</code></li><li><code>docs/spec/08-meta/decisions-log.md</code> (D188)</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0052-plan-operating-state-and-approval-boundary.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0052PlanOperatingStateAndApprovalBoundary = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0052PlanOperatingStateAndApprovalBoundary as default
};
