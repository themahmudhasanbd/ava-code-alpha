import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0168: Main-owned http(s)/mailto allowlist for openExternal","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0168-main-owned-open-external-allowlist.md","filePath":"adr/0168-main-owned-open-external-allowlist.md","lastUpdated":1788686344000}');
const _sfc_main = { name: "adr/0168-main-owned-open-external-allowlist.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0168-main-owned-http-s-mailto-allowlist-for-openexternal" tabindex="-1">ADR 0168: Main-owned http(s)/mailto allowlist for <code>openExternal</code> <a class="header-anchor" href="#adr-0168-main-owned-http-s-mailto-allowlist-for-openexternal" aria-label="Permalink to &quot;ADR 0168: Main-owned http(s)/mailto allowlist for \`openExternal\`&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-06</li><li>Deciders: PI-Desktop core</li><li>Related: D330, ADR 0109, GitHub pull request #45</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Chat markdown, plugin panels, the plugin launcher, OAuth login, and the embedded preview all eventually call Electron <code>shell.openExternal</code>. The main window and launcher <code>setWindowOpenHandler</code> previously forwarded the raw URL. A prompt-injected <code>file:</code>, <code>javascript:</code>, <code>ms-msdt:</code>, or custom URI scheme would reach the operating-system protocol handler after a user click.</p><p>Plugin <code>pi.shell.openExternal</code> already documented HTTP(S) and <code>mailto:</code> (ADR 0109). Preview and plugin-view handlers used a <code>/^https?:/i</code> prefix check, which is weaker than a parsed URL and did not cover the app shell.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>Electron Main owns one parser (<code>parseAllowedExternalUrl</code>). A URL reaches <code>shell.openExternal</code> only when <code>new URL</code> yields <code>http:</code>, <code>https:</code> with a hostname and a written <code>//</code>, or <code>mailto:</code> with an address. The opener receives the normalized href. Control characters fail closed.</li><li>Disallowed URLs throw. Callers that cannot surface the error (window-open handlers) catch and ignore. OAuth treats a rejection as &quot;browser did not open&quot;. Plugins keep <code>INVALID_ARGUMENT</code>.</li><li>Workspace files stay on <code>shell.openPath</code> after the existing path gate. Preview &quot;open in browser&quot; uses the allowlist for http(s)/mailto and <code>openPath</code> for an in-root <code>file:</code> page.</li><li>Main-constructed URLs (GitHub feedback, releases) still pass the parser.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Custom URI schemes from chat, plugins, or <code>window.open</code> cannot launch OS handlers.</li><li><code>mailto:</code> remains a supported plugin and link target.</li><li><code>https:alert(1)</code>-style special-scheme filling is rejected.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><h3 id="http-https-only-blocking-mailto" tabindex="-1">http/https only, blocking mailto <a class="header-anchor" href="#http-https-only-blocking-mailto" aria-label="Permalink to &quot;http/https only, blocking mailto&quot;">​</a></h3><p>Rejected: it would break the documented plugin API and ADR 0109.</p><h3 id="prefix-regex-https-i" tabindex="-1">Prefix regex <code>/^https?:/i</code> <a class="header-anchor" href="#prefix-regex-https-i" aria-label="Permalink to &quot;Prefix regex \`/^https?:/i\`&quot;">​</a></h3><p>Rejected: it matches <code>https:payload</code> without a host and diverges from the plugin runtime&#39;s <code>://</code> check.</p><h3 id="returning-false-instead-of-throwing" tabindex="-1">Returning <code>false</code> instead of throwing <a class="header-anchor" href="#returning-false-instead-of-throwing" aria-label="Permalink to &quot;Returning \`false\` instead of throwing&quot;">​</a></h3><p>Rejected: OAuth already maps a rejected <code>openExternal</code> promise to <code>opened: false</code>. A silent <code>false</code> return reports success.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0168-main-owned-open-external-allowlist.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0168MainOwnedOpenExternalAllowlist = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0168MainOwnedOpenExternalAllowlist as default
};
