import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0246: Opt-in subagent inheritance of the parent tool catalog","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0246-subagent-opt-in-parent-tool-inherit.md","filePath":"adr/0246-subagent-opt-in-parent-tool-inherit.md","lastUpdated":1789353684000}');
const _sfc_main = { name: "adr/0246-subagent-opt-in-parent-tool-inherit.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0246-opt-in-subagent-inheritance-of-the-parent-tool-catalog" tabindex="-1">ADR 0246: Opt-in subagent inheritance of the parent tool catalog <a class="header-anchor" href="#adr-0246-opt-in-subagent-inheritance-of-the-parent-tool-catalog" aria-label="Permalink to &quot;ADR 0246: Opt-in subagent inheritance of the parent tool catalog&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-14</li><li>Deciders: PI-Desktop core</li><li>Related: ADR 0062, ADR 0089, ADR 0100, D201, D415, issue #215, PR #319</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>ADR 0062 made a subagent a bounded worker: it could only receive tools from <code>SUBAGENT_ASSIGNABLE_TOOLS</code> (Read, Glob, Grep, BrowserPreview, Bash, Edit, Write). Plugin tools, <code>Skill</code>, <code>ToolSearch</code>, mode tools, and <code>Task</code> were out of reach on purpose so a definition remained the place a reader could see what a delegate may do.</p><p>Permission mode (<code>permission: inherit</code>) and the session model (empty <code>model</code> pin) already follow the parent. Tool capability did not. A parent that had loaded <code>Skill</code>, MCP, or plugin tools could not give any of that to a worker it just spawned. Builtins that stay on today&#39;s whitelist (explorer, code-reviewer) are still the right default; user-defined workers that <em>should</em> share the parent catalog had no opt-in.</p><p>Always inheriting would make every delegation as dangerous as the session. Putting <code>Skill</code> alone on the assignable list would still leave MCP and plugin tools as a second catalog.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><p>Frontmatter may opt a definition into parent-tool inheritance:</p><div class="language-yaml vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">tools</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">inherit</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># or</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">tools</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: [</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">inherit</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">Bash</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">]</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p><code>inherit</code> sets <code>SubagentDefinition.inheritTools</code>. Extra names still have to be on <code>SUBAGENT_ASSIGNABLE_TOOLS</code>. Builtins do not opt in.</p></li><li><p>At <code>Task</code> spawn, <code>resolveSubagentToolNames</code> unions the session runtime&#39;s <strong>live <code>toolCatalog</code> keys</strong> (including deferred plugin/MCP tools the parent is allowed to call) with any declared extras, then drops <code>SUBAGENT_INHERIT_DENY_TOOLS</code>:</p><table tabindex="0"><thead><tr><th>Never inherited</th><th>Why</th></tr></thead><tbody><tr><td><code>Task</code> / <code>TaskWait</code> / <code>TaskList</code> / <code>TaskStop</code></td><td>no nested fan-out</td></tr><tr><td><code>EnterPlanMode</code> / <code>EnterGoalMode</code></td><td>mode stays the parent&#39;s</td></tr><tr><td><code>asktool</code></td><td>a delegate has no user</td></tr><tr><td><code>new_context</code></td><td>compaction is a parent-runtime flag</td></tr><tr><td><code>ToolSearch</code></td><td>activates deferred tools on the parent catalog</td></tr></tbody></table><p>The child receives the full allowed catalog without <code>ToolSearch</code>. Inherit is stronger than the parent&#39;s first-request active set; that is the point of handing Skill/MCP/plugin tools to a worker.</p></li><li><p>Spawn-time resolved names drive the delegate tool list, mutation framing, search/edit/Bash guidance, and — when <code>Skill</code> is present — the same <code># Skills</code> catalog the parent already received. The Task catalog prints <code>inherit</code> (plus extras) rather than dumping every MCP name.</p></li><li><p>host-core&#39;s user-subagent scanner keeps the <code>inherit</code> token in <code>tools</code> so <code>tools: inherit</code> alone still appears in <code>agents.active</code> and round-trips through Settings. The editor exposes an inherit checkbox; saving must not strip the token.</p></li><li><p>Default documents that omit <code>tools</code> stay read-only (<code>Read, Glob, Grep</code>). <code>tools: &quot;*&quot;</code> still means the seven assignable tools, not the session catalog. A session cannot lend mutation to a read-only delegate unless that definition opted into inherit.</p></li></ol><p>This amends ADR 0062 §2 and the rejected alternative &quot;Let delegates inherit the parent&#39;s tools&quot;: inherit is now an explicit document opt-in with a deny list, not silent session equality.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A user-owned worker can call Skill/MCP/plugin tools the parent already had, without turning explorer into a nested Agent.</li><li>Inherit of Bash/Edit/Write is inheriting mutation. Permission mode and the external-path gate are unchanged; <code>permission: inherit</code> still follows the session.</li><li>Definitions remain the readable grant: <code>tools: inherit</code> is visible in the Markdown and in Settings.</li><li>Child <code>ToolSearch</code> / <code>new_context</code> cannot mutate parent runtime state.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Always inherit for user agents, never for builtins.</strong> Rejected as a hidden default. Markdown opt-in is the same shape as <code>permission: inherit</code>.</li><li><strong>Inherit only currently active tools.</strong> Rejected because MCP/plugin tools are deferred behind <code>ToolSearch</code>; a worker would still lack the catalog the issue asked for, and inheriting <code>ToolSearch</code> would activate tools on the parent.</li><li><strong>Add <code>Skill</code> to <code>SUBAGENT_ASSIGNABLE_TOOLS</code> only.</strong> Rejected: no MCP/plugin tools, still a second catalog.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0246-subagent-opt-in-parent-tool-inherit.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0246SubagentOptInParentToolInherit = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0246SubagentOptInParentToolInherit as default
};
