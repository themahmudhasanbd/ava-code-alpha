import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"14. Secrets Storage","description":"","frontmatter":{},"headers":[],"relativePath":"spec/03-runtime/14-secrets-storage.md","filePath":"spec/03-runtime/14-secrets-storage.md","lastUpdated":1790042669000}');
const _sfc_main = { name: "spec/03-runtime/14-secrets-storage.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="_14-secrets-storage" tabindex="-1">14. Secrets Storage <a class="header-anchor" href="#_14-secrets-storage" aria-label="Permalink to &quot;14. Secrets Storage&quot;">​</a></h1><h2 id="_1-goal" tabindex="-1">1. Goal <a class="header-anchor" href="#_1-goal" aria-label="Permalink to &quot;1. Goal&quot;">​</a></h2><p>Store provider credentials and future sensitive tokens safely under Rust host ownership, with zero raw secret leakage to renderer logs/UI persistence.</p><h2 id="_2-ownership" tabindex="-1">2. Ownership <a class="header-anchor" href="#_2-ownership" aria-label="Permalink to &quot;2. Ownership&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Concern</th><th>Owner</th></tr></thead><tbody><tr><td>secret write/read/delete</td><td>Rust host-core</td></tr><tr><td>secret metadata index</td><td>SQLite <code>secrets_meta</code></td></tr><tr><td>OS secure storage integration</td><td>Rust host-core</td></tr><tr><td>vendor-account login/refresh orchestration</td><td>Electron main (<code>oauth.ts</code>)</td></tr><tr><td>renderer knowledge</td><td><code>hasSecret</code> / <code>hasOauth</code> booleans and a non-secret account label</td></tr></tbody></table><p>Node pi sidecar may receive secret <strong>ephemerally in-memory</strong> for a run via host RPC, never persisted by sidecar. For a vendor-account provider it receives even less: a short-lived <code>ModelAuth</code> resolved per request, never the OAuth refresh token (§10).</p><h2 id="_3-backends" tabindex="-1">3. Backends <a class="header-anchor" href="#_3-backends" aria-label="Permalink to &quot;3. Backends&quot;">​</a></h2><h3 id="primary" tabindex="-1">Primary <a class="header-anchor" href="#primary" aria-label="Permalink to &quot;Primary&quot;">​</a></h3><ul><li><strong>Electron/OS safe storage style backend</strong> mediated by host</li><li>macOS: Keychain-backed path preferred for first release</li></ul><h3 id="fallback" tabindex="-1">Fallback <a class="header-anchor" href="#fallback" aria-label="Permalink to &quot;Fallback&quot;">​</a></h3><p>If primary backend unavailable:</p><ol><li>encrypt secret blob with machine-local key material</li><li>store ciphertext under app data</li><li>mark <code>backend=file_fallback</code> in metadata</li><li>surface security warning in settings</li></ol><p>MVP must implement both paths with automatic selection.</p><h2 id="_4-data-model" tabindex="-1">4. Data model <a class="header-anchor" href="#_4-data-model" aria-label="Permalink to &quot;4. Data model&quot;">​</a></h2><div class="language-ts vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">type</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> SecretMeta</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  secretRef</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  providerId</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  kind</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;api_key&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;bearer_token&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;azure_api_key&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;custom&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  backend</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;safeStorage&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;file_fallback&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  updatedAt</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// raw value never appears in SQLite tables</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// raw value never appears in IPC list/get provider responses</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br></div></div><p><code>secretRef</code> formats:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>secret:provider:&lt;providerId&gt;:api_key</span></span>
<span class="line"><span>secret:provider:&lt;providerId&gt;:oauth</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>The two refs are independent, so one provider row may hold an API key, a vendor account, or both. The OAuth ref stores the serialized pi-ai <code>OAuthCredential</code> (access token, refresh token, expiry) written through the generic <code>secrets.set</code> path, so it is encrypted by the same backend but is not indexed in <code>secrets_meta</code>; provider delete clears both refs and any metadata row for them.</p><h2 id="_4a-provider-readiness-flags" tabindex="-1">4a. Provider readiness flags <a class="header-anchor" href="#_4a-provider-readiness-flags" aria-label="Permalink to &quot;4a. Provider readiness flags&quot;">​</a></h2><table tabindex="0"><thead><tr><th>flag</th><th>meaning</th></tr></thead><tbody><tr><td><code>hasSecret</code></td><td>the row has <strong>either</strong> credential — an API key or a vendor account</td></tr><tr><td><code>hasOauth</code></td><td>the row has a vendor-account credential</td></tr><tr><td><code>oauthAccountLabel</code></td><td>non-secret display label for the signed-in account (from <code>config_json.oauth.accountLabel</code>)</td></tr></tbody></table><p><code>hasSecret</code> deliberately stayed the single readiness signal, so model pickers, composer guards, and provider lists needed no new condition when vendor accounts arrived. <code>hasOauth</code> only drives presentation: the account badge, and hiding the API key input on a vendor row.</p><h2 id="_5-host-rpc" tabindex="-1">5. Host RPC <a class="header-anchor" href="#_5-host-rpc" aria-label="Permalink to &quot;5. Host RPC&quot;">​</a></h2><ul><li><code>secrets.set</code> <code>{ secretRef, value, meta }</code></li><li><code>secrets.delete</code> <code>{ secretRef }</code></li><li><code>secrets.has</code> <code>{ secretRef } -&gt; boolean</code></li><li><code>secrets.getForRuntime</code> <code>{ secretRef, reason, runId }</code> <strong>internal only</strong> (main/host → not exposed to renderer)</li></ul><h3 id="renderer-facing-surface" tabindex="-1">Renderer-facing surface <a class="header-anchor" href="#renderer-facing-surface" aria-label="Permalink to &quot;Renderer-facing surface&quot;">​</a></h3><p>Renderer uses provider methods that accept optional <code>secretValue</code> on create/update and only reads <code>hasSecret</code>.</p><h2 id="_6-access-rules" tabindex="-1">6. Access rules <a class="header-anchor" href="#_6-access-rules" aria-label="Permalink to &quot;6. Access rules&quot;">​</a></h2><ol><li>Renderer cannot list raw secrets</li><li>Logs redaction: mask values matching secret patterns / known secret refs</li><li><code>getForRuntime</code> requires active run context and is audited</li><li>Export excludes secrets by default</li><li>Uninstall/reset app deletes secrets unless future explicit migrate tool says otherwise</li><li>Provider delete defaults to deleting linked secret — both the API key and the OAuth credential</li><li>An OAuth refresh token never crosses a process boundary: only Electron main reads it, and only to mint request auth</li><li>Portable configuration sync exports provider API keys or MCP environment/header values only after explicit credential opt-in. Values remain inside host-owned encrypted staging and are restored through the receiving device&#39;s local secret store. WebDAV credentials, machine encryption keys, OAuth sessions, and cookies are never portable.</li></ol><h2 id="_7-redaction-policy" tabindex="-1">7. Redaction policy <a class="header-anchor" href="#_7-redaction-policy" aria-label="Permalink to &quot;7. Redaction policy&quot;">​</a></h2><p>Never write to logs:</p><ul><li>Authorization headers</li><li>api keys</li><li>bearer tokens</li><li>query params named <code>key</code>/<code>token</code>/<code>api_key</code></li></ul><p>Replace with:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>***REDACTED***</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><h2 id="_8-failure-modes" tabindex="-1">8. Failure modes <a class="header-anchor" href="#_8-failure-modes" aria-label="Permalink to &quot;8. Failure modes&quot;">​</a></h2><table tabindex="0"><thead><tr><th>case</th><th>behavior</th></tr></thead><tbody><tr><td>set fails</td><td>provider update fails atomically if secret required</td></tr><tr><td>backend downgrades to fallback</td><td>warn once per session in settings</td></tr><tr><td>missing secret at run</td><td><code>PROVIDER_SECRET_MISSING</code></td></tr><tr><td>decrypt failure</td><td>treat as missing + prompt re-enter</td></tr></tbody></table><h2 id="_9-acceptance-criteria" tabindex="-1">9. Acceptance criteria <a class="header-anchor" href="#_9-acceptance-criteria" aria-label="Permalink to &quot;9. Acceptance criteria&quot;">​</a></h2><ul><li>[ ] set/has/delete works on native macOS arm64 and Intel x64 paths</li><li>[ ] renderer never receives raw secret on provider list/get</li><li>[ ] runtime can fetch secret ephemerally for a turn</li><li>[ ] logs do not contain raw key material in normal failure tests</li><li>[ ] fallback backend works when primary unavailable (dev/test harness)</li><li>[ ] a vendor-account row reports <code>hasSecret</code> and <code>hasOauth</code> without exposing the credential</li></ul><h2 id="_10-vendor-account-credentials" tabindex="-1">10. Vendor-account credentials <a class="header-anchor" href="#_10-vendor-account-credentials" aria-label="Permalink to &quot;10. Vendor-account credentials&quot;">​</a></h2><p>Electron main owns login, account-removal, and refresh orchestration (ADR 0095, D237, D240) because pi-ai declares it app-owned. <code>oauth.ts</code> implements pi-ai&#39;s <code>CredentialStore</code> on top of <code>secrets.getForRuntime</code> / <code>secrets.set</code> / <code>secrets.delete</code>, serializing <code>modify</code> per provider so pi-ai&#39;s locked-refresh assumption holds across concurrent turns. Each OAuth provider row gets its own collection and store scope; two rows with the same vendor key never share a credential or refresh lock.</p><p>Request auth flows one way only:</p><ol><li>The launch payload for an <code>authKind: &quot;oauth&quot;</code> row carries <code>apiKey: &quot;&quot;</code>. Main does not read the credential at launch.</li><li>Per request, the runtime calls the host-proxy method <code>provider.resolveAuth</code> with <code>{ sessionId, providerId }</code>.</li><li>Electron main answers it locally — the call is <strong>never forwarded to host-core</strong> — after checking the pair against the per-launch binding table. An unbound provider fails with <code>PROVIDER_NOT_BOUND</code>.</li><li>The reply is a short-lived <code>ModelAuth</code> (<code>apiKey</code>, <code>headers</code>, <code>baseUrl</code>), refreshed under the store lock only when it has expired.</li></ol><p>The sidecar therefore holds a revocable, roughly hour-long token scoped to the one provider its session is bound to — strictly less than the long-lived API key it receives for a keyed row.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("spec/03-runtime/14-secrets-storage.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _14SecretsStorage = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _14SecretsStorage as default
};
