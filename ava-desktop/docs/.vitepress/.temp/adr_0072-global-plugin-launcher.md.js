import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0072: Add a global plugin launcher","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0072-global-plugin-launcher.md","filePath":"adr/0072-global-plugin-launcher.md","lastUpdated":1786678051000}');
const _sfc_main = { name: "adr/0072-global-plugin-launcher.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0072-add-a-global-plugin-launcher" tabindex="-1">ADR 0072: Add a global plugin launcher <a class="header-anchor" href="#adr-0072-add-a-global-plugin-launcher" aria-label="Permalink to &quot;ADR 0072: Add a global plugin launcher&quot;">​</a></h1><ul><li>Status: Accepted for implementation</li><li>Date: 2026-08-11</li><li>Deciders: PI-Desktop core</li><li>Related: D211 · <a href="./../spec/04-ux/06-settings-ia">Settings IA</a> · <a href="./../spec/04-ux/09-interaction-patterns">Interaction patterns</a> · E2E-120</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Installed plugin panels are reachable from the Plugins destination, but opening one interrupts the user&#39;s current work and requires pointer navigation. A system-wide shortcut needs a dedicated native window: the normal renderer cannot receive keys while PI-Desktop is unfocused, and the existing plugin panel host should remain the only authority that opens plugin UI.</p><p>Chinese plugin names also need useful keyboard search without requiring an author to add aliases to every manifest.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>Add the shared, customizable <code>openPluginLauncher</code> shortcut, defaulting to <code>Alt+Space</code>. Electron renders it as Option+Space on macOS and Alt+Space on Windows/Linux and registers it with <code>globalShortcut</code> after application boot. Windows reserves Alt+Space for the active window system menu, so host-core also installs a narrow low-level keyboard hook for that exact default binding. The hook consumes the chord and emits a host notification to Electron, allowing the launcher to work while another application is focused. The focused main window remains a last-resort fallback.</li><li>Electron main owns one centered, frameless 620×440 utility window on the display nearest the pointer. ADR 0080 supersedes its original first-use lazy creation with hidden post-boot warm-up. It is non-resizable, absent from the taskbar, always on top while visible, and hides on blur or Escape. macOS uses a panel window visible across workspaces; no platform receives native window controls.</li><li>The sandboxed launcher renderer uses the existing preload and the additive Electron-only toggle, dismiss, and shown channels. It lists plugins through the existing plugin API and opens a result through the existing sandboxed panel host; no plugin permission or host-core boundary changes.</li><li>Only enabled, ready plugins with a panel are candidates. Search normalizes name, id, and description and derives tone-free full pinyin and pinyin initials from the display name. Up/Down selects, Enter or click opens, Escape dismisses, and IME composition never dispatches. Successful opens are remembered in renderer-local device storage (D219): an empty query lists the plugins in most-recently-used order, while a typed query ranks relevance first and uses recency only as a tiebreaker.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A plugin panel can be opened without navigating away from the current task.</li><li>The shortcut remains visible and resettable in Settings → Shortcuts.</li><li><code>pinyin-pro</code> is bundled into renderer output rather than shipped as a runtime package tree.</li><li>A custom shortcut continues to use Electron&#39;s global shortcut API. The host-core hook is enabled only for Windows&#39; reserved default chord, and a hook installation failure is logged; the focused-window fallback remains usable in that case.</li><li>The renderer IPC additions remain Electron-local and additive; the native fallback adds one host method/notification without changing protocol v9 or storage schema v11.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><h3 id="reuse-global-search" tabindex="-1">Reuse global search <a class="header-anchor" href="#reuse-global-search" aria-label="Permalink to &quot;Reuse global search&quot;">​</a></h3><p>Rejected. Global search is part of the main renderer and cannot appear while the application is unfocused without first restoring the entire main window.</p><h3 id="open-plugins-directly-from-a-native-menu" tabindex="-1">Open plugins directly from a native menu <a class="header-anchor" href="#open-plugins-directly-from-a-native-menu" aria-label="Permalink to &quot;Open plugins directly from a native menu&quot;">​</a></h3><p>Rejected. A static menu cannot provide Chinese/pinyin fuzzy search and would need to be rebuilt for every plugin lifecycle change.</p><h3 id="put-pinyin-aliases-in-plugin-manifests" tabindex="-1">Put pinyin aliases in plugin manifests <a class="header-anchor" href="#put-pinyin-aliases-in-plugin-manifests" aria-label="Permalink to &quot;Put pinyin aliases in plugin manifests&quot;">​</a></h3><p>Rejected. It makes search quality depend on every plugin author and duplicates data that can be derived consistently at query time.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0072-global-plugin-launcher.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0072GlobalPluginLauncher = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0072GlobalPluginLauncher as default
};
