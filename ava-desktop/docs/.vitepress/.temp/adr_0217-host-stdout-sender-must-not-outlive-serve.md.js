import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0217: Host Stdout Sender Must Not Outlive Serve","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0217-host-stdout-sender-must-not-outlive-serve.md","filePath":"adr/0217-host-stdout-sender-must-not-outlive-serve.md","lastUpdated":1789070236000}');
const _sfc_main = { name: "adr/0217-host-stdout-sender-must-not-outlive-serve.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0217-host-stdout-sender-must-not-outlive-serve" tabindex="-1">ADR 0217: Host Stdout Sender Must Not Outlive Serve <a class="header-anchor" href="#adr-0217-host-stdout-sender-must-not-outlive-serve" aria-label="Permalink to &quot;ADR 0217: Host Stdout Sender Must Not Outlive Serve&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-11</li><li>Deciders: PI-Desktop core</li><li>Related: D390 / ADR 0216, issue #211</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>ADR 0216 stopped regenerate from shipping the kept transcript as one NDJSON line, and stopped an oversize line from ending the stdin reader. That is necessary but not sufficient for the Windows failure in issue #211.</p><p>On v0.14.6 an NDJSON line over 64 MiB made the stdin reader send an error and break. <code>serve()</code> then dropped its stdout sender and waited for the writer thread. On macOS and Linux that wait returns immediately because nothing else holds the channel. On Windows, <code>keyboard::start</code> stores a strong <code>UnboundedSender</code> in a <code>OnceLock</code> for the Alt+Space hook. That clone keeps the writer thread blocked in <code>blocking_recv</code>, <code>serve()</code> never returns, and host-core stays alive. Electron still considers the child available, so the in-flight call hits the 130 s deadline (<code>host RPC timeout: session.replaceMessages</code>) instead of <code>host-core exited</code>. A second retry also times out. The leftover turn stays <code>running</code>.</p><p>A <code>LIMIT_EXCEEDED</code> reply with a null JSON-RPC id has the same client symptom: Electron ignores <code>id: null</code> as a notification and waits out the deadline.</p><p>Measured on a 4659-message / 76 MB session: <code>session.replaceMessages</code> of the kept prefix is a 56.91 MiB line and finishes in ~565 ms on macOS. The 130 s Windows timeout is therefore not the rewrite cost. A 65 MiB line on the v0.14.6 binary exits in ~50 ms on macOS (<code>EPIPE</code> / process exit).</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>The Windows keyboard hook retains only a <code>WeakUnboundedSender</code>. Sending a shortcut upgrades it. Dropping serve&#39;s last strong sender still closes the writer channel, so host-core can exit after stdin EOF.</li><li>After stdin ends, <code>serve()</code> waits at most 5 s for the stdout writer, then returns even if some other clone leaked. Protocol version stays at 11.</li><li>Electron measures the UTF-8 byte length of each host RPC payload and rejects a line over 64 MiB with <code>LIMIT_EXCEEDED</code> before writing stdin. The constant lives in <code>@pi-desktop/shared</code> and must match host-core.</li><li>If the host still sees an oversize line, it peeks the JSON-RPC id from the truncated prefix so the <code>LIMIT_EXCEEDED</code> reply can be matched.</li></ol><p><code>session.truncateFrom</code> remains the regenerate path (ADR 0216). Whole-transcript <code>session.replaceMessages</code> (message delete, unanswered smart Stop) still exists; the client precheck is the guard if those payloads grow past 64 MiB.</p><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>O(tail) jsonl truncation:</strong> makes regenerate cheaper on huge sessions, but does not explain the 130 s Windows timeout. Deferred.</li><li><strong>Daemonize the stdout writer thread:</strong> process exit would still wait for non-daemon OS threads on some platforms. Rejected in favor of dropping the strong sender.</li><li><strong>Bump protocol version:</strong> host and Electron ship together; nothing a v11 client relies on is removed. Rejected, matching ADR 0216.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Windows host-core exits after stdin EOF the same way macOS/Linux already did.</li><li>An oversized control-pipe write fails immediately with <code>LIMIT_EXCEEDED</code> instead of <code>host RPC timeout</code>.</li><li>A leftover strong sender cannot keep <code>serve()</code> blocked for more than 5 s.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0217-host-stdout-sender-must-not-outlive-serve.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0217HostStdoutSenderMustNotOutliveServe = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0217HostStdoutSenderMustNotOutliveServe as default
};
