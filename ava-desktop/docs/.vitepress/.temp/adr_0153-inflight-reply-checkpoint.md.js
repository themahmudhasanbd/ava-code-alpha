import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0153: Checkpoint the streaming reply beside the transcript","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0153-inflight-reply-checkpoint.md","filePath":"adr/0153-inflight-reply-checkpoint.md","lastUpdated":1788663994000}');
const _sfc_main = { name: "adr/0153-inflight-reply-checkpoint.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0153-checkpoint-the-streaming-reply-beside-the-transcript" tabindex="-1">ADR 0153: Checkpoint the streaming reply beside the transcript <a class="header-anchor" href="#adr-0153-checkpoint-the-streaming-reply-beside-the-transcript" aria-label="Permalink to &quot;ADR 0153: Checkpoint the streaming reply beside the transcript&quot;">​</a></h1><ul><li>Status: Accepted (amended 2026-09-06 by D327)</li><li>Date: 2026-09-05</li><li>Deciders: PI-Desktop core</li><li>Related: D119, D287, D288, D299, D327, E2E-010, E2E-171, E2E-184, ADR 0030, ADR 0041</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Spec 04 makes assistant and tool rows durable at their end events: the agent runtime emits <code>message_end</code> once, and Electron main appends that row through the persistence outbox. Everything the user watches stream before that point lives only in the renderer and in the sidecar&#39;s memory. Agentic replies stream for minutes, and the app is quit or restarted often. A quit, a sidecar crash, or a restart mid-reply therefore lost the entire reply: the boot sweep marked the turn <code>aborted</code>, and the reopened session showed the prompt with nothing under it. Session logs from this week show eight such turns in one day.</p><p>Two smaller defects compounded it. <code>before-quit</code> disposed host-core first and then killed the sidecar outright, so even a reply that had reached its final message could not be persisted. And the renderer&#39;s Stop path, for long sessions, re-read the durable transcript, replaced its live rows with it, and rewrote the transcript from that snapshot; the runtime&#39;s aborted final row was usually still in the outbox, so the rewrite hid the reply immediately and could delete it if the append landed in between. The same rewrite wrote the renderer&#39;s 64 KB display-capped rows back over the full ones.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><strong>Checkpoint file, not transcript lines.</strong> Host-core owns <code>sessions/&lt;id&gt;.inflight.json</code>: one atomically replaced object holding the session&#39;s streaming assistant message and its turn id. Electron main observes <code>message_update</code>, keeps the newest snapshot, and calls <code>session.saveInflightMessage</code> at most every 1.5 s with a trailing write. Appending checkpoints as transcript lines was rejected: a multi-minute reply would leave dozens of near-copies that bloat the file and skew the physical-line paging window.</li><li><strong>Settlement rules.</strong> The final <code>session.appendMessage</code> of the same id removes the file. <code>session.endTurn</code> removes it for <code>completed</code>/<code>error</code>, promotes it as an <code>aborted</code> assistant row when asked to <code>recoverInflight</code> (sidecar gone), and leaves it alone for a plain <code>aborted</code> (user Stop) so the runtime&#39;s own final row can supersede it. Host boot promotes every leftover whose final row never landed. A checkpoint for an id that is already indexed is dropped, so a write still in flight when the final row lands cannot resurrect it. Checkpoints bypass the outbox; delegate replies are not checkpointed.</li><li><strong>Quit settles turns first.</strong> <code>before-quit</code> flushes checkpoints, aborts active turns through the sidecar, and waits at most 2 s for the aborted rows to drain while host-core is alive, then disposes host and sidecar.</li><li><strong>Renderer Stop never rewrites a started reply.</strong> Settling is in-memory; the durable copy is the runtime&#39;s aborted row or the promoted checkpoint. The unanswered-prompt undo and message delete rewrite only from the full durable transcript merged with the live rows, and the undo is re-evaluated on that merge.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Loss on quit or crash is bounded to the last 1.5 s of a reply plus tool rows still running; the reopened session shows the prompt and the partial reply as an <code>aborted</code> row under an <code>aborted</code> turn.</li><li>One small file write per interval per streaming session; nothing new in SQLite. <code>session.endTurn</code> gains <code>recoverInflight</code> and <code>recovered</code>.</li><li>Session delete removes the checkpoint file with the other session files.</li><li>Spec 01 §5.3, 04 §2.1 and §4.7, 06, 07 §5; E2E-171; D299.</li></ul><h2 id="amendment-2026-09-06-—-completed-replies-survive-process-restart-d327" tabindex="-1">Amendment (2026-09-06) — Completed replies survive process restart (D327) <a class="header-anchor" href="#amendment-2026-09-06-—-completed-replies-survive-process-restart-d327" aria-label="Permalink to &quot;Amendment (2026-09-06) — Completed replies survive process restart (D327)&quot;">​</a></h2><p>Issue #42 showed the same user-only transcript after a full quit that D324 fixed for an in-process session switch. Two settlement rules dropped a finished reply that was still only in the outbox:</p><ol><li><code>message_end</code> called <code>settle()</code> before enqueueing, so the host checkpoint was up to 1.5 s stale and a trailing write of the finished snapshot was cancelled.</li><li><code>session.endTurn</code> for <code>completed</code>/<code>error</code> deleted the checkpoint even when that id was not yet indexed. Boot recovery then refused to promote a leftover whose turn was already <code>completed</code>. Combined with a fire-and-forget outbox flush after handshake, a cold <code>session.get</code> painted only user rows.</li></ol><p>Amendment:</p><ul><li>Electron main checkpoints the finished <code>message_end</code> snapshot, then <code>settleIf</code> so a newer turn started during that write is not wiped.</li><li><code>completed</code>/<code>error</code> endTurn deletes the checkpoint only when the final row is already indexed. An unindexed leftover is kept for the outbox or boot.</li><li>Boot/recovery promotion of a leftover whose turn is <code>completed</code> writes the row as <code>complete</code>, not <code>aborted</code>. Other leftovers stay <code>aborted</code>.</li><li>Host handshake <strong>awaits</strong> the persistence outbox drain before the renderer can <code>session.get</code>, then calls <code>session.recoverInflightMessages</code> (amends ADR 0041). Boot recovery skips completed leftovers so the finished outbox row wins. Tool rows in the same outbox are covered by that drain; they are still not checkpointed.</li></ul><p>Quit still waits (bounded) for active turns. A hard kill after the outbox file is written is recovered by the awaited handshake drain. A hard kill before that persist is recovered from the finished checkpoint.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0153-inflight-reply-checkpoint.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0153InflightReplyCheckpoint = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0153InflightReplyCheckpoint as default
};
