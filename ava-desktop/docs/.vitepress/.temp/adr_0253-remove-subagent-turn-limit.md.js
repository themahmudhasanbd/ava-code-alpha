import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0253: Remove the subagent turn limit","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0253-remove-subagent-turn-limit.md","filePath":"adr/0253-remove-subagent-turn-limit.md","lastUpdated":1789403906000}');
const _sfc_main = { name: "adr/0253-remove-subagent-turn-limit.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0253-remove-the-subagent-turn-limit" tabindex="-1">ADR 0253: Remove the subagent turn limit <a class="header-anchor" href="#adr-0253-remove-the-subagent-turn-limit" aria-label="Permalink to &quot;ADR 0253: Remove the subagent turn limit&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-15</li><li>Deciders: PI-Desktop core</li><li>Related: D423, ADR 0062, ADR 0063, ADR 0089, ADR 0119, ADR 0126, ADR 0166, ADR 0189, ADR 0210, <code>03-runtime/02-agent-runtime.md</code> §5f, <code>04-ux/06-settings-ia.md</code> §7, E2E-155, E2E-SUBAGENT-legacy-turn-limit-frontmatter-is-ignored</li><li>Supersedes: the <code>maxTurns</code> clauses of ADR 0062, ADR 0063, ADR 0119, ADR 0126, ADR 0166, and ADR 0210. Historical ADR files are retained. This record removes the field those records described; it does not change their other clauses.</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>ADR 0062 introduced subagents as bounded workers and gave a definition a <code>maxTurns</code> frontmatter cap. ADR 0119 made the key optional — omission, <code>none</code> and <code>0</code> meant unlimited, and an explicit value was clamped to 80. ADR 0126 and ADR 0063 carried the field into the parser and the Subagent editor. ADR 0166 then withdrew the idle and duration watchdogs but deliberately kept <code>maxTurns</code> as the remaining definition-level kill switch, and D328 recorded that &quot;explicit <code>maxTurns</code> and the concurrency cap of 10 stay&quot;. ADR 0210 reused the same loose key-spelling rules for the sibling <code>maxTokens</code> cap.</p><p>The cap could not be reasoned about correctly, because the parent cannot see the delegate&#39;s live work. It cannot tell a delegate that is one turn from converging from one that never will, so the only honest cap would be &quot;large enough&quot;, which is no cap at all. The shipped values were arbitrary (60, 50, 40, 80), the editor defaulted to unlimited anyway, and the failure mode was worse than the problem it solved: a delegate that reached the cap was killed mid-task and surfaced as <code>truncated</code> with a partial report — a state that reads as a failure to the user and to the parent model, and that neither can resume.</p><p>The kill paths that remain are all explicit and observable: the parent agent&#39;s <code>TaskStop</code>, the user&#39;s Stop, and a terminal parent error aborting leftovers (ADR 0189). A turn count is none of those.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><p><strong>Delete the mechanism.</strong> A delegate has no turn limit. It ends when it finishes, when the parent calls <code>TaskStop</code>, when the user Stops, or when a terminal parent error aborts it. There is no <code>truncated</code> outcome and no turn-related termination path.</p></li><li><p><strong>Remove <code>maxTurns</code> from the contract.</strong> The field leaves <code>SubagentDefinition</code>, the frontmatter parser and its invalid/clamped warnings, <code>MAX_SUBAGENT_MAX_TURNS</code>, <code>UserSubagentRecord</code> / <code>UserSubagentInput</code>, the host-core registry (record, input, frontmatter parse, document render, <code>MAX_TURNS_CEILING</code>), the five built-in documents, <code>SUBAGENT_PRESETS</code>, and the Subagent editor&#39;s Advanced disclosure. The host-core input struct keeps ignoring unknown fields, so a renderer that still sends <code>maxTurns</code> does not fail.</p></li><li><p><strong>Legacy documents keep loading.</strong> <code>maxTurns</code>, <code>max-turns</code> and <code>max_turns</code> become unrecognized frontmatter keys and are ignored exactly like every other unknown key: no error, no warning, the definition still resolves, and nothing rewrites the user&#39;s file. A definition that declared a cap therefore loses it silently.</p></li><li><p><strong>Remove the <code>truncated</code> status.</strong> <code>SubagentRunStatus</code> drops it, and the renderer&#39;s <code>SubagentOutcome</code> union, the <code>chat.subagentStatus</code> catalog entry in every locale, and the delegation topology&#39;s &quot;finished with warnings&quot; count drop it with it. <code>timed_out</code> stays in the type even though the watchdogs that produced it are withdrawn (D328), because no other record withdraws it.</p></li><li><p><strong>No protocol and no schema change.</strong> <code>PROTOCOL_VERSION</code> stays 11 and <code>SCHEMA_VERSION</code> stays 16. The removal is tolerated in both directions: an older renderer&#39;s extra <code>maxTurns</code> field is ignored by host-core (the input struct does not deny unknown fields), a newer renderer simply omits it, and <code>truncated</code> is a value the sidecar can no longer emit. Storage was never involved — the registry parses Markdown frontmatter and no column stores the cap.</p></li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A looping delegate now runs until the user Stops it, or the parent calls <code>TaskStop</code>. That exposure already existed for every definition that omitted <code>maxTurns</code>, which was the default and the editor&#39;s own starting state, and D328 shipped with it.</li><li>Existing user documents that relied on the cap lose it without a prompt. The migration path is the one this ADR documents: stop the delegate with Stop or <code>TaskStop</code>, and put the stopping condition in the prompt body, where a delegate is already told when its work is done.</li><li>Settings no longer offers a &quot;no limit&quot; spelling for turns because there is no limit left to express. The Advanced disclosure keeps the model, thinking, output limit and scope fields.</li><li>The built-in definitions lose one frontmatter line each and no longer carry a number that had no derivation.</li><li>E2E-155 steps and the topology&#39;s warning count change with the status and the built-in backstops. The new ignore contract is covered by E2E-SUBAGENT-legacy-turn-limit-frontmatter-is-ignored and by unit tests in <code>packages/shared</code>, <code>packages/agent-runtime</code>, and <code>crates/host-core</code>.</li></ul><h2 id="alternatives-rejected" tabindex="-1">Alternatives rejected <a class="header-anchor" href="#alternatives-rejected" aria-label="Permalink to &quot;Alternatives rejected&quot;">​</a></h2><ul><li><strong>Raise the ceiling, or make every builtin unlimited.</strong> Keeps a knob that cannot be set correctly, keeps the <code>truncated</code> state and its user-visible copy, and still kills a delegate mid-task at an arbitrary point.</li><li><strong>Keep parsing the key and warn on it.</strong> Unknown frontmatter keys are already ignored silently, so a warning would make <code>maxTurns</code> the only key with a special diagnostic while the definition still ignored it. D328&#39;s <code>idle-timeout</code> / <code>max-duration</code> precedent is the opposite case: those keys still exist, they are simply not armed, so they keep their parse warnings.</li><li><strong>Keep the field and never enforce it.</strong> A contract field that does nothing is worse than no field: the editor would keep offering it, the settings API would keep round-tripping it, and <code>truncated</code> would stay in the status union as dead surface the UI must still render.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0253-remove-subagent-turn-limit.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0253RemoveSubagentTurnLimit = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0253RemoveSubagentTurnLimit as default
};
