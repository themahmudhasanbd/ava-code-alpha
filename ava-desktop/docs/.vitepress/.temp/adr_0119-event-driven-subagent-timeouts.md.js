import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0119: Event-Driven Subagent Timeouts","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0119-event-driven-subagent-timeouts.md","filePath":"adr/0119-event-driven-subagent-timeouts.md","lastUpdated":1787843716000}');
const _sfc_main = { name: "adr/0119-event-driven-subagent-timeouts.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0119-event-driven-subagent-timeouts" tabindex="-1">ADR 0119: Event-Driven Subagent Timeouts <a class="header-anchor" href="#adr-0119-event-driven-subagent-timeouts" aria-label="Permalink to &quot;ADR 0119: Event-Driven Subagent Timeouts&quot;">​</a></h1><ul><li>Status: Accepted for implementation; amended by ADR 0129</li><li>Date: 2026-08-24</li><li>Deciders: PI-Desktop core</li><li>Related: D254, ADR 0062 (bounded subagents), ADR 0089 (background delegation), ADR 0129 (idle bounds silence, not slowness)</li></ul><blockquote><p><strong>Amended by ADR 0129.</strong> The event allow-list in decision 2 is superseded: every <code>AgentEvent</code> now re-arms the idle timer, and the idle default is 300 seconds rather than 600. The built-ins named in decision 4 as unlimited now carry turn backstops. The rejection of &quot;use only idle time&quot; below concerns dropping the total-duration ceiling, which ADR 0129 keeps.</p></blockquote><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The original delegate loop used a default turn cap. A delegate that made one or two useful tool calls per turn could therefore be terminated while it was actively working, and there was no way to distinguish a genuinely idle worker from one waiting on a long-running tool.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>A delegate has two independent watchdogs: 600 seconds without activity and 21,600 seconds of total runtime. The total timer includes tool execution.</li><li>Activity is any delegate turn, message, or tool lifecycle event. The idle timer is paused between <code>tool_execution_start</code> and its matching <code>tool_execution_end</code>, so a long Bash call is governed by the tool timeout, not by delegate idleness.</li><li>A watchdog terminates the delegate with <code>timed_out</code> and one of <code>SUBAGENT_IDLE_TIMEOUT</code> or <code>SUBAGENT_DURATION_TIMEOUT</code>. The report carries the latest partial assistant output when one exists. Provider failures, parent aborts, and explicit turn caps retain their existing outcomes.</li><li><code>maxTurns</code> is optional. Omission, <code>none</code>, and <code>0</code> mean unlimited turns; positive explicit values remain a bounded backstop, capped at 80.</li><li>Definitions may override <code>idle-timeout</code> and <code>max-duration</code>. Idle values are clamped to 10–21,600 seconds and duration values to 60–21,600 seconds; non-numeric values warn and use the defaults.</li><li>The built-in <code>explorer</code> gains <code>Bash</code> for bounded read-only inspection such as <code>git log</code>; <code>code-reviewer</code> remains read-only. Because Bash can mutate, Explorer continues to use the existing mutation/permission classification.</li><li>The shared status contract and desktop topology expose <code>timed_out</code>; the renderer labels it in English and Simplified Chinese and presents it as a warning outcome.</li></ol><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Raise the turn cap:</strong> rejected because any fixed cap still terminates an actively working delegate and scales poorly across models and tool density.</li><li><strong>Use only a total duration:</strong> rejected because a provider or delegate can remain stuck indefinitely while still below the overall ceiling.</li><li><strong>Use only idle time:</strong> rejected because continuous model/tool activity could otherwise run without a hard resource ceiling.</li><li><strong>Keep Explorer read-only without Bash:</strong> rejected because it prevents harmless repository inspection commands that the native search tools cannot express; the existing permission path remains authoritative for Bash.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Delegates can run past the previous 20–24 turn behavior when they remain active, while idle and total-runtime failures remain bounded and diagnosable.</li><li>Timeout reports are visible to <code>TaskWait</code>, <code>TaskList</code>, and the delegation topology, including their structured error codes and timing log entries.</li><li>A definition that explicitly sets <code>maxTurns</code> retains a deterministic hard stop for specialized or untrusted workloads.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0119-event-driven-subagent-timeouts.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0119EventDrivenSubagentTimeouts = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0119EventDrivenSubagentTimeouts as default
};
