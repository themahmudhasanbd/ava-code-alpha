import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0160: Shipped locale registry and searchable language picker","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0160-shipped-locale-registry-and-language-picker.md","filePath":"adr/0160-shipped-locale-registry-and-language-picker.md","lastUpdated":1788802493000}');
const _sfc_main = { name: "adr/0160-shipped-locale-registry-and-language-picker.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0160-shipped-locale-registry-and-searchable-language-picker" tabindex="-1">ADR 0160: Shipped locale registry and searchable language picker <a class="header-anchor" href="#adr-0160-shipped-locale-registry-and-searchable-language-picker" aria-label="Permalink to &quot;ADR 0160: Shipped locale registry and searchable language picker&quot;">​</a></h1><ul><li>Status: Accepted (amended by ADR 0182)</li><li>Date: 2026-09-05</li><li>Decision owners: PI-Desktop desktop/i18n maintainers</li><li>Related: D012, D073, ADR 0009</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The General appearance card presented language as three preview cards (Auto / 简体中文 / English). That control does not scale: every new locale needed a hard-coded card, sample glyph, and catalog keys for its name. The product now ships more than two UI locales, starting with Turkish, so the picker and the locale list have to come from one registry.</p><p>Plugin labels retain the <code>en</code> + <code>zh-CN</code> contract with English fallback. The product changelog originally shipped those two locales; ADR 0182 adds Traditional Chinese without requiring plugins to gain every shell locale.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><code>@pi-desktop/i18n</code> owns a <code>supportedLocales</code> registry: id, native name (endonym, never translated), and English name. <code>resolveLocale</code> maps OS tags onto that list (<code>tr</code> / <code>tr-TR</code> → <code>tr</code>, Simplified Chinese tags → <code>zh-CN</code>, Traditional Chinese tags → <code>zh-TW</code>, else <code>en</code>). <code>catalogs[locale]</code> is the lookup used by the renderer, application menu, tray, and native consent dialogs.</li><li><code>AppSettings.language</code> is <code>&quot;auto&quot;</code> plus every registry id. Auto still follows <code>app.getLocale()</code> through the sandboxed preload bridge.</li><li>Settings → General → Appearance keeps Theme as three preview cards. Language is a searchable picker row (same anchored-menu pattern as Font and Service): Auto pinned at the top with the detected native name, then shipped locales by native name. Search matches native name, English name, and locale id.</li><li>Plugin <code>PluginLocalizedString</code> still requires <code>en</code> and <code>zh-CN</code>. Other shell locales fall back to English. The changelog follows shipped shell locales when a translation exists and falls back to English otherwise; its initial <code>en</code> / <code>zh-CN</code> set is amended by ADR 0182 with <code>zh-TW</code>.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Adding a UI locale is a catalog file, a registry row, an <code>AppSettings.language</code> union member, and an Electron locale pack. The picker does not change.</li><li>Native names stay findable even when the current UI language is unknown to the user.</li><li>Plugins and release notes do not have to ship every new shell locale on day one.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><ul><li>Keep language as wrapping preview cards: unreadable past three or four options, and names would still be hard-coded.</li><li>Require plugins to add every shipped locale: breaks existing manifests and is out of proportion to a shell translation.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0160-shipped-locale-registry-and-language-picker.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0160ShippedLocaleRegistryAndLanguagePicker = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0160ShippedLocaleRegistryAndLanguagePicker as default
};
