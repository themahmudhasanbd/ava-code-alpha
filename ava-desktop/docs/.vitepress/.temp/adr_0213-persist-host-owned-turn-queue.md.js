import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0213: Persist the Host-owned turn queue in host-core","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0213-persist-host-owned-turn-queue.md","filePath":"adr/0213-persist-host-owned-turn-queue.md","lastUpdated":1789036405000}');
const _sfc_main = { name: "adr/0213-persist-host-owned-turn-queue.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0213-persist-the-host-owned-turn-queue-in-host-core" tabindex="-1">ADR 0213: Persist the Host-owned turn queue in host-core <a class="header-anchor" href="#adr-0213-persist-the-host-owned-turn-queue-in-host-core" aria-label="Permalink to &quot;ADR 0213: Persist the Host-owned turn queue in host-core&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-10</li><li>Decision: D386</li><li>Related: ADR 0205 (D375), <code>03-runtime/04-data-storage.md</code> §4.6b, <code>03-runtime/06-host-rpc-protocol.md</code>, <code>02-architecture/05-remote-agent-control.md</code> §6</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>D375 moved the per-session prompt queue out of renderer memory into the Host and chose to persist it. The headless Agent Host module (<code>packages/agent-host</code>) owns admission and draining, but it needs a store that survives a restart and never starts work on its own. Rust host-core owns SQLite exclusively (frozen decision 12), so the store is a host-core table and RPC surface, not a file the module writes.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><strong>Schema v15 adds <code>turn_queue</code>.</strong> Columns: <code>id</code>, <code>session_id</code> (FK, cascade), <code>principal</code>, <code>idempotency_key</code>, <code>input_hash</code>, <code>content</code>, <code>attachments_json</code>, <code>permission_mode</code>, <code>position</code>, <code>created_at</code>; a per-session <code>position</code> index and a unique <code>(session_id, principal, idempotency_key)</code> index. The v14→v15 migration is additive: it creates the table and indexes, every existing chain gains the step, and a <code>pi.sqlite.v14.bak</code> copy is kept like every prior migration.</li><li><strong>Four additive RPC methods.</strong> <code>session.queuePush</code> is idempotent on <code>(session, principal, idempotencyKey)</code>, returns the existing entry when the input hash matches, fails with <code>IDEMPOTENCY_CONFLICT</code> when a key is reused with other input, and fails with <code>AGENT_BUSY</code> when the session already holds eight entries. <code>session.queueList</code> returns one session&#39;s entries or every entry in position order. <code>session.queueRemove</code> deletes one entry. <code>session.queuePrioritize</code> moves one entry to the head of its session (the desktop&#39;s &quot;send now&quot;, RACP <code>turn/prioritize</code>). Protocol version stays v11.</li><li><strong>The store never decides execution.</strong> The Agent Host module restores entries when host-core answers, holds every restored session until a controller attaches, and drains one entry only after the active turn&#39;s terminal event. The startup fence still never replays or auto-starts work.</li><li><strong>The renderer&#39;s in-memory queue is retired.</strong> The composer pushes through <code>agent/queue/push</code>, mirrors <code>agent/event/queueChanged</code>, and its &quot;send now&quot; is <code>agent/queue/prioritize</code> followed by a graceful stop.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A reboot no longer loses queued prompts; they reappear, held, in the snapshot&#39;s <code>queuedTurns</code>.</li><li>Every client of a Host sees the same queue once the renderer switch lands.</li><li>Deleting a session cascades to its queue entries.</li><li>Fresh installs and every migration path produce schema v15; the <code>04-data-storage.md</code> chain, the baseline, and the README move to v15.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li>Keep the queue in Agent Host memory, as the D373 draft did. Rejected by D375: a restart loses prompts.</li><li>Persist in the renderer&#39;s <code>localStorage</code>. Rejected: invisible to other clients and to a headless Host.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0213-persist-host-owned-turn-queue.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0213PersistHostOwnedTurnQueue = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0213PersistHostOwnedTurnQueue as default
};
