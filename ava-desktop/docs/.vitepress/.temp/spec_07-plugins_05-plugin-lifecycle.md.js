import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"05. Plugin Lifecycle","description":"","frontmatter":{},"headers":[],"relativePath":"spec/07-plugins/05-plugin-lifecycle.md","filePath":"spec/07-plugins/05-plugin-lifecycle.md","lastUpdated":1789963612000}');
const _sfc_main = { name: "spec/07-plugins/05-plugin-lifecycle.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="_05-plugin-lifecycle" tabindex="-1">05. Plugin Lifecycle <a class="header-anchor" href="#_05-plugin-lifecycle" aria-label="Permalink to &quot;05. Plugin Lifecycle&quot;">​</a></h1><h2 id="_1-goals" tabindex="-1">1. Goals <a class="header-anchor" href="#_1-goals" aria-label="Permalink to &quot;1. Goals&quot;">​</a></h2><p>Define the complete state machine from discovery to uninstall, guaranteeing:</p><ul><li>Predictable behavior</li><li>Recoverable failures</li><li>Auditable start/stop</li><li>Consistency with command palette / AgentTool registration</li></ul><h2 id="_2-state-machine" tabindex="-1">2. State machine <a class="header-anchor" href="#_2-state-machine" aria-label="Permalink to &quot;2. State machine&quot;">​</a></h2><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>discovered</span></span>
<span class="line"><span> → validated</span></span>
<span class="line"><span> → installed</span></span>
<span class="line"><span> → enabled</span></span>
<span class="line"><span> → loaded</span></span>
<span class="line"><span> → running</span></span>
<span class="line"><span> → load_error</span></span>
<span class="line"><span> → disabled</span></span>
<span class="line"><span> → install_error</span></span>
<span class="line"><span> → invalid</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br></div></div><h3 id="state-descriptions" tabindex="-1">State descriptions <a class="header-anchor" href="#state-descriptions" aria-label="Permalink to &quot;State descriptions&quot;">​</a></h3><table tabindex="0"><thead><tr><th>State</th><th>Meaning</th></tr></thead><tbody><tr><td>discovered</td><td>Plugin directory or package scanned</td></tr><tr><td>validated</td><td>manifest / file integrity passed</td></tr><tr><td>installed</td><td>Written to the installed directory and registered</td></tr><tr><td>enabled</td><td>Enabled by the user, allowed to load</td></tr><tr><td>loaded</td><td>Runtime loaded, contribution points registered</td></tr><tr><td>running</td><td>Has an active panel / background logic</td></tr><tr><td>disabled</td><td>Installed but turned off by the user</td></tr><tr><td>load_error</td><td>Load failed after enabling</td></tr><tr><td>install_error</td><td>Install failed</td></tr><tr><td>invalid</td><td>Validation failed, unusable</td></tr></tbody></table><h2 id="_3-lifecycle-hooks" tabindex="-1">3. Lifecycle hooks <a class="header-anchor" href="#_3-lifecycle-hooks" aria-label="Permalink to &quot;3. Lifecycle hooks&quot;">​</a></h2><p><strong>Implemented today:</strong> the runtime (<code>apps/desktop/electron/main/plugin-runtime.ts</code>) invokes <code>onLoad</code> (when a plugin is loaded on load/enable) and <code>onUnload</code> (dispatched into the plugin process on unload/disable/reload/app quit, 5s budget — 1.5s on quit — then the process is stopped); unloading tears down the plugin&#39;s registered commands and tools. The other hooks below are declared in the API but not yet fired.</p><p><strong>Planned:</strong> once the full lifecycle lands, hooks fire in this order:</p><ol><li><code>onInstall</code> (once, only after a successful install)</li><li><code>onEnable</code></li><li><code>onLoad</code></li><li>runtime events</li><li><code>onUnload</code></li><li><code>onDisable</code></li><li><code>onUninstall</code></li></ol><h3 id="invocation-constraints" tabindex="-1">Invocation constraints <a class="header-anchor" href="#invocation-constraints" aria-label="Permalink to &quot;Invocation constraints&quot;">​</a></h3><ul><li>Hooks must be able to time out (default 5s, configurable)</li><li>A hook exception must not crash the host</li><li>If <code>onLoad</code> fails, enter <code>load_error</code> and automatically roll back the contribution points already registered</li></ul><h2 id="_3-1-resident-services" tabindex="-1">3.1 Resident services <a class="header-anchor" href="#_3-1-resident-services" aria-label="Permalink to &quot;3.1 Resident services&quot;">​</a></h2><p>A service declared in <code>contributes.services</code> is driven by the host, not by the plugin&#39;s own hooks, so its window is strictly inside the plugin&#39;s lifetime:</p><ol><li><code>onLoad</code> completes and contribution points are registered</li><li>For each declared service (at most 4 per plugin, gated on <code>background.service</code>), the broker calls <code>service.start</code> in the plugin process with a 5s budget. A start failure marks that one service <code>failed</code> and leaves the rest of the plugin loaded.</li><li>On unload / disable / reload, <code>service.stop</code> runs <strong>before</strong> <code>onUnload</code>, so the service is quiet while the plugin still has its API</li></ol><p>Status per service is <code>starting</code> | <code>running</code> | <code>stopped</code> | <code>failed</code> plus a restart count, readable over the plugin IPC surface and shown on the Plugins page.</p><h3 id="restart-policy" tabindex="-1">Restart policy <a class="header-anchor" href="#restart-policy" aria-label="Permalink to &quot;Restart policy&quot;">​</a></h3><p>The service lives in the plugin&#39;s host process, so a crash takes it with the process. The supervisor then restarts the whole plugin:</p><ul><li>Backoff <code>1s, 2s, 4s, 8s, 16s</code>, capped at 30s</li><li>At most 5 restarts; after that the plugin stays down in <code>failed</code> so the user sees the failure instead of a silent crash loop</li><li>A process that survives 60s is considered healthy and the backoff resets to zero</li><li><code>autoRestart: false</code> on a service opts its plugin out of restarts entirely</li><li>Restarts are skipped when the user re-enabled or removed the plugin while the backoff timer was pending</li></ul><p>Manual enable / disable always wins over the supervisor: an explicit action clears the pending timer and the attempt counter.</p><h3 id="app-quit" tabindex="-1">App quit <a class="header-anchor" href="#app-quit" aria-label="Permalink to &quot;App quit&quot;">​</a></h3><p>Quitting stops every plugin host <strong>as a shutdown</strong>, not as a crash. Each plugin is marked as disposing and its pending restarts are cancelled before anything else, then services stop and <code>onUnload</code> runs, in parallel across plugins.</p><p>This is what separates the two exits: a host process that dies without being marked is reported as a crash, which means an error log, a &quot;stopped unexpectedly&quot; toast, and a supervisor scheduling restarts into an app that is closing. None of that may happen on a clean quit.</p><p><strong>A crash report carries the diagnosis.</strong> The crash path reports the host process&#39;s exit code because on Windows a hard fault (<code>0xC0000005</code> and friends, which Electron hands over as a negative signed int) and a plugin&#39;s own <code>process.exit(1)</code> are different bugs. The exit code reaches the load error, the <code>failed</code> service state, the <code>plugin.crash</code> audit record and the <code>plugin</code> log channel. Plugin stdout/stderr is not copied into the crash report because it may contain workspace data or secrets. Nothing new is persisted, and no new permission or API surface is involved.</p><p>The sequence is bounded — <code>onUnload</code> gets 1.5s per plugin and teardown 3s in total, after which the children are killed outright. A plugin&#39;s cleanup must never be the reason the app appears to hang on quit.</p><h2 id="_4-enable-disable-semantics" tabindex="-1">4. Enable / disable semantics <a class="header-anchor" href="#_4-enable-disable-semantics" aria-label="Permalink to &quot;4. Enable / disable semantics&quot;">​</a></h2><h3 id="enable" tabindex="-1">enable <a class="header-anchor" href="#enable" aria-label="Permalink to &quot;enable&quot;">​</a></h3><ul><li>Set state to enabled</li><li>Attempt load</li><li>Success: register commands / tools / skills / themes / MCP servers, then start resident services</li><li>Failure: automatically fall back to disabled and surface the error to the user. This is frozen by D017 (enable→load failure auto-disables the plugin).</li></ul><h3 id="disable" tabindex="-1">disable <a class="header-anchor" href="#disable" aria-label="Permalink to &quot;disable&quot;">​</a></h3><ul><li>Unregister commands / tools</li><li>Close panel</li><li>Stop resident services and disconnect MCP servers</li><li>Cancel any pending restart backoff</li><li>Call <code>onUnload</code> / <code>onDisable</code></li><li>Persist as disabled</li></ul><h2 id="_5-startup-recovery" tabindex="-1">5. Startup recovery <a class="header-anchor" href="#_5-startup-recovery" aria-label="Permalink to &quot;5. Startup recovery&quot;">​</a></h2><p>On app startup:</p><ol><li>Scan installed plugins</li><li>Read the enabled state</li><li>Load only enabled plugins</li><li>Skip a single failed plugin without affecting other plugins or the main app</li></ol><h2 id="_6-developer-mode" tabindex="-1">6. Developer mode <a class="header-anchor" href="#_6-developer-mode" aria-label="Permalink to &quot;6. Developer mode&quot;">​</a></h2><p><code>dev-loaded</code> plugins:</p><ul><li>Not copied to <code>installed</code></li><li>Reference the local path directly</li><li>Can watch and hot reload</li><li>Reload flow: <code>unload → validate → load</code></li></ul><p>On hot reload:</p><ul><li>Preserve plugin settings as much as possible</li><li>Panel in-memory state is not guaranteed to be preserved</li></ul><h2 id="_7-contribution-point-register-unregister-transaction" tabindex="-1">7. Contribution-point register/unregister transaction <a class="header-anchor" href="#_7-contribution-point-register-unregister-transaction" aria-label="Permalink to &quot;7. Contribution-point register/unregister transaction&quot;">​</a></h2><p>Each plugin&#39;s load process should be approximately transactional:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>begin</span></span>
<span class="line"><span> register commands</span></span>
<span class="line"><span> register tools</span></span>
<span class="line"><span> register skills</span></span>
<span class="line"><span> register themes</span></span>
<span class="line"><span> register MCP servers (lazy connect)</span></span>
<span class="line"><span>commit</span></span>
<span class="line"><span> start resident services</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><p>On mid-way failure:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>rollback all registrations from this plugin</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Avoid a half-loaded state where &quot;the command exists but the tool does not&quot;.</p><h2 id="_8-audit-events" tabindex="-1">8. Audit events <a class="header-anchor" href="#_8-audit-events" aria-label="Permalink to &quot;8. Audit events&quot;">​</a></h2><p>Record at least:</p><ul><li>plugin.install</li><li>plugin.uninstall</li><li>plugin.enable</li><li>plugin.disable</li><li>plugin.load.success</li><li>plugin.load.error</li><li>plugin.unload</li><li>plugin.crash</li><li>plugin.service.start / plugin.service.stop</li><li>plugin.service.restart / plugin.service.restart.scheduled</li><li>plugin.services.skipped (missing permission or over the per-plugin cap)</li></ul><p>Fields:</p><ul><li>pluginId</li><li>version</li><li>source (<code>installed</code> | <code>dev</code> | <code>marketplace</code>)</li><li>ts</li><li>errorCode?</li><li>attempt? / delayMs? (service restarts)</li><li>exitCode? (crash), exitCodeHex? (Windows hard fault)</li></ul><h2 id="_9-uninstall-strategy" tabindex="-1">9. Uninstall strategy <a class="header-anchor" href="#_9-uninstall-strategy" aria-label="Permalink to &quot;9. Uninstall strategy&quot;">​</a></h2><p>Before uninstall:</p><ol><li>disable + unload</li><li>Call <code>onUninstall</code></li><li>Delete installed files</li><li>Clean up plugin-private data (may ask the user whether to keep it)</li></ol><p>Default recommendation:</p><ul><li>Clean up settings/data on uninstall (D016: uninstall deletes plugin data by default)</li><li>Provide a &quot;keep data&quot; advanced option (can be deferred)</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("spec/07-plugins/05-plugin-lifecycle.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _05PluginLifecycle = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _05PluginLifecycle as default
};
