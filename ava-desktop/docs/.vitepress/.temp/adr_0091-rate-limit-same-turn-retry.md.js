import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0091: Route provider rate limits through bounded same-turn retry","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0091-rate-limit-same-turn-retry.md","filePath":"adr/0091-rate-limit-same-turn-retry.md","lastUpdated":1787217111000}');
const _sfc_main = { name: "adr/0091-rate-limit-same-turn-retry.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0091-route-provider-rate-limits-through-bounded-same-turn-retry" tabindex="-1">ADR 0091: Route provider rate limits through bounded same-turn retry <a class="header-anchor" href="#adr-0091-route-provider-rate-limits-through-bounded-same-turn-retry" aria-label="Permalink to &quot;ADR 0091: Route provider rate limits through bounded same-turn retry&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-08-14 (amended 2026-08-19)</li><li>Deciders: PI-Desktop core</li><li>Related: D233, D245, E2E-149, ADR 0050, D186</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>A provider HTTP 429 is transient, but it can happen both before a response is established and after a stream has already started. The old implementation split those cases between pi-ai&#39;s one setup retry and the runtime&#39;s one mid-stream replay. That exposed persistent but still-recoverable rate limits to the user too early, and made the effective budget depend on which phase failed.</p><p>OpenCode&#39;s session retry policy provides the behavioral reference: rate limits are retried silently with a finite maximum, provider retry headers take precedence over client backoff, the wait is abortable, and only the exhausted failure is rendered. PI-Desktop must keep that behavior without importing OpenCode or multiplying retries through pi-ai&#39;s own wrapper.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><code>PROVIDER_RATE_LIMITED</code> owns a runtime retry budget of five retries after the initial provider attempt. The budget is shared by request setup and stream recovery, so a turn has at most six provider attempts regardless of where the 429s occur. pi-ai&#39;s nested retry is disabled for this path.</li><li>Each 429 retry is silent and abortable. Delay precedence is <code>retry-after-ms</code>, <code>retry-after</code> seconds, <code>retry-after</code> HTTP-date, then OpenCode-style exponential backoff starting at 2 seconds with up to 25% positive jitter. Every server or calculated delay is capped at 30 seconds. The runtime captures status and headers from the underlying fetch because pi-ai&#39;s normal response callback does not expose failed responses.</li><li>A 429 before streaming is retried by the provider stream adapter. A 429 after streaming starts is replayed by the runtime after removing the failed assistant from model context. Both paths use the same controller and the same budget. The captured response status is applied before classifying the provider message in both phases, so a generic 429 body still enters the 429 budget while known non-retryable classifications remain terminal. The main session and every builtin subagent use this policy.</li><li>Intermediate assistant errors, lifecycle end events, and duplicate assistant bubbles are suppressed. A recovered attempt reuses the original visible assistant message id and emits one terminal lifecycle. The failed attempt is not persisted into the next model context.</li><li>Authentication, model-selection, malformed-request, context, and other non-retryable failures do not enter the 429 path. Existing non-429 transient behavior remains bounded separately: one setup retry and one mid-stream replay.</li><li>After the fifth 429 retry fails, the normal assistant error and lifecycle error are emitted once. Diagnostics retain the redacted provider error, <code>phase</code>, <code>providerStatus</code>, bounded wait/timing fields, and <code>retryAttempt: 5</code>. Aborting during a wait cancels the timer and prevents a later provider request.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Short provider rate-limit bursts recover without an error card, toast, or manual action, whether the 429 arrives during setup or streaming.</li><li>Persistent rate limiting remains bounded: six total attempts, at most five backoffs, and a maximum 30-second individual wait. The final failure stays visible and actionable.</li><li>Retry headers from providers such as OpenAI-compatible gateways are honored, while malformed or excessive values cannot hold a turn indefinitely.</li><li>Main sessions and subagents cannot drift into different rate-limit policies, and lifecycle consumers receive one coherent assistant turn.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><ul><li>Keep pi-ai&#39;s setup retry and add another runtime retry. Rejected: nested wrappers multiply attempts and make the budget phase-dependent.</li><li>Retry every provider failure five times. Rejected: credentials, model ids, malformed requests, and context overflow are not repaired by repetition.</li><li>Keep one same-turn retry and rely on the UI&#39;s manual action. Rejected: it surfaces transient 429s too early and defeats the OpenCode-style behavior requested for the provider boundary.</li></ul><h2 id="references" tabindex="-1">References <a class="header-anchor" href="#references" aria-label="Permalink to &quot;References&quot;">​</a></h2><ul><li>OpenCode session retry behavior (behavioral reference only)</li><li><code>docs/spec/03-runtime/02-agent-runtime.md</code> §5d</li><li><code>docs/spec/03-runtime/08-error-codes.md</code></li><li><code>docs/spec/06-delivery/04-e2e-test-plan.md</code> E2E-149</li><li><code>docs/spec/08-meta/decisions-log.md</code> D245</li><li><code>packages/agent-runtime/src/provider-retry.ts</code></li><li><code>packages/agent-runtime/src/runtime.ts</code></li><li><code>packages/agent-runtime/src/subagent.ts</code></li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0091-rate-limit-same-turn-retry.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0091RateLimitSameTurnRetry = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0091RateLimitSameTurnRetry as default
};
