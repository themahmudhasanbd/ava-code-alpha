import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR tray-session-shortcuts: Bounded session navigation in the native tray","description":"","frontmatter":{},"headers":[],"relativePath":"adr/tray-session-shortcuts.md","filePath":"adr/tray-session-shortcuts.md","lastUpdated":1789828397000}');
const _sfc_main = { name: "adr/tray-session-shortcuts.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-tray-session-shortcuts-bounded-session-navigation-in-the-native-tray" tabindex="-1">ADR tray-session-shortcuts: Bounded session navigation in the native tray <a class="header-anchor" href="#adr-tray-session-shortcuts-bounded-session-navigation-in-the-native-tray" aria-label="Permalink to &quot;ADR tray-session-shortcuts: Bounded session navigation in the native tray&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-13</li><li>Related: Issue #293, ADR 0078, ADR 0016, E2E-TRAY-bounded-session-navigation</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The resident tray exposes only Open and Quit. Session pins and archives are renderer-owned presentation metadata; Host owns session records and the durable notification inbox. Background turns continue after the main window is hidden or closed, so a renderer-only menu snapshot would become stale.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>Extend the existing native menu with Running, Unread, and Pinned groups in that order. Assign each eligible session to its highest-priority group before allocating rows: every non-empty group keeps up to three rows, then the share smaller groups leave unused goes to the groups that still overflow, in the same priority order, up to nine rows in total. A single busy group can therefore fill the whole menu when the others are empty, while no group is ever crowded out below its own share. Hide empty groups. Follow the existing session sort preference; unread results use newest-first inbox order and the same latest-result/read rule as sidebar outcome badges.</li><li>Renderer mirrors its session pin/archive/order metadata, archived project paths, and effective session sort through the main-window-only <code>tray/setSessionPreferences</code> IPC. Main validates it and retains only an ephemeral copy; localStorage remains the owner of organization preferences. No groups are shown until the first preference synchronization.</li><li>A Main-owned tray service reads <code>session.list</code> and <code>notification.list</code> (the existing 200-record inbox), consumes root agent lifecycle/status events, and refreshes after session/inbox mutations. It coalesces pending reads, rejects obsolete host generations, and clears shortcuts on a failed refresh. Windows/Linux hover/right-click retries a failed read. macOS does not subscribe to mouse-enter, which would replace the native status item. It continues to refresh with no renderer attached; deleting a session or archiving it or its project removes the shortcut.</li><li>A session click restores/focuses the main window and sends <code>tray/event/sessionActivated { sessionId }</code> (<code>null</code> means View more) only after the renderer finishes bootstrap and acknowledges menu readiness. Recheck existence and archive state after asynchronous waits. Renderer uses normal <code>selectSession</code>, including project alignment, navigation ownership, and read acknowledgement.</li><li>Opening the macOS tray menu does not restore the window or mark results read. Single-click opens the attached menu; double-click and Open retain window restore behavior. Each overflowing group has View more, which restores the main window and expands the existing session sidebar. Titles are one line and capped at 32 display columns including an ellipsis, counting an East Asian wide or emoji code point as two; labels use the active shipped locale. Quit retains confirmation and shutdown.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Two additive allowlisted desktop IPC channels; the preferences setter is excluded from local MCP control and rejected for non-main-window senders.</li><li>No database migration, host protocol version change, persisted preference format change, Plugin SDK change, new timers, or renderer persistence owner.</li><li>Tray reuses existing organization sorting and latest-result helpers without changing their renderer behavior. Main retains its 1,500-line budget.</li><li>Native menu rendering and cross-platform activation require the documented E2E scenario; unit/build results alone do not prove OS menu behavior.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/tray-session-shortcuts.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const traySessionShortcuts = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  traySessionShortcuts as default
};
