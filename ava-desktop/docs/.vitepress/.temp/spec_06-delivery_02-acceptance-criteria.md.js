import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"02. Acceptance Criteria","description":"","frontmatter":{},"headers":[],"relativePath":"spec/06-delivery/02-acceptance-criteria.md","filePath":"spec/06-delivery/02-acceptance-criteria.md","lastUpdated":1789282627000}');
const _sfc_main = { name: "spec/06-delivery/02-acceptance-criteria.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="_02-acceptance-criteria" tabindex="-1">02. Acceptance Criteria <a class="header-anchor" href="#_02-acceptance-criteria" aria-label="Permalink to &quot;02. Acceptance Criteria&quot;">​</a></h1><blockquote><p>Language: English (per ADR 0009). Sign-off state: M1–M4 accepted 2026-07-25 per <code>docs/project/BOARD.md</code> validation snapshot; M5 items tracked in <a href="./01-mvp-milestones">milestones</a>. Evidence keys: <code>auto:&lt;script&gt;</code> = automated check, <code>manual:M&lt;n&gt;</code> = verified during that milestone&#39;s exit review, <code>open</code> = not yet verified.</p></blockquote><h2 id="_1-mvp-acceptance-statement" tabindex="-1">1. MVP acceptance statement <a class="header-anchor" href="#_1-mvp-acceptance-statement" aria-label="Permalink to &quot;1. MVP acceptance statement&quot;">​</a></h2><p>MVP passes when:</p><blockquote><p>On macOS, a user can configure a model, open a project, complete one permission-gated task that uses tools, and restore the session after an app restart.</p></blockquote><h2 id="_2-functional-acceptance" tabindex="-1">2. Functional acceptance <a class="header-anchor" href="#_2-functional-acceptance" aria-label="Permalink to &quot;2. Functional acceptance&quot;">​</a></h2><h3 id="a-app-startup" tabindex="-1">A. App startup <a class="header-anchor" href="#a-app-startup" aria-label="Permalink to &quot;A. App startup&quot;">​</a></h3><ul><li>[x] App launches and shows the main window — auto:<code>test:e2e:boot</code></li><li>[x] Main↔renderer bridge works — auto:<code>test:e2e:boot</code> (sandboxed preload + IPC round-trip)</li><li>[x] App version info is exposed — auto:<code>test:e2e:boot</code> (version + host protocol)</li><li>[x] First-run inline checklist appears on a fresh profile — manual:M2</li></ul><h3 id="b-model-configuration" tabindex="-1">B. Model configuration <a class="header-anchor" href="#b-model-configuration" aria-label="Permalink to &quot;B. Model configuration&quot;">​</a></h3><ul><li>[x] A provider can be added — auto:<code>test:e2e</code> (provider CRUD)</li><li>[x] An API key can be saved — auto:<code>test:e2e</code> (secret set/get via host)</li><li>[x] Key survives restart (no re-entry needed) — manual:M2 (safeStorage-backed store)</li><li>[x] Clear blocking message when no model is configured — manual:M2 (<code>MODEL_NOT_CONFIGURED</code>)</li></ul><h3 id="c-sessions-and-streaming-chat" tabindex="-1">C. Sessions and streaming chat <a class="header-anchor" href="#c-sessions-and-streaming-chat" aria-label="Permalink to &quot;C. Sessions and streaming chat&quot;">​</a></h3><ul><li>[x] New session can be created — auto:<code>test:e2e</code></li><li>[x] Message can be sent — auto:<code>test:e2e</code> (live model, when key provided)</li><li>[x] Assistant output streams token-by-token — auto:<code>test:e2e</code> / <code>e2e-agent-live</code></li><li>[x] Generation can be aborted — manual:M2 (abort → partial output preserved)</li><li>[x] Switching between history sessions works — manual:M2</li><li>[x] An idle session can be forked into an independent conversation — auto:host-core + desktop contract tests</li></ul><h3 id="d-workspace" tabindex="-1">D. Workspace <a class="header-anchor" href="#d-workspace" aria-label="Permalink to &quot;D. Workspace&quot;">​</a></h3><ul><li>[x] A project directory can be selected — manual:M3</li><li>[x] UI shows the current project — manual:M3</li><li>[x] Tool paths resolve against the project root — auto:host-core tests (<code>workspace::tests</code>)</li></ul><h3 id="e-tools-and-permissions" tabindex="-1">E. Tools and permissions <a class="header-anchor" href="#e-tools-and-permissions" aria-label="Permalink to &quot;E. Tools and permissions&quot;">​</a></h3><ul><li>[x] Plan and Goal deny Write/Edit/plugin tools under every permission mode — auto:<code>test:e2e:plan</code> E2E-105 + host-core permission tests</li><li>[x] Plan and Goal Bash prompt under Ask/Accept edits and run without confirmation under explicit Auto — auto:<code>test:e2e:plan</code> E2E-105 + host-core permission tests</li><li>[x] Agent mode uses permission policy for Write/Edit/Bash — manual:M3</li><li>[x] Permission timeout (120s) becomes deny — manual:M3 (D005)</li><li>[x] Read/Glob/Grep work inside the project — auto:<code>test:e2e</code> (glob tool)</li><li>[x] Write/Edit/Bash trigger an inline, session-scoped confirmation card — manual:M3</li><li>[x] Background events and permission requests never activate or cover another session — unit:desktop permission contracts; full UI manual:M5</li><li>[x] Deny prevents execution — manual:M3</li><li>[x] Allow returns the result to model and UI — manual:M3</li><li>[x] Paths outside the workspace are permission-gated — auto:host-core tests (outside-path prompt/deny/allow and Auto execution)</li></ul><h3 id="m6-plan-checkpoint-and-shell-execution" tabindex="-1">M6. Plan checkpoint and shell execution <a class="header-anchor" href="#m6-plan-checkpoint-and-shell-execution" aria-label="Permalink to &quot;M6. Plan checkpoint and shell execution&quot;">​</a></h3><ul><li>[x] One pi Agent owns Agent, planning, approval, and post-approval execution — auto:agent-runtime + <code>test:e2e:plan</code>/<code>test:e2e:plan-ui</code></li><li>[x] Goal reuses the same host-owned approval pipeline, writes a distinct <code>.pi/goal/*.md</code> artifact, and resumes in Agent mode to verify acceptance criteria — auto:agent-runtime + desktop/runtime contract coverage</li><li>[x] <code>EnterPlanMode</code> and UI/session Plan selection converge on the same state — auto:host-core CAS tests + <code>test:e2e:plan-ui</code></li><li>[x] <code>SubmitPlan(title, markdown, question)</code> preserves exact Markdown bytes in a unique <code>.pi/plan/*.md</code> artifact, keeps title/question structured in <code>plan_approvals</code>, and records path/hash/size — auto:<code>test:e2e:plan</code> E2E-106</li><li>[x] Approval offers only Approve/Reject; Approve requires an explicit permission mode with Ask selected by default — auto:<code>test:e2e:plan-ui</code> E2E-106/E2E-117</li><li>[x] The approval deadline is an absolute 30 minutes and stale responses fail closed — auto:<code>test:e2e:plan</code> E2E-107 + host-core late-expiry test</li><li>[x] Renderer retains the latest Plan/Goal proposal/execution snapshot only for the current renderer lifetime; <code>plans.pending</code> rehydrates only pending rows and original deadlines after renderer reload, terminal cards are not rehydrated after renderer reload, and Host restart restores no stale action — auto:<code>test:e2e:plan-ui</code> same-Host PID/negative reload assertions + <code>test:e2e:plan</code> E2E-108/E2E-109</li><li>[x] Schema v7 first reaches v8 and then uses the guarded v8→v11 path; the v8→v11 migration is one atomic transaction with a WAL checkpoint and exact readable <code>pi.sqlite.v8.bak</code> before destructive work, while schema v9 and v10 receive readable backups. Malformed app settings/scheduled config, invalid top-level operating modes, and unknown or wrong-platform default shells fail closed with schema v8 authoritative; platform-valid shells remain migratable when temporarily unavailable. Sessions, transcripts, nested extension modes, and <code>plan_approvals</code> artifact/execution fields survive — auto:host-core migration tests (139/139; 15 focused DB tests)</li><li>[x] Pending, queued, and running Plan/Goal work is interrupted on host restart with no replay; an already-approved interrupted execution leaves the session Agent — auto:<code>test:e2e:plan</code> E2E-108/E2E-109</li><li>[x] Scheduled/unattended Plan/Goal runs fail before the provider request — auto:<code>test:e2e:plan</code> E2E-110</li><li>[x] Plan/Goal plugin tools remain denied despite low risk, grants, or Auto — auto:<code>test:e2e:plan</code> E2E-105 + host-core policy tests</li><li>[x] Shell catalog selection persists a platform-valid ID, falls back to the first available platform shell when a later lookup is unavailable, rejects a stale turn ID/dialect, streams stdout/stderr, enforces the 60s default timeout, and kills process trees on abort — auto:<code>test:e2e:plan</code> E2E-112–E2E-116 + host-core fallback test</li></ul><h3 id="m6-current-product-increment" tabindex="-1">M6+ Current product increment <a class="header-anchor" href="#m6-current-product-increment" aria-label="Permalink to &quot;M6+ Current product increment&quot;">​</a></h3><ul><li>[x] Extensions keeps only Installed and Marketplace tabs; Settings &gt; Agent provides independent Skills, MCP, and global-only Subagents pages with fixed columns, local enablement, project selection, and source scanning — unit/source contracts plus E2E-100–E2E-103</li><li>[x] Session import, scheduled task records, composer file references and clipboard files, global plugin launcher, and next-turn configuration are represented by the current E2E catalog — E2E-036, E2E-059, E2E-102, E2E-102a, E2E-103, E2E-120</li></ul><h3 id="f-persistence" tabindex="-1">F. Persistence <a class="header-anchor" href="#f-persistence" aria-label="Permalink to &quot;F. Persistence&quot;">​</a></h3><ul><li>[x] Sessions survive restart — manual:M2 (SQLite via host-core)</li><li>[x] Message history is restored — manual:M2</li><li>[x] Session deletion takes effect — auto:<code>test:e2e</code></li><li>[x] Forked source/child histories persist and diverge independently — auto:host-core tests</li></ul><h3 id="g-plugin-system-local-minimum" tabindex="-1">G. Plugin system (local minimum) <a class="header-anchor" href="#g-plugin-system-local-minimum" aria-label="Permalink to &quot;G. Plugin system (local minimum)&quot;">​</a></h3><ul><li>[x] Plugin loads from a local directory — auto:<code>test:e2e</code> (dev load)</li><li>[x] Plugin command appears in the palette and executes — manual:M4</li><li>[ ] Plugin can open a panel (if declared) — open (ui.openPanel is a toast stub; PluginPanelHost tracked post-MVP)</li><li>[x] Plugin can register and serve at least one agent tool — auto:<code>test:e2e</code> (E2E-024 dispatch roundtrip)</li><li>[x] Disabling removes commands and tools — auto:<code>test:e2e</code> (plugin disable)</li><li>[x] A plugin exception does not crash the app — manual:M4</li><li>[ ] A plugin observes exactly one turn-end event per host turn — draft:E2E-PLUGIN-turn-ended-once-per-host-turn (not run locally; requires <code>test:e2e</code>)</li></ul><h3 id="h-diagnostics" tabindex="-1">H. Diagnostics <a class="header-anchor" href="#h-diagnostics" aria-label="Permalink to &quot;H. Diagnostics&quot;">​</a></h3><ul><li>[x] Errors expose stable codes — auto:<code>test:e2e</code> (fatal-path assertions)</li><li>[x] Logs folder can be opened from the app — manual:M5 (Open logs action)</li><li>[x] Secrets never appear in logs on normal flows — auto:<code>test:e2e</code> (no-secret-leak)</li></ul><h2 id="_3-security-acceptance" tabindex="-1">3. Security acceptance <a class="header-anchor" href="#_3-security-acceptance" aria-label="Permalink to &quot;3. Security acceptance&quot;">​</a></h2><ul><li>[x] Renderer has no Node integration (sandboxed, contextIsolation) — auto:<code>test:e2e:boot</code></li><li>[x] Non-whitelisted IPC channels cannot be invoked — manual:M1 (preload whitelist assertion)</li><li>[x] Secrets are never written to plain logs — auto:<code>test:e2e</code> + Logger/audit redaction</li><li>[x] High-risk tools require confirmation by default — manual:M3</li><li>[ ] Plugin without a granted permission cannot call the API — open (enforcement matrix lands with plugin runtime isolation, see 07-plugins/13)</li><li>[x] Plugins cannot read provider API keys — manual:M4 (no secrets surface in plugin host services)</li></ul><h2 id="_4-quality-acceptance" tabindex="-1">4. Quality acceptance <a class="header-anchor" href="#_4-quality-acceptance" aria-label="Permalink to &quot;4. Quality acceptance&quot;">​</a></h2><ul><li>[x] No crash on the main path — auto:<code>test:e2e:boot</code> + <code>test:e2e:supervision</code></li><li>[x] Errors show a readable message — manual:M2 (AppError message surfaced)</li><li>[x] Long output does not freeze the UI — manual:M3 (per-tool budgets; search 128KB / 4000 lines)</li><li>[x] Key operations show loading/running state — manual:M2</li><li>[x] Release package has no duplicate renderer/runtime dependency trees — auto:desktop package contract + native macOS arm64/Intel x64 package audit:E2E-092</li><li>[ ] Packaged startup and local renderer/runtime capabilities remain available offline on every native target — draft:E2E-092</li></ul><h2 id="_5-acceptance-demo-script" tabindex="-1">5. Acceptance demo script <a class="header-anchor" href="#_5-acceptance-demo-script" aria-label="Permalink to &quot;5. Acceptance demo script&quot;">​</a></h2><ol><li>Launch PI-Desktop</li><li>Configure a working model</li><li>Open a local sample project</li><li>Ask: explain the project structure</li><li>Ask: modify a harmless file and add a comment</li><li>Approve on the permission card</li><li>Abort one generation mid-stream</li><li>Restart the app, confirm the session is still there</li><li>Load the sample plugin, run one plugin command</li><li>Disable the plugin and confirm the command disappears</li></ol><p>All steps passing = MVP functional acceptance.</p><h2 id="_6-failure-handling" tabindex="-1">6. Failure handling <a class="header-anchor" href="#_6-failure-handling" aria-label="Permalink to &quot;6. Failure handling&quot;">​</a></h2><ul><li>Blocking: any failure in A/B/C/E/F/G or Security means MVP cannot be declared complete.</li><li>Non-blocking: UI details, copy, off-main-path bugs go to known issues.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("spec/06-delivery/02-acceptance-criteria.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _02AcceptanceCriteria = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _02AcceptanceCriteria as default
};
