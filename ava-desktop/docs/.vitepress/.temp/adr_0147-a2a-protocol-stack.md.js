import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0147: A2A Protocol Stack for Subagent Coordination","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0147-a2a-protocol-stack.md","filePath":"adr/0147-a2a-protocol-stack.md","lastUpdated":1788625977000}');
const _sfc_main = { name: "adr/0147-a2a-protocol-stack.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0147-a2a-protocol-stack-for-subagent-coordination" tabindex="-1">ADR 0147: A2A Protocol Stack for Subagent Coordination <a class="header-anchor" href="#adr-0147-a2a-protocol-stack-for-subagent-coordination" aria-label="Permalink to &quot;ADR 0147: A2A Protocol Stack for Subagent Coordination&quot;">​</a></h1><ul><li>Status: Superseded by ADR 0165</li><li>Date: 2026-09-02</li><li>Deciders: PI-Desktop core</li><li>Related: D277, D318, D321, ADR 0062, ADR 0089, ADR 0100, ADR 0162, ADR 0164, <code>03-runtime/02-agent-runtime.md</code> §5f.2, <code>03-runtime/06-host-rpc-protocol.md</code> §4, E2E-165, E2E-165b, E2E-165c, E2E-165d</li><li>Supersedes: ADR 0138 and ADR 0140. The in-process <code>SubagentMailbox</code> and the single <code>Peer</code> tool are removed. The coordination need those ADRs identified is preserved; the mechanism is replaced.</li><li>Amended by: ADR 0162 lifts same-context-only addressing. ADR 0164 lets the parent agent register as <code>kind: &quot;parent&quot;</code> and call <code>A2A</code> to other parents; parents still cannot address subagents.</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>ADR 0138 introduced a session-scoped <code>SubagentMailbox</code> and delegate-only peer tools; ADR 0140 folded them into one <code>Peer</code> tool with <code>send | inbox | wait</code> actions. Both kept coordination entirely in-process in the Node agent-runtime sidecar: messages never reached host-core, carried no durable state, and had no task lifecycle, discovery, or authorization model beyond a runtime-supplied sender name.</p><p>That mechanism worked for bounded text between siblings but was a private protocol that could not grow. It had no task state machine (a message was fire-and-forget), no durable history a delegate could re-read after a restart, no typed payloads beyond text, no discovery surface a delegate could query, and no capability model — only the fact that the sidecar injected <code>from</code>. It also diverged from the industry Agent2Agent (A2A) protocol, so nothing PI-Desktop built could interoperate with, or be reasoned about against, that contract.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Replace peer messaging with a real <strong>A2A (Agent2Agent) protocol stack</strong> whose broker lives in the Rust host-core process. Each subagent in the agent-runtime sidecar is an A2A <strong>client</strong> that reaches the broker over the existing stdio JSON-RPC 2.0 / NDJSON transport — the same pipe the <code>plans.*</code> domain uses — through a new <code>a2a.*</code> method domain. There is no HTTP, gRPC, or REST server and no network OAuth stack; the entire A2A surface is bound to the local transport.</p><p>The seven A2A pillars map to local semantics:</p><table tabindex="0"><thead><tr><th>A2A pillar</th><th>Standard form</th><th>Local mapping</th></tr></thead><tbody><tr><td>Transport</td><td>HTTP + JSON-RPC / SSE</td><td>Existing stdio NDJSON JSON-RPC 2.0, new <code>a2a.*</code> method domain</td></tr><tr><td>Agent Card discovery</td><td><code>GET /.well-known/agent-card.json</code></td><td>Broker holds an in-memory agent registry; <code>a2a.agents.list</code> returns cards derived from each <code>SubagentDefinition</code> (name/description/skills)</td></tr><tr><td>Task state machine</td><td>Server-managed task lifecycle</td><td>Durable SQLite <code>a2a_tasks</code> rows in host-core: <code>submitted, working, input-required, auth-required, completed, canceled, failed, rejected</code>; the last four are terminal and never transition again; the broker enforces legal transitions and <code>a2a.tasks.status</code> drives a task to a new state. Each task records both <code>agentName</code> (the worker that serves it) and <code>requesterName</code> (the peer that sent the first message)</td></tr><tr><td>Message / Parts</td><td>Typed <code>Part</code> union</td><td><code>TextPart{kind:&quot;text&quot;,text}</code> | <code>FilePart{kind:&quot;file&quot;,file:{name?,mimeType?,uri?,bytes?}}</code> | <code>DataPart{kind:&quot;data&quot;,data}</code>, mirrored in TS (<code>packages/shared/src/a2a.ts</code>) and Rust (<code>crates/host-core/src/a2a/types.rs</code>)</td></tr><tr><td>Streaming</td><td><code>message/stream</code> over SSE</td><td>Host→client JSON-RPC notification <code>a2a.task.event</code> carrying a <code>TaskStatusUpdateEvent</code> / <code>TaskArtifactUpdateEvent</code>, shaped <code>{ recipient, contextId, event }</code>; routing is <strong>counterpart-based</strong> — a status/terminal event routes to the counterpart of whoever caused it (a worker&#39;s reply or completion wakes the requester; a requester&#39;s follow-up wakes the worker), so an agent waits for events addressed to itself</td></tr><tr><td>Push notification</td><td>Webhook config</td><td>Host-owned push config (<code>a2a.tasks.pushNotificationConfig.set/get</code>) plus an <code>a2a.push</code> notification <code>{ recipient, contextId, taskId, token?, status }</code></td></tr><tr><td>Auth</td><td>OAuth / API key</td><td>Host-minted per-agent capability token: <code>a2a.agents.register</code> returns <code>{ agentId, token }</code>, every subsequent <code>a2a.*</code> call carries the token, and the host validates it and authorizes addressing by it. The token is injected by the runtime and never visible to the model, preserving the invariant that a sender&#39;s <code>from</code> cannot be forged</td></tr></tbody></table><p><code>contextId</code> equals the <code>sessionId</code> and groups a task with the requester&#39;s session. Discovery and addressing span every live agent on the host (ADR 0162); a stranger still cannot read a task they are not a party to.</p><p>The <code>a2a.*</code> methods, each carrying a <code>token</code> except <code>register</code>, are specified in <code>03-runtime/06-host-rpc-protocol.md</code> §4; <code>a2a.tasks.status({ token, id, state, message? })</code> drives a task to a new state (completion / failure / interactive pause), validating the transition against the state table and stamping any optional <code>message</code> with broker-owned <code>from</code>/<code>contextId</code>. On the runtime side, the single subagent-facing tool is <code>A2A</code> (replacing <code>Peer</code>) with actions <code>discover | send | get | wait | complete | cancel</code>; <code>complete</code> finishes a task the delegate serves and wakes its requester.</p><p>Event routing is <strong>counterpart-based</strong>. Task creation addresses the new task to the worker; <code>a2a.message.send</code> on an existing task, <code>a2a.tasks.status</code>, and <code>a2a.tasks.cancel</code> route the <code>a2a.task.event</code> (and, for a terminal state with a push config, the <code>a2a.push</code>) to the counterpart of the caller — a worker&#39;s reply or completion wakes the requester, a requester&#39;s follow-up wakes the worker; <code>a2a.tasks.resubscribe</code> re-emits to the caller itself. This closes the delegation round-trip a worker&#39;s completion previously never reached the requester. The delegate <code>A2A</code> tool is registered/deregistered per delegation on spawn/settle and closes over the host-minted token. <code>SUBAGENT_A2A_TOOLS = [&quot;A2A&quot;]</code> replaces <code>SUBAGENT_PEER_TOOLS = [&quot;Peer&quot;]</code>. ADR 0164 additionally registers the parent as <code>kind: &quot;parent&quot;</code> and puts <code>A2A</code> in the Agent-mode catalog for parent-to-parent use only.</p><p>Bounds are enforced by the broker: <code>A2A_MAX_TEXT_CHARS = 16000</code>, <code>A2A_MAX_FILE_BYTES = 20MB</code>, <code>A2A_MAX_TASK_HISTORY = 256</code>, <code>A2A_MAX_TASKS_PER_CONTEXT = 128</code>, <code>A2A_MAX_SENDS_PER_RUN = 200</code>, <code>A2A_MAX_STREAM_WAIT_SECONDS = 120</code>, <code>A2A_DEFAULT_STREAM_WAIT_SECONDS = 30</code>. Errors use JSON-RPC numeric code <code>1400</code> with <code>data.errorCode</code> one of <code>A2A_UNKNOWN_TOKEN</code>, <code>A2A_UNKNOWN_AGENT</code>, <code>A2A_UNKNOWN_TASK</code>, <code>A2A_CROSS_CONTEXT_DENIED</code>, <code>A2A_INVALID_TRANSITION</code>, <code>A2A_TASK_TERMINAL</code>, <code>A2A_SEND_CAP</code>, <code>A2A_NO_PEERS</code>, <code>A2A_PAYLOAD_TOO_LARGE</code>.</p><h2 id="non-goals" tabindex="-1">Non-goals <a class="header-anchor" href="#non-goals" aria-label="Permalink to &quot;Non-goals&quot;">​</a></h2><ul><li><strong>No gRPC, HTTP, or REST binding.</strong> The only transport binding is the local stdio JSON-RPC pipe. The <code>/.well-known/agent-card.json</code> discovery endpoint and the SSE / webhook transports are mapped onto local RPC, not served.</li><li><strong>No real OAuth.</strong> Authorization is a host-minted in-memory capability token, not a network OAuth or API-key exchange.</li><li><strong>No cross-machine or remote agents.</strong> Every agent is a subagent of a local session on this host; there is no agent outside the host process. Cross-session addressing on the same host is allowed (ADR 0162).</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li><strong>Protocol v10 and schema v12 are breaking.</strong> <code>PROTOCOL_VERSION</code> moves from 9 to 10 and the <code>app.handshake</code> capabilities array now includes <code>&quot;a2a&quot;</code>; an old host that cannot advertise <code>a2a</code> is rejected at handshake before the UI becomes interactive. <code>SCHEMA_VERSION</code> moves from 11 to 12: <code>migrate_v11_to_v12</code> adds <code>a2a_agents</code>, <code>a2a_tasks</code>, <code>a2a_messages</code>, <code>a2a_artifacts</code>, and <code>a2a_push_configs</code>. The <code>a2a_messages</code> primary key is composite <code>(task_id, message_id)</code> so client-supplied message ids cannot collide across tasks, and each mutating call (task creation, existing-task send, status update) persists its multiple writes in a single SQLite transaction. Capability tokens are in-memory only and are invalidated on deregister.</li><li>Coordination is now durable and inspectable: a task and its message history survive in SQLite, so a delegate can re-read a task with <code>a2a.tasks.get</code> and the lifecycle is auditable, unlike the destructive-drain mailbox.</li><li>Payloads are typed (<code>TextPart</code>/<code>FilePart</code>/<code>DataPart</code>) rather than text-only, so delegates can exchange files and structured data within the bounds above.</li><li>The forged-sender invariant is preserved by construction: the capability token is injected by the runtime and never exposed to the model, so a delegate can neither spoof another agent nor address a task it does not own.</li><li>Boundaries from the superseded ADRs still hold: the <code>A2A</code> tool never enters the parent&#39;s <code>toolCatalog</code>; a settled delegate is deregistered, invalidating its token; and the stream wait ceiling (120s) stays below the 300-second idle watchdog.</li><li>Not addressed: messaging with the parent, nested delegation, and any remote or cross-machine transport. Cross-session addressing is decided in ADR 0162.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Keep the in-process mailbox (ADR 0138/0140):</strong> rejected. It cannot carry a task lifecycle, durable history, typed payloads, discovery, or a real authorization model, and it is a private contract no other component can reason about.</li><li><strong>Run a real A2A HTTP/SSE server in host-core:</strong> rejected. It adds a network listener, an OAuth stack, and a cross-machine surface for a purely local, same-session coordination need. Binding the A2A semantics onto the existing JSON-RPC pipe gives the protocol shape with none of that attack surface.</li><li><strong>Put the broker in the sidecar rather than host-core:</strong> rejected. Durable task state, migrations, and capability-token authority belong with the process that already owns SQLite and the security boundary; a sidecar-owned broker would duplicate persistence and could not survive a sidecar restart.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0147-a2a-protocol-stack.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0147A2aProtocolStack = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0147A2aProtocolStack as default
};
