import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0118: Keep queued prompts renderer-owned and stop runs at turn boundaries","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0118-renderer-owned-queued-prompts.md","filePath":"adr/0118-renderer-owned-queued-prompts.md","lastUpdated":1787544026000}');
const _sfc_main = { name: "adr/0118-renderer-owned-queued-prompts.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0118-keep-queued-prompts-renderer-owned-and-stop-runs-at-turn-boundaries" tabindex="-1">ADR 0118: Keep queued prompts renderer-owned and stop runs at turn boundaries <a class="header-anchor" href="#adr-0118-keep-queued-prompts-renderer-owned-and-stop-runs-at-turn-boundaries" aria-label="Permalink to &quot;ADR 0118: Keep queued prompts renderer-owned and stop runs at turn boundaries&quot;">​</a></h1><ul><li>Status: Accepted (Implemented 2026-08-24)</li><li>Date: 2026-08-24</li><li>Related: Issue #11, ADR 0073</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The desktop host enforces one running turn per session. Disabling the composer while that turn runs makes users wait or abort a useful reply before they can provide the next instruction. Moving a second prompt into host-core or durable storage would introduce replay and lifecycle ownership that is not needed for a transient editor queue.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>The renderer owns an in-memory FIFO queue keyed by session id. A queued item contains its visible prompt and composer draft snapshot, so file references remain independently removable and can be sent through the normal prompt channel. The queue is not persisted and is discarded on application restart or session deletion. Switching sessions only changes which queue is projected above the active composer.</p><p>The Send and Stop controls coexist while a run is active. A normal Send adds a queue item. The renderer drains the queue one item at a time after the owning session receives <code>agent_end</code>; each item uses the existing <code>agent/prompt</code> path, preserving the host&#39;s single-running-turn invariant. Send now moves its item to the head and calls the additive <code>agent/stop</code> IPC. The sidecar maps that request to pi-agent-core&#39;s <code>shouldStopAfterTurn</code> hook, so the current reply and completed tool batch finish normally and the durable turn closes as <code>completed</code> before the prioritized item starts.</p><p>Immediate abort remains separate: it cancels the active runtime immediately and leaves queued items untouched for explicit later sending.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Users can continue typing and queue multiple next-turn instructions while a long model/tool run is active.</li><li>Send now has precise next-boundary semantics without a second concurrent durable turn or an <code>AGENT_BUSY</code> race.</li><li>Queue contents are intentionally lost on restart and are not visible to another session; persistence can be considered separately if a future product requirement needs it.</li><li>Graceful stop is only as precise as the pi-agent-core boundary: an active provider stream is allowed to finish, while a pending permission can still wait for its normal resolution.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0118-renderer-owned-queued-prompts.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0118RendererOwnedQueuedPrompts = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0118RendererOwnedQueuedPrompts as default
};
