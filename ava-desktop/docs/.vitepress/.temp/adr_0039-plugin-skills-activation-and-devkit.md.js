import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0039: Activate plugin skills and ship plugin authoring as a first-party devkit","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0039-plugin-skills-activation-and-devkit.md","filePath":"adr/0039-plugin-skills-activation-and-devkit.md","lastUpdated":1785490900000}');
const _sfc_main = { name: "adr/0039-plugin-skills-activation-and-devkit.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0039-activate-plugin-skills-and-ship-plugin-authoring-as-a-first-party-devkit" tabindex="-1">ADR 0039: Activate plugin skills and ship plugin authoring as a first-party devkit <a class="header-anchor" href="#adr-0039-activate-plugin-skills-and-ship-plugin-authoring-as-a-first-party-devkit" aria-label="Permalink to &quot;ADR 0039: Activate plugin skills and ship plugin authoring as a first-party devkit&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-07-31</li><li>Related: <a href="./../spec/07-plugins/10-plugin-devex">Plugin developer experience</a>, <a href="./../spec/07-plugins/13-plugin-permissions-matrix">Plugin permissions matrix</a>, <a href="./0008-plugin-runtime-isolation-target">ADR 0008</a>, <a href="./0037-project-instruction-chain">ADR 0037</a>, <a href="./0040-plugin-resident-services-and-message-bus">ADR 0040</a></li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p><code>contributes.skills</code> has been part of manifest v1 since R1: <code>validateManifest</code> accepts it and <code>examples/plugins/hello</code> ships a skill file. Nothing read it. Declaring a skill had no observable effect, so the contribution point was documentation, not a feature.</p><p>The authoring path was incomplete in the other direction. A <code>.piplug</code> package could be installed but not produced: host-core owns <code>install_from_package</code>, and nothing in the repository could write the store-only zip it accepts. There were no templates, no manifest validation ahead of install, and every source edit to a development plugin required re-picking its folder through the native dialog.</p><p>The two gaps are the same gap: PI-Desktop asked users to write plugins without giving them the loop — scaffold, run, inspect, package.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><strong>Skills reach the model as a permission-gated catalog it can load from.</strong><code>PluginRuntime</code> indexes each declared skill file at load time, only for plugins granted <code>agent.prompt.inject</code>, and only through the same containment guard as the gated <code>fs</code> APIs. The system prompt carries a <code># Skills</code> section of one line per skill — id, name, applicability — and the model pulls a body on demand with the <code>Skill</code> tool, served from Electron main because the plugin directories live there. A skill document may be 128 KiB and a plugin may teach 32 of them, because an unread document costs nothing (D174, which supersedes this ADR&#39;s original 16 KiB whole-body prompt injection).</li><li><strong>Prompt order is host skills, then plugin skills, then project instructions.</strong> Later text carries more weight, so a user&#39;s own instruction files keep the last word and an installed plugin can refine the built-in guidance but never the reverse.</li><li><strong>Runtime reuse keys on the catalog digest, not on bodies.</strong> Enabling a plugin, revoking <code>agent.prompt.inject</code> or renaming a skill changes the text the model reads, so it retires the idle runtime rather than reusing a stale prompt. An edit to a body needs no retirement: the <code>Skill</code> tool reads the file at call time.</li><li><strong>Plugin authoring ships as a first-party devkit, not as a plugin.</strong><code>@pi-desktop/plugin-devkit</code> owns scaffold, check and pack; three surfaces share that one implementation — the <code>pi-plugin</code> CLI, the <code>PluginScaffold</code> / <code>PluginCheck</code> / <code>PluginPack</code> agent tools served from Electron main, and the plugins-page template action.</li><li><strong>The built-in plugin-development skill activates only for plugin workspaces</strong> — a <code>manifest.json</code> at the workspace root, or a loaded plugin directory inside it. Ordinary sessions pay for three tool descriptions and nothing else; scaffolding writes a manifest, which activates the full skill on the next prompt.</li><li><strong>Hot reload may not widen a permission set.</strong> A watched development plugin reloads on save, but the reload reads the manifest first: permissions outside the set approved when the folder was picked stop the reload with an error telling the user to load the plugin again. Grants do follow the manifest downwards, so a removed permission stops being available.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A skill file is now executable surface. <code>agent.prompt.inject</code> keeps its high-risk tier in the permissions matrix and is the only gate; the install review already shows it.</li><li><code>check</code> passing implies install will pass, because the devkit reproduces the rules host-core enforces (store-only entries, 2000 files, 50 MB, no symlinks, no <code>.git</code>/<code>node_modules</code>). Those limits are duplicated in TypeScript and Rust and must move together.</li><li>Skill files are read when the model asks for one rather than every prompt, so an edit takes effect on the next <code>Skill</code> call and a session that needs no skill reads nothing. The catalog metadata is indexed at load time, so renaming a skill still needs a reload — which a watched development plugin does on save.</li><li>A failed hot reload leaves the plugin unloaded but still watched, so the next save recovers it. host-core&#39;s registry still reports <code>ready</code> in that window: it has no RPC for a runtime-side load failure, so the reload reports itself through a toast and <code>pluginChanged</code>, exactly as a plugin crash does.</li><li>Skill front-matter parsing lives in the plugin SDK (<code>parseSkillFrontmatter</code>), shared by the runtime registry, the built-in skills and the devkit&#39;s <code>check</code>.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><h3 id="ship-plugin-authoring-as-a-bundled-plugin" tabindex="-1">Ship plugin authoring as a bundled plugin <a class="header-anchor" href="#ship-plugin-authoring-as-a-bundled-plugin" aria-label="Permalink to &quot;Ship plugin authoring as a bundled plugin&quot;">​</a></h3><p>Rejected. A plugin cannot produce a <code>.piplug</code>: there is no archive API in <code>HOST_API_ALLOWLIST</code> and adding one would hand every plugin a zip writer. Scaffolding would also require <code>fs.write.workspace</code>, a high-risk permission, for a capability the application itself should provide. First-party means always available, with no permission prompt and no chicken-and-egg.</p><h3 id="inject-the-plugin-development-skill-into-every-session" tabindex="-1">Inject the plugin-development skill into every session <a class="header-anchor" href="#inject-the-plugin-development-skill-into-every-session" aria-label="Permalink to &quot;Inject the plugin-development skill into every session&quot;">​</a></h3><p>Rejected. A plugin-authoring primer in every system prompt is pure waste for users who will never write one. The three tool descriptions are enough to bootstrap, and the workspace gate turns the full skill on exactly when it becomes relevant.</p><h3 id="share-the-32-kib-instruction-chain-budget-with-skills" tabindex="-1">Share the 32 KiB instruction-chain budget with skills <a class="header-anchor" href="#share-the-32-kib-instruction-chain-budget-with-skills" aria-label="Permalink to &quot;Share the 32 KiB instruction-chain budget with skills&quot;">​</a></h3><p>Rejected. Skills come from third-party code and instructions come from the user. A shared budget would let an installed plugin displace a project&#39;s own rules.</p><h3 id="reload-with-the-previously-granted-permissions-regardless-of-the-manifest" tabindex="-1">Reload with the previously granted permissions regardless of the manifest <a class="header-anchor" href="#reload-with-the-previously-granted-permissions-regardless-of-the-manifest" aria-label="Permalink to &quot;Reload with the previously granted permissions regardless of the manifest&quot;">​</a></h3><p>Rejected. The grant set was approved against a specific manifest. Reusing it after a manifest edit would let a file write widen capabilities behind the permission gateway — the one boundary the plugin system exists to hold.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0039-plugin-skills-activation-and-devkit.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0039PluginSkillsActivationAndDevkit = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0039PluginSkillsActivationAndDevkit as default
};
