import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse(`{"title":"ADR: Remote header variables accept the registry's {name} spelling","description":"","frontmatter":{},"headers":[],"relativePath":"adr/registry-header-variable-spelling.md","filePath":"adr/registry-header-variable-spelling.md","lastUpdated":1789962486000}`);
const _sfc_main = { name: "adr/registry-header-variable-spelling.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-remote-header-variables-accept-the-registry-s-name-spelling" tabindex="-1">ADR: Remote header variables accept the registry&#39;s <code>{name}</code> spelling <a class="header-anchor" href="#adr-remote-header-variables-accept-the-registry-s-name-spelling" aria-label="Permalink to &quot;ADR: Remote header variables accept the registry&#39;s \`{name}\` spelling&quot;">​</a></h1><ul><li>Status: Proposed</li><li>Date: 2026-09-20</li><li>Deciders: Pending maintainer review</li><li>Related: ADR 0245, E2E-MCP-MARKET-SEMANTICS</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The official MCP registry spells a remote header variable <code>{name}</code> and declares the name in the header&#39;s <code>variables</code> map. Its schema describes <code>variables</code> as the values that replace &quot;keys in the input <code>value</code> that are wrapped in <code>{curly_braces}</code>&quot;. Declared names may use lower case, such as <code>api_key</code>. The regression fixtures cover both cases; this ADR does not assert the frequency of either spelling in the live registry.</p><p>The builtin catalog and stdio templates spell a template variable <code>\${NAME}</code> in upper case. The registry adapter and the catalog template matcher knew only that spelling. In the reproduced mapping and resolution path, a registry record using the official spelling retained <code>Authorization: Bearer {api_key}</code> and had no <code>requiredEnv</code> entry. This establishes an unresolved template at the parser boundary. The tests do not establish rendered install-sheet behavior, actual HTTP transmission, or a user&#39;s observed response status.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>The registry adapter resolves <code>{name}</code> only when that same header declares it in <code>variables</code>; undeclared braces remain literal. Legacy <code>\${NAME}</code> tokens keep working without a variables map. Names need not be upper case when declared by the registry. Each header owns its variable definitions.</li><li>Add optional <code>headerBindings</code> to the shared catalog template. Each header maps exact tokens to an editable <code>requiredEnv</code> input or a fixed literal. Presence of <code>headerBindings</code>, including an empty map, selects explicit binding mode for all headers. A header absent from that map has no bound tokens: both <code>{name}</code> and <code>\${NAME}</code> remain literal, regardless of global <code>requiredEnv</code> names or defaults. Only catalogs with no <code>headerBindings</code> metadata use the legacy global input scope. Same-named inputs in separate headers get distinct collision-safe names. Variable <code>default</code> prefills the editable input; omitted <code>isRequired</code> means optional. A fixed <code>value</code> is not editable and its content is never parsed again as a template. Bindings do not enter the host <code>McpServerInput</code> or persisted server config, so the runtime protocol remains unchanged.</li><li>Header templates accept both spellings and consume the <code>$</code>, so a <code>\${NAME}</code> header resolves without a stray dollar. URL templates keep the legacy <code>\${NAME}</code> spelling with upper-case names. A header declaration never makes bare <code>{name}</code> text in a URL editable or substitutable, including when the header has a default. <code>collectCatalogPlaceholders</code> and <code>resolveCatalogEntry</code> select the same pattern for each field, so discovery and resolution agree.</li><li>stdio templates keep the <code>\${NAME}</code> spelling and the upper-case name rule. A brace pair inside a command or an argument is never rewritten, and the existing stdio contract is unchanged.</li><li><code>requiredEnv</code> names are validated against the transport: variable names for <code>http</code>, environment-variable names for <code>stdio</code>.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Declared editable header variables yield prompts and resolve according to their own defaults and requiredness. Fixed variables and unbound brace text do not create prompts. Header-local bindings prevent cross-header leakage of defaults, optional flags, and literal values. Bare URL tokens remain literal through mapping, resolution and host persistence; explicit legacy <code>\${NAME}</code> URL templates retain their existing behavior.</li><li>Builtin catalog entries and stdio packages behave exactly as before.</li><li>The catalog can express a header variable that is not an environment variable name, which is what the registry publishes.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/registry-header-variable-spelling.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const registryHeaderVariableSpelling = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  registryHeaderVariableSpelling as default
};
