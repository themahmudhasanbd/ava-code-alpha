import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0112: Agent Capability Management Roots and Settings IA","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0112-agent-capability-management-roots-and-settings-ia.md","filePath":"adr/0112-agent-capability-management-roots-and-settings-ia.md","lastUpdated":1787243067000}');
const _sfc_main = { name: "adr/0112-agent-capability-management-roots-and-settings-ia.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0112-agent-capability-management-roots-and-settings-ia" tabindex="-1">ADR 0112: Agent Capability Management Roots and Settings IA <a class="header-anchor" href="#adr-0112-agent-capability-management-roots-and-settings-ia" aria-label="Permalink to &quot;ADR 0112: Agent Capability Management Roots and Settings IA&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-08-20</li><li>Deciders: PI-Desktop core</li><li>Supersedes the capability-storage and capability-IA portions of ADR 0056, ADR 0058, and ADR 0063; updates D193, D194, and D202</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>MCP servers, skills, and subagent definitions were previously described as registries under the application data directory, and the Extensions page grew to contain their management surfaces. That model made capability files hard to carry between installations, mixed configuration with app-local activation state, and made the Extensions page responsible for unrelated authoring flows. It also left several documents referring to <code>.pi/</code> capability directories.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><h3 id="_1-agents-is-the-only-capability-file-root" tabindex="-1">1. <code>.agents</code> is the only capability file root <a class="header-anchor" href="#_1-agents-is-the-only-capability-file-root" aria-label="Permalink to &quot;1. \`.agents\` is the only capability file root&quot;">​</a></h3><p>The host scans and writes only these directories:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>~/.agents/skills                 global skills</span></span>
<span class="line"><span>&lt;project&gt;/.agents/skills         project skills</span></span>
<span class="line"><span>~/.agents/servers                global MCP configuration</span></span>
<span class="line"><span>&lt;project&gt;/.agents/servers        project MCP configuration</span></span>
<span class="line"><span>~/.agents/subagents              global subagent definitions</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>There is no project-level subagent directory. <code>.pi/agents</code>, <code>.pi/skills</code>, and <code>.pi/mcp</code> are not capability sources; the unrelated <code>.pi/prompts</code> store is unchanged.</p><p>Skills are Markdown documents. Their <code>name</code> and <code>description</code> frontmatter are scanned into the catalog while the body remains on disk until the <code>Skill</code> tool needs it. MCP servers are one JSON file per id. Subagents are Markdown Documents with the frontmatter consumed by the runtime.</p><h3 id="_2-file-ownership-and-activation-state-are-separate" tabindex="-1">2. File ownership and activation state are separate <a class="header-anchor" href="#_2-file-ownership-and-activation-state-are-separate" aria-label="Permalink to &quot;2. File ownership and activation state are separate&quot;">​</a></h3><p>Capability documents never contain <code>enabled</code> or a project override. host-core stores app-local state in:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&lt;data&gt;/agent-capabilities/skills.json</span></span>
<span class="line"><span>&lt;data&gt;/agent-capabilities/mcp.json</span></span>
<span class="line"><span>&lt;data&gt;/agent-capabilities/subagents.json</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>Global capabilities default to enabled and may have a per-project override. Project capabilities have state for their owning project. Scanning a directory prunes state for files that no longer exist; removing a global file removes all of its project overrides, while a project scan only removes that project&#39;s orphaned entries.</p><h3 id="_3-project-precedence-is-resolved-before-filtering" tabindex="-1">3. Project precedence is resolved before filtering <a class="header-anchor" href="#_3-project-precedence-is-resolved-before-filtering" aria-label="Permalink to &quot;3. Project precedence is resolved before filtering&quot;">​</a></h3><p>For the active runtime, project records shadow global records by id or case-insensitive display name. The project record wins even when its local state is disabled; only after shadowing does the host filter disabled records. This prevents a disabled project definition from making the global definition visible again.</p><h3 id="_4-management-lives-under-settings-agent" tabindex="-1">4. Management lives under Settings &gt; Agent <a class="header-anchor" href="#_4-management-lives-under-settings-agent" aria-label="Permalink to &quot;4. Management lives under Settings &gt; Agent&quot;">​</a></h3><p>Skills, MCP, and Subagents are three independent Settings destinations, not tabs. Skills and MCP use fixed-height global/project columns; their project column has a recent-project picker. Subagents use one global column and no project picker. The Extensions destination keeps only Installed and Marketplace tabs.</p><p>Skills expose one single-file native import action per column and physically copy the selected document into that column&#39;s <code>.agents/skills</code> directory. MCP create and edit reuse <code>McpEditorSheet</code>; editing locks the id, validation is shared with the host, and same-level id or label duplicates are rejected. Testing a saved MCP connection reports the result in the editor and a toast.</p><h3 id="_5-protocol-queries-are-explicit" tabindex="-1">5. Protocol queries are explicit <a class="header-anchor" href="#_5-protocol-queries-are-explicit" aria-label="Permalink to &quot;5. Protocol queries are explicit&quot;">​</a></h3><p>Capability list, read, remove, import, and enable calls carry <code>level</code> and, when needed, <code>projectPath</code>. A project-level request without <code>projectPath</code> is invalid. Runtime activation uses the merged <code>mcp.active</code> and <code>skills.active</code> results for the selected project; subagent activation is global-only.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Capability files are portable, inspectable, and safe to share without copying application-local enablement decisions.</li><li>A project can override or disable a global capability without changing the global file.</li><li>The Settings IA is larger, but Extensions is a focused plugin/marketplace surface and every capability page can expose its own affordances.</li><li>Existing plugin activation scopes remain unchanged; they are not reused for the file-level capability pages.</li><li>The host retains compatibility-shaped legacy scope RPC fields as no-op inputs where needed, but new UI state is represented by level and local state.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0112-agent-capability-management-roots-and-settings-ia.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0112AgentCapabilityManagementRootsAndSettingsIa = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0112AgentCapabilityManagementRootsAndSettingsIa as default
};
