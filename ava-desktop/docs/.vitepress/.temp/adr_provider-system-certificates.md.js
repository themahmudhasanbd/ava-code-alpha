import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse(`{"title":"ADR: Desktop sidecar uses the operating system's trusted certificates","description":"","frontmatter":{},"headers":[],"relativePath":"adr/provider-system-certificates.md","filePath":"adr/provider-system-certificates.md","lastUpdated":1789914100000}`);
const _sfc_main = { name: "adr/provider-system-certificates.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-desktop-sidecar-uses-the-operating-system-s-trusted-certificates" tabindex="-1">ADR: Desktop sidecar uses the operating system&#39;s trusted certificates <a class="header-anchor" href="#adr-desktop-sidecar-uses-the-operating-system-s-trusted-certificates" aria-label="Permalink to &quot;ADR: Desktop sidecar uses the operating system&#39;s trusted certificates&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-20</li><li>Related: Issue #714, ADR 0177, ADR 0271</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>HTTPS inspection can present a chain anchored in a locally installed trusted root. Chromium and the agent sidecar use different TLS implementations; the sidecar&#39;s bundled roots alone can reject a chain trusted by the operating system. Rebuilding an undici pool does not change that trust decision.</p><p>The current Electron 43 runtime supports Node&#39;s <code>--use-system-ca</code>. A Windows probe of Electron 43.6.0 / Node 24.20.0 confirmed that this flag includes the system roots in the default CA set. The older Node 22.16.0 behavior reported in #714 is not evidence that the current packaged runtime lacks this capability.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>The desktop launcher passes <code>--use-system-ca</code> before the sidecar entry point. Node combines its bundled roots with system roots and inherited <code>NODE_EXTRA_CA_CERTS</code>. Certificate chain, expiration and hostname validation remain enabled. The app neither installs roots nor exports a certificate bundle. The operating system&#39;s existing trust policy is the authority for local roots.</p><p>This applies to the desktop sidecar&#39;s default Node TLS clients, including the direct, HTTP proxy and SOCKS provider transports. It is a process-level default, not a per-provider exception. Headless pi-host launch policy is unchanged. Restart the desktop after changing roots or its extra-CA startup environment.</p><p>Explicit certificate verification codes make <code>NETWORK_ERROR</code> non-retriable and exclude it from transport rebuilding. The same classification is used before setup replay and after the adapter flattens the error, for both the session and built-in delegates. Other TLS/protocol failures retain their existing recovery behavior. The transcript explains certificate validation failure without asserting that security software must be its cause.</p><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><ul><li>Exporting the Windows stores to PEM adds platform-specific subprocesses, filesystem lifecycle, and duplicate trust-policy maintenance. The current runtime&#39;s native support avoids those costs.</li><li>A new custom-CA setting introduces persistence and restart semantics. The inherited <code>NODE_EXTRA_CA_CERTS</code> option already remains available.</li><li>Disabling certificate or hostname verification is unacceptable.</li><li>Suppressing every <code>tls</code> category is too broad: that category also includes protocol errors, not just certificate validation failures.</li></ul><h2 id="consequences-and-validation" tabindex="-1">Consequences and validation <a class="header-anchor" href="#consequences-and-validation" aria-label="Permalink to &quot;Consequences and validation&quot;">​</a></h2><p>Organizations that install trusted inspection roots now extend that trust to the desktop sidecar. Untrusted chains and hostname mismatches remain rejected. No IPC fields, database format, credentials, or proxy settings change.</p><p><code>scripts/e2e-provider-certificates.mjs</code> exercises the real desktop launcher, bundled sidecar, pi-ai and a loopback HTTPS provider. It checks inclusion of system roots without modifying OS stores, then rejection of an untrusted root, success with an inherited extra CA, and rejection of a mismatched hostname. Native macOS/Linux and the reporter&#39;s security product require separate checks.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/provider-system-certificates.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const providerSystemCertificates = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  providerSystemCertificates as default
};
