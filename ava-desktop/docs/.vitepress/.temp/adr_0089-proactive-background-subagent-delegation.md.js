import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0089: Proactive Background Subagent Delegation","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0089-proactive-background-subagent-delegation.md","filePath":"adr/0089-proactive-background-subagent-delegation.md","lastUpdated":1789643736000}');
const _sfc_main = { name: "adr/0089-proactive-background-subagent-delegation.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0089-proactive-background-subagent-delegation" tabindex="-1">ADR 0089: Proactive Background Subagent Delegation <a class="header-anchor" href="#adr-0089-proactive-background-subagent-delegation" aria-label="Permalink to &quot;ADR 0089: Proactive Background Subagent Delegation&quot;">​</a></h1><ul><li>Status: Accepted for implementation (resumable delegations amended by ADR 0279)</li><li>Date: 2026-08-16</li><li>Deciders: PI-Desktop core</li><li>Related: D201, D202, ADR 0062, ADR 0063, ADR 0048, ADR 0100, ADR 0279, E2E-142</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>ADR 0062 shipped delegation as context economy: <code>Task</code> runs one delegate to completion and returns its report, and the tool description tells the model to delegate only when a wide search or long survey would otherwise fill the parent context. In practice the main agent almost never delegates:</p><ul><li>The system prompt says nothing about delegation, so the model only ever sees the tool&#39;s defensive description (&quot;do not delegate what you can finish in a couple of tool calls&quot;) with no positive trigger patterns to act on.</li><li><code>Task</code> blocks the parent&#39;s tool loop until the delegate finishes, so from the model&#39;s perspective delegation trades one long job for another; there is no way to keep working while a delegate runs.</li><li>All builtin delegates are read-only report machines, so there is nothing to delegate implementation work to, and a write-capable delegate would hit the session&#39;s permission prompts for every file it touches.</li><li>The concurrency cap (4) and the fire-and-forget contract leave no room for orchestration patterns like &quot;review my change before I commit&quot; or &quot;explore three directions in parallel, then converge&quot;.</li></ul><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Make delegation a first-class, proactive orchestration capability while keeping every containment boundary ADR 0062 established.</p><h3 id="_1-the-delegate-lifecycle-is-non-blocking-and-tool-driven" tabindex="-1">1. The delegate lifecycle is non-blocking and tool-driven <a class="header-anchor" href="#_1-the-delegate-lifecycle-is-non-blocking-and-tool-driven" aria-label="Permalink to &quot;1. The delegate lifecycle is non-blocking and tool-driven&quot;">​</a></h3><p><code>Task</code> starts a delegate in the background and returns immediately with a <code>delegationId</code>. Three new Agent-mode tools drive the lifecycle, all in the core tool set beside <code>Task</code>:</p><ul><li><code>TaskWait(delegationIds?, mode?, minCompleted?, timeoutSeconds?)</code> converges on running delegations (all by default) and returns their reports; <code>mode: &quot;any&quot;</code> + <code>minCompleted</code> converges as soon as the first N finish, and settled delegations return immediately so reports can be re-read by id.</li><li><code>TaskList()</code> reports the session&#39;s delegations with status.</li><li><code>TaskStop(delegationIds?)</code> stops running delegations; stopped reads as <code>stopped</code>.</li></ul><p>The session runtime owns a delegation registry (id, agent, status, timings, result, completion promise, abort handle). Settled records are retained for the session (capped at 100) so <code>TaskWait</code> can re-read a report without re-running the delegate. Running delegates are aborted when the run ends (<code>agent_end</code>), when the parent aborts, or when the runtime is disposed; the system prompt instructs the model to converge or stop before ending a turn.</p><h3 id="_2-the-system-prompt-names-positive-trigger-patterns" tabindex="-1">2. The system prompt names positive trigger patterns <a class="header-anchor" href="#_2-the-system-prompt-names-positive-trigger-patterns" aria-label="Permalink to &quot;2. The system prompt names positive trigger patterns&quot;">​</a></h3><p>The base prompt gains a <code>## Delegation</code> section (only when the catalog is non-empty) listing concrete situations: parallel exploration of independent directions, adversarial review before commit, multi-file implementation via <code>fixer</code>, context-economy searches, and batch sharding — plus the convergence rules (continue working after <code>Task</code>, fill <code>description</code> for the user, never end the turn with running delegates). The <code>Task</code> description is rewritten to lead with these triggers and keep the old &quot;do not&quot; list as a single boundary line.</p><h3 id="_3-new-builtins-a-faster-explorer-and-a-write-capable-fixer" tabindex="-1">3. New builtins: a faster <code>explorer</code> and a write-capable <code>fixer</code> <a class="header-anchor" href="#_3-new-builtins-a-faster-explorer-and-a-write-capable-fixer" aria-label="Permalink to &quot;3. New builtins: a faster \`explorer\` and a write-capable \`fixer\`&quot;">​</a></h3><p><code>explorer</code> is rewritten in the style of the omo-slim explorer prompts: tool selection guidance (Grep for patterns, Glob for discovery, Read for files), parallel searches, and a structured <code>&lt;files&gt;</code> / <code>&lt;answer&gt;</code> report shape.</p><p>A fourth builtin, <code>fixer</code>, implements multi-file changes from a complete spec: <code>tools: [Read, Glob, Grep, Edit, Write, Bash]</code>, <code>maxTurns: 40</code>, with a <code>&lt;summary&gt;</code> / <code>&lt;changes&gt;</code> / <code>&lt;verification&gt;</code> report shape. It is the only write-capable builtin; the other three stay read-only.</p><h3 id="_4-definitions-may-declare-a-permission-scope" tabindex="-1">4. Definitions may declare a permission scope <a class="header-anchor" href="#_4-definitions-may-declare-a-permission-scope" aria-label="Permalink to &quot;4. Definitions may declare a permission scope&quot;">​</a></h3><p>Frontmatter gains <code>permission: inherit | ask | accept-edits | auto</code> (default <code>inherit</code>). A definition with an explicit non-<code>inherit</code> scope causes the sidecar to attach that scope to the delegate&#39;s <code>tools.execute</code> calls, and host-core resolves each call under that mode instead of the session&#39;s effective permission mode. An omitted or <code>inherit</code> scope carries no override, so the call uses the parent session&#39;s effective mode. The override is a permission-mode override only: the contract modes&#39; hard deny and the external-path gate stay in force, so <code>accept-edits</code> auto-allows <code>Write</code>/<code>Edit</code> inside the workspace and scratch roots while Bash and external paths keep the session&#39;s behavior. The builtin <code>fixer</code> uses the default <code>inherit</code> scope; explicit scopes remain available to eligible builtin and user definitions.</p><h3 id="_5-concurrency" tabindex="-1">5. Concurrency <a class="header-anchor" href="#_5-concurrency" aria-label="Permalink to &quot;5. Concurrency&quot;">​</a></h3><p><code>MAX_SUBAGENT_CONCURRENCY</code> becomes a per-session running cap of 10 (was a per-batch cap of 4). <code>Task</code> fails with a tool error when the session already runs 10 delegates, telling the model to wait or stop first.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Delegation stops being &quot;a long tool call&quot; and becomes background orchestration: the parent can implement while <code>explorer</code> searches and <code>code-reviewer</code> reviews, then converge with <code>TaskWait</code>.</li><li>Models receive positive trigger patterns in the system prompt and a full lifecycle vocabulary, which is what makes delegation proactive rather than permitted-but-unused.</li><li>A write-capable delegate is bounded by its definition&#39;s <code>permission</code> scope, not by the session&#39;s prompts; host-core stays the authority and the external path gate is untouched.</li><li>Old transcripts keep rendering: <code>Task</code> rows keep their delegation grouping, and the new tools map to the same delegate presentation.</li><li>A delegate that outlives its turn is stopped, not left running; the prompt rule plus the turn-end abort make that the exception rather than the policy.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Background tasks across turns (Proma-style).</strong> Delegates that keep running after the parent turn ends, with the UI showing pending background work, would maximize parallelism but requires reworking Electron main&#39;s turn lifecycle, completion semantics and the renderer&#39;s session-idle handling. Within-turn background delegation delivers the parent-continues-its-mainline behavior the pattern exists for, with the existing turn model unchanged.</li><li><strong>Let delegates inherit the session permission mode.</strong> Rejected: a delegate that needs <code>auto</code> would silently re-enable prompting or force the whole session to <code>auto</code>; the per-definition scope keeps the relaxation explicit and reviewable in the definition document.</li><li><strong>Keep <code>Task</code> blocking and add a <code>wait</code> flag.</strong> Rejected: a flag the model must remember to set is a flag it forgets; a lifecycle with distinct tools makes &quot;start, work, converge&quot; a sequence the model can follow and the prompt can teach.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0089-proactive-background-subagent-delegation.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0089ProactiveBackgroundSubagentDelegation = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0089ProactiveBackgroundSubagentDelegation as default
};
