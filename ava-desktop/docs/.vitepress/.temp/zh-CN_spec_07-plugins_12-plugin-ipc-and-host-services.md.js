import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"12. 插件 IPC 和主机服务","description":"","frontmatter":{},"headers":[],"relativePath":"zh-CN/spec/07-plugins/12-plugin-ipc-and-host-services.md","filePath":"zh-CN/spec/07-plugins/12-plugin-ipc-and-host-services.md","lastUpdated":1789885187000}');
const _sfc_main = { name: "zh-CN/spec/07-plugins/12-plugin-ipc-and-host-services.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="_12-插件-ipc-和主机服务" tabindex="-1">12. 插件 IPC 和主机服务 <a class="header-anchor" href="#_12-插件-ipc-和主机服务" aria-label="Permalink to &quot;12. 插件 IPC 和主机服务&quot;">​</a></h1><blockquote><p><strong>翻译说明：</strong> 本页是与 <a href="/spec/07-plugins/12-plugin-ipc-and-host-services">英文源规格</a> 一一对应的机器辅助翻译。代码、协议字段和标识符保持原文；如翻译与英文源事实有歧义，以英文版本为准。</p></blockquote><h2 id="_1-目标" tabindex="-1">1. 目标 <a class="header-anchor" href="#_1-目标" aria-label="Permalink to &quot;1. 目标&quot;">​</a></h2><p>完成与插件相关的主机服务和 UI IPC，以便实现不依赖于临时约定。</p><h2 id="_2-主进程服务" tabindex="-1">2. 主进程服务 <a class="header-anchor" href="#_2-主进程服务" aria-label="Permalink to &quot;2. 主进程服务&quot;">​</a></h2><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>PluginManager</span></span>
<span class="line"><span> ├─ PluginRegistryStore</span></span>
<span class="line"><span> ├─ ManifestValidator</span></span>
<span class="line"><span> ├─ PackageInstaller</span></span>
<span class="line"><span> ├─ PermissionGateway</span></span>
<span class="line"><span> ├─ ContributionRegistry</span></span>
<span class="line"><span> │ ├─ CommandRegistry</span></span>
<span class="line"><span> │ ├─ AgentToolRegistry</span></span>
<span class="line"><span> │ ├─ SkillRegistry</span></span>
<span class="line"><span> │ ├─ ThemeRegistry</span></span>
<span class="line"><span> │ └─ McpServerRegistry</span></span>
<span class="line"><span> ├─ PluginRuntimeBroker</span></span>
<span class="line"><span> │ ├─ ServiceSupervisor</span></span>
<span class="line"><span> │ └─ MessageBus</span></span>
<span class="line"><span> ├─ PluginPanelHostService</span></span>
<span class="line"><span> └─ MarketClient</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br></div></div><h2 id="_3-ui-ipc-添加" tabindex="-1">3. UI IPC（添加） <a class="header-anchor" href="#_3-ui-ipc-添加" aria-label="Permalink to &quot;3. UI IPC（添加）&quot;">​</a></h2><h3 id="插件域" tabindex="-1">插件域 <a class="header-anchor" href="#插件域" aria-label="Permalink to &quot;插件域&quot;">​</a></h3><ul><li><code>plugin/list</code></li><li><code>plugin/detail</code></li><li><code>plugin/loadDev</code></li><li><code>plugin/reload</code> — 解析注册的插件路径，在 Electron 中重新加载它 main，并刷新开发插件权限上限</li><li><code>plugin/installFromPath</code> ✅</li><li><code>plugin/installFromPackage</code> ✅</li><li><code>plugin/enable</code></li><li><code>plugin/disable</code></li><li><code>plugin/uninstall</code></li><li><code>plugin/reload</code></li><li><code>plugin/getLogs</code></li><li><code>plugin/getPermissions</code></li><li><code>plugin/grantPermissions</code></li><li><code>plugin/revokePermissions</code></li><li><code>plugin/openDataDir</code></li><li><code>plugin/openInstallDir</code></li><li><code>plugin/openPanel</code></li><li><code>plugin/setAutoUpdate</code></li><li><code>plugin/themes</code> ✅ — 每个加载的插件的清理主题 CSS，用于主题 选择器和注入的 <code>&lt;style&gt;</code> 元素</li><li><code>plugin/services</code> ✅ — 居民服务状态 (<code>starting</code> | <code>running</code> | <code>stopped</code> | <code>failed</code>) 加上重新启动计数，用于插件页面芯片</li></ul><h3 id="commandpalette-域" tabindex="-1">commandPalette 域 <a class="header-anchor" href="#commandpalette-域" aria-label="Permalink to &quot;commandPalette 域&quot;">​</a></h3><ul><li><code>commandPalette/search</code></li><li><code>commandPalette/execute</code></li><li><code>commandPalette/listRecent</code></li></ul><h3 id="市场域-已实现" tabindex="-1">市场域（已实现） <a class="header-anchor" href="#市场域-已实现" aria-label="Permalink to &quot;市场域（已实现）&quot;">​</a></h3><ul><li><code>market/search</code></li><li><code>market/getDetail</code></li><li><code>market/install</code></li><li><code>market/checkUpdates</code></li><li><code>market/applyUpdates</code></li><li><code>market/listProviders</code>（目前为单一官方提供商）</li></ul><h2 id="_4-事件-主-→-渲染器" tabindex="-1">4. 事件（主 → 渲染器） <a class="header-anchor" href="#_4-事件-主-→-渲染器" aria-label="Permalink to &quot;4. 事件（主 → 渲染器）&quot;">​</a></h2><ul><li><code>plugin/event/changed</code>（installed/enabled 更改）</li><li><code>plugin/event/loadError</code></li><li><code>plugin/event/permissionRequired</code></li><li><code>market/event/updateAvailable</code></li></ul><p>附带的 <code>pluginChanged</code> 事件带有 <code>reason</code>，以便渲染器可以决定 重新获取的内容：<code>install</code>、<code>loadDev</code>、<code>enable</code>、<code>disable</code>、<code>uninstall</code>、<code>crash</code>、 <code>service</code>、<code>market.install</code>、<code>market.applyUpdates</code>。 <code>service</code> 在每个 监管过渡是其中最便宜的——只需要服务列表 重新加载。</p><h2 id="_4-1-事件-主机→插件进程" tabindex="-1">4. 1 事件（主机→插件进程） <a class="header-anchor" href="#_4-1-事件-主机→插件进程" aria-label="Permalink to &quot;4. 1 事件（主机→插件进程）&quot;">​</a></h2><p>代理还将单向帧推送到插件的 <code>utilityProcess</code>：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>{ t: &quot;event&quot;, event: &quot;bus.message&quot;, subscriptionId, message }</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>没有回复帧，也没有背压：交付是一劳永逸的，所以 楔入的订阅者无法阻止发布者。孩子分派给处理程序 注册 <code>subscriptionId</code> 和任何 <code>pi.events.on</code> 侦听器；一次投掷 处理程序已记录，永远不会致命。这与最终使 <code>pi.events.on</code> / <code>off</code> 真实（参见 <a href="/zh-CN/spec/07-plugins/03-plugin-api">03-plugin-api.md</a> §5)。</p><h2 id="_5-contributionregistry-行为" tabindex="-1">5. ContributionRegistry 行为 <a class="header-anchor" href="#_5-contributionregistry-行为" aria-label="Permalink to &quot;5. ContributionRegistry 行为&quot;">​</a></h2><h3 id="注册" tabindex="-1">注册 <a class="header-anchor" href="#注册" aria-label="Permalink to &quot;注册&quot;">​</a></h3><ul><li>密钥必须是唯一的</li><li>插件命令共享前缀：<code>plugin.&lt;pluginId&gt;.&lt;commandId&gt;</code></li><li>插件工具前缀策略：<code>plugin_&lt;pluginIdSafe&gt;_&lt;toolName&gt;</code>（在实现中修复）</li></ul><h3 id="查询" tabindex="-1">查询 <a class="header-anchor" href="#查询" aria-label="Permalink to &quot;查询&quot;">​</a></h3><ul><li>命令面板仅查询已启用+加载成功的贡献</li><li>Agent 查看已注册的工具；无论风险如何，Plan 均不会收到任何插件工具 或授予状态</li></ul><h3 id="注销" tabindex="-1">注销 <a class="header-anchor" href="#注销" aria-label="Permalink to &quot;注销&quot;">​</a></h3><ul><li>删除 disable/unload/uninstall 上的所有内容，包括停止驻留 服务、断开 MCP 服务器、删除总线订阅以及删除 来自选择器的插件主题</li></ul><h2 id="_6-runtimebroker-调用链" tabindex="-1">6. RuntimeBroker 调用链 <a class="header-anchor" href="#_6-runtimebroker-调用链" aria-label="Permalink to &quot;6. RuntimeBroker 调用链&quot;">​</a></h2><p>插件 API 调用：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>plugin runtime</span></span>
<span class="line"><span> → RPC to PluginRuntimeBroker</span></span>
<span class="line"><span> → PermissionGateway.check</span></span>
<span class="line"><span> → Host service execute</span></span>
<span class="line"><span> → audit log</span></span>
<span class="line"><span> → response</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><h3 id="_6-1-实时能力的白名单名称与审计操作" tabindex="-1">6.1 实时能力的白名单名称与审计操作 <a class="header-anchor" href="#_6-1-实时能力的白名单名称与审计操作" aria-label="Permalink to &quot;6.1 实时能力的白名单名称与审计操作&quot;">​</a></h3><p>代理的 <code>HOST_API_ALLOWLIST</code> 新增三个已实现的条目，全部由 <code>keyboard.globalShortcut</code> 把关：</p><ul><li><code>keyboard.registerGlobalShortcut</code></li><li><code>keyboard.unregisterGlobalShortcut</code></li><li><code>keyboard.listGlobalShortcuts</code></li></ul><p>对应的审计操作是 <code>keyboard.globalShortcut.register</code>、 <code>keyboard.globalShortcut.unregister</code> 和 <code>keyboard.globalShortcut.trigger</code> （快捷键被触发）。注册与触发的条目会记录加速键和命令；按键事件与输入文本绝不记录。</p><p>套接字能力已实现：<code>net.websocket.connect</code> / <code>net.websocket.send</code> / <code>net.websocket.close</code> 已注册进同一份白名单，由 <code>net.websocket</code> 把关。它的审计 操作是 <code>net.websocket.connect</code> 与 <code>net.websocket.close</code>（记录插件 id 与结果， 从不记录载荷、请求头或密钥），外加被拒绝的 <code>net.websocket.send</code>；成功的 <code>send</code> 不记入审计。帧只回到持有它的插件，以宿主事件 <code>net:websocket:open</code>、 <code>net:websocket:message</code>、<code>net:websocket:close</code> 和 <code>net:websocket:error</code> 的形式送达。</p><p>音频能力的名字已注册进同一份白名单：<code>audio.getInputDevices</code>、 <code>audio.openInput</code>、<code>audio.closeInput</code>、<code>audio.getCaptureState</code>、 <code>audio.onInputFrame</code>、<code>audio.offInputFrame</code>、<code>audio.openOutput</code>、 <code>audio.writeOutput</code>、<code>audio.stopOutput</code> 和 <code>audio.closeOutput</code>。权限把关先执行： 没有授权的调用会与其他任何需要把关的 API 一样，以 <code>PERMISSION_DENIED</code> 拒绝， 并归到 <code>audio.capture.background</code> / <code>audio.playback.background</code> 名下。当前宿主 还没有设备后端，所以每个通过把关的调用都会作为 <code>UNSUPPORTED</code> 拒绝记入审计 —— <code>{ api: &quot;audio.&lt;method&gt;&quot;, ok: false, errorCode: &quot;UNSUPPORTED&quot; }</code> —— 并以该错误码 拒绝；两个同步注册辅助函数 <code>audio.onInputFrame</code> / <code>audio.offInputFrame</code> 会同步 抛出它。为设备服务预留的审计操作名：<code>audio.input.open</code> / <code>audio.input.close</code>、 <code>audio.output.open</code> / <code>audio.output.stop</code> / <code>audio.output.close</code>（ADR 0257）。</p><h2 id="_7-panelhost交互" tabindex="-1">7. PanelHost交互 <a class="header-anchor" href="#_7-panelhost交互" aria-label="Permalink to &quot;7. PanelHost交互&quot;">​</a></h2><ul><li>打开面板时创建独立视图</li><li>传入pluginId /主题令牌</li><li>关闭时销毁视图和消息订阅。需要 <code>webContents</code> 身份的清理必须在窗口销毁前复制该 id；<code>closed</code> 处理程序不得在已销毁的窗口上读取 <code>webContents</code>，否则宿主会抛出未捕获的 <code>TypeError: Object has been destroyed</code>。</li><li>preload 通过 <code>pluginBridge.getDroppedFilePath(file)</code> 暴露文件路径，但不向页面暴露 Node。面板可以把路径传给 <code>fs.registerDropped</code>；宿主会一次性消费发送方最近的拖拽记录，为 <code>fs.stat</code> / <code>fs.readRange</code> 签发单文件读取授权。</li></ul><p>面板桥接文件通道的权限如下：</p><table tabindex="0"><thead><tr><th>通道</th><th>所需权限</th></tr></thead><tbody><tr><td><code>fs.readText</code>、<code>fs.stat</code>、<code>fs.readRange</code>、<code>fs.readPreview</code>、<code>fs.openDefault</code>、<code>fs.reveal</code>、<code>fs.glob</code>、<code>fs.list</code></td><td><code>fs.read</code></td></tr><tr><td><code>fs.registerDropped</code></td><td><code>fs.read</code> 加真实拖拽手势</td></tr><tr><td><code>fs.writeText</code></td><td><code>fs.write</code></td></tr></tbody></table><h2 id="_8-故障隔离" tabindex="-1">8. 故障隔离 <a class="header-anchor" href="#_8-故障隔离" aria-label="Permalink to &quot;8. 故障隔离&quot;">​</a></h2><ul><li>插件API超时：返回TIMEOUT</li><li>运行时崩溃：标记load_error，清理贡献</li><li>面板崩溃：只关闭面板，不卸载插件（可提示重新加载）</li></ul><p><strong>实施（2026-07-29，ADR 0008）：</strong> 经纪人居住在 <code>electron/main/plugin-runtime.ts</code> 和每个插件调用都是对 插件自己的 <code>utilityProcess</code>。预算：加载 15 秒，生命周期挂钩 5 秒，命令 30 秒， 工具 110 秒（根据 host-core 的 150 秒调度预算）。进程退出经纪人 使用 <code>PLUGIN_CRASHED</code> 拒绝挂起的调用，取消注册该插件的命令 和工具，关闭其面板，写入 <code>plugin.crash</code> 审核条目，并发出 toast 加上 <code>pluginChanged</code> 到渲染器。</p><h2 id="_9-验收" tabindex="-1">9. 验收 <a class="header-anchor" href="#_9-验收" aria-label="Permalink to &quot;9. 验收&quot;">​</a></h2><p>1.插件列表IPC有效 2.命令面板IPC可以执行插件命令 3. Start/stop 触发贡献 registration/deregistration 4. 市场 IPC 在模拟提供商下端到端运行（后来的里程碑）</p><h2 id="附录-代理工具调度协议-m5实现" tabindex="-1">附录：代理工具调度协议（M5实现） <a class="header-anchor" href="#附录-代理工具调度协议-m5实现" aria-label="Permalink to &quot;附录：代理工具调度协议（M5实现）&quot;">​</a></h2><p>插件代理工具在桌面运行程序 (Electron main) 中执行，而 权限门和结果信封保留在 host-core 中：</p><p>1.模型调用<code>plugin_&lt;pluginIdSafe&gt;_&lt;toolName&gt;</code>； sidecar 转发它 像任何内置工具一样托管 <code>tools.execute</code>。 2. host-core 首先解析持久操作模式。在 Agent 中，它运行 正常权限流程（风险、会话授予、120 秒超时），然后发出 通知 <code>plugins.execute</code><code>{ executionId, sessionId, toolCallId, toolName, args, turnId }</code>。<code>turnId</code> 是 运行时回合身份，原样转发，以便插件工具上下文能与 <code>session:turnEnded</code> 事件对应。 3. Plan 调用在主机策略步骤失败并显示 <code>PLUGIN_DISABLED_IN_PLAN</code>；他们 永远不会达到 Electron 或插件运行时。 Agent 呼叫继续 Electron主要执行注册的插件工具JS并通过RPC应答 <code>plugins.resolveExecution</code> <code>{ executionId, ok, content, errorCode? }</code>。 4. host-core 解析待执行并返回一个标准 <code>ToolsExecuteResult</code> 到 sidecar。调度最多等待 150 秒 （<code>DESKTOP_TOOL_DISPATCH_TIMEOUT_MS</code>，高于 110 秒的插件工具预算，也高于最宽的 MCP 支路：10 秒惰性握手 + 30 秒 <code>tools/list</code> 遍历 + 100 秒调用），超时映射到 <code>TOOL_TIMEOUT</code>；unknown/unloaded 工具映射到 <code>TOOL_NOT_FOUND</code>。这类调用的传输截止 时间覆盖 120 秒权限等待、30 秒准入排队、上述调度和 10 秒余量（<code>rpcTimeoutMs</code>）， 因此外层不会在 host-core 报告结果之前先放弃。</p><p>面向模型的注册表根据提示获得插件工具：已注册主要通道 defs（<code>fullName</code>、描述、JSON 架构参数）到 <code>agent.prompt</code>，以及 运行时将它们保存在延迟目录中，而不是序列化每个 架构到第一个请求中。该模型通过加载匹配的插件工具 本地 <code>ToolSearch</code> 工具；下一轮接收选定的模式并 然后使用上面相同的主机 permission/dispatch 路径。协议涵盖 烟雾场景 E2E-024 和运行时加载场景 E2E-008a。</p><p>从插件的 MCP 服务器发现的工具会进入相同的注册表 <code>plugin_&lt;pluginIdSafe&gt;_&lt;serverId&gt;_&lt;toolName&gt;</code>，所以上面的步骤1-4不变； 只有步骤 3 内部有所不同，转发到 MCP 客户端而不是插件 JS。</p><p>技能使用单独的、更简单的路径。目录（id、名称、描述）是一部分 在基本系统提示符中，<code>Skill</code> 模式本身被推迟到后面 <code>ToolSearch</code>，其主体由 Electron main 的本地 <code>Skill</code> 工具获取 直接服务——sidecar从不保存技能文本和技能文档 仅当模型需要时才到达模型 (D174/D185)。</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("zh-CN/spec/07-plugins/12-plugin-ipc-and-host-services.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _12PluginIpcAndHostServices = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _12PluginIpcAndHostServices as default
};
