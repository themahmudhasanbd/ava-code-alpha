import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"图片生成与编辑","description":"","frontmatter":{},"headers":[],"relativePath":"zh-CN/spec/03-runtime/21-image-generation.md","filePath":"zh-CN/spec/03-runtime/21-image-generation.md","lastUpdated":1790040855000}');
const _sfc_main = { name: "zh-CN/spec/03-runtime/21-image-generation.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="图片生成与编辑" tabindex="-1">图片生成与编辑 <a class="header-anchor" href="#图片生成与编辑" aria-label="Permalink to &quot;图片生成与编辑&quot;">​</a></h1><blockquote><p><strong>翻译说明：</strong> 本页对应 <a href="/spec/03-runtime/21-image-generation">英文源规格</a>。代码、协议字段和标识符保持原文；如有歧义，以英文版本为准。</p></blockquote><p>桌面通过 <code>AppSettings.imageGeneration</code> 保存当前默认生图绑定，并通过可选的 <code>AppSettings.imageGenerationModels</code> 保存已标记的生图模型列表。缺省候选列表时兼容旧数据，把单绑定视为唯一候选；<code>providerId</code> 和 <code>modelId</code> 仍是每个绑定的字段。host-core 使用现有设置存储校验并持久化，无需数据库版本升级。每个候选独立于默认对话模型，引用已启用的 API-key 或免认证服务商及其已配置模型。</p><h2 id="配置" tabindex="-1">配置 <a class="header-anchor" href="#配置" aria-label="Permalink to &quot;配置&quot;">​</a></h2><p>模型高级设置将“设为生图模型”与图片、文档附件能力放在同一能力组中，不再单独占一行；复选框支持多选，保存服务商表单后把所有勾选模型写入 <code>imageGenerationModels</code>，取消不会改变设置。保存候选不会替换默认对话模型。取消勾选当前生图模型并保存后，默认生图绑定改为剩余的第一个可用候选；没有可用候选时清空。取消标记的模型恢复为可选对话模型，重新打开设置后仍保持这一状态。保存其他服务商时保留仍可用的默认生图模型。默认模型下方同一面板中的“生图模型”显示当前默认，并提供菜单从所有已标记候选中选择一个。没有候选时隐藏摘要行；已存在但服务商停用、缺少凭据或模型移除的候选仅显示“暂不可用”。OAuth 账户不适用，也不会自动回退。 所有已标记的服务商／模型组合都会从默认对话模型选择器、服务商快速设为默认操作和 Composer 模型菜单中排除。其他服务商的同名模型独立保留。已有会话绑定和历史不改写；仍绑定任一生图候选的会话必须选择对话模型才能发送，运行时也会在推理前拒绝所有已标记生图模型。</p><h2 id="agent-合约" tabindex="-1">Agent 合约 <a class="header-anchor" href="#agent-合约" aria-label="Permalink to &quot;Agent 合约&quot;">​</a></h2><p><code>GenerateImages({items: [{prompt, count?, images?}]})</code> 仅供 Agent 模式使用。<code>count</code> 默认为 1，支持不同提示词和同一提示词的多个变体，总输出为 1–10 张。每项可通过 <code>images</code> 提供 1–4 张本地参考图，之前生成的路径可用于迭代编辑。提示词最多 32,000 个字符；不支持或超限输入直接拒绝，不截断。Plan 和 Goal 不得执行此工具。</p><p>内置 <code>pi-desktop/imagegen</code> skill 可在普通会话中发现，并通过现有 Skill 工具加载。它说明提示词、批量、参考图编辑、保留原图、部分失败处理和项目素材交付，不授予权限，也不携带凭据。</p><p>可信桌面桥先使用相同身份、参数和权限范围调用 host-core <code>tools.execute</code>。host-core 应用现有高风险工具策略，仅返回授权结果；获得批准后桥才执行图片服务。宿主授权审计与 sidecar 的实际工具结果分开记录。取消会传递给待批准操作和网络请求；宿主断开或 sidecar 销毁时中止本地工作。</p><h2 id="openai-images-适配器" tabindex="-1">OpenAI Images 适配器 <a class="header-anchor" href="#openai-images-适配器" aria-label="Permalink to &quot;OpenAI Images 适配器&quot;">​</a></h2><p>仅支持 OpenAI-compatible Images。根地址自动补充 <code>/v1</code>，显式路径前缀保留。生成通过 <code>POST images/generations</code> 发送 <code>model</code>、<code>prompt</code> 和 <code>n: 1</code>；编辑通过 <code>POST images/edits</code> 发送 multipart 图片、提示词、模型和 <code>n: 1</code>。不包含浏览器蒙版编辑器。服务商可能只支持生成；编辑错误直接报告，不悄悄改为生成或切换模型。</p><p>对于精确匹配的 DALL-E 2/3 模型 ID，生成请求显式发送 <code>response_format: b64_json</code>，支持的 DALL-E 编辑请求使用同名表单字段。GPT Image 和未知兼容模型省略该参数。编辑使用二进制 multipart 上传，单图字段为 <code>image</code>，多图为 <code>image[]</code>，Content-Type boundary 由传输层生成。</p><p>每批请求在分发前快照模型绑定和服务商，使用两个工作线程，并按输入顺序返回结果。每张输出的请求预算为 180 秒。请求不会自动重试；认证失败停止队列工作。取消停止队列并中止活动 HTTP 请求，但不能保证上游停止处理或收费。已完成的输出文件保留。</p><p>每次响应接受一张 Base64 图片或 HTTPS 图片地址。JSON 和下载响应体有大小限制；图片最多 16 MiB，只接受 PNG、JPEG、WebP 文件签名。下载使用已校验并固定的公网 DNS 地址，拒绝重定向和私网目标，也不携带服务商请求头。</p><p>编辑输入必须在 realpath 校验后属于会话项目、该会话 scratch 目录或附件存储。每组参考图最多 32 MiB，每批输入缓存预算为 64 MiB。凭据不会进入渲染进程或工具结果。</p><h2 id="结果与恢复" tabindex="-1">结果与恢复 <a class="header-anchor" href="#结果与恢复" aria-label="Permalink to &quot;结果与恢复&quot;">​</a></h2><p>每项结果记录序号、状态（<code>succeeded</code>、<code>failed</code>、<code>cancelled</code>）、成功路径/MIME 类型或安全错误码。输出以唯一文件名保存至会话 scratch，编辑不覆盖原图。工具结果和对话只保留文件引用，不保存 Base64；预览复用现有受限图片读取和文件查看器。</p><p>本地工具抛出的稳定 <code>errorCode</code> 跨真实 sidecar RPC 边界保留，独立于普通结构化工具失败结果。现有 RPC code、message 和 data 保留，不序列化任意 Error 属性；sidecar 接收端同时提供错误码与 data。</p><p>部分成功时仍展示成功图片和逐项失败。未配置返回结构化错误及“设置 → 模型”跳转操作。会话重载或重启后同一引用仍可显示。图片结果和配置操作显示在可折叠过程详情之外，展开工具详情不会重复图片列表。</p><p>生成文件绝对路径（包括 Windows 盘符路径）的 Markdown 图片引用通过现有受限宿主图片读取接口解析。URL 清理仍启用，宿主继续拒绝允许的工作区、scratch 和附件根目录之外的文件。</p><p>验证：<code>node scripts/e2e-image-generation.mjs</code> 覆盖宿主、stdio、HTTP 和存储；<code>node scripts/e2e-image-generation-ui.mjs</code> 使用 API 边界夹具覆盖真实 React/Chromium 交互。单元及服务测试覆盖限制、取消、部分失败、认证、超时、不安全路径和受限下载。</p><p><code>node scripts/e2e-image-chat.mjs</code> 在隔离桌面中使用本地模型/图片 HTTP 夹具，覆盖相邻默认设置、Composer 提交、批量结果、引用生成文件编辑、收起详情和配置跳转。真实接口验证通过 <code>scripts/test-image-generation-live.mjs</code> 显式启用，仅限一次生成和一次编辑，不属于默认测试命令。</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("zh-CN/spec/03-runtime/21-image-generation.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _21ImageGeneration = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _21ImageGeneration as default
};
