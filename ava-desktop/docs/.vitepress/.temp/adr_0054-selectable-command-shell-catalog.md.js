import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0054: Selectable command shell catalog and execution identity","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0054-selectable-command-shell-catalog.md","filePath":"adr/0054-selectable-command-shell-catalog.md","lastUpdated":1789019034000}');
const _sfc_main = { name: "adr/0054-selectable-command-shell-catalog.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0054-selectable-command-shell-catalog-and-execution-identity" tabindex="-1">ADR 0054: Selectable command shell catalog and execution identity <a class="header-anchor" href="#adr-0054-selectable-command-shell-catalog-and-execution-identity" aria-label="Permalink to &quot;ADR 0054: Selectable command shell catalog and execution identity&quot;">​</a></h1><ul><li>Status: Accepted for implementation (timeout bounds in §4 amended by ADR 0167 / D329; the PowerShell 7 entry is added by ADR 0209 / D381)</li><li>Date: 2026-07-31</li><li>Baseline: <code>0.4.14</code></li><li>Protocol: v9</li><li>Storage schema: v10</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The Bash tool currently resolves one Bash implementation per process. That prevents users from choosing the command language that matches their project and makes a changed executable hard to detect. The protocol name and Agent tool vocabulary must remain stable while shell selection becomes explicit and host-authoritative.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><h3 id="_1-host-owned-shell-catalog" tabindex="-1">1. Host-owned shell catalog <a class="header-anchor" href="#_1-host-owned-shell-catalog" aria-label="Permalink to &quot;1. Host-owned shell catalog&quot;">​</a></h3><p>Host-core exposes a platform-aware catalog with stable IDs:</p><table tabindex="0"><thead><tr><th>ID</th><th>Shell</th><th>Discovery</th></tr></thead><tbody><tr><td><code>windows-powershell</code></td><td>in-box Windows PowerShell 5.1</td><td><code>powershell.exe</code>/native PowerShell on Windows</td></tr><tr><td><code>windows-pwsh</code></td><td>PowerShell 7+ (side-by-side install)</td><td><code>pwsh.exe</code> under <code>%ProgramFiles%\\PowerShell\\7</code> or on PATH</td></tr><tr><td><code>cmd</code></td><td>Windows Command Prompt</td><td><code>cmd.exe</code> on Windows</td></tr><tr><td><code>git-bash</code></td><td>Git for Windows Bash</td><td>Git for Windows installation and PATH</td></tr><tr><td><code>bash</code></td><td>Unix Bash</td><td><code>/bin/bash</code>, <code>/usr/bin/bash</code>, or an approved PATH entry on macOS/Linux</td></tr></tbody></table><p>The Windows catalog contains <code>windows-powershell</code>, <code>windows-pwsh</code>, <code>cmd</code>, and <code>git-bash</code>; the Unix catalog contains <code>bash</code>. The catalog does not accept an arbitrary renderer- or sidecar-supplied executable path. Windows PowerShell 5.1 stays the platform default; <code>windows-pwsh</code> is selectable but never selected implicitly, so choosing it cannot change an existing user&#39;s shell by surprise.</p><p>Settings writes accept only an available ID for the current platform. Unknown, unavailable, and wrong-platform IDs are rejected. If a persisted ID later becomes unavailable, host-core intentionally selects the first available shell in the platform catalog and reports <code>fallback: true</code>; if none is available, Bash fails with <code>SHELL_NOT_FOUND</code>.</p><h3 id="_2-persisted-default-and-stable-identity" tabindex="-1">2. Persisted default and stable identity <a class="header-anchor" href="#_2-persisted-default-and-stable-identity" aria-label="Permalink to &quot;2. Persisted default and stable identity&quot;">​</a></h3><p>The host persists one <code>defaultCommandShell</code> ID in app settings. Selection is allowed only from the available catalog and only while the affected session is idle. At turn launch, the runtime pins the effective shell ID and dialect. The execution request carries the pinned ID; host-core resolves the catalog again before spawn and rejects a changed effective ID or dialect with <code>COMMAND_SHELL_CHANGED</code>. This identity check is about the catalog selection, not executable path hashing. A runtime fallback is selected before the turn is pinned; execution never silently changes shell after that point.</p><h3 id="_3-stable-bash-protocol-contract" tabindex="-1">3. Stable Bash protocol contract <a class="header-anchor" href="#_3-stable-bash-protocol-contract" aria-label="Permalink to &quot;3. Stable Bash protocol contract&quot;">​</a></h3><p>The Agent tool and host method remain <code>Bash</code> and <code>tools.execute</code>; shell choice is data on the request, not a new <code>PowerShell</code>, <code>Cmd</code>, or <code>GitBash</code> tool name. The command runs non-interactively in the originating session workspace with the selected shell&#39;s documented invocation form.</p><p>Host-core streams stdout and stderr as separate ordered output events. The final tool result remains bounded and records whether either stream was truncated. No stream chunk may contain secrets or be attributed to another session/turn.</p><h3 id="_4-timeout-and-cancellation" tabindex="-1">4. Timeout and cancellation <a class="header-anchor" href="#_4-timeout-and-cancellation" aria-label="Permalink to &quot;4. Timeout and cancellation&quot;">​</a></h3><p>Every Bash execution has a mandatory 60-second default timeout. A caller may request a host-validated override only from 1 second through 300 seconds; missing values use exactly 60 seconds, and out-of-range values are rejected. Timeout and user abort terminate the complete process tree, not only the shell leader: Unix uses a process group and Windows uses a job/process-tree boundary. The host waits for shutdown, closes the stream, records the terminal outcome, and returns <code>TOOL_TIMEOUT</code> or <code>TURN_ABORTED</code> without leaving an orphan.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Users can choose the command language once and keep that default across sessions and restarts.</li><li>A changed effective shell selection cannot receive commands under stale assumptions, while a persisted unavailable preference can recover through the intentional catalog fallback.</li><li>Streaming makes long commands observable without weakening final result limits.</li><li>The stable Bash protocol avoids multiplying tool schemas and compatibility paths.</li></ul><h2 id="alternatives-rejected" tabindex="-1">Alternatives rejected <a class="header-anchor" href="#alternatives-rejected" aria-label="Permalink to &quot;Alternatives rejected&quot;">​</a></h2><h3 id="always-use-bash" tabindex="-1">Always use Bash <a class="header-anchor" href="#always-use-bash" aria-label="Permalink to &quot;Always use Bash&quot;">​</a></h3><p>Rejected because it excludes native Windows command workflows and makes the user&#39;s shell preference invisible.</p><h3 id="let-the-caller-provide-an-arbitrary-executable-path" tabindex="-1">Let the caller provide an arbitrary executable path <a class="header-anchor" href="#let-the-caller-provide-an-arbitrary-executable-path" aria-label="Permalink to &quot;Let the caller provide an arbitrary executable path&quot;">​</a></h3><p>Rejected because it bypasses catalog policy and makes identity validation and security review unreliable.</p><h3 id="create-one-protocol-tool-per-shell" tabindex="-1">Create one protocol tool per shell <a class="header-anchor" href="#create-one-protocol-tool-per-shell" aria-label="Permalink to &quot;Create one protocol tool per shell&quot;">​</a></h3><p>Rejected because it breaks existing Bash skills and expands the permission and audit matrix without adding authority.</p><h2 id="related-docs" tabindex="-1">Related docs <a class="header-anchor" href="#related-docs" aria-label="Permalink to &quot;Related docs&quot;">​</a></h2><ul><li><code>docs/adr/0053-plan-checkpoint-artifact-and-execution-epoch.md</code></li><li><code>docs/spec/03-runtime/01-ipc-protocol.md</code></li><li><code>docs/spec/03-runtime/03-tools-and-permissions.md</code></li><li><code>docs/spec/03-runtime/05-host-core-rust.md</code></li><li><code>docs/spec/03-runtime/06-host-rpc-protocol.md</code></li><li><code>docs/spec/03-runtime/07-process-model.md</code></li><li><code>docs/spec/03-runtime/08-error-codes.md</code></li><li><code>docs/spec/03-runtime/09-logging-and-observability.md</code></li><li><code>docs/spec/03-runtime/16-tool-result-limits.md</code></li><li><code>docs/spec/04-ux/06-settings-ia.md</code></li><li><code>docs/spec/04-ux/08-component-spec.md</code></li><li><code>docs/spec/04-ux/09-interaction-patterns.md</code></li><li><code>docs/spec/05-security/01-security.md</code></li><li><code>docs/spec/06-delivery/04-e2e-test-plan.md</code></li><li><code>docs/spec/08-meta/decisions-log.md</code> (D190)</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0054-selectable-command-shell-catalog.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0054SelectableCommandShellCatalog = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0054SelectableCommandShellCatalog as default
};
