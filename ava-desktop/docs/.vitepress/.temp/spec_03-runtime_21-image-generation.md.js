import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Image generation and editing","description":"","frontmatter":{},"headers":[],"relativePath":"spec/03-runtime/21-image-generation.md","filePath":"spec/03-runtime/21-image-generation.md","lastUpdated":1790040855000}');
const _sfc_main = { name: "spec/03-runtime/21-image-generation.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="image-generation-and-editing" tabindex="-1">Image generation and editing <a class="header-anchor" href="#image-generation-and-editing" aria-label="Permalink to &quot;Image generation and editing&quot;">​</a></h1><p>The desktop exposes <code>AppSettings.imageGeneration</code> as the current default image-generation binding and <code>AppSettings.imageGenerationModels</code> as the optional list of models marked for image generation. The legacy single binding remains supported: when the list is absent, it is treated as the only candidate. A null or empty current binding means no default; host-core validates and persists both fields through the existing settings store. No schema bump is needed. Each candidate is independent of the conversation default and references an existing enabled API-key or no-auth provider and one of its configured models.</p><h2 id="configuration" tabindex="-1">Configuration <a class="header-anchor" href="#configuration" aria-label="Permalink to &quot;Configuration&quot;">​</a></h2><p>Model Advanced exposes <strong>Set as image model</strong> alongside the image and document attachment capabilities in the model capability group, not as a separate control row. The checkbox is multi-select: saving a provider persists every checked model in <code>imageGenerationModels</code>; Cancel leaves settings unchanged. Saving candidates does not replace the default conversation model. Unchecking the current image model and saving removes its default binding: the first remaining runnable candidate becomes the default, or the default becomes null if none remains. The unmarked model is available for chat again after saving and reopening settings. Saving another provider preserves a still-runnable image default. Below the default model row in the same defaults panel, <strong>Image generation model</strong> shows the current default and offers a menu to choose one from all marked candidates. When no candidate is configured, the summary row is hidden. An existing candidate that is missing, disabled, credential-less or removed displays only <strong>Currently unavailable</strong>. OAuth accounts are not eligible; there is no fallback. All marked provider/model pairs are excluded from the default conversation picker, provider quick-default action, and Composer model menu. Other providers with the same model ID remain independent. Existing conversation bindings and history are preserved; a conversation still pinned to any image candidate must select a chat model before sending. Runtime launch rejects every marked image model before inference.</p><h2 id="agent-contract" tabindex="-1">Agent contract <a class="header-anchor" href="#agent-contract" aria-label="Permalink to &quot;Agent contract&quot;">​</a></h2><p><code>GenerateImages({items: [{prompt, count?, images?}]})</code> is an Agent-mode tool. <code>count</code> defaults to 1. Both distinct prompts and same-prompt variants are supported, with 1–10 output images total. An optional <code>images</code> array supplies 1–4 local source files per item for editing; a previous generated path supports iterative edits. Prompt length is bounded at 32,000 characters. Unsupported or excessive input is rejected rather than truncated. Plan and Goal cannot execute the tool.</p><p>The bundled <code>pi-desktop/imagegen</code> skill is discoverable in ordinary sessions and loads through the existing Skill tool. It teaches prompting, batches, reference edits, preserving originals, partial-failure handling and project asset delivery. It does not grant permission or carry credentials.</p><p>The trusted desktop bridge first calls host-core <code>tools.execute</code> with the same identity, arguments and permission scope. Host-core applies the existing high-risk tool policy and returns authorization only. The bridge executes the image service only after approval. The host authorization audit is distinct from the actual sidecar tool result. Runtime cancellation reaches both pending approval and the network request; host loss or sidecar disposal aborts local work.</p><h2 id="openai-images-adapter" tabindex="-1">OpenAI Images adapter <a class="header-anchor" href="#openai-images-adapter" aria-label="Permalink to &quot;OpenAI Images adapter&quot;">​</a></h2><p>Only OpenAI-compatible Images is supported. A root base URL acquires <code>/v1</code>; an explicit path prefix is retained. Generation uses <code>POST images/generations</code> with <code>model</code>, <code>prompt</code>, <code>n: 1</code>. Editing uses <code>POST images/edits</code>, multipart image files, the prompt, model and <code>n: 1</code>. No browser mask editor is included. A configured service may implement generation without editing; its error is reported without silently switching to generation or another model. For exact DALL-E 2/3 model IDs, generation requests explicitly ask for <code>response_format: b64_json</code>; the same form field is used for DALL-E edits where supported. GPT Image and unknown compatible model IDs omit that parameter. Editing retains binary multipart uploads (<code>image</code> for one reference, <code>image[]</code> for multiple), with the transport generating the Content-Type boundary.</p><p>Each batch snapshots the binding and provider before dispatch, uses two workers, and returns results in input order. Each output has a 180-second request budget. Requests are never automatically retried. Authentication failure stops queued work. Cancellation stops queued work and aborts active HTTP; it cannot promise the upstream provider stopped processing or billing. Completed output files survive.</p><p>Responses accept exactly one Base64 image or HTTPS image URL per request. JSON and download bodies are bounded; image files are capped at 16 MiB and restricted to PNG, JPEG and WebP signatures. Downloads use checked, pinned public DNS addresses, reject redirects and private destinations, and never receive provider headers. Input edits accept the session project, that session&#39;s scratch directory and the attachment store after realpath containment. Each edit input set is capped at 32 MiB, with a 64 MiB input cache budget for the batch. Credentials remain outside the renderer and tool results.</p><h2 id="results-and-recovery" tabindex="-1">Results and recovery <a class="header-anchor" href="#results-and-recovery" aria-label="Permalink to &quot;Results and recovery&quot;">​</a></h2><p>Each result records index, status (<code>succeeded</code>, <code>failed</code>, <code>cancelled</code>), successful path/MIME type or a safe error code. New files get unique names in session scratch; editing never overwrites its source. The tool result and transcript retain file references, not Base64. Existing bounded image reads and file viewers serve previews. Thrown local-tool errors preserve stable <code>errorCode</code> values across the real sidecar RPC boundary, independently of ordinary structured tool-result failures. Existing RPC code, message and data are retained; arbitrary Error properties are not serialized. The sidecar receiver exposes the code alongside its data. Successful images and per-item failures render even when only part of a batch completed. Missing configuration returns a structured error and a Settings → Models navigation action. The same references render after session reload/restart. Image results and setup actions remain visible outside the turn&#39;s collapsible process details; opening tool details does not duplicate the image gallery. Markdown image references to generated absolute paths, including Windows drive paths, use the existing bounded host image reader. URL sanitization stays enabled; the host still rejects files outside its permitted workspace/scratch/attachment roots.</p><p>Validation: <code>node scripts/e2e-image-generation.mjs</code> covers host/stdio/HTTP/storage; <code>node scripts/e2e-image-generation-ui.mjs</code> covers real React/Chromium interactions with an API-boundary fixture. Unit/service tests cover limits, cancellation, partial failures, authentication, timeout, unsafe paths, and bounded downloads. <code>node scripts/e2e-image-chat.mjs</code> exercises the full isolated desktop with local model/image HTTP fixtures: adjacent default settings, composer submission, batch previews, editing a generated file, collapsed results, and setup navigation. Live verification is opt-in via <code>scripts/test-image-generation-live.mjs</code>, limited to one generation plus one edit and never a default test command.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("spec/03-runtime/21-image-generation.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _21ImageGeneration = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _21ImageGeneration as default
};
