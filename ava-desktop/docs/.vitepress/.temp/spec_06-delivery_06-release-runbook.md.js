import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"06. Desktop Release Runbook","description":"","frontmatter":{},"headers":[],"relativePath":"spec/06-delivery/06-release-runbook.md","filePath":"spec/06-delivery/06-release-runbook.md","lastUpdated":1790060879000}');
const _sfc_main = { name: "spec/06-delivery/06-release-runbook.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="_06-desktop-release-runbook" tabindex="-1">06. Desktop Release Runbook <a class="header-anchor" href="#_06-desktop-release-runbook" aria-label="Permalink to &quot;06. Desktop Release Runbook&quot;">​</a></h1><blockquote><p>Scope: D126/D285/D603 tag artifacts for macOS arm64 and Intel x64, Windows x64, and Linux x64, including the Linux system-Electron ASAR asset; macOS signing/notarization remains the detailed qualification lane below. Cross-references: <a href="./01-mvp-milestones">milestones</a> · <a href="./../03-runtime/07-process-model">process model</a> · <a href="./../05-security/01-security">security</a></p></blockquote><h2 id="_1-build-lanes" tabindex="-1">1. Build lanes <a class="header-anchor" href="#_1-build-lanes" aria-label="Permalink to &quot;1. Build lanes&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Lane</th><th>Command</th><th>Signing</th><th>Use</th></tr></thead><tbody><tr><td>Dev</td><td><code>pnpm dev</code></td><td>none</td><td>daily development</td></tr><tr><td>Local package</td><td><code>pnpm --filter @pi-desktop/desktop pack</code></td><td>unsigned without a configured certificate</td><td>packaging smoke (<code>--dir</code> output)</td></tr><tr><td>Local DMG</td><td><code>pnpm --filter @pi-desktop/desktop dist</code></td><td>unsigned without a configured certificate</td><td>local install test</td></tr><tr><td>Release</td><td><code>scripts/release-macos.sh</code></td><td>Developer ID + mandatory notarization</td><td>distributable artifact</td></tr></tbody></table><p>The static electron-builder config does not embed a certificate identity, so contributors without certificates can still package locally. The release lane requires an injected Developer ID identity (local) or <code>CSC_LINK</code> certificate (CI), and fails before publication if signing or notarization verification does not pass.</p><p>On macOS, <code>pnpm dev</code> creates and reuses a fingerprinted branded Electron host bundle under <code>.cache/electron-dev/</code>. Its bundle name, executable, identifier, and ICNS resource are development-only PI-Desktop values, so AppKit shows PI-Desktop in the application menu and uses the canonical icon in the native About panel. The runtime also applies <code>build/icon_1024.png</code> to the Dock. Stock files under <code>node_modules</code> are never modified. Windows/Linux development keeps the normal electron-vite executable. Windows Main nevertheless registers the same <code>net.aiuo.pi-desktop</code> AppUserModelID used by the NSIS package before Electron readiness, preventing the stock host identity from owning native notifications or taskbar groups. The Windows package additionally pins the <code>PI-Desktop</code> executable and Start menu shortcut names. The launcher sets <code>PI_DESKTOP_DEV=1</code> so runtime packaging checks keep update delivery disabled and preserve developer workspace defaults despite the branded executable name. The first <code>pnpm dev</code> on Electron 43+ downloads the Electron binary on demand (the package no longer installs it during <code>pnpm install</code>). Packaged lanes use <code>build/icon.icns</code> through electron-builder on macOS and <code>build/icon.ico</code> for the Windows executable and native window icon. The renderer imports the same PNG through <code>BrandLogo</code>. The PNG is canonical; <code>scripts/make-icon.py</code> derives the multi-size Windows ICO, the 512px Windows/Linux package PNG, the transparent monochrome <code>build/tray-icon-mac.png</code> template, and the iconset/ICNS when macOS <code>iconutil</code> is available, without overwriting the canonical source.</p><h2 id="_2-prerequisites-release-lane" tabindex="-1">2. Prerequisites (release lane) <a class="header-anchor" href="#_2-prerequisites-release-lane" aria-label="Permalink to &quot;2. Prerequisites (release lane)&quot;">​</a></h2><ol><li>Apple Developer account with a <strong>Developer ID Application</strong> certificate in the login keychain. Official certificate: <code>Developer ID Application: XingYu Liu (DUV63RKYTW)</code> (Team ID <code>DUV63RKYTW</code>).</li><li>Environment variables for the local signed lane: <ul><li><code>MAC_SIGNING_IDENTITY</code> — bare common name <code>XingYu Liu (DUV63RKYTW)</code>; electron-builder rejects a name that keeps the <code>Developer ID Application:</code> prefix, so the script strips it</li><li><code>APPLE_ID</code>, <code>APPLE_APP_SPECIFIC_PASSWORD</code>, <code>APPLE_TEAM_ID</code> — required for notarization (<code>APPLE_TEAM_ID</code> must be <code>DUV63RKYTW</code>)</li></ul></li><li>Rust toolchain and pnpm workspace installed. The Rust toolchain must run on the native macOS runner: arm64 for Apple Silicon or x86_64 for Intel.</li></ol><h2 id="_3-what-the-build-ships" tabindex="-1">3. What the build ships <a class="header-anchor" href="#_3-what-the-build-ships" aria-label="Permalink to &quot;3. What the build ships&quot;">​</a></h2><ul><li>Electron app with hardened runtime + entitlements (<code>build/entitlements.mac.plist</code>: V8 JIT, unsigned-executable-memory, library-validation disable for Electron helpers and plugin native addons, and microphone input for plugin capture after user grant — ADR 0257), plus <code>NSLocalNetworkUsageDescription</code> and <code>NSMicrophoneUsageDescription</code> in Info.plist (via <code>apps/desktop/package.json</code> → <code>mac.extendInfo</code>). The local network string is required so macOS 15+ prompts for Local Network access and grants it to both the Chromium main process and the <code>ELECTRON_RUN_AS_NODE</code> agent sidecar — without it, LAN provider requests from the sidecar fail with <code>EHOSTUNREACH</code> even though the main process&#39;s Test Provider fetch succeeds (issue #573).</li><li><code>Resources/bin/pi-desktop-host-core</code> — Rust host binary (release build).</li><li>Windows NSIS builds include an x64 <code>pi-desktop-host-core.exe</code> statically linked to the MSVC CRT, so a clean Windows x64 or Windows 11 ARM64 (x64-emulated) installation does not need a separate Visual C++ Redistributable before the local service can start.</li><li><code>Resources/agent-runtime/</code> — bundled sidecar, executed with <code>ELECTRON_RUN_AS_NODE=1</code> (no separate Node shipped).</li><li><code>Resources/licenses/</code> — notices that must remain distributable when the corresponding dependency&#39;s build-only source tree is pruned.</li><li><code>Resources/app.asar</code> — Electron Main, preload, renderer output, and only the runtime-resolved production modules. Renderer libraries are already present in Vite output and are not copied again as raw package trees.</li><li>Chromium locale packs for English, Simplified Chinese, Traditional Chinese, Turkish, German, Spanish, French, and Korean. Product catalogs remain bundled independently of Chromium locales.</li><li>App icons <code>build/icon.icns</code> and <code>build/icon.ico</code> (derived from canonical <code>build/icon_1024.png</code> by <code>scripts/make-icon.py</code>).</li><li>macOS menu bar template <code>build/tray-icon-mac.png</code>, derived from the dark PI mark with a transparent background; Windows/Linux use the product PNG tray resource.</li></ul><h2 id="_4-release-steps" tabindex="-1">4. Release steps <a class="header-anchor" href="#_4-release-steps" aria-label="Permalink to &quot;4. Release steps&quot;">​</a></h2><h3 id="_4-1-mandatory-release-version-surface-gate-d164-d260" tabindex="-1">4.1 Mandatory release version-surface gate (D164 + D260) <a class="header-anchor" href="#_4-1-mandatory-release-version-surface-gate-d164-d260" aria-label="Permalink to &quot;4.1 Mandatory release version-surface gate (D164 + D260)&quot;">​</a></h3><p><strong>Every product release that bumps a stable app version and cuts a tag MUST first update every version-bearing surface: the shipped-locale in-app product changelog and the version numbers stated in project documentation.</strong> Tagging a stable version while any surface still describes an older version is a <strong>release process failure</strong>: packaged builds cannot show &quot;what&#39;s new&quot; without a network fetch, the READMEs advertise a stale release line, and GitHub auto-generated release bodies are <strong>not</strong> a substitute (extends D120 / ADR 0022).</p><p>Surfaces in scope:</p><table tabindex="0"><thead><tr><th>Surface</th><th>Requirement</th></tr></thead><tbody><tr><td><code>apps/desktop/resources/models.dev/api.json</code></td><td>Refreshed from <a href="https://models.dev/api.json" target="_blank" rel="noreferrer">https://models.dev/api.json</a> before tagging; the release workflow packages this snapshot unchanged</td></tr><tr><td><code>packages/shared/src/changelog.ts</code></td><td>Newest-first English, zh-CN, and zh-TW entries, matching highlight counts</td></tr><tr><td><code>packages/shared/src/changelog-de.ts</code>, <code>changelog-es.ts</code>, <code>changelog-fr.ts</code>, <code>changelog-ko.ts</code>, <code>changelog-tr.ts</code></td><td>Same versions and highlight counts as English</td></tr><tr><td><code>packages/shared/src/changelog.test.ts</code></td><td>Version added at the top of the newest-first list</td></tr><tr><td><code>package.json</code>, <code>apps/*/package.json</code>, <code>packages/*/package.json</code>, <code>docs/package.json</code></td><td>Same version (<code>docs</code> is a third workspace root, not under <code>apps</code>/<code>packages</code>)</td></tr><tr><td><code>Cargo.toml</code> <code>[workspace.package]</code>, <code>Cargo.lock</code> <code>host-core</code></td><td>Same version</td></tr><tr><td><code>packages/shared/src/protocol.ts</code> <code>APP_VERSION</code></td><td>Same version</td></tr><tr><td><code>README.md</code>, <code>README.zh-CN.md</code></td><td>Status section states the current <code>&lt;major&gt;.&lt;minor&gt;.x</code> release line; toolchain, command, and roadmap claims still true</td></tr></tbody></table><p>Blocking steps:</p><ol><li>Refresh <code>apps/desktop/resources/models.dev/api.json</code> <strong>before</strong> tagging. <code>scripts/release.mjs</code> does this by default for every bump, including prereleases. A no-op refresh (already current) still counts: the snapshot in the tagged tree is what artifacts ship. Do not treat a minified one-line JSON diff as absent.</li><li>Edit <code>packages/shared/src/changelog.ts</code> <strong>before</strong><code>node scripts/release.mjs &lt;version&gt;</code> / <code>git tag</code>: <ul><li>Add a <strong>newest-first</strong> entry under <code>en</code> and every shipped product locale (<code>zh-CN</code> / <code>zh-TW</code> in this file; <code>de</code> / <code>es</code> / <code>fr</code> / <code>ko</code> / <code>tr</code> in <code>packages/shared/src/changelog-*.ts</code>).</li><li>Same <code>version</code> string (semver <strong>without</strong> a leading <code>v</code>, matching <code>apps/desktop</code> / <code>APP_VERSION</code> for a stable cut).</li><li>Optional ISO <code>date</code> (<code>YYYY-MM-DD</code>).</li><li>Matching highlight counts; English is the source of truth (ADR 0009).</li><li>Each bullet is one short user-facing idea (not raw PR titles).</li></ul></li><li>Do <strong>not</strong> catalog the prerelease identifier (<code>x.y.z-rc.*</code>, <code>x.y.z-beta.*</code>) as its own in-app changelog version. When the prerelease is a preview of the next stable line, add that <strong>stable</strong> version&#39;s entries (<code>x.y.z</code>) so testers can read &quot;what&#39;s new&quot; without a network fetch. Omit the catalog only when product explicitly ships no notes for this cut.</li><li>Sync the newest-first version list in <code>packages/shared/src/changelog.test.ts</code> (add the new stable version at the top), then run <code>pnpm --filter @pi-desktop/shared test</code> and confirm catalog alignment (version sets + highlight counts) still passes.</li><li>Update <code>README.md</code> and <code>README.zh-CN.md</code> when the release line changes (<code>0.10.x</code> → <code>0.11.x</code>) and whenever the release ships user-visible behavior the Highlights, Download, Getting started, Status, or Development sections now describe incorrectly. Both locales stay structurally in sync; English is the source of truth and the zh-CN file links the <code>docs/zh-CN/</code> mirrors.</li><li>Run the preflight and fix every reported surface: <code>pnpm check:release-docs [version]</code> (<code>node scripts/check-release-docs.mjs</code>). For a prerelease, run it against the <strong>stable</strong> version being previewed (<code>pnpm check:release-docs x.y.z</code>) so changelog/README alignment is checked even though <code>scripts/release.mjs</code> skips that preflight for <code>x.y.z-beta.*</code> / <code>x.y.z-rc.*</code>. The preflight compiles the TypeScript changelog in a temporary directory, so it does not require a prior workspace build. <code>scripts/release.mjs</code> still refreshes models.dev for prereleases; <code>--skip-docs-check</code> exists only for a deliberate non-release bump.</li><li>Commit the documentation updates so the tagged commit contains notes and accurate version claims for that version (alone or adjacent to the bump).</li><li>GitHub Release bodies may still use <code>generate_release_notes: true</code> for the web page; they remain web-only and are <strong>not</strong> the in-app notes source.</li></ol><p>Pre-tag checklist:</p><ul><li>[ ] <code>apps/desktop/resources/models.dev/api.json</code> is refreshed or confirmed current in the tagged tree</li><li>[ ] <code>packages/shared/src/changelog.ts</code> has English / zh-CN / zh-TW entries for the stable version being shipped or previewed</li><li>[ ] <code>packages/shared/src/changelog-de.ts</code> and the other locale catalogs match the English version set and highlight counts</li><li>[ ] Highlight counts match across locales</li><li>[ ] Shared changelog tests pass</li><li>[ ] <code>README.md</code> and <code>README.zh-CN.md</code> state the current release line and contain no claims the release invalidates</li><li>[ ] <code>node scripts/check-release-docs.mjs</code> passes on the release commit (use the stable version when tagging a prerelease preview)</li><li>[ ] <code>release.mjs</code> / tag runs only after the documentation commit is on the release branch</li></ul><h3 id="_4-2-build-package" tabindex="-1">4.2 Build / package <a class="header-anchor" href="#_4-2-build-package" aria-label="Permalink to &quot;4.2 Build / package&quot;">​</a></h3><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">export</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> MAC_SIGNING_IDENTITY</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;XingYu Liu (DUV63RKYTW)&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">export</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> APPLE_ID</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">...</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">export</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> APPLE_APP_SPECIFIC_PASSWORD</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">...</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">export</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> APPLE_TEAM_ID</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">...</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">scripts/release-macos.sh</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>Artifacts land in <code>apps/desktop/release/</code> (DMG + ZIP + blockmaps).</p><p><code>scripts/release-macos.sh</code> defaults to the host architecture and accepts <code>MAC_ARCH=arm64</code> or <code>MAC_ARCH=x64</code> only when that architecture matches the host. This keeps the native Rust host sidecar and Electron package aligned.</p><h3 id="_4-3-github-tag-and-manual-workflow" tabindex="-1">4.3 GitHub tag and manual workflow <a class="header-anchor" href="#_4-3-github-tag-and-manual-workflow" aria-label="Permalink to &quot;4.3 GitHub tag and manual workflow&quot;">​</a></h3><p>The GitHub Release workflow starts all native platform runners without a separate validation-job barrier. Each runner validates that the pushed tag matches <code>apps/desktop/package.json</code> immediately after checkout, before package inputs are prepared.</p><p>On every platform, the release preparation step starts the locked Rust host build in parallel with pnpm installation and native dependency rebuilding. It then builds only the workspace dependencies selected by <code>@pi-desktop/desktop^...</code>, failing if that dependency selection is unexpectedly empty. The platform <code>dist:*</code> command remains responsible for bundling the agent runtime, verifying the host build, building the Desktop application once, and invoking electron-builder. This avoids a redundant Desktop build without changing the package scripts or release artifacts.</p><p><strong>Default macOS release policy:</strong> GitHub tag releases Developer ID-sign, notarize, staple, and Gatekeeper-verify macOS DMG/ZIP before upload (D450 / ADR 0289). Missing signing or notarization secrets fail the job. A <code>workflow_dispatch</code> run may set <code>sign_macos: false</code> only to produce unsigned debug artifacts; that path must not be used for a GitHub Release tag. Local <code>scripts/release-macos.sh</code> remains the explicit signed local lane; <code>pnpm dist:mac</code> stays unsigned without a configured certificate (D078).</p><p>The macOS matrix uses <code>macos-15</code> for arm64 and <code>macos-15-intel</code> for Intel x64. Each job verifies <code>uname -m</code>, passes the matching <code>--arm64</code> or <code>--x64</code> flag to electron-builder, and builds <code>pi-desktop-host-core</code> on that same native runner. Tag builds and <code>sign_macos: true</code> (the dispatch default) receive <code>CSC_LINK</code>, <code>CSC_KEY_PASSWORD</code>, <code>APPLE_ID</code>, <code>APPLE_APP_SPECIFIC_PASSWORD</code>, and <code>APPLE_TEAM_ID</code> only from GitHub Actions secrets, pin the certificate through <code>CSC_NAME=XingYu Liu (DUV63RKYTW)</code> (bare common name — electron-builder rejects the <code>Developer ID Application:</code> prefix), force code signing and <code>notarytool</code> notarization of <code>PI-Desktop.app</code>. The DMG is then submitted to the same service on its own (<code>scripts/notarize-and-staple-macos-release-dmg.sh</code>), and only an <code>Accepted</code> status allows the ticket to be stapled. Verification then checks the identity, code-signing integrity (including <code>pi-desktop-host-core</code>), Gatekeeper <code>Notarized Developer ID</code>, and both stapled tickets before any artifact upload. The per-architecture <code>latest-mac.yml</code> files are renamed before upload; the publish job merges them into one feed after downloading both artifacts.</p><p>The shared electron-builder configuration applies the architecture-labelled pattern at the macOS platform level for ZIPs and overrides it at the DMG target level. Both public architectures are therefore explicit: the arm64 lane publishes <code>PI-Desktop-&lt;version&gt;-arm64.dmg</code> and <code>PI-Desktop-&lt;version&gt;-arm64-mac.zip</code>, while the Intel x64 lane publishes <code>PI-Desktop-&lt;version&gt;-x64.dmg</code> and <code>PI-Desktop-&lt;version&gt;-x64-mac.zip</code>. This applies to both unsigned and signed macOS lanes, including local release builds, and ensures each generated updater feed references its architecture-labelled asset names and matching checksums. Before upload, each macOS runner requires exactly one architecture-labelled DMG and ZIP (including blockmaps) and rejects any unlabelled or wrong-architecture macOS artifact.</p><p>The DMG uses a branded 720×440 background with a two-icon drag-to-Applications gesture. The app and Applications link are the only items in the window. The opening-help note and the executable command helper are not included in the DMG.</p><p>The macOS ZIP includes both <code>PI-Desktop-macOS-opening-help.txt</code> and the executable <code>PI-Desktop-macOS-open.command</code> at the package root. After moving <code>PI-Desktop.app</code> to <code>/Applications</code> or <code>~/Applications</code>, ZIP users can double-click the helper. It searches only those two fixed locations, removes only the recursive <code>com.apple.quarantine</code> attribute when present, and opens PI-Desktop. Before doing so it verifies <code>CFBundleIdentifier=net.aiuo.pi-desktop</code>. It does not use <code>sudo</code> or accept an arbitrary application path. The manual fallback for the standard system location is:</p><div class="language-sh vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">xattr</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -r</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -d</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> com.apple.quarantine</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> /Applications/PI-Desktop.app</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>This helper is only for a trusted unsigned artifact when macOS reports that the app is damaged. Signed and notarized builds should open without it.</p><p>DMG, ZIP, NSIS, AppImage, deb, rpm, blockmap, and updater feed outputs are already compressed or compression-insensitive. The workflow therefore uploads their temporary Actions artifacts with compression level zero before the publish job assembles the GitHub Release. The Linux runner also copies <code>linux-unpacked/resources/app.asar</code> to the versioned <code>PI-Desktop-&lt;version&gt;-linux-x64.asar</code> asset before upload. This preserves the exact archive used by the Linux installers for downstream repackaging with a system Electron.</p><h3 id="_4-4-documentation-site-deployment" tabindex="-1">4.4 Documentation site deployment <a class="header-anchor" href="#_4-4-documentation-site-deployment" aria-label="Permalink to &quot;4.4 Documentation site deployment&quot;">​</a></h3><p>The <code>pi-desktop-docs</code> Vercel project is published only from a published GitHub Release ref. <code>docs/vercel.json</code> disables Vercel Git auto-deployments, so pushes to <code>main</code>, release branches, and pull requests do not create documentation deployments or Vercel bot comments. The <code>docs-deploy</code> job in <code>.github/workflows/release.yml</code> runs after the release workflow&#39;s <code>publish</code> job succeeds, checks out the exact release tag, and deploys the production site with the Vercel CLI. Keeping this job in the same workflow avoids relying on a second workflow being triggered by <code>GITHUB_TOKEN</code>.</p><p>The workflow requires the repository secrets <code>VERCEL_ORG_ID</code>, <code>VERCEL_PROJECT_ID</code>, and <code>VERCEL_TOKEN</code>.</p><h3 id="_4-5-cnb-mirror-trigger" tabindex="-1">4.5 CNB mirror trigger <a class="header-anchor" href="#_4-5-cnb-mirror-trigger" aria-label="Permalink to &quot;4.5 CNB mirror trigger&quot;">​</a></h3><p>After <code>softprops/action-gh-release</code> publishes or updates a GitHub Release, <code>.github/workflows/mirror-to-cnb.yml</code> starts the CNB pipeline at <code>aixk/Pi-Desktop</code>. GitHub Release remains the canonical artifact source; CNB is a copy of the same tag for users who pull from <a href="https://cnb.cool/aixk/Pi-Desktop" target="_blank" rel="noreferrer">https://cnb.cool/aixk/Pi-Desktop</a>.</p><p>The job:</p><ul><li>runs only on <code>vastsa/PI-Desktop</code></li><li>fires on <code>release</code> <code>published</code> / <code>edited</code>, and on <code>workflow_dispatch</code> with an explicit tag such as <code>v0.14.6</code></li><li>sends event <code>api_trigger_mirror</code> and <code>MIRROR_TAGS</code> set to that tag</li><li>uses repository secret <code>CNB_MIRROR_TOKEN</code> (already configured) and fails closed if the secret is empty</li><li>builds the JSON body with <code>jq</code> so a missing tag cannot produce an empty <code>MIRROR_TAGS</code> value on a manual run</li></ul><p>Re-running the workflow for the same tag is safe if the CNB pipeline is idempotent. It does not rebuild desktop artifacts and does not change electron-updater feeds.</p><h3 id="_4-6-github-actions-secrets-for-macos-signing" tabindex="-1">4.6 GitHub Actions secrets for macOS signing <a class="header-anchor" href="#_4-6-github-actions-secrets-for-macos-signing" aria-label="Permalink to &quot;4.6 GitHub Actions secrets for macOS signing&quot;">​</a></h3><p>Create these under GitHub → repository <code>vastsa/PI-Desktop</code> → Settings → Secrets and variables → Actions. Never commit the p12, password, Apple ID, or app-specific password. Never <code>echo</code> these values in CI.</p><table tabindex="0"><thead><tr><th>Secret</th><th>Value</th></tr></thead><tbody><tr><td><code>CSC_LINK</code></td><td>Base64 of the exported Developer ID Application <code>.p12</code> (Certificate + Private Key). electron-builder also accepts a file path, but CI uses the secret body.</td></tr><tr><td><code>CSC_KEY_PASSWORD</code></td><td>Password used when exporting that <code>.p12</code></td></tr><tr><td><code>APPLE_ID</code></td><td>Apple ID email that belongs to team <code>DUV63RKYTW</code></td></tr><tr><td><code>APPLE_APP_SPECIFIC_PASSWORD</code></td><td>App-specific password from <a href="https://appleid.apple.com" target="_blank" rel="noreferrer">https://appleid.apple.com</a> → Sign-In and Security → App-Specific Passwords</td></tr><tr><td><code>APPLE_TEAM_ID</code></td><td><code>DUV63RKYTW</code></td></tr></tbody></table><p>Encode the p12 locally (do not paste the output into chat or the repo):</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">base64</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -i</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> developer-id-application.p12</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> pbcopy</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>On Linux use <code>base64 -w0 developer-id-application.p12</code>. Files that must never enter git: <code>*.p12</code>, <code>*.cer</code>, <code>*.p8</code>, <code>*.mobileprovision</code>.</p><h3 id="_4-7-macos-signing-observability-and-timeouts" tabindex="-1">4.7 macOS signing observability and timeouts <a class="header-anchor" href="#_4-7-macos-signing-observability-and-timeouts" aria-label="Permalink to &quot;4.7 macOS signing observability and timeouts&quot;">​</a></h3><p><code>electron-builder</code> prints one line before signing — <code>signing file=release/mac-arm64/PI-Desktop.app platform=darwin type=distribution identityName=...</code> — and then nothing until the phase is over. Three mechanisms hide in that gap, and the macOS lanes now expose all three:</p><table tabindex="0"><thead><tr><th>Point in the phase</th><th>What happens</th><th>How it is visible</th></tr></thead><tbody><tr><td>Walk</td><td><code>@electron/osx-sign</code> walks <code>PI-Desktop.app/Contents</code> and collects every Mach-O file plus nested <code>.app</code> and <code>.framework</code> bundles</td><td><code>DEBUG=electron-osx-sign*</code> prints <code>Walking... &lt;dir&gt;</code>; <code>scripts/macos-bundle-inventory.mjs</code> prints the same bundle&#39;s counts right after packaging</td></tr><tr><td>Per-file signing</td><td><code>codesign --force --sign &lt;identity&gt; --timestamp --entitlements ... &lt;file&gt;</code> runs serially, deepest file first, the app bundle last</td><td><code>DEBUG=electron-osx-sign*</code> prints <code>Signing... &lt;file&gt;</code> and <code>Executing... &lt;file&gt; codesign ...</code>; the codesign shim times every invocation. If a keychain ever refuses to hand the key to a wrapped <code>codesign</code>, <code>PI_SIGNING_NO_CODESIGN_SHIM=1</code> runs the phase without the shim</td></tr><tr><td>Silent retry</td><td>A failing pass is retried up to three more times with a 5s/10s/15s backoff and no log line</td><td>The watchdog&#39;s <code>codesign-calls</code> and <code>failures</code> lines expose repeated passes</td></tr><tr><td>App notarization</td><td><code>@electron/notarize</code> zips the app, uploads it, and waits for Apple&#39;s queue (<code>mac.notarize=true</code>)</td><td><code>DEBUG=electron-notarize*</code> prints <code>zipping application to</code>, <code>attempting to upload file to Apple</code>, <code>notarization success</code>, then electron-builder prints <code>notarization successful</code></td></tr><tr><td>DMG notarization</td><td>The DMG carries its own signature, so the next step submits it again with <code>xcrun notarytool submit --wait</code></td><td>The same watchdog keeps that wait observable and bounded</td></tr></tbody></table><p><code>scripts/macos-signing-watchdog.mjs</code> wraps both long phases. It forwards every child line with a <code>[sign] </code> prefix and keeps the child&#39;s exit code, so the failure semantics of the lane do not change. stdout and stderr are forwarded as two independent streams, so their relative order can differ from a direct run, and the child receives no stdin. It prints a heartbeat while the child is silent (elapsed time, phase, last file, active codesign target), dumps diagnostics when the phase produces no output and no codesign activity for <code>PI_SIGNING_STALL_SECONDS</code> (the last file, the <code>ps</code> state of the signing processes, the codesign log tail), and reports per-file codesign timings — call count, total, p50, p95, maximum, and the slowest files — in a <code>[sign] summary</code> block. The knobs:</p><table tabindex="0"><thead><tr><th>Setting</th><th>Default</th><th>Effect</th></tr></thead><tbody><tr><td><code>PI_SIGNING_TIMEOUT_SECONDS</code></td><td>2400 (CI: 1800 packaging, 1200 DMG)</td><td>Hard limit for the wrapped phase: diagnostics are dumped, the process group is killed, and the step exits 124 instead of hanging</td></tr><tr><td><code>PI_SIGNING_STALL_SECONDS</code></td><td>300</td><td>Silence with no codesign activity for this long triggers one diagnostics dump; the phase keeps running, because Apple&#39;s notarization queue is a legitimate wait</td></tr><tr><td><code>PI_SIGNING_HEARTBEAT_SECONDS</code></td><td>60</td><td>Heartbeat interval while the child produces no output</td></tr><tr><td><code>DEBUG</code></td><td><code>electron-osx-sign*,electron-notarize*</code></td><td>Namespaces that expose walking, per-file signing, and notarization progress</td></tr></tbody></table><p><code>DEBUG</code> lists only the two namespaces that sanitize their own command lines, because <code>electron-builder</code>&#39;s namespace is not safe here: builder-util prints every spawned command through a stem list that does not cover <code>security set-key-partition-list -k &lt;p12 password&gt;</code>. On top of that the watchdog redacts the values of <code>CSC_KEY_PASSWORD</code> and <code>APPLE_APP_SPECIFIC_PASSWORD</code> at any length, the values of <code>CSC_LINK</code> and <code>APPLE_ID</code>, and any <code>--password</code> or <code>-k</code> argument, from everything it emits — including the diagnostics, whose process view prints <code>comm</code> only and never <code>argv</code>, and the summary, which carries counts and redacted target paths. GitHub additionally masks every value that comes from a secret.</p><p>Measured on the maintainer machine, one macOS arm64 bundle needs 93 <code>codesign</code> invocations (91 signing, 1 verification, 1 entitlement display) and about 49s of <code>codesign</code> wall time; only 16 of those files are Mach-O code and 5 are nested bundles. <code>@electron/osx-sign</code> also signs binary resources — 33 <code>.pak</code> files plus <code>.nib</code>, <code>.dat</code>, <code>.bin</code>, <code>.png</code>, <code>.icns</code>, and <code>app.asar</code> — because its walk selects every file that looks binary, not only Mach-O. Excluding exactly those data files with <code>mac.signIgnore</code> would remove roughly three quarters of the calls, but it changes what the release artifacts carry and needs a notarized release to validate, so it is deliberately not enabled.</p><p><code>scripts/macos-signing-diagnostics.sh</code> records the runner baseline before the certificate is imported: system version, <code>codesign --version</code>, keychain identities/list/default, <code>xcrun --find notarytool</code>, and the reachability and latency of <code>http://timestamp.apple.com/ts01</code>. The Developer ID identity is expected to be absent at that point, because electron-builder imports it from <code>CSC_LINK</code> while packaging; only <code>--require-identity</code> makes a missing identity fatal.</p><p>Signer status: <code>@electron/osx-sign@1.3.3</code> is pinned exactly by <code>app-builder-lib@26.15.3</code> and no override applies to it. Its <code>signApplication()</code> awaits one <code>codesign</code> per file; it has no batch or parallel path and no option or environment variable that enables one. A faster signer therefore needs the <code>mac.sign</code> replacement hook, which is a rewrite rather than a configuration switch, so the lane keeps the pinned signer and the diagnostics above.</p><h2 id="_5-verification-gates" tabindex="-1">5. Verification gates <a class="header-anchor" href="#_5-verification-gates" aria-label="Permalink to &quot;5. Verification gates&quot;">​</a></h2><p>Unsigned debug artifacts (<code>workflow_dispatch</code> with <code>sign_macos: false</code>) are not Gatekeeper-qualified. Tag releases must pass the signature, notarization, and staple checks below or the workflow fails.</p><p>Two separate notarization submissions exist, because Apple notarizes one artifact per submission and electron-builder only covers the app:</p><table tabindex="0"><thead><tr><th>Artifact</th><th>Submitted by</th><th>Ticket</th></tr></thead><tbody><tr><td><code>PI-Desktop.app</code> (inside the ZIP)</td><td>electron-builder <code>-c.mac.notarize=true</code></td><td>stapled by electron-builder</td></tr><tr><td><code>PI-Desktop-&lt;version&gt;-&lt;arch&gt;.dmg</code></td><td><code>scripts/notarize-and-staple-macos-release-dmg.sh</code> (<code>notarytool submit --wait</code>)</td><td>stapled by the same script after <code>status: Accepted</code></td></tr></tbody></table><p>A DMG that was never submitted has no ticket, so stapling it fails with <code>Could not find base64 encoded ticket ... Error 65</code>. Stapler retries are only allowed after Apple returns <code>Accepted</code>.</p><p>Run after every signed release build:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">for</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> APP </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">in</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> apps/desktop/release/mac-*/PI-Desktop.app</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">do</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">  codesign</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -dv</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --verbose=4</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">$APP</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">          # identity + hardened runtime flags</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">  codesign</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --verify</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --deep</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --strict</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --verbose=2</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">$APP</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">  spctl</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --assess</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --type</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> execute</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --verbose=4</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">$APP</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">  xcrun</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> stapler</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> validate</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">$APP</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">done</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">xcrun</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> stapler</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> validate</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> apps/desktop/release/</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">*</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">.dmg</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p>To read the Apple notarization log for a submission (the Release workflow does this automatically when a submission is not accepted):</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">xcrun</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> notarytool</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> log</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">submission-i</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">d</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> \\</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  --apple-id</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">$APPLE_ID</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> \\</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  --password</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">$APPLE_APP_SPECIFIC_PASSWORD</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> \\</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  --team-id</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">$APPLE_TEAM_ID</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><h3 id="_5-1-package-footprint-gate" tabindex="-1">5.1 Package footprint gate <a class="header-anchor" href="#_5-1-package-footprint-gate" aria-label="Permalink to &quot;5.1 Package footprint gate&quot;">​</a></h3><p>Inspect every native-runner package before publication and record all compressed artifact formats, unpacked application, ASAR, Electron framework/runtime, locale, and unpacked-native sizes. Compare them with the previous stable release; an unexplained increase above 15% blocks publication until reviewed.</p><p>The package inventory must confirm:</p><ul><li>exactly one <code>Resources/agent-runtime/sidecar.js</code> and one target-native Rust host binary</li><li>no raw renderer packages such as Mermaid, Shiki, React, KaTeX, or Lucide under packaged <code>node_modules</code></li><li>no dependency <code>*.map</code>, test, example, declaration, or second agent-runtime tree in ASAR</li><li>required third-party license and notice files remain in ASAR or <code>Resources/licenses</code> when their non-runtime package trees are pruned</li><li>only the configured English, Simplified Chinese, Traditional Chinese, Turkish, German, Spanish, French, and Korean Chromium locale packs</li></ul><p>The first audited optimized package establishes the platform baseline. Keep per-platform measurements rather than applying one budget to different Electron target layouts.</p><p>The first macOS arm64 baseline was captured on 2026-07-30 from an unsigned <code>electron-builder --dir</code> package. Sizes below sum regular-file bytes so they remain comparable across filesystems; the compressed artifact is not applicable to this directory-only validation build.</p><table tabindex="0"><thead><tr><th>Inventory</th><th style="${ssrRenderStyle({ "text-align": "right" })}">Bytes</th><th style="${ssrRenderStyle({ "text-align": "right" })}">MiB</th></tr></thead><tbody><tr><td>Unpacked application</td><td style="${ssrRenderStyle({ "text-align": "right" })}">251,724,810</td><td style="${ssrRenderStyle({ "text-align": "right" })}">240.1</td></tr><tr><td><code>Contents/Frameworks</code></td><td style="${ssrRenderStyle({ "text-align": "right" })}">218,567,792</td><td style="${ssrRenderStyle({ "text-align": "right" })}">208.4</td></tr><tr><td><code>Contents/Resources</code></td><td style="${ssrRenderStyle({ "text-align": "right" })}">33,102,807</td><td style="${ssrRenderStyle({ "text-align": "right" })}">31.6</td></tr><tr><td><code>Resources/app.asar</code></td><td style="${ssrRenderStyle({ "text-align": "right" })}">20,944,962</td><td style="${ssrRenderStyle({ "text-align": "right" })}">20.0</td></tr><tr><td><code>Resources/app.asar.unpacked</code> native payload</td><td style="${ssrRenderStyle({ "text-align": "right" })}">137,336</td><td style="${ssrRenderStyle({ "text-align": "right" })}">0.1</td></tr><tr><td>Historical English and Simplified Chinese Chromium locale packs baseline</td><td style="${ssrRenderStyle({ "text-align": "right" })}">1,033,673</td><td style="${ssrRenderStyle({ "text-align": "right" })}">1.0</td></tr><tr><td>Agent sidecar</td><td style="${ssrRenderStyle({ "text-align": "right" })}">3,258,983</td><td style="${ssrRenderStyle({ "text-align": "right" })}">3.1</td></tr><tr><td>Rust host</td><td style="${ssrRenderStyle({ "text-align": "right" })}">7,160,000</td><td style="${ssrRenderStyle({ "text-align": "right" })}">6.8</td></tr></tbody></table><p>The pre-optimization unpacked regular-file total was 559,355,716 bytes (533.4 MiB). The audited package is 307,630,906 bytes smaller, a 55.0% reduction. Its curated renderer output is 14.1 MiB, down from 20.5 MiB.</p><h4 id="renderer-output-budget" tabindex="-1">Renderer output budget <a class="header-anchor" href="#renderer-output-budget" aria-label="Permalink to &quot;Renderer output budget&quot;">​</a></h4><p>The renderer bundle carries three standing controls. Regressing any of them shows up as renderer growth in the table above and must be explained before publication:</p><ul><li><strong>Minification is explicit.</strong> <code>electron-vite</code> hard-defaults the renderer preset to <code>minify: false</code>, unlike plain Vite, so <code>apps/desktop/electron.vite.config.ts</code> sets <code>minify: &quot;esbuild&quot;</code>. Removing it silently doubles emitted JS.</li><li><strong>Legacy font formats are stripped.</strong> The <code>pi-drop-legacy-font-fallbacks</code> plugin removes <code>woff</code> and <code>truetype</code> <code>src</code> entries before Vite registers them as assets. The bundled Chromium supports <code>woff2</code> universally, so those faces would be emitted and never served.</li><li><strong>Brand marks are renderer-sized.</strong> <code>src/assets/brand/logo-{light,dark}.png</code> are the renderer assets. <code>build/icon_1024.png</code> and <code>build/logo_dark.png</code> are electron-builder installer icons and must not be imported by the renderer.</li></ul><p>Renderer output measured on 2026-08-26 after applying these three controls, against the same tree at <code>v0.10.8</code>:</p><table tabindex="0"><thead><tr><th>Renderer group</th><th style="${ssrRenderStyle({ "text-align": "right" })}">Before</th><th style="${ssrRenderStyle({ "text-align": "right" })}">After</th></tr></thead><tbody><tr><td>JavaScript (120 chunks)</td><td style="${ssrRenderStyle({ "text-align": "right" })}">12.53 MiB</td><td style="${ssrRenderStyle({ "text-align": "right" })}">7.72 MiB</td></tr><tr><td><code>woff2</code></td><td style="${ssrRenderStyle({ "text-align": "right" })}">15.71 MiB</td><td style="${ssrRenderStyle({ "text-align": "right" })}">15.71 MiB</td></tr><tr><td><code>woff</code> + <code>ttf</code> legacy fallbacks (40 files)</td><td style="${ssrRenderStyle({ "text-align": "right" })}">0.78 MiB</td><td style="${ssrRenderStyle({ "text-align": "right" })}">0</td></tr><tr><td>PNG brand assets</td><td style="${ssrRenderStyle({ "text-align": "right" })}">1.23 MiB</td><td style="${ssrRenderStyle({ "text-align": "right" })}">0.27 MiB</td></tr><tr><td>CSS</td><td style="${ssrRenderStyle({ "text-align": "right" })}">0.42 MiB</td><td style="${ssrRenderStyle({ "text-align": "right" })}">0.35 MiB</td></tr><tr><td><strong>Total <code>out/renderer</code></strong></td><td style="${ssrRenderStyle({ "text-align": "right" })}"><strong>31 MiB</strong></td><td style="${ssrRenderStyle({ "text-align": "right" })}"><strong>24 MiB</strong></td></tr></tbody></table><p>The renderer no longer emits any application font face. D598 / ADR 0298 removed the four bundled families (Geist, Inter, Noto Sans SC, LXGW WenKai), so the only <code>woff2</code> files left in <code>out/renderer</code> are KaTeX&#39;s math glyphs. Measured on this machine from a clean <code>pnpm install --frozen-lockfile</code>, with and without the removal:</p><table tabindex="0"><thead><tr><th>Renderer group</th><th style="${ssrRenderStyle({ "text-align": "right" })}">Bundled fonts</th><th style="${ssrRenderStyle({ "text-align": "right" })}">After D598</th></tr></thead><tbody><tr><td>JavaScript (121 chunks)</td><td style="${ssrRenderStyle({ "text-align": "right" })}">9.04 MiB</td><td style="${ssrRenderStyle({ "text-align": "right" })}">9.04 MiB</td></tr><tr><td><code>woff2</code> (23 → 19 files)</td><td style="${ssrRenderStyle({ "text-align": "right" })}">15.71 MiB</td><td style="${ssrRenderStyle({ "text-align": "right" })}">0.24 MiB</td></tr><tr><td>CSS (1 file)</td><td style="${ssrRenderStyle({ "text-align": "right" })}">0.48 MiB</td><td style="${ssrRenderStyle({ "text-align": "right" })}">0.48 MiB</td></tr><tr><td>PNG brand assets (4 files)</td><td style="${ssrRenderStyle({ "text-align": "right" })}">0.08 MiB</td><td style="${ssrRenderStyle({ "text-align": "right" })}">0.08 MiB</td></tr><tr><td>GIF (2 files)</td><td style="${ssrRenderStyle({ "text-align": "right" })}">0.05 MiB</td><td style="${ssrRenderStyle({ "text-align": "right" })}">0.05 MiB</td></tr><tr><td><strong>Total <code>out/renderer</code></strong> (152 → 148 files)</td><td style="${ssrRenderStyle({ "text-align": "right" })}"><strong>25.36 MiB</strong></td><td style="${ssrRenderStyle({ "text-align": "right" })}"><strong>9.89 MiB</strong></td></tr></tbody></table><p>The entire difference is the four deleted faces, at the sizes the build reports: <code>lxgw-wenkai.woff2</code> 8,016.75 kB, <code>noto-sans-sc.woff2</code> 7,782.07 kB, <code>inter.woff2</code> 352.24 kB, and <code>geist.woff2</code> 69.65 kB — 16,220.71 kB, which is the whole 15.47 MiB drop in the total. Chinese text now renders from the system tier (<code>PingFang SC</code>, <code>Hiragino Sans GB</code>, <code>Microsoft YaHei</code>), so no glyph coverage is lost to subsetting: no face ships. The other two controls are unchanged: minification stays explicit, and the legacy <code>woff</code>/<code>truetype</code> strip stays because KaTeX still declares those sources.</p><p>The three-controls table above is the <code>v0.10.8</code> record and predates the removal, so its <code>woff2</code> row is no longer current.</p><p>Manual smoke on a clean profile (<code>PI_DESKTOP_DATA_DIR=$(mktemp -d)</code>):</p><ol><li><code>pnpm dev</code> launches with <code>PI-Desktop</code> in the macOS application menu and the canonical icon in both the Dock and native About panel; no Electron brand is visible.</li><li>App launches from DMG install, window appears, and the application-menu, About-panel, and Dock branding match the development lane.</li><li>Empty home and expanded/collapsed sidebar show the canonical PI-Desktop logo; composer prompt rows have no leading brand icon; New task and project/Temporary create controls use the message-plus session icon.</li><li>Onboarding checklist appears; configure provider; one streamed chat turn.</li><li>One permissioned tool call (Write) allow + deny paths.</li><li>Quit/relaunch → session history restored, window bounds restored.</li><li><code>~/.pi-desktop/logs/</code> contains categorized NDJSON under <code>app/</code>, <code>host/</code>, and <code>agent/</code>; key lifecycle, tool, provider, plugin, and error records are available without dedicated timing files.</li><li>With network access disabled, the shell still starts; English/Chinese switching, syntax highlighting, shell highlighting, KaTeX, Mermaid fallback/rendering, host health, and sidecar health continue to use packaged local assets.</li></ol><h2 id="_6-native-runner-release-packages" tabindex="-1">6. Native-runner release packages <a class="header-anchor" href="#_6-native-runner-release-packages" aria-label="Permalink to &quot;6. Native-runner release packages&quot;">​</a></h2><p>The repository exposes native-runner commands for every release target. Each packaging command first runs <code>build:host-release</code>, then bundles the agent runtime and Electron app. D126/D285/D603 tag workflows publish these outputs and their electron-updater manifests. Run a target command on that target OS:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>macOS Apple Silicon: pnpm --filter @pi-desktop/desktop run dist:mac -- --arm64</span></span>
<span class="line"><span>macOS Intel:         pnpm --filter @pi-desktop/desktop run dist:mac -- --x64</span></span>
<span class="line"><span>Windows: pnpm --filter @pi-desktop/desktop dist:win</span></span>
<span class="line"><span>Linux:   pnpm --filter @pi-desktop/desktop dist:linux</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>The Windows <code>dist:win</code> command runs <code>scripts/build-desktop-release.mjs</code>, which invokes electron-builder once for NSIS and once for ZIP so each package gets the correct updater distribution marker.</p><p>The macOS packages include <code>bin/pi-desktop-host-core</code> built for their runner architecture; Windows includes <code>bin/pi-desktop-host-core.exe</code>; Linux includes <code>bin/pi-desktop-host-core</code>. Signing, rollback, and installer upgrade qualification remain release hardening work; publication is active under D126/D285/D603.</p><p>Native-runner output matrix:</p><ul><li>macOS arm64: <code>PI-Desktop-&lt;version&gt;-arm64.dmg</code> and <code>PI-Desktop-&lt;version&gt;-arm64-mac.zip</code></li><li>macOS Intel x64: <code>PI-Desktop-&lt;version&gt;-x64.dmg</code> and <code>PI-Desktop-&lt;version&gt;-x64-mac.zip</code></li><li>Windows x64: NSIS installer <code>PI-Desktop-Setup-&lt;version&gt;.exe</code> and portable ZIP <code>PI-Desktop-Portable-&lt;version&gt;.zip</code></li><li>Linux x64: AppImage, deb, and rpm</li><li>Linux x64 system Electron asset: <code>PI-Desktop-&lt;version&gt;-linux-x64.asar</code></li></ul><p>The portable Windows ZIP target does not write <code>latest.yml</code>. The Windows release helper builds NSIS and ZIP separately and stamps the ZIP app metadata with <code>piDistribution = &quot;zip&quot;</code>; packaged ZIP runs use notify-and-link delivery. Legacy portable executables remain manual when <code>PORTABLE_EXECUTABLE_FILE</code> is present. NSIS keeps the in-app download and quit-and-install lane. Data stays in the existing application data directory. Users extract the ZIP and launch <code>PI-Desktop.exe</code> directly, so the package does not run a self-extracting wrapper or request administrator execution.</p><p>RPM targets pass <code>_build_id_links none</code> to FPM. Bundled Electron binaries live under <code>/opt/PI-Desktop</code>; omitting global <code>/usr/lib/.build-id</code> links prevents collisions with other applications that bundle the same Electron binaries.</p><p>The ASAR asset contains the Electron application archive, not a complete Linux distribution. To repackage it, place it as the application archive in the target Electron resources layout together with the native host and other resources from the target package, then launch it with:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">electron</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> PI-Desktop-</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">versio</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">n</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">-linux-x64.asar</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Shell smoke on each native runner:</p><ol><li>Confirm no File/Edit/View/Window/Help menu appears inside the window.</li><li>Verify F10 and Shift+F10 remain available to focused content.</li><li>Execute application and editing shortcuts from a focused editor.</li><li>Minimize, maximize, restore, and close from the custom controls.</li><li>Relaunch with <code>PI_DESKTOP_START_MAXIMIZED=1</code>; confirm the initial maximize/restore glyph matches the queried native state.</li><li>Verify unknown menu/window IPC actions fail with the window open and closed.</li></ol><h2 id="_7-known-limitations" tabindex="-1">7. Known limitations <a class="header-anchor" href="#_7-known-limitations" aria-label="Permalink to &quot;7. Known limitations&quot;">​</a></h2><ul><li>Linux deb/rpm and the Windows portable ZIP remain notify-and-link update modes. Packaged macOS, Windows NSIS, and Linux AppImage use in-app <code>electron-updater</code>.</li><li>Linux x64 packages are built on Ubuntu 22.04 so host-core needs glibc 2.35 or newer (Ubuntu 22.04, Debian 12, Fedora 36+). The tag job runs <code>scripts/check-linux-host-glibc.mjs</code> and refuses a binary that needs a newer glibc.</li><li>Rollback, staged rollout, and prerelease channel policy remain open release work. Existing unsigned macOS installs may need one manual signed DMG before in-app updates succeed.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("spec/06-delivery/06-release-runbook.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _06ReleaseRunbook = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _06ReleaseRunbook as default
};
