import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"20. 宿主语音","description":"","frontmatter":{},"headers":[],"relativePath":"zh-CN/spec/03-runtime/20-speech.md","filePath":"zh-CN/spec/03-runtime/20-speech.md","lastUpdated":1789760880000}');
const _sfc_main = { name: "zh-CN/spec/03-runtime/20-speech.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="_20-宿主语音" tabindex="-1">20. 宿主语音 <a class="header-anchor" href="#_20-宿主语音" aria-label="Permalink to &quot;20. 宿主语音&quot;">​</a></h1><blockquote><p><strong>翻译说明：</strong> 本页是与 <a href="/spec/03-runtime/20-speech">英文源规格</a> 一一对应的机器辅助翻译。代码、协议字段和标识符保持原文；如翻译与英文源事实有歧义，以英文版本为准。</p></blockquote><blockquote><p>事实来源：<code>packages/shared/src/types/speech.ts</code>、 <code>packages/agent-runtime/src/speech/</code>、 <code>apps/desktop/electron/main/services/speech-service.ts</code>。 ADR：<a href="/adr/0281-host-speech-capability">0281-host-speech-capability</a>。</p></blockquote><h2 id="_1-能力" tabindex="-1">1. 能力 <a class="header-anchor" href="#_1-能力" aria-label="Permalink to &quot;1. 能力&quot;">​</a></h2><p>独立于聊天流和模型选择器：</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>transcribe({ sessionId, path, mimeType?, language? }) → { text }</span></span>
<span class="line"><span>synthesize({ sessionId, text, voice?, format? }) → { path, mimeType, dataUrl? }</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>输入是 session scratch 文件路径。输出音频写入该 session 的 scratch。 渲染器不接收 provider 密钥。TTS <code>dataUrl</code> 在文件大于8 MiB 时省略。 输入上限25 MiB（<code>SPEECH_INPUT_TOO_LARGE</code>）。</p><h2 id="_2-绑定" tabindex="-1">2. 绑定 <a class="header-anchor" href="#_2-绑定" aria-label="Permalink to &quot;2. 绑定&quot;">​</a></h2><p><code>AppSettings.speech</code> 为可选字段，不升 schema：</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>{</span></span>
<span class="line"><span>  transcribe?: { providerId, modelId, protocol, voice?, format?, path?, extra? }</span></span>
<span class="line"><span>  synthesize?: { providerId, modelId, protocol, voice?, format?, path?, extra? }</span></span>
<span class="line"><span>}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>协议 id 匹配 <code>^[a-z][a-z0-9._-]{0,63}$</code>。speech 为空或缺省即未配置状态；应用界面 不再读取该绑定，调用方只有插件与显式 IPC 调用（ADR 0291）。</p><h2 id="_3-内置协议" tabindex="-1">3. 内置协议 <a class="header-anchor" href="#_3-内置协议" aria-label="Permalink to &quot;3. 内置协议&quot;">​</a></h2><table tabindex="0"><thead><tr><th>id</th><th>transcribe</th><th>synthesize</th></tr></thead><tbody><tr><td><code>openai_audio</code></td><td><code>POST {baseUrl}/audio/transcriptions</code> multipart</td><td><code>POST {baseUrl}/audio/speech</code> JSON</td></tr><tr><td><code>openai_chat_audio</code></td><td>不支持</td><td><code>POST {baseUrl}/chat/completions</code> + <code>audio:{format,voice}</code>；读取 <code>choices.0.message.audio.data</code></td></tr></tbody></table><p>本地 OpenAI-Audio 兼容服务器使用 <code>openai_compatible</code> provider + <code>openai_audio</code>。推荐模型：OpenAI <code>whisper-1</code> / <code>tts-1</code>、Groq <code>whisper-large-v3</code>、小米 <code>mimo-v2.5-tts</code>。</p><h2 id="_4-ipc" tabindex="-1">4. IPC <a class="header-anchor" href="#_4-ipc" aria-label="Permalink to &quot;4. IPC&quot;">​</a></h2><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>pi-desktop/speech/getStatus → SpeechStatus（无密钥）</span></span>
<span class="line"><span>pi-desktop/speech/transcribe</span></span>
<span class="line"><span>pi-desktop/speech/synthesize</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>错误码：<code>SPEECH_NOT_CONFIGURED</code>、<code>SPEECH_PROTOCOL_UNSUPPORTED</code>、 <code>SPEECH_INPUT_TOO_LARGE</code>。网络/认证复用 <code>NETWORK_ERROR</code> / <code>PROVIDER_UNAUTHORIZED</code>。provider 缺失或禁用返回 <code>NOT_FOUND</code>。</p><h2 id="_5-插件适配器" tabindex="-1">5. 插件适配器 <a class="header-anchor" href="#_5-插件适配器" aria-label="Permalink to &quot;5. 插件适配器&quot;">​</a></h2><p><code>pi.speech.registerAdapter({ protocol, label, roles, handle })</code> 需要 <code>speech.adapter.register</code>（高风险）。handle 留在插件进程内。 宿主仅存元数据并调用 <code>speech.handle</code>。handle 可返回 <code>{ kind: &quot;text&quot; }</code>、<code>{ kind: &quot;audio&quot;, mimeType, data }</code> 或 <code>{ kind: &quot;http&quot;, call }</code> 由宿主用绑定密钥代发。HTTP URL 必须保持在 provider origin。内置协议 id 受保留。卸载时注销。</p><h2 id="_6-产品" tabindex="-1">6. 产品 <a class="header-anchor" href="#_6-产品" aria-label="Permalink to &quot;6. 产品&quot;">​</a></h2><p>设置页面<strong>不再提供</strong>语音入口（ADR 0291）。绑定由调用方通过宿主设置接口写入， 消费方只有插件与 <code>speech/*</code> IPC，渲染器没有任何转写或朗读控件。 Whisper / TTS 模型不得出现在聊天模型选择器中。</p><p>v1 不实现麦克风采集、Realtime 和 agent 工具。</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("zh-CN/spec/03-runtime/20-speech.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _20Speech = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _20Speech as default
};
