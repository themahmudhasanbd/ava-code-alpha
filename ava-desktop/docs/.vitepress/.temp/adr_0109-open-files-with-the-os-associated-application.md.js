import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0109: Open Files entries with the OS-associated application","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0109-open-files-with-the-os-associated-application.md","filePath":"adr/0109-open-files-with-the-os-associated-application.md","lastUpdated":1787160088000}');
const _sfc_main = { name: "adr/0109-open-files-with-the-os-associated-application.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0109-open-files-entries-with-the-os-associated-application" tabindex="-1">ADR 0109: Open Files entries with the OS-associated application <a class="header-anchor" href="#adr-0109-open-files-entries-with-the-os-associated-application" aria-label="Permalink to &quot;ADR 0109: Open Files entries with the OS-associated application&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-08-20</li><li>Deciders: PI-Desktop maintainers</li><li>Related: <a href="./0105-files-as-a-bundled-plugin">ADR 0105</a> · <a href="./0088-declared-file-scope-for-plugins">ADR 0088</a> · <a href="./../spec/07-plugins/03-plugin-api">07-plugins/03-plugin-api</a> · E2E-153</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The bundled <code>pi.files</code> view can inspect text in its isolated viewer, but image, document, archive, and other files are better handled by the operating system&#39;s associated application. The existing <code>shell.openExternal</code> API intentionally accepts only web and mail URLs, so converting a workspace path into a <code>file:</code> URL would either weaken that boundary or give the plugin an unscoped file opener.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>Add <code>pi.fs.openDefault(pathFromRoot)</code> and the matching panel bridge channel <code>fs.openDefault</code>.</li><li>The API is covered by the existing <code>fs.read</code> permission. The host resolves the root-relative path through the complete read gate: workspace/user-selected root, real-path containment, protected paths, deny-list, and declared scope or user consent. It accepts existing regular files only.</li><li>Electron Main calls <code>shell.openPath</code> after the gate, causing the operating system to choose the file association. The plugin never receives an absolute path and cannot choose an executable or command line.</li><li>The bundled Files view exposes the action only from its selected-file viewer, including for binary and image files. Success and failure are audited by the plugin id and root-relative path.</li><li>This is an additive plugin API change; the desktop IPC and host protocol versions do not change. The existing <code>shell.openExternal</code> URL restrictions remain unchanged.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Files can hand off content that the in-app text viewer cannot render.</li><li>The default application may copy, transform, or sync the file according to its own behavior; the user explicitly initiates the handoff from the viewer.</li><li>Plugins with <code>fs.read</code> still cannot open credentials, repository internals, protected app data, arbitrary absolute paths, or directories through this API.</li><li>A failed OS handoff is reported to the plugin as <code>OPEN_FAILED</code> and is visible through the Files view&#39;s error toast.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><h3 id="reuse-shell-openexternal-with-a-file-url" tabindex="-1">Reuse <code>shell.openExternal</code> with a <code>file:</code> URL <a class="header-anchor" href="#reuse-shell-openexternal-with-a-file-url" aria-label="Permalink to &quot;Reuse \`shell.openExternal\` with a \`file:\` URL&quot;">​</a></h3><p>Rejected: that API is for external links and would blur URL egress with local file access. It also makes the file scope difficult to enforce consistently.</p><h3 id="add-a-private-bundled-plugin-ipc-channel" tabindex="-1">Add a private bundled-plugin IPC channel <a class="header-anchor" href="#add-a-private-bundled-plugin-ipc-channel" aria-label="Permalink to &quot;Add a private bundled-plugin IPC channel&quot;">​</a></h3><p>Rejected: Files is intentionally a public plugin API consumer. A private bridge would recreate the trust-boundary exception that ADR 0105 removed.</p><h3 id="add-a-new-permission-separate-from-fs-read" tabindex="-1">Add a new permission separate from <code>fs.read</code> <a class="header-anchor" href="#add-a-new-permission-separate-from-fs-read" aria-label="Permalink to &quot;Add a new permission separate from \`fs.read\`&quot;">​</a></h3><p>Rejected for this action: the host does not expose an arbitrary opener. The path must already be a readable file in the plugin&#39;s declared scope, and the UI action does not let the plugin select an executable or URL. The operation remains audited so later policy changes have an explicit boundary to revise.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0109-open-files-with-the-os-associated-application.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0109OpenFilesWithTheOsAssociatedApplication = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0109OpenFilesWithTheOsAssociatedApplication as default
};
