import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0008: Plugin runtime targets isolation in a separate process","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0008-plugin-runtime-isolation-target.md","filePath":"adr/0008-plugin-runtime-isolation-target.md","lastUpdated":1789914931000}');
const _sfc_main = { name: "adr/0008-plugin-runtime-isolation-target.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0008-plugin-runtime-targets-isolation-in-a-separate-process" tabindex="-1">ADR 0008: Plugin runtime targets isolation in a separate process <a class="header-anchor" href="#adr-0008-plugin-runtime-targets-isolation-in-a-separate-process" aria-label="Permalink to &quot;ADR 0008: Plugin runtime targets isolation in a separate process&quot;">​</a></h1><ul><li>Status: Accepted (Implemented 2026-07-29)</li><li>Date: 2026-07-25</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Plugin code is untrusted; we must prevent it from dragging down or intruding into the host.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Target architecture: the plugin main runs in a <strong>separate process</strong> (UtilityProcess/Child Process) and accesses the Host API via RPC.</p><p>If MVP progress is constrained, a lighter isolation may be adopted temporarily, but it must not break:</p><ul><li>The permission gateway</li><li>The API allowlist</li><li>Unified registration of contribution points</li><li>Crash non-fatality</li></ul><p>The temporary solution and its migration plan must be noted in the implementation ADR.</p><h2 id="rationale" tabindex="-1">Rationale <a class="header-anchor" href="#rationale" aria-label="Permalink to &quot;Rationale&quot;">​</a></h2><ol><li>Crash isolation</li><li>Clearer permission proxying</li><li>Resource limits can be added later</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><h3 id="positive" tabindex="-1">Positive <a class="header-anchor" href="#positive" aria-label="Permalink to &quot;Positive&quot;">​</a></h3><ul><li>Better security and stability</li></ul><h3 id="negative" tabindex="-1">Negative <a class="header-anchor" href="#negative" aria-label="Permalink to &quot;Negative&quot;">​</a></h3><ul><li>Higher implementation and debugging cost</li></ul><h2 id="implementation-2026-07-29" tabindex="-1">Implementation (2026-07-29) <a class="header-anchor" href="#implementation-2026-07-29" aria-label="Permalink to &quot;Implementation (2026-07-29)&quot;">​</a></h2><p>The target architecture shipped; no transitional in-main runtime remains.</p><ul><li><code>apps/desktop/electron/main/plugin-host-process.mjs</code> is the per-plugin entry, forked with <code>utilityProcess.fork</code> (one process per plugin, bundled to <code>out/main/plugin-host-process.js</code>). It receives a minimal environment from <code>pluginChildEnv</code> (<code>child-process-env.ts</code>): <code>PATH</code>, toolchain dirs, <code>HOME</code> / <code>USER</code> / <code>USERPROFILE</code>, plus <code>PI_PLUGIN_ID</code> and <code>NODE_ENV</code>. The host&#39;s other shell env and provider keys never reach plugin code (issue #717).</li><li><code>apps/desktop/electron/main/plugin-runtime.ts</code> became the broker: it keeps the registry of commands/tools, and every <code>pi.*</code> call arrives as RPC, passes <code>HOST_API_ALLOWLIST</code>, then <code>assertPermission</code>, then the host service, then the audit log. Plugin code holds no host object and no <code>require</code> of host modules.</li><li>Contribution points register by descriptor only; the callable half stays in the plugin process and is invoked back over RPC with a timeout (command 30s, tool 110s, lifecycle hook 5s, load 15s).</li><li>A dying plugin process is contained: pending calls reject with <code>PLUGIN_CRASHED</code>, contributions are deregistered, the panel closes, and the renderer gets a toast plus <code>pluginChanged</code>.</li><li><code>onUnload</code> now runs (in the child) before the process is killed.</li></ul><p>Known limits, deliberately out of this change:</p><ul><li>The plugin process is a Node environment; permission gating covers the <code>pi.*</code> surface, not raw <code>require(&quot;node:fs&quot;)</code> inside the plugin process. Capability sandboxing (D009) remains future work.</li><li>Resource limits (CPU/memory) are not enforced yet.</li><li>Declared manifest permissions are still auto-granted at load time.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0008-plugin-runtime-isolation-target.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0008PluginRuntimeIsolationTarget = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0008PluginRuntimeIsolationTarget as default
};
