import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0176: Per-provider User-Agent override","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0176-per-provider-user-agent.md","filePath":"adr/0176-per-provider-user-agent.md","lastUpdated":1788788350000}');
const _sfc_main = { name: "adr/0176-per-provider-user-agent.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0176-per-provider-user-agent-override" tabindex="-1">ADR 0176: Per-provider User-Agent override <a class="header-anchor" href="#adr-0176-per-provider-user-agent-override" aria-label="Permalink to &quot;ADR 0176: Per-provider User-Agent override&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-07</li><li>Deciders: PI-Desktop core</li><li>Amends ADR 0095 / ADR 0156</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Some gateways and vendor subscriptions inspect <code>User-Agent</code>. pi-ai stamps <code>pi (&lt;platform&gt; …)</code>, Anthropic OAuth inference sends <code>claude-cli/&lt;version&gt;</code>, OpenCode Go sends <code>pi-desktop/&lt;APP_VERSION&gt;</code>, and Codex overwrites User-Agent after extra headers. Settings had no way to set a per-row value. The provider schema listed a <code>headers</code> map that was never implemented.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Each provider row — API-key AI service and OAuth vendor account — may store an optional <code>userAgent</code> in <code>config_json.userAgent</code>.</p><ul><li>Empty or omitted keeps today&#39;s adapter default.</li><li>A non-empty trimmed value is sent as <code>User-Agent</code> on that row&#39;s outbound HTTP: session turns, builtin subagents, prompt enhancement, plugin one-shots, <code>/models</code> discovery (including an unsaved form value), connection tests, and OAuth token refresh.</li><li>A fetch wrapper is the last writer so Codex and the Anthropic SDK cannot overwrite it. The same value is also placed on pi-ai stream-option headers so OpenCode&#39;s caller-wins rule stays true.</li><li>Update with <code>&quot;&quot;</code> clears the override. Max 256 bytes; CR/LF are rejected.</li><li>Not a secret. No SQLite or host-protocol version bump.</li><li>UI lives in Advanced on the AI-service dialog (named and custom) and the vendor-account editor. First OAuth login does not collect a User-Agent; it is edited after the account exists.</li><li><code>AgentRuntime.matches()</code> includes <code>userAgent</code> so editing it rebuilds the warm runtime.</li><li>The unused <code>headers</code> map stays unimplemented. If added later, <code>userAgent</code> remains the UI alias and wins over <code>headers[&quot;User-Agent&quot;]</code>. <strong>Superseded by ADR 0178:</strong> <code>config_json.headers</code> is the supported override; leftover <code>userAgent</code> migrates into <code>headers[&quot;User-Agent&quot;]</code>.</li></ul><p>Overriding Anthropic OAuth&#39;s <code>claude-cli/…</code> User-Agent can make Claude Pro/Max reject the request. That is the user&#39;s choice.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Users can impersonate another client per service or account without a global User-Agent or a full custom-header editor.</li><li>OAuth login HTTP uses the row value only after it is saved; first-login traffic keeps pi-ai defaults.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0176-per-provider-user-agent.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0176PerProviderUserAgent = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0176PerProviderUserAgent as default
};
