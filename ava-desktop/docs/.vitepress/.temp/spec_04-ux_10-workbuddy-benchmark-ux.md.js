import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"10. WorkBuddy Benchmark — UX Spec Proposal","description":"","frontmatter":{},"headers":[],"relativePath":"spec/04-ux/10-workbuddy-benchmark-ux.md","filePath":"spec/04-ux/10-workbuddy-benchmark-ux.md","lastUpdated":1789750637000}');
const _sfc_main = { name: "spec/04-ux/10-workbuddy-benchmark-ux.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="_10-workbuddy-benchmark-—-ux-spec-proposal" tabindex="-1">10. WorkBuddy Benchmark — UX Spec Proposal <a class="header-anchor" href="#_10-workbuddy-benchmark-—-ux-spec-proposal" aria-label="Permalink to &quot;10. WorkBuddy Benchmark — UX Spec Proposal&quot;">​</a></h1><blockquote><p>Source: hands-on walkthrough of WorkBuddy v5.3.5 (macOS, Electron) on 2026-07-25, captured via CDP screenshots. This doc records what WorkBuddy does UX-wise, and specs which patterns PI-Desktop should adopt, adapt, or reject. Baseline shell IA: <a href="./01-ui-ia">01-ui-ia</a>.</p></blockquote><h2 id="_1-what-workbuddy-is-observed" tabindex="-1">1. What WorkBuddy is (observed) <a class="header-anchor" href="#_1-what-workbuddy-is-observed" aria-label="Permalink to &quot;1. What WorkBuddy is (observed)&quot;">​</a></h2><p>A consumer/prosumer &quot;AI work companion&quot;: chat-first desktop app whose unit of work is a <strong>Task</strong> (a conversation with a goal), organized under <strong>Spaces / Projects</strong>, extended by a marketplace of <strong>Experts (personas) · Skills · Connectors</strong>, plus <strong>Automations</strong> (scheduled tasks). It is persona- and template-heavy; PI-Desktop is developer- and workspace-heavy. The value here is interaction patterns, not product scope.</p><h2 id="_2-observed-ia" tabindex="-1">2. Observed IA <a class="header-anchor" href="#_2-observed-ia" aria-label="Permalink to &quot;2. Observed IA&quot;">​</a></h2><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>+----------------------------+------------------------------------------+</span></span>
<span class="line"><span>| Sidebar                    | Main pane                                |</span></span>
<span class="line"><span>|  收起 · 搜索 · 筛选 (top)   |  Per-destination header (tabs/actions)   |</span></span>
<span class="line"><span>|  新建任务                   |  Content                                 |</span></span>
<span class="line"><span>|  助理 (local assistant)     |                                          |</span></span>
<span class="line"><span>|  项目 (Projects)            |  Chat destination:                       |</span></span>
<span class="line"><span>|  专家·技能·连接器 (flyout)   |   transcript + floating composer          |</span></span>
<span class="line"><span>|  自动化 (Automations)       |   composer: + · 权限选择 · 模型选择 ·      |</span></span>
<span class="line"><span>|  更多 (flyout: 文件/知识库/  |            语音 · 发送                    |</span></span>
<span class="line"><span>|        灵感)                |   AI-content disclaimer footer            |</span></span>
<span class="line"><span>|  任务 (N)  — recent tasks   |                                          |</span></span>
<span class="line"><span>|  空间 (N)  — spaces         |                                          |</span></span>
<span class="line"><span>|  Footer: avatar · 通知 · 分享|                                          |</span></span>
<span class="line"><span>+----------------------------+------------------------------------------+</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br></div></div><p>Notable per-screen details:</p><ul><li><strong>Chat / Task</strong>: title in header with search / share / history / panel icons; per-message action row (copy, like/dislike, TTS, retry, share, more) plus a <strong>token-cost chip</strong> (<code>共消耗 ◇2.85</code>) and model badge inline; global disclaimer &quot;内容由 AI 生成，请核实重要信息&quot; under the composer.</li><li><strong>Composer</strong>: single pill; placeholder teaches syntax (&quot;@ 引用对话文件， / 调用技能与指令&quot;); left cluster <code>+</code> and a <strong>permission-mode dropdown</strong> (&quot;默认权限&quot;); right cluster model selector (&quot;Auto&quot;/named model), mic, send.</li><li><strong>助理 (Assistant)</strong>: a persistent &quot;local assistant&quot; chat with a <strong>connection status in the header</strong> (&quot;已连接: 微信小程序&quot; + gear) — the assistant is presented as an entity with integrations, not just a thread.</li><li><strong>项目 (Projects)</strong>: landing page with hero (title, one-line value prop, illustration), primary CTA &quot;新建项目&quot;, &quot;我的项目&quot; card list with kebab menus, then a <strong>&quot;从模版创建&quot; template gallery</strong> (PRD flow, market research, team knowledge base, delivery, bug tracking).</li><li><strong>自动化 (Automations)</strong>: tabs 定时任务 / 运行记录; empty state (icon + one-liner + primary CTA); below it a <strong>template gallery</strong> of ~12 recipes (daily AI news, weekly report, meeting prep…) so the empty state still offers one-click value.</li><li><strong>专家·技能·连接器</strong>: sidebar item opens a 3-way flyout; the page has segmented tabs (专家/技能/连接器), search, &quot;我的专家&quot;, featured-scenario banner cards, category chip row, sort (综合/最热/最新), and rich persona cards (avatar, tagline, capability tags).</li><li><strong>更多</strong>: overflow flyout for secondary destinations (我的文件, 腾讯文档, ima知识库, 乐享知识库, 灵感) — keeps the rail at ~6 primary items.</li><li><strong>我的文件 (My Files)</strong>: not a file manager — an <strong>artifact browser for task outputs</strong>. Tabs 任务成果 / 云端网盘 (cloud-drive sync); type filter, search across &quot;文件、任务或工作空间&quot;, favorites toggle; a table (name / type / updated-by / updated-at / size) whose rows are <strong>grouped by the task that produced them</strong> (group header shows task name + count, e.g. &quot;未分组 · 1个任务&quot; → task → main.js / styles.css / index.html). Every deliverable an agent produces is findable later without reopening the conversation.</li><li><strong>空间 (Spaces)</strong>: a sidebar section between 任务 and the footer. A Space is <strong>bound to a local directory</strong> (hovering the space &quot;tools&quot; reveals a tooltip such as <code>/Users/example/Projects/tools</code>) and <strong>expands inline to list the tasks run inside it</strong>; row actions on hover: <code>+</code> (new task in this space) and <code>…</code> (manage). Tasks in both lists carry status badges — green dot = running, red <code>!</code> = failed. Cloud/collab Projects (项目) also appear in this section, so &quot;Space&quot; is the umbrella for &quot;place work happens&quot;, local folder or shared project alike.</li><li><strong>Onboarding</strong>: the first assistant message is a structured interview (name/style for the agent; how to address you, city, current focus) — personalization by conversation instead of a form wizard.</li></ul><h2 id="_3-adopt-spec-changes-for-pi-desktop" tabindex="-1">3. Adopt (spec changes for PI-Desktop) <a class="header-anchor" href="#_3-adopt-spec-changes-for-pi-desktop" aria-label="Permalink to &quot;3. Adopt (spec changes for PI-Desktop)&quot;">​</a></h2><p>Each item below is a concrete proposal; decisions go to the decisions log.</p><h3 id="_3-1-permission-mode-in-the-composer" tabindex="-1">3.1 Permission mode in the composer <a class="header-anchor" href="#_3-1-permission-mode-in-the-composer" aria-label="Permalink to &quot;3.1 Permission mode in the composer&quot;">​</a></h3><p>WorkBuddy puts the permission posture next to the send button. PI-Desktop exposes the same control as a composer chip (current effective mode: <code>Ask every time / Accept edits / Auto</code>) opening a small menu. The menu shows only those three modes and marks the effective selection directly; it does not surface global-default or inherit provenance. Choosing a mode creates a per-session override. This complements, rather than replaces, the inline permission cards in <a href="./03-permission-ux">03-permission-ux</a> (D132).</p><h3 id="_3-2-per-message-meta-cost-model" tabindex="-1">3.2 Per-message meta: cost + model <a class="header-anchor" href="#_3-2-per-message-meta-cost-model" aria-label="Permalink to &quot;3.2 Per-message meta: cost + model&quot;">​</a></h3><p>Adopt the inline <strong>token/cost chip and model badge</strong> on assistant messages (collapsed by default into the message action row; hover to expand input/output/cached breakdown). Data already exists in the runtime usage events.</p><p><strong>Adopted in D103 (tokens-only first cut), placement amended by D347</strong>: completed assistant turns show a model badge under the answer. The compact Codex-style context inspector lives in the composer toolbar next to the model picker and always mirrors the newest assistant turn that reported usage. The trigger shows remaining context percentage in a small ring; clicking it toggles a light summary panel with remaining/window counts and two unboxed turn/speed values. Provider input/output/cache/reasoning usage stays available as one inline exact-usage row, while tool usage is reduced to one aggregate row with tool types, calls, and estimated tokens. Per-tool rows, share bars, badges, and explanatory estimate copy are omitted from the default view. Generation rate is a completed-turn snapshot rather than a live streaming counter. The context-window total comes from the same <code>pi-ai</code> model metadata used by the agent sidecar, with provider metadata and the default window only as fallbacks for unknown models. Tool estimates remain separate from exact provider totals because providers do not expose per-tool context allocation. Currency pricing remains deferred. Regenerate is available as a quiet action chip next to Copy and rewrites the current turn in place (D105); when multiple variants exist, a ChatGPT-style <code>current / total</code> pager on the root user turn restores archived branches (D109). Completed assistant rows also expose Fork and reversible Edit, while omitting Delete; both edits diverge into isolated sessions so the source cache/runtime stays untouched (D134).</p><h3 id="_3-3-template-galleries-on-empty-states" tabindex="-1">3.3 Template galleries on empty states <a class="header-anchor" href="#_3-3-template-galleries-on-empty-states" aria-label="Permalink to &quot;3.3 Template galleries on empty states&quot;">​</a></h3><p>WorkBuddy never ships a dead empty state: Automations and Projects both pair the empty hero with click-to-instantiate templates. <strong>Spec</strong>: Scheduled page empty state gains 4–6 developer recipes (nightly test run, dependency-update digest, PR review sweep, changelog draft); the Settings Project archive keeps its table but gains a &quot;Start from template&quot; row when empty.</p><h3 id="_3-4-teaching-placeholder-in-the-composer" tabindex="-1">3.4 Teaching placeholder in the composer <a class="header-anchor" href="#_3-4-teaching-placeholder-in-the-composer" aria-label="Permalink to &quot;3.4 Teaching placeholder in the composer&quot;">​</a></h3><p>Replace the static placeholder with a syntax-teaching one: &quot;Describe a task — @ to reference files, / for commands&quot;. The empty home keeps this direct-entry surface focused on the hero, optional onboarding, and bottom composer, without adding developer starter cards or a contextual quick-action layer (D204/D206).</p><h3 id="_3-5-overflow-more-flyout-for-secondary-destinations" tabindex="-1">3.5 Overflow &quot;More&quot; flyout for secondary destinations <a class="header-anchor" href="#_3-5-overflow-more-flyout-for-secondary-destinations" aria-label="Permalink to &quot;3.5 Overflow &quot;More&quot; flyout for secondary destinations&quot;">​</a></h3><p>Keep the sidebar rail at ≤6 primary items. As destinations grow (Logs, Files, Knowledge, future panels), park them under a &quot;More&quot; flyout instead of lengthening the rail.</p><h3 id="_3-6-conversational-onboarding-first-run" tabindex="-1">3.6 Conversational onboarding (first-run) <a class="header-anchor" href="#_3-6-conversational-onboarding-first-run" aria-label="Permalink to &quot;3.6 Conversational onboarding (first-run)&quot;">​</a></h3><p>Complement the checklist onboarding (05-onboarding) with a first assistant turn that interviews the user (project, preferred tone, guardrails) and writes results to workspace memory/config — a form disguised as a chat.</p><h3 id="_3-7-artifacts-view-from-我的文件" tabindex="-1">3.7 Artifacts view (from 我的文件) <a class="header-anchor" href="#_3-7-artifacts-view-from-我的文件" aria-label="Permalink to &quot;3.7 Artifacts view (from 我的文件)&quot;">​</a></h3><p>Sessions produce files the user later can&#39;t find without scrolling the transcript. <strong>Adopted first step in D128</strong>: clicking a file artifact creates a path-keyed, closeable work-panel tab; its second half — a successful workspace Write/Edit opening Review by itself — was withdrawn in D451, and Review now opens only from an explicit user action. <strong>Adopted in D179</strong>: the transcript also places a message-scoped review card directly after each successful file mutation; its status, +/− totals, and expandable hunks stay attached to that tool row rather than becoming a global footer entry. These renderer tabs and cards are transient views, not a second persistence model; the host-owned <code>artifacts</code> table remains authoritative. A future dedicated Artifacts destination may list files grouped by session with name / kind / session / time columns and Finder/diff actions. Skip the cloud-drive tab (no cloud storage in scope).</p><h3 id="_3-8-workspace-scoped-session-tree-from-空间" tabindex="-1">3.8 Workspace-scoped session tree (from 空间) <a class="header-anchor" href="#_3-8-workspace-scoped-session-tree-from-空间" aria-label="Permalink to &quot;3.8 Workspace-scoped session tree (from 空间)&quot;">​</a></h3><p>WorkBuddy nests tasks under the folder-bound Space they ran in, with <code>+</code> (new task here) on hover and status badges (running dot / failure mark) per task. <strong>Adopted and extended in D093</strong>: PI-Desktop renders every retained, path-keyed project as a compact, independently collapsible group, followed by a Temporary sessions group for path-less sessions. Each directory title is the single disclosure target (chevron, folder, label, and remaining row hit area), while <code>+</code> and overflow actions appear on hover/focus. The active project is still the only selected host workspace; retained groups do not create parallel workspace singletons. This supersedes D088&#39;s one-current-project sidebar limitation while preserving its exact-path and Temporary boundaries. <strong>Refined in D135</strong>: each conversation row uses a fixed leading status slot with distinct color and geometry: accent-blue selection ring, warning-orange breathing in-progress dot, success-green check, and error-red alert. In-progress outranks selection, selection outranks the latest terminal outcome, and reduced motion keeps the in-progress dot static.</p><h2 id="_3-9-transcript-density-and-user-plate-alignment" tabindex="-1">3.9 Transcript density and user-plate alignment <a class="header-anchor" href="#_3-9-transcript-density-and-user-plate-alignment" aria-label="Permalink to &quot;3.9 Transcript density and user-plate alignment&quot;">​</a></h2><p>WorkBuddy&#39;s task transcript keeps user turns as compact right-side plates and assistant turns as full-width transparent prose, with quiet hover actions under each turn. <strong>Adopted in D101</strong>: PI-Desktop keeps the developer-tool restraint (no mascot, no like/dislike) but densifies row spacing, right-aligns the user plate at <code>min(78%, 560px)</code>, softens the plate border/shadow, and shows copy chips only on hover/focus-within. Streaming assistant answers use a thin accent left rule instead of a heavy pulse frame.</p><h2 id="_4-adapt-with-caution" tabindex="-1">4. Adapt with caution <a class="header-anchor" href="#_4-adapt-with-caution" aria-label="Permalink to &quot;4. Adapt with caution&quot;">​</a></h2><ul><li><strong>Assistant-with-integrations header</strong> (&quot;已连接: X&quot;): good pattern for showing MCP/connector health per session; adapt as a small connector status cluster in the context panel, not the chat header.</li><li><strong>Persona marketplace</strong>: out of scope as a store, but the <strong>card grammar</strong> (avatar/icon, one-line tagline, capability tags, category chips, sort) is the right template for the Plugins page as the catalog grows.</li><li><strong>Voice input / TTS</strong>: note as future; not MVP.</li></ul><h2 id="_5-reject" tabindex="-1">5. Reject <a class="header-anchor" href="#_5-reject" aria-label="Permalink to &quot;5. Reject&quot;">​</a></h2><ul><li><strong>Mascot artwork</strong> floating over the transcript — conflicts with the restrained developer-tool aesthetic (07-ui-design-system).</li><li><strong>Like/dislike + share on every message</strong> — no backend to serve; keep the action row to copy / retry / (cost chip).</li><li><strong>Consumer template content</strong> (bedtime stories, wallpapers) — replaced by developer recipes in §3.3.</li><li><strong>Blanket AI disclaimer footer</strong> — redundant in a tool whose every output is inspectable; permission UX already carries the trust surface.</li></ul><h2 id="_6-open-questions" tabindex="-1">6. Open questions <a class="header-anchor" href="#_6-open-questions" aria-label="Permalink to &quot;6. Open questions&quot;">​</a></h2><ol><li>Does the composer permission chip write through to Settings or stay session-scoped? (Proposed: session-scoped, Settings unchanged.)</li><li>Cost chip currency: tokens only, or provider-priced estimate?</li><li>Scheduled templates: hardcoded or plugin-contributable?</li></ol></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("spec/04-ux/10-workbuddy-benchmark-ux.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _10WorkbuddyBenchmarkUx = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _10WorkbuddyBenchmarkUx as default
};
