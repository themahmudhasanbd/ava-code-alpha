import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0142: Allow non-loopback HTTP MCP endpoints with explicit risk disclosure","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0142-allow-non-loopback-http-mcp.md","filePath":"adr/0142-allow-non-loopback-http-mcp.md","lastUpdated":1788253500000}');
const _sfc_main = { name: "adr/0142-allow-non-loopback-http-mcp.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0142-allow-non-loopback-http-mcp-endpoints-with-explicit-risk-disclosure" tabindex="-1">ADR 0142: Allow non-loopback HTTP MCP endpoints with explicit risk disclosure <a class="header-anchor" href="#adr-0142-allow-non-loopback-http-mcp-endpoints-with-explicit-risk-disclosure" aria-label="Permalink to &quot;ADR 0142: Allow non-loopback HTTP MCP endpoints with explicit risk disclosure&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-01</li><li>Related: <a href="./0038-plugin-mcp-bridge">ADR 0038</a>, <a href="./0056-extension-activation-scope">ADR 0056</a>, Issue #25</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Self-hosted MCP servers are often deployed on a private LAN and expose only a plain HTTP endpoint. The existing MCP client already speaks streamable HTTP, but validation rejects every non-loopback <code>http://</code> URL. This prevents common configurations such as <code>http://192.168.1.20:8080/mcp</code> even when the user has deliberately configured the server.</p><p>The old restriction also applied to plugin-declared MCP servers. Removing it must not remove the plugin permission or the plugin egress boundary: an HTTP endpoint can observe credentials, tool arguments, and tool results, and plain HTTP does not provide confidentiality or integrity.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>User-owned and plugin-declared MCP servers accept absolute <code>http://</code> and <code>https://</code> URLs. Unsupported schemes and malformed URLs remain invalid.</li><li>The user MCP editor displays an explicit warning for non-loopback HTTP: credentials and tool calls may be intercepted. The warning is informational; entering and saving a user-owned MCP endpoint is the user&#39;s consent.</li><li>A plugin-declared HTTP MCP server still requires <code>mcp.server.remote</code> and its host must be covered by <code>manifest.net.domains</code>. The same warning is shown in plugin-facing review surfaces. The existing high-risk permission remains the consent for sending tool calls to a remote endpoint.</li><li>The MCP HTTP client disables automatic redirect following, limits redirects to five hops, permits only HTTP(S) redirect targets, and invokes the optional endpoint policy for every hop. A plugin redirect to a host outside its declared network allowlist is blocked before the next request.</li><li>Marketplace catalog and package download policy is unchanged. Non-loopback HTTP package downloads remain refused because this decision concerns MCP runtime connectivity, not plugin distribution.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>LAN-hosted self-developed MCP servers work without requiring TLS termination.</li><li>The network allowlist remains authoritative for plugin-owned egress, including redirect targets.</li><li>Users and plugin reviewers can see when a connection is unencrypted, but the host cannot make a plain HTTP server safe against network interception. Trusted network placement remains an operational requirement.</li><li>SDK, renderer, host-core, client tests, security documentation, and E2E scenarios must describe the same HTTP policy.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0142-allow-non-loopback-http-mcp.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0142AllowNonLoopbackHttpMcp = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0142AllowNonLoopbackHttpMcp as default
};
