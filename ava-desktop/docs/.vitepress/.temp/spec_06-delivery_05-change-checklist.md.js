import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"05. Change Checklist","description":"","frontmatter":{},"headers":[],"relativePath":"spec/06-delivery/05-change-checklist.md","filePath":"spec/06-delivery/05-change-checklist.md","lastUpdated":1790000602000}');
const _sfc_main = { name: "spec/06-delivery/05-change-checklist.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="_05-change-checklist" tabindex="-1">05. Change Checklist <a class="header-anchor" href="#_05-change-checklist" aria-label="Permalink to &quot;05. Change Checklist&quot;">​</a></h1><blockquote><p>A practical checklist agents must run before finishing work.<br> Cross-references: <a href="./03-ai-development-workflow">ai-development-workflow</a> · <a href="./04-e2e-test-plan">e2e-test-plan</a> · <a href="./../08-meta/decisions-log">decisions-log</a> · <a href="./../../adr/README">ADR index</a> · <a href="./../../project/BOARD">BOARD</a></p></blockquote><hr><h2 id="_0-github-issue-intake" tabindex="-1">0. GitHub Issue Intake <a class="header-anchor" href="#_0-github-issue-intake" aria-label="Permalink to &quot;0. GitHub Issue Intake&quot;">​</a></h2><p>When the prompt includes a GitHub issue URL or an unambiguous issue number for this repository, complete this gate before the request-start checklist:</p><ul><li>[ ] Issue title, body, labels, comments, and state were fetched.</li><li>[ ] The reported problem was independently verified (reproduced or evidenced for a bug; confirmed missing or incomplete and in scope for a feature).</li><li>[ ] Implementation started only after the problem was confirmed to exist.</li><li>[ ] If the problem does not exist: the issue received a verification comment and was closed when the conclusion was clear, or left open when inconclusive.</li><li>[ ] After a confirmed fix was merged: the issue received a resolution comment and was closed.</li><li>[ ] The comment uses the issue&#39;s language.</li><li>[ ] No unrelated issue was commented on or closed.</li><li>[ ] Git push was not inferred from the issue link.</li></ul><p>See <a href="./03-ai-development-workflow#r5--verify-linked-github-issues-before-work-then-reply-and-close">R5 — Verify linked GitHub issues</a>.</p><hr><h2 id="_0-1-github-pull-request-intake" tabindex="-1">0.1 GitHub Pull Request Intake <a class="header-anchor" href="#_0-1-github-pull-request-intake" aria-label="Permalink to &quot;0.1 GitHub Pull Request Intake&quot;">​</a></h2><p>When the prompt includes a GitHub pull request URL or an unambiguous pull request number for this repository, complete this gate before rewriting the change or starting follow-up:</p><ul><li>[ ] Pull request title, body, files, commits, comments, checks, draft state, base/head, and linked issues were fetched.</li><li>[ ] The <strong>root-cause bar</strong> was independently judged: real in-scope problem; the change actually removes that failure mode at the root; the diff is the smallest coherent fix; approach compatible with baseline, security, and architecture.</li><li>[ ] Completeness gaps (specs, extra tests, i18n, e2e docs, style, naming) were not treated as merge blockers <strong>unless</strong> they are required to prove the root-cause fix.</li><li>[ ] If the root-cause bar holds: the pull request was merged first, preserving the contributor&#39;s commits; landing blockers received only smallest-on-top commits.</li><li>[ ] Follow-up started only after the pull request was in <code>main</code>, using a new R4 request branch and worktree.</li><li>[ ] If the root-cause bar fails or a harm blocker exists: the pull request was not merged, was not approved as &quot;direction is fine&quot;, and a comment recorded the evidence. The idea was not silently reimplemented.</li><li>[ ] A draft pull request was not merged unless the user explicitly asked.</li><li>[ ] The comment uses the pull request&#39;s language.</li><li>[ ] No unrelated pull request was commented on or merged.</li><li>[ ] The contributor&#39;s branch was not force-pushed. Unrelated remote publishing was not inferred from the pull request link.</li></ul><p>See <a href="./03-ai-development-workflow#r6--land-a-linked-pull-request-only-when-it-fixes-the-root-cause-with-a-minimal-diff">R6 — Land a linked pull request only when it fixes the root cause with a minimal diff</a>.</p><hr><h2 id="_1-request-start-checklist" tabindex="-1">1. Request Start Checklist <a class="header-anchor" href="#_1-request-start-checklist" aria-label="Permalink to &quot;1. Request Start Checklist&quot;">​</a></h2><p>Before editing any file for a new request:</p><ul><li>[ ] Existing uncommitted work is identified and preserved.</li><li>[ ] <code>origin/main</code> is fetched and local <code>main</code> is fast-forwarded when its worktree is clean.</li><li>[ ] A dedicated <code>&lt;type&gt;/&lt;short-description&gt;</code> request branch and worktree are created from that updated <code>main</code> commit.</li><li>[ ] The request worktree reuses the primary checkout&#39;s host toolchains, compatible dependency tree, package stores, caches, and ignored local configuration where safe; no separate environment is installed for E2E.</li><li>[ ] Only mutable, incompatible, or concurrency-sensitive environment state stays worktree-local and ignored.</li><li>[ ] The current branch is not <code>main</code> before implementation begins.</li><li>[ ] Delivery scope is recorded: a commit request stays on the request branch; a push request includes PR-based remote <code>main</code> integration after <code>pnpm check:pr-base</code>. Explicit branch-only or draft-only limits are honored.</li></ul><hr><h2 id="_2-impact-analysis" tabindex="-1">2. Impact Analysis <a class="header-anchor" href="#_2-impact-analysis" aria-label="Permalink to &quot;2. Impact Analysis&quot;">​</a></h2><p>Before starting implementation, answer these questions:</p><ul><li>[ ] What behavior changes does this change introduce?</li><li>[ ] Which specs are affected? (list file paths)</li><li>[ ] Does this change touch an architectural boundary? (process model, IPC, storage, security, plugin API)</li><li>[ ] Does this change affect user-visible or protocol-visible behavior?</li><li>[ ] Is local validation necessary for this change&#39;s risk and regression scope? If so, what is the smallest targeted check set?</li><li>[ ] Which milestone deliverable does this relate to? (M1–M6, or none)</li></ul><p>Reference the <a href="./03-ai-development-workflow#3-spec-update-matrix">spec update matrix</a> to determine required doc updates.</p><hr><h2 id="_3-spec-sync-checklist" tabindex="-1">3. Spec Sync Checklist <a class="header-anchor" href="#_3-spec-sync-checklist" aria-label="Permalink to &quot;3. Spec Sync Checklist&quot;">​</a></h2><p>After implementation (or alongside it):</p><ul><li>[ ] Every affected spec file is updated with the new behavior.</li><li>[ ] If <code>AGENTS.md</code> changed: <code>CLAUDE.md</code> still mirrors the non-negotiables, both files share the same <code>Policy-Sync:</code> token, and <code>pnpm check:agent-policy</code> passes. The reverse applies when only <code>CLAUDE.md</code> changed.</li><li>[ ] If architectural boundary changed: ADR is written or updated in <code>docs/adr/</code>.</li><li>[ ] If an implementation default changed: <code>decisions-log.md</code> entry updated.</li><li>[ ] If baseline frozen decisions are affected: baseline bump + explicit ADR (not MVP-normal).</li><li>[ ] Cross-references between specs are still correct (no stale links).</li></ul><hr><h2 id="_4-e2e-test-doc-checklist" tabindex="-1">4. E2E / Test Doc Checklist <a class="header-anchor" href="#_4-e2e-test-doc-checklist" aria-label="Permalink to &quot;4. E2E / Test Doc Checklist&quot;">​</a></h2><ul><li>[ ] If user-visible or protocol-visible behavior changed: new or updated scenario in <a href="./04-e2e-test-plan">04-e2e-test-plan.md</a>.</li><li>[ ] If a scenario was added or updated, it follows the template (ID, title, preconditions, steps, expected, specs, acceptance, milestone, status).</li><li>[ ] If a scenario was added or updated, the traceability matrix in §8 is current.</li><li>[ ] Unit tests added or updated when the change&#39;s risk makes them necessary.</li><li>[ ] Integration tests added or updated when an IPC/RPC contract change or cross-component regression risk makes them necessary.</li><li>[ ] The smallest necessary targeted local checks passed, or local validation was assessed as unnecessary with no separate approval or waiver required.</li><li>[ ] Relevant E2E suites were selected and passed on a candidate that contains latest <code>origin/main</code> before the request branch was pushed and a PR/MR was opened, or before a commit-only delivery was declared complete; required validation needs no separate user request. Documentation-only changes retain their existing exemption.</li><li>[ ] Task-candidate E2E used the host development environment already provisioned in the primary checkout; no <code>pnpm install</code>/<code>npm install</code> or second dependency/runtime environment was created solely for E2E. Any missing/incompatible-host exception is recorded with its reason.</li><li>[ ] Results apply to the commit the gate ran on, and the affected suites were rerun when the landed executable content changed. Any required suite not run is recorded with its reason, alternative validation, and remaining risk; delivery remains incomplete until that gate passes.</li><li>[ ] Hosting-platform E2E jobs that start before the remote merge were observed and reported but do not replace the local <code>main</code> gate; a post-merge rerun happened when the landed executable content differs from the commit the gate ran on. Dispatch or rerun follows the hosting platform&#39;s or repository workflow&#39;s requirements.</li></ul><hr><h2 id="_5-git-commit-checklist" tabindex="-1">5. Git Commit Checklist <a class="header-anchor" href="#_5-git-commit-checklist" aria-label="Permalink to &quot;5. Git Commit Checklist&quot;">​</a></h2><ul><li>[ ] Change is one logical unit (or split into focused commits).</li><li>[ ] No secrets, tokens, or local data in the diff.</li><li>[ ] No <code>node_modules/</code>, build artifacts, or release packages in the diff.</li><li>[ ] Commit message follows conventional format: <code>type(scope): description</code> (English only).</li><li>[ ] Spec updates committed with code when tightly coupled, or adjacent <code>docs:</code> commit for pure docs.</li><li>[ ] <code>git diff --stat</code> reviewed — nothing unexpected.</li></ul><hr><h2 id="_6-pull-merge-request-checklist" tabindex="-1">6. Pull/Merge Request Checklist <a class="header-anchor" href="#_6-pull-merge-request-checklist" aria-label="Permalink to &quot;6. Pull/Merge Request Checklist&quot;">​</a></h2><p>Before marking requested integration complete, apply the R4 delivery scope; explicit branch-only or draft-only requests retain their narrower scope:</p><ul><li>[ ] Before a PR/MR is opened or updated, <code>origin/main</code> is an ancestor of the request head (<code>pnpm check:pr-base</code>). A PR behind <code>origin/main</code> is not opened.</li><li>[ ] For a code-bearing change: the relevant E2E gate ran against a candidate that contains latest <code>origin/main</code> before the branch push and PR/MR creation, or before a commit-only delivery was declared complete, or its <code>NOT RUN</code> limitation is recorded with reason, alternative validation, and remaining risk.</li><li>[ ] Commit-only delivery did not publish remotely without separate authorization.</li><li>[ ] For authorized remote delivery: the request branch was pushed, its PR/MR targeted <code>main</code> with only this task&#39;s changes, and the description documented impacted specs, E2E scenarios, and validation.</li><li>[ ] For authorized remote delivery: PR self-review, required checks (including the PR-base ancestry gate), and reviews passed; the PR/MR merged into remote <code>main</code> using a permitted strategy; local <code>main</code> was synchronized with the landed change; when the landed executable content differs from the commit the gate ran on, the affected suites were rerun and recorded.</li><li>[ ] No direct push to <code>main</code>, force-push, discarded unrelated work, or bypassed gate was inferred from the delivery request. Genuine blockers were reported.</li><li>[ ] Request worktree is removed after merge.</li><li>[ ] Merged request branch is deleted locally (<code>git branch -d</code>).</li><li>[ ] Any remotely published request branch is deleted after merge.</li><li>[ ] Issue reference is included when applicable (e.g. <code>Refs #12</code> or <code>Closes #12</code>).</li></ul><hr><h2 id="_6-1-merge-cleanup-checklist" tabindex="-1">6.1 Merge Cleanup Checklist <a class="header-anchor" href="#_6-1-merge-cleanup-checklist" aria-label="Permalink to &quot;6.1 Merge Cleanup Checklist&quot;">​</a></h2><p>Run immediately after the request branch is integrated into <code>main</code>, whether the merge happened remotely via PR/MR or locally in the primary checkout:</p><ul><li>[ ] Expected commits are verified present in local <code>main</code>, and remote <code>main</code> when remote delivery was requested.</li><li>[ ] The request worktree is clean — no uncommitted or untracked request files remain.</li><li>[ ] <code>git worktree remove &lt;worktree-path&gt;</code> succeeded without forcing.</li><li>[ ] <code>git branch -d &lt;type&gt;/&lt;short-description&gt;</code> succeeded (no <code>-D</code> fallback on an unmerged branch).</li><li>[ ] <code>git worktree prune</code> leaves <code>git worktree list</code> free of stale entries for this request.</li><li>[ ] No other agent&#39;s worktree or branch was removed.</li><li>[ ] If launch was requested after delivery, the app was built and started from the integrated <code>main</code> checkout and its development environment.</li></ul><hr><h2 id="_7-app-version-release-checklist-stable-tag" tabindex="-1">7. App version release checklist (stable tag) <a class="header-anchor" href="#_7-app-version-release-checklist-stable-tag" aria-label="Permalink to &quot;7. App version release checklist (stable tag)&quot;">​</a></h2><p>Required for every stable app version bump / tag (D164). Skip only for documentation-only work or non-release chores.</p><ul><li>[ ] <code>packages/shared/src/changelog.ts</code> has a newest-first entry for the release version under <code>en</code> and every shipped product locale (no leading <code>v</code>).</li><li>[ ] Highlight counts match across locales; English is the source of truth.</li><li>[ ] Bullets are short user-facing product notes (not raw PR/commit lists).</li><li>[ ] Pre-release-only versions are omitted from the product catalog unless product explicitly ships in-app notes for that channel.</li><li>[ ] <code>packages/shared/src/changelog.test.ts</code> lists the new version first.</li><li>[ ] <code>pnpm --filter @pi-desktop/shared test</code> passes catalog alignment.</li><li>[ ] <code>README.md</code> and <code>README.zh-CN.md</code> state the current <code>&lt;major&gt;.&lt;minor&gt;.x</code> release line and contain no toolchain, command, Highlights, or roadmap claim the release invalidates.</li><li>[ ] <code>node scripts/check-release-docs.mjs</code> passes (version surfaces, shipped-locale catalog, README release line).</li><li>[ ] Documentation commit is on the release branch <strong>before</strong><code>node scripts/release.mjs &lt;version&gt; --tag</code> / <code>git tag v&lt;version&gt;</code>.</li><li>[ ] GitHub auto-generated release body is treated as web-only, not the in-app source (<a href="./06-release-runbook#41-mandatory-release-version-surface-gate-d164--d260">06-release-runbook.md §4.1</a>).</li></ul><hr><h2 id="_8-final-definition-of-done-gate" tabindex="-1">8. Final Definition-of-Done Gate <a class="header-anchor" href="#_8-final-definition-of-done-gate" aria-label="Permalink to &quot;8. Final Definition-of-Done Gate&quot;">​</a></h2><p>Before marking work complete, verify <strong>all applicable</strong> conditions under the user&#39;s delivery scope:</p><table tabindex="0"><thead><tr><th>#</th><th>Gate</th><th>Source</th></tr></thead><tbody><tr><td>1</td><td>Request branch and worktree created from an up-to-date <code>origin/main</code>; primary environment reused where safe</td><td><a href="./03-ai-development-workflow#r4--request-branch--worktree--merge-gate">R4 — Request branch + worktree + merge gate</a></td></tr><tr><td>2</td><td>Code/doc implements the planned change</td><td>Step 4 of <a href="./03-ai-development-workflow#2-development-loop">development loop</a></td></tr><tr><td>3</td><td>All impacted specs updated</td><td><a href="./03-ai-development-workflow#r1--spec-first--spec-sync">R1 — Spec-sync</a></td></tr><tr><td>4</td><td>E2E scenarios documented (or confirmed not needed)</td><td><a href="./03-ai-development-workflow#r3--e2e-coverage-doc">R3 — E2E coverage doc</a></td></tr><tr><td>5</td><td>Targeted local checks follow the existing risk standard; for a code-bearing change the relevant E2E gate passed on a candidate that contains latest <code>origin/main</code> (<code>pnpm check:pr-base</code>) before the branch push, the PR/MR, or a commit-only completion (or its <code>NOT RUN</code> limitation is recorded); after the remote merge the affected suites were rerun when the landed executable content changed; required tests need no separate user request</td><td>Steps 7, 10, and 11 of development loop</td></tr><tr><td>6</td><td>Change committed with conventional message</td><td><a href="./03-ai-development-workflow#r2--commit-per-change">R2 — Commit-per-change</a></td></tr><tr><td>7</td><td>BOARD updated if milestone deliverable completed</td><td>Step 9 of development loop</td></tr><tr><td>8</td><td>No secrets or local data in commit</td><td><a href="./03-ai-development-workflow#44-never-commit">§4.4 Never commit</a></td></tr><tr><td>9</td><td>Requested local/remote <code>main</code> integration completed under R4; expected commits verified and merged worktree/branch removed; any requested launch uses integrated <code>main</code></td><td><a href="./03-ai-development-workflow#r4--request-branch--worktree--merge-gate">R4 — Request branch + worktree + merge gate</a></td></tr><tr><td>10</td><td>No merged worktree left on disk; <code>git worktree list</code> has no stale entry for this request</td><td><a href="#61-merge-cleanup-checklist">§6.1 Merge Cleanup Checklist</a></td></tr><tr><td>11</td><td>If a GitHub issue was linked: verified before work; commented in the issue language; closed when conclusive</td><td><a href="./03-ai-development-workflow#r5--verify-linked-github-issues-before-work-then-reply-and-close">R5 — Verify linked GitHub issues</a></td></tr><tr><td>12</td><td>If a GitHub pull request was linked: root-cause bar reviewed; merged only when it actually fixes the problem with a minimal diff; follow-up after merge; contributor work not discarded for nits</td><td><a href="./03-ai-development-workflow#r6--land-a-linked-pull-request-only-when-it-fixes-the-root-cause-with-a-minimal-diff">R6 — Land a linked pull request only when it fixes the root cause with a minimal diff</a></td></tr></tbody></table><p>If any applicable gate fails, the requested integration is <strong>not Done</strong>; report the blocker without weakening the gate.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("spec/06-delivery/05-change-checklist.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _05ChangeChecklist = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _05ChangeChecklist as default
};
