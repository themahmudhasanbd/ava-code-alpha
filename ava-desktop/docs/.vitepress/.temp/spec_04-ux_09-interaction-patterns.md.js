import { ssrRenderAttrs, ssrInterpolate, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"09. Interaction Patterns","description":"","frontmatter":{},"headers":[],"relativePath":"spec/04-ux/09-interaction-patterns.md","filePath":"spec/04-ux/09-interaction-patterns.md","lastUpdated":1790060879000}');
const _sfc_main = { name: "spec/04-ux/09-interaction-patterns.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="_09-interaction-patterns" tabindex="-1">09. Interaction Patterns <a class="header-anchor" href="#_09-interaction-patterns" aria-label="Permalink to &quot;09. Interaction Patterns&quot;">​</a></h1><blockquote><p>Design system tokens: <a href="./07-ui-design-system">07-ui-design-system.md</a><br> Component anatomy: <a href="./08-component-spec">08-component-spec.md</a><br> Permission UX: <a href="./03-permission-ux">03-permission-ux.md</a><br> Command palette: <a href="./04-builtin-commands">04-builtin-commands.md</a></p></blockquote><h2 id="_1-keyboard-shortcuts-baseline" tabindex="-1">1. Keyboard shortcuts baseline <a class="header-anchor" href="#_1-keyboard-shortcuts-baseline" aria-label="Permalink to &quot;1. Keyboard shortcuts baseline&quot;">​</a></h2><h3 id="_1-1-global-shortcuts" tabindex="-1">1.1 Global shortcuts <a class="header-anchor" href="#_1-1-global-shortcuts" aria-label="Permalink to &quot;1.1 Global shortcuts&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Shortcut</th><th>Action</th><th>Context</th></tr></thead><tbody><tr><td><code>Option + Space</code> (macOS) / <code>Alt + Space</code> (Windows/Linux)</td><td>Open plugin launcher</td><td>OS-global after application boot; customizable</td></tr><tr><td><code>Cmd/Ctrl + Shift + P</code></td><td>Open command palette</td><td>Global (D014)</td></tr><tr><td><code>Cmd/Ctrl + N</code></td><td>New chat/session</td><td>Global</td></tr><tr><td><code>Cmd/Ctrl + O</code></td><td>Open project</td><td>Global</td></tr><tr><td><code>Alt + Shift + W</code></td><td>Show or hide the window (toggle)</td><td>OS-global (D439); hides to the tray, never quits</td></tr><tr><td><code>Cmd/Ctrl + ,</code></td><td>Open settings</td><td>Global</td></tr><tr><td><code>Cmd/Ctrl + B</code></td><td>Toggle sidebar</td><td>Global</td></tr><tr><td><code>Cmd/Ctrl + J</code></td><td>Toggle work panel</td><td>Global; active session</td></tr><tr><td><code>Cmd/Ctrl + [</code></td><td>Previous destination</td><td>Global</td></tr><tr><td><code>Cmd/Ctrl + ]</code></td><td>Next destination</td><td>Global</td></tr><tr><td><code>Cmd/Ctrl + .</code></td><td>Abort active turn</td><td>Global (same as abort button)</td></tr><tr><td><code>Cmd/Ctrl + K</code></td><td>Open command palette</td><td>Global</td></tr></tbody></table><h3 id="_1-2-conversation-context-shortcuts" tabindex="-1">1.2 Conversation context shortcuts <a class="header-anchor" href="#_1-2-conversation-context-shortcuts" aria-label="Permalink to &quot;1.2 Conversation context shortcuts&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Shortcut</th><th>Action</th><th>Context</th></tr></thead><tbody><tr><td><code>Enter</code></td><td>Send message when Enter-to-send is on; newline when it is off</td><td>Composer focused</td></tr><tr><td><code>Cmd/Ctrl + Enter</code></td><td>Send message when Enter-to-send is off</td><td>Composer focused</td></tr><tr><td><code>Shift + Enter</code></td><td>Newline</td><td>Composer focused</td></tr><tr><td><code>Alt + Enter</code> (<code>Option + Enter</code> on macOS)</td><td>Steer the current turn; send normally when idle</td><td>Composer focused, outside IME composition</td></tr><tr><td><code>Escape</code></td><td>Clear input / blur composer</td><td>Composer focused</td></tr><tr><td><code>Cmd/Ctrl + ↑</code></td><td>Scroll to top of transcript</td><td>Transcript focused</td></tr><tr><td><code>Cmd/Ctrl + ↓</code></td><td>Scroll to bottom of transcript</td><td>Transcript focused</td></tr></tbody></table><h3 id="_1-3-command-palette-shortcuts-within-palette" tabindex="-1">1.3 Command palette shortcuts (within palette) <a class="header-anchor" href="#_1-3-command-palette-shortcuts-within-palette" aria-label="Permalink to &quot;1.3 Command palette shortcuts (within palette)&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Shortcut</th><th>Action</th><th>Context</th></tr></thead><tbody><tr><td><code>↑ / ↓</code></td><td>Navigate results</td><td>Palette open</td></tr><tr><td><code>Enter</code></td><td>Execute selected command</td><td>Palette open</td></tr><tr><td><code>Escape</code></td><td>Close palette</td><td>Palette open</td></tr></tbody></table><h3 id="_1-4-shortcut-rules" tabindex="-1">1.4 Shortcut rules <a class="header-anchor" href="#_1-4-shortcut-rules" aria-label="Permalink to &quot;1.4 Shortcut rules&quot;">​</a></h3><ul><li>macOS application-menu shortcuts are discoverable through system-menu accelerators. Windows/Linux shortcuts remain available without rendering an application menubar; command-only shortcuts are discoverable via command palette search (keyword &quot;shortcut&quot; or &quot;keybinding&quot;).</li><li>Shortcuts must not conflict with macOS system shortcuts or common browser shortcuts</li><li>Never override <code>Cmd/Ctrl + C</code>, <code>Cmd/Ctrl + V</code>, <code>Cmd/Ctrl + A</code>, <code>Cmd/Ctrl + S</code></li><li>Shortcuts are consistent across macOS (Cmd) and Windows/Linux (Ctrl)</li><li>A missing shortcut override uses the shared platform default; a valid string uses the custom binding; an explicit <code>null</code> means <code>Unbound</code> and never dispatches. Unbound actions do not conflict with other bindings.</li><li>A modifier-only keydown and an IME composition/229 keydown never dispatch a command. Repeated keydown events do not repeatedly traverse destination history; each back/forward chord advances at most once per physical press.</li><li>Command-only shortcut changes require updating the command palette metadata; native roles and visible application-menu accelerators remain menu-owned</li><li>The plugin launcher is registered through Electron&#39;s native global shortcut API. Windows&#39; reserved default <code>Alt + Space</code> additionally uses a host-core low-level keyboard hook that consumes the system-menu chord and emits an Electron host notification, so it works while another application is focused. A focused-window fallback remains available if the hook cannot be installed. An unbound launcher disables both the hook and focused-window fallback. Custom bindings continue to use Electron&#39;s global shortcut API. Electron starts warming the launcher in a hidden window as soon as Electron is ready, in parallel with backend and main-window boot; shortcut delivery during warm-up joins the same in-flight load. The macOS show path relies on the panel&#39;s normal activation instead of issuing a second application activation or window-stack move. The launcher always opens on the display nearest the pointer.</li><li>The window visibility key is one toggle (<code>Alt + Shift + W</code>): a visible, focused window hides to the tray, and anything else — hidden, minimized, or behind another application — is shown and focused. Hiding never enters the close path, so it raises no close-behaviour prompt, destroys nothing, and never quits the app. The key is globally registered, so it deliberately avoids <code>Cmd/Ctrl + W</code>, which macOS spends on its own close-window command and which would be taken from every application if the app claimed it. The retired <code>Cmd/Ctrl + Shift + W</code> summon chord is not registered either, and stored <code>closeWindow</code>/<code>summonWindow</code> overrides are folded into the toggle when the map is read (D438, D439).</li></ul><h3 id="_1-5-plugin-launcher-shortcuts" tabindex="-1">1.5 Plugin launcher shortcuts <a class="header-anchor" href="#_1-5-plugin-launcher-shortcuts" aria-label="Permalink to &quot;1.5 Plugin launcher shortcuts&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Shortcut</th><th>Action</th><th>Context</th></tr></thead><tbody><tr><td><code>↑ / ↓</code></td><td>Cycle matching plugins</td><td>Launcher focused</td></tr><tr><td><code>Enter</code></td><td>Open selected plugin panel</td><td>Launcher focused, not composing IME text</td></tr><tr><td><code>Escape</code></td><td>Dismiss launcher</td><td>Launcher focused</td></tr></tbody></table><p>The launcher opens with an empty query and shows enabled, ready panel plugins in most-recently-used order from renderer-local device history, so the last opened plugin stays one Enter away. Typing still ranks search relevance first; recency only breaks ties between equally relevant matches.</p><h3 id="_1-5-platform-application-menus" tabindex="-1">1.5 Platform application menus <a class="header-anchor" href="#_1-5-platform-application-menus" aria-label="Permalink to &quot;1.5 Platform application menus&quot;">​</a></h3><ul><li>macOS application-menu accelerators dispatch the same allowlisted shell commands as renderer controls. Native Edit/View/Window roles retain platform text-editing, zoom, fullscreen, hide, and quit behavior.</li><li>Windows/Linux render no application menu in the window. Their frameless titlebar keeps sidebar actions at the left edge and native window controls at the conversation pane&#39;s right edge while the work panel is closed. While the work panel is open, those controls stay viewport-fixed at the window&#39;s right edge over the panel header, which reserves the control band plus the work-panel toggle so resource close remains reachable. The sole panel collapse control is that viewport-fixed toggle. Destination history has no visible back/forward controls and remains available through the renderer shortcuts. The first transcript row starts below the 46px titlebar control band so user and assistant content cannot overlap the minimize, maximize/restore, or close targets. Destination pages and the plugin detail sheet start below the same band, so page header actions and the sheet close control never stack under those targets. F10 and Shift+F10 are not consumed by shell chrome.</li><li>Windows/Linux keep New Task, Open Project, Settings, close-window, zoom, fullscreen, search, command-palette, sidebar, and work-panel shortcuts through renderer key handling. Standard editing shortcuts remain native web-content behavior.</li><li>Developer tools are opt-in. With developer mode enabled, Main handles F12 on every platform and Ctrl+Shift+I on Windows/Linux; macOS exposes its native developer-tools role in View; the conversation overflow menu adds Copy conversation ID and Open session path. With the mode disabled these product entry points remain unavailable, and disabling it closes an open console.</li><li>Main queues native commands until the renderer acknowledges that its menu event subscription is active on macOS. Closing and recreating a window resets this handshake.</li><li>Frameless minimize, maximize/restore, and close controls remain outside the drag region. Maximize state is queried on mount and updated from native window events, so the restore affordance never depends only on optimistic renderer state.</li><li>Windows/Linux explicit minimize actions use native minimize and keep the taskbar entry. On Windows, clicking the focused window&#39;s taskbar button also uses native minimize and keeps the taskbar entry; clicking it again restores/focuses the same window, while clicking the entry for a merely covered window keeps the normal bring-to-front behavior (D252 / ADR 0117). macOS native minimize remains tray-resident. Windows/Linux close behavior is user-configurable (ADR 0090): an unset preference asks once via a native prompt (Cancel / Close to tray / Quit); <code>tray</code> hides the window under that same tray icon, whose click restores the window; <code>quit</code> exits the app. Close behavior never creates or destroys the tray — D216 owns it, so the icon is resident under either choice. The choice is persisted, revisitable in Settings → General, and applied by both the close button and the close shortcut. Explicit quit (Cmd+Q, application-menu Quit, tray Quit) is a separate confirm step (D363): Cancel leaves the app running; Confirm runs the ordered shutdown. A D230 window-close Quit does not ask again. Automated boot, supervision, and capture probes skip the dialog, as does the restart that installs an already-downloaded update — its installer is already running and gives up when the app stays alive. macOS keeps the native Dock lifecycle (close keeps the app in the Dock; activating recreates the window). The bounds watchdog never restores a minimized or tray-hidden window.</li></ul><h3 id="_1-5-1-tray-resident-and-taskbar-minimize" tabindex="-1">1.5.1 Tray-resident and taskbar minimize <a class="header-anchor" href="#_1-5-1-tray-resident-and-taskbar-minimize" aria-label="Permalink to &quot;1.5.1 Tray-resident and taskbar minimize&quot;">​</a></h3><ul><li>Explicit application minimize means <strong>native taskbar minimize</strong> on Windows and Linux: the renderer&#39;s window-control button and native-menu minimize action use the normal OS transition. macOS traffic-light minimize and the macOS Window → Minimize role remain <strong>hide to tray</strong>.</li><li>On Windows, clicking the taskbar button of the focused visible main window means <strong>native minimize</strong>. The window stays represented by its taskbar entry; the next click restores and focuses it. A taskbar click while the window is merely covered brings it to the front and does not hide it to the tray.</li><li>Tray hiding, including a Windows/Linux close with <code>tray</code>, removes the main window from the taskbar/dock window list while the Electron process and background work remain alive. It does not persist a minimized geometry or dispose the host/sidecar.</li><li>Double-clicking the PI-Desktop tray icon (or single-clicking on Windows/Linux), choosing Open, or activating the app from the macOS dock restores and focuses the existing window. If the window was closed, the same action creates a fresh window.</li><li>The tray menu is localized with the active shipped shell locale and exposes Open, bounded session groups, and an explicit Quit action. Quit uses the existing ordered shutdown path. What closing the window does is the user&#39;s own choice on Windows/Linux (ADR 0090) and a Dock-lifecycle close on macOS; the tray icon itself is created once at startup either way.</li></ul><h3 id="_1-5-2-tray-session-navigation-issue-293" tabindex="-1">1.5.2 Tray session navigation (issue #293) <a class="header-anchor" href="#_1-5-2-tray-session-navigation-issue-293" aria-label="Permalink to &quot;1.5.2 Tray session navigation (issue #293)&quot;">​</a></h3><ul><li>The native menu shows Running, Unread, and Pinned in that order, at most nine sessions in total. Every non-empty group keeps up to three rows; the share smaller groups leave unused goes to the groups that still overflow, in priority order, so one busy group can fill all nine while the others are empty. Membership is assigned before applying limits; higher-priority overflow never spills into a lower group.</li><li>Empty groups are hidden. Archived sessions/projects and deleted sessions are excluded. Running/Pinned follow sidebar sorting; Unread follows the latest unread result per session, newest first, including failed results.</li><li>Long titles use one line capped at 32 display columns including the ellipsis; an East Asian wide or emoji code point counts as two, so a CJK row stays as wide as a Latin one. An overflowing group offers View more to restore the window and expand session navigation. A session row restores/focuses its exact conversation, activating its project through the existing selection flow.</li><li>macOS single-click opens the menu without restoring/focusing a conversation or marking it read. Entering a conversation uses normal acknowledgement. Open and double-click restore the window; Quit keeps its confirmation and ordered shutdown. Group/action labels follow the active shipped locale.</li><li>Start/finish, read, pin, rename, archive, delete, and backend restart update the menu. The menu remains available when the main window is hidden or closed, without creating another window until an explicit activation.</li><li>macOS does not listen for tray mouse-enter: that event replaces the native status item and hides the extra. Windows/Linux still retry a failed Host read on hover/right-click; macOS retries from the next session or inbox event.</li></ul><h3 id="_1-6-sidebar-project-and-conversation-organization" tabindex="-1">1.6 Sidebar project and conversation organization <a class="header-anchor" href="#_1-6-sidebar-project-and-conversation-organization" aria-label="Permalink to &quot;1.6 Sidebar project and conversation organization&quot;">​</a></h3><p>The sidebar is a path-keyed presentation of host-owned projects and sessions. The <code>Sessions</code> heading appears first and contains path-less conversations plus their create and sort controls. Its bounded list keeps standalone work visible without consuming the full sidebar. The following <code>Projects</code> section heading exposes the project picker above retained project groups. Several project groups may be retained while exactly one workspace supplies the visible shell context.</p><h4 id="project-tab-lifecycle" tabindex="-1">Project tab lifecycle <a class="header-anchor" href="#project-tab-lifecycle" aria-label="Permalink to &quot;Project tab lifecycle&quot;">​</a></h4><ol><li><strong>Open</strong> — selecting a project from Settings → Project archive or the picker adds its normalized path to the retained set and activates it. Existing tabs remain.</li><li><strong>Activate</strong> — selecting a different group calls the existing <code>project.set</code> bridge. Its path then drives topbar identity, active workspace state, and new-task scope.</li><li><strong>Collapse</strong> — disclosure state belongs to each project path. Collapsing hides children only; it neither changes the selected session nor stops a run. The directory row is one full-width disclosure target containing its chevron, folder, and label: selecting an inactive directory activates it first, and every directory-row click toggles that group&#39;s children without changing any other group&#39;s state. Project actions are separate sibling controls and never toggle the directory.</li><li><strong>Close</strong> — closing removes only the retained tab. If it was active, the last remaining tab is selected or the visible workspace is cleared. Durable projects, sessions, and transcripts remain.</li></ol><h4 id="organization-actions" tabindex="-1">Organization actions <a class="header-anchor" href="#organization-actions" aria-label="Permalink to &quot;Organization actions&quot;">​</a></h4><ul><li><strong>Rename</strong> — the session row menu and Project archive task rows open the same modal editor. Saving trims the title and persists 1–80 Unicode code points; blank values are not submittable. The title is metadata only, so the task&#39;s transcript, activity ordering, project binding, and empty-session state are unchanged. Escape, Cancel, or clicking the scrim dismisses the editor.</li><li><strong>Edit project</strong> — the project overflow menu in the sidebar and Project archive opens the same editor for the selected logical project. The editor trims and persists a 1–80 Unicode-code-point group name and lists every registered folder. The Primary folder stays first and cannot be removed; additional folders can be added through the native multi-selection picker or removed individually. Saving updates the host-owned group while preserving the normalized paths, workspace identity, sessions, transcripts, and on-disk folders. A folder with existing chats cannot be removed.</li><li><strong>Pin</strong> toggles presentation priority. Pinned projects/conversations appear before unpinned rows within the selected secondary order. In the sidebar, a pinned project replaces its Folder glyph with a filled accent Star so its state remains recognizable without opening its overflow menu.</li><li><strong>Archive</strong> is non-destructive. Archived rows are hidden by default, available through Show archived, and restorable. Archiving does not cancel a turn or delete a transcript.</li><li><strong>Delete</strong> removes a session or a project permanently and takes two clicks: the first arms the overflow item and relabels it (<code>nav.deleteTaskConfirm</code> / <code>project.deleteMenuConfirm</code>), and only the second click removes the row. The arm expires on its own, so a row never stays one stray click away from a permanent delete, and the folder on disk is never touched. A project whose turn is still live still opens the confirmation dialog that names those sessions and stops them first; an idle project is removed on that second click.</li><li><strong>Create branch</strong> snapshots an idle conversation&#39;s complete active transcript into an independent session in the same project/Temporary scope. The command is disabled while the source runs. Success selects the child and focuses the composer; failure leaves the source visible and unchanged.</li><li>Archiving the visible conversation/project first moves the visible context to a non-archived sibling. With no sibling, a conversation receives a fresh draft in the same scope and a project clears the visible workspace; the app never leaves a hidden archived row as the active context.</li><li><strong>Sort</strong> offers Recently updated (<code>recent</code>), Created date (<code>created</code>), Oldest first (<code>oldest</code>), and Name (<code>name</code>). Missing/invalid values fall back to <code>recent</code>. Pressing a project title and moving 8px, or ArrowUp/ArrowDown on that focused title, switches project ordering to <code>manual</code> and persists a contiguous order per normalized path. Archived and pinned priority remains ahead of the manual order; projects without an assigned order fall back to a stable path order until they are moved.</li><li>Each project group shows the ten most-recent rows in the active sort order by default; the remaining sessions fold behind a <strong>Load N more…</strong> control (the same affordance used for time-grouped overflow). Selecting it expands the full time-grouped list, and the expanded state is per-group, for the current session only, and not persisted.</li><li>Presentation changes are saved best-effort. Storage failure must not block project activation, session selection, or agent execution.</li></ul><h4 id="session-isolation-across-tabs" tabindex="-1">Session isolation across tabs <a class="header-anchor" href="#session-isolation-across-tabs" aria-label="Permalink to &quot;Session isolation across tabs&quot;">​</a></h4><ul><li>Selecting a row immediately marks that destination as selected. A 120ms pointer hover or keyboard focus may prefetch its transcript; duplicate reads share one in-flight request and the renderer retains at most five recent transcript snapshots.</li><li>A transcript window that reports no messages for a session the sidebar counts as having history is read as unreadable, not as empty (<strong>D615</strong>, issue #795): the selection asks once more, then keeps the snapshot the user already has, and otherwise reports <code>chat.sessionTranscriptEmpty</code> instead of committing an empty transcript. Such a page is never cached, so a hover prefetch cannot re-serve emptiness on every later open.</li><li>Transcript loading starts without waiting for an older superseded selection. When session summary metadata is available, project activation/clearing and transcript IO run in parallel. A monotonic navigation generation permits only the newest selection to project the visible workspace, transcript, run state, navigation history, and work-panel context.</li><li>The chat surface retains one pane per session, keyed by session id and bounded to three (the visible pane plus the two most recent). Hidden panes stay mounted and inert — <code>visibility: hidden</code> plus <code>content-visibility: hidden</code>, never <code>display: none</code>, which would discard their scroll offset — and each pane keeps its own scroll position for its lifetime. Switching to a session that still has a pane (warm) reveals it immediately with its retained content and position: nothing is dimmed, no skeleton appears, and no transcript remounts. If the destination is running or still holds a completed reply the durable page has not caught up to, revalidation treats the durable read as a lower-water mark and keeps its renderer-owned assistant/tool tail; completed durable rows may be added, but the partial or just-finished reply cannot be rolled back. The bounded durable page is stitched onto the live snapshot in chronological order: live rows older than that page stay before it, and an optimistic, streaming, or not-yet-flushed tail stays after it. Live-only rows are never appended after the page, which would move the newest turn out of the mounted trailing window (D317 / D261 / D324). Live provenance is cleared only once that page already contains every live row.</li><li>Switching to a session with no retained pane (cold) leaves the visible pane on its own session until the destination commits. Only a thin progress track and <code>aria-busy</code> mark the wait, the composer stays non-interactive so a prompt cannot reach the session being left, and the destination session id is never paired with another session&#39;s messages. The destination is then revealed at its final record without a top-of-history or empty-home flash. An evicted session is indistinguishable from a first visit.</li><li>New Task is not a cold switch. Creating a session reveals the empty home on the first frame (the previous conversation and retained panes clear before <code>session.create</code>). Reusing the group&#39;s latest empty session commits that empty transcript on the same frame rather than waiting for <code>session.get</code>. The durable row is inserted from the <code>session.create</code> summary; send and paste wait for that in-flight create instead of opening a second slot (ADR 0154).</li><li>A first-opened session settles at its newest turn. A revisited pane returns to the offset the user left, and a pane still pinned re-anchors to the bottom; activation no longer resets manual-scroll state for a revisit (ADR 0137). History continuation (D269) does not page earlier rows from a collapsed scroller or from a pinned overflowing transcript whose <code>scrollTop</code> has been reset to 0; a real gesture in the near-top band still continues history. An empty first paint does not spend the first-commit hydration gate, so a later long page is still bounded and re-bottomed in the layout phase, before the browser paints it.</li><li>Selecting a project-scoped conversation activates its project as part of the store-owned selection transaction. Selecting a Temporary conversation clears the visible workspace. Project-scoped new-session actions pass their target path to that same store transaction; sidebar and project-index handlers do not perform a second project navigation before session creation or selection.</li><li>Run state, permission grants, and streamed events are keyed by session id. A project/tab switch does not abort a background turn or copy its events into the visible transcript. Background message and tool events update that session&#39;s renderer-owned live cache, so reopening a running session does not lose the partial tail when its durable detail read completes. Transcript revalidation and older-page prepends are idempotent by message id and keep the last version at the first row position, so reopening or a stale page response cannot add a second copy of a user message. These events never activate their session, change the visible project/page, or move focus. Creating a new session or switching to one that is not running returns the composer to its idle Send state on that first frame: a turn still streaming in the previously selected session never leaves the destination session&#39;s send button stuck in the Abort/stop state, and that background turn&#39;s later completion does not alter the destination composer. Their work-panel artifacts and Browser resource update only the originating session&#39;s retained renderer context and do not reveal or resize the visible panel. Only an explicit session/notification activation navigates and projects the destination session&#39;s retained panel context.</li><li>The composer draft is also session-scoped in renderer memory (D301): the cache outlives any one Composer mount, so switching sessions, empty-home ↔ docked, chat ↔ other pages, or OS windows saves/restores the source text and file references. An uncached destination starts empty, and the home composer has its own draft slot. Creating a new session does not copy another slot. A completed send clears only the draft belonging to the session that submitted it, even if the user switches sessions while the request is in flight; deleted sessions cannot retain drafts.</li><li>Every tool call resolves <code>workspaceRoot</code> from the originating durable session, not from the currently selected project tab. Background completion refreshes the matching row without redirecting the active conversation.</li></ul><h4 id="focus-and-semantics" tabindex="-1">Focus and semantics <a class="header-anchor" href="#focus-and-semantics" aria-label="Permalink to &quot;Focus and semantics&quot;">​</a></h4><ul><li>Project directory rows expose <code>aria-expanded</code> and <code>aria-controls</code>; new-project/new-session controls have scope-specific accessible names, and sort/archive menu choices expose their checked state. Active session rows retain <code>aria-current</code>.</li><li>Toggling disclosure or a menu action keeps focus on its control. Selecting a project/session returns focus to the composer after loading.</li><li>Sort, archive, restore, pin, Create branch, and close actions remain keyboard-reachable; they cannot exist only as pointer-hover affordances.</li><li>Sidebar body-level menus opened from toolbar or row triggers remain content-sized and use the same fixed rule as right-click menus: open 4px to the anchor&#39;s right without flipping to the left. Their surface width is capped for narrow viewports. This includes the Sessions sort menu, session/project overflow menus, and section create menus.</li></ul><h4 id="floating-dropdown-surfaces" tabindex="-1">Floating dropdown surfaces <a class="header-anchor" href="#floating-dropdown-surfaces" aria-label="Permalink to &quot;Floating dropdown surfaces&quot;">​</a></h4><ul><li>Every renderer-owned custom dropdown/menu opens as a viewport-fixed floating layer, outside its triggering row or card, so opening it never changes parent height, width, or scroll allocation.</li><li>Shared anchored menus are measured before reveal, clamp to the viewport, prefer the requested side, and recalculate on anchor movement, scroll, and resize. Outside press and Escape close the surface and restore focus to its trigger unless the pattern explicitly retains input focus.</li><li>Native <code>&lt;select&gt;</code> popups remain platform-owned; this rule covers custom renderer surfaces only.</li><li>Pointer-anchored context menus (transcript rows, conversation background, markdown links) are the same family: they portal to <code>document.body</code> as a viewport-fixed layer, measure before reveal so they never flash at the origin, clamp inside the viewport instead of flipping, and close on outside press, Escape, Tab, window blur, or a scroll of anything behind them. An empty item list never opens a surface.</li></ul><h3 id="_1-6-local-profile-footer" tabindex="-1">1.6 Local profile footer <a class="header-anchor" href="#_1-6-local-profile-footer" aria-label="Permalink to &quot;1.6 Local profile footer&quot;">​</a></h3><ul><li>The <code>44px</code> profile trigger toggles the menu; its chevron and <code>aria-expanded</code> state change together.</li><li>The <code>280px</code> menu opens <code>8px</code> above the transparent footer band. Opening it moves focus to the first actionable row after the non-interactive identity header and divider.</li><li><code>ArrowDown</code> / <code>ArrowUp</code> wrap among Settings, Logs, and Theme. <code>Home</code> and <code>End</code> move to the first and last action.</li><li><code>Escape</code> closes the menu and restores focus to the profile trigger. A pointer press outside closes it without stealing focus from the pointer target.</li><li>Selecting Settings, Logs, or Theme closes the menu before performing the action. Theme applies the next theme value without reopening the menu.</li><li>The separate <code>32px</code> Help button bypasses the profile menu and navigates directly to Settings → Info.</li><li>Collapsing the sidebar closes the menu and restores the collapsed rail&#39;s normal navigation state.</li></ul><h3 id="_1-7-notification-inbox-d117" tabindex="-1">1.7 Notification inbox (D117) <a class="header-anchor" href="#_1-7-notification-inbox-d117" aria-label="Permalink to &quot;1.7 Notification inbox (D117)&quot;">​</a></h3><h4 id="event-to-surface-flow" tabindex="-1">Event-to-surface flow <a class="header-anchor" href="#event-to-surface-flow" aria-label="Permalink to &quot;Event-to-surface flow&quot;">​</a></h4><ol><li>Renderer reports the current chat&#39;s session id to Electron Main; navigating away clears it. Main combines this hint with its own window visibility and focus state when a turn reaches <code>completed</code> or <code>error</code>.</li><li>If the exact finishing session is already visible in the focused window, <code>session.endTurn</code> closes the turn without inserting a notification. Any background session or unfocused/hidden window creates the durable record. An <code>aborted</code> turn never creates one.</li><li>Electron emits <code>notification.changed</code> to every live renderer so the bell badge and currently open inbox refresh.</li><li>For a task result, a focused main window produces no native banner. If it is unfocused and native notifications are supported, Electron shows one platform notification derived from the event kind and session title. The separate interactive ask/permission/plan path may alert for a focused background session while suppressing the exact visible session. On Windows, the banner is attributed to the canonical PI-Desktop AppUserModelID shared with the NSIS package and taskbar identity.</li><li>Clicking the native notification shows/restores and focuses the main window, then emits <code>notification.activated { sessionId }</code>.</li><li>Renderer activation selects the bound project when present, loads the session, and focuses the transcript/composer using the same path as an inbox row click. Native and in-app activation must not diverge.</li></ol><h4 id="popover-behavior" tabindex="-1">Popover behavior <a class="header-anchor" href="#popover-behavior" aria-label="Permalink to &quot;Popover behavior&quot;">​</a></h4><ul><li>The list shows <code>task.failed</code> rows only and the bell badge counts only unread failures. <code>task.completed</code> rows are still persisted and still drive the sidebar outcome badge and the native notification, but the inbox hides them so failures are not buried under routine completions (D295).</li><li>Bell click toggles the non-modal popover; a second click, Escape, or outside press closes it. Escape restores focus to the bell.</li><li>Opening preserves the most recently selected <code>All</code> / <code>Unread</code> filter for the current renderer lifetime and never marks rows read implicitly.</li><li>Arrow keys move through rows with wrap disabled; <code>Home</code> / <code>End</code> jump to the first/last row; Enter/Space marks the row read and activates its session.</li><li>Mark all read updates every unread row in one host transaction. Clear removes all inbox rows in one host transaction. Both operations are idempotent, refresh the exact unread count, and leave sessions/turns intact.</li><li>The renderer does not synthesize notification records from stream events. Host-core&#39;s unique <code>turn_id</code> is the exactly-once boundary across repeated terminal updates, renderer reloads, and process restarts.</li><li>All visible event labels and native title/body strings are localized at the presentation boundary from structured fields; persisted rows never contain localized prose.</li></ul><h3 id="_1-8-work-panel-entry-and-resources-d128-d142-d154-d173-d179-d207-d221" tabindex="-1">1.8 Work panel entry and resources (D128, D142, D154, D173, D179, D207, D221) <a class="header-anchor" href="#_1-8-work-panel-entry-and-resources-d128-d142-d154-d173-d179-d207-d221" aria-label="Permalink to &quot;1.8 Work panel entry and resources (D128, D142, D154, D173, D179, D207, D221)&quot;">​</a></h3><ul><li>The shell starts without a visible work panel. The viewport-fixed toggle and <code>Cmd/Ctrl + J</code> both toggle the active session&#39;s panel: they reveal the retained context without creating a resource tab, and collapse the visible panel without deleting tabs, retaining tabs, active resource, and committed width. They are a no-op without an active session or while Settings is the active page. The panel&#39;s <code>+</code> trigger can then create a New launcher tab whose body offers Browser or an in-scope plugin view.</li><li>An artifact trigger atomically creates or reuses its resource, activates it, and opens the panel. Background artifacts never open the visible panel.</li><li>File resources use normalized paths as identity. Browser and plugin views are singletons; repeated triggers preserve resource order and activate the existing resource.</li><li>Once open, the panel header is a <code>tablist</code> that scrolls horizontally while a tight <code>+</code> trigger stays fixed beside it. Each tab owns its active state and close button; the active tab is scrolled into view. Clicking <code>+</code> creates a unique New launcher tab; its data-driven Review, Files, Browser, and plugin view rows are ordinary buttons in the page body.</li><li>Tab focus uses roving <code>tabIndex</code>: ArrowLeft/ArrowRight/Home/End move across tabs and Delete/Backspace closes the focused tab. Middle-click closes a tab; closing an active tab selects the right neighbor, then the left. Selecting a launcher row replaces that New tab with the destination or activates its existing singleton. Shortcut labels appear only for bindings that actually exist.</li><li>Activating a tool that is already open activates its existing resource instead of replacing it, so Browser keeps its URL and Files its selection (D173).</li><li>Every resource can be closed from its tab. Closing the active resource selects the right neighbor, then the left; closing the final tab keeps the panel open on the New launcher. The viewport-fixed panel toggle hides the panel without deleting tabs.</li><li>On every platform, opening and collapsing the visible panel change only the internal flex allocation; native window bounds remain unchanged. The inner divider updates the renderer-owned panel target from 244px upward, capped by the live three-column budget, while native window edges resize only the fixed application window (ADR 0151).</li><li>No tool result creates or activates a work-panel tab. Review opens only from an explicit user action — its <code>+</code> launcher row, or the retained context the viewport-fixed toggle and <code>Cmd/Ctrl + J</code> reveal — so a successful workspace Write/Edit never takes the panel away from what the user was reading. Failed and scratch writes behave the same. Background-session events update only their retained context and never open, activate, resize, focus, or change the visible panel.</li><li>Each successful workspace Write/Edit tool result carries one durable review snapshot. Its compact InlineReviewCard is rendered in the same activity disclosure, immediately after its tool row; it is never moved to the transcript bottom and never shared with another session. Its status badge covers added, modified, and deleted changes, while counts and expandable hunks come from that message&#39;s result, not a current Git diff.</li><li>The transcript cards and Review consume the active session&#39;s persisted message history. A commit, workspace focus change, or external Git state change cannot remove or rewrite an old card. Review is a chronological snapshot history and each reversible card exposes host-guarded rollback; conflicts are reported without replacing a later edit. Scratch, failed, denied, and unstructured results do not render a card. A background session&#39;s card remains with its own transcript and becomes visible only after that session is selected; its event never renders in the currently visible session. A successful workspace artifact cannot create or activate the singleton Review tab; it appears only after the user opens it.</li><li>Each session retains <code>{open, tabs, activeTabId, browserResource}</code> in renderer memory. Selecting another session swaps the visible context atomically and switching back restores it; selecting a workspace without an active conversation hides the panel. Session/workspace identity remains attached to every relative resource, preventing cross-context reinterpretation.</li><li>Relaunch discards every session context, including Browser resources; only the committed preferred panel width persists. Native window state is stored independently from normal bounds, including when the app closes while maximized or before a pending bounds-save debounce completes. Panel width remains fixed rather than being responsively clamped.</li></ul><h3 id="_1-9-application-updates-d120" tabindex="-1">1.9 Application updates (D120) <a class="header-anchor" href="#_1-9-application-updates-d120" aria-label="Permalink to &quot;1.9 Application updates (D120)&quot;">​</a></h3><ul><li>Electron Main checks the fixed release feed 15 seconds after packaged app startup and every 6 hours afterward. Development builds remain disabled. The checker always tracks GitHub&#39;s latest stable release (<code>allowPrerelease = false</code>), so installs that still carry a prerelease version such as <code>0.2.0-rc.6</code> are offered the newer stable tag instead of staying pinned to the same prerelease channel.</li><li>Settings → Info and application-menu checks share one typed update state. Manual checks expose up-to-date or error feedback; automatic failures do not open a toast or ambient banner.</li><li>Manual delivery (non-AppImage Linux and Windows ZIP runs, or legacy Windows portable runs with <code>PORTABLE_EXECUTABLE_FILE</code>) stops at <code>available</code> and offers the fixed GitHub Releases page. In-app delivery (packaged macOS, Windows NSIS, and Linux AppImage) automatically advances through <code>downloading</code> to the stable <code>downloaded</code> state.</li><li><code>downloaded</code> remains actionable until Restart to update or normal app quit; later scheduled/manual checks do not replace it with <code>checking</code>.</li><li>A compact update notice appears in the main pane&#39;s top-right safe area only for manual <code>available</code>, in-app <code>downloading</code>, or <code>downloaded</code>. It stays clear of the bottom composer at every supported window size and draft height. The notice uses a stable icon/title/message hierarchy, shows determinate download progress when available, and keeps the relevant action inside the same surface. Dismissal suppresses the current version-and-status stage; a later stage such as <code>downloaded</code> appears again.</li><li>When Main attaches localized product notes for the discovered version (<code>UpdateState.releaseNotes</code>, D164), the notice and Settings → Info Updates row show a compact &quot;What&#39;s new&quot; list under the status message. Notes come from the shipped-locale changelog catalog selected by the product UI locale — never from a renderer-supplied feed or remote URL. Missing catalog entries omit the section; locale changes re-resolve notes without a new check.</li><li>Settings → Info keeps a Release notes action available in every updater state. It opens a modal over Settings with the complete local stable changelog in newest-first order, localized from the same shared catalog. The current release and a discovered available release are identified with compact badges. The list scrolls independently, closes by its close control, Escape, or the backdrop, and restores focus to the invoking control.</li><li>D126 tag releases publish all platform manifests and installers. Packaged macOS, Windows NSIS, and Linux AppImage use the in-app lane; Linux deb/rpm and Windows ZIP remain notify-and-link delivery modes.</li></ul><h2 id="_2-streaming-message-behavior" tabindex="-1">2. Streaming message behavior <a class="header-anchor" href="#_2-streaming-message-behavior" aria-label="Permalink to &quot;2. Streaming message behavior&quot;">​</a></h2><h3 id="_2-1-token-rendering" tabindex="-1">2.1 Token rendering <a class="header-anchor" href="#_2-1-token-rendering" aria-label="Permalink to &quot;2.1 Token rendering&quot;">​</a></h3><ul><li>Tokens append to the current assistant MessageBubble as they arrive</li><li>Renderer displays runtime stream chunks directly; it does not enqueue a second requestAnimationFrame-driven typewriter state loop</li><li>Rendering uses incremental markdown parse — do not re-render the entire message on each token</li><li>Transcript reconciliation keeps completed history in a memoized history boundary; token updates do not reconcile each historical row in React while preserving the full history for selection, copying, minimap anchors, and accessibility.</li><li>Within the active assistant turn, unchanged activity groups without Task delegations also keep their memoized boundary across text updates. Changed tool messages still render, and Task groups still receive later lifecycle status and completion timing updates from the same turn.</li><li>An unfinished <code>mermaid</code> fence remains a source code block. After its closing fence arrives, answer prose loads and renders the diagram only when it approaches the viewport; thinking disclosures always retain Mermaid source.</li><li>Diagram render failure or the 20,000-character / 500-edge safety limit keeps the source visible and copyable instead of failing the assistant turn.</li><li>Cursor indicator: subtle pulsing accent dot or line at the end of streaming content</li><li>Before the first assistant or tool event, the active turn shows one compact localized <code>Working…</code> status with elapsed time. When the runtime reports a quiet interval, that same row names the wait: starting, waiting for the model, preparing the next request, compacting context, recovering an empty response, retrying a provider request, or waiting for delegated work (with each running subagent&#39;s latest coarse action). Existing thinking, tool, or answer output does not hide the row: the running turn keeps one tail status through output pauses. Pending permissions, questions, and plan/goal approvals suppress it; terminal turns and history reading have no live row.</li><li>When stream completes: cursor indicator replaced by success state (2s fade)</li></ul><h3 id="_2-2-auto-scroll" tabindex="-1">2.2 Auto-scroll <a class="header-anchor" href="#_2-2-auto-scroll" aria-label="Permalink to &quot;2.2 Auto-scroll&quot;">​</a></h3><ul><li>Opening a session for the first time resets follow mode and positions the transcript at its last record before the browser paints its pane. Revealing a retained pane restores that pane&#39;s own follow state and offset instead: still pinned re-anchors to the bottom, scrolled up returns to the same offset. Neither path animates through history, and no pane may ever expose the transcript top or another session&#39;s scroll position.</li><li>A first open whose history exceeds the initial mount budget settles under an opaque skeleton veil (D287). The veil is in the same commit as the bounded first paint, covers the scroller but not the composer, and lifts only once the scroller&#39;s <code>scrollHeight</code> and <code>clientHeight</code> have read the same for three consecutive frames, or after a 600ms cap. Every sampled frame re-pins a pinned transcript, so the frame the veil reveals is already at the newest turn. The minimap and the jump control mount after the veil lifts. Short transcripts never show the veil.</li><li>Auto-scroll to bottom on each new token group (throttled: check every 100ms, not every token)</li><li>The first upward manual scroll movement pauses auto-scroll immediately and cancels any pending follow frame; small trackpad deltas must not snap back to the bottom</li><li>Sending a new prompt, retrying, or regenerating always re-pins follow mode and jumps to the bottom before the turn continues, even if the user had scrolled up</li><li>&quot;Scroll to bottom&quot; floating button appears as soon as manual upward scrolling releases follow mode</li><li>Click &quot;Scroll to bottom&quot; button: resumes auto-scroll and snaps to bottom</li><li>Stream completion: if user was auto-scrolling, keep at bottom; if manual, stay at position</li><li>An asynchronously completed diagram height update follows the same rule: ResizeObserver keeps a pinned transcript at the bottom, while a user who has scrolled upward remains at their reading position.</li></ul><h3 id="_2-4-active-turn-surface" tabindex="-1">2.4 Active turn surface <a class="header-anchor" href="#_2-4-active-turn-surface" aria-label="Permalink to &quot;2.4 Active turn surface&quot;">​</a></h3><ul><li>An active turn keeps the lower transcript surface clear. Streamed assistant and tool rows remain inline with the transcript; no generic understanding, working, or checking card is rendered underneath them. A compact runtime status row remains in the reserved tail lane for the running turn. It shows the runtime phase when known, otherwise Planning/Goal or Working. Text and tool rows can stop changing while the turn remains active; their presence must not suppress that feedback. User-interaction waits suppress the row.</li><li>A permission card remains visible only when the agent is blocked on an explicit approval. It is an actionable interruption, not a progress status card.</li><li>Background sessions continue without adding progress chrome to the visible session or moving focus. Reduced motion therefore has no progress-card transitions to preserve.</li></ul><h3 id="_2-5-turn-outcome-closure" tabindex="-1">2.5 Turn outcome closure <a class="header-anchor" href="#_2-5-turn-outcome-closure" aria-label="Permalink to &quot;2.5 Turn outcome closure&quot;">​</a></h3><ul><li>A failed visible turn without a structured assistant error renders one session-scoped recovery card after the transcript content. It is based on the terminal agent event, not a timeout or a guessed spinner state. If the failed turn already contains a structured assistant error, that inline error card is the only failure surface and the session-scoped recovery card is omitted; users must not see duplicate failure summaries for one turn. Completed turns do not add a success card; their existing transcript and message-scoped review cards remain the completion evidence.</li><li>Failure copy states that the existing work remains available. The applicable failure surface has exactly one <strong>Continue</strong> action and no <strong>Regenerate</strong> action. Continue appends the current locale&#39;s continuation prompt (<code>Continue the user&#39;s unfinished task.</code> / <code>继续用户未完成的任务</code>) to the same session and starts a new turn without truncating the failed turn or its completed work.</li><li>Aborted turns do not render a failure card. Starting a new turn clears the previous card, and background-session results remain scoped until that session is selected.</li></ul><h3 id="_2-3-stream-interruption" tabindex="-1">2.3 Stream interruption <a class="header-anchor" href="#_2-3-stream-interruption" aria-label="Permalink to &quot;2.3 Stream interruption&quot;">​</a></h3><ul><li>If connection drops mid-stream: show error state on partial message</li><li>Partial message is preserved — not deleted</li><li>User sees &quot;Stream interrupted&quot; with retry option</li></ul><h2 id="_3-abort-running-agent" tabindex="-1">3. Abort running agent <a class="header-anchor" href="#_3-abort-running-agent" aria-label="Permalink to &quot;3. Abort running agent&quot;">​</a></h2><h3 id="_3-1-trigger-methods" tabindex="-1">3.1 Trigger methods <a class="header-anchor" href="#_3-1-trigger-methods" aria-label="Permalink to &quot;3.1 Trigger methods&quot;">​</a></h3><ul><li>Topbar abort button (visible during running state)</li><li>Keyboard shortcut: <code>Cmd/Ctrl + .</code></li></ul><h3 id="_3-2-abort-behavior" tabindex="-1">3.2 Abort behavior <a class="header-anchor" href="#_3-2-abort-behavior" aria-label="Permalink to &quot;3.2 Abort behavior&quot;">​</a></h3><ol><li>Cancel the current agent turn immediately</li><li>Cancel any pending permission request (per <a href="./03-permission-ux">03-permission-ux.md</a> §7)</li><li>If no assistant text, thinking, or tool row has begun, remove the just-sent user row and restore its pre-serialization composer draft</li><li>The restored draft keeps ordinary text and file-reference chips as separate state; serialized canonical paths never occupy the textarea</li><li>If a reply has begun, preserve the user turn and partial assistant/tool rows with aborted status and restore no draft. Preserve the measured stream duration and use provider output usage when available; otherwise store a visibly estimated output count so the conversation still shows throughput</li><li>Composer re-activates (unblocked)</li><li>Abort is idempotent — pressing abort when already aborting does nothing</li></ol><h3 id="_3-3-abort-ux" tabindex="-1">3.3 Abort UX <a class="header-anchor" href="#_3-3-abort-ux" aria-label="Permalink to &quot;3.3 Abort UX&quot;">​</a></h3><ul><li>Abort button changes to &quot;Aborting...&quot; briefly (100ms), then disappears</li><li>No confirmation dialog for abort — it is always immediate</li><li>A partial aborted message gets a muted &quot;(aborted)&quot; suffix. Only the unanswered smart-stop branch deletes its just-sent user row.</li></ul><h3 id="_3-4-queued-send" tabindex="-1">3.4 Queued send <a class="header-anchor" href="#_3-4-queued-send" aria-label="Permalink to &quot;3.4 Queued send&quot;">​</a></h3><ul><li>While a session is running, the composer shows Send when the draft has content and Stop when it is empty. Normal Send and Enter-to-send are follow-up actions. Accepted follow-ups clear the composer and append to that session&#39;s Host-owned, persisted FIFO queue; session switching never moves or clears another session&#39;s queue.</li><li>The queue renders above the composer. Each row has an independently keyboard-reachable Remove action and a Send now action once Host admission returns a durable id. While admission is pending, row actions are disabled with Saving tooltips and a Saving label on Send now; edit/remove leave both the queue and composer draft unchanged.</li><li>Send now moves its row to the head and requests the new <code>agent/stop</code> channel. The current assistant response and completed tool batch finish normally; after <code>agent_end</code> and durable turn finalization, the promoted row is dispatched through the normal <code>agent/prompt</code> flow before the remaining rows. An idle Send now dispatches immediately.</li><li>Without Send now, the next FIFO row starts automatically after the active turn completes, fails, or is aborted. A terminal event can arrive before persistence releases the session; finalization must wake the queue again after releasing ownership. No additional send or session switch is required.</li><li>Abort remains immediate and never clears the queue. Queued prompts survive application restart and remain held until a controller attaches (ADR 0213). Finalization during application shutdown must not start another queued turn.</li></ul><h3 id="_3-5-steer-the-current-turn" tabindex="-1">3.5 Steer the current turn <a class="header-anchor" href="#_3-5-steer-the-current-turn" aria-label="Permalink to &quot;3.5 Steer the current turn&quot;">​</a></h3><ul><li><code>Alt+Enter</code> submits the visible draft to the current turn immediately. On macOS this is <code>Option+Enter</code>. It works with Enter-to-send on or off and takes precedence over an open autocomplete menu. An idle composer sends normally.</li><li><code>Shift+Enter</code> and <code>Alt+Shift+Enter</code> insert a newline. An Enter confirming an IME candidate (<code>isComposing</code> or key code 229) never sends or steers.</li><li>Steering appears as a user message in the current transcript, clears the draft immediately, and reaches the next model request after the current response/tool batch. It creates no FIFO row and does not interrupt tools.</li><li>Submission captures the session and current turn identity. If that target ends, rejects input, or is awaiting approval, the draft is restored in its own session and a concise error is shown. New text typed after submission takes precedence over restoration. The running turn is not marked failed.</li><li>File/image chips use the existing attachment checks and the active model&#39;s capability. Queued configuration changes apply to the next ordinary turn; steering keeps the current configuration and sends slash-prefixed text literally, without dispatching local mode or extension commands.</li><li>Stop retains all accepted steering input as history. Smart Stop does not remove the latest steering row or restore the original prompt over it, including after renderer reload. The persisted message marker is the source of truth; the renderer does not keep a separate steering registry.</li><li>The Send tooltip identifies follow-up and uses the platform&#39;s key labels for the steering shortcut (<code>⌥+Enter</code> on macOS). The existing single Send/Stop slot and Host-owned follow-up list are retained.</li></ul><h2 id="_3a-context-checkpoint-lifecycle" tabindex="-1">3A. Context checkpoint lifecycle <a class="header-anchor" href="#_3a-context-checkpoint-lifecycle" aria-label="Permalink to &quot;3A. Context checkpoint lifecycle&quot;">​</a></h2><ul><li><code>turn_end</code> marks one completed model/tool turn and may be followed by another provider request. It never re-enables the composer or session configuration.</li><li>Automatic context protection evaluates after every <code>turn_end</code> and runs inline: the user waits for it. The model can also ask for it early through <code>new_context</code>, which lands at the same boundary.</li><li><code>compaction_start</code> keeps the session running. Threshold and overflow <code>compaction_end</code> events remain inside the active run; only <code>agent_end</code> or <code>error</code> settles it. A manual-only checkpoint settles on <code>compaction_end</code>.</li><li>Every successful compaction shows one warning toast: earlier detail is gone, and starting a fresh session is a decision only the user can make. The three more specific toasts stay on top of it — a successful manual <code>/compact</code> result, a warning before an overflow retry, and the fallback warning below.</li><li>If automatic summary generation fails but a retained-tail checkpoint is persisted, <code>compaction_end.fallback = &quot;retained_tail&quot;</code> shows one warning toast and the active run continues with reduced historical context.</li><li>Manual failure shows one error toast. Automatic hard/overflow failure does not duplicate the assistant error with a toast; the terminal error remains attached to the failed turn.</li><li>Compaction never removes visible transcript messages. The checkpoint affects only future model context and survives session switching/restart.</li><li>Each compaction adds one divider row to the transcript, immediately after the last message it covers, reading how many times the session has compacted and the summary&#39;s estimated token cost (or that no summary was generated). The row has no actions and is not selectable.</li><li>The context usage inspector keeps one muted line for the newest checkpoint, shown while its panel is open — the count and summary cost sit below the compact model/tool usage summaries without adding explanatory copy.</li></ul><h2 id="_4-long-content-collapse-expand" tabindex="-1">4. Long content collapse / expand <a class="header-anchor" href="#_4-long-content-collapse-expand" aria-label="Permalink to &quot;4. Long content collapse / expand&quot;">​</a></h2><h3 id="_4-1-collapse-thresholds" tabindex="-1">4.1 Collapse thresholds <a class="header-anchor" href="#_4-1-collapse-thresholds" aria-label="Permalink to &quot;4.1 Collapse thresholds&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Content type</th><th>Default state</th><th>Collapse threshold</th><th>Expand limit</th></tr></thead><tbody><tr><td>Assistant markdown message</td><td>Expanded</td><td>50 lines → collapsed to 20 lines visible</td><td>Full</td></tr><tr><td>Tool activity input</td><td>Row collapsed</td><td>Always behind disclosure</td><td>220px scroll region</td></tr><tr><td>Tool activity output</td><td>Row collapsed</td><td>Always behind disclosure</td><td>220px scroll region (per D033 host cap)</td></tr><tr><td>Bash output</td><td>Row collapsed</td><td>Always behind disclosure</td><td>220px scroll region</td></tr><tr><td>Error messages</td><td>Expanded</td><td>No collapse</td><td>—</td></tr></tbody></table><h3 id="_4-2-collapse-indicator" tabindex="-1">4.2 Collapse indicator <a class="header-anchor" href="#_4-2-collapse-indicator" aria-label="Permalink to &quot;4.2 Collapse indicator&quot;">​</a></h3><ul><li>Tool activity starts as a lightweight collapsed item row. Failed and denied calls keep their issue in the row header and do not auto-expand their payload.</li><li>Both modes give each loaded assistant turn one whole-process disclosure. It contains thinking, tools, hosted searches and intermediate progress text; the trailing answer, assistant errors and stopped trailing text remain outside it.</li><li>A contiguous activity segment receives a group disclosure only when it has at least two mode-visible items. Progress text ends the segment, a singleton uses its item disclosure directly, and compact-hidden thinking does not create a redundant group. Existing Task topology remains separate.</li><li>Detailed starts active and completed whole processes open. Its active ordinary group starts open and closes when it completes only if untouched; completed groups otherwise start closed. Compact starts the process and groups closed, except an untouched active process with any recorded failed/denied tool remains open through recovery and closes on completion.</li><li>In Detailed, leaf auto-open applies only when the literal final item of the last activity group is an eligible tool-call or hosted-search row. Failed/denied items stay closed, and a final thinking item never causes a backward scan. Compact keeps every item payload closed and hides reasoning text/excerpts while retaining its active thinking indicator.</li><li>Activating a process, group or item header toggles only that level. Closing a parent preserves child state, reopening restores it, and sibling groups remain independent. Opening a parent is never an expand-all action.</li><li>A manual item action claims its group and process as user-owned without toggling them. Streaming and completion cannot reopen a manual close or close around content the user opened, focused or selected. Choices survive mode changes, singleton-to-group growth and remounts while the retained session pane lives.</li><li>Search/navigation opens the process and activity group that own the named message, once per reveal request. Item-level targeting is not part of this change. Compact reasoning requires an explicit switch to Detailed. Closing search does not collapse the revealed path.</li><li>Pending permission, question, plan/goal approval and other action cards remain reachable outside hidden process content.</li><li>Each disclosure uses its own button, <code>aria-expanded</code> and <code>aria-controls</code>; closed descendants leave the tab and accessibility order. Reduced-motion disables non-essential marker and chevron animation.</li></ul><h3 id="_4-3-tool-result-truncation" tabindex="-1">4.3 Tool result truncation <a class="header-anchor" href="#_4-3-tool-result-truncation" aria-label="Permalink to &quot;4.3 Tool result truncation&quot;">​</a></h3><ul><li>Per D306 / D194: budgets are per tool class (see <a href="./../03-runtime/16-tool-result-limits">16-tool-result-limits</a>). Search/read results cap at 128KB / 4000 lines; Bash stdout/stderr cap at 96KB / 4000 lines with a spill file.</li><li>Read/Glob/Grep report <code>truncated: true</code> only when this result was cut short (budget, a clipped line, or remaining Grep/Glob hits). A filled Read window of a longer file is not truncated; <code>notice</code> names the next offset.</li><li>Bash markers name which end survived and the spill path, for example <code>[truncated: kept the first 4000 of 51234 lines; limit 4000 lines / 96KB. …]</code>.</li><li>Truncated content is never silently omitted — always marked</li><li>Disclosure expansion does not load content beyond the host-enforced cap</li><li>The collapsed-row <code>truncated</code> chip follows <code>details.truncated</code></li></ul><h2 id="_5-permission-interrupt-flow" tabindex="-1">5. Permission interrupt flow <a class="header-anchor" href="#_5-permission-interrupt-flow" aria-label="Permalink to &quot;5. Permission interrupt flow&quot;">​</a></h2><h3 id="_5-1-flow-sequence" tabindex="-1">5.1 Flow sequence <a class="header-anchor" href="#_5-1-flow-sequence" aria-label="Permalink to &quot;5.1 Flow sequence&quot;">​</a></h3><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Agent calls a permission-gated tool (including Plan/Goal Bash under Ask or Accept edits)</span></span>
<span class="line"><span>  → PermissionCard inserted inline in transcript</span></span>
<span class="line"><span>  → Composer disabled (cannot send new prompt)</span></span>
<span class="line"><span>  → Countdown starts (120s)</span></span>
<span class="line"><span>  → User responds: Allow once / Allow session / Deny</span></span>
<span class="line"><span>  → Card transitions to resolved state</span></span>
<span class="line"><span>  → Composer re-enabled</span></span>
<span class="line"><span>  → Agent continues or receives denial result</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><h3 id="_5-2-multiple-pending-permissions" tabindex="-1">5.2 Multiple pending permissions <a class="header-anchor" href="#_5-2-multiple-pending-permissions" aria-label="Permalink to &quot;5.2 Multiple pending permissions&quot;">​</a></h3><ul><li>Each session has at most one active permission card because that agent loop is paused; multiple sessions may wait independently.</li><li>Abort cancels only the active session&#39;s pending permission.</li><li>Timeout (120s from original receipt) auto-denies only the matching request; switching sessions never resets the deadline.</li></ul><h3 id="_5-3-focus-management-during-permission" tabindex="-1">5.3 Focus management during permission <a class="header-anchor" href="#_5-3-focus-management-during-permission" aria-label="Permalink to &quot;5.3 Focus management during permission&quot;">​</a></h3><ul><li>A visible permission card is announced through <code>aria-live</code> without forcing focus. A background session&#39;s card is not mounted and cannot move focus.</li><li>Action buttons are tab-reachable within the card</li><li>After resolution: focus returns to composer</li><li>Full spec: <a href="./03-permission-ux">03-permission-ux.md</a></li></ul><h2 id="_5a-plan-and-goal-workflow" tabindex="-1">5A. Plan and Goal workflow <a class="header-anchor" href="#_5a-plan-and-goal-workflow" aria-label="Permalink to &quot;5A. Plan and Goal workflow&quot;">​</a></h2><ol><li>The user selects Plan or Goal while the session is idle, or the same Agent calls <code>EnterPlanMode</code> / <code>EnterGoalMode</code>; the host persists/validates the matching contract mode and the renderer projects <code>planning</code>.</li><li>The Agent investigates with the selected contract tool set. Read/Glob/Grep and BrowserPreview are allowed; Bash follows the visible permission mode. A contract-mode Bash command may mutate under Auto, so the mode chip remains visible. While that turn is live <code>planning</code>, the Composer mode chip pulses and a compact Planning row occupies the same reserved tail slot as Working until completion or pending user interaction. A known runtime phase takes precedence; tool and answer output do not hide the running status.</li><li>The Agent calls <code>SubmitPlan</code> or <code>SubmitGoal</code> alone in its tool batch. Host-core preserves the exact Markdown bytes in a new immutable <code>.pi/plan/*.md</code> or <code>.pi/goal/*.md</code> artifact, records its path/hash/size and structured title/question, and the renderer displays the shared contract approval card with only the title and artifact opener; the question remains host-side contract data. The opener hands that path to the bundled file view when it is launchable and to the host file tab otherwise, so the artifact opens beside the conversation in the same view the user&#39;s other project files use (D452).</li><li>Approve requires Ask / Accept edits / Auto selection. The renderer remembers the last selected mode on this device and uses it as the next approval&#39;s default. Host-core commits the approval, <code>mode = agent</code>, permission mode, and <code>queued</code> state atomically; the same Agent continues on a fresh turn with Agent tools.</li><li>Reject stops the pending run and keeps the durable session in its contract mode. The live state returns to editable planning; revisions are new-turn <code>SubmitPlan</code>/<code>SubmitGoal</code> calls with a new complete Markdown snapshot and new artifact. Earlier snapshots remain immutable; there is no request-changes action.</li><li>Expiry, abort, persistence failure, renderer/host/sidecar crash, or stale response renders a failed-closed state. A host restart interrupts pending, queued, and running work without replay; an already-approved interruption keeps the session in Agent.</li></ol><p>The approval card is session-scoped. Background sessions may retain a pending approval or queued/running execution state in <code>plan_approvals</code>, but opening another session never covers it or moves focus; returning to the originating session restores the renderer-lifetime snapshot; while the host remains alive, <code>plans.pending</code> can rehydrate a still-pending row. The approval card does not expose a validity/deadline concept. Mode/provider/model/permission/shell configuration and new prompts remain disabled while an active <code>pending</code> approval or turn exists. During pending approval the existing draft remains in the textarea but is read-only; only Approve and Reject remain enabled on the approval surface. Reject, expiry, or interruption re-enables them; terminal proposal snapshots do not keep the gate closed. The renderer retains the latest checkpoint/execution status per session only for its current lifetime, so rejected, expired, interrupted, approved, queued, and running outcomes may remain visible across session switches. A renderer reload rehydrates only a pending row; terminal cards are dropped and are not restored. Host restart interrupts prior work without replay or stale action, and the UI is not required to present the interrupted terminal snapshot. The Composer-left Agent/Plan/Goal chip is the only active-session mode control.</p><p>During project or session initialization, the home composer can render before an <code>activeSessionId</code> is projected. Idle mode, Thinking, and permission controls remain usable in that interval; the durable empty session row is created or selected by the New Task action and the first configuration action applies to that session once it is projected. The startup-only home composer may still materialize a session when pasted input arrives before selection. Running turns and pending approvals continue to gate the controls.</p><h2 id="_6-toast-vs-inline-error" tabindex="-1">6. Toast vs inline error <a class="header-anchor" href="#_6-toast-vs-inline-error" aria-label="Permalink to &quot;6. Toast vs inline error&quot;">​</a></h2><h3 id="_6-1-toast-notifications-use-for" tabindex="-1">6.1 Toast notifications (use for) <a class="header-anchor" href="#_6-1-toast-notifications-use-for" aria-label="Permalink to &quot;6.1 Toast notifications (use for)&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Scenario</th><th>Toast type</th><th>Duration</th><th>Rationale</th></tr></thead><tbody><tr><td>Provider connection test result</td><td>Success/Error</td><td>4s/8s</td><td>Transient feedback, not blocking workflow</td></tr><tr><td>Plugin load/unload success</td><td>Success</td><td>4s</td><td>Confirmation of background action</td></tr><tr><td>Settings saved</td><td>Success</td><td>4s</td><td>Quick confirmation</td></tr><tr><td>Manual menu update check failure</td><td>Error</td><td>8s</td><td>Direct feedback for an explicit command</td></tr><tr><td>Context checkpoint completed</td><td>Info (Warning before overflow retry)</td><td>4s/8s</td><td>Confirms a background context transition without altering transcript rows</td></tr><tr><td>Manual context checkpoint failure</td><td>Error</td><td>8s</td><td>Direct feedback for explicit <code>/compact</code>; automatic terminal failures stay inline</td></tr></tbody></table><h3 id="_6-2-inline-errors-use-for" tabindex="-1">6.2 Inline errors (use for) <a class="header-anchor" href="#_6-2-inline-errors-use-for" aria-label="Permalink to &quot;6.2 Inline errors (use for)&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Scenario</th><th>Inline placement</th><th>Rationale</th></tr></thead><tbody><tr><td>Tool call failure</td><td>Error state on ToolCallCard</td><td>Context-dependent, user needs to see which tool failed</td></tr><tr><td>Permission denial</td><td>Resolved state on PermissionCard</td><td>Already inline, part of conversation flow</td></tr><tr><td>Stream interruption</td><td>Error state on MessageBubble</td><td>Belongs to the message that failed</td></tr><tr><td>Provider/model turn failure</td><td>Assistant error message in transcript</td><td>Keeps summary, stable code, redacted detail, and recovery action attached to the failed turn</td></tr><tr><td>Provider configuration validation error</td><td>Inline in settings form</td><td>User needs to see which field is wrong</td></tr><tr><td>Application update status/error</td><td>Settings → Info Updates row</td><td>Preserves the latest Main-owned state without interrupting background checks</td></tr><tr><td>Composer validation (no model)</td><td>Disabled state + tooltip on send button</td><td>Immediate context</td></tr></tbody></table><h3 id="_6-3-rules" tabindex="-1">6.3 Rules <a class="header-anchor" href="#_6-3-rules" aria-label="Permalink to &quot;6.3 Rules&quot;">​</a></h3><ul><li>Never use toast for errors that are tied to a specific message or tool call</li><li>Assistant error detail uses a keyboard-operable disclosure with <code>aria-expanded</code> / <code>aria-controls</code>; it is open on first render so the provider response is immediately discoverable, and supports copying the redacted text</li><li>Never use inline error for transient background operations (plugin load, connection test)</li><li>Toasts stack vertically, newest on top, at top-center</li><li>Error toasts require manual dismiss or timeout at 8s (longer than success)</li><li>Success toasts auto-dismiss at 4s</li></ul><h3 id="_6-4-icon-only-action-labels" tabindex="-1">6.4 Icon-only action labels <a class="header-anchor" href="#_6-4-icon-only-action-labels" aria-label="Permalink to &quot;6.4 Icon-only action labels&quot;">​</a></h3><ul><li>Every icon-only action exposes a localized purpose through both its accessible name and its hover/focus tooltip.</li><li>Use the shared <code>TooltipButton</code> for interactive buttons and <code>Tooltip</code> for non-button controls. Both render the themed tooltip in a body-level portal so it is not clipped by pane overflow or hidden below a neighboring surface. Native <code>title</code> remains for full-value metadata such as paths, IDs, and descriptions; rich hover cards and popovers keep their specialized surfaces.</li><li>Decorative icons remain <code>aria-hidden</code> and do not need a tooltip.</li><li>Tooltip text must describe the action, not the icon shape, and must come from the active i18n catalog.</li><li>Clicking an action dismisses its tooltip immediately and suppresses it until the pointer leaves or focus moves away; keyboard focus still reveals the tooltip before activation.</li><li>A tooltip is bound to one live trigger. It closes when that trigger unmounts or is detached, when the window loses focus, when the document is hidden, and on Escape; a trigger that moves in the DOM within a quarter second without being replaced keeps the tooltip instead of blinking it. A tooltip revealed by keyboard focus is not closed by unrelated pointer movement, and at most one themed tooltip is ever painted, so a pointer crossing between two adjacent buttons never shows both. The guard listeners behind this are shared by the whole renderer, so a long transcript does not add one listener set per row.</li></ul><h2 id="_7-focus-management" tabindex="-1">7. Focus management <a class="header-anchor" href="#_7-focus-management" aria-label="Permalink to &quot;7. Focus management&quot;">​</a></h2><h3 id="_7-1-focus-flow-on-page-load" tabindex="-1">7.1 Focus flow on page load <a class="header-anchor" href="#_7-1-focus-flow-on-page-load" aria-label="Permalink to &quot;7.1 Focus flow on page load&quot;">​</a></h3><ol><li>Composer textarea receives initial focus in main chat view</li><li>Settings pages: first interactive element receives focus</li><li>Command palette: search input receives focus on open</li></ol><h3 id="_7-2-focus-flow-after-actions" tabindex="-1">7.2 Focus flow after actions <a class="header-anchor" href="#_7-2-focus-flow-after-actions" aria-label="Permalink to &quot;7.2 Focus flow after actions&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Action</th><th>Focus target</th></tr></thead><tbody><tr><td>New session created</td><td>Composer textarea</td></tr><tr><td>Session switched</td><td>Composer textarea</td></tr><tr><td>Message sent</td><td>Composer textarea (cleared, ready for next)</td></tr><tr><td>Stream completed</td><td>Composer textarea (re-enabled)</td></tr><tr><td>Permission resolved</td><td>Composer textarea</td></tr><tr><td>Abort completed</td><td>Composer textarea</td></tr><tr><td>Command palette closed</td><td>Previously focused element</td></tr><tr><td>Dialog closed</td><td>Previously focused element</td></tr><tr><td>Notification popover closed with Escape</td><td>Notification bell</td></tr><tr><td>Notification row/native notification activated</td><td>Activated session composer after transcript load</td></tr></tbody></table><h3 id="_7-3-focus-trap" tabindex="-1">7.3 Focus trap <a class="header-anchor" href="#_7-3-focus-trap" aria-label="Permalink to &quot;7.3 Focus trap&quot;">​</a></h3><ul><li>Command palette: focus trapped within palette while open</li><li>Settings modals: focus trapped</li><li>Escape always closes the trapped surface and returns focus</li></ul><h3 id="_7-4-focus-ring-rules" tabindex="-1">7.4 Focus ring rules <a class="header-anchor" href="#_7-4-focus-ring-rules" aria-label="Permalink to &quot;7.4 Focus ring rules&quot;">​</a></h3><ul><li>Only show focus ring on <code>focus-visible</code> (keyboard focus), not on click/mouse focus</li><li>Focus ring: 2px accent color border, 2px offset from element edge</li><li>Per <a href="./07-ui-design-system">07-ui-design-system.md</a> §6.4</li><li>Never remove focus rings globally — accessibility requirement</li></ul><h3 id="_7-5-text-selection" tabindex="-1">7.5 Text selection <a class="header-anchor" href="#_7-5-text-selection" aria-label="Permalink to &quot;7.5 Text selection&quot;">​</a></h3><ul><li>Application chrome is non-selectable by default to prevent accidental selection while clicking or dragging the shell.</li><li>Editable controls (<code>input</code>, <code>textarea</code>, <code>select</code>, and <code>[contenteditable]</code>) preserve normal text editing and <code>Cmd/Ctrl+A/C/V</code> behavior.</li><li>Transcript prose, rendered Markdown, code blocks, and tool input/output remain text-selectable for inspection and copying.</li><li>Interactive controls nested inside selectable content remain non-selectable and must keep their click and keyboard behavior.</li><li>Selection rules must not disable <code>focus-visible</code> feedback or native window drag regions.</li></ul><h2 id="_8-drag-drop" tabindex="-1">8. Drag / drop <a class="header-anchor" href="#_8-drag-drop" aria-label="Permalink to &quot;8. Drag / drop&quot;">​</a></h2><h3 id="_8-1-mvp-status" tabindex="-1">8.1 MVP status <a class="header-anchor" href="#_8-1-mvp-status" aria-label="Permalink to &quot;8.1 MVP status&quot;">​</a></h3><p>Work-panel and application-window resizing are implemented in MVP:</p><ul><li><p>The 10px inner left-edge separator anchors to the press position and starting panel width, then follows pointer delta without jumping. Moving it left grows the panel until the shared budget is exhausted; when MainChat reaches its 450px minimum the expanded sidebar collapses immediately. Moving it right gives space back to MainChat.</p></li><li><p>The inner divider&#39;s target clamps to the shared three-column budget (<code>client width - 450px - expanded sidebar</code>, with no fixed pixel cap); pointer movement is frame-coalesced and release commits the preferred width. Escape, pointer cancellation, and lost capture restore the press-time panel width. A double-click restores the default 360px width inside those same live bounds.</p></li><li><p>Opening and closing animate the dock&#39;s <code>width</code> and <code>flex-basis</code> together with the bounded opacity/transform feedback, so MainChat reflows continuously inside the existing client area without crossing its 450px minimum instead of changing width before the first motion frame. While <code>sidebar-out</code> still occupies flex space, the shared budget continues to count the sidebar.</p></li><li><p>Reopening a sidebar the layout collapsed spends work-panel width first: the panel keeps its width while MainChat stays at or above 450px, and otherwise the reopen targets 460px. Closing the panel restores only a sidebar the layout collapsed; a manual collapse stays collapsed.</p></li><li><p>No panel action requests a positive native reservation: the preferred panel width is renderer-local, the native seam stays at zero, and native window edges resize only the fixed app window. Background-session artifacts never update the visible panel or window geometry.</p></li><li><p>The native Browser view still follows the renderer-measured panel rectangle; it is detached before collapse motion because it cannot participate in renderer CSS animation. Native bounds recovery and persistence continue to apply to ordinary window resize/move gestures without panel-specific deltas.</p></li><li><p>Preview mode unmounts MainChat and lets the work panel fill the client area beside the sidebar. A window-level 46px chrome row keeps New Task, sidebar, and native window controls available through a pointer-transparent row that declares neither drag nor no-drag across the panel. The panel header&#39;s drag border box starts after the shell actions plus an 8px gap, including expanded sidebar New Task. All platforms use an 8px left inset, except collapsed-sidebar windowed macOS (88px). That reserve uses <code>--ds-window-lead-inset</code>: the traffic-light cluster&#39;s 76px right edge (from <code>@pi-desktop/shared</code>) plus 12px. Native pointer clicks must operate the controls and dragging empty header space must move the window; DOM/CDP clicks alone do not establish native hit testing.</p></li></ul><p>The expanded sidebar is user-resizable from 240px to 520px (default 275px) via the right-edge handle. Pointer motion below 160px collapses the sidebar and keeps the preferred expanded width. Keyboard Arrow/Home/End resize without collapsing. Double-clicking the handle restores the 275px default inside the live budget.</p><p>Project ordering is implemented for retained project groups. There is no reorder grip. Pressing the project title and moving 8px starts a project drag, so a click still activates and toggles collapse, and menus and nested session rows keep their existing click behavior. A drop inserts before or after the target group based on the pointer position and persists the result.</p><p>Sidebar drag/drop is implemented:</p><ul><li>A session row is draggable while idle. Dropping it on another project group moves that session to the project: the host updates only the session&#39;s project association, and the transcript, attachments, tasks, revisions, artifacts, notifications, and scratch data stay with the session.</li><li>A running session is not draggable, and the session menu&#39;s project targets are not available. The host rejects the move as well, so a turn that starts mid-drag cannot leave the agent bound to the previous project&#39;s instructions.</li><li>The dragged row paints at opacity 0.5 and the eligible project group highlights with an accent outline. A session&#39;s own project group is not a drop target, so a same-project drag never issues a request.</li><li>Project reassignment is available through the drag/drop interaction only; the session context menu does not contain a project list.</li><li>Dropping a folder on the projects list adds it as a project, or switches to it when it is already known; duplicate paths resolve to one project row. A drop that carries no folder reports why nothing happened.</li></ul><p>Native file-system drops into the composer are implemented. The target uses an accent outline without changing layout; regular files use the session-scratch reference flow below. A dropped folder is never attached: it raises an explicit choice between opening it as a project and inserting the literal directory path into the draft, so an unknown directory tree cannot enter the context.</p><h3 id="_8-2-project-drag-drop-contract" tabindex="-1">8.2 Project drag/drop contract <a class="header-anchor" href="#_8-2-project-drag-drop-contract" aria-label="Permalink to &quot;8.2 Project drag/drop contract&quot;">​</a></h3><p>Project drag/drop follows these patterns:</p><ul><li>The project title is the reorder control: press and move 8px to arm a drag</li><li>A click with no qualifying movement still selects and toggles collapse</li><li>Touch does not start a reorder so the list can scroll</li><li>An accent insertion line shows before/after placement</li><li>Cancel drag with Escape</li><li>Drag feedback: opacity 0.5 on source</li><li>ArrowUp/ArrowDown on the focused title moves the project one row and persists the same manual order without requiring a pointer</li></ul><h2 id="_8a-composer-autocomplete-and-clipboard-files-d123–d125-d197-d209-d262-d362-d397-adr-0131-adr-0222" tabindex="-1">8a. Composer autocomplete and clipboard files (D123–D125, D197, D209, D262, D362, D397, ADR 0131, ADR 0222) <a class="header-anchor" href="#_8a-composer-autocomplete-and-clipboard-files-d123–d125-d197-d209-d262-d362-d397-adr-0131-adr-0222" aria-label="Permalink to &quot;8a. Composer autocomplete and clipboard files (D123–D125, D197, D209, D262, D362, D397, ADR 0131, ADR 0222)&quot;">​</a></h2><h3 id="_8a-1-triggers" tabindex="-1">8a.1 Triggers <a class="header-anchor" href="#_8a-1-triggers" aria-label="Permalink to &quot;8a.1 Triggers&quot;">​</a></h3><ul><li><code>/</code> opens command mode only when it is the first character of the input and the cursor is still inside that first token (no whitespace typed yet). A space after the command name closes the menu; arguments are free text.</li><li><code>@</code> opens file mode when the token containing the cursor starts with <code>@</code> and the character before <code>@</code> is start-of-input, whitespace, or one of the pi delimiters (<code>&quot;</code>, <code>&#39;</code>, <code>=</code>). The query is the text between <code>@</code> and the cursor; a query containing <code>/</code> matches across path segments. A quoted token (<code>@&quot;…</code>) is treated as one token until the closing quote.</li><li>Pasting text never opens a menu unless the caret lands inside a valid trigger token.</li><li>File results keep each row compact by rendering only the leaf name (with a trailing <code>/</code> for directories). The full relative path remains available as the row tooltip and accessible name. Accepting a file (Enter/Tab/click) replaces the <code>@</code> token with an inline sentinel-backed leaf-name chip at the caret, backed by the full <code>entry.path</code>; that confirmation does not send. Accepting a directory keeps the full literal path in the draft so deeper completion can continue.</li></ul><h3 id="_8a-2-reference-chips-and-clipboard-files" tabindex="-1">8a.2 Reference chips and clipboard files <a class="header-anchor" href="#_8a-2-reference-chips-and-clipboard-files" aria-label="Permalink to &quot;8a.2 Reference chips and clipboard files&quot;">​</a></h3><ul><li>Select non-whitespace <code>text/plain</code> over accompanying generated <code>image/*</code> copies only when every file lacks a native path (Word text selection). Native files, any non-image file, and image-only/whitespace-plus-image paste retain their attachment flow. This uses the existing preload file-path resolver and does not reread the system clipboard.</li><li>Selected text stays editable when its character count is at or below the persisted <code>largePasteThreshold</code> (default 600); larger text becomes a temporary session file reference. Small multiline paste preserves blank/trailing lines, surrounding text, caret, and native undo; CRLF/CR becomes editor LF. Literal HTML remains text: only escaped text and generated line breaks are inserted.</li><li>While bytes are being transferred, the textarea is read-only and exposes <code>aria-busy=&quot;true&quot;</code>; the send and autocomplete controls are disabled.</li><li>Electron main saves bounded bytes under the originating session&#39;s scratch root and returns unique absolute paths plus sanitized original leaf names. The composer leaves visible text unchanged, appends leaf-name reference chips in clipboard order, then restores the textarea selection and focus.</li><li>For an oversized text-only paste, the renderer sends the exact UTF-8 <code>text/plain</code> bytes through the same session bridge, inserts a sentinel-backed <code>pasted-text-*.txt</code> chip at the original selection, and keeps a token-to-canonical-path mapping in the draft. Clicking the chip or pressing Enter/Space reads the bounded text file and replaces the sentinel in place with editable text, removing the reference and placing the caret after the inserted content. A failed or unsupported read leaves the chip intact. Pasting in the middle of a draft keeps both the prefix and suffix intact.</li><li>If the home composer has no active session, it creates or reuses one before writing. Failure leaves the existing draft unchanged and shows the error in the normal toast surface.</li><li>A chip remove button removes only that draft reference and restores textarea focus; it does not eagerly delete session scratch bytes. Backspace on an empty textarea removes the most recent active reference.</li><li>A text/plain or <code>.txt</code> chip exposes button semantics and expands on click or Enter/Space. The read is bounded by the existing <code>fsRead</code> policy; binary, image, oversized, or failed reads show the normal error toast and preserve the chip.</li><li>A reference-only draft enables Send. Before dispatch, active references are appended after visible text and ordinary references are serialized with the canonical relative or absolute paths and existing whitespace quoting. Pasted references are submitted as structured attachments so the main process can choose visual input or the same path fallback from the exact model capability. Inline large-paste references are resolved in the visible draft instead of being appended or sent as duplicate attachments. Successful dispatch clears both; failed or rejected dispatch retains both. References are session-scoped and scratch references survive a workspace switch while their owning session remains available. Workspace <code>@</code> chips are relative to the project that produced them and are removed from the draft, sentinels included, when the workspace changes.</li><li>When an image reference is active, Composer shows one compact live status line. It names visual transport for a model whose pi-ai <code>input</code> includes <code>image</code>, and names the file-path fallback for unknown/non-vision models. The status is informational, keyboard-safe, and never relies on color alone.</li><li>A native file-system drop over the Composer prevents the browser&#39;s default file-open behavior and shows the same accent target outline for the whole shell. Regular files are read through the existing bounded paste bridge and become removable leaf-name chips in drop order. A dropped folder is not traversed or copied; its complete native path is inserted at the caret using the literal <code>@&lt;path&gt;/</code> directory form so the path remains visible; directory tokens without spaces can continue into <code>@</code> completion. Mixed file/folder drops preserve their order, and the draft/focus/caret are retained across the asynchronous file save.</li><li>Accepted dispatch retains an in-memory, session/turn-scoped copy of the visible text and structured references only while unanswered smart Stop can undo the send. That undo restores the original chip order and labels; it never parses serialized <code>@path</code> text. Once reply content begins, abort keeps the partial transcript and restores no draft.</li><li>After a successful send, the user bubble parses those serialized <code>@path</code> tokens back into composer-matching leaf-name chips for display only. The persisted message and model context stay canonical <code>@path</code> text. Clicking a chip completes the reference through <code>pi-desktop/fs/resolveRef</code>, which searches the whole open project — its group&#39;s folders, primary first (ADR 0263) — and then opens where it resolved: a project file in the bundled <code>pi.file-manager</code> work-panel view (the host <code>file:</code> tab when that view is not available), a session-scratch or attachment file in the host <code>file:</code> tab, and a <code>.html</code>/<code>.htm</code> page of the project&#39;s primary folder in the side browser, because the side browser is rooted at that folder. The address handed to the work panel follows the folder that answered: a file of the primary folder travels as a project-relative path, a file of a sibling folder of the same project as an absolute one, exactly as a scratch or attachment file does. A chip whose reference matches nothing opens nothing and reports itself; the OS default application is no longer what this click does, though that action stays reachable from the file view&#39;s own context menu.</li><li>The same destination rule governs every other surface of the transcript that names a file, because one opener serves them all: clicking the file path in a tool row&#39;s summary (Read, Write, Edit, fetch) and clicking a path in a tool result&#39;s file or match list both complete the reference the same way and open where it resolved (ADR 0262). A tool surface therefore picks no destination of its own, and a reference it cannot resolve reports itself instead of opening a panel.</li></ul><h3 id="_8a-3-keyboard-while-open" tabindex="-1">8a.3 Keyboard while open <a class="header-anchor" href="#_8a-3-keyboard-while-open" aria-label="Permalink to &quot;8a.3 Keyboard while open&quot;">​</a></h3><ul><li>↑/↓ move the highlight with wraparound; Home/End are left to the textarea.</li><li>Enter / Tab accept the highlighted item; Alt+Enter uses active-turn steering instead of accepting a suggestion. Enter and Cmd/Ctrl+Enter never send while the menu has a highlighted item (this precedes the Enter-to-send setting). otherwise keeps its behavior).</li><li>Escape closes only the menu — it takes precedence over the composer&#39;s &quot;clear input or blur&quot; Escape and must not propagate to overlay handlers.</li><li>Any other typing re-filters in place; zero matches behaves as closed.</li></ul><h3 id="_8a-4-ime-first-normative-ime-rules" tabindex="-1">8a.4 IME (first normative IME rules) <a class="header-anchor" href="#_8a-4-ime-first-normative-ime-rules" aria-label="Permalink to &quot;8a.4 IME (first normative IME rules)&quot;">​</a></h3><ul><li>All autocomplete key handling sits behind the standard guard (<code>isComposing || keyCode === 229</code>).</li><li>During active composition the trigger detector neither opens, updates, nor closes the menu; state re-evaluates on <code>compositionend</code>.</li><li>Enter that confirms an IME candidate never sends and never accepts a menu item; ↑/↓ during candidate navigation belong to the IME.</li></ul><h3 id="_8a-5-close-and-focus-rules" tabindex="-1">8a.5 Close and focus rules <a class="header-anchor" href="#_8a-5-close-and-focus-rules" aria-label="Permalink to &quot;8a.5 Close and focus rules&quot;">​</a></h3><ul><li>Close on: outside mousedown, textarea blur, deleting past the trigger character, session or workspace switch, accepting an item (except <code>@dir/</code> continuation, which keeps the menu open on the deeper query).</li><li>Focus stays in the textarea for the menu&#39;s whole lifecycle (input-retained overlay); the menu is never a focus trap and never steals the caret.</li></ul><h2 id="_9-scroll-behavior" tabindex="-1">9. Scroll behavior <a class="header-anchor" href="#_9-scroll-behavior" aria-label="Permalink to &quot;9. Scroll behavior&quot;">​</a></h2><h3 id="_9-1-transcript-scrolling" tabindex="-1">9.1 Transcript scrolling <a class="header-anchor" href="#_9-1-transcript-scrolling" aria-label="Permalink to &quot;9.1 Transcript scrolling&quot;">​</a></h3><ul><li>Default: auto-scroll to bottom on new content during stream while pinned</li><li>The first upward scroll <strong>gesture</strong> (wheel / trackpad / touch / scrollbar / keyboard) pauses auto-scroll and shows the &quot;↓ Scroll to bottom&quot; button; queued stream or resize follow work must not reverse that movement</li><li>Programmatic follow scrolling and layout-driven clamps never release follow: a scroll event with no preceding user input (for example a scrollTop clamp when the composer collapses after send or an indicator row unmounts) is treated as layout noise and re-baselined instead of being mistaken for a user scrolling up</li><li>A pinned transcript re-pins in the same frame the content or the viewport changes size, never one frame later. That includes the composer growing under a multi-line draft: the bottom reserve is padding on the transcript content, so the content is observed on its border box and the newest turn moves up with the composer instead of sliding behind it (D287).</li><li>A manual disclosure — whole process, activity group, tool/search/thinking item, delegate brief, or error detail — holds the reading position of the scroller that owns it (issue #324). Only the initiating level claims the anchor; marking ancestors user-owned does not claim their scroll positions. The title is handed to the scroller before the state changes, follow mode is left, and the scroller restores the title&#39;s viewport offset for every frame of the height change. A nested scroller (the delegate run dock, D302) holds its own position and passes the hold outward because growing it also grows the outer content. Search reveal opens the required ancestors at message precision and uses the Search reveal opens the required ancestors and uses the precise target as the final anchor. Leaving follow for any disclosure is not a re-pin: the transcript stays where the reader put it, with jump-to-latest visible, until real scroll input, that control, a new turn or navigation releases the hold (D430).</li><li>Scroll input is attributed to the scroller that can consume it. A press on a row, a control or an editable field is an ordinary click rather than the start of a scroll; a keystroke inside a text field belongs to that field; and input a nested scroller consumes is not the outer scroller&#39;s gesture. Arrow keys still scroll and Space still activates a focused title.</li><li>User send / retry / regenerate: re-pins, hides the jump control, and positions the latest content in the layout phase so the new turn is visible without a top-of-history flash; subsequent persisted and streamed rows continue to follow the bottom</li><li>Scroll-to-bottom button: position fixed at bottom-right of transcript area, offset 12px</li><li>Button appears as soon as upward scrolling releases follow mode</li><li>Click button: scrolls to bottom, resumes auto-scroll</li><li>Button disappears when at bottom</li><li>The subagent task dock uses the same single-body scroll owner as the work panel. It renders the task description followed by the delegate&#39;s live thinking, tool, and answer rows in normal content flow; it does not mount a nested <code>.subagent-run-rows</code> workflow scrollbar. While the panel is pinned, new process rows stay in view; a real upward gesture pauses follow and shows the standard jump-to-latest control. This keeps the process readable without a second scrollbar or an empty tail.</li><li>Clicking a delegation topology node toggles an inset grouped side sheet in the right-side work-panel dock instead of expanding the transcript. Clicking the selected node again closes the side sheet; selecting another node replaces the current detail in place. The dock has a sticky identity header (avatar, name, and model caption on the left; status capsule and elapsed time trailing on the same row), the Task call&#39;s selectable description as a full-width grouped card under a Task section label, capped at four lines with an inline Show more / Show less control for longer tasks, and its live process under an Activity section on one subtle vertical timeline; it does not render separate details, output, or workflow tabs. At the minimum panel width, long commands, paths, and tool summaries remain contained by the dock instead of expanding the side sheet past the client area. Selecting another node replaces the task in place, closing it restores the prior resource view when present, and switching sessions or routes hides the selection. <code>Cmd/Ctrl + J</code> hides the whole dock.</li></ul><h3 id="_9-1a-sidebar-project-path-and-open-folder" tabindex="-1">9.1a Sidebar project path and open folder <a class="header-anchor" href="#_9-1a-sidebar-project-path-and-open-folder" aria-label="Permalink to &quot;9.1a Sidebar project path and open folder&quot;">​</a></h3><ul><li>Hovering or focusing a retained project title shows the full absolute path.</li><li>The truncated project name remains visible in the row; the full path is tooltip/accessible-description only and never forces horizontal scroll.</li><li>Right-clicking a project row or opening its overflow menu exposes <strong>Open folder</strong> as a project action, along with the project management actions. It does not expose project activation; click the directory row to activate it. Conversation overflow no longer carries the folder action.</li><li>Choosing <strong>Open folder</strong> opens the project directory in the system file manager without changing the active session transcript.</li></ul><h3 id="_9-1b-sidebar-session-hover-card" tabindex="-1">9.1b Sidebar session hover card <a class="header-anchor" href="#_9-1b-sidebar-session-hover-card" aria-label="Permalink to &quot;9.1b Sidebar session hover card&quot;">​</a></h3><ul><li>Hovering or focusing a session row reveals a multi-line hover card after the same 500ms delay used by the project path tooltip; the card never anchors to a torn-down row.</li><li>The card surfaces only key metadata, in this order, top to bottom: title; a Session task chip when the session was created by another session; the mode/permission chip; live status; collaboration details when present; the readable model display name (falling back to the provider&#39;s readable name); workspace name and branch on one row; and <strong>Updated ${ssrInterpolate(_ctx.when)}</strong> without seconds. Temporary/scratch sessions show the localized &quot;Temporary&quot; / &quot;临时对话&quot; placeholder instead of a workspace name. The card does not show the session UUID, a Local task chip, a separate Provider/Model label pair, or the collaboration poll timestamp.</li><li>For a session with host-owned collaboration activity, the card adds a bounded collaboration section after the chips: creator/source session when present (title, not UUID), current task preview, up to two recent exchanges with direction, and terminal result. Created-session references remain keyboard-navigable buttons (at most eight). It may show a live <code>running</code> or <code>waiting_permission</code> state, but never loads the complete transcript or exposes message content beyond the host&#39;s bounded preview. Completion and failure results are derived from the durable target turn and remain visible after reload.</li><li>Before showing a project session card, the renderer re-reads the active workspace through the existing project-read operation. This keeps the Git branch current after an external checkout without activating a project or changing the selected conversation. If the read fails, the last cached branch is used.</li><li>The card is rendered through a portal at <code>document.body</code>, never widens beyond 320px, and never causes horizontal scroll on the underlying row. It is interactive only through its own session links (real buttons with an accessible open-session name); the rest of the card is not a control, so a click on the card&#39;s background never leaks into the row behind it.</li><li>The session row does not set a native <code>title</code> attribute. The hover card is the only full-title surface, so the browser tooltip never stacks on the card.</li><li>The card cancels on pointer leave, focus blur, scroll (any scroll container), resize, and the moment a context menu opens.</li></ul><h3 id="_9-1c-session-row-hover-and-row-actions" tabindex="-1">9.1c Session row hover and row actions <a class="header-anchor" href="#_9-1c-session-row-hover-and-row-actions" aria-label="Permalink to &quot;9.1c Session row hover and row actions&quot;">​</a></h3><ul><li>A session row and a project header are each one click target. Their hover-revealed actions (the row overflow control, the header&#39;s add and menu controls) are inert while hidden: the space they occupy before they appear never swallows a click that belonged to the row. A click in that space opens the conversation, or activates and toggles the project group, exactly as a click on the title does; a no-hover pointer gets the controls revealed so it never meets a hidden target.</li><li>Project headers and conversation rows (project, pinned and standalone) use the same full-row hover surface, radius and transition. The title button is transparent; hover never draws a nested title tile. The selected conversation keeps its selected fill on hover. A current workspace uses only the project dot, not another selected background; folding a selected child or leaving the chat page never promotes its project to a selected navigation item.</li><li>Keyboard focus keeps its outline independently of selection. The add and overflow buttons retain local hover feedback, and drag-target paint takes precedence over ordinary header hover.</li><li>Hover paint belongs to the pointer that caused it. When the window loses focus the row and the project header drop their hover background and their revealed actions hide, so nothing is left lit or armed after the window returns; moving the pointer over the row again re-arms it.</li><li>Revealed actions become clickable the moment the row is hovered or focused, and remain reachable through keyboard focus (<code>:focus-within</code> / <code>:focus-visible</code>) without a pointer. A spelled-out control never triggers the row or header underneath it as well.</li><li>The hover card&#39;s own navigation controls are the only interactive surfaces inside the card; the row keeps receiving pointer events everywhere else on it.</li></ul><h3 id="_9-2-sidebar-scrolling" tabindex="-1">9.2 Sidebar scrolling <a class="header-anchor" href="#_9-2-sidebar-scrolling" aria-label="Permalink to &quot;9.2 Sidebar scrolling&quot;">​</a></h3><ul><li>The standalone Sessions body is capped at five compact rows and scrolls internally when additional sessions exist.</li><li>Retained project groups occupy the remaining sidebar height and scroll in a separate region. Both regions stay independent from the footer and primary navigation.</li><li>No horizontal scroll in sidebar</li><li>Scroll indicators use the platform&#39;s subtle overlay treatment without changing either region&#39;s width. The 6px thumb is transparent at rest and appears when the owning list is hovered or focused; dragging keeps it visible until the interaction ends.</li></ul><h3 id="_9-3-settings-scrolling" tabindex="-1">9.3 Settings scrolling <a class="header-anchor" href="#_9-3-settings-scrolling" aria-label="Permalink to &quot;9.3 Settings scrolling&quot;">​</a></h3><ul><li>Settings content scrolls independently within main area</li><li>Left nav (settings sections) is sticky, does not scroll</li></ul><h2 id="_10-reduced-motion" tabindex="-1">10. Reduced motion <a class="header-anchor" href="#_10-reduced-motion" aria-label="Permalink to &quot;10. Reduced motion&quot;">​</a></h2><h3 id="_10-1-policy" tabindex="-1">10.1 Policy <a class="header-anchor" href="#_10-1-policy" aria-label="Permalink to &quot;10.1 Policy&quot;">​</a></h3><p>All animations must respect <code>prefers-reduced-motion: reduce</code>:</p><ol><li><strong>Suppress:</strong> streaming pulse, expand/collapse transitions, dropdown slide, hover color transitions</li><li><strong>Keep (instant):</strong> state changes still occur (card status changes, loading → complete) but with no transition duration</li><li><strong>Never remove:</strong> focus rings, status colors, layout positioning — these are structural, not decorative</li></ol><h3 id="_10-2-implementation" tabindex="-1">10.2 Implementation <a class="header-anchor" href="#_10-2-implementation" aria-label="Permalink to &quot;10.2 Implementation&quot;">​</a></h3><div class="language-css vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">css</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">@media</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> (prefers-reduced-motion: reduce) {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  *</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">*</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">::before</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">*</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">::after</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">    animation-duration</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">0.01</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">ms</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> !important</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">    animation-iteration-count</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> !important</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">    transition-duration</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">0.01</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">ms</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> !important</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p>This does not prevent state changes — it makes them instant.</p><h3 id="_10-3-affected-patterns-from-this-doc" tabindex="-1">10.3 Affected patterns from this doc <a class="header-anchor" href="#_10-3-affected-patterns-from-this-doc" aria-label="Permalink to &quot;10.3 Affected patterns from this doc&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Pattern</th><th>Normal</th><th>Reduced motion</th></tr></thead><tbody><tr><td>Streaming pulse</td><td>accent pulse on left border</td><td>static accent border (no pulse)</td></tr><tr><td>Tool card expand/collapse</td><td>200ms transition</td><td>instant toggle</td></tr><tr><td>Hover state transition</td><td>150ms background change</td><td>instant color change</td></tr><tr><td>Startup splash</td><td>Brand splash + progress, min dwell then fade out</td><td>Instant static splash, no bar motion, instant reveal</td></tr><tr><td>Dialog / search enter</td><td>overlay-in + surface-in via motion tokens</td><td>Near-zero duration enter</td></tr><tr><td>Scroll-to-bottom button fade-in</td><td>150ms opacity</td><td>instant appear</td></tr><tr><td>Toast slide-in</td><td>200ms slide</td><td>instant appear</td></tr><tr><td>Modal/dialog enter</td><td>300ms fade+scale</td><td>instant appear</td></tr><tr><td>Notification popover enter</td><td>menu-scale/fade token</td><td>instant appear</td></tr></tbody></table><h3 id="_10-4-programmatic-scrolling" tabindex="-1">10.4 Programmatic scrolling <a class="header-anchor" href="#_10-4-programmatic-scrolling" aria-label="Permalink to &quot;10.4 Programmatic scrolling&quot;">​</a></h3><ul><li>Session activation uses an immediate layout-phase bottom position so the first visible frame is already stable at the latest record.</li><li>Jump-to-latest and minimap navigation use smooth scrolling only when the OS has not requested reduced motion.</li><li>Turn-start following uses an immediate layout-phase update and then a frame-coalesced instant follow; it does not start overlapping smooth-scroll animations for token groups.</li><li>Manual upward movement cancels a queued pinned-follow frame before it can restore the previous bottom position. Follow remains released across content growth until the viewport is scrolled down within 48px of the bottom or an explicit turn-start / jump-to-latest action re-pins it.</li><li>Released-follow detection is gated on a recent user scroll input. Native scroll events from a follow <code>scrollTo</code> whose position was later clamped by layout changes (composer height, indicator rows) arrive after the fact and look like an upward gesture; because they have no preceding input they are ignored and follow mode is preserved.</li><li>Resize observers never synchronously measure every transcript row from their callback. The one synchronous action they may take is the bottom re-pin of a pinned, visible transcript (a single <code>scrollTo</code>), because a frame requested from inside the callback lands after the current frame has already painted the grown content unpinned (D287).</li></ul><h2 id="_11-acceptance-criteria" tabindex="-1">11. Acceptance criteria <a class="header-anchor" href="#_11-acceptance-criteria" aria-label="Permalink to &quot;11. Acceptance criteria&quot;">​</a></h2><ol><li>All keyboard shortcuts in §1 are functional and do not conflict with system shortcuts</li><li>Enter sends when Enter-to-send is on; when it is off, Cmd/Ctrl+Enter sends and Enter/Shift+Enter insert a newline</li><li>Abort immediately cancels running turn and pending permissions without confirmation dialog 3a. Send stays enabled while running, queues prompts per session, and Send now finishes the current boundary before releasing its prioritized prompt</li><li>Long content (&gt;50 lines for messages, &gt;10 for args, &gt;20 for results) is collapsed by default with expand link</li><li>Tool results that were cut short show a truncation marker or chip per D306; a filled Read window of a longer file does not</li><li>Permission interrupt inserts inline card, disables composer, shows countdown, and re-enables after resolution</li><li>Toasts used for transient background operations; inline errors used for context-specific failures</li><li>Focus returns to composer after session switch, message send, permission resolution, and abort</li><li>Background message, tool, completion, and permission events never change the active session/project/page or keyboard focus; concurrent permission requests remain independently actionable in their originating transcripts, and background artifacts update only their session&#39;s retained work-panel context 9a. Creating a new session or switching to a non-running session returns the composer to its idle Send state even while another session is still streaming; the destination session&#39;s own run state alone decides the send/abort button. New Task reveals the empty destination on the first frame rather than leaving the previous transcript visible until host IO completes.</li><li>Focus rings visible on <code>focus-visible</code> only, 2px accent offset 2px</li><li>Command palette traps focus; Escape returns to previous focus</li><li>All animations respect <code>prefers-reduced-motion: reduce</code> — state changes are instant, no decorative motion</li><li>Project/session rows support non-destructive pin/archive, independent project collapse, project drag/manual reorder, and the documented user-facing sort modes</li><li>Shell chrome does not create accidental text selections, while editable controls and transcript/code/tool content remain selectable and copyable</li><li>Retained project tabs survive restart; activating one changes the selected shell workspace without redirecting background session tool roots</li><li>Project groups can be reordered by dragging the title or with ArrowUp/ArrowDown on that title; the normalized-path order survives a renderer restart and does not change the host workspace identity</li><li>Completed and failed turns appear exactly once in the durable inbox; aborted turns never appear</li><li>All/Unread, mark-all-read, clear, row activation, Escape/focus restore, and arrow/Home/End keyboard navigation behave as documented in §1.7</li><li>Native task notifications appear only while the main window is unfocused; interactive prompt notifications may alert for a focused background session. Activation focuses the window and opens the corresponding session</li><li>Streamed message updates stay within the chat render boundary; shell navigation, composer, completed rows, and work-panel content do not rerender solely because the current assistant message appended content</li><li>The work panel opens and collapses inside the fixed client area; the inner divider follows the shared budget while MainChat keeps its 450px minimum, the expanded sidebar yields at the threshold and returns when the panel closes, and divider cancellation restores the prior panel width (ADR 0033 / ADR 0151 / ADR 0238)</li></ol></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("spec/04-ux/09-interaction-patterns.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _09InteractionPatterns = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _09InteractionPatterns as default
};
