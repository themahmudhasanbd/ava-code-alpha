import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0272: Judge a public-network address on the route the request will dial","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0272-connection-time-public-network-route.md","filePath":"adr/0272-connection-time-public-network-route.md","lastUpdated":1789974692000}');
const _sfc_main = { name: "adr/0272-connection-time-public-network-route.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0272-judge-a-public-network-address-on-the-route-the-request-will-dial" tabindex="-1">ADR 0272: Judge a public-network address on the route the request will dial <a class="header-anchor" href="#adr-0272-judge-a-public-network-address-on-the-route-the-request-will-dial" aria-label="Permalink to &quot;ADR 0272: Judge a public-network address on the route the request will dial&quot;">​</a></h1><ul><li>Status: Accepted for implementation</li><li>Date: 2026-09-17</li><li>Deciders: PI-Desktop core</li><li>Related: ADR 0177, ADR 0243, ADR 0245, D413, D414, issue #419, PR #473</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p><code>createPublicHttpsClient</code> guards every credential-free public HTTPS fetch the skill market makes from Electron main (ADR 0243). Before each hop it classified the target host with <strong>Node&#39;s local resolver</strong> (<code>node:dns</code>) and refused any hop whose answers were not public.</p><p>The request itself never used that answer. <code>fetchImpl</code> is <code>net.fetch</code>, which dials through Chromium&#39;s proxy stack, and ADR 0177 points that stack at the configured proxy (System, or a Custom <code>http</code>/<code>https</code>/<code>socks</code> URL). Under an HTTP/HTTPS proxy the hostname travels in the <code>CONNECT</code> line and the <em>proxy</em> resolves it; nothing in this app resolves or dials the destination.</p><p>The pre-check therefore judged an address no request would ever use, and a whole class of working setups was refused for it. Clash, Mihomo and Surge answer DNS with a synthesized fake-IP (<code>198.18.0.0/15</code> by default) while their TUN device or proxy carries the real connection: <code>classifyIpLiteral</code> reads <code>198.18.0.1</code> as <code>benchmark</code>, which is not public, so every catalog source failed with &quot;the catalog source was blocked by the app&#39;s address check&quot; even though the in-app browser and the transport itself worked (issue #419, reported on 0.14.8).</p><p>PR #473 made that refusal legible — structured <code>reason</code>, <code>addressKind</code>, <code>NETWORK_RESOLVE_FAILED</code>, the market&#39;s <code>unresolved</code> kind — and left the boundary alone, because moving the classification out of the pre-check is a security boundary change. This ADR is that change.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><p><strong>The address verdict follows the route the request will actually take.</strong> For every hop, immediately before that hop is dialed, the client asks the session that carries <code>fetchImpl</code> for its own proxy decision on exactly that URL (<code>Session.resolveProxy</code>, wired beside <code>net.fetch</code> in <code>skill-market-catalog.ts</code>). The shared <code>classifyProxyRoute</code> reduces the answer to <code>proxied</code>, <code>direct</code>, or <code>unknown</code>.</p></li><li><p><strong>A proxied route replaces the local address verdict; it does not remove it.</strong> On <code>proxied</code>, the shared <code>isAcceptableResolvedAddress</code> tolerates exactly one class the old rule refused: <code>benchmark</code> — the RFC 2544 range a TUN fake-IP resolver synthesizes, i.e. the resolver&#39;s own artifact rather than a target. Every other non-public class (<code>private</code>, <code>loopback</code>, <code>link-local</code>, <code>cgnat</code>, <code>multicast</code>, <code>reserved</code>, <code>documentation</code>, <code>ula</code>, <code>site-local</code>, <code>invalid</code>, <code>unspecified</code>) still refuses, and an unanswered resolver still refuses as <code>resolve-failed</code>. A local answer naming a real internal target stays evidence of a split-horizon or hostile resolver, and this app cannot see what the proxy would dial for it.</p></li><li><p><strong><code>direct</code> and <code>unknown</code> keep the pre-ADR rule by default.</strong> The app dials the resolved address itself there, so only <code>public</code> passes. An explicit <code>allowFakeIp</code> setting may additionally permit the <code>benchmark</code> placeholder class for transparent router/TUN deployments; it never permits any other non-public class. <code>unknown</code> covers no route resolver wired at all, a resolver that throws, an empty or unparsable answer, and a list that offers <code>DIRECT</code> anywhere. Fail closed unless that narrow opt-in is enabled.</p></li><li><p><strong>The syntactic guard is untouched.</strong> <code>isSafePublicHttpsUrl</code> still runs first for every hop and still refuses non-HTTPS, credential-bearing, non-public literal, and <code>localhost</code> / <code>.local</code> / <code>.internal</code> URLs. Redirects stay <code>manual</code> with a five-hop cap, and <strong>every hop</strong> re-runs the whole check, route included.</p></li><li><p><strong>The route travels with the refusal.</strong> <code>PublicNetworkRefusal</code> / <code>PublicNetworkRefusalDetail</code> gain <code>route</code>, so the diagnostics line and the market&#39;s <code>failureDetails</code> say which route the address was judged on. A <code>benchmark</code> refusal on a <code>direct</code> route means &quot;the transport sees no proxy for this URL and the app would dial the synthesized address itself&quot;; the same refusal on <code>unknown</code> means no route could be read at all.</p></li></ol><h2 id="threat-model" tabindex="-1">Threat model <a class="header-anchor" href="#threat-model" aria-label="Permalink to &quot;Threat model&quot;">​</a></h2><p>What the guard stopped before this ADR:</p><ul><li>Non-HTTPS, credential-bearing, and non-public-literal URLs — unchanged.</li><li>A hop whose <strong>local</strong> resolver answers with a private, loopback, link-local, CGNAT, ULA, site-local, multicast, reserved, or documentation address. On a direct route that is the address the app dials, so this was real protection and stays exactly as it was.</li></ul><p>What changes:</p><ul><li><strong>Still stopped:</strong> everything above on <code>direct</code> / <code>unknown</code>, plus every address class except <code>benchmark</code> on <code>proxied</code>.</li><li><strong>Weakened, precisely:</strong> on a <code>proxied</code> route, a public hostname whose <em>local</em> answer is inside <code>198.18.0.0/15</code> is no longer refused. Consequences: <ul><li><strong>SSRF to an internal service through the proxy.</strong> If the proxy — or the TUN capture behind a fake-IP answer — resolves that name to an internal address, the app will have carried the request to it. The app cannot observe that hop: the only peer it ever dialed is the proxy. Reaching internal targets is now bounded by the proxy&#39;s own rule set (Clash&#39;s default rule set rejects LAN targets; a permissive or corporate proxy will not).</li><li><strong>Cloud metadata endpoints.</strong> <code>169.254.169.254</code>, <code>fd00:ec2::254</code> and similar are still refused whenever the <em>local</em> answer names them (link-local / ULA), on both routes. The new hole is indirect: a name whose local answer is a fake-IP address, which the proxy then resolves to a metadata endpoint, is no longer judged.</li><li><strong><code>198.18.0.0/15</code> as an intranet range.</strong> The range is reserved for benchmarking and is not publicly allocated, but a lab or VPN could address hosts there; a name answering from it is no longer refused on a proxied route, and the TUN/proxy decides where it goes.</li><li><strong>Chromium proxy fallback.</strong> The route is read as the <em>resolved</em> proxy list. Chromium may still retry a failed hop without the proxy, so <code>proxied</code> is a property of the configured route, not a proof about the socket that was used. A <code>DIRECT</code> entry anywhere in the list is therefore read as <code>unknown</code>.</li><li><strong>Who controls the route.</strong> The user does, through Settings → Network (ADR 0177) or the OS/browser proxy configuration. Nothing in the renderer, in a catalog entry, or in a plugin can influence it, and nothing in the market path can write it.</li></ul></li></ul><p>Why that is acceptable:</p><ul><li>The property the guard protects is &quot;this app never dials an internal address by itself&quot;. On a proxied route that property is <em>stronger</em> than before rather than weaker: the app&#39;s only possible peer is the user-configured proxy, and the destination is chosen by that proxy instead of by a name whose resolution an attacker may control. What the pre-check actually enforced on a proxied route was a claim about a resolver the request did not use — it produced the false positive in #419 without ever inspecting the dialed peer.</li><li>The one class now tolerated is <em>by construction</em> the signature of local interception rather than of an internal target: <code>198.18.0.0/15</code> is RFC 2544 benchmarking space and hosts no service.</li><li>Everything the guard can still verify in-process — URL shape, per-hop re-validation, every real internal address class, an unanswered resolver — is still verified, and the verdict is now made about the object the request will actually use.</li></ul><p>When to roll this back:</p><ul><li>If Electron ever exposes the peer a request actually connected to, and it is not the proxy, replace the route verdict with that peer check. The route verdict exists only because the dialed peer is not observable.</li><li>If a report shows a proxied hop reaching a private or metadata target through a fake-IP answer, or Chromium falling back to a direct connection the route list did not advertise, restore the strict verdict for that case and keep the fake-IP case behind an explicit user setting instead.</li><li>If <code>Session.resolveProxy</code> stops reporting the configured proxy, the route collapses to <code>unknown</code> and the guard returns to the strict behavior by itself. That failure direction — the pre-#419 false positive returns rather than a private target being dialed — is the intended one.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A proxied or fake-IP user&#39;s skill market works: no source is refused for an address the request would never dial (issue #419).</li><li>A <code>direct</code>-route user keeps the old verdicts by default. The explicit <code>allowFakeIp</code> opt-in is limited to benchmark placeholders and cannot be used to reach a private, loopback, link-local, or other non-public address.</li><li>The MCP market now applies the same route-aware verdict before each hop. A fully proxied hop uses the session&#39;s <code>net.fetch</code>, so a TUN fake-IP answer can reach the configured proxy; direct and unknown hops retain the pinned Node HTTPS path and strict public-address rule. Real private answers remain refused on every route. ADR 0245 defines the MCP-specific transport and body limits.</li><li>A TUN user who configured no proxy the app can see is still refused: the transport reports <code>DIRECT</code> while the local resolver answers fake-IP. That case is deliberately left strict (clause 3), and its remedy is to enable the system proxy or set the app&#39;s Custom proxy, after which the route is proxied and the market works.</li><li>The route is read once per hop, so the market pays one extra <code>resolveProxy</code> round trip per hop — bounded by the same five-hop cap.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><ul><li><strong>Delete the DNS gate for every request.</strong> Rejected: it removes the SSRF guard for direct users too, and the guard would no longer stop a private answer from being dialed by the app itself.</li><li><strong>Relax <code>198.18.0.0/15</code> unconditionally.</strong> Rejected: it accepts an answer the app cannot attribute to an interception layer, and it silently allows a connection to a host addressed in that range on a direct route.</li><li><strong>Let the user allowlist a source.</strong> Rejected: it moves a security decision to a user who cannot see what the address would be used for, needs new settings and UI for a case the app can decide, and would not have helped in #419, where every source was refused at once.</li><li><strong>Check the peer after connecting.</strong> Unavailable, and the honest reason this ADR exists: Electron&#39;s <code>net.fetch</code> performs proxy resolution and dialing inside the network service and exposes no peer address afterwards. The pre-ADR guard could not do this either, which is why its proxied verdict was noise.</li><li><strong>Read the route from the app&#39;s own proxy settings only.</strong> Rejected as incomplete: it would miss PAC and OS-level proxy configuration, which is exactly what a Clash-style &quot;system proxy&quot; setup uses. <code>Session.resolveProxy</code> reports what the session will really use, PAC included.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0272-connection-time-public-network-route.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0272ConnectionTimePublicNetworkRoute = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0272ConnectionTimePublicNetworkRoute as default
};
