import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0070: Separate Composer File-Reference Display from Prompt Serialization","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0070-separate-composer-file-reference-display-from-prompt.md","filePath":"adr/0070-separate-composer-file-reference-display-from-prompt.md","lastUpdated":1786299821000}');
const _sfc_main = { name: "adr/0070-separate-composer-file-reference-display-from-prompt.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0070-separate-composer-file-reference-display-from-prompt-serialization" tabindex="-1">ADR 0070: Separate Composer File-Reference Display from Prompt Serialization <a class="header-anchor" href="#adr-0070-separate-composer-file-reference-display-from-prompt-serialization" aria-label="Permalink to &quot;ADR 0070: Separate Composer File-Reference Display from Prompt Serialization&quot;">​</a></h1><ul><li>Status: Accepted for implementation</li><li>Date: 2026-08-10</li><li>Deciders: PI-Desktop core</li><li>Related: D124, D197, D209, ADR 0024, ADR 0059</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Workspace autocomplete and clipboard paste both produce canonical file paths that the agent must receive as plain-text <code>@</code> references. Rendering those paths directly in the controlled composer textarea exposes long workspace paths and, for pasted files, UUID-backed absolute scratch paths. The paths can wrap across multiple lines and crowd out the actual task while still being necessary at dispatch time.</p><p>A native textarea cannot render one substring differently from its underlying value. A shortened textarea value also cannot be expanded by basename lookup: duplicate leaf names are valid, and scratch files require their exact absolute paths.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>A completed file selection and each materialized clipboard file become a renderer-owned, transient composer reference. The visible textarea keeps only the user&#39;s ordinary text. Directory autocomplete remains literal so it can continue into deeper path segments.</li><li>Each reference stores its canonical path and a compact leaf label. Workspace references keep the complete relative index path; pasted references keep the UUID-backed absolute scratch path while displaying the sanitized original leaf name returned by <code>composer/pasteFiles</code>.</li><li>The composer renders references as compact removable chips above the textarea. A chip persistently shows only its leaf label; its canonical path remains available through the tooltip and accessible name. Duplicate labels remain separate references with separate canonical paths.</li><li>Immediately before the existing submit dispatcher runs, the renderer appends references in stable addition order and serializes each with the existing <code>formatFileInsert</code> quoting rules. The resulting prompt remains plain text and contains the exact <code>@relative/path</code> or <code>@absolute/path</code> that pi and the agent already consume.</li><li>Reference-only drafts are sendable. References are appended after visible text so slash templates and <code>/agent-mode</code>, <code>/plan-mode</code>, and <code>/goal-mode</code> remain recognizable at the start of the draft. Successful local or prompt dispatch clears the active editor, while rejected or failed dispatch retains it. An accepted prompt also retains a renderer-only, session- and turn-scoped snapshot of its pre-serialization text and references until the turn leaves the unanswered smart-stop window.</li><li>Stopping before assistant text, thinking, or a tool row begins removes the just-sent user row and restores the structured snapshot. Canonical paths return behind their leaf-name chips, never as textarea text. Once a reply begins, abort keeps the partial transcript and restores no draft. The snapshot is not reconstructed from persisted message content and adds no IPC, host RPC, or storage field.</li><li>References are scoped by durable session id and cleared when their workspace changes. Removing a chip removes only the draft reference; scratch bytes keep the existing session lifecycle.</li><li><code>ComposerPastedFile.name</code> is the sanitized original leaf display name. The unique UUID storage name is represented by <code>path</code>, not duplicated into the display label. This is an additive semantic clarification inside the existing Electron-only shape; no host protocol or storage schema changes.</li></ol><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Insert only the basename into the prompt:</strong> rejected because duplicate names and external scratch files would no longer resolve reliably.</li><li><strong>Overlay shortened text on a transparent textarea:</strong> rejected because the displayed and native text lengths diverge, breaking caret, selection, wrapping, IME, and accessibility behavior.</li><li><strong>Replace the textarea with <code>contenteditable</code>:</strong> rejected for this focused change because it would reopen the complete IME, selection, undo, paste, and accessibility contract.</li><li><strong>Parse serialized <code>@path</code> tokens back out of an unanswered user message:</strong> rejected because user-authored references and renderer-appended references are indistinguishable, and the persisted text no longer carries the original display label or duplicate-reference identity.</li><li><strong>Send provider-specific binary attachments:</strong> rejected because file tools already consume the materialized path and the prompt contract remains provider-independent plain text.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Long and UUID-backed paths no longer occupy the visible prompt row.</li><li>Canonical references remain exact in persisted messages and model context.</li><li>The renderer owns transient reference state in addition to textarea text.</li><li>Clipboard storage containment, limits, cleanup, and security boundaries are unchanged from ADR 0059.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0070-separate-composer-file-reference-display-from-prompt.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0070SeparateComposerFileReferenceDisplayFromPrompt = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0070SeparateComposerFileReferenceDisplayFromPrompt as default
};
