import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0265: Priority block and row actions for the Host-owned turn queue","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0265-turn-queue-priority-block-and-row-actions.md","filePath":"adr/0265-turn-queue-priority-block-and-row-actions.md","lastUpdated":1789478101000}');
const _sfc_main = { name: "adr/0265-turn-queue-priority-block-and-row-actions.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0265-priority-block-and-row-actions-for-the-host-owned-turn-queue" tabindex="-1">ADR 0265: Priority block and row actions for the Host-owned turn queue <a class="header-anchor" href="#adr-0265-priority-block-and-row-actions-for-the-host-owned-turn-queue" aria-label="Permalink to &quot;ADR 0265: Priority block and row actions for the Host-owned turn queue&quot;">​</a></h1><ul><li>Status: Accepted for implementation</li><li>Date: 2026-09-15</li><li>Amends: ADR 0213 (persisted Host-owned turn queue), ADR 0118 (renderer-owned queued prompts and turn-boundary stops)</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>ADR 0213 moved the queue into host-core and gave &quot;Send now&quot; exactly one shape: <code>prioritize</code> rewrites the entry&#39;s <code>position</code> below the current minimum, so the entry jumps to the head of the whole queue. The renderer mirrored that as a single in-memory <code>sendNowRequested</code> flag cleared on every other row, which meant only one row could ever be waiting for the next boundary, and two &quot;Send now&quot; clicks resolved as &quot;the last click wins&quot;.</p><p>The queued row also only offered Remove and Send now. Users could not correct a queued prompt without deleting it and retyping, and could not reorder the plain queue at all even though the Host already orders it by a durable <code>position</code>.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><strong>A promotion block, ordered by click.</strong> <code>turn_queue</code> gains a nullable <code>priority</code> column (schema v18). Promoting an entry writes <code>COALESCE(MAX(priority), 0) + 1</code> inside the session, so the first promotion is delivered first and each later promotion queues behind it. Delivery order is <code>priority ASC, position ASC</code> for promoted entries followed by <code>position ASC</code> for the rest; the plain queue keeps its own order.</li><li><strong>Promotion is one-way and idempotent-hostile.</strong> Promoting an entry that already carries a <code>priority</code> fails with <code>CONFLICT</code>/<code>ALREADY_PRIORITIZED</code> instead of silently moving it again. <code>session.queuePrioritize</code> keeps moving optimistic mirror does the same.</li><li><strong>A promoted row is locked.</strong> Because the Host has already committed to starting it, its row disables move up, move down, edit, and remove, and its Send now button reads as decided (<code>chat.sendNowPending</code>). The disabled controls keep their tooltip and <code>aria-disabled</code> state so the lock is explained rather than silent. There is deliberately no un-promote path: a promotion cannot be half-applied.</li><li><strong>A reorder operation for the plain queue.</strong> <code>session.queueReorder</code> swaps one entry&#39;s <code>position</code> with its adjacent non-prioritized neighbour in the requested direction, and reports <code>moved: false</code> for a missing entry, a promoted entry, or a block/queue edge. Promoted entries are never neighbours, so reordering can never cross into the priority block. The renderer mirrors one swap and never reaches the Host for a boundary no-op.</li><li><strong>Edit returns the row to the composer.</strong> Editing is renderer-local: the row is removed (through the existing removal path) and its captured <code>ComposerDraftSnapshot</code> — text plus inline file references — replaces the composer input through the existing <code>composerPrefill</code> channel. The token-stripped <code>content</code> is never used to rebuild the prompt. The action is refused with a toast while the input is non-empty, and the emptiness check runs against the live editor read because the draft cache is not written per keystroke.</li><li><strong>The promoted block is delivered as adjacent messages, not as separate turns.</strong> Promotion still does not touch the running turn by itself: the renderer requests the existing graceful <code>agent/stop</code>, and the block leaves at the next boundary. The first promoted entry then starts the turn, and every later promoted entry is injected into that same turn as user input over the Composer&#39;s existing steering channel. The transcript therefore reads <code>user: first</code>, <code>user: second</code> and the model answers once. The injection is retried a bounded number of times because the runtime only accepts input for a live run; an entry that is still undelivered stays queued and leaves at the next boundary as its own turn, which is the previous behavior and never a lost prompt. An injected entry&#39;s own RACP turn is canceled: its input was delivered by another turn, and no client may be left believing it is still waiting.</li><li><strong>The turn&#39;s owner is authoritative about its end.</strong> A runtime terminal event is not a reliable release: Main drops one that names a turn it no longer owns (<code>isStaleTerminalEvent</code>), and an abort need not produce one at all. A turn left active inside the module holds its session&#39;s queue forever, which is how &quot;Send now, then Stop&quot; stranded a queued row. So the settlement reported by <code>finishTurn</code> closes the turn in the module (marking it as the settlement reason, canceling its approvals and inputs) and lets the queue run, and the module&#39;s drain request is remembered while a pass is running instead of being dropped.</li></ol><h2 id="consequences-and-validation" tabindex="-1">Consequences and validation <a class="header-anchor" href="#consequences-and-validation" aria-label="Permalink to &quot;Consequences and validation&quot;">​</a></h2><p>No breaking change to existing channels: <code>agentQueuePrioritize</code> / <code>session.queuePrioritize</code> keep their names and gain the block semantics, and <code>agentQueueReorder</code> / <code>session.queueReorder</code> are additive. Existing rows migrate with <code>priority = NULL</code>, so an upgraded database keeps its FIFO order and its eight-entry bound. Row-level unit coverage lives in <code>turn_queue</code> tests (promotion order, reorder, prioritized-row rejection, v16 upgrade), <code>turn-queue.test.ts</code> / <code>agent-host.test.ts</code> (block order, permissions), and <code>composer-send-state.test.mjs</code> (lock, reorder, and edit contracts). The rendered journeys are specified as E2E-QUEUE-promote-orders-delivery-by-click, E2E-QUEUE-reorder-moves-plain-neighbours, and E2E-QUEUE-edit-restores-draft-only-when-input-empty, and remain Draft until rendered E2E validation is performed.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0265-turn-queue-priority-block-and-row-actions.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0265TurnQueuePriorityBlockAndRowActions = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0265TurnQueuePriorityBlockAndRowActions as default
};
