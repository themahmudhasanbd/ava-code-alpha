import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR active-turn-steering: Bind Composer steering to the active durable turn","description":"","frontmatter":{},"headers":[],"relativePath":"adr/active-turn-steering.md","filePath":"adr/active-turn-steering.md","lastUpdated":1789800041000}');
const _sfc_main = { name: "adr/active-turn-steering.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-active-turn-steering-bind-composer-steering-to-the-active-durable-turn" tabindex="-1">ADR active-turn-steering: Bind Composer steering to the active durable turn <a class="header-anchor" href="#adr-active-turn-steering-bind-composer-steering-to-the-active-durable-turn" aria-label="Permalink to &quot;ADR active-turn-steering: Bind Composer steering to the active durable turn&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-12</li><li>Issue: <a href="https://github.com/vastsa/PI-Desktop/issues/164" target="_blank" rel="noreferrer">https://github.com/vastsa/PI-Desktop/issues/164</a></li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The Composer queues follow-ups, but cannot redirect a running turn. A second prompt is correctly rejected with <code>AGENT_BUSY</code>. pi-agent-core already supplies <code>Agent.steer</code>; Codex&#39;s <a href="https://learn.chatgpt.com/docs/app-server#steer-an-active-turn" target="_blank" rel="noreferrer">turn/steer contract</a> provides the expected-turn admission model.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Keep Send/Enter as follow-up. Alt+Enter (Option+Enter on macOS) uses an additive <code>agent/steer</code> channel with <code>expectedTurnId</code>. The existing queue slice submits optimistic input and restores rejected drafts. The agent IPC handler reuses attachment validation against the running model and workspace; the sidecar rechecks the target after asynchronous preparation and calls <code>Agent.steer</code>.</p><p>Steering keeps the active configuration and durable turn. Started tools finish before the next model request consumes input. The runtime handles admission at its closing boundary and while awaiting delegates. Stop closes admission and retains accepted input as history without independently replaying it.</p><p>The existing event-persistence module journals input through the outbox. <code>precedingAssistant</code> reserves an unfinished reply&#39;s position before the user row. Host append permits a terminal assistant to replace its own streaming reservation, retaining sequence and turn ownership; completed rows remain immutable on replay. Recovery updates the reservation in place. The persisted <code>UiMessage.steering</code> marker protects input from Smart Stop, including after renderer reload; no separate renderer submission registry is needed. Host provenance treats steering into a claimed collaboration delivery turn as extra human input: it must target that delivery&#39;s session, does not inherit the delivery&#39;s agent origin, and strips any client-supplied <code>session_message</code> (D597).</p><h2 id="consequences-and-validation" tabindex="-1">Consequences and validation <a class="header-anchor" href="#consequences-and-validation" aria-label="Permalink to &quot;Consequences and validation&quot;">​</a></h2><p>No new provider transport, host protocol version, or storage migration is required. Streaming reservation updates are the sole exception to append-only message persistence. Existing regression suites cover surrounding behavior; E2E-AGENT-alt-enter-steers-active-turn specifies the full journey and remains Draft until rendered E2E validation is performed.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/active-turn-steering.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const activeTurnSteering = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  activeTurnSteering as default
};
