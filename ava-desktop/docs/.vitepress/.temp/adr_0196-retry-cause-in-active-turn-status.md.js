import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0196: Show provider retry causes in the active-turn status","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0196-retry-cause-in-active-turn-status.md","filePath":"adr/0196-retry-cause-in-active-turn-status.md","lastUpdated":1788951705000}');
const _sfc_main = { name: "adr/0196-retry-cause-in-active-turn-status.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0196-show-provider-retry-causes-in-the-active-turn-status" tabindex="-1">ADR 0196: Show provider retry causes in the active-turn status <a class="header-anchor" href="#adr-0196-show-provider-retry-causes-in-the-active-turn-status" aria-label="Permalink to &quot;ADR 0196: Show provider retry causes in the active-turn status&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-08</li><li>Related: ADR 0175, D358, US-UI-60d</li><li>Note: First filed as ADR 0186, then 0195; 0186 is session titles and 0195 is the work-panel toggle.</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The live <code>retrying</code> status row tells users that a provider request is being retried, but not why. The same classified provider failures already become structured assistant errors when the retry budget is exhausted. During the backoff, users should be able to inspect the current cause without creating a duplicate transcript error or waiting for the retry to finish.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Extend the shared <code>AgentActivity.retrying</code> payload with optional bounded error details: the stable error code, the already-redacted and length-limited provider message, and the HTTP status when known. The runtime supplies the details for both request-setup and mid-stream retries.</p><p>The renderer keeps the existing compact retry row at rest. Hovering or focusing its retry label reveals a small error-styled tooltip that reuses the assistant error card&#39;s hierarchy: icon, localized summary, stable code/status, and the provider message. The trigger is keyboard-focusable and exposes the same reason through its accessible name. The tooltip closes when the activity phase clears; the final assistant error and outcome surfaces remain the only terminal error presentation.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Users can identify rate limits, timeouts, network failures, and provider errors during a retry wait.</li><li>The transcript remains free of intermediate error rows and retry behavior is unchanged.</li><li>The protocol exposes only diagnostics already classified and redacted by the runtime; arbitrary provider payloads do not cross the boundary.</li><li>Older status payloads without <code>error</code> continue to render the ordinary retry label.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><h3 id="use-only-a-native-title" tabindex="-1">Use only a native <code>title</code> <a class="header-anchor" href="#use-only-a-native-title" aria-label="Permalink to &quot;Use only a native \`title\`&quot;">​</a></h3><p>Rejected because it cannot match the existing error presentation, is difficult to read for long provider messages, and provides inconsistent keyboard access.</p><h3 id="add-an-assistant-error-row-for-every-retry" tabindex="-1">Add an assistant error row for every retry <a class="header-anchor" href="#add-an-assistant-error-row-for-every-retry" aria-label="Permalink to &quot;Add an assistant error row for every retry&quot;">​</a></h3><p>Rejected because intermediate failures would duplicate the final assistant error and make a successful same-turn retry look like a failed turn.</p><h3 id="expose-the-full-provider-response" tabindex="-1">Expose the full provider response <a class="header-anchor" href="#expose-the-full-provider-response" aria-label="Permalink to &quot;Expose the full provider response&quot;">​</a></h3><p>Rejected because provider responses can contain unstable or sensitive data. The existing classifier&#39;s bounded message and low-cardinality status are sufficient for diagnosis.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0196-retry-cause-in-active-turn-status.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0196RetryCauseInActiveTurnStatus = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0196RetryCauseInActiveTurnStatus as default
};
