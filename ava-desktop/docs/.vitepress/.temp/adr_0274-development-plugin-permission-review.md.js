import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0274: A development plugin is reviewed before it is loaded","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0274-development-plugin-permission-review.md","filePath":"adr/0274-development-plugin-permission-review.md","lastUpdated":1789582491000}');
const _sfc_main = { name: "adr/0274-development-plugin-permission-review.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0274-a-development-plugin-is-reviewed-before-it-is-loaded" tabindex="-1">ADR 0274: A development plugin is reviewed before it is loaded <a class="header-anchor" href="#adr-0274-a-development-plugin-is-reviewed-before-it-is-loaded" aria-label="Permalink to &quot;ADR 0274: A development plugin is reviewed before it is loaded&quot;">​</a></h1><ul><li>Status: Accepted for implementation</li><li>Date: 2026-09-17</li><li>Deciders: PI-Desktop core</li><li>Related: <a href="./0005-user-installable-plugin-system">ADR 0005</a> · <a href="./0110-plugin-panel-chrome-spacing-contract">ADR 0110</a> · <a href="./../spec/07-plugins/12-plugin-ipc-and-host-services">07-plugins/12</a> · <a href="./../spec/07-plugins/13-plugin-permissions-matrix">07-plugins/13</a></li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>A plugin&#39;s permissions are approved by the user at load time, and the approval is the ceiling: hot reload never widens it (<code>plugin-runtime.ts</code>, <code>reloadDevPlugin</code>). Two holes sat around that rule.</p><p>First, loading a development plugin folder was never a review. The folder picker called <code>plugins.loadDev</code> and <code>PluginRuntime.loadFromPath</code> with the permissions the manifest declared, so the declaration <em>was</em> the grant. The permission-review modal that guards the marketplace install path (<code>PluginDialogs.tsx</code>) was unreachable for development plugins, which is where plugin authors actually work.</p><p>Second, the manual <strong>Reload</strong> used the registry row&#39;s permission list as the approval and passed it through <code>loadFromPath</code>&#39;s <code>granted ∩ declared</code> filter. A manifest edit that added a permission therefore reloaded <em>silently</em> with that permission dropped, and <code>watchDevPlugin</code> then recorded the intersection as the new ceiling. The plugin came back partly broken, the reason was invisible, and every following save hit <code>PERMISSION_DENIED: manifest now requests ui.microphone; load the plugin again to review</code> — advice that pointed at a picker which reviewed nothing.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>Choosing a development plugin folder is a request, not consent. <code>plugin/loadDev</code> returns <code>{ canceled: false, review }</code> where <code>review</code> is the validated declaration (<code>PluginPermissionReview</code>): id, name, version, every declared permission, and what is beyond the current approval. Nothing is registered and nothing is loaded at this point.</li><li><code>plugin/loadDevConfirm</code> commits the answer. It registers the folder (<code>plugins.loadDev</code>, which rewrites the registry row from the manifest), loads it with the accepted permissions only, and arms the watcher whose ceiling is that same accepted set.</li><li><code>plugin/reload</code> compares the manifest against the <strong>recorded approval</strong> (<code>PluginRuntime.devApproval</code>), never against the registry row. Permission names and file scope are both compared: a new glob is asking for more, exactly like a new permission. When nothing was added it reloads immediately — the ordinary edit/save/reload loop must not become a dialog.</li><li>When something was added, <code>plugin/reload</code> returns a review instead of loading. The plugin keeps running under its current approval until the user answers through <code>plugin/reloadConfirm</code>, which reloads under the accepted set and makes that set the new ceiling.</li><li>The scaffold flow (<code>plugin/createFromTemplate</code>) writes files and then returns the same review. Registering a scaffolded plugin goes through the reviewed load like any picked folder.</li><li>Paths and identities in a confirmation come from the host: the renderer sends an id (or the path it just picked) and the accepted permission list, never a path of its own choosing for an already-registered plugin.</li><li>The <code>PERMISSION_DENIED</code> text of the hot-reload refusal now names an action that exists: <em>reload it from the Plugins page to review</em>.</li><li>Both reviews render through one <code>PermissionGroups</code> component, so the install review and the development review cannot drift apart about what is risky.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A plugin author sees the same permission review a marketplace install shows, at the moment they choose a folder, and again whenever an edit asks for more. The answer, not the manifest, is what the runtime enforces.</li><li>A manifest edit that widens silently can no longer produce a half-working plugin: it is either applied with the user&#39;s answer or not applied at all.</li><li>Nothing is granted against an unreadable manifest: the declaration is validated before it becomes a review, and a failed read refuses the flow.</li><li>The registry row is rewritten on every reviewed load, so it always states the current declaration rather than the one from first load; the <em>approval</em> is the watcher ceiling, which is the record that matters for reload.</li><li>Widening still requires a user decision even in development, so the security property the hot-reload guard exists for survives the fix.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0274-development-plugin-permission-review.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0274DevelopmentPluginPermissionReview = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0274DevelopmentPluginPermissionReview as default
};
