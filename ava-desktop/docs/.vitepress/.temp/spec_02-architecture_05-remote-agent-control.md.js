import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Remote Agent Control Target Architecture","description":"","frontmatter":{},"headers":[],"relativePath":"spec/02-architecture/05-remote-agent-control.md","filePath":"spec/02-architecture/05-remote-agent-control.md","lastUpdated":1789764972000}');
const _sfc_main = { name: "spec/02-architecture/05-remote-agent-control.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="remote-agent-control-target-architecture" tabindex="-1">Remote Agent Control Target Architecture <a class="header-anchor" href="#remote-agent-control-target-architecture" aria-label="Permalink to &quot;Remote Agent Control Target Architecture&quot;">​</a></h1><ul><li>Status: Target specification; post-MVP</li><li>Decision: D373 / ADR 0205, amended by D374 and D375</li><li>Scope: Remote observation and control of a PI-Desktop Agent Host</li><li>Source of truth: <code>03-runtime/19-remote-agent-control-protocol.md</code></li></ul><h2 id="_1-scope-and-status" tabindex="-1">1. Scope and status <a class="header-anchor" href="#_1-scope-and-status" aria-label="Permalink to &quot;1. Scope and status&quot;">​</a></h2><p>This document specifies the target architecture for controlling a PI-Desktop Agent from another client. It does not enable a network listener in the current desktop release and does not change the frozen MVP boundary in <code>00-baseline.md</code> or ADR 0004.</p><p>The target capability lets an authenticated client:</p><ul><li>discover and attach to sessions owned by an Agent Host;</li><li>start, queue, observe, stop, and interrupt turns;</li><li>receive ordered assistant, tool, approval, and lifecycle events;</li><li>answer host-owned permission, contract approval, and input requests with the same decisions the desktop offers locally;</li><li>reconnect after a transport failure without losing the session state; and</li><li>use the same workspace, tool, secret, and permission boundaries as a local turn.</li></ul><p>The feature is a control-plane API. It is not remote desktop streaming, an arbitrary shell service, a provider proxy, or a replacement for the local MCP control plane.</p><p>D375 fixes the order in which the topologies ship. The first remote deployment is the desktop itself acting as the Remote Client of a headless Host on another machine over an SSH tunnel, which is what users asked for in issues #176 and #140. The second is an outbound messaging integration that runs beside the Host (issue #100). Gateway routing and browser access remain fully specified here and in the protocol, but they are unscheduled until a demand signal or product decision schedules them.</p><h2 id="_2-design-principles" tabindex="-1">2. Design principles <a class="header-anchor" href="#_2-design-principles" aria-label="Permalink to &quot;2. Design principles&quot;">​</a></h2><ol><li><strong>The Agent Host is the source of truth.</strong> A client is a viewer and controller that may disconnect. A running turn is not owned by a browser tab or Electron window.</li><li><strong>The semantic contract is transport-neutral.</strong> <code>RACP-WS</code> is the normative v1 binding and is first deployed over an SSH tunnel. <code>RACP-HTTP</code> is the browser profile of the same contract and is unscheduled. <code>RACP-GRPC</code> is reserved. Every shipped binding exposes the same session, turn, event, approval, and attachment semantics.</li><li><strong>Local boundaries remain local.</strong> Rust host-core continues to speak only stdio NDJSON JSON-RPC with its trusted Electron Main or headless host supervisor, wherever that host runs. The Node pi sidecar continues to reach host services through the host-process proxy.</li><li><strong>The public edge is a capability boundary.</strong> Remote clients never receive raw <code>host.proxy</code>, Electron IPC, host-core RPC, provider credentials, or an arbitrary operation catalog.</li><li><strong>State synchronization is explicit.</strong> Every durable remote event has an <code>{ epoch, sequence }</code> cursor. Reconnect uses a cursor or a complete state snapshot; it never relies on wall-clock timestamps.</li><li><strong>Mutations are idempotent.</strong> A lost response must not cause a second turn, duplicate approval, or repeated attachment mutation.</li><li><strong>A binding is replaceable.</strong> A client may select a binding based on its capabilities without changing the Agent Host&#39;s behavior.</li><li><strong>The Host is headless.</strong> Session and turn admission, the turn queue, the approval broker, the event log, and the snapshot builder live in a module with no Electron dependency. Desktop IPC, the local MCP control plane, RACP, and the messaging integration are callers of that one module.</li><li><strong>A remote session lives entirely on its Host.</strong> Its transcript, tools, workspace, permissions, and provider secrets are on the machine that runs the Host. The desktop displays and controls it; it never executes remote tools locally or stores the remote transcript beyond display state.</li><li><strong>User-local by construction.</strong> No project-operated service is in the control path. Every credential is issued by the user&#39;s own Host, and the only outbound connections are to the user&#39;s SSH hosts, the messaging channels the user configured, the model providers the user configured, and the read-only GitHub Releases download of <code>pi-host</code> (D385).</li></ol><h2 id="_3-reference-implementations-and-design-inputs" tabindex="-1">3. Reference implementations and design inputs <a class="header-anchor" href="#_3-reference-implementations-and-design-inputs" aria-label="Permalink to &quot;3. Reference implementations and design inputs&quot;">​</a></h2><p>The design takes patterns from, but does not adopt wholesale, the following public projects:</p><ul><li><a href="https://github.com/openai/codex/blob/main/codex-rs/app-server/README.md" target="_blank" rel="noreferrer">OpenAI Codex App Server</a> uses JSON-RPC-shaped messages over stdio and WebSocket, separates threads, turns, and items, streams lifecycle notifications, and sends approval requests from the server to the client.</li><li><a href="https://github.com/microsoft/vscode-docs/blob/main/docs/agents/concepts/agent-host.md" target="_blank" rel="noreferrer">VS Code Agent Host</a> puts the Agent in a dedicated host, keeps the host as the state source, supports remote JSON-RPC over WebSocket, and resynchronizes clients with snapshots and ordered actions.</li><li><a href="https://github.com/modelcontextprotocol/modelcontextprotocol/blob/main/docs/specification/2025-03-26/basic/transports.mdx" target="_blank" rel="noreferrer">MCP transports</a> demonstrate JSON-RPC over local stdio and independent-process HTTP with optional server-sent events.</li><li><a href="https://github.com/a2aproject/A2A/blob/main/docs/specification.md" target="_blank" rel="noreferrer">Google A2A</a> separates a canonical data model and abstract operations from JSON-RPC, gRPC, and HTTP/JSON bindings.</li></ul><p>The SSH-tunnel topology follows the VS Code Remote-SSH and JetBrains Gateway model: the server side is bootstrapped over the user&#39;s own SSH session and the client reaches it through a forwarded loopback port.</p><p>PI-Desktop does not revive the withdrawn subagent A2A/Peer channel. ADR 0165 continues to govern <code>Task</code> subagent coordination. The separate official Session Orchestrator plugin may use the host-owned, local-only collaboration ledger defined by ADR 0239; that reviewed path is not a remote Gateway or A2A transport and does not change the remote-control target.</p><h2 id="_4-logical-components" tabindex="-1">4. Logical components <a class="header-anchor" href="#_4-logical-components" aria-label="Permalink to &quot;4. Logical components&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Component</th><th>Responsibility</th><th>Must not own</th></tr></thead><tbody><tr><td>Remote Client</td><td>Render state, send user intent, answer approvals and input requests</td><td>Workspace authority, provider credentials, final permission decisions, the prompt queue</td></tr><tr><td>Desktop RACP client adapter (Electron Main)</td><td>Present a remote Host to the renderer through the existing <code>lib/api.ts</code> surface; own SSH bootstrap, pairing, and port forwarding</td><td>A second transcript store; local execution of remote tools</td></tr><tr><td>Agent Host</td><td>Own sessions, turns, the per-session turn queue, event cursors, attachment records, tool execution, and lifecycle</td><td>Browser presentation state</td></tr><tr><td>Headless Agent Host module (<code>packages/agent-host</code>)</td><td>Own session/turn admission, the turn queue, the approval broker, the in-memory event log, and the snapshot builder; expose one typed API to desktop IPC, local MCP, RACP, and integrations</td><td>Electron, renderer, or transport dependencies; a second permission or persistence implementation</td></tr><tr><td><code>pi-host</code> headless bundle</td><td>Run the module, the Node pi sidecar, and Rust host-core on a remote machine, bound to loopback, at the same version as the desktop, downloaded from GitHub Releases by the bootstrap script</td><td>A desktop UI, plugin panels, another Host&#39;s secrets</td></tr><tr><td>Messaging integration adapter</td><td>Subscribe to host-scope events in the Host process and relay redacted summaries to outbound channels; map a fixed command vocabulary to turn and approval operations</td><td>Its own permission policy, an inbound listener, raw transcript content</td></tr><tr><td>Self-hosted Gateway (unscheduled)</td><td>Admit Host-issued device credentials, authorize routing, maintain Host links, rate-limit, audit, buffer attachment uploads transiently, and (reserved) push redacted summaries</td><td>Provider secrets, durable transcript truth, arbitrary host-core access, attachment bytes beyond the upload window</td></tr><tr><td>Node pi sidecar</td><td>Run the pi Agent loop and provider streams</td><td>Remote authentication, workspace policy, secret storage</td></tr><tr><td>Rust host-core</td><td>Own SQLite, tools, workspace boundaries, permissions and the pending permission table, secrets, and durable local records</td><td>Public network listeners</td></tr></tbody></table><p>In the first phase the logical Agent Host is Electron Main hosting the headless module beside its supervised sidecars. The <code>pi-host</code> bundle runs the same module and supervision on a remote machine. The external contract is the same in both deployments.</p><h2 id="_5-deployment-topologies" tabindex="-1">5. Deployment topologies <a class="header-anchor" href="#_5-deployment-topologies" aria-label="Permalink to &quot;5. Deployment topologies&quot;">​</a></h2><h3 id="_5-1-current-local-desktop" tabindex="-1">5.1 Current local desktop <a class="header-anchor" href="#_5-1-current-local-desktop" aria-label="Permalink to &quot;5.1 Current local desktop&quot;">​</a></h3><p>This topology is unchanged:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>PI-Desktop</span></span>
<span class="line"><span>├── Electron Main</span></span>
<span class="line"><span>│   ├── Renderer</span></span>
<span class="line"><span>│   ├── Node pi sidecar</span></span>
<span class="line"><span>│   └── Rust host-core</span></span>
<span class="line"><span>└── local MCP control plane (optional, loopback-only)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><p>The local MCP endpoint remains governed by ADR 0203. It is not a remote Gateway and cannot be configured to bind a LAN or public interface.</p><h3 id="_5-2-remote-host-over-an-ssh-tunnel-first-remote-topology" tabindex="-1">5.2 Remote Host over an SSH tunnel (first remote topology) <a class="header-anchor" href="#_5-2-remote-host-over-an-ssh-tunnel-first-remote-topology" aria-label="Permalink to &quot;5.2 Remote Host over an SSH tunnel (first remote topology)&quot;">​</a></h3><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>PI-Desktop (Remote Client)                    Remote machine</span></span>
<span class="line"><span>├── Renderer ── lib/api.ts ─┐                 ┌── pi-host (headless Agent Host)</span></span>
<span class="line"><span>├── Electron Main           │ RACP-WS over    │   ├── packages/agent-host</span></span>
<span class="line"><span>│   ├── RACP client adapter ┼─ SSH port ──────┼──▶│   ├── Node pi sidecar</span></span>
<span class="line"><span>│   └── local sessions      │ forward         │   │   └── Rust host-core (loopback)</span></span>
<span class="line"><span>│       (unchanged)         │                 │   └── workspace, ~/.agents, secrets</span></span>
<span class="line"><span>└── Rust host-core (local)  │                 └── sshd (user&#39;s own keys and config)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p>Bootstrap runs over the user&#39;s own SSH session, never over RACP:</p><ol><li>The desktop opens SSH with the user&#39;s existing configuration and keys. A login password MAY be supplied instead of a key (ADR 0293): it is handed to the <code>ssh</code> client through an askpass helper, never as an argument, and is stored encrypted in the desktop&#39;s secure storage so the host can reconnect after a restart.</li><li>It uploads a small bootstrap script that downloads the <code>pi-host</code> bundle for the remote platform at the desktop&#39;s version from GitHub Releases, verifies the published SHA-256, and installs it under the user&#39;s home. A machine without outbound access to GitHub cannot be bootstrapped in the first version.</li><li>It starts <code>pi-host</code> bound to loopback and receives a single-use pairing token over the SSH channel.</li><li>It forwards a local port to the Host&#39;s loopback port and connects <code>RACP-WS</code> with the header profile, exchanging the pairing token for a device token that the desktop stores in its secure storage.</li><li>The Host records the desktop device as <code>owner</code> of that Host.</li></ol><p>Provider configuration for the remote Host is written over the same SSH channel by the bootstrap step, as Host-local configuration. It never crosses RACP, so the secret boundary in <code>05-security/02-remote-control-security.md</code> §7 is unchanged.</p><p>The Host binds loopback only. Plain <code>ws://</code> is accepted on that port only when both the bind address and the peer address are loopback and a valid device token is presented, because the SSH channel provides confidentiality and the SSH login already proves shell access to the machine. A non-loopback bind requires TLS and a device token exactly as before.</p><h3 id="_5-3-self-hosted-gateway-unscheduled" tabindex="-1">5.3 Self-hosted Gateway (unscheduled) <a class="header-anchor" href="#_5-3-self-hosted-gateway-unscheduled" aria-label="Permalink to &quot;5.3 Self-hosted Gateway (unscheduled)&quot;">​</a></h3><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Browser / Native Client ── HTTPS or WSS ── Remote Gateway</span></span>
<span class="line"><span>                                             │</span></span>
<span class="line"><span>                          outbound WSS Host  │</span></span>
<span class="line"><span>                          link (relay)       │</span></span>
<span class="line"><span>                                             ▼</span></span>
<span class="line"><span>                                      Agent Host</span></span>
<span class="line"><span>                                      ├── pi sidecar</span></span>
<span class="line"><span>                                      └── Rust host-core</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><p>The Agent Host opens the outbound connection. The Gateway does not require an inbound port on the user&#39;s desktop and does not turn the host-core process into a public service. The Host link is a relay profile (<code>03-runtime/19-remote-agent-control-protocol.md</code> §11.4): it multiplexes logical client connections so that server-initiated approval requests reach the right client and attachment bytes reach the Host without an inbound port. The Gateway keeps a short-lived route from a host identity to a live connection and may queue control-plane metadata, but it does not queue non-idempotent turn commands while the Agent Host is offline. PI does not operate a Gateway: if this topology is ever scheduled, the user runs it on their own infrastructure and it admits clients with Host-issued device credentials (D385). It is specified so the contract does not drift; it is not scheduled.</p><h2 id="_6-ownership-and-authority" tabindex="-1">6. Ownership and authority <a class="header-anchor" href="#_6-ownership-and-authority" aria-label="Permalink to &quot;6. Ownership and authority&quot;">​</a></h2><h3 id="_6-1-agent-host-ownership" tabindex="-1">6.1 Agent Host ownership <a class="header-anchor" href="#_6-1-agent-host-ownership" aria-label="Permalink to &quot;6.1 Agent Host ownership&quot;">​</a></h3><p>The Agent Host is authoritative for:</p><ul><li>session and turn state, including the per-session turn queue;</li><li>the current operating mode and permission mode, and the remote permission ceiling applied to remote-initiated turns;</li><li>the workspace/project binding;</li><li>epoch and sequence allocation and replay retention;</li><li>approval and input request lifecycle, including requests raised before a client attached;</li><li>attachment ownership and hash verification;</li><li>tool execution and result classification; and</li><li>crash, abort, and no-replay behavior.</li></ul><p>The Gateway and Remote Client must treat host responses as authoritative. A client-side optimistic state is display-only.</p><h3 id="_6-2-client-roles" tabindex="-1">6.2 Client roles <a class="header-anchor" href="#_6-2-client-roles" aria-label="Permalink to &quot;6.2 Client roles&quot;">​</a></h3><p>An authenticated principal receives one or more scoped roles per session:</p><ul><li><code>viewer</code>: read session metadata, history, and subscribe to events;</li><li><code>controller</code>: start or queue a turn, stop, interrupt, or cancel a turn, answer input requests, and upload input;</li><li><code>approver</code>: resolve tool and contract approvals allowed by policy; and</li><li><code>owner</code>: manage session membership, revoke clients, and archive a session.</li></ul><p>Roles are additive but never bypass host policy. A <code>controller</code> cannot approve its own request unless the policy explicitly grants the <code>approver</code> role.</p><p>Only one turn may run in a session. Multiple viewers are allowed. Queued turns are Host state shared by every client, including the local desktop. Concurrent mutations are serialized by the Agent Host and rejected with a conflict when their expected session revision is stale. A turn started by a Gateway-routed principal runs under the Host&#39;s remote permission ceiling (<code>03-runtime/19-remote-agent-control-protocol.md</code> §7.3). A desktop device paired through the SSH bootstrap holds <code>owner</code> and is exempt from the ceiling, because SSH access to the machine already exceeds anything the ceiling withholds; the Host policy <code>applyCeilingToPairedDevices</code> (default off) re-applies it.</p><h3 id="_6-3-remote-session-ownership-split" tabindex="-1">6.3 Remote session ownership split <a class="header-anchor" href="#_6-3-remote-session-ownership-split" aria-label="Permalink to &quot;6.3 Remote session ownership split&quot;">​</a></h3><p>For a session on a remote Host:</p><ul><li><strong>On the remote Host</strong>: transcript and SQLite, turns and the queue, the built-in tool catalog and workspace boundaries, permissions and session grants, provider secrets, skills and subagent definitions from that machine&#39;s <code>~/.agents</code>, MCP servers configured on that Host, and scheduled tasks.</li><li><strong>On the desktop</strong>: the window and shell, local sessions, the settings UI for the local application, plugin panels, the browser preview, and the display of notifications.</li><li><strong>Relayed from the desktop</strong>: the desktop advertises its user-configured MCP servers and the plugin tools that do not require the session workspace through <code>tools/advertise</code>; they appear in the remote session&#39;s catalog as relayed tools and execute on the desktop through the <code>tool/execute</code> server request under the desktop&#39;s own plugin permissions (<code>03-runtime/19-remote-agent-control-protocol.md</code> §9.4). Plugin tools that require workspace or filesystem access are excluded, because they would act on the desktop&#39;s filesystem while the session root is on the Host.</li><li><strong>Work panel</strong>: file listing, file reads, and the working-tree diff use the remote-host profile operations in <code>03-runtime/19-remote-agent-control-protocol.md</code> §6.2 against the remote session root; the terminal runs on the remote machine through the <code>terminal/*</code> operations with a bounded replay ring; the browser preview stays local.</li></ul><h3 id="_6-4-gateway-ownership-unscheduled" tabindex="-1">6.4 Gateway ownership (unscheduled) <a class="header-anchor" href="#_6-4-gateway-ownership-unscheduled" aria-label="Permalink to &quot;6.4 Gateway ownership (unscheduled)&quot;">​</a></h3><p>The Gateway owns identity-to-host routing, not workspace state. It may store:</p><ul><li>host registration and connection health;</li><li>user/session membership and revocation metadata;</li><li>rate-limit counters;</li><li>audit metadata;</li><li>short-lived transport buffers, including attachment bytes only until the Host confirms the upload or it expires; and</li><li>(reserved) push-notification registrations for redacted approval and turn summaries.</li></ul><p>It must not persist provider API keys, raw tool arguments, raw tool results, or full transcripts unless a separate product decision explicitly grants that retention.</p><p>A Gateway is self-hosted and has no identity source of its own: it admits clients with device credentials issued by the user&#39;s Host (D385); see <code>05-security/02-remote-control-security.md</code> §3.1. OIDC federation and the pi-backend account service are out of scope.</p><h2 id="_7-transport-profiles" tabindex="-1">7. Transport profiles <a class="header-anchor" href="#_7-transport-profiles" aria-label="Permalink to &quot;7. Transport profiles&quot;">​</a></h2><p>The protocol specification defines one abstract operation model and these bindings:</p><table tabindex="0"><thead><tr><th>Profile</th><th>Intended client</th><th>Direction</th><th>Status</th></tr></thead><tbody><tr><td>Local stdio JSON-RPC</td><td>Electron Main and sidecars</td><td>Full duplex</td><td>Existing; unchanged</td></tr><tr><td><code>RACP-WS</code> JSON-RPC over WSS</td><td>Desktop as Remote Client over SSH, native clients, Electron</td><td>Full duplex</td><td>Normative v1 binding; first deployed over the SSH tunnel</td></tr><tr><td><code>RACP-HTTP</code> HTTP/JSON + SSE</td><td>Browser and simple integrations</td><td>Commands plus server stream</td><td>Browser profile; unscheduled</td></tr><tr><td><code>RACP-GRPC</code> gRPC over TLS</td><td>Native service clients</td><td>Unary plus server stream</td><td>Reserved; not in v1 conformance</td></tr><tr><td>Host link <code>racp-hostlink.v1</code></td><td>Gateway to Host</td><td>Multiplexed full duplex</td><td>Relay profile over <code>RACP-WS</code> framing; unscheduled with the Gateway</td></tr></tbody></table><p>The binding-neutral contract is specified in <code>03-runtime/19-remote-agent-control-protocol.md</code>. A binding may be added only when it preserves the same state transitions, error meaning, authorization scope, event ordering, and cursor behavior, and it joins the conformance fixture before it ships.</p><h2 id="_8-session-and-event-synchronization" tabindex="-1">8. Session and event synchronization <a class="header-anchor" href="#_8-session-and-event-synchronization" aria-label="Permalink to &quot;8. Session and event synchronization&quot;">​</a></h2><p>Each session has a Host-generated <code>epoch</code> and, inside it, a monotonically increasing <code>sequence</code> for durable events. The pair is never reused for that session. Every event carries:</p><ul><li><code>scope</code> (<code>session</code> or <code>host</code>);</li><li><code>sessionId</code> and optional <code>turnId</code>;</li><li><code>eventId</code>;</li><li><code>epoch</code>, plus <code>sequence</code> for durable events or <code>afterSequence</code> for ephemeral ones;</li><li><code>revision</code>;</li><li>semantic <code>kind</code>;</li><li>optional <code>parentToolCallId</code> and <code>agentName</code> for subagent rows; and</li><li>a typed payload that carries the shared normalized <code>AgentEvent</code> for turn-scoped kinds.</li></ul><p>Durable events are item and lifecycle boundaries, approvals, inputs, and session changes. Ephemeral events are streaming deltas, tool progress, and activity phases; they are delivered live, never sequenced, never replayed, and never counted against the replay window. A snapshot&#39;s <code>activeItems</code> carry what the deltas accumulated, so a reconnecting client loses nothing it could not rebuild.</p><p>The first subscription response includes a snapshot and its <code>cursor</code>. Subsequent durable events are ordered by <code>sequence</code>. A reconnect supplies <code>after</code>:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>cursor in the current epoch and retained -&gt; replay durable events with sequence &gt; after</span></span>
<span class="line"><span>epoch changed or cursor evicted          -&gt; resync.required + current snapshot</span></span>
<span class="line"><span>cursor ahead of the Host                 -&gt; invalid cursor; client must refresh the snapshot</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>The first implementation keeps the durable log in Agent Host process memory; a Host restart starts a new epoch and every client resynchronizes from a snapshot. Rust host-core keeps exclusive SQLite ownership (frozen decision 12); persisting the log there would need its own ADR. The Gateway must not renumber events. If the Gateway reconnects a Host link, each logical client connection resumes from its last acknowledged cursor. A client may render events optimistically, but it must drop duplicates, pause on a durable gap, and apply a snapshot before continuing.</p><h2 id="_9-turn-and-approval-path" tabindex="-1">9. Turn and approval path <a class="header-anchor" href="#_9-turn-and-approval-path" aria-label="Permalink to &quot;9. Turn and approval path&quot;">​</a></h2><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Client -&gt; initialize / attach / subscribe</span></span>
<span class="line"><span>Client -&gt; turn/start (idempotency key, admission)</span></span>
<span class="line"><span>Host   -&gt; accepted turn + cursor (turn.queued when queued)</span></span>
<span class="line"><span>Host   -&gt; turn.started, ordered item events, ephemeral deltas</span></span>
<span class="line"><span>Host   -&gt; approval/request or input/request (when policy requires a decision)</span></span>
<span class="line"><span>Client -&gt; approval/respond | input/respond</span></span>
<span class="line"><span>Host   -&gt; tool and item events</span></span>
<span class="line"><span>Host   -&gt; turn.completed | turn.interrupted | turn.failed</span></span>
<span class="line"><span>Host   -&gt; next queued turn starts</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br></div></div><p><code>turn/start</code> is an admission call. It returns quickly with a <code>turnId</code>; it must not hold an HTTP request open until model execution ends. With <code>admission: &quot;queue&quot;</code> the Host places the turn in its per-session queue and releases it after the active turn&#39;s terminal event and durable finalization; when terminal-event draining observes a busy runtime, the desktop must wake the queue again after releasing turn ownership and its finalization guard. Shutdown does not wake queued work. The queue is Host state, persisted by host-core, restored after a restart, and held until a controller attaches, so the local desktop and every remote client see the same pending prompts. The turn continues after the client disconnects. <code>turn/stop</code> is the graceful stop at the next assistant/tool boundary; <code>turn/interrupt</code> is the immediate abort; both are explicit and idempotent. A transport disconnect alone never means stop or interrupt.</p><p>Permission, Plan, Goal, and input rules remain host-owned. A remote client receives the same decision vocabulary the desktop offers: <code>allow-once</code>, <code>allow-session</code>, and <code>deny</code> for tools; <code>approve</code> with an explicit permission mode, or <code>reject</code>, for Plan and Goal contracts; and per-question answers or skips for asktool prompts. A Plan or Goal approval is a session-level transition that outlives the submitting turn. A remote client cannot select an unadvertised permission mode or execute a tool directly; changing a durable mode uses <code>session/configure</code> from the remote-host profile and is idle-only, exactly as locally. A Gateway-routed turn never exceeds the Host&#39;s remote permission ceiling. Approval lifetime is Host policy: the local default stays 120 seconds then deny, and while a remote subscriber is attached the default is 30 minutes (D375), bounded and operator-adjustable, because a remote approver is rarely at the keyboard.</p><h2 id="_10-failure-and-recovery-model" tabindex="-1">10. Failure and recovery model <a class="header-anchor" href="#_10-failure-and-recovery-model" aria-label="Permalink to &quot;10. Failure and recovery model&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Failure</th><th>Required behavior</th></tr></thead><tbody><tr><td>Client disconnect</td><td>Keep the turn running; retain durable events within the replay window</td></tr><tr><td>Client reconnect</td><td>Authenticate again, attach, replay from cursor or return snapshot</td></tr><tr><td>SSH tunnel drop</td><td>The desktop adapter re-establishes the forward and resumes by cursor; the remote turn continues</td></tr><tr><td>Gateway disconnect</td><td>Agent Host retries the Host link with bounded exponential backoff; local turns continue</td></tr><tr><td>Agent Host unavailable</td><td>Reject new mutations with <code>AGENT_UNAVAILABLE</code>; never replay them automatically</td></tr><tr><td>Agent Host restart</td><td>New epoch; clients resync from a snapshot; the persisted queue is restored in order and held until a controller attaches; nothing already started is replayed</td></tr><tr><td>Agent Host crash</td><td>Existing host recovery rules apply; interrupted work is never replayed automatically</td></tr><tr><td>Duplicate mutation</td><td>Return the original idempotent result for the same principal and key</td></tr><tr><td>Durable event gap</td><td>Stop applying events and request a snapshot; never guess intermediate state</td></tr><tr><td>Slow client</td><td>Drop ephemeral events first; disconnect with a resumable cursor before losing a durable event</td></tr><tr><td>Expired approval</td><td>Return <code>APPROVAL_EXPIRED</code>; do not execute the tool</td></tr></tbody></table><h2 id="_11-migration-boundary" tabindex="-1">11. Migration boundary <a class="header-anchor" href="#_11-migration-boundary" aria-label="Permalink to &quot;11. Migration boundary&quot;">​</a></h2><p>The first implementation delivers the headless Agent Host module (<code>packages/agent-host</code>). It owns session and turn admission, the per-session turn queue, the approval broker, the in-memory event log with epochs, and the snapshot builder, and it has no Electron dependency. Electron Main hosts the module and its IPC handlers become adapters over it; that move may be incremental, with the module first wrapping the existing handlers and then absorbing them. The RACP server binds to the module, never to renderer IPC, <code>host.proxy</code>, or Rust host-core RPC from a network listener. The local MCP control plane (ADR 0203) is unchanged now and may later move onto the same module.</p><p>Two local changes accompany the module: Rust host-core exposes the pending permission table through a <code>permissions.pending</code> read so late-attaching clients receive open requests, and the renderer&#39;s in-memory prompt queue is replaced by the Host-owned turn queue, persisted by host-core under its own ADR and schema bump (D375), before more than one client can control a session.</p><p>The SSH-tunnel milestone adds two pieces without changing the wire contract: the <code>pi-host</code> bundle, which packages the module with the Node sidecar and the platform&#39;s host-core binary, and the desktop RACP client adapter, which sits under <code>lib/api.ts</code> so the renderer needs no transport knowledge. The same milestone carries the <code>tools/advertise</code> / <code>tool/execute</code> relay and the <code>terminal/*</code> operations. The messaging integration is a further caller of the module inside the Host process and needs no transport at all.</p><p>The migration is complete when the local desktop, a <code>pi-host</code> bundle, and, once scheduled, a Gateway route expose the same session/turn/event behavior.</p><h2 id="_12-acceptance-criteria" tabindex="-1">12. Acceptance criteria <a class="header-anchor" href="#_12-acceptance-criteria" aria-label="Permalink to &quot;12. Acceptance criteria&quot;">​</a></h2><ol><li>A remote client can attach to a session without taking ownership of its workspace path or secrets.</li><li>A turn continues after the initiating client disconnects.</li><li>A second client can observe the same turn and receive the same ordered durable events.</li><li>Reconnection either replays every durable event after the supplied cursor or returns a complete snapshot with an explicit resync reason; streaming deltas are recovered from the snapshot&#39;s active items.</li><li>Every mutation is idempotent and scoped to an authenticated principal.</li><li>Permission, contract, and input requests can be answered remotely with the full local decision vocabulary without bypassing host policy, and a client that attaches late sees requests raised before it attached.</li><li>Rust host-core remains unreachable from the network, on the desktop and on a remote machine.</li><li>Every shipped binding produces equivalent domain results for the same command sequence.</li><li>The current local stdio JSON-RPC and loopback MCP paths remain unchanged.</li><li>A Gateway-routed turn never runs above the Host&#39;s remote permission ceiling.</li><li>The headless Agent Host module runs its test suite without Electron, and desktop IPC, local MCP, and RACP call the same module.</li><li>A remote session&#39;s transcript, tools, workspace, and secrets live on the remote Host; the desktop stores nothing from the remote workspace beyond display state.</li><li>A remote session&#39;s catalog contains the remote Host&#39;s tools plus the tools the desktop advertised for relay; a relayed tool executes on the desktop and never against the remote workspace.</li><li>A session terminal runs on the remote machine inside the session root and opens only for principals allowed by policy.</li></ol><h2 id="_13-amendment-history" tabindex="-1">13. Amendment history <a class="header-anchor" href="#_13-amendment-history" aria-label="Permalink to &quot;13. Amendment history&quot;">​</a></h2><p>D374 (2026-09-10) amended the D373 target before implementation: one normative v1 binding with a browser profile and a reserved gRPC binding, the headless Agent Host module as the first deliverable, the Host-owned turn queue, <code>{ epoch, sequence }</code> cursors with ephemeral deltas, the full local approval vocabulary, the Host link relay profile, the remote permission ceiling, and the remote approval lifetime policy.</p><p>D375 (2026-09-10) re-sequenced the topologies around recorded demand: the SSH-tunnel remote Host with the desktop as Remote Client ships first, the outbound messaging integration second, and the Gateway and browser topologies stay specified but unscheduled. It added the <code>pi-host</code> bundle, the desktop RACP client adapter, the SSH bootstrap and loopback rule, the remote session ownership split, and the ceiling exemption for SSH-paired owner devices. Its design-gate answers, recorded the same day, put the reverse tool relay and the terminal in R2, download <code>pi-host</code> from GitHub Releases, persist the turn queue in host-core, default the remote approval lifetime to 30 minutes, make the paired-device exemption a Host policy, and fix the Gateway identity source to the PI account service.</p><p>D385 (2026-09-10) made remote control user-local by construction: no project-operated identity or account service, Host-issued device credentials everywhere, and a Gateway only as a self-hosted relay.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("spec/02-architecture/05-remote-agent-control.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _05RemoteAgentControl = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _05RemoteAgentControl as default
};
