import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0206: Extend provider retries and show bounded progress","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0206-ten-provider-retries-and-progress-status.md","filePath":"adr/0206-ten-provider-retries-and-progress-status.md","lastUpdated":1789906805000}');
const _sfc_main = { name: "adr/0206-ten-provider-retries-and-progress-status.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0206-extend-provider-retries-and-show-bounded-progress" tabindex="-1">ADR 0206: Extend provider retries and show bounded progress <a class="header-anchor" href="#adr-0206-extend-provider-retries-and-show-bounded-progress" aria-label="Permalink to &quot;ADR 0206: Extend provider retries and show bounded progress&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-10</li><li>Deciders: PI-Desktop core</li><li>Related: D378, E2E-096, E2E-149, ADR 0091, ADR 0128, ADR 0196</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><h3 id="amendment-successful-response-boundary-issue-699-2026-09-20" tabindex="-1">Amendment: successful response boundary (issue #699, 2026-09-20) <a class="header-anchor" href="#amendment-successful-response-boundary-issue-699-2026-09-20" aria-label="Permalink to &quot;Amendment: successful response boundary (issue #699, 2026-09-20)&quot;">​</a></h3><p>Desktop fault injection reproduced a long task stopping on its eleventh independent network failure after ten successful recoveries and tool calls. The budget previously survived successful model responses for the whole user turn. Both retry classes now reset after a complete successful model response, including a tool-call response, in the main runtime and builtin subagents. Partial output, response headers, and phase changes do not reset the counters. The ten-retry bound, separate classes, cancellation, and failed-request-only replay remain unchanged. Exhaustion diagnostics read the appropriate counter, not temporary activity state. This narrows the budget scope in decision 1 below without introducing a new setting or changing persisted contracts.</p><p>PI-Desktop already owns provider retries so request setup and mid-stream failures share one counter and pi-ai does not multiply attempts through a nested retry loop. The current budgets of five rate-limit retries and four other transient retries still surface short provider outages earlier than the product&#39;s target behavior. The active-turn row also shows only the retry number, so it does not tell the user how long the current wait is or where the retry sits in the budget.</p><h3 id="amendment-opt-in-infinite-provider-retry" tabindex="-1">Amendment: opt-in infinite provider retry <a class="header-anchor" href="#amendment-opt-in-infinite-provider-retry" aria-label="Permalink to &quot;Amendment: opt-in infinite provider retry&quot;">​</a></h3><p>The default ten-retry budgets remain unchanged. AppSettings may opt into <code>infiniteProviderRetry</code>; the runtime then skips only the retry-count ceiling for admitted network/transient provider failures in the main session and builtin subagents. Backoff, server retry hints, response-only reset, cancellation, terminal classification, and failed-request-only replay remain unchanged. The Settings copy warns that the user can stop the turn but API usage may continue while the switch is enabled. One-shot completions keep the bounded budget.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><code>PROVIDER_RATE_LIMITED</code> and the admitted non-429 transient provider errors each receive a shared budget of ten retries after the initial provider attempt. Setup and stream failures continue to draw from the same budget within their class, for at most eleven provider attempts. The two classes remain separate.</li><li><code>PROVIDER_RETRY_MAX_RETRIES</code> in <code>packages/shared</code> is the single budget constant used by the runtime and renderer. The main session, builtin subagents, and one-shot composer enhancement continue to use the same retry budgets and classifications. pi-ai&#39;s nested retry remains disabled.</li><li>Delay behavior is unchanged except that the non-429 schedule remains at its 8-second cap for retries after the first four waits. Provider headers, the 30-second 429 cap, abortability, failed-request-only replay, and terminal diagnostics remain as defined by ADR 0091 and ADR 0128.</li><li>The active-turn retry status uses the current <code>retryDelayMs</code> and <code>since</code> to render a whole-second countdown and shows the shared budget, for example <code>Retrying in 0s · attempt 9/10</code>. It remains a compact status row and does not create intermediate transcript errors.</li><li>Authentication, model-selection, malformed-request, context, mutation, compaction, and other non-provider recovery policies are unchanged.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Short provider outages can recover through a longer, still bounded same-turn window without duplicating assistant messages or lifecycle errors.</li><li>Persistent provider failures take longer to surface, but remain abortable and end with one structured terminal error after the retry budget is exhausted.</li><li>Users can see the active wait and the retry budget in the same status row, including during the final retry.</li><li>No host protocol, storage schema, provider configuration, or nested SDK retry behavior changes.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><h3 id="increase-pi-ai-s-maxretries" tabindex="-1">Increase pi-ai&#39;s <code>maxRetries</code> <a class="header-anchor" href="#increase-pi-ai-s-maxretries" aria-label="Permalink to &quot;Increase pi-ai&#39;s \`maxRetries\`&quot;">​</a></h3><p>Rejected because nested retries would multiply attempts, obscure phase sharing, and reintroduce provider-controlled sleeping outside the runtime&#39;s abortable policy.</p><h3 id="use-one-budget-for-rate-limits-and-other-transient-failures" tabindex="-1">Use one budget for rate limits and other transient failures <a class="header-anchor" href="#use-one-budget-for-rate-limits-and-other-transient-failures" aria-label="Permalink to &quot;Use one budget for rate limits and other transient failures&quot;">​</a></h3><p>Rejected because rate limits and upstream outages need different delay scales, and one class should not consume the other&#39;s recovery budget.</p><h3 id="show-only-a-fixed-10-label" tabindex="-1">Show only a fixed <code>/10</code> label <a class="header-anchor" href="#show-only-a-fixed-10-label" aria-label="Permalink to &quot;Show only a fixed \`/10\` label&quot;">​</a></h3><p>Rejected because the status already carries the delay and start time needed for an accurate countdown; deriving the remaining seconds keeps the UI useful while waiting instead of showing stale information.</p><h2 id="references" tabindex="-1">References <a class="header-anchor" href="#references" aria-label="Permalink to &quot;References&quot;">​</a></h2><ul><li><code>docs/spec/03-runtime/02-agent-runtime.md</code> §5d</li><li><code>docs/spec/03-runtime/08-error-codes.md</code></li><li><code>docs/spec/06-delivery/04-e2e-test-plan.md</code> E2E-096, E2E-149</li><li><code>docs/spec/08-meta/decisions-log.md</code> D378</li><li><code>packages/shared/src/provider-retry.ts</code></li><li><code>packages/agent-runtime/src/provider-retry.ts</code></li><li><code>apps/desktop/src/components/ChatTranscript.tsx</code></li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0206-ten-provider-retries-and-progress-status.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0206TenProviderRetriesAndProgressStatus = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0206TenProviderRetriesAndProgressStatus as default
};
