import { ssrRenderAttrs, ssrInterpolate } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR message-quotes-and-side-chats: Message Quotes and Renderer-Owned Side Chats","description":"","frontmatter":{},"headers":[],"relativePath":"adr/message-quotes-and-side-chats.md","filePath":"adr/message-quotes-and-side-chats.md","lastUpdated":1789547461000}');
const _sfc_main = { name: "adr/message-quotes-and-side-chats.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-message-quotes-and-side-chats-message-quotes-and-renderer-owned-side-chats" tabindex="-1">ADR message-quotes-and-side-chats: Message Quotes and Renderer-Owned Side Chats <a class="header-anchor" href="#adr-message-quotes-and-side-chats-message-quotes-and-renderer-owned-side-chats" aria-label="Permalink to &quot;ADR message-quotes-and-side-chats: Message Quotes and Renderer-Owned Side Chats&quot;">​</a></h1><blockquote><p>Superseded by ADR 0268.</p></blockquote><ul><li>Status: Superseded by ADR 0268</li><li>Date: 2026-09-11</li><li>Deciders: PI-Desktop runtime and desktop UI maintainers</li><li>Amends: D209, D301</li><li>Preserves: D097, D128, D134, D154</li><li>Related: <a href="./../spec/04-ux/08-component-spec">04-ux/08-component-spec.md</a> · <a href="./../spec/04-ux/09-interaction-patterns">04-ux/09-interaction-patterns.md</a> · <a href="./../spec/04-ux/01-ui-ia">04-ux/01-ui-ia.md</a> · <a href="./../spec/06-delivery/04-e2e-test-plan">06-delivery/04-e2e-test-plan.md</a> · E2E-CHAT-quote-prefill, E2E-CHAT-side-chat-fork, E2E-CHAT-side-chat-stream, E2E-CHAT-side-chat-add-to-main, E2E-CHAT-side-chat-promote, E2E-CHAT-side-chat-close · Amendment: D-LOCAL-selection-overlay, E2E-CHAT-selection-markdown</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>A long conversation carries earlier prompts and answers the user wants to reuse. Copying them by hand or re-pasting into the composer is the only shipped path, and re-reading an answer to paraphrase it loses the exact wording the next turn should reference.</p><p>The existing <code>session.fork</code> (ADR 0023, D134) already produces an independent durable child from an anchor message, but every dialog that uses it activates the child. That is correct for a divergence the user wants to continue, and wrong for a side question: the fork replaces the visible main conversation, the main transcript leaves the screen, and coming back costs a session switch.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><p>Every user message and every assistant turn gains a <strong>Quote</strong> action in its hover action row, beside Copy, Edit, Delete, Fork, and Retry.</p></li><li><p>Quote inserts a Markdown blockquote of the message into the composer draft of the active session through the existing composer-prefill contract and then focuses the composer. It never sends a prompt, never creates a session, and writes nothing to the transcript. The excerpt is the live text selection when that selection is inside the clicked message row, otherwise the message&#39;s own text (for an assistant turn, its answer text).</p></li><li><p>Quote text is <code>&gt; </code> per excerpt line, one blank line, then the attribution line rendered from <code>chat.quoteSource</code> (&quot;Quoted from ${ssrInterpolate(_ctx.title)}&quot;; Chinese &quot;引用自 ${ssrInterpolate(_ctx.title)}&quot;), where the title is the source session&#39;s title. The excerpt is capped at 2000 characters with a trailing ellipsis. The quote is ordinary draft text: D209&#39;s smart Stop still restores an unanswered send and D301&#39;s per-session draft retention is unchanged. Quoting adds no composer chip kind and no file reference.</p></li><li><p><strong>Open side chat</strong> is offered on an assistant turn (fork anchored at that assistant message) and on a user message (fork anchored at that user message), with the tooltip and accessible name <code>chat.startSideChat</code>. It opens a renderer-only draft anchored at the selected message. Opening and typing do not call <code>session.fork</code>. First explicit Send with nonempty text forks the child without activating it, then sends through the existing prompt path. Concurrent sends share one creation. The resulting child is durable.</p></li><li><p>Before first Send the work panel uses a renderer-only draft id. Creation replaces that tab with <code>sidechat:&lt;childSessionId&gt;</code> in place, including the parent&#39;s retained panel context. Switching sessions or closing during creation must not reopen a panel. Draft text lives in the side-chat entry, survives panel switches and failed creation/sends, and is cleared after acceptance only if the user has not edited it in the meantime.</p></li><li><p>While a side chat is registered, the child&#39;s agent events are projected into a renderer-owned per-session transcript map from the same event stream the active transcript consumes (message and tool start / update / end), reusing the existing background-transcript reducer. The panel therefore streams live even though the child is never the app&#39;s active session.</p></li><li><p>Panel content: a compact header with the side-chat title, &quot;Add to main chat&quot; (<code>sideChat.addToMain</code>), &quot;Open as a conversation&quot; (<code>sideChat.openAsSession</code>), and the shared tab-close control; the child&#39;s transcript rendered with the existing transcript component; the existing permission card and ask card when the child requests approval or asks a question (both already resolve by the request&#39;s own session id), so a child that needs an answer never stalls behind an invisible prompt; and a compact input (placeholder <code>sideChat.placeholder</code>, empty-state line <code>sideChat.empty</code>) with Send and Stop, where Send targets the child session through the existing prompt path and Stop aborts that child session. The tab label reuses <code>sideChat.title</code>.</p></li><li><p>&quot;Add to main chat&quot; copies the side chat&#39;s newest assistant answer into the main conversation&#39;s composer as a quote of that answer, under the same quote contract and 2000-character cap as item 3, attributed to the side chat&#39;s title. It never sends automatically.</p></li><li><p>&quot;Open as a conversation&quot; activates the child through the normal session-selection path, so the full composer, prompt queue, and stop controls apply, and releases the side-chat registration and its tab.</p></li><li><p>Closing the tab removes the registration and its transcript projection. The unsent draft leaves no host record. An already created child session is not deleted and remains an ordinary session in the sidebar, session lists, and search.</p></li><li><p>Side-chat entries are removed when the tab closes, when the child is opened as a conversation, and when the parent or child session is deleted.</p></li><li><p>Boundaries: no host protocol bump (v11), no storage-schema change (v15), no new IPC channel, and no new permission. The feature reuses <code>session.fork</code>, the existing prompt and abort paths, and the existing work-panel surface. Side chats are renderer-owned and are not persisted across app restarts; the durable child session is.</p></li><li><p>i18n keys introduced: <code>chat.quote</code>, <code>chat.quoteSource</code>, <code>chat.startSideChat</code>, <code>sideChat.title</code>, <code>sideChat.sessionTitle</code>, <code>sideChat.placeholder</code>, <code>sideChat.empty</code>, <code>sideChat.addToMain</code>, <code>sideChat.openAsSession</code>. Send reuses <code>chat.send</code> and Stop reuses <code>chat.stopGenerating</code>.</p></li></ol><h2 id="preserved-behavior" tabindex="-1">Preserved behavior <a class="header-anchor" href="#preserved-behavior" aria-label="Permalink to &quot;Preserved behavior&quot;">​</a></h2><ul><li>D097 / D128 / D154 work-panel ownership is unchanged: the panel is still a docked, resizable, renderer-owned surface whose resources are opened by their trigger, keyed by identity, closed with right-then-left neighbor selection, and switched atomically per session. A <code>sidechat</code> tab is one more resource in that model, not a second panel.</li><li>D209 and D301 are amended only by extension: quoted text is plain draft text, so chips keep their canonical-path serialization, smart Stop keeps its pre-serialization undo snapshot, and per-session draft slots are unchanged. The quote prefill adds one draft source and changes none of those rules.</li><li>D134&#39;s fork/edit toolbar behavior is preserved for assistant turns: Fork, Edit, and Regenerate still use the same durable anchor and still activate their child.</li><li>The child session is an ordinary session for storage, listing, title, search, deletion, and remote-control purposes; no side-chat flag is persisted.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A user can reuse an exact earlier prompt or answer in the next turn without leaving the composer, and the quoted text is visible and editable before it is sent.</li><li>A side question streams beside the main conversation instead of replacing it; the visible session, its transcript, its scroll position, and its retained panel context stay where they were.</li><li>The panel loses nothing when the side chat closes: only the renderer-owned projection and registration go away, because the child is a durable session.</li><li>Side chats do not survive an app restart as panel state, so a user who wants to keep working with the child opens it from the sidebar as an ordinary conversation.</li><li>The 2000-character cap bounds how much of a quote can reach the composer and, after a send, the prompt context.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>A new composer chip kind or file reference for quotes:</strong> rejected. A chip would need a new serialization kind beside D209/D362 sentinel references for no user-visible gain, while plain blockquote text already survives draft retention and smart Stop unchanged.</li><li><strong>Reuse fork-and-activate for side chat:</strong> rejected; that is exactly &quot;Open as a conversation&quot;. Activating a fork to ask a side question would replace the visible main conversation, which is the behavior this feature exists to avoid.</li><li><strong>Host-owned side-chat storage and RPC:</strong> rejected. A durable child plus a temporary renderer-side projection needs no protocol, schema, or permission change.</li><li><strong>Poll <code>session.get</code> for the panel&#39;s content:</strong> rejected. The existing agent event stream already carries message and tool lifecycle, and the background-transcript reducer already turns it into rows.</li><li><strong>Auto-send after &quot;Add to main chat&quot;:</strong> rejected. The main composer stays a reviewable draft; only the user sends it.</li><li><strong>Keep the quote action at the end of the message only:</strong> rejected in D-LOCAL-selection-overlay. The action row is the right home for quoting a whole message, but quoting a sentence, a formula, or a table means reaching the end of the very message the user is still reading.</li></ul><h2 id="amendment-d-local-selection-overlay-2026-09-11-—-the-quote-affordance-follows-the-selection" tabindex="-1">Amendment (D-LOCAL-selection-overlay, 2026-09-11) — the quote affordance follows the selection <a class="header-anchor" href="#amendment-d-local-selection-overlay-2026-09-11-—-the-quote-affordance-follows-the-selection" aria-label="Permalink to &quot;Amendment (D-LOCAL-selection-overlay, 2026-09-11) — the quote affordance follows the selection&quot;">​</a></h2><p>Geometry and behavior follow the ChatGPT desktop app&#39;s selected-text overlay; the excerpt and the side-chat target stay PI-Desktop&#39;s own contracts (D-LOCAL-message-quotes).</p><ul><li>Decision 1 stands: every user message and every assistant turn keeps its Quote action for the whole message. A non-empty text selection inside a transcript row additionally floats <strong>one</strong> overlay above it.</li><li>Placement: the overlay is horizontally centered on the selection&#39;s visible rect, sits <code>SELECTION_QUOTE_GAP</code> (8 px) above that rect, and is clamped into its <strong>bounds</strong> on both axes with <code>SELECTION_QUOTE_MARGIN</code> (8 px) of slack. The bounds are the intersection of every clipping ancestor&#39;s rect (the transcript scroller is one) with the viewport, capped by the top of the docked composer — which floats over the transcript, so the scroller&#39;s own bottom edge is not the visible bottom. <code>Composer</code> publishes the <code>data-composer-dock</code> hook for that cap instead of the overlay guessing a class name. The overlay is portaled to <code>document.body</code> and lives in the body-portaled popover layer, so it never participates in the transcript&#39;s layout or scroll extent.</li><li>The selection has to live in <strong>one</strong> row: a drag that crosses rows raises no overlay, and a range whose ends leave the row is clamped back to that row&#39;s contents before it is quoted.</li><li>While the thread scrolls the overlay <strong>recomputes and follows</strong> the selection (it does not hide); a scroll of something unrelated to the selection leaves it alone. It also recomputes on selection change, double click, key up, pointer up, pointer cancel, and resize, at most once per animation frame, and it hides when a press lands outside it or when the selection collapses.</li><li>Actions, in the reference overlay&#39;s order: <strong>Add to chat</strong> (<code>chat.addToChat</code>) writes the excerpt into the active session&#39;s composer draft through D-LOCAL-message-quotes decision 3&#39;s contract and focuses the composer via the existing prefill path; <strong>Ask in side chat</strong> (<code>chat.askInSideChat</code>) opens a renderer-only side-chat draft and prefills the excerpt as a Markdown blockquote without sending; <strong>Copy</strong> reuses <code>chat.copy</code> and writes the Markdown to the clipboard. The side-chat action is disabled while the visible session is running, because the host refuses a fork mid-turn. Every action clears the native selection first, so the overlay does not outlive its own click. The overlay is not rendered in a read-only projection (D-LOCAL-message-quotes decision 12&#39;s rule extends to it).</li><li>Decision 3&#39;s excerpt is still recovered from the rendered DOM rather than from <code>Selection.toString()</code>. A range that touches a formula is expanded to the whole formula and quoted from KaTeX&#39;s <code>application/x-tex</code> annotation as <code>$…$</code> (inline) or <code>$$…$$</code> (display); a code block becomes a fence whose delimiter outgrows the longest backtick run inside it and keeps its language; inline code keeps its backticks; a table row becomes one <code>a | b</code> line; task checkboxes become <code>[x] </code>/<code>[ ] </code>; transcript chrome (action rows, copy buttons) is dropped while a file-reference chip keeps its code text. The row action and the overlay call that one recovery path, so they cannot drift.</li><li>Two deliberate deviations from the reference implementation, both because the quote lands in a <strong>Markdown draft</strong> PI-Desktop renders back: formulas use <code>$…$</code> / <code>$$…$$</code> (this renderer parses remark-math, not <code>\\(…\\)</code>), and table rows use <code>a | b</code> (Markdown) rather than tab-separated text. The reference&#39;s numbered <code>annotation</code> model is not adopted: D-LOCAL-message-quotes decision 3 keeps the excerpt as ordinary, visible, editable draft text.</li><li>The 2000-character cap, the <code>&gt; </code> blockquote, the <code>chat.quoteSource</code> attribution, the append-to-draft behavior, focus, and the no-send/no-session/ no-transcript-write boundaries are unchanged. i18n keys introduced: <code>chat.addToChat</code>, <code>chat.askInSideChat</code> (Copy reuses <code>chat.copy</code>).</li><li>Boundaries remain those of D-LOCAL-message-quotes decision 12: no host protocol bump, no storage schema change, no new IPC channel, no new permission. The overlay is renderer-only and holds no durable state beyond the existing composer draft.</li><li>Covered by E2E-CHAT-quote-prefill (overlay placement, follow, and actions), E2E-CHAT-selection-markdown (formula, table, code, and inline-code recovery), and E2E-CHAT-selection-side-chat (ask in side chat).</li></ul><h2 id="upstream-integration-amendment-2026-09-14" tabindex="-1">Upstream integration amendment (2026-09-14) <a class="header-anchor" href="#upstream-integration-amendment-2026-09-14" aria-label="Permalink to &quot;Upstream integration amendment (2026-09-14)&quot;">​</a></h2><p>Side-chat registrations use the upstream work-panel tab strip and launcher. Closing the final tab leaves the launcher open; closing a side chat removes only its registration/projection, never its durable child or unrelated tabs. The shared upstream delta-aware transcript reducer also feeds docked children. Registered transcripts and drafts persist across tab switches in renderer-owned state. Scroll position is component-local and may reset on remount. Side-chat registrations and drafts do not persist across application restarts.</p><h2 id="first-send-creation-amendment-issue-421" tabindex="-1">First-send creation amendment (Issue #421) <a class="header-anchor" href="#first-send-creation-amendment-issue-421" aria-label="Permalink to &quot;First-send creation amendment (Issue #421)&quot;">​</a></h2><p>Opening a side chat is a reversible draft interaction. Selection-overlay Ask in side chat prefills a Markdown blockquote and never sends automatically. Open as a conversation and Add to main chat are unavailable until a child exists. A successful fork followed by a failed send keeps that child for retry rather than creating another one. Closing after an explicit Send may retain the created child even if the provider rejects the prompt. Existing sessions are never deleted as cleanup. No IPC, host ownership, or database schema changes.</p><h3 id="side-chat-send-availability" tabindex="-1">Side-chat Send availability <a class="header-anchor" href="#side-chat-send-availability" aria-label="Permalink to &quot;Side-chat Send availability&quot;">​</a></h3><p>Drafts use the parent session&#39;s live availability. A running/busy parent disables first Send with a visible explanation; a read-only parent also disables it. The submission action shares the same gate and reports blocked programmatic submissions without creating a child or clearing text. After creation, the submission action checks the child&#39;s current availability again before sending. Existing children keep their normal queue and Stop behavior. Recovery re-enables Send automatically.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/message-quotes-and-side-chats.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const messageQuotesAndSideChats = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  messageQuotesAndSideChats as default
};
