import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0264: Host-Mediated File Actions Follow the Folder a View Is Browsing","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0264-host-mediated-actions-follow-the-browsed-folder.md","filePath":"adr/0264-host-mediated-actions-follow-the-browsed-folder.md","lastUpdated":1789483301000}');
const _sfc_main = { name: "adr/0264-host-mediated-actions-follow-the-browsed-folder.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0264-host-mediated-file-actions-follow-the-folder-a-view-is-browsing" tabindex="-1">ADR 0264: Host-Mediated File Actions Follow the Folder a View Is Browsing <a class="header-anchor" href="#adr-0264-host-mediated-file-actions-follow-the-folder-a-view-is-browsing" aria-label="Permalink to &quot;ADR 0264: Host-Mediated File Actions Follow the Folder a View Is Browsing&quot;">​</a></h1><ul><li><strong>Status</strong>: Accepted</li><li><strong>Date</strong>: 2026-09-15 (amends <a href="./0263-project-folder-roots-for-plugin-views">ADR 0263</a>)</li><li><strong>Related</strong>: <a href="./0241-vendored-updatable-file-view-plugin">ADR 0241</a> · <a href="./0249-chatgpt-style-logical-project-groups">ADR 0249</a> · <a href="./../spec/07-plugins/03-plugin-api">07-plugins/03-plugin-api</a></li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>ADR 0263 §6 let a contributed view browse one folder of a multi-folder project at a time, and made the view responsible for remembering which one. It said nothing about the two actions a view cannot perform itself: opening a file with the system default application (<code>fs.openDefault</code>) and revealing it in the OS file manager (<code>fs.reveal</code>). Both are host-mediated, and both were defined against the <strong>workspace root</strong>: the request carries a workspace-relative path, and the host resolves it there (ADR 0241, <code>plugin-runtime.ts</code> <code>resolveFsRequest</code>).</p><p>That contract was correct while the workspace root was also the only folder a view could browse. Once the view can select a sibling folder it is not: the entry list is relative to the <em>selected</em> folder, so a view that hands a relative path to the host has it resolved against the primary folder instead. Two folders holding a same-named file make that silent — opening <code>shared.txt</code> while browsing the second folder opens the first folder&#39;s file — and a file only the second folder holds reports not found. Both were reachable from the folder switcher.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><strong>A view addresses a file by an absolute path when the selected folder is not the primary one.</strong> A relative path keeps meaning the workspace root, so the primary-folder case is unchanged, byte for byte. This is the same rule ADR 0263 §4 already applies to chat file references: the address shape follows the folder that owns the file.</li><li><strong><code>fs.openDefault</code> and <code>fs.reveal</code> accept an absolute path inside any registered folder root of the open project.</strong> That root then becomes the containment base for the request, which ADR 0249 §5 already sanctions for tool requests. The relative form is matched against the declared scope of the folder that answered.</li><li><strong>The widening is limited to these two actions.</strong> They hand a path to the OS and return no file content; every other fs mode keeps the workspace root, so no plugin gains byte access to another folder. A plugin whose declared root is <code>userSelected</code> is unaffected: its base stays the directory the user chose.</li><li><strong>The guards are the ones every other request passes.</strong> The request still needs the declared <code>fs.read</code>; a path inside a project folder that the declared scope does not cover is refused rather than consented; credential and repository internals are refused exactly as under the workspace root; and containment is resolved through links, so a symlink or junction inside a registered folder cannot carry the request out of it.</li><li><strong>The end of a session is the end of its documents.</strong> When a view&#39;s open file closes — on a folder switch, or any other close — its editor buffer is cleared, not merely hidden: the pane must not keep a document from a folder the user has left, and a stale buffer must never be what a later save or a same-named file in another folder meets.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Opening or revealing a file behaves the same in every folder of a project: the action reaches the file the user right-clicked, in the folder they are browsing.</li><li>The group grant stays a user-authored list. A plugin cannot walk to a folder the user did not register, and cannot read a file it could not read before.</li><li>A view that ignores the rule (sends a relative path while browsing a sibling folder) still gets the old behaviour: it opens the primary folder&#39;s file. The contract for that case is therefore documented in the plugin API rather than left implicit.</li><li>Editing a file remains the plugin&#39;s own business: its reads and writes still go through its own Node fs under its own jail (ADR 0241), and only these two mediated actions go through the host.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><h3 id="widen-the-plugin-s-whole-fs-root-to-the-selected-folder" tabindex="-1">Widen the plugin&#39;s whole <code>fs</code> root to the selected folder <a class="header-anchor" href="#widen-the-plugin-s-whole-fs-root-to-the-selected-folder" aria-label="Permalink to &quot;Widen the plugin&#39;s whole \`fs\` root to the selected folder&quot;">​</a></h3><p>Rejected: the host does not know which folder a view is browsing — that choice is plugin-local by design (ADR 0263 §6) — and a per-call base would let any plugin claim another project folder for reading and writing, which is a grant ADR 0249 §5 deliberately tied to a user-visible tool request.</p><h3 id="have-the-view-send-a-path-relative-to-the-workspace-root" tabindex="-1">Have the view send a path relative to the workspace root <a class="header-anchor" href="#have-the-view-send-a-path-relative-to-the-workspace-root" aria-label="Permalink to &quot;Have the view send a path relative to the workspace root&quot;">​</a></h3><p>Rejected: only computable when the sibling folder sits inside the primary one. Sibling folders that are unrelated directories — the ordinary case for a project group — have no such path, and inventing <code>../</code> forms is exactly the escape the containment checks refuse.</p><h3 id="resolve-the-action-inside-the-plugin-process" tabindex="-1">Resolve the action inside the plugin process <a class="header-anchor" href="#resolve-the-action-inside-the-plugin-process" aria-label="Permalink to &quot;Resolve the action inside the plugin process&quot;">​</a></h3><p>Rejected: the point of the host-mediated route is that the host owns the decision and the audit. The plugin deliberately does not declare a shell or a system-open capability, and it should not grow one for a file it is already displaying.</p><h3 id="disable-the-two-actions-while-a-sibling-folder-is-selected" tabindex="-1">Disable the two actions while a sibling folder is selected <a class="header-anchor" href="#disable-the-two-actions-while-a-sibling-folder-is-selected" aria-label="Permalink to &quot;Disable the two actions while a sibling folder is selected&quot;">​</a></h3><p>Rejected: it turns a fixable addressing mistake into a missing feature. The user who switched folders is looking at those files and expects the same context menu to work on them.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0264-host-mediated-actions-follow-the-browsed-folder.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0264HostMediatedActionsFollowTheBrowsedFolder = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0264HostMediatedActionsFollowTheBrowsedFolder as default
};
