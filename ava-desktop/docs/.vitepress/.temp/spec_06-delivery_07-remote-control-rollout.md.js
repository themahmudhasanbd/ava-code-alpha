import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Remote Agent Control Rollout and Acceptance","description":"","frontmatter":{},"headers":[],"relativePath":"spec/06-delivery/07-remote-control-rollout.md","filePath":"spec/06-delivery/07-remote-control-rollout.md","lastUpdated":1789837487000}');
const _sfc_main = { name: "spec/06-delivery/07-remote-control-rollout.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="remote-agent-control-rollout-and-acceptance" tabindex="-1">Remote Agent Control Rollout and Acceptance <a class="header-anchor" href="#remote-agent-control-rollout-and-acceptance" aria-label="Permalink to &quot;Remote Agent Control Rollout and Acceptance&quot;">​</a></h1><ul><li>Status: Target delivery specification; post-MVP</li><li>Decision: D373 / ADR 0205, amended by D374 and D375</li><li>Normative protocol: <code>03-runtime/19-remote-agent-control-protocol.md</code></li><li>Normative security: <code>05-security/02-remote-control-security.md</code></li></ul><h2 id="_1-delivery-boundary-and-order" tabindex="-1">1. Delivery boundary and order <a class="header-anchor" href="#_1-delivery-boundary-and-order" aria-label="Permalink to &quot;1. Delivery boundary and order&quot;">​</a></h2><p>Remote control is a post-MVP capability. The current MVP continues to ship:</p><ul><li>Electron Main as the local orchestrator;</li><li>Node pi sidecar as the Agent runtime;</li><li>Rust host-core over local stdio NDJSON JSON-RPC;</li><li>optional loopback-only MCP control under ADR 0203; and</li><li>no public or LAN Gateway listener.</li></ul><p>Remote work MUST NOT begin by exposing the existing host-core or IPC endpoint. The implementation starts with the typebox contract, the headless Agent Host module, and conformance tests, then adds transport bindings behind explicit feature flags.</p><p>D375 fixes the order of the milestones around recorded demand rather than around transport breadth:</p><ul><li>issues #176 and #140 ask to operate projects on a remote Linux or WSL machine from the local desktop, which is the SSH-tunnel remote Host topology;</li><li>issue #100 asks for task completion and approval notifications on Telegram, WeChat, Slack, or a webhook, plus simple commands back, which is an outbound integration beside the Host; and</li><li>no recorded request asks for a browser or phone client of the desktop, so the Gateway and browser milestones are unscheduled.</li></ul><p>The scheduled order is R0, R1, R2, R3. Milestones marked unscheduled keep their specifications so the contract does not drift, and they are scheduled only by a later product decision.</p><h2 id="_2-milestones" tabindex="-1">2. Milestones <a class="header-anchor" href="#_2-milestones" aria-label="Permalink to &quot;2. Milestones&quot;">​</a></h2><h3 id="r0-—-contract-and-test-fixtures" tabindex="-1">R0 — Contract and test fixtures <a class="header-anchor" href="#r0-—-contract-and-test-fixtures" aria-label="Permalink to &quot;R0 — Contract and test fixtures&quot;">​</a></h3><p>Deliver:</p><ul><li>RACP v1 resource and operation schemas as typebox definitions in <code>packages/shared</code>, the single source of the contract, including the remote-host profile operations of protocol §6.2;</li><li>generated JSON Schema fixtures and shared request/response/event traces;</li><li>error mapping and capability negotiation fixtures;</li><li>cursor, epoch, snapshot, idempotency, turn-queue, and approval state-machine tests, including <code>allow-session</code> and Plan/Goal permission-mode selection;</li><li>binding-independent authorization test vectors, including the remote permission ceiling and the SSH-paired owner exemption; and</li><li>threat model review against the security specification.</li></ul><p>Exit criteria:</p><ol><li>A fixture trace has an identical semantic result for every shipped binding adapter.</li><li>Duplicate, stale, expired, and unauthorized requests have deterministic outcomes.</li><li>No fixture requires direct host-core or renderer access.</li><li>No hand-written second copy of the contract exists.</li></ol><h3 id="r1-—-headless-agent-host-module" tabindex="-1">R1 — Headless Agent Host module <a class="header-anchor" href="#r1-—-headless-agent-host-module" aria-label="Permalink to &quot;R1 — Headless Agent Host module&quot;">​</a></h3><p>Deliver <code>packages/agent-host</code>, a module with no Electron dependency that owns session and turn admission, the per-session turn queue, the approval broker, the in-memory event log with epochs, and the snapshot builder including active items and pending requests. Electron Main hosts it; the existing IPC handlers become adapters over it, incrementally if needed. Two local changes ship with it:</p><ul><li>Rust host-core exposes <code>permissions.pending</code> so a late-attaching client receives open requests; and</li><li>the renderer&#39;s in-memory prompt queue is replaced by the Host-owned turn queue, persisted by Rust host-core under its own ADR and schema bump (D375), restored after a restart and held until a controller attaches, so every client sees the same pending prompts and a reboot never starts work unattended.</li></ul><p>A development-only loopback RACP-WS endpoint drives the module; it is not reachable outside loopback or an explicit development tunnel.</p><p>Exit criteria:</p><ol><li>A client can close and reopen without interrupting a turn.</li><li>A stale cursor or a new epoch results in a snapshot, not an invented event sequence.</li><li>Existing local renderer behavior and the local MCP control plane are unchanged, and the desktop&#39;s queued prompts now come from the Host queue.</li><li>The module&#39;s test suite runs without Electron.</li><li>A client that attaches during an open approval sees it in the snapshot and its decision closes the local desktop card.</li></ol><p>Timing: the extraction starts after the release in flight ships, and each of <code>permissions.pending</code>, the Host queue, and the module extraction lands as its own commit, because the repository has concurrent sessions and 54 tests that match <code>electron/main/index.ts</code> by source pattern and must be repointed.</p><h3 id="r2-—-remote-host-over-an-ssh-tunnel" tabindex="-1">R2 — Remote Host over an SSH tunnel <a class="header-anchor" href="#r2-—-remote-host-over-an-ssh-tunnel" aria-label="Permalink to &quot;R2 — Remote Host over an SSH tunnel&quot;">​</a></h3><p>Deliver the first remote topology (<code>02-architecture/05-remote-agent-control.md</code> §5.2): the desktop as Remote Client of a headless Host on another machine.</p><p>R2 lands in two ordered slices so the desktop-side kernel can ship, be tested, and stay dead code until the full topology is ready:</p><ul><li><strong>R2a — Desktop kernel (delivered, ADR 0286).</strong> The single interception seam, the transport-agnostic backend, the event bridge, the coordinator, an encrypted-at-rest registry, and the boot hook. With an empty registry (default install) the kernel is a full no-op; the router has no remote backends, every renderer call still hits the local handler byte-for-byte. Every subsystem has a <code>node --test</code> fixture that exercises it against a fake — or, for the RACP adapter, against the real in-memory harness in <code>@pi-desktop/racp/test-harness</code>.</li><li><strong>R2b — Pairing, SSH bootstrap, terminal, and reverse tool relay.</strong> The remaining bullets below. R2&#39;s exit criteria stay unchanged and land with R2b; R2a alone is not user-visible and does not attempt them.</li></ul><p>Deliverables:</p><ul><li>the <code>pi-host</code> bundle: the module, the Node pi sidecar, and the platform&#39;s host-core binary, versioned with the desktop, bound to loopback, downloaded from GitHub Releases by a bootstrap script the desktop uploads over SSH with the published SHA-256 verified before install, then started and paired over that SSH session;</li><li>the desktop RACP client adapter in Electron Main under <code>lib/api.ts</code>, so the renderer needs no transport knowledge and a remote session renders like a local one;</li><li><code>RACP-WS</code> on the header profile over the forwarded port, with the loopback rule of security §5.1 and the device-token pairing of security §3.2;</li><li>the remote-host profile operations (<code>session/configure</code>, <code>session/fork</code>, <code>session/rename</code>, <code>session/delete</code>, <code>session/compact</code>, <code>workspace/list</code>, <code>workspace/read</code>, <code>workspace/diff</code>) so mode, model, thinking level, and the work panel&#39;s files and diff work against the remote session; and</li><li>the remote session ownership split of architecture §6.3;</li><li>the reverse tool relay: <code>tools/advertise</code> and the <code>tool/execute</code> server request, so desktop MCP servers and workspace-free plugin tools run on the desktop for a remote session;</li><li>the terminal: <code>terminal/open</code>, <code>terminal/input</code>, <code>terminal/resize</code>, <code>terminal/close</code>, <code>terminal.output</code>, and a bounded replay ring, running on the remote machine; and</li><li>the Settings → Remote Hosts destination: a compact host inventory and one Add form with SSH and Pair tabs, no instructional copy, marked Experimental on the settings rail and page title because the topology may still fail, and shown — with its settings-search hits — only while developer mode is on (<code>04-ux/06-settings-ia.md</code> §1, §3).</li></ul><p>Design decisions (D375, recorded 2026-09-10):</p><ol><li>Provider configuration on the remote Host is written over the SSH bootstrap channel as Host-local configuration; nothing crosses RACP.</li><li>Desktop user MCP servers and workspace-free plugin tools reach remote sessions through the reverse tool relay in this milestone; plugin tools that require workspace or filesystem access are excluded.</li><li>The work-panel terminal ships in this milestone as the <code>terminal/*</code> operations, running on the remote machine.</li><li><code>pi-host</code> is downloaded from GitHub Releases per platform at the desktop&#39;s version by a bootstrap script the desktop uploads over SSH, with the published SHA-256 verified; a version mismatch is <code>PROTOCOL_MISMATCH</code> and the desktop offers to re-run the download. A machine without outbound access to GitHub is not supported in the first version.</li><li>The SSH-paired desktop device holds <code>owner</code> and is exempt from the remote permission ceiling unless the Host policy <code>applyCeilingToPairedDevices</code> is enabled.</li><li>R2 ships as one milestone; it is not split into sub-milestones.</li></ol><p>Exit criteria:</p><ol><li>E2E-231 passes: a turn started from the desktop reads, writes, and runs commands on the remote machine only, approvals appear in the desktop card, and the remote host-core binds loopback only.</li><li>Dropping and restoring the SSH session mid-turn resumes by cursor without duplicating the turn.</li><li>Mode, model, and thinking-level changes on a remote session behave as locally, idle-only.</li><li>The remote tool catalog contains no desktop plugin tool.</li><li>A <code>pi-host</code> at another version is rejected and the update path is offered.</li><li>A desktop-configured MCP tool advertised for relay executes on the desktop during a remote turn, a workspace-requiring plugin tool is absent from the remote catalog, and closing the desktop mid-call fails the tool without interrupting the turn.</li><li>A terminal opened on a remote session runs on the remote machine inside the session root and its output resumes from the replay ring after an SSH drop.</li><li>The bootstrap download verifies the published checksum and refuses a tampered bundle.</li></ol><h3 id="r3-—-outbound-messaging-integration" tabindex="-1">R3 — Outbound messaging integration <a class="header-anchor" href="#r3-—-outbound-messaging-integration" aria-label="Permalink to &quot;R3 — Outbound messaging integration&quot;">​</a></h3><p>Deliver the integration adapter beside the Host (issue #100): a further caller of the module inside the Host process, with no transport and no inbound listener.</p><ul><li>It subscribes to host-scope and session events and relays redacted summaries of <code>turn.completed</code>, <code>turn.failed</code>, <code>approval.requested</code>, and <code>input.requested</code> to outbound channels: webhook first, then Telegram and Slack through their outbound polling or socket modes; channels that require an inbound callback are deferred.</li><li>It maps a fixed command vocabulary from a linked chat to <code>turn/start</code>, <code>turn/stop</code>, <code>turn/interrupt</code>, and <code>approval/respond</code>, executed under the linked principal&#39;s roles and the same Host policy as any client; unlinked chats are ignored and audited.</li><li>It never blocks a turn: delivery failures are logged and retried with a bound, and the Host runs the same whether the adapter is configured or not.</li></ul><p>Exit criteria: E2E-232 passes; payloads contain summaries and ids only; commands from an unlinked chat have no effect; the Host has no new listener.</p><h3 id="unscheduled-—-gateway-and-host-link-formerly-r3" tabindex="-1">Unscheduled — Gateway and Host link (formerly R3) <a class="header-anchor" href="#unscheduled-—-gateway-and-host-link-formerly-r3" aria-label="Permalink to &quot;Unscheduled — Gateway and Host link (formerly R3)&quot;">​</a></h3><p>A separate Remote Gateway and the outbound Host link (<code>racp-hostlink.v1</code>) with identity, routing, rate limits, audit, revocation, and transient attachment relay. PI does not operate it: a user runs it on their own infrastructure and it admits clients with Host-issued device credentials, so there is no identity source beyond the Host (D385). Exit criteria remain those of E2E-227.</p><h3 id="unscheduled-—-browser-profile-formerly-r4" tabindex="-1">Unscheduled — Browser profile (formerly R4) <a class="header-anchor" href="#unscheduled-—-browser-profile-formerly-r4" aria-label="Permalink to &quot;Unscheduled — Browser profile (formerly R4)&quot;">​</a></h3><p><code>RACP-HTTP</code> and the cookie authentication profile for browser clients on both <code>RACP-WS</code> and SSE. Before it is scheduled, the browser client&#39;s first needs are already covered by the remote-host profile (<code>session/configure</code>, history, workspace reads); a Host-issued session cookie for Gateway-less use still needs a specification clause. Exit criteria remain those of E2E-228.</p><h3 id="reserved-—-grpc-binding-formerly-r5" tabindex="-1">Reserved — gRPC binding (formerly R5) <a class="header-anchor" href="#reserved-—-grpc-binding-formerly-r5" aria-label="Permalink to &quot;Reserved — gRPC binding (formerly R5)&quot;">​</a></h3><p><code>RACP-GRPC</code> is delivered only when a named native service consumer or Gateway implementation needs it. Generate the <code>.proto</code> and clients from the typebox source; do not maintain an independent hand-written gRPC contract. It is not a release gate for remote control.</p><h2 id="_3-implementation-rules" tabindex="-1">3. Implementation rules <a class="header-anchor" href="#_3-implementation-rules" aria-label="Permalink to &quot;3. Implementation rules&quot;">​</a></h2><h3 id="_3-1-preserve-the-local-path" tabindex="-1">3.1 Preserve the local path <a class="header-anchor" href="#_3-1-preserve-the-local-path" aria-label="Permalink to &quot;3.1 Preserve the local path&quot;">​</a></h3><p>The RACP server calls the headless Agent Host module, which calls the same host and sidecar paths the renderer uses. It does not call the renderer, <code>host.proxy</code>, or Rust host-core from a network listener. The <code>pi-host</code> bundle runs the module and supervision on another machine without changing RACP.</p><h3 id="_3-2-keep-the-agent-independent-of-clients" tabindex="-1">3.2 Keep the Agent independent of clients <a class="header-anchor" href="#_3-2-keep-the-agent-independent-of-clients" aria-label="Permalink to &quot;3.2 Keep the Agent independent of clients&quot;">​</a></h3><p>The Host owns active turns, the turn queue, and event cursors. Client lifetime, browser tab lifetime, Electron window visibility, and SSH session lifetime do not control Agent execution. A client must explicitly call <code>turn/stop</code> or <code>turn/interrupt</code> to end a turn.</p><h3 id="_3-3-keep-the-queue-in-the-host" tabindex="-1">3.3 Keep the queue in the Host <a class="header-anchor" href="#_3-3-keep-the-queue-in-the-host" aria-label="Permalink to &quot;3.3 Keep the queue in the Host&quot;">​</a></h3><p>Queued prompts are Host state. No client, including the local renderer, keeps a private queue once the module ships; a queued turn is visible to every attached client and can be canceled by any controller.</p><h3 id="_3-4-keep-the-renderer-transport-agnostic" tabindex="-1">3.4 Keep the renderer transport-agnostic <a class="header-anchor" href="#_3-4-keep-the-renderer-transport-agnostic" aria-label="Permalink to &quot;3.4 Keep the renderer transport-agnostic&quot;">​</a></h3><p>The renderer reaches every backend through <code>lib/api.ts</code>. The desktop RACP client adapter implements that surface for a remote Host; features the remote-host profile does not cover are hidden by capability negotiation, not stubbed. The renderer never learns whether a session is local or remote beyond a display badge.</p><h3 id="_3-5-make-replay-a-first-class-test-surface" tabindex="-1">3.5 Make replay a first-class test surface <a class="header-anchor" href="#_3-5-make-replay-a-first-class-test-surface" aria-label="Permalink to &quot;3.5 Make replay a first-class test surface&quot;">​</a></h3><p>Every event-producing test records:</p><ul><li>the initial snapshot revision and cursor;</li><li>the exact command and idempotency key;</li><li>every durable event sequence applied;</li><li>the disconnect point;</li><li>the replay request; and</li><li>the final snapshot hash.</li></ul><p>The test fails if a duplicate, gap, out-of-order durable event, or unannounced state change appears. Ephemeral events are excluded from ordering assertions and must be reconstructible from the snapshot&#39;s active items.</p><h3 id="_3-6-keep-bindings-semantically-equal" tabindex="-1">3.6 Keep bindings semantically equal <a class="header-anchor" href="#_3-6-keep-bindings-semantically-equal" aria-label="Permalink to &quot;3.6 Keep bindings semantically equal&quot;">​</a></h3><p>The conformance fixture is the source of truth for behavior. Each shipped binding may choose its native status and serialization, but it must preserve:</p><ul><li>operation acceptance and rejection;</li><li>authorization scope and the remote permission ceiling;</li><li>idempotency result;</li><li>durable event order and cursor;</li><li>queue order;</li><li>approval lifecycle and decision vocabulary;</li><li>attachment hash and size checks; and</li><li>terminal turn state.</li></ul><h2 id="_4-validation-plan" tabindex="-1">4. Validation plan <a class="header-anchor" href="#_4-validation-plan" aria-label="Permalink to &quot;4. Validation plan&quot;">​</a></h2><h3 id="_4-1-unit-and-contract-validation" tabindex="-1">4.1 Unit and contract validation <a class="header-anchor" href="#_4-1-unit-and-contract-validation" aria-label="Permalink to &quot;4.1 Unit and contract validation&quot;">​</a></h3><ul><li>generated schema validation for every request, response, and event;</li><li>state-machine transition tests for Session, Turn, turn queue, Approval, Input, and Attachment;</li><li>idempotency tests with lost response and retry;</li><li>cursor replay, epoch change, expiry, gap, and snapshot tests;</li><li>authorization matrix tests for every role and operation, including the permission ceiling, its SSH-paired exemption, and <code>allow-session</code> policy;</li><li>redaction tests for prompts, tool data, credentials, pending-request reads, integration payloads, and attachments; and</li><li>generated binding round-trip tests.</li></ul><h3 id="_4-2-integration-validation" tabindex="-1">4.2 Integration validation <a class="header-anchor" href="#_4-2-integration-validation" aria-label="Permalink to &quot;4.2 Integration validation&quot;">​</a></h3><ul><li>the headless module inside Electron Main with the real sidecar and host-core supervision;</li><li>a <code>pi-host</code> bundle on a Linux test machine reached through an SSH port forward, including bootstrap, pairing, version mismatch, and re-pairing;</li><li>the desktop adapter rendering a remote session through the unchanged renderer;</li><li>SSH session drop and restore while a remote turn is running;</li><li>host restart while a turn is running and turns are queued, verifying that the persisted queue is restored and held;</li><li>relayed tool execution and terminal streaming across an SSH drop;</li><li>bootstrap download with a valid and a tampered checksum;</li><li>the integration adapter against a webhook sink and a long-polling bot fixture; and</li><li>when scheduled: Gateway routing across two Hosts, browser reconnect on the cookie profile, and the Host link relay through a NAT harness.</li></ul><h3 id="_4-3-security-validation" tabindex="-1">4.3 Security validation <a class="header-anchor" href="#_4-3-security-validation" aria-label="Permalink to &quot;4.3 Security validation&quot;">​</a></h3><ul><li>invalid, expired, revoked, and wrong-Host device tokens;</li><li>non-loopback peers and non-loopback binds without TLS on a <code>pi-host</code>;</li><li>pairing token reuse and pairing over a non-SSH channel;</li><li>wrong role, wrong Session, wrong Host, and stale revision;</li><li>SSRF and arbitrary file/path attempts, including <code>workspace/read</code> outside the session root;</li><li>oversized and hash-mismatched uploads;</li><li>prompt-injected requests attempting to select secrets or permissions;</li><li>integration commands from unlinked chats and replayed commands;</li><li>slow-reader and connection-exhaustion tests;</li><li>audit-log redaction and retention checks; and</li><li>when scheduled: Origin, CORS, CSRF, cookie, and Gateway relay checks.</li></ul><h3 id="_4-4-operational-validation" tabindex="-1">4.4 Operational validation <a class="header-anchor" href="#_4-4-operational-validation" aria-label="Permalink to &quot;4.4 Operational validation&quot;">​</a></h3><p>Record at minimum:</p><ul><li>turn admission latency and queue depth;</li><li>event delivery latency and sequence lag over the SSH forward;</li><li>replay, resync, and epoch-change counts;</li><li>approval wait time and expiry count;</li><li>Host connection health, bootstrap duration, and version mismatches;</li><li>authentication and authorization failures;</li><li>integration delivery latency and retry counts;</li><li>event queue drops, ephemeral and durable separately; and</li><li>active Host/Session/client counts.</li></ul><p>The first production target is a bounded, observable remote control service, not an unbounded real-time stream. Load tests must demonstrate that a slow or disconnected client does not affect Agent execution on either machine.</p><h2 id="_5-rollout-and-rollback" tabindex="-1">5. Rollout and rollback <a class="header-anchor" href="#_5-rollout-and-rollback" aria-label="Permalink to &quot;5. Rollout and rollback&quot;">​</a></h2><ol><li>Ship the typebox contract, generated fixtures, and disabled code paths first.</li><li>Enable R1 only in development profiles.</li><li>Enable the SSH-tunnel topology behind a feature flag for allowlisted test users, then generally.</li><li>Enable the integration adapter as an opt-in setting with no channel configured by default.</li><li>Schedule the Gateway, browser, and gRPC milestones only by a recorded product decision.</li><li>Keep a kill switch that rejects new remote connections and cancels queued turns submitted by remote principals while preserving already-running local desktop sessions.</li></ol><p>Rollback MUST:</p><ul><li>stop accepting new remote connections and stop the integration adapter;</li><li>leave a remote <code>pi-host</code> process running or stopped according to the user&#39;s choice, never deleting its transcripts;</li><li>leave local stdio, renderer, and MCP paths usable;</li><li>preserve completed local transcript data; and</li><li>never replay a turn merely because a remote feature flag changed.</li></ul><h2 id="_6-release-acceptance" tabindex="-1">6. Release acceptance <a class="header-anchor" href="#_6-release-acceptance" aria-label="Permalink to &quot;6. Release acceptance&quot;">​</a></h2><p>The scheduled feature set is not production-ready until:</p><ol><li>remote scenarios E2E-221 through E2E-226, E2E-229, E2E-230, and E2E-231 are green in the approved remote harness, and E2E-232 for the integration adapter;</li><li>the security acceptance gates in <code>05-security/02-remote-control-security.md</code> that apply to the scheduled milestones are signed off;</li><li>a failure-injection run proves no duplicate execution after reconnect, including an SSH session drop;</li><li><code>pi-host</code> bundles are published to GitHub Releases with checksums for every Linux platform the desktop&#39;s release pipeline publishes, and the version-mismatch and tampered-download paths are tested;</li><li>the Host operational metrics are available; and</li><li>a new release/rollback runbook names the feature flag, pairing revocation path, data retention on the remote machine, and incident owner.</li></ol><p>Binding parity and E2E-227 / E2E-228 become gates when their milestones are scheduled.</p><h2 id="_7-implementation-status" tabindex="-1">7. Implementation status <a class="header-anchor" href="#_7-implementation-status" aria-label="Permalink to &quot;7. Implementation status&quot;">​</a></h2><p>Recorded on the <code>feat/remote-agent-host</code> branch, 2026-09-10:</p><ul><li>R0 shipped: the RACP contract as typebox schemas in <code>packages/shared</code> (<code>racp.ts</code>), the generated JSON Schema fixture, the local-to-RACP event mapping, the remote permission ceiling, and the shared error codes.</li><li>R1 shipped: host-core <code>permissions.pending</code>; the headless <code>packages/agent-host</code> module with the epoch event log, bounded fan-out, the approval broker, the turn queue, and the snapshot builder; the Electron bridge that hosts the module over the existing IPC handlers and feeds every agent event through it; and schema v15 with the persisted <code>turn_queue</code> and its RPC methods (D386 / ADR 0213).</li><li>R1 shipped: the renderer&#39;s in-memory prompt queue is retired; the composer pushes through <code>agent/queue/push</code>, mirrors <code>agent/event/queueChanged</code>, and &quot;send now&quot; is <code>turn/prioritize</code> plus a graceful stop.</li><li>R1 partial (2026-09-18): the runtime-level per-turn permission ceiling is plumbed end-to-end (<code>AgentPromptRequest.permissionMode</code> → agent-ipc → <code>RuntimeService.startTurn</code> → sidecar <code>agent.prompt</code>), and the bridge no longer fails closed on every session/effective-mode mismatch. A widening request (effective more permissive than session) is still refused as defence-in-depth; a narrower ceiling is forwarded and recorded by the sidecar as a documented stub. Turn-scoped enforcement in host-core (<code>session.beginTurn</code> accepting an override) is the remaining piece, so a narrower ceiling on a local turn does not yet clamp tool decisions.</li><li>R2 started (2026-09-18, D447 / ADR 0284): <code>packages/host-runtime</code> holds the Electron-independent runtime layer — the host-core and sidecar stdio transports, the restart supervisor, <code>RuntimeService</code> (the module&#39;s <code>RuntimePort</code> with the durable turn lifecycle), transcript persistence, a headless launch resolver, and approved Plan/Goal dispatch — and Electron main runs on it through thin adapters.</li><li>R2 (2026-09-18, D448 / ADR 0285): <code>packages/racp</code> holds the <code>RACP-WS</code> server and client cores, the <code>ws</code> binding on loopback, and device-token pairing; handshake, authorization, idempotency, queue order, approvals, cursor replay, eviction, epoch change, slow clients, and reconnect without duplicate execution are package tests. The <code>pi-host</code> bundle, the desktop adapter, and the SSH bootstrap have since started: the R2a desktop kernel (D449 / ADR 0286) brought the adapter and the bundle, and the SSH bootstrap followed in D453 / ADR 0292.</li><li>R2b partial (2026-09-19, D453 / ADR 0292): the desktop installs and pairs a <code>pi-host</code> over the user&#39;s own <code>ssh</code> client with <code>BatchMode=yes</code>, so the user&#39;s configuration, agent, and jump hosts apply and no SSH secret reaches the app. <code>remote/pi-host-release.ts</code> holds the pure release coordinates (remote platform, desktop version, published SHA-256, refusal of unpublished targets) and <code>remote/pi-host-bootstrap-script.ts</code> generates the single <code>umask 077</code> script that downloads, verifies, installs under the remote <code>$HOME</code>, restarts the host on loopback with <code>--pair</code>, and echoes <code>PI_HOST_READY</code> / <code>PI_HOST_PAIRING_TOKEN</code>; <code>remote/ssh-transport.ts</code> is the injectable transport port and <code>remote/ssh-tunnel.ts</code> owns one durable <code>ssh -N -L</code> forward per host, re-established on every launch and adopted from the bootstrap so pairing opens exactly one tunnel. Records carry <code>metadata.transport = &quot;ssh&quot;</code> plus an SSH descriptor instead of a URL, and <code>pi-desktop/remoteHost/bootstrap</code> joins <code>list</code> / <code>pair</code> / <code>remove</code>. The terminal work-panel client, the reverse tool relay, and provider-configuration propagation over the SSH channel are not in this slice.</li></ul><h2 id="_8-amendment-history" tabindex="-1">8. Amendment history <a class="header-anchor" href="#_8-amendment-history" aria-label="Permalink to &quot;8. Amendment history&quot;">​</a></h2><p>D374 (2026-09-10) replaced the Electron facade milestone with the headless Agent Host module, added the Host queue and <code>permissions.pending</code> changes, made <code>RACP-WS</code> the only normative v1 binding with a browser profile and a reserved gRPC binding, added the Host link relay and identity-source decision, and made tenant isolation tests conditional on a multi-tenant harness.</p><p>D375 (2026-09-10) re-sequenced the milestones around recorded demand: R2 is the SSH-tunnel remote Host with the desktop as client, R3 is the outbound messaging integration, and the Gateway, browser, and gRPC milestones are unscheduled. It added the <code>pi-host</code> bundle, the desktop adapter rule, and E2E-231 / E2E-232 as the acceptance targets. Its design-gate answers, recorded the same day, put the reverse tool relay and the terminal in R2 as one milestone, download <code>pi-host</code> from GitHub Releases, persist the turn queue in host-core, default the remote approval lifetime to 30 minutes, make the paired-device exemption a Host policy, and fix the Gateway identity source to the PI account service.</p><p>D385 (2026-09-10) withdrew the identity-source clause: remote control is user-local by construction, and any Gateway is self-hosted and admits clients with Host-issued device credentials.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("spec/06-delivery/07-remote-control-rollout.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _07RemoteControlRollout = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _07RemoteControlRollout as default
};
