import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0143: Make Session Titles User-Renamable","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0143-user-renamable-session-titles.md","filePath":"adr/0143-user-renamable-session-titles.md","lastUpdated":1788253846000}');
const _sfc_main = { name: "adr/0143-user-renamable-session-titles.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0143-make-session-titles-user-renamable" tabindex="-1">ADR 0143: Make Session Titles User-Renamable <a class="header-anchor" href="#adr-0143-make-session-titles-user-renamable" aria-label="Permalink to &quot;ADR 0143: Make Session Titles User-Renamable&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-01</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Tasks are durable sessions, but their labels are currently controlled by the default title and first-prompt auto-title flow. Once a task has accumulated history, users need a stable label that is easier to recognize in the sidebar, Project archive, topbar, and search results. Issue #27 requests renaming a project task without changing the task itself.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Treat a task name as session metadata and expose one shared rename dialog. The dialog is available from the Sidebar session overflow/right-click menu and from the Project archive task row. It trims input, accepts 1–80 Unicode code points, and keeps the Save action unavailable for an empty value. The host validates the same rules so direct IPC callers cannot persist invalid titles.</p><p>Renaming updates only <code>sessions.title</code>. It does not modify the transcript, message count, project binding, empty-session predicate, or <code>updated_at</code> activity timestamp. A non-default title continues to opt out of first-prompt automatic title generation. Existing <code>session.rename</code> IPC and host RPC channels are used; no schema or protocol version change is required.</p><p>Historical notification title snapshots remain unchanged. Newly rendered surfaces read the current session summary, so the renamed title is shown in the Sidebar, topbar, Project archive, and search results after the local store update and across restart.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Users can name a task from both primary task navigation and the project index.</li><li>Host-side validation provides one durable contract for renderer and future clients.</li><li>Renaming cannot accidentally move a task in recent-activity order or alter agent context.</li><li>The renderer owns modal focus, error presentation, and the immediate local title update after the host confirms the session exists.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li>Rename only from the topbar: rejected because the task can be discovered and managed from the Sidebar or Project archive before opening it.</li><li>Rename the project folder or project record: rejected because the request is about an individual task/session, not the workspace identity.</li><li>Add a command-palette command: deferred; the existing row actions are the discoverable surface and no new global command is needed for this operation.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0143-user-renamable-session-titles.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0143UserRenamableSessionTitles = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0143UserRenamableSessionTitles as default
};
