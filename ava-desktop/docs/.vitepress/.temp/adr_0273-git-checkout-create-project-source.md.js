import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0273: Git Checkout as a Create Project Source","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0273-git-checkout-create-project-source.md","filePath":"adr/0273-git-checkout-create-project-source.md","lastUpdated":1789579853000}');
const _sfc_main = { name: "adr/0273-git-checkout-create-project-source.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0273-git-checkout-as-a-create-project-source" tabindex="-1">ADR 0273: Git Checkout as a Create Project Source <a class="header-anchor" href="#adr-0273-git-checkout-as-a-create-project-source" aria-label="Permalink to &quot;ADR 0273: Git Checkout as a Create Project Source&quot;">​</a></h1><ul><li>Status: Accepted for implementation</li><li>Date: 2026-09-17</li><li>Deciders: PI-Desktop desktop UI maintainers</li><li>Amends: ADR 0233, ADR 0247</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>ADR 0233 gave the renderer one Create project surface that collects a name and local folders, and the home switcher already cloned a git project through <code>project/clone</code> under the public-host rules of ADR 0247. A user with no project at all still could not start from a repository: the switcher only renders inside the hero of a project-bound session, and the dialog had no repository source. The first project of a fresh installation therefore required a local folder before any clone entry was reachable.</p><p>The existing clone picks its parent through a native dialog and returns a workspace the renderer activates immediately. Reusing it inside the dialog would either end the dialog flow halfway or activate a project before its group exists.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>The Create project dialog owns a source selector with two equal peers: This computer (local folders) and Git repository.</li><li>The git source keeps the dialog&#39;s one name field, adds a repository URL field and one clone destination row, and parses the URL in the renderer with the same <code>parseGitCloneUrl</code> rules the home switcher uses (ADR 0247).</li><li>Main exposes additive <code>project/cloneCheckout({ url, parentPath })</code>: it clones into an explicit parent folder and returns <code>{ path, name }</code> without touching the active workspace and without opening a picker.</li><li>Project creation still flows through <code>project-group/create</code>. A checkout becomes the primary root of one logical group and the entered name names the group, exactly like a folder pick (ADR 0233). Folder picks and checkouts share one creation helper in the project slice.</li><li><code>project/clone</code> keeps its current behavior for the home switcher; the two clone entries do not share UI.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A fresh installation can create its first project directly from a public repository without opening an unrelated folder first.</li><li>The clone destination is explicit: the dialog collects it before <code>git clone</code> runs, so a checkout never lands in an implicit directory.</li><li>Private, loopback, link-local, credential-bearing, and malformed remotes stay rejected before git runs (ADR 0247); the dialog disables Create for them.</li><li>A checkout that succeeds while group creation fails leaves the cloned folder on disk; the error surfaces as a toast and the folder can be added afterwards.</li><li>No protocol, schema, host RPC, or storage change: the new channel is a narrow main-process capability, and the host still owns every durable project record.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Move the home switcher clone entry to the empty home:</strong> rejected because the hero without a project-bound session has no switcher surface, and the create dialog is the documented first-project entry.</li><li><strong>Reuse <code>project/clone</code> inside the dialog:</strong> rejected because its native parent picker and immediate workspace return would activate a project before the group is created.</li><li><strong>Let the main process pick the destination silently:</strong> rejected because a <code>git clone</code> should never choose its own destination folder.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0273-git-checkout-create-project-source.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0273GitCheckoutCreateProjectSource = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0273GitCheckoutCreateProjectSource as default
};
