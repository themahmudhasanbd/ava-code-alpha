import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0255: Theme assets are absolute paths","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0255-theme-assets-by-absolute-path.md","filePath":"adr/0255-theme-assets-by-absolute-path.md","lastUpdated":1789409625000}');
const _sfc_main = { name: "adr/0255-theme-assets-by-absolute-path.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0255-theme-assets-are-absolute-paths" tabindex="-1">ADR 0255: Theme assets are absolute paths <a class="header-anchor" href="#adr-0255-theme-assets-are-absolute-paths" aria-label="Permalink to &quot;ADR 0255: Theme assets are absolute paths&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-15</li><li>Decision: D422</li><li>Supersedes: ADR 0248 §1 for asset path resolution; the scheme, the <code>url()</code> rewrite, the extension whitelist, the size budget, and the unload revocation carry over unchanged.</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>ADR 0248 let a theme reference plugin package bytes: <code>contributes.themes[].assets</code> listed package-relative paths, the host resolved them inside the plugin package, and each matching <code>url()</code> was rewritten to <code>plugin-asset://</code>.</p><p>That boundary has two costs in practice:</p><ul><li>A plugin that wants a user-chosen image has to copy it into its own package. A development plugin is watched recursively (<code>plugin-watcher.ts</code>), so every such write reloads the plugin, and a reload closes that plugin&#39;s panel window (<code>plugin-runtime.ts</code> unload path → <code>closePanel</code>).</li><li>The plugin&#39;s own data directory — <code>pi.plugin.getDataPath()</code>, the one writable area a plugin owns outside its package — cannot be referenced at all.</li></ul><p>Plugins already run as ordinary Node processes with full filesystem access. The package scope constrained what theme <em>CSS text</em> could reach, not what the plugin itself could read.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>A theme asset is an <strong>absolute path</strong>: <code>C:/art/bg.png</code>, <code>/art/bg.png</code>, or either spelled as a <code>file:</code> URL. Package-relative references are no longer assets; <code>normalizeThemeAssetPath</code> (SDK) and <code>normalize_theme_asset_path</code> (host-core) reject them.</li><li>Both authorization routes accept absolute paths: <ul><li><code>contributes.themes[].assets</code> — load-time, auditable in the manifest;</li><li><code>pi.themes.upsert({ id, label, base, css })</code> — the runtime route now resolves asset references in the sheet and registers them in that plugin&#39;s asset map for as long as the plugin stays loaded. The upsert is a message, not a file write, so a theme can pick up a new image without a reload.</li></ul></li><li>Everything else about the mechanism is unchanged: the extension whitelist (<code>png</code>, <code>jpg</code>, <code>jpeg</code>, <code>webp</code>, <code>avif</code>, <code>svg</code>, <code>woff2</code>), the summed 4 MB budget for declared assets, <code>.</code>/<code>..</code> rejection, the <code>url()</code> → <code>plugin-asset://</code> rewrite, <code>nosniff</code>, <code>no-store</code>, and revocation when the plugin unloads.</li><li>Absolute keys are percent-encoded in the <code>plugin-asset://</code> URL (<code>themeAssetUrl</code>) and decoded by the handler, so spaces, <code>?</code>, <code>#</code>, and the drive colon cannot be re-read as anything but a path.</li><li>The handler serves what the plugin registered: a path from its manifest, or a path its own sanitized sheet referenced through an upsert.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A plugin can hand the renderer <strong>any local file it can read</strong>, as an image or a font. The host no longer enforces &quot;theme assets live in the plugin package&quot;, so a theme — which is data, and may arrive inside a shared theme pack — can name a file the user never chose. Manifest-declared assets keep an audit trail in a static, reviewable file; runtime-registered ones have none and vanish with the plugin.</li><li>The extension whitelist still bounds what may be served, the size budget still bounds declared assets, and an unregistered reference is still refused.</li><li>Because a theme may point at the plugin&#39;s data directory, a development plugin no longer reloads when the user picks or replaces an image: the upload writes into <code>getDataPath()/images/</code>, and the theme reaches it through an upsert.</li><li>Themes that declared package-relative assets stop resolving. This is a deliberate breaking change; the affected surface is limited to themes that use <code>assets</code> at all.</li><li><code>resolveInsidePlugin</code> remains for the theme sheet path itself (<code>contributes.themes[].path</code>), which is still package-relative.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Allow both spellings.</strong> Rejected: the owner chose an absolute-only rule, and keeping two resolutions for one concept invites exactly the confusion this change removes.</li><li><strong>Add only the plugin data directory as a second root.</strong> Narrower, but it does not cover a file the user picked outside the data directory without a copy step, and the runtime route would still have no way to authorize it.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0255-theme-assets-by-absolute-path.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0255ThemeAssetsByAbsolutePath = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0255ThemeAssetsByAbsolutePath as default
};
