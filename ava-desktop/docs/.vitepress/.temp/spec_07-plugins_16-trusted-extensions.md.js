import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"16. Trusted Extensions","description":"","frontmatter":{},"headers":[],"relativePath":"spec/07-plugins/16-trusted-extensions.md","filePath":"spec/07-plugins/16-trusted-extensions.md","lastUpdated":1790057981000}');
const _sfc_main = { name: "spec/07-plugins/16-trusted-extensions.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="_16-trusted-extensions" tabindex="-1">16. Trusted Extensions <a class="header-anchor" href="#_16-trusted-extensions" aria-label="Permalink to &quot;16. Trusted Extensions&quot;">​</a></h1><blockquote><p>Status: Implemented v1.1 (D387 / D388, ADR 0214 / ADR 0215 / ADR 0244); implementation notes are marked &quot;v1 note&quot; Scope: v1.1. v2 and v3 items are listed in §12 and are not committed.</p></blockquote><h2 id="_1-purpose-and-terminology" tabindex="-1">1. Purpose and terminology <a class="header-anchor" href="#_1-purpose-and-terminology" aria-label="Permalink to &quot;1. Purpose and terminology&quot;">​</a></h2><p>Plugins (<a href="./01-plugin-system">01-plugin-system.md</a>) are the one extension surface of PI-Desktop. This document specifies one plugin contribution, <code>contributes.agentExtensions</code>: TypeScript or JavaScript modules that run inside the Agent sidecar, receive an <code>ExtensionAPI</code> object, and register tools, commands, and event handlers directly on the agent loop. The <code>ExtensionAPI</code> contract is the one defined by <code>@earendil-works/pi-coding-agent</code>, which PI-Desktop adopts alongside the <code>pi-ai</code> and <code>pi-agent-core</code> kernel (ADR 0002), so an extension written for the pi CLI is the module a plugin contributes. D388 folded the earlier standalone &quot;trusted extensions&quot; registry into this contribution; the engine below is unchanged.</p><p>Provider declarations are a separate manifest surface rather than part of this contract: <code>contributes.providers</code> materializes Host-owned provider rows (<a href="./02-plugin-manifest-schema">02-plugin-manifest-schema.md</a> §5.4, ADR 0259), so it is neither an <code>ExtensionAPI</code> member nor a row in the §5 support matrix. <code>registerProvider</code> (§5) remains the session-scoped extension counterpart.</p><table tabindex="0"><thead><tr><th>Term</th><th>Meaning</th></tr></thead><tbody><tr><td>Agent extension</td><td>One module a plugin lists in <code>contributes.agentExtensions</code>, written against <code>ExtensionAPI</code>, running with the trust level of the Agent sidecar</td></tr><tr><td>Plugin</td><td>A PI-Desktop plugin with a manifest, running in its own process under the permission gateway (ADR 0008); the owner, installer, and enablement record of its agent extensions</td></tr><tr><td>Adapter</td><td>The layer in <code>packages/agent-runtime</code> that implements <code>ExtensionAPI</code> on top of the desktop runtime</td></tr><tr><td>Runner</td><td>One desktop-owned <code>TrustedExtensionRunner</code> instance bound to one desktop session (v1 note: the pi-coding-agent <code>ExtensionRunner</code> is not reused because it binds the terminal theme; its <code>ExtensionAPI</code> types are a types-only dependency)</td></tr></tbody></table><h2 id="_2-positioning-and-trust-model" tabindex="-1">2. Positioning and trust model <a class="header-anchor" href="#_2-positioning-and-trust-model" aria-label="Permalink to &quot;2. Positioning and trust model&quot;">​</a></h2><ol><li>Agent extensions are installed, enabled, scoped, updated, and removed as part of their plugin. There is no second list, store, or settings page.</li><li>An agent extension is trusted code. It executes inside the Agent sidecar, which already holds the bash, edit, and write tools, so granting the <code>agent.extension</code> permission grants exactly what running the agent already grants. The plugin sandbox in <a href="./04-plugin-security">04-plugin-security.md</a> does not cover these modules, which is why the permission is a separate, high-risk grant rather than an implicit part of <code>agent.tool.register</code>.</li><li>Nothing runs without the grant. A plugin that declares <code>contributes.agentExtensions</code> without <code>agent.extension</code> fails manifest validation; a plugin whose recorded grants omit the permission loads with its modules skipped and audited (<code>plugin.agentExtensions.skipped</code>). D007 stays in force: PI-Desktop never auto-imports <code>~/.pi</code>.</li><li>Project scope is the plugin&#39;s activation scope. A plugin limited to some projects contributes its modules only to sessions in those projects. v1 note: there is no separate project trust state, so the plugin scope is the trust decision and <code>project_trust</code> is not emitted.</li><li>Marketplace distribution of plugins holding <code>agent.extension</code> is not enabled in v1.1: the permission is accepted from local imports and development plugins. Marketplace listing waits for signing (spec 08).</li></ol><h2 id="_3-contribution-and-import" tabindex="-1">3. Contribution and import <a class="header-anchor" href="#_3-contribution-and-import" aria-label="Permalink to &quot;3. Contribution and import&quot;">​</a></h2><h3 id="_3-1-manifest" tabindex="-1">3.1 Manifest <a class="header-anchor" href="#_3-1-manifest" aria-label="Permalink to &quot;3.1 Manifest&quot;">​</a></h3><div class="language-json vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">{</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;id&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;acme.git-helper&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;name&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;Git helper&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;version&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;1.0.0&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;main&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;main.js&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;permissions&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: [</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;agent.extension&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">],</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;contributes&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: { </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">&quot;agentExtensions&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: [</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;src/index.ts&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">] }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><p>Rules: at most eight entries; each is a relative <code>.ts</code>, <code>.mts</code>, <code>.js</code>, or <code>.mjs</code> path inside the plugin directory; the file must exist at load; a manifest that lists entries without the permission is invalid (<a href="./02-plugin-manifest-schema">02-plugin-manifest-schema.md</a> §4 and §7). <code>main</code> may be a no-op module when the plugin contributes nothing else.</p><h3 id="_3-2-importing-a-pi-cli-extension-or-skill-package" tabindex="-1">3.2 Importing a pi CLI extension or skill package <a class="header-anchor" href="#_3-2-importing-a-pi-cli-extension-or-skill-package" aria-label="Permalink to &quot;3.2 Importing a pi CLI extension or skill package&quot;">​</a></h3><p>Plugins → &quot;Import pi extension&quot; opens a native picker (main owns the path, D344) for an explicit local file or directory. Main copies the selected source under <code>&lt;dataDir&gt;/plugins/imported/&lt;slug&gt;/src/</code>, writes a generated CommonJS no-op <code>main.cjs</code> and a manifest with id <code>imported.&lt;slug&gt;</code> (a unique suffix is added for repeated imports), and registers the directory through the existing local-plugin flow. The confirmation before the picker remains the trust decision; the generated manifest declares the permissions needed by its actual contributions. The manifest&#39;s <code>main</code> points to <code>main.cjs</code> regardless of the source package&#39;s <code>type</code>; both copied package declarations retain their module semantics. Loading an imported plugin whose <code>main</code> is the generated CommonJS <code>main.js</code> wrapper rewrites that file in place to <code>main.cjs</code> and updates the manifest; copied package files, grants, and activation scope stay as they are. The rewrite matches only the generated no-op (including the original comment text). A customized <code>main.js</code> is left untouched. Re-importing without removal creates a separate plugin with a unique suffix; it does not copy grants or activation scope from the older copy.</p><p>For extension files and packages without <code>pi.skills</code>, entry discovery keeps the existing <code>pi-coding-agent</code> rules: <code>package.json</code> <code>pi.extensions</code>, otherwise <code>index.ts</code> / <code>index.js</code>, otherwise loose <code>*.ts</code> / <code>*.js</code> files one level deep. A package that explicitly declares <code>pi.skills</code> and has no <code>pi.extensions</code> (or an empty array) is skill-only: incidental scripts, including <code>index.js</code>, are copied as resources but never promoted to executable agent extensions.</p><p>A directory that ships a <code>package.json</code> also has it (plus its npm lockfile) copied to the plugin root with any <code>workspaces</code> field stripped. When it declares production or optional dependencies, main performs a bounded two-step install: it first resolves <code>npm install --package-lock-only --omit=dev --legacy-peer-deps --no-audit --no-fund --ignore-scripts</code>, validates the complete generated lockfile, then runs <code>npm ci</code> with the same safety flags. Direct specs in <code>dependencies</code>, <code>optionalDependencies</code>, <code>devDependencies</code>, and <code>peerDependencies</code> must be registry-only because npm may inspect all four; the git resolver is disabled. No lifecycle script runs. Failed installs remove partial dependencies/cache and are reported to the renderer without blocking the import. The confirm discloses the npm step alongside the skills disclosure.</p><p>If npm or its Node.js runtime is unavailable, the installer returns a structured unavailable error. Only this category opens a native Main-owned message box with &quot;Choose npm&quot; and &quot;Cancel&quot;; ordinary registry, network, dependency-policy, or installation errors never open an executable-path prompt. The message explains that npm must come from a trusted Node.js installation, that the choice is remembered for future imports, and that Node.js must be installed first if absent. Choosing npm opens a native executable picker with <code>showHiddenFiles</code>, allowing navigation into installations such as <code>~/.nvm</code>. On macOS, <code>noResolveAliases</code> preserves the selected <code>bin/npm</code> symlink rather than returning its internal CLI target, so the sibling <code>node</code> remains discoverable. Selected paths never come from renderer text input.</p><p>Main validates the selected executable and its Node.js runtime with bounded version checks before saving or installing. The checks share the installation budget and stop at a bounded ceiling, because a slow machine must not report a working npm as unavailable and send the user back to the picker. Invalid selections (including npm without usable Node.js) show a localized native warning and let the user choose again or cancel; the guidance is to choose npm in the same directory as <code>node</code>. Only a validated selection is persisted, atomically, in Main-owned <code>&lt;dataDir&gt;/npm-path.json</code>, not renderer settings or Host SQLite. Future imports reuse it after validation; a stale saved path returns to the recovery prompt. If persistence fails, a native warning explains that the choice could not be saved but the current import can still use the validated executable.</p><p>The selected executable&#39;s directory is added only to the install child&#39;s <code>PATH</code> so npm can find <code>node</code>; no shell startup probing or global environment mutation is permitted. Version checks and installation retain a minimal environment with no inherited credentials. The registry-only proxy, isolated npm configuration, bounded two-step install, disabled git resolution, and disabled lifecycle scripts remain unchanged. Configured Windows <code>.cmd</code>/<code>.bat</code> launchers use the adjacent <code>node.exe</code> and <code>node_modules/npm/bin/npm-cli.js</code> directly, without shell interpolation; a nonstandard launcher missing this layout is rejected with recovery guidance. Recovery retries dependency installation in the already-generated plugin directory: it never recopies the source or allocates another plugin id. After recovery succeeds, is cancelled (including closing the picker), or ends in a dependency error, the generated plugin is loaded and registered exactly once; dependency errors remain visible without blocking registration.</p><p>All native recovery labels use the active locale&#39;s flat <code>plugins.npmMissingTitle</code>, <code>npmMissingBody</code>, <code>npmChoose</code>, <code>npmPickerTitle</code>, <code>npmInvalidTitle</code>, <code>npmInvalidBody</code>, <code>npmSaveFailedTitle</code>, and <code>npmSaveFailedBody</code> keys, plus existing <code>common.cancel</code>. Bodies are standalone localized text without interpolation placeholders; Main appends any dynamic diagnostic on a new line.</p><table tabindex="0"><thead><tr><th>Source</th><th>Becomes</th></tr></thead><tbody><tr><td>A pi extension directory or file</td><td>A local plugin under <code>plugins/imported</code>, id <code>imported.&lt;slug&gt;</code></td></tr><tr><td>A plugin package declaring <code>contributes.agentExtensions</code></td><td>Installed like any plugin; the grant is asked for at install</td></tr><tr><td>A <code>package.json</code> with <code>pi.extensions</code></td><td>Entries under <code>src/</code>, exposed through <code>contributes.agentExtensions</code> with <code>agent.extension</code></td></tr><tr><td>A <code>package.json</code> with <code>pi.skills</code></td><td>Markdown documents under <code>src/</code>, exposed through <code>contributes.skills</code> with <code>agent.prompt.inject</code></td></tr><tr><td>A skill-only package</td><td>A no-op plugin holding <code>agent.prompt.inject</code>, without <code>agent.extension</code></td></tr></tbody></table><p><code>pi.skills</code> is an array of at most 32 nonempty relative Markdown-file or directory paths. An explicit <code>.md</code> file contributes that document. For a directory, its own <code>SKILL.md</code> takes precedence; otherwise directly contained <code>.md</code> files are included and subdirectories are searched for <code>SKILL.md</code>. Nested skill directories stop at their own <code>SKILL.md</code>, so support documents are not turned into extra skills. Scanning skips dot-prefixed entries and <code>node_modules</code>, deduplicates documents, and has a budget of 256 directories. More than 32 discovered skills, a missing declaration, or an unsupported path fails the import rather than silently yielding an incomplete catalog. Each contribution has an explicit, stable plugin-local ID derived from its package-relative path; different directories named <code>SKILL.md</code> remain independent skills. Normal plugin skill parsing, size limits, grants, and unload behavior remain in force.</p><p>Copying uses paths relative to the selected package. A package installed under an ancestor <code>node_modules</code> directory is copied normally; only its own <code>node_modules</code> directory segments are excluded. References, assets, helper scripts, and other ordinary source files remain under <code>src/</code>, preserving skill-relative resource paths. Credential files (<code>.env*</code>, <code>.npmrc</code>, <code>.netrc</code>, <code>.pypirc</code>, private-key and certificate files) and repository metadata directories are not copied. The selected root is resolved to its real path. Contribution paths must stay inside that root, cannot traverse <code>..</code>, and cannot point into its dependency directories. Absolute <code>pi.skills</code> paths and descendant symbolic links are rejected. Copying also rejects symbolic links among retained resources and removes a partial copy on failure. The generated destination is created atomically and must not be inside the selected source.</p><p>This is an explicit local import, not a pi CLI package manager. It never automatically scans or imports <code>~/.pi</code>, does not read the CLI&#39;s installed package registry, and does not run npm lifecycle scripts. When dependencies are declared, the bounded installer accepts only registry version specs and registry-resolved npm lockfiles, rejects unsafe package locations and nested dependency specs, disables git resolution, and isolates npm&#39;s config/cache from the user&#39;s credentials and proxy settings. Importing a package does not promise that every third-party extension dependency can execute.</p><h2 id="_4-loading-and-runtime" tabindex="-1">4. Loading and runtime <a class="header-anchor" href="#_4-loading-and-runtime" aria-label="Permalink to &quot;4. Loading and runtime&quot;">​</a></h2><h2 id="_4-loading-and-runtime-1" tabindex="-1">4. Loading and runtime <a class="header-anchor" href="#_4-loading-and-runtime-1" aria-label="Permalink to &quot;4. Loading and runtime&quot;">​</a></h2><h3 id="_4-1-where-extensions-run" tabindex="-1">4.1 Where extensions run <a class="header-anchor" href="#_4-1-where-extensions-run" aria-label="Permalink to &quot;4.1 Where extensions run&quot;">​</a></h3><p>Extensions load inside the Agent sidecar process (<code>packages/agent-runtime</code>), never in Electron main, the renderer, or a plugin host process.</p><h3 id="_4-2-loader" tabindex="-1">4.2 Loader <a class="header-anchor" href="#_4-2-loader" aria-label="Permalink to &quot;4.2 Loader&quot;">​</a></h3><ul><li>The sidecar pins <code>@earendil-works/pi-coding-agent</code> at exactly the version pinned for <code>pi-ai</code> and <code>pi-agent-core</code>. The three versions must match; CI fails when they drift. Native Pi sessions run its extension loader in process, so the pin is a bundled runtime dependency and not a types-only contract.</li><li>The loader mirrors the <code>pi-coding-agent</code> discovery rules and uses <code>jiti/static</code> with <code>virtualModules</code>, so the babel transform is bundled and no path resolution happens at runtime. The bundling step is verified by a contract test that runs the bundle outside the repository (E2E-245).</li><li>The bundle is built with <code>--define:PI_BUNDLED_NODE=true</code>. The <code>pi-coding-agent</code> extension loader embeds the kernel modules and <code>typebox</code> for a compiled binary, for a bundled Node distribution, and for its own TypeScript-source runtime; every other Node build resolves them from the importing file, which a packaged install (<code>resources/agent-runtime/</code>) cannot satisfy. An entry point that bundles pi&#39;s session or extension loader in process needs the same define.</li><li>Import aliases: <code>pi-ai</code>, <code>pi-agent-core</code>, and <code>typebox</code> resolve to the sidecar&#39;s copies; <code>@earendil-works/pi-coding-agent</code> resolves to a runtime shim that exports <code>defineTool</code> and the tool-result type guards. <code>@earendil-works/pi-tui</code> resolves to a stub module that exports every symbol as an inert value so a top-level import never fails. Using a stubbed symbol raises a diagnostic at call time.</li></ul><h3 id="_4-3-runner-per-session" tabindex="-1">4.3 Runner per session <a class="header-anchor" href="#_4-3-runner-per-session" aria-label="Permalink to &quot;4.3 Runner per session&quot;">​</a></h3><ul><li>Each desktop session gets its own Runner. The Runner is created with the session&#39;s runtime and disposed when the session&#39;s runtime is discarded.</li><li>Module instances are shared across Runners because jiti caches modules. Module-level state is therefore shared between sessions, matching what an extension author sees when pi runs several sessions in one process. This is documented, not worked around, in v1.</li><li>Enabling, disabling, or rescanning invalidates every Runner; affected sessions reload extensions at the next turn boundary. A running turn is never interrupted by a reload.</li></ul><h3 id="_4-4-load-failures" tabindex="-1">4.4 Load failures <a class="header-anchor" href="#_4-4-load-failures" aria-label="Permalink to &quot;4.4 Load failures&quot;">​</a></h3><p>A load error never fails the session. The extension is marked <code>error</code> with the message and stack in diagnostics, the remaining extensions load, and the turn proceeds. The composer shows a one-line notice when an enabled extension failed to load for the active session.</p><h2 id="_5-api-support-matrix-v1" tabindex="-1">5. API support matrix (v1) <a class="header-anchor" href="#_5-api-support-matrix-v1" aria-label="Permalink to &quot;5. API support matrix (v1)&quot;">​</a></h2><p>Every <code>ExtensionAPI</code> member falls into exactly one class. Unsupported members exist on the object, do nothing, return the documented neutral value, and emit one diagnostic per extension per member. They never throw, so an extension that only uses supported members works even if it also touches unsupported ones.</p><table tabindex="0"><thead><tr><th>Class</th><th>Members</th></tr></thead><tbody><tr><td>Supported</td><td><code>registerTool</code>, <code>registerCommand</code>, <code>registerAgent</code>, <code>registerProvider</code> (plugin-owned compatibility alias; same shape as <code>registerAgent</code>), <code>unregisterAgent</code>, <code>unregisterProvider</code>, <code>on(...)</code> for every event in §6, <code>exec</code>, <code>getActiveTools</code>, <code>getAllTools</code>, <code>setActiveTools</code>, <code>getCommands</code>, <code>setModel</code> (configured models and plugin agents; idle-only; persists the current session binding), <code>getThinkingLevel</code>, <code>setThinkingLevel</code>, <code>setSessionName</code>, <code>getSessionName</code>, <code>sendUserMessage</code> (Host-owned queue, D386), <code>getFlag</code></td></tr><tr><td>Supported on context</td><td><code>ui.notify</code>, <code>ui.confirm</code>, <code>ui.select</code>, <code>ui.input</code>, <code>ui.setStatus</code>, <code>ui.setWorkingMessage</code>, <code>cwd</code>, <code>modelRegistry</code>, <code>isIdle</code>, <code>abort</code>, <code>hasPendingMessages</code>, <code>getContextUsage</code>, <code>compact</code>, <code>getSystemPrompt</code>, <code>waitForIdle</code>, <code>newSession</code>, <code>fork</code></td></tr><tr><td>Deferred to v2</td><td><code>sendMessage</code>, <code>appendEntry</code>, <code>setLabel</code>, <code>sessionManager</code> read API, <code>switchSession</code>, <code>registerShortcut</code>, <code>registerMarkdownTransformer</code>, <code>ui.setEditorText</code>, <code>ui.getEditorText</code>, <code>ui.addAutocompleteProvider</code>, <code>registerFlag</code> value editing</td></tr><tr><td>Unsupported</td><td><code>ui.setWidget</code>, <code>ui.setFooter</code>, <code>ui.setHeader</code>, <code>ui.setTitle</code>, <code>ui.custom</code>, <code>ui.overlay</code>, <code>ui.onTerminalInput</code>, <code>ui.setWorkingVisible</code>, <code>ui.setWorkingIndicator</code>, <code>ui.setHiddenThinkingLabel</code>, <code>ui.pasteToEditor</code>, <code>ui.editor</code>, <code>registerMessageRenderer</code>, <code>registerEntryRenderer</code>, <code>navigateTree</code>, <code>shutdown</code></td></tr></tbody></table><p><code>registerAgent({ id, name?, models, stream? | complete? })</code> registers a session-scoped plugin-owned LLM integration. Each model declares bounded public metadata (<code>id</code>, display name, API label, modalities, reasoning and limits). The plugin callback receives the pi-ai model/context/options and owns endpoint, authentication, request serialization, and response conversion. It must honor <code>options.signal</code> for cancellation. <code>complete</code> is adapted to a one-result stream.</p><p>The host assigns <code>extension-agent:&lt;encoded-agent-key&gt;</code> as the provider id. A successful idle <code>setModel</code> persists that provider/model pair through <code>session.configure</code>; the next turn reloads the trusted extension and restores the agent implementation. <code>modelRegistry</code> exposes only models and auth availability; it never exposes Host API keys, secret refs, OAuth tokens, arbitrary Host headers, or Host provider internals. <code>registerProvider</code> and its unregister counterpart accept the same plugin-owned shape as a compatibility alias — a <code>stream</code> or <code>complete</code> implementation, in the upstream <code>(id, config)</code> form and in the object form; provider credentials in the upstream config are ignored by Host and are not persisted.</p><p>Neutral values: <code>getFlag</code> returns the declared default; <code>registerFlag</code> records the declaration so <code>getFlag</code> works but exposes no CLI or UI in v1; <code>sessionManager</code> accessors return empty results; UI setters return a no-op <code>dispose</code>.</p><h2 id="_6-event-mapping" tabindex="-1">6. Event mapping <a class="header-anchor" href="#_6-event-mapping" aria-label="Permalink to &quot;6. Event mapping&quot;">​</a></h2><p>Events fire from the desktop runtime&#39;s existing hook points. Handler results are honored where the event type defines a result.</p><table tabindex="0"><thead><tr><th>Event</th><th>Desktop hook point</th><th>Result honored</th></tr></thead><tbody><tr><td><code>session_start</code>, <code>session_shutdown</code></td><td>Runner creation and disposal</td><td>No</td></tr><tr><td><code>session_info_changed</code></td><td>Session rename through <code>setSessionName</code></td><td>No</td></tr><tr><td><code>project_trust</code></td><td>v1 note: not emitted; enablement per project is the trust decision</td><td>No</td></tr><tr><td><code>resources_discover</code></td><td>v1 note: not emitted; skills and prompt discovery stay in Electron main</td><td>n/a</td></tr><tr><td><code>before_agent_start</code></td><td>Before the first provider request of a turn</td><td>Yes, system prompt replacement only</td></tr><tr><td><code>context</code></td><td><code>prepareNextTurn</code></td><td>Yes, replacement message list</td></tr><tr><td><code>before_provider_request</code>, <code>before_provider_headers</code>, <code>after_provider_response</code></td><td>Provider call wrapper</td><td>Request return value; headers mutate the payload in place</td></tr><tr><td><code>agent_start</code>, <code>agent_end</code>, <code>agent_settled</code></td><td>Agent loop boundaries</td><td>No</td></tr><tr><td><code>turn_start</code>, <code>turn_end</code></td><td>Turn boundaries</td><td>No</td></tr><tr><td><code>message_start</code>, <code>message_update</code>, <code>message_end</code></td><td>Agent message events</td><td>v1 note: no, pi-agent-core offers no post-hoc replacement</td></tr><tr><td><code>tool_call</code></td><td><code>beforeToolCall</code></td><td>Yes, block with reason</td></tr><tr><td><code>tool_execution_start</code>, <code>tool_execution_update</code>, <code>tool_execution_end</code></td><td>Tool execution stream</td><td>No</td></tr><tr><td><code>tool_result</code></td><td><code>afterToolCall</code></td><td>Yes, replacement result</td></tr><tr><td><code>model_select</code>, <code>thinking_level_select</code></td><td>v1 note: not emitted; a binding change retires the runtime</td><td>No</td></tr><tr><td><code>session_before_compact</code>, <code>session_compact</code>, <code>session_compact_failed</code></td><td>Compaction pipeline</td><td>Yes for <code>session_before_compact</code></td></tr><tr><td><code>session_before_fork</code></td><td>v1 note: not emitted; fork runs in Electron main</td><td>n/a</td></tr><tr><td><code>input</code></td><td>v1 note: not emitted; Host queue admission is not wired yet</td><td>n/a</td></tr><tr><td><code>user_bash</code>, <code>session_before_switch</code>, <code>session_before_tree</code>, <code>session_tree</code>, <code>ui_prompt_start</code>, <code>ui_prompt_end</code></td><td>Not emitted in v1</td><td>n/a</td></tr></tbody></table><p>Desktop event capabilities are maintained in <code>packages/agent-runtime/src/extensions/event-capabilities.ts</code>: result, mutation, notification, or deferred. Registering a deferred event remains accepted but emits an <code>unsupported_api</code> diagnostic in the existing plugin diagnostics; it does not prevent supported handlers from loading.</p><p>Every event handler, including startup, shutdown, and notifications, has a 30-second <strong>per-handler</strong> wait budget. Module loading and factory initialization also have separate 30-second wait budgets, reported as load/factory errors. A handler that throws or times out produces a diagnostic and counts as <code>undefined</code>; subsequent handlers still run in registration order. Existing result folding and fail-open semantics are unchanged. This is not a mandatory security-check mechanism. Multiple stalled handlers can each consume their budget.</p><p>Abort retires pending event dispatches. Disposal first rejects new dispatches and cancels existing waits, then runs shutdown once even under concurrent disposal. Old dispatches return no result and never invoke their remaining handlers; late settlements do not add diagnostics or overwrite results. A factory finishing after disposal cannot publish tools or commands. Runtime shutdown cancels agent work before waiting for extension shutdown. Stopping during preflight hooks prevents the provider request and retains the user message; a later prompt can run normally.</p><p>Each invocation now owns an abort signal exposed as <code>ctx.signal</code>. Finishing, timing out, stopping or disposing retires that invocation. SDK calls from its late callbacks are rejected, including commands waiting for idle, session creation, fork or queue admission. An already admitted Host transaction is not rolled back; late completion cannot start a subsequent queue-priority update or mutate runtime model state. Commands and tool executions have no event-style 30-second limit: they may run until completion, their supplied signal aborts, Stop, or disposal. Tool updates/results after retirement are discarded; accepted updates and results are detached before publication so later extension mutations cannot rewrite them. SDK <code>exec</code> owns its process group/tree and terminates it on scope retirement or its explicit timeout; disposal waits for tracked process cleanup, with cleanup failures diagnosed. Processes deliberately escaping the group and direct Node API spawns are outside this ownership contract.</p><p>Result-bearing hook inputs and outputs are detached copies. Header mutations are committed only after a handler succeeds within its budget. Late in-place mutations cannot alter the caller&#39;s payload or another handler&#39;s input.</p><p>These are cooperative lifecycle boundaries, not forced execution isolation: trusted code may still block the JS thread or use direct Node APIs for external side effects. Native Pi sessions use the upstream SDK lifecycle and are outside this Desktop change. See ADR <code>trusted-extension-operation-ownership</code>. The 30-second event budget also applies when a handler waits for a UI prompt; the UI broker&#39;s own prompt timeout does not extend that budget.</p><h2 id="_7-tools" tabindex="-1">7. Tools <a class="header-anchor" href="#_7-tools" aria-label="Permalink to &quot;7. Tools&quot;">​</a></h2><ol><li>A registered tool joins the session&#39;s tool catalog under its declared name. A name that collides with a core tool, a plugin tool, or a user MCP tool is rejected with a diagnostic; the earlier registration wins.</li><li>Extension tools are non-core: they follow the same mode gating and ToolSearch deferral as plugin tools. They are available in Agent mode and follow the existing per-mode allowlist elsewhere.</li><li>Execution happens in the sidecar with the <code>ExtensionAPI</code> <code>execute</code> signature. No host permission prompt is raised; the trust decision was made at enablement. <code>onUpdate</code> streams map to tool execution update events.</li><li>Every execution writes an audit line with extension id, tool name, and duration. Parameters are not logged.</li><li><code>exec</code> runs in the sidecar with the session&#39;s working directory and the session&#39;s proxy and environment settings.</li></ol><h2 id="_8-commands" tabindex="-1">8. Commands <a class="header-anchor" href="#_8-commands" aria-label="Permalink to &quot;8. Commands&quot;">​</a></h2><ol><li><code>registerCommand</code> entries appear in the Commands section of global search (see <a href="./09-plugin-command-palette">09-plugin-command-palette.md</a>) as <code>/&lt;name&gt;</code> with the extension&#39;s label as the source, after built-in and plugin commands.</li><li>A command runs in the sidecar with the extension command context bound to the active session. It requires an active session whose runtime has loaded extensions in this app run; otherwise the composer reports that a chat must be started first.</li><li>Commands typed in the composer as <code>/&lt;name&gt;</code> resolve in this order: built-in, prompt template, plugin, extension. Collisions are diagnostics.</li><li>A running command blocks composer submission the same way a plugin command does and can be cancelled from the status line.</li></ol><h2 id="_9-ui-bridge" tabindex="-1">9. UI bridge <a class="header-anchor" href="#_9-ui-bridge" aria-label="Permalink to &quot;9. UI bridge&quot;">​</a></h2><p>Interactive context calls travel sidecar → Electron main → renderer and back.</p><table tabindex="0"><thead><tr><th>Call</th><th>Renderer surface</th><th>Timeout</th><th>On abort</th></tr></thead><tbody><tr><td><code>ui.notify</code></td><td>Toast</td><td>none</td><td>dropped</td></tr><tr><td><code>ui.confirm</code></td><td>Modal with two actions</td><td>5 min</td><td>resolves <code>false</code></td></tr><tr><td><code>ui.select</code></td><td>Modal list</td><td>5 min</td><td>resolves <code>undefined</code></td></tr><tr><td><code>ui.input</code></td><td>Modal text field</td><td>5 min</td><td>resolves <code>undefined</code></td></tr><tr><td><code>ui.setStatus</code>, <code>ui.setWorkingMessage</code></td><td>Floating status line for the active session (v1 note: not inside the composer)</td><td>none</td><td>cleared</td></tr></tbody></table><p>Rules:</p><ul><li>One pending interactive prompt per session. A second call queues behind the first.</li><li>Aborting the turn cancels pending prompts with the abort values above.</li><li>Every new sidecar UI request has an invocation-owned request ID. Cancellation targets that ID plus the session/extension identity, drops queued requests, and sends retirement for the exact visible prompt to the renderer. Stale cancellation cannot close a later request. Legacy requests without IDs retain session-wide cancellation. Settled queue tails are released.</li><li>Under remote control (Post-MVP) the prompt fails immediately with <code>UNSUPPORTED</code> until the remote protocol routes it; that routing is v3.</li><li>Prompts show the extension label and source path so the user knows who is asking.</li></ul><h2 id="_10-protocol-and-ipc-additions" tabindex="-1">10. Protocol and IPC additions <a class="header-anchor" href="#_10-protocol-and-ipc-additions" aria-label="Permalink to &quot;10. Protocol and IPC additions&quot;">​</a></h2><p>No host-core RPC method, protocol version, or SQLite schema changes in v1.</p><h3 id="_10-1-sidecar-→-main-host-proxy-allowlist" tabindex="-1">10.1 Sidecar → main (host.proxy allowlist) <a class="header-anchor" href="#_10-1-sidecar-→-main-host-proxy-allowlist" aria-label="Permalink to &quot;10.1 Sidecar → main (host.proxy allowlist)&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Method</th><th>Purpose</th></tr></thead><tbody><tr><td><code>extensions.commands.publish</code></td><td>Replace the session&#39;s registered command list</td></tr><tr><td><code>extensions.ui.request</code></td><td>One interactive or status call from §9</td></tr><tr><td><code>extensions.diagnostics.publish</code></td><td>Replace the session&#39;s diagnostics list</td></tr><tr><td><code>extensions.model.configure</code></td><td>Validate and persist a plugin-owned provider/model binding through <code>session.configure</code>, then broadcast <code>session:modelChanged</code></td></tr><tr><td><code>session.rename</code>, <code>session.create</code>, <code>session.fork</code>, <code>session.queuePush</code>, <code>session.queuePrioritize</code></td><td>Existing methods, now reachable from the adapter</td></tr></tbody></table><h3 id="_10-2-main-↔-renderer-electron-ipc" tabindex="-1">10.2 Main ↔ renderer (Electron IPC) <a class="header-anchor" href="#_10-2-main-↔-renderer-electron-ipc" aria-label="Permalink to &quot;10.2 Main ↔ renderer (Electron IPC)&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Channel</th><th>Direction</th><th>Purpose</th></tr></thead><tbody><tr><td><code>plugin/importExtension</code></td><td>request</td><td>Native picker, generate the plugin, register it as a development plugin</td></tr><tr><td><code>extensions/commands/run</code></td><td>request</td><td>Run a registered command in the active session</td></tr><tr><td><code>extensions/ui/respond</code></td><td>request</td><td>Answer a pending prompt</td></tr><tr><td><code>extensions/ui/prompt</code></td><td>event</td><td>A prompt is pending</td></tr><tr><td><code>extensions/event/status</code></td><td>event</td><td><code>ui.setStatus</code> / <code>ui.setWorkingMessage</code> text changed</td></tr><tr><td><code>plugin/list</code></td><td>request</td><td>Plugin rows carry <code>agentExtension</code> state, tool, command, custom-agent names, and diagnostics</td></tr><tr><td><code>event/pluginChanged</code></td><td>event</td><td>Also fires when a session publishes commands, diagnostics, or model binding changes</td></tr></tbody></table><p>All channels are sender-validated like other plugin channels. The MCP control plane exposes <code>extensions/commands/run</code> (write) and <code>extensions/ui/respond</code> (dangerous, confirm required); the import is a native picker and stays local. Main audits each prompt id in <code>logs/app/plugin.log</code>.</p><h2 id="_11-plugin-row-surface" tabindex="-1">11. Plugin row surface <a class="header-anchor" href="#_11-plugin-row-surface" aria-label="Permalink to &quot;11. Plugin row surface&quot;">​</a></h2><p>The Plugins page shows agent extensions on the owning plugin&#39;s row:</p><ul><li>The <code>agentExtension</code> capability chip and the <code>agent.extension</code> permission chip (high risk) beside the other capabilities and permissions.</li><li>A details section with a state chip (<code>enabled</code> until a session loads the modules in this app run, <code>loaded</code>, <code>error</code>), the registered tool, slash command and custom-agent names, and the diagnostics: load errors, unsupported API calls with counts, rejected registrations, handler timeouts.</li><li>&quot;Import pi extension&quot; in the page&#39;s overflow actions, guarded by a confirm that states what the grant means.</li></ul><h2 id="_12-phasing" tabindex="-1">12. Phasing <a class="header-anchor" href="#_12-phasing" aria-label="Permalink to &quot;12. Phasing&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Phase</th><th>Content</th><th>Commitment</th></tr></thead><tbody><tr><td>v1</td><td>Loader, Runner per session, support matrix, events, tools, commands, UI bridge</td><td>Shipped (D387)</td></tr><tr><td>v1.1</td><td>Modules become <code>contributes.agentExtensions</code> with the <code>agent.extension</code> grant; import of pi CLI extensions as development plugins; the standalone registry and settings tab are removed</td><td>Shipped (D388)</td></tr><tr><td>v1.1 amendment</td><td>Plugin-owned custom agents via <code>registerAgent</code>, provider compatibility alias, redacted model registry, idle-only session binding and restore through <code>extension-agent:</code> ids</td><td>Implemented (D426 / ADR 0258)</td></tr><tr><td>v2</td><td>Custom session entries (<code>sendMessage</code>, <code>appendEntry</code>) with a schema bump and a generic renderer, <code>sessionManager</code> read shim, <code>switchSession</code>, editor read and write, autocomplete providers, <code>registerShortcut</code>, markdown transformers</td><td>Planned, needs a decision on entry persistence and compaction</td></tr><tr><td>v3</td><td><code>pi</code> package manifests and installation, read-only hints from the pi CLI&#39;s <code>settings.json</code>, unified skill and prompt discovery, remote-control routing for prompts, marketplace listing</td><td>Not scheduled</td></tr></tbody></table><p>v1 delivery order: bundling spike (E2E-245), shared protocol types, then the runtime, main, and renderer tracks in parallel.</p><h2 id="_13-versioning-policy" tabindex="-1">13. Versioning policy <a class="header-anchor" href="#_13-versioning-policy" aria-label="Permalink to &quot;13. Versioning policy&quot;">​</a></h2><ul><li>Upgrading any pi package upgrades all three together.</li><li>A fixture set of sample extensions covering each supported member runs as a contract test on every upgrade.</li><li>New <code>ExtensionAPI</code> members land in the Unsupported class with a diagnostic until a later decision moves them.</li><li>Public documentation promises only the Supported and Supported-on-context classes in §5.</li></ul><h2 id="_14-open-decisions" tabindex="-1">14. Open decisions <a class="header-anchor" href="#_14-open-decisions" aria-label="Permalink to &quot;14. Open decisions&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Question</th><th>Default until decided</th></tr></thead><tbody><tr><td>Should v2 custom entries persist to host-core and take part in compaction?</td><td>Persist; excluded from compaction summaries</td></tr><tr><td>Should v3 read the pi CLI&#39;s <code>settings.json</code> enabled paths as discovery hints?</td><td>Read-only hints, never written</td></tr><tr><td>Should extension tools be selectable per project like plugin tools?</td><td>Scope from §3.2 is the only gate</td></tr></tbody></table></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("spec/07-plugins/16-trusted-extensions.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _16TrustedExtensions = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _16TrustedExtensions as default
};
