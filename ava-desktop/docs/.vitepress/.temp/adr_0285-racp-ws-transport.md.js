import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0285: RACP-WS transport in packages/racp","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0285-racp-ws-transport.md","filePath":"adr/0285-racp-ws-transport.md","lastUpdated":1789728753000}');
const _sfc_main = { name: "adr/0285-racp-ws-transport.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0285-racp-ws-transport-in-packages-racp" tabindex="-1">ADR 0285: <code>RACP-WS</code> transport in <code>packages/racp</code> <a class="header-anchor" href="#adr-0285-racp-ws-transport-in-packages-racp" aria-label="Permalink to &quot;ADR 0285: \`RACP-WS\` transport in \`packages/racp\`&quot;">​</a></h1><ul><li>Status: Accepted for implementation</li><li>Date: 2026-09-18</li><li>Decision: D448</li><li>Related: ADR 0205 (D373 / D374 / D375), ADR 0284 (D447), <code>03-runtime/19-remote-agent-control-protocol.md</code> §3, §4, §8, §11.1, §14a, <code>05-security/02-remote-control-security.md</code> §3.4, §5.1</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>R0 froze the RACP contract as typebox schemas and R1 delivered the headless Agent Host module. Nothing yet moved a JSON-RPC frame between two processes: the module was only driven by Electron IPC. R2 needs the normative <code>RACP-WS</code> binding on both sides of an SSH tunnel — a server that the <code>pi-host</code> bundle binds on loopback and a client that Electron main runs — and it needs the device-token pairing of security §3.4, which the security spec described but no code implemented.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><strong>A new workspace package, <code>packages/racp</code>, holds both ends of the binding.</strong> It depends on <code>shared</code>, <code>agent-host</code>, and <code>ws</code> only. The server core (<code>RacpServer</code>) and the client core (<code>RacpClient</code>) take an injected transport, so the same code runs over <code>ws</code> in production and over an in-memory pair in tests; <code>bindRacpWebSocket</code> and <code>wsClientTransport</code> are the <code>ws</code> adapters.</li><li><strong>The server is a thin catalog over the module.</strong> Every operation in <code>RACP_OPERATIONS</code> is dispatched through the catalog&#39;s role rule; the session, turn, event, approval, and input operations call the Agent Host module directly; the remote-host profile (session catalog mutations, projects, workspace reads, terminals) goes through the <code>RacpHostOperations</code> interface that <code>pi-host</code> implements. The server never touches host-core RPC, a filesystem, or a pty (security §7).</li><li><strong>Header-profile authentication with Host-issued device tokens.</strong> The upgrade request must carry <code>Authorization: Bearer</code>; a token in the URL, a non-loopback peer, a wrong path, a missing subprotocol, or a binary frame is refused before any RPC. Tokens are <code>pdt1.</code> (device) or <code>ppt1.</code> (pairing), stored as SHA-256 hashes, and compared in constant time. A pairing token is single-use and expiring; it authenticates an unprivileged connection that may only call <code>connection/pair</code>, which mints an <code>owner</code> device.</li><li><strong>Three catalog additions, all in the remote-host profile:</strong><code>connection/pair</code>, <code>project/register</code>, and <code>project/browse</code>. Six codes join the shared error registry: <code>PAIRING_FAILED</code>, <code>PAIRING_TOKEN_EXPIRED</code>, <code>CAPABILITY_UNAVAILABLE</code>, <code>REMOTE_PATH_NOT_FOUND</code>, <code>REMOTE_PATH_FORBIDDEN</code>, and the desktop-side connection codes <code>HOST_DISCONNECTED</code>, <code>HOST_BOOTSTRAP_FAILED</code>, <code>HOST_VERSION_MISMATCH</code>, <code>REMOTE_AUTH_FAILED</code>, <code>REMOTE_CONNECTION_FAILED</code>, <code>REMOTE_FORWARD_FAILED</code>. <code>connection/initialize</code> gains <code>server.hostId</code>. The binding details that the protocol left open are recorded in spec §14a.</li><li><strong>Reconnect is the client&#39;s, replay is the Host&#39;s.</strong> The client reconnects with bounded backoff, rejects the dropped connection&#39;s in-flight calls with <code>HOST_DISCONNECTED</code>, and never re-sends them; it remembers the last durable cursor per session so the caller resubscribes with <code>after</code>. The Host answers with a replay, or with a snapshot and a <code>resyncReason</code>, exactly as the module already did for IPC. A dropped connection releases its subscriptions and terminal attachments and touches no turn.</li><li><strong><code>StartTurnParams.input.userMessageId</code></strong> is threaded from <code>turn/start</code>&#39;s <code>input.messageId</code> through the queue record to the runtime, so a remote client keeps its optimistic user row id (D288) the way the renderer does over IPC.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li><code>pi-host</code> composes <code>RacpServer</code> + <code>bindRacpWebSocket</code> over the module and <code>RuntimeService</code> (ADR 0284); the desktop adapter composes <code>RacpClient</code> + <code>wsClientTransport</code>.</li><li>The conformance behaviors of spec §14 that do not need a machine boundary (handshake, authorization, idempotency, queue ordering, approval decisions, cursor replay, eviction, epoch change, slow clients, reconnect without duplicate execution) are tests in <code>packages/racp</code> and run without Electron.</li><li>Attachments and the tool relay are advertised as unavailable (<code>attachments: false</code>, <code>toolRelay: false</code>) and their operations fail with <code>CAPABILITY_UNAVAILABLE</code>; they land with their own change.</li><li>The server bind refuses a non-loopback address outright rather than offering a TLS path; a non-loopback deployment is a later decision.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Run RACP inside Electron main first (development loopback endpoint).</strong> Rejected for this change: the only scheduled client is the desktop, and the first server is <code>pi-host</code>; a desktop-side listener would add a network surface with no consumer.</li><li><strong>Cookie profile alongside the header profile.</strong> Rejected: it belongs to the unscheduled browser milestone (D375).</li><li><strong>Static shared secret instead of pairing.</strong> Rejected by security §3.2 and §3.4: a pairing token must be single-use and become a per-device credential that can be revoked on its own.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0285-racp-ws-transport.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0285RacpWsTransport = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0285RacpWsTransport as default
};
