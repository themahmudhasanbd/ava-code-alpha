import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0292: SSH bootstrap for remote hosts","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0292-ssh-remote-host-bootstrap.md","filePath":"adr/0292-ssh-remote-host-bootstrap.md","lastUpdated":1789761544000}');
const _sfc_main = { name: "adr/0292-ssh-remote-host-bootstrap.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0292-ssh-bootstrap-for-remote-hosts" tabindex="-1">ADR 0292: SSH bootstrap for remote hosts <a class="header-anchor" href="#adr-0292-ssh-bootstrap-for-remote-hosts" aria-label="Permalink to &quot;ADR 0292: SSH bootstrap for remote hosts&quot;">​</a></h1><ul><li>Status: Accepted for implementation</li><li>Date: 2026-09-19</li><li>Decision: D453</li><li>Related: ADR 0205 (D373 / D374 / D375), ADR 0285 (D448), ADR 0286 (D449), <code>02-architecture/05-remote-agent-control.md</code> §5.2, <code>05-security/02-remote-control-security.md</code> §3.4, <code>06-delivery/07-remote-control-rollout.md</code> §2 R2b</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>ADR 0286 delivered the desktop-side R2a kernel and left the bootstrap explicitly out of scope: &quot;Every paired host today assumes the loopback URL already exists.&quot; Reaching another machine meant the user installed <code>pi-host</code> there, started it, opened <code>ssh -L</code> themselves, and pasted the resulting URL and a pairing token into Settings.</p><p>R2&#39;s exit criteria do not accept that (rollout §2 R2b, security §3.4). The desktop has to install and pair a host on a machine the user already reaches over SSH, under two boundaries the security spec fixes: the desktop never uploads executable bytes, and it never holds an SSH secret.</p><p>Three properties of the system as it stands shaped the design:</p><ul><li><code>release.yml</code> publishes <code>pi-host-&lt;version&gt;-&lt;platform&gt;-&lt;arch&gt;.tar.gz</code> and its <code>.sha256</code> to the <code>v&lt;version&gt;</code> GitHub Release, and the bundle matrix is Linux x64 only.</li><li>A paired host is a URL in <code>remote-hosts.json</code>, but a bootstrapped host has no stable URL: the local port of a forward is chosen per launch.</li><li><code>bootstrap/remote-hosts.ts</code> already owns every host&#39;s lifecycle, so the bootstrap plugs into that layer instead of beside it.</li></ul><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>The bootstrap is five modules under <code>apps/desktop/electron/main/remote/</code>, one extended boot hook, and one new IPC channel.</p><ol><li><p><strong>The system <code>ssh</code> client is the transport.</strong> Shelling out to <code>ssh</code> beats linking an SSH library because the user&#39;s <code>~/.ssh/config</code>, agent, jump hosts, and <code>known_hosts</code> then apply unchanged, and the app never holds a secret to leak. <code>BatchMode=yes</code> is what keeps that honest: a host needing an interactive password or passphrase fails immediately with a typed error instead of hanging a modal behind an invisible prompt. The module always sets <code>StrictHostKeyChecking=accept-new</code>, <code>ConnectTimeout=15</code>, and <code>ExitOnForwardFailure=yes</code>.</p></li><li><p><strong>Pure release coordinates (<code>pi-host-release.ts</code>).</strong> The artifact is resolved for the <em>remote</em> platform at the <em>desktop&#39;s</em> version, and the SHA-256 the release publishes is the trust anchor. <code>PUBLISHED_TARGETS</code> is the release matrix (<code>linux-x64</code> today), so any other target is refused with <code>HOST_BOOTSTRAP_FAILED</code> before a download starts rather than as a 404 halfway through one. URL derivation, <code>uname</code> parsing, checksum-file parsing, constant-work digest comparison, and the version-parity rule are all pure functions with no network and no SSH.</p></li><li><p><strong>One script runs remotely (<code>pi-host-bootstrap-script.ts</code>).</strong> It downloads the pinned URL, verifies the digest, installs under <code>$HOME/.pi-desktop/pi-host</code>, (re)starts the host on loopback with <code>--pair</code>, and echoes <code>PI_HOST_READY</code> / <code>PI_HOST_PAIRING_TOKEN</code>. It runs under <code>umask 077</code>, so the single-use pairing token only ever exists in a work file inside the bootstrap directory and on the SSH channel&#39;s stdout — never a world-readable path, never a URL (security §3.4). Every failure exits non-zero after a <code>PI_HOST_BOOTSTRAP_FAILED &lt;step&gt;</code> line, so the desktop reports the step that failed instead of a bare exit code. Generating it is pure and every input is shell-quoted by <code>shellQuote</code>, which is what lets the tests execute the generated text for real.</p></li><li><p><strong><code>ssh-transport.ts</code> is a port, not a dependency.</strong> <code>exec</code> / <code>execWithInput</code> / <code>forward</code> / <code>dispose</code> let the orchestrator be tested end to end against a fake with no process spawned; <code>createSystemSshTransport</code> is the only implementation that spawns. <code>execWithInput</code> pipes the script into <code>sh -s</code>, so the script never survives an argv round trip and never lands in a remote file to clean up. Values that reach the <code>ssh</code> argv are refused by <code>assertSshArgument</code> when they are empty or start with <code>-</code>: passed as its own argv entry, a leading dash becomes an option (<code>-oProxyCommand=…</code>), not a destination.</p></li><li><p><strong>Forwards are durable and owned, not incidental (<code>ssh-tunnel.ts</code>).</strong> The persisted record stores the SSH descriptor, not a URL; the manager reserves a loopback port, runs <code>ssh -N -L 127.0.0.1:&lt;local&gt;:127.0.0.1:&lt;remotePort&gt;</code>, and derives <code>ws://127.0.0.1:&lt;port&gt;/v1/racp/ws</code> per launch, so a restart re-establishes the tunnel before connecting instead of dialling a stale port. The bootstrap&#39;s own forward is <em>adopted</em>, not closed and reopened, so pairing pays for exactly one tunnel. A forward counts as ready only once its local port accepts a connection, because a healthy <code>ssh -N -L</code> prints nothing.</p></li><li><p><strong>Version parity is checked before a forward exists.</strong> The desktop&#39;s version is what gets downloaded — there is no version negotiation — and <code>PI_HOST_READY.version</code> must equal it (<code>v</code> prefix tolerated either way). A mismatch raises <code>HOST_VERSION_MISMATCH</code> (D375) before any forward exists, and the desktop tears down the SSH processes it spawned on the way out instead of leaving a half-connected host behind.</p></li><li><p><strong>The registry keeps a descriptor, not a URL.</strong> <code>metadata.transport = &quot;ssh&quot;</code> plus <code>metadata.ssh</code> (host, optional port / user / identity file, <code>remotePort</code>, <code>version</code>). <code>remote-hosts.json</code> is user-editable, so the descriptor is re-validated on every read: a missing host or a <code>remotePort</code> outside 1..65535 degrades the record to &quot;not an SSH host&quot; instead of producing an <code>ssh</code> spawn with garbage arguments. Records written before this change carry no <code>transport</code> and read as <code>direct</code>.</p></li><li><p><strong>One IPC channel and one pairing exchange.</strong><code>pi-desktop/remoteHost/bootstrap</code> joins <code>list</code> / <code>pair</code> / <code>remove</code> and carries a host, never a credential. The <code>connection/pair</code> exchange was factored into <code>exchangePairingToken</code>, used by both the pasted-URL path and the bootstrap path, and it closes its throwaway connection on every path. <code>bootstrapHost</code> adopts the forward before persisting the record, because the device token only works over that forward.</p></li></ol><h2 id="invariants" tabindex="-1">Invariants <a class="header-anchor" href="#invariants" aria-label="Permalink to &quot;Invariants&quot;">​</a></h2><ul><li>No SSH secret is stored, prompted for, or transmitted by the app. The only credentials the bootstrap touches are the ones the system <code>ssh</code> client resolves itself.</li><li>No executable bytes travel over the SSH channel. The remote script downloads the bundle; the bytes the desktop sends are the script text on stdin.</li><li><code>pi-host</code> keeps binding loopback only. The desktop reaches it through a forward and never opens a listener of its own.</li><li>The renderer stays transport-agnostic. <code>RemoteHostSummary.transport</code> is optional and rendered as a label; no renderer call branches on it.</li><li>An empty registry stays a full no-op: nothing connects and no tunnel opens.</li><li>A failed bootstrap leaves nothing running: its transport is disposed unless the caller adopts the forward, and an SSH host whose connect fails has its tunnel closed rather than left idle.</li></ul><h2 id="out-of-scope" tabindex="-1">Out of scope <a class="header-anchor" href="#out-of-scope" aria-label="Permalink to &quot;Out of scope&quot;">​</a></h2><p>Still open for R2b and later:</p><ul><li><strong>Terminal work-panel client (Stage 5)</strong> and <strong>reverse tool relay (Stage 6)</strong> from ADR 0286. Both remain unbuilt.</li><li><strong>Provider configuration over the SSH channel.</strong> Spec §3.4 and §5.2 name it as part of the bootstrap step, but it needs host-side provider schema work and is not in this change, so a freshly bootstrapped host still fails <code>turn/start</code> closed with <code>MODEL_NOT_CONFIGURED</code> until a provider is configured on it.</li><li><strong>The resync watchdog</strong>: <code>resync.required</code> events are still dropped.</li><li><strong>Non-Linux remote targets, and Windows as a <em>remote</em> target.</strong> Only <code>linux-x64</code> is published, so both are refused before a download starts.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>An <code>ssh2</code>-style library instead of the system client.</strong> Rejected: it re-implements config, agent, and jump-host semantics the user already has, and asks them to re-enter credentials the app should never hold.</li><li><strong>Download the artifact on the desktop and upload it.</strong> Rejected by security §3.4: the desktop never uploads executable bytes.</li><li><strong>Let the script read the published <code>.sha256</code> itself.</strong> Rejected: the desktop should hold the trust anchor, and this keeps the script&#39;s only input a URL plus a digest.</li><li><strong>Store the forwarded URL in the registry.</strong> Simpler read path, but the local port is not stable across launches, so a restart would dial a port nothing listens on. Rejected in favour of the descriptor.</li><li><strong>Install the host into a user-level service manager.</strong> Rejected: the script installs under <code>$HOME</code> and restarts the process its own pidfile tracks, which keeps the bootstrap privilege-free.</li></ul><h2 id="testing" tabindex="-1">Testing <a class="header-anchor" href="#testing" aria-label="Permalink to &quot;Testing&quot;">​</a></h2><p>The release rules and the script generator are pure and covered by <code>node --test</code> fixtures; the generator&#39;s tests run <code>sh -n</code> over the produced text and execute it against a stubbed <code>PATH</code>. The orchestrator runs end to end against a fake transport, a fake checksum fetch, reserved ports, and a fake pairing exchange, so step order, the digest failure path, the version-mismatch path with no forward, and the adopt-don&#39;t-reopen rule are asserted without spawning a process.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li><code>remote-hosts.json</code> gains a second record shape and stays backward compatible: existing records have no <code>transport</code> and are read as <code>direct</code>.</li><li>A machine without outbound GitHub access still cannot be bootstrapped (D375).</li><li>The desktop now spawns <code>ssh</code> processes, which is new and visible to endpoint-security tooling; until now the remote stack only opened outbound WebSocket connections.</li><li>The 16 new <code>settings.remoteHosts</code> i18n keys must be translated in every shipped locale, because the catalog type is exhaustive.</li><li>No new runtime dependency: the bootstrap adds no SSH library, no HTTP client, and no archive tool to <code>apps/desktop</code>.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0292-ssh-remote-host-bootstrap.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0292SshRemoteHostBootstrap = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0292SshRemoteHostBootstrap as default
};
