import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0095: Sign in with a vendor account instead of pasting an API key","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0095-vendor-account-oauth-login.md","filePath":"adr/0095-vendor-account-oauth-login.md","lastUpdated":1790052354000}');
const _sfc_main = { name: "adr/0095-vendor-account-oauth-login.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0095-sign-in-with-a-vendor-account-instead-of-pasting-an-api-key" tabindex="-1">ADR 0095: Sign in with a vendor account instead of pasting an API key <a class="header-anchor" href="#adr-0095-sign-in-with-a-vendor-account-instead-of-pasting-an-api-key" aria-label="Permalink to &quot;ADR 0095: Sign in with a vendor account instead of pasting an API key&quot;">​</a></h1><ul><li>Status: Accepted for implementation</li><li>Date: 2026-08-18</li><li>Deciders: PI-Desktop core</li><li>Related: D237, D240, ADR 0098, D028, D031, ADR 0012, ADR 0020, ADR 0027</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>A provider row could only be authenticated one way: the user pastes an API key, host-core encrypts it under <code>secret:provider:&lt;id&gt;:api_key</code>, and Electron main reads the plaintext back on every launch so the sidecar can sign requests with a constant <code>{ auth: { apiKey } }</code>. Users who already pay for a vendor subscription — Claude Pro/Max, ChatGPT Plus/Pro, GitHub Copilot — had to buy separate API credit to use PI-Desktop at all.</p><p><code>@earendil-works/pi-ai</code> already ships everything the protocol side needs: seven OAuth flows, a <code>CredentialStore</code> contract, and locked token refresh. What it does not ship is the host half — its own <code>auth/types.d.ts</code> says &quot;The app persists a credential after login via <code>modify(...)</code>. Login/logout orchestration is app-owned.&quot; PI-Desktop had no such half.</p><p>Two properties made this more than an extra settings field. Vendor access tokens expire in about an hour, so a credential resolved once at launch goes stale mid-session. And a refresh token is materially more dangerous than an API key: it mints new credentials on demand, so the existing &quot;read the secret and hand it to the sidecar at launch&quot; pattern was not acceptable for it.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><p><strong>The credential is a second secret on the same provider row.</strong> OAuth credentials serialize to JSON and are stored through the existing encrypted secret store under <code>secret:provider:&lt;id&gt;:oauth</code>, beside — never instead of — <code>secret:provider:&lt;id&gt;:api_key</code>. <code>has_secret</code> widens to mean &quot;has either credential&quot;, so every readiness check in the renderer keeps working unchanged; a new <code>has_oauth</code> distinguishes the two for badges and for hiding the key input. <code>auth_kind</code> gains the value <code>oauth</code>; it was already a free string, so neither the host protocol version nor the storage schema changes.</p></li><li><p><strong>Login orchestration lives in Electron main, in <code>oauth.ts</code>.</strong> It implements <code>CredentialStore</code> over host-core&#39;s <code>secrets.*</code> RPC, serializing <code>modify</code> per provider so pi-ai&#39;s locked-refresh assumption holds, and bridges pi-ai&#39;s <code>AuthInteraction</code> to renderer events over five new invoke channels and one event channel under <code>pi-desktop/providers/oauth/*</code>. Browser-callback, device-code, select and paste-a-code steps are all the same event stream, so the renderer renders what the vendor asked for rather than a per-vendor script, and cancel aborts the local callback server or the polling loop.</p></li><li><p><strong>The vendor list is derived, not enumerated.</strong> Cards come from <code>models.getProviders().filter(p =&gt; p.auth.oauth)</code>, so all seven vendors are supported on day one and a vendor that gains or loses a flow in pi-ai needs no change here. Because <code>auth/oauth/load.js</code> loads flows through a dynamic import with a variable specifier — a path electron-vite cannot bundle — main calls <code>registerBunOAuthFlows()</code> once at startup to register them statically.</p></li><li><p><strong>The sidecar resolves auth per request and never sees a refresh token.</strong> An OAuth row&#39;s launch payload carries <code>apiKey: &quot;&quot;</code>. The runtime injects a <code>resolveAuth</code> callback that calls a new host-proxy method, <code>provider.resolveAuth</code>, which main answers itself and never forwards to host-core; main validates the <code>(sessionId, providerId)</code> pair against the binding table it rewrites on every launch and refuses anything else. The reply is a short-lived <code>ModelAuth</code> — <code>apiKey</code>, <code>headers</code>, <code>baseUrl</code> — which pi-ai passes straight through, so Copilot&#39;s per-account endpoint and Kimi&#39;s header-only auth need no special case. pi-ai calls <code>getAuth</code> on every stream and caches nothing, and refreshes only past expiry under the store lock, so this is both correct and cheap.</p></li><li><p><strong>A vendor row&#39;s identity stays stable across turns.</strong> <code>matches()</code> compares the provider row, whose <code>apiKey</code> is permanently <code>&quot;&quot;</code>; a fresh <code>resolveAuth</code> closure per launch is a function property and disappears through <code>JSON.stringify</code>. An OAuth session therefore reuses its warm runtime instead of rebuilding it once an hour, or once a turn.</p></li><li><p><strong>Model discovery and connection tests go through the account.</strong> For an OAuth row, <code>providers.listModels</code> reads the authenticated catalog (<code>models.getAvailable</code>, which applies the vendor&#39;s own <code>filterModels</code>, so Copilot shows what the subscription actually includes) instead of probing <code>/models</code> with a key it does not have, and the connection test proves the account by resolving auth. xAI is the exception: the resolved account token calls <code>GET /v1/models</code>, and that chat-model list replaces the pinned catalog when the request succeeds. Login stores a non-secret account label in the row config and picks the row&#39;s <code>apiStyle</code> from the selected model — a vendor may span wire APIs. The original one-row-per-vendor assumption is amended by ADR 0098: every login now creates an independent row and credential scope. Two styles are added for this: <code>openai_codex_responses</code> and <code>pi_messages</code>.</p></li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A subscriber signs in from Settings → Model configuration and picks models normally; no API credit is required for the seven supported vendors.</li><li>The privilege boundary tightens rather than loosens. The sidecar previously received a long-lived API key unconditionally; for a vendor row it now receives a revocable, roughly hour-long token for the one provider its session is bound to, and no refresh token at all.</li><li>Vendor logins depend on main being alive to answer <code>provider.resolveAuth</code>; a host or main restart mid-turn fails the request rather than signing it with a stale token, which is the intended failure direction.</li><li>API-key rows are untouched: same storage, same launch payload, same resolve path. The two credential kinds remain in the same provider storage domain; each OAuth account owns its own provider row and secret ref (ADR 0098).</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><ul><li><strong>Resolve the credential once at launch and put it in the payload.</strong> Rejected: a Codex token expires in about an hour, so long sessions would break mid-turn; refreshing would change the payload and make <code>matches()</code> miss every turn, rebuilding the runtime and discarding warm state; and it would put a token the user never typed into a process that runs model-directed code.</li><li><strong>Give the sidecar the credential store and let pi-ai refresh in-process.</strong> Rejected: that hands the refresh token — the durable secret — to the least trusted process, and contradicts the existing host-proxy allowlist rule that the sidecar must not be able to pull secrets or mutate configuration.</li><li><strong>Store OAuth credentials in a new dedicated table or file.</strong> Rejected: the encrypted secret store already provides the exact guarantees needed, and a second backend would need its own lifecycle, deletion path, and audit.</li><li><strong>Ship two or three vendors first.</strong> Rejected: the list is derived from pi-ai&#39;s catalog, so restricting it would have meant writing a hardcoded allowlist — more code for less coverage.</li></ul><h2 id="references" tabindex="-1">References <a class="header-anchor" href="#references" aria-label="Permalink to &quot;References&quot;">​</a></h2><ul><li><code>docs/spec/03-runtime/11-provider-model-system.md</code></li><li><code>docs/spec/03-runtime/12-provider-config-schema.md</code></li><li><code>docs/spec/03-runtime/14-secrets-storage.md</code></li><li><code>docs/spec/03-runtime/01-ipc-protocol.md</code></li><li><code>docs/spec/06-delivery/04-e2e-test-plan.md</code></li><li><code>apps/desktop/electron/main/oauth.ts</code>, <code>apps/desktop/electron/main/agent-sidecar.ts</code></li><li><code>packages/agent-runtime/src/provider-binding.ts</code></li><li><code>crates/host-core/src/secrets.rs</code>, <code>crates/host-core/src/providers.rs</code></li><li>Decision D237, D028, D031</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0095-vendor-account-oauth-login.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0095VendorAccountOauthLogin = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0095VendorAccountOauthLogin as default
};
