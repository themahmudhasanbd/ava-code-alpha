import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0231: Ideographic Comma Opens the Composer Slash Menu","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0231-ideographic-comma-opens-the-slash-menu.md","filePath":"adr/0231-ideographic-comma-opens-the-slash-menu.md","lastUpdated":1789148233000}');
const _sfc_main = { name: "adr/0231-ideographic-comma-opens-the-slash-menu.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0231-ideographic-comma-opens-the-composer-slash-menu" tabindex="-1">ADR 0231: Ideographic Comma Opens the Composer Slash Menu <a class="header-anchor" href="#adr-0231-ideographic-comma-opens-the-composer-slash-menu" aria-label="Permalink to &quot;ADR 0231: Ideographic Comma Opens the Composer Slash Menu&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-11</li><li>Deciders: PI-Desktop desktop UI maintainers</li><li>Amends: D123, D139, ADR 0024</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The composer <code>/</code> menu mirrors the pi CLI grammar: the trigger is the ASCII <code>/</code> as the very first character of the draft. A Chinese IME produces <code>、</code> (U+3001, ideographic comma) for that keystroke, so a user writing in Chinese has to switch to ASCII input before they can reach the command menu, then switch back. The menu already ignores in-flight IME composition (D139), so the missing piece is only the alias itself.</p><p>Every other trigger character in the grammar is deliberate: <code>@</code> opens the file menu, and a <code>/</code> anywhere but the first character is ordinary prose. The alias must not turn legitimate punctuation into commands.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>A <code>、</code> committed as the first character of an empty composer draft is rewritten to <code>/</code> before trigger detection runs. Afterwards the draft is an ordinary slash invocation: the same menu opens, the same filtering applies, and the same send path handles it.</li><li>Only that position is rewritten. The composer requires the draft to have been empty before the keystroke, so a <code>、</code> that appears later — including at the start of a draft that already holds text — stays ordinary punctuation.</li><li>The rewrite is a pure string function in the shared composer-trigger module, unit tested next to the trigger grammar it feeds. The <code>@</code> file menu is unaffected.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A Chinese IME user reaches <code>/new</code>, <code>/compact</code>, the mode aliases, template commands, plugin commands, and Skills without leaving the input method.</li><li>A message that genuinely starts with <code>、</code> in an empty composer is rewritten. The menu opens with <code>/</code> and the user can keep typing prose, which keeps the substitution visible instead of silently altering text mid-sentence.</li><li>No IPC, storage, or autocomplete-source change is required.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Accept <code>、</code> as an additional trigger character in <code>detectTrigger</code>:</strong> rejected because the draft would keep a character the send path and transcript chip would then have to understand, and the menu would open on a mark the pi CLI grammar does not define.</li><li><strong>Rewrite on every <code>、</code>, not just the first character:</strong> rejected because it would corrupt ordinary prose, where <code>、</code> is the standard list separator.</li><li><strong>A toolbar button for commands:</strong> rejected as a duplicate entry point to a menu that already exists (see D123).</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0231-ideographic-comma-opens-the-slash-menu.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0231IdeographicCommaOpensTheSlashMenu = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0231IdeographicCommaOpensTheSlashMenu as default
};
