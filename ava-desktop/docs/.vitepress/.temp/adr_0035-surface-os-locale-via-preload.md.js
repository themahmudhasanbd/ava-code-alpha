import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0035: Surface the OS locale through the preload bridge","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0035-surface-os-locale-via-preload.md","filePath":"adr/0035-surface-os-locale-via-preload.md","lastUpdated":1785419910000}');
const _sfc_main = { name: "adr/0035-surface-os-locale-via-preload.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0035-surface-the-os-locale-through-the-preload-bridge" tabindex="-1">ADR 0035: Surface the OS locale through the preload bridge <a class="header-anchor" href="#adr-0035-surface-the-os-locale-through-the-preload-bridge" aria-label="Permalink to &quot;ADR 0035: Surface the OS locale through the preload bridge&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-07-30</li><li>Related: <a href="./../spec/04-ux/06-settings-ia">04-ux/06-settings-ia</a> · <a href="./../spec/04-ux/02-i18n-english-first">04-ux/02-i18n-english-first</a> · <a href="./../spec/06-delivery/04-e2e-test-plan">04-e2e-test-plan</a> · E2E-091</li><li>Updates: the <code>window.piDesktop</code> preload contract in <a href="../..//apps/desktop/src/lib/api.ts">api.ts</a></li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The Settings → Basics → Language control offers an <strong>Auto</strong> option that should follow the user&#39;s OS display language. The original implementation resolved &quot;auto&quot; from the renderer&#39;s <code>navigator.language</code>.</p><p>In Electron, the renderer&#39;s <code>navigator.language</code> is the embedded browser locale and defaults to <code>en-US</code> regardless of the actual OS language. On a Chinese system the renderer still reports <code>en-US</code>, so &quot;Auto&quot; resolved to English even though the OS was Simplified Chinese. The same default also seeded the initial i18n language on first paint.</p><p>The reliable source is the main process: <code>app.getLocale()</code> reads the OS display language (macOS <code>AppleLanguages</code>, Windows user UI language, Linux <code>LANG</code>). The main process already owns OS-level facts; the renderer only needed a way to read them synchronously before first paint.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>Expose the authoritative OS locale from the preload bridge as a synchronous field <code>window.piDesktop.locale</code>. The main process resolves <code>app.getLocale()</code> while creating the window and passes it through <code>webPreferences.additionalArguments</code>; sandboxed preload code reads that argument rather than importing Electron&#39;s main-only <code>app</code> module (alongside the existing <code>platform</code> field).</li><li>Add optional <code>locale?: string</code> to the <code>window.piDesktop</code> type in <code>api.ts</code>; non-Electron contexts do not receive a window creation argument.</li><li>Resolve &quot;auto&quot; language through a new <code>resolveOsLocale()</code> helper in <code>lib/app-language.ts</code> that prefers <code>window.piDesktop.locale</code> and falls back to <code>navigator.language</code> / <code>userLanguage</code> for non-Electron contexts (e.g. tests).</li><li>Seed the initial i18n <code>lng</code> in <code>main.tsx</code> from <code>resolveOsLocale()</code> instead of <code>navigator.language</code>.</li></ol><p>This keeps OS-fact resolution in the main process and requires no new IPC channel (the value is available before first paint, like <code>platform</code>).</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>&quot;Auto&quot; language now matches the real OS display language (e.g. Simplified Chinese on a Chinese system), and the Auto card shows the detected language inline (&quot;当前：简体中文&quot;).</li><li>Initial i18n language on first paint is correct for the OS locale.</li><li>The <code>window.piDesktop</code> preload contract grows one read-only string field.</li><li>Non-Electron contexts (unit tests, potential web builds) fall back to <code>navigator.language</code> and remain functional.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><h3 id="keep-using-navigator-language" tabindex="-1">Keep using <code>navigator.language</code> <a class="header-anchor" href="#keep-using-navigator-language" aria-label="Permalink to &quot;Keep using \`navigator.language\`&quot;">​</a></h3><p>Rejected: it is the root cause of the misdetection and cannot see the OS language from the renderer in Electron.</p><h3 id="add-an-async-ipc-call-for-the-os-locale" tabindex="-1">Add an async IPC call for the OS locale <a class="header-anchor" href="#add-an-async-ipc-call-for-the-os-locale" aria-label="Permalink to &quot;Add an async IPC call for the OS locale&quot;">​</a></h3><p>Rejected: an async round-trip would delay first paint of the correct language and complicate the synchronous pre-paint setup that already uses <code>platform</code>. Surfacing the value synchronously from the bridge is simpler and sufficient.</p><h2 id="references" tabindex="-1">References <a class="header-anchor" href="#references" aria-label="Permalink to &quot;References&quot;">​</a></h2><ul><li><code>apps/desktop/electron/main/index.ts</code> (passes <code>app.getLocale()</code> through <code>additionalArguments</code>)</li><li><code>apps/desktop/electron/preload/index.ts</code> (reads the locale argument without importing <code>app</code>)</li><li><code>apps/desktop/src/lib/api.ts</code> (<code>window.piDesktop.locale</code> type)</li><li><code>apps/desktop/src/lib/app-language.ts</code> (<code>resolveOsLocale</code>, <code>resolveAppLanguage</code>)</li><li><code>apps/desktop/src/main.tsx</code> (initial <code>lng</code> from <code>resolveOsLocale</code>)</li><li><code>apps/desktop/src/pages/SettingsPage.tsx</code> (Auto card shows detected language)</li><li><code>docs/spec/06-delivery/04-e2e-test-plan.md</code> (E2E-091 added)</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0035-surface-os-locale-via-preload.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0035SurfaceOsLocaleViaPreload = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0035SurfaceOsLocaleViaPreload as default
};
