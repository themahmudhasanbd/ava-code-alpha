import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0235: Preserve Domain Facades and Enforce Architecture Budgets","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0235-domain-facades-and-architecture-budgets.md","filePath":"adr/0235-domain-facades-and-architecture-budgets.md","lastUpdated":1789207546000}');
const _sfc_main = { name: "adr/0235-domain-facades-and-architecture-budgets.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0235-preserve-domain-facades-and-enforce-architecture-budgets" tabindex="-1">ADR 0235: Preserve Domain Facades and Enforce Architecture Budgets <a class="header-anchor" href="#adr-0235-preserve-domain-facades-and-enforce-architecture-budgets" aria-label="Permalink to &quot;ADR 0235: Preserve Domain Facades and Enforce Architecture Budgets&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-12</li><li>Deciders: PI-Desktop maintainers</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The desktop application had several large modules that combined wiring with multiple domain responsibilities. Splitting those modules reduces change coupling, but replacing established entry points would break imports, source contracts, plugin integrations, or protocol assumptions. The repository also needs a repeatable way to prevent the same hotspots from growing again.</p><p>The refactor covers Electron main IPC and runtime wiring, renderer pages, host-core persistence and service domains, shared public types, and E2E test infrastructure. These boundaries must preserve existing IPC channels, plugin SDK behavior, host RPC data, and persisted formats.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>Keep stable facade modules at established public paths. Electron main <code>index.ts</code>, renderer page entry points, shared <code>types.ts</code>, and the Rust <code>plugins</code>, <code>db</code>, <code>providers</code>, and <code>plans</code> modules remain compatible facades over their domain implementations.</li><li>Put implementation responsibility in domain modules: Electron IPC and runtime modules, renderer <code>features/*</code> modules, host-core domain submodules, and <code>packages/shared/src/types/*</code> files. The facades may wire or re-export these modules but must not become a second implementation.</li><li>Keep source-contract tests at the owning module boundary and retain public re-export coverage for compatibility paths. Shared E2E boot, session, plan, plugin, host, fixture, wait, and assertion helpers are the common test surface for protocol and lifecycle suites.</li><li>Enforce source-size budgets with <code>node scripts/check-architecture.mjs</code>. The checker reports repository metrics, protects the main and store facade limits, rejects newly added oversized ordinary TypeScript files, and requires a documented reason for oversized Rust files. Exceptions are explicit and reviewed with the allowlist.</li><li>Run the focused Biome lint, architecture checker, Rust formatting, and workspace Clippy checks in CI alongside the existing build, typecheck, and test gates. Existing Clippy warnings remain visible while they are reduced in focused follow-up changes.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Existing import paths, IPC channels, plugin SDK contracts, host RPC data, and persistence formats remain stable while implementation ownership is easier to locate.</li><li>New domain work has a clear home and is measured against source-size limits.</li><li>A small set of legacy modules remains explicitly documented as technical debt instead of being expanded silently.</li><li>The architecture checker adds a repository-level maintenance gate and its metrics make future refactor progress reviewable.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Replace the public modules with new paths:</strong> rejected because it would make compatibility the responsibility of every caller and plugin.</li><li><strong>Apply a broad formatter or stylistic lint migration:</strong> rejected because it would mix historical cleanup with the architecture change and obscure behavioral review.</li><li><strong>Use an undocumented size exception:</strong> rejected because a ratchet without an explicit reason would allow new hotspots to accumulate.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0235-domain-facades-and-architecture-budgets.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0235DomainFacadesAndArchitectureBudgets = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0235DomainFacadesAndArchitectureBudgets as default
};
