import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"16. Tool Result Limits & Truncation","description":"","frontmatter":{},"headers":[],"relativePath":"spec/03-runtime/16-tool-result-limits.md","filePath":"spec/03-runtime/16-tool-result-limits.md","lastUpdated":1788674034000}');
const _sfc_main = { name: "spec/03-runtime/16-tool-result-limits.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="_16-tool-result-limits-truncation" tabindex="-1">16. Tool Result Limits &amp; Truncation <a class="header-anchor" href="#_16-tool-result-limits-truncation" aria-label="Permalink to &quot;16. Tool Result Limits &amp; Truncation&quot;">​</a></h1><h2 id="_1-goal" tabindex="-1">1. Goal <a class="header-anchor" href="#_1-goal" aria-label="Permalink to &quot;1. Goal&quot;">​</a></h2><p>Keep agent context healthy and UI responsive by bounding tool outputs without silent data corruption.</p><h2 id="_2-default-limits" tabindex="-1">2. Default limits <a class="header-anchor" href="#_2-default-limits" aria-label="Permalink to &quot;2. Default limits&quot;">​</a></h2><p>Budgets are per tool class, not one shared cap. A single 256KB cap governing everything meant no cap in practice: measured sessions averaged 154KB per <code>Read</code> and spent 56% of their whole context on read/search results, which forced compaction and made the agent re-search what it had already found.</p><p>Read/Glob/Grep get a tighter budget than shell because their results are re-fetchable on demand (narrow the pattern, advance the offset). 48KB was too tight: a default window of ordinary source already overflowed, so almost every Read reported <code>truncated</code> and the agent re-searched what it had.</p><p>Bash output passes two independent ceilings. The <strong>capture</strong> layer bounds what the host retains in memory while the process streams, and is what the spill file is written from. The <strong>result</strong> budget bounds what reaches the model. Capture is deliberately the looser of the two: if it matched the result budget, a spilled copy could never be fuller than the excerpt it exists to back.</p><table tabindex="0"><thead><tr><th>channel</th><th>limit</th><th>action when exceeded</th></tr></thead><tbody><tr><td>Read / Glob / Grep result (<code>BUDGET_SEARCH</code>)</td><td>128 KB, 4000 lines</td><td>bound the window + <code>notice</code> naming the next step</td></tr><tr><td>Bash stdout (<code>BUDGET_SHELL</code>)</td><td>96 KB, 4000 lines, head</td><td>truncate + marker + spill</td></tr><tr><td>Bash stderr (<code>BUDGET_SHELL_ERR</code>)</td><td>96 KB, 4000 lines, <strong>tail</strong></td><td>truncate + marker + spill</td></tr><tr><td>any single line (<code>MAX_LINE_CHARS</code>)</td><td>16,384 chars</td><td>clip, count it in <code>notice</code></td></tr><tr><td>Read window</td><td>2000 lines default (max 4000), <code>offset</code>/<code>limit</code>; <code>totalLines</code> always reported</td><td>paginate; <code>truncated</code> only when this window was cut</td></tr><tr><td>Grep matches (<code>headLimit</code>)</td><td>200 default</td><td>stop with <code>truncated: true</code></td></tr><tr><td>Glob entries (<code>limit</code>)</td><td>100 default, 1000 max</td><td>stop with <code>truncated: true</code></td></tr><tr><td>Bash capture retention (<code>CAPTURE_MAX_BYTES</code> / <code>CAPTURE_MAX_LINES</code>)</td><td>512 KB, 200000 lines</td><td>stop retaining; report omitted bytes and lines</td></tr><tr><td>spilled full output (<code>SPILL_MAX_BYTES</code>)</td><td>512 KB</td><td>stop retaining; marker still names the file</td></tr><tr><td>Bash output stream</td><td>per-stream sequence</td><td>preserve stdout/stderr separation</td></tr><tr><td>Bash timeout</td><td>60s default; 1–21,600s override</td><td>kill process tree + error</td></tr><tr><td><code>Edit.ops</code> payload</td><td>256 KB, 200 ops</td><td><code>INVALID_ARGUMENT</code>; further Edit caps in <a href="./18-line-anchored-edit-contract">18</a> §12</td></tr></tbody></table><p>A clipped line is not a displayed line. <code>Read</code> excludes every line it cut at <code>MAX_LINE_CHARS</code> from the provenance set the <code>Edit</code> contract validates against (<a href="./18-line-anchored-edit-contract">18-line-anchored-edit-contract</a> §4.3), so a minified or generated line must be narrowed into view before it can be edited. Clipping therefore bounds context <em>and</em> blocks blind edits on the part that was cut, instead of only the first.</p><p>Limits are host-enforced. Tool descriptions in <code>builtin_tool_defs()</code> carry the numbers and the scoping parameters verbatim: a tool that looks incapable of the scoped thing gets routed around through Bash, and hand-rolled shell pipelines are what exhausted context in the first place.</p><p>Read never refuses on file size. The former &gt;512KB rejection told the model to &quot;use Grep or Bash to sample it&quot;, which is exactly how an unpaginated read became a <code>sed</code>/<code>awk</code> pipeline whose output nothing bounded.</p><h2 id="_3-truncation-marker-format" tabindex="-1">3. Truncation marker format <a class="header-anchor" href="#_3-truncation-marker-format" aria-label="Permalink to &quot;3. Truncation marker format&quot;">​</a></h2><p>Implemented markers (host-core <code>truncate_to</code>), appended for a head cut and prepended for a tail cut:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[truncated: kept the first 4000 of 51234 lines; limit 4000 lines / 96KB. Full output saved to &lt;path&gt; — Grep it, or Read it with offset/limit.]</span></span>
<span class="line"><span>[truncated: kept the last 1200 of 51234 lines; limit 4000 lines / 96KB. Narrow the request to see more.]</span></span>
<span class="line"><span>[truncated: no complete line fits the 96KB limit; kept 98304 bytes of a single 4200000-byte line. Full output saved to &lt;path&gt; — Grep it, or Read it with offset/limit.]</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>A marker always states which end survived, how much was kept out of the total, the limit that applied, and where to get the rest. The spill sentence appears only when a full copy was actually written.</p><p>Read/Glob/Grep do not embed a marker in their payload: the window metadata (<code>offset</code>, <code>lineCount</code>, <code>totalLines</code>, <code>truncated</code>) plus a <code>notice</code> string carry the same information as sibling fields, which keeps the payload itself mechanically parseable. <code>Read</code> content is line-numbered and headed by <code>[path#TAG]</code> (ADR 0087); it is no longer byte-faithful, so a consumer copying it into <code>Write</code> must strip the header and the <code>N:</code> prefixes, which <code>Write</code> also does defensively.</p><p>Checkpoint-only aggregate truncation uses the distinct model-context marker in §4 so diagnostics can distinguish where information was shortened.</p><h2 id="_3a-spill-files" tabindex="-1">3a. Spill files <a class="header-anchor" href="#_3a-spill-files" aria-label="Permalink to &quot;3a. Spill files&quot;">​</a></h2><p>When Bash output exceeds its budget, the fuller copy (up to <code>SPILL_MAX_BYTES</code>) is written to <code>&lt;data_dir&gt;/scratch/&lt;session_id&gt;/tool-output/&lt;label&gt;-&lt;ms&gt;-&lt;seq&gt;.log</code> and named in the marker. That reuses the per-session scratch lifecycle (<code>scratch::remove_session_dir</code> / <code>sweep</code>), so spills die with their session and stale ones are swept at startup — no separate retention policy.</p><p>The directory is created on first spill, not on session start, so sessions that stayed under budget leave nothing behind. A failed spill costs the hint only, never the tool result.</p><p>Grep can read spill files, because an explicit <code>path</code> argument stops parent ignore files from applying — the same rule that lets <code>path</code> reach into <code>node_modules</code> or <code>dist</code>.</p><h2 id="_4-model-facing-vs-ui-facing" tabindex="-1">4. Model-facing vs UI-facing <a class="header-anchor" href="#_4-model-facing-vs-ui-facing" aria-label="Permalink to &quot;4. Model-facing vs UI-facing&quot;">​</a></h2><ul><li>model receives truncated payload with marker</li><li>Renderer receives ordered <code>stdout</code> and <code>stderr</code> chunks while Bash runs; the final model/UI result remains the bounded combined payload.</li><li>UI may offer “open full output in viewer” for Bash/Read later (post-MVP optional)</li><li>full raw output is not required to persist forever; session may store truncated form in MVP</li><li>the per-result host cap does not bound a parallel batch in aggregate, and it does not need to during context compaction: an active-turn checkpoint retains only the latest user message while an active turn continues, and a completed-turn checkpoint retains none, so no tool result crosses the boundary at all (D203/D275). Completed turns carry no user message past the boundary; tool output reaches the next context solely through the checkpoint summary.</li><li>the one active user message a checkpoint may truncate crosses the 20,000-token retention limit. It keeps a 75/25 head/tail share of its text with this marker in between, rather than being dropped:</li></ul><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[checkpoint truncated: this message crossed the retained context budget]</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><ul><li>checkpoint-only truncation never rewrites the original transcript message or its UI/diagnostic result; it changes only future reconstructed model context</li></ul><h2 id="_5-partial-result-flags" tabindex="-1">5. Partial result flags <a class="header-anchor" href="#_5-partial-result-flags" aria-label="Permalink to &quot;5. Partial result flags&quot;">​</a></h2><p>Every bounded tool reports <code>truncated: boolean</code>. For Read, that flag is true only when this window was cut short of what the caller asked for (the byte budget stopped the scan, or a line was clipped). A Read that returned the requested or default window of a longer file reports <code>truncated: false</code>; <code>totalLines</code>, <code>offset</code>, <code>lineCount</code>, and a next-offset <code>notice</code> describe the remainder. Grep and Glob set <code>truncated: true</code> when the match/entry cap hid remaining hits.</p><p>Read/Glob/Grep additionally report what was bounded and how to continue:</p><div class="language-ts vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">type</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> ReadResult</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  path</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">root</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;workspace&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;scratch&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;external&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  content</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">          // &quot;[path#TAG]&quot; header + &quot;N:&quot;-prefixed window</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  tag</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">              // 4 hex, whole-file; the Edit anchor</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  offset</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> number</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">lineCount</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> number</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  totalLines</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> number</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">       // always reported via fast pre-scan</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  fileBytes</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> number</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  truncated</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> boolean</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  notice</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">          // next offset, budget stop, clipped-line count</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">type</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> GrepResult</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  |</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">matches</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">path</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">line</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> number</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">text</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> }[]; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">tags</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> Record</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&gt;; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">count</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> number</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">files</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> number</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">truncated</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> boolean</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">notice</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  |</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">files</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">[]; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">count</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> number</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">truncated</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> boolean</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">notice</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> }        </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// outputMode: filesWithMatches</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  |</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">counts</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">path</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">count</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> number</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> }[]; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">count</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> number</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">truncated</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> boolean</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">notice</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> }  </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// outputMode: count</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">type</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> GlobResult</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">matches</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">[]; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">count</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> number</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">truncated</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> boolean</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">notice</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> }</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br></div></div><p><code>tags</code> is present only in <code>content</code> mode, because only that mode displays lines. The other two modes are searchable but not editable anchors.</p><p>For an approved external path, <code>path</code> is absolute; <code>Read</code> also reports <code>root: &quot;external&quot;</code>. <code>Glob</code> and <code>Grep</code> use absolute paths for their external matches. The sidecar emits <code>filesWithMatches</code>; host-core also normalizes the common <code>files_with_matches</code> and <code>files-with-matches</code> provider spellings.</p><p><code>notice</code> is model-facing prose, not a stable contract: it names the next offset, the budget that stopped the scan, or how many lines were clipped. It does not tell the model to Grep after a successful paged Read. <code>truncated</code> and the counts are the stable signals. The UI truncated chip follows <code>truncated</code>.</p><h2 id="_6-priority-rules" tabindex="-1">6. Priority rules <a class="header-anchor" href="#_6-priority-rules" aria-label="Permalink to &quot;6. Priority rules&quot;">​</a></h2><ol><li>never omit truncation marker when truncated</li><li>Bash stdout keeps its head; Bash stderr keeps its <strong>tail</strong>, because a failing command&#39;s actionable message is the last thing it printed and dropping it for 96KB of progress noise is what makes the model retry blindly</li><li>binary files: do not dump raw binary into model; return metadata error <code>TOOL_BINARY_CONTENT</code>. Detected by extension blacklist plus a sniff of the first 4KB (any NUL byte, or &gt;30% non-printable). Grep skips binary files silently rather than matching lossily-decoded bytes</li><li>a single line longer than the whole budget yields a char-boundary-safe prefix (or suffix, for a tail cut), never an empty payload</li><li>aggregate checkpoint truncation must preserve every provider-valid assistant tool-call/result pair and re-estimate the resulting tail before persistence</li><li>relevance ordering for Glob and Grep is file modification time, newest first, so a capped result keeps the half more likely to be asked about</li><li>Timeout and abort close both output streams only after the complete process tree has been shut down; no orphan process may continue writing output</li></ol><h2 id="_7-acceptance-criteria" tabindex="-1">7. Acceptance criteria <a class="header-anchor" href="#_7-acceptance-criteria" aria-label="Permalink to &quot;7. Acceptance criteria&quot;">​</a></h2><ul><li>[x] oversize Bash output truncates with marker and spills the fuller copy</li><li>[x] Bash stderr retains its final lines when truncated</li><li>[x] Grep stops at <code>headLimit</code> with <code>truncated: true</code></li><li>[x] Grep and Read clip lines at 16,384 chars, and a clipped line is excluded from the <code>Edit</code> provenance set</li><li>[x] Read paginates a multi-megabyte file instead of refusing it, reports the next offset, and does not set <code>truncated</code> when the requested window was filled</li><li>[x] Read refuses binary content with <code>TOOL_BINARY_CONTENT</code></li><li>[x] an explicit <code>path</code> reaches into an ignored tree (<code>node_modules</code>, spill dir)</li><li>[x] Glob and Grep order results by modification time, newest first</li><li>[x] truncated results still valid UTF-8 text</li><li>[x] the capture ceiling sits above the result budget, so a spilled copy can be fuller than the excerpt it backs, and reports the bytes and lines it omitted</li><li>[ ] stdout and stderr stream separately with stable per-tool sequence values</li><li>[ ] Bash uses the 60s default and rejects an override outside 1–21,600s</li><li>[ ] timeout/abort stops the complete process tree and emits no later chunks</li><li>[ ] an oversized parallel result batch compacts to a bounded marked tail, survives restart, and leaves the original transcript results unchanged</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("spec/03-runtime/16-tool-result-limits.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _16ToolResultLimits = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _16ToolResultLimits as default
};
