import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0024: Composer Slash Commands and @ File References","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0024-composer-commands-and-file-references.md","filePath":"adr/0024-composer-commands-and-file-references.md","lastUpdated":1787140670000}');
const _sfc_main = { name: "adr/0024-composer-commands-and-file-references.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0024-composer-slash-commands-and-file-references" tabindex="-1">ADR 0024: Composer Slash Commands and @ File References <a class="header-anchor" href="#adr-0024-composer-slash-commands-and-file-references" aria-label="Permalink to &quot;ADR 0024: Composer Slash Commands and @ File References&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-07-27</li><li>Deciders: PI-Desktop core</li><li>Related: D123, D124, D125, D197, D209, D250, ADR 0019 (work panel subsystems), ADR 0059 (clipboard file paste), ADR 0070 (compact reference display), ADR 0106 (core five builtin commands), D114 (scratch dir), D119 (transcript file store)</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The composer is a plain textarea today. The command palette (Cmd/Ctrl+Shift+P) holds app commands, but the chat input itself has no in-place command system and no way to reference workspace files, and spec 08 §11.7 explicitly scope-cut both. The embedded pi runtime (<code>@earendil-works/pi-agent-core</code> 0.82) natively defines both concepts for its CLI:</p><ul><li><strong>Prompt templates</strong>: markdown files in <code>&lt;workspace&gt;/.pi/prompts/</code> and <code>~/.pi/agent/prompts/</code> with <code>description</code> / <code>argument-hint</code> frontmatter. <code>/name args</code> is expanded client-side (<code>parseCommandArgs</code> + <code>substituteArgs</code>: <code>$1..$n</code>, <code>$@</code>, <code>$ARGUMENTS</code>, <code>\${@:N:L}</code>) and sent as an ordinary user message — the model never sees the slash form.</li><li><strong><code>@path</code> references</strong>: literal text in the prompt; the model follows up with its Read tool. No inlining, no attachment conversion.</li><li>pi&#39;s built-in slash commands (<code>/new</code>, <code>/model</code>, …) are client-side behaviors of its TUI, not runtime features.</li></ul><p>PI-Desktop uses the low-level <code>Agent</code> class (not <code>AgentHarness</code>), so none of this is active in the desktop today, but the loader/expansion helpers are exported by the installed package and directly reusable.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><strong>Slash commands come from three sources</strong>, merged into one composer menu: pi prompt templates (project dir overrides user-global on name conflict), the builtin command-palette registry (slash aliases defined in <code>electron/main/builtin-commands.ts</code>, executed by the existing renderer-side switch), and plugin palette commands (executed via <code>commandPalette/execute</code>). The builtin registry is intentionally limited to the five core entries defined by ADR 0106. Unknown <code>/foo</code> is sent as literal text (pi CLI parity).</li><li><strong>Template expansion happens in the Electron main <code>agent/prompt</code> handler, before persistence.</strong> The persisted user message stores <code>content = expanded text</code> plus a new optional <code>command</code> field holding the typed invocation (<code>/name args</code>). Agent reseed replays <code>content</code>, so the model context stays identical across restarts; the transcript renders the <code>command</code> field as a compact chip. Extra message fields are already tolerated by host storage (revision* precedent).</li><li><strong><code>@path</code> stays a plain-text light reference</strong> exactly like pi CLI at prompt dispatch. The composer provides fuzzy autocomplete; directory acceptance inserts <code>@dir/</code> so completion can continue, while completed files become renderer-owned compact references under ADR 0070. Before dispatch those references serialize to <code>@relative/path </code> (quoted <code>@&quot;a b.txt&quot;</code> when the path contains spaces). Plan and Agent carry Read/Glob/Grep, so references work in both. No content inlining or binary content enters the prompt. OS clipboard file/image paste is materialized as a session-scratch file reference by ADR 0059; it does not use pi-ai <code>ImageContent</code> or change the text-only prompt contract.</li><li><strong>Workspace file index is served by Electron main</strong>, not agent tools — same rationale as ADR 0019: user-initiated browsing must not spam permission prompts or the audit trail. New read-only channel <code>pi-desktop/fs/index</code> returns up to 8000 workspace-rooted entries (<code>git ls-files -co --exclude-standard</code> fast path, ignore-set walk fallback, short TTL cache); <code>pi-desktop/composer/commands</code> returns the merged command list. Both fail soft without a workspace. These Electron-only channels do not change the host RPC protocol version.</li></ol><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Expand templates in the agent sidecar</strong>: keeps pi imports in one place, but main persists the user message first, so the sidecar cannot influence what reseed replays without a second write path. Rejected.</li><li><strong>Migrate the runtime to <code>AgentHarness</code></strong> for <code>promptFromTemplate</code>: replaces the whole prompt/loop integration for one feature. Rejected for now; the standalone helpers give identical semantics.</li><li><strong>Inline referenced file content into the prompt</strong> (pi CLI argument-mode behavior): inflates context, needs truncation rules and an attachment channel for binaries. Rejected — the Read-tool light reference is pi&#39;s interactive semantic and costs nothing.</li><li><strong>Reuse <code>fs/list</code> recursively from the renderer</strong>: one IPC round-trip per directory level makes fuzzy search sluggish and chatty. Rejected.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>The composer gains a keyboard-first autocomplete surface (D125 defines the interaction/IME contract — the first explicit IME spec in the project).</li><li><code>.pi/prompts</code> templates become shared assets between pi CLI and PI-Desktop.</li><li>The transcript user-message schema gains an optional <code>command</code> field; renderers that ignore it keep working.</li><li>Inline binary attachments and preview tiles remain deferred. Compact textual reference chips are renderer-only draft state under ADR 0070; clipboard files/images still use session-scratch <code>@</code> references as defined by ADR 0059.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0024-composer-commands-and-file-references.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0024ComposerCommandsAndFileReferences = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0024ComposerCommandsAndFileReferences as default
};
