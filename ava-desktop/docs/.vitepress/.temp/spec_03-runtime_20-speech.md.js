import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"20. Host speech","description":"","frontmatter":{},"headers":[],"relativePath":"spec/03-runtime/20-speech.md","filePath":"spec/03-runtime/20-speech.md","lastUpdated":1789760880000}');
const _sfc_main = { name: "spec/03-runtime/20-speech.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="_20-host-speech" tabindex="-1">20. Host speech <a class="header-anchor" href="#_20-host-speech" aria-label="Permalink to &quot;20. Host speech&quot;">​</a></h1><blockquote><p>Source of truth: <code>packages/shared/src/types/speech.ts</code>, <code>packages/agent-runtime/src/speech/</code>, <code>apps/desktop/electron/main/services/speech-service.ts</code>. ADR: <a href="./../../adr/0281-host-speech-capability">0281-host-speech-capability</a>.</p></blockquote><h2 id="_1-capability" tabindex="-1">1. Capability <a class="header-anchor" href="#_1-capability" aria-label="Permalink to &quot;1. Capability&quot;">​</a></h2><p>Independent of chat streams and the model picker:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>transcribe({ sessionId, path, mimeType?, language? }) → { text }</span></span>
<span class="line"><span>synthesize({ sessionId, text, voice?, format? }) → { path, mimeType, dataUrl? }</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>Input is a session-scratch file path. Output audio is written to that session&#39;s scratch. The renderer never receives a provider secret. TTS <code>dataUrl</code> is omitted when the file is larger than 8 MiB. Input is capped at 25 MiB (<code>SPEECH_INPUT_TOO_LARGE</code>).</p><h2 id="_2-bindings" tabindex="-1">2. Bindings <a class="header-anchor" href="#_2-bindings" aria-label="Permalink to &quot;2. Bindings&quot;">​</a></h2><p><code>AppSettings.speech</code> is optional and does not bump the storage schema:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>{</span></span>
<span class="line"><span>  transcribe?: { providerId, modelId, protocol, voice?, format?, path?, extra? }</span></span>
<span class="line"><span>  synthesize?: { providerId, modelId, protocol, voice?, format?, path?, extra? }</span></span>
<span class="line"><span>}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>Protocol ids match <code>^[a-z][a-z0-9._-]{0,63}$</code>. Absent/empty speech is a valid unconfigured state. No app surface reads it: callers are plugins and IPC consumers (ADR 0291).</p><h2 id="_3-built-in-protocols" tabindex="-1">3. Built-in protocols <a class="header-anchor" href="#_3-built-in-protocols" aria-label="Permalink to &quot;3. Built-in protocols&quot;">​</a></h2><table tabindex="0"><thead><tr><th>id</th><th>transcribe</th><th>synthesize</th></tr></thead><tbody><tr><td><code>openai_audio</code></td><td><code>POST {baseUrl}/audio/transcriptions</code> multipart</td><td><code>POST {baseUrl}/audio/speech</code> JSON</td></tr><tr><td><code>openai_chat_audio</code></td><td>unsupported</td><td><code>POST {baseUrl}/chat/completions</code> with <code>audio:{format,voice}</code>; read <code>choices.0.message.audio.data</code></td></tr></tbody></table><p>Local OpenAI-Audio-compatible servers use an <code>openai_compatible</code> provider and <code>openai_audio</code>. Suggested models: OpenAI <code>whisper-1</code> / <code>tts-1</code>, Groq <code>whisper-large-v3</code>, Xiaomi <code>mimo-v2.5-tts</code>.</p><h2 id="_4-ipc" tabindex="-1">4. IPC <a class="header-anchor" href="#_4-ipc" aria-label="Permalink to &quot;4. IPC&quot;">​</a></h2><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>pi-desktop/speech/getStatus → SpeechStatus (no secrets)</span></span>
<span class="line"><span>pi-desktop/speech/transcribe</span></span>
<span class="line"><span>pi-desktop/speech/synthesize</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>Errors: <code>SPEECH_NOT_CONFIGURED</code>, <code>SPEECH_PROTOCOL_UNSUPPORTED</code>, <code>SPEECH_INPUT_TOO_LARGE</code>. Network/auth reuse <code>NETWORK_ERROR</code> / <code>PROVIDER_UNAUTHORIZED</code>. A missing or disabled provider returns <code>NOT_FOUND</code>.</p><h2 id="_5-plugin-adapters" tabindex="-1">5. Plugin adapters <a class="header-anchor" href="#_5-plugin-adapters" aria-label="Permalink to &quot;5. Plugin adapters&quot;">​</a></h2><p><code>pi.speech.registerAdapter({ protocol, label, roles, handle })</code> requires <code>speech.adapter.register</code> (high risk). The handle stays in the plugin process. The host stores metadata only and calls <code>speech.handle</code>. A handle may return <code>{ kind: &quot;text&quot; }</code>, <code>{ kind: &quot;audio&quot;, mimeType, data }</code>, or <code>{ kind: &quot;http&quot;, call }</code> for the host to execute with the bound key. HTTP URLs must stay on the provider origin. Built-in protocol ids are reserved. Unload unregisters.</p><h2 id="_6-product" tabindex="-1">6. Product <a class="header-anchor" href="#_6-product" aria-label="Permalink to &quot;6. Product&quot;">​</a></h2><p>Settings exposes <strong>no</strong> speech surface (ADR 0291). A binding is written by a caller through the host settings API; plugins and the <code>speech/*</code> IPC are its only consumers, and the renderer has no transcription or speech control. Whisper / TTS models must not appear in the chat model picker.</p><p>v1 does not implement microphone capture, Realtime, or agent tools.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("spec/03-runtime/20-speech.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _20Speech = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _20Speech as default
};
