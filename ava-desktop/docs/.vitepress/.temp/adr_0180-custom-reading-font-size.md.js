import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0180: Custom global UI type scale","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0180-custom-reading-font-size.md","filePath":"adr/0180-custom-reading-font-size.md","lastUpdated":1788800350000}');
const _sfc_main = { name: "adr/0180-custom-reading-font-size.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0180-custom-global-ui-type-scale" tabindex="-1">ADR 0180: Custom global UI type scale <a class="header-anchor" href="#adr-0180-custom-global-ui-type-scale" aria-label="Permalink to &quot;ADR 0180: Custom global UI type scale&quot;">​</a></h1><ul><li>Status: Accepted (amended)</li><li>Date: 2026-09-08</li><li>Baseline: <code>0.4.16</code></li><li>Protocol: unchanged (renderer-owned <code>AppSettings</code> JSON field)</li><li>Storage schema: unchanged (optional <code>AppSettings.fontScale</code>)</li><li>Related: D343, ADR 0083, <code>04-ux/06-settings-ia.md</code>, <code>04-ux/07-ui-design-system.md</code></li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The <code>--text-*</code> ramp is a frozen set of product sizes (<code>--text-base</code> is 14px, with denser chrome and larger headings). The application menu exposes Zoom In / Zoom Out / Reset Zoom, but those scale the entire window, including layout and controls. Users who want denser or larger type have no remembered preference that keeps the relative steps between body, chrome, and headings.</p><p>An earlier draft of this decision stored a reading-only px value and remapped only the transcript and composer. That left sidebar and settings at the product size, required the user to pick a px number, and did not scale surfaces that already use different steps of the same ramp.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><p><strong>Settings → General → Appearance</strong> gains a <strong>Font size</strong> row under Font. Four Starbucks-style cup presets (Tall 85% / Grande 100% / Venti 115% / Trenta 125%; zh-CN: 中杯 / 大杯 / 超大杯 / 超超大杯) sit above a percentage slider. Valid values are 80%–150% in 2.5% steps. Absent means 100%. The UI never asks for a px value.</p></li><li><p><strong>Persistence</strong> is optional <code>AppSettings.fontScale</code> (<code>1</code> = product ramp). Invalid writes are rejected at the renderer edge; reads clamp through <code>normalizeFontScale</code>. An unreleased leftover <code>fontSize</code> px field, if present without <code>fontScale</code>, migrates as <code>px / 14</code>. No host protocol or storage schema version bump.</p></li><li><p><strong>Application</strong> is global. The renderer sets <code>--font-scale</code> on <code>document.documentElement</code>. Every <code>--text-*</code> token and <code>--leading-row</code> is <code>calc(&lt;product px&gt; * var(--font-scale))</code>, so body, chrome, headings, code, sidebar, and settings stay in proportion without a reload. Shared Lucide wrappers size glyphs with <code>calc(&lt;px&gt; * var(--font-scale))</code> so toolbar, sidebar, composer, and settings icons track the same multiplier.</p></li><li><p><strong>Zoom stays independent.</strong> Menu and shortcut Zoom In / Zoom Out / Reset Zoom continue to scale the whole window. Type scale and zoom compose.</p></li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>One control scales every <code>--text-*</code> consumer; places that are already smaller or larger than body stay relatively smaller or larger.</li><li>Users do not pick a px number that only matches <code>--text-base</code>.</li><li>Large scales also enlarge compact chrome, including Lucide glyphs. Rows whose height is a fixed px value (not <code>--leading-row</code>) may still feel tighter; the 150% cap bounds that.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><ul><li><strong>Reading-only remap of <code>.thread-wrap</code> / <code>.composer-dock</code>:</strong> keeps chrome compact, but users then have two type sizes in one window and still need a px number for the body step.</li><li><strong>Zoom In/Out only:</strong> already exists; it scales chrome together with text and is not a type preference.</li><li><strong>Change <code>html { font-size }</code> and switch the ramp to rem:</strong> larger refactor; the product ramp is px tokens and the token guard forbids raw rem in component CSS.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0180-custom-reading-font-size.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0180CustomReadingFontSize = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0180CustomReadingFontSize as default
};
