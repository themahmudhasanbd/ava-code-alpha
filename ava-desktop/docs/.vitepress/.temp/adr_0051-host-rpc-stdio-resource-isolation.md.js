import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0051: Isolate host RPC stdio from the Tokio blocking pool","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0051-host-rpc-stdio-resource-isolation.md","filePath":"adr/0051-host-rpc-stdio-resource-isolation.md","lastUpdated":1785841331000}');
const _sfc_main = { name: "adr/0051-host-rpc-stdio-resource-isolation.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0051-isolate-host-rpc-stdio-from-the-tokio-blocking-pool" tabindex="-1">ADR 0051: Isolate host RPC stdio from the Tokio blocking pool <a class="header-anchor" href="#adr-0051-isolate-host-rpc-stdio-from-the-tokio-blocking-pool" aria-label="Permalink to &quot;ADR 0051: Isolate host RPC stdio from the Tokio blocking pool&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-08-04</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The host already bounds RPC tasks, tool classes, shell processes, and queued work. A parallel Bash burst could nevertheless end the host with <code>Resource temporarily unavailable (os error 35)</code>. Electron correctly mapped that child exit to <code>HOST_UNAVAILABLE</code>, but the mapping hid the process-level failure from the session.</p><p>The remaining control-path risk was host-core&#39;s use of <code>tokio::io::stdin()</code> and <code>tokio::io::stdout()</code>. Tokio implements these adapters through its blocking pool. When the OS refuses another worker thread, the non-mandatory blocking spawn path panics instead of returning an ordinary I/O error. The host then exits while tool requests are in flight. The Unix login shell PATH probe had a second version of the same problem because an ordinary <code>std::thread::spawn</code> can panic when the OS cannot create a thread.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>Host-core&#39;s NDJSON stdin reader runs on one named thread created with <code>std::thread::Builder</code>. Its stdout writer runs on a second named thread. The async RPC dispatcher communicates with those threads through channels; request and tool tasks never call Tokio stdio adapters.</li><li>The control threads retry <code>EINTR</code> and transient <code>EAGAIN</code>/<code>EWOULDBLOCK</code> (<code>errno</code> 11 or 35) with a short bounded delay. The reader preserves partial input until a complete line arrives, and the writer tracks partial writes so a retry cannot duplicate bytes.</li><li>Failure to create a control thread is returned as a host startup error. A closed or unrecoverable pipe ends the normal host lifecycle and remains visible to Electron&#39;s generation-aware supervision; it is never converted into an unhandled Rust thread-spawn panic.</li><li>The login-shell PATH probe also uses <code>thread::Builder</code>; if that optional helper cannot start, Bash falls back to the inherited host PATH. Existing RPC and tool admission limits are unchanged.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Temporary OS thread pressure no longer reaches Tokio&#39;s panic-on-no-worker stdio path, removing the observed host exit cause behind <code>HOST_UNAVAILABLE</code>.</li><li>The host has two long-lived control threads instead of creating a blocking pool worker per stdio operation.</li><li>NDJSON framing, response ordering, request concurrency, and overload codes remain unchanged.</li><li>A genuinely unavailable stdin/stdout pipe still ends the host and is handled by the existing Electron restart/fatal-degradation policy.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><h3 id="increase-the-tokio-blocking-pool-limit" tabindex="-1">Increase the Tokio blocking-pool limit <a class="header-anchor" href="#increase-the-tokio-blocking-pool-limit" aria-label="Permalink to &quot;Increase the Tokio blocking-pool limit&quot;">​</a></h3><p>Rejected. It raises the number of threads competing for the same exhausted OS resource and leaves the panic-on-thread-creation path intact.</p><h3 id="keep-tokio-stdio-and-catch-the-panic" tabindex="-1">Keep Tokio stdio and catch the panic <a class="header-anchor" href="#keep-tokio-stdio-and-catch-the-panic" aria-label="Permalink to &quot;Keep Tokio stdio and catch the panic&quot;">​</a></h3><p>Rejected. Catching a panic around the async runtime would be brittle and would not make partial NDJSON writes or shutdown ordering explicit.</p><h3 id="remove-concurrency-limits-instead" tabindex="-1">Remove concurrency limits instead <a class="header-anchor" href="#remove-concurrency-limits-instead" aria-label="Permalink to &quot;Remove concurrency limits instead&quot;">​</a></h3><p>Rejected. Admission limits remain necessary for tools and subprocesses; they do not solve the independent control-pipe dependency on dynamic blocking-pool workers.</p><h2 id="references" tabindex="-1">References <a class="header-anchor" href="#references" aria-label="Permalink to &quot;References&quot;">​</a></h2><ul><li><code>crates/host-core/src/rpc/mod.rs</code></li><li><code>crates/host-core/src/tools/shell.rs</code></li><li><code>docs/spec/03-runtime/05-host-core-rust.md</code></li><li><code>docs/spec/03-runtime/06-host-rpc-protocol.md</code></li><li><code>docs/spec/03-runtime/07-process-model.md</code></li><li><code>docs/spec/06-delivery/04-e2e-test-plan.md</code></li><li>Decision D187</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0051-host-rpc-stdio-resource-isolation.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0051HostRpcStdioResourceIsolation = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0051HostRpcStdioResourceIsolation as default
};
