import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0283: Remote MCP Server OAuth 2.1 Authentication","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0283-remote-mcp-oauth.md","filePath":"adr/0283-remote-mcp-oauth.md","lastUpdated":1789722357000}');
const _sfc_main = { name: "adr/0283-remote-mcp-oauth.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0283-remote-mcp-server-oauth-2-1-authentication" tabindex="-1">ADR 0283: Remote MCP Server OAuth 2.1 Authentication <a class="header-anchor" href="#adr-0283-remote-mcp-server-oauth-2-1-authentication" aria-label="Permalink to &quot;ADR 0283: Remote MCP Server OAuth 2.1 Authentication&quot;">​</a></h1><ul><li><strong>Status</strong>: Accepted</li><li><strong>Date</strong>: 2026-09-18</li><li><strong>Related</strong>: <a href="./0038-plugin-mcp-bridge">ADR 0038</a> · <a href="./0098-multiple-vendor-oauth-accounts">ADR 0098</a> · <a href="./0142-allow-non-loopback-http-mcp">ADR 0142</a> · <a href="./../spec/03-runtime/01-ipc-protocol">03-runtime/01-ipc-protocol</a> · <a href="./../spec/03-runtime/06-host-rpc-protocol">03-runtime/06-host-rpc-protocol</a></li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Remote HTTP MCP servers (such as Notion, Linear, or custom enterprise servers) often protect endpoints with OAuth 2.1 authorization rather than static tokens. Model Context Protocol specifies authorization discovery via RFC 9728 (OAuth Protected Resource Metadata) and RFC 8414 (Authorization Server Metadata), dynamic registration via RFC 7591, and resource indicators via RFC 8707.</p><p>Previous MCP implementations in PI-Desktop supported only static HTTP headers. Users had to manually obtain Bearer tokens or were unable to connect to OAuth-protected servers.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><strong>Authentication Architecture</strong>: <ul><li>OAuth login runs in the Electron main process via <code>McpOAuthManager</code>, mirroring the <code>VendorOAuth</code> non-blocking pattern (ADR 0098): <code>mcp/oauth/start</code> returns <code>{ ok: true, loginId }</code> immediately and streams progress, authorization URL, completion, or failure events via <code>pi-desktop/mcp/oauth/event</code>.</li><li>IPC calls are never blocked during the user&#39;s browser interaction. A user or UI cancellation triggers <code>pi-desktop/mcp/oauth/cancel</code>, which closes the loopback listener and aborts the login.</li></ul></li><li><strong>Loopback &amp; Security Boundaries</strong>: <ul><li>The loopback callback server binds strictly to IPv4 loopback <code>127.0.0.1</code> on an ephemeral port (<code>port 0</code>), and the redirect URI is formatted as <code>http://127.0.0.1:&lt;port&gt;/callback</code> per RFC 8252 (preventing IPv6 loopback resolution mismatches).</li><li>Inbound query parameters (<code>error</code>, <code>error_description</code>, <code>code</code>, <code>state</code>) rendered on the loopback HTML completion page are strictly HTML-entity escaped to eliminate reflected XSS.</li><li>Per ADR 0142, discovery and token requests use <code>redirect: &quot;manual&quot;</code> to prevent silent cross-host redirect attacks.</li></ul></li><li><strong>MCP Protocol Compliance</strong>: <ul><li>RFC 8707 <code>resource</code> indicators are passed on both the authorization endpoint and token exchange/refresh requests, pointing to the protected resource URI discovered via RFC 9728.</li><li>Dynamic Client Registration (RFC 7591) credentials (<code>clientId</code>, <code>clientSecret</code>) are cached and reused for subsequent logins against the same registration endpoint.</li><li>Token lifetimes (<code>expires_in</code>) are parsed as numbers or strings (e.g. <code>&quot;3600&quot;</code>), computing an absolute expiry time.</li></ul></li><li><strong>Token Storage &amp; Isolation</strong>: <ul><li>OAuth tokens never reach the renderer. They are stored in host-core encrypted secrets under <code>secret:mcp:&lt;serverId&gt;:oauth</code>.</li><li>Token refresh operations are serialized per server to prevent race conditions with rotating refresh tokens.</li><li>Moving or renaming an MCP server (<code>mcp.transfer</code>) migrates the corresponding <code>secret:mcp:&lt;serverId&gt;:oauth</code> to the new ID.</li></ul></li><li><strong>Runtime Integration</strong>: <ul><li><code>UserMcpRuntime</code> injects the valid OAuth access token as <code>Authorization: Bearer &lt;token&gt;</code> into live HTTP MCP connections.</li><li>Token refreshes or re-authorizations invalidate stale connection entries so live clients immediately adopt updated credentials.</li><li>If a tool invocation returns a 401 Unauthorized status, the server is marked with <code>authRequired: true</code> and <code>state: &quot;failed&quot;</code>.</li></ul></li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Remote HTTP MCP servers requiring OAuth 2.1 PKCE can be authorized securely from the Settings UI.</li><li>Long-running browser interactions do not block IPC channels or leave orphan HTTP listeners upon window reload or quit.</li><li>Token material is kept out of the renderer process and ordinary logs.</li></ul><h2 id="amendment-2026-09-18-—-landing-hardening" tabindex="-1">Amendment (2026-09-18) — landing hardening <a class="header-anchor" href="#amendment-2026-09-18-—-landing-hardening" aria-label="Permalink to &quot;Amendment (2026-09-18) — landing hardening&quot;">​</a></h2><ul><li>Authorization-server endpoints (<code>authorization_endpoint</code>, <code>token_endpoint</code>, <code>registration_endpoint</code>, discovered <code>authorization_servers</code>, and <code>resource_metadata</code>) must be HTTPS. Loopback <code>http://127.0.0.1</code> / <code>localhost</code> / <code>::1</code> remains allowed so local mock and LAN-loopback AS still work. The MCP resource URL itself may still be <code>http://</code> (ADR 0142).</li><li>Dynamic client registration prefers RFC 8252 portless <code>http://127.0.0.1/callback</code> plus the current exact URI. Stored clients are reused only when that portless URI is on file, or the exact current redirect matches. Otherwise a new client is registered. Login also tries to rebind the previously used loopback port so strict AS that require an exact URI keep working.</li><li>Loopback <code>/callback</code> validates <code>state</code> before looking at <code>error</code> or <code>code</code>. A mismatched <code>state</code> is a stray request and does not abort the login. A matching callback is consumed once (replay returns 409).</li><li><code>invalid_grant</code> on refresh deletes the stored secret; transient 5xx keeps the existing access token.</li><li><code>mcp/oauth/start</code> passes the listed <code>McpServerRecord</code> through to <code>onAuthorized</code> so a project-level server that is not in the current workspace runtime can still handshake after login.</li><li>Scope selection remains an MVP heuristic (<code>default</code> if advertised, else <code>scopes_supported[0]</code>). There is no per-server scope picker, device-code flow, or DPoP.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0283-remote-mcp-oauth.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0283RemoteMcpOauth = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0283RemoteMcpOauth as default
};
