import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0245: Harden the MCP market public-network boundary","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0245-mcp-market-public-network-boundary.md","filePath":"adr/0245-mcp-market-public-network-boundary.md","lastUpdated":1789974692000}');
const _sfc_main = { name: "adr/0245-mcp-market-public-network-boundary.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0245-harden-the-mcp-market-public-network-boundary" tabindex="-1">ADR 0245: Harden the MCP market public-network boundary <a class="header-anchor" href="#adr-0245-harden-the-mcp-market-public-network-boundary" aria-label="Permalink to &quot;ADR 0245: Harden the MCP market public-network boundary&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-14</li><li>Related: ADR 0142, ADR 0177, PR #285</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The MCP market fetches user-configurable Registry and catalog sources from Electron Main. A syntactic HTTPS check and a separate DNS lookup are not enough to enforce a public-network boundary: a DNS-rebinding source can resolve to a public address during validation and to a private address when the HTTP client connects. Unbounded catalog responses can also exhaust the main process.</p><p>The official Registry also publishes package versions and launcher arguments. Dropping those fields makes an install resolve mutable latest content rather than the version the Registry described.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>Shared renderer and Main URL validation accepts credentials-free HTTPS only. Loopback, unspecified, private, CGNAT, link-local, multicast, reserved, documentation, benchmark, ULA, site-local, IPv4-mapped, and IPv4-compatible address ranges are rejected, including URL normalization and trailing-dot forms.</li><li>Main asks the Electron session carrying each request for its proxy route immediately before connecting. On a <code>direct</code> or <code>unknown</code> route it resolves every hostname and pins the selected public address to the actual HTTPS socket, retaining the original host as TLS SNI and HTTP Host. On a fully <code>proxied</code> route it uses that session&#39;s <code>net.fetch</code> so system/PAC and custom proxies can resolve fake-IP names; the local resolver&#39;s <code>benchmark</code> class is tolerated there under ADR 0272, while real private targets remain rejected.</li><li>Redirects are manual, HTTPS-only, and revalidated at every hop, with a maximum of five redirects.</li><li>Market source responses are bounded to 4 MiB, DNS and HTTP requests share an eight-second deadline, at most 16 sources are processed per call, and the in-memory catalog/search caches are bounded.</li><li>Registry npm packages are mapped as <code>identifier@version</code> and PyPI packages as <code>identifier==version</code> when a version is present. Runtime and package arguments are preserved in order. Only <code>streamable-http</code> remotes with public HTTPS endpoints are installable; remote header placeholders become explicit install-form values.</li><li>Market HTTP catalog entries are restricted to public HTTPS endpoints. Manual user-owned MCP configuration retains the existing local/LAN endpoint policy. MCP HTTP redirects never forward authorization, cookie, proxy-authorization, or API-key headers to another origin, and response reading remains bounded by the transport deadline and body limit.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Public market data cannot use DNS rebinding to turn the Main fetcher into a private-network request, subject to the operating system and proxy&#39;s own network behavior.</li><li>Slow, oversized, malformed, or excessively configured sources fail as one source and do not grow the Main process without bound.</li><li>Installs are reproducible with respect to the Registry package version and preserve the launch semantics that the publisher supplied.</li><li>A Registry/catalog entry cannot be used as a market shortcut for a private MCP endpoint; users can still add such endpoints through the explicit MCP editor permitted by ADR 0142.</li><li>Direct and unknown routes keep pinned Node HTTPS sockets and the strict public-address rule. Fully proxied routes use the Electron session transport; an explicit <code>allowFakeIp</code> setting additionally permits only benchmark placeholders for transparent router/TUN deployments. No credentials are sent to the market source implicitly.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Preflight DNS followed by ordinary fetch:</strong> rejected because the two resolutions leave a DNS-rebinding race.</li><li><strong>Allowlist only the official Registry hostname:</strong> rejected because the product supports user-hosted catalog and Registry sources.</li><li><strong>Install the Registry&#39;s latest package:</strong> rejected because it ignores the version selected and published by the Registry.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0245-mcp-market-public-network-boundary.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0245McpMarketPublicNetworkBoundary = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0245McpMarketPublicNetworkBoundary as default
};
