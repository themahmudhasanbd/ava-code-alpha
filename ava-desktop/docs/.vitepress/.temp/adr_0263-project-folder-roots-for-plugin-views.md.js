import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse(`{"title":"ADR 0263: Expose a Project's Folder Roots and Complete References Across Them","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0263-project-folder-roots-for-plugin-views.md","filePath":"adr/0263-project-folder-roots-for-plugin-views.md","lastUpdated":1789483301000}`);
const _sfc_main = { name: "adr/0263-project-folder-roots-for-plugin-views.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0263-expose-a-project-s-folder-roots-and-complete-references-across-them" tabindex="-1">ADR 0263: Expose a Project&#39;s Folder Roots and Complete References Across Them <a class="header-anchor" href="#adr-0263-expose-a-project-s-folder-roots-and-complete-references-across-them" aria-label="Permalink to &quot;ADR 0263: Expose a Project&#39;s Folder Roots and Complete References Across Them&quot;">​</a></h1><ul><li><strong>Status</strong>: Accepted</li><li><strong>Date</strong>: 2026-09-14 (amends <a href="./0262-chat-file-refs-open-in-the-file-view">ADR 0262</a>)</li><li><strong>Related</strong>: <a href="./0016-sidebar-organization-and-multi-project-tabs">ADR 0016</a> · <a href="./0233-renderer-owned-multi-folder-project-creation">ADR 0233</a> · <a href="./0241-vendored-updatable-file-view-plugin">ADR 0241</a> · <a href="./0249-chatgpt-style-logical-project-groups">ADR 0249</a> · <a href="./../spec/07-plugins/03-plugin-api">07-plugins/03-plugin-api</a></li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>ADR 0249 made a project a host-owned logical group: a display name, an ordered list of local folder roots, and a primary root. Two of its clauses matter here. The host keeps the <strong>primary root as the one visible workspace</strong> for builtin file tools, and a tool request using an absolute path under <strong>another registered group root</strong> is canonicalized and may use that root as its containment base. Group membership never grants arbitrary filesystem access.</p><p>Nothing of that reached a plugin. <code>pi.workspace.get()</code> returned <code>{ path, name }</code> — the primary root and its leaf name — and main filled it from the single active workspace. So a contributed view could not learn that its project had other folders at all.</p><p>ADR 0262 then made a chat file reference open in the bundled file view, and handed it a <strong>project-root-relative</strong> path. With one folder that is unambiguous. With a group it is not: <code>src/a.ts</code> does not say which folder it belongs to, and the view&#39;s own containment base was the workspace root, so a reference that resolved into a sibling folder could only be opened as an external file — outside the tree, unhighlighted, and with the jail bypassed for a file that is in fact part of the project.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><strong><code>pi.workspace.get()</code> gains <code>projectId</code> and <code>roots</code>.</strong> <code>path</code> and <code>name</code> keep their meaning — the primary root and its leaf name — so every existing plugin, including the file view shipped today, behaves exactly as before. <code>roots</code> is <code>{ path, name, primary }[]</code> in group order, primary first. The <code>workspace:changed</code> event carries the same object. This is project metadata the sidebar already shows, so it needs no new permission and no new method.</li><li><strong>Main answers from a snapshot of the group records.</strong> host-core owns them; main keeps a copy so the synchronous <code>workspace:changed</code> broadcast and the asynchronous pull cannot disagree. The one authoritative list call (<code>project.groups.list</code>) fills it, every group mutation refreshes it, and a cold snapshot is fetched once on the first workspace of a run before the event is repeated with the folders.</li><li><strong>Completion searches the whole project.</strong> <code>fs.resolveRef</code> walks the group&#39;s folders — primary first, then the group&#39;s own order — before the session scratch store, exactly as it walked the single workspace before. The first root that answers still wins outright, and the match now names the folder it answered from.</li><li><strong>The address shape follows the folder.</strong> A match in the primary root travels as a project-relative path, byte-identical to ADR 0262. A match in any sibling folder travels as an <strong>absolute</strong> path — the shape scratch and attachment files already use — which the file view maps back to the folder that contains it. This keeps the payload a single opaque string, so no view contract changes and no new field has to be understood by a plugin that does not care.</li><li><strong>The host file tab reaches the group too.</strong> Its extra containment roots gain the project&#39;s other folders, which ADR 0249 §5 already sanctions, so a reference that resolves into a sibling folder still opens when the file view is absent or disabled instead of failing containment.</li><li><strong>The file view&#39;s own jail base becomes the selected folder.</strong> Its reads, writes, listing and search stay confined to one registered root at a time — never the whole group — with the same symlink and junction refusals and the same credential deny list. Which folder that is, and remembering it per project, is the view&#39;s own business.</li><li><strong>The switch is plugin-local.</strong> Choosing another folder changes what that view browses. It does not change the app&#39;s visible workspace, the agent&#39;s tool roots, a session&#39;s primary path, project instructions or project memory. ADR 0249&#39;s one-visible-workspace model is untouched.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A short reference now resolves anywhere in the project, and the view that opens it can show the file in its own folder rather than as an external file.</li><li>A multi-folder project can be browsed folder by folder in the work panel, remembered per project, without the user changing what the agent works on.</li><li>The container model, the permission model and the session model do not change: no new permission, no new IPC capability for plugins, one containment base at a time.</li><li>A plugin that ignores <code>roots</code> keeps working; a host that cannot resolve a group sends the original payload and the view falls back to single-root behaviour, so the feature degrades rather than breaking.</li><li><code>fs.resolveRef</code> now has a wider notion of &quot;the project&quot;. A project group is a user-authored list, so completion can reach folders the user added deliberately and nothing else.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><h3 id="let-the-renderer-push-the-group-into-the-view-payload" tabindex="-1">Let the renderer push the group into the view payload <a class="header-anchor" href="#let-the-renderer-push-the-group-into-the-view-payload" aria-label="Permalink to &quot;Let the renderer push the group into the view payload&quot;">​</a></h3><p>Rejected: a view also loads from the launcher with no chat reference at all, and <code>location</code> is a one-shot subject, not metadata a long-lived view can rely on.</p><h3 id="add-a-pi-project-roots-method-instead-of-extending-workspace-get" tabindex="-1">Add a <code>pi.project.roots()</code> method instead of extending <code>workspace.get()</code> <a class="header-anchor" href="#add-a-pi-project-roots-method-instead-of-extending-workspace-get" aria-label="Permalink to &quot;Add a \`pi.project.roots()\` method instead of extending \`workspace.get()\`&quot;">​</a></h3><p>Rejected: <code>workspace.get</code> is already where a plugin learns which root it is working in. Two methods answering the same question would drift, and the additive fields cost nothing to a plugin that ignores them.</p><h3 id="run-completion-against-every-group-folder-with-equal-weight" tabindex="-1">Run completion against every group folder with equal weight <a class="header-anchor" href="#run-completion-against-every-group-folder-with-equal-weight" aria-label="Permalink to &quot;Run completion against every group folder with equal weight&quot;">​</a></h3><p>Rejected: the primary root is the folder the agent&#39;s tools default to, so it should answer first; the group&#39;s order then ranks the rest. An exact absolute reference still outranks every shorthand, so nothing about this hides a real hit.</p><h3 id="make-the-folder-switch-activate-that-folder-as-the-workspace" tabindex="-1">Make the folder switch activate that folder as the workspace <a class="header-anchor" href="#make-the-folder-switch-activate-that-folder-as-the-workspace" aria-label="Permalink to &quot;Make the folder switch activate that folder as the workspace&quot;">​</a></h3><p>Rejected: it would move the agent&#39;s tool roots, a session&#39;s primary path and the project&#39;s instructions, which is exactly the multi-root execution ADR 0233 and ADR 0249 refused. The user asked for a file browser that can look at the project&#39;s other folders, not for the project to change underneath the conversation.</p><h3 id="widen-the-bundled-view-s-declared-fs-root-to-the-whole-group" tabindex="-1">Widen the bundled view&#39;s declared <code>fs</code> root to the whole group <a class="header-anchor" href="#widen-the-bundled-view-s-declared-fs-root-to-the-whole-group" aria-label="Permalink to &quot;Widen the bundled view&#39;s declared \`fs\` root to the whole group&quot;">​</a></h3><p>Rejected: the manifest&#39;s root model is one directory, and widening it would give the view standing access to every folder of every project rather than to the one the user is looking at. One containment base at a time is the narrower grant and is what the view already does.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0263-project-folder-roots-for-plugin-views.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0263ProjectFolderRootsForPluginViews = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0263ProjectFolderRootsForPluginViews as default
};
