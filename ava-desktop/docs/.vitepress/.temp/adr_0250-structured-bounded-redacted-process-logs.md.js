import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0250 — Structured, bounded, and redacted process logs","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0250-structured-bounded-redacted-process-logs.md","filePath":"adr/0250-structured-bounded-redacted-process-logs.md","lastUpdated":1789389600000}');
const _sfc_main = { name: "adr/0250-structured-bounded-redacted-process-logs.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0250-—-structured-bounded-and-redacted-process-logs" tabindex="-1">ADR 0250 — Structured, bounded, and redacted process logs <a class="header-anchor" href="#adr-0250-—-structured-bounded-and-redacted-process-logs" aria-label="Permalink to &quot;ADR 0250 — Structured, bounded, and redacted process logs&quot;">​</a></h1><ul><li><strong>Status</strong>: Accepted for implementation</li><li><strong>Date</strong>: 2026-09-14</li><li><strong>Amends</strong>: <a href="./0046-categorized-process-logs">ADR 0046</a> · <a href="./0212-remove-diagnostic-timing-log-streams">ADR 0212</a></li><li><strong>Related</strong>: <a href="./../spec/03-runtime/09-logging-and-observability">Logging and observability</a></li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The categorized logger removed the old aggregate and timing streams, but its remaining records were still too dependent on free-form messages. In particular, a normal tool call produced separate <code>tool start</code> and <code>tool end</code> rows while console mirroring discarded most correlation fields. Child stderr and error details could also contain credentials, absolute local paths, or unbounded diagnostic text.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>Every process log record has a stable dot-separated <code>event</code> in addition to its short human-readable <code>message</code>. Correlation fields (<code>traceId</code>, <code>requestId</code>, <code>sessionId</code>, <code>turnId</code>, <code>toolCallId</code>, parent tool call, agent, plugin, and execution IDs) remain top-level fields.</li><li>A normal tool execution emits one <code>tool.execution.completed</code> or <code>tool.execution.failed</code> record after <code>tool_end</code>. If the sidecar exits while a tool is active, it emits one <code>tool.execution.interrupted</code> record for that tool. The sidecar <code>tool_start</code>/<code>tool_end</code> protocol events and transcript persistence are unchanged. Functional duration metadata remains allowed; no timing stream is introduced.</li><li>The Electron logger redacts sensitive object keys, authorization schemes, URL credentials, and common provider token formats. Home, application-data, and log-directory prefixes are replaced with placeholders. Strings, object depth, field counts, array items, and serialized <code>data</code> are bounded; <code>data</code> is capped at 8 KiB per record. Host-core audit payloads apply the corresponding string/path redaction and are capped at 8 KiB after shaping.</li><li>Tool results are represented by safe summaries: outcome, stable error/code fields, duration, field names, content-block count, and stdout/stderr sizes. Raw tool arguments, file contents, command output, and plugin responses are not copied to normal process logs.</li><li>Child stderr is emitted as a bounded, ANSI-free <code>data.output</code> value with the stable <code>child.process.stderr</code> event. The main-process error fallback uses the same structured and redacted shape. Console mirrors contain the same sanitized record as the file; in production only errors are mirrored.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Normal tool-loop storage and console noise are reduced without losing the final outcome, identifiers, error code, or duration needed for diagnosis.</li><li>Logs are searchable by event and correlation ID and can be joined across app, host, and agent channels without parsing message text.</li><li>Sensitive or unusually large diagnostic values are removed, summarized, or truncated before writing. Logging remains best-effort and cannot fail the caller.</li><li>Existing protocol, transcript, audit timing fields, retention, category files, and historical log files remain compatible.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><ul><li><strong>Keep start/end records and improve their wording.</strong> Rejected because it preserves duplicate storage and still leaves the outcome split across rows.</li><li><strong>Log complete tool arguments and results behind a debug switch.</strong> Rejected because local debug logs are still user data and can contain credentials or private workspace content.</li><li><strong>Add a separate timing stream for duration.</strong> Rejected by ADR 0212; duration is retained only as functional metadata on the relevant outcome or audit record.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0250-structured-bounded-redacted-process-logs.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0250StructuredBoundedRedactedProcessLogs = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0250StructuredBoundedRedactedProcessLogs as default
};
