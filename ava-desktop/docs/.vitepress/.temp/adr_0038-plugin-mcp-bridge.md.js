import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0038: Bridge plugin-declared MCP servers in Electron main","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0038-plugin-mcp-bridge.md","filePath":"adr/0038-plugin-mcp-bridge.md","lastUpdated":1789885701000}');
const _sfc_main = { name: "adr/0038-plugin-mcp-bridge.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0038-bridge-plugin-declared-mcp-servers-in-electron-main" tabindex="-1">ADR 0038: Bridge plugin-declared MCP servers in Electron main <a class="header-anchor" href="#adr-0038-bridge-plugin-declared-mcp-servers-in-electron-main" aria-label="Permalink to &quot;ADR 0038: Bridge plugin-declared MCP servers in Electron main&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-07-31</li><li>Related: <a href="./../spec/07-plugins/02-plugin-manifest-schema">Plugin manifest schema</a>, <a href="./../spec/07-plugins/04-plugin-security">Plugin security</a>, <a href="./0008-plugin-runtime-isolation-target">ADR 0008</a>, <a href="./0142-allow-non-loopback-http-mcp">ADR 0142</a></li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The plugin roadmap listed an &quot;MCP plugin type&quot; with nothing behind it. A plugin that wanted to expose an MCP server&#39;s tools had to reimplement each one as a plugin agent tool and proxy the traffic itself — duplicated work, and the proxying happened in plugin code the host cannot inspect.</p><p>Two constraints shape the design. First, the agent already has a working path for plugin tools: <code>plugin_&lt;pluginIdSafe&gt;_&lt;toolName&gt;</code> flows from the model through the sidecar to host-core&#39;s permission gate and back out to Electron main (<code>07-plugins/12</code> appendix). Anything that reuses that path inherits the timeout, the audit trail, and the per-plugin disable switch for free. Second, MCP servers are two very different things wearing one name: a local executable we spawn, and a remote endpoint we send arguments to. They fail differently and they leak differently.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>MCP servers are <strong>declared in the manifest</strong>, never opened at runtime. <code>contributes.mcpServers</code> carries <code>{ id, label?, transport }</code> plus exactly one transport&#39;s fields — <code>stdio</code> uses <code>command</code> / <code>args</code> / <code>env</code>, <code>http</code> uses <code>url</code> / <code>headers</code>. A manifest that mixes them fails validation in both host-core and the SDK validator.</li><li>The two transports carry <strong>separate permissions</strong>: <code>mcp.server.local</code> for stdio, <code>mcp.server.remote</code> for HTTP. Running a local binary and shipping tool arguments to a third party are different consents, so the permission copy states each plainly.</li><li>The client lives in <code>apps/desktop/electron/main/plugin-mcp.ts</code> and speaks protocol <code>2025-06-18</code>: <code>initialize</code>, <code>tools/list</code>, <code>tools/call</code>. Framing is NDJSON over stdio pipes, or streamable HTTP with SSE responses. Budgets: 10s to complete the handshake, 100s per call, 4MB per stdio line, 8 servers per plugin. 2048 tools, 100 <code>tools/list</code> pages, 30s for the whole traversal, and a cursor that repeats or is malformed — a server that breaks any bound is refused instead of truncated. host-core&#39;s 150s dispatch deadline carries the whole leg — handshake, traversal, and call — so the client reports its own timeout first. Connection is lazy — declaring a server costs nothing until a tool is called — and teardown follows unload.</li><li>Discovered tools register into the <strong>existing</strong> plugin tool map as <code>plugin_&lt;pluginIdSafe&gt;_&lt;serverId&gt;_&lt;toolName&gt;</code>, so no new routing exists anywhere between the model and the server.</li><li>Every discovered tool is registered at <code>risk: &quot;medium&quot;</code>. The name, schema, and description come from a third-party server; a self-declared risk level from that source is not evidence.</li><li>Credentials resolve <strong>only</strong> from the plugin&#39;s own settings through <code>{ &quot;setting&quot;: &quot;&lt;key&gt;&quot; }</code> (D018). A stdio child receives <code>PATH</code>, temp/locale variables, <code>PI_PLUGIN_ID</code>, and the declared values — not the host environment, which holds provider keys. <code>command</code> must be a bare PATH name or stay inside the plugin directory; <code>url</code> may use <code>http</code> or <code>https</code>, with non-loopback HTTP subject to ADR 0142 and the plugin network allowlist.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A plugin ships an MCP server as a manifest entry, and its tools appear to the agent with correct namespacing, auditing, and timeouts.</li><li>Reviewing what a plugin can reach means reading its manifest: every endpoint and executable is declared text, not a runtime decision.</li><li>Catalog size is a protocol guard, not a prompt budget: a server with 300 tools contributes all 300, because MCP tools reach the model as deferred on-demand entries behind <code>ToolSearch</code> and only the prompt block that advertises them is capped. A server that breaks a guard (count, pages, cursor, time) is refused and contributes nothing, rather than flooding the prompt with a prefix of its catalog.</li><li>Because MCP tools sit in the same map as hand-written plugin tools, disabling the plugin removes both, and a crashed plugin loses both.</li><li>A remote server sees tool arguments. No amount of host-side care changes that; the permission and its copy exist so the user decides.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><h3 id="stdio-only" tabindex="-1">stdio only <a class="header-anchor" href="#stdio-only" aria-label="Permalink to &quot;stdio only&quot;">​</a></h3><p>Rejected. Hosted MCP endpoints are common, and stdio-only would push plugin authors to wrap them in a local shim process — an extra process plus an unreviewable proxy, which is worse on both counts than a declared HTTP(S) URL.</p><h3 id="a-separate-type-mcp-plugin-kind" tabindex="-1">A separate <code>type: &quot;mcp&quot;</code> plugin kind <a class="header-anchor" href="#a-separate-type-mcp-plugin-kind" aria-label="Permalink to &quot;A separate \`type: &quot;mcp&quot;\` plugin kind&quot;">​</a></h3><p>Rejected. It would need its own lifecycle, its own management UI, and its own tool routing. A contribution point on the existing plugin type reuses install, enable/disable, permissions, and audit as they are.</p><h3 id="let-the-plugin-process-own-the-mcp-connection" tabindex="-1">Let the plugin process own the MCP connection <a class="header-anchor" href="#let-the-plugin-process-own-the-mcp-connection" aria-label="Permalink to &quot;Let the plugin process own the MCP connection&quot;">​</a></h3><p>Rejected. The connection would then live behind the plugin&#39;s own code, outside the broker&#39;s view, so the host could neither cap the tool set nor keep credentials out of the plugin&#39;s reach.</p><h3 id="trust-the-server-s-declared-tool-risk" tabindex="-1">Trust the server&#39;s declared tool risk <a class="header-anchor" href="#trust-the-server-s-declared-tool-risk" aria-label="Permalink to &quot;Trust the server&#39;s declared tool risk&quot;">​</a></h3><p>Rejected. Risk drives host-core&#39;s confirmation flow. A value chosen by the inspected party cannot gate the inspection.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0038-plugin-mcp-bridge.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0038PluginMcpBridge = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0038PluginMcpBridge as default
};
