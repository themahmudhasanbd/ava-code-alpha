import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0276: Official plugin channel and backup channels","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0276-official-plugin-channel-and-backup-channels.md","filePath":"adr/0276-official-plugin-channel-and-backup-channels.md","lastUpdated":1789630686000}');
const _sfc_main = { name: "adr/0276-official-plugin-channel-and-backup-channels.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0276-official-plugin-channel-and-backup-channels" tabindex="-1">ADR 0276: Official plugin channel and backup channels <a class="header-anchor" href="#adr-0276-official-plugin-channel-and-backup-channels" aria-label="Permalink to &quot;ADR 0276: Official plugin channel and backup channels&quot;">​</a></h1><ul><li>Status: Accepted for implementation</li><li>Date: 2026-09-17</li><li>Deciders: PI-Desktop plugin and distribution maintainers</li><li>Related: ADR 0102, D238, E2E-024P, E2E-PLUGIN-official-channel-resolves-through-the-platform</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The client&#39;s marketplace source was a three-value union — <code>official</code>, <code>mirror</code>, <code>custom</code> — mapped to hardcoded catalog URLs, and <code>official</code> was indistinguishable from &quot;never set&quot;: both landed on the GitHub default. A package was fetched by joining the catalog&#39;s relative <code>url</code> onto <code>artifactBaseUrl</code> or the catalog directory, and the <code>verified</code> trust tier was recognized only while the catalog URL equaled one of two constants.</p><p>Since ADR 0102 the plugin center at <code>plugins.aiuo.net</code> is the publishing authority, and it defines the client-facing download contract:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>GET  /catalog.json                  the catalog the client reads</span></span>
<span class="line"><span>POST /api/v1/download/resolve       JSON { deviceId, pluginId, version? }</span></span>
<span class="line"><span>                                    -&gt; 200 { sha256, sizeBytes, downloads[] }</span></span>
<span class="line"><span>                                       Cache-Control: no-store</span></span>
<span class="line"><span>GET  /download/{id}/{version}       byte route for the marketplace page and</span></span>
<span class="line"><span>                                    console; not the client&#39;s path</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><p>The platform&#39;s own rules, verified against production: it never hosts the bytes of a published package, only answers where they are; <code>downloads</code> is ordered and never empty on <code>200</code>; a client calls resolve once per install or update, sends a <strong>stable</strong> device identifier, never caches the answer, verifies <code>sha256</code> before installing, and tries the next mirror when one fails; <code>counted: false</code> is normal; rate limiting is per device identifier (otherwise per source address), 600 requests per minute by default, with <code>429</code> and <code>Retry-After</code>; refusals are <code>400 PLUGIN_REQUIRED</code>, <code>403 NOT_PUBLISHED</code>, <code>403 PLUGIN_ARCHIVED</code>, <code>404 NOT_FOUND</code>, <code>429</code>, and <code>503 NO_DOWNLOAD_SOURCE</code>; and a platform-unreachable install falls back to the catalog&#39;s URL and is not counted. Counting identity is the device identifier, the platform stores counts rather than identities, and the optional <code>source</code> field is a statistics hint only.</p><p>Measured on the same day: the catalog declares <code>artifactBaseUrl = raw.githubusercontent.com/AIUO-Net/pi-desktop-plugins/main</code>; all three mirrors answered <code>200</code> and every mirror host is already inside the client&#39;s download allowlist; the CNB mirror still served an older distribution (22 plugins, and <code>pi.todo-0.6.5</code> at 92487 bytes where the other mirror served 92951 bytes). A version string may contain <code>+</code> (for example <code>0.3.0+0.2.0</code>), so the platform&#39;s byte route cannot be assumed to carry every published version.</p><p>The client&#39;s source model therefore has to become &quot;the center first, the two Git hosts as backups&quot;, and the install path has to follow the platform&#39;s contract.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><h3 id="_1-four-channels-and-official-keeps-its-meaning" tabindex="-1">1. Four channels, and <code>official</code> keeps its meaning <a class="header-anchor" href="#_1-four-channels-and-official-keeps-its-meaning" aria-label="Permalink to &quot;1. Four channels, and \`official\` keeps its meaning&quot;">​</a></h3><table tabindex="0"><thead><tr><th>#</th><th>Channel</th><th>Value</th><th>Catalog URL</th><th>Install path</th></tr></thead><tbody><tr><td>1</td><td>Official channel</td><td><code>official</code> (default)</td><td><code>https://plugins.aiuo.net/catalog.json</code></td><td>Platform resolve</td></tr><tr><td>2</td><td>GitHub backup</td><td><code>github</code> (new)</td><td><code>https://raw.githubusercontent.com/AIUO-Net/pi-desktop-plugins/main/catalog.json</code></td><td>Static relative URL</td></tr><tr><td>3</td><td>CNB backup</td><td><code>mirror</code> (unchanged)</td><td><code>https://cnb.cool/aixk/pi-desktop-plugins/-/git/raw/main/catalog.json</code></td><td>Static relative URL</td></tr><tr><td>4</td><td>Custom</td><td><code>custom</code> (unchanged)</td><td>user-provided</td><td>Static relative URL</td></tr></tbody></table><p><code>official</code> keeps meaning &quot;the project&#39;s own first-party channel&quot; and now points at the center, so an unset value and an unrecognized value resolve to it and no persisted setting is migrated. <code>mirror</code> keeps meaning CNB; <code>github</code> is new because the GitHub backup no longer is the default. Display names are localized — Official channel / GitHub backup / CNB backup / Custom in English. A package URL never crosses providers: whichever channel served the catalog also serves the package.</p><h3 id="_2-the-official-channel-resolves-through-the-platform" tabindex="-1">2. The official channel resolves through the platform <a class="header-anchor" href="#_2-the-official-channel-resolves-through-the-platform" aria-label="Permalink to &quot;2. The official channel resolves through the platform&quot;">​</a></h3><ol><li>Refresh the catalog, then <code>POST {center origin}/api/v1/download/resolve</code> with <code>{ deviceId, pluginId, version }</code> — once per install or update. POST rather than GET, because the platform documents that a device id in a query string ends up in access logs.</li><li>Try the returned <code>downloads</code> entries in order. Every attempt is downloaded to a temporary file, checked against the returned <code>sha256</code> and the announced <code>sizeBytes</code>, and extracted only when both agree. A mirror that fails — network error, HTTP error, digest mismatch, size mismatch — is abandoned and the next one is tried. Requests stay on the download allowlist.</li><li>Only when the platform could not be reached, or when no mirror in the list could serve the version, the client falls back to the catalog&#39;s own package URL (<code>artifactBaseUrl</code> plus the relative <code>url</code>). That is the fallback the platform defines, and that install is not counted.</li><li>The answer is never cached (<code>Cache-Control: no-store</code> is the platform&#39;s confirmation of this), so two installs of the same version are two calls.</li><li>The resolve digest is authoritative on the resolve path; the catalog&#39;s <code>shasum</code> stays authoritative on the fallback and on the backup channels. <code>sizeBytes</code> is a sanity check on both.</li><li>Refusals are reported, not papered over: <code>403 NOT_PUBLISHED</code> (no retry), <code>403 PLUGIN_ARCHIVED</code> (hide the plugin from install and update selection), <code>404</code> (the platform has no such version — including a <code>+</code>-suffixed version the byte route cannot carry), <code>429</code> (wait the <code>Retry-After</code> interval, retry once, then report), <code>503</code> (report the deployment problem; <code>NO_DOWNLOAD_SOURCE</code> means no mirror can serve the version).</li><li>Nothing switches the channel automatically. An unreachable platform is reported after the fallback, and the user switches through the existing selector.</li></ol><h3 id="_3-a-stable-device-identifier-sent-as-a-digest" tabindex="-1">3. A stable device identifier, sent as a digest <a class="header-anchor" href="#_3-a-stable-device-identifier-sent-as-a-digest" aria-label="Permalink to &quot;3. A stable device identifier, sent as a digest&quot;">​</a></h3><p>The resolve request carries a <code>deviceId</code> that host-core derives from the machine identity the operating system exposes — the Windows <code>HKLM\\SOFTWARE\\Microsoft\\Cryptography\\MachineGuid</code>, the macOS platform UUID, or Linux <code>/etc/machine-id</code> (falling back to <code>/var/lib/dbus/machine-id</code> and <code>/sys/class/dmi/id/product_uuid</code>). What leaves the machine is <code>sha256(&quot;pi-desktop.device.v1:&quot; + &lt;machine id&gt;)</code> as 64-character lowercase hex, never the machine code itself: the platform only needs to compare two requests for equality, so it has no use for the code, and a raw hardware identifier in a third-party request is a privacy downgrade the counting feature does not require. The fixed prefix domain-separates the value from every other hash the app computes, so a leaked digest cannot be matched against another digest of the same machine.</p><p>When no machine identity is readable, host-core generates one random 64-hex id on first use and persists it under the application data directory (<code>plugins/market/device.json</code>), reusing it from then on. Either way the value is read once per process, is stable for an installation across restarts, is never shown in the UI, and is not a setting. It is a counting and rate-limit key, not an account.</p><h3 id="_4-the-backup-channels-and-custom-are-unchanged" tabindex="-1">4. The backup channels and <code>custom</code> are unchanged <a class="header-anchor" href="#_4-the-backup-channels-and-custom-are-unchanged" aria-label="Permalink to &quot;4. The backup channels and \`custom\` are unchanged&quot;">​</a></h3><p><code>github</code>, <code>mirror</code>, and <code>custom</code> keep the static relative-URL resolution, including <code>artifactBaseUrl</code> precedence, the download allowlist, redirect re-validation, and shasum verification, byte for byte. The persisted source keeps working for existing users, the cache stays keyed by <code>sourceUrl</code> so a switch ignores rather than deletes another channel&#39;s snapshot, and an installed record&#39;s <code>providerId</code> now names the channel it actually came from — already installed records are not rewritten.</p><h3 id="_5-trust-tiers-follow-the-trusted-channels" tabindex="-1">5. Trust tiers follow the trusted channels <a class="header-anchor" href="#_5-trust-tiers-follow-the-trusted-channels" aria-label="Permalink to &quot;5. Trust tiers follow the trusted channels&quot;">​</a></h3><p><code>verified</code> renders as written only from the three project channels (official, GitHub backup, CNB backup). <code>custom</code> still degrades to <code>community</code>, and a v1 catalog&#39;s boolean <code>verified</code> mapping is unchanged. The predicate changes from &quot;is this the official source&quot; to &quot;is this a trusted channel&quot;.</p><h3 id="_6-the-download-allowlist-does-not-change" tabindex="-1">6. The download allowlist does not change <a class="header-anchor" href="#_6-the-download-allowlist-does-not-change" aria-label="Permalink to &quot;6. The download allowlist does not change&quot;">​</a></h3><p>The allowlist stays <code>github.com</code>, <code>githubusercontent.com</code>, <code>cnb.cool</code>, and the host that served the current catalog. The platform&#39;s resolve answers name mirrors on exactly those hosts today, so no entry is added. A mirror on any other host is refused like any other package URL: the next mirror is tried, and if every entry names such a host the install fails instead of widening the list. Moving a mirror outside the table is therefore a client-release dependency, not a platform-side change.</p><h3 id="_7-an-install-reports-progress-and-can-be-cancelled-while-it-downloads" tabindex="-1">7. An install reports progress, and can be cancelled while it downloads <a class="header-anchor" href="#_7-an-install-reports-progress-and-can-be-cancelled-while-it-downloads" aria-label="Permalink to &quot;7. An install reports progress, and can be cancelled while it downloads&quot;">​</a></h3><p>An install or update is a single request, so a download that takes a minute is otherwise indistinguishable from a frozen window. Host-core therefore reports where the install is: <code>plugin.installProgress</code> carries <code>pluginId</code>, <code>version</code>, <code>phase</code> (<code>resolve</code>, <code>download</code>, <code>verify</code>, <code>install</code>, <code>enable</code>), the mirror in use (<code>source</code>) with its 1-based <code>attempt</code> / <code>attempts</code>, <code>receivedBytes</code> / <code>totalBytes</code> (<code>0</code> meaning the size is unknown), every mirror tried so far (<code>tried[]</code>, each <code>{ source, url, error }</code>), and <code>error</code> on the report that ends a failed install. Reports are throttled to at most one per 200 ms, plus one per phase change and the terminal report, so byte-level progress cannot flood the RPC channel or the interface.</p><p>The report goes to the interface rather than only to the log. A log line helps an operator afterwards; the person waiting needs to see that the install moved on to the second mirror and how far it is. Progress is therefore observer-shaped, host-core emits it as a notification, and the RPC layer owns the throttle.</p><p><code>market.cancelInstall { id }</code> answers <code>{ cancelled, id }</code> and cancels only an install that is running right now, never a queued or finished one. Cancellation is honoured while bytes arrive, before each mirror is tried, and at the last safe point before the package is written, so only a download is actually interruptible: stopping after the package has been unpacked and the plugin&#39;s row is being written would leave the plugin directory and the registry in a state no later step can describe, which is worse than letting that step finish. A cancelled install fails with <code>PLUGIN_CANCELLED</code> (code 1019), and the dialog closes quietly instead of reporting an error.</p><p>The dialog is for a manual install or update only; a background auto-update stays silent. It shows the phase, <code>mirror n/N · name</code>, a determinate bar, and a cancel button enabled only during <code>resolve</code> and <code>download</code>; success closes it about two seconds after the install finishes, failure keeps it open with the readable error, the mirrors that were tried with a copy action, and a retry action.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>The official channel gains per-mirror fallback, withdrawal and archive signals, and download counting, and it acquires a dependency: an install asks the platform. The documented catalog-URL fallback and the two backup channels are the mitigation.</li><li>Per-mirror digest verification becomes the reason a divergent mirror cannot break an install. The measured CNB divergence (<code>pi.todo-0.6.5</code> at 92487 bytes versus 92951) is exactly the case this absorbs.</li><li>A first third-party service now receives an installation-level identifier. Only a digest is sent, the platform stores counts rather than identities, and the privacy policy and the decisions log state it.</li><li><code>429</code> costs at most one <code>Retry-After</code> wait per install; a batch update makes one call per plugin, not one per mirror.</li><li>An install is no longer opaque: the interface sees the phase, the mirror being tried, and the byte count, and a user can stop a download without leaving a half-installed plugin. The cost is progress notifications on the RPC channel, bounded by the 200 ms throttle and the per-phase reports.</li><li>No <code>.piplug</code> or manifest change, no catalog v1/v2 parsing change, no permission-review change, no storage schema change, no migration of persisted source values, and no change to the two backup install paths.</li><li>Still open, deliberately: signature verification (optional as before), any automatic channel switching, and the center&#39;s own byte route, which stays the marketplace page and console path.</li></ul><h2 id="rejected-alternatives" tabindex="-1">Rejected alternatives <a class="header-anchor" href="#rejected-alternatives" aria-label="Permalink to &quot;Rejected alternatives&quot;">​</a></h2><ul><li><strong>Keep resolving the catalog&#39;s declared base on the client only.</strong> The catalog is a static file, so it cannot say which mirrors currently serve a version, it cannot refuse an archived or unpublished plugin, and it is the same list for every install. The client would also keep trusting a publisher-influenced base without any per-mirror signal, and the platform would lose the counting and rate-limit identity its contract depends on.</li><li><strong>Send a per-request identifier.</strong> The platform counts one download per device, version, and day and rate-limits per device, so a value that changes per call counts every call as a new installation and degrades the limiter to the source address. The requirement is stability.</li><li><strong>Send the raw machine code, or hash it without a prefix.</strong> The platform only compares values, so shipping the code itself would hand a third party a reusable hardware identifier for no functional gain, and an unprefixed digest could be correlated with other digests of the same machine.</li><li><strong>Expose the identifier in Settings (show or reset it).</strong> It answers no user question, it invites treating a counting key as an account, and a reset button would silently let one machine inflate the platform&#39;s counters. The fallback file is documented instead.</li><li><strong>An automatic fallback between channels.</strong> A channel switch changes which catalog, versions, trust verdicts, and bytes the user sees, so doing it silently would attribute an install to a source the user did not choose. The only automatic fallback stays inside the official channel, to the catalog URL that channel served, and it is not counted.</li><li><strong>Cache the resolve answer.</strong> The platform marks it <code>no-store</code> because mirrors and availability change; a cached answer would pin a dead mirror and could serve a version the platform has since withdrawn.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0276-official-plugin-channel-and-backup-channels.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0276OfficialPluginChannelAndBackupChannels = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0276OfficialPluginChannelAndBackupChannels as default
};
