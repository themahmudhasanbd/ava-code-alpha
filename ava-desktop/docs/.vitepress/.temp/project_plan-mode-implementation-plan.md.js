import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Plan Checkpoint and Shell Implementation Plan","description":"","frontmatter":{},"headers":[],"relativePath":"project/plan-mode-implementation-plan.md","filePath":"project/plan-mode-implementation-plan.md","lastUpdated":1789015571000}');
const _sfc_main = { name: "project/plan-mode-implementation-plan.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="plan-checkpoint-and-shell-implementation-plan" tabindex="-1">Plan Checkpoint and Shell Implementation Plan <a class="header-anchor" href="#plan-checkpoint-and-shell-implementation-plan" aria-label="Permalink to &quot;Plan Checkpoint and Shell Implementation Plan&quot;">​</a></h1><ul><li>Status: Implemented and accepted on 2026-08-05</li><li>Scope: Plan checkpoint approval/execution and selectable command shells</li><li>Core runtime: one <code>@earendil-works/pi-agent-core</code> Agent per session</li><li>Baseline: <code>0.4.14</code></li><li>Host protocol: v9</li><li>Database schema: v10</li><li>Delivery: M6 complete; Host/no-replay, pending-restore, and terminal-card non-hydration evidence accepted</li></ul><h2 id="_1-executive-decision" tabindex="-1">1. Executive decision <a class="header-anchor" href="#_1-executive-decision" aria-label="Permalink to &quot;1. Executive decision&quot;">​</a></h2><p>PI-Desktop continues to run one pi Agent. The selector is <code>Agent | Plan</code>, with Agent as the default. Plan is the same Agent in planning state, not a second planner, model, service, permission mode, or security sandbox.</p><p>Plan exposes Read, Glob, Grep, BrowserPreview, Bash, CompactContext, EnterPlanMode, and SubmitPlan. Write, Edit, plugin tools, and unknown tools are denied by Rust host-core. Bash follows the selected permission mode, so Plan is planning intent rather than strict read-only security.</p><p><code>SubmitPlan(title, markdown, question)</code> is the only tool in its assistant batch. Host-core writes the submitted Markdown bytes unchanged to one new immutable file under <code>&lt;workspaceRoot&gt;/.pi/plan/*.md</code>. It records the unique relative path, SHA-256, and byte size, plus the structured title and question, in the same approval row. It does not add a title/question wrapper or replace an earlier artifact. The approval surface offers only Approve and Reject, defaults the explicit permission choice to Ask, and opens the artifact for review.</p><p>Approval atomically changes the durable session to Agent, stores the selected permission mode, and queues the same Agent&#39;s next execution turn. The approval uses one absolute 30-minute deadline and reports expiry as <code>PLAN_APPROVAL_TIMEOUT</code>. Pending, queued, and running work is interrupted on host restart with no replay. A pending interruption leaves the session Plan; an already-approved queued or running interruption leaves it Agent.</p><p>The Bash tool retains its protocol name while using a host shell catalog. The catalog IDs are <code>windows-powershell</code>, <code>windows-pwsh</code>, <code>cmd</code>, <code>git-bash</code>, and <code>bash</code>. The effective shell ID and dialect are pinned for each turn, stdout/stderr stream separately, the default timeout is exactly 60 seconds, explicit timeouts are bounded to 1-300 seconds, and cancellation shuts down the full process tree.</p><h2 id="_2-invariants-and-vocabulary" tabindex="-1">2. Invariants and vocabulary <a class="header-anchor" href="#_2-invariants-and-vocabulary" aria-label="Permalink to &quot;2. Invariants and vocabulary&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Concept</th><th>Values</th><th>Authority</th></tr></thead><tbody><tr><td>Durable operating mode</td><td><code>agent</code>, <code>plan</code></td><td>Rust host SQLite</td></tr><tr><td>Live Plan state</td><td><code>planning</code>, <code>awaiting_approval</code>, <code>inactive</code>, <code>stopped</code></td><td>Host/runtime projection</td></tr><tr><td>Approval action</td><td><code>approve</code>, <code>reject</code></td><td>Host RPC</td></tr><tr><td>Approval status</td><td><code>pending</code>, <code>approved</code>, <code>rejected</code>, <code>expired</code>, <code>interrupted</code></td><td><code>plan_approvals.status</code></td></tr><tr><td>Execution state</td><td><code>queued</code>, <code>running</code>, <code>completed</code>, <code>interrupted</code></td><td><code>plan_approvals.execution_state</code></td></tr><tr><td>Permission mode</td><td><code>inherit</code>, <code>ask</code>, <code>accept-edits</code>, <code>auto</code></td><td>Host settings/session policy</td></tr><tr><td>Shell ID</td><td><code>windows-powershell</code>, <code>windows-pwsh</code>, <code>cmd</code>, <code>git-bash</code>, <code>bash</code></td><td>Host catalog/settings</td></tr><tr><td>Shell dialect</td><td><code>powershell</code>, <code>cmd</code>, <code>posix</code></td><td>Effective shell option</td></tr></tbody></table><p>The renderer and sidecar hold projections. The renderer retains the latest Plan proposal/execution snapshot per session only for its current lifetime from live Host events. <code>plans.pending</code> rehydrates only pending approvals; terminal rows remain Host-owned durable records but do not rehydrate terminal cards. Neither renderer nor sidecar can authorize a mode, write or replace a plan artifact, choose an executable path, or revive an interrupted approval or execution.</p><h2 id="_3-state-lifecycle" tabindex="-1">3. State lifecycle <a class="header-anchor" href="#_3-state-lifecycle" aria-label="Permalink to &quot;3. State lifecycle&quot;">​</a></h2><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Agent / inactive</span></span>
<span class="line"><span>  -&gt; user selects Plan or Agent calls EnterPlanMode</span></span>
<span class="line"><span>Plan / planning</span></span>
<span class="line"><span>  -&gt; SubmitPlan(title, markdown, question)</span></span>
<span class="line"><span>Plan / awaiting_approval</span></span>
<span class="line"><span>  -&gt; approve(permission mode) -&gt; Agent / queued -&gt; Agent / running</span></span>
<span class="line"><span>  -&gt; reject | expiry | abort | persistence failure -&gt; Plan / stopped</span></span>
<span class="line"><span>host restart</span></span>
<span class="line"><span>  -&gt; plan_approvals.pending -&gt; interrupted</span></span>
<span class="line"><span>  -&gt; plan_approvals.execution_state queued/running -&gt; interrupted</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br></div></div><p>Rules:</p><ol><li>One pi Agent exists for the session before, during, and after planning.</li><li><code>EnterPlanMode</code> and <code>SubmitPlan</code> are each the only tool call in their assistant batch.</li><li>Approval resolution matches proposal, session, turn, tool-call, and version identity. There is no serialized process-epoch field.</li><li>A pending approval has one absolute 30-minute deadline; renderer reload never resets it and restores only that still-pending row through <code>plans.pending</code>. Terminal proposal/execution snapshots are not rehydrated.</li><li>Reject and expiry leave a pending session in Plan and grant no execution.</li><li>Approval commits Agent mode before a queued/running execution begins.</li><li>Startup fences prior live work transactionally before serving RPC. No provider, tool, or queue work is replayed.</li></ol><h2 id="_4-turn-and-configuration-boundaries" tabindex="-1">4. Turn and configuration boundaries <a class="header-anchor" href="#_4-turn-and-configuration-boundaries" aria-label="Permalink to &quot;4. Turn and configuration boundaries&quot;">​</a></h2><p>Each session has one active turn, at most one pending approval, and at most one queued or running execution. A second prompt, Plan submission, execution, or mode/provider/model/permission/shell configuration change is rejected while that boundary is active. Configuration is accepted only while idle. Approval actions are the only enabled controls for a pending request. Different sessions retain existing independent turn and workspace-root behavior.</p><p>Scheduled or unattended Plan is rejected before provider work, artifact write, approval, or queue insertion with <code>PLAN_REQUIRES_INTERACTIVE_SESSION</code>.</p><h2 id="_5-submitplan-and-artifact-contract" tabindex="-1">5. SubmitPlan and artifact contract <a class="header-anchor" href="#_5-submitplan-and-artifact-contract" aria-label="Permalink to &quot;5. SubmitPlan and artifact contract&quot;">​</a></h2><div class="language-ts vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">type</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> SubmitPlanInput</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  title</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  markdown</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  question</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">};</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>Host-core validates the project-bound session and writes the exact bytes of <code>markdown</code> to a new unique file matching:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&lt;workspaceRoot&gt;/.pi/plan/&lt;unique-name&gt;.md</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>The write is host-owned and create-new. The artifact directory is created defensively, the bytes are flushed, and the relative path is stored in <code>plan_approvals.artifact_relative_path</code>. <code>title</code> and <code>question</code> are stored in their own structured columns. No canonical UTF-8/LF conversion, final newline, title heading, question heading, or other wrapper is added. A path validation, symlink, write, or collision failure creates no approval row and returns the corresponding <code>PLAN_ARTIFACT_*</code> error.</p><p>Approval receives the artifact opener path and durable hash/size metadata. The UI is required to display the title, question, artifact opener, expiry, and status; it is not required to inline Markdown, SHA-256, or byte size.</p><h2 id="_6-approval-and-execution-contract" tabindex="-1">6. Approval and execution contract <a class="header-anchor" href="#_6-approval-and-execution-contract" aria-label="Permalink to &quot;6. Approval and execution contract&quot;">​</a></h2><div class="language-ts vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">type</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> PlanResolveRequest</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  proposalId</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  sessionId</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  turnId</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  toolCallId</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  version</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> number</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  action</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;approve&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;reject&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  targetPermissionMode</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;ask&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;accept-edits&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;auto&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">};</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br></div></div><p>Approval is an atomic <code>plan_approvals</code> transaction:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>BEGIN</span></span>
<span class="line"><span>  plan_approvals.status: pending -&gt; approved</span></span>
<span class="line"><span>  plan_approvals.execution_id: new ID</span></span>
<span class="line"><span>  plan_approvals.execution_state: NULL -&gt; queued</span></span>
<span class="line"><span>  sessions.mode: plan -&gt; agent</span></span>
<span class="line"><span>  sessions.permission_mode: explicit selected mode</span></span>
<span class="line"><span>COMMIT</span></span>
<span class="line"><span>dispatch the same Agent&#39;s fresh execution turn</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><p>Reject records <code>rejected</code> without a permission mode and leaves the session in Plan. Expiry records <code>expired</code> with <code>PLAN_APPROVAL_TIMEOUT</code>. Abort and startup recovery record <code>interrupted</code>. The execution worker claims <code>queued</code>, runs it, and finishes it as <code>completed</code> or <code>interrupted</code>. No process epoch is serialized in the row or request; startup state marking is the replay fence.</p><h2 id="_7-storage-contract" tabindex="-1">7. Storage contract <a class="header-anchor" href="#_7-storage-contract" aria-label="Permalink to &quot;7. Storage contract&quot;">​</a></h2><p>Schema v10 continues the host-owned <code>plan_approvals</code> table. Its checkpoint and execution fields include:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>request_id, session_id, turn_id, tool_call_id</span></span>
<span class="line"><span>plan_json                 exact submitted Markdown snapshot</span></span>
<span class="line"><span>title, question           structured DB fields</span></span>
<span class="line"><span>status, action, target_permission_mode, feedback</span></span>
<span class="line"><span>created_at, updated_at, expires_at, resolved_at, error_code, version</span></span>
<span class="line"><span>artifact_relative_path, artifact_sha256, artifact_size_bytes</span></span>
<span class="line"><span>execution_id, execution_state</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p>There are no separate <code>plan_artifacts</code> or <code>plan_runs</code> tables and no serialized <code>hostEpoch</code> field. The artifact itself is the immutable Markdown file under <code>.pi/plan/</code>; the approval row indexes it and carries the execution descriptor.</p><p>At database open, one startup transaction marks every prior <code>pending</code> approval and every prior <code>queued</code> or <code>running</code> execution state as <code>interrupted</code>, aborts associated running turns, and commits audit records before the host serves RPC. Renderer reload within the same host can recover a pending row and its original deadline. Rejected, expired, approved/completed, and interrupted terminal cards are not rehydrated after reload. A host restart cannot recover actionable work, restores no stale action, and never replays it; the UI is not required to show the interrupted terminal snapshot.</p><p>The v8-to-v10 path checkpoints WAL, creates an exact readable <code>pi.sqlite.v8.bak</code> before destructive work, and applies one atomic transaction; the v9 path creates <code>pi.sqlite.v9.bak</code>, while v7 first reaches v8 and then uses the same guarded path. The migration preserves sessions, transcripts, turns, permissions, and legacy approval data while adding/backfilling the artifact and execution fields and indexes on <code>plan_approvals</code>. It maps persisted <code>chat</code> values to <code>plan</code>, validates the shell setting, and writes <code>PRAGMA user_version = 10</code> last. Malformed app settings or scheduled config, invalid modes, and an invalid default shell fail closed with schema v8 authoritative. It does not reconstruct artifacts or queue work from transcript text.</p><h2 id="_8-selectable-shell-contract" tabindex="-1">8. Selectable shell contract <a class="header-anchor" href="#_8-selectable-shell-contract" aria-label="Permalink to &quot;8. Selectable shell contract&quot;">​</a></h2><p>Host-core returns a platform-aware catalog:</p><table tabindex="0"><thead><tr><th>Platform</th><th>IDs in catalog</th></tr></thead><tbody><tr><td>Windows</td><td><code>windows-powershell</code>, <code>windows-pwsh</code>, <code>cmd</code>, <code>git-bash</code></td></tr><tr><td>macOS/Linux</td><td><code>bash</code></td></tr></tbody></table><p>Each option contains its stable ID, display label, dialect, availability, and default marker. Settings writes reject unknown, unavailable, and wrong-platform IDs with <code>COMMAND_SHELL_INVALID</code>. If a persisted configured ID later becomes unavailable, the effective selection intentionally falls back to the first available shell for that platform and marks <code>fallback: true</code>. If no shell is available, Bash returns <code>SHELL_NOT_FOUND</code>.</p><p>The runtime pins the effective ID and dialect at turn launch. The Bash request includes the expected ID; host-core resolves the current catalog immediately before spawn and rejects a stale ID/dialect with <code>COMMAND_SHELL_CHANGED</code>. This is a catalog identity check, not executable path hashing. A fallback may select the effective shell before the turn is pinned, but execution never changes shell after the pin.</p><p><code>Bash</code> and <code>tools.execute</code> remain the protocol/tool names. Host streams stdout/stderr independently and returns a bounded final result. Missing <code>timeoutMs</code> means exactly 60,000 ms; an explicit value is accepted only in 1,000..300,000 ms. Timeout and user abort terminate the complete Unix process group or Windows process/job tree before streams close.</p><h2 id="_9-delivery-slices" tabindex="-1">9. Delivery slices <a class="header-anchor" href="#_9-delivery-slices" aria-label="Permalink to &quot;9. Delivery slices&quot;">​</a></h2><ol><li><strong>Contract freeze</strong>: ADR 0053/0054, D189/D190, baseline 0.4.14, protocol v9, schema v10, and E2E-104-E2E-117.</li><li><strong>Storage/host boundary</strong>: immutable artifact writer, <code>plan_approvals</code> fields/indexes, startup interruption transaction, queue transitions, scheduled rejection, and error mapping.</li><li><strong>Agent/RPC vertical slice</strong>: SubmitPlan, same-Agent approval, reject/expiry, idle/configuration guards, and no-replay restart recovery.</li><li><strong>Shell execution slice</strong>: catalog/default persistence, platform validation, effective fallback, turn pinning, stale-ID rejection, streamed output, timeout bounds, and process-tree cancellation.</li><li><strong>Desktop interaction</strong>: artifact opener approval card, status/expiry presentation, shell Settings, renderer reload behavior, localization, and diagnostics.</li><li><strong>Focused verification</strong>: migration, RPC, host authorization, immutable artifact bytes, expiry, restart fence, shell fallback, stale identity, streams, timeout, and abort checks. E2E runs remain opt-in.</li></ol><h2 id="_10-focused-verification-checklist" tabindex="-1">10. Focused verification checklist <a class="header-anchor" href="#_10-focused-verification-checklist" aria-label="Permalink to &quot;10. Focused verification checklist&quot;">​</a></h2><h3 id="plan-and-storage" tabindex="-1">Plan and storage <a class="header-anchor" href="#plan-and-storage" aria-label="Permalink to &quot;Plan and storage&quot;">​</a></h3><ul><li>one Agent identity across Agent, Plan, approval, queue, and execution;</li><li>unique artifact path per submission and byte-for-byte Markdown preservation;</li><li>title/question stored structurally and artifact opener resolves the file;</li><li>approve/reject-only schema and Ask default;</li><li>absolute expiry returns <code>PLAN_APPROVAL_TIMEOUT</code>;</li><li>one active turn, one pending approval, and one queued/running execution;</li><li>v8-to-v10 migration and rollback;</li><li>startup transaction interrupts prior pending/queued/running work before RPC;</li><li>no provider/tool/queue replay after restart;</li><li>pending interruption leaves Plan; approved interruption leaves Agent;</li><li>scheduled Plan rejection before provider/artifact/queue work.</li></ul><h3 id="shell-and-process" tabindex="-1">Shell and process <a class="header-anchor" href="#shell-and-process" aria-label="Permalink to &quot;Shell and process&quot;">​</a></h3><ul><li>exact platform catalog IDs and settings validation;</li><li>persisted unavailable ID falls back to the first available platform shell;</li><li>turn pins effective ID/dialect and stale identity fails closed;</li><li>Bash protocol name remains stable;</li><li>separate ordered stdout/stderr events and bounded final result;</li><li>exact 60-second default and 1-300 second override bounds;</li><li>timeout and user abort terminate the complete process tree.</li></ul><h2 id="_11-acceptance-evidence" tabindex="-1">11. Acceptance evidence <a class="header-anchor" href="#_11-acceptance-evidence" aria-label="Permalink to &quot;11. Acceptance evidence&quot;">​</a></h2><p>The E2E plan automates these M6 scenarios: E2E-104 migration, E2E-105 host Plan policy, E2E-106 immutable artifact approval, E2E-107 expiry, E2E-108 startup interruption, E2E-109 no replay/Agent retention, E2E-110 scheduled rejection, E2E-111 active/configuration boundaries, E2E-112 shell selection and fallback, E2E-113 stale shell identity, E2E-114 streaming, E2E-115 timeout, E2E-116 process abort, and E2E-117 UX/locales.</p><p>Acceptance on 2026-08-05 combined the host-core suite (139/139 passed, including 15 focused DB tests), 97 agent-runtime tests, desktop/shared/i18n suites, full JavaScript build/typecheck/lint, the long-timeout <code>test:e2e:plan</code> host workflow, and the raw-CDP <code>test:e2e:plan-ui</code> Electron workflow. The two host states that public RPC cannot manufacture safely — late approval expiry and a previously persisted shell becoming unavailable — are covered directly by deterministic Rust tests. The same-Host renderer evidence covers pending restore, same-lifetime terminal controls, stable Electron/Host identity, and rejected plus approved/completed terminal-card absence after renderer reload. E2E-108/E2E-109 cover Host restart interruption, stale-response rejection, and no replay.</p><h2 id="_12-explicit-non-goals" tabindex="-1">12. Explicit non-goals <a class="header-anchor" href="#_12-explicit-non-goals" aria-label="Permalink to &quot;12. Explicit non-goals&quot;">​</a></h2><ul><li>a second planner Agent/model/service;</li><li>request-changes feedback in the approval card;</li><li>renderer/sidecar writes or replacement of plan artifacts;</li><li>replaying any Plan work after host restart;</li><li>automatic approval for scheduled/background Plan;</li><li>arbitrary executable paths outside the host shell catalog;</li><li>separate PowerShell/cmd/Git Bash protocol tools;</li><li>executable path hashing as shell identity;</li><li>interactive PTY behavior for the Agent Bash tool.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("project/plan-mode-implementation-plan.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const planModeImplementationPlan = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  planModeImplementationPlan as default
};
