import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0256: Preserve DeepSeek reasoning across context compaction","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0256-deepseek-reasoning-across-compaction.md","filePath":"adr/0256-deepseek-reasoning-across-compaction.md","lastUpdated":1789458750000}');
const _sfc_main = { name: "adr/0256-deepseek-reasoning-across-compaction.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0256-preserve-deepseek-reasoning-across-context-compaction" tabindex="-1">ADR 0256: Preserve DeepSeek reasoning across context compaction <a class="header-anchor" href="#adr-0256-preserve-deepseek-reasoning-across-context-compaction" aria-label="Permalink to &quot;ADR 0256: Preserve DeepSeek reasoning across context compaction&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-15</li><li>Deciders: PI-Desktop core</li><li>Amends: D389 / ADR 0136 / ADR 0064</li><li>Fixes: #296</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>DeepSeek thinking mode requires every replayed Completions assistant message to carry a reasoning field (<code>reasoning_content</code>, or <code>reasoning_text</code> / <code>reasoning</code> on some relays). Official <code>deepseek.com</code> endpoints accept <code>&quot;&quot;</code> for turns that produced no thinking (D389 / #223). OpenCode and third-party relays for DeepSeek V4.1 Flash reject empty echoes with HTTP 400 (&quot;reasoning_text must be passed back&quot;).</p><p>Codex-shaped compaction (ADR 0064 / ADR 0136) drops assistant turns from the retained tail so tool-call pairs cannot strand and completed user prompts do not look like new tasks. That also drops thinking. Reloading a session rebuilds assistant content from <code>UiMessage.thinking</code> without a Completions <code>thinkingSignature</code>, so convertMessages silently omits the reasoning field and the #223 empty backfill fills <code>&quot;&quot;</code> — which strict relays reject.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>History rebuild stamps <code>thinkingSignature: &quot;reasoning_content&quot;</code> only for DeepSeek-compatible OpenAI Completions rows. Other APIs keep unsigned thinking so pi-ai preserves their existing plain-text fallback; their provider-native signatures are not interchangeable with Completions fields.</li><li>Checkpoints for models with <code>requiresReasoningContentOnAssistantMessages</code> store the last few thinking turns in opaque <code>details.retainedReasoning</code> (text + thinking only, no tool calls). Session context injects those turns between the compaction summary and the user tail only for the live DeepSeek-compatible Completions binding; other providers do not receive synthetic DeepSeek reasoning.</li><li>The pi-ai patch backfills the reasoning field already present on the history (<code>reasoning_content</code> | <code>reasoning_text</code> | <code>reasoning</code>). Official DeepSeek URLs keep empty-string fill. Non-official DeepSeek-family Completions rows set <code>requiresNonEmptyReasoningReplay</code> and receive the documented placeholder <code>[reasoning not retained for this turn]</code> instead of <code>&quot;&quot;</code>.</li><li>ADR 0136 retained-tail rules are unchanged: user-only tails, no tool-call replay.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Post-compaction DeepSeek requests on OpenCode / aggregators keep real or placeholder reasoning and no longer 400 on empty echoes.</li><li>Official DeepSeek and #223 empty-backfill behaviour remain available.</li><li>Checkpoint <code>details</code> grow by a bounded reasoning excerpt; the visible transcript and host schema do not change.</li><li>Unit tests cover convertMessages + compaction retained-reasoning wire shape. Live OpenCode / aggregator verification remains deferred (see E2E-005E).</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><ul><li>Degrade to non-thinking mode after the 400: avoids the echo requirement but loses reasoning for that turn and hides the compaction gap.</li><li>Put full assistant/tool messages back in <code>retainedTail</code>: preserves reasoning but reintroduces stranded tool calls and weakens the task boundary (rejected by ADR 0136).</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0256-deepseek-reasoning-across-compaction.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0256DeepseekReasoningAcrossCompaction = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0256DeepseekReasoningAcrossCompaction as default
};
