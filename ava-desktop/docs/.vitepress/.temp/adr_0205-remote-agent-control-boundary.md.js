import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0205: Remote Agent Control Uses a Dedicated Host Boundary","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0205-remote-agent-control-boundary.md","filePath":"adr/0205-remote-agent-control-boundary.md","lastUpdated":1789036160000}');
const _sfc_main = { name: "adr/0205-remote-agent-control-boundary.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0205-remote-agent-control-uses-a-dedicated-host-boundary" tabindex="-1">ADR 0205: Remote Agent Control Uses a Dedicated Host Boundary <a class="header-anchor" href="#adr-0205-remote-agent-control-uses-a-dedicated-host-boundary" aria-label="Permalink to &quot;ADR 0205: Remote Agent Control Uses a Dedicated Host Boundary&quot;">​</a></h1><ul><li>Status: Accepted for implementation (post-MVP); amended by D374 and D375</li><li>Date: 2026-09-09</li><li>Decision: D373 (amended by D374 and D375)</li><li>Related: ADR 0004, ADR 0011, ADR 0203, ADR 0165, <code>02-architecture/05-remote-agent-control.md</code>, <code>03-runtime/19-remote-agent-control-protocol.md</code>, <code>05-security/02-remote-control-security.md</code></li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>PI-Desktop currently embeds <code>pi-agent-core</code> in a Node sidecar. Electron Main owns the sidecar and Rust host-core bridges, while the renderer communicates through typed Electron IPC. The local sidecar and host-core boundaries use stdio NDJSON JSON-RPC. ADR 0203 adds an opt-in, loopback-only MCP control plane for reviewed local desktop automation.</p><p>Remote control introduces a different trust and lifecycle problem. A remote UI must be able to disconnect while a turn continues, reconnect without missing events, answer host-owned approvals, and share a session with other clients. Exposing the existing IPC, <code>host.proxy</code>, or host-core JSON-RPC would bypass the current authority boundaries and make the Electron process a public backend.</p><p>The GitHub implementations reviewed for this decision converge on a layered model: OpenAI Codex and Microsoft VS Code use JSON-RPC-shaped interactive host protocols; MCP separates local stdio from remote HTTP streaming; Google A2A separates a canonical operation model from JSON-RPC, gRPC, and HTTP/JSON bindings.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>Keep Electron Main, the Node pi sidecar, and Rust host-core as the local authority path. Rust host-core continues to bind no network port.</li><li>Define a logical <strong>Agent Host</strong> boundary that owns sessions, turns, event sequences, approvals, attachments, workspace policy, and crash recovery.</li><li>Define the <strong>PI Remote Agent Control Protocol (RACP) v1</strong> as a transport-neutral semantic contract.</li><li>Use JSON-RPC over WSS as the primary interactive remote binding, HTTP/JSON plus SSE as the browser binding, and gRPC over TLS as the native and Gateway-to-Host binding. These bindings MUST share the same state machine, cursor, idempotency, authorization, and error semantics.</li><li>Put authentication, user authorization, routing, rate limiting, revocation, and audit at a separate Remote Gateway in production. The Agent Host opens the Gateway connection outbound so a desktop does not require a public inbound port.</li><li>During migration, Electron Main may host a development-only facade above existing handlers. Production deployment targets a standalone Agent Host process group beside the workspace; the wire contract does not change.</li><li>Keep remote control post-MVP. ADR 0004 remains true for the current MVP; this ADR is the dedicated follow-up required by its Follow-up section.</li><li>Do not reuse the withdrawn subagent A2A/Peer coordination mechanism. A2A is a design reference for binding separation only.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><h3 id="positive" tabindex="-1">Positive <a class="header-anchor" href="#positive" aria-label="Permalink to &quot;Positive&quot;">​</a></h3><ul><li>Local security boundaries remain intact.</li><li>A running Agent is independent of any one UI connection.</li><li>WebSocket, browser HTTP/SSE, and gRPC clients can share one semantic API.</li><li>Event cursors, snapshots, idempotency, and approval lifecycles are explicit.</li><li>A Gateway can route multiple Hosts without owning workspace secrets or durable transcript truth.</li><li>Go, Rust, Node, and browser clients can use the binding most natural to their deployment without creating separate product behavior.</li></ul><h3 id="costs-and-risks" tabindex="-1">Costs and risks <a class="header-anchor" href="#costs-and-risks" aria-label="Permalink to &quot;Costs and risks&quot;">​</a></h3><ul><li>A Host/Gateway identity and revocation system is required.</li><li>Event replay and snapshot retention add storage and testing complexity.</li><li>Long-lived WebSocket/gRPC connections require load-balancer and backpressure design.</li><li>A canonical schema and generated binding conformance suite become release surfaces.</li><li>The standalone Agent Host extraction is a migration project, not a small transport switch.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><h3 id="expose-the-current-json-rpc-stdio-endpoint" tabindex="-1">Expose the current JSON-RPC stdio endpoint <a class="header-anchor" href="#expose-the-current-json-rpc-stdio-endpoint" aria-label="Permalink to &quot;Expose the current JSON-RPC stdio endpoint&quot;">​</a></h3><p>Rejected. Stdio is a local process boundary, has no remote identity model, and would expose host internals rather than a reviewed control surface.</p><h3 id="use-grpc-for-every-link" tabindex="-1">Use gRPC for every link <a class="header-anchor" href="#use-grpc-for-every-link" aria-label="Permalink to &quot;Use gRPC for every link&quot;">​</a></h3><p>Rejected. gRPC is appropriate for typed service-to-service communication, but an interactive Agent also needs browser compatibility, server-initiated approval/input requests, and easy transport debugging. gRPC remains a required binding, not the only binding.</p><h3 id="use-json-rpc-over-http-only" tabindex="-1">Use JSON-RPC over HTTP only <a class="header-anchor" href="#use-json-rpc-over-http-only" aria-label="Permalink to &quot;Use JSON-RPC over HTTP only&quot;">​</a></h3><p>Rejected as the complete design. It is simple for commands, but a full remote control session needs an efficient bidirectional path for approvals, input, events, and cancellation. HTTP/SSE remains the browser binding with explicit response endpoints.</p><h3 id="expose-electron-main-directly-on-a-public-port" tabindex="-1">Expose Electron Main directly on a public port <a class="header-anchor" href="#expose-electron-main-directly-on-a-public-port" aria-label="Permalink to &quot;Expose Electron Main directly on a public port&quot;">​</a></h3><p>Rejected. It expands the desktop attack surface, couples public availability to the UI process, and gives a network-facing process too much local lifecycle authority.</p><h3 id="extend-local-mcp-into-a-public-remote-api" tabindex="-1">Extend local MCP into a public remote API <a class="header-anchor" href="#extend-local-mcp-into-a-public-remote-api" aria-label="Permalink to &quot;Extend local MCP into a public remote API&quot;">​</a></h3><p>Rejected. MCP remains a local, reviewed desktop automation surface. RACP needs durable Sessions, Turns, cursors, multi-client roles, Gateway identity, and binding parity that are outside the current MCP contract.</p><h3 id="restore-the-historical-a2a-peer-stack" tabindex="-1">Restore the historical A2A/Peer stack <a class="header-anchor" href="#restore-the-historical-a2a-peer-stack" aria-label="Permalink to &quot;Restore the historical A2A/Peer stack&quot;">​</a></h3><p>Rejected. ADR 0165 removed it because subagent coordination was not a product requirement. The remote control protocol is client-to-host control, not subagent messaging.</p><h2 id="amendment-d374" tabindex="-1">Amendment (D374) <a class="header-anchor" href="#amendment-d374" aria-label="Permalink to &quot;Amendment (D374)&quot;">​</a></h2><p>Date: 2026-09-10. A pre-implementation review found that the D373 draft was right about its layering but did not match the desktop it has to wrap, and required more than v1 can carry. The following changes apply to decisions 3, 4, and 6 above and to the four remote specifications.</p><ol><li><strong>One normative v1 binding.</strong> <code>RACP-WS</code> is the only normative binding in v1. <code>RACP-HTTP</code> is the browser profile of the same contract and must ship before any browser client. <code>RACP-GRPC</code> is reserved, not required, and is generated from the contract source if it is ever adopted. Decision 4&#39;s &quot;MUST share&quot; rule applies to every shipped binding.</li><li><strong>One IDL.</strong> The RACP resources are authored as typebox schemas in <code>packages/shared</code> (frozen decision 28). JSON Schema fixtures, documentation tables, and any Protobuf file are generated from them. The draft&#39;s &quot;JSON normative, proto generated&quot; direction is withdrawn.</li><li><strong>Headless Agent Host module first.</strong> Decision 6&#39;s Electron facade is replaced by <code>packages/agent-host</code>, a module with no Electron dependency that owns session and turn admission, the per-session turn queue, the approval broker, the in-memory event log, and the snapshot builder. Desktop IPC, local MCP, and RACP are three callers of that module, so the standalone Host extraction moves a module rather than re-splitting Main.</li><li><strong>Full local decision vocabulary.</strong> Remote approvals offer <code>allow-once</code>, <code>allow-session</code>, and <code>deny</code> for tools and <code>approve</code> with an explicit permission mode, or <code>reject</code>, for Plan and Goal contracts; input requests carry the asktool question and skip semantics. A Plan or Goal approval is a session-level transition that outlives the submitting turn.</li><li><strong>Cursors carry an epoch; deltas are ephemeral.</strong> The cursor is <code>{ epoch, sequence }</code>. Streaming deltas, tool progress, and activity phases are delivered live, never sequenced, never replayed, and never counted against the replay window; snapshots carry active items instead. The first event log lives in Agent Host memory and a restart starts a new epoch; moving it into host-core would need its own ADR.</li><li><strong>Host-owned turn queue.</strong> <code>turn/start</code> admits immediately or into a bounded Host queue that every client, including the local desktop, sees. The renderer&#39;s in-memory prompt queue is replaced before more than one client can control a session. <code>turn/stop</code> (graceful) and <code>turn/interrupt</code> (abort) are separate operations.</li><li><strong>Pending requests are Host state.</strong> Rust host-core exposes <code>permissions.pending</code> so a late-attaching client receives open requests, and a remote decision closes the local desktop card.</li><li><strong>Remote permission ceiling and approval lifetime.</strong> A remote-initiated turn runs under the lower of the session mode and a Host-configured ceiling that defaults to <code>ask</code>. The local 120-second deny timeout stays the default; a Host with remote control enabled may configure a longer bounded lifetime for approvals raised while a remote subscriber is attached.</li><li><strong>Host link relay profile.</strong> The Gateway-to-Host connection multiplexes logical client connections so server-initiated approval requests and attachment bytes reach the right endpoint without an inbound port; the Gateway buffers upload bytes only until the Host confirms them.</li><li><strong>Browser authentication profiles.</strong> Browsers cannot set headers on the WebSocket and EventSource APIs, so the cookie profile (HttpOnly cookie, Origin allowlist, CSRF token) is the browser path and the header profile is the non-browser path; URL tokens remain forbidden in both.</li><li><strong>Identity source and tenancy.</strong> The Gateway may validate either an OIDC/OAuth 2.0 provider or first-party product account tokens; the choice is recorded when rollout R3 starts (superseded by D375 item 10, which fixes the PI account service). The first deployment is single-tenant; <code>tenantId</code> stays in every route and cross-tenant tests run once a multi-tenant harness exists.</li><li><strong>Catalog additions.</strong> <code>host/list</code>, <code>project/list</code>, <code>session/history</code>, host-scope event subscriptions, and <code>turn/cancel</code> are added; deferred local operations are listed by name so no binding invents a substitute.</li></ol><h2 id="amendment-d375" tabindex="-1">Amendment (D375) <a class="header-anchor" href="#amendment-d375" aria-label="Permalink to &quot;Amendment (D375)&quot;">​</a></h2><p>Date: 2026-09-10. Recorded demand, not transport breadth, now orders the milestones. Issues #176 and #140 ask to operate a project on a remote Linux or WSL machine from the local desktop; issue #100 asks for task and approval notifications on messaging channels with simple commands back; no recorded request asks for a browser or phone client of the desktop. The design-gate answers below were chosen by the maintainer the same day.</p><ol><li><strong>First remote topology: the desktop as Remote Client of a <code>pi-host</code> over an SSH tunnel.</strong> The <code>pi-host</code> bundle packages the headless module, the Node sidecar, and the platform&#39;s host-core binary at the desktop&#39;s version. A bootstrap script uploaded over the user&#39;s own SSH session downloads it from GitHub Releases, verifies the published SHA-256, starts it bound to loopback, and pairs it with the desktop. It is reached through an SSH port forward on the <code>RACP-WS</code> header profile; plain <code>ws://</code> is accepted only when bind and peer are loopback and a device token is presented. A machine without outbound access to GitHub is not supported in the first version.</li><li><strong>Desktop RACP client adapter.</strong> Electron Main presents a remote Host to the renderer through the existing <code>lib/api.ts</code> surface; the renderer stays transport-agnostic and hides uncovered features by capability.</li><li><strong>Remote-host profile (RACP v1.1).</strong> <code>session/configure</code>, <code>session/fork</code>, <code>session/rename</code>, <code>session/delete</code>, <code>session/compact</code>, <code>workspace/list</code>, <code>workspace/read</code>, <code>workspace/diff</code>, and the <code>terminal/open</code>, <code>terminal/input</code>, <code>terminal/resize</code>, <code>terminal/close</code> operations join the catalog, so mode, model, the work panel&#39;s files and diff, and a terminal on the remote machine work against a remote session.</li><li><strong>Reverse tool relay in the same milestone.</strong> The paired desktop advertises its user-configured MCP servers and workspace-free plugin tools with <code>tools/advertise</code>; the Host merges them into the remote session&#39;s catalog and executes them through the <code>tool/execute</code> server request on the desktop, under the desktop&#39;s own plugin permissions, after the Host&#39;s permission decision. Plugin tools that require workspace or filesystem access are excluded. R2 ships as one milestone.</li><li><strong>Remote session ownership split.</strong> Transcript, tools, workspace, permissions, provider secrets, <code>~/.agents</code> definitions, MCP servers configured on the Host, and scheduled tasks live on the remote Host. Provider configuration for the remote Host is written over the SSH bootstrap channel, never through RACP.</li><li><strong>Ceiling exemption as Host policy.</strong> A desktop device paired through the SSH bootstrap holds <code>owner</code> and is exempt from the remote permission ceiling by default, because SSH access already exceeds anything the ceiling withholds; the Host policy <code>applyCeilingToPairedDevices</code> re-applies it.</li><li><strong>Persisted turn queue.</strong> Queued turns and their idempotency keys are persisted by Rust host-core, restored in order after a restart, and held until a controller attaches. The schema bump is recorded by its own ADR when R1 starts.</li><li><strong>Remote approval lifetime.</strong> While a remote subscriber is attached the default approval lifetime is 30 minutes, operator-adjustable within a bound; the local 120-second default is unchanged.</li><li><strong>Second scheduled milestone: outbound messaging integration.</strong> An adapter beside the Host relays redacted event summaries to a webhook first, then Telegram and Slack through outbound channels, and maps a fixed command vocabulary to turn and approval operations under the linked principal&#39;s roles. It opens no listener and never blocks a turn.</li><li><strong>Gateway identity source.</strong> When the Gateway is scheduled, it validates first-party tokens of the PI account service specified in the pi-backend repository; OIDC federation is not planned.</li><li><strong>Unscheduled.</strong> The Gateway with its Host link, the browser profile with its cookie authentication, and the reserved gRPC binding keep their specifications and are scheduled only by a later product decision.</li><li><strong>Acceptance.</strong> E2E-231 and E2E-232 are the acceptance targets of the scheduled milestones; E2E-227 and E2E-228 apply when their milestones are scheduled.</li></ol><h2 id="amendment-d385" tabindex="-1">Amendment (D385) <a class="header-anchor" href="#amendment-d385" aria-label="Permalink to &quot;Amendment (D385)&quot;">​</a></h2><p>Date: 2026-09-10. The maintainer requires remote control to be user-local by construction: no project-operated identity, account, or relay service may be in the path, and a user&#39;s client must never authenticate through a service the project runs.</p><ol><li><strong>No first-party identity.</strong> D375 item 10 is withdrawn. The only credential a client holds is a device token issued by the user&#39;s own Host at pairing. OIDC federation and the pi-backend account service are out of scope for remote control.</li><li><strong>Gateway only as a self-hosted relay.</strong> PI does not operate a Gateway. If the Gateway topology is ever scheduled, the user runs it on their own infrastructure and it admits clients with Host-issued device credentials; its route context carries the Host id, not a tenant of the project&#39;s.</li><li><strong>Outbound connections are the user&#39;s own.</strong> The Host connects only to the user&#39;s SSH hosts, the messaging channels the user configured with their own bot tokens or webhooks, the model providers the user configured, and the read-only, checksum-verified GitHub Releases download of <code>pi-host</code>.</li><li><strong>Unchanged.</strong> The SSH-tunnel topology, device pairing, the messaging integration, and every RACP shape already satisfy this rule; the specifications change wording, not structure.</li></ol></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0205-remote-agent-control-boundary.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0205RemoteAgentControlBoundary = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0205RemoteAgentControlBoundary as default
};
