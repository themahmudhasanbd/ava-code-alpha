import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0053: Plan checkpoint artifact, approval, and execution epoch","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0053-plan-checkpoint-artifact-and-execution-epoch.md","filePath":"adr/0053-plan-checkpoint-artifact-and-execution-epoch.md","lastUpdated":1786032712000}');
const _sfc_main = { name: "adr/0053-plan-checkpoint-artifact-and-execution-epoch.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0053-plan-checkpoint-artifact-approval-and-execution-epoch" tabindex="-1">ADR 0053: Plan checkpoint artifact, approval, and execution epoch <a class="header-anchor" href="#adr-0053-plan-checkpoint-artifact-approval-and-execution-epoch" aria-label="Permalink to &quot;ADR 0053: Plan checkpoint artifact, approval, and execution epoch&quot;">​</a></h1><ul><li>Status: Accepted for implementation</li><li>Date: 2026-07-31</li><li>Supersedes: ADR 0052 and D188</li><li>Baseline: <code>0.4.14</code></li><li>Protocol: v9</li><li>Storage schema: v10</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The previous Plan contract used an in-memory approval waiter around a structured proposal and allowed request-changes feedback. It did not give the user one exact, inspectable plan artifact or define how an approved execution could be interrupted without replay. Plan approval must be durable enough for renderer reload, but a host restart must never replay work created by an older host process.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><h3 id="_1-one-agent-and-one-plan-submission-tool" tabindex="-1">1. One Agent and one Plan submission tool <a class="header-anchor" href="#_1-one-agent-and-one-plan-submission-tool" aria-label="Permalink to &quot;1. One Agent and one Plan submission tool&quot;">​</a></h3><p>The product selector remains <code>Agent | Plan</code>, with Agent as the default and one pi Agent per session. Plan is the same Agent in planning state. Plan exposes <code>Read</code>, <code>Glob</code>, <code>Grep</code>, <code>BrowserPreview</code>, <code>Bash</code>, <code>EnterPlanMode</code>, and <code>SubmitPlan</code> (it also exposed <code>CompactContext</code> until ADR 0061 removed that tool, which ADR 0064 restores as <code>new_context</code>); Write, Edit, plugin tools, and unknown tools remain host-denied. <code>SubmitPlan</code> is the only assistant tool call in its batch and is valid only for the active Plan turn.</p><p>The input is exactly:</p><div class="language-ts vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">type</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> SubmitPlanInput</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  title</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  markdown</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  question</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">};</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>There is no <code>ExitPlanMode</code>, structured step schema, <code>proposedCommands</code> field, or <code>request_changes</code> action. A revision is a new <code>SubmitPlan</code> after the current proposal is rejected or expires.</p><h3 id="_2-immutable-host-written-plan-artifacts" tabindex="-1">2. Immutable host-written plan artifacts <a class="header-anchor" href="#_2-immutable-host-written-plan-artifacts" aria-label="Permalink to &quot;2. Immutable host-written plan artifacts&quot;">​</a></h3><p>For a project-bound session, host-core writes the submitted Markdown bytes unchanged to a new artifact under:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&lt;workspaceRoot&gt;/.pi/plan/&lt;unique-name&gt;.md</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>The directory and filename are host-owned. Each submission gets a unique file; an accepted submission never replaces an earlier artifact. The input <code>title</code> and <code>question</code> remain structured fields in <code>plan_approvals</code>; host-core does not prepend a title, append a question section, normalize line endings, or add any other wrapper to <code>markdown</code>.</p><p>Host-core validates the session root, creates the file without replacement, flushes the exact bytes, and records the workspace-relative artifact path, SHA-256, and byte size in the same approval record. A path escape, symlink ambiguity, or write failure creates no approval or execution descriptor.</p><h3 id="_3-approval-and-permission-selection" tabindex="-1">3. Approval and permission selection <a class="header-anchor" href="#_3-approval-and-permission-selection" aria-label="Permalink to &quot;3. Approval and permission selection&quot;">​</a></h3><p>The approval card displays the structured title and question, an opener for the host-created artifact, the absolute expiry, and the current status. Opening the artifact reads the immutable Markdown file; the card does not need to inline the Markdown or display its hash or byte size. It has only <strong>Approve</strong> and <strong>Reject</strong> actions. Approve requires an explicit execution permission mode: <code>ask</code>, <code>accept-edits</code>, or <code>auto</code>; the UI selects <code>ask</code> by default. Reject never selects or grants an execution mode. There is no feedback field and no implicit approval from a timeout, reload, scheduled task, or stale renderer.</p><p>The approval deadline is an absolute 30-minute deadline from host creation. Renderer reload preserves the original deadline while the host remains alive. Expiry is recorded with the canonical error <code>PLAN_APPROVAL_TIMEOUT</code>; the deadline never extends when the card is reopened.</p><h3 id="_4-process-epoch-fence-and-recovery" tabindex="-1">4. Process-epoch fence and recovery <a class="header-anchor" href="#_4-process-epoch-fence-and-recovery" aria-label="Permalink to &quot;4. Process-epoch fence and recovery&quot;">​</a></h3><p>The host process has an internal boot epoch, but it is not serialized in the database and is not a protocol field. The single <code>plan_approvals</code> row carries both approval status and execution fields:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>status: pending -&gt; approved | rejected | expired | interrupted</span></span>
<span class="line"><span>execution_state: queued -&gt; running -&gt; completed | interrupted</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>Approval atomically changes the session to Agent with the selected permission mode, records <code>execution_id</code>, and sets <code>execution_state = queued</code>; the same Agent then starts the execution turn. Only one Plan approval may be pending and only one execution may be queued or running for a session.</p><p>Before serving any RPC after startup, host-core runs one transaction that marks all prior <code>pending</code> approvals as <code>interrupted</code> and all prior <code>queued</code> or <code>running</code> execution states as <code>interrupted</code>; associated running turns are aborted. No approval, queue entry, provider call, or tool execution is replayed. A pending interruption leaves the session in Plan. If approval already committed the session to Agent, interrupting its queued or running execution leaves the session in Agent; the user may start a new turn without automatic Plan re-entry.</p><h3 id="_5-turn-and-configuration-boundaries" tabindex="-1">5. Turn and configuration boundaries <a class="header-anchor" href="#_5-turn-and-configuration-boundaries" aria-label="Permalink to &quot;5. Turn and configuration boundaries&quot;">​</a></h3><p>Each session has one active turn. A second prompt, second Plan submission, mode/provider/model/permission/shell configuration change, or second Plan run is rejected while that turn or Plan run is active. Session configuration is accepted only while idle. Approval actions are the only controls enabled for a pending Plan request. Cross-session work keeps the existing session-scoped workspace and event isolation.</p><h3 id="_6-scheduled-plan-policy" tabindex="-1">6. Scheduled Plan policy <a class="header-anchor" href="#_6-scheduled-plan-policy" aria-label="Permalink to &quot;6. Scheduled Plan policy&quot;">​</a></h3><p>Scheduled or unattended Plan execution is rejected before provider work, artifact creation, approval, or queue insertion with <code>PLAN_REQUIRES_INTERACTIVE_SESSION</code>. A scheduled task must be explicitly changed to Agent before it can run unattended.</p><h3 id="_7-versioned-contracts" tabindex="-1">7. Versioned contracts <a class="header-anchor" href="#_7-versioned-contracts" aria-label="Permalink to &quot;7. Versioned contracts&quot;">​</a></h3><p>Protocol v9 carries <code>SubmitPlan</code>, the unique artifact path and metadata, approve/reject responses, absolute expiry, execution state, shell selection, and streamed command output. Storage schema v10 continues the single <code>plan_approvals</code> checkpoint table with structured title/question, artifact fields, execution ID/state, and the persisted default shell setting while preserving existing transcript and session data. The v8-to-v10 migration is transactional; <code>PRAGMA user_version = 10</code> is written last.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><h3 id="positive" tabindex="-1">Positive <a class="header-anchor" href="#positive" aria-label="Permalink to &quot;Positive&quot;">​</a></h3><ul><li>Every submitted proposal has its own host-owned file with verifiable bytes.</li><li>Renderer reload is recoverable without making a host restart replay work.</li><li>The Agent/Plan boundary and post-approval Agent state are explicit.</li><li>Shell choice, output, timeout, and cancellation can share the same host audit and turn boundaries.</li></ul><h3 id="tradeoffs" tabindex="-1">Tradeoffs <a class="header-anchor" href="#tradeoffs" aria-label="Permalink to &quot;Tradeoffs&quot;">​</a></h3><ul><li>Rejecting a plan is terminal; revisions require another model turn and a new artifact.</li><li>A host restart interrupts even an approved queued or running execution.</li><li>Plan artifacts accumulate under <code>.pi/plan/</code>; they are immutable and are not a renderer-owned draft store.</li></ul><h2 id="alternatives-rejected" tabindex="-1">Alternatives rejected <a class="header-anchor" href="#alternatives-rejected" aria-label="Permalink to &quot;Alternatives rejected&quot;">​</a></h2><h3 id="request-changes-approval" tabindex="-1">Request-changes approval <a class="header-anchor" href="#request-changes-approval" aria-label="Permalink to &quot;Request-changes approval&quot;">​</a></h3><p>Rejected for this checkpoint because it mixed plan editing with the approval boundary. Revisions are new submissions with new artifact hashes.</p><h3 id="replay-a-durable-queue-after-host-restart" tabindex="-1">Replay a durable queue after host restart <a class="header-anchor" href="#replay-a-durable-queue-after-host-restart" aria-label="Permalink to &quot;Replay a durable queue after host restart&quot;">​</a></h3><p>Rejected because a persisted execution request can outlive the process state, tool identity, shell identity, and user intent that created it. Restart recovery is interruption-only.</p><h3 id="renderer-owned-or-sidecar-written-plan-files" tabindex="-1">Renderer-owned or sidecar-written plan files <a class="header-anchor" href="#renderer-owned-or-sidecar-written-plan-files" aria-label="Permalink to &quot;Renderer-owned or sidecar-written plan files&quot;">​</a></h3><p>Rejected because the host must own the workspace write, path validation, hash, size, and approval identity.</p><h2 id="related-docs" tabindex="-1">Related docs <a class="header-anchor" href="#related-docs" aria-label="Permalink to &quot;Related docs&quot;">​</a></h2><ul><li><code>docs/adr/0054-selectable-command-shell-catalog.md</code></li><li><code>docs/spec/00-baseline.md</code></li><li><code>docs/spec/03-runtime/01-ipc-protocol.md</code></li><li><code>docs/spec/03-runtime/02-agent-runtime.md</code></li><li><code>docs/spec/03-runtime/03-tools-and-permissions.md</code></li><li><code>docs/spec/03-runtime/04-data-storage.md</code></li><li><code>docs/spec/03-runtime/05-host-core-rust.md</code></li><li><code>docs/spec/03-runtime/06-host-rpc-protocol.md</code></li><li><code>docs/spec/03-runtime/07-process-model.md</code></li><li><code>docs/spec/03-runtime/08-error-codes.md</code></li><li><code>docs/spec/03-runtime/10-session-state-machine.md</code></li><li><code>docs/spec/04-ux/03-permission-ux.md</code></li><li><code>docs/spec/04-ux/06-settings-ia.md</code></li><li><code>docs/spec/04-ux/08-component-spec.md</code></li><li><code>docs/spec/04-ux/09-interaction-patterns.md</code></li><li><code>docs/spec/05-security/01-security.md</code></li><li><code>docs/spec/06-delivery/02-acceptance-criteria.md</code></li><li><code>docs/spec/06-delivery/04-e2e-test-plan.md</code></li><li><code>docs/spec/08-meta/decisions-log.md</code> (D189)</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0053-plan-checkpoint-artifact-and-execution-epoch.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0053PlanCheckpointArtifactAndExecutionEpoch = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0053PlanCheckpointArtifactAndExecutionEpoch as default
};
