import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0241: Ship the file view as a vendored, updatable plugin","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0241-vendored-updatable-file-view-plugin.md","filePath":"adr/0241-vendored-updatable-file-view-plugin.md","lastUpdated":1789320566000}');
const _sfc_main = { name: "adr/0241-vendored-updatable-file-view-plugin.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0241-ship-the-file-view-as-a-vendored-updatable-plugin" tabindex="-1">ADR 0241: Ship the file view as a vendored, updatable plugin <a class="header-anchor" href="#adr-0241-ship-the-file-view-as-a-vendored-updatable-plugin" aria-label="Permalink to &quot;ADR 0241: Ship the file view as a vendored, updatable plugin&quot;">​</a></h1><ul><li>Status: Accepted (supersedes ADR 0105; issue #304)</li><li>Date: 2026-09-13</li><li>Deciders: PI-Desktop core</li><li>Related: <a href="./0104-plugin-contributed-work-panel-views">ADR 0104</a> · <a href="./0105-files-as-a-bundled-plugin">ADR 0105</a> · <a href="./0109-open-files-with-the-os-associated-application">ADR 0109</a> · <a href="./0111-reveal-files-in-file-manager">ADR 0111</a> · <a href="./0169-classified-file-preview-and-live-workspace-events">ADR 0169</a> · <a href="./../spec/07-plugins/07-plugin-marketplace">07-plugins/07-plugin-marketplace</a></li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The bundled <code>pi.files</code> view (ADR 0105) browsed and previewed the workspace and nothing else: no editing, no image, CSV, JSON, or SQLite surface, and a line cap on text preview. Anything more than looking at a file meant leaving the app.</p><p><code>pi.file-manager</code> is a third-party plugin that covers exactly that surface on the same public channel — <code>contributes.views</code>, a sandboxed page over <code>pluginBridge</code>, <code>ui.view</code> plus <code>fs.read</code> with root <code>workspace</code> and scope <code>**</code>, no host-only capability. It is also published through the official marketplace.</p><p>Shipping it raises two questions this ADR answers: how the editor&#39;s writes are mediated, and how a copy inside the application stays consistent with the copy in the marketplace.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>The bundled file view is <code>pi.file-manager</code>, vendored into <code>apps/desktop/resources/plugins/</code>. The vendored files are the upstream release&#39;s own artifacts, byte for byte, except for a <code>license</code> field added to the manifest; <code>UPSTREAM.md</code> records the repository, tag, commit, and checksums. <code>pi.files</code> is removed, and a build that stops shipping a bundled plugin leaves no orphan registry row (ADR 0104).</li><li>It declares only <code>ui.view</code> and <code>fs.read</code> (<code>root: workspace</code>, <code>scope: [&quot;**&quot;]</code>) — the same permissions the view it replaces declared. The page is sandboxed and reaches the host only through the public bridge.</li><li>Editing writes are the plugin&#39;s own: its host process keeps the path jail, atomic writes, <code>mtime</code>/size conflict detection, and the write audit. The host permission gateway does not mediate them. A manifest cannot express a whole-tree write, and this host does not sandbox a plugin process&#39;s <code>fs</code> access to begin with, so bundling the plugin grants no capability a third-party plugin does not already have. The boundary is stated in the manifest&#39;s <code>safetyNotes</code> and is part of why this record exists.</li><li><strong>Bundled means default and non-removable, not frozen.</strong> A bundled plugin cannot be uninstalled, but it can be updated from the marketplace, and that update survives the next launch: <ul><li><code>sync_builtin</code> keeps an installed row whose source is not <code>builtin</code> for as long as this build does not ship a <em>strictly newer</em> version. An app update still reaches a user who never installed anything, and a stale install cannot pin the plugin.</li><li><code>uninstall</code> refuses by id against the set of plugins this build ships, rebuilt from disk on every launch. An updated bundled plugin stays uninstallable, and a plugin this build stops shipping becomes removable again.</li></ul></li><li>A catalog version is offered as an update only when it is strictly newer than what is installed. Equality is not an update, and an older catalog entry is not one either — it would be a downgrade wearing the update affordance.</li><li>The panel title is the manifest&#39;s localized title, as for any plugin. The renderer does not carry a label for it, and no view id is hardcoded.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>The work panel&#39;s file surface is a vendor copy, so the application&#39;s own release cycle no longer decides when the view improves; upstream releases do, and users can take them without waiting for an app update.</li><li>Maintaining the copy is a deliberate act: <code>UPSTREAM.md</code> and <code>apps/desktop/test/bundled-plugins.test.mjs</code> pin the vendored release, so a silent edit or a half-done re-sync fails the build.</li><li>&quot;Cannot be uninstalled&quot; is now separate from &quot;came from <code>resources/plugins</code>&quot;. <code>PluginSummary.bundled</code> carries that distinction; it is recomputed on every launch and defaults to false in registries written before it existed.</li><li>The marketplace keeps listing a bundled plugin and offers its updates. That entry is the supported path for a newer version, not a confusing duplicate.</li><li>A plugin the user updated keeps a marketplace/<code>local</code> source while remaining bundled. Anything that reasons about <code>source == &quot;builtin&quot;</code> must not be used to answer &quot;is this protected&quot; — see the registry rules above.</li><li>The bundled view&#39;s filesystem reach is the plugin&#39;s own; a defect in the plugin&#39;s path jail is not caught by the host gateway. This is the same trust position as any installed plugin, taken deliberately in exchange for a capable, updatable file view.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><h3 id="reimplement-the-missing-viewers-and-editing-in-pi-files" tabindex="-1">Reimplement the missing viewers and editing in <code>pi.files</code> <a class="header-anchor" href="#reimplement-the-missing-viewers-and-editing-in-pi-files" aria-label="Permalink to &quot;Reimplement the missing viewers and editing in \`pi.files\`&quot;">​</a></h3><p>Rejected: it duplicates a maintained third-party plugin, and the line cap, media viewers, structured viewers, and SQLite browsing are a large surface to keep in the host. The public channel exists so first-party surfaces do not have to live there.</p><h3 id="bundle-it-read-only-and-leave-editing-to-the-marketplace-copy" tabindex="-1">Bundle it read-only and leave editing to the marketplace copy <a class="header-anchor" href="#bundle-it-read-only-and-leave-editing-to-the-marketplace-copy" aria-label="Permalink to &quot;Bundle it read-only and leave editing to the marketplace copy&quot;">​</a></h3><p>Rejected: the same plugin cannot offer two capability levels without forking it, and a read-only default would leave the most common request — change this line — unanswered. The write path is unchanged either way.</p><h3 id="let-the-bundled-copy-always-win-with-no-updates" tabindex="-1">Let the bundled copy always win, with no updates <a class="header-anchor" href="#let-the-bundled-copy-always-win-with-no-updates" aria-label="Permalink to &quot;Let the bundled copy always win, with no updates&quot;">​</a></h3><p>Rejected: a user could then only get a newer upstream release by updating the application, which defeats the point of vendoring a published plugin. The strictly-newer rule keeps both directions honest.</p><h3 id="hide-the-marketplace-entry-for-a-bundled-id" tabindex="-1">Hide the marketplace entry for a bundled id <a class="header-anchor" href="#hide-the-marketplace-entry-for-a-bundled-id" aria-label="Permalink to &quot;Hide the marketplace entry for a bundled id&quot;">​</a></h3><p>Rejected: the entry is where a newer upstream release is published. Hiding it would remove the update path this ADR establishes.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0241-vendored-updatable-file-view-plugin.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0241VendoredUpdatableFileViewPlugin = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0241VendoredUpdatableFileViewPlugin as default
};
