import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0177: User-configurable outbound proxy","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0177-user-configurable-outbound-proxy.md","filePath":"adr/0177-user-configurable-outbound-proxy.md","lastUpdated":1789575730000}');
const _sfc_main = { name: "adr/0177-user-configurable-outbound-proxy.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0177-user-configurable-outbound-proxy" tabindex="-1">ADR 0177: User-configurable outbound proxy <a class="header-anchor" href="#adr-0177-user-configurable-outbound-proxy" aria-label="Permalink to &quot;ADR 0177: User-configurable outbound proxy&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-08</li><li>Deciders: PI-Desktop core</li><li>Related: D340, ADR 0083, ADR 0096, <code>04-ux/06-settings-ia.md</code>, <code>03-runtime/07-process-model.md</code></li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Outbound HTTP is split across processes:</p><ul><li>Electron main — model discovery, models.dev refresh, plugin <code>net.fetch</code>, OAuth polling, GitHub-adjacent fetches</li><li>Agent sidecar (Node <code>fetch</code> / pi-ai) — LLM provider calls</li><li>host-core — marketplace catalog and package <code>curl</code></li><li>Chromium sessions — default session, <code>persist:work-browser</code>, plugin panels</li><li>electron-updater — Chromium / Electron net</li></ul><p>None of those surfaces honored a product setting. Users behind Clash, V2Ray, corporate HTTP proxies, or SOCKS5 had working OS/TUN proxies for the in-app browser but silent failures on model calls, because Node&#39;s <code>fetch</code> does not use the system proxy.</p><p>A single Settings control should apply one proxy to app-owned traffic.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><p><strong>Settings → General → Network</strong> exposes Proxy as System / Direct / Custom. Custom accepts <code>http</code>, <code>https</code>, <code>socks</code>, <code>socks5</code>, and <code>socks5h</code> URLs plus a bypass list. Persistence is optional <code>AppSettings.networkProxy</code> in the existing host settings blob. No protocol or storage schema version bump.</p></li><li><p><strong>System</strong> (default): Chromium <code>session.setProxy({ mode: &quot;system&quot; })</code>. Node sidecar stays direct unless the process already inherited proxy env from the launching shell. This preserves today&#39;s GUI-app behavior.</p></li><li><p><strong>Direct</strong>: Chromium <code>{ mode: &quot;direct&quot; }</code>. Proxy env keys are cleared in Electron main. host-core marketplace curl uses <code>--noproxy &#39;*&#39;</code>.</p></li><li><p><strong>Custom</strong>: Chromium <code>proxyRules</code> + <code>proxyBypassRules</code>; Electron main <code>fetch</code> is <code>net.fetch</code> so SOCKS5 uses the Chromium stack; sidecar sets an undici dispatcher (<code>ProxyAgent</code> for HTTP(S), SOCKS5 CONNECT + the same undici <code>fetch</code>); host-core curl gets <code>--proxy</code> / <code>--noproxy</code> from the stored settings. Default bypass is <code>localhost,127.0.0.1,::1,&lt;local&gt;</code> so loopback MCP and local models stay direct.</p></li><li><p><strong>Not rewritten</strong>: workspace Bash (host-core spawn env strips proxy keys so credentials cannot leak into <code>env</code>), and the system browser used for OAuth (<code>shell.openExternal</code>). Plugin utility processes keep their stripped env; <code>pi.net.fetch</code> still goes through main.</p></li><li><p><strong>Apply without restart.</strong> <code>settings.set</code> updates Chromium sessions (including <code>session-created</code>), main env, and <code>sidecar.configure</code>. A Test action (<code>pi-desktop/network/testProxy</code>) runs one bounded Chromium fetch through the supplied config and does not persist it.</p></li><li><p><strong>Secrets.</strong> Proxy userinfo lives in the settings JSON next to other non-API-key preferences. Logs redact passwords. host-core never <code>set_var</code>s the URL onto its process env. Chromium <code>proxyRules</code> cannot include userinfo (it fails with <code>net::ERR_NO_SUPPORTED_PROXIES</code>) and cannot speak SOCKS5 username/password, so Electron main points Chromium at a <code>127.0.0.1</code> SOCKS5 relay that injects the stored credentials (issue #490). Node, undici, and curl keep the canonical URL with userinfo.</p></li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>LLM, marketplace, updates, and the in-app browser share one proxy.</li><li>System mode does not magically make Node follow the macOS/Windows system proxy; users who need model calls through Clash still choose Custom (typically <code>http://127.0.0.1:7890</code> or <code>socks5://127.0.0.1:1080</code>).</li><li>Adding <code>undici</code> to the sidecar bundle keeps the dispatcher and <code>fetch</code> implementation on one package.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><ul><li>Env-only (<code>HTTP_PROXY</code>): Node <code>fetch</code> ignores it without a dispatcher; SOCKS5 is incomplete; host-core Bash would inherit credentials.</li><li><code>app.commandLine.appendSwitch(&#39;proxy-server&#39;)</code>: cannot change at runtime.</li><li>Per-provider proxy: does not cover marketplace, updates, or the browser.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0177-user-configurable-outbound-proxy.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0177UserConfigurableOutboundProxy = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0177UserConfigurableOutboundProxy as default
};
