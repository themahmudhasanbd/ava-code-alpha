import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"03. AI-Assisted Development Workflow","description":"","frontmatter":{},"headers":[],"relativePath":"spec/06-delivery/03-ai-development-workflow.md","filePath":"spec/06-delivery/03-ai-development-workflow.md","lastUpdated":1790000602000}');
const _sfc_main = { name: "spec/06-delivery/03-ai-development-workflow.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="_03-ai-assisted-development-workflow" tabindex="-1">03. AI-Assisted Development Workflow <a class="header-anchor" href="#_03-ai-assisted-development-workflow" aria-label="Permalink to &quot;03. AI-Assisted Development Workflow&quot;">​</a></h1><blockquote><p>Scope: AI agents and human collaborators working on PI-Desktop Status: Accepted Cross-references: <a href="./../00-baseline">00-baseline</a> · <a href="./../08-meta/decisions-log">decisions-log</a> · <a href="./02-acceptance-criteria">acceptance-criteria</a> · <a href="./04-e2e-test-plan">e2e-test-plan</a> · <a href="./05-change-checklist">change-checklist</a> · <a href="./../../adr/README">ADR index</a></p></blockquote><hr><h2 id="_1-core-immutable-rules" tabindex="-1">1. Core Immutable Rules <a class="header-anchor" href="#_1-core-immutable-rules" aria-label="Permalink to &quot;1. Core Immutable Rules&quot;">​</a></h2><p>The rules below govern every change to the PI-Desktop codebase and documentation. R1–R4 restate the five numbered Immutable Rules in <code>AGENTS.md</code> (R4 covers both the merge-back and worktree clean-up rules); R5 and R6 restate its GitHub issue and pull request handling sections. They cannot be relaxed by an agent without explicit human override.</p><h3 id="r1-—-spec-first-spec-sync" tabindex="-1">R1 — Spec-first / Spec-sync <a class="header-anchor" href="#r1-—-spec-first-spec-sync" aria-label="Permalink to &quot;R1 — Spec-first / Spec-sync&quot;">​</a></h3><blockquote><p><strong>No behavior change without updating the corresponding spec.</strong></p></blockquote><ul><li>Every code, config, or UX change that alters observable behavior must update the relevant <code>docs/spec/</code> document before or alongside the change.</li><li>Architectural boundary changes (process model, IPC contract, storage ownership, security boundary) also require an ADR — see <code>docs/adr/README.md</code>.</li><li>Pure refactor that preserves behavior and API contracts does not require spec updates, but must still be committed (R2).</li></ul><h3 id="r2-—-commit-per-change" tabindex="-1">R2 — Commit-per-change <a class="header-anchor" href="#r2-—-commit-per-change" aria-label="Permalink to &quot;R2 — Commit-per-change&quot;">​</a></h3><blockquote><p><strong>Every completed logical change must be git committed.</strong></p></blockquote><ul><li>No large uncommitted piles of work. Each logical unit of work — a feature, a fix, a spec update, a chore — gets its own commit.</li><li>Uncommitted work at session end is a violation of this rule.</li><li>If a change is incomplete, either commit it as a draft with a <code>WIP:</code> prefix or roll it back.</li></ul><h3 id="r3-—-e2e-coverage-doc" tabindex="-1">R3 — E2E coverage doc <a class="header-anchor" href="#r3-—-e2e-coverage-doc" aria-label="Permalink to &quot;R3 — E2E coverage doc&quot;">​</a></h3><blockquote><p><strong>Every feature/fix that affects user-visible or protocol-visible behavior must update e2e test documentation.</strong></p></blockquote><ul><li>&quot;User-visible&quot;: anything the end-user sees or interacts with (UI, CLI output, dialogs, notifications).</li><li>&quot;Protocol-visible&quot;: IPC messages, RPC methods, plugin API surfaces, event payloads.</li><li>Document the scenario in <code>06-delivery/04-e2e-test-plan.md</code> — even before the automated test exists.</li><li>Internal-only changes (logging format, internal variable rename) do not require e2e doc updates.</li></ul><h3 id="r4-—-request-branch-worktree-merge-gate" tabindex="-1">R4 — Request branch + worktree + merge gate <a class="header-anchor" href="#r4-—-request-branch-worktree-merge-gate" aria-label="Permalink to &quot;R4 — Request branch + worktree + merge gate&quot;">​</a></h3><blockquote><p><strong>Every development request starts from current <code>origin/main</code> in a dedicated branch and worktree. Opening or updating a pull request requires that head to contain the latest <code>origin/main</code>.</strong></p></blockquote><ul><li>Before editing, preserve any existing uncommitted work, fetch <code>origin/main</code>, fast-forward local <code>main</code> when its worktree is clean, and create a new request branch and worktree from that up-to-date commit. Existing work in the primary checkout must never be moved, stashed, or overwritten merely to start a new request.</li><li>Use one short-lived branch per request. Name it <code>&lt;type&gt;/&lt;short-description&gt;</code>, where <code>type</code> matches the conventional change type when practical, for example <code>feat/provider-import</code> or <code>docs/request-branch-workflow</code>.</li><li>Use one dedicated worktree per request. Do not implement a new request in the primary checkout or reuse another request&#39;s worktree.</li><li>Reuse the primary checkout&#39;s development environment where safe: installed toolchains, package-manager stores, build caches, and ignored local environment configuration remain the canonical environment. Reference or link those resources into the request worktree when required; do not copy environment state into tracked files. Generate other worktree-local state only when isolation or version compatibility requires it.</li><li>Task-candidate E2E runs from the request worktree but must reuse the host dependency/runtime environment already provisioned in the primary checkout. Do not run <code>pnpm install</code>, <code>npm install</code>, or create a second environment solely for E2E. Keep temporary profiles, data directories, sockets, ports, logs, and artifacts isolated; install or rebuild only when host dependencies are missing or incompatible, and record the reason.</li><li>Development commits and direct pushes on <code>main</code> are forbidden.</li><li>A commit request does not itself authorize remote publishing. If remote delivery has not been authorized, stop after the request-branch commit in the worktree. Do not merge the task into local <code>main</code> to create an E2E candidate.</li><li>Before opening or updating a PR/MR, fetch <code>origin/main</code> and make it an ancestor of the request head (<code>git merge-base --is-ancestor origin/main HEAD</code>, or <code>pnpm check:pr-base</code>). Refresh in the task worktree (<code>git rebase origin/main</code> on a private branch; a non-destructive merge when rewriting history would be unsafe). Do not open or update a PR that is behind <code>origin/main</code>.</li><li>When a push or other remote delivery is authorized, the fixed delivery order in <code>AGENTS.md</code> applies: refresh against latest <code>origin/main</code>, run task-candidate E2E in the worktree, push the request branch, open a PR/MR targeting <code>main</code>, pass the required remote checks (including the PR-base gate) and reviews, and merge using a permitted strategy. Then fetch and safely synchronize local <code>main</code> with the landed change. Do not infer permission to push directly to <code>main</code>, force-push, or discard unrelated local work.</li><li>Both routes retain the required validation, security, and conflict gates. Relevant E2E runs on a candidate that already contains latest <code>origin/main</code> (R7). If a gate, authentication, permissions, or required review prevents integration, report the actual blocker and remaining work; the requested integration is not Done.</li><li>Worktree cleanup is mandatory and immediate. As soon as the request branch is integrated into remote <code>main</code> (or the user-authorized local-only delivery is complete), remove the worktree and delete the merged branch. A merged request must not leave a worktree on disk. Remove only your own worktree and branch, and only after verifying the merge commits are present in <code>main</code>.</li><li>If the user requests a launch after delivery, build and start the app from the integrated <code>main</code> checkout and its development environment.</li></ul><h3 id="r5-—-verify-linked-github-issues-before-work-then-reply-and-close" tabindex="-1">R5 — Verify linked GitHub issues before work, then reply and close <a class="header-anchor" href="#r5-—-verify-linked-github-issues-before-work-then-reply-and-close" aria-label="Permalink to &quot;R5 — Verify linked GitHub issues before work, then reply and close&quot;">​</a></h3><blockquote><p><strong>A linked GitHub issue is not a task until the reported problem is shown to exist. After the outcome is conclusive, reply on that issue and close it.</strong></p></blockquote><p>This rule applies when the user prompt includes a GitHub issue URL or an unambiguous issue number for this repository.</p><ul><li>Fetch the issue (title, body, labels, comments, and state) before creating a worktree or changing files for the claimed problem.</li><li>Independently verify the claim against the current codebase. For a bug, reproduce it or cite concrete code/spec evidence. For a feature or improvement, confirm the requested behavior is actually missing or incomplete and in scope.</li><li>Do not start implementation until verification confirms the problem exists.</li><li>If the problem does not exist (already fixed, invalid, or a misunderstanding): comment with the verification evidence and close the issue when that conclusion is clear. If verification is inconclusive, comment with what was tried and leave the issue open.</li><li>If the problem exists: follow R4, implement the smallest coherent change, and after the request is merged into local <code>main</code>, comment with the resolution and close the issue.</li><li>Write the GitHub comment in the language of the original issue title and body. Code, commits, specs, and other repository documentation stay English.</li><li>An issue link authorizes commenting on and closing <strong>that</strong> issue only. It does not authorize a git push. Remote publishing remains opt-in per R4 and <code>AGENTS.md</code>.</li><li>Do not comment on or close unrelated issues. Do not reopen a closed issue unless the user explicitly asks.</li></ul><h3 id="r6-—-land-a-linked-pull-request-only-when-it-fixes-the-root-cause-with-a-minimal-diff" tabindex="-1">R6 — Land a linked pull request only when it fixes the root cause with a minimal diff <a class="header-anchor" href="#r6-—-land-a-linked-pull-request-only-when-it-fixes-the-root-cause-with-a-minimal-diff" aria-label="Permalink to &quot;R6 — Land a linked pull request only when it fixes the root cause with a minimal diff&quot;">​</a></h3><blockquote><p><strong>A linked GitHub pull request lands only when it actually removes the reported failure mode at the root, with the smallest coherent change. A sound direction is not enough. Completeness nits may follow after merge; an incomplete fix may not.</strong></p></blockquote><p>This rule applies when the user prompt includes a GitHub pull request URL or an unambiguous pull request number for this repository.</p><ul><li>Fetch the pull request (title, body, files, commits, comments, checks, draft state, base/head, and linked issues) before creating a replacement implementation or requesting a rewrite.</li><li>Independently verify all of: <ol><li>The reported problem is real and in scope (same bar as R5).</li><li>The change must fix the reported root cause with the smallest coherent change — not a nearby symptom, a docs-only restatement, a config contract test, or a partial workaround that leaves the original path intact.</li><li>Extra files, refactors, and spec theater do not compensate for an incomplete fix. The approach must still be compatible with the baseline, security boundaries, and architecture (or be a justified spec-backed amendment).</li></ol></li><li>Do not reimplement the pull request as a replacement, close it for nits, or ask the contributor to start over when (1)–(3) hold.</li><li>If (1)–(3) hold: <ol><li>Merge <strong>that</strong> pull request first, preserving the contributor&#39;s commits. Use a repository-permitted merge strategy that keeps the contributor as author of the landed work.</li><li>Missing additional test coverage, documentation, naming cleanup, formatting, and other non-blocking polish are follow-up work <strong>only when they are not required to prove the root-cause fix</strong>. Build, typecheck, relevant existing test, required E2E, security, data-safety, protocol-compatibility, and merge-conflict failures remain landing blockers.</li><li>Landing blockers that would break <code>main</code> (the change does not compile, fails existing tests for the changed area, or has merge conflicts) may receive the smallest commits <strong>on top of</strong> the author&#39;s work so the pull request can land. Do not squash away the author. Do not rewrite the design. Do not expand the diff beyond the root-cause fix.</li><li>After the pull request is in <code>main</code>, follow R4 for any follow-up improvements from the updated <code>main</code>.</li><li>Comment on the pull request in its language: acknowledge the contribution, state what was merged, and list follow-up if any.</li></ol></li><li>If (1)–(3) fail, or a harm blocker exists (secrets, sandbox or privilege bypass, malicious or clearly destructive changes, out-of-scope reversal of a frozen decision, unrelated drive-by payload): do not merge, do not approve as &quot;direction is fine, follow up later&quot;, and comment with the evidence in the pull request&#39;s language. Prefer a minimal completion of the author&#39;s approach when that approach can actually reach the root cause. Do not silently reimplement the same idea as if the pull request never existed unless the user asks to take the work over.</li><li>Do not merge a draft pull request the author has not marked ready, unless the user explicitly asks to merge the draft. Comment with the review and wait until it is ready.</li><li>A pull request link authorizes reviewing, commenting on, and merging <strong>that</strong> pull request when this rule applies. It does not authorize force-pushing the contributor&#39;s branch or publishing unrelated branches. Follow-up still follows R4&#39;s opt-in remote publishing rule.</li><li>Do not comment on or merge unrelated pull requests. An already-merged pull request is not reopened; remaining gaps become ordinary follow-up.</li><li>When both an issue and a pull request are linked, R6 applies to the pull request and R5 still applies to the issue after the merged outcome.</li></ul><h3 id="r7-—-code-bearing-changes-require-relevant-e2e-after-main-integration" tabindex="-1">R7 — Code-bearing changes require relevant E2E after main integration <a class="header-anchor" href="#r7-—-code-bearing-changes-require-relevant-e2e-after-main-integration" aria-label="Permalink to &quot;R7 — Code-bearing changes require relevant E2E after main integration&quot;">​</a></h3><blockquote><p><strong>Every code-bearing change must pass relevant E2E on a candidate that already contains the latest <code>origin/main</code>, before its request branch is pushed or a PR/MR is opened.</strong></p></blockquote><p>This rule applies to changes that modify executable or runtime-affecting content, including <code>apps/</code>, <code>packages/</code>, <code>crates/</code>, runtime scripts, build or CI configuration, packaging behavior, protocol behavior, and persisted data. Documentation-only changes are exempt when they do not alter executable behavior.</p><p>E2E execution is mandatory for code-bearing changes. Task-candidate E2E runs in the request worktree after incorporating <code>origin/main</code>. Do not merge the task into local <code>main</code> to satisfy this gate. An E2E run on a branch that is behind <code>origin/main</code> does not satisfy R7. Select suites using the regression-surface guidance in <code>04-e2e-test-plan.md</code>; build, typecheck, lint, unit tests, integration tests, manual review, and source inspection do not replace relevant E2E.</p><p>Required validation is part of the authorized integration workflow and does not require a separate user request to run tests.</p><p>If a required suite cannot run in the current environment, record the suite, reason, alternative validation, and remaining risk as <code>NOT RUN</code>. The branch push and PR/MR may still proceed with that record so the change can be validated in a capable environment, but the gate is not satisfied and delivery remains incomplete until the suite passes against a candidate that contains latest <code>origin/main</code>. A failed required suite blocks the push, the PR/MR, and declaring the change delivered until the failure is classified and fixed.</p><p>After the PR/MR merges into remote <code>main</code>, rerun the affected suites when the landed executable content differs from the commit the gate ran on (landing fixes, conflict resolution, or commits added during review). Otherwise the recorded result stands. Always state the commit the recorded E2E evidence applies to.</p><h3 id="github-issue-templates" tabindex="-1">GitHub issue templates <a class="header-anchor" href="#github-issue-templates" aria-label="Permalink to &quot;GitHub issue templates&quot;">​</a></h3><p><code>.github/ISSUE_TEMPLATE</code> is the only public intake path (<code>blank_issues_enabled: false</code>). English is the source label language; Chinese remains on the same fields.</p><ul><li><strong>Bug report</strong> requires: description, reproduction steps, expected behavior, actual behavior, app version, and OS. Logs, extra environment, and screenshots are optional. Settings → Info prefills version, OS, and environment when opened from the app (D313).</li><li><strong>Feature request</strong> requires: problem and proposed change. Alternatives and extra context are optional.</li></ul><p>Do not weaken these required fields. Blank issues stay disabled.</p><hr><h2 id="_2-development-loop" tabindex="-1">2. Development Loop <a class="header-anchor" href="#_2-development-loop" aria-label="Permalink to &quot;2. Development Loop&quot;">​</a></h2><p>Every change follows this sequence. Steps may be iterated if the implementation reveals new requirements. If the prompt includes a GitHub issue, complete R5 verification before step 1. If the prompt includes a GitHub pull request, complete the R6 root-cause review (and merge only when the change actually fixes the problem with a minimal diff) before starting a replacement or follow-up implementation.</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>0. If a GitHub issue is linked: verify the claim (R5) before any implementation</span></span>
<span class="line"><span>0b. If a GitHub pull request is linked: review root cause and minimality (R6); merge only when it actually fixes the problem; start follow-up only after it is in \`main\`</span></span>
<span class="line"><span>1. Sync main + create a request branch and worktree</span></span>
<span class="line"><span>2. Read baseline + relevant specs</span></span>
<span class="line"><span>3. Plan change + list impacted specs and necessary validation</span></span>
<span class="line"><span>4. Implement</span></span>
<span class="line"><span>5. Update specs / ADR / decisions-log if needed</span></span>
<span class="line"><span>6. Update or add e2e scenarios when R3 applies</span></span>
<span class="line"><span>7. Run targeted local checks necessary for the change&#39;s risk</span></span>
<span class="line"><span>8. Commit with conventional message</span></span>
<span class="line"><span>9. Update BOARD if milestone-related</span></span>
<span class="line"><span>10. Refresh against latest \`origin/main\` (\`pnpm check:pr-base\`) and run</span></span>
<span class="line"><span>    task-candidate E2E in the worktree (R7); when remote delivery is authorized,</span></span>
<span class="line"><span>    push branch + open PR/MR to main only after that gate</span></span>
<span class="line"><span>11. Merge the PR/MR into remote \`main\` through the remote gates when remote</span></span>
<span class="line"><span>    delivery is authorized, synchronize local \`main\`, verify, and clean up</span></span>
<span class="line"><span>12. If launch was requested: build and start from integrated main</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br></div></div><h3 id="step-by-step" tabindex="-1">Step-by-step <a class="header-anchor" href="#step-by-step" aria-label="Permalink to &quot;Step-by-step&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Step</th><th>Action</th><th>Output</th></tr></thead><tbody><tr><td><strong>0. Issue verify</strong></td><td>When a GitHub issue is linked, fetch it and independently verify that the reported problem exists. Stop here (comment, and close only if conclusive) when it does not.</td><td>Verified issue, or a comment and close/leave-open decision.</td></tr><tr><td><strong>0b. PR review</strong></td><td>When a GitHub pull request is linked, fetch it and independently verify that it fixes the reported root cause with the smallest coherent change. Merge only when it does; start follow-up only after it is in <code>main</code>. Stop (comment, do not rewrite) when it does not.</td><td>Merged contributor PR plus follow-up plan, or a comment and no merge.</td></tr><tr><td><strong>1. Branch + worktree</strong></td><td>Preserve existing work, update from <code>origin/main</code>, and create a dedicated request branch in a dedicated worktree. Reuse the primary checkout&#39;s environment where safe.</td><td>Isolated task files on current <code>main</code> with a consistent development environment.</td></tr><tr><td><strong>2. Read</strong></td><td>Read <code>00-baseline.md</code> and any specs relevant to the change area.</td><td>Mental model of constraints.</td></tr><tr><td><strong>3. Plan</strong></td><td>Describe the intended change. List every spec, ADR, and e2e scenario that will need updates, and assess whether local validation is necessary.</td><td>Change plan + impact and validation list.</td></tr><tr><td><strong>4. Implement</strong></td><td>Write code, config, or assets.</td><td>Changed files.</td></tr><tr><td><strong>5. Spec-sync</strong></td><td>Update specs per the impact list. Add ADR if architectural. Update <code>decisions-log.md</code> if an implementation default changes.</td><td>Updated docs/spec/* and/or docs/adr/*.</td></tr><tr><td><strong>6. E2E doc</strong></td><td>When R3 applies, add or update scenario entries in <code>04-e2e-test-plan.md</code> and link to acceptance criteria IDs (A–H). Otherwise, confirm no scenario update is needed.</td><td>Updated e2e test plan, or confirmed not applicable.</td></tr><tr><td><strong>7. Validate</strong></td><td>Use change risk and regression scope to select the smallest useful local checks. The relevant E2E gate for a code-bearing change runs in the request worktree after <code>origin/main</code> is incorporated (<code>pnpm check:pr-base</code>) and before any branch push or PR/MR; a suite that cannot run is recorded as <code>NOT RUN</code> and keeps delivery incomplete.</td><td>Targeted check and E2E results, or an explicit environment limitation.</td></tr><tr><td><strong>8. Commit</strong></td><td>Git commit with conventional message (see §4).</td><td>One or more commits.</td></tr><tr><td><strong>9. BOARD</strong></td><td>If the change completes a milestone deliverable, update <code>docs/project/BOARD.md</code>.</td><td>Updated board.</td></tr><tr><td><strong>10. Refresh + gate</strong></td><td>Refresh against latest <code>origin/main</code>, confirm it is an ancestor of HEAD (<code>pnpm check:pr-base</code>), then run task-candidate E2E in the worktree. When remote publishing is authorized, push the request branch and open a PR/MR targeting <code>main</code> only after that gate.</td><td>Verified candidate with the E2E gate result, or a recorded <code>NOT RUN</code> limitation; reviewable remote change or a local-only delivery route.</td></tr><tr><td><strong>11. Remote merge + cleanup</strong></td><td>For authorized remote delivery, merge the PR/MR into remote <code>main</code> through the required gates (including the PR-base check) and synchronize local <code>main</code>; rerun the affected suites when the landed executable content differs from the commit the gate ran on. Verify the expected commits and remove the merged worktree and branch.</td><td>Requested integration complete, or an explicit blocker / narrower user-requested handoff.</td></tr><tr><td><strong>12. Launch</strong></td><td>When requested, build and start from the integrated <code>main</code> checkout and its development environment.</td><td>Running app includes the delivered change.</td></tr></tbody></table><h3 id="local-validation-and-e2e-execution-policy" tabindex="-1">Local Validation and E2E Execution Policy <a class="header-anchor" href="#local-validation-and-e2e-execution-policy" aria-label="Permalink to &quot;Local Validation and E2E Execution Policy&quot;">​</a></h3><ul><li>Local validation is risk-based rather than an automatic prerequisite for delivery. Documentation-only changes and low-risk mechanical edits normally require no local tests or checks and proceed to the authorized delivery route under R4. No separate approval or waiver is needed to skip unnecessary checks; the required E2E gate on a candidate that already contains latest <code>origin/main</code> still applies to code-bearing changes.</li><li>Changes with material regression risk, including security boundaries, protocol contracts, data migrations, build configuration, or widely shared behavior, normally require the smallest targeted non-E2E validation that can address that risk. A full local suite is not the default.</li><li>E2E scenario documentation and E2E execution are separate concerns. R3 still requires scenario updates for user-visible or protocol-visible behavior.</li><li>Every code-bearing change must run at least one relevant E2E suite on a candidate that contains latest <code>origin/main</code> before its branch is pushed or a PR/MR is opened, and must run the union of suites required by the affected regression surfaces. The available commands are defined by the root <code>package.json</code> and the selection matrix in <code>04-e2e-test-plan.md</code>.</li><li>Task-candidate E2E may use the request worktree&#39;s source, but it must use the host dependency/runtime environment described in R4. Do not reinstall the repository environment for each run; only missing or incompatible host dependencies justify installation or rebuild, and that reason must be recorded. Temporary profiles, data, sockets, ports, logs, and artifacts remain isolated from the host&#39;s mutable runtime state.</li><li>Development-time iteration remains risk-based: an E2E run on a stale request branch may be used for debugging, but only a run after incorporating <code>origin/main</code> satisfies this policy.</li><li>If the environment cannot run a required suite, record the suite, reason, alternative validation, and remaining risk as <code>NOT RUN</code>. The branch push and PR/MR may proceed with that record, but the gate is not satisfied and delivery remains incomplete until the suite passes against a candidate that contains latest <code>origin/main</code>.</li><li>Required E2E jobs that the hosting platform starts before a remote merge do not replace the task-candidate gate. Observe and report their result; after the remote merge, rerun the affected suites when the landed executable content differs from the commit the gate ran on. Otherwise the recorded result stands.</li></ul><h3 id="marketplace-update-diagnosis-gate" tabindex="-1">Marketplace/update diagnosis gate <a class="header-anchor" href="#marketplace-update-diagnosis-gate" aria-label="Permalink to &quot;Marketplace/update diagnosis gate&quot;">​</a></h3><p>Plugin update incidents require an evidence-first prompt flow before code changes:</p><ol><li>Capture the exact plugin ID, installed version, displayed version, expected release, catalog URL, and observation time.</li><li>Fetch the live catalog and inspect the exact entry, then inspect the local catalog cache and installed registry independently.</li><li>Classify the failure boundary: publisher/catalog data, fetch/cache fallback, host version comparison, IPC propagation, or renderer presentation.</li><li>Run <code>pnpm check:marketplace -- --url &lt;catalog-url&gt; --plugin &lt;id&gt;</code>. Missing <code>shasum</code>, <code>url</code>, positive <code>sizeBytes</code>, or <code>permissions</code>, or a catalog <code>author</code> that is not a string (for example <code>{ name, url }</code> copied from a plugin manifest), is a release-data failure, not evidence of a stale renderer. Incomplete releases remain non-installable.</li><li>Reproduce with a fixture containing unsorted versions and incomplete metadata before changing host or renderer code.</li></ol><p>The agent must state which boundary failed and what evidence rules out the other boundaries. A client-side fallback may preserve safe discovery, but it must not be used to conceal an invalid marketplace release.</p><hr><h2 id="_3-spec-update-matrix" tabindex="-1">3. Spec Update Matrix <a class="header-anchor" href="#_3-spec-update-matrix" aria-label="Permalink to &quot;3. Spec Update Matrix&quot;">​</a></h2><p>Which change types require which doc updates.</p><table tabindex="0"><thead><tr><th>Change type</th><th>Spec update</th><th>ADR</th><th>Decisions-log</th><th>E2E doc</th><th>BOARD</th></tr></thead><tbody><tr><td>New feature (user-visible)</td><td>Related domain spec</td><td>If architectural boundary</td><td>—</td><td>New scenario</td><td>If milestone deliverable</td></tr><tr><td>Bug fix (user-visible)</td><td>Related spec if behavior clarified</td><td>—</td><td>—</td><td>New or updated scenario</td><td>—</td></tr><tr><td>Bug fix (internal)</td><td>—</td><td>—</td><td>—</td><td>—</td><td>—</td></tr><tr><td>Refactor (behavior preserved)</td><td>—</td><td>—</td><td>—</td><td>—</td><td>—</td></tr><tr><td>Architectural change</td><td>Related specs + baseline</td><td><strong>New ADR</strong></td><td>Update entry if default changes</td><td>Update affected scenarios</td><td>—</td></tr><tr><td>New IPC/RPC method</td><td><code>03-runtime/01-ipc-protocol.md</code> or <code>06-host-rpc-protocol.md</code></td><td>If contract boundary</td><td>—</td><td>New protocol scenario</td><td>—</td></tr><tr><td>Plugin API addition</td><td><code>07-plugins/03-plugin-api.md</code></td><td>If boundary change</td><td>—</td><td>New plugin scenario</td><td>If M4 deliverable</td></tr><tr><td>Security change</td><td><code>05-security/01-security.md</code></td><td>If boundary change</td><td>Update if D001–D010 touched</td><td>New security scenario</td><td>—</td></tr><tr><td>UX change</td><td>Related <code>04-ux/</code> spec</td><td>—</td><td>—</td><td>New UI scenario</td><td>—</td></tr><tr><td>Spec-only update</td><td>The spec itself</td><td>—</td><td>—</td><td>—</td><td>—</td></tr><tr><td>Chore (deps, tooling)</td><td>—</td><td>—</td><td>If tooling decision</td><td>—</td><td>—</td></tr><tr><td><strong>App version release / stable tag</strong></td><td><code>06-delivery/06-release-runbook.md</code> (mandatory version-surface gate before tag: shipped-locale <code>packages/shared/src/changelog.ts</code>, its test list, all workspace/Cargo/<code>APP_VERSION</code> versions, and the release line in <code>README.md</code> + <code>README.zh-CN.md</code>)</td><td>—</td><td>If release policy changes</td><td>Confirm E2E-067B still accurate</td><td>If milestone ship</td></tr></tbody></table><hr><h2 id="_4-git-commit-rules" tabindex="-1">4. Git Commit Rules <a class="header-anchor" href="#_4-git-commit-rules" aria-label="Permalink to &quot;4. Git Commit Rules&quot;">​</a></h2><h3 id="_4-1-conventional-commits" tabindex="-1">4.1 Conventional Commits <a class="header-anchor" href="#_4-1-conventional-commits" aria-label="Permalink to &quot;4.1 Conventional Commits&quot;">​</a></h3><p>Format: <code>type(scope): description</code></p><table tabindex="0"><thead><tr><th>Type</th><th>Use for</th></tr></thead><tbody><tr><td><code>feat</code></td><td>New feature</td></tr><tr><td><code>fix</code></td><td>Bug fix</td></tr><tr><td><code>docs</code></td><td>Documentation-only change</td></tr><tr><td><code>test</code></td><td>Adding or updating tests</td></tr><tr><td><code>chore</code></td><td>Build, deps, tooling, CI</td></tr><tr><td><code>refactor</code></td><td>Code restructuring, no behavior change</td></tr><tr><td><code>perf</code></td><td>Performance improvement</td></tr><tr><td><code>build</code></td><td>Build system or external dependency change</td></tr><tr><td><code>ci</code></td><td>CI/CD configuration change</td></tr></tbody></table><p><strong>Scope</strong> is optional but encouraged — e.g. <code>feat(host-core):</code>, <code>fix(ui):</code>, <code>docs(spec):</code>.</p><h3 id="_4-2-language" tabindex="-1">4.2 Language <a class="header-anchor" href="#_4-2-language" aria-label="Permalink to &quot;4.2 Language&quot;">​</a></h3><ul><li>Commit messages: <strong>English only</strong> (matches baseline language policy).</li><li>Body: optional; use for non-obvious context.</li></ul><h3 id="_4-3-one-logical-change-per-commit" tabindex="-1">4.3 One logical change per commit <a class="header-anchor" href="#_4-3-one-logical-change-per-commit" aria-label="Permalink to &quot;4.3 One logical change per commit&quot;">​</a></h3><ul><li>Prefer small, focused commits.</li><li>Spec updates that are tightly coupled to the code change should be in the same commit.</li><li>Pure doc changes (spec rewrite, ADR) may be a separate adjacent <code>docs:</code> commit.</li></ul><h3 id="_4-4-never-commit" tabindex="-1">4.4 Never commit <a class="header-anchor" href="#_4-4-never-commit" aria-label="Permalink to &quot;4.4 Never commit&quot;">​</a></h3><ul><li>Secrets, API keys, tokens, passwords</li><li>Local-only data (user configs, session data, logs)</li><li><code>node_modules/</code>, build artifacts, release packages</li><li>Generated files that should be rebuilt per CI</li></ul><h3 id="_4-5-pre-commit-checklist" tabindex="-1">4.5 Pre-commit checklist <a class="header-anchor" href="#_4-5-pre-commit-checklist" aria-label="Permalink to &quot;4.5 Pre-commit checklist&quot;">​</a></h3><p>Before committing, verify:</p><ol><li>Change is one logical unit (or clearly split).</li><li>No secrets or local data in the diff.</li><li>Specs updated per §3 matrix.</li><li>E2E doc updated if behavior changed.</li><li><code>git diff --stat</code> review — nothing unexpected.</li><li>Commit message follows conventional format.</li></ol><hr><h2 id="_5-branching-model" tabindex="-1">5. Branching Model <a class="header-anchor" href="#_5-branching-model" aria-label="Permalink to &quot;5. Branching Model&quot;">​</a></h2><p>The repository uses a mandatory request-branch and worktree workflow:</p><ul><li><strong><code>main</code></strong> is always deployable and is the protected integration target. Do not develop or create development commits on it, or push it directly.</li><li><strong>Request branches and worktrees</strong> are mandatory for every development request, including docs, chores, and small fixes. Create each branch and worktree from an up-to-date <code>main</code>, then remove both immediately after the branch is merged into <code>main</code>.</li><li><strong>Branch names</strong> use <code>&lt;type&gt;/&lt;short-description&gt;</code> with a lowercase, kebab-case description. Allowed type prefixes mirror §4.1.</li><li><strong>No long-lived development branch</strong> exists. Each request gets a new branch; an old request branch must not be reused for unrelated work.</li><li><strong>The primary checkout owns the default development environment.</strong> Request worktrees reuse its toolchains, package-manager stores, caches, and ignored local configuration where safe. A request may create isolated local state when sharing would be unsafe or incompatible, but that state stays ignored and must not leak into commits.</li><li><strong>Delivery follows R4&#39;s authorization boundary and its fixed order.</strong> Commit-only delivery stops at the request-branch commit in the worktree. Authorized push/remote delivery first refreshes against latest <code>origin/main</code> (<code>pnpm check:pr-base</code>), runs task-candidate E2E in the worktree (R7), then pushes the request branch and opens the PR/MR, merges it into remote <code>main</code> through the PR/MR gates (including the PR-base check), and synchronizes local <code>main</code>. Explicit branch-only or draft-only requests retain their narrower scope. Do not open or update a PR that is behind <code>origin/main</code>.</li></ul><p>Typical request start (run from the primary checkout; choose a path outside it):</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">git</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> status</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --short</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">git</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> fetch</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> origin</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> main</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">git</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> worktree</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> add</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -b</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">typ</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">e</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">/</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">short-descriptio</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">n</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">worktree-pat</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">h</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> origin/main</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>If <code>main</code> is checked out in a clean primary worktree, <code>git switch main</code> plus <code>git fetch origin main</code> and <code>git pull --ff-only origin main</code> should be run before <code>git worktree add</code>. If local <code>main</code> has diverged, synchronize with <code>git merge origin/main</code> and resolve the divergence before starting new work without discarding commits. If the primary worktree is not clean or is on another branch, leave it untouched and create the request worktree directly from the fetched <code>origin/main</code>. Never discard, stash, move, or overwrite unrelated work merely to satisfy this sequence.</p><p>Environment reuse is resource-specific. Package-manager stores and language toolchains are normally shared automatically. A compatible dependency tree and Electron/Rust build targets should be referenced or linked from the primary checkout when a task needs them; do not run <code>pnpm install</code> or <code>npm install</code> merely because the request uses a separate worktree. Build outputs that can race, mutable runtime data, temporary E2E profiles, and incompatible dependency trees must remain worktree-local. Clean CI and release runners are the exception and may install from lockfiles.</p><p>Typical authorized GitHub delivery (use the hosting platform&#39;s equivalent when needed; synchronize and clean up from the clean primary checkout after merge):</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># from the request worktree</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">git</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> fetch</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> origin</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> main</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">git</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> rebase</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> origin/main</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">   # private branch; do not force-push a shared branch</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> check:pr-base</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># run the required task-candidate E2E suites for this change (R7)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">git</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> push</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -u</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> origin</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">typ</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">e</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">/</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">short-descriptio</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">n</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">gh</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> pr</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> create</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --base</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> main</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --head</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">typ</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">e</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">/</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">short-descriptio</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">n</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">gh</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> pr</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> checks</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --watch</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">gh</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> pr</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> merge</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --merge</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">git</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> fetch</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> origin</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> main</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># from a clean primary checkout</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">git</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> switch</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> main</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">git</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> merge</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --ff-only</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> origin/main</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">git</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> worktree</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> remove</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">worktree-pat</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">h</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">git</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> branch</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -d</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">typ</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">e</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">/</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">short-descriptio</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">n</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">git</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> worktree</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> prune</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">git</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> push</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> origin</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --delete</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">typ</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">e</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">/</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">short-descriptio</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">n</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br></div></div><p>After the remote merge, synchronize local <code>main</code> with a fast-forward when possible. Reset local <code>main</code> to <code>origin/main</code> only after the request commits are verified present in remote <code>main</code> and no local-only commits remain.</p><p>If the primary checkout cannot take that synchronization, use a short-lived worktree for <code>main</code> instead of disturbing unrelated work.</p><p>Do not merge the request branch into local <code>main</code> to create an E2E candidate or to open a PR. Local-only delivery (no remote PR/MR) stays on the request branch until the user explicitly asks to integrate it.</p><p>The worktree must be clean before removal; commit or discard the request&#39;s own leftover changes first. Use <code>git branch -d</code> rather than <code>-D</code> so an unmerged branch refuses to delete. If <code>git worktree remove</code> reports the worktree as dirty or locked, resolve that state instead of forcing removal, and never remove another request&#39;s worktree.</p><hr><h2 id="_6-definition-of-done" tabindex="-1">6. Definition of Done <a class="header-anchor" href="#_6-definition-of-done" aria-label="Permalink to &quot;6. Definition of Done&quot;">​</a></h2><p>A change is <strong>Done</strong> when all applicable conditions are true, respecting an explicit branch-only or draft-only delivery scope:</p><ol><li>A dedicated request branch and worktree were created from an up-to-date <code>main</code>.</li><li>Code (or doc) implements the planned change.</li><li>All impacted specs are updated.</li><li>E2E scenarios are documented (or confirmed not needed per §3).</li><li>Necessary targeted local validation passes; for a code-bearing change the relevant E2E gate has run on a candidate that contains latest <code>origin/main</code> before the branch push, the PR/MR, or a commit-only completion, or the environment limitation is recorded as <code>NOT RUN</code> and delivery remains incomplete until that suite passes; automatically triggered remote gates also pass, including the PR-base ancestry check.</li><li>Change is committed with a conventional message.</li><li>BOARD is updated if a milestone deliverable completed.</li><li>No secrets or local data are present in the commit.</li><li>Before a PR/MR is opened or updated, <code>origin/main</code> is an ancestor of the request head (<code>pnpm check:pr-base</code>). When remote delivery was authorized, the PR/MR was reviewed and merged into remote <code>main</code>, local <code>main</code> includes the landed change, and the affected suites were rerun when the landed executable content differs from the commit the gate ran on.</li><li>After integration, the expected commits were verified in <code>main</code>, the request worktree was removed, and the merged request branch was deleted.</li><li>If a GitHub issue was linked: the claim was verified before implementation; the issue received a comment in its language; and the issue was closed when the outcome was conclusive.</li><li>If a GitHub pull request was linked: the root-cause bar was reviewed; the pull request was merged only when it actually fixed the problem with a minimal diff; follow-up landed after merge; the contributor&#39;s work was not discarded.</li><li>If launch was requested after delivery: the app was built and started from the integrated <code>main</code> checkout and its development environment.</li></ol><h3 id="release-version-tag-gate" tabindex="-1">Release / version-tag gate <a class="header-anchor" href="#release-version-tag-gate" aria-label="Permalink to &quot;Release / version-tag gate&quot;">​</a></h3><p>When the change is a <strong>stable app version release</strong> (version bump + tag), Definition of Done also requires, <strong>before</strong> the tag, that every version-bearing surface describes the new version: the shipped-locale in-app changelog entries in <code>packages/shared/src/changelog.ts</code> (English and every shipped product locale, with aligned highlight counts) with its <code>changelog.test.ts</code> list, every workspace <code>package.json</code> (including <code>docs/package.json</code>), the Cargo workspace version and <code>host-core</code> lockfile entry, <code>APP_VERSION</code>, and the release line stated in <code>README.md</code> + <code>README.zh-CN.md</code>. <code>node scripts/check-release-docs.mjs</code> must pass; <code>scripts/release.mjs</code> runs it and refuses to tag otherwise. See <a href="./06-release-runbook#41-mandatory-release-version-surface-gate-d164--d260">06-release-runbook.md §4.1</a>, D164, and D260. GitHub release notes are not a substitute.</p><hr><h2 id="_7-forbidden-practices" tabindex="-1">7. Forbidden Practices <a class="header-anchor" href="#_7-forbidden-practices" aria-label="Permalink to &quot;7. Forbidden Practices&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Practice</th><th>Why</th></tr></thead><tbody><tr><td>Committing secrets</td><td>Security violation</td></tr><tr><td>Large uncommitted diffs</td><td>Violates R2; loss of granularity</td></tr><tr><td>Changing behavior without spec update</td><td>Violates R1; specs become unreliable</td></tr><tr><td>Skipping e2e doc for user-visible changes</td><td>Violates R3; traceability gap</td></tr><tr><td>Declaring a code-bearing change complete without successful relevant E2E after incorporating latest <code>origin/main</code></td><td>Violates R7 and leaves cross-process behavior unverified</td></tr><tr><td>Treating the full local test/check suite as an automatic pre-push requirement</td><td>Ignores risk-based validation and delays delivery without evidence of need</td></tr><tr><td>Developing or creating development commits on <code>main</code>, or pushing it directly</td><td>Violates R4; bypasses isolation and review gates</td></tr><tr><td>Developing a new request in the primary checkout or another request&#39;s worktree</td><td>Violates R4; mixes task files and local state</td></tr><tr><td>Reusing a request branch for unrelated work</td><td>Mixes request scope and weakens traceability</td></tr><tr><td>Opening or updating a PR/MR whose head is behind <code>origin/main</code></td><td>Violates R4; review starts on a stale base</td></tr><tr><td>Stopping at a task-branch commit or push when the user requested remote delivery</td><td>Violates R4 unless the user explicitly limited delivery to a branch or draft</td></tr><tr><td>Leaving a merged request worktree on disk</td><td>Violates R4; stale worktrees accumulate and invite cross-request contamination</td></tr><tr><td>Modifying baseline frozen decisions without ADR + version bump</td><td>Baseline is frozen; changes need formal process</td></tr><tr><td>Committing generated artifacts that CI should rebuild</td><td>Repo bloat, merge conflicts</td></tr><tr><td>Mixing multiple logical changes in one commit without clear message</td><td>Loss of history granularity</td></tr><tr><td>Implementing a linked GitHub issue without verifying the problem exists</td><td>Violates R5; wastes work on invalid or already-fixed claims</td></tr><tr><td>Closing a linked GitHub issue without a comment in the issue language</td><td>Violates R5; leaves no public record of the outcome</td></tr><tr><td>Closing, rewriting, or requesting a restart of a linked pull request that already fixes the root cause with a minimal diff, solely for nits</td><td>Violates R6; discards the contributor&#39;s work</td></tr><tr><td>Blocking merge of a root-cause-complete linked pull request solely for missing specs, extra tests, style, or agent-workflow completeness</td><td>Violates R6; those are follow-up after merge</td></tr><tr><td>Merging a linked pull request that only has a sound direction, leaves the original failure mode, or is larger than the smallest coherent fix without a stated reason</td><td>Violates R6; direction is not enough</td></tr><tr><td>Merging a linked pull request that introduces a harm blocker</td><td>Violates R6</td></tr><tr><td>Force-pushing a contributor&#39;s branch to land a linked pull request</td><td>Violates R6; landing fixes go on top of the author&#39;s commits</td></tr><tr><td>Tagging a stable app release without updating <code>packages/shared/src/changelog.ts</code> for every shipped locale</td><td>Violates D164 / D345 / release runbook; in-app What&#39;s new is empty for that locale and version</td></tr><tr><td>Tagging a stable app release while <code>README.md</code> / <code>README.zh-CN.md</code> still state an older release line, or bypassing <code>scripts/check-release-docs.mjs</code> with <code>--skip-docs-check</code></td><td>Violates D260 / release runbook; published documentation advertises a version the release no longer matches</td></tr><tr><td>Pushing a request branch or opening a PR/MR before the required E2E has run against a candidate that contains latest <code>origin/main</code>, or declaring a code-bearing change delivered without that result (except a recorded <code>NOT RUN</code> limitation)</td><td>Violates R7&#39;s fixed order; review would start on an unvalidated commit</td></tr></tbody></table><hr><h2 id="_8-acceptance-criteria-for-this-workflow" tabindex="-1">8. Acceptance Criteria for This Workflow <a class="header-anchor" href="#_8-acceptance-criteria-for-this-workflow" aria-label="Permalink to &quot;8. Acceptance Criteria for This Workflow&quot;">​</a></h2><p>This workflow spec itself is accepted when:</p><ul><li>[ ] R1/R2/R3/R4/R5/R6/R7 are stated clearly and cross-linked to relevant specs.</li><li>[ ] Development loop is documented and referenced by <code>AGENTS.md</code>.</li><li>[ ] Spec update matrix covers all change types in the baseline.</li><li>[ ] Git commit rules match existing repo commit style (<code>docs:</code>, <code>chore:</code>).</li><li>[ ] Every development request is required to use a dedicated branch and worktree created from current <code>main</code>.</li><li>[ ] Request worktrees reuse the primary checkout&#39;s environment where safe without committing local environment state.</li><li>[ ] Opening or updating a PR/MR requires <code>origin/main</code> to be an ancestor of the request head (<code>pnpm check:pr-base</code>); a PR behind <code>origin/main</code> is rejected.</li><li>[ ] Remote publishing requires authorization and uses PR/MR gates before remote <code>main</code> integration and local synchronization.</li><li>[ ] Refreshing against latest <code>origin/main</code> and the relevant E2E gate precede the request branch push and the pull request. Do not merge the task into local <code>main</code> to create that candidate.</li><li>[ ] Worktree removal and branch deletion are required immediately after the request branch is merged into <code>main</code>.</li><li>[ ] Relevant E2E execution is mandatory for code-bearing changes on a candidate that contains latest <code>origin/main</code> before the PR/MR is opened, without a separate test request; E2E documentation remains mandatory under R3.</li><li>[ ] Local validation is risk-based; unnecessary checks may be skipped without blocking commit, push, or PR/MR creation.</li><li>[ ] Definition of Done is complete and actionable.</li><li>[ ] Forbidden practices list covers known risk areas.</li><li>[ ] <code>AGENTS.md</code> points to this doc, <code>04-e2e-test-plan.md</code>, and <code>05-change-checklist.md</code>.</li><li>[ ] Linked GitHub issues are verified before implementation, then commented on in the issue language and closed when conclusive.</li><li>[ ] Linked GitHub pull requests land only when they fix the reported root cause with a minimal diff; contributor work is not discarded for nits.</li><li>[ ] All indexes updated (NAV, delivery README, spec README, docs README, BOARD).</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("spec/06-delivery/03-ai-development-workflow.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _03AiDevelopmentWorkflow = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _03AiDevelopmentWorkflow as default
};
