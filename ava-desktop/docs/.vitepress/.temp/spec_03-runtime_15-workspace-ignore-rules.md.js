import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"15. Workspace Ignore Rules","description":"","frontmatter":{},"headers":[],"relativePath":"spec/03-runtime/15-workspace-ignore-rules.md","filePath":"spec/03-runtime/15-workspace-ignore-rules.md","lastUpdated":1788991173000}');
const _sfc_main = { name: "spec/03-runtime/15-workspace-ignore-rules.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="_15-workspace-ignore-rules" tabindex="-1">15. Workspace Ignore Rules <a class="header-anchor" href="#_15-workspace-ignore-rules" aria-label="Permalink to &quot;15. Workspace Ignore Rules&quot;">​</a></h1><h2 id="_1-goal" tabindex="-1">1. Goal <a class="header-anchor" href="#_1-goal" aria-label="Permalink to &quot;1. Goal&quot;">​</a></h2><p>Prevent tools from scanning/reading/writing sensitive or useless paths by default, while allowing an explicit, visible permission decision when a task intentionally targets a path outside the session workspace.</p><h2 id="_2-rule-layers-priority-high-→-low" tabindex="-1">2. Rule layers (priority high → low) <a class="header-anchor" href="#_2-rule-layers-priority-high-→-low" aria-label="Permalink to &quot;2. Rule layers (priority high → low)&quot;">​</a></h2><ol><li><strong>Security denylist</strong> (always on, not user-disable in MVP)</li><li><strong>App defaults</strong> (shipped)</li><li><strong>Workspace rules</strong> (<code>.pi-desktopignore</code> at the workspace root)</li><li><strong>User global ignore</strong> (<code>&lt;data_dir&gt;/ignore</code>, i.e. <code>~/.pi-desktop/ignore</code> by default)</li><li>Explicit tool path still subject to the security denylist and the outside-path permission gate</li></ol><p>An explicit <code>path</code> argument on <code>Glob</code>/<code>Grep</code> opts that walk out of layers 2–4 (the same way it already bypasses parent <code>.gitignore</code> rules), so a caller who names <code>node_modules/pkg</code> or <code>dist</code> can still search it. Layer 1 applies to every walk and every explicit path.</p><h2 id="_3-security-denylist-always" tabindex="-1">3. Security denylist (always) <a class="header-anchor" href="#_3-security-denylist-always" aria-label="Permalink to &quot;3. Security denylist (always)&quot;">​</a></h2><p>Outside-workspace read/write/search is denied by default. An explicit <code>Read</code>/<code>Glob</code>/<code>Grep</code>/<code>Write</code>/<code>Edit</code> path may proceed only after the host applies the permission mode: <code>auto</code> allows it, while <code>ask</code> and <code>accept-edits</code> ask the user. An implicit recursive walk never gains outside-workspace access.</p><p>Also deny inside workspace (and inside the scratch or an approved external root) for:</p><ul><li><code>.git/objects/**</code></li><li>private key patterns: <code>*.pem</code>, <code>*.key</code>, <code>id_rsa</code>, <code>id_ed25519</code></li><li><code>.env</code>, <code>.env.*</code> — except the documentation variants <code>.env.example</code>, <code>.env.sample</code>, and <code>.env.template</code>, which hold no secrets and are what a coding task usually needs</li><li>credential files: <code>*.p12</code>, <code>*.pfx</code>, <code>credentials.json</code> (Google), <code>.npmrc</code> with tokens (best-effort)</li></ul><p>File-name matching is case-insensitive. <code>Glob</code> and <code>Grep</code> drop matching files from their results silently; an explicit <code>Read</code>, <code>Write</code>, or <code>Edit</code> (including the <code>Edit</code> move destination) fails with <code>WORKSPACE_PATH_DENIED</code>, and an outside-path grant does not lift the denial. <code>Bash</code> is not filtered (§6).</p><blockquote><p>Read may be allowed with an explicit permission prompt in a later revision; MVP fails closed.</p></blockquote><h2 id="_4-default-ignore-app" tabindex="-1">4. Default ignore (app) <a class="header-anchor" href="#_4-default-ignore-app" aria-label="Permalink to &quot;4. Default ignore (app)&quot;">​</a></h2><div class="language-gitignore vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">gitignore</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>.git/</span></span>
<span class="line"><span>node_modules/</span></span>
<span class="line"><span>dist/</span></span>
<span class="line"><span>build/</span></span>
<span class="line"><span>.target/</span></span>
<span class="line"><span>target/</span></span>
<span class="line"><span>.venv/</span></span>
<span class="line"><span>venv/</span></span>
<span class="line"><span>__pycache__/</span></span>
<span class="line"><span>.pytest_cache/</span></span>
<span class="line"><span>.mypy_cache/</span></span>
<span class="line"><span>.DS_Store</span></span>
<span class="line"><span>*.log</span></span>
<span class="line"><span>coverage/</span></span>
<span class="line"><span>.turbo/</span></span>
<span class="line"><span>.next/</span></span>
<span class="line"><span>.cache/</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br></div></div><h2 id="_5-workspace-file" tabindex="-1">5. Workspace file <a class="header-anchor" href="#_5-workspace-file" aria-label="Permalink to &quot;5. Workspace file&quot;">​</a></h2><p>Support:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>.pi-desktopignore</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Syntax: gitignore-compatible subset.</p><h2 id="_6-tool-behavior" tabindex="-1">6. Tool behavior <a class="header-anchor" href="#_6-tool-behavior" aria-label="Permalink to &quot;6. Tool behavior&quot;">​</a></h2><table tabindex="0"><thead><tr><th>tool</th><th>ignore application</th></tr></thead><tbody><tr><td>Glob</td><td>unscoped walk: layers 1–4 filter results; explicit <code>path</code>: layer 1 only</td></tr><tr><td>Grep</td><td>unscoped walk: layers 1–4 filter the file set (in-process walker and the system <code>rg</code> fast path alike); explicit <code>path</code>: layer 1 only</td></tr><tr><td>Read</td><td><code>WORKSPACE_PATH_DENIED</code> on a denylisted file; otherwise permission-gated when the explicit path is outside; <code>TOOL_DENIED</code> after denial</td></tr><tr><td>Write/Edit</td><td><code>WORKSPACE_PATH_DENIED</code> on a denylisted file or move destination; otherwise permission-gated when the explicit path is outside; <code>TOOL_DENIED</code> after denial</td></tr><tr><td>Bash</td><td>path sandbox still enforced by host; ignore file does not expand bash powers</td></tr></tbody></table><h2 id="_7-diagnostics" tabindex="-1">7. Diagnostics <a class="header-anchor" href="#_7-diagnostics" aria-label="Permalink to &quot;7. Diagnostics&quot;">​</a></h2><p>Tools should return stable errors:</p><ul><li><code>PATH_OUTSIDE_WORKSPACE</code> — path escapes the workspace root before an outside-path permission decision</li><li><code>TOOL_DENIED</code> — outside-path permission was denied, timed out, or cancelled</li><li><code>WORKSPACE_PATH_DENIED</code> — an explicit path hit the security denylist (see <a href="./08-error-codes">08-error-codes §3.3</a>)</li></ul><p>UI can show “hidden by ignore rules” counts for Glob/Grep optionally later.</p><h2 id="_8-acceptance-criteria" tabindex="-1">8. Acceptance criteria <a class="header-anchor" href="#_8-acceptance-criteria" aria-label="Permalink to &quot;8. Acceptance criteria&quot;">​</a></h2><ul><li>[x] outside paths require permission in non-auto modes and are allowed in Auto</li><li>[x] default ignores hide node_modules from Glob/Grep</li><li>[x] workspace ignore file honored</li><li>[x] security denylist cannot be disabled from UI in MVP</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("spec/03-runtime/15-workspace-ignore-rules.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _15WorkspaceIgnoreRules = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _15WorkspaceIgnoreRules as default
};
