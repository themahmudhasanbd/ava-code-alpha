import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0289: Signed macOS GitHub Releases and in-app update delivery","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0289-signed-macos-github-releases.md","filePath":"adr/0289-signed-macos-github-releases.md","lastUpdated":1789755582000}');
const _sfc_main = { name: "adr/0289-signed-macos-github-releases.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0289-signed-macos-github-releases-and-in-app-update-delivery" tabindex="-1">ADR 0289: Signed macOS GitHub Releases and in-app update delivery <a class="header-anchor" href="#adr-0289-signed-macos-github-releases-and-in-app-update-delivery" aria-label="Permalink to &quot;ADR 0289: Signed macOS GitHub Releases and in-app update delivery&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-18</li><li>Deciders: PI-Desktop core</li><li>Decision: D450</li><li>Amends: ADR 0022, ADR 0145, ADR 0191, ADR 0204, D078</li><li>Related: D120, D126, D164, D364, ADR 0197, ADR 0232, ADR 0257, ADR 0278, E2E-196a, E2E-196c, E2E-067A</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Official GitHub tag releases already produce native macOS arm64 and Intel x64 DMG/ZIP artifacts, merge <code>latest-mac.yml</code>, and ship <code>electron-updater</code>. The default CI lane still disabled identity discovery, skipped notarization, and kept packaged macOS on notify-and-link delivery because unsigned artifacts cannot provide a qualified in-app upgrade. Local packaging without a certificate must stay possible (D078). Contributors must not commit certificate material.</p><p>A Developer ID Application certificate for team <code>DUV63RKYTW</code> is now available for the official <code>vastsa/PI-Desktop</code> release lane.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>Every GitHub tag release (<code>vX.Y.Z</code>) Developer ID-signs the app through electron-builder 26 and notarizes it with <code>xcrun notarytool</code> (<code>-c.mac.notarize=true</code>). Because electron-builder notarizes only the app, the final DMG is submitted separately (<code>xcrun notarytool submit --wait</code>) and must return <code>status: Accepted</code> before <code>xcrun stapler staple</code> may run; the run then verifies the app as <code>Notarized Developer ID</code> and both stapled tickets before upload. Stapler retries are bounded and only allowed after Apple accepts. Missing signing or notarization secrets fail the job; unsigned macOS artifacts must not be published from a tag.</li><li>The signing certificate is <code>Developer ID Application: XingYu Liu (DUV63RKYTW)</code>; Apple team id <code>DUV63RKYTW</code>. CI pins it with <code>CSC_NAME=XingYu Liu (DUV63RKYTW)</code>. The name must be the bare common name: electron-builder 26 rejects an identity that keeps the <code>Developer ID Application:</code> prefix, and the verification step re-adds that prefix when it compares the <code>codesign</code> authority.</li><li>Certificate material stays in GitHub Actions secrets: <code>CSC_LINK</code> (p12, file path or base64), <code>CSC_KEY_PASSWORD</code>, <code>APPLE_ID</code>, <code>APPLE_APP_SPECIFIC_PASSWORD</code>, and <code>APPLE_TEAM_ID</code>. Nothing of that set is committed, echoed, or written into electron-builder config.</li><li>Local <code>pnpm dist:mac</code> / <code>pnpm pack</code> remain unsigned when no identity is configured. <code>scripts/release-macos.sh</code> remains the local signed lane. <code>workflow_dispatch</code> may set <code>sign_macos: false</code> only to produce unsigned debug artifacts; that path must not be used for a GitHub Release tag.</li><li>Packaged macOS uses the existing in-app <code>electron-updater</code> lane (ZIP + merged <code>latest-mac.yml</code> + <code>quitAndInstall</code>), same as Windows NSIS and Linux AppImage. Linux deb/rpm and Windows portable stay notify-and-link. Renderer IPC, feed ownership, <code>allowPrerelease = false</code>, and auto-check timing are unchanged.</li><li>Do not reintroduce <code>afterPack</code> / <code>afterSign</code> adhoc codesign (ADR 0278). electron-builder&#39;s Developer ID pass signs the app, helpers, and the <code>pi-desktop-host-core</code> sidecar.</li><li>The unsigned first-launch note and ZIP helper remain for trusted local or debug unsigned builds. Official GitHub Release DMGs are signed and notarized and must not claim otherwise.</li><li>Hardened runtime stays on. Entitlements stay the minimum required set: V8 JIT, unsigned executable memory, library-validation disable (Electron helpers and plugin-loaded native addons), and microphone input for the existing plugin capture permission (ADR 0257), with <code>NSMicrophoneUsageDescription</code> in Info.plist.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Users who download a tagged DMG should open PI-Desktop without a Gatekeeper “unidentified developer” or quarantine-damaged warning.</li><li>Packaged macOS installs can check GitHub Releases, download the arch ZIP, and restart into the new version. Existing unsigned installs may still need one manual signed DMG before in-app updates succeed.</li><li>Windows and Linux packaging, artifact names, and updater modes are unchanged.</li><li>Operators must create the five Actions secrets before the next tag.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li>Keep unsigned tag artifacts and opt-in signing: rejected; it leaves production users on the Gatekeeper warning path.</li><li>Hard-code the identity in <code>apps/desktop/package.json</code>: rejected; local packaging without the certificate must keep working (D078).</li><li>Apple API key (<code>APPLE_API_KEY</code>) instead of Apple ID + app-specific password: deferred; the existing electron-builder 26 Apple ID path is already wired.</li><li>Separate macOS updater implementation: rejected; reuse <code>electron-updater</code>.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0289-signed-macos-github-releases.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0289SignedMacosGithubReleases = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0289SignedMacosGithubReleases as default
};
