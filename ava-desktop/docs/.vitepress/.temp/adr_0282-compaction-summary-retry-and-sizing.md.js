import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0282: Retry and right-size the compaction summary before retained-tail recovery","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0282-compaction-summary-retry-and-sizing.md","filePath":"adr/0282-compaction-summary-retry-and-sizing.md","lastUpdated":1790057120000}');
const _sfc_main = { name: "adr/0282-compaction-summary-retry-and-sizing.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0282-retry-and-right-size-the-compaction-summary-before-retained-tail-recovery" tabindex="-1">ADR 0282: Retry and right-size the compaction summary before retained-tail recovery <a class="header-anchor" href="#adr-0282-retry-and-right-size-the-compaction-summary-before-retained-tail-recovery" aria-label="Permalink to &quot;ADR 0282: Retry and right-size the compaction summary before retained-tail recovery&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-18</li><li>Deciders: PI-Desktop runtime maintainers</li><li>Amends: ADR 0049 (decision 1, the preflight guard; the &quot;retry indefinitely&quot; rejection stands), D203 / ADR 0064 (the summary family only)</li><li>Amended by: ADR 0302 (a prompt that still does not fit after the reduced pass is summarized in chunks; only a range the planner cannot split falls back)</li><li>Related: issue #543 · PR #554 (superseded) · <a href="./../spec/03-runtime/02-agent-runtime">03-runtime/02-agent-runtime</a> · <a href="./../spec/03-runtime/01-ipc-protocol">03-runtime/01-ipc-protocol</a> · E2E-084</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>ADR 0049 made an automatic compaction failure survivable: when the summary request fails, the runtime writes a retained-tail checkpoint (previous summary if any, a fixed recovery notice, and a bounded tail) and the run continues. Issue #543 reports that on real long sessions this fallback is the common outcome rather than the exception: six of ten checkpoints on the reporter&#39;s machine were the ~112-token recovery notice, and the transcript row labelled every one of them <code>summary ≈112 tokens</code>.</p><p>Three things in the summary path made a fallback far more likely than the provider&#39;s actual failure rate:</p><ol><li><strong>No retry on the summary request.</strong> <code>compact()</code> was called with no <code>RetryPolicy</code>, so pi-ai returned the first failed response as-is. The main turn&#39;s provider requests already retry transient failures through the <code>streamFn</code> wrapper (D186), but pi-agent-core&#39;s summary request goes through <code>Models.completeSimple</code> and never reaches that wrapper. One dropped stream or 503 discarded the whole summary.</li><li><strong>The preflight guard measured the wrong thing.</strong> It summed <code>estimateTokens</code> over the raw messages, but pi-agent-core serializes the conversation into one text prompt and caps every tool result at 2 000 characters while doing so. A tool-heavy session looked several times larger than the prompt it would actually send, and the guard skipped the model for summaries that would have fit. The reporter&#39;s <code>tokensBefore</code> values (~200k–885k on a 200k window) are exactly this shape.</li><li><strong>The UI could not tell a fallback from a summary.</strong> <code>ContextCompactionMark</code> only distinguishes the <code>fresh_window</code> rollover; a retained-tail checkpoint rendered as a successful summary of N tokens.</li></ol><p>PR #554 proposed an outer retry loop around <code>buildCheckpoint</code> and a shrink step that dropped the oldest messages from the summary input. The loop retried every <code>recoverable</code> failure including deterministic ones (quota, auth, &quot;no new context&quot;), and dropping messages silently narrowed what the checkpoint claimed to summarize. Its direction — retry, then reduce, then fall back — is kept here; those two mechanisms are not.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><strong>The summary request retries transient failures, bounded.</strong><code>generateCompaction</code> passes pi-ai a <code>RetryPolicy</code> of three retries with 2 s / 4 s / 8 s backoff (<code>COMPACTION_SUMMARY_RETRY_POLICY</code>). pi-ai&#39;s own classifier decides what is transient: overload, 429/5xx, dropped streams, timeouts, and connection resets retry; quota, billing, auth, and malformed requests return on the first attempt. The backoff sleeps honour the compaction abort signal, so Stop still cancels immediately. A flapping provider costs at most ~14 s before the ADR 0049 fallback runs; ADR 0049&#39;s rejection of unbounded retry stands.</li><li><strong>The preflight guard sizes the prompt pi will send.</strong><code>compactionSummaryWouldExceedBudget</code> serializes the input with pi&#39;s own <code>convertToLlm</code> + <code>serializeConversation</code> (tool results already capped) and applies the four-characters-per-token heuristic the rest of the runtime uses. A split turn counts the larger of its two requests. The limit itself (window − output allowance − safety margin) is unchanged.</li><li><strong>One bounded reduction before giving up.</strong> When the full prompt still exceeds the limit, the runtime tries exactly one reduced input: every tool result cut to a 500-character prefix with a visible marker, assistant thinking dropped. User text, assistant text, and tool-call arguments are never touched, and no message is removed, so the summary still covers every message the checkpoint files behind its boundary. If the reduced prompt still does not fit, or nothing was reducible, the ADR 0049 fallback runs as before. The checkpoint&#39;s <code>messagesToSummarize</code> and <code>retainedTail</code> are the originals; only the request payload is reduced.</li><li><strong>The mark says when a checkpoint is a fallback.</strong> <code>ContextCompactionMark</code> gains an optional <code>fallback?: &quot;retained_tail&quot;</code>, derived from the persisted <code>details.fallback</code> the same way <code>summarized</code> is derived from <code>details.strategy</code>. The transcript row renders such a mark as &quot;summary generation failed · recent context retained&quot; instead of <code>summary ≈N tokens</code>; the inspector line is unchanged. The field is additive: older marks without it render exactly as before, and no record schema, protocol version, or host-core change is needed.</li></ol><p>Manual <code>/compact</code> inherits the retry and sizing (it is the same request) and keeps its fail-fast, no-fallback semantics. The <code>fresh_window</code> family issues no summary request and is untouched.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Sessions on a flapping provider keep a real model summary far more often; the fallback is reserved for sustained failures and inputs that cannot be reduced under the window.</li><li>Tool-heavy sessions no longer skip the summary because of a raw-size estimate that pi&#39;s serialization would never have sent.</li><li>A compaction can now take up to ~14 s longer on a sustained outage before the fallback lands. The compacting activity state already covers this; Stop aborts the backoff immediately.</li><li>The reduced prompt can produce a thinner summary of tool output than the full one would; it is still a model summary of the complete message range, which is strictly better than the recovery notice it replaces.</li><li>The transcript row is honest about fallbacks. Users who saw <code>summary ≈112 tokens</code> will now see the failure label on the same rows, including historical ones, because the mark is derived from persisted details on session open.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><h3 id="outer-retry-loop-around-buildcheckpoint-pr-554" tabindex="-1">Outer retry loop around <code>buildCheckpoint</code> (PR #554) <a class="header-anchor" href="#outer-retry-loop-around-buildcheckpoint-pr-554" aria-label="Permalink to &quot;Outer retry loop around \`buildCheckpoint\` (PR #554)&quot;">​</a></h3><p>Rejected. It re-ran preparation and retried every recoverable failure including deterministic ones, and could not tell a transient provider error from &quot;no new context to compact&quot; without re-implementing pi-ai&#39;s classifier. The policy hook on <code>compact()</code> already exists for exactly this.</p><h3 id="shrink-by-dropping-the-oldest-messages-pr-554" tabindex="-1">Shrink by dropping the oldest messages (PR #554) <a class="header-anchor" href="#shrink-by-dropping-the-oldest-messages-pr-554" aria-label="Permalink to &quot;Shrink by dropping the oldest messages (PR #554)&quot;">​</a></h3><p>Rejected. The checkpoint&#39;s <code>throughMessageId</code> still covered the dropped messages, so the summary silently claimed a range it had not seen. Reducing tool output keeps the range intact.</p><h3 id="map-reduce-summarization-for-oversized-inputs" tabindex="-1">Map-reduce summarization for oversized inputs <a class="header-anchor" href="#map-reduce-summarization-for-oversized-inputs" aria-label="Permalink to &quot;Map-reduce summarization for oversized inputs&quot;">​</a></h3><p>Deferred. It is the right answer for inputs that do not fit even reduced, but it changes the summary prompt contract and needs its own budget model. The fallback remains for that case; #543&#39;s reported failures fit after reduction.</p><h3 id="retry-inside-the-runtime-with-provider-retry-ts" tabindex="-1">Retry inside the runtime with <code>provider-retry.ts</code> <a class="header-anchor" href="#retry-inside-the-runtime-with-provider-retry-ts" aria-label="Permalink to &quot;Retry inside the runtime with \`provider-retry.ts\`&quot;">​</a></h3><p>Rejected. That module wraps <code>streamFn</code> and classifies streamed events; the summary is a <code>completeSimple</code> call that pi-agent-core builds itself. Using pi-ai&#39;s policy keeps one retry implementation per request shape.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0282-compaction-summary-retry-and-sizing.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0282CompactionSummaryRetryAndSizing = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0282CompactionSummaryRetryAndSizing as default
};
