import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0100: Make builtin subagents inherit the parent permission mode","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0100-builtin-subagents-inherit-parent-permission-mode.md","filePath":"adr/0100-builtin-subagents-inherit-parent-permission-mode.md","lastUpdated":1788992254000}');
const _sfc_main = { name: "adr/0100-builtin-subagents-inherit-parent-permission-mode.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0100-make-builtin-subagents-inherit-the-parent-permission-mode" tabindex="-1">ADR 0100: Make builtin subagents inherit the parent permission mode <a class="header-anchor" href="#adr-0100-make-builtin-subagents-inherit-the-parent-permission-mode" aria-label="Permalink to &quot;ADR 0100: Make builtin subagents inherit the parent permission mode&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-08-18</li><li>Deciders: PI-Desktop core</li><li>Related: D115, D242, ADR 0057, ADR 0089</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The proactive delegation decision gave the builtin <code>fixer</code> an explicit <code>permission: accept-edits</code> scope. That made workspace <code>Write</code> and <code>Edit</code> calls convenient when the parent session was in <code>ask</code>, but it also replaced the parent&#39;s effective permission mode for every other call. In a parent session set to <code>auto</code>, a <code>fixer</code> call to <code>Glob</code> or <code>Write</code> an explicit path outside the session workspace therefore opened a permission card even though <code>auto</code> is supposed to allow that path. The card correctly identified the request as coming from <code>fixer</code>; the incorrect part was the builtin&#39;s unexpected narrower override.</p><p>The external-path gate remains an intentional capability boundary. The permission mode is what decides whether that boundary needs a card, and an explicit scope on a user-owned definition must remain meaningful.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Builtin subagent definitions use the default <code>permission: inherit</code> behavior. The builtin <code>fixer</code> no longer declares <code>permission: accept-edits</code>; its available tools remain <code>[Read, Glob, Grep, Edit, Write, Bash]</code> and its workspace/path containment rules do not change.</p><p>With no scope attached to a delegate <code>tools.execute</code> call, host-core resolves the call under the parent session&#39;s effective permission mode:</p><ul><li><code>ask</code> keeps approval for high-risk and explicit external-path calls;</li><li><code>accept-edits</code> auto-allows only in-root <code>Write</code>/<code>Edit</code> and keeps the other approval boundaries;</li><li><code>auto</code> auto-allows the same calls the parent could make, including explicit external paths.</li></ul><p>An explicitly declared non-<code>inherit</code> scope on an eligible builtin or user subagent remains an intentional override. Project definitions still cannot use a permission declaration to escalate beyond the session mode.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A parent in <code>auto</code> can use builtin <code>fixer</code> without an unexpected subagent authorization card.</li><li>A parent in <code>ask</code> or <code>accept-edits</code> is not silently made more permissive by delegation.</li><li>The builtin fixer may prompt for its writes in <code>ask</code>; callers that need a different posture can use an explicit user definition scope.</li><li>Host-core&#39;s external-path containment and permission evaluation do not need a special subagent exception.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Keep <code>fixer</code> at <code>accept-edits</code>:</strong> rejected because it reproduces the observed auto-mode popup and makes a built-in delegate ignore the user&#39;s selected permission posture.</li><li><strong>Change <code>fixer</code> to <code>auto</code>:</strong> rejected because it would bypass approval even when the parent session is <code>ask</code>.</li><li><strong>Ignore every delegate scope when the parent is <code>auto</code>:</strong> rejected because an explicit user-owned <code>ask</code> scope is a deliberate stricter policy and must remain enforceable.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0100-builtin-subagents-inherit-parent-permission-mode.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0100BuiltinSubagentsInheritParentPermissionMode = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0100BuiltinSubagentsInheritParentPermissionMode as default
};
