import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0275: A floating widget placement for plugin panels","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0275-plugin-panel-floating-widget.md","filePath":"adr/0275-plugin-panel-floating-widget.md","lastUpdated":1789583553000}');
const _sfc_main = { name: "adr/0275-plugin-panel-floating-widget.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0275-a-floating-widget-placement-for-plugin-panels" tabindex="-1">ADR 0275: A floating widget placement for plugin panels <a class="header-anchor" href="#adr-0275-a-floating-widget-placement-for-plugin-panels" aria-label="Permalink to &quot;ADR 0275: A floating widget placement for plugin panels&quot;">​</a></h1><ul><li>Status: Accepted for implementation</li><li>Date: 2026-09-17</li><li>Deciders: PI-Desktop core</li><li>Related: <a href="./0081-host-owned-plugin-panel-chrome">ADR 0081</a> · <a href="./0092-plugin-owned-panel-surface">ADR 0092</a> · <a href="./0093-plugin-panel-strict-drag-band">ADR 0093</a> · <a href="./0110-plugin-panel-chrome-spacing-contract">ADR 0110</a> · <a href="./../spec/07-plugins/03-plugin-api">07-plugins/03-plugin-api</a></li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Every plugin panel is a frameless <code>BrowserWindow</code> that reserves a 46px host drag band and carries one three-control capsule in its top-right corner (ADR 0092, ADR 0093, ADR 0110). That grammar suits a panel that shows content, and does not suit a plugin whose whole interface is a small round companion — a voice orb, a timer, a status light.</p><p>Such a plugin cannot exist today. The host paints an opaque background, forces a 360×280 minimum, and draws the capsule over the top of the page. What an author gets is a rectangle with a toolbar strip, not a floating sphere; the transparent strip that makes the panel chrome work reads as a seam around a shape that is not a rectangle at all. Electron can already host a transparent window, and the plugin surface itself — preload bridge, session partition, permission gate, egress policy — needs no change to allow it.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>A manifest may declare <code>&quot;ui&quot;: { &quot;shape&quot;: &quot;widget&quot; }</code>. <code>&quot;panel&quot;</code> stays the default; an absent or <code>&quot;panel&quot;</code> value keeps today&#39;s behavior unchanged.</li><li>A widget window is <code>transparent: true</code> with a fully transparent <code>backgroundColor</code>, <code>hasShadow: false</code>, <code>frame: false</code>, <code>maximizable: false</code>, <code>fullscreenable: false</code>, and <code>skipTaskbar: true</code>. The plugin draws its own silhouette, shadow, and glow.</li><li>A widget has no 46px band and no capsule. The preload publishes <code>--pi-plugin-titlebar-height: 0px</code>, never applies the legacy additive offset, and instead installs a drag map over the whole window: empty space drags, while standard controls and every element marked <code>data-pi-plugin-no-drag</code> stay clickable. The mechanics are the paint-through segment map of ADR 0093, applied to the full rectangle.</li><li>The capsule is the panel&#39;s only close affordance (ADR 0093 §4), so the host owns an equivalent menu behind the widget surface&#39;s own context menu: close, minimize, and an always-on-top toggle. It travels on the existing sender-validated window-control channel as one new <code>contextMenu</code> action, so <code>window.pluginBridge</code> still gains no window primitive.</li><li><code>ui.width</code> / <code>ui.height</code> are honoured down to 120×120 for a widget (a panel&#39;s minimum stays 360×280). <code>ui.alwaysOnTop</code> defaults to <code>false</code>; <code>ui.resizable</code> defaults to <code>false</code> for a widget and <code>true</code> for a panel.</li><li>The preload publishes the placement as <code>document.documentElement.dataset.piPluginPanelShape</code> (<code>panel</code> | <code>widget</code> | <code>view</code>) before page scripts run, so one HTML entry can serve every placement without a bridge round trip.</li><li>The plugin surface is otherwise unchanged: same preload, same <code>pluginBridge</code> channels, same permission gate, same per-plugin session partition, same egress policy, same localization rules. A widget needs no new permission.</li><li><code>manifest.ui.shape</code>, <code>ui.alwaysOnTop</code>, and <code>ui.resizable</code> are validated at install by the Plugin SDK, carried in the shared manifest type, and parsed by host-core&#39;s <code>PluginUiMeta</code>, so the Rust catalog and the TypeScript host agree on the contract.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A plugin can be a floating orb: transparent, small, always-on-top when it asks for it, draggable from its empty space, and closable from a host menu even when the plugin never drew a close button.</li><li>The 46px band, the capsule, and the v2 spacing contract stay exactly as they are for panels and docked views; existing plugins and installed packages are unaffected.</li><li>Placement is a per-plugin manifest choice, not a per-page one: the same HTML entry may serve a panel, a widget, and a docked view by reading the published placement and the titlebar variable.</li><li>A widget surface has one more pointer rule to document: an element that must receive clicks is either a standard control or carries <code>data-pi-plugin-no-drag</code>; everything else drags the window.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0275-plugin-panel-floating-widget.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0275PluginPanelFloatingWidget = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0275PluginPanelFloatingWidget as default
};
