import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0048: Lazy per-turn tool activation","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0048-lazy-per-turn-tool-activation.md","filePath":"adr/0048-lazy-per-turn-tool-activation.md","lastUpdated":1789117120000}');
const _sfc_main = { name: "adr/0048-lazy-per-turn-tool-activation.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0048-lazy-per-turn-tool-activation" tabindex="-1">ADR 0048: Lazy per-turn tool activation <a class="header-anchor" href="#adr-0048-lazy-per-turn-tool-activation" aria-label="Permalink to &quot;ADR 0048: Lazy per-turn tool activation&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-08-02</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>PI-Desktop currently registers the product tools, plugin tools, skills, and plugin-development helpers before the first provider request. The complete JSON schemas are then serialized into that request even when the prompt is a greeting or a read-only task. This makes the first input disproportionately large and repeats the same optional schemas on every new user turn.</p><p>pi&#39;s coding-agent surface separates the active tool set from the complete registry and uses compact prompt snippets for capabilities that are not active. pi-agent-core also supports replacing the context at a turn boundary and marking tool results with <code>addedToolNames</code>; pi-ai adapters can use that marker for native deferred-tool search when the provider supports it.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>PI-Desktop keeps a complete sidecar-local tool registry but sends an active subset to the provider:</p><ul><li>Agent starts with <code>Read</code>, <code>Bash</code>, <code>Edit</code>, and <code>Write</code>, matching pi&#39;s coding-agent core.</li><li>Chat starts with <code>Read</code>, <code>Glob</code>, and <code>Grep</code>.</li><li>A local <code>ToolSearch</code> tool remains active when deferred capabilities exist. <em>(ADR 0061 removed the <code>CompactContext</code> tool that this list also kept always-active; ADR 0064 restores it as <code>new_context</code>, again always-active in every mode.)</em></li><li>Agent-mode <code>Glob</code> and <code>Grep</code>, <code>BrowserPreview</code>, plugin tools, <code>Skill</code>, and plugin-development helpers are deferred until requested.</li></ul><p>The base prompt contains a bounded <code># On-demand tools</code> catalog with names and compact one-line descriptions, never the deferred JSON parameter schemas. The model calls <code>ToolSearch</code> with an exact name or a short capability query. The sidecar ranks matches, activates at most four, returns their names in <code>addedToolNames</code>, and rebuilds the context before the next provider request. Providers with native deferred-tool search can serialize the newly activated schemas at that load point; providers without it receive the active schemas through the ordinary tool list.</p><p>The in-memory deferred set is cleared at the start of every new user prompt and then rebuilt from successful activation evidence in the effective session context. A successful <code>ToolSearch</code> result contributes its <code>addedToolNames</code>, and a successful result from a deferred tool contributes that tool&#39;s name. Only names still present in the current mode&#39;s deferred catalog are restored; failed, interrupted, and missing-result rows are ignored, as is assistant/user prose. ToolSearch does not call host-core and does not bypass permissions, workspace containment, timeouts, or audit behavior. The activation marker is retained inside the persisted tool result so transcript reconstruction preserves provider message semantics. A restarted runtime can reuse an eligible deferred capability while its successful marker remains in effective context; a fresh ToolSearch call is required when no such marker is present.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Simple first turns no longer pay for every optional tool schema.</li><li>Core coding workflows keep pi&#39;s Read/Bash/Edit/Write set without an extra discovery call; file enumeration and search remain one ToolSearch away.</li><li>A task that needs an ancillary capability incurs one explicit ToolSearch turn before that capability is available.</li><li>ToolSearch is a normal model tool activity row, so the discovery step is visible and durable rather than an opaque side effect.</li><li>Provider compatibility remains centralized in pi-ai: native deferred search is an optimization, while the active-context fallback works for every adapter.</li><li>Plugin descriptions are bounded in the prompt catalog, and large plugin registries cannot reintroduce the original schema flood.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><h3 id="send-every-schema-on-every-request" tabindex="-1">Send every schema on every request <a class="header-anchor" href="#send-every-schema-on-every-request" aria-label="Permalink to &quot;Send every schema on every request&quot;">​</a></h3><p>Rejected because it wastes first-turn context on capabilities the model does not need and grows with the plugin registry.</p><h3 id="activate-tools-from-a-prompt-text-heuristic" tabindex="-1">Activate tools from a prompt-text heuristic <a class="header-anchor" href="#activate-tools-from-a-prompt-text-heuristic" aria-label="Permalink to &quot;Activate tools from a prompt-text heuristic&quot;">​</a></h3><p>Rejected because user wording and project instructions are not a reliable capability classifier, and a missed heuristic would hide a valid tool.</p><h3 id="use-only-provider-native-tool-search" tabindex="-1">Use only provider-native tool search <a class="header-anchor" href="#use-only-provider-native-tool-search" aria-label="Permalink to &quot;Use only provider-native tool search&quot;">​</a></h3><p>Rejected because not every configured provider supports it. The sidecar needs a provider-independent active-tool contract, with native search as an adapter optimization.</p><h2 id="references" tabindex="-1">References <a class="header-anchor" href="#references" aria-label="Permalink to &quot;References&quot;">​</a></h2><ul><li><code>docs/spec/03-runtime/02-agent-runtime.md</code></li><li><code>docs/spec/03-runtime/03-tools-and-permissions.md</code></li><li><code>docs/spec/06-delivery/04-e2e-test-plan.md</code> (E2E-008a)</li><li><code>https://github.com/badlogic/pi-mono/blob/main/packages/coding-agent/src/core/system-prompt.ts</code></li><li><code>https://github.com/badlogic/pi-mono/blob/main/packages/coding-agent/docs/extensions.md</code></li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0048-lazy-per-turn-tool-activation.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0048LazyPerTurnToolActivation = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0048LazyPerTurnToolActivation as default
};
