import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0178: Per-provider custom HTTP headers","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0178-per-provider-custom-headers.md","filePath":"adr/0178-per-provider-custom-headers.md","lastUpdated":1788849585000}');
const _sfc_main = { name: "adr/0178-per-provider-custom-headers.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0178-per-provider-custom-http-headers" tabindex="-1">ADR 0178: Per-provider custom HTTP headers <a class="header-anchor" href="#adr-0178-per-provider-custom-http-headers" aria-label="Permalink to &quot;ADR 0178: Per-provider custom HTTP headers&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-08</li><li>Deciders: PI-Desktop core</li><li>Amends ADR 0176 / ADR 0095 / ADR 0156</li><li>Supersedes the User-Agent-only surface of ADR 0176</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>ADR 0176 added a per-row User-Agent override because gateways inspect that header and Codex overwrites it after extra headers. Users then needed the same last-writer path for other non-secret request headers (routing, tenant, or client identification), not a dedicated User-Agent field. The provider schema already listed <code>headers</code>; 0176 left that map unimplemented and said User-Agent would win if both existed.</p><p>The Advanced editor for a single User-Agent field sat in a two-column grid and looked sparse.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Each provider row — API-key AI service and OAuth vendor account — may store an optional <code>headers</code> map in <code>config_json.headers</code>.</p><ul><li>Empty, omitted, or update <code>{}</code> keeps today&#39;s adapter defaults.</li><li>A non-empty map is last-writer on that row&#39;s outbound HTTP: session turns, builtin subagents, prompt enhancement, plugin one-shots, <code>/models</code> discovery (including unsaved form values), connection tests, and OAuth token refresh.</li><li>A fetch wrapper is the last writer so Codex and the Anthropic SDK cannot overwrite the map. The same values are also placed on pi-ai stream-option headers so OpenCode&#39;s caller-wins rule stays true.</li><li>Stored <code>config_json.userAgent</code> migrates to <code>headers[&quot;User-Agent&quot;]</code> on read. Writing <code>headers</code> drops leftover <code>userAgent</code>. If both exist, <code>headers</code> wins for a User-Agent key already in the map.</li><li>Host validation: trim keys and values; case-insensitive unique keys; at most 32 headers; key ≤ 256 bytes; value ≤ 4096 bytes; no CR/LF; header names are alphanumeric plus hyphen. Reserved keys are rejected so this cannot smash signing or hop-by-hop framing: <code>authorization</code>, <code>proxy-authorization</code>, <code>host</code>, <code>content-type</code>, <code>content-length</code>, <code>cookie</code>, <code>set-cookie</code>, <code>connection</code>, <code>transfer-encoding</code>, <code>te</code>, <code>trailer</code>, <code>upgrade</code>, <code>keep-alive</code>, <code>x-api-key</code>, <code>api-key</code>, <code>chatgpt-account-id</code>, <code>x-opencode-session</code>.</li><li>Not a secret. No SQLite or host-protocol version bump.</li><li>UI is an explicit Advanced settings button in the upper-right dialog actions (named, custom, and vendor account) that opens a separate compact modal. The modal offers common header presets including <code>User-Agent</code>, Copy JSON of the same normalized record used for persistence, JSON import for either a direct header object or <code>{ &quot;headers&quot;: { ... } }</code>, and case-insensitive merge without duplicate rows. At most two header rows are visible; additional rows scroll inside the editor. Named display name stays above the header controls. First OAuth login does not collect headers; they are edited after the account exists.</li><li><code>AgentRuntime.matches()</code> includes the header map so editing it rebuilds the warm runtime.</li></ul><p>Overriding Anthropic OAuth&#39;s <code>claude-cli/…</code> User-Agent can make Claude Pro/Max reject the request. That is the user&#39;s choice.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Users can impersonate another client and attach gateway-specific headers per service or account without a global header dump or an auth-bearing editor.</li><li>OAuth login HTTP uses the row map only after it is saved; first-login traffic keeps pi-ai defaults.</li><li>A previously saved User-Agent still applies until the row is next saved, at which point it is stored only in <code>headers</code>.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0178-per-provider-custom-headers.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0178PerProviderCustomHeaders = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0178PerProviderCustomHeaders as default
};
