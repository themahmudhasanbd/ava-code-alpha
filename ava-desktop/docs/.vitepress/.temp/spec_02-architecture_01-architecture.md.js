import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"01. Architecture","description":"","frontmatter":{},"headers":[],"relativePath":"spec/02-architecture/01-architecture.md","filePath":"spec/02-architecture/01-architecture.md","lastUpdated":1788979471000}');
const _sfc_main = { name: "spec/02-architecture/01-architecture.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="_01-architecture" tabindex="-1">01. Architecture <a class="header-anchor" href="#_01-architecture" aria-label="Permalink to &quot;01. Architecture&quot;">​</a></h1><h2 id="_1-overview" tabindex="-1">1. Overview <a class="header-anchor" href="#_1-overview" aria-label="Permalink to &quot;1. Overview&quot;">​</a></h2><p>PI-Desktop uses a layered desktop architecture:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>┌──────────────────────────────────────────────────────────┐</span></span>
<span class="line"><span>│ Renderer (React UI, English-first i18n) │</span></span>
<span class="line"><span>│ - chat / sessions / settings / plugins / command palette│</span></span>
<span class="line"><span>│ - no Node integration │</span></span>
<span class="line"><span>└───────────────────────────▲──────────────────────────────┘</span></span>
<span class="line"><span> │ preload IPC</span></span>
<span class="line"><span>┌───────────────────────────┴──────────────────────────────┐</span></span>
<span class="line"><span>│ Electron Main (thin orchestrator) │</span></span>
<span class="line"><span>│ - window lifecycle │</span></span>
<span class="line"><span>│ - IPC routing │</span></span>
<span class="line"><span>│ - process supervision │</span></span>
<span class="line"><span>│ - optional loopback MCP control │</span></span>
<span class="line"><span>└───────────────▲─────────────────────────────▲────────────┘</span></span>
<span class="line"><span> │ local RPC │ process bridge</span></span>
<span class="line"><span>┌───────────────┴──────────────┐ ┌──────────┴────────────┐</span></span>
<span class="line"><span>│ Rust Host Core │ │ Node pi Agent Sidecar │</span></span>
<span class="line"><span>│ - tools + sandbox │ │ - pi-ai │</span></span>
<span class="line"><span>│ - permission gateway │ │ - pi-agent-core │</span></span>
<span class="line"><span>│ - plugin host services │◄─►│ - turn orchestration │</span></span>
<span class="line"><span>│ - persistence adapters │ │ - provider streaming │</span></span>
<span class="line"><span>│ - secrets adapter │ └───────────────────────┘</span></span>
<span class="line"><span>└──────────────────────────────┘</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br><span class="line-number">19</span><br><span class="line-number">20</span><br><span class="line-number">21</span><br><span class="line-number">22</span><br></div></div><h2 id="_2-design-principles" tabindex="-1">2. Design principles <a class="header-anchor" href="#_2-design-principles" aria-label="Permalink to &quot;2. Design principles&quot;">​</a></h2><ol><li><strong>UI and privileged runtime are separated</strong></li><li><strong>Rust owns host/system capabilities, durable mode, and approval policy</strong></li><li><strong>pi owns model/agent loop semantics</strong></li><li><strong>Renderer is unprivileged</strong></li><li><strong>All cross-boundary contracts are typed</strong></li><li><strong>English is the product source language</strong></li><li><strong>Plan is a state of the one pi Agent, never a second planner</strong></li></ol><h2 id="_3-subsystems" tabindex="-1">3. Subsystems <a class="header-anchor" href="#_3-subsystems" aria-label="Permalink to &quot;3. Subsystems&quot;">​</a></h2><h3 id="_3-1-app-shell-electron" tabindex="-1">3.1 App Shell (Electron) <a class="header-anchor" href="#_3-1-app-shell-electron" aria-label="Permalink to &quot;3.1 App Shell (Electron)&quot;">​</a></h3><ul><li>windows/menus</li><li>app lifecycle</li><li>fixed-feed update check/download/install lifecycle</li><li>process boot order</li></ul><p>Electron Main exclusively owns the update client and fixed GitHub Releases target. The renderer can request allowlisted operations and render typed state, but cannot supply a feed URL or access the updater directly. App updates do not pass through Rust host-core or the agent sidecar (D120 / ADR 0022).</p><h3 id="_3-2-ui-react" tabindex="-1">3.2 UI (React) <a class="header-anchor" href="#_3-2-ui-react" aria-label="Permalink to &quot;3.2 UI (React)&quot;">​</a></h3><ul><li>session UX</li><li>streaming transcript</li><li>permission cards</li><li>settings</li><li>plugin manager UI</li><li>command palette</li></ul><h3 id="_3-3-rust-host-core" tabindex="-1">3.3 Rust Host Core <a class="header-anchor" href="#_3-3-rust-host-core" aria-label="Permalink to &quot;3.3 Rust Host Core&quot;">​</a></h3><ul><li>workspace path enforcement</li><li>builtin tool execution</li><li>permission policy evaluation</li><li>durable session mode resolution (<code>agent | plan</code>)</li><li>plan approval records, requests, and atomic Plan → Agent transition</li><li>plugin install/registry/lifecycle services</li><li>sqlite adapters / secure storage glue</li><li>audit logs</li></ul><h3 id="_3-4-node-pi-agent-runtime" tabindex="-1">3.4 Node pi Agent Runtime <a class="header-anchor" href="#_3-4-node-pi-agent-runtime" aria-label="Permalink to &quot;3.4 Node pi Agent Runtime&quot;">​</a></h3><ul><li>model catalog/provider setup</li><li><code>Agent.prompt/abort</code></li><li>event normalization from pi events</li><li>tool call requests emitted to host core</li><li>one-Agent planning state, host-written Plan checkpoint submission, and approve/reject execution boundary</li></ul><h3 id="_3-5-plugin-system" tabindex="-1">3.5 Plugin System <a class="header-anchor" href="#_3-5-plugin-system" aria-label="Permalink to &quot;3.5 Plugin System&quot;">​</a></h3><ul><li>manifest validation</li><li>contribution registry (commands/tools/skills)</li><li>plugin panels</li><li>permission grants</li></ul><h3 id="_3-6-local-mcp-control-plane" tabindex="-1">3.6 Local MCP control plane <a class="header-anchor" href="#_3-6-local-mcp-control-plane" aria-label="Permalink to &quot;3.6 Local MCP control plane&quot;">​</a></h3><p>When <code>PI_DESKTOP_MCP_CONTROL=1</code> is set, Electron Main starts an optional Streamable HTTP MCP server on <code>127.0.0.1</code>. The server exposes named tools for the common project/session/Agent/workspace flows and a reviewed catalog of generic desktop operations. Each call delegates to the same registered main process IPC handler used by the renderer; it does not create a second permission or persistence implementation.</p><p>The server creates a persistent bearer token and a connection manifest in the Electron user-data directory. It never binds a non-loopback address, exposes no secret channels or secret-write provider/OAuth/MCP paths, and does not expose renderer-only native pickers. Dangerous generic operations and <code>session/configure</code> require <code>confirm: true</code> as an agent acknowledgement. Successful <strong>mutating</strong> project/session calls reuse the existing renderer session-change event so an external Agent and the visible desktop converge on the same active state. This is a local automation surface, not the deferred remote Gateway / WebUI architecture.</p><h3 id="_3-7-remote-agent-control-target-post-mvp" tabindex="-1">3.7 Remote Agent Control target (post-MVP) <a class="header-anchor" href="#_3-7-remote-agent-control-target-post-mvp" aria-label="Permalink to &quot;3.7 Remote Agent Control target (post-MVP)&quot;">​</a></h3><p>Remote control is specified separately in <a href="./05-remote-agent-control">05-remote-agent-control</a>. The target introduces a headless Agent Host module above the existing sidecars and exposes a transport-neutral RACP contract: WebSocket JSON-RPC is the normative v1 binding, HTTP/JSON + SSE is its browser profile, and gRPC is reserved (D374). It does not expose Electron IPC, <code>host.proxy</code>, or host-core RPC, and it does not change the current MVP exclusion of a remote Gateway. The first implementation hosts the module inside Electron Main, where desktop IPC, local MCP, and RACP call it. The first remote deployment (D375) packages the same module as a headless <code>pi-host</code> on another machine, reached from the desktop over an SSH tunnel; Gateway routing and browser access remain specified but unscheduled.</p><h2 id="_4-request-path-conversation-tool" tabindex="-1">4. Request path (conversation + tool) <a class="header-anchor" href="#_4-request-path-conversation-tool" aria-label="Permalink to &quot;4. Request path (conversation + tool)&quot;">​</a></h2><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>1. UI submits prompt</span></span>
<span class="line"><span>2. Electron main routes to agent sidecar</span></span>
<span class="line"><span>3. pi runtime starts turn and streams events</span></span>
<span class="line"><span>4. UI renders text deltas</span></span>
<span class="line"><span>5. On tool call:</span></span>
<span class="line"><span> 5.1 pi requests tool execution via host bridge</span></span>
<span class="line"><span>  5.2 Rust resolves the durable session mode and evaluates the authoritative</span></span>
<span class="line"><span>      Plan/Goal/Agent tool policy before permission modes</span></span>
<span class="line"><span>  5.3 UI confirms if required, including a separate Plan/Goal approval request and</span></span>
<span class="line"><span>      the selected shell identity for Bash</span></span>
<span class="line"><span> 5.4 Rust resolves the durable session&#39;s project and executes the tool in that</span></span>
<span class="line"><span>     workspace sandbox (never whichever sidebar tab is currently active)</span></span>
<span class="line"><span> 5.5 result returns to pi runtime</span></span>
<span class="line"><span>6. turn ends; session persistence updates</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br></div></div><p>When the same Agent calls <code>SubmitPlan</code>, host-core preserves the exact Markdown bytes in a new immutable <code>&lt;workspaceRoot&gt;/.pi/plan/*.md</code> artifact, records its relative path/hash/size and structured title/question in <code>plan_approvals</code>, and waits for <code>plans.resolve</code>. The approval card opens that artifact. Approval atomically changes the durable session to Agent with the selected permission mode and queues a fresh execution turn. Reject, expiry, host/sidecar crash, and persistence failure grant no execution capability. A startup transaction interrupts pending approvals and queued/running execution fields before RPC service, with no replay; an already-approved interrupted execution leaves the session in Agent.</p><p>The renderer may display Plan state and approval UI, but it is only a projection of live host/runtime events for the current renderer lifetime. It retains the latest proposal/execution snapshot per session in renderer memory; a renderer reload rehydrates only a still-pending row through <code>plans.pending</code>, not a terminal approval or execution card. It cannot authorize a tool or choose a mode for host policy by sending a conflicting request field.</p><p>The renderer may retain several project tabs, but this does not create several host workspace singletons. One project supplies visible shell context; session-bound project identity supplies each turn&#39;s privileged tool root.</p><h2 id="_5-why-hybrid-rust-pi" tabindex="-1">5. Why hybrid Rust + pi <a class="header-anchor" href="#_5-why-hybrid-rust-pi" aria-label="Permalink to &quot;5. Why hybrid Rust + pi&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Approach</th><th>Verdict</th></tr></thead><tbody><tr><td>Pure TS Electron main for everything</td><td>simpler, weaker systems boundary</td></tr><tr><td>Full Rust rewrite of agent loop</td><td>too expensive, loses pi leverage</td></tr><tr><td><strong>Rust host + pi sidecar</strong></td><td>chosen: strong host + mature agent engine</td></tr></tbody></table><h2 id="_6-process-model" tabindex="-1">6. Process model <a class="header-anchor" href="#_6-process-model" aria-label="Permalink to &quot;6. Process model&quot;">​</a></h2><p>Transport: Rust sidecar + stdio JSON-RPC (NDJSON).</p><p>MVP target processes:</p><ol><li>Electron main (including the optional loopback MCP control server)</li><li>Electron renderer</li><li>Rust host core sidecar</li><li>Node pi agent sidecar</li></ol><p>Dev mode may colocate some services, but contracts stay the same.</p><h2 id="_7-extension-points" tabindex="-1">7. Extension points <a class="header-anchor" href="#_7-extension-points" aria-label="Permalink to &quot;7. Extension points&quot;">​</a></h2><ul><li>Tool providers (builtin / plugin / user MCP)</li><li>Local MCP control clients for reviewed desktop operations</li><li>Session backends</li><li>Model catalog sources</li><li>Permission policy packs</li><li>Locale packs</li><li>Market providers (post-MVP)</li></ul><h2 id="_8-packaging-implications" tabindex="-1">8. Packaging implications <a class="header-anchor" href="#_8-packaging-implications" aria-label="Permalink to &quot;8. Packaging implications&quot;">​</a></h2><p>Desktop package must ship:</p><ul><li>Electron app</li><li>one target-native Rust host binary</li><li>one bundled pi sidecar entry under <code>Resources/agent-runtime/sidecar.js</code>, run by the Electron binary with <code>ELECTRON_RUN_AS_NODE=1</code></li><li>Every shipped product locale catalog, plus only the Chromium locale packs needed for those product languages</li><li>target-native runtime modules only when a retained capability cannot be bundled safely</li></ul><p>Renderer-only libraries are build inputs. Vite must emit their executable code and lazy assets under <code>out/renderer</code>; electron-builder must not also copy their original production <code>node_modules</code> trees into ASAR. Electron Main may inline pure-JS workspace helpers while native or runtime-resolved modules stay external. Release packages exclude dependency source maps, tests, examples, declarations, and non-target native prebuilds without replacing local assets with network fetches.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("spec/02-architecture/01-architecture.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _01Architecture = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _01Architecture as default
};
