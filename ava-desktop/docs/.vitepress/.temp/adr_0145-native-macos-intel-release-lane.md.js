import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0145: Publish Native macOS Intel Artifacts","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0145-native-macos-intel-release-lane.md","filePath":"adr/0145-native-macos-intel-release-lane.md","lastUpdated":1789747744000}');
const _sfc_main = { name: "adr/0145-native-macos-intel-release-lane.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0145-publish-native-macos-intel-artifacts" tabindex="-1">ADR 0145: Publish Native macOS Intel Artifacts <a class="header-anchor" href="#adr-0145-publish-native-macos-intel-artifacts" aria-label="Permalink to &quot;ADR 0145: Publish Native macOS Intel Artifacts&quot;">​</a></h1><ul><li>Status: Accepted (amended by D353, ADR 0191, D450 / ADR 0289)</li><li>Date: 2026-09-01</li><li>Deciders: PI-Desktop core</li><li>Related: D126, D285, D353, D354, D450, ADR 0022, ADR 0191, ADR 0289, E2E-092</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The tag release workflow published only Apple Silicon macOS artifacts. The electron-builder configuration also pinned both macOS targets to <code>arm64</code>, so an Intel build could not be selected without changing the shared package configuration. The Rust host sidecar is packaged from the native <code>target/release</code> output, which makes a cross-compiled Electron package unsafe unless the sidecar architecture is independently managed and verified.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>The release matrix publishes two native macOS lanes: <code>macos-15</code> for arm64 and <code>macos-15-intel</code> for x64. The workflow verifies <code>uname -m</code> before preparing package inputs.</li><li>The static macOS electron-builder targets remain DMG and ZIP without a fixed <code>arch</code>. The workflow passes the explicit matching <code>--arm64</code> or <code>--x64</code> flag to electron-builder.</li><li>Each macOS runner builds <code>pi-desktop-host-core</code> locally and packages that same native output. The local signed release script defaults to the host architecture and rejects a <code>MAC_ARCH</code> override that does not match it.</li><li>Each macOS job renames its generated <code>latest-mac.yml</code> before uploading. The publish job validates both feeds, merges their files, and publishes one combined <code>latest-mac.yml</code> alongside both architectures&#39; installers.</li><li>D353 originally gave the Intel x64 job target-specific artifact patterns, publishing <code>PI-Desktop-&lt;version&gt;-Intel.dmg</code> and <code>PI-Desktop-&lt;version&gt;-Intel-mac.zip</code>, while arm64 kept generic names. ADR 0191 supersedes that suffix convention: both lanes now use their standard architecture labels, <code>-arm64</code> and <code>-x64</code>, with updater URLs and checksums generated from those final names.</li><li>macOS in-app update delivery is qualified separately by D450 / ADR 0289. This decision changes release artifact coverage and native packaging only; it does not change updater ownership.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Intel Mac users receive native DMG and ZIP artifacts from every tag release.</li><li>The Rust host and Electron executable have a deterministic, matching architecture on both macOS lanes.</li><li>Release publication needs one metadata merge step because electron-builder emits one macOS updater feed per architecture.</li><li>Every macOS public download carries an explicit standard <code>arm64</code> or <code>x64</code> marker, so neither architecture depends on a generic version-only name.</li><li>Intel package footprint and native launch qualification must be recorded separately from the existing arm64 baseline.</li><li>A developer cannot use the signed local lane to cross-build the other macOS architecture without first moving to the matching native runner.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li>Keep macOS arm64-only: rejected because Intel users remain unable to install a native release and the requested platform coverage is not met.</li><li>Build Intel packages on an Apple Silicon runner: rejected because the Rust sidecar is produced from the host&#39;s native release target and could be mismatched with the Electron package.</li><li>Publish separate updater feeds permanently: rejected because the current macOS delivery mode is notify-and-link and the GitHub Release should expose one macOS feed when a signed update channel becomes available.</li></ul><h2 id="amendment-d450-adr-0289" tabindex="-1">Amendment (D450 / ADR 0289) <a class="header-anchor" href="#amendment-d450-adr-0289" aria-label="Permalink to &quot;Amendment (D450 / ADR 0289)&quot;">​</a></h2><p>The merged <code>latest-mac.yml</code> now drives in-app macOS updates. Architecture coverage and native packaging in this ADR are unchanged.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0145-native-macos-intel-release-lane.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0145NativeMacosIntelReleaseLane = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0145NativeMacosIntelReleaseLane as default
};
