import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0258: Trusted extensions may provide custom agents","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0258-trusted-extension-custom-agents.md","filePath":"adr/0258-trusted-extension-custom-agents.md","lastUpdated":1789474785000}');
const _sfc_main = { name: "adr/0258-trusted-extension-custom-agents.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0258-trusted-extensions-may-provide-custom-agents" tabindex="-1">ADR 0258: Trusted extensions may provide custom agents <a class="header-anchor" href="#adr-0258-trusted-extensions-may-provide-custom-agents" aria-label="Permalink to &quot;ADR 0258: Trusted extensions may provide custom agents&quot;">​</a></h1><ul><li>Status: Accepted for implementation</li><li>Date: 2026-09-15</li><li>Decision: D426</li><li>Related: ADR 0214, ADR 0215, <code>07-plugins/16-trusted-extensions.md</code>, Issue #401</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Trusted extensions run beside the Agent runtime and are the appropriate surface for providers that cannot be represented by a built-in pi-ai adapter. Issue #401 asks for model selection and custom agent/provider registration so a plugin can integrate services such as Command Code with its own endpoint and authentication flow.</p><p>The pinned pi-coding-agent API has <code>registerProvider</code>, but no <code>registerAgent</code>, and its provider registry assumes coding-agent-owned credentials and model persistence. Passing that registry through would bypass PI-Desktop&#39;s session binding and secret ownership boundaries.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>Add a PI-Desktop <code>registerAgent</code> ExtensionAPI member. A trusted extension registers an id, bounded model metadata, and either a <code>stream</code> or <code>complete</code> implementation. The plugin owns endpoint, authentication, request serialization, and response conversion.</li><li>Keep the registration in the Agent sidecar. The host receives only the selected opaque provider id/model id and persists the session binding through the existing <code>session.configure</code> flow.</li><li>Use namespaced <code>extension-agent:&lt;encoded-agent-key&gt;</code> provider ids. On later turns the Electron launcher passes the binding as a placeholder provider; the sidecar reloads the trusted extension and reactivates the matching implementation.</li><li>Expose a read-only model registry projection to the extension context. It may list models and auth availability, but never returns host API keys, secret refs, OAuth tokens, arbitrary provider headers, or host provider internals.</li><li><code>registerProvider</code> accepts the same plugin-owned streaming shape as a compatibility alias. Literal provider credentials are not consumed by the host and no provider registration is written to the Host database or global defaults.</li><li>Registration is session-scoped and is removed with the runtime. Custom agents are available only to trusted extensions holding the existing high-risk <code>agent.extension</code> grant.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Command Code and similar integrations can implement their own auth and wire protocol without an app release or a new Rust transport.</li><li>Session model selection remains host-owned and observable through the existing <code>session:modelChanged</code> event.</li><li>Custom agent definitions are not visible in the ordinary provider Settings picker; a plugin may expose its own command or UI to select them.</li><li>Trusted extension code has sidecar-level authority by design. Marketplace distribution remains subject to the existing signing/trust policy.</li><li>The public ExtensionAPI contract is PI-Desktop-specific for <code>registerAgent</code>; upstream pi API upgrades must preserve the adapter contract or add a compatibility translation.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><h3 id="expose-pi-coding-agent-modelregistry-directly" tabindex="-1">Expose pi-coding-agent ModelRegistry directly <a class="header-anchor" href="#expose-pi-coding-agent-modelregistry-directly" aria-label="Permalink to &quot;Expose pi-coding-agent ModelRegistry directly&quot;">​</a></h3><p>Rejected: it exposes credential resolution and assumes pi&#39;s persistence model.</p><h3 id="add-every-custom-provider-to-rust-sqlite" tabindex="-1">Add every custom provider to Rust/SQLite <a class="header-anchor" href="#add-every-custom-provider-to-rust-sqlite" aria-label="Permalink to &quot;Add every custom provider to Rust/SQLite&quot;">​</a></h3><p>Rejected: the plugin owns the implementation and credentials, while Host storage must not persist opaque plugin transport code or secrets.</p><h3 id="register-an-executable-subagent-instead" tabindex="-1">Register an executable subagent instead <a class="header-anchor" href="#register-an-executable-subagent-instead" aria-label="Permalink to &quot;Register an executable subagent instead&quot;">​</a></h3><p>Deferred to a separate API. This decision covers custom LLM/Agent transport, not a new autonomous worker or tool-permission profile.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0258-trusted-extension-custom-agents.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0258TrustedExtensionCustomAgents = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0258TrustedExtensionCustomAgents as default
};
