import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0230: Skill Ships with the Agent Core Tool Set","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0230-skill-ships-with-the-agent-core-tool-set.md","filePath":"adr/0230-skill-ships-with-the-agent-core-tool-set.md","lastUpdated":1789148224000}');
const _sfc_main = { name: "adr/0230-skill-ships-with-the-agent-core-tool-set.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0230-skill-ships-with-the-agent-core-tool-set" tabindex="-1">ADR 0230: Skill Ships with the Agent Core Tool Set <a class="header-anchor" href="#adr-0230-skill-ships-with-the-agent-core-tool-set" aria-label="Permalink to &quot;ADR 0230: Skill Ships with the Agent Core Tool Set&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-11</li><li>Deciders: PI-Desktop runtime maintainers</li><li>Amends: D174, ADR 0048, ADR 0219</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>ADR 0048 keeps the first Agent request on a small core set and defers every other capability behind the local <code>ToolSearch</code> tool, so a large plugin surface cannot recreate the original prompt bloat. <code>Skill</code> was registered into that deferred set: it only appears in the provider schema after the model searches for it.</p><p>Two later decisions assume a <code>Skill</code> tool the model can call immediately:</p><ul><li>D174 makes the skill catalog the model-invoked way to load a document, and advertises it in the <code># Skills</code> system-prompt section with an instruction to load a matching skill first.</li><li>ADR 0219 answers a user-typed <code>/skill-id</code> by persisting an instruction to call the local <code>Skill</code> tool with the validated id on that turn.</li></ul><p>Neither can be satisfied when the tool is absent from the first request&#39;s tool list. A model that does not search for it either re-searches, calls a tool it cannot see, or answers from the catalog line alone; a user who explicitly asked for a skill pays one or two extra round trips before the body is ever loaded.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><code>Skill</code> joins <code>AGENT_CORE_TOOL_NAMES</code>, so an Agent-mode request carries its schema from the first turn. Its registration gate is unchanged: the tool exists only when the skill catalog is non-empty, and Plan and Goal continue to omit it entirely.</li><li>The tool stays out of the deferred catalog and therefore never appears under <code># On-demand tools</code>. Other on-demand capabilities (<code>BrowserPreview</code>, plugin tools, plugin-development helpers, and MCP tools) keep their lazy behavior, and <code>ToolSearch</code> is still registered whenever any of them exist.</li><li>No protocol, storage, permission, or skill-body change follows from this. Bodies still load on demand through the same local tool and its existing host path and permission checks.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A matching task loads its skill on the first turn instead of discovering the tool first, and a <code>/skill-id</code> invocation works as ADR 0219 describes.</li><li>Every Agent-mode request carries one more tool schema. The catalog is already bounded per session, and the delegation lifecycle was admitted to the core set for the same reason: a capability the model has to go looking for is one it will not use.</li><li>Removing the <code>Skill</code> row from the deferred catalog also removes the only path that could route a skill through <code>ToolSearch</code>, so the catalog description for it is deleted rather than left unreachable.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Keep <code>Skill</code> deferred and rely on prompting:</strong> rejected because a tool absent from the schema cannot be called no matter what the system prompt says, which is the behavior that produced this issue.</li><li><strong>Inject skill bodies into the system prompt:</strong> rejected for the cost and reload semantics D174 and ADR 0039 already settled.</li><li><strong>Make every on-demand tool core:</strong> rejected because it recreates the prompt bloat ADR 0048 exists to prevent. Only <code>Skill</code> is admitted here.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0230-skill-ships-with-the-agent-core-tool-set.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0230SkillShipsWithTheAgentCoreToolSet = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0230SkillShipsWithTheAgentCoreToolSet as default
};
