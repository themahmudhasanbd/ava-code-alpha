import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0270: Builtin Subagents Can Be Switched Off","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0270-builtin-subagents-can-be-disabled.md","filePath":"adr/0270-builtin-subagents-can-be-disabled.md","lastUpdated":1789557374000}');
const _sfc_main = { name: "adr/0270-builtin-subagents-can-be-disabled.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0270-builtin-subagents-can-be-switched-off" tabindex="-1">ADR 0270: Builtin Subagents Can Be Switched Off <a class="header-anchor" href="#adr-0270-builtin-subagents-can-be-switched-off" aria-label="Permalink to &quot;ADR 0270: Builtin Subagents Can Be Switched Off&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-17</li><li>Related: D202, ADR 0062, ADR 0063, ADR 0112, ADR 0126</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Settings &gt; Agent &gt; Subagents lists two groups: the five shipped builtins (<code>explorer</code>, <code>code-reviewer</code>, <code>test-runner</code>, <code>fixer</code>, <code>ui-designer</code>) and the user&#39;s own <code>~/.agents/subagents/*.md</code> documents. Only the user group had an enablement switch, because only those records are files: the builtins are constants in <code>packages/agent-runtime</code>, and ADR 0063 §2 tied activation to the document scan that prunes state for deleted files.</p><p>That left one delegate the user could not turn off. The only route was to copy a builtin into <code>~/.agents/subagents</code>, disable the copy — and the shipped definition still reached the catalog, because disabled user documents are filtered out before the loader merges, so the copy shadowed nothing. Turning off a delegate the instructions steer toward (a <code>fixer</code> that may write files, a <code>test-runner</code> that may run commands) had no supported path at all.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><strong>Activation for a handle with no document lives in its own app-local file.</strong> host-core stores switched-off builtin handles in <code>&lt;data&gt;/agent-capabilities/subagent-builtins.json</code>, keyed by <code>Task</code> handle at the global level. It is deliberately not <code>subagents.json</code>: the user-document scan prunes state for ids it can no longer see, a builtin is never scanned, so a shared file would drop every builtin exclusion on the next scan.</li><li><strong>Two RPCs own that state.</strong> <code>agents.disabledBuiltins</code> reads the switched-off handles (sorted), <code>agents.setBuiltinEnabled(id, enabled)</code> writes one and echoes the normalized handle. A handle no current builtin uses is stored inertly rather than refused: host-core does not ship the builtin list, and a stale entry costs nothing.</li><li><strong>The catalog carries the switch, not the document.</strong> Electron main supplies the disabled handles to <code>loadSubagentDefinitions</code>, which drops them from <code>definitions</code> — what <code>Task</code> may offer — and returns every builtin that still wins its handle in a sibling <code>builtins</code> field, switched off ones included, so Settings can render that row and its switch. <code>subagent/catalog</code> answers with both lists.</li><li><strong>Switching a builtin off is not deleting it.</strong> A user document of the same handle keeps working and keeps shadowing the shipped definition, and turning the builtin back on needs no document, because nothing was removed.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>The Built-in group gains the switch the user rows already have, in the same place and with the same optimistic flip. Reveal and delete stay absent: there is still no file.</li><li>A switched-off builtin stays listed and dimmed, which is the only way back on.</li><li>Session launch and the Settings catalog read the same state, so the page shows what <code>Task</code> offers — the property E2E-SUBAGENT-settings-lists-builtin-defaults asserts.</li><li>A host that is unavailable contributes no exclusions: an unreadable state file offers a delegate the user had turned off rather than removing one they kept.</li><li>No Markdown file is written, no project capability layer appears, and <code>~/.agents/subagents</code> remains the only user-managed subagent source (ADR 0063 §1, ADR 0112 §1).</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><ul><li><strong>A tombstone document in <code>~/.agents/subagents</code>.</strong> Switching a builtin off would write a file the user never asked for, show up in the Global group, and turn a one-click toggle into visible filesystem state.</li><li><strong>Keeping the state in <code>agent-capabilities/subagents.json</code>.</strong> Cheapest to write, but the scan-time prune that keeps deleted documents from leaving orphaned rows would delete a builtin exclusion at the same moment it was written.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0270-builtin-subagents-can-be-disabled.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0270BuiltinSubagentsCanBeDisabled = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0270BuiltinSubagentsCanBeDisabled as default
};
