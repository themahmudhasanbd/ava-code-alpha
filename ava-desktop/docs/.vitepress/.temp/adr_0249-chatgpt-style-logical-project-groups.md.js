import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0249: ChatGPT-Style Logical Project Groups","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0249-chatgpt-style-logical-project-groups.md","filePath":"adr/0249-chatgpt-style-logical-project-groups.md","lastUpdated":1789385560000}');
const _sfc_main = { name: "adr/0249-chatgpt-style-logical-project-groups.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0249-chatgpt-style-logical-project-groups" tabindex="-1">ADR 0249: ChatGPT-Style Logical Project Groups <a class="header-anchor" href="#adr-0249-chatgpt-style-logical-project-groups" aria-label="Permalink to &quot;ADR 0249: ChatGPT-Style Logical Project Groups&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-13</li><li>Deciders: PI-Desktop maintainers</li><li>Amends: ADR 0233, ADR 0234, ADR 0016</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The first multi-folder project flow selected several directories but treated every directory after the first as an independent sidebar project. That does not match the user-facing meaning of a ChatGPT Project: one named container should own its chats, instructions, memory, and attached sources.</p><p>PI-Desktop still has a frozen security boundary in which host-core exposes one visible workspace to the agent at a time. A logical project group must therefore not be implemented by making the renderer or sidecar own a multi-root security policy.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>A project group is a host-owned logical identity with a display name, an ordered list of local folder roots, and a primary root. The first selected folder is always primary.</li><li>Group records and group-level instructions/memory are stored through the host-owned <code>kv</code> extension boundary. Existing path projects remain readable as legacy single-root groups, so no user database reset or migration is required.</li><li>Sessions created for a group continue to carry their primary project path for compatibility. The runtime resolves group instructions and memory from that path before launching the agent, so all chats in the logical group see the same project context.</li><li>The sidebar and Project archive render one row per logical group. Expanding a row shows all roots and the group&#39;s sessions; additional roots are not opened as independent project tabs.</li><li>The host keeps the primary root as the default visible workspace for builtin file tools. A tool request using an absolute path under another registered group root is canonicalized and may use that root as its containment base; group membership never grants arbitrary external filesystem access.</li><li>The project overflow action is named <strong>Edit project</strong>. The editor can change the group name and adjust its root list through the additive <code>project.group.update</code> capability. The primary root remains first and cannot be removed; a root with existing chats cannot be detached.</li><li>Group creation, editing, shared instruction editing, and shared memory editing are additive IPC capabilities. Legacy path-scoped instruction and memory APIs remain available for legacy single-root groups.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A named group behaves like one ChatGPT-style project in the UI: its chats, instructions, and memory are shared across the selected roots.</li><li>Existing projects retain their paths, sessions, transcripts, and renderer presentation metadata.</li><li>The first folder remains meaningful as the default execution root and cannot be reordered in this iteration. Additional roots can be adjusted from Edit project, subject to chat-preservation and host ownership checks, and are available only through explicit absolute paths that pass host canonical containment.</li><li>Selecting another group still changes the one visible host workspace; a background session remains bound to its own primary path.</li><li>The native picker remains local-only. Remote project sources are not implied.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Keep additional folders as independent tabs:</strong> rejected because it loses the logical project relationship and duplicates the ChatGPT Project identity.</li><li><strong>Move multi-root execution into the renderer or sidecar:</strong> rejected because it bypasses the host-owned filesystem and permission boundary.</li><li><strong>Add a new relational schema immediately:</strong> rejected because the existing host <code>kv</code> extension boundary can durably store this configuration without changing the schema version.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0249-chatgpt-style-logical-project-groups.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0249ChatgptStyleLogicalProjectGroups = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0249ChatgptStyleLogicalProjectGroups as default
};
