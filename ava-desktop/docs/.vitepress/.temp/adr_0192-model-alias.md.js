import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0192: Alias a configured model and make model ids copyable","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0192-model-alias.md","filePath":"adr/0192-model-alias.md","lastUpdated":1788924820000}');
const _sfc_main = { name: "adr/0192-model-alias.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0192-alias-a-configured-model-and-make-model-ids-copyable" tabindex="-1">ADR 0192: Alias a configured model and make model ids copyable <a class="header-anchor" href="#adr-0192-alias-a-configured-model-and-make-model-ids-copyable" aria-label="Permalink to &quot;ADR 0192: Alias a configured model and make model ids copyable&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-08</li><li>Deciders: PI-Desktop renderer and UX maintainers</li><li>Amends: D266</li><li>Related: E2E-201</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Settings → Model configuration lists models by their wire id, while the composer shows the catalog&#39;s published <code>displayName</code>. Two gaps surfaced in daily use:</p><ol><li>One provider row can carry several models with near-identical published names, and a user cannot attach a short personal label (&quot;fast&quot;, &quot;pro&quot;) to one of them. The published display name is catalog-owned and read-only.</li><li>Model ids in the configuration page could not be copied. The shell disables text selection globally, and the id sits inside the row&#39;s <code>&lt;label&gt;</code>, so a drag-select toggled the model checkbox instead of selecting text.</li></ol><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><code>ModelBinding</code> gains an optional <code>alias?: string</code>, persisted with the provider&#39;s <code>models</code> array. It is a display label only: <code>id</code> remains the wire identity sent to the provider.</li><li>The alias is edited in the selected model&#39;s Advanced body and shown as a chip beside the id in the selected-model row.</li><li>Where the composer names a model, a non-empty trimmed alias replaces the published display name. An absent or blank alias leaves the published name unchanged, so clearing the field restores catalog naming.</li><li>Model ids and names in the configuration page opt back into text selection (<code>.selectable</code>). A click that carries a selection no longer toggles the model checkbox, so drag-to-copy and click-to-toggle coexist.</li><li>Host-core persists and normalizes the alias inside the existing provider <code>config_json</code> <code>models</code> array: a blank or absent alias is dropped, and an alias longer than 60 characters is rejected with <code>MODEL_ALIAS_TOO_LONG</code>. No protocol, host RPC, or SQLite schema change; the storage schema stays v13.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>One provider can host several models under short personal labels without renaming anything the catalog owns.</li><li>The composer, its search, and the model picker show the alias, while the configuration page and the transcript badge keep the real id, so a mistyped alias cannot hide which model will be called.</li><li>Copying a model id no longer requires retyping it by hand.</li></ul><h2 id="rejected-alternatives" tabindex="-1">Rejected alternatives <a class="header-anchor" href="#rejected-alternatives" aria-label="Permalink to &quot;Rejected alternatives&quot;">​</a></h2><ul><li><strong>Rename <code>displayName</code> on the binding:</strong> duplicates catalog data and loses the published name once the alias is cleared.</li><li><strong>A separate alias registry keyed by model id:</strong> an alias belongs to one provider row&#39;s configuration; a global map would collide across providers that serve the same id.</li><li><strong>Make the whole row selectable and drop click-to-toggle:</strong> removes the fast multi-select gesture the page is built around.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0192-model-alias.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0192ModelAlias = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0192ModelAlias as default
};
