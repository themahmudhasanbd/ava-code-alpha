import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0144: Allow User-Configured Thinking-Level Overrides","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0144-user-configurable-thinking-level-overrides.md","filePath":"adr/0144-user-configurable-thinking-level-overrides.md","lastUpdated":1788272696000}');
const _sfc_main = { name: "adr/0144-user-configurable-thinking-level-overrides.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0144-allow-user-configured-thinking-level-overrides" tabindex="-1">ADR 0144: Allow User-Configured Thinking-Level Overrides <a class="header-anchor" href="#adr-0144-allow-user-configured-thinking-level-overrides" aria-label="Permalink to &quot;ADR 0144: Allow User-Configured Thinking-Level Overrides&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-01</li><li>Deciders: PI-Desktop core</li><li>Updates ADR 0114 and ADR 0134</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The provider model list combines published model metadata with a persisted <code>ModelBinding</code>. Electron main applied the binding before returning <code>ModelInfo</code> to Settings. An existing binding with no enabled thinking levels therefore made a models.dev reasoning record look non-reasoning, even when the catalog correctly described the model as reasoning-capable.</p><p>The same intersection also prevented a compatible proxy or newly released model from being configured when its endpoint supported a thinking level that the current catalog snapshot did not publish.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Keep <code>ModelInfo</code> as the raw published models.dev record. Stored binding values must not rewrite its published reasoning fields or capability tags. Effective provider and session capability is resolved separately from the exact <code>ModelBinding</code> for the selected model.</p><p>The Settings picker always renders the seven canonical thinking levels: <code>off</code>, <code>minimal</code>, <code>low</code>, <code>medium</code>, <code>high</code>, <code>xhigh</code>, and <code>max</code>. Published levels seed a newly added known-model binding. For a non-reasoning or unknown model, the same choices are shown unselected with a concise manual-override hint. Explicit user selections are saved unchanged and are not intersected with the models.dev list. An empty binding or a binding containing only <code>off</code> means thinking is disabled; at least one non-<code>off</code> selection enables reasoning for that provider/model.</p><p>Composer resolves the selected model&#39;s exact binding before rendering its reasoning menu. Runtime clamping uses the binding&#39;s enabled levels. This is an explicit endpoint configuration, not an automatic claim that the upstream model supports the selected level; the user is responsible for choosing levels accepted by a proxy or endpoint.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Correct models.dev reasoning metadata remains visible in Settings, including when an older or manually cleared binding exists.</li><li>OpenAI-compatible proxies and newly released models can opt into thinking without waiting for a catalog update.</li><li>Catalog metadata remains the sole source of published model facts; it only seeds defaults and does not silently erase explicit endpoint configuration.</li><li>No IPC, storage schema, or host protocol change is required.</li><li>An endpoint may reject an explicitly enabled level; the desktop does not infer or guarantee upstream support from the user&#39;s selection.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li>Treat models.dev as an absolute thinking capability gate: rejected because it made valid proxy configurations impossible and caused stored bindings to hide published reasoning metadata.</li><li>Mark every OpenAI-compatible model as reasoning-capable automatically: rejected because compatibility endpoints differ and discovery must not infer unsupported behavior.</li><li>Add a separate per-model &quot;Enable thinking&quot; boolean: rejected because the existing <code>thinkingLevels</code> set already expresses both the enabled levels and the deterministic default.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0144-user-configurable-thinking-level-overrides.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0144UserConfigurableThinkingLevelOverrides = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0144UserConfigurableThinkingLevelOverrides as default
};
