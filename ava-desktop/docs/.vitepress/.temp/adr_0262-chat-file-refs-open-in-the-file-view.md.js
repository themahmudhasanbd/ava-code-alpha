import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0262: Chat File References Complete in Main and Open in the File View","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0262-chat-file-refs-open-in-the-file-view.md","filePath":"adr/0262-chat-file-refs-open-in-the-file-view.md","lastUpdated":1789483301000}');
const _sfc_main = { name: "adr/0262-chat-file-refs-open-in-the-file-view.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0262-chat-file-references-complete-in-main-and-open-in-the-file-view" tabindex="-1">ADR 0262: Chat File References Complete in Main and Open in the File View <a class="header-anchor" href="#adr-0262-chat-file-references-complete-in-main-and-open-in-the-file-view" aria-label="Permalink to &quot;ADR 0262: Chat File References Complete in Main and Open in the File View&quot;">​</a></h1><ul><li><strong>Status</strong>: Accepted</li><li><strong>Date</strong>: 2026-09-14</li><li><strong>Related</strong>: <a href="./0104-plugin-contributed-work-panel-views">ADR 0104</a> · <a href="./0124-temporary-session-scratch-workspace">ADR 0124</a> · <a href="./0163-transcript-file-reference-chips">ADR 0163</a> · <a href="./0241-vendored-updatable-file-view-plugin">ADR 0241</a> · <a href="./../spec/04-ux/09-interaction-patterns">04-ux/09-interaction-patterns</a></li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>ADR 0163 made a file reference in the transcript clickable, but the click trusted the token as written. An agent that works on <code>/root/dir/openimage.js</code> routinely names only <code>openimage.js</code> in its reply, so the reference resolved against the workspace root, pointed at a path that does not exist, and the work panel painted an empty state — or, for a <code>.html</code> token, opened a blank page in the side browser. Nothing told the user which of the two had happened: a stale reference and a slow load looked the same.</p><p>The same click also chose the wrong surface. Every file went to the host <code>file:</code> tab, a read-only viewer, while the work panel&#39;s capable file surface is the vendored <code>pi.file-manager</code> view (ADR 0241) — which can edit, preview images, media, CSV, JSON and SQLite, and which a user can already open from the launcher. It had no way to be told <em>which</em> file to show: ADR 0104 gives a contributed view a manifest entry and nothing else, and the <code>location</code> field a work-panel tab already carries was honoured by main for <code>pi.browser</code> only.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><strong>Completion runs in Electron main</strong>, as <code>fs.resolveRef</code>, because only main sees both the open project and the session&#39;s own scratch store (<code>&lt;data_dir&gt;/scratch/&lt;sessionId&gt;/</code>, ADR 0124). The order is a product contract, not an implementation detail: <ul><li>an absolute reference that already names a real file inside a known root wins outright — that is path equality, not a guess;</li><li>an <code>attachments/&lt;sha256&gt;</code> blob names a stored file by hash, so it resolves against the attachment store directly;</li><li>otherwise the roots are searched in priority order — <strong>the open project first, the session scratch store second, the attachment store last</strong> — and the first root that answers wins, so the project is searched to exhaustion before the scratch store is considered;</li><li>inside one root, an exact path beats a shorthand; among shorthands the longest matching tail wins, then the shallowest path, so <code>src/dir/a.ts</code> beats a second <code>dir/a.ts</code> buried deeper;</li><li>the files panel&#39;s ignore set applies, so a dependency tree is never searched;</li><li>a reference that matches nothing opens <strong>nothing</strong> and reports itself. No empty panel, no blank browser page.</li></ul></li><li><strong>The destination follows where the reference resolved.</strong> A project file opens in the <code>pi.file-manager</code> view. A file in the session scratch or attachment store opens in the host <code>file:</code> tab, because it lives outside that view&#39;s project root — see point 4. A workspace <code>.html</code> / <code>.htm</code> file still opens in the side browser (ADR 0163), for an agent reply and a user chip alike: it is a page to run, not a file to read. When the file view is not loaded, a project file falls back to the host <code>file:</code> tab, so the click never regresses to nothing.</li><li><strong>A contributed view&#39;s <code>location</code> stops being browser-only.</strong> It travels as the view entry URL&#39;s <code>piViewOpen</code> query parameter on creation — the only channel that cannot race a document that has not run yet — and as the <code>view:open</code> preload event once the document has loaded, through the same channel every other panel event uses. A location that arrives before the first <code>did-finish-load</code> restarts the load instead. A loaded view is never navigated: a plugin may hold unsaved edits, and a chat click must not discard them. Re-opening the same location does nothing. The payload is opaque to the host; each plugin decides what it means. No new permission, no new SDK method, no host-only capability.</li><li><strong>The view decides its own presentation.</strong> <code>pi.file-manager</code> v0.4.0 opens the requested file, expands its ancestors, and collapses its own left file list for a host request — a new persisted state with a keyboard-reachable header toggle, so the stored split width comes back when it is expanded again. An absolute path outside the project root is treated as an explicitly host-chosen file: raw Node <code>fs</code>, with the credential deny list still applied to the raw, the normalized and the <code>realpath</code> form, and no root containment. This is deliberate and is why the plugin&#39;s <code>safetyNotes</code> states the exception instead of claiming the project-root containment still covers everything it reads.</li><li><strong>The bundled copy moves to v0.4.0</strong> through ADR 0241&#39;s re-sync procedure, and the same release is published to the plugin center: the marketplace and the bundled copy ship the same bytes.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A click on a shorthand lands on the file the agent meant, or says that nothing matched. The two failure modes ADR 0163 left indistinguishable are now distinguishable.</li><li>The transcript&#39;s file surface is the same editable view the launcher offers, so &quot;show me that file&quot; and &quot;let me change it&quot; are one surface.</li><li>The host now depends on a plugin version for a chat affordance. The fallback in point 2 bounds that dependency: an absent, disabled or older view degrades to the surface the click used before.</li><li>The file view is no longer confined to the project root whenever, and only when, the host asks it for a path the host itself picked. A defect in that path handling is not caught by the host gateway; that is the same trust position ADR 0241 already took, extended by one explicitly-named case.</li><li>A chat click no longer hands a file to the OS default application. That action is still reachable from the file view&#39;s own context menu.</li><li><code>fs.resolveRef</code> is a new renderer-facing channel. <code>fs/open</code> (ADR 0163) remains part of the IPC surface; the transcript is simply no longer one of its callers.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><h3 id="resolve-the-shorthand-in-the-renderer" tabindex="-1">Resolve the shorthand in the renderer <a class="header-anchor" href="#resolve-the-shorthand-in-the-renderer" aria-label="Permalink to &quot;Resolve the shorthand in the renderer&quot;">​</a></h3><p>Rejected: the renderer cannot see the session scratch store, and the two roots have to be ranked against each other in one place for the priority order in point 1 to hold.</p><h3 id="add-an-ui-openview-pluginid-viewid-payload-sdk-method" tabindex="-1">Add an <code>ui.openView(pluginId, viewId, payload)</code> SDK method <a class="header-anchor" href="#add-an-ui-openview-pluginid-viewid-payload-sdk-method" aria-label="Permalink to &quot;Add an \`ui.openView(pluginId, viewId, payload)\` SDK method&quot;">​</a></h3><p>Rejected for now: the work-panel tab already carries <code>location</code>, and <code>PluginViewHost.broadcast</code> already reaches docked views, so the feature needs no new public API surface. A method that opened <em>another</em> plugin&#39;s view for a plugin author would be a different decision, with its own consent question.</p><h3 id="reload-the-view-with-the-new-location" tabindex="-1">Reload the view with the new location <a class="header-anchor" href="#reload-the-view-with-the-new-location" aria-label="Permalink to &quot;Reload the view with the new location&quot;">​</a></h3><p>Rejected: it discards whatever a plugin is holding, including unsaved edits. The one reload in point 3 happens only before the first document has run, where there is nothing to lose.</p><h3 id="widen-the-plugin-s-declared-fs-root-to-the-scratch-store" tabindex="-1">Widen the plugin&#39;s declared <code>fs</code> root to the scratch store <a class="header-anchor" href="#widen-the-plugin-s-declared-fs-root-to-the-scratch-store" aria-label="Permalink to &quot;Widen the plugin&#39;s declared \`fs\` root to the scratch store&quot;">​</a></h3><p>Rejected: the manifest&#39;s root model is <code>workspace</code> or a user-selected directory. Widening it would grant every project&#39;s view the right to read every session&#39;s scratch data, which is a much larger grant than &quot;the host asked for this one file&quot;.</p><h3 id="route-workspace-html-through-the-file-view-for-agent-replies" tabindex="-1">Route workspace HTML through the file view for agent replies <a class="header-anchor" href="#route-workspace-html-through-the-file-view-for-agent-replies" aria-label="Permalink to &quot;Route workspace HTML through the file view for agent replies&quot;">​</a></h3><p>Rejected: the file view renders a page as source. ADR 0163 chose the browser for the same reason, and one contract is easier to reason about than two.</p><h3 id="keep-the-os-default-application-as-the-destination" tabindex="-1">Keep the OS default application as the destination <a class="header-anchor" href="#keep-the-os-default-application-as-the-destination" aria-label="Permalink to &quot;Keep the OS default application as the destination&quot;">​</a></h3><p>Rejected: it is not the right panel, and it is what the request replaces. The action survives in the file view&#39;s context menu, which is where the file is already on screen.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0262-chat-file-refs-open-in-the-file-view.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0262ChatFileRefsOpenInTheFileView = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0262ChatFileRefsOpenInTheFileView as default
};
