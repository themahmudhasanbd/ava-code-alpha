import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0172: Contained in-chat image display","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0172-contained-in-chat-image-display.md","filePath":"adr/0172-contained-in-chat-image-display.md","lastUpdated":1788761294000}');
const _sfc_main = { name: "adr/0172-contained-in-chat-image-display.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0172-contained-in-chat-image-display" tabindex="-1">ADR 0172: Contained in-chat image display <a class="header-anchor" href="#adr-0172-contained-in-chat-image-display" aria-label="Permalink to &quot;ADR 0172: Contained in-chat image display&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-07</li><li>Related: ADR 0019, ADR 0163, ADR 0169, decisions D320/D334, E2E-187</li><li>Amends: the <code>fs/read</code> workspace-only clause of the desktop IPC spec</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Pasted and uploaded images are stored as extension-less <code>attachments/&lt;sha256&gt;</code> blobs under the data directory. Local Markdown images are workspace-relative. The renderer origin cannot load those files, so history turns showed a chip instead of the picture. Opening an <code>attachments/&lt;sha256&gt;</code> chip through <code>fs/open</code> also failed because the path was treated as workspace-relative.</p><p>A generic &quot;any absolute regular file&quot; read channel would let the renderer exfiltrate arbitrary disk contents.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>Keep <code>fs/list</code> workspace-only. Extend <code>fs/read</code>, <code>fs/reveal</code>, and <code>fs/open</code> so they share <code>resolveOpenablePath</code>: workspace-relative paths, absolute paths already inside the workspace / <code>&lt;data_dir&gt;/scratch/</code> / <code>&lt;data_dir&gt;/attachments/</code>, and content-addressed <code>attachments/&lt;sha256&gt;</code> refs. Reads additionally <code>realpath</code> the target and re-check containment.</li><li>Add renderer-only <code>fs/readImageDataUrl({ref, mimeType?})</code>. It returns a bounded image data URL or <code>missing</code> / <code>notImage</code> / <code>tooLarge</code> and never returns non-image bytes. It is not a plugin host API; plugins keep <code>fs.readPreview</code>.</li><li>A known image extension always wins over a client-supplied <code>mimeType</code>. Extension-less attachment blobs accept only the existing <code>IMAGE_MIME</code> allowlist. Arbitrary <code>image/*</code> values are ignored.</li><li>Chat thumbnails and local Markdown images load through that channel. Clicking a resolved image opens the host files viewer on the same ref.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>History attachments and local Markdown images render inline when the file is still present and under the cap.</li><li><code>/etc/passwd</code> and other outside-root paths stay unreadable even with a spoofed <code>mimeType</code>.</li><li>Plugin Files views remain workspace-scoped; transcript artifacts still use the host <code>file:</code> tab.</li></ul><h2 id="alternatives-rejected" tabindex="-1">Alternatives rejected <a class="header-anchor" href="#alternatives-rejected" aria-label="Permalink to &quot;Alternatives rejected&quot;">​</a></h2><h3 id="new-unbounded-absolute-path-read" tabindex="-1">New unbounded absolute-path read <a class="header-anchor" href="#new-unbounded-absolute-path-read" aria-label="Permalink to &quot;New unbounded absolute-path read&quot;">​</a></h3><p>Rejected because the renderer IPC is not a user-click gate.</p><h3 id="reuse-fs-readpreview-from-the-chat-renderer" tabindex="-1">Reuse <code>fs.readPreview</code> from the chat renderer <a class="header-anchor" href="#reuse-fs-readpreview-from-the-chat-renderer" aria-label="Permalink to &quot;Reuse \`fs.readPreview\` from the chat renderer&quot;">​</a></h3><p>Rejected because that API is plugin-scoped to the workspace root and cannot see <code>attachments/&lt;sha256&gt;</code> blobs.</p><h2 id="references" tabindex="-1">References <a class="header-anchor" href="#references" aria-label="Permalink to &quot;References&quot;">​</a></h2><ul><li><code>apps/desktop/electron/main/fs-panel.ts</code></li><li><code>apps/desktop/src/lib/use-referenced-image-data-url.ts</code></li><li><code>docs/spec/03-runtime/01-ipc-protocol.md</code></li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0172-contained-in-chat-image-display.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0172ContainedInChatImageDisplay = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0172ContainedInChatImageDisplay as default
};
