import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0022: Application Update Delivery","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0022-application-update-delivery.md","filePath":"adr/0022-application-update-delivery.md","lastUpdated":1790060879000}');
const _sfc_main = { name: "adr/0022-application-update-delivery.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0022-application-update-delivery" tabindex="-1">ADR 0022: Application Update Delivery <a class="header-anchor" href="#adr-0022-application-update-delivery" aria-label="Permalink to &quot;ADR 0022: Application Update Delivery&quot;">​</a></h1><ul><li>Status: Accepted (amended by D364 / D603 / ADR 0197, D450 / ADR 0289)</li><li>Date: 2026-07-26</li><li>Deciders: PI-Desktop core</li><li>Related: D120, D126, D364, D603, D010, D450, ADR 0021, ADR 0197, ADR 0289</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>PI-Desktop needs a release-update path that preserves the renderer sandbox, does not expose feed configuration to untrusted UI code, and reflects each platform&#39;s actual installer capabilities. A single automatic-install policy is not appropriate: unsigned macOS packages cannot provide a qualified in-app upgrade lane, while Windows NSIS and Linux AppImage support electron-updater&#39;s download-and-install flow.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>Electron Main exclusively owns <code>electron-updater</code>, the fixed GitHub Releases feed, update polling, and install lifecycle. Renderer IPC exposes only allowlisted check, state, release-link, and install operations; callers cannot supply a feed URL.</li><li>Development builds keep updates disabled. Packaged macOS, Windows NSIS, and Linux AppImage use in-app download and quit-and-install delivery. Non-AppImage Linux (deb/rpm) and Windows ZIP builds (<code>piDistribution = &quot;zip&quot;</code>) use notify-and-link delivery so an NSIS installer cannot replace a no-install run. Legacy portable executables remain manual when <code>PORTABLE_EXECUTABLE_FILE</code> is present. (Packaged macOS was notify-and-link until D450 / ADR 0289 qualified the signed in-app channel.)</li><li>The updater always sets <code>allowPrerelease = false</code>. electron-updater would otherwise pin prerelease installs (for example <code>0.2.0-rc.6</code>) to the same custom channel (<code>rc</code>) and never offer a newer stable GitHub latest release. Prerelease-channel policy remains an operational follow-up if a dedicated RC feed is needed later.</li><li>Automatic checks run after startup and periodically. Automatic failures remain ambient; explicit checks expose status and errors through the menu and Settings -&gt; Info. A downloaded update remains actionable until install or normal shutdown.</li><li>Feed manifests and artifact hashes are generated by electron-builder. The client ships no GitHub credential and fails closed when the feed or package cannot be validated.</li><li>D126 later lifts D010&#39;s macOS-only publication scope and publishes all platform artifacts and update manifests produced by the release matrix. D450 / ADR 0289 qualifies the signed macOS in-app channel on that same feed.</li><li>Dual-locale product &quot;what&#39;s new&quot; text (D164) is maintained in <code>packages/shared</code> as EN + zh-CN catalogs. Main formats notes for the discovered version using the product UI locale and attaches them as optional <code>UpdateState.releaseNotes</code> on the existing updates path. GitHub auto-generated release bodies remain web-only; the renderer never supplies a notes URL.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Update state is process-owned and consistent across the application menu, Settings, and the ambient banner.</li><li>The sandboxed renderer cannot redirect update traffic or install arbitrary packages.</li><li>Windows NSIS, Linux AppImage, and packaged macOS can update in-app from published tag feeds; Linux deb and Windows ZIP users install from the release page after extracting the archive.</li><li>Prerelease installs graduate to newer stable releases through the same latest feed; a dedicated RC channel is not active.</li><li>In-app bilingual release highlights ship with the build and follow the product locale without a second network surface.</li><li>Rollback, staged rollout, and optional prerelease-channel policy remain operational follow-ups rather than renderer capabilities.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><ul><li>Renderer-owned updater: rejected because it violates the process and sandbox boundary.</li><li>Caller-provided feed URLs: rejected because it creates an arbitrary package installation path.</li><li>Force one delivery mode on every platform: rejected because installer and signing guarantees differ by target.</li></ul><h2 id="amendment-d450-adr-0289" tabindex="-1">Amendment (D450 / ADR 0289) <a class="header-anchor" href="#amendment-d450-adr-0289" aria-label="Permalink to &quot;Amendment (D450 / ADR 0289)&quot;">​</a></h2><p>Official GitHub tag macOS artifacts are Developer ID-signed, notarized, and stapled. Packaged macOS therefore uses the same in-app <code>electron-updater</code> lane as Windows NSIS and Linux AppImage. Local unsigned packaging without a certificate remains available (D078).</p><h2 id="amendment-d603" tabindex="-1">Amendment (D603) <a class="header-anchor" href="#amendment-d603" aria-label="Permalink to &quot;Amendment (D603)&quot;">​</a></h2><p>The Windows no-install lane now publishes a normal ZIP archive instead of electron-builder&#39;s self-extracting <code>portable</code> executable. The release helper builds NSIS and ZIP separately and stamps the ZIP app metadata with <code>piDistribution = &quot;zip&quot;</code>; the updater uses that marker to keep the ZIP manual even though ordinary ZIP launches do not set <code>PORTABLE_EXECUTABLE_FILE</code>. Users extract the archive and run <code>PI-Desktop.exe</code>; the NSIS lane and existing data directory remain unchanged.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0022-application-update-delivery.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0022ApplicationUpdateDelivery = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0022ApplicationUpdateDelivery as default
};
