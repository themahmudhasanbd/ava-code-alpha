import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0059: Persist Composer Clipboard Files in Session Scratch","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0059-composer-clipboard-files-in-session-scratch.md","filePath":"adr/0059-composer-clipboard-files-in-session-scratch.md","lastUpdated":1789398649000}');
const _sfc_main = { name: "adr/0059-composer-clipboard-files-in-session-scratch.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0059-persist-composer-clipboard-files-in-session-scratch" tabindex="-1">ADR 0059: Persist Composer Clipboard Files in Session Scratch <a class="header-anchor" href="#adr-0059-persist-composer-clipboard-files-in-session-scratch" aria-label="Permalink to &quot;ADR 0059: Persist Composer Clipboard Files in Session Scratch&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-08-05</li><li>Deciders: PI-Desktop core</li><li>Related: D197, D209, ADR 0024 (composer commands and @ file references), ADR 0070 (compact reference display), D114 (session scratch directory), D119 (transcript file store)</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The composer is a controlled textarea. Chromium exposes pasted operating-system files and screenshots as <code>File</code> objects, while the native picker exposes files that may live outside the active workspace. The text-only prompt contract has no binary <code>ImageContent</code> channel and an attached file must remain available to the agent after the prompt is sent. Writing the bytes into the project would dirty git state and would not follow the session-bound workspace/scratch ownership rules.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><p>The renderer intercepts paste only when the clipboard contains one or more <code>File</code> objects. Text-only paste remains native textarea behavior.</p></li><li><p>The renderer transfers bounded file bytes plus the browser-provided name and MIME type to Electron main through <code>composer/pasteFiles</code>, together with the durable session id. A home composer creates or reuses a session before the transfer. Native picker selections use the additive <code>composer/importFiles</code> channel with the same session id; main resolves and copies those source paths before returning references to the renderer.</p></li><li><p>Electron main validates that the session exists, limits the request to 20 files, 64 MiB per file, and 128 MiB total, strips directory components and unsafe name characters, and writes unique files with exclusive-create semantics below:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&lt;data_dir&gt;/scratch/&lt;sessionId&gt;/pasted/</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div></li><li><p>Main returns each UUID-backed absolute path plus its sanitized original leaf name. Under ADR 0070 the renderer keeps the path in transient reference state, shows the leaf name as a compact chip, and serializes the path into the prompt as an <code>@</code> reference using the existing whitespace quoting rule. The prompt and transcript carry paths, never clipboard bytes.</p></li><li><p>Pasted files follow the existing scratch lifecycle: deleting the session or the orphan/stale startup sweep removes them. They are not workspace artifacts and never change project git status.</p></li></ol><h2 id="security-and-boundary-notes" tabindex="-1">Security and boundary notes <a class="header-anchor" href="#security-and-boundary-notes" aria-label="Permalink to &quot;Security and boundary notes&quot;">​</a></h2><ul><li>The renderer cannot select the destination directory; the session id is checked in main and the output root is constructed from the host data dir. Picker source paths are resolved through <code>realpath</code> and must be regular files; they never become prompt references or destination paths.</li><li>Renderer names are reduced to a basename and sanitized. A UUID prefix and exclusive creation prevent collisions and overwrite-by-name.</li><li>The bridge is Electron-only. It adds no host RPC method and does not expose arbitrary filesystem write access to the renderer.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Insert the browser file name only:</strong> loses the bytes and gives the agent no usable path. Rejected.</li><li><strong>Write into the workspace:</strong> makes a normal paste dirty the project and breaks session scratch isolation. Rejected.</li><li><strong>Send binary inline with the prompt:</strong> changes the text-only prompt contract, inflates context, and requires provider-specific attachment handling. Rejected.</li><li><strong>Use an Electron file picker for paste:</strong> does not support screenshots and adds an extra interaction for the common clipboard workflow. Rejected as the paste path; the separate picker upload action now reuses this scratch contract for explicitly selected files.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>File and image paste works from both home and docked composers without a project file mutation.</li><li>The visible draft gains compact leaf-name references; the dispatched prompt gains the same normal <code>@absolute/path</code> references, so existing Read/Glob/Grep behavior handles the materialized files.</li><li>Large or malformed clipboard payloads fail visibly in the composer and do not partially write because bytes are validated before the first write.</li></ul><h2 id="amendment-2026-09-14-clipboard-text-representation-for-mixed-word-pastes" tabindex="-1">Amendment (2026-09-14): clipboard text representation for mixed Word pastes <a class="header-anchor" href="#amendment-2026-09-14-clipboard-text-representation-for-mixed-word-pastes" aria-label="Permalink to &quot;Amendment (2026-09-14): clipboard text representation for mixed Word pastes&quot;">​</a></h2><p>Issue #138: Microsoft Word can place non-whitespace <code>text/plain</code> together with an <code>image/*</code> copy of the same selection on the clipboard, so the <code>File</code>-only rule of decision 1 discarded editable text and materialized the image instead. This amendment replaces that selection rule; the remaining decisions are unchanged.</p><ul><li>The renderer selects the clipboard representation before the attachment flow. It prefers the editable text when the text is not whitespace-only and every accompanying clipboard file is <code>image/*</code> and resolves to no native filesystem path.</li><li>Any native file path, any non-image file, absent or whitespace-only text, and image-only pastes keep the file and image attachment flow of decision 1.</li><li>Selected text follows ADR 0131&#39;s existing <code>largePasteThreshold</code>: text at or below the threshold stays editable inline, and larger text becomes a session scratch <code>text/plain</code> reference.</li><li>Short multiline text is escaped and inserted as text plus generated line breaks, so paragraphs, blank and trailing lines, the replaced selection, the surrounding text, the caret, and native undo all survive. Clipboard HTML is still never read, and CRLF/CR line endings become editor LF line breaks.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0059-composer-clipboard-files-in-session-scratch.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0059ComposerClipboardFilesInSessionScratch = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0059ComposerClipboardFilesInSessionScratch as default
};
