import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0104: Plugin-contributed work panel views","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0104-plugin-contributed-work-panel-views.md","filePath":"adr/0104-plugin-contributed-work-panel-views.md","lastUpdated":1787640443000}');
const _sfc_main = { name: "adr/0104-plugin-contributed-work-panel-views.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0104-plugin-contributed-work-panel-views" tabindex="-1">ADR 0104: Plugin-contributed work panel views <a class="header-anchor" href="#adr-0104-plugin-contributed-work-panel-views" aria-label="Permalink to &quot;ADR 0104: Plugin-contributed work panel views&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-08-19</li><li>Deciders: PI-Desktop core</li><li>Related: <a href="./0019-work-panel-subsystems">ADR 0019</a> · <a href="./0033-internal-dock-work-panel">ADR 0033</a> · <a href="./0122-reserve-native-width-while-work-panel-visible">ADR 0122</a> · <a href="./0081-host-owned-plugin-panel-chrome">ADR 0081</a> · <a href="./0092-plugin-owned-panel-surface">ADR 0092</a> · <a href="./0105-files-as-a-bundled-plugin">ADR 0105</a> · <a href="./0108-remove-built-in-interactive-terminal">ADR 0108</a> · <a href="./../spec/07-plugins/README">07-plugins</a> · <a href="./../spec/04-ux/08-component-spec">04-ux/08-component-spec §5</a></li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Before ADR 0108 removed the interactive terminal, the right work panel exposed four capabilities — Review, Terminal, Browser, Files (ADR 0019) — as a hard-coded list in the renderer: a <code>HEADER_TOOLS</code> constant, a closed <code>WorkPanelTabKind</code> union, and a four-way branch on tab kind in the panel body. Adding any new surface meant changing host code.</p><p>Plugins already have a mature and strictly bounded UI channel, but it only produces a <strong>separate window</strong>: <code>ui.panel</code> opens a frameless <code>BrowserWindow</code> with a sandboxed preload, a per-plugin persisted session partition, and a <code>net.domains</code> egress allowlist (ADR 0092). A plugin cannot put anything inside the main window, so capabilities that belong next to the conversation — a Git history browser, a file manager, an issue list — either become a detached window that competes with the app for screen space, or do not exist.</p><p>The work panel is the natural home for those surfaces. It is already the &quot;inspect and steer the workspace&quot; column, it is already session-scoped (ADR 0028), and it is an in-flow part of the client area with a temporary native reservation while visible (ADR 0122).</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><p><strong>A plugin declares work panel surfaces with <code>contributes.views</code>.</strong> Each entry carries an <code>id</code>, a <code>title</code> (plain or <code>{ en, &quot;zh-CN&quot; }</code>), an optional <code>icon</code> token, an HTML <code>entry</code> relative to the plugin, and an optional <code>order</code>. A plugin may declare several: a Git plugin can ship &quot;Changes&quot; and &quot;History&quot; as independent entries. The new <code>ui.view</code> permission gates the contribution, parallel to <code>ui.panel</code>; the two are independent, so a plugin may have docked views without a detached window.</p></li><li><p><strong>A view is rendered as a <code>WebContentsView</code> owned by the main process</strong>, attached to the main window and positioned from a renderer-measured rect — the mechanism the preview browser has used since ADR 0019/0033. It reuses the plugin&#39;s existing isolation wholesale: the same sandboxed <code>plugin-panel</code> preload, the same persisted <code>persist:pi-plugin-&lt;id&gt;</code> partition as that plugin&#39;s detached window, and the same <code>net.domains</code> egress filter. The egress policy and the partition name are extracted to shared functions so the docked and detached placements cannot drift into different security postures.</p></li><li><p><strong>The renderer is the visibility authority.</strong> A <code>WebContentsView</code> composites above renderer content, so the panel hides every native surface — the browser and any plugin view alike — whenever it is not the active tab, the panel is animating, the divider is being dragged, or a blocking overlay is open. One <code>panelBlocked</code> condition governs all of them.</p></li><li><p><strong>An embedded view drops the window-control chrome.</strong> A docked view has no window to minimize, maximize, or drag, so the preload skips the three-button capsule and the 46px safe area that ADR 0092 defines, and publishes <code>--pi-plugin-titlebar-height: 0px</code> instead of <code>46px</code>. Plugin authors read the variable rather than hard-coding a value, so one entry file lays out correctly in both placements. <code>window.pluginBridge</code> is byte-for-byte the same, so a view and a panel share their code.</p></li><li><p><strong>Icons are tokens from a host-owned closed list</strong>, never plugin-supplied markup. The icon is drawn inside host chrome next to first-party controls; accepting SVG there would be an injection surface and would let a plugin dress up as the host. An unknown token is not an error — it degrades to a lettered tile — so a plugin written against a newer host still lists correctly on an older one.</p></li><li><p><strong>The view list is filtered by activation scope.</strong> This differs deliberately from <code>pluginThemes</code>, which is <em>not</em> scope-filtered because the selected theme is one global app setting. A view is something a plugin does inside a project, so a project-scoped plugin must not offer its view in another project. Permission, scope, and entry existence are re-checked when a view is opened; the renderer is never the authority on what a plugin may show.</p></li><li><p><strong>Views are cached, bounded, and tied to the plugin&#39;s lifetime.</strong> A view survives tab switches so a plugin keeps its scroll position and in-page state, up to four live views, evicting the least recently shown and never the one on screen. Disable, uninstall, reload, and crash all destroy the plugin&#39;s views; the tab may outlive the page, so the renderer re-opens on the lifecycle event rather than leaving a permanently blank pane.</p></li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>The work panel becomes an extension point rather than a fixed set. Third-party plugins reach it through the same route first-party code will use once ADR 0103&#39;s migration lands, which is what makes that migration a real test of whether the plugin API is sufficient.</li><li>A plugin&#39;s storage is one thing regardless of placement, because a view and that plugin&#39;s detached window share a partition. Opening a view therefore inherits any session state the panel window already had.</li><li><code>windowForSender</code> must scan windows directly instead of resolving a plugin id first: a docked view now also resolves to a plugin id, and routing window controls through that would let a docked view close the same plugin&#39;s separate window. This is asserted by a test rather than left to review.</li><li>The z-order caveat ADR 0019 accepted for the browser now applies to every plugin view. Top-center toasts may overlap a view&#39;s corner; blocking overlays hide it entirely.</li><li>Four live <code>WebContentsView</code>s is four extra renderer processes at worst. The bound is a deliberate trade against destroying and reloading a plugin page on every tab switch.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><h3 id="an-iframe-inside-the-host-renderer" tabindex="-1">An <code>&lt;iframe&gt;</code> inside the host renderer <a class="header-anchor" href="#an-iframe-inside-the-host-renderer" aria-label="Permalink to &quot;An \`&lt;iframe&gt;\` inside the host renderer&quot;">​</a></h3><p>Rejected. It would z-order correctly and need no bounds syncing, but Electron cannot give an iframe its own session partition, so the plugin page would share the host renderer&#39;s process and storage. That is a strictly weaker boundary than the one plugin windows already have, and the <code>net.domains</code> allowlist could not be enforced on it. Isolation parity with the existing panel window is the point.</p><h3 id="plugin-supplied-react-components-rendered-by-the-host" tabindex="-1">Plugin-supplied React components rendered by the host <a class="header-anchor" href="#plugin-supplied-react-components-rendered-by-the-host" aria-label="Permalink to &quot;Plugin-supplied React components rendered by the host&quot;">​</a></h3><p>Rejected outright. It would put third-party code in the host renderer with access to the app&#39;s own state and IPC bridge, discarding the entire plugin trust boundary for a rendering convenience.</p><h3 id="reuse-ui-panel-and-let-the-host-decide-where-to-show-it" tabindex="-1">Reuse <code>ui.panel</code> and let the host decide where to show it <a class="header-anchor" href="#reuse-ui-panel-and-let-the-host-decide-where-to-show-it" aria-label="Permalink to &quot;Reuse \`ui.panel\` and let the host decide where to show it&quot;">​</a></h3><p>Rejected. A panel is one surface per plugin with a window&#39;s dimensions and title; a view is one of several, sized by the panel column, and needs its own menu label, icon, and order. Overloading one field would make both worse and leave no way to ship a plugin that offers a docked view <em>and</em> a detached window.</p><h3 id="both-selected-by-a-isolation-process-inline-manifest-field" tabindex="-1">Both, selected by a <code>isolation: &quot;process&quot; | &quot;inline&quot;</code> manifest field <a class="header-anchor" href="#both-selected-by-a-isolation-process-inline-manifest-field" aria-label="Permalink to &quot;Both, selected by a \`isolation: &quot;process&quot; | &quot;inline&quot;\` manifest field&quot;">​</a></h3><p>Rejected for this change. It doubles the rendering and security paths to maintain for a performance benefit that has not been measured, and the weaker mode would become the default simply because it is easier to write against.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0104-plugin-contributed-work-panel-views.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0104PluginContributedWorkPanelViews = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0104PluginContributedWorkPanelViews as default
};
