import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"15. 工作区忽略规则","description":"","frontmatter":{},"headers":[],"relativePath":"zh-CN/spec/03-runtime/15-workspace-ignore-rules.md","filePath":"zh-CN/spec/03-runtime/15-workspace-ignore-rules.md","lastUpdated":1788993356000}');
const _sfc_main = { name: "zh-CN/spec/03-runtime/15-workspace-ignore-rules.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="_15-工作区忽略规则" tabindex="-1">15. 工作区忽略规则 <a class="header-anchor" href="#_15-工作区忽略规则" aria-label="Permalink to &quot;15. 工作区忽略规则&quot;">​</a></h1><blockquote><p><strong>翻译说明：</strong> 本页是与 <a href="/spec/03-runtime/15-workspace-ignore-rules">英文源规格</a> 一一对应的机器辅助翻译。代码、协议字段和标识符保持原文；如翻译与英文源事实有歧义，以英文版本为准。</p></blockquote><h2 id="_1-goal" tabindex="-1">1. Goal <a class="header-anchor" href="#_1-goal" aria-label="Permalink to &quot;1. Goal&quot;">​</a></h2><p>通过以下方式防止工具进入 scanning/reading/writing 敏感或无用路径 默认情况下，同时允许在执行任务时进行明确的、可见的权限决策 故意以会话工作区之外的路径为目标。</p><h2 id="_2-规则层-优先级高→低" tabindex="-1">2. 规则层（优先级高→低） <a class="header-anchor" href="#_2-规则层-优先级高→低" aria-label="Permalink to &quot;2. 规则层（优先级高→低）&quot;">​</a></h2><ol><li><strong>安全拒绝列表</strong>（始终开启，不在 MVP 中由用户禁用）</li><li><strong>应用程序默认设置</strong>（已发货）</li><li><strong>工作区规则</strong>（工作区根目录下的 <code>.pi-desktopignore</code>）</li><li><strong>用户全局忽略</strong>（<code>&lt;data_dir&gt;/ignore</code>，默认即 <code>~/.pi-desktop/ignore</code>）</li><li>显式工具路径仍受安全拒绝名单和 外部路径权限门</li></ol><p><code>Glob</code>/<code>Grep</code> 的显式 <code>path</code> 参数会让遍历跳出第 2–4 层（与它已经绕过上层 <code>.gitignore</code> 规则的方式相同），因此显式点名 <code>node_modules/pkg</code> 或 <code>dist</code> 的 调用者仍然可以搜索它们。第 1 层对每一次遍历和每一个显式路径都生效。</p><h2 id="_3-安全拒绝名单-始终" tabindex="-1">3. 安全拒绝名单（始终） <a class="header-anchor" href="#_3-安全拒绝名单-始终" aria-label="Permalink to &quot;3. 安全拒绝名单（始终）&quot;">​</a></h2><p>默认情况下，工作空间外 read/write/search 被拒绝。明确的 Goal/scanning/reading/writing/MVP/<code>.pi-desktopignore</code>/<code>~/.pi-desktop/ignore</code> 路径只有在主机申请后才能继续 权限模式：<code>auto</code> 允许，而 <code>ask</code> 和 <code>accept-edits</code> 询问 用户。隐式递归遍历永远不会获得工作空间外部的访问权限。</p><p>在工作区内（以及 scratch 目录或已批准的外部根目录内）还拒绝以下内容：</p><ul><li><code>.git/objects/**</code></li><li>私钥模式：<code>*.pem</code>、<code>*.key</code>、<code>id_rsa</code>、<code>id_ed25519</code></li><li><code>.env</code>、<code>.env.*</code> —— 但文档变体 <code>.env.example</code>、<code>.env.sample</code> 和 <code>.env.template</code> 除外，它们不含密钥，而且通常正是编码任务需要的</li><li>凭证文件：<code>*.p12</code>、<code>*.pfx</code>、<code>credentials.json</code> (Google)、带有令牌的 <code>.npmrc</code>（尽力而为）</li></ul><p>文件名匹配不区分大小写。<code>Glob</code> 和 <code>Grep</code> 会静默地把命中的文件从结果中丢掉； 显式的 <code>Read</code>、<code>Write</code> 或 <code>Edit</code>（包括 <code>Edit</code> 的移动目标）以 <code>WORKSPACE_PATH_DENIED</code> 失败，外部路径授权也不会解除这一拒绝。<code>Bash</code> 不做 过滤（§6）。</p><blockquote><p>后续版本中 Read 可能在明确的权限提示下被允许；MVP 一律失败关闭。</p></blockquote><h2 id="_4-默认忽略-应用程序" tabindex="-1">4. 默认忽略（应用程序） <a class="header-anchor" href="#_4-默认忽略-应用程序" aria-label="Permalink to &quot;4. 默认忽略（应用程序）&quot;">​</a></h2><div class="language-gitignore vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">gitignore</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>.git/</span></span>
<span class="line"><span>node_modules/</span></span>
<span class="line"><span>dist/</span></span>
<span class="line"><span>build/</span></span>
<span class="line"><span>.target/</span></span>
<span class="line"><span>target/</span></span>
<span class="line"><span>.venv/</span></span>
<span class="line"><span>venv/</span></span>
<span class="line"><span>__pycache__/</span></span>
<span class="line"><span>.pytest_cache/</span></span>
<span class="line"><span>.mypy_cache/</span></span>
<span class="line"><span>.DS_Store</span></span>
<span class="line"><span>*.log</span></span>
<span class="line"><span>coverage/</span></span>
<span class="line"><span>.turbo/</span></span>
<span class="line"><span>.next/</span></span>
<span class="line"><span>.cache/</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br></div></div><h2 id="_5-工作区文件" tabindex="-1">5. 工作区文件 <a class="header-anchor" href="#_5-工作区文件" aria-label="Permalink to &quot;5. 工作区文件&quot;">​</a></h2><p>支持：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>.pi-desktopignore</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>语法：与 gitignore 兼容的子集。</p><h2 id="_6-工具行为" tabindex="-1">6. 工具行为 <a class="header-anchor" href="#_6-工具行为" aria-label="Permalink to &quot;6. 工具行为&quot;">​</a></h2><table tabindex="0"><thead><tr><th>工具</th><th>忽略应用程序</th></tr></thead><tbody><tr><td>Glob</td><td>无范围遍历：第 1–4 层过滤结果；显式 <code>path</code>：仅第 1 层</td></tr><tr><td>Grep</td><td>无范围遍历：第 1–4 层过滤文件集（进程内遍历器与系统 <code>rg</code> 快速路径一致）；显式 <code>path</code>：仅第 1 层</td></tr><tr><td>Read</td><td>命中拒绝名单的文件返回 <code>WORKSPACE_PATH_DENIED</code>；否则当显式路径在外部时权限门控；拒绝后 <code>TOOL_DENIED</code></td></tr><tr><td>Write/Edit</td><td>命中拒绝名单的文件或移动目标返回 <code>WORKSPACE_PATH_DENIED</code>；否则当显式路径在外部时权限门控；拒绝后 <code>TOOL_DENIED</code></td></tr><tr><td>Bash</td><td>路径沙箱仍然由主机强制执行；忽略文件不会扩展 bash 权限</td></tr></tbody></table><h2 id="_7-诊断" tabindex="-1">7. 诊断 <a class="header-anchor" href="#_7-诊断" aria-label="Permalink to &quot;7. 诊断&quot;">​</a></h2><p>工具应返回稳定的错误：</p><ul><li><code>PATH_OUTSIDE_WORKSPACE</code> — 在做出外部路径权限决策之前，路径逃逸了 工作区根目录</li><li><code>TOOL_DENIED</code> — 外部路径权限被拒绝、超时或取消</li><li><code>WORKSPACE_PATH_DENIED</code> — 显式路径命中了安全拒绝名单（请参阅 <a href="/zh-CN/spec/03-runtime/08-error-codes">08-错误代码 §3.3</a>）</li></ul><p>UI 可以选择稍后显示 Glob/Grep 的“被忽略规则隐藏”计数。</p><h2 id="_8-验收标准" tabindex="-1">8. 验收标准 <a class="header-anchor" href="#_8-验收标准" aria-label="Permalink to &quot;8. 验收标准&quot;">​</a></h2><ul><li>[x] 外部路径在非自动模式下需要许可，并且在自动模式下允许</li><li>[x] 默认忽略规则在 Glob/Grep 中隐藏 node_modules</li><li>[x] 工作区忽略文件得到遵守</li><li>[x] 无法从 MVP 中的 UI 禁用安全拒绝列表</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("zh-CN/spec/03-runtime/15-workspace-ignore-rules.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _15WorkspaceIgnoreRules = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _15WorkspaceIgnoreRules as default
};
