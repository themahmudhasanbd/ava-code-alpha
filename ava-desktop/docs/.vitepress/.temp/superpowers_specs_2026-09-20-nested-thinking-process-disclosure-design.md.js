import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Nested Thinking and Tool Process Disclosure","description":"","frontmatter":{},"headers":[],"relativePath":"superpowers/specs/2026-09-20-nested-thinking-process-disclosure-design.md","filePath":"superpowers/specs/2026-09-20-nested-thinking-process-disclosure-design.md","lastUpdated":1789913128000}');
const _sfc_main = { name: "superpowers/specs/2026-09-20-nested-thinking-process-disclosure-design.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="nested-thinking-and-tool-process-disclosure" tabindex="-1">Nested Thinking and Tool Process Disclosure <a class="header-anchor" href="#nested-thinking-and-tool-process-disclosure" aria-label="Permalink to &quot;Nested Thinking and Tool Process Disclosure&quot;">​</a></h1><h2 id="status-and-scope" tabindex="-1">Status and scope <a class="header-anchor" href="#status-and-scope" aria-label="Permalink to &quot;Status and scope&quot;">​</a></h2><ul><li>Status: Proposed for review; implementation is not authorized by this document.</li><li>Date: 2026-09-20.</li><li>Branch: <code>feat/thinking-process-collapse</code>, directly in the project checkout as requested.</li><li>Inspected baseline: <code>aad46adb</code> (local <code>main</code> and cached <code>origin/main</code> at intake). Fetching remote <code>main</code> failed with SSH <code>Permission denied (publickey)</code>; this proposal does not claim verification against the latest remote revision.</li><li>Deliverable: interaction design, implementation boundaries, and acceptance criteria only. No runtime, UI, settings, or accepted specification changes are included.</li><li>Validation scope, per the user&#39;s explicit instruction: static checks and compilation only. Runtime tests are not required for this proposal or its implementation; section 8 defines the scoped validation commands.</li><li>Reference: the three screenshots supplied with the request. They demonstrate whole-process and command-group disclosure; they do not establish Codex&#39;s live defaults, persistence rules, or internal implementation. Those details below are PI-Desktop design decisions.</li></ul><h2 id="_1-problem-and-verified-current-behavior" tabindex="-1">1. Problem and verified current behavior <a class="header-anchor" href="#_1-problem-and-verified-current-behavior" aria-label="Permalink to &quot;1. Problem and verified current behavior&quot;">​</a></h2><p>In the supplied PI-Desktop screenshot, consecutive search/tool and thinking rows remain visible between assistant progress paragraphs. Users can inspect individual details, but cannot collapse the entire block of rows as one unit in detailed mode. A long operation therefore makes progress narration and the final answer harder to scan.</p><p>The gap is at the container levels, not an absence of all disclosure controls:</p><table tabindex="0"><thead><tr><th>Surface</th><th>Verified implementation</th><th>Consequence</th></tr></thead><tbody><tr><td>Whole process</td><td><code>shouldGroupTurnProcess()</code> returns true only for compact mode</td><td>Detailed mode has no whole-process disclosure</td></tr><tr><td>Ordinary activity group</td><td><code>AssistantTurn</code> passes <code>embedded</code>; <code>ActivityGroup</code> returns rows directly for embedded groups without subagents</td><td>There is no group header to hide a cluster of tool/thinking rows</td></tr><tr><td>Individual tool/search/thinking</td><td><code>ToolRow</code>, <code>HostedSearchRow</code>, and <code>ThinkingRow</code> already have disclosure behavior</td><td>Preserve these controls and their payload rendering</td></tr><tr><td>Automatic opening</td><td>Only the literal final item of the last activity group receives the tool/search auto-open flag; failed/denied tools suppress it. If that final item is thinking, no earlier tool is selected instead</td><td>Preserve this positional rule; nesting must not open every payload</td></tr><tr><td>User ownership</td><td><code>useAutomaticDisclosure()</code> stops automatic changes after manual interaction for that component&#39;s lifetime</td><td>Preserve and extend this principle across ancestors</td></tr><tr><td>Existing specification</td><td>The accepted turn-process ADR explicitly says detailed mode does not wrap a process</td><td>This proposal intentionally changes that presentation decision; it is not an already-approved contract</td></tr></tbody></table><p>Source references are listed in section 10. Findings are based on source and test inspection; the running desktop was not used to reproduce the screenshots.</p><h2 id="_2-recommended-interaction-model" tabindex="-1">2. Recommended interaction model <a class="header-anchor" href="#_2-recommended-interaction-model" aria-label="Permalink to &quot;2. Recommended interaction model&quot;">​</a></h2><p>Use three independent disclosure levels:</p><ol><li><strong>Turn process:</strong> all process content belonging to one existing assistant-turn entry, with a duration/status header. The trailing response and assistant errors remain outside it.</li><li><strong>Activity group:</strong> one contiguous segment of tools, provider-hosted searches, and reasoning rows between assistant progress paragraphs. Collapsing it hides all its item headers and details, leaving one summary row.</li><li><strong>Item details:</strong> the existing tool arguments/results, command output, search results, or reasoning text for one item.</li></ol><p>Progress narration remains between groups within the turn process. Users can read the explanation with execution details folded away, expand one group, then inspect one result. Closing the outer process hides the complete process with one action.</p><p>“Process” is a UI umbrella term. Provider reasoning, assistant progress text, tool execution, and the final answer remain distinct data and are not merged into one reasoning field.</p><h3 id="example-the-user-s-search-sequence" tabindex="-1">Example: the user&#39;s search sequence <a class="header-anchor" href="#example-the-user-s-search-sequence" aria-label="Permalink to &quot;Example: the user&#39;s search sequence&quot;">​</a></h3><p>Fully collapsed process:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&gt; Processed for 9m 36s · 4 tools</span></span>
<span class="line"><span></span></span>
<span class="line"><span>The final answer remains visible here.</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>Process expanded, activity groups collapsed:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>v Processed for 9m 36s · 4 tools</span></span>
<span class="line"><span>  I will locate the compression threshold and its defaults.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>  &gt; Searched code · 2 searches · includes thinking</span></span>
<span class="line"><span></span></span>
<span class="line"><span>  I found the core file and will inspect its defaults.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>  &gt; Searched code · 2 searches · includes thinking</span></span>
<span class="line"><span></span></span>
<span class="line"><span>The final answer remains visible here.</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br></div></div><p>One activity group expanded, followed by an explicit click to expand one search item:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>v Processed for 9m 36s · 4 tools</span></span>
<span class="line"><span>  I will locate the compression threshold and its defaults.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>  v Searched code · 2 searches · includes thinking</span></span>
<span class="line"><span>    &gt; Search  (compact|compress).*threshold</span></span>
<span class="line"><span>    v Search  autoCompact|auto_compact</span></span>
<span class="line"><span>      Search arguments and results...</span></span>
<span class="line"><span>    &gt; Thinking  Inspect the shared context settings...</span></span>
<span class="line"><span></span></span>
<span class="line"><span>  I found the core file and will inspect its defaults.</span></span>
<span class="line"><span>  &gt; Searched code · 2 searches · includes thinking</span></span>
<span class="line"><span></span></span>
<span class="line"><span>The final answer remains visible here.</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br></div></div><p>The “includes thinking” text illustrates the mixed content in the screenshot. It may be omitted when space is tight, but the accessible group name must describe the content accurately. The same structure uses “Ran 5 commands” for a command-only group.</p><h3 id="grouping-rules" tabindex="-1">Grouping rules <a class="header-anchor" href="#grouping-rules" aria-label="Permalink to &quot;Grouping rules&quot;">​</a></h3><ul><li>Reuse existing <code>AssistantTurnPart</code> activity boundaries and item order. Do not infer phases from natural-language headings, elapsed-time thresholds, or tool names.</li><li>Assistant progress text ends an activity group. Do not combine groups across progress paragraphs merely because they run the same tool.</li><li>Thinking may remain in the same activity group as adjacent tools, matching the user&#39;s red-box examples. A thinking row alone is a direct item disclosure; avoid an empty extra group layer.</li><li>A single ordinary tool/search item also uses its item disclosure directly. A group header is useful for two or more visible items. Hidden compact-mode thinking does not create an otherwise redundant group.</li><li>When a live singleton grows into a group, retain its item identity and any manual choice. A manually opened item makes the new group open and user-owned, so newly introduced nesting does not hide content being read.</li><li>Keep existing Task topology boundaries. Do not fold parent tools into a subagent&#39;s work or duplicate subagent rows in an ordinary activity group. The existing topology card serves as that segment&#39;s second-level container. Keep its detail-panel presentation; extend navigation into that existing panel as specified in section 5.</li><li>User/system messages and compaction dividers keep their existing turn boundaries. Only loaded history is grouped; no reconstruction of unloaded turns is implied.</li></ul><h2 id="_3-default-states-and-manual-ownership" tabindex="-1">3. Default states and manual ownership <a class="header-anchor" href="#_3-default-states-and-manual-ownership" aria-label="Permalink to &quot;3. Default states and manual ownership&quot;">​</a></h2><p>The setting remains <code>thinkingDisplayMode: detailed | compact</code>, with absent/unknown values resolving to detailed. No additional settings are needed.</p><p>The table describes untouched controls. Explicit user choices override these defaults.</p><table tabindex="0"><thead><tr><th>Level</th><th>Detailed: active</th><th>Detailed: completed/history</th><th>Compact</th></tr></thead><tbody><tr><td>Whole process</td><td>Open</td><td>Open</td><td>Closed, except an untouched active turn containing any recorded failed/denied tool remains open, even after later successful tools</td></tr><tr><td>Ordinary activity group</td><td>Open while it owns the active execution segment</td><td>Closed</td><td>Closed</td></tr><tr><td>Tool/search payload</td><td>Preserve the literal-last-item rule described below</td><td>Preserve that same rule inside closed ancestors</td><td>Closed</td></tr><tr><td>Reasoning item</td><td>Keep existing live reasoning behavior</td><td>Closed</td><td>No reasoning text or excerpt; active thinking indicator only</td></tr><tr><td>Task topology</td><td>Retain current live/manual policy</td><td>Retain current policy</td><td>Retain current policy</td></tr></tbody></table><p>Detailed mode keeps completed progress narration visible by default, preserving its purpose. It gains a whole-process collapse control while completed execution groups become easier to scan. Adopting Codex-like nesting does not require silently changing detailed mode into compact mode.</p><p>The preserved leaf default applies only when the literal final item of the last activity group is a tool or hosted-search row, with that row&#39;s existing failure/denial guards. It does not scan backward past a thinking item to find a tool. Opening an untouched completed group exposes its precomputed leaf defaults; it does not reset them closed. Thus a group ending in an eligible tool can reveal that tool&#39;s output immediately, while the screenshot example ending in thinking reveals only headers until an item is explicitly opened.</p><h3 id="state-transition-rules" tabindex="-1">State transition rules <a class="header-anchor" href="#state-transition-rules" aria-label="Permalink to &quot;State transition rules&quot;">​</a></h3><ul><li>Clicking a header changes only that level. Opening a group never means “expand all descendants.” Closing a parent does not reset any child choices.</li><li>Opening a previously closed parent restores child states. Sibling groups remain independent; this is not an exclusive accordion.</li><li>A manual action on an item claims the containing group and turn as user-owned, without toggling either ancestor. Completion must not close the container around an output the user explicitly opened.</li><li>If a user closes an active group or the whole process, new tool calls, streaming text, retries, and completion do not reopen it. Only summary/status information changes.</li><li>Automatic completion may close an untouched active group. It must not close a group whose output is being selected, whose body holds keyboard focus, or whose body was explicitly interacted with. Those actions establish user ownership.</li><li>A tool error does not imply that the assistant turn failed. A later successful recovery does not erase the earlier tool&#39;s failure marker.</li><li>Show a collapsed group&#39;s running or failure summary even when its payload is hidden. If the whole process is closed, propagate the aggregate to its visible header. Do not reopen manually closed ancestors to show an error. Preserve the compact outer exception: while the turn is active, any recorded failed/denied tool keeps an untouched outer process open through later progress or successful recovery. It closes automatically on turn completion only if still untouched. Inner groups remain closed with issue counts visible; raw error output requires explicit opening. The design must account for failure -&gt; progress -&gt; successful tool -&gt; turn completion, both untouched and manually closed; review this path statically.</li><li>No timer-based auto-collapse and no automatic “collapse everything when the answer arrives.” The default detailed process remains open unless the user closes it.</li></ul><h3 id="state-lifetime" tabindex="-1">State lifetime <a class="header-anchor" href="#state-lifetime" aria-label="Permalink to &quot;State lifetime&quot;">​</a></h3><p>Disclosure state belongs to the transcript presentation, keyed by session, stable turn identity, activity identity, and item identity. Do not use array indexes or localized labels as keys.</p><p>The initial implementation should retain choices while the owning session pane is retained, including streaming, completion, mode changes, and row remounts within its mounted-history window. Evicting the session pane, deleting the session, or restarting the renderer releases those choices and reapplies defaults. Cross-restart persistence is out of scope.</p><p>Use one authoritative pane-owned disclosure map for retained state; component-local state cannot satisfy remount and reparenting guarantees. Each node records its effective <code>open</code> value, ownership (<code>automatic</code> or <code>user</code>), and last applied reveal request. Stable item keys use message ID plus kind; hosted search adds its round ID. Group identity derives from its first unfiltered activity item. Mode filtering cannot change keys. Keep this map out of persisted messages and the central workflow store.</p><p>When a singleton gains a group wrapper, reuse its item record unchanged. Create the group record from the mode default unless a manually opened or focused/selected child requires an open, user-owned group; an explicitly closed ancestor still wins. When filtering removes a redundant wrapper, retain its record for later restoration. Explicit reveal/toggle events apply in event order; automatic updates never overwrite user-owned records. Search applies only once per new request. Release records on session-pane eviction/deletion/restart and prune nodes authoritatively removed from the transcript, not nodes merely outside the mounted window.</p><p>Mode switches preserve explicit choices for surviving nodes and reapply defaults only to untouched nodes. Compact still suppresses all reasoning text, regardless of a retained thinking-item open state. Switching back to detailed restores that item&#39;s choice within the retained pane.</p><h2 id="_4-headers-hierarchy-and-status" tabindex="-1">4. Headers, hierarchy, and status <a class="header-anchor" href="#_4-headers-hierarchy-and-status" aria-label="Permalink to &quot;4. Headers, hierarchy, and status&quot;">​</a></h2><h3 id="whole-process-header" tabindex="-1">Whole-process header <a class="header-anchor" href="#whole-process-header" aria-label="Permalink to &quot;Whole-process header&quot;">​</a></h3><p>Use one lightweight header with a consistent chevron, status/duration, a count, and an issue marker when necessary. Use the existing recorded timing helpers; UI clocks must not introduce persisted timing fields or invent a precise historical duration when evidence is absent.</p><p>Suggested localized labels:</p><table tabindex="0"><thead><tr><th>Meaning</th><th>English</th><th>Simplified Chinese</th></tr></thead><tbody><tr><td>Reasoning is currently active</td><td time="">Thinking for</td><td time="">思考中</td></tr><tr><td>Tools or preparation are active</td><td time="">Processing for</td><td time="">处理中</td></tr><tr><td>Process has ended</td><td time="">Processed for</td><td time="">用时</td></tr><tr><td>Homogeneous command group</td><td>Ran {count} commands</td><td>运行了 {count} 条命令</td></tr><tr><td>Homogeneous search group</td><td>Searched code · {count} searches</td><td>搜索代码 · {count} 次</td></tr><tr><td>Mixed group</td><td>Tool activity · {count} tools</td><td>工具操作 · {count} 项</td></tr><tr><td>Mixed group with reasoning</td><td>Includes thinking</td><td>含思考</td></tr><tr><td>Group contains failure</td><td>{count} failed</td><td>{count} 项失败</td></tr></tbody></table><p>Final wording goes through existing i18n/pluralization conventions. The process count should name what it counts: tools/search rounds rather than silently including progress paragraphs under a “tools” label. Count each direct tool invocation and hosted search round once; do not double-count delegated child work. If there are no tools, omit the tool count. Detailed thinking-only processes remain collapsible; completed compact thinking-only processes leave no empty header.</p><h3 id="group-and-item-headers" tabindex="-1">Group and item headers <a class="header-anchor" href="#group-and-item-headers" aria-label="Permalink to &quot;Group and item headers&quot;">​</a></h3><ul><li>Derive group labels from existing action categories; use a neutral mixed label rather than calling every tool a command. Multiple reasoning-only items use a Thinking group with a reasoning-item count, never a zero-tool activity label. Compact omits completed reasoning-only groups entirely.</li><li>A collapsed group shows count, running state, and issue count as applicable. Live updates remain bounded to the summary row; do not print raw output or long reasoning excerpts beneath a folded group.</li><li>Keep command text, file/search summary, output preview links, copy actions, status and duration on item headers as currently supported. Long commands truncate in the header; full content remains available through existing detail/copy surfaces.</li><li>Failed, denied, cancelled and running states use text/icon distinctions, not color alone. Status aggregation must reuse existing command exit-code and delegation outcome logic; a tool transport success with a nonzero shell exit remains a failed command.</li><li>Preserve existing single-owner live-status selection. The new group must not add another independent “working” indicator beside the transcript&#39;s current authoritative status.</li></ul><h3 id="visual-treatment" tabindex="-1">Visual treatment <a class="header-anchor" href="#visual-treatment" aria-label="Permalink to &quot;Visual treatment&quot;">​</a></h3><p>Use existing typography, color, radius and motion tokens with lightweight rows. Indent each container level modestly; avoid three nested cards or deep indentation that consumes command-output width. Keep chevrons visible at rest, pointing right when closed and down when open. Hover treatment supplements the chevron rather than being the only discovery cue.</p><p>Each header and optional collapse rail controls its own container. Nested CSS selectors must be scoped to direct children so opening the outer process does not rotate every inner chevron or expand every descendant body. Expanded output may retain its existing bounded scrolling; do not add a second scrollable container for ordinary groups.</p><p>On narrow windows, preserve status and disclosure controls; truncate optional summaries first. Cover light, dark, and plugin themes and reduced-motion preferences using existing tokens.</p><h2 id="_5-search-accessibility-and-reading-position" tabindex="-1">5. Search, accessibility, and reading position <a class="header-anchor" href="#_5-search-accessibility-and-reading-position" aria-label="Permalink to &quot;5. Search, accessibility, and reading position&quot;">​</a></h2><h3 id="search-and-navigation" tabindex="-1">Search and navigation <a class="header-anchor" href="#search-and-navigation" aria-label="Permalink to &quot;Search and navigation&quot;">​</a></h3><p>For an existing search/navigation target, reveal the ancestor path needed to expose it: turn, containing group, then item detail when the target is in that detail. The current renderer <code>TranscriptSearchTarget</code> carries only <code>messageId</code>, which cannot distinguish thinking, response text and multiple hosted-search rounds. Add a renderer-local target descriptor with item kind (<code>message</code>, <code>thinking</code>, <code>tool</code>, <code>hostedSearch</code>), message ID, hosted-search round ID where applicable, and the owning Task/delegation identity for delegated children. Preserve the current session/query/request fields. Existing message-only search results continue to target message content; a more precise kind is supplied only when the navigation source can identify that surface. Do not invent a target from an ambiguous message ID or change host indexing as part of this design.</p><p>Give each target a stable item DOM anchor using the same item identity as disclosure state, rather than selecting the first matching message ID. Apply the reveal request to its ancestor path, await the React commit and target registration in the owning scroll surface, then highlight/focus/anchor it. Use layout/registration synchronization rather than a fixed delay. A newer request, session change or pane disposal invalidates outstanding focus work.</p><p>For a delegated-child target, reveal the owning Task topology and select the existing subagent detail panel; do not introduce an additional inline Task rendering. Resolve the latest owning Task in a resumed delegation chain, then reveal the child&#39;s item inside the panel and focus it through the panel&#39;s own scroll owner. Extend the panel&#39;s reveal input and item anchors for this purpose. If the owning Task or child cannot be loaded through existing navigation, report that the target is unavailable rather than focusing an unrelated main-turn row.</p><p>A new explicit reveal request may override a previous manual close. Re-rendering the same request must not repeatedly force-open a node after the user closes it. Closing search does not collapse the path automatically.</p><p>This proposal does not expand search indexing scope. Compact-mode reasoning remains hidden: a reasoning-only target should offer the existing mode switch to detailed rather than silently revealing text or changing the saved setting. If the target is not loaded, use existing history loading before resolving the disclosure path.</p><h3 id="keyboard-and-assistive-technology" tabindex="-1">Keyboard and assistive technology <a class="header-anchor" href="#keyboard-and-assistive-technology" aria-label="Permalink to &quot;Keyboard and assistive technology&quot;">​</a></h3><ul><li>Use real buttons for disclosure headers with <code>aria-expanded</code>, stable <code>aria-controls</code>, localized names and visible focus treatment. Enter and Space toggle only the focused header.</li><li>Closed descendants are hidden/inert or unmounted, excluded from tab order and accessibility traversal. Do not nest buttons inside buttons.</li><li>Copy, file-preview and link actions do not bubble into a parent toggle. Text selection does not toggle a disclosure.</li><li>Before hiding content that contains focus, move focus to its visible controlling header. If an ancestor is also closing, focus the nearest remaining visible ancestor header.</li><li>Announce material status changes politely through the existing status owner; do not announce every token or every elapsed second.</li><li>Collapse rails must have distinct accessible labels, such as “Collapse tool group” and “Collapse command output.” They supplement the header control.</li></ul><h3 id="scroll-ownership" tabindex="-1">Scroll ownership <a class="header-anchor" href="#scroll-ownership" aria-label="Permalink to &quot;Scroll ownership&quot;">​</a></h3><p>Preserve the existing disclosure anchor mechanism. Capture the clicked level&#39;s header and viewport offset before changing height, and keep that header stable through layout/animation. Only the initiating disclosure claims the scroll anchor; claiming ancestors for manual ownership must not also claim their scroll positions.</p><p>If the user has scrolled away from the bottom, expanding/collapsing does not jump to the latest output. Stream-follow resumes only through existing follow/jump behavior. Opening ancestors for search uses the search target as its final anchor. Parent visibility changes do not reset a retained child output&#39;s reading position.</p><h2 id="_6-responses-errors-and-actionable-content" tabindex="-1">6. Responses, errors, and actionable content <a class="header-anchor" href="#_6-responses-errors-and-actionable-content" aria-label="Permalink to &quot;6. Responses, errors, and actionable content&quot;">​</a></h2><p>Reuse <code>projectTurnProcess()</code> semantics: the trailing assistant text is provisionally visible outside the process while streaming. If later activity proves it was progress, move it into the process in original order. Do not infer a final answer from its wording. Preserve its identity and reading anchor during reclassification, and do not reopen a manually closed process.</p><p>Assistant errors and stopped trailing partial answers remain visible outside the process. Keep user messages, permission requests, ask/question interactions, plan/goal approval surfaces and outcome cards reachable according to their existing ownership. Never make a pending decision accessible only by expanding a hidden ancestor.</p><p>Review/change cards need an explicit placement check during implementation: completed informational cards may remain with their tool, while any card requiring a user action must have an always-reachable existing action surface. Do not duplicate approval controls or change their execution semantics to solve visibility.</p><p>Parent process collapse does not close an already-open subagent detail panel or cancel work. Folding is purely presentational: no effect on tool execution, permissions, retries, cancellation, stored reasoning, context use, export or copy payloads.</p><h2 id="_7-implementation-outline-and-compatibility" tabindex="-1">7. Implementation outline and compatibility <a class="header-anchor" href="#_7-implementation-outline-and-compatibility" aria-label="Permalink to &quot;7. Implementation outline and compatibility&quot;">​</a></h2><p>This is a renderer presentation change. No new dependency, host API, database migration, provider protocol change, or settings enum is needed for the proposed core behavior.</p><table tabindex="0"><thead><tr><th>Area</th><th>Proposed work</th></tr></thead><tbody><tr><td><code>lib/turn-process.ts</code></td><td>Enable the process container in both modes; separate container availability from mode-specific defaults and visible-item counts; preserve response projection</td></tr><tr><td><code>AssistantTurn.tsx</code></td><td>Compose one process plus responses; retain stable identities and existing delegation status ownership</td></tr><tr><td><code>ActivityGroup.tsx</code></td><td>Restore ordinary group disclosure when there are multiple visible items; preserve singleton and topology treatments; keep presentation logic separate from delegation/runtime status</td></tr><tr><td><code>shared.tsx</code> / disclosure hook</td><td>Preserve manual ownership, add precise ancestor claiming and any necessary pane-owned state; keep scroll anchoring under its current owner</td></tr><tr><td><code>ToolRow.tsx</code>, <code>HostedSearchRow.tsx</code>, <code>ThinkingRow</code></td><td>Reuse item rendering; connect precise reveal paths and retained item states without duplicating payload logic</td></tr><tr><td>Transcript pane/search integration</td><td>Scope state lifecycle and reveal identity; retain existing loading and live-status authority</td></tr><tr><td><code>styles/messages.css</code>, i18n</td><td>Add scoped nested-container styles, meaningful localized headers and accessible names</td></tr></tbody></table><p>Do not add all new state and policy to the already-large transcript components. Extract only the directly required pure grouping/default-state logic or disclosure hook. Avoid a generic arbitrary-depth tree framework: the product needs the three levels defined here and the existing subagent panel.</p><p>Implementation sequence after design agreement:</p><ol><li>Review the current source against the screenshot sequence and singleton, compact, error and delegation behavior requirements.</li><li>Implement deterministic container/default rules and stable disclosure identities; review pure logic and state transitions statically.</li><li>Wire both modes to the outer process and restore ordinary activity groups, preserving existing item details and user actions.</li><li>Statically review interaction paths, search, focus, scrolling and stream updates; complete the compilation and static checks listed in section 8.</li><li>Update the accepted turn-process ADR, directly affected UX specs, translations and release notes in the implementation change. Synchronize existing scenario documentation only where necessary to avoid contradicting the new behavior; this does not require adding or running runtime tests.</li></ol><p>The current proposal does not overwrite accepted specs to make them claim that the feature already exists. The following documents need coordinated updates when implemented:</p><ul><li><code>docs/adr/turn-process-and-thinking-display.md</code>: revise the “detailed does not wrap a process” decision and explain compatibility/default choices.</li><li><code>docs/spec/04-ux/08-component-spec.md</code>: hierarchy, defaults, state lifetime and headers.</li><li><code>docs/spec/04-ux/09-interaction-patterns.md</code>: nested disclosure, search reveal, focus and scroll rules.</li><li><code>docs/spec/04-ux/06-settings-ia.md</code>: update the descriptions of Detailed/Compact without adding settings.</li><li><code>docs/spec/06-delivery/04-e2e-test-plan.md</code>: if existing scenario descriptions contradict the new behavior, synchronize their text and record this task&#39;s static-only validation scope. This is documentation alignment, not a requirement to add or execute E2E tests.</li><li>Corresponding documentation translations and user-facing release notes.</li></ul><h2 id="_8-acceptance-and-validation" tabindex="-1">8. Acceptance and validation <a class="header-anchor" href="#_8-acceptance-and-validation" aria-label="Permalink to &quot;8. Acceptance and validation&quot;">​</a></h2><p>Per the user&#39;s explicit instruction, acceptance for this task and its implementation is limited to static checks and compilation. The user path and edge cases below define intended product behavior for source/design review; they are not executable test requirements or a manual runtime test checklist. Do not add or run unit, mounted-component, integration, browser, Electron or E2E tests for this task, and do not start or attach to a desktop instance for validation.</p><h3 id="representative-user-path" tabindex="-1">Representative user path <a class="header-anchor" href="#representative-user-path" aria-label="Permalink to &quot;Representative user path&quot;">​</a></h3><p>Given a detailed-mode turn containing progress paragraph A, two searches plus thinking, progress paragraph B, two commands plus thinking, and a final response:</p><ol><li>Both progress paragraphs and the final response are readable with completed groups collapsed.</li><li>Open the search group: only that group&#39;s item headers appear.</li><li>Open one search result: only its details appear; its siblings remain closed.</li><li>Close and reopen the group: the selected search result retains its state.</li><li>Open the command group independently; copy its command without collapsing any level.</li><li>Close the whole process: all progress/groups disappear; the final response stays visible.</li><li>Reopen the process: both groups and item choices are restored.</li><li>The clicked header remains at its reading position throughout manual toggles.</li></ol><h3 id="edge-cases-for-static-design-review" tabindex="-1">Edge cases for static design review <a class="header-anchor" href="#edge-cases-for-static-design-review" aria-label="Permalink to &quot;Edge cases for static design review&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Scenario</th><th>Expected result</th></tr></thead><tbody><tr><td>Active group manually closed, then more tools/results arrive</td><td>Stays closed; count/status updates</td></tr><tr><td>Item manually opened, then group/turn completes</td><td>Ancestors stay open; item state retained</td></tr><tr><td>Untouched active group ends</td><td>May close according to defaults; never hides focused/selected content</td></tr><tr><td>Singleton becomes a multi-item group</td><td>No loss of manual state, duplicated payload, or hidden focused item</td></tr><tr><td>Completed history in detailed mode</td><td>Outer process open; ordinary groups closed; item defaults preserved inside</td></tr><tr><td>Parent closed while a child is open</td><td>Child state and reading position retained</td></tr><tr><td>Detailed → compact → detailed</td><td>Reasoning absent in compact; explicit choices restored for surviving nodes</td></tr><tr><td>Failure, denial, nonzero exit, cancellation or recovery</td><td>Accurate item/group status; no false whole-turn failure</td></tr><tr><td>Permission/question/proposal pending</td><td>Actionable surface remains reachable with process closed</td></tr><tr><td>Pure reasoning / pure answer / tools without answer</td><td>No redundant or empty compact containers; available details remain inspectable</td></tr><tr><td>Search targets progress vs tool output vs hosted search</td><td>Only required ancestors open; target is unambiguous and visible</td></tr><tr><td>Same search request rerenders after manual close</td><td>No repeated forced reopening</td></tr><tr><td>Retained pane switch / history row remount / pane eviction</td><td>Choices retained within pane lifetime; eviction resets deliberately</td></tr><tr><td>Delegations and later parent tools</td><td>Existing topology ownership preserved; no duplicate or misleading nesting</td></tr><tr><td>Narrow window, themes, keyboard, reduced motion</td><td>Controls usable; focus and status readable; no ancestor CSS leakage</td></tr><tr><td>Repeated streaming in a long transcript</td><td>Unchanged historical groups retain memoized boundaries; hidden payloads are not eagerly formatted</td></tr></tbody></table><h3 id="static-and-compilation-checks" tabindex="-1">Static and compilation checks <a class="header-anchor" href="#static-and-compilation-checks" aria-label="Permalink to &quot;Static and compilation checks&quot;">​</a></h3><p>After implementation, run the existing static/build commands applicable to the changed renderer surface:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> build:js</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --filter</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> @pi-desktop/desktop</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> typecheck</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> lint</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> docs:check</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">git</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> diff</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --check</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>Review the diff for stable disclosure identities, state ownership, ancestor visibility, search-target wiring, i18n keys and scoped CSS selectors. Existing runtime-test sources may be read as evidence of prior behavior; this does not authorize running them or require expanding them.</p><p>For this document-only change, check local references, encoding, whitespace and document consistency; application compilation is deferred until implementation changes application code. Report only commands actually executed. Static checks and compilation do not prove runtime scrolling, focus, streaming or visual behavior, so do not report those paths as runtime-validated.</p><h2 id="_9-alternatives-and-decisions-to-review" tabindex="-1">9. Alternatives and decisions to review <a class="header-anchor" href="#_9-alternatives-and-decisions-to-review" aria-label="Permalink to &quot;9. Alternatives and decisions to review&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Alternative</th><th>Assessment</th></tr></thead><tbody><tr><td>Only expose ordinary activity-group headers</td><td>Fixes the red-box symptom, but leaves detailed mode without the requested whole-process collapse</td></tr><tr><td>Add only a whole-process wrapper</td><td>Lets users hide everything, but cannot retain progress narration while hiding one execution block</td></tr><tr><td>Collapse completed whole processes by default in detailed mode</td><td>Reduces height further, but hides progress narration and makes Detailed/Compact less distinct; not recommended for the initial change</td></tr><tr><td>Always add all three levels, even for one item</td><td>Creates redundant clicks and repeated labels; use singleton elision with retained item state</td></tr><tr><td>Persist every disclosure to the host</td><td>Adds storage and lifecycle complexity without a requirement; retain pane-lifetime state only</td></tr></tbody></table><p>Recommended review baseline: adopt all three independent levels, keep detailed process narration open by default, fold completed ordinary groups, and retain the current leaf-detail defaults. Do not add expand-all actions or new preferences until actual use demonstrates a need.</p><h2 id="_10-inspected-source-and-existing-contracts" tabindex="-1">10. Inspected source and existing contracts <a class="header-anchor" href="#_10-inspected-source-and-existing-contracts" aria-label="Permalink to &quot;10. Inspected source and existing contracts&quot;">​</a></h2><p>Paths are relative to this proposal. They describe the inspected baseline, not future guarantees.</p><ul><li><a href="../../../apps/desktop/src/lib/turn-process.ts">Turn process projection and defaults</a></li><li><a href="../../../apps/desktop/src/lib/assistant-turns.ts">Assistant turn and activity grouping</a></li><li><code>apps/desktop/src/features/chat/transcript/AssistantTurn.tsx</code> — AssistantTurn composition</li><li><code>apps/desktop/src/features/chat/transcript/TurnProcess.tsx</code> — TurnProcess container</li><li><code>apps/desktop/src/features/chat/transcript/ActivityGroup.tsx</code> — ActivityGroup and embedded behavior</li><li><code>apps/desktop/src/features/chat/transcript/ToolRow.tsx</code> — ToolRow disclosure and command outcome</li><li><code>apps/desktop/src/features/chat/transcript/HostedSearchRow.tsx</code> — HostedSearchRow</li><li><code>apps/desktop/src/features/chat/transcript/shared.tsx</code> — Disclosure ownership and ThinkingRow</li><li><a href="../../../apps/desktop/src/features/chat/transcript/hooks/useTranscriptScroll.ts">Transcript scroll owner</a></li><li><code>scripts/e2e/turn-process.tsx</code> — Current turn-process interaction probe</li><li><code>scripts/e2e/transcript-disclosure-anchor.tsx</code> — Disclosure anchoring probe</li><li><a href="./../../adr/turn-process-and-thinking-display">Accepted turn-process ADR</a></li><li><a href="./../../adr/transcript-reading-ownership">Transcript reading ownership ADR</a></li><li><a href="./../../spec/04-ux/08-component-spec">Component specification</a></li><li><a href="./../../spec/04-ux/09-interaction-patterns">Interaction specification</a></li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("superpowers/specs/2026-09-20-nested-thinking-process-disclosure-design.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _20260920NestedThinkingProcessDisclosureDesign = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _20260920NestedThinkingProcessDisclosureDesign as default
};
