import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0239: Host-owned session collaboration messages","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0239-session-collaboration-messages.md","filePath":"adr/0239-session-collaboration-messages.md","lastUpdated":1789800041000}');
const _sfc_main = { name: "adr/0239-session-collaboration-messages.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0239-host-owned-session-collaboration-messages" tabindex="-1">ADR 0239: Host-owned session collaboration messages <a class="header-anchor" href="#adr-0239-host-owned-session-collaboration-messages" aria-label="Permalink to &quot;ADR 0239: Host-owned session collaboration messages&quot;">​</a></h1><ul><li>Status: Accepted; amended by D446</li><li>Date: 2026-09-13</li><li>Decision: D409 (amended by D446)</li><li>Amends: ADR 0237, ADR 0165, ADR 0213</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Session Orchestrator must address existing durable sessions in both directions, reuse their context, deliver completion callbacks, and expose reliable state. Polling assistant text cannot establish a turn&#39;s outcome. Plugin settings must not become an authority for session identity, authorization, or execution state.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Rust host-core owns a durable session communication ledger keyed by message ID, referencing the existing source and target Session IDs. Session IDs remain the only session identity. Message IDs identify deliveries and idempotency, never a second worker or work identity. A delivery is bound to its actual durable turn before execution, and results derive from that turn&#39;s persisted terminal state.</p><p>The official plugin composes reviewed desktop operations for spawn, send, status, result, and cancellation. The host authenticates the sending Session ID from a correlated plugin tool invocation; plugins cannot supply a forged sender. Existing target sessions keep their project, model, context, and permissions. New sessions inherit the initiating session&#39;s project and permission mode. Autonomous session creation is bounded separately from bidirectional messaging.</p><p>The Agent Host&#39;s existing admission and turn queue remain the execution owner. The ledger retains deliveries across dequeue and process failure. A busy target queues a message; an idle target starts it. Restored work follows the existing startup fence and is never replayed merely because the application restarted. Permission checks remain host-owned; session messaging cannot raise a target&#39;s effective permission above the initiating operation&#39;s authorized ceiling.</p><p>Messages have explicit task, message, or completion provenance, persisted with the transcript and rendered separately from human input. Live model prompts and restored history use the same source framing. Session messages are not new human authorization and do not participate in user-message editing or regeneration. Accepted user steering into a claimed delivery turn remains human input: it does not receive the delivery origin, and a client-supplied <code>session_message</code> is stripped (D597).</p><p>A requested completion callback produces at most one durable completion message to the originating session. It references the original delivery and the actual turn outcome. Completion messages never request another automatic callback. The host bounds autonomous communication chains and retains delivery failures for passive inspection. Cancellation preserves the session and its history.</p><p>The plugin obtains available models from the host&#39;s public model catalog. An unspecified model is chosen from bindings enabled for AI delegation; if none are enabled, the configured default is used. Explicit requests resolve against real configured keys, IDs, aliases, names, and supported capability intent. Ambiguous or unmatched requests return candidates or a clear error without silently substituting an unrelated model. Reusing a session does not reselect its model.</p><p>The existing session-list hover card gains an on-demand host projection of its creation source, current task, recent exchanges, current status, and turn-bound result. The projection is bounded, supports keyboard focus, and does not load complete transcripts or add a read for every sidebar row.</p><h2 id="compatibility-and-validation" tabindex="-1">Compatibility and validation <a class="header-anchor" href="#compatibility-and-validation" aria-label="Permalink to &quot;Compatibility and validation&quot;">​</a></h2><p>The storage migration is additive. Existing conversations, plugin settings, and the core Task family remain intact. This is a deliberate plugin-mediated exception to ADR 0165&#39;s withdrawn cross-session coordination feature; the old A2A broker, tools, and protocol are not restored. ADR 0237&#39;s parent-only messaging restriction and polling-based result inference are superseded.</p><p>Validation covers concurrent senders, durable identity, reuse, queue admission, source preservation across reload, exactly-once callback creation, failure and cancellation results, permission ceilings, model resolution, hover lifecycle, and the relevant host/Electron E2E journeys.</p><h2 id="amendment-2026-09-18-d446-a-completion-notice-s-own-reply-may-stay-silent" tabindex="-1">Amendment (2026-09-18, D446): a completion notice&#39;s own reply may stay silent <a class="header-anchor" href="#amendment-2026-09-18-d446-a-completion-notice-s-own-reply-may-stay-silent" aria-label="Permalink to &quot;Amendment (2026-09-18, D446): a completion notice&#39;s own reply may stay silent&quot;">​</a></h2><p>Issue #504: the completion prompt states that a notice needs no acknowledgement, yet the runtime&#39;s silent-turn recovery (spec 02-agent-runtime §5e) retried the resulting silence and reported <code>EMPTY_MODEL_RESPONSE</code> after a task that had succeeded. This amendment bounds one exception; every other decision above is unchanged.</p><ul><li>The first settled assistant reply to a completion notice may end with no visible text and no tool call without silent-turn recovery or <code>EMPTY_MODEL_RESPONSE</code>. The reply is emitted as a completed message with the normal terminal lifecycle. Provider errors and aborts keep their handling; a provider retry of the same attempt keeps the exception.</li><li>The exception covers exactly that reply. It is spent by the first settled response whether silent, textual, or a tool batch, so a reply that follows tool results or accepted user steering in the same run is ordinary. It is also revoked as soon as an accepted steering message enters the model context, and every new run recomputes it.</li><li>Only provenance that Main resolved from the queued Host ledger record can enable it: <code>kind: completion</code>, the current target session, and nonempty message and reply-to IDs. Prompt text, plugin or model content, task and ordinary message deliveries, copied notice framing, and restored history cannot. No protocol field or caller authority is added.</li><li>An accepted silent reply is still not worth resending. Main persists it as an empty completed row (hidden by the renderer, <code>NULL</code> text in the host), but the runtime keeps it out of its entries and pi&#39;s transcript state, and the context projection drops any assistant without content blocks, matching what a restored transcript already did. The next request therefore carries no empty assistant message for a provider to reject or skip.</li></ul><p>Validation: runtime unit coverage for the accepted shapes, the trust boundary, tool batches, provider retry, steering before and after the reply, and context exclusion; <code>pnpm test:e2e:session-completion</code> against the real host ledger, sidecar, and a local SSE provider. See E2E-SESSION-completion-notice-allows-silence.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0239-session-collaboration-messages.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0239SessionCollaborationMessages = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0239SessionCollaborationMessages as default
};
