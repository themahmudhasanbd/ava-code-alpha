import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0138: Subagent Peer Messaging","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0138-subagent-peer-messaging.md","filePath":"adr/0138-subagent-peer-messaging.md","lastUpdated":1788336030000}');
const _sfc_main = { name: "adr/0138-subagent-peer-messaging.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0138-subagent-peer-messaging" tabindex="-1">ADR 0138: Subagent Peer Messaging <a class="header-anchor" href="#adr-0138-subagent-peer-messaging" aria-label="Permalink to &quot;ADR 0138: Subagent Peer Messaging&quot;">​</a></h1><ul><li>Status: Superseded by ADR 0147</li><li>Date: 2026-08-31</li><li>Deciders: PI-Desktop core</li><li>Related: D201, D277, ADR 0062, ADR 0089, ADR 0100, ADR 0140, ADR 0147, <code>03-runtime/02-agent-runtime.md</code> §5f / §5f.2, E2E-165</li><li>Amends: nothing. ADR 0062&#39;s parent-only integration point and ADR 0089&#39;s background delegation lifecycle are both preserved; this adds an opt-in sibling channel beside them.</li></ul><blockquote><p>Superseded by ADR 0147. The in-process <code>SubagentMailbox</code> and the peer tools this ADR introduced are removed and replaced by the A2A protocol stack: a host-core broker with durable tasks, typed message parts, agent-card discovery, and capability-token authorization over the existing JSON-RPC transport. Retained because the coordination failures it documents — stale premises under the path lock, disproved shared assumptions, duplicated searches — are exactly what the A2A stack now addresses, and because the invariants it froze (no forged sender, delegate-only tool, settled delegates leave) carry forward unchanged.</p></blockquote><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Delegation as built by ADR 0062 and ADR 0089 has exactly one integration point: the parent. A delegate receives one <code>task</code> brief at spawn time, runs alone, and returns one report bounded to 12k characters. Concurrent delegates cannot see each other. The only thing they share is mutual exclusion — <code>PathMutex</code> in the sidecar and host-core&#39;s one-in-flight-mutation-per-session rule — and both are locks with no payload.</p><p>For the fan-out pattern the <code>Task</code> description recommends (one delegate per independent direction, in one assistant message), that isolation is correct: the parent asked for independent answers and gets them. It stops being correct as soon as the directions turn out not to be independent, which the parent cannot always know when it writes the briefs:</p><ul><li>Two write-capable delegates discover mid-run that they need the same file. The path lock serializes the writes, so neither corrupts the other, but the second one&#39;s edit is built on a version it read before the first one&#39;s change. The lock prevents a torn write, not a stale premise.</li><li>One delegate disproves an assumption every brief was written against. It has no way to say so. It reports the correction, the parent reads it after <code>TaskWait</code>, and by then the other delegates have finished spending their turns on the wrong premise.</li><li>One delegate has already found the fact another is about to spend fifteen tool calls searching for. Both searches are paid in full, in separate contexts, and the duplication is invisible until the reports land.</li></ul><p>Routing these through the parent does not work. The parent is blocked in <code>TaskWait</code> while its delegates run, <code>task</code> is write-once so a running brief cannot be corrected, and adding a parent-mediated relay would mean every coordination note enters the parent&#39;s context — which is the exact cost delegation exists to avoid.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Add a <strong>session-scoped peer mailbox</strong> and three delegate-only tools, opt-in per definition.</p><ol><li><code>SubagentMailbox</code> is owned by the session runtime, one per session. A delegate never holds a reference to it; it reaches it only through the tools the runtime builds for it.</li><li>Three tools, declarable in a definition&#39;s <code>tools:</code> list alongside the existing seven: <ul><li><code>PeerSend(to?, text)</code> — deliver a note to one running peer, or to all of them when <code>to</code> is omitted.</li><li><code>PeerInbox()</code> — drain queued messages and list running peers; never blocks.</li><li><code>PeerWait(timeoutSeconds?)</code> — block until a message arrives, the timeout expires, the last peer leaves, or the run aborts.</li></ul></li><li><strong>Addressing is by peer id, not delegation id.</strong> When a single delegation of a definition runs, its peer id equals its agent name. When multiple delegations of the same definition run concurrently (e.g. three &quot;discussant&quot; roundtable participants), each gets a unique peer id by appending a numeric suffix (&quot;discussant-2&quot;, &quot;discussant-3&quot;) so every peer is individually addressable. Exposing delegation ids would hand a delegate a handle on the delegation registry, which is the parent&#39;s alone.</li><li><strong>The sender is bound at spawn time.</strong> Each peer tool closes over the delegate&#39;s own agent name, supplied by the runtime, so <code>from</code> is not a model input and a delegate can neither spoof a sender nor read another inbox.</li><li><strong>Opt-in and default-off.</strong> A definition that does not name a peer tool is unchanged, so every existing definition — including all four builtins — keeps ADR 0062 isolation exactly. None of the builtins declare one.</li><li><strong>Peer tools are absent from <code>toolCatalog</code>.</strong> They are constructed per delegate at spawn. The parent already owns the delegation lifecycle through the four <code>Task*</code> tools and must not gain a second, weaker channel to its delegates.</li><li><strong>A peer tool is not a host tool call.</strong> Messages are in-process, so they bypass <code>scopeDelegateTools</code>, carry no <code>permissionScope</code>, never reach host-core, and consume no tool budget. There is nothing to gate: no file, no process, no network.</li><li><strong>Peer traffic never enters the parent&#39;s model context.</strong> The parent still learns only what a report says. A delegate is told, in its prompt, that anything mattering to the parent must also be in its report.</li><li><strong>Messaging alone is not a delegate.</strong> A definition declaring only peer tools is refused at <code>Task</code> time with a tool error: it could talk but not work.</li><li>Every dimension is bounded, because a mailbox is shared mutable state between agents that are each trying to fill their own context: 2,000 characters per message, 32 messages per inbox (oldest dropped first, with the loss reported to the reader), 40 sends per run, and a <code>PeerWait</code> ceiling of 120 seconds against a 300-second delegate idle watchdog.</li><li><strong>Draining is destructive.</strong> Once read, a message is gone from the mailbox; the delegate&#39;s own context is the only copy.</li><li><strong>Membership tracks running delegates.</strong> A delegate joins when it starts and leaves when it settles. Leaving wakes every waiter, so a delegate parked in <code>PeerWait</code> on an agent that just exited returns immediately instead of waiting out its timeout.</li><li><strong>Peer ids are unique per concurrent delegation.</strong> When a definition declares peer tools, each delegation receives a unique peer id and its own mailbox inbox. The first delegation keeps the bare name (&quot;discussant&quot;); subsequent concurrent delegations of the same definition get suffixed ids (&quot;discussant-2&quot;, &quot;discussant-3&quot;). This ensures every delegate can address and be addressed individually, which is essential for multi-instance patterns such as structured roundtable debates.</li></ol><p>No IPC, storage, host protocol, or renderer change. <code>AgentEventEnvelope</code> is untouched: peer messages are tool calls of the delegate that sent or read them, so they already appear in the transcript attributed by <code>parentToolCallId</code> and <code>agentName</code> with no new wire contract.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Two write-capable delegates can claim files before editing them, turning the path lock from the only coordination primitive into the backstop it should be.</li><li>A delegate that disproves a shared assumption can say so while its peers can still act on it, instead of after <code>TaskWait</code> when their turns are spent.</li><li>The parent&#39;s context cost is unchanged. Coordination that used to be impossible does not become parent context; it stays between the delegates.</li><li>Peer messages are visible in the transcript as ordinary delegate tool calls, so a user reviewing a fan-out can see what the delegates told each other without a new UI surface.</li><li>Four invariants callers must keep: <ul><li><strong>A peer tool must never be added to <code>toolCatalog</code>.</strong> That would give the parent <code>PeerSend</code> and, with it, a way to inject messages that bypasses <code>Task</code>&#39;s brief and the report boundary.</li><li><strong><code>from</code> must stay runtime-supplied.</strong> The moment a sender name becomes a tool parameter, attribution in the transcript becomes a model claim.</li><li><strong>A settled delegate must leave the mailbox.</strong> A stale participant makes <code>PeerSend</code> report success for a message nobody will ever read, and leaves waiters blocked on an agent that is gone.</li><li><strong><code>PeerWait</code>&#39;s ceiling must stay below the idle watchdog.</strong> A wait longer than 300 seconds of silence would let a delegate kill itself waiting, and the failure would read as a hung delegate rather than an unanswered question.</li></ul></li><li>A delegate can now spend turns on messages. The caps bound the worst case, and the prompt is explicit that coordination is not conversation, but a definition that opts in accepts that its delegate may spend a few turns coordinating.</li><li>Not addressed: cross-session messaging, messaging with the parent, nested delegation, and durable peer history. All four stay out.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Route peer messages through the parent:</strong> rejected. The parent is blocked in <code>TaskWait</code> while delegates run, so it cannot relay in time, and every note would land in the context delegation exists to protect. It also makes the parent&#39;s model responsible for correctly forwarding messages it has no interest in.</li><li><strong>Give delegates <code>TaskList</code>/<code>TaskWait</code> over their siblings:</strong> rejected. Those are lifecycle tools over the delegation registry. A delegate waiting on a sibling&#39;s <em>completion</em> rather than a message reintroduces deadlock (two delegates each waiting for the other) and hands a worker control over work it did not start.</li><li><strong>Shared scratch files as the channel:</strong> rejected. It works today and is exactly the failure mode worth avoiding: no delivery semantics, no bounds, no attribution in the transcript, and a polling loop in every delegate that wants to listen. It also puts coordination state in the workspace or scratch tree, where it outlives the session.</li><li><strong>Always-on peer tools for every definition:</strong> rejected. It would change the behaviour of all four builtins and every existing user definition at once, for a capability most delegations do not need. A <code>code-reviewer</code> reviewing one change has nobody to coordinate with, and the tool would only invite it to look.</li><li><strong>Interrupt-style delivery that injects a message into a peer&#39;s next request:</strong> rejected. A delegate mid-edit cannot act on an interrupt coherently, and an injected message would appear in a context the delegate did not ask to change. Pull delivery keeps the delegate in control of when it reads, at the cost of a note sometimes being read late — which is why the prompt frames every exchange as best effort.</li><li><strong>Broadcast only, with no directed messages:</strong> rejected. The common case is one delegate telling one specific peer that it owns a file. Broadcasting that to eight delegates spends eight contexts to inform one.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0138-subagent-peer-messaging.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0138SubagentPeerMessaging = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0138SubagentPeerMessaging as default
};
