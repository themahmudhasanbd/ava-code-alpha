import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0166: Parent-judged subagent lifetime","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0166-parent-judged-subagent-lifetime.md","filePath":"adr/0166-parent-judged-subagent-lifetime.md","lastUpdated":1788884981000}');
const _sfc_main = { name: "adr/0166-parent-judged-subagent-lifetime.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0166-parent-judged-subagent-lifetime" tabindex="-1">ADR 0166: Parent-judged subagent lifetime <a class="header-anchor" href="#adr-0166-parent-judged-subagent-lifetime" aria-label="Permalink to &quot;ADR 0166: Parent-judged subagent lifetime&quot;">​</a></h1><ul><li>Status: Accepted for implementation</li><li>Date: 2026-09-06</li><li>Deciders: PI-Desktop core</li><li>Related: D328, ADR 0089, ADR 0119, ADR 0129, issue discussion on GitHub #44 follow-on (parent closing kills unfinished delegates)</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>ADR 0089 made <code>Task</code> non-blocking and told the parent to <code>TaskWait</code> or <code>TaskStop</code> before ending a turn. The safety net was to abort leftover delegates on parent <code>agent_end</code>. ADR 0119 / ADR 0129 then armed a 300-second idle watchdog and a 6-hour duration cap.</p><p>That combination failed in practice: the parent cannot see the delegate&#39;s live work, <code>TaskWait</code> returns after at most 900 seconds, the model treats that as permission to finish, and <code>agent_end</code> kills the still-running delegate. Long compiles, tests, and audits died because the parent closed, not because the work was done.</p><p>Prompting the model to wait again does not fix this. Waiting is an event loop; models are not reliable event loops.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><strong>Do not time-kill a delegate.</strong> Idle and duration watchdogs are not armed. <code>idle-timeout</code> / <code>max-duration</code> still parse so existing documents load, but they have no effect. Explicit <code>maxTurns</code> remains a definition backstop. Concurrency stays capped at 10.</li><li><strong>Do not abort on parent idle.</strong> <code>agent_end</code> / <code>turn_end</code> while delegates are running are swallowed. The durable turn stays open. User Stop, <code>TaskStop</code>, runtime dispose, and a parent fatal error (D352 / ADR 0189) abort a delegate.</li><li><strong>Deliver reports when they finish.</strong> After the parent loop idles with running delegates, the runtime waits for them and prompts the parent with the joined reports (not shown as a user bubble). The parent then judges: integrate, start more work, or <code>TaskStop</code>.</li><li><strong>Give the parent a heartbeat, not the transcript.</strong> <code>TaskList</code> and a timed-out <code>TaskWait</code> include agent, status, elapsed seconds, turns, tool calls, and last tool name. Process rows stay out of the parent context.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A long delegate can outlive the parent&#39;s last tool call. The user still sees a live turn (Stop remains available) until the reports are delivered and the parent actually finishes.</li><li>The parent no longer has to poll correctly for the job to survive.</li><li>A looping delegate without <code>maxTurns</code> can run until the user Stops it.</li><li>ADR 0119 / ADR 0129 watchdog policy is withdrawn for killing. Their &quot;silence vs slowness&quot; analysis remains historical.</li></ul><h2 id="alternatives-rejected" tabindex="-1">Alternatives rejected <a class="header-anchor" href="#alternatives-rejected" aria-label="Permalink to &quot;Alternatives rejected&quot;">​</a></h2><ul><li><strong>Progress file the parent Reads (Claude Code).</strong> Puts live transcript into the parent context and requires the model to remember to poll.</li><li><strong>Keep abort-on-end and only raise TaskWait.</strong> The model still closes.</li><li><strong>Survive the turn with no auto-resume.</strong> The user has to send 继续.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0166-parent-judged-subagent-lifetime.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0166ParentJudgedSubagentLifetime = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0166ParentJudgedSubagentLifetime as default
};
