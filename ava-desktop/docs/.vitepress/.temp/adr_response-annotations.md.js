import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR response-annotations: Response Annotations as Prompt Attachments","description":"","frontmatter":{},"headers":[],"relativePath":"adr/response-annotations.md","filePath":"adr/response-annotations.md","lastUpdated":1789547461000}');
const _sfc_main = { name: "adr/response-annotations.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-response-annotations-response-annotations-as-prompt-attachments" tabindex="-1">ADR response-annotations: Response Annotations as Prompt Attachments <a class="header-anchor" href="#adr-response-annotations-response-annotations-as-prompt-attachments" aria-label="Permalink to &quot;ADR response-annotations: Response Annotations as Prompt Attachments&quot;">​</a></h1><blockquote><p>Superseded by ADR 0268.</p></blockquote><ul><li>Status: Superseded by ADR 0268</li><li>Date: 2026-09-11</li><li>Deciders: PI-Desktop runtime and desktop UI maintainers</li><li>Amends: D-LOCAL-message-quotes decision 3 (for assistant turns)</li><li>Preserves: D209, D301, D097, D128</li><li>Related: <a href="./../spec/04-ux/08-component-spec">04-ux/08-component-spec.md</a> · <a href="./../spec/04-ux/09-interaction-patterns">04-ux/09-interaction-patterns.md</a> · <a href="./../spec/03-runtime/11-provider-model-system">03-runtime/11-provider-model-system.md</a> · ADR message-quotes-and-side-chats · E2E-CHAT-annotation-attachments, E2E-CHAT-annotation-session-state · Amendment: 2026-09-12 (comment editor)</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>ADR message-quotes-and-side-chats (D-LOCAL-message-quotes) made quoting a <strong>draft edit</strong>: the excerpt becomes <code>&gt; </code>-prefixed Markdown text inside the composer, attributed with <code>chat.quoteSource</code>. That is simple and visible, but it copies the model&#39;s words into the draft the user is writing, and it makes &quot;refer to this pass&quot; indistinguishable from &quot;write this text myself&quot;. Reusing several passes mixes excerpts and instructions in one editable buffer, and a long answer can be quoted only as one block.</p><p>The reference implementation (ChatGPT desktop app) solves the same problem with <strong>annotations</strong>. Selecting text in a response and choosing &quot;Add to chat&quot; attaches a numbered annotation to that response: the annotated pass gains an inline numbered marker, the composer shows one annotation attachment, and the next prompt carries the excerpts in a structured block:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span># Response annotations:</span></span>
<span class="line"><span>Each item contains text selected from an earlier Codex response and may include a user comment. …</span></span>
<span class="line"><span>&lt;response-annotations&gt;</span></span>
<span class="line"><span>[{&quot;text&quot;:&quot;…&quot;,&quot;annotation&quot;:&quot;…&quot;,&quot;source&quot;:{…}}]</span></span>
<span class="line"><span>&lt;/response-annotations&gt;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>## My request:</span></span>
<span class="line"><span>…the user&#39;s own prompt…</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><p>The user&#39;s own words stay the user&#39;s own words; the excerpts travel with the prompt as data, numbered so the model can address <code>Annotation 1</code>, <code>Annotation 2</code>, and so on.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>Selecting text inside an <strong>assistant turn</strong> and activating the selection overlay&#39;s <strong>Add to chat</strong> creates one annotation instead of editing the draft. An annotation carries an id, the anchored turn (<code>messageId</code>), the excerpt (Markdown, capped at 2000 characters with a trailing ellipsis), an empty <code>annotation</code> comment field, and a creation time.</li><li>Annotations are <strong>renderer-owned session state</strong>, keyed by session id, in capture order. Array order is the numbering: the first annotation is <code>Annotation 1</code>. Re-annotating an excerpt that is already attached is a no-op, so no excerpt is sent twice under two numbers. Annotations are not persisted, not sent to the host on their own, and do not survive relaunch.</li><li>Rendering: <ul><li>The answer body is <strong>never decorated by this app</strong>. An annotation shows up in the model&#39;s answer only where the model cites it, exactly as in the reference: a <code>:codex-annotation{index=&quot;N&quot;}</code> directive in the answer&#39;s source becomes a small accent-colored numbered reference (a remark pass turns the token into a marker element; the sanitizer keeps the marker scheme on its href), whose tooltip is the annotated excerpt and any comment. A marker is a reference, not text the user picked: it takes no part in a selection, a quote, or a copy, and it never breaks the line it sits in.</li><li>The composer shows one annotation attachment chip above the input with the count (<code>chat.annotationChip</code>) whose tooltip lists <code>N. excerpt</code> per annotation, plus one control that drops them all (<code>chat.clearAnnotations</code>). The chip is not a draft chip: it never enters the editable text, so D209&#39;s smart Stop and D301&#39;s per-session draft retention are untouched. It is also the only place the user&#39;s own annotations are visible before the model answers.</li></ul></li><li>Sending a prompt while annotations exist composes the prompt as the reference does: the <code># Response annotations:</code> heading, the instruction sentence, the <code>&lt;response-annotations&gt;</code> block with <code>[{&quot;text&quot;, &quot;annotation&quot;, &quot;source&quot;: {&quot;messageId&quot;}}]</code> in numbering order, then <code>## My request:</code> and the user&#39;s text. The send consumes the annotations. The visible draft, the optimistic row, the sidebar title, and the composer&#39;s edit seed never contain the block; a stored prompt that carries one is displayed through <code>requestTextWithoutAnnotations</code>, which reduces it back to the request.</li><li>The instruction sentence is the reference&#39;s own text, including its requirement that the model cite every annotation it addresses with <code>:codex-annotation{index=&quot;N&quot;}</code>; that directive is what renders as the numbered reference in decision 3. An app that decorates the answer itself would put markers where the model never claimed to answer them, which is also what made the first implementation unreadable.</li><li>Routing stays explicit per surface: the overlay&#39;s <strong>Add to chat</strong> annotates an assistant turn and keeps D-LOCAL-message-quotes&#39;s draft quote for any other row; the assistant turn&#39;s action row annotates (its selection when there is one, the whole answer otherwise); a user message&#39;s action row and the side chat&#39;s <strong>Add to main chat</strong> keep quoting into the draft.</li><li>i18n keys introduced: <code>chat.annotate</code>, <code>chat.annotationMarker</code>, <code>chat.annotationChip</code>, <code>chat.clearAnnotations</code>. The overlay&#39;s other actions (<code>chat.addToChat</code>, <code>chat.askInSideChat</code>, <code>chat.copy</code>) are unchanged.</li><li>Boundaries: no host protocol bump (v11), no storage schema change (v15), no new IPC channel, and no new permission. Annotations are a renderer-side projection of the reference contract; the prompt that reaches the model is ordinary prompt text.</li></ol><h2 id="preserved-behavior" tabindex="-1">Preserved behavior <a class="header-anchor" href="#preserved-behavior" aria-label="Permalink to &quot;Preserved behavior&quot;">​</a></h2><ul><li>D-LOCAL-message-quotes&#39;s draft quote is preserved where it is the right model: quoting a <strong>user</strong> message, and moving a side chat&#39;s newest answer into the main composer. Its contract (<code>&gt; </code> blockquote, <code>chat.quoteSource</code> attribution, 2000-character cap, append-to-draft, focus, no send) is unchanged.</li><li>D209&#39;s smart Stop restores the pre-send draft, and D301&#39;s per-session draft slots are unchanged: annotations are session state beside the draft, so neither path has to learn about them.</li><li>The selection overlay&#39;s geometry, bounds, follow-on-scroll behavior, actions, and read-only-projection exclusion are unchanged (ADR message-quotes-and-side-chats amendment).</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>The model receives each annotated pass as a numbered data item, so &quot;address annotation 2&quot; is unambiguous, and the user&#39;s prompt stays exactly what the user typed.</li><li>A user can annotate several passes of one answer, from any turn in the session, and then write one instruction that covers them; the composer shows one attachment instead of a growing block of quoted text.</li><li>Annotations are lost on relaunch and after the send that carries them; a user who wants a durable record can still quote into the draft or copy the pass.</li><li>A stored prompt is longer than what the user typed (the block is part of it), which is why every display surface reduces it back to the request first.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Keep writing excerpts into the draft (D-LOCAL-message-quotes):</strong> rejected for assistant turns. It mixes the model&#39;s words into the user&#39;s own prompt text, and several references become one long editable block with no numbering the model can address.</li><li><strong>Annotate through the host (new RPC or message field):</strong> rejected. The prompt block is ordinary prompt text, so no protocol, schema, or permission change is needed, and the annotations only have to live as long as the send.</li><li><strong>Mark the annotated pass inside the answer:</strong> rejected. It would decorate text the model has not answered yet, it needs the annotated range to survive every re-render (React owns the transcript tree), and a marker placed by excerpt lookup lands mid-word whenever the selection ended mid-word — the first implementation shipped exactly that damage. The model&#39;s own citation is the reference&#39;s marker.</li><li><strong>Store the annotations with the message for a persistent list:</strong> rejected for now. It would need a storage-schema change and a durable annotation surface; the send-scoped attachment matches the reference behavior.</li></ul><h2 id="amendment-2026-09-12-—-the-attachment-opens-a-comment-editor" tabindex="-1">Amendment (2026-09-12) — the attachment opens a comment editor <a class="header-anchor" href="#amendment-2026-09-12-—-the-attachment-opens-a-comment-editor" aria-label="Permalink to &quot;Amendment (2026-09-12) — the attachment opens a comment editor&quot;">​</a></h2><p>The reference attaches an excerpt through a comment entry, not a silent action: selecting text in a response and choosing Comment opens a compact editor over the selection with the quote in place. This amendment replaces decision 1&#39;s &quot;attaches with an empty <code>annotation</code> field&quot; reading and decision 2&#39;s silent duplicate no-op with that editor; the annotation shape, the prompt block, and every boundary of decision 8 are unchanged.</p><ul><li><strong>Add to chat</strong> on an assistant turn, and the turn&#39;s annotate action, open the comment editor instead of attaching the excerpt immediately. The editor shows the excerpt snapshot — the Markdown serialized when the selection was taken, before focus moves into the editor and collapses the selection — above a multiline, optional comment. <strong>Save</strong> attaches the annotation (or updates the one being edited) with <code>annotation</code> set to the comment with surrounding whitespace trimmed; <strong>Cancel</strong>, <strong>Escape</strong> outside IME composition, and a press starting on the backdrop discard it. Dragging selected text onto the backdrop does not dismiss it. Escape is consumed before application shortcuts; closing restores focus to the trigger or the rich composer if the trigger is gone. A save with an empty comment still attaches, so Add to chat keeps working as a plain reference. Opening or saving the editor sends nothing: no prompt, no session, no transcript row.</li><li>Re-annotating an excerpt that is already attached reopens <strong>that annotation&#39;s</strong> editor with its stored comment instead of the old silent no-op, so decision 2&#39;s one-annotation-per-excerpt rule holds while the comment stays editable. Editing keeps the annotation&#39;s id, position, and excerpt: only <code>annotation</code> changes.</li><li>The composer&#39;s attachment (decision 3) is now a disclosure. Its count chip is a button that opens the session&#39;s annotation list: each item shows its number, its excerpt, and its comment, with one control to edit it (the same editor, seeded with the stored comment) and one to remove just that item; the existing control still drops all of them (<code>chat.clearAnnotations</code>). The chip&#39;s tooltip still lists <code>N. excerpt</code>, and the list is not draft text.</li><li>The editor belongs to the session it was opened in: a session switch closes it rather than carrying a half-written comment into another conversation, and a save whose annotation was already sent or removed changes nothing and never recreates it.</li><li>i18n keys added: <code>chat.annotationCommentTitle</code>, <code>chat.annotationCommentPlaceholder</code>, <code>chat.annotationEdit</code>, <code>chat.annotationRemove</code>, <code>chat.annotationReview</code>. The store actions are <code>openResponseAnnotationEditor</code>, <code>saveResponseAnnotationEditor</code>, and <code>closeResponseAnnotationEditor</code>, replacing <code>addResponseAnnotation</code>.</li><li>Boundaries of decision 8 stand: no host protocol bump, no storage schema change, no new IPC channel, no new permission. The comment is ordinary <code>annotation</code> field content in the existing prompt block.</li></ul><h2 id="upstream-integration-amendment-2026-09-14" tabindex="-1">Upstream integration amendment (2026-09-14) <a class="header-anchor" href="#upstream-integration-amendment-2026-09-14" aria-label="Permalink to &quot;Upstream integration amendment (2026-09-14)&quot;">​</a></h2><p>The renderer keeps upstream Composer submit/steering hooks and store slices. Ordinary send/queue snapshots this session&#39;s annotation objects and consumes only unchanged submitted objects after host acknowledgement. Rejection retains them; concurrent additions and comment edits survive. The internal <code>enqueuePrompt</code> action returns <code>Promise&lt;boolean&gt;</code> so the existing composer rejection restoration also covers queue failures and unexpected pre-host exceptions without replacing newer input. A per-session in-flight submission guard rejects duplicate submissions until acknowledgement settles, including the draft-to-created-session handoff. It does not block other sessions or steering and always releases on exit. No host API changes.</p><p>Upstream Alt+Enter and the steer button remain text-only: annotations wait for the next ordinary send. Annotation-only steering does nothing. Shift+Enter remains newline, and the comment editor&#39;s Enter/IME semantics remain independent. Queue previews and edit seeds hide the recognized generated block without changing stored/wire content. Editing does not rebuild previously consumed attachments; ordinary retry reuses the stored prompt. See E2E-CHAT-annotation-ack-and-steering.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/response-annotations.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const responseAnnotations = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  responseAnnotations as default
};
