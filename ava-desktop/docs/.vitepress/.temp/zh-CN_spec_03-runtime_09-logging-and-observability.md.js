import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"09. 日志记录和可观测性","description":"","frontmatter":{},"headers":[],"relativePath":"zh-CN/spec/03-runtime/09-logging-and-observability.md","filePath":"zh-CN/spec/03-runtime/09-logging-and-observability.md","lastUpdated":1789892142000}');
const _sfc_main = { name: "zh-CN/spec/03-runtime/09-logging-and-observability.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="_09-日志记录和可观测性" tabindex="-1">09. 日志记录和可观测性 <a class="header-anchor" href="#_09-日志记录和可观测性" aria-label="Permalink to &quot;09. 日志记录和可观测性&quot;">​</a></h1><blockquote><p><strong>翻译说明：</strong> 本页是与 <a href="/spec/03-runtime/09-logging-and-observability">英文源规格</a> 一一对应的机器辅助翻译。代码、协议字段和标识符保持原文；如翻译与英文源事实有歧义，以英文版本为准。</p></blockquote><h2 id="_1-目标" tabindex="-1">1. 目标 <a class="header-anchor" href="#_1-目标" aria-label="Permalink to &quot;1. 目标&quot;">​</a></h2><ol><li>快速诊断故障。</li><li>审计敏感的工具和插件操作。</li><li>避免泄露秘密。</li><li>保持 MVP 本地优先，并让正常运行保持安静。</li></ol><h2 id="_2-日志级别" tabindex="-1">2. 日志级别 <a class="header-anchor" href="#_2-日志级别" aria-label="Permalink to &quot;2. 日志级别&quot;">​</a></h2><ul><li><code>debug</code></li><li><code>info</code></li><li><code>warn</code></li><li><code>error</code></li></ul><p>默认运行时级别：</p><ul><li>开发：<code>debug</code></li><li>发布：<code>info</code></li></ul><h2 id="_3-通道" tabindex="-1">3. 通道 <a class="header-anchor" href="#_3-通道" aria-label="Permalink to &quot;3. 通道&quot;">​</a></h2><table tabindex="0"><thead><tr><th>通道</th><th>内容</th><th>位置</th></tr></thead><tbody><tr><td>app</td><td>启动、IPC、窗口、进程监控</td><td><code>~/.pi-desktop/logs/app/&lt;category&gt;.log</code></td></tr><tr><td>host</td><td>Rust host-core 事件（stderr 捕获）</td><td><code>~/.pi-desktop/logs/host/&lt;category&gt;.log</code></td></tr><tr><td>agent</td><td>pi sidecar 事件（stderr 捕获）</td><td><code>~/.pi-desktop/logs/agent/&lt;category&gt;.log</code></td></tr><tr><td>audit</td><td>敏感的权限、工具和插件操作</td><td>host-core SQLite <code>audit_log</code> 表</td></tr><tr><td>plugin</td><td>每个插件的日志</td><td><code>~/.pi-desktop/plugins/logs/&lt;id&gt;.log</code></td></tr></tbody></table><p>表中的 <code>~/.pi-desktop</code> 路径属于正式打包版。开发构建把同一棵目录树写在 <code>~/.pi-desktop-dev</code> 下，<code>PI_DESKTOP_DATA_DIR</code> 会整体替换任一默认根目录（D599）。 <code>app</code>、<code>host</code> 和 <code>agent</code> 是由 Electron 主进程 <code>Logger</code> （<code>apps/desktop/electron/main/logger.ts</code>）写入的 NDJSON 文件。host 和 agent 的 stderr 行会被包装成对应通道的记录。audit 通道由 host-core 独占写入 SQLite，因此可以独立于轮换的诊断文件进行查询。</p><h3 id="_3a-类别路由" tabindex="-1">3a. 类别路由 <a class="header-anchor" href="#_3a-类别路由" aria-label="Permalink to &quot;3a. 类别路由&quot;">​</a></h3><p>三个进程通道是目录，而不是聚合文件。主进程调用点明确选择类别。 host 和 agent stderr 使用标记进行分类；无法分类的子进程输出使用 <code>runtime</code>。</p><p>应用程序类别包括：</p><ul><li><p><code>lifecycle</code> — 启动、关闭和应用监控</p></li><li><p><code>session</code> — 提示、回合、会话生命周期和压缩</p></li><li><p><code>tool</code> — 工具执行结果和中断</p></li><li><p><code>permission</code> — 权限请求和决定</p></li><li><p><code>plugin</code> — 插件加载、服务和插件工具执行</p></li><li><p><code>provider</code> — provider/model 发现、重试和缓存失败</p></li><li><p><code>persistence</code> — 成绩单和发件箱持久化失败</p></li><li><p><code>updater</code> — 更新器诊断和错误</p></li><li><p><code>diagnostics</code> — 阻止导航、菜单、模板以及对外请求的诊断。技能市场的两个通道会为每个没有产出结果的源或文档各记录一条 <code>skillMarket.sourceFailed</code> / <code>skillMarket.documentFailed</code>：<code>data</code> 里带 <code>source</code>、<code>host</code>、<code>kind</code>、<code>address</code>,以及被守卫拒绝时的 <code>reason</code>、<code>addressKind</code> 与 <code>route</code>。<code>kind</code> 在守卫判定的是目标自身地址时为 <code>policy</code>,判定的是本地代理伪造的 fake-IP 占位地址时为 <code>fake-ip</code>,本地解析没有返回答案时为 <code>unresolved</code>,其余为 <code>network</code>；<code>code</code> 前两者为 <code>NETWORK_POLICY_BLOCKED</code>,第三者为 <code>NETWORK_RESOLVE_FAILED</code>,因此一行日志即可区分「目标地址不是公网」「代理用了 fake-IP」与「解析器没有应答」。<code>reason</code> 记录守卫自己的分支（<code>url-syntax</code>、<code>resolve-failed</code>、<code>non-public-address</code>、<code>redirect-limit</code>）,<code>addressKind</code> 记录被拒地址的类别（TUN fake-IP 为 <code>benchmark</code>,RFC1918 为 <code>private</code>）,<code>route</code> 记录该地址是在哪条线路上被判定的（<code>proxied</code>、<code>direct</code>,或传输层读不出线路时的 <code>unknown</code>）,因此「直连线路上的 fake-IP 拒绝」与「读不出线路的拒绝」可以区分（ADR 0272）。记录会保留主机名、被解析到的地址、该地址的类别与该线路 —— 但绝不包含 URL、其路径、查询串或凭据 —— 因为目录源 URL 由用户提供,而被拒绝的主机、地址及其类别正是全部诊断价值所在（issue #419）。Chromium 进程崩溃后，下一次启动会写一条 <code>crashDumpsFound</code> 记录（任一新转储属于 browser/main 进程则为 <code>error</code>，否则为 <code>warn</code>），<code>data</code> 带 <code>count</code>、<code>total</code>、<code>byProcessType</code>、<code>directory</code> 与 <code>newestMtimeMs</code>。已恢复的 renderer 崩溃因此不会被写成「上次运行已死」。扫描失败是一条 <code>crashDumpReportFailed</code> 警告，永不挡住第一扇窗口（D602）。</p></li><li><p><code>runtime</code> — host/sidecar 生命周期、未分类的子进程输出，以及主进程 <code>uncaughtException</code> / <code>unhandledRejection</code> 记录</p></li></ul><p>不存在独立的 <code>timing</code> 类别。较早运行生成的 timing 文件保持不变， 但当前代码不会创建或追加这些文件。旧的 <code>app.log</code>、<code>host.log</code> 和 <code>agent.log</code> 文件同样保持不变。</p><h2 id="_4-必填字段" tabindex="-1">4. 必填字段 <a class="header-anchor" href="#_4-必填字段" aria-label="Permalink to &quot;4. 必填字段&quot;">​</a></h2><p>每个结构化日志行应包括：</p><div class="language-ts vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">type</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> LogRecord</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  ts</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  level</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;debug&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;info&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;warn&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;error&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  channel</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  category</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  event</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">              // 稳定的点号分隔机器可读名称</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  message</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  traceId</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  requestId</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  sessionId</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  turnId</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  toolCallId</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  parentToolCallId</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  agentName</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  pluginId</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  executionId</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  code</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  data</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> unknown</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br><span class="line-number">19</span><br></div></div><p>格式：NDJSON 文件。</p><p><code>event</code> 是稳定的查询键，<code>message</code> 是简短的人类可读摘要。关联字段位于 顶层，因此无需解析自由文本即可串联工具失败、权限请求和父子 agent。 <code>data</code> 仅用于诊断元数据，不是成绩单或命令输出；它会脱敏、限制深度和集合 大小，并且每条记录最多 8 KiB。</p><h2 id="_5-必须记录的内容" tabindex="-1">5. 必须记录的内容 <a class="header-anchor" href="#_5-必须记录的内容" aria-label="Permalink to &quot;5. 必须记录的内容&quot;">​</a></h2><h3 id="始终记录" tabindex="-1">始终记录 <a class="header-anchor" href="#始终记录" aria-label="Permalink to &quot;始终记录&quot;">​</a></h3><ul><li>应用启动和关闭；</li><li>host/agent 生成、握手和意外退出；</li><li>会话 create/delete；</li><li>提示 accepted/aborted；</li><li>工具完成/失败/中断以及权限 request/decision/timeout；</li><li>Plan 工件创建、approval、expiry、拒绝、执行转换和启动中断；</li><li>shell 身份、超时、中止和进程树关闭；</li><li>插件 enable/disable/load/error；</li><li>工具准入拒绝、队列或资源耗尽，以及更新器错误。</li></ul><p>这些记录在可用时应包含对应的会话、回合、工具调用、插件或稳定错误码。 正常工具调用在 <code>tool_end</code> 后只产生一条完成或失败记录；sidecar 意外退出时， 为每个仍在运行的工具产生一条中断记录。sidecar 的 <code>tool_start</code>/<code>tool_end</code> 协议事件和成绩单持久化保持不变。正常成功操作不应输出逐阶段或逐请求的延迟记录。</p><h3 id="绝不记录" tabindex="-1">绝不记录 <a class="header-anchor" href="#绝不记录" aria-label="Permalink to &quot;绝不记录&quot;">​</a></h3><ul><li>API 密钥或原始秘密；</li><li>完整的安全存储有效负载；</li><li>审计记录中不必要的大型读取完整文件内容。</li></ul><h2 id="_6-脱敏规则" tabindex="-1">6. 脱敏规则 <a class="header-anchor" href="#_6-脱敏规则" aria-label="Permalink to &quot;6. 脱敏规则&quot;">​</a></h2><ol><li>与 token、secret、password、API key、authorization、cookie、credential、 private key 或 client secret 匹配的键名做脱敏处理。</li><li>字符串中的 Bearer/Basic 凭据、URL 用户信息和常见 provider token 格式也做 脱敏处理。</li><li>home、应用数据和日志目录前缀替换为占位符；诊断记录不保留原始本机路径。</li><li>任意字符串有长度上限；结构化数据限制深度和集合大小，每条记录的 <code>data</code> 最多 8 KiB；host-core 审计 payload 整形后也最多 8 KiB。</li><li>工具参数和结果不会整体复制到常规日志。工具结果只保留结果、错误码、时长、 字段名、内容块数量以及 stdout/stderr 大小等安全元数据；审计记录中的长命令 输出应计数或截断。</li><li>子进程 stderr 以稳定的 <code>child.process.stderr</code> 事件写入有界、去 ANSI 的 <code>data.output</code>，而不是放入记录消息。</li></ol><h2 id="_7-追踪关联" tabindex="-1">7. 追踪关联 <a class="header-anchor" href="#_7-追踪关联" aria-label="Permalink to &quot;7. 追踪关联&quot;">​</a></h2><p>尽可能为每个用户可见的操作使用一个 <code>traceId</code>：</p><ul><li>提示 → <code>turnId</code>；</li><li>工具调用 → <code>toolCallId</code>；</li><li>权限流 → <code>toolCallId</code> / <code>requestId</code>。</li></ul><p>Renderer、Electron、host 和 agent 应传播这些标识符。</p><h2 id="_7a-功能性时长元数据" tabindex="-1">7a. 功能性时长元数据 <a class="header-anchor" href="#_7a-功能性时长元数据" aria-label="Permalink to &quot;7a. 功能性时长元数据&quot;">​</a></h2><p>应用仍会保留产品功能所需的有界时长元数据： <code>ToolsExecuteResult.duration_ms</code>、成绩单中的 <code>toolDurationMs</code> 和 <code>responseDurationMs</code>、委托任务的开始/完成时间戳，以及有界的 provider 诊断信息。这些值用于成绩单、上下文检查器、吞吐量展示和审计记录； 它们不会创建 timing 日志行。</p><p>host-core 的 audit 行可以保留既有的权限和执行时长字段用于取证检查。 它们是结构化审计数据，不是独立的 <code>timing.log</code> 流。</p><h2 id="_8-面向用户的诊断" tabindex="-1">8. 面向用户的诊断 <a class="header-anchor" href="#_8-面向用户的诊断" aria-label="Permalink to &quot;8. 面向用户的诊断&quot;">​</a></h2><p>MVP 提供：</p><ol><li>带稳定代码的应用内错误文本；</li><li>“打开日志文件夹”命令；</li><li>可选复制错误详细信息（代码和 <code>traceId</code>）。</li></ol><p>MVP 不包含远程遥测管道或云崩溃分析。</p><h2 id="_9-保留" tabindex="-1">9. 保留 <a class="header-anchor" href="#_9-保留" aria-label="Permalink to &quot;9. 保留&quot;">​</a></h2><ul><li>app/host/agent 类别日志：每个类别文件达到 5 MB 后轮换，并在旁边保留两个 轮换文件（<code>&lt;category&gt;.1.log</code>、<code>&lt;category&gt;.2.log</code>）；</li><li>audit 日志（SQLite）：与数据库一起保留，并按 host 保留策略清理；</li><li><code>&lt;data_dir&gt;/crash-dumps</code> 中的 Crashpad minidump 不会被 logger 轮换，直到 用户删除为止；</li><li>轮换和日志写入失败绝不能让调用者失败。</li></ul><p>会话成绩单属于用户数据，不会因日志轮换而删除。</p><h2 id="_10-验收" tabindex="-1">10. 验收 <a class="header-anchor" href="#_10-验收" aria-label="Permalink to &quot;10. 验收&quot;">​</a></h2><ol><li>失败和中断的工具调用可以通过 <code>toolCallId</code> 跨关键日志追踪，正常执行每次只 有一条结果记录。</li><li>正常流程中秘密永远不会出现在日志文件中。</li><li>可从应用或命令面板打开日志文件夹。</li><li>boot、host、sidecar、plugin、updater 和 renderer 路径只输出生命周期、 状态变更、错误或安全相关记录；正常运行不会创建 timing 类别文件。</li><li>当 stdout 是断开的管道时，日志或控制台镜像绝不能让主进程崩溃。</li><li>主机重启、权限失败、shell 超时和进程中止仍可通过稳定的生命周期记录和 错误码诊断。</li></ol></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("zh-CN/spec/03-runtime/09-logging-and-observability.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _09LoggingAndObservability = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _09LoggingAndObservability as default
};
