import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0140: Fold the Three Peer Tools Into One Peer Tool","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0140-peer-tool-consolidation.md","filePath":"adr/0140-peer-tool-consolidation.md","lastUpdated":1788336030000}');
const _sfc_main = { name: "adr/0140-peer-tool-consolidation.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0140-fold-the-three-peer-tools-into-one-peer-tool" tabindex="-1">ADR 0140: Fold the Three Peer Tools Into One <code>Peer</code> Tool <a class="header-anchor" href="#adr-0140-fold-the-three-peer-tools-into-one-peer-tool" aria-label="Permalink to &quot;ADR 0140: Fold the Three Peer Tools Into One \`Peer\` Tool&quot;">​</a></h1><ul><li>Status: Superseded by ADR 0147</li><li>Date: 2026-08-31</li><li>Deciders: PI-Desktop core</li><li>Related: D277, ADR 0138, ADR 0062, ADR 0089, ADR 0147, <code>03-runtime/02-agent-runtime.md</code> §5f, E2E-165, E2E-165b</li><li>Amends: ADR 0138 clauses 2 and 3. The mailbox semantics, the caps, and every boundary ADR 0138 established are unchanged; this decision only changes the <em>tool surface</em> a delegate declares and calls.</li></ul><blockquote><p>Superseded by ADR 0147. The single <code>Peer</code> tool this ADR settled on is replaced by the <code>A2A</code> tool (actions <code>discover | send | get | wait | cancel</code>) against a host-core A2A broker. Retained because the counting argument for one tool over three carries into the A2A tool surface, and because <code>SUBAGENT_A2A_TOOLS = [&quot;A2A&quot;]</code> is the direct successor of the <code>SUBAGENT_PEER_TOOLS = [&quot;Peer&quot;]</code> mapping this ADR defined.</p></blockquote><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>ADR 0138 introduced peer messaging as three delegate-only tools — <code>PeerSend</code>, <code>PeerInbox</code> and <code>PeerWait</code> — declarable in a definition&#39;s <code>tools:</code> list. A delegate that opts in writes <code>tools: [..., &quot;PeerSend&quot;, &quot;PeerInbox&quot;, &quot;PeerWait&quot;]</code> and its spawner materialises three separate tools.</p><p>That split has a counting problem. Peer messaging is one capability: it carries bounded text between running delegates. Listing three machine tool names for it inflates a definition&#39;s declared tool count, forces a roundtable brief to spell out three names at every use site, and makes the parent&#39;s <code>Task</code> tool catalog read as if peer messaging were three unrelated capabilities. The operations are also mutually exclusive per call — a delegate either sends, or reads, or waits — so they never compose in one call, which is exactly the shape an <code>action</code> parameter is for.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Fold the three tools into <strong>one <code>Peer</code> tool</strong> selected by a required <code>action</code> parameter:</p><ul><li><code>Peer(action=&quot;send&quot;, to?, text, topic?, inReplyTo?)</code> — the former <code>PeerSend</code>: deliver a note to one running peer, or to all of them when <code>to</code> is omitted. A broadcast costs one send.</li><li><code>Peer(action=&quot;inbox&quot;, from?)</code> — the former <code>PeerInbox</code>: drain queued messages and list running peers; never blocks.</li><li><code>Peer(action=&quot;wait&quot;, timeoutSeconds?, from?)</code> — the former <code>PeerWait</code>: block until a message arrives, the timeout expires, the last peer leaves, or the run aborts.</li></ul><p>Everything else in ADR 0138 stands exactly as decided:</p><ul><li><code>SUBAGENT_PEER_TOOLS</code> is now <code>[&quot;Peer&quot;]</code>; <code>isSubagentPeerTool</code> and <code>subagentUsesPeerMessaging</code> keep their meaning (declared peer tool → peer messaging is on).</li><li><code>action</code> is the only required parameter (<code>Type.Union</code> of the <code>PEER_ACTIONS</code> literals); all operation-specific parameters are optional and validated in the dispatch body. An omitted or unknown <code>action</code> falls back to <code>inbox</code>, the read-only, never-blocking operation — the safe default.</li><li>The tool is still built per delegate at spawn time, still closes over the runtime-supplied peer id, and is still <strong>absent from <code>toolCatalog</code></strong>, so the parent cannot reach it.</li><li>The mailbox (session-scoped <code>SubagentMailbox</code>, join/leave/send/drain/wait, caps of 2,000 chars, 80-char topics, 64-message inboxes, 60 sends per run, 120-second wait ceiling) is untouched. The operation names <code>send</code>, <code>inbox</code>, <code>wait</code> map one-to-one onto the mailbox&#39;s <code>send</code>, <code>drain</code> and <code>waitForMessages</code> methods.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A peer-enabled definition now declares one tool: <code>tools: [..., &quot;Peer&quot;]</code>, and guidance can describe the capability as a unit.</li><li>The <code>Peer</code> tool dispatches inside one <code>execute</code>, so the three operations share one parameter surface and one <code>details</code> shape that records which <code>action</code> ran; the transcript still records the call as a single peer tool call attributed by <code>parentToolCallId</code> and <code>agentName</code>.</li><li>The roundtable example, the agent-runtime guidance block, and the delegation roundtable bullet all reference <code>Peer(action=...)</code> instead of three names.</li><li>Four invariants from ADR 0138 remain hard: the <code>Peer</code> tool must never enter <code>toolCatalog</code>; the sender name must stay runtime-supplied; a settled delegate must leave the mailbox; and the wait ceiling must stay below the 300-second idle watchdog.</li><li>Not addressed: nothing new. Cross-session messaging, messaging with the parent, nested delegation, and durable peer history stay out, exactly as in ADR 0138.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Keep three tools:</strong> rejected because the request that prompted this decision stated the contract should read as one tool; three names also inflate every counting surface and every brief.</li><li><strong>A <code>Peer</code> tool with three boolean operation flags:</strong> rejected — exactly one operation runs per call, so mutually exclusive booleans invite invalid calls that an <code>action</code> enum rules out by construction.</li><li><strong>Keep operation names as separate tools but hide them behind a single declared name:</strong> rejected — the half-measure keeps the extra transcript and catalog surface while adding a confusing declaration↔runtime mapping.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0140-peer-tool-consolidation.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0140PeerToolConsolidation = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0140PeerToolConsolidation as default
};
