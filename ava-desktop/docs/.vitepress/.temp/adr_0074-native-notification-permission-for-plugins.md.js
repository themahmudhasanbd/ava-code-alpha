import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0074: Native notification permission for plugins","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0074-native-notification-permission-for-plugins.md","filePath":"adr/0074-native-notification-permission-for-plugins.md","lastUpdated":1786507083000}');
const _sfc_main = { name: "adr/0074-native-notification-permission-for-plugins.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0074-native-notification-permission-for-plugins" tabindex="-1">ADR 0074: Native notification permission for plugins <a class="header-anchor" href="#adr-0074-native-notification-permission-for-plugins" aria-label="Permalink to &quot;ADR 0074: Native notification permission for plugins&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-08-12</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The plugin <code>notify</code> capability currently exposes only an in-app Toast. PI-Desktop already uses Electron&#39;s native notification API for terminal task outcomes, but that surface is intentionally owned by the application and backed by the durable task notification inbox. Plugins need a way to ask the operating system for notification access and deliver a native notification without gaining access to Electron objects or the task inbox.</p><p>Electron does not expose a cross-platform, read-only notification permission query or a separate permission-request method for its main-process <code>Notification</code> class. Native delivery reports success or failure through the notification lifecycle, and operating-system policies may suppress a banner without reporting a durable state.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Keep <code>pi.ui.notify</code> as the backwards-compatible in-app Toast. Add three permission-gated APIs under <code>pi.ui</code>:</p><ul><li><code>getNotificationPermission()</code> returns <code>granted</code>, <code>denied</code>, <code>unknown</code>, or <code>unsupported</code>.</li><li><code>requestNotificationPermission()</code> performs a short native notification probe and returns the best-effort result.</li><li><code>showNativeNotification({ title, body? })</code> sends a native notification and returns <code>{ shown, permission }</code>.</li></ul><p>All three APIs use the existing low-risk manifest <code>notify</code> permission. The Electron main process owns the native objects, limits title/body lengths, and keeps plugin notifications separate from the durable task inbox. Native plugin notifications do not create inbox rows or session activation events on click.</p><p>The permission result is deliberately best-effort. Before a native result is observed, or when the operating system does not report one, the API returns <code>unknown</code>; unsupported platforms return <code>unsupported</code>. This avoids pretending that Electron can reliably reflect a platform setting it cannot query.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Plugins can provide OS-level reminders and status updates while remaining behind the existing plugin permission review.</li><li>Existing plugins keep their Toast behavior and source compatibility.</li><li>A plugin must handle <code>unknown</code>, <code>denied</code>, and <code>unsupported</code> without assuming that a native banner was delivered.</li><li>The task notification inbox remains application-owned and cannot be used as a plugin persistence or navigation channel.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li>Replacing <code>pi.ui.notify</code> with native delivery: rejected because it silently changes existing plugin behavior and conflates Toasts with OS notifications.</li><li>Adding a platform-specific permission package: rejected for this API revision; it would add platform-specific dependencies without a common Electron permission contract.</li><li>Allowing plugins to construct Electron <code>Notification</code> objects: rejected by the plugin isolation boundary.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0074-native-notification-permission-for-plugins.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0074NativeNotificationPermissionForPlugins = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0074NativeNotificationPermissionForPlugins as default
};
