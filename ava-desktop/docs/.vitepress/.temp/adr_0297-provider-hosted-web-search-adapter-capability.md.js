import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0297: Provider-hosted web search as an adapter capability","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0297-provider-hosted-web-search-adapter-capability.md","filePath":"adr/0297-provider-hosted-web-search-adapter-capability.md","lastUpdated":1790015926000}');
const _sfc_main = { name: "adr/0297-provider-hosted-web-search-adapter-capability.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0297-provider-hosted-web-search-as-an-adapter-capability" tabindex="-1">ADR 0297: Provider-hosted web search as an adapter capability <a class="header-anchor" href="#adr-0297-provider-hosted-web-search-as-an-adapter-capability" aria-label="Permalink to &quot;ADR 0297: Provider-hosted web search as an adapter capability&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-19</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Some providers execute web search server-side: Anthropic Messages ships a <code>web_search_20250305</code> server tool, and OpenAI Responses (plus compatible gateways) ships a <code>web_search</code> tool. Unlike an MCP search server, the search runs inside the provider request, is billed by the provider per search, and returns structured blocks (<code>server_tool_use</code> + <code>web_search_tool_result</code>, <code>web_search_call</code>, <code>url_citation</code> annotations) that the client must keep in the conversation for multi-turn grounding.</p><p>PR #570 previously added this as a composer toggle with runtime-level SSE re-parsing; it was reverted (#608) with the owner asking for a plugin-shaped follow-up. A capability review found the plugin SDK cannot reach the request payload, the response stream, the composer, the transcript, or message persistence, so &quot;web search as a plugin&quot; would require four new SDK extension points — larger than the feature itself. This ADR records the host-built shape that landed instead.</p><p>Two upstream facts forced the design:</p><ul><li>pi-ai 0.86.1 drops vendor search blocks in both adapters: the Anthropic stream loop has no <code>server_tool_use</code> / <code>web_search_tool_result</code> / <code>citations_delta</code> branches, and the Responses item loop has no <code>web_search_call</code> slot or annotation handling. Extraction must therefore live inside the adapters, not beside them.</li><li>Anthropic requires <code>server_tool_use</code> + <code>web_search_tool_result</code> blocks (including <code>encrypted_content</code>) to be replayed verbatim on later turns. A display-only normalization cannot satisfy that; replay needs the raw wire blocks.</li></ul><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><p><strong>Capability is declared data, evaluated in one place.</strong> The per-model opt-in is <code>ModelBinding.nativeWebSearch</code> (settings UI: a checkbox in the model&#39;s advanced sheet, disabled unless the provider&#39;s API style is <code>responses</code> or <code>anthropic_messages</code>). models.dev publishes no hosted-tool capability, so there is no catalog default; absent means off. The single evaluation point is <code>resolveNativeWebSearch</code> in <code>packages/shared/src/native-web-search.ts</code>, keyed on the resolved wire API — never vendor names, base URL hostnames, or model-id substrings.</p></li><li><p><strong>Attachment and extraction live in the pi-ai adapters</strong>, delivered by extending <code>patches/@earendil-works__pi-ai@0.86.1.patch</code>:</p><ul><li><code>anthropic-messages.js</code> appends the <code>web_search_20250305</code> tool when <code>model.webSearch === true</code>, captures search blocks as <code>hostedSearch</code> content parts (raw wire block kept whole, streamed <code>input_json_delta</code> accumulated so the query is available live), collects <code>citations_delta</code>, and replays both halves of each pair in order on later turns.</li><li><code>openai-responses(-shared).js</code> (and the Azure variant&#39;s own <code>buildParams</code>) appends the <code>web_search</code> tool, asks for <code>web_search_call.action.sources</code> via the opt-in <code>include</code> channel, creates a <code>hostedSearch</code> slot for <code>web_search_call</code> items, collects <code>url_citation</code> annotations, and replays the item for the same model. Both adapters push a <code>hosted_search_update</code> stream event per block transition; <code>patches/@earendil-works__pi-agent-core@0.86.1.patch</code> teaches the agent loop to forward it as <code>message_update</code> — without that second patch the events die in the loop&#39;s switch and search activity renders only when the whole turn finishes. The patches are a stopgap; the same changes should be proposed upstream and dropped once released.</li></ul></li><li><p><strong>The flag travels the existing model-config channel.</strong><code>ModelConfigWithBinding</code> copies <code>nativeWebSearch</code> into <code>ModelConfig.webSearch</code>; <code>buildProviderModel</code> spreads catalog fields into the pi-ai <code>Model</code>, so no new IPC, sidecar parameter, or runtime rebuild hook is needed.</p></li><li><p><strong>Display data and replay data are separated, and both persist.</strong> The runtime normalizes <code>hostedSearch</code> blocks into <code>UiMessage.hostedSearch</code> (<code>status/rounds[]</code> for activity rows; <code>replay[]</code> for the adapter&#39;s raw content parts, stripped of streaming scratch). Persistence is an additive <code>hostedSearch</code> transcript block (<code>ui_to_record</code> / <code>record_to_ui</code>) — no SQL migration. <code>historyToEntries</code> restores <code>replay</code> after thinking and before text so convertMessages can ground later turns after a restart. Transcripts written without <code>replay</code> still render; they just cannot ground. The wire-slimming functions (<code>streamingMessageIdentity</code>, <code>applyMessageUpdate</code>) carry the field so delta frames cannot drop it.</p></li><li><p><strong>UI is a transcript activity row per round, not a composer control.</strong> Each round of a <code>hostedSearch</code> activity renders as a <code>HostedSearchRow</code> on the tool-row idiom (icon + name + query summary, chevron disclosure): searching state while the round is in flight, sources when expanded. Sources are plain text links — no favicon fetches — so reading a transcript never leaks source hostnames anywhere (the #579 rule). Citation badges in message text are out of scope for v1; links stay ordinary markdown links.</p></li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Enabling the tool is per model, always-on while enabled, and takes effect on the next turn (runtime rebuild follows from modelConfig change). A session-level toggle and composer affordance can layer on later without touching the adapter contract.</li><li>Gateways that mangle replay blocks surface as provider errors on the next turn; the user-visible remedy is unchecking the model&#39;s opt-in. No silent fallback strips the tool — a search that quietly did not happen is worse than an error.</li><li><code>pause_turn</code> keeps pi-ai&#39;s existing mapping to <code>stop</code>; a long searching turn may end early on the official Anthropic wire. This is a known limitation to revisit with the upstream patch.</li><li>The original implementation omitted search from compaction serialization. The 2026-09-22 contract repair supplies actual search replay projections to the summary request instead. Prefix/tail retention is unchanged: compacted content becomes a model-generated text summary (not lossless raw replay), and retained-tail search follows the existing adapter replay policy. The model can search again when the summary lacks sufficient grounding.</li><li>Search executes on the provider. There is no local fetch, no ask/allow prompt, and billing is the provider&#39;s. The model-level opt-in (default off) is the user consent surface.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0297-provider-hosted-web-search-adapter-capability.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0297ProviderHostedWebSearchAdapterCapability = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0297ProviderHostedWebSearchAdapterCapability as default
};
