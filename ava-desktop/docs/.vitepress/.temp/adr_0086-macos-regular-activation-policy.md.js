import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0086: Keep macOS on the regular activation policy","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0086-macos-regular-activation-policy.md","filePath":"adr/0086-macos-regular-activation-policy.md","lastUpdated":1786693323000}');
const _sfc_main = { name: "adr/0086-macos-regular-activation-policy.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0086-keep-macos-on-the-regular-activation-policy" tabindex="-1">ADR 0086: Keep macOS on the regular activation policy <a class="header-anchor" href="#adr-0086-keep-macos-on-the-regular-activation-policy" aria-label="Permalink to &quot;ADR 0086: Keep macOS on the regular activation policy&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-08-14</li><li>Deciders: PI-Desktop core</li><li>Related: D223, E2E-127, ADR 0072, ADR 0078, ADR 0080</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The global plugin launcher (ADR 0072) is a frameless always-on-top panel that should appear over whatever the user is doing, so its window asked Electron for <code>setVisibleOnAllWorkspaces(true, { visibleOnFullScreen: true })</code>. On macOS that option is not a per-window flag: Electron implements it by calling <code>TransformProcessType(kProcessTransformToUIElementApplication)</code> on the whole process, because macOS 10.14 and later only let accessory applications float windows above another app&#39;s fullscreen Space. The process therefore became a UIElement app and PI-Desktop disappeared from the Dock and from Cmd+Tab.</p><p>ADR 0080 then moved launcher creation into boot warm-up, so the transform ran on every launch even when the launcher was never opened. The window itself was still on screen, which made the symptom look like a window-manager bug rather than an activation-policy change.</p><p>Minimize hides the main window into the tray (ADR 0078), and macOS only emits Electron&#39;s <code>activate</code> event from <code>applicationShouldHandleReopen:</code> — a Dock click or a relaunch. Cmd+Tab and App Exposé do not reach it, so a tray-hidden window had no keyboard route back even once the app was listed again.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>PI-Desktop is a regular (foreground) macOS application for its entire lifetime. No code path may transform the process type: the launcher passes <code>skipTransformProcessType: true</code>, and <code>app.dock.hide()</code> and <code>app.setActivationPolicy</code> stay out of the main process. Dock and Cmd+Tab presence is an invariant, not a side effect of one window&#39;s options.</li><li>The launcher keeps its collection behavior: it joins every regular Space and floats above PI-Desktop&#39;s own fullscreen window. It does not overlay another application&#39;s fullscreen Space; macOS reserves that for accessory apps, and activation switches Spaces there instead.</li><li>macOS activation restores the shell through <code>did-become-active</code> as well as <code>activate</code>, gated on a booted, non-quitting app with no visible window. Cmd+Tab and App Exposé bring a tray-hidden window back, while showing the launcher or a plugin panel does not drag the main window forward with it.</li><li>Tests assert the launcher call shape and the activation handler, so a future window option cannot silently make the app accessory again.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>The app is always reachable from the Dock, Cmd+Tab, App Exposé, and the tray.</li><li>The launcher cannot overlay another app&#39;s fullscreen Space. Invoking it from one activates PI-Desktop and switches Spaces, which is ordinary foreground-app behavior.</li><li>Panel focus is unchanged: <code>show()</code> activates the app and makes the panel key under both policies, so ADR 0080&#39;s latency work is untouched.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><ul><li>Toggle the policy around each launcher show (<code>app.dock.hide()</code> before, <code>app.dock.show()</code> after). It restores fullscreen overlay, but it flickers the Dock icon on every invocation, adds work to the path ADR 0080 optimized, keeps a window where Cmd+Tab cannot see the app, and leaves the app permanently accessory if the restore is ever missed.</li><li>Drop <code>visibleOnFullScreen</code> entirely. Same Dock fix, but the launcher would also stop covering PI-Desktop&#39;s own fullscreen window for no gain.</li><li>Ship PI-Desktop as an <code>LSUIElement</code> tray app. That contradicts ADR 0078&#39;s resident main window and the product&#39;s main-window shape. Overlaying other apps&#39; fullscreen Spaces would need a separate accessory helper process, not a process-wide transform in the app that owns the main window.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0086-macos-regular-activation-policy.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0086MacosRegularActivationPolicy = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0086MacosRegularActivationPolicy as default
};
