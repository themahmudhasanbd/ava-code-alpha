import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"10. Plugin Developer Experience","description":"","frontmatter":{},"headers":[],"relativePath":"spec/07-plugins/10-plugin-devex.md","filePath":"spec/07-plugins/10-plugin-devex.md","lastUpdated":1788878032000}');
const _sfc_main = { name: "spec/07-plugins/10-plugin-devex.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="_10-plugin-developer-experience" tabindex="-1">10. Plugin Developer Experience <a class="header-anchor" href="#_10-plugin-developer-experience" aria-label="Permalink to &quot;10. Plugin Developer Experience&quot;">​</a></h1><h2 id="_1-goals" tabindex="-1">1. Goals <a class="header-anchor" href="#_1-goals" aria-label="Permalink to &quot;1. Goals&quot;">​</a></h2><p>Let a developer create and load a local plugin within 10 minutes.</p><p>The task-oriented <a href="./../../plugin-development">zero-to-one plugin development guide</a> covers the complete author journey. This specification freezes the developer surfaces and their acceptance criteria.</p><h2 id="_2-developer-path" tabindex="-1">2. Developer path <a class="header-anchor" href="#_2-developer-path" aria-label="Permalink to &quot;2. Developer path&quot;">​</a></h2><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Create from template            (the folder opens as the project)</span></span>
<span class="line"><span> → edit manifest / main / panel   (hot reload keeps it live)</span></span>
<span class="line"><span> → verify in the command palette</span></span>
<span class="line"><span> → check</span></span>
<span class="line"><span> → pack piplug</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>The first step has three entry points, all calling the same <code>@pi-desktop/plugin-devkit</code> implementation:</p><ul><li><strong>Plugins page</strong> — the overflow menu&#39;s &quot;New plugin from template&quot;, or the button on the empty state. Picks a template, asks for a folder, writes the files, loads the result as a development plugin, and then opens that folder as the active project so the sources are already in the workspace the agent and the file panel read. If the folder cannot be opened as a project the plugin still stays loaded, and the toast says only that it was created and loaded.</li><li><strong>Agent</strong> — <code>PluginScaffold</code>, in a conversation (&quot;write me a plugin that …&quot;). It writes inside the current workspace, which is already open.</li><li><strong>CLI</strong> — <code>pnpm pi-plugin init &lt;template&gt; &lt;dir&gt;</code>.</li></ul><h2 id="_3-template-types" tabindex="-1">3. Template types <a class="header-anchor" href="#_3-template-types" aria-label="Permalink to &quot;3. Template types&quot;">​</a></h2><p>Official templates, all four available:</p><ol><li><code>panel-basic</code>: panel + command</li><li><code>agent-tool-basic</code>: register a tool</li><li><code>skill-pack</code>: skills only</li><li><code>full-demo</code>: panel + command + tool + skill + settings</li></ol><p>Every template scaffolds a manifest with <code>schemaVersion: 1</code>, a <code>main.js</code>, a README, and only the permissions the template actually uses. Scaffolding refuses to write into a non-empty directory.</p><p>Panel templates include the current <code>pi-plugin-chrome</code> v2 marker and the neutral PI-Desktop surface tokens. Their body uses <code>var(--pi-plugin-titlebar-height, 0px)</code> so the same entry works in a detached window and a docked work-panel view without adding a second top spacer.</p><p>Current repo example:</p><ul><li><code>examples/plugins/hello</code></li></ul><h2 id="_4-sdk-and-devkit" tabindex="-1">4. SDK and devkit <a class="header-anchor" href="#_4-sdk-and-devkit" aria-label="Permalink to &quot;4. SDK and devkit&quot;">​</a></h2><p><code>@pi-desktop/plugin-sdk</code> is imported by plugin code itself and stays dependency-free and Node-free. It provides:</p><ul><li>manifest types</li><li>permission enums</li><li>API types (<code>PiPluginHostApi</code>)</li><li>manifest validation function</li><li>test helper (mock host)</li></ul><p>The SDK&#39;s clipboard surface includes <code>pi.clipboard.getHistory()</code>, which returns bounded, newest-first text and image entries through the existing <code>clipboard.read</code> permission. The host records explicit writes and Composer paste events without background polling. Plugin authors should use this API for clipboard-history features instead of polling <code>readText()</code> and maintaining a second store.</p><p><code>@pi-desktop/plugin-devkit</code> is tooling, not runtime, and may use Node. It owns <code>scaffold</code> / <code>check</code> / <code>pack</code> and the <code>pi-plugin</code> CLI. All three developer surfaces (CLI, agent tools, plugins page) call it, so a rule enforced once holds everywhere.</p><h2 id="_5-local-development-commands" tabindex="-1">5. Local development commands <a class="header-anchor" href="#_5-local-development-commands" aria-label="Permalink to &quot;5. Local development commands&quot;">​</a></h2><p>The CLI is currently delivered as a private workspace package. From a checkout of this repository, install dependencies and build the devkit plus its dependencies once:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># repository setup</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> install</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --filter</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> @pi-desktop/plugin-devkit...</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> build</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># create from a template</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> pi-plugin</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> init</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> full-demo</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> /tmp/my-plugin</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># validate manifest and package contents</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> pi-plugin</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> check</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> .</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># pack</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> pi-plugin</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> pack</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> .</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># outputs dist/demo.hello-0.1.0.piplug</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># pack and pin the version to the commit that produced it</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> pi-plugin</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> publish</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> .</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># also writes dist/demo.hello-0.1.0.submission.json</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br><span class="line-number">19</span><br></div></div><p><code>publish</code> is for distributing through the plugin center. It packs, then records the canonical repository URL, the tag or commit ref, the resolved commit, and the plugin subdirectory alongside the package checksum, so the artifact and the source it claims to come from describe one moment. It refuses a dirty worktree and a git remote carrying credentials, and warns when no tag points at HEAD. The center re-resolves everything itself; a submission is a claim to be checked. See <a href="./15-plugin-center">15-plugin-center.md</a>.</p><p><code>check</code> reproduces every rule the installer enforces, so <code>check</code> passing implies install will pass. It reports errors — a missing or unparseable <code>manifest.json</code>, missing <code>main</code> / <code>ui.panel</code> / skill files, a skill path escaping the plugin directory, an unknown permission, a symlink, more than 2000 files, more than 50 MB — and warnings, which do not block: high-risk permissions, permissions declared but never used by the code, <code>contributes.skills</code> without <code>agent.prompt.inject</code> (the skills would be inert), and an empty <code>contributes</code>.</p><p><code>pack</code> writes <code>dist/&lt;id&gt;-&lt;version&gt;.piplug</code>, skipping <code>.git</code> and <code>node_modules</code> exactly as the installer&#39;s copy does, and prints the sha256. It runs <code>check</code> first and refuses to pack a plugin with errors. <strong>Entries are stored uncompressed (method 0)</strong>: the installer accepts nothing else, so a <code>.piplug</code> must never be built with <code>zip</code> or another shell tool.</p><h2 id="_6-agent-tools" tabindex="-1">6. Agent tools <a class="header-anchor" href="#_6-agent-tools" aria-label="Permalink to &quot;6. Agent tools&quot;">​</a></h2><p>Three tools are served from Electron main (host-core never sees them), each resolving its <code>directory</code> argument against the session&#39;s workspace root and refusing to escape it:</p><table tabindex="0"><thead><tr><th>Tool</th><th>Modes</th><th>Effect</th></tr></thead><tbody><tr><td><code>PluginCheck</code></td><td>all</td><td>Validates a plugin directory; read-only</td></tr><tr><td><code>PluginScaffold</code></td><td>agent</td><td>Writes a template, then loads it as a development plugin</td></tr><tr><td><code>PluginPack</code></td><td>agent</td><td>Validates, then writes <code>dist/&lt;id&gt;-&lt;version&gt;.piplug</code></td></tr></tbody></table><p>A built-in skill, <code>apps/desktop/resources/skills/plugin-development.md</code>, documents the manifest schema, the permission tiers, the host API surface, and this loop. It activates only when the session workspace looks like plugin development — a plugin <code>manifest.json</code> at the workspace root, or a loaded development plugin inside it — so ordinary sessions pay only for the three tool descriptions. Scaffolding writes a manifest, which turns the full skill on from the next prompt; creating from a template on the plugins page also opens the new folder as the project, so the workspace test passes right away.</p><h2 id="_7-hot-reload" tabindex="-1">7. Hot reload <a class="header-anchor" href="#_7-hot-reload" aria-label="Permalink to &quot;7. Hot reload&quot;">​</a></h2><p>A plugin loaded from a folder is watched from then on, including across restarts: the folder is picked once, not once per edit.</p><ul><li>Any change under the plugin directory reloads it, debounced 300 ms, so one save burst is one reload. <code>node_modules</code>, <code>.git</code>, <code>dist</code>, <code>target</code> and editor scratch files are ignored — a plugin writing into its own <code>dist/</code> must not reload itself forever.</li><li>A reload unloads the previous process and runs the plugin again from disk, so a manifest, <code>main</code>, or skill change all take effect the same way. Panels are re-created from the reloaded contribution.</li><li><strong>A reload can never widen permissions.</strong> The reload reads the manifest first and compares it against the set approved when the folder was picked; anything new stops the reload with <code>PERMISSION_DENIED</code> and a message to load the plugin again so the grant can be reviewed. Removed permissions do take effect immediately — grants follow the manifest downwards, never upwards.</li><li>The Plugins page offers Reload for <code>source: &quot;dev&quot;</code> rows inside the row&#39;s More actions menu. After a permission-gated hot reload, choosing it explicitly reloads the registered folder with the current manifest and refreshes the permission ceiling used by later file-watch reloads. The action does not require picking the folder again.</li><li>A failed reload (syntax error, invalid manifest) leaves the plugin unloaded but still watched, so the save that fixes it recovers the plugin. The failure is reported as a toast plus a plugin-changed event; the registry row does not currently move to <code>load_error</code>, because host-core has no RPC for a runtime-side load failure.</li><li>Watchers are released on unload, disable, uninstall and quit, and are capped at 16 plugins; past the cap the app logs and edits need a manual reload.</li></ul><h2 id="_8-debugging" tabindex="-1">8. Debugging <a class="header-anchor" href="#_8-debugging" aria-label="Permalink to &quot;8. Debugging&quot;">​</a></h2><p>Implemented today:</p><ul><li>Load and hot-reload failures appear as toasts; persisted load failures also appear on the plugin row.</li><li>Open <strong>Settings → Info → Logs</strong> and filter records by <code>pluginId</code> to inspect lifecycle, host API, tool, service, and bus activity.</li><li>The Plugins page shows declared capabilities, permissions, and resident service state. Registered commands can be verified in global search.</li></ul><p>Later:</p><ul><li>Dedicated per-plugin log panel with stack-copy affordance</li><li>Dedicated DevTools for the panel</li><li>Mock tool invoker</li></ul><h2 id="_9-documentation-checklist-developer-site-repo-docs" tabindex="-1">9. Documentation checklist (developer site / repo docs) <a class="header-anchor" href="#_9-documentation-checklist-developer-site-repo-docs" aria-label="Permalink to &quot;9. Documentation checklist (developer site / repo docs)&quot;">​</a></h2><ul><li>Quick start</li><li>manifest fields</li><li>Permission reference</li><li>API manual</li><li>Publishing manual (pack/sign)</li><li>Security best practices</li></ul><h2 id="_10-quality-gate-recommended-before-publishing" tabindex="-1">10. Quality gate (recommended before publishing) <a class="header-anchor" href="#_10-quality-gate-recommended-before-publishing" aria-label="Permalink to &quot;10. Quality gate (recommended before publishing)&quot;">​</a></h2><ul><li><code>pi-plugin check</code> reports no errors</li><li>No calls to undeclared permissions</li><li>Has a README</li><li>Has a version changelog</li><li>If it includes a tool: provide parameter examples</li></ul><h2 id="_11-acceptance" tabindex="-1">11. Acceptance <a class="header-anchor" href="#_11-acceptance" aria-label="Permalink to &quot;11. Acceptance&quot;">​</a></h2><ol><li>A new plugin can be created from a template, from the plugins page, the agent or the CLI</li><li>Development load succeeds</li><li>An edit reloads the plugin without re-picking its folder, and a broken edit recovers on the next save</li><li><code>check</code> passes and the <code>pack</code> artifact installs</li><li>A declared skill reaches the model when <code>agent.prompt.inject</code> is granted, and stops reaching it when the permission is revoked</li></ol></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("spec/07-plugins/10-plugin-devex.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _10PluginDevex = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _10PluginDevex as default
};
