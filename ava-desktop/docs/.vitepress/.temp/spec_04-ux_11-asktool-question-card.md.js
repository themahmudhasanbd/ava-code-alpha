import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"11. asktool Question Card","description":"","frontmatter":{},"headers":[],"relativePath":"spec/04-ux/11-asktool-question-card.md","filePath":"spec/04-ux/11-asktool-question-card.md","lastUpdated":1789570169000}');
const _sfc_main = { name: "spec/04-ux/11-asktool-question-card.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="_11-asktool-question-card" tabindex="-1">11. asktool Question Card <a class="header-anchor" href="#_11-asktool-question-card" aria-label="Permalink to &quot;11. asktool Question Card&quot;">​</a></h1><p>The asktool card is an inline composer approval surface, not a permission dialog. It is mounted in the same dock area as the Plan and Goal approval card, immediately above the composer input, so a paused question stays available at the active decision point instead of moving into transcript history.</p><p>It rides the composer plate — <code>--ds-bg-composer</code> with <code>--ds-shadow-composer</code>, like the Plan and Goal approval bar — rather than the in-flow <code>--ds-tile</code> wash, and its option rows and custom input are inlaid <code>--ds-tile-deep</code> fills on that plate (D297, D435). The message width, typography, and button tokens still come from the conversation so a paused question remains part of it. The card shell stays slim — 14 px × 16 px padding, matching the permission card&#39;s compact footprint, with no accent rail (D297). The question text uses the compact card body size (<code>--text-md</code>, 13 px) at medium weight — the same scale as the permission card&#39;s title and prompt in the same dock area — so it reads as the card&#39;s primary focal point without competing with the surrounding transcript. Options and the custom input use the same compact size, keeping the card&#39;s content in one coordinated tier.</p><p>The header identifies the prompt and shows progress. Small clickable indicators encode answered, unanswered, skipped, and current state without competing with the question text. Choice controls use radio semantics for single-select and checkbox semantics for multi-select. Every question includes a visible custom input choice. The action row contains Skip and Next/Submit, with Decline all as a quiet secondary action in the header.</p><p>The card has no countdown or expiration copy. On narrow screens options remain full-width and actions may share the row; question text and custom input may wrap naturally without clipping.</p><h2 id="typography-hierarchy" tabindex="-1">Typography hierarchy <a class="header-anchor" href="#typography-hierarchy" aria-label="Permalink to &quot;Typography hierarchy&quot;">​</a></h2><p>The card uses a compact two-tier type scale — quiet labels, then content at the card body size — so the question and options stay coordinated:</p><table tabindex="0"><thead><tr><th>Element</th><th>Token</th><th>Size</th><th>Weight</th><th>Notes</th></tr></thead><tbody><tr><td>Card title</td><td><code>--text-xs</code></td><td>11 px</td><td>medium</td><td>Uppercase, wide tracking</td></tr><tr><td>Question number</td><td><code>--text-xs</code></td><td>11 px</td><td>medium</td><td>Wide tracking, eyebrow role</td></tr><tr><td>Progress</td><td><code>--text-2xs</code></td><td>10.5 px</td><td>—</td><td>Faint, most subtle</td></tr><tr><td>Question text</td><td><code>--text-md</code></td><td>13 px</td><td>medium</td><td>Primary focal point, compact</td></tr><tr><td>Option text</td><td><code>--text-md</code></td><td>13 px</td><td>—</td><td>Matches question scale</td></tr><tr><td>Option mark</td><td><code>--text-xs</code></td><td>11 px</td><td>—</td><td>15 px box, proportional</td></tr><tr><td>Custom input text</td><td><code>--text-md</code></td><td>13 px</td><td>—</td><td>Matches option text</td></tr><tr><td>Decline button</td><td><code>--text-xs</code></td><td>11 px</td><td>—</td><td>Quiet secondary action</td></tr></tbody></table><h2 id="control-density" tabindex="-1">Control density <a class="header-anchor" href="#control-density" aria-label="Permalink to &quot;Control density&quot;">​</a></h2><p>The card shell is a slim rail: 14 px × 16 px padding with a 2 px accent rail, the same footprint family as the permission and turn-outcome cards.</p><p>The internal rhythm stays airy rather than packed: the status indicators keep 12 px above and 14 px below breathing room, the question renders at <code>--leading-normal</code> (1.4) with 8 px under the eyebrow label, and each content section (indicators → question → options → custom input → actions) is separated by 12 px.</p><p>The option rows follow the app&#39;s global menu-row density instead of card-scale blocks: 30 px min-height, 4 px × 8 px padding, 8 px row gaps, and the same 15 px radio/checkbox mark used by the sidebar checkboxes. The Skip / Next / Submit buttons use the app&#39;s compact button size (4 px × 10 px padding, 12 px text); Decline remains a quiet text link in the header.</p><p>When a question has enough options to exceed the available dock height, the option list becomes the scroll container instead of allowing the composer dock to grow beyond the viewport. The list uses a dynamic viewport cap of <code>min(320px, 36dvh)</code> (with a <code>vh</code> fallback), keeps a stable scrollbar gutter, contains scroll chaining, and preserves keyboard scroll padding. The question header, question text, custom-answer input, and Skip / Next / Submit actions remain visible while the user scrolls through the options. This behavior also applies on narrow screens; the card does not rely on page-level scrolling.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("spec/04-ux/11-asktool-question-card.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _11AsktoolQuestionCard = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _11AsktoolQuestionCard as default
};
