import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0128: Share one bounded budget for transient provider failures","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0128-bounded-transient-provider-retry.md","filePath":"adr/0128-bounded-transient-provider-retry.md","lastUpdated":1787837145000}');
const _sfc_main = { name: "adr/0128-bounded-transient-provider-retry.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0128-share-one-bounded-budget-for-transient-provider-failures" tabindex="-1">ADR 0128: Share one bounded budget for transient provider failures <a class="header-anchor" href="#adr-0128-share-one-bounded-budget-for-transient-provider-failures" aria-label="Permalink to &quot;ADR 0128: Share one bounded budget for transient provider failures&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-08-21</li><li>Deciders: PI-Desktop core</li><li>Related: D259, E2E-096, E2E-149, ADR 0050, ADR 0091, D186, D245</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Upstream gateway faults are common on OpenAI-compatible endpoints. A relay that answers <code>OpenAI API error (502): {&quot;type&quot;:&quot;api_error&quot;,&quot;message&quot;:&quot;Upstream API request failed.&quot;}</code> is reporting a momentary upstream outage, not a rejected request: the same prompt usually succeeds on the next attempt.</p><p>ADR 0091 gave HTTP 429 a shared five-retry budget but deliberately left every other transient failure on the older split policy: one retry during request setup and one mid-stream replay, each guarded by its own boolean. Two gaps followed. First, a 502 that arrived before headers consumed the single setup retry, so a second 502 became a terminal error card even though a third attempt would likely have succeeded. Second, the stream phase excluded <code>PROVIDER_ERROR</code> entirely, so a mid-stream gateway fault was never replayed at all. Builtin subagents were stricter still: their claim required <code>phase === &quot;request&quot;</code>, so a delegate failed on the first mid-stream transient error. The three provider entry points (main session, subagent, composer enhancement) had drifted into three different policies.</p><p>The non-429 backoff also ignored <code>Retry-After</code>. Gateways that emit 502/503 with an explicit <code>Retry-After</code> were retried on a fixed 750 ms mid-stream timer or a 500 ms setup timer regardless of what the server asked for.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>Non-429 transient provider failures share one bounded logical-turn budget of four retries after the initial attempt, for five provider attempts total. The budget is shared by request setup and stream delivery, so a fault that moves between phases cannot reset or multiply it.</li><li>The budget admits exactly <code>NETWORK_ERROR</code>, <code>TIMEOUT</code>, <code>STREAM_FAILED</code>, and <code>PROVIDER_ERROR</code>. <code>PROVIDER_ERROR</code> is now admitted in the stream phase as well as during setup. Every other classification, including a non-retryable <code>PROVIDER_ERROR</code> from a malformed 400/422 request, stays terminal.</li><li>Retry delay honors the server first. <code>retry-after-ms</code>, <code>retry-after</code> seconds, then <code>retry-after</code> HTTP-date take precedence over client backoff, capped at 8 seconds for the non-429 path. Captured response headers are retained for any status that can carry a usable delay (429, 408, 409, and 5xx) instead of 429 alone. Without a usable header the wait is a plain doubling schedule of 1, 2, 4, then 8 seconds, identical in both phases and deterministic (no jitter), because it paces one failed request rather than a synchronized rate-limit burst.</li><li>The main session, builtin subagents, and one-shot composer enhancement use the same codes, the same budget size, and the same delay precedence.</li><li>Retries stay silent and abortable, and 429 keeps its own separate five-retry budget from ADR 0091. The two budgets do not draw from each other.</li><li>Exhaustion emits one terminal assistant error and one lifecycle error, with <code>retryAttempt: 4</code> for a persistent non-429 transient failure.</li><li>Only the failed request is replayed. The session, transcript, and tool state are untouched: the failed assistant leaves the next model context and its visible message id is reused, so a retry never restarts the turn or re-runs a completed tool call.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A short upstream 502/503/504 burst recovers without an error card, whether it lands before headers or mid-stream.</li><li>A persistent upstream outage stays bounded and visible: five attempts, at most four backoffs totalling 15 seconds, and an individual wait capped at 8 seconds.</li><li>Worst-case latency before a terminal non-429 error grows, bounded by four waits (15 seconds of backoff) instead of one.</li><li>Gateways that state their own pacing are respected, while malformed or excessive header values cannot hold a turn.</li><li>The three provider entry points can no longer drift into different transient policies.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><ul><li>Raise pi-ai&#39;s <code>maxRetries</code> instead. Rejected for the reason recorded in ADR 0091: nested wrappers multiply attempts, make the budget phase-dependent, and reintroduce a non-abortable SDK sleep.</li><li>Fold non-429 transient failures into the 429 budget. Rejected: a rate limit and a gateway outage want different delay scales, and one shared counter would let a 502 burst consume the rate-limit budget.</li><li>Retry every retriable classification three times. Rejected: <code>EMPTY_MODEL_RESPONSE</code> and context failures have their own recovery paths, and re-sending the same request does not repair them.</li></ul><h2 id="references" tabindex="-1">References <a class="header-anchor" href="#references" aria-label="Permalink to &quot;References&quot;">​</a></h2><ul><li><code>docs/spec/03-runtime/02-agent-runtime.md</code> §5d</li><li><code>docs/spec/03-runtime/08-error-codes.md</code></li><li><code>docs/spec/06-delivery/04-e2e-test-plan.md</code> E2E-096, E2E-149</li><li><code>docs/spec/08-meta/decisions-log.md</code> D259</li><li><code>packages/agent-runtime/src/provider-retry.ts</code></li><li><code>packages/agent-runtime/src/runtime.ts</code></li><li><code>packages/agent-runtime/src/subagent.ts</code></li><li><code>packages/agent-runtime/src/prompt-enhancement.ts</code></li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0128-bounded-transient-provider-retry.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0128BoundedTransientProviderRetry = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0128BoundedTransientProviderRetry as default
};
