import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"16. 受信任扩展","description":"","frontmatter":{},"headers":[],"relativePath":"zh-CN/spec/07-plugins/16-trusted-extensions.md","filePath":"zh-CN/spec/07-plugins/16-trusted-extensions.md","lastUpdated":1790057981000}');
const _sfc_main = { name: "zh-CN/spec/07-plugins/16-trusted-extensions.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="_16-受信任扩展" tabindex="-1">16. 受信任扩展 <a class="header-anchor" href="#_16-受信任扩展" aria-label="Permalink to &quot;16. 受信任扩展&quot;">​</a></h1><blockquote><p><strong>翻译说明：</strong> 本页是与 <a href="/spec/07-plugins/16-trusted-extensions">英文源规格</a> 一一对应的机器辅助翻译。代码、协议字段和标识符保持原文；如翻译与英文源事实有歧义，以英文版本为准。</p></blockquote><blockquote><p>状态：v1.1 已实现（D387 / D388、ADR 0214 / ADR 0215）；实现说明标注为“v1 说明” 范围：v1.1。v2 与 v3 事项列于 §12，不构成承诺。</p></blockquote><h2 id="_1-目的与术语" tabindex="-1">1. 目的与术语 <a class="header-anchor" href="#_1-目的与术语" aria-label="Permalink to &quot;1. 目的与术语&quot;">​</a></h2><p>插件（<a href="/zh-CN/spec/07-plugins/01-plugin-system">01-plugin-system.md</a>）是 PI-Desktop 唯一的扩展面。本文规定其中一种插件贡献点 <code>contributes.agentExtensions</code>：在 Agent sidecar 内运行的 TypeScript 或 JavaScript 模块，接收一个 <code>ExtensionAPI</code> 对象，直接在 agent 循环上注册工具、命令和事件处理器。<code>ExtensionAPI</code> 契约即 <code>@earendil-works/pi-coding-agent</code> 定义的契约，PI-Desktop 与 <code>pi-ai</code>、<code>pi-agent-core</code> 内核（ADR 0002）一起采纳，因此为 pi CLI 写的扩展就是插件贡献的模块。D388 把此前 独立的“受信任扩展”注册表并入了这个贡献点；下文的引擎部分不变。</p><table tabindex="0"><thead><tr><th>术语</th><th>含义</th></tr></thead><tbody><tr><td>Agent 扩展</td><td>插件在 <code>contributes.agentExtensions</code> 中列出的一个模块，面向 <code>ExtensionAPI</code> 编写，以 Agent sidecar 的信任级别运行</td></tr><tr><td>插件</td><td>带 manifest 的 PI-Desktop 插件，在独立进程中、权限网关之下运行（ADR 0008）；是其 agent 扩展的拥有者、安装者和启用记录</td></tr><tr><td>适配层</td><td><code>packages/agent-runtime</code> 中在桌面运行时之上实现 <code>ExtensionAPI</code> 的层</td></tr><tr><td>Runner</td><td>绑定到一个桌面会话的一个桌面自有 <code>TrustedExtensionRunner</code> 实例（v1 说明：不复用 pi-coding-agent 的 <code>ExtensionRunner</code>，因为它绑定终端主题；其 <code>ExtensionAPI</code> 类型仅作类型依赖）</td></tr></tbody></table><h2 id="_2-定位与信任模型" tabindex="-1">2. 定位与信任模型 <a class="header-anchor" href="#_2-定位与信任模型" aria-label="Permalink to &quot;2. 定位与信任模型&quot;">​</a></h2><ol><li>Agent 扩展随其插件一起安装、启用、限定范围、更新和移除。没有第二个列表、存储或 设置页。</li><li>Agent 扩展是受信任代码。它在 Agent sidecar 内执行，而 sidecar 已持有 bash、edit 和 write 工具，因此授予 <code>agent.extension</code> 权限授予的正是运行 agent 已经授予的东西。 <a href="/zh-CN/spec/07-plugins/04-plugin-security">04-plugin-security.md</a> 的插件沙箱不 覆盖这些模块，这正是该权限作为独立高风险授权、而非 <code>agent.tool.register</code> 隐含 部分的原因。</li><li>没有授权就不运行。声明了 <code>contributes.agentExtensions</code> 却没有 <code>agent.extension</code> 的 manifest 校验不通过；记录的授权中缺少该权限的插件照常加载但跳过其模块并记审计 （<code>plugin.agentExtensions.skipped</code>）。D007 继续有效：PI-Desktop 永不自动导入 <code>~/.pi</code>。</li><li>项目范围就是插件的激活范围。限定到某些项目的插件只向这些项目的会话贡献模块。v1 说明：没有独立的项目信任状态，插件范围即信任决定，<code>project_trust</code> 不触发。</li><li>v1.1 不开放持有 <code>agent.extension</code> 的插件在市场分发：该权限只接受本地导入和开发 插件。市场上架等签名机制（规格 08）到位后再定。</li></ol><h2 id="_3-贡献与导入" tabindex="-1">3. 贡献与导入 <a class="header-anchor" href="#_3-贡献与导入" aria-label="Permalink to &quot;3. 贡献与导入&quot;">​</a></h2><h3 id="_3-1-manifest" tabindex="-1">3.1 Manifest <a class="header-anchor" href="#_3-1-manifest" aria-label="Permalink to &quot;3.1 Manifest&quot;">​</a></h3><div class="language-json vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">{</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;id&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;acme.git-helper&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;name&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;Git helper&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;version&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;1.0.0&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;main&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;main.js&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;permissions&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: [</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;agent.extension&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">],</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;contributes&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: { </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">&quot;agentExtensions&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: [</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;src/index.ts&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">] }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><p>规则：最多八个条目；每个条目是插件目录内的相对 <code>.ts</code>、<code>.mts</code>、<code>.js</code> 或 <code>.mjs</code> 路径； 加载时文件必须存在；列出条目却没有权限的 manifest 无效 （<a href="/zh-CN/spec/07-plugins/02-plugin-manifest-schema">02-plugin-manifest-schema.md</a> §4 与 §7）。 当插件不贡献其他内容时，<code>main</code> 可以是空操作模块。</p><h3 id="_3-2-导入-pi-cli-扩展或技能包" tabindex="-1">3.2 导入 pi CLI 扩展或技能包 <a class="header-anchor" href="#_3-2-导入-pi-cli-扩展或技能包" aria-label="Permalink to &quot;3.2 导入 pi CLI 扩展或技能包&quot;">​</a></h3><p>插件页 →“导入 pi 扩展”打开原生选择器（main 拥有路径，D344），由用户明确选择本地 文件或目录。main 把所选源码复制到 <code>&lt;dataDir&gt;/plugins/imported/&lt;slug&gt;/src/</code>，生成空操作 CommonJS <code>main.cjs</code> 和 id 为 <code>imported.&lt;slug&gt;</code> 的 manifest（重复导入时追加唯一后缀），再通过 既有本地插件流程注册。选择器之前的确认仍是信任决定；生成的 manifest 只声明实际贡献 所需的权限。无论源包的 <code>type</code> 为何，manifest 的 <code>main</code> 都指向 <code>main.cjs</code>； 两份复制的包声明保留原有模块语义。加载时若导入插件的 <code>main</code> 仍是生成的 CommonJS <code>main.js</code> 空包装器，则就地改写为 <code>main.cjs</code> 并更新 manifest；复制的包文件、授权和 激活范围保持不变。只匹配生成的空操作（含最初的注释文本）。自定义过的 <code>main.js</code> 不会改动。不删除就重新导入仍会创建带唯一后缀的独立插件，不会从旧副本复制授权或 激活范围。</p><p>扩展文件及未声明 <code>pi.skills</code> 的包保持既有 <code>pi-coding-agent</code> 入口发现规则：先取 <code>package.json</code> 的 <code>pi.extensions</code>，否则取 <code>index.ts</code> / <code>index.js</code>，再否则取一层深度内 的松散 <code>*.ts</code> / <code>*.js</code> 文件。明确声明 <code>pi.skills</code> 且没有 <code>pi.extensions</code>（或该数组为空） 的包视为仅技能包：包括 <code>index.js</code> 在内的附带脚本作为资源复制，不会被提升为可执行的 Agent 扩展。</p><p>目录若自带 <code>package.json</code>，会（连同其 npm lockfile）一并复制到插件根并剥离 <code>workspaces</code> 字段。 若声明了生产或可选依赖，main 会在首次加载前执行有界的两阶段安装：先运行 <code>npm install --package-lock-only --omit=dev --legacy-peer-deps --no-audit --no-fund --ignore-scripts</code> 并校验完整生成的 lockfile，再使用相同安全参数运行 <code>npm ci</code>。 <code>dependencies</code>、<code>optionalDependencies</code>、<code>devDependencies</code> 和 <code>peerDependencies</code> 中的 直接 spec 都必须来自 registry，因为 npm 可能检查全部四者；git resolver 会被禁用。 不会运行生命周期脚本。安装失败会清理部分依赖/cache、上报渲染层且不阻塞导入。确认对话框 会与技能披露一并说明 npm 安装步骤。</p><table tabindex="0"><thead><tr><th>来源</th><th>结果</th></tr></thead><tbody><tr><td>一个 pi 扩展目录或文件</td><td><code>plugins/imported</code> 下的本地插件，id 为 <code>imported.&lt;slug&gt;</code></td></tr><tr><td>声明了 <code>contributes.agentExtensions</code> 的插件包</td><td>像其他插件一样安装；安装时询问该授权</td></tr><tr><td>带 <code>pi.extensions</code> 的 <code>package.json</code></td><td><code>src/</code> 下的入口，通过 <code>contributes.agentExtensions</code> 贡献，需 <code>agent.extension</code></td></tr><tr><td>带 <code>pi.skills</code> 的 <code>package.json</code></td><td><code>src/</code> 下的 Markdown 文档，通过 <code>contributes.skills</code> 贡献，需 <code>agent.prompt.inject</code></td></tr><tr><td>仅技能包</td><td>持有 <code>agent.prompt.inject</code> 的空操作插件，不授予 <code>agent.extension</code></td></tr></tbody></table><p><code>pi.skills</code> 是最多含 32 条非空路径的数组，每条路径相对于包目录，指向 Markdown 文件 或目录。明确指定的 <code>.md</code> 文件直接作为技能。对于目录，优先使用其自身的 <code>SKILL.md</code>； 若不存在，则纳入该目录直接包含的 <code>.md</code> 文件，并在子目录中查找 <code>SKILL.md</code>。 嵌套技能目录找到自身的 <code>SKILL.md</code> 后停止向下扫描，避免把支持文档变成额外技能。 扫描跳过点号开头的条目和 <code>node_modules</code>，对文档去重，目录扫描预算为 256。 发现超过 32 个技能、声明的路径不存在或路径类型不受支持时，导入失败，不会静默生成 不完整目录。每项贡献都按包内相对路径生成明确、稳定的插件内 ID，不同目录下同名的 <code>SKILL.md</code> 保持独立。既有插件技能正文解析、大小限制、权限与卸载行为保持不变。</p><p>复制时按所选包的相对路径判断排除项。包的祖先路径含 <code>node_modules</code> 不影响复制， 只排除包自身依赖目录中的 <code>node_modules</code> 路径段。引用文档、素材、辅助脚本及其他 普通源码文件保留在 <code>src/</code> 下，使技能的相对资源引用仍然成立。凭据文件（<code>.env*</code>、 <code>.npmrc</code>、<code>.netrc</code>、<code>.pypirc</code>、私钥和证书文件）及仓库元数据目录不会被复制。所选 根目录先解析为真实路径；贡献路径必须位于根目录内，不能包含 <code>..</code> 穿越，也不能指向 包内依赖目录。绝对 <code>pi.skills</code> 路径与后代符号链接会被拒绝；复制保留资源时也拒绝 符号链接，复制失败会清理部分生成的目录。生成目标以原子方式创建，不能位于所选源目录内。</p><p>这是显式本地导入，不是 pi CLI 包管理器：不会自动扫描或导入 <code>~/.pi</code>，不会读取 CLI 已安装包注册表，也不会执行 npm 生命周期脚本。声明依赖时，有界安装器只接受 registry 版本说明和 registry 来源的 npm lockfile，拒绝不安全的包路径和嵌套依赖 spec，禁用 git 解析，并隔离 npm 的配置/cache 与用户凭据和代理设置。导入包不代表其所有第三方扩展依赖都能执行。</p><h2 id="_4-加载与运行时" tabindex="-1">4. 加载与运行时 <a class="header-anchor" href="#_4-加载与运行时" aria-label="Permalink to &quot;4. 加载与运行时&quot;">​</a></h2><h3 id="_4-1-扩展在哪里运行" tabindex="-1">4.1 扩展在哪里运行 <a class="header-anchor" href="#_4-1-扩展在哪里运行" aria-label="Permalink to &quot;4.1 扩展在哪里运行&quot;">​</a></h3><p>扩展在 Agent sidecar 进程（<code>packages/agent-runtime</code>）内加载，永远不在 Electron main、渲染层或插件宿主进程中。</p><h3 id="_4-2-loader" tabindex="-1">4.2 Loader <a class="header-anchor" href="#_4-2-loader" aria-label="Permalink to &quot;4.2 Loader&quot;">​</a></h3><ul><li>sidecar 以与 <code>pi-ai</code>、<code>pi-agent-core</code> 完全相同的锁定版本依赖 <code>@earendil-works/pi-coding-agent</code>，仅作类型依赖。三者版本必须一致；漂移时 CI 失败。</li><li>loader 镜像 <code>pi-coding-agent</code> 的发现规则，使用带 <code>virtualModules</code> 的 <code>jiti/static</code>，babel 转换被打进包内，运行时不做路径解析。打包步骤由一个在仓库 之外运行打包产物的契约测试验证（E2E-245）。</li><li>导入别名：<code>pi-ai</code>、<code>pi-agent-core</code> 和 <code>typebox</code> 解析到 sidecar 自带的副本； <code>@earendil-works/pi-coding-agent</code> 解析到一个运行时 shim，导出 <code>defineTool</code> 和 工具结果类型守卫。<code>@earendil-works/pi-tui</code> 解析到一个桩 模块，它把每个符号导出为惰性值，使顶层 import 永不失败。调用被桩替代的 符号时在调用点产生一条诊断。</li></ul><h3 id="_4-3-每会话一个-runner" tabindex="-1">4.3 每会话一个 Runner <a class="header-anchor" href="#_4-3-每会话一个-runner" aria-label="Permalink to &quot;4.3 每会话一个 Runner&quot;">​</a></h3><ul><li>每个桌面会话拥有自己的 Runner。Runner 随会话运行时创建，随其丢弃而销毁。</li><li>由于 jiti 缓存模块，模块实例在 Runner 之间共享。因此模块级状态在会话之间 共享，这与扩展作者在 pi 单进程运行多会话时看到的一致。v1 记录这一点而不 绕开它。</li><li>启用、禁用或重新扫描会使所有 Runner 失效；受影响的会话在下一个回合边界重新 加载扩展。进行中的回合永不被重新加载打断。</li></ul><h3 id="_4-4-加载失败" tabindex="-1">4.4 加载失败 <a class="header-anchor" href="#_4-4-加载失败" aria-label="Permalink to &quot;4.4 加载失败&quot;">​</a></h3><p>加载错误永不导致会话失败。该扩展在诊断中标记为 <code>error</code> 并附消息和堆栈，其余 扩展继续加载，回合照常进行。当某个已启用扩展在当前会话加载失败时，composer 显示一行提示。</p><h2 id="_5-api-支持矩阵-v1" tabindex="-1">5. API 支持矩阵（v1） <a class="header-anchor" href="#_5-api-支持矩阵-v1" aria-label="Permalink to &quot;5. API 支持矩阵（v1）&quot;">​</a></h2><p>每个 <code>ExtensionAPI</code> 成员恰好落入一个类别。不支持的成员仍存在于对象上，不做 任何事，返回文档规定的中性值，并按扩展、按成员各产生一条诊断。它们永不抛出， 因此只使用受��持成员的扩展即使同时触碰了不支持的成员也能工作。</p><table tabindex="0"><thead><tr><th>类别</th><th>成员</th></tr></thead><tbody><tr><td>支持</td><td><code>registerTool</code>、<code>registerCommand</code>、§6 中每个事件的 <code>on(...)</code>、<code>exec</code>、<code>getActiveTools</code>、<code>getAllTools</code>、<code>setActiveTools</code>、<code>getCommands</code>、<code>setModel</code>（v1 说明：返回 <code>false</code>，桌面拥有会话的 provider 绑定）、<code>getThinkingLevel</code>、<code>setThinkingLevel</code>、<code>setSessionName</code>、<code>getSessionName</code>、<code>sendUserMessage</code>（Host 队列，D386）、<code>getFlag</code></td></tr><tr><td>上下文上支持</td><td><code>ui.notify</code>、<code>ui.confirm</code>、<code>ui.select</code>、<code>ui.input</code>、<code>ui.setStatus</code>、<code>ui.setWorkingMessage</code>、<code>cwd</code>、<code>modelRegistry</code>、<code>isIdle</code>、<code>abort</code>、<code>hasPendingMessages</code>、<code>getContextUsage</code>、<code>compact</code>、<code>getSystemPrompt</code>、<code>waitForIdle</code>、<code>newSession</code>、<code>fork</code></td></tr><tr><td>推迟到 v2</td><td><code>sendMessage</code>、<code>appendEntry</code>、<code>setLabel</code>、<code>sessionManager</code> 只读 API、<code>switchSession</code>、<code>registerShortcut</code>、<code>registerMarkdownTransformer</code>、<code>ui.setEditorText</code>、<code>ui.getEditorText</code>、<code>ui.addAutocompleteProvider</code>、<code>registerFlag</code> 值编辑</td></tr><tr><td>不支持</td><td><code>ui.setWidget</code>、<code>ui.setFooter</code>、<code>ui.setHeader</code>、<code>ui.setTitle</code>、<code>ui.custom</code>、<code>ui.overlay</code>、<code>ui.onTerminalInput</code>、<code>ui.setWorkingVisible</code>、<code>ui.setWorkingIndicator</code>、<code>ui.setHiddenThinkingLabel</code>、<code>ui.pasteToEditor</code>、<code>ui.editor</code>、<code>registerMessageRenderer</code>、<code>registerEntryRenderer</code>、<code>navigateTree</code>、<code>shutdown</code></td></tr></tbody></table><p>中性值：<code>getFlag</code> 返回声明的默认值；<code>registerFlag</code> 记录声明使 <code>getFlag</code> 可用， 但 v1 不暴露 CLI 或 UI；<code>sessionManager</code> 访问器返回空结果；UI setter 返回空操作 的 <code>dispose</code>。</p><h2 id="_6-事件映射" tabindex="-1">6. 事件映射 <a class="header-anchor" href="#_6-事件映射" aria-label="Permalink to &quot;6. 事件映射&quot;">​</a></h2><p>事件从桌面运行时现有的 hook 点触发。凡事件类型定义了返回结果的，处理器结果 均被采纳。</p><table tabindex="0"><thead><tr><th>事件</th><th>桌面 hook 点</th><th>是否采纳结果</th></tr></thead><tbody><tr><td><code>session_start</code>、<code>session_shutdown</code></td><td>Runner 创建与销毁</td><td>否</td></tr><tr><td><code>session_info_changed</code></td><td>经 <code>setSessionName</code> 的会话改名</td><td>否</td></tr><tr><td><code>project_trust</code></td><td>v1 说明：不触发；按项目启用即信任决定</td><td>否</td></tr><tr><td><code>resources_discover</code></td><td>v1 说明：不触发；skills 与提示发现留在 Electron main</td><td>不适用</td></tr><tr><td><code>before_agent_start</code></td><td>回合内首个 provider 请求之前</td><td>是，仅替换系统提示词</td></tr><tr><td><code>context</code></td><td><code>prepareNextTurn</code></td><td>是，替换消息列表</td></tr><tr><td><code>before_provider_request</code>、<code>before_provider_headers</code>、<code>after_provider_response</code></td><td>provider 调用包装</td><td>请求采纳返回值；头部原地修改 payload</td></tr><tr><td><code>agent_start</code>、<code>agent_end</code>、<code>agent_settled</code></td><td>Agent 循环边界</td><td>否</td></tr><tr><td><code>turn_start</code>、<code>turn_end</code></td><td>回合边界</td><td>否</td></tr><tr><td><code>message_start</code>、<code>message_update</code>、<code>message_end</code></td><td>Agent 消息事件</td><td>v1 说明：否，pi-agent-core 不提供事后替换</td></tr><tr><td><code>tool_call</code></td><td><code>beforeToolCall</code></td><td>是，可带理由阻止</td></tr><tr><td><code>tool_execution_start</code>、<code>tool_execution_update</code>、<code>tool_execution_end</code></td><td>工具执行流</td><td>否</td></tr><tr><td><code>tool_result</code></td><td><code>afterToolCall</code></td><td>是，替换结果</td></tr><tr><td><code>model_select</code>、<code>thinking_level_select</code></td><td>v1 说明：不触发；绑定变更会重建运行时</td><td>否</td></tr><tr><td><code>session_before_compact</code>、<code>session_compact</code>、<code>session_compact_failed</code></td><td>压缩流水线</td><td><code>session_before_compact</code> 为是</td></tr><tr><td><code>session_before_fork</code></td><td>v1 说明：不触发；fork 在 Electron main 执行</td><td>不适用</td></tr><tr><td><code>input</code></td><td>v1 说明：不触发；Host 队列准入尚未接入</td><td>不适用</td></tr><tr><td><code>user_bash</code>、<code>session_before_switch</code>、<code>session_before_tree</code>、<code>session_tree</code>、<code>ui_prompt_start</code>、<code>ui_prompt_end</code></td><td>v1 不触发</td><td>不适用</td></tr></tbody></table><p>Desktop 事件能力由 <code>packages/agent-runtime/src/extensions/event-capabilities.ts</code> 维护，分为返回值、原地修改、通知和未接通。未接通事件仍可注册，但会在现有插件诊断中 显示 <code>unsupported_api</code>，不妨碍其他已支持的处理器加载。</p><p>所有事件处理器（包括启动、关闭和通知）均有每个处理器 30 秒的等待上限。模块加载和 工厂初始化分别有 30 秒上限，失败归入加载或工厂诊断。处理器异常或超时记诊断并视为 返回 <code>undefined</code>，后续处理器按注册顺序继续。既有结果归并和失败继续策略保持不变， 不能将其作为强制安全检查。多个挂起处理器可能分别耗尽各自的时间预算。</p><p>中止会使等待中的事件派发失效。销毁先拒绝新派发并取消已有等待，再执行关闭处理器； 并发销毁只关闭一次。旧派发不返回结果、不再执行剩余处理器，迟到的完成或异常不会覆盖 结果或增加诊断。销毁后完成的工厂不能发布工具和命令。Runtime 在等待扩展关闭前先停止 Agent 工作。在请求前 hook 等待期间停止，不会继续请求模型，并保留用户消息；之后可正常 发送下一条消息。</p><p>每次调用拥有独立的 <code>ctx.signal</code>，完成、超时、Stop 或销毁后失效。旧回调再调用 SDK 会被拒绝，包括等待空闲、创建会话、fork 和发送消息的后续步骤。已提交给 Host 的事务 不回滚，但迟到返回不再触发队列优先级更新或修改 Runtime 模型状态。命令与工具不套用 事件的 30 秒上限，可运行至完成、传入信号取消、Stop 或销毁；迟到的工具进度和结果被丢弃。 已采纳的工具进度和结果会先复制再发布，扩展之后的原地修改不能改写它们。<code>pi.exec</code> 创建的进程树随作用域退出或显式超时终止，销毁等待已登记进程清理并报告失败。 主动逃离进程组或通过 Node API 直接创建的进程不在此所有权范围。</p><p>带返回值的 Hook 输入输出使用独立副本，头部修改仅在处理器及时成功后提交。 迟到的原地修改不会影响宿主或下一处理器。UI 请求按请求 ID、会话和扩展身份取消， 排队请求被丢弃，已显示的弹窗向渲染层发送精确退役通知；旧取消不会关闭新请求。</p><p>这些是协作式生命周期约束，不是强制执行隔离：可信代码仍可同步阻塞 JS 或直接使用 Node API 产生外部副作用。Native Pi 会话由上游 SDK 管理，不属于本次 Desktop 变更。 事件处理器等待 UI 提示时也受 30 秒限制， UI broker 自身的提示超时不会延长该预算。</p><h2 id="_7-工具" tabindex="-1">7. 工具 <a class="header-anchor" href="#_7-工具" aria-label="Permalink to &quot;7. 工具&quot;">​</a></h2><ol><li>注册的工具以其声明名称加入会话工具目录。与核心工具、插件工具或用户 MCP 工具同名的注册被拒绝并记诊断；先注册者胜出。</li><li>扩展工具是非核心工具：与插件工具遵循相同的模式门控和 ToolSearch 延迟。它们 在 Agent 模式可用，其他模式遵循现有的按模式白名单。</li><li>执行在 sidecar 内按 <code>ExtensionAPI</code> 的 <code>execute</code> 签名进行。不弹出宿主权限提示；信任决定已 在启用时做出。<code>onUpdate</code> 流映射到工具执行更新事件。</li><li>每次执行写一条审计记录，含扩展 id、工具名和耗时。不记录参数。</li><li><code>exec</code> 在 sidecar 内以会话工作目录、会话代理和环境设置运行。</li></ol><h2 id="_8-命令" tabindex="-1">8. 命令 <a class="header-anchor" href="#_8-命令" aria-label="Permalink to &quot;8. 命令&quot;">​</a></h2><ol><li><code>registerCommand</code> 条目出现在全局搜索的 Commands 区（见 <a href="/zh-CN/spec/07-plugins/09-plugin-command-palette">09-plugin-command-palette.md</a>）， 形式为 <code>/&lt;name&gt;</code>，来源显示扩展标签，排在内置和插件命令之后。</li><li>命令在 sidecar 内运行，扩展命令上下文绑定到当前会话。它需要一个在本次应用 运行中已加载扩展的活动会话；否则 composer 提示需先开始对话。</li><li>composer 中输入的 <code>/&lt;name&gt;</code> 按此顺序解析：内置、提示模板、插件、扩展。冲突 记为诊断。</li><li>运行中的命令与插件命令一样阻止 composer 提交，可从状态栏取消。</li></ol><h2 id="_9-ui-桥接" tabindex="-1">9. UI 桥接 <a class="header-anchor" href="#_9-ui-桥接" aria-label="Permalink to &quot;9. UI 桥接&quot;">​</a></h2><p>交互式上下文调用经 sidecar → Electron main → 渲染层往返。</p><table tabindex="0"><thead><tr><th>调用</th><th>渲染层界面</th><th>超时</th><th>中止时</th></tr></thead><tbody><tr><td><code>ui.notify</code></td><td>Toast</td><td>无</td><td>丢弃</td></tr><tr><td><code>ui.confirm</code></td><td>双动作模态框</td><td>5 分钟</td><td>解析为 <code>false</code></td></tr><tr><td><code>ui.select</code></td><td>模态列表</td><td>5 分钟</td><td>解析为 <code>undefined</code></td></tr><tr><td><code>ui.input</code></td><td>模态文本框</td><td>5 分钟</td><td>解析为 <code>undefined</code></td></tr><tr><td><code>ui.setStatus</code>、<code>ui.setWorkingMessage</code></td><td>当前会话的浮动状态行（v1 说明：不在 composer 内）</td><td>无</td><td>清空</td></tr></tbody></table><p>规则：</p><ul><li>每会话同一时刻只有一个待处理交互提示。第二个调用排在第一个之后。</li><li>中止回合时以上述中止值取消待处理提示。</li><li>远程控制（MVP 后）下提示立即以 <code>UNSUPPORTED</code> 失败，直到远程协议路由它；该 路由属于 v3。</li><li>提示显示扩展标签和来源路径，让用户知道是谁在询问。</li></ul><h2 id="_10-协议与-ipc-新增" tabindex="-1">10. 协议与 IPC 新增 <a class="header-anchor" href="#_10-协议与-ipc-新增" aria-label="Permalink to &quot;10. 协议与 IPC 新增&quot;">​</a></h2><p>v1 不改任何 host-core RPC 方法、协议版本或 SQLite schema。</p><h3 id="_10-1-sidecar-→-main-host-proxy-白名单" tabindex="-1">10.1 sidecar → main（host.proxy 白名单） <a class="header-anchor" href="#_10-1-sidecar-→-main-host-proxy-白名单" aria-label="Permalink to &quot;10.1 sidecar → main（host.proxy 白名单）&quot;">​</a></h3><table tabindex="0"><thead><tr><th>方法</th><th>用途</th></tr></thead><tbody><tr><td><code>extensions.commands.publish</code></td><td>替换会话已注册的命令列表</td></tr><tr><td><code>extensions.ui.request</code></td><td>§9 中的一次交互或状态调用</td></tr><tr><td><code>extensions.diagnostics.publish</code></td><td>替换会话的诊断列表</td></tr><tr><td><code>extensions.model.configure</code></td><td>校验插件自有的 provider/模型绑定，经 <code>session.configure</code> 持久化，然后广播 <code>session:modelChanged</code></td></tr><tr><td><code>session.rename</code>、<code>session.create</code>、<code>session.fork</code>、<code>session.queuePush</code>、<code>session.queuePrioritize</code></td><td>已有方法，现可从适配层到达</td></tr></tbody></table><h3 id="_10-2-main-↔-渲染层-electron-ipc" tabindex="-1">10.2 main ↔ 渲染层（Electron IPC） <a class="header-anchor" href="#_10-2-main-↔-渲染层-electron-ipc" aria-label="Permalink to &quot;10.2 main ↔ 渲染层（Electron IPC）&quot;">​</a></h3><table tabindex="0"><thead><tr><th>通道</th><th>方向</th><th>用途</th></tr></thead><tbody><tr><td><code>plugin/importExtension</code></td><td>请求</td><td>原生选择器、生成插件、注册为开发插件</td></tr><tr><td><code>extensions/commands/run</code></td><td>请求</td><td>在当前会话运行已注册命令</td></tr><tr><td><code>extensions/ui/respond</code></td><td>请求</td><td>回答一个待处理提示</td></tr><tr><td><code>extensions/ui/prompt</code></td><td>事件</td><td>有提示待处理</td></tr><tr><td><code>extensions/event/status</code></td><td>事件</td><td><code>ui.setStatus</code> / <code>ui.setWorkingMessage</code> 文本变化</td></tr><tr><td><code>plugin/list</code></td><td>请求</td><td>插件行携带 <code>agentExtension</code> 状态、工具与命令名和诊断</td></tr><tr><td><code>event/pluginChanged</code></td><td>事件</td><td>会话发布命令或诊断时同样触发</td></tr></tbody></table><p>所有通道像其他插件通道一样做 sender 校验。MCP 控制面暴露 <code>extensions/commands/run</code> （写）和 <code>extensions/ui/respond</code>（危险，需 confirm）；导入是原生选择器，保持本地。 main 在 <code>logs/app/plugin.log</code> 审计每个提示 id。</p><h2 id="_11-插件行界面" tabindex="-1">11. 插件行界面 <a class="header-anchor" href="#_11-插件行界面" aria-label="Permalink to &quot;11. 插件行界面&quot;">​</a></h2><p>插件页在所属插件的行上展示 agent 扩展：</p><ul><li><code>agentExtension</code> 能力标记和 <code>agent.extension</code> 权限标记（高风险），与其他能力和权限 并列。</li><li>详情区含状态标记（<code>enabled</code> 直到本次应用运行中有会话加载模块、<code>loaded</code>、<code>error</code>）、 已注册的工具与斜杠命令名，以及诊断：加载错误、带计数的不支持 API 调用、被拒绝的 注册、处理器超时。</li><li>页面溢出菜单中的“导入 pi 扩展”，前置一个说明授权含义的确认。</li></ul><h2 id="_12-分阶段" tabindex="-1">12. 分阶段 <a class="header-anchor" href="#_12-分阶段" aria-label="Permalink to &quot;12. 分阶段&quot;">​</a></h2><table tabindex="0"><thead><tr><th>阶段</th><th>内容</th><th>承诺</th></tr></thead><tbody><tr><td>v1</td><td>loader、每会话 Runner、支持矩阵、事件、工具、命令、UI 桥接</td><td>已交付（D387）</td></tr><tr><td>v1.1</td><td>模块成为带 <code>agent.extension</code> 授权的 <code>contributes.agentExtensions</code>；把 pi CLI 扩展导入为开发插件；独立注册表和设置标签移除</td><td>已交付（D388）</td></tr><tr><td>v2</td><td>自定义会话条目（<code>sendMessage</code>、<code>appendEntry</code>）含 schema 升版和通用渲染、<code>sessionManager</code> 只读 shim、<code>switchSession</code>、编辑器读写、补全 provider、<code>registerShortcut</code>、markdown 转换器</td><td>已规划，需先决定条目持久化与压缩</td></tr><tr><td>v2</td><td>自定义会话条目（<code>sendMessage</code>、<code>appendEntry</code>）与一次 schema 升级及通用渲染层、<code>sessionManager</code> 只读 shim、<code>switchSession</code>、编辑器读写、自动补全 provider、<code>registerShortcut</code>、markdown 转换器</td><td>计划中，需要就条目持久化与压缩作出决定</td></tr><tr><td>v3</td><td><code>pi</code> 包 manifest 与安装、pi CLI <code>settings.json</code> 的只读提示、统一 skill 与提示发现、提示的远程控制路由、市场列出</td><td>未排期</td></tr></tbody></table><p>v1 交付顺序：打包 spike（E2E-245）、shared 协议类型，然后运行时、main、渲染层 三条线并行。</p><h2 id="_13-版本策略" tabindex="-1">13. 版本策略 <a class="header-anchor" href="#_13-版本策略" aria-label="Permalink to &quot;13. 版本策略&quot;">​</a></h2><ul><li>升级任一 pi 包即同时升级三个包。</li><li>一组覆盖每个受支持成员的样例扩展在每次升级时作为契约测试运行。</li><li>新增的 <code>ExtensionAPI</code> 成员先落入“不支持”类别并产生诊断，直到后续决策 移动它们。</li><li>对外文档只承诺 §5 中“支持”和“上下文上支持”两个类别。</li></ul><h2 id="_14-待决事项" tabindex="-1">14. 待决事项 <a class="header-anchor" href="#_14-待决事项" aria-label="Permalink to &quot;14. 待决事项&quot;">​</a></h2><table tabindex="0"><thead><tr><th>问题</th><th>决定前的默认</th></tr></thead><tbody><tr><td>v2 自定义条目是否持久化到 host-core 并参与压缩？</td><td>持久化；不进入压缩摘要</td></tr><tr><td>v3 是否把 pi CLI <code>settings.json</code> 的启用路径作为发现提示读取？</td><td>只读提示，永不写入</td></tr><tr><td>扩展工具是否像插件工具一样按项目可选？</td><td>§3.2 的范围是唯一门控</td></tr></tbody></table></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("zh-CN/spec/07-plugins/16-trusted-extensions.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _16TrustedExtensions = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _16TrustedExtensions as default
};
