import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0215: Agent extensions are a plugin contribution","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0215-agent-extensions-as-plugin-contribution.md","filePath":"adr/0215-agent-extensions-as-plugin-contribution.md","lastUpdated":1789330443000}');
const _sfc_main = { name: "adr/0215-agent-extensions-as-plugin-contribution.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0215-agent-extensions-are-a-plugin-contribution" tabindex="-1">ADR 0215: Agent extensions are a plugin contribution <a class="header-anchor" href="#adr-0215-agent-extensions-are-a-plugin-contribution" aria-label="Permalink to &quot;ADR 0215: Agent extensions are a plugin contribution&quot;">​</a></h1><ul><li>Status: Accepted (implemented 2026-09-11)</li><li>Date: 2026-09-11</li><li>Decision: D388 (amends D387 / ADR 0214)</li><li>Related: ADR 0008, ADR 0214, <code>07-plugins/16-trusted-extensions.md</code>, <code>07-plugins/02-plugin-manifest-schema.md</code> §4, <code>07-plugins/13-plugin-permissions-matrix.md</code></li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>ADR 0214 delivered ExtensionAPI modules as a separate surface next to plugins: its own discovery over <code>~/.pi/agent/extensions</code>, its own enablement store, its own Settings destination. That gave users two lists that mean &quot;extend the agent&quot; and left pi CLI extensions outside the plugin lifecycle (install, scope, update, remove, marketplace). The maintainer&#39;s requirement is one surface: a pi CLI extension is converted into a PI-Desktop plugin and used as one.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><strong><code>contributes.agentExtensions</code> + <code>agent.extension</code>.</strong> A plugin lists ExtensionAPI modules in its manifest and must hold the <code>agent.extension</code> permission (high risk, explicit confirmation). Validation rejects the contribution without the permission; a recorded grant list that omits it skips the modules with an audit entry. The modules run in the Agent sidecar exactly as before; the plugin sandbox does not cover them, which the permission text states.</li><li><strong>Plugin lifecycle owns enablement and scope.</strong> Enabling, disabling, per-project activation, reload, and removal are the plugin&#39;s. The sidecar receives the modules of enabled plugins active in the session&#39;s project.</li><li><strong>Import generates a plugin.</strong> &quot;Import pi extension&quot; copies the chosen file or directory into <code>&lt;dataDir&gt;/plugins/imported/&lt;slug&gt;/src</code>, writes a manifest with the contribution and the permission, and registers it as a development plugin. The confirm before the picker is the trust decision.</li><li><strong>The standalone surface is removed.</strong> No registry file, no Settings destination, no list / enable / rescan / add-path / remove IPC. The plugin row shows the capability chip, the permission chip, the load state, the registered tool and command names, and the diagnostics.</li><li><strong>Marketplace distribution waits for signing.</strong> <code>agent.extension</code> is accepted from local imports and development plugins in v1.1.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>One mental model for users: extensions are plugins. Issue 183&#39;s ecosystem gap closes with &quot;import as a plugin&quot;.</li><li>The sidecar engine (loader, Runner, hooks, command and prompt bridges) is untouched; only the ownership layer moved.</li><li>Sandbox level is unchanged and now explicit as a permission. A sandboxed variant that runs modules in the plugin host process remains a v2 option.</li><li>The v1 harness covers plugin-form fixtures and the importer/runtime contracts; the native picker, npm install, and provider-turn journey remain separate environment-dependent validation.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li>Keep two surfaces and cross-link them. Rejected by the maintainer.</li><li>Source-convert pi extensions into plugin main modules. Rejected: the ExtensionAPI semantics (in-process hooks, tool execution beside the agent) do not map onto the sandboxed plugin API.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0215-agent-extensions-as-plugin-contribution.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0215AgentExtensionsAsPluginContribution = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0215AgentExtensionsAsPluginContribution as default
};
