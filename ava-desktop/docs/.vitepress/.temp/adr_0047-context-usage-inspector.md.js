import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0047: Context usage inspector with exact and estimated token sources","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0047-context-usage-inspector.md","filePath":"adr/0047-context-usage-inspector.md","lastUpdated":1788937388000}');
const _sfc_main = { name: "adr/0047-context-usage-inspector.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0047-context-usage-inspector-with-exact-and-estimated-token-sources" tabindex="-1">ADR 0047: Context usage inspector with exact and estimated token sources <a class="header-anchor" href="#adr-0047-context-usage-inspector-with-exact-and-estimated-token-sources" aria-label="Permalink to &quot;ADR 0047: Context usage inspector with exact and estimated token sources&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-08-02</li><li>Amended by: D225 (click-toggled panel), D244 (compact summary presentation), D347 (composer toolbar placement), and D355 (last-request occupancy)</li><li>Related: <a href="./../spec/08-meta/decisions-log">D103</a> · <a href="./../spec/08-meta/decisions-log">D183</a> · <a href="./../spec/08-meta/decisions-log">D184</a> · <a href="./../spec/03-runtime/01-ipc-protocol">IPC protocol</a> · <a href="./../spec/04-ux/08-component-spec">Component spec</a></li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The first context-usage ring exposed only a percentage and a small aggregate breakdown. It did not answer why a turn became large, which tool contributed the most context, or how quickly the model generated its output. The runtime already has provider usage, tool arguments/results, and a model stream timing anchor, but providers do not expose exact per-tool allocation.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>Replace the standalone ring with a compact Codex-style context inspector trigger. <em>(Amended by D225: hover and focus no longer open the panel; clicking or keyboard-activating the trigger toggles it.)</em> Activating the trigger reveals one scrollable, non-modal panel.</li><li>Keep provider-reported input/output/cache/reasoning usage authoritative and show aggregate output throughput as <code>outputTokens / responseDurationMs</code> on completed turns only; active streams do not render a live estimate.</li><li>Estimate each tool&#39;s argument and result footprint with the existing pi-agent-core token heuristic. Persist the estimate on the tool message, mark it as estimated in the UI, and never add it to the provider total.</li><li>Carry <code>responseDurationMs</code> on assistant messages and <code>toolUsage</code> on tool messages. Add <code>toolUsage</code> to <code>tool_end</code> as an optional event field so live, persisted, and restored transcripts use the same values.</li><li>Preserve additive compatibility: missing fields use the renderer fallback estimate or omit throughput when no stream duration is available.</li><li>Render the inspector panel through <code>document.body</code> as a fixed viewport overlay. Measure the trigger and panel rectangles, choose the side with available space, clamp the result to a viewport margin, and update the placement on transcript scroll, window resize, or panel-size changes. Keep the open/close and Escape dismissal behavior independent from the transcript scroll container.</li><li>Aggregate repeated tool rows by their exact tool name, preserving the first-seen order. Each row shows the number of calls and sums argument, result, token, and known duration estimates across those calls.</li><li>Resolve the context-window total from the same <code>pi-ai</code> model record that Electron passes to the agent sidecar, and enrich cached/discovered model rows with that value. Use provider metadata and the 128K default only when the selected model is absent from the <code>pi-ai</code> catalog.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Users can inspect exact model usage and see each tool type&#39;s relative context footprint without opening logs, while repeated calls stay compact and auditable through their call counts.</li><li>Historical tool rows remain useful through a deterministic fallback estimate.</li><li>Tool estimates are transparent but cannot claim billing precision.</li><li>The transcript protocol and storage shape gain optional fields, while the existing protocol version remains compatible.</li><li>The panel is no longer constrained by the transcript&#39;s overflow clipping or stacking context; collision-aware placement keeps its complete contents visible at viewport edges.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><ul><li>Keep the ring-only UI: rejected because it hides the source of large turns.</li><li>Claim exact per-tool provider usage: rejected because the provider response does not contain that attribution.</li><li>Calculate throughput from wall-clock turn time: rejected because tool wait and provider wait would distort model generation speed.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0047-context-usage-inspector.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0047ContextUsageInspector = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0047ContextUsageInspector as default
};
