import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0162: Cross-session A2A addressing","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0162-cross-session-a2a-addressing.md","filePath":"adr/0162-cross-session-a2a-addressing.md","lastUpdated":1788625977000}');
const _sfc_main = { name: "adr/0162-cross-session-a2a-addressing.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0162-cross-session-a2a-addressing" tabindex="-1">ADR 0162: Cross-session A2A addressing <a class="header-anchor" href="#adr-0162-cross-session-a2a-addressing" aria-label="Permalink to &quot;ADR 0162: Cross-session A2A addressing&quot;">​</a></h1><ul><li>Status: Superseded by ADR 0165</li><li>Date: 2026-09-05</li><li>Deciders: PI-Desktop core</li><li>Related: D318, D277, ADR 0147, ADR 0062, ADR 0089, <code>03-runtime/02-agent-runtime.md</code> §5f.2, <code>03-runtime/06-host-rpc-protocol.md</code> §4, E2E-165, E2E-165b, E2E-165c</li><li>Amends: ADR 0147. The local A2A broker, capability-token auth, typed parts, durable task lifecycle, and parent-cannot-call-A2A boundary are unchanged. Same-context-only addressing is lifted.</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>ADR 0147 bound every A2A agent to <code>contextId = sessionId</code> and refused cross-context discovery, send, and task access with <code>A2A_CROSS_CONTEXT_DENIED</code>. That matched the in-process mailbox it replaced, which was session-scoped by construction.</p><p>The broker itself is already process-global: one in-memory registry in host-core, one sidecar JSON-RPC pipe, and one <code>DesktopAgentRuntime</code> per session. Agents from different sessions already sit in the same map; isolation was a filter. Concurrent work in two open sessions therefore cannot share a fact, a file claim, or a roundtable seat even though both delegates are local to the same host.</p><p>The remaining risk is identity, not transport. Peer ids are unique per session (<code>discussant</code>, <code>discussant-2</code>) but not across sessions, so two chats can both run <code>discussant</code>. Tasks store <code>agentName</code> / <code>requesterName</code> as those ids, and the sidecar broadcasts <code>a2a.task.event</code> to every session runtime. A naive lift of the filter would let the wrong <code>discussant</code> read a task or wake on the other session&#39;s event.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Allow A2A discovery and addressing across sessions on the same host.</p><ol><li><strong><code>a2a.agents.list</code> returns every other live agent</strong>, not only the caller&#39;s <code>contextId</code>. Each Agent Card carries <code>contextId</code> (the session id it registered under) so a delegate can tell same-session peers from other-session peers. The caller&#39;s own card stays excluded.</li><li><strong><code>a2a.message.send</code> may address any registered peer.</strong> When <code>to</code> is omitted, the broker still prefers a same-session peer (preserving today&#39;s single-other-peer default) and only then a unique other-session peer.</li><li><strong>Task access is membership, not context.</strong> The caller must be the task&#39;s <code>requesterName</code> or <code>agentName</code>. A stranger — same session or not — still fails with <code>A2A_UNKNOWN_AGENT</code>. <code>A2A_CROSS_CONTEXT_DENIED</code> is kept in the error-code list for wire compatibility and is no longer produced. <code>contextId</code> on the task remains the requester&#39;s session id and still caps <code>A2A_MAX_TASKS_PER_CONTEXT</code>.</li><li><strong>Live peer ids are unique across the registry.</strong> On <code>a2a.agents.register</code>, if <code>card.name</code> is already taken the broker suffixes it (<code>discussant-2</code>, …) and returns the uniquified <code>agentId</code>. The runtime adopts that id for the delegate&#39;s A2A tool, wait queue, and prompt, so <code>agentName</code> / <code>requesterName</code> on a task cannot collide with another live agent.</li><li><strong>Events carry <code>recipientContextId</code>.</strong> <code>a2a.task.event</code> and <code>a2a.push</code> are shaped <code>{ recipient, recipientContextId, contextId, … }</code>. Each session runtime delivers an event only when <code>recipientContextId</code> equals its <code>sessionId</code> (an omitted field keeps today&#39;s same-session delivery). This stops a broadcast on the shared sidecar pipe from queueing work for the wrong session.</li><li><strong>Unchanged boundaries (amended by ADR 0164).</strong> A settled delegate is deregistered. There is still no nested delegation and no remote or cross-machine transport. Parent-to-parent A2A is ADR 0164; parents still cannot address subagents.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Two A2A-capable delegates in different open sessions can discover each other, create a durable task, and complete the counterpart round-trip.</li><li>Same-session A2A is unchanged for the common case: unique names, omitted <code>to</code> picks the other local peer, events still wake the local waiter.</li><li>A definition named <code>discussant</code> in a second session may register as <code>discussant-2</code> when the first session already holds <code>discussant</code>. The delegate is told its assigned peer id.</li><li><code>A2A_CROSS_CONTEXT_DENIED</code> remains a documented code but is unused. New cross-session failures are <code>A2A_UNKNOWN_AGENT</code>, <code>A2A_UNKNOWN_TASK</code>, or <code>A2A_NO_PEERS</code>.</li><li>Not addressed: nested delegation and any network A2A binding. Parent-to- parent A2A is decided in ADR 0164. Name reuse after deregister (a later <code>discussant</code> reading a stale task that still names <code>discussant</code>) is the same intra-session hazard as today.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Keep same-session isolation:</strong> rejected. The user-visible request is inter-session coordination, and the broker is already global; the deny was a leftover mailbox boundary, not a transport limit.</li><li><strong>Address by <code>(name, contextId)</code> without uniquifying names:</strong> rejected. Tasks persist <code>agentName</code> / <code>requesterName</code> as strings; two live <code>bob</code> agents would both pass membership checks. Uniquifying at register keeps that contract.</li><li><strong>Qualify stored names as <code>name@contextId</code>:</strong> rejected. It would leak session ids into every task summary the model reads, and would still need event filtering on the shared pipe.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0162-cross-session-a2a-addressing.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0162CrossSessionA2aAddressing = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0162CrossSessionA2aAddressing as default
};
