import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0078: Cross-platform tray-resident minimize","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0078-cross-platform-tray-resident-minimize.md","filePath":"adr/0078-cross-platform-tray-resident-minimize.md","lastUpdated":1789293028000}');
const _sfc_main = { name: "adr/0078-cross-platform-tray-resident-minimize.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0078-cross-platform-tray-resident-minimize" tabindex="-1">ADR 0078: Cross-platform tray-resident minimize <a class="header-anchor" href="#adr-0078-cross-platform-tray-resident-minimize" aria-label="Permalink to &quot;ADR 0078: Cross-platform tray-resident minimize&quot;">​</a></h1><ul><li>Status: Accepted for implementation (amended by ADR 0117, ADR 0123, and ADR tray-session-shortcuts)</li><li>Date: 2026-08-12</li><li>Deciders: PI-Desktop core</li><li>Related: D216, D252, D256, E2E-124, ADR 0117, ADR 0123</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>PI-Desktop already has custom window controls on Windows/Linux and native traffic-light controls on macOS. Their minimize actions currently use native window minimization, which makes the app disappear into different OS window surfaces and does not provide a consistent way to keep background work resident. The Electron Main process already owns window lifecycle and shutdown, so a tray integration belongs there rather than in the renderer bridge.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>Electron Main creates one tray icon on every supported desktop platform. Packaged builds carry the existing product PNG as an extra resource for Windows/Linux. macOS carries a separate transparent monochrome template asset derived from the PI mark; the light application tile is not part of the menu bar silhouette.</li><li>Main intercepts the <code>minimize</code> event for the main window and hides it to the tray only for macOS traffic-light minimization. On Windows/Linux, native minimize transitions — including the renderer and native-menu actions — complete as ordinary OS minimization so the taskbar entry remains available. Hiding does not close the window or dispose either backend.</li><li>Tray click and double-click, the Show menu item, and macOS app activation restore and focus the existing window. If the window was closed, they create a new one through the existing window factory.</li><li>The tray menu contains localized Show PI-Desktop and Quit PI-Desktop items. Quit calls <code>app.quit()</code> and therefore follows the existing <code>before-quit</code> shutdown sequence. Closing the main window remains an explicit quit action.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>macOS keeps its tray-resident minimize behavior, while Windows/Linux use the normal taskbar minimize/restore affordance for explicit minimize actions. Close-to-tray remains available through the user-configurable close behavior.</li><li>The renderer needs no new privileged IPC surface.</li><li>A tray icon is a required packaged resource; a missing icon is logged and the app remains usable rather than crashing during boot.</li><li>Window bounds persistence remains unchanged because a hidden window retains its normal bounds and is not treated as a new window state.</li></ul><h2 id="session-menu-amendment" tabindex="-1">Session-menu amendment <a class="header-anchor" href="#session-menu-amendment" aria-label="Permalink to &quot;Session-menu amendment&quot;">​</a></h2><p><a href="./tray-session-shortcuts">ADR tray-session-shortcuts</a> extends clauses 3–4 with bounded session groups and makes macOS single-click open the menu without restoring the main window. Open, double-click, and Quit retain their lifecycle behavior.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0078-cross-platform-tray-resident-minimize.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0078CrossPlatformTrayResidentMinimize = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0078CrossPlatformTrayResidentMinimize as default
};
