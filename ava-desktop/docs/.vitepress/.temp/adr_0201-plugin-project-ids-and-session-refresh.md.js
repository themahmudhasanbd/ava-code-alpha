import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0201: Explicit Plugin Project IDs and Host-Owned Session Refresh","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0201-plugin-project-ids-and-session-refresh.md","filePath":"adr/0201-plugin-project-ids-and-session-refresh.md","lastUpdated":1788959642000}');
const _sfc_main = { name: "adr/0201-plugin-project-ids-and-session-refresh.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0201-explicit-plugin-project-ids-and-host-owned-session-refresh" tabindex="-1">ADR 0201: Explicit Plugin Project IDs and Host-Owned Session Refresh <a class="header-anchor" href="#adr-0201-explicit-plugin-project-ids-and-host-owned-session-refresh" aria-label="Permalink to &quot;ADR 0201: Explicit Plugin Project IDs and Host-Owned Session Refresh&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-09</li><li>Decision: D368</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The P0/P1 plugin session API deliberately kept imported sessions unbound, but external-history importers need an explicit way to place a session under a durable project. The existing <code>workspace.set</code> method both creates a project and changes the active workspace, so exposing it to plugins would couple import to the user&#39;s current UI context. Plugin session writes also currently have no renderer notification path, leaving the sidebar stale until another refresh.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ul><li>Add the permission-gated <code>pi.project.create({ path })</code> API. It creates or reuses a durable host project row without activating the workspace and returns the host-generated <code>projectId</code>, canonical path, and display name.</li><li>Allow <code>projectId</code> on plugin import items. The id must already exist and the plugin must hold <code>project.create</code>; omitted ids preserve the safe unbound behavior. <code>projectPath</code>, <code>modelId</code>, and <code>providerId</code> remain historical origin metadata unless an explicit active binding is supplied.</li><li>Make plugin session list/get projections report the explicit project binding and id while retaining ownership filtering and all existing import limits.</li><li>Electron main emits one <code>pi-desktop/session/event/changed</code> event after each successful plugin import, batch import, rename, or delete. The renderer handles it through the existing <code>refreshSessions()</code> store action. Skipped imports do not emit the event, plugins never emit it, and a refresh never reopens a closed project tab.</li></ul><p>The host RPC remains additive to protocol v11; the new <code>projects.create</code> operation is only reachable from the permission-checked plugin bridge.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><p>Importers can explicitly opt into a project-backed session without silently activating a workspace or turning historical paths into tool roots. The host owns sidebar synchronization, so import plugins do not need a private event bridge. The project permission is high risk because an explicit project id is also a session tool-root authority; users must approve it at install time.</p><h2 id="verification" tabindex="-1">Verification <a class="header-anchor" href="#verification" aria-label="Permalink to &quot;Verification&quot;">​</a></h2><p>Coverage includes project creation without workspace switching, explicit and unknown project ids during import, bound list/get projections, independent project permission checks, and the host-to-renderer refresh event contract. E2E-216 records the full UI journey; local UI E2E remains deferred by policy.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0201-plugin-project-ids-and-session-refresh.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0201PluginProjectIdsAndSessionRefresh = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0201PluginProjectIdsAndSessionRefresh as default
};
