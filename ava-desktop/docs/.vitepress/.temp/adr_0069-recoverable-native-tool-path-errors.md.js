import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0069: Make native-tool path mistakes recoverable","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0069-recoverable-native-tool-path-errors.md","filePath":"adr/0069-recoverable-native-tool-path-errors.md","lastUpdated":1786725729000}');
const _sfc_main = { name: "adr/0069-recoverable-native-tool-path-errors.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0069-make-native-tool-path-mistakes-recoverable" tabindex="-1">ADR 0069: Make native-tool path mistakes recoverable <a class="header-anchor" href="#adr-0069-make-native-tool-path-mistakes-recoverable" aria-label="Permalink to &quot;ADR 0069: Make native-tool path mistakes recoverable&quot;">​</a></h1><ul><li>Status: Accepted for implementation</li><li>Date: 2026-08-10</li><li>Deciders: PI-Desktop core</li><li>Amends: D185, D192</li><li>Amended by: <a href="./0087-line-anchored-edit-contract">ADR 0087</a> (§2&#39;s byte-faithful <code>Read</code> content is replaced by line-numbered, tagged output)</li><li>Related: <a href="./../spec/03-runtime/02-agent-runtime">02-agent-runtime</a> · <a href="./../spec/03-runtime/03-tools-and-permissions">03-tools-and-permissions</a> · <a href="./../spec/04-ux/08-component-spec">08-component-spec §9</a> · E2E-019e · E2E-083 · E2E-095</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Recent durable sessions showed a repeated path-shape mismatch between the model and the native tools. <code>Read</code> was called with a directory after a guessed file name did not exist, and <code>Grep.path</code> was repeatedly given one explicit file even though the host accepted only directories. These were recoverable inspection mistakes, but they were reported as generic execution failures.</p><p>The transcript compounded the problem. A failed tool row correctly retained its own error, while the containing activity group also derived a terminal failure from any child error. A later successful search and final answer could therefore leave a completed turn labeled <code>Failed after ...</code>.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>Preserve D185&#39;s Agent core and deferred-tool boundary. <code>Glob</code> and <code>Grep</code> remain on demand; each new Agent prompt must activate <code>Glob</code> through <code>ToolSearch</code> when a directory must be listed or a file name is uncertain.</li><li>Enforce and advertise one portable path contract: <ul><li><code>Read.path</code> accepts an existing regular text file, never a directory.</li><li><code>Glob.path</code> accepts a directory.</li><li><code>Grep.path</code> accepts either one file or a directory tree. An explicit file is searched directly, and <code>include</code> still filters its basename.</li></ul></li><li>A directory passed to <code>Read</code> returns public <code>INVALID_ARGUMENT</code>, not <code>TOOL_FAILED</code>. Its result includes <code>suggestedTool: &quot;Glob&quot;</code> and bounded <code>suggestedArgs</code> with the original path and <code>pattern: &quot;**/*&quot;</code> so the model can correct the call without guessing.</li><li>Host definitions, runtime TypeBox schemas, main-Agent guidance, and subagent guidance carry the same path semantics. Subagents do not receive <code>ToolSearch</code>, so their guidance refers only to tools actually available to the definition.</li><li>A tool error remains visible and auto-expanded on its own ToolCallRow. The activity group represents processing duration and step containment only; it never infers terminal turn failure from child rows. Terminal agent errors, the TurnOutcomeCard, sidebar state, and notifications remain the single turn-outcome surfaces.</li></ol><p>No host protocol or storage-schema version changes. The existing tool result is already a structured JSON value, and the Grep input shape remains a string.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Common single-file Grep calls stop failing on a directory-only contract.</li><li>A mistaken directory Read receives an actionable, non-retriable correction rather than an ambiguous execution failure.</li><li>Recovered turns no longer look terminally failed, while the original failed call remains auditable in the expanded transcript.</li><li>Deferred search keeps the measured first-request context reduction from D185; the prompt must make the per-user-prompt activation boundary explicit.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><h3 id="put-glob-and-grep-back-in-the-agent-core" tabindex="-1">Put Glob and Grep back in the Agent core <a class="header-anchor" href="#put-glob-and-grep-back-in-the-agent-core" aria-label="Permalink to &quot;Put Glob and Grep back in the Agent core&quot;">​</a></h3><p>Rejected. It reverses D185&#39;s bounded first-request context decision, and the observed failure can be corrected without paying both schemas on every prompt.</p><h3 id="silently-execute-glob-when-read-receives-a-directory" tabindex="-1">Silently execute Glob when Read receives a directory <a class="header-anchor" href="#silently-execute-glob-when-read-receives-a-directory" aria-label="Permalink to &quot;Silently execute Glob when Read receives a directory&quot;">​</a></h3><p>Rejected. It would make the audited tool name disagree with the executed operation and return a result shape outside Read&#39;s contract.</p><h3 id="keep-grep-directory-only" tabindex="-1">Keep Grep directory-only <a class="header-anchor" href="#keep-grep-directory-only" aria-label="Permalink to &quot;Keep Grep directory-only&quot;">​</a></h3><p>Rejected. A direct file path is unambiguous, cheaper than walking its parent, and common across coding-agent tool conventions.</p><h3 id="mark-the-activity-group-failed-after-the-turn-settles" tabindex="-1">Mark the activity group failed after the turn settles <a class="header-anchor" href="#mark-the-activity-group-failed-after-the-turn-settles" aria-label="Permalink to &quot;Mark the activity group failed after the turn settles&quot;">​</a></h3><p>Rejected. Settlement does not mean every intermediate tool call succeeded; the dedicated terminal outcome surfaces already own turn failure.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0069-recoverable-native-tool-path-errors.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0069RecoverableNativeToolPathErrors = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0069RecoverableNativeToolPathErrors as default
};
