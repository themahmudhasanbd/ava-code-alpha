import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0037: Resolve project instructions in Electron main","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0037-project-instruction-chain.md","filePath":"adr/0037-project-instruction-chain.md","lastUpdated":1785652476000}');
const _sfc_main = { name: "adr/0037-project-instruction-chain.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0037-resolve-project-instructions-in-electron-main" tabindex="-1">ADR 0037: Resolve project instructions in Electron main <a class="header-anchor" href="#adr-0037-resolve-project-instructions-in-electron-main" aria-label="Permalink to &quot;ADR 0037: Resolve project instructions in Electron main&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-07-30</li><li>Related: <a href="./../spec/03-runtime/02-agent-runtime">Agent runtime</a></li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>PI-Desktop initially loaded only <code>&lt;project root&gt;/AGENTS.md</code> at runtime creation. That omits directory-specific rules in monorepos and provides no compatibility path for projects that use <code>CLAUDE.md</code>. Letting the model-driven agent sidecar scan the workspace would weaken the existing boundary that keeps workspace file access in Electron main and host-core.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>Electron main resolves instructions only inside the session-bound project root; it never follows an instruction file whose canonical path escapes that root.</li><li>The global file <code>~/.pi/agent/AGENTS.md</code> is loaded before all project entries. Settings manages this fixed path. A project&#39;s <code>AGENTS.md</code> is managed from that project&#39;s list-menu action in the Projects view; dedicated IPC accepts only a project root registered by host-core and never an arbitrary file path.</li><li>For every project directory, the first non-empty candidate wins in this order: <code>AGENTS.override.md</code>, <code>AGENTS.md</code>, <code>CLAUDE.md</code>, <code>.claude/CLAUDE.md</code>.</li><li>Sources are concatenated from root to target directory, capped at 32 KiB of UTF-8 content. Later, closer entries take precedence and each source is labelled in the prompt.</li><li>The root chain is loaded when the runtime is created. Before a file-path tool executes, the sidecar may request a path-specific chain through the Electron-owned <code>project.instructions.resolve</code> local proxy. The sidecar does not read instruction files directly.</li><li>Electron main passes the session-bound project root with runtime launch metadata and registers it before each prompt or compaction request. The local proxy resolves the target path against that main-owned binding instead of issuing a per-file <code>session.get</code> RPC. The sidecar caches one resolution claim per project-root/target-directory pair for the current prompt only.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Repository-wide rules load without a recursive workspace scan.</li><li>Global defaults are editable without a project; project instructions remain reviewable and versionable in the repository root.</li><li>Nested rules become available only when an agent accesses a matching path. Each file tool replaces the active chain with that target&#39;s complete chain, preventing sibling-directory rules from leaking into later tool calls.</li><li>Path-specific resolution is best-effort and bounded at two seconds. If the resolver or its host RPC is unavailable, the tool falls back to the base chain instead of waiting on the general host RPC deadline.</li><li>Path preflight records its own duration and cache/fallback markers so <code>hostRttMs</code> continues to describe the actual host tool call.</li><li>Root instruction changes rebuild an idle runtime on the next prompt. Nested rules are re-resolved for future file-path tool calls.</li><li>This private sidecar proxy remains constrained to a session id and a path that Electron main validates against that session&#39;s project root.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><h3 id="load-every-nested-instruction-file-at-startup" tabindex="-1">Load every nested instruction file at startup <a class="header-anchor" href="#load-every-nested-instruction-file-at-startup" aria-label="Permalink to &quot;Load every nested instruction file at startup&quot;">​</a></h3><p>Rejected. Large monorepos could inject unrelated rules and consume the model context before the task identifies a relevant directory.</p><h3 id="let-the-sidecar-scan-the-workspace" tabindex="-1">Let the sidecar scan the workspace <a class="header-anchor" href="#let-the-sidecar-scan-the-workspace" aria-label="Permalink to &quot;Let the sidecar scan the workspace&quot;">​</a></h3><p>Rejected. The sidecar executes model-directed paths. Workspace discovery stays in Electron main so the existing containment boundary remains explicit.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0037-project-instruction-chain.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0037ProjectInstructionChain = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0037ProjectInstructionChain as default
};
