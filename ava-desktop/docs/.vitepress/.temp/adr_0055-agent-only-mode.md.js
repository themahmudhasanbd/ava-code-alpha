import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0055: Agent-only mode; Chat becomes an internal read-only profile","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0055-agent-only-mode.md","filePath":"adr/0055-agent-only-mode.md","lastUpdated":1785927009000}');
const _sfc_main = { name: "adr/0055-agent-only-mode.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0055-agent-only-mode-chat-becomes-an-internal-read-only-profile" tabindex="-1">ADR 0055: Agent-only mode; Chat becomes an internal read-only profile <a class="header-anchor" href="#adr-0055-agent-only-mode-chat-becomes-an-internal-read-only-profile" aria-label="Permalink to &quot;ADR 0055: Agent-only mode; Chat becomes an internal read-only profile&quot;">​</a></h1><ul><li>Status: Superseded by ADR 0052 / ADR 0053</li><li>Date: 2026-08-05</li></ul><blockquote><p>Superseded. The mode concept this ADR removed returned as the Plan operating state (ADR 0052, superseded in turn by ADR 0053), so the product again exposes an <code>Agent | Plan</code> selector and <code>chat</code> migrates to <code>plan</code>, not <code>agent</code>. Retained because the reasoning about a negative permission gate and about failing closed on an unknown <code>mode</code> value still holds, and because it records why the toggle was removed before it was reintroduced with a different contract.</p></blockquote><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Since D003/D004 a session carried one of two tool profiles. <code>agent</code> got the full coding surface (<code>Read</code>/<code>Bash</code>/<code>Edit</code>/<code>Write</code>/<code>Glob</code>/<code>Grep</code>/<code>BrowserPreview</code> plus plugin tools); <code>chat</code> got a read-only subset (<code>Read</code>/<code>Glob</code>/<code>Grep</code>) that host-core hard-denies past every permission mode (D115).</p><p>The product is an agent desktop. <code>chat</code> was never a destination: it existed as a safety valve, but the UI advertised it as a peer choice through a top-bar segmented toggle, a Settings default-mode row, <code>/chat-mode</code> and <code>/agent-mode</code> palette commands, and four localized labels. That had three costs:</p><ol><li>A user who switched to Chat and later removed the toggle from their muscle memory could leave a session permanently unable to write files. With the toggle gone the session would have been stranded with no way back.</li><li>Every tool, prompt-composition, and permission change had to be reasoned about twice, and the lazy tool-activation core sets (D185) forked on mode.</li><li>The mode chip competed for space with the controls users do change — model, thinking level, permission mode.</li></ol><p>Deleting the profile outright was not acceptable either: imported sessions and rows written by older builds carry a <code>mode</code> string the current UI does not produce, and the narrow tool surface is a security boundary worth keeping for anything that is not explicitly <code>agent</code>.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><strong><code>agent</code> is the only mode the product exposes.</strong> The top-bar segmented toggle, the Settings default-mode row, both palette commands and their slash aliases, the <code>.ct-mode*</code> styles, and the <code>settings.mode*</code> i18n keys are removed. <code>newSession</code> always requests <code>agent</code>, and boot normalizes a stored <code>defaultMode</code> that is not <code>agent</code>.</li><li><strong><code>chat</code> is renamed <code>read-only</code>.</strong> The shared type becomes <code>Mode = &quot;read-only&quot; | &quot;agent&quot;</code>. Host-core&#39;s <code>SESSION_MODES</code> is <code>[&quot;agent&quot;, &quot;read-only&quot;]</code>, and <code>normalize_session_mode</code> folds the pre-D188 <code>chat</code> spelling into <code>read-only</code> on every write path (<code>session.create</code>, <code>session.configure</code>, <code>session.import</code>), rejecting anything else. Callers store the normalized value, never the raw input.</li><li><strong>The permission gate is negative.</strong> <code>PermissionManager</code> denies when <code>mode != &quot;agent&quot;</code> and the tool is outside <code>read_only_mode_allows</code> (<code>Read</code>/<code>Glob</code>/<code>Grep</code> plus <code>plugin_*</code>). An unknown or legacy <code>mode</code> string therefore fails closed into the read-only surface instead of silently gaining Write/Edit/Bash. The hard deny continues to outrank every D115 permission mode, including <code>auto</code>.</li><li><strong>The error codes are renamed</strong> to <code>BASH_DISABLED_IN_READ_ONLY</code> and <code>WRITE_DISABLED_IN_READ_ONLY</code>. Neither had a localized message, so the rename is confined to <code>packages/shared/src/errors.ts</code>, the host&#39;s code selection, and the specs.</li><li><strong>Existing <code>chat</code> rows are migrated to <code>agent</code> at open.</strong> <code>boot_maintenance</code> runs <code>UPDATE sessions SET mode = &#39;agent&#39; WHERE mode = &#39;chat&#39;</code> and folds a stored <code>app.defaultMode</code> of <code>chat</code> into <code>agent</code> via <code>json_set</code>. Both statements are idempotent; after the first open nothing matches.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>No session can be stranded read-only: the only rows that can hold <code>read-only</code> are ones a future import or external writer produces, and they keep the narrow, host-enforced surface by design.</li><li><code>SCHEMA_VERSION</code> stays at 7. <code>Database::open</code> archives-and-resets anything below the current version (D119), so a version bump would have destroyed the very sessions this change exists to rescue. The fix-up is a data repair inside the existing schema instead.</li><li>The mode string stays in the RPC contract (<code>tools.execute</code>, <code>session.configure</code>, <code>SessionSummary</code>) and in the <code>sessions.mode</code> column. Removing it would be a protocol break for no gain, and the host still needs it to pick a tool profile.</li><li>The conversation top bar keeps only the model picker plus task actions; the composer chip row now leads with Thinking. <code>ModelSelect</code> remains the writer of the session thinking level on a model switch.</li><li>Tool activation (D185) still has two core sets. The read-only one is simply unreachable from the UI.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><h3 id="drop-the-read-only-profile-entirely" tabindex="-1">Drop the <code>read-only</code> profile entirely <a class="header-anchor" href="#drop-the-read-only-profile-entirely" aria-label="Permalink to &quot;Drop the \`read-only\` profile entirely&quot;">​</a></h3><p>Rejected. It would leave imported and legacy rows with a <code>mode</code> the host does not recognize, and the natural fallback for an unrecognized value would be the full agent surface — a silent privilege widening at exactly the wrong moment.</p><h3 id="keep-the-value-spelled-chat" tabindex="-1">Keep the value spelled <code>chat</code> <a class="header-anchor" href="#keep-the-value-spelled-chat" aria-label="Permalink to &quot;Keep the value spelled \`chat\`&quot;">​</a></h3><p>Rejected. The name described a product mode that no longer exists and read as a peer of Agent in code, logs, and error codes. <code>read-only</code> says what the profile actually is, which is what the remaining host-side enforcement is for.</p><h3 id="keep-the-toggle-but-hide-it-behind-developer-mode" tabindex="-1">Keep the toggle but hide it behind developer mode <a class="header-anchor" href="#keep-the-toggle-but-hide-it-behind-developer-mode" aria-label="Permalink to &quot;Keep the toggle but hide it behind developer mode&quot;">​</a></h3><p>Rejected. It preserves both the dual-toolset reasoning cost and the stranding failure mode, and a debug-only path into a security-relevant profile is worse than no path.</p><h3 id="bump-the-schema-version-to-carry-the-migration" tabindex="-1">Bump the schema version to carry the migration <a class="header-anchor" href="#bump-the-schema-version-to-carry-the-migration" aria-label="Permalink to &quot;Bump the schema version to carry the migration&quot;">​</a></h3><p>Rejected. v7 is a breaking reset, not a migration chain (D119): opening a pre-current database archives it and bootstraps a fresh file. A bump would discard the user&#39;s sessions to fix one column value in them.</p><h2 id="references" tabindex="-1">References <a class="header-anchor" href="#references" aria-label="Permalink to &quot;References&quot;">​</a></h2><ul><li><code>packages/shared/src/types.ts</code>, <code>packages/shared/src/errors.ts</code></li><li><code>packages/agent-runtime/src/runtime.ts</code></li><li><code>crates/host-core/src/sessions.rs</code>, <code>crates/host-core/src/permissions.rs</code></li><li><code>crates/host-core/src/db.rs</code>, <code>crates/host-core/src/rpc/mod.rs</code></li><li><code>apps/desktop/src/components/ConversationTopbar.tsx</code></li><li><code>apps/desktop/src/pages/SettingsPage.tsx</code></li><li><code>apps/desktop/electron/main/builtin-commands.ts</code></li><li><code>docs/spec/03-runtime/03-tools-and-permissions.md</code></li><li><code>docs/spec/03-runtime/04-data-storage.md</code></li><li><code>docs/spec/03-runtime/08-error-codes.md</code></li><li><code>docs/spec/06-delivery/04-e2e-test-plan.md</code> (E2E-018, E2E-088)</li><li>Decision D191; amended D003, D004, D115; superseded by D188 / D189</li><li><a href="./0052-plan-operating-state-and-approval-boundary">ADR 0052</a>, <a href="./0053-plan-checkpoint-artifact-and-execution-epoch">ADR 0053</a></li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0055-agent-only-mode.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0055AgentOnlyMode = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0055AgentOnlyMode as default
};
