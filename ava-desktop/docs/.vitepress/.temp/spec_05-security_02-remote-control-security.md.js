import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Remote Agent Control Security Specification","description":"","frontmatter":{},"headers":[],"relativePath":"spec/05-security/02-remote-control-security.md","filePath":"spec/05-security/02-remote-control-security.md","lastUpdated":1789795544000}');
const _sfc_main = { name: "spec/05-security/02-remote-control-security.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="remote-agent-control-security-specification" tabindex="-1">Remote Agent Control Security Specification <a class="header-anchor" href="#remote-agent-control-security-specification" aria-label="Permalink to &quot;Remote Agent Control Security Specification&quot;">​</a></h1><ul><li>Status: Target specification; post-MVP</li><li>Decision: D373 / ADR 0205, amended by D374 and D375</li><li>Applies to: RACP-WS over the SSH tunnel and any later binding: RACP-HTTP, the reserved RACP-GRPC, and the Host link</li><li>Does not weaken: local MCP, host-core, plugin, or provider-secret boundaries</li></ul><h2 id="_1-security-goals" tabindex="-1">1. Security goals <a class="header-anchor" href="#_1-security-goals" aria-label="Permalink to &quot;1. Security goals&quot;">​</a></h2><p>Remote control MUST provide:</p><ol><li>authenticated user and device identity;</li><li>authorization scoped to tenant, Host, project, Session, operation, and role;</li><li>confidentiality and integrity in transit;</li><li>no direct network access to Rust host-core;</li><li>host-owned permission and workspace enforcement;</li><li>revocation and auditability;</li><li>bounded resource use and safe disconnect behavior;</li><li>no replay of a completed or previously admitted mutation; and</li><li>a remote permission ceiling, so a remote controller cannot turn a session into unattended execution.</li></ol><p>The security design assumes that an Agent can be prompt-injected. A prompt, tool result, attachment, or model output is untrusted data and MUST NOT grant an authority that the authenticated principal does not already have.</p><h2 id="_2-trust-zones" tabindex="-1">2. Trust zones <a class="header-anchor" href="#_2-trust-zones" aria-label="Permalink to &quot;2. Trust zones&quot;">​</a></h2><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>┌──────────────────────┐       HTTPS/WSS        ┌─────────────────────┐</span></span>
<span class="line"><span>│ Remote Client        │ ──────────────────────▶ │ Remote Gateway      │</span></span>
<span class="line"><span>│ browser/native/CLI   │                         │ identity + routing  │</span></span>
<span class="line"><span>└──────────────────────┘                         └──────────┬──────────┘</span></span>
<span class="line"><span>                                                           │ outbound mTLS</span></span>
<span class="line"><span>                                                           │ Host link</span></span>
<span class="line"><span>                                                           ▼</span></span>
<span class="line"><span>                                               ┌─────────────────────────┐</span></span>
<span class="line"><span>                                               │ Agent Host               │</span></span>
<span class="line"><span>                                               │ session + policy owner   │</span></span>
<span class="line"><span>                                               ├──────────┬──────────────┤</span></span>
<span class="line"><span>                                               │ pi       │ Rust host    │</span></span>
<span class="line"><span>                                               │ sidecar  │ core         │</span></span>
<span class="line"><span>                                               └──────────┴──────────────┘</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br></div></div><table tabindex="0"><thead><tr><th>Zone</th><th>Trust assumption</th><th>Required boundary</th></tr></thead><tbody><tr><td>Remote Client</td><td>Authenticated but UI and prompt data are untrusted</td><td>Scoped session or bearer credential; no secret authority by default</td></tr><tr><td>Gateway</td><td>User-hosted relay, routable and exposed</td><td>Authn, authz, rate limits, audit, transient buffers only, no raw host RPC</td></tr><tr><td>Agent Host</td><td>Trusted local authority beside the workspace</td><td>mTLS/device identity, signed route context, host policy, remote permission ceiling</td></tr><tr><td>Node sidecar</td><td>Agent runtime, not policy owner</td><td>Main/Host proxy allowlist</td></tr><tr><td>Rust host-core</td><td>Workspace, storage, tool, permission, and secret authority</td><td>stdio only; no public listener</td></tr></tbody></table><h2 id="_3-identity-and-enrollment" tabindex="-1">3. Identity and enrollment <a class="header-anchor" href="#_3-identity-and-enrollment" aria-label="Permalink to &quot;3. Identity and enrollment&quot;">​</a></h2><h3 id="_3-1-client-to-gateway" tabindex="-1">3.1 Client to Gateway <a class="header-anchor" href="#_3-1-client-to-gateway" aria-label="Permalink to &quot;3.1 Client to Gateway&quot;">​</a></h3><p>The production Gateway MUST validate an established identity before routing. D385 makes remote control user-local by construction. No project-operated identity or account service is in the path: the only credential a client ever holds is a device token issued by the user&#39;s own Host at pairing (§3.4). A Gateway, if a user runs one, is self-hosted on the user&#39;s infrastructure and admits clients with those same Host-issued device credentials; it validates the token&#39;s Host id, expiry, and revocation state before routing. OIDC federation and the pi-backend account service are out of scope for remote control, and refresh tokens do not exist in this model.</p><p>Because a browser cannot set request headers on the <code>WebSocket</code> and <code>EventSource</code> APIs, the Gateway offers two authentication profiles:</p><ul><li><strong>Header profile</strong> for non-browser clients: the access token is sent in the <code>Authorization</code> header of every request and of the WebSocket upgrade.</li><li><strong>Cookie profile</strong> for browser clients: after device pairing the self-hosted Gateway or the Host issues an <code>HttpOnly</code>, <code>Secure</code>, <code>SameSite=Lax</code> or stricter session cookie; the WebSocket upgrade and the SSE request are authorized by that cookie plus the per-tenant Origin allowlist, and every mutation carries a CSRF token issued with the session. A browser client MAY instead stream events through <code>fetch</code> with the header profile.</li></ul><p>In both profiles access tokens MUST NOT be placed in query strings, WebSocket URLs, SSE URLs, attachment names, or event payloads. Bearer-only APIs still validate Origin for browser requests.</p><p>The Gateway and the cookie profile belong to the unscheduled Gateway and browser milestones (D375). The first remote topology uses the header profile with a device token obtained through the SSH bootstrap pairing in §3.4.</p><p>For the first trusted-device prototype, a one-time pairing code MAY bootstrap the identity link. It MUST be short-lived, single-use, displayed out of band, and exchanged over TLS. A pairing code MUST NOT become a long-lived API token.</p><h3 id="_3-2-agent-host-to-gateway" tabindex="-1">3.2 Agent Host to Gateway <a class="header-anchor" href="#_3-2-agent-host-to-gateway" aria-label="Permalink to &quot;3.2 Agent Host to Gateway&quot;">​</a></h3><p>The Agent Host MUST open the production connection outbound. The Host link uses mutual TLS with a per-host identity certificate or an equivalent signed device credential.</p><ul><li>Enrollment credentials are one-time and expire after the enrollment window.</li><li>Host credentials are scoped to one tenant and Host identity.</li><li>The Gateway rejects a certificate or device credential after revocation.</li><li>Credentials rotate without exposing provider secrets to the Gateway.</li><li>The Host rejects a Gateway connection whose server identity is not pinned to the configured product trust roots.</li><li>The Host never accepts an unauthenticated inbound control socket.</li><li>The Host link authenticates the Gateway only. User authority on a relayed connection comes solely from the per-connection route context in §3.3.</li></ul><p>Direct LAN or development connections still use TLS and an expiring device token. Plain <code>ws://</code>, plain HTTP, and static shared tokens in URLs are not supported.</p><h3 id="_3-3-host-capability-context" tabindex="-1">3.3 Host capability context <a class="header-anchor" href="#_3-3-host-capability-context" aria-label="Permalink to &quot;3.3 Host capability context&quot;">​</a></h3><p>After the Gateway authenticates a user, it issues a short-lived signed route context for each logical client connection:</p><div class="language-ts vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">type</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> HostRouteContext</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  tenantId</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  hostId</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  subject</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  clientConnectionId</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  sessionScopes</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">[]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  roles</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">[]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  issuedAt</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  expiresAt</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  tokenId</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br></div></div><p>The Agent Host verifies the signature, audience, Host id, expiry, and session scope before executing any mutation. The Gateway&#39;s transport connection is not itself authorization for a session, and a route context for one <code>clientConnectionId</code> cannot be replayed on another.</p><h3 id="_3-4-ssh-bootstrap-pairing-first-remote-topology" tabindex="-1">3.4 SSH bootstrap pairing (first remote topology) <a class="header-anchor" href="#_3-4-ssh-bootstrap-pairing-first-remote-topology" aria-label="Permalink to &quot;3.4 SSH bootstrap pairing (first remote topology)&quot;">​</a></h3><p>The desktop bootstraps a <code>pi-host</code> on a remote machine over the user&#39;s own SSH session (<code>02-architecture/05-remote-agent-control.md</code> §5.2). The trust argument is that an SSH login already proves shell access to that machine; pairing only binds a desktop device to the Host it started.</p><ul><li>The pairing token is generated by the Host at start, is single-use, expires within the bootstrap window, and travels only over the SSH channel; it is never written to a world-readable file or a URL.</li><li>The desktop exchanges it once, over the forwarded loopback port, for a device token that it stores in its secure storage; the Host records the device as <code>owner</code> of that Host.</li><li>The Host binds loopback only and accepts a device token only from a loopback peer; a non-loopback bind requires TLS and the same device token.</li><li>The bootstrap script, uploaded over SSH, downloads the <code>pi-host</code> bundle for the remote platform at the desktop&#39;s version from GitHub Releases, verifies the SHA-256 published with the release, and installs it under the user&#39;s home; the desktop never uploads executable bytes itself. A machine without outbound access to GitHub cannot be bootstrapped in the first version.</li><li>Revoking the device token on the Host, or removing the Host from the desktop, ends the pairing; a new pairing needs a new SSH bootstrap.</li><li>Provider configuration for the remote Host is written over the SSH channel by the bootstrap step as Host-local configuration; it never crosses RACP.</li></ul><p><strong>SSH credential handling (ADR 0293).</strong> The desktop MAY hold the SSH login password for a host the user paired that way, under these rules:</p><ul><li>The password is supplied by the user in Settings, is passed to the system <code>ssh</code> client through OpenSSH&#39;s askpass helper, and is never an <code>ssh</code> argument, an environment variable value, or part of a URL.</li><li>Credential material exists on disk only inside a <code>0700</code> directory as a <code>0600</code> file, only while an <code>ssh</code> child can still prompt for it, and is removed on every path including a failed forward and <code>dispose</code>.</li><li>It is persisted at rest only through the same OS-keychain encryption the device token uses, and a password that cannot be decrypted costs the secret rather than the paired host.</li><li>The renderer never receives it: <code>RemoteHostSummary</code> and <code>RemoteHostSshMetadata</code> carry no secret field.</li><li>A password containing a line break cannot survive the askpass round trip and is rejected before any remote command runs. Windows OpenSSH cannot execute the helper and is refused with a remedy instead.</li><li>Password mode is exclusive: <code>PubkeyAuthentication=no</code> and <code>NumberOfPasswordPrompts=1</code>. Default identities are not tried, because an encrypted local key would consume the single askpass answer as a passphrase. A user with both a key and a password picks key mode.</li></ul><h2 id="_4-authorization-model" tabindex="-1">4. Authorization model <a class="header-anchor" href="#_4-authorization-model" aria-label="Permalink to &quot;4. Authorization model&quot;">​</a></h2><h3 id="_4-1-role-matrix" tabindex="-1">4.1 Role matrix <a class="header-anchor" href="#_4-1-role-matrix" aria-label="Permalink to &quot;4.1 Role matrix&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Operation</th><th style="${ssrRenderStyle({ "text-align": "right" })}">Viewer</th><th style="${ssrRenderStyle({ "text-align": "right" })}">Controller</th><th style="${ssrRenderStyle({ "text-align": "right" })}">Approver</th><th style="${ssrRenderStyle({ "text-align": "right" })}">Owner</th></tr></thead><tbody><tr><td>List/get visible hosts, projects, and sessions</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td></tr><tr><td>Subscribe to session or host events</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td></tr><tr><td>Read session history</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td></tr><tr><td>Create/attach as viewer</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td></tr><tr><td>Start or queue a turn</td><td style="${ssrRenderStyle({ "text-align": "right" })}">no</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td><td style="${ssrRenderStyle({ "text-align": "right" })}">optional</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td></tr><tr><td>Stop, interrupt, or cancel a session turn</td><td style="${ssrRenderStyle({ "text-align": "right" })}">no</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td><td style="${ssrRenderStyle({ "text-align": "right" })}">optional</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td></tr><tr><td>Resolve tool approval (<code>allow-once</code>, <code>deny</code>)</td><td style="${ssrRenderStyle({ "text-align": "right" })}">no</td><td style="${ssrRenderStyle({ "text-align": "right" })}">no by default</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td></tr><tr><td>Resolve tool approval with <code>allow-session</code></td><td style="${ssrRenderStyle({ "text-align": "right" })}">no</td><td style="${ssrRenderStyle({ "text-align": "right" })}">no</td><td style="${ssrRenderStyle({ "text-align": "right" })}">policy</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td></tr><tr><td>Resolve Plan/Goal approval with permission mode</td><td style="${ssrRenderStyle({ "text-align": "right" })}">no</td><td style="${ssrRenderStyle({ "text-align": "right" })}">no by default</td><td style="${ssrRenderStyle({ "text-align": "right" })}">explicit policy</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td></tr><tr><td>Answer an input request</td><td style="${ssrRenderStyle({ "text-align": "right" })}">no</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td><td style="${ssrRenderStyle({ "text-align": "right" })}">optional</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td></tr><tr><td>Upload an attachment</td><td style="${ssrRenderStyle({ "text-align": "right" })}">no</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td><td style="${ssrRenderStyle({ "text-align": "right" })}">optional</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td></tr><tr><td>Revoke membership</td><td style="${ssrRenderStyle({ "text-align": "right" })}">no</td><td style="${ssrRenderStyle({ "text-align": "right" })}">no</td><td style="${ssrRenderStyle({ "text-align": "right" })}">no</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td></tr><tr><td>Archive a session</td><td style="${ssrRenderStyle({ "text-align": "right" })}">no</td><td style="${ssrRenderStyle({ "text-align": "right" })}">no</td><td style="${ssrRenderStyle({ "text-align": "right" })}">no</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td></tr><tr><td>Open or use a session terminal</td><td style="${ssrRenderStyle({ "text-align": "right" })}">no</td><td style="${ssrRenderStyle({ "text-align": "right" })}">policy</td><td style="${ssrRenderStyle({ "text-align": "right" })}">policy</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td></tr><tr><td>Advertise relayed tools</td><td style="${ssrRenderStyle({ "text-align": "right" })}">no</td><td style="${ssrRenderStyle({ "text-align": "right" })}">no</td><td style="${ssrRenderStyle({ "text-align": "right" })}">no</td><td style="${ssrRenderStyle({ "text-align": "right" })}">yes</td></tr></tbody></table><p>Role checks are necessary but not sufficient. The Host MUST additionally check:</p><ul><li>the Session belongs to the requested tenant and Host;</li><li>the principal is allowed to use the Session&#39;s project;</li><li>the operation is legal in the Session state;</li><li>the durable permission/mode policy allows the proposed action;</li><li>the remote permission ceiling has been applied to the turn; and</li><li>the request&#39;s expected revision and idempotency key are valid.</li></ul><h3 id="_4-2-no-privilege-escalation-through-protocol-fields" tabindex="-1">4.2 No privilege escalation through protocol fields <a class="header-anchor" href="#_4-2-no-privilege-escalation-through-protocol-fields" aria-label="Permalink to &quot;4.2 No privilege escalation through protocol fields&quot;">​</a></h3><p>The following client fields are advisory only or forbidden:</p><ul><li><code>permissionMode</code> cannot upgrade a durable Session policy; the only accepted permission-mode field is the explicit selection on a Plan/Goal <code>approve</code>, and it is validated against <code>allowedPermissionModes</code>;</li><li><code>admission: &quot;queue&quot;</code> cannot bypass single-turn execution; it only places a bounded, cancelable entry in the Host queue;</li><li><code>workspaceRoot</code> cannot replace a Host-owned project binding;</li><li><code>toolName</code> cannot select a tool outside the Host catalog;</li><li><code>confirm</code> cannot replace an approval request or create an approval result;</li><li><code>providerApiKey</code>, secret values, and secret references cannot be supplied in a turn payload; and</li><li>a client cannot claim another <code>principal</code>, <code>agentName</code>, <code>connectionId</code>, or <code>clientConnectionId</code>.</li></ul><p>The Host chooses the effective model/provider configuration from its own session and provider state. Remote control does not become a credential relay.</p><h3 id="_4-3-remote-permission-ceiling" tabindex="-1">4.3 Remote permission ceiling <a class="header-anchor" href="#_4-3-remote-permission-ceiling" aria-label="Permalink to &quot;4.3 Remote permission ceiling&quot;">​</a></h3><p>A turn started by a remote principal runs under the lower of the Session&#39;s durable permission mode and the Host&#39;s configured <code>remoteMaxPermissionMode</code>, ordered <code>ask</code> &lt; <code>accept-edits</code> &lt; <code>auto</code>. The default ceiling is <code>ask</code>. The Host operator may raise it; a principal may exceed it only when the principal holds <code>approver</code> and Host policy allows approvers to use the session&#39;s own mode. The applied value is reported as <code>effectivePermissionMode</code> and never changes the durable session mode.</p><p>The ceiling applies to Gateway-routed principals. A desktop device paired through the SSH bootstrap (§3.4) holds <code>owner</code> on that Host and is exempt: an SSH login already grants shell access to the machine, so a ceiling would withhold nothing. Its turns report the session&#39;s own mode as <code>effectivePermissionMode</code>. The Host policy <code>applyCeilingToPairedDevices</code> (default off) re-applies the ceiling to paired devices for an operator who wants every remote turn to start at <code>ask</code>.</p><p><code>allow-session</code> is offered to a remote approver only when Host policy allows remote session grants; otherwise the request&#39;s <code>allowedDecisions</code> omit it. A session grant made remotely is the same by-tool-name grant as a local one (frozen decision 18) and ends with the Session.</p><h2 id="_5-network-and-transport-protections" tabindex="-1">5. Network and transport protections <a class="header-anchor" href="#_5-network-and-transport-protections" aria-label="Permalink to &quot;5. Network and transport protections&quot;">​</a></h2><h3 id="_5-1-tls" tabindex="-1">5.1 TLS <a class="header-anchor" href="#_5-1-tls" aria-label="Permalink to &quot;5.1 TLS&quot;">​</a></h3><ul><li>Public HTTP, SSE, and WebSocket endpoints MUST use TLS 1.2 or newer; TLS 1.3 is preferred. A reserved gRPC binding inherits the same rule.</li><li>A <code>pi-host</code> bound to loopback and reached through an SSH port forward MAY accept plain <code>ws://</code> when both the bind address and the peer address are loopback and a valid device token is presented; the SSH channel provides confidentiality, matching the loopback rule of ADR 0203. Any non-loopback bind requires TLS.</li><li>The production Host link MUST use mutual TLS or an equivalent device-bound authenticated channel.</li><li>Certificate validation MUST include hostname or service identity validation; disabling verification is not a development shortcut supported by the product.</li><li>WebSocket upgrade credentials, header or cookie, are validated before accepting the connection.</li></ul><h3 id="_5-2-origin-and-cross-site-controls" tabindex="-1">5.2 Origin and cross-site controls <a class="header-anchor" href="#_5-2-origin-and-cross-site-controls" aria-label="Permalink to &quot;5.2 Origin and cross-site controls&quot;">​</a></h3><ul><li>The Gateway maintains an explicit browser Origin allowlist per tenant and checks it on every WebSocket upgrade and SSE request from a browser.</li><li>Local-only endpoints bind loopback and validate loopback Origin as required by ADR 0203.</li><li>Cookie-profile sessions use <code>SameSite</code> protection and a CSRF token on every mutation, including mutations sent over an already-open WebSocket.</li><li>A bearer token in a URL is always rejected.</li><li>CORS exposes only the required methods, headers, and response types.</li></ul><h3 id="_5-3-request-binding-and-replay-protection" tabindex="-1">5.3 Request binding and replay protection <a class="header-anchor" href="#_5-3-request-binding-and-replay-protection" aria-label="Permalink to &quot;5.3 Request binding and replay protection&quot;">​</a></h3><p>Every mutation carries a principal-bound idempotency key. The Host stores the key and result for at least the active turn lifetime and rejects reuse with different payload bytes. A Gateway retry MUST preserve the key and trace context.</p><p>Requests with an expired route context, stale session revision, or revoked connection fail before they reach the Agent runtime.</p><h2 id="_6-workspace-file-and-attachment-security" tabindex="-1">6. Workspace, file, and attachment security <a class="header-anchor" href="#_6-workspace-file-and-attachment-security" aria-label="Permalink to &quot;6. Workspace, file, and attachment security&quot;">​</a></h2><ol><li>A remote request identifies a Session, not an arbitrary filesystem root.</li><li>The Host resolves every tool path against that Session&#39;s durable project or scratch root.</li><li>Remote clients send attachment bytes or opaque attachment ids, never local absolute paths or <code>file://</code> URLs.</li><li>The Host validates declared size, actual size, MIME policy, SHA-256, and expiration before a turn can reference an attachment.</li><li>Attachment storage is private to the owning tenant, Host, and Session.</li><li>Uploads are not executable and are not automatically added to a tool root.</li><li>Gateway URL fetches are not accepted as an attachment source, preventing SSRF through a remote-control request.</li><li>Workspace reads and writes continue to use the current host sandbox, ignore rules, path checks, and permission policy.</li><li>Behind a Gateway the upload target is served by the Gateway. The Gateway enforces only the size bound, relays the bytes to the Host in bounded chunks, deletes its copy when <code>attachment/complete</code> succeeds or the upload expires, and never inspects, persists, or serves those bytes elsewhere.</li></ol><h2 id="_7-tool-and-approval-security" tabindex="-1">7. Tool and approval security <a class="header-anchor" href="#_7-tool-and-approval-security" aria-label="Permalink to &quot;7. Tool and approval security&quot;">​</a></h2><p>Remote control MUST use the existing host-owned tool execution path. It MUST NOT expose:</p><ul><li>raw <code>host.proxy</code> calls;</li><li>raw Rust host-core methods;</li><li>generic Electron IPC invocation;</li><li>provider secret get/set/delete methods;</li><li>arbitrary process spawning; or</li><li>a remote equivalent of a disabled permission mode.</li></ul><p>Approval requests contain a bounded, redacted summary. The client submits a decision for a live request; it does not submit a tool invocation to be executed after approval. The decision vocabulary is the local one: <code>allow-once</code>, <code>allow-session</code>, and <code>deny</code> for tools; <code>approve</code> with an explicit permission mode, or <code>reject</code>, for Plan and Goal contracts. The Host verifies request id, Session id, turn id, principal role, expiry, allowed decision, permission-mode selection, and current state in one operation.</p><p>Pending requests are Host state. Rust host-core keeps the pending permission table and its timer; the Agent Host reads it through <code>permissions.pending</code> so a late-attaching client receives open requests. That read is redacted the same way as the request event and never returns tool arguments beyond the bounded preview.</p><p>Approval lifetime is Host policy. The local default remains 120 seconds then deny (frozen decision 17). While a remote subscriber is attached the default is 30 minutes (D375); the operator may shorten or lengthen it within a bound, the blocked tool waits for that lifetime unless a local or remote decision arrives earlier, and a client disconnect never extends it.</p><p>An approval response that arrives after disconnect, expiry, abort, crash, or turn completion is a no-op or a structured stale/expired error. It never restarts the turn. The first valid decision wins across local and remote clients; later valid responses receive the stored result.</p><p>A remote session&#39;s catalog contains the remote Host&#39;s tools plus the tools the paired desktop advertised for relay: its user-configured MCP servers and plugin tools that do not require the session workspace. A relayed tool executes on the desktop under the desktop&#39;s own plugin permissions and confirmation rules, never on the Host and never against the remote workspace; the Host&#39;s permission decision precedes the relay request, the Host sends only the Agent&#39;s arguments and never a secret, and a lost relay connection fails the tool without interrupting the turn. Provider secrets never cross RACP in either direction; the remote Host&#39;s providers are configured over the SSH bootstrap channel (§3.4).</p><p>A session terminal is a shell on the Host machine running as the <code>pi-host</code> user with the session root as its working directory. Only the SSH-paired owner device or a principal holding the explicit <code>terminal</code> scope may open one; Gateway-routed principals need that scope from policy. Terminal output is ephemeral and recoverable only from the terminal&#39;s bounded replay ring.</p><h2 id="_8-gateway-and-tenant-isolation" tabindex="-1">8. Gateway and tenant isolation <a class="header-anchor" href="#_8-gateway-and-tenant-isolation" aria-label="Permalink to &quot;8. Gateway and tenant isolation&quot;">​</a></h2><p>The Gateway milestone is unscheduled (D375). These rules bind when it is scheduled and are retained so the contract does not drift.</p><ul><li>Every route is keyed by <code>(tenantId, hostId, sessionId)</code>.</li><li>A user-local deployment has exactly one tenant, the Host itself; PI never operates a shared Gateway (D385).</li><li>The first deployment is single-tenant. Routes already carry <code>tenantId</code> so a second tenant is an operational change, not a protocol change; cross-tenant isolation tests run once a multi-tenant harness exists.</li><li>A client cannot enumerate Host or Session ids outside its signed scope.</li><li>Gateway caches contain opaque ids and routing metadata, not provider secrets.</li><li>A Host reconnect replaces the old connection only after identity and tenant match; stale links are closed.</li><li>A tenant&#39;s rate limits and event queues are independent of other tenants.</li><li>Logs and metrics carry tenant/host/session identifiers only where the retention policy permits; prompt and tool content is excluded by default.</li></ul><h2 id="_9-abuse-controls-and-resource-bounds" tabindex="-1">9. Abuse controls and resource bounds <a class="header-anchor" href="#_9-abuse-controls-and-resource-bounds" aria-label="Permalink to &quot;9. Abuse controls and resource bounds&quot;">​</a></h2><p>The Gateway and Host enforce the lower of their configured limits:</p><table tabindex="0"><thead><tr><th>Resource</th><th style="${ssrRenderStyle({ "text-align": "right" })}">Initial target</th></tr></thead><tbody><tr><td>Control requests per principal</td><td style="${ssrRenderStyle({ "text-align": "right" })}">120/minute</td></tr><tr><td>Turn starts per Session</td><td style="${ssrRenderStyle({ "text-align": "right" })}">20/minute</td></tr><tr><td>Queued turns per Session</td><td style="${ssrRenderStyle({ "text-align": "right" })}">8</td></tr><tr><td>Concurrent clients per Host</td><td style="${ssrRenderStyle({ "text-align": "right" })}">16</td></tr><tr><td>Concurrent subscriptions per connection</td><td style="${ssrRenderStyle({ "text-align": "right" })}">8</td></tr><tr><td>Request/event frame</td><td style="${ssrRenderStyle({ "text-align": "right" })}">1 MiB</td></tr><tr><td>Prompt payload</td><td style="${ssrRenderStyle({ "text-align": "right" })}">256 KiB</td></tr><tr><td>Attachment</td><td style="${ssrRenderStyle({ "text-align": "right" })}">50 MiB</td></tr><tr><td>In-flight attachment uploads per principal</td><td style="${ssrRenderStyle({ "text-align": "right" })}">4</td></tr><tr><td>Event send queue</td><td style="${ssrRenderStyle({ "text-align": "right" })}">4 MiB or 1,000 durable events</td></tr><tr><td>Open terminals per Session</td><td style="${ssrRenderStyle({ "text-align": "right" })}">2</td></tr></tbody></table><p>Rate-limit responses include a retry hint but never disclose another tenant&#39;s quota. Slow clients lose ephemeral events first and are disconnected with a resumable cursor before a durable event is lost. The Host never blocks the Agent turn indefinitely on a remote client that stopped reading.</p><h2 id="_10-audit-and-observability" tabindex="-1">10. Audit and observability <a class="header-anchor" href="#_10-audit-and-observability" aria-label="Permalink to &quot;10. Audit and observability&quot;">​</a></h2><p>Every remote control mutation produces a structured audit record containing:</p><ul><li><code>traceId</code>, <code>connectionId</code>, <code>clientConnectionId</code>, <code>principal</code>, <code>tenantId</code>, <code>hostId</code>;</li><li>Session and turn ids;</li><li>operation and outcome, including the admission mode and <code>effectivePermissionMode</code> of a started turn;</li><li>authorization decision and role;</li><li>idempotency key hash, not the raw key;</li><li>event epoch and sequence range, when applicable; and</li><li>error code, or approval decision with the selected permission mode.</li></ul><p>Audit records MUST NOT contain provider credentials, raw prompt text, raw tool arguments, raw tool output, attachment bytes, or approval secrets by default. The runtime logger may record bounded redacted summaries under the existing redaction policy.</p><p>Metrics SHOULD cover connection count, reconnects, authentication failures, authorization failures, event lag, replay/resync counts, epoch changes, queue depth, turn admission latency, approval latency, queue drops, and Host availability.</p><p>Trace propagation uses W3C <code>traceparent</code> where the selected transport supports it. A Gateway MUST preserve the trace id across the Host link.</p><h2 id="_11-revocation-and-incident-response" tabindex="-1">11. Revocation and incident response <a class="header-anchor" href="#_11-revocation-and-incident-response" aria-label="Permalink to &quot;11. Revocation and incident response&quot;">​</a></h2><p>The Gateway MUST be able to revoke:</p><ul><li>a user session;</li><li>a Host device;</li><li>a client connection;</li><li>a Session membership; and</li><li>a pending attachment or upload target.</li></ul><p>Revocation closes active connections, prevents new mutations, cancels queued turns the revoked principal submitted, and leaves the Agent Host&#39;s local turn policy unchanged. A running turn is interrupted only when the revoked scope or incident policy explicitly requires it; revocation must not silently replay or roll back a completed turn.</p><p>Provider credential rotation remains an Agent Host operation. Remote clients cannot use the control protocol to export, test, or replace a secret unless a separate, explicitly specified credential-management capability is added.</p><h2 id="_12-security-acceptance-gates" tabindex="-1">12. Security acceptance gates <a class="header-anchor" href="#_12-security-acceptance-gates" aria-label="Permalink to &quot;12. Security acceptance gates&quot;">​</a></h2><ol><li>Plain HTTP and <code>ws://</code> are rejected outside an explicitly isolated local test harness.</li><li>Rust host-core has no public listener and cannot be addressed by a remote client.</li><li>A viewer cannot start a turn or resolve an approval.</li><li>A controller cannot select an unauthorized Session, workspace, tool, model, permission mode, or provider secret.</li><li>A prompt-injected tool result cannot change the authenticated principal or role.</li><li>Duplicate mutation keys cannot create duplicate turns or approvals.</li><li>An expired/revoked credential cannot resume a connection or upload bytes.</li><li>Cross-tenant Host, Session, event, attachment, and audit access is denied once a multi-tenant harness exists.</li><li>Event replay never crosses a Session or principal scope.</li><li>Gateway and Host logs contain no provider secrets or unredacted tool data.</li><li>Slow clients cannot exhaust Host memory or stall an Agent turn.</li><li>Host crash, Gateway reconnect, and client reconnect do not replay an already-admitted execution.</li><li>A remote-initiated turn never reports an <code>effectivePermissionMode</code> above the configured ceiling, and <code>allow-session</code> is absent unless policy allows it.</li><li>Browser WebSocket and SSE connections succeed only on the cookie profile with Origin and CSRF checks, or on the header profile through <code>fetch</code>; URL tokens fail in both. Applies when the browser milestone is scheduled.</li><li>A relayed server-initiated approval request is answered exactly once, and the answer reaches only the Host that raised it. Applies when the Gateway milestone is scheduled.</li><li>A <code>pi-host</code> bound to loopback accepts only loopback peers with a valid device token; a non-loopback bind without TLS fails to start.</li><li>A pairing token is single-use, arrives only over the SSH channel, and cannot be exchanged twice or from a non-loopback peer.</li><li>A remote session exposes only the remote Host&#39;s tool catalog; desktop plugin tools and desktop MCP servers never execute against a remote workspace, and provider secrets never cross RACP.</li><li>A relayed tool never executes on the Host and never receives a Host secret; the Host&#39;s approval precedes the relay request; a lost relay connection fails the tool without interrupting the turn.</li><li>A session terminal opens only for the SSH-paired owner or a principal with the <code>terminal</code> scope, with its working directory inside the session root. root.</li><li>An SSH login password is supplied only by the user, reaches <code>ssh</code> only through the askpass helper, is stored only encrypted, and never appears in a process argument list, the renderer, or a log line.</li></ol><h2 id="_13-amendment-history" tabindex="-1">13. Amendment history <a class="header-anchor" href="#_13-amendment-history" aria-label="Permalink to &quot;13. Amendment history&quot;">​</a></h2><p>D374 (2026-09-10) added the browser cookie/header authentication profiles, the two accepted identity sources, the remote permission ceiling, the local decision vocabulary, the remote approval lifetime policy, the Host link and Gateway attachment relay rules, the single-tenant-first clause, and gates 13–15.</p><p>D375 (2026-09-10) added the SSH bootstrap pairing (§3.4), the loopback rule for <code>pi-host</code> behind an SSH port forward, the ceiling exemption for SSH-paired owner devices, the remote tool-catalog and provider-configuration rules, gates 16–18, and marked the Gateway and cookie-profile clauses as belonging to unscheduled milestones.</p><p>The D375 design-gate answers, recorded the same day, fixed the identity source to the PI account service, the GitHub Releases download for <code>pi-host</code>, the relay and terminal rules with gates 19–20, the 30-minute remote approval lifetime, and the <code>applyCeilingToPairedDevices</code> policy.</p><p>D385 (2026-09-10) withdrew the first-party identity source: remote control is user-local by construction, every credential is issued by the user&#39;s own Host, and any Gateway is self-hosted and admits clients with those device credentials.</p><p>D454 (2026-09-19) added SSH password authentication for the bootstrap (§3.4, ADR 0293): the credential-handling rules above and gate 21. It relaxes <code>BatchMode=yes</code> for a password target only, with <code>NumberOfPasswordPrompts=1</code> and <code>PubkeyAuthentication=no</code>, and keeps a key or agent as the default path.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("spec/05-security/02-remote-control-security.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _02RemoteControlSecurity = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _02RemoteControlSecurity as default
};
