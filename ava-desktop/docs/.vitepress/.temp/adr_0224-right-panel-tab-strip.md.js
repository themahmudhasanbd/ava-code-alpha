import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0224: Right Panel Tab Strip and Data-Driven Add Menu","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0224-right-panel-tab-strip.md","filePath":"adr/0224-right-panel-tab-strip.md","lastUpdated":1789113922000}');
const _sfc_main = { name: "adr/0224-right-panel-tab-strip.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0224-right-panel-tab-strip-and-data-driven-add-menu" tabindex="-1">ADR 0224: Right Panel Tab Strip and Data-Driven Add Menu <a class="header-anchor" href="#adr-0224-right-panel-tab-strip-and-data-driven-add-menu" aria-label="Permalink to &quot;ADR 0224: Right Panel Tab Strip and Data-Driven Add Menu&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-11</li><li>Related issue: #229</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The right work panel used one header menu for both tool launchers and conversation-opened resources. That made open resources harder to scan, put close actions far from their tabs, and allowed the menu to become a second scrolling surface. The issue&#39;s HTML demo establishes the intended structure and interaction shape, but its simulated content is not an application data source.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>The work-panel header is a horizontally scrollable tab strip followed by a fixed, tight <code>+</code> trigger. The strip is the only horizontally scrolling region; the <code>+</code> remains visible. Every open Review, file, or plugin view is represented by one ARIA tab. Tabs activate on click, close on their hover/focus close button or middle-click, and support ArrowLeft/ArrowRight/Home/End plus Delete/Backspace. Closing the final tab keeps the panel open and shows a New launcher in the body.</p><p>The <code>+</code> menu has one <strong>Tools &amp; panels</strong> group. Review is the only host-owned entry; all other entries come from the current plugin-view metadata returned by <code>contributes.views</code>. The renderer must not hardcode bundled Files or Browser entries, and must not show shortcut labels unless the corresponding binding is actually registered. Open plugin views remain singleton tabs and reopening one activates its existing tab.</p><p>The viewport-fixed panel toggle remains the sole collapse control. It uses the two-state panel icon, <code>aria-pressed</code>, and a localized tooltip/accessibility name that includes the user&#39;s resolved <code>Mod+J</code> binding when one exists. The binding remains customizable through the existing keyboard-shortcut machinery.</p><p>The new-profile default panel width is 360px within the existing 244–720px range. A saved width is not rewritten by this change. No protocol, persisted tab schema, plugin manifest, or native-window reservation is introduced.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>The tab strip makes the active resource and its close affordance visible while preserving panel width and the existing native-surface occlusion boundary.</li><li>The New launcher gives an intentional next action after the last tab closes without changing the panel&#39;s session-scoped state model.</li><li>Plugin authors automatically receive a panel launcher whenever their scoped view contribution is available; the host&#39;s built-in list does not drift from plugin metadata.</li><li>The larger default gives the header enough room for the 166px Windows/Linux titlebar reservation while preserving existing user-selected widths.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0224-right-panel-tab-strip.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0224RightPanelTabStrip = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0224RightPanelTabStrip as default
};
