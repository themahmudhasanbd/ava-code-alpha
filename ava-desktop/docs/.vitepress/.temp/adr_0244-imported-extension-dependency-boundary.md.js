import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0244: Bound dependency installation for imported extensions","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0244-imported-extension-dependency-boundary.md","filePath":"adr/0244-imported-extension-dependency-boundary.md","lastUpdated":1789849710000}');
const _sfc_main = { name: "adr/0244-imported-extension-dependency-boundary.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0244-bound-dependency-installation-for-imported-extensions" tabindex="-1">ADR 0244: Bound dependency installation for imported extensions <a class="header-anchor" href="#adr-0244-bound-dependency-installation-for-imported-extensions" aria-label="Permalink to &quot;ADR 0244: Bound dependency installation for imported extensions&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-14</li><li>Decision: Imported pi extension dependencies use a registry-only, non-lifecycle npm boundary</li><li>Related: ADR 0214, ADR 0215, <code>07-plugins/16-trusted-extensions.md</code> §3.2</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The explicit <code>Plugins → Import pi extension</code> flow copies a local pi package and may install its declared dependencies before the first sidecar load. A normal <code>npm install</code> inherits user configuration and can resolve local paths, git repositories, private registries, proxies, or HTTP tarballs. <code>--ignore-scripts</code> alone does not provide a sufficient boundary, and a packaged PI-Desktop build does not ship a standalone Node/npm executable.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>The importer validates registry-only specs in <code>dependencies</code>, <code>optionalDependencies</code>, <code>devDependencies</code>, and <code>peerDependencies</code>, plus recursive <code>overrides</code>. Invalid values fail before npm starts.</li><li>npm lockfiles are accepted only when package locations, nested dependency specs, and every <code>resolved</code> URL are registry-safe. Unsupported lockfile formats are removed; unsafe lockfiles are not reused.</li><li>Real npm runs with a minimal environment: user/global config files are isolated, the registry is fixed to <code>https://registry.npmjs.org/</code>, git resolution is disabled, lifecycle scripts/audit/fund/update notifications are disabled, and npm uses a per-import cache removed after completion.</li><li>Real npm traffic goes through a loopback proxy that permits only <code>registry.npmjs.org</code> on ports 80/443. This blocks non-registry transitive HTTP(S) tarballs and redirects. Git sources are rejected by validation or the disabled git resolver.</li><li>Dependency resolution and installation are separate bounded commands: <code>npm install --package-lock-only</code> validates the complete resolved lockfile, then <code>npm ci</code> installs it. A failure removes partial <code>node_modules</code>, cache, and generated lockfiles while preserving a safe source lockfile when possible.</li><li>Imported copies omit credential files and repository metadata, create the destination directory atomically, and derive the plugin id from the final unique directory name. Missing npm or Node.js has a structured unavailable error and a Main-owned native executable picker recovery flow (spec §3.2). Cancellation or a dependency failure remains non-blocking for registration.</li><li>A validated user-selected npm path is saved atomically in Main-owned <code>&lt;dataDir&gt;/npm-path.json</code> and revalidated on future imports. Stale choices prompt again; save failure warns without preventing the current install. Validation uses bounded npm/Node version checks and the install child alone receives the selected directory in <code>PATH</code>. No shell startup probing, global environment mutation, or credential inheritance is introduced. Recovery retries the existing generated directory and registers it exactly once.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Private registries, private dependency URLs, git dependencies, local paths, npm aliases, and lifecycle-build dependencies are intentionally unsupported by this import path.</li><li>The explicit import flow still requires a user-trusted local extension and a usable Node.js/npm installation, either on <code>PATH</code> or explicitly selected from a trusted installation. Ordinary registry/network errors do not prompt for a different executable.</li><li>The main process owns the proxy and child-process lifecycle; dependency installation remains outside the renderer and sidecar.</li><li>The dependency install and full picker/provider journey remain separate E2E validation surfaces; deterministic boundary and cleanup tests cover the security contract.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0244-imported-extension-dependency-boundary.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0244ImportedExtensionDependencyBoundary = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0244ImportedExtensionDependencyBoundary as default
};
