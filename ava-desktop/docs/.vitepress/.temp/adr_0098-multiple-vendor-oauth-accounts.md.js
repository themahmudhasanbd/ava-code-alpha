import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0098: Treat every vendor OAuth account as an independent provider row","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0098-multiple-vendor-oauth-accounts.md","filePath":"adr/0098-multiple-vendor-oauth-accounts.md","lastUpdated":1787041580000}');
const _sfc_main = { name: "adr/0098-multiple-vendor-oauth-accounts.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0098-treat-every-vendor-oauth-account-as-an-independent-provider-row" tabindex="-1">ADR 0098: Treat every vendor OAuth account as an independent provider row <a class="header-anchor" href="#adr-0098-treat-every-vendor-oauth-account-as-an-independent-provider-row" aria-label="Permalink to &quot;ADR 0098: Treat every vendor OAuth account as an independent provider row&quot;">​</a></h1><ul><li>Status: Accepted for implementation</li><li>Date: 2026-08-18</li><li>Deciders: PI-Desktop core</li><li>Related: D240, ADR 0095, D027, D028, D031</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The first vendor-account implementation used the pi-ai vendor id as the local identity. Electron main kept one provider-row id and one <code>CredentialStore</code> per vendor, so a second sign-in could only reuse the first account. The renderer also showed OAuth rows in the AI provider list while a separate Vendor accounts card managed the same rows. Its sign-out action cleared the credential but left the provider row behind, which made the two surfaces disagree.</p><p>Multiple accounts are a real requirement: a user may have separate personal, work, or team subscriptions for one vendor. The account boundary must also be the security boundary, because refresh and access tokens must never cross from one account into another.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><strong>The provider row id is the account identity.</strong> Every successful login starts with a new <code>authKind: &quot;oauth&quot;</code> provider row, even if another row has the same <code>vendorKey</code>. Its OAuth credential is stored only at <code>secret:provider:&lt;providerId&gt;:oauth</code>.</li><li><strong>Each account gets a scoped pi-ai collection.</strong> Electron main creates one <code>MutableModels</code> instance and one <code>CredentialStore</code> for each provider row. The store accepts the builtin vendor id only inside that instance and translates it to the row-specific secret ref. Refresh serialization is keyed by the row id, so concurrent accounts cannot read-modify-write each other&#39;s token.</li><li><strong>Vendor accounts own OAuth row lifecycle.</strong> The Vendor accounts card lists every local account, allows the vendor picker to be selected repeatedly, and removes an account through <code>providers.delete</code>. Host-core therefore clears the row, OAuth secret, API-key secret, and secret metadata together. A stale row without a credential remains visible as <code>Needs sign-in</code> until the user removes it.</li><li><strong>AI services and vendor accounts are separate settings surfaces.</strong> The AI services list contains API-key and custom/no-auth services only. Connected OAuth rows remain available to the default-model selector and runtime, but are edited and deleted only from Vendor accounts.</li><li><strong>Runtime bindings use provider ids end to end.</strong> The sidecar binding table stores only the OAuth provider row ids allowed for a session. Its <code>provider.resolveAuth</code> request is checked against that set and main resolves the corresponding row-scoped collection. A vendor key is never used as an ambiguous account lookup at the sidecar boundary.</li><li><strong>Ambiguous subagent aliases fail closed.</strong> A subagent pin may use an exact provider id. A vendor/name alias is accepted only when it resolves to one provider row; duplicate vendor accounts require an exact row id rather than silently selecting the first account.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>The account list can contain several rows for one vendor and labels duplicate rows with a stable ordinal for scanning.</li><li>Removing the selected default account clears or repairs the default to the first remaining ready provider.</li><li>Existing OAuth rows remain readable after upgrade; the next sign-in creates a new row instead of mutating the existing one.</li><li>The IPC host protocol and SQLite schema do not need a new table or version. The renderer uses the Electron-main <code>providers/oauth/delete</code> invoke channel, which delegates to the existing host provider deletion contract.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><ul><li><strong>Keep one shared credential per vendor and add account labels.</strong> Rejected: the credential store and pi-ai model collection are vendor-keyed, so one account would overwrite or refresh the other.</li><li><strong>Keep OAuth rows in the generic AI services list.</strong> Rejected: it presents two owners for one object and makes deletion semantics unclear.</li><li><strong>Clear only the OAuth secret on account removal.</strong> Rejected: it leaves an unusable provider row, stale default identity, and a misleading service entry.</li></ul><h2 id="references" tabindex="-1">References <a class="header-anchor" href="#references" aria-label="Permalink to &quot;References&quot;">​</a></h2><ul><li><code>docs/adr/0095-vendor-account-oauth-login.md</code></li><li><code>docs/spec/03-runtime/11-provider-model-system.md</code> §8a and §17</li><li><code>docs/spec/03-runtime/12-provider-config-schema.md</code> §3 and §9</li><li><code>docs/spec/03-runtime/01-ipc-protocol.md</code> vendor accounts</li><li><code>docs/spec/04-ux/06-settings-ia.md</code> Model configuration</li><li><code>docs/spec/06-delivery/04-e2e-test-plan.md</code> E2E-151</li><li><code>apps/desktop/electron/main/oauth.ts</code></li><li><code>apps/desktop/electron/main/agent-sidecar.ts</code></li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0098-multiple-vendor-oauth-accounts.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0098MultipleVendorOauthAccounts = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0098MultipleVendorOauthAccounts as default
};
