import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0101: Model-aware image attachment transport","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0101-model-aware-image-attachments.md","filePath":"adr/0101-model-aware-image-attachments.md","lastUpdated":1789079251000}');
const _sfc_main = { name: "adr/0101-model-aware-image-attachments.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0101-model-aware-image-attachment-transport" tabindex="-1">ADR 0101: Model-aware image attachment transport <a class="header-anchor" href="#adr-0101-model-aware-image-attachment-transport" aria-label="Permalink to &quot;ADR 0101: Model-aware image attachment transport&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-08-18</li><li>Deciders: PI-Desktop runtime and desktop UI maintainers</li><li>Amends: D197, ADR 0059, ADR 0070</li><li>Amended by: D361 (inline bound is 10 MB, matching MiniMax&#39;s OpenAI-compatible cap), D392 / ADR 0218 (binding image-input overrides)</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Clipboard images were materialized successfully, but the composer reduced every file to <code>@&lt;path&gt;</code> text. A model that supports image input therefore received a filesystem reference instead of an image block. The capability signal must be owned by the same model catalog that pi-ai uses to serialize provider requests; renderer discovery and user-entered model ids are not sufficient evidence.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>Keep the composer draft compact and textual. Each file reference retains its kind, name, MIME type, and source path as structured metadata; the visible textarea never contains pasted binary data.</li><li>Resolve vision capability from the exact pi-ai model record. Only <code>model.input.includes(&quot;image&quot;)</code> enables image transport. Unknown and custom model ids stay on the conservative non-vision path, even when discovery metadata claims <code>vision</code>.</li><li>Electron main remains the attachment boundary. It validates every source path against the session scratch root, the session-bound project root, or the content-addressed attachment root. Images are stored as <code>attachments/&lt;sha256&gt;</code> and the durable <code>UiMessage</code> stores only the reference and metadata.</li><li>For a vision-capable model, an image within the 10 MB inline limit crosses the sidecar only as transient base64 data and becomes a pi-ai image content block. The base64 value is never persisted in SQLite, JSONL, or renderer transcript state.</li><li>For a non-vision model, an unknown model, or an image above the inline limit, the prompt receives a safe <code>@path</code> fallback. Replayed content-store images are copied into the session scratch <code>replayed/</code> directory before that fallback is exposed to the model.</li><li>The Composer shows a compact accessible status row when an image is attached: it states whether the selected model can receive visual input. The model picker and session/provider summaries expose the same authoritative vision capability.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>GPT/vision-capable models can inspect pasted images without requiring a model-specific renderer implementation.</li><li>Text-only and non-vision models retain the existing file-tool workflow.</li><li>Image bytes are deduplicated and can survive retry, fork, and runtime recreation without putting binary data in the transcript.</li><li>Attachment garbage collection remains a later storage task; references are content-addressed so it can be added without changing the message contract.</li><li>Full visual previews, drag-and-drop, image transforms, and provider-specific image limits remain out of scope. The 10 MB inline threshold is an app-side safety bound; providers may impose stricter limits and return their normal provider error.</li></ul><h2 id="rejected-alternatives" tabindex="-1">Rejected alternatives <a class="header-anchor" href="#rejected-alternatives" aria-label="Permalink to &quot;Rejected alternatives&quot;">​</a></h2><ul><li><strong>Always send <code>@path</code> text:</strong> preserves non-vision behavior but cannot satisfy vision models.</li><li><strong>Trust renderer/provider discovery flags:</strong> can advertise a capability that the runtime adapter cannot serialize and would make unknown models unsafe to classify.</li><li><strong>Persist base64 in messages:</strong> makes reloads and SQLite/JSONL growth unbounded and crosses the persistence boundary unnecessarily.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0101-model-aware-image-attachments.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0101ModelAwareImageAttachments = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0101ModelAwareImageAttachments as default
};
