import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0115: Keep plugin clipboard history host-owned and in memory","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0115-plugin-clipboard-history-is-host-owned.md","filePath":"adr/0115-plugin-clipboard-history-is-host-owned.md","lastUpdated":1788878032000}');
const _sfc_main = { name: "adr/0115-plugin-clipboard-history-is-host-owned.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0115-keep-plugin-clipboard-history-host-owned-and-in-memory" tabindex="-1">ADR 0115: Keep plugin clipboard history host-owned and in memory <a class="header-anchor" href="#adr-0115-keep-plugin-clipboard-history-host-owned-and-in-memory" aria-label="Permalink to &quot;ADR 0115: Keep plugin clipboard history host-owned and in memory&quot;">​</a></h1><ul><li>Status: Accepted (amended 2026-08-21)</li><li>Date: 2026-08-21</li><li>Related: Issue #9, ADR 0008, ADR 0059</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Plugins can read and write the current clipboard, but a plugin process has no Electron clipboard access. Clipboard-history plugins therefore used to poll <code>readText()</code> and maintain a second store, while the host polled the native clipboard to capture images. That work repeatedly read and encoded unchanged screenshots, consumed resources while idle, and still missed copies between samples.</p><p>Clipboard history is also more sensitive than the current clipboard: it may retain screenshots, credentials, or private documents. Giving each plugin its own recorder would duplicate data, make retention inconsistent, and weaken the permission boundary.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>The Electron main process maintains one rolling, in-memory history and exposes it to plugins as <code>pi.clipboard.getHistory()</code>. The API reuses <code>clipboard.read</code>, returns newest-first text and image entries, and audits every call with the entry count. Plugin processes receive only the typed result over the existing broker; they do not receive Electron objects or a new capability.</p><p>The host does not poll the system clipboard. It records text written through <code>writeText</code> and records content from the Composer&#39;s user-initiated <code>paste</code> event. The renderer passes the bytes it already received from that event to the host; the host never reads the OS clipboard again for the same paste. Images are normalized to PNG during that paste request only.</p><p>Consecutive identical content is collapsed with a refreshed timestamp. The history is cleared on application exit and is bounded to:</p><ul><li>30 days retention;</li><li>500 entries and 256 MiB total payload;</li><li>100 KiB of UTF-8 text or 50 MiB of PNG image bytes per entry.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Idle applications do not read or encode clipboard contents, and Composer paste handling does not perform a second native clipboard read.</li><li><code>getHistory()</code> contains host writes and content pasted into Composer; a copy that is never pasted is intentionally not captured. Content copied before the app starts is not recoverable.</li><li>The history remains host-owned, in-memory only, and protected by the existing <code>clipboard.read</code> permission. This limits privacy exposure without changing the plugin API.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0115-plugin-clipboard-history-is-host-owned.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0115PluginClipboardHistoryIsHostOwned = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0115PluginClipboardHistoryIsHostOwned as default
};
