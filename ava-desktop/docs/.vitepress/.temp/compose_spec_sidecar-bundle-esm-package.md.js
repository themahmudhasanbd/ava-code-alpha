import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Sidecar Bundle ESM package.json","description":"","frontmatter":{"feature":"sidecar-bundle-esm-package","status":"delivered","updated":"2026-09-17T00:00:00.000Z","branch":"fix/sidecar-bundle-esm-package","commits":"8d826433868f6bcc992c29bae2f799cc6de1e069..7d6890ebc7816675c31f707460efcf0601310e40"},"headers":[],"relativePath":"compose/spec/sidecar-bundle-esm-package.md","filePath":"compose/spec/sidecar-bundle-esm-package.md","lastUpdated":1789631624000}');
const _sfc_main = { name: "compose/spec/sidecar-bundle-esm-package.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="sidecar-bundle-esm-package-json" tabindex="-1">Sidecar Bundle ESM package.json <a class="header-anchor" href="#sidecar-bundle-esm-package-json" aria-label="Permalink to &quot;Sidecar Bundle ESM package.json&quot;">​</a></h1><h2 id="report" tabindex="-1">Report <a class="header-anchor" href="#report" aria-label="Permalink to &quot;Report&quot;">​</a></h2><p><strong>What was built</strong> — Packaged installs copy <code>packages/agent-runtime/dist-bundle</code> to <code>resources/agent-runtime</code> via electron-builder <code>extraResources</code>. The directory previously contained only the esbuild ESM <code>sidecar.js</code>, so Node treated the entry as CommonJS and agent runtime died at startup with <code>SyntaxError: Cannot use import statement outside a module</code> (issue #507). The <code>bundle</code> script now chains a write of <code>dist-bundle/package.json</code> (<code>{&quot;type&quot;:&quot;module&quot;}</code>) after esbuild. Desktop packaging and the sidecar spawn path are unchanged. Regression tests lock the extraResources directory-copy mapping and the bundle write-step contract (including a real execution of the write command in a temp dir).</p><p><strong>Verification</strong> —</p><ul><li><code>pnpm -C packages/shared build</code> — PASS</li><li><code>pnpm -C packages/agent-runtime bundle</code> — PASS; <code>dist-bundle/{sidecar.js,package.json}</code> with <code>&quot;type&quot;:&quot;module&quot;</code></li><li><code>node --test test/agent-runtime-bundle-package.test.mjs test/packaging-footprint.test.mjs</code> from <code>apps/desktop</code> — PASS 13/13</li><li>Independent review of <code>8d826433..7d6890eb</code> — no critical findings</li></ul><p><strong>Journey log</strong> —</p><ul><li>Issue #507’s “VPN” framing is not supported by logs: model discovery returned HTTP 401 (auth), while the agent path failed on sidecar ESM packaging. Scope kept to the packaging defect.</li><li>Fresh worktrees need <code>pnpm -C packages/shared build</code> before <code>agent-runtime</code> bundle; cold esbuild fails on missing shared <code>dist/</code> exports before the write step runs.</li><li>Unit tests intentionally skip full esbuild (shared rebuild + multi-second CPU); contract + write-step execution covers the packaging invariant, with full bundle verified out of band.</li><li>Desktop unit-test runner is <code>node --test test/*.test.mjs</code> — <code>pnpm --filter @pi-desktop/desktop test -- &lt;file&gt;</code> does not isolate a single file.</li></ul><h2 id="s1-problem" tabindex="-1">[S1] Problem <a class="header-anchor" href="#s1-problem" aria-label="Permalink to &quot;[S1] Problem&quot;">​</a></h2><p>Issue #507 (v0.14.8 Windows packaged install) shows the agent sidecar dying at startup:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>SyntaxError: Cannot use import statement outside a module</span></span>
<span class="line"><span>  at .../resources/agent-runtime/sidecar.js:1</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p><code>resources/agent-runtime/sidecar.js</code> is the esbuild ESM bundle copied from <code>packages/agent-runtime/dist-bundle</code> via electron-builder <code>extraResources</code>. <code>dist-bundle</code> contains only <code>sidecar.js</code>. Node&#39;s nearest-<code>package.json</code> lookup finds no <code>&quot;type&quot;: &quot;module&quot;</code>, so the <code>.js</code> entry is loaded as CommonJS and the ESM <code>import</code> banner fails. Development is unaffected because the repo package itself declares <code>&quot;type&quot;: &quot;module&quot;</code>.</p><p>This is independent of VPN and of the separate <code>model list request failed (401)</code> auth error in the same report.</p><h2 id="s2-design" tabindex="-1">[S2] Design <a class="header-anchor" href="#s2-design" aria-label="Permalink to &quot;[S2] Design&quot;">​</a></h2><ol><li>The <code>bundle</code> script in <code>packages/agent-runtime/package.json</code> must, after esbuild, write <code>dist-bundle/package.json</code> containing at least <code>{&quot;type&quot;:&quot;module&quot;}</code> so electron-builder ships it beside <code>sidecar.js</code>.</li><li>No change to <code>apps/desktop</code> <code>extraResources</code> mapping: the whole <code>dist-bundle</code> directory is already copied to <code>agent-runtime</code>.</li><li>No change to <code>agent-sidecar.ts</code> spawn path or entry filename.</li><li>Regression coverage must assert the packaging contract: <ul><li>after a successful bundle, <code>dist-bundle/package.json</code> exists and sets <code>&quot;type&quot;: &quot;module&quot;</code>;</li><li>desktop <code>extraResources</code> still sources <code>agent-runtime</code> from <code>../../packages/agent-runtime/dist-bundle</code> (so the new file is shipped).</li></ul></li></ol><h2 id="s3-out-of-scope" tabindex="-1">[S3] Out of Scope <a class="header-anchor" href="#s3-out-of-scope" aria-label="Permalink to &quot;[S3] Out of Scope&quot;">​</a></h2><ul><li>HTTP 401 / API-key UX for model discovery (user config; not this bug).</li><li>Issue #506 plugin ESM/CJS main generation.</li><li>Renaming the entry to <code>sidecar.mjs</code> or switching the bundle to CJS.</li><li>Proxy/VPN transport behavior.</li></ul><h2 id="tasks" tabindex="-1">Tasks <a class="header-anchor" href="#tasks" aria-label="Permalink to &quot;Tasks&quot;">​</a></h2><ul><li>[x] T1: Emit <code>dist-bundle/package.json</code> with <code>&quot;type&quot;:&quot;module&quot;</code> from the agent-runtime bundle script — acceptance: <code>pnpm -C packages/agent-runtime bundle</code> produces <code>dist-bundle/sidecar.js</code> and <code>dist-bundle/package.json</code> with <code>&quot;type&quot;:&quot;module&quot;</code> (covers: S2)</li><li>[x] T2: Add packaging regression tests for the ESM package.json contract — acceptance: tests fail without T1 and pass with it; they cover both the bundle output and the desktop extraResources source path (covers: S2; depends: T1)</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("compose/spec/sidecar-bundle-esm-package.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const sidecarBundleEsmPackage = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  sidecarBundleEsmPackage as default
};
