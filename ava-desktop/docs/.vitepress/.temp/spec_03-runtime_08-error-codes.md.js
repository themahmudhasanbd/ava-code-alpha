import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"08. Error Codes","description":"","frontmatter":{},"headers":[],"relativePath":"spec/03-runtime/08-error-codes.md","filePath":"spec/03-runtime/08-error-codes.md","lastUpdated":1790043493000}');
const _sfc_main = { name: "spec/03-runtime/08-error-codes.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="_08-error-codes" tabindex="-1">08. Error Codes <a class="header-anchor" href="#_08-error-codes" aria-label="Permalink to &quot;08. Error Codes&quot;">​</a></h1><blockquote><p>Source of truth: <code>packages/shared/src/errors.ts</code> (<code>ErrorCodes</code>). Codes in §3.7 are reserved (documented ahead of emission); everything else is live.</p></blockquote><h2 id="_1-goal" tabindex="-1">1. Goal <a class="header-anchor" href="#_1-goal" aria-label="Permalink to &quot;1. Goal&quot;">​</a></h2><p>Provide one stable error vocabulary across:</p><ul><li>Renderer UI</li><li>Electron IPC</li><li>Rust host RPC</li><li>Node pi sidecar bridge</li></ul><h2 id="_2-error-object" tabindex="-1">2. Error object <a class="header-anchor" href="#_2-error-object" aria-label="Permalink to &quot;2. Error object&quot;">​</a></h2><div class="language-ts vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">type</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> AppError</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  code</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">            // stable machine code, e.g. TOOL_DENIED</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  message</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">         // English UI/default message</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  details</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> unknown</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  retriable</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> boolean</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  source</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;renderer&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;electron&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;host&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;agent&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;plugin&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  causeCode</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">      // nested/transport code if mapped</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  traceId</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br></div></div><p>Rules:</p><ol><li><code>code</code> is immutable once published</li><li><code>message</code> is English source text (i18n key may map separately)</li><li>UI should prefer i18n key derived from <code>code</code> when available</li></ol><p>The desktop test suite (<code>apps/desktop/test/error-code-registry.test.mjs</code>) verifies that every <code>ErrorCodes</code> entry appears in this document and that every <code>errorCode</code> host-core emits from its RPC dispatcher and native tools is registered; reserved codes in §3.7 remain intentionally absent from <code>ErrorCodes</code> until an implementation emits them.</p><h2 id="_3-code-registry" tabindex="-1">3. Code registry <a class="header-anchor" href="#_3-code-registry" aria-label="Permalink to &quot;3. Code registry&quot;">​</a></h2><h3 id="_3-1-app-protocol" tabindex="-1">3.1 App / protocol <a class="header-anchor" href="#_3-1-app-protocol" aria-label="Permalink to &quot;3.1 App / protocol&quot;">​</a></h3><table tabindex="0"><thead><tr><th>code</th><th>retriable</th><th>meaning</th></tr></thead><tbody><tr><td><code>PROTOCOL_MISMATCH</code></td><td>no</td><td>handshake/protocol version mismatch</td></tr><tr><td><code>HOST_UNAVAILABLE</code></td><td>yes</td><td>Rust host not running/reachable</td></tr><tr><td><code>HOST_OVERLOADED</code></td><td>yes</td><td>bounded host RPC/tool capacity is full; retry after backpressure</td></tr><tr><td><code>AGENT_UNAVAILABLE</code></td><td>yes</td><td>pi sidecar not running/reachable</td></tr><tr><td><code>APP_DEGRADED</code></td><td>yes</td><td>app running with limited capabilities</td></tr><tr><td><code>INTERNAL</code></td><td>maybe</td><td>unexpected internal failure</td></tr><tr><td><code>INVALID_ARGUMENT</code></td><td>no</td><td>request schema/args invalid, including a native-tool path of the wrong file/directory kind</td></tr><tr><td><code>INVALID_PARAMS</code></td><td>no</td><td>host-core RPC parameter validation failed (numeric <code>1002</code>); the sidecar and renderer surface it unchanged</td></tr><tr><td><code>UNAUTHORIZED</code></td><td>no</td><td>capability/auth boundary rejected call</td></tr><tr><td><code>NOT_FOUND</code></td><td>no</td><td>entity not found</td></tr><tr><td><code>SESSION_NOT_FOUND</code></td><td>no</td><td>a session-scoped RPC (including <code>tools.execute</code>) named a session the host does not have; an unknown id never inherits the global workspace</td></tr><tr><td><code>CONFLICT</code></td><td>maybe</td><td>state conflict / busy resource</td></tr><tr><td><code>UNSUPPORTED</code></td><td>no</td><td>the operation has no implementation on this surface, e.g. a trusted-extension prompt while no desktop window can show it (spec 16 §9)</td></tr><tr><td><code>FORBIDDEN</code></td><td>no</td><td>RACP: the principal&#39;s roles do not admit the method (spec 19 §13)</td></tr><tr><td><code>METHOD_NOT_FOUND</code></td><td>no</td><td>RACP: unknown method on this Host, e.g. <code>host/list</code> without a Gateway</td></tr><tr><td><code>IDEMPOTENCY_CONFLICT</code></td><td>no</td><td>a queue or turn idempotency key was reused with different input (D386)</td></tr><tr><td><code>CURSOR_EXPIRED</code></td><td>no</td><td>RACP: the replay cursor is older than the retained window; resubscribe from a snapshot</td></tr><tr><td><code>CLIENT_TOO_SLOW</code></td><td>yes</td><td>RACP: the client fell behind the event stream and was disconnected</td></tr><tr><td><code>APPROVAL_EXPIRED</code></td><td>no</td><td>RACP: the approval deadline passed before an answer arrived</td></tr><tr><td><code>APPROVAL_STALE</code></td><td>no</td><td>RACP: the approval was already settled or belongs to an older turn</td></tr><tr><td><code>PAYLOAD_TOO_LARGE</code></td><td>no</td><td>RACP: a frame exceeded the negotiated size bound</td></tr><tr><td><code>TIMEOUT</code></td><td>yes</td><td>generic timeout</td></tr><tr><td><code>NETWORK_POLICY_BLOCKED</code></td><td>no</td><td>the main-process public-network guard refused a fetch because it <em>judged</em> the target: the URL failed the syntactic public-HTTPS check, or the local DNS lookup returned an address the policy classifies as non-public — including a fake-IP placeholder a local proxy invented (ADR 0243). A desktop-only code; a refusal is a verdict, so retrying cannot succeed until the address changes. A resolver that returned no answer at all is <code>NETWORK_RESOLVE_FAILED</code> instead (issue #419).</td></tr><tr><td><code>NETWORK_RESOLVE_FAILED</code></td><td>yes</td><td>the main-process public-network guard could not classify the target host: the local DNS lookup returned no answer, or threw before returning one. The request is refused exactly as a policy refusal is, but no address was judged, so no page or log may report it as an address-check decision. Distinct from <code>NETWORK_ERROR</code>, which is a failure of the request itself. Retriable: a resolver or proxy that starts answering the same host makes the same request succeed (ADR 0243, issue #419).</td></tr><tr><td><code>HOST_SHUTTING_DOWN</code></td><td>yes</td><td>the host received EOF and is draining; the call was refused rather than started</td></tr><tr><td><code>RATE_LIMITED</code></td><td>yes</td><td>a per-caller host budget (plugin session import, batch operations) was exceeded inside its window</td></tr><tr><td><code>LIMIT_EXCEEDED</code></td><td>no</td><td>a payload exceeded a fixed host bound (item count, byte size, or a 64 MiB NDJSON request line) and was refused</td></tr><tr><td><code>CONFIG_SYNC_INVALID</code></td><td>no</td><td>invalid sync configuration, password, path, request, or approval input</td></tr><tr><td><code>CONFIG_SYNC_LOCKED</code></td><td>no</td><td>the local encrypted sync vault is not unlocked</td></tr><tr><td><code>CONFIG_SYNC_UNSUPPORTED</code></td><td>no</td><td>the vault format or WebDAV server capability is unsupported</td></tr><tr><td><code>CONFIG_SYNC_REMOTE</code></td><td>maybe</td><td>remote WebDAV object, authentication, quota, or availability failure</td></tr><tr><td><code>CONFIG_SYNC_CONFLICT</code></td><td>maybe</td><td>remote head, vault identity, or approval digest conflict</td></tr><tr><td><code>CONFIG_SYNC_CRYPTO</code></td><td>no</td><td>authenticated encryption, object identity, or ciphertext validation failed</td></tr><tr><td><code>CONFIG_SYNC_MAPPING_REQUIRED</code></td><td>no</td><td>imported project-scoped configuration needs an explicit local folder/group mapping</td></tr><tr><td><code>CONFIG_SYNC_LIMIT_EXCEEDED</code></td><td>no</td><td>encrypted sync state exceeded an entity, object, resource, archive, or decompression bound</td></tr></tbody></table><p><code>HOST_UNAVAILABLE</code> is reserved for a missing or broken host process/transport, not ordinary admission pressure. RPC capacity returns <code>HOST_OVERLOADED</code>, and an admitted shell that cannot start because the OS is temporarily out of process resources returns <code>PROCESS_RESOURCE_EXHAUSTED</code>. Host-core&#39;s control stdio is isolated from Tokio&#39;s dynamic blocking pool so the latter condition does not turn temporary thread pressure into a host process exit.</p><h3 id="_3-2-agent-session" tabindex="-1">3.2 Agent / session <a class="header-anchor" href="#_3-2-agent-session" aria-label="Permalink to &quot;3.2 Agent / session&quot;">​</a></h3><table tabindex="0"><thead><tr><th>code</th><th>retriable</th><th>meaning</th></tr></thead><tbody><tr><td><code>AGENT_BUSY</code></td><td>no</td><td>session already has active turn; leftover subagents after a terminal parent error do not keep the session busy (D352)</td></tr><tr><td><code>AGENT_NOT_FOUND</code></td><td>no</td><td>session missing</td></tr><tr><td><code>TURN_NOT_FOUND</code></td><td>no</td><td>turn id invalid</td></tr><tr><td><code>TURN_ABORTED</code></td><td>no</td><td>turn aborted by user/system</td></tr><tr><td><code>MODEL_NOT_CONFIGURED</code></td><td>no</td><td>no usable model selected, or provider rejects the selected model as unknown</td></tr><tr><td><code>PROVIDER_ERROR</code></td><td>yes</td><td>upstream provider failure; a retryable one (5xx gateway) gets up to ten same-turn retries, a malformed 400/422 request is terminal</td></tr><tr><td><code>PROVIDER_UNAUTHORIZED</code></td><td>no</td><td>bad/missing provider credentials</td></tr><tr><td><code>PROVIDER_RATE_LIMITED</code></td><td>yes</td><td>provider rate limited; runtime silently retries up to ten times across setup/stream before the terminal event</td></tr><tr><td><code>CONTEXT_TOO_LARGE</code></td><td>no</td><td>prompt/context still exceeds the safe model budget after recovery, the second provider overflow occurred, or automatic recovery is disabled</td></tr><tr><td><code>CONTEXT_COMPACTION_FAILED</code></td><td>no</td><td>automatic retained-tail recovery could not prepare, persist, or fit a checkpoint, or manual checkpoint summary generation / durable append failed; the guarded next provider request does not start</td></tr><tr><td><code>STREAM_FAILED</code></td><td>yes</td><td>provider stream was terminated, closed prematurely, or otherwise ended before a complete response; up to ten same-turn retries may precede the terminal event</td></tr><tr><td><code>EMPTY_MODEL_RESPONSE</code></td><td>yes</td><td>the model ended its turn with no tool call and no visible text twice: once as streamed, once after the automatic re-run; the first reply to a Host-ledger completion notice is exempt (spec 02-agent-runtime §5e, D446)</td></tr><tr><td><code>PROMPT_ENHANCEMENT_EMPTY</code></td><td>no</td><td>the one-shot enhancement model returned no text</td></tr><tr><td><code>SPEECH_NOT_CONFIGURED</code></td><td>no</td><td>host speech ASR or TTS is not bound in settings</td></tr><tr><td><code>SPEECH_PROTOCOL_UNSUPPORTED</code></td><td>no</td><td>the speech protocol is unknown or does not support this role</td></tr><tr><td><code>SPEECH_INPUT_TOO_LARGE</code></td><td>no</td><td>speech input exceeds 25 MB</td></tr><tr><td><code>SUBAGENT_IDLE_TIMEOUT</code></td><td>no</td><td>withdrawn (D328): idle watchdogs are not armed; the code remains for stored results</td></tr><tr><td><code>SUBAGENT_DURATION_TIMEOUT</code></td><td>no</td><td>withdrawn (D328): duration watchdogs are not armed; the code remains for stored results</td></tr><tr><td><code>SUBAGENT_CONTEXT_OVERFLOW</code></td><td>no</td><td>a delegate&#39;s own model context exceeded its safe budget and neither automatic turn-boundary compaction nor the degraded retry that keeps only the task brief and the most recent messages brought it back below the limit; the failure names the actionable recovery instead of the provider&#39;s overflow text</td></tr></tbody></table><h3 id="_3-3-workspace-tools-permissions" tabindex="-1">3.3 Workspace / tools / permissions <a class="header-anchor" href="#_3-3-workspace-tools-permissions" aria-label="Permalink to &quot;3.3 Workspace / tools / permissions&quot;">​</a></h3><table tabindex="0"><thead><tr><th>code</th><th>retriable</th><th>meaning</th></tr></thead><tbody><tr><td><code>WORKSPACE_REQUIRED</code></td><td>no</td><td>no workspace bound</td></tr><tr><td><code>PATH_OUTSIDE_WORKSPACE</code></td><td>no</td><td>path escapes sandbox before an explicit outside-path permission decision, or a prompt attachment is outside its session scratch/project/attachment roots</td></tr><tr><td><code>WORKSPACE_PATH_DENIED</code></td><td>no</td><td>an explicit <code>Read</code>/<code>Write</code>/<code>Edit</code> path hit the always-on security denylist (private keys, <code>.env</code> files, credential bundles, <code>.git/objects</code>); an outside-path grant does not lift it (spec 15 §3)</td></tr><tr><td><code>READ_PATH_IS_DIRECTORY</code></td><td>no</td><td><code>Read</code> was given a directory; the result carries a <code>Glob</code> suggestion</td></tr><tr><td><code>TOOL_BINARY_CONTENT</code></td><td>no</td><td><code>Read</code> refused to dump a binary file into the model context</td></tr><tr><td><code>TOOL_NOT_FOUND</code></td><td>no</td><td>unknown tool</td></tr><tr><td><code>TOOL_DENIED</code></td><td>no</td><td>permission denied / mode forbidden</td></tr><tr><td><code>TOOL_TIMEOUT</code></td><td>yes</td><td>tool execution timeout</td></tr><tr><td><code>TOOL_FAILED</code></td><td>maybe</td><td>tool executed but failed</td></tr><tr><td><code>TOOL_ABORTED</code></td><td>no</td><td>the tool was cancelled by a user stop or a turn abort before it finished</td></tr><tr><td><code>MUTATION_RETRY_BUDGET_EXHAUSTED</code></td><td>yes</td><td>the repeat guard ended the turn after same-path <code>Edit</code> or shell patch failures; carries <code>details.kind</code> (<code>edit</code> or <code>patch-command</code>), the last tool error code, and a class-specific <code>details.recovery</code> hint</td></tr><tr><td><code>PROCESS_RESOURCE_EXHAUSTED</code></td><td>yes</td><td>shell process could not start because the OS temporarily exhausted process resources</td></tr><tr><td><code>SHELL_NOT_FOUND</code></td><td>no</td><td>no effective platform shell is available after catalog fallback; message carries guidance</td></tr><tr><td><code>COMMAND_SHELL_CHANGED</code></td><td>no</td><td>pinned shell ID or dialect changed before execution</td></tr><tr><td><code>COMMAND_SHELL_INVALID</code></td><td>no</td><td>settings supplied an unknown, unavailable, or wrong-platform shell ID</td></tr><tr><td><code>PERMISSION_TIMEOUT</code></td><td>no</td><td>permission prompt timed out (mapped to deny)</td></tr><tr><td><code>PERMISSION_REQUIRED</code></td><td>no</td><td>waiting for user decision</td></tr><tr><td><code>WRITE_DISABLED_IN_PLAN</code></td><td>no</td><td>contract-mode hard-deny for Write</td></tr><tr><td><code>EDIT_DISABLED_IN_PLAN</code></td><td>no</td><td>contract-mode hard-deny for Edit</td></tr><tr><td><code>PLUGIN_DISABLED_IN_PLAN</code></td><td>no</td><td>contract-mode hard-deny for every plugin tool</td></tr><tr><td><code>TOOL_DISABLED_IN_PLAN</code></td><td>no</td><td>contract-mode hard-deny for an unknown/unlisted tool</td></tr><tr><td><code>PLAN_NOT_ACTIVE</code></td><td>no</td><td>a submit tool ran while no contract was being negotiated</td></tr><tr><td><code>PLAN_KIND_MISMATCH</code></td><td>no</td><td><code>SubmitPlan</code> in Goal mode, or <code>SubmitGoal</code> in Plan mode</td></tr><tr><td><code>PLAN_APPROVAL_REQUIRED</code></td><td>no</td><td>SubmitPlan/SubmitGoal is waiting for a separate approval</td></tr><tr><td><code>PLAN_APPROVAL_TIMEOUT</code></td><td>no</td><td>absolute 30-minute plan approval deadline expired</td></tr><tr><td><code>PLAN_APPROVAL_STALE</code></td><td>no</td><td>response does not match the live proposal/session/turn/tool-call/version</td></tr><tr><td><code>PLAN_APPROVAL_INTERRUPTED</code></td><td>no</td><td>pending approval closed during abort, crash, or persistence failure</td></tr><tr><td><code>PLAN_ARTIFACT_WRITE_FAILED</code></td><td>no</td><td>host could not write exact bytes to a new <code>.pi/&lt;kind&gt;/*.md</code> artifact</td></tr><tr><td><code>PLAN_EXECUTION_INTERRUPTED</code></td><td>no</td><td>approved queued/running Plan or Goal execution stopped without replay</td></tr><tr><td><code>PLAN_REQUIRES_INTERACTIVE_SESSION</code></td><td>no</td><td>unattended/scheduled Plan or Goal run cannot request approval</td></tr><tr><td><code>PLAN_NOT_FOUND</code></td><td>no</td><td>no approval row matches the proposal id</td></tr><tr><td><code>PLAN_SESSION_NOT_FOUND</code></td><td>no</td><td>the Plan/Goal RPC named a session the host does not have</td></tr><tr><td><code>PLAN_WORKSPACE_REQUIRED</code></td><td>no</td><td>the session has no persisted project; temporary sessions cannot enter Plan or Goal</td></tr><tr><td><code>PLAN_ALREADY_ACTIVE</code></td><td>no</td><td>the session already has a contract being negotiated</td></tr><tr><td><code>PLAN_ALREADY_PENDING</code></td><td>no</td><td>a submit arrived while an approval for the same turn is still pending</td></tr><tr><td><code>PLAN_ALREADY_RESOLVED</code></td><td>no</td><td>a second approve/reject reached an already-resolved approval</td></tr><tr><td><code>PLAN_APPROVAL_CONFLICT</code></td><td>no</td><td>the approval row changed underneath a version-guarded update</td></tr><tr><td><code>PLAN_INVALID_ACTION</code></td><td>no</td><td>the approval response is neither <code>approve</code> nor <code>reject</code></td></tr><tr><td><code>PLAN_INVALID_ARGUMENT</code></td><td>no</td><td>submit/resolve arguments failed validation</td></tr><tr><td><code>PLAN_PERMISSION_MODE_REQUIRED</code></td><td>no</td><td>approve did not select <code>ask</code>, <code>accept-edits</code>, or <code>auto</code></td></tr><tr><td><code>PLAN_PERMISSION_MODE_INVALID</code></td><td>no</td><td>the selected permission mode is not one of the three</td></tr><tr><td><code>PLAN_MARKDOWN_TOO_LARGE</code></td><td>no</td><td>the submitted Markdown exceeds the artifact size bound</td></tr><tr><td><code>PLAN_REJECTED</code></td><td>no</td><td>the user rejected the proposal; the turn ends without execution</td></tr><tr><td><code>PLAN_SUBMIT_FAILED</code></td><td>maybe</td><td>the host could not record the proposal</td></tr><tr><td><code>PLAN_CONFIGURATION_BLOCKED</code></td><td>no</td><td><code>session.configure</code> was refused while a proposal or execution is live</td></tr><tr><td><code>PLAN_ARTIFACT_INVALID</code></td><td>no</td><td>the checkpoint artifact failed validation before execution</td></tr><tr><td><code>PLAN_ARTIFACT_NOT_READY</code></td><td>no</td><td>execution was claimed before the artifact was durably written</td></tr><tr><td><code>PLAN_ARTIFACT_PATH_UNSAFE</code></td><td>no</td><td>the artifact path escaped <code>&lt;workspaceRoot&gt;/.pi/&lt;kind&gt;/</code></td></tr><tr><td><code>PLAN_ARTIFACT_COLLISION_LIMIT</code></td><td>no</td><td>the host ran out of unique artifact names</td></tr><tr><td><code>PLAN_ARTIFACT_HASH_MISMATCH</code></td><td>no</td><td>artifact bytes no longer match the recorded hash at execution time</td></tr><tr><td><code>PLAN_EXECUTION_ACTIVE</code></td><td>no</td><td>an approved execution is already running for the session</td></tr><tr><td><code>PLAN_EXECUTION_NOT_FOUND</code></td><td>no</td><td>no queued execution matches the claim</td></tr><tr><td><code>PLAN_EXECUTION_ALREADY_CLAIMED</code></td><td>no</td><td>another claimant took the queued execution first</td></tr><tr><td><code>PLAN_EXECUTION_STALE</code></td><td>no</td><td>the execution epoch no longer matches the live session</td></tr><tr><td><code>PLAN_EXECUTION_STATUS_INVALID</code></td><td>no</td><td>a status transition was not allowed from the current state</td></tr><tr><td><code>PLAN_EXECUTION_CONFLICT</code></td><td>no</td><td>the execution row changed underneath a version-guarded update</td></tr><tr><td><code>PLAN_EXECUTION_FAILED</code></td><td>maybe</td><td>the approved execution ended in an error</td></tr><tr><td><code>PLAN_INTERNAL</code></td><td>maybe</td><td>a Plan/Goal host failure with no finer classification</td></tr><tr><td><code>WRITE_DISABLED_IN_CHAT</code></td><td>no</td><td>historical (pre-D188 Chat profile); registered for stored transcripts, no longer emitted</td></tr><tr><td><code>BASH_DISABLED_IN_CHAT</code></td><td>no</td><td>historical (pre-D188 Chat profile); registered for stored transcripts, no longer emitted</td></tr></tbody></table><p>The <code>_IN_PLAN</code> suffix and the <code>PLAN_</code> prefix are historical: both contract modes (Plan and Goal) share these codes rather than duplicating a <code>_IN_GOAL</code> set (<strong>D198</strong>). The renderer picks its wording from the proposal&#39;s <code>kind</code>, so one code can surface as either &quot;Plan&quot; or &quot;Goal&quot; copy.</p><h3 id="_3-4-edit-contract-adr-0087" tabindex="-1">3.4 Edit contract (ADR 0087) <a class="header-anchor" href="#_3-4-edit-contract-adr-0087" aria-label="Permalink to &quot;3.4 Edit contract (ADR 0087)&quot;">​</a></h3><p>Emitted only by <code>Edit</code>. Version and provenance failures have their own codes because each names a different next action; reporting them as <code>TOOL_FAILED</code> loses that. See <a href="./18-line-anchored-edit-contract">18-line-anchored-edit-contract</a> §11.</p><table tabindex="0"><thead><tr><th>code</th><th>retriable</th><th>meaning</th></tr></thead><tbody><tr><td><code>EDIT_TAG_REQUIRED</code></td><td>no</td><td><code>tag</code> missing or not 4 hex digits</td></tr><tr><td><code>EDIT_TAG_MISMATCH</code></td><td>yes after a <code>Read</code></td><td>tag does not hash the live file and drift recovery declined; carries the live tag and current content at the anchors</td></tr><tr><td><code>EDIT_TAG_UNKNOWN</code></td><td>yes after a <code>Read</code></td><td>tag is well-formed but the session recorded no such content for the path</td></tr><tr><td><code>EDIT_LINES_UNSEEN</code></td><td>yes</td><td>anchors reference lines the session never displayed; carries the revealed content</td></tr><tr><td><code>EDIT_PARSE_FAILED</code></td><td>no</td><td>malformed op header, body row under a colonless header, missing body, or a <code>-</code>/context row; the host message identifies the required syntax when possible</td></tr><tr><td><code>EDIT_RANGE_INVALID</code></td><td>no</td><td>reversed range, out-of-bounds line, overlapping ops, or duplicate anchor</td></tr><tr><td><code>EDIT_BLOCK_UNRESOLVED</code></td><td>no</td><td>a <code>N*</code> locator did not resolve; message names the plain-range alternative</td></tr><tr><td><code>EDIT_REGISTER_EMPTY</code></td><td>no</td><td>paste from an unset register</td></tr><tr><td><code>EDIT_REGISTER_AMBIGUOUS</code></td><td>no</td><td>anonymous paste with more than one pending anonymous capture</td></tr><tr><td><code>EDIT_REPAIR_AMBIGUOUS</code></td><td>no</td><td>boundary-repair candidates tied at minimum cost</td></tr><tr><td><code>EDIT_NO_CHANGE</code></td><td>no</td><td>the apply produced text identical to the input</td></tr><tr><td><code>EDIT_AMPLIFICATION_LIMIT</code></td><td>no</td><td>lowering exceeded the expansion cap</td></tr></tbody></table><p><code>EDIT_LINES_UNSEEN</code> is retriable <strong>without</strong> a further <code>Read</code> when its message reports a complete reveal: the revealed lines are merged into the session&#39;s provenance, so the same <code>tag</code> retried unchanged applies. A truncated reveal merges nothing and requires the re-read.</p><p><code>EDIT_TAG_MISMATCH</code>, <code>EDIT_TAG_UNKNOWN</code>, and <code>EDIT_LINES_UNSEEN</code> each get one free attempt per path before the repeat guard counts them, because each already carries what the retry needs. The remaining codes count on first occurrence, and the failure that exhausts the budget surfaces as §3.3&#39;s <code>MUTATION_RETRY_BUDGET_EXHAUSTED</code> on the assistant row (<a href="./18-line-anchored-edit-contract">18-line-anchored-edit-contract</a> §9.3). Its <code>details.recovery</code> value distinguishes syntax correction from the fresh-read path, so a follow-up does not blindly re-read a file when the payload itself is malformed.</p><h3 id="_3-5-secrets-settings" tabindex="-1">3.5 Secrets / settings <a class="header-anchor" href="#_3-5-secrets-settings" aria-label="Permalink to &quot;3.5 Secrets / settings&quot;">​</a></h3><table tabindex="0"><thead><tr><th>code</th><th>retriable</th><th>meaning</th></tr></thead><tbody><tr><td><code>PROVIDER_SECRET_MISSING</code></td><td>no</td><td>enabled provider requires an API key</td></tr><tr><td><code>MODEL_ALIAS_TOO_LONG</code></td><td>no</td><td>configured model alias exceeds 60 Unicode characters</td></tr><tr><td><code>MODEL_BINDINGS_DEGRADED</code></td><td>no</td><td>stored model bindings are unreadable; explicit model-array replacement is blocked to prevent data loss</td></tr><tr><td><code>SECRET_STORE_UNAVAILABLE</code></td><td>maybe</td><td>OS secure storage unavailable (reserved)</td></tr><tr><td><code>SETTINGS_INVALID</code></td><td>no</td><td>settings payload invalid (reserved)</td></tr></tbody></table><h3 id="_3-6-plugins" tabindex="-1">3.6 Plugins <a class="header-anchor" href="#_3-6-plugins" aria-label="Permalink to &quot;3.6 Plugins&quot;">​</a></h3><table tabindex="0"><thead><tr><th>code</th><th>retriable</th><th>meaning</th></tr></thead><tbody><tr><td><code>PLUGIN_NOT_FOUND</code></td><td>no</td><td>plugin id missing</td></tr><tr><td><code>PLUGIN_INVALID</code></td><td>no</td><td>manifest/package invalid</td></tr><tr><td><code>PLUGIN_LOAD_FAILED</code></td><td>maybe</td><td>enable/load failed</td></tr><tr><td><code>PLUGIN_DISABLED</code></td><td>no</td><td>plugin disabled (reserved)</td></tr><tr><td><code>PLUGIN_PERMISSION_DENIED</code></td><td>no</td><td>plugin lacks the declared and granted permission the call needs</td></tr><tr><td><code>PLUGIN_INTEGRITY</code></td><td>no</td><td>package checksum or signature did not match the catalog entry</td></tr><tr><td><code>PLUGIN_NETWORK</code></td><td>yes</td><td>marketplace download or catalog fetch failed</td></tr><tr><td><code>PLUGIN_HOST_TOO_OLD</code></td><td>no</td><td>the package&#39;s <code>engines.piDesktop</code> range excludes this host</td></tr><tr><td><code>PLUGIN_MARKET_INVALID</code></td><td>no</td><td>the marketplace catalog is malformed or missing required release fields</td></tr><tr><td><code>PLUGIN_MARKET_UNTRUSTED_HOST</code></td><td>no</td><td>the catalog or package URL is outside the trusted marketplace hosts</td></tr><tr><td><code>PLUGIN_MARKET_YANKED</code></td><td>no</td><td>the requested release was withdrawn from the catalog</td></tr><tr><td><code>PLUGIN_MARKET_NOT_PUBLISHED</code></td><td>no</td><td>the platform has the version and is not offering it yet</td></tr><tr><td><code>PLUGIN_MARKET_ARCHIVED</code></td><td>no</td><td>the plugin was withdrawn from the platform</td></tr><tr><td><code>PLUGIN_MARKET_NOT_FOUND</code></td><td>no</td><td>the platform does not have that plugin or version</td></tr><tr><td><code>PLUGIN_MARKET_RATE_LIMITED</code></td><td>yes</td><td>the download endpoint asked the client to wait</td></tr><tr><td><code>PLUGIN_MARKET_NO_SOURCE</code></td><td>maybe</td><td>no distribution target can serve the package</td></tr><tr><td><code>PLUGIN_CANCELLED</code></td><td>no</td><td>the user cancelled an install while it was downloading</td></tr><tr><td><code>MCP_INVALID</code></td><td>no</td><td>a user MCP server definition failed validation</td></tr><tr><td><code>SKILL_INVALID</code></td><td>no</td><td>a user skill document failed validation</td></tr><tr><td><code>SUBAGENT_INVALID</code></td><td>no</td><td>a user subagent document failed validation</td></tr><tr><td><code>CAPABILITY_INVALID</code></td><td>no</td><td>an agent capability root or scope setting failed validation</td></tr><tr><td><code>PLUGIN_COMMAND_NOT_FOUND</code></td><td>no</td><td>command id missing (reserved)</td></tr><tr><td><code>PLUGIN_CRASHED</code></td><td>yes</td><td>plugin runtime crashed (reserved)</td></tr><tr><td><code>PLUGIN_CONTRACT_MISMATCH</code></td><td>no</td><td>unsupported manifest/api version (reserved)</td></tr></tbody></table><h3 id="_3-7-reserved-detail-codes-not-yet-emitted" tabindex="-1">3.7 Reserved detail codes (not yet emitted) <a class="header-anchor" href="#_3-7-reserved-detail-codes-not-yet-emitted" aria-label="Permalink to &quot;3.7 Reserved detail codes (not yet emitted)&quot;">​</a></h3><p>Finer-grained provider/tool distinctions documented for future mapping. Until emitted, implementations use the canonical parent code shown.</p><table tabindex="0"><thead><tr><th>reserved code</th><th>canonical parent today</th><th>notes</th></tr></thead><tbody><tr><td><code>PROVIDER_BASE_URL_INVALID</code></td><td><code>PROVIDER_ERROR</code></td><td>endpoint invalid (400)</td></tr><tr><td><code>PROVIDER_PROTOCOL_MISMATCH</code></td><td><code>PROVIDER_ERROR</code></td><td>wrong protocol profile</td></tr><tr><td><code>PROVIDER_MODEL_NOT_FOUND</code></td><td><code>MODEL_NOT_CONFIGURED</code></td><td>unknown model id (404)</td></tr><tr><td><code>PROVIDER_TIMEOUT</code></td><td><code>TIMEOUT</code></td><td>network/server timeout (retriable)</td></tr><tr><td><code>PROVIDER_UNSUPPORTED_CAPABILITY</code></td><td><code>PROVIDER_ERROR</code></td><td>tools/vision unsupported</td></tr><tr><td><code>PROVIDER_DISABLED</code></td><td><code>MODEL_NOT_CONFIGURED</code></td><td>provider disabled</td></tr></tbody></table><p><code>WORKSPACE_PATH_DENIED</code> and <code>TOOL_BINARY_CONTENT</code> left this table when the host started emitting them (§3.3).</p><p>Historical aliases (never use in new code): <code>PROVIDER_AUTH_FAILED</code> → <code>PROVIDER_UNAUTHORIZED</code>; <code>PROVIDER_STREAM_INTERRUPTED</code> → <code>STREAM_FAILED</code>; <code>WORKSPACE_OUTSIDE_ROOT</code> → <code>PATH_OUTSIDE_WORKSPACE</code>; <code>SECRET_MISSING</code> → <code>PROVIDER_SECRET_MISSING</code>; <code>SHELL_UNAVAILABLE</code> → <code>SHELL_NOT_FOUND</code>; <code>SHELL_IDENTITY_STALE</code> → <code>COMMAND_SHELL_CHANGED</code>; <code>PLAN_APPROVAL_EXPIRED</code> → <code>PLAN_APPROVAL_TIMEOUT</code>. Truncation is not an error: a bounded tool result carries a marker naming which end survived and where the rest is, or reports the bounded window in sibling result fields (see <a href="./16-tool-result-limits">16-tool-result-limits</a>).</p><h3 id="_3-8-remote-control-racp-ws-ssh-bootstrap" tabindex="-1">3.8 Remote control (RACP-WS / SSH bootstrap) <a class="header-anchor" href="#_3-8-remote-control-racp-ws-ssh-bootstrap" aria-label="Permalink to &quot;3.8 Remote control (RACP-WS / SSH bootstrap)&quot;">​</a></h3><p>Emitted by the desktop&#39;s remote-host client and the <code>pi-host</code> server when a session lives on a paired remote machine driven over <code>RACP-WS</code> (see <a href="./19-remote-agent-control-protocol">19-remote-agent-control-protocol</a>, <a href="./../05-security/02-remote-control-security">../05-security/02-remote-control-security</a>, ADR 0285). The renderer never sees the local/remote split beyond a badge; these codes surface through the same error object as any other call.</p><table tabindex="0"><thead><tr><th>code</th><th>retriable</th><th>meaning</th></tr></thead><tbody><tr><td><code>HOST_DISCONNECTED</code></td><td>yes</td><td>the remote host connection dropped; in-flight calls are rejected and the client reconnects and resubscribes by cursor</td></tr><tr><td><code>HOST_BOOTSTRAP_FAILED</code></td><td>no</td><td>provisioning the remote <code>pi-host</code> over SSH failed (download, checksum mismatch, or <code>install.sh</code>); <code>details.reason</code> names the stage</td></tr><tr><td><code>HOST_VERSION_MISMATCH</code></td><td>no</td><td>the remote <code>pi-host</code> version does not match the desktop; the desktop refuses to drive an incompatible host</td></tr><tr><td><code>REMOTE_AUTH_FAILED</code></td><td>no</td><td>the device or pairing token was rejected on the RACP-WS upgrade</td></tr><tr><td><code>REMOTE_CONNECTION_FAILED</code></td><td>yes</td><td>the RACP-WS transport could not connect (non-loopback URL, refused socket)</td></tr><tr><td><code>REMOTE_FORWARD_FAILED</code></td><td>yes</td><td>the SSH loopback port forward could not be established</td></tr><tr><td><code>REMOTE_PATH_NOT_FOUND</code></td><td>no</td><td>a remote project/workspace path does not exist on the host</td></tr><tr><td><code>REMOTE_PATH_FORBIDDEN</code></td><td>no</td><td>a remote path is outside the host&#39;s permitted roots</td></tr><tr><td><code>PAIRING_FAILED</code></td><td>no</td><td><code>connection/pair</code> could not mint a device credential</td></tr><tr><td><code>PAIRING_TOKEN_EXPIRED</code></td><td>no</td><td>the single-use pairing token expired before pairing completed</td></tr><tr><td><code>CAPABILITY_UNAVAILABLE</code></td><td>no</td><td>an operation was requested for a capability the host advertised as unavailable (e.g. attachments, tool relay)</td></tr></tbody></table><h2 id="_4-mapping-rules" tabindex="-1">4. Mapping rules <a class="header-anchor" href="#_4-mapping-rules" aria-label="Permalink to &quot;4. Mapping rules&quot;">​</a></h2><h3 id="host-rpc-numeric-→-apperror-code" tabindex="-1">Host RPC numeric → AppError.code <a class="header-anchor" href="#host-rpc-numeric-→-apperror-code" aria-label="Permalink to &quot;Host RPC numeric → AppError.code&quot;">​</a></h3><p>See <code>06-host-rpc-protocol.md</code> numeric table.<br> Example: host <code>1004</code> → <code>TOOL_DENIED</code>.</p><h3 id="provider-exceptions" tabindex="-1">Provider exceptions <a class="header-anchor" href="#provider-exceptions" aria-label="Permalink to &quot;Provider exceptions&quot;">​</a></h3><p>Node sidecar maps provider SDK errors into:</p><ul><li><code>PROVIDER_UNAUTHORIZED</code></li><li><code>PROVIDER_RATE_LIMITED</code></li><li><code>MODEL_NOT_CONFIGURED</code> (provider rejects the selected model with 404)</li><li><code>PROVIDER_ERROR</code></li><li><code>NETWORK_ERROR</code></li><li><code>STREAM_FAILED</code></li></ul><p>An exact <code>terminated</code> provider message and equivalent premature stream-close messages map to <code>STREAM_FAILED</code>. A request-setup or post-response <code>PROVIDER_RATE_LIMITED</code> uses the shared runtime budget: ten retries after the initial attempt, with setup and stream failures counting together. Non-429 transient failures — <code>STREAM_FAILED</code>, <code>NETWORK_ERROR</code>, <code>TIMEOUT</code>, and retryable <code>PROVIDER_ERROR</code> such as an upstream gateway 502/503/504 — share their own bounded budget of ten retries after the initial attempt, also counted together across setup and stream, and separate from the 429 budget. Both budgets are abortable and reset after a complete successful model response, including a tool-call response, in both the main session and builtin subagents. Headers, partial output, and phase changes do not replenish them. Terminal exhaustion reports <code>retryAttempt: 10</code> from the applicable budget even after retry activity cleanup. The 429 path honors <code>retry-after-ms</code>, <code>retry-after</code> seconds, and HTTP-date headers before client backoff and caps a wait at 30 seconds; the non-429 path applies the same precedence with an 8-second cap and otherwise waits 1, 2, 4, then remains at 8 seconds for later retries. Only the failed request is replayed; the session and its tool state are untouched. A non-retryable <code>PROVIDER_ERROR</code> from a malformed 400/422 request never enters either budget. The persisted <code>infiniteProviderRetry</code> setting is false by default; when true it removes only the retry-count ceiling for the admitted transient/network classes (including 429). Backoff, <code>Retry-After</code>, cancellation, and terminal classification remain unchanged, and the setting may continue API usage until the user stops the turn.</p><p>A <code>NETWORK_ERROR</code> carries the failing transport layer as bounded <code>details</code>: <code>networkCategory</code> (<code>dns</code>, <code>tls</code>, <code>timeout</code>, <code>refused</code>, <code>unreachable</code>, <code>reset</code>, <code>proxy</code>, or <code>unknown</code> when nothing survived), <code>networkCode</code> (the errno, e.g. <code>ENOTFOUND</code>, <code>ECONNRESET</code>, <code>EPROTO</code>, <code>UND_ERR_SOCKET</code>), and, when the transport reported them, <code>networkSyscall</code> and <code>networkHost</code>. Only the bare hostname is kept — never a URL, port, path, query, or credential — and <code>providerCode</code> is omitted when it would repeat <code>networkCode</code>. Per-layer codes (<code>DNS_ERROR</code>, <code>TLS_ERROR</code>, <code>SOCKET_RESET</code>, …) are deliberately not introduced: the category splits the layers without adding user-visible codes and locale strings for each of them.</p><p>The diagnosis is read from the live cause chain at the fetch boundary, not only from the provider message. pi-ai flattens a rejected request into <code>errorMessage</code>, so by the time classification runs the errno undici keeps in <code>error.cause</code> is already gone and a bare <code>fetch failed</code> can only be reported as <code>networkCategory: unknown</code>; the fetch wrapper still holds the original Error and supplies the same validated fields from it. A captured cause also settles the phase: the fault is reported as <code>phase: request</code> because no response ever arrived, which is what distinguishes it from a stream that ended mid-response. <code>networkRoute</code> (<code>direct</code>, <code>environment-proxy</code>, <code>http-proxy</code>, <code>socks5-proxy</code>) names the hop the request was taking, so a failure at the proxy is readable without guessing from an errno.</p><p>When one origin fails this way repeatedly inside a turn — twice in a row, without any response — the provider transport is rebuilt before the next attempt instead of replaying into the same undici pool. The rebuild is process-wide and deliberately bounded: one rebuild per streak, at most one every 30 seconds, and never for a <code>dns</code> failure, which a fresh pool cannot change. The replacement is installed before the previous dispatcher is closed, and the previous one is closed gracefully, so a request another session already dispatched finishes on the pool it started on. The route in effect is reproduced, never downgraded to a direct connection.</p><h3 id="permission-timeout" tabindex="-1">Permission timeout <a class="header-anchor" href="#permission-timeout" aria-label="Permalink to &quot;Permission timeout&quot;">​</a></h3><p>UI/host timeout emits <code>PERMISSION_TIMEOUT</code> internally, tool result presented as denied (<code>TOOL_DENIED</code>) to agent.</p><h3 id="shell-and-plan-goal-checkpoint-failures" tabindex="-1">Shell and Plan/Goal checkpoint failures <a class="header-anchor" href="#shell-and-plan-goal-checkpoint-failures" aria-label="Permalink to &quot;Shell and Plan/Goal checkpoint failures&quot;">​</a></h3><p><code>SHELL_NOT_FOUND</code> is returned only when catalog fallback finds no available platform shell. <code>COMMAND_SHELL_CHANGED</code> never retries with a different shell; the turn must obtain a fresh effective ID/dialect. <code>PLAN_ARTIFACT_WRITE_FAILED</code> never creates an approval row. <code>PLAN_APPROVAL_TIMEOUT</code> applies only to the absolute pending deadline; <code>PLAN_EXECUTION_INTERRUPTED</code> identifies an already-approved queued/running execution interrupted by abort or host recovery. <code>PLAN_KIND_MISMATCH</code> is a terminating tool error like <code>PLAN_NOT_ACTIVE</code>: the submit tool ran against the wrong contract, so no artifact is written and no approval row is created.</p><h3 id="local-request-preparation-failures" tabindex="-1">Local request preparation failures <a class="header-anchor" href="#local-request-preparation-failures" aria-label="Permalink to &quot;Local request preparation failures&quot;">​</a></h3><p>A structured <code>LOCAL_REQUEST_ERROR</code> from context validation, context estimation, or request preparation maps to the existing <code>INTERNAL</code> code with <code>retriable: false</code>. Preserve its local origin and phase before adapter errors are flattened to text. Diagnostics may retain the cause type, but must not copy request content, search results, credentials or arbitrary cause messages into the UI. Do not identify these failures by matching an exception sentence or by treating all JavaScript <code>TypeError</code>s alike: fetch transport failures retain the existing network/retry and cancellation behavior.</p><p>Restored-history validation may fail before a runtime stream exists. In that case the existing RPC error <code>data</code> carries <code>errorCode</code>, <code>retriable: false</code>, and safe <code>details</code> (<code>origin</code>, <code>phase</code>, optional cause type). No provider request is made, the sidecar stays available, and a stored record is never rewritten. A container that is not a stored block list still fails this way.</p><p>A single stored block that cannot be replayed is a different case: this app itself stores display-only blocks when a gateway drops ids, so the whole stored replay for that message degrades to &quot;no replay&quot; instead of failing every later turn. The turn continues, display rounds are unchanged, and the diagnostic records the block count and phases without copying search content, results or credentials.</p><h2 id="_5-ui-handling-guidelines" tabindex="-1">5. UI handling guidelines <a class="header-anchor" href="#_5-ui-handling-guidelines" aria-label="Permalink to &quot;5. UI handling guidelines&quot;">​</a></h2><table tabindex="0"><thead><tr><th>class</th><th>UI behavior</th></tr></thead><tbody><tr><td>auth/config (<code>PROVIDER_SECRET_MISSING</code>, <code>MODEL_NOT_CONFIGURED</code>)</td><td>assistant error message with settings CTA</td></tr><tr><td>permission denials</td><td>inline tool card state</td></tr><tr><td>retriable provider/network</td><td>assistant error message with diagnostic details and Continue; the session-scoped failed-turn recovery card is a fallback only when no structured assistant error is present</td></tr><tr><td>internal/host unavailable</td><td>degraded banner + recovery tip</td></tr></tbody></table><p>Message-bound provider failures never use a toast or floating global banner. A <code>PROVIDER_RATE_LIMITED</code> failure remains invisible while its bounded retry budget is available; only exhaustion renders the assistant error and lifecycle error. The assistant error message shows a localized summary and stable code, with an accessible details disclosure containing the redacted provider response, provider ID, and model ID. Provider detail is capped at 600 characters and common credential/header values are redacted before event emission or persistence. When available, the details disclosure may also show bounded <code>phase</code>, <code>providerStatus</code>, <code>providerCode</code>, <code>providerWaitMs</code>, <code>streamMs</code>, <code>retryAttempt</code>, <code>networkCategory</code>, <code>networkCode</code>, <code>networkSyscall</code>, <code>networkHost</code>, <code>networkRoute</code>, <code>requestMessages</code>, <code>requestBytes</code>, and <code>compactionGeneration</code> fields. The request fields are counts and byte sizes only and the compaction field is the checkpoint generation counter; none of them carries message content. While a transient provider failure retries, the activity indicator&#39;s reason popover shows the localized summary, the stable code, and — for a network failure — the transport errno (<code>NETWORK_ERROR · ENOTFOUND</code>), so the failing layer is visible during the retry loop as well as in the log record. The assistant error card offers a localized Continue action that resends the continuation prompt (<code>继续当前任务</code> / <code>Continue the current task</code>) in the same session without truncating the failed turn. The session-scoped failed-turn recovery card is used only when no structured assistant error is present; neither failure surface offers Regenerate.</p><h2 id="_6-i18n-key-convention" tabindex="-1">6. i18n key convention <a class="header-anchor" href="#_6-i18n-key-convention" aria-label="Permalink to &quot;6. i18n key convention&quot;">​</a></h2><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>errors.&lt;code&gt;</span></span>
<span class="line"><span>errors.&lt;code&gt;.action</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>Examples:</p><ul><li><code>errors.PROVIDER_SECRET_MISSING</code></li><li><code>errors.PROVIDER_SECRET_MISSING.action</code></li><li><code>errors.HOST_UNAVAILABLE</code></li></ul><h2 id="_7-acceptance" tabindex="-1">7. Acceptance <a class="header-anchor" href="#_7-acceptance" aria-label="Permalink to &quot;7. Acceptance&quot;">​</a></h2><ol><li>Every IPC failure returns <code>AppError.code</code></li><li>No raw untyped string-only failures on main paths</li><li>Plan/Goal hard-denies use explicit tool-specific codes; Bash is never denied by either contract mode solely because of the operating mode and instead follows permission policy</li><li>Host numeric codes map to stable string codes</li><li>Invalid shell settings, no-effective-shell/stale-pin, artifact-write, expiry, scheduled-rejection, and restart-interruption paths map to stable codes; only the documented pre-turn catalog fallback is allowed and no work is replayed</li></ol><h3 id="certificate-verification-failures-issue-714" tabindex="-1">Certificate verification failures (issue #714) <a class="header-anchor" href="#certificate-verification-failures-issue-714" aria-label="Permalink to &quot;Certificate verification failures (issue #714)&quot;">​</a></h3><p><code>NETWORK_ERROR</code> is non-retriable when <code>details.networkCode</code> is a recognized certificate verification failure, including an untrusted/self-signed chain, an expired/not-yet-valid certificate, or <code>ERR_TLS_CERT_ALTNAME_INVALID</code>. A concrete certificate cause takes precedence over generic socket/proxy wrapper codes. Captured fetch causes apply this policy after adapter error flattening as well as during direct classification. Unknown and non-certificate TLS/protocol errors retain existing recovery behavior.</p><p>The transcript keeps the stable error code, transport errno and raw details, but uses localized certificate guidance instead of the generic connectivity summary. It asks the user to check the certificate, clock, and trusted roots used by security software/proxies, then restart after changing trust. It does not claim that interception is the only possible cause or offer a TLS bypass. Manual Continue remains available after the cause is corrected.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("spec/03-runtime/08-error-codes.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _08ErrorCodes = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _08ErrorCodes as default
};
