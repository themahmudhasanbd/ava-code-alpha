import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0126: Agent Capability Pages Are One Workbench That Can Author","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0126-agent-capability-workbench.md","filePath":"adr/0126-agent-capability-workbench.md","lastUpdated":1787812970000}');
const _sfc_main = { name: "adr/0126-agent-capability-workbench.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0126-agent-capability-pages-are-one-workbench-that-can-author" tabindex="-1">ADR 0126: Agent Capability Pages Are One Workbench That Can Author <a class="header-anchor" href="#adr-0126-agent-capability-pages-are-one-workbench-that-can-author" aria-label="Permalink to &quot;ADR 0126: Agent Capability Pages Are One Workbench That Can Author&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-08-27</li><li>Related: ADR 0063, ADR 0112, D193, D194, D202</li><li>Baseline: <code>0.10.8</code></li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p><code>d24e1ee0</code> moved Skills, MCP, and Subagents out of Extensions and into Settings &gt; Agent. The move carried the list and the enablement switch, but it deleted <code>SkillEditorSheet.tsx</code> and <code>SubagentEditorSheet.tsx</code> along the way. The host side was never reduced: <code>skills.create</code>, <code>skills.update</code>, <code>skills.remove</code>, <code>skills.read</code>, <code>skills.reveal</code>, <code>subagents.create</code>, <code>subagents.update</code>, <code>subagents.remove</code>, and <code>mcp.remove</code> all still resolve end to end through <code>api.ts</code> and <code>rpc/mod.rs</code>. Only the renderer stopped calling them.</p><p>Three problems followed from the shape the pages were left in:</p><ol><li><strong>The level was structure, not a filter.</strong> Each page stacked a Global card block and a Project card block, each with its own heading, its own resolved path, its own count, and its own actions. A page with four skills spent most of its height on two headers, and the project picker was reachable only inside the second block, below the first list.</li><li><strong>One pending request locked the page.</strong> Rows were rendered with <code>busy={loading || busyKey !== null}</code> and <code>disabled={loading || busyKey !== null}</code>, so toggling any one capability disabled every control on the page, including the unrelated level&#39;s rows.</li><li><strong>A toggle flashed the skeletons.</strong> <code>toggle()</code> awaited <code>load()</code> on success, and <code>load()</code> set <code>loading</code> unconditionally, so a switch replaced the whole list with skeleton rows and then rebuilt it.</li></ol><p>Together these made the pages read as a place capabilities are listed rather than a place they are managed. D202 additionally froze the Subagents page as &quot;one fixed-height global list&quot;, which spelled the read-only shape into the decision log.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><strong>One workbench per page.</strong> Each of Skills, MCP, and Subagents renders one toolbar above one elevated panel. The toolbar owns the level filter as a segmented control with live counts, one search field, the project picker, and the page&#39;s primary actions. The level becomes a filter over one list instead of a pair of sections; inside the panel each level is a group header row (level name, resolved <code>.agents</code> path, count) followed by its rows. Every row also carries its own level badge, so a row scrolled away from its group header still says where it lives.</li><li><strong>The pages author.</strong> Create, edit, and delete return to Settings for all three capabilities, through the recovered editor sheets and the host calls that already existed. New capabilities land at the level the filter points at, and the primary action&#39;s tooltip names that destination. Destructive row actions live in a per-row overflow menu and arm before they fire: the first press relabels the item to ask for confirmation, and the arming lapses on its own after a few seconds or when the menu closes. This overrides D202&#39;s &quot;one fixed-height global list with no project picker&quot; for the Subagents page presentation only; subagents remain global-only, which is why that page has no level filter and no project picker.</li><li><strong>Busy is per row, and a refresh is not a first paint.</strong> Only the row with an in-flight request is busy; the rest of the page stays interactive. A page tracks whether it has ever hydrated: skeletons render on first paint only, and every later load keeps the rows already on screen and dims the list while announcing the refresh to assistive technology.</li><li><strong>Enablement is optimistic.</strong> The switch flips locally, the host call follows, and the previous position is restored only if the host refuses. Success needs no reload, because the local patch already matches what the host did — which removes both the skeleton flash and a round trip per toggle.</li><li><strong><code>skills.reveal</code> takes the level.</strong> <code>revealUserSkill</code> and the <code>IPC.invoke.skillReveal</code> handler now accept <code>{ id, level?, projectPath? }</code> and forward it to <code>skills.read</code>, which has always parsed both. The handler still accepts a bare id string, so no caller breaks.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Reading and managing capabilities happen in the same place, and the Settings pages no longer depend on Extensions for authoring that the host already supported.</li><li>Revealing a project-level skill opens that project&#39;s file. Previously the id alone was sent, so a project skill with no global counterpart of the same id could not be resolved.</li><li>D202&#39;s frozen page shape is superseded for presentation. The data boundary it set — global-only documents under <code>~/.agents/subagents</code>, enabled state in <code>&lt;data&gt;/agent-capabilities/subagents.json</code>, nothing written into the Markdown — is unchanged.</li><li><code>maxTurns</code> is expressible as &quot;no limit&quot; in the Subagents editor. The field&#39;s old renderer default (24) came from a <code>DEFAULT_SUBAGENT_MAX_TURNS</code> constant that <code>f8868b81</code> removed; the parser now treats an absent <code>maxTurns</code> as unlimited, and <code>UserSubagentInput</code> treats <code>0</code> as clearing the override, so the editor leaves the field empty rather than imposing a cap that hand-writing the document would not.</li><li>The change is renderer-only apart from the <code>skillReveal</code> payload widening. No host-core change, no new RPC, no storage or schema change.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0126-agent-capability-workbench.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0126AgentCapabilityWorkbench = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0126AgentCapabilityWorkbench as default
};
