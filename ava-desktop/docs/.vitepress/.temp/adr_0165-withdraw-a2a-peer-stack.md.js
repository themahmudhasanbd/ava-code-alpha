import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0165: Withdraw the A2A / Peer coordination stack","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0165-withdraw-a2a-peer-stack.md","filePath":"adr/0165-withdraw-a2a-peer-stack.md","lastUpdated":1788625977000}');
const _sfc_main = { name: "adr/0165-withdraw-a2a-peer-stack.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0165-withdraw-the-a2a-peer-coordination-stack" tabindex="-1">ADR 0165: Withdraw the A2A / Peer coordination stack <a class="header-anchor" href="#adr-0165-withdraw-the-a2a-peer-coordination-stack" aria-label="Permalink to &quot;ADR 0165: Withdraw the A2A / Peer coordination stack&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-05</li><li>Deciders: PI-Desktop core</li><li>Related: D326, D277, D318, D321, D325, ADR 0062, ADR 0089, <code>03-runtime/02-agent-runtime.md</code> §5f.2, <code>03-runtime/03-tools-and-permissions.md</code> §10.2, <code>03-runtime/06-host-rpc-protocol.md</code> §3–§4, E2E-165</li><li>Supersedes: ADR 0147, ADR 0162, and ADR 0164. ADR 0138 and ADR 0140 remain superseded by ADR 0147; this ADR withdraws the replacement those records described. Historical ADR files are retained.</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>ADR 0138 introduced in-process subagent peer messaging (<code>PeerSend</code> / <code>PeerInbox</code> / <code>PeerWait</code>). ADR 0140 folded those into one <code>Peer</code> tool. ADR 0147 replaced that mailbox with a host-core Agent2Agent (A2A) broker, an <code>a2a.*</code> RPC domain, durable <code>a2a_*</code> SQLite tables, and a delegate <code>A2A</code> tool. ADR 0162 then lifted same-session-only addressing; ADR 0164 / D325 gave Agent-mode parents a core <code>A2A</code> tool so conversations could coordinate.</p><p>That stack is no longer wanted. Sibling and parent-to-parent channels sat beside the existing <code>Task</code> / <code>TaskWait</code> / <code>TaskList</code> / <code>TaskStop</code> contract, added a protocol capability the rest of the app did not use, and were easy for the model to miss or misuse. Concurrent delegates already report through the parent; that is enough.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Remove the A2A / Peer coordination stack from the product:</p><ol><li><strong>No A2A or Peer tool.</strong> Neither name is a core Agent tool, an assignable subagent tool, or a <code>ToolSearch</code> result. A definition that lists <code>A2A</code> or <code>Peer</code> is treated like any other unknown tool name: dropped with a parse warning.</li><li><strong>No broker.</strong> Delete the host-core <code>a2a</code> module, the in-memory registry, the leftover in-process <code>SubagentMailbox</code>, and every <code>a2a.*</code> RPC method and <code>a2a.task.event</code> / <code>a2a.push</code> notification.</li><li><strong>No parent or sibling channel.</strong> Concurrent delegates coordinate only by writing self-contained reports the parent collects through <code>Task*</code>. Parents do not discover, message, or wait on other conversations.</li><li><strong>Protocol v11.</strong> Handshake <code>PROTOCOL_VERSION</code> moves from 10 to 11. The <code>capabilities</code> array no longer advertises <code>&quot;a2a&quot;</code>. A v10 host or client is rejected at handshake, the same exact-match rule as every prior bump.</li><li><strong>Schema v13.</strong> <code>SCHEMA_VERSION</code> moves from 12 to 13. <code>migrate_v12_to_v13</code> drops the A2A tables (<code>a2a_tasks</code>, <code>a2a_messages</code>, <code>a2a_artifacts</code>, <code>a2a_push_configs</code>). Fresh databases never create them. The v11→v12 step remains in the chain as a version-only bump so existing backups and chained migrations keep working; it no longer creates tables that v13 would immediately drop.</li></ol><p>Delegation itself (ADR 0062 / ADR 0089) is unchanged: <code>Task</code> still fans out bounded workers, reports still stay out of the parent model context until <code>TaskWait</code>, and builtins stay read-only by default.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Cross-conversation and sibling messaging are gone. Users who want two sessions to share facts do so in the prompt, not through a tool.</li><li>The roundtable example plugin no longer tells delegates to call <code>Peer</code> or <code>A2A</code>. It runs independent concurrent <code>Task</code>s and has the parent synthesize the reports, optionally feeding earlier reports into a later round&#39;s brief.</li><li>Historical changelog entries for 0.11.3 (peer messaging) and 0.12.0 (A2A) stay as shipped history. Product READMEs drop current A2A claims.</li><li>ADRs 0138, 0140, 0147, 0162, and 0164 remain on disk as records.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0165-withdraw-a2a-peer-stack.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0165WithdrawA2aPeerStack = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0165WithdrawA2aPeerStack as default
};
