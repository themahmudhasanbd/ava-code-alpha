import { ssrRenderAttrs, ssrInterpolate } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Decisions Log","description":"","frontmatter":{},"headers":[],"relativePath":"spec/08-meta/decisions-log.md","filePath":"spec/08-meta/decisions-log.md","lastUpdated":1790075952000}');
const _sfc_main = { name: "spec/08-meta/decisions-log.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="decisions-log" tabindex="-1">Decisions Log <a class="header-anchor" href="#decisions-log" aria-label="Permalink to &quot;Decisions Log&quot;">​</a></h1><blockquote><p>Baseline delta: <code>0.3.0</code> → <code>0.4.16</code> Date: <code>2026-08-14</code> Status: Accepted for implementation</p></blockquote><p>This log freezes previously open questions into concrete decisions.</p><h2 id="a-high-priority-architecture-decisions" tabindex="-1">A. High-priority architecture decisions <a class="header-anchor" href="#a-high-priority-architecture-decisions" aria-label="Permalink to &quot;A. High-priority architecture decisions&quot;">​</a></h2><table tabindex="0"><thead><tr><th>ID</th><th>Topic</th><th>Decision</th><th>Rationale</th></tr></thead><tbody><tr><td>D001</td><td>Electron ↔ Rust transport</td><td><strong>Rust sidecar + stdio JSON-RPC (NDJSON)</strong></td><td>Simple isolation, debuggable, replaceable later</td></tr><tr><td>D002</td><td>SQLite ownership</td><td><strong>Rust host-core owns SQLite exclusively</strong></td><td>Single writer, clearer privilege boundary</td></tr><tr><td>D003</td><td>Default mode</td><td><strong>Agent</strong></td><td>Product is an agent desktop, not pure chat</td></tr><tr><td>D004</td><td>Former restricted profile</td><td><em>(superseded by D189)</em> <strong>The former Chat read-only profile is removed; persisted Chat values migrate to Plan.</strong></td><td>The product now has one Agent with a planning state and a separate approval boundary</td></tr><tr><td>D005</td><td>Permission timeout</td><td><strong>120s → deny</strong></td><td>Fail closed, do not hang forever</td></tr><tr><td>D006</td><td><code>allow-session</code> scope</td><td><strong>By <code>toolName</code></strong></td><td>Simple UX; workspace sandbox still enforces path safety</td></tr><tr><td>D007</td><td><code>~/.pi</code> compatibility</td><td><strong>No auto-import in MVP</strong></td><td>Keep config ownership clean in <code>~/.pi-desktop</code></td></tr><tr><td>D008</td><td>Node runtime packaging</td><td><strong>Dev uses system Node; release runs the bundled sidecar on the Electron binary via <code>ELECTRON_RUN_AS_NODE=1</code> (no separate Node shipped)</strong></td><td>Unblock M1–M4; resolved at M5, see 03-runtime/07-process-model §6</td></tr><tr><td>D009</td><td>Plugin runtime isolation</td><td><strong>Target = separate process; M4 may use host-managed sandboxed runtime</strong></td><td>Ship plugin foundation pragmatically without weakening API gateway</td></tr><tr><td>D010</td><td>First release platform</td><td><strong>macOS arm64 only</strong></td><td>Focus acceptance and packaging</td></tr><tr><td>D373</td><td>Remote Agent Control host boundary</td><td><em>(amended by D374 and D375)</em> <strong>Remote control (post-MVP) runs through a logical Agent Host that owns Sessions, Turns, event cursors, approvals, attachments, workspace policy, and crash recovery; a production Gateway owns identity, routing, rate limits, revocation, and audit, and the Agent Host connects outbound. The existing Electron IPC, <code>host.proxy</code>, and host-core stdio boundaries are never exposed.</strong></td><td>A remote surface needs its own boundary rather than a tunnel into local IPC or the host-core stdio contract (ADR 0205, E2E-221 through E2E-230)</td></tr><tr><td>D374</td><td>Remote Agent Control v1 target amendments</td><td><strong>Amend D373 / ADR 0205: <code>RACP-WS</code> is the only normative v1 binding (<code>RACP-HTTP</code> is the browser profile, <code>RACP-GRPC</code> is reserved); typebox in <code>packages/shared</code> is the single contract source; the headless <code>packages/agent-host</code> module is the first deliverable and is shared by desktop IPC, local MCP, and RACP; cursors are <code>{ epoch, sequence }</code> with ephemeral deltas; the turn queue moves into the Host; host-core exposes <code>permissions.pending</code>; remote approvals carry the full local decision vocabulary under a default <code>ask</code> ceiling; browser clients use a cookie profile; the first deployment is single-tenant.</strong></td><td>Reviewing the D373 draft against the shipped desktop found the remote approval vocabulary narrower than the local contracts, pending requests held as connection state, per-token deltas exhausting the replay window, and more bindings than v1 can carry (ADR 0205)</td></tr><tr><td>D447</td><td>Headless runtime boundary</td><td><strong>Amend ADR 0205 rollout R2 prerequisites (ADR 0284): the Electron-independent runtime layer under the Agent Host module lives in <code>packages/host-runtime</code> — the host-core and sidecar stdio transports, the restart supervisor (process-model §4 policy), the durable turn lifecycle (<code>RuntimeService</code> as the module&#39;s <code>RuntimePort</code>: <code>session.beginTurn</code> → user row → prompt → <code>session.endTurn</code>, abort lock, stale-terminal guard, single-flight finalization), transcript persistence with the D299 checkpoint, a headless launch resolver over host-core&#39;s own registries, approved Plan/Goal dispatch (D189), and the host-core adapters for <code>SessionPort</code> / <code>QueueStore</code> / <code>permissions.pending</code>. Electron main keeps thin adapters (binary and bundle locations, <code>ELECTRON_RUN_AS_NODE</code>, redacted stderr, schema/glibc diagnoses, renderer status pushes) and its own local prompt path. <code>TurnStartRequest</code> gains an optional <code>userMessageId</code>. No IPC, sidecar, host-core, Plugin SDK, default, or persisted-data change.</strong></td><td><code>pi-host</code> must run the same lifecycle invariants as the desktop without Electron; a second implementation of abort locks, stale terminal events, and queue release after durable settlement would drift from the one the desktop already fixed</td></tr><tr><td>D448</td><td>RACP-WS transport and device pairing</td><td><strong>Amend ADR 0205 rollout R2 (ADR 0285): <code>packages/racp</code> holds both ends of the normative <code>RACP-WS</code> binding. <code>RacpServer</code> dispatches every catalog operation through its role rule onto the Agent Host module and a <code>RacpHostOperations</code> interface the Host implements; <code>RacpClient</code> correlates requests, answers server-initiated requests, tracks the last durable cursor per session, reconnects with bounded backoff, and fails the dropped connection&#39;s in-flight calls with <code>HOST_DISCONNECTED</code> instead of re-sending them. Authentication is the header profile with Host-issued <code>pdt1.</code> device tokens and single-use, expiring <code>ppt1.</code> pairing tokens (hashed, constant-time compared; never in a URL); <code>connection/pair</code> mints an <code>owner</code> device. <code>connection/pair</code>, <code>project/register</code>, <code>project/browse</code>, <code>server.hostId</code>, the <code>events/closed</code> notification, and the codes <code>PAIRING_FAILED</code>, <code>PAIRING_TOKEN_EXPIRED</code>, <code>CAPABILITY_UNAVAILABLE</code>, <code>REMOTE_PATH_NOT_FOUND</code>, <code>REMOTE_PATH_FORBIDDEN</code>, <code>HOST_DISCONNECTED</code>, <code>HOST_BOOTSTRAP_FAILED</code>, <code>HOST_VERSION_MISMATCH</code>, <code>REMOTE_AUTH_FAILED</code>, <code>REMOTE_CONNECTION_FAILED</code>, <code>REMOTE_FORWARD_FAILED</code> join the contract (spec §14a). The bind refuses non-loopback addresses; attachments and the tool relay are advertised unavailable.</strong></td><td>The SSH-tunnel topology needs a server <code>pi-host</code> can bind and a client the desktop can run, with the pairing the security spec describes; the conformance behaviors that need no machine boundary become package tests</td></tr><tr><td>D451</td><td>Review opens only on explicit user action</td><td><strong>A tool result never opens, activates, or resizes the work panel. The <code>tool_end</code> artifact gate and <code>shouldOpenReviewArtifact</code> are removed; Review is reached only from the <code>+</code> New launcher row or from the retained session context the viewport-fixed toggle / <code>Cmd/Ctrl + J</code> reveals. A successful workspace Write/Edit still records its message-owned inline review card in the transcript.</strong></td><td>An agent edit silently revealing a side panel competes with the user&#39;s own reading and layout, and the transcript card already carries the change evidence. See ADR 0043, <code>04-ux/01-ui-ia.md</code>, <code>04-ux/08-component-spec.md</code> §5, <code>04-ux/09-interaction-patterns.md</code>, E2E-057.</td></tr><tr><td>D452</td><td>Plan and goal artifacts open in the bundled file view</td><td><strong>The contract approval card&#39;s artifact opener hands the immutable <code>.pi/plan/*.md</code> or <code>.pi/goal/*.md</code> path to the bundled <code>pi.file-manager</code> view whenever that view is launchable, and to the host file tab otherwise — the same preference and fallback a chat file reference already uses (ADR 0241). The artifact still creates or activates a tab in its originating session, so the approval reveal itself is unchanged; only the surface changes. Renderer-only; no protocol, host, storage, artifact-content, or permission change. See D189, ADR 0043, <code>04-ux/08-component-spec.md</code> §5.4 and §10A.2, <code>04-ux/09-interaction-patterns.md</code> §5A, E2E-106.</strong></td><td>The approval opener used a host file tab while every other project file the conversation names already opens in the user&#39;s own file view, so a plan or goal landed in the one surface the user does not browse project files in.</td></tr><tr><td>D459</td><td>Restore resizable sidebar width with collapse-below-threshold</td><td><strong>Amend D408 / ADR 0238 and restore ADR 0141 (ADR 0290): the expanded sidebar is again a renderer-owned <code>240px..520px</code> column (default <code>275px</code>) with a right-edge handle that previews on pointer-down, persists on release, and supports ArrowLeft/ArrowRight (16px), Home, and End. A pointer width below <code>160px</code> collapses as a user action without overwriting the preferred expanded width; keyboard resize never collapses. Live maximum is the three-column remainder after MainChat&#39;s 450px floor. Renderer only; existing <code>pi.desktop.sidebarWidth</code> preference. See E2E-168.</strong></td><td>Long labels need reclaimable width; ADR 0238&#39;s fixed 275px pin blocked that while the live three-column budget could already cap a user-chosen width. This decision was mistakenly recorded as D451 (already used by Review-opens-only-on-explicit-user-action); renumbered per issue #620.</td></tr><tr><td>D457</td><td>Signed macOS DMG is a two-icon install</td><td><strong>Amend D406 / ADR 0232 / ADR 0204: official and local DMGs contain only PI-Desktop.app and the Applications link on a branded 720×440 plate. They no longer include <code>If app won&#39;t open, read this.txt</code> or the command helper. macOS ZIP packages still ship both the opening note and <code>PI-Desktop-macOS-open.command</code> for trusted unsigned artifacts. See ADR 0296 and E2E-196b.</strong></td><td>Tagged DMGs are signed and notarized (D450); the unsigned-opening note on the installer read as an error.</td></tr><tr><td>D450</td><td>Signed macOS GitHub Releases</td><td><strong>Amend D078 / ADR 0022: GitHub tag releases Developer ID-sign, notarize (<code>notarytool</code> via electron-builder 26), staple, and Gatekeeper-verify macOS DMG/ZIP before upload, using identity <code>Developer ID Application: XingYu Liu (DUV63RKYTW)</code> / team <code>DUV63RKYTW</code> from Actions secrets (<code>CSC_LINK</code>, <code>CSC_KEY_PASSWORD</code>, <code>APPLE_ID</code>, <code>APPLE_APP_SPECIFIC_PASSWORD</code>, <code>APPLE_TEAM_ID</code>). Missing secrets fail the job. Local unsigned packaging without a certificate remains. <code>workflow_dispatch</code> may set <code>sign_macos: false</code> only for unsigned debug artifacts. Packaged macOS uses in-app <code>electron-updater</code> (ZIP + merged <code>latest-mac.yml</code>); Linux deb/rpm and Windows portable ZIP stay notify-and-link. No afterPack/afterSign adhoc codesign (ADR 0278).</strong></td><td>Production DMGs must open without a Gatekeeper warning, and signed macOS installs can download and restart into a new tag. See ADR 0289, E2E-196c, E2E-067A.</td></tr></tbody></table><h2 id="b-secondary-implementation-defaults" tabindex="-1">B. Secondary implementation defaults <a class="header-anchor" href="#b-secondary-implementation-defaults" aria-label="Permalink to &quot;B. Secondary implementation defaults&quot;">​</a></h2><table tabindex="0"><thead><tr><th>ID</th><th>Topic</th><th>Decision</th></tr></thead><tbody><tr><td>D011</td><td>TS schema validation</td><td><strong>typebox</strong></td></tr><tr><td>D012</td><td>i18n library</td><td><strong>i18next + react-i18next</strong></td></tr><tr><td>D013</td><td>Bash execution style in M3</td><td><strong>Non-interactive only</strong> (no PTY yet)</td></tr><tr><td>D014</td><td>Command palette shortcut</td><td><strong>Cmd/Ctrl + Shift + P</strong></td></tr><tr><td>D015</td><td>Plugin tool exposed name</td><td><strong>Forced prefix</strong> <code>plugin_&lt;pluginIdSafe&gt;_&lt;toolName&gt;</code></td></tr><tr><td>D016</td><td>Uninstall plugin data</td><td><strong>Delete by default</strong>, optional keep-data later</td></tr><tr><td>D017</td><td>enable → load failure</td><td><strong>Auto fallback to disabled</strong></td></tr><tr><td>D018</td><td>Plugin secrets in settings</td><td><strong>Not allowed in MVP</strong></td></tr><tr><td>D019</td><td>Plugin session summary access</td><td><strong>Denied by default</strong> <em>(amended by D336: granted only through <code>session.read</code>, and only for the in-flight tool session)</em></td></tr><tr><td>D020</td><td>Auto-update</td><td><strong>Post-MVP</strong></td></tr><tr><td>D021</td><td>First-run onboarding</td><td><strong>Inline checklist (not modal wizard)</strong></td></tr><tr><td>D022</td><td>Local telemetry</td><td><strong>Local logs only in MVP (no remote telemetry)</strong></td></tr></tbody></table><h2 id="c-provider-model-coverage-decisions-0-3-4" tabindex="-1">C. Provider &amp; model coverage decisions (0.3.4) <a class="header-anchor" href="#c-provider-model-coverage-decisions-0-3-4" aria-label="Permalink to &quot;C. Provider &amp; model coverage decisions (0.3.4)&quot;">​</a></h2><table tabindex="0"><thead><tr><th>ID</th><th>Topic</th><th>Decision</th><th>Rationale</th></tr></thead><tbody><tr><td>D023</td><td>Provider coverage goal</td><td><strong>Universal market coverage</strong> (not a tiny fixed vendor list)</td><td>Globalization + real coding workflows</td></tr><tr><td>D024</td><td>Coverage strategy</td><td><strong>pi-ai native providers + first-class OpenAI-compatible + custom providers</strong></td><td>Maximum reach without rewriting every SDK</td></tr><tr><td>D308</td><td>Zhipu / Z.AI named endpoint presets</td><td><strong>Amend D024 / ADR 0116: the add-provider Service select offers four OpenAI-compatible Zhipu endpoints — China and international standard API plus GLM Coding Plan — with locked published URLs and models.dev <code>vendorKey</code>s (<code>zhipuai</code>, <code>zhipuai-coding-plan</code>, <code>zai</code>, <code>zai-coding-plan</code>). No new apiStyle or wire adapter. Completions requests on those URLs use <code>thinkingFormat: &quot;zai&quot;</code> and <code>zaiToolStream: true</code>. Custom endpoint remains available.</strong></td><td>Users had to know which BigModel / Z.AI URL to paste, and <code>vendorKey: &quot;custom&quot;</code> could catalog-match the wrong API vs Coding Plan record. Named presets keep coverage on the existing OpenAI-compatible path. See ADR 0155 and E2E-005D.</td></tr><tr><td>D389</td><td>DeepSeek reasoning_content replay on aggregators</td><td><strong>Amend D024: Completions requests for DeepSeek-family models set <code>requiresReasoningContentOnAssistantMessages: true</code> when the row <code>vendorKey</code>, base URL, model id, or catalog <code>family</code> identifies DeepSeek. Do not change <code>thinkingFormat</code> for that match. pi-ai still auto-detects official <code>deepseek.com</code> URLs.</strong></td><td>DeepSeek thinking mode 400s if any replayed assistant message omits <code>reasoning_content</code>. PI-Desktop stores a UUID as <code>model.provider</code>, so SiliconFlow, Volcengine, and custom gateways never match pi-ai&#39;s <code>provider === &quot;deepseek&quot;</code> check. See E2E-005E.</td></tr><tr><td>D424</td><td>DeepSeek reasoning across compaction</td><td><strong>Amend D389 / ADR 0136: restore thinkingSignature on history rebuild; store recent thinking in checkpoint <code>details.retainedReasoning</code> and replay after the summary; non-official DeepSeek Completions rows set <code>requiresNonEmptyReasoningReplay</code> so the pi-ai patch uses a documented placeholder instead of <code>&quot;&quot;</code>. Official deepseek.com keeps empty-string fill.</strong></td><td>OpenCode / third-party DeepSeek V4.1 Flash 400s after compaction when empty reasoning is echoed (#296).</td></tr><tr><td>D309</td><td>Add-provider common path is Service + key</td><td><strong>Amend D308 / ADR 0116 / ADR 0155: a new add-provider dialog starts with only Service. Named endpoints (Zhipu / Z.AI, OpenCode Go) then show Service + API key and a host summary. Custom endpoint then shows Name, Base URL, and API key. Name (named) and API format (custom) stay in Advanced. OpenCode Go is a Service option. No stepper or vendor-card grid.</strong></td><td>Stacking Name, Base URL, and API format next to Service made a known service look like a generic gateway. The common path should be pick a service and paste a key. See ADR 0156 and E2E-005 / E2E-005B / E2E-005D.</td></tr><tr><td>D310</td><td>Custom API format stays beside the key; Service lists top models.dev vendors</td><td><strong>Amend D309 / ADR 0155 / ADR 0156: Custom endpoint shows Name + Base URL, then API key beside API format on the common path. Named Service options expand to models.dev-backed international and China first-party endpoints (OpenAI, Anthropic, Google, OpenRouter, Groq, xAI, Mistral, Together, Fireworks, OpenCode Go, Z.AI, DeepSeek, Qwen, Moonshot, Zhipu, SiliconFlow, Volcengine, MiniMax Anthropic Messages, MiniMax OpenAI-compatible, Kimi For Coding), each with a published URL and wire style. Named display names stay in Advanced.</strong></td><td>Custom gateways need the format next to the key, while named vendors should cover the usual domestic and international APIs without a closed allowlist. See E2E-005 / E2E-005B / E2E-005D.</td></tr><tr><td>D311</td><td>Service picker is searchable; discovery waits for a stable choice</td><td><strong>Amend D310: the add-provider Service control is a searchable anchored menu (same pattern as the default-model picker), not a native select. Filtering is client-side over localized name, vendor key, aliases, and host. Named add-path discovery does not start until an API key is present (editing still reuses the stored secret). Custom endpoints still probe a valid URL without a key. The discovery hook does not mark loading until the debounce fires, and an endpoint change clears the previous list immediately.</strong></td><td>Native selects with optgroups are sluggish in the overlay, and changing Service or typing a key used to refetch/re-render on every change. Search matches how models are filtered. See E2E-005 / E2E-005D.</td></tr><tr><td>D312</td><td>Service list is a flat vendor catalog; add Xiaomi</td><td><strong>Amend D310 / D311: named Service options are one vendor list, not International / China groups. Add Xiaomi (<code>vendorKey: &quot;xiaomi&quot;</code>, <code>https://api.xiaomimimo.com/v1</code>, Chat Completions). Custom stays first. Search still matches localized name, vendor key, alias, and host.</strong></td><td>Region headings split the same vendors users already search by name. Xiaomi&#39;s first-party MiMo API is a models.dev vendor. See E2E-005.</td></tr><tr><td>D025</td><td>Model allowlist</td><td><strong>No closed product allowlist</strong></td><td>Models churn; power users need free-form IDs</td></tr><tr><td>D026</td><td>Catalog sources</td><td><strong>bundled snapshot + discovery/refresh + user-defined</strong></td><td>Works offline and stays current</td></tr><tr><td>D027</td><td>Default identity</td><td><strong>Model selection is <code>(providerId, modelId)</code></strong></td><td>Same model id can exist on many gateways</td></tr><tr><td>D028</td><td>Secrets</td><td><strong>OS safeStorage (or controlled fallback) via secretRef; never in provider JSON</strong></td><td>Security boundary with Rust host ownership</td></tr><tr><td>D029</td><td>Local models</td><td><strong>Supported through OpenAI-compatible local gateways</strong></td><td>Ollama/LM Studio/vLLM without special-case architecture</td></tr><tr><td>D030</td><td>Connection test</td><td><strong>First-class host method before trusting provider for runs</strong></td><td>Fail early, actionable setup UX</td></tr><tr><td>D031</td><td>Secrets backend</td><td><strong>OS safeStorage primary + encrypted file fallback</strong></td><td>Robust on macOS first release</td></tr><tr><td>D032</td><td>Workspace ignore</td><td><strong>security denylist + defaults + <code>.pi-desktopignore</code></strong></td><td>Safe/predictable tool FS behavior</td></tr><tr><td>D033</td><td>Tool result limits</td><td><strong>256KB/4000 lines defaults with explicit truncation markers</strong> <em>(amended by D194, D306)</em></td><td>Protect context &amp; UI</td></tr><tr><td>D306</td><td>Search/read truncation is a cut, not a window</td><td><strong>Amend D033 / D194: Read/Glob/Grep <code>truncated</code> is true only when the host cut content the caller asked for (byte budget, per-line clip, Grep/Glob match cap). A Read that returned the requested or default window of a longer file is complete for that window; <code>totalLines</code>/<code>offset</code>/<code>lineCount</code> and a next-offset <code>notice</code> describe the remainder. Default Read window is 2000 lines (max 4000). <code>BUDGET_SEARCH</code> is 128KB / 4000 lines. Per-line clip is 16,384 characters. The UI truncated chip follows this flag.</strong></td><td>The 48KB / 500-line window marked almost every source and spec file truncated, including successful paged reads, which hid real cuts and forced the agent to re-search what it already had.</td></tr><tr><td>D307</td><td>Revision payloads follow the live branch</td><td><strong>Amend D109: a regenerate branch keeps growing after its archive (later prompts, error-ended turns that never reach <code>agent_end</code>), so every operation that discards the live branch first writes it back over the variant it belongs to. <code>session.activateRevision</code> re-archives the live branch of the family from the durable transcript before switching and takes the prefix from there; the regenerate path refreshes the stamped variant via <code>session.saveRevision { revisionIndex }</code>; <code>session.saveActiveRevision</code> refreshes an already-archived index instead of skipping it. The variant refreshed is the one the live root&#39;s <code>activeRevision</code> stamp names; a stamped variant with no index row yet is stored as its own variant, never over a previous one. Switching also carries each surviving message&#39;s owning <code>turn_id</code> and keeps checkpoints whose anchors survive.</strong></td><td>The archive was written once and reused forever: paging away from a branch that had grown since its agent_end archive, then back, restored the stale copy and silently deleted every later turn from the transcript and its JSONL.</td></tr><tr><td>D390</td><td>Host-owned regenerate truncate</td><td><strong>Amend D199 / D258 / D307: <code>agent/prompt</code> truncates through <code>session.truncateFrom</code> under the host lock (identity-first cut, abort leftover running turn, archive discarded tail, rewrite prefix). The kept transcript does not cross JSON-RPC. An NDJSON request line over 64 MiB is <code>LIMIT_EXCEEDED</code> and does not end the stdin reader. Protocol version stays at 11. See ADR 0216 and E2E-246.</strong></td><td>Retrying a multi-thousand-message session timed out at 130 s on <code>session.replaceMessages</code> and could kill host stdin at 64 MiB (issue #211).</td></tr><tr><td>D391</td><td>Host stdout sender must not outlive serve</td><td><strong>Amend D390 / ADR 0216: the Windows Alt+Space hook retains only a weak clone of the stdout sender. After stdin EOF, dropping serve&#39;s sender closes the writer channel and host-core exits. Electron rejects an NDJSON request over 64 MiB before writing, with <code>LIMIT_EXCEEDED</code>. A host-side oversize reply peeks the JSON-RPC id from the truncated prefix so the client does not wait 130 s. Serve waits at most 5 s for the stdout writer after stdin ends. Protocol version stays at 11. See ADR 0217 and E2E-247.</strong></td><td>On Windows v0.14.6 a 64 MiB stdin cap ended the reader, but a strong keyboard sender kept the writer thread alive, so host-core became a zombie and Electron reported <code>host RPC timeout: session.replaceMessages</code> (issue #211).</td></tr><tr><td>D392</td><td>Effective image-input overrides across Composer and transport</td><td><strong>Amend D243 / ADR 0101: image capability starts with the published model record, then an exact binding&#39;s <code>supportsImages</code> value wins when it is <code>true</code> or <code>false</code>; absent or <code>null</code> follows the published value. Composer badges, attachment status, and main-process image transport use the same effective result. Unknown/custom models remain conservative without an explicit override. See ADR 0218 and E2E-163.</strong></td><td>A configured endpoint could already transport an image through its binding override while the Composer row still hid the vision badge, or could show a published badge after image input was disabled for that endpoint.</td></tr><tr><td>D393</td><td>User-invoked Skills in the composer</td><td><strong>Amend D123 / D174 / ADR 0024 / ADR 0039: active built-in, plugin, and user Skills appear in a separate <code>Skills</code> group at the end of the composer slash menu. Selecting one inserts its exact id; Electron main revalidates the active project scope at send time and asks the model to call the local <code>Skill</code> tool, preserving on-demand body loading and existing permissions. Existing command names win collisions; inactive Skills remain literal slash text. See ADR 0219 and E2E-088b.</strong></td><td>D174&#39;s model-invoked catalog remains the body-loading and security contract, while a final explicit entry makes known workflows discoverable without moving Skill bodies into the renderer, prompt, or host protocol.</td></tr><tr><td>D394</td><td>Windows work-panel chrome keeps one resource action cluster</td><td><strong>Amend D154 / D357 / ADR 0195: the open work-panel header keeps one compact resource switcher; resource close is owned by the existing keyboard-operable context-menu rows, the viewport-fixed toggle remains the only panel collapse control, and subagent detail returns with a back chevron. Windows/Linux native controls remain fixed at the window edge. Renderer-only; no panel state, window geometry, IPC, protocol, or storage change. See ADR 0220 and E2E-067.</strong></td><td>The header resource <code>X</code>, viewport-fixed toggle, and Windows native close cluster read as duplicate close actions and became cramped at narrow panel widths.</td></tr><tr><td>D396</td><td>Renderer and plugin-panel scrollbars share one compact contract</td><td><strong>Amend D300: every renderer scroll container uses one 6px, trackless, transparent-at-rest scrollbar with the same hover, focus-within, scroll-reveal, and dragged-thumb states. Remove the sidebar-specific width and opacity override. The plugin-panel preload applies the same contract and 300ms reveal mark to docked and detached plugin documents, including the bundled Files view. External pages loaded inside the Browser guest remain page-owned. Presentation-only; no protocol, storage, host runtime, or external-page behavior change. See E2E-157.</strong></td><td>Windows&#39; classic scrollbar made the right-side work-panel Files view visibly heavier than the conversation, while the sidebar retained a second scrollbar treatment.</td></tr><tr><td>D-LOCAL-message-quotes</td><td>Message quotes and renderer-owned side chats</td><td><em>(retired by ADR 0268 on 2026-09-16)</em> <strong>Amend D209 / D301: every user message and assistant turn gains a Quote action that inserts a <code>&gt; </code>-prefixed Markdown blockquote plus a <code>chat.quoteSource</code> attribution into the active session&#39;s composer draft and focuses it, using the live selection inside that message row when one exists and the message&#39;s own text otherwise; it never sends and adds no chip kind, with a 2000-character cap. Open side chat forks the anchored message through <code>session.fork</code> without activating the child, registers the child as a renderer-owned side chat of the parent, and opens one <code>sidechat:&lt;childSessionId&gt;</code> tab in the existing docked panel that streams from the same event stream through the background-transcript reducer, with Add to main chat, Open as a conversation, a compact Send/Stop input, and the existing permission card. Closing the tab or activating the child removes the registration; the durable child stays an ordinary session. No protocol, schema, IPC, or permission change. See ADR message-quotes-and-side-chats and E2E-CHAT-quote-prefill through E2E-CHAT-side-chat-close.</strong></td><td>Users needed to reuse an exact earlier message or answer and to ask a side question without replacing the visible main conversation, and the existing fork path always activated its child. <em>(Amended by D-LOCAL-selection-overlay: the excerpt is recovered from the rendered DOM as Markdown, and a selection inside a transcript row offers Add to chat, Ask in side chat, and Copy from a selection-following overlay.)</em></td></tr><tr><td>D-LOCAL-response-annotations</td><td>Response annotations as prompt attachments</td><td><em>(retired by ADR 0268 on 2026-09-16)</em> <strong>Amend D-LOCAL-message-quotes (decision 3) for assistant turns: selecting text in a response and choosing Add to chat attaches a numbered annotation to that turn instead of editing the composer draft — the answer body is not decorated (only a citation the model writes renders, as a numbered reference), the composer shows one annotation attachment chip whose tooltip lists the excerpts plus one control that drops them, and the next prompt carries them as <code># Response annotations:</code> + an instruction + <code>&lt;response-annotations&gt;</code> JSON (<code>[{text, annotation, source:{messageId}}]</code>) + <code>## My request:</code> before the user&#39;s own text, consuming them. The visible draft, optimistic row, title, and edit seed stay annotation free, and a stored prompt carrying the block is displayed as its request. <em>(Amended 2026-09-12: Add to chat opens a compact comment editor whose Save writes the user&#39;s optional comment into the annotation; re-annotating an attached excerpt reopens that editor instead of a silent no-op, and the composer attachment lists each item with edit and remove controls beside clear-all.)</em> Quoting a user message, and the side chat&#39;s Add to main chat, keep D-LOCAL-message-quotes&#39;s draft quote; no protocol, storage-schema, IPC, or permission change. See ADR response-annotations and E2E-CHAT-annotation-attachments, E2E-CHAT-annotation-session-state.</strong></td><td>The draft quote copied the model&#39;s words into the user&#39;s own prompt text, and several referenced passes had no numbering the model could address; the reference implementation attaches them as numbered prompt data instead.</td></tr><tr><td>D398</td><td>Context usage display preference</td><td><strong>Amend D347 / ADR 0184: the context usage inspector&#39;s leading figure — the trigger ring arc, percentage, token label, popover heading, tooltip, and <code>aria-label</code> — is configurable via <code>AppSettings.contextUsageDisplay</code> (<code>&quot;remaining&quot;</code> or <code>&quot;used&quot;</code>). Default and fallback for absent/unrecognised values is <code>&quot;remaining&quot;</code>. When <code>&quot;used&quot;</code>, the ring fills by <code>usedRatio</code>, and text shows the used-capacity pair. Warning and critical color thresholds (remaining ≤ 25 % / ≤ 10 %) stay based on remaining capacity regardless of display mode. Settings → AI → Defaults adds a segmented control (Remaining / Used) after Link open destination and before Enter-to-send. Renderer only; no protocol, storage, host, or migration change. See ADR 0222 and E2E-250.</strong></td><td>The remaining-only display gave weak signal at low occupancy and did not match users who reason in terms of &quot;how much have I spent&quot;. Color must stay on remaining to avoid a misleading green ring at 90 % used.</td></tr><tr><td>D399</td><td>Project group manual ordering</td><td><em>(amended by D402)</em> <strong>Amend D093: retained project groups expose a visible-on-hover/focus grip. Native drag/drop and ArrowUp/ArrowDown on that grip write contiguous normalized-path <code>order</code> values and set <code>projectSort</code> to <code>manual</code> in renderer-local sidebar preferences. Archived and pinned priority remains ahead of manual order; project activation, host workspace identity, session ordering, and on-disk directories are unchanged.</strong></td><td>Users with several active repositories need a stable working order independent of recent activity or alphabetical names, while the existing path-keyed and host-owned project model remains untouched.</td></tr><tr><td>D400</td><td>Restore deferred tools from effective session context</td><td><strong>Amend D185 / ADR 0048: before each new prompt and after a mode switch, clear the in-memory deferred activation set, then restore names from successful <code>ToolSearch</code> results (<code>addedToolNames</code>) and successful deferred-tool results in the effective <code>buildSessionContext</code> projection. Keep only names still in the current deferred catalog and mode; ignore errors, interrupted or missing-result placeholders, and assistant/user prose. No host permission or workspace boundary changes.</strong></td><td>Clearing activations while retaining their successful transcript markers left the model able to see a capability that was absent from the next provider schema. Reconstructing only from effective successful evidence keeps the provider request coherent without parsing prose or reviving stale or disallowed tools. See ADR 0225 and E2E-008a.</td></tr><tr><td>D402</td><td>Long-press project title to reorder</td><td><em>(amended by D403)</em> <strong>Amend D399 / D093 / ADR 0227: retained project groups have no visible reorder grip. A 400ms still press on the project title arms a pointer reorder; movement beyond 8px before that delay cancels it so a click still selects and toggles collapse. ArrowUp/ArrowDown on the focused title is the keyboard path; Escape cancels. Persistence, pin/archive buckets, host workspace identity, session ordering, and on-disk directories are unchanged. See ADR 0228 and E2E-253.</strong></td><td>The dedicated grip consumed a leading column and made reorder a second control beside the title that already selects and collapses the group.</td></tr><tr><td>D403</td><td>Press-and-move project title reorder</td><td><strong>Amend D402 / D093 / ADR 0228: mouse and pen reorder by pressing the project title and moving 8px; a click with no qualifying movement still selects and toggles collapse. Touch does not start a reorder. An accent insertion line shows before/after placement. ArrowUp/ArrowDown and Escape are unchanged. See ADR 0229 and E2E-253.</strong></td><td>A 400ms still press is a mobile long-press pattern and is slower than ChatGPT-style desktop sidebar lists.</td></tr><tr><td>D404</td><td>Skill ships with the Agent core tool set</td><td><strong>Amend D174 / D185 / ADR 0048 / ADR 0219: <code>Skill</code> joins the Agent-mode core tool set, so its schema is present on the first provider request whenever the skill catalog is non-empty. It is removed from the deferred catalog and never appears under <code># On-demand tools</code>; the other on-demand capabilities and <code>ToolSearch</code> are unchanged, and Plan and Goal still omit the tool entirely. No protocol, storage, permission, or skill-body change. See ADR 0230 and E2E-254.</strong></td><td>A user-typed <code>/skill-id</code> and the <code># Skills</code> section both ask the model to call <code>Skill</code>, and a tool that is absent from the schema cannot be called at all: the deferred entry added a discovery round trip before any skill body could load (issue #204).</td></tr><tr><td>D405</td><td>Ideographic comma opens the slash menu</td><td><strong>Amend D123 / D139 / ADR 0024: a <code>、</code> (U+3001) committed as the first character of an empty composer draft is rewritten to <code>/</code> before trigger detection, so a Chinese IME reaches the ordinary slash menu without switching input methods. Only that position is rewritten; a <code>、</code> anywhere else stays ordinary punctuation, and the <code>@</code> file menu is unaffected. Shared grammar and renderer only; no IPC, storage, or autocomplete-source change. See ADR 0231 and E2E-255.</strong></td><td>Reaching <code>/new</code>, <code>/compact</code>, a mode alias, or a Skill forced a Chinese IME user to switch to ASCII input mid-sentence and then switch back (issue #65).</td></tr><tr><td>D406</td><td>Keep macOS DMG opening guidance text-only</td><td><em>(amended by D457)</em> <strong>Amend D371 / ADR 0204: macOS DMGs expose the opening-help note as <code>If app won&#39;t open, read this.txt</code> and no longer include the executable <code>PI-Desktop-macOS-open.command</code>. macOS ZIP packages retain both the note and the helper. The note provides the narrow Terminal fallback for trusted unsigned builds; signed and notarized builds do not need it. See ADR 0232 and E2E-196b.</strong></td><td>The DMG should keep the normal app-to-Applications flow focused while still giving users a visible, actionable answer when an unsigned app does not open.</td></tr><tr><td>D407</td><td>Restore archived projects after session import</td><td><strong>Additive renderer behavior for issue #250: when a core or plugin import adds a new project-bound session, the import-triggered session refresh normalizes its project path and clears the renderer&#39;s archived presentation state for that project. Pathless sessions, skipped imports, historical plugin paths without an active binding, and ordinary refreshes leave archive state unchanged. Host project rows, IPC channels, plugin methods, storage schema, and data formats do not change. See ADR 0236 and E2E-257.</strong></td><td>The host can successfully materialize an imported session under a project while the renderer still hides that project&#39;s sidebar row as archived. Restoring only the newly imported binding makes the result discoverable without weakening deliberate archive choices during ordinary refreshes (issue #250).</td></tr><tr><td>D408</td><td>Prioritize MainChat in the three-column shell</td><td><strong>Amend ADR 0226 / ADR 0151 / ADR 0033 for issue #267: MainChat keeps a hard 450px minimum, the work panel is capped by the live budget (<code>client width - 450px - expanded sidebar</code>, with no fixed maximum), and the expanded sidebar yields at that threshold — including while <code>sidebar-out</code> still occupies flex space. A manual sidebar reopen spends panel width first and otherwise targets 460px; closing the panel restores only a sidebar the layout collapsed. The native window never changes: the reservation seam stays at zero and no geometry is applied. Preview mode temporarily unmounts MainChat and uses a window-level chrome row; collapsed-sidebar macOS preview reserves 88px, or 8px in fullscreen, for traffic lights (D433). See ADR 0238 and E2E-LAYOUT-three-column-width-priority.</strong></td><td>The fixed client area had no explicit width priority, so the side docks could pin MainChat to its floor and leave the composer unusable. Making the yield order explicit keeps the chat readable inside the fixed window without reintroducing native window growth (issue #267).</td></tr><tr><td>D409</td><td>Host-owned session collaboration messages</td><td><em>(amended by D446)</em> <strong>Amend ADR 0237 / ADR 0165 / ADR 0213: Rust host-core owns a durable session-collaboration ledger keyed by message id and real source/target Session IDs. Plugin-mediated <code>spawn</code>, <code>send</code>, <code>status</code>, <code>result</code>, and <code>cancel</code> operations use the reviewed desktop-control gateway; the sender is bound to the active plugin Agent tool invocation, target turns retain their existing configuration, and each delivery is claimed by its actual durable turn. Completion callbacks are durable, at-most-once, and reference the settled turn. Provenance is persisted with transcript rows and cannot be forged, stripped, or edited through regeneration. The additive schema v16 migration retains queued work across restart without unattended replay, applies permission ceilings and bounded autonomous hops, and keeps the existing Task family unchanged. See ADR 0239 and E2E-PLUGIN-session-orchestrator-real-workers.</strong></td><td>The plugin&#39;s prior create/prompt polling path could infer neither a durable turn outcome nor a safe bidirectional sender identity. A host-owned ledger makes delivery, provenance, callback, cancellation, and restart behavior auditable without restoring the withdrawn A2A protocol.</td></tr><tr><td>D410</td><td>Independent session discovery and navigable collaboration projections</td><td><strong>Amend ADR 0239: add the reviewed read operation <code>session/collaboration/list</code>, bounded to 100 non-deleted Agent sessions and redacted to Session IDs, titles, status, updated time, readable provider/model labels, and bounded creation links. Extend the sidebar projection with readable model labels and at most eight created-session references. Render creator/created-session references as keyboard-focusable navigation buttons; independent sessions do not receive fabricated creator links. No renderer storage ownership or collaboration mutation boundary changes. See ADR 0240, E2E-SESSION-independent-top-level-communication, and E2E-SESSION-hover-card-model-and-links.</strong></td><td>Existing Session IDs were valid send targets but could be undiscoverable when they were not created by the plugin, while the hover card exposed only IDs and non-interactive provenance. A bounded host directory and navigable projection make durable sessions communicable and explainable without exposing transcripts or credentials.</td></tr><tr><td>D446</td><td>Silent completion notices</td><td><strong>Amend D409 / ADR 0239 and the D193 silent-turn contract for issue #504: the first settled assistant reply to a current Host-resolved completion notice may end with no visible text and no tool call without silent-turn recovery or <code>EMPTY_MODEL_RESPONSE</code>. The exception is spent by that reply (silent, textual, or a tool batch), survives a provider retry of the same attempt, and is revoked once accepted user steering enters the model context. Only provenance Main resolved from the Host ledger enables it (<code>kind: completion</code>, the current target session, nonempty message and reply-to IDs); prompt text, plugin content, task/message deliveries, and restored history cannot. An accepted silent reply is persisted as an empty completed row but is excluded from runtime entries and model context, as a restored transcript already is. See the ADR 0239 amendment and E2E-SESSION-completion-notice-allows-silence.</strong></td><td>The completion prompt already says no acknowledgement is needed, yet the runtime retried the silence and reported a failure after a task that had succeeded (issue #504). Bounding the exception to one ledger-verified reply keeps ordinary prompts, deliveries, tool work, and user steering under D193, and keeping the empty reply out of context avoids a provider rejecting the next request.</td></tr><tr><td>D447</td><td>Stage Manager bounds watchdog is macOS-only</td><td><strong>The permanent 1.5s <code>boundsWatchdog</code> in <code>bootstrap/window.ts</code> runs on macOS alone; every other platform clears the interval on its first tick. Its two inputs are macOS-only (the <code>/tmp/pi-window-bounds</code> CG helper, and a &quot;window smaller than 500x400&quot; test the 1040x700 minimum window size makes unreachable), so elsewhere it could never recover anything and only called <code>window.setAlwaysOnTop(false)</code> forever. On Linux/X11 that repeats a <code>_NET_WM_STATE</code> removal, and Mutter answers it by raising the window (<code>meta_window_unmake_above</code> calls <code>meta_window_raise</code> unconditionally), so the app yanked itself back to the top of the stack about 1.5s after the user focused another window even though <code>_NET_WM_STATE</code> never carried <code>_NET_WM_STATE_ABOVE</code>. macOS Stage Manager recovery is unchanged. See D039, D053, D083 and US-UI-19.</strong></td><td>A window that re-raises itself over whatever the user just focused is not usable, and the interval had no purpose off macOS; the fix removes the message instead of teaching each platform to tolerate it.</td></tr><tr><td>D413</td><td>Skill market public-HTTPS catalog fetch</td><td><strong>Additive: Settings → Skills Market discovers SKILL.md catalogs in Electron main under a shared public-HTTPS policy (syntactic public host + DNS classification + per-hop redirect re-validation). The renderer does not fetch. Install remains <code>skills.create</code>. Catalog ids match host <code>valid_capability_id</code>. Expanded documents over 128 KiB are refused. Builtin titles are English. See ADR 0243, E2E-SKILL-MARKET-*, issue #287.</strong></td><td>Community skill discovery needs main-process egress without a plugin-marketplace host allowlist, and copied classifiers would collide with the MCP market.</td></tr><tr><td>D421</td><td>Native Pi session continuation</td><td><strong>Amend baseline D007: discover Pi v3 sessions as source-discriminated projections and continue them through coding-agent <code>AgentSession</code>/<code>SessionManager</code> against their canonical JSONL. Rust remains authoritative for Desktop SQLite/transcripts. Native continuation requires exact saved provider/auth, project trust, canonical path/header identity, and a cooperative lease plus byte/leaf validation; failures remain browseable/read-only. First slice excludes native rename/delete/move/revisions/Plan/Goal/queue/collaboration; the 2026-09-14 ADR 0254 amendment adds native fork with exact stream re-keying and inode-tracked publication; the side-chat panel it added is retired by ADR 0268. See ADR 0254 and E2E-SESSION-native-pi-*.</strong></td><td>Importing a flattened copy cannot preserve Pi&#39;s tree or make later Desktop turns visible to Pi Web.</td></tr><tr><td>D412</td><td>Delta-only coalesced streaming updates</td><td><strong>Amend the local <code>message_update</code> contract: append-only streaming frames carry <code>stream: delta</code> plus <code>deltaText</code>/<code>deltaThinking</code> (and reset flags) without growing <code>content</code>/<code>thinking</code>. Runtime coalesces those frames every 16ms and flushes before semantic boundaries. AgentHost, inflight checkpoints, and the renderer apply deltas; <code>message_start</code>/<code>message_end</code> remain full snapshots. Transcript activity parts keep object identity when only the tail token changes. Protocol version stays 11. See ADR 0242, E2E-STREAM-long-turn-keeps-realtime, and issue #299.</strong></td><td>Each token re-serialized the full assistant snapshot across sidecar, AgentHost, and IPC, so a long turn cost O(n²) bytes and backlogged later short chunks.</td></tr><tr><td>D416</td><td>Git clone accepts only syntactically public hosts</td><td><strong>Amend home git clone: <code>parseGitCloneUrl</code> reuses <code>isPublicHostname</code> so loopback, private, CGNAT, link-local, ULA, and <code>.local</code>/<code>.localhost</code> remotes are rejected before <code>git clone</code> runs. HTTPS/HTTP/SSH/<code>git@host:path</code> to public hosts remain valid. <code>file:</code> and URL passwords stay rejected. Git still performs its own DNS; this is not a market-style pin. See ADR 0247 and E2E-CLONE-public-hostname-rejects-private.</strong></td><td>Clone accepted <code>http://127.0.0.1/...</code> and RFC1918 literals, which is a LAN/SSRF hole the market fetchers already close for HTTPS catalogs.</td></tr><tr><td>D417</td><td>Plugin runtime theme APIs + sidebar image token</td><td><strong>Add <code>pi.app.setTheme</code> and <code>pi.themes.upsert</code>/<code>remove</code>/<code>list</code> under <code>ui.theme</code> (ADR 0260 / issue #352). Remove <code>MAX_THEMES_PER_PLUGIN</code>. Runtime upsert sanitizes CSS like load-time registration and emits <code>pluginChanged</code> (<code>reason: &quot;themes&quot;</code>); <code>setTheme</code> persists <code>AppSettings.theme</code> and emits <code>settingsChanged</code>. Split sidebar paint: <code>--ds-bg-sidebar</code> stays a color; optional <code>--ds-bg-sidebar-image</code> holds gradients/images, with macOS vibrancy stacking sheen over the image layer.</strong></td><td>Theme editor plugins cannot apply a theme from their panel, cannot ship an unlimited library, and cannot live-edit production CSS without reload; sidebar gradients broke <code>color-mix</code> / vibrancy consumers when stuffed into the color token.</td></tr><tr><td>D420</td><td>Structured, bounded, and redacted process logs</td><td><strong>Amend ADR 0046 / ADR 0212: every app/host/agent NDJSON record has a stable event and top-level correlation fields. A normal tool call emits one completion/failure record, while an unexpected sidecar exit emits interruption records for active tools; the tool protocol and transcript remain unchanged. Central logging redacts credentials and local paths, bounds structured data to 8 KiB, summarizes tool results instead of copying output, and mirrors the same sanitized record to development console output.</strong></td><td>The old <code>tool start</code> / <code>tool end</code> rows were redundant and unclear, while free-form child/error details could leak secrets or consume unbounded storage.</td></tr><tr><td>D422</td><td>Host turn-end event for plugins</td><td><strong><code>session:turnEnded</code> is a host event with payload <code>{ sessionId, turnId, reason }</code> (<code>completed</code> / <code>aborted</code> / <code>error</code>), broadcast once per turn actually started by <code>session.beginTurn</code> at the end of turn teardown, after the durable <code>session.endTurn</code> attempt. The emitted <code>turnId</code> is the identity the terminal runtime event carried rather than whichever turn is active, and the plugin tool context&#39;s <code>turnId</code> is populated with the same value. There is no ack and no replay: a live subscribed plugin receives it once, delivery that races a crash, reload, or host quit is not guaranteed, and receiving it does not mean every in-flight tool of that turn has exited, so cleanup must be serialised or scoped by <code>turnId</code>. No new permission is required, and no published host emits it yet (0.14.8 does not include it). See ADR 0252.</strong></td><td>Plugins driving a GUI had to guess turn completion with idle timers, which fire mid-turn and again after the turn ends. A host-owned once-per-turn terminal event with an explicit turn identity lets a plugin settle exactly once, and the same identity in the tool context lets it correlate late tool results.</td></tr><tr><td>D423</td><td>Remove the subagent turn limit</td><td><strong>Amend D328 / ADR 0062 / ADR 0063 / ADR 0119 / ADR 0126 / ADR 0166 / ADR 0210: <code>maxTurns</code> and <code>MAX_SUBAGENT_MAX_TURNS</code> leave the definition type, the frontmatter parser and its clamp/invalid warnings, <code>UserSubagentRecord</code> / <code>UserSubagentInput</code>, the host-core registry (record, input, frontmatter parse, document render, <code>MAX_TURNS_CEILING</code>), the five built-in documents, <code>SUBAGENT_PRESETS</code>, and the Subagent editor. A delegate ends only when it finishes, when the parent calls <code>TaskStop</code>, when the user Stops, or when a terminal parent error aborts it (ADR 0189). <code>maxTurns</code> / <code>max-turns</code> / <code>max_turns</code> in an existing document is now an unrecognized frontmatter key and is ignored like any other unknown key: no error, no warning, no definition-load failure, and no rewrite of the user&#39;s file. The <code>truncated</code> value leaves <code>SubagentRunStatus</code>, the renderer&#39;s <code>SubagentOutcome</code> union, the <code>chat.subagentStatus</code> catalog entry in every locale, and the delegation topology&#39;s warnings count; <code>timed_out</code> stays. No protocol version, schema version, or storage change. See ADR 0253, E2E-155, and E2E-SUBAGENT-legacy-turn-limit-frontmatter-is-ignored.</strong></td><td>The parent cannot see a delegate&#39;s live work, so it cannot size a turn cap, and the shipped 60 / 50 / 40 / 80 backstops had no derivation. The cap&#39;s only effect was to kill a delegate mid-task and surface it as <code>truncated</code> with a partial report — a state neither the user nor the parent model can resume.</td></tr></tbody></table><p>| D244 | Compact context usage summary | <strong>Amend D103 / D184 / ADR 0047: keep the context inspector&#39;s remaining-capacity trigger, used/window counts, turn total, completed-turn speed, exact provider values, aggregate tool types/calls/tokens, and checkpoint summary, but render them as a short summary. Remove the per-tool rows, share bars, source badges, explanatory estimate paragraph, and used-capacity meter from the default panel. No protocol, storage, runtime accounting, or model metadata changes.</strong> <em>(Amended by D347: the trigger moves to the composer toolbar.)</em> | The prior diagnostic layout made a routine capacity check tall and visually dense. Keeping the aggregate signal while removing drill-down chrome makes the default status surface scannable without changing the underlying usage data. See ADR 0103 and E2E-060d / US-UI-61. | | D347 | Composer-docked context usage inspector | <strong>Amend D103 / D184 / D244 / ADR 0047 / ADR 0103: the compact context inspector lives in the composer right toolbar, immediately left of the model × reasoning chip, and always mirrors the newest assistant turn that reported usage. The trigger keeps the remaining-capacity ring and percentage and drops the redundant Context label. The popover heading is remaining tokens plus percentage; rows below share one label/value rhythm separated by spacing, with no inner section rules (D297). Assistant meta keeps the model badge. Renderer only.</strong> | The inspector under the newest answer scrolled out of reach. One composer entry is the single authority for the latest snapshot, and the doubled heading rule is fixed by dropping the extra caption rather than adding hairlines. See ADR 0184 and E2E-060d / US-UI-61. | | D355 | Last-request occupancy in the context inspector | <strong>Amend D103 / D184 / D244 / D347 / ADR 0047 / ADR 0103 / ADR 0184: remaining capacity, used/window counts, turn total, and provider input/output/cache/reasoning/hit-rate are the newest usage-bearing assistant message (the last model request). Occupancy is <code>input + output + reasoning + cacheRead + cacheWrite</code> on that message. They are not the sum of every model call in the visual tool-loop. Completed-turn speed and the aggregate tool row still describe that visual turn. Renderer only; host turn rollups and Token Insights stay additive billing.</strong> | Summing cache reads across a tool loop put 367k cache read next to a 55k window. OpenCode&#39;s context widget uses only the last assistant message. See ADR 0193 and E2E-060d. | | D356 | Optional subagent thinking override | <strong>Amend ADR 0062 / ADR 0144: subagent frontmatter accepts <code>thinkingLevel: omit</code> in addition to inherit and the seven canonical levels. <code>omit</code> keeps the agent bookkeeping state at <code>off</code> but uses the low-level provider stream so no thinking override is synthesized. Explicit <code>off</code> still disables thinking. Model-configuration thinking chips use an accent/inverted-text selected state in both themes. No storage schema or protocol version change. See ADR 0194 and E2E-203.</strong> | Users need inherit, explicit off, and leave-the-provider-default as three distinct choices, and selected thinking chips were unreadable in dark mode. | | D348 | macOS sidebar source-list vibrancy | <strong>Amend D304: the main window uses Electron <code>vibrancy: &quot;sidebar&quot;</code> instead of <code>under-window</code>. <code>nativeTheme.themeSource</code> follows the app theme (<code>system</code> / <code>light</code> / <code>dark</code> / plugin base) process-wide. Vibrancy re-applies only when <code>themeSource</code> changes; a missing plugin theme falls back to <code>system</code>.</strong> | macOS 26 Liquid Glass made <code>under-window</code> a light plate that the 40% tint could not keep dark, including when app and OS appearances were both dark. See <code>04-ux/08-component-spec.md</code> §1.7 and US-UI-74 / E2E-076. | | D243 | Model-aware image attachment transport | <strong>Amend D197 / ADR 0059: Composer file references retain structured kind/name/MIME metadata. Electron main resolves vision from the exact models.dev model record when matched, or the exact pi-ai fallback record (<code>modalities.input</code> / <code>input</code> includes <code>image</code>), then applies an exact binding&#39;s explicit <code>supportsImages</code> override. It stores image bytes as <code>attachments/&lt;sha256&gt;</code>, sends eligible images as transient image blocks, and falls back to a safe <code>@path</code> for unknown/non-vision or oversized images. Durable messages store refs and metadata, never base64; the renderer shows an accessible capability status.</strong> | The previous path-only contract made a vision-capable GPT model receive a scratch path instead of image content. Keeping the published capability authoritative by default while allowing an explicit endpoint-local override keeps Composer and transport aligned without mutating shared model metadata (ADR 0101, amended by D266 and D392 / ADR 0218). | | D245 | OpenCode-style bounded provider 429 retry | <strong>Amend D186 / D233 / ADR 0091: <code>PROVIDER_RATE_LIMITED</code> gets five silent, abortable retries after the initial attempt, with one counter shared across request setup and mid-stream recovery. Disable nested pi-ai retries; capture failed response status/headers; honor <code>retry-after-ms</code>, <code>retry-after</code> seconds, HTTP-date, then 2-second exponential backoff with 25% positive jitter and a 30-second cap. Reuse the assistant bubble and suppress intermediate lifecycle/error events in main sessions and builtin subagents. Authentication, model-selection, malformed-request, and context errors remain terminal; exhaustion records <code>retryAttempt: 5</code> and <code>providerStatus: 429</code>.</strong> | The prior split one-retry policy exposed transient 429s too early and allowed setup and stream layers to drift. OpenCode&#39;s bounded, header-aware policy preserves user control without hiding persistent provider failure or multiplying nested retries. | | D237 | Vendor-account (OAuth) login for providers | <strong>A provider row may be authenticated by a vendor subscription instead of an API key. pi-ai&#39;s seven OAuth flows are registered statically at startup (<code>registerBunOAuthFlows()</code>); Electron main owns login/logout orchestration and implements pi-ai&#39;s <code>CredentialStore</code> over host-core&#39;s encrypted secret store under the new ref <code>secret:provider:&lt;id&gt;:oauth</code>, serializing <code>modify</code> per provider for locked refresh. <code>auth_kind</code> gains <code>oauth</code>, <code>has_secret</code> widens to &quot;api key <strong>or</strong> oauth&quot;, and a new <code>has_oauth</code> plus a non-secret <code>oauthAccountLabel</code> drive badges and hide the key input; host protocol and storage schema are unchanged. The vendor card list is derived from <code>models.getProviders().filter(p =&gt; p.auth.oauth)</code>, and login upserts one row per <code>vendorKey</code>. The sidecar&#39;s launch payload for an OAuth row carries <code>apiKey: &quot;&quot;</code>; the runtime injects a <code>resolveAuth</code> callback that calls the new host-proxy method <code>provider.resolveAuth</code>, which Electron main answers itself — never forwarding to host-core — after checking the <code>(sessionId, providerId)</code> pair against the per-launch binding table. The reply is a short-lived <code>ModelAuth</code> (<code>apiKey</code>/<code>headers</code>/<code>baseUrl</code>), so a refresh token never leaves main and <code>matches()</code> keeps a vendor runtime warm across turns. <code>providers.listModels</code> and the connection test go through the authenticated account instead of probing <code>/models</code>; apiStyle follows the selected model and gains <code>openai_codex_responses</code> and <code>pi_messages</code>. Five invoke channels plus one event channel under <code>pi-desktop/providers/oauth/*</code> carry the interaction.</strong> | Subscribers of Claude Pro/Max, ChatGPT Plus/Pro and Copilot had to buy separate API credit to use the app. pi-ai ships the flows but declares login orchestration app-owned. Vendor tokens expire in about an hour, so resolving once at launch would break long sessions and churn <code>matches()</code> every turn; per-request resolution keeps the runtime stable while giving the model-directed process only a revocable token for the provider its session is bound to — strictly less than the long-lived API key it receives today (ADR 0095). |</p><p>| D238 | Flat Settings directory and marketplace context ownership | <strong>Settings renders one flat searchable directory in the exact order Basics / AI / Shortcuts / Instructions / Model configuration / Import / Project archive / Info. Personal, Integrations, Coding, and other group headings are removed. The Settings Extensions destination is removed; its official/mirror/custom marketplace source selector moves into Extensions → Marketplace beside the catalog and retains the same persisted settings and refresh behavior. No IPC, host protocol, storage, provider, permission, or project ownership contract changes.</strong> | The nine-entry Settings rail repeated its own navigation hierarchy and used the same Extensions label as the app-shell plugin destination. Putting marketplace source selection beside marketplace browsing removes the duplicate entry while preserving the mirror/custom-source workflow (ADR 0096). |</p><p>| D239 | Place global defaults under AI | <strong>The eight-destination Settings directory remains unchanged. Basics owns Appearance and platform-supported close behavior. 全局 AI / AI owns Permissions and the Defaults card containing default operating mode (Agent / Plan / Goal), command shell selection/fallback status, and Enter-to-send. Model configuration retains the default provider/model selector. Only renderer content and Settings search ownership move; persisted settings, host APIs, runtime semantics, and deep-link contracts remain unchanged (ADR 0097).</strong> | Default mode, command shell, and Enter-to-send change global agent behavior, so placing them beside appearance mixed unrelated concerns and made the AI destination incomplete. | | D240 | Independent vendor OAuth accounts and settings ownership | <strong>Amend D237 / ADR 0095: every OAuth login creates a fresh provider row and row-scoped <code>CredentialStore</code>, even when <code>vendorKey</code> matches an existing account. The Vendor accounts card owns the full OAuth row lifecycle and deletes through <code>providers.delete</code>; the AI services list excludes OAuth rows but the default selector may still choose them. Vendor auth bindings and resolution use the exact <code>providerId</code>; ambiguous vendor/name aliases for subagents fail closed.</strong> | One vendor-global row made a second account overwrite or reuse the first credential, while sign-out left a stale AI-service row. The row id is the only unambiguous account identity and lets deletion remove exactly one credential and configuration. See ADR 0098. | | D339 | Per-provider User-Agent override | <strong>Superseded by D341.</strong> Each AI-service and OAuth provider row may store optional <code>config_json.userAgent</code>. Empty keeps adapter defaults (pi-ai / <code>claude-cli</code> / OpenCode). A non-empty value is last-writer <code>User-Agent</code> on that row&#39;s outbound HTTP (turns, subagents, one-shots, discovery, connection test, OAuth refresh) via a fetch wrapper plus stream-option headers. Update <code>&quot;&quot;</code> clears. Max 256 bytes; no CR/LF. Advanced UI only; first OAuth login does not collect it. <code>matches()</code> includes <code>userAgent</code>. The unused <code>headers</code> map stays unimplemented. No schema/protocol bump. | Gateways and vendor subscriptions inspect User-Agent; Codex overwrites it after extra headers. See ADR 0176 and E2E-005G. | | D341 | Per-provider custom HTTP headers | <strong>Amend D339 / ADR 0176: each AI-service and OAuth row stores optional <code>config_json.headers</code>. Empty / <code>{}</code> keeps adapter defaults. A non-empty map is last-writer on that row&#39;s outbound HTTP (turns, subagents, one-shots, discovery, connection test, OAuth refresh) via a fetch wrapper plus stream-option headers. Leftover <code>userAgent</code> migrates to <code>headers[&quot;User-Agent&quot;]</code>. Max 32 headers; key ≤ 256 bytes; value ≤ 4096 bytes; no CR/LF; reserved auth/hop-by-hop/routing keys rejected. Compact KeyValueRows Advanced UI; first OAuth login does not collect headers. <code>matches()</code> includes the map. No schema/protocol bump.</strong> | Users needed arbitrary non-secret headers, not only User-Agent. See ADR 0178 and E2E-005G. | | D242 | Builtin subagents inherit the parent permission mode | <strong>Builtin subagents use the default <code>permission: inherit</code> behavior. The builtin <code>fixer</code> no longer overrides the parent session, so <code>auto</code> covers its in-root and explicit external-path calls without a second authorization card; explicit non-<code>inherit</code> scopes on eligible builtin or user definitions remain intentional overrides.</strong> | The observed popup came from the builtin <code>fixer</code> replacing an <code>auto</code> parent with <code>accept-edits</code>; inheriting the parent fixes the UX without weakening host-core containment or external-path permission rules (ADR 0100). | | D241 | Titled Settings navigation clusters | <strong>Keep the eight-destination Settings directory flat and searchable, but render four non-interactive localized group headings — Personal / 个人 (Basics, AI, Shortcuts), Agent / 智能体 (Instructions, Model configuration), Workspace / 工作区 (Import, Project archive), and About / 关于 (Info). Headings use whitespace for separation and no divider lines. Empty groups disappear with filtered search. This supersedes only D238&#39;s prohibition on group headings; destination order, IDs, search ownership, and marketplace placement remain unchanged.</strong> | The flat rows became visually dense without scan landmarks. Muted headings restore grouping while preserving one-level navigation and the existing destination ownership. | | D442 | Marketplace download requests carry a device identifier | <strong>Amend D238: the official plugin channel resolves every install and update through <code>POST /api/v1/download/resolve</code> and sends a stable <code>deviceId</code> — the SHA-256 digest of a platform machine identifier, or a locally persisted random id when none is readable. The client tries the returned mirrors in order, verifies <code>sha256</code> and <code>sizeBytes</code> before extracting, never caches the answer, and falls back to the catalog&#39;s own package URL only when the platform could not be reached. The GitHub and CNB backup channels and <code>custom</code> keep the existing static relative-URL resolution. See ADR 0276 and E2E-PLUGIN-official-channel-resolves-through-the-platform.</strong> | The platform needs one stable installation identity to count a download once and to rate-limit per device instead of per source address; the value is a digest, so no machine code leaves the machine, and no new UI surface is introduced. |</p><h2 id="d-codex-visual-parity-decisions-0-3-5" tabindex="-1">D. Codex visual parity decisions (0.3.5+) <a class="header-anchor" href="#d-codex-visual-parity-decisions-0-3-5" aria-label="Permalink to &quot;D. Codex visual parity decisions (0.3.5+)&quot;">​</a></h2><p>Gold source: local Codex electron captures; latest row wins where rows conflict.</p><table tabindex="0"><thead><tr><th>ID</th><th>Topic</th><th>Decision</th><th>Rationale</th></tr></thead><tbody><tr><td>D034</td><td>Desktop visual baseline</td><td><strong>Codex electron-dark 1:1 shell (charcoal gray, floating composer, ~275px sidebar)</strong></td><td>Match local Codex usability and density; keep PI-Desktop product branding</td></tr><tr><td>D035</td><td>Shell display name</td><td><em>(superseded by D094)</em> <strong>UI chrome uses shellName &quot;Codex&quot;; product/about remains PI-Desktop</strong></td><td>Satisfy visual 1:1 replica goal while preserving product identity in about/settings</td></tr><tr><td>D036</td><td>Theme chrome tokens</td><td><strong>All shell chrome (nav, threads, chips, title buttons) uses semantic <code>--ds-*</code> text/surface tokens; no raw gray-0 text in light mode</strong></td><td>Light macOS default was unusable when nav used white ink on <code>#f3f3f3</code></td></tr><tr><td>D037</td><td>Dark sidebar surface</td><td><strong>Dark sidebar uses <code>#000000</code> (Codex <code>surface-under</code>); main pane stays <code>#181818</code> (<code>gray-900</code>)</strong></td><td>Match electron-dark sideBar vs main surface separation</td></tr><tr><td>D038</td><td>Dark composer plate</td><td><em>(superseded by D047, then D061)</em> Dark floating composer uses solid <code>#212121</code> with stronger elevation shadow than light</td><td>Codex elevated-primary must read as a box against <code>#181818</code>; transparent mix alone looks flat</td></tr><tr><td>D039</td><td>Stage Manager bounds</td><td><strong>Permanent host watchdog restores footprint while width/height remain collapsed</strong></td><td>20s burst was insufficient under Stage Manager thrash</td></tr><tr><td>D040</td><td>Composer intelligence control</td><td><em>(superseded by D091)</em> Custom effort chip opens a popover (effort radio + model heading + settings) instead of cycling on click</td><td>Replaced because the control changed labels without configuring pi</td></tr><tr><td>D041</td><td>Profile footer</td><td><em>(superseded by D113)</em> <strong>Custom footer opens profile menu (Settings / Logs / Theme); cloud badge remains update stand-in</strong></td><td>D113 replaces the cloud/update stand-in and generic gear row with a truthful local-profile footer</td></tr><tr><td>D042</td><td>Projects page</td><td><em>(superseded by D066 index table)</em> Projects is a card grid of recent/active local workspaces with pin + glyph color (localStorage recents)</td><td>Match Codex Projects destination density without cloud project backend</td></tr><tr><td>D043</td><td>Settings shell</td><td><em>(superseded by D062/D063 full-page shell)</em> Settings uses left nav rail on sidebar surface + content pane (General/Providers/Plugins/About)</td><td>Closer to Codex settings IA than a top-only tab strip</td></tr><tr><td>D044</td><td>Destination list chrome</td><td><strong>PRs/Scheduled/Plugins use shared dest-row list + filter chips; light cards white elevated</strong></td><td>Match Codex destination density without full cloud backends</td></tr><tr><td>D045</td><td>Home empty stack</td><td><strong>Empty chat keeps composer in home flow (not absolute bottom-only dock); refined by D047 split grow</strong></td><td>Initial fix for large empty gap; D047 corrects dual-grow vertical model</td></tr><tr><td>D046</td><td>Composer placeholder</td><td><em>(superseded by D094)</em> <strong>Empty draft uses Codex placeholder (EN/zh-CN) instead of blank</strong></td><td>Empty white plate read as broken without ink; match the earlier visual gold copy</td></tr><tr><td>D047</td><td>Home split grow</td><td><em>(geometry superseded by D111)</em> <strong>Empty home used upper/lower grow regions (hero items-end + composer justify-end); dark box uses Codex elevation-prominent</strong></td><td>Match electron dual grow; D111 replaces dual-grow portal with a scrollable flow stack to stop composer/card collisions</td></tr><tr><td>D048</td><td>Sidebar recents label</td><td><em>(superseded by D088)</em> <strong>Recents section uses live Codex gold label EN <code>Recents</code> / zh-CN <code>最近</code> (not asar-only <code>Tasks</code>/<code>任务</code>)</strong></td><td>Visual gold + live coding shell section heading between plugins and thread list</td></tr><tr><td>D049</td><td>Home suggestion cards</td><td><em>(superseded by D131)</em> <strong>Empty home shows 4 ambient cards under hero (auto-fit row) and prefills starter prompts on click</strong></td><td>D131 removes the card row and prompt-prefill entry points from the empty home</td></tr><tr><td>D050</td><td>Empty composer plate height</td><td><em>(superseded by D055, then D061)</em> Home empty composer min-height ~112px (compact); model chip shows model id (effort stays in menu)</td><td>Match Codex empty plate density; model chip chrome closer to electron model picker trigger</td></tr><tr><td>D051</td><td>Sidebar nav density</td><td><em>(session-list IA superseded by D088; row density retained)</em> <strong>Nav rows ~32px pitch, recents rows ~28–31px, section label <code>最近</code>/<code>Recents</code></strong></td><td>Close light-home sidebar residual vs cx-home-clean</td></tr><tr><td>D052</td><td>Home vertical + night box polish</td><td><em>(workspace-chip surface superseded by D095; remaining guidance retained)</em> <strong>Upper pb ~62px (hero first-ink ~y305); light chips <code>#f3f3f3</code>; light composer elevation stronger; dark home composer solid <code>#212121</code>; toolbar controls 28px</strong></td><td>Close residual heat at hero y≈300 and composer band; night plate must not flatten into <code>#181818</code></td></tr><tr><td>D053</td><td>Stage Manager CG detection</td><td><strong>CG bounds helper matches any window layer by pid; missing-CG needs streak≥3 before shelf recovery; avoid permanent alwaysOnTop</strong></td><td>alwaysOnTop floating layer broke layer-0 helpers and caused restore thrash</td></tr><tr><td>D054</td><td>Empty draft row + infinity cue</td><td><em>(∞ cue superseded by D094; leading brand cue superseded by D160)</em> <strong>Composer auto-resize must never collapse empty textarea height (&lt;28px); keep a visible brand cue left of draft; solid disabled send (<code>#bdbdbd</code> light); denser placeholder ink; night plate solid <code>#212121</code></strong></td><td>Empty <code>height:0</code> auto-resize hid placeholder and read as broken night/light box; gold draft row needs visible mark + ink density</td></tr><tr><td>D055</td><td>Empty plate draft Y</td><td><em>(plate-height guidance superseded by D061; workspace-chip density superseded by D095)</em> Home empty shell min-height ~148px (bottom-aligned) so draft densest ink ≈y556 vs gold; chips compact 28px</td><td>112px plate left draft ~30px low; grow plate upward without moving toolbar footing</td></tr><tr><td>D056</td><td>Empty-home workspace chips</td><td><em>(superseded by D095)</em> <strong>Hide project/Local/branch capsule on empty home always; show only in thread-docked composer</strong></td><td>cx-home-clean empty gold has no capsule band above the plate even with project title</td></tr><tr><td>D057</td><td>Home mark + hero title optical</td><td><em>(home mark superseded by D094; title guidance retained)</em> <strong>Empty-home Codex mark uses denser stroke; short workspace basenames display as <code>PI-Desktop</code> for gold title span</strong></td><td>Hero residual was thin mark + short project label under-inking title vs Codex gold</td></tr><tr><td>D058</td><td>Home content width + dark ink tokens</td><td><strong>Home dual-grow max width uses <code>768px</code> (not <code>48rem</code> under 14px root); home horizontal pad 12px; hero title/night controls use theme tokens; night home plate scoped to dark only</strong></td><td><code>48rem</code> at 14px root shrank plate ~120px vs Codex gold; hardcoded light hero ink made night title unreadable</td></tr><tr><td>D059</td><td>Light disabled send ink</td><td><strong>Disabled send chip <code>#8e8e90</code> + white arrow (not <code>#bdbdbd</code>)</strong></td><td>Pixel-match cx-home-clean empty send control</td></tr><tr><td>D060</td><td>Light New task ghost row</td><td><strong>Light empty-home New task is transparent (no solid chip); only hover wash</strong></td><td>Gold has icon+label without filled pill; filled <code>#e8</code> chip was main nav residual</td></tr><tr><td>D061</td><td>Empty plate Y + night elevated-primary</td><td><strong>Home empty plate min-height 140px + wrap bottom pad 16px (top ~y536–538 / draft ~y552 / foot shadow ~y674); light+dark home plates use elevated-primary fill and downward elevation (no upward omni glow); dark fill <code>#212121f5</code></strong></td><td>Plate was high with pre-plate halo; solid night plate + heavy omni shadow diverged from Codex elevated-primary and gold foot band</td></tr><tr><td>D062</td><td>Settings Codex shell</td><td><strong>Settings uses Codex grouped rail (Personal/Integrations) + search + Back to app; content is elevated row panels; Providers/Plugins retained for local-first; MCP empty state under Integrations</strong></td><td>Destination parity gap; prior 4-item flat rail diverged from Codex settings IA</td></tr><tr><td>D063</td><td>Settings full-page takeover</td><td><strong>Settings replaces app sidebar with Codex full-page shell: back+search+icon groups (Personal/Integrations/Coding), elevated permission/general cards, local Providers/Plugins retained</strong></td><td>Nested settings-inside-main-pane diverged from live Codex settings gold</td></tr><tr><td>D064</td><td>Settings general content parity</td><td><strong>Basics card rows match Codex: default open target, language, menu bar, bottom panel; nav adds Pets/Appshots; sun/pet/snapshot icons; pill selects</strong></td><td>Closer 1:1 to live Codex settings gold content band</td></tr><tr><td>D065</td><td>Settings general gold polish</td><td><strong>Permission rows include blue Learn more links + full-access risk copy; open-target pill shows VS Code glyph; Agent uses circular-arrow icon; Integrations order Appshots→Plugins→Browser→Computer→MCP; Enter-to-send moves to Agent</strong></td><td>Residual gaps vs cx-settings-try after full-page shell</td></tr><tr><td>D066</td><td>Home-with-project chrome + projects index</td><td><em>(composer intelligence label superseded by D091; workspace-chip portion superseded by D095)</em> Home shows workspace chips when project open (no ∞); home placeholder 随心输入/Ask anything; footer gear+help; Projects page is Codex index (search/columns/expand/actions) using setProject</td><td>Gold cx-home-clean with project + projects-index-page parity</td></tr><tr><td>D067</td><td>Home suggestion glyphs + chip gap</td><td><em>(suggestion-glyph portion superseded by D131; composer chip-gap portion superseded by D095)</em> <strong>Suggestion icons match Codex (code/hammer/refresh/bug) with blue/purple/green/orange tones; composer chip gap 8px and denser capsule</strong></td><td>D131 removes the cards; D095 remains authoritative for composer spacing</td></tr><tr><td>D068</td><td>Recents row actions + fixture titles</td><td><em>(sidebar actions superseded by D088, then reorganized by D093; fixture-title guidance retained)</em> <strong>Active/hover recent rows show pin + panel trailing actions; capture/fixtures prefer Chinese titled empty sessions (同步代码) over bare New task</strong></td><td>Gold sidebar selected row chrome; reduce selection residual</td></tr><tr><td>D069</td><td>Destination title scale + dark New task ghost</td><td><strong>Destination page titles use Codex 28px/560 weight; New task is transparent ghost in dark too; capture drops English noise fixtures and pins 同步代码</strong></td><td>PR/Projects title mismatch; dark New task read as selected chip</td></tr><tr><td>D070</td><td>Settings gold metric polish</td><td><strong>Settings rail 275px/#f4f4f4; denser nav; content title offset; 32×20 accent toggles; Account arrow-up-right; 14px cards; 720px content band</strong></td><td>Residual vs cx-settings-try (rail width, toggle size, title Y, external mark)</td></tr><tr><td>D071</td><td>Transcript interaction parity</td><td><strong>Tool calls render as Codex-style lightweight disclosure rows (caret + name + mono arg hint + spinner/status, clamped inset body) replacing boxed cards; auto-scroll only while pinned to bottom with floating jump-to-latest pill (send / retry / regenerate re-pin per D151); readable Working… line with elapsed time and a compact running marker (amended by D290); hover copy on messages and code blocks</strong></td><td>Boxed tool cards and forced scrollIntoView diverged from Codex transcript feel; spec 7.4 scroll pause was unimplemented</td></tr><tr><td>D072</td><td>Typography/radius token enforcement</td><td><strong>All font-size/weight/line-height/letter-spacing/border-radius values must use <code>@theme</code> token vars (<code>--text-*</code> ramp with <code>-plus</code> half-steps, <code>--font-weight-*</code> incl. 520/560, <code>--leading-*</code>, <code>--tracking-*</code>, 12-step <code>--radius-*</code>); raw literals in CSS and TSX arbitrary utilities are blocked by <code>scripts/check-style-tokens.mjs</code> wired into <code>pnpm lint</code>; pixel values preserved exactly (no visual change)</strong></td><td>~130 scattered literals drifted from any scale; design-system doc §5.2/§6.2 tables were stale vs implementation</td></tr><tr><td>D073</td><td>Full renderer i18n coverage</td><td><strong>Every user-visible renderer string flows through i18next (<code>en</code> source of truth, <code>zh-CN</code> via <code>satisfies EnglishCatalog</code>): ContextPanel/CommandPalette/PermissionDialog wired; toast/aria/title/placeholder literals keyed; session default titles come from <code>i18n.t</code> with a shared case-insensitive <code>isDefaultSessionTitle</code> matcher covering legacy titles across locales; proper nouns (VS Code, Finder) and native language names stay untranslated</strong></td><td>Six components bypassed i18n entirely; default-title matching was duplicated in store and Sidebar and missed zh &quot;新对话&quot;</td></tr><tr><td>D131</td><td>Empty home without suggestion cards</td><td><em>(starter-grid clause amended by D205, then superseded by D206)</em> <strong>The empty chat home temporarily rendered only the hero, optional first-run checklist, and composer; the original four Explore / Build / Review / Fix cards, their colored glyphs, and their prompt-prefill actions were removed. This superseded D049/D067 and the original card-specific clauses of D111 while retaining its single scrollable flow layout.</strong></td><td>The direct composer remained the primary task entry; removing the original decorative starter row addressed noisy card treatment. D205 briefly reintroduced a quieter, non-submitting developer grid before D206 returned the empty state to direct entry.</td></tr><tr><td>D133</td><td>Project index moves to Settings archive</td><td><em>(five-destination count/order superseded by D166; flat-list presentation superseded by D168)</em> <strong>The home sidebar no longer has a standalone Projects destination. Settings adds Project archive (zh-CN: 项目归档) after Import and before Info, bringing the compact directory to Basics / Model configuration / Import / Project archive / Info. The archive reuses the durable Projects index and always includes archived records, with search, add, activate, task expansion, pin, archive/restore, and close actions. Project and session groups remain in the home sidebar for active work. Global search exposes the archive as a Settings result, not a standalone page. This supersedes the standalone-destination clauses of D042/D066 and D090&#39;s four-destination limit without changing project storage or activation semantics.</strong></td><td>Active project work already lives in retained sidebar groups; moving historical project management into Settings reduces primary navigation while keeping recovery and archive controls discoverable.</td></tr><tr><td>D168</td><td>Project archive presentation redesign</td><td><em>(band layout and per-section panels superseded by D267)</em> <strong>Project archive renders three stacked bands: an overview banner (intent sentence, primary Add project, and four derived counters for projects, open, archived, sessions), a toolbar (search with clear affordance and live match count, plus a Recent/Name sort segmented control), and a grouped index whose always-visible sections run Pinned / All projects / Archived with per-section counts, one settings panel per section, and hairline row separators. Rows carry a disclosure control, glyph, name with Active/Open/pinned/Archived tags, one meta line (shortened monospace path, branch, session count), relative last-active time, and hover/focus-revealed New task plus row menu; the menu groups create/edit above pin, archive/restore, and destructive Close, and dismisses on Escape or outside press. Archived rows are grouped and softened, never hidden or filtered, so D133&#39;s no-visibility-toggle rule still holds. This supersedes D133&#39;s flat-list presentation without changing project storage, search matching, session batching, or activation semantics.</strong></td><td>The flat single-list archive gave equal weight to pinned, working, and archived records and hid its disclosure and row actions behind hover, so scanning a long durable index meant reading every row. Grouping with derived counters and an explicit sort makes state legible at a glance while keeping archived history permanently reachable.</td></tr><tr><td>D267</td><td>Project archive is one workbench, not three bands</td><td><em>(row anatomy and inline expansion superseded by D455)</em> <strong>Revise D168&#39;s band layout: Settings → Project archive renders the D257 one-workbench composition — a quiet intro line carrying only the page description, one toolbar (Recent/Name sort on the shared <code>settings-segment</code> primitive, search with clear affordance and live match count, primary Add project), and one settings panel whose always-visible Pinned / All projects / Archived groups are non-interactive in-panel header strips with per-section counts instead of one panel per section. The decorative gradient hero band is removed together with the four page-level overview counters it carried, retiring the <code>project.statProjects</code>, <code>project.statOpen</code>, <code>project.statArchived</code>, and <code>project.statSessions</code> keys; the per-group counts on the panel&#39;s header strips are now the only totals. The external uppercase section labels are removed as well; row geometry matches the capability rows (32px controls, 14px list gap, 28px row glyph). Beyond dropping the four retired counter keys this is presentation only: D168&#39;s row anatomy, row menu grouping, search matching, session batching, activation semantics, and accessibility semantics are unchanged, and no ADR is required.</strong></td><td>D168&#39;s overview banner used a decorative gradient and <code>--text-xl</code> counter tiles, which the design system forbids, and its per-section panels repeated the same elevated frame three times. Demoting the counters to an inline run kept the clutter without earning it: every total they showed is already legible from the per-group strip counts, so restating them above the toolbar duplicated numbers and gave the destination a header no sibling page has. Dropping them leaves the durable index looking and behaving like the agent capability pages.</td></tr><tr><td>D455</td><td>Project archive is a list + inspector</td><td><em>(row anatomy superseded by the same-day &quot;Project archive reads as an inset grouped card list&quot; entry)</em> <strong>Revise D267&#39;s expanding rows: Settings → Project archive keeps the quiet intro and toolbar, then a one-column workbench. The index remains Pinned / All projects / Archived with per-section counts and no visibility toggle (D133). Compact rows show glyph, name, one status tag, session count, and relative time. A click selects and stays in Settings; double-click, Enter, or the inspector Open action activates and returns to chat. Folders, chats (batches of eight), and the row menu open under the selected row at full width. Search still matches session titles and selects the owning project. Presentation only: no IPC, storage, or host change. See ADR 0294 and E2E-038.</strong></td><td>Inline expansion and per-row menus made a long durable index hard to scan, and clicking a name left Settings instead of managing the project.</td></tr><tr><td>D456</td><td>Session thinking-parameter omission</td><td><strong>Amend ADR 0194 / ADR 0144 / ADR 0221: session <code>thinkingLevel</code> accepts <code>omit</code> in addition to the seven canonical levels. Composer prepends <code>omit</code> on a reasoning model. Settings <code>defaultThinkingLevel</code> also accepts <code>omit</code> when the binding enables any reasoning level. Runtime bookkeeping stays <code>off</code> and uses the low-level provider stream so no thinking override is synthesized. Binding/catalog capability lists stay canonical. Schema v19 widens the session CHECK to include <code>omit</code>. Handshake protocol version is unchanged. See ADR 0295 and E2E-203a.</strong></td><td>Explicit <code>off</code> still serializes a disable; users need a session-level do-not-send matching subagents.</td></tr><tr><td>D458</td><td>Composer reasoning slider on the menu root</td><td><strong>Amend the combined model × reasoning menu: when more than one level is listed, a native range slider with one labeled stop per level sits under the Reasoning level entry. Slider and tick commits persist the last pending level through <code>configureActiveSession</code> without leaving the root; the entry still opens the classic radio list. Tick labels are not tab stops. The slider shows a rail with one track dot per stop and a visible label under each stop; the dots row and labels row are full-width n-column grids and the range input overlays the rail inset by half a column minus the thumb radius, so the dot, thumb and label share one column center for every stop count. The selected stop uses the accent token, the rest muted. Renderer only. See issue #417, <code>04-ux/08-component-spec.md</code>, and E2E-050.</strong></td><td>Switching a level required a submenu trip; a Codex-style slider keeps the radio list while making quick adjustments one drag.</td></tr></tbody></table><h2 id="e-m5-hardening-decisions-0-4-0" tabindex="-1">E. M5 hardening decisions (0.4.0) <a class="header-anchor" href="#e-m5-hardening-decisions-0-4-0" aria-label="Permalink to &quot;E. M5 hardening decisions (0.4.0)&quot;">​</a></h2><table tabindex="0"><thead><tr><th>ID</th><th>Topic</th><th>Decision</th><th>Rationale</th></tr></thead><tbody><tr><td>D078</td><td>macOS signing lanes</td><td><em>(amended by D450 / ADR 0289)</em> <strong>Static configuration does not embed a certificate identity, so local builds without a configured certificate remain unsigned. Official GitHub tag releases require Developer ID signing and Apple notarization; CI receives certificate material through Actions secrets and rejects unstapled artifacts.</strong></td><td>Contributors can package without credentials while published macOS artifacts satisfy Gatekeeper. See 06-delivery/06-release-runbook.</td></tr><tr><td>D079</td><td>App icon / brand mark v1</td><td><em>(renderer asset path refined by D221)</em> <strong><code>build/icon_1024.png</code> is the canonical PI-Desktop logo; <code>scripts/make-icon.py</code> derives <code>build/icon.icns</code> without overwriting the PNG; packaged macOS builds, <code>pnpm dev</code>, and renderer chrome reuse those assets</strong></td><td>Keep one visual identity across development, renderer, and packaged lanes while preventing the derivation script from restoring the obsolete generated mark</td></tr><tr><td>D080</td><td>Backend supervision</td><td><strong>Child exit rejects in-flight RPCs immediately; backoff restarts (0.5s→4s, max 3 per 2min); <code>hostStatus</code> events drive renderer degradation UI</strong></td><td>Crash recovery without hangs; fail visible, not silent</td></tr><tr><td>D081</td><td>Renderer sandbox</td><td><strong><code>sandbox: true</code> with fully bundled CJS preload; production CSP drops <code>unsafe-eval</code> and localhost connect-src</strong></td><td>Electron security baseline; verified by <code>test:e2e:boot</code></td></tr><tr><td>D082</td><td>Log channels</td><td><em>(superseded in part by D182)</em> <strong>app/host/agent NDJSON files with 5MB rotation (keep 2 rotated) via main-process Logger; audit channel stays in host-core SQLite</strong></td><td>Diagnosable failures without unbounded growth; audit needs queryability</td></tr><tr><td>D083</td><td>Window state</td><td><strong>Persist last good bounds to <code>window-state.json</code> (min 960×640 to restore); Stage Manager shelf recovery keeps the Codex footprint; capture runs force deterministic bounds</strong></td><td>Users keep their window; shelf recovery and pixel captures stay deterministic</td></tr><tr><td>D084</td><td>Cross-platform shell strategy</td><td><em>(superseded by D190)</em> <strong>The Bash tool runs bash on every platform, resolved once per process: <code>PI_DESKTOP_BASH</code> override → Unix well-known paths + PATH → Windows <code>bash.exe</code> derived from Git for Windows (git on PATH, standard install dirs, then PATH minus the WSL <code>System32</code> launcher); Unix uses <code>bash -lc</code>, Windows <code>bash -c</code> + <code>CREATE_NO_WINDOW</code>; no bash bundled in installers; missing shell surfaces stable <code>SHELL_NOT_FOUND</code> with install guidance</strong></td><td>Superseded by the selectable shell catalog and stable execution identity in D190</td></tr><tr><td>D085</td><td>Toast system v2</td><td><strong>Single global toast stack (<code>ToastHost</code> + store queue) replaces the string <code>setToast</code>: <code>showToast(message, {variant, duration})</code> with info/success/warning/error variants (Lucide icon tinted by semantic token on a neutral elevated plate), auto-dismiss 4s / error 8s / 0 sticky owned by the system (no caller timers), hover pause, max 4 with dedupe, enter/exit motion + reduced-motion-safe removal, <code>aria-live</code> + role status/alert; usage rules in 08-component-spec §17</strong></td><td>Old toast was a bare fixed div: no variants or stacking, and most call sites never cleared it so messages persisted forever; callers hand-rolled timeouts</td></tr><tr><td>D086</td><td>Storage schema v2</td><td><strong>Single <code>pi.sqlite</code> (host-core exclusive) rebuilt per 03-runtime/04: <code>kv</code> namespaces replace <code>meta</code>/<code>settings</code> and host plugin settings; <code>projects</code> replaces the workspace singleton; transcripts become canonical block arrays (<code>messages.content_json</code> + extracted <code>text</code>, ms-integer times, O(1) per-session <code>seq</code>, stable <code>mid</code> rowid) with <code>turns</code> carrying state-machine status + usage rollups and FTS5 trigram search; new <code>models</code> catalog, <code>artifacts</code>, <code>scheduled_tasks</code>+<code>task_runs</code> (moved out of Electron&#39;s JSON, fixing a D002 violation); indexed prunable <code>audit_log</code>; <code>PRAGMA user_version</code> migrations with pre-migration <code>.bak</code>; dead <code>plugins</code>/<code>provider_models</code> tables dropped (registry.json stays authoritative)</strong></td><td>v1 schema was a lossy UI projection (no turns/usage/blocks/attachments), ordered by <code>MAX+1</code> scans, had zero secondary indexes, two dead tables, RFC3339 text times, and scheduled tasks bypassing host ownership; spec&#39;d features (artifacts view, cost chips, run history, project grouping, global search, catalog refresh) had no storage to land on</td></tr><tr><td>D087</td><td>Immersive composer context rail</td><td><em>(superseded by D095)</em> <strong>Project / Local / branch remain one rail, but the rail now attaches directly to the composer shell, shares its theme surface and sole elevation, and drops the visible 8px gap plus independent capsule shadow; supersedes the gap portion of D067</strong></td><td>The detached capsule and differently colored plate made context and prompt input read as unrelated controls instead of one Codex-style immersive composer</td></tr><tr><td>D089</td><td>Composer draft height</td><td><strong>The prompt textarea shows one visible line by default, auto-grows from wrapped content through seven visible lines, scrolls internally beyond line seven, and contracts as content is removed; the home shell is content-driven instead of keeping D061&#39;s fixed 140px minimum</strong></td><td>Preserve transcript space and Codex-like density while keeping multiline editing usable</td></tr><tr><td>D088</td><td>Scoped home sidebar sessions</td><td><em>(superseded by D093 only for the one-current-project and no-row-actions limitations)</em> <strong>Replace the Recents aggregate with one current-project session group plus persistent path-less Temporary sessions; keep other projects in the Projects index; remove Recents pin/panel row actions; scope empty-draft reuse and explicit <code>+</code> creation by project context</strong></td><td>D093 retains exact-path grouping, scoped draft reuse, and the Temporary boundary while allowing several retained project groups and scoped organization actions</td></tr></tbody></table><h2 id="f-baseline-0-4-2-product-decisions" tabindex="-1">F. Baseline 0.4.2 product decisions <a class="header-anchor" href="#f-baseline-0-4-2-product-decisions" aria-label="Permalink to &quot;F. Baseline 0.4.2 product decisions&quot;">​</a></h2><table tabindex="0"><thead><tr><th>ID</th><th>Topic</th><th>Decision</th><th>Rationale</th></tr></thead><tbody><tr><td>D090</td><td>Compact settings directory</td><td><em>(four-destination count and order superseded by D133)</em> <strong>Settings retains the D063 full-page shell and D070 visual metrics, but its rail contains exactly Basics, Agent, Import, and Info in that order. Appearance moves into Basics; Providers moves into Agent. Plugin management remains in the app shell&#39;s existing Plugins destination, with load/enable/disable/uninstall available there, and is not duplicated in Settings. This supersedes the broader grouped navigation and standalone Appearance/Providers/Plugins placements in D062–D065, plus D070&#39;s Account-specific rail metric.</strong></td><td>Remove empty, low-value, and duplicate destinations while keeping every shipped workflow reachable and making the local-first settings surface easier to scan</td></tr><tr><td>D091</td><td>Composer runtime configuration</td><td><strong>Mode and provider/model controls update the active session and are read from that session by the pi prompt path; controls without an end-to-end runtime implementation are not rendered.</strong></td><td>Prevent decorative effort/attachment controls and keep every visible composer action operational</td></tr><tr><td>D092</td><td>Responsive settings content</td><td><strong>The settings content fills the width available after the fixed 275px rail and pane gutters, resizing through CSS flex layout with the native window. This supersedes only D070&#39;s fixed 720px content band and the corresponding visual-metric retention in D090.</strong></td><td>Use wide desktop windows efficiently without adding renderer resize state or changing the compact settings directory</td></tr><tr><td>D104</td><td>Settings rail menu rename</td><td><em>(Agent label superseded by D110; directory order superseded by D133)</em> <strong>Settings rail destination labels are Basics / Agent / Import / Info (zh-CN: 基础 / 智能体 / 导入 / 信息). Destination IDs remain general / agent / import / about; only user-facing labels change. This renames the D090 compact directory labels without changing order, contents, or deep-link targets.</strong></td><td>Shorter, action-oriented labels scan faster while preserving the compact directory</td></tr></tbody></table><h2 id="g-baseline-0-4-3-sidebar-and-project-decisions" tabindex="-1">G. Baseline 0.4.3 sidebar and project decisions <a class="header-anchor" href="#g-baseline-0-4-3-sidebar-and-project-decisions" aria-label="Permalink to &quot;G. Baseline 0.4.3 sidebar and project decisions&quot;">​</a></h2><table tabindex="0"><thead><tr><th>ID</th><th>Topic</th><th>Decision</th><th>Rationale</th></tr></thead><tbody><tr><td>D093</td><td>Sidebar organization, retained project tabs, and session workspace isolation</td><td><strong>The renderer retains normalized open-project paths and local project/session presentation metadata for pin, archive, collapse, sort, and manual order. The sidebar renders one independently collapsible group per retained path plus Temporary sessions. User-facing sort modes are recent, created, oldest, and name; project groups can opt into <code>manual</code> by dragging the project title after a small pointer movement or with ArrowUp/ArrowDown on that title, while session <code>manual</code> remains a compatibility value. Activating a group reuses <code>project.set</code>, so the shell still has one selected host workspace. Tool execution resolves its root from the durable session project, and per-session turns/grants remain independent when another tab becomes active. Archive, close, and reorder are renderer-local and non-destructive.</strong></td><td>Preserve a multi-repository working set and make long conversation lists manageable without creating multiple host workspace singletons or allowing an active-tab switch to redirect a background session&#39;s tools</td></tr><tr><td>D094</td><td>Renderer product branding</td><td><em>(docked composer logo superseded by D160)</em> <strong>All user-visible shell identity uses <code>PI-Desktop</code>: the sidebar shell name, composer placeholder, and settings copy. <code>BrandLogo</code> imports the marks derived from <code>build/icon_1024.png</code> through Vite (see D221) for the home hero, expanded/collapsed sidebar, and docked composer; new-session controls use a dedicated message-plus icon. <code>Codex</code> remains only where it identifies an external import source or a design reference.</strong></td><td>Remove accidental third-party branding and vector approximations from the product surface while preserving import compatibility and the Codex-derived layout system</td></tr><tr><td>D135</td><td>Distinct sidebar task status indicators</td><td><strong>Conversation rows reserve one compact leading status slot with semantic, shape-distinct states: neutral-accent outlined ring for selected, warning-orange breathing dot for in progress, success-green check for completed, and error-red circled alert for failed. Precedence is in progress, selected, then latest terminal outcome. Starting a new turn clears the prior outcome; abort produces no failure. Every state has localized accessible text and reduced motion makes the in-progress dot static.</strong></td><td>The prior running dot reused the accent token and disappeared when idle, making selected, active work, completion, and failure difficult to scan or distinguish by more than row background</td></tr></tbody></table><h2 id="h-baseline-0-4-4-composer-decisions" tabindex="-1">H. Baseline 0.4.4 composer decisions <a class="header-anchor" href="#h-baseline-0-4-4-composer-decisions" aria-label="Permalink to &quot;H. Baseline 0.4.4 composer decisions&quot;">​</a></h2><table tabindex="0"><thead><tr><th>ID</th><th>Topic</th><th>Decision</th><th>Rationale</th></tr></thead><tbody><tr><td>D095</td><td>Remove composer workspace context rail</td><td><strong>The home and thread-docked composer variants never render the passive project / Local / branch rail or reserve layout/elevation for it. Project selection, session binding, branch metadata, and workspace-scoped tools remain available through non-composer surfaces. This supersedes the context-rail portions of D052, D055, D056, D066, D067, and D087.</strong></td><td>The rail duplicated navigation, showed two passive values, and could display misleading fallback branch metadata while consuming prompt space</td></tr></tbody></table><h2 id="i-baseline-0-4-5-thinking-mode-decisions" tabindex="-1">I. Baseline 0.4.5 thinking-mode decisions <a class="header-anchor" href="#i-baseline-0-4-5-thinking-mode-decisions" aria-label="Permalink to &quot;I. Baseline 0.4.5 thinking-mode decisions&quot;">​</a></h2><table tabindex="0"><thead><tr><th>ID</th><th>Topic</th><th>Decision</th><th>Rationale</th></tr></thead><tbody><tr><td>D096</td><td>End-to-end thinking mode</td><td>**Thinking level is session-scoped with canonical values \`off</td><td>minimal</td></tr><tr><td>D102</td><td>Custom provider thinking presets</td><td><strong>Settings expose Off / On-off only / Graded (plus advanced custom lists). On-off only persists <code>supportedThinkingLevels: [&quot;off&quot;,&quot;high&quot;]</code>; Graded clears the sparse override and uses the conservative default set; Composer renders only the resolved set and never invents graded options for boolean-like models such as mimo.</strong></td><td>Custom OpenAI-compatible endpoints often expose boolean thinking rather than a full effort ladder</td></tr></tbody></table><h2 id="k-work-panel-decisions" tabindex="-1">K. Work panel decisions <a class="header-anchor" href="#k-work-panel-decisions" aria-label="Permalink to &quot;K. Work panel decisions&quot;">​</a></h2><table tabindex="0"><thead><tr><th>ID</th><th>Topic</th><th>Decision</th><th>Rationale</th></tr></thead><tbody><tr><td>D097</td><td>Docked work panel replaces context panel</td><td><strong>The right side hosts a docked, drag-resizable (320–min(720px, 60vw)) work panel with Review / Terminal / Browser / Files tabs, toggled by the titlebar button or Cmd/Ctrl+J; <code>{open, tab, width}</code> persist in localStorage. The former ContextPanel overlay and its <code>context.*</code> copy are removed; workspace/model/status live in composer chips and Settings.</strong></td><td>Codex-parity working surface for inspecting agent output; the info-only overlay duplicated data available elsewhere</td></tr><tr><td>D098</td><td>Review tab reads the git working tree <em>(superseded by D180)</em></td><td><strong>Historical decision: Review rendered the workspace&#39;s uncommitted state through the git CLI.</strong></td><td>Superseded because commits erased message-level evidence and could not support safe per-message rollback</td></tr><tr><td>D099</td><td>Terminal tab is a real PTY <em>(superseded by D251)</em></td><td><strong>Historical decision: the former work panel exposed an interactive shell as a session-scoped terminal tab.</strong></td><td>The current product removes that separate shell surface; Agent Bash remains non-interactive and transcript-owned</td></tr><tr><td>D100</td><td>Browser tab embeds a WebContentsView</td><td><strong>The preview browser is a main-process WebContentsView with renderer-driven bounds sync, hardened (deny popups→external, deny permission requests, http(s)-only navigation, isolated persist partition); it hides while blocking overlays (palette, permission dialog, settings) are open. The user drives it; the agent does not.</strong></td><td>Recommended modern embedding without webview-tag caveats; hide rules resolve its compositor z-order over renderer overlays</td></tr><tr><td>D138</td><td>Session-scoped inline permission requests</td><td><strong>Tool approval is an inline PermissionCard owned by its originating <code>sessionId</code>, never a global dialog. Different sessions retain independent pending requests and absolute timeout deadlines; background message/tool/permission events update only scoped state and never activate, cover, or focus another conversation. Resolution and cleanup match both session and request identity. This supersedes only D100&#39;s permission-dialog overlay/hide clause; palette, search, settings, and other blocking surfaces retain their existing browser hide behavior.</strong></td><td>Concurrent agents must not steal the active workflow or overwrite each other&#39;s approval requests; the existing protocol already carries the required session/request identity.</td></tr><tr><td>D139</td><td>Navigation intent and shortcut event guards</td><td><strong>Every explicit session, project, page, fork, or history navigation begins or reuses one renderer navigation intent; asynchronous work commits visible state only while that intent remains current. Global shortcuts ignore modifier-only and IME composition events, while history navigation also ignores key-repeat events.</strong></td><td>Late session/project loads and incomplete keyboard events must not cause unexplained page or history jumps.</td></tr><tr><td>D128</td><td>Artifact-driven work panel tabs (shortcut clause superseded by D207)</td><td><strong>The work panel has no empty manual entry point, welcome chooser, titlebar/menu command, or Cmd/Ctrl+J shortcut. A file/URL/BrowserPreview/successful-command artifact creates and activates a closeable top tab; successful active-session workspace Write/Edit artifacts create and activate the singleton Review tab. File tabs are keyed by lexically normalized path, singleton tool tabs deduplicate, closing the active tab selects its right neighbor then left, closing the last tab hides the panel, and the sole panel-level control collapses it without deleting retained runtime tabs. Changing the visible session or workspace closes and clears tabs so relative resources never cross context boundaries. Startup is closed with no tabs; only panel width persists, while temporary OS-window expansion is excluded from launch bounds. Background-session, failed, and scratch writes do not steal focus. This supersedes D097&#39;s fixed tab entry points and <code>{open, tab}</code> persistence, refines D098&#39;s automatic refresh, and supersedes only D112&#39;s welcome-chooser clause.</strong></td><td>Match Codex&#39;s output-driven work surface, avoid an empty tool launcher, and make each visible tab correspond to work the session actually produced or explicitly previewed. D128 corrects the initially duplicated D119 identifier; D119 remains the transcript file-store decision.</td></tr><tr><td>D140</td><td>Session-owned dirty-workspace transcript review entry <em>(superseded by D179)</em></td><td><strong>After a session produces a successful workspace Write/Edit, its transcript ends with one explicit Review changes command outside collapsed activity groups while that Git working tree remains dirty; other sessions in the same project do not inherit the command. It reports the capped file count and addition/deletion totals and creates, reopens, or activates D128&#39;s singleton Review tab. The entry and Review share one workspace-keyed diff refreshed on workspace activation, successful Write/Edit/Bash completion (500ms debounce), explicit Review refresh, and window focus; sequenced requests discard prior-workspace responses. Clean and non-Git results clear review ownership for that workspace; clean, non-Git, missing-workspace, and failed-refresh states hide the entry. Review ownership is renderer-memory state discarded on relaunch with D142&#39;s work-panel contexts. This is a contextual artifact/status entry, not the empty manual launcher forbidden by D128.</strong></td><td>Automatic panel opening alone leaves no discoverable return path after collapse or tab close, while session ownership prevents unrelated conversations from claiming project-wide edits. Sharing the real diff makes the conversation entry accurate and keeps Review deduplicated.</td></tr><tr><td>D179</td><td>Message-scoped inline review cards <em>(superseded by D180)</em></td><td><strong>Historical decision: each successful workspace Write/Edit row rendered a card by matching the current workspace diff.</strong></td><td>Superseded because current Git state disappears after commit and cannot provide message-owned rollback</td></tr><tr><td>D180</td><td>Message-owned review snapshots and guarded rollback</td><td><strong>Successful workspace Write/Edit results carry bounded <code>details.review</code> evidence: snapshot id, message/tool id, path, operation, added/modified/deleted status, +/− counts, hunks, and rollback state. The host stores pre-tool bytes and before/after hashes outside the workspace under the session, and the renderer derives the adjacent card and chronological Review history from transcript messages only. <code>review.rollback</code> verifies the current post-tool hash before restoring prior bytes or removing a newly-created file; conflicts never overwrite later edits. Session deletion and startup cleanup remove snapshots; forked evidence is visible but non-reversible.</strong></td><td>A message-owned snapshot survives Git commits, separates same-path edits, and makes rollback explicit without trusting mutable repository state</td></tr><tr><td>D142</td><td>Session-scoped work-panel runtime contexts (no-launcher clause amended by D207)</td><td><strong>Each conversation owns an in-memory work-panel context containing open state, ordered tabs, active tab, and Browser resource. Session selection atomically projects that context, switching away never deletes it, and switching back restores it. File/URL/BrowserPreview/command/Review artifacts are recorded against their originating <code>sessionId</code>; BrowserPreview renderer events therefore carry <code>sessionId</code>. Background artifacts may update their retained context but never open, activate, resize, navigate, or focus the visible panel. A workspace selection without an active conversation hides the panel, and relative resources remain bound to their session/workspace. Relaunch discards every context and Browser resource; only panel width persists. This supersedes only D128&#39;s requirement to close and clear tabs when the visible session or workspace changes; D128&#39;s artifact triggers, deduplication, and close/collapse behavior remain unchanged, while D207 adds an active-session shortcut that only reveals the retained context. No process ownership boundary changes.</strong></td><td>Permission-gated tools can finish while another conversation is loading or visible. A global destructive tab set either flashes open before being cleared or loses the originating conversation&#39;s tools; session-keyed renderer state preserves continuity without allowing background work to steal focus or making transient resources durable.</td></tr><tr><td>D154</td><td>Work-panel activity rail and resource switcher</td><td><strong>Once an artifact has opened the work panel, a 44px activity rail exposes Review, Terminal, and Browser as one-click 32px tool buttons; opening a missing tool still uses D128&#39;s <code>openWorkPanelTab</code> path. The 46px content header shows and closes the active resource and exposes a bounded keyboard-operable switcher for every open tool/file resource. The panel minimum becomes 364px so the rail preserves the previous 320px content floor <em>(width clamp superseded by D167)</em>. This replaces the horizontally scrolling top tabs and hidden empty-header context menu while preserving D128&#39;s artifact-driven panel entry, resource deduplication/order, close-neighbor behavior, and session-scoped runtime ownership.</strong></td><td>High-frequency tools should be visible and spatially stable, while long or numerous file names need a labeled overflow surface rather than compressing every action into one titlebar row.</td></tr><tr><td>D157</td><td>One visual assistant turn per user turn</td><td><strong>Provider-level assistant messages separated by thinking/tool activity remain distinct canonical transcript records, but ChatTranscript composes all records after one user message and before the next into one <code>role=article</code> assistant turn. Ordered markdown fragments and activity disclosures remain visible; only the composed turn owns the trailing aggregate model/usage row and Copy/Fork/Retry toolbar. Copy joins all contentful fragments with paragraph breaks; Fork and Retry use the last contentful assistant record as their durable boundary.</strong></td><td>Tool-capable providers close and reopen assistant messages around every tool call. Rendering those transport boundaries as separate responses duplicated action toolbars and made one agent run look like many AI replies.</td></tr><tr><td>D162</td><td>Latest-wins cached session switching</td><td><strong>The renderer marks the newest selected row immediately, coalesces session-detail prefetch/load work, retains an LRU-style five-transcript memory cache, and starts the newest transcript read without waiting for superseded reads. Workspace alignment may overlap transcript IO; navigation generations gate the atomic visible commit. ChatSurface keeps the previous complete view non-interactive while React defers a changed session tree, then paints the destination at its final record. Cached snapshots are always revalidated.</strong></td><td>The former global selection promise queue made every click wait behind obsolete full-transcript JSONL reads, while one synchronous long Markdown commit delayed visible feedback. Latest-wins IO plus a stable deferred frame matches Codex-style navigation without weakening session/workspace isolation.</td></tr><tr><td>D156</td><td>Independent native-window and work-panel resize ownership</td><td><em>(responsive-clamp and no-native-reservation clauses superseded by D163)</em> <strong>Electron Main exclusively owns BrowserWindow bounds at a 1040x700 minimum, while the renderer owns a persisted preferred work-panel width. The divider uses anchored, frame-coalesced preview and commit-on-release; Escape, pointer cancellation, and lost capture roll back. Native window-edge resize never rewrites the panel preference. This supersedes D083&#39;s restore minimum and removes the <code>window/resizeBy</code> delta channel and resize-attribution heuristic.</strong></td><td>Circular ownership made divider release resize the OS window a second time, introduced async races and platform differences, moved windows near display edges, and rewrote a panel preference during unrelated native resize.</td></tr><tr><td>D163</td><td>Native width reservation for the fixed work panel</td><td><em>(width range superseded by D167)</em> **An open docked panel keeps one committed fixed width in <code>364..720</code>; native window/sidebar changes never clamp it. Renderer sets the visible target through idempotent \`window/setWorkPanelReservation({width: 0</td><td>364..720}) -&gt; {requested, reserved}<code>: open requests the committed width, collapse/final close requests zero, and divider commit updates it. In normal state Main adds available native work-area width and reverses it symmetrically, so chat stays stable when the full target fits; otherwise the panel remains fixed and chat absorbs </code>requested - reserved\`. Native edges resize chat only. Maximized/fullscreen requests defer until normal; display/work-area changes reconcile the target against current available width without reapplying it during ordinary same-display movement. Persisted base bounds exclude reservation width and its x shift, and background artifacts cannot alter the visible reservation. This supersedes ADR 0029/D156 clauses that clamp the panel inside current client width or prohibit panel-driven target geometry, while preserving Main bounds ownership and divider gesture rules (ADR 0032).**</td></tr><tr><td>D167</td><td>Slimmer default work-panel width</td><td><strong>The docked work panel opens at a 280px committed default (one third narrower than D163&#39;s 420px) and clamps to <code>244..720px</code>; the renderer constant, the Electron reservation validator, and the <code>.work-panel</code> CSS floor share those bounds. Double-click on the divider restores 280px. The 44px-rail-plus-320px-content rationale behind D154&#39;s 364px floor no longer applies now that tools live in the header switcher, so the floor scales with the default. Persisted wider widths stay valid and are unchanged on upgrade; the 720px maximum, divider gesture rules, and native reservation lifecycle are unchanged (D255, ADR 0122).</strong></td><td>The panel opened wider than most review/terminal/file content needs, taking readable width from chat inside the fixed client area, and the old 364px floor made a narrower default unreachable.</td></tr><tr><td>D255</td><td>Restore native reservation while the work panel is visible</td><td><strong>Keep the work panel as an in-flow fixed-width renderer column, but request its committed width through <code>window/setWorkPanelReservation</code> before presenting it. Keep the panel mounted during exit, then request zero and unmount only after the reservation succeeds. The native window therefore grows to preserve MainChat width while the panel is visible when the display work area allows it, and collapses symmetrically back to the base bounds when the panel is folded or closed. Constrained work areas retain the existing capped reservation and chat shortfall; persisted bounds exclude temporary reservation geometry. This supersedes ADR 0033&#39;s no-native-expansion decision without changing the IPC shape, Browser measured-bounds path, Main-owned edge resize, or session-scoped panel state.</strong></td><td>The fixed-window dock left the application window wider after the user collapsed the panel, causing MainChat to occupy the released right-column width instead of returning to the chat-only window bounds.</td></tr><tr><td>D256</td><td>Native taskbar minimize for Windows/Linux window controls</td><td><strong>Amend D216 / D252 and ADR 0078 / ADR 0117: Windows/Linux renderer and native-menu <code>minimize</code> actions call Electron&#39;s native minimize transition, and their <code>minimize</code> event is not converted to <code>hide()</code>, so the taskbar entry remains available. macOS native minimize remains tray-resident. Windows/Linux close behavior remains D230 / ADR 0090: the close button hides to the tray only for <code>tray</code> and exits for <code>quit</code>. No IPC action, storage, host protocol, or background-process contract changes.</strong></td><td>The custom top-right minimize button was using the same tray-hide effect as close-to-tray, so users lost the normal taskbar restore path even though close behavior was already configurable.</td></tr><tr><td>D258</td><td>Transcript layout index, one window coordinate space, and identity truncation</td><td><strong>Refine ADR 0120: host-core keeps a per-session transcript layout (byte offset of every message and compaction line plus the <code>file_len</code> those offsets were recorded against) and serves a bounded window by seeking to its first selected line, so opening a session costs the window instead of the whole history. The layout is an in-memory cache of derived data: growth scans only the tail, shrink or replacement rescans, and every rewrite/delete path drops the entry; a torn trailing line stays outside both the offsets and <code>file_len</code>. Lines are classified by one depth-aware scan for the top-level <code>type</code> key rather than parsed, so a nested <code>type</code> inside an open-ended tool result or checkpoint detail cannot decide a line kind; <code>type</code> is now written first so the scan usually stops at the first key, and legacy lines that carry it after their payload are read by the same scan. Read windows are physical message-line positions clamped against the layout and never against <code>last_seq</code>, whose deduplicated count sits permanently below the file whenever an append&#39;s index commit was lost - which had been cutting the newest messages out of the tail. The compaction chain is still returned whole with any window. <code>agent/prompt</code> gains <code>truncateFromMessageId</code>, resolved by the host against its own transcript and rejected with <code>NOT_FOUND</code> when unknown, replacing a <code>messageStart + userIndex</code> sum that mixed file offsets with renderer array indices. Recording a forked child (list row, cached transcript, checkpoint marks) becomes unconditional while only the visible switch stays gated on the D139 navigation intent.</strong></td><td>ADR 0120 bounded the payload but left a full sequential scan to locate each page, so long sessions still opened slowly and degraded as they grew; and two coordinate-space confusions were silently losing the newest messages and truncating the wrong turn on a paged transcript (ADR 0127)</td></tr><tr><td>D259</td><td>Bounded shared budget for transient provider failures</td><td><strong>Amend D186 / D245 and ADR 0050 / ADR 0091: non-429 transient provider failures share one bounded logical-turn budget of four retries after the initial attempt, for five provider attempts total, shared across request setup and stream delivery. The budget admits exactly <code>NETWORK_ERROR</code>, <code>TIMEOUT</code>, <code>STREAM_FAILED</code>, and retryable <code>PROVIDER_ERROR</code>; <code>PROVIDER_ERROR</code> is now retried in the stream phase as well as during setup, so an upstream gateway 502/503/504 recovers wherever it lands. Non-429 delay honors <code>retry-after-ms</code>, <code>retry-after</code> seconds, then HTTP-date before a deterministic 1s/2s/4s/8s schedule, capped at 8 seconds, and captured headers are retained for every status that can state a delay (429, 408, 409, 5xx). Only the failed request is replayed; the session, transcript, and completed tool calls are untouched. The main session, builtin subagents, and one-shot composer enhancement share the same codes, budget size, and precedence. The 429 five-retry budget stays separate and the two do not draw from each other. Exhaustion records <code>retryAttempt: 4</code>. No IPC, storage, host protocol, or provider-config change.</strong></td><td>A relay answering <code>OpenAI API error (502): {&quot;type&quot;:&quot;api_error&quot;,&quot;message&quot;:&quot;Upstream API request failed.&quot;}</code> reports a momentary upstream outage, but the split one-retry-per-phase policy turned a second 502 into a terminal error card and never replayed a mid-stream 502 at all, while subagents refused any non-request-phase transient retry. One shared bounded budget recovers ordinary gateway flapping without hiding a persistent outage.</td></tr><tr><td>D164</td><td>Dual-locale in-app product changelog</td><td><strong>Product &quot;what&#39;s new&quot; text for app updates is maintained as a dual EN/zh-CN catalog in <code>packages/shared</code> (<code>CHANGELOG</code>). Electron Main formats notes for the discovered <code>availableVersion</code> using the product UI locale and attaches them as optional <code>UpdateState.releaseNotes</code> plain text on the existing updates IPC/event path. The ambient banner and Settings → Info Updates row show a compact What&#39;s new section when notes exist. English is the source of truth; zh-CN mirrors versions and highlight counts. GitHub auto-generated release bodies remain web-only and are not the in-app source. No new feed URL, notes channel, or renderer-owned remote fetch is introduced (extends D120 / ADR 0022).</strong></td><td>Users need bilingual release highlights at update time without a second network surface or weakening Main&#39;s sole ownership of update delivery.</td></tr><tr><td>D357</td><td>Viewport-fixed work panel toggle</td><td><strong>Amend D128 / D207 / D221 and ADR 0068 / ADR 0085: AppShell owns one viewport-fixed toggle at the top-right of every non-Settings route. It is the pointer equivalent of <code>Cmd/Ctrl + J</code> — reveal the active session&#39;s retained context without creating a tab, collapse a visible panel without deleting tabs, disabled with no session. It is the sole panel-level collapse control. Windows/Linux window controls stay viewport-fixed; while the panel is open the header reserves that band plus the toggle. No host protocol, IPC channel, or native application-menu command. Artifact-driven tabs and session-scoped contexts are unchanged.</strong></td><td>The shortcut-only entry left the panel undiscoverable for pointer users. A Cmd/Ctrl+J equivalent is not D097&#39;s empty fixed-tab launcher. See ADR 0195 and E2E-056.</td></tr></tbody></table><h2 id="l-transcript-presentation-decisions" tabindex="-1">L. Transcript presentation decisions <a class="header-anchor" href="#l-transcript-presentation-decisions" aria-label="Permalink to &quot;L. Transcript presentation decisions&quot;">​</a></h2><table tabindex="0"><thead><tr><th>ID</th><th>Topic</th><th>Decision</th><th>Rationale</th></tr></thead><tbody><tr><td>D101</td><td>WorkBuddy-inspired transcript density</td><td><strong>User turns render as compact right-aligned soft plates (<code>min(78%, 560px)</code>, subtle border + hairline shadow). Assistant turns stay transparent full-width prose (max 720px). Message row vertical padding tightens to 10px. Hover/focus-within reveals quiet copy chips under each turn (right-aligned for user, left-aligned for assistant). Streaming assistant answers use a thin accent left rule. No mascot, reactions, or cost-chip UI yet.</strong> <em>(amended by D323)</em></td><td>Current right-aligned user bubbles were underspec&#39;d and visually sparse versus WorkBuddy&#39;s task chat; denser plates improve scanability without abandoning the Codex/developer restraint</td></tr><tr><td>D103</td><td>Per-message model + token meta and retry</td><td><strong>Completed assistant turns surface modelId + token usage chips under the answer (tokens-only; hover breakdown for input/output/cache/reasoning). Usage is attached on runtime message_end from pi-ai Usage, persisted in message meta_json, and reloaded with the transcript. Action row adds Retry, which re-sends the nearest preceding user prompt. No currency pricing and no like/dislike.</strong></td><td>WorkBuddy per-message meta improves trust/scanability; token totals already flow from the provider while priced cost still needs a catalog</td></tr><tr><td>D105</td><td>In-place regenerate for assistant turns</td><td><strong>Regenerate truncates the session transcript to the nearest preceding user prompt (exclusive of that prompt and everything after), disposes the live pi-agent for the session, and re-sends the prompt so the new assistant/tool tail replaces the discarded branch instead of stacking a duplicate turn.</strong></td><td>Users expect regenerate to rewrite the current turn; append-only retry polluted long sessions and left stale answers above the redo</td></tr><tr><td>D106</td><td>Preserve user hard newlines in transcript</td><td><strong>User bubbles render plaintext with hard newlines intact. Composer only trims leading/trailing whitespace; transcript uses <code>message-user-text</code> with <code>white-space: pre-wrap</code> (no forced mid-glyph word-break) so multi-line prompts never collapse into one paragraph. Copy and session reload keep the original line breaks.</strong></td><td>Multi-line prompts (code snippets, lists, pasted blocks) are common in coding agents; collapsing newlines makes the transcript hard to re-read and re-edit</td></tr><tr><td>D107</td><td>Configuration provider studio</td><td><strong>Settings → Agent uses a compact provider studio: a Defaults card, separate vendor-account rows with edit/test/remove actions, modal add/edit dialogs, and card-based AI-service management with secret badges, test connection, and make-default/delete actions. This refines the Providers presentation inside the compact settings directory without adding rail destinations.</strong></td><td>Dense stacked forms, a redundant summary hero, and cramped list rows made multi-provider setup hard to scan and over-emphasized secondary fields</td></tr><tr><td>D110</td><td>Model configuration label + add-provider dialog</td><td><strong>Settings rail label for the agent destination is Model configuration (zh-CN: 模型配置). Adding a provider opens a modal dialog instead of an inline collapsible composer; the destination id remains <code>agent</code>.</strong></td><td>Clarify the model-setup purpose of the tab and reduce page churn while editing provider credentials</td></tr><tr><td>D108</td><td>Conversation minimap only when overflowing</td><td><strong>The left-edge conversation minimap rail renders only when at least two visible user/assistant messages exist and the transcript overflows one viewport (<code>scrollHeight &gt; clientHeight</code>). Short one-page threads hide the rail; streaming growth, content resize, and window resize re-evaluate visibility.</strong></td><td>A navigation rail is noise when every message already fits on screen; overflow is the signal that jump/preview navigation is useful</td></tr><tr><td>D109</td><td>ChatGPT-style regenerate revision history</td><td><strong>Regenerate archives the discarded assistant/tool tail under a stable <code>revisionRootId</code> family in <code>message_revisions</code> (schema v4). The live root user turn carries <code>revisionCount</code> / <code>activeRevision</code> and a quiet <code>current / total</code> pager switches linear variants in place. First regenerate stores the original branch as revision 1; later regenerates append new branches and mark the newest active. No free-form branch tree.</strong> <em>(amended by D307)</em></td><td>Users expect regenerate to keep prior answers reachable like ChatGPT; D105 in-place rewrite alone deleted history that was still useful for comparison</td></tr><tr><td>D111</td><td>Empty home scroll stack</td><td><em>(card-specific clauses superseded by D131)</em> <strong>Empty chat home is a single scrollable vertical stack inside <code>home-main-content</code>. Short windows top-align and scroll; content stays in document flow and the home composer remains non-docked.</strong></td><td>Dual-grow + absolute portal let the home composer overlap guidance on shorter windows; flow layout preserves every remaining block without collision</td></tr><tr><td>D112</td><td>Readable chat beside the work panel</td><td><em>(dynamic width clamp superseded by D163; welcome chooser superseded by D128)</em> <strong>MainChat has a 360px readability target beside the panel. D163 preserves it through native width reservation whenever the display work area can supply the complete committed panel width; otherwise MainChat absorbs the unavoidable shortfall while the panel remains fixed.</strong></td><td>A panel-only width cap could leave roughly 109–205px for chat at supported window sizes; native reservation now preserves chat without compressing the tool surface.</td></tr><tr><td>D113</td><td>WorkBuddy-inspired local profile footer</td><td><strong>The expanded sidebar ends in a transparent 58px footer. Its 44px profile trigger contains a 30px circular local-user glyph, two-line <code>Custom</code> + <code>Local profile</code> / <code>本地配置</code> identity, and a chevron; a separate 32px Help shortcut opens Settings → Info. The 280px profile menu opens 8px above the footer with a repeated identity header, divider, and Settings / Logs / Theme actions, preserving Escape, outside-click, arrow-key, and focus-restore behavior. This supersedes D041; no cloud account, notification, share, or update capability is implied.</strong></td><td>Adapt WorkBuddy&#39;s avatar-and-actions footer grammar to PI-Desktop&#39;s truthful local-only capabilities while improving identity hierarchy and eliminating the stale cloud stand-in</td></tr><tr><td>D137</td><td>Glyph-only message toolbars; edit means edit-the-prompt</td><td><strong>Message toolbars carry icons only: the label lives in a CSS hover/focus tooltip (<code>data-tip</code>) plus <code>aria-label</code>, never as a visible chip caption (worded buttons stay only on error surfaces). Edit moves off the assistant answer onto the user turn: it opens the prompt in an inline textarea (slash turns seed the typed <code>command</code> form so the resend re-expands the template) and saving replays the D105/D109 regenerate path with the new text in the same session — the replaced prompt and its whole answer tail are archived as a revision, so the existing <code>current / total</code> pager walks back to the original. Editing the assistant&#39;s own text and its fork-into-a-child-session variant (D134) are dropped; Fork stays as the explicit divergence action.</strong></td><td>Four worded chips under every answer read as a sentence and crowded the transcript; and the useful correction is almost always &quot;I asked it wrong&quot;, which users expect to re-run in place with history intact (ChatGPT semantics) rather than to hand-edit the model&#39;s words in a new session</td></tr><tr><td>D165</td><td>Safe lazy Mermaid diagrams in assistant answers</td><td><strong>A completed <code>mermaid</code> fence in assistant answer prose renders as a theme-aware SVG after entering the near-viewport band. Partial stream fences and all thinking prose stay source code. The renderer dynamically loads official Mermaid, serializes its global theme renders, caps source at 20,000 characters and edges at 500, locks strict/no-HTML/no-link configuration, and applies a second SVG-profile sanitizer. Invalid or oversized diagrams fall back to visible copyable source; the diagram toolbar toggles source and copies it.</strong></td><td>Diagrams improve architecture and flow explanations, but parsing partial streams or every offscreen historical fence would undermine direct-stream and fast-session-switch behavior. Strict bounded local rendering adds the capability without a new protocol, network, or Electron privilege boundary.</td></tr><tr><td>D261</td><td>The mounted transcript is a window, not the loaded history</td><td><strong>Refine ADR 0120 / D258 in the renderer: the mounted history is a trailing window over the loaded history — 15 rows in the first commit after a session switch, a 60-row steady state, grown 40 rows at a time. Reaching the top escalates in two stages: grow the window while it is partial, and call <code>loadOlder</code> only once the window covers all loaded history. Window growth and a fetched page take the same pre-paint scroll anchor, because both add height above the reading position. The window resets per session and is clamped to the loaded history, so a stale budget from the previous session cannot over-mount. The hydration spacer stays scoped to the first commit. The conversation minimap is built from the mounted entries rather than from every loaded message. No IPC, storage, host protocol, or pagination change.</strong></td><td>ADR 0120 bounded what crossed the IPC boundary and D258 bounded the cost of locating a page, but the renderer still mounted every row it had ever paged in and kept it mounted for the life of the session. <code>content-visibility: auto</code> skips layout and paint for those rows while retaining their React trees, Markdown ASTs, and Shiki token arrays — the wrong resource on a low-memory Windows machine, where the reported symptom was the chat area getting progressively less responsive in a long session. Windowing bounds retained memory and per-frame reconciliation together; feeding the minimap the full set would have drawn dashes whose click target no longer exists.</td></tr><tr><td>D317</td><td>Live/durable transcript merge keeps chronological order</td><td><strong>Amend ADR 0120 / D261 / ADR 0137 in the renderer: <code>mergeLiveSessionMessages</code> stitches a bounded durable <code>session.get</code> page onto the live snapshot in chronological order. Live rows older than the page stay before it; an optimistic user row or in-flight assistant/tool tail stays after it. Completed overlapping ids still prefer the durable row. Live-only rows are never appended after the durable page. A previously appended older prefix is healed back in front when its <code>createdAt</code> precedes the page. Renderer only: no IPC, storage, host-protocol, or pagination change.</strong> <em>(amended by D324)</em></td><td>A session that stayed open past the newest-100 page accumulated hundreds of live rows. Revalidation used the durable page as the array prefix and appended the rest, so D261&#39;s trailing mounted window painted old history and hid the just-sent prompt while the turn kept running. Abort then idle reload showed only the durable page, which is why the messages appeared to come back after Stop plus another switch.</td></tr><tr><td>D324</td><td>Idle session switch keeps a not-yet-flushed completed tail</td><td><strong>Amend D317 / ADR 0137 in the renderer: <code>selectSession</code> stitches the bounded durable page onto the live snapshot whenever the session is running or still has live provenance (<code>liveSessionTranscripts</code>). A completed assistant/tool row that the durable page does not yet contain stays. Live provenance is cleared only once that page already contains every live id, not merely because the turn ended. Renderer only: no IPC, storage, host-protocol, or pagination change.</strong></td><td>User prompts persist before the model runs; assistant/tool rows go through the async outbox and land after <code>message_end</code>. After <code>agent_end</code>, idle revalidation used the durable page only and dropped replies that were already on screen (issue #41).</td></tr><tr><td>D327</td><td>Completed replies survive process restart</td><td><strong>Amend D299 / ADR 0153 / ADR 0041: the finished <code>message_end</code> snapshot is checkpointed before the outbox append; <code>completed</code>/<code>error</code> <code>session.endTurn</code> deletes <code>.inflight.json</code> only when that id is already indexed; boot recovery skips completed leftovers so the outbox can append first; handshake awaits that drain then <code>session.recoverInflightMessages</code> promotes any leftover as <code>complete</code>. Additive RPC, no protocol or schema version change.</strong></td><td>The same user-only transcript as issue #41 after a full quit (issue #42): user rows are durable before the model runs, assistant/tool rows sit in the outbox, and D324&#39;s live merge does not survive process teardown.</td></tr><tr><td>D444</td><td>Cross-session message id collision remaps; outbox UNIQUE is not a pause</td><td><strong>Amend ADR 0041 / D327: <code>session.appendMessage</code> still treats an id already indexed in the same session as a no-op (or a terminal-assistant replacement of a streaming row). If that globally unique <code>messages.id</code> already belongs to another session, the host remaps to <code>{sessionId}:{id}</code> before writing JSONL or the index, and a later replay of the original id is also a no-op against the remapped row. Electron&#39;s persistence outbox treats <code>UNIQUE constraint failed: messages.id</code> as an idempotent ack and continues draining, so one colliding toolCallId cannot stall every later assistant/tool row or fill the 1024-entry cap. No protocol or schema version change.</strong></td><td>Provider tool rows used <code>toolCallId</code> (e.g. <code>call_421522</code>) as <code>messages.id</code>. Those ids are not globally unique. A collision failed SQLite after the JSONL write, the FIFO outbox paused on that head, later rows from every session never persisted, and a quit showed earlier history as gone (issue #523).</td></tr><tr><td>D597</td><td>Drain poison outbox entries; accept steering into a delivery turn</td><td><strong>Amend ADR 0041 / ADR 0239 / ADR active-turn-steering / D444: Electron&#39;s persistence outbox treats a host append whose message carries a <code>PERMISSION_DENIED:</code> prefix as a permanent rejection, drops that row, and keeps draining. <code>PLUGIN_PERMISSION_DENIED</code> and other failures still pause. Host provenance accepts <code>UiMessage.steering</code> into a claimed collaboration delivery turn when the target session matches, without stamping the delivery origin and after stripping any client-supplied <code>session_message</code>. Cross-session steering is still <code>PERMISSION_DENIED</code>. No protocol or schema version change.</strong></td><td>Alt+Enter into a session-collaboration delivery turn was rejected as a delivery mismatch, the outbox retried that head forever, filled to 1024, and later assistant/tool rows were discarded so the transcript kept only user messages.</td></tr><tr><td>D328</td><td>Parent-judged subagent lifetime</td><td><strong>Amend ADR 0089 / ADR 0119 / ADR 0129: idle and duration watchdogs are not armed; parent <code>agent_end</code> does not abort running delegates; the runtime keeps the durable turn open and prompts the parent with reports when they finish. <code>TaskWait</code> timeout and <code>TaskList</code> return a heartbeat (agent, status, elapsed, turns, last tool). Only <code>TaskStop</code> and user Stop abort a delegate. Explicit <code>maxTurns</code> and the concurrency cap of 10 stay. Runtime only: no IPC, storage, or host-protocol change.</strong> <em>(amended by D352: a terminal parent error does abort leftover delegates and returns the session to idle)</em></td><td>The parent cannot see live delegate work and treats <code>TaskWait</code> expiry as permission to finish, after which abort-on-end killed the unfinished job. Waiting is an event loop; models are not.</td></tr><tr><td>D352</td><td>Parent fatal error aborts leftover delegates</td><td><strong>Amend D328 / ADR 0166: parent idle still keeps the durable turn open and does not abort running delegates. A terminal parent provider/stream error (including exhausted HTTP 429), overflow-recovery failure, mutation-budget termination, or rejected prompt aborts leftover delegates, skips the D328 resume prompt, and emits <code>agent_end</code>. <code>isRunning</code> does not count leftover delegates after that error, so Continue is not <code>AGENT_BUSY</code>. A later <code>agent_end</code> must not overwrite the failed TurnOutcomeCard. Each new parent prompt bumps a turn epoch and only auto-resumes delegates started in that turn. Runtime and renderer only: no IPC, storage, or host-protocol change.</strong></td><td>After a parent 429 the UI showed Continue while a Gemini (or other) subagent kept the sidecar busy, so the next prompt was <code>session already has an active turn</code>. See ADR 0189 and E2E-155.</td></tr><tr><td>D354</td><td>Label both macOS release architectures</td><td><strong>Amend D353 / ADR 0145: both native macOS release lanes pass target-specific electron-builder patterns. Public assets are <code>PI-Desktop-&lt;version&gt;-arm64.dmg</code> / <code>PI-Desktop-&lt;version&gt;-arm64-mac.zip</code> and <code>PI-Desktop-&lt;version&gt;-x64.dmg</code> / <code>PI-Desktop-&lt;version&gt;-x64-mac.zip</code>. Generated per-architecture updater feeds retain these URLs and checksums. This changes release asset naming only; updater ownership, signing, and delivery mode remain unchanged.</strong></td><td>The arm64 asset <code>PI-Desktop-0.14.4.dmg</code> did not identify its architecture, while the x64 lane used a different <code>Intel</code> convention. Both public architectures need self-describing, standard labels.</td></tr><tr><td>D353</td><td>Disambiguate Intel macOS release artifacts</td><td><strong>Amend D285 / ADR 0145: the native macOS x64 release lane passes target-specific electron-builder patterns so its public assets are <code>PI-Desktop-&lt;version&gt;-Intel.dmg</code> and <code>PI-Desktop-&lt;version&gt;-Intel-mac.zip</code>. The generated <code>latest-mac-x64.yml</code> retains those URLs and checksums. The arm64 lane keeps the generic <code>PI-Desktop-&lt;version&gt;.dmg</code> and <code>PI-Desktop-&lt;version&gt;-mac.zip</code> names. This changes release asset naming only; updater ownership, signing, and delivery mode remain unchanged.</strong></td><td>Users could not reliably distinguish the Intel download from the arm64 download when both used generic version names. Naming the x64 assets at build time also prevents the updater feed from retaining stale pre-rename URLs.</td></tr><tr><td>D329</td><td>Agent-chosen Bash timeout</td><td><strong>Amend D190 / D273 / ADR 0054: Bash still defaults to 60 seconds. An explicit <code>timeout</code> is 1–21,600 seconds. A value above 21,600 is read as milliseconds, converted, and clamped to 21,600 seconds. In-range values including 600 and 1800 are seconds. Out-of-range values still fail before spawn. Timeout/abort still kills the process tree. Host-core <code>MAX_BASH_TIMEOUT_MS</code> matches. No protocol-version bump.</strong></td><td>The 300-second ceiling killed legitimate builds and rejected <code>timeout: 600</code> / <code>1800</code> (issue #44). A finite host bound remains so RPC cannot hang forever.</td></tr><tr><td>D331</td><td>Host-owned completed-turn token history</td><td><strong>Amend D103: parent <code>message.usage</code> stays pi-ai Usage for that assistant message. Subagent spend accumulates on the durable turn only. Electron sums parent <code>message_end</code> usages plus <code>turn_end.subagentUsage</code> into <code>session.endTurn.usage</code>. <code>stats.getTokenUsageHistory</code> rolls up completed turns over a bounded local-calendar window with filled <code>day</code> / ISO <code>week</code> / <code>month</code> buckets. Additive IPC/RPC and <code>idx_turns_ended_at</code>; no protocol or schema version bump. No currency pricing. The user-facing dashboard is plugin <code>pi.token-insights</code> (D335).</strong></td><td>The turns table was already the usage rollup, but Electron never sent <code>usage</code>, and merging subagent tokens into message chips would lie to the context inspector.</td></tr><tr><td>D335</td><td>Plugin-owned token usage dashboard</td><td><strong>Amend D331 / ADR 0171: Settings has no Usage destination. Global token history is marketplace plugin <code>pi.token-insights</code>. Host still persists <code>session.endTurn.usage</code> and exposes <code>stats.getTokenUsageHistory</code>. The plugin may fold host completed-turn remainders (including subagents) into its PI-Desktop fact cube without rewriting <code>message.usage</code>. No protocol or schema version bump.</strong></td><td>Settings duplicated a weaker heatmap the plugin already ships. See ADR 0173 and E2E-186.</td></tr><tr><td>D326</td><td>Withdraw A2A and Peer coordination</td><td><strong>Remove the Agent2Agent broker, <code>a2a.*</code> RPC domain, leftover <code>SubagentMailbox</code> / <code>Peer</code> tool, and parent/delegate <code>A2A</code> tools. Concurrent delegates coordinate only through parent <code>Task*</code> reports. Handshake <code>PROTOCOL_VERSION</code> 10→11 (no <code>&quot;a2a&quot;</code> capability). Storage <code>SCHEMA_VERSION</code> 12→13 drops the <code>a2a_*</code> tables. ADR 0165 supersedes 0147 / 0162 / 0164.</strong></td><td>The sibling and parent-to-parent channels sat beside <code>Task*</code> without a product need, and the model could miss or misuse them.</td></tr><tr><td>D318</td><td>Restore a missing sessions row from its transcript / outbox</td><td><strong>Amend ADR 0041 / D119 in host-core and Electron main: a live <code>sessions/&lt;id&gt;.jsonl</code> whose SQLite sessions row is gone is restored at host boot (<code>recover_orphaned_sessions</code>) and on <code>session.appendMessage</code>. Restore reinserts the row and rebuilds the search index from the file so append stays idempotent. If both the row and the file are gone, append inserts a stub row under the existing id so a queued outbox can drain. <code>session.delete</code> drops that session&#39;s outbox entries so a stub recreate cannot resurrect a user-deleted conversation. No protocol version, schema, or renderer change.</strong></td><td>A long running session could lose its <code>sessions</code> row (WAL/index loss) while turns stayed in <code>session-message-outbox.json</code>. <code>appendMessage</code> then failed <code>session not found</code>, the outbox paused at the head, the sidebar listed nothing, and the chat showed only the first flushed user row. Re-inserting the row let the outbox drain (issue #39).</td></tr><tr><td>D262</td><td>Spill large composer text pastes into session scratch</td><td><strong>Text-only composer pastes at or below the persisted <code>largePasteThreshold</code> remain native textarea input; the default is 600 characters and valid values are integers from 1 through 1,000,000. Above the threshold, the renderer sends exact UTF-8 <code>text/plain</code> bytes through the existing session paste bridge, Electron stores them under <code>&lt;data_dir&gt;/scratch/&lt;sessionId&gt;/pasted/</code>, and the Composer inserts a generated <code>@&lt;temporary-name&gt; </code> token at the original selection. The renderer retains a session-scoped token-to-canonical-path mapping, resolves it in place exactly once before dispatch, and excludes it from duplicate attachment/fallback serialization. Existing clipboard file/image chips and bridge bounds remain unchanged; no workspace, artifact-store, host-protocol, or schema change.</strong></td><td>Very large native pastes are hard to edit and visually overwhelm the composer, while the existing session scratch flow already provides bounded, isolated storage and canonical path semantics without dirtying a project (ADR 0131).**</td></tr><tr><td>D265</td><td>One delegation reads as a card, like a fan-out</td><td><strong>Amends D201 / ADR 0062 in the renderer: every <code>Task</code> call in an activity group renders as a full-width delegation card — main-agent root, connector, one node per delegate — a lone delegation included, instead of falling back to a compact tool row. The aggregate header takes a count, so a single delegation is not announced in the plural: English gains <code>_one</code> forms and Chinese, which has one plural category, drops the English plural marker. Nothing else about the card changes, and no IPC, storage, host-protocol, or delegation-runtime change is implied.</strong> <em>(amended by D319)</em></td><td>Reserving the card for two or more delegates made the same work look like two different features, and the single case — the common one — was the one that lost the outcome, the recorded runtime and the delegate&#39;s step count (ADR 0062 §6).</td></tr><tr><td>D319</td><td>A delegation card is only the Task fan-out</td><td><strong>Amends D201 / D265 / ADR 0062 in the renderer: consecutive <code>Task</code> starts form their own activity group and are the only rows inside the delegation card. Parent thinking, workspace tools, and lifecycle rows (<code>TaskWait</code>/<code>TaskList</code>/<code>TaskStop</code>) stay in ordinary processing groups before and/or after that card, never on the topology tile. The card keeps 16px inset from its tile edge. A topology group stays live (open once, ticking elapsed from its own delegation timestamps) while any of its delegates is still running, even when the parent has already moved on to a later processing group in the same turn. Renderer only: no IPC, storage, host-protocol, or delegation-runtime change.</strong></td><td>Consecutive thinking and tool rows used to share one activity group, so a parent that kept working after <code>Task</code> painted its own Read/Grep/thinking flush against the subagent tile and labelled them “Subagent working”. The topology already names the main agent; the parent&#39;s later work belongs in the turn stream, not inside the card.</td></tr><tr><td>D320</td><td>Sent file references stay chips and open on click</td><td><strong>Amend D209 / ADR 0070 in the renderer and Electron main: a sent user turn parses serialized <code>@path</code> / <code>@&quot;quoted path&quot;</code> tokens and paints each as a composer-matching leaf-name chip (icon, ellipsized name, canonical path in tooltip/name). Workspace <code>.html</code>/<code>.htm</code> chips open the work-panel browser; every other allowed file opens with the OS default handler through <code>pi-desktop/fs/open</code>. That channel contains relative paths to the workspace and absolute paths to the workspace, <code>&lt;data_dir&gt;/scratch/</code>, or <code>&lt;data_dir&gt;/attachments/</code>. HTTP(S) URLs stay text links. Tool-row previews are unchanged. No host-protocol or storage schema change.</strong></td><td>Serialized full paths undid the compact composer node the moment the user sent, and the files tab cannot preview HTML as a page or open suffix-specific OS apps (ADR 0163).</td></tr><tr><td>D322</td><td>Relative transcript paths preview and open</td><td><strong>Amend the chat/markdown file-link resolver: unprefixed relative paths stay workspace-rooted; <code>./</code> and <code>../</code> resolve against the workspace root in chat and against the viewed file&#39;s directory when previewing a markdown document. <code>.</code> / <code>..</code> normalize in place; a walk that leaves the workspace stays inert. Assistant markdown linkifies bare file tokens (known extension) in addition to inline code, links, and images. The linkify pass is a unified attacher that returns a transformer; it is never the transformer itself, so freeze does not invoke it with a missing tree. No host-protocol, IPC, or storage schema change.</strong></td><td>Agents write workspace-relative paths in prose and markdown files use <code>./</code> / <code>../</code> links; those were either not clickable or resolved as if they lived at the workspace root, so preview/open failed. Passing the transformer to <code>remarkPlugins</code> made unified call it at freeze with <code>tree === undefined</code>, crashing session open on <code>tree.type</code>.</td></tr><tr><td>D323</td><td>Live parent turns stay transparent</td><td><strong>Amends D101 / D297 in the renderer: a streaming assistant turn does not sit on a <code>--ds-tile</code> and does not reserve a 14px inset or left rail. Thinking, workspace tools, and answer fragments stay on the page background like a completed turn. The tile remains only on the delegation card (D319) and the nested <code>.subagent-run</code>. Renderer only: no IPC, storage, host-protocol, or runtime change.</strong></td><td>D297 replaced the streaming rail with a whole-turn tile so the tint would not reflow. That boxed ordinary answers the same way as a subagent card. D101 already required transparent assistant prose; D319 already assigned the tile to the Task group.</td></tr><tr><td>D268</td><td>A lifecycle row is a subagent row</td><td><strong>Refines D201 / D265 / ADR 0062 and ADR 0089 in the renderer: the delegation lifecycle rows (<code>TaskWait</code>/<code>TaskList</code>/<code>TaskStop</code>) stay compact tool rows and stay out of the topology counts, but are presented as subagent rows. They summarize from the roster the runtime returned — agent names read from <code>details.delegations[]</code> or <code>details.stopped[]</code>, a repeat counted rather than listed twice — never from their own <code>delegationIds</code> argument; their badge rolls that roster up using the existing <code>chat.subagentStatus.*</code> vocabulary; their label names the action taken on subagents instead of &quot;Delegated&quot;; and their body is the roster as a named table led by the joined reports, instead of the raw <code>delegations[]</code> JSON. No IPC, storage, host-protocol, or delegation-runtime change is implied.</strong></td><td>The lifecycle rows carry the settled outcome the <code>Task</code> card cannot know (ADR 0089), yet they rendered as generic tool calls whose summary was a bare UUID and whose body was a JSON dump — the least readable part of a feature whose card is otherwise the most readable. Reading the roster it already returns costs nothing and makes the whole delegation scan as one thing.</td></tr><tr><td>D263</td><td>A cross-display drag is user intent, not an OS adjustment</td><td><strong>Amend D255 / ADR 0122 in Main: the work-panel reservation reconcile no longer treats every display change alike. <code>os-adjusted</code> (the window manager re-fitting bounds we asked for, or a topology change) preserves ADR 0122 verbatim; <code>user-moved</code> (the user dragging the window to another display) takes the current position as the new base bounds, normalizing only the origin into the target work area and keeping the size. Attribution keys off an unaccounted native <code>move</code> stream rather than a deadline, and reconciliation is deferred until that stream goes quiet, so no bounds are re-planned mid-drag. A cross-display drag advances the remembered display key and is persisted. No IPC, storage, or host-protocol change.</strong></td><td>The old logic re-planned from the origin display&#39;s base bounds and was then clamped to the target work area&#39;s edge, so the window jumped on pointer release and stale coordinates were written to <code>window-state.json</code> (issue #18, ADR 0132).</td></tr><tr><td>D269</td><td>History continuation keeps the conversation outline reachable</td><td><strong>Amend D108 / D261 / ADR 0130 in the renderer: when loaded rows are withheld above the mounted window or the host reports an older page, the conversation outline remains present with one dotted earlier-history continuation even if the mounted tail has fewer than two markers or does not overflow. The continuation runs the same two-stage grow-then-fetch path and ordinary message dashes still come only from mounted rows, so every message dash has a real DOM target. The transcript&#39;s top loading boundary is observed as well as checked on scroll; while that boundary remains visible after an underfilled tail, a fetched-but-withheld page, or a window transition, history advances again without waiting for a native scroll event. Once no earlier history remains, D108&#39;s two-marker-plus-overflow visibility rule applies unchanged. No IPC, storage, host protocol, or pagination change.</strong></td><td>A bounded tail can contain one tool-heavy turn that collapses below one viewport, and fetching an older page can leave every new row outside the trailing mounted window. Neither transition necessarily changes <code>scrollTop</code>, so the scroll-only trigger stranded older history and left the outline absent. An explicit continuation represents unavailable navigation honestly, while boundary visibility closes the progress loop without inventing phantom message markers or eagerly loading the whole transcript.</td></tr><tr><td>D270</td><td>One model picker for both credential kinds</td><td><strong>Refines D237 / D240 in the renderer: the model picker is one shared renderer component rendered by both the AI service dialog and the vendor account dialog. A vendor account therefore gets the same discovered model list, the same <code>ModelBinding</code> shape, the same per-model context-window and max-output editing, the same thinking-level controls, and the same explicit binding persistence. Only the left pane heading differs between the two dialogs. The thinking-level catalog-gating clauses are superseded by D283. Renderer only: no IPC, storage, host-protocol, or provider-config schema change is implied, and no ADR is required.</strong></td><td>The two surfaces were copies of one picker, and the copy silently lost the advanced controls, so a vendor-account binding could not edit the same model settings as an AI service. One component makes the guarantee structural instead of a convention two files had to remember.</td></tr><tr><td>D271</td><td>An expanded delegate run scrolls in place</td><td><strong>Refines D201 / D268 / ADR 0062 in the renderer: a delegate&#39;s nested rows render in a bounded <code>.subagent-run-rows</code> scroll area of <code>min(420px, 48dvh)</code> with <code>overscroll-behavior-y: contain</code>, placed on that inner wrapper rather than on <code>.subagent-run</code> so the absolutely positioned collapse rail is not clipped. The run heading stays outside the scroll area and the area is a labelled, focusable group. <code>fields</code> tables gain the same 260px cap the other detail blocks already had, and a lifecycle row&#39;s joined reports render as a bounded <code>output</code> block instead of an unbounded note. Presentation only: no IPC, storage, host-protocol, or runtime change.</strong></td><td>Expanding one delegation could add dozens of rows in a single commit — a delegate that made forty tool calls grew the transcript by forty rows — so the reading position jumped and the parent&#39;s own next row was pushed out of view. <code>fields</code> was the one detail block with no height limit, and D268 had routed up to 50k characters of joined reports into a <code>note</code>, which has none either.</td></tr><tr><td>D302</td><td>An expanded delegate run follows its latest output</td><td><strong>Refines D271 / D071 in the renderer: the bounded <code>.subagent-run-rows</code> scroller uses the parent transcript&#39;s pinned-follow contract independently. Expanding pins to the newest row; content growth follows the bottom while pinned; the first real upward gesture (wheel / trackpad / touch / scrollbar / keyboard) pauses follow and shows a nested jump-to-latest control; layout clamps and programmatic <code>scrollTo</code> never release follow. Native overflow anchoring is disabled on the nested scroller. The jump control overlays a relative wrapper around <code>.subagent-run-rows</code>, never on <code>.subagent-run</code>, so the collapse rail stays unclipped. Presentation only: no IPC, storage, host-protocol, or runtime change, and no ADR is required.</strong></td><td>D271 capped the nested run so it would not grow the transcript, but left that scroller without stick-to-bottom. A live delegate&#39;s new thinking, tool, and answer rows therefore appended below the viewport of an expanded card, and the user had to scroll by hand to see progress (issue #37).</td></tr><tr><td>D272</td><td>Quitting stops plugins as a shutdown, not as a crash</td><td><strong>Refines spec 07 §3.1 in the main process: <code>before-quit</code> calls <code>PluginRuntime.disposeAll()</code> instead of <code>disposeWatchers()</code>. It marks every loaded plugin as disposing and cancels its pending restarts before anything else, disposes watchers, then stops services and runs <code>onUnload</code> in parallel — 1.5s per plugin, 3s for the whole sequence, after which the children are killed. <code>onPluginCrash</code> no longer sends its own toast, since the runtime already raised one on that path. Main process only: no IPC, storage, host-protocol, or manifest change, and no ADR is required.</strong></td><td>Quit left the plugin children to be killed by the process teardown, and an unmarked exit is indistinguishable from a crash: <code>handleChildExit</code>&#39;s <code>disposing</code> guard never applied, so every quit logged <code>PLUGIN_CRASHED</code> per plugin at <code>exitCode: 0</code>, toasted &quot;stopped unexpectedly&quot; twice, and scheduled restarts into a closing app. 613 of 633 crash reports in one local log were this, which is what made the real crashes hard to see.</td></tr><tr><td>D273</td><td>A model&#39;s habitual argument name is accepted, not corrected</td><td><strong>Refines spec 03 §4 in the agent runtime: <code>Read</code>/<code>Write</code>/<code>Edit</code>/<code>BrowserPreview</code> accept <code>file_path</code> beside <code>path</code>, and <code>Glob</code>/<code>Grep</code> accept <code>query</code> beside <code>pattern</code>. Both spellings are optional in the schema so either validates; the runtime folds the alias onto the canonical name before the write lock, the host call, and the transcript, requires exactly one of the pair, and lets the canonical name win when both arrive. <code>Bash.timeout</code> widens its schema maximum to 3600000 so a millisecond value validates, and the runtime reads a value of at least 1000 as milliseconds, clamped to the honoured 300-second ceiling, while 301 to 999 stays an out-of-range seconds value. Runtime only: no IPC, storage, or host-protocol change, and host-core&#39;s schemas are unchanged.</strong></td><td>Argument names are pretraining habits, not instructions a schema can correct. 852 of 1679 failed tool calls in one local month were a parameter-name miss, 724 of them <code>Read</code> sent with <code>file_path</code>, which alone was 87% of <code>Read</code>&#39;s failures and put its error rate at 11%. Another 69 were a <code>timeout</code> in milliseconds. Each cost a turn on an error the model could not see itself making.</td></tr><tr><td>D274</td><td>Retry an unchanged edited prompt</td><td><strong>Amends D137 in the renderer: confirming a valid user-prompt edit always dispatches the existing edit-resend/Regenerate path, even when the trimmed text matches the original. The inline controls are localized Retry and Cancel actions; Retry retains the existing revision archive, identity-based truncation, slash-command expansion, and attachment behavior. Renderer and i18n only: no IPC, storage, host-protocol, or runtime contract change.</strong></td><td>Treating an unchanged confirmation as a no-op made the “Edit and resend” action appear broken (issue #23), while the old Send label suggested a new ordinary prompt rather than replaying the selected turn.</td></tr><tr><td>D275</td><td>Preserve the active task boundary across context compaction</td><td><strong>Amends D203 / ADR 0064 in the agent runtime: checkpoints record opaque <code>details.retainedTailMode</code>. An active-turn checkpoint retains only the latest user message (up to the existing 20,000-token cap) when the provider must continue after a tool result, <code>toolUse</code>, or overflow recovery. A completed-turn checkpoint has an empty retained tail at terminal boundaries, before a new prompt, and for manual compaction; its summary is authoritative for completed work. Legacy records without the mode normalize to their latest user message. No visible transcript, storage schema, host ownership, or protocol shape changes.</strong></td><td>Keeping several recent user prompts while compacting away completion messages made the next prompt look like a continuation of an old task (issue #22). The boundary must be explicit at restore time while active tool loops still retain the prompt needed to continue.</td></tr><tr><td>D278</td><td>Subagent model selection</td><td><strong>Task.model overrides definition pin; only models with <code>availableForSubagents</code> appear in the delegation catalog; on-demand RPC resolves models not pre-resolved at launch</strong></td><td>The parent agent needs per-task model choice without exposing the full provider configuration. An opt-in flag keeps the delegation catalog bounded and intentional, and on-demand resolution avoids stale bindings for models added after sidecar launch. See §3/02 §5f, §3/11 §7, and E2E-166.</td></tr><tr><td>D365</td><td>Named quiet intervals on the live activity row</td><td><strong>Amend D338 / ADR 0175: <code>AgentActivity</code> adds <code>preparing</code>, <code>compacting</code>, and <code>recovering</code>; <code>starting</code> shows its own label; <code>waiting-subagents</code> carries a live running snapshot (<code>name</code>, <code>lastPhase</code>, <code>lastToolName</code>) that updates on child tool/thinking changes, not on every token. The row stays one compact inline status and does not restore an activity-group capsule.</strong></td><td>Compaction, silent-turn recovery, the post-tool gap, and startup all looked like a stuck generic wait, and a parent wait hid what its delegates were doing (ADR 0198, E2E-008c / E2E-094)</td></tr><tr><td>D599</td><td>A development build is its own installation</td><td><strong>Narrow D236 / amend ADR 0094: a development build (unpackaged, or <code>PI_DESKTOP_DEV=1</code>) takes <code>PI-Desktop Dev</code> as its Electron <code>userData</code> — and with it the single-instance lock, renderer <code>localStorage</code>, the plugin panel partitions, and browser pane cookies — and reads <code>~/.pi-desktop-dev</code>. An explicit <code>--user-data-dir</code> still wins, which is how the E2E harnesses point a build at a throwaway profile. A packaged installation keeps <code>PI-Desktop</code> and <code>~/.pi-desktop</code>, so no existing profile is relocated. <code>PI_DESKTOP_DATA_DIR</code> still overrides either profile and is made absolute before it reaches host-core as a child-process environment variable; Electron main publishes the resolved directory back to that variable so the plugin runtime reads one root. No IPC, protocol, schema, or packaged-installation path change. See <code>03-runtime/07-process-model.md</code> and E2E-150.</strong></td><td>A packaged app that was already running held the lock, so <code>pnpm dev</code> quit on arrival; a development host that won the race instead put a second host-core over the same single-writer <code>pi.sqlite</code>, the outbox, and the log tree.</td></tr><tr><td>D602</td><td>Crash dumps stay in the data directory</td><td><strong>Electron&#39;s Crashpad reporter starts local-only (<code>uploadToServer: false</code>) before <code>ready</code>. Dumps live under <code>&lt;data_dir&gt;/crash-dumps</code>, not the default Electron <code>userData</code> crashDumps path, so a <code>PI_DESKTOP_DATA_DIR</code> profile does not share dumps. The next lock-holding launch writes one <code>diagnostics</code> line for dumps newer than <code>crash-dumps.json</code>, classified by Crashpad <code>ptype</code>: <code>error</code> if any new dump is the browser/main process, <code>warn</code> for recovered renderer/GPU/utility crashes. Host-core and sidecar crashes stay on the supervisor path. No upload, no IPC, no schema change.</strong></td><td>A crash left a minidump nobody read. Crashpad also records recovered renderer crashes, so a next-launch <code>error</code> that said the previous run died was a lie; and dumps outside the data directory escaped <code>PI_DESKTOP_DATA_DIR</code> isolation.</td></tr><tr><td>D619</td><td>A copied formula is its TeX source</td><td><strong>Renderer only: a copy whose selection covers rendered math writes <code>text/plain</code> from the MathML <code>annotation</code> — <code>$…$</code> inline, <code>$$…$$</code> on its own lines, the delimiters <code>lib/latex-math.ts</code> normalizes <code>\\(…\\)</code> and <code>\\[…\\]</code> to, each run widened past any run inside the formula as a code span&#39;s fence is — instead of the two trees KaTeX paints. A cut that lands inside a formula grows to the whole formula. Only the formulas are rewritten: the reduced clone is read back through <code>Selection.toString()</code>, the serializer a copy itself runs, so prose, lists, tables and code blocks sharing the selection keep the platform&#39;s own reading — <code>user-select: none</code> chrome left behind included, which <code>innerText</code> would have written out. A selection with no formula in it, and a copy raised where the selection does not live, are left to the platform entirely. One flavour is written, <code>text/plain</code>: taking the event over drops the platform&#39;s <code>text/html</code> too and none is written back, because the reduced clone is app markup that would carry the <code>user-select: none</code> chrome the text reading drops, and because carrying the rendering instead would paste every formula twice — KaTeX&#39;s stylesheet is the only thing hiding the MathML tree and no stylesheet travels on the clipboard. One document <code>copy</code> listener owned by the shell, and the transcript&#39;s right-click Copy reads the same selection through the same module.</strong></td><td>A formula pasted as its glyphs, once per rendered tree, so it could not be carried into a LaTeX document or another Markdown editor (issue #414). ADR 0268 removed quoting on the grounds that the OS clipboard was the substitute; the clipboard had to actually carry the source.</td></tr></tbody></table><h2 id="m0-model-catalog-decisions" tabindex="-1">M0. Model catalog decisions <a class="header-anchor" href="#m0-model-catalog-decisions" aria-label="Permalink to &quot;M0. Model catalog decisions&quot;">​</a></h2><table tabindex="0"><thead><tr><th>ID</th><th>Topic</th><th>Decision</th><th>Rationale</th></tr></thead><tbody><tr><td>D266</td><td>Use models.dev as the primary model catalog</td><td><strong>Amend D136 / ADR 0027: Electron main loads <code>https://models.dev/api.json</code> as the primary provider/model metadata source, matches by provider key or normalized API URL, and maps its limits, modalities, reasoning options, tool-call, structured-output, display-name, and cost fields into the existing model surfaces. The pinned <code>pi-ai</code> catalog remains the local fallback for unavailable/missing remote records and supplies adapter-specific compatibility data where models.dev has no equivalent. Provider endpoint discovery and explicit model IDs remain available for custom/account-specific models; no provider credential is sent to models.dev, and no host schema/protocol change is introduced.</strong></td><td>The pinned pi-ai catalog is accurate for runtime adapters but lags the broader market catalog. A bounded, main-process models.dev fetch gives Settings and Composer current model coverage while preserving offline/local behavior and pi-ai&#39;s transport fallback.</td></tr></tbody></table><h2 id="m-agent-runtime-decisions" tabindex="-1">M. Agent runtime decisions <a class="header-anchor" href="#m-agent-runtime-decisions" aria-label="Permalink to &quot;M. Agent runtime decisions&quot;">​</a></h2><table tabindex="0"><thead><tr><th>ID</th><th>Topic</th><th>Decision</th><th>Rationale</th></tr></thead><tbody><tr><td>D114</td><td>Per-session scratch directory for agent temp files</td><td><strong>Each session gets <code>&lt;data_dir&gt;/scratch/&lt;sessionId&gt;/</code> as a second containment root for <code>Read</code>/<code>Write</code>/<code>Edit</code> (absolute paths only; relative paths stay workspace-bound). The path is advertised in the system prompt and as <code>PI_SCRATCH_DIR</code> in Bash. Scratch writes auto-allow without a permission card and are excluded from the artifacts table. Scratch is created lazily, deleted with the session, and swept at startup (orphans, &gt;7 days stale). Glob/Grep/BrowserPreview remain workspace-only. Plan does not expose Write/Edit; Plan Bash can still mutate scratch under its resolved permission mode.</strong></td><td>Temp/intermediate files (one-off scripts, downloaded data, drafts) were dirtying the user&#39;s project and git status; a host-owned root with identical lexical + symlink defenses keeps the sandbox model intact while giving the model a legitimate place for scratch work</td></tr><tr><td>D115</td><td>Permission modes: global default + per-session override</td><td><em>(composer presentation superseded by D132; checkpoint Plan clause superseded by D189)</em> <strong>A permission mode governs high-risk tool approval: <code>ask</code> (confirm everything, default), <code>accept-edits</code> (auto-allow Write/Edit, confirm Bash/plugins), <code>auto</code> (auto-allow all). Global default lives in settings <code>defaultPermissionMode</code>; each session stores <code>permission_mode</code> (schema v5, default <code>inherit</code>) which overrides it when not inherit. Resolution: session override → global default → ask, enforced solely in host-core <code>tools.execute</code>. Plan retains the selector; its Write/Edit/plugin hard deny outranks every mode, while Bash follows the selected mode.</strong></td><td>Confirming every Write/Edit made long agent runs high-friction, but a single global toggle is too coarse — trusted scratch sessions and risky repo sessions need different postures; keeping enforcement host-side preserves the security boundary</td></tr><tr><td>D132</td><td>Composer permission menu shows effective modes only</td><td><strong>The agent-mode composer chip and menu expose only <code>ask</code>, <code>accept-edits</code>, and <code>auto</code>, with the effective mode selected directly and no global-default/inherit entry or provenance label. Choosing an item stores that explicit session override. Existing <code>inherit</code> persistence and resolution from D115 remain unchanged until the user chooses a mode. This supersedes only D115&#39;s composer-presentation clause.</strong></td><td>The inherited entry repeated a selectable mode and exposed storage provenance instead of the permission posture the user is choosing; presenting the three effective modes makes the control direct without changing host enforcement</td></tr><tr><td>D116</td><td>Provider failures as assistant messages</td><td><strong>Every provider/model turn failure is attached to a durable <code>role=assistant</code>, <code>status=error</code> transcript message through optional <code>UiMessage.error</code>. The message shows a localized summary and stable code, keeps redacted provider detail behind an accessible disclosure, and offers context-appropriate Retry or Settings actions. Message-bound failures never use toast/global banner presentation and never re-enter later model context.</strong></td><td>Errors belong to the failed turn; preserving them in the transcript makes the response diagnosable after session switches/restarts without contaminating the next model request or exposing credentials</td></tr><tr><td>D127</td><td>Context-preserving reseed + transport retry</td><td><strong>Reseeding a recreated pi runtime from the persisted transcript restores tool call/result pairs (from tool rows&#39; <code>toolCallId</code>/<code>toolArgs</code>/<code>toolResult</code>) in addition to user/assistant text and thinking. Interrupted tool rows restore as errored results; orphaned tool rows get a synthesized call-only assistant carrier so pairs stay adjacent and well-formed. Failed assistant turns stay transcript-only. Separately, request setup uses one bounded pi-ai retry for transient transport/provider failures; post-response stream recovery is defined by D186.</strong></td><td>Text-only reseed collapsed a session&#39;s context after any runtime recreation (regenerate/edit, config change, restart): the model lost every tool result it had gathered and — seeing its own history answer without visible tool use — stopped calling tools, degrading agent sessions into bare chat. The incident trigger was a single un-retried provider timeout that forced the user into regenerate. D127 corrects the initially duplicated D120 identifier; D120 remains the earlier application-update decision frozen by baseline 0.4.6.</td></tr><tr><td>D136</td><td>pi-ai owns known-model metadata</td><td><strong>For every model resolved from the pinned pi-ai catalog, Electron main passes the complete pi model snapshot to the sidecar and PI-Desktop replaces only connection identity. Provider Settings and the model menu do not override reasoning support, thinking levels, context/output limits, temperature, or compatibility. Unknown free-form ids remain usable through an explicit generic text-only, non-reasoning fallback. This supersedes D102 and the provider-override clauses of D096/D107.</strong></td><td>A second desktop-owned model matrix discarded pi metadata, drifted from adapter behavior, and made model semantics depend on conflicting configurations; fixes for known models now belong in pi-ai or a pi-ai upgrade.</td></tr><tr><td>D183</td><td>Segmented tool and model latency logs <em>(superseded by D385 / ADR 0212; UI clause superseded by D184)</em></td><td><strong>Every <code>tools.execute</code> call is timed in segments instead of one opaque duration: host-core emits a <code>tool timing</code> line on the <code>host</code> channel and persists <code>prompted</code>, <code>permissionWaitMs</code>, <code>overheadMs</code>, and <code>totalMs</code> next to the existing <code>durationMs</code> on <code>tool_execute</code> / <code>tool_denied</code> audit rows; the sidecar writes greppable <code>[timing] kind=tool …</code> (<code>hostRttMs</code>) and <code>[timing] kind=model …</code> (<code>providerWaitMs</code>, <code>streamMs</code>, including failed/aborted turns) lines to the <code>agent</code> channel, suppressible with <code>PI_DESKTOP_TIMING=0</code>. The original no-UI clause is superseded by D184; logging remains unchanged.</strong></td><td>&quot;Executing a command is slow&quot; was undiagnosable from the logs: approval waiting, the tool body, and the provider round trip were indistinguishable, so a 45s gap between two audit rows with 0ms durations gave no clue whether it was the user, the model, or the host. Splitting the stages makes the answer readable without reproducing the run.</td></tr><tr><td>D158</td><td>Turn-boundary context checkpoint compaction <em>(soft-boundary, model-tool, and visibility clauses superseded by D200; the model tool and visibility restored in Codex&#39;s shape by D203)</em></td><td><strong>PI-Desktop reuses pi-agent-core&#39;s context estimation, session-context, and compaction primitives but owns the orchestration and durability. After every <code>turn_end</code>, before any next provider request, the runtime evaluates model-aware soft/hard budgets. A transient deduplicated instruction can ask the model to call the internal <code>CompactContext</code> tool; the tool&#39;s normal activity row is visible/durable, while the instruction is not. Crossing the hard budget forces checkpoint generation and blocks the request on failure. A final atomic tool batch that reaches half the hard budget is fairly head/tail-truncated only in the checkpoint copy, with explicit markers and every call/result envelope retained; original transcript rows remain complete. Exact provider overflow removes the failed assistant from model context, creates one checkpoint, and retries once. Host protocol v6 appends checkpoint records beside the untouched visible JSONL transcript; restart, late truncation, and included-boundary forks preserve the newest valid checkpoint. Disabling automatic compaction removes the tool and all automatic threshold/overflow recovery, while <code>/compact</code> remains available. OpenCode DCP is an AGPL-3.0 behavioral reference only and is neither linked nor copied (ADR 0030).</strong></td><td>pi&#39;s end-of-run-only behavior cannot protect long tool loops, and a model reminder alone cannot guarantee provider safety. Reusing pi&#39;s tested compaction format while adding a deterministic <code>turn_end</code> gate prevents another provider request from crossing the known window, retains user-visible history, and avoids importing an incompatible plugin/runtime and license boundary.</td></tr><tr><td>D185</td><td>Lazy per-turn tool activation <em>(the always-active <code>CompactContext</code> clause is void under D200, and holds again for <code>new_context</code> under D203; activation restoration amended by D400 / ADR 0225)</em></td><td><strong>The sidecar keeps a complete local tool registry but sends only the mode core set and local <code>ToolSearch</code> on each new prompt. <code>BrowserPreview</code>, plugin tools, <code>Skill</code>, and plugin-development helpers appear as bounded compact catalog entries and are activated by exact-name or capability search; the next turn receives their schemas, native pi-ai deferred search is used when supported, and the in-memory set resets before the next user prompt, then restores only successful activation evidence still present in the effective context and allowed by the current catalog and mode. Host permissions, containment, timeouts, and audits are unchanged.</strong></td><td>Full tool schemas made simple first requests disproportionately large and repeated optional capability cost across turns. A pi-style active set preserves core coding ergonomics while making ancillary tools pay-as-you-go and provider-independent; restoring successful context evidence prevents the model from seeing a capability marker without receiving the corresponding schema.</td></tr><tr><td>D200</td><td>Imperceptible background context compaction <em>(background pre-computation, incremental trigger, silence, and no-model-tool clauses superseded by D203)</em></td><td><strong>Compaction becomes a host-owned background activity with no user-visible surface. <code>contextBudget()</code> keeps the D158 hard limit and request headroom, derives the retained-tail target from the model window (<code>clamp(hardLimit * 0.2, 8k, 64k)</code>, still capped at half the hard budget) instead of settings, and adds <code>backgroundLimit = floor(hardLimit * 0.7)</code> as a pre-computation trigger; the soft boundary is deleted. Checkpoint generation is split from installation: <code>buildCheckpoint</code> produces one without persisting or activating it, and installation re-estimates, appends through host-core, and emits <code>compaction_end</code>. Pre-computation runs only in provider-idle windows — while a tool executes and after a run ends — and only when the context is past the background limit <strong>and</strong> grew by at least the retained-tail target since the newest checkpoint&#39;s baseline, so a large tail cannot trigger a summary every turn. A pre-computed checkpoint installs at the next turn boundary or prompt only if its base is still active, its <code>throughMessageId</code> anchor still exists, and it still fits the current model&#39;s budget; any miss falls through to the unchanged blocking path, and a failed background build is discarded with no event, no persistence, and no ADR 0049 fallback. The <code>CompactContext</code> tool, the <code>&lt;context_management&gt;</code> nudge, and the host no-confirmation allowlist entry are removed, so triggering is entirely deterministic. <code>compaction_start</code>/<code>compaction_end</code> gain an optional <code>phase</code> (<code>background</code> | <code>blocking</code>, absent means <code>blocking</code>) and <code>compaction_end</code> gains an optional <code>status { generation, summaryTokens }</code>; both are additive inside protocol v9. A successful automatic compaction produces no toast, no run-state change, and no transcript row; only a <code>retained_tail</code> fallback, an overflow retry, and manual <code>/compact</code> still notify. The context usage inspector is the single visible trace, reading <code>status</code> and the durable <code>SessionDetail.compaction</code>, with the generation counter carried inside the checkpoint&#39;s opaque <code>details</code> so no record schema change is needed. Settings exposes no compaction controls and persisted <code>contextCompaction</code> values are ignored (ADR 0061).</strong></td><td>Compaction was correct but intrusive: it toasted, moved the run state, spent a model turn on a tool call, left a transcript row, and always ran at the moment the user was waiting. Codex&#39;s graded trigger and increment-scoped threshold show the summary can be paid for off the critical path, and its host-only triggering removes a class of wasted turns. Deriving budgets from the model window also fixes applying one pair of absolute token counts to both a 32k and a 1M window; keeping the hard boundary untouched means none of this trades provider safety for UX.</td></tr><tr><td>D201</td><td>Bounded subagents behind a <code>Task</code> tool</td><td><strong>Agent mode exposes <code>Task</code> when the catalog contains one of the four inline builtins or an enabled global document under <code>~/.agents/subagents/*.md</code>. The catalog is loaded by Electron main for the next prompt, is capped at 16, and keeps delegate reports out of the parent model context. There is no project-level subagent capability directory; <code>.pi/agents</code> is not scanned.</strong></td><td>Keeping delegation as a bounded tool preserves parent ownership of context and permissions while a global user directory makes personal delegates portable across projects (ADR 0062, ADR 0112).</td></tr><tr><td>D202</td><td>Managed global subagent definitions</td><td><em>(Page-shape clause superseded by D257 / ADR 0126; the global-only data boundary stands)</em> <strong>host-core scans global Markdown documents under <code>~/.agents/subagents</code>, stores enabled state in <code>&lt;data&gt;/agent-capabilities/subagents.json</code>, and exposes them through the Settings &gt; Agent &gt; Subagents page. The page is one fixed-height global list with no project picker or project-level source. The runtime combines enabled global documents with builtins; malformed or deleted documents produce diagnostics or disappear after scanning, and no capability state is written into the Markdown file.</strong></td><td>Personal delegates follow the user without creating repository changes; the explicit global-only boundary avoids a second project precedence model and keeps Extensions focused on Installed and Marketplace (ADR 0063, ADR 0112).</td></tr><tr><td>D203</td><td>Codex-parity context compaction</td><td><strong>Compaction is rebuilt to match Codex&#39;s mechanism, reversing four D200 clauses and keeping the rest. All background pre-computation and the incremental trigger scope are deleted: <code>prepareNextTurn()</code> compacts synchronously when the total context crosses <code>hardLimit</code> or the model asked for a new window. A checkpoint carries the summary plus recent <strong>user</strong> messages only — pi&#39;s cut point still marks the boundary, but its split-turn prefix and recent tail are folded back into the summary input so the summary covers the whole range, and the retained tail is rebuilt newest-first from user messages up to 20,000 tokens (capped at half the hard budget) with the crossing message truncated rather than dropped, then restored to chronological order. Two families run the identical lifecycle: <code>summary</code>, and a <code>fresh_window</code> rollover that requests no summary and stores a fixed marker text, selected by construction option or <code>PI_DESKTOP_COMPACTION_STRATEGY</code> and exposed in neither settings nor i18n. The model-facing <code>new_context</code> tool returns (parameterless, Codex&#39;s description verbatim, on the host no-confirmation allowlist, never assignable to a subagent) together with two per-window budget reminders appended to the current turn&#39;s system prompt — one at <code>clamp(hardLimit * 0.15, 8k, 32k)</code> remaining, one at 2,000 — each claimed once and reset on install. host-core persists the whole checkpoint chain (<code>read_compactions</code>, <code>write_transcript_with_compactions</code>, per-record fork validation, <code>SessionDetail.compactions</code>) with <code>compaction</code> kept as its newest element. <code>compaction_start</code>/<code>compaction_end</code> drop <code>phase</code>, and <code>compaction_end</code> replaces <code>status</code> with <code>mark { id, throughMessageId, generation, summaryTokens, summarized }</code>; the transcript draws one divider row per compaction after the message it covers, ending the assistant turn it lands inside and dropping a mark whose anchor is gone, and every successful compaction raises one warning toast on top of the fallback/overflow/manual toasts. Deliberate deviations from Codex: the summary precedes the retained users because <code>buildSessionContext</code> fixes that order, <code>hardLimit</code> stays &quot;window − output reserve&quot; instead of 90% of the window, the tool is registered in both families, and the reminders are system-prompt appends with our own thresholds and wording (ADR 0064).</strong></td><td>The previous round cited Codex while implementing its opposite on four counts, and its Context section claimed Codex has no model-side compaction tool when <code>new_context</code> exists. The user asked for Codex&#39;s mechanism specifically, after being told it reverses the imperceptibility goal. Parity also buys three things on its own merits: a checkpoint that keeps only user messages is far cheaper and cannot strand a tool call without its result, a visible row makes a lossy operation auditable from the transcript again, and a warning puts the &quot;start a fresh session instead&quot; decision where it belongs. The cost is accepted: the user waits for compaction again.</td></tr><tr><td>D186</td><td>Bounded provider stream recovery and diagnostics <em>(429 budget amended by D245)</em></td><td><strong>Provider request setup uses one bounded retry for non-rate-limit transient failures. A transient <code>STREAM_FAILED</code>, <code>NETWORK_ERROR</code>, or <code>TIMEOUT</code> after streaming begins is replayed once in the same turn after abortable backoff; the failed assistant is removed from model context and its visible message id is reused. HTTP 429 setup and stream recovery follow D245&#39;s shared five-retry budget. Provider <code>AppError.details</code> carries only bounded phase, timing, provider status/code, and retry-attempt diagnostics. Mutation guidance uses one fresh read/regeneration after an <code>Edit</code> mismatch; a second failed <code>Edit</code> for the same path or a second failed shell patch command returns a terminating tool hint instead of repairing old patch artifacts.</strong></td><td>Unbounded or regenerate-driven recovery made transient stream termination expensive and made patch loops consume turns without new information; finite runtime budgets preserve context and user control while keeping failures diagnosable (ADR 0050, amended by ADR 0091 and D245).</td></tr></tbody></table><p>| D187 | Resource-isolated host RPC stdio | <strong>host-core reads stdin and serializes stdout through one dedicated named OS thread per direction, never through Tokio&#39;s dynamic blocking pool. The threads retry interrupted and transient <code>EAGAIN</code>/<code>EWOULDBLOCK</code> errors while preserving NDJSON framing; inability to create a control thread is a structured startup failure. The login-shell PATH probe also treats helper-thread creation as best effort and falls back to the inherited PATH. RPC/tool admission limits remain unchanged.</strong> | Tokio stdio can panic when OS thread creation returns <code>Resource temporarily unavailable</code> (errno 35 on macOS), turning temporary resource pressure into <code>HOST_UNAVAILABLE</code>; isolating the control pipe removes that process-level crash path while retaining bounded overload behavior (ADR 0051). | | D191 | Agent-only mode; Chat renamed to read-only | <em>(superseded by D188/D189: the mode selector returned as <code>Agent | Plan</code>, and <code>chat</code> migrates to <code>plan</code>)</em> <strong><code>agent</code> is the only session mode the product exposes. The former <code>chat</code> profile is renamed <code>read-only</code> and keeps its <code>Read</code>/<code>Glob</code>/<code>Grep</code> hard deny in host-core, but it has no UI surface: no top-bar toggle, no composer chip, no Settings row, no palette command or slash alias, and no localized labels. The host normalizes <code>chat</code> to <code>read-only</code> on every write path (<code>session.create</code>, <code>session.configure</code>, <code>session.import</code>) and the permission gate is negative — anything that is not <code>agent</code> gets the read-only surface — so an unknown or legacy value can never widen the tool set. Error codes become <code>BASH_DISABLED_IN_READ_ONLY</code> / <code>WRITE_DISABLED_IN_READ_ONLY</code>. A boot fix-up rewrites existing <code>sessions.mode = &#39;chat&#39;</code> rows and a stored <code>defaultMode</code> of <code>chat</code> to <code>agent</code>.</strong> | A mode switch the product never intends users to reach is a footgun and dead UI weight: sessions could be stranded on a read-only profile with no way back, and two toolsets doubled the surface every tool, prompt, and permission change had to be reasoned about. Keeping the narrow profile enforced host-side preserves the security boundary for imported and legacy rows without shipping a control for it (ADR 0055). | | D369 | Effective subagent thinking metadata | <em>(amended by D395)</em> <strong>The immediate <code>Task</code> result, <code>SubagentRunResult</code>, and lifecycle snapshots carry the effective <code>modelId</code> and <code>thinkingLevel</code> passed to each child run; the topology node and side-dock header show the raw canonical non-<code>off</code> level after the model name, while <code>off</code>, <code>omit</code>, and unsupported reasoning stay model-only. No host protocol, storage schema, provider request, or lifecycle behavior change.</strong> | Delegation cards need the level actually sent after the target model clamps it, not a value re-derived from the parent or the definition (ADR 0202, ADR 0221, E2E-219) |</p><p>| D395 | Canonical thinking-level values in the UI | <strong>Amend D369 / ADR 0202: Composer, model configuration, and delegation surfaces render <code>off</code>, <code>minimal</code>, <code>low</code>, <code>medium</code>, <code>high</code>, <code>xhigh</code>, and <code>max</code> directly instead of translating them. Remove these values from every locale catalog; effective metadata, clamping, provider requests, protocol, and storage remain unchanged. See ADR 0221 and E2E-219.</strong> | Thinking levels are stable protocol values, and locale-specific labels made the same provider/runtime setting vary across the application. | | D445 | Compaction summary retries and is right-sized before retained-tail recovery | <strong>Amend ADR 0049 decision 1 / D203: the automatic summary request carries a bounded pi-ai <code>RetryPolicy</code> (3 retries, 2s/4s/8s backoff, abort-aware; pi-ai classifies what is transient). The preflight guard sizes the prompt pi actually serializes (tool results capped at 2 000 chars) instead of summing raw message estimates. When that prompt still exceeds the window, exactly one reduced input is tried (tool results cut to a 500-char prefix, thinking dropped, no message removed) before the retained-tail fallback runs. <code>ContextCompactionMark</code> gains an additive <code>fallback?: &quot;retained_tail&quot;</code> derived from persisted <code>details.fallback</code>; the transcript row labels such checkpoints as a failed summary instead of <code>summary ≈N tokens</code>. No record schema, protocol version, or host-core change. See ADR 0282 and E2E-084.</strong> | Issue #543: six of ten checkpoints on a real long session were the ~112-token recovery notice. The summary request had no retry, the raw-size guard skipped summaries that would have fit, and the row presented every fallback as a successful summary. |</p><h2 id="n-notification-decisions" tabindex="-1">N. Notification decisions <a class="header-anchor" href="#n-notification-decisions" aria-label="Permalink to &quot;N. Notification decisions&quot;">​</a></h2><table tabindex="0"><thead><tr><th>ID</th><th>Topic</th><th>Decision</th><th>Rationale</th></tr></thead><tbody><tr><td>D117</td><td>Durable task notification inbox</td><td><strong>Rust host-core exclusively owns a schema-v6 <code>notifications</code> table and atomically inserts one structured <code>task.completed</code> / <code>task.failed</code> row when <code>session.endTurn</code> moves a running turn to completed/error only if Electron reports that result was not already visible. Renderer supplies the current chat session through an allowlisted viewing-context IPC and a matching prompt-dispatch snapshot; Main suppresses insertion only when its window is visible/focused and that session matches, while unknown, background, hidden, or unfocused state fails safe to notification. <code>turn_id UNIQUE</code> prevents duplicates, abort is silent, session deletion cascades, and only the newest 200 rows remain. The titlebar bell exposes exact unread count, All/Unread, row mark-read/session activation, mark-all-read, and clear with complete keyboard/accessibility behavior. Protocol v4 adds singular <code>notification.list/markRead/markAllRead/clear</code>; <code>session.endTurn</code> returns an inserted record, Electron emits renderer <code>notification.changed</code>, and only while the main window is unfocused it also shows a native system notification whose click restores/focuses the window and emits <code>notification.activated</code>. Persisted rows contain structured kind/session/turn/error data plus the session-name snapshot, never localized notification title/body prose. The task inbox has no permission, scheduled-reminder, preference, cloud-notification, or plugin-notification source; plugin-native notifications are the separate D213 surface. D113&#39;s profile footer remains unchanged.</strong></td><td>Notifications should recover task outcomes the user did not see, not duplicate a result already visible in the current chat. A bounded host-owned inbox keeps background/unfocused outcomes durable and navigable without violating SQLite ownership, duplicating events, or turning every terminal event into notification history.</td></tr></tbody></table><h2 id="o-desktop-shell-decisions" tabindex="-1">O. Desktop shell decisions <a class="header-anchor" href="#o-desktop-shell-decisions" aria-label="Permalink to &quot;O. Desktop shell decisions&quot;">​</a></h2><table tabindex="0"><thead><tr><th>ID</th><th>Topic</th><th>Decision</th><th>Rationale</th></tr></thead><tbody><tr><td>D118</td><td>Platform application menu and window chrome</td><td><strong>macOS installs a conventional system application menu and keeps hidden-inset traffic lights. Windows/Linux use the shared 46px frameless shell with localized File/Edit/View/Window/Help menus and renderer-drawn minimize/maximize-or-restore/close controls. Both menu surfaces route renderer-owned actions through a fixed <code>AppMenuCommand</code> allowlist; renderer menus route native editing/window actions through a separate fixed allowlist. Target packaging builds the local release host before Electron packaging. This adds platform-ready shell behavior but does not reverse D010: Windows/Linux release qualification remains post-MVP.</strong></td><td>A default Electron menu leaves macOS shell commands incomplete, while a frameless Windows/Linux window otherwise loses both application menus and window controls. Shared allowlists keep behavior consistent without exposing an arbitrary privileged command bridge.</td></tr><tr><td>D120</td><td>Application update delivery</td><td><em>(amended by D364 / D603)</em> <strong>Electron Main exclusively owns a fixed GitHub Releases feed, update polling, typed state, and install lifecycle. Development is disabled; packaged macOS and non-AppImage Linux use notify-and-link delivery, while Windows NSIS and Linux AppImage download in-app and install on quit. Renderer IPC cannot provide feed URLs. Automatic failures stay ambient, explicit checks surface status, and downloaded state remains actionable. The updater always forces <code>allowPrerelease = false</code> so prerelease installs (for example <code>0.2.0-rc.6</code>) track GitHub&#39;s latest stable release instead of electron-updater&#39;s default same-channel pin. D126 later publishes every platform feed produced by the tag matrix while macOS remains manual until a signed channel is qualified.</strong></td><td>Keep package installation outside the sandboxed renderer, match delivery to each installer format, and provide one consistent state across menus, Settings, and the update banner (ADR 0022). Without the stable-channel pin, RC builds never surface newer stables because electron-updater treats <code>rc</code> as a custom channel.</td></tr><tr><td>D313</td><td>Main-owned GitHub issue feedback</td><td><strong>GitHub issue forms are the only intake path. The bug form requires description, reproduction steps, expected and actual behavior, app version, and OS; the feature form requires a problem and a proposed change. Settings → Info exposes one Report a problem action on <code>pi-desktop/app/openFeedback</code>. Electron Main builds a fixed <code>bug_report.yml</code> URL, prefills <code>app-version</code> / <code>os</code> / <code>environment</code> from Main-owned version info, and opens it with <code>shell.openExternal</code>. The renderer cannot supply a URL. Feature requests stay on GitHub&#39;s template picker. No host-protocol, storage, or update-feed change (ADR 0157).</strong></td><td>Reports without version or steps cannot be triaged, and a renderer-chosen destination would weaken the same Main-owned GitHub URL rule as D120.</td></tr><tr><td>D330</td><td>Main-owned <code>openExternal</code> protocol allowlist</td><td><strong>Every <code>shell.openExternal</code> call parses the URL first. Only <code>http:</code>, <code>https:</code> (hostname plus a written <code>//</code>), and <code>mailto:</code> (non-empty address) reach the OS, as a normalized href. <code>file:</code>, <code>javascript:</code>, <code>data:</code>, and custom URI schemes throw <code>DISALLOWED_EXTERNAL_URL</code> / plugin <code>INVALID_ARGUMENT</code>. Window-open handlers deny in-app and catch the error. Workspace files keep using <code>shell.openPath</code> after the path gate (ADR 0109). Preview &quot;open in browser&quot; uses the allowlist for web/mail URLs and <code>openPath</code> for an in-root file page (ADR 0168).</strong></td><td>Unvalidated <code>setWindowOpenHandler</code> → <code>openExternal</code> lets a clicked <code>ms-msdt:</code> / <code>file:</code> / custom-scheme link invoke an OS protocol handler.</td></tr><tr><td>D332</td><td>Classified plugin file preview and live workspace events</td><td><strong>Amend ADR 0104 / 0105 / 0109 / 0111: add <code>fs.readPreview</code> under <code>fs.read</code> (text / image data URL / binary / tooLarge, same caps as the host Files tab). Broadcast panel events to docked views as well as detached windows. Deliver <code>workspace:changed</code> to panels and plugin processes. The bundled Files view restores Open with default app, search, image preview, and copy path on those public channels.</strong></td><td><code>fs.readText</code> cannot preview images or cap large binaries; docked views missed <code>appearance:changed</code>; Files polled for project switches.</td></tr><tr><td>D334</td><td>Contained in-chat image display</td><td><strong>Amend the desktop <code>fs/read</code> workspace-only clause: <code>fs/read</code>, <code>fs/reveal</code>, and <code>fs/open</code> share <code>resolveOpenablePath</code> (workspace, <code>&lt;data_dir&gt;/scratch/</code>, <code>&lt;data_dir&gt;/attachments/</code>, plus <code>attachments/&lt;sha256&gt;</code> blobs). Reads <code>realpath</code> the target. Add renderer-only <code>fs/readImageDataUrl</code> that returns a bounded image data URL or <code>missing</code> / <code>notImage</code> / <code>tooLarge</code> and never non-image bytes. A known image extension wins over client <code>mimeType</code>; extension-less blobs accept only the <code>IMAGE_MIME</code> allowlist. Chat thumbnails and local Markdown images load through that channel; click opens the host files viewer. Not a plugin API. See ADR 0172 and E2E-187.</strong></td><td>History attachments are extension-less data-root blobs the renderer origin cannot load; an unbounded absolute-path read would leak arbitrary files.</td></tr><tr><td>D336</td><td>Host-owned plugin completions and session context</td><td><strong>Amend D019: plugins may read the in-flight tool session&#39;s model-facing transcript through <code>pi.session.getLlmContext()</code> when <code>session.read</code> is granted, and may run a one-shot completion through <code>pi.agent.complete()</code> when <code>agent.complete</code> is granted. <code>pi.models.list()</code> lists ready provider/model rows (<code>models.list</code>). Credentials never leave Electron main. <code>includeSessionContext</code> requires both complete permissions and an in-flight tool session; plugins cannot pass a session id. Completions are rate-braked (8/60s) and size-capped. <code>session:modelChanged</code> is pushed after <code>session.configure</code>. The bundled first-party Advisor UX is temporarily not shipped; the public APIs remain available to explicitly installed plugins. No protocol/schema bump. See ADR 0174 and E2E-188 / E2E-189.</strong></td><td>Second-opinion tools need the user&#39;s models and the current LLM context without holding secrets or calling providers from plugin code.</td></tr><tr><td>D340</td><td>User-configurable outbound proxy</td><td><strong>Settings → General → Network exposes Proxy as System / Direct / Custom. Custom accepts http/https/socks5 URLs and a bypass list, persisted as optional <code>AppSettings.networkProxy</code>. Chromium <code>session.setProxy</code> covers the in-app browser and <code>net.fetch</code>; the agent sidecar applies an undici dispatcher (SOCKS5 CONNECT or HTTP ProxyAgent) via <code>sidecar.configure</code>; host-core marketplace curl uses <code>--proxy</code> from the stored blob and does not inherit proxy env (workspace Bash stays clean). OAuth still uses the system browser. No protocol or schema version bump. See ADR 0177 and E2E-190.</strong></td><td>Node fetch ignores the OS proxy, so Clash/V2Ray/SOCKS5 users could browse but not call models. One settings control should own app-owned HTTP.</td></tr><tr><td>D342</td><td>Import model configuration from local agent stores</td><td><strong>Amend D007: Settings → Import still never auto-imports <code>~/.pi</code> (or any other tool). An explicit Model configuration card scans Claude Code, Codex, OpenCode, Pi, and CC Switch (<code>~/.cc-switch/cc-switch.db</code>, fallback <code>config.json</code>) provider rows, lists drafts without secrets, and <code>modelConfig/importRun</code> copies API keys through <code>providers.create</code>. A live tool file that matches a CC Switch endpoint and credential is not listed twice. OAuth/subscription tokens stay in the source app. Equivalent providers (normalized base URL + API style + same credential) are skipped; different credentials at one endpoint remain independent rows (ADR 0188). If no global default model exists, the first newly created provider becomes it. Electron IPC only; no host protocol or schema version bump. See ADR 0179, ADR 0188, and E2E-209.</strong></td><td>Users already import sessions from those stores and otherwise retype the same endpoints and keys on the Models page.</td></tr><tr><td>D351</td><td>Preserve distinct credentials during model configuration import</td><td><strong>Amend D342 / ADR 0179: an imported provider is equivalent only when normalized endpoint, API style, and credential all match. Same-endpoint profiles with different API keys create independent provider rows and remain selectable in Composer. Electron main resolves existing API keys through the host secret boundary; secrets never reach the renderer, logs, or provider metadata. The live-file-versus-CC-Switch scan uses the same credential-aware rule. No host protocol or storage schema version bump. See ADR 0188 and E2E-209.</strong></td><td>CC Switch stores multiple accounts at one gateway endpoint; endpoint-only idempotence silently dropped all but the first profile during import.</td></tr><tr><td>D344</td><td>Main-owned picker capabilities</td><td><strong>Amend D197 / D334: composer native file and image pickers keep selected absolute paths in Electron main behind a short-lived, sender-bound, one-shot token. <code>composer/importFiles</code> accepts only that token and a durable session id; the renderer never supplies source paths. The MVP file picker offers regular files only, so unsupported directory selection and its misleading UI copy are removed. Main retains realpath, regular-file, session-scratch, and size-limit validation. See ADR 0181 and E2E-102h.</strong></td><td>Renderer IPC is not a user-click gate: accepting arbitrary absolute paths in the import payload creates a local file exfiltration primitive. Folder import needs separate bounded traversal and symlink semantics.</td></tr><tr><td>D314</td><td>Shipped locale registry and language picker</td><td><strong>Amend D073: UI locales are listed in <code>@pi-desktop/i18n</code> (<code>en</code>, <code>zh-CN</code>, <code>zh-TW</code>, <code>tr</code>). Native names stay untranslated. Settings → General language is a searchable picker (Auto + registry), not three preview cards. Plugin labels keep their <code>en</code> + <code>zh-CN</code> contract with English fallback for shell locales without a plugin translation; the product changelog follows every shipped product locale. See ADR 0160, ADR 0182, and E2E-091.</strong></td><td>Preview cards cannot scale past two languages; a registry lets additional locales ship without rewriting the Appearance card.</td></tr></tbody></table><p>| D345 | Traditional Chinese shell locale | <strong>Amend D314 / ADR 0160: ship <code>zh-TW</code> as an independent full shell catalog with native name 繁體中文. <code>zh-TW</code>, <code>zh-Hant</code>, <code>zh-HK</code>, and <code>zh-MO</code> resolve to the Traditional Chinese shell; generic <code>zh</code> and Simplified Chinese regions continue to resolve to <code>zh-CN</code>. Persisted <code>AppSettings.language</code> includes <code>zh-TW</code>, Electron packages both <code>zh-TW</code> and <code>zh_TW</code> locale directories, and the in-app changelog includes a matching <code>zh-TW</code> catalog so release notes follow the active Traditional Chinese shell. No host protocol or storage schema change.</strong> | Traditional Chinese users need an independent shell and release-note language; reusing the Simplified Chinese catalog makes Auto detection and visible terminology incorrect. | | D346 | P0 international shell locales | <strong>Amend D314 / ADR 0160 / ADR 0182: ship complete <code>de</code>, <code>es</code>, and <code>fr</code> shell catalogs with native names Deutsch, Español, and Français. <code>de-*</code>, <code>es-*</code>, and <code>fr-*</code> resolve to their shipped base catalogs; persisted <code>AppSettings.language</code> accepts the three ids; matching changelog catalogs keep release notes in the active locale. No host protocol, storage schema, or IPC version change.</strong> | Spanish, French, and German provide the highest-value missing P0 international coverage while the registry and searchable picker already scale to additional locales. | | D349 | Korean shell locale | <strong>Amend D314 / ADR 0160 / ADR 0183: ship a complete <code>ko</code> shell catalog with native name 한국어 and English name Korean. <code>ko</code> and <code>ko-*</code> resolve to the Korean catalog; persisted <code>AppSettings.language</code> accepts <code>ko</code>; Electron packages the Korean Chromium locale; and a matching Korean changelog keeps release notes in the active locale. No host protocol, storage schema, or IPC version change.</strong> | Korean users need a distinct complete shell and release-note language, while one base catalog preserves the existing searchable locale contract for regional Korean variants. | | D350 | Focus-aware native task notifications | <strong>Amend D117 / ADR 0107: <code>notification/showNative</code> carries <code>kind</code> as <code>task</code> or <code>interactive</code>. Omitted or unknown values default to <code>task</code>. Task banners are suppressed whenever the main window is visible and focused, including a focused background session. Interactive prompts are suppressed only for the exact visible focused session. Interactive prompts never create a durable task inbox row. Plugin notifications stay on their permission-gated path. No host protocol or storage schema change. See ADR 0187 and E2E-065 / E2E-065a.</strong> | Task completions and interactive asks share one native channel but need different focus rules. The field is <code>kind</code>; a colliding <code>source</code> name is not part of the contract. | | D358 | Show provider retry causes in the active-turn status | <strong>Amend ADR 0175: <code>AgentActivity.retrying</code> may carry bounded, already-redacted error details. The compact retry row stays at rest; hover or keyboard focus reveals the localized summary, stable code/status, and provider message. Intermediate retries never become transcript error rows. See ADR 0196 and US-UI-60d.</strong> | Users need the current retry cause without duplicating the final assistant error. | | D359 | Summarize first-turn session titles with a main-owned one-shot | <strong>Keep the first-prompt fallback synchronous. After <code>agent_end</code>, the renderer calls allowlisted <code>session/summarizeTitle</code>. Electron resolves the session model and runs a thinking-disabled one-shot; a valid result persists through <code>session.rename</code>. Automatic replacement refuses a persisted <code>manualTitle</code> and any title that is neither a default nor the first-prompt fallback. No host schema change. See ADR 0186 and E2E-021a.</strong> | A truncated first prompt is a poor sidebar label, but title generation must not block the turn or expose credentials to the renderer. | | D361 | Inline image transport uses a 10 MB app-side bound | <strong>Amend ADR 0101 / D197: <code>MAX_INLINE_IMAGE_BYTES</code> is 10,000,000. Vision-capable models receive images at or below that decimal 10 MB bound as transient base64; larger images and non-vision models keep the <code>@path</code> fallback. Electron main and the sidecar share the constant so replay cannot take a different path. No protocol or schema change.</strong> | MiniMax&#39;s OpenAI-compatible endpoint accepts one image up to 10 MB; a higher app bound produced provider rejections after the composer had already inlined the bytes. | | D316 | Searchable theme picker | <strong>Amend ADR 0160: Settings → General theme is a searchable picker row (same anchored-menu pattern as Language), not three preview cards. System / Light / Dark stay pinned at the top; plugin themes follow after a divider. <code>AppSettings.theme</code> and plugin-theme fallback are unchanged. See ADR 0161 and E2E-091.</strong> | Plugin themes wrap a three-column card grid, and Language already solved the growing-list control. | | D121 | Branded macOS development host | <strong><code>pnpm dev</code> on macOS launches electron-vite through a fingerprinted, ad-hoc-signed PI-Desktop copy of the installed Electron host bundle under <code>.cache/electron-dev/</code>. The generated bundle changes only development host metadata, executable name, bundle identifier, and the ICNS resource; it never mutates <code>node_modules</code>. Windows/Linux keep the stock development executable, while packaged lanes remain electron-builder-owned.</strong> | AppKit ignores runtime app-name/menu overrides for the top-level application identity and takes the native menu name and About icon from the host bundle; a branded development host is required for parity with packaged PI-Desktop. | | D129 | Menu-free Windows/Linux window chrome | <strong>The application menu is a macOS system-menu surface only. Windows/Linux retain the shared frameless 46px titlebar and renderer-drawn minimize/maximize-or-restore/close controls, but render no File/Edit/View/Window/Help menu inside the window and reserve no left-side titlebar space for one. Existing application, editing, zoom, fullscreen, and close shortcuts remain available through renderer/native web-content handling; update checks remain reachable from Settings -&gt; Info. This supersedes only the Windows/Linux renderer-menubar portions of D118 and ADR 0021.</strong> | An in-window desktop menu duplicates macOS-specific system-menu chrome, consumes navigation space, and does not belong in PI-Desktop&#39;s frameless Windows/Linux titlebar. | | D130 | Sidebar-footer notification entry | <strong>The durable notification Bell moves from the main titlebar to the separate <code>32px</code> action at the right of the expanded sidebar footer, replacing D113&#39;s Help shortcut. Its unread badge and complete D117 inbox behavior remain unchanged; the popover opens above and to the right of the footer, and no duplicate Bell remains in the main titlebar. This supersedes only the entry-location clauses of D113 and D117.</strong> | Notification history belongs with the persistent local profile controls and the footer position keeps the main titlebar quiet while preserving a compact, familiar status entry. | | D141 | Canonical Windows native application identity | <em>(amended by D443)</em> <strong>Electron Main sets the product name before readiness and registers <code>com.pi-desktop.app</code> as the Windows process AppUserModelID before creating any window. That ID is the existing electron-builder/NSIS application ID; Windows packaging explicitly retains <code>PI-Desktop</code> for the executable and Start menu shortcut. Native notification attribution, notification settings, taskbar grouping, installed shortcuts, and packaged executable identity must expose <code>PI-Desktop</code>, not the stock Electron host. D121 remains unchanged: Windows development may use the stock Electron executable while its OS-facing runtime identity uses the canonical AUMID.</strong> | <code>app.setName</code> changes Electron&#39;s internal name but not the Windows identity used by notifications and shell integration. One stable ID across runtime and packaging prevents both the observed notification-source leak and adjacent shell-brand drift without changing the published NSIS upgrade identity. | | D204 | Empty home task-entry surface | <em>(supersedes D111&#39;s non-docked home-composer clause and its contextual quick-action clauses; starter-grid amendment in D205 is superseded by D206)</em> <strong>The empty chat home keeps the restrained hero and optional first-run checklist in the scrollable content region. The home composer is a bottom-reserved sibling of the scroller, remains visible at the bottom of the chat surface, and never covers the content.</strong> | The direct composer remains the stable primary action and the flow layout preserves checklist reachability in short windows (ADR 0066). | | D205 | ChatGPT-inspired empty-home guidance | <em>(superseded by D206)</em> <strong>The empty chat home adds a compact four-card developer starter grid between the hero and optional checklist: Explore a codebase, Build a feature, Fix a bug, and Review a change. Each localized card only prefills and focuses the bottom composer; it never sends a prompt or creates a turn. The bottom-reserved composer and single scrollable home flow from D204 remain unchanged.</strong> | The previous hero-only middle left too much unused space and offered no starting cues. ChatGPT&#39;s clear empty-state hierarchy improves first-task discoverability while developer-specific prompts keep the surface purposeful rather than promotional. | | D206 | Remove empty-home developer starter cards | <strong>The empty chat home does not render developer starter cards, starter glyphs, or a contextual quick-action row. It keeps the restrained hero, short supporting line, optional onboarding checklist, and D204&#39;s bottom-reserved composer; task entry starts directly in the composer. This supersedes D205 without changing D204&#39;s scroll and bottom-reservation layout.</strong> | Review confirmed that the direct composer is the preferred task-entry surface and that the cards add an unnecessary decision layer to the empty home. | | D208 | Recoverable native-tool path contracts | <strong>Keep D185&#39;s deferred Glob/Grep boundary, but make every prompt and schema explicit that Read accepts an existing regular file, Glob accepts a directory, and Grep accepts a file or directory. A directory Read returns <code>INVALID_ARGUMENT</code> plus structured Glob recovery args; an explicit-file Grep searches only that file and applies <code>include</code> to its basename. Tool errors remain visible on their ToolCallRows, while activity groups report processing duration only and never infer terminal turn failure from a child row; terminal agent events and the dedicated outcome surfaces remain authoritative (ADR 0069).</strong> | Durable sessions showed directory Read and file-as-directory Grep mistakes repeatedly, then displayed recovered work as terminally failed. Compatibility at the narrow host boundary plus one outcome owner removes retries and false failure UI without restoring every search schema to the Agent core. | | D216 | Cross-platform tray-resident minimize | <em>(Windows taskbar clause amended by D252 / ADR 0117; explicit Windows/Linux action clauses amended by D256 / ADR 0123)</em> <strong>Electron Main creates one packaged-resource tray icon on macOS, Windows, and Linux. Explicit application minimize actions and macOS/Linux native minimize hide the window without disposing the host or sidecar; Windows&#39; native taskbar minimize remains an ordinary taskbar-visible OS minimize. Tray click/double-click, Show, and macOS app activation restore and focus the existing window (or create one if it was closed). The localized tray menu exposes Show PI-Desktop and an explicit Quit PI-Desktop action.</strong> | Users need background work to continue without losing the app window, while Windows users also expect the active taskbar button to retain its normal minimize/restore behavior. Main-owned tray lifecycle avoids renderer privilege expansion and keeps explicit exit observable. | | D218 | Host-owned cross-platform plugin panel chrome | <strong>Plugin panel windows adopt the main window&#39;s 46px platform chrome: macOS uses <code>hiddenInset</code> with traffic lights at <code>{x:16,y:16}</code>, while Windows/Linux are frameless with a 112px custom minimize/maximize-or-restore/close band. The sandboxed plugin preload renders the manifest title and controls in a closed Shadow DOM, offsets content by the titlebar height in addition to existing top padding, and consumes a private sender-validated fixed window-action channel; <code>window.pluginBridge</code>, the per-plugin partition, and host protocol v9 do not expand. Reopening a minimized panel restores and focuses it.</strong> | Default Electron frames made plugin tools look detached from PI-Desktop and varied by platform. Preload-owned chrome provides parity without moving untrusted plugin HTML into the host renderer or exposing general Electron window authority (ADR 0081). | | D234 | Plugin-owned panel surface with a host window-control capsule | <strong>Plugin panels use frameless windows on macOS, Windows, and Linux. The sandboxed preload reserves a transparent 46px safe area and renders only one fixed top-right capsule with exactly three buttons — minimize, maximize/restore, and close — in a closed Shadow DOM. It no longer renders a panel title or development reminder. The plugin owns its title, toolbar, background, and all other visible UI; normal-flow content is offset automatically, fixed/sticky UI uses <code>--pi-plugin-titlebar-height: 46px</code>, and plugin-owned drag regions use <code>-webkit-app-region: drag</code> with interactive controls opting out. The private sender-validated control channel, localized labels, page-adaptive colors, <code>window.pluginBridge</code>, per-plugin partition, host protocol v9, and storage schema remain unchanged. This amends D218 / ADR 0081 and D232 / ADR 0082.</strong> | The host titlebar consumed the plugin&#39;s visual hierarchy and still made macOS different from Windows/Linux. A compact host capsule preserves safe native window actions while leaving the panel surface to the plugin. | | D235 | Strict 46px plugin drag band with a minimal capsule | <strong>Plugin panels reserve exactly 46px for a transparent host drag band on every platform. Normal-flow content is offset by that value, the band blocks plugin clicks outside the capsule, and development panels show a localized reminder that the top 46px is drag-only. The host renders no panel title. The fixed top-right capsule stays inside the band, contains exactly minimize, maximize/restore, and close, and uses a subtle page-adaptive surface without heavy shadow or blur. This amends D234 / ADR 0092; the private sender-validated control channel, closed Shadow DOM, <code>window.pluginBridge</code>, protocol v9, and storage schema remain unchanged.</strong> | Frameless panels need a reliable drag target and a clear authoring constraint, but the host band must not grow or compete with the plugin&#39;s visual hierarchy. | | D343 | Custom global UI type scale | <strong>Settings → General → Appearance gains a Font size row under Font. Starbucks-style cup presets (Tall / Grande / Venti / Trenta) plus a percentage slider persist as optional <code>AppSettings.fontScale</code> (<code>1</code> = product ramp, absent = 1, range 0.8–1.5 in 0.025 steps). The renderer sets <code>--font-scale</code> on the root; every <code>--text-*</code> token and <code>--leading-row</code> is <code>calc(&lt;product px&gt; * var(--font-scale))</code>, and shared Lucide wrappers size glyphs with the same multiplier, so body, chrome, headings, code, and icons stay in proportion without a reload. The UI never asks for a px value. Window Zoom In/Out/Reset stays independent. An unreleased leftover <code>fontSize</code> px field migrates as <code>px / 14</code> when <code>fontScale</code> is absent. No host protocol or storage schema version bump.</strong> | The <code>--text-*</code> ramp is a frozen set of relative sizes; a single multiplier scales every step at once. Window zoom still scales layout. A reading-only px field would leave two type sizes in one window and force the user to pick a number that only matches <code>--text-base</code> (ADR 0180). | | D232 | Custom global UI font | <em>(amended by D598)</em> <strong>Settings → Basics → Appearance gains a searchable Font picker (trigger previews the current family). Selections persist as <code>AppSettings.fontFamily</code>, a CSS stack; absent means the built-in <code>--font-sans</code> token stack, and the renderer applies the stack by overriding <code>--font-sans</code> on the root element without a reload. Four bundled families — Geist, Inter, Noto Sans SC, LXGW WenKai — ship locally as woff2 under the SIL OFL 1.1 with license texts; every custom stack appends a CJK fallback tier and the mono stack is unchanged. Installed system families are enumerated by Electron main using platform tooling only (macOS: <code>osascript</code> JXA bridging the CoreText query <code>CTFontManagerCopyAvailableFontFamilyNames</code> — the same API dbx&#39;s <code>font_kit::all_families()</code> calls — with <code>system_profiler</code> as a slow fallback; Windows: PowerShell; Linux: <code>fc-list</code>), deduplicated/sorted/filtered and cached 60 s, exposed through the additive allowlisted channel <code>pi-desktop/app/systemFonts</code>; host protocol v9 and storage schema v10 are unchanged.</strong> | Users want a Codex/dbx-style global font preference, but the sandboxed renderer cannot enumerate OS fonts and the host RPC should stay unchanged for a renderer-only preference. Bundling OFL-licensed families keeps every offered font commercially safe and offline, while the fast CoreText path returns the canonical CSS family names (e.g. PingFang SC) in tens of milliseconds and the 60 s cache bounds repeated enumeration (ADR 0083). | | D598 | The app ships no fonts | <strong>Amend D232 / ADR 0083: every bundled family is removed. <code>apps/desktop/src/assets/fonts/</code> (the four <code>woff2</code> files and their OFL license texts), <code>apps/desktop/src/styles/fonts.css</code>, and the <code>@import &quot;./fonts.css&quot;;</code> line in <code>styles/globals.css</code> are deleted, and <code>BUNDLED_FONTS</code>, the <code>BundledFont</code> type, <code>FontOption.license</code>, the picker&#39;s license badge, and <code>settings.fontBundled</code> in all eight locale catalogs go with them. The picker keeps System default (persisted as the empty string) followed by installed system families, enumerated exactly as before through the allowlisted <code>pi-desktop/app/systemFonts</code> channel, and a stored stack that matches no option still appears first under Saved (<code>custom</code>). Every generated stack now appends the system-only CJK tier <code>&quot;PingFang SC&quot;, &quot;Hiragino Sans GB&quot;, &quot;Microsoft YaHei&quot;, sans-serif</code> with no bundled family in front of it. A previously stored bundled stack (e.g. <code>&quot;Geist&quot;, &quot;PingFang SC&quot;, …</code>) is preserved byte for byte, shows under Saved, and renders through that tier. No protocol version, schema version, or storage migration change.</strong> | Four OFL faces duplicated coverage every target OS already provides, cost about 15.7 MiB of <code>woff2</code> in <code>out/renderer</code> (7.6 MiB LXGW WenKai plus 7.4 MiB Noto Sans SC), and carried font-file notice obligations for four options beside the list the OS already installs. See ADR 0298, E2E-126, and E2E-092. | | D230 | User-configurable close behavior with close-to-tray | <strong>Windows/Linux close behavior is a persisted preference stored by Electron main in <code>&lt;data&gt;/close-behavior.json</code>. <code>ask</code> is the transient unset state: the first close shows a native modal (Cancel / Close to tray / Quit); picking one persists it forever, Cancel keeps the window open and unset. Only <code>tray</code> and <code>quit</code> are ever settable — Settings -&gt; General renders a two-option radio segment (Close to tray / Quit app) for Windows/Linux only, and <code>pi-desktop/window/closeBehavior/set</code> rejects <code>ask</code>, so a choice can be switched but never reverted to prompting. <code>tray</code> hides the window under the resident D216 tray icon (click restores, menu shows or quits); switching to <code>quit</code> leaves that icon in place, because minimize-to-tray still needs it. Close interception runs for every non-macOS close that is not already an approved quit; <code>quitting</code> and macOS closes fall through, a <code>quit</code> close calls <code>app.quit()</code> itself, and <code>window-all-closed</code> stays silent only under <code>tray</code>, so the boot probe and explicit quits are unaffected and a resident tray never keeps a <code>quit</code> session alive. <code>closeBehavior/set</code> also fails with <code>INVALID_ARGUMENT</code> on macOS. The bounds watchdog skips minimized and hidden windows so a tray-hidden window is never force-restored. macOS keeps the native Dock lifecycle (D216, ADR 0078, ADR 0090).</strong> | Minimizing already hides the window into the D216 tray; the gap was close: it quit outright on Windows/Linux. A fixed close-to-tray would surprise users who expect exit, so the choice is asked once, remembered, and revisitable in Settings — matching how Codex-style shells keep long-running sessions alive without taking over the close button. | | D363 | Explicit quit confirms before shutdown | <strong>Amend D230 / D216: Cmd+Q, the application-menu Quit item, and the tray Quit item show one native warning (Cancel / Quit). Cancel leaves the app running and allows a later quit to prompt again. Confirm runs the ordered <code>before-quit</code> shutdown. A window-close path that already chose Quit in the D230 dialog sets <code>quitConfirmed</code> and does not ask again. Boot, supervision, and capture probes skip the dialog, and so does the quit an in-app update performs: <code>autoUpdater.quitAndInstall()</code> spawns the platform installer before <code>app.quit()</code>, and that installer aborts once the app outlives its wait window, so the update restart must reach the ordered shutdown without a prompt. No IPC, storage, or host-protocol change. See E2E-204.</strong> | An explicit quit tears down host-core, the sidecar, and in-flight work; a one-step confirm prevents an accidental Cmd+Q or tray Quit from doing that, without re-prompting a user who already chose Quit on the close-behavior dialog. | | D252 | Windows taskbar preserves native minimize/restore | <em>(Explicit renderer/menu minimize clauses superseded by D256 / ADR 0123)</em> <strong>Amend D216 / ADR 0078: when the focused Windows main window is toggled from its taskbar button, Electron lets the native <code>minimize</code> transition complete instead of hiding the window. The taskbar entry remains available and the next taskbar click restores/focuses it. Explicit renderer/menu minimize actions still hide to the resident tray; macOS/Linux native minimize remains tray-resident. No IPC, storage, host protocol, or background-work changes.</strong> | D216&#39;s unconditional <code>window.hide()</code> handled the taskbar-originated Windows <code>minimize</code> event as a tray hide, unexpectedly removing the taskbar entry and leaving the user with only the tray restore path. | | D236 | One desktop instance per data directory | <strong>Electron main takes <code>app.requestSingleInstanceLock()</code> during module evaluation — after <code>app.setName</code>, because the lock lives under the name-derived <code>userData</code> path, and before the logger, the persistence outbox, or any other data-directory access. A launch that does not take the lock calls <code>app.quit()</code> and boots nothing: the readiness and <code>before-quit</code> handlers both return early, so it creates no window, tray, child process, or log line inside the running instance&#39;s data directory. The lock holder answers <code>second-instance</code> by restoring and focusing its main window through the same path as the tray&#39;s Show action, which recreates a window that was closed or hidden into the tray. The lock is requested only when <code>PI_DESKTOP_DATA_DIR</code> is unset, so E2E harnesses, the capture rig, and deliberate side-by-side profiles keep the current start-anytime behavior. No IPC channel, host protocol, or storage schema changes.</strong> | A second launch booted a complete second app: another host-core over the single-writer <code>pi.sqlite</code> (D002), another outbox and log tree in the same directory, another tray, launcher-chord registration, and updater, leaving two shells divergent over one database. Relaunching the app is a request to see the one already running, and scoping the lock to the installation keeps isolated-data-directory runs launchable (ADR 0094). | | D367 | Host-owned plugin session import and ownership API | <strong><code>pi.session</code> gains <code>import</code>, <code>importBatch</code>, <code>list</code>, <code>get</code>, <code>listMessages</code>, <code>rename</code>, and <code>delete</code>; <code>contributes.sessionSources</code> is required for every source; host-core generates ids and scopes every operation to <code>(pluginId, source, externalId)</code> ownership; imports never activate project/provider/model bindings. Schema v14 adds <code>session_import_origins</code> and <code>sessions.deleted_at</code>; protocol v11 is unchanged. Trash keeps the transcript for owner-only purge.</strong> | External-history plugins need durable import/read/update/delete, and the in-flight <code>session.getLlmContext</code> and core <code>session.import</code> boundaries are not safe plugin ownership boundaries (ADR 0200, E2E-214 / E2E-215) | | D368 | Explicit plugin project ids and host-owned session refresh | <strong>Permission-gated <code>pi.project.create({ path })</code> creates or reuses a project and returns its host-generated id without activating the workspace; import items may bind an existing <code>projectId</code>, omitted ids stay unbound, and historical origin paths never become tool roots. Electron main emits <code>pi-desktop/session/event/changed</code> after successful plugin imports, renames, and deletes; the renderer reuses <code>refreshSessions()</code>, skipped imports emit nothing, and closed project tabs are not reopened.</strong> | Exposing <code>workspace.set</code> to plugins would change the user&#39;s active workspace, and plugin writes need a host-owned renderer refresh path (ADR 0201, E2E-216) | | D370 | Local MCP control plane for desktop operations | <em>(amended by D372)</em> <strong>An opt-in Streamable HTTP MCP server inside Electron Main binds to <code>127.0.0.1</code>, authenticates with a persistent random bearer token, validates supplied Origins against loopback hostnames, writes a mode-restricted connection manifest, and delegates to the same registered IPC handlers the renderer uses. Named tools cover the project/session/Agent flow; <code>pi_desktop_invoke</code> reaches a risk-tagged operation catalog; dangerous operations require <code>confirm: true</code>; successful external mutations reuse the renderer session-change event. Local-only and fail-soft at startup.</strong> | An external Agent needs to drive the running desktop without a second permission or persistence implementation and without touching the deferred remote Gateway / WebUI boundary (ADR 0203, E2E-220) | | D372 | Tightened local MCP control-plane boundary | <strong>Amend D370 / ADR 0203: the first-version catalog is the project/session/Agent/workspace flow plus reviewed reads; secret-shaped arguments are stripped; <code>session/configure</code> is dangerous; listen addresses are asserted loopback; protocol versions are negotiated rather than echoed; results including <code>structuredContent</code> are bounded; renderer session refresh is mutation-only.</strong> | The first catalog still exposed native pickers, secret-write provider/OAuth/MCP paths, and unconfirmed <code>session/configure</code>, and treated session reads as renderer mutations (E2E-220) | | D377 | Plugin desktop control requires native user consent | <strong>A <code>dangerous</code> desktop operation invoked through <code>pi.desktop.invoke</code> needs the plugin&#39;s <code>confirm: true</code> and then the user&#39;s answer to a host-owned native dialog that shows only the catalog operation id, description, and a bounded argument preview; dismissal, Deny, and a host without a dialog service fail with <code>PERMISSION_DENIED</code>. <code>read</code>/<code>write</code> operations and the MCP contract (D372) are unchanged.</strong> | The controller&#39;s <code>confirm</code> flag is set by the caller, so a plugin (or a model behind it) could delete sessions or switch a session to <code>auto</code> with no human in the loop, and any plugin-drawn confirmation card can be relabeled by a prompt-injected transcript (ADR 0208, E2E-236) |</p><h2 id="p-transcript-storage-decisions" tabindex="-1">P. Transcript storage decisions <a class="header-anchor" href="#p-transcript-storage-decisions" aria-label="Permalink to &quot;P. Transcript storage decisions&quot;">​</a></h2><table tabindex="0"><thead><tr><th>ID</th><th>Topic</th><th>Decision</th><th>Rationale</th></tr></thead><tbody><tr><td>D119</td><td>Transcript file store; SQLite index-only</td><td><strong>Schema v7: message content moves out of SQLite into per-session JSONL files under <code>~/.pi-desktop/sessions/</code> — <code>&lt;id&gt;.jsonl</code> (a session-header line, then one canonical block-array message line per message, RFC3339 stamps) plus an append-only <code>&lt;id&gt;.revisions.jsonl</code> for regenerate branches. <code>messages</code> drops <code>content_json</code>/<code>meta_json</code> and becomes a pure index (ordering, promoted filter columns, extracted <code>text</code> feeding FTS); <code>message_revisions</code> swaps <code>messages_json</code> for <code>message_count</code>, with <code>is_active</code> tracked in the DB only. Writes are file-first then index transaction; reads skip unknown/torn lines and dedupe repeated message ids keep-last; full rewrites are temp-file + atomic rename; session files are deleted only with their session and never age/orphan-swept. Opening a pre-v7 database archives it as <code>pi.sqlite.v6.bak</code> and bootstraps fresh — an explicit breaking reset, with all v1–v6 migration code removed. RPC wire format is unchanged, so Electron/renderer/importers need no changes.</strong></td><td>The database grew without bound carrying tool args/results and thinking payloads; codex/claude-code-style per-session files keep transcripts human-readable, greppable, and portable while SQLite stays a small, fast index (list, search, badges). A dev-phase breaking reset was chosen over migration machinery.</td></tr><tr><td>D122</td><td>Independent conversation session fork</td><td><strong>Protocol v5 adds host-owned <code>session.fork</code>: an idle source&#39;s complete active canonical transcript is copied into a new independent session with remapped message/tool-call ids and inherited project/provider/model/mode/thinking/permission configuration. Turns, regenerate revisions, notifications, artifacts, session grants, arbitrary scratch outputs, runtime state, pin state, and parent-child lineage are not copied. Referenced existing pasted/imported inputs are copied into child-owned scratch files and their transcript/checkpoint paths are remapped (ADR 0023). Create branch activates the child; D109 remains unchanged because no message-level branch tree is introduced.</strong></td><td>A single host-owned snapshot preserves canonical blocks and persistence consistency while giving users a Codex-style divergence workflow without conflating independent conversations with regenerate variants.</td></tr><tr><td>D134</td><td>Assistant response fork and reversible edit</td><td><em>(edit clause superseded by D137)</em> <strong>The completed-assistant toolbar exposes Copy, Fork, Edit, and Regenerate but no Delete. Fork calls the existing host-owned <code>session.fork</code> with optional <code>throughMessageId</code>, producing an independent session whose canonical transcript ends at that response. Edit uses the same isolated child, replaces only the selected assistant text there, and stores original/edited tails as a two-entry D109 revision family so the existing pager can restore either. Both require an idle source, remap message/tool-call ids, and never share the source session id, runtime, transcript, revisions, or provider cache state.</strong></td><td>Response-level divergence and correction should remain reversible without mutating the source or letting an edited history reuse cached runtime state built from different assistant content.</td></tr><tr><td>D199</td><td>Regenerate branch archived under the host RPC lock</td><td><strong><code>session.saveActiveRevision</code> performs the read, the branch archive, and the <code>revisionCount</code> / <code>activeRevision</code> stamp in one host call under the state lock, replacing Electron main&#39;s <code>session.get</code> + <code>session.replaceMessages</code> read-modify-write. The stamp rewrites only the root user&#39;s transcript line and re-reads the file at write time, so a line appended meanwhile survives; Electron main drains the persistence outbox first and skips the archive with a warning rather than archiving an incomplete branch. <code>session.replaceMessages</code> carries each surviving message&#39;s owning <code>turn_id</code> across a rewrite and is documented as safe only for a caller that owns the whole transcript for the call&#39;s duration (ADR 0060).</strong></td><td>Assistant and tool messages reach SQLite asynchronously through the ADR 0041 outbox, so the renderer-side snapshot could predate the turn&#39;s final message — and the whole-transcript rewrite then deleted it from both the transcript file and the index, along with every row&#39;s <code>turn_id</code>. Only the host can read and write the transcript atomically.</td></tr></tbody></table><h2 id="q-still-deferred" tabindex="-1">Q. Still deferred <a class="header-anchor" href="#q-still-deferred" aria-label="Permalink to &quot;Q. Still deferred&quot;">​</a></h2><ol><li>Exact marketplace domain / provider IDs</li><li>Private marketplace auth mechanism</li><li>Signature key distribution operational details</li><li>Remote catalog update channel details (URL/signature)</li><li>Exact recommended default model per vendor preset</li></ol><p>The full open list lives in <a href="./open-questions">open-questions.md</a>; this section mirrors only marketplace/catalog items still blocking nothing.</p><h2 id="u-settings-rail-iconography" tabindex="-1">U. Settings rail iconography <a class="header-anchor" href="#u-settings-rail-iconography" aria-label="Permalink to &quot;U. Settings rail iconography&quot;">​</a></h2><table tabindex="0"><thead><tr><th>ID</th><th>Topic</th><th>Decision</th><th>Rationale</th></tr></thead><tbody><tr><td>D143</td><td>Settings directory rail icons</td><td><strong>The five settings destinations use fixed Lucide glyphs: Basics=<code>SlidersHorizontal</code>, Model configuration=<code>Bot</code>, Import=<code>Download</code>, Project archive=<code>Archive</code>, Info=<code>Info</code>. Refresh/rotate glyphs are not used on this rail.</strong></td><td>Prior mapping reused Settings/RefreshCcw/RotateCw, which read as generic gear/reload rather than the destination semantics; monochrome Lucide keeps the compact directory scannable.</td></tr><tr><td>D166</td><td>Settings directory split into AI and Shortcuts destinations</td><td><strong>The Settings rail grows from five to seven destinations in order: Basics (<code>SlidersHorizontal</code>), 全局 AI/AI (<code>Sparkles</code>, new), Shortcuts (<code>Keyboard</code>, new), Model configuration (<code>Bot</code>), Import (<code>Download</code>), Project archive (<code>Archive</code>), Info (<code>Info</code>). Permissions and Context management move from Basics to the new AI destination; Keyboard shortcuts moves from Basics to the new Shortcuts destination; Developer moves from Basics to Info. Basics keeps only Appearance and Defaults. This supersedes the five-destination count/order of D133 and the five-icon set of D143 (adding <code>Sparkles</code> and <code>Keyboard</code>) without changing any setting&#39;s semantics, the full-page shell, or rail metrics.</strong></td><td>Basics accumulated six unrelated cards, burying global AI behavior (permission mode, context compaction) and shortcut configuration alongside look-and-feel; splitting them gives each concern a scannable home while keeping provider/connection config separate in Model configuration.</td></tr></tbody></table><h2 id="r-decision-rules-going-forward" tabindex="-1">R. Decision rules going forward <a class="header-anchor" href="#r-decision-rules-going-forward" aria-label="Permalink to &quot;R. Decision rules going forward&quot;">​</a></h2><ul><li>Architecture-boundary changes require a new ADR</li><li>Implementation defaults can be updated in this log + related specs</li><li>Any reversal of D001–D010 requires explicit baseline bump</li></ul><h2 id="s-composer-input-decisions" tabindex="-1">S. Composer input decisions <a class="header-anchor" href="#s-composer-input-decisions" aria-label="Permalink to &quot;S. Composer input decisions&quot;">​</a></h2><table tabindex="0"><thead><tr><th>ID</th><th>Topic</th><th>Decision</th><th>Rationale</th></tr></thead><tbody><tr><td>D123</td><td>Composer slash commands, three sources</td><td><strong>Typing <code>/</code> at position 0 of the composer opens an inline command menu merging (a) pi prompt templates from <code>&lt;workspace&gt;/.pi/prompts/*.md</code> and <code>~/.pi/agent/prompts/*.md</code> (project overrides user-global on name conflict; frontmatter <code>description</code>/<code>argument-hint</code>), (b) builtin palette commands through slash aliases defined in one registry shared with palette search, and (c) plugin palette commands. On send, builtin/plugin invocations execute locally through the existing renderer switch / <code>commandPalette/execute</code> without creating a session or prompting the model; template invocations are expanded in the Electron main <code>agent/prompt</code> handler before persistence (<code>parseCommandArgs</code> + <code>substituteArgs</code> from pi-agent-core), persisting <code>content = expanded</code> plus a new optional <code>command</code> field carrying the typed form; unknown <code>/foo</code> is sent as literal text.</strong></td><td>Reuses pi&#39;s exact CLI semantics and template assets, keeps agent reseed faithful (reseed replays <code>content</code>), and keeps the transcript readable by rendering the typed invocation as a chip (ADR 0024).</td></tr><tr><td>D124</td><td><code>@</code> file references are plain-text light references</td><td><strong><code>@</code> at a token boundary opens a fuzzy file menu over a workspace index served by the new Electron-only channel <code>pi-desktop/fs/index</code> (files+dirs, <code>git ls-files -co --exclude-standard</code> fast path with ignore-set walk fallback, 8000-entry cap with truncation flag, short TTL cache, workspace-rooted, fails soft without a workspace). Accepting creates the canonical <code>@rel/path </code> reference for files (quoted <code>@&quot;a b.txt&quot; </code> when the path has spaces) and inserts <code>@dir/</code> without trailing space for directories; D209 amends completed-file draft presentation without changing the dispatched plain-text path. The model follows references with its Read tool in both Plan and Agent. No prompt content inlining or provider attachment conversion; clipboard file materialization is defined by D197.</strong></td><td>Matches pi CLI&#39;s model-facing semantics, avoids context inflation and truncation rules, and keeps user-driven file browsing out of the agent tool/permission path per ADR 0019.</td></tr><tr><td>D125</td><td>Composer autocomplete interaction and IME contract</td><td>**One menu component anchored above the composer, full composer width, focus always stays in the textarea. Keys while open: ↑/↓ cycle with wraparound, Enter/Tab accept, Escape closes only the menu (takes precedence over the composer&#39;s clear/blur Escape), typing filters live; Enter never sends while an item is highlighted, and an empty result list counts as closed. The menu closes on outside mousedown, blur, deleting past the trigger character, or session switch. All key handling and trigger detection sit behind the IME guard (\`isComposing</td><td></td></tr><tr><td>D197</td><td>Composer clipboard files become session-scratch references</td><td><strong>When the composer clipboard contains one or more OS files/images, the renderer intercepts the file paste, transfers bounded bytes plus name/MIME metadata through the Electron-only <code>pi-desktop/composer/pasteFiles</code> channel, and includes the durable session id. Electron main verifies the session, sanitizes names, and writes unique files below <code>&lt;data_dir&gt;/scratch/&lt;sessionId&gt;/pasted/</code>; D209 amends the draft presentation while preserving each returned absolute path as the quoted-or-unquoted <code>@</code> reference sent to the agent. Text-only paste remains native. A home composer creates or reuses a durable session first. Clipboard bytes never enter the prompt, workspace, or artifact store, and session deletion removes the pasted files with the scratch root.</strong></td><td>Fixes the broken file/image paste path without dirtying the project or inventing provider-specific binary prompt plumbing; it reuses the existing scratch containment and lifecycle contract.</td></tr><tr><td>D209</td><td>Composer file references separate compact display from canonical prompt paths</td><td><strong>Completed workspace-file selections and materialized clipboard files become renderer-owned, session-scoped reference chips. A chip persistently displays only the leaf name, exposes the canonical path through tooltip/accessibility metadata, and can be removed without deleting scratch bytes. Immediately before submit, references serialize in stable order after the visible draft with D124&#39;s exact quoting, so reference-only drafts, slash templates, and Agent/Plan/Goal mode aliases retain their current behavior. Accepted dispatch clears the active editor but keeps a renderer-only, turn-scoped pre-serialization snapshot while smart Stop can still undo an unanswered send. That undo restores the original text and chips rather than prefilling serialized message content; abort after reply start keeps the partial transcript and restores nothing. Rejected/failed sends retain the active draft. <code>ComposerPastedFile.name</code> is the sanitized original leaf label while <code>path</code> retains the UUID-backed absolute storage identity. No binary payload, provider attachment, host RPC, or schema is added.</strong> <em>(amended by D362: chips are inline sentinel-backed elements in the contenteditable draft)</em></td><td>Long relative paths and UUID-backed scratch paths overwhelmed the prompt row even though only the agent needs them. Separating presentation from serialization preserves exact tool-readable paths, duplicate-name identity, textarea/IME behavior, and the text-only provider contract (ADR 0070).</td></tr><tr><td>D362</td><td>Accepted <code>@</code> files insert the same inline chip as paste</td><td><strong>Amend D209 / D125 in the renderer: accepting a completed workspace file from the <code>@</code> menu (Enter, Tab, or click) replaces the trigger token with one private-use sentinel in the draft and stores that token on the reference, the same path clipboard paste already uses. The chip paints at the caret; that confirmation never sends. Directories still insert <code>@dir/</code> as text. Token-less references are not a visible draft surface. Workspace-relative chips still drop on project switch; absolute scratch/paste paths stay. No IPC, storage, host-protocol, or schema change.</strong></td><td>After chips moved inline, file accept still stripped <code>@query</code> and stored a token-less reference that no longer painted, so Enter made the file vanish.</td></tr><tr><td>D333</td><td>Work-panel browser is the bundled <code>pi.browser</code> plugin</td><td><strong>The work-panel Browser launcher, chrome, and agent CDP ship as bundled plugin <code>pi.browser</code> (enabled by default, disableable, not uninstallable). The guest <code>WebContentsView</code> and debugger remain host-owned and are reached only through public <code>pi.browser.*</code> gated by <code>browser.cdp</code>. Guest bounds are clamped to the calling plugin view. Raw CDP is allowlisted (cookies/storage/Target/Fetch denied). Host <code>BrowserPreview</code> stays a Plan-safe facade that fails if the plugin is disabled. No protocol/schema bump.</strong></td><td>Users need the whole browser to be optional like Files, without putting arbitrary web content inside a sandboxed plugin view. See ADR 0170.</td></tr><tr><td>D211</td><td>Global plugin launcher</td><td><strong>A customizable <code>openPluginLauncher</code> shortcut defaults to Option+Space on macOS and Alt+Space on Windows/Linux. Electron main registers it during startup and owns one prewarmed 620×440 frameless utility window centered on the display nearest the pointer; macOS uses a cross-workspace panel, while Windows uses a host-core low-level hook for the reserved default Alt+Space chord and retains a focused-window fallback if that hook is unavailable. The sandboxed renderer lists only enabled, ready panel plugins, matches Chinese names by original characters, tone-free full pinyin, or pinyin initials (plus normalized name/id/description), and opens the selected result through the existing plugin panel host. Up/Down selects, Enter/click opens, Escape/blur hides, and IME composition never dispatches. The additive toggle/dismiss/shown IPC is Electron-local; host-core&#39;s additive shortcut method/notification carries only the binding event.</strong></td><td>Plugin panels need a keyboard-first entry point that works while PI-Desktop is unfocused, without restoring the full shell or weakening the existing panel sandbox/permission boundary (ADR 0072/0076/0080).</td></tr><tr><td>D212</td><td>Running composers stage next-turn configuration and stopped turns preserve throughput</td><td><strong>During an active turn, draft text and mode/thinking/permission selectors remain editable. The single submit slot renders Stop only for an empty draft; a non-empty draft renders enabled Send and queues the next prompt. The renderer projects the latest full configuration per session and calls the existing idle-only <code>session/configure</code> only after the terminal event, so the running runtime remains pinned; pending Plan/Goal approval still gates editing. User-stopped partial answers preserve <code>responseDurationMs</code>; when final provider output usage is absent, visible thinking+answer text is estimated at four Unicode code points per token into optional <code>responseOutputTokens</code>. Rust transcript metadata persists both fields, exact usage wins, and the UI marks estimate-only throughput.</strong></td><td>Preparing the next prompt should not wait for the current stream, but host safety requires immutable in-flight configuration. A stopped visible answer still has enough measured data for useful, explicitly approximate generation speed (ADR 0073).</td></tr><tr><td>D215</td><td>Windows-reserved global shortcut fallback</td><td><strong>On Windows, host-core installs a narrow <code>WH_KEYBOARD_LL</code> hook for the effective <code>Alt+Space</code> default binding because the shell may reserve it from Electron&#39;s <code>globalShortcut</code>. The hook consumes only the matching chord and emits <code>keyboard.shortcut({ binding: &quot;Alt+Space&quot; })</code>; Electron toggles the existing plugin launcher. Electron calls additive <code>keyboard.setGlobalShortcut({ binding })</code> whenever settings change, and host-core enables the hook only for that exact binding. Other platforms and custom bindings remain on Electron&#39;s global shortcut path.</strong></td><td>Windows&#39; active-window system menu reservation made the shipped global launcher unavailable whenever PI-Desktop was unfocused. A narrowly scoped host fallback preserves the shortcut without reading text or expanding plugin/renderer privileges (ADR 0076).</td></tr><tr><td>D217</td><td>Early plugin launcher warm-up</td><td><strong>As soon as Electron is ready, before backend/plugin boot completes, Electron starts creating and loading the retained plugin launcher while it remains hidden. Window creation uses one shared in-flight promise, so a shortcut received during warm-up joins the same renderer load; failures destroy the incomplete window, clear the promise, and remain retryable on the next invocation. Each show still refreshes the live plugin catalog. On macOS, showing the panel relies on its normal activation and skips a redundant application activation/window-stack move.</strong></td><td>Overlapping launcher renderer startup with backend boot and avoiding duplicate macOS activation work removes first-invocation delay and visible stutter without weakening plugin freshness, IPC, or sandbox boundaries (ADR 0080).</td></tr><tr><td>D219</td><td>Plugin launcher most-recently-used history</td><td><strong>The launcher renderer records each successfully opened plugin id with a timestamp in renderer-local <code>localStorage</code> (key <code>pi.desktop.pluginLaunchHistory</code>, capped at 24) and presents an empty query in most-recently-used order. A typed query ranks search relevance first and uses recency only as a tiebreaker, so unused plugins sort after the recent ones by name. Corrupted or unavailable storage degrades to the existing name order without breaking launching. This refines D211&#39;s presentation without changing IPC, host RPC, permissions, protocol v9, or storage schema v11.</strong></td><td>Keyboard-first launcher users reopen the same plugins repeatedly; remembering the last-used order keeps the most frequent target one Enter away while relevance stays authoritative once the user types.</td></tr><tr><td>D221</td><td>Renderer output size controls</td><td><em>(bundled CJK clause superseded by D598)</em> <strong>The renderer build sets <code>minify: &quot;esbuild&quot;</code> explicitly because <code>electron-vite</code> hard-defaults its renderer preset to <code>minify: false</code>. A <code>pi-drop-legacy-font-fallbacks</code> plugin strips comma-prefixed <code>woff</code>/<code>truetype</code> <code>src</code> entries with <code>enforce: &quot;pre&quot;</code>, before Vite registers them as assets, since the bundled Chromium always supports <code>woff2</code>; a face whose only source is a legacy format keeps it. <code>BrandLogo</code> imports 192x192 marks derived from the canonical masters at <code>src/assets/brand/logo-{light,dark}.png</code> rather than the 1024px installer icons in <code>build/</code>, refining D079/D094 without changing the visual identity. <code>packaging-footprint.test.mjs</code> asserts all three so a framework default or a reverted import cannot silently regress them. The two bundled CJK faces stay unsubset per ADR 0083 section 2.</strong></td><td><code>out/renderer</code> fell from 31 MiB to 24 MiB with no feature loss; the unminified default and the installer-icon import were accidental costs, while CJK subsetting would drop glyphs from user content and needs an ADR revision</td></tr><tr><td>D198</td><td>Goal mode is the second contract mode</td><td><strong>Goal is a third durable operating mode (<code>agent</code> / <code>plan</code> / <code>goal</code>) that reuses the Plan approval pipeline end to end. <code>EnterGoalMode</code> switches an active Agent turn into the Goal contract state; <code>SubmitGoal(title, markdown, question)</code> writes an immutable host-owned <code>.pi/goal/&lt;unique-name&gt;.md</code> artifact and inserts one pending <code>plan_approvals</code> row. The single approval table gains a <code>kind</code> column (<code>plan</code> or <code>goal</code>, defaulting to <code>plan</code>) in schema v11; the kind — not the projected planning state — selects the prompt, artifact directory, submit tool, and i18n namespace, so one approval bar and one execution queue serve both. Plan and Goal are together the contract modes: they share one tool allowlist (Read/Glob/Grep/BrowserPreview/Bash plus their own submit tool), one host hard deny for Write/Edit/plugin/unknown tools evaluated against the durable mode, the shared <code>*_IN_PLAN</code> error codes, and the <code>PLAN_REQUIRES_INTERACTIVE_SESSION</code> rejection of unattended runs. An approved Goal executes autonomously in Agent: it chooses its own approach and keeps working until every acceptance criterion is verified or a boundary blocks it, then reports criterion by criterion. The wire <code>kind</code> is optional and absent means <code>plan</code>, so this is additive inside protocol v9.</strong></td><td>Plan answers &quot;carry out these steps&quot;; users also need &quot;reach this outcome, decide the steps yourself&quot; (Claude Code&#39;s plan mode and Codex&#39;s goal-shaped runs). Making the goal statement, acceptance criteria, and boundaries an approved contract keeps autonomy auditable, and discriminating one pipeline by kind avoids a parallel table, approval surface, and permission boundary that would inevitably drift.</td></tr><tr><td>D144</td><td>Sidebar primary chrome at 14px</td><td><strong>Expanded sidebar primary chrome (New task, Plugins, session titles, footer identity name, profile menu actions) uses <code>--text-base</code> (14px). Project/group titles and empty-state copy use <code>--text-md</code> (13px). Section labels use <code>--text-sm</code> (12px). Primary sidebar content must not use the micro <code>--text-xs</code> band.</strong></td><td>13px sidebar body felt undersized next to the 14px chat surface; bumping only primary chrome keeps density while restoring visual balance without a global type-scale change.</td></tr><tr><td>D145</td><td>Disable browser text correction on editable fields</td><td><strong>Every text <code>input</code> and <code>textarea</code> in the desktop renderer disables browser text correction: <code>spellCheck={false}</code>, <code>autoCorrect=&quot;off&quot;</code>, and <code>autoCapitalize=&quot;off&quot;</code>. Shared <code>Input</code>/<code>Textarea</code> primitives default these values; raw fields (composer, message edit, command palette, global search, settings/plugins/projects search, model search, browser URL bar, provider model combo) set them explicitly. Checkboxes and non-text controls are unchanged.</strong></td><td>Coding prompts, paths, model ids, and URLs must not be red-underlined or auto-mutated by Chromium/OS text correction; the shell is an application, not a document editor.</td></tr></tbody></table><p>| D146 | Startup splash + motion tokens | <strong>While bootstrap is incomplete the renderer shows a branded full-window startup splash (logo, shell name, tagline, accessible <code>app.starting</code>, soft progress bar) instead of plain status text. After <code>ready</code>, the splash holds a short minimum dwell (~420ms), then fades out (~280ms) over the mounted shell. Global motion uses CSS tokens <code>--motion-duration-{fast,normal,slow}</code> and <code>--motion-ease-{out,in,standard}</code> with shared overlay/surface enter keyframes; interactive transitions prefer these tokens. Reduced motion collapses splash/overlay motion to near-zero and freezes the progress bar. Crash chrome uses <code>app.uiCrashed</code>.</strong> | Boot is a first-run moment that previously felt unfinished; a short branded splash communicates readiness without decorative theatre, and shared motion tokens make shell transitions consistent and silkier while remaining feedback-only. | | D147 | Interaction detail polish (selection, CJK labels, motion fills) | <strong>Copyable surfaces use theme-aware <code>::selection</code> (text-primary mix), <code>caret-color</code>, and <code>accent-color</code> on the monochrome ramp; focus rings mix accent with transparent (no white wash). High-traffic chrome (jump-latest, stop, menus, search rows, notifications, work-panel tab close, brand chip) transitions via <code>--motion-duration-fast</code>. Scrollbars are 8px with a stronger hover thumb. Empty-home stack gap is 24px. Under <code>lang=zh-CN</code>, section labels drop uppercase/wide tracking. Undefined <code>--radius-token-row</code> is replaced by <code>--radius-sm</code>.</strong> | Residual gold-polish gaps after the neutral accent + motion-token pass: browser-blue selection, abrupt hover fills, Latin-only label styling on Chinese chrome, and one undefined radius token. | | D150 | Composer runtime chip descenders | <strong>Composer toolbar chips (Agent/Plan, Thinking, permission mode, model ID) keep labels fully inked inside the 28px capsule: chip and label line-height is <code>--leading-compact</code>, chips do not clip with <code>overflow: hidden</code> on the control, and the model label uses horizontal ellipsis without <code>leading-none</code>. Descenders on <code>g</code>/<code>y</code>/<code>p</code>/<code>q</code>/<code>j</code> must remain visible in light and dark.</strong> | <code>leading-none</code> plus truncate overflow crushed glyph descenders on model IDs and labels such as Agent / Accept edits, making the bottom toolbar look cut off. | | D151 | Send re-pins transcript follow | <strong>Starting a turn via send, retry, or regenerate always re-pins transcript follow mode, hides the jump-to-latest pill, and scrolls to the bottom before new content arrives. Manual scroll during a turn still pauses follow; the jump control remains the only non-turn way to resume. This refines D071 without restoring forced scroll on every token.</strong> | Users who scroll up to inspect history still expect the next prompt they send to land at the latest exchange; leaving follow paused after send hid the new turn behind the jump pill. | | D152 | Direct runtime stream rendering | <strong>Assistant content renders each runtime stream chunk directly through the incremental Markdown block cache. The renderer does not add a requestAnimationFrame typewriter state loop. KaTeX&#39;s Vite-inlined fonts remain local-only assets and are admitted by the narrow <code>font-src &#39;self&#39; data:</code> CSP directive.</strong> | The duplicate animation loop could trip React&#39;s nested-update guard during sustained streams, while the previous CSP blocked bundled math fonts and produced console errors. | | D153 | Reasoning sessions default to maximum thinking | <em>(superseded by D303)</em> <strong>A newly created session whose inherited default model supports reasoning starts at the highest canonical entry in that model&#39;s pi-published <code>supportedThinkingLevels</code>. Non-reasoning models and missing capability metadata start at <code>off</code>; existing sessions retain their durable choice. This refines D096 without adding a provider override.</strong> | Reasoning-capable models should use their strongest available effort by default while preserving explicit per-session choices and pi-ai&#39;s model authority. | | D264 | Composer typing avoids full sorts and redundant DOM writes | <strong>The <code>@</code> file menu picks its visible rows with a bounded top-K selection over the match list instead of sorting every match; the selection returns exactly the row set and order a full sort produced, and the small, kind-grouped command list keeps its full sort. Composer auto-resize and <code>--composer-dock-height</code> publication are idempotent: an unchanged height performs no DOM write and no document-wide style invalidation, and the <code>height: auto</code> measurement probe is taken only when the box may need to shrink. Growth through seven rows, internal scrolling past the seventh, and contraction on delete or submit are unchanged. Draft file-reference state and the cursor keep their identity and value when a keystroke changes neither, so the draft-cache serialization effect and autocomplete trigger detection stop re-running per keystroke. No IPC, storage, host-protocol, or schema change.</strong> | With a large workspace and the <code>@</code> menu open, every keystroke paid a full sort over the whole 8000-entry file index to show 50 rows, plus a forced synchronous reflow from the measurement probe, while a fresh <code>fileReferences</code> array identity per keystroke re-ran the draft-cache serialization effect. The cost was typing latency, not correctness; ordering equivalence with the full sort is covered by unit tests. |</p><h2 id="t-release-delivery-decisions" tabindex="-1">T. Release delivery decisions <a class="header-anchor" href="#t-release-delivery-decisions" aria-label="Permalink to &quot;T. Release delivery decisions&quot;">​</a></h2><table tabindex="0"><thead><tr><th>ID</th><th>Topic</th><th>Decision</th><th>Rationale</th></tr></thead><tbody><tr><td>D126</td><td>Three-platform release delivery (lifts D010)</td><td><em>(amended by D364 / D603)</em> <strong>Tag builds publish every artifact the matrix produces to the GitHub Release: macOS dmg/zip (arm64), Windows NSIS x64, Linux AppImage + deb (x64), each with blockmaps and the platform&#39;s <code>latest*.yml</code> electron-updater feed. Publishing the feeds activates D120&#39;s in-app update lanes for Windows NSIS and Linux AppImage; macOS stays in notify-and-link mode until a signed channel is qualified. The NSIS artifact name is pinned space-free (<code>PI-Desktop-Setup-\${version}.\${ext}</code>) because GitHub asset URLs mangle spaces. D010&#39;s macOS-only scope is lifted per the baseline-bump rule (baseline <code>0.4.7</code>); the release pipeline itself was qualified end-to-end on v0.1.1-rc.1/v0.1.1.</strong></td><td>The pipeline builds and validates all three platforms on every tag anyway; keeping installers as expiring Actions artifacts (90-day retention) withheld them from users without adding safety. Publishing the update feeds is the point of shipping: platforms with in-app lanes update silently, and future platform regressions surface through real installs instead of unused artifacts.</td></tr><tr><td>D364</td><td>Windows portable exe without installer</td><td><em>(superseded by D603)</em> <strong>Amend D120 / D126 / ADR 0022: tag builds publish a Windows x64 portable exe <code>PI-Desktop-Portable-\${version}.exe</code> alongside the NSIS installer <code>PI-Desktop-Setup-\${version}.exe</code>. The portable target does not write <code>latest.yml</code>. Packaged portable runs (<code>PORTABLE_EXECUTABLE_FILE</code>) use notify-and-link delivery. NSIS installs keep in-app download and quit-and-install. Data stays in the existing application data directory. Portable requests user execution level.</strong></td><td>Company environments that whitelist a single executable cannot run the NSIS installer. Applying the NSIS updater to a portable run would install the app, so portable stays manual (ADR 0197, E2E-211).</td></tr><tr><td>D603</td><td>Windows portable delivery uses a normal ZIP</td><td><strong>Amend D364 / ADR 0022 / ADR 0197: tag builds publish a Windows x64 portable ZIP <code>PI-Desktop-Portable-\${version}.zip</code> alongside the NSIS installer <code>PI-Desktop-Setup-\${version}.exe</code>. The Windows release helper builds NSIS and ZIP separately, stamps ZIP app metadata with <code>piDistribution = &quot;zip&quot;</code>, and keeps the ZIP target out of <code>latest.yml</code>. Extracted ZIP runs use notify-and-link delivery; the updater still recognizes <code>PORTABLE_EXECUTABLE_FILE</code> for older portable executables. Data stays in the existing application data directory.</strong></td><td>The self-extracting portable wrapper could trigger administrator prompts and did not provide a reliable normal executable identity for the taskbar. A user-extracted ZIP launches <code>PI-Desktop.exe</code> directly and preserves the manual-update boundary.</td></tr><tr><td>D260</td><td>Release documentation is a version surface</td><td><strong>A stable version bump must update every version-bearing surface before the tag: the shipped-locale in-app changelog and its test list, every workspace <code>package.json</code> including <code>docs/package.json</code> (a third workspace root <code>scripts/release.mjs</code> previously skipped), the Cargo workspace version and <code>host-core</code> lockfile entry, <code>APP_VERSION</code>, and the <code>&lt;major&gt;.&lt;minor&gt;.x</code> release line stated in <code>README.md</code> and <code>README.zh-CN.md</code>. <code>scripts/check-release-docs.mjs</code> verifies all of them; <code>scripts/release.mjs</code> runs it after bumping and refuses to commit or tag while any surface disagrees, with <code>--skip-docs-check</code> reserved for deliberate non-release bumps. Extends D164.</strong></td><td>The in-app changelog gate alone left published documentation behind: the READMEs still advertised the <code>0.5.x</code> line at <code>0.10.8</code>, and <code>docs/package.json</code> sat at <code>0.5.8</code>. A tag is irreversible, so the check runs before the tag exists rather than as review etiquette.</td></tr><tr><td>D371</td><td>Explicit unsigned macOS first-launch helper</td><td><em>(amended by D406 and D443)</em> <strong>Every macOS distribution ships an executable <code>PI-Desktop-macOS-open.command</code>, placed on the DMG in a visible first-launch row below the drag-to-Applications gesture. It searches only <code>/Applications/PI-Desktop.app</code> and <code>~/Applications/PI-Desktop.app</code>, verifies <code>CFBundleIdentifier</code> is <code>net.aiuo.pi-desktop</code>, removes only <code>com.apple.quarantine</code> when present, and opens the app. It never uses <code>sudo</code>, accepts no arbitrary path, and does not replace Developer ID signing or notarization.</strong></td><td>The unsigned macOS lane can be blocked by quarantine with a misleading damaged-app message, and the terminal-only <code>xattr -cr</code> note was broader than the launch failure requires (ADR 0204, E2E-196b)</td></tr><tr><td>D443</td><td>Canonical application ID and macOS codesign identifier</td><td><strong>Amend D141 / D371 / ADR 0204: the application ID is <code>net.aiuo.pi-desktop</code> (<code>APP_ID</code>, electron-builder <code>appId</code>, macOS <code>CFBundleIdentifier</code>, Windows AppUserModelID). Development macOS hosts use <code>net.aiuo.pi-desktop.dev</code>. Do not adhoc-sign the unsigned pack in <code>afterPack</code>/<code>afterSign</code>: nested Electron helpers are still unsigned and <code>codesign</code> fails with <code>code object is not signed at all</code>. See ADR 0278 and issue #524.</strong></td><td>The owner domain is <code>net.aiuo.pi-desktop</code>. Binding the unsigned outer identifier is deferred until a helper-safe pack path exists.</td></tr></tbody></table><h2 id="v-extension-activation-decisions" tabindex="-1">V. Extension activation decisions <a class="header-anchor" href="#v-extension-activation-decisions" aria-label="Permalink to &quot;V. Extension activation decisions&quot;">​</a></h2><table tabindex="0"><thead><tr><th>ID</th><th>Topic</th><th>Decision</th><th>Rationale</th></tr></thead><tbody><tr><td>D192</td><td>Activation scope shared by every extension kind</td><td><strong>Plugins, user MCP servers and user skills each carry <code>enabled: boolean</code> plus <code>scope: { mode: &quot;global&quot; | &quot;projects&quot;; projects: string[] }</code>. <code>enabled</code> stays separate so switching an extension off never discards its project list, and the three-state control (<code>off</code> / <code>projects</code> / <code>global</code>) is derived, not stored. Matching is case-insensitive, trailing-separator-insensitive and subdirectory-inclusive; a missing scope resolves to global; a <code>projects</code>-mode extension is inactive in a session with no project. Scope is enforced both when a per-turn catalog is assembled and again at dispatch (<code>tools.execute</code>, <code>UserMcpRuntime.callTool</code>, <code>loadUserSkillBody</code>, command execution). Agent-facing surfaces filter on the session&#39;s project, app-facing surfaces on the active window&#39;s; themes are not scoped at all.</strong></td><td>A single boolean made every extension global, so a work-only MCP server spent context in every unrelated session and the only remedy was toggling by hand on each project switch. One scope shape lets one control and one predicate serve all three kinds, and enforcing at dispatch closes the window in which a session still remembers a tool the user has just scoped away (ADR 0056).</td></tr><tr><td>D193</td><td>User-owned MCP servers need no plugin</td><td><strong>host-core stores one JSON configuration file per MCP id under <code>~/.agents/servers</code> or <code>&lt;project&gt;/.agents/servers</code>; enablement and project overrides live only in <code>&lt;data&gt;/agent-capabilities/mcp.json</code>. Level-aware <code>mcp.list</code>, <code>mcp.active</code>, <code>mcp.upsert</code>, <code>mcp.remove</code>, and <code>mcp.setEnabled</code> calls drive the Settings &gt; Agent &gt; MCP page; a project record shadows a global record by id or label before disabled records are filtered. The existing MCP editor and desktop <code>mcp/test</code> action provide transport validation and connection feedback.</strong></td><td>A user-owned server is portable configuration, not a plugin package. Separating its file from app-local state lets projects override a global server without mutating the user file and makes deletion pruning deterministic (ADR 0112).</td></tr><tr><td>D194</td><td>User-owned skills are Markdown documents in <code>.agents</code> roots</td><td><strong>host-core scans <code>~/.agents/skills</code> and <code>&lt;project&gt;/.agents/skills</code>, accepts direct Markdown files and <code>&lt;skill&gt;/SKILL.md</code>, and exposes frontmatter <code>name</code>/<code>description</code> through level-aware <code>skills.list</code> and <code>skills.active</code>. A single-file import physically copies the source into the selected directory. Enablement and project overrides live in <code>&lt;data&gt;/agent-capabilities/skills.json</code>, never in the document; project records shadow global records before disabled records are filtered.</strong></td><td>A skill remains a portable Markdown document, while the app-local state file keeps activation decisions out of user-authored content (ADR 0112).</td></tr><tr><td>D257</td><td>Capability pages are one workbench that can author</td><td><strong>Refine D193 / D194 and supersede D202&#39;s page shape: Settings &gt; Agent &gt; Skills / MCP / Subagents each render one toolbar (level filter as a segmented control with live counts, one search field, project picker, primary actions) above one elevated panel whose rows are divided by level group headers and carry their own level badge. All three pages create, edit, and delete through the host calls that already existed; new capabilities land at the level the filter points at and the primary action names that destination. Destructive row actions live in a per-row overflow menu and arm before firing. Busy state is scoped to the row with the in-flight request, skeletons render on first paint only, later loads dim the rows already on screen, and enablement flips optimistically and reverts only if the host refuses. <code>skills.reveal</code> accepts <code>{ id, level?, projectPath? }</code> and forwards it to <code>skills.read</code>, which already parsed both; a bare id string still works. Subagents stay global-only (no filter, no picker), and every capability-state, precedence, and <code>.agents</code> path rule from D193 / D194 / D202 is unchanged.</strong></td><td>The pages listed capabilities but could not manage them: <code>d24e1ee0</code> deleted both editor sheets while the create/update/remove/reveal RPCs stayed live, the level was page structure rather than a filter so two headers consumed most of a short page, one pending request disabled every control through <code>busy={loading || busyKey !== null}</code>, and awaiting a reload after each toggle replaced the list with skeletons. Revealing a project skill also sent the id alone, which cannot resolve a project record with no global counterpart (ADR 0126).</td></tr></tbody></table><h2 id="_2026-07-28-—-plugin-marketplace-panels-and-high-risk-apis" tabindex="-1">2026-07-28 — Plugin marketplace, panels, and high-risk APIs <a class="header-anchor" href="#_2026-07-28-—-plugin-marketplace-panels-and-high-risk-apis" aria-label="Permalink to &quot;2026-07-28 — Plugin marketplace, panels, and high-risk APIs&quot;">​</a></h2><ul><li>Official local marketplace provider can browse/search/install <code>.piplug</code> packages with checksum verification.</li><li>Plugin panels run in sandboxed isolated windows via <code>pluginBridge</code>.</li><li>High-risk plugin host APIs (<code>fs.write</code>, <code>net.fetch</code>, clipboard, openExternal) are available only with explicit grants.</li><li>Per-plugin auto-update is supported; permission-expanding upgrades require review.</li></ul><h2 id="_2026-07-28-—-official-plugin-marketplace-repository" tabindex="-1">2026-07-28 — Official plugin marketplace repository <a class="header-anchor" href="#_2026-07-28-—-official-plugin-marketplace-repository" aria-label="Permalink to &quot;2026-07-28 — Official plugin marketplace repository&quot;">​</a></h2><ul><li>Dedicated repo <code>vastsa/pi-desktop-plugins</code> is the official marketplace source.</li><li>PI-Desktop fetches <code>catalog.json</code> remotely (cached under <code>~/.pi-desktop/plugins/market/</code>).</li><li>Plugin maintainers pack sources with repo scripts and publish by pushing to that repository.</li><li>Local bundled catalog remains fallback only when remote fetch fails.</li></ul><h2 id="_2026-07-28-—-marketplace-template-detail-pane" tabindex="-1">2026-07-28 — Marketplace template + detail pane <a class="header-anchor" href="#_2026-07-28-—-marketplace-template-detail-pane" aria-label="Permalink to &quot;2026-07-28 — Marketplace template + detail pane&quot;">​</a></h2><ul><li>Official warehouse gained a practical template plugin <code>demo.workspace-summary</code> and CONTRIBUTING guide.</li><li>PI-Desktop marketplace UI now opens a detail pane with README, changelog, and version list via <code>market.getDetail</code>.</li></ul><h2 id="_2026-07-28-—-sidebar-type-balance" tabindex="-1">2026-07-28 — Sidebar type balance <a class="header-anchor" href="#_2026-07-28-—-sidebar-type-balance" aria-label="Permalink to &quot;2026-07-28 — Sidebar type balance&quot;">​</a></h2><ul><li>Expanded sidebar primary chrome uses <code>--text-base</code> (14px) instead of <code>--text-md</code> (13px).</li><li>Project/group titles step to <code>--text-md</code>; section labels stay secondary at <code>--text-sm</code>.</li><li>Decision D144: keep micro tokens off primary left-rail content so the rail matches body readability without changing the global type ramp.</li></ul><h2 id="_2026-07-28-—-chat-markdown-prose-redesign" tabindex="-1">2026-07-28 — Chat markdown prose redesign <a class="header-anchor" href="#_2026-07-28-—-chat-markdown-prose-redesign" aria-label="Permalink to &quot;2026-07-28 — Chat markdown prose redesign&quot;">​</a></h2><ul><li>Assistant markdown (<code>.prose-chat</code>) was restyled for denser, calmer transcript reading: clearer heading hierarchy, accent-tinted blockquotes, quieter list markers, bordered inline code, zebra/hover tables, and inset code cards with monospace language tags.</li><li>Thinking prose reuses the same hierarchy at secondary color / <code>text-sm-plus</code> so reasoning stays visually subordinate to the answer.</li><li>Renderer behavior (streaming block split, GFM/math, Shiki) is unchanged; this is a presentation-only pass in <code>globals.css</code> + component/design specs.</li></ul><h2 id="_2026-07-28-—-neutral-gray-accent-no-blue-brand" tabindex="-1">2026-07-28 — Neutral gray accent (no blue brand) <a class="header-anchor" href="#_2026-07-28-—-neutral-gray-accent-no-blue-brand" aria-label="Permalink to &quot;2026-07-28 — Neutral gray accent (no blue brand)&quot;">​</a></h2><ul><li><code>--ds-accent</code> / <code>--ds-accent-hover</code> / <code>--ds-accent-soft</code> / <code>--ds-info</code> now resolve to the gray scale (dark: white→gray-100→gray-300; light: <code>#1a1c1f</code>→<code>#303030</code>→<code>#5d5d5d</code>) instead of Codex blue.</li><li>Markdown links, blockquotes, focus rings, plugin CTAs, toggles, and selected-session rings inherit the neutral accent automatically via tokens.</li><li>Project color dots and docs/specs updated to drop blue as the brand accent.</li></ul><h2 id="_2026-07-28-—-plugins-page-light-theme-token-pass" tabindex="-1">2026-07-28 — Plugins page light-theme token pass <a class="header-anchor" href="#_2026-07-28-—-plugins-page-light-theme-token-pass" aria-label="Permalink to &quot;2026-07-28 — Plugins page light-theme token pass&quot;">​</a></h2><ul><li>Marketplace/plugin chrome CSS dropped raw blue-slate fallbacks (<code>#4f7cff</code>, <code>#2a3144</code>, <code>#121826</code>, …) and now consumes only <code>--ds-*</code> tokens.</li><li>Tabs, actions, search, cards, permission modal, and badges adapt to light/dark via the neutral gray accent system.</li></ul><h2 id="_2026-07-28-—-markdown-light-theme-paper-pass" tabindex="-1">2026-07-28 — Markdown light-theme paper pass <a class="header-anchor" href="#_2026-07-28-—-markdown-light-theme-paper-pass" aria-label="Permalink to &quot;2026-07-28 — Markdown light-theme paper pass&quot;">​</a></h2><ul><li>Light <code>.prose-chat</code> / <code>.code-block</code> surfaces were retuned for white chat paper: softer underlined links, flat gray code cards (no muddy shadow), quieter blockquotes/tables/kbd/math, and secondary thinking ink.</li><li>Dark markdown treatment is unchanged in spirit (inset charcoal code, light-gray links via accent-soft).</li></ul><h2 id="_2026-07-28-—-one-dark-pro-code-highlighting" tabindex="-1">2026-07-28 — One Dark Pro code highlighting <a class="header-anchor" href="#_2026-07-28-—-one-dark-pro-code-highlighting" aria-label="Permalink to &quot;2026-07-28 — One Dark Pro code highlighting&quot;">​</a></h2><ul><li>Chat fence highlighting switched from <code>github-light</code>/<code>github-dark</code> to <code>one-light</code>/<code>one-dark-pro</code>.</li><li>Code cards paint a single editor surface (<code>#fafafa</code> / <code>#282c34</code>); nested <code>pre</code>/<code>code</code>/token backgrounds are forced transparent so there is no double wash.</li></ul><h2 id="_2026-07-28-—-disable-text-correction-on-editable-fields" tabindex="-1">2026-07-28 — Disable text correction on editable fields <a class="header-anchor" href="#_2026-07-28-—-disable-text-correction-on-editable-fields" aria-label="Permalink to &quot;2026-07-28 — Disable text correction on editable fields&quot;">​</a></h2><ul><li>Shared <code>Input</code>/<code>Textarea</code> primitives default <code>spellCheck={false}</code>, <code>autoCorrect=&quot;off&quot;</code>, and <code>autoCapitalize=&quot;off&quot;</code>.</li><li>Composer, message edit, palette/search fields, settings/plugins/projects search, model search, browser URL bar, and provider model combo follow the same contract.</li><li>Decision D145: browser/OS spelling and autocorrect chrome stays off across the desktop shell.</li></ul><h2 id="_2026-07-28-—-startup-splash-and-motion-tokens" tabindex="-1">2026-07-28 — Startup splash and motion tokens <a class="header-anchor" href="#_2026-07-28-—-startup-splash-and-motion-tokens" aria-label="Permalink to &quot;2026-07-28 — Startup splash and motion tokens&quot;">​</a></h2><ul><li>Boot path paints <code>StartupSplash</code> (brand mark, shell name, tagline, progress bar) until host/settings bootstrap finishes.</li><li>Shared CSS motion tokens and overlay/surface enter keyframes polish dialogs, search, toasts, and interactive fills.</li><li>Decision D146: splash is boot feedback with reduced-motion-safe exit; catalogs gain <code>app.uiCrashed</code> and finish zh-CN empty-home/custom copy.</li></ul><h2 id="_2026-07-28-—-interaction-detail-polish" tabindex="-1">2026-07-28 — Interaction detail polish <a class="header-anchor" href="#_2026-07-28-—-interaction-detail-polish" aria-label="Permalink to &quot;2026-07-28 — Interaction detail polish&quot;">​</a></h2><ul><li>Theme-aware <code>::selection</code>, <code>caret-color</code>, and <code>accent-color</code> keep copy/edit chrome on the neutral gray accent ramp.</li><li>Hover fills on jump-latest, stop, composer-plus, search rows, profile/notification menus, and work-panel tab close use shared motion tokens.</li><li>CJK section labels under <code>:lang(zh-CN)</code> use normal tracking without forced uppercase.</li><li>Empty-home stack gap clamped to 24px; scrollbars refined to 8px with hover thumb; brand chip radius uses <code>--radius-sm</code>.</li><li>Decision D147.</li></ul><h2 id="_2026-07-28-—-work-panel-settings-light-surface-polish" tabindex="-1">2026-07-28 — Work panel / settings light-surface polish <a class="header-anchor" href="#_2026-07-28-—-work-panel-settings-light-surface-polish" aria-label="Permalink to &quot;2026-07-28 — Work panel / settings light-surface polish&quot;">​</a></h2><ul><li>Light work panel uses a quiet <code>#fafafa</code> inset column with a white header band so it separates from white chat paper without a heavy border.</li><li>Browser URL, generic field controls, shortcut keycaps, segment tracks, and toggle knobs receive light-theme surfaces and focus rings aligned with settings pills.</li><li>File tree, diff headers, resize handle, and destination filters ease hover fills with shared motion tokens.</li><li>Light dialog scrim softens to 28% ink so elevated white dialogs stay readable.</li><li>Decision D148.</li></ul><h2 id="_2026-07-28-—-user-facing-i18n-copy-pass" tabindex="-1">2026-07-28 — User-facing i18n copy pass <a class="header-anchor" href="#_2026-07-28-—-user-facing-i18n-copy-pass" aria-label="Permalink to &quot;2026-07-28 — User-facing i18n copy pass&quot;">​</a></h2><ul><li>English and zh-CN catalogs rewrite high-traffic shell copy away from internal jargon: local service instead of host/backend, AI provider instead of bare provider, project instead of workspace in user strings, marketplace refresh instead of &quot;from repo&quot;, temporary chats, and calmer status/error phrasing.</li><li>Empty states, onboarding, settings help, plugin permissions, and notifications explain outcomes in plain language while keeping stable i18n keys and interpolation names.</li><li>Decision D149.</li></ul><h2 id="_2026-07-28-—-composer-runtime-chip-descenders" tabindex="-1">2026-07-28 — Composer runtime chip descenders <a class="header-anchor" href="#_2026-07-28-—-composer-runtime-chip-descenders" aria-label="Permalink to &quot;2026-07-28 — Composer runtime chip descenders&quot;">​</a></h2><ul><li>Model, mode, thinking, and permission chips no longer use <code>leading-none</code> under overflow clipping; labels use <code>--leading-compact</code> so descenders stay visible.</li><li>Long model IDs still ellipsize horizontally via <code>.model-chip-label</code>.</li><li>Decision D150.</li></ul><h2 id="_2026-07-28-—-send-re-pins-transcript-follow" tabindex="-1">2026-07-28 — Send re-pins transcript follow <a class="header-anchor" href="#_2026-07-28-—-send-re-pins-transcript-follow" aria-label="Permalink to &quot;2026-07-28 — Send re-pins transcript follow&quot;">​</a></h2><ul><li>Starting a turn from send, retry, or regenerate re-pins the transcript and jumps to the bottom even if the user had scrolled up through history.</li><li>Stream follow remains paused only for manual scroll during an active turn; the jump-to-latest pill still resumes follow without starting a new turn.</li><li>Decision D151.</li></ul><h2 id="_2026-07-28-—-direct-runtime-stream-rendering" tabindex="-1">2026-07-28 — Direct runtime stream rendering <a class="header-anchor" href="#_2026-07-28-—-direct-runtime-stream-rendering" aria-label="Permalink to &quot;2026-07-28 — Direct runtime stream rendering&quot;">​</a></h2><ul><li>Assistant responses now display the runtime&#39;s progressive chunks directly through the incremental Markdown block cache, without a second per-frame React state loop.</li><li>Renderer CSP admits only local and Vite-inlined data fonts, allowing bundled KaTeX glyphs without opening a remote font origin.</li><li>Decision D152.</li></ul><h2 id="_2026-07-28-—-reasoning-sessions-default-to-maximum-thinking" tabindex="-1">2026-07-28 — Reasoning sessions default to maximum thinking <a class="header-anchor" href="#_2026-07-28-—-reasoning-sessions-default-to-maximum-thinking" aria-label="Permalink to &quot;2026-07-28 — Reasoning sessions default to maximum thinking&quot;">​</a></h2><ul><li>New sessions inherit the app&#39;s default model and select its highest pi-published thinking level when that model supports reasoning.</li><li>Non-reasoning or unresolved models remain <code>off</code>, while existing sessions keep their stored thinking preference.</li><li>Decision D153. Superseded by D303 once Settings persisted a per-model <code>defaultThinkingLevel</code>.</li></ul><h2 id="_2026-07-28-—-work-panel-activity-rail" tabindex="-1">2026-07-28 — Work-panel activity rail <a class="header-anchor" href="#_2026-07-28-—-work-panel-activity-rail" aria-label="Permalink to &quot;2026-07-28 — Work-panel activity rail&quot;">​</a></h2><ul><li>The open work panel now keeps Review, Terminal, and Browser in a compact 44px activity rail with clear active/open states.</li><li>The content header shows the active resource, closes it directly, and uses a bounded keyboard-operable switcher for all open tool and file resources.</li><li>The former horizontally scrolling tabs and hidden header context menu are removed; artifact-driven panel opening and session ownership are unchanged.</li><li>Decision D154.</li></ul><h2 id="_2026-07-28-—-sidebar-project-session-list-type-density" tabindex="-1">2026-07-28 — Sidebar project/session list type density <a class="header-anchor" href="#_2026-07-28-—-sidebar-project-session-list-type-density" aria-label="Permalink to &quot;2026-07-28 — Sidebar project/session list type density&quot;">​</a></h2><ul><li>Sidebar project group titles and session/thread titles step one token quieter than the previous body-chrome sizing so dense lists scan more cleanly.</li><li>Session titles use <code>--text-md</code> (13px); project/group titles and empty-state copy use <code>--text-sm</code> (12px). Primary chrome (New task, Plugins, footer) stays at <code>--text-base</code>.</li><li>Decision D155; superseded by D159 for the expanded sidebar&#39;s primary list content.</li></ul><h2 id="_2026-07-28-—-independent-window-and-work-panel-resizing" tabindex="-1">2026-07-28 — Independent window and work-panel resizing <a class="header-anchor" href="#_2026-07-28-—-independent-window-and-work-panel-resizing" aria-label="Permalink to &quot;2026-07-28 — Independent window and work-panel resizing&quot;">​</a></h2><ul><li>Native window edges now resize only the Electron shell; work-panel open, collapse, close, and divider commits leave outer bounds unchanged.</li><li>The divider uses anchored pointer delta, frame-coalesced preview, rollback on cancellation, and a wider stable hit area. Responsive clamping no longer overwrites the persisted preferred width.</li><li>Removed the renderer-to-Main <code>window/resizeBy</code> channel, programmatic resize attribution, and panel-specific window-state offset.</li><li>Decision D156; ADR 0029.</li></ul><h2 id="_2026-07-28-—-single-assistant-toolbar-per-user-turn" tabindex="-1">2026-07-28 — Single assistant toolbar per user turn <a class="header-anchor" href="#_2026-07-28-—-single-assistant-toolbar-per-user-turn" aria-label="Permalink to &quot;2026-07-28 — Single assistant toolbar per user turn&quot;">​</a></h2><ul><li>Provider-level assistant messages separated by tool calls remain canonical transcript records for model reseeding, persistence, and fork boundaries.</li><li>The chat transcript composes every assistant/thinking/tool record after one user message and before the next into one visual assistant turn, preserving order while exposing one aggregate meta row and one Copy/Fork/Retry toolbar.</li><li>Copy joins all contentful assistant fragments; Fork and Retry target the last contentful assistant record. Decision D157.</li></ul><h2 id="_2026-07-28-—-turn-boundary-context-checkpoint-compaction" tabindex="-1">2026-07-28 — Turn-boundary context checkpoint compaction <a class="header-anchor" href="#_2026-07-28-—-turn-boundary-context-checkpoint-compaction" aria-label="Permalink to &quot;2026-07-28 — Turn-boundary context checkpoint compaction&quot;">​</a></h2><ul><li>PI-Desktop now evaluates context after every <code>turn_end</code>, before the next provider request, with a transient soft reminder and a deterministic hard checkpoint guard.</li><li>Durable checkpoint records rebuild model context without deleting or hiding visible transcript messages; exact provider overflow receives one compacted retry.</li><li>The implementation reuses pi-agent-core primitives. OpenCode DCP remains an AGPL-3.0 behavioral reference, not a dependency or copied implementation.</li><li>Decision D158; ADR 0030.</li></ul><h2 id="_2026-07-28-—-sidebar-typography-aligned-with-the-global-body-scale" tabindex="-1">2026-07-28 — Sidebar typography aligned with the global body scale <a class="header-anchor" href="#_2026-07-28-—-sidebar-typography-aligned-with-the-global-body-scale" aria-label="Permalink to &quot;2026-07-28 — Sidebar typography aligned with the global body scale&quot;">​</a></h2><ul><li>Expanded-sidebar session titles return to <code>--text-base</code> (14px), matching the app body and primary sidebar chrome; project/group titles and empty-state copy return to the adjacent <code>--text-md</code> (13px) tier.</li><li>Section labels and secondary metadata remain at <code>--text-sm</code> (12px), and the existing compact row pitch, truncation, and sidebar dimensions are unchanged.</li><li>Decision D159; supersedes D155 and restores D144&#39;s primary-list hierarchy.</li></ul><h2 id="_2026-07-28-—-icon-free-composer-prompt-row" tabindex="-1">2026-07-28 — Icon-free composer prompt row <a class="header-anchor" href="#_2026-07-28-—-icon-free-composer-prompt-row" aria-label="Permalink to &quot;2026-07-28 — Icon-free composer prompt row&quot;">​</a></h2><ul><li>Home and thread-docked composer prompt rows render no leading brand icon; draft text and placeholder ink align directly with the input gutter.</li><li>The canonical logo remains in the home hero, sidebar, native application identity, startup splash, and About surfaces. Session-creation controls keep their dedicated message-plus icon. Decision D160; ADR 0031.</li></ul><h2 id="_2026-07-28-—-smooth-latest-wins-session-switching" tabindex="-1">2026-07-28 — Smooth latest-wins session switching <a class="header-anchor" href="#_2026-07-28-—-smooth-latest-wins-session-switching" aria-label="Permalink to &quot;2026-07-28 — Smooth latest-wins session switching&quot;">​</a></h2><ul><li>Session rows now acknowledge the newest selection immediately, prefetch on deliberate hover/focus, coalesce detail reads, and retain five recent transcripts for warm revisits with background revalidation.</li><li>Superseded transcript reads no longer block the latest request. Workspace alignment overlaps transcript IO where summary metadata permits, while the navigation generation still owns the only visible commit.</li><li>The previous complete transcript remains as a dimmed, non-interactive frame while React prepares a changed session&#39;s Markdown tree; reduced motion uses a static progress track. Decision D162.</li></ul><h2 id="_2026-07-28-—-compact-sidebar-session-titles" tabindex="-1">2026-07-28 — Compact sidebar session titles <a class="header-anchor" href="#_2026-07-28-—-compact-sidebar-session-titles" aria-label="Permalink to &quot;2026-07-28 — Compact sidebar session titles&quot;">​</a></h2><ul><li>Expanded-sidebar session titles use <code>--text-md</code> (13px), matching the compact project/group tier while primary actions and footer identity remain at <code>--text-base</code> (14px).</li><li>Row height, truncation, indentation, weight, and sidebar dimensions remain unchanged.</li><li>Decision D161; refines D159&#39;s session-title size.</li></ul><h2 id="_2026-07-29-—-native-width-reservation-for-the-fixed-work-panel" tabindex="-1">2026-07-29 — Native width reservation for the fixed work panel <a class="header-anchor" href="#_2026-07-29-—-native-width-reservation-for-the-fixed-work-panel" aria-label="Permalink to &quot;2026-07-29 — Native width reservation for the fixed work panel&quot;">​</a></h2><ul><li>The docked panel now keeps its committed <code>364..720px</code> width while open; native edges resize MainChat only.</li><li>Open, collapse/final close, and divider commit set one idempotent native-width target. Chat remains stable when the work area can reserve the full width and absorbs only the unavoidable shortfall otherwise.</li><li>Maximized/fullscreen geometry waits for normal state. Persisted base bounds exclude reservation width/x shift, and background artifacts cannot change the visible target.</li><li>Decision D163; ADR 0032 (the window-expansion portion is superseded by ADR 0033: the work panel is an internal dock that never expands the OS window). This supersedes the contrary portions of D156 and ADR 0029.</li></ul><h2 id="_2026-07-29-—-dual-locale-in-app-product-changelog" tabindex="-1">2026-07-29 — Dual-locale in-app product changelog <a class="header-anchor" href="#_2026-07-29-—-dual-locale-in-app-product-changelog" aria-label="Permalink to &quot;2026-07-29 — Dual-locale in-app product changelog&quot;">​</a></h2><ul><li>Ship EN + zh-CN product highlights in <code>packages/shared</code> and attach them to <code>UpdateState.releaseNotes</code> from Electron Main when an update is discovered.</li><li>The update banner and Settings → Info surface a compact What&#39;s new list in the active product locale without a new feed or IPC domain.</li><li>Release tagging requires updating both locale catalogs before the tag build.</li><li>Decision D164; extends D120 / ADR 0022.</li></ul><h2 id="_2026-07-29-—-release-process-mandatory-dual-locale-changelog-gate" tabindex="-1">2026-07-29 — Release process: mandatory dual-locale changelog gate <a class="header-anchor" href="#_2026-07-29-—-release-process-mandatory-dual-locale-changelog-gate" aria-label="Permalink to &quot;2026-07-29 — Release process: mandatory dual-locale changelog gate&quot;">​</a></h2><ul><li>Stable app version bumps / tags <strong>must</strong> update <code>packages/shared/src/changelog.ts</code> (EN + zh-CN) before the tag; shipping without catalog entries is a release process failure (D164).</li><li>Codified in the release runbook §4.1, AI development workflow matrix + forbidden practices, change checklist, <code>AGENTS.md</code>, and <code>scripts/release.mjs</code> header so agents and humans hit the same gate.</li><li>GitHub auto-generated release notes remain web-only.</li></ul><h2 id="_2026-07-29-—-bounded-atomic-tool-batches-in-context-checkpoints" tabindex="-1">2026-07-29 — Bounded atomic tool batches in context checkpoints <a class="header-anchor" href="#_2026-07-29-—-bounded-atomic-tool-batches-in-context-checkpoints" aria-label="Permalink to &quot;2026-07-29 — Bounded atomic tool batches in context checkpoints&quot;">​</a></h2><ul><li>Automatic compaction now bounds an oversized final parallel tool-result batch inside the checkpoint copy instead of repeatedly failing at the same transcript boundary.</li><li>The retained copy preserves every call/result envelope, distributes text budget fairly with explicit head/tail truncation markers, and omits duplicate provider-irrelevant details; original transcript rows remain complete.</li><li>Decision D158; amends ADR 0030&#39;s previous policy of blocking an indivisible batch above the retained-tail cap.</li></ul><h2 id="_2026-07-29-—-safe-lazy-mermaid-diagrams-in-assistant-answers" tabindex="-1">2026-07-29 — Safe lazy Mermaid diagrams in assistant answers <a class="header-anchor" href="#_2026-07-29-—-safe-lazy-mermaid-diagrams-in-assistant-answers" aria-label="Permalink to &quot;2026-07-29 — Safe lazy Mermaid diagrams in assistant answers&quot;">​</a></h2><ul><li>Completed <code>mermaid</code> fences in answer prose render through a dynamically loaded, theme-aware Mermaid chunk only near the viewport; partial streams and thinking prose remain source code.</li><li>Rendering is serialized and bounded at 20,000 source characters / 500 edges. Strict Mermaid configuration plus DOMPurify SVG sanitization removes links, embedded media, foreign HTML, and URL attributes before DOM insertion.</li><li>Invalid or oversized diagrams fall back to source with copy and view controls; no IPC, storage, process, CSP, or external-network boundary changes.</li><li>Decision D165.</li></ul><h2 id="_2026-07-31-—-slimmer-default-work-panel-width" tabindex="-1">2026-07-31 — Slimmer default work-panel width <a class="header-anchor" href="#_2026-07-31-—-slimmer-default-work-panel-width" aria-label="Permalink to &quot;2026-07-31 — Slimmer default work-panel width&quot;">​</a></h2><ul><li>The docked work panel now opens at 280px instead of 420px, a third narrower, and its fixed clamp becomes <code>244..720px</code>.</li><li><code>WORK_PANEL_DEFAULT_WIDTH</code>, <code>WORK_PANEL_MIN_WIDTH</code> (renderer and Electron Main), and the <code>.work-panel</code> CSS floor stay in sync; divider double-click restores the new default.</li><li>D154&#39;s 364px floor was sized for a 44px activity rail beside 320px of content; the rail became a header switcher, so the floor scales with the default.</li><li>Persisted wider widths remain valid, and the 720px maximum is unchanged.</li><li>Decision D167; supersedes the width clamp in D154/D163 and ADR 0033 §4.</li></ul><h2 id="_2026-07-31-—-project-archive-presentation-redesign-bands-superseded-by-d267" tabindex="-1">2026-07-31 — Project archive presentation redesign (bands superseded by D267) <a class="header-anchor" href="#_2026-07-31-—-project-archive-presentation-redesign-bands-superseded-by-d267" aria-label="Permalink to &quot;2026-07-31 — Project archive presentation redesign (bands superseded by D267)&quot;">​</a></h2><ul><li>Settings → Project archive is rebuilt as three bands: an overview banner with four derived counters, a search + Recent/Name sort toolbar, and a grouped index in the order Pinned / All projects / Archived with per-section counts.</li><li>Rows gain an always-visible disclosure control, state tags, a single meta line (path, branch, session count), a relative last-active time, and a quick New task action beside the row menu; the menu now groups create/edit above pin, archive/restore, and Close and dismisses on Escape or outside press.</li><li>Archived records stay grouped and softened rather than filtered, so the destination still has no visibility toggle (D133). Project storage, search matching, batch-of-eight session reveal, and activation semantics are unchanged.</li><li>Decision D168; supersedes D133&#39;s flat-list presentation. D267 later replaced the overview banner and the per-section panels with one quiet description line and one panel, retiring the four page-level counters; the row anatomy and grouping rules carry over, and the per-group counts remain on the strips.</li></ul><h2 id="_2026-07-31-—-plugins-page-redesign-presentation-amended-by-d196" tabindex="-1">2026-07-31 — Plugins page redesign (presentation amended by D196) <a class="header-anchor" href="#_2026-07-31-—-plugins-page-redesign-presentation-amended-by-d196" aria-label="Permalink to &quot;2026-07-31 — Plugins page redesign (presentation amended by D196)&quot;">​</a></h2><ul><li>The original Plugins page was rebuilt as four bands: an overview band with four derived counters (installed, enabled, updates, high-risk access), a header that keeps one contextual primary action and moves check-updates / apply-auto-updates / install-package / load-local into an overflow menu, a segmented Installed / Marketplace switch carrying counts, and the tab body.</li><li>Installed rows group by state as Needs attention / Updates available / Active / Turned off inside one hairline-separated panel. <code>status: &quot;error&quot; | &quot;load_error&quot;</code> and <code>errorMessage</code> are now surfaced instead of dropped; row actions collapse to a hover-revealed panel button plus an overflow menu (auto-update, Uninstall as a danger item) beside an always-visible switch.</li><li>Permissions are tiered from the risk column of <code>07-plugins/13-plugin-permissions-matrix.md</code>: risk-tinted chips with collapsed overflow on rows, and High / Medium / Low sections in the detail sheet and the install dialog. Upgrade reviews tag permissions the new version adds as New, and the install queue deduplicates the declared set against the diff.</li><li>Details move from a nested sidebar to a right-side sheet (scrim, Escape and outside-press dismiss, sticky install action, selectable version rows). Marketplace cards render a monogram glyph and never fetch <code>iconUrl</code>, so the renderer performs no remote image loads.</li><li>Decision D169; supersedes the flat list, pill tabs, and inline detail pane of <code>07-plugins/07-plugin-marketplace.md</code> §7/§14.</li></ul><h2 id="_2026-07-31-—-renderer-stylesheet-split-into-per-surface-partials" tabindex="-1">2026-07-31 — Renderer stylesheet split into per-surface partials <a class="header-anchor" href="#_2026-07-31-—-renderer-stylesheet-split-into-per-surface-partials" aria-label="Permalink to &quot;2026-07-31 — Renderer stylesheet split into per-surface partials&quot;">​</a></h2><ul><li><code>apps/desktop/src/styles/globals.css</code> becomes an import-only entry point. The rules move into 22 partials in the same directory, each owning one surface: <code>tokens</code>, <code>base</code>, <code>chrome</code>, <code>chat-shell</code>, <code>composer</code>, <code>sidebar-threads</code>, <code>messages</code>, <code>prose</code>, <code>ui-kit</code>, <code>overlays</code>, <code>theme-overrides</code>, <code>composer-menus</code>, <code>settings</code>, <code>destinations</code>, <code>projects</code>, <code>sessions</code>, <code>work-panel</code>, <code>providers</code>, <code>chat-links</code>, <code>composer-autocomplete</code>, <code>plugins</code>, <code>responsive</code>.</li><li>Import order <strong>is</strong> the cascade and must not be reordered: tokens and base first, feature layers in build order, the responsive / reduced-motion tail last so it can still override what precedes it.</li><li>The split is contiguous — no rule changed position relative to another. The joined partials reproduce the pre-split file byte for byte, and the built renderer CSS is byte-identical before and after, so Tailwind v4 resolves the <code>@theme</code> block from <code>tokens.css</code> unchanged.</li><li>Style assertions load the effective cascade through <code>apps/desktop/test/helpers/styles.mjs</code>, which inlines the local <code>@import</code> lines in declaration order. Tests must not read a partial directly.</li><li>Sidebar session styles live in both <code>sidebar-threads.css</code> and <code>sessions.css</code> because the original file interleaved them; the file headers cross-reference.</li><li>The design-token scales now live in <code>styles/tokens.css</code>; the guard in <code>scripts/check-style-tokens.mjs</code> walks the whole <code>src</code> tree and is unaffected.</li><li>Decision D170; the single-file layout assumed by <code>04-ux/07-ui-design-system.md</code> §Typography and <code>04-ux/08-component-spec.md</code> no longer holds.</li></ul><h2 id="_2026-07-31-—-plugin-skills-activation-and-the-plugin-devkit" tabindex="-1">2026-07-31 — Plugin skills activation and the plugin devkit <a class="header-anchor" href="#_2026-07-31-—-plugin-skills-activation-and-the-plugin-devkit" aria-label="Permalink to &quot;2026-07-31 — Plugin skills activation and the plugin devkit&quot;">​</a></h2><ul><li><code>contributes.skills</code> is activated. <code>PluginRuntime.getSkills()</code> reads each declared skill file at prompt time, only for plugins granted <code>agent.prompt.inject</code>, and only through the containment guard the gated <code>fs</code> APIs use. The agent runtime renders a <code># Plugin skills</code> section capped at 16 KiB total and 8 KiB per skill — its own budget, not the 32 KiB instruction chain of ADR 0037 — and orders it after the built-in skills but before project instructions, so a user&#39;s own files keep the last word. Runtime reuse keys on a skills digest, so enabling a plugin, revoking the permission, or editing a skill file retires the idle runtime instead of reusing a stale prompt. Closes roadmap gap R2.</li><li>Plugin authoring ships as a first-party package, <code>@pi-desktop/plugin-devkit</code>, which owns <code>scaffold</code> / <code>check</code> / <code>pack</code> over one implementation shared by the <code>pi-plugin</code> CLI, the <code>PluginScaffold</code> / <code>PluginCheck</code> / <code>PluginPack</code> agent tools served from Electron main, and the plugins page&#39;s New plugin from template action. <code>check</code> reproduces the rules host-core enforces, so passing it implies install will pass; <code>pack</code> writes store-only (method 0) <code>.piplug</code> entries because <code>extract_zip_bytes</code> accepts nothing else. Closes roadmap gap R3&#39;s template and <code>check</code>/<code>pack</code> items.</li><li>A bundled plugin was rejected as the delivery vehicle: a plugin cannot produce a <code>.piplug</code> (no archive API in <code>HOST_API_ALLOWLIST</code>) and scaffolding would need high-risk <code>fs.write.workspace</code> for a capability the application should provide itself.</li><li>The built-in <code>plugin-development</code> skill activates only for plugin workspaces — a plugin <code>manifest.json</code> at the workspace root, or a loaded development plugin inside it — so an ordinary session pays only for three tool descriptions.</li><li>Development plugins are watched and hot reload on save, debounced 300 ms, ignoring <code>node_modules</code> / <code>.git</code> / <code>dist</code> / <code>target</code>, capped at 16 plugins, and re-armed across restarts. A reload can never widen a permission set: the manifest is compared against the set approved when the folder was picked and a new permission stops the reload with <code>PERMISSION_DENIED</code>, while removed permissions do take effect. A failed reload keeps the watch so the fixing save recovers the plugin, and reports through a toast plus <code>pluginChanged</code> — host-core has no RPC for a runtime-side load failure, so the registry row does not move to <code>load_error</code>. Closes roadmap gap R3&#39;s hot-reload item.</li><li>Decision D171; recorded as ADR 0039.</li><li>The prompt-injection half of this decision was replaced the same day by D174: skills now reach the model as a catalog plus a <code>Skill</code> tool. The devkit, hot-reload and workspace-gate clauses stand unchanged.</li></ul><h2 id="_2026-07-31-—-creating-a-plugin-from-a-template-opens-the-folder" tabindex="-1">2026-07-31 — Creating a plugin from a template opens the folder <a class="header-anchor" href="#_2026-07-31-—-creating-a-plugin-from-a-template-opens-the-folder" aria-label="Permalink to &quot;2026-07-31 — Creating a plugin from a template opens the folder&quot;">​</a></h2><ul><li>Creating a plugin from a template now also opens the chosen folder as the active project (<code>workspace.set</code>, which registers the project and switches to chat), not just as a loaded development plugin. Loading only makes the plugin run; development needs the sources inside the workspace the agent, the file panel and the built-in <code>plugin-development</code> skill all read, and requiring the user to re-pick the same folder through Open folder was pure friction.</li><li>The activation runs in the renderer through the existing <code>activateProject</code> action rather than from the template IPC handler, so project state, the sidebar project list and the navigation intent guard keep their single owner.</li><li>The success toast distinguishes the two outcomes: if the folder cannot be opened as a project the plugin stays loaded and the toast says only that, instead of claiming a workspace that is not there.</li><li>Loading an existing local plugin folder (Load local plugin) deliberately keeps its current behavior: running someone else&#39;s plugin is not a reason to switch the user&#39;s project.</li><li>Decision D172; no ADR — this completes the flow ADR 0039 describes.</li></ul><h2 id="_2026-07-31-—-work-panel-header-menu-tools-first-no-duplicated-entries" tabindex="-1">2026-07-31 — Work panel header menu: tools first, no duplicated entries <a class="header-anchor" href="#_2026-07-31-—-work-panel-header-menu-tools-first-no-duplicated-entries" aria-label="Permalink to &quot;2026-07-31 — Work panel header menu: tools first, no duplicated entries&quot;">​</a></h2><ul><li>The unified header menu lists the four tools (Review, Terminal, Browser, Files) first, in a fixed order, and only then — after a divider, and only when they exist — the resources the transcript opened. The previous layout listed every open tool twice: once in the resource switcher and again in the create-new section, which made &quot;open&quot; and &quot;switch to&quot; indistinguishable. Each tool row now carries its own open state and its own close control, so a single row is the whole affordance for that tool.</li><li>Activating a tool that is already open activates its existing tab instead of replacing it with a fresh singleton, so the Browser keeps its URL. The header action cluster is pinned right (<code>margin-left: auto</code>) so the close/collapse controls no longer slide with the label length, and the trailing close slot in each row is always reserved so labels and open dots never shift.</li><li>Menu rows own real DOM focus (WAI-ARIA menu pattern) rather than a roving highlight: the trigger&#39;s ArrowDown/ArrowUp opens on the active or last row, Arrow/Home/End walk rows only, Delete/Backspace closes the focused row while the menu stays open with focus on its neighbor, and Escape/Tab/selection return focus to the trigger. Only a session switch dismisses the menu implicitly — selecting a row closes it explicitly, so the previous active-tab-keyed auto-dismiss (which fired whenever a background artifact changed the active tab) is gone.</li><li>Missing <code>panel.tabs.file</code> was the reason a bare Files tab showed a literal <code>file</code> label; the catalogs now carry it plus <code>panel.tools</code>, and the obsolete <code>panel.openTool</code> is removed.</li><li>Decision D173; no ADR — presentation and input handling only, inside the existing work-panel architecture (ADR 0033).</li></ul><h2 id="_2026-07-31-—-plugin-skills-are-model-invoked" tabindex="-1">2026-07-31 — Plugin skills are model-invoked <a class="header-anchor" href="#_2026-07-31-—-plugin-skills-are-model-invoked" aria-label="Permalink to &quot;2026-07-31 — Plugin skills are model-invoked&quot;">​</a></h2><ul><li><code>contributes.skills</code> is activated at load time behind <code>agent.prompt.inject</code>. Each entry may be a path or <code>{ path, id?, name?, description? }</code>; front matter in the document supplies <code>name</code> / <code>description</code> when the manifest does not.</li><li>The base system prompt carries only a catalog — skill id, name, and a description trimmed to 240 chars. Bodies are not in the prompt; the model fetches one through a built-in <code>Skill</code> tool that Electron main serves locally against <code>plugins.loadSkillBody(id)</code>, so the sidecar never holds skill text.</li><li>Caps: 32 skills per plugin, 128KB per document. A manifest without <code>agent.prompt.inject</code> still validates — skills predate the permission gate — and the runtime simply skips them.</li><li>Skill ids join the runtime-reuse key in <code>packages/agent-runtime/src/runtime.ts</code>, so enabling or disabling a plugin rebuilds the runtime instead of serving a stale catalog.</li><li>Rejected: user-facing slash commands. A skill is guidance the agent should reach for when a task calls for it, not a command the user has to know exists.</li><li>Decision D174; closes the &quot;parsed but never activated&quot; gap in <code>07-plugins/14-plugin-roadmap.md</code> R2.</li></ul><h2 id="_2026-09-11-—-user-invoked-skills-in-the-composer-d393" tabindex="-1">2026-09-11 — User-invoked Skills in the composer (D393) <a class="header-anchor" href="#_2026-09-11-—-user-invoked-skills-in-the-composer-d393" aria-label="Permalink to &quot;2026-09-11 — User-invoked Skills in the composer (D393)&quot;">​</a></h2><ul><li>D174&#39;s model-invoked catalog remains the Skill body-loading and security contract, but its rejection of a user-facing slash entry made active Skills difficult to discover for users who already knew the workflow they wanted.</li><li>D393 / ADR 0219 adds active built-in, plugin, and user Skills as the final group in the composer <code>/</code> menu. Selection inserts the exact id; Electron main revalidates the current project scope and asks the model to call the local <code>Skill</code> tool. The typed command remains the transcript chip, and no host protocol or durable schema changes.</li></ul><h2 id="_2026-09-11-—-windows-work-panel-chrome-keeps-one-resource-action-cluster-d394" tabindex="-1">2026-09-11 — Windows work-panel chrome keeps one resource action cluster (D394) <a class="header-anchor" href="#_2026-09-11-—-windows-work-panel-chrome-keeps-one-resource-action-cluster-d394" aria-label="Permalink to &quot;2026-09-11 — Windows work-panel chrome keeps one resource action cluster (D394)&quot;">​</a></h2><ul><li>The open work-panel header now keeps one compact resource switcher. Resource close is available from the existing unified context-menu rows, so a second <code>X</code> does not sit beside the Windows native close control.</li><li>The viewport-fixed work-panel toggle remains the sole panel-level collapse control. Subagent detail uses a back chevron, and Windows/Linux native controls remain fixed at the window edge.</li><li>Decision D394 amends D154 / D357 / ADR 0195. This is renderer-only and does not change panel state, window geometry, IPC, protocol, or storage. See ADR 0220 and E2E-067.</li></ul><h2 id="_2026-09-11-—-canonical-thinking-level-values-in-the-ui-d395" tabindex="-1">2026-09-11 — Canonical thinking-level values in the UI (D395) <a class="header-anchor" href="#_2026-09-11-—-canonical-thinking-level-values-in-the-ui-d395" aria-label="Permalink to &quot;2026-09-11 — Canonical thinking-level values in the UI (D395)&quot;">​</a></h2><ul><li>Thinking levels are stable protocol values, but translating them made the same provider/runtime setting vary with the application locale.</li><li>Decision D395 / ADR 0221 amends D369 / ADR 0202: Composer, model configuration, and delegation surfaces render <code>off</code>, <code>minimal</code>, <code>low</code>, <code>medium</code>, <code>high</code>, <code>xhigh</code>, and <code>max</code> directly. These values are removed from every locale catalog; effective metadata, clamping, provider requests, protocol, and storage are unchanged. See E2E-219.</li></ul><h2 id="_2026-09-11-—-renderer-and-plugin-panel-scrollbars-share-one-compact-contract-d396" tabindex="-1">2026-09-11 — Renderer and plugin-panel scrollbars share one compact contract (D396) <a class="header-anchor" href="#_2026-09-11-—-renderer-and-plugin-panel-scrollbars-share-one-compact-contract-d396" aria-label="Permalink to &quot;2026-09-11 — Renderer and plugin-panel scrollbars share one compact contract (D396)&quot;">​</a></h2><ul><li>Windows&#39; classic scrollbar made the right-side work-panel Files view visibly heavier than the conversation even though the host renderer already had a quiet custom scrollbar. The sidebar also retained a separate width and thumb opacity override, so the app had two scrollbar treatments.</li><li>Decision D396 amends D300: every renderer scroll container uses the same 6px, trackless, transparent-at-rest scrollbar with the same hover, focus-within, scroll-reveal, and dragged-thumb states. The sidebar-specific override is removed. The plugin-panel preload applies the same contract and 300ms reveal mark to docked and detached plugin documents, including the bundled Files view. An external page loaded inside the Browser guest remains page-owned.</li><li>This is a presentation-only change; there is no protocol, storage, host runtime, or external-page behavior change. See <code>04-ux/07-ui-design-system.md</code>, <code>04-ux/08-component-spec.md</code>, and E2E-157.</li></ul><h2 id="_2026-09-11-—-composer-accepts-native-file-and-folder-drops-d397" tabindex="-1">2026-09-11 — Composer accepts native file and folder drops (D397) <a class="header-anchor" href="#_2026-09-11-—-composer-accepts-native-file-and-folder-drops-d397" aria-label="Permalink to &quot;2026-09-11 — Composer accepts native file and folder drops (D397)&quot;">​</a></h2><ul><li>Native file-system drops into the Composer now prevent the browser default and use one accent outline around the complete shell without changing its layout. Regular files reuse the bounded session-scratch paste bridge and become removable leaf-name chips; folders are never read, traversed, or copied.</li><li>The preload resolves the path of each user-dropped <code>File</code> with Electron&#39;s <code>webUtils.getPathForFile</code>. The renderer inserts a dropped folder&#39;s complete native path at the caret as literal <code>@&lt;path&gt;/</code> text, preserving mixed item order, the surrounding draft, focus, and the caret after asynchronous file saving. No host RPC, protocol, workspace, or durable-schema change is added.</li><li>Decision D397 amends ADR 0101&#39;s previous drag/drop scope. See ADR 0222 and E2E-102i.</li></ul><h2 id="_2026-09-11-—-message-quotes-and-renderer-owned-side-chats-d-local-message-quotes" tabindex="-1">2026-09-11 — Message quotes and renderer-owned side chats (D-LOCAL-message-quotes) <a class="header-anchor" href="#_2026-09-11-—-message-quotes-and-renderer-owned-side-chats-d-local-message-quotes" aria-label="Permalink to &quot;2026-09-11 — Message quotes and renderer-owned side chats (D-LOCAL-message-quotes)&quot;">​</a></h2><p><em>(Retired by ADR 0268 on 2026-09-16: the feature and its surfaces were removed, and these IDs stay retired and must not be reused. The record is kept for history.)</em></p><ul><li>Every user message and every assistant turn gains a Quote action beside Copy, Edit, Delete, Fork, and Retry. It inserts a <code>&gt; </code>-prefixed Markdown blockquote plus a <code>chat.quoteSource</code> attribution line into the active session&#39;s composer draft, uses the live text selection when it lies inside the clicked message row and the message&#39;s own text otherwise, caps the excerpt at 2000 characters with a trailing ellipsis, focuses the composer, and never sends.</li><li>Open side chat forks the anchored assistant or user message through the existing <code>session.fork</code> without activating the child, so the main conversation keeps its visible session. The child is registered as a renderer-owned side chat of the parent and the work panel opens one <code>sidechat</code> tab that streams from the same event stream through the shared background-transcript reducer, with Add to main chat, Open as a conversation, a compact Send/Stop input, and the existing permission card.</li><li>Closing the tab or opening the child as a conversation removes the registration and its transcript projection; the durable child remains an ordinary session in the sidebar, session lists, and search. No protocol, storage-schema, IPC channel, or permission change. See ADR message-quotes-and-side-chats and E2E-CHAT-quote-prefill through E2E-CHAT-side-chat-close.</li></ul><h2 id="_2026-09-11-—-context-usage-display-preference-d398" tabindex="-1">2026-09-11 — Context usage display preference (D398) <a class="header-anchor" href="#_2026-09-11-—-context-usage-display-preference-d398" aria-label="Permalink to &quot;2026-09-11 — Context usage display preference (D398)&quot;">​</a></h2><ul><li>The context usage inspector&#39;s leading figure — the trigger ring arc (<code>strokeDashoffset</code>), percentage, token label, popover heading, tooltip, and <code>aria-label</code> — is configurable via <code>AppSettings.contextUsageDisplay</code> (<code>&quot;remaining&quot;</code> or <code>&quot;used&quot;</code>). Default and fallback for absent or unrecognised values is <code>&quot;remaining&quot;</code>.</li><li>When <code>&quot;used&quot;</code>, the ring fills by <code>usedRatio</code>, and text shows the used-capacity pair instead of the remaining pair.</li><li>Warning and critical color thresholds (remaining ≤ 25 % / ≤ 10 %) stay based on remaining capacity regardless of display mode.</li><li>Settings → AI → Defaults adds a segmented control (Remaining / Used) after Link open destination and before Enter-to-send.</li><li>Decision D398 amends D347 / ADR 0184. See ADR 0223 and E2E-250.</li></ul><h2 id="_2026-09-11-—-project-groups-can-be-manually-reordered-d399" tabindex="-1">2026-09-11 — Project groups can be manually reordered (D399) <a class="header-anchor" href="#_2026-09-11-—-project-groups-can-be-manually-reordered-d399" aria-label="Permalink to &quot;2026-09-11 — Project groups can be manually reordered (D399)&quot;">​</a></h2><ul><li>Retained project groups expose a grip that appears on hover and keyboard focus. Native drag/drop inserts the source before or after the target based on the pointer position; ArrowUp/ArrowDown on the focused grip provides the keyboard path, and Escape cancels an active drag.</li><li>Reordering writes contiguous normalized-path <code>order</code> values and switches the renderer-local project sort to <code>manual</code>. Pinned and archived priority, project activation, host workspace identity, session ordering, and on-disk directories are unchanged.</li><li>Decision D399 amends D093. No protocol, host, IPC, or durable project-schema change is introduced; see the sidebar interaction specs and E2E-253.</li></ul><h2 id="_2026-09-11-—-restore-deferred-tools-from-effective-session-context-d400" tabindex="-1">2026-09-11 — Restore deferred tools from effective session context (D400) <a class="header-anchor" href="#_2026-09-11-—-restore-deferred-tools-from-effective-session-context-d400" aria-label="Permalink to &quot;2026-09-11 — Restore deferred tools from effective session context (D400)&quot;">​</a></h2><ul><li>A new prompt or mode switch clears the in-memory deferred activation set, then restores successful activation evidence from the effective session context. <code>ToolSearch</code> rows contribute <code>addedToolNames</code>; successful deferred-tool rows contribute their own tool name.</li><li>Restoration keeps only names still in the current mode&#39;s deferred catalog. Failed, interrupted, and missing-result placeholder rows are ignored, and assistant/user prose is never parsed as activation evidence. Host permissions, workspace containment, and audit behavior remain unchanged.</li><li>Decision D400 amends D185 / ADR 0048. See ADR 0225 and E2E-008a.</li></ul><h2 id="_2026-07-31-—-plugin-themes-ship-css-files" tabindex="-1">2026-07-31 — Plugin themes ship CSS files <a class="header-anchor" href="#_2026-07-31-—-plugin-themes-ship-css-files" aria-label="Permalink to &quot;2026-07-31 — Plugin themes ship CSS files&quot;">​</a></h2><ul><li><code>contributes.themes</code> declares <code>{ id, label, path, base? }</code> and requires <code>ui.theme</code>. <code>path</code> is a plugin-relative <code>.css</code> file; <code>base</code> (<code>light</code> | <code>dark</code>, default <code>dark</code>) names the palette the overrides layer on.</li><li>The main process reads the file and runs <code>sanitizeThemeCss()</code>: no <code>@import</code>, no <code>url()</code> outside <code>data:</code>, no unparseable <code>url(</code>, no <code>javascript:</code> / <code>expression(</code>, no markup sequences, 256KB cap, 8 themes per plugin. The renderer receives finished text over <code>plugin/themes</code> and injects it into one <code>&lt;style id=&quot;pi-plugin-theme&quot;&gt;</code> appended after the app&#39;s own stylesheets.</li><li><code>AppSettings.theme</code> widens to <code>plugin:&lt;pluginId&gt;:&lt;themeId&gt;</code>. When the providing plugin is disabled, uninstalled, or fails to load, the app falls back to <code>system</code> rather than rendering an unstyled shell.</li><li>Rejected: a token-JSON contribution. It would have been safer to validate, but it can only express the tokens we thought to enumerate; a stylesheet lets a theme reach a surface the token list forgot, and the sanitizer plus append-order rule bound the risk to appearance.</li><li>Decision D175.</li></ul><h2 id="_2026-07-31-—-plugin-mcp-servers-over-stdio-and-remote-http" tabindex="-1">2026-07-31 — Plugin MCP servers over stdio and remote HTTP <a class="header-anchor" href="#_2026-07-31-—-plugin-mcp-servers-over-stdio-and-remote-http" aria-label="Permalink to &quot;2026-07-31 — Plugin MCP servers over stdio and remote HTTP&quot;">​</a></h2><ul><li><code>contributes.mcpServers</code> declares <code>{ id, label?, transport }</code> plus exactly one transport&#39;s fields: <code>stdio</code> takes <code>command</code> / <code>args</code> / <code>env</code>, <code>http</code> takes <code>url</code> / <code>headers</code>. Permissions are separate: <code>mcp.server.local</code> for stdio, <code>mcp.server.remote</code> for HTTP.</li><li><code>apps/desktop/electron/main/plugin-mcp.ts</code> speaks protocol <code>2025-06-18</code> — <code>initialize</code>, <code>tools/list</code>, <code>tools/call</code> — as NDJSON over stdio or streamable HTTP/SSE. Budgets: 10s connect, 100s per call, 8 <code>tools/list</code> pages, 4MB per stdio line, 64 tools per server, 8 servers per plugin. Connection is lazy; teardown follows unload.</li><li>Discovered tools register as <code>plugin_&lt;pluginIdSafe&gt;_&lt;serverId&gt;_&lt;toolName&gt;</code> in the existing plugin tool map, so they inherit the audit trail, the timeout, and the disable switch with no new routing. They are always <code>risk: &quot;medium&quot;</code>: the schema and description come from a third party, so a self-declared risk level is not trustworthy.</li><li><code>env</code> and <code>headers</code> resolve only from the plugin&#39;s own settings via <code>{ &quot;setting&quot;: &quot;&lt;key&gt;&quot; }</code>; the host environment is never passed through (D018). A stdio child gets <code>PATH</code>, temp/locale vars, and the declared values — nothing else. <code>command</code> must be a bare PATH name or plugin-relative; <code>url</code> must be <code>https</code> unless the host is loopback.</li><li>Both transports ship rather than stdio alone: a hosted MCP endpoint is common enough that stdio-only would have pushed plugins to wrap it in a local shim, which is strictly worse — an extra process and an unreviewable proxy.</li><li>Decision D176; ADR <a href="./../../adr/0038-plugin-mcp-bridge">0038</a>.</li></ul><h2 id="_2026-07-31-—-resident-plugin-services-and-their-restart-policy" tabindex="-1">2026-07-31 — Resident plugin services and their restart policy <a class="header-anchor" href="#_2026-07-31-—-resident-plugin-services-and-their-restart-policy" aria-label="Permalink to &quot;2026-07-31 — Resident plugin services and their restart policy&quot;">​</a></h2><ul><li><code>contributes.services</code> declares <code>{ id, label?, autoRestart? }</code> behind <code>background.service</code>, at most 4 per plugin. The plugin calls <code>pi.services.register({ id, start, stop })</code>; the broker calls <code>start</code> after <code>onLoad</code> (5s budget) and <code>stop</code> before <code>onUnload</code>, so a service is never live outside the plugin&#39;s own lifetime.</li><li>A service lives in the plugin&#39;s <code>utilityProcess</code>, so a crash takes it down with the process and the supervisor restarts the whole plugin: backoff 1s, 2s, 4s, 8s, 16s capped at 30s; at most 5 attempts; a process that survives 60s is healthy and the counter resets. <code>autoRestart: false</code> opts out. After the last attempt the plugin stays <code>failed</code> — a visible failure beats a silent crash loop.</li><li>Per-service state (<code>starting</code> | <code>running</code> | <code>stopped</code> | <code>failed</code>) and the restart count are read over <code>plugin/services</code> and rendered as chips on the Plugins page; every transition emits <code>pluginChanged</code> with <code>reason: &quot;service&quot;</code> and a <code>plugin.service.*</code> audit entry.</li><li>Manual enable / disable outranks the supervisor: an explicit action cancels the pending timer and clears the attempt counter.</li><li>Decision D177; ADR <a href="./../../adr/0040-plugin-resident-services-and-message-bus">0040</a>.</li></ul><h2 id="_2026-07-31-—-inter-plugin-message-bus-routes-declared-topics-only" tabindex="-1">2026-07-31 — Inter-plugin message bus routes declared topics only <a class="header-anchor" href="#_2026-07-31-—-inter-plugin-message-bus-routes-declared-topics-only" aria-label="Permalink to &quot;2026-07-31 — Inter-plugin message bus routes declared topics only&quot;">​</a></h2><ul><li><code>pi.bus.publish</code> / <code>subscribe</code> require <code>bus.publish</code> / <code>bus.subscribe</code> <strong>and</strong> a matching entry in <code>contributes.bus</code>: publishers list concrete topics, subscribers list patterns. A granted permission alone routes nothing, so the manifest stays a complete description of what a plugin says and hears.</li><li>Topics are dot-separated segments (<code>[a-zA-Z0-9][a-zA-Z0-9_-]*</code>, ≤8 segments, ≤128 chars). <code>*</code> matches one segment; <code>**</code> matches one or more trailing segments and may appear only last.</li><li>Routing lives in the broker. A message carries <code>topic</code>, <code>from</code>, <code>payload</code>, and a host-assigned <code>at</code>; the publisher is excluded from its own fan-out; delivery is fire-and-forget over a new one-way <code>{ t: &quot;event&quot; }</code> frame, so a wedged subscriber cannot stall the sender. That frame also makes the previously stubbed <code>pi.events.on</code> / <code>off</code> real.</li><li>Caps: 64KB per payload, 16 subscriptions per plugin, 100 publishes per rolling 10s window, failing with <code>LIMIT_EXCEEDED</code> / <code>RATE_LIMITED</code> and an audit line.</li><li>A payload conveys data, never capability: receiving a message grants the subscriber nothing it did not already hold, so a topic should be treated as public within the app.</li><li>Decision D178; ADR <a href="./../../adr/0040-plugin-resident-services-and-message-bus">0040</a>.</li></ul><h2 id="_2026-08-02-—-bash-tool-inherits-the-user-s-login-shell-path" tabindex="-1">2026-08-02 — Bash tool inherits the user&#39;s login-shell PATH <a class="header-anchor" href="#_2026-08-02-—-bash-tool-inherits-the-user-s-login-shell-path" aria-label="Permalink to &quot;2026-08-02 — Bash tool inherits the user&#39;s login-shell PATH&quot;">​</a></h2><ul><li>On Unix, the first Bash call probes the user&#39;s login shell for its PATH — <code>$SHELL</code> (fallback <code>/bin/zsh</code> → <code>/bin/bash</code> → <code>/bin/sh</code>) with <code>-lic &#39;printf %s &quot;$PATH&quot;&#39;</code> — so <code>-l</code> sources login files and <code>-i</code> sources the interactive rc, matching a fresh terminal. The probe is bounded to 5s and cached per process (<code>OnceLock</code>); only the last stdout line is kept (rc banners are ignored), stderr is discarded (missing-tty noise), and a non-zero exit, missing shell, or timeout silently falls back to the host PATH.</li><li>Every Bash subprocess gets the probed PATH injected via <code>cmd.env(&quot;PATH&quot;, ...)</code>; <code>bash -lc</code> still re-runs the bash profile at startup (conda/brew hooks may prepend/dedupe/reorder entries on top of the injected base). Agent commands remain POSIX bash; the resolved bash binary is unchanged. Windows keeps <code>bash -c</code> with the host environment (no change).</li><li>Fixes macOS Finder/Dock launches where <code>bash -lc</code> alone cannot see nvm, pnpm, or Homebrew tooling initialized in <code>~/.zshrc</code> / <code>~/.zprofile</code>.</li><li>Decision D181; ADR <a href="./../../adr/0045-bash-inherits-user-login-path">0045</a>.</li></ul><h2 id="_2026-08-02-—-route-process-logs-into-category-files" tabindex="-1">2026-08-02 — Route process logs into category files <a class="header-anchor" href="#_2026-08-02-—-route-process-logs-into-category-files" aria-label="Permalink to &quot;2026-08-02 — Route process logs into category files&quot;">​</a></h2><ul><li>The <code>app</code>, <code>host</code>, and <code>agent</code> channels remain local NDJSON files, but each channel is now a directory containing focused <code>&lt;category&gt;.log</code> files. App records use explicit lifecycle/session/tool/permission/plugin/provider/ persistence/updater/diagnostics/terminal/runtime categories; host and agent stderr is classified into the same categories, with timing lines isolated in <code>timing.log</code>.</li><li>Every record carries a <code>category</code> field. Child stderr is buffered by line, decoded as UTF-8, and stripped of ANSI control sequences before it is persisted. Unknown child output goes to <code>runtime.log</code>.</li><li>Rotation remains 5 MB with two rotated files, but the limit applies to each category file. The logger uses byte length for UTF-8 records and treats rotation and disk failures as best effort. Existing flat log files are not deleted during migration.</li><li>Decision D182; ADR <a href="./../../adr/0046-categorized-process-logs">0046</a>.</li></ul><h2 id="_2026-08-02-—-context-usage-inspector" tabindex="-1">2026-08-02 — Context usage inspector <a class="header-anchor" href="#_2026-08-02-—-context-usage-inspector" aria-label="Permalink to &quot;2026-08-02 — Context usage inspector&quot;">​</a></h2><ul><li>Replace the oversized context ring with a compact Codex-style trigger that combines a remaining-capacity ring, <code>Context</code> label, and percentage. Hover and keyboard focus open a non-modal panel <em>(superseded by D225: the trigger is click-toggled)</em> with the context window, exact provider input/output/cache/reasoning usage, aggregate generation speed in <code>tokens/s</code>, and each unique tool type in first-seen execution order.</li><li>Tool rows aggregate repeated calls and expose call count, argument tokens, result tokens, total estimated footprint, share bar, and cumulative known duration. Runtime estimates use pi-agent-core&#39;s existing four-characters- per-token heuristic; provider-reported usage remains the authoritative total and the UI labels tool rows as estimates.</li><li>Generation speed is a completed-turn snapshot from provider output and final stream duration; active assistant streams do not show a live token-rate counter.</li><li>The context-window total comes from the matching <code>pi-ai</code> model metadata used by the agent sidecar; provider metadata and the 128K default remain fallbacks for unknown models.</li><li><code>UiMessage.responseDurationMs</code>, <code>UiMessage.toolUsage</code>, and the optional <code>tool_end.toolUsage</code> event field are additive, so older persisted messages and peers remain readable.</li><li>The inspector panel is rendered at the document body level as a fixed, collision-aware viewport overlay. It follows transcript scroll and window resize, flips around the trigger, and clamps to viewport margins instead of being clipped by the transcript scroll container.</li><li>The inspector resolves its context-window total from the same <code>pi-ai</code> model record passed to the agent sidecar, enriching cached/discovered model rows; provider metadata and the 128K default remain fallbacks for unknown models.</li><li>Decision D184; ADR <a href="./../../adr/0047-context-usage-inspector">0047</a>.</li></ul><h2 id="_2026-08-02-—-lazy-per-turn-tool-activation" tabindex="-1">2026-08-02 — Lazy per-turn tool activation <a class="header-anchor" href="#_2026-08-02-—-lazy-per-turn-tool-activation" aria-label="Permalink to &quot;2026-08-02 — Lazy per-turn tool activation&quot;">​</a></h2><ul><li>The sidecar keeps a complete local registry but sends only the mode&#39;s core tools and local <code>ToolSearch</code> on a new prompt. (<code>CompactContext</code> was also always-active here until D200 removed the tool; D203 restores it as <code>new_context</code>, again always-active.) Agent follows pi&#39;s coding-agent core (<code>Read</code>/<code>Bash</code>/<code>Edit</code>/<code>Write</code>), while Chat keeps (<code>Read</code>/<code>Glob</code>/<code>Grep</code>). Agent-mode <code>Glob</code>/<code>Grep</code>, <code>BrowserPreview</code>, plugin tools, <code>Skill</code>, and plugin-development helpers are represented by bounded compact catalog entries instead of full parameter schemas.</li><li>An exact-name or capability search activates at most four matches for the next model turn. <code>addedToolNames</code> lets pi-ai providers with native deferred search serialize those definitions at the load point; other providers use the rebuilt active tool list. Activation resets before the next user prompt.</li><li>Host permissions, workspace and scratch containment, timeouts, and audit behavior are unchanged. Persisted tool results retain activation markers for valid transcript reconstruction.</li><li>Decision D185; ADR <a href="./../../adr/0048-lazy-per-turn-tool-activation">0048</a>.</li></ul><h2 id="_2026-08-04-—-bound-provider-stream-recovery-and-diagnostics" tabindex="-1">2026-08-04 — Bound provider stream recovery and diagnostics <a class="header-anchor" href="#_2026-08-04-—-bound-provider-stream-recovery-and-diagnostics" aria-label="Permalink to &quot;2026-08-04 — Bound provider stream recovery and diagnostics&quot;">​</a></h2><ul><li>Provider request setup now has one bounded pi-ai retry. A transient stream, network, or timeout failure after streaming begins gets one same-turn retry with a short abortable backoff; the failed assistant is removed from model context and the visible assistant id is reused.</li><li><code>terminated</code> and equivalent incomplete stream messages map to <code>STREAM_FAILED</code>. A second failure remains terminal and carries bounded phase, timing, provider status/code, and retry-attempt diagnostics when available.</li><li>Mutation recovery is explicitly finite: use <code>Edit</code> for one unique local replacement, use <code>Write</code> for a coherent rewrite, then allow one fresh read/regeneration after a mismatch. A second same-path <code>Edit</code> failure or failed shell patch command emits the terminating tool hint instead of repairing an old patch artifact.</li><li>Decision D186; see <a href="./../../adr/0050-bounded-provider-stream-recovery">ADR 0050</a>.</li></ul><h2 id="_2026-08-04-—-recover-automatic-compaction-failures-with-a-retained-tail" tabindex="-1">2026-08-04 — Recover automatic compaction failures with a retained tail <a class="header-anchor" href="#_2026-08-04-—-recover-automatic-compaction-failures-with-a-retained-tail" aria-label="Permalink to &quot;2026-08-04 — Recover automatic compaction failures with a retained tail&quot;">​</a></h2><ul><li>Automatic threshold and provider-overflow compaction failures now attempt a deterministic, aggressively bounded retained-tail checkpoint before ending the turn. The previous checkpoint summary is preserved when available, the complete visible transcript remains untouched, and host persistence plus the hard-budget recheck remain mandatory.</li><li>The summary input is preflighted against the model window so an obviously oversized summary request goes directly to the bounded recovery path instead of waiting for a provider rejection or timeout.</li><li>The lifecycle event marks recovery with <code>fallback: &quot;retained_tail&quot;</code>, so the renderer keeps the run active and shows a warning. Manual <code>/compact</code> remains fail-fast and never silently discards historical context.</li><li>Decision D158; amends ADR 0030 and adds ADR 0049.</li></ul><h2 id="_2026-08-04-—-resource-isolated-host-rpc-stdio" tabindex="-1">2026-08-04 — Resource-isolated host RPC stdio <a class="header-anchor" href="#_2026-08-04-—-resource-isolated-host-rpc-stdio" aria-label="Permalink to &quot;2026-08-04 — Resource-isolated host RPC stdio&quot;">​</a></h2><ul><li>Host-core no longer uses Tokio&#39;s stdio adapters for its NDJSON control pipe. One named OS thread reads stdin and one named OS thread serializes stdout, keeping per-message framing and retrying interrupted or transient <code>EAGAIN</code>/<code>EWOULDBLOCK</code> errors.</li><li>This closes the process-exit path where Tokio&#39;s blocking pool panicked after the OS refused another worker thread with <code>Resource temporarily unavailable</code> (errno 35 on macOS). Failure to create a control thread is reported as a startup error; it is not an unhandled thread-spawn panic.</li><li>The login-shell PATH probe uses <code>thread::Builder</code> as well and falls back to the inherited PATH when the optional helper cannot start.</li><li>Decision D187; see <a href="./../../adr/0051-host-rpc-stdio-resource-isolation">ADR 0051</a>.</li></ul><h2 id="plan-checkpoint-and-shell-decisions-0-4-14" tabindex="-1">Plan checkpoint and shell decisions (0.4.14) <a class="header-anchor" href="#plan-checkpoint-and-shell-decisions-0-4-14" aria-label="Permalink to &quot;Plan checkpoint and shell decisions (0.4.14)&quot;">​</a></h2><p>The earlier local Plan operating-state decision remains recorded as D188 for history. D189 is the implementation authority and supersedes D188 and ADR 0052. D190 defines the shell execution contract used by D189. The agent-only decision developed in parallel is renumbered D191 (ADR 0055) and is superseded by D188/D189; the three decisions that shipped alongside it follow as D192, D193, and D194.</p><table tabindex="0"><thead><tr><th>ID</th><th>Topic</th><th>Decision</th><th>Rationale</th></tr></thead><tbody><tr><td>D188</td><td>Replace Chat profile with Plan state</td><td><em>(superseded by D189)</em> **PI-Desktop has one pi Agent and one product selector: \`Agent</td><td>Plan<code>. Plan is that Agent after entering planning state, never a second Agent, planner model, planner service, or permission mode. Agent remains the default. Persisted sessions, app defaults, and scheduled values stored as </code>chat<code>migrate to</code>plan<code>; the internal </code>page = &quot;chat&quot;<code>route may remain as a conversation-surface detail. Plan exposes</code>Read<code>, </code>Glob<code>, </code>Grep<code>, </code>BrowserPreview<code>, </code>Bash<code>, </code>CompactContext<code>, and </code>ExitPlanMode<code>; it denies </code>Write<code>, </code>Edit<code>, plugin tools, and unknown tools. Plan retains permission-mode selection: Bash prompts under </code>ask<code>and</code>accept-edits<code>, and runs without confirmation under </code>auto\`, so Plan is planning intent rather than a strict read-only security profile.**</td></tr><tr><td>D189</td><td>Plan checkpoint artifact, approval, and execution epoch</td><td>**The same pi Agent uses \`Agent</td><td>Plan<code>, with Agent default. Plan calls </code>SubmitPlan(title, markdown, question)<code>as the only tool in its assistant batch. Rust host-core writes the submitted Markdown bytes unchanged to a new immutable unique file under</code>&lt;workspaceRoot&gt;/.pi/plan/*.md<code>; it stores the relative artifact path, SHA-256, and byte size together with structured title/question fields in the existing </code>plan_approvals<code>row. No title/question wrapper is added and no prior artifact is replaced. The approval surface displays title, question, an artifact opener, absolute expiry, and status, and offers only Approve or Reject. Approve requires an explicit</code>ask<code>, </code>accept-edits<code>, or </code>auto<code>permission mode, with Ask selected by default; Reject carries no mode. The approval expires at one absolute 30-minute deadline and uses</code>PLAN_APPROVAL_TIMEOUT<code>. The same </code>plan_approvals<code>row carries</code>execution_id<code>and</code>execution_state<code>through</code>queued → running → completed</td></tr><tr><td>D190</td><td>Selectable command shell catalog and execution identity</td><td><strong>Host-core exposes stable platform-aware catalog IDs: <code>windows-powershell</code>, <code>cmd</code>, <code>git-bash</code>, and <code>bash</code>; the platform catalog contains only IDs supported by that platform. <code>defaultCommandShell</code> persists in host settings, and settings writes reject unavailable or wrong-platform IDs. If a persisted choice later becomes unavailable, the effective shell intentionally falls back to the first available platform shell. The <code>Bash</code> tool and <code>tools.execute</code> protocol name remain unchanged; each turn pins the effective shell ID and dialect, and host rejects a stale ID/dialect before spawn with <code>COMMAND_SHELL_CHANGED</code>. Shell identity is the catalog selection, not an executable path hash. Bash streams stdout and stderr separately, uses a mandatory 60-second default timeout with a 1–300 second override, and cancellation/timeout shuts down the complete process tree.</strong></td><td>Users can choose the command language without multiplying protocol tools, while platform validation, explicit fallback, and turn-pinned catalog identity keep execution predictable</td></tr></tbody></table><h2 id="_2026-08-05-—-agent-only-mode" tabindex="-1">2026-08-05 — Agent-only mode <a class="header-anchor" href="#_2026-08-05-—-agent-only-mode" aria-label="Permalink to &quot;2026-08-05 — Agent-only mode&quot;">​</a></h2><ul><li><code>agent</code> is the only session mode. The former <code>chat</code> profile is renamed <code>read-only</code>; host-core still hard-denies Write/Edit/Bash for it, but nothing in the UI can select, display, or command it.</li><li>The mode gate is negative everywhere (<code>mode != &quot;agent&quot;</code> → read-only surface) and <code>chat</code> normalizes to <code>read-only</code> on every host write path, so a legacy or unknown value fails closed instead of widening tools.</li><li><code>BASH_DISABLED_IN_CHAT</code> / <code>WRITE_DISABLED_IN_CHAT</code> become <code>BASH_DISABLED_IN_READ_ONLY</code> / <code>WRITE_DISABLED_IN_READ_ONLY</code>.</li><li>Database open rewrites <code>sessions.mode = &#39;chat&#39;</code> and a stored <code>defaultMode</code> of <code>chat</code> to <code>agent</code> — a data fix-up inside schema v7, not a version bump.</li><li>Decision D191, superseded by D189; see <a href="./../../adr/0055-agent-only-mode">ADR 0055</a>. The mode concept it removed returned as Plan (D188/D189, ADR 0052/0053); the entry is retained because the <code>read-only</code> normalization and the <code>chat</code> fix-up it describes shipped.</li></ul><h2 id="_2026-08-05-—-structured-tool-result-presentation-in-the-transcript" tabindex="-1">2026-08-05 — Structured tool-result presentation in the transcript <a class="header-anchor" href="#_2026-08-05-—-structured-tool-result-presentation-in-the-transcript" aria-label="Permalink to &quot;2026-08-05 — Structured tool-result presentation in the transcript&quot;">​</a></h2><ul><li>Expanded tool rows no longer render <code>JSON.stringify</code> of the arguments and the result. A pure renderer module maps each known payload to labeled blocks: file and written content as highlighted code, commands as shell, stdout and stderr as separate blocks, Glob results as clickable paths, Grep hits grouped per file, failures as an error note. Unmapped plugin payloads degrade to label/value fields and labeled blocks; only nested objects keep a JSON body.</li><li>The pi-ai result envelope carries the structured payload in <code>details</code> and repeats it as text for the model. Only the structured half is rendered, so no byte is shown twice.</li><li>Collapsed rows carry outcome chips (exit code, match/file counts, replacement count, written or read size, <code>truncated</code>, <code>scratch</code>) so a result reads without expanding. A successful exit earns no chip.</li><li>Search results follow the host&#39;s <code>outputMode</code>: grouped hits, a path list, or per-file totals. A host <code>notice</code> (scoping, clipped lines, Read window) renders as a neutral note under the blocks it qualifies, never as an error.</li><li>Edit rows draw their own diff only when no ReviewChangeCard owns one, keeping workspace edits single-sourced. The inline permission card shares the same block renderer for its args preview.</li><li>Blocks are built on expansion only and highlighting is skipped above 100 KB or 800 lines; lists and diffs are capped and report the hidden remainder.</li><li>Decision D192; renderer-only presentation, so no ADR. See <code>04-ux/08-component-spec.md</code> §9 and E2E-145.</li></ul><h2 id="_2026-08-05-—-a-silent-assistant-turn-re-runs-once-before-it-is-an-error" tabindex="-1">2026-08-05 — A silent assistant turn re-runs once before it is an error <a class="header-anchor" href="#_2026-08-05-—-a-silent-assistant-turn-re-runs-once-before-it-is-an-error" aria-label="Permalink to &quot;2026-08-05 — A silent assistant turn re-runs once before it is an error&quot;">​</a></h2><ul><li>A turn that ends with no tool call and no visible text is no longer reported as complete. The runtime pops the empty assistant out of model context, appends a no-output nudge to the system prompt, and calls <code>agent.continue()</code> once — reusing the visible bubble id and swallowing the first attempt&#39;s <code>turn_end</code> / <code>agent_end</code>, so a successful recovery is invisible to the user.</li><li>Blank visible text triggers it even when reasoning content is present. The observed failure was exactly that shape: a 2830-character conclusion in thinking and an empty <code>text</code>, which reached the user as nothing. The timing log keeps <code>thinkingOnly</code> so the two shapes stay distinguishable.</li><li>The nudge rides on <code>state.systemPrompt</code> rather than <code>prepareNextTurn</code>, which only shapes in-flight turns and whose context is discarded once the loop stops. It is restored only if still unchanged, so a concurrent prompt rebuild wins.</li><li>A second silence is terminal: retriable <code>EMPTY_MODEL_RESPONSE</code>, which the existing assistant error row renders with a &quot;Try again&quot; action. One re-run per prompt, so overflow recovery in the same prompt cannot multiply attempts.</li><li>Decision D193; recovery inside the existing loop contract, so no ADR. See <code>03-runtime/02-agent-runtime.md</code> §5e, <code>03-runtime/08-error-codes.md</code> §3.2, and E2E-146.</li></ul><h2 id="_2026-08-05-—-per-tool-output-budgets-scoped-search-and-stated-collaboration-rules" tabindex="-1">2026-08-05 — Per-tool output budgets, scoped search, and stated collaboration rules <a class="header-anchor" href="#_2026-08-05-—-per-tool-output-budgets-scoped-search-and-stated-collaboration-rules" aria-label="Permalink to &quot;2026-08-05 — Per-tool output budgets, scoped search, and stated collaboration rules&quot;">​</a></h2><ul><li>The single 256KB / 4000-line cap is replaced by per-tool budgets: 48KB for Read/Glob/Grep, 96KB for Bash stdout, 96KB tail-kept for Bash stderr, 2000 chars per line everywhere. Search results are re-fetchable — narrow the pattern, advance the offset — so they earn the tighter half; a failing command&#39;s last line is the actionable one, so stderr keeps its tail.</li><li><code>Read</code> paginates with <code>offset</code> / <code>limit</code> and never refuses on file size. The old &gt;512KB rejection said &quot;use Grep or Bash to sample it&quot;, which is how an unpaginated read became an unbounded <code>sed</code> pipeline. <code>Grep</code> takes <code>path</code>, <code>include</code>, <code>outputMode</code>, <code>headLimit</code>; <code>Glob</code> takes <code>path</code>, <code>limit</code>; both order by modification time, newest first.</li><li>An explicit <code>path</code> disables parent ignore files. Without that, scoping a search to <code>node_modules</code> or <code>dist</code> returned zero and pushed the model back to shell — and Grep could not read its own spill files.</li><li>Over-budget Bash output spills into the per-session scratch dir so the marker names a real file. Read/Glob/Grep embed no marker: <code>content</code> stays byte-faithful so text copied out of it still matches for <code>Edit</code>, and the window metadata plus <code>notice</code> carry the same facts as sibling fields.</li><li>The system prompt now states collaboration rules outright — answer in the user&#39;s language, a sentence before each tool batch, never more than one batch without visible text, answer in text rather than only in reasoning, finish end to end — and states a preference for the scoped tools over shell equivalents. &quot;Prefer concise, actionable answers&quot; was the only nearby rule, and a reasoning model executed it as saying nothing.</li><li>Decision D194; tool schemas widen without breaking callers and prompt text is not an interface, so no ADR. See <code>03-runtime/16-tool-result-limits.md</code>, <code>03-runtime/02-agent-runtime.md</code> §7, and E2E-147.</li></ul><h2 id="_2026-08-05-—-mcp-servers-and-skills-the-user-owns-scoped-per-project" tabindex="-1">2026-08-05 — MCP servers and skills the user owns, scoped per project <a class="header-anchor" href="#_2026-08-05-—-mcp-servers-and-skills-the-user-owns-scoped-per-project" aria-label="Permalink to &quot;2026-08-05 — MCP servers and skills the user owns, scoped per project&quot;">​</a></h2><ul><li>Plugins, user MCP servers and user skills now share one activation shape: <code>enabled</code> plus <code>{ mode: &quot;global&quot; | &quot;projects&quot;, projects: [] }</code>. The boolean stays separate from the scope so switching something off keeps the project list it was narrowed to, and the <code>off</code> / <code>projects</code> / <code>global</code> control the UI renders is derived from the pair rather than stored as a third mode.</li><li>Matching is case-insensitive and trailing-separator-insensitive because macOS and Windows both hand us case-varying spellings of one directory, and a scoped path covers its subdirectories so a monorepo root does not have to be listed package by package. A <code>projects</code>-scoped extension is inactive in a session with no project: &quot;these projects&quot; is a claim about projects.</li><li>Scope is checked when the per-turn catalog is built <strong>and</strong> again at dispatch. A session outlives the prompt that listed its tools, and a tool the model can see is a tool it will try to call, so filtering in one place leaves a hole between re-scoping and the next prompt. Themes stay unscoped — appearance is app-wide, not a per-project capability.</li><li>Adding an MCP server is a paste. <code>parseMcpImport</code> reads the <code>mcpServers</code> document every README prints plus the <code>servers</code>, bare-map and single-object variants, infers <code>http</code> from a <code>url</code> because half the configs omit <code>type</code>, and reports per-entry skip reasons so one bad entry in fifteen is not fatal.</li><li>A user MCP server connects on first use and caches its tools; a failed handshake stays failed until the user edits it or presses Test. Saving a change to what the server <em>is</em> drops the connection, renaming it does not — a stale tool list is worse than a missing one.</li><li>A user skill is one <code>SKILL.md</code>. D174&#39;s contract is untouched, which is why the editor requires a description and puts it above the body: the description is the only part that enters the prompt.</li><li>Four tabs rather than one merged list. Plugins are installed, MCP servers are configured, skills are written; their rows need a connection light, a byte counter and a permissions matrix respectively, and one list would hide all of that behind a lowest-common-denominator row.</li><li>Decisions D192, D193, D194; new host-core registries, new RPC methods and a new main-process runtime, so ADR 0053. See <code>07-plugins/01-plugin-system.md</code> and <code>07-plugins/03-plugin-api.md</code>.</li></ul><h2 id="_2026-08-05-—-permission-gated-external-paths-and-portable-native-search" tabindex="-1">2026-08-05 — Permission-gated external paths and portable native search <a class="header-anchor" href="#_2026-08-05-—-permission-gated-external-paths-and-portable-native-search" aria-label="Permalink to &quot;2026-08-05 — Permission-gated external paths and portable native search&quot;">​</a></h2><ul><li>Explicit <code>path</code> arguments for <code>Read</code>, <code>Glob</code>, <code>Grep</code>, <code>Write</code>, and <code>Edit</code> are classified against the durable session workspace and scratch roots before the normal risk matrix. In <code>ask</code> and <code>accept-edits</code>, an outside path emits the existing permission card with the requested path preview; in <code>auto</code>, it executes without a card. Allow-once and the existing tool-scoped allow-session grant remain available, while denial, timeout, and cancellation return <code>TOOL_DENIED</code> without touching the path.</li><li>The approved resolver canonicalizes the deepest existing ancestor again at execution time, so <code>..</code> and symlink escapes cannot skip the boundary. The outside location is not promoted to a workspace root; external reads and searches retain absolute paths, and external mutations stay outside Review and workspace artifact records.</li><li>The sidecar exposes the host&#39;s bounded search controls (<code>Read.offset/limit</code>, <code>Glob.path/limit</code>, and <code>Grep.path/include/outputMode/headLimit</code>) with the canonical <code>filesWithMatches</code> spelling; the host normalizes common <code>files_with_matches</code> and <code>files-with-matches</code> provider aliases. Guidance prefers native tools and portable workspace-relative paths, with shell search as a bounded, platform-specific fallback.</li><li>Decision D195; the external path capability and widened search schemas change the host/runtime boundary, so ADR 0057. See <code>03-runtime/03-tools-and-permissions.md</code>, <code>03-runtime/06-host-rpc-protocol.md</code>, <code>03-runtime/15-workspace-ignore-rules.md</code>, <code>03-runtime/16-tool-result-limits.md</code>, and E2E-019/E2E-019e.</li></ul><h2 id="_2026-08-05-—-extensions-page-density-and-theme-readable-actions" tabindex="-1">2026-08-05 — Extensions page density and theme-readable actions <a class="header-anchor" href="#_2026-08-05-—-extensions-page-density-and-theme-readable-actions" aria-label="Permalink to &quot;2026-08-05 — Extensions page density and theme-readable actions&quot;">​</a></h2><ul><li>The Extensions destination no longer renders the four-card numeric overview band. Installed, MCP, Skills, and Marketplace remain separate tabs; tab counts, installed state-group counts, and the pending-update alert retain actionable state without duplicating it in a static summary row.</li><li>Shared primary and secondary buttons use semantic accent, surface, text, and border tokens. Primary actions invert the current theme surface; secondary actions keep an opaque elevated surface and visible border in both themes.</li><li>Decision D196 amends the presentation portion of D169 without changing plugin, MCP, skill, marketplace, permission, or runtime contracts. See ADR 0058, <code>04-ux/01-ui-ia.md</code>, <code>04-ux/07-ui-design-system.md</code>, <code>07-plugins/07-plugin-marketplace.md</code>, and E2E-024N/E2E-060b.</li></ul><h2 id="_2026-08-05-—-clipboard-files-become-session-scratch-references" tabindex="-1">2026-08-05 — Clipboard files become session-scratch references <a class="header-anchor" href="#_2026-08-05-—-clipboard-files-become-session-scratch-references" aria-label="Permalink to &quot;2026-08-05 — Clipboard files become session-scratch references&quot;">​</a></h2><ul><li>The composer now distinguishes native text paste from a clipboard payload containing files or images. File bytes are sent to Electron main only after the renderer has a durable session id; a home composer creates or reuses one before the transfer.</li><li>Main validates the session and writes bounded, uniquely named files under <code>&lt;data_dir&gt;/scratch/&lt;sessionId&gt;/pasted/</code>. The draft receives ordinary <code>@absolute/path</code> references, with quoting for whitespace, so the existing Read/Glob/Grep path semantics handle the pasted material without binary prompt content or project mutations.</li><li>Decision D197 and ADR 0059 define the new renderer/main boundary. See <code>04-ux/08-component-spec.md</code> §11.7–11.8, <code>03-runtime/01-ipc-protocol.md</code> §13c, <code>03-runtime/03-tools-and-permissions.md</code> §4b, <code>03-runtime/04-data-storage.md</code>, and E2E-102.</li></ul><h2 id="_2026-08-06-—-goal-mode-joins-plan-as-a-contract-mode" tabindex="-1">2026-08-06 — Goal mode joins Plan as a contract mode <a class="header-anchor" href="#_2026-08-06-—-goal-mode-joins-plan-as-a-contract-mode" aria-label="Permalink to &quot;2026-08-06 — Goal mode joins Plan as a contract mode&quot;">​</a></h2><ul><li>Sessions now persist one of three operating modes: <code>agent</code>, <code>plan</code>, or <code>goal</code>. The Composer-left chip cycles Agent → Plan → Goal → Agent, and <code>/goal-mode</code> (<code>builtin.mode.goal</code>) is its palette/slash entry.</li><li>Goal negotiates a goal statement, acceptance criteria, and boundaries instead of ordered steps. <code>EnterGoalMode</code> and <code>SubmitGoal</code> mirror <code>EnterPlanMode</code> and <code>SubmitPlan</code>, writing <code>.pi/goal/&lt;unique-name&gt;.md</code> and one pending <code>plan_approvals</code> row. <code>plan_approvals.kind</code> (schema v11) is the only discriminator; a submit tool run against the other kind fails with <code>PLAN_KIND_MISMATCH</code>.</li><li>Plan and Goal are the contract modes. The host hard deny for Write/Edit/plugin/unknown tools, the Bash-follows-permission-mode rule, the <code>*_IN_PLAN</code> error codes, the single-pending-approval invariant, and the <code>PLAN_REQUIRES_INTERACTIVE_SESSION</code> rejection of scheduled runs all apply to both, evaluated against the session&#39;s durable mode.</li><li>After approval the session becomes Agent and the queued execution carries the kind, so a Goal run chooses its own approach, self-checks against every acceptance criterion, stops at a stated boundary, and reports criterion by criterion.</li><li>Decision D198 defines this. See <code>03-runtime/01-ipc-protocol.md</code> §5.4, <code>03-runtime/02-agent-runtime.md</code> §5b/§7.2a, <code>03-runtime/03-tools-and-permissions.md</code> §10.1, <code>03-runtime/04-data-storage.md</code> §4.6a/§7, <code>03-runtime/06-host-rpc-protocol.md</code> §5.1, <code>03-runtime/08-error-codes.md</code>, <code>03-runtime/10-session-state-machine.md</code>, <code>04-ux/04-builtin-commands.md</code>, and <code>04-ux/08-component-spec.md</code> §11.</li></ul><h2 id="_2026-08-06-—-the-regenerate-branch-is-archived-under-the-host-rpc-lock" tabindex="-1">2026-08-06 — The regenerate branch is archived under the host RPC lock <a class="header-anchor" href="#_2026-08-06-—-the-regenerate-branch-is-archived-under-the-host-rpc-lock" aria-label="Permalink to &quot;2026-08-06 — The regenerate branch is archived under the host RPC lock&quot;">​</a></h2><ul><li>Turn completion used to archive the finished regenerate branch with a read-modify-write from Electron main: <code>session.get</code>, then <code>session.replaceMessages</code> to stamp <code>revisionCount</code> / <code>activeRevision</code> on the user root. Assistant and tool messages reach SQLite asynchronously through the persistence outbox (ADR 0041), so the snapshot could predate the turn&#39;s final message and the whole-transcript rewrite deleted it from the transcript file and the index, along with every row&#39;s <code>turn_id</code>.</li><li><code>session.saveActiveRevision</code> now performs the read, the archive, and the stamp in one host call under the state lock. The stamp rewrites only the root user&#39;s transcript line and re-reads the file at write time, so a line appended in the meantime survives. Electron main drains the outbox first and skips the archive with a warning rather than archiving an incomplete branch.</li><li><code>session.replaceMessages</code> carries each surviving message&#39;s owning <code>turn_id</code> across the rewrite, and is documented as safe only for a caller that owns the whole transcript for the duration of the call.</li><li>Decision D199 and ADR 0060 define this. See <code>03-runtime/04-data-storage.md</code> §4.9/§7, <code>03-runtime/06-host-rpc-protocol.md</code> §4, and E2E-118.</li></ul><h2 id="_2026-08-06-—-context-compaction-becomes-imperceptible" tabindex="-1">2026-08-06 — Context compaction becomes imperceptible <a class="header-anchor" href="#_2026-08-06-—-context-compaction-becomes-imperceptible" aria-label="Permalink to &quot;2026-08-06 — Context compaction becomes imperceptible&quot;">​</a></h2><ul><li>Compaction is host-driven and silent. The <code>CompactContext</code> tool, the <code>&lt;context_management&gt;</code> soft-boundary nudge, and the host no-confirmation allowlist entry are gone, so a long session never spends a model turn asking to be compacted and never grows a transcript row for it.</li><li><code>contextBudget()</code> keeps the D158 hard limit and headroom untouched, derives the retained-tail target from the model window instead of settings, and adds a background limit at 70% of the hard budget. Background pre-computation also requires growth of at least the retained-tail target since the newest checkpoint&#39;s baseline, so a large tail cannot re-trigger every turn.</li><li>Generation and installation are separate. A checkpoint is built only in provider-idle windows — while a tool executes, and after a run ends — so the summary request never shares the provider connection with a streaming turn. It installs at the next turn boundary or prompt if its base is still active, its anchor still exists, and it still fits the current model&#39;s budget; otherwise the unchanged blocking hard boundary handles it. A failed background build is discarded with no event, no persistence, and no ADR 0049 fallback.</li><li><code>compaction_start</code>/<code>compaction_end</code> gain optional <code>phase</code>, and <code>compaction_end</code> gains optional <code>status { generation, summaryTokens }</code>; both are additive inside protocol v9. A successful automatic compaction notifies nobody. The context usage inspector is the only visible trace, and Settings exposes no compaction controls at all.</li><li>Decision D200 and ADR 0061 amend D158 / ADR 0030 / ADR 0049. See <code>03-runtime/01-ipc-protocol.md</code> §6, <code>03-runtime/02-agent-runtime.md</code> §5.1/§7.1, <code>03-runtime/03-tools-and-permissions.md</code> §0/§2/§10.1, <code>03-runtime/06-host-rpc-protocol.md</code>, <code>04-ux/06-settings-ia.md</code>, <code>04-ux/08-component-spec.md</code> §7.3/§8.3/§11.5, <code>04-ux/09-interaction-patterns.md</code> §3A, and E2E-084.</li></ul><h2 id="_2026-08-06-—-context-compaction-is-rebuilt-to-match-codex" tabindex="-1">2026-08-06 — Context compaction is rebuilt to match Codex <a class="header-anchor" href="#_2026-08-06-—-context-compaction-is-rebuilt-to-match-codex" aria-label="Permalink to &quot;2026-08-06 — Context compaction is rebuilt to match Codex&quot;">​</a></h2><ul><li>The previous round cited Codex and then implemented its opposite on four counts. Codex compacts only synchronously, measures the whole context by default, emits a <code>ContextCompaction</code> item and a <code>Warning</code> for every compaction, and has a real model-facing tool (<code>new_context</code>). D200&#39;s Context section also stated that Codex has no such tool, which is wrong. The user asked for Codex&#39;s mechanism after being told it reverses the imperceptibility they had asked for one round earlier.</li><li>Compaction is inline again. Background pre-computation, its 70% limit, the increment-scoped trigger, and the three idle call sites are deleted; <code>prepareNextTurn()</code> compacts when the context crosses <code>hardLimit</code> or the model called <code>new_context</code>, matching Codex&#39;s <code>should_roll_over</code>.</li><li>A checkpoint carries the summary plus recent user messages, nothing else. pi&#39;s cut point still marks the boundary, but its split-turn prefix and recent tail are folded back into the summary input, so no message leaves the model context uncovered — the reason a filtered tail alone would have been a data-loss bug. Retention is newest-first to 20,000 tokens with the crossing message truncated rather than dropped, and assistant messages take their tool calls with them, so no orphaned call reaches a provider.</li><li>Both Codex families exist: the summary path, and a <code>fresh_window</code> rollover that asks for no summary and installs a fixed marker text. The family is an internal switch (construction option, then <code>PI_DESKTOP_COMPACTION_STRATEGY</code>), absent from settings and i18n, because no user can judge that trade-off from a settings row and Codex does not ask them to.</li><li><code>new_context</code> is parameterless with Codex&#39;s description verbatim, sits on the host no-confirmation allowlist, and is never assignable to a subagent. Two budget reminders — at <code>clamp(hardLimit * 0.15, 8k, 32k)</code> and at 2,000 tokens remaining — are appended to the current turn&#39;s system prompt, claimed once per checkpoint window, and never persisted or shown.</li><li>host-core keeps the whole checkpoint chain, per-record valid, because one transcript row per compaction has to survive a restart, a rewrite, and a fork. <code>compaction_end</code> drops <code>phase</code> and carries <code>mark</code> instead of <code>status</code>; the transcript draws a divider row after the message each checkpoint covers, and every successful compaction raises one warning toast. The context inspector keeps its line, now the newest mark&#39;s.</li><li>Three deviations from Codex are deliberate: the summary precedes the retained users because <code>buildSessionContext</code> fixes that order, <code>hardLimit</code> stays &quot;window − output reserve&quot; rather than 90% of the window (we have no separate full-window guard), and the tool is registered in both families rather than only the rollover one.</li><li>Decision D203 and ADR 0064 amend D200 / ADR 0061 and restore D158 / ADR 0030&#39;s visibility property. See <code>03-runtime/01-ipc-protocol.md</code> §6, <code>03-runtime/02-agent-runtime.md</code> §5.1, <code>03-runtime/03-tools-and-permissions.md</code> §2/§10.1, <code>03-runtime/04-data-storage.md</code> §2.1, <code>03-runtime/16-tool-result-limits.md</code> §4, <code>04-ux/08-component-spec.md</code> §7.3/§7.4/§8.4a/§11.5, <code>04-ux/09-interaction-patterns.md</code> §3A, and E2E-084.</li></ul><h2 id="_2026-08-06-—-bounded-subagents-behind-a-task-tool" tabindex="-1">2026-08-06 — Bounded subagents behind a <code>Task</code> tool <a class="header-anchor" href="#_2026-08-06-—-bounded-subagents-behind-a-task-tool" aria-label="Permalink to &quot;2026-08-06 — Bounded subagents behind a \`Task\` tool&quot;">​</a></h2><ul><li>A session can delegate one self-contained piece of work to a subagent and get back a single written report. Definitions are Markdown documents — four inline builtins (<code>explorer</code>, <code>code-reviewer</code>, <code>test-runner</code>, <code>fixer</code>) plus enabled global user documents under <code>~/.agents/subagents/*.md</code>, re-read on every launch, capped at 16, with malformed documents degraded to launch diagnostics. There is no project-level subagent capability source.</li><li>A definition declares its own <code>tools</code> from Read/Glob/Grep/BrowserPreview/ Bash/Edit/Write and is read-only (<code>Read, Glob, Grep</code>) when it declares none. A delegate never inherits mutation rights from its session, cannot reach plugin, skill, mode or meta tools, and has no <code>Task</code> tool of its own. Its calls run through the same <code>tools.execute</code> host path, so containment and permission modes are unchanged.</li><li>A definition may pin <code>model: &lt;provider&gt;/&lt;model&gt;</code>, resolved once per launch in Electron main against configured providers (by id, vendor key or display name, 8 distinct providers maximum). An unresolvable pin fails the <code>Task</code> call with a tool error instead of falling back to the session model.</li><li><code>Task</code> is offered in Agent mode only. Fan-out comes from execution modes: the session Agent runs <code>toolExecution: &quot;parallel&quot;</code>, every other tool is <code>sequential</code>, and only an all-<code>Task</code> batch runs concurrently, capped at 4 slots and each delegate at its own <code>maxTurns</code> (default 24, maximum 80). The sidecar serializes same-path <code>Write</code>/<code>Edit</code> calls through a <code>PathMutex</code>.</li><li>The parent&#39;s model context gains the bounded report (12k chars) and nothing else. Delegate messages and tool rows are emitted and persisted with <code>parentToolCallId</code> / <code>agentName</code> in the message <code>meta</code>, but the runtime skips them when rebuilding context, and a delegate&#39;s termination collapses into the tool result rather than reaching Electron main&#39;s turn handling.</li><li>The transcript nests attributed rows one level inside their <code>Task</code> row and keeps them out of the turn stream and the minimap. One <code>Task</code> stays compact; two or more in an activity group derive one renderer-only delegation card with aggregate status and a main-agent-to-delegate topology. Nodes reuse the existing row disclosure, structured outcome and nested rows. The topology is derived from persisted attribution on live and reload, adds no protocol or storage shape, and invents neither delegate dependencies nor an unavailable parent-summary node. Pending permission requests become a per-session queue: head-only answering, id-matched removal, whole queue denied on abort, and a card that names the delegate that asked and how many wait behind it.</li><li>Decision D201 and ADR 0062 define this. See <code>03-runtime/02-agent-runtime.md</code> §5f/§7.2b/§8, <code>03-runtime/03-tools-and-permissions.md</code> §10.2, <code>03-runtime/04-data-storage.md</code> §4.7a, <code>04-ux/03-permission-ux.md</code> §6a, <code>04-ux/08-component-spec.md</code> §9.9, and E2E-119.</li></ul><h2 id="_2026-08-07-—-empty-home-uses-direct-bottom-task-entry" tabindex="-1">2026-08-07 — Empty home uses direct bottom task entry <a class="header-anchor" href="#_2026-08-07-—-empty-home-uses-direct-bottom-task-entry" aria-label="Permalink to &quot;2026-08-07 — Empty home uses direct bottom task entry&quot;">​</a></h2><ul><li>Empty chat home uses a direct bottom composer and keeps the hero and optional onboarding content in one scrollable region.</li><li>The home composer is a bottom-reserved sibling of that region, so it remains visible at the bottom while checklist content scrolls independently on short windows.</li><li>Decision D204 and ADR 0066 define the layout. See <code>04-ux/01-ui-ia.md</code>, <code>04-ux/07-ui-design-system.md</code>, <code>04-ux/08-component-spec.md</code>, and E2E-063 / US-UI-64.</li></ul><h2 id="_2026-08-07-—-empty-home-gains-developer-starter-guidance-superseded" tabindex="-1">2026-08-07 — Empty home gains developer starter guidance (superseded) <a class="header-anchor" href="#_2026-08-07-—-empty-home-gains-developer-starter-guidance-superseded" aria-label="Permalink to &quot;2026-08-07 — Empty home gains developer starter guidance (superseded)&quot;">​</a></h2><ul><li>The empty home now presents four compact, localized developer starters under the hero so the middle surface is useful without becoming a marketing panel.</li><li>Activating a starter pre-fills and focuses the composer. It does not send a prompt or create a session turn, and short-window scrolling still preserves access to every block.</li><li>Decision D205 defined this amendment to D204 before D206 superseded it. See <code>04-ux/01-ui-ia.md</code>, <code>04-ux/07-ui-design-system.md</code>, <code>04-ux/08-component-spec.md</code>, and E2E-063 / US-UI-64.</li></ul><h2 id="_2026-08-07-—-empty-home-removes-developer-starter-cards" tabindex="-1">2026-08-07 — Empty home removes developer starter cards <a class="header-anchor" href="#_2026-08-07-—-empty-home-removes-developer-starter-cards" aria-label="Permalink to &quot;2026-08-07 — Empty home removes developer starter cards&quot;">​</a></h2><ul><li>The empty home no longer renders development task cards, starter glyphs, or contextual quick actions.</li><li>The hero, optional onboarding checklist, and direct bottom composer remain; task entry starts directly in the composer.</li><li>Decision D206 supersedes D205 while retaining D204&#39;s scrollable layout. See <code>04-ux/01-ui-ia.md</code>, <code>04-ux/07-ui-design-system.md</code>, <code>04-ux/08-component-spec.md</code>, and E2E-063 / US-UI-34 / US-UI-48.</li></ul><h2 id="_2026-08-07-—-work-panel-gains-a-direct-keyboard-entry-point" tabindex="-1">2026-08-07 — Work panel gains a direct keyboard entry point <a class="header-anchor" href="#_2026-08-07-—-work-panel-gains-a-direct-keyboard-entry-point" aria-label="Permalink to &quot;2026-08-07 — Work panel gains a direct keyboard entry point&quot;">​</a></h2><ul><li><code>Cmd/Ctrl + J</code> is a customizable renderer shortcut named <code>openWorkPanel</code>. It reveals the active session&#39;s retained work-panel context at the committed width without creating a resource tab; the context menu remains the place to create Review, Terminal, Browser, or Files. No active session and the Settings page are no-op contexts. <em>(Open-only clause amended by D221: the shortcut also collapses the visible panel.)</em></li><li>Artifact-driven resource creation, session ownership, background-event isolation, collapse behavior, and width persistence remain unchanged.</li><li>Decision D207 supersedes D128&#39;s no-global-shortcut clause and amends D142&#39;s no-launcher presentation. See ADR 0068, <code>04-ux/01-ui-ia.md</code>, <code>04-ux/08-component-spec.md</code>, <code>04-ux/09-interaction-patterns.md</code>, and E2E-056.</li></ul><h2 id="_2026-08-10-—-native-path-mistakes-recover-without-false-turn-failure" tabindex="-1">2026-08-10 — Native path mistakes recover without false turn failure <a class="header-anchor" href="#_2026-08-10-—-native-path-mistakes-recover-without-false-turn-failure" aria-label="Permalink to &quot;2026-08-10 — Native path mistakes recover without false turn failure&quot;">​</a></h2><ul><li>Read is file-only, Glob is directory-only, and Grep accepts a file or directory; directory Read errors include structured Glob recovery args.</li><li>Recovered tool errors remain visible on their own rows but no longer turn the containing processing group into a terminal failure surface.</li><li>D185&#39;s deferred Agent search tools remain unchanged. Decision D208 and ADR 0069 define the compatible tool and transcript contracts.</li></ul><h2 id="_2026-08-10-—-composer-file-references-use-compact-draft-chips" tabindex="-1">2026-08-10 — Composer file references use compact draft chips <a class="header-anchor" href="#_2026-08-10-—-composer-file-references-use-compact-draft-chips" aria-label="Permalink to &quot;2026-08-10 — Composer file references use compact draft chips&quot;">​</a></h2><ul><li>Completed workspace-file selections and pasted session-scratch files now live as renderer-owned, session-scoped references instead of exposing their canonical paths in the textarea.</li><li>The composer shows only removable leaf-name chips. Full paths remain available to tooltips and assistive technology, and duplicate leaf names retain distinct canonical identities.</li><li>Immediately before the existing submit dispatcher, references serialize after visible text with the unchanged quoted-or-unquoted <code>@path</code> grammar. Reference- only sends, slash templates, and Agent/Plan/Goal aliases keep their existing behavior; failed sends retain both text and references.</li><li>An accepted send retains only an in-memory, session/turn-scoped copy of its pre-serialization draft while unanswered smart Stop remains possible. That Stop restores the original text and chips; after reply content begins, abort preserves the partial transcript and restores no draft.</li><li><code>ComposerPastedFile.name</code> is the sanitized original leaf label, while <code>path</code> keeps the UUID-backed absolute storage name. Clipboard bytes, scratch containment, cleanup, host RPC, storage schema, and provider payloads remain unchanged.</li><li>Decision D209 and ADR 0070 amend the draft-presentation clauses of D124 / ADR 0024 and D197 / ADR 0059. See <code>03-runtime/01-ipc-protocol.md</code> §13c, <code>03-runtime/03-tools-and-permissions.md</code> §4b, <code>04-ux/07-ui-design-system.md</code> §8.1, <code>04-ux/08-component-spec.md</code> §11.7–11.8, <code>04-ux/09-interaction-patterns.md</code> §8a, and E2E-102/E2E-102a.</li></ul><h2 id="_2026-08-11-—-global-corners-use-an-apple-inspired-shape-hierarchy" tabindex="-1">2026-08-11 — Global corners use an Apple-inspired shape hierarchy <a class="header-anchor" href="#_2026-08-11-—-global-corners-use-an-apple-inspired-shape-hierarchy" aria-label="Permalink to &quot;2026-08-11 — Global corners use an Apple-inspired shape hierarchy&quot;">​</a></h2><ul><li>Fixed radii use a regular 4/6/8/10/12/14/16/18/20/24px ladder. Compact and medium desktop controls use rounded rectangles; explicit pill and circle tokens remain reserved for semantic capsules, equal-width icon controls, switches, tracks, avatars, and dots.</li><li>Nested surfaces follow Apple&#39;s concentricity rule when their corners align: the outer radius equals the inner radius plus the inset between their edges. Structural full-width shell panels remain square.</li><li>The composer keeps its 20px visible radius through the shared radius scale. Experimental <code>corner-shape</code> rendering is deferred because Electron 37&#39;s Chromium 138 runtime does not support it.</li><li>Decision D210 amends D072&#39;s previously pixel-preserving radius ladder without changing its token-only enforcement. ADR 0071 records the rationale. See <code>04-ux/07-ui-design-system.md</code> §6.2 and US-UI-72.</li></ul><h2 id="_2026-08-11-—-global-plugin-launcher-and-uninterrupted-next-turn-preparation" tabindex="-1">2026-08-11 — Global plugin launcher and uninterrupted next-turn preparation <a class="header-anchor" href="#_2026-08-11-—-global-plugin-launcher-and-uninterrupted-next-turn-preparation" aria-label="Permalink to &quot;2026-08-11 — Global plugin launcher and uninterrupted next-turn preparation&quot;">​</a></h2><ul><li>Option+Space on macOS and Alt+Space on Windows/Linux opens a centered, frameless plugin launcher. It searches launchable panels by Chinese name, full pinyin, pinyin initials, normalized name/id, and description, then opens the highlighted result through the existing panel host.</li><li>During an active answer the composer draft and mode, thinking, and permission choices remain editable for the next turn, while Send remains disabled. The renderer flushes only the newest full choice after the host reports idle.</li><li>User-stopped partial responses retain elapsed duration and exact provider output when present; absent final usage falls back to a visibly approximate output count so generation throughput survives persistence and reload.</li><li>Decisions D211/D212 and ADRs 0072/0073 define the native-window, IPC, in-flight configuration, and transcript metadata contracts. See <code>03-runtime/01-ipc-protocol.md</code>, <code>03-runtime/02-agent-runtime.md</code>, <code>04-ux/07-ui-design-system.md</code>, <code>04-ux/08-component-spec.md</code>, <code>04-ux/09-interaction-patterns.md</code>, and E2E-120.</li></ul><h2 id="_2026-08-12-—-plugins-can-request-and-send-native-notifications" tabindex="-1">2026-08-12 — Plugins can request and send native notifications <a class="header-anchor" href="#_2026-08-12-—-plugins-can-request-and-send-native-notifications" aria-label="Permalink to &quot;2026-08-12 — Plugins can request and send native notifications&quot;">​</a></h2><ul><li><code>pi.ui.notify</code> remains an in-app Toast for compatibility. Plugins with the existing <code>notify</code> permission gain <code>getNotificationPermission</code>, <code>requestNotificationPermission</code>, and <code>showNativeNotification</code> APIs.</li><li>Native plugin notification objects remain owned by Electron main. The API returns a best-effort <code>granted</code> / <code>denied</code> / <code>unknown</code> / <code>unsupported</code> state because Electron has no cross-platform read-only permission query. Native plugin notifications do not create durable task inbox rows or session activation events.</li><li>Decision D213 and ADR 0074 define the public API and the separation from D117&#39;s application-owned task notification contract. See <code>07-plugins/01-plugin-system.md</code>, <code>07-plugins/03-plugin-api.md</code>, <code>07-plugins/13-plugin-permissions-matrix.md</code>, and E2E-122.</li></ul><h2 id="_2026-08-12-—-development-plugins-get-an-explicit-permission-ceiling-reload" tabindex="-1">2026-08-12 — Development plugins get an explicit permission-ceiling reload <a class="header-anchor" href="#_2026-08-12-—-development-plugins-get-an-explicit-permission-ceiling-reload" aria-label="Permalink to &quot;2026-08-12 — Development plugins get an explicit permission-ceiling reload&quot;">​</a></h2><ul><li>Automatic development-plugin reloads continue to reject manifest permission additions, preserving the no-silent-widening boundary.</li><li>The Plugins page now offers Reload for <code>source: &quot;dev&quot;</code> rows. The new <code>plugin/reload</code> invoke resolves the registered folder, reloads it through the existing Electron runtime path, and re-arms the watcher with the current declared permissions as its new ceiling.</li><li>Decision D214 and ADR 0075 define this additive renderer/main IPC surface. Host RPC and storage schema versions remain unchanged; see <code>07-plugins/10-plugin-devex.md</code>, <code>07-plugins/12-plugin-ipc-and-host-services.md</code>, and E2E-022B.</li></ul><h2 id="_2026-08-12-—-windows-reserves-the-plugin-launcher-chord" tabindex="-1">2026-08-12 — Windows reserves the plugin launcher chord <a class="header-anchor" href="#_2026-08-12-—-windows-reserves-the-plugin-launcher-chord" aria-label="Permalink to &quot;2026-08-12 — Windows reserves the plugin launcher chord&quot;">​</a></h2><ul><li>Windows&#39; <code>Alt+Space</code> system-menu reservation can cause Electron&#39;s global shortcut registration to fail while the application is unfocused. The host core now owns a narrow low-level keyboard hook for the default binding, consumes the chord, and emits <code>keyboard.shortcut</code> to Electron. Electron toggles the existing launcher window; custom bindings continue to use the normal Electron global shortcut path.</li><li>Decision D215 and ADR 0076 define this additive host integration. Protocol version 9 and storage schema 11 remain unchanged.</li></ul><h2 id="_2026-08-13-—-plugin-launcher-renderer-warms-during-startup" tabindex="-1">2026-08-13 — Plugin launcher renderer warms during startup <a class="header-anchor" href="#_2026-08-13-—-plugin-launcher-renderer-warms-during-startup" aria-label="Permalink to &quot;2026-08-13 — Plugin launcher renderer warms during startup&quot;">​</a></h2><ul><li>Electron starts creating and loading the retained plugin launcher as soon as Electron is ready, before backend/plugin boot completes, while keeping the window hidden.</li><li>Shortcut delivery during warm-up joins the same in-flight creation promise; failed warm-up remains retryable, and each visible invocation still refreshes the plugin catalog. macOS reveals the panel through one normal activation path, without a redundant application focus steal or window-stack move.</li><li>Decision D217 and ADR 0080 supersede only D211/ADR 0072&#39;s first-use lazy creation clause. IPC, host RPC, protocol v9, and storage schema v11 are unchanged.</li></ul><h2 id="_2026-08-12-—-approval-cards-focus-on-the-artifact-and-remember-the-mode" tabindex="-1">2026-08-12 — Approval cards focus on the artifact and remember the mode <a class="header-anchor" href="#_2026-08-12-—-approval-cards-focus-on-the-artifact-and-remember-the-mode" aria-label="Permalink to &quot;2026-08-12 — Approval cards focus on the artifact and remember the mode&quot;">​</a></h2><ul><li>Plan/Goal approval cards now show only the title, host-created artifact opener/path, Reject, and Approve. Submitted question/description, status, validity/deadline, and inline warnings are not rendered.</li><li>The selected Ask / Accept edits / Auto approval mode is remembered in renderer-local device preferences and becomes the next approval&#39;s default; Ask remains the safe fallback.</li><li>Title-derived artifact filenames preserve Unicode alphanumeric characters so localized plan titles remain recognizable. The host&#39;s existing internal deadline remains a compatibility/fail-closed boundary but is not exposed by the approval card.</li><li>Decision D215 and ADR 0076 amend D189/ADR 0053. Protocol and storage versions remain unchanged.</li></ul><h2 id="_2026-08-14-—-plugin-launcher-remembers-recently-opened-plugins" tabindex="-1">2026-08-14 — Plugin launcher remembers recently opened plugins <a class="header-anchor" href="#_2026-08-14-—-plugin-launcher-remembers-recently-opened-plugins" aria-label="Permalink to &quot;2026-08-14 — Plugin launcher remembers recently opened plugins&quot;">​</a></h2><ul><li>The shortcut-invoked launcher records every successfully opened plugin id in renderer-local <code>localStorage</code> and shows an empty query in most-recently-used order, so the last opened plugin stays one Enter away.</li><li>Typing keeps search relevance authoritative and uses recency only as a tiebreaker between equally relevant matches; corrupted or unavailable storage degrades to the existing name order without breaking launching.</li><li>Decision D219 refines D211&#39;s presentation. IPC, host RPC, protocol v9, and storage schema v11 remain unchanged.</li></ul><h2 id="_2026-08-14-—-new-task-stays-out-of-history-until-it-carries-input" tabindex="-1">2026-08-14 — New task stays out of history until it carries input <a class="header-anchor" href="#_2026-08-14-—-new-task-stays-out-of-history-until-it-carries-input" aria-label="Permalink to &quot;2026-08-14 — New task stays out of history until it carries input&quot;">​</a></h2><ul><li>Clicking New Task now opens an unpersisted draft: the renderer no longer calls <code>session.create</code> (or reuses an old empty draft) at click time. The first message — or pasted file attachments, which need a session to attach to — materializes the session, which then appears in the sidebar history titled with the prompt.</li><li>Composer toolbar selections made before the first message (mode, thinking level, permission mode, model) are retained on the draft and applied when the session is created; a toolbar-only interaction creates no history row.</li><li>The sidebar history filter drops sessions whose title is still a default untitled value, which hides legacy empty drafts created before this change as well as any future accidentally empty sessions.</li><li>Decision D220 supersedes the empty-draft reuse clause of D088/D093: there is nothing to reuse because no draft session exists until first input. Protocol v9, host RPC, and storage schema v11 are unchanged; the change is confined to the renderer store, Composer, and Sidebar.</li></ul><h2 id="_2026-08-21-—-new-task-creates-an-immediate-durable-empty-slot" tabindex="-1">2026-08-21 — New Task creates an immediate durable empty slot <a class="header-anchor" href="#_2026-08-21-—-new-task-creates-an-immediate-durable-empty-slot" aria-label="Permalink to &quot;2026-08-21 — New Task creates an immediate durable empty slot&quot;">​</a></h2><ul><li>New Task now resolves the current project or Temporary group by its most recent non-archived session. An empty latest session is selected and reused; a non-empty latest session causes one real empty session to be created, refreshed into the sidebar, and selected before the first prompt.</li><li>The renderer serializes same-group New Task requests so rapid repeated clicks cannot race multiple empty-session inserts. The rule is shared by project groups and path-less Temporary sessions; an older empty row is intentionally untouched when a newer non-empty session is the latest.</li><li><code>SessionSummary.messageCount</code> is derived by host-core from <code>sessions.last_seq</code> and replaces title heuristics as the empty predicate. Empty rows are visible; title localization and manual renaming remain presentation concerns.</li><li>Decision D252 supersedes D220 and ADR 0084. The startup home remains an unpersisted renderer draft, but explicit New Task is eager and durable. <code>messageCount</code> is additive inside protocol v9; storage schema v11 is unchanged. See ADR 0113 and E2E-011b / E2E-011d / E2E-011e.</li></ul><h2 id="_2026-08-14-—-the-work-panel-shortcut-toggles-instead-of-only-opening" tabindex="-1">2026-08-14 — The work panel shortcut toggles instead of only opening <a class="header-anchor" href="#_2026-08-14-—-the-work-panel-shortcut-toggles-instead-of-only-opening" aria-label="Permalink to &quot;2026-08-14 — The work panel shortcut toggles instead of only opening&quot;">​</a></h2><ul><li><code>Cmd/Ctrl + J</code> (<code>openWorkPanel</code>) now collapses the visible work panel as well as revealing a closed one, going through the same store action as the header collapse control. Collapsing retains the session&#39;s tabs, active resource, Browser resource, and committed width, so a second press restores the previous surface.</li><li>The shortcut id stays <code>openWorkPanel</code> so existing keybinding overrides keep working; only the Settings → Shortcuts label changes to describe toggling.</li><li>The no-active-session and Settings no-op contexts, artifact-driven resource creation, session ownership, and background-event isolation are unchanged.</li><li>Decision D221 amends D207 and reverses ADR 0068&#39;s rejected toggle alternative; D128&#39;s and D142&#39;s remaining clauses stand. See ADR 0085, <code>04-ux/01-ui-ia.md</code>, <code>04-ux/08-component-spec.md</code>, <code>04-ux/09-interaction-patterns.md</code>, and E2E-056. Protocol v9, host RPC, IPC channels, and storage schema v11 are unchanged.</li></ul><h2 id="_2026-08-14-—-a-recorded-change-is-a-list-row-not-a-card" tabindex="-1">2026-08-14 — A recorded change is a list row, not a card <a class="header-anchor" href="#_2026-08-14-—-a-recorded-change-is-a-list-row-not-a-card" aria-label="Permalink to &quot;2026-08-14 — A recorded change is a list row, not a card&quot;">​</a></h2><ul><li>The review change card flattens into a single line on the <code>.tool-row</code> rhythm (24px row, disclosure caret, mono path, <code>+</code>/<code>−</code> counts on the right): the 1px border, 2px status rail, icon plate, status pill, second meta line, shadow, and light-theme white fill are all gone, leaving hover fill as the only row chrome. The 280px-default Review panel was spending most of its width on that chrome instead of on the path.</li><li>Status now travels as a Git-style letter (<code>A</code>/<code>M</code>/<code>D</code>) tinted by the existing per-status accent, so it is never carried by color alone; the localized status word — plus the rolled-back state, which the row shows as a strikethrough — stays in the row&#39;s accessible name. The expanded hunks, hash-guarded rollback, <code>aria-expanded</code>/<code>aria-controls</code> disclosure, and message ownership are unchanged; rollback becomes a borderless text action.</li><li>The Review tab&#39;s summary bar loses its icon, tinted band, and pill border, and the tab body drops its inset fill so the list reads flat. The Git-diff-era <code>.diff-file*</code> rules that no surface had rendered since the Review tab became a session change history were deleted (<code>.diff-file-counts</code> survives, renamed <code>.diff-counts</code>).</li><li>Decision D222 refines the presentation of D097&#39;s Review tab and the message-owned card; the review truth model, rollback semantics, and panel ownership rules stand. Presentation-only: see <code>04-ux/08-component-spec.md</code> §4.2/§4.4/§4.5 and §5. Protocol v9, host RPC, IPC channels, and storage schema v11 are unchanged.</li></ul><h2 id="_2026-08-14-—-pi-desktop-stays-a-regular-macos-application" tabindex="-1">2026-08-14 — PI-Desktop stays a regular macOS application <a class="header-anchor" href="#_2026-08-14-—-pi-desktop-stays-a-regular-macos-application" aria-label="Permalink to &quot;2026-08-14 — PI-Desktop stays a regular macOS application&quot;">​</a></h2><ul><li>The plugin launcher window asked Electron for <code>visibleOnFullScreen</code> without <code>skipTransformProcessType</code>, so Electron ran <code>TransformProcessType(kProcessTransformToUIElementApplication)</code> on the whole process and PI-Desktop vanished from the Dock and Cmd+Tab. ADR 0080&#39;s boot warm-up made that happen on every launch. The launcher now passes <code>skipTransformProcessType: true</code>; the process type is never transformed, and <code>app.dock.hide()</code>/<code>app.setActivationPolicy</code> remain forbidden in Main.</li><li>The launcher keeps <code>canJoinAllSpaces</code> and <code>FullScreenAuxiliary</code>: it still covers every regular Space and PI-Desktop&#39;s own fullscreen window. Overlaying another application&#39;s fullscreen Space is given up — macOS reserves it for accessory apps — and invoking the launcher from one activates PI-Desktop.</li><li>macOS activation restores the shell from <code>did-become-active</code> as well as <code>activate</code>, gated on a booted, non-quitting app with no visible window, so Cmd+Tab and App Exposé resurface a tray-hidden window (D216/ADR 0078) while launcher and plugin-panel activations leave the main window where it is.</li><li>Decision D223 constrains ADR 0072&#39;s launcher window and completes D216&#39;s restore paths; ADR 0080&#39;s warm-up and launcher latency budget stand. See ADR 0086, <code>03-runtime/07-process-model.md</code> §5, and E2E-127. Protocol v9, host RPC, IPC channels, and storage schema v11 are unchanged.</li></ul><h2 id="_2026-08-14-—-revealed-work-panel-with-no-resource-lists-its-four-tools" tabindex="-1">2026-08-14 — Revealed work panel with no resource lists its four tools <a class="header-anchor" href="#_2026-08-14-—-revealed-work-panel-with-no-resource-lists-its-four-tools" aria-label="Permalink to &quot;2026-08-14 — Revealed work panel with no resource lists its four tools&quot;">​</a></h2><ul><li><code>Cmd/Ctrl+J</code> reveals the panel without creating a tab, which left the body as blank space below the title bar. The body now renders an empty state — tiled icon, title, one line of copy — followed by the same four tools the header menu lists (Review, Terminal, Browser, Files) as plain 28px entry rows.</li><li>A row calls the same create-or-select path as its menu counterpart, so it cannot produce a duplicate tab. The rows exist only while the body has no tab at all; the shortcut itself still creates nothing, keeping D128&#39;s artifact-driven entry model intact — the rows are a user choice, not a side effect of revealing the panel.</li><li>The empty body is not exposed as a <code>role=&quot;tabpanel&quot;</code> because no tab labels it; its rows are buttons inside a <code>role=&quot;group&quot;</code> labelled &quot;Tools&quot;.</li><li>All panel empty states (the no-resource body and each tab&#39;s own) now share one <code>WorkTabEmpty</code> component on the proportions the rest of the app already uses (<code>.ext-empty</code>, <code>.projects-empty</code>): 38px round tiled icon, title, muted copy wrapping at 34ch instead of 48ch because the panel can be 244px wide.</li><li>Entry rows stay restrained — hover fill and a focus ring, no cards, no hero art, no large icons — so this does not reintroduce what D206 removed from the empty home, and it stays inside design-system §14&#39;s &quot;no marketing-style empty states&quot;.</li><li>No action button in the &quot;open a project&quot; empty states: <code>openProject()</code> resets the panel context and hides the panel, so the button would undo the surface that offered it.</li><li>Decision D224 is renderer-only. Protocol, host RPC, and storage schema are unchanged.</li></ul><h2 id="_2026-08-14-—-context-usage-inspector-opens-on-click" tabindex="-1">2026-08-14 — Context usage inspector opens on click <a class="header-anchor" href="#_2026-08-14-—-context-usage-inspector-opens-on-click" aria-label="Permalink to &quot;2026-08-14 — Context usage inspector opens on click&quot;">​</a></h2><ul><li>The inspector trigger no longer opens on <code>pointerenter</code> or focus. A click — or <code>Enter</code>/<code>Space</code> on the focused button — toggles the panel, and the 140ms hover-grace close timer that kept it alive between trigger and panel is deleted with it. Reading a token breakdown, scrolling the tool list, and selecting text inside the panel all outlast a pointer that wanders off, which the hover model punished.</li><li>Dismissal is explicit: a second activation, a capture-phase pointerdown outside both trigger and panel, or <code>Escape</code>, which also returns focus to the trigger. The existing rule that scrolling the trigger out of view closes the panel stands, as does the body-level collision-aware placement (ADR 0047 §6).</li><li>The panel stops being a tooltip. It carries <code>role=&quot;dialog&quot;</code> with the localized <code>Context</code> label; the trigger swaps <code>aria-describedby</code> for <code>aria-haspopup=&quot;dialog&quot;</code> and keeps <code>aria-expanded</code>/<code>aria-controls</code>. Keyboard users gain a stable panel instead of one that vanished on blur.</li><li>Decision D225 amends D184/ADR 0047 and is renderer-only. Protocol, host RPC, and storage schema are unchanged.</li></ul><h2 id="_2026-08-14-—-a-run-row-s-command-lives-in-its-head-not-in-its-body" tabindex="-1">2026-08-14 — A run row&#39;s command lives in its head, not in its body <a class="header-anchor" href="#_2026-08-14-—-a-run-row-s-command-lives-in-its-head-not-in-its-body" aria-label="Permalink to &quot;2026-08-14 — A run row&#39;s command lives in its head, not in its body&quot;">​</a></h2><ul><li>Expanding a <code>run</code> row opened with the command it had just printed in its collapsed head, so the first thing the reader met was the line they already had, and the output they expanded for started below it. The body now carries only the channels the command produced (Output, Errors); the command appears once, in the head.</li><li>The head therefore grows the two things the body no longer offers. A copy button beside the chevron yields the command as it was written — the head&#39;s summary is squeezed onto one line, so it is not what gets copied — and the outcome is stated in words (<code>Done</code>, <code>Failed</code>, <code>Denied</code>, <code>Working…</code>) with a dot tinted to match. The dot pulses while the command runs and replaces the row&#39;s spinner; the label carries the meaning, so the tint is decoration and <code>prefers-reduced-motion</code> stills it.</li><li>Withholding the command is gated on <code>hideSummaryArg</code>, the option that means &quot;the collapsed row already shows this argument&quot;. <code>PermissionCard</code> builds its presentation without it and so keeps showing the command it is asking about, which is the whole point of that card. The generic argument fallback is suppressed for the same reason: a command withheld because the head shows it must not return as an argument the moment it prints nothing.</li><li>The head is a flex row holding the disclosure button, the copy button, and a pointer-only chevron, because a button cannot nest inside a button. Hover fill moved from the header to the head so it does not stop short beneath the new controls, and <code>:has()</code> keeps a row with nothing to expand unfilled. Copy and chevron are quiet until hover, focus, or expansion; the status label is always visible. The chevron duplicates the header for pointers only (<code>aria-hidden</code>, <code>tabIndex={-1}</code>), and the visible status doubles as the row&#39;s live region, so the outcome is announced once rather than twice.</li><li>This replaces the withdrawn in-place terminal card: reverted for reproducing panel chrome inside the transcript. The row keeps its standard disclosure shape and gains only what the reader was missing.</li><li>Decision D226 refines D192&#39;s structured tool presentation and is renderer-only. Protocol, host RPC, IPC channels, and storage schema are unchanged. See <code>04-ux/08-component-spec.md</code> §9.2/§9.3/§9.5/§9.10 and E2E-129.</li></ul><h2 id="_2026-08-14-—-a-run-row-reports-what-the-command-did" tabindex="-1">2026-08-14 — A run row reports what the command did <a class="header-anchor" href="#_2026-08-14-—-a-run-row-reports-what-the-command-did" aria-label="Permalink to &quot;2026-08-14 — A run row reports what the command did&quot;">​</a></h2><ul><li>The row&#39;s outcome was the tool call&#39;s status, so a command that exited 1 could read <code>Done</code> whenever a layer forgot to flag the call as failed. It is now read from the shell&#39;s own report: <code>exitCode</code> non-zero is <code>Failed</code>, a killed shell that reports no code at all is <code>Failed</code>, and only <code>exitCode: 0</code> is <code>Done</code>. The host already applied that rule when marking Bash results; the row no longer depends on it having been applied upstream.</li><li>Tools that report no exit code fall back to the call&#39;s status. A row with neither — an import, a half-written message — states nothing rather than claiming success, and hands the announcement back to the hidden live region. The auto-open on failure follows the derived outcome, so a failing command opens its own row whichever layer noticed.</li><li>The expanded body loses its card. A <code>run</code> row&#39;s blocks drop the heading, the border, the fill and the per-block copy button, leaving the output as plain monospace text that reads like the terminal it came from. The 260px cap and its scroll stay: a long build must not bury the rest of the transcript.</li><li>Dropping the headings would have left <code>Errors</code> distinguished by tint alone, so each channel&#39;s name is still rendered for assistive technology — visually hidden, semantically present.</li><li>Decision D227 refines D226 and is renderer-only. Protocol, host RPC, IPC channels, and storage schema are unchanged. See <code>04-ux/08-component-spec.md</code> §9.2/§9.3/§9.5/§9.10 and E2E-129.</li></ul><h2 id="_2026-08-14-—-mid-stream-rate-limits-use-the-bounded-retry" tabindex="-1">2026-08-14 — Mid-stream rate limits use the bounded retry <a class="header-anchor" href="#_2026-08-14-—-mid-stream-rate-limits-use-the-bounded-retry" aria-label="Permalink to &quot;2026-08-14 — Mid-stream rate limits use the bounded retry&quot;">​</a></h2><blockquote><p>Superseded by D245 on 2026-08-19; the one-retry behavior below is retained as the historical decision and is no longer the active provider policy.</p></blockquote><ul><li>A mid-stream HTTP 429 is now classified as retryable <code>PROVIDER_RATE_LIMITED</code> and joins <code>STREAM_FAILED</code>, <code>NETWORK_ERROR</code>, and <code>TIMEOUT</code> in the runtime&#39;s same-turn bounded replay path: one 750 ms abortable backoff, the failed assistant is removed from model context, and the turn is replayed once with the same visible assistant bubble.</li><li>The retry budget is unchanged — one same-turn retry per prompt. A second 429 remains terminal and emits the normal assistant error plus lifecycle <code>error</code> event with <code>retryAttempt: 1</code> and <code>providerStatus: 429</code> in the details, so the transcript keeps its manual retry action.</li><li>Request-setup 429s were already covered by pi-ai&#39;s one bounded retry that honors <code>Retry-After</code> up to the 8-second cap; this closes the post-stream gap where that wrapper can no longer act.</li><li>Decision D233 refines D186 and is runtime-only. Protocol, host RPC, IPC channels, and storage schema are unchanged. See <code>03-runtime/02-agent-runtime.md</code> §5d, <code>03-runtime/08-error-codes.md</code>, ADR 0091, and E2E-149.</li></ul><h2 id="_2026-08-15-—-a-stopped-mutation-loop-says-so" tabindex="-1">2026-08-15 — A stopped mutation loop says so <a class="header-anchor" href="#_2026-08-15-—-a-stopped-mutation-loop-says-so" aria-label="Permalink to &quot;2026-08-15 — A stopped mutation loop says so&quot;">​</a></h2><ul><li>D186&#39;s repeat guard counted every same-path <code>Edit</code> failure alike, so the two failures it allows could both be spent on errors that already told the model what to do next. <code>EDIT_TAG_MISMATCH</code>, <code>EDIT_TAG_UNKNOWN</code>, and <code>EDIT_LINES_UNSEEN</code> each hand back the live tag or the withheld content, so each now gets one free attempt per path. Every other code still counts on its first occurrence, and a write that lands clears that path&#39;s history.</li><li>The guard stops the loop by terminating the tool batch, which pi-agent-core reads as &quot;no more work&quot;. The turn therefore ended with a failed tool card and no final message, indistinguishable from a model that chose to say nothing. The runtime now finalizes the assistant row with <code>MUTATION_RETRY_BUDGET_EXHAUSTED</code> — retriable, so the transcript keeps its retry affordance — and emits the matching error event, which also records the turn as <code>error</code> rather than <code>completed</code>.</li><li>Decision D228 refines D186 and is runtime-only. Protocol, host RPC, IPC channels, and storage schema are unchanged. See <code>03-runtime/18-line-anchored-edit-contract.md</code> §9.3, <code>03-runtime/03-tools-and-permissions.md</code> §4d, <code>03-runtime/08-error-codes.md</code> §3.3, ADR 0089, and E2E-140/E2E-141.</li></ul><h2 id="_2026-08-15-—-a-file-permission-carries-a-range-and-a-delete-is-recoverable" tabindex="-1">2026-08-15 — A file permission carries a range, and a delete is recoverable <a class="header-anchor" href="#_2026-08-15-—-a-file-permission-carries-a-range-and-a-delete-is-recoverable" aria-label="Permalink to &quot;2026-08-15 — A file permission carries a range, and a delete is recoverable&quot;">​</a></h2><ul><li>The three fs permissions were workspace-wide switches: <code>fs.read.workspace</code> reached <code>.env</code>, and <code>recursive: false</code> bounded one <code>fs.remove</code> call while a <code>glob</code> plus a loop reached the whole tree. A permission now says whether a plugin may touch files at all; <code>manifest.fs</code> says which ones, per mode, with globs relative to the mode&#39;s root. <code>fs.read</code> may ask for the whole tree — egress is confined to <code>manifest.net.domains</code>, so a broad read no longer carries anything out — but <code>fs.write</code> and <code>fs.delete</code> are refused a whole-tree pattern at validation time.</li><li>Every <code>pi.fs.*</code> call passes four gates in a fixed order and a later gate can only refuse: declared ∩ granted, <code>realpath</code> containment on both sides (so a symlink out of the workspace fails where a string comparison passed), the unconditional credential deny-list plus the host&#39;s own data directory, then the declared scope. Outside the scope the user is asked natively — deny / allow once / allow this session — and a session answer lives in memory and dies with the process. A host with no consent service refuses rather than assumes yes.</li><li>Deletion is bounded rather than scoped alone, because it is the one operation re-running the plugin cannot undo. <code>own: true</code> lets a plugin remove what it wrote itself, tracked by a path+mtime ledger in its own data directory and invalidated the moment the user edits the file; anything else needs a declared scope. Removal goes through <code>shell.trashItem</code>, stays non-recursive, and is braked at 50 removals per rolling minute. The host copies none of the user&#39;s data to provide the undo — the operating-system trash is the whole mechanism.</li><li><code>root: &quot;userSelected&quot;</code> trades standing power for reach: <code>pi.fs.requestDirectory()</code> asks the user to pick a directory, needs no manifest scope inside it, and the handle is memory-only. This is what keeps whole-disk plugins possible without a permanent grant.</li><li>The pre-scope names still install and are downgraded on load: <code>fs.read.workspace</code> keeps the whole tree, <code>fs.write.workspace</code> can write nothing until the manifest says where, <code>fs.delete.workspace</code> becomes <code>own: true</code>. Capability is reduced rather than a hole left open; the marketplace preflight and <code>pi-plugin check</code> both name the downgrade so an author sees it before a user does.</li><li>Decision D229 continues ADR 0008 D009 and implements the confirmation policy <code>07-plugins/13-plugin-permissions-matrix.md</code> promised but never enforced. The manifest gains <code>fs</code>, and <code>pi.fs.remove</code> / <code>pi.fs.requestDirectory</code> join the brokered API; host RPC and storage schema are otherwise unchanged. See ADR 0088, <code>07-plugins/02-plugin-manifest-schema.md</code> §5.2, <code>07-plugins/04-plugin-security.md</code> §6, and <code>07-plugins/13-plugin-permissions-matrix.md</code>.</li></ul><h2 id="_2026-08-16-—-proactive-background-subagent-delegation" tabindex="-1">2026-08-16 — Proactive background subagent delegation <a class="header-anchor" href="#_2026-08-16-—-proactive-background-subagent-delegation" aria-label="Permalink to &quot;2026-08-16 — Proactive background subagent delegation&quot;">​</a></h2><ul><li><code>Task</code> stops blocking: it starts a delegate in the background and returns a <code>delegationId</code> immediately. Three new Agent-mode core tools drive the lifecycle — <code>TaskWait</code> (converge on running delegations, <code>mode: &quot;any&quot;</code> + <code>minCompleted</code> for early convergence, settled delegations re-readable by id), <code>TaskList</code> (status report) and <code>TaskStop</code> (stop running delegations). The runtime owns a per-session delegation registry; delegates still running at run end, on parent abort, or on dispose are stopped.</li><li>The base system prompt gains a <code>## Delegation</code> section (when the catalog is non-empty) with positive trigger patterns — parallel exploration, adversarial review, multi-file implementation, context-economy searches, batch sharding — and convergence rules; the <code>Task</code> description is rewritten to lead with those triggers.</li><li>Builtins grow to four: <code>explorer</code> is rewritten in the omo-slim style (tool-choice guidance, parallel searches, <code>&lt;files&gt;</code>/<code>&lt;answer&gt;</code> report shape), and a new write-capable <code>fixer</code> implements multi-file changes from a complete spec (<code>tools: [Read, Glob, Grep, Edit, Write, Bash]</code>, <code>maxTurns: 40</code>, <code>&lt;summary&gt;</code>/<code>&lt;changes&gt;</code>/<code>&lt;verification&gt;</code> report shape).</li><li>Builtin and user definitions may declare <code>permission: inherit | ask | accept-edits | auto</code> (default <code>inherit</code>). The sidecar attaches the scope to the delegate&#39;s <code>tools.execute</code> calls; host-core resolves the call under that mode instead of the session&#39;s effective permission mode, with the contract modes&#39; hard deny and the external-path gate still in force. A project definition may not declare a scope: it arrives with the repository, so the declaration is dropped at parse time with a warning and its delegates run under the session&#39;s effective mode. <code>fixer</code> ships with <code>accept-edits</code>, so it writes inside the workspace without prompting while Bash and external paths keep the session&#39;s behavior.</li><li><code>TaskWait</code> blocks the turn, so its <code>timeoutSeconds</code> defaults to 600 and is clamped to 900. A timeout returns the finished reports plus a note to call again, so the ceiling costs one round-trip and keeps Stop responsive.</li><li><code>MAX_SUBAGENT_CONCURRENCY</code> becomes a per-session running cap of 10; <code>Task</code> fails with a tool error when the session already runs 10 delegates.</li><li>Decision D231 amends D201/ADR 0062 and touches the sidecar, host-core&#39;s <code>tools.execute</code> permission resolution, the builtin definitions, the system prompt, and the renderer&#39;s tool presentation mapping. See ADR 0089 and <code>03-runtime/02-agent-runtime.md</code> §5f/§5f.1.</li></ul><h2 id="_2026-08-17-—-plugin-panels-leave-the-visible-surface-to-plugins" tabindex="-1">2026-08-17 — Plugin panels leave the visible surface to plugins <a class="header-anchor" href="#_2026-08-17-—-plugin-panels-leave-the-visible-surface-to-plugins" aria-label="Permalink to &quot;2026-08-17 — Plugin panels leave the visible surface to plugins&quot;">​</a></h2><ul><li>Plugin panel windows are now frameless on macOS, Windows, and Linux. The preload reserves the same transparent 46px safe area on every platform and draws only a fixed capsule in the top-right with minimize, maximize/restore, and close.</li><li>The host no longer renders the manifest title or a development safe-area reminder. Plugin authors own the title, toolbar, background, and all other visible panel UI; fixed or sticky UI uses the existing <code>--pi-plugin-titlebar-height</code> variable and plugin-owned drag regions can use <code>-webkit-app-region: drag</code>.</li><li>Decision D234 amends D218 / ADR 0081 and D232 / ADR 0082. The private sender-validated control channel, closed Shadow DOM, localized labels, theme-adaptive capsule, <code>window.pluginBridge</code>, protocol v9, and storage schema remain unchanged. See ADR 0092, <code>07-plugins/01-plugin-system.md</code>, <code>04-ux/07-ui-design-system.md</code>, and E2E-024D.</li></ul><h2 id="_2026-08-17-—-plugin-panels-keep-a-strict-46px-drag-band" tabindex="-1">2026-08-17 — Plugin panels keep a strict 46px drag band <a class="header-anchor" href="#_2026-08-17-—-plugin-panels-keep-a-strict-46px-drag-band" aria-label="Permalink to &quot;2026-08-17 — Plugin panels keep a strict 46px drag band&quot;">​</a></h2><ul><li>Plugin panels reserve exactly 46px for a transparent host drag band on every platform. Normal-flow content is offset by that value, and clicks in the band are unavailable outside the capsule.</li><li>The host renders no panel title. Development panels show a localized, non-interactive reminder that the top 46px is drag-only; production panels keep the reminder hidden. The minimal capsule stays fixed at the top-right inside the band with minimize, maximize/restore, and close.</li><li>Decision D235 amends D234 / ADR 0092. The page-adaptive capsule keeps its closed Shadow DOM, localized labels, focus and reduced-motion behavior, private sender-validated control channel, <code>window.pluginBridge</code>, protocol v9, and storage schema. See ADR 0093 and E2E-024D.</li></ul><h2 id="_2026-08-18-—-one-desktop-instance-per-data-directory" tabindex="-1">2026-08-18 — One desktop instance per data directory <a class="header-anchor" href="#_2026-08-18-—-one-desktop-instance-per-data-directory" aria-label="Permalink to &quot;2026-08-18 — One desktop instance per data directory&quot;">​</a></h2><ul><li>Launching PI-Desktop while it is already running no longer starts a second app. Electron main takes the single-instance lock before it touches the data directory, and the duplicate launch quits before it creates a window, a tray, a child process, or a log line.</li><li>The running instance answers the relaunch by restoring and focusing its main window through the tray&#39;s Show path, so a window that was closed or hidden into the tray comes back instead of a second shell appearing beside it.</li><li>Decision D236 protects D002&#39;s single SQLite writer and the singletons around it (outbox, tray, launcher shortcut, updater). The lock is scoped to the installation, so runs with their own <code>PI_DESKTOP_DATA_DIR</code> — E2E harnesses, the capture rig, side-by-side profiles — start as before. No IPC, host protocol, or storage schema changed. See ADR 0094, <code>03-runtime/07-process-model.md</code>, and E2E-150.</li></ul><h2 id="_2026-08-18-—-vendor-account-oauth-login" tabindex="-1">2026-08-18 — Vendor-account (OAuth) login <a class="header-anchor" href="#_2026-08-18-—-vendor-account-oauth-login" aria-label="Permalink to &quot;2026-08-18 — Vendor-account (OAuth) login&quot;">​</a></h2><ul><li>A provider row can now be authenticated by a vendor subscription. The seven pi-ai OAuth vendors — anthropic, openai-codex, github-copilot, openrouter, kimi-coding, xai, radius — ship at once because the card list is derived from <code>models.getProviders().filter(p =&gt; p.auth.oauth)</code> rather than hardcoded. <code>registerBunOAuthFlows()</code> runs once at startup: pi-ai loads flows through a dynamic import with a variable specifier, which electron-vite cannot bundle.</li><li>Credentials live in the existing encrypted secret store under the new ref <code>secret:provider:&lt;id&gt;:oauth</code>, beside (never instead of) the row&#39;s API key. <code>has_secret</code> widens to &quot;either credential&quot; so existing readiness checks keep working; <code>has_oauth</code> and a non-secret <code>oauthAccountLabel</code> are new. <code>auth_kind</code> gains <code>oauth</code> — it was already a free string, so host protocol v9 and storage schema v10 are unchanged. Deleting a provider deletes both refs and both <code>secrets_meta</code> rows.</li><li>Electron main owns login/logout: <code>oauth.ts</code> implements pi-ai&#39;s <code>CredentialStore</code> over <code>secrets.*</code>, serializes <code>modify</code> per provider for locked refresh, and bridges <code>AuthInteraction</code> to the renderer through <code>providersOauthVendors/Start/Respond/Cancel/Logout</code> plus the <code>pi-desktop/providers/oauth/event</code> stream. Browser-callback, device-code, select and paste-a-code steps all travel that one stream, so a single dialog renders whatever the vendor asked for and cancel aborts the local callback server or the polling loop.</li><li>The sidecar resolves auth per request. An OAuth row launches with <code>apiKey: &quot;&quot;</code>; the runtime injects <code>resolveAuth</code>, which calls the host-proxy method <code>provider.resolveAuth</code>. Main answers it locally — never forwarding to host-core — after checking <code>(sessionId, providerId)</code> against the binding table it rewrites on every launch, and returns a short-lived <code>ModelAuth</code> (<code>apiKey</code>/<code>headers</code>/<code>baseUrl</code>, which is how Copilot pins its per-account endpoint). A refresh token never leaves main, and the sidecar receives strictly less than the long-lived API key it gets for a keyed row.</li><li><code>matches()</code> needs no new field: a vendor row&#39;s <code>apiKey</code> is permanently <code>&quot;&quot;</code> and the per-launch <code>resolveAuth</code> closure disappears through <code>JSON.stringify</code>, so an OAuth session reuses its warm runtime across turns.</li><li>Model discovery and the connection test go through the account: <code>providers.listModels</code> reads <code>models.getAvailable</code> (which applies the vendor&#39;s own <code>filterModels</code>, so Copilot lists what the subscription includes), and the test proves the account by resolving auth instead of probing <code>/models</code>. A row&#39;s <code>apiStyle</code> follows the selected model — Copilot spans wire APIs — and two styles are added: <code>openai_codex_responses</code> and <code>pi_messages</code>.</li><li>Settings → Model configuration gains a Vendor accounts section above the provider list; a signed-in row shows its account label and default model, and its edit dialog exposes only those non-secret fields instead of API-key inputs.</li><li>Decision D237 extends D028/D031 and touches host-core secrets and providers, Electron main, the agent runtime&#39;s provider binding, and the settings UI. See ADR 0095 and <code>03-runtime/14-secrets-storage.md</code>.</li></ul><h2 id="_2026-08-18-—-independent-vendor-oauth-accounts" tabindex="-1">2026-08-18 — Independent vendor OAuth accounts <a class="header-anchor" href="#_2026-08-18-—-independent-vendor-oauth-accounts" aria-label="Permalink to &quot;2026-08-18 — Independent vendor OAuth accounts&quot;">​</a></h2><ul><li>D240 amends D237 / ADR 0095. The provider row id is the OAuth account identity, and every login creates a new row even when <code>vendorKey</code> is the same as an existing account. Each row owns its own pi-ai collection, credential store, refresh serialization chain, and <code>secret:provider:&lt;id&gt;:oauth</code> ref.</li><li>Settings now has one owner per concept: Vendor accounts renders and removes OAuth rows; AI services renders API-key/custom rows only. A connected OAuth row remains selectable in the default model control, while removing it clears or repairs a default that points at the deleted id.</li><li>The sidecar binds a set of exact OAuth provider ids and main resolves auth by that id. Subagent vendor/name aliases are accepted only when unique, so a duplicate vendor account cannot be selected accidentally. See ADR 0098, <code>03-runtime/11-provider-model-system.md</code>, and E2E-151.</li></ul><h2 id="_2026-08-18-—-builtin-subagents-inherit-the-parent-permission-mode" tabindex="-1">2026-08-18 — Builtin subagents inherit the parent permission mode <a class="header-anchor" href="#_2026-08-18-—-builtin-subagents-inherit-the-parent-permission-mode" aria-label="Permalink to &quot;2026-08-18 — Builtin subagents inherit the parent permission mode&quot;">​</a></h2><ul><li>The builtin <code>fixer</code> no longer declares <code>permission: accept-edits</code>; like the other builtins, it defaults to <code>permission: inherit</code> and uses the parent session&#39;s effective permission mode.</li><li>In <code>auto</code>, builtin delegate calls therefore auto-allow the same in-root, high-risk, and explicit external-path operations as the parent. <code>ask</code> and <code>accept-edits</code> keep their existing approval boundaries, and explicit non-<code>inherit</code> scopes on eligible builtin or user definitions remain intentional overrides.</li><li>Decision D242 amends D231 / ADR 0089. Host-core&#39;s external-path gate and containment checks are unchanged; the fix removes an accidental builtin scope override rather than weakening the security boundary. See ADR 0100, <code>03-runtime/02-agent-runtime.md</code> §5f/§5f.1, and E2E-142.</li></ul><h2 id="_2026-08-18-—-model-aware-image-attachment-transport" tabindex="-1">2026-08-18 — Model-aware image attachment transport <a class="header-anchor" href="#_2026-08-18-—-model-aware-image-attachment-transport" aria-label="Permalink to &quot;2026-08-18 — Model-aware image attachment transport&quot;">​</a></h2><ul><li>Clipboard images remain compact structured composer references instead of being inserted as base64 or forced into visible <code>@path</code> text. Electron main validates the source against the session roots and stores image bytes under <code>&lt;data&gt;/attachments/&lt;sha256&gt;</code>; the durable user message stores only the ref, kind, name, MIME type, and size.</li><li>The exact pi-ai model record owns visual capability. A model with <code>input.includes(&quot;image&quot;)</code> receives eligible images as transient image blocks through the sidecar. Unknown/custom models, non-vision models, and images above the 10 MB inline bound receive a safe scratch/project path fallback; replayed content-store images use a session <code>replayed/</code> copy. An explicit <code>supportsImages</code> binding override may enable or disable the published image capability for that exact endpoint; discovery cannot promote an unknown model without such an override.</li><li>Decision D243 amends the image portion of D197 / ADR 0059, and D392 / ADR 0218 amends its capability-resolution rule. It fixes the observed GPT vision gap without moving provider logic into the renderer or putting binary data into host persistence. See ADR 0101, <code>03-runtime/01-ipc-protocol.md</code> §5.1/§13c, <code>03-runtime/04-data-storage.md</code>, <code>04-ux/08-component-spec.md</code> §11.7–11.8, and E2E-102c–102e / E2E-163.</li></ul><h2 id="_2026-09-11-—-effective-image-input-overrides-across-composer-and-transport-d392" tabindex="-1">2026-09-11 — Effective image-input overrides across Composer and transport (D392) <a class="header-anchor" href="#_2026-09-11-—-effective-image-input-overrides-across-composer-and-transport-d392" aria-label="Permalink to &quot;2026-09-11 — Effective image-input overrides across Composer and transport (D392)&quot;">​</a></h2><ul><li>Advanced model settings persist <code>supportsImages</code> per provider/model binding as a three-state value. <code>null</code> or an absent value follows the published model record; <code>true</code> or <code>false</code> explicitly enables or disables image input for that endpoint.</li><li>Composer model rows, attachment status, and Electron main transport all use the same effective binding capability. The shared published <code>ModelInfo</code> is not mutated, and unknown/custom models remain conservative without an explicit override.</li><li>Decision D392 amends D243 / ADR 0101. See ADR 0218, <code>03-runtime/13-model-catalog-and-selection.md</code> §11.2, <code>04-ux/08-component-spec.md</code> §11.7–11.8, and E2E-163.</li></ul><h2 id="_2026-08-19-—-models-dev-is-the-primary-model-catalog" tabindex="-1">2026-08-19 — models.dev is the primary model catalog <a class="header-anchor" href="#_2026-08-19-—-models-dev-is-the-primary-model-catalog" aria-label="Permalink to &quot;2026-08-19 — models.dev is the primary model catalog&quot;">​</a></h2><ul><li>D266 amends D136 / ADR 0027. Electron main loads the public <code>https://models.dev/api.json</code> catalog with a bounded timeout and matches provider records by vendor key or normalized API URL. Settings, Composer, and runtime model metadata use the matching record first.</li><li>The catalog maps display names, context/output limits, input modalities, reasoning options, tool-call and structured-output flags, and prices into the existing model surfaces. API keys and OAuth credentials never accompany the request. <code>catalogSource</code> distinguishes models.dev from the pi-ai fallback in renderer metadata without changing the host schema.</li><li>If the remote catalog cannot be loaded or has no matching record, the pinned pi-ai catalog supplies known native models and adapter compatibility. Custom or account-specific provider discovery and explicit IDs remain available after that fallback. See ADR 0133, <code>03-runtime/11-provider-model-system.md</code>, <code>03-runtime/13-model-catalog-and-selection.md</code>, and E2E-066/E2E-080/E2E-154.</li></ul><h2 id="_2026-08-19-—-the-work-panel-becomes-a-plugin-extension-point" tabindex="-1">2026-08-19 — The work panel becomes a plugin extension point <a class="header-anchor" href="#_2026-08-19-—-the-work-panel-becomes-a-plugin-extension-point" aria-label="Permalink to &quot;2026-08-19 — The work panel becomes a plugin extension point&quot;">​</a></h2><ul><li>Decision D246. A plugin declares work panel surfaces with <code>contributes.views</code> (id, plain or localized title, icon token, HTML entry, order), gated by the new low-risk <code>ui.view</code> permission. <code>ui.view</code> and <code>ui.panel</code> are independent, so a plugin may ship docked views, a detached window, or both.</li><li>A view renders as a main-process <code>WebContentsView</code> positioned from a renderer-measured rect — the mechanism the preview browser has used since D100 — and reuses the plugin&#39;s existing isolation wholesale: the same sandboxed preload, the same <code>persist:pi-plugin-&lt;id&gt;</code> partition as that plugin&#39;s panel window, and the same <code>net.domains</code> egress filter. The egress policy and partition name are shared functions so the two placements cannot drift apart. An <code>&lt;iframe&gt;</code> was rejected because Electron cannot give one its own partition, which would make a docked view strictly less isolated than the detached window it replaces.</li><li>Icons are tokens from a host-owned closed list, never plugin markup: the icon is drawn inside host chrome, so SVG there would be an injection surface and would let a plugin impersonate the host. An unknown token degrades to a lettered tile rather than failing validation.</li><li>The view list is filtered by activation scope, unlike contributed themes (D-themes/<code>pluginThemes</code>), which stay unfiltered because the selected theme is one global app setting. A view is scoped work, so a project-scoped plugin must not offer it elsewhere. Permission, scope, and entry existence are re-checked on open; the renderer is never the authority.</li><li>Decision D247. Review, Terminal, and Files move out of the host and ship as bundled first-party plugins on that same <code>contributes.views</code> channel, leaving Browser as the only built-in tool. This gives the plugin API a first-party consumer, so a gap in it becomes a shipped-feature bug. It requires new <code>pi.review.*</code> and <code>pi.terminal.*</code> APIs; <code>terminal.pty</code> is classified critical because a PTY is arbitrary execution as the user, and it is accepted deliberately rather than sidestepped with private first-party privileges.</li><li>See ADR 0104, ADR 0105, <code>07-plugins/02-plugin-manifest-schema.md</code> §4/§5/§7, <code>07-plugins/03-plugin-api.md</code> §6, <code>07-plugins/13-plugin-permissions-matrix.md</code> §2/§3, <code>04-ux/08-component-spec.md</code> §5, and E2E-152.</li></ul><h2 id="_2026-08-18-—-compact-context-usage-summary" tabindex="-1">2026-08-18 — Compact context usage summary <a class="header-anchor" href="#_2026-08-18-—-compact-context-usage-summary" aria-label="Permalink to &quot;2026-08-18 — Compact context usage summary&quot;">​</a></h2><ul><li>The context inspector keeps the click/keyboard trigger, remaining percentage, used/window counts, turn total, completed-turn speed, exact provider values, aggregate tool summary, checkpoint summary, body-level portal, placement, outside dismissal, and Escape focus return.</li><li>The default panel becomes a short summary: turn/speed values are unboxed, provider and tool usage become compact inline rows, and the per-tool rows, share bars, source badges, explanatory estimate paragraph, and used-capacity meter are removed. The <code>~</code> aggregate tool total still signals estimation.</li><li>Decision D244 amends D103 / D184 / ADR 0047. Protocol, storage, runtime accounting, and model metadata remain unchanged. See ADR 0103, <code>04-ux/08-component-spec.md</code>, and E2E-060d / US-UI-61.</li></ul><h2 id="_2026-08-19-—-opencode-style-bounded-provider-429-retry" tabindex="-1">2026-08-19 — OpenCode-style bounded provider 429 retry <a class="header-anchor" href="#_2026-08-19-—-opencode-style-bounded-provider-429-retry" aria-label="Permalink to &quot;2026-08-19 — OpenCode-style bounded provider 429 retry&quot;">​</a></h2><ul><li>HTTP 429 is now handled by one runtime-owned policy in both request setup and stream recovery. The budget is five retries after the initial request, shared across phases and disabled inside pi-ai&#39;s nested retry wrapper.</li><li>Retries stay silent and abortable. <code>retry-after-ms</code>, <code>retry-after</code> seconds, and HTTP-date values take precedence over the 2-second exponential fallback; positive 25% jitter and a 30-second cap keep the client bounded. Failed response status and headers are captured from fetch because pi-ai&#39;s ordinary response callback does not expose setup failures.</li><li>Main sessions and builtin subagents reuse the visible assistant row and suppress intermediate lifecycle/error events. Exhaustion emits one terminal <code>PROVIDER_RATE_LIMITED</code> error with <code>retryAttempt: 5</code> and <code>providerStatus: 429</code>; aborting during the wait starts no later provider request.</li><li>Authentication, model-selection, malformed-request, and context failures remain terminal. Other transient provider failures retained one setup or mid-stream retry until D258 gave them their own shared bounded budget.</li><li>Decision D245 amends D186 and D233. See ADR 0091, <code>03-runtime/02-agent-runtime.md</code> §5d, <code>03-runtime/08-error-codes.md</code>, and E2E-149.</li></ul><h2 id="_2026-08-19-—-files-ships-as-the-first-bundled-plugin" tabindex="-1">2026-08-19 — Files ships as the first bundled plugin <a class="header-anchor" href="#_2026-08-19-—-files-ships-as-the-first-bundled-plugin" aria-label="Permalink to &quot;2026-08-19 — Files ships as the first bundled plugin&quot;">​</a></h2><ul><li>Decision D248 implements the first step of D247. Browsing the project is now the bundled <code>pi.files</code> plugin, reached through <code>contributes.views</code> like any third-party one, and the host&#39;s tool list no longer carries a Files entry. Review and Terminal stay built in until their capabilities exist as plugin APIs.</li><li>Bundled plugins carry <code>source: &quot;builtin&quot;</code>. host-core reconciles its registry against the directory Electron reports in <code>PI_DESKTOP_BUILTIN_PLUGINS_DIR</code> on every launch, so an app update refreshes version and contributions, while the two pieces of state the user owns — enabled, and activation scope — carry across. They cannot be uninstalled, only disabled, and a plugin dropped from a newer build leaves no orphan registry row.</li><li>Only the <em>tool</em> migrated. A <code>file:&lt;path&gt;</code> tab is a transcript artifact — a file link or plan checkpoint the conversation opened — and stays host-owned, exactly like Review&#39;s artifacts. The same split will apply when Review moves.</li><li>Writing the plugin proved D247&#39;s premise immediately: <code>fs.glob</code> returns a flat, 500-capped file list with no directories and cannot back a lazy tree, so <code>pi.fs.list</code> was added under the existing <code>fs.read</code> permission. It applies the same guards as <code>glob</code> — declared scope, deny-list, protected paths, skipped heavy directories — because a listing is a read; directories are always returned so a narrow scope still yields a navigable tree.</li><li>See ADR 0105, <code>07-plugins/03-plugin-api.md</code> §3/§6, <code>07-plugins/13-plugin-permissions-matrix.md</code> §2, <code>04-ux/08-component-spec.md</code> §5, and E2E-153.</li></ul><h2 id="_2026-08-19-—-the-builtin-command-surface-is-reduced-to-five-core-entries" tabindex="-1">2026-08-19 — The builtin command surface is reduced to five core entries <a class="header-anchor" href="#_2026-08-19-—-the-builtin-command-surface-is-reduced-to-five-core-entries" aria-label="Permalink to &quot;2026-08-19 — The builtin command surface is reduced to five core entries&quot;">​</a></h2><ul><li>Decision D250 freezes the first-party registry at <code>builtin.session.new</code>, <code>builtin.agent.compact</code>, and the three <code>builtin.mode.*</code> entries. Their only builtin composer aliases are <code>/new</code>, <code>/compact</code>, <code>/agent-mode</code>, <code>/plan-mode</code>, and <code>/goal-mode</code>.</li><li>Session deletion, abort, project, settings, plugin, log, rename, command palette, reload-window, and DevTools entries are removed from the registry and renderer dispatch. The legacy <code>newChat</code>, <code>openProject</code>, and <code>openSettings</code> aliases are removed as well. Dedicated UI workflows and plugin commands are unaffected.</li><li>Mode aliases keep the one-shot <code>/mode &lt;prompt&gt;</code> behavior: mode changes first, then the remaining text is sent as the visible user turn. Former aliases are no longer supported builtin contracts.</li><li>See ADR 0106, <code>04-ux/04-builtin-commands.md</code>, ADR 0024, ADR 0034, and E2E-117.</li></ul><h2 id="_2026-08-19-—-the-panel-s-built-in-interactive-terminal-is-removed" tabindex="-1">2026-08-19 — The panel&#39;s built-in interactive terminal is removed <a class="header-anchor" href="#_2026-08-19-—-the-panel-s-built-in-interactive-terminal-is-removed" aria-label="Permalink to &quot;2026-08-19 — The panel&#39;s built-in interactive terminal is removed&quot;">​</a></h2><ul><li>Decision D251 supersedes D249 and the terminal clauses of ADR 0019/0105. The work panel no longer owns an interactive shell; users use an external terminal for interactive shell work.</li><li>Browser remains the host-built work-panel tool. Files remains a bundled plugin, Review and <code>file:&lt;path&gt;</code> remain artifact surfaces, and contributed plugin views remain available beside them.</li><li>Agent Bash stays non-interactive and transcript-owned: command, output, status, copy behavior, and its terminal icon remain unchanged. The removal deletes only terminal-specific PTY UI, IPC, types, dependencies, packaging, and tests; the desktop protocol version does not change.</li><li>No plugin PTY permission or bundled-plugin replacement is introduced.</li><li>See ADR 0108, <code>04-ux/08-component-spec.md</code> §5, and E2E-058.</li></ul><h2 id="_2026-08-24-—-windows-taskbar-minimize-keeps-the-taskbar-entry" tabindex="-1">2026-08-24 — Windows taskbar minimize keeps the taskbar entry <a class="header-anchor" href="#_2026-08-24-—-windows-taskbar-minimize-keeps-the-taskbar-entry" aria-label="Permalink to &quot;2026-08-24 — Windows taskbar minimize keeps the taskbar entry&quot;">​</a></h2><ul><li>Decision D252 amends D216 / ADR 0078 for the native Windows taskbar path. Clicking the focused window&#39;s taskbar button completes the ordinary native minimize and keeps the PI-Desktop taskbar entry. Clicking it again restores and focuses the window; clicking the entry while the window is merely covered keeps the existing bring-to-front behavior.</li><li>Explicit renderer/menu minimize actions remain tray-resident, as do the macOS/Linux native minimize paths. The resident tray, background processes, window bounds, IPC, and storage contracts are unchanged.</li><li>See ADR 0117, <code>03-runtime/07-process-model.md</code>, <code>04-ux/09-interaction-patterns.md</code>, and E2E-124.</li></ul><h2 id="_2026-08-24-—-renderer-owned-queued-prompts-and-graceful-send-now" tabindex="-1">2026-08-24 — Renderer-owned queued prompts and graceful Send now <a class="header-anchor" href="#_2026-08-24-—-renderer-owned-queued-prompts-and-graceful-send-now" aria-label="Permalink to &quot;2026-08-24 — Renderer-owned queued prompts and graceful Send now&quot;">​</a></h2><ul><li>Decision D253 adds a per-session, renderer-local FIFO queue for prompts sent while an agent is running. The queue holds the visible prompt and its draft snapshot, supports independent removal, survives session switches, and is intentionally not persisted or replayed after restart.</li><li>The composer has one submit slot. While a run is active, a non-empty draft renders Send and appends to the queue; an empty draft renders Stop, so the user clears the draft to expose the immediate-stop action. After the owning session&#39;s <code>agent_end</code>, the renderer drains one item through the existing <code>agent/prompt</code> path. Send now promotes one item and calls additive <code>pi-desktop/agent/stop</code>; the sidecar sets pi-agent-core&#39;s one-shot <code>shouldStopAfterTurn</code> flag, so the current assistant response/tool batch completes and the durable turn closes normally before the prioritized prompt starts. Immediate abort keeps its current behavior and never clears the queue.</li><li>This changes renderer state ownership and adds a public Electron IPC channel, but does not change host-core protocol v9, storage schema v11, or the one-running-turn constraint. See ADR 0118 and E2E-011f.</li></ul><h2 id="_2026-08-24-—-subagents-use-event-driven-idle-and-duration-timeouts" tabindex="-1">2026-08-24 — Subagents use event-driven idle and duration timeouts <a class="header-anchor" href="#_2026-08-24-—-subagents-use-event-driven-idle-and-duration-timeouts" aria-label="Permalink to &quot;2026-08-24 — Subagents use event-driven idle and duration timeouts&quot;">​</a></h2><ul><li>Decision D254 replaces the default subagent turn cap with a 600-second idle watchdog and a 21,600-second total-duration ceiling. The idle timer resets for turn, message, and tool lifecycle events and pauses during tool execution; the duration timer includes tool execution. (Amended by D260: the idle default is now 300 seconds and every agent event resets the timer.)</li><li>A timeout returns <code>timed_out</code> with <code>SUBAGENT_IDLE_TIMEOUT</code> or <code>SUBAGENT_DURATION_TIMEOUT</code>, preserving the latest partial assistant output where available. Fatal provider errors, parent aborts, and explicit per-definition <code>maxTurns</code> remain unchanged.</li><li><code>maxTurns</code> is now optional (<code>none</code>, <code>0</code>, and omission mean unlimited), with explicit values capped at 80. Definitions may override the watchdogs via <code>idle-timeout</code> and <code>max-duration</code> within their documented bounds; invalid values warn and fall back to defaults. (Amended by D260: the builtins each declare an explicit backstop rather than running unlimited.)</li><li>Builtin <code>explorer</code> gains <code>Bash</code> for bounded repository inspection while <code>code-reviewer</code> remains read-only. Shared status types, runtime timing, delegation topology, i18n, and E2E coverage expose the new timeout outcome.</li><li>See ADR 0119, <code>03-runtime/02-agent-runtime.md</code> §5f, <code>03-runtime/08-error-codes.md</code>, <code>03-runtime/09-logging-and-observability.md</code>, and E2E-155.</li></ul><h2 id="_2026-08-25-—-collapsing-the-work-panel-restores-the-base-window-bounds" tabindex="-1">2026-08-25 — Collapsing the work panel restores the base window bounds <a class="header-anchor" href="#_2026-08-25-—-collapsing-the-work-panel-restores-the-base-window-bounds" aria-label="Permalink to &quot;2026-08-25 — Collapsing the work panel restores the base window bounds&quot;">​</a></h2><ul><li>Decision D255 supersedes ADR 0033. The renderer still lays the work panel out as an in-flow flex column, but requests a native reservation equal to the committed panel width before presenting it. Exit keeps the panel mounted until the zero-width reservation succeeds, so folding the right panel returns the application window to its chat-only bounds instead of letting MainChat consume the released column.</li><li>The target-state reservation planner, constrained-display behavior, Browser measured-bounds sync, Main-owned native edge resize, and session-scoped panel state remain unchanged. See ADR 0122 and E2E-056.</li></ul><h2 id="_2026-08-25-—-window-controls-use-native-taskbar-minimize" tabindex="-1">2026-08-25 — Window controls use native taskbar minimize <a class="header-anchor" href="#_2026-08-25-—-window-controls-use-native-taskbar-minimize" aria-label="Permalink to &quot;2026-08-25 — Window controls use native taskbar minimize&quot;">​</a></h2><ul><li>Decision D256 amends D216 / D252 for Windows/Linux renderer and native-menu minimize actions. The custom top-right minimize button now uses the native OS minimize transition, so the taskbar entry remains available for restore; close-to-tray remains controlled only by the remembered close behavior.</li><li>macOS native minimize stays tray-resident, and the resident tray, close preference, IPC shape, host protocol, and background lifecycle are otherwise unchanged.</li><li>See ADR 0123, <code>03-runtime/01-ipc-protocol.md</code>, <code>04-ux/09-interaction-patterns.md</code>, and E2E-124.</li></ul><h2 id="_2026-08-27-—-agent-capability-pages-become-one-workbench" tabindex="-1">2026-08-27 — Agent capability pages become one workbench <a class="header-anchor" href="#_2026-08-27-—-agent-capability-pages-become-one-workbench" aria-label="Permalink to &quot;2026-08-27 — Agent capability pages become one workbench&quot;">​</a></h2><ul><li>Decision D257 refines D193 / D194 and supersedes D202&#39;s page-shape clause for Settings &gt; Agent &gt; Skills / MCP / Subagents. Each page is now one toolbar (segmented level filter with counts, one search field, project picker, primary actions) above one panel divided by level group headers, and all three pages create, edit, and delete through host calls that already existed before this change.</li><li>Per-row busy replaces page-wide locking, skeletons are first-paint only, and enablement flips optimistically with a revert on host rejection.</li><li><code>skills.reveal</code> now carries <code>level</code> and <code>projectPath</code> so a project skill with no global counterpart resolves; the handler still accepts a bare id.</li><li>Renderer-only apart from that payload widening: no host-core change, no new RPC, no storage or schema change. Every capability-state, precedence, and <code>.agents</code> path rule from D193 / D194 / D202 is unchanged.</li><li>See ADR 0126, <code>04-ux/06-settings-ia.md</code>, and E2E-103.</li></ul><h2 id="_2026-08-27-—-release-documentation-is-a-version-surface-d260" tabindex="-1">2026-08-27 — Release documentation is a version surface (D260) <a class="header-anchor" href="#_2026-08-27-—-release-documentation-is-a-version-surface-d260" aria-label="Permalink to &quot;2026-08-27 — Release documentation is a version surface (D260)&quot;">​</a></h2><ul><li>Stable app version bumps / tags <strong>must</strong> update every version-bearing surface before the tag: the shipped-locale changelog and its test list, all workspace package versions (including the previously skipped <code>docs/package.json</code>), the Cargo workspace + <code>host-core</code> lockfile versions, <code>APP_VERSION</code>, and the release line stated in <code>README.md</code> / <code>README.zh-CN.md</code> (D260, extends D164).</li><li><code>scripts/check-release-docs.mjs</code> verifies the whole set; <code>scripts/release.mjs</code> invokes it after bumping and refuses to commit or tag on failure.</li><li>Codified in the release runbook §4.1, AI development workflow matrix + Definition of Done + forbidden practices, change checklist, and <code>AGENTS.md</code>.</li></ul><h2 id="_2026-08-26-—-transient-provider-failures-share-a-bounded-retry-budget" tabindex="-1">2026-08-26 — Transient provider failures share a bounded retry budget <a class="header-anchor" href="#_2026-08-26-—-transient-provider-failures-share-a-bounded-retry-budget" aria-label="Permalink to &quot;2026-08-26 — Transient provider failures share a bounded retry budget&quot;">​</a></h2><ul><li>Decision D259 amends D186 / D245 and ADR 0050 / ADR 0091. Non-429 transient provider failures now share one bounded budget of four retries after the initial attempt across request setup and stream delivery, so an upstream gateway 502/503/504 recovers whether it arrives before headers or mid-stream. Only the failed request is replayed; the session and its tool state are untouched.</li><li>The budget admits <code>NETWORK_ERROR</code>, <code>TIMEOUT</code>, <code>STREAM_FAILED</code>, and retryable <code>PROVIDER_ERROR</code>. Non-429 delay honors <code>retry-after-ms</code>, <code>retry-after</code> seconds, then HTTP-date before a deterministic 1s/2s/4s/8s schedule, capped at 8 seconds, and captured headers are kept for every status that can state a delay.</li><li>The main session, builtin subagents, and one-shot composer enhancement share one policy. The 429 five-retry budget stays separate. Exhaustion records <code>retryAttempt: 4</code>.</li><li>Authentication, model-selection, malformed-request, and context failures remain terminal. No IPC, storage, host protocol, or provider-config change.</li><li>See ADR 0128, <code>03-runtime/02-agent-runtime.md</code> §5d, <code>03-runtime/08-error-codes.md</code>, E2E-096, and E2E-149.</li></ul><h2 id="_2026-08-27-—-the-subagent-idle-watchdog-bounds-silence-not-slowness" tabindex="-1">2026-08-27 — The subagent idle watchdog bounds silence, not slowness <a class="header-anchor" href="#_2026-08-27-—-the-subagent-idle-watchdog-bounds-silence-not-slowness" aria-label="Permalink to &quot;2026-08-27 — The subagent idle watchdog bounds silence, not slowness&quot;">​</a></h2><ul><li>Decision D260 amends D254. Every agent event re-arms the idle timer, a single streamed token arriving as <code>message_update</code> included, so the watchdog fires only on total unresponsiveness. A delegate that keeps producing output is never idle-terminated however slow its turn is, and the pause across tool execution is unchanged.</li><li>Because the window now measures dead air rather than work, the default idle timeout drops from 600 to 300 seconds, sized from the measured 174-second p99.9 wait between a delegate&#39;s last streamed token and its next response rather than from how long work may take. It must stay below the 600-second <code>TaskWait</code> default so a genuinely stuck delegate settles as <code>timed_out</code> within one wait instead of holding the parent for a full window and beyond. The 21,600-second duration ceiling and the 10–21,600 override bounds are unchanged.</li><li><code>TaskWait</code> expiry now reports &quot;Still running after Ns&quot; and states that this is not a failure and the delegates keep working, so the parent stops reading an ordinary unfinished wait as a failed delegation.</li><li>The builtins gain a turn backstop sized to their job — <code>explorer</code> 60, <code>code-reviewer</code> 50, <code>test-runner</code> 40, <code>fixer</code> 80 — replacing unlimited turns, so a delegate that loops without converging ends as <code>truncated</code> with its partial report rather than running to the duration ceiling.</li><li>No IPC, storage, host protocol, or provider-config change. See ADR 0129, <code>03-runtime/02-agent-runtime.md</code> §5f, <code>03-runtime/08-error-codes.md</code>, and E2E-155.</li></ul><h2 id="_2026-08-27-—-the-mounted-transcript-is-a-window-not-the-loaded-history-d261" tabindex="-1">2026-08-27 — The mounted transcript is a window, not the loaded history (D261) <a class="header-anchor" href="#_2026-08-27-—-the-mounted-transcript-is-a-window-not-the-loaded-history-d261" aria-label="Permalink to &quot;2026-08-27 — The mounted transcript is a window, not the loaded history (D261)&quot;">​</a></h2><ul><li>Decision D261 refines ADR 0120 / D258 in the renderer. Those bounded what crossed the IPC boundary and what locating a page costs; the renderer still mounted every row it had paged in and kept it mounted for the session&#39;s life.</li><li>The mounted history becomes a trailing window: 15 rows in the first commit after a session switch, a 60-row steady state, grown 40 at a time. Reaching the top grows the window while it is partial and fetches an older page only once the window covers all loaded history.</li><li><code>content-visibility: auto</code> was already skipping layout and paint for those offscreen rows, but it retains their React trees, Markdown ASTs, and Shiki token arrays — the wrong resource on a low-memory Windows machine, which is where the chat area was reported as progressively less responsive in a long session. Windowing bounds retained memory and per-frame reconciliation at once.</li><li>Window growth takes the same pre-paint scroll anchor as a fetched page, because both add height above the reading position. The window resets per session and is clamped to the loaded history, so a stale budget cannot over-mount. The hydration spacer stays scoped to the first commit: a permanent spacer under the steady-state window would put a blank viewport in front of the growth trigger.</li><li>The conversation minimap is built from the mounted entries. It resolves a click by finding the marker&#39;s node in the scroller, so the full set would have drawn dashes that jump nowhere. The tradeoff is that the browser&#39;s own find-in-page only reaches mounted rows.</li><li>No IPC, storage, host protocol, or pagination change. See ADR 0130, <code>04-ux/08-component-spec.md</code> §8, and E2E-159.</li></ul><h2 id="_2026-08-28-—-large-text-pastes-become-session-scratch-references-d262" tabindex="-1">2026-08-28 — Large text pastes become session-scratch references (D262) <a class="header-anchor" href="#_2026-08-28-—-large-text-pastes-become-session-scratch-references-d262" aria-label="Permalink to &quot;2026-08-28 — Large text pastes become session-scratch references (D262)&quot;">​</a></h2><ul><li>Text-only pastes at or below the persisted <code>largePasteThreshold</code> remain native textarea input; the default is 600 characters and valid values are integers from 1 through 1,000,000.</li><li>Larger text is transferred as exact UTF-8 <code>text/plain</code> bytes through the existing paste bridge and stored under the owning session&#39;s <code>&lt;data_dir&gt;/scratch/&lt;sessionId&gt;/pasted/</code> directory. The Composer inserts a generated <code>@&lt;temporary-name&gt; </code> token at the original caret/selection and keeps its canonical path mapping in the renderer draft, including cached and unanswered smart-Stop snapshots.</li><li>Dispatch resolves the generated token in place exactly once. It is not appended as a basename, sent as a duplicate structured attachment, or written into the workspace. Existing clipboard file/image chip behavior and scratch cleanup remain unchanged.</li><li>The setting is an additive app-settings JSON field; host normalization gives older settings the default without a database migration. See ADR 0131, <code>04-ux/06-settings-ia.md</code>, <code>04-ux/07-ui-design-system.md</code> §8.1, <code>04-ux/08-component-spec.md</code> §11.7–11.8, <code>04-ux/09-interaction-patterns.md</code> §8a, and E2E-102g.</li></ul><h2 id="_2026-08-28-—-a-cross-display-drag-is-user-intent-not-an-os-adjustment-d263" tabindex="-1">2026-08-28 — A cross-display drag is user intent, not an OS adjustment (D263) <a class="header-anchor" href="#_2026-08-28-—-a-cross-display-drag-is-user-intent-not-an-os-adjustment-d263" aria-label="Permalink to &quot;2026-08-28 — A cross-display drag is user intent, not an OS adjustment (D263)&quot;">​</a></h2><ul><li>Decision D263 amends D255 / ADR 0122 in Main. The work-panel reservation is planned from remembered base bounds and reconciled against the last rect Main applied. That reconcile treated every display change as one <code>displayChanged</code> boolean and reused the remembered base bounds, which is right for an OS re-fit and wrong for a window the user dragged to another display.</li><li>Reported as issue #18 and reproducible on every release from 0.10.0: dragging the window across a display boundary made it jump on pointer release. The reconcile was wired to each native <code>move</code>, so it ran mid-drag and re-planned from the origin display&#39;s base bounds; the target work area then clamped that x to its edge and kept the old display&#39;s y. Panel state was irrelevant — a closed panel requests width 0 but still re-plans and calls <code>setBounds</code>.</li><li>The boolean becomes a <code>DisplayTransition</code> of <code>none</code>, <code>os-adjusted</code>, or <code>user-moved</code>. <code>os-adjusted</code> preserves ADR 0122 verbatim so a constrained display keeps the user&#39;s intent and a roomy one restores the full reservation. <code>user-moved</code> derives base bounds from where the window now is, with only the origin normalized into the target display&#39;s work area. The size is kept even on a smaller display, because base bounds are the restorable intent under ADR 0122 and a shrink would be persisted with no way back; the reservation absorbs the shortfall instead.</li><li>Attribution keys off an unaccounted native <code>move</code> stream rather than a deadline, because Electron exposes no drag-begin/drag-end pair for native moves and a deadline would make correctness depend on main-process scheduling and on maximized windows that defer geometry. The <code>move</code> handler only marks the pending drag and defers reconciliation until the stream goes quiet, so Main never fights the window server mid-drag. Display topology events clear the marker first, so a hotplug right after a drag stays OS-owned.</li><li>The same misattribution had been writing stale coordinates to <code>window-state.json</code>, reopening the window on the display the user had left. A <code>user-moved</code> transition now advances the remembered display key and persists, normalized the same way as the reservation path because a maximized window defers geometry and leaves the save as the drag&#39;s only consumer, while <code>os-adjusted</code> still refuses to. An unconsumed marker is retired on a deadline so it cannot leak into a later OS display change. A forced Stage Manager recovery clears the pending attribution because it is not user intent.</li><li>No IPC, storage, or host protocol change; <code>window/setWorkPanelReservation</code> keeps its shape. See ADR 0132, <code>03-runtime/01-ipc-protocol.md</code>, and E2E-160.</li></ul><h2 id="_2026-08-28-—-composer-typing-is-a-measured-latency-path-d264" tabindex="-1">2026-08-28 — Composer typing is a measured latency path (D264) <a class="header-anchor" href="#_2026-08-28-—-composer-typing-is-a-measured-latency-path-d264" aria-label="Permalink to &quot;2026-08-28 — Composer typing is a measured latency path (D264)&quot;">​</a></h2><ul><li>Per-keystroke lag in the chat composer had three measured causes, all on the typing path and none of them a correctness bug. With the <code>@</code> menu open, each keystroke fuzzy-matched up to the whole workspace file index (8000 entries) and then fully sorted every match to display 50 rows; a bounded top-K selection now returns the same rows in the same order for 0.25–0.78 ms per keystroke instead of 2.4–3.0 ms. <code>filterCommands</code> keeps its full sort: the command list is small and its comparison is grouped by command kind, where a top-K pass buys nothing.</li><li>The auto-resize effect forced a <code>height: auto</code> measurement probe on every keystroke, and each probe is a synchronous reflow. The probe cannot simply be dropped, because it is the only reading that reveals the draft now needs fewer rows: at a fixed applied height an overflowing box reports that height, not the content&#39;s. That is also why reading at the applied height is equivalent while the content overflows — the single <code>scrollHeight</code> reading is then the full content height. The probe is therefore reserved for the case where the box may need to shrink: applied height above the minimum and content no longer filling it.</li><li>Republishing an unchanged <code>--composer-dock-height</code> is not free. The property lives on <code>document.documentElement</code>, so every <code>setProperty</code> invalidates style for the whole document, including the transcript. Skipping the write when the rounded height is unchanged keeps typing inside one row off that path, and height and <code>overflowY</code> are written only when the computed value differs from what was last applied.</li><li>The remaining cost was React identity churn. <code>Array.prototype.filter</code> allocates even when it drops nothing, so the reference filters now return the existing array unchanged and the draft-cache serialization effect keyed on <code>fileReferences</code> stops re-running per keystroke; the same no-op guard covers the <code>workspacePath</code> effect and <code>clearDraftForKey</code>. <code>setCursor</code> ignores an unchanged value because <code>onSelect</code> fires on every caret move, which had made plain arrow-key navigation re-render the composer and re-run autocomplete trigger detection.</li><li>No IPC, storage, host protocol, or schema change, and no new dependency. Behavior is unchanged: identical menu rows and order, the same 28px one-line floor, and the same seven-row cap. See <code>04-ux/08-component-spec.md</code> §11.5 and US-UI-55.</li></ul><h2 id="_2026-08-29-—-an-expanded-delegate-run-scrolls-in-place-d271" tabindex="-1">2026-08-29 — An expanded delegate run scrolls in place (D271) <a class="header-anchor" href="#_2026-08-29-—-an-expanded-delegate-run-scrolls-in-place-d271" aria-label="Permalink to &quot;2026-08-29 — An expanded delegate run scrolls in place (D271)&quot;">​</a></h2><ul><li>Decision D271 refines D201 / D268 / ADR 0062 in the renderer. Opening one delegation node inlined the delegate&#39;s entire run into the parent transcript, so a delegate that made forty tool calls added forty rows at once.</li><li>Three unbounded surfaces were involved. <code>.subagent-run</code> had no height limit, <code>.tool-fields</code> was the only detail block without the 260px cap that <code>.tool-row-content</code>, <code>.tool-file-list</code> and <code>.tool-match-list</code> already carried, and D268 had routed the joined <code>TaskWait</code> reports — bounded at 50k characters by the runtime — into a <code>note</code>, which has no cap either.</li><li>The scroll deliberately sits on a new inner <code>.subagent-run-rows</code> wrapper rather than on <code>.subagent-run</code>: the collapse rail is absolutely positioned at <code>left: -8px</code>, outside the run&#39;s padding box, so an <code>overflow</code> on the run would have clipped the rail that marks the delegate-context boundary. The heading stays outside the scroll area so the attribution cannot scroll away from the rows it labels.</li><li>The scroll area is a labelled, focusable <code>role=&quot;group&quot;</code>, because a pointer user could scroll it and a keyboard user could not otherwise reach it.</li></ul><h2 id="_2026-08-29-—-a-lifecycle-row-is-a-subagent-row-d268" tabindex="-1">2026-08-29 — A lifecycle row is a subagent row (D268) <a class="header-anchor" href="#_2026-08-29-—-a-lifecycle-row-is-a-subagent-row-d268" aria-label="Permalink to &quot;2026-08-29 — A lifecycle row is a subagent row (D268)&quot;">​</a></h2><ul><li>Decision D268 refines D201 / D265 / ADR 0062 and ADR 0089 in the renderer. D265 made every <code>Task</code> call a full-width delegation card, but left the three lifecycle rows (<code>TaskWait</code>/<code>TaskList</code>/<code>TaskStop</code>) as generic tool rows. Those rows are the only place a settled outcome appears — <code>Task</code> returns the moment the delegate starts and says <code>running</code> forever — so they were simultaneously the most informative and the least readable part of a delegation.</li><li>The visible defects were narrow. A lifecycle row is called with delegation ids, so <code>getToolSummary</code> printed a bare UUID list; its result payload had no per-tool mapping, so the body fell back to pretty-printed <code>delegations[]</code> JSON; and its label read &quot;Delegated&quot;, which is what <code>Task</code> does, not what a wait or a stop does.</li><li>The fix reads the roster the runtime already returns. <code>details.delegations[]</code> (<code>TaskWait</code>/<code>TaskList</code>) and <code>details.stopped[]</code> (<code>TaskStop</code>) carry the agent name, status and registry timestamps, which is exactly what a reader needs: the row summarizes by agent name with repeats counted, badges a rolled-up status from the shared <code>chat.subagentStatus.*</code> vocabulary, and renders the roster as a named field table led by the joined reports.</li><li>Deliberately unchanged: a lifecycle row is still <strong>not</strong> a topology node and still must not inflate the subagent counts (<code>isDelegationActivityItem</code>), the runtime and its <code>delegationId</code> contract are untouched, and no IPC, storage or host-protocol surface moves. This is presentation only.</li></ul><h2 id="_2026-08-28-—-one-delegation-reads-as-a-card-like-a-fan-out-d265" tabindex="-1">2026-08-28 — One delegation reads as a card, like a fan-out (D265) <a class="header-anchor" href="#_2026-08-28-—-one-delegation-reads-as-a-card-like-a-fan-out-d265" aria-label="Permalink to &quot;2026-08-28 — One delegation reads as a card, like a fan-out (D265)&quot;">​</a></h2><ul><li>Decision D265 amends D201 / ADR 0062 in the renderer. That decision reserved the delegation card for two or more <code>Task</code> calls in one activity group and left a lone <code>Task</code> as a compact tool row. The same work therefore looked like two different features, and the single case — the common one — was the one that lost the outcome, the recorded runtime and the delegate&#39;s step count.</li><li>Every <code>Task</code> call in an activity group now renders as a delegation card: root node, connector, one node per delegate. Nothing else about the card changes, so the aggregate header, the live-open-once rule, the node disclosures and the print-the-report-once rule carry over unchanged.</li><li>The aggregate header is count-aware. English gains <code>_one</code> forms for the four status labels; Chinese has a single plural category, so its <code>_other</code> forms carry every count and drop the English plural marker that made a one-node card say &quot;1 个 Subagents&quot;.</li><li>No IPC, storage, host protocol, or delegation-runtime change: the card is still derived on both live and reload from the persisted <code>parentToolCallId</code> / <code>agentName</code> attribution. See ADR 0062 §6, <code>04-ux/08-component-spec.md</code> §9.9, and E2E-119.</li></ul><h2 id="_2026-08-28-—-the-titlebar-band-is-reserved-on-every-platform-d266" tabindex="-1">2026-08-28 — The titlebar band is reserved on every platform (D266) <a class="header-anchor" href="#_2026-08-28-—-the-titlebar-band-is-reserved-on-every-platform-d266" aria-label="Permalink to &quot;2026-08-28 — The titlebar band is reserved on every platform (D266)&quot;">​</a></h2><ul><li>Decision D266 fixes a macOS-only regression on the destination pages. The 46px band at the top of the main pane is an opaque, absolutely positioned <code>.main-titlebar</code>, so scrolling route content passes underneath it on every platform. Only <code>win32</code> and <code>linux</code> padded <code>.page-frame</code> for it, and that reservation was framed as a window-controls concern — macOS draws its traffic lights natively, so darwin looked exempt.</li><li>It was not. The band is opaque regardless of who draws the window controls, so on macOS it covered the Plugins page header: the title row was clipped and the Installed / Marketplace segmented control sat at the window&#39;s top edge.</li><li><code>.page-frame</code> now reserves <code>--ds-toolbar-height</code> plus an 8px buffer on darwin as well, matching what <code>.thread-content</code> already did for the transcript on all three platforms. The fix covers Plugins, Scheduled, and Pull requests, since they share the frame.</li><li>Surfaces that stack above the band keep their current treatment: the plugin detail sheet renders at <code>z-index: 60</code> and still starts at the top edge, with only <code>win32</code> / <code>linux</code> dropping below the band to clear the window controls.</li><li>Renderer CSS only: no IPC, storage, host protocol, or component-structure change. See <code>04-ux/08-component-spec.md</code> §2.3 and E2E-087a.</li></ul><h2 id="_2026-08-29-—-the-project-archive-is-one-workbench-not-three-bands-d267" tabindex="-1">2026-08-29 — The project archive is one workbench, not three bands (D267) <a class="header-anchor" href="#_2026-08-29-—-the-project-archive-is-one-workbench-not-three-bands-d267" aria-label="Permalink to &quot;2026-08-29 — The project archive is one workbench, not three bands (D267)&quot;">​</a></h2><ul><li>Decision D267 revises D168&#39;s presentation for Settings → Project archive and adopts the D257 one-workbench composition already used by Skills / MCP / Subagents: a quiet intro line, one toolbar, one panel.</li><li>The overview banner is gone. It was a decorative gradient card holding four <code>--text-xl</code> counter tiles, which the design system forbids outright. The four page-level counters go with it: every total they carried is already legible from the per-group strip counts in the panel, so restating them above the toolbar duplicated numbers and gave the destination a header no sibling page has. This retires D168&#39;s overview-counter requirement along with the <code>project.statProjects</code>, <code>project.statOpen</code>, <code>project.statArchived</code>, and <code>project.statSessions</code> keys, which are removed from both catalogs. The intro is now one quiet description line, matching the capability pages.</li><li>The three per-section panels become one panel: Pinned / All projects / Archived are non-interactive in-panel header strips with their counts, so the page has one elevated frame and no heading nested inside the list. The bespoke sort pill reuses the shared segmented-control primitive, and row geometry matches the capability rows (32px controls, 14px list gap, a 28px row glyph); the external uppercase section labels are dropped.</li><li>Beyond the four retired counter keys this is presentation only: row anatomy, row menu grouping, search matching, the batch-of-eight session reveal, activation semantics, and accessibility semantics are all unchanged. No IPC, storage, host protocol, or component-ownership change, so no ADR is required.</li><li>See <code>04-ux/06-settings-ia.md</code>, <code>04-ux/07-ui-design-system.md</code>, E2E-038, and US-UI-23.</li></ul><h2 id="_2026-08-29-—-history-continuation-keeps-the-conversation-outline-reachable-d269" tabindex="-1">2026-08-29 — History continuation keeps the conversation outline reachable (D269) <a class="header-anchor" href="#_2026-08-29-—-history-continuation-keeps-the-conversation-outline-reachable-d269" aria-label="Permalink to &quot;2026-08-29 — History continuation keeps the conversation outline reachable (D269)&quot;">​</a></h2><ul><li>Decision D269 amends D108 / D261 / ADR 0130 in the renderer. D261 made the mounted history a trailing window and fed the minimap the mounted entries so no dash could point at a withheld row. That was correct about dashes, but it left the outline with nothing to say about the history it was hiding, and D108&#39;s visibility rule then removed the rail entirely whenever the mounted tail had fewer than two markers or fit one viewport.</li><li>Two transitions make that reachable in normal use. A bounded tail can be one tool-heavy turn that collapses below one viewport, and a fetched older page can land entirely outside the trailing mounted window. Neither necessarily changes <code>scrollTop</code>, and the only escalation trigger was a native scroll event, so upward travel could stop with older history still on disk and no rail to navigate with.</li><li>The outline now stays present while earlier history exists — withheld loaded rows (<code>hiddenAbove &gt; 0</code>) or an older host page (<code>hasMoreBefore</code>) — and carries one dotted earlier-history continuation at its top. It is a real control, not a message dash: it runs the same two-stage grow-then-fetch path, is disabled and labeled while a page is loading, and never claims a message preview. Ordinary message dashes are still built only from mounted rows, so D261&#39;s reachability rule holds exactly.</li><li>Progress no longer depends on a scroll event. The transcript&#39;s existing top loading row is the observed boundary: while it stays inside the near-top band after an underfilled tail, a fetched-but-withheld page, or a window transition, the transcript keeps escalating. The near-top scroll check is unchanged and now shares one threshold constant with the observer.</li><li>Once no earlier history remains, D108&#39;s two-marker-plus-overflow rule applies unchanged, so a completed short conversation still shows no rail. Renderer only: no IPC, storage, host protocol, or pagination change, and the mounted window budgets are untouched.</li><li>See <code>04-ux/08-component-spec.md</code> §7 and §8, ADR 0130, and E2E-159.</li></ul><h2 id="_2026-08-30-—-one-model-picker-for-both-credential-kinds-d270" tabindex="-1">2026-08-30 — One model picker for both credential kinds (D270) <a class="header-anchor" href="#_2026-08-30-—-one-model-picker-for-both-credential-kinds-d270" aria-label="Permalink to &quot;2026-08-30 — One model picker for both credential kinds (D270)&quot;">​</a></h2><ul><li>Decision D270 refines D237 / D240 in the renderer. Settings → Model configuration carried two copies of the same model picker: the AI service dialog had the full one, and the vendor account dialog had a reduced copy of it that had drifted.</li><li>The copy shared the list and the <code>ModelBinding</code> shape but had no Advanced disclosure, so an account&#39;s per-model context window, max output and thinking levels were not editable, and it had no explicit binding persistence — an account could not configure the same model capabilities as an AI service.</li><li>One shared renderer component now owns row merging, filtering, selection, the custom-model entry, the chosen pane, the Advanced disclosure and the explicit model-binding persistence. Both dialogs render it; only the left pane heading differs, and the vendor dialog additionally passes its saving state. The account dialog grows to the provider dialog&#39;s two-pane size and the duplicated chosen-pane / custom-model CSS is deleted. D283 later supersedes the catalog-gating clauses for thinking levels.</li><li>Renderer only: no IPC, host protocol, storage, or provider-config schema change, so no ADR is required. The single user-visible change is that vendor accounts now offer the same per-model advanced controls and explicit binding persistence as AI services.</li><li>See <code>04-ux/06-settings-ia.md</code>, <code>04-ux/08-component-spec.md</code> §19, <code>03-runtime/11-provider-model-system.md</code> §10, and E2E-162.</li></ul><h2 id="_2026-08-30-—-quitting-stops-plugins-as-a-shutdown-not-as-a-crash-d272" tabindex="-1">2026-08-30 — Quitting stops plugins as a shutdown, not as a crash (D272) <a class="header-anchor" href="#_2026-08-30-—-quitting-stops-plugins-as-a-shutdown-not-as-a-crash-d272" aria-label="Permalink to &quot;2026-08-30 — Quitting stops plugins as a shutdown, not as a crash (D272)&quot;">​</a></h2><ul><li>Decision D272 refines spec 07 §3.1 in the main process. <code>before-quit</code> disposed the plugin <em>watchers</em> and left the host child processes to be killed by the process teardown that followed.</li><li>An exit nobody announced is indistinguishable from a crash. Nothing set <code>disposing</code>, so the guard at the top of <code>handleChildExit</code> never applied and a normal quit ran the crash path per plugin: a <code>PLUGIN_CRASHED</code> error log at <code>exitCode: 0</code>, a &quot;stopped unexpectedly&quot; toast, and <code>superviseCrash</code> scheduling restarts into an app that was closing. The toast arrived twice, because the runtime and the <code>onPluginCrash</code> handler each sent one for the same event.</li><li>The cost was diagnostic. In one local profile 613 of 633 crash reports were this, all within five seconds of <code>app shutdown</code>, which left the 20 real ones effectively invisible.</li><li><code>disposeAll()</code> now owns quit: mark every plugin disposing and cancel its pending restarts first, in one pass, so a child that dies while a sibling is still stopping is already covered; dispose watchers; then stop services and run <code>onUnload</code> in parallel across plugins. The sequence is bounded — 1.5s per <code>onUnload</code> (shorter than the 5s an explicit unload gets, because the user has asked the app to close) and 3s overall, after which the children are killed outright. The duplicate toast in <code>onPluginCrash</code> is deleted.</li><li>Main process only: no IPC, storage, host-protocol, or manifest change, so no ADR is required. Plugins gain one guarantee they did not have — <code>onUnload</code> runs on quit — and the user stops seeing failure toasts on every clean exit.</li><li>See <code>07-plugins/05-plugin-lifecycle.md</code> §3.1.</li></ul><h2 id="_2026-08-30-—-a-model-s-habitual-argument-name-is-accepted-not-corrected-d273" tabindex="-1">2026-08-30 — A model&#39;s habitual argument name is accepted, not corrected (D273) <a class="header-anchor" href="#_2026-08-30-—-a-model-s-habitual-argument-name-is-accepted-not-corrected-d273" aria-label="Permalink to &quot;2026-08-30 — A model&#39;s habitual argument name is accepted, not corrected (D273)&quot;">​</a></h2><ul><li>Decision D273 refines spec 03 §4 in the agent runtime. The tool schemas named <code>path</code> and <code>pattern</code>; models sent <code>file_path</code> and <code>query</code> anyway.</li><li>The numbers made it a design problem rather than a model problem. Of 1679 failed tool calls in one local month, 852 were a parameter-name miss. 724 of those were <code>Read</code> called with <code>file_path</code> — 87% of every <code>Read</code> failure, and enough on its own to put <code>Read</code>&#39;s error rate at 11%. <code>Write</code> (55), <code>Edit</code> (56), <code>Grep</code> (13), and <code>Glob</code> (4) repeated the pattern with the same two aliases.</li><li>An argument name is a pretraining habit, not something a description can talk a model out of. The rejection also came from the validator before the call reached the host, so the turn was spent on an error the model had no way to anticipate and, from its side, no reason to expect.</li><li><code>Read</code>, <code>Write</code>, <code>Edit</code>, and <code>BrowserPreview</code> now accept <code>file_path</code> beside <code>path</code>; <code>Glob</code> and <code>Grep</code> accept <code>query</code> beside <code>pattern</code>. Both spellings are optional in the schema so either one validates, and the runtime folds the alias onto the canonical name before anything downstream sees it — the write-lock key, the <code>tools.execute</code> args, and the persisted transcript carry canonical names only, so nothing below this line learns two spellings. Exactly one of each pair is required; the canonical name wins when both arrive.</li><li><code>Bash.timeout</code> had a second version of the same problem: 69 failures were a timeout in milliseconds (600000, 900000, 1800000) against a field capped at 300 seconds. The schema maximum widens to 3600000 — above the largest value observed, 1_800_000 — so a millisecond value validates at all, and the runtime reads a value of at least 1000 as milliseconds, clamped to the honoured 300-second ceiling. The threshold is 1000 rather than 300 deliberately: something like 301 is far more likely a seconds value that overshot the cap, and rewriting it to 0.301s would be worse than the error it earns today.</li><li>Runtime only: no IPC, storage, or host-protocol change. host-core&#39;s schemas and its argument parsing are untouched, because the runtime normalizes above them and <code>tools.list</code> has no other consumer.</li><li>See <code>03-runtime/03-tools-and-permissions.md</code> §4.</li></ul><h2 id="_2026-08-31-—-retry-an-unchanged-edited-prompt-d274" tabindex="-1">2026-08-31 — Retry an unchanged edited prompt (D274) <a class="header-anchor" href="#_2026-08-31-—-retry-an-unchanged-edited-prompt-d274" aria-label="Permalink to &quot;2026-08-31 — Retry an unchanged edited prompt (D274)&quot;">​</a></h2><ul><li>Decision D274 amends D137 in the renderer. Confirming a valid user-prompt edit now always dispatches the existing edit-resend/Regenerate path, even when the trimmed text is identical to the original.</li><li>The inline controls say Retry and Cancel in the active locale. Retry keeps the existing revision archive, identity-based truncation, slash-command expansion, and attachment behavior; Cancel and Escape leave the prompt untouched.</li><li>Renderer and i18n only: no IPC, storage, host-protocol, or runtime contract change. See ADR 0135, <code>04-ux/08-component-spec.md</code>, and E2E-073.</li></ul><h2 id="_2026-08-31-—-preserve-the-active-task-boundary-across-context-compaction-d275" tabindex="-1">2026-08-31 — Preserve the active task boundary across context compaction (D275) <a class="header-anchor" href="#_2026-08-31-—-preserve-the-active-task-boundary-across-context-compaction-d275" aria-label="Permalink to &quot;2026-08-31 — Preserve the active task boundary across context compaction (D275)&quot;">​</a></h2><ul><li>Decision D275 amends D203 / ADR 0064 in the agent runtime. Checkpoints now persist an opaque <code>details.retainedTailMode</code> boundary.</li><li>While a provider must continue an active tool turn, the checkpoint retains only the latest user message, bounded by the existing 20,000-token limit. At a completed turn boundary, before a new prompt, or for manual compaction, it retains no naked historical user messages; the summary is authoritative and the next prompt is the new task.</li><li>Legacy checkpoints without the mode normalize to their latest user message. The visible transcript, host-owned storage, and protocol shape are unchanged. See ADR 0136, the runtime/storage/tool-result specs, and E2E-164.</li></ul><h2 id="_2026-08-31-—-a-visited-session-keeps-its-own-mounted-pane-d276" tabindex="-1">2026-08-31 — A visited session keeps its own mounted pane (D276) <a class="header-anchor" href="#_2026-08-31-—-a-visited-session-keeps-its-own-mounted-pane-d276" aria-label="Permalink to &quot;2026-08-31 — A visited session keeps its own mounted pane (D276)&quot;">​</a></h2><ul><li>Decision D276 amends D162 and ADR 0130 clauses 4–5. The chat surface mounts one pane per recently visited session, bounded to three, so switching is a visibility swap instead of re-pointing one transcript at different data.</li><li>A warm switch reveals the destination pane with its own scroll position on the first frame. A cold switch keeps the current pane fully legible under the thin progress track with the composer inert; the opacity dim is removed.</li><li>First activation still settles at the newest turn; a revisit restores that pane&#39;s offset. Hidden panes keep their layout box, stay inert, and perform no follow scrolling, pagination, or measurement.</li><li>Renderer only: no IPC, storage, host-protocol, or runtime contract change. See ADR 0137, <code>04-ux/08-component-spec.md</code>, <code>04-ux/09-interaction-patterns.md</code>, E2E-011, E2E-071d, and E2E-071g.</li></ul><h2 id="_2026-08-31-—-concurrent-subagents-may-message-each-other-directly-d277" tabindex="-1">2026-08-31 — Concurrent subagents may message each other directly (D277) <a class="header-anchor" href="#_2026-08-31-—-concurrent-subagents-may-message-each-other-directly-d277" aria-label="Permalink to &quot;2026-08-31 — Concurrent subagents may message each other directly (D277)&quot;">​</a></h2><ul><li>Decision D277 extends D201 / ADR 0062 / ADR 0089 without amending them. A session-scoped mailbox lets concurrent delegates exchange bounded text through one opt-in tool — <code>Peer(action=...)</code> — declarable in a definition&#39;s <code>tools:</code> list. The <code>action</code> parameter selects the operation: <code>send</code> (with <code>to?</code>, <code>text</code>, <code>topic?</code>, <code>inReplyTo?</code>), <code>inbox</code> (with <code>from?</code>), or <code>wait</code> (with <code>timeoutSeconds?</code>, <code>from?</code>). The three operations were originally three tools (<code>PeerSend</code> / <code>PeerInbox</code> / <code>PeerWait</code>); ADR 0140 folded them into one tool so peer messaging counts and reads as a single capability.</li><li>It exists because the parent writes every brief before any delegate starts, so it cannot always know the directions are independent. The path lock prevents a torn write but not a stale premise, and relaying notes through a parent that is blocked in <code>TaskWait</code> would spend the context delegation exists to protect.</li><li>Opt-in and default-off: no builtin declares <code>Peer</code>, so every existing definition keeps ADR 0062 isolation. A definition declaring only <code>Peer</code> and no working tool is refused at <code>Task</code> time.</li><li>Addressing is by agent name, never delegation id; the sender is bound by the runtime at spawn, so <code>from</code> is not a model input. The <code>Peer</code> tool is built per delegate and stays out of the session tool catalog, so the parent has no second channel to its delegates.</li><li>Messages are in-process: no <code>permissionScope</code>, no host-core call, no tool budget. Bounds are 2,000 characters per message, 64 messages per inbox (oldest dropped, loss reported), 60 sends per run, and a 120-second wait ceiling below the 300-second idle watchdog. Draining is destructive.</li><li>Peer traffic never enters the parent&#39;s model context, and a delegate is told its report remains the only thing the parent reads. No cross-session messaging, no messaging with the parent, no nested delegation, no durable history.</li><li>Runtime and shared only: no IPC, storage, host-protocol, or renderer change, and no <code>AgentEventEnvelope</code> change — a peer message is already a delegate tool call attributed by <code>parentToolCallId</code> and <code>agentName</code>. See ADR 0138, ADR 0140, <code>03-runtime/02-agent-runtime.md</code> §5f.2, and E2E-165.</li></ul><h2 id="_2026-09-01-—-subagent-model-selection-d278" tabindex="-1">2026-09-01 — Subagent model selection (D278) <a class="header-anchor" href="#_2026-09-01-—-subagent-model-selection-d278" aria-label="Permalink to &quot;2026-09-01 — Subagent model selection (D278)&quot;">​</a></h2><ul><li><p>Decision D278 extends D201 / ADR 0062 / ADR 0089 in the agent runtime and provider-model system. The <code>Task</code> tool gains an optional <code>model</code> parameter (<code>&quot;provider/modelId&quot;</code>) that overrides the delegate&#39;s frontmatter pin for that run. Resolution priority: Task.model → definition frontmatter pin → session model.</p></li><li><p><code>ModelBinding.availableForSubagents</code> (boolean, default false) is an opt-in flag on provider model bindings. When enabled, the model appears in the delegation catalog injected into the parent agent&#39;s system prompt. The parent sees a model summary listing all available delegation models.</p></li><li><p>When the parent specifies a model that is not configured or not enabled for delegation, the Task tool returns a tool error listing available models. An exact repeat of the current session provider/model is treated as inheritance, equivalent to omitting <code>model</code>, so an empty delegation catalog does not turn the parent model into a false unavailable-model error.</p></li><li><p>Implementation clarification (2026-09-13, ADR subagent-model-opt-in): <code>subagentProviders</code> may contain definition-only pins and is not an override allowlist. The additive <code>subagentModelKeys</code> launch field carries opt-in separately, defaults to empty, and participates in runtime reuse matching. Only launch opt-in keys enter that reuse snapshot. Successfully authorized on-demand results use a separate cache, must not overwrite a pin with a different provider id, and do not retire an idle runtime. Repeating a definition&#39;s own pin key is omit. On-demand matching uses unique provider lookup. The Task catalog discloses each definition&#39;s default model.</p></li><li><p>Scope clarification (2026-09-15, issue #386): the opt-in governs every entry point through which the AI picks a model for delegated work, not only <code>Task.model</code>. A <code>session/collaboration/spawn</code> <code>modelKey</code> naming a model without the opt-in is refused with <code>PERMISSION_DENIED</code> before a worker is created; omitting the key, or naming the default model&#39;s own key, remains inheritance. <code>models.list</code> still reports every ready model with its flag — the flag advises the caller and is authoritative only in Electron main.</p></li><li><p>Models not pre-resolved at sidecar launch are resolved on-demand via the <code>provider.resolveSubagentModel</code> RPC to Electron main, where credentials and the models.dev snapshot live.</p></li><li><p>See <code>03-runtime/02-agent-runtime.md</code> §5f, <code>03-runtime/11-provider-model-system.md</code> §7, and E2E-166.</p></li></ul><h2 id="_2026-09-01-—-native-window-edge-resize-settles-before-recovery-d279" tabindex="-1">2026-09-01 — Native window edge resize settles before recovery (D279) <a class="header-anchor" href="#_2026-09-01-—-native-window-edge-resize-settles-before-recovery-d279" aria-label="Permalink to &quot;2026-09-01 — Native window edge resize settles before recovery (D279)&quot;">​</a></h2><ul><li>The main window keeps Electron&#39;s native edge and corner resize ownership on every platform, including the frameless Windows/Linux shell, with the existing 1040×700 minimum. Renderer drag regions do not replace OS hit testing or introduce a second resize path.</li><li>Bounds recovery waits for a 300ms stable native bounds snapshot before it can restore a tiny/shelved window. Normal base bounds remain debounced by 600ms after the last resize/move event, while close still flushes the latest stable state. A panel reservation&#39;s expected <code>setBounds</code> result is excluded from cross-display user-move attribution.</li><li>No IPC, storage, host protocol, or renderer resize contract changes. This refines D156/D163 and is covered by E2E-167.</li></ul><h2 id="_2026-09-01-—-expanded-sidebar-width-is-user-resizable-d280-superseded-by-d408" tabindex="-1">2026-09-01 — Expanded sidebar width is user-resizable (D280; superseded by D408) <a class="header-anchor" href="#_2026-09-01-—-expanded-sidebar-width-is-user-resizable-d280-superseded-by-d408" aria-label="Permalink to &quot;2026-09-01 — Expanded sidebar width is user-resizable (D280; superseded by D408)&quot;">​</a></h2><ul><li>The expanded sidebar owns a persisted preferred width with a <code>240..520px</code> clamp and a <code>275px</code> default. The right-edge renderer handle previews width from the pointer-down position, lets MainChat reflow continuously, and saves only on pointer release.</li><li>The handle is a vertical ARIA separator. ArrowLeft/ArrowRight adjust it in 16px steps, Home and End select the bounds, and keyboard changes commit immediately. Escape, pointer cancellation, lost ownership, and unmount roll back an in-progress pointer gesture to its starting width.</li><li>Sidebar collapse remains independent from the preferred expanded width. No IPC, native-window bounds, work-panel reservation, or project/session order contract changes. See ADR 0141 and E2E-168.</li><li>D408 later pinned the live shell at 275px and hid the handle. D459 / ADR 0290 restores the resizable handle and adds collapse below 160px.</li></ul><h2 id="_2026-09-01-—-non-loopback-http-mcp-endpoints-are-supported-d281" tabindex="-1">2026-09-01 — Non-loopback HTTP MCP endpoints are supported (D281) <a class="header-anchor" href="#_2026-09-01-—-non-loopback-http-mcp-endpoints-are-supported-d281" aria-label="Permalink to &quot;2026-09-01 — Non-loopback HTTP MCP endpoints are supported (D281)&quot;">​</a></h2><ul><li>User-owned and plugin-declared MCP servers accept absolute <code>http://</code> and <code>https://</code> URLs, including trusted LAN addresses. Unsupported schemes and malformed URLs remain invalid.</li><li>The user MCP editor shows an explicit unencrypted-connection warning for non-loopback HTTP. Plugin-declared endpoints still require <code>mcp.server.remote</code> and a host covered by <code>manifest.net.domains</code>.</li><li>The MCP client turns off automatic redirects, limits the chain to five hops, accepts only HTTP(S) targets, and re-checks the plugin egress policy for every hop. A redirect to an undeclared host is blocked before the next request.</li><li>Marketplace catalog and package download rules remain unchanged: non-loopback HTTP package downloads are still refused. See ADR 0142 and E2E-024K/E2E-100.</li></ul><h2 id="_2026-09-01-—-user-renamable-task-titles-d282" tabindex="-1">2026-09-01 — User-renamable task titles (D282) <a class="header-anchor" href="#_2026-09-01-—-user-renamable-task-titles-d282" aria-label="Permalink to &quot;2026-09-01 — User-renamable task titles (D282)&quot;">​</a></h2><ul><li>A task name is session metadata and can be edited from the Sidebar session overflow/right-click menu or the Project archive task row. Both entry points open the same localized modal editor with focus management, Escape/Cancel dismissal, and a 1–80 Unicode code-point input bound.</li><li>The existing <code>session.rename</code> IPC/RPC path validates and trims the title at the host boundary, returning <code>INVALID_PARAMS</code> for blank or overlong values. A successful rename updates only <code>sessions.title</code>; it does not update <code>updated_at</code>, transcript data, message count, project binding, empty-session state, or historical notification title snapshots.</li><li>A custom title continues to suppress first-prompt automatic title generation. The current summary is projected consistently in the Sidebar, topbar, Project archive, and search after save and after restart. See ADR 0143 and E2E-021a.</li></ul><h2 id="_2026-09-01-—-user-configurable-thinking-level-overrides-d283" tabindex="-1">2026-09-01 — User-configurable thinking-level overrides (D283) <a class="header-anchor" href="#_2026-09-01-—-user-configurable-thinking-level-overrides-d283" aria-label="Permalink to &quot;2026-09-01 — User-configurable thinking-level overrides (D283)&quot;">​</a></h2><ul><li><code>ModelInfo</code> remains the raw models.dev record. An existing <code>ModelBinding</code> no longer overwrites its published reasoning fields or capability tags, so a catalogued reasoning model remains visible as such in Settings.</li><li>Settings always renders the seven canonical thinking levels. Published levels seed new known-model bindings; unknown and catalogued non-reasoning models show the same choices unselected and can be explicitly enabled for a compatible endpoint.</li><li>The exact model binding owns effective thinking capability. Its explicit levels are preserved at save time and used by Composer, Electron main, and the sidecar without intersecting them with the catalog. Empty or <code>off</code>-only bindings resolve to <code>off</code>; there is no automatic reasoning inference. No IPC, storage, or host protocol change. See ADR 0144 and E2E-005/E2E-050/E2E-162/ E2E-163.</li></ul><h2 id="_2026-09-01-—-settings-rail-uses-a-compact-label-scale-d284" tabindex="-1">2026-09-01 — Settings rail uses a compact label scale (D284) <a class="header-anchor" href="#_2026-09-01-—-settings-rail-uses-a-compact-label-scale-d284" aria-label="Permalink to &quot;2026-09-01 — Settings rail uses a compact label scale (D284)&quot;">​</a></h2><ul><li>Settings navigation uses short, parallel destination labels: General, AI, Shortcuts, Instructions, Models, Skills, MCP, Subagents, Import, Projects, and Info. The Chinese locale uses the corresponding concise labels 常规、AI、 快捷键、指令、模型、技能、MCP、子智能体、导入、项目 and 信息.</li><li>Visual clusters are named Preferences, Agent, Workspace, and System (偏好、智能体、工作区、系统). Destination IDs, order, search behavior, and content ownership remain unchanged.</li><li>Rail labels may stay shorter than the descriptive selected-page title, such as Model configuration or Project archive, so the navigation stays aligned without removing context from the content pane.</li></ul><h2 id="_2026-09-01-—-native-macos-intel-release-lane-d285" tabindex="-1">2026-09-01 — Native macOS Intel release lane (D285) <a class="header-anchor" href="#_2026-09-01-—-native-macos-intel-release-lane-d285" aria-label="Permalink to &quot;2026-09-01 — Native macOS Intel release lane (D285)&quot;">​</a></h2><ul><li>D285 extends D126 and ADR 0145. Tag builds now use <code>macos-15</code> for arm64 and <code>macos-15-intel</code> for Intel x64, alongside the existing Windows x64 and Linux x64 lanes.</li><li>The static macOS targets expose DMG and ZIP without a fixed architecture; each native runner passes the matching electron-builder flag and builds the Rust host sidecar locally. The workflow rejects a runner architecture that does not match the matrix entry.</li><li>macOS updater feeds are renamed per architecture before artifact upload and merged into one <code>latest-mac.yml</code> during publication. macOS remains notify-and-link until a signed in-app channel is qualified.</li></ul><h2 id="_2026-09-02-—-work-panel-outer-and-inner-resize-ownership-d286" tabindex="-1">2026-09-02 — Work panel outer and inner resize ownership (D286) <a class="header-anchor" href="#_2026-09-02-—-work-panel-outer-and-inner-resize-ownership-d286" aria-label="Permalink to &quot;2026-09-02 — Work panel outer and inner resize ownership (D286)&quot;">​</a></h2><ul><li>With the right work panel open, the outer native right edge and right corners resize the panel target (<code>244..720px</code>) while preserving the base conversation width. Main previews and commits that target through an Electron-local event; left and other native edges retain base chat resizing.</li><li>The inner renderer divider resizes the base conversation width through the bounded <code>window/setWorkPanelChatWidth</code> channel (<code>1040..10000px</code>) while the panel reservation remains unchanged. Pointer updates are serialized and cancellation restores the press-time target.</li><li>This is an Electron-local behavior change; the host protocol remains v9. See ADR 0146 and E2E-056/E2E-167.</li></ul><h2 id="_2026-09-03-—-transcript-settle-veil-and-pre-paint-follow-d287" tabindex="-1">2026-09-03 — Transcript settle veil and pre-paint follow (D287) <a class="header-anchor" href="#_2026-09-03-—-transcript-settle-veil-and-pre-paint-follow-d287" aria-label="Permalink to &quot;2026-09-03 — Transcript settle veil and pre-paint follow (D287)&quot;">​</a></h2><ul><li>A session whose first commit is bounded (history above the initial mount budget) mounts under an opaque skeleton veil in that same commit. The veil covers the transcript scroller only; the docked composer stays visible and usable. It lifts once the scroller&#39;s <code>scrollHeight</code> and <code>clientHeight</code> have read the same for three consecutive frames, or after a 600ms cap, and fades over 160ms. Each sampled frame re-pins a pinned transcript so the revealed frame is at the newest turn. The minimap and jump control mount after the veil lifts. Short transcripts never show it.</li><li>Pinned follow re-pins inside the content ResizeObserver callback, before the browser paints, instead of requesting a frame from it: the requested frame landed after the grown content had already painted unpinned once. The content is observed on its border box so the composer&#39;s published bottom reserve (padding) is a follow trigger too; the scroller itself is observed so viewport resizes keep the bottom in view.</li><li>Sending clears the composer before the host round trip and restores the draft if the store rejects the send; text typed after a failed send is never overwritten. The send reads the textarea&#39;s live value, and the two silent refusals (model not configured, paste still saving) now surface a toast. See E2E-071i.</li></ul><h2 id="_2026-09-03-—-user-row-inserted-before-the-host-round-trip-d288" tabindex="-1">2026-09-03 — User row inserted before the host round trip (D288) <a class="header-anchor" href="#_2026-09-03-—-user-row-inserted-before-the-host-round-trip-d288" aria-label="Permalink to &quot;2026-09-03 — User row inserted before the host round trip (D288)&quot;">​</a></h2><ul><li>The user&#39;s prompt appears in the transcript in the frame it is sent, not when the host echoes it. Before this the row waited on the host&#39;s turn setup (runtime launch, <code>session.beginTurn</code>, attachment preparation, persistence) and a slow host left the transcript unchanged for visibly long after Enter.</li><li>The renderer mints the message id (<code>crypto.randomUUID()</code>), inserts the row under it (<code>optimisticUserMessage</code>), and sends the id as <code>AgentPromptRequest.messageId</code>. The host persists and echoes the durable row under that id when it is a UUID the session does not already hold, otherwise it mints its own. The echo therefore upserts the optimistic row in place; a durable read that already holds the echo wins over it, and one that does not yet hold it keeps the row visible (<code>mergeLiveSessionMessages</code>).</li><li>The optimistic row shows the typed text and its file references under their source paths; the echo brings the expanded slash body, the <code>command</code> chip, session-scoped attachment refs and revision metadata.</li><li>Withdrawal: when the send never reached the host (rejected by a pending plan, or the IPC failed) the renderer removes its own row object only, so a failure after persistence leaves the durable echo in place next to the error row. Smart stop already treats the row as the sent prompt and pulls it back into the composer. A visible session inserts into <code>messages</code>; a background session (queued prompt draining) inserts into its renderer cache, where the echo lands too.</li><li>Edit-resend follows the same path: the rewritten prompt replaces the old row immediately and the host echo settles it. See E2E-071i.</li></ul><h2 id="_2026-09-03-—-explicitly-disable-application-keyboard-shortcuts-d289" tabindex="-1">2026-09-03 — Explicitly disable application keyboard shortcuts (D289) <a class="header-anchor" href="#_2026-09-03-—-explicitly-disable-application-keyboard-shortcuts-d289" aria-label="Permalink to &quot;2026-09-03 — Explicitly disable application keyboard shortcuts (D289)&quot;">​</a></h2><ul><li>Application shortcut overrides have three states: an absent property uses the platform default, a valid portable binding string customizes it, and explicit JSON <code>null</code> means <code>Unbound</code> and dispatches nothing. Invalid strings retain the existing safe fallback to the default.</li><li>Settings exposes Disable separately from Restore default and renders a localized Unbound state. Unbound actions do not participate in conflicts and remain editable. The shared state drives renderer matching, macOS menu accelerators, and the global plugin launcher.</li><li>Unbinding <code>openPluginLauncher</code> unregisters any prior Electron global shortcut, disables the Windows host hook, and removes the focused-window fallback. The host hook starts disabled until Electron applies the effective settings. Configurable macOS native-role menu items become role-less clickable items when unbound so Electron cannot restore their default accelerator.</li><li>The existing settings JSON, IPC methods, protocol version, storage schema, and plugin-local shortcut contract remain unchanged. See ADR 0148 and E2E-072.</li></ul><h2 id="_2026-09-04-—-calm-running-status-motion-in-the-transcript-d290" tabindex="-1">2026-09-04 — Calm running status motion in the transcript (D290) <a class="header-anchor" href="#_2026-09-04-—-calm-running-status-motion-in-the-transcript-d290" aria-label="Permalink to &quot;2026-09-04 — Calm running status motion in the transcript (D290)&quot;">​</a></h2><ul><li>Replace the high-contrast text shimmer used by the Working indicator and live activity labels with static readable text plus compact status markers. The pre-stream Working indicator uses three small staggered dots; active activity labels use one small pulse marker, while tool spinners and the existing assistant streaming rail retain their state feedback.</li><li>The marker pulse is compositor-friendly, capped at a one-second loop, and is disabled under <code>prefers-reduced-motion</code>; the status remains visible as static text and dots. No protocol, storage, or runtime behavior changes. See ADR 0149 and E2E-053/E2E-083.</li></ul><h2 id="_2026-09-04-—-replace-the-empty-home-mascot-sprite-with-a-calm-inline-agent-mark-d291" tabindex="-1">2026-09-04 — Replace the empty-home mascot sprite with a calm inline agent mark (D291) <a class="header-anchor" href="#_2026-09-04-—-replace-the-empty-home-mascot-sprite-with-a-calm-inline-agent-mark-d291" aria-label="Permalink to &quot;2026-09-04 — Replace the empty-home mascot sprite with a calm inline agent mark (D291)&quot;">​</a></h2><ul><li>Replace the randomized raster pose atlas in the empty-home hero with a 100px inline SVG containing a compact neutral agent, a thin orbit, and one signal point. The core breathes slowly and the eyes blink occasionally.</li><li>The mark is deterministic and decorative. Pointer hover does not alter its cadence or geometry; reduced motion freezes its CSS animation while keeping the same size, placement, and semantic colors.</li><li>No protocol, storage, or runtime behavior changes. See ADR 0150 and E2E-046/E2E-099/US-UI-17.</li></ul><h2 id="_2026-09-04-—-keep-the-work-panel-inside-the-fixed-application-window-d292" tabindex="-1">2026-09-04 — Keep the work panel inside the fixed application window (D292) <a class="header-anchor" href="#_2026-09-04-—-keep-the-work-panel-inside-the-fixed-application-window-d292" aria-label="Permalink to &quot;2026-09-04 — Keep the work panel inside the fixed application window (D292)&quot;">​</a></h2><ul><li>The work panel remains an in-flow right column. Opening and collapsing it animates flex allocation inside the existing client area instead of expanding or shrinking the native BrowserWindow.</li><li>The renderer-owned inner divider adjusts the persisted <code>244..720px</code> panel width; native window edges resize only the application window and never rewrite the panel preference. Escape, cancellation, and lost pointer capture restore the press-time panel width.</li><li>The <code>window/setWorkPanelReservation</code> seam is retained for compatibility but normalizes every valid request to <code>{ requested: 0, reserved: 0 }</code>. The native Browser view still follows the renderer-measured panel rectangle. See ADR 0151 and E2E-056/E2E-160/E2E-167.</li></ul><h2 id="_2026-09-04-—-play-an-eight-frame-waving-mascot-on-empty-home-d293" tabindex="-1">2026-09-04 — Play an eight-frame waving mascot on empty home (D293) <a class="header-anchor" href="#_2026-09-04-—-play-an-eight-frame-waving-mascot-on-empty-home-d293" aria-label="Permalink to &quot;2026-09-04 — Play an eight-frame waving mascot on empty home (D293)&quot;">​</a></h2><ul><li>Replace the empty-home inline SVG agent mark with a processed eight-frame GIF in the existing 100px slot. The loop holds the idle frame briefly, then plays the wave; pointer hover does not change cadence.</li><li>Reduced motion swaps the GIF for the still first-frame PNG through CSS. The mark stays decorative with no random selection, JavaScript timer, or hover state.</li><li>No protocol, storage, or runtime behavior changes. See ADR 0152 and E2E-046/E2E-099/US-UI-17.</li></ul><h2 id="_2026-09-04-—-theme-specific-empty-home-mascot-gifs-d294" tabindex="-1">2026-09-04 — Theme-specific empty-home mascot GIFs (D294) <a class="header-anchor" href="#_2026-09-04-—-theme-specific-empty-home-mascot-gifs-d294" aria-label="Permalink to &quot;2026-09-04 — Theme-specific empty-home mascot GIFs (D294)&quot;">​</a></h2><ul><li>Replace the single empty-home mascot GIF with dedicated light and dark eight-frame assets plus matching still PNGs. CSS follows <code>document.documentElement[data-theme]</code>; anything other than <code>light</code> uses the dark pair.</li><li>Reduced motion still swaps to the first-frame PNG of the active theme. Playback, hover, and decorative role are unchanged.</li><li>No protocol, storage, or runtime behavior changes. See ADR 0152 and E2E-046/E2E-099/US-UI-17.</li></ul><h2 id="_2026-09-04-—-notification-inbox-lists-failures-only-d295" tabindex="-1">2026-09-04 — Notification inbox lists failures only (D295) <a class="header-anchor" href="#_2026-09-04-—-notification-inbox-lists-failures-only-d295" aria-label="Permalink to &quot;2026-09-04 — Notification inbox lists failures only (D295)&quot;">​</a></h2><ul><li>The sidebar-footer inbox renders only <code>task.failed</code> rows, and its unread badge counts only unread failures. Routine successful completions had buried the rows that actually need attention.</li><li>Host-core still persists <code>task.completed</code> rows unchanged. They continue to drive the sidebar session outcome badge and the unfocused-window native notification; only the inbox popover filters them out at the renderer boundary (<code>inboxNotifications</code> / <code>inboxUnreadCount</code>).</li><li>Mark-all-read and clear still operate on the full durable record, so hidden completions are read or removed together with the visible failures. No protocol or storage changes. Supersedes only the &quot;task completion rows&quot; clause of D117.</li></ul><h2 id="_2026-09-04-—-extensions-page-draws-no-dividers-d296" tabindex="-1">2026-09-04 — Extensions page draws no dividers (D296) <a class="header-anchor" href="#_2026-09-04-—-extensions-page-draws-no-dividers-d296" aria-label="Permalink to &quot;2026-09-04 — Extensions page draws no dividers (D296)&quot;">​</a></h2><ul><li>The Extensions destination replaces every in-flow rule with tone and spacing: the header and toolbar lose their bottom borders, the segmented tabs sit on a tinted pill track with the active tab raised by shadow, and the installed index is a stack of soft tiles under each group label instead of one hairline-separated panel. Marketplace source settings, category chips, cards, empty states, the row detail block and the detail sheet&#39;s install strip and sections follow the same three-layer scheme (page, tile, raised). Chips that carried inset rings are filled instead.</li><li>Hairlines remain only on floating layers — row and page menus, tooltips, the detail sheet and dialogs — where an edge is an elevation cue rather than a partition. The menu group separator becomes a gap.</li><li>The detail sheet reserves the titlebar band on every platform, not only Windows/Linux: the route surface keeps a transform after its entry animation, so the sheet&#39;s fixed layer stacks inside the route and the band painted over its title row on macOS.</li><li>Capture rig: the plugins fixture is re-seeded after the page mounts (the mount refresh had been replacing it), the marketplace search and detail calls are stubbed while seeded, and <code>pi-plugins-row-details</code> / <code>pi-plugins-sheet</code> scenes are added. No protocol, storage, or runtime behavior changes. Refines the Installed-tab clause of D169 and §3.5 of the UI IA.</li></ul><h2 id="_2026-09-05-—-scrollbars-show-only-on-hover-or-while-scrolling-d300" tabindex="-1">2026-09-05 — Scrollbars show only on hover or while scrolling (D300) <a class="header-anchor" href="#_2026-09-05-—-scrollbars-show-only-on-hover-or-while-scrolling-d300" aria-label="Permalink to &quot;2026-09-05 — Scrollbars show only on hover or while scrolling (D300)&quot;">​</a></h2><ul><li>Every renderer scroll container now uses the one quiet scrollbar defined in <code>base.css</code>: 8px, trackless, thumb transparent at rest. The thumb is painted only while the owning scroller is hovered (<code>:hover::-webkit-scrollbar-thumb</code>) or carries <code>data-scrolling</code>, and strengthens under the pointer or while dragged. Previously the global thumb was always visible at 16% ink and only the two sidebar lists hid it at rest (D147, E2E-157).</li><li><code>lib/scrollbar-reveal.ts</code>, installed from <code>main.tsx</code>, supplies the state CSS cannot express: a passive capture <code>scroll</code> listener on <code>document</code> marks the element that scrolled with <code>data-scrolling</code> and clears it 800 ms after its last scroll event; a viewport scroll marks <code>&lt;html&gt;</code>. Programmatic scrolls (pinned-follow while streaming) reveal the thumb too, matching macOS overlay bars. Disposal clears every mark it set.</li><li>Partials no longer set <code>scrollbar-width</code> or <code>scrollbar-color</code> (ask-tool options, sub-agent rows, tool fields, sidebar lists). WebKit and Chromium ignore <code>::-webkit-scrollbar</code> on any element that sets either, so those surfaces had been rendering an always-visible native bar instead of the custom one. The sidebar lists keep their 6px width, 20% ink, and focus-within reveal on top of the global rule; the code-block override is removed as redundant. <code>interaction-polish.test.mjs</code> asserts the rest/reveal rules and the absence of the standard properties; <code>scrollbar-reveal.test.mjs</code> covers the mark lifecycle. Supersedes the scrollbar clause of D147 and generalises the sidebar treatment in the UI design system §sidebar. No protocol, storage, or runtime behavior changes.</li></ul><h2 id="_2026-09-05-—-divider-free-surfaces-across-the-app-d297" tabindex="-1">2026-09-05 — Divider-free surfaces across the app (D297) <a class="header-anchor" href="#_2026-09-05-—-divider-free-surfaces-across-the-app-d297" aria-label="Permalink to &quot;2026-09-05 — Divider-free surfaces across the app (D297)&quot;">​</a></h2><ul><li>Extends D296 from the Extensions page to every renderer surface. In-flow strokes — panel rings, card outlines, row and section dividers, left rails, form-field borders, inset hairline chips, the sidebar footer and rail seams, the work panel&#39;s edge and header rules, the composer&#39;s elevation stroke — are replaced by three tonal layers plus spacing: page, <code>--ds-tile</code> (3.5% text mix; hover 6%, deep 8%) and <code>--ds-raised</code> with <code>--ds-raised-shadow</code>. The tokens live in <code>tokens.css</code>; the D296 <code>--plugins-*</code> variables alias them.</li><li>Lists become stacks of tile rows with a 2–6px gap (settings rows, shortcut map, capability pages, provider and model rows, project archive, session import, notification inbox, version history). Selection (theme and language cards, segmented controls, level filters) is a raised pill or tile plus the existing check; no selected border. Form controls are tile fills with an accent focus ring. Markdown loses code-block, table, blockquote and heading rules (zebra rows, tile plates, spacing). Subagent trees draw connectors as 2px tinted bars on a tile group with raised nodes.</li><li>Floating layers keep their edge: menus, popovers, autocomplete, the search dialog, dialogs, tooltips, toasts and hover cards retain <code>0 0 0 0.5px border-default</code> plus shadow, but rules inside them are gone. Control affordances (switch focus ring, scrollbar padding, spinner strokes, status-dot knockouts) are not dividers and stay.</li><li>The theme-card check glyph takes <code>--ds-bg-primary</code> ink instead of <code>#fff</code>, which was invisible on the white dark-theme accent. Style tests assert the new scheme per file. No protocol, storage, or runtime behavior changes. Supersedes the stroke rows of §6.4 in the UI design system and the D148 0.5px field stroke.</li></ul><h2 id="_2026-09-05-—-streaming-replies-are-checkpointed-and-recovered-d299" tabindex="-1">2026-09-05 — Streaming replies are checkpointed and recovered (D299) <a class="header-anchor" href="#_2026-09-05-—-streaming-replies-are-checkpointed-and-recovered-d299" aria-label="Permalink to &quot;2026-09-05 — Streaming replies are checkpointed and recovered (D299)&quot;">​</a></h2><ul><li>The assistant reply currently streaming in a session is checkpointed by Electron main to host-core at most every 1.5 s (<code>session.saveInflightMessage</code>) into <code>sessions/&lt;id&gt;.inflight.json</code>, one atomically replaced file per session. Previously a reply became durable only at <code>message_end</code>, so an app quit, sidecar crash, or restart mid-reply lost the whole text the user had already watched stream; after relaunch the session showed only the prompt.</li><li>The checkpoint is transient. The final row of the same id removes it, a <code>completed</code>/<code>error</code> turn end removes it, and the host boot sweep plus a sidecar-loss <code>session.endTurn { recoverInflight: true }</code> promote a leftover whose final row never landed into the transcript as the turn&#39;s <code>aborted</code> assistant row. A user Stop leaves it for the arriving final row. A late checkpoint for an already-indexed id is dropped. Delegate replies are not checkpointed.</li><li>Quit flushes the checkpoints, aborts active turns through the sidecar, and waits (bounded, 2 s) for their aborted rows to drain before host-core is disposed. The sidecar used to be killed while host-core was already closing.</li><li>Renderer Stop no longer rewrites the transcript to settle a started reply; it settles in memory and leaves the durable copy to the runtime&#39;s aborted final row. That rewrite raced the outbox append and could delete the reply it was meant to keep, and it wrote the renderer&#39;s display-capped rows back over the full ones. The unanswered-prompt undo (and message delete) now rewrite from the full durable transcript merged with the live rows, and the undo is re-evaluated on that merge. ADR 0153, E2E-171.</li></ul><h2 id="_2026-09-05-—-composer-drafts-survive-remounts-and-window-switches-d301" tabindex="-1">2026-09-05 — Composer drafts survive remounts and window switches (D301) <a class="header-anchor" href="#_2026-09-05-—-composer-drafts-survive-remounts-and-window-switches-d301" aria-label="Permalink to &quot;2026-09-05 — Composer drafts survive remounts and window switches (D301)&quot;">​</a></h2><ul><li>Composer drafts stay in renderer memory per session, but the cache is now a module-level map rather than a <code>useRef</code> inside one Composer instance. Home and docked composers still remount when the empty state toggles, and <code>ChatSurface</code> still unmounts when leaving chat for Settings/Plugins/other pages; those remounts previously dropped the in-memory Map, so unsent input vanished on a window/page switch (issue #38).</li><li>The next mount hydrates from the same slot. Unmount (layout cleanup), session switch, file-reference changes, and window blur/visibility-hidden capture the live contenteditable value so a keystroke that has not re-rendered yet is not lost. If Chromium wipes the contenteditable while the window is in the background, focus/visibility restore paints the cached value back. Home file references are keyed by the empty session id, not the <code>__home__</code> cache key.</li><li>A successful send still clears only the submitting session&#39;s slot; deleting a session still drops its slot; nothing is written to disk. <code>composer-draft-cache.ts</code> plus <code>composer-draft-cache.test.mjs</code> cover isolation, prune, remount, and Composer wiring. Extends E2E-011c. No protocol, storage, or runtime behavior changes.</li></ul><h2 id="_2026-09-05-—-an-expanded-delegate-run-follows-its-latest-output-d302" tabindex="-1">2026-09-05 — An expanded delegate run follows its latest output (D302) <a class="header-anchor" href="#_2026-09-05-—-an-expanded-delegate-run-follows-its-latest-output-d302" aria-label="Permalink to &quot;2026-09-05 — An expanded delegate run follows its latest output (D302)&quot;">​</a></h2><ul><li>Decision D302 refines D271 / D071 in the renderer. D271 put a delegate&#39;s nested rows in a bounded <code>.subagent-run-rows</code> scroller so expanding a forty-call run would not grow the parent transcript, but that scroller had no stick-to-bottom. A live card therefore stayed at the offset it had when opened while thinking, tool, and answer rows appended below the viewport (issue #37).</li><li>The nested scroller now uses the parent transcript&#39;s pinned-follow contract independently: expanding pins to the newest row, content growth follows the bottom while pinned, and the first real upward gesture pauses follow and shows a nested jump-to-latest control. Layout clamps and programmatic <code>scrollTo</code> never count as that gesture. Native overflow anchoring is disabled on <code>.subagent-run-rows</code> so it cannot fight pinned follow.</li><li>The jump control overlays a relative wrapper around the scroller, never <code>.subagent-run</code>, so the absolutely positioned collapse rail stays unclipped. Presentation only: no IPC, storage, host-protocol, or runtime change. E2E-173.</li></ul><h2 id="_2026-09-05-—-new-sessions-start-at-the-binding-default-thinking-level-d303" tabindex="-1">2026-09-05 — New sessions start at the binding default thinking level (D303) <a class="header-anchor" href="#_2026-09-05-—-new-sessions-start-at-the-binding-default-thinking-level-d303" aria-label="Permalink to &quot;2026-09-05 — New sessions start at the binding default thinking level (D303)&quot;">​</a></h2><ul><li>Settings already persists <code>ModelBinding.defaultThinkingLevel</code> and E2E-163 requires that value to be the level a new session starts at. The home draft Composer chip and <code>persistSessionAndSelect</code> instead seeded <code>highestSupportedThinkingLevel</code>, so a user who chose Low still opened every new session at Max (or the strongest enabled level). Launch-time fallback could never reach the stored default because the session row already carried that strongest level.</li><li>New drafts and newly persisted sessions now take the selected model&#39;s binding default, clamped onto the enabled ladder. Strongest-enabled remains the fallback only when the binding has no stored default. Switching models on a draft without an active session also reseeds from the new model&#39;s default; an existing session still preserves its current level when the new model supports it.</li><li>No protocol, storage, or runtime clamp change. Existing session rows keep their stored <code>thinkingLevel</code>. Supersedes D153. See E2E-082 / E2E-163 / E2E-174.</li></ul><h2 id="_2026-09-05-—-new-task-reveals-the-empty-destination-before-host-io-d305" tabindex="-1">2026-09-05 — New Task reveals the empty destination before host IO (D305) <a class="header-anchor" href="#_2026-09-05-—-new-task-reveals-the-empty-destination-before-host-io-d305" aria-label="Permalink to &quot;2026-09-05 — New Task reveals the empty destination before host IO (D305)&quot;">​</a></h2><ul><li>New Task used to await <code>session.list</code>, <code>session.create</code>, another <code>session.list</code>, and <code>session.get</code> before leaving the previous transcript. That chain made the click feel stuck: the old conversation stayed on screen, then jumped to empty. The list refresh existed only so a first message sent moments earlier would not look empty; the renderer already knows that case from <code>runningSessions</code>, live rows, and submitted drafts.</li><li>Creating a session now clears the previous conversation on the first frame and inserts the durable row from the <code>session.create</code> summary. Reusing the group&#39;s latest empty session commits an empty transcript on that same frame. Send and paste wait for the in-flight create instead of opening a second slot; keystrokes typed on the empty home during create move onto the new session.</li><li>Decision D305 amends D252 / ADR 0113 timing only. The reuse rule, durable empty slot, and protocol/storage versions are unchanged. ADR 0154, E2E-011a / E2E-011d / E2E-011g.</li></ul><h2 id="_2026-09-05-—-startup-splash-shares-the-macos-sidebar-glass-d304" tabindex="-1">2026-09-05 — Startup splash shares the macOS sidebar glass (D304) <a class="header-anchor" href="#_2026-09-05-—-startup-splash-shares-the-macos-sidebar-glass-d304" aria-label="Permalink to &quot;2026-09-05 — Startup splash shares the macOS sidebar glass (D304)&quot;">​</a></h2><ul><li>On macOS the boot splash (<code>.startup-splash</code>) drops its opaque <code>--ds-bg-primary</code> fill for the sidebar glass recipe: <code>--ds-sidebar-glass-tint</code> plus the top/bottom sheen gradients, over the window&#39;s native <code>under-window</code> vibrancy. The shell mounts under the splash as soon as <code>ready</code> flips, ahead of the minimum dwell; behind glass it would bleed through the tint, so on darwin the shell stays <code>visibility: hidden</code> while the splash is loading and fades in with an opacity transition (not an animation, so the sidebar&#39;s own <code>sidebar-in</code> mount animation is not replayed) under the splash&#39;s exit fade — a cross-fade from glass into the opaque main pane and glass sidebar.</li><li>Windows and Linux keep the opaque splash; the rule is scoped to <code>:root[data-platform=&quot;darwin&quot;]</code> like the sidebar rule. Splash, sidebar, and rail share one glass selector so the recipe cannot drift. The glass tokens&#39; comment now names both consumers. <code>macos-sidebar-vibrancy.test.mjs</code> asserts the splash uses the tint and sheen on darwin and stays <code>--ds-bg-primary</code> in the base rule. Extends §8.3 of the UI design system; no protocol, storage, or runtime behavior changes. E2E-076.</li></ul><h2 id="_2026-09-05-—-search-read-truncation-is-a-cut-not-a-window-d306" tabindex="-1">2026-09-05 — Search/read truncation is a cut, not a window (D306) <a class="header-anchor" href="#_2026-09-05-—-search-read-truncation-is-a-cut-not-a-window-d306" aria-label="Permalink to &quot;2026-09-05 — Search/read truncation is a cut, not a window (D306)&quot;">​</a></h2><ul><li><code>Read.truncated</code> was true whenever a file continued after the returned window, and the 48KB / 500-line cap cut ordinary source and spec tables. Almost every Read then showed the UI truncated chip and told the model to Grep, including after an explicit <code>offset</code>/<code>limit</code> that had already been filled.</li><li><code>truncated</code> is now true only when this result was cut short: the byte budget stopped the window, a line was clipped, or Grep/Glob hid remaining hits behind their caps. A filled Read window of a longer file is <code>truncated: false</code>; <code>notice</code> names the next offset instead of nagging Grep.</li><li>Default Read window returns to 2000 lines (max 4000). <code>BUDGET_SEARCH</code> is 128KB / 4000 lines so that window of typical source actually fits. Per-line clip is 16,384 characters so a decision-log row survives while a minified one-liner still clips.</li><li>Decision D306 amends D033 / D194. Host-core tool results, sidecar Read schema copy, and the truncated chip contract. No ADR: the field stays boolean and callers that already honored <code>truncated</code> keep working. See <code>03-runtime/16-tool-result-limits.md</code> and E2E-147 / E2E-175.</li></ul><h2 id="_2026-09-05-—-revision-payloads-follow-the-live-branch-d307" tabindex="-1">2026-09-05 — Revision payloads follow the live branch (D307) <a class="header-anchor" href="#_2026-09-05-—-revision-payloads-follow-the-live-branch-d307" aria-label="Permalink to &quot;2026-09-05 — Revision payloads follow the live branch (D307)&quot;">​</a></h2><ul><li>A regenerate family&#39;s active variant was archived once, on the first <code>agent_end</code> after the regenerate, and never touched again. The live branch kept growing: every later prompt appended to it, and a turn that ended in a provider error never reached <code>agent_end</code> at all. Paging the root away and back restored the archive taken at that first <code>agent_end</code> and rewrote the transcript and its JSONL from it, deleting every later turn. A real session had 17 messages (a turn that edited files) exposed this way.</li><li>Every operation that discards the live branch now writes it back over its own variant first. <code>session.activateRevision</code> reads the durable transcript, finds the family (root id, or the first user message stamped with that <code>revisionRootId</code>), refreshes the variant the root&#39;s <code>activeRevision</code> stamp names, and only then switches; when the family is present the prefix comes from the transcript, not the renderer&#39;s copy. The regenerate path in the app process passes <code>revisionIndex</code> to <code>session.saveRevision</code> so the discarded tail refreshes its variant instead of being dropped as &quot;already archived&quot;. <code>session.saveActiveRevision</code> refreshes an already-archived index.</li><li>A refresh is one more line in the append-only revisions file (last record for <code>(rootUserId, revisionIndex)</code> wins, as <code>read_revision</code> already did) plus a <code>message_count</code> update; no index row is minted. A branch that was stamped by regenerate but never archived because its turn failed is stored as a new variant, so it can never bury the previous one.</li><li>The switch also carries each surviving message&#39;s owning <code>turn_id</code> across the index rebuild (it used to reset the whole session&#39;s attribution, as <code>replace_messages</code> once did) and keeps checkpoints whose anchors survive.</li><li>Decision D307 amends D109. Host-core <code>sessions.rs</code> / <code>rpc/mod.rs</code> and the regenerate path in the app main process; no renderer change. No ADR: the file format and protocol shape are unchanged, <code>revisionIndex</code> is an optional parameter. See <code>03-runtime/04-data-storage.md</code> §4.9 and <code>03-runtime/06-host-rpc-protocol.md</code>.</li></ul><h2 id="_2026-09-05-—-zhipu-z-ai-named-endpoint-presets-d308" tabindex="-1">2026-09-05 — Zhipu / Z.AI named endpoint presets (D308) <a class="header-anchor" href="#_2026-09-05-—-zhipu-z-ai-named-endpoint-presets-d308" aria-label="Permalink to &quot;2026-09-05 — Zhipu / Z.AI named endpoint presets (D308)&quot;">​</a></h2><ul><li>Adding 智谱 required knowing which of four URLs to paste. The add-provider dialog now offers a Service select for China and international standard API plus GLM Coding Plan. Choosing one fills the name, locks the published Base URL, and stores the models.dev <code>vendorKey</code>.</li><li>Rows stay <code>openai_compatible</code> + <code>chat_completions</code>. pi-ai already speaks those hosts; PI-Desktop does not add a Zhipu apiStyle or SDK.</li><li>Completions requests on a matching URL or vendorKey set <code>thinkingFormat: &quot;zai&quot;</code> and <code>zaiToolStream: true</code>, because the stored provider id is a UUID and cannot use pi-ai&#39;s <code>zai</code> / <code>zai-coding-cn</code> provider-name detection.</li><li>Decision D308 amends D024. See ADR 0155, <code>03-runtime/11-provider-model-system.md</code>, <code>03-runtime/12-provider-config-schema.md</code>, and E2E-005D.</li></ul><h2 id="_2026-09-05-—-add-provider-common-path-is-service-key-d309" tabindex="-1">2026-09-05 — Add-provider common path is Service + key (D309) <a class="header-anchor" href="#_2026-09-05-—-add-provider-common-path-is-service-key-d309" aria-label="Permalink to &quot;2026-09-05 — Add-provider common path is Service + key (D309)&quot;">​</a></h2><ul><li>The add-provider form stacked Service, Name, Base URL, API key, and API format. Named endpoints already know name, URL, and wire format.</li><li>A new dialog starts with Service. Named Zhipu / Z.AI and OpenCode Go then show Service + API key and a host summary. Custom endpoint then shows Name, Base URL, and API key. Advanced keeps Name (named) and API format (custom).</li><li>OpenCode Go moves into the Service select so users do not look for it under API format.</li><li>Decision D309 amends D308. See ADR 0156, <code>04-ux/06-settings-ia.md</code>, and E2E-005 / E2E-005B / E2E-005D.</li></ul><h2 id="_2026-09-05-—-custom-format-beside-the-key-top-models-dev-vendors-d310" tabindex="-1">2026-09-05 — Custom format beside the key; top models.dev vendors (D310) <a class="header-anchor" href="#_2026-09-05-—-custom-format-beside-the-key-top-models-dev-vendors-d310" aria-label="Permalink to &quot;2026-09-05 — Custom format beside the key; top models.dev vendors (D310)&quot;">​</a></h2><ul><li>Custom endpoint now keeps API format beside the API key instead of Advanced.</li><li>Service lists models.dev-backed international and China first-party endpoints, each with a published URL and wire style.</li><li>Decision D310 amends D309. See <code>04-ux/06-settings-ia.md</code> and E2E-005.</li></ul><h2 id="_2026-09-05-—-searchable-service-picker-without-discovery-jank-d311" tabindex="-1">2026-09-05 — Searchable Service picker without discovery jank (D311) <a class="header-anchor" href="#_2026-09-05-—-searchable-service-picker-without-discovery-jank-d311" aria-label="Permalink to &quot;2026-09-05 — Searchable Service picker without discovery jank (D311)&quot;">​</a></h2><ul><li>The Service control was a native <code>&lt;select&gt;</code> with optgroups. Opening it in the overlay was sluggish, and there was no way to filter ~20 vendors.</li><li>It is now a searchable anchored menu, matching the default-model picker: type to filter by localized name, vendor key, alias, or host; groups stay International / China with Custom first.</li><li>Picking a named vendor no longer probes <code>/models</code> until an API key is present. Typing a key no longer paints <code>loading</code> on every keystroke; the hook waits for the 600 ms debounce. Switching endpoints clears the previous list immediately so stale models do not linger.</li><li>Decision D311 amends D310. See <code>04-ux/06-settings-ia.md</code> and E2E-005 / E2E-005D.</li></ul><h2 id="_2026-09-05-—-flat-vendor-service-list-and-xiaomi-d312" tabindex="-1">2026-09-05 — Flat vendor Service list and Xiaomi (D312) <a class="header-anchor" href="#_2026-09-05-—-flat-vendor-service-list-and-xiaomi-d312" aria-label="Permalink to &quot;2026-09-05 — Flat vendor Service list and Xiaomi (D312)&quot;">​</a></h2><ul><li>Service options no longer split International / China. Custom stays first, then one searchable vendor list.</li><li>Xiaomi is a named endpoint: models.dev <code>vendorKey: &quot;xiaomi&quot;</code>, <code>https://api.xiaomimimo.com/v1</code>, Chat Completions. Aliases <code>mimo</code> and <code>xiaomimimo</code> match the catalog.</li><li>Decision D312 amends D310 / D311. See <code>04-ux/06-settings-ia.md</code> and E2E-005.</li></ul><h2 id="_2026-09-05-—-shipped-locale-registry-and-language-picker-d314" tabindex="-1">2026-09-05 — Shipped locale registry and language picker (D314) <a class="header-anchor" href="#_2026-09-05-—-shipped-locale-registry-and-language-picker-d314" aria-label="Permalink to &quot;2026-09-05 — Shipped locale registry and language picker (D314)&quot;">​</a></h2><ul><li>Language in Settings → General was three preview cards (Auto / 简体中文 / English). That does not scale, and names were hard-coded.</li><li>UI locales now live in <code>@pi-desktop/i18n</code>: <code>en</code>, <code>zh-CN</code>, and <code>tr</code>. Native names stay endonyms. Auto still follows <code>app.getLocale()</code> through preload.</li><li>The Appearance card keeps Theme as three preview cards. Language is a searchable picker: Auto pinned at the top with the detected native name, then shipped locales. Search matches native name, English name, and id.</li><li>Plugin labels and the changelog stay <code>en</code> + <code>zh-CN</code> with English fallback.</li><li>Decision D314 amends D073. See ADR 0160, <code>04-ux/02-i18n-english-first.md</code>, <code>04-ux/06-settings-ia.md</code>, and E2E-091.</li></ul><h2 id="_2026-09-05-—-grep-prefers-a-system-rg-when-installed-d315" tabindex="-1">2026-09-05 — Grep prefers a system <code>rg</code> when installed (D315) <a class="header-anchor" href="#_2026-09-05-—-grep-prefers-a-system-rg-when-installed-d315" aria-label="Permalink to &quot;2026-09-05 — Grep prefers a system \`rg\` when installed (D315)&quot;">​</a></h2><ul><li>Codex searches with shell <code>rg</code> when the machine has it. PI-Desktop keeps Grep as the model-facing tool and uses a user-installed <code>rg</code> as its implementation backend when one is on the process PATH or Unix login PATH.</li><li>The result shape, 128KB / <code>headLimit</code> budget, newest-first order, and explicit-<code>path</code> ignore exception stay host-defined. <code>rg</code> is exec&#39;d with a null stdin and argv, not through Bash. Spawn failure or exit 2 falls back to the in-process <code>ignore</code> + <code>regex</code> searcher. <code>PI_DESKTOP_RG</code> selects a binary; <code>PI_DESKTOP_DISABLE_RG</code> forces the fallback.</li><li>The agent still calls Grep rather than shelling out to <code>rg</code>. Read is unchanged. No <code>rg</code> binary is bundled.</li><li>Decision D315 amends ADR 0057 / D195. See <code>03-runtime/03-tools-and-permissions.md</code>, <code>03-runtime/02-agent-runtime.md</code> §7, and E2E-147.</li></ul><h2 id="_2026-09-05-—-searchable-theme-picker-d316" tabindex="-1">2026-09-05 — Searchable theme picker (D316) <a class="header-anchor" href="#_2026-09-05-—-searchable-theme-picker-d316" aria-label="Permalink to &quot;2026-09-05 — Searchable theme picker (D316)&quot;">​</a></h2><ul><li>Theme in Settings → General was three preview cards (System / Light / Dark), with plugin themes wrapping into the same grid.</li><li>Appearance now uses the same searchable picker as Language: System, Light, and Dark pinned at the top, plugin themes after a divider. Search matches labels, descriptions, ids, and plugin ids.</li><li><code>AppSettings.theme</code> and the unavailable-plugin fallback to <code>system</code> are unchanged.</li><li>Decision D316 amends ADR 0160. See ADR 0161, <code>04-ux/06-settings-ia.md</code>, and E2E-091.</li></ul><h2 id="_2026-09-05-—-a2a-addressing-spans-sessions-on-the-same-host-d318" tabindex="-1">2026-09-05 — A2A addressing spans sessions on the same host (D318) <a class="header-anchor" href="#_2026-09-05-—-a2a-addressing-spans-sessions-on-the-same-host-d318" aria-label="Permalink to &quot;2026-09-05 — A2A addressing spans sessions on the same host (D318)&quot;">​</a></h2><ul><li>Decision D318 amends ADR 0147. The local A2A broker stays in host-core with capability-token auth and durable tasks; same-context-only discovery and send are lifted so two A2A-capable delegates in different open sessions can coordinate.</li><li><code>a2a.agents.list</code> returns every other live agent; cards carry <code>contextId</code>. <code>a2a.message.send</code> may address any registered peer (omitted <code>to</code> still prefers a same-session peer). Task access is membership — requester or worker — not context. <code>A2A_CROSS_CONTEXT_DENIED</code> is kept on the wire list and is no longer produced.</li><li>Live peer ids are unique across the registry: a colliding <code>card.name</code> is suffix-uniquified at register and the runtime adopts the returned <code>agentId</code>. <code>a2a.task.event</code> / <code>a2a.push</code> gain <code>recipientContextId</code>; each session runtime delivers an event only when that field matches its <code>sessionId</code>.</li><li>Unchanged: the parent cannot call <code>A2A</code>, settled delegates deregister, no nested delegation, no remote agents. See ADR 0162, <code>03-runtime/02-agent-runtime.md</code> §5f.2, and E2E-165c.</li></ul><h2 id="_2026-09-05-—-live-durable-transcript-merge-keeps-chronological-order-d317" tabindex="-1">2026-09-05 — Live/durable transcript merge keeps chronological order (D317) <a class="header-anchor" href="#_2026-09-05-—-live-durable-transcript-merge-keeps-chronological-order-d317" aria-label="Permalink to &quot;2026-09-05 — Live/durable transcript merge keeps chronological order (D317)&quot;">​</a></h2><ul><li>Revalidating a running session used the bounded durable page as the array prefix and appended every live-only row after it. A long session that had accumulated more than the newest-100 page therefore painted old history at the bottom of D261&#39;s trailing mounted window, hiding the just-sent user prompt while the turn kept running. Stop plus another switch looked like a restore because the idle path skipped the merge and showed the durable page.</li><li><code>mergeLiveSessionMessages</code> now stitches in chronological order: live rows older than the page stay before it, overlapping completed rows prefer the durable copy, and an optimistic or streaming tail stays after it. A previously appended older prefix is healed back in front when its <code>createdAt</code> precedes the page.</li><li>Decision D317 amends ADR 0120 / D261 / ADR 0137. See <code>04-ux/09-interaction-patterns.md</code> § session isolation, and E2E-177.</li></ul><h2 id="_2026-09-05-—-restore-a-missing-sessions-row-from-its-transcript-outbox-d318" tabindex="-1">2026-09-05 — Restore a missing sessions row from its transcript / outbox (D318) <a class="header-anchor" href="#_2026-09-05-—-restore-a-missing-sessions-row-from-its-transcript-outbox-d318" aria-label="Permalink to &quot;2026-09-05 — Restore a missing sessions row from its transcript / outbox (D318)&quot;">​</a></h2><ul><li>A long session could vanish from the sidebar and reset the transcript to the first user row while hundreds of turns sat in <code>session-message-outbox.json</code>. The SQLite <code>sessions</code> row was gone; <code>session.appendMessage</code> failed <code>session not found</code>; the outbox paused at that head and never drained.</li><li>Host boot now restores every live JSONL whose sessions row is missing, reinserting the row and rebuilding the search index from the file. <code>session.appendMessage</code> does the same restore, or inserts a stub row under the existing id when the file is also gone, so a queued outbox can drain. <code>session.delete</code> drops that session&#39;s outbox entries so a stub cannot resurrect a user-deleted conversation.</li><li>Decision D318 amends ADR 0041 / D119. See <code>03-runtime/04-data-storage.md</code>, <code>03-runtime/07-process-model.md</code>, and E2E-178.</li></ul><h2 id="_2026-09-05-—-parent-agents-collaborate-across-conversations-d321" tabindex="-1">2026-09-05 — Parent agents collaborate across conversations (D321) <a class="header-anchor" href="#_2026-09-05-—-parent-agents-collaborate-across-conversations-d321" aria-label="Permalink to &quot;2026-09-05 — Parent agents collaborate across conversations (D321)&quot;">​</a></h2><ul><li>Decision D321 amends ADR 0147 / ADR 0162. Agent-mode session runtimes register a <code>kind: &quot;parent&quot;</code> A2A card for their lifetime and expose <code>A2A</code> as a core parent tool so two conversations on the same host can coordinate.</li><li>Discovery and send are kind-scoped: parents see other parents, subagents see other subagents. A parent cannot address a subagent, including its own, so <code>Task*</code> remains the only channel to delegates.</li><li>Inbound parent events queue; an idle session prepends them to the next user prompt rather than auto-starting a turn. A conversation is reachable after it has an Agent runtime this process.</li><li>See ADR 0164, <code>03-runtime/02-agent-runtime.md</code> §5f.2, and E2E-165d.</li></ul><h2 id="_2026-09-05-—-sent-file-references-stay-chips-and-open-on-click-d320" tabindex="-1">2026-09-05 — Sent file references stay chips and open on click (D320) <a class="header-anchor" href="#_2026-09-05-—-sent-file-references-stay-chips-and-open-on-click-d320" aria-label="Permalink to &quot;2026-09-05 — Sent file references stay chips and open on click (D320)&quot;">​</a></h2><ul><li>Composer chips serialized to full <code>@path</code> text in the user bubble, so the node the user placed in the input became a long path after send. Scratch absolute paths were not clickable.</li><li>The transcript now renders those tokens as composer-matching leaf-name chips. Workspace HTML opens in the side browser; other allowed files open with the OS default application via <code>pi-desktop/fs/open</code>.</li><li>Decision D320 amends D209 / ADR 0070. See ADR 0163, <code>04-ux/08-component-spec.md</code> §8.3 / §11.8, <code>04-ux/09-interaction-patterns.md</code> §8a.2, <code>03-runtime/01-ipc-protocol.md</code> § fs, and E2E-180.</li></ul><h2 id="_2026-09-05-—-relative-transcript-paths-preview-and-open-d322" tabindex="-1">2026-09-05 — Relative transcript paths preview and open (D322) <a class="header-anchor" href="#_2026-09-05-—-relative-transcript-paths-preview-and-open-d322" aria-label="Permalink to &quot;2026-09-05 — Relative transcript paths preview and open (D322)&quot;">​</a></h2><ul><li>Relative file paths in assistant markdown were often not clickable, and <code>./</code> / <code>../</code> links in a viewed markdown document resolved from the workspace root instead of the file&#39;s directory.</li><li>The renderer now linkifies bare workspace file tokens in markdown, and resolves <code>./</code> / <code>../</code> against the viewed file when previewing markdown in the files tab. Parent escapes stay inert. The remark plugin is a unified attacher so opening a session does not crash on <code>tree.type</code>.</li><li>Decision D322. See <code>04-ux/08-component-spec.md</code> §8.3 and E2E-182.</li></ul><h2 id="_2026-09-05-—-live-parent-turns-stay-transparent-d323" tabindex="-1">2026-09-05 — Live parent turns stay transparent (D323) <a class="header-anchor" href="#_2026-09-05-—-live-parent-turns-stay-transparent-d323" aria-label="Permalink to &quot;2026-09-05 — Live parent turns stay transparent (D323)&quot;">​</a></h2><ul><li>D297 replaced the streaming left rail with a whole-turn <code>--ds-tile</code> so the tint fading in/out would not reflow text. That boxed ordinary thinking, tools, and answer fragments the same way as a subagent card.</li><li>A streaming parent turn now stays on the page background like a completed turn: no rail, no reserved 14px inset, no whole-turn tile. The tile remains only on the delegation card (D319) and the nested <code>.subagent-run</code>.</li><li>Decision D323 amends D101 / D297. See <code>04-ux/08-component-spec.md</code> §8.4 / §9.9 and E2E-059a / US-UI-60.</li></ul><h2 id="_2026-09-05-—-idle-session-switch-keeps-a-not-yet-flushed-completed-tail-d324" tabindex="-1">2026-09-05 — Idle session switch keeps a not-yet-flushed completed tail (D324) <a class="header-anchor" href="#_2026-09-05-—-idle-session-switch-keeps-a-not-yet-flushed-completed-tail-d324" aria-label="Permalink to &quot;2026-09-05 — Idle session switch keeps a not-yet-flushed completed tail (D324)&quot;">​</a></h2><ul><li>After a turn finished, switching away and back reloaded the durable <code>session.get</code> page and dropped assistant/tool rows that were still only in the renderer (the persistence outbox had not flushed them yet). User prompts survived because they are written before the model runs.</li><li><code>selectSession</code> now merges live rows for an idle session that still has live provenance, the same way a running session already did. The live flag is cleared only once the durable page contains every live id.</li><li>Decision D324 amends D317 / ADR 0137. See <code>04-ux/09-interaction-patterns.md</code> § session isolation, and E2E-183.</li></ul><h2 id="_2026-09-05-—-parent-a2a-is-always-in-the-agent-catalog-d325" tabindex="-1">2026-09-05 — Parent A2A is always in the Agent catalog (D325) <a class="header-anchor" href="#_2026-09-05-—-parent-a2a-is-always-in-the-agent-catalog-d325" aria-label="Permalink to &quot;2026-09-05 — Parent A2A is always in the Agent catalog (D325)&quot;">​</a></h2><ul><li>D321 registered the parent only after a successful broker mint, then injected <code>A2A</code> into the catalog. A late or failed register left the model with no <code>A2A</code> tool. <code>ToolSearch</code> also never lists core tools, so the model treated a missing on-demand hit as &quot;A2A does not exist&quot;.</li><li>Agent mode now puts <code>A2A</code> in the core catalog at runtime construction. The first call (or first prompt) mints the token. The tool is never deferred. Cross-conversation guidance is in the Agent system prompt whenever the mode is Agent, not only after register.</li><li>Decision D325 amends ADR 0164 / D321. See <code>03-runtime/02-agent-runtime.md</code> §5f.2 and E2E-165d.</li></ul><h2 id="_2026-09-05-—-withdraw-a2a-and-peer-coordination-d326" tabindex="-1">2026-09-05 — Withdraw A2A and Peer coordination (D326) <a class="header-anchor" href="#_2026-09-05-—-withdraw-a2a-and-peer-coordination-d326" aria-label="Permalink to &quot;2026-09-05 — Withdraw A2A and Peer coordination (D326)&quot;">​</a></h2><ul><li>The in-process <code>Peer</code> mailbox (D277 / ADR 0138 / ADR 0140) and the host-core A2A stack (ADR 0147 / ADR 0162 / ADR 0164 / D318 / D321 / D325) are withdrawn. There is no sibling channel and no parent-to-parent channel.</li><li><code>A2A</code> and <code>Peer</code> are not core tools, not assignable subagent tools, and not <code>ToolSearch</code> results. A definition that names either is treated as an unknown tool and dropped with a parse warning.</li><li>Handshake protocol version moves 10→11 and no longer advertises <code>&quot;a2a&quot;</code>. Storage schema moves 12→13 and drops the A2A tables. A mixed v10/v11 pair is rejected at handshake.</li><li>Decision D326 is recorded as ADR 0165, which supersedes ADR 0147, ADR 0162, and ADR 0164. See <code>03-runtime/02-agent-runtime.md</code> §5f.2, <code>03-runtime/03-tools-and-permissions.md</code> §10.2, <code>03-runtime/06-host-rpc-protocol.md</code> §3–§4, and E2E-165.</li></ul><h2 id="_2026-09-06-—-completed-replies-survive-process-restart-d327" tabindex="-1">2026-09-06 — Completed replies survive process restart (D327) <a class="header-anchor" href="#_2026-09-06-—-completed-replies-survive-process-restart-d327" aria-label="Permalink to &quot;2026-09-06 — Completed replies survive process restart (D327)&quot;">​</a></h2><ul><li>Closing the app after a finished turn reloaded only user prompts (issue #42). User rows are durable before the model runs; assistant/tool rows go through the async outbox after <code>message_end</code>. D324 kept those live rows across an in-process session switch; process teardown drops renderer memory.</li><li><code>message_end</code> now checkpoints the finished snapshot before the outbox append, and <code>settleIf</code> will not wipe a newer turn that started during that write. <code>completed</code>/<code>error</code> <code>session.endTurn</code> deletes <code>.inflight.json</code> only when that id is already indexed. Boot recovery promotes a leftover whose turn is <code>completed</code> as a <code>complete</code> assistant row, not <code>aborted</code>.</li><li>Host handshake awaits the outbox drain before the renderer can <code>session.get</code>, then <code>session.recoverInflightMessages</code> promotes leftover completed checkpoints the outbox did not land. Boot recovery skips completed leftovers so the finished outbox row can win.</li><li>Decision D327 amends D299 / ADR 0153 / ADR 0041. See <code>03-runtime/04-data-storage.md</code> §2.1 and §5, <code>03-runtime/07-process-model.md</code> §5, <code>03-runtime/10-session-state-machine.md</code> §4, E2E-171, and E2E-184.</li></ul><h2 id="_2026-09-06-—-parent-judged-subagent-lifetime-d328" tabindex="-1">2026-09-06 — Parent-judged subagent lifetime (D328) <a class="header-anchor" href="#_2026-09-06-—-parent-judged-subagent-lifetime-d328" aria-label="Permalink to &quot;2026-09-06 — Parent-judged subagent lifetime (D328)&quot;">​</a></h2><ul><li>The parent agent was aborting unfinished delegates because <code>TaskWait</code> expired, the model closed, and <code>agent_end</code> killed leftover runs. Idle and duration watchdogs were a second, independent kill.</li><li>Decision D328 amends ADR 0089 / ADR 0119 / ADR 0129. The runtime no longer arms idle or duration timers. Parent idle does not abort delegates. When they finish, the runtime prompts the parent with their reports in the same durable turn. <code>TaskList</code> / timed-out <code>TaskWait</code> carry a heartbeat. Only <code>TaskStop</code> and user Stop abort a delegate.</li><li>See ADR 0166, <code>03-runtime/02-agent-runtime.md</code> §5f, and E2E-155.</li></ul><h2 id="_2026-09-06-—-agent-chosen-bash-timeout-d329" tabindex="-1">2026-09-06 — Agent-chosen Bash timeout (D329) <a class="header-anchor" href="#_2026-09-06-—-agent-chosen-bash-timeout-d329" aria-label="Permalink to &quot;2026-09-06 — Agent-chosen Bash timeout (D329)&quot;">​</a></h2><ul><li>The 300-second Bash override ceiling killed long builds and rejected explicit <code>timeout: 600</code> / <code>1800</code>. Default 60 seconds is unchanged.</li><li>Decision D329 amends D190 / D273 / ADR 0054: honour 1–21,600 seconds; values above that ceiling are milliseconds, converted and clamped. Timeout and abort still kill the process tree.</li><li>See ADR 0167, <code>03-runtime/03-tools-and-permissions.md</code> §5, and E2E-115.</li></ul><h2 id="_2026-09-06-—-main-owned-openexternal-allowlist-d330" tabindex="-1">2026-09-06 — Main-owned openExternal allowlist (D330) <a class="header-anchor" href="#_2026-09-06-—-main-owned-openexternal-allowlist-d330" aria-label="Permalink to &quot;2026-09-06 — Main-owned openExternal allowlist (D330)&quot;">​</a></h2><ul><li>Chat markdown, plugin panels, the launcher, OAuth, and the embedded preview could hand a raw URL to <code>shell.openExternal</code>. A clicked <code>file:</code>, <code>javascript:</code>, <code>ms-msdt:</code>, or custom scheme reached the OS protocol handler.</li><li>Electron Main now parses every such URL. Only http(s) with a hostname and <code>mailto:</code> with an address open. Disallowed URLs throw so OAuth reports <code>opened: false</code> and plugins keep <code>INVALID_ARGUMENT</code>. Workspace files stay on <code>shell.openPath</code>.</li><li>Decision D330 amends ADR 0109. See ADR 0168, <code>05-security/01-security.md</code>, <code>07-plugins/04-plugin-security.md</code> §8, and E2E-185.</li></ul><h2 id="_2026-09-06-—-accepted-files-stay-as-inline-chips-d362" tabindex="-1">2026-09-06 — Accepted <code>@</code> files stay as inline chips (D362) <a class="header-anchor" href="#_2026-09-06-—-accepted-files-stay-as-inline-chips-d362" aria-label="Permalink to &quot;2026-09-06 — Accepted \`@\` files stay as inline chips (D362)&quot;">​</a></h2><ul><li>Completing a workspace file from the <code>@</code> menu still replaced the trigger with an empty string and stored a token-less reference. That was the chips-above-textarea contract; after chips moved into the contenteditable draft, only sentinel characters paint, so Enter made the file disappear.</li><li>Accepting a file now inserts one private-use sentinel at the caret and stores that token on the reference, matching clipboard paste. Enter/Tab confirm the menu and do not send. Directories still insert <code>@dir/</code> as text. Workspace-relative chips still drop on project switch (absolute scratch paths stay); their sentinels are stripped from the draft with them.</li><li>Decision D362 amends D209 / D125. See <code>04-ux/08-component-spec.md</code> §11.8, <code>04-ux/09-interaction-patterns.md</code> §8a.1, and E2E-102a.</li></ul><h2 id="_2026-09-06-—-bundled-files-preview-and-live-workspace-events-d332" tabindex="-1">2026-09-06 — Bundled Files preview and live workspace events (D332) <a class="header-anchor" href="#_2026-09-06-—-bundled-files-preview-and-live-workspace-events-d332" aria-label="Permalink to &quot;2026-09-06 — Bundled Files preview and live workspace events (D332)&quot;">​</a></h2><ul><li>The Files plugin could not preview images, did not expose Open with default app after reveal replaced the header action, polled for project switches, and missed theme/locale events while docked.</li><li><code>fs.readPreview</code> classifies one readable file as text, image, binary, or tooLarge under the existing <code>fs.read</code> gate. Panel events now reach docked views. <code>workspace:changed</code> is delivered to panels and plugin processes.</li><li>Decision D332 is recorded as ADR 0169. See <code>07-plugins/03-plugin-api.md</code> §3/§5/§6, <code>04-ux/08-component-spec.md</code> §5.2.2, and E2E-153.</li></ul><h2 id="_2026-09-06-—-work-panel-browser-is-the-bundled-pi-browser-plugin-d333" tabindex="-1">2026-09-06 — Work-panel browser is the bundled <code>pi.browser</code> plugin (D333) <a class="header-anchor" href="#_2026-09-06-—-work-panel-browser-is-the-bundled-pi-browser-plugin-d333" aria-label="Permalink to &quot;2026-09-06 — Work-panel browser is the bundled \`pi.browser\` plugin (D333)&quot;">​</a></h2><ul><li>The work-panel Browser launcher, chrome, and agent CDP were a host-built <code>HEADER_TOOLS</code> row plus a Main-owned guest. Files already proved a first-party surface can ship as a bundled plugin; users need the same enable/disable product for the whole browser without putting arbitrary web content inside a sandboxed plugin view.</li><li>Bundled <code>pi.browser</code> owns chrome and the agent <code>Browser</code> tool. The guest <code>WebContentsView</code> and debugger stay host-owned and are reached only through public <code>pi.browser.*</code> gated by <code>browser.cdp</code>. Guest bounds are clamped to the calling plugin view. Raw CDP is allowlisted (cookies/storage/Target/Fetch denied). Host <code>BrowserPreview</code> stays a Plan-safe facade and fails if the plugin is disabled. No protocol/schema bump.</li><li>Decision D333 amends ADR 0019 / 0104 / 0105 / 0108. See ADR 0170, <code>07-plugins/03-plugin-api.md</code>, and E2E-008b.</li></ul><h2 id="_2026-09-07-—-contained-in-chat-image-display-d334" tabindex="-1">2026-09-07 — Contained in-chat image display (D334) <a class="header-anchor" href="#_2026-09-07-—-contained-in-chat-image-display-d334" aria-label="Permalink to &quot;2026-09-07 — Contained in-chat image display (D334)&quot;">​</a></h2><ul><li>Pasted/uploaded images are stored as extension-less <code>attachments/&lt;sha256&gt;</code> blobs. The renderer origin cannot load them, so history turns showed a chip instead of the picture, and <code>fs/open</code> treated the ref as workspace-relative.</li><li><code>fs/read</code>, <code>fs/reveal</code>, and <code>fs/open</code> now share <code>resolveOpenablePath</code> (workspace, scratch, attachments, plus the blob ref). Reads <code>realpath</code> the target. <code>fs/readImageDataUrl</code> returns only a bounded image data URL. Client <code>mimeType</code> cannot reclassify a non-image extension.</li><li>Decision D334 is recorded as ADR 0172. See <code>03-runtime/01-ipc-protocol.md</code>, <code>04-ux/08-component-spec.md</code> §8.3, and E2E-187.</li></ul><h2 id="_2026-09-07-—-host-owned-plugin-completions-and-session-context-d336" tabindex="-1">2026-09-07 — Host-owned plugin completions and session context (D336) <a class="header-anchor" href="#_2026-09-07-—-host-owned-plugin-completions-and-session-context-d336" aria-label="Permalink to &quot;2026-09-07 — Host-owned plugin completions and session context (D336)&quot;">​</a></h2><ul><li>Plugins could register tools but could not list authenticated models, read the current LLM context, or spend the user&#39;s provider quota on a side call.</li><li><code>pi.models.list</code>, <code>pi.session.getLlmContext</code>, and <code>pi.agent.complete</code> are public host APIs. Credentials and the wire call stay in Electron main. Session context is bound to the in-flight tool session (D019 amended).</li><li>The bundled first-party Advisor UX is temporarily not shipped. The public APIs remain available to explicitly installed plugins.</li><li>Decision D336 is recorded as ADR 0174. See <code>07-plugins/03-plugin-api.md</code>, <code>07-plugins/13-plugin-permissions-matrix.md</code>, and E2E-188 / E2E-189.</li></ul><h2 id="_2026-09-07-—-opencode-go-session-routing-headers-d337" tabindex="-1">2026-09-07 — OpenCode Go session routing headers (D337) <a class="header-anchor" href="#_2026-09-07-—-opencode-go-session-routing-headers-d337" aria-label="Permalink to &quot;2026-09-07 — OpenCode Go session routing headers (D337)&quot;">​</a></h2><ul><li>OpenCode Go rejects LLM requests that omit <code>x-opencode-session</code> (<code>MissingSessionID</code>). pi-ai 0.85 does not emit that header; the official Pi coding-agent injects it in the agent layer.</li><li>Decision D337 amends ADR 0116: agent-runtime sends <code>x-opencode-session</code>, <code>x-opencode-client: pi-desktop</code>, and a PI-Desktop <code>User-Agent</code> on session, subagent, prompt-enhancement, and plugin one-shot requests to OpenCode Go and any <code>opencode.ai</code> host, using the durable conversation id.</li><li>See <code>03-runtime/02-agent-runtime.md</code> §6.2, <code>03-runtime/11-provider-model-system.md</code>, <code>03-runtime/12-provider-config-schema.md</code>, ADR 0116, and E2E-005D.</li></ul><h2 id="_2026-09-07-—-explain-quiet-active-turns-with-live-agent-activity-status-d338" tabindex="-1">2026-09-07 — Explain quiet active turns with live agent activity status (D338) <a class="header-anchor" href="#_2026-09-07-—-explain-quiet-active-turns-with-live-agent-activity-status-d338" aria-label="Permalink to &quot;2026-09-07 — Explain quiet active turns with live agent activity status (D338)&quot;">​</a></h2><ul><li>The transcript showed a generic running timer before the first event, but a provider wait, retry backoff, or parent-side delegated-work wait could still leave no row that explained the delay. Users could only infer activity from the Stop button.</li><li>The existing normalized <code>status</code> event now carries an optional runtime-owned <code>AgentActivity</code> phase: <code>starting</code>, <code>waiting-model</code>, <code>retrying</code>, or <code>waiting-subagents</code>. The renderer stores it per session and renders one compact localized status row with elapsed time; it does not add a percentage, duplicate progress card, or alter abort semantics.</li><li>Decision D338 is recorded as ADR 0175. See <code>03-runtime/01-ipc-protocol.md</code>, <code>03-runtime/02-agent-runtime.md</code>, <code>04-ux/09-interaction-patterns.md</code>, and E2E-008c / E2E-094.</li></ul><h2 id="_2026-09-07-—-per-provider-user-agent-override-d339" tabindex="-1">2026-09-07 — Per-provider User-Agent override (D339) <a class="header-anchor" href="#_2026-09-07-—-per-provider-user-agent-override-d339" aria-label="Permalink to &quot;2026-09-07 — Per-provider User-Agent override (D339)&quot;">​</a></h2><ul><li>Gateways and vendor subscriptions inspect User-Agent. pi-ai, Anthropic OAuth, OpenCode, and Codex each stamp a different default, and Codex overwrites User-Agent after extra headers. The schema listed <code>headers</code> but never stored or sent them.</li><li>Decision D339: each AI-service and OAuth row may store optional <code>config_json.userAgent</code>. Empty keeps adapter defaults. A non-empty value is last-writer <code>User-Agent</code> on that row&#39;s outbound HTTP via a fetch wrapper plus stream-option headers. Advanced UI only; first OAuth login does not collect it. <code>matches()</code> includes <code>userAgent</code>.</li><li>See ADR 0176, <code>03-runtime/12-provider-config-schema.md</code>, and E2E-005G.</li></ul><h2 id="_2026-09-08-—-per-provider-custom-http-headers-d341" tabindex="-1">2026-09-08 — Per-provider custom HTTP headers (D341) <a class="header-anchor" href="#_2026-09-08-—-per-provider-custom-http-headers-d341" aria-label="Permalink to &quot;2026-09-08 — Per-provider custom HTTP headers (D341)&quot;">​</a></h2><ul><li>Gateways need more than User-Agent. ADR 0176 left the schema <code>headers</code> map unimplemented and used a sparse single-field Advanced editor.</li><li>Decision D341: each AI-service and OAuth row may store optional <code>config_json.headers</code>. Empty keeps adapter defaults. A non-empty map is last-writer on that row&#39;s outbound HTTP via a fetch wrapper plus stream-option headers. Leftover <code>userAgent</code> migrates into <code>headers[&quot;User-Agent&quot;]</code>. Reserved auth and hop-by-hop keys are rejected. Advanced UI is a compact key/value editor. First OAuth login does not collect headers. <code>matches()</code> includes the map.</li><li>See ADR 0178, <code>03-runtime/12-provider-config-schema.md</code>, and E2E-005G.</li></ul><h2 id="_2026-09-08-—-user-configurable-outbound-proxy-d340" tabindex="-1">2026-09-08 — User-configurable outbound proxy (D340) <a class="header-anchor" href="#_2026-09-08-—-user-configurable-outbound-proxy-d340" aria-label="Permalink to &quot;2026-09-08 — User-configurable outbound proxy (D340)&quot;">​</a></h2><ul><li>Model calls, marketplace downloads, updates, and the in-app browser each used a different HTTP stack, and none of them honored a product setting. Node <code>fetch</code> in the sidecar ignored the OS proxy.</li><li>Settings → General → Network adds System / Direct / Custom. Custom is one HTTP or SOCKS5 URL plus a loopback bypass. Chromium sessions, main <code>net.fetch</code>, sidecar undici, and host-core marketplace curl share it. Workspace Bash does not inherit proxy credentials.</li><li>Decision D340 is recorded as ADR 0177. See <code>04-ux/06-settings-ia.md</code>, <code>03-runtime/07-process-model.md</code>, and E2E-190.</li></ul><h2 id="_2026-09-08-—-main-owned-picker-capabilities-d344" tabindex="-1">2026-09-08 — Main-owned picker capabilities (D344) <a class="header-anchor" href="#_2026-09-08-—-main-owned-picker-capabilities-d344" aria-label="Permalink to &quot;2026-09-08 — Main-owned picker capabilities (D344)&quot;">​</a></h2><ul><li>The composer picker previously returned native absolute paths to the renderer, which could then replay arbitrary paths through <code>composer/importFiles</code>; it also advertised folders while the importer rejected directories.</li><li>Picker IPC now stores user-selected paths in Electron main behind a short-lived, sender-bound, one-shot token. Import accepts only that token and a durable session id, while the MVP picker offers regular files only.</li><li>Decision D344 is recorded as ADR 0181. See <code>03-runtime/01-ipc-protocol.md</code>, <code>04-ux/08-component-spec.md</code> §11.7–11.8, and E2E-102h.</li></ul><h2 id="_2026-09-08-—-custom-global-ui-type-scale-d343" tabindex="-1">2026-09-08 — Custom global UI type scale (D343) <a class="header-anchor" href="#_2026-09-08-—-custom-global-ui-type-scale-d343" aria-label="Permalink to &quot;2026-09-08 — Custom global UI type scale (D343)&quot;">​</a></h2><ul><li>The <code>--text-*</code> ramp is a frozen set of relative sizes. Window zoom scales layout together with text and is not a type preference. A reading-only px field would leave two type sizes in one window.</li><li>Settings → General → Appearance adds Font size: Starbucks-style cup presets Tall / Grande / Venti / Trenta plus a percentage slider (80%–150%), persisted as optional <code>AppSettings.fontScale</code> (<code>1</code> = product ramp). The renderer sets <code>--font-scale</code> so every <code>--text-*</code> step and shared Lucide icon scales in proportion.</li><li>Decision D343 is recorded as ADR 0180. See <code>04-ux/06-settings-ia.md</code>, <code>04-ux/07-ui-design-system.md</code>, and E2E-193.</li></ul><h2 id="_2026-09-08-—-p0-international-shell-locales-d346" tabindex="-1">2026-09-08 — P0 international shell locales (D346) <a class="header-anchor" href="#_2026-09-08-—-p0-international-shell-locales-d346" aria-label="Permalink to &quot;2026-09-08 — P0 international shell locales (D346)&quot;">​</a></h2><ul><li>Ship complete German, Spanish, and French shell catalogs, searchable native names, regional base-locale resolution, and matching in-app changelog catalogs. Extend <code>AppSettings.language</code> with <code>de</code>, <code>es</code>, and <code>fr</code> without a host protocol, storage schema, or IPC version change.</li><li>Decision D346 is recorded as ADR 0183. See <code>04-ux/02-i18n-english-first.md</code>, <code>04-ux/06-settings-ia.md</code>, and E2E-091.</li></ul><h2 id="_2026-09-08-—-composer-docked-context-usage-inspector-d347" tabindex="-1">2026-09-08 — Composer-docked context usage inspector (D347) <a class="header-anchor" href="#_2026-09-08-—-composer-docked-context-usage-inspector-d347" aria-label="Permalink to &quot;2026-09-08 — Composer-docked context usage inspector (D347)&quot;">​</a></h2><ul><li>The compact context inspector hung under the newest assistant turn and scrolled out of reach. Collaborators agreed to move the single entry next to the model picker rather than keep two copies of the same numbers.</li><li>Decision D347: the inspector lives in the composer right toolbar, left of the model × reasoning chip, and always mirrors the newest assistant turn that reported usage. The trigger is the ring plus percentage. The popover heading is remaining tokens plus percentage; inner section rules stay forbidden (D297). Assistant meta keeps the model badge.</li><li>See ADR 0184, <code>04-ux/08-component-spec.md</code> §8.3 / §11.3, and E2E-060d.</li></ul><h2 id="_2026-09-08-—-macos-sidebar-uses-the-source-list-vibrancy-material-d348" tabindex="-1">2026-09-08 — macOS sidebar uses the source-list vibrancy material (D348) <a class="header-anchor" href="#_2026-09-08-—-macos-sidebar-uses-the-source-list-vibrancy-material-d348" aria-label="Permalink to &quot;2026-09-08 — macOS sidebar uses the source-list vibrancy material (D348)&quot;">​</a></h2><ul><li>The washed-gray dock reproduced with a <strong>dark app theme and a dark OS</strong> on macOS 26. Syncing <code>nativeTheme.themeSource</code> alone would fix dark-app + light-OS; it does not keep <code>under-window</code> charcoal when Liquid Glass still paints a light plate under dark appearance. That is why the material changes from D304&#39;s <code>under-window</code> to <code>sidebar</code>.</li><li>Decision D348: the main window uses Electron <code>vibrancy: &quot;sidebar&quot;</code>. <code>nativeTheme.themeSource</code> follows the app theme preference (<code>system</code> / <code>light</code> / <code>dark</code> / plugin base) so native menus and the vibrancy plate match the renderer. The assignment is process-wide, so Windows/Linux <code>shouldUseDarkColors</code> at window creation also follows the app preference next to <code>windowSetBackgroundColor</code>. Re-apply when a <code>plugin:</code> theme disappears. Only re-set vibrancy when <code>themeSource</code> actually changes. The thin <code>--ds-sidebar-glass-tint</code> recipe is unchanged. Amends D304.</li><li><code>macos-sidebar-vibrancy.test.mjs</code> asserts the sidebar material, preference mapping, settings/pluginChanged apply path, and the darwin live-window vibrancy guard. See <code>04-ux/08-component-spec.md</code> §1.7 and US-UI-74 / E2E-076.</li></ul><h2 id="_2026-09-08-—-session-title-summaries-and-focus-aware-native-task-notifications-d359-d350" tabindex="-1">2026-09-08 — Session title summaries and focus-aware native task notifications (D359/D350) <a class="header-anchor" href="#_2026-09-08-—-session-title-summaries-and-focus-aware-native-task-notifications-d359-d350" aria-label="Permalink to &quot;2026-09-08 — Session title summaries and focus-aware native task notifications (D359/D350)&quot;">​</a></h2><ul><li>The initial prompt remains visible immediately as a normalized 48-character fallback. After the first turn, Electron resolves the session&#39;s effective provider/model and runs the main-owned <code>session/summarizeTitle</code> one-shot with thinking disabled; the renderer persists a successful result through <code>session.rename</code>.</li><li>Renderer-local session metadata persists <code>manualTitle</code>. Automatic title generation skips that marker and any persisted title that is neither a known default nor the first-prompt fallback, so manual and already-summarized titles survive renderer restart without a host schema change.</li><li>Native task completion and interactive prompt alerts share an Electron IPC entry but have explicit <code>kind</code> values. Task banners are suppressed whenever the main window is visible and focused, including focused background sessions; interactive prompts retain exact-visible-session suppression so a focused background request still alerts. Durable task insertion remains D117&#39;s visibility decision and plugin notifications remain separate.</li><li>See ADR 0186 / ADR 0187, <code>03-runtime/01-ipc-protocol.md</code>, <code>03-runtime/02-agent-runtime.md</code>, <code>04-ux/08-component-spec.md</code>, and E2E-021a / E2E-065.</li></ul><h2 id="_2026-09-08-—-preserve-distinct-credentials-during-model-configuration-import-d351" tabindex="-1">2026-09-08 — Preserve distinct credentials during model configuration import (D351) <a class="header-anchor" href="#_2026-09-08-—-preserve-distinct-credentials-during-model-configuration-import-d351" aria-label="Permalink to &quot;2026-09-08 — Preserve distinct credentials during model configuration import (D351)&quot;">​</a></h2><ul><li>CC Switch can store multiple named profiles at one gateway endpoint. Endpoint-only import matching created the first row and silently skipped profiles with different API keys.</li><li>Import equivalence now includes normalized endpoint, API style, and the credential. Same-endpoint profiles with different keys create independent providers and remain selectable in Composer; unchanged credentials remain idempotently skipped.</li><li>Electron main resolves existing API keys through the host secret boundary for matching. Raw values never reach the renderer, logs, or provider metadata.</li><li>Decision D351 amends D342 and ADR 0179. It is recorded as ADR 0188 and is covered by E2E-209. No host protocol or storage schema change.</li></ul><h2 id="_2026-09-08-—-parent-fatal-error-aborts-leftover-delegates-d352" tabindex="-1">2026-09-08 — Parent fatal error aborts leftover delegates (D352) <a class="header-anchor" href="#_2026-09-08-—-parent-fatal-error-aborts-leftover-delegates-d352" aria-label="Permalink to &quot;2026-09-08 — Parent fatal error aborts leftover delegates (D352)&quot;">​</a></h2><ul><li>D328 kept leftover delegates running after parent idle so long jobs could finish. A terminal parent 429 still closed the durable turn and showed Continue, while a subagent on another model kept <code>isRunning</code> true.</li><li>Decision D352 amends D328 / ADR 0166. Parent idle is unchanged. A terminal parent error aborts leftover delegates, skips the resume prompt, and leaves the session idle so Continue is accepted. The failed TurnOutcomeCard survives a later <code>agent_end</code>.</li><li>See ADR 0189, <code>03-runtime/02-agent-runtime.md</code> §5f, and E2E-155.</li></ul><h2 id="_2026-09-09-—-disambiguate-intel-macos-release-artifacts-d353" tabindex="-1">2026-09-09 — Disambiguate Intel macOS release artifacts (D353) <a class="header-anchor" href="#_2026-09-09-—-disambiguate-intel-macos-release-artifacts-d353" aria-label="Permalink to &quot;2026-09-09 — Disambiguate Intel macOS release artifacts (D353)&quot;">​</a></h2><ul><li>The native macOS lanes could produce generic version-only installer names, making the Intel download easy to confuse with the arm64 download.</li><li>Decision D353 amends D285 / ADR 0145: the Intel x64 lane passes target-specific electron-builder patterns and publishes <code>PI-Desktop-&lt;version&gt;-Intel.dmg</code> and <code>PI-Desktop-&lt;version&gt;-Intel-mac.zip</code>. The x64 updater feed keeps those URLs and checksums; arm64 keeps the generic macOS names.</li><li>This is a release-asset naming change only. Updater ownership, signing, and notify-and-link delivery remain unchanged. See E2E-092.</li></ul><h2 id="_2026-09-09-—-label-both-macos-release-architectures-d354" tabindex="-1">2026-09-09 — Label both macOS release architectures (D354) <a class="header-anchor" href="#_2026-09-09-—-label-both-macos-release-architectures-d354" aria-label="Permalink to &quot;2026-09-09 — Label both macOS release architectures (D354)&quot;">​</a></h2><ul><li>D353 left the arm64 macOS asset with a generic version-only name while using <code>-Intel</code> only for the x64 asset, so <code>PI-Desktop-0.14.4.dmg</code> still did not identify its architecture.</li><li>Decision D354 amends D353 / ADR 0145: both native lanes pass target-specific naming templates and publish <code>-arm64</code> or <code>-x64</code> DMG/ZIP suffixes. The per-architecture updater feeds keep the final URLs and checksums.</li><li>This changes release asset naming only. Updater ownership, signing, and notify-and-link delivery remain unchanged. See E2E-092.</li></ul><h2 id="_2026-09-09-—-last-request-occupancy-in-the-context-inspector-d355" tabindex="-1">2026-09-09 — Last-request occupancy in the context inspector (D355) <a class="header-anchor" href="#_2026-09-09-—-last-request-occupancy-in-the-context-inspector-d355" aria-label="Permalink to &quot;2026-09-09 — Last-request occupancy in the context inspector (D355)&quot;">​</a></h2><ul><li>The composer inspector mixed last-message occupancy with a visual-turn sum, so cache read and turn total could exceed the context window after a tool loop.</li><li>Decision D355 amends D103 / D184 / D244 / D347 / ADR 0047 / ADR 0103 / ADR 0184: occupancy, remaining capacity, used/window counts, turn total, and provider cache/input/output/reasoning/hit-rate are the last usage-bearing assistant message. Occupancy is <code>input + output + reasoning + cacheRead + cacheWrite</code>. Speed and the aggregate tool row still describe the visual turn.</li><li>Renderer only. Host completed-turn rollups and Token Insights stay additive billing. See ADR 0193 and E2E-060d.</li></ul><h2 id="_2026-09-09-—-optional-subagent-thinking-override-and-readable-selected-levels-d356" tabindex="-1">2026-09-09 — Optional subagent thinking override and readable selected levels (D356) <a class="header-anchor" href="#_2026-09-09-—-optional-subagent-thinking-override-and-readable-selected-levels-d356" aria-label="Permalink to &quot;2026-09-09 — Optional subagent thinking override and readable selected levels (D356)&quot;">​</a></h2><ul><li>The subagent editor now offers inherit-session, do-not-send, and the seven canonical thinking levels. Do-not-send is stored as <code>thinkingLevel: omit</code>.</li><li><code>omit</code> keeps the agent&#39;s bookkeeping state at <code>off</code> but uses the low-level provider stream so no thinking override is synthesized. Existing inheritance and explicit <code>off</code> remain distinct.</li><li>Model-configuration thinking chips use an accent/inverted-text selected state in both themes so enabled levels are obvious. No storage schema or protocol version change is required. See ADR 0194 and E2E-203.</li></ul><h2 id="_2026-09-09-—-viewport-fixed-work-panel-toggle-d357" tabindex="-1">2026-09-09 — Viewport-fixed work panel toggle (D357) <a class="header-anchor" href="#_2026-09-09-—-viewport-fixed-work-panel-toggle-d357" aria-label="Permalink to &quot;2026-09-09 — Viewport-fixed work panel toggle (D357)&quot;">​</a></h2><ul><li>Decision D357 amends D128 / D207 / D221 and ADR 0068 / ADR 0085. AppShell owns one viewport-fixed toggle at the top-right of every non-Settings route. It is the pointer equivalent of <code>Cmd/Ctrl + J</code>: it reveals the active session&#39;s retained context without creating a tab and collapses a visible panel without deleting tabs. With no session it is disabled.</li><li>The toggle is the sole panel-level collapse control. The work-panel header keeps resource close only. Windows/Linux window controls stay viewport-fixed at the window&#39;s right edge; while the panel is open the header reserves that band plus the toggle so close-tab remains reachable.</li><li>No host protocol, IPC channel, or native application-menu command. Artifact triggers and session-scoped contexts are unchanged. See ADR 0195 and E2E-056 / E2E-070 / US-UI-57.</li></ul><h2 id="_2026-09-09-—-windows-portable-exe-without-installer-d364" tabindex="-1">2026-09-09 — Windows portable exe without installer (D364) <a class="header-anchor" href="#_2026-09-09-—-windows-portable-exe-without-installer-d364" aria-label="Permalink to &quot;2026-09-09 — Windows portable exe without installer (D364)&quot;">​</a></h2><ul><li>Windows tag releases published only <code>PI-Desktop-Setup-&lt;version&gt;.exe</code>, which company environments that whitelist a single executable or block installers cannot run.</li><li>Decision D364 amends D120 / D126 / ADR 0022: the Windows x64 lane publishes <code>PI-Desktop-Portable-&lt;version&gt;.exe</code> alongside the NSIS installer. The portable target does not write <code>latest.yml</code>. Packaged portable runs (<code>PORTABLE_EXECUTABLE_FILE</code>) use notify-and-link delivery; NSIS keeps in-app download and quit-and-install. Data stays in the existing application data directory. Portable requests user execution level.</li><li>See ADR 0197 and E2E-211.</li></ul><h2 id="_2026-09-22-—-windows-portable-delivery-uses-a-normal-zip-d603" tabindex="-1">2026-09-22 — Windows portable delivery uses a normal ZIP (D603) <a class="header-anchor" href="#_2026-09-22-—-windows-portable-delivery-uses-a-normal-zip-d603" aria-label="Permalink to &quot;2026-09-22 — Windows portable delivery uses a normal ZIP (D603)&quot;">​</a></h2><ul><li>Decision D603 amends D364 / ADR 0022 / ADR 0197. The Windows x64 lane now publishes <code>PI-Desktop-Portable-&lt;version&gt;.zip</code> beside the NSIS installer. The release helper builds the two targets separately and stamps the ZIP app metadata with <code>piDistribution = &quot;zip&quot;</code>; the ZIP target does not write <code>latest.yml</code>.</li><li>Users extract the ZIP and launch <code>PI-Desktop.exe</code> directly. The ZIP run uses notify-and-link update delivery, while the updater keeps recognizing <code>PORTABLE_EXECUTABLE_FILE</code> for older portable executables. The existing data directory and NSIS in-app update lane are unchanged.</li><li>See ADR 0197 and E2E-211.</li></ul><h2 id="_2026-09-09-—-name-every-quiet-interval-on-the-live-activity-row-d365" tabindex="-1">2026-09-09 — Name every quiet interval on the live activity row (D365) <a class="header-anchor" href="#_2026-09-09-—-name-every-quiet-interval-on-the-live-activity-row-d365" aria-label="Permalink to &quot;2026-09-09 — Name every quiet interval on the live activity row (D365)&quot;">​</a></h2><ul><li>ADR 0175 named only <code>waiting-model</code>, <code>retrying</code>, and <code>waiting-subagents</code>. Compaction, silent-turn recovery, the post-tool gap before the next request, and <code>starting</code> still looked like a stuck generic wait. A parent wait on delegates also omitted what those delegates were doing.</li><li>Decision D365 amends D338 / ADR 0175: <code>AgentActivity</code> adds <code>preparing</code>, <code>compacting</code>, and <code>recovering</code>; <code>starting</code> is shown as its own label; and <code>waiting-subagents</code> carries a live running snapshot (<code>name</code>, <code>lastPhase</code>, <code>lastToolName</code>) that updates on child tool/thinking changes, not on every token. The row stays one compact inline status and does not restore an activity-group capsule.</li><li>See ADR 0198, <code>03-runtime/01-ipc-protocol.md</code>, <code>03-runtime/02-agent-runtime.md</code>, <code>04-ux/09-interaction-patterns.md</code>, and E2E-008c / E2E-094.</li></ul><blockquote><p>D366 is unassigned. It was first used for the plugin session import decision, which was renumbered to D367 / ADR 0200 while syncing with <code>main</code>, and then for a docs-deploy-only-from-releases decision (with ADR 0199) that was reverted on 2026-09-09. Both D366 and ADR 0199 stay retired; do not reuse them. D376 is likewise unassigned: <code>main</code> renumbered the remote-control amendment it briefly carried to D374 and assigned D375 to the rollout sequence, so D376 stays retired too.</p></blockquote><h2 id="_2026-09-09-—-host-owned-plugin-session-import-and-ownership-api-d367" tabindex="-1">2026-09-09 — Host-owned plugin session import and ownership API (D367) <a class="header-anchor" href="#_2026-09-09-—-host-owned-plugin-session-import-and-ownership-api-d367" aria-label="Permalink to &quot;2026-09-09 — Host-owned plugin session import and ownership API (D367)&quot;">​</a></h2><ul><li>External-history plugins need durable import/read/update/delete operations, but the existing in-flight <code>session.getLlmContext</code> and core <code>session.import</code> boundaries are not safe plugin ownership boundaries.</li><li>Decision D367 / ADR 0200 adds the P0/P1 <code>pi.session</code> methods: <code>import</code>, <code>importBatch</code>, <code>list</code>, <code>get</code>, <code>listMessages</code>, <code>rename</code>, and <code>delete</code>. <code>contributes.sessionSources</code> is required for every source. Host-core generates ids and scopes all operations to <code>(pluginId, source, externalId)</code> ownership; imports never activate project/provider/model bindings.</li><li>Schema v14 adds <code>session_import_origins</code> and <code>sessions.deleted_at</code>; protocol v11 remains unchanged. Trash preserves the transcript for owner-only purge. P2/P3 create, message mutation, binding, batch-delete, and tag APIs remain deferred. See ADR 0200 and E2E-214/E2E-215.</li></ul><h2 id="_2026-09-09-—-explicit-plugin-project-ids-and-host-owned-session-refresh-d368" tabindex="-1">2026-09-09 — Explicit plugin project ids and host-owned session refresh (D368) <a class="header-anchor" href="#_2026-09-09-—-explicit-plugin-project-ids-and-host-owned-session-refresh-d368" aria-label="Permalink to &quot;2026-09-09 — Explicit plugin project ids and host-owned session refresh (D368)&quot;">​</a></h2><ul><li>Imported sessions need an explicit way to belong to a durable project, but exposing <code>workspace.set</code> to plugins would also change the user&#39;s active workspace. Plugin writes also need a host-owned renderer refresh path.</li><li>Decision D368 / ADR 0201 adds permission-gated <code>pi.project.create({ path })</code>. It creates or reuses a project and returns its host-generated id without activating the workspace. Import items may provide an existing <code>projectId</code>; omitted ids remain unbound, and historical origin paths do not become tool roots. List/get projections report the explicit binding.</li><li>Electron main emits <code>pi-desktop/session/event/changed</code> after successful plugin imports, renames, and deletes; the renderer reuses <code>refreshSessions()</code>, while skipped imports emit nothing and closed project tabs are not reopened. See E2E-216.</li></ul><h2 id="_2026-09-09-—-effective-subagent-thinking-metadata-d369" tabindex="-1">2026-09-09 — Effective subagent thinking metadata (D369) <a class="header-anchor" href="#_2026-09-09-—-effective-subagent-thinking-metadata-d369" aria-label="Permalink to &quot;2026-09-09 — Effective subagent thinking metadata (D369)&quot;">​</a></h2><ul><li>Delegation cards need the thinking level actually passed to each child run, not a value re-derived from the parent or definition after the target model clamps it.</li><li>Decision D369 / ADR 0202 adds effective <code>modelId</code> and <code>thinkingLevel</code> to the immediate <code>Task</code> result, <code>SubagentRunResult</code>, and lifecycle snapshots. The topology node and side-dock header show the raw canonical non-<code>off</code> level after the model name; <code>off</code>, <code>omit</code>, and unsupported reasoning remain model-only. No host protocol, storage schema, provider request, or lifecycle behavior changes. D395 / ADR 0221 removes translation of the canonical value. See E2E-219.</li></ul><h2 id="_2026-09-09-—-explicit-unsigned-macos-first-launch-helper-d371-amended-by-d406" tabindex="-1">2026-09-09 — Explicit unsigned macOS first-launch helper (D371; amended by D406) <a class="header-anchor" href="#_2026-09-09-—-explicit-unsigned-macos-first-launch-helper-d371-amended-by-d406" aria-label="Permalink to &quot;2026-09-09 — Explicit unsigned macOS first-launch helper (D371; amended by D406)&quot;">​</a></h2><ul><li>The default macOS lane remains unsigned, and a downloaded trusted artifact can still be blocked by Apple&#39;s quarantine check with a misleading damaged-app message. The existing <code>xattr -cr</code> note was terminal-only and broader than the launch failure requires.</li><li>Decision D371 / ADR 0204 adds an executable <code>PI-Desktop-macOS-open.command</code> to every macOS distribution. The DMG places it in a visible first-launch row below the drag-to-Applications gesture.</li><li>The helper searches only <code>/Applications/PI-Desktop.app</code> and <code>~/Applications/PI-Desktop.app</code>, verifies <code>CFBundleIdentifier</code> is <code>com.pi-desktop.app</code>, removes only <code>com.apple.quarantine</code> recursively when present, and opens PI-Desktop. It never uses <code>sudo</code>, accepts no arbitrary path, and does not replace Developer ID signing or notarization. The opening note keeps the explicit trusted-source warning and a narrow Terminal fallback. See E2E-196b.</li></ul><h2 id="_2026-09-09-—-local-mcp-control-plane-for-desktop-operations-d370" tabindex="-1">2026-09-09 — Local MCP control plane for desktop operations (D370) <a class="header-anchor" href="#_2026-09-09-—-local-mcp-control-plane-for-desktop-operations-d370" aria-label="Permalink to &quot;2026-09-09 — Local MCP control plane for desktop operations (D370)&quot;">​</a></h2><ul><li>An external Agent needs to drive the running desktop for project, session, Agent, workspace, and reviewed application operations, while the deferred remote Gateway / WebUI boundary remains unchanged.</li><li>Decision D370 / ADR 0203 adds an opt-in Streamable HTTP MCP server inside Electron Main. It binds to <code>127.0.0.1</code>, authenticates with a persistent random bearer token, validates supplied Origins against loopback hostnames, writes a mode-restricted connection manifest, and delegates to the same registered IPC handlers used by the renderer.</li><li>Named tools cover the common project/session/Agent flow; <code>pi_desktop_invoke</code> reaches an explicit risk-tagged operation catalog. Secret channels and renderer-only native pickers are excluded, while dangerous operations require <code>confirm: true</code>. Successful external mutations reuse the existing renderer session-change event so visible state follows the control call.</li><li>The endpoint is local-only and fail-soft at startup; it does not create a remote authentication, cloud sync, or second permission implementation. See <code>02-architecture/01-architecture.md</code>, <code>03-runtime/01-ipc-protocol.md</code>, <code>05-security/01-security.md</code>, and E2E-220.</li></ul><h2 id="_2026-09-10-—-tighten-the-local-mcp-control-plane-boundary-d372" tabindex="-1">2026-09-10 — Tighten the local MCP control-plane boundary (D372) <a class="header-anchor" href="#_2026-09-10-—-tighten-the-local-mcp-control-plane-boundary-d372" aria-label="Permalink to &quot;2026-09-10 — Tighten the local MCP control-plane boundary (D372)&quot;">​</a></h2><ul><li>D370&#39;s first catalog still exposed native pickers (<code>plugin/loadDev</code>), secret-write provider/OAuth/MCP paths, and <code>session/configure</code> without confirmation, and treated session reads as renderer mutations.</li><li>Decision D372 amends ADR 0203: the first-version catalog is the project/session/Agent/workspace flow plus reviewed reads; secret-shaped arguments are stripped; <code>session/configure</code> is dangerous; listen addresses are asserted loopback; protocol versions are negotiated rather than echoed; results including <code>structuredContent</code> are bounded; renderer session refresh is mutation-only. See E2E-220.</li></ul><h2 id="_2026-09-10-—-remote-agent-control-uses-a-dedicated-host-boundary-d373" tabindex="-1">2026-09-10 — Remote Agent Control uses a dedicated Host boundary (D373) <a class="header-anchor" href="#_2026-09-10-—-remote-agent-control-uses-a-dedicated-host-boundary-d373" aria-label="Permalink to &quot;2026-09-10 — Remote Agent Control uses a dedicated Host boundary (D373)&quot;">​</a></h2><ul><li>Remote control is a post-MVP capability and must not expose the existing Electron IPC, <code>host.proxy</code>, or Rust host-core stdio boundary.</li><li>Decision D373 / ADR 0205 defines a logical Agent Host that owns Sessions, Turns, event cursors, approvals, attachments, workspace policy, and crash recovery. A production Gateway owns identity, routing, rate limits, revocation, and audit, while the Agent Host connects outbound.</li><li>RACP v1 keeps the semantic operation model independent of transport: WebSocket JSON-RPC is the primary interactive binding, HTTP/JSON + SSE is the browser binding, and gRPC over TLS is the native/Gateway binding. The bindings share event sequences, snapshots, idempotency, authorization, and error semantics. See the remote architecture, protocol, security, and rollout specifications and E2E-221 through E2E-230.</li></ul><h2 id="_2026-09-10-—-amend-the-remote-agent-control-target-before-implementation-d374" tabindex="-1">2026-09-10 — Amend the remote Agent Control target before implementation (D374) <a class="header-anchor" href="#_2026-09-10-—-amend-the-remote-agent-control-target-before-implementation-d374" aria-label="Permalink to &quot;2026-09-10 — Amend the remote Agent Control target before implementation (D374)&quot;">​</a></h2><ul><li>A review of the D373 draft against the shipped desktop found that the remote approval vocabulary was narrower than the local <code>allow-once</code> / <code>allow-session</code> / <code>deny</code> and Plan/Goal permission-mode contracts, that pending permission requests were connection state rather than Host state, that per-token deltas would exhaust the replay window, that three required bindings and two IDLs were more than v1 can carry, and that browsers cannot satisfy header-only authentication on WebSocket and SSE.</li><li>Decision D374 amends ADR 0205: <code>RACP-WS</code> is the only normative v1 binding with <code>RACP-HTTP</code> as the browser profile and <code>RACP-GRPC</code> reserved; typebox in <code>packages/shared</code> is the single contract source; the first deliverable is the headless <code>packages/agent-host</code> module that desktop IPC, local MCP, and RACP all call; cursors are <code>{ epoch, sequence }</code> with ephemeral deltas and an in-memory first log; the turn queue moves into the Host; host-core exposes <code>permissions.pending</code>; remote approvals carry the full local decision vocabulary; a remote permission ceiling defaults to <code>ask</code>; the approval lifetime becomes a bounded Host policy for remote subscribers; the Host link is a relay profile; browser clients use a cookie profile; the Gateway identity source is decided at R3; and the first deployment is single-tenant. See the amended remote architecture, protocol, security, and rollout specifications and E2E-221 through E2E-230.</li></ul><h2 id="_2026-09-10-—-sequence-remote-control-around-recorded-demand-d375" tabindex="-1">2026-09-10 — Sequence remote control around recorded demand (D375) <a class="header-anchor" href="#_2026-09-10-—-sequence-remote-control-around-recorded-demand-d375" aria-label="Permalink to &quot;2026-09-10 — Sequence remote control around recorded demand (D375)&quot;">​</a></h2><ul><li>Issues #176 and #140 ask to operate projects on a remote Linux or WSL machine from the local desktop; issue #100 asks for task and approval notifications on Telegram, WeChat, Slack, or a webhook with simple commands back. No recorded request asks for a browser or phone client of the desktop, and the maintainer has publicly committed to #100.</li><li>Decision D375 amends ADR 0205 a second time: rollout R2 becomes the SSH-tunnel remote Host, with a <code>pi-host</code> bundle downloaded from GitHub Releases by a bootstrap script uploaded over the user&#39;s own SSH session, checksum-verified, bound to loopback, paired with the desktop, reached through a port forward on the <code>RACP-WS</code> header profile, and rendered by the unchanged renderer through a desktop RACP client adapter under <code>lib/api.ts</code>. RACP gains the remote-host profile (<code>session/configure</code>, <code>session/fork</code>, <code>session/rename</code>, <code>session/delete</code>, <code>session/compact</code>, <code>workspace/list</code>, <code>workspace/read</code>, <code>workspace/diff</code>, <code>terminal/*</code>) and the reverse tool relay (<code>tools/advertise</code>, <code>tool/execute</code>) in the same milestone, which is not split. A remote session lives on its Host; provider configuration is written over SSH; the SSH-paired desktop device is exempt from the remote permission ceiling unless <code>applyCeilingToPairedDevices</code> is set; queued turns are persisted by host-core and held after a restart; the remote approval lifetime defaults to 30 minutes. R3 becomes the outbound messaging integration, webhook first. The Gateway, browser profile, and gRPC binding stay specified but unscheduled, and the Gateway identity source is fixed to the PI account service of the pi-backend specification. See the amended remote specifications and E2E-231 / E2E-232.</li></ul><h2 id="_2026-09-10-—-remote-control-stays-user-local-no-first-party-identity-d385" tabindex="-1">2026-09-10 — Remote control stays user-local: no first-party identity (D385) <a class="header-anchor" href="#_2026-09-10-—-remote-control-stays-user-local-no-first-party-identity-d385" aria-label="Permalink to &quot;2026-09-10 — Remote control stays user-local: no first-party identity (D385)&quot;">​</a></h2><ul><li>The maintainer requires that a user&#39;s client never authenticate through a service the project operates and that remote control stay entirely on the user&#39;s own machines and infrastructure.</li><li>Decision D385 amends ADR 0205 a third time and withdraws D375 item 10: no project-operated identity, account, or relay service is in the path; the only credential a client holds is a device token issued by the user&#39;s own Host at pairing; a Gateway, if ever scheduled, is self-hosted by the user and admits clients with those Host-issued credentials; OIDC federation and the pi-backend account service are out of scope. The Host&#39;s only outbound connections are the user&#39;s SSH hosts, the user&#39;s own messaging channels, the model providers the user configured, and the read-only GitHub Releases download of <code>pi-host</code>. The SSH-tunnel topology, device pairing, and the messaging integration already satisfy the rule.</li></ul><h2 id="_2026-09-10-—-persist-the-host-owned-turn-queue-in-host-core-d386" tabindex="-1">2026-09-10 — Persist the Host-owned turn queue in host-core (D386) <a class="header-anchor" href="#_2026-09-10-—-persist-the-host-owned-turn-queue-in-host-core-d386" aria-label="Permalink to &quot;2026-09-10 — Persist the Host-owned turn queue in host-core (D386)&quot;">​</a></h2><ul><li>D375 moved queued prompts into the Host and chose persistence; the headless Agent Host module needs a store that survives a restart and never starts work by itself, and host-core owns SQLite exclusively.</li><li>Decision D386 / ADR 0213 adds schema v15 with the <code>turn_queue</code> table and the additive <code>session.queuePush</code> / <code>session.queueList</code> / <code>session.queueRemove</code> / <code>session.queuePrioritize</code> methods. Push is idempotent per principal and key, bounded at eight entries per session, and cascades with session deletion. The module restores entries when host-core answers, holds every restored session until a controller attaches, and drains one entry only after the active turn&#39;s terminal event. Protocol stays v11; the renderer&#39;s in-memory queue is retired, and its &quot;send now&quot; becomes RACP <code>turn/prioritize</code> plus a graceful stop.</li></ul><h2 id="_2026-09-10-—-trusted-extensions-run-in-the-agent-sidecar-d387" tabindex="-1">2026-09-10 — Trusted extensions run in the Agent sidecar (D387) <a class="header-anchor" href="#_2026-09-10-—-trusted-extensions-run-in-the-agent-sidecar-d387" aria-label="Permalink to &quot;2026-09-10 — Trusted extensions run in the Agent sidecar (D387)&quot;">​</a></h2><ul><li>Plugins are sandboxed and run outside the agent, which is right for distributed, untrusted code but leaves no surface for code that must sit on the agent loop itself: tools that execute in-process, hooks on every turn and provider request, and slash commands with session context.</li><li>Decision D387 / ADR 0214 adds trusted extensions as the second extension surface. The contract is the <code>ExtensionAPI</code> of <code>pi-coding-agent</code>, adopted at the same pinned version as <code>pi-ai</code> and <code>pi-agent-core</code>; the sidecar reuses its loader and <code>ExtensionRunner</code>, one Runner per session, with the API implemented over the runtime&#39;s existing hooks. Extensions are labelled &quot;Trusted extension&quot;, disabled until the user enables each entry, and discovered from <code>~/.pi/agent/extensions</code>, <code>&lt;workspace&gt;/.pi/extensions</code>, or a manual path; D007 stays: <code>~/.pi</code> is never imported. Every API member is Supported, Deferred (v2), or Unsupported; unsupported members are inert with a diagnostic and never throw; terminal-UI surfaces stay unsupported. Plugins, host-core RPC, protocol version, and schema are untouched in v1; the new traffic is sidecar proxy methods and Electron IPC. Delivery starts with the bundling spike (E2E-245). Spec: <code>07-plugins/16-trusted-extensions.md</code>; scenarios E2E-241 to E2E-245.</li></ul><h2 id="_2026-09-10-—-extend-provider-retries-and-show-bounded-progress-d378" tabindex="-1">2026-09-10 — Extend provider retries and show bounded progress (D378) <a class="header-anchor" href="#_2026-09-10-—-extend-provider-retries-and-show-bounded-progress-d378" aria-label="Permalink to &quot;2026-09-10 — Extend provider retries and show bounded progress (D378)&quot;">​</a></h2><ul><li>The runtime-owned provider retry budgets surfaced short provider outages earlier than the product target, and the active-turn row showed only a retry number without the current wait or its bound.</li><li>Decision D378 amends D245 / D259 and ADR 0091 / ADR 0128: both the HTTP 429 and admitted non-429 transient provider paths receive ten retries after the initial attempt, with setup and stream failures sharing one counter per error class. The two classes stay separate, pi-ai&#39;s nested retry remains disabled, and non-429 waits stay at the 8-second cap after the 1/2/4-second schedule.</li><li><code>PROVIDER_RETRY_MAX_RETRIES</code> becomes the shared runtime/renderer budget constant. The active-turn status derives a countdown from the existing <code>retryDelayMs</code> and <code>since</code> fields and renders the attempt bound, for example <code>Retrying in 0s · attempt 9/10</code>. No host protocol, storage schema, provider configuration, or unrelated recovery policy changes. See ADR 0206 and E2E-096 / E2E-149.</li></ul><h2 id="_2026-09-10-—-name-downgraded-and-non-native-builds-at-boot-d380" tabindex="-1">2026-09-10 — Name downgraded and non-native builds at boot (D380) <a class="header-anchor" href="#_2026-09-10-—-name-downgraded-and-non-native-builds-at-boot-d380" aria-label="Permalink to &quot;2026-09-10 — Name downgraded and non-native builds at boot (D380)&quot;">​</a></h2><ul><li>Installing 0.14.5 over a data directory that 0.14.6-rc.4 had migrated to schema 14 produced three silent host restarts and &quot;Can&#39;t reach the local service&quot;; only <code>logs/host/runtime.log</code> said <code>database schema version 14 is newer than supported 13</code>. An Intel macOS build on Apple Silicon likewise ran under Rosetta with no hint.</li><li>Decision D380 extends the Linux glibc guard pattern (E2E-195): Electron parses host-core&#39;s schema refusal from the last stderr, stops supervising on the first failure, and pushes <code>hostStatus</code> with <code>DB_SCHEMA_TOO_NEW</code> plus both schema numbers so the banner can say which version to install. Boot also detects a non-native build (<code>sysctl.proc_translated</code> on macOS, <code>os.machine()</code> elsewhere) and ships <code>archMismatch</code> on the boot status; the renderer shows a dismissible hint naming Intel / Apple Silicon. No new error code: both keep <code>HOST_UNAVAILABLE</code> and reuse the status-token <code>message</code> channel. No downward migration is attempted. See E2E-239 / E2E-240.</li></ul><h2 id="_2026-09-10-—-allow-three-same-path-mutation-recovery-failures-d379" tabindex="-1">2026-09-10 — Allow three same-path mutation recovery failures (D379) <a class="header-anchor" href="#_2026-09-10-—-allow-three-same-path-mutation-recovery-failures-d379" aria-label="Permalink to &quot;2026-09-10 — Allow three same-path mutation recovery failures (D379)&quot;">​</a></h2><ul><li>The previous repeat guard ended a prompt after two counted failures on one <code>Edit</code> path or recognized shell patch key. That boundary could stop the model while it was still applying a distinct recovery hint from the prior failure.</li><li>Decision D379 amends D186 and ADR 0087 / ADR 0207: the same-path mutation guard allows three counted failures per prompt. Recoverable error codes keep their one-code grace, successful mutations clear the path history, and the third counted failure returns <code>terminate: true</code> with <code>MUTATION_RETRY_BUDGET_EXHAUSTED</code>. The same limit applies to recognized shell patch commands. No IPC, storage, host-protocol, or tool-result shape changes. See E2E-140 / E2E-141.</li></ul><h2 id="_2026-09-10-—-plugin-desktop-control-requires-native-user-consent-d377" tabindex="-1">2026-09-10 — Plugin desktop control requires native user consent (D377) <a class="header-anchor" href="#_2026-09-10-—-plugin-desktop-control-requires-native-user-consent-d377" aria-label="Permalink to &quot;2026-09-10 — Plugin desktop control requires native user consent (D377)&quot;">​</a></h2><ul><li>A code and documentation review found that a plugin holding <code>desktop.control</code> could run <code>dangerous</code> catalog operations (session delete, permission-mode change, tool approval) with no human in the loop: the shared controller&#39;s <code>confirm: true</code> is set by the plugin itself, and the only confirmation was whatever card the plugin chose to draw. The same review found the Electron <code>fetch</code> service let an allowlisted host redirect a plugin request to an undeclared host, that <code>manifest.main</code> and <code>ui.panel</code> skipped path containment, and that the spec 15 security denylist and ignore layers had never been implemented in host-core.</li><li>Decision D377 (ADR 0208): a <code>dangerous</code> desktop operation from a plugin needs the plugin&#39;s <code>confirm: true</code> and then the user&#39;s answer to a host-owned native dialog that names only the catalog operation, description, and a bounded argument preview; refusals and headless hosts fail with <code>PERMISSION_DENIED</code>. The MCP contract (D372) is unchanged.</li><li>In the same change, without new decisions: <code>pi.net.fetch</code> has one path, the runtime hop loop that re-checks <code>net.domains</code> before every redirect; <code>manifest.main</code> and <code>ui.panel</code> are validated and resolved inside the plugin; host-core enforces the spec 15 security denylist (<code>WORKSPACE_PATH_DENIED</code>), app defaults, <code>.pi-desktopignore</code>, and <code>&lt;data_dir&gt;/ignore</code> for unscoped walks; the path resolver follows dangling symlinks so a write through one cannot leave the workspace; and a <code>tools.execute</code> for an unknown session returns <code>SESSION_NOT_FOUND</code> instead of inheriting the global workspace. See E2E-234 through E2E-238.</li></ul><h2 id="_2026-09-10-—-powershell-7-as-a-selectable-windows-command-shell-d381" tabindex="-1">2026-09-10 — PowerShell 7 as a selectable Windows command shell (D381) <a class="header-anchor" href="#_2026-09-10-—-powershell-7-as-a-selectable-windows-command-shell-d381" aria-label="Permalink to &quot;2026-09-10 — PowerShell 7 as a selectable Windows command shell (D381)&quot;">​</a></h2><ul><li>Issue #151: a Windows user who installs PowerShell 7 (<code>pwsh.exe</code>) had no way to run <code>Bash</code> under it. PowerShell 7 installs side by side with, and never replaces, the in-box 5.1 that ADR 0054&#39;s <code>windows-powershell</code> entry resolves, and the catalog exposed no other entry: <code>crates/host-core/src/tools/shell.rs</code> had no <code>pwsh.exe</code> resolution path, and <code>COMMAND_SHELL_IDS</code> listed four IDs.</li><li>Decision D381 (ADR 0209) adds the stable ID <code>windows-pwsh</code> (&quot;PowerShell 7&quot;) to the Windows catalog, amending ADR 0054 §1. It resolves <code>%ProgramFiles%\\\\PowerShell\\\\7\\\\pwsh.exe</code> (plus <code>%ProgramW6432%</code> when the host process is 32-bit), then <code>pwsh.exe</code> on PATH; a miss returns <code>SHELL_NOT_FOUND</code> naming both searched locations. It keeps the <code>powershell</code> dialect and the pinned non-interactive script, and it is never selected implicitly, so the platform default and existing users&#39; shells are unchanged.</li><li>Protocol, storage, and tool surfaces are additive: <code>CommandShellId</code> gains one member while the folded settings validation and the first-available fallback need no new code path, and no RPC method, tool name, or schema migration is added. See E2E-112.</li></ul><h2 id="_2026-09-10-—-a-settled-subagent-reads-back-why-it-failed-d382" tabindex="-1">2026-09-10 — A settled subagent reads back why it failed (D382) <a class="header-anchor" href="#_2026-09-10-—-a-settled-subagent-reads-back-why-it-failed-d382" aria-label="Permalink to &quot;2026-09-10 — A settled subagent reads back why it failed (D382)&quot;">​</a></h2><ul><li>A delegate that ended <code>failed</code>, <code>timed_out</code>, or <code>aborted</code> showed only its outcome capsule and an elapsed time. The reason existed but had no reader: <code>SubagentRunResult.error</code> travels on the delegation roster entry, while a node and its dock render the <code>Task</code> row&#39;s own result, which ADR 0089 fixes at <code>running</code> the moment the delegate starts. A delegate that died before emitting a message row therefore left a panel of steps and no explanation.</li><li>Decision D382: the delegation&#39;s <code>error: { code, message }</code> is collected from the lifecycle rows&#39; <code>details.delegations[]</code> and <code>details.stopped[]</code> with the same last-write-wins rule as the settled status, and the dock closes with an error card that reuses the transcript error card&#39;s visual language and its <code>errors.&lt;code&gt;</code> vocabulary, falling back to the localized <code>chat.subagentStatus.*</code> outcome when a code is not registered. The card follows a non-success terminal outcome rather than the error field alone, so a completed or running delegate never shows one. No runtime, IPC, storage, or tool-result change: the runtime already reported the error, and this stops discarding it. See <code>04-ux/08-component-spec.md</code> §5.7 and E2E-198.</li></ul><h2 id="_2026-09-10-—-a-delegate-can-declare-its-own-output-limit-d383" tabindex="-1">2026-09-10 — A delegate can declare its own output limit (D383) <a class="header-anchor" href="#_2026-09-10-—-a-delegate-can-declare-its-own-output-limit-d383" aria-label="Permalink to &quot;2026-09-10 — A delegate can declare its own output limit (D383)&quot;">​</a></h2><ul><li>A subagent definition could bound its turns but not its response length. <code>maxTurns</code> ran through the frontmatter, the record, the input, the editor draft and the runtime, while the only output bound a delegate had was the one its model binding published: <code>ModelBinding.maxTokens</code> in <code>packages/agent-runtime/src/model-capabilities.ts</code> caps every caller of that model, so raising it for one long-winded delegate raised it for the session too. <code>SubagentDefinition</code>, <code>SubagentDraft</code> and <code>UserSubagentRecord</code> had no <code>maxTokens</code>, and <code>SubagentRun</code> never passed one.</li><li>Decision D383 (ADR 0210): a definition may declare <code>maxTokens</code>, parsed under the same loose key spellings as <code>maxTurns</code> and bounded by a 200000 ceiling. Omitting it, <code>none</code> or <code>0</code> follows the model&#39;s published limit; a value outside 1–200000 is treated as a typo that is reported and ignored or clamped rather than forwarded. The declared value overrides <code>maxTokens</code> on the model the delegate builds for itself, so the adapter&#39;s derived <code>max_tokens</code> / <code>max_completion_tokens</code> / <code>max_output_tokens</code> carry it while the session&#39;s own requests keep the model binding. It is stored in the capability document&#39;s frontmatter and edited beside the turn limit in the editor&#39;s existing Advanced disclosure. No new IPC, tool-result, or storage shape: the field rides the existing subagent record and input. See E2E-119 / E2E-155.</li></ul><h2 id="_2026-09-10-—-plan-safe-plugin-actions-and-summon-window-shortcut-d384" tabindex="-1">2026-09-10 — Plan-safe plugin actions and summon-window shortcut (D384) <a class="header-anchor" href="#_2026-09-10-—-plan-safe-plugin-actions-and-summon-window-shortcut-d384" aria-label="Permalink to &quot;2026-09-10 — Plan-safe plugin actions and summon-window shortcut (D384)&quot;">​</a></h2><p><em>(the summon-window half is superseded by D438, which replaced the pair, and by D439, which moved the toggle off the macOS close-window chord; the plan-safe plugin-action opt-in below stays)</em></p><ul><li>Plan and Goal modes could not invoke plugin tools at all, even for read-only actions like fetching a URL through the bundled Browser plugin, and the keyboard shortcut catalog only exposed a close-window binding with no symmetric way to bring a tray-hidden window back.</li><li>Decision D384 amends ADR 0052 / ADR 0053 with a per-action plugin opt-in: a plugin tool may declare a <code>planSafeActions</code> list naming the exact actions it considers safe in contract modes. The runtime hides plugins without such a list in Plan and Goal, the host admits the declared list in <code>tools.execute</code>, and the desktop runner rejects any call whose action is not in the list. The bundled Browser plugin declares <code>[&quot;navigate&quot;, &quot;snapshot&quot;, &quot;screenshot&quot;, &quot;console&quot;]</code>; the mutating actions stay Agent-only. The shortcut catalog gains <code>summonWindow</code> (<code>Mod+Shift+W</code>) in the <code>window</code> group, paired with <code>closeWindow</code> (<code>Mod+W</code>), and the desktop main process registers it through <code>globalShortcut</code> and the native menu. See ADR 0211 and E2E-PLAN-005.</li></ul><h2 id="_2026-09-17-—-one-window-toggle-replaces-the-summon-close-pair-360-d438" tabindex="-1">2026-09-17 — One window toggle replaces the summon/close pair (#360, D438) <a class="header-anchor" href="#_2026-09-17-—-one-window-toggle-replaces-the-summon-close-pair-360-d438" aria-label="Permalink to &quot;2026-09-17 — One window toggle replaces the summon/close pair (#360, D438)&quot;">​</a></h2><ul><li>Issue #360 item 3 asks for the summon-window and hide/close-window global shortcuts to become one key. D384 shipped the opposite arrangement — <code>summonWindow</code> (<code>Mod+Shift+W</code>) as the &quot;symmetrical counterpart&quot; of <code>closeWindow</code> (<code>Mod+W</code>), mirrored by <code>04-ux/09-interaction-patterns.md</code> §1.1 — so D438 supersedes exactly that half of D384; the plan-safe plugin-action opt-in from the same decision is untouched. The numbering skips D435 and D437, which other in-flight work already claims.</li><li>The catalog ships one <code>toggleWindow</code> id in the <code>window</code> group, and <code>closeWindow</code> and <code>summonWindow</code> leave <code>KEYBOARD_SHORTCUT_IDS</code>, so neither retired chord is spent on anything: neither is a shipped default, registered with <code>globalShortcut</code>, a settings row, or a menu accelerator, and the freed <code>Mod+Shift+W</code> becomes available to plugins again. D439 later moved the toggle&#39;s own default off <code>Mod+W</code>, which D438 had chosen; the merged id, the toggle semantics, and the migration below are unchanged by that.</li><li>Pressing the key is a decision, not a close. <code>windowToggleAction</code> hides a visible, focused window through <code>Window.hide()</code> and otherwise shows and focuses it (hidden, minimized, or behind another application all count as &quot;not visible&quot;). <code>Window.hide()</code> is the whole hide path, so the Windows/Linux close-behaviour prompt, its <code>Quit</code> choice, <code>window-all-closed</code>, and <code>before-quit</code> are never reached, no window is destroyed, and the app keeps running; the tray icon, the same key, or macOS application activation brings the window back. <code>windowControl(&quot;close&quot;)</code> remains the window&#39;s own close button, which is still the only way into the close behaviour.</li><li>Persisted <code>settings.keybindings</code> can still name the two retired ids, and the same rule applies in both processes: <code>migrateKeybindingOverrides</code> folds the map as it is read — in Electron main before it registers accelerators or builds the menu, and in the renderer store before any consumer sees it — so no surface can act on the retired ids. The rules are ordered and idempotent: an existing <code>toggleWindow</code> override wins unchanged; otherwise the first retired entry that carries a <em>binding</em> wins, <code>closeWindow</code> first because hiding is the toggle&#39;s primary job; a retired entry that is only <code>null</code> is honoured when no binding competes, so an explicit unbind is never replaced by a shipped default; and a stored value equal to its own retired default carries no intent (the settings UI deletes overrides that match a shipped default), so <code>Mod+W</code>/<code>Mod+Shift+W</code> values are dropped rather than resurrected. Nothing is rewritten in storage: the folded map is what the next shortcut save persists, and an older release reading a newer map simply ignores the unknown id.</li><li>The window-visibility key is a supported global shortcut on macOS, Windows, and Linux, so it is documented as one: menu label and settings row are localized in all eight catalogs, and the settings card shows a single Window row instead of two contradictory ones.</li><li>See ADR 0211 (amended), <code>04-ux/09-interaction-patterns.md</code> §1.1 and §1.4, <code>04-ux/06-settings-ia.md</code> (shortcuts tab), <code>07-plugins/03-plugin-api.md</code>, <code>07-plugins/04-plugin-security.md</code>, <code>03-runtime/01-ipc-protocol.md</code> (<code>NATIVE_MENU_ACTIONS</code>), E2E-072, and <code>apps/desktop/test/window-toggle-shortcut.test.mjs</code>.</li></ul><h2 id="_2026-09-17-—-the-window-toggle-avoids-the-macos-close-window-chord-360-d439" tabindex="-1">2026-09-17 — The window toggle avoids the macOS close-window chord (#360, D439) <a class="header-anchor" href="#_2026-09-17-—-the-window-toggle-avoids-the-macos-close-window-chord-360-d439" aria-label="Permalink to &quot;2026-09-17 — The window toggle avoids the macOS close-window chord (#360, D439)&quot;">​</a></h2><ul><li>D438 merged the pair onto <code>Mod+W</code>, but that binding cannot be the default of a <em>process-wide</em> accelerator: macOS spends <code>Cmd+W</code> on its own close-window command, so claiming it globally takes the chord away from every other application instead of only closing this window. <code>Mod+W</code> is therefore the one key the toggle must not hold.</li><li>The catalog now ships <code>toggleWindow</code> on <code>Alt+Shift+W</code> — no platform modifier, so it is one chord on macOS, Windows, and Linux — which collides with no shipped default, no reserved editing chord, and no platform command. The merge, the hide/show semantics, and the fold of the retired <code>closeWindow</code>/<code>summonWindow</code> overrides stay exactly as D438 defined them.</li><li>On macOS <code>Mod+W</code> is now also refused to plugins (<code>isReservedKeybinding</code>), so the platform keeps the chord it owns even though the app no longer uses it.</li><li>A stored <code>toggleWindow</code> value that only repeats a default this release ships or already superseded carries no user intent, so the fold drops it: a profile that persisted the short-lived <code>Mod+W</code> default moves to <code>Alt+Shift+W</code> instead of freezing the macOS chord, while a real rebind (<code>Ctrl+Alt+T</code>) and an explicit unbind (<code>null</code>) survive untouched, and a retired customization still wins over a dropped superseded default.</li><li>The number is D439 because D435 and D437 were already claimed by other in-flight work. <strong>Note</strong>: this log currently carries two <code>D438</code> entries — the window-toggle merge above (#360, the pre-rebind decision) and the Git-checkout decision (ADR 0273) near the end of the file. The latter was appended after the toggle&#39;s D438 had merged, so its number needs to move to D440 or the next free id; this entry does not rewrite another decision&#39;s text.</li><li>See ADR 0211 (amended again), <code>04-ux/09-interaction-patterns.md</code> §1.1 and §1.4, <code>04-ux/06-settings-ia.md</code> (shortcuts tab), <code>07-plugins/03-plugin-api.md</code>, <code>07-plugins/04-plugin-security.md</code>, E2E-072, and <code>apps/desktop/test/window-toggle-shortcut.test.mjs</code>.</li></ul><h2 id="_2026-09-10-—-remove-diagnostic-timing-log-streams-d385" tabindex="-1">2026-09-10 — Remove diagnostic timing log streams (D385) <a class="header-anchor" href="#_2026-09-10-—-remove-diagnostic-timing-log-streams-d385" aria-label="Permalink to &quot;2026-09-10 — Remove diagnostic timing log streams (D385)&quot;">​</a></h2><ul><li>The timing-only process log streams introduced by D137/D183 are retired: sidecar <code>[timing]</code> records, host <code>tool timing</code> records, boot phase timing, updater timing records, the dedicated <code>timing</code> category, and <code>PI_DESKTOP_TIMING</code> are no longer emitted.</li><li>Key lifecycle, state-change, permission, tool, plugin, provider, persistence, updater, and error records remain. Existing audit fields and UI/protocol duration metadata remain unchanged because they support security forensics, transcript presentation, and bounded provider diagnostics rather than verbose process logging.</li><li>Existing timing files are historical local data and are not deleted or migrated automatically.</li></ul><h2 id="_2026-09-11-—-agent-extensions-are-a-plugin-contribution-d388" tabindex="-1">2026-09-11 — Agent extensions are a plugin contribution (D388) <a class="header-anchor" href="#_2026-09-11-—-agent-extensions-are-a-plugin-contribution-d388" aria-label="Permalink to &quot;2026-09-11 — Agent extensions are a plugin contribution (D388)&quot;">​</a></h2><ul><li>D387 shipped ExtensionAPI modules as a second surface with its own registry, settings tab, and enablement store. The maintainer wants one surface: a pi CLI extension should become a PI-Desktop plugin and be installed, enabled, scoped, and shown like one.</li><li>Decision D388 / ADR 0215 amends D387: modules are declared as <code>contributes.agentExtensions</code> and gated by the new high-risk permission <code>agent.extension</code>; the manifest is invalid without it, and a plugin whose recorded grants omit it loads with the modules skipped and audited. Enablement and project scope are the plugin&#39;s. &quot;Import pi extension&quot; copies a pi CLI extension into <code>&lt;dataDir&gt;/plugins/imported/&lt;slug&gt;</code> with a generated manifest and registers it as a development plugin; the confirm before the picker is the trust decision. The standalone registry, its store file, the Settings destination, and the list / enable / rescan / add-path / remove channels are removed; the plugin row shows the <code>agentExtension</code> capability, the permission, load state, registered names, and diagnostics. The sidecar loader, Runner, hooks, command and prompt bridges are unchanged. Marketplace distribution of plugins holding <code>agent.extension</code> waits for signing. Spec 16 §1 to §3, §10.2, §11; E2E-241 to E2E-245 re-executed on plugin-form fixtures.</li></ul><h2 id="_2026-09-10-—-deepseek-reasoning-content-replay-on-aggregators-d389" tabindex="-1">2026-09-10 — DeepSeek reasoning_content replay on aggregators (D389) <a class="header-anchor" href="#_2026-09-10-—-deepseek-reasoning-content-replay-on-aggregators-d389" aria-label="Permalink to &quot;2026-09-10 — DeepSeek reasoning_content replay on aggregators (D389)&quot;">​</a></h2><ul><li>DeepSeek thinking mode requires every replayed assistant message to include <code>reasoning_content</code>. Turns that produced no thinking still need the field as <code>&quot;&quot;</code>; omitting it returns HTTP 400.</li><li>pi-ai auto-detects that flag only when <code>model.provider === &quot;deepseek&quot;</code> or the base URL contains <code>deepseek.com</code>. PI-Desktop stores a UUID as <code>model.provider</code>, so SiliconFlow, Volcengine, custom proxies, and other aggregators never match.</li><li>Decision D389 amends D024: Completions rows whose <code>vendorKey</code>, URL, model id, or catalog <code>family</code> identifies DeepSeek receive <code>requiresReasoningContentOnAssistantMessages: true</code>. <code>thinkingFormat</code> is not changed by that match, so OpenRouter and other aggregators keep their existing thinking wire shape. See E2E-005E and <code>03-runtime/11-provider-model-system.md</code>.</li></ul><h2 id="_2026-09-10-—-regenerates-truncate-under-the-host-rpc-lock" tabindex="-1">2026-09-10 — Regenerates truncate under the host RPC lock <a class="header-anchor" href="#_2026-09-10-—-regenerates-truncate-under-the-host-rpc-lock" aria-label="Permalink to &quot;2026-09-10 — Regenerates truncate under the host RPC lock&quot;">​</a></h2><ul><li>Retry, regenerate, and edit-resend used to load the whole transcript in Electron main, archive the discarded tail, and <code>session.replaceMessages</code> the kept prefix. That JSON-RPC line is one NDJSON record: an 80 MB session both exceeds the 64 MiB stdin cap and races the 130 s client deadline, leaving <code>turns.status = running</code> after the UI has failed (issue #211).</li><li><code>session.truncateFrom</code> now performs the cut, the leftover-turn abort, and the discarded-branch archive inside one host call. <code>agent/prompt</code> loads only a bounded <code>session.get</code> for launch configuration.</li><li>Oversized control-pipe lines are drained and answered with <code>LIMIT_EXCEEDED</code> instead of ending the stdin reader.</li><li>Decision D390 and ADR 0216 define this. See <code>03-runtime/04-data-storage.md</code> §4.9/§7, <code>03-runtime/06-host-rpc-protocol.md</code> §4, <code>03-runtime/01-ipc-protocol.md</code>, and E2E-246.</li></ul><h2 id="_2026-09-11-—-host-stdout-sender-must-not-outlive-serve" tabindex="-1">2026-09-11 — Host stdout sender must not outlive serve <a class="header-anchor" href="#_2026-09-11-—-host-stdout-sender-must-not-outlive-serve" aria-label="Permalink to &quot;2026-09-11 — Host stdout sender must not outlive serve&quot;">​</a></h2><ul><li>ADR 0216 stopped regenerate from shipping the kept transcript and stopped an oversize line from killing the stdin reader. On Windows that was not enough: the Alt+Space hook held a strong stdout sender, so after stdin ended the writer thread never stopped, host-core stayed alive, and Electron reported <code>host RPC timeout: session.replaceMessages</code> (issue #211).</li><li>The hook now stores a weak sender. Serve waits at most 5 s for the stdout writer. Electron rejects a request line over 64 MiB before writing. Host-side <code>LIMIT_EXCEEDED</code> peeks the JSON-RPC id from the truncated prefix.</li><li>Decision D391 and ADR 0217 define this. See <code>03-runtime/07-process-model.md</code>, <code>03-runtime/06-host-rpc-protocol.md</code> §7, and E2E-247.</li></ul><h2 id="_2026-09-11-—-selection-overlay-for-quotes-d-local-selection-overlay" tabindex="-1">2026-09-11 — Selection overlay for quotes (D-LOCAL-selection-overlay) <a class="header-anchor" href="#_2026-09-11-—-selection-overlay-for-quotes-d-local-selection-overlay" aria-label="Permalink to &quot;2026-09-11 — Selection overlay for quotes (D-LOCAL-selection-overlay)&quot;">​</a></h2><p><em>(Retired by ADR 0268 on 2026-09-16: the feature and its surfaces were removed, and these IDs stay retired and must not be reused. The record is kept for history.)</em></p><ul><li>Quote no longer has to be reached at the end of the answer: a non-empty text selection inside one transcript row floats one overlay <strong>above</strong> it, centered on the selection and clamped into its clipping ancestors&#39; rects (the transcript scroller is one) and capped by the top of the docked composer. It is portaled like the app&#39;s other body-portaled popovers and follows the selection while the thread scrolls, rather than hiding.</li><li>The overlay offers Add to chat (<code>chat.addToChat</code>), Ask in side chat (<code>chat.askInSideChat</code>) and Copy (<code>chat.copy</code>). Add to chat writes the excerpt into the active session&#39;s composer draft under D-LOCAL-message-quotes&#39;s quote contract; Ask in side chat forks at that row and sends the excerpt as that child&#39;s prompt, so the question is answered in the panel instead of the visible conversation; Copy writes the Markdown to the clipboard. Every action clears the native selection, a drag that crosses rows raises no overlay, and a read-only projection renders none.</li><li>The excerpt is recovered from the rendered DOM instead of <code>Selection.toString()</code>: a range touching a formula expands to the whole formula and quotes KaTeX&#39;s <code>application/x-tex</code> annotation as <code>$…$</code> or <code>$$…$$</code>; a code block becomes a fence with its language and a delimiter longer than any backtick run inside it; a table row becomes one <code>a | b</code> line; inline code keeps its backticks and file-reference chips keep their code text. The row action and the overlay share this one recovery path.</li><li>The 2000-character cap, the <code>&gt; </code> blockquote, the <code>chat.quoteSource</code> attribution, focus, and the no-send/no-session/no-transcript-write boundaries are unchanged. Two deviations from the reference implementation are deliberate, because the quote lands in a Markdown draft this app renders back: formulas use <code>$…$</code> / <code>$$…$$</code> (remark-math), and table rows use <code>a | b</code>. Renderer-only; no protocol, storage-schema, IPC, or permission change beyond the composer&#39;s <code>data-composer-dock</code> hook. See ADR message-quotes-and-side-chats and E2E-CHAT-quote-prefill, E2E-CHAT-selection-markdown, E2E-CHAT-selection-side-chat.</li></ul><h2 id="_2026-09-11-—-response-annotations-as-prompt-attachments-d-local-response-annotations" tabindex="-1">2026-09-11 — Response annotations as prompt attachments (D-LOCAL-response-annotations) <a class="header-anchor" href="#_2026-09-11-—-response-annotations-as-prompt-attachments-d-local-response-annotations" aria-label="Permalink to &quot;2026-09-11 — Response annotations as prompt attachments (D-LOCAL-response-annotations)&quot;">​</a></h2><p><em>(Retired by ADR 0268 on 2026-09-16: the feature and its surfaces were removed, and these IDs stay retired and must not be reused. The record is kept for history.)</em></p><ul><li>Selecting text in an assistant turn and choosing Add to chat attaches a <strong>numbered annotation</strong> to that turn: an id, the anchored turn, the excerpt (Markdown, 2000-character cap), an empty comment field, and capture order as the numbering. Annotations are renderer-owned session state; re-annotating the same excerpt is a no-op, and they are neither persisted nor sent on their own.</li><li>The answer body is left alone: this app never inserts a marker into the text the user reads. A <code>:codex-annotation{index=&quot;N&quot;}</code> directive the model writes (the instruction asks it to cite every annotation it addresses) renders as a small numbered reference whose tooltip is the excerpt, and no marker joins a selection, quote, or copy. The composer shows one attachment chip with the count, whose tooltip lists <code>N. excerpt</code>, plus one control that drops them all. The chip never enters the editable text, so D209&#39;s smart Stop and D301&#39;s draft retention are untouched.</li><li>A send with annotations composes <code># Response annotations:</code> + the instruction + <code>&lt;response-annotations&gt;</code> with <code>[{&quot;text&quot;,&quot;annotation&quot;,&quot;source&quot;:{&quot;messageId&quot;}}]</code> + <code>## My request:</code> + the user&#39;s text, and consumes them. The visible draft, the optimistic row, the sidebar title, and the edit seed stay annotation free; a stored prompt that carries the block is displayed as its request.</li><li>Quoting a user message and the side chat&#39;s Add to main chat keep D-LOCAL-message-quotes&#39;s draft quote. No protocol, storage-schema, IPC channel, or permission change. See ADR response-annotations and E2E-CHAT-annotation-attachments, E2E-CHAT-annotation-session-state.</li><li><em>(Amended 2026-09-12.)</em> Add to chat, and the assistant turn&#39;s annotate action, open a compact comment editor instead of attaching silently: the excerpt is snapshotted before focus collapses the selection, the comment is multiline and optional, Save attaches or updates the annotation with the comment in <code>annotation</code>, and Cancel/Escape discards it without sending. Re-annotating an excerpt that is already attached reopens that annotation&#39;s editor, and the composer&#39;s count chip opens a list where each excerpt&#39;s comment can be edited or that item removed beside the existing clear-all. The editor belongs to the session it was opened in, and a save for an annotation that was already sent or removed changes nothing. No protocol, storage-schema, IPC channel, or permission change.</li><li><em>(Floating-index amendment, ADR floating-annotation-index.)</em> The user-requested collapsible floating index replaces the composer popover. Matching source badges and non-interactive highlights use renderer-only text offsets, never inserted Markdown. Locate reveals history and releases follow mode; duplicate identity now includes source row and selected occurrence. List/badge/prompt numbering stays identical. Send-scoped lifecycle and host boundaries remain unchanged. See E2E-CHAT-annotation-source-index.</li></ul><h2 id="_2026-09-11-—-right-panel-uses-a-tab-strip-and-data-driven-add-menu-d399" tabindex="-1">2026-09-11 — Right panel uses a tab strip and data-driven add menu (D399) <a class="header-anchor" href="#_2026-09-11-—-right-panel-uses-a-tab-strip-and-data-driven-add-menu-d399" aria-label="Permalink to &quot;2026-09-11 — Right panel uses a tab strip and data-driven add menu (D399)&quot;">​</a></h2><ul><li>Issue #229 replaces the right panel&#39;s combined resource menu with a horizontally scrollable header tab strip and a fixed tight <code>+</code> trigger. Only the strip scrolls; tabs use ARIA tab semantics, close on their own hover or focus affordance and middle-click, and keep the active resource in view.</li><li>The <code>+</code> menu has one <strong>Tools &amp; panels</strong> group: host-owned Review followed by the current scoped <code>contributes.views</code> metadata. Files and Browser remain plugin data rather than a renderer hardcoded list. Shortcut labels are conditional on a real binding. The same list is used by the no-tab New launcher, so closing the final tab keeps the panel open instead of hiding it.</li><li>The viewport-fixed toggle remains the sole collapse control and keeps the existing customizable <code>Mod+J</code> binding. Its icon has explicit collapsed and expanded states, and its localized tooltip/accessibility name includes the resolved binding when present.</li><li>A new profile starts with a 360px panel width inside the existing 244–720px range; persisted widths are left unchanged. No protocol, storage schema, plugin manifest, or native-window reservation changes. ADR 0224, UX §4–§5, and E2E-056 define the shipped behavior.</li></ul><h2 id="_2026-09-11-—-reserve-chat-width-for-composer-controls-d401" tabindex="-1">2026-09-11 — Reserve chat width for composer controls (D401) <a class="header-anchor" href="#_2026-09-11-—-reserve-chat-width-for-composer-controls-d401" aria-label="Permalink to &quot;2026-09-11 — Reserve chat width for composer controls (D401)&quot;">​</a></h2><ul><li>MainPane and its chat surface retain a 515px minimum when the sidebar or in-flow work panel changes width. Side docks cannot consume or paint over the reserved composer space.</li><li>The composer toolbar stays on one row with non-shrinking left and right control groups. Mode and permission labels remain single-line and ellipsize inside their chips when localized text is longer than the label slot.</li><li>No IPC, storage, or native-window reservation changes. See ADR 0226 and E2E-168.</li><li>D408 supersedes the live 515px floor with a 450px MainChat floor.</li></ul><h2 id="_2026-09-11-—-long-press-the-project-title-to-reorder-d402" tabindex="-1">2026-09-11 — Long-press the project title to reorder (D402) <a class="header-anchor" href="#_2026-09-11-—-long-press-the-project-title-to-reorder-d402" aria-label="Permalink to &quot;2026-09-11 — Long-press the project title to reorder (D402)&quot;">​</a></h2><ul><li>Retained project groups no longer render a reorder grip. A 400ms still press on the project title arms a pointer reorder; pointer travel beyond 8px before that delay cancels the press so a click still selects the project and toggles collapse.</li><li>After the title is armed, moving the pointer highlights another group in the same pinned/archived bucket; release inserts before or after that group from the pointer&#39;s vertical midpoint. ArrowUp/ArrowDown on the focused title is the keyboard path, and Escape cancels an active drag.</li><li>Reordering still writes contiguous normalized-path <code>order</code> values and switches the renderer-local project sort to <code>manual</code>. Host workspace identity, session ordering, and on-disk directories are unchanged.</li><li>Decision D402 amends D399 / D093 / ADR 0227. See ADR 0228 and E2E-253.</li></ul><h2 id="_2026-09-11-—-press-and-move-project-title-reorder-d403" tabindex="-1">2026-09-11 — Press-and-move project title reorder (D403) <a class="header-anchor" href="#_2026-09-11-—-press-and-move-project-title-reorder-d403" aria-label="Permalink to &quot;2026-09-11 — Press-and-move project title reorder (D403)&quot;">​</a></h2><ul><li>Mouse and pen reorder by pressing the project title and moving 8px. A click with no qualifying movement still selects the project and toggles collapse. Touch does not start a reorder so the list can scroll.</li><li>An accent insertion line shows before or after the target group from the pointer&#39;s vertical midpoint. ArrowUp/ArrowDown and Escape are unchanged.</li><li>Decision D403 amends D402 / D093 / ADR 0228. See ADR 0229 and E2E-253.</li></ul><h2 id="_2026-09-11-—-skill-ships-with-the-agent-core-tool-set-d404" tabindex="-1">2026-09-11 — Skill ships with the Agent core tool set (D404) <a class="header-anchor" href="#_2026-09-11-—-skill-ships-with-the-agent-core-tool-set-d404" aria-label="Permalink to &quot;2026-09-11 — Skill ships with the Agent core tool set (D404)&quot;">​</a></h2><ul><li><code>Skill</code> joins the Agent-mode core tool set, so its schema is present on the first provider request whenever the skill catalog is non-empty. It is no longer part of the deferred catalog and never appears under <code># On-demand tools</code>.</li><li>Registration is unchanged: the tool exists only with a non-empty catalog, and Plan and Goal still omit it entirely. Every other on-demand capability and <code>ToolSearch</code> itself keep their lazy behavior.</li><li>Decision D404 amends D174 / D185 / ADR 0048 / ADR 0219. See ADR 0230 and E2E-254.</li></ul><h2 id="_2026-09-11-—-ideographic-comma-opens-the-slash-menu-d405" tabindex="-1">2026-09-11 — Ideographic comma opens the slash menu (D405) <a class="header-anchor" href="#_2026-09-11-—-ideographic-comma-opens-the-slash-menu-d405" aria-label="Permalink to &quot;2026-09-11 — Ideographic comma opens the slash menu (D405)&quot;">​</a></h2><ul><li>A <code>、</code> committed as the first character of an empty composer draft is rewritten to <code>/</code> before trigger detection runs, so a Chinese IME reaches the ordinary slash menu without switching input methods.</li><li>Only that position is rewritten: a <code>、</code> anywhere later in the draft stays ordinary punctuation, and the <code>@</code> file menu is unaffected.</li><li>Decision D405 amends D123 / D139 / ADR 0024. See ADR 0231 and E2E-255.</li></ul><h2 id="_2026-09-12-—-keep-macos-dmg-opening-guidance-text-only-d406" tabindex="-1">2026-09-12 — Keep macOS DMG opening guidance text-only (D406) <a class="header-anchor" href="#_2026-09-12-—-keep-macos-dmg-opening-guidance-text-only-d406" aria-label="Permalink to &quot;2026-09-12 — Keep macOS DMG opening guidance text-only (D406)&quot;">​</a></h2><ul><li>macOS DMGs expose <code>PI-Desktop-macOS-opening-help.txt</code> with the Finder label <code>If app won&#39;t open, read this.txt</code> and no longer include or expose the executable <code>PI-Desktop-macOS-open.command</code>.</li><li>macOS ZIP packages retain both the opening note and the executable helper. The note provides the narrow Terminal fallback for trusted unsigned builds; signed and notarized builds do not need it.</li><li>Decision D406 amends D371 / ADR 0204. See ADR 0232 and E2E-196b.</li></ul><h2 id="_2026-09-12-—-restore-archived-projects-after-session-import-d407" tabindex="-1">2026-09-12 — Restore archived projects after session import (D407) <a class="header-anchor" href="#_2026-09-12-—-restore-archived-projects-after-session-import-d407" aria-label="Permalink to &quot;2026-09-12 — Restore archived projects after session import (D407)&quot;">​</a></h2><ul><li>A core or plugin import that adds a new project-bound session clears the renderer&#39;s archived presentation state for that session&#39;s normalized project path during the import-triggered refresh.</li><li>Pathless sessions, skipped imports, history-only plugin paths, and ordinary refreshes preserve archive state. The host model and all existing IPC, plugin, storage, and data-format contracts remain unchanged.</li><li>Decision D407 records the issue #250 fix. See ADR 0236 and E2E-257.</li></ul><h2 id="_2026-09-13-—-prioritize-mainchat-in-the-three-column-shell-d408" tabindex="-1">2026-09-13 — Prioritize MainChat in the three-column shell (D408) <a class="header-anchor" href="#_2026-09-13-—-prioritize-mainchat-in-the-three-column-shell-d408" aria-label="Permalink to &quot;2026-09-13 — Prioritize MainChat in the three-column shell (D408)&quot;">​</a></h2><ul><li>The in-flow shell treats MainChat as the first-priority column: a hard 450px minimum, a panel capped by the live budget (<code>client width - 450px - expanded sidebar</code>, no fixed maximum), and an expanded sidebar that yields at the threshold while its exit animation still counts toward the shared budget.</li><li>A manual sidebar reopen spends work-panel width first and otherwise targets 460px. Closing the panel restores only a sidebar the layout collapsed; a manual collapse stays collapsed.</li><li>The native window never participates: the reservation seam stays at zero and no panel width or x-offset geometry is applied.</li><li>Preview mode unmounts MainChat and gives the client area to the work panel beside the sidebar. AppShell supplies a window-level chrome row for shell actions and native controls; macOS reserves 88px in windowed mode and 8px in fullscreen when the sidebar is collapsed.</li><li>Decision D408 records the issue #267 behavior. See ADR 0238 and E2E-LAYOUT-three-column-width-priority.</li></ul><h2 id="_2026-09-13-—-host-owned-session-collaboration-messages-d409" tabindex="-1">2026-09-13 — Host-owned session collaboration messages (D409) <a class="header-anchor" href="#_2026-09-13-—-host-owned-session-collaboration-messages-d409" aria-label="Permalink to &quot;2026-09-13 — Host-owned session collaboration messages (D409)&quot;">​</a></h2><ul><li>Rust host-core owns a durable ledger for plugin-mediated messages between real Session IDs. A delivery is bound to its actual durable target turn before execution, and the result is derived from that turn&#39;s persisted terminal state rather than assistant-text polling.</li><li>The reviewed plugin gateway authenticates the source from the active Agent tool invocation. Existing target sessions retain their project, model, context, and permission configuration; new workers inherit the initiating session&#39;s project and permission ceiling.</li><li>Completion callbacks are durable and at-most-once. Session-message provenance is persisted with transcript rows and is immutable across replacement or regeneration. Restart recovery retains queued work but never replays an unclaimed or interrupted turn automatically.</li><li>Decision D409 amends ADR 0237, ADR 0165, and ADR 0213. See ADR 0239 and E2E-PLUGIN-session-orchestrator-real-workers.</li></ul><h2 id="_2026-09-13-—-independent-session-discovery-and-navigable-collaboration-projections-d410" tabindex="-1">2026-09-13 — Independent session discovery and navigable collaboration projections (D410) <a class="header-anchor" href="#_2026-09-13-—-independent-session-discovery-and-navigable-collaboration-projections-d410" aria-label="Permalink to &quot;2026-09-13 — Independent session discovery and navigable collaboration projections (D410)&quot;">​</a></h2><ul><li>The reviewed plugin gateway exposes a bounded <code>session/collaboration/list</code> read for non-deleted Agent sessions, including sessions created outside Session Orchestrator. It returns no project paths, credentials, transcripts, or message previews; <code>send</code> continues to use the real Session ID and the existing host authorization path.</li><li>Sidebar collaboration summaries now carry readable provider/model labels and bounded creator/created-session references. The renderer presents those references as keyboard-focusable navigation buttons and opens the durable session through the existing selection path.</li><li>Decision D410 amends ADR 0239. See ADR 0240 and E2E-SESSION-independent-top-level-communication / E2E-SESSION-hover-card-model-and-links.</li></ul><h2 id="_2026-09-12-—-steer-the-active-turn-with-alt-enter" tabindex="-1">2026-09-12 — Steer the active turn with Alt+Enter <a class="header-anchor" href="#_2026-09-12-—-steer-the-active-turn-with-alt-enter" aria-label="Permalink to &quot;2026-09-12 — Steer the active turn with Alt+Enter&quot;">​</a></h2><ul><li>Normal Send/Enter remains a Host-owned follow-up; Alt+Enter submits input to the current durable turn using a required expected turn id.</li><li>Reuse pi-agent-core steering at the next model-request boundary, preserve started tools, and retain the active model/workspace/permission configuration.</li><li>Stop and ended targets reject without redirecting input to another turn. Accepted input remains history without independent replay after cancellation.</li><li>Journal accepted user input with a provisional preceding reply when needed; finalize only an indexed streaming assistant in place and retain completed message idempotency. See ADR active-turn-steering and E2E-AGENT-alt-enter-steers-active-turn.</li></ul><h3 id="unified-transcript-reading-and-complete-search-navigation" tabindex="-1">Unified transcript reading and complete search navigation <a class="header-anchor" href="#unified-transcript-reading-and-complete-search-navigation" aria-label="Permalink to &quot;Unified transcript reading and complete search navigation&quot;">​</a></h3><p><a href="/adr/transcript-reading-ownership">ADR transcript-reading-ownership</a> amends <a href="/adr/session-content-search">ADR session-content-search</a> with one renderer reading view for ordinary history and search, shared by MainChat and the subagent dock. Live/model caches remain canonical. Nested targets receive capped parent Task context through additive <code>navigationParent</code>; parser source offsets locate hidden Markdown and file-chip matches. Read ownership rejects interrupted pages, and explicit message actions hydrate canonical input when history is partial or text is capped. Validation: E2E-SESSION-content-search-and-message-navigation.</p><h2 id="_2026-09-13-—-harden-session-collaboration-navigation-and-delivery" tabindex="-1">2026-09-13 — Harden session collaboration navigation and delivery <a class="header-anchor" href="#_2026-09-13-—-harden-session-collaboration-navigation-and-delivery" aria-label="Permalink to &quot;2026-09-13 — Harden session collaboration navigation and delivery&quot;">​</a></h2><ul><li>Collaboration references now carry <code>available</code>. A reference to a session that was deleted or is otherwise gone is reported with <code>available: false</code>; the renderer renders it as text instead of a navigation control, and opening a session that no longer exists reports a visible error instead of committing an empty transcript. <code>session_collaboration_messages.source_session_id</code> intentionally has no foreign key, so a delivery record survives deletion of its sender and is reported as unavailable.</li><li>The six <code>session/collaboration/*</code> operations are first-party-plugin-only: they require an authenticated plugin tool invocation context, so they are excluded from the MCP-visible catalog while remaining available through <code>pi.desktop.listOperations</code> and <code>pi.desktop.invoke</code>. This amends the &quot;same reviewed operation catalog&quot; claim of ADR 0203 / D370 and the <code>desktop.control</code> row of the plugin permission matrix.</li><li>Settling a delivery now asserts that the conditional <code>queued</code> to <code>running</code> claim actually changed a row, so losing that race cannot create a second turn for one delivery, and <code>spawn</code> evaluates its worker limits inside the same transaction as the session insert.</li><li>Electron delivery settlement no longer drops a settlement recorded before a host restart, and a drain that is already running picks up settlements queued during it instead of deferring them to an unrelated trigger. A persisted delivery failure keeps a stable <code>CODE: message</code> form.</li><li>Review fixes for the orchestration refresh path bound the model-catalog lookup work per read instead of relying on cache eviction, and replace E2E boot conditions that could not fail with observations the probe does not itself guarantee.</li><li>See ADR 0239, ADR 0240, E2E-SESSION-hover-card-model-and-links.</li></ul><h3 id="global-sidebar-conversation-pins-issue-306" tabindex="-1">Global sidebar conversation pins (issue #306) <a class="header-anchor" href="#global-sidebar-conversation-pins-issue-306" aria-label="Permalink to &quot;Global sidebar conversation pins (issue #306)&quot;">​</a></h3><p><a href="/adr/global-sidebar-pins">ADR global-sidebar-pins</a> amends ADR 0016: conversation pins occupy one global section above standalone and project history, including closed or collapsed projects. Archive visibility and session sorting still apply. Pins carry project context and are removed from normal history before date grouping and row limits. Persisted metadata and host ownership stay intact. Validation contract: E2E-SIDEBAR-global-pinned-conversations.</p><h2 id="_2026-09-13-—-vendor-the-file-view-as-an-updatable-plugin-issue-304" tabindex="-1">2026-09-13 — Vendor the file view as an updatable plugin (issue #304) <a class="header-anchor" href="#_2026-09-13-—-vendor-the-file-view-as-an-updatable-plugin-issue-304" aria-label="Permalink to &quot;2026-09-13 — Vendor the file view as an updatable plugin (issue #304)&quot;">​</a></h2><ul><li>The work panel&#39;s file view is no longer <code>pi.files</code>. It is a vendored copy of the third-party <code>pi.file-manager</code> release, shipped from <code>apps/desktop/resources/plugins/</code> and recorded in its own <code>UPSTREAM.md</code>. The old plugin is removed, and host-core drops its stale registry row on the next launch.</li><li>Bundled means default and non-removable, not frozen: a bundled plugin can be updated from the marketplace, that update survives the next launch, and a build that ships a strictly newer version still wins. <code>uninstall</code> refuses by ID against what the build ships, not by the row&#39;s <code>source</code>, so an updated plugin stays uninstallable and a plugin dropped from a build is removable again.</li><li>A marketplace entry is offered as an update only when it is strictly newer. Equality is not an update, and an older catalog version is not one either.</li><li>Editing writes stay inside the plugin&#39;s own process, which keeps the workspace path jail, atomic writes, conflict detection, and its write audit. The host gateway does not mediate them: a manifest cannot declare a whole-tree write.</li><li>See ADR 0241 (supersedes ADR 0105), ADR 0104, E2E-153, E2E-PLUGIN-bundled-plugin-keeps-a-marketplace-update.</li></ul><h2 id="_2026-09-14-—-delta-only-coalesced-streaming-updates-d412" tabindex="-1">2026-09-14 — Delta-only coalesced streaming updates (D412) <a class="header-anchor" href="#_2026-09-14-—-delta-only-coalesced-streaming-updates-d412" aria-label="Permalink to &quot;2026-09-14 — Delta-only coalesced streaming updates (D412)&quot;">​</a></h2><ul><li>Append-only <code>message_update</code> frames carry <code>stream: &quot;delta&quot;</code> and the new chunk only. Growing <code>content</code>/<code>thinking</code> stay in the runtime accumulator and in <code>message_end</code>. Retry replacements still send a full snapshot.</li><li>A 16ms coalescer concatenates pending deltas and flushes before tool, terminal, abort, error, and retry events so order is preserved.</li><li>AgentHost applies deltas to live items and skips a hot-path <code>JSON.stringify</code> for small delta frames. Inflight checkpoints reconstruct the snapshot from the same deltas. The renderer concatenates same-frame deltas, then patches the live row. Unchanged activity parts keep object identity.</li><li>Protocol version remains 11. See ADR 0242, E2E-STREAM-long-turn-keeps-realtime, and issue #299.</li></ul><h2 id="_2026-09-13-—-skill-market-public-https-catalog-fetch-d413" tabindex="-1">2026-09-13 — Skill market public-HTTPS catalog fetch (D413) <a class="header-anchor" href="#_2026-09-13-—-skill-market-public-https-catalog-fetch-d413" aria-label="Permalink to &quot;2026-09-13 — Skill market public-HTTPS catalog fetch (D413)&quot;">​</a></h2><ul><li>Skill Market search/fetch run in Electron main. Renderer CSP still forbids the network. Install is existing <code>skills.create</code>.</li><li>Shared <code>isSafePublicHttpsUrl</code> / <code>isPublicHostname</code> / <code>isPublicIpLiteral</code> are the syntactic classifier. Main re-checks DNS and every redirect hop.</li><li>Scanned ids are sanitized to host <code>valid_capability_id</code>. Preview shows the assembled body; over 128 KiB is not written.</li><li>See ADR 0243, E2E-SKILL-MARKET-NET-BOUNDARY / EXPANSION / INSTALL / ID-ALIGN, and issue #287.</li></ul><h2 id="_2026-09-14-—-harden-the-mcp-market-public-network-boundary-d414" tabindex="-1">2026-09-14 — Harden the MCP market public-network boundary (D414) <a class="header-anchor" href="#_2026-09-14-—-harden-the-mcp-market-public-network-boundary-d414" aria-label="Permalink to &quot;2026-09-14 — Harden the MCP market public-network boundary (D414)&quot;">​</a></h2><ul><li>MCP market source and catalog endpoint validation is shared by Renderer and Electron Main. It accepts credentials-free public HTTPS only and rejects loopback, private, CGNAT, link-local, multicast, reserved, documentation, benchmark, ULA, and site-local address ranges, including mapped and compatible IPv6 forms.</li><li>Main resolves each hostname immediately before the request and pins the selected public address to the HTTPS socket. Redirects are followed manually, checked and pinned at every hop, and limited to five hops. Source responses are capped at 4 MiB, calls accept at most 16 sources, and browse/search caches are bounded by age, key count, and entry count.</li><li>Registry npm/PyPI package versions and runtime/package arguments are retained in install templates. Only <code>streamable-http</code> public HTTPS remotes are mapped; remote header placeholders become explicit install values. Cross-origin MCP redirects do not forward caller headers.</li><li>Manual user-owned MCP configuration keeps ADR 0142&#39;s explicit local/LAN endpoint policy. See ADR 0245 and E2E-MCP-MARKET-*.</li></ul><h2 id="_2026-09-14-—-opt-in-subagent-inheritance-of-the-parent-tool-catalog-d415" tabindex="-1">2026-09-14 — Opt-in subagent inheritance of the parent tool catalog (D415) <a class="header-anchor" href="#_2026-09-14-—-opt-in-subagent-inheritance-of-the-parent-tool-catalog-d415" aria-label="Permalink to &quot;2026-09-14 — Opt-in subagent inheritance of the parent tool catalog (D415)&quot;">​</a></h2><ul><li>A subagent definition may declare <code>tools: inherit</code> (alone or with assignable extras). Builtins stay on today&#39;s whitelist.</li><li>At spawn the runtime unions the live <code>toolCatalog</code> minus Task*, mode switches, <code>asktool</code>, <code>new_context</code>, and <code>ToolSearch</code>. Skill/MCP/plugin tools the parent is allowed to call are included; child ToolSearch/new_context do not mutate parent runtime state.</li><li>host-core keeps the <code>inherit</code> token so inherit-only documents load and Settings round-trips them. See ADR 0246, issue #215, PR #319, and E2E-SUBAGENT-inherit-parent-tools.</li></ul><h3 id="tray-session-shortcuts-issue-293" tabindex="-1">Tray session shortcuts (issue #293) <a class="header-anchor" href="#tray-session-shortcuts-issue-293" aria-label="Permalink to &quot;Tray session shortcuts (issue #293)&quot;">​</a></h3><p><a href="/adr/tray-session-shortcuts">ADR tray-session-shortcuts</a> amends the D216 native tray menu with Running, Unread, and Pinned groups after global priority assignment: each non-empty group keeps up to three rows, then overflowing groups reclaim the share smaller groups leave unused, in priority order, up to nine rows in total. Host session/inbox reads and runtime events remain the source of truth; renderer organization is mirrored without a persistence change. macOS single-click opens the menu without restoring the window; selection is delivered after bootstrap and uses normal session navigation. Validation contract: E2E-TRAY-bounded-session-navigation.</p><h2 id="_2026-09-14-—-preserve-local-annotation-and-side-chat-contracts-across-upstream-integration" tabindex="-1">2026-09-14 — Preserve local annotation and side-chat contracts across upstream integration <a class="header-anchor" href="#_2026-09-14-—-preserve-local-annotation-and-side-chat-contracts-across-upstream-integration" aria-label="Permalink to &quot;2026-09-14 — Preserve local annotation and side-chat contracts across upstream integration&quot;">​</a></h2><p><em>(Retired by ADR 0268 on 2026-09-16: the feature and its surfaces were removed, and these IDs stay retired and must not be reused. The record is kept for history.)</em></p><ul><li>Local ADRs now use semantic names <code>message-quotes-and-side-chats</code>, <code>response-annotations</code>, and <code>floating-annotation-index</code>; former local 0223–0225 collided with upstream ADRs. Local D398–D400 references use <code>D-LOCAL-*</code>, and local E2E-249–259 references use <code>E2E-CHAT-*</code>. Upstream identities are unchanged.</li><li>Upstream transcript modules, composer hooks, store slices, delta streaming, work-panel tab strip/launcher, and text-only Alt+Enter steering remain canonical. Quote/annotation controls and side-chat registrations have no upstream equivalent and remain renderer-owned additions at those seams, not duplicate legacy modules.</li><li>Send/queue consumes only acknowledged unchanged annotations in the submitting session. Failures retain them; concurrent edits/new attachments survive. Steering leaves annotations pending, and annotation-only steering submits nothing.</li><li>Request-only queue previews/edit seeds hide the recognized annotation block; ordinary headings and stored/wire content remain intact. Editing does not restore consumed attachments. No native Pi session unification is introduced.</li><li>See ADR response-annotations and E2E-CHAT-annotation-ack-and-steering.</li></ul><h2 id="_2026-09-14-—-native-pi-session-continuation-d421" tabindex="-1">2026-09-14 — Native Pi session continuation (D421) <a class="header-anchor" href="#_2026-09-14-—-native-pi-session-continuation-d421" aria-label="Permalink to &quot;2026-09-14 — Native Pi session continuation (D421)&quot;">​</a></h2><ul><li>Native Pi v3 sessions are discovered as source-discriminated projections and continued through the coding-agent SDK against their original JSONL.</li><li>Rust host-core remains authoritative for Desktop SQLite and Desktop transcripts; native turns never enter the Desktop outbox or create imports.</li><li>Exact version/newline/cwd/trust/provider/auth checks and a cooperative lease with byte/leaf validation make uncertain ownership fail closed. Unsupported native mutations remain explicit rather than silently succeeding.</li><li>See ADR 0254 and E2E-SESSION-native-pi-*.</li></ul><h2 id="_2026-09-14-—-git-clone-accepts-only-syntactically-public-hosts-d416" tabindex="-1">2026-09-14 — Git clone accepts only syntactically public hosts (D416) <a class="header-anchor" href="#_2026-09-14-—-git-clone-accepts-only-syntactically-public-hosts-d416" aria-label="Permalink to &quot;2026-09-14 — Git clone accepts only syntactically public hosts (D416)&quot;">​</a></h2><ul><li>Home Clone git project still accepts https/http/ssh/<code>git@host:path</code> remotes without embedded passwords or <code>file:</code> URLs.</li><li>Hosts are classified with the shared <code>isPublicHostname</code> helper used by the market guards. Private and loopback literals fail in the parser.</li><li>Git&#39;s own DNS/SSH is unchanged; there is no Electron Main pin.</li><li>See ADR 0247 and E2E-CLONE-public-hostname-rejects-private.</li></ul><h2 id="_2026-09-14-—-theme-css-validation-only-inspects-css-that-runs-d417" tabindex="-1">2026-09-14 — Theme CSS validation only inspects CSS that runs (D417) <a class="header-anchor" href="#_2026-09-14-—-theme-css-validation-only-inspects-css-that-runs-d417" aria-label="Permalink to &quot;2026-09-14 — Theme CSS validation only inspects CSS that runs (D417)&quot;">​</a></h2><ul><li>The <code>ui.theme</code> sanitizer masks comment bodies and string literals before the <code>@import</code>, markup, and script keyword checks, one space per masked character so offsets still point at the source, and keeps every <code>url(...)</code> argument verbatim so a real reference is still judged by its target.</li><li>A character-level three-state scan, not a <code>/* … */</code> strip: in <code>content: &quot;/*&quot;;</code> the browser sees a string, and a naive strip would read a comment there and hide the real <code>@import &quot;x.css&quot;;</code> behind it.</li><li><code>examples/plugins/hello/themes/midnight.css</code> loads again. Only the false rejection narrows: no new capability, no format change, and sheets that never name a banned token behave exactly as before. See issue #334 and E2E-024J.</li></ul><h2 id="_2026-09-14-—-themed-package-assets-and-native-window-backgrounds-d418" tabindex="-1">2026-09-14 — Themed package assets and native window backgrounds (D418) <a class="header-anchor" href="#_2026-09-14-—-themed-package-assets-and-native-window-backgrounds-d418" aria-label="Permalink to &quot;2026-09-14 — Themed package assets and native window backgrounds (D418)&quot;">​</a></h2><ul><li><code>contributes.themes[].assets</code> declares package-relative image and font files (whitelisted extensions, 4 MB summed). The host resolves each inside the plugin package, refuses the dependency directory, rewrites every matching <code>url()</code> to <code>plugin-asset://&lt;pluginId&gt;/&lt;path&gt;</code>, and serves it through a privileged host-owned scheme whose handler answers only from the loaded plugin&#39;s declared list. An undeclared reference is still refused and the raw path never reaches the renderer, so the <code>data:</code>-only rule and every existing rejection are unchanged. Permission stays <code>ui.theme</code>.</li><li><code>contributes.windowAppearance.backgroundColor.{light,dark}</code> accepts <code>#rrggbb</code> / <code>#rrggbbaa</code> behind the new <code>ui.window.appearance</code> grant. It applies only while one of that plugin&#39;s themes is the selected theme and only off macOS, and it is restored by derivation: the renderer recomputes the colour from the persisted preference and the live catalog, so a switch, a disable, and an uninstall all converge on the host palette with no stored value to unwind.</li><li>Built-in themes reach the same value through one shared table (<code>packages/shared/src/theme.ts</code>): <code>BUILTIN_THEMES</code> holds each palette&#39;s <code>windowBackground</code> once and <code>isThemeColorScheme</code> holds the built-in-id question once, read by the renderer, main, the panel host, the panel preload, and the theme picker. The pair <code>#ffffff</code> / <code>#181818</code> was restated in four files before this, so the built-in and contributed paths could drift apart. See ADR 0248, issue #335, and E2E-024J.</li></ul><h2 id="_2026-09-14-—-the-dock-column-is-a-token-not-a-literal-d419" tabindex="-1">2026-09-14 — The dock column is a token, not a literal (D419) <a class="header-anchor" href="#_2026-09-14-—-the-dock-column-is-a-token-not-a-literal-d419" aria-label="Permalink to &quot;2026-09-14 — The dock column is a token, not a literal (D419)&quot;">​</a></h2><ul><li>The work-panel column and the bars inside it take their surface from <code>--ds-bg-dock</code> and <code>--ds-bg-dock-raised</code>. In light those are <code>#fafafa</code> and <code>#ffffff</code>; in dark they are <code>var(--ds-bg-secondary)</code> and <code>transparent</code>, which is exactly what the base rules resolved to before.</li><li>Six <code>:root[data-theme=&quot;light&quot;]</code> literals in <code>work-panel.css</code> used to raise specificity above the base rule <em>and</em> skip the variable, so the whole column stayed host-coloured under any contributed theme. Those overrides are gone.</li><li>The design-system surface table now records both tokens, and §6.4 states the rule they exist to enforce: a surface colour the shell paints must come from a token. A literal inside a <code>:root[data-theme]</code> override is the failure mode.</li><li>Same class of hole remains in <code>settings.css</code> (rail, search fields, toggle knob, capability search) and a few other sheets; see issue #339.</li></ul><h2 id="_2026-09-14-—-structured-bounded-and-redacted-process-logs-d420" tabindex="-1">2026-09-14 — Structured, bounded, and redacted process logs (D420) <a class="header-anchor" href="#_2026-09-14-—-structured-bounded-and-redacted-process-logs-d420" aria-label="Permalink to &quot;2026-09-14 — Structured, bounded, and redacted process logs (D420)&quot;">​</a></h2><ul><li>Every app/host/agent NDJSON record has a stable dot-separated <code>event</code> and top-level correlation fields. A normal tool call emits one completion or failure record; an unexpected sidecar exit emits interruption records for active tools. The tool protocol and transcript remain unchanged.</li><li>Central logging redacts credential formats, sensitive keys, and local paths; bounds strings and structured values; and caps each record&#39;s <code>data</code> at 8 KiB. Host-core audit payloads receive corresponding shaping and a serialized payload cap.</li><li>Tool results retain outcome, error/code, duration, field names, content-block count, and stdout/stderr sizes rather than copying raw arguments, output, or plugin responses. Child stderr and main-process fallbacks use stable events; development console mirrors contain the same sanitized record.</li><li>See ADR 0250 and E2E-034.</li></ul><h2 id="_2026-09-15-—-deleting-a-project-removes-its-owned-sessions-d421" tabindex="-1">2026-09-15 — Deleting a project removes its owned sessions (D421) <a class="header-anchor" href="#_2026-09-15-—-deleting-a-project-removes-its-owned-sessions-d421" aria-label="Permalink to &quot;2026-09-15 — Deleting a project removes its owned sessions (D421)&quot;">​</a></h2><ul><li><code>projects.remove({ path })</code> is the new additive host RPC. It removes one durable project row, every session attached to that row, those sessions&#39; transcript/scratch/review files, and the project&#39;s durable memory, and it never touches the project folder on disk. An unknown path returns <code>{ removed: false, sessionsRemoved: 0 }</code> instead of an error, and the desktop removes its own record for that path anyway: a stale recent-project entry is the only thing that can keep such a row visible.</li><li>The Projects index is a union of four sources (group projections of durable rows, <code>pi.desktop.recentProjects</code>, session-derived projects, and the active workspace). Deletion therefore removes the renderer-local record in the same action; deleting only the database row leaves the row visible, which is why the previous manual workaround also required clearing renderer storage.</li><li>A path that is a root of a stored multi-folder project group is refused with a message so the group keeps a valid primary root; legacy single-root projections delete normally.</li><li>The delete is refused while any attached session has a running turn (1008 / <code>CONFLICT</code>, &quot;project has running sessions&quot;): a live turn still owns its tools and working directory and is still appending to its transcript, so the bulk delete waits for the project to be idle instead of resurrecting a stub session afterwards. The renderer blocks the same action up front with <code>project.deleteRunningBlocked</code>. Single-conversation delete keeps its current semantics.</li><li>The action is deliberately absent from <code>CONTROL_OPERATION_SPECS</code>, so local MCP control cannot delete projects. See ADR 0251 and E2E-PROJECT-delete-removes-project-and-owned-sessions.</li></ul><h2 id="_2026-09-13-—-host-turn-end-event-for-plugins-d422" tabindex="-1">2026-09-13 — Host turn-end event for plugins (D422) <a class="header-anchor" href="#_2026-09-13-—-host-turn-end-event-for-plugins-d422" aria-label="Permalink to &quot;2026-09-13 — Host turn-end event for plugins (D422)&quot;">​</a></h2><ul><li><code>session:turnEnded</code> is delivered to plugin processes, plugin panel pages, and docked views with <code>{ sessionId, turnId, reason }</code>, where <code>reason</code> is <code>completed</code>, <code>aborted</code>, or <code>error</code>, once per turn actually started by <code>session.beginTurn</code> (a user submission, an approved plan execution, or a scheduled run).</li><li>The announcement runs at the end of turn teardown, after the durable <code>session.endTurn</code> attempt, and carries the <code>turnId</code> of the terminal runtime event rather than whichever turn is active. The plugin tool context&#39;s <code>turnId</code> is populated with the same identity, forwarded from the host.</li><li>The finalizer is the single entry every terminal path funnels through and it requires an explicit <code>turnId</code>: a terminal event that carries none, or one whose turn no longer owns the session, settles nothing and announces nothing. The turn&#39;s record is keyed by <code>(sessionId, turnId)</code>, so a repeat terminal event joins the first claim instead of announcing twice.</li><li>The event needs no permission, has no ack and no replay, is not guaranteed across plugin crash, reload, or host quit, and does not prove that every in-flight tool of the turn has exited. No published host emits it yet: the release that ships it has not been published.</li><li>Decision D422 is recorded by ADR 0252.</li></ul><h2 id="_2026-09-15-—-remove-the-subagent-turn-limit-d423" tabindex="-1">2026-09-15 — Remove the subagent turn limit (D423) <a class="header-anchor" href="#_2026-09-15-—-remove-the-subagent-turn-limit-d423" aria-label="Permalink to &quot;2026-09-15 — Remove the subagent turn limit (D423)&quot;">​</a></h2><ul><li><code>maxTurns</code> was the last definition-level kill switch left after ADR 0166 withdrew the idle and duration watchdogs, and the parent cannot size it: it cannot see the delegate&#39;s live work, so it cannot tell a delegate that is one turn from converging from one that never will. The shipped backstops (60, 50, 40, 80) had no derivation, and the editor&#39;s own starting state was unlimited.</li><li>Decision D423 removes the field from <code>SubagentDefinition</code>, the frontmatter parser, <code>UserSubagentRecord</code> / <code>UserSubagentInput</code>, the host-core registry, the five built-in documents, <code>SUBAGENT_PRESETS</code>, and the Subagent editor&#39;s Advanced disclosure. A delegate ends only when it finishes, when the parent calls <code>TaskStop</code>, when the user Stops, or when a terminal parent error aborts it.</li><li>An existing document that declares <code>maxTurns</code> keeps loading: the key is now unrecognized frontmatter and is ignored exactly like any other unknown key, with no error, no warning, and no rewrite of the user&#39;s file. A definition that relied on the cap therefore loses it silently.</li><li>The <code>truncated</code> subagent status leaves the shared run-status union, the renderer outcome union, the <code>chat.subagentStatus</code> catalog entry in every locale, and the delegation topology&#39;s warnings count. <code>timed_out</code> stays in the type even though D328 withdrew the watchdogs that produced it.</li><li>No protocol version, schema version, or storage change: host-core&#39;s subagent input struct still ignores unknown fields, and the registry parses Markdown frontmatter rather than a table column.</li><li>See ADR 0253, <code>03-runtime/02-agent-runtime.md</code> §5f, <code>04-ux/06-settings-ia.md</code> §7, E2E-155, and E2E-SUBAGENT-legacy-turn-limit-frontmatter-is-ignored.</li></ul><h2 id="_2026-09-15-—-theme-assets-are-absolute-paths-d422" tabindex="-1">2026-09-15 — Theme assets are absolute paths (D422) <a class="header-anchor" href="#_2026-09-15-—-theme-assets-are-absolute-paths-d422" aria-label="Permalink to &quot;2026-09-15 — Theme assets are absolute paths (D422)&quot;">​</a></h2><ul><li>ADR 0248 required theme assets to be package-relative, which forced a plugin to copy a user-chosen image into its own package. A development plugin is watched recursively, so every copy reloaded the plugin and closed that plugin&#39;s panel window; the plugin&#39;s own data directory could not be referenced at all.</li><li><code>normalizeThemeAssetPath</code> (SDK) and <code>normalize_theme_asset_path</code> (host-core) now accept absolute paths only (<code>C:/art/bg.png</code>, <code>/art/bg.png</code>, or either spelled as a <code>file:</code> URL). Package-relative asset references stop resolving; themes that declared them are the only affected surface.</li><li><code>pi.themes.upsert</code> resolves asset references in the sheet and registers them in that plugin&#39;s asset map for as long as the plugin stays loaded, so a theme can pick up a new image without any file write inside the package.</li><li>Unchanged: the extension whitelist, the summed 4 MB budget for declared assets, <code>.</code>/<code>..</code> rejection, the <code>url()</code> → <code>plugin-asset://</code> rewrite, <code>nosniff</code>, <code>no-store</code>, and revocation on unload. Absolute keys are percent-encoded in the URL and decoded by the handler.</li><li>A plugin can now hand the renderer any local file it can read. Manifest-declared assets stay auditable in a static file; runtime-registered ones do not survive the plugin.</li><li>See ADR 0255, <code>07-plugins/02-plugin-manifest-schema.md</code>, <code>07-plugins/04-plugin-security.md</code>, and ADR 0248 §1 (superseded for path resolution).</li></ul><h2 id="_2026-09-15-—-deepseek-reasoning-across-compaction-d424" tabindex="-1">2026-09-15 — DeepSeek reasoning across compaction (D424) <a class="header-anchor" href="#_2026-09-15-—-deepseek-reasoning-across-compaction-d424" aria-label="Permalink to &quot;2026-09-15 — DeepSeek reasoning across compaction (D424)&quot;">​</a></h2><ul><li>OpenCode / third-party DeepSeek relays reject empty <code>reasoning_*</code> echoes after context compaction drops assistant thinking (#296). Official <code>deepseek.com</code> still accepts <code>&quot;&quot;</code> (#223 / D389).</li><li>Decision D424 amends D389 / ADR 0136: restore <code>thinkingSignature</code> on history rebuild; stash recent thinking in checkpoint <code>details.retainedReasoning</code> and replay it after the summary; set <code>requiresNonEmptyReasoningReplay</code> for non-official DeepSeek Completions rows so the pi-ai patch fills a documented placeholder instead of <code>&quot;&quot;</code>. See ADR 0256 and E2E-005E.</li></ul><h2 id="_2026-09-15-—-plugin-real-time-capabilities-d425" tabindex="-1">2026-09-15 — Plugin real-time capabilities (D425) <a class="header-anchor" href="#_2026-09-15-—-plugin-real-time-capabilities-d425" aria-label="Permalink to &quot;2026-09-15 — Plugin real-time capabilities (D425)&quot;">​</a></h2><ul><li>Plugins gain four host-mediated capabilities: background microphone capture, background playback, system-wide accelerators, and real-time bidirectional connections. The host owns every device and transport; plugins exchange typed frames and never receive a device handle, a raw socket, or a Node stream.</li><li>New permissions <code>audio.capture.background</code> (high), <code>audio.playback.background</code> (medium), <code>keyboard.globalShortcut</code> (medium), <code>net.websocket</code> (high), plus <code>contributes.globalShortcuts</code> (at most 8 accelerator → own-command entries) as the declarative form. <code>ui.microphone</code> keeps its panel-scoped meaning and is not widened.</li><li>Shortcut registration is refused rather than taken over: <code>SHORTCUT_CONFLICT</code>, <code>SHORTCUT_UNAVAILABLE</code>, <code>INVALID_ACCELERATOR</code>, <code>LIMIT_EXCEEDED</code>. An accelerator can only run one of the owning plugin&#39;s own commands — no key monitoring, no raw events, no hooks.</li><li>Implemented in this change: <code>keyboard.globalShortcut</code> and <code>net.websocket</code> (host-owned sockets, confined to <code>manifest.net.domains</code>, bounded, and released with the plugin). The two audio permissions are declared, present in the plugin API, and gated: with the grant the host has no device backend, so every call is answered with a coded <code>UNSUPPORTED</code> refusal that is audited, until the device service lands. See ADR 0257, <code>07-plugins/03-plugin-api.md</code>, <code>07-plugins/04-plugin-security.md</code>, and E2E-PLUGIN-global-shortcut-owns-only-its-own-command.</li></ul><h2 id="_2026-09-15-—-trusted-extension-custom-agents-d426" tabindex="-1">2026-09-15 — Trusted extension custom agents (D426) <a class="header-anchor" href="#_2026-09-15-—-trusted-extension-custom-agents-d426" aria-label="Permalink to &quot;2026-09-15 — Trusted extension custom agents (D426)&quot;">​</a></h2><p><strong>Add the PI-Desktop <code>registerAgent</code> ExtensionAPI contract for trusted extensions. A plugin registers bounded model metadata and a <code>stream</code> or <code>complete</code> callback; the plugin owns endpoint and authentication. The sidecar exposes a redacted model registry, assigns <code>extension-agent:&lt;encoded-agent-key&gt;</code> ids, and routes idle <code>setModel</code> through Electron main to the existing host-owned <code>session.configure</code> binding. The host never receives or persists plugin transport credentials, and <code>registerProvider</code> is a compatibility alias for the same plugin-owned stream shape. See ADR 0258, <code>07-plugins/16-trusted-extensions.md</code>, and E2E-TRUSTED-EXTENSION-custom-agent-stream-and-binding.</strong></p><p>The pinned upstream pi-coding-agent package has <code>registerProvider</code> but no <code>registerAgent</code>; the PI-Desktop member is therefore an explicit adapter contract, not an unreviewed upstream registry passthrough.</p><h2 id="_2026-09-15-—-plugin-declared-providers-are-host-owned-rows-d427" tabindex="-1">2026-09-15 — Plugin-declared providers are Host-owned rows (D427) <a class="header-anchor" href="#_2026-09-15-—-plugin-declared-providers-are-host-owned-rows-d427" aria-label="Permalink to &quot;2026-09-15 — Plugin-declared providers are Host-owned rows (D427)&quot;">​</a></h2><p><strong>A plugin may declare <code>contributes.providers</code>, and the Host materializes each entry as a provider row in the native Settings → Provider list, owned by that plugin (<code>providers.owner_plugin_id</code>, schema v17; row id <code>plugin:&lt;pluginId&gt;:&lt;declaredId&gt;</code>). The declaration is re-read on every load and is authoritative for its own fields, so a dropped entry is deleted together with both credential references; <code>providers.update</code> / <code>providers.delete</code> refuse a plugin-owned row with <code>PROVIDER_OWNED_BY_PLUGIN</code>. A non-empty declaration needs the new high-risk <code>provider.register</code> permission, and an <code>oauth</code> block or <code>authKind: &quot;oauth&quot;</code> is refused until a Host-owned login flow exists. See ADR 0259, <code>07-plugins/02-plugin-manifest-schema.md</code> §5.4, <code>07-plugins/13-plugin-permissions-matrix.md</code>, <code>03-runtime/04-data-storage.md</code> §4.3/§7, and E2E-PLUGIN-declared-provider-appears-in-the-native-provider-list.</strong></p><p>It deliberately does not include plugin OAuth: the <code>provider.oauth</code> permission and a Host-owned plugin login flow are future work, so a declared provider has no OAuth login, token refresh, or account label today.</p><h2 id="_2026-09-15-—-side-chats-materialize-on-first-send-421" tabindex="-1">2026-09-15 — Side chats materialize on first Send (#421) <a class="header-anchor" href="#_2026-09-15-—-side-chats-materialize-on-first-send-421" aria-label="Permalink to &quot;2026-09-15 — Side chats materialize on first Send (#421)&quot;">​</a></h2><p><em>(Retired by ADR 0268 on 2026-09-16: the feature and its surfaces were removed, and these IDs stay retired and must not be reused. The record is kept for history.)</em></p><p>Side-chat opening and selection quote prefill are renderer-only drafts. The first explicit nonempty Send creates the anchored child, with concurrent submissions deduplicated and failed drafts retained. Closing unsent drafts creates no history; existing children remain. See the <a href="./../../adr/message-quotes-and-side-chats">message quotes and side chats ADR</a> and E2E-CHAT-side-chat-fork.</p><h2 id="_2026-09-15-—-a-tooltip-never-outlives-its-trigger-d428" tabindex="-1">2026-09-15 — A tooltip never outlives its trigger (D428) <a class="header-anchor" href="#_2026-09-15-—-a-tooltip-never-outlives-its-trigger-d428" aria-label="Permalink to &quot;2026-09-15 — A tooltip never outlives its trigger (D428)&quot;">​</a></h2><ul><li><strong>The shared tooltip hook in <code>apps/desktop/src/components/ui.tsx</code> gains one lifecycle contract: at most one themed tooltip is painted at a time (claiming the slot closes the previous owner), and a visible tooltip closes when its anchor unmounts, when the window loses focus, when the document is hidden, on Escape, or when the pointer that revealed it leaves the anchor. Every close also cancels a show that has not painted yet, so Escape or a window blur inside the delay no longer paints a tooltip on an unfocused window. A trigger that moves in the DOM within a quarter second without being replaced keeps the tooltip instead of blinking it, and a tooltip revealed by keyboard focus is not closed by unrelated pointer movement. The guard listeners (window blur, visibility, Escape, pointer) are installed once for the whole renderer instead of once per trigger. No delay, offset, placement, or text behavior changes.</strong></li><li><strong>Row-level hover paint and hover-revealed row actions now follow the same &quot;still relevant?&quot; rule: an idle row&#39;s hidden overflow control is pointer-inert so the space it occupies belongs to the row — a click there opens the conversation, or activates and toggles the project group, as the title does — and a no-hover pointer gets the controls revealed under <code>@media (hover: none)</code> so it never meets a hidden target. A window that loses focus drops the row&#39;s and the project title&#39;s hover paint and hides the revealed actions instead of leaving them latched.</strong></li><li>Renderer only: no protocol, storage, host, permission, or migration change. See <code>04-ux/09-interaction-patterns.md</code> §6.4 and §9.1c, E2E-UI-tooltip-never-outlives-its-trigger, and E2E-UI-row-actions-do-not-swallow-the-row-click.</li></ul><p>Users reported a tooltip that occasionally never went away (the trigger unmounted, the window lost focus, or the pointer left the window without a leave event) and a session row whose hidden overflow control silently opened its menu instead of the conversation.</p><h2 id="_2026-09-15-—-turn-queue-priority-block-and-row-actions-d429" tabindex="-1">2026-09-15 — Turn-queue priority block and row actions (D429) <a class="header-anchor" href="#_2026-09-15-—-turn-queue-priority-block-and-row-actions-d429" aria-label="Permalink to &quot;2026-09-15 — Turn-queue priority block and row actions (D429)&quot;">​</a></h2><ul><li>&quot;Send now&quot; becomes a priority block instead of a head jump: a promoted entry takes <code>MAX(priority) + 1</code> inside its session (schema v18, nullable <code>priority</code>), so several promotions are delivered in the order they were clicked and the waiting queue keeps its own <code>position</code> order behind them. Promoting an entry that already carries a priority is <code>CONFLICT</code>, not another move.</li><li>The queued row gains move up, move down, and edit beside Send now and remove. Move up/down swaps a waiting entry with its adjacent waiting neighbour through <code>session.queueReorder</code> and never crosses the promoted block; editing removes the row and returns its captured draft (text plus inline file references) to the composer, and is refused while the input is non-empty.</li><li>A promoted row is locked until the Host delivers it: move up/down, edit, and remove are disabled, and Send now reads as already decided.</li><li>The promoted block is delivered as adjacent user messages: the first promoted entry starts the turn at the boundary and the rest are injected into that same turn through the steering channel, so the model answers once for the whole block instead of once per row. An injected entry&#39;s own turn is canceled; an entry the runtime refuses to accept stays queued and leaves at the next boundary as its own turn.</li><li>A settled turn is authoritative for the queue. A terminal event can be dropped (Main never forwards one that names a turn it no longer owns) or never emitted, and a turn left active inside the Host held its session&#39;s queue forever — the &quot;Send now, then Stop&quot; stall. The settlement now closes the turn, and a drain request that arrives while a pass is running is retried instead of dropped. See ADR 0265, <code>03-runtime/01-ipc-protocol.md</code> (§5.6), <code>03-runtime/04-data-storage.md</code>, <code>04-ux/08-component-spec.md</code> (§11), and E2E-QUEUE-promote-orders-delivery-by-click.</li></ul><h2 id="_2026-09-16-—-a-manual-disclosure-keeps-the-reader-s-place-d430" tabindex="-1">2026-09-16 — A manual disclosure keeps the reader&#39;s place (D430) <a class="header-anchor" href="#_2026-09-16-—-a-manual-disclosure-keeps-the-reader-s-place-d430" aria-label="Permalink to &quot;2026-09-16 — A manual disclosure keeps the reader&#39;s place (D430)&quot;">​</a></h2><ul><li><strong>A manual disclosure hands its own title to the scroller that owns it, synchronously, before the expansion state changes.</strong> The content <code>ResizeObserver</code> still re-pins a pinned transcript on every content resize (D287) and cannot tell a stream from a reader expanding a row, so the reader states their intent first: the transcript leaves follow mode and the scroller restores that title&#39;s viewport offset from inside the same observer, for as many frames as the height keeps changing (an activity group animates its <code>grid-template-rows</code>). <code>overflow-anchor: none</code> on <code>.thread-scroll</code> means this has to be explicit.</li><li><strong>The anchor is the title element&#39;s offset from the scroller&#39;s top edge, not a <code>scrollTop</code>, and the browser&#39;s own boundary clamping is adopted as the new anchor.</strong> A correction the content cannot reach, and a height change above the title, are both handled without fighting the browser frame after frame. There is no delayed &quot;take the bottom back&quot; compensation: the hold ends on real scroll input, the jump-to-latest control, a new turn, or a navigation.</li><li><strong>Every scroll owner holds its own position and passes the hold outward.</strong> The transcript scroller and the nested delegate dock (<code>useFollowScroll</code>, D302) share one controller; growing the dock grows the transcript&#39;s content, so an outer scroller still in follow mode would drag the same title away.</li><li><strong>Input is attributed to the scroller that can consume it.</strong> Only input that really moves a container may release follow or a held position: a press on a row, a control or an editable field is an ordinary click, a keystroke in a text field belongs to the field, and input a nested scroller consumes is not the outer scroller&#39;s gesture. Arrow keys still scroll and Space still activates a focused title.</li><li><strong>Programmatic immediate scrolling records the <code>scrollTop</code> the scroller actually reached</strong>, and a sub-pixel tolerance is applied only to scroll events no gesture produced. At DPR 1.1 follow asked for 841 and got 840.909; compared strictly that fraction read as the reader scrolling up and dropped follow mode. Real gestures are still compared exactly, so a one-pixel scroll unpins.</li><li>Renderer only: no protocol, storage, host, permission, or migration change. See <code>04-ux/09-interaction-patterns.md</code> §9.1 and E2E-CHAT-disclosure-toggle-keeps-reading-position.</li></ul><p>Readers reported that clicking a tool, thinking or activity title to expand it dragged the whole transcript up by the height of the opened detail, in a session that was sitting at the bottom — including after the turn had finished.</p><h2 id="_2026-09-16-—-the-delete-dialog-not-the-menu-owns-the-running-task-refusal-360-d431" tabindex="-1">2026-09-16 — The delete dialog, not the menu, owns the running-task refusal (#360, D431) <a class="header-anchor" href="#_2026-09-16-—-the-delete-dialog-not-the-menu-owns-the-running-task-refusal-360-d431" aria-label="Permalink to &quot;2026-09-16 — The delete dialog, not the menu, owns the running-task refusal (#360, D431)&quot;">​</a></h2><ul><li>Amends the renderer half of D421. The sidebar menu and the Projects index menu refused Delete project with a transient <code>project.deleteRunningBlocked</code> warning whenever any of the project&#39;s sessions was running, and returned before <code>ProjectDeleteDialog</code> was ever mounted. The refusal vanished with the toast and left no path forward, so a project with one live task could not be deleted at all — which is how #360 (&quot;项目管理中无法真正删除项目&quot;) reads.</li><li>Both menus now always open the dialog. Each surface passes the project&#39;s live running session ids (<code>runningSessions[session.id]</code>, over the rows it already matches: <code>entry.sessions</code> in the sidebar, <code>sessionMatchesIndexProject</code> in the index), and the dialog derives its copy from that prop on every render, so a turn that starts or finishes while the dialog is open is reflected before the user confirms.</li><li>The dialog adds a warning line naming <code>${ssrInterpolate(_ctx.count)}</code> running sessions and swaps its confirm label to <code>project.deleteRunningConfirm</code> (&quot;Stop tasks and delete&quot;). Confirming aborts exactly those sessions and only then calls <code>deleteProject</code>, so removing a running turn stays a second, explicit confirmation of a stated consequence. Cancelling removes nothing.</li><li>The host guard is unchanged: <code>projects.remove</code> still refuses with 1008 / <code>CONFLICT</code> while an attached session has a running turn, and the dialog still maps that refusal to <code>project.deleteRunningBlocked</code>. A turn that starts after the abort loop is what that fallback covers.</li><li>Renderer only: no protocol, storage, host, permission, or migration change, and no new default. <code>project.deleteRunningBlocked</code> keeps its meaning, copy, and every translation. See ADR 0251, D421, and E2E-PROJECT-delete-running-sessions-are-named-and-stopped.</li></ul><h2 id="_2026-09-16-—-plugin-workspace-fs-roots-follow-the-calling-session-d432" tabindex="-1">2026-09-16 — Plugin <code>workspace</code> fs roots follow the calling session (D432) <a class="header-anchor" href="#_2026-09-16-—-plugin-workspace-fs-roots-follow-the-calling-session-d432" aria-label="Permalink to &quot;2026-09-16 — Plugin \`workspace\` fs roots follow the calling session (D432)&quot;">​</a></h2><ul><li>Every plugin <code>pi.fs.*</code> call whose mode root is <code>workspace</code> resolved the one window-global visible workspace, so a plugin agent tool invoked from session B (project B) read project A&#39;s root as soon as the user switched tabs, and failed <code>NOT_FOUND</code> for every session at once when no workspace was visible (D093 fixed the same rule for built-in tool execution; it never reached a plugin).</li><li><code>plugin-runtime.ts</code> now resolves that root with a new private <code>fsRoot(loaded, rule)</code> helper: the project of the tool session that invoked the call, asked for through the additive host service <code>getWorkspacePathForSession</code>. <code>plugin-services.ts</code> wires it from the session-to-project map it already keeps, so no new source of truth is introduced.</li><li>Unchanged: the visible workspace stays the root for a panel-bridge call, which has no tool session, and for a session the host does not track; a <code>userSelected</code> mode still keeps the directory the user picked through <code>requestDirectory()</code>.</li><li>A session root now resolves when no workspace is visible at all, so a temporary chat keeps working per session instead of failing for every session at once. A panel call in that state still fails closed. <code>NOT_FOUND</code> for a <code>workspace</code> root is therefore narrower: neither the invoking session&#39;s project nor a visible workspace resolved.</li><li>No permission, realpath-containment, deny-list, or declared-scope/consent gate changes, and the plugin API surface does not change: <code>pi.workspace.get</code> still answers with the visible workspace and its project group.</li><li>See ADR 0266, <code>07-plugins/03-plugin-api.md</code> §3, <code>07-plugins/13-plugin-permissions-matrix.md</code> §6, and E2E-PLUGIN-fs-root-follows-the-calling-session.</li></ul><h2 id="_2026-09-16-—-quotes-annotations-and-side-chats-are-removed-adr-0268" tabindex="-1">2026-09-16 — Quotes, annotations, and side chats are removed (ADR 0268) <a class="header-anchor" href="#_2026-09-16-—-quotes-annotations-and-side-chats-are-removed-adr-0268" aria-label="Permalink to &quot;2026-09-16 — Quotes, annotations, and side chats are removed (ADR 0268)&quot;">​</a></h2><ul><li>Quote, the text-selection overlay, response annotations (comment editor, floating index, source badges, composer attachment), and renderer-owned side chats are removed from every surface. Message action rows keep Copy, Edit, Delete, Fork, Retry, and Regenerate; the composer keeps its draft, chips, smart Stop, and per-session draft slots; the work panel keeps its other tab kinds; <code>session.fork</code> and the Fork action are unchanged.</li><li>Sends no longer compose a <code># Response annotations:</code> block, and the display-side reduction <code>requestTextWithoutAnnotations</code> is gone, so a prompt stored before the removal is shown exactly as it was stored in the transcript, the edit seed, and the queue preview.</li><li>Retired and never reusable: <code>D-LOCAL-message-quotes</code>, <code>D-LOCAL-selection-overlay</code>, <code>D-LOCAL-response-annotations</code>, <code>D-native-sidechat-stream</code>, and the E2E IDs E2E-CHAT-quote-prefill, E2E-CHAT-selection-markdown, E2E-CHAT-selection-side-chat, E2E-CHAT-annotation-attachments, E2E-CHAT-annotation-session-state, E2E-CHAT-annotation-source-index, E2E-CHAT-annotation-ack-and-steering, E2E-CHAT-side-chat-fork, E2E-CHAT-side-chat-stream, E2E-CHAT-side-chat-add-to-main, E2E-CHAT-side-chat-promote, E2E-CHAT-side-chat-close, and E2E-SESSION-native-side-chat-fork-survives-close.</li><li>No host protocol (v11), storage schema (v15), IPC channel, permission, or Rust change. See ADR 0268.</li></ul><h2 id="_2026-09-16-—-a-control-with-no-label-states-its-square-geometry" tabindex="-1">2026-09-16 — A control with no label states its square geometry <a class="header-anchor" href="#_2026-09-16-—-a-control-with-no-label-states-its-square-geometry" aria-label="Permalink to &quot;2026-09-16 — A control with no label states its square geometry&quot;">​</a></h2><ul><li><code>.icon-btn</code> is shared with label-driven pills, so it takes its width from its content — glyph plus 8px of side padding. Every icon-only use of it was therefore taller than it was wide: the sidebar collapse control and the composer&#39;s add button rendered 31×28, the composer&#39;s enhance and undo controls 35×28 inside <code>.composer-right</code>, and the settings provider actions 30×28 from their 14px glyphs. The same control in the conversation topbar was already a 28×28 square, so one function had two sizes.</li><li>Those controls now state <code>.icon-btn-square</code>, which pins both axes to the new <code>--ds-control-size</code> (28px) token, keeps <code>flex: 0 0</code> so a crowded toolbar row cannot shrink the control back out of square, and drops the side padding that under the global <code>border-box</code> would leave a 12px content box for a 15px glyph. Icon-only controls now match the square the topbar toggle, the work-panel actions, and the send/stop controls already used.</li><li><code>.composer-right .icon-btn</code> no longer sets inline padding. The label-driven model/thinking chip sets its own padding with <code>!important</code>, so that declaration only ever widened the icon-only controls beside it; the maximizing enhance control keeps its label geometry while it is loading.</li><li>Renderer only: no protocol, storage, host, permission, or migration change. See <code>04-ux/07-ui-design-system.md</code> §11.1 and <code>04-ux/08-component-spec.md</code> §3.7.</li></ul><h2 id="_2026-09-16-—-one-geometry-for-every-chrome-icon-control" tabindex="-1">2026-09-16 — One geometry for every chrome icon control <a class="header-anchor" href="#_2026-09-16-—-one-geometry-for-every-chrome-icon-control" aria-label="Permalink to &quot;2026-09-16 — One geometry for every chrome icon control&quot;">​</a></h2><ul><li>The preview- and route-band lane actions (<code>.title-nav-btn</code>) rendered as 22px tiles with <code>--radius-2xs</code> and a <code>--ds-tile</code> seat, beside 28px siblings: the same dock toggle in the conversation topbar, the viewport-fixed work-panel toggle, and the work-panel&#39;s own actions. One control, two sizes and two shapes, in the same 46px band.</li><li>Those actions now take the shared chrome-control geometry (the <code>.ct-icon-btn</code> group): a 28px square from <code>--ds-work-panel-toggle-size</code>, <code>flex: 0 0</code> so a crowded band cannot squeeze them out of square, <code>--radius-md</code>, a transparent seat with the semantic hover wash, and the 15px glyph every other chrome control uses. <code>.title-nav-btn</code> keeps only its press feedback and active tone.</li><li>The preview action lane is derived from the control it reserves room for: <code>--ds-preview-action-lane-width: calc(2 * var(--ds-work-panel-toggle-size) + 4px + 8px)</code>, so the panel header&#39;s reserve and the buttons cannot drift apart.</li><li>Renderer only: no protocol, storage, host, permission, or migration change. See <code>04-ux/08-component-spec.md</code> §2.</li></ul><h2 id="_2026-09-16-—-the-macos-traffic-light-reserve-has-one-source-d433" tabindex="-1">2026-09-16 — The macOS traffic-light reserve has one source (D433) <a class="header-anchor" href="#_2026-09-16-—-the-macos-traffic-light-reserve-has-one-source-d433" aria-label="Permalink to &quot;2026-09-16 — The macOS traffic-light reserve has one source (D433)&quot;">​</a></h2><ul><li>The renderer&#39;s windowed lead-in for the macOS traffic lights was a hand-copied <code>76px</code>: five declarations across <code>apps/desktop/src/styles/chrome.css</code> and <code>apps/desktop/src/styles/work-panel.css</code> — <code>.main-titlebar-left</code> (<code>64px</code>, that number minus the row&#39;s own 12px padding), <code>.conversation-topbar.ct-collapsed</code> (<code>--ct-lead-inset</code>), <code>.sidebar-header</code>, <code>.window-chrome-row:not(.sidebar-expanded)</code>, and the preview <code>.work-panel-header</code> (<code>calc(76px + var(--ds-preview-action-lane-width))</code>) — each with its own <code>[data-fullscreen=&quot;true&quot;]</code> sibling. None of them was tied to the <code>trafficLightPosition</code> the main process hands Electron, so either side changing on its own would have drifted from the other.</li><li>The native geometry now has one home, <code>packages/shared/src/window-chrome.ts</code>: <code>MAC_TRAFFIC_LIGHT_POSITION</code> (<code>{x:16,y:16}</code>) and <code>MAC_TRAFFIC_LIGHT_CLUSTER_WIDTH_DIP</code> (<code>60</code>), with <code>MAC_TRAFFIC_LIGHT_EDGE_DIP</code> (<code>16 + 60 = 76</code>) as the cluster&#39;s right edge. The main process positions the buttons from those constants, and the renderer injects the edge into <code>--ds-traffic-light-edge</code> at boot. Every consumer reads <code>--ds-window-lead-inset</code>, which <code>chrome.css</code> composes once for <code>:root[data-platform=&quot;darwin&quot;]</code> as <code>--ds-traffic-light-edge</code> + <code>--ds-traffic-light-gap</code> (<code>12px</code>) = <code>88px</code>.</li><li>The buttons used to sit flush against the green light, with a 0 gap. The reserve now keeps <code>12px</code> of breathing room between the cluster&#39;s right edge and the first shell control, which is why windowed mode moved from <code>76px</code> to <code>88px</code>.</li><li>The per-selector <code>[data-fullscreen=&quot;true&quot;]</code> overrides are gone. Fullscreen hides the buttons, and the same token&#39;s <code>[data-fullscreen=&quot;true&quot;]</code> rule drops the reserve to the ordinary <code>8px</code> gutter for every consumer at once.</li><li>Windows and Linux are unchanged: <code>--ds-window-lead-inset</code> keeps its <code>0px</code> default and those platforms still use the renderer-drawn controls in the 120px right-side band. Plugin-panel windows create a plain frameless <code>BrowserWindow</code> with no native traffic lights, so they are unaffected.</li><li>Renderer + shared constant only: no protocol, storage, host, permission, or migration change, and no new default.</li><li>See <code>04-ux/08-component-spec.md</code>, <code>04-ux/09-interaction-patterns.md</code>, and E2E-LAYOUT-three-column-width-priority.</li></ul><h2 id="_2026-09-16-—-the-work-panel-header-s-buttons-are-one-group" tabindex="-1">2026-09-16 — The work-panel header&#39;s buttons are one group <a class="header-anchor" href="#_2026-09-16-—-the-work-panel-header-s-buttons-are-one-group" aria-label="Permalink to &quot;2026-09-16 — The work-panel header&#39;s buttons are one group&quot;">​</a></h2><ul><li>The work-panel header read as two clusters: the action group inside the header (<code>+</code> and maximize, 4px apart) and the viewport-fixed collapse toggle outside it. The separator between them was compounded from four declarations on <code>.work-panel-actions</code> — <code>margin-right: 8px</code>, <code>padding-right: 8px</code>, a 1px <code>--ds-border-subtle</code> divider, and a separate <code>--ds-work-panel-toggle-gap: 20px</code> lane token — for a real 37px gap plus a hairline rule, so the same three 28px squares in the same 46px row looked like two unrelated clusters.</li><li>One token now spaces the whole row. <code>--ds-work-panel-control-gap: 4px</code> replaces <code>--ds-work-panel-toggle-gap</code> and is the header&#39;s flex gap (tab strip to action group), the <code>.work-panel-actions</code> gap (<code>+</code> to maximize), and — spent by the header&#39;s right padding as <code>calc(size + inset + control-gap)</code>, 44px — the gap from the action group to the fixed collapse toggle. <code>.work-panel-actions</code> states spacing only: the divider, inset, and margin are gone.</li><li>The lane still reserves the whole toggle, and the <code>+</code> trigger still clears the toggle by 36px inside the lane (4px + the 28px maximize control + 4px), so the <code>WORK_PANEL_HEADER_PROBE</code> 24px assertion and E2E-152&#39;s hit-region contract still hold.</li><li>Renderer only: no protocol, storage, host, permission, or migration change, and no new default. See <code>04-ux/08-component-spec.md</code> §5.2, <code>04-ux/07-ui-design-system.md</code> §4.3, and E2E-LAYOUT-three-column-width-priority.</li></ul><h2 id="_2026-09-16-—-the-work-panel-controls-are-quiet-icons-not-filled-seats" tabindex="-1">2026-09-16 — The work-panel controls are quiet icons, not filled seats <a class="header-anchor" href="#_2026-09-16-—-the-work-panel-controls-are-quiet-icons-not-filled-seats" aria-label="Permalink to &quot;2026-09-16 — The work-panel controls are quiet icons, not filled seats&quot;">​</a></h2><ul><li>The work-panel header&#39;s <code>+</code> and maximize controls were painted with a <code>--ds-tile</code> seat of their own, and the viewport-fixed collapse toggle added <code>--ds-tile-deep</code> plus <code>--ds-raised-shadow</code> while <code>aria-pressed=&quot;true&quot;</code>. In the light theme that turned a row of three 28px controls into filled squares, with the toggle reading as a raised circular pill floating over the panel header — nothing like the top-left dock toggle and new-task controls beside them.</li><li><code>+</code> and maximize now join the shared chrome-control group in <code>chrome.css</code> — the same rule that seats the topbar&#39;s dock toggle, the viewport-fixed panel toggle, and the preview/route-band lane actions — so they take one geometry (28px square, <code>--radius-md</code>, <code>flex: 0 0</code>), one transparent seat, the semantic <code>--ds-bg-hover</code> wash on pointer, and the <code>opacity: 0.4</code> disabled state. Their duplicated rules in <code>work-panel.css</code> are gone; the partial keeps only the action group&#39;s spacing.</li><li>The toggle&#39;s pressed state drops the fill and the raised shadow and keeps the engaged ink, so the open state is the glyph swap plus <code>--ds-text-primary</code>. <code>aria-pressed</code> still carries the state to assistive technology, and <code>.app-work-panel-toggle</code> keeps its viewport-fixed ownership and <code>no-drag</code> carve-out.</li><li>Keyboard focus is unchanged: the global <code>:focus-visible</code> outline applies as it does to every other chrome icon control, so removing the retired <code>:focus-visible</code> background wash costs no focus affordance.</li><li>Renderer only: no geometry, gap, inset, protocol, storage, host, permission, or migration change, and no new token. See <code>04-ux/08-component-spec.md</code> §2.3 and §5.2, <code>04-ux/07-ui-design-system.md</code> §4.3, and E2E-LAYOUT-three-column-width-priority.</li></ul><h2 id="_2026-09-17-—-builtin-subagents-can-be-switched-off-d434" tabindex="-1">2026-09-17 — Builtin subagents can be switched off (D434) <a class="header-anchor" href="#_2026-09-17-—-builtin-subagents-can-be-switched-off-d434" aria-label="Permalink to &quot;2026-09-17 — Builtin subagents can be switched off (D434)&quot;">​</a></h2><ul><li>Settings &gt; Agent &gt; Subagents listed the five shipped defaults as read-only rows while the user&#39;s own documents had an enablement switch, so one delegate could not be turned off at all: copying a builtin and disabling the copy left the shipped definition in the catalog, because disabled user documents are filtered out before the loader merges and so shadowed nothing.</li><li>Every Built-in row now carries the same switch. The handle is stored in <code>&lt;data&gt;/agent-capabilities/subagent-builtins.json</code>, a file of its own because the user-document scan prunes state for ids it can never see, and host-core exposes it through <code>agents.disabledBuiltins</code> / <code>agents.setBuiltinEnabled</code>. Electron main hands the disabled handles to <code>loadSubagentDefinitions</code>, which drops them from the delegation catalog while <code>subagent/catalog</code> still returns the builtin, flagged <code>enabled: false</code>, in its new <code>builtins</code> list.</li><li>A switched-off builtin keeps its row, dimmed, so the switch is the way back on. A user document of the same handle keeps working and keeps shadowing the shipped definition, reveal and delete stay absent because a builtin is not a file, and an unavailable host contributes no exclusions. See ADR 0270, <code>04-ux/06-settings-ia.md</code> §2, <code>03-runtime/01-ipc-protocol.md</code> §12c, and E2E-SUBAGENT-settings-lists-builtin-defaults.</li></ul><h2 id="_2026-09-17-—-judge-a-public-network-address-on-the-route-the-request-will-dial-d436" tabindex="-1">2026-09-17 — Judge a public-network address on the route the request will dial (D436) <a class="header-anchor" href="#_2026-09-17-—-judge-a-public-network-address-on-the-route-the-request-will-dial-d436" aria-label="Permalink to &quot;2026-09-17 — Judge a public-network address on the route the request will dial (D436)&quot;">​</a></h2><ul><li>The skill market&#39;s main-process guard classified the target host with a <em>local</em> resolver while <code>net.fetch</code> dials through Chromium&#39;s proxy stack (ADR 0177). Under a TUN / fake-IP resolver that answer is a synthesized <code>198.18.0.0/15</code> address for a connection the app never makes, so every catalog source was refused as &quot;blocked by the app&#39;s address check&quot; (issue #419).</li><li>Each hop now asks the session that carries <code>net.fetch</code> (<code>Session.resolveProxy</code>) for its own decision, and the shared <code>classifyProxyRoute</code> reduces that answer to <code>proxied</code> / <code>direct</code> / <code>unknown</code>. On a <code>proxied</code> route <code>isAcceptableResolvedAddress</code> tolerates only the resolver-artifact class (<code>benchmark</code>); every real internal class and an unanswered resolver still refuse. A <code>direct</code> route keeps the pre-change semantics by default; an explicit <code>allowFakeIp</code> setting may permit only the benchmark placeholder. A <code>DIRECT</code> entry anywhere in the list is read as <code>unknown</code> because Chromium may fall back to it, and <code>unknown</code> stays strict.</li><li>Refusals and the market&#39;s <code>failureDetails</code> now carry <code>route</code>, so a fake-IP refusal on a direct route reads apart from one on an unreadable route.</li><li>The MCP market now asks the Electron session for its route per hop: fully proxied hops use <code>net.fetch</code> so fake-IP sources can reach the configured proxy, while direct and unknown hops retain the pinned Node HTTPS guard and strict public-address rule by default. The explicit <code>allowFakeIp</code> setting permits only benchmark placeholders for transparent router/TUN deployments; real private answers remain refused. See ADR 0245.</li><li>See ADR 0272, <code>05-security/01-security.md</code> §4.1, <code>03-runtime/09-logging-and-observability.md</code>, and <code>06-delivery/04-e2e-test-plan.md</code> E2E-SKILL-MARKET-NET-BOUNDARY.</li></ul><h2 id="_2026-09-17-—-the-dock-question-card-is-a-composer-plate-360-d437" tabindex="-1">2026-09-17 — The dock question card is a composer plate (#360, D437) <a class="header-anchor" href="#_2026-09-17-—-the-dock-question-card-is-a-composer-plate-360-d437" aria-label="Permalink to &quot;2026-09-17 — The dock question card is a composer plate (#360, D437)&quot;">​</a></h2><ul><li>The asktool question card is mounted in the transparent composer dock (<code>Composer.tsx</code> renders it as a direct <code>.composer-stack</code> child, and the transcript mount is gone), yet it still painted the in-flow <code>--ds-tile</code> wash: a 3.5% ink mix with no shadow. On the light page that leaves a <code>#f7f7f7</code> panel with white option rows next to the composer plate below it, which is how #360 item 1 (&quot;选择交互面板缺少背景色&quot;) reads. The dock rule&#39;s own comment already claimed the card &quot;uses the same decision surface as Plan and Goal approval&quot;, while <code>.plan-approval-bar</code> in that exact slot paints <code>--ds-bg-composer</code> with <code>--ds-shadow-composer</code> (<code>04-ux/03-permission-ux.md</code> §9, <code>04-ux/08-component-spec.md</code> §11.5).</li><li><code>.composer-stack &gt; .asktool-card</code> now paints that plate: <code>--ds-bg-composer</code> with <code>--ds-shadow-composer</code> (light <code>#ffffff</code>, dark <code>color-mix(in oklab, #212121 96%, transparent)</code> with the composer shadow).</li><li>Its controls move to the plate&#39;s inlaid tier, the layer <code>.plan-approval-split</code> already uses on that same plate: <code>.asktool-option</code> and <code>.asktool-custom-input</code> drop <code>--ds-raised</code> and <code>--ds-raised-shadow</code> for <code>--ds-tile-deep</code>, and the hover/selected mix is rebased on <code>--ds-tile-deep</code>. Without that flip the rows would be white on white in the light palette, where <code>--ds-raised</code> and <code>--ds-bg-composer</code> are both <code>#ffffff</code>. The 15 px option mark keeps <code>--ds-tile-deep</code> — the sidebar checkbox&#39;s mark — which still reads against the inlaid row exactly as it does against the sidebar.</li><li>Renderer only: no protocol, storage, host, permission, migration, or preference change, and no new default. Both layers are existing tokens in both palettes, so a contributed theme moves the plate and the rows with <code>--ds-bg-composer</code> / <code>--ds-tile-deep</code>. See <code>04-ux/11-asktool-question-card.md</code>, <code>04-ux/07-ui-design-system.md</code> §6.4, and E2E-078.</li></ul><h2 id="_2026-09-17-—-a-git-checkout-is-a-create-project-source-d438" tabindex="-1">2026-09-17 — A git checkout is a Create project source (D438) <a class="header-anchor" href="#_2026-09-17-—-a-git-checkout-is-a-create-project-source-d438" aria-label="Permalink to &quot;2026-09-17 — A git checkout is a Create project source (D438)&quot;">​</a></h2><ul><li>The Create project dialog only collected a project name and local folders (ADR 0233), and the home switcher&#39;s Clone git project entry renders inside the hero of a project-bound session. A fresh installation therefore had to open an unrelated local folder before any repository could be cloned.</li><li>The dialog now owns a source selector with two equal peers: This computer and Git repository. The git source keeps the one name field (seeded from the repository name until the user types), adds a repository URL field and one clone destination row, and parses URLs with the switcher&#39;s <code>parseGitCloneUrl</code> rules, so private, loopback, link-local, credential-bearing, and malformed remotes keep Create disabled (ADR 0247).</li><li>Main exposes additive <code>project/cloneCheckout({ url, parentPath })</code>: it clones into the explicit parent folder and returns <code>{ path, name }</code> without touching the active workspace and without opening a picker. <code>project/clone</code> keeps its native-picker behavior for the home switcher.</li><li>Project creation is unchanged: both sources call one project-slice helper, <code>project-group/create</code> still writes the only durable record, and a checkout becomes the primary root of the same logical group (ADR 0233).</li><li>Renderer plus one narrow main-process capability: no protocol, schema, host RPC, permission, storage, or preference change. See ADR 0273, <code>03-runtime/01-ipc-protocol.md</code> §9, <code>04-ux/08-component-spec.md</code>, and E2E-258.</li></ul><h2 id="_2026-09-16-—-draggable-chat-content-width-d439" tabindex="-1">2026-09-16 — Draggable chat content width (D439) <a class="header-anchor" href="#_2026-09-16-—-draggable-chat-content-width-d439" aria-label="Permalink to &quot;2026-09-16 — Draggable chat content width (D439)&quot;">​</a></h2><ul><li><p>The centered conversation band, empty-home stack, and composer share one preferred max width, default 760px, persisted as <code>AppSettings.chatContentMaxWidth</code>. Dual edge handles (invisible at rest; a short primary-ink capsule on hover, slightly stronger while dragging) change that width together so the column stays centered. Drag floor is 560px.</p></li><li><p>The live used width is <code>min(available pane minus 24px gutters, preferred)</code>, so a squeezed sidebar or work panel compresses the band without rewriting the preference. The collapsed-sidebar 640px ceiling is removed.</p></li><li><p>Assistant, tool, and decision rows follow the band. User plates stay <code>min(82%, 600px)</code>. Renderer only. See ADR 0277, E2E-208, and E2E-CHAT-content-width-handles.</p></li></ul><h2 id="_2026-09-17-—-session-and-project-rows-delete-on-the-second-click-d441" tabindex="-1">2026-09-17 — Session and project rows delete on the second click (D441) <a class="header-anchor" href="#_2026-09-17-—-session-and-project-rows-delete-on-the-second-click-d441" aria-label="Permalink to &quot;2026-09-17 — Session and project rows delete on the second click (D441)&quot;">​</a></h2><ul><li>The sidebar&#39;s session item and both project menus (sidebar and Projects index) removed the row on the first click of Delete. Every other destructive row action — the settings provider rows, vendor accounts, and the capability tables — arms first and relabels the control, so the two most destructive actions were the only ones that fired on a single click.</li><li><code>hooks/use-armed-delete.ts</code> now owns that pattern for the whole renderer: one <code>ARMED_DELETE_MS</code> (3200ms) expiry and one <code>useArmedDelete()</code>. The capability pages keep importing it from <code>AgentCapabilityLayout</code>, which re-exports the shared hook instead of holding a second copy with a timeout of its own, so the pattern cannot drift between the settings rows and the two menus.</li><li>A session item arms on <code>session.id</code>; both project menus arm on a <code>project:</code>-prefixed key, so a session and a project can never share an arm. An armed item carries <code>data-armed=&quot;true&quot;</code>, a danger wash, and the label <code>nav.deleteTaskConfirm</code> / <code>project.deleteMenuConfirm</code> (&quot;Delete?&quot; / &quot;确认删除？&quot;) in all eight catalogs. The menu stays open between the two clicks, and an outside press, Escape, or the expiry clears the arm without removing anything.</li><li>Amends D431 for the idle case: a project whose turn is still live still opens <code>ProjectDeleteDialog</code>, which names those sessions and stops them before it deletes, and the host&#39;s 1008 / <code>CONFLICT</code> refusal is still mapped to <code>project.deleteRunningBlocked</code> on both paths. A project with no live turn is removed by the same store action the dialog used, with the same success toast, so its second click is the confirmation the user already gave.</li><li>The number is D441 because D438 was already claimed twice — by the window-toggle merge (#360, D438 with the D439 rebind) and by the Git-checkout decision (ADR 0273), which this log flags to move to D440. This entry does not rewrite another decision&#39;s text.</li><li>Renderer only: no protocol, storage, host, permission, or migration change, and no new default. The folder on disk is still never touched. See <code>04-ux/09-interaction-patterns.md</code>, D421, D431, <code>06-delivery/04-e2e-test-plan.md</code> E2E-PROJECT-delete-removes-project-and-owned-sessions, and <code>apps/desktop/test/two-step-delete.test.mjs</code>.</li></ul><h2 id="_2026-09-16-—-the-sidebar-list-rhythm-is-1px-2px-8px" tabindex="-1">2026-09-16 — The sidebar list rhythm is 1px / 2px / 8px <a class="header-anchor" href="#_2026-09-16-—-the-sidebar-list-rhythm-is-1px-2px-8px" aria-label="Permalink to &quot;2026-09-16 — The sidebar list rhythm is 1px / 2px / 8px&quot;">​</a></h2><ul><li>The sidebar&#39;s session lists had no rhythm to read. Rows and group headers sat flush inside a group (a 0 gap), two project groups sat 2px apart, and the only air was the 4px above a section label. Adjacent 28px rows painted rounded hover fills that touched, so two rows read as one block, a collapsed group sat as far from its neighbour as an expanded group with ten rows under it, and a section label was closer to the rows above it than to the rows it introduces.</li><li>The lists now state one ladder: rows sit 1px apart, a project group header and a section label sit 2px above their first row, a sidebar section sits 8px below the one above it, and an expanded project group carries an 8px tail.</li><li>That tail belongs to the expanded group itself, so the spacing is decided by the preceding group alone: an expanded group is followed by 8px whether the next group is expanded or collapsed, and a collapsed group by 1px either way. The scroller&#39;s own gap stays a uniform 1px and the difference lives in the group body&#39;s inset, which leaves with the rows. Collapsing a group therefore cannot fork the spacing on a neighbour&#39;s state, and cannot snap its own tail.</li><li>The dated labels inside a project group are ordinary rows of that rhythm rather than a second level of groups: they keep no state, no disclosure, and no <code>aria-expanded</code>, and their own inset becomes a symmetric 4px instead of 6px above and 2px below, so a label no longer outweighs the 1px row gap it sits in.</li><li><code>space-0.25</code> (1px) joins the spacing scale as the hairline step for dense list rhythms; the settings rail already used 1px between its navigation items. The section-gap rule no longer claims the sidebar, which has its own rhythm.</li><li>Two row budgets follow the pitch: the pinned body still shows eight rows (233px, was 224px) and the standalone Sessions body still shows five (146px, was 140px), because the 28px row grid no longer fits its own gaps inside the old numbers.</li><li>Renderer only: no protocol, storage, host, permission, or migration change. See <code>04-ux/07-ui-design-system.md</code> §6.1 and §13, and <code>04-ux/08-component-spec.md</code> §6.2 and §6.6.</li></ul><h2 id="_2026-09-16-—-the-sidebar-project-group-folds-as-one-grid-row" tabindex="-1">2026-09-16 — The sidebar project group folds as one grid row <a class="header-anchor" href="#_2026-09-16-—-the-sidebar-project-group-folds-as-one-grid-row" aria-label="Permalink to &quot;2026-09-16 — The sidebar project group folds as one grid row&quot;">​</a></h2><ul><li>The rhythm above shipped with the group fold animating <code>max-height: 2000px → 0</code> over the 300ms slow duration, plus a separate 200ms opacity fade and a padding transition. A <code>max-height</code> curve spends most of its length above the content, so the visible move was a short snap at the end, and the shorter opacity curve emptied the group roughly two thirds of the way through: the rows disappeared first and the empty box closed afterwards. Collapsing a project read as two separate animations.</li><li>The group body is now a one-row grid whose row animates <code>1fr</code> → <code>0fr</code> over the 200ms normal duration. Every frame is a real fraction of the group&#39;s measured height, so the fold is a single continuous motion, and <code>opacity</code> stays 1 for the whole fold — the rows leave by being clipped, not by fading.</li><li>The body is three layers: the grid, a clip with <code>min-height: 0</code> and <code>overflow: hidden</code>, and the list that owns the 1px row gap and the fixed 2px / 7px inset. The inset has to sit inside the clip, because vertical padding on the animating box holds a <code>0fr</code> row open and leaves the group&#39;s tail behind after its rows are gone.</li><li>A folded group keeps its rows mounted inside the <code>0fr</code> row, so it is <code>inert</code> as well as <code>aria-hidden</code> and leaves the tab order. Under <code>prefers-reduced-motion: reduce</code> the fold keeps both endpoints and runs in a near-zero duration.</li><li>The scroll budgets, the indent, the pinned and standalone lists, and the shared <code>.sidebar-session-group-body</code> flex column are unchanged; only the project body folds as a grid.</li><li>Renderer only: no protocol, storage, host, permission, or migration change. See <code>04-ux/08-component-spec.md</code> §6.2 and <code>06-delivery/04-e2e-test-plan.md</code> E2E-LAYOUT-sidebar-project-group-fold.</li></ul><h2 id="_2026-09-17-—-sidebar-selection-is-distinct-from-workspace-context" tabindex="-1">2026-09-17 — Sidebar selection is distinct from workspace context <a class="header-anchor" href="#_2026-09-17-—-sidebar-selection-is-distinct-from-workspace-context" aria-label="Permalink to &quot;2026-09-17 — Sidebar selection is distinct from workspace context&quot;">​</a></h2><ul><li>Project headers and project, pinned and standalone conversation rows share one row-level hover surface, radius and transition. Project title buttons stay transparent; selected conversation paint takes precedence over hover.</li><li>A project is a workspace/disclosure control, not a separate selected page. The current workspace keeps its dot and all existing workspace behavior but no persistent row fill, including when no conversation is selected. The renderer marks that context with <code>data-current-workspace</code>, not an <code>active</code> project class. Session selection remains chat-page-aware and follows the destination immediately while a switch is pending.</li><li>Folding a selected conversation does not transfer selection to its project. Keyboard focus retains its outline, action buttons retain local feedback, window blur releases hover without clearing selection, and drop-target paint takes precedence over hover.</li><li>Renderer presentation only: no persistence, workspace activation, permissions or protocol changes. See <code>04-ux/01-ui-ia.md</code>, <code>04-ux/08-component-spec.md</code>, <code>04-ux/09-interaction-patterns.md</code> §9.1c and E2E-LAYOUT-sidebar-row-states in <code>06-delivery/04-e2e-test-plan.md</code>.</li></ul><h2 id="_2026-09-17-—-settings-shares-sidebar-material-and-restores-layout-without-entrance" tabindex="-1">2026-09-17 — Settings shares sidebar material and restores layout without entrance <a class="header-anchor" href="#_2026-09-17-—-settings-shares-sidebar-material-and-restores-layout-without-entrance" aria-label="Permalink to &quot;2026-09-17 — Settings shares sidebar material and restores layout without entrance&quot;">​</a></h2><ul><li>Main and settings navigation now share <code>sidebar-surface</code>: the same opaque color/image fallback on Windows/Linux and the existing native vibrancy plus tint/sheen on macOS. Settings wrapper ancestry allows that native material through, while its content pane and content-column titlebar remain opaque. Only the inner settings content animates, not the navigation backing.</li><li>Built-in settings rail colors no longer diverge from the sidebar. Legacy <code>--ds-settings-rail-bg</code> remains an optional shared fallback; a canonical <code>--ds-bg-sidebar</code> override wins. No theme or native-window API changes.</li><li>Sidebar mounting no longer implies expansion. A dedicated transition hook distinguishes visible collapsed-state changes from route restoration. Only real collapse/expand changes animate; settings navigation cancels unfinished phases, and returning restores the retained width/state without replaying <code>sidebar-in</code>. Animation-end guards, scoped timeout cleanup, auto-restore and reduced motion remain supported. Boot presentation uses the existing splash reveal instead of a second sidebar width animation.</li><li>No changes to item indentation, widths, workspace state, data or permissions. See <code>04-ux/06-settings-ia.md</code>, <code>04-ux/07-ui-design-system.md</code>, <code>04-ux/08-component-spec.md</code> and E2E-LAYOUT-sidebar-settings in <code>06-delivery/04-e2e-test-plan.md</code>.</li></ul><h2 id="_2026-09-17-—-a-route-surface-stops-holding-a-stacking-context-after-its-entrance" tabindex="-1">2026-09-17 — A route surface stops holding a stacking context after its entrance <a class="header-anchor" href="#_2026-09-17-—-a-route-surface-stops-holding-a-stacking-context-after-its-entrance" aria-label="Permalink to &quot;2026-09-17 — A route surface stops holding a stacking context after its entrance&quot;">​</a></h2><ul><li>The plugin install consent modal — and the plugin detail sheet with it — painted under the opaque 46px titlebar band, so the band was not covered by the modal&#39;s scrim and the band&#39;s controls stayed clickable. A route page is authored inside <code>.route-surface</code>, whose entrance animation carried <code>animation-fill-mode: both</code>: the fill kept the finished entrance &quot;in effect&quot; for as long as the page was mounted, and therefore kept the surface a stacking context, so an overlay rendered inside the page could not paint above the chrome.</li><li><code>.route-surface</code> and <code>.settings-content-enter</code> no longer carry <code>forwards</code> / <code>both</code> on their entrance (<code>apps/desktop/src/styles/chat-shell.css</code>). The entrance ends on the element&#39;s own state, so the fill was redundant; without it the surface stops holding a stacking context once the entrance finishes, and an overlay authored inside a route page paints above the titlebar band again. The keyframes stay opacity-only and the entrance still plays.</li><li>The plugin detail sheet keeps reserving <code>--ds-toolbar-height</code> at its top (<code>apps/desktop/src/styles/plugins.css</code>), because on Windows/Linux the renderer-drawn window controls own that top-right corner at <code>z-index: 200</code> and stay above any overlay&#39;s z-index.</li><li>The plugin overlays returned to <code>z-dialog</code> (40) from their off-scale <code>80</code> and <code>60</code> (<code>plugins.css</code>, and the release-notes overlay with them): once the route surface stopped trapping them they competed in the root stacking context, and at 80 they covered the leaf popups (60) and toasts (50) a dialog raises from inside itself — the install dialog&#39;s &quot;copied&quot; toast among them. A source test pins that relationship now.</li><li>Renderer only: no IPC, storage, schema, protocol, or plugin-SDK change, and no new default. See <code>04-ux/07-ui-design-system.md</code> §9, <code>apps/desktop/test/settings-dialog-overlay.test.mjs</code>, and <code>06-delivery/04-e2e-test-plan.md</code> E2E-LAYOUT-three-column-width-priority (its four overlay assertions live in <code>scripts/e2e-three-column-layout.mjs</code>).</li><li>Not done, and recorded as a follow-up: true modality — inertness, focus containment, shortcut stand-down — needs these overlays on the browser&#39;s top layer (<code>&lt;dialog&gt;.showModal()</code>). That is blocked until the popups, tooltips, and toasts they open, which are portaled to <code>document.body</code>, also move to the top layer, because top-layer content paints above them and makes them unusable; an attempt was withdrawn for exactly that reason.</li></ul><h2 id="_2026-09-18-—-prompt-enhancement-ships-a-substantive-rewrite-with-user-overridable-templates-d447" tabindex="-1">2026-09-18 — Prompt enhancement ships a substantive rewrite with user-overridable templates (D447) <a class="header-anchor" href="#_2026-09-18-—-prompt-enhancement-ships-a-substantive-rewrite-with-user-overridable-templates-d447" aria-label="Permalink to &quot;2026-09-18 — Prompt enhancement ships a substantive rewrite with user-overridable templates (D447)&quot;">​</a></h2><ul><li>The default enhancement prompt moves from one conservative sentence to a structured system prompt plus a templated user message: role, analysis, rewrite principles, an explicit do-not list, language-following rules that forbid language meta notes, a length brake (about twice the draft; no 800-character cap), and an output contract. The old <code>If the draft is already good, return it with at most minor polish</code> clause is removed: it made the action look inert on short drafts, which is the reported complaint.</li><li>Two constraints the old prompt lacked are now explicit: code, commands, file paths, identifiers, API names, and other proper nouns must be reproduced exactly, and the answer must not open with a language meta note such as &quot;The draft is in Chinese&quot;. Both are default-value decisions, not implementation details, because they change what every user receives.</li><li>The user template becomes an <code>AppSettings</code> override (<code>promptEnhancementUserTemplate</code>) with its default in <code>packages/shared/src/prompt-enhancement.ts</code>; a blank override means &quot;use the default&quot;, and editing the field back to the exact default text clears the override instead of freezing a copy. Storing the default text as a user value was rejected: a later improvement to the default would then never reach those users.</li><li>The system prompt stays built in and is not user-editable. It carries rules the spec and E2E scenario assert, so a stored override could silently remove one; host-core drops such a value if an older build wrote it. Editing the system prompt remains a source change with a spec update.</li><li>The user template carries <code>${ssrInterpolate(_ctx.draft)}</code>. host-core rejects a non-blank user template without it, and each template must stay within <code>PROMPT_ENHANCEMENT_TEMPLATE_MAX_LENGTH</code>; main falls back to the default if an unusable value ever reaches the runtime. One matching pair of wrapping quotation marks is stripped from the model&#39;s answer.</li><li>The settings surface is one row with a <code>Use a custom template</code> switch and the subagent rows&#39; edit icon button, opening an editor sheet, rather than inline textareas or a row of labelled actions: the AI settings tab stays compact and the row reads as one control cluster. The switch, not the text, decides whether a stored template applies, so turning it off preserves the user&#39;s text. It is enabled only once a usable template exists and saving one turns it on, because an always-enabled switch would choose between two identical states before any template was written.</li><li><code>promptEnhancementProviderId</code> / <code>promptEnhancementModelId</code> let the rewrite run on a model other than the conversation&#39;s. An unresolvable pin falls back to the Composer&#39;s current model with a warning rather than failing the action.</li><li>The enhancement model and reasoning level move to Settings -&gt; Models, in a dedicated <code>Enhancement prompt</code> card below Defaults, whose model row is titled <code>Default model</code> like the Defaults card&#39;s row (the card heading separates them). Both are decisions about which model runs the rewrite, and the model page already owns the one model picker a user learns; the prompt card keeps only the prompt. The enhancement-model row reuses the default-model row&#39;s anchored menu rather than introducing a second picker.</li><li>The enhancement reasoning row lists only the levels the selected model actually supports, resolved the same way a turn resolves them (binding <code>thinkingLevels</code>, then the live catalog, then the provider default), and is disabled when the model supports none. An earlier revision offered the full canonical ladder regardless of model, which is wrong for a model without reasoning. Switching model re-clamps and rewrites the stored level, so a persisted level is always runnable.</li><li>The enhancement reasoning level becomes configurable and defaults to <code>off</code>; it never inherits the conversation&#39;s effort. A rewrite rarely benefits from reasoning, and reasoning is the slow path, so &quot;follow the session&quot; would silently opt every enhancement on a reasoning model into the slowest setting. The level is clamped by the resolved model&#39;s capabilities.</li><li>One enhancement request is bounded by a 60-second ceiling: expiry aborts the in-flight call (best-effort) and races the promise so the renderer is released, then fails with <code>TIMEOUT</code> instead of retrying on the session model. The provider retry budget can already spend about a minute, the renderer has no cancel, and a hidden second attempt would double the wait. This is a behavior change for a slow provider: the action now fails visibly instead of hanging.</li><li>No IPC method, process boundary, storage ownership, or security boundary changes; the existing <code>prompt/enhance</code> payload is unchanged. See <code>04-ux/12-prompt-enhancement.md</code> §3 and §5, ADR 0121, and <code>06-delivery/04-e2e-test-plan.md</code> E2E-259.</li></ul><h2 id="_2026-09-19-—-restore-resizable-sidebar-width-with-collapse-below-threshold-d459" tabindex="-1">2026-09-19 — Restore resizable sidebar width with collapse-below-threshold (D459) <a class="header-anchor" href="#_2026-09-19-—-restore-resizable-sidebar-width-with-collapse-below-threshold-d459" aria-label="Permalink to &quot;2026-09-19 — Restore resizable sidebar width with collapse-below-threshold (D459)&quot;">​</a></h2><ul><li>Amend D408 / ADR 0238 and restore ADR 0141: the expanded sidebar is again a renderer-owned <code>240px..520px</code> column (default <code>275px</code>). The right-edge handle previews width from the pointer-down position, persists on release, and supports ArrowLeft/ArrowRight (16px), Home, and End. Escape, cancellation, lost capture, and unmount restore the press-time width.</li><li>A pointer width below <code>160px</code> collapses the sidebar immediately as a user action. The preferred expanded width is not overwritten. Keyboard resize never collapses; <code>Cmd/Ctrl+B</code> remains the keyboard fold.</li><li>The live maximum is the three-column remainder after MainChat&#39;s 450px floor and an occupying work panel&#39;s requested width, so a user-chosen sidebar width does not trip D408&#39;s yield. Work-panel growth and window shrink still collapse the expanded sidebar at that threshold.</li><li>Renderer only: existing <code>pi.desktop.sidebarWidth</code> preference, no IPC or native window change. See ADR 0290 and E2E-168.</li></ul><h2 id="_2026-09-19-—-mcp-catalogs-are-bounded-by-protocol-guards-not-truncated-d452" tabindex="-1">2026-09-19 — MCP catalogs are bounded by protocol guards, not truncated (D452) <a class="header-anchor" href="#_2026-09-19-—-mcp-catalogs-are-bounded-by-protocol-guards-not-truncated-d452" aria-label="Permalink to &quot;2026-09-19 — MCP catalogs are bounded by protocol guards, not truncated (D452)&quot;">​</a></h2><ul><li>Amend D176 / ADR 0038: the per-server <code>tools/list</code> bound stops being a silent truncation at 64 tools and 8 pages. The client now follows pagination to the last page under Codex-parity protocol guards — 2048 tools, 100 pages, a cursor that repeats or is malformed, and 30s for the whole traversal — and a server that breaks one loses its handshake instead of contributing a prefix of its catalog.</li><li>The cap was never the prompt&#39;s protection: MCP tools reach the model as deferred on-demand entries behind <code>ToolSearch</code>, and the prompt block that advertises them is capped independently, so a 300-tool server costs nothing until one of its tools is activated.</li><li><code>plugin.mcp.tools.truncated</code> is gone. A refusal is audited as <code>plugin.mcp.connect</code> / <code>mcp.connect</code> with the guard&#39;s error code and message, and reaches the user as the status message of a user-configured server.</li><li>See ADR 0038 and E2E-024K; <code>apps/desktop/test/plugin-mcp.test.mjs</code> covers the whole catalog, each guard, and the old truncation as a failing baseline.</li></ul><h2 id="_2026-09-19-—-the-voice-settings-card-is-removed-adr-0291" tabindex="-1">2026-09-19 — The Voice settings card is removed (ADR 0291) <a class="header-anchor" href="#_2026-09-19-—-the-voice-settings-card-is-removed-adr-0291" aria-label="Permalink to &quot;2026-09-19 — The Voice settings card is removed (ADR 0291)&quot;">​</a></h2><ul><li>Settings → AI no longer carries a speech section: <code>VoiceSettingsCard</code> and <code>features/settings/voice-settings.tsx</code>, the AI destination&#39;s <code>settings.speechTitle</code> / <code>settings.speechTranscribe</code> / <code>settings.speechSynthesize</code> search keywords, the <code>.settings-speech-*</code> styles, the thirteen <code>settings.speech*</code> keys in all eight shipped locale catalogs, and the seven unreferenced <code>chat.transcribe*</code> / <code>chat.speak*</code> keys written for those withdrawn Composer controls are gone.</li><li>The host capability stays: <code>speech/getStatus</code>, <code>speech/transcribe</code>, <code>speech/synthesize</code>, <code>AppSettings.speech</code> validation, the two built-in protocols, the <code>speech.adapter.register</code> plugin permission, and the renderer API bridge are unchanged. A binding is written by a caller through the host settings API, and plugins and IPC calls are its only consumers.</li><li>Already withdrawn, and repaired here in the docs: ADR 0281 item 5&#39;s Composer transcription and draft-speech entry point — its mic and read-aloud controls were removed on 2026-09-18 (<code>344ef4ec2</code>, PR #555) — and the Composer speech controls <code>04-ux/08-component-spec.md</code> §2.5 described. The copy written for them is retired with them.</li><li>Renderer only: no IPC channel, storage schema, host RPC, permission, or Rust change, and no stored binding is dropped. See ADR 0291, <code>04-ux/06-settings-ia.md</code>, <code>03-runtime/20-speech.md</code>, E2E-008e.</li></ul><h2 id="_2026-09-19-—-ssh-bootstrap-for-remote-hosts-d453" tabindex="-1">2026-09-19 — SSH bootstrap for remote hosts (D453) <a class="header-anchor" href="#_2026-09-19-—-ssh-bootstrap-for-remote-hosts-d453" aria-label="Permalink to &quot;2026-09-19 — SSH bootstrap for remote hosts (D453)&quot;">​</a></h2><ul><li>The desktop installs and pairs a <code>pi-host</code> on a machine the user already reaches over SSH using the system <code>ssh</code> client, so <code>~/.ssh/config</code>, the agent, and jump hosts apply unchanged and the app holds no SSH secret; <code>BatchMode=yes</code> makes a host that needs an interactive password or passphrase fail with a typed error instead of hanging behind an invisible prompt.</li><li>The desktop resolves the bundle for the remote platform at its own version, holds the SHA-256 the release publishes, and refuses an unpublished target (<code>linux-x64</code> only today) before any download. The uploaded script downloads, verifies, and installs the bundle under the remote <code>$HOME</code>; no executable bytes cross the SSH channel.</li><li>The script runs under <code>umask 077</code> and echoes <code>PI_HOST_READY</code> / <code>PI_HOST_PAIRING_TOKEN</code>, so the single-use pairing token exists only in a work file and on the SSH channel, never in a world-readable path or a URL (security §3.4). <code>PI_HOST_READY.version</code> must equal the desktop&#39;s; a mismatch is <code>HOST_VERSION_MISMATCH</code> (D375) and is checked before a forward exists.</li><li>A paired host now stores an SSH descriptor (<code>metadata.transport = &quot;ssh&quot;</code>) instead of a URL, because the local forward port is not stable across launches; the tunnel manager re-establishes <code>ssh -N -L</code> on every launch and adopts the bootstrap&#39;s own live forward so pairing opens exactly one tunnel. The descriptor is re-validated on every registry read, and a malformed one degrades to &quot;not an SSH host&quot; rather than spawning <code>ssh</code> with garbage arguments.</li><li><code>pi-desktop/remoteHost/bootstrap</code> joins <code>list</code> / <code>pair</code> / <code>remove</code>, and the <code>connection/pair</code> exchange was factored into one <code>exchangePairingToken</code> used by the pasted-URL path and the bootstrap path alike.</li><li>Not in this slice: the terminal work-panel client (Stage 5), the reverse tool relay (Stage 6), the resync watchdog, non-Linux and Windows remote targets, and provider configuration over the SSH channel — a freshly bootstrapped host still fails <code>turn/start</code> closed with <code>MODEL_NOT_CONFIGURED</code> until a provider is configured on it. See ADR 0292, <code>06-delivery/07-remote-control-rollout.md</code> §7, and <code>05-security/02-remote-control-security.md</code> §3.4.</li></ul><h2 id="_2026-09-19-—-ssh-password-authentication-for-the-bootstrap-d454" tabindex="-1">2026-09-19 — SSH password authentication for the bootstrap (D454) <a class="header-anchor" href="#_2026-09-19-—-ssh-password-authentication-for-the-bootstrap-d454" aria-label="Permalink to &quot;2026-09-19 — SSH password authentication for the bootstrap (D454)&quot;">​</a></h2><ul><li>ADR 0292 kept the app free of SSH secrets and made <code>BatchMode=yes</code> the enforcer, which is the right default but leaves a machine whose only credential is a password unreachable from the app — the user has to install a key from a terminal first, which is the detour the bootstrap exists to remove. A password may now be supplied <em>instead of</em> a key, per host, and it is opt-in: with no password the argv and every other behaviour are unchanged.</li><li>The secret reaches the <code>ssh</code> client through OpenSSH&#39;s askpass helper (<code>remote/ssh-askpass.ts</code>): <code>SSH_ASKPASS</code> points at a generated <code>/bin/sh</code> script and <code>SSH_ASKPASS_REQUIRE=force</code> makes it apply without a terminal, so the password is never an <code>ssh</code> argument or an environment value. Its confidentiality rests on modes — a <code>0700</code> directory, a <code>0600</code> secret, a <code>0700</code> helper — and the material is deleted once no child can still be prompting (after an exec child closes, after a forward&#39;s port is up, or on <code>dispose</code>), so nothing lingers between operations.</li><li><code>BatchMode=yes</code> becomes <code>BatchMode=no</code> only for a password target, together with <code>NumberOfPasswordPrompts=1</code> and <code>PubkeyAuthentication=no</code>: the helper answers every prompt with the same secret, so a retry could only repeat a wrong password, and repeated failures are what trip a server&#39;s own lockout. Default identities are skipped so an encrypted local key cannot consume that single prompt as a passphrase; password mode in Settings replaces a key.</li><li>The password is persisted encrypted through the same OS-keychain <code>EncryptionPort</code> the device token uses, as a new optional <code>encryptedSshSecret</code> in <code>remote-hosts.json</code>; the descriptor gains <code>auth: &quot;password&quot;</code> and nothing else, so a key descriptor keeps exactly its previous shape and the file&#39;s <code>version</code> stays <code>1</code>. A password that will not decrypt costs the secret, not the paired host, because the device token is independent of it.</li><li>The renderer never sees it: <code>RemoteHostSummary</code> is unchanged and the secret travels beside <code>RemoteHostSshMetadata</code> (plaintext, persisted metadata) rather than inside it. <code>assertSshPassword</code> rejects a line break with <code>INVALID_ARGUMENT</code> before any remote command runs, because OpenSSH reads the helper&#39;s answer only up to the first break; Windows is refused with <code>HOST_BOOTSTRAP_FAILED</code> and a remedy, because its OpenSSH cannot execute a shell-script helper. A host recorded as password-authenticated with no usable secret is not opened at all, which surfaces as the host being offline.</li><li>Settings gains an authentication mode on the SSH form with a masked password field and a reveal toggle, and the copy that promised no password was ever stored is replaced in all eight shipped locales. See ADR 0293, <code>05-security/02-remote-control-security.md</code> §3.4 and gate 21, <code>02-architecture/05-remote-agent-control.md</code> §5.2, E2E-REMOTE-HOST-ssh-password-authentication.</li></ul><h2 id="_2026-09-20-—-project-archive-is-a-list-inspector-d455" tabindex="-1">2026-09-20 — Project archive is a list + inspector (D455) <a class="header-anchor" href="#_2026-09-20-—-project-archive-is-a-list-inspector-d455" aria-label="Permalink to &quot;2026-09-20 — Project archive is a list + inspector (D455)&quot;">​</a></h2><ul><li>Decision D455 revises D267&#39;s expanding rows for Settings → Project archive. The quiet intro and toolbar stay; the single expanding list becomes compact rows with a full-width inspector under the selected row (ADR 0294).</li><li>Clicking a row selects it and keeps Settings open. Double-click, Enter, or the inspector Open action activates the project and returns to chat. Archive and close still keep the destination open (D133 / D267).</li><li>Folders, chats (latest first, batches of eight), and the existing menu live in the inspector. Archived records stay grouped and visible.</li><li>Presentation only: no IPC, storage, or host protocol change. See <code>04-ux/06-settings-ia.md</code> and E2E-038.</li></ul><h2 id="_2026-09-20-—-prompt-enhancement-model-card-moves-to-settings-→-ai" tabindex="-1">2026-09-20 — Prompt-enhancement model card moves to Settings → AI <a class="header-anchor" href="#_2026-09-20-—-prompt-enhancement-model-card-moves-to-settings-→-ai" aria-label="Permalink to &quot;2026-09-20 — Prompt-enhancement model card moves to Settings → AI&quot;">​</a></h2><ul><li>Amend ADR 0121 and the 2026-09-18 D447 placement: the Enhancement prompt card (default model + reasoning) leaves Settings → Models and sits on Settings → AI immediately below Prompt enhancement, so the template and the rewrite model for the same Composer action live on one destination. The card keeps its own title because the model picker still needs title / current value / control. Settings search indexes those rows on the AI tab. Presentation only: no IPC, storage, or host-protocol change. See <code>04-ux/06-settings-ia.md</code> and <code>04-ux/12-prompt-enhancement.md</code> §5.</li></ul><h2 id="_2026-09-20-—-prompt-enhancement-is-one-settings-→-ai-card" tabindex="-1">2026-09-20 — Prompt enhancement is one Settings → AI card <a class="header-anchor" href="#_2026-09-20-—-prompt-enhancement-is-one-settings-→-ai-card" aria-label="Permalink to &quot;2026-09-20 — Prompt enhancement is one Settings → AI card&quot;">​</a></h2><ul><li>Amend the same-day two-card placement: the Enhancement prompt card is folded into Prompt enhancement on Settings → AI. The custom-template switch, default model, and reasoning rows share one heading. Settings search still indexes <code>promptEnhancementModelTitle</code> so &quot;增强提示词&quot; finds the AI tab. Presentation only: no IPC, storage, or host-protocol change.</li></ul><h2 id="_2026-09-20-—-session-thinking-parameter-omission-d456" tabindex="-1">2026-09-20 — Session thinking-parameter omission (D456) <a class="header-anchor" href="#_2026-09-20-—-session-thinking-parameter-omission-d456" aria-label="Permalink to &quot;2026-09-20 — Session thinking-parameter omission (D456)&quot;">​</a></h2><ul><li>Composer, session storage, and host validation accept <code>thinkingLevel: omit</code> next to the seven canonical levels. Binding chips and catalog capability lists stay canonical; <code>omit</code> is a client selector, not a published level.</li><li>Runtime clamping keeps <code>omit</code> on a reasoning model. Agent bookkeeping stays <code>off</code> and the parent stream uses the same low-level omit path as subagents, so adapters send no thinking field. Explicit <code>off</code> still disables thinking.</li><li>The Composer reasoning menu prepends <code>omit</code> whenever the selected model exposes enabled canonical levels, and the chip renders the canonical string. Settings <code>defaultThinkingLevel</code> also accepts <code>omit</code> on a reasoning binding so new sessions can start without a thinking override. Schema v19 rebuilds <code>sessions</code> so the CHECK includes <code>omit</code>. See ADR 0295 and E2E-203a.</li></ul><h2 id="_2026-09-19-—-drain-poison-outbox-entries-accept-steering-into-a-delivery-turn-d597" tabindex="-1">2026-09-19 — Drain poison outbox entries; accept steering into a delivery turn (D597) <a class="header-anchor" href="#_2026-09-19-—-drain-poison-outbox-entries-accept-steering-into-a-delivery-turn-d597" aria-label="Permalink to &quot;2026-09-19 — Drain poison outbox entries; accept steering into a delivery turn (D597)&quot;">​</a></h2><ul><li>Decision D597 amends ADR 0041, ADR 0239, ADR active-turn-steering, and D444. A host append whose error message carries a <code>PERMISSION_DENIED:</code> prefix is a permanent, message-level rejection. The Electron outbox drops that row and keeps draining so one poison head cannot fill the 1024-entry cap. Substring matches such as <code>PLUGIN_PERMISSION_DENIED</code> still pause, as do other host failures.</li><li>Steering into a claimed collaboration delivery turn is extra human input in that session. It is exempt from the delivery content/attachment contract, does not inherit the delivery&#39;s agent origin, and has any client-supplied <code>session_message</code> stripped. Steering that names another session is still <code>PERMISSION_DENIED</code>.</li><li>See <code>03-runtime/04-data-storage.md</code>, <code>03-runtime/06-host-rpc-protocol.md</code>, E2E-SESSION-outbox-poison-does-not-drop-history.</li></ul><h2 id="_2026-09-20-—-project-archive-reads-as-an-inset-grouped-card-list" tabindex="-1">2026-09-20 — Project archive reads as an inset grouped card list <a class="header-anchor" href="#_2026-09-20-—-project-archive-reads-as-an-inset-grouped-card-list" aria-label="Permalink to &quot;2026-09-20 — Project archive reads as an inset grouped card list&quot;">​</a></h2><ul><li>Revise D455&#39;s presentation only: the destination stays one column with the detail under the selected row and no side-by-side pane, but the index is now an inset grouped list in the iOS sense. Each row reads left to right as identity (color glyph, name, one status tag, shortened monospace path) and right to left as detail (session count, relative last-active time, disclosure indicator). Rows stay tiles separated by the row gap, never by rules.</li><li>The selected row is the header of its own card, so the detail panel repeats no name, path, or status tag: it opens with an action bar (New task, Open while the project is not the live workspace, overflow menu), then the read-only folder and branch facts and the sessions count, then the chats. The overflow menu keeps its items, its ordering, and its two-step delete.</li><li>Arrow-key selection walks the rendered order (section order with the active sort) instead of the index build order and moves real focus to the row it selected, and the handler yields to the open card so Enter inside the detail stays that control&#39;s own action. Row element ids are encoded, so two paths that differ only in separators can no longer collide. An empty index shows the pending indicator until the host listing settles instead of &quot;no projects yet&quot;, and the expanded card and the disclosure indicator honor reduced motion.</li><li>Clicking the row whose card is open closes it again, through one shared toggle. Selection and the open card are separate states, so the current row keeps its ring while its card is closed and the disclosure indicator turns down only while the card is really open. Moving the selection with the arrow keys always opens the row it landed on. Presentation only: no IPC, storage, host-protocol, or activation-semantics change. See <code>04-ux/06-settings-ia.md</code>, <code>04-ux/07-ui-design-system.md</code>, and ADR 0294.</li></ul><h2 id="_2026-09-20-—-project-archive-opens-closed-and-keeps-no-page-level-prose" tabindex="-1">2026-09-20 — Project archive opens closed and keeps no page-level prose <a class="header-anchor" href="#_2026-09-20-—-project-archive-opens-closed-and-keeps-no-page-level-prose" aria-label="Permalink to &quot;2026-09-20 — Project archive opens closed and keeps no page-level prose&quot;">​</a></h2><ul><li>The destination renders no description line: the toolbar is the first thing under the page title, the same shape as the agent capability and Import destinations. The <code>project.archiveSubtitle</code> key is retired from every catalog rather than left behind unused.</li><li>Nothing is expanded until the user clicks a row. Selection and the open card were two states; they collapse into one, so a closed index highlights nothing, <code>aria-expanded</code> and the rotating disclosure indicator describe the open card only, and a row whose project leaves the list (a search that no longer matches it) closes with it. <code>resolveSelectedPath</code>, which always resolved a selection (the live workspace first, then the first row), is removed together with the auto-expansion it existed to serve.</li><li>Presentation only: no IPC, storage, host-protocol, or activation-semantics change. Search still matches session titles and keeps the owning project in the index. See <code>04-ux/06-settings-ia.md</code>.</li></ul><h2 id="_2026-09-20-—-the-menu-root-carries-the-reasoning-slider-417-d458" tabindex="-1">2026-09-20 — The menu root carries the reasoning slider (#417, D458) <a class="header-anchor" href="#_2026-09-20-—-the-menu-root-carries-the-reasoning-slider-417-d458" aria-label="Permalink to &quot;2026-09-20 — The menu root carries the reasoning slider (#417, D458)&quot;">​</a></h2><ul><li>The Composer&#39;s Reasoning level selection lived only inside a vertical radio list one submenu deep. Issue #417 asked for a Codex-desktop-style slider while keeping the existing click-to-list interaction.</li><li>The combined model × reasoning menu root now shows a native range input with one stop per enabled level plus a clickable tick label per stop, directly beneath the Reasoning level entry. Dragging the slider or clicking a tick commits the level immediately through the same <code>configureActiveSession</code> path and leaves the menu where it is, so quick adjustments never cost a submenu trip. The Reasoning level entry itself still opens the classic radio list, which keeps its radio semantics, trailing check, Up/Down/Enter/Left contract, and root-return behavior.</li><li>The slider owns its arrow/Home/End/Enter keys while focused, so those keys adjust the level instead of driving menu navigation, while Escape still closes the menu. Dragging across several stops keeps only the last pending level; commits serialize latest-wins so an in-flight idle persist cannot land an intermediate stop, and a session or model change invalidates pending work. A local drag lead keeps the controlled input from snapping back while the store confirmation lands. Tick labels are clickable but not tab stops. A binding with a single enabled level hides the slider entirely.</li><li>Level values remain untranslated canonical strings, including session <code>omit</code> (D456) when the binding exposes enabled canonical levels. The seven-level ladder, provider filtering, and clamping rules are unchanged. Renderer only: no protocol, storage, host, permission, or migration change. See <code>04-ux/08-component-spec.md</code>, <code>04-ux/07-ui-design-system.md</code>, and E2E-050.</li></ul><h2 id="_2026-09-20-—-a-development-build-is-its-own-installation-d599" tabindex="-1">2026-09-20 — A development build is its own installation (D599) <a class="header-anchor" href="#_2026-09-20-—-a-development-build-is-its-own-installation-d599" aria-label="Permalink to &quot;2026-09-20 — A development build is its own installation (D599)&quot;">​</a></h2><ul><li>A packaged PI-Desktop and a <code>pnpm dev</code> host no longer share the two directories that define an installation. A development build runs under <code>PI-Desktop Dev</code> in the OS application-data root — its own Electron <code>userData</code>, and with it the single-instance lock, renderer <code>localStorage</code>, the plugin panel partitions, and browser pane cookies — and reads <code>~/.pi-desktop-dev</code>. A packaged installation keeps <code>PI-Desktop</code> and <code>~/.pi-desktop</code> unchanged, so no existing profile is relocated.</li><li>A shipped app that is already running therefore no longer refuses the lock a development launch needs, and the two never put a second host-core over one <code>pi.sqlite</code>, one persistence outbox, or one log tree. <code>PI_DESKTOP_DATA_DIR</code> still wins over either profile and is now made absolute before it reaches host-core as a child-process environment variable. Electron main publishes the resolved directory back to that variable so the plugin runtime reads one root instead of falling back to the shipped default. An explicit <code>--user-data-dir</code> still wins, because the E2E harnesses point a build at a throwaway profile with it.</li><li>Decision D599 narrows D236 and amends ADR 0094: the lock keeps its installation scope, and a development build is now a second installation rather than an ambiguous second process. No IPC channel, host protocol, storage schema, or packaged-installation path changed. See <code>03-runtime/07-process-model.md</code>, <code>03-runtime/04-data-storage.md</code>, and <code>02-architecture/03-repo-structure.md</code>.</li></ul><h2 id="_2026-09-20-—-the-app-ships-no-fonts-d598" tabindex="-1">2026-09-20 — The app ships no fonts (D598) <a class="header-anchor" href="#_2026-09-20-—-the-app-ships-no-fonts-d598" aria-label="Permalink to &quot;2026-09-20 — The app ships no fonts (D598)&quot;">​</a></h2><ul><li>Decision D598 amends D232 / ADR 0083. The four bundled families (Geist, Inter, Noto Sans SC, LXGW WenKai), their <code>woff2</code> files, their OFL license texts, <code>styles/fonts.css</code>, and its <code>@import</code> are gone, together with <code>BUNDLED_FONTS</code>, the <code>BundledFont</code> type, <code>FontOption.license</code>, the picker&#39;s license badge, and the <code>settings.fontBundled</code> key in all eight catalogs.</li><li>The Settings Font picker keeps its shape: System default persists the empty string, then the installed system families Electron main enumerates over <code>pi-desktop/app/systemFonts</code> using platform tooling only (deduplicated, filtered, sorted, cached 60 s). The <code>bundled</code> group is gone; a stored stack that matches no option still leads the list under Saved.</li><li>Every generated stack appends the system-only CJK tier <code>&quot;PingFang SC&quot;, &quot;Hiragino Sans GB&quot;, &quot;Microsoft YaHei&quot;, sans-serif</code>, with no bundled family in front of it. A previously stored bundled stack (for example <code>&quot;Geist&quot;, &quot;PingFang SC&quot;, …</code>) is kept byte for byte, shows under Saved, and renders through that tier. No migration, protocol, or schema change.</li><li><code>out/renderer</code> emits no application font face any more; only KaTeX&#39;s <code>woff2</code> glyphs remain. See ADR 0298, <code>04-ux/06-settings-ia.md</code>, <code>04-ux/07-ui-design-system.md</code>, and E2E-126.</li></ul><h2 id="_2026-09-20-—-stdio-mcp-spawn-uses-the-login-shell-path-d600-issue-571" tabindex="-1">2026-09-20 — Stdio MCP spawn uses the login-shell PATH (D600, issue #571) <a class="header-anchor" href="#_2026-09-20-—-stdio-mcp-spawn-uses-the-login-shell-path-d600-issue-571" aria-label="Permalink to &quot;2026-09-20 — Stdio MCP spawn uses the login-shell PATH (D600, issue #571)&quot;">​</a></h2><ul><li>Finder/Dock launches Electron with <code>/usr/bin:/bin:/usr/sbin:/sbin</code>. User MCP stdio spawn copies that PATH and uses <code>shell: false</code>, so a market-installed Fetch server (<code>uvx mcp-server-fetch</code>) failed with <code>spawn uvx ENOENT</code> even when <code>uvx</code> worked in a terminal. Same hole for <code>npx</code>.</li><li>Electron main now probes the login shell the way host-core already does for Bash (ADR 0045): <code>$SHELL -lic &#39;printf %s &quot;$PATH&quot;&#39;</code>, 5s-bounded, cached, last stdout line only. Well-known user bin dirs that exist (<code>/opt/homebrew/bin</code>, <code>~/.local/bin</code>, <code>~/.cargo/bin</code>, …) are merged in. Windows stays on the inherited PATH. Command names stay bare; arguments stay literal.</li><li>A missing executable is reported as <code>command not found: &lt;name&gt;</code> instead of a raw <code>spawn uvx ENOENT</code>.</li><li>See ADR 0045, <code>03-runtime/03-tools-and-permissions.md</code>, issue #571.</li></ul><h2 id="_2026-09-20-—-settings-explain-themselves-on-demand-d601" tabindex="-1">2026-09-20 — Settings explain themselves on demand (D601) <a class="header-anchor" href="#_2026-09-20-—-settings-explain-themselves-on-demand-d601" aria-label="Permalink to &quot;2026-09-20 — Settings explain themselves on demand (D601)&quot;">​</a></h2><ul><li>Decision D601 is renderer-only. A settings row no longer carries a permanent second line of prose under its title: <code>SettingsRow.description</code> is a string that renders behind the question mark beside the title, and <code>SettingsCard</code> gains the same <code>description</code> for a card heading. <code>HelpIcon</code> (<code>components/ui.tsx</code>) is the single affordance — a real button, so a keyboard reaches the sentence a pointer hovers, and the sentence itself is its accessible name. <code>Field</code>&#39;s <code>hint</code> moved to it as well.</li><li>A row&#39;s live value is not an explanation and stays visible: the pinned default model travels through the new <code>SettingsRow.detail</code>, and <code>NetworkProxySection</code>&#39;s local copy of the row skeleton is gone in favour of the shared one, so the retired line cannot return through a second renderer.</li><li>The mark is a sibling of the element it explains wherever that element is a heading, because a nested button joins the heading&#39;s accessible name. A settings <code>Field</code> is the deliberate exception: its hint stays inside the <code>&lt;label&gt;</code>, so the field&#39;s accessible name carries the sentence too. Moving it out would cost click-to-focus on every field, which a longer name does not justify.</li><li>No IPC channel, host protocol, storage schema, persisted setting, or i18n key changed: every sentence keeps its key and only its placement moved. See <code>04-ux/06-settings-ia.md</code> and <code>04-ux/07-ui-design-system.md</code>.</li></ul><h2 id="_2026-09-20-—-crash-dumps-stay-in-the-data-directory-d602" tabindex="-1">2026-09-20 — Crash dumps stay in the data directory (D602) <a class="header-anchor" href="#_2026-09-20-—-crash-dumps-stay-in-the-data-directory-d602" aria-label="Permalink to &quot;2026-09-20 — Crash dumps stay in the data directory (D602)&quot;">​</a></h2><ul><li>Crashpad starts local-only (<code>uploadToServer: false</code>) before <code>ready</code>. Dumps live under <code>&lt;data_dir&gt;/crash-dumps</code>, not Electron&#39;s default <code>userData</code> crashDumps path, so a <code>PI_DESKTOP_DATA_DIR</code> profile does not share dumps with another installation. The marker <code>crash-dumps.json</code> records the newest mtime already reported.</li><li>The next lock-holding launch writes one <code>diagnostics</code> line for dumps newer than that marker, classified by Crashpad&#39;s <code>ptype</code> annotation: <code>error</code> if any new dump is the browser/main process, <code>warn</code> for a recovered renderer, GPU, or utility crash. A scan failure is one <code>crashDumpReportFailed</code> warn and never blocks the first window.</li><li>Host-core and sidecar crashes stay on the supervisor path and the <code>host</code> / <code>agent</code> log channels. Nothing is uploaded. See <code>03-runtime/07-process-model.md</code> §4, <code>03-runtime/09-logging-and-observability.md</code>, and <code>03-runtime/04-data-storage.md</code>.</li></ul><h2 id="_2026-09-20-—-the-dock-occludes-transcript-paint-below-composer-d603-issue-728" tabindex="-1">2026-09-20 — The dock occludes transcript paint below Composer (D603, issue #728) <a class="header-anchor" href="#_2026-09-20-—-the-dock-occludes-transcript-paint-below-composer-d603-issue-728" aria-label="Permalink to &quot;2026-09-20 — The dock occludes transcript paint below Composer (D603, issue #728)&quot;">​</a></h2><ul><li>The transcript scrollport fills the conversation pane while its content reserves the measured Composer height. Because the absolutely positioned dock was transparent, a row could keep painting below the floating shell and stay visible in its bottom gap or outside its rounded corners.</li><li><code>.composer-dock-docked</code> now paints the opaque <code>--ds-bg-primary</code> workspace surface across its full width. The Composer retains its existing translucent elevated token, radius, shadow, and measured scroll reserve; the backing band removes only transcript paint that has crossed the Composer boundary.</li><li>Renderer CSS and the theme surface regression change only. There is no scroll state, protocol, persistence, theme schema, or permission change. See <code>04-ux/08-component-spec.md</code> and E2E-CHAT-opaque-floating-decision-and-retry-surfaces.</li></ul><h2 id="_2026-09-21-—-the-context-estimate-is-calibrated-conservatively-d606" tabindex="-1">2026-09-21 — The context estimate is calibrated conservatively (D606) <a class="header-anchor" href="#_2026-09-21-—-the-context-estimate-is-calibrated-conservatively-d606" aria-label="Permalink to &quot;2026-09-21 — The context estimate is calibrated conservatively (D606)&quot;">​</a></h2><ul><li>Every budget threshold reads one number, and that number is corrected against what requests actually cost. pi&#39;s estimator anchors on the last assistant usage and estimates the rest as <code>chars / 4</code>, which under-counts CJK text by roughly a factor of 2–4 and, with no anchor left, omits the system prompt and tool schemas entirely.</li><li>The two errors are applied separately: the per-character bias as a scale-free ratio over the guessed tail, and an unanchored residual as a ratio only for observations taken at a comparable scale (0.5×–2×), otherwise as the observed overhead capped at 32,000 tokens. A 100k-scale sample therefore cannot be applied as a ratio to a 1M projection.</li><li>The correction is asymmetric: upward applies once three observations exist; downward needs three agreeing samples, is capped at 15 % per step, and can never take the value below 85 % of the raw estimate, so a projection at 1.18× the hard limit still compacts. A report outside 0.5×–3× of what the calibration predicted is a misreport, and two consecutive misreports freeze the downward direction. See <code>03-runtime/02-agent-runtime.md</code> §5.1.</li></ul><h2 id="_2026-09-21-—-a-tool-call-id-is-unique-in-every-request-d608-issue-718" tabindex="-1">2026-09-21 — A tool-call id is unique in every request (D608, issue #718) <a class="header-anchor" href="#_2026-09-21-—-a-tool-call-id-is-unique-in-every-request-d608-issue-718" aria-label="Permalink to &quot;2026-09-21 — A tool-call id is unique in every request (D608, issue #718)&quot;">​</a></h2><ul><li>Anthropic-family endpoints, DeepSeek&#39;s included, reject a whole turn with <code>tool_use ids must be unique</code> when the request carries a call id twice, and the session cannot continue while that lasts. The transcript is an append-only snapshot stream that tolerates a retried append, so the same call can reach the assembled context twice: under one row id, which the host&#39;s keep-last read already collapses, or under two, which it cannot.</li><li>The last view before the wire therefore keeps the first occurrence of each <code>toolCall</code> id and drops a later call or a later result for that id, so the call/result pair the provider validates stays well-formed. A request with no duplicates is returned unchanged (identity, not a copy). Nothing is rewritten on disk and no compaction or retention rule changes.</li><li>A drop is reported once on the <code>agent</code> log channel with the session and the ids, so the next report of this names the writer instead of only the provider&#39;s sentence. See <code>03-runtime/02-agent-runtime.md</code> §5.</li><li>The guard is deliberately the request boundary rather than the history rebuild: it also covers a duplicate that appears while the session runs, which a rebuild-time filter cannot see.</li></ul><h2 id="_2026-09-21-—-a-plugin-crash-reports-its-exit-code-without-copying-raw-output-d607-issue-747" tabindex="-1">2026-09-21 — A plugin crash reports its exit code without copying raw output (D607, issue #747) <a class="header-anchor" href="#_2026-09-21-—-a-plugin-crash-reports-its-exit-code-without-copying-raw-output-d607-issue-747" aria-label="Permalink to &quot;2026-09-21 — A plugin crash reports its exit code without copying raw output (D607, issue #747)&quot;">​</a></h2><ul><li>The host-process crash path reported only the plugin id, so a report read <code>plugin host process exited: &lt;id&gt;</code> and carried nothing else — the reporter in issue #747 had exactly that sentence and no way to say more. The exit code it already had is now part of the message, the <code>failed</code> service state, the <code>plugin.crash</code> audit record and the <code>plugin</code> log channel; on Windows a hard fault (<code>0xC0000005</code> and friends, delivered as a negative signed int, printed alongside its unsigned hex form) and a plugin&#39;s own <code>process.exit(1)</code> are different bugs and this is the only field that tells them apart.</li><li>Plugin stdout/stderr is not copied into the load error, crash audit record, or crash log payload because it may contain workspace data or secrets. Nothing new is persisted and no permission or API surface changes; the existing <code>plugin.stdio</code> audit stream is otherwise unchanged.</li><li>See <code>07-plugins/05-plugin-lifecycle.md</code> §3.1.</li></ul><h2 id="_2026-09-20-—-the-create-project-name-defaults-to-the-primary-folder-d609" tabindex="-1">2026-09-20 — The Create project name defaults to the primary folder (D609) <a class="header-anchor" href="#_2026-09-20-—-the-create-project-name-defaults-to-the-primary-folder-d609" aria-label="Permalink to &quot;2026-09-20 — The Create project name defaults to the primary folder (D609)&quot;">​</a></h2><ul><li>The Create project dialog required a typed name before Create became available, so a local folder pick could not be confirmed without inventing a title; the git source already seeded the field from the repository name (D438, ADR 0273).</li><li>The name field is now optional for both sources. It seeds from the picked source — the first selected folder&#39;s name for a local pick, the repository name for a git checkout — and stops following the source as soon as the user types their own name. Create is gated by the source alone (one folder, or a parsed URL plus a destination), and a field left empty falls back to the same derived name.</li><li>Derived names are cut to <code>MAX_PROJECT_NAME_CHARS</code> (80) so the persisted sidebar preference and the name field accept them unchanged. Folder-name parsing moves to <code>apps/desktop/src/lib/project-name.ts</code> and is shared with the dialog&#39;s folder rows.</li><li>Renderer only: no protocol, storage, host, permission, or migration change, and no new default. See <code>04-ux/08-component-spec.md</code>, ADR 0233, ADR 0273, and E2E-012a / E2E-256.</li></ul><h2 id="_2026-09-21-—-a-stored-model-binding-array-is-read-entry-by-entry-d610-issue-784" tabindex="-1">2026-09-21 — A stored model binding array is read entry by entry (D610, issue #784) <a class="header-anchor" href="#_2026-09-21-—-a-stored-model-binding-array-is-read-entry-by-entry-d610-issue-784" aria-label="Permalink to &quot;2026-09-21 — A stored model binding array is read entry by entry (D610, issue #784)&quot;">​</a></h2><ul><li><code>config_json.models</code> is the only place a provider&#39;s model list lives, and it has more than one writer: a settings save, a plugin declaration, and an external edit of the stored row. It was decoded as a single <code>Vec&lt;ModelBinding&gt;</code>, so one entry that no longer matched the schema discarded every sibling — after which the provider read back as the single legacy default model derived from <code>defaultModelId</code>, every other configured model gone, and nothing said about why. The report reproduced it by deleting one binding&#39;s <code>maxTokens</code>: 12 stored bindings read back as 1.</li><li><code>contextWindow</code> and <code>maxTokens</code> are therefore optional on the wire, and each entry of the array is decoded on its own. An absent key, or an explicit <code>0</code>, reads as zero and is seeded with the generic default (128,000 / 8,192) by the normalisation that already existed for the explicit zero — the same value a record carrying only <code>defaultModelId</code> is materialized with, and the same value a plugin manifest that declares no limits already produces. The two paths now agree instead of one rejecting what the other accepts.</li><li>An entry that still fails to decode costs itself, not the array: the readable entries survive in their stored order, and the failure is reported on the host log with the provider id, the entry&#39;s index and the reason, so it is locatable rather than silent. An absent <code>models</code> key, an empty array, and an array whose every entry was unreadable all still read as the legacy binding, so a provider stays selectable whatever its stored shape; only the third is reported, because an empty array is a legal state and an absent one predates bindings.</li><li>Nothing is rewritten on disk and the write path remains explicit: <code>providers.update</code> still persists exactly the bindings the client sends. Before accepting an explicit model-array replacement, the host checks the stored value and rejects it with <code>MODEL_BINDINGS_DEGRADED</code> when the value is degraded, so a partial settings view cannot erase unreadable entries; unrelated provider fields remain updatable. See <code>03-runtime/12-provider-config-schema.md</code> §2.</li></ul><h2 id="_2026-09-21-—-a-hand-typed-custom-model-id-is-seeded-from-the-model-library-d611" tabindex="-1">2026-09-21 — A hand-typed custom model id is seeded from the model library (D611) <a class="header-anchor" href="#_2026-09-21-—-a-hand-typed-custom-model-id-is-seeded-from-the-model-library-d611" aria-label="Permalink to &quot;2026-09-21 — A hand-typed custom model id is seeded from the model library (D611)&quot;">​</a></h2><ul><li>Adding a custom model by id gave every binding the generic 128,000 / 8,192 seed and no thinking levels, even when the model library already published that id, so the user had to retype limits the app could have known.</li><li>The settings picker now matches the typed id against models.dev through a new snapshot-only renderer channel, <code>providers.lookupModel</code>, and seeds the binding the way a picked model is seeded — published context window, max output tokens, and thinking levels — while the stored id stays exactly what the user typed. The row is written first with the generic seed and upgraded in place when the answer arrives, so a slow, failed, or unpublished lookup still leaves exactly one usable row, and an edit or delete made meanwhile is never overwritten.</li><li>No provider network request, host call, schema, or persisted-data change; a miss behaves exactly as before. See <code>03-runtime/12-provider-config-schema.md</code> §2 and §9.</li></ul><h2 id="_2026-09-21-—-adding-a-provider-never-takes-over-an-app-default-d612" tabindex="-1">2026-09-21 — Adding a provider never takes over an app default (D612) <a class="header-anchor" href="#_2026-09-21-—-adding-a-provider-never-takes-over-an-app-default-d612" aria-label="Permalink to &quot;2026-09-21 — Adding a provider never takes over an app default (D612)&quot;">​</a></h2><ul><li>The provider add flow wrote <code>defaultProviderId</code>/<code>defaultModelId</code>, and the image flow wrote <code>imageGeneration</code>, whether or not the app already had one, so configuring a second service silently moved the default away from the model the user was running.</li><li>The add flow now follows the rule editing already followed: a stored default survives while it still resolves, and only an unresolvable one is replaced. For the model default that means the default provider row still exists and still offers the model — the same resolution the settings summary renders through — so an id left behind by a deleted provider is not a default and a newly added provider may fill what would otherwise render as unset. The image default uses the mirrored rule against its stored binding, and its candidate list still accumulates the new provider&#39;s image models, so nothing vanishes from the picker.</li><li>Settings are written only when no default resolves; the explicit &quot;make default&quot; action, the edit path, and the fallback to the first remaining binding after removing the selected model are unchanged. Behaviour is pinned by <code>apps/desktop/test/default-model-display.test.mjs</code> and <code>apps/desktop/test/image-generation-default.test.mjs</code>, with <code>provider-model-config.test.mjs</code> asserting the add branch consults them.</li></ul><h2 id="_2026-09-22-—-the-loop-owns-its-context-array-d620" tabindex="-1">2026-09-22 — The loop owns its context array (D620) <a class="header-anchor" href="#_2026-09-22-—-the-loop-owns-its-context-array-d620" aria-label="Permalink to &quot;2026-09-22 — The loop owns its context array (D620)&quot;">​</a></h2><ul><li>A turn whose later iterations ran tools left <code>state.messages</code> holding two copies of every message those iterations appended, and the next turn&#39;s first request was assembled from that array. The duplicate of an assistant message that carried text plus a tool call survived the request guard (D608) as a text-only clone sitting between the call and the result answering it; pi-ai then closed the still-pending call with a synthesized &quot;No result provided&quot; output next to the real one, so one call id carried two outputs and the endpoint rejected the whole turn with <code>Duplicate tool output for call_id</code> (HTTP 400, not retriable). The array is reused, so every following turn failed the same way and <code>继续</code> could not recover the session.</li><li>pi-ai&#39;s loop appends each streamed assistant message and each tool result to the context it was handed, while its own <code>message_end</code> listener appends the same message object to <code>state.messages</code>. <code>rebuiltAgentContext()</code> returned that live array, so both appends landed in one array; it now returns a copy, exactly like pi&#39;s own <code>createContextSnapshot()</code> does for <code>prompt()</code> and <code>continue()</code>. The delegate&#39;s turn boundary handed over the same live array (<code>subagent.ts</code> <code>prepareNextTurn</code>) and now copies too. The loop&#39;s view and <code>state.messages</code> carry the same messages in the same order at each message boundary, they are simply no longer the same array — which is what keeps the duplicates out of the next request.</li><li>The request guard at <code>convertToLlm</code> (D608) now matches what the provider validates: call and result ids are compared by their wire-visible part, a result whose item half disagrees with its call is paired to the call&#39;s id, and an assistant message whose tool calls were all already claimed is dropped whole instead of being kept as the clone that separates a call from its result. The drop log line also states how many messages were removed. A message that replays one claimed call while carrying a new one is left in place with its new call, the only id sharing the guard accepts; no writer reaches that shape.</li><li>No provider transport, host protocol, version, storage migration, transcript rewrite, or compaction/retention/budget rule changes. <code>03-runtime/02-agent-runtime.md</code> §5c and E2E-RUNTIME-loop-context-ownership record the contract and its coverage.</li></ul><h2 id="_2026-09-21-—-bound-desktop-trusted-extension-lifecycle-waits" tabindex="-1">2026-09-21 — Bound Desktop trusted-extension lifecycle waits <a class="header-anchor" href="#_2026-09-21-—-bound-desktop-trusted-extension-lifecycle-waits" aria-label="Permalink to &quot;2026-09-21 — Bound Desktop trusted-extension lifecycle waits&quot;">​</a></h2><ul><li>Apply the existing 30-second handler budget to notification, startup and shutdown handlers as well as result handlers; module loading and factory initialization each use the same budget. This changes previously unbounded waits, including event handlers waiting for a UI answer.</li><li>Abort retires current waits; disposal rejects new dispatches and runs shutdown once. Late settlements cannot supply results to the retired dispatch. Existing fail-open error handling, result folding, plugin ownership and permissions remain unchanged. In-process code is not forcibly terminated.</li><li>Deferred events remain registrable and now appear in existing diagnostics. See <code>07-plugins/16-trusted-extensions.md</code> §6 and <code>E2E-HOOKS-cancel-and-dispose</code>.</li></ul><h2 id="_2026-09-22-—-an-unreadable-command-source-refuses-a-slash-submission-d613-issue-795" tabindex="-1">2026-09-22 — An unreadable command source refuses a slash submission (D613, issue #795) <a class="header-anchor" href="#_2026-09-22-—-an-unreadable-command-source-refuses-a-slash-submission-d613-issue-795" aria-label="Permalink to &quot;2026-09-22 — An unreadable command source refuses a slash submission (D613, issue #795)&quot;">​</a></h2><ul><li>Composer send-time resolution read the merged command list and swallowed a failure as <code>null</code>, which the submit path could not tell apart from &quot;no such command&quot;. <code>/compact</code> typed while that IPC read failed was therefore sent to the model as literal prompt text, and the model acted on it as an instruction.</li><li>Resolution now answers with three outcomes instead of one nullable value: resolved (builtin/plugin/extension dispatch), unknown (templates, aliases, and id-less entries continue as prompt text), and unavailable. Unavailable refuses the submission, keeps the draft, and shows <code>chat.slashCommandSourceUnavailable</code>. The failed read leaves the TTL cache cold, so the next submit retries it; a warm cache still resolves through a source blip.</li><li>The refusal is fail-closed on purpose: only the command source can say whether <code>/name</code> is a control command, so while it cannot be read a <code>name</code> that looks like plain text is refused rather than guessed. Templates and genuinely unknown aliases keep the old prompt path. Pinned by <code>apps/desktop/test/slash-command-source.test.mjs</code> and <code>03-runtime/01-ipc-protocol.md</code> §13c.</li></ul><h2 id="_2026-09-22-—-a-manual-compaction-has-its-own-transport-deadline-and-a-durable-verdict-d614-issue-795" tabindex="-1">2026-09-22 — A manual compaction has its own transport deadline and a durable verdict (D614, issue #795) <a class="header-anchor" href="#_2026-09-22-—-a-manual-compaction-has-its-own-transport-deadline-and-a-durable-verdict-d614-issue-795" aria-label="Permalink to &quot;2026-09-22 — A manual compaction has its own transport deadline and a durable verdict (D614, issue #795)&quot;">​</a></h2><ul><li><code>agent.compact</code> is a blocking RPC that spends a whole model summary request inside the sidecar: pi serializes the conversation, streams the summary, and retries a transient failure. It ran under the flat 130s transport default, so a ~158s compaction on an 888KB context ended as <code>sidecar RPC timeout</code> while the sidecar kept working and persisted the checkpoint — the user was told the compaction failed, and the next turn proved it had succeeded.</li><li>The deadline is now derived from the ceilings the sidecar actually enforces: one stream watchdog (180s) per attempt, <code>1 + 3</code> attempts, the 2s/4s/8s retry backoff, and transport slack — <code>AGENT_COMPACT_RPC_TIMEOUT_MS</code>, deliberately per-method rather than a wider global default.</li><li>Either host path (Electron <code>agentCompact</code> IPC and <code>RuntimeService.compact</code>) also stops treating a transport timeout as the sidecar&#39;s verdict: it re-reads the durable <code>session.compaction</code> record and reports success when a new checkpoint landed, logs the mismatch, and rethrows the timeout otherwise. A verdict the sidecar reported itself is never reconciled. Pinned by <code>packages/host-runtime/src/runtime-service.test.ts</code>, <code>packages/shared/src/protocol.test.ts</code>, <code>apps/desktop/test/plugin-timeout-budgets.test.mjs</code>, and <code>03-runtime/01-ipc-protocol.md</code> §5.4.</li></ul><h2 id="_2026-09-22-—-an-empty-transcript-read-is-retried-and-never-cached-d615-issue-795" tabindex="-1">2026-09-22 — An empty transcript read is retried and never cached (D615, issue #795) <a class="header-anchor" href="#_2026-09-22-—-an-empty-transcript-read-is-retried-and-never-cached-d615-issue-795" aria-label="Permalink to &quot;2026-09-22 — An empty transcript read is retried and never cached (D615, issue #795)&quot;">​</a></h2><ul><li>The renderer treated every durable <code>session.get</code> window as the truth, and cached it. A window that came back empty for a session with thousands of messages — the host answers such a read from a transcript file it may be rewriting — was stored as an empty snapshot, hover prefetch re-served it, and the pane stayed blank until the app restarted. Nothing distinguished a failed or stale read from an empty conversation, and no path retried one.</li><li>A read is now judged against the session&#39;s own count: zero messages for a session the sidebar counts as having history triggers one more read, then keeps the snapshot the user already has, and otherwise reports <code>chat.sessionTranscriptEmpty</code>. The empty page is never written to the transcript cache, so a hover prefetch cannot poison every later open.</li><li>This is a defense, not the host-side root cause: the transcript rewrite is the reporter&#39;s 0.15.1 storage layout, and the read side still answers &quot;session exists, no messages&quot; instead of reporting an unreadable transcript. See <code>04-ux/09-interaction-patterns.md</code> §Session isolation across tabs.</li></ul><h2 id="_2026-09-22-—-a-renderer-that-never-reaches-its-first-state-gets-a-bounded-surface-and-a-quit-channel-d616" tabindex="-1">2026-09-22 — A renderer that never reaches its first state gets a bounded surface and a quit channel (D616) <a class="header-anchor" href="#_2026-09-22-—-a-renderer-that-never-reaches-its-first-state-gets-a-bounded-surface-and-a-quit-channel-d616" aria-label="Permalink to &quot;2026-09-22 — A renderer that never reaches its first state gets a bounded surface and a quit channel (D616)&quot;">​</a></h2><ul><li>The renderer&#39;s startup had no timeout of its own: <code>bootstrap()</code> either publishes the initial state or nothing, so one read that never settled left the window on the boot surface with no menu, no data, and nothing to act on except force-quitting the process (issue #831). The renderer now watches its own wait. <code>STARTUP_SLOW_HINT_MS</code> (30s) adds logs, diagnostics, and quit to the boot surface without calling the boot a failure; <code>STARTUP_STALLED_MS</code> (180s) turns it into the recovery surface, which also offers a retry that re-runs <code>bootstrap()</code>.</li><li>Both bounds sit above the main↔host RPC ceiling (<code>DEFAULT_RPC_TIMEOUT_MS</code>, 130s), so a slow but successful boot is never read as a failure, and the watchdog never cancels the startup it watches: a boot that finishes replaces the surface with the shell. The recovery surface replaces the splash (exactly one boot surface is mounted), and the renderer-drawn window controls stay above it, so a frameless Windows/Linux window can always be closed.</li><li>The renderer gains the quit channel <code>pi-desktop/app/quit</code> (<code>api.quitApp()</code>), whose handler calls <code>app.quit()</code> exactly like the Quit menu item, so the ordered shutdown, the confirmation dialog, and the close behavior stay the ones the app already has. See <code>03-runtime/07-process-model.md</code> §3 and E2E-076.</li></ul><h2 id="_2026-09-22-—-a-failed-compaction-keeps-the-recent-window-d617-issue-827" tabindex="-1">2026-09-22 — A failed compaction keeps the recent window (D617, issue #827) <a class="header-anchor" href="#_2026-09-22-—-a-failed-compaction-keeps-the-recent-window-d617-issue-827" aria-label="Permalink to &quot;2026-09-22 — A failed compaction keeps the recent window (D617, issue #827)&quot;">​</a></h2><ul><li>An automatic compaction whose summary request failed installed a retained-tail checkpoint that carried at most the latest user message, and a rebuild narrowed a stored checkpoint&#39;s tail the same way. A long turn therefore reached the next model request as one user sentence: the reported session lost roughly 168 messages (3 user / 34 assistant / 131 tool) between two checkpoints while the transcript on disk and the UI row stayed intact.</li><li>The fallback now retains the real recent window — the newest contiguous messages of the compacted range, every role, bounded by the keep-recent target and by what the carried summary and the recovery notice leave — and marks it with <code>details.retainedTailShape</code>, so a rebuild replays it. A checkpoint without the marker (every successful checkpoint, and every record written before it existed) still normalizes to the latest user message. An <code>active_turn</code> fallback also keeps the active task&#39;s user message when the window cannot hold it, the fallback drops the assistant messages pi drops anyway and any tool result whose call is not in the window, and <code>details.failureReason</code> records <code>no_new_history</code> / <code>summary_budget</code> / <code>summary_provider</code> / <code>checkpoint_oversized</code> instead of provider error text.</li><li>A summary prompt that is still too large after the single reduced pass is no longer skipped: the range is split into contiguous chunks that each fit, at most 16 requests, each carrying the previous chunk&#39;s summary, and the checkpoint reports the summed usage of the requests that produced it. Only an empty range, or one past that request bound, still falls back on budget grounds. See ADR 0302, <code>03-runtime/02-agent-runtime.md</code>, ADR 0049, ADR 0282.</li></ul><h2 id="_2026-09-20-—-a-copied-formula-is-its-tex-source-d619-issue-414" tabindex="-1">2026-09-20 — A copied formula is its TeX source (D619, issue #414) <a class="header-anchor" href="#_2026-09-20-—-a-copied-formula-is-its-tex-source-d619-issue-414" aria-label="Permalink to &quot;2026-09-20 — A copied formula is its TeX source (D619, issue #414)&quot;">​</a></h2><p>Math boundaries remain parseable after copying: touching inline fences get one separator, and every prose dollar in the copied text is escaped, together with backslash runs that would otherwise escape a fence. Annotation whitespace is preserved; widened multiline inline math uses a literal <code>&lt;span&gt;</code> wrapper to prevent a flow opener when pasted at column zero. TeX newlines are not flattened because they can terminate <code>%</code> comments. The wrapper is Markdown source in <code>text/plain</code>, not a <code>text/html</code> payload; compatibility with external editors that disallow inline HTML is not promised. Regression coverage checks both copy entry points and Markdown round trips for adjacent formulas, prose dollars on either side, formatting wrappers, line/block boundaries, padding, and multiline math including TeX comments. Display math containing <code>- x</code>, <code>+ x</code>, <code>* x</code>, <code>&gt; x</code>, or internal blank lines must remain a single math node after copying. The shared remark grammar keeps unclosed math in the streaming tail until its fence closes; subsequent prose and its source offsets remain intact. A footnote whose definition follows its reference — adjacently or streamed in later — renders as a real reference and note section rather than a literal <code>[^1]</code>, which is what block splitting costs when a slice is parsed without the rest of the message. CRLF, ordinary lists, quotes, GFM tables, fenced code and the sources that must keep splitting are covered by <code>markdown-blocks.test.mjs</code>.</p><ul><li>KaTeX paints one formula twice — a MathML tree for assistive technology and a visual tree of positioned spans — and both are real text in the document. The platform&#39;s own copy therefore wrote a formula out as its glyphs, twice over, and never as the source the answer was written in.</li><li>A copy whose selection covers rendered math now reads the TeX back out of the MathML <code>annotation</code>: <code>$…$</code> inline, <code>$$…$$</code> on its own lines. Those are the delimiters <code>lib/latex-math.ts</code> normalizes <code>\\(…\\)</code> and <code>\\[…\\]</code> to before <code>remark-math</code> runs, so a pasted formula renders as the one it came from. A cut that lands inside a formula grows to the whole formula, because half a TeX expression is not one and a cut between the two trees would take the source from one and the glyphs from the other.</li><li>Each delimiter run is widened past any run inside the formula, the way a code span&#39;s fence is, because <code>$\\$5 + x$</code> closes at the escaped dollar and pastes back as prose. Inline stays the narrow run otherwise, and not for the reason it first looks like: a single-line <code>$$…$$</code> never opens a display block, since math flow forbids <code>$</code> in the meta after its opening fence. It stays narrow because an inline formula&#39;s TeX can carry a newline — only the <code>\\(…\\)</code> path has its newlines flattened by the normalization — and a <code>$$</code> run would then land at the start of a line with the rest of the formula behind it, which is a flow opening and swallows the paragraph.</li><li>Display math is not always a block here. <code>normalizeLatexMathDelimiters</code> rewrites <code>\\[ … \\]</code> in place — the rewrite is length-preserving — so the math node stays inline and <code>remarkLatexBracketDisplay</code> promotes it, leaving KaTeX to paint <code>.katex-display</code> inside the sentence&#39;s own paragraph. The reduction puts its paragraph slot there unchanged; a <code>p</code> nested in a <code>p</code> is invalid as markup and harmless here, because the clone is only ever read through the text serializer, which treats the nested block as one boundary. One newline above <code>$$</code> is enough for <code>remark-math</code> to open the display block, so the formula pastes back as the display formula it came from. Both <code>\\[ … \\]</code> shapes are pinned by E2E-CHAT-copy-formula-as-tex.</li><li>A table cell is the one place the block slot had to be measured rather than reasoned about, and the measurement is recorded because the reasoning points the wrong way. <code>remarkLatexBracketDisplay</code> promotes a <code>\\[ … \\]</code> node wherever it sits, cells included, so <code>.katex-display</code> really does land inside a <code>&lt;td&gt;</code> — and the table serializer joins cells with tabs, which makes it look as though the <code>$$</code> fence there would be closed by a tab and swallow the rest of the row (<code>a\\tb\\n$$\\nE = mc^2\\n$$\\t2</code> is one math node whose value runs to the end of the line). Chromium writes no such string: a block box is a block boundary inside a cell too, so the row breaks around it, the fence closes on its own line, and the neighbouring cell arrives on one of its own. The slot is not inventing that boundary either — the platform&#39;s own reading of the same row has no tab in it, because <code>.katex-display</code> breaks the row for it as well — so keeping the display form here is the parity the rest of this decision rests on rather than an exception to it. Narrowing to the inline run in a cell was considered and rejected: it would pay display math to repair a shape the serializer does not produce, and it would put a tab back that the platform itself does not write. E2E-CHAT-copy-formula-as-tex asserts both halves, so a Chromium that started tab-joining a cell holding a block would fail there rather than in a user&#39;s clipboard.</li><li>Only the formulas are rewritten. Everything else in the selection — prose, lists, tables, code blocks — is serialized by the platform itself: the reduced clone is selected and read back through <code>Selection.toString()</code>, the serializer a copy actually runs. A hand-written walk of the tree could only approximate those whitespace rules, and everywhere it fell short it would silently rewrite content the platform already got right: source line wrapping put back into a paragraph, blank lines between list items, a code block&#39;s own blank lines collapsed.</li><li>The serializer has to be the copy&#39;s own, not a near neighbour. <code>innerText</code> reads almost identically and has no notion of <code>user-select</code>, so it writes out the chrome <code>base.css</code> marks inert — a code block&#39;s language rail reaching the clipboard as a stray <code>js</code> line. Restating that rule inside the reduction would be the hand-written walk this decision rejects; running the platform&#39;s own serializer makes the parity exact instead of approximate. The clone is read inside the element the selection came from rather than parked on <code>body</code>, so the cascade deciding that reading is the live one; restating the selection contract on the clone instead (an inline <code>user-select: text</code>) inverts it, because the shell is unselectable by default and document-like surfaces opt back in — chrome that is inert only by inheritance would arrive in the clipboard.</li><li>Every other selection is left alone: a selection with no formula in it is the platform&#39;s business. There is no second test of whether the copy &quot;belongs to&quot; the selection, because Chromium derives the event target from the selection itself, so a non-collapsed selection always raises its copy inside itself; a target check only rejects ranges built with <code>selectNodeContents</code>, the transcript&#39;s own Select text among them. A copy raised in the composer cannot carry a stale transcript selection either — a frame holds one selection, so focusing a field collapses it, and a collapsed selection is already declined.</li><li>One clipboard flavour, <code>text/plain</code>. Taking the copy over drops the platform&#39;s <code>text/html</code> as well, and none is written back. The reduced clone is the app&#39;s own markup: serializing it would carry the chrome <code>base.css</code> marks <code>user-select: none</code> — a code block&#39;s <code>js</code> rail and its copy button — that the text reading drops, plus <code>data-source-*</code> bookkeeping and a display slot nested in a paragraph, which is a second and worse reading of one selection. Writing the rendering back instead is not an option either: KaTeX&#39;s stylesheet is the only thing that hides the MathML tree, and no stylesheet travels on the clipboard, so a rich paste target would show every formula twice — the very duplication this decision removes. Leaving the flavour out settles both: a rich paste target falls back to the plain text, which is the source. The cost is that a selection holding a formula pastes into a rich target without the tag-level formatting the platform&#39;s own <code>text/html</code> would have carried; the source is what the copy is for.</li><li>One document <code>copy</code> listener, installed and removed by the shell (<code>hooks/use-copy-tex.ts</code>), because <code>Markdown</code> renders no wrapper of its own — answers, a work-panel file preview and a plugin readme reach the same formulas through different parents — and because a copy is a document gesture. The transcript&#39;s right-click Copy reads the same selection through the same module, so the two entry points cannot put two readings of one selection on the clipboard.</li><li>Splitting a message into independently parsed blocks, which predates D619 and which D619 moved onto the rendering grammar, is only sound for a slice that needs no other slice&#39;s parse context. A link or footnote definition is exactly the counterexample: it resolves across the whole message, and a footnote also numbers, reuses and back-links across it, so a reference parsed without its definition survives as the literal <code>[^1]</code> and the note is dropped. A message declaring a definition anywhere therefore renders undivided. Injecting definitions into every slice was rejected: it renumbers footnotes per slice and aims the back-links at the wrong reference. The accepted cost is that such a message loses per-block memoization while it streams, which is the cost the renderer carried before it split blocks at all and which its rarity in chat answers keeps bounded.</li><li>D619 adds no surface, no store state, and no action-row item: it is the OS clipboard that ADR 0268 already rested the removal of quotes on, made honest. Renderer only — no IPC channel, host protocol, storage schema, permission, Plugin SDK, or i18n key changed. See <code>04-ux/08-component-spec.md</code> §8.7 and E2E-CHAT-copy-formula-as-tex.</li></ul><h2 id="_2026-09-22-—-pi-s-file-op-collector-is-fed-the-names-it-reads-d618-issue-827" tabindex="-1">2026-09-22 — pi&#39;s file-op collector is fed the names it reads (D618, issue #827) <a class="header-anchor" href="#_2026-09-22-—-pi-s-file-op-collector-is-fed-the-names-it-reads-d618-issue-827" aria-label="Permalink to &quot;2026-09-22 — pi&#39;s file-op collector is fed the names it reads (D618, issue #827)&quot;">​</a></h2><ul><li>A checkpoint&#39;s <code>readFiles</code> / <code>modifiedFiles</code>, and the <code>&lt;read-files&gt;</code> section a summary appends, come from pi&#39;s own <code>extractFileOpsFromMessage</code>, which switches on the lowercase names <code>read</code> / <code>write</code> / <code>edit</code> — the names pi&#39;s tools carry. PI-Desktop registers <code>Read</code> / <code>Write</code> / <code>Edit</code>, so the collector matched nothing and every checkpoint reported an empty file list; issue #827 turned this up while reading a compaction report.</li><li>The conversion stays on our side of the boundary. <code>withPiFileOpToolNames</code> copies the entries handed to pi&#39;s <code>prepareCompaction</code> with exactly those three names respelled; no stored byte changes — a transcript, a checkpoint and a rebuilt context keep our names — and the input array comes back untouched when no such call is present, so the common path allocates nothing.</li><li>Only those three are converted. pi&#39;s collector reads nothing else, so <code>Grep</code>, <code>Glob</code>, <code>Bash</code>, plugin and MCP names keep the spelling we register, and the summarized text changes only where pi consumes the name.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("spec/08-meta/decisions-log.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const decisionsLog = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  decisionsLog as default
};
