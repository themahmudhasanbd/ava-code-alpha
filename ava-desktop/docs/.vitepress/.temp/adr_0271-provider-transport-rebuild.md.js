import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0271: Rebuild the shared provider transport after repeated unanswered failures","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0271-provider-transport-rebuild.md","filePath":"adr/0271-provider-transport-rebuild.md","lastUpdated":1789914100000}');
const _sfc_main = { name: "adr/0271-provider-transport-rebuild.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0271-rebuild-the-shared-provider-transport-after-repeated-unanswered-failures" tabindex="-1">ADR 0271: Rebuild the shared provider transport after repeated unanswered failures <a class="header-anchor" href="#adr-0271-rebuild-the-shared-provider-transport-after-repeated-unanswered-failures" aria-label="Permalink to &quot;ADR 0271: Rebuild the shared provider transport after repeated unanswered failures&quot;">​</a></h1><ul><li>Status: Accepted for implementation</li><li>Date: 2026-09-16</li><li>Related: <a href="./../spec/03-runtime/08-error-codes">Error codes</a> · <a href="./../spec/03-runtime/02-agent-runtime">Agent runtime</a> · <a href="./0212-remove-diagnostic-timing-log-streams">ADR 0212</a> · issue #234</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>One turn on a long session retried a Codex request ten times over ~112 seconds and reported <code>NETWORK_ERROR: fetch failed</code> for every attempt; a new turn on the same provider, model, and network succeeded immediately. The log record carried <code>phase=stream</code>, <code>streamMs=1~2</code>, and <code>retryAttempt=10</code>, and nothing else.</p><p>Two readings of that evidence were possible, and the first one was wrong:</p><ul><li><code>phase=stream</code> with a two-millisecond stream looks like a failure <em>after</em> the response headers arrived, which would mean the connection worked and the retry hypothesis is empty.</li><li>The pair is actually the fingerprint of the opposite: pi-agent-core emits <code>message_start</code> for a stream that ended <strong>without</strong> ever emitting <code>start</code>, so <code>streamMs</code> measures the synthetic start/end pair, not a started stream. With <code>providerWaitMs=112442</code> covering the whole retry loop and each attempt spending seconds in <code>phase=request</code> retries, every one of the ten attempts died before any response.</li></ul><p>The consequence of the wrong reading was that the shared transport was never touched: <code>node-proxy.ts</code> installs one process-wide undici dispatcher (a <code>ProxyAgent</code>, a SOCKS5 agent, or the plain agent) and <code>closeActiveDispatchers()</code> ran only when settings changed. A pooled connection that died without the pool noticing therefore stayed in use for the whole retry budget, and the first-hand <code>error.cause</code> — where node and undici keep <code>ENOTFOUND</code>, <code>ECONNRESET</code>, <code>UND_ERR_SOCKET</code>, or a TLS code — was already flattened into an <code>errorMessage</code> string by pi-ai before classification, so the log could only say <code>networkCategory: unknown</code>.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>Describe the rejected provider fetch where the original Error still exists: the fetch wrapper in <code>provider-retry.ts</code> runs <code>describeNetworkFailure</code> on the live cause chain and reports the same validated fields a directly classified network error carries (<code>networkCategory</code>, <code>networkCode</code>, <code>networkSyscall</code>, <code>networkHost</code>) instead of letting a bare <code>fetch failed</code> fall back to <code>unknown</code>. Field shapes, bounds, and redaction stay owned by <code>agent-errors.ts</code>; no new error code and no new locale string is introduced.</li><li>Report the transport route (<code>networkRoute</code>: <code>direct</code>, <code>environment-proxy</code>, <code>http-proxy</code>, <code>socks5-proxy</code>) with the diagnosis, so a failure at the proxy hop is readable without inferring it from an errno.</li><li>Report <code>phase: request</code> for a failure that never received a response. The phase describes what actually happened to the attempt, not which message lifecycle surfaced it.</li><li>Rebuild the shared transport when one origin fails this way <strong>repeatedly</strong>: after the second consecutive unanswered failure for the same origin inside a turn, once per streak, at most once every 30 seconds process-wide, never for a <code>dns</code> failure (name resolution happens before a socket exists). Rebuild means rebuilding the configured route from the settings already applied.</li><li>Swap before closing: the replacement dispatcher is installed, then the previous one is closed with <code>close()</code> — never <code>destroy()</code> — and the captured original-dispatcher reference is repointed if the closed pool was it. In-flight requests finish on the pool they were dispatched on.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>The next occurrence of issue #234 is diagnosable: the log names the layer and the route, and the retry indicator names the errno.</li><li>A turn can now spend one of its retry attempts on a fresh pool, so a transport that never recovers on its own stops being reused. A rebuild does not change the retry budget, the backoff curve, or the error classification.</li><li>Other sessions can observe a rebuild: their idle pooled sockets are closed, so their next request pays a new TCP/TLS handshake. That is a latency cost, never a correctness cost, and the 30-second throttle plus the per-origin streak bound it.</li><li>A transport failure during a <code>dns</code> outage triggers no rebuild, which is deliberate: throttling the pool would spend the cost for no possible benefit.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Rebuild on every failure.</strong> Rejected: the dispatcher is process-wide, so one session&#39;s single transient failure would churn every other session&#39;s pool, and a long outage would rebuild once per attempt.</li><li><strong>Never rebuild.</strong> Rejected: it leaves a repeated unanswered failure on the same origin with no recovery path, which is exactly the reported behavior.</li><li><strong>Per-session dispatchers.</strong> Rejected for this change: it changes the ownership model of the transport and the proxy/bypass composition for every provider request, which is a larger and separate architectural decision.</li><li><strong>Retain the raw cause message and its <code>name</code>.</strong> Rejected: it would put free-form provider text (URLs, header values, query strings) into logs and the transcript for a small diagnostic gain over the errno, the category, and the route.</li></ul><h2 id="amendment-certificate-verification-issue-714" tabindex="-1">Amendment: certificate verification (issue #714) <a class="header-anchor" href="#amendment-certificate-verification-issue-714" aria-label="Permalink to &quot;Amendment: certificate verification (issue #714)&quot;">​</a></h2><p>Recognized certificate verification errors also skip rebuilding. Unlike the broader TLS/protocol category, these cannot be repaired by replacing a socket pool. See <a href="./provider-system-certificates">system certificate trust</a>.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0271-provider-transport-rebuild.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0271ProviderTransportRebuild = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0271ProviderTransportRebuild as default
};
