import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR: Separate Subagent Model Opt-In from Definition Pins","description":"","frontmatter":{},"headers":[],"relativePath":"adr/subagent-model-opt-in.md","filePath":"adr/subagent-model-opt-in.md","lastUpdated":1790056329000}');
const _sfc_main = { name: "adr/subagent-model-opt-in.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-separate-subagent-model-opt-in-from-definition-pins" tabindex="-1">ADR: Separate Subagent Model Opt-In from Definition Pins <a class="header-anchor" href="#adr-separate-subagent-model-opt-in-from-definition-pins" aria-label="Permalink to &quot;ADR: Separate Subagent Model Opt-In from Definition Pins&quot;">​</a></h1><ul><li>Status: Accepted for implementation</li><li>Date: 2026-09-13</li><li>Related: D278, ADR 0062, ADR 0089, ADR 0237, E2E-166, issues #286, #386</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Electron resolves definition pins and automatically selectable models into one credential-bearing <code>subagentProviders</code> map. Treating every map key as a <code>Task.model</code> selection both advertises private pins and bypasses main&#39;s <code>availableForSubagents</code> check on the on-demand path. A definition pin expresses one delegate&#39;s default, not permission to select that model for any delegate.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Add an optional <code>subagentModelKeys: string[]</code> field to the internal Electron-to- sidecar launch payload, runtime options, and reuse comparison. Main includes only successfully resolved bindings independently marked <code>availableForSubagents</code> on enabled providers. Reusing a resolved pin does not skip this independent opt-in decision. <code>subagentProviders</code> continues to carry all resolved bindings so existing definition defaults keep working. If an opted-in row&#39;s vendor/model alias collides with a binding from another row, main uses that row&#39;s exact provider ID as the override key; one account&#39;s opt-in cannot authorize another account&#39;s pinned credentials.</p><p>The runtime uses the separate keys for its model summary. Missing keys mean no cached override authorization. Other keys still use the existing main-owned <code>provider.resolveSubagentModel</code> check; only a successful response can populate a separate override cache. That cache is not part of launch reuse matching and must not overwrite a definition pin with a different provider id. On-demand provider matching uses the same unique id / vendor / name rule as pin resolution; ambiguous vendor aliases fail closed unless the caller uses the exact provider id. A changed launch list retires an idle runtime on the next prompt, including after opt-in is revoked. On-demand grants expire at every new parent prompt or approved plan/goal execution, even when the launch list is unchanged. Late responses from an expired turn are discarded. Resume uses the same authorization boundary: only the current definition&#39;s pin/fallbacks, session inheritance, and current override grants may be selected; a remembered key is reauthorized when necessary. Other definitions&#39; pins do not authorize a model-id match (#841).</p><p>The opt-in belongs to the decision, not to the Task tool: it governs every entry point through which the AI picks a model for work it delegates. <code>session/collaboration/spawn</code> is the second such entry point (ADR 0237), and its <code>modelKey</code> is that same selection written by a plugin on the agent&#39;s behalf, so an unopted model is refused with <code>PERMISSION_DENIED</code> before a worker exists. The inheritance half is unchanged: omitting <code>modelKey</code> still takes the first opted-in model and otherwise the default, and naming the default model&#39;s own key is that inheritance spelled out, exactly as repeating a definition&#39;s own pin is on the Task path. <code>models.list</code> keeps reporting every ready model with its <code>availableForSubagents</code> flag — the flag is advice to the caller and authority only in main, which is the side a plugin cannot rewrite (#386).</p><p>D278&#39;s priority remains Task.model → definition pin → session model. The existing exact-session-model exception remains unchanged. Repeating the target definition&#39;s own pin key is treated as omitting <code>model</code>, so catalog echo does not become a tool error. The Task catalog displays each definition default and says that omitting or repeating that key keeps it; this does not prohibit deliberately selecting an opted-in override for a different model.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>No provider credential leaves its existing process boundary. No Host RPC, persisted setting, database schema, or Plugin SDK change is required.</li><li>Older callers that omit the additive field retain definition defaults and session inheritance. Explicit overrides require on-demand authorization; missing authorization support fails closed instead of promoting pins.</li><li>Runtime, model transport, and launch wiring regression coverage prove the boundary. The deterministic sidecar E2E uses a local OpenAI-compatible model fixture; provider UI interaction and external provider quality are separate.</li><li>Reversing model-selection priority is a product decision outside this fix.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/subagent-model-opt-in.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const subagentModelOptIn = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  subagentModelOptIn as default
};
