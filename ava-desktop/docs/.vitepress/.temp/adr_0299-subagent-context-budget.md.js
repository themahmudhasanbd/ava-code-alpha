import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0299: Subagent context budget and delegate compaction","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0299-subagent-context-budget.md","filePath":"adr/0299-subagent-context-budget.md","lastUpdated":1789916690000}');
const _sfc_main = { name: "adr/0299-subagent-context-budget.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0299-subagent-context-budget-and-delegate-compaction" tabindex="-1">ADR 0299: Subagent context budget and delegate compaction <a class="header-anchor" href="#adr-0299-subagent-context-budget-and-delegate-compaction" aria-label="Permalink to &quot;ADR 0299: Subagent context budget and delegate compaction&quot;">​</a></h1><ul><li>Status: Accepted for implementation</li><li>Date: 2026-09-20</li><li>Deciders: PI-Desktop core</li><li>Amends: ADR 0064 (extends its compaction contract to delegates); extends ADR 0062 / ADR 0279</li><li>Related: ADR 0030 (hard boundary), ADR 0049 (retained-tail recovery), ADR 0136 (active task boundary), ADR 0246, ADR 0253, ADR subagent-model-fallback</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Every context protection the session Agent has is wired for the session Agent only. A delegate (<code>Task</code>, ADR 0062) runs the same pi <code>Agent</code> class with none of it:</p><ul><li><code>SubagentRun</code>&#39;s constructor (<code>packages/agent-runtime/src/subagent.ts:214</code>) passes <code>streamFn</code>, <code>getApiKey</code>, <code>convertToLlm</code>, <code>afterToolCall</code> and <code>initialState</code>. It does not pass <code>prepareNextTurnWithContext</code>. That hook has exactly one wiring in the source tree, <code>packages/agent-runtime/src/runtime.ts:1894</code>, and it belongs to the parent session.</li><li><code>subagent.ts</code> has no notion of <code>contextWindow</code>, holds no token budget, and calls no compaction primitive. A delegate&#39;s context grows until the provider rejects the request.</li><li>The parent has three layers the delegate lacks: <code>contextBudget()</code> (<code>runtime.ts:5652</code>, <code>hardLimit = contextWindow − requestHeadroom</code>), a pre-flight compaction in <code>prompt()</code> (<code>runtime.ts:7434</code>), and turn-boundary compaction in <code>prepareNextTurn()</code> (<code>runtime.ts:5839</code>, ADR 0064).</li><li>A delegate cannot compact on its own initiative either: <code>new_context</code> is on <code>SUBAGENT_INHERIT_DENY_TOOLS</code> (<code>packages/shared/src/subagent-definition.ts:117</code>), because executing it sets the <em>parent</em> runtime&#39;s <code>pendingModelCompaction</code> flag. That denial is correct and stays.</li><li>A resumed chain (ADR 0279) is seeded whole: <code>seedDelegateMessages()</code> (<code>packages/agent-runtime/src/delegation-history.ts:311</code>) puts the original task first and appends every converted chain row, de-duplicating the task brief and truncating nothing. The chain-level <code>MAX_RESUMABLE_READ_LINES</code> guard removes an over-read chain from the reusable list; it does not bound what a still-reusable chain seeds.</li><li>On overflow the provider error classifies as <code>CONTEXT_TOO_LARGE</code> with <code>retriable = false</code> (<code>packages/agent-runtime/src/agent-errors.ts:407</code>, <code>:412</code>, <code>:426</code>) and <code>SubagentRun.run()</code> returns <code>result(&quot;failed&quot;, ...)</code> (<code>subagent.ts:275</code>). What reaches the parent model is the provider&#39;s raw English sentence, which names no recovery it can act on.</li><li><code>fallbackModels</code> is the only escape today, and it does not work for this failure: <code>useNextModel()</code> (<code>subagent.ts:302</code>) carries the accumulated context across unchanged, so an alternative whose window is not larger fails identically and burns a model slot doing it.</li></ul><p>ADR 0064 does not mention subagents or delegates anywhere. This is an omission from that design, not a deliberate exclusion: the delegate loop is a second <code>Agent</code> in the same sidecar, running the same provider requests against the same kind of window, and nothing in ADR 0064&#39;s reasoning distinguishes it. The practical consequence is that the feature whose purpose is to keep large reads out of the session&#39;s window (ADR 0062) is the one path with no window protection at all.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><p><strong>One budget formula, shared.</strong> The parent&#39;s budget derivation moves verbatim into a pure module, <code>packages/agent-runtime/src/context-budget.ts</code>, and both <code>PiRuntime</code> and <code>SubagentRun</code> call it. <code>hardLimit = contextWindow − requestHeadroom</code> with the existing headroom, reserve-floor, and <code>keepRecentTokens</code> clamps is unchanged, so the session&#39;s observable behavior is identical before and after the extraction. The module takes a model window and maximum output and returns a budget; it reads no runtime state, so the two callers cannot drift.</p></li><li><p><strong>A delegate budget derives from the delegate&#39;s own model.</strong> The window and output cap come from the model the run actually resolved — a <code>Task.model</code> override, a definition pin, or the inherited session model (§5f) — not from the session model and not from the definition. A per-definition <code>maxTokens</code> cap (ADR 0210) participates as the output budget it already is.</p></li><li><p><strong>Turn-boundary compaction for delegates.</strong> <code>SubagentRun</code> wires <code>prepareNextTurnWithContext</code>. At a delegate turn boundary the run re-estimates its own context; at or above <code>hardLimit</code> it compacts synchronously before the next provider request, using the <code>prepareCompaction</code> and <code>generateSummary</code> primitives <code>@earendil-works/pi-agent-core</code> 0.85.1 already exports. Retention follows the parent&#39;s rule (ADR 0136): a boundary with pending tool results retains as an active turn, a completed turn retains as a completed turn. There is no pre-computation and no second threshold, matching ADR 0064 clause 1.</p></li><li><p><strong>Degradation before failure.</strong> When a summary cannot be generated, or the compacted context still exceeds <code>hardLimit</code>, the run keeps the original task brief plus the most recent message(s) and discards the rest of its history. The run continues and records that it was degraded, so the parent&#39;s report and the lifecycle details say the delegate lost history rather than presenting a complete answer.</p></li><li><p><strong>A terminal code the parent can act on.</strong> When even the degraded context does not fit, the run fails with <code>SUBAGENT_CONTEXT_OVERFLOW</code> (<code>retriable: no</code>) carrying guidance the parent model can execute: narrow the task, delegate to a model with a larger context window, or read less at once. The provider&#39;s raw overflow text is no longer what the parent receives for this failure.</p></li><li><p><strong>Fallback models are re-evaluated against their own window.</strong> Before advancing to an alternative, the controller checks the carried context against that alternative&#39;s budget. An alternative that cannot fit is skipped and recorded in <code>modelFailures</code> with that reason instead of being attempted into the same failure. Ordering, duplicate skipping, retry budgets, and the rest of ADR subagent-model-fallback are unchanged.</p></li><li><p><strong>Resume seeds within the budget.</strong> <code>seedDelegateMessages()</code> truncates the seeded chain against the delegate&#39;s budget. The original task brief and the most recent turns are preserved; the oldest tool results are dropped first. A resume therefore starts below <code>hardLimit</code> instead of overflowing on its first request.</p></li><li><p><strong><code>new_context</code> stays denied to delegates.</strong> Delegate compaction is entirely automatic. The tool mutates parent runtime state, so inheriting it would let a delegate compact its parent; <code>SUBAGENT_INHERIT_DENY_TOOLS</code> keeps it out.</p></li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A long delegate no longer ends in a provider overflow error. It compacts, and if it cannot compact it degrades, and only then fails — with a code that tells the parent what to change.</li><li>A compacted delegate is lossier in exactly the way a compacted session is: assistant reasoning and tool output survive only through the summary. A delegate&#39;s report is the only thing the parent ever sees, so this is less visible than for the session — and for the same reason, a degraded run must say so, or the parent would read a partial answer as a complete one.</li><li>Compaction costs the delegate a summary request at the moment it is already working, so a delegate that crosses the boundary is slower and more expensive than one that does not. The alternative it replaces is a failed run.</li><li>Sharing one budget module makes the parent&#39;s formula load-bearing for two callers. A change to it now changes both, which is the point; it also means the parent&#39;s existing budget tests are the regression surface for delegates.</li><li>Fallback re-evaluation can leave a configured alternative unused. The <code>modelFailures</code> entry names the reason, so the skip is auditable rather than silent.</li><li>Resume truncation means a resumed delegate can start without history it had in an earlier round. It keeps the task brief and the recent turns, so the resume is still warmer than a cold start, but it is no longer a promise that the whole chain is in context.</li></ul><h2 id="not-decided-here-out-of-scope" tabindex="-1">Not decided here / out of scope <a class="header-anchor" href="#not-decided-here-out-of-scope" aria-label="Permalink to &quot;Not decided here / out of scope&quot;">​</a></h2><ul><li><strong>Transcript rows.</strong> Delegate compaction affects the delegate&#39;s model context only. It rewrites no persisted transcript row. The delegate&#39;s rows stay complete and visible, and the session runtime already keeps rows carrying <code>parentToolCallId</code> out of the parent&#39;s model context (§5f, Events and context), so nothing about the parent&#39;s context changes.</li><li><strong>Durable checkpoints.</strong> No host-core compaction record is written for a delegate. The checkpoint chain of ADR 0064 clause 6 remains the session&#39;s. A delegate&#39;s compaction is in-memory for the duration of its run.</li><li><strong>Transcript presentation.</strong> No compaction divider row, no warning toast, and no context-inspector line for a delegate. The user&#39;s visible surface for a delegate stays the delegation card and its report.</li><li><strong><code>new_context</code> for delegates.</strong> Denied, per clause 8.</li><li><strong>Parent behavior.</strong> Clause 1 is an extraction, not a change. Nothing in this record alters when or how the session Agent compacts.</li><li><strong>Cross-run budget accounting.</strong> Concurrent delegates are budgeted independently, each against its own model. There is no session-wide ceiling across delegates and none is proposed here.</li></ul><h2 id="references" tabindex="-1">References <a class="header-anchor" href="#references" aria-label="Permalink to &quot;References&quot;">​</a></h2><ul><li><code>docs/adr/0064-codex-parity-context-compaction.md</code></li><li><code>docs/adr/0062-bounded-subagents-behind-a-task-tool.md</code></li><li><code>docs/adr/0279-resumable-subagent-delegations.md</code></li><li><code>docs/adr/0136-active-task-boundary-across-compaction.md</code></li><li><code>docs/adr/subagent-model-fallback.md</code></li><li><code>docs/spec/03-runtime/02-agent-runtime.md</code> §5.1, §5f</li><li><code>docs/spec/03-runtime/08-error-codes.md</code> §3.2</li><li><code>docs/spec/06-delivery/04-e2e-test-plan.md</code></li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0299-subagent-context-budget.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0299SubagentContextBudget = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0299SubagentContextBudget as default
};
