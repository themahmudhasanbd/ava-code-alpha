import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"01. UI Information Architecture","description":"","frontmatter":{},"headers":[],"relativePath":"spec/04-ux/01-ui-ia.md","filePath":"spec/04-ux/01-ui-ia.md","lastUpdated":1790003601000}');
const _sfc_main = { name: "spec/04-ux/01-ui-ia.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="_01-ui-information-architecture" tabindex="-1">01. UI Information Architecture <a class="header-anchor" href="#_01-ui-information-architecture" aria-label="Permalink to &quot;01. UI Information Architecture&quot;">​</a></h1><blockquote><p>Language: English (per ADR 0009). This describes the shipped Codex-aligned shell (D034+). Component detail: <a href="./08-component-spec">08-component-spec</a>; visual tokens: <a href="./07-ui-design-system">07-ui-design-system</a>; behavior: <a href="./09-interaction-patterns">09-interaction-patterns</a>.</p></blockquote><h2 id="_1-goal" tabindex="-1">1. Goal <a class="header-anchor" href="#_1-goal" aria-label="Permalink to &quot;1. Goal&quot;">​</a></h2><p>A clear, restrained, developer-first workbench: one window, one active destination, chat as the home surface, tools and permissions inline.</p><h2 id="_2-shell-regions" tabindex="-1">2. Shell regions <a class="header-anchor" href="#_2-shell-regions" aria-label="Permalink to &quot;2. Shell regions&quot;">​</a></h2><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>+----------------------------------------------------------------------+</span></span>
<span class="line"><span>| Platform titlebar: macOS traffic lights / Windows/Linux actions     |</span></span>
<span class="line"><span>+------------------+--------------------------------+------------------+</span></span>
<span class="line"><span>| Sidebar (275px) | Main pane (active destination) | Work panel       |</span></span>
<span class="line"><span>|                  |  chat home / transcript        |  (optional,      |</span></span>
<span class="line"><span>|                  |  or Extensions page            |   resizable      |</span></span>
<span class="line"><span>|                  |                                |   ≥244px, dynamic|</span></span>
<span class="line"><span>|  Sessions     +↕ |                                | surface          |</span></span>
<span class="line"><span>|   Recent rows ↕  |                                |                  |</span></span>
<span class="line"><span>|  Projects      + |                                | ◫ | App.tsx  ⌄ × |</span></span>
<span class="line"><span>|   Project A      |                                | &gt; |              |</span></span>
<span class="line"><span>|   Project B      |                                | ◎ | Active       |</span></span>
<span class="line"><span>| Footer [⚙][plug][☾][bell] |  Floating composer (chat) |   | resource     |</span></span>
<span class="line"><span>+------------------+--------------------------------+------------------+</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br></div></div><ul><li><strong>Sidebar</strong>: primary navigation — path-less conversations under a compact <strong>Sessions</strong> section with new-session and sort actions, retained open-project groups under a following <strong>Projects</strong> section with a persistent new-project action, and the WorkBuddy-inspired footer. The footer keeps compact Settings, Extensions, Scheduled (clock), and notification icon actions; Pull requests remains omitted from the home sidebar. Each retained project is a path-keyed tab/group that can be collapsed independently. Project and conversation rows expose non-destructive pin/archive actions, an independent conversation-branch command, and sortable views. Projects not retained in the sidebar remain discoverable through Settings → Project archive. Collapsible to an icon rail (Cmd/Ctrl+B). Its expanded column is user-resizable from 240px to 520px (default 275px); dragging below 160px collapses it.</li><li><strong>Product identity</strong>: runtime shell copy uses <code>PI-Desktop</code>; the home hero and sidebar reuse the derived <code>src/assets/brand/logo-*.png</code> marks, while composer prompt rows have no leading brand icon and session-creation controls use a dedicated message-plus icon. On Windows/Linux, the expanded sidebar begins with a keyboard-accessible Home brand and Collapse sidebar at the right; activating the brand returns the main pane to chat. The macOS expanded sidebar omits the logo/title brand and places only Collapse sidebar at the right of the traffic-light row. <code>Codex</code> remains only an external import source or a design-reference term.</li><li><strong>Main pane</strong>: exactly one destination at a time; destinations replace the pane (they are pages, not modals). Once Settings or Extensions is selected, bootstrap completion and background refreshes must not replace that destination with the chat home; only an explicit navigation action may do so. The outer pane stays fluid while the sidebar is collapsed. The centered chat content band defaults to 760px and is user-resizable (D439); it compresses with <code>min(available pane, preferred)</code> instead of tightening to a 640px ceiling.</li><li><strong>Titlebar</strong>: platform-native desktop chrome (D118). macOS uses <code>hiddenInset</code> traffic lights and the system application menu. The expanded sidebar keeps Collapse sidebar in the same 46px row, aligned to the right outside the traffic-light safety area; no logo/title is rendered there, including in fullscreen. When the work panel is open, native window controls stay viewport-fixed at the window&#39;s right edge and the panel header reserves that band plus the work-panel toggle. The panel header is a horizontally scrollable tab strip followed by a fixed <code>+</code> add trigger; tab close actions stay in the tabs, so the Windows native close control is not visually duplicated by a second header <code>×</code>. Windows/Linux use a menu-free frameless 46px row with sidebar actions on the left and accessible minimize / maximize-or-restore / close controls at the right edge of the conversation pane when the panel is closed (D129). When the work panel is open, those controls stay viewport-fixed over the panel header rather than travelling with MainPane. One window-level control band stays outside pane stacking contexts across panel open, preview, restore, and Settings transitions. Its background follows the adjacent titlebar surface (dock header when open, conversation surface when closed) in both themes. Boot splash, search, and toasts stay above that band. Preview navigation must also remain above the panel; macOS keeps native traffic lights and its existing fullscreen insets. Destination history is shortcut-first (<code>Cmd/Ctrl+[</code> and <code>Cmd/Ctrl+]</code>) with no dedicated back/forward chrome; while Extensions is active, the footer Plugins button performs one Back step as the only pointer affordance. The main titlebar has no notification action; the durable local inbox opens from the sidebar footer bell instead (D130/D117). In work-panel preview mode, MainChat is unmounted and a window-level 46px chrome row keeps New Task, sidebar, and native window controls available without owning a drag or no-drag rectangle across the panel. The panel header alone owns dragging in the preview pane; its actual border box starts after the shell action lane plus an 8px gap, including the expanded-sidebar New Task button, on every platform. The left inset is 8px, or 88px for collapsed-sidebar windowed macOS. Its right native-control exclusion is unchanged. Header paint fills the excluded lane without an opaque overlay hiding tabs or panel actions. The macOS inset uses the shared <code>--ds-window-lead-inset</code> token — the cluster&#39;s 76px right edge (from <code>@pi-desktop/shared</code>) plus a 12px gap — and the main process positions the buttons from that same shared geometry.</li><li><strong>Work panel</strong>: docked right column (not an overlay) opened by an artifact, the viewport-fixed toggle, or <code>Cmd/Ctrl + J</code>. File, URL, browser-preview, and successful workspace-edit artifacts create their resources atomically. The 46px content header exposes a tablist and a fixed <code>+</code> trigger. Its tokenized 44px right-side safe lane (the 28px control, its 12px viewport inset, and the header&#39;s 4px control gap) keeps the <code>+</code>, maximize, and viewport-fixed work-panel toggle one button group, spaced by that same gap, while the trigger keeps a distinct hit target. Clicking <code>+</code> creates and activates a unique New launcher tab; its body presents the same data-driven Review and plugin-view rows as buttons, so the user chooses a destination in the page instead of opening a dropdown. Selecting a row replaces that launcher tab with the destination or activates an existing singleton. File paths stay distinct while plugin views deduplicate by view reference. The viewport-fixed toggle and <code>Cmd/Ctrl + J</code> both toggle the active session&#39;s retained panel context — revealing it without creating a resource tab and collapsing it without discarding one; the create trigger remains unavailable while the panel is closed. Closing the final tab keeps the panel open and shows the New launcher. No agent or tool result opens, activates, or resizes the panel: Review is reached only through an explicit user action, so a successful workspace Write/Edit leaves the panel exactly as the user left it and shows its evidence as a transcript card instead. The inner divider resizes the panel through the shared three-column budget; moving it left takes space until MainChat reaches 450px, at which point the expanded sidebar yields immediately, and moving it right gives space back. A manual sidebar reopen spends work-panel width first and otherwise targets a 460px MainChat width. The sole panel-level control is the viewport-fixed toggle; each session retains its own runtime open state, tab set, active tab, and Browser resource in renderer memory. Selecting another session swaps the visible panel context without deleting either session&#39;s state; selecting a workspace without an active conversation hides the panel rather than reinterpreting relative resources. Background artifacts update only their originating session&#39;s retained panel context and never open, activate, or resize the visible panel. Startup is closed with no retained session contexts, and only the preferred panel width persists across launches. The work panel remains a fixed-width in-flow column beside MainChat inside the existing client area (ADR 0033 / ADR 0151). MainChat keeps a hard 450px minimum; the work panel&#39;s effective maximum is the remaining client width after the expanded sidebar and that floor (ADR 0238). When the budget is exhausted the sidebar collapses immediately through its existing animation (the budget still counts it while <code>sidebar-out</code> occupies flex space) and returns when the panel closes. Opening and collapsing change only the shell&#39;s internal flex allocation and never expand or shrink native window bounds; no panel action requests a positive native reservation. The panel-header preview toggle temporarily unmounts MainChat and expands the panel across the client area beside the sidebar; leaving preview restores the prior panel width and sidebar state without changing native bounds. The renderer-measured panel rectangle continues to position the native Browser view. Native window edges resize the app window only; they do not change the panel target. The outer window remains natively resizable from all OS edges and corners, with a minimum supported size of 1040×700. Replaces the former context-panel overlay; workspace/model/status info lives in the composer chips and Settings instead.</li><li><strong>Composer</strong>: workspace-agnostic floating pill anchored to the conversation destination — centered empty-home content above a bottom-reserved composer (D111/D204/D206), bottom-docked in a transcript, with no project / Local / branch rail (D095). Its left-of-input operating-mode chip is the sole active-session control for <strong>Agent</strong>, <strong>Plan</strong>, and <strong>Goal</strong>. Plan shows the same Agent&#39;s planning state; Goal shows the same approval boundary for an outcome contract. Both keep the permission-mode chip and expose their host-written immutable <code>.pi/plan/*.md</code> or <code>.pi/goal/*.md</code> artifact opener after submission. The conversation top bar retains only the task title and window actions; the Composer owns model and reasoning selection as well as mode control.</li><li><strong>Backend status capsule</strong>: appears under the titlebar while the backend restarts or is fatally degraded (D080), with an Open-logs action.</li></ul><h2 id="_3-destinations" tabindex="-1">3. Destinations <a class="header-anchor" href="#_3-destinations" aria-label="Permalink to &quot;3. Destinations&quot;">​</a></h2><h3 id="_3-1-chat-home-default" tabindex="-1">3.1 Chat home (default) <a class="header-anchor" href="#_3-1-chat-home-default" aria-label="Permalink to &quot;3.1 Chat home (default)&quot;">​</a></h3><ul><li>Empty state: a restrained hero title (&quot;What can I help you build?&quot; — a project-bound session turns the project name into a dotted-underline switcher that lists the sidebar&#39;s open projects, can search them, can clone a git repository from a syntactically public remote (ADR 0247 / D416), and can open another local folder), an optional first-run checklist, and a bottom-reserved composer. Task entry starts directly in the composer; no redundant supporting paragraph, developer starter cards, or contextual quick-action row is rendered (D204/D206).</li><li>With transcript: message stream + tool disclosure rows (D071), a contextual message-scoped review card immediately after each successful workspace Write/Edit row, docked composer, and a session-scoped permission card inline. The card reads the message&#39;s durable review snapshot rather than the current Git diff, so it stays visible after commit. It shows the file status and addition/deletion counts, expands the exact message hunks in place, and offers guarded rollback; it is not a global transcript entry. A background session&#39;s message, tool, and permission events never replace or cover the visible conversation.</li></ul><h3 id="_3-2-sidebar-project-groups" tabindex="-1">3.2 Sidebar project groups <a class="header-anchor" href="#_3-2-sidebar-project-groups" aria-label="Permalink to &quot;3.2 Sidebar project groups&quot;">​</a></h3><ul><li><strong>Sections</strong>: the compact <code>Sessions</code> heading precedes <code>Projects</code> and owns path-less conversation creation plus the existing sort/archive-view menu. Its toolbar places sorting before new-session creation. Both headings keep quiet glyph actions and also accept a right-click create menu on the heading or empty list chrome so section creation stays discoverable without extra chrome. Its list shows at most five compact rows (146px) before scrolling internally, so standalone work stays visible without displacing project navigation. The following <code>Projects</code> heading exposes the folder-picker action; retained project groups use the remaining height and scroll independently.</li><li><strong>Identity</strong>: each project group is keyed by a host-owned logical group id; each root path remains canonical and is never inferred from an ambiguous folder basename. Legacy single-folder projects are compatibility groups.</li><li><strong>Header</strong>: project name, current-workspace dot, disclosure, new-task action, and an overflow menu. Workspace context is not navigation selection: project headers have no persistent selected background, including when no conversation is selected. Only the current conversation on the chat page receives selected row paint. Project and conversation rows share full-row hover feedback; the project title itself stays transparent. The directory title is one full-row disclosure target; collapse/expand affects only child visibility, and adjacent groups form one dense tree rather than detached cards. Hovering or focusing the project title reveals the full project path. Pressing the title and moving 8px reorders the group.</li><li><strong>Project actions</strong>: open folder reveals the primary project directory; Edit project changes the host-owned logical group name and adjusts eligible non-primary roots (keeping renderer metadata in sync); pin/unpin changes presentation priority; archive/restore hides or restores the group in the default view; close removes the retained primary tab without deleting or archiving group roots, sessions, or memory. Expanded Project archive details list every group root.</li><li><strong>Conversation actions</strong>: rename, pin/unpin, archive/restore, fork, and delete remain separate actions. Rename edits the task label only; archive never removes the transcript. Open folder is a project action, not a conversation action.</li><li><strong>Temporary-task attachments</strong>: selecting a saved attachment opens its file preview even without an open project. Back returns to the no-project browsing state. Branches preserve referenced pasted/imported inputs as child-owned copies; deleting the source task does not break these previews.</li><li><strong>Sort</strong>: user-facing modes are Recently updated, Created date, Oldest first, and Name. Pinned rows precede unpinned rows. Project groups switch to <code>manual</code> by dragging a title or using ArrowUp/ArrowDown on that title. Session <code>manual</code> remains a compatibility value.</li><li><strong>Conversation list</strong>: each group shows the ten most-recent sessions in the active sort order by default; the remainder folds behind a <strong>Load N more…</strong> row that expands the full time-grouped list on click. Pinned rows precede unpinned rows and are never pushed behind the fold; the expansion state is not persisted.</li><li><strong>Standalone sessions</strong>: path-less sessions remain in the separate Sessions section and never inherit the last active project&#39;s workspace.</li><li><strong>Concurrency</strong>: the shell selects one visible project at a time, while agent run state remains keyed by session. Switching project tabs does not cancel a background turn. Background events update only their originating session and never change the active session, page, project, or keyboard focus.</li></ul><h3 id="_3-3-pull-requests" tabindex="-1">3.3 Pull requests <a class="header-anchor" href="#_3-3-pull-requests" aria-label="Permalink to &quot;3.3 Pull requests&quot;">​</a></h3><p>Segmented Open/Draft/All filters with counts; rows carry icon plate, number, title, status badge, branch meta, external link, and &quot;Review with agent&quot; (creates a chat turn). Requires an active workspace and <code>gh</code>.</p><h3 id="_3-4-scheduled" tabindex="-1">3.4 Scheduled <a class="header-anchor" href="#_3-4-scheduled" aria-label="Permalink to &quot;3.4 Scheduled&quot;">​</a></h3><p>Tasks and Run history views, with an explicit create/edit form, a cadence dropdown, time, next occurrence, saved project, pause/resume and delete confirmation. Hourly schedules repeat at one-hour intervals without a time selector. Daily schedules use a themed time-period dropdown: Morning 09:00, Afternoon 14:00, Evening 19:00, Night 22:00. The form does not expose hour/minute editing. AI tools may set an exact time; a non-preset time displays as Custom with its HH:mm value and survives other form edits until the user explicitly selects a preset. Weekly schedules select one or more weekdays (Monday = 0) in a separate dropdown listing Monday through Sunday with selection markers. Each day toggles independently; there are no preset combinations. An empty selection disables saving. The menu supports arrows, Home/End, Enter/Space, Escape/outside dismissal, and exposes selected states. The footer clock and global search open this route. Run now dispatches in the background and selects Run history; a conversation link opens the real transcript. The latest 100 runs show running, completed, failed or interrupted status. Automatic runs never steal foreground focus. See <a href="./../../adr/scheduled-desktop-automations">desktop automations</a>.</p><p>The application must remain running. The host polls every 30 seconds and skips occurrences more than 90 seconds late or overlapping a running task. Startup rearms future occurrences only. Hourly schedules wait a full hour after saving, enabling, startup or the preceding automatic admission; Run now leaves the automatic occurrence unchanged. Legacy cadence-only tasks require explicit schedule configuration. The current project is captured when first configured; subsequent foreground project changes do not retarget it. This includes Manual tasks and tasks saved without a project: Run now, renaming, and cadence changes preserve that binding, including after restart. Only legacy tasks without a saved binding capture the current project on their first explicit configuration. Automatic runs use Ask permissions and may wait for input in their conversation. New tasks default to Agent. A migrated Plan or Goal task is allowed to remain stored, but an unattended run is explicitly rejected before provider, artifact, or queue work with <code>PLAN_REQUIRES_INTERACTIVE_SESSION</code>; it cannot display or auto-approve a contract. The user must explicitly switch it to Agent before enabling unattended execution.</p><p>Agent tools can change a Manual task to Hourly by supplying only its id and <code>cadence: &quot;hourly&quot;</code>; no calendar time is required. Preserve existing schedule fields and paused state. Daily and Weekly still require a valid saved or supplied schedule. Renaming an Hourly task does not restart its interval.</p><h3 id="_3-5-extensions" tabindex="-1">3.5 Extensions <a class="header-anchor" href="#_3-5-extensions" aria-label="Permalink to &quot;3.5 Extensions&quot;">​</a></h3><p>The Extensions destination is a focused plugin surface with a compact header and only two tabs: <strong>Installed</strong> and <strong>Marketplace</strong>. Installed groups plugin rows by state — Needs attention / Updates available / Active / Turned off — as soft tiles stacked under a group label (D296). Marketplace remains the browse/install card grid. The page draws no dividers: header, toolbar, rows, source settings, cards and the detail sheet&#39;s sections are set apart by tone and spacing, and hairlines are reserved for floating layers (menus, sheet, dialogs). The marketplace source settings show the source selector without a redundant provider explanation or active-source status line. MCP, Skills, and Subagents are not tabs or sections of Extensions.</p><h3 id="_3-6-settings-full-page-takeover" tabindex="-1">3.6 Settings (full-page takeover) <a class="header-anchor" href="#_3-6-settings-full-page-takeover" aria-label="Permalink to &quot;3.6 Settings (full-page takeover)&quot;">​</a></h3><h3 id="_3-6-settings-full-page-takeover-1" tabindex="-1">3.6 Settings (full-page takeover) <a class="header-anchor" href="#_3-6-settings-full-page-takeover-1" aria-label="Permalink to &quot;3.6 Settings (full-page takeover)&quot;">​</a></h3><p>Settings replaces the whole shell (D063): back-to-app + search + a grouped settings rail with concise, parallel destination labels. The Agent group contains independent Skills, MCP, and Subagents destinations alongside Instructions and Model configuration; selecting one changes the page destination rather than a tab inside a shared capability panel. Appearance lives inside General; global AI behavior (permissions and context management) lives inside 全局 AI; keyboard shortcuts and global/project instructions have their own destinations; provider management lives inside Model configuration. Import scans supported local agent stores for sessions and, independently, for model configuration, and presents candidates in collapsible groups. Project path is an alternate grouping for sessions alongside the default source grouping, and every scan or grouping change starts with all groups collapsed. Model-configuration import copies stored API keys and skips subscription logins. Project archive owns the durable D086 Projects index (search, add, expand, pin, archive/restore, close, and reopen) and always includes archived records. Opening or switching a project retains a sidebar tab, selects that project as the active workspace, and returns to chat. Other retained tabs stay open. Extension management remains solely on the app shell&#39;s independent Extensions destination described in §3.5. Settings &gt; Agent has the following shared capability contract:</p><ul><li>Each capability destination starts with a quiet localized description and scope note, then uses the same neutral elevated Settings surface as the other destinations; no capability page has a decorative hero, colored top bar, or separate visual theme.</li><li>Skills and MCP use stacked global/project card blocks in one column. Each block has a quiet heading row with a scope title, scope description, resolved <code>.agents</code> path, localized count, and its actions; the project block shows a recent-project picker. Project records take precedence over global records.</li><li>Skills have one native <strong>Import</strong> action per surface. It accepts exactly one file and physically copies it into the selected <code>.agents/skills</code> directory.</li><li>MCP has one <strong>Add</strong> action per surface. Add and Edit open the existing <code>McpEditorSheet</code> as a modal overlay with stdio/HTTP branches, validation, duplicate checks, locked edit ids, scope text, and Test connection feedback.</li><li>Subagents use one full-width global surface under <code>~/.agents/subagents</code>; they have no project picker, project surface, or project-level toggle. Creating or editing a subagent picks the pinned model from configured provider models, or inherits the session model; it does not require typing a <code>provider/model</code> id.</li><li>All three lists flow at natural page height, render a quiet centered empty state inside the panel, dim disabled rows, and store enablement in app-local state rather than capability files. Loading and project changes render skeleton rows with the same anatomy and disable competing controls until the host refresh completes.</li></ul><h2 id="_4-overlays" tabindex="-1">4. Overlays <a class="header-anchor" href="#_4-overlays" aria-label="Permalink to &quot;4. Overlays&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Overlay</th><th>Trigger</th><th>Notes</th></tr></thead><tbody><tr><td>Command palette</td><td>Cmd/Ctrl+K (also Cmd/Ctrl+Shift+P per D014)</td><td>builtin + plugin commands</td></tr><tr><td>Model menu</td><td>Composer-right model × reasoning chip</td><td>configured provider/model choices + settings entry (D091)</td></tr><tr><td>Profile menu</td><td>sidebar footer</td><td>Settings / Logs / Theme cycle (D041)</td></tr><tr><td>Notification inbox</td><td>sidebar footer bell</td><td>All/Unread views, task failure rows only (successful completions are hidden, D295), mark-all-read and clear actions (D130/D117)</td></tr><tr><td>Toasts</td><td>events (plugin toast, backend restored, copy)</td><td>top-center; 4s default, 8s for errors</td></tr><tr><td>Project switcher</td><td>empty-home underlined project name</td><td>sidebar open projects + search + clone git project + open project</td></tr></tbody></table><h2 id="_5-navigation-model" tabindex="-1">5. Navigation model <a class="header-anchor" href="#_5-navigation-model" aria-label="Permalink to &quot;5. Navigation model&quot;">​</a></h2><ul><li><code>page</code> state: <code>chat | pulls | scheduled | plugins | settings</code>; <code>chat</code> is the conversation-surface route, not an operating mode. The project archive is the <code>projects</code> settings tab rather than a standalone page.</li><li>Destination history is linear; <code>Cmd/Ctrl+[</code> and <code>Cmd/Ctrl+]</code> traverse it without persistent back/forward chrome. While Extensions is active, the footer Plugins button reuses one Back step (§2 shell regions); no separate back or forward control is added.</li><li>Selecting a project tab reuses <code>project.set</code> when its path differs from the selected host workspace and keeps the other tabs retained.</li><li>Selecting a project-scoped thread activates its project before switching to <code>chat</code>. Selecting a temporary thread clears the visible active workspace before loading it.</li><li>Empty home has three explicit session states: a project-bound session shows the project-underlined welcome; clicking the name opens a searchable switcher of the sidebar&#39;s open projects instead of the folder picker. A temporary session shows dedicated temporary-chat copy with no project underline or switcher; and no active session keeps the generic welcome title.</li><li>New task resolves the current project or temporary group by its most recent session: if that session has <code>messageCount = 0</code>, it is selected and reused; otherwise a durable empty session is created immediately and appears in the sidebar. Repeated clicks therefore keep one empty slot per visible group; an empty slot remains persisted until the user deletes or archives it.</li></ul><h2 id="_6-keyboard-map-ia-level" tabindex="-1">6. Keyboard map (IA level) <a class="header-anchor" href="#_6-keyboard-map-ia-level" aria-label="Permalink to &quot;6. Keyboard map (IA level)&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Keys</th><th>Action</th></tr></thead><tbody><tr><td>Cmd/Ctrl+K, Cmd/Ctrl+Shift+P</td><td>command palette</td></tr><tr><td>Cmd/Ctrl+B</td><td>toggle sidebar</td></tr><tr><td>Cmd/Ctrl+[</td><td>previous destination</td></tr><tr><td>Cmd/Ctrl+]</td><td>next destination</td></tr><tr><td>Cmd/Ctrl+N</td><td>new task</td></tr><tr><td>Cmd/Ctrl+O</td><td>open project</td></tr><tr><td>Cmd/Ctrl+,</td><td>settings</td></tr><tr><td>Cmd/Ctrl+.</td><td>abort current run</td></tr><tr><td>Enter / Shift+Enter / Cmd/Ctrl+Enter</td><td>send / newline (Enter-to-send; when off, Cmd/Ctrl+Enter sends)</td></tr><tr><td>Esc</td><td>dismiss overlay/menu</td></tr></tbody></table><h2 id="_7-state-dependent-chrome" tabindex="-1">7. State-dependent chrome <a class="header-anchor" href="#_7-state-dependent-chrome" aria-label="Permalink to &quot;7. State-dependent chrome&quot;">​</a></h2><ul><li>No provider configured → blocking guidance toward Settings before first run (<code>MODEL_NOT_CONFIGURED</code>).</li><li>No workspace → home hero without project underline; Pull requests shows a workspace-required empty state. The composer never renders a workspace rail.</li><li>Background project session → the originating project row retains its running/error indicator. Selected shell state can move independently while the session tool root remains bound to its durable project; its artifacts are retained in that session&#39;s work-panel context without opening or activating tabs over the currently selected project. Messages, tool events, permission requests, and panel resources remain scoped to that session. Explicitly opening the conversation restores its retained panel context and reveals any pending permission card with its original deadline.</li><li>Completed/failed turn not already visible → host-core appends one durable inbox row. A result shown in the visible, focused current chat and every <code>aborted</code> turn append none. Background sessions and any turn finishing while the window is unfocused still append. The sidebar footer bell lists only <code>task.failed</code> rows and its badge counts only unread failures; successful completions stay in the durable record for the sidebar outcome badge and native notification but never appear in the inbox (D295). Selecting a row marks it read and activates its bound project/session. Electron additionally presents a native task notification only when the app window is unfocused, and clicking it focuses the window before activating the same session (D117). Interactive ask/permission/plan prompts use their separate native path and may alert for a focused background session. Receiving either durable or native notification events never navigates by itself; only explicit activation does.</li><li>Backend degraded → status capsule (restarting) or fatal banner with Open logs (D080); composer submits are rejected with readable errors while down. <ul><li>Plan/Goal checkpoint → the originating session shows only the structured title and an opener for its immutable <code>.pi/plan/*.md</code> artifact. The renderer retains the latest proposal/execution snapshot per session only for the current renderer lifetime, updated by live Host events; only a live <code>pending</code> row forms the approval gate. Reload through <code>plans.pending</code> while the same Host remains alive restores a still-pending row with its original deadline. Rejected, expired, approved/completed, and interrupted terminal cards are not rehydrated; a terminal card may remain visible and non-actionable only until renderer reload. Reject, expiry, or interruption clears the approval gate, leaves the session in its contract state and editable, and requires a later turn to create a new artifact. While pending, the draft remains visible but read-only and only Approve or Reject actions are enabled. Host/app restart interrupts prior work before RPC with no replay or stale action; pending unapproved work remains Plan, while already-approved interrupted execution remains Agent. The UI is not required to present that interrupted terminal snapshot after restart.</li></ul></li></ul><h2 id="_8-i18n" tabindex="-1">8. i18n <a class="header-anchor" href="#_8-i18n" aria-label="Permalink to &quot;8. i18n&quot;">​</a></h2><p>English is the source locale. Shipped translations (zh-CN, zh-TW, Turkish, German, Spanish, French, and Korean) cover shell chrome; labels are asserted by US-UI e2e scenarios. Copy rules live in <a href="./02-i18n-english-first">02-i18n-english-first</a>.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("spec/04-ux/01-ui-ia.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _01UiIa = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _01UiIa as default
};
