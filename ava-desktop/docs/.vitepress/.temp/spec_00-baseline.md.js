import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"PI-Desktop Baseline Freeze","description":"","frontmatter":{},"headers":[],"relativePath":"spec/00-baseline.md","filePath":"spec/00-baseline.md","lastUpdated":1789839347000}');
const _sfc_main = { name: "spec/00-baseline.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="pi-desktop-baseline-freeze" tabindex="-1">PI-Desktop Baseline Freeze <a class="header-anchor" href="#pi-desktop-baseline-freeze" aria-label="Permalink to &quot;PI-Desktop Baseline Freeze&quot;">​</a></h1><ul><li>Baseline Version: <code>0.4.18</code></li><li>Date: <code>2026-09-14</code></li><li>Status: <code>Frozen for implementation details (Plan checkpoint artifact + approval/execution startup fence + protocol v11 + schema v16 + host-owned plugin session import/read/update/delete P0/P1 + selectable shell catalog + icon-free composer prompt row + turn-boundary context checkpoint compaction + session-scoped work panel + edge-specific work-panel/chat resize ownership + models.dev model catalog with a bundled release snapshot + provider/runtime safety + M5 hardening + settings IA + project archive + sidebar organization + app update delivery + three-platform release + Extensions page density and theme-readable actions + custom global UI font + ChatGPT-style logical project groups)</code></li><li>Language policy: <strong>English-first</strong></li><li>Backend policy: <strong>Rust host core + pi agent sidecar</strong></li></ul><blockquote><p>Version history: <code>0.3.4</code> froze provider/runtime-safety decisions (D001–D033). <code>0.4.0</code> absorbs the Codex visual-parity decision series (D034+, gold source = decisions-log §D) and the M5 hardening decisions (D078–D083: signing lanes, brand icon, supervision, renderer sandbox, log channels, window state). <code>0.4.1</code> freezes the compact four-destination settings directory from D090 / ADR 0013. <code>0.4.2</code> replaces the frozen 720px settings content cap with the window-responsive D092 / ADR 0015 layout. <code>0.4.3</code> adopts retained multi-project sidebar tabs, non-destructive project/session organization, and session-rooted tool isolation through D093 / ADR 0016. <code>0.4.4</code> removes the passive composer context rail through D095. <code>0.4.5</code> freezes end-to-end thinking levels and provider presets through D096/D102 and ADR 0018. <code>0.4.6</code> supersedes D020&#39;s blanket deferral with the packaged application update modes in D120 / ADR 0022 while preserving D010. <code>0.4.7</code> lifts D010&#39;s macOS-only release scope through D126: tag builds publish installers and electron-updater feeds for macOS arm64, Windows x64, and Linux x64. D285 adds a native macOS Intel x64 tag lane alongside the arm64 lane; both macOS architectures publish DMG/ZIP artifacts from their matching runners. <code>0.4.8</code> moves the durable Projects index out of the home sidebar and into Settings as the fifth <strong>Project archive</strong> destination through D133 / ADR 0026. <code>0.4.9</code> made the pinned pi-ai catalog authoritative for known-model metadata and removed desktop-owned model parameter overrides through D136 / ADR 0027. ADR 0133 / D266 first introduced models.dev as a remote primary; ADR 0134 supersedes that fallback design and makes models.dev the sole metadata source with a checked-in release snapshot. pi-ai remains the transport, OAuth, and account-availability layer. <code>0.4.10</code> replaces destructive work-panel clearing on conversation switches with runtime session-scoped contexts through D142 / ADR 0028. <code>0.4.11</code> adopts turn-boundary model-context checkpoint compaction through D158 / ADR 0030 while preserving the complete visible transcript. The context-recovery amendment in ADR 0049 adds a durable retained-tail fallback for automatic compaction failures. D200 / ADR 0061 derives the budgets from the model window instead of settings and removes the compaction settings. D203 / ADR 0064 then rebuilds the mechanism to match Codex: compaction is inline only, a checkpoint carries the summary plus the latest active user message only while a turn continues; completed turns carry no naked historical user messages. The model-facing <code>new_context</code> tool and two budget reminders are back, each compaction adds a transcript row and one warning, and a no-summary rollover family exists behind an internal switch. <code>0.4.12</code> standardizes home and thread-docked composer prompt rows without a leading brand mark through D160 / ADR 0031 while preserving shell branding elsewhere. <code>0.4.13</code> replaces the Chat operating profile with the Plan operating state through D188 / ADR 0052. Plan is the same pi Agent in planning state, keeps permission-mode selection, exposes Bash subject to that policy, denies Write/Edit/plugin tools, and submits structured plans through a separate host-owned approval transition. The host protocol is v7 and storage schema v8; persisted Chat values migrate to Plan while Agent remains the default. <code>0.4.14</code> replaces that proposal with immutable host-written Markdown checkpoints under <code>&lt;workspaceRoot&gt;/.pi/plan/*.md</code> through D189 / ADR 0053. SubmitPlan accepts title, Markdown, and question; the Markdown bytes are preserved exactly while title/question remain structured approval fields. Approval is approve/reject only with explicit permission selection defaulting to Ask, and opens the artifact for review. Pending, queued, and running work is interrupted by the startup process fence without replay, while an already-approved session remains Agent. ADR 0054 adds the selectable shell catalog while retaining the Bash protocol name. The host protocol is v9 and storage schema is v10. <code>0.4.15</code> amends the D169 Extensions presentation through D196 / ADR 0058: the four-card numeric overview band is removed, and shared button surfaces use semantic theme tokens so primary and secondary actions remain visible in dark and light themes. No host protocol or storage schema changes. <code>0.4.16</code> adds a user-selectable global UI font through D232 / ADR 0083: the Settings Appearance card gains a searchable Font picker; the selection persists as <code>AppSettings.fontFamily</code> and overrides <code>--font-sans</code>. Four open-licensed (SIL OFL 1.1) families — Geist, Inter, Noto Sans SC, and LXGW WenKai — shipped locally with license texts, and installed system families are enumerated by Electron main through the additive allowlisted channel <code>pi-desktop/app/systemFonts</code>. D598 / ADR 0298 later removed the bundled families: the app ships no font, the picker offers System default plus installed system families, and every stack ends in the system-only CJK fallback tier. No host protocol or storage schema changes. <code>0.4.17</code> replaces the renderer-owned multi-folder tab projection with ChatGPT-style host-owned logical project groups through ADR 0249. A group owns its name, ordered local roots, shared instructions, shared memory, and grouped sessions; its first root remains the only visible host workspace. The additive group data uses the existing <code>kv</code> extension boundary, so the storage schema and host protocol versions remain unchanged. <code>0.4.18</code> renames the project overflow action to Edit project and adds host-backed adjustment of logical project roots. The editor keeps the Primary root fixed, supports adding/removing eligible additional roots, and rejects removal of roots that still own chats.</p></blockquote><blockquote><p>The current post-baseline amendments add the P0/P1 host-owned plugin session API through ADR 0200 / D367, explicit project ids plus host-owned session refresh through ADR 0201 / D368, and the opt-in local MCP control plane through ADR 0203 / D370 (catalog and bind tightened by D372). Protocol v11 remains unchanged; schema v16 adds the host-owned session collaboration ledger (D409 / ADR 0239) on top of schema v15. Schema v15 adds the Host-owned turn queue (D386 / ADR 0213) on top of schema v14, which added the plugin origin sidecar and soft-delete marker. Session mutation, arbitrary re-binding, provider/model binding, batch-delete, and tag operations remain deferred; an explicit <code>projectId</code> is the limited project-binding exception for imported sessions. The local control plane is loopback-only and does not reopen the deferred remote Gateway / WebUI scope. ADR 0205 / D373 defines the remote Agent Host, Gateway, and multi-binding control-plane target for a future post-MVP milestone; D374 amends that target to one normative WebSocket binding, a headless Agent Host module, and the full local approval vocabulary, and D375 schedules the SSH-tunnel remote Host first while Gateway and browser access stay unscheduled. None of them changes the current exclusion.</p></blockquote><h2 id="frozen-decisions" tabindex="-1">Frozen Decisions <a class="header-anchor" href="#frozen-decisions" aria-label="Permalink to &quot;Frozen Decisions&quot;">​</a></h2><ol><li>Product name: <strong>PI-Desktop</strong></li><li>Desktop shell: <strong>Electron</strong></li><li>UI: <strong>React + TypeScript + Vite + Tailwind</strong></li><li>UI language default: <strong>English</strong></li><li>Docs / issues / commits language: <strong>English primary</strong></li><li>Agent engine: <strong>pi (<code>pi-ai</code> + <code>pi-agent-core</code>)</strong></li><li>Backend host core: <strong>Rust</strong></li><li>Agent loop location: <strong>Node/TypeScript pi sidecar</strong> (not renderer)</li><li>Electron main role: <strong>thin orchestrator</strong></li><li>Bridge: <strong>preload IPC only for renderer</strong></li><li>Host services transport: <strong>Rust sidecar + stdio JSON-RPC (NDJSON)</strong></li><li>Storage ownership: <strong>Rust host-core owns SQLite exclusively</strong></li><li>MVP domain: <strong>local coding agent</strong></li><li>Default mode: <strong>Agent</strong></li><li>Product operating selector: <strong>Agent | Plan</strong>; the internal <code>page = &quot;chat&quot;</code> value remains a conversation-surface implementation detail, not an operating mode</li><li>Agent tools: <strong>Read / Glob / Grep / Write / Edit / Bash</strong></li><li>Permission timeout: <strong>120s → deny</strong></li><li>Session grant scope: <strong>by toolName</strong></li><li><code>~/.pi</code> one-shot auto-import: <strong>not in MVP</strong>. ADR 0254 adds read-only native-session discovery and explicit continuation against the canonical Pi v3 JSONL; it does not silently import or copy sessions into Desktop storage.</li><li>Not in MVP: <strong>Gateway / remote WebUI control</strong>; local loopback MCP control is the post-baseline, opt-in exception recorded by D370</li><li>Extension model: <strong>user-installable plugin system</strong></li><li>Plugin first phase: <strong>commands / panel / agentTools / skills</strong></li><li>Plugin runtime target: <strong>separate process</strong>; M4 may use host-managed sandboxed runtime</li><li>Plugin market: <strong>protocol defined, implementation postponed</strong></li><li>Plugin package format: <strong><code>.piplug</code> (zip)</strong></li><li>Plugin trust first step: <strong>sha256 checksum; signature later</strong></li><li>First release platform: <strong>macOS arm64 only</strong> — lifted in <code>0.4.7</code>/D126; tag builds now publish native macOS arm64 and Intel x64, Windows x64, and Linux x64 AppImage, deb, and rpm artifacts</li><li>TS schema library: <strong>typebox</strong></li><li>i18n library: <strong>i18next</strong></li><li>Bash: <strong>non-interactive, streamed, and resolved from the selectable shell catalog; default timeout 60s with a bounded override</strong></li><li>Onboarding: <strong>inline checklist</strong></li><li>Observability MVP: <strong>local logs only</strong></li><li>Error model: <strong>shared AppError code registry</strong></li><li>Provider coverage: <strong>universal via pi-ai native + OpenAI-compatible + custom</strong></li><li>Model policy: <strong>no closed allowlist; models.dev release catalog, generic unknown IDs, and free-form model IDs</strong></li><li>Provider storage: <strong>Rust SQLite configs + OS secret store references</strong></li><li>Secrets backend: <strong>safeStorage primary + encrypted file fallback</strong></li><li>Workspace ignore: <strong>denylist + defaults + <code>.pi-desktopignore</code></strong></li><li>Tool result limits: <strong>per-tool budgets (128KB / 4000 lines search, 96KB / 4000 lines shell); <code>truncated</code> only when a result is cut short</strong></li><li>Settings directory: <strong>Basics / Model configuration / Import / Project archive / Info</strong>; the project archive owns durable project discovery, archive, restore, and reopen workflows; plugin management remains the app shell&#39;s independent <strong>Plugins</strong> destination</li><li>Sidebar organization: <strong>retained logical project groups with host-owned ordered local roots and renderer-local project/session pin, archive, collapse, and sort metadata</strong></li><li>Project activation: <strong>one visible host workspace via existing <code>project.set</code>; group sessions and context default to the primary root, while explicit absolute paths under registered group roots use host canonical containment</strong></li><li>Context management: <strong>pi-native checkpoint summaries in Codex&#39;s shape — inline compaction at the deterministic pre-request hard guard, the summary plus only the latest active user message while a turn continues (and no naked historical user messages after a completed turn), durable host checkpoints, and one overflow retry. The model can request a new window through <code>new_context</code>; every compaction adds a transcript row and one warning. No user-facing settings</strong></li><li>Plan tools and policy: <strong>Read / Glob / Grep / BrowserPreview / Bash plus <code>EnterPlanMode</code> and <code>SubmitPlan</code>; Write/Edit/plugin and unknown tools are denied. Bash follows <code>ask</code>, <code>accept-edits</code>, or <code>auto</code>, so Plan is planning intent, not a strict read-only security profile.</strong></li><li>Plan checkpoint: <strong><code>SubmitPlan(title, markdown, question)</code> causes host-core to preserve the exact Markdown bytes in a new unique <code>&lt;workspaceRoot&gt;/.pi/plan/*.md</code> artifact, while title/question remain structured fields in the existing <code>plan_approvals</code> row. The row records the artifact path/hash/size and execution fields. Approve/reject are the only actions; approval explicitly selects <code>ask</code>, <code>accept-edits</code>, or <code>auto</code> with Ask as the UI default, opens the artifact for review, and expires after 30 absolute minutes with <code>PLAN_APPROVAL_TIMEOUT</code>.</strong></li><li>Plan recovery and shells: <strong>a startup transaction marks prior pending, queued, and running Plan work interrupted before serving RPC, with no replay; an already-approved interrupted execution leaves the session Agent. Configuration is idle-only and each session has one running turn. The renderer may stage one latest next-turn configuration while a turn is running, but it submits that choice only after the host reports idle. <code>defaultCommandShell</code> selects the platform catalog entry; unavailable persisted choices fall back to the first available platform shell, each turn pins effective ID/dialect, and host rejects stale identity before streaming output under the 60-second default timeout.</strong></li></ol><h2 id="source-of-truth" tabindex="-1">Source of Truth <a class="header-anchor" href="#source-of-truth" aria-label="Permalink to &quot;Source of Truth&quot;">​</a></h2><ul><li>Spec index: <code>docs/spec/README.md</code></li><li>Navigation: <code>docs/spec/NAV.md</code></li><li>Decisions log: <code>docs/spec/08-meta/decisions-log.md</code></li><li>ADRs: <code>docs/adr/</code></li><li>Example plugin: <code>examples/plugins/hello</code></li></ul><h2 id="delivery-status" tabindex="-1">Delivery Status <a class="header-anchor" href="#delivery-status" aria-label="Permalink to &quot;Delivery Status&quot;">​</a></h2><p><strong>M6 — Plan</strong> was implemented and accepted on 2026-08-05 against these frozen details:</p><ol><li>shared Plan/session/shell contracts and protocol v10</li><li>schema v10 migration, immutable plan artifacts, and the <code>plan_approvals</code> execution fields/startup fence</li><li>Rust-authoritative Plan policy, shell identity, and process cancellation</li><li>one-Agent SubmitPlan/approval/execution state transitions</li><li>renderer artifact approval, shell selection, and EN/zh-CN UX</li><li>focused migration, policy, streaming, timeout, recovery, and rendered EN/zh-CN verification</li></ol><p>The frozen protocol remains v9 and storage schema remains v10. Future changes must preserve the automated M6 scenarios E2E-104 through E2E-117 or update the relevant decision record before changing the contract.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("spec/00-baseline.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _00Baseline = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _00Baseline as default
};
