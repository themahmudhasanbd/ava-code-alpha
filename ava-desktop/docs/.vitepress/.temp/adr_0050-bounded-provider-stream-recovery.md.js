import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0050: Bounded provider stream recovery and diagnostics","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0050-bounded-provider-stream-recovery.md","filePath":"adr/0050-bounded-provider-stream-recovery.md","lastUpdated":1789007486000}');
const _sfc_main = { name: "adr/0050-bounded-provider-stream-recovery.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0050-bounded-provider-stream-recovery-and-diagnostics" tabindex="-1">ADR 0050: Bounded provider stream recovery and diagnostics <a class="header-anchor" href="#adr-0050-bounded-provider-stream-recovery-and-diagnostics" aria-label="Permalink to &quot;ADR 0050: Bounded provider stream recovery and diagnostics&quot;">​</a></h1><ul><li>Status: Accepted (amended by D259 and D378, see below)</li><li>Date: 2026-08-04</li></ul><h2 id="amendment-d259-2026-09" tabindex="-1">Amendment (D259, 2026-09) <a class="header-anchor" href="#amendment-d259-2026-09" aria-label="Permalink to &quot;Amendment (D259, 2026-09)&quot;">​</a></h2><p>Decision D259 supersedes the retry arithmetic in points 2 and 3 of this ADR. The &quot;one provider-level retry&quot; during request setup and the &quot;retry once after a 750 ms backoff&quot; after a stream has started were replaced by one shared, bounded budget for non-429 transient failures: four retries after the initial attempt (five provider attempts in total), drawn from the same pool whether the failure lands during request setup or mid-stream, with a deterministic 1 s / 2 s / 4 s / 8 s schedule capped at 8 s and <code>retry-after</code> headers honoured first. <code>PROVIDER_ERROR</code> joined <code>STREAM_FAILED</code>, <code>NETWORK_ERROR</code>, and <code>TIMEOUT</code> in the retriable set. The constants live in <code>packages/agent-runtime/src/provider-retry.ts</code> (<code>PROVIDER_TRANSIENT_MAX_RETRIES</code>, <code>PROVIDER_SETUP_RETRY_INITIAL_DELAY_MS</code>, <code>PROVIDER_SETUP_MAX_RETRY_DELAY_MS</code>). The 429 rate-limit budget stays separate. Decision D378 (ADR 0206, 2026-09-10) then raised both budgets to ten retries after the initial attempt, shared as <code>PROVIDER_RETRY_MAX_RETRIES</code>, while keeping the 1 s / 2 s / 4 s schedule and its 8 s cap for non-429 waits. Everything else in this ADR (classification, single visible bubble, diagnostics, mutation recovery) still stands. The original text below is kept as written for the record.</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The provider retry layer covers failures while establishing a request, but a provider can also terminate after the assistant stream has started. The previous runtime classified the common <code>terminated</code> message as a generic <code>PROVIDER_ERROR</code>, ended the turn, and left regenerate to create a new runtime and reseed the transcript. This made transient stream failures expensive and made a long provider wait difficult to attribute from logs.</p><p>Repeated mutation failures had a related cost: the model could keep repairing an obsolete patch or retrying an <code>Edit</code> with stale context instead of taking a fresh snapshot of the target file.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>PI-Desktop applies the following bounded recovery strategy:</p><ol><li>Classify <code>terminated</code>, premature stream closure, and equivalent incomplete stream messages as retryable <code>STREAM_FAILED</code> errors. Preserve only safe, low-cardinality provider status/code fields in <code>AppError.details</code>.</li><li>Configure pi-ai request setup with one provider-level retry and an interruptible delay capped at 8 seconds. Forward the durable session id so provider adapters that support request affinity can reuse it.</li><li>When a transient <code>STREAM_FAILED</code>, <code>NETWORK_ERROR</code>, or <code>TIMEOUT</code> occurs after a stream has started, retry once in the same turn after a 750 ms abortable backoff. Remove the failed assistant from the next model context, reuse its visible assistant message id, and replace the partial content instead of creating a duplicate bubble. A second failure is terminal.</li><li>Attach <code>phase</code>, <code>providerWaitMs</code>, <code>streamMs</code>, provider status/code, and <code>retryAttempt</code> when available. These fields are diagnostic only; messages remain redacted and bounded and never contain credentials or raw provider bodies beyond the existing capped summary.</li><li>Mutation instructions select <code>Edit</code> for one small unique replacement and <code>Write</code> for a coherent whole-file rewrite. After an edit mismatch, the agent follows the line-anchored contract&#39;s fresh-read or complete-reveal recovery and may have three counted failures per path in one prompt. The third counted failed <code>Edit</code>, or the third failed shell patch command, returns pi-agent-core&#39;s terminating tool hint, so the agent stops with the exact mismatch. Shell <code>apply_patch</code>, <code>git apply</code>, and <code>patch</code> are explicitly treated as patch commands; the prompt directs the agent to use <code>Edit</code> or <code>Write</code> instead. It must not hand-edit unified-diff artifacts or issue concurrent mutations for one path.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A single transient stream termination no longer forces regenerate or a new transcript/runtime boundary.</li><li>The renderer sees one assistant bubble and one terminal lifecycle for a recovered turn.</li><li>Provider errors remain diagnosable from agent timing and app session logs without exposing secrets.</li><li>The retry budget is finite: persistent outages, authentication errors, context failures, and the second stream failure remain visible and actionable instead of entering an unbounded loop.</li><li>Mutation recovery is deterministic and bounded. A genuinely stale file or invalid shell patch requires a new user/model decision after the second failure rather than another automatic tool turn.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><h3 id="retry-every-provider-error-in-the-same-turn" tabindex="-1">Retry every provider error in the same turn <a class="header-anchor" href="#retry-every-provider-error-in-the-same-turn" aria-label="Permalink to &quot;Retry every provider error in the same turn&quot;">​</a></h3><p>Rejected because authentication, model-selection, malformed-request, and context errors are not fixed by replaying the same request.</p><h3 id="retry-stream-failures-indefinitely" tabindex="-1">Retry stream failures indefinitely <a class="header-anchor" href="#retry-stream-failures-indefinitely" aria-label="Permalink to &quot;Retry stream failures indefinitely&quot;">​</a></h3><p>Rejected because it hides persistent outages, consumes provider quota, and prevents the user from regaining control of the session.</p><h3 id="create-a-new-assistant-bubble-for-every-stream-attempt" tabindex="-1">Create a new assistant bubble for every stream attempt <a class="header-anchor" href="#create-a-new-assistant-bubble-for-every-stream-attempt" aria-label="Permalink to &quot;Create a new assistant bubble for every stream attempt&quot;">​</a></h3><p>Rejected because partial and failed attempts would look like multiple answers and would complicate durable transcript reconciliation.</p><h3 id="keep-patch-repair-entirely-prompt-driven" tabindex="-1">Keep patch repair entirely prompt-driven <a class="header-anchor" href="#keep-patch-repair-entirely-prompt-driven" aria-label="Permalink to &quot;Keep patch repair entirely prompt-driven&quot;">​</a></h3><p>Rejected because wording alone does not reliably bound a model&#39;s retry loop. The host&#39;s unique-context checks and per-session mutation serialization remain the execution boundary, while the runtime&#39;s terminating tool hint enforces the second Edit or shell-patch failure stop in pi-agent-core.</p><h2 id="references" tabindex="-1">References <a class="header-anchor" href="#references" aria-label="Permalink to &quot;References&quot;">​</a></h2><ul><li><code>docs/spec/03-runtime/02-agent-runtime.md</code></li><li><code>docs/spec/03-runtime/03-tools-and-permissions.md</code></li><li><code>docs/spec/03-runtime/08-error-codes.md</code></li><li><code>docs/spec/06-delivery/04-e2e-test-plan.md</code></li><li>Decision D186</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0050-bounded-provider-stream-recovery.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0050BoundedProviderStreamRecovery = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0050BoundedProviderStreamRecovery as default
};
