import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"05. 插件生命周期","description":"","frontmatter":{},"headers":[],"relativePath":"zh-CN/spec/07-plugins/05-plugin-lifecycle.md","filePath":"zh-CN/spec/07-plugins/05-plugin-lifecycle.md","lastUpdated":1789963612000}');
const _sfc_main = { name: "zh-CN/spec/07-plugins/05-plugin-lifecycle.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="_05-插件生命周期" tabindex="-1">05. 插件生命周期 <a class="header-anchor" href="#_05-插件生命周期" aria-label="Permalink to &quot;05. 插件生命周期&quot;">​</a></h1><blockquote><p><strong>翻译说明：</strong> 本页是与 <a href="/spec/07-plugins/05-plugin-lifecycle">英文源规格</a> 一一对应的机器辅助翻译。代码、协议字段和标识符保持原文；如翻译与英文源事实有歧义，以英文版本为准。</p></blockquote><h2 id="_1-目标" tabindex="-1">1. 目标 <a class="header-anchor" href="#_1-目标" aria-label="Permalink to &quot;1. 目标&quot;">​</a></h2><p>定义从发现到卸载的完整状态机，保证：</p><ul><li>可预测的行为</li><li>可恢复的故障</li><li>可审计的 start/stop</li><li>与命令面板/AgentTool 注册保持一致</li></ul><h2 id="_2-状态机" tabindex="-1">2. 状态机 <a class="header-anchor" href="#_2-状态机" aria-label="Permalink to &quot;2. 状态机&quot;">​</a></h2><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>discovered</span></span>
<span class="line"><span> → validated</span></span>
<span class="line"><span> → installed</span></span>
<span class="line"><span> → enabled</span></span>
<span class="line"><span> → loaded</span></span>
<span class="line"><span> → running</span></span>
<span class="line"><span> → load_error</span></span>
<span class="line"><span> → disabled</span></span>
<span class="line"><span> → install_error</span></span>
<span class="line"><span> → invalid</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br></div></div><h3 id="状态描述" tabindex="-1">状态描述 <a class="header-anchor" href="#状态描述" aria-label="Permalink to &quot;状态描述&quot;">​</a></h3><table tabindex="0"><thead><tr><th>状态</th><th>含义</th></tr></thead><tbody><tr><td>发现了</td><td>扫描插件目录或包</td></tr><tr><td>已验证</td><td>清单/文件完整性已通过</td></tr><tr><td>已安装</td><td>写入安装目录并注册</td></tr><tr><td>已启用</td><td>由用户启用，允许加载</td></tr><tr><td>已加载</td><td>运行时加载，贡献点注册</td></tr><tr><td>跑步</td><td>具有活动面板/后台逻辑</td></tr><tr><td>残疾人</td><td>已安装但被用户关闭</td></tr><tr><td>加载错误</td><td>启用后加载失败</td></tr><tr><td>安装错误</td><td>安装失败</td></tr><tr><td>无效</td><td>验证失败，无法使用</td></tr></tbody></table><h2 id="_3-生命周期挂钩" tabindex="-1">3. 生命周期挂钩 <a class="header-anchor" href="#_3-生命周期挂钩" aria-label="Permalink to &quot;3. 生命周期挂钩&quot;">​</a></h2><p>**今天实现：**运行时（<code>apps/desktop/electron/main/plugin-runtime.ts</code>）调用<code>onLoad</code>（当在load/enable上加载插件时）和<code>onUnload</code>（在unload/disable/reload/应用退出上调度到插件进程，预算5s——退出时为1.5s，然后进程停止）；卸载会删除插件注册的命令和工具。下面的其他挂钩在 API 中声明但尚未触发。</p><p>**计划：**一旦完整生命周期落地，钩子将按以下顺序触发：</p><ol><li><code>onInstall</code>（一次，仅在成功安装后） 2.<code>onEnable</code> 3.<code>onLoad</code> 4.运行时事件 5.<code>onUnload</code> 6.<code>onDisable</code> 7.<code>onUninstall</code></li></ol><h3 id="调用约束" tabindex="-1">调用约束 <a class="header-anchor" href="#调用约束" aria-label="Permalink to &quot;调用约束&quot;">​</a></h3><ul><li>Hooks 必须能够超时（默认 5 秒，可配置）</li><li>钩子异常不得使主机崩溃</li><li>如果<code>onLoad</code>失败，请输入<code>load_error</code>并自动回滚已注册的贡献点</li></ul><h2 id="_3-1-居民服务" tabindex="-1">3. 1 居民服务 <a class="header-anchor" href="#_3-1-居民服务" aria-label="Permalink to &quot;3. 1 居民服务&quot;">​</a></h2><p><code>contributes.services</code> 中声明的服务由主机驱动，而不是由主机驱动 插件自己的钩子，因此它的窗口严格在插件的生命周期内：</p><ol><li><code>onLoad</code>完成并注册贡献积分</li><li>对于每个声明的服务（每个插件最多 4 个，门控 <code>background.service</code>)，broker 在插件进程中调用 <code>service.start</code> 预算为 5 秒。启动失败标志着一个服务 <code>failed</code> 并离开 加载插件的其余部分。</li><li>卸载/禁用/重新加载时，<code>service.stop</code> <strong>先于</strong> <code>onUnload</code> 运行，因此 服务很安静，而插件仍然有其 API</li></ol><p>每项服务的状态为 <code>starting</code> | <code>running</code> | <code>stopped</code> | <code>failed</code> 加一个 重新启动计数，可通过插件 IPC 表面读取并显示在插件上 页。</p><h3 id="重启政策" tabindex="-1">重启政策 <a class="header-anchor" href="#重启政策" aria-label="Permalink to &quot;重启政策&quot;">​</a></h3><p>该服务存在于插件的主机进程中，因此崩溃会导致它崩溃 过程。然后主管重新启动整个插件：</p><ul><li>回退 <code>1s, 2s, 4s, 8s, 16s</code>，上限为 30 秒</li><li>最多重启5次；之后插件会在 <code>failed</code> 中保持关闭状态，因此用户 看到失败而不是无声的崩溃循环</li><li>存活 60 秒的进程被认为是健康的，并且退避重置为 零</li><li>服务上的 <code>autoRestart: false</code> 选择其插件完全不重新启动</li><li>当用户重新启用或删除插件时，将跳过重新启动 退避计时器待处理</li></ul><p>手动启用/禁用始终赢得主管的青睐：明确的操作 清除挂起的计时器和尝试计数器。</p><h3 id="应用退出" tabindex="-1">应用退出 <a class="header-anchor" href="#应用退出" aria-label="Permalink to &quot;应用退出&quot;">​</a></h3><p>退出会把每个插件宿主按 <strong>关闭</strong> 而不是崩溃来停止。先把每个插件标记为正在 释放并取消其待处理的重启，然后停止服务并运行 <code>onUnload</code>，各插件之间并行。</p><p>这就是两种退出的区别：一个没有被标记就死掉的宿主进程会被当成崩溃上报，也就 是一条错误日志、一个&quot;意外停止&quot;提示，以及主管把重启排进一个正在关闭的应用。 干净退出时这些都不允许发生。</p><p><strong>崩溃上报必须带上诊断。</strong> 崩溃路径会报出宿主进程的<strong>退出码</strong>：Windows 上的硬故障（<code>0xC0000005</code> 一类，Electron 以负的有 符号整数交付）与插件自己调用 <code>process.exit(1)</code> 是两类完全不同的问题。退出码会出现在加载错误、<code>failed</code> 服务状态、<code>plugin.crash</code> 审计记录与 <code>plugin</code> 日志通道里。插件 stdout/stderr 可能包含工作区数据或敏感信息，因此不会复制进崩溃上报。不新增任何持久化，也不涉及 新的权限或 API 面。</p><p>整个过程是有界的——每个插件的 <code>onUnload</code> 有 1.5s，整体拆除有 3s，超时后直接 杀掉子进程。插件的清理绝不能成为应用退出时看起来卡住的原因。</p><h2 id="_4-启用-禁用语义" tabindex="-1">4. 启用/禁用语义 <a class="header-anchor" href="#_4-启用-禁用语义" aria-label="Permalink to &quot;4. 启用/禁用语义&quot;">​</a></h2><h3 id="启用" tabindex="-1">启用 <a class="header-anchor" href="#启用" aria-label="Permalink to &quot;启用&quot;">​</a></h3><ul><li>将状态设置为启用</li><li>尝试加载</li><li>成功：注册命令/工具/技能/主题/MCP服务器，然后启动 居民服务</li><li>失败：自动回退到禁用状态并向用户显示错误。这被 D017 冻结（启用→加载失败自动禁用插件）。</li></ul><h3 id="禁用" tabindex="-1">禁用 <a class="header-anchor" href="#禁用" aria-label="Permalink to &quot;禁用&quot;">​</a></h3><ul><li>取消注册命令/工具</li><li>关闭面板</li><li>停止常驻服务并断开 MCP 服务器的连接</li><li>取消任何挂起的重启退避</li><li>致电 <code>onUnload</code> / <code>onDisable</code></li><li>继续作为残疾人</li></ul><h2 id="_5-启动恢复" tabindex="-1">5. 启动恢复 <a class="header-anchor" href="#_5-启动恢复" aria-label="Permalink to &quot;5. 启动恢复&quot;">​</a></h2><p>在应用程序启动时：</p><p>1.扫描已安装的插件 2. 读取使能状态 3.仅加载已启用的插件 4. 跳过单个失败的插件而不影响其他插件或主应用程序</p><h2 id="_6-开发者模式" tabindex="-1">6. 开发者模式 <a class="header-anchor" href="#_6-开发者模式" aria-label="Permalink to &quot;6. 开发者模式&quot;">​</a></h2><p><code>dev-loaded</code> 插件：</p><ul><li>未复制到 <code>installed</code></li><li>直接引用本地路径</li><li>可以观看和热重载</li><li>重新加载流程：<code>unload → validate → load</code></li></ul><p>热重载时：</p><ul><li>尽可能保留插件设置</li><li>不保证保留面板内存中状态</li></ul><h2 id="_7-贡献点register-unregister交易" tabindex="-1">7. 贡献点register/unregister交易 <a class="header-anchor" href="#_7-贡献点register-unregister交易" aria-label="Permalink to &quot;7. 贡献点register/unregister交易&quot;">​</a></h2><p>每个插件的加载过程应该大致是事务性的：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>begin</span></span>
<span class="line"><span> register commands</span></span>
<span class="line"><span> register tools</span></span>
<span class="line"><span> register skills</span></span>
<span class="line"><span> register themes</span></span>
<span class="line"><span> register MCP servers (lazy connect)</span></span>
<span class="line"><span>commit</span></span>
<span class="line"><span> start resident services</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><p>关于中途失败：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>rollback all registrations from this plugin</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>避免出现“命令存在但工具不存在”的半加载状态。</p><h2 id="_8-审计事件" tabindex="-1">8. 审计事件 <a class="header-anchor" href="#_8-审计事件" aria-label="Permalink to &quot;8. 审计事件&quot;">​</a></h2><p>至少记录：</p><ul><li>插件.安装</li><li>插件.卸载</li><li>插件.启用</li><li>插件.禁用</li><li>插件.加载.成功</li><li>插件加载错误</li><li>插件.卸载</li><li>插件崩溃</li><li>插件.服务.启动/插件.服务.停止</li><li>插件.服务.重新启动 / 插件.服务.重新启动.预定 -plugin.services.skipped（缺少权限或超过每个插件的上限）</li></ul><p>领域：</p><ul><li>插件ID</li><li>版本</li><li>源（<code>installed</code> | <code>dev</code> | <code>marketplace</code>）</li><li>TS</li><li>错误代码？</li><li>尝试？ / 延迟女士？ （服务重新启动）</li><li>exitCode?（崩溃）、exitCodeHex?（Windows 硬故障）</li></ul><h2 id="_9-卸载策略" tabindex="-1">9. 卸载策略 <a class="header-anchor" href="#_9-卸载策略" aria-label="Permalink to &quot;9. 卸载策略&quot;">​</a></h2><p>卸载前： 1.禁用+卸载 2. 调用<code>onUninstall</code> 3.删除已安装的文件 4.清理插件私有数据（可能会询问用户是否保留）</p><p>默认推荐：</p><ul><li>卸载时清理 settings/data（D016：卸载默认删除插件数据）</li><li>提供“保留数据”高级选项（可以推迟）</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("zh-CN/spec/07-plugins/05-plugin-lifecycle.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _05PluginLifecycle = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _05PluginLifecycle as default
};
