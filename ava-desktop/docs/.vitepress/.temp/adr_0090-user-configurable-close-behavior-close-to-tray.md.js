import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0090: User-Configurable Close Behavior with Close-to-Tray","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0090-user-configurable-close-behavior-close-to-tray.md","filePath":"adr/0090-user-configurable-close-behavior-close-to-tray.md","lastUpdated":1787654633000}');
const _sfc_main = { name: "adr/0090-user-configurable-close-behavior-close-to-tray.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0090-user-configurable-close-behavior-with-close-to-tray" tabindex="-1">ADR 0090: User-Configurable Close Behavior with Close-to-Tray <a class="header-anchor" href="#adr-0090-user-configurable-close-behavior-with-close-to-tray" aria-label="Permalink to &quot;ADR 0090: User-Configurable Close Behavior with Close-to-Tray&quot;">​</a></h1><ul><li>Status: Accepted for implementation</li><li>Date: 2026-08-12</li><li>Deciders: PI-Desktop core</li><li>Related: D230, D256, ADR 0021, ADR 0025, ADR 0078, ADR 0123</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>On Windows/Linux, closing the main window calls <code>app.quit()</code> (<code>window-all-closed</code>), so the app exits and its taskbar entry disappears. Minimizing already hides the window into the tray that D216 (ADR 0078) keeps resident on every platform, but users with long-running chats expect closing the window to keep the app available too. Different users want different defaults, and a fixed close-to-tray behavior would surprise users who expect close to quit.</p><p>The app has no close interception and no user-facing choice for this lifecycle decision. macOS is out of scope: the native Dock lifecycle (close keeps the app in the Dock, <code>activate</code> recreates the window) already matches the desired behavior.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>Windows/Linux close behavior becomes a persisted, user-configurable preference with three values, of which two are ever settable: <ul><li><code>ask</code>: the transient unset state — the first close prompts once. After a choice is made it is remembered permanently and cannot be reverted to prompting (<code>closeBehavior/set</code> rejects <code>ask</code>).</li><li><code>tray</code>: closing the window hides it under the resident D216 system-tray icon and keeps the app running; the tray menu restores the window or quits the app.</li><li><code>quit</code>: legacy behavior — closing the window exits the app.</li></ul></li><li>The first close with an unset preference shows a native modal dialog (main process) with Cancel / Close to tray / Quit. Picking a non-cancel option persists it forever; Cancel keeps the window open and leaves the preference unset (so the next close prompts again).</li><li>The preference is stored by Electron main in <code>&lt;data&gt;/close-behavior.json</code> (same ownership pattern as <code>window-state.json</code>), NOT in host-core settings: it is app-shell lifecycle state, read and written only by the main process, and needs no host RPC or schema change.</li><li>Two additive IPC channels expose it to the renderer: <code>pi-desktop/window/closeBehavior/get</code> (returns <code>{ behavior, supported }</code>) and <code>pi-desktop/window/closeBehavior/set</code>, which accepts only <code>tray</code> and <code>quit</code> (<code>ask</code> and unknown values fail with <code>INVALID_ARGUMENT</code>). <code>supported</code> is <code>false</code> on macOS, where the Settings row is hidden and <code>set</code> itself fails with <code>INVALID_ARGUMENT</code> — the renderer is not the only caller, so the guard lives in main.</li><li>Settings (General tab) renders a two-option radio segment — Close to tray / Quit app — for Windows/Linux only; an unset preference shows no selection. Changing it applies immediately. The tray icon is not reconciled: D216 owns it and it stays resident under either choice, because minimize-to-tray needs it whatever close does.</li><li>The close handler intercepts every non-macOS <code>close</code> that is not already an approved quit; <code>before-quit</code> (<code>quitting</code>) and macOS closes always fall through, so an explicit quit, the tray Quit item, and the automated boot probe are unaffected. Under <code>quit</code> the handler calls <code>app.quit()</code> itself rather than leaning on <code>window-all-closed</code>, and <code>window-all-closed</code> keys off the preference — it stays silent only under <code>tray</code>, keeping the app alive when the window is tray-hidden or destroyed unexpectedly, and quits otherwise even though the D216 tray is present. A <code>tray</code> close whose tray icon does not exist falls back to a real quit rather than hiding the window with no way back.</li><li>The bounds watchdog (<code>ensureStableBounds</code>) skips minimized and hidden windows, so minimize always stays minimized and a tray-hidden window is never force-restored by the Stage-Manager shelf recovery.</li><li>Known limitation: on Windows system shutdown/logoff, the OS may deliver a <code>close</code> that is intercepted while the preference is <code>ask</code> or <code>tray</code>. Windows force-terminates the session after its shutdown timeout, so no data is lost, but shutdown is not accelerated by the app.</li></ol><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Store the preference in host-core settings (<code>AppSettings</code>):</strong> rejected because it would add a Rust schema field and host RPC surface for pure shell lifecycle state that only Electron main consumes.</li><li><strong>Renderer-drawn first-close dialog:</strong> rejected because the window is closing; a native modal keeps the decision in the process that owns the close lifecycle and works before the renderer has mounted.</li><li><strong>Always close-to-tray without a choice:</strong> rejected — it changes the meaning of the close button for users who expect exit.</li><li><strong>Tray icon reconciled with the preference:</strong> rejected; D216 keeps one tray icon resident on every platform for minimize-to-tray, so destroying it when close behavior is <code>quit</code> would break an unrelated feature. Close behavior decides what a close does, not whether the tray exists.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Windows/Linux explicit minimize uses the normal taskbar transition; a close choice of <code>tray</code> still hides the window under the resident D216 tray icon. macOS native minimize remains tray-resident under D216.</li><li>Close on Windows/Linux either hides to tray or quits, per user choice, remembered across launches and changeable in Settings.</li><li>The tray menu and the first-close dialog reuse the existing <code>@pi-desktop/i18n</code> catalogs (English and Simplified Chinese).</li><li>No host protocol, storage schema, or macOS behavior changes.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0090-user-configurable-close-behavior-close-to-tray.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0090UserConfigurableCloseBehaviorCloseToTray = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0090UserConfigurableCloseBehaviorCloseToTray as default
};
