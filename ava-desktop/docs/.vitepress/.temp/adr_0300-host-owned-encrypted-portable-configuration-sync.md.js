import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0300: Host-owned encrypted portable configuration sync","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0300-host-owned-encrypted-portable-configuration-sync.md","filePath":"adr/0300-host-owned-encrypted-portable-configuration-sync.md","lastUpdated":1790050795000}');
const _sfc_main = { name: "adr/0300-host-owned-encrypted-portable-configuration-sync.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0300-host-owned-encrypted-portable-configuration-sync" tabindex="-1">ADR 0300: Host-owned encrypted portable configuration sync <a class="header-anchor" href="#adr-0300-host-owned-encrypted-portable-configuration-sync" aria-label="Permalink to &quot;ADR 0300: Host-owned encrypted portable configuration sync&quot;">​</a></h1><ul><li>Status: Accepted for implementation</li><li>Date: 2026-09-22</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>PI-Desktop has portable preferences and user-owned capabilities spread across Rust-owned SQLite, capability files, the host secret store, project groups, and plugin metadata. Copying the database or the local secrets directory would export device state, break ownership boundaries, and could activate executable content on another machine. A cloud backup must also converge concurrent edits without trusting WebDAV timestamps or silently replacing local data.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Add a host-core configuration-sync subsystem. It uses explicit per-domain capture and apply adapters, encrypted immutable revision/resource objects, an encrypted CAS head, and the existing host secret store for the WebDAV password and local vault-key reference. Argon2id derives a wrapping key from a separate backup password; AES-256-GCM authenticates every stored object with purpose/vault-derived object keys and domain-separated associated data.</p><p>The renderer receives only typed status, preview, conflict, and activation summaries. Electron Main is a thin lifecycle/transport bridge. Portable secrets require explicit opt-in; OAuth sessions, cookies, local encryption keys, readiness flags, plugin binaries, arbitrary files, and local paths are not portable. App-managed instructions are limited to the fixed global <code>~/.pi/agent/AGENTS.md</code> and registered project-root <code>AGENTS.md</code> files; nested repository files are not scanned. Imported executable or security-sensitive content remains staged until the receiving device approves the entity digest. Project and workspace bindings are local overlays and require explicit mapping. Standalone project identities are assigned and retained by Host metadata; project-group roots use the portable group identity plus ordered root position, so absolute folder paths never become cross-device identity.</p><p>Strict WebDAV initialization and head publication require reliable conditional writes: <code>If-None-Match: *</code> for creation and strong-ETag <code>If-Match</code> for updates. A CAS failure restarts reconciliation. ADR 0301 defines the separately confirmed append-only compatibility mode for servers that fail this probe; there is no silent downgrade to unconditional last-writer-wins. HTTPS is the default transport; the settings UI may explicitly acknowledge LAN HTTP risk, and Host restricts that exception to localhost, <code>.local</code>, or private / link-local IP addresses. Public HTTP endpoints remain rejected.</p><p>The capability probe may record an endpoint-specific <code>502 Bad Gateway</code> response for a missing object, because some WebDAV gateways use that status instead of <code>404</code>. Only that observed status is treated as absence for subsequent reads; arbitrary <code>502</code> responses are not globally treated as an empty remote. This compatibility does not change strict mode; servers that ignore conditional-write headers are supported only through the explicit append-only mode in ADR 0301.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><p>Positive:</p><ul><li>The renderer and Agent Runtime never gain direct access to WebDAV, encryption keys, or raw exported secrets.</li><li>Existing local persistence owners remain authoritative; no parallel provider, MCP, skill, or subagent database is introduced.</li><li>Immutable revisions allow authenticated recovery and deterministic three-way merge, while category opt-out remains subscription policy rather than a remote delete.</li><li>A fresh device cannot activate a synchronized command, endpoint, script, or automation without local review.</li></ul><p>Costs and limits:</p><ul><li>A malicious WebDAV server can still deny availability or roll back a fresh device that has no trusted head history; this design protects confidentiality and integrity, not availability.</li><li>Folder mappings support ordered multi-root project groups. History/restore, backup-password rewrap, bounded Skill package resources, and an in-process WebDAV conditional-write fixture are part of the Host-owned baseline. A restore creates a new revision and a local recovery point; it does not make an old copied vault key revocable.</li><li>Strict conditional-write behavior is stricter than many simple WebDAV servers. The explicit append-only mode is a labeled bidirectional compatibility path with retained history and weaker publication guarantees; it must not be presented as strict CAS.</li></ul><h2 id="alternatives-rejected" tabindex="-1">Alternatives rejected <a class="header-anchor" href="#alternatives-rejected" aria-label="Permalink to &quot;Alternatives rejected&quot;">​</a></h2><ol><li>Exporting SQLite or the application data directory: violates ownership, leaks device-specific state, and cannot express approval or merge policy.</li><li>Uploading a plaintext JSON backup: exposes all configuration and secrets to the WebDAV server and every transport intermediary.</li><li>Last-writer-wins based on wall-clock timestamps: loses edits under clock skew and cannot safely resolve delete-versus-edit or executable changes.</li><li>Scheduling synchronization in Electron Main or the renderer: creates a second owner for sync lifecycle and weakens host durability guarantees.</li></ol></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0300-host-owned-encrypted-portable-configuration-sync.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0300HostOwnedEncryptedPortableConfigurationSync = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0300HostOwnedEncryptedPortableConfigurationSync as default
};
