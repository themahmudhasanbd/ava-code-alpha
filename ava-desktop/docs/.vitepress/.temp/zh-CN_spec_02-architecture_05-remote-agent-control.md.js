import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"远程 Agent 控制目标架构","description":"","frontmatter":{},"headers":[],"relativePath":"zh-CN/spec/02-architecture/05-remote-agent-control.md","filePath":"zh-CN/spec/02-architecture/05-remote-agent-control.md","lastUpdated":1789036160000}');
const _sfc_main = { name: "zh-CN/spec/02-architecture/05-remote-agent-control.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="远程-agent-控制目标架构" tabindex="-1">远程 Agent 控制目标架构 <a class="header-anchor" href="#远程-agent-控制目标架构" aria-label="Permalink to &quot;远程 Agent 控制目标架构&quot;">​</a></h1><ul><li>状态：目标规格，属于 MVP 之后</li><li>决策：D373 / ADR 0205，经 D374 与 D375 修订</li><li>英文源规格：<a href="/spec/02-architecture/05-remote-agent-control">英文源规格</a></li></ul><p>本页是英文规范的中文导读。远程控制当前仍不属于 MVP，不会把现有 Electron IPC、<code>host.proxy</code> 或 Rust host-core 暴露到网络。</p><h2 id="_1-范围、顺序与原则" tabindex="-1">1. 范围、顺序与原则 <a class="header-anchor" href="#_1-范围、顺序与原则" aria-label="Permalink to &quot;1. 范围、顺序与原则&quot;">​</a></h2><p>目标是让经过认证的远程客户端查看和控制 Agent Host，同时保持会话、 回合、回合队列、事件游标、审批、附件、工作区和权限都由 Host 掌权。</p><p>D375 固定了拓扑的交付顺序：首个远程部署是桌面本身作为远端无头 Host 的 客户端，经 SSH 隧道连接（#176、#140）；第二个是 Host 旁的出站消息集成 （#100）。Gateway 路由与浏览器访问保留完整规格，但在有需求信号或产品决策 之前不排期。</p><ul><li>客户端可以断开，Agent 回合仍继续。</li><li>重连使用快照或 <code>{ epoch, sequence }</code> 游标回放，不依赖时间戳。</li><li><code>RACP-WS</code> 是 v1 唯一规范绑定，首先部署在 SSH 隧道上；<code>RACP-HTTP</code> 是 浏览器 profile，不排期；<code>RACP-GRPC</code> 保留。</li><li>Host 是无头模块：会话与回合准入、回合队列、审批代理、事件日志和快照 构建都不依赖 Electron，桌面 IPC、本地 MCP、RACP 和消息集成都是它的调用方。</li><li>远程会话完全存在于其 Host 上：transcript、工具、工作区、权限和 provider secret 都在运行 Host 的机器上，桌面只展示和控制。</li><li>用户本地化是结构性的：控制链路中没有任何项目方运营的服务，所有凭据由用户 自己的 Host 签发，出站连接只有用户的 SSH 主机、用户配置的消息渠道与模型 provider，以及只读的 GitHub Releases <code>pi-host</code> 下载（D385）。</li><li>本地 stdio NDJSON JSON-RPC、Rust host-core 和 loopback MCP 保持不变。</li></ul><h2 id="_2-参考实现" tabindex="-1">2. 参考实现 <a class="header-anchor" href="#_2-参考实现" aria-label="Permalink to &quot;2. 参考实现&quot;">​</a></h2><p>设计参考 OpenAI Codex App Server、VS Code Agent Host、MCP Transport 和 Google A2A 的分层方式；SSH 隧道拓扑沿用 VS Code Remote-SSH 与 JetBrains Gateway 的模式，服务端经用户自己的 SSH 会话引导，客户端通过转发的 loopback 端口连接。不恢复已撤回的子代理 A2A/Peer 协调通道。</p><h2 id="_3-组件职责" tabindex="-1">3. 组件职责 <a class="header-anchor" href="#_3-组件职责" aria-label="Permalink to &quot;3. 组件职责&quot;">​</a></h2><table tabindex="0"><thead><tr><th>组件</th><th>职责</th><th>不得拥有</th></tr></thead><tbody><tr><td>Remote Client</td><td>展示状态、发送用户意图、回答审批和输入</td><td>工作区权限、provider 凭据、最终权限决定、prompt 队列</td></tr><tr><td>桌面 RACP 客户端适配层（Electron Main）</td><td>通过现有 <code>lib/api.ts</code> 表面把远端 Host 呈现给 renderer；负责 SSH 引导、配对和端口转发</td><td>第二份 transcript 存储；在本地执行远端工具</td></tr><tr><td>Agent Host</td><td>拥有会话、回合、每会话回合队列、事件游标、附件、工具执行和生命周期</td><td>浏览器展示状态</td></tr><tr><td>无头 Agent Host 模块（<code>packages/agent-host</code>）</td><td>会话/回合准入、回合队列、审批代理、内存事件日志、快照构建；向桌面 IPC、本地 MCP、RACP 和集成暴露同一套 API</td><td>Electron、renderer 或传输依赖；第二套权限或持久化实现</td></tr><tr><td><code>pi-host</code> 无头包</td><td>在远端机器上以桌面同版本运行模块、Node pi sidecar 和 Rust host-core，只绑定 loopback，由引导脚本从 GitHub Releases 下载</td><td>桌面 UI、插件面板、其他 Host 的 secret</td></tr><tr><td>消息集成适配层</td><td>在 Host 进程内订阅 Host 范围事件，把脱敏摘要转发到出站渠道；把固定指令词汇映射到回合与审批操作</td><td>自己的权限策略、入站监听器、原始 transcript 内容</td></tr><tr><td>自托管 Gateway（不排期）</td><td>以 Host 签发的设备凭据准入、路由、Host link、限流、审计、上传字节的瞬态缓冲、（保留）推送脱敏摘要</td><td>provider secret、完整 transcript、host-core 访问、上传窗口之外的附件字节</td></tr><tr><td>Node pi sidecar</td><td>运行 pi Agent 和 provider stream</td><td>远程认证、工作区策略、secret storage</td></tr><tr><td>Rust host-core</td><td>SQLite、工具、工作区、权限及待处理权限表、secret 和本地持久化</td><td>公网监听器</td></tr></tbody></table><h2 id="_4-部署拓扑" tabindex="-1">4. 部署拓扑 <a class="header-anchor" href="#_4-部署拓扑" aria-label="Permalink to &quot;4. 部署拓扑&quot;">​</a></h2><h3 id="_4-1-当前本地桌面" tabindex="-1">4.1 当前本地桌面 <a class="header-anchor" href="#_4-1-当前本地桌面" aria-label="Permalink to &quot;4.1 当前本地桌面&quot;">​</a></h3><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>PI-Desktop</span></span>
<span class="line"><span>├── Electron Main</span></span>
<span class="line"><span>│   ├── Renderer</span></span>
<span class="line"><span>│   ├── Node pi sidecar</span></span>
<span class="line"><span>│   └── Rust host-core</span></span>
<span class="line"><span>└── optional loopback MCP</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><h3 id="_4-2-ssh-隧道上的远端-host-首个远程拓扑" tabindex="-1">4.2 SSH 隧道上的远端 Host（首个远程拓扑） <a class="header-anchor" href="#_4-2-ssh-隧道上的远端-host-首个远程拓扑" aria-label="Permalink to &quot;4.2 SSH 隧道上的远端 Host（首个远程拓扑）&quot;">​</a></h3><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>PI-Desktop (Remote Client)              Remote machine</span></span>
<span class="line"><span>├── Renderer ── lib/api.ts ─┐           ┌── pi-host (headless Agent Host)</span></span>
<span class="line"><span>├── Electron Main           │ RACP-WS   │   ├── packages/agent-host</span></span>
<span class="line"><span>│   └── RACP client adapter ┼─ over SSH ┼──▶│   ├── Node pi sidecar</span></span>
<span class="line"><span>└── local sessions          │ forward   │   │   └── Rust host-core (loopback)</span></span>
<span class="line"><span>    (unchanged)             │           │   └── workspace, ~/.agents, secrets</span></span>
<span class="line"><span>                            │           └── sshd</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p>引导只走用户自己的 SSH 会话，不走 RACP：桌面用现有 SSH 配置登录，上传一段引导脚本，由它从 GitHub Releases 下载与桌面同版本的 <code>pi-host</code> 包并校验公布的 SHA-256，启动它并绑定 loopback，经 SSH 通道拿到一次性 配对 token，转发本地端口后以 header profile 连接 <code>RACP-WS</code>，用配对 token 换取 设备 token 存入桌面安全存储；Host 把该桌面设备记为 <code>owner</code>。远端 Host 的 provider 配置由引导步骤经 SSH 通道写入，是 Host 本地配置，绝不经过 RACP。 Host 只绑定 loopback；只有绑定地址与对端地址都是 loopback且出示有效设备 token 时才接受明文 <code>ws://</code>，因为 SSH 通道已提供机密性，SSH 登录也已证明对该机器的 shell 访问。非 loopback 绑定仍要求 TLS 与设备 token。首版无法引导没有 GitHub 出网能力的机器。</p><h3 id="_4-3-自托管-gateway-不排期" tabindex="-1">4.3 自托管 Gateway（不排期） <a class="header-anchor" href="#_4-3-自托管-gateway-不排期" aria-label="Permalink to &quot;4.3 自托管 Gateway（不排期）&quot;">​</a></h3><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Client ── HTTPS/WSS ── Gateway</span></span>
<span class="line"><span>                         │ outbound WSS Host link (relay)</span></span>
<span class="line"><span>                         ▼</span></span>
<span class="line"><span>                      Agent Host ── pi + Rust host-core</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>生产模式由 Agent Host 主动建立出站 Host link，Gateway 不要求桌面开放入站 端口。Host link 是中继 profile，复用多个逻辑客户端连接。PI 不运营 Gateway：若日后 排期，由用户在自己的基础设施上运行，并以 Host 签发的设备凭据准入（D385）。本拓扑 保留规格以免契约漂移，但不排期。</p><h2 id="_5-角色、队列、归属与事件同步" tabindex="-1">5. 角色、队列、归属与事件同步 <a class="header-anchor" href="#_5-角色、队列、归属与事件同步" aria-label="Permalink to &quot;5. 角色、队列、归属与事件同步&quot;">​</a></h2><p>每个会话允许多个 viewer，但同一时间只允许一个活动回合。controller 可以启动、排队、停止或中断回合并回答输入请求，approver 只能在授权范围内 解决审批，owner 管理成员。排队中的回合是 Host 状态，由 host-core 持久化，重启后恢复并保持挂起直到有 controller 接入，本地桌面和所有远程客户端看到同一份队列。经 Gateway 路由的主体受远程权限上限约束；经 SSH 引导 配对的桌面设备持有 <code>owner</code> 并豁免上限，因为 SSH 访问已经超过上限能限制的一切； Host 策略 <code>applyCeilingToPairedDevices</code>（默认关闭）可重新施加。</p><p>远程会话的归属划分：远端 Host 拥有 transcript 与 SQLite、回合与队列、内置工具 目录与工作区边界、权限与会话授权、provider secret、该机器 <code>~/.agents</code> 下的 skills 与子代理定义、该 Host 配置的 MCP 服务器和定时任务；桌面保留窗口与 shell、本地会话、本地应用的设置 UI、插件面板、浏览器预览和通知展示。桌面通过 <code>tools/advertise</code> 公布其用户配置的 MCP 服务器和不需要会话工作区的插件工具，它们 以中继工具身份出现在远程会话目录中，经 <code>tool/execute</code> 服务端请求在桌面自身的插件 权限下执行；需要工作区或文件系统访问的插件工具被排除，因为它们会作用于桌面的 文件系统。工作面板的文件列表、文件读取和 diff 使用远端 Host profile 操作；终端 通过 <code>terminal/*</code> 操作在远端机器运行并带有界回放环；浏览器预览留在本地。</p><p>Host 为每个会话生成 <code>epoch</code>，并在其中为持久事件分配严格递增的 <code>sequence</code>。delta、工具进度和活动阶段是瞬态事件，只带 <code>afterSequence</code>， 不进入回放窗口；快照的 <code>activeItems</code> 携带它们累计的内容。首个实现把持久 日志放在 Host 进程内存，Host 重启即开启新 epoch。</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>cursor in current epoch and retained -&gt; replay durable sequence &gt; after</span></span>
<span class="line"><span>epoch changed or cursor evicted      -&gt; resync.required + snapshot</span></span>
<span class="line"><span>cursor ahead                         -&gt; reject and refresh snapshot</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Client -&gt; initialize / attach / subscribe</span></span>
<span class="line"><span>Client -&gt; turn/start (admission)</span></span>
<span class="line"><span>Host   -&gt; turn.queued | turn.started, ordered events</span></span>
<span class="line"><span>Host   -&gt; approval/request or input/request when required</span></span>
<span class="line"><span>Client -&gt; approval/respond | input/respond</span></span>
<span class="line"><span>Host   -&gt; turn terminal event, next queued turn starts</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><p>远程审批使用与桌面相同的决策词汇：工具审批为 <code>allow-once</code>、 <code>allow-session</code>、<code>deny</code>；Plan/Goal 审批为带显式权限模式的 <code>approve</code> 或 <code>reject</code>；asktool 输入支持逐题回答或跳过。修改持久模式使用远端 Host profile 的 <code>session/configure</code>，与本地一样仅限空闲时。审批寿命由 Host 策略决定：本地 默认仍是 120 秒后拒绝，有远程订阅者接入时默认 30 分钟（D375），有界且可由运维调整。</p><h2 id="_6-传输档案" tabindex="-1">6. 传输档案 <a class="header-anchor" href="#_6-传输档案" aria-label="Permalink to &quot;6. 传输档案&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Profile</th><th>Intended client</th><th>Direction</th><th>Status</th></tr></thead><tbody><tr><td>Local stdio JSON-RPC</td><td>Electron Main / sidecars</td><td>Full duplex</td><td>Existing</td></tr><tr><td>RACP-WS</td><td>经 SSH 的桌面客户端、Native、Electron</td><td>Full duplex</td><td>v1 规范绑定，首先部署在 SSH 隧道上</td></tr><tr><td>RACP-HTTP</td><td>Browser、simple integrations</td><td>Commands + server stream</td><td>浏览器 profile，不排期</td></tr><tr><td>RACP-GRPC</td><td>Native service</td><td>Unary + server stream</td><td>保留，不在 v1</td></tr><tr><td>Host link <code>racp-hostlink.v1</code></td><td>Gateway → Host</td><td>Multiplexed full duplex</td><td>基于 RACP-WS 帧的中继 profile，随 Gateway 不排期</td></tr></tbody></table><h2 id="_7-故障与迁移" tabindex="-1">7. 故障与迁移 <a class="header-anchor" href="#_7-故障与迁移" aria-label="Permalink to &quot;7. 故障与迁移&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Failure</th><th>Required behavior</th></tr></thead><tbody><tr><td>Client disconnect</td><td>回合继续，保留持久事件回放窗口</td></tr><tr><td>Client reconnect</td><td>重新认证，按游标回放或返回快照</td></tr><tr><td>SSH tunnel drop</td><td>桌面适配层重建转发并按游标恢复，远端回合继续</td></tr><tr><td>Gateway disconnect</td><td>Host 有界退避重连 Host link，本地回合继续</td></tr><tr><td>Host unavailable</td><td>拒绝新的 mutation，不自动重放</td></tr><tr><td>Host restart</td><td>新 epoch，客户端从快照重同步，持久化的队列按序恢复并挂起到 controller 接入，已开始的工作不重放</td></tr><tr><td>Host crash</td><td>使用现有恢复策略，已中断工作不自动重放</td></tr><tr><td>Duplicate mutation</td><td>相同主体和 key 返回原 idempotent 结果</td></tr><tr><td>Durable event gap</td><td>停止应用并请求快照</td></tr><tr><td>Slow client</td><td>先丢弃瞬态事件，丢失持久事件前带游标断开</td></tr><tr><td>Expired approval</td><td>返回 <code>APPROVAL_EXPIRED</code>，不执行工具</td></tr></tbody></table><p>首个实现交付无头 <code>packages/agent-host</code> 模块，Electron Main 承载它，现有 IPC handler 成为它的适配层；随之落地 host-core 的 <code>permissions.pending</code> 读取和用 host-core 持久化的 Host 队列替换 renderer 内存队列（需单独 ADR 与 schema 升级， D375）。SSH 隧道里程碑再加两件东西而不改线上契约：<code>pi-host</code> 包，以及位于 <code>lib/api.ts</code> 之下的桌面 RACP 客户端适配层；同一里程碑还包含 <code>tools/advertise</code> / <code>tool/execute</code> 中继与 <code>terminal/*</code> 操作。消息集成是 模块在 Host 进程内的又一个调用方，不需要任何传输。</p><h2 id="_8-验收要点" tabindex="-1">8. 验收要点 <a class="header-anchor" href="#_8-验收要点" aria-label="Permalink to &quot;8. 验收要点&quot;">​</a></h2><p>远程客户端不能取得 host-core、IPC、<code>host.proxy</code> 或 provider secret；回合 断线后仍可继续；晚接入的客户端能看到已打开的审批；经 Gateway 的回合不超过 权限上限；远程会话的 transcript、工具、工作区和 secret 都在远端 Host，桌面 只保留展示状态；远程会话的目录包含远端 Host 的工具与桌面公布的中继工具，中继 工具只在桌面执行；会话终端在远端机器的会话根内运行且只对策略允许的主体开放；回放、审批、幂等和 所有已发布绑定的一致性测试必须通过；无头模块的测试不依赖 Electron。</p><p>详见英文源规格中的完整组件归属、迁移边界和验收条款。</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("zh-CN/spec/02-architecture/05-remote-agent-control.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _05RemoteAgentControl = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _05RemoteAgentControl as default
};
