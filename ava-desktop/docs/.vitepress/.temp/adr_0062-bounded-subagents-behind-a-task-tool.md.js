import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0062: Bounded Subagents Behind a Task Tool","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0062-bounded-subagents-behind-a-task-tool.md","filePath":"adr/0062-bounded-subagents-behind-a-task-tool.md","lastUpdated":1789643736000}');
const _sfc_main = { name: "adr/0062-bounded-subagents-behind-a-task-tool.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0062-bounded-subagents-behind-a-task-tool" tabindex="-1">ADR 0062: Bounded Subagents Behind a Task Tool <a class="header-anchor" href="#adr-0062-bounded-subagents-behind-a-task-tool" aria-label="Permalink to &quot;ADR 0062: Bounded Subagents Behind a Task Tool&quot;">​</a></h1><ul><li>Status: Accepted for implementation (definition roots amended by ADR 0112; timeout policy amended by ADR 0119; delegation presentation amended by D265; opt-in parent-tool inherit amended by ADR 0246; resumable delegations amended by ADR 0279)</li><li>Date: 2026-08-06</li><li>Deciders: PI-Desktop core</li><li>Related: D201, ADR 0041 (persistence outbox), ADR 0048 (lazy per-turn tool activation), ADR 0053 (plan checkpoint and execution epoch), D123 (prompt template documents), D138 (session-scoped inline permission requests), D198 (contract modes), ADR 0112 (capability roots and Settings IA), ADR 0119 (event-driven subagent timeouts), D265 (one delegation reads as a card too), ADR 0246 (opt-in <code>tools: inherit</code>), ADR 0279 (resumable delegations)</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>A single agent loop pays for everything it reads. Wide searches, long build logs and multi-file surveys are separable work whose intermediate output is worthless once the conclusion is known, yet all of it lands in the session&#39;s context window and stays there for the rest of the task. Automatic compaction (ADR 0049) recovers space after the fact; it cannot avoid spending it.</p><p>Delegation is the standard answer: run the separable piece in its own context and return only a report. Adding it to PI-Desktop touches every layer, and the open questions are not about the loop itself but about the boundaries — where definitions come from, what a delegate may do to the workspace, how a delegate&#39;s rows relate to the parent&#39;s model context and to the transcript, and what happens when several delegates want the user&#39;s attention at once.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><h3 id="_1-definitions-are-markdown-documents-builtin-plus-global-user-documents" tabindex="-1">1. Definitions are Markdown documents, builtin plus global user documents <a class="header-anchor" href="#_1-definitions-are-markdown-documents-builtin-plus-global-user-documents" aria-label="Permalink to &quot;1. Definitions are Markdown documents, builtin plus global user documents&quot;">​</a></h3><p>A subagent definition is frontmatter plus a Markdown body that becomes the delegate&#39;s system prompt, mirroring prompt templates (D123):</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>~/.agents/subagents/&lt;name&gt;.md</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>PI-Desktop ships three builtins inline in <code>agent-runtime</code> (<code>explorer</code>, <code>code-reviewer</code>, <code>test-runner</code>). User documents under <code>~/.agents/subagents</code> are combined with the builtins by name. There is no project-level subagent capability source; the global user catalog is the only user-managed layer. The catalog is re-read on every session launch, capped at <code>MAX_SUBAGENT_DEFINITIONS</code> (16), and a malformed document degrades to a launch diagnostic — it never costs the session its other delegates or its turn.</p><p>Frontmatter keys: <code>name</code>, <code>description</code>, <code>tools</code>, <code>model</code>, <code>thinkingLevel</code>, <code>maxTurns</code>, <code>idle-timeout</code>, and <code>max-duration</code>. Key spelling is matched loosely (<code>max-turns</code>, <code>max_turns</code>, <code>maxTurns</code>). <code>maxTurns</code> is optional; omission, <code>none</code>, and <code>0</code> mean unlimited turns. The timeout defaults and bounds are defined by ADR 0119.</p><h3 id="_2-a-definition-declares-its-tools-and-is-read-only-when-it-does-not" tabindex="-1">2. A definition declares its tools, and is read-only when it does not <a class="header-anchor" href="#_2-a-definition-declares-its-tools-and-is-read-only-when-it-does-not" aria-label="Permalink to &quot;2. A definition declares its tools, and is read-only when it does not&quot;">​</a></h3><p><code>tools</code> may only name file, search and shell tools (<code>SUBAGENT_ASSIGNABLE_TOOLS</code>: Read, Glob, Grep, BrowserPreview, Bash, Edit, Write). Plugin, skill, mode and meta tools are out of reach: a delegate is a bounded worker, not a second full session. A definition that omits <code>tools</code> gets <code>Read, Glob, Grep</code>, and <code>tools: &quot;*&quot;</code> expands to the assignable set rather than to everything the session has. A delegate never inherits mutation rights from the parent session — write capability comes only from its own declaration.</p><p>Every delegate tool call goes through the same <code>tools.execute</code> host path as the parent&#39;s, so containment, permission modes and hard denies are unchanged. A delegate cannot ask the model-facing questions the parent can: no plan or goal submission, no mode transition, no nested <code>Task</code>.</p><h3 id="_3-a-definition-may-pin-its-own-provider-and-model" tabindex="-1">3. A definition may pin its own provider and model <a class="header-anchor" href="#_3-a-definition-may-pin-its-own-provider-and-model" aria-label="Permalink to &quot;3. A definition may pin its own provider and model&quot;">​</a></h3><p><code>model: &lt;provider&gt;/&lt;model&gt;</code> binds a delegate to a specific model regardless of the session&#39;s. Pins are resolved once per launch in Electron main, where credentials and the pi model catalog live, and are capped at <code>MAX_SUBAGENT_PROVIDERS</code> (8) distinct providers. Because stored provider ids are UUIDs, a pin matches on id, vendor key or display name.</p><p>An unresolvable pin (no matching provider, no API key) is left out of the binding map instead of falling back to the session provider, and the <code>Task</code> call fails with a tool error naming the pin. A definition that asks for a cheap model must never silently spend the expensive one.</p><h3 id="_4-delegation-is-one-agent-mode-tool-and-parallel-by-construction" tabindex="-1">4. Delegation is one Agent-mode tool, and parallel by construction <a class="header-anchor" href="#_4-delegation-is-one-agent-mode-tool-and-parallel-by-construction" aria-label="Permalink to &quot;4. Delegation is one Agent-mode tool, and parallel by construction&quot;">​</a></h3><p>The <code>Task</code> tool takes <code>agent</code>, <code>task</code> and an optional short <code>description</code>. The catalog of available delegates rides in the tool description rather than the system prompt, because the two change together.</p><p><code>Task</code> is offered only in Agent mode. Plan and Goal are read-only contract negotiations (D198), and a delegate with Bash or Edit would drive straight through one.</p><p>Concurrency is expressed through pi&#39;s execution modes: the session Agent runs with <code>toolExecution: &quot;parallel&quot;</code>, every catalog tool carries <code>executionMode: &quot;sequential&quot;</code>, and <code>Task</code> alone carries <code>&quot;parallel&quot;</code>. pi runs a batch sequentially as soon as it contains one sequential tool, so the only batch that fans out is a batch of nothing but <code>Task</code> calls. Existing tool ordering guarantees are untouched. Fan-out is capped at <code>MAX_SUBAGENT_CONCURRENCY</code> (4) by a semaphore. Delegate runtime bounds are the event-driven idle and total-duration watchdogs in ADR 0119; an explicit per-definition <code>maxTurns</code> remains an optional hard backstop with a maximum of 80.</p><p>Fan-out makes one same-process ordering problem real, and the sidecar owns it. host-core admits one mutation per session at a time, so concurrent writes cannot tear, but it defines no order between same-path mutations — and the sidecar&#39;s edit-recovery contract allows three counted failures per path before terminating the prompt, which only means &quot;re-read and retry&quot; if the attempts were ordered. A <code>PathMutex</code> in the sidecar serializes mutating calls per normalized path; different paths never wait on each other, which is the point. Delegates also run under the same bounded provider retry policy as the parent (one retry, 8s delay cap), so a fan-out cannot turn one failing provider into a retry storm.</p><h3 id="_5-the-parent-s-context-gains-the-report-and-only-the-report" tabindex="-1">5. The parent&#39;s context gains the report, and only the report <a class="header-anchor" href="#_5-the-parent-s-context-gains-the-report-and-only-the-report" aria-label="Permalink to &quot;5. The parent&#39;s context gains the report, and only the report&quot;">​</a></h3><p>A <code>SubagentRun</code> is a second pi <code>Agent</code> in the same sidecar process. Its final message, bounded to <code>MAX_SUBAGENT_REPORT_CHARS</code> (12k), becomes the <code>Task</code> tool result, with <code>agent</code>, <code>status</code>, <code>turns</code>, <code>toolCalls</code> and <code>usage</code> as structured details.</p><p>Delegate messages and tool rows are still emitted and persisted — they are what makes a delegation reviewable — but every row carries <code>parentToolCallId</code> and <code>agentName</code>, and the runtime skips those rows when it rebuilds model context. Replaying them would both contradict what the parent actually saw and reintroduce the context cost delegation exists to avoid.</p><p>A delegate&#39;s termination — success, cap, timeout, failure, abort — collapses into the tool result. It never reaches Electron main&#39;s turn handling, so the parent turn remains the only thing that can end a turn. See ADR 0119 for the timeout policy and <code>timed_out</code> outcome.</p><h3 id="_6-attribution-is-persisted-and-the-transcript-derives-its-topology-from-it" tabindex="-1">6. Attribution is persisted, and the transcript derives its topology from it <a class="header-anchor" href="#_6-attribution-is-persisted-and-the-transcript-derives-its-topology-from-it" aria-label="Permalink to &quot;6. Attribution is persisted, and the transcript derives its topology from it&quot;">​</a></h3><p>host-core stores <code>parentToolCallId</code> and <code>agentName</code> in the message <code>meta</code> object, so a reloaded session nests identically to a live one. The renderer groups every attributed row under the <code>Task</code> row that spawned it and renders them one level in; the turn stream and the minimap see only the parent&#39;s rows. The report is printed once: in the <code>Task</code> body when the delegate produced no answer row, as the nested answer row otherwise.</p><p>Every <code>Task</code> call in an activity group is drawn as a delegation summary, a lone one included (D265). Consecutive <code>Task</code> starts are the only rows in that group (D319): parent thinking, workspace tools, and lifecycle rows stay in ordinary processing groups around the card. Its header derives agent count, settled count, elapsed time and aggregate status from those same tool messages; its expanded body renders a one-level main-agent-to-delegate topology. Each delegate node retains the original row disclosure, brief, report, counters and nested steps. Live fan-out opens the topology once when it first appears, then leaves expansion under user control; reloaded history stays collapsed. No graph record, edge, IPC field or storage schema is added: live and reload both derive the same topology from the persisted parent ids, and the UI never invents delegate-to-delegate edges or a parent summary it does not own.</p><h3 id="_7-permission-requests-queue-per-session" tabindex="-1">7. Permission requests queue per session <a class="header-anchor" href="#_7-permission-requests-queue-per-session" aria-label="Permalink to &quot;7. Permission requests queue per session&quot;">​</a></h3><p>The renderer used to hold one pending permission per session. Parallel delegates break that: two delegates can each be waiting on a <code>Bash</code> call. Pending requests become a per-session queue, oldest first. Only the head is answerable, answers are matched by request id so a late answer cannot clear a successor, a host-expired request is removed by tool call id from anywhere in the queue, and an abort denies the whole queue. The card names the delegate that asked and how many requests wait behind it.</p><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Serial delegates in v1.</strong> Simpler, and it would have avoided the path lock and the permission queue. Rejected: the value of delegation is largely in fanning out a survey, and retrofitting concurrency would have meant redoing the transcript and permission work.</li><li><strong>Inherit the session model always.</strong> Cheaper to reason about. Rejected: a wide search is exactly the work worth doing on a cheap fast model while the parent keeps the expensive one.</li><li><strong>Let delegates inherit the parent&#39;s tools.</strong> Rejected for silent, always-on inherit: it makes every delegation as dangerous as the session, and a definition is the only place a reader can see what a delegate may do. Amended by ADR 0246: a document may opt in with <code>tools: inherit</code> and a deny list; builtins stay on today&#39;s whitelist.</li><li><strong>A separate process per delegate.</strong> Real isolation, but it duplicates the host connection, provider setup and event plumbing for a bounded worker that already runs under host-core containment. Rejected for v1.</li><li><strong>Flatten delegate rows into the transcript.</strong> Rejected: with parallel delegates the interleaving is unreadable, and it implies the parent saw work it never saw.</li><li><strong>Feed delegate messages back into the parent&#39;s context.</strong> Rejected: it is precisely the cost delegation removes.</li><li><strong>Definitions in settings/SQLite.</strong> Rejected: definitions are prompts and remain Markdown documents on disk. ADR 0112 places user-managed definitions under <code>~/.agents/subagents</code> so they follow the user across repositories without introducing a project capability directory.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Wide searches, reviews and command runs move out of the session&#39;s context at the price of one report each.</li><li>A user can add a delegate under <code>~/.agents/subagents</code>; no rebuild, no settings change, effective on the next prompt when enabled locally.</li><li>Two capability surfaces now exist for the same tools: the session&#39;s mode and a definition&#39;s <code>tools</code> list. A delegate can be strictly weaker than its session, never stronger.</li><li>The permission UX must handle more than one waiting request, which is visible to any user of parallel delegates.</li><li>Delegate rows make transcripts longer on disk; they stay collapsed and out of model context, but review and rollback still see them.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0062-bounded-subagents-behind-a-task-tool.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0062BoundedSubagentsBehindATaskTool = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0062BoundedSubagentsBehindATaskTool as default
};
