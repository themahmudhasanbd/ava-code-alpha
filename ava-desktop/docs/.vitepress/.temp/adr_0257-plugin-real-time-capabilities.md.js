import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0257: Host-mediated real-time capabilities for plugins","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0257-plugin-real-time-capabilities.md","filePath":"adr/0257-plugin-real-time-capabilities.md","lastUpdated":1789483301000}');
const _sfc_main = { name: "adr/0257-plugin-real-time-capabilities.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0257-host-mediated-real-time-capabilities-for-plugins" tabindex="-1">ADR 0257: Host-mediated real-time capabilities for plugins <a class="header-anchor" href="#adr-0257-host-mediated-real-time-capabilities-for-plugins" aria-label="Permalink to &quot;ADR 0257: Host-mediated real-time capabilities for plugins&quot;">​</a></h1><h2 id="status" tabindex="-1">Status <a class="header-anchor" href="#status" aria-label="Permalink to &quot;Status&quot;">​</a></h2><p>Accepted for implementation.</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>A plugin that wants to behave like an always-on voice assistant needs four capabilities the plugin runtime does not offer today. Each one would otherwise force a plugin out of the host&#39;s control:</p><ul><li><code>ui.microphone</code> grants a <em>visible</em> plugin surface an audio-only <code>media</code> permission inside its own Chromium session. It cannot run with no page open, and a page is not a lifecycle this app can keep alive in the background.</li><li><code>globalShortcut</code> is used in exactly two places, both owned by the app (<code>bootstrap/launcher.ts</code>), with a Windows low-level hook as the fallback for <code>Alt+Space</code> in host-core. A plugin has no path to a system-wide accelerator, and the plugin-scoped shortcut setting is deliberately window-local.</li><li><code>pi.net.fetch</code> is confined to <code>manifest.net.domains</code> but carries one request and one response; it cannot hold a long-lived frame stream.</li><li>The plugin host process is a Node <code>utilityProcess</code>; the plugin itself runs inside that process. &quot;Not exposed by the host API&quot; is a contract enforced by the broker, not a sandbox.</li></ul><p>The four capabilities are therefore added as host-owned services behind explicit permissions, following the existing broker, permission-gateway and audit model rather than widening what plugin code may touch.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><p>Four permissions are added additively: <code>audio.capture.background</code> (high), <code>audio.playback.background</code> (medium), <code>keyboard.globalShortcut</code> (medium), <code>net.websocket</code> (high). <code>ui.microphone</code> keeps its panel-scoped meaning and is not widened. Manifests, install copy, devkit checks, the renderer risk map and the validators (TypeScript and Rust) gain the same entries.</p></li><li><p>Devices and transports are host-owned. Plugins exchange typed frames — PCM16 in and out, text and binary messages — through host APIs. No <code>MediaStream</code>, device handle, Node stream, socket object, or native handle crosses the boundary, and no plugin code runs inside the audio path.</p></li><li><p>System-wide shortcuts stay inside the host&#39;s registration model. A plugin declares <code>contributes.globalShortcuts</code> (at most 8 entries, each mapping one accelerator to one of its own declared commands) and may adjust that set at runtime through <code>pi.keyboard.*</code>. Registration is refused, never taken over: <code>SHORTCUT_CONFLICT</code> for an OS-reserved binding, one the app itself spends, or one another plugin holds; <code>SHORTCUT_UNAVAILABLE</code> when the platform refuses; <code>INVALID_ACCELERATOR</code>; <code>LIMIT_EXCEEDED</code>. A plugin cannot monitor keys, read raw events, or install a hook — the only thing an accelerator can do is run a command that belongs to that plugin.</p></li><li><p><code>net.websocket</code> connects are confined by the same <code>manifest.net.domains</code> allowlist and the same <code>assertEgress</code> chokepoint as <code>pi.net.fetch</code>. The host owns the socket, so an undeclared domain, a loopback or private host not in the allowlist, and a redirect to an undeclared host are refused for exactly the same reasons they are refused today.</p></li><li><p>Fail closed, and release on every exit path. An undeclared or ungranted capability denies the call and is audited. Plugin disable, unload, crash, permission revocation, and app exit stop capture, drop queued playback, close sockets, and unregister accelerators; none of them may leave an orphan device, process, listener, or timer behind.</p></li><li><p>The host bounds every stream: one input stream per plugin, a bounded frame queue with backpressure and dropped-frame accounting, bounded send and receive buffers, a maximum frame size, and a maximum socket count.</p></li><li><p>Audit records the operations that change authority — open and close of an input, open, stop and close of an output, shortcut register and unregister, socket connect and close — with plugin id, operation, result, and timestamp. It never records audio bytes, message bodies, request headers, tokens, or keys.</p></li><li><p>Existing Control semantics are unchanged. A voice assistant drives the app through <code>pi.desktop.*</code> exactly as any other plugin does; dangerous operations keep their native confirmation even when a caller passes <code>confirm: true</code>, and <code>session/collaboration/*</code> remains reachable only from an authenticated plugin agent tool invocation.</p></li><li><p>Delivery is staged under this single decision. The first change implements the permission plumbing for all four capabilities and the full <code>keyboard.globalShortcut</code> runtime; <code>net.websocket</code> ships in the same line of work. The two audio permissions are declared and their <code>pi.audio</code> surface is callable — the permission gate still runs first — but the device service is not implemented, so every authorized call is answered with a coded <code>UNSUPPORTED</code> refusal, audited, rather than silently degrading; the two synchronous registration helpers throw the same code. That refusal is the part a later change replaces.</p></li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>The plugin surface grows by four permissions and three host API namespaces (<code>pi.audio</code>, <code>pi.keyboard</code>, <code>pi.net.websocket</code>). The permission matrix, install dialog copy in every shipped locale, devkit checks, and both validators must stay in sync from now on.</li><li>Background audio adds a second audio path to the app that is not the renderer&#39;s own playback. It must stay off the main thread and be released deterministically, which is why the device is host-owned in this decision.</li><li>A signed macOS build cannot capture audio until the microphone usage string and the audio-input entitlement are added to packaging; background playback works without them. Deployment stays incomplete until that is done.</li><li>Because the plugin process is a Node process, these permissions govern what the host <em>provides</em> and audits, not what the process could technically reach. The existing note that the plugin sandbox is a contract, not a kernel boundary, remains true.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><ul><li>Hand plugins a <code>MediaStream</code> or a Node stream. Rejected: it grants device and process authority the host cannot bound, audit, or release.</li><li>Keep an invisible panel alive to own the audio. Rejected: renderer throttling and teardown would drop frames silently, and the microphone would outlive any component the user can see.</li><li>Move voice into a dedicated sidecar process. Rejected: it duplicates the plugin lifecycle, permission model, and packaging surface for no gain.</li><li>Reach system-wide shortcuts through a low-level keyboard hook on every platform. Rejected: accelerator → own command is all a plugin needs, and a hook is precisely the keylogger-shaped surface the security model forbids.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0257-plugin-real-time-capabilities.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0257PluginRealTimeCapabilities = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0257PluginRealTimeCapabilities as default
};
