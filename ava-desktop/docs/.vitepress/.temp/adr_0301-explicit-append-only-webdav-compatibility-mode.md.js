import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0301: Explicit append-only WebDAV compatibility mode","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0301-explicit-append-only-webdav-compatibility-mode.md","filePath":"adr/0301-explicit-append-only-webdav-compatibility-mode.md","lastUpdated":1790050795000}');
const _sfc_main = { name: "adr/0301-explicit-append-only-webdav-compatibility-mode.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0301-explicit-append-only-webdav-compatibility-mode" tabindex="-1">ADR 0301: Explicit append-only WebDAV compatibility mode <a class="header-anchor" href="#adr-0301-explicit-append-only-webdav-compatibility-mode" aria-label="Permalink to &quot;ADR 0301: Explicit append-only WebDAV compatibility mode&quot;">​</a></h1><ul><li>Status: Accepted for implementation</li><li>Date: 2026-09-22</li><li>Amends: ADR 0300</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Some WebDAV servers accept <code>If-None-Match</code> and <code>If-Match</code> headers but ignore their preconditions. Treating a successful unconditional <code>PUT</code> as a compare- and-swap would allow two devices to overwrite a shared head without detecting the race. CloudDrive-style WebDAV endpoints are useful for local-network sync, but a server&#39;s WebDAV support does not by itself prove conditional-write semantics.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Keep strict conditional-write mode as the default. Add an explicitly selected append-only compatibility mode for servers that fail the conditional-write probe but prove that they can list a WebDAV collection with <code>PROPFIND</code>.</p><p>Compatibility mode uses the same encrypted header, immutable revision manifests, and authenticated resources as strict mode, but changes publication as follows:</p><ol><li>Each device receives a random stable device identifier.</li><li>Each device publishes only its own encrypted pointer at <code>vault/&lt;vault-id&gt;/heads/&lt;device-id&gt;</code>; the pointer is verified by a readback.</li><li>A sync lists all device pointers, includes a legacy strict head when present, walks their bounded parent graph, removes ancestor pointers, and merges all remaining tips against the last acknowledged base.</li><li>New revisions and resources use unique immutable identifiers. Existing resources are read and authenticated before reuse; compatibility mode does not overwrite an existing immutable object.</li><li>Compatibility mode retains immutable remote history and does not run remote cleanup, because a server without conditional writes cannot provide a safe cross-device acknowledgement protocol.</li></ol><p>The settings page labels the mode as compatibility mode, explains that it does not provide atomic compare-and-swap, and requires confirmation before saving. All devices sharing a vault must select the same mode. The mode is not a silent downgrade, and a server that cannot prove directory listing remains unsupported for bidirectional synchronization.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Concurrent writers publish separate tips instead of silently overwriting one shared head; compatible disjoint edits can still converge through the normal merge engine.</li><li>A device with a duplicated device identifier or a server that changes a device pointer during readback is reported as a conflict rather than treated as converged.</li><li>Compatibility mode is not equivalent to strict CAS. A server can still lose an in-flight unconditional write, deny or roll back availability, race vault header initialization, or return an incomplete directory listing. The UI discloses these limits and history is retained to make recovery possible.</li><li>New-device executable imports remain staged behind the existing digest-bound approval boundary; selecting compatibility mode does not grant activation.</li><li>Password rewrap updates the encrypted header unconditionally in this mode and verifies the resulting header. Rotation of a copied vault key still requires vault re-encryption and server-credential rotation.</li></ul><h2 id="alternatives-rejected" tabindex="-1">Alternatives rejected <a class="header-anchor" href="#alternatives-rejected" aria-label="Permalink to &quot;Alternatives rejected&quot;">​</a></h2><ol><li>Silently accepting ignored preconditions: unsafe last-writer-wins behavior and data loss under concurrent sync.</li><li>Keeping one shared mutable head with an unconditional <code>PUT</code>: the exact race this mode is intended to avoid.</li><li>Treating every WebDAV <code>GET</code> failure as an empty collection: hides outages and can cause accidental reinitialization. Only the endpoint-specific observed missing-object status remains compatible.</li></ol></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0301-explicit-append-only-webdav-compatibility-mode.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0301ExplicitAppendOnlyWebdavCompatibilityMode = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0301ExplicitAppendOnlyWebdavCompatibilityMode as default
};
