import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"04. Builtin Commands","description":"","frontmatter":{},"headers":[],"relativePath":"spec/04-ux/04-builtin-commands.md","filePath":"spec/04-ux/04-builtin-commands.md","lastUpdated":1789148233000}');
const _sfc_main = { name: "spec/04-ux/04-builtin-commands.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="_04-builtin-commands" tabindex="-1">04. Builtin Commands <a class="header-anchor" href="#_04-builtin-commands" aria-label="Permalink to &quot;04. Builtin Commands&quot;">​</a></h1><h2 id="_1-goal" tabindex="-1">1. Goal <a class="header-anchor" href="#_1-goal" aria-label="Permalink to &quot;1. Goal&quot;">​</a></h2><p>Define the five first-party command palette entries available without plugins. The same entries are available in the composer <code>/</code> menu.</p><p>Shortcut: <strong>Cmd/Ctrl + Shift + P</strong> (D014)</p><h2 id="_2-command-id-convention" tabindex="-1">2. Command ID convention <a class="header-anchor" href="#_2-command-id-convention" aria-label="Permalink to &quot;2. Command ID convention&quot;">​</a></h2><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>builtin.&lt;domain&gt;.&lt;action&gt;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><h2 id="_3-core-builtin-catalog" tabindex="-1">3. Core builtin catalog <a class="header-anchor" href="#_3-core-builtin-catalog" aria-label="Permalink to &quot;3. Core builtin catalog&quot;">​</a></h2><p>The builtin registry intentionally contains exactly these five commands. Plugin commands extend the searchable command list dynamically; app navigation, project management, settings, plugin management, and diagnostics are not builtin command contracts.</p><table tabindex="0"><thead><tr><th>id</th><th>title</th><th>keywords</th><th>category</th><th>risk</th><th>behavior</th></tr></thead><tbody><tr><td><code>builtin.session.new</code></td><td>New Task</td><td>new, chat, task</td><td>Session</td><td>low</td><td>reuse the latest empty session in the current group or create a durable empty session, then focus the composer</td></tr><tr><td><code>builtin.agent.compact</code></td><td>Compact Conversation Context</td><td>compact, context, tokens</td><td>Session</td><td>low</td><td>create a model-context checkpoint for the idle active session</td></tr><tr><td><code>builtin.mode.agent</code></td><td>Switch to Agent</td><td>mode, agent</td><td>Session</td><td>low</td><td>set the idle session mode to Agent</td></tr><tr><td><code>builtin.mode.plan</code></td><td>Switch to Plan</td><td>mode, plan, planning</td><td>Session</td><td>low</td><td>set the idle session mode to Plan</td></tr><tr><td><code>builtin.mode.goal</code></td><td>Switch to Goal</td><td>mode, goal, objective, autonomous</td><td>Session</td><td>low</td><td>set the idle session mode to Goal</td></tr></tbody></table><h2 id="_4-visibility-and-execution-rules" tabindex="-1">4. Visibility and execution rules <a class="header-anchor" href="#_4-visibility-and-execution-rules" aria-label="Permalink to &quot;4. Visibility and execution rules&quot;">​</a></h2><ul><li>The five IDs are the complete first-party registry. Removed IDs are not palette results and are not renderer dispatch cases; plugin commands remain independently discoverable.</li><li><code>New Task</code> uses the current project or temporary group. It selects the group&#39;s most recent empty session when present; otherwise it reveals the empty home on the first frame and creates the durable empty session. The action is idempotent within a group.</li><li><code>Compact Conversation Context</code> is available while the active session is idle; an active turn or checkpoint remains busy according to the existing compaction contract.</li><li>Mode commands use the same active-session configuration path as the Composer Agent/Plan/Goal chip. They update an idle session immediately. With no active session, they update the persisted default for the next session; a running session or pending approval is not changed.</li><li><code>SubmitPlan</code> and <code>SubmitGoal</code> are model tools, not palette commands. There is no Chat mode or request-changes alias.</li><li>The former app/project/settings/plugin/log commands remain available through their dedicated surfaces where applicable, but are not part of the command palette or the composer builtin namespace.</li></ul><h2 id="_5-execution-results" tabindex="-1">5. Execution results <a class="header-anchor" href="#_5-execution-results" aria-label="Permalink to &quot;5. Execution results&quot;">​</a></h2><p>Commands return:</p><div class="language-ts vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">type</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> CommandExecutionResult</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  |</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">ok</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> true</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">navigation</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">message</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">  |</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">ok</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> false</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">error</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> AppError</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> }</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><h2 id="_6-acceptance" tabindex="-1">6. Acceptance <a class="header-anchor" href="#_6-acceptance" aria-label="Permalink to &quot;6. Acceptance&quot;">​</a></h2><ol><li>The builtin registry contains exactly five unique, prefixed IDs.</li><li>Palette search matches each title and keyword set.</li><li>Mode switch commands update the idle session mode immediately; Plan, Goal, and Agent refer to the same pi Agent.</li><li>Compact works while idle and returns <code>AGENT_BUSY</code> during an active turn/checkpoint.</li><li>Removed IDs and the legacy <code>newChat</code>, <code>openProject</code>, and <code>openSettings</code> dispatch aliases do not appear in the registry or renderer switch.</li></ol><h2 id="_7-composer-slash-aliases-d123-adr-0024-adr-0106" tabindex="-1">7. Composer slash aliases (D123, ADR 0024, ADR 0106) <a class="header-anchor" href="#_7-composer-slash-aliases-d123-adr-0024-adr-0106" aria-label="Permalink to &quot;7. Composer slash aliases (D123, ADR 0024, ADR 0106)&quot;">​</a></h2><p>Builtin commands surface in the composer <code>/</code> menu through short aliases defined in the same registry that feeds palette search (<code>electron/main/builtin-commands.ts</code>); execution reuses the renderer switch.</p><table tabindex="0"><thead><tr><th>alias</th><th>palette id</th></tr></thead><tbody><tr><td><code>/new</code></td><td><code>builtin.session.new</code></td></tr><tr><td><code>/compact</code></td><td><code>builtin.agent.compact</code></td></tr><tr><td><code>/agent-mode</code></td><td><code>builtin.mode.agent</code></td></tr><tr><td><code>/plan-mode</code></td><td><code>builtin.mode.plan</code></td></tr><tr><td><code>/goal-mode</code></td><td><code>builtin.mode.goal</code></td></tr></tbody></table><p>Aliases share one namespace with template and plugin command names; builtin aliases win collisions, then project templates, then user templates, then plugin commands. Selecting an alias inserts <code>/alias </code>; sending <code>/new</code> or <code>/compact</code> alone executes locally without creating an empty prompt. The Agent/Plan/Goal aliases also support a prompt body: <code>/agent-mode &lt;prompt&gt;</code>, <code>/plan-mode &lt;prompt&gt;</code>, or <code>/goal-mode &lt;prompt&gt;</code> switches the idle session (or the next-session default) and sends <code>&lt;prompt&gt;</code> through the normal prompt path. A mode alias sent with no body remains a local mode switch. The prompt body remains the visible user turn; a failed dispatch does not clear the composer draft. Former builtin aliases are no longer resolved and are handled as ordinary unknown slash text unless supplied by another command source.</p><h2 id="_8-composer-skill-entries" tabindex="-1">8. Composer skill entries <a class="header-anchor" href="#_8-composer-skill-entries" aria-label="Permalink to &quot;8. Composer skill entries&quot;">​</a></h2><p>Active built-in, plugin, and user-owned Skills also surface in the composer <code>/</code> menu. They use the exact Skill id as the slash name, show the Skill&#39;s display name and description, and form a separate <strong>Skills</strong> group after extension commands. This group is always last; a Skill never shadows a command or template with the same name.</p><p>Selecting a Skill inserts <code>/&lt;skill-id&gt; </code>. Sending <code>/&lt;skill-id&gt;</code> with optional prompt text keeps that typed form as the visible transcript chip and asks the model to call the existing <code>Skill</code> tool with the validated id before answering. Only Skills active for the current project are listed or accepted, so project scope and plugin activation remain enforced at send time. If the Skill is no longer active, the text follows the normal unknown-slash prompt path.</p><h2 id="_9-ideographic-comma-opens-the-slash-menu-d405" tabindex="-1">9. Ideographic comma opens the slash menu (D405) <a class="header-anchor" href="#_9-ideographic-comma-opens-the-slash-menu-d405" aria-label="Permalink to &quot;9. Ideographic comma opens the slash menu (D405)&quot;">​</a></h2><p>A Chinese IME produces <code>、</code> (U+3001) where the ASCII <code>/</code> is meant, so reaching the menu otherwise means switching input methods mid-sentence. When the composer is empty, a committed <code>、</code> as its first character is rewritten to <code>/</code> before trigger detection runs, and the ordinary slash menu opens with the same insertion, filtering, and send behavior described above.</p><p>Only the first character of an empty draft is rewritten. A <code>、</code> anywhere else in the draft is ordinary punctuation and is never touched, and the alias has no effect on the <code>@</code> file menu.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("spec/04-ux/04-builtin-commands.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _04BuiltinCommands = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _04BuiltinCommands as default
};
