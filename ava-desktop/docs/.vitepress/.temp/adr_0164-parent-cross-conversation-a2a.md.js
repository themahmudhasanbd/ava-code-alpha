import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0164: Parent agents collaborate across conversations","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0164-parent-cross-conversation-a2a.md","filePath":"adr/0164-parent-cross-conversation-a2a.md","lastUpdated":1788625977000}');
const _sfc_main = { name: "adr/0164-parent-cross-conversation-a2a.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0164-parent-agents-collaborate-across-conversations" tabindex="-1">ADR 0164: Parent agents collaborate across conversations <a class="header-anchor" href="#adr-0164-parent-agents-collaborate-across-conversations" aria-label="Permalink to &quot;ADR 0164: Parent agents collaborate across conversations&quot;">​</a></h1><ul><li>Status: Superseded by ADR 0165</li><li>Date: 2026-09-05</li><li>Deciders: PI-Desktop core</li><li>Related: D321, D318, ADR 0147, ADR 0162, ADR 0062, <code>03-runtime/02-agent-runtime.md</code> §5f.2, <code>03-runtime/06-host-rpc-protocol.md</code> §4, E2E-165d</li><li>Amends: ADR 0147 (parent must not call <code>A2A</code>) and ADR 0162 (no messaging with the parent). Subagent sibling coordination is unchanged.</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>ADR 0147 kept the <code>A2A</code> tool out of the parent catalog so the parent could not open a second, weaker channel to its own delegates. ADR 0162 then let subagents address peers in other sessions, but still not the parent.</p><p>That split does not match how users collaborate: they want two conversations in the sidebar to coordinate, not two nested workers. The parent is the session&#39;s only agent that already has the user&#39;s context. Asking the user to spawn a <code>discussant</code> in each chat just to pass a note is the wrong seam.</p><p>The broker is already process-global. Each session runtime already lives in the sidecar map after the first prompt. The missing piece is that the parent never registers as an A2A client.</p><p>The remaining hazard is the original one: if the parent can address its own subagents, <code>Task</code>&#39;s brief and report boundary collapses.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><strong>Each Agent-mode session runtime registers a parent A2A agent</strong> for its lifetime (first prompt through dispose). The card uses <code>kind: &quot;parent&quot;</code>, name <code>parent</code> (host-uniquified to <code>parent-2</code>, …), and a description that carries the session title. Plan/Goal runtimes do not register.</li><li><strong><code>A2A</code> is a core Agent-mode parent tool</strong>, present in the catalog from runtime construction — not after broker register. The first call (or the first prompt) mints the token. It stays out of Plan/Goal.</li><li><strong>Kinds do not mix.</strong> Agent cards carry <code>kind: &quot;parent&quot; | &quot;subagent&quot;</code> (default <code>subagent</code>). <code>a2a.agents.list</code> and recipient resolution only consider the caller&#39;s kind. A parent cannot send to a subagent; a subagent cannot send to a parent. Same-session parent-to-delegate backchannel is therefore impossible.</li><li><strong>Inbound notes join the next parent turn.</strong> Events addressed to the parent queue on its peer id. A parent that called <code>A2A(wait)</code> wakes as today. If the parent is idle, the next <code>prompt()</code> prepends a short A2A inbox to the user text so the model sees the other conversation&#39;s note without a separate auto-started turn.</li><li><strong>Reachability is runtime-lifetime.</strong> A conversation is discoverable after it has an Agent runtime this process (after the first agent prompt) and until that runtime is disposed. Sidebar rows with no live runtime are not in the registry.</li></ol><p>Unchanged: capability tokens, counterpart event routing, <code>recipientContextId</code> delivery, parent still uses <code>Task*</code> for its own delegates, no remote A2A, no nested delegation.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Two Agent conversations on the same host can <code>discover</code> / <code>send</code> / <code>wait</code> / <code>complete</code> with each other.</li><li>A parent that omits <code>to</code> addresses the unique other live parent, or <code>A2A_NO_PEERS</code> when several exist — same rule ADR 0162 used for remote subagents.</li><li>Subagent A2A no longer lists parent cards, so a roundtable cannot page the user through A2A.</li><li>An idle conversation does not start a provider turn on inbound A2A; the note waits for the user&#39;s next message in that session.</li><li>Not addressed: auto-waking an idle conversation, addressing a session that has never run this process, and parent-to-foreign-subagent.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Keep parent A2A-less; users spawn discussants:</strong> rejected. That is not cross-conversation collaboration; it is nested worker coordination the user did not ask for.</li><li><strong>Let parents address every live agent:</strong> rejected. It reopens the parent→own-delegate backchannel ADR 0147 closed.</li><li><strong>Auto-start a turn in the idle session:</strong> rejected for this change. It spends a provider request the user did not send and needs a durable host turn the sidecar cannot mint. Inbox-on-next-prompt is enough to collaborate once both conversations are in use.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0164-parent-cross-conversation-a2a.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0164ParentCrossConversationA2a = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0164ParentCrossConversationA2a as default
};
