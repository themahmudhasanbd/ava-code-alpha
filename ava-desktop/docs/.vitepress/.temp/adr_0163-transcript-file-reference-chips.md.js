import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0163: Transcript File References Render as Previewable Chips","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0163-transcript-file-reference-chips.md","filePath":"adr/0163-transcript-file-reference-chips.md","lastUpdated":1788611927000}');
const _sfc_main = { name: "adr/0163-transcript-file-reference-chips.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0163-transcript-file-references-render-as-previewable-chips" tabindex="-1">ADR 0163: Transcript File References Render as Previewable Chips <a class="header-anchor" href="#adr-0163-transcript-file-references-render-as-previewable-chips" aria-label="Permalink to &quot;ADR 0163: Transcript File References Render as Previewable Chips&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-05</li><li>Deciders: PI-Desktop core</li><li>Related: D124, D209, D320, ADR 0024, ADR 0070, ADR 0019</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Composer file references are compact leaf-name chips in the draft (ADR 0070). On send they serialize to canonical <code>@path</code> / <code>@&quot;path with spaces&quot;</code> text so the agent can Read them. The transcript then painted those tokens as full paths, which crowded the user bubble and hid the node the user had just placed in the input.</p><p>Clicking a path opened the work-panel files viewer, which cannot render HTML as a page and cannot open PDF, office, or other OS-handled types. Scratch absolute paths also failed the workspace-relative preview gate, so pasted files were not clickable at all.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>The transcript parses serialized <code>@path</code> tokens (quoted and unquoted, relative and absolute) and renders each as a compact chip matching the composer node: file-family icon, ellipsized leaf name, canonical path in the tooltip and accessible name. HTTP(S) URLs stay inline text links.</li><li>Clicking a workspace <code>.html</code> / <code>.htm</code> chip opens the work-panel browser (existing local-file preview). Clicking any other allowed file opens it with the OS default application for that suffix via a new read-only Electron channel <code>pi-desktop/fs/open</code>.</li><li><code>fs/open</code> resolves relative paths inside the current workspace and absolute paths only when they already live under the workspace, <code>&lt;data_dir&gt;/scratch/</code>, or <code>&lt;data_dir&gt;/attachments/</code>. Traversal, <code>~</code>, and paths outside those roots are rejected. No host-protocol or storage schema change.</li><li>Structured message attachments that are not already represented by an inline <code>@path</code> chip use the same chip and click contract. Tool-row summaries keep the existing work-panel files/URL preview.</li></ol><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Keep full-path text links:</strong> rejected because it undoes ADR 0070&#39;s compact display the moment the user sends.</li><li><strong>Always open in the files tab:</strong> rejected because HTML should preview as a page and many suffixes need the OS handler.</li><li><strong>Always <code>shell.openPath</code>:</strong> rejected for workspace HTML, which already has a live-reloading in-app browser.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Sent user turns show the same file nodes as the composer.</li><li>Users can preview HTML in-app and open other files with the default app.</li><li>Main gains one allowlisted open channel with the same containment roots as scratch/workspace file tools.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0163-transcript-file-reference-chips.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0163TranscriptFileReferenceChips = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0163TranscriptFileReferenceChips as default
};
