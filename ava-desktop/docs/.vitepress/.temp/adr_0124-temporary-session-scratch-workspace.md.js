import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0124: Bind Temporary Sessions to Their Own Scratch Workspace","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0124-temporary-session-scratch-workspace.md","filePath":"adr/0124-temporary-session-scratch-workspace.md","lastUpdated":1787711625000}');
const _sfc_main = { name: "adr/0124-temporary-session-scratch-workspace.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0124-bind-temporary-sessions-to-their-own-scratch-workspace" tabindex="-1">ADR 0124: Bind Temporary Sessions to Their Own Scratch Workspace <a class="header-anchor" href="#adr-0124-bind-temporary-sessions-to-their-own-scratch-workspace" aria-label="Permalink to &quot;ADR 0124: Bind Temporary Sessions to Their Own Scratch Workspace&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-08-26</li><li>Related: Issue #16, ADR 0059, ADR 0118</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Temporary sessions intentionally keep <code>projectPath</code> empty so they do not inherit or mutate the currently visible project. That left their native tools without a root, however, even though each session already owns a deterministic <code>&lt;data_dir&gt;/scratch/&lt;sessionId&gt;</code> directory. The empty home also used the same hero copy for a temporary chat and for having no active session.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>Host-core resolves a persisted path-less session&#39;s tool workspace to its own <code>&lt;data_dir&gt;/scratch/&lt;sessionId&gt;</code> directory and creates that directory when the binding is first resolved. The session schema remains unchanged and <code>projectPath</code> stays absent.</li><li>Read, Glob, Grep, Write, Edit, and Bash use that scratch directory as the temporary session&#39;s workspace root. The root is never taken from the mutable global workspace, and the existing containment and permission checks still apply. A missing-session compatibility call may retain the legacy global-workspace fallback.</li><li>Plan and Goal workspace validation continues to require a persisted project binding, so a scratch workspace does not broaden contract-mode execution.</li><li>The renderer derives the empty-home hero state from the selected session: project sessions retain the project-underlined welcome, temporary sessions show dedicated temporary-chat copy without a project action, and no active session shows the generic welcome.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Temporary chats can safely use native tools without touching a project or inheriting the most recently active project.</li><li>Relative tool paths in temporary chats are useful immediately; scratch data remains ephemeral and follows the existing session deletion/startup sweep.</li><li>Temporary sessions remain visibly and structurally distinct from project sessions without introducing a new session type or persistence migration.</li><li>Plan and Goal keep their existing project-root boundary.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Inherit the visible project:</strong> rejected because it violates the temporary session isolation invariant and can mutate unrelated project files.</li><li><strong>Add a new persisted session type:</strong> rejected because the existing path-less session model already provides the correct lifecycle and grouping.</li><li><strong>Leave tools unavailable:</strong> rejected because it makes temporary chats unable to perform otherwise isolated inspection and scratch work.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0124-temporary-session-scratch-workspace.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0124TemporarySessionScratchWorkspace = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0124TemporarySessionScratchWorkspace as default
};
