import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0259: Plugin-declared providers are Host-owned rows","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0259-plugin-declared-providers.md","filePath":"adr/0259-plugin-declared-providers.md","lastUpdated":1789474785000}');
const _sfc_main = { name: "adr/0259-plugin-declared-providers.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0259-plugin-declared-providers-are-host-owned-rows" tabindex="-1">ADR 0259: Plugin-declared providers are Host-owned rows <a class="header-anchor" href="#adr-0259-plugin-declared-providers-are-host-owned-rows" aria-label="Permalink to &quot;ADR 0259: Plugin-declared providers are Host-owned rows&quot;">​</a></h1><ul><li>Status: Accepted for implementation</li><li>Date: 2026-09-15</li><li>Decision: D427</li><li>Related: ADR 0011, <code>07-plugins/02-plugin-manifest-schema.md</code>, <code>07-plugins/13-plugin-permissions-matrix.md</code>, <code>03-runtime/04-data-storage.md</code>, <code>03-runtime/12-provider-config-schema.md</code></li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>A plugin that integrates an LLM service has two surfaces today: an agent tool, or a session-scoped agent implementation registered through the trusted-extension API (ADR 0258). Neither puts the service in the native provider list, so the user cannot select it for a session and the plugin ends up keeping a private provider registry beside the Host&#39;s one.</p><p>The Host is the single owner of provider rows, provider credentials, the model catalog cache, and the Settings provider list. Duplicating that state in a plugin-side registry would give the same service two identities and two lifecycles, and would hide a service the user installed from the settings surface that manages every other endpoint.</p><p>Ownership is the part that needs a decision: a provider row is no longer only &quot;the user&#39;s&quot;, and the Host must be able to tell whose row it is.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>Add <code>contributes.providers[]</code> to the plugin manifest. The entry shape is fixed by <code>07-plugins/02-plugin-manifest-schema.md</code> §5.4: <code>id</code>, required <code>name</code>, optional <code>vendorKey</code>, <code>baseUrl</code>, <code>apiStyle</code>, <code>authKind</code>, and 1..64 <code>models</code>.</li><li>The Host materializes each declaration as a real row in <code>providers</code>, not in a plugin-side registry. The row id is <code>plugin:&lt;pluginId&gt;:&lt;declaredId&gt;</code>, it is owned by that plugin, and it appears in the native Settings → Provider list, so it is selectable for a session like any user-created row.</li><li>Schema v17 adds the nullable <code>providers.owner_plugin_id</code> column and its partial index. The migration is additive: every pre-v17 row is a user row and keeps a NULL owner. The public provider payload carries <code>ownerPluginId</code>.</li><li>The declaration is re-read from the manifest on every plugin load and is authoritative for the fields it declares (name, vendorKey, baseUrl, authKind, apiStyle, models). Values the declaration does not own — stored headers and the OAuth account label — are preserved across a refresh.</li><li>Credentials stay in the Host secret store under the same refs as any provider: <code>secret:provider:&lt;id&gt;:api_key</code> and <code>secret:provider:&lt;id&gt;:oauth</code>. The plugin neither holds nor exports the credential, and the row is an ordinary runtime row for model resolution, discovery, and connection tests.</li><li>The user path refuses a plugin-owned row. <code>providers.update</code> and <code>providers.delete</code> fail with a <code>PROVIDER_OWNED_BY_PLUGIN</code> error, because the manifest declaration would overwrite a user edit on the next load and only the plugin&#39;s own lifecycle can decide the row is gone.</li><li>Disabling a plugin keeps its rows and sets <code>enabled = 0</code>, so re-enabling restores the credential the user already stored. Uninstalling the plugin, or dropping an entry from the declaration, deletes the row together with both credential references, so a later re-declaration can never inherit a stale token.</li><li>A non-empty declaration requires the new high-risk <code>provider.register</code> permission. A manifest that declares providers without it fails validation, and the permission matrix and the manifest schema document the grant.</li><li>Reconciliation runs on <code>plugins.enable</code>, <code>plugins.disable</code>, <code>plugins.loadDev</code>, <code>plugins.uninstall</code>, and once at host startup for every registered plugin. A provider sync failure is logged as a warning; it never changes plugin enablement.</li><li>OAuth is staged, not shipped: an <code>oauth</code> block or <code>authKind: &quot;oauth&quot;</code> is refused at manifest validation (<code>plugin OAuth providers are not supported in this release</code> / <code>unsupported authKind oauth</code>), because the Host has no plugin OAuth login flow. The <code>provider.oauth</code> permission and a Host-owned login flow are future work and are not available today.</li><li>The plugin SDK mirrors the same authoring-time rules, requires the same permission, and derives the <code>providers</code> capability token for the plugin row.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A plugin-provided service is a first-class provider: it lists, discovers models, and resolves through the same Host path as a user row, with no new wire adapter.</li><li>Provider rows now have an owner, so every Host path that mutates a row must respect <code>owner_plugin_id</code>; the user path can no longer assume it owns the row it is editing or deleting.</li><li>An unreviewed manifest can add rows to the user&#39;s provider list. The high-risk permission, the install confirmation, and the local-import and development-plugin limit for v1.1 bound that exposure.</li><li>A credential is only as durable as the declaration: dropping an entry from the manifest, or uninstalling the plugin, deletes the stored key or token.</li><li>The plugin-side surface is deliberately narrow (no OAuth, no plugin-owned headers, no user editing), so a row cannot become a writable side channel around the provider config schema.</li><li>The v15→v16 session-collaboration step now stamps its own version <code>16</code> instead of the latest schema constant, so a v15 file can walk v15→v16→v17 in one launch.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><h3 id="a-sidecar-only-provider-registry" tabindex="-1">A sidecar-only provider registry <a class="header-anchor" href="#a-sidecar-only-provider-registry" aria-label="Permalink to &quot;A sidecar-only provider registry&quot;">​</a></h3><p>Rejected: the same service would exist twice, with a private identity the UI, the model catalog cache, the session binding, and the Host secret store cannot address. Only the plugin&#39;s own panel could select it, and every Host feature that iterates <code>providers</code> — default model, subagent catalog, connection test, model discovery — would need a second code path.</p><h3 id="let-the-plugin-write-provider-rows-through-providers-create" tabindex="-1">Let the plugin write provider rows through <code>providers.create</code> <a class="header-anchor" href="#let-the-plugin-write-provider-rows-through-providers-create" aria-label="Permalink to &quot;Let the plugin write provider rows through \`providers.create\`&quot;">​</a></h3><p>Rejected: the plugin host is not a second provider author. It would have to hold provider-write authority, the user&#39;s own edit and the declaration would fight over the same row on every load, and the row would not be tied to the plugin&#39;s lifecycle, so an uninstall would leave an orphan. A manifest declaration plus Host reconciliation keeps ownership explicit and the plugin write path one-way.</p><h3 id="store-plugin-providers-outside-the-database" tabindex="-1">Store plugin providers outside the database <a class="header-anchor" href="#store-plugin-providers-outside-the-database" aria-label="Permalink to &quot;Store plugin providers outside the database&quot;">​</a></h3><p>Rejected: a JSON file in the plugin data directory cannot participate in the relational state a provider row anchors. The model catalog cache, session provider/model bindings, credential metadata, and audit rows all reference <code>providers.id</code>; a file would also move provider identity out of the single-writer store (ADR 0011) and make the Settings list depend on whether a plugin&#39;s file was readable.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0259-plugin-declared-providers.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0259PluginDeclaredProviders = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0259PluginDeclaredProviders as default
};
