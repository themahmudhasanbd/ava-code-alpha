import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0043: Message-owned review snapshots and guarded rollback","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0043-message-owned-review-snapshots-and-rollback.md","filePath":"adr/0043-message-owned-review-snapshots-and-rollback.md","lastUpdated":1786725729000}');
const _sfc_main = { name: "adr/0043-message-owned-review-snapshots-and-rollback.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0043-message-owned-review-snapshots-and-guarded-rollback" tabindex="-1">ADR 0043: Message-owned review snapshots and guarded rollback <a class="header-anchor" href="#adr-0043-message-owned-review-snapshots-and-guarded-rollback" aria-label="Permalink to &quot;ADR 0043: Message-owned review snapshots and guarded rollback&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-08-02</li><li>Related: <a href="./../spec/03-runtime/03-tools-and-permissions">03-tools-and-permissions</a> · <a href="./../spec/03-runtime/04-data-storage">04-data-storage</a> · <a href="./../spec/03-runtime/06-host-rpc-protocol">06-host-rpc-protocol</a> · <a href="./../spec/04-ux/08-component-spec">08-component-spec</a> · <a href="./../spec/06-delivery/04-e2e-test-plan">E2E-057</a></li><li>Supersedes: ADR 0042&#39;s Git-dependent review source, D098, and D179</li><li>Amended by: <a href="./0087-line-anchored-edit-contract">ADR 0087</a> (<code>Edit</code> may now record a move as two entries, and a rollback invalidates session edit snapshots for the path)</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The previous Review implementation reconstructed each card from the current Git working tree. A commit therefore removed the evidence for an earlier tool message, and a later edit to the same path could make an old message appear to describe work it never performed. The same model had no safe way to restore a specific message&#39;s previous file content.</p><p>Review is a conversation feature, so its evidence must belong to the message that produced it rather than to a mutable repository snapshot.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>Before a workspace <code>Write</code> or <code>Edit</code>, host-core captures the previous file bytes in a session-scoped snapshot outside the workspace. After a successful tool, it writes a bounded <code>details.review</code> record into the tool result. The record contains the snapshot id, message/tool id, relative path, operation, added/modified/deleted status, addition/deletion counts, expandable hunks, active/rolled-back state, and whether rollback is reversible.</li><li>The renderer derives <code>InlineReviewCard</code> and the Review panel&#39;s chronological history from persisted tool messages only. It never refreshes or matches a current Git diff. A card stays immediately after its owning tool row and remains visible after commit, workspace switching, and restart.</li><li><code>review.rollback({sessionId, snapshotId})</code> is a host-owned operation. Before writing, host-core verifies that the current file hash equals the post-tool hash recorded in the snapshot. A mismatch returns <code>conflict</code> and leaves the file untouched. A match restores the previous bytes or removes a file that the message created, then persists <code>state: &quot;rolledBack&quot;</code> on the tool row.</li><li>Snapshot storage is bounded and session-owned: <code>&lt;data_dir&gt;/review-changes/&lt;sessionId&gt;/&lt;snapshotId&gt;/{before,meta.json}</code>. Session deletion removes it; startup removes directories for missing sessions. Forked transcripts retain visible diff evidence but mark rollback unavailable because the source snapshot belongs to another session.</li><li>Scratch-root writes, failed tools, and tools without a structured workspace path do not create review records. Large or binary files may still show status/count metadata while omitting hunks and rollback when the bounded previous content cannot be retained.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Committing after a tool no longer makes its review number or card disappear.</li><li>Every visible diff is tied to the exact message that produced it, including additions, deletions, and modifications.</li><li>Rollback is explicit, idempotent, and refuses to overwrite later work.</li><li>Review no longer promises to describe unrelated shell mutations; structured Write/Edit results are the durable review boundary.</li><li>Snapshot bytes add host-owned local storage, bounded by file and diff caps.</li></ul><h2 id="alternatives-rejected" tabindex="-1">Alternatives rejected <a class="header-anchor" href="#alternatives-rejected" aria-label="Permalink to &quot;Alternatives rejected&quot;">​</a></h2><ul><li><strong>Keep reading Git HEAD:</strong> commits erase the evidence and cannot identify the message responsible for a later same-path edit.</li><li><strong>Store only a renderer patch:</strong> the renderer cannot safely restore files, and the patch would be lost on restart or session reload.</li><li><strong>Rollback unconditionally:</strong> it could overwrite a user&#39;s later edit or an external change, so the post-tool hash guard is mandatory.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0043-message-owned-review-snapshots-and-rollback.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0043MessageOwnedReviewSnapshotsAndRollback = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0043MessageOwnedReviewSnapshotsAndRollback as default
};
