import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0210: Subagent output-token cap","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0210-subagent-output-token-cap.md","filePath":"adr/0210-subagent-output-token-cap.md","lastUpdated":1789026948000}');
const _sfc_main = { name: "adr/0210-subagent-output-token-cap.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0210-subagent-output-token-cap" tabindex="-1">ADR 0210: Subagent output-token cap <a class="header-anchor" href="#adr-0210-subagent-output-token-cap" aria-label="Permalink to &quot;ADR 0210: Subagent output-token cap&quot;">​</a></h1><ul><li>Status: Accepted (issue #171, merged in #193)</li><li>Date: 2026-09-10</li><li>Deciders: PI-Desktop runtime and UX maintainers</li><li>Related: D383, E2E-155, E2E-119, ADR 0062, ADR 0063, ADR 0194</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>A subagent definition could bound a delegate&#39;s turns and its reasoning level, but not its response length. The only output limit in play was the one the model binding carries: <code>ModelBinding.maxTokens</code> is edited under <strong>Max output</strong> in a model&#39;s Advanced settings, and <code>modelConfigWithBinding</code> folds it into the model the session builds. That value belongs to the model, not to a caller, so it applies to every request that model serves.</p><p>Delegation is where that is the wrong granularity. A delegate is a bounded worker by design (ADR 0062): <code>explorer</code> sweeps many files and reports a conclusion, while <code>fixer</code> edits one path. Capping the model to size the cheap one would also cap the session and every other delegate, and a definition cannot express &quot;this worker answers briefly&quot; at all. Users therefore had no way to bound a delegate&#39;s own output, and the cost the issue names — a search delegate spending far more output than its report needs — was not addressable from Settings.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><strong>D383 — a definition declares its own output cap.</strong> Subagent frontmatter and the settings API accept <code>maxTokens</code>, an optional non-negative integer. Omitted, <code>none</code>, or <code>0</code> means &quot;follow the model&#39;s published limit&quot;, which is the behavior every existing document already had. The value travels the same path as <code>maxTurns</code>: frontmatter, <code>SubagentDefinition</code>, <code>UserSubagentRecord</code>, the settings input, and the editor draft.</li><li><strong>The ceiling is 200000 and the accepted range starts at 1.</strong> The clamp is defensive, not policy: no published model accepts an output limit above 128k, so a larger number is a typo, and a limit of <code>0</code> would mean &quot;no output&quot; — a state the empty field already expresses. A value above the ceiling is clamped to it; a negative or fractional value is not a cap at all and is ignored, leaving the delegate on the model&#39;s limit. The parser reports both as warnings rather than silently changing what the document asked for.</li><li><strong>The cap overrides the model built for that delegate, and only that delegate.</strong> <code>SubagentRun</code> already builds its own model and its own single-model provider registry, so the cap is applied as <code>{ ...builtModel, maxTokens }</code> on that object. The adapters derive <code>max_tokens</code> / <code>max_completion_tokens</code> / <code>max_output_tokens</code> from this field, so no adapter-specific branch is introduced, and the session&#39;s requests keep the binding untouched.</li><li><strong>The editor places it beside the turn limit, in the Advanced area that already exists.</strong> Both are caps that belong to the definition rather than to the model; the pre-existing Advanced disclosure already carries model, thinking, turn limit and scope, so no new surface is created. The empty field reads as &quot;follow the model&quot;, not &quot;no limit&quot;, so its placeholder is the model default.</li><li><strong>Additive only.</strong> No SQLite migration, protocol version bump, IPC method, or tool-result shape changes. A document without <code>maxTokens</code> parses, stores and runs exactly as before.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A delegate&#39;s output can be bounded without changing the model binding, so sizing one worker no longer resizes the session or its siblings.</li><li>A cap may be stricter than the model&#39;s own limit but never looser in effect: the provider still enforces its own maximum, and the ceiling keeps a typo from reaching it.</li><li>Definitions gain a field that only matters when a delegate writes long answers; an existing document keeps following the model.</li><li>The built-in definitions do not declare a cap, so their behavior is unchanged. Presets could carry one per builtin role later; that is a follow-up rather than part of this decision, because it would change what a preset overwrites in the editor.</li></ul><h2 id="alternatives-rejected" tabindex="-1">Alternatives rejected <a class="header-anchor" href="#alternatives-rejected" aria-label="Permalink to &quot;Alternatives rejected&quot;">​</a></h2><ul><li><strong>Reuse <code>ModelBinding.maxTokens</code>.</strong> It is a per-model value, so it would cap the session and every delegate pinned to that model. It also cannot express two delegates on one model with different budgets.</li><li><strong>Make the cap a turn-level or session-level setting.</strong> Both are coarser than the problem: the issue is one delegate&#39;s report length, and a session-wide cap would silently truncate the parent&#39;s answers.</li><li><strong>Reject a value above the ceiling instead of clamping it.</strong> A definition document is hand-editable, and refusing to load it over one typo would take the delegate out of the catalog. Clamping with a warning keeps the delegate usable and says what happened.</li><li><strong>Default the field to a concrete number.</strong> A default would change existing delegates&#39; requests on upgrade, which is exactly what an opt-in cap must not do.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0210-subagent-output-token-cap.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0210SubagentOutputTokenCap = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0210SubagentOutputTokenCap as default
};
