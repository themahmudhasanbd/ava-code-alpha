import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0202: Expose Effective Subagent Thinking Metadata","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0202-effective-subagent-thinking-metadata.md","filePath":"adr/0202-effective-subagent-thinking-metadata.md","lastUpdated":1788966506000}');
const _sfc_main = { name: "adr/0202-effective-subagent-thinking-metadata.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0202-expose-effective-subagent-thinking-metadata" tabindex="-1">ADR 0202: Expose Effective Subagent Thinking Metadata <a class="header-anchor" href="#adr-0202-expose-effective-subagent-thinking-metadata" aria-label="Permalink to &quot;ADR 0202: Expose Effective Subagent Thinking Metadata&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-09</li><li>Decision: D369</li><li>Related: ADR 0062, ADR 0089, ADR 0144, ADR 0194</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Delegation cards already receive the provider/model selected for each <code>Task</code> run, but they do not receive the thinking level that the runtime actually passed to that delegate. The selected level can differ from the definition or parent setting after inheritance and target-model capability clamping. Making the renderer infer it would make live, restored, and parallel delegations inconsistent.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ul><li>Resolve the delegate&#39;s effective thinking selection once, immediately before constructing <code>SubagentRun</code>, using the same provider binding and clamping path that controls the request.</li><li>Include <code>modelId</code> and <code>thinkingLevel</code> in the immediate <code>Task</code> result, the <code>SubagentRunResult</code>, and lifecycle snapshots (<code>TaskWait</code>, <code>TaskList</code>, and <code>TaskStop</code>). <code>omit</code> remains an explicit no-provider-override value.</li><li>Render the result&#39;s model and a localized thinking label together on each delegation node and in the side-dock identity header. Omit the label for <code>off</code>, <code>omit</code>, or any unrecognized value; never derive a replacement from the parent session or subagent definition.</li></ul><p>This is additive to the existing delegation result details. It changes no host protocol, storage schema, provider request, or delegation lifecycle behavior.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><p>Live and restored cards have one runtime-owned source of truth, and parallel delegates can show different effective levels without cross-talk. The UI can keep narrow layouts bounded while exposing the complete combined label through the accessible name and hover title. A provider default selected by <code>omit</code> is intentionally not represented as a concrete visible level because the runtime does not know that adapter-owned default as a canonical thinking value.</p><h2 id="verification" tabindex="-1">Verification <a class="header-anchor" href="#verification" aria-label="Permalink to &quot;Verification&quot;">​</a></h2><p>Runtime tests cover result metadata and capability clamping; renderer source contracts cover both the topology node and side-dock header, including the <code>off</code>/<code>omit</code> suppression rule. E2E-219 records the live, narrow-layout, and history-restoration journey.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0202-effective-subagent-thinking-metadata.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0202EffectiveSubagentThinkingMetadata = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0202EffectiveSubagentThinkingMetadata as default
};
