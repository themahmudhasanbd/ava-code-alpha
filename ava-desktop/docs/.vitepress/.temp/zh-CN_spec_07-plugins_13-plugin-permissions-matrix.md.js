import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"13. 插件权限矩阵","description":"","frontmatter":{},"headers":[],"relativePath":"zh-CN/spec/07-plugins/13-plugin-permissions-matrix.md","filePath":"zh-CN/spec/07-plugins/13-plugin-permissions-matrix.md","lastUpdated":1789739317000}');
const _sfc_main = { name: "zh-CN/spec/07-plugins/13-plugin-permissions-matrix.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="_13-插件权限矩阵" tabindex="-1">13. 插件权限矩阵 <a class="header-anchor" href="#_13-插件权限矩阵" aria-label="Permalink to &quot;13. 插件权限矩阵&quot;">​</a></h1><blockquote><p><strong>翻译说明：</strong> 本页是与 <a href="/spec/07-plugins/13-plugin-permissions-matrix">英文源规格</a> 一一对应的机器辅助翻译。代码、协议字段和标识符保持原文；如翻译与英文源事实有歧义，以英文版本为准。</p></blockquote><h2 id="_1-目标" tabindex="-1">1. 目标 <a class="header-anchor" href="#_1-目标" aria-label="Permalink to &quot;1. 目标&quot;">​</a></h2><p>提供权限-能力-风险-默认策略参考表，供 UI 复制和验证重用。</p><h2 id="_2-矩阵" tabindex="-1">2. 矩阵 <a class="header-anchor" href="#_2-矩阵" aria-label="Permalink to &quot;2. 矩阵&quot;">​</a></h2><table tabindex="0"><thead><tr><th>许可</th><th>风险</th><th>允许的 API/功能</th><th>默认政策</th><th>注释</th></tr></thead><tbody><tr><td><code>ui.panel</code></td><td>低</td><td>打开插件面板</td><td>安装时授予</td><td>几乎所有 UI 插件都需要</td></tr><tr><td><code>ui.view</code></td><td>低</td><td><code>contributes.views</code> 在工作面板中列出并可打开</td><td>安装时授予</td><td>与面板窗口同级隔离：沙箱页面、按插件划分的会话分区、<code>net.domains</code> 出口限制。按激活范围过滤</td></tr><tr><td><code>ui.theme</code></td><td>低</td><td><code>contributes.themes</code> CSS 已在“设置”中加载并提供</td><td>安装时授予</td><td>CSS 由主机清理；它无法编写脚本。已声明的 <code>assets</code> 通过主机的只读 <code>plugin-asset:</code> 协议提供</td></tr><tr><td><code>ui.window.appearance</code></td><td>低</td><td>该插件主题被选中时，用 <code>contributes.windowAppearance</code> 设置原生窗口背景</td><td>安装时授予</td><td>仅接受 <code>#rrggbb</code> / <code>#rrggbbaa</code>；按解析后的明暗生效，主题消失后回到宿主默认值。macOS 保持 vibrancy</td></tr><tr><td><code>clipboard.read</code></td><td>中等</td><td><code>clipboard.readText</code>、<code>clipboard.getHistory</code></td><td>首次使用时确认</td><td>可能会读取敏感信息和保留的剪贴板历史</td></tr><tr><td><code>clipboard.write</code></td><td>中等</td><td><code>clipboard.writeText</code></td><td>首次使用时确认</td><td>防止剪贴板污染</td></tr><tr><td><code>notify</code></td><td>低</td><td><code>ui.notify</code>、<code>ui.getNotificationPermission</code>、<code>ui.requestNotificationPermission</code>、<code>ui.showNativeNotification</code></td><td>可以默认授予</td><td>本机交付由操作系统控制；避免通知垃圾邮件滥用</td></tr><tr><td><code>fs.read</code></td><td>中等</td><td><code>fs.readText</code> / <code>fs.readPreview</code> / <code>fs.openDefault</code> / <code>fs.reveal</code> / <code>fs.glob</code> / <code>fs.list</code> / <code>fs.requestDirectory</code></td><td>安装时授予，范围由 <code>manifest.fs.read</code> 限定</td><td><code>fs.readPreview</code>、<code>fs.openDefault</code> 和 <code>fs.reveal</code> 仅限显式选择的文件和相同的读取范围；所有文件调用仍受根目录与拒绝列表保护</td></tr><tr><td><code>fs.write</code></td><td>高</td><td><code>fs.writeText</code></td><td>安装时授予，范围由 <code>manifest.fs.write</code> 限定</td><td>必须声明范围；整棵树的模式无法通过校验。范围外要问用户</td></tr><tr><td><code>fs.delete</code></td><td>高</td><td><code>fs.remove</code></td><td>安装时授予，范围由 <code>manifest.fs.delete</code> 限定</td><td>分两档（<code>own</code> / <code>scope</code>），一律进系统回收站，不递归，并有速率刹车（§2B）</td></tr><tr><td><code>fs.read.workspace</code></td><td>中等</td><td>—</td><td>加载时降级为 <code>fs.read</code> + 整棵树范围</td><td>旧权限名，早于文件范围机制</td></tr><tr><td><code>fs.write.workspace</code></td><td>高</td><td>—</td><td>加载时降级为 <code>fs.write</code> 且<strong>没有</strong>范围</td><td>旧权限名；在 manifest 声明范围之前，每次写入都要问用户</td></tr><tr><td><code>fs.delete.workspace</code></td><td>高</td><td>—</td><td>加载时降级为 <code>fs.delete</code> + <code>own: true</code></td><td>旧权限名；只有插件自己写过的文件才不用问</td></tr><tr><td><code>agent.tool.register</code></td><td>高</td><td>注册代理工具</td><td>安装时确认</td><td>工具执行情况单独审核</td></tr><tr><td><code>agent.prompt.inject</code></td><td>高</td><td>注入系统提示符；激活 <code>contributes.skills</code></td><td>默认拒绝/强确认</td><td>容易导致行为劫持</td></tr><tr><td><code>agent.extension</code></td><td>高</td><td>在 agent 进程内运行 <code>contributes.agentExtensions</code> 模块</td><td>显式确认；v1.1 仅限本地导入和开发插件</td><td>与 agent 自身工具同等权限；插件沙箱不适用（规格 16）</td></tr><tr><td><code>provider.register</code></td><td>高</td><td><code>contributes.providers</code> 成为原生 Provider 列表中的行，归插件所有并在每次加载时按 manifest 刷新</td><td>显式确认；v1.1 仅限本地导入和开发插件，与 <code>agent.extension</code> 一致</td><td>用户路径拒绝该行（<code>PROVIDER_OWNED_BY_PLUGIN</code>）；凭据仍存放在 Host secret store 的常规 provider 引用下；暂不启用 <code>oauth</code> 声明</td></tr><tr><td><code>net.fetch</code></td><td>高</td><td><code>net.fetch</code></td><td>默认拒绝</td><td>限定在 <code>manifest.net.domains</code> 之内；列表为空或非法即完全不放行出网（§2A）</td></tr><tr><td><code>net.websocket</code></td><td>高</td><td><code>pi.net.websocket.connect</code> / <code>send</code> / <code>close</code>（套接字由宿主持有；每个插件最多 4 个，帧封顶 1 MiB）</td><td>默认拒绝</td><td>与 <code>net.fetch</code> 一样被限制在 <code>manifest.net.domains</code> 之内；被拒绝的主机永远到不了传输层，插件卸载、被禁用或崩溃时每个套接字都会被关闭</td></tr><tr><td><code>shell.openExternal</code></td><td>中等</td><td>打开外部链接</td><td>首次使用时确认</td><td>防止网络钓鱼链接</td></tr><tr><td><code>mcp.server.local</code></td><td>高</td><td>生成清单中声明的 <code>transport: &quot;stdio&quot;</code> MCP 服务器</td><td>默认拒绝</td><td>运行本地可执行文件；其工具到达代理</td></tr><tr><td><code>mcp.server.remote</code></td><td>高</td><td>连接 <code>transport: &quot;http&quot;</code> MCP 服务器</td><td>默认拒绝</td><td>将工具参数发送到第三方端点；非回环 HTTP 不加密</td></tr><tr><td><code>background.service</code></td><td>中等</td><td>启动 <code>contributes.services</code> 并保持插件进程常驻</td><td>安装时确认</td><td>受后退监督；在插件页面上可见</td></tr><tr><td><code>bus.publish</code></td><td>中等</td><td><code>bus.publish</code> 声明的主题</td><td>安装时确认</td><td>其他插件可以对消息进行操作</td></tr><tr><td><code>bus.subscribe</code></td><td>中等</td><td><code>bus.subscribe</code> 到声明的模式</td><td>安装时确认</td><td>可以观察另一个插件的消息</td></tr><tr><td><code>browser.cdp</code></td><td>高</td><td>对宿主工作面板访客页调用 <code>pi.browser.*</code></td><td>安装时确认</td><td>访客页边界夹紧到调用插件视图；CDP 走白名单</td></tr><tr><td><code>desktop.control</code></td><td>高</td><td><code>pi.desktop.listOperations</code>、<code>pi.desktop.invoke</code></td><td>安装时确认</td><td>与本地 MCP 控制平面共用同一份已审查的操作目录，但标记为 plugin-only 的操作例外：六个 <code>session/collaboration/*</code> 操作可以经由插件网关调用，却被刻意排除在 MCP 可见目录之外，且没有渲染器变更通道；<code>dangerous</code> 操作需要插件传 <code>confirm: true</code> <strong>并且</strong>用户在宿主拥有的原生对话框中作答，对话框点名目录中的操作；MCP bearer token 和 Electron 通道名永不暴露</td></tr><tr><td><code>ui.microphone</code></td><td>中等</td><td>在插件的隔离面板内调用 <code>navigator.mediaDevices.getUserMedia({ audio: true })</code></td><td>安装时确认</td><td>仅音频；摄像头和其他所有设备权限仍被拒绝；插件拿不到原生句柄或宿主密钥</td></tr><tr><td><code>audio.capture.background</code></td><td>高</td><td><code>pi.audio.getInputDevices</code>、<code>openInput</code>、<code>closeInput</code>、<code>getCaptureState</code>、<code>onInputFrame</code> / <code>offInputFrame</code>（已注册在插件 API 中并由该权限把关；两个同步注册辅助函数同步抛出带错误码的拒绝）</td><td>默认拒绝</td><td>设备由宿主持有；只交换 PCM16 帧，没有设备句柄或 <code>MediaStream</code>。宿主目前还没有设备后端，所以获得授权的调用会以带错误码的 <code>UNSUPPORTED</code> 拒绝并记入审计；不会打开任何设备</td></tr><tr><td><code>audio.playback.background</code></td><td>中等</td><td><code>pi.audio.openOutput</code>、<code>writeOutput</code>、<code>stopOutput</code>、<code>closeOutput</code>（已注册在插件 API 中并由该权限把关）</td><td>安装时确认</td><td>播放队列由宿主持有，仅 PCM16。宿主目前还没有设备后端，所以获得授权的调用会以带错误码的 <code>UNSUPPORTED</code> 拒绝并记入审计；不会打开任何设备</td></tr><tr><td><code>keyboard.globalShortcut</code></td><td>中等</td><td><code>pi.keyboard.registerGlobalShortcut</code>、<code>unregisterGlobalShortcut</code>、<code>listGlobalShortcuts</code>；<code>contributes.globalShortcuts</code></td><td>安装时确认</td><td>宿主持有 Electron 的 <code>globalShortcut</code>；快捷键只能运行插件自己的命令；冲突会被拒绝（<code>SHORTCUT_CONFLICT</code> / <code>SHORTCUT_UNAVAILABLE</code> / <code>INVALID_ACCELERATOR</code> / <code>LIMIT_EXCEEDED</code>，每个插件最多 8 条）；卸载、禁用或崩溃时释放</td></tr><tr><td><code>models.list</code></td><td>中等</td><td><code>pi.models.list</code></td><td>安装时确认</td><td>仅已就绪的 provider/model 行；不含密钥</td></tr><tr><td><code>project.create</code></td><td>高</td><td><code>pi.project.create</code> 及会话导入中的显式 <code>projectId</code></td><td>安装时确认</td><td>创建或复用持久项目记录但不激活工作区；只有显式传入 id 的导入会绑定项目</td></tr><tr><td><code>session.read</code></td><td>高</td><td><code>pi.session.getLlmContext</code></td><td>安装时确认</td><td>仅限进行中的工具会话；带 compaction 的投影（D019 / D336）</td></tr><tr><td><code>session.import</code></td><td>高</td><td><code>pi.session.import</code>、<code>pi.session.importBatch</code></td><td>安装时确认</td><td>只能导入插件声明来源；有大小和频率限制</td></tr><tr><td><code>session.read.own</code></td><td>中等</td><td><code>pi.session.list</code>、<code>pi.session.get</code>、<code>pi.session.listMessages</code></td><td>安装时确认</td><td>只能读取本插件导入的会话；不能跨插件访问</td></tr><tr><td><code>session.update.own</code></td><td>中等</td><td><code>pi.session.rename</code></td><td>安装时确认</td><td>只能重命名本插件拥有的活动导入会话</td></tr><tr><td><code>session.delete.own</code></td><td>高</td><td><code>pi.session.delete</code></td><td>安装时确认</td><td>只能回收或清除本插件导入的会话；有频率限制</td></tr><tr><td><code>usage.read</code></td><td>中等</td><td><code>pi.usage.listTurns</code></td><td>安装时确认</td><td>已完成 turn 事实行的只读列举（每回合 token 计数与标识符，keyset 分页）；不含消息正文，无写路径</td></tr><tr><td><code>agent.complete</code></td><td>高</td><td><code>pi.agent.complete</code></td><td>安装时确认</td><td>宿主代发一次性补全；消耗用户额度；<code>includeSessionContext</code> 还需要 <code>session.read</code></td></tr><tr><td><code>speech.adapter.register</code></td><td>高</td><td><code>pi.speech.registerAdapter</code> / <code>unregisterAdapter</code></td><td>安装时确认</td><td>注册语音协议。handle 留在插件进程；HTTP 计划由宿主用绑定密钥代发且必须同 origin</td></tr></tbody></table><h2 id="_2a-权限是开关-manifest-承载范围" tabindex="-1">2A. 权限是开关，manifest 承载范围 <a class="header-anchor" href="#_2a-权限是开关-manifest-承载范围" aria-label="Permalink to &quot;2A. 权限是开关，manifest 承载范围&quot;">​</a></h2><p>有两种能力光靠一个权限名说不清楚：名字负责回答「插件能不能做」， manifest 里的字段负责回答「能做到多远」。两个字段都由主机强制执行、 在权限旁展示给用户，并在安装时校验。</p><table tabindex="0"><thead><tr><th>字段</th><th>限定的范围</th><th>缺失或为空时</th></tr></thead><tbody><tr><td><code>net.domains</code></td><td>主机掌握的每一条出网路径：面板 session、<code>pi.net.fetch</code>、远程 HTTP MCP 端点</td><td>完全不放行出网，无论 <code>net.fetch</code> 是否声明</td></tr><tr><td><code>fs.read</code> / <code>fs.write</code> / <code>fs.delete</code></td><td>该文件模式可以触碰哪些路径</td><td>没有常驻可达范围；每次访问都落到确认弹窗</td></tr></tbody></table><p>字段缺失时一律 fail closed，这正是它们可以省略的原因：manifest 什么都不说， 就什么都不授予。参见 <a href="/zh-CN/spec/07-plugins/04-plugin-security">04-plugin-security.md</a> §6 与 §8.1，以及 ADR 0088。</p><p>两者还互相牵连。<code>fs.read</code> 之所以可以声明整棵树，是因为读取只有在字节能离开时 才变成泄露，而 <code>net.domains</code> 已经把这一半关上了。<code>fs.write</code> 和 <code>fs.delete</code> 本身就有破坏性，所以整棵树的模式（<code>**</code>、<code>**/*</code>、<code>*/**</code>、<code>./*</code>）在这两种模式下 无法通过清单校验。</p><h2 id="_2b-删除" tabindex="-1">2B. 删除 <a class="header-anchor" href="#_2b-删除" aria-label="Permalink to &quot;2B. 删除&quot;">​</a></h2><p><code>fs.delete</code> 是唯一一种「重跑一遍插件也补不回来」的文件操作，因此比其他模式多三道约束：</p><ol><li><strong>两档。</strong> <code>own: true</code> 允许插件删除自己写过的文件 —— 主机在插件数据目录里 维护一份写入台账 —— 无需范围、无需弹窗；用户之后改过的文件会掉出台账。 删别的东西必须声明 <code>scope</code>，范围之外要问用户。</li><li><strong>系统回收站。</strong> 删除走 <code>shell.trashItem</code>，不走 <code>rm</code>，并且永不递归： 非空目录直接拒绝而不是清空。主机不为此保留用户数据的任何副本。</li><li><strong>速率刹车。</strong> 每个插件每滚动 60 秒 50 次删除。超过之后问用户一次， 理由写的是速率而不是路径 —— 因为 <code>recursive: false</code> 只能约束单次调用， 约束不了 <code>glob</code> 加一个循环。</li></ol><h2 id="_3-权限依赖" tabindex="-1">3. 权限依赖 <a class="header-anchor" href="#_3-权限依赖" aria-label="Permalink to &quot;3. 权限依赖&quot;">​</a></h2><ul><li>加载面板条目需要 <code>ui.panel</code></li><li>贡献工作面板视图需要 <code>ui.view</code>；它与 <code>ui.panel</code> 相互独立， 因此插件可以只提供停靠视图而没有独立窗口</li><li>需要<code>agent.tool.register</code>来贡献agent工具</li><li>当 <code>fs.write</code> 存在时，建议同时声明 <code>fs.read</code></li><li><code>manifest.fs.&lt;mode&gt;</code> 需要对应的 <code>fs.&lt;mode&gt;</code> 权限；没人能用的范围会导致校验失败， 而不是被悄悄忽略</li><li><code>fs.requestDirectory</code>（<code>userSelected</code> root）由 <code>fs.read</code> 把关；在用户选中的目录里 写入或删除仍然需要 <code>fs.write</code> / <code>fs.delete</code></li><li>缺少权限的贡献未通过清单验证 （<code>themes</code>、<code>mcpServers</code>、<code>services</code>、<code>bus</code>）； <code>skills</code> 是例外，并且是 相反，在加载时跳过（参见 <a href="/zh-CN/spec/07-plugins/02-plugin-manifest-schema">02-plugin-manifest-schema.md</a> §7)</li><li>生命周期与状态事件不需要权限：<code>workspace:changed</code>、<code>session:modelChanged</code>、 <code>session:turnEnded</code> 和 <code>plugin:settingsChanged</code> 走既有的插件事件通道， 订阅未知的事件名也不会报错</li></ul><h2 id="_3a。-plan-操作状态规则" tabindex="-1">3A。 Plan 操作状态规则 <a class="header-anchor" href="#_3a。-plan-操作状态规则" aria-label="Permalink to &quot;3A。 Plan 操作状态规则&quot;">​</a></h2><p>每个 <code>agentTools</code> 贡献都会在 Plan 中被拒绝，无论此矩阵的值如何 风险或违约政策。 <code>agent.tool.register</code> 授权注册 Agent，在 Plan 中不可见。主机返回 <code>PLUGIN_DISABLED_IN_PLAN</code> 直接 Plan 调用并记录拒绝。仅插件工具符合资格 在同一个 Agent 被批准进入 Agent 模式后。</p><h2 id="_4-权限显示副本" tabindex="-1">4. 权限显示副本 <a class="header-anchor" href="#_4-权限显示副本" aria-label="Permalink to &quot;4. 权限显示副本&quot;">​</a></h2><p>英文是主要副本。 zh-CN 列保存本地化的示例字符串。 文件权限从不单独展示：声明的范围会渲染在它旁边， 所以「修改它列出的文件」后面紧跟着那份清单。</p><table tabindex="0"><thead><tr><th>许可</th><th>英文副本</th><th>zh-CN 示例</th></tr></thead><tbody><tr><td><code>fs.read</code></td><td>Read the files it lists</td><td>读取它列出的文件</td></tr><tr><td><code>clipboard.read</code></td><td>Read the current clipboard and retained history</td><td>读取当前剪贴板和保留的历史</td></tr><tr><td><code>fs.write</code></td><td>Modify the files it lists</td><td>修改它列出的文件</td></tr><tr><td><code>fs.delete</code></td><td>Delete the files it lists, to the trash</td><td>删除它列出的文件（进回收站）</td></tr><tr><td><code>notify</code></td><td>显示应用内和本机通知</td><td>显示应用内和系统通知</td></tr><tr><td><code>agent.tool.register</code></td><td>为AI Agent提供可执行工具</td><td>向AI Agent提供可执行工具</td></tr><tr><td><code>agent.prompt.inject</code></td><td>调整代理指令</td><td>调整智能体指令</td></tr><tr><td><code>agent.extension</code></td><td>在 agent 内运行代码</td><td>在 agent 内运行代码</td></tr><tr><td><code>net.fetch</code></td><td>访问网络</td><td>访问网络</td></tr><tr><td><code>shell.openExternal</code></td><td>打开外部链接</td><td>打开外部链接</td></tr><tr><td><code>ui.theme</code></td><td>提供一个主题</td><td>提供主题</td></tr><tr><td><code>ui.settings</code></td><td>Add a sandboxed Settings entry in Extensions</td><td>在“扩展”中添加沙盒设置项</td></tr><tr><td><code>ui.window.appearance</code></td><td>设置窗口背景</td><td>设置窗口背景</td></tr><tr><td><code>mcp.server.local</code></td><td>运行本地 MCP 服务器</td><td>运行本地 MCP 服务</td></tr><tr><td><code>mcp.server.remote</code></td><td>到达远程 MCP 服务器</td><td>连接远端 MCP 服务</td></tr><tr><td><code>background.service</code></td><td>保持后台服务运行</td><td>保持后台服务运行</td></tr><tr><td><code>bus.publish</code></td><td>向其他插件发送消息</td><td>向其他插件发送消息</td></tr><tr><td><code>bus.subscribe</code></td><td>接收来自其他插件的消息</td><td>接收其他插件的消息</td></tr><tr><td><code>browser.cdp</code></td><td>Control the work-panel browser</td><td>控制工作面板浏览器</td></tr><tr><td><code>models.list</code></td><td>List authenticated models</td><td>列出已登录的模型</td></tr><tr><td><code>session.read</code></td><td>Read the current conversation sent to the model</td><td>读取当前发给模型的对话</td></tr><tr><td><code>session.import</code></td><td>Import bounded session history into your declared sources</td><td>导入受限的会话历史到已声明的数据源</td></tr><tr><td><code>session.read.own</code></td><td>Read sessions imported by this plugin</td><td>读取此插件导入的会话</td></tr><tr><td><code>session.update.own</code></td><td>Rename sessions imported by this plugin</td><td>重命名此插件导入的会话</td></tr><tr><td><code>session.delete.own</code></td><td>Trash or purge sessions imported by this plugin</td><td>将此插件导入的会话移入回收站或清除</td></tr><tr><td><code>usage.read</code></td><td>Read usage statistics</td><td>读取用量统计</td></tr><tr><td><code>agent.complete</code></td><td>Run a one-shot completion with your models</td><td>用你的模型发起一次补全</td></tr><tr><td><code>speech.adapter.register</code></td><td>Register a speech adapter</td><td>注册语音适配器</td></tr><tr><td><code>audio.capture.background</code></td><td>Use the microphone in the background</td><td>后台使用麦克风</td></tr><tr><td><code>audio.playback.background</code></td><td>Play audio in the background</td><td>后台播放声音</td></tr><tr><td><code>keyboard.globalShortcut</code></td><td>Register system-wide shortcuts</td><td>注册系统级快捷键</td></tr><tr><td><code>net.websocket</code></td><td>Open real-time connections</td><td>建立实时双向连接</td></tr></tbody></table><h2 id="_5-添加升级权限" tabindex="-1">5. 添加升级权限 <a class="header-anchor" href="#_5-添加升级权限" aria-label="Permalink to &quot;5. 添加升级权限&quot;">​</a></h2><p>如果升级时出现新权限：</p><ol><li>计算差异 2.强制用户确认</li><li>如果没有确认，请取消升级或禁用新功能（建议取消升级）</li></ol><h2 id="_6-运行时检查伪代码" tabindex="-1">6. 运行时检查伪代码 <a class="header-anchor" href="#_6-运行时检查伪代码" aria-label="Permalink to &quot;6. 运行时检查伪代码&quot;">​</a></h2><div class="language-ts vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">assertPermission</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(pluginId, perm) {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> if</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> (</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">!</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">granted</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(pluginId, perm)) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">throw</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> ERROR_PERMISSION_DENIED</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>每个主机 API 入口点必须首先置位。文件类入口点之后还要再过三道门， 顺序固定 —— 后面的门只能拒绝，永远不能放宽：</p><div class="language-ts vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">assertFsAccess</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(pluginId, mode, requestedPath, sessionId) {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> assertPermission</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(pluginId, </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">\`fs.\${</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">mode</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">}\`</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)              </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// 已声明且已授予</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> full </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> realpathWithinRoot</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">root</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(pluginId, mode, sessionId), requestedPath)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> if</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> (</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">!</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">full) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">throw</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> NOT_FOUND</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> INVALID_ARGUMENT</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">         // 先解析软链</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> if</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> (</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">isDenied</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(full) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">||</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> isHostReserved</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(full)) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">throw</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> ERROR_PERMISSION_DENIED</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> if</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> (</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">!</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">inScope</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(full, </span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">declaredScope</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(pluginId, mode))) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">await</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> confirmWithUser</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">...</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p><code>workspace</code> 根是调用该调用的工具会话所属的项目，面板调用没有工具会话，回退到可见工作区（ADR 0266）。</p><h2 id="_7-验收" tabindex="-1">7. 验收 <a class="header-anchor" href="#_7-验收" aria-label="Permalink to &quot;7. 验收&quot;">​</a></h2><ol><li>未经授权的API调用失败</li><li>权限副本在安装 UI 中可见，且文件权限会同时显示它声明的范围 3.添加权限提示用户的升级</li><li>声明范围之外的写入或删除会弹窗，拒绝会以 <code>PERMISSION_DENIED</code> 记入审计</li><li>在整棵树的读取范围下，<code>.env</code> 与 <code>.git/**</code> 仍然不可读，也不会出现在 <code>fs.glob</code> 的结果里</li><li>root 之内指向外部的软链不能把访问带出去</li><li>删除进系统回收站、拒绝非空目录，并在滚动一分钟内超过 50 次后被打断</li><li>只声明旧权限名 <code>fs.*.workspace</code> 的插件会失去写入与删除的可达范围， 插件页面会把这件事说出来</li></ol></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("zh-CN/spec/07-plugins/13-plugin-permissions-matrix.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _13PluginPermissionsMatrix = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _13PluginPermissionsMatrix as default
};
