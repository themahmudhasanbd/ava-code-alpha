import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0171: Host-owned completed-turn token history","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0171-host-owned-completed-turn-token-history.md","filePath":"adr/0171-host-owned-completed-turn-token-history.md","lastUpdated":1788765396000}');
const _sfc_main = { name: "adr/0171-host-owned-completed-turn-token-history.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0171-host-owned-completed-turn-token-history" tabindex="-1">ADR 0171: Host-owned completed-turn token history <a class="header-anchor" href="#adr-0171-host-owned-completed-turn-token-history" aria-label="Permalink to &quot;ADR 0171: Host-owned completed-turn token history&quot;">​</a></h1><ul><li>Status: Accepted (amended by ADR 0173)</li><li>Date: 2026-09-07</li><li>Deciders: PI-Desktop core</li><li>Related: D103, D157, D331, D335, ADR 0014, ADR 0173, <code>03-runtime/04-data-storage.md</code> §4.6, <code>03-runtime/06-host-rpc-protocol.md</code>, <code>04-ux/06-settings-ia.md</code>, E2E-186</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The <code>turns</code> table already stores <code>input_tokens</code>, <code>output_tokens</code>, and <code>usage_json</code>, and <code>session.endTurn</code> already accepted <code>usage</code>. Electron never sent it. Per-message chips (D103) read provider usage from assistant <code>meta_json</code>. Mixing subagent spend into those chips would inflate the context inspector and the composed-turn total.</p><p>A durable completed-turn total is still required so a later dashboard can show what the user actually spent, including subagents, without a new schema version.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><strong>Parent <code>message.usage</code> stays provider-reported.</strong> Subagent totals never merge into an assistant row.</li><li><strong>Turn rollup is <code>session.endTurn.usage</code>.</strong> Electron sums every parent assistant <code>message_end</code> usage for the durable turn and adds <code>turn_end.subagentUsage</code> deltas. Only that sum is stored on <code>turns</code>.</li><li><strong><code>stats.getTokenUsageHistory</code> is an additive host RPC.</strong> It reads completed turns in a bounded local-calendar window, buckets by <code>day</code> / ISO <code>week</code> / <code>month</code>, fills empty buckets, and does not bump <code>PROTOCOL_VERSION</code> or <code>SCHEMA_VERSION</code>. <code>idx_turns_ended_at</code> is created with <code>CREATE INDEX IF NOT EXISTS</code> at boot.</li><li><strong>The user-facing dashboard is not Settings.</strong> ADR 0173 moves that surface to marketplace plugin <code>pi.token-insights</code>. This RPC still exists so local completed-turn history, including subagent spend, has a host owner. Historical rows from before Electron sent <code>usage</code> may be zero.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Context inspector and D103 chips keep exact provider values.</li><li>New completed turns populate host history; older turns may be zero.</li><li>The heatmap lives in <code>pi.token-insights</code> (ADR 0173).</li><li>A later backfill from transcript <code>meta.usage</code> would be a separate change.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><ul><li>Rewrite parent <code>message.usage</code> with subagent spend: rejected (D103).</li><li>Scan JSONL transcripts in the host Settings page: rejected (unbounded, wrong owner). The marketplace plugin may scan local tool metadata, including JSONL.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0171-host-owned-completed-turn-token-history.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0171HostOwnedCompletedTurnTokenHistory = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0171HostOwnedCompletedTurnTokenHistory as default
};
