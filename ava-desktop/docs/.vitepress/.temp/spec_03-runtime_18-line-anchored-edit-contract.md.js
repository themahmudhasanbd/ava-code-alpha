import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"18. Line-Anchored Edit Contract","description":"","frontmatter":{},"headers":[],"relativePath":"spec/03-runtime/18-line-anchored-edit-contract.md","filePath":"spec/03-runtime/18-line-anchored-edit-contract.md","lastUpdated":1788986066000}');
const _sfc_main = { name: "spec/03-runtime/18-line-anchored-edit-contract.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="_18-line-anchored-edit-contract" tabindex="-1">18. Line-Anchored Edit Contract <a class="header-anchor" href="#_18-line-anchored-edit-contract" aria-label="Permalink to &quot;18. Line-Anchored Edit Contract&quot;">​</a></h1><blockquote><p>Decisions applied: ADR 0087. Amends D186, ADR 0069 §2, ADR 0043 §1. Source of truth for implementation: <code>crates/host-core/src/tools/hashline/</code> (new), <code>crates/host-core/src/tools/mod.rs</code>, <code>packages/agent-runtime/src/runtime.ts</code>.</p></blockquote><h2 id="_1-purpose" tabindex="-1">1. Purpose <a class="header-anchor" href="#_1-purpose" aria-label="Permalink to &quot;1. Purpose&quot;">​</a></h2><p><code>Edit</code> names <strong>positions</strong> in a file and supplies <strong>new content only</strong>. It never asks the model to reproduce existing bytes. Version correctness and read provenance are proven by the host, not asserted by the model.</p><p>Three properties are enforced, in this order, before any byte is written:</p><ol><li><strong>Version.</strong> The <code>tag</code> the model supplies must hash the live file.</li><li><strong>Provenance.</strong> Every line the edit anchors on must have been displayed to this session.</li><li><strong>Determinism.</strong> The op must resolve to exactly one span. Ambiguity fails; it is never resolved by preference.</li></ol><h2 id="_2-frozen-policy-summary" tabindex="-1">2. Frozen policy summary <a class="header-anchor" href="#_2-frozen-policy-summary" aria-label="Permalink to &quot;2. Frozen policy summary&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Topic</th><th>Decision</th></tr></thead><tbody><tr><td><code>Edit</code> parameters</td><td><code>path</code>, <code>tag</code>, <code>ops</code></td></tr><tr><td>Removed</td><td><code>old_string</code>, <code>new_string</code>, and any mode switch between contracts</td></tr><tr><td>Tag</td><td>4 uppercase hex, derived from the whole normalized file</td></tr><tr><td>Tag minted by</td><td><code>Read</code>, <code>Grep</code>, <code>Write</code>, and every successful <code>Edit</code></td></tr><tr><td>Anchor unit</td><td>1-indexed original line numbers of the tagged snapshot</td></tr><tr><td>Body rows</td><td><code>+</code>-prefixed final content only; no <code>-old</code>, no context rows</td></tr><tr><td>Sections per call</td><td>exactly one (<code>path</code> in args)</td></tr><tr><td>Named registers</td><td>session-scoped, survive across <code>Edit</code> calls</td></tr><tr><td>Anonymous register</td><td>call-local</td></tr><tr><td>Block ops</td><td>tree-sitter; decline when unresolvable, never approximate</td></tr><tr><td>Stale tag</td><td>recover only when provably unique and safe; otherwise reject</td></tr><tr><td>No-op apply</td><td>error</td></tr><tr><td>Snapshot store</td><td>in-memory, per session, bounded, dropped with the session</td></tr></tbody></table><h2 id="_3-snapshot-tags" tabindex="-1">3. Snapshot tags <a class="header-anchor" href="#_3-snapshot-tags" aria-label="Permalink to &quot;3. Snapshot tags&quot;">​</a></h2><h3 id="_3-1-normalization" tabindex="-1">3.1 Normalization <a class="header-anchor" href="#_3-1-normalization" aria-label="Permalink to &quot;3.1 Normalization&quot;">​</a></h3><p>Before hashing, and before any line is addressed, file text is normalized:</p><ol><li>A leading UTF-8 BOM is stripped and retained for restoration on write.</li><li>Line endings are detected and normalized to <code>LF</code>; the dominant original ending is retained for restoration on write.</li><li>Trailing <code>[ \\t\\r]</code> is removed from every line, including the last.</li></ol><p>Step 3 applies to the <strong>hash input only</strong>, never to the content the file keeps. It exists so that a <code>CRLF</code> file and a display that trimmed trailing whitespace still mint the same tag as the bytes on disk.</p><p>Line addressing splits normalized text on <code>LF</code>. A terminal newline terminates the preceding line and is not itself addressable content; a trailing empty element produced by the split is dropped.</p><h3 id="_3-2-computation" tabindex="-1">3.2 Computation <a class="header-anchor" href="#_3-2-computation" aria-label="Permalink to &quot;3.2 Computation&quot;">​</a></h3><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>tag(text) = uppercase_hex_4( low_16_bits( digest( normalize_for_hash(text) ) ) )</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p><code>digest</code> is the SHA-256 primitive already used by <code>crates/host-core/src/review.rs</code>; the tag takes its low 16 bits. Reusing the existing primitive keeps one hashing story in host-core and avoids a new dependency. The cost is one whole-file digest per read and per write, which is bounded by file size and measured in single-digit milliseconds for the file sizes <code>Read</code> serves.</p><h3 id="_3-3-collision-policy" tabindex="-1">3.3 Collision policy <a class="header-anchor" href="#_3-3-collision-policy" aria-label="Permalink to &quot;3.3 Collision policy&quot;">​</a></h3><p>The tag has 65 536 values. Collisions are expected, not exceptional. The tag is an <strong>index, never an identity</strong>:</p><ul><li>Snapshot deduplication requires <strong>full-text equality</strong>, not tag equality. Two distinct texts sharing a tag are two snapshots.</li><li>Lookup by tag returns the most recently recorded matching version.</li><li>Every decision that could corrupt a file — provenance validation, drift recovery, path recovery — additionally compares full text or validates surrounding context. A tag match alone never authorizes a write.</li></ul><p>Fusing two texts under one tag would attach one text&#39;s displayed lines to the other&#39;s content, which is the one way a 16-bit tag can cause real damage.</p><h2 id="_4-session-snapshot-store" tabindex="-1">4. Session snapshot store <a class="header-anchor" href="#_4-session-snapshot-store" aria-label="Permalink to &quot;4. Session snapshot store&quot;">​</a></h2><h3 id="_4-1-shape" tabindex="-1">4.1 Shape <a class="header-anchor" href="#_4-1-shape" aria-label="Permalink to &quot;4.1 Shape&quot;">​</a></h3><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>session_id → canonical_path → [Snapshot]   (newest first)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Snapshot {</span></span>
<span class="line"><span>  text:        String,        // full normalized text, LF, no BOM</span></span>
<span class="line"><span>  tag:         String,        // 4 hex, uppercase</span></span>
<span class="line"><span>  recorded_at: Instant,</span></span>
<span class="line"><span>  seen_lines:  Option&lt;BTreeSet&lt;u32&gt;&gt;,   // 1-indexed lines actually displayed</span></span>
<span class="line"><span>}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><p><code>canonical_path</code> is the resolved path already produced by <code>workspace::resolve_tool_path</code>, so workspace, scratch, and approved external roots share one keyspace and a path spelled two ways resolves to one entry.</p><p><code>seen_lines == None</code> means &quot;no provenance recorded&quot; — the provenance gate is then skipped rather than failing closed, so an externally minted or aged-out tag degrades to version checking only.</p><h3 id="_4-2-recording-and-read-fusion" tabindex="-1">4.2 Recording and read fusion <a class="header-anchor" href="#_4-2-recording-and-read-fusion" aria-label="Permalink to &quot;4.2 Recording and read fusion&quot;">​</a></h3><p><code>record(path, text, seen_lines)</code> returns the tag and:</p><ul><li>If a retained version has the same tag <strong>and</strong> the same full text, that version is promoted to head, its recency refreshed, and <code>seen_lines</code> unioned in. The tag is reused.</li><li>Otherwise a new version is unshifted onto the front of the path&#39;s history.</li></ul><p>Read fusion is what makes paginated reading work: reading lines 1–2000 and then 1800–3600 of an unchanged file produces one tag whose <code>seen_lines</code> covers 1–3600, so a later <code>Edit</code> anywhere in that range validates without a third read.</p><h3 id="_4-3-which-lines-count-as-seen" tabindex="-1">4.3 Which lines count as seen <a class="header-anchor" href="#_4-3-which-lines-count-as-seen" aria-label="Permalink to &quot;4.3 Which lines count as seen&quot;">​</a></h3><p>A line joins <code>seen_lines</code> only when the producer emitted it <strong>in full</strong>:</p><ul><li><code>Read</code> records every line it returned, <strong>excluding</strong> lines clipped at <code>MAX_LINE_CHARS</code> (16,384). A clipped line was not displayed; its tail is exactly where a blind edit does damage.</li><li><code>Grep</code> in <code>content</code> mode records only its matched lines. <code>filesWithMatches</code> and <code>count</code> modes record nothing and mint a tag with <code>seen_lines</code> empty, which is not the same as <code>None</code>: the tag is usable for version checking, and every anchor will be rejected until something displays the lines.</li><li><code>Write</code> and a successful <code>Edit</code> record the post-write content with <strong>all</strong> lines seen: the session authored them.</li></ul><p>An empty <code>seen_lines</code> set is treated as &quot;nothing seen&quot;, so every anchor is rejected. <code>None</code> is treated as &quot;unknown&quot;, so no anchor is rejected. The distinction is deliberate and must not be collapsed.</p><h3 id="_4-4-bounds-and-lifecycle" tabindex="-1">4.4 Bounds and lifecycle <a class="header-anchor" href="#_4-4-bounds-and-lifecycle" aria-label="Permalink to &quot;4.4 Bounds and lifecycle&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Bound</th><th>Value</th><th>Behavior at the limit</th></tr></thead><tbody><tr><td>Paths per session</td><td>64</td><td>LRU eviction of the coldest path history</td></tr><tr><td>Versions per path</td><td>4</td><td>oldest dropped first</td></tr><tr><td>Retained text per session</td><td>8 MiB</td><td>LRU path eviction until under the ceiling</td></tr><tr><td>Single-file retention</td><td>2 MiB</td><td>file is hashed and tagged but its text is not retained; recovery is unavailable for it</td></tr></tbody></table><p>The store lives in host-core process memory under the existing session state lock. It is dropped when the session is deleted, when the workspace is switched, and at process exit. It is never persisted: a tag from a previous run is unrecognized, which correctly forces a fresh <code>Read</code>.</p><p>Eviction is safe by construction. Losing a snapshot loses the ability to <em>recover</em> from drift and the ability to <em>validate</em> provenance; both degrade to a rejection or a warning, never to a wrong write.</p><h2 id="_5-producer-output-shapes" tabindex="-1">5. Producer output shapes <a class="header-anchor" href="#_5-producer-output-shapes" aria-label="Permalink to &quot;5. Producer output shapes&quot;">​</a></h2><h3 id="_5-1-read" tabindex="-1">5.1 <code>Read</code> <a class="header-anchor" href="#_5-1-read" aria-label="Permalink to &quot;5.1 \`Read\`&quot;">​</a></h3><p><code>content</code> becomes line-numbered and carries a section header. This reverses ADR 0069 §2&#39;s byte-faithful <code>content</code>, whose only purpose was <code>old_string</code> matching.</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[src/main.rs#A1B2]</span></span>
<span class="line"><span>41:fn main() {</span></span>
<span class="line"><span>42:    println!(&quot;hi&quot;);</span></span>
<span class="line"><span>43:}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><ul><li>The header is the tag of the <strong>whole file</strong>, not of the returned window.</li><li>Line numbers are absolute file lines, so <code>offset</code> does not shift them.</li><li>The <code>N:</code> prefix is not counted against <code>MAX_LINE_CHARS</code>; clipping still applies to the line text and still excludes that line from <code>seen_lines</code>.</li><li>Existing sibling fields (<code>path</code>, <code>root</code>, <code>offset</code>, <code>lineCount</code>, <code>totalLines</code>, <code>truncated</code>, <code>fileBytes</code>, <code>notice</code>) are unchanged. <code>tag</code> is added as a first-class field so consumers need not parse the header.</li></ul><h3 id="_5-2-grep" tabindex="-1">5.2 <code>Grep</code> <a class="header-anchor" href="#_5-2-grep" aria-label="Permalink to &quot;5.2 \`Grep\`&quot;">​</a></h3><p>Each <code>matches[]</code> entry keeps <code>{path, line, text}</code> and the result gains a <code>tags: { &lt;path&gt;: &lt;tag&gt; }</code> map plus a header line per file in the rendered form. A grep hit is therefore a usable edit anchor for the matched line only.</p><h3 id="_5-3-write" tabindex="-1">5.3 <code>Write</code> <a class="header-anchor" href="#_5-3-write" aria-label="Permalink to &quot;5.3 \`Write\`&quot;">​</a></h3><p>Returns <code>tag</code> and the <code>[path#TAG]</code> header of the content that actually landed, so an immediately following <code>Edit</code> needs no <code>Read</code>.</p><p><code>Write</code> continues to strip pasted <code>[path#TAG]</code> headers and <code>N:</code> line prefixes from incoming <code>content</code>; with line-numbered <code>Read</code> output this stops being a convenience and becomes required.</p><h3 id="_5-4-post-write-drift" tabindex="-1">5.4 Post-write drift <a class="header-anchor" href="#_5-4-post-write-drift" aria-label="Permalink to &quot;5.4 Post-write drift&quot;">​</a></h3><p>The tag recorded after a write is computed from the bytes the write path reports as landed, not from the bytes the tool intended. When a formatter or an external writethrough transforms content on save, recording the intended text would publish a tag for content that does not exist, and the next <code>Edit</code> would resolve against a baseline the file has already left.</p><p>The intended text is still what the review record and the model-visible summary describe, so a drifted write reports a one-line warning rather than a whole-file diff.</p><h2 id="_6-edit-request-shape" tabindex="-1">6. <code>Edit</code> request shape <a class="header-anchor" href="#_6-edit-request-shape" aria-label="Permalink to &quot;6. \`Edit\` request shape&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Required</th><th>Description</th></tr></thead><tbody><tr><td><code>path</code></td><td>string</td><td>yes</td><td>Existing regular file, workspace-relative or an approved absolute path. <code>Edit</code> never creates files; use <code>Write</code>.</td></tr><tr><td><code>tag</code></td><td>string</td><td>yes</td><td>4 uppercase hex from the most recent <code>Read</code> / <code>Grep</code> / <code>Write</code> / <code>Edit</code> result for this path.</td></tr><tr><td><code>ops</code></td><td>string</td><td>yes</td><td>One or more operation headers with their body rows, newline separated.</td></tr></tbody></table><p><code>ops</code> may optionally begin with a <code>[path#TAG]</code> header line. When present it must agree with <code>path</code> and <code>tag</code>; a disagreement is <code>INVALID_ARGUMENT</code> rather than a silent preference, because the two spellings disagreeing means the model has lost track of which file it is editing.</p><h2 id="_7-patch-language" tabindex="-1">7. Patch language <a class="header-anchor" href="#_7-patch-language" aria-label="Permalink to &quot;7. Patch language&quot;">​</a></h2><h3 id="_7-1-grammar" tabindex="-1">7.1 Grammar <a class="header-anchor" href="#_7-1-grammar" aria-label="Permalink to &quot;7.1 Grammar&quot;">​</a></h3><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ops        := header_block+</span></span>
<span class="line"><span>header_block := op_header NEWLINE body_row*        // colon-terminated headers</span></span>
<span class="line"><span>              | op_line                            // colonless headers</span></span>
<span class="line"><span>op_header  := &quot;PUT&quot; SP locator &quot;:&quot;</span></span>
<span class="line"><span>op_line    := &quot;PUT&quot; SP locator SP register</span></span>
<span class="line"><span>            | &quot;CUT&quot; SP locator [SP register]</span></span>
<span class="line"><span>            | &quot;REM&quot;</span></span>
<span class="line"><span>            | &quot;MV&quot; SP dest</span></span>
<span class="line"><span>locator    := line &quot;.=&quot; line        // inclusive range</span></span>
<span class="line"><span>            | line &quot;*&quot;              // syntactic block opening at line</span></span>
<span class="line"><span>            | &quot;&lt;&quot; line              // gap before line</span></span>
<span class="line"><span>            | &quot;&gt;&quot; line              // gap after line</span></span>
<span class="line"><span>            | &quot;&gt;&quot; &quot;$&quot;               // end of file</span></span>
<span class="line"><span>            | &quot;&gt;&quot; line &quot;*&quot;          // gap after the block opening at line</span></span>
<span class="line"><span>line       := [1-9][0-9]*</span></span>
<span class="line"><span>register   := &quot;@&quot; [A-Za-z0-9_-]{1,32}</span></span>
<span class="line"><span>body_row   := &quot;+&quot; TEXT</span></span>
<span class="line"><span>dest       := path | quoted_path</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br></div></div><h3 id="_7-2-operations" tabindex="-1">7.2 Operations <a class="header-anchor" href="#_7-2-operations" aria-label="Permalink to &quot;7.2 Operations&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Header</th><th>Body</th><th>Meaning</th></tr></thead><tbody><tr><td><code>PUT N.=M:</code></td><td>required</td><td>Replace original inclusive lines <code>N</code>–<code>M</code> with the body. <code>N.=N</code> for one line.</td></tr><tr><td><code>PUT N*:</code></td><td>required</td><td>Replace the syntactic block opening at line <code>N</code>.</td></tr><tr><td><code>PUT &lt;N:</code></td><td>required</td><td>Insert the body before line <code>N</code>. <code>PUT &lt;1:</code> is the file head.</td></tr><tr><td><code>PUT &gt;N:</code></td><td>required</td><td>Insert the body after line <code>N</code>.</td></tr><tr><td><code>PUT &gt;$:</code></td><td>required</td><td>Append at end of file.</td></tr><tr><td><code>PUT &gt;N*:</code></td><td>required</td><td>Insert after the end of the block opening at <code>N</code>, at sibling depth.</td></tr><tr><td><code>CUT N.=M</code></td><td>none</td><td>Delete lines <code>N</code>–<code>M</code> and capture them.</td></tr><tr><td><code>CUT N*</code></td><td>none</td><td>Delete the block opening at <code>N</code> and capture it.</td></tr><tr><td><code>CUT … @name</code></td><td>none</td><td>Capture into the named register instead of the anonymous one.</td></tr><tr><td><code>PUT &lt;N @name</code> / <code>PUT &gt;N @name</code></td><td>none</td><td>Paste a register at the gap.</td></tr><tr><td><code>PUT N.=M @name</code> / <code>PUT N* @name</code></td><td>none</td><td>Paste a register over the range or resolved block. <code>@name</code> is mandatory in the span form.</td></tr><tr><td><code>REM</code></td><td>none</td><td>Delete the file named by <code>path</code>.</td></tr><tr><td><code>MV DEST</code></td><td>none</td><td>Move/rename to <code>DEST</code> after applying every other op to the source.</td></tr></tbody></table><h3 id="_7-3-anchoring-rules" tabindex="-1">7.3 Anchoring rules <a class="header-anchor" href="#_7-3-anchoring-rules" aria-label="Permalink to &quot;7.3 Anchoring rules&quot;">​</a></h3><ol><li>All line numbers refer to the <strong>tagged snapshot</strong>. They are never shifted by earlier ops in the same call.</li><li>Ranges are inclusive and ordered (<code>N &lt;= M</code>).</li><li>Ranges name only the lines being <strong>changed</strong>. A pure insertion uses a gap locator; a widened <code>PUT</code> that restates surviving lines is the single most damaging authoring mistake and is what §7.4&#39;s <code>+</code>-only body rules exist to prevent.</li><li>Two ops may not overlap, and two ops may not target the same original anchor.</li><li>A range may not start or end inside a collapsed or clipped region. Lines excluded from <code>seen_lines</code> are rejected by §9&#39;s provenance gate, which covers this case mechanically rather than by convention.</li></ol><h3 id="_7-4-body-rows" tabindex="-1">7.4 Body rows <a class="header-anchor" href="#_7-4-body-rows" aria-label="Permalink to &quot;7.4 Body rows&quot;">​</a></h3><p>A body row is <code>+</code> followed by the row&#39;s final text, with leading whitespace preserved. A bare <code>+</code> is an empty line.</p><p>There is no <code>-old</code> row and no context row. The range already expresses the deletion; the body is only what the file will contain. A literal leading <code>-</code> or <code>+</code> in the content is written as <code>+- item</code> and <code>++ item</code>.</p><p>Colonless headers take <strong>no</strong> body rows. A body row under a register paste is <code>INVALID_ARGUMENT</code>, not a silent discard.</p><h3 id="_7-5-registers" tabindex="-1">7.5 Registers <a class="header-anchor" href="#_7-5-registers" aria-label="Permalink to &quot;7.5 Registers&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Register</th><th>Scope</th><th>Lifetime</th></tr></thead><tbody><tr><td>anonymous</td><td>one <code>Edit</code> call</td><td>cleared when the call returns</td></tr><tr><td><code>@name</code></td><td>session</td><td>until overwritten, the session ends, or the store is dropped</td></tr></tbody></table><p>Cross-file moves use two calls: <code>CUT 1* @fn</code> on the source, then <code>PUT &lt;1 @fn</code> on the destination. This is the sanctioned form; <code>Edit</code> is single-path by §13.2.</p><p>Two or more un-pasted anonymous <code>CUT</code>s in one call make the next anonymous paste ambiguous. It fails with a message naming the register syntax rather than picking the most recent capture.</p><p>A named register is captured content, not a live reference. Deleting or moving the source file afterwards does not invalidate it.</p><h2 id="_8-lowering-and-application" tabindex="-1">8. Lowering and application <a class="header-anchor" href="#_8-lowering-and-application" aria-label="Permalink to &quot;8. Lowering and application&quot;">​</a></h2><h3 id="_8-1-intermediate-representation" tabindex="-1">8.1 Intermediate representation <a class="header-anchor" href="#_8-1-intermediate-representation" aria-label="Permalink to &quot;8.1 Intermediate representation&quot;">​</a></h3><p>The parser lowers every op to a flat list of five edit kinds, all anchored on original line numbers:</p><table tabindex="0"><thead><tr><th>Kind</th><th>Produced by</th></tr></thead><tbody><tr><td><code>insert{cursor, text, mode?}</code></td><td>body rows of any <code>PUT</code></td></tr><tr><td><code>delete{anchor}</code></td><td>every line consumed by a range or block</td></tr><tr><td><code>cut{range, register?}</code></td><td><code>CUT</code></td></tr><tr><td><code>paste{target, register?}</code></td><td>register <code>PUT</code></td></tr><tr><td><code>block{anchor, payloads, mode?, register?}</code></td><td>any <code>N*</code> locator, before resolution</td></tr></tbody></table><p>A multi-line replacement decomposes into one <code>insert</code> per body row plus one <code>delete</code> per consumed line. Because every anchor indexes the original snapshot, all positions are computed once; no op observes another op&#39;s effect.</p><h3 id="_8-2-block-resolution" tabindex="-1">8.2 Block resolution <a class="header-anchor" href="#_8-2-block-resolution" aria-label="Permalink to &quot;8.2 Block resolution&quot;">​</a></h3><p><code>block</code> edits carry no span at parse time. <code>resolve_blocks(text, path, line)</code> runs before validation and returns an inclusive <code>{start, end}</code> or <code>None</code>.</p><p>Resolution returns <code>None</code> — and the op is rejected with an actionable message — for any of:</p><ul><li>an extension outside the supported grammar set;</li><li>a line number outside the file;</li><li>a line that opens no multi-line construct (a bare statement, a closing delimiter, or the construct&#39;s last line);</li><li>a file that does not parse cleanly.</li></ul><p>Supported grammars in the first implementation:</p><p><code>rust</code>, <code>typescript</code>, <code>tsx</code>, <code>javascript</code>, <code>jsx</code>, <code>json</code>, <code>markdown</code>, <code>css</code>, <code>python</code>, <code>toml</code>, <code>yaml</code>, <code>html</code>, <code>bash</code>.</p><p>Additional grammars are additive and require no contract change. An unsupported language loses block ops only; every range and gap op still works, and the rejection message says so explicitly.</p><p>Two resolution rules are contract, not implementation detail:</p><ul><li><strong>Leading attached nodes are separate.</strong> A decorator, attribute, or doc comment above a declaration is its own node. Anchoring the declaration line orphans them; anchoring the first decorator includes both. Standalone line comments are never swept into a block.</li><li><strong>Markdown headings are block openers.</strong> A block op on <code>##</code> covers that section through deeper headings up to the next same-or-higher heading.</li></ul><p>Every resolution is echoed in the tool result as <code>{anchorLine, start, end, op}</code> so a wrong-opener anchor is visible rather than silently applied.</p><p>Block resolution runs against the text the tag names: the live file when the tag matches, and the tagged snapshot when the file has drifted (so §10 can remap the resulting span). When the tagged snapshot is needed and unavailable, the span cannot be placed and the call is rejected.</p><h3 id="_8-3-clipboard-pre-pass" tabindex="-1">8.3 Clipboard pre-pass <a class="header-anchor" href="#_8-3-clipboard-pre-pass" aria-label="Permalink to &quot;8.3 Clipboard pre-pass&quot;">​</a></h3><p>Before ordinary application, <code>cut</code> and <code>paste</code> are expanded:</p><ol><li><code>cut</code> captures the range&#39;s current lines into its register and lowers to one <code>delete</code> per line.</li><li><code>paste</code> expands to one plain <code>insert</code> per captured line. A span target additionally expands to per-line <code>delete</code>s — <strong>only after the register read succeeds</strong>, so a paste from an empty register leaves no orphan deletes.</li></ol><p>The pre-pass validates sequencing first and reports it with its own message: pasting before any capture, and capturing over un-pasted anonymous content, are distinct errors from a tag mismatch and must not be reported as one.</p><h3 id="_8-4-replacement-boundary-repair" tabindex="-1">8.4 Replacement-boundary repair <a class="header-anchor" href="#_8-4-replacement-boundary-repair" aria-label="Permalink to &quot;8.4 Replacement-boundary repair&quot;">​</a></h3><p>A range that swallows one unchanged boundary row, or a body that restates a row surviving just outside the range, is repaired by a bounded candidate search. The repair is deliberately conservative:</p><ul><li>An exact restatement of a line immediately outside the range is normalized away on line equality alone.</li><li>If the authored result still does not parse, the search may retain the range&#39;s first or effective-last row, and may combine that retention with echo removal.</li><li>Retention never follows parse success alone: on a baseline that parsed, deleting the row must itself break syntax, and the candidate must also satisfy source-range structure and indentation evidence.</li><li><strong>Distinct candidate texts tied at the minimum repair cost are rejected, not ranked.</strong> A guess that is usually right is the failure mode this whole spec exists to remove.</li></ul><p>Every repair emits a warning naming what it changed.</p><h2 id="_9-validation-gates" tabindex="-1">9. Validation gates <a class="header-anchor" href="#_9-validation-gates" aria-label="Permalink to &quot;9. Validation gates&quot;">​</a></h2><p><code>Edit</code> resolves to exactly one of four outcomes. Let <code>expected</code> be the supplied tag and <code>live</code> the tag of the current file.</p><table tabindex="0"><thead><tr><th>Branch</th><th>Condition</th><th>Behavior</th></tr></thead><tbody><tr><td><strong>A. Direct</strong></td><td><code>live == expected</code></td><td>Run the provenance gate, then apply. Anchor numbers index the live file 1:1, so resolved block spans are echoed back.</td></tr><tr><td><strong>B. Position-stable</strong></td><td><code>live != expected</code>, and every op targets only the file head or tail</td><td>Apply to live content with a drift warning. <code>&gt;$</code> and <code>&lt;1</code> cannot be moved by content drift, so a stale tag is not fatal here.</td></tr><tr><td><strong>C. Recovery</strong></td><td><code>live != expected</code>, anchored ops present</td><td>Attempt §10. On success, apply with a recovery warning; resolved spans are <strong>not</strong> echoed, because line numbers moved.</td></tr><tr><td><strong>D. Reject</strong></td><td>recovery declined</td><td><code>EDIT_TAG_MISMATCH</code> with the live tag, the anchor lines, and current content around them.</td></tr></tbody></table><h3 id="_9-1-provenance-gate" tabindex="-1">9.1 Provenance gate <a class="header-anchor" href="#_9-1-provenance-gate" aria-label="Permalink to &quot;9.1 Provenance gate&quot;">​</a></h3><p>On branch A only, every anchor line is checked against the <code>seen_lines</code> of the snapshot whose text equals the live content:</p><ul><li><code>seen_lines</code> absent (<code>None</code>) → gate skipped.</li><li>All anchors seen → apply.</li><li>Otherwise → reject, and inline the real content of the unseen anchor lines.</li></ul><p>The rejection is designed so an honest retry succeeds without another <code>Read</code>:</p><ul><li>At most <strong>40</strong> unseen lines are revealed, each clipped at <strong>512</strong> characters.</li><li>When the reveal covered <strong>every</strong> unseen anchor line at <strong>full width</strong>, those lines are merged into <code>seen_lines</code>. The content in the error is itself the proof that the model has now seen them, so retrying the same <code>tag</code> applies.</li><li>When the reveal was truncated by either cap, <strong>no</strong> line is merged and the message keeps its &quot;re-read the range&quot; instruction.</li></ul><p>The asymmetry is load-bearing. Merging a truncated reveal would let a blind wide edit be split into under-cap retries that each reveal a slice and eventually apply without any read, and would let a minified line be laundered into <code>seen_lines</code> while most of its width stayed unseen.</p><h3 id="_9-2-path-recovery" tabindex="-1">9.2 Path recovery <a class="header-anchor" href="#_9-2-path-recovery" aria-label="Permalink to &quot;9.2 Path recovery&quot;">​</a></h3><p>When <code>path</code> does not exist on disk but its <strong>basename and tag</strong> together match exactly one file the session recorded, the edit is rebound to that file and a warning is emitted. Requirements:</p><ul><li>basename and tag must both match;</li><li>exactly one candidate — a tie declines;</li><li>the authored path&#39;s own recorded snapshot is excluded, so a deleted file cannot recover onto its own history;</li><li>rebinding happens <strong>before</strong> the write-permission gate, so a mistyped path resolves to its real, writable location instead of failing against a path the model never meant.</li></ul><h3 id="_9-3-no-op-and-the-repeat-guard" tabindex="-1">9.3 No-op and the repeat guard <a class="header-anchor" href="#_9-3-no-op-and-the-repeat-guard" aria-label="Permalink to &quot;9.3 No-op and the repeat guard&quot;">​</a></h3><p>An apply that produces text identical to the input is <code>EDIT_NO_CHANGE</code>, not success.</p><p>§4d&#39;s repeat guard still stops a prompt that keeps failing on one path, but it does not count every failure the same way. The three recoverable codes — <code>EDIT_TAG_MISMATCH</code>, <code>EDIT_TAG_UNKNOWN</code>, <code>EDIT_LINES_UNSEEN</code> — each hand back what the retry needs: the live tag, or the content of the lines the host refused to write blind (§9.1). One honest retry is the designed response to them, so each code gets <strong>one free attempt per path</strong> before it counts. Every other code — malformed ops, an invalid range, a no-op apply — counts on its first occurrence, because repeating one of those means the model is guessing.</p><table tabindex="0"><thead><tr><th>Sequence on one path within one prompt</th><th>Outcome</th></tr></thead><tbody><tr><td><code>EDIT_TAG_MISMATCH</code>, then <code>EDIT_LINES_UNSEEN</code></td><td>Neither counts: two different honest failures, each with its own grace</td></tr><tr><td><code>EDIT_TAG_MISMATCH</code> twice</td><td>The second counts as attempt 1</td></tr><tr><td><code>EDIT_PARSE_FAILED</code> three times</td><td>Attempt 3 — the turn stops</td></tr><tr><td>A failure, then a successful <code>Edit</code>, then a failure</td><td>Attempt 1 — a write that landed clears that path&#39;s history</td></tr></tbody></table><p>Counting a grace is per code, not per call, so a stale tag followed by unseen lines is two distinct honest failures while the same code twice is not.</p><p>When the count does reach the limit the tool result carries <code>terminate: true</code> and the agent loop stops after that batch. Stopping there must not leave a turn that merely ends: the runtime finalizes the assistant row with <code>MUTATION_RETRY_BUDGET_EXHAUSTED</code> — retriable, <code>details.kind</code> of <code>edit</code> or <code>patch-command</code>, plus the last error code and a class-specific <code>details.recovery</code> hint — and emits a matching error event, so the user sees that the agent stopped on purpose and keeps the continue affordance. For <code>EDIT_PARSE_FAILED</code>, the hint corrects the operation syntax without asking for another <code>Read</code>: a <code>PUT</code> with body rows must end its header with <code>:</code>, for example <code>PUT 48.=48:</code>. Stale-tag and unseen-line failures continue to direct the model to re-read or use the complete reveal. A terminated turn with no message is indistinguishable from a model that chose to say nothing.</p><h2 id="_10-drift-recovery" tabindex="-1">10. Drift recovery <a class="header-anchor" href="#_10-drift-recovery" aria-label="Permalink to &quot;10. Drift recovery&quot;">​</a></h2><p>Recovery proves that every anchor still maps to one unchanged region, then replays the ops against live content. It never edits the tagged snapshot and writes that.</p><ol><li>Look up the snapshot whose tag equals <code>expected</code>. Absent → decline.</li><li>Diff snapshot text against live text and build a map of <strong>unchanged</strong> lines only: <code>old_line → new_line</code>.</li><li>Every anchor must be present in the map. Missing → decline. For <code>cut</code> and span <code>paste</code>, <strong>every captured interior line</strong> is an anchor: changed interior content cannot be moved safely.</li><li>Validate each anchor&#39;s surrounding context: <ul><li>if the anchor&#39;s text is unique in both the old and the new file, at least one adjacent non-anchor line must map at the same offset;</li><li>if the text is duplicated on either side, <strong>both</strong> adjacent non-anchor lines must map at the same offset.</li></ul></li><li>All anchors must move by <strong>one consistent offset</strong>. Any divergence declines.</li><li>Replay against live content. An apply failure, or a replay that changes nothing, declines.</li></ol><p>Recovery warnings distinguish cause, because the corrective action differs:</p><table tabindex="0"><thead><tr><th>Warning</th><th>Condition</th></tr></thead><tbody><tr><td>external change</td><td>the tagged snapshot is still the head — something outside the session wrote the file</td></tr><tr><td>session chain</td><td>the tag names an older version — the model reused a stale tag from earlier in the session</td></tr><tr><td>line remap</td><td>anchors moved by a non-zero offset</td></tr></tbody></table><h2 id="_11-errors" tabindex="-1">11. Errors <a class="header-anchor" href="#_11-errors" aria-label="Permalink to &quot;11. Errors&quot;">​</a></h2><table tabindex="0"><thead><tr><th>code</th><th>retriable</th><th>meaning</th></tr></thead><tbody><tr><td><code>EDIT_TAG_REQUIRED</code></td><td>no</td><td><code>tag</code> missing or not 4 hex</td></tr><tr><td><code>EDIT_TAG_MISMATCH</code></td><td>yes after a <code>Read</code></td><td>tag does not hash the live file and recovery declined; carries the live tag and current content at the anchors</td></tr><tr><td><code>EDIT_TAG_UNKNOWN</code></td><td>yes after a <code>Read</code></td><td>tag is well-formed but this session recorded no such content for the path</td></tr><tr><td><code>EDIT_LINES_UNSEEN</code></td><td>yes</td><td>anchors reference lines never displayed; carries the revealed content</td></tr><tr><td><code>EDIT_PARSE_FAILED</code></td><td>no</td><td>malformed header, body row under a colonless header, missing body, <code>-</code>/context row</td></tr><tr><td><code>EDIT_RANGE_INVALID</code></td><td>no</td><td>reversed range, out-of-bounds line, overlapping ops, duplicate anchor</td></tr><tr><td><code>EDIT_BLOCK_UNRESOLVED</code></td><td>no</td><td><code>N*</code> could not resolve; names the plain-range alternative</td></tr><tr><td><code>EDIT_REGISTER_EMPTY</code></td><td>no</td><td>paste from an unset register</td></tr><tr><td><code>EDIT_REGISTER_AMBIGUOUS</code></td><td>no</td><td>anonymous paste with more than one pending anonymous capture</td></tr><tr><td><code>EDIT_REPAIR_AMBIGUOUS</code></td><td>no</td><td>boundary repair candidates tied at minimum cost</td></tr><tr><td><code>EDIT_NO_CHANGE</code></td><td>no</td><td>apply produced identical text</td></tr><tr><td><code>EDIT_AMPLIFICATION_LIMIT</code></td><td>no</td><td>lowering exceeded the expansion cap</td></tr></tbody></table><p>All of these are <code>Edit</code>-scoped and additive to <a href="./08-error-codes">08-error-codes</a> §3.4. Existing <code>INVALID_ARGUMENT</code>, <code>TOOL_DENIED</code>, <code>PATH_OUTSIDE_WORKSPACE</code>, and <code>TOOL_FAILED</code> semantics are unchanged; <code>Edit</code> no longer reports version or provenance problems as the generic <code>TOOL_FAILED</code>, because both are recoverable with a specific next action.</p><p><code>MUTATION_RETRY_BUDGET_EXHAUSTED</code> is not in this table because it is not an <code>Edit</code> result: the tool call already failed with one of the codes above, and the runtime adds that code to the assistant row it writes when the repeat guard ends the turn (§9.3, <a href="./08-error-codes">08-error-codes</a> §3.3).</p><p>Every error message names one concrete next step. &quot;Re-read and retry&quot; without a range is not an acceptable message when the host knows the range.</p><h2 id="_12-limits" tabindex="-1">12. Limits <a class="header-anchor" href="#_12-limits" aria-label="Permalink to &quot;12. Limits&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Limit</th><th>Value</th><th>Behavior at the limit</th></tr></thead><tbody><tr><td><code>ops</code> payload</td><td>256 KB</td><td><code>INVALID_ARGUMENT</code></td></tr><tr><td>Ops per call</td><td>200</td><td><code>INVALID_ARGUMENT</code></td></tr><tr><td>Expanded IR lines</td><td>100 000</td><td><code>EDIT_AMPLIFICATION_LIMIT</code></td></tr><tr><td>Unseen-line reveal</td><td>40 lines</td><td>truncate the reveal, merge nothing</td></tr><tr><td>Reveal line width</td><td>512 chars</td><td>clip, mark truncated, merge nothing</td></tr><tr><td>Register capture</td><td>4096 lines / 1 MiB</td><td><code>INVALID_ARGUMENT</code> on the <code>CUT</code></td></tr><tr><td>Named registers per session</td><td>16</td><td>LRU eviction of the coldest</td></tr><tr><td>Block resolution parse</td><td>4 MiB per file</td><td>decline the block op</td></tr><tr><td>Snapshot store</td><td>§4.4</td><td>LRU eviction</td></tr></tbody></table><h2 id="_13-interaction-with-existing-subsystems" tabindex="-1">13. Interaction with existing subsystems <a class="header-anchor" href="#_13-interaction-with-existing-subsystems" aria-label="Permalink to &quot;13. Interaction with existing subsystems&quot;">​</a></h2><h3 id="_13-1-review-snapshots-and-rollback-adr-0043" tabindex="-1">13.1 Review snapshots and rollback (ADR 0043) <a class="header-anchor" href="#_13-1-review-snapshots-and-rollback-adr-0043" aria-label="Permalink to &quot;13.1 Review snapshots and rollback (ADR 0043)&quot;">​</a></h3><p><code>review::prepare_change</code> keys on <code>tool_name</code> and <code>args.path</code> only, both of which survive unchanged, so pre-tool capture needs no modification for the common case. Two additions are required:</p><ul><li><code>MV DEST</code> produces a source deletion and a destination creation. It records as two review entries under one tool call, and rollback restores both or neither.</li><li><code>REM</code> records as a deletion whose rollback restores the captured bytes.</li></ul><p>Rollback&#39;s post-tool hash check is unaffected: it uses the full digest, not the 16-bit tag. After a rollback the session snapshot store must be invalidated for that path, or the model would hold a tag for content the rollback replaced.</p><h3 id="_13-2-single-path-edit" tabindex="-1">13.2 Single-path <code>Edit</code> <a class="header-anchor" href="#_13-2-single-path-edit" aria-label="Permalink to &quot;13.2 Single-path \`Edit\`&quot;">​</a></h3><p>The permission gate, review snapshot, artifacts row (<code>crates/host-core/src/rpc/mod.rs:2550</code>), and per-session mutation permit are all keyed on one <code>args.path</code>. <code>Edit</code> therefore stays single-path, and cross-file moves use session-scoped named registers across two calls (§7.5).</p><h3 id="_13-3-mutation-ordering" tabindex="-1">13.3 Mutation ordering <a class="header-anchor" href="#_13-3-mutation-ordering" aria-label="Permalink to &quot;13.3 Mutation ordering&quot;">​</a></h3><p>§4d&#39;s serialization is unchanged and becomes more important: the snapshot store is mutated by both producers and <code>Edit</code>, and two concurrent mutations on one session could interleave a record between a validation and its write. The existing per-session mutation permit already excludes that.</p><h3 id="_13-4-renderer" tabindex="-1">13.4 Renderer <a class="header-anchor" href="#_13-4-renderer" aria-label="Permalink to &quot;13.4 Renderer&quot;">​</a></h3><p>The Edit row renders from the review record&#39;s hunks — which ADR 0043 already produces — and shows the op headers verbatim as the model&#39;s stated intent. Resolved block spans and every warning from §8.4, §9.2, and §10 are surfaced on the row, not swallowed.</p><h3 id="_13-5-subagents" tabindex="-1">13.5 Subagents <a class="header-anchor" href="#_13-5-subagents" aria-label="Permalink to &quot;13.5 Subagents&quot;">​</a></h3><p>Subagents are separate sessions and therefore have separate snapshot stores and separate registers. A parent cannot hand a subagent a tag, and a subagent must <code>Read</code> before it edits. This is the correct default: provenance is per reader.</p><h3 id="_13-6-tool-descriptions-and-prompts" tabindex="-1">13.6 Tool descriptions and prompts <a class="header-anchor" href="#_13-6-tool-descriptions-and-prompts" aria-label="Permalink to &quot;13.6 Tool descriptions and prompts&quot;">​</a></h3><p><code>builtin_tool_defs()</code> in host-core and <code>buildToolDefinitions()</code> in <code>packages/agent-runtime/src/runtime.ts</code> must carry the same operation table, body-row rules, and anti-patterns. The model-facing guidance is part of the contract: the ops table, the <code>+</code>-only body rule, &quot;ranges name changed lines only&quot;, &quot;re-ground after every edit&quot;, and the worked anti-patterns (empty <code>PUT</code> used as a delete, range sized to post-edit content, <code>-</code>/context rows, widened <code>PUT</code> for a pure insertion, <code>&gt;N*</code> anchored on a closer, body rows under a register paste).</p><h2 id="_14-deliberate-deviations-from-oh-my-pi-s-hashline" tabindex="-1">14. Deliberate deviations from <code>oh-my-pi</code>&#39;s <code>hashline</code> <a class="header-anchor" href="#_14-deliberate-deviations-from-oh-my-pi-s-hashline" aria-label="Permalink to &quot;14. Deliberate deviations from \`oh-my-pi\`&#39;s \`hashline\`&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Deviation</th><th>Reason</th></tr></thead><tbody><tr><td>Single section per <code>Edit</code> call, not a multi-section patch</td><td>four PI-Desktop subsystems key on one <code>args.path</code> (§13.2); named registers already cover cross-file moves</td></tr><tr><td>SHA-256 low 16 bits instead of <code>xxHash32</code></td><td>reuses the primitive already in host-core; no new dependency</td></tr><tr><td>Snapshot store in-memory only, never persisted</td><td>a tag surviving a restart would outlive the reads that justified it</td></tr><tr><td>No <code>apply_patch</code> / <code>replace</code> fallback mode and no per-model exclusion list</td><td>ADR 0087 §1: one write contract</td></tr><tr><td>No internal URL schemes (<code>artifact://</code>, <code>xd://</code>, …) as <code>Edit</code> targets</td><td>out of scope; PI-Desktop&#39;s path rules stay as specified in §4 of 03-tools-and-permissions</td></tr><tr><td>64 paths / 8 MiB store bounds instead of 30 / 64 MiB</td><td>host-core is a long-lived desktop process holding many sessions, not a per-invocation CLI</td></tr></tbody></table><h2 id="_15-phasing" tabindex="-1">15. Phasing <a class="header-anchor" href="#_15-phasing" aria-label="Permalink to &quot;15. Phasing&quot;">​</a></h2><p>Each phase is independently shippable and leaves the contract coherent.</p><table tabindex="0"><thead><tr><th>Phase</th><th>Contents</th><th>Exit criterion</th></tr></thead><tbody><tr><td>1</td><td>Snapshot store, <code>session_id</code> threaded into <code>execute_tool_with_path_access</code>, tags on <code>Read</code>/<code>Grep</code>/<code>Write</code>, line-numbered <code>Read</code></td><td><strong>Shipped.</strong> tags round-trip</td></tr><tr><td>2</td><td>Ranges and gaps (<code>PUT N.=M:</code>, <code>PUT &lt;N:</code>, <code>PUT &gt;N:</code>, <code>PUT &gt;$:</code>, <code>CUT N.=M</code>), tag validation, provenance gate, <code>REM</code>/<code>MV</code></td><td><strong>Shipped.</strong> <code>old_string</code> removed; §9 branches A/B/D live</td></tr><tr><td>3</td><td>Drift recovery (§10) and path recovery (§9.2)</td><td>branch C live</td></tr><tr><td>4</td><td>tree-sitter block ops (<code>N*</code>, <code>&gt;N*</code>) with resolution echo</td><td>block ops decline cleanly on unsupported languages (<code>EDIT_BLOCK_UNRESOLVED</code> today)</td></tr><tr><td>5</td><td>Registers (§7.5) and boundary repair (§8.4)</td><td>named <code>CUT</code>/<code>PUT @name</code> capture; boundary-repair ties reject</td></tr></tbody></table><p>Phase 2 is the point of no return for the old contract and must ship with the renderer change in §13.4 and the prompt change in §13.6 in the same release.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("spec/03-runtime/18-line-anchored-edit-contract.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _18LineAnchoredEditContract = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _18LineAnchoredEditContract as default
};
