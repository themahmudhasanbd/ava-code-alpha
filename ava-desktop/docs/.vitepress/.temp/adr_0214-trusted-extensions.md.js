import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0214: Trusted extensions run in the Agent sidecar","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0214-trusted-extensions.md","filePath":"adr/0214-trusted-extensions.md","lastUpdated":1789036405000}');
const _sfc_main = { name: "adr/0214-trusted-extensions.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0214-trusted-extensions-run-in-the-agent-sidecar" tabindex="-1">ADR 0214: Trusted extensions run in the Agent sidecar <a class="header-anchor" href="#adr-0214-trusted-extensions-run-in-the-agent-sidecar" aria-label="Permalink to &quot;ADR 0214: Trusted extensions run in the Agent sidecar&quot;">​</a></h1><ul><li>Status: Accepted (v1 implemented 2026-09-10)</li><li>Date: 2026-09-10</li><li>Decision: D387</li><li>Related: ADR 0002, ADR 0008, D007, <code>07-plugins/16-trusted-extensions.md</code>, <code>07-plugins/01-plugin-system.md</code> §14</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>PI-Desktop&#39;s plugin system (ADR 0008) runs plugins in their own process behind a permission gateway. That is the right shape for distributed, untrusted code, but it leaves no surface for code that must sit on the agent loop itself: tools that execute in-process with the session&#39;s working directory, hooks on every turn, tool call, and provider request, and slash commands that carry session context. The desktop runtime in <code>packages/agent-runtime/src/runtime.ts</code> already exposes these hook points internally; nothing lets a user attach code to them.</p><p>The kernel packages <code>pi-ai</code> and <code>pi-agent-core</code> (ADR 0002) have a sibling, <code>pi-coding-agent</code>, that defines an <code>ExtensionAPI</code> contract and a loader for TypeScript extension modules. Adopting that contract gives PI-Desktop a second extension surface with an existing, documented API and an existing body of extensions written against it.</p><p>Three routes were evaluated: implement <code>ExtensionAPI</code> over the desktop runtime; express the same capabilities as a new plugin contribution type; or replace the desktop runtime with <code>pi-coding-agent</code>&#39;s <code>AgentSession</code>.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><strong>Trusted extensions are the second extension surface.</strong> The sidecar pins <code>pi-coding-agent</code> at the same version as the other two kernel packages as a types-only dependency, mirrors its discovery rules, loads modules with <code>jiti/static</code> and virtual modules, and implements <code>ExtensionAPI</code> on top of the desktop runtime&#39;s hook points in a desktop-owned Runner. One Runner per session. (The upstream <code>ExtensionRunner</code> binds the terminal theme and is not reused.)</li><li><strong>Trusted, opt-in, no auto-import.</strong> Extensions are labelled &quot;Trusted extension&quot;, run with sidecar trust, and are disabled until the user enables each one. D007 is unchanged: <code>~/.pi</code> is scanned for candidates, never imported. Project-scoped extensions are enabled per project.</li><li><strong>Explicit support classes.</strong> Every <code>ExtensionAPI</code> member is Supported, Deferred, or Unsupported. Unsupported members are inert and produce diagnostics; they never throw. Terminal-UI surfaces stay Unsupported.</li><li><strong>Plugins are untouched.</strong> No plugin manifest, permission, or process boundary changes. Extension tools and commands enter the same catalogs plugins use, after plugins, and lose name collisions.</li><li><strong>No host-core change in v1.</strong> Enablement is app settings; new traffic is sidecar-to-main proxy methods and Electron IPC only.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Users can attach in-process tools, hooks, and commands to the agent loop without a new app release.</li><li>Two extension surfaces coexist and are explained in user documentation as &quot;plugins&quot; (sandboxed) and &quot;trusted extensions&quot; (in-sidecar).</li><li>The three kernel packages move in lockstep; a contract test with sample extensions guards every upgrade.</li><li>The sidecar bundle must keep jiti loadable; this is verified first.</li><li>Module-level extension state is shared across sessions in one sidecar.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li>A new plugin contribution type for in-process hooks. Rejected: it would either punch through the plugin sandbox or duplicate <code>ExtensionAPI</code> under a second name with no existing extensions to run.</li><li>Replace the desktop runtime with <code>AgentSession</code>. Rejected for now: it discards the desktop&#39;s modes, plans, deferred tools, and subagent logic, and terminal-UI surfaces remain unusable regardless. Kept as a long-term option.</li><li>Run extensions inside the plugin host process. Rejected: the permission gateway would deny most of what extensions do, and they would fail at load.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0214-trusted-extensions.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0214TrustedExtensions = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0214TrustedExtensions as default
};
