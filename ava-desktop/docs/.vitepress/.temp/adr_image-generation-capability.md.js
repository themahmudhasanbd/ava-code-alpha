import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR: Image generation as a configured Agent capability","description":"","frontmatter":{},"headers":[],"relativePath":"adr/image-generation-capability.md","filePath":"adr/image-generation-capability.md","lastUpdated":1789965768000}');
const _sfc_main = { name: "adr/image-generation-capability.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-image-generation-as-a-configured-agent-capability" tabindex="-1">ADR: Image generation as a configured Agent capability <a class="header-anchor" href="#adr-image-generation-as-a-configured-agent-capability" aria-label="Permalink to &quot;ADR: Image generation as a configured Agent capability&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-21</li><li>Related: ADR 0281, ADR 0101, ADR 0172; <a href="./../spec/03-runtime/21-image-generation">image generation spec</a></li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The desktop understands image input and previews but has no image production path. Users need one selectable image model, conversational generation/editing, and batches without changing their default chat model. A Skill alone cannot execute image requests. Pure image endpoints cannot be treated as chat adapters.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Keep a separate optional settings binding referencing existing provider credentials. Expose one Agent tool plus a bundled, lazily loaded imagegen skill. Use an independent OpenAI Images adapter and batch scheduler in agent-runtime; the desktop service coordinates host-owned settings/credentials, contained source files and saved output. Renderer code handles configuration, previews and navigation only.</p><p>The existing local-tool shortcut does not perform host permission checks. For this billable tool, the bridge must obtain host-core authorization through <code>tools.execute</code> before invoking the local service. Host-core never performs the HTTP call; its result is authorization, while the sidecar result records actual generated files. Local tools gain an abort signal so this path can stop requests on cancellation, timeout and process loss. Existing tools retain their deadlines and behavior.</p><h2 id="alternatives-and-consequences" tabindex="-1">Alternatives and consequences <a class="header-anchor" href="#alternatives-and-consequences" aria-label="Permalink to &quot;Alternatives and consequences&quot;">​</a></h2><ul><li>An independent image workspace would duplicate conversation/history ownership; use existing chat and previews first.</li><li>A shell/API-only skill would bypass model settings and require scripts to handle credentials, files and retries. The skill instead calls the bounded host tool.</li><li>A broad multi-protocol framework is unnecessary for the requested single Images protocol. Generation and multipart editing share its adapter and scheduler.</li><li>Optional settings preserve existing stored data and conversation defaults. No schema migration, plugin API change or direct renderer credential access occurs.</li><li>Partial results remain useful; no automatic retry avoids duplicate paid work. Upstream model support and billing remain provider responsibilities.</li><li>Headless hosts without the desktop image runner do not perform image requests.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/image-generation-capability.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const imageGenerationCapability = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  imageGenerationCapability as default
};
