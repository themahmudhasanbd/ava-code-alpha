import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"03. Tools and Permissions","description":"","frontmatter":{},"headers":[],"relativePath":"spec/03-runtime/03-tools-and-permissions.md","filePath":"spec/03-runtime/03-tools-and-permissions.md","lastUpdated":1789965768000}');
const _sfc_main = { name: "spec/03-runtime/03-tools-and-permissions.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="_03-tools-and-permissions" tabindex="-1">03. Tools and Permissions <a class="header-anchor" href="#_03-tools-and-permissions" aria-label="Permalink to &quot;03. Tools and Permissions&quot;">​</a></h1><blockquote><p>Decisions applied: D003, D004, D005, D006, D013, D015, D093, D114, D115, D181, D186, D189, D190, D195 (ADR 0057), D315, D384 (ADR 0211), ADR 0087</p></blockquote><h2 id="_0-frozen-policy-summary" tabindex="-1">0. Frozen policy summary <a class="header-anchor" href="#_0-frozen-policy-summary" aria-label="Permalink to &quot;0. Frozen policy summary&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Topic</th><th>Decision</th></tr></thead><tbody><tr><td>Default mode</td><td>Agent</td></tr><tr><td>Agent tools</td><td>Read / Glob / Grep / Write / Edit / Bash + registered plugin tools</td></tr><tr><td>Plan tools</td><td>Read / Glob / Grep / BrowserPreview / Bash / SubmitPlan + plugin tools that declare plan-safe actions</td></tr><tr><td>Goal tools</td><td>Read / Glob / Grep / BrowserPreview / Bash / SubmitGoal + plugin tools that declare plan-safe actions</td></tr><tr><td>Plan and Goal hard deny</td><td>Write / Edit / plugin tools without <code>planSafeActions</code> / unknown tools / the other kind&#39;s submit tool</td></tr><tr><td>Plugin <code>planSafeActions</code></td><td>Non-empty array of <code>action</code> strings; runtime hides plugin tools without one in Plan/Goal, host admits listed tools, plugin-runtime rejects any action outside the list (ADR 0211)</td></tr><tr><td>Permission timeout</td><td>120s → deny</td></tr><tr><td>allow-session scope</td><td>toolName</td></tr><tr><td>Bash style</td><td>non-interactive; selected host catalog shell with streamed output</td></tr><tr><td>Edit contract</td><td>line-anchored ops + whole-file <code>tag</code>; no <code>old_string</code>/<code>new_string</code> (ADR 0087)</td></tr><tr><td>asktool</td><td>interactive multi-question tool; no validity deadline; skipped answers become empty output fields</td></tr></tbody></table><h2 id="_1-goal" tabindex="-1">1. Goal <a class="header-anchor" href="#_1-goal" aria-label="Permalink to &quot;1. Goal&quot;">​</a></h2><p>Let the agent get things done, but stay under control by default.</p><h2 id="_2-mvp-built-in-tools" tabindex="-1">2. MVP Built-in Tools <a class="header-anchor" href="#_2-mvp-built-in-tools" aria-label="Permalink to &quot;2. MVP Built-in Tools&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Tool</th><th>Risk</th><th>Description</th></tr></thead><tbody><tr><td><code>Read</code></td><td>low</td><td>Read files within the workspace; returns line-numbered content and a <code>[path#TAG]</code> header</td></tr><tr><td><code>new_context</code></td><td>low</td><td>Start a new context window at the next turn boundary; takes no parameters and changes no environment state</td></tr><tr><td><code>Glob</code></td><td>low</td><td>List files by pattern</td></tr><tr><td><code>Grep</code></td><td>low</td><td>Content search; uses system <code>rg</code> when installed, else in-process; mints a per-file <code>tag</code></td></tr><tr><td><code>BrowserPreview</code></td><td>low</td><td>Open a workspace-relative preview in the bundled Browser plugin (fails if <code>pi.browser</code> is disabled)</td></tr><tr><td><code>EnterPlanMode</code></td><td>low</td><td>Move the same Agent from Agent to Plan after host validation</td></tr><tr><td><code>SubmitPlan</code></td><td>low</td><td>Preserve exact Markdown bytes in a new <code>.pi/plan/*.md</code> artifact and request approval</td></tr><tr><td><code>EnterGoalMode</code></td><td>low</td><td>Move the same Agent from Agent to Goal after host validation</td></tr><tr><td><code>SubmitGoal</code></td><td>low</td><td>Preserve exact Markdown bytes in a new <code>.pi/goal/*.md</code> artifact and request approval</td></tr><tr><td><code>Write</code></td><td>high</td><td>Create/overwrite files; returns the post-write <code>tag</code></td></tr><tr><td><code>Edit</code></td><td>high</td><td>Modify files through line-anchored ops against a verified <code>tag</code> (<a href="./18-line-anchored-edit-contract">18</a>)</td></tr><tr><td><code>Bash</code></td><td>high</td><td>Execute commands</td></tr><tr><td><code>asktool</code></td><td>low</td><td>Ask one or more user questions and return the submitted answers as tool output</td></tr></tbody></table><blockquote><p>Names may be fine-tuned during implementation, but semantics stay consistent.</p></blockquote><h3 id="_2-1-deferred-ancillary-tools-d185-adr-0048" tabindex="-1">2.1 Deferred ancillary tools (D185, ADR 0048) <a class="header-anchor" href="#_2-1-deferred-ancillary-tools-d185-adr-0048" aria-label="Permalink to &quot;2.1 Deferred ancillary tools (D185, ADR 0048)&quot;">​</a></h3><p>Following pi&#39;s coding-agent default, the first Agent request activates only <code>Read</code>, <code>Bash</code>, <code>Edit</code>, and <code>Write</code>; <code>Glob</code> and <code>Grep</code> are loaded on demand. Plan and Goal keep their read/inspection core. <code>Skill</code> is deliberately not deferred: a <code>/skill-id</code> invocation instructs the model to call it, and a tool absent from the schema cannot be called at all, so it ships with the first request whenever the skill catalog is non-empty (D404, ADR 0230). The runtime also registers capabilities without sending their full schemas up front:</p><ul><li><code>Glob</code> and <code>Grep</code> in Agent mode</li><li><code>BrowserPreview</code></li><li><code>PluginCheck</code>, <code>PluginScaffold</code>, and <code>PluginPack</code></li><li>plugin-declared agent tools</li></ul><p>These tools appear in a bounded <code># On-demand tools</code> catalog with compact descriptions. The model calls the local <code>ToolSearch</code> tool with an exact name or capability query; the matching schemas become available on the next model turn. At the beginning of every new user prompt, the sidecar clears the in-memory deferred set and restores only successful activation evidence from the effective session context: <code>addedToolNames</code> on successful <code>ToolSearch</code> results and the names of successful deferred-tool results. Failed rows, interrupted or missing result placeholders, and assistant/user prose are ignored. Restored names must still be in the current mode&#39;s deferred catalog. The host permission, workspace/scratch containment, timeout, and audit rules do not change when a tool is loaded. <code>ToolSearch</code> itself never executes a workspace operation and never bypasses host-core policy.</p><h2 id="_3-common-tool-constraints" tabindex="-1">3. Common Tool Constraints <a class="header-anchor" href="#_3-common-tool-constraints" aria-label="Permalink to &quot;3. Common Tool Constraints&quot;">​</a></h2><p>Every non-interactive execution tool must have:</p><ol><li>JSON schema / typebox parameter definition</li><li>timeout</li><li>workspace path validation</li><li>output truncation policy</li><li>trace id</li><li>structured results</li></ol><p><code>asktool</code> is the interactive exception: it has a typed request event, waits for the renderer response without an expiry, and returns a bounded structured tool result. Stopping the turn resolves outstanding questions as skipped.</p><h2 id="_4-path-rules" tabindex="-1">4. Path Rules <a class="header-anchor" href="#_4-path-rules" aria-label="Permalink to &quot;4. Path Rules&quot;">​</a></h2><p>Native file and search tools enforce distinct path shapes (D208, ADR 0069):</p><ul><li><code>Read.path</code> is an existing regular file. A directory returns <code>INVALID_ARGUMENT</code> with a structured <code>Glob</code> suggestion rather than a generic execution failure.</li><li><code>Glob.path</code> is a directory search root.</li><li><code>Grep.path</code> may be one file or a directory tree. A directly named file is searched without walking siblings, while <code>include</code> still filters its base name and every output budget remains unchanged. Grep prefers a user-installed <code>rg</code> on PATH (and the Unix login PATH) and falls back to the in-process searcher when <code>rg</code> is missing or fails (D315). The model-facing contract does not change.</li></ul><p>Workspace-relative paths in tool results use <code>/</code> for platform separators. On POSIX, a literal backslash in a filename remains a backslash so the result can be passed back to <code>Read</code> or <code>Edit</code>; Windows path separators are normalized to <code>/</code>.</p><p>Agent mode keeps <code>Glob</code>/<code>Grep</code> deferred under D185. Each new user prompt clears their live activation and restores only eligible successful markers still in context; when no such marker exists, directory discovery activates <code>Glob</code> through <code>ToolSearch</code> for that prompt instead of guessing a file name or calling <code>Read</code> on a directory.</p><p>The runtime accepts one alias per canonical argument name and folds it away before the host sees the call (D273):</p><table tabindex="0"><thead><tr><th>Tool</th><th>Canonical</th><th>Accepted alias</th></tr></thead><tbody><tr><td><code>Read</code> / <code>Write</code> / <code>Edit</code> / <code>BrowserPreview</code></td><td><code>path</code></td><td><code>file_path</code></td></tr><tr><td><code>Glob</code> / <code>Grep</code></td><td><code>pattern</code></td><td><code>query</code></td></tr></tbody></table><p>Both spellings are optional in the schema and the runtime requires exactly one; a call naming neither fails with <code>INVALID_ARGUMENT</code>. When a call carries both, the canonical name wins. <code>Bash.timeout</code> accepts up to 100000000 in the schema so a millisecond value validates: a value above the honoured 21,600-second ceiling is read as milliseconds and converted to seconds, then clamped to 21,600 seconds (D273 / D329). In-range values, including 600 and 1800, are seconds.</p><ul><li>For a durable <code>sessionId</code>, <code>workspaceRoot</code> is resolved from that session&#39;s persisted project binding. A path-less temporary session instead binds its <code>workspaceRoot</code> to <code>&lt;data_dir&gt;/scratch/&lt;sessionId&gt;</code>. Neither root is read from the mutable active sidebar tab at execution time.</li><li>All file paths are relative to the resolved <code>workspaceRoot</code> by default</li><li>After normalization they must still reside within the workspace unless the call receives explicit outside-path permission</li><li><code>..</code> escapes and symlink escapes are outside-path requests, not implicit access</li><li>Symlinks are resolved again immediately before execution, after permission approval, so approval cannot skip the canonicalization step</li><li>Exception (D114): absolute paths inside the session scratch directory are a second legal root for <code>Read</code>/<code>Write</code>/<code>Edit</code> — see §4b. Both roots run the same lexical + symlink containment defense. <code>Read</code>/<code>Glob</code>/<code>Grep</code>/<code>Write</code>/<code>Edit</code> may address an explicit path outside both roots only through the permission policy below; a denied or unapproved request returns <code>TOOL_DENIED</code>.</li></ul><h3 id="_4a-explicit-outside-path-permission" tabindex="-1">4a. Explicit outside-path permission <a class="header-anchor" href="#_4a-explicit-outside-path-permission" aria-label="Permalink to &quot;4a. Explicit outside-path permission&quot;">​</a></h3><p>An explicit <code>path</code> argument that resolves outside the session workspace and scratch roots is a separate capability. The host checks it before the normal low-risk auto-allow decision:</p><ul><li><code>auto</code> allows the outside path without a card;</li><li><code>ask</code> and <code>accept-edits</code> emit the ordinary permission card;</li><li><code>allow-once</code> executes only the current call, while <code>allow-session</code> follows the existing per-tool session grant scope;</li><li>denial, timeout, or cancellation never executes the operation;</li><li>relative <code>..</code> escapes and symlink escapes use the same rule as absolute paths;</li><li>successful external <code>Read</code>/<code>Write</code>/<code>Edit</code> results carry <code>root: &quot;external&quot;</code> and an absolute canonical path; external <code>Glob</code>/<code>Grep</code> matches are absolute so the access remains visible in the transcript.</li></ul><p>The exception applies only to the explicit path argument. It does not expand the workspace root, Bash&#39;s working directory, or any implicit directory walk.</p><h2 id="_4b-session-scratch-directory-d114" tabindex="-1">4b. Session scratch directory (D114) <a class="header-anchor" href="#_4b-session-scratch-directory-d114" aria-label="Permalink to &quot;4b. Session scratch directory (D114)&quot;">​</a></h2><p>Temporary/intermediate files an agent produces (one-off scripts, downloaded data, drafts) must not dirty the user&#39;s project or its git status. Each session gets a scratch directory outside the workspace:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&lt;data_dir&gt;/scratch/&lt;sessionId&gt;/</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>OS clipboard files and images pasted into the composer are materialized by Electron main below <code>&lt;data_dir&gt;/scratch/&lt;sessionId&gt;/pasted/</code> before their paths and metadata are captured as transient composer references. At dispatch, main validates the source against the session scratch/project roots. Images are also copied into the content-addressed <code>&lt;data_dir&gt;/attachments/&lt;sha256&gt;</code> store; the effective model capability applies the published image input plus the configured binding&#39;s <code>supportsImages</code> override. Eligible images receive image blocks when that effective capability is enabled, while unknown/custom models without an explicit override, disabled image input, and oversized images receive <code>@</code> fallback paths. They use the same session lifecycle as other scratch data and do not enter the workspace, artifacts, or the persisted prompt as binary content.</p><ul><li><strong>Addressing.</strong> In a project session, the model addresses scratch by absolute path only; the path is advertised in the system prompt, relative tool paths resolve against the project workspace, and <code>Bash</code> exports <code>PI_SCRATCH_DIR</code>. In a temporary session, that same scratch directory is the session workspace root, so relative Read/Glob/Grep/Write/Edit/Bash paths work there without inheriting a project.</li><li><strong>Containment.</strong> <code>resolve_tool_path</code> tries the workspace root first, then the scratch root, applying the identical two-layer defense (lexical <code>..</code> normalization + canonicalized-ancestor symlink check) to each. A symlink planted inside scratch cannot reach the workspace or anywhere else.</li><li><strong>Permissions.</strong> <code>Write</code>/<code>Edit</code> whose <code>path</code> is lexically inside the session&#39;s scratch root auto-allow without a permission card — they cannot touch the project. The lexical check only skips the prompt; execution still goes through the full resolver, so it is not an escape vector. Plan and Goal do not expose Write/Edit, so the scratch auto-allow rule cannot make those tools available in either. A contract-mode Bash call may still create or mutate scratch data when its permission mode allows it.</li><li><strong>Artifacts.</strong> Successful scratch writes are not recorded in the <code>artifacts</code> table; artifact-driven file tabs represent workspace deliverables only, while the Files surface may still browse the active workspace. Tool results carry <code>root: &quot;workspace&quot; | &quot;scratch&quot;</code> to make this decision and the UI rendering explicit.</li><li><strong>Tool coverage.</strong> <code>Read</code>/<code>Write</code>/<code>Edit</code> use the workspace and scratch roots; <code>Glob</code>/<code>Grep</code> use the workspace root by default and may search an explicitly scoped scratch directory or an explicitly approved external directory. The model should use bounded native search tools instead of shell directory walks. <code>BrowserPreview</code> remains workspace-relative in v1. Its Main-process handler resolves the root from the originating durable session, and the renderer event carries <code>sessionId</code>; the selected foreground workspace is never used for a background preview.</li><li><strong>Lifecycle.</strong> Created lazily on the first <code>Write</code>/<code>Edit</code>/<code>Bash</code> or composer clipboard paste of a session. Deleted with <code>session.delete</code>. A startup sweep removes scratch dirs whose session no longer exists and dirs untouched for over 7 days (crash/force-quit fallback; no scheduled job needed).</li><li>A project switch does not redirect or cancel a background session&#39;s tools; sessions A and B remain sandboxed to projects A and B respectively.</li><li>A Temporary/path-less session uses only its own scratch directory as its workspace root, even if another project is visible or recently active. It never inherits that project. Plan and Goal still require a persisted project root, so this binding does not expand contract-mode execution. High-risk tools operate only inside the temporary session&#39;s scratch root.</li><li>Legacy calls that do not resolve to a durable session may use the selected host workspace only during the compatibility window.</li><li>A session lookup/storage error fails the tool request; it must never be treated as a missing legacy session or redirected to the selected workspace.</li></ul><h2 id="_4c-message-owned-review-snapshots-and-rollback" tabindex="-1">4c. Message-owned review snapshots and rollback <a class="header-anchor" href="#_4c-message-owned-review-snapshots-and-rollback" aria-label="Permalink to &quot;4c. Message-owned review snapshots and rollback&quot;">​</a></h2><p><code>Write</code> and <code>Edit</code> are the structured review boundary. For a successful workspace-root mutation, host-core captures the previous file before execution and adds bounded review evidence to the tool result:</p><div class="language-ts vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">type</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> ReviewChange</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  version</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  snapshotId</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  messageId</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  path</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  operation</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;write&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;edit&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;delete&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  status</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;added&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;modified&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;deleted&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  state</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;active&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;rolledBack&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  additions</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> number</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  deletions</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> number</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  hunks</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> DiffHunk</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">[];</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  binary</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> boolean</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  truncated</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> boolean</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  reversible</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> boolean</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">};</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br></div></div><ul><li>The renderer persists and displays this record with the tool message; it does not recompute Review from Git, <code>HEAD</code>, or the current dirty tree.</li><li>Scratch-root, failed, denied, and unresolvable writes have no <code>review</code> record. Binary or oversized content may omit hunks and be non-reversible.</li><li>Rollback is host-owned and hash-guarded. It restores the captured previous bytes, or removes a newly-created file, only when the current content still equals the post-tool hash. A later edit returns <code>conflict</code> without touching the file. A completed rollback invalidates the session&#39;s snapshot entries for that path, so the model cannot keep editing against a tag the rollback replaced.</li><li>An <code>Edit</code> carrying <code>MV DEST</code> records two entries under one tool call — a source deletion and a destination creation — and rollback restores both or neither. <code>REM</code> records a deletion whose rollback restores the captured bytes. The hash guard uses the full digest, not the 16-bit <code>tag</code>.</li><li>Review snapshot files live outside the workspace and are removed with their session; orphaned session directories are swept on host startup.</li></ul><h2 id="_4d-mutation-ordering-and-edit-recovery" tabindex="-1">4d. Mutation ordering and edit recovery <a class="header-anchor" href="#_4d-mutation-ordering-and-edit-recovery" aria-label="Permalink to &quot;4d. Mutation ordering and edit recovery&quot;">​</a></h2><p><code>Write</code> and <code>Edit</code> are serialized within each session. Read/search tools may continue in parallel, and different sessions may mutate different roots concurrently, but a session never has two in-flight mutations. The host holds the per-session mutation permit before consuming a global mutation slot, so a queued mutation cannot reserve capacity while it waits for an earlier edit.</p><p><code>Edit</code> names positions and supplies new content only; it never matches existing text. Every call carries the whole-file <code>tag</code> minted by whichever tool last displayed the content, and the host rejects a call whose tag does not hash the live file or whose anchors reference lines this session never displayed. The full contract — tag computation, the session snapshot store, the op grammar, block resolution, registers, and drift recovery — is <a href="./18-line-anchored-edit-contract">18-line-anchored-edit-contract</a>; this section keeps only the ordering and loop-guard rules. The agent mutation workflow is:</p><ol><li>Edit the deliverable directly with <code>Edit</code> or <code>Write</code> when it is inside the advertised workspace.</li><li>If a dedicated worktree is outside that root, perform one guarded edit in that worktree with Bash and verify the resulting diff.</li><li>Classify a failed edit before recovering. For a stale tag or unseen lines, perform one fresh <code>Read</code> of the current target and regenerate the change once (a complete <code>EDIT_LINES_UNSEEN</code> reveal may be retried unchanged). For a deterministic syntax or range error such as <code>EDIT_PARSE_FAILED</code>, correct the operation payload directly; another <code>Read</code> does not repair malformed syntax. A body-bearing replacement must use a header such as <code>PUT 48.=48:</code>. After three counted failures on one path in a prompt (18-line-anchored-edit-contract §9.3), the third counted failed <code>Edit</code> for that path — or the third failed shell patch command (<code>apply_patch</code>, <code>git apply</code>, or <code>patch</code>) — returns a terminating tool result with an error-specific recovery hint, so the agent stops after reporting the exact mismatch. Do not hand-edit old unified-diff hunk headers or continue a repair loop.</li><li>Keep mutations to one path sequential, even when read/search calls are issued in parallel.</li></ol><p>An <code>EDIT_LINES_UNSEEN</code> rejection whose reveal was complete is exempt from step 3&#39;s re-read: the error already displayed the missing lines and merged them into the session&#39;s provenance, so the same <code>tag</code> retried unchanged applies. That retry is also the one grace <code>EDIT_LINES_UNSEEN</code> gets on the path, so a second one does count toward the guard.</p><p>Serialization also protects the snapshot store, which both producers and <code>Edit</code> mutate: without the per-session permit, a concurrent record could land between a validation and its write.</p><h2 id="_5-bash-rules" tabindex="-1">5. Bash Rules <a class="header-anchor" href="#_5-bash-rules" aria-label="Permalink to &quot;5. Bash Rules&quot;">​</a></h2><p>Host execution baseline:</p><ul><li>A project-bound session workspace is required</li><li>Default cwd = the originating session&#39;s <code>workspaceRoot</code></li><li>Confirmation required by default</li><li>Set a mandatory 60s timeout; accept a 1s–21,600s override (D329)</li><li>Stream stdout and stderr separately, then return bounded final output</li><li>Truncate large output without mixing the two streams</li><li>No interactive TTY (MVP)</li><li>A command that exits non-zero returns <code>ok: false</code>, <code>isError: true</code>, and <code>errorCode: TOOL_FAILED</code> while preserving its <code>exitCode</code>, stdout, and stderr in <code>content</code> so the agent can diagnose the command without blindly retrying.</li></ul><p>Shell catalog (D190) exposes the stable IDs <code>windows-powershell</code>, <code>windows-pwsh</code>, <code>cmd</code>, <code>git-bash</code>, and <code>bash</code> where supported by the platform. The host persists <code>defaultCommandShell</code>; if that persisted choice later becomes unavailable, the effective catalog selection intentionally falls back to the first available platform shell. A turn pins the effective shell ID and dialect. <code>Bash</code> remains the tool/protocol name, and the request carries the pinned shell ID separately. Host-core resolves the entry again before spawn and rejects a changed ID/dialect with <code>COMMAND_SHELL_CHANGED</code>; settings writes reject unavailable or wrong-platform IDs with <code>COMMAND_SHELL_INVALID</code>. No arbitrary executable path or executable path hash is accepted as shell identity.</p><ol><li><code>PI_DESKTOP_BASH</code> env override (path to a bash executable)</li><li>Unix: well-known locations (<code>/bin/bash</code>, <code>/usr/bin/bash</code>, <code>/usr/local/bin/bash</code>, Homebrew), then PATH</li><li>Windows: <code>bash.exe</code> from Git for Windows — derived from the <code>git</code> on PATH, then standard install dirs, then PATH excluding the WSL launcher in <code>System32</code></li></ol><ul><li>Unix invokes <code>bash -lc</code> (login shell keeps profile PATH for Finder/Dock launches); Windows invokes <code>bash -c</code> with <code>CREATE_NO_WINDOW</code></li><li>On Unix, the Bash tool additionally probes the user&#39;s login shell for its PATH — <code>$SHELL</code> (fallback <code>/bin/zsh</code> → <code>/bin/bash</code> → <code>/bin/sh</code>) with <code>-lic &#39;printf %s &quot;$PATH&quot;&#39;</code>, 5s-bounded, cached per process — and injects it into every subprocess. <code>bash -lc</code> alone sources only the <em>bash</em> profile; on macOS the default shell is zsh, so nvm/pnpm/Homebrew initialized in <code>~/.zshrc</code> / <code>~/.zprofile</code> would otherwise be invisible to agent commands. The probe is best-effort: missing shell, non-zero exit, or timeout fall back to the host PATH unchanged. Agent commands stay POSIX bash (D181 / ADR 0045). Electron main applies the same lookup to stdio MCP spawn (<code>uvx</code>/<code>npx</code> from the market) so a Finder/Dock launch can find them (D600 / issue #571).</li><li>No bash bundled in the installer: Git for Windows is the Windows prerequisite (the app requires git anyway)</li><li>Resolution failure returns stable <code>SHELL_NOT_FOUND</code> with install guidance</li><li>Windows PowerShell and cmd use their native non-interactive invocation.</li><li>PowerShell 7 resolves <code>pwsh.exe</code> from <code>%ProgramFiles%\\PowerShell\\7</code> (or <code>ProgramW6432</code> when the host process is 32-bit), then PATH, which covers machine-scope, Store, user-scope, and portable installs. The PATH probe treats Microsoft Store / MSIX app-execution aliases under <code>WindowsApps</code> (<code>IO_REPARSE_TAG_APPEXECLINK</code>) as present executables, because winget&#39;s default PowerShell 7 layout is an <code>msixbundle</code> that does not create <code>%ProgramFiles%\\PowerShell\\7</code>. It shares the Windows PowerShell 5.1 invocation contract and is never selected implicitly, so it cannot change an existing user&#39;s default shell. Resolution failure returns <code>SHELL_NOT_FOUND</code> naming the locations that were searched.</li><li>Git Bash uses the discovered Git for Windows executable.</li><li>Unix Bash uses an approved system Bash entry.</li><li>User abort and timeout terminate the complete process tree before returning.</li></ul><p>Initial denylist (extensible):</p><ul><li>Directly reading/writing sensitive paths outside the workspace</li><li>Destructive operations without confirmation (policy governed by the permission layer)</li></ul><h2 id="_6-permission-model" tabindex="-1">6. Permission Model <a class="header-anchor" href="#_6-permission-model" aria-label="Permalink to &quot;6. Permission Model&quot;">​</a></h2><h3 id="risk-levels" tabindex="-1">Risk Levels <a class="header-anchor" href="#risk-levels" aria-label="Permalink to &quot;Risk Levels&quot;">​</a></h3><table tabindex="0"><thead><tr><th>risk</th><th>Example</th><th>Default policy</th></tr></thead><tbody><tr><td>low</td><td>Read/Glob/Grep inside the session roots</td><td>Auto-allow</td></tr><tr><td>medium</td><td>low-risk network/metadata</td><td>Confirm or allow by policy</td></tr><tr><td>high</td><td>Write/Edit/Bash</td><td>Confirm by default</td></tr></tbody></table><h3 id="decision-types" tabindex="-1">Decision Types <a class="header-anchor" href="#decision-types" aria-label="Permalink to &quot;Decision Types&quot;">​</a></h3><ul><li><code>allow-once</code></li><li><code>allow-session</code></li><li><code>deny</code></li></ul><p>May be added later:</p><ul><li><code>allow-always-for-tool</code></li><li><code>allow-always-for-command-pattern</code></li></ul><h3 id="permission-modes-d115-d132" tabindex="-1">Permission Modes (D115/D132) <a class="header-anchor" href="#permission-modes-d115-d132" aria-label="Permalink to &quot;Permission Modes (D115/D132)&quot;">​</a></h3><p>How high-risk tool calls get approved is governed by a <strong>permission mode</strong>:</p><table tabindex="0"><thead><tr><th>Mode</th><th>Write/Edit</th><th>Bash / plugin tools</th></tr></thead><tbody><tr><td><code>ask</code> (default)</td><td>confirm</td><td>confirm</td></tr><tr><td><code>accept-edits</code></td><td>auto-allow</td><td>confirm</td></tr><tr><td><code>auto</code></td><td>auto-allow</td><td>auto-allow</td></tr></tbody></table><p>An explicit outside-workspace path is an exception to the low-risk row: it is auto-allowed only in <code>auto</code>; <code>ask</code> and <code>accept-edits</code> both confirm it.</p><p>Resolution order per tool call (host-core <code>tools.execute</code>):</p><ol><li>Session&#39;s persisted <code>permission_mode</code>, unless it is <code>inherit</code></li><li>Global <code>defaultPermissionMode</code> from app settings (<code>ask</code> / <code>accept-edits</code> / <code>auto</code>)</li><li><code>ask</code></li></ol><p>Rules:</p><ul><li>The session value is stored in <code>sessions.permission_mode</code> (<code>inherit | ask | accept-edits | auto</code>, default <code>inherit</code>, schema v5) and set via <code>session.configure</code> <code>permissionMode</code>.</li><li>Plan&#39;s hard deny wins over every permission mode for Write/Edit and plugin tools that lack <code>planSafeActions</code>. <code>auto</code> cannot re-enable a hidden or denied tool.</li><li>Low-risk tools (<code>Read</code>/<code>Glob</code>/<code>Grep</code>) inside the session roots auto-allow in every mode, as before.</li><li><code>BrowserPreview</code> is an explicit read-only UI inspection capability and is available in both operating modes.</li><li>Plan retains the permission-mode selector. Bash is confirmed under <code>ask</code> and <code>accept-edits</code>, and is auto-allowed under <code>auto</code>; therefore Plan is planning intent, not a strict read-only security profile.</li><li><code>allow-session</code> grants continue to work under <code>ask</code> and stay scoped to the session; under <code>accept-edits</code>/<code>auto</code> they are simply never needed.</li><li>Scratch-directory writes (D114) stay prompt-free in every mode.</li><li>UI: Settings → segmented global default; composer shows a per-session chip in Agent, Plan, and Goal whose menu offers the three effective modes without a separate global-default/inherit entry. The chip and selected menu item display the effective mode; choosing an item stores that explicit session override. Existing inherited sessions continue to resolve through the global setting until the user chooses a mode.</li><li>Enforcement lives in host-core only; the sidecar/model is never told the mode and cannot influence it.</li></ul><h2 id="_7-permission-flow" tabindex="-1">7. Permission Flow <a class="header-anchor" href="#_7-permission-flow" aria-label="Permalink to &quot;7. Permission Flow&quot;">​</a></h2><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>tool call</span></span>
<span class="line"><span> → policy.evaluate()</span></span>
<span class="line"><span> → allow? execute</span></span>
<span class="line"><span> → need confirm? push to UI</span></span>
<span class="line"><span> → deny? return tool error result</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>Permission confirmation timeout:</p><ul><li>After 120s, auto-deny (D005: fail closed, do not hang forever)</li></ul><h2 id="_8-tool-result-visibility-to-the-model" tabindex="-1">8. Tool Result Visibility to the Model <a class="header-anchor" href="#_8-tool-result-visibility-to-the-model" aria-label="Permalink to &quot;8. Tool Result Visibility to the Model&quot;">​</a></h2><ul><li>Success result: given to the model</li><li>Failure result: given to the model (with error info)</li><li>User denial: give the model an explicit &quot;user denied permission&quot;</li><li>Sensitive info: redact before persisting/displaying</li></ul><h2 id="_9-auditing" tabindex="-1">9. Auditing <a class="header-anchor" href="#_9-auditing" aria-label="Permalink to &quot;9. Auditing&quot;">​</a></h2><p>Each tool call records:</p><ul><li>sessionId</li><li>turnId</li><li>toolCallId</li><li>toolName</li><li>args hash / preview</li><li>externalPathPermission classification when an explicit path is present</li><li>decision</li><li>duration</li><li>success / error code</li></ul><p>MVP may start by writing to SQLite or a log file.</p><p>Audit rows may retain the existing segmented timing fields for forensic inspection: <code>prompted</code> (whether a permission card was shown), <code>permissionWaitMs</code>, <code>durationMs</code> (the tool body), <code>overheadMs</code> (host bookkeeping), and <code>totalMs</code>. Denied calls carry the same fields with a zero tool body. These are structured audit fields; they are not emitted as process-log timing lines. See <a href="./09-logging-and-observability">09. Logging and Observability</a> for the current key-log policy.</p><h2 id="_10-operating-mode-matrix" tabindex="-1">10. Operating-mode matrix <a class="header-anchor" href="#_10-operating-mode-matrix" aria-label="Permalink to &quot;10. Operating-mode matrix&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Mode</th><th>Read/Glob/Grep</th><th>BrowserPreview</th><th>Write/Edit</th><th>Bash</th><th>Plugins</th></tr></thead><tbody><tr><td>Agent</td><td>allow</td><td>allow</td><td>permission policy</td><td>permission policy</td><td>registered risk policy</td></tr><tr><td>Plan</td><td>allow</td><td>allow</td><td>deny</td><td><code>ask</code>/<code>accept-edits</code>: confirm; <code>auto</code>: allow</td><td>plan-safe actions only</td></tr><tr><td>Goal</td><td>allow</td><td>allow</td><td>deny</td><td><code>ask</code>/<code>accept-edits</code>: confirm; <code>auto</code>: allow</td><td>plan-safe actions only</td></tr></tbody></table><h3 id="notes" tabindex="-1">Notes <a class="header-anchor" href="#notes" aria-label="Permalink to &quot;Notes&quot;">​</a></h3><ul><li>Plan and Goal hard-deny Write/Edit and plugin tools without <code>planSafeActions</code> before permission UI; a direct host call cannot bypass the matrix. Plugin tools that declare a non-empty list are admitted; the runner still rejects any action outside that list (ADR 0211).</li><li>Agent mode uses permission cards or the selected automatic policy for Write/Edit/Bash and registered plugin tools.</li><li>Plan and Goal Bash may mutate workspace or scratch state when the user selected Auto; the UI must make that tradeoff visible.</li><li>allow-session is remembered per toolName for the active session only</li><li>Session grants follow <code>sessionId</code> across project-tab switches and are never inherited by another session or Temporary conversation</li></ul><h3 id="_10-1-plan-and-goal-control-tools" tabindex="-1">10.1 Plan and Goal control tools <a class="header-anchor" href="#_10-1-plan-and-goal-control-tools" aria-label="Permalink to &quot;10.1 Plan and Goal control tools&quot;">​</a></h3><p><code>new_context</code> is available in every mode and needs no confirmation: it only asks the runtime to compact at the next turn boundary, which the host would do on its own once the hard budget is reached (see <a href="./02-agent-runtime">02-agent-runtime</a> §5.1). A submit tool is available only in its own contract mode and must be the only tool call in its assistant batch. It preserves the exact Markdown bytes in a new unique artifact under the kind&#39;s directory (<code>.pi/plan/*.md</code> for <code>SubmitPlan</code>, <code>.pi/goal/*.md</code> for <code>SubmitGoal</code>) through host-core before creating one pending approval. <code>EnterPlanMode</code> and <code>EnterGoalMode</code> are available only in Agent, and each must be the only tool call in its batch. The host validates the durable mode, the proposal kind, and the active-turn/configuration boundary before any transition; the visible tool list is guidance, not the security boundary.</p><h3 id="_10-2-delegation-and-subagent-tool-scope-d201-adr-0062" tabindex="-1">10.2 Delegation and subagent tool scope (D201, ADR 0062) <a class="header-anchor" href="#_10-2-delegation-and-subagent-tool-scope-d201-adr-0062" aria-label="Permalink to &quot;10.2 Delegation and subagent tool scope (D201, ADR 0062)&quot;">​</a></h3><p><code>Task</code> is available in Agent mode only, and only when the session has at least one subagent definition. Plan and Goal are read-only contract negotiations, so a delegate with <code>Bash</code>, <code>Edit</code> or <code>Write</code> would drive straight through them.</p><p>A definition declares the tools its delegate may call. By default those names are drawn only from the seven working tools <code>Read</code>, <code>Glob</code>, <code>Grep</code>, <code>BrowserPreview</code>, <code>Bash</code>, <code>Edit</code> and <code>Write</code>. A definition that declares none gets <code>Read</code>, <code>Glob</code>, <code>Grep</code>; <code>tools: &quot;*&quot;</code> means all seven working tools. An unrecognized name — including the withdrawn <code>A2A</code> and <code>Peer</code> tools (D326 / ADR 0165) — is dropped with a parse warning.</p><p>A document may opt into the parent session&#39;s live tool catalog with <code>tools: inherit</code> or <code>tools: [inherit, Bash]</code> (ADR 0246 / D415). At <code>Task</code> spawn the runtime unions <code>toolCatalog</code> keys (including deferred plugin/MCP tools) with any assignable extras, then drops <code>Task</code> / <code>TaskWait</code> / <code>TaskList</code> / <code>TaskStop</code>, <code>EnterPlanMode</code> / <code>EnterGoalMode</code>, <code>asktool</code>, <code>new_context</code>, and <code>ToolSearch</code>. Builtins do not opt in. <code>inherit</code> is visible in the Markdown and in Settings; host-core keeps the token so an inherit-only document still loads. Plugin tools, <code>Skill</code>, and MCP tools are therefore available to a delegate only through this opt-in, never by putting those names on the assignable whitelist.</p><p>Without <code>tools: inherit</code>, a delegate&#39;s available tools are its definition&#39;s, never its session&#39;s: it cannot gain a tool because the parent has it, and a session cannot lend mutation rights to a read-only delegate. Delegate calls are built by the session runtime and go through the same <code>tools.execute</code> path, so path rules (§4), Bash rules (§5), permission modes (§6), the operating-mode matrix (§10) and auditing (§9) apply unchanged — evaluated against the owning session.</p><p>A <strong>builtin or user</strong> definition may additionally declare <code>permission: inherit | ask | accept-edits | auto</code> (ADR 0089, default <code>inherit</code>). With the default <code>inherit</code> (including all builtins), the sidecar attaches no override and the delegate uses the session&#39;s effective permission mode. Thus a parent in <code>auto</code> allows the delegate&#39;s explicit external paths without a second authorization card. A project definition may not declare a scope: it arrives with the repository, so its declaration is dropped at parse time with a warning and its delegates resolve under the session&#39;s effective mode — cloning a repository never grants it a permission upgrade. When an eligible builtin or user explicitly declares a non-<code>inherit</code> scope, the sidecar attaches it to the delegate&#39;s <code>tools.execute</code> calls and host-core resolves each call under that mode instead of the session&#39;s effective permission mode. The scope is a permission-mode override only: the contract modes&#39; hard deny and the external-path gate (§4.1) stay in force. <code>accept-edits</code> therefore auto-allows <code>Write</code>/<code>Edit</code> inside the workspace and scratch roots, while external paths and other tools retain their normal approval behavior.</p><p>Permission requests from a delegate carry the asking delegate&#39;s name, so the card can say which delegate wants the call (see <code>04-ux/03-permission-ux.md</code> §6a). Session-scoped <code>allow-session</code> grants are still per <code>toolName</code> and per session: one delegate&#39;s approval of <code>Bash</code> applies to the whole session, including the parent and other delegates.</p><h2 id="_11-plugin-tools" tabindex="-1">11. Plugin Tools <a class="header-anchor" href="#_11-plugin-tools" aria-label="Permalink to &quot;11. Plugin Tools&quot;">​</a></h2><p>Plugins can contribute tools via <code>agentTools</code>. Agent mode sees every registered plugin tool. Plan and Goal see only tools whose <code>planSafeActions</code> list is non-empty (ADR 0211 / D384):</p><ol><li>manifest declaration</li><li>user grants <code>agent.tool.register</code></li><li>PluginManager registers them into the ToolHost</li><li>execution goes through the unified permission/audit/timeout wrapper</li></ol><p>A plugin tool without <code>planSafeActions</code> is hidden from the model in Plan and Goal. A direct attempt returns <code>PLUGIN_DISABLED_IN_PLAN</code> — the <code>_IN_PLAN</code> codes are shared by both contract modes rather than duplicated per kind — and is audited as a contract-mode policy denial. When the list is present, host-core admits the tool and the plugin-runtime rejects any <code>action</code> outside the list with <code>PERMISSION_DENIED</code>. Missing or invalid plugin risk defaults to <code>medium</code> for Agent and never grants contract-mode access by itself.</p><p>Naming:</p><ul><li>Internal full name: <code>plugin.&lt;pluginId&gt;.&lt;toolName&gt;</code></li><li>Name exposed to the model: forced prefix <code>plugin_&lt;pluginIdSafe&gt;_&lt;toolName&gt;</code> (D015) to avoid conflicts</li></ul><h2 id="_12-future-extensions" tabindex="-1">12. Future Extensions <a class="header-anchor" href="#_12-future-extensions" aria-label="Permalink to &quot;12. Future Extensions&quot;">​</a></h2><ul><li>MCP tools</li><li>tool group toggles</li><li>command allowlist / denylist</li><li>dry-run mode</li><li>apply patches after preview</li></ul><h2 id="image-generation-and-editing" tabindex="-1">Image generation and editing <a class="header-anchor" href="#image-generation-and-editing" aria-label="Permalink to &quot;Image generation and editing&quot;">​</a></h2><p><code>GenerateImages</code> is a high-risk Agent-only capability, authorized by host-core before the trusted desktop executes the request. Plan/Goal remain denied even under Auto. See <a href="./21-image-generation">image generation</a> for cancellation, limits and result semantics.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("spec/03-runtime/03-tools-and-permissions.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _03ToolsAndPermissions = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _03ToolsAndPermissions as default
};
