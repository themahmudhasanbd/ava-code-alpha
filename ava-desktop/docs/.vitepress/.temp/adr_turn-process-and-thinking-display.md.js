import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR turn-process-and-thinking-display: Turn process and thinking presentation","description":"","frontmatter":{},"headers":[],"relativePath":"adr/turn-process-and-thinking-display.md","filePath":"adr/turn-process-and-thinking-display.md","lastUpdated":1789930953000}');
const _sfc_main = { name: "adr/turn-process-and-thinking-display.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-turn-process-and-thinking-display-turn-process-and-thinking-presentation" tabindex="-1">ADR turn-process-and-thinking-display: Turn process and thinking presentation <a class="header-anchor" href="#adr-turn-process-and-thinking-display-turn-process-and-thinking-presentation" aria-label="Permalink to &quot;ADR turn-process-and-thinking-display: Turn process and thinking presentation&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-17</li><li>Amended: 2026-09-20</li><li>Issues: #510, #461</li><li>Amends: D071, <a href="./0242-delta-only-streaming-updates">ADR 0242</a></li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>A model can alternate reasoning, tool calls and progress text many times before answering one user request. Separate activity groups leave those progress messages at the same visual level as the answer, while one flat process container still leaves long tool/search sequences difficult to scan. Some readers also need a thinking indicator without rapidly changing reasoning text.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Both display modes project each loaded assistant-turn entry into one turn-process disclosure plus its trailing answer. Thinking, tools and intermediate assistant text keep their original order inside the process. A trailing assistant text stays visible while streaming; if later tool or thinking activity follows, that text moves into the process without changing the stored message. There is no semantic final-answer marker in <code>UiMessage</code>, so the renderer does not guess intent from wording. User/system messages and compaction dividers retain their existing turn boundaries.</p><p>The process has three independent disclosure levels: the whole turn process, an ordinary activity group, and one item&#39;s details. An ordinary activity group contains one contiguous tool/search/thinking segment between progress paragraphs and appears only when the mode has at least two visible items. A singleton uses its item disclosure directly; hidden compact-mode thinking does not create a redundant wrapper. Existing Task topology remains its segment&#39;s container and is not duplicated inside an ordinary activity group.</p><p>Detailed mode starts active and completed whole-process disclosures open. The ordinary group that owns the active execution segment starts open, then closes on completion only while untouched. Other completed ordinary groups start closed. Compact mode starts process and ordinary-group disclosures closed, but an untouched active process containing any recorded failed or denied tool stays open through later recovery and closes on turn completion if still untouched. Compact mode keeps every tool/search payload closed and renders no reasoning text or excerpt; it shows only the active thinking indicator and omits empty completed thinking-only containers.</p><p>Detailed mode preserves the leaf default only for the literal final item of the last activity group. If that item is an eligible tool-call or hosted-search row, its payload starts open; failed and denied items remain guarded closed. The renderer does not scan backward past a final thinking item to open an earlier tool. Opening a closed ancestor exposes the retained leaf state without opening all descendants.</p><p>Each header toggles only its own level. Parent and child states are independent: closing a parent preserves descendant choices, reopening restores them, and sibling groups do not form an accordion. Manual interaction with an item claims the containing group and process as user-owned without toggling either ancestor; completion must not close a container around content the user opened, focused, or selected. Manual choices survive streaming, completion, mode changes, reparenting from singleton to group, and row remounts while the owning retained session pane remains alive. Pane eviction, session deletion, or renderer restart releases this presentation memory; it is not stored in messages or host settings.</p><p>Search/navigation reveals the ancestor path its target needs: the process, then the activity group that owns the named message. Item-level targeting is not part of this change, so each row&#39;s own details stay behind its own disclosure. Replaying the same request does not repeatedly override a later manual close. Compact-mode reasoning remains hidden until the user chooses Detailed. Assistant errors, stopped trailing partial answers, permission/question/plan/goal decisions, and any other pending action surface remain outside hidden process content and reachable without expanding it.</p><p>Settings → AI → Defaults retains <code>thinkingDisplayMode</code>, the optional <code>detailed | compact</code> <code>AppSettings</code> field. Absent or unrecognized values resolve to detailed. The field changes presentation only; it does not alter provider thinking levels, runtime/model context, stored reasoning, export, permissions, execution, or copy payloads. Process headers use recorded timing, direct tool/search counts, running state, and issue counts; they do not double-count delegated child work or treat a failed child as a failed assistant turn.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Both modes expose one whole-process disclosure while keeping the final answer and actionable interruptions reachable outside it.</li><li>Detailed mode keeps progress narration visible by default, folds untouched completed activity groups, and preserves the literal-final-item leaf default.</li><li>Compact mode remains the low-detail option: the process is folded, payloads stay closed, and reasoning content is suppressed.</li><li>Disclosure memory is pane-owned presentation state with stable turn, group, and item identities; it is neither a persisted transcript contract nor a central workflow-store concern.</li><li>This groups loaded transcript entries only; it does not reconstruct unloaded history or join turns across compaction boundaries.</li></ul><h2 id="validation" tabindex="-1">Validation <a class="header-anchor" href="#validation" aria-label="Permalink to &quot;Validation&quot;">​</a></h2><p>For the 2026-09-20 amendment, the request explicitly limits validation to static checks and compilation. The linked E2E scenarios describe intended behavior for source and design review; no unit, component, integration, browser, Electron, or E2E tests are added or run for this amendment. See E2E-CHAT-turn-process-and-thinking-display for the synchronized scenario text.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/turn-process-and-thinking-display.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const turnProcessAndThinkingDisplay = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  turnProcessAndThinkingDisplay as default
};
