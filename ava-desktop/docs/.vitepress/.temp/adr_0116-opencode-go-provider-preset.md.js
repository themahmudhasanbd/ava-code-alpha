import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0116: Add OpenCode Go as a Fixed Provider Preset","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0116-opencode-go-provider-preset.md","filePath":"adr/0116-opencode-go-provider-preset.md","lastUpdated":1788918391000}');
const _sfc_main = { name: "adr/0116-opencode-go-provider-preset.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0116-add-opencode-go-as-a-fixed-provider-preset" tabindex="-1">ADR 0116: Add OpenCode Go as a Fixed Provider Preset <a class="header-anchor" href="#adr-0116-add-opencode-go-as-a-fixed-provider-preset" aria-label="Permalink to &quot;ADR 0116: Add OpenCode Go as a Fixed Provider Preset&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-08-24</li><li>Deciders: PI-Desktop core</li><li>Updates ADR 0012 and ADR 0020</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The OpenCode Go service exposes an OpenAI-compatible Chat Completions API, but its endpoint is service-owned and should not be edited like a generic gateway. Users should be able to configure it without copying an endpoint from separate documentation, while the existing provider studio must continue to support arbitrary OpenAI-compatible services.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Add <code>opencode_go</code> as a persisted API-style value and expose it in the custom provider dialog. Selecting it applies these immutable connection defaults:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>name:    OpenCode Go</span></span>
<span class="line"><span>baseUrl: https://opencode.ai/zen/go/v1</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>The renderer keeps the name and endpoint visible but read-only, accepts the API key as the only editable connection field, and continues to use the normal discovered-model selector. Save-time normalization also enforces the fixed values so stale or manually constructed form state cannot override them.</p><p>The runtime maps <code>opencode_go</code> to pi-ai&#39;s OpenAI Chat Completions adapter unless a model-level catalog entry pins another wire API (see #105). Model discovery calls <code>/models</code> with a Bearer key. Secrets remain owned by the existing Rust host secret store; no OpenCode-specific secret or database table is introduced.</p><p>OpenCode Go requires a stable <code>x-opencode-session</code> header on LLM requests so the gateway can pin a conversation to one backend. pi-ai does not emit that header. Agent-runtime injects it (plus <code>x-opencode-client: pi-desktop</code> and a PI-Desktop <code>User-Agent</code>) on session, subagent, prompt-enhancement, and plugin one-shot streams, using the durable conversation id. Detection matches <code>apiStyle: opencode_go</code>, vendor/provider ids <code>opencode</code> / <code>opencode-go</code>, or an <code>opencode.ai</code> base URL so a UUID provider row and a custom Completions row pointed at Go both work. This stays in the agent layer, matching the official Pi coding-agent attribution helper.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>OpenCode Go is recognizable in the provider row and durable configuration.</li><li>The service cannot be accidentally pointed at a different endpoint while the preset is selected.</li><li>Generic OpenAI-compatible configuration remains available as a separate API style for user-controlled gateways.</li><li>The preset does not create a second wire adapter or constrain the service&#39;s model list to a hardcoded catalog.</li><li>OpenCode Go chat, subagent, and one-shot requests carry a stable session routing header without waiting on a pi-ai change.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0116-opencode-go-provider-preset.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0116OpencodeGoProviderPreset = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0116OpencodeGoProviderPreset as default
};
