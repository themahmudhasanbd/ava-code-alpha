import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0102: Publisher-owned plugin source with a Git-hosted artifact store","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0102-publisher-owned-plugin-source-and-git-hosted-artifacts.md","filePath":"adr/0102-publisher-owned-plugin-source-and-git-hosted-artifacts.md","lastUpdated":1787113588000}');
const _sfc_main = { name: "adr/0102-publisher-owned-plugin-source-and-git-hosted-artifacts.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0102-publisher-owned-plugin-source-with-a-git-hosted-artifact-store" tabindex="-1">ADR 0102: Publisher-owned plugin source with a Git-hosted artifact store <a class="header-anchor" href="#adr-0102-publisher-owned-plugin-source-with-a-git-hosted-artifact-store" aria-label="Permalink to &quot;ADR 0102: Publisher-owned plugin source with a Git-hosted artifact store&quot;">​</a></h1><blockquote><p>Amended 2026-08-19: artifacts are committed to the existing distribution repository rather than published as releases of the center repository. The ownership model and every other decision below are unchanged.</p></blockquote><ul><li>Status: Accepted for implementation</li><li>Date: 2026-08-18</li><li>Deciders: PI-Desktop plugin and distribution maintainers</li><li>Supersedes: ADR 0006 (marketplace postponed)</li><li>Amends: ADR 0007 (plugin package format), ADR 0005 (user-installable plugins)</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The shipped marketplace stores everything in one repository. <code>vastsa/pi-desktop-plugins</code> holds plugin source under <code>plugins/&lt;id&gt;/</code>, built packages under <code>packages/*.piplug</code>, and the <code>catalog.json</code> the client reads. Publishing means a maintainer commits somebody else&#39;s source into the marketplace repository and regenerates the catalog with a local script.</p><p>That model does not survive third-party publishers:</p><ul><li>Plugin source has no owner outside the marketplace repository. A publisher cannot iterate, tag, or version independently.</li><li>The marketplace repository grows binary packages forever and its Git history becomes the artifact store by accident.</li><li>Nothing binds a published package to the source that produced it. The catalog records a checksum, not a repository, commit, or builder.</li><li><code>verified</code> is a hand-edited catalog field. A publisher with commit access can assert it.</li><li><code>download_url</code> follows redirects to any host. That is acceptable while every package URL resolves under one known repository, and unacceptable once package URLs are publisher-supplied.</li></ul><p>The plugin center design (<code>vastsa/pi-plugin-center</code>) answers the ownership half: plugin source stays in the publisher&#39;s own repository, and the platform stores a permission-verified snapshot, build evidence, review reports, and a signed catalog. It specifies S3/R2 plus a CDN as the artifact store.</p><p>We want the ownership model without operating object storage.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><h3 id="_1-the-publisher-s-repository-is-the-source-of-truth" tabindex="-1">1. The publisher&#39;s repository is the source of truth <a class="header-anchor" href="#_1-the-publisher-s-repository-is-the-source-of-truth" aria-label="Permalink to &quot;1. The publisher&#39;s repository is the source of truth&quot;">​</a></h3><p>A publisher submits a repository coordinate — canonical HTTPS repository URL, path, and a ref that resolves to a 40-hex commit. PI-Desktop never copies plugin source into a project-owned repository. <code>pluginId</code>, <code>publisherId</code>, the linked repository, and the packaged <code>manifest.json</code> identity must agree, and a published version is pinned to exactly one <code>(repository, commit, path)</code> tuple.</p><h3 id="_2-management-and-distribution-are-separate-repositories" tabindex="-1">2. Management and distribution are separate repositories <a class="header-anchor" href="#_2-management-and-distribution-are-separate-repositories" aria-label="Permalink to &quot;2. Management and distribution are separate repositories&quot;">​</a></h3><p><code>vastsa/pi-plugin-center</code> is the management plane: submissions, ownership verification, isolated builds, review, the policy gate, and the registry that records what has been published.</p><p><code>vastsa/pi-desktop-plugins</code> stays the distribution repository. It keeps serving <code>catalog.json</code> from its root and packages from <code>packages/</code>, and the CNB copy keeps mirroring it. There is no S3, no R2, and no separately operated CDN.</p><ul><li>The publisher builds and attaches <code>&lt;id&gt;-&lt;version&gt;.piplug</code> to a release in their own repository, or the center&#39;s isolated runner builds it from the pinned commit.</li><li>The center verifies the bytes, then commits the package to <code>pi-desktop-plugins/packages/</code> together with a regenerated <code>catalog.json</code>, in one commit.</li><li>Package URLs stay relative, so they resolve against whichever host served the catalog. Switching between GitHub and the CNB mirror therefore cannot send a download to the other provider or change the checksum being verified.</li></ul><p>Keeping distribution where it already is means the default catalog URL never changes and no installed client needs to migrate. It also means the artifact bytes and the digest that describes them land in the same Git commit, which is a stronger record than a release asset: changing published bytes requires rewriting history rather than re-uploading a file. The residual risk is a force-push, which branch protection on the distribution repository is expected to prevent.</p><p>The accepted cost is repository growth. Committed binaries are permanent — an unused version can be removed from <code>packages/</code> but stays in history, so every clone pays for every version ever published.</p><h3 id="_3-catalog-schema-v2-is-the-client-contract" tabindex="-1">3. Catalog schema v2 is the client contract <a class="header-anchor" href="#_3-catalog-schema-v2-is-the-client-contract" aria-label="Permalink to &quot;3. Catalog schema v2 is the client contract&quot;">​</a></h3><p><code>catalog.json</code> gains <code>schemaVersion: 2</code> and carries, per version: the source repository, ref, commit and path; the builder identity; the review decision, risk tier and policy version; <code>yanked</code> state; and an optional detached signature. The client keeps reading v1 catalogs unchanged so the existing marketplace repository and any custom source keep working during migration.</p><h3 id="_4-the-host-restricts-where-a-package-may-come-from" tabindex="-1">4. The host restricts where a package may come from <a class="header-anchor" href="#_4-the-host-restricts-where-a-package-may-come-from" aria-label="Permalink to &quot;4. The host restricts where a package may come from&quot;">​</a></h3><p>Because package URLs are now publisher-influenced, host-core enforces a download host allowlist for marketplace packages: the GitHub release and raw asset hosts, and the CNB host. Redirects are restricted to HTTPS and the final effective URL is re-checked against the allowlist. A custom or enterprise catalog may only download from the host that served its own catalog, unless the user has explicitly configured additional hosts.</p><h3 id="_5-trust-and-review-verdicts-are-center-issued-never-publisher-asserted" tabindex="-1">5. Trust and review verdicts are center-issued, never publisher-asserted <a class="header-anchor" href="#_5-trust-and-review-verdicts-are-center-issued-never-publisher-asserted" aria-label="Permalink to &quot;5. Trust and review verdicts are center-issued, never publisher-asserted&quot;">​</a></h3><p><code>trust</code>, <code>review.decision</code>, <code>review.risk</code>, and <code>policyVersion</code> are written by the center&#39;s policy evaluator. Publisher-submitted metadata cannot set them. The client renders <code>community</code> for anything it cannot attribute to the center, and never renders an unverifiable claim as <code>verified</code>.</p><h3 id="_6-a-yanked-version-is-not-installable" tabindex="-1">6. A yanked version is not installable <a class="header-anchor" href="#_6-a-yanked-version-is-not-installable" aria-label="Permalink to &quot;6. A yanked version is not installable&quot;">​</a></h3><p><code>yanked: true</code> removes a version from install and update selection, keeps it visible in version history with its reason, and marks an installed copy as needing attention.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Third-party publishers can ship plugins without write access to any PI-Desktop-owned repository, and can version on their own schedule.</li><li>The default catalog URL is unchanged, so no client release and no user action is needed to reach plugins published through the center.</li><li>The client needs no code change for this decision: relative package URLs already resolve against the catalog that carried them, and the download host allowlist already covers both distribution hosts.</li><li>Every installed marketplace plugin can be traced to a repository and commit, and the desktop can show that provenance before install.</li><li>Losing object storage costs WORM semantics and versioned-bucket retention. The Git record above is tamper-evident rather than tamper-proof, and the distribution repository grows without bound. Both are accepted trades.</li><li>The client must tolerate two catalog schemas while the distribution repository serves v1 entries alongside center-published v2 ones.</li><li>Publisher signing keys remain out of scope. The center signs; publisher-held keys stay in ADR 0008&#39;s later phase.</li></ul><h2 id="rejected-alternatives" tabindex="-1">Rejected alternatives <a class="header-anchor" href="#rejected-alternatives" aria-label="Permalink to &quot;Rejected alternatives&quot;">​</a></h2><ul><li><strong>Keep source in the marketplace repository.</strong> Simple, already working, and cannot support a publisher who is not a maintainer.</li><li><strong>Index only; download from publisher repositories.</strong> Removes the center from the download path entirely, but a publisher can delete or replace a release asset out from under a published version, and the client would need an open-ended host allowlist.</li><li><strong>Publish artifacts as releases of the distribution repository.</strong> Keeps Git history free of binaries and allows pruning old versions, at the cost of a catalog that must declare an artifact base, and a CNB mirror that has to replicate release assets separately instead of riding along with the Git mirror that already exists.</li><li><strong>Center builds every plugin from source with no publisher artifact.</strong> Strongest reproducibility, and makes the center responsible for every plugin&#39;s toolchain and dependency supply chain from day one. Retained as the runner path for plugins that opt in, not as the only path.</li><li><strong>S3/R2 plus CDN as specified in the plugin center document.</strong> Gives WORM, object lock, and cache control, at the cost of the operational and billing surface this decision exists to avoid.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0102-publisher-owned-plugin-source-and-git-hosted-artifacts.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0102PublisherOwnedPluginSourceAndGitHostedArtifacts = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0102PublisherOwnedPluginSourceAndGitHostedArtifacts as default
};
