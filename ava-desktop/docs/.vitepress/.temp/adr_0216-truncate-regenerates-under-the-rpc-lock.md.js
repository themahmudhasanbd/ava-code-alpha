import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0216: Truncate Regenerates Under the RPC Lock","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0216-truncate-regenerates-under-the-rpc-lock.md","filePath":"adr/0216-truncate-regenerates-under-the-rpc-lock.md","lastUpdated":1789070236000}');
const _sfc_main = { name: "adr/0216-truncate-regenerates-under-the-rpc-lock.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0216-truncate-regenerates-under-the-rpc-lock" tabindex="-1">ADR 0216: Truncate Regenerates Under the RPC Lock <a class="header-anchor" href="#adr-0216-truncate-regenerates-under-the-rpc-lock" aria-label="Permalink to &quot;ADR 0216: Truncate Regenerates Under the RPC Lock&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-10</li><li>Deciders: PI-Desktop core</li><li>Related: D199 / ADR 0060, D258 / ADR 0127, D307, issue #211</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Retry, regenerate, and edit-resend truncated the live transcript from Electron main: <code>session.get</code> of the whole history, <code>session.saveRevision</code> of the discarded tail, then <code>session.replaceMessages</code> with the kept prefix.</p><p>That round trip ships the kept transcript as one NDJSON JSON-RPC line. Host stdin rejects a line over 64 MiB by ending the reader, and every non-Bash RPC waits only 130 s. A session of a few thousand messages (tens of megabytes on disk) therefore fails with <code>host RPC timeout: session.replaceMessages</code> while host-core may still be rewriting. The client timeout does not cancel the host task, so a second retry queues another full rewrite. The previous turn can stay <code>running</code> because <code>beginTurn</code> never ran.</p><p>ADR 0060 already moved turn-completion archive off this path. Truncation still used the whole-transcript primitive.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><code>session.truncateFrom</code> is a host-owned cut: <code>{ sessionId, fromMessageId? , truncateBefore? }</code>. It resolves the boundary against the durable transcript (identity first, count as the older fallback), aborts a leftover running turn, archives the discarded regenerate tail the way Electron used to, and rewrites the kept prefix under the state lock. The RPC result is counts and optional pager metadata. No transcript snapshot crosses the process boundary.</li><li><code>agent/prompt</code> calls that method when a truncate is requested, loads only a bounded <code>session.get</code> for launch configuration, and disposes the sidecar session after the cut. It no longer calls <code>session.replaceMessages</code> or <code>session.saveRevision</code> on this path.</li><li>An NDJSON request line over 64 MiB is drained through to the next newline and answered with <code>LIMIT_EXCEEDED</code>. It no longer ends the stdin reader.</li><li>Protocol version stays at 11. Host and Electron ship together; a host without the method fails the call, which regenerate already treats as fatal before the live transcript is cut.</li></ol><p><code>session.replaceMessages</code> remains for callers that truly replace the whole array (message delete, unanswered smart Stop). It is still unsafe as a read-modify-write from a snapshot taken outside the lock.</p><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Raise the 130 s deadline for <code>session.replaceMessages</code>:</strong> the 64 MiB stdin cap still kills or rejects the line. Rejected as the sole fix.</li><li><strong>Chunk the kept array across many RPCs:</strong> keeps the snapshot outside the lock and still races <code>session.appendMessage</code>. Rejected.</li><li><strong>Bump the protocol version:</strong> handshake requires exact equality between components that ship together, and nothing a v11 client relies on changes. Rejected, matching ADR 0060.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Retry and regenerate on a multi-thousand-message session no longer depend on shipping the kept prefix through JSON-RPC.</li><li>A leftover <code>running</code> turn is settled as <code>aborted</code> inside the same lock, so the following <code>beginTurn</code> is not <code>AGENT_BUSY</code> and UI failure cannot leave sqlite <code>running</code>.</li><li>Oversized control-pipe lines no longer take down host-core.</li><li>On Windows, a leftover strong stdout sender in the Alt+Space hook could still keep host-core alive after stdin ended (the 130 s timeout in issue #211). See ADR 0217.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0216-truncate-regenerates-under-the-rpc-lock.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0216TruncateRegeneratesUnderTheRpcLock = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0216TruncateRegeneratesUnderTheRpcLock as default
};
