import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0243: Skill market public-HTTPS catalog fetch","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0243-skill-market-public-https-catalog.md","filePath":"adr/0243-skill-market-public-https-catalog.md","lastUpdated":1789326365000}');
const _sfc_main = { name: "adr/0243-skill-market-public-https-catalog.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0243-skill-market-public-https-catalog-fetch" tabindex="-1">ADR 0243: Skill market public-HTTPS catalog fetch <a class="header-anchor" href="#adr-0243-skill-market-public-https-catalog-fetch" aria-label="Permalink to &quot;ADR 0243: Skill market public-HTTPS catalog fetch&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-13</li><li>Decision: D413</li><li>Amends: ADR 0009</li><li>Issue: #287</li><li>PR: #290</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Settings → Skills gained a Market view that installs community <code>SKILL.md</code> documents through the existing <code>skills.create</code> write path. The renderer CSP only allows localhost, so catalog discovery and document fetch run in Electron main. Users can add arbitrary source URLs. A naive <code>net.fetch</code> would follow redirects onto loopback or RFC1918 space.</p><p>The plugin marketplace already uses a <strong>fixed host allowlist</strong>. A skill market that accepts user GitHub repos cannot reuse that allowlist. MCP market (#285) needs the same public-HTTPS classifier; duplicating <code>isPublicHostname</code> under two <code>export *</code> names would collide.</p><p>Host-core <code>valid_capability_id</code> accepts <code>[a-z0-9][a-z0-9-]{0,63}</code>. A scan that kept underscores produced ids the host rewrote, so the Market &quot;Installed&quot; badge never matched.</p><p>User skills are a single markdown file capped at 128 KiB. Inlining sibling <code>.md</code> files can exceed that cap after preview succeeded.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>Keep a single I/O-free classifier in <code>packages/shared</code> (<code>isSafePublicHttpsUrl</code>, <code>isPublicHostname</code>, <code>isPublicIpLiteral</code>). Skill market wraps it as <code>isSafeSkillSourceUrl</code>. Future MCP market imports the same module.</li><li>Main-process fetches go through <code>createPublicHttpsClient</code>: HTTPS only, DNS classification of every resolved address, <code>redirect: &quot;manual&quot;</code> with per-hop re-validation, and retries only for non-policy failures.</li><li>The renderer never fetches catalog or document URLs. Install remains <code>skills.create</code>. Host-core stays unaware of the market.</li><li>Scanned and catalog ids are sanitized to host <code>valid_capability_id</code> before they reach the UI.</li><li>Preview shows the assembled body (skill text plus inlined sibling markdown). A document that would exceed <code>MAX_SKILL_BYTES</code> cannot be installed.</li><li>Builtin catalog titles are English (ADR 0009).</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>User-added <code>https://github.com/org/repo</code> sources make main walk the git tree and pull jsDelivr copies. The boundary is &quot;public HTTPS + public resolved IPs&quot;, not a host allowlist. DNS rebinding between lookup and Chromium <code>net.fetch</code> remains a residual risk.</li><li>Installing a skill still writes model instructions. Preview is the disclosure; <code>verified</code> on builtin rows is not a signature.</li><li>MCP market must import <code>public-network.ts</code> instead of copying the classifier.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><ul><li>Plugin-marketplace host allowlist: too narrow for user GitHub sources.</li><li>Directory install (<code>~/.agents/skills/&lt;slug&gt;/</code>): incompatible with the current single-file user-skill model.</li><li>Pin fetched IPs into undici: stronger against rebinding, but would skip Chromium&#39;s proxy stack that <code>net.fetch</code> honors.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0243-skill-market-public-https-catalog.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0243SkillMarketPublicHttpsCatalog = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0243SkillMarketPublicHttpsCatalog as default
};
