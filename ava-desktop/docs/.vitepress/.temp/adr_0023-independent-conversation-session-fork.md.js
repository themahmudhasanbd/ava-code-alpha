import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0023: Independent Conversation Session Fork","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0023-independent-conversation-session-fork.md","filePath":"adr/0023-independent-conversation-session-fork.md","lastUpdated":1790003601000}');
const _sfc_main = { name: "adr/0023-independent-conversation-session-fork.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0023-independent-conversation-session-fork" tabindex="-1">ADR 0023: Independent Conversation Session Fork <a class="header-anchor" href="#adr-0023-independent-conversation-session-fork" aria-label="Permalink to &quot;ADR 0023: Independent Conversation Session Fork&quot;">​</a></h1><h2 id="status" tabindex="-1">Status <a class="header-anchor" href="#status" aria-label="Permalink to &quot;Status&quot;">​</a></h2><p>Accepted.</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>PI-Desktop already preserves linear regenerate variants within one conversation. Users also need a Codex-style command that copies the current conversation so later prompts and configuration changes can diverge without rewriting the source.</p><p>Composing <code>session.create</code> and message replacement in the renderer would expose partial children, lose canonical transcript blocks, omit per-session execution configuration, and cross the Rust host&#39;s persistence ownership boundary. Adding the operation while retaining protocol v4 would also allow an older host to pass startup handshake and fail only when the new command is invoked.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ul><li>Protocol v5 adds required host RPC <code>session.fork</code> and renderer IPC <code>session/fork</code>.</li><li>The Rust host owns one snapshot operation: by default it copies the source&#39;s complete active canonical transcript into a new session and remaps message and tool-call identifiers. Optional <code>throughMessageId</code> makes the snapshot end inclusively at that message; an unknown boundary creates no child and returns <code>NOT_FOUND</code>.</li><li>The child inherits project, provider, model, mode, thinking, and permission mode. It does not inherit turns, regenerate revisions, notifications, artifacts, session grants, arbitrary scratch outputs, pin state, or live runtime state.</li><li>Referenced existing files under the source session&#39;s <code>scratch/&lt;id&gt;/pasted/</code> directory are copied into the child&#39;s own input directory. Canonical message and retained compaction references are rewritten before indexing. This keeps pasted/imported inputs usable after source deletion without granting access to another session&#39;s scratch directory. Already-expired inputs remain missing; unrelated scratch outputs and unreferenced files are not inherited.</li><li>No parent/child lineage is stored. This is an independent conversation copy, not a message tree and not a replacement for linear regenerate history.</li><li>Assistant response Fork uses the bounded snapshot directly. Assistant Edit uses the same bounded child, stores original/edited response tails in that child&#39;s existing linear revision store, and activates the edited tail. The source transcript, revisions, runtime, and provider-cache state remain untouched.</li><li>Fork is available only while the source is idle. Electron exposes <code>AGENT_BUSY</code>; the host retains a persisted running-turn <code>CONFLICT</code> guard that Electron normalizes at the IPC boundary.</li><li>A handled file or index failure removes the child transcript and copied inputs and leaves no visible child. Process crashes continue to follow the transcript store&#39;s existing orphan-file recovery policy.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Renderer and host binaries from protocol v4 are rejected during startup instead of failing lazily when Create branch is selected.</li><li>Source and child can evolve, reconfigure, persist, and delete independently.</li><li>Fork storage cost is proportional to the active transcript and its referenced pasted inputs.</li><li>Message-scoped Fork/Edit storage cost is proportional to the canonical prefix through the selected response plus any child-only revision payloads.</li><li>Every child has a new session id and first creates/reseeds its own pi runtime; edited context never reuses cache state built from the source transcript.</li><li>The initial implementation intentionally has no ancestry UI, merge operation, or arbitrary message-level branch tree; Edit rollback stays a two-entry linear revision family.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><ul><li>Renderer-owned <code>create + replaceMessages</code>: rejected because it is not atomic across the host-owned transcript and index.</li><li>Persistent parent/child lineage: deferred because the requested workflow only needs an independent copy and D109 keeps message revision navigation linear.</li><li>Copy regenerate revisions: rejected because revision roots and message identifiers would require a second branch graph and ambiguous pager history.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0023-independent-conversation-session-fork.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0023IndependentConversationSessionFork = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0023IndependentConversationSessionFork as default
};
