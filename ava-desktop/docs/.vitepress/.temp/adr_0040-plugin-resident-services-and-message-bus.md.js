import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0040: Resident plugin services and the inter-plugin message bus","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0040-plugin-resident-services-and-message-bus.md","filePath":"adr/0040-plugin-resident-services-and-message-bus.md","lastUpdated":1785489746000}');
const _sfc_main = { name: "adr/0040-plugin-resident-services-and-message-bus.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0040-resident-plugin-services-and-the-inter-plugin-message-bus" tabindex="-1">ADR 0040: Resident plugin services and the inter-plugin message bus <a class="header-anchor" href="#adr-0040-resident-plugin-services-and-the-inter-plugin-message-bus" aria-label="Permalink to &quot;ADR 0040: Resident plugin services and the inter-plugin message bus&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-07-31</li><li>Related: <a href="./../spec/07-plugins/05-plugin-lifecycle">Plugin lifecycle</a>, <a href="./../spec/07-plugins/12-plugin-ipc-and-host-services">Plugin IPC and host services</a>, <a href="./0008-plugin-runtime-isolation-target">ADR 0008</a></li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Two roadmap items were blocked on the same missing pieces.</p><p><strong>Background services.</strong> A plugin could only do work when something called into it: a command, a tool, or <code>onLoad</code>. A watcher, a poller, or a sync worker had no supported home. Nothing stopped a plugin from starting a timer inside <code>onLoad</code>, but the host had no idea it existed, could not report it, and could not restart it — a crash simply lost the work silently.</p><p><strong>Inter-plugin messaging.</strong> <code>pi.events.on</code> / <code>off</code> existed in the SDK as no-ops. Plugins that wanted to cooperate had only the filesystem, which is invisible to the host and unbounded.</p><p>Both need the same two things: a way for the host to know a plugin is doing something, and a parent→child push channel. The plugin wire protocol had only request/response frames (<code>init</code> / <code>call</code> / <code>res</code> / <code>log</code>) — every message originated in the child.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><h3 id="one-new-frame" tabindex="-1">One new frame <a class="header-anchor" href="#one-new-frame" aria-label="Permalink to &quot;One new frame&quot;">​</a></h3><p>The broker gains a one-way parent→child frame:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>{ t: &quot;event&quot;, event, ... }   // no reply, no backpressure</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p><code>bus.message</code> is its first user. The same frame finally implements <code>pi.events.on</code> / <code>off</code>, which now sees the raw event stream.</p><h3 id="resident-services" tabindex="-1">Resident services <a class="header-anchor" href="#resident-services" aria-label="Permalink to &quot;Resident services&quot;">​</a></h3><ol><li>A service is <strong>declared</strong> (<code>contributes.services</code>, at most 4 per plugin) and gated on <code>background.service</code>. <code>pi.services.register({ id, start, stop })</code> in the child is local bookkeeping only — the manifest already said the service exists, so registration cannot create one.</li><li>The <strong>broker decides when it runs</strong>: <code>start</code> after <code>onLoad</code> completes (5s budget), <code>stop</code> before <code>onUnload</code>. A service is therefore never live outside the window in which the plugin still has its API. <code>start</code> is idempotent inside one process.</li><li>A service lives in the plugin&#39;s <code>utilityProcess</code> (ADR 0008), so a crash takes it down with the process. The supervisor restarts the <strong>whole plugin</strong>: backoff 1s, 2s, 4s, 8s, 16s capped at 30s; at most 5 attempts; a process that stays up 60s is healthy and resets the counter. <code>autoRestart: false</code> opts out.</li><li>After the last attempt the plugin stays <code>failed</code>. Per-service state (<code>starting</code> | <code>running</code> | <code>stopped</code> | <code>failed</code>) plus the restart count is read over <code>plugin/services</code>, rendered as chips on the Plugins page, and audited as <code>plugin.service.*</code>.</li><li>Manual enable / disable outranks the supervisor: an explicit user action cancels the pending timer and clears the attempt counter.</li></ol><h3 id="message-bus" tabindex="-1">Message bus <a class="header-anchor" href="#message-bus" aria-label="Permalink to &quot;Message bus&quot;">​</a></h3><ol><li>Traffic is <strong>declared in the manifest</strong>: <code>contributes.bus.publish</code> lists concrete topics, <code>contributes.bus.subscribe</code> lists patterns. The permissions <code>bus.publish</code> / <code>bus.subscribe</code> are necessary but not sufficient — a granted permission with no declaration routes nothing.</li><li>Topics are dot-separated segments (<code>[a-zA-Z0-9][a-zA-Z0-9_-]*</code>, ≤8 segments, ≤128 chars). <code>*</code> matches one segment; <code>**</code> matches one or more trailing segments and may appear only as the final segment.</li><li><strong>Routing lives in the broker.</strong> A message carries <code>topic</code>, <code>from</code>, <code>payload</code>, and a host-assigned <code>at</code>. The publisher is excluded from its own fan-out. Delivery is fire-and-forget, so a wedged subscriber cannot stall a publisher.</li><li>Caps: 64KB per payload, 16 subscriptions per plugin, 100 publishes per rolling 10s window; over-cap calls fail <code>LIMIT_EXCEEDED</code> / <code>RATE_LIMITED</code> and are audited with the topic.</li><li>A payload is <strong>data, never capability</strong>. Receiving a message grants the subscriber nothing it did not already hold, so no permission can be laundered across the bus.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A background worker is now a first-class, visible, supervised thing: the user sees it running, sees it restart, and sees it give up.</li><li>Restarting the plugin rather than the service is coarse — a crash re-runs <code>onLoad</code> — but it is the only honest unit, because the crash already destroyed the process the service lived in.</li><li>The 5-attempt ceiling means a genuinely broken service stops retrying. That is the intent: a visible <code>failed</code> chip beats an invisible crash loop burning CPU.</li><li>Declared topics make plugin-to-plugin coupling reviewable, and let the host reject undeclared traffic without knowing anything about payload semantics.</li><li>Because the publisher is excluded from its own fan-out, a plugin cannot use the bus as an internal event emitter — it must use ordinary function calls, which is what it should have done anyway.</li><li>Any plugin holding <code>bus.subscribe</code> and a matching pattern sees a topic, so a topic is effectively public within the app. Secrets do not belong on it.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><h3 id="let-plugins-start-their-own-timers-and-call-it-a-service" tabindex="-1">Let plugins start their own timers and call it a service <a class="header-anchor" href="#let-plugins-start-their-own-timers-and-call-it-a-service" aria-label="Permalink to &quot;Let plugins start their own timers and call it a service&quot;">​</a></h3><p>Rejected. That is the status quo. The host cannot report, stop, or restart work it does not know about, and the user cannot see it at all.</p><h3 id="restart-the-service-in-place-instead-of-the-plugin" tabindex="-1">Restart the service in place instead of the plugin <a class="header-anchor" href="#restart-the-service-in-place-instead-of-the-plugin" aria-label="Permalink to &quot;Restart the service in place instead of the plugin&quot;">​</a></h3><p>Rejected. The service runs inside the plugin&#39;s process; if it crashed the process, there is no in-place to restart into. A separate process per service would multiply ADR 0008&#39;s isolation cost for no gain at this scale.</p><h3 id="unlimited-restarts" tabindex="-1">Unlimited restarts <a class="header-anchor" href="#unlimited-restarts" aria-label="Permalink to &quot;Unlimited restarts&quot;">​</a></h3><p>Rejected. A crash loop with backoff still burns CPU forever and hides the failure behind a chip that flickers between <code>starting</code> and <code>failed</code>.</p><h3 id="an-open-pub-sub-with-permissions-only" tabindex="-1">An open pub/sub with permissions only <a class="header-anchor" href="#an-open-pub-sub-with-permissions-only" aria-label="Permalink to &quot;An open pub/sub with permissions only&quot;">​</a></h3><p>Rejected. <code>bus.publish</code> would then mean &quot;talk to every plugin about anything&quot;, which is unreviewable. Declared topics keep the manifest a complete statement of what a plugin says and hears.</p><h3 id="direct-plugin-to-plugin-rpc" tabindex="-1">Direct plugin-to-plugin RPC <a class="header-anchor" href="#direct-plugin-to-plugin-rpc" aria-label="Permalink to &quot;Direct plugin-to-plugin RPC&quot;">​</a></h3><p>Rejected. It creates hard dependencies between independently installable, independently versioned plugins, and it hands one plugin a handle on another. Topic pub/sub keeps both sides ignorant of each other.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0040-plugin-resident-services-and-message-bus.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0040PluginResidentServicesAndMessageBus = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0040PluginResidentServicesAndMessageBus as default
};
