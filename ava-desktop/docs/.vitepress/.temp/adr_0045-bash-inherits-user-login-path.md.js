import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse(`{"title":"ADR 0045: Bash tool inherits the user's login-shell PATH","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0045-bash-inherits-user-login-path.md","filePath":"adr/0045-bash-inherits-user-login-path.md","lastUpdated":1789871994000}`);
const _sfc_main = { name: "adr/0045-bash-inherits-user-login-path.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0045-bash-tool-inherits-the-user-s-login-shell-path" tabindex="-1">ADR 0045: Bash tool inherits the user&#39;s login-shell PATH <a class="header-anchor" href="#adr-0045-bash-tool-inherits-the-user-s-login-shell-path" aria-label="Permalink to &quot;ADR 0045: Bash tool inherits the user&#39;s login-shell PATH&quot;">​</a></h1><ul><li>Status: Accepted (amended 2026-09-20, D600 / issue #571)</li><li>Date: 2026-08-02</li><li>Related: <a href="./../spec/08-meta/decisions-log">D084</a> · <a href="./../spec/08-meta/decisions-log">D181</a> · <a href="./../spec/03-runtime/03-tools-and-permissions">Tools and permissions</a></li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The Bash tool runs agent commands through <code>bash -lc</code> (D084). A login bash sources only the bash profile, so on macOS — where the default shell is zsh and toolchains like nvm, pnpm, and Homebrew are initialized in <code>~/.zshrc</code> / <code>~/.zprofile</code> — commands cannot resolve <code>node</code>, <code>npm</code>, <code>pnpm</code>, or anything else that only the user&#39;s own shell exports. Launching the app from Finder/Dock further shrinks the environment to a minimal GUI PATH.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>On Unix, the first Bash call probes the user&#39;s login shell for its PATH: <code>$SHELL</code> (fallback <code>/bin/zsh</code> → <code>/bin/bash</code> → <code>/bin/sh</code>) runs <code>-lic &#39;printf %s &quot;$PATH&quot;&#39;</code> — <code>-l</code> sources login files, <code>-i</code> sources the interactive rc — with a 5s bound so a wedged rc cannot stall the tool. Only the last stdout line is kept, so rc banners cannot contaminate it; stderr is discarded (missing-tty/job-control noise).</li><li>The probed PATH is cached per process (<code>OnceLock</code>) and injected into every Bash subprocess via <code>cmd.env(&quot;PATH&quot;, ...)</code>.</li><li>The probe is strictly best-effort: on failure (no <code>$SHELL</code>, non-executable, non-zero exit, timeout) the host PATH is used unchanged. Windows keeps <code>bash -c</code> with the host environment (no change).</li><li>Agent commands remain POSIX bash; only the child environment&#39;s <code>PATH</code> is enriched. The resolved bash binary itself is unchanged.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li><code>node</code>/<code>npm</code>/<code>pnpm</code>, Homebrew tooling, and other login-shell exports resolve inside Bash tool calls, matching what a fresh terminal offers.</li><li><code>bash -lc</code> still re-runs the bash profile at startup; conda/brew hooks may prepend, dedupe, or reorder entries — the injected login PATH remains the base the user&#39;s bash profile builds on.</li><li>One bounded subprocess per process lifetime (the probe) is the entire overhead; every later Bash call is cache-only.</li><li>A slow or interactive-only user rc degrades gracefully to the previous behavior instead of failing the tool.</li><li>Stdio MCP servers spawned from Electron main use the same login-shell PATH (D600 / issue #571), because they share the Finder/Dock GUI PATH problem and cannot reuse the host-core probe.</li></ul><h2 id="alternatives-rejected" tabindex="-1">Alternatives rejected <a class="header-anchor" href="#alternatives-rejected" aria-label="Permalink to &quot;Alternatives rejected&quot;">​</a></h2><ul><li><strong>Run commands in <code>$SHELL</code> instead of bash:</strong> breaks the D084 contract that agent commands are POSIX bash; zsh differences (<code>$path</code> array, echo/glob semantics) would silently fork behavior.</li><li><strong>Source rc files from the wrapper command:</strong> fragile — the command line would have to special-case every shell&#39;s rc grammar, and injected prefixes would appear in every result.</li><li><strong>Bundle a default PATH in the host:</strong> cannot know the user&#39;s toolchain; replicates the Finder-launch problem in disguise.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0045-bash-inherits-user-login-path.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0045BashInheritsUserLoginPath = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0045BashInheritsUserLoginPath as default
};
