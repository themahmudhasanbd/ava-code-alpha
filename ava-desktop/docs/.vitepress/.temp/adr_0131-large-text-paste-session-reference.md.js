import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0131: Spill Large Composer Text Pastes into Session Scratch","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0131-large-text-paste-session-reference.md","filePath":"adr/0131-large-text-paste-session-reference.md","lastUpdated":1789398649000}');
const _sfc_main = { name: "adr/0131-large-text-paste-session-reference.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0131-spill-large-composer-text-pastes-into-session-scratch" tabindex="-1">ADR 0131: Spill Large Composer Text Pastes into Session Scratch <a class="header-anchor" href="#adr-0131-spill-large-composer-text-pastes-into-session-scratch" aria-label="Permalink to &quot;ADR 0131: Spill Large Composer Text Pastes into Session Scratch&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-08-28</li><li>Deciders: PI-Desktop core</li><li>Related: Issue #20, D262, ADR 0059, ADR 0070, ADR 0124</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Native textarea paste is useful for short prompts, but very large pasted blocks make the composer difficult to edit and inflate the visible prompt. The application already has a bounded Electron-to-main paste bridge and a session-owned scratch directory for clipboard files. The solution must preserve the exact pasted bytes, work when the caret is in the middle of a draft, avoid mutating the project, and keep the model-facing prompt addressable by the existing <code>@path</code> file semantics.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>Add the persisted <code>largePasteThreshold</code> app setting. It defaults to 600 characters and accepts integer values from 1 through 1,000,000. Text-only pastes at or below the threshold retain native textarea behavior.</li><li>A text-only paste above the threshold is intercepted by the Composer and sent through the existing <code>composer/pasteFiles</code> bridge as one UTF-8 <code>text/plain</code> file. Electron main validates the durable session and stores it under <code>&lt;data_dir&gt;/scratch/&lt;sessionId&gt;/pasted/</code> using the existing bounded, sanitized, unique-file policy.</li><li>The Composer inserts <code>@&lt;temporary-name&gt; </code> at the original selection and stores a renderer-only token-to-canonical-path mapping with the draft. The token remains visible inline, so the prefix and suffix of an existing draft remain editable. It is resolved in place exactly once immediately before dispatch; it is not appended as a second reference and is not included as a duplicate structured attachment.</li><li>The mapping remains session-scoped, survives project/workspace switches for its owning session, participates in draft caching and unanswered smart-Stop restoration, and is removed when the token is removed from the text. Session scratch lifecycle and cleanup remain the existing host-owned behavior.</li><li>Clipboard files and images keep their existing compact chip and structured attachment flow. This decision changes only text-only pastes above the configured threshold.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Large pasted blocks no longer occupy the editable prompt body; the user sees a compact, addressable reference at the exact paste position.</li><li>The agent can read the exact saved bytes through the canonical scratch path, while the project tree, artifact store, and host protocol schema remain unchanged.</li><li>The setting is backward-compatible because older app settings gain the default through host normalization rather than a database migration.</li><li>The renderer must retain a small amount of draft metadata to distinguish a generated inline token from user-authored <code>@</code> text and to avoid duplicate serialization.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Keep all text inline:</strong> rejected because large blocks remain difficult to edit and unnecessarily enlarge prompt content.</li><li><strong>Write directly into the workspace:</strong> rejected because a paste is transient input and must not dirty the user&#39;s project.</li><li><strong>Use a chip only:</strong> rejected because the requested affordance is an inline reference at the paste location, and the surrounding draft must remain readable.</li><li><strong>Send the token as a basename or append a second path:</strong> rejected because scratch files require their canonical path and either option can be ambiguous or duplicate the same input.</li></ul><h2 id="amendment-2026-09-14-text-representation-selected-beside-generated-image-copies" tabindex="-1">Amendment (2026-09-14): text representation selected beside generated image copies <a class="header-anchor" href="#amendment-2026-09-14-text-representation-selected-beside-generated-image-copies" aria-label="Permalink to &quot;Amendment (2026-09-14): text representation selected beside generated image copies&quot;">​</a></h2><p>ADR 0059&#39;s #138 amendment can select a text representation for a paste that also carries <code>image/*</code> copies, which is what Word supplies for a copied selection. That selected text uses this decision&#39;s <code>largePasteThreshold</code>, so short text stays editable inline and larger text spills into session scratch as before. The clipboard-file and image flows of decision 5 are unchanged.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0131-large-text-paste-session-reference.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0131LargeTextPasteSessionReference = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0131LargeTextPasteSessionReference as default
};
