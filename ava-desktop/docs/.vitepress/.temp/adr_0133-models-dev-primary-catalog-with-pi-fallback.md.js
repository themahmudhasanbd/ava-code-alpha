import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0133: Use models.dev as the primary model catalog with pi-ai fallback","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0133-models-dev-primary-catalog-with-pi-fallback.md","filePath":"adr/0133-models-dev-primary-catalog-with-pi-fallback.md","lastUpdated":1788992254000}');
const _sfc_main = { name: "adr/0133-models-dev-primary-catalog-with-pi-fallback.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0133-use-models-dev-as-the-primary-model-catalog-with-pi-ai-fallback" tabindex="-1">ADR 0133: Use models.dev as the primary model catalog with pi-ai fallback <a class="header-anchor" href="#adr-0133-use-models-dev-as-the-primary-model-catalog-with-pi-ai-fallback" aria-label="Permalink to &quot;ADR 0133: Use models.dev as the primary model catalog with pi-ai fallback&quot;">​</a></h1><ul><li>Status: Superseded by ADR 0134</li><li>Date: 2026-08-29</li><li>Deciders: PI-Desktop core</li><li>Amends: ADR 0027, D136, D243</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>PI-Desktop&#39;s pinned <code>@earendil-works/pi-ai</code> package provides high-quality runtime adapters and a useful built-in model catalog, but its model coverage and release cadence do not represent the full current market. Provider settings therefore cannot reliably offer current model names, limits, modalities, or capability flags from the catalog users expect.</p><p>The application already has a main-process model discovery path and a Rust-owned per-provider cache. Model metadata must remain outside the renderer, provider credentials must not be sent to a catalog service, and custom/local providers must remain usable when no public catalog knows them.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Use <code>https://models.dev/api.json</code> as the primary remote model catalog.</p><ol><li>Electron main fetches the fixed URL with a bounded timeout and parses only the provider/model fields needed by PI-Desktop. The renderer never fetches this URL and no API key, OAuth token, or other provider credential is sent with the request.</li><li>A models.dev provider is matched by configured <code>vendorKey</code> first and by a normalized provider API URL second. An exact model ID match supplies the display name, context/output limits, input modalities, price hints, reasoning options, tool-call flag, and structured-output flag used by <code>ModelInfo</code> and runtime model configuration.</li><li>Catalog precedence for a configured model is: <code>models.dev</code> → <code>pi-ai</code> → provider endpoint discovery → generic defaults. The pinned pi-ai catalog remains the fallback for unavailable or missing models.dev records and supplies adapter-specific compatibility data when models.dev has no equivalent. Provider endpoint discovery remains available for custom and account-specific model IDs.</li><li>The existing <code>ModelBinding</code> remains the explicit user configuration for a selected provider/model. Catalog data supplies defaults and capability metadata; edits to a binding continue to control its configured limits and enabled thinking levels.</li><li>The remote snapshot is loaded at most once per Electron process unless an explicit future refresh policy replaces it. A failed fetch preserves any successful in-memory snapshot and falls through without clearing the Rust-owned provider cache. The existing provider cache stores its current normalized fields; main re-decorates cached rows from the latest catalog.</li><li><code>ModelInfo.catalogSource</code> is a renderer-facing annotation only. It records <code>models.dev</code> or <code>pi-ai</code> without changing the host RPC/storage schema or persisting the raw remote document.</li></ol><p>Models.dev reasoning options map to the canonical PI-Desktop levels: effort values are retained when recognized (<code>none</code> becomes <code>off</code>); toggle and token budget options use <code>off</code> plus <code>medium</code> as the enabled representative; a reasoning record without a level list uses the conservative <code>low</code>/<code>medium</code>/<code>high</code> set. Entries whose input or output modalities are not text are not offered in the text agent model picker.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Settings and Composer can use a current broad provider/model catalog without shipping a large static matrix in the application.</li><li>Offline and unavailable-catalog operation remains possible through cached provider rows, pi-ai&#39;s bundled catalog, provider discovery, and free-form model IDs.</li><li>The remote catalog can update names and limits independently of an app release, so fixture-based parser and precedence tests are required.</li><li>models.dev does not define provider wire adapters. The selected API style and pi-ai compatibility data still govern request serialization; a catalog entry cannot grant an unsupported transport or image capability to an unknown ID.</li><li>A remote catalog outage is non-fatal and must not erase user-selected bindings or credentials.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><h3 id="keep-pi-ai-as-the-sole-catalog" tabindex="-1">Keep pi-ai as the sole catalog <a class="header-anchor" href="#keep-pi-ai-as-the-sole-catalog" aria-label="Permalink to &quot;Keep pi-ai as the sole catalog&quot;">​</a></h3><p>Rejected because its native catalog is narrower and can lag models.dev&#39;s market coverage, despite remaining the best local fallback and adapter source.</p><h3 id="fetch-models-dev-in-the-renderer" tabindex="-1">Fetch models.dev in the renderer <a class="header-anchor" href="#fetch-models-dev-in-the-renderer" aria-label="Permalink to &quot;Fetch models.dev in the renderer&quot;">​</a></h3><p>Rejected because catalog loading belongs to Electron main, keeps one network boundary, and avoids mixing provider credentials or remote response handling with renderer state.</p><h3 id="replace-provider-discovery-entirely" tabindex="-1">Replace provider discovery entirely <a class="header-anchor" href="#replace-provider-discovery-entirely" aria-label="Permalink to &quot;Replace provider discovery entirely&quot;">​</a></h3><p>Rejected because custom/local endpoints and authenticated vendor catalogs can expose models not present in a public catalog. Discovery remains the fallback for those cases.</p><h2 id="references" tabindex="-1">References <a class="header-anchor" href="#references" aria-label="Permalink to &quot;References&quot;">​</a></h2><ul><li><code>apps/desktop/electron/main/models-dev-catalog.ts</code></li><li><code>apps/desktop/electron/main/index.ts</code></li><li><code>packages/agent-runtime/src/model-capabilities.ts</code></li><li><code>docs/spec/03-runtime/11-provider-model-system.md</code></li><li><code>docs/spec/03-runtime/12-provider-config-schema.md</code></li><li><code>docs/spec/03-runtime/13-model-catalog-and-selection.md</code></li><li><code>docs/spec/06-delivery/04-e2e-test-plan.md</code> (E2E-066, E2E-080, E2E-154)</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0133-models-dev-primary-catalog-with-pi-fallback.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0133ModelsDevPrimaryCatalogWithPiFallback = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0133ModelsDevPrimaryCatalogWithPiFallback as default
};
