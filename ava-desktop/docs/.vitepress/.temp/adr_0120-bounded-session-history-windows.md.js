import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0120: Bounded Session History Windows","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0120-bounded-session-history-windows.md","filePath":"adr/0120-bounded-session-history-windows.md","lastUpdated":1787554742000}');
const _sfc_main = { name: "adr/0120-bounded-session-history-windows.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0120-bounded-session-history-windows" tabindex="-1">ADR 0120: Bounded Session History Windows <a class="header-anchor" href="#adr-0120-bounded-session-history-windows" aria-label="Permalink to &quot;ADR 0120: Bounded Session History Windows&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-08-24</li><li>Deciders: PI-Desktop core</li><li>Related: D119, ADR 0041, <code>03-runtime/04-data-storage.md</code>, <code>03-runtime/06-host-rpc-protocol.md</code></li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Opening a session previously read its complete JSONL transcript, projected every message, and sent the complete result through host-core, Electron, and the renderer. A single large pasted or tool-produced value could therefore make a 50 MB transcript allocation and block the desktop while a long session was being opened. Mount-time virtualization did not solve the problem because the full payload had already crossed the process boundary.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>Renderer-facing <code>session.get</code> supports an additive read window with <code>messageBefore</code>, <code>messageLimit</code>, and <code>contentLimit</code>. The host returns <code>messageStart</code> and <code>hasMoreBefore</code> so the renderer can page toward older messages while preserving the newest view.</li><li>Host-core reads JSONL with a buffered sequential parser and projects only the requested page. <code>contentLimit</code> truncates only the derived display projection, with an explicit marker; the lossless transcript on disk and the uncapped sidecar/model path remain authoritative.</li><li>The desktop opens sessions with the newest 100 messages and a 64 KiB per-message display budget. Scrolling near the top fetches older pages and preserves the viewport&#39;s scroll position. Deliberate full-history rewrites first rehydrate the complete transcript before replacing it, so a partial renderer window cannot delete older messages.</li><li>The uncapped default remains available to non-renderer callers, so model reconstruction and persistence semantics do not depend on UI pagination.</li></ol><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Only virtualize the message list:</strong> rejected because the expensive full parse and IPC payload would still happen before rendering.</li><li><strong>Store only a preview in the transcript:</strong> rejected because it loses user data and changes model context semantics.</li><li><strong>Use a database payload table for paging:</strong> rejected for now; the JSONL transcript remains the lossless source and a streaming window avoids a schema migration while fixing the boundary that caused the stall.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Session activation has bounded renderer memory and IPC work independent of the total visible history, apart from the sequential scan needed to locate the requested page.</li><li>Older history is available on demand, and display-only truncation is clear to the user while the complete content remains available to the agent.</li><li>Full-history edit, delete, revision-switch, and abort operations may perform a deliberate uncapped read before mutation; this preserves transcript data rather than allowing a partial UI window to overwrite older messages.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0120-bounded-session-history-windows.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0120BoundedSessionHistoryWindows = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0120BoundedSessionHistoryWindows as default
};
