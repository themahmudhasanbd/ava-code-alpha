import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0219: User-Invoked Skills in the Composer Slash Menu","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0219-user-invoked-skills-in-composer.md","filePath":"adr/0219-user-invoked-skills-in-composer.md","lastUpdated":1789092197000}');
const _sfc_main = { name: "adr/0219-user-invoked-skills-in-composer.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0219-user-invoked-skills-in-the-composer-slash-menu" tabindex="-1">ADR 0219: User-Invoked Skills in the Composer Slash Menu <a class="header-anchor" href="#adr-0219-user-invoked-skills-in-the-composer-slash-menu" aria-label="Permalink to &quot;ADR 0219: User-Invoked Skills in the Composer Slash Menu&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-11</li><li>Deciders: PI-Desktop runtime and desktop UI maintainers</li><li>Amends: D123, D174, ADR 0024, ADR 0039</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>D174 established the Skill catalog and local <code>Skill</code> tool as a model-invoked capability, and explicitly rejected user-facing slash commands. That keeps skill bodies out of the system prompt and preserves the existing permission, activation, and runtime-reload boundaries, but it also makes active skills undiscoverable when a user knows that a specific workflow should be applied.</p><p>The composer already merges several command sources through an Electron-only channel. It can expose a user-invoked entry without moving skill bodies into the renderer, prompt, or host protocol.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>Active built-in, plugin, and user Skills contribute entries to the composer slash menu. They appear in a separate <code>Skills</code> group after extension commands; the group is always last. The exact skill id is the slash name. Existing command names win collisions, so a Skill cannot shadow a template, builtin, plugin, or extension command.</li><li>Selecting a Skill inserts <code>/&lt;skill-id&gt; </code>. Sending follows the ordinary prompt path. Electron main resolves the command against the current session project, revalidates its active scope and permissions at send time, and keeps the typed slash form for the transcript chip.</li><li>Main persists a model-facing instruction asking the model to call the local <code>Skill</code> tool with the validated id, followed by any user body text. The Skill body is still loaded on demand by that tool; it is not sent to the renderer or injected directly into the prompt. <code>agent.prompt.inject</code> and existing user-Skill activation rules remain authoritative.</li><li>No host protocol or storage schema changes are required. The additive command contract permits <code>kind: &quot;skill&quot;</code> and <code>skillId</code>, and the existing composer-command IPC response carries the extra entries. Skill catalog, body-size, path, and runtime-reuse boundaries remain those of D174 and ADR 0039.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Users can explicitly discover and invoke an active Skill while the model can still choose Skills implicitly from the same catalog.</li><li>A Skill disabled or removed between autocomplete and send falls back to the existing literal slash-prompt behavior rather than bypassing activation.</li><li>The final autocomplete group may grow with the active Skill catalog, but its stable placement keeps existing command ordering predictable.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Inject the Skill body directly into the prompt:</strong> rejected because it bypasses the local <code>Skill</code> tool&#39;s scope checks, on-demand loading, and runtime reload semantics.</li><li><strong>Auto-trigger a Skill from the renderer:</strong> rejected because invocation and active-scope validation belong in Electron main, not in a renderer hint.</li><li><strong>Add a new host protocol or durable message schema:</strong> rejected because the existing Electron command channel and optional transcript command metadata are sufficient.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0219-user-invoked-skills-in-composer.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0219UserInvokedSkillsInComposer = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0219UserInvokedSkillsInComposer as default
};
