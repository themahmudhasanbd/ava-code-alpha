import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0129: The Subagent Idle Watchdog Bounds Silence, Not Slowness","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0129-idle-watchdog-bounds-silence.md","filePath":"adr/0129-idle-watchdog-bounds-silence.md","lastUpdated":1788992254000}');
const _sfc_main = { name: "adr/0129-idle-watchdog-bounds-silence.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0129-the-subagent-idle-watchdog-bounds-silence-not-slowness" tabindex="-1">ADR 0129: The Subagent Idle Watchdog Bounds Silence, Not Slowness <a class="header-anchor" href="#adr-0129-the-subagent-idle-watchdog-bounds-silence-not-slowness" aria-label="Permalink to &quot;ADR 0129: The Subagent Idle Watchdog Bounds Silence, Not Slowness&quot;">​</a></h1><ul><li>Status: Accepted for implementation (amended by ADR 0166: watchdogs no longer kill; the parent judges subagent lifetime)</li><li>Date: 2026-08-27</li><li>Deciders: PI-Desktop core</li><li>Related: D260 (amends D254), ADR 0119 (event-driven subagent timeouts), ADR 0089 (background delegation)</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>ADR 0119 replaced the delegate turn cap with an idle watchdog whose timer was re-armed by an allow-list of five event types. The list was the problem, not the policy: it decided liveness by event <em>kind</em>, so any <code>AgentEvent</code> variant outside it — present or future — was silently dropped, and reading the code did not answer the question the watchdog is supposed to answer. Whether a delegate that streams slowly stays alive was an incidental consequence of which types happened to be listed rather than a stated contract.</p><p>Session telemetry also showed the 600-second default was chosen without reference to what it measures. Because the timer is re-armed by streaming and paused across tool execution, the only interval it actually bounds is the wait between a delegate&#39;s last streamed token and its next response. In this project that wait measures 5.6s at p50, 13.4s at p90, 55s at p99, and 174s at p99.9 — an order of magnitude below 600s. Meanwhile 600s equals the <code>TaskWait</code> default, so a hung delegate could not settle inside a single wait, and the parent read an ordinary unfinished wait as if the delegation had failed.</p><p>Separately, the built-ins carried no turn backstop at all, so a delegate that looped without converging ran to the 6-hour duration ceiling.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>Every <code>AgentEvent</code> re-arms the idle timer, a single streamed token arriving as <code>message_update</code> included. The watchdog fires on total unresponsiveness and nothing else. The pause between <code>tool_execution_start</code> and its matching <code>tool_execution_end</code> is unchanged, as is the duration timer running through tool execution.</li><li>The default idle timeout is 300 seconds, sized from the measured 174-second p99.9 pre-token wait plus margin for the delegate&#39;s own provider retry backoff, which is silent by design. It must stay below the 600-second <code>TaskWait</code> default so a genuinely stuck delegate settles as <code>timed_out</code> inside one wait. The 21,600-second duration ceiling and the 10–21,600 override bounds are unchanged.</li><li><code>TaskWait</code> expiry reports &quot;Still running after Ns&quot; and states that this is not a failure and the unfinished delegates keep working.</li><li>The built-ins carry a turn backstop sized to their job: <code>explorer</code> 60, <code>code-reviewer</code> 50, <code>test-runner</code> 40, <code>fixer</code> 80. A non-converging delegate ends as <code>truncated</code> with its partial report instead of running to the duration ceiling.</li></ol><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Keep the event allow-list and only lower the default:</strong> rejected because it leaves liveness defined by an enumeration that must be revisited whenever <code>AgentEvent</code> grows a variant, and leaves the intended contract unstated.</li><li><strong>Emit synthetic activity across provider retry backoff:</strong> rejected for now. It is the more principled fix for the one interval the runtime itself chooses to wait, but it makes the watchdog blind to a provider that fails and backs off forever. Sizing the window above the retry envelope keeps one source of truth for liveness. Revisit if retry budgets grow.</li><li><strong>Raise the idle default to 900s and lengthen <code>TaskWait</code>:</strong> rejected because it inverts the relationship that matters — the idle window must be shorter than a wait, or a hang cannot surface inside one.</li><li><strong>Leave the built-ins unlimited:</strong> rejected because the duration ceiling is a 6-hour backstop, not a convergence signal; a looping delegate should return its partial work in minutes.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A delegate that keeps producing output is never idle-terminated, however slow its turn is. Only silence expires it, and the reason is legible in the code.</li><li>Two previously-survivable classes now expire at 300s instead of 600s: extreme provider latency outliers and a long chain of rate-limit backoff. Both return <code>timed_out</code> with the latest partial report rather than being lost.</li><li>A hung delegate surfaces within a single <code>TaskWait</code> instead of holding the parent for a full window and beyond.</li><li>Adding an <code>AgentEvent</code> variant no longer requires touching the watchdog.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0129-idle-watchdog-bounds-silence.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0129IdleWatchdogBoundsSilence = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0129IdleWatchdogBoundsSilence as default
};
