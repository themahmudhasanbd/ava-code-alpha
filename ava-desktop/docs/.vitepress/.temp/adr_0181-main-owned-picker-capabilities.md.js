import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0181: Main-owned picker capabilities","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0181-main-owned-picker-capabilities.md","filePath":"adr/0181-main-owned-picker-capabilities.md","lastUpdated":1788799210000}');
const _sfc_main = { name: "adr/0181-main-owned-picker-capabilities.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0181-main-owned-picker-capabilities" tabindex="-1">ADR 0181: Main-owned picker capabilities <a class="header-anchor" href="#adr-0181-main-owned-picker-capabilities" aria-label="Permalink to &quot;ADR 0181: Main-owned picker capabilities&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-08</li><li>Deciders: PI-Desktop core</li><li>Related: ADR 0059, ADR 0172, D197, D334, D344, <code>03-runtime/01-ipc-protocol.md</code> §13c, E2E-102h</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Composer attachments can be selected through a native Electron file picker and then copied into the owning session&#39;s scratch directory. The renderer needs to request the copy after it materializes a home draft, but a renderer IPC payload must not be treated as proof that a user selected the paths it contains.</p><p>The earlier picker flow returned native absolute paths to the renderer and then accepted those paths again through <code>composer/importFiles</code>. That allowed any renderer code with access to the bridge to request a copy of an arbitrary regular file, including outside the workspace and attachment roots. The same flow also advertised directory selection even though the importer accepted regular files only.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><code>composer/pickFiles</code> and <code>composer/pickPhotos</code> execute the native dialog in Electron main. Main stores the selected paths against a random token bound to the invoking <code>WebContents</code>.</li><li>The picker token expires after 60 seconds and is consumed before the import starts. <code>composer/importFiles</code> accepts the token and durable <code>sessionId</code>, never renderer-supplied source paths. A token cannot be replayed or used by another renderer WebContents.</li><li><code>pickFiles</code> offers regular files only in the MVP. Folder import remains a separate future feature that must define bounded traversal and ownership rules before it is exposed in the UI.</li><li>Main still realpaths and stats every recorded source, enforces the existing per-file and total-size limits, and copies only into the session scratch directory.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A compromised or stale renderer cannot turn the picker import channel into an arbitrary absolute-path copy primitive.</li><li>Canceling a picker does not create a draft session or write scratch files.</li><li>The renderer retains only the returned session-owned scratch references after import, while the source paths remain in Electron main.</li><li>Directory selection is no longer offered with a misleading unsupported label.</li></ul><h2 id="alternatives-rejected" tabindex="-1">Alternatives rejected <a class="header-anchor" href="#alternatives-rejected" aria-label="Permalink to &quot;Alternatives rejected&quot;">​</a></h2><ul><li>Returning native paths from the picker and validating them again in the import IPC: rejected because validation does not prove user selection.</li><li>Importing directly before returning from the picker IPC: rejected because the renderer must materialize a home draft only after a non-canceled selection.</li><li>Supporting folders by recursive copy in this change: rejected because it needs explicit traversal, size, symlink, and UI semantics beyond the current MVP.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0181-main-owned-picker-capabilities.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0181MainOwnedPickerCapabilities = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0181MainOwnedPickerCapabilities as default
};
