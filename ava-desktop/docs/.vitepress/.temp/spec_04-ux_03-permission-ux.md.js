import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"03. Permission UX","description":"","frontmatter":{},"headers":[],"relativePath":"spec/04-ux/03-permission-ux.md","filePath":"spec/04-ux/03-permission-ux.md","lastUpdated":1789523577000}');
const _sfc_main = { name: "spec/04-ux/03-permission-ux.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="_03-permission-ux" tabindex="-1">03. Permission UX <a class="header-anchor" href="#_03-permission-ux" aria-label="Permalink to &quot;03. Permission UX&quot;">​</a></h1><h2 id="_1-goal" tabindex="-1">1. Goal <a class="header-anchor" href="#_1-goal" aria-label="Permalink to &quot;1. Goal&quot;">​</a></h2><p>Make high-risk local actions visible, interruptible, and predictable.</p><h2 id="_2-mode-matrix" tabindex="-1">2. Mode matrix <a class="header-anchor" href="#_2-mode-matrix" aria-label="Permalink to &quot;2. Mode matrix&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Mode</th><th>Read/Glob/Grep</th><th>BrowserPreview</th><th>Write/Edit</th><th>Bash</th><th>Plugins</th></tr></thead><tbody><tr><td>Agent</td><td>allow</td><td>allow</td><td>permission policy</td><td>permission policy</td><td>registered risk policy</td></tr><tr><td>Plan</td><td>allow</td><td>allow</td><td>deny</td><td><code>ask</code>/<code>accept-edits</code>: confirm; <code>auto</code>: allow</td><td>deny</td></tr><tr><td>Goal</td><td>allow</td><td>allow</td><td>deny</td><td><code>ask</code>/<code>accept-edits</code>: confirm; <code>auto</code>: allow</td><td>deny</td></tr></tbody></table><p>The Read/Glob/Grep <code>allow</code> cells apply to paths inside the session workspace and scratch roots. An explicit path outside both roots is an exception: <code>auto</code> allows it, while <code>ask</code> and <code>accept-edits</code> show the same inline card as other permission-gated tools. The card&#39;s argument preview includes the requested path, and the external result remains absolute in the transcript.</p><p>Decision source: <strong>D003/D189/D190/D195 (ADR 0057)</strong>.</p><p>Plan and Goal keep this permission-mode control visible. Plan exposes the effective permission choice; Goal shows the same chip geometry with a fixed Auto label in the Composer and does not open a menu. Goal&#39;s approval card is the separate place to choose the execution permission for the approved run. They are contract intents, not strict read-only security profiles: a Bash command can mutate workspace or scratch state under Auto. Write/Edit/plugin tools are denied by the host before a permission card, regardless of grants or Auto.</p><h2 id="_3-decision-types" tabindex="-1">3. Decision types <a class="header-anchor" href="#_3-decision-types" aria-label="Permalink to &quot;3. Decision types&quot;">​</a></h2><ul><li><code>allow-once</code></li><li><code>allow-session</code> (scoped by toolName, <strong>D006</strong>)</li><li><code>deny</code></li></ul><p>No <code>allow-always</code> in MVP.</p><h2 id="_4-permission-card-states" tabindex="-1">4. Permission card states <a class="header-anchor" href="#_4-permission-card-states" aria-label="Permalink to &quot;4. Permission card states&quot;">​</a></h2><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>pending → allowed_once</span></span>
<span class="line"><span>pending → allowed_session</span></span>
<span class="line"><span>pending → denied</span></span>
<span class="line"><span>pending → timeout_denied</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><ul><li>A request is keyed by both <code>sessionId</code> and <code>requestId</code>.</li><li>A session without subagents has at most one pending request, because its agent loop is paused. Parallel delegates (§6a) can put more than one in flight; different sessions still wait for independent approvals.</li><li>Replacing or resolving one request never removes another session&#39;s request or a newer request in the same session.</li></ul><h2 id="_5-timeout" tabindex="-1">5. Timeout <a class="header-anchor" href="#_5-timeout" aria-label="Permalink to &quot;5. Timeout&quot;">​</a></h2><ul><li>Default timeout: <strong>120 seconds</strong></li><li>On timeout: auto <code>deny</code></li><li>UI shows timeout state explicitly</li><li>Agent receives tool error result: user denied / timed out</li></ul><h2 id="_6-card-content-requirements" tabindex="-1">6. Card content requirements <a class="header-anchor" href="#_6-card-content-requirements" aria-label="Permalink to &quot;6. Card content requirements&quot;">​</a></h2><p>Must show:</p><ol><li>tool name</li><li>risk level</li><li>short reason</li><li>args preview (redacted if needed)</li><li>workspace context</li><li>actions: Allow once / Allow for session / Deny</li></ol><p>The card is rendered inline only in its originating session&#39;s transcript. Background requests remain pending without opening an overlay, changing the active page/project/session, or moving keyboard focus. Opening that session reveals its card with the original absolute countdown deadline.</p><p>Resolving a request never initiates navigation. Any resulting tool artifact is recorded in the same session&#39;s retained work-panel context. If that session is backgrounded before completion, the artifact must not open or resize the visible panel; explicitly returning to the session restores its retained panel open state, tabs, active tab, and Browser resource without a transient panel open/close cycle in the intervening conversation.</p><h2 id="_6a-queued-requests-from-parallel-delegates-d201-adr-0062" tabindex="-1">6a. Queued requests from parallel delegates (D201, ADR 0062) <a class="header-anchor" href="#_6a-queued-requests-from-parallel-delegates-d201-adr-0062" aria-label="Permalink to &quot;6a. Queued requests from parallel delegates (D201, ADR 0062)&quot;">​</a></h2><p>Parallel subagents can each stop on a gated tool at the same moment, so a session holds a <strong>queue</strong> of pending requests, oldest first, not a single slot. The alternative — a stack of cards for calls the user cannot tell apart — is not answerable.</p><ul><li>Only the head of the queue is rendered and answerable. The rest wait invisibly; their delegates stay blocked, which is the intended back-pressure.</li><li>Answers are matched by <code>requestId</code>, never by position, so a late answer can only clear the request it answered and can never resolve a successor.</li><li>A request the host closed itself (expiry, cancelled tool call) is removed by <code>toolCallId</code> from anywhere in the queue, so a card that was never shown still leaves. The 120s deadline (§5) runs from arrival for every request, queued or not — a request can therefore expire while waiting, and the host&#39;s own denial is what the delegate sees.</li><li>Abort denies the <strong>whole</strong> queue, not just the visible card: a queued delegate would otherwise keep its tool call alive behind a stop the user already asked for.</li><li>A session with nothing pending has no queue at all, so &quot;does this session need attention&quot; stays a presence check and the sidebar indicator is unchanged.</li></ul><p>The card adds two lines of provenance when they apply, on top of §6:</p><ul><li>who asked — &quot;Asked by the <code>&lt;agent&gt;</code> subagent&quot; — present only for a delegate&#39;s request, so a parent&#39;s own request looks exactly as it does today;</li><li>how many wait behind it — &quot;N more request(s) are waiting&quot; — so answering does not look like it finished the session&#39;s questions.</li></ul><p>Session grants are unchanged and still per <code>toolName</code> per session: a delegate&#39;s &quot;Allow for session&quot; also covers the parent and the other delegates (<code>03-runtime/03-tools-and-permissions.md</code> §10.2).</p><h2 id="_7-composer-interaction-while-pending" tabindex="-1">7. Composer interaction while pending <a class="header-anchor" href="#_7-composer-interaction-while-pending" aria-label="Permalink to &quot;7. Composer interaction while pending&quot;">​</a></h2><ul><li>user may continue editing text</li><li>sending another prompt or changing active-session mode/provider/model/permission is blocked while a <code>pending</code> Plan or Goal approval exists</li><li>For a pending Plan or Goal proposal specifically, the existing draft is preserved and the prompt becomes read-only. Composer mode, thinking, permission, model, and send controls are disabled; the approval surface&#39;s Approve and Reject actions remain enabled.</li><li>the left-of-input Composer Agent/Plan/Goal chip and Composer model × reasoning picker re-enable when the host closes the approval as rejected, expired, or interrupted; terminal proposal snapshots are not gates</li><li>Abort concurrently cancels the turn and explicitly denies the matching host permission request; late cleanup cannot clear a replacement request</li><li>another session remains independently editable/runnable and its own pending request is unaffected</li></ul><h2 id="_8-session-grants-surface" tabindex="-1">8. Session grants surface <a class="header-anchor" href="#_8-session-grants-surface" aria-label="Permalink to &quot;8. Session grants surface&quot;">​</a></h2><p>Active session grants (toolName, grantedAt, clear action) remain runtime-owned. A durable grants-management surface is deferred until a host-backed settings schema exists; Settings must not render a control that cannot persist or affect the permission runtime.</p><h2 id="_9-plan-and-goal-contract-approval-card" tabindex="-1">9. Plan and Goal contract approval card <a class="header-anchor" href="#_9-plan-and-goal-contract-approval-card" aria-label="Permalink to &quot;9. Plan and Goal contract approval card&quot;">​</a></h2><p>Plan and Goal approval are not generic tool permission cards. They are rendered inline in the originating session after <code>SubmitPlan(...)</code> or <code>SubmitGoal(...)</code> causes host-core to preserve the exact Markdown bytes in a new immutable <code>.pi/plan/*.md</code> or <code>.pi/goal/*.md</code> artifact. The card sits in the transparent composer dock, so it paints <code>--ds-bg-composer</code> with <code>--ds-shadow-composer</code> rather than the in-flow <code>--ds-tile</code> wash.</p><p>The card shows only the structured title and an opener for the exact artifact path. It does not render the submitted question/description, status, or approval validity/deadline. It offers only:</p><ul><li><strong>Approve</strong> with an explicit target permission mode (<code>Ask</code>, <code>Accept edits</code>, or <code>Auto</code>; the last selected mode is remembered on this device and is the default for the next approval)</li><li><strong>Reject</strong>, which stops the run and leaves the contract state active; a later turn must submit a new complete snapshot/artifact</li></ul><p>The card has distinct <code>pending</code>, <code>resolving</code>, <code>approved</code>, <code>queued</code>, <code>running</code>, <code>rejected</code>, <code>expired</code>, and <code>interrupted</code> states. Approval is enabled only for a matching live proposal/session/turn/tool-call/version request and the deadline is exactly 30 minutes from creation. Renderer state retains the latest proposal/execution snapshot per session only for the current renderer lifetime, driven by live Host events; only <code>pending</code> is actionable or a Composer gate. Renderer reload may restore a still-pending row without resetting its deadline while the same Host remains alive, but does not rehydrate rejected, expired, approved/completed, or interrupted terminal cards. Such a card may remain visible and non-actionable only until reload. Startup interruption marks pending and queued/running work interrupted before RPC service, offers no stale action, and never replays it. Expiry uses <code>PLAN_APPROVAL_TIMEOUT</code>. An already-approved interrupted run keeps the session in Agent; the UI is not required to present its interrupted terminal snapshot after a full Host/app restart.</p><h2 id="_10-acceptance" tabindex="-1">10. Acceptance <a class="header-anchor" href="#_10-acceptance" aria-label="Permalink to &quot;10. Acceptance&quot;">​</a></h2><ol><li>Plan and Goal deny Write/Edit/plugins in every permission mode</li><li>Plan and Goal Bash prompt under Ask and Accept edits and run without confirmation under Auto, with the mutation tradeoff visible</li><li>Agent mode uses the normal high-risk permission policy</li><li>timeout becomes deny in UI + tool result</li><li>allow-session suppresses repeat prompts for same toolName only</li><li>concurrent session requests remain isolated and never take over the visible conversation or its work panel; post-approval artifacts remain assigned to the request&#39;s originating session</li><li>Plan/Goal approval displays only the title and artifact opener, remembers the selected permission mode for the next approval on this device, and sends that mode only on approval; no question/description, validity/deadline, status, inline Markdown/hash/byte-size, or revision/feedback action is rendered</li><li>reject, expiry, abort, crash, stale response, and persistence failure close pending Plan/Goal work in its contract state with no execution capability; a later prompt may revise and submit a new immutable artifact</li><li>host restart interrupts pending/queued/running work without replay or stale action and keeps already-approved interrupted sessions in Agent; no terminal card restoration is required after restart</li></ol></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("spec/04-ux/03-permission-ux.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _03PermissionUx = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _03PermissionUx as default
};
