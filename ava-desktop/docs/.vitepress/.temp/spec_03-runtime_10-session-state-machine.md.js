import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"10. Session, Plan, and Goal State Machine","description":"","frontmatter":{},"headers":[],"relativePath":"spec/03-runtime/10-session-state-machine.md","filePath":"spec/03-runtime/10-session-state-machine.md","lastUpdated":1789398369000}');
const _sfc_main = { name: "spec/03-runtime/10-session-state-machine.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="_10-session-plan-and-goal-state-machine" tabindex="-1">10. Session, Plan, and Goal State Machine <a class="header-anchor" href="#_10-session-plan-and-goal-state-machine" aria-label="Permalink to &quot;10. Session, Plan, and Goal State Machine&quot;">​</a></h1><h2 id="_0-durable-operating-mode-versus-live-planning-state" tabindex="-1">0. Durable operating mode versus live planning state <a class="header-anchor" href="#_0-durable-operating-mode-versus-live-planning-state" aria-label="Permalink to &quot;0. Durable operating mode versus live planning state&quot;">​</a></h2><p>Each session persists exactly one operating mode: <code>agent | plan | goal</code>. There is one pi Agent. Live planning state and execution status are host/runtime projections. Plan and Goal are <strong>contract modes</strong>: both negotiate a proposal before executing and share one projection, one approval row, and one hard deny (<strong>D198</strong>). <code>kind</code> (<code>plan | goal</code>) is what distinguishes them, and the diagram below reads the same with Goal/<code>SubmitGoal</code> substituted for Plan/<code>SubmitPlan</code>:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Agent / inactive</span></span>
<span class="line"><span>  -- user selects Plan while idle OR Agent calls EnterPlanMode --&gt; Plan / planning</span></span>
<span class="line"><span>  -- user selects Goal while idle OR Agent calls EnterGoalMode --&gt; Goal / planning</span></span>
<span class="line"><span>Plan | Goal / planning</span></span>
<span class="line"><span>  -- SubmitPlan | SubmitGoal (title, markdown, question) --&gt; awaiting_approval</span></span>
<span class="line"><span>Plan | Goal / awaiting_approval</span></span>
<span class="line"><span>  -- approve(permission mode) --&gt; Agent / queued, same Agent continues</span></span>
<span class="line"><span>  -- reject | expiry | abort | crash | persistence failure</span></span>
<span class="line"><span>       --&gt; same contract mode / planning</span></span>
<span class="line"><span>Agent / queued</span></span>
<span class="line"><span>  -- dispatcher starts --&gt; Agent / running</span></span>
<span class="line"><span>Agent / running</span></span>
<span class="line"><span>  -- complete | fail | abort --&gt; Agent / inactive</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br></div></div><p>Both contract modes retain the permission-mode selector. Their <code>Bash</code> policy is <code>ask</code> or <code>accept-edits</code> = confirmation and <code>auto</code> = no confirmation, so a contract mode expresses negotiating intent but is not a strict read-only security profile. Write/Edit and plugin tools remain denied by host policy in every Plan or Goal permission mode.</p><p>The kinds differ only in what the contract says and what the queued execution instruction asks for: Plan proposes ordered steps to carry out, while Goal proposes a goal statement, acceptance criteria, and boundaries, and its execution keeps working — choosing its own approach — until every acceptance criterion is verified or a boundary blocks it.</p><p>Mode and configuration changes through the UI/session API are allowed only while idle. Approval is not a generic tool permission: it is a separate host-owned state transition. A host restart interrupts every pending approval and queued/running execution field without replay; an already-approved interruption keeps the durable session in Agent.</p><h2 id="_1-session-status" tabindex="-1">1. Session status <a class="header-anchor" href="#_1-session-status" aria-label="Permalink to &quot;1. Session status&quot;">​</a></h2><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>idle &lt;-&gt; running &lt;-&gt; waiting_permission</span></span>
<span class="line"><span>           \\/ aborted</span></span>
<span class="line"><span>           \\/ error</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><table tabindex="0"><thead><tr><th>status</th><th>meaning</th></tr></thead><tbody><tr><td><code>idle</code></td><td>no active turn</td></tr><tr><td><code>running</code></td><td>model/tool turn active</td></tr><tr><td><code>waiting_permission</code></td><td>blocked on user permission decision</td></tr><tr><td><code>aborted</code></td><td>terminal for current turn (then returns idle)</td></tr><tr><td><code>error</code></td><td>terminal for current turn (then returns idle)</td></tr></tbody></table><h2 id="_2-turn-lifecycle" tabindex="-1">2. Turn lifecycle <a class="header-anchor" href="#_2-turn-lifecycle" aria-label="Permalink to &quot;2. Turn lifecycle&quot;">​</a></h2><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>accept_prompt</span></span>
<span class="line"><span> -&gt; turn_start</span></span>
<span class="line"><span> -&gt; streaming</span></span>
<span class="line"><span> -&gt; (optional tool_loop)</span></span>
<span class="line"><span>   -&gt; permission_maybe</span></span>
<span class="line"><span>   -&gt; tool_exec</span></span>
<span class="line"><span> -&gt; turn_end</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p>A turn reaches one of three terminal reasons — <code>completed</code>, <code>aborted</code>, or <code>error</code> — matching the <code>aborted</code> / <code>error</code> rows in section 1. The terminal reason is decided once: an abort records its decision before the cancel request is issued, so a later <code>agent_end</code> cannot restate an aborted turn as completed. Terminal events are attributed by turn identity, not by session: a terminal event whose turn no longer owns the session changes neither the current turn&#39;s state nor its resources, and late message and tool rows are still recorded as history. The host announces the terminal state once per started turn through the <code>session:turnEnded</code> plugin event (see ADR 0252, <code>docs/adr/0252-plugin-host-turn-end-event.md</code>).</p><p>A steering input is judged by the same identity: one that names a turn which was cancelled, has started finalizing, or no longer owns the session is refused as a turn that has ended.</p><h2 id="_3-transition-rules" tabindex="-1">3. Transition rules <a class="header-anchor" href="#_3-transition-rules" aria-label="Permalink to &quot;3. Transition rules&quot;">​</a></h2><ol><li>Only one active turn per session</li><li>A direct host prompt is rejected with <code>AGENT_BUSY</code> while running/waiting_permission. The renderer&#39;s Send-while-running path pushes the next prompt into the Host-owned turn queue (schema v15, D375 / D386 / ADR 0213) through <code>agent/queue/push</code> and mirrors the durable entries from <code>agent/event/queueChanged</code>; the Agent Host module releases one entry after <code>agent_end</code>, holds a restored queue until the owner attaches, and moves an entry to the head on <code>agent/queue/prioritize</code>, so normal user sends do not surface <code>AGENT_BUSY</code>.</li><li>A graceful stop completes the current assistant/tool boundary as a normal <code>completed</code> turn before the renderer releases a queued prompt.</li><li>Abort from running or waiting_permission is allowed. Renderer smart Stop removes an unanswered root user row and restores its session/turn-scoped pre-serialization composer snapshot; once assistant text, thinking, or any tool row begins, abort preserves the partial transcript and restores no draft. The snapshot keeps structured file/image references and is never reconstructed by parsing model-facing <code>@path</code> text.</li><li>Permission timeout moves to tool denied, then agent may continue or end based on runtime handling</li><li>Session status returns to idle after terminal turn states are persisted</li><li>Changing the renderer&#39;s active project/session does not transition or abort any background session</li><li>A tool transition retains the originating session&#39;s persisted project root, or that session&#39;s own scratch root when it is temporary; it never adopts the newly active project&#39;s root</li><li><code>session.endTurn</code> moves only a <code>running</code> turn to terminal. In that same transaction, unseen <code>completed</code> inserts <code>task.completed</code>, unseen <code>error</code> inserts <code>task.failed</code>, and a result already visible in the focused current chat or any <code>aborted</code> turn inserts no notification (D117). Repeated terminal calls are no-ops. Renderer terminal lifecycle events update the transcript and turn result card; the sidebar terminal mark is derived only from the corresponding unread notification, never from <code>agent_end</code> alone.</li><li>Fork is allowed only while the source is idle. The child begins idle with no turn or waiting-permission state. Electron returns <code>AGENT_BUSY</code> for its active runtime guard and normalizes the host&#39;s persisted running-turn <code>CONFLICT</code> fallback to the same IPC error. Neither path produces a partial child.</li><li>Supplying <code>throughMessageId</code> changes only the snapshot boundary. Assistant Fork/Edit still creates a new idle session id with no shared turn, permission wait, runtime, or provider-cache state (D134).</li><li><code>EnterPlanMode</code>, <code>EnterGoalMode</code>, <code>SubmitPlan</code>, and <code>SubmitGoal</code> must be the only tool call in their assistant batch. A submit tool preserves exact Markdown bytes in a new host-owned <code>.pi/&lt;kind&gt;/*.md</code> artifact and creates one pending <code>plan_approvals</code> row with its <code>kind</code> plus structured title/question and artifact fields. A submit tool called against the other kind&#39;s mode fails with <code>PLAN_KIND_MISMATCH</code> and writes nothing.</li><li>Only a matching <code>plans.resolve</code> can settle a pending proposal. Approval atomically changes the durable mode to Agent, stores the selected explicit permission mode, assigns an execution ID, and changes the row&#39;s <code>execution_state</code> to <code>queued</code>.</li><li>Approve and reject are the only resolution actions. Rejection and expiry close the pending row, then return the live state to the editable planning state of the same contract mode and grant no execution tools. A pending interruption does the same; a queued/running interruption after approval stays Agent.</li><li>A second prompt, Plan or Goal submission, configuration change, or execution is rejected while the session has an active turn, pending approval, or queued/running execution. Configuration is accepted only while idle.</li><li>A later turn in the same contract mode may revise a rejected/expired/interrupted checkpoint and must create a new immutable artifact rather than overwrite the earlier snapshot.</li><li>A terminal parent provider/stream error aborts leftover delegates and returns the session to idle so Continue is accepted. Parent idle with running delegates still keeps the turn open (D328 / D352).</li></ol><h2 id="_4-persistence-points" tabindex="-1">4. Persistence points <a class="header-anchor" href="#_4-persistence-points" aria-label="Permalink to &quot;4. Persistence points&quot;">​</a></h2><p>Message persistence is two-step per 04-data-storage §5 (D119): fsync&#39;d transcript-file line first, index transaction second.</p><ul><li>user message: on accept, including attachment kind/name/MIME/size and a content-addressed image ref when applicable; transient base64 is never a persistence field</li><li>turn run row: on start + terminal <code>session.endTurn</code> update</li><li>notification row: same transaction as an unseen completed/error terminal update; never for a visible-current result or abort</li><li>assistant/tool messages: on message_end/tool_end. Electron retains each in-flight tool&#39;s metadata with its owning turn until the terminal event is persisted; delayed cleanup is scoped to that turn so a later turn&#39;s long <code>TaskWait</code> cannot lose its name, args, or duration. A completed tool row is replayed through <code>message_end</code> as a renderer recovery path, allowing a reload that dropped the running row to append the terminal message. The finished assistant snapshot is checkpointed before the outbox append; handshake awaits that drain before a cold <code>session.get</code> (D327). Renderer revalidation of a running or just-completed session stitches the bounded durable page onto the live snapshot in chronological order so older live rows stay before that page and the in-flight or not-yet-flushed tail stays after it (D317, D324). Live provenance is cleared only once that page already contains every live row.</li><li>orphaned session restore: a live JSONL whose sessions row is gone is reinserted at host boot and on <code>session.appendMessage</code>; a queued outbox drains after that restore, and <code>session.delete</code> drops that session&#39;s outbox entries (D318)</li><li>unanswered smart Stop: mark the turn aborted through the existing lifecycle, then atomically rewrite the transcript to the prefix before its root user row; the structured composer snapshot remains renderer-memory-only</li><li>mode/project fields: on change</li><li>temporary-session tool binding: a path-less session uses its own <code>&lt;data_dir&gt;/scratch/&lt;sessionId&gt;</code> root while keeping <code>projectPath</code> absent; Plan/Goal workspace validation continues to require a persisted project</li><li>Plan/Goal submission: write exact Markdown bytes to a new unique <code>.pi/&lt;kind&gt;/*.md</code>, record path/hash/size plus the kind and structured title/question, and insert a <code>pending</code><code>plan_approvals</code> row before the approval event</li><li>Plan/Goal approval: approval outcome, mode transition, permission mode, execution ID, and <code>queued</code> state in one transaction; reject/expiry/interruption retain the contract mode and return live planning to editable state</li><li>startup recovery: transactionally interrupt pending approvals and queued/running execution states before serving RPC; abort associated running turns and never replay work</li><li>fork snapshot: new transcript file plus one child session/index transaction; source persistence remains untouched; a message-scoped snapshot ends inclusively at the selected message</li></ul><h2 id="_5-acceptance" tabindex="-1">5. Acceptance <a class="header-anchor" href="#_5-acceptance" aria-label="Permalink to &quot;5. Acceptance&quot;">​</a></h2><ol><li>Busy session cannot start second concurrent turn</li><li>Abort is idempotent</li><li>waiting_permission is visible in UI status</li><li>sessions in two retained project tabs may run independently without transcript-event or workspace-root crossover</li><li>each unseen completed/failed turn produces exactly one notification record while a visible-current result or aborted turn produces none</li><li>an idle fork starts as an independent idle session; a busy source cannot produce a child</li><li>a message-scoped fork excludes later rows and begins with no source runtime or provider-cache state</li><li>a running session can queue removable FIFO prompts per session; Send now completes the current turn at the next boundary, while immediate Abort leaves the queue intact</li><li>Plan, Goal, and Agent use one pi Agent; the Composer-left mode chip, UI entry, and <code>EnterPlanMode</code>/<code>EnterGoalMode</code> converge on the same planning state, and approval resumes that Agent in Agent mode</li><li>Contract-mode policy permits Bash only through the selected permission mode and denies Write/Edit/plugins regardless of <code>auto</code> or session grants, in Goal exactly as in Plan</li><li>SubmitPlan/SubmitGoal writes an exact unique <code>.pi/&lt;kind&gt;/*.md</code> artifact with hash/size, keeps title/question structured, and only approve/reject can resolve its <code>plan_approvals</code> row</li><li>Expiry uses <code>PLAN_APPROVAL_TIMEOUT</code>; startup interruption, shell failure, and process recovery are fail closed, and restart does not replay pending, queued, or running work</li><li>A Goal execution reports each acceptance criterion&#39;s outcome before ending the turn, and a scheduled/unattended Goal run is rejected with <code>PLAN_REQUIRES_INTERACTIVE_SESSION</code> exactly like Plan</li></ol></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("spec/03-runtime/10-session-state-machine.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _10SessionStateMachine = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _10SessionStateMachine as default
};
