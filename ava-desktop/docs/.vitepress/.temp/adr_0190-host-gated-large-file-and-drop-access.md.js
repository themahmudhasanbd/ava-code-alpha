import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0190: Host-gated large-file and dropped-file access","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0190-host-gated-large-file-and-drop-access.md","filePath":"adr/0190-host-gated-large-file-and-drop-access.md","lastUpdated":1788893044000}');
const _sfc_main = { name: "adr/0190-host-gated-large-file-and-drop-access.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0190-host-gated-large-file-and-dropped-file-access" tabindex="-1">ADR 0190: Host-gated large-file and dropped-file access <a class="header-anchor" href="#adr-0190-host-gated-large-file-and-dropped-file-access" aria-label="Permalink to &quot;ADR 0190: Host-gated large-file and dropped-file access&quot;">​</a></h1><ul><li><strong>Status:</strong> Accepted</li><li><strong>Date:</strong> 2026-09-09</li><li><strong>Deciders:</strong> PI-Desktop maintainers</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The log-viewer plugin needs byte-range reads and file metadata to page and follow very large logs. Its previous implementation used <code>node:fs</code> in the plugin process for those operations and used a renderer worker to read dropped <code>File</code> snapshots. That split the engine and left the file-range path outside the host permission gateway.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Add host-owned <code>fs.stat</code> and bounded <code>fs.readRange</code> APIs under the existing <code>fs.read</code> root, scope, symlink, protected-path, deny-list, consent, and audit checks. A range call is capped at 8 MiB and returns <code>{ bytes, totalSize }</code>.</p><p>Expose <code>webUtils.getPathForFile</code> through the panel preload as <code>pluginBridge.getDroppedFilePath</code>. The panel host records paths from a real drop gesture for a short time and lets <code>fs.registerDropped</code> consume one path once. The runtime issues a memory-only grant for exactly that canonical regular file. The grant is read-only, cannot be used with another path, is rechecked on every access, and dies with the plugin process.</p><p>The log viewer uses the host APIs for both picked and dropped files. It has no raw <code>node:fs</code> fallback and does not provide export or write functionality.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Large-file indexing, pagination, search, follow, and rotation share one permission-gated engine.</li><li>Dropped files can follow appends during the current plugin session.</li><li>Dropped-file grants do not become manifest scope and are not persisted.</li><li>Older hosts without the new APIs cannot load this plugin version; it fails closed instead of silently bypassing the gateway.</li><li>The real drag-and-drop flow still requires desktop gesture testing; unit and integration tests cover the grant, path, and byte-range boundaries.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li>Keep raw <code>node:fs</code> in the plugin: rejected because it bypasses host policy.</li><li>Keep renderer <code>Blob.slice</code> worker reads: rejected because it duplicates the engine and cannot observe file growth or preserve a host file identity.</li><li>Add a broad user-selected root for dropped files: rejected because a single dropped file should not authorize its containing directory.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0190-host-gated-large-file-and-drop-access.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0190HostGatedLargeFileAndDropAccess = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0190HostGatedLargeFileAndDropAccess as default
};
