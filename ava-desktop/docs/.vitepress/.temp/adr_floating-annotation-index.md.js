import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR floating-annotation-index: Floating Annotation Index and Source Locations","description":"","frontmatter":{},"headers":[],"relativePath":"adr/floating-annotation-index.md","filePath":"adr/floating-annotation-index.md","lastUpdated":1789547461000}');
const _sfc_main = { name: "adr/floating-annotation-index.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-floating-annotation-index-floating-annotation-index-and-source-locations" tabindex="-1">ADR floating-annotation-index: Floating Annotation Index and Source Locations <a class="header-anchor" href="#adr-floating-annotation-index-floating-annotation-index-and-source-locations" aria-label="Permalink to &quot;ADR floating-annotation-index: Floating Annotation Index and Source Locations&quot;">​</a></h1><blockquote><p>Superseded by ADR 0268.</p></blockquote><ul><li>Status: Superseded by ADR 0268</li><li>Date: 2026-09-12</li><li>Amends: ADR response-annotations / D-LOCAL-response-annotations (annotation presentation and duplicate identity)</li><li>Preserves: D209, D301, D261, D-LOCAL-selection-overlay; protocol v11 and schema v15</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The comment editor solved missing comment entry, but a composer-only attachment list did not show where selections were made. The user explicitly requested a Codex-like collapsible floating panel with annotation numbers and source locations. The reference bundle uses out-of-flow numbered source buttons; inserting marker text into Markdown previously broke words, selections, and formulas.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>Replace the composer attachment popover with one floating annotation index in the visible, writable transcript pane, above the composer. It starts expanded, collapses to its count header, and stays open during reading. It lists numbered excerpts, comments, locate, edit, remove, and clear-all controls. Collapsing does not remove annotations or source badges. The existing modal comment editor stays.</li><li>Source badges use the same one-based array order as the list and prompt payload. Every saved, resolvable selection stays highlighted by a non-interactive overlay while visible, without a click and even when the index is collapsed. Clicking a list item or badge navigates within the pane; it does not switch off other highlights. Removal/clear/send removes the corresponding highlights; an unresolved row fallback never highlights the whole answer. The transcript releases follow-to-bottom first; an older source expands the mounted history window by at most D261&#39;s 40 rows per frame or loads older pages, stopping on exhaustion, error, or no progress.</li><li>A renderer-only optional anchor stores start/end offsets and exact text from answer text nodes, captured before selection collapse. Code chrome, KaTeX&#39;s duplicate MathML, and citation markers are excluded; formula endpoints expand to the whole formula. Resolve at the original offset first; a shifted selection relocates only when its exact text is unique. An unresolved selection falls back to its source row, with a row-location tooltip rather than a false exact match.</li><li>Deduplication uses source row, excerpt, and selected offsets. Repeating the same selection edits it; identical words at distinct locations or in different turns remain separate annotations with separate numbers. When either entry point lacks offsets, the location is unknown, not a distinct occurrence: the same row/excerpt reopens the existing annotation. Editing preserves anchors.</li><li>Source badges/highlights are portaled outside the answer DOM and cannot change answer layout, serialized Markdown, clipboard content, or prompt text. They follow scroll, resize, and content mutations, clipped above the composer. Coincident badges stack; badges that cannot fit the visible band are omitted from that band only, while every annotation remains reachable in the index.</li><li>Hidden retained panes and read-only side-chat projections render no annotation overlays. State and numbering remain session-owned. Send/clear/remove use the existing attachment lifecycle; nothing is persisted or retained after send. Anchor metadata is explicitly omitted from the existing prompt payload.</li></ol><h2 id="scope" tabindex="-1">Scope <a class="header-anchor" href="#scope" aria-label="Permalink to &quot;Scope&quot;">​</a></h2><p>No new dependency, IPC, host command, schema, storage, or permission. The user has not requested persistent review history or movable/native windows; the panel is an in-app floating surface. Model-authored citation rendering is unchanged and is separate from these user-owned source badges.</p><h2 id="validation" tabindex="-1">Validation <a class="header-anchor" href="#validation" aria-label="Permalink to &quot;Validation&quot;">​</a></h2><p>Executable tests cover duplicate occurrences, ambiguous/stale anchors, split text nodes, comment/anchor payload separation, collision placement, and floating-panel toggle/locate/edit/remove handlers. Source contracts cover pane visibility and history navigation. E2E-CHAT-annotation-source-index documents real-app checks; no E2E or GUI run is claimed.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/floating-annotation-index.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const floatingAnnotationIndex = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  floatingAnnotationIndex as default
};
