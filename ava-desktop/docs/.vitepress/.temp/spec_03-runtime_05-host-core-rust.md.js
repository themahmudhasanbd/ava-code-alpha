import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"05. Rust Host Core","description":"","frontmatter":{},"headers":[],"relativePath":"spec/03-runtime/05-host-core-rust.md","filePath":"spec/03-runtime/05-host-core-rust.md","lastUpdated":1789302449000}');
const _sfc_main = { name: "spec/03-runtime/05-host-core-rust.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="_05-rust-host-core" tabindex="-1">05. Rust Host Core <a class="header-anchor" href="#_05-rust-host-core" aria-label="Permalink to &quot;05. Rust Host Core&quot;">​</a></h1><h2 id="_1-purpose" tabindex="-1">1. Purpose <a class="header-anchor" href="#_1-purpose" aria-label="Permalink to &quot;1. Purpose&quot;">​</a></h2><p><code>host-core</code> is the privileged local backend of PI-Desktop.</p><p>It does <strong>not</strong> replace pi. It provides safe host capabilities to:</p><ul><li>Electron shell</li><li>pi agent runtime</li><li>plugin system</li></ul><h2 id="_2-responsibilities" tabindex="-1">2. Responsibilities <a class="header-anchor" href="#_2-responsibilities" aria-label="Permalink to &quot;2. Responsibilities&quot;">​</a></h2><ol><li>Workspace path canonicalization, boundary checks, and permission-gated explicit outside paths</li><li>Builtin tool execution (Read/Glob/Grep/Write/Edit/Bash)</li><li>Authoritative durable session-mode and tool-policy evaluation</li><li>Permission policy evaluation, including Plan/Goal Bash prompts</li><li>Immutable <code>.pi/plan/*.md</code> and <code>.pi/goal/*.md</code> artifact writer, <code>plan_approvals</code> broker, and startup interruption fence</li><li>Selectable shell catalog, identity validation, streamed output, and process tree shutdown</li><li>Plugin registry/install/lifecycle services</li><li>Contribution registration bookkeeping (with TS side)</li><li>Persistence adapters (sessions/settings metadata, <code>plan_approvals</code> artifact and execution fields, durable notification inbox, and session collaboration ledger)</li><li>Secrets storage integration points</li><li>Audit logging for sensitive actions</li></ol><h2 id="_3-non-responsibilities" tabindex="-1">3. Non-responsibilities <a class="header-anchor" href="#_3-non-responsibilities" aria-label="Permalink to &quot;3. Non-responsibilities&quot;">​</a></h2><ul><li>LLM provider SDKs</li><li>agent turn graph/orchestration</li><li>React rendering</li><li>marketplace web frontend</li></ul><h2 id="_4-suggested-crate-layout" tabindex="-1">4. Suggested crate layout <a class="header-anchor" href="#_4-suggested-crate-layout" aria-label="Permalink to &quot;4. Suggested crate layout&quot;">​</a></h2><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>crates/host-core/</span></span>
<span class="line"><span> src/</span></span>
<span class="line"><span> main.rs # sidecar entry</span></span>
<span class="line"><span> lib.rs</span></span>
<span class="line"><span> rpc/</span></span>
<span class="line"><span> tools/</span></span>
<span class="line"><span> permissions/</span></span>
<span class="line"><span> workspace/</span></span>
<span class="line"><span> plugins/</span></span>
<span class="line"><span> storage/</span></span>
<span class="line"><span> notifications.rs</span></span>
<span class="line"><span> secrets/</span></span>
<span class="line"><span> audit/</span></span>
<span class="line"><span> util/</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br></div></div><h2 id="_5-rpc-transport" tabindex="-1">5. RPC transport <a class="header-anchor" href="#_5-rpc-transport" aria-label="Permalink to &quot;5. RPC transport&quot;">​</a></h2><p>Frozen: <strong>stdio JSON-RPC NDJSON</strong> with Electron main (see <code>06-host-rpc-protocol.md</code>).</p><h3 id="_5a-control-pipe-resource-isolation" tabindex="-1">5a. Control-pipe resource isolation <a class="header-anchor" href="#_5a-control-pipe-resource-isolation" aria-label="Permalink to &quot;5a. Control-pipe resource isolation&quot;">​</a></h3><p>The host&#39;s stdin reader and stdout writer each run on one named, dedicated OS thread. They must not use Tokio&#39;s <code>tokio::io::{stdin, stdout}</code> adapters: those adapters obtain a blocking-pool worker for each operation, and an exhausted OS thread budget can otherwise panic the host before a structured error reaches Electron. The dedicated threads retry <code>EINTR</code> and transient <code>EAGAIN</code>/<code>EWOULDBLOCK</code> (<code>errno</code> 11 or 35) with a short delay, preserve NDJSON framing, and stop only on EOF or an unrecoverable pipe error. Failure to create either control thread is a startup error rather than an unhandled panic.</p><p>The best-effort login-shell PATH probe follows the same rule: a failed probe thread creation returns <code>None</code>, so Bash falls back to the host environment.</p><h2 id="_5b-rpc-surface-logical" tabindex="-1">5b. RPC surface (logical) <a class="header-anchor" href="#_5b-rpc-surface-logical" aria-label="Permalink to &quot;5b. RPC surface (logical)&quot;">​</a></h2><p>Domains:</p><ul><li><code>app.*</code></li><li><code>workspace.*</code></li><li><code>tools.*</code></li><li><code>permissions.*</code></li><li><code>plugins.*</code></li><li><code>session.*</code> (adapter level)</li><li><code>notification.*</code> (adapter level; durable inbox)</li><li><code>session.collaboration.*</code> (host-internal delivery ledger and turn binding)</li><li><code>plans.*</code> (approval broker and recovery)</li><li><code>shell.*</code> (catalog and default selection)</li><li><code>settings.*</code> (adapter level)</li><li><code>secrets.*</code></li><li><code>audit.*</code></li></ul><p>Example:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>tools.execute</span></span>
<span class="line"><span>plans.resolve</span></span>
<span class="line"><span>plans.pending</span></span>
<span class="line"><span>permissions.request</span></span>
<span class="line"><span>plugins.list</span></span>
<span class="line"><span>plugins.load_dev</span></span>
<span class="line"><span>workspace.set</span></span>
<span class="line"><span>secrets.set</span></span>
<span class="line"><span>notification.list</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br></div></div><h2 id="_6-security-invariants" tabindex="-1">6. Security invariants <a class="header-anchor" href="#_6-security-invariants" aria-label="Permalink to &quot;6. Security invariants&quot;">​</a></h2><ol><li>No unchecked path escape from workspace tools or <code>.pi/plan/*.md</code>; an explicit outside path is resolved only after host permission evaluation</li><li>Host resolves the durable session mode; request-supplied mode is never authoritative</li><li>Plan and Goal deny Write/Edit/plugin/unknown tools before permission evaluation</li><li>Plan and Goal Bash follow the durable permission mode and may mutate under Auto</li><li>Plan and Goal artifact bytes, path, hash, size, and approval/execution identity are host-authenticated</li><li>Plan/Goal approval is host-authenticated, durable, and atomic before Agent entry</li><li>Effective shell ID/dialect is checked before spawn; settings reject unavailable/wrong-platform IDs, and a persisted unavailable choice falls back only during catalog selection</li><li>Secrets never returned to renderer logs</li><li>Crash in plugin, shell, or approval path fails closed and does not grant or replay execution</li><li>Session collaboration is host-authenticated: source identity comes from the active plugin invocation, target permission ceilings are rechecked at turn admission, callbacks are at-most-once, and restart recovery never replays an interrupted delivery</li></ol><h2 id="_7-packaging" tabindex="-1">7. Packaging <a class="header-anchor" href="#_7-packaging" aria-label="Permalink to &quot;7. Packaging&quot;">​</a></h2><ul><li>build target per platform</li><li>ship binary next to Electron resources</li><li>versioned protocol handshake with Electron/Node</li></ul><h2 id="_8-mvp-acceptance" tabindex="-1">8. MVP acceptance <a class="header-anchor" href="#_8-mvp-acceptance" aria-label="Permalink to &quot;8. MVP acceptance&quot;">​</a></h2><ol><li>Electron can start Rust host sidecar</li><li>healthcheck RPC succeeds</li><li>at least one tool path executes through Rust</li><li>permission deny path works</li><li>unseen completed/failed turns create exactly one durable notification through the <code>session.endTurn</code> transaction; results already visible in the focused current chat and aborted turns create none</li><li>a durable Plan or Goal session cannot authorize Write/Edit/plugin tools through a conflicting request mode, and Plan/Goal Bash follows the resolved permission mode</li><li>SubmitPlan writes exact Markdown bytes to a new <code>.pi/plan/*.md</code> artifact and stores durable path/hash/size plus structured title/question in <code>plan_approvals</code>; approval is approve/reject-only, session/turn/version scoped, and expires at 30 absolute minutes with <code>PLAN_APPROVAL_TIMEOUT</code></li><li>Pending/queued/running Plan or Goal work is interrupted on host restart with no replay; approved interruptions leave the session Agent</li><li>Shell selection/fallback, stale ID/dialect rejection, stdout/stderr streaming, 60s timeout, bounded override, and process-tree abort are host-enforced</li><li>Session collaboration delivery, provenance, idempotency, callback settlement, cancellation, permission ceilings, hop limits, and schema v16 recovery are durable and test-covered without changing the core <code>Task</code> family</li></ol></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("spec/03-runtime/05-host-core-rust.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _05HostCoreRust = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _05HostCoreRust as default
};
