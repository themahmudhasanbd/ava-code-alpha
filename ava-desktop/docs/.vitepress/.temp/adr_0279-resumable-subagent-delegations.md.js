import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0279: Resumable subagent delegations","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0279-resumable-subagent-delegations.md","filePath":"adr/0279-resumable-subagent-delegations.md","lastUpdated":1790056329000}');
const _sfc_main = { name: "adr/0279-resumable-subagent-delegations.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0279-resumable-subagent-delegations" tabindex="-1">ADR 0279: Resumable subagent delegations <a class="header-anchor" href="#adr-0279-resumable-subagent-delegations" aria-label="Permalink to &quot;ADR 0279: Resumable subagent delegations&quot;">​</a></h1><ul><li>Status: Accepted for implementation</li><li>Date: 2026-09-17</li><li>Deciders: PI-Desktop core</li><li>Related: <a href="./0062-bounded-subagents-behind-a-task-tool">ADR 0062</a> · <a href="./0089-proactive-background-subagent-delegation">ADR 0089</a> · <a href="./0119-event-driven-subagent-timeouts">ADR 0119</a> · <a href="./0165-withdraw-a2a-peer-stack">ADR 0165</a> · <a href="./../spec/03-runtime/02-agent-runtime">03-runtime/02-agent-runtime.md</a> §5f</li><li>Amends: ADR 0062 (the <code>Task</code> tool contract) and ADR 0089 (the delegation lifecycle). Both are preserved — every existing boundary still holds; this adds one opt-in parameter and one piece of bookkeeping.</li><li>Tracking: #513</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Delegation as built by ADR 0062 and ADR 0089 spawns one <code>SubagentRun</code> per <code>Task</code> call. Each run starts with <code>messages: []</code> (<code>packages/agent-runtime/src/subagent.ts</code>) and is single-use. A delegate&#39;s transcript rows are persisted — they carry <code>parentToolCallId</code> and <code>agentName</code> and land in the session transcript — but nothing ever reads them back into a delegate, and the session runtime skips them when it rebuilds model context (<code>runtime.ts</code>, <code>if (m.parentToolCallId) continue;</code>).</p><p>That makes every <code>Task</code> call a cold start even when the parent delegates the same subagent for the same files twice. The context the first run bought — which files it read, what it found, what it already changed — is discarded, and the second run pays for it again, or worse, works from a stale file version it never re-reads.</p><p>The pieces to do better are already in place: the rows are persisted with enough fidelity for a full rebuild (tool call/result pairs, thinking blocks, and the original <code>Task</code> arguments on the tool row), and the session runtime already contains a <code>UiMessage[] → AgentMessage[]</code> converter that produces well-formed provider messages including synthesized carriers for orphan tool rows.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Add an opt-in <code>resume</code> parameter to the <code>Task</code> tool. When present, the new delegation is seeded with the prior delegation&#39;s reconstructed context instead of starting empty.</p><h3 id="_1-task-gains-resume-delegationid" tabindex="-1">1. <code>Task</code> gains <code>resume?: delegationId</code> <a class="header-anchor" href="#_1-task-gains-resume-delegationid" aria-label="Permalink to &quot;1. \`Task\` gains \`resume?: delegationId\`&quot;">​</a></h3><p>Resolution is by <code>delegationId</code> only — the id the <code>Task</code> result already returns and <code>TaskWait</code> already accepts. A second, stable internal key (<code>delegateSessionId</code>) exists solely to chain the runs; it never appears in a tool parameter, tool result, or prompt. The runtime keeps a reverse map from every <code>delegationId</code> issued on a live chain to that chain, so an id from the middle of a chain resolves too.</p><h3 id="_2-chains-not-records-are-the-unit-of-resumability" tabindex="-1">2. Chains, not records, are the unit of resumability <a class="header-anchor" href="#_2-chains-not-records-are-the-unit-of-resumability" aria-label="Permalink to &quot;2. Chains, not records, are the unit of resumability&quot;">​</a></h3><p>A <strong>chain</strong> is one <code>delegateSessionId</code> plus the ordered <code>Task</code> toolCallIds that belong to it. Each <code>resume</code> appends the new toolCallId. Child transcript rows are keyed by <code>parentToolCallId</code>, so reconstructing a chain&#39;s context means selecting every row whose <code>parentToolCallId</code> is in the chain and whose <code>agentName</code> matches.</p><p><code>DelegationRecord</code> gains <code>delegateSessionId</code>, plus <code>resumedFrom</code> and <code>modelChangedFrom</code> on a run that continues a chain. Read tracking lives on the chain, not the record: the registry keeps one entry per <code>delegateSessionId</code> and a secondary index from every <code>delegationId</code> on that chain to it.</p><h3 id="_3-context-reconstruction-is-a-pure-function" tabindex="-1">3. Context reconstruction is a pure function <a class="header-anchor" href="#_3-context-reconstruction-is-a-pure-function" aria-label="Permalink to &quot;3. Context reconstruction is a pure function&quot;">​</a></h3><p>A new module (<code>packages/agent-runtime/src/delegation-history.ts</code>) owns:</p><ul><li>selecting the chain&#39;s rows from the session transcript,</li><li>synthesizing the original <code>task</code> as the first user message from the first <code>Task</code> tool row&#39;s persisted arguments,</li><li>converting to <code>AgentMessage[]</code> with the delegate&#39;s own provider/model binding (a pinned delegation model is not the session model),</li><li>a read-file / line-count budget gate.</li></ul><p><code>SubagentRun</code> gains <code>initialMessages?: AgentMessage[]</code>; when present it seeds <code>initialState.messages</code> instead of <code>[]</code>. The run is otherwise a completely normal run: it occupies a <code>MAX_SUBAGENT_CONCURRENCY</code> slot, inherits the definition&#39;s permission scope, and settles through the existing <code>settleDelegation</code> path with its own <code>delegationId</code>, report, and counters.</p><h3 id="_4-validation-is-visible-never-thrown" tabindex="-1">4. Validation is visible, never thrown <a class="header-anchor" href="#_4-validation-is-visible-never-thrown" aria-label="Permalink to &quot;4. Validation is visible, never thrown&quot;">​</a></h3><p><code>resume</code> is validated before the run starts; every failure is a tool error the model can act on:</p><table tabindex="0"><thead><tr><th>Condition</th><th>Error</th></tr></thead><tbody><tr><td>unknown or evicted id</td><td><code>Unknown delegation &quot;&lt;id&gt;&quot;</code> plus the current resumable list</td></tr><tr><td>still running</td><td><code>Delegation &lt;id&gt; is still running</code>, call TaskWait first — a resume is never queued</td></tr><tr><td><code>stopped</code> / <code>aborted</code></td><td>not resumable; a future revive path (out of scope here)</td></tr><tr><td><code>interrupted</code> (the app closed mid-run)</td><td>not resumable; start a new delegation</td></tr><tr><td><code>Task.agent</code> differs from the chain&#39;s agent</td><td>name mismatch, lists available</td></tr><tr><td><code>Task.model</code> present on a resume</td><td>not allowed on a resumed run — start a new delegation to change models</td></tr><tr><td>chain exceeds the read budget</td><td><code>Delegation &lt;id&gt; has read too much to resume cheaply</code></td></tr><tr><td>the chain resolves but has no rows left to replay</td><td>the chain is dropped and reported as having no recorded history; the same id is never listed as reusable</td></tr></tbody></table><p>No new error codes for &quot;evicted&quot; versus &quot;never existed&quot;: both resolve to the same unknown-id error, because the model&#39;s only correct action is identical.</p><p>A resume keeps the chain&#39;s own model binding. The <code>providerId/modelId</code> key the chain recorded is preferred; a chain rebuilt from the transcript only knows the model id, which is matched only against the current definition&#39;s pin/fallbacks, the session binding, and currently authorized overrides. A remembered key whose turn grant expired is reauthorized before use; a denied key cannot select another account by model id. Other definitions&#39; private pins are never candidates (#841). Changing the parent&#39;s own session model therefore does not strand a chain, and a delegate never swaps models by accident. If nothing resolves the recorded binding any more, the run continues on the binding the definition resolves to now and records the previous model id in the delegation&#39;s lifecycle details as <code>modelChangedFrom</code>: refusing instead would strand the chain forever, and swapping silently would hand the same conversation to a different model with no trace.</p><h3 id="_5-only-completed-and-failed-are-resumable" tabindex="-1">5. Only <code>completed</code> and <code>failed</code> are resumable <a class="header-anchor" href="#_5-only-completed-and-failed-are-resumable" aria-label="Permalink to &quot;5. Only \`completed\` and \`failed\` are resumable&quot;">​</a></h3><p>A <code>failed</code> run&#39;s reads and findings still have value; its failed assistant row is dropped by the converter like any other error row. <code>stopped</code> and <code>aborted</code> encode the user&#39;s or the parent&#39;s decision to abandon that line of work, and resuming it would contradict the stop. <code>interrupted</code> (the app closed while the run still worked) means the same thing for this purpose: the delegate never settled, so there is nothing trustworthy to continue. (<code>timed_out</code> is a vestigial status — ADR 0119&#39;s timeouts were withdrawn by D328 — and is treated as <code>failed</code> for this purpose.)</p><h3 id="_6-one-live-record-per-chain-ever" tabindex="-1">6. One live record per chain, ever <a class="header-anchor" href="#_6-one-live-record-per-chain-ever" aria-label="Permalink to &quot;6. One live record per chain, ever&quot;">​</a></h3><p>There is no queuing and no message-append. A chain has at most one running record at a time; a <code>resume</code> while the chain is running is rejected with the TaskWait error above. This keeps the chain a strict straight line with no forking successors.</p><h3 id="_7-resumable-chains-are-bounded-lru-per-agent-name" tabindex="-1">7. Resumable chains are bounded, LRU, per agent name <a class="header-anchor" href="#_7-resumable-chains-are-bounded-lru-per-agent-name" aria-label="Permalink to &quot;7. Resumable chains are bounded, LRU, per agent name&quot;">​</a></h3><p><code>MAX_RESUMABLE_CHAINS_PER_AGENT = 2</code>. Chains are exempt from the existing <code>pruneFinishedDelegations</code> oldest-first sweep. Exceeding the bound evicts the least-recently-used whole chain when a delegation settles — registry entry and every reverse-map entry together. A chain whose latest run is still working is never evicted: dropping it would strand the delegate writing into it, so the bound counts reusable chains and a live chain may momentarily push the group over it. Tombstones are unnecessary: <code>delegationId</code> is a random UUID, so an evicted id cannot collide with a future one.</p><h3 id="_8-read-budget-gates-chain-growth" tabindex="-1">8. Read budget gates chain growth <a class="header-anchor" href="#_8-read-budget-gates-chain-growth" aria-label="Permalink to &quot;8. Read budget gates chain growth&quot;">​</a></h3><p>A delegate has no compaction of its own, so an unbounded chain would eventually overflow its own context window. Rather than trimming inside the chain, a chain whose cumulative read volume exceeds <code>MAX_RESUMABLE_READ_LINES</code> (50,000) silently leaves the resumable list — the next delegation for that work is a cold start, which still works. The gate is checked when the resumable list is built, so it costs nothing at delegation time.</p><p>Reads are counted from the delegate&#39;s read-only tool rows (<code>Read</code>, <code>Glob</code>, <code>Grep</code>, <code>BrowserPreview</code>), accumulated per chain; write-tool targets are not counted and not listed.</p><h3 id="_9-the-parent-sees-a-resumable-list-in-its-system-prompt" tabindex="-1">9. The parent sees a resumable list in its system prompt <a class="header-anchor" href="#_9-the-parent-sees-a-resumable-list-in-its-system-prompt" aria-label="Permalink to &quot;9. The parent sees a resumable list in its system prompt&quot;">​</a></h3><p>Each prompt composes the current resumable chains into the parent&#39;s system prompt: agent name, delegation id, the one-line objective, and the files the chain has read (capped at 8, truncated). Only settled, in-budget chains appear — a running chain is never listed, so the parent is not lured into resuming one.</p><p>The <code>resume</code> parameter&#39;s own schema description carries the rule that reuse requires the id; prose alone does not resume anything.</p><h3 id="_10-same-session-only" tabindex="-1">10. Same session only <a class="header-anchor" href="#_10-same-session-only" aria-label="Permalink to &quot;10. Same session only&quot;">​</a></h3><p><code>resume</code> resolves only against the current session&#39;s transcript. There is no cross-session addressing; sharing facts between conversations stays in the prompt, as ADR 0165 concluded when it withdrew the peer stack.</p><h3 id="_11-definition-edits-take-effect-on-resume-with-a-diagnostic" tabindex="-1">11. Definition edits take effect on resume, with a diagnostic <a class="header-anchor" href="#_11-definition-edits-take-effect-on-resume-with-a-diagnostic" aria-label="Permalink to &quot;11. Definition edits take effect on resume, with a diagnostic&quot;">​</a></h3><p>A definition edited between two delegations is applied as-is on resume — the same &quot;editing takes effect on the next prompt&quot; semantics the catalog already has — and the change is recorded in the delegation&#39;s lifecycle details. No definition snapshot is stored; snapshots would need new persistent state and would contradict the existing semantics.</p><h3 id="_12-chain-index-is-rebuilt-from-the-transcript-on-startup" tabindex="-1">12. Chain index is rebuilt from the transcript on startup <a class="header-anchor" href="#_12-chain-index-is-rebuilt-from-the-transcript-on-startup" aria-label="Permalink to &quot;12. Chain index is rebuilt from the transcript on startup&quot;">​</a></h3><p>The chain registry is in memory, but the chain relationship is recoverable: <code>Task</code> tool rows are persisted with their arguments, including <code>resume</code>. At session launch the runtime scans the transcript&#39;s <code>Task</code> rows, groups them by their <code>resume</code> links into chains, and rehydrates the secondary index. Restart does not lose resumability.</p><p>Two details make a rebuilt chain behave like a live one. Agent names are normalized on rebuild (<code>Explorer</code> and <code>explorer.md</code> both mean <code>explorer</code>), so the rebuilt chain still selects its own rows and still matches <code>Task.agent</code>. And each call carries the status its own <code>Task</code> row recorded: the settlement projection rewrites that row in place, so a chain knows whether its last run was <code>completed</code> or <code>failed</code>. A run the app closed while it still worked never settled: its row still says <code>running</code>, which rebuilds as <code>interrupted</code> — not resumable, rather than staying open forever. A row that records no status at all is trusted as settled, because every <code>Task</code> row this app writes carries one. The model binding itself is not persisted as a key, so a rebuilt chain is matched to a configured binding by model id; two accounts offering the same model id are indistinguishable at that point.</p><h3 id="_13-the-transcript-renders-a-chain-as-one-continuous-conversation" tabindex="-1">13. The transcript renders a chain as one continuous conversation <a class="header-anchor" href="#_13-the-transcript-renders-a-chain-as-one-continuous-conversation" aria-label="Permalink to &quot;13. The transcript renders a chain as one continuous conversation&quot;">​</a></h3><p>A resumed delegation&#39;s card shows the whole chain&#39;s rows as consecutive turns (task → reply → task → reply) with no special &quot;resumed&quot; badge. The new run&#39;s counters start at zero; <code>details.resumedFrom</code> keeps the link auditable.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li><code>Task</code>&#39;s parameter schema widens by one optional field. Every existing call is unchanged: <code>resume</code> omitted is exactly today&#39;s behavior.</li><li>The delegate→parent context boundary is untouched. Reconstructed rows seed the delegate&#39;s own context only; the parent still receives nothing but reports through <code>TaskWait</code>, and <code>parentToolCallId</code> rows are still skipped when the parent&#39;s context is rebuilt.</li><li>No new storage schema, tables, or event types. Chain identity, read tracking, and the model binding live on the in-memory chain; <code>resumedFrom</code> and <code>modelChangedFrom</code> ride the existing lifecycle details projection.</li><li>A resumed run is billed and bounded exactly like a fresh one — same concurrency slot, same permission scope, same fallback models, same abort sources.</li><li>Failure mode of the read budget is graceful degradation to a cold start, never a broken or oversized request.</li></ul><h2 id="out-of-scope-二期" tabindex="-1">Out of scope (二期) <a class="header-anchor" href="#out-of-scope-二期" aria-label="Permalink to &quot;Out of scope (二期)&quot;">​</a></h2><ul><li>Reviving a <code>stopped</code> / <code>aborted</code> delegation.</li><li>In-chain compaction or placeholder substitution for old tool results.</li><li>Task queuing / work append on a running delegation.</li><li>Cross-session resumption.</li><li>User-visible settings for the bounds; they stay code constants.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0279-resumable-subagent-delegations.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0279ResumableSubagentDelegations = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0279ResumableSubagentDelegations as default
};
