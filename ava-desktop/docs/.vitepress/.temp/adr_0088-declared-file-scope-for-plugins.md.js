import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0088: Plugin file access is declared per mode, and deletion is recoverable","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0088-declared-file-scope-for-plugins.md","filePath":"adr/0088-declared-file-scope-for-plugins.md","lastUpdated":1786734866000}');
const _sfc_main = { name: "adr/0088-declared-file-scope-for-plugins.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0088-plugin-file-access-is-declared-per-mode-and-deletion-is-recoverable" tabindex="-1">ADR 0088: Plugin file access is declared per mode, and deletion is recoverable <a class="header-anchor" href="#adr-0088-plugin-file-access-is-declared-per-mode-and-deletion-is-recoverable" aria-label="Permalink to &quot;ADR 0088: Plugin file access is declared per mode, and deletion is recoverable&quot;">​</a></h1><ul><li>Status: Accepted (Implemented 2026-08-15)</li><li>Date: 2026-08-15</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>ADR 0008 put plugin main code in its own <code>utilityProcess</code>, but noted that the process is a full Node environment: permissions gate the <code>pi.*</code> surface, not <code>require(&quot;node:fs&quot;)</code>. Capability sandboxing is still the open half (ADR 0008, D009). Until it lands, the <code>pi.*</code> gate is what a well-behaved plugin is held to and what a review can be based on, so its granularity is the whole design.</p><p>That granularity was three workspace-wide switches:</p><ul><li><code>fs.read.workspace</code> → any file in the workspace, including <code>.env</code> and <code>.git/config</code></li><li><code>fs.write.workspace</code> → any path in the workspace</li><li><code>fs.delete.workspace</code> → <code>fs.remove</code> with <code>recursive: false</code>, which bounds one call and not the blast radius: <code>fs.glob(&quot;**/*&quot;)</code> followed by a loop empties a workspace as thoroughly as <code>rm -rf</code> does</li></ul><p>Two implementation faults made it worse than the design admitted. Containment was a lexical prefix comparison, so a symlink inside the workspace pointing at <code>~/.ssh</code> satisfied it; and the runtime fell back to <code>grantedPermissions ?? manifest.permissions</code>, which auto-granted everything the manifest asked for whenever a caller omitted the grants.</p><p><code>07-plugins/13-plugin-permissions-matrix.md</code> had always specified &quot;Confirm on first use&quot; / &quot;Confirm each time or per session&quot; for these permissions. None of it was implemented. The narrowing below is that spec being delivered, not a new direction.</p><p>Tightening cannot be the whole answer, because a plugin system that can only read is not worth installing. The risks are not symmetric, and the response should not be either:</p><ul><li>A <strong>read</strong> is dangerous when the bytes can leave. That half is closed separately by the egress allowlist (<code>manifest.net.domains</code>), which confines the panel session, <code>pi.net.fetch</code>, and remote MCP endpoints to declared hostnames. With egress closed, a broad read is a much smaller problem.</li><li>A <strong>write</strong> is dangerous on its own, and the damage is bounded by where it can land.</li><li>A <strong>delete</strong> is dangerous on its own and, unlike the other two, is unrecoverable — unless the deletion goes to the operating system&#39;s trash, in which case it is fully recoverable and no worse than a write.</li></ul><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><h3 id="_1-manifest-fs-declares-scope-permission-names-stay-flat-strings" tabindex="-1">1. <code>manifest.fs</code> declares scope; permission names stay flat strings <a class="header-anchor" href="#_1-manifest-fs-declares-scope-permission-names-stay-flat-strings" aria-label="Permalink to &quot;1. \`manifest.fs\` declares scope; permission names stay flat strings&quot;">​</a></h3><p><code>permissions</code> remains <code>readonly string[]</code> and gains <code>fs.read</code>, <code>fs.write</code>, <code>fs.delete</code> — &quot;may this plugin touch files at all&quot;. A separate <code>fs</code> block says &quot;which files&quot;:</p><div class="language-json vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">{</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;permissions&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: [</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;fs.read&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;fs.write&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;fs.delete&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">],</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;fs&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">    &quot;read&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: { </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">&quot;root&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;workspace&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">&quot;scope&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: [</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;**/*&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">] },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">    &quot;write&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: { </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">&quot;root&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;workspace&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">&quot;scope&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: [</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;docs/**&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;*.md&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">] },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">    &quot;delete&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: { </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">&quot;own&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">true</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">&quot;scope&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: [</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;dist/**&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">] }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><p>A separate field rather than objects inside <code>permissions</code> because permissions are handled as strings end to end — install review, chips, grant round-trips, catalog validation — and the marketplace catalog format is versioned separately.</p><p><code>fs.read</code> may declare the whole tree; <code>fs.write</code> and <code>fs.delete</code> may not, and a whole-tree pattern (<code>**</code>, <code>**/*</code>, <code>*/**</code>, <code>./*</code>) is refused at validation time. An absent or empty scope is not an error: it means the plugin has no standing reach and every access falls to the user.</p><h3 id="_2-four-gates-in-a-fixed-order" tabindex="-1">2. Four gates, in a fixed order <a class="header-anchor" href="#_2-four-gates-in-a-fixed-order" aria-label="Permalink to &quot;2. Four gates, in a fixed order&quot;">​</a></h3><p>Every <code>pi.fs.*</code> call passes, in order:</p><ol><li><code>assertPermission</code> — is <code>fs.&lt;mode&gt;</code> declared <strong>and</strong> granted (intersection, so a revoked permission actually stops working)</li><li>Containment — <code>realpath</code> on both the root and the target; for a path being created, <code>realpath</code> of the nearest existing ancestor. A symlink out of the root fails here, and a path that simply does not exist is reported <code>NOT_FOUND</code> rather than as an escape attempt</li><li>Unconditional deny-list — credentials and history refused under every root, scope, and grant: <code>.env*</code>, <code>.npmrc</code>, <code>.netrc</code>, <code>.git-credentials</code>, <code>id_rsa*</code>, <code>*.pem</code>/<code>*.p12</code>/<code>*.pfx</code>/<code>*.keystore</code>, and anything inside <code>.git</code>, <code>.ssh</code>, <code>.aws</code>, <code>.gnupg</code>, <code>.kube</code>, <code>.docker</code>, plus the host&#39;s own data directory (provider keys, session store)</li><li>Declared scope, else user consent</li></ol><p><code>pi.fs.glob</code> answers to the same rules: results are filtered by the read scope, denied paths and reserved trees are omitted (a name is a read too), and <code>node_modules</code> / <code>.git</code> / <code>.venv</code> / <code>__pycache__</code> are not walked at all.</p><h3 id="_3-deletion-has-two-tiers-and-always-goes-to-the-trash" tabindex="-1">3. Deletion has two tiers, and always goes to the trash <a class="header-anchor" href="#_3-deletion-has-two-tiers-and-always-goes-to-the-trash" aria-label="Permalink to &quot;3. Deletion has two tiers, and always goes to the trash&quot;">​</a></h3><ul><li><strong><code>fs.delete</code> with <code>own: true</code></strong> — the host appends every successful <code>fs.writeText</code> to a write ledger in the plugin&#39;s own data directory (path plus mtime, host-owned, not reachable from the plugin API). A path in the ledger can be removed with no scope and no prompt: cleaning up your own output surprises nobody. If the file&#39;s mtime has moved past the recorded one, the user has touched it since and it is no longer the plugin&#39;s — the delete falls to consent.</li><li><strong><code>fs.delete</code> with <code>scope</code></strong> — deleting somebody else&#39;s file requires the globs up front, and they are shown to the user where the permissions are shown.</li></ul><p>Both tiers additionally:</p><ul><li>go through <code>shell.trashItem</code>, never <code>rm</code>. This is the backstop that makes the whole delete path survivable: any single gate above can be wrong without the user losing data</li><li>keep <code>recursive: false</code>; a non-empty directory is refused rather than emptied</li><li>answer to a rolling brake of 50 deletes per 60s per plugin. Past it the user is asked once, with the reason stated as &quot;this many, this fast&quot; rather than being asked to adjudicate one file. This is the only thing that separates a cleanup routine from a wipe</li></ul><h3 id="_4-consent-is-a-native-dialog-and-never-more-durable-than-the-session" tabindex="-1">4. Consent is a native dialog, and never more durable than the session <a class="header-anchor" href="#_4-consent-is-a-native-dialog-and-never-more-durable-than-the-session" aria-label="Permalink to &quot;4. Consent is a native dialog, and never more durable than the session&quot;">​</a></h3><p>Out-of-scope access calls <code>dialog.showMessageBox</code>: <strong>Deny</strong> / <strong>Allow once</strong> / <strong>Allow this session</strong>. A session grant covers the containing directory and dies with the process; nothing is persisted. A rate-brake prompt is offered no session option at all. A host with no consent service refuses — a host that cannot ask must not assume yes.</p><h3 id="_5-root-userselected" tabindex="-1">5. <code>root: &quot;userSelected&quot;</code> <a class="header-anchor" href="#_5-root-userselected" aria-label="Permalink to &quot;5. \`root: &quot;userSelected&quot;\`&quot;">​</a></h3><p><code>pi.fs.requestDirectory()</code> opens the native directory picker and returns a handle. Inside that directory the plugin needs no manifest scope — the user just pointed at it, which <em>is</em> the grant — while containment and the deny-list still apply. The handle is memory-only and dies with the process.</p><p>This is where the capability comes back. &quot;Organize my whole photo folder&quot;, &quot;bulk import/export&quot; plugins become possible with <strong>zero standing power</strong>, modelled on the browser&#39;s File System Access API.</p><h3 id="_6-migration-is-a-breaking-downgrade" tabindex="-1">6. Migration is a breaking downgrade <a class="header-anchor" href="#_6-migration-is-a-breaking-downgrade" aria-label="Permalink to &quot;6. Migration is a breaking downgrade&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Legacy permission</th><th>What it is worth now</th></tr></thead><tbody><tr><td><code>fs.read.workspace</code></td><td><code>fs.read</code>, <code>scope: [&quot;**/*&quot;]</code> — unchanged, because egress is closed</td></tr><tr><td><code>fs.write.workspace</code></td><td><code>fs.write</code> with an empty scope — every write asks the user</td></tr><tr><td><code>fs.delete.workspace</code></td><td><code>fs.delete</code> with <code>own: true</code> — only its own output goes without asking</td></tr></tbody></table><p>Capability is reduced; no hole is left open. An explicit rule always beats the legacy default, so an author upgrades by adding <code>fs</code>, never by renaming anything. The Plugins page marks a downgraded plugin so the user knows why it stopped working and what the author has to do.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A plugin that writes or deletes outside its declared scope now prompts, and a prompt the user denies is audited as <code>PERMISSION_DENIED</code>. Authors who never declared scope see writes fail until they do.</li><li>Installed plugins that predate <code>manifest.fs</code> lose write and delete reach on the next load. This is intentional and visible rather than silent.</li><li>Reads stay generous, which keeps the interesting plugins possible. That is a bet on the egress allowlist: if egress were reopened, read scope would have to be revisited.</li><li>Deletion is recoverable through the OS trash on every platform Electron supports it, and we store none of the user&#39;s data to achieve that — no quarantine copy, no undo journal.</li><li>The rate brake is per plugin and rolling, so a legitimate large cleanup is interrupted once rather than refused.</li><li>None of this binds a <em>malicious</em> plugin, which can still call <code>require(&quot;node:fs&quot;)</code> directly. It bounds what a plugin can do through the API it is supposed to use, and gives review something concrete to read. The malicious case remains ADR 0008 D009 — capability sandboxing inside the plugin process — and is the next item on this line of work.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>A host-owned quarantine directory with an undo UI</strong> instead of the OS trash: rejected. It would mean copying and retaining the user&#39;s files inside app storage to provide a recovery path the operating system already provides.</li><li><strong>Objects inside <code>permissions</code></strong> (<code>{ &quot;name&quot;: &quot;fs.write&quot;, &quot;scope&quot;: [...] }</code>): rejected. Permissions are strings across the install review, the grant round-trip, the registry, and the marketplace catalog; the change would touch all of them to express something a sibling field expresses cleanly.</li><li><strong>Prompt on every file access, no manifest scope</strong>: rejected. Consent fatigue turns into blanket approval, and a plugin that legitimately writes 40 files becomes unusable.</li><li><strong>Keeping <code>recursive: false</code> as the only delete bound</strong>: rejected. It bounds a call, not a loop, which is what the rate brake exists to catch.</li><li><strong>Denying <code>fs.write</code> outright and offering only the plugin&#39;s data directory</strong>: rejected as too narrow — an export or formatter plugin has to be able to put a file where the user asked for it.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0088-declared-file-scope-for-plugins.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0088DeclaredFileScopeForPlugins = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0088DeclaredFileScopeForPlugins as default
};
