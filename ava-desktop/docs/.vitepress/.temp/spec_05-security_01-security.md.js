import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"01. Security","description":"","frontmatter":{},"headers":[],"relativePath":"spec/05-security/01-security.md","filePath":"spec/05-security/01-security.md","lastUpdated":1790060879000}');
const _sfc_main = { name: "spec/05-security/01-security.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="_01-security" tabindex="-1">01. Security <a class="header-anchor" href="#_01-security" aria-label="Permalink to &quot;01. Security&quot;">​</a></h1><blockquote><p>Language: English (per ADR 0009). Statuses reflect the implementation as of M5 hardening. Cross-references: <a href="./../03-runtime/09-logging-and-observability">logging</a> · <a href="./../03-runtime/07-process-model">process model</a> · <a href="./../07-plugins/04-plugin-security">plugin security</a></p></blockquote><h2 id="_1-security-goals" tabindex="-1">1. Security goals <a class="header-anchor" href="#_1-security-goals" aria-label="Permalink to &quot;1. Security goals&quot;">​</a></h2><ol><li>The renderer must never gain unconstrained system access</li><li>Protect provider API keys</li><li>Bound the blast radius of agent tool execution</li><li>Keep sensitive operations auditable</li></ol><h2 id="_2-electron-baseline" tabindex="-1">2. Electron baseline <a class="header-anchor" href="#_2-electron-baseline" aria-label="Permalink to &quot;2. Electron baseline&quot;">​</a></h2><p>Required (all <strong>implemented</strong>):</p><ul><li><code>contextIsolation: true</code></li><li><code>nodeIntegration: false</code></li><li><code>sandbox: true</code> — the preload is a fully bundled CJS file with no runtime module resolution, verified end-to-end by <code>test:e2e:boot</code></li><li>No remote module (Electron ≥ 14 default)</li><li>Navigation locked down: <code>setWindowOpenHandler</code> denies in-app windows and forwards only parsed <code>http:</code> / <code>https:</code> / <code>mailto:</code> URLs to the OS browser (<code>parseAllowedExternalUrl</code>, D330 / ADR 0168). <code>file:</code>, <code>javascript:</code>, <code>data:</code>, and custom URI schemes never reach <code>shell.openExternal</code>. <code>will-navigate</code> blocks all non-dev-server navigations</li><li>Every web contents Electron creates starts with a deny-all window-open handler and a blocked <code>&lt;webview&gt;</code> attach (<code>app.on(&quot;web-contents-created&quot;)</code>); the owning surface replaces the handler with its own policy, so a window that forgets to wire one denies popups instead of inheriting Chromium&#39;s defaults</li><li>Preload exposes a whitelist-checked <code>invoke</code>/<code>on</code> bridge only (<code>IPC_WHITELIST</code> enforced on both preload and main sides)</li><li>Transcript Markdown is sanitized (<code>rehype-sanitize</code>), but remote <code>http(s)</code> images, audio, and video that a model writes into a reply are fetched on render, without a click. This is a deliberate readability trade-off: a reply can therefore reveal the user&#39;s IP to the host it names. Links never navigate in-app and always route through the external-open path.</li></ul><h3 id="content-security-policy" tabindex="-1">Content Security Policy <a class="header-anchor" href="#content-security-policy" aria-label="Permalink to &quot;Content Security Policy&quot;">​</a></h3><ul><li>Dev: <code>script-src &#39;self&#39; &#39;unsafe-inline&#39; &#39;unsafe-eval&#39;</code> (required by Vite HMR tooling), localhost websocket connect-src. Fonts are restricted to <code>&#39;self&#39;</code> and <code>data:</code> so Vite-inlined KaTeX WOFF2 assets can render without admitting remote font origins.</li><li>Production build: <code>&#39;unsafe-eval&#39;</code> and localhost connect-src are stripped at build time (<code>tightenCsp</code> plugin in <code>electron.vite.config.ts</code>); <code>connect-src &#39;self&#39;</code> only. Provider network traffic happens in the Node sidecar, never in the renderer.</li><li>Assistant Mermaid diagrams do not widen CSP or renderer privileges. Only a completed answer fence may dynamically load the bundled local renderer. Mermaid runs with <code>securityLevel: strict</code>, protected security/theme/limit configuration, HTML labels disabled, a 20,000-character source limit, and a 500-edge limit. Its generated SVG then passes through DOMPurify&#39;s SVG profile; links, URL attributes, <code>foreignObject</code>, script, embedded media, and external image elements are removed before the SVG reaches the DOM. Invalid or oversized input fails closed to escaped source text.</li></ul><h3 id="future-hardening-tracked-post-mvp" tabindex="-1">Future hardening (tracked, post-MVP) <a class="header-anchor" href="#future-hardening-tracked-post-mvp" aria-label="Permalink to &quot;Future hardening (tracked, post-MVP)&quot;">​</a></h3><ul><li>Electron fuses (<code>runAsNode</code>, <code>nodeCliInspect</code> off) at package time</li><li><code>webSecurity</code> assertions in an automated security e2e</li></ul><h2 id="_3-secrets" tabindex="-1">3. Secrets <a class="header-anchor" href="#_3-secrets" aria-label="Permalink to &quot;3. Secrets&quot;">​</a></h2><ul><li>Keys stored via Electron <code>safeStorage</code> encryption, managed by host-core (see <a href="./../03-runtime/14-secrets-storage">14-secrets-storage</a>)</li><li>UI shows configured/not-configured only; never echoes key material</li><li>Logs must never contain secrets: Logger redaction (key-name patterns + <code>sk-</code>-style token pattern) in Electron main, <code>redact_value</code> in host-core audit writes; verified by the no-secret-leak smoke check</li><li>Error messages must not echo full keys</li></ul><h2 id="_4-workspace-sandbox" tabindex="-1">4. Workspace sandbox <a class="header-anchor" href="#_4-workspace-sandbox" aria-label="Permalink to &quot;4. Workspace sandbox&quot;">​</a></h2><ul><li>File tools are restricted to the project root by default; an explicit path outside the session workspace and scratch roots requires the host permission decision described in <code>03-runtime/03-tools-and-permissions.md</code></li><li>Path normalization + root boundary check in host-core (<code>workspace::tests::blocks_escape</code> covers escape attempts)</li><li>Symlink targets outside the root are rejected when detectable unless the explicit path was approved by the host permission layer</li></ul><p>Plan is not itself the workspace security boundary. Host-core resolves the durable session mode for every <code>tools.execute</code> call and applies the Plan matrix before permission modes, grants, plugin risk, or renderer/sidecar state. Plan denies Write/Edit/plugin/unknown tools, while BrowserPreview is the explicit read-only UI inspection exception (it reveals bundled <code>pi.browser</code> chrome; raw CDP plugin tools stay denied in Plan). Bash remains available in Plan: Ask and Accept edits prompt, and Auto runs without confirmation and may mutate the workspace or scratch directory. The UI must state this tradeoff. <code>SubmitPlan</code> preserves exact Markdown bytes in a new unique <code>&lt;workspaceRoot&gt;/.pi/plan/*.md</code> file through host-core, validates the in-root artifact path, computes SHA-256 and byte size, and only then creates the <code>plan_approvals</code> record with structured title/question fields. Renderer and sidecar state cannot write or replace an artifact.</p><h2 id="_4-1-skill-market-egress" tabindex="-1">4.1 Skill market egress <a class="header-anchor" href="#_4-1-skill-market-egress" aria-label="Permalink to &quot;4.1 Skill market egress&quot;">​</a></h2><p>The renderer does not fetch skill catalogs or SKILL.md documents. Electron main performs those HTTPS requests under the public-network policy (ADR 0243 / D413, amended by ADR 0272 / D436): <code>https</code> only, a shared syntactic public-host check, <code>redirect: &quot;manual&quot;</code>, and a per-hop verdict that follows the route the request will actually take. Before each hop the client asks the session that carries <code>net.fetch</code> for its own proxy decision (<code>Session.resolveProxy</code>): on a proxied route the hop is judged on its route rather than on a local address the app would never dial, so only the resolver-artifact class (<code>benchmark</code>, a TUN fake-IP) is tolerated there, while a direct or unreadable route keeps the full local classification and rejects loopback, RFC1918, ULA, link-local, mapped IPv6, and every other non-public class by default. The explicit <code>allowFakeIp</code> setting may additionally permit only the <code>benchmark</code> placeholder for a transparent router/TUN deployment. Install writes markdown only through <code>skills.create</code>. The host document cap remains 128 KiB after sibling markdown is inlined.</p><h2 id="_4-2-mcp-market-egress" tabindex="-1">4.2 MCP market egress <a class="header-anchor" href="#_4-2-mcp-market-egress" aria-label="Permalink to &quot;4.2 MCP market egress&quot;">​</a></h2><p>The MCP market accepts only credentials-free public HTTPS sources and catalog endpoints. Main asks the same Electron session that carries the request for its proxy route before every hop. On a fully <code>proxied</code> route, it uses Chromium <code>net.fetch</code>, which lets system/PAC and custom proxies resolve fake-IP names; the local resolver&#39;s <code>benchmark</code> fake-IP class is tolerated there, while real private and other non-public classes remain rejected. On <code>direct</code> or <code>unknown</code> routes, Main keeps the existing Node HTTPS path and pins the selected public address to the socket, retaining the original host for TLS SNI and HTTP Host. The explicit <code>allowFakeIp</code> setting may additionally permit only benchmark answers on those routes; it never permits other non-public classes. Redirects are manual, HTTPS-only, limited to five hops, and checked again before each connection. connection. Responses are capped at 4 MiB, requests share an 8-second deadline, and source/cache/entry counts are bounded. Cross-origin user-MCP redirects do not forward caller headers.</p><p>Manual user-owned MCP configuration remains covered by ADR 0142 and may use explicit local/LAN endpoints; the market path does not widen that policy.</p><h2 id="_4-3-portable-configuration-sync" tabindex="-1">4.3 Portable configuration sync <a class="header-anchor" href="#_4-3-portable-configuration-sync" aria-label="Permalink to &quot;4.3 Portable configuration sync&quot;">​</a></h2><p>WebDAV sync is a host-core network boundary. The renderer and Agent Runtime cannot access the endpoint, WebDAV password, backup password, vault key, or portable secret values. Host-core validates the selected HTTPS endpoint, rejects userinfo and redirects, constrains relative paths, bounds remote object size and KDF parameters, and requires strong conditional-write behavior before publishing a shared head in strict mode. An explicitly confirmed append-only compatibility mode may be used after a bounded <code>PROPFIND</code> directory-listing probe succeeds; it publishes per-device encrypted pointers and retains immutable history rather than pretending an unconditional <code>PUT</code> is CAS.</p><p>Every remote payload is authenticated ciphertext. The WebDAV server receives neither the vault password nor the local machine encryption key. Credentials are exported only after explicit category opt-in and are never included in status, preview, conflict labels, or logs. Restored provider/MCP secrets are written through the host secret store; OAuth sessions and cookies are never portable.</p><p>Imported commands, endpoints, scripts, skills, plugins, and automations are staged behind a digest-bound local approval. Local paths and approvals are overlays, not shared entities. A new device therefore cannot execute a synchronized capability merely because its desired enabled flag was imported. The compatibility-mode warning states that all devices sharing a vault must use the same mode and that concurrent changes can still require review. It does not weaken approval, secret export, redirect, path, object-size, or freshness protections. The server can still deny availability or replay a valid old head to a fresh device that has no trusted history; sync does not claim availability or freshness against a malicious server.</p><h2 id="_5-command-execution" tabindex="-1">5. Command execution <a class="header-anchor" href="#_5-command-execution" aria-label="Permalink to &quot;5. Command execution&quot;">​</a></h2><ul><li>Bash requires confirmation by default (risk-tiered permission cards); in either Agent or Plan, explicit Auto may run it without confirmation</li><li>The Bash protocol name remains stable, but host-core selects a catalog shell (<code>windows-powershell</code>, <code>windows-pwsh</code>, <code>cmd</code>, <code>git-bash</code>, or <code>bash</code>) from persisted <code>defaultCommandShell</code> where supported by the platform. Settings writes reject unavailable/wrong-platform IDs. If a persisted choice later becomes unavailable, catalog resolution intentionally falls back to the first available platform shell; each turn pins its effective ID/dialect and the host rejects a changed pin before spawn with <code>COMMAND_SHELL_CHANGED</code>.</li><li>Timeouts are mandatory: 60s default with a 1–21,600s bounded override (D329). Output streams as separate stdout/stderr channels and is truncated at 96KB / 4000 lines with an explicit <code>[truncated: …]</code> marker that names which end survived (see <a href="./../03-runtime/16-tool-result-limits">16-tool-result-limits</a>)</li><li>User abort and timeout shut down the complete process tree before the tool closes; no orphan process may continue writing output.</li><li>Full command line recorded in the audit log (SQLite, redacted), with shell ID and dialect rather than an untrusted executable path or path hash</li><li>Allowlist/denylist refinement is a tracked follow-up (<a href="./../03-runtime/03-tools-and-permissions">03-tools-and-permissions</a>)</li></ul><h2 id="_6-supply-chain" tabindex="-1">6. Supply chain <a class="header-anchor" href="#_6-supply-chain" aria-label="Permalink to &quot;6. Supply chain&quot;">​</a></h2><ul><li>Dependency versions locked via <code>pnpm-lock.yaml</code> / <code>Cargo.lock</code> committed to the repo; upgrades are explicit commits</li><li>Prefer official pi packages</li><li>Marketplace package installation is remote-capable. Current SHA-256 checks verify transfer integrity against the catalog value, but signatures and publisher provenance are not yet enforced; see the plugin trust limitation in <code>07-plugins/08-plugin-signing-updates.md</code> before treating marketplace code as trusted.</li><li>A plugin main currently runs with raw Node built-ins in its own <code>utilityProcess</code>. The brokered <code>pi.*</code> permission gate does not constrain direct Node access, so marketplace plugins must be treated as unrestricted user-privileged code until capability sandboxing is implemented.</li></ul><h2 id="_7-application-update-security-d120" tabindex="-1">7. Application update security (D120) <a class="header-anchor" href="#_7-application-update-security-d120" aria-label="Permalink to &quot;7. Application update security (D120)&quot;">​</a></h2><ul><li>Electron Main owns <code>electron-updater</code>; renderer IPC cannot provide or override the fixed HTTPS GitHub owner/repository or releases URL.</li><li>The updater forces <code>allowPrerelease = false</code> so discovery always uses GitHub&#39;s latest stable release rather than a same-channel prerelease pin.</li><li>Feed manifests bind artifacts with electron-builder hashes. An error, unavailable feed, hash mismatch, or invalid updater state must not install.</li><li>Packaged macOS, Windows NSIS, and Linux AppImage download and install in-app from the GitHub Releases feed. Linux deb/rpm and Windows ZIP detect a release and open the fixed releases page. Legacy Windows portable executables remain manual when <code>PORTABLE_EXECUTABLE_FILE</code> is present.</li><li>D126 tag releases publish Windows NSIS and Linux AppImage installers with their update manifests, plus Linux deb/rpm packages and a Windows portable ZIP. The NSIS and AppImage artifacts activate the existing in-app lanes. The portable ZIP uses notify-and-link delivery and does not write <code>latest.yml</code>. macOS tag artifacts are Developer ID-signed, notarized, and stapled before upload; rollback and staged-rollout qualification remain release follow-ups.</li><li>The client carries no GitHub token. A private or otherwise unreachable feed fails closed; automatic failures stay ambient and explicit checks expose the error.</li><li>Unsigned macOS distributions keep a narrow first-launch fallback for trusted sources. The DMG is a two-icon install and does not include that note. The ZIP package includes a text note and the executable helper, which searches only <code>/Applications/PI-Desktop.app</code> and <code>~/Applications/PI-Desktop.app</code>, verifies <code>CFBundleIdentifier</code> is <code>net.aiuo.pi-desktop</code>, removes only <code>com.apple.quarantine</code> recursively when present, and opens the app. It accepts no arbitrary path, uses no privilege escalation, and is not a substitute for Developer ID signing or notarization. The note gives the manual <code>com.apple.quarantine</code> command and says signed/notarized builds do not need it.</li><li>Localized product &quot;what&#39;s new&quot; text (D164/D345) is selected in Main from the shipped changelog catalog and attached to <code>UpdateState.releaseNotes</code>. The renderer cannot supply a notes URL, feed, or remote body; missing catalog entries simply omit the section.</li><li>The Developer ID + notarization lane remains documented in the <a href="./../06-delivery/06-release-runbook">release runbook</a>.</li></ul><h2 id="_8-local-mcp-control-plane" tabindex="-1">8. Local MCP control plane <a class="header-anchor" href="#_8-local-mcp-control-plane" aria-label="Permalink to &quot;8. Local MCP control plane&quot;">​</a></h2><p>The local MCP control server is an explicit automation boundary, not a general remote-control listener:</p><ul><li>It is disabled by default and only starts with <code>PI_DESKTOP_MCP_CONTROL=1</code>.</li><li>It binds <code>127.0.0.1</code> only and refuses to start if the listen address is not loopback. There is no configuration path for a LAN or public interface, and the feature does not revive the deferred remote Gateway / WebUI scope.</li><li>It validates any supplied <code>Origin</code> against local loopback hostnames to block DNS-rebinding access from remote web content. Non-browser clients may omit <code>Origin</code>.</li><li>A random 256-bit bearer token is persisted in the Electron user-data directory. The token and connection manifest are mode <code>0600</code> where supported, and the manifest is marked inactive during shutdown.</li><li>Secret get/set/delete channels, provider/OAuth/MCP secret-write paths, settings writes, and renderer-only native picker/dialog channels (including <code>plugin/loadDev</code>) are excluded. Secret-shaped argument fields are stripped before dispatch. The reviewed catalog is explicit; newly added IPC handlers are not exposed automatically.</li><li>Calls delegate to the existing main-process IPC handlers, so host availability, workspace boundaries, permission checks, input validation, and redacted logging remain authoritative. Dangerous generic operations, session-configure (permission mode), and destructive named tools require an explicit <code>confirm: true</code>. That flag is an agent acknowledgement, not a user prompt.</li><li>Request and serialized-result sizes, including <code>structuredContent</code>, are bounded. Startup failure is fail-soft for the desktop.</li></ul><p>An Agent using this endpoint has the same local-user authority as the running desktop for the operations it invokes. Users must protect the user-data directory and token; the endpoint is not intended for untrusted local users or remote clients. <code>confirm: true</code> does not ask the visible desktop for approval.</p><p>The post-MVP remote control target is a separate security boundary. It is specified in <a href="./02-remote-control-security"><code>02-remote-control-security.md</code></a> and <a href="./../02-architecture/05-remote-agent-control"><code>02-architecture/05-remote-agent-control.md</code></a>. Those specifications require an authenticated Gateway/Agent Host link, session-scoped authorization, event replay controls, and no network access to host-core. They do not change the loopback-only rule above.</p><h2 id="_9-host-process-attack-surface" tabindex="-1">9. Host process attack surface <a class="header-anchor" href="#_9-host-process-attack-surface" aria-label="Permalink to &quot;9. Host process attack surface&quot;">​</a></h2><ul><li>host-core speaks NDJSON JSON-RPC on stdio to the Electron main process only; it binds no network ports</li><li>The agent sidecar reaches host services only through the main-process proxy (<code>host.proxy</code>), which enforces a <strong>method allowlist</strong> (<code>tools.execute</code>, <code>tools.list</code>, <code>session.get</code>, <code>session.appendMessage</code>, <code>workspace.get</code>, <code>app.health</code>) — the sidecar cannot pull secrets or mutate providers/settings/plugins through the proxy</li><li>host-core child processes (Bash tool) run with the user&#39;s privileges; containment relies on the permission layer, catalog identity, process-group/ job-tree shutdown, and workspace sandbox rather than OS sandboxing</li></ul><h2 id="_10-threat-model-summary" tabindex="-1">10. Threat model (summary) <a class="header-anchor" href="#_10-threat-model-summary" aria-label="Permalink to &quot;10. Threat model (summary)&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Threat</th><th>Mitigation</th></tr></thead><tbody><tr><td>Malicious web content in renderer</td><td>no Node, sandbox, navigation lock, CSP</td></tr><tr><td>Prompt-injected destructive tool use</td><td>host-owned durable mode policy, permission confirmation, path boundary, secret isolation</td></tr><tr><td>Dependency poisoning</td><td>lockfiles, few deps, native-module review</td></tr><tr><td>Malicious local plugin</td><td>declared permissions, no secret access, process isolation tracked post-MVP (ADR 0008)</td></tr><tr><td>Skill market SSRF via user source URL</td><td>public-HTTPS classifier + DNS + per-hop redirect checks in main; renderer CSP forbids the fetch (ADR 0243)</td></tr></tbody></table><h2 id="_11-security-acceptance-gates" tabindex="-1">11. Security acceptance gates <a class="header-anchor" href="#_11-security-acceptance-gates" aria-label="Permalink to &quot;11. Security acceptance gates&quot;">​</a></h2><ol><li>Renderer cannot <code>require(&#39;fs&#39;)</code> (sandbox + no nodeIntegration) — verified</li><li>Plan Write/Edit/plugin calls cannot run under any permission mode; Bash is confirmed under Ask/Accept edits and may run without confirmation only under explicit Auto</li><li>Writing outside the workspace fails — verified (host tests)</li><li>API keys never appear in plaintext in exports/logs by default — verified</li><li>Non-whitelisted IPC channels are rejected — verified (M1)</li><li>A forged renderer/sidecar mode cannot override the durable host session mode</li><li>Plan artifact bytes/path/hash/size are host-authenticated; approval is approve/reject-only and scheduled Plan is rejected before artifact/queue work</li><li>Plan expiry, rejection, host restart, and stale responses never replay pending/queued/running work; an approved interruption leaves the session Agent</li><li>Invalid settings and stale shell ID/dialect fail closed; Bash output streams separately and timeout/abort kills the complete process tree</li><li>Local MCP control is loopback-only, bearer-authenticated, opt-in, bounded, excludes secret writes and native pickers, and requires confirmation for session permission-mode changes</li></ol><h2 id="_12-native-pi-session-boundary-adr-0254" tabindex="-1">12. Native Pi session boundary (ADR 0254) <a class="header-anchor" href="#_12-native-pi-session-boundary-adr-0254" aria-label="Permalink to &quot;12. Native Pi session boundary (ADR 0254)&quot;">​</a></h2><p>Native session paths remain sidecar-private. Renderer-visible ids are opaque hashes of canonical path plus verified header id. Every discovery/open resolves the real path below the configured Pi session root and revalidates header id and cwd; path traversal and symlink escape are rejected.</p><p>Writable continuation requires a mode-0600 cooperative PI-Desktop lease beside the session and full-byte identity checks before each SDK append. After an append, the adapter accepts only the unchanged prior prefix plus exactly one entry whose id and parent match the SDK operation. Any foreign/interleaved change disposes the runtime and requires reload. A stale lease is reclaimed only for a provably dead process on the same host when the target is unchanged or is a complete same-file append-only extension with the original byte prefix and a continuous parent chain. This lease is not treated as proof that Pi Web/CLI is absent because those clients do not yet share its protocol.</p><p>Native continuation passes <code>noTools: &quot;all&quot;</code> to the SDK: no built-in or extension model tools are exposed, including filesystem/shell tools. <code>permissionMode: &quot;inherit&quot;</code> is not a permission bridge. Enabling native tools requires an explicit Desktop permission integration and updated security decision. Native Pi extensions still execute as trusted local code with the native resource lifecycle; they are not Desktop plugins and this is not a sandbox claim. Capability checks recognize this service&#39;s owned lease and reclaimable dead local owners without stealing live, remote, malformed, or uncertain leases. Native fork reuses the same source ownership gate: an owned idle runtime keeps its lease, an unowned source is held under a short-lived lease for the snapshot window, and a live/remote/malformed foreign lease or a changed source refuses the fork. The child is written as a private mode-0600 non-jsonl staging file in the parent&#39;s session directory (fsync, then a no-clobber hardlink to the final name); cleanup removes only files whose device/inode and content still match what this operation created, and unexpected filesystem failures cross the preload boundary only as a path-free classified error.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("spec/05-security/01-security.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _01Security = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _01Security as default
};
