import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0134: Use models.dev as the sole model metadata source with a local snapshot","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0134-models-dev-sole-model-metadata-source.md","filePath":"adr/0134-models-dev-sole-model-metadata-source.md","lastUpdated":1788992254000}');
const _sfc_main = { name: "adr/0134-models-dev-sole-model-metadata-source.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0134-use-models-dev-as-the-sole-model-metadata-source-with-a-local-snapshot" tabindex="-1">ADR 0134: Use models.dev as the sole model metadata source with a local snapshot <a class="header-anchor" href="#adr-0134-use-models-dev-as-the-sole-model-metadata-source-with-a-local-snapshot" aria-label="Permalink to &quot;ADR 0134: Use models.dev as the sole model metadata source with a local snapshot&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-08-29</li><li>Deciders: PI-Desktop core</li><li>Amends: ADR 0027, ADR 0133, D136, D266</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The first models.dev integration still retained a second model metadata path in pi-ai. That made it possible for a package upgrade or a provider-specific pi record to change the model name, capabilities, limits, thinking levels, input modes, or prices that PI-Desktop presented and sent to the sidecar. It also did not persist the complete public models.dev document, so model updates depended on the process staying online.</p><p>The supplied models.dev records publish more than context and output limits: they include model descriptions, families, attachments, reasoning options, tool and structured-output support, temperature support, knowledge/release metadata, input/output modalities, weights, status, interleaving, limits, and cost tiers. Those fields need one stable owner and a local offline snapshot.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p><code>https://models.dev/api.json</code> is the only model metadata/configuration source.</p><ol><li>Electron main reads the bundled public document from <code>resources/models.dev/api.json</code> in development and <code>&lt;resources&gt;/models.dev/api.json</code> in packaged builds. The file is part of the repository/release artifact, not the user data directory.</li><li><code>scripts/release.mjs &lt;version&gt; --tag</code> fetches <code>https://models.dev/api.json</code>, validates it, atomically replaces the checked-in resource, and includes that file in the release commit before tagging.</li><li>On startup, Electron reads the bundled snapshot without network I/O. Settings → Model configuration exposes <strong>Refresh model catalog</strong>, which always refetches the URL and replaces the in-memory snapshot for the current process; it never writes the packaged file or a user cache.</li><li>The parser maps all supported models.dev fields into the existing <code>ModelInfo</code> and sidecar <code>ModelConfig</code>: <code>id</code>, <code>name</code>, <code>description</code>, <code>family</code>, <code>attachment</code>, <code>reasoning</code>, <code>reasoning_options</code>, <code>tool_call</code>, <code>structured_output</code>, <code>temperature</code>, <code>knowledge</code>, <code>release_date</code>, <code>last_updated</code>, <code>modalities.input/output</code>, <code>open_weights</code>, <code>limit.context</code> / <code>input</code> / <code>output</code>, <code>cost</code> including audio/reasoning/cache/tier prices, <code>interleaved</code>, <code>status</code>, <code>experimental</code>, and <code>provider</code>. Provider/model matching accepts the configured vendor key, normalized API URL, catalog provider identity, and unambiguous vendor-prefixed IDs; an unprefixed configured ID can match a <code>vendor/model</code> catalog ID without changing the user-visible ID.</li><li>The runtime passes the models.dev <code>ModelConfig</code> to pi-ai&#39;s selected wire adapter. pi-ai remains an implementation dependency for request serialization, OAuth login/account availability, and stream handling only; its builtin model catalog and model capability functions are not consulted.</li><li>A model absent from models.dev remains runnable through an explicit generic text model shape with conservative limits and no reasoning/vision claims. Provider <code>/models</code> discovery and authenticated OAuth availability still supply IDs that models.dev does not list, but they cannot invent metadata.</li></ol><p>Models.dev reasoning mappings retain <code>effort</code> values that match the canonical levels (<code>none</code> → <code>off</code>), map <code>toggle</code>/<code>budget_tokens</code> to <code>off</code> + <code>medium</code>, and use <code>low</code>/<code>medium</code>/<code>high</code> when a reasoning record has no usable level list. Input and output modality arrays retain <code>text</code>, <code>image</code>, <code>audio</code>, <code>video</code>, and <code>pdf</code>; the current text agent picker excludes models that cannot accept and produce text, while the raw bundled snapshot keeps those records for future surfaces. Image attachments use the transient image block supported by pi-ai; PDF capability is surfaced from the catalog, but PDF files remain bounded file references until a transport with native PDF blocks is available.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Model metadata has one authoritative source and is reproducible offline from the checked-in release resource.</li><li>Every release refreshes the catalog before tag creation without requiring a database or host-core schema migration.</li><li>Settings can fetch a newer snapshot immediately for the current process; the next app launch returns to the bundled release snapshot until a new release is shipped.</li><li>Provider bindings remain user-owned selections/overrides; the catalog owns published model semantics and the generic fallback owns unknown IDs.</li><li>Models.dev outages preserve the last local snapshot, provider endpoint discovery, and explicit custom IDs.</li><li>Changes to the models.dev schema require parser fixtures and source/runtime contract tests before release.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><h3 id="keep-pi-ai-as-a-model-metadata-fallback" tabindex="-1">Keep pi-ai as a model metadata fallback <a class="header-anchor" href="#keep-pi-ai-as-a-model-metadata-fallback" aria-label="Permalink to &quot;Keep pi-ai as a model metadata fallback&quot;">​</a></h3><p>Rejected: it creates a second authority and allows package updates to silently change the model configuration selected by the user. It remains for transport and OAuth only.</p><h3 id="keep-a-user-data-snapshot" tabindex="-1">Keep a user-data snapshot <a class="header-anchor" href="#keep-a-user-data-snapshot" aria-label="Permalink to &quot;Keep a user-data snapshot&quot;">​</a></h3><p>Rejected: the release artifact must be reproducible and the user cache would make different installations use different model configuration. Settings refresh is intentionally process-local until the next release.</p><h3 id="fetch-only-when-a-provider-dialog-opens" tabindex="-1">Fetch only when a provider dialog opens <a class="header-anchor" href="#fetch-only-when-a-provider-dialog-opens" aria-label="Permalink to &quot;Fetch only when a provider dialog opens&quot;">​</a></h3><p>Rejected: model capability state is also needed by Composer and runtime launch; the bundled release snapshot gives all surfaces one deterministic baseline. Settings refresh remains an explicit remote action.</p><h2 id="references" tabindex="-1">References <a class="header-anchor" href="#references" aria-label="Permalink to &quot;References&quot;">​</a></h2><ul><li><code>scripts/release.mjs</code> (release-time catalog refresh)</li><li><code>apps/desktop/resources/models.dev/api.json</code> (bundled snapshot)</li><li><code>apps/desktop/electron/main/models-dev-catalog.ts</code> (catalog parsing and refresh)</li><li><code>apps/desktop/electron/main/index.ts</code> (main-process loading and IPC)</li><li><code>apps/desktop/src/components/settings/ModelConfigPage.tsx</code> (settings refresh action)</li><li><code>packages/agent-runtime/src/model-capabilities.ts</code></li><li><code>packages/agent-runtime/src/provider-binding.ts</code></li><li><code>docs/spec/03-runtime/11-provider-model-system.md</code></li><li><code>docs/spec/03-runtime/12-provider-config-schema.md</code></li><li><code>docs/spec/03-runtime/13-model-catalog-and-selection.md</code></li><li><code>docs/spec/06-delivery/04-e2e-test-plan.md</code> (E2E-066, E2E-080, E2E-154)</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0134-models-dev-sole-model-metadata-source.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0134ModelsDevSoleModelMetadataSource = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0134ModelsDevSoleModelMetadataSource as default
};
