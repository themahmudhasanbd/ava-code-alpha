import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0114: Persist Provider Model Bindings and Thinking Configuration","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0114-provider-model-bindings-and-thinking-configuration.md","filePath":"adr/0114-provider-model-bindings-and-thinking-configuration.md","lastUpdated":1787305198000}');
const _sfc_main = { name: "adr/0114-provider-model-bindings-and-thinking-configuration.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0114-persist-provider-model-bindings-and-thinking-configuration" tabindex="-1">ADR 0114: Persist Provider Model Bindings and Thinking Configuration <a class="header-anchor" href="#adr-0114-persist-provider-model-bindings-and-thinking-configuration" aria-label="Permalink to &quot;ADR 0114: Persist Provider Model Bindings and Thinking Configuration&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-08-21</li><li>Deciders: PI-Desktop core</li><li>Updates ADR 0020 and ADR 0027</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The provider studio previously stored one <code>defaultModelId</code> and kept context, output, and thinking capability decisions in runtime/catalog code. That made a provider unable to retain settings for more than one model and made custom models indistinguishable from an unconfigured default. The new provider dialog needs to select several models, retain independent limits for each one, and allow users to explicitly enable or disable thinking levels.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p><code>ProviderPublic</code>, <code>ProviderCreateInput</code>, and <code>ProviderUpdateInput</code> expose a <code>models: ModelBinding[]</code> field. A binding contains:</p><div class="language-ts vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">type</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> ModelBinding</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  id</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  contextWindow</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> number</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  maxTokens</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> number</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  thinkingLevels</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> ThinkingLevel</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">[]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  defaultThinkingLevel</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> ThinkingLevel</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> null</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p>Rust host-core persists the array under <code>providers.config_json.models</code>, which keeps provider-specific settings additive and inside the existing storage owner. <code>default_model_id</code> remains a compatibility field and mirrors the first binding on new writes. Current conversations continue to resolve the first binding; a future conversation model picker may select another binding.</p><p>The built-in pi-ai catalog remains authoritative for known-model metadata. The settings UI uses its context window, max output, reasoning flag, and thinking-level map as initial values, but writes user edits into the binding. Unknown custom models use 128,000 context, 8,192 max output, no thinking levels, and a null default. A model may be manually enabled for thinking after it is created.</p><h2 id="migration-and-compatibility" tabindex="-1">Migration and compatibility <a class="header-anchor" href="#migration-and-compatibility" aria-label="Permalink to &quot;Migration and compatibility&quot;">​</a></h2><p>When a stored provider has no <code>config_json.models</code> array, host-core materializes one binding from <code>default_model_id</code> with the custom fallback values. The materialized binding is returned by <code>providers.list</code> / <code>providers.get</code>; the next provider create/update writes the new array. Existing clients may continue to send or read <code>defaultModelId</code>, and Electron main falls back through <code>models[0]?.id</code> before the legacy field when resolving a runtime model.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Multiple model configurations survive restart and provider edits.</li><li>Thinking-level fallback is deterministic: the canonical order is <code>off</code>, <code>minimal</code>, <code>low</code>, <code>medium</code>, <code>high</code>, <code>xhigh</code>, <code>max</code>.</li><li>Empty thinking support is represented by <code>thinkingLevels: []</code> and <code>defaultThinkingLevel: null</code>, rather than a fake <code>off</code> selection.</li><li>The provider storage contract changes additively without a new SQLite table or migration version.</li><li>Conversation model switching and routing policies remain explicitly out of scope; the first binding is the current runtime default.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0114-provider-model-bindings-and-thinking-configuration.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0114ProviderModelBindingsAndThinkingConfiguration = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0114ProviderModelBindingsAndThinkingConfiguration as default
};
