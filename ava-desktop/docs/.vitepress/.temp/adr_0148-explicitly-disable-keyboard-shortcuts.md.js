import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0148: Explicitly disable application keyboard shortcuts","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0148-explicitly-disable-keyboard-shortcuts.md","filePath":"adr/0148-explicitly-disable-keyboard-shortcuts.md","lastUpdated":1788455239000}');
const _sfc_main = { name: "adr/0148-explicitly-disable-keyboard-shortcuts.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0148-explicitly-disable-application-keyboard-shortcuts" tabindex="-1">ADR 0148: Explicitly disable application keyboard shortcuts <a class="header-anchor" href="#adr-0148-explicitly-disable-application-keyboard-shortcuts" aria-label="Permalink to &quot;ADR 0148: Explicitly disable application keyboard shortcuts&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-03</li><li>Deciders: PI-Desktop core</li><li>Related: Issue #31, E2E-072, ADR 0072, ADR 0076</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The shared application shortcut settings supported a missing override or a custom binding, but deleting an override always restored the default. Users could not intentionally unbind an action, and a null-like value was treated as invalid and then silently resolved back to the default.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p><code>AppSettings.keybindings</code> uses three states per application shortcut:</p><ul><li>an absent property uses the platform default;</li><li>a valid portable binding string overrides the default;</li><li>an explicit JSON <code>null</code> means <code>Unbound</code> and disables dispatch.</li></ul><p>Invalid strings continue to fall back to the default so corrupt or hand-edited settings do not silently disable product actions. The settings UI exposes Disable separately from Restore default, displays a localized Unbound state, and keeps an unbound action out of conflict checks. The application shortcut map is shared by renderer dispatch, macOS menu accelerators, and the plugin launcher.</p><p>When <code>openPluginLauncher</code> is unbound, Electron unregisters any previous global accelerator; Windows also disables the host-core <code>Alt+Space</code> hook and focused window fallback. Configurable macOS native-role items use a role-less clickable item while unbound, because Electron restores a role&#39;s default accelerator when its accelerator is omitted.</p><p>The setting remains in the existing JSON settings object. No new IPC method, protocol version, storage migration, or plugin-local shortcut behavior is added. The value is portable and not an Electron accelerator string.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Users can disable any built-in application shortcut without losing the ability to restore its default or record a new binding.</li><li><code>resolveKeybinding</code> returns <code>string | null</code>, so all consumers must handle the no-binding state explicitly.</li><li>Existing settings retain their behavior; absent and valid string entries are unchanged, while only newly persisted <code>null</code> values disable actions.</li><li>macOS menu clicks remain available even when their configurable shortcut is unbound.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0148-explicitly-disable-keyboard-shortcuts.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0148ExplicitlyDisableKeyboardShortcuts = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0148ExplicitlyDisableKeyboardShortcuts as default
};
