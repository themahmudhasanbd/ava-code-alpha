import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"07. UI Design System","description":"","frontmatter":{},"headers":[],"relativePath":"spec/04-ux/07-ui-design-system.md","filePath":"spec/04-ux/07-ui-design-system.md","lastUpdated":1789842850000}');
const _sfc_main = { name: "spec/04-ux/07-ui-design-system.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="_07-ui-design-system" tabindex="-1">07. UI Design System <a class="header-anchor" href="#_07-ui-design-system" aria-label="Permalink to &quot;07. UI Design System&quot;">​</a></h1><h2 id="_1-goals" tabindex="-1">1. Goals <a class="header-anchor" href="#_1-goals" aria-label="Permalink to &quot;1. Goals&quot;">​</a></h2><ol><li>Provide a <strong>single source of truth</strong> for visual tokens, component foundations, and layout metrics across PI-Desktop</li><li>Ensure <strong>high readability and contrast</strong> in both light and dark themes — this is a developer workstation, not a marketing surface</li><li>Map all design decisions to <strong>Tailwind CSS tokens</strong> so that spec → implementation is unambiguous</li><li>Enable <strong>future shadcn-like primitive extraction</strong> without re-specifying foundations</li></ol><h2 id="visual-baseline-codex-aligned" tabindex="-1">Visual baseline (Codex-aligned) <a class="header-anchor" href="#visual-baseline-codex-aligned" aria-label="Permalink to &quot;Visual baseline (Codex-aligned)&quot;">​</a></h2><p>The desktop shell targets a 1:1 visual match with the local Codex desktop client (ChatGPT.app electron-dark): charcoal surfaces (<code>#181818</code>), neutral gray scale (not blue-slate), ~275px sidebar, 46px toolbar rhythm, and a floating pill composer. Semantic token names remain stable; values follow the Codex gray system with a <strong>neutral gray accent</strong> (no blue brand accent).</p><h2 id="_2-non-goals" tabindex="-1">2. Non-goals <a class="header-anchor" href="#_2-non-goals" aria-label="Permalink to &quot;2. Non-goals&quot;">​</a></h2><ol><li>A consumer-brand identity system with vibrant gradients or playful illustrations</li><li>A full component library spec (that is <a href="./08-component-spec">08-component-spec.md</a>)</li><li>Custom font services or CDN font hosting — use local bundling</li><li>Complex theme marketplace or user-customizable color palettes (MVP: system/light/dark only)</li><li>Pixel-perfect Figma handoff artifacts</li></ol><h2 id="_3-visual-principles" tabindex="-1">3. Visual principles <a class="header-anchor" href="#_3-visual-principles" aria-label="Permalink to &quot;3. Visual principles&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Principle</th><th>Application</th></tr></thead><tbody><tr><td><strong>Clarity over decoration</strong></td><td>No ornamental borders, gradients, or hero images. Every visual element carries information.</td></tr><tr><td><strong>Developer density</strong></td><td>Compact spacing, small-but-readable type, minimal marketing whitespace. Information-rich, not sparse.</td></tr><tr><td><strong>Dark-base defaults</strong></td><td>Dark theme is the primary theme for a coding agent. Light must be fully supported but is secondary.</td></tr><tr><td><strong>Restraint</strong></td><td>One accent color family. No rainbow status colors — use semantic token names (success, warning, error).</td></tr><tr><td><strong>Motion as feedback</strong></td><td>Animations convey state change (streaming, loading, expand/collapse). Never decorative.</td></tr><tr><td><strong>Keyboard-first</strong></td><td>Focus rings, tab order, and shortcut labels are primary UX, not afterthoughts.</td></tr><tr><td><strong>Continuous shell chrome</strong></td><td>Titlebars stay borderless and continuous with their surfaces; reserve faint separators for boundaries that clarify ownership, such as the window-control side seam.</td></tr></tbody></table><h3 id="_3-1-ai-generated-page-copy" tabindex="-1">3.1 AI-generated page copy <a class="header-anchor" href="#_3-1-ai-generated-page-copy" aria-label="Permalink to &quot;3.1 AI-generated page copy&quot;">​</a></h3><p>AI-generated page surfaces use concise, task-oriented copy:</p><ul><li>Keep visible text to labels, headings, actions, statuses, and helper text that changes a user decision or clarifies non-obvious behavior.</li><li>Do not add filler introductions, repeated summaries, implementation notes, or prose that merely explains an obvious control or layout.</li><li>Empty states default to a title and the next useful action. Keep body text only for required context, risk, error cause, or a non-obvious next step.</li><li>Preserve permission, security, validation, destructive-action, keyboard, path/scope, and error details even when they are longer.</li><li>Put rationale, usage guidance, and implementation detail in documentation or code comments rather than page UI unless the user requests it.</li></ul><h3 id="_3-2-text-selection" tabindex="-1">3.2 Text selection <a class="header-anchor" href="#_3-2-text-selection" aria-label="Permalink to &quot;3.2 Text selection&quot;">​</a></h3><p>PI-Desktop behaves like a desktop application shell, so accidental drag selection is suppressed for chrome by default. The selection contract is:</p><ul><li>Navigation, titlebar chrome, buttons, labels, badges, menus, and other controls are not text-selectable.</li><li><code>input</code>, <code>textarea</code>, <code>select</code>, and editable content remain selectable so users can edit drafts, search, and use native <code>Cmd/Ctrl+A/C/V</code> behavior.</li><li>Transcript message bodies, rendered Markdown, code blocks, and tool input/output remain selectable for copy and inspection.</li><li>Transient surfaces whose text a user may need to keep — toast messages in particular — remain selectable; a toast&#39;s icon and dismiss control stay non-selectable chrome.</li><li>New document-like surfaces must opt into the shared <code>.selectable</code> class (or an equivalent explicit <code>user-select: text</code> rule).</li><li>The Electron renderer sets both <code>user-select</code> and <code>-webkit-user-select</code>; selection rules must not remove focus-visible rings or window drag regions.</li><li>Copyable selection paint uses the monochrome accent contract via <code>::selection</code> (<code>color-mix</code> of <code>--ds-text-primary</code> at ~18% over the surface, text remains <code>--ds-text-primary</code>). Browser-default blue highlights are not allowed on shell surfaces.</li><li><code>caret-color</code> and <code>accent-color</code> resolve to <code>--ds-text-primary</code> / <code>--ds-accent</code> so native carets and form accents stay on-theme.</li><li>Focus-visible rings use <code>color-mix(in oklab, var(--ds-accent) 80%, transparent)</code> (no white wash that drifts off the neutral ramp).</li></ul><h3 id="_3-3-locale-aware-chrome-labels" tabindex="-1">3.3 Locale-aware chrome labels <a class="header-anchor" href="#_3-3-locale-aware-chrome-labels" aria-label="Permalink to &quot;3.3 Locale-aware chrome labels&quot;">​</a></h3><p>Section labels that use Latin micro-style (<code>text-transform: uppercase</code> + <code>letter-spacing: wide</code>) must relax under <code>:lang(zh-CN)</code>:</p><ul><li><code>letter-spacing</code> returns to <code>--tracking-normal</code></li><li><code>text-transform</code> is <code>none</code> (CJK has no case and wide tracking splits glyphs)</li></ul><p>Applies to sidebar section labels, settings rail group labels, destination section labels, and keyboard-shortcut group labels.</p><p>Plugin panel chrome follows the same shell context: the host reserves exactly a transparent 46px drag band and renders only a minimal page-adaptive capsule with three window controls at the fixed top-right corner. The capsule stays inside that band, uses the panel page&#39;s computed surface/text colors with the active theme as a transparent-page fallback, and must not force a black surface on a light plugin page. The band is not clickable outside the capsule; development panels expose a localized reminder. The plugin owns its title, toolbar, and every other visible panel surface.</p><p>Plugin pages that use the current chrome contract declare <code>&lt;meta name=&quot;pi-plugin-chrome&quot; content=&quot;v2&quot;&gt;</code> and lay out normal-flow content with <code>--pi-plugin-titlebar-height</code>. Detached panels publish <code>46px</code>; docked views publish <code>0px</code>. The host does not add a second top padding to v2 pages, which keeps the capsule&#39;s safe band from becoming an empty spacer. Pages without the marker retain the additive legacy offset so older installed plugins remain usable while they migrate.</p><p>When a plugin page uses a stable scrollbar gutter, it belongs on that page&#39;s content scroller only. The root <code>html</code>/<code>body</code> viewport must not reserve another gutter, because Windows&#39; classic scrollbars make the duplicate visible as an empty rail outside the panel surface.</p><h3 id="_3-4-product-identity-and-marks" tabindex="-1">3.4 Product identity and marks <a class="header-anchor" href="#_3-4-product-identity-and-marks" aria-label="Permalink to &quot;3.4 Product identity and marks&quot;">​</a></h3><p>The visible product identity is <strong>PI-Desktop</strong>, even where the shell borrows Codex as a visual reference. The identity contract is deliberately small:</p><ul><li>The sidebar shell name, settings copy, and composer placeholder use <code>PI-Desktop</code>; <code>Codex</code> is reserved for the external session-import source or historical design-reference text.</li><li><code>build/icon_1024.png</code> is the canonical shell logo master; the renderer imports the 192x192 marks derived from it under <code>src/assets/brand/</code> (ADR 0125). <code>BrandLogo</code> imports those through Vite so the renderer bundle, development Dock, and packaged application all use the same visual asset.</li><li>On macOS, both development and packaged launches expose <code>PI-Desktop</code> as the native application-menu name. The native About panel uses the PI-Desktop name, version, and canonical icon; no stock Electron name or icon is visible. Development launches use a generated branded host bundle because AppKit reads this identity from the host bundle rather than Electron runtime APIs.</li><li>On Windows, Electron Main registers the canonical <code>net.aiuo.pi-desktop</code> AppUserModelID before readiness. The runtime ID, packaged executable name, and NSIS shortcut identity stay aligned so native notifications, notification settings, and taskbar groups identify the app as <code>PI-Desktop</code> rather than Electron.</li><li>The empty-home hero uses a 100px <code>HomeMascotLogo</code> GIF: an eight-frame waving mascot compiled from the supplied light and dark action sets, with a short idle hold on the first frame. CSS selects the pair from <code>document.documentElement[data-theme]</code>; anything other than <code>light</code> uses the dark artwork. Playback is native to the GIF. There is no random pose selection, JavaScript timer, or hover-driven speed change. Reduced motion swaps the GIF for the matching first-frame PNG without changing the 100px slot. <code>BrandLogo</code> remains 20px/18px in the expanded/collapsed sidebar and 64px in the startup splash. Composer prompt rows do not render a leading brand icon in either home or thread-docked mode.</li><li>The empty-home hero keeps three session identities: project sessions retain the underlined project action, temporary sessions use dedicated temporary chat copy without a project or folder action, and no active session uses the generic welcome copy. The state is derived from the selected session, not merely from the visible workspace.</li><li>New-session controls use the dedicated message-plus icon at 15–16px. The generic plus icon remains reserved for non-session additions such as adding a project.</li><li>Marks are decorative (<code>aria-hidden</code>); the surrounding controls provide the localized accessible names and keyboard behavior.</li></ul><h2 id="_4-color-tokens" tabindex="-1">4. Color tokens <a class="header-anchor" href="#_4-color-tokens" aria-label="Permalink to &quot;4. Color tokens&quot;">​</a></h2><h3 id="_4-1-semantic-token-naming" tabindex="-1">4.1 Semantic token naming <a class="header-anchor" href="#_4-1-semantic-token-naming" aria-label="Permalink to &quot;4.1 Semantic token naming&quot;">​</a></h3><p>All color references in components use <strong>semantic token names</strong>, never raw hex values.</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>--color-bg-primary        → main background (chat area, panels)</span></span>
<span class="line"><span>--color-bg-secondary      → sidebar, cards, nested surfaces</span></span>
<span class="line"><span>--color-bg-tertiary       → hover states, elevated surfaces</span></span>
<span class="line"><span>--color-bg-inset          → code blocks, inset areas</span></span>
<span class="line"><span>--color-text-primary      → main body text</span></span>
<span class="line"><span>--color-text-secondary    → secondary/label text</span></span>
<span class="line"><span>--color-text-muted        → disabled, placeholder, hint</span></span>
<span class="line"><span>--color-border-default    → default borders</span></span>
<span class="line"><span>--color-border-subtle     → subtle separators (divider lines)</span></span>
<span class="line"><span>--color-accent            → primary accent (CTA, active states)</span></span>
<span class="line"><span>--color-accent-hover      → accent hover</span></span>
<span class="line"><span>--color-success           → success/run states</span></span>
<span class="line"><span>--color-warning           → warning/caution states</span></span>
<span class="line"><span>--color-error             → error/denied states</span></span>
<span class="line"><span>--color-info              → informational states</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br></div></div><h3 id="_4-2-dark-theme-primary" tabindex="-1">4.2 Dark theme (primary) <a class="header-anchor" href="#_4-2-dark-theme-primary" aria-label="Permalink to &quot;4.2 Dark theme (primary)&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Token</th><th>Hex</th><th>Tailwind mapping</th><th>Usage</th></tr></thead><tbody><tr><td><code>--color-bg-primary</code></td><td><code>#181818</code></td><td>Codex <code>gray-900</code></td><td>Main surface</td></tr><tr><td><code>--color-bg-sidebar</code> / under</td><td><code>#000000</code> (dark) / <code>#f3f3f3</code> (light)</td><td>Codex <code>surface-under</code> / gray-75</td><td>Sidebar rail</td></tr><tr><td><code>--ds-bg-sidebar-image</code></td><td><code>none</code> (optional <code>&lt;image&gt;</code>)</td><td>—</td><td>Sidebar <code>background-image</code> only (gradients / pictures). <code>--ds-bg-sidebar</code> stays a color for glass tint, borders, and <code>color-mix</code></td></tr><tr><td><code>--color-bg-secondary</code></td><td><code>#212121</code></td><td>Codex <code>gray-800</code></td><td>Elevated surfaces, composer</td></tr><tr><td><code>--color-bg-tertiary</code></td><td><code>#282828</code></td><td>Codex <code>gray-750</code></td><td>Hover / opaque elevated</td></tr><tr><td><code>--color-bg-inset</code></td><td><code>#0d0d0d</code></td><td>Codex <code>gray-1000</code></td><td>Code blocks, deepest inset</td></tr><tr><td><code>--color-text-primary</code></td><td><code>#FFFFFF</code></td><td>Codex <code>gray-0</code></td><td>Body text</td></tr><tr><td><code>--color-text-secondary</code></td><td><code>rgba(255,255,255,0.70)</code></td><td>Codex secondary</td><td>Labels, secondary</td></tr><tr><td><code>--color-text-muted</code></td><td><code>#5d5d5d</code></td><td>Codex <code>gray-500</code></td><td>Disabled, hints</td></tr><tr><td><code>--color-border-default</code></td><td><code>rgba(255,255,255,0.08)</code></td><td>Codex border</td><td>Default borders</td></tr><tr><td><code>--color-border-subtle</code></td><td><code>rgba(255,255,255,0.05)</code></td><td>Codex border subtle</td><td>Subtle separators</td></tr><tr><td><code>--color-accent</code></td><td><code>#FFFFFF</code> (dark) / <code>#1a1c1f</code> (light)</td><td>inverted gray ink</td><td>Primary accent, CTA</td></tr><tr><td><code>--color-accent-hover</code></td><td><code>#EDEDED</code> (dark) / <code>#303030</code> (light)</td><td>gray-100 / gray-700</td><td>Accent hover</td></tr><tr><td><code>--color-accent-soft</code></td><td><code>#AFAFAF</code> (dark) / <code>#5d5d5d</code> (light)</td><td>gray-300 / gray-500</td><td>Soft accent, links</td></tr><tr><td><code>--color-success</code></td><td><code>#22C55E</code></td><td><code>text-green-500</code></td><td>Success, run complete</td></tr><tr><td><code>--color-warning</code></td><td><code>#F59E0B</code></td><td><code>text-amber-500</code></td><td>Warning, caution</td></tr><tr><td><code>--color-error</code></td><td><code>#EF4444</code></td><td><code>text-red-500</code></td><td>Error, denied</td></tr><tr><td><code>--color-info</code></td><td><code>#6366F1</code></td><td><code>text-indigo-500</code></td><td>Informational</td></tr></tbody></table><h3 id="_4-3-light-theme-codex-electron-light" tabindex="-1">4.3 Light theme (Codex electron-light) <a class="header-anchor" href="#_4-3-light-theme-codex-electron-light" aria-label="Permalink to &quot;4.3 Light theme (Codex electron-light)&quot;">​</a></h3><p>Neutral gray scale only — no blue-slate surfaces. Chrome components must consume semantic <code>--ds-*</code> tokens so light ink stays dark on <code>#f3f3f3</code> / <code>#ffffff</code>.</p><table tabindex="0"><thead><tr><th>Token</th><th>Hex / value</th><th>Usage</th></tr></thead><tbody><tr><td><code>--color-bg-primary</code></td><td><code>#ffffff</code></td><td>Main surface</td></tr><tr><td><code>--color-bg-secondary</code> / sidebar</td><td><code>#f3f3f3</code> (gray-75)</td><td>Sidebar surface</td></tr><tr><td><code>--color-bg-tertiary</code></td><td><code>#f3f3f3</code></td><td>Nested / hover base</td></tr><tr><td><code>--color-bg-inset</code></td><td><code>#ededed</code> (gray-100)</td><td>Code blocks, inset</td></tr><tr><td><code>--color-text-primary</code></td><td><code>#1a1c1f</code></td><td>Body + brand</td></tr><tr><td><code>--color-text-secondary</code></td><td><code>color-mix(#1a1c1f 70%, transparent)</code></td><td>Nav items, chips, thread titles</td></tr><tr><td><code>--color-text-muted</code></td><td><code>#5d5d5d</code> (gray-500)</td><td>Section labels</td></tr><tr><td><code>--color-text-faint</code></td><td><code>#afafaf</code> (gray-300)</td><td>Placeholder</td></tr><tr><td><code>--color-border-default</code></td><td><code>color-mix(#1a1c1f 8%, transparent)</code></td><td>Default borders</td></tr><tr><td><code>--color-border-subtle</code></td><td><code>color-mix(#1a1c1f 5%, transparent)</code></td><td>Sidebar edge / dividers</td></tr><tr><td><code>--color-accent</code></td><td><code>#1a1c1f</code></td><td>Primary accent, CTA, footer badge (neutral ink)</td></tr><tr><td><code>--color-success</code> / warning / error</td><td>green-500 / orange-500 / red-500</td><td>Status</td></tr></tbody></table><p><strong>Invariant:</strong> never paint chrome text with raw <code>gray-0</code> (<code>#fff</code>) under <code>data-theme=&quot;light&quot;</code>. Use <code>--ds-text-primary</code> / <code>--ds-text-secondary</code>.</p><p>Shared buttons must use semantic theme tokens for both their surface and ink: primary actions pair <code>--ds-accent</code> with <code>--ds-bg-primary</code>, while secondary actions sit on the <code>--ds-tile</code> fill with primary text and no stroke (D297). Hover states use the corresponding accent/tile-hover tokens rather than opacity-only changes, so actions remain legible in dark and light themes.</p><p>Light-surface polish (D148):</p><ul><li>Docked work panel uses quiet inset paper (<code>#fafafa</code>) with a white header band and a combined create trigger in the header so the tool column stays on content without any divider (D297 removed the remaining edge rules).</li><li>The work-panel header spends one control gap (<code>--ds-work-panel-control-gap</code>, 4px) on the whole row: the tab strip to the action group, <code>+</code> to maximize, and — through the tokenized 44px safe lane the panel header reserves for the viewport-fixed panel toggle — the action group to that toggle. The three buttons read as one group with no divider between the maximize control and the collapse toggle. Because maximize sits between them, the <code>+</code> trigger still keeps more than 24px of visual separation from the toggle hit target on supported window sizes. All three are the shared chrome icon control — 28px square on a transparent seat, hover wash on pointer, dimmed when disabled — so <code>+</code>, maximize, and the collapse toggle stay quiet icons instead of filled or raised squares. The toggle&#39;s <code>aria-pressed</code> state changes glyph and ink only.</li><li>Shared form fields, browser URL, settings segment tracks, and shortcut keycaps use <code>--ds-tile</code> fills with no stroke (D297); focus lifts to white with an accent-tinted ring. An Unbound shortcut uses a localized text state instead of an empty keycap and keeps its recorder and restore controls keyboard-focusable.</li><li>Settings toggles keep a near-black on-track and force a white knob in light mode. Off/on track and knob colours come from the <code>--ds-switch-*</code> theme tokens; a per-theme <code>:root[data-theme=&quot;…&quot;] .settings-toggle</code> background override out-specifies <code>.settings-toggle.on</code> and strands the on-state on the off fill.</li><li>Switch off-state carries a dim fill plus a 1px inset ring so an empty track still reads as a control; the dark-theme off knob stays light (<code>--gray-300</code>) so the knob does not disappear into the track.</li><li>Dialog scrim softens to ~28% ink so elevated white dialogs remain readable.</li></ul><h3 id="_4-4-system-theme-behavior" tabindex="-1">4.4 System theme behavior <a class="header-anchor" href="#_4-4-system-theme-behavior" aria-label="Permalink to &quot;4.4 System theme behavior&quot;">​</a></h3><ul><li><code>system</code> theme follows <code>prefers-color-scheme</code> media query</li><li>Transition between themes must not flash white when switching to dark on launch</li><li>Initial load: detect system preference before first paint (Electron preload can relay this)</li><li>Native controls inherit the active <code>color-scheme</code>. Every native <code>select</code> trigger and its opened <code>option</code>/<code>optgroup</code> list also uses opaque semantic foreground/background colors, so Windows Chromium does not fall back to an unreadable system palette outside Settings either.</li></ul><h3 id="_4-5-sidebar-task-status-semantics" tabindex="-1">4.5 Sidebar task status semantics <a class="header-anchor" href="#_4-5-sidebar-task-status-semantics" aria-label="Permalink to &quot;4.5 Sidebar task status semantics&quot;">​</a></h3><p>Compact task rows reserve one <code>12px</code> leading status slot. State is never communicated by color alone, and each status consumes an existing semantic token rather than introducing a decorative palette:</p><table tabindex="0"><thead><tr><th>State</th><th>Semantic color</th><th>Shape / motion</th><th>Meaning</th></tr></thead><tbody><tr><td>Selected</td><td>neutral accent</td><td>static outlined ring</td><td>current conversation</td></tr><tr><td>In progress</td><td>warning orange</td><td>filled dot with a restrained breathing pulse</td><td>agent is producing or executing</td></tr><tr><td>Completed</td><td>success green</td><td>check mark</td><td>latest unread task turn completed</td></tr><tr><td>Failed</td><td>error red</td><td>circled alert mark</td><td>latest unread task turn failed</td></tr></tbody></table><p>Precedence is <code>in progress → selected → completed/failed</code>. Starting another turn clears the prior terminal outcome; abort clears the live indicator without creating a failure. Opening a conversation acknowledges its unread terminal outcome: the terminal mark clears immediately and the matching durable task notification is marked read so the mark cannot return after a notification refresh or app restart. Outcomes already marked read never produce a terminal mark. Reduced-motion mode disables the breathing animation while retaining its orange fill and localized accessible name.</p><h3 id="_4-6-tailwind-css-variable-stub" tabindex="-1">4.6 Tailwind CSS variable stub <a class="header-anchor" href="#_4-6-tailwind-css-variable-stub" aria-label="Permalink to &quot;4.6 Tailwind CSS variable stub&quot;">​</a></h3><p>The following CSS custom properties stub is the canonical bridge between spec tokens and Tailwind classes. It is <strong>not an app source file</strong> — it documents the intended mapping for implementation.</p><div class="language-css vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">css</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">/* === Design System Token Bridge (spec reference, not runtime file) === */</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">/* Dark theme (default) */</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">:root</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">[</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">data-theme</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;dark&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">] {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  --color-bg-primary</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:       </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">#181818</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  --color-bg-secondary</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:     </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">#212121</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  --color-bg-tertiary</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:      </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">#282828</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  --color-bg-inset</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:         </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">#0d0d0d</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  --color-text-primary</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:     </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">#FFFFFF</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  --color-text-secondary</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:   </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">rgba</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">255</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">255</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">255</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">0.70</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">);</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  --color-text-muted</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:       </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">#5d5d5d</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  --color-border-default</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:   </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">#282828</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  --color-border-subtle</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:    </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">#212121</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  --color-accent</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:           </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">#FFFFFF</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  --color-accent-hover</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:     </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">#EDEDED</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  --color-success</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:          </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">#22C55E</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  --color-warning</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:          </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">#F59E0B</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  --color-error</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:            </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">#EF4444</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  --color-info</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:             </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">#6366F1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">/* Light theme */</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">:root</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">[</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">data-theme</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;light&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">] {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  --color-bg-primary</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:       </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">#FFFFFF</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  --color-bg-secondary</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:     </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">#FFFFFF</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  --color-bg-tertiary</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:      </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">#F1F5F9</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  --color-bg-inset</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:         </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">#F1F5F9</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  --color-text-primary</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:     </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">#181818</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  --color-text-secondary</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:   </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">#475569</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  --color-text-muted</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:       </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">#94A3B8</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  --color-border-default</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:   </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">#E2E8F0</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  --color-border-subtle</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:    </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">#F1F5F9</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  --color-accent</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:           </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">#1a1c1f</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  --color-accent-hover</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:     </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">#303030</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  --color-success</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:          </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">#16A34A</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  --color-warning</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:          </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">#D97706</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  --color-error</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:            </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">#DC2626</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  --color-info</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:             </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">#4F46E5</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">/* Tailwind v4 theme extension (in tailwind config) */</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">/* Maps semantic tokens to utility classes */</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">/*</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">@theme {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">  --color-bg-primary:    var(--color-bg-primary);</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">  --color-bg-secondary:  var(--color-bg-secondary);</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">  --color-bg-tertiary:   var(--color-bg-tertiary);</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">  --color-bg-inset:      var(--color-bg-inset);</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">  --color-text-primary:  var(--color-text-primary);</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">  --color-text-secondary: var(--color-text-secondary);</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">  --color-text-muted:    var(--color-text-muted);</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">  --color-border-default: var(--color-border-default);</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">  --color-border-subtle:  var(--color-border-subtle);</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">  --color-accent:        var(--color-accent);</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">  --color-accent-hover:  var(--color-accent-hover);</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">  --color-success:       var(--color-success);</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">  --color-warning:       var(--color-warning);</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">  --color-error:         var(--color-error);</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">  --color-info:          var(--color-info);</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">}</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">*/</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br><span class="line-number">19</span><br><span class="line-number">20</span><br><span class="line-number">21</span><br><span class="line-number">22</span><br><span class="line-number">23</span><br><span class="line-number">24</span><br><span class="line-number">25</span><br><span class="line-number">26</span><br><span class="line-number">27</span><br><span class="line-number">28</span><br><span class="line-number">29</span><br><span class="line-number">30</span><br><span class="line-number">31</span><br><span class="line-number">32</span><br><span class="line-number">33</span><br><span class="line-number">34</span><br><span class="line-number">35</span><br><span class="line-number">36</span><br><span class="line-number">37</span><br><span class="line-number">38</span><br><span class="line-number">39</span><br><span class="line-number">40</span><br><span class="line-number">41</span><br><span class="line-number">42</span><br><span class="line-number">43</span><br><span class="line-number">44</span><br><span class="line-number">45</span><br><span class="line-number">46</span><br><span class="line-number">47</span><br><span class="line-number">48</span><br><span class="line-number">49</span><br><span class="line-number">50</span><br><span class="line-number">51</span><br><span class="line-number">52</span><br><span class="line-number">53</span><br><span class="line-number">54</span><br><span class="line-number">55</span><br><span class="line-number">56</span><br><span class="line-number">57</span><br><span class="line-number">58</span><br><span class="line-number">59</span><br><span class="line-number">60</span><br></div></div><p>Implementation note: Tailwind v4 supports CSS-first configuration. The <code>@theme</code> directive maps custom properties to utility classes (<code>bg-bg-primary</code>, <code>text-text-primary</code>, etc.). Implementation should validate naming to avoid double-prefix collision (e.g., <code>bg-bg</code> is awkward — consider aliasing to <code>bg-primary</code>, <code>text-primary</code> etc. at the Tailwind level).</p><h2 id="_5-typography" tabindex="-1">5. Typography <a class="header-anchor" href="#_5-typography" aria-label="Permalink to &quot;5. Typography&quot;">​</a></h2><h3 id="_5-1-font-stacks" tabindex="-1">5.1 Font stacks <a class="header-anchor" href="#_5-1-font-stacks" aria-label="Permalink to &quot;5.1 Font stacks&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Role</th><th>Primary</th><th>Fallback stack</th><th>Tailwind</th></tr></thead><tbody><tr><td><strong>UI (sans)</strong></td><td>Inter</td><td><code>system-ui, -apple-system, BlinkMacSystemFont, &quot;Segoe UI&quot;, Roboto, &quot;Helvetica Neue&quot;, Arial, sans-serif</code></td><td><code>font-sans</code></td></tr><tr><td><strong>Code (mono)</strong></td><td>JetBrains Mono</td><td><code>&quot;Fira Code&quot;, ui-monospace, &quot;Cascadia Code&quot;, &quot;SF Mono&quot;, Menlo, Consolas, &quot;DejaVu Sans Mono&quot;, monospace</code></td><td><code>font-mono</code></td></tr></tbody></table><p>The UI stack is user-overridable from Settings → Basics → Appearance (ADR 0083). The Font row persists a CSS stack in <code>AppSettings.fontFamily</code>; an absent or empty value keeps the token stack above. The app ships no font files (ADR 0298): the picker offers the System default and the installed system families enumerated by Electron main, and a stack saved while a removed family existed still appears under Saved. Every generated stack ends in the system-only CJK fallback tier (<code>PingFang SC</code>, <code>Hiragino Sans GB</code>, <code>Microsoft YaHei</code>, <code>sans-serif</code>) so Chinese text stays readable. The mono stack (<code>--font-mono</code>) is not user-configurable.</p><p>The Font size row (D343 / ADR 0180) persists an optional multiplier in <code>AppSettings.fontScale</code> (default 1, range 0.8–1.5). The renderer sets <code>--font-scale</code> on the root so every <code>--text-*</code> token (and <code>--leading-row</code>) scales in proportion. Shared Lucide icons use the same multiplier. Window zoom remains independent. The UI never asks for a px value.</p><h3 id="_5-2-type-scale" tabindex="-1">5.2 Type scale <a class="header-anchor" href="#_5-2-type-scale" aria-label="Permalink to &quot;5.2 Type scale&quot;">​</a></h3><p>All font sizes come from the <code>--text-*</code> ramp defined in the <code>@theme</code> block of <code>styles/tokens.css</code> (imported first by <code>styles/globals.css</code>, which is now only an import sequence — see D170). Raw px literals for <code>font-size</code>, <code>font-weight</code>, <code>line-height</code>, and <code>letter-spacing</code> are <strong>forbidden</strong> in component CSS and TSX arbitrary utilities (<code>text-[13px]</code> etc.) — enforced by <code>scripts/check-style-tokens.mjs</code> (runs in <code>pnpm lint</code>). <code>-plus</code> suffixed tokens are the Codex half-steps between named sizes.</p><table tabindex="0"><thead><tr><th>Token</th><th>Size</th><th>Usage</th></tr></thead><tbody><tr><td><code>--text-3xs</code></td><td>10.5px</td><td>Smallest chrome (kbd hints)</td></tr><tr><td><code>--text-xs</code> (alias <code>--text-2xs</code>)</td><td>11px</td><td>Timestamps, badges, tool status</td></tr><tr><td><code>--text-xs-plus</code></td><td>11.5px</td><td>Muted metadata, menu subtitles</td></tr><tr><td><code>--text-sm</code></td><td>12px</td><td>Secondary labels, tool rows, sidebar section labels</td></tr><tr><td><code>--text-sm-plus</code></td><td>12.5px</td><td>Chips, working indicator, code text</td></tr><tr><td><code>--text-md</code></td><td>13px</td><td>Sidebar session/project titles, empty-state copy, compact chrome</td></tr><tr><td><code>--text-md-plus</code></td><td>13.5px</td><td>Composer labels, list rows</td></tr><tr><td><code>--text-base</code></td><td>14px</td><td>Body text, chat messages, input, primary sidebar chrome</td></tr><tr><td><code>--text-base-plus</code></td><td>15px</td><td>Brand, prominent labels</td></tr><tr><td><code>--text-lg</code></td><td>16px</td><td>Section headers, card titles</td></tr><tr><td><code>--text-lg-plus</code></td><td>18px</td><td>Large card titles</td></tr><tr><td><code>--text-xl</code></td><td>20px</td><td>Page-level emphasis</td></tr><tr><td><code>--text-2xl</code></td><td>28px</td><td>Destination page titles, home hero</td></tr></tbody></table><p>Line-height tokens: <code>--leading-none</code> 1, <code>--leading-heading</code> 1.15, <code>--leading-tighter</code> 1.2, <code>--leading-tight</code> 1.25, <code>--leading-compact</code> 1.3, <code>--leading-compact-plus</code> 1.35, <code>--leading-normal</code> 1.4, <code>--leading-body</code> 1.45 (app default), <code>--leading-relaxed</code> 1.5, <code>--leading-chat</code> 1.55, <code>--leading-prose</code> 1.6, <code>--leading-row</code> 18px (fixed-height sidebar rows).</p><p>Letter-spacing tokens: <code>--tracking-tighter</code> −0.03em, <code>--tracking-tight</code> −0.02em, <code>--tracking-normal</code> 0, <code>--tracking-wide</code> 0.02em.</p><blockquote><p>Note: 14px base is intentional for developer-density. Do not bump to 16px default. Users who want larger or denser type change Font size in Appearance; that multiplies the whole <code>--text-*</code> ramp through <code>--font-scale</code>.</p><p>Sidebar primary chrome (nav items, footer identity, profile menu actions) uses <code>--text-base</code> so the left rail matches main body readability. Session titles, project/group titles, and empty-state copy use the compact <code>--text-md</code> tier; only section labels and secondary metadata use <code>--text-sm</code>. Never use the micro <code>--text-xs</code> band for primary list content.</p></blockquote><h3 id="_5-3-code-text-sizing" tabindex="-1">5.3 Code text sizing <a class="header-anchor" href="#_5-3-code-text-sizing" aria-label="Permalink to &quot;5.3 Code text sizing&quot;">​</a></h3><ul><li>Code blocks and tool output: <code>--text-sm</code>/<code>--text-sm-plus</code> with <code>font-mono</code></li><li>Inline code within messages: <code>--text-sm-plus</code> <code>font-mono</code>, soft text-tint background, borderless, rounded</li><li>Chat prose (<code>.prose-chat</code>) uses <code>--text-base</code> / <code>--leading-prose</code> for body, with a heading ramp of <code>text-xl</code> → <code>text-lg-plus</code> → <code>text-lg</code> → <code>text-base-plus</code> → <code>text-base</code> so multi-block answers stay scannable without document-scale drama. Headings carry no rules/borders (hierarchy comes from size, weight, and space above); links keep a soft permanent underline that firms up on hover instead of relying on color alone; blockquotes are a quiet 2px left rail without a background fill</li></ul><h3 id="_5-4-weight-rules" tabindex="-1">5.4 Weight rules <a class="header-anchor" href="#_5-4-weight-rules" aria-label="Permalink to &quot;5.4 Weight rules&quot;">​</a></h3><p>Weights use <code>--font-weight-*</code> tokens only (Codex uses variable-font intermediate weights):</p><ul><li><code>--font-weight-normal</code> 400: default body/label text</li><li><code>--font-weight-medium</code> 500: emphasis, chips, row titles</li><li><code>--font-weight-medium-plus</code> 520: select Codex chrome labels</li><li><code>--font-weight-strong</code> 560: destination page titles (Codex electron metric)</li><li><code>--font-weight-semibold</code> 600: brand, CTA buttons</li><li>Never use 700+</li></ul><h2 id="_6-spacing-radius-elevation-borders" tabindex="-1">6. Spacing, radius, elevation, borders <a class="header-anchor" href="#_6-spacing-radius-elevation-borders" aria-label="Permalink to &quot;6. Spacing, radius, elevation, borders&quot;">​</a></h2><h3 id="_6-1-spacing-scale" tabindex="-1">6.1 Spacing scale <a class="header-anchor" href="#_6-1-spacing-scale" aria-label="Permalink to &quot;6.1 Spacing scale&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Token</th><th>Value</th><th>Usage</th></tr></thead><tbody><tr><td><code>space-0.25</code></td><td>1px</td><td>Hairline row gaps in dense list rhythms (sidebar session rows, settings rail items)</td></tr><tr><td><code>space-0.5</code></td><td>2px</td><td>Tight inline gaps</td></tr><tr><td><code>space-1</code></td><td>4px</td><td>Icon-text gaps, badge padding</td></tr><tr><td><code>space-1.5</code></td><td>6px</td><td>Compact inner padding</td></tr><tr><td><code>space-2</code></td><td>8px</td><td>Standard inner padding, list gaps</td></tr><tr><td><code>space-3</code></td><td>12px</td><td>Card inner padding, section gaps</td></tr><tr><td><code>space-4</code></td><td>16px</td><td>Section margins, composer padding</td></tr><tr><td><code>space-6</code></td><td>24px</td><td>Panel gaps, major separations</td></tr><tr><td><code>space-8</code></td><td>32px</td><td>Page-level margins (rare)</td></tr></tbody></table><h3 id="_6-2-radius-scale" tabindex="-1">6.2 Radius scale <a class="header-anchor" href="#_6-2-radius-scale" aria-label="Permalink to &quot;6.2 Radius scale&quot;">​</a></h3><p>All radii come from <code>--radius-*</code> tokens (raw px forbidden, same guard as typography):</p><p>The scale follows Apple&#39;s current shape guidance while preserving the density of a desktop coding tool:</p><ul><li>Fixed rounded rectangles are the default for compact and medium controls.</li><li>Capsules are reserved for pills, segmented selections, status labels, and intentionally prominent actions; circles are reserved for equal-width icon controls, avatars, and dots.</li><li>Nested surfaces should be concentric where their corners visually align: <code>outer radius = inner radius + the gap between their edges</code>.</li><li>Radius grows with surface size and elevation. Full-width structural panels such as the sidebar, titlebar, and work panel remain square at the window edge.</li></ul><table tabindex="0"><thead><tr><th>Token</th><th>Value</th><th>Usage</th></tr></thead><tbody><tr><td><code>--radius-3xs</code></td><td>4px</td><td>Inline code</td></tr><tr><td><code>--radius-2xs</code></td><td>6px</td><td>Small inline chips</td></tr><tr><td><code>--radius-xs</code></td><td>8px</td><td>Compact buttons, copy buttons</td></tr><tr><td><code>--radius-sm</code></td><td>10px</td><td>Standard buttons, inputs, menu items, tool rows, kbd</td></tr><tr><td><code>--radius-md</code></td><td>12px</td><td>Menus and compact cards</td></tr><tr><td><code>--radius-md-plus</code></td><td>14px</td><td>Cards and code blocks</td></tr><tr><td><code>--radius-lg</code></td><td>16px</td><td>Panels and settings cards</td></tr><tr><td><code>--radius-lg-plus</code></td><td>18px</td><td>Large panels and dialogs</td></tr><tr><td><code>--radius-xl</code></td><td>20px</td><td>Message bubbles and the composer</td></tr><tr><td><code>--radius-2xl</code></td><td>24px</td><td>Composer-adjacent prominent surfaces</td></tr><tr><td><code>--radius-full</code></td><td>9999px</td><td>Pills, badges, scroll thumbs</td></tr><tr><td><code>--radius-round</code></td><td>50%</td><td>Circular buttons, avatars, dots</td></tr></tbody></table><h3 id="_6-3-elevation-shadows" tabindex="-1">6.3 Elevation / shadows <a class="header-anchor" href="#_6-3-elevation-shadows" aria-label="Permalink to &quot;6.3 Elevation / shadows&quot;">​</a></h3><p>Dark theme: elevation is expressed via <strong>background surface layering</strong> (bg-secondary → bg-tertiary), not box-shadow.</p><p>Light theme: minimal shadows only where layering is insufficient.</p><table tabindex="0"><thead><tr><th>Level</th><th>Dark</th><th>Light</th><th>Usage</th></tr></thead><tbody><tr><td><code>elevation-0</code></td><td>flat (bg-primary)</td><td>flat (bg-primary)</td><td>Default surface</td></tr><tr><td><code>elevation-1</code></td><td>bg-secondary</td><td>bg-secondary + <code>shadow-sm</code></td><td>Cards, sidebar</td></tr><tr><td><code>elevation-2</code></td><td>bg-tertiary</td><td>bg-tertiary + <code>shadow-md</code></td><td>Hover, dropdowns</td></tr><tr><td><code>elevation-3</code></td><td>bg-tertiary + border-accent</td><td>bg-white + <code>shadow-lg</code></td><td>Dialogs, overlays</td></tr></tbody></table><p>Sidebar footer: a transparent utility band with no separator. The Settings, Plugins and notification icon buttons stay grouped on the left. The build/version chip is right-aligned and remains the update check/release entry point. Hover and active states use semantic sidebar surfaces; neither side adds a persistent card fill.</p><p>Every scroll container in the renderer uses one quiet scrollbar: 6px, trackless, with a thumb that is transparent at rest. The thumb appears only while the pointer is over the owning scroll region, the region contains the keyboard focus, or the region is scrolling (the renderer marks the scrolling element with <code>data-scrolling</code> for 300ms after the last scroll event, so wheel, trackpad, keyboard, and pinned-follow scrolls all reveal it); it strengthens under the pointer and while dragged. Scrollbars are styled only through the <code>::-webkit-scrollbar</code> pseudo-elements. Partials never set <code>scrollbar-width</code> or <code>scrollbar-color</code>, because WebKit and Chromium then ignore the pseudo-elements and the surface falls back to an always-visible native bar. Reserved gutters (<code>scrollbar-gutter: stable</code>) stay where layout needs them; they are simply empty at rest. Sidebar lists use this same rule without a narrower or darker override, so the navigation tree, conversation, and work-panel scrollbars remain visually consistent on Windows as well as macOS and Linux.</p><p>The preload-owned document for a docked or detached plugin panel applies the same 6px contract and scroll-reveal mark. This keeps first-party surfaces such as the Files view aligned with the host renderer; the external page loaded inside the Browser guest remains page-owned and keeps its own scrollbar style.</p><p>The expanded sidebar is user-resizable from 240px to 520px (default 275px). Dragging the right-edge handle below 160px collapses the column. Collapse/open preserves the preferred expanded width.</p><p>The profile menu is <code>280px</code> wide, opens <code>8px</code> above the footer, and uses the standard opaque elevated-menu surface, subtle border, and dialog shadow. Its first block repeats the local identity with the same glyph and two-line text, followed by a divider and compact Settings / Logs / Theme rows.</p><p>Toolbar rows are 46px. macOS places traffic lights at <code>{x:16,y:16}</code> and keeps the expanded sidebar&#39;s Collapse sidebar icon button right-aligned in that same row. The macOS row omits the sidebar logo/title, reserves <code>88px</code> on the left for native chrome in windowed mode, and reclaims that padding in fullscreen. That reserve is the shared <code>--ds-window-lead-inset</code> token — the cluster&#39;s <code>76px</code> right edge (the same <code>@pi-desktop/shared</code> geometry the main process positions the buttons with) plus <code>12px</code> of breathing room. Windows/Linux keep the identity and sidebar actions in their first row and reserve the rightmost 120px for three frameless-window controls. The controls retain 112px of full-height hit targets, while the outer band adds an 8px visual buffer before adjacent work-panel actions. The band paints an opaque <code>bg-primary</code> surface so page content never shows through the controls, and its leading and bottom edges use the same <code>border-subtle</code> rule as the adjacent titlebar so the 46px chrome reads as one continuous surface. Main, Settings, and work-panel drag regions must terminate before this reservation rather than overlap it and rely only on descendant <code>no-drag</code>, so every visible control pixel remains clickable. Termination is geometric: a region ends where the element&#39;s border box ends, so an element that only pads its content clear of the band still covers the controls with its rectangle. The open work-panel header uses a horizontally scrollable tab strip with a fixed <code>+</code> add trigger; each tab owns its close action and the header does not add a second <code>×</code> beside the native Windows close control. The band floats over the destination pages, so on Windows/Linux a page frame and any right-edge detail sheet start below it instead of placing their own header actions or close control under the window controls. No application menu is rendered inside the window. Other menu popovers use the standard opaque elevated-menu surface, <code>radius-sm</code>, subtle border, and dialog shadow; they are never translucent over readable content.</p><p>All renderer-owned custom dropdowns and menus are viewport-fixed floating layers: they are body-portaled (or use the shared anchored-menu primitive), measured before reveal, clamped to the viewport, and repositioned when the anchor or viewport moves. Opening one never adds to or squeezes its parent layout. The work-panel header has no dropdown: its <code>+</code> action creates a real New launcher tab, and the tool choices live in that tab&#39;s body. Native plugin surfaces such as Browser therefore keep their full measured bounds while the user creates a new page or selects another tab. Native <code>&lt;select&gt;</code> popups remain OS-owned and are outside this renderer contract.</p><p>Composer elevation (Codex <code>elevation-prominent</code>):</p><ul><li>soft: <code>0 3px 7.5px rgba(0,0,0,0.039)</code> + <code>0 0 20px rgba(0,0,0,0.051)</code> (Codex <code>#0000000a</code> / <code>#0000000d</code>, both themes)</li><li>no stroke (D297): the composer lifts by shadow alone; <code>--ds-elevation-stroke</code> remains a token for floating layers only</li></ul><p>Shadow token values (light theme only):</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>shadow-sm:  0 1px 2px rgba(0,0,0,0.05)</span></span>
<span class="line"><span>shadow-md:  0 2px 8px rgba(0,0,0,0.08)</span></span>
<span class="line"><span>shadow-lg:  0 8px 24px rgba(0,0,0,0.12)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><h3 id="_6-4-border-rules" tabindex="-1">6.4 Border rules <a class="header-anchor" href="#_6-4-border-rules" aria-label="Permalink to &quot;6.4 Border rules&quot;">​</a></h3><p>In-flow surfaces draw no strokes (D297). Structure inside a page comes from three tonal layers plus spacing, and the border tokens are reserved for floating layers where an edge is an elevation cue rather than a partition.</p><table tabindex="0"><thead><tr><th>Layer</th><th>Token</th><th>Use</th></tr></thead><tbody><tr><td>Page</td><td><code>--ds-bg-primary</code></td><td>The route or dialog body itself</td></tr><tr><td>Tile</td><td><code>--ds-tile</code> (3.5% text mix); hover <code>--ds-tile-hover</code> (6%); deep <code>--ds-tile-deep</code> (8%)</td><td>Panels, list rows, cards, form fields, chips, code blocks, empty states</td></tr><tr><td>Raised</td><td><code>--ds-raised</code> + <code>--ds-raised-shadow</code></td><td>The active pill of a segmented control, a disclosed detail block, a recorder keycap</td></tr><tr><td>Dock</td><td><code>--ds-bg-dock</code> (the column), <code>--ds-bg-dock-raised</code> (its header and viewer strips)</td><td>The work-panel column and the bars inside it. Both are tokens, not literals, so a contributed theme can move them (D419)</td></tr><tr><td>Sidebar / settings rail</td><td><code>--ds-bg-sidebar</code> (opaque fallback: light <code>#f3f3f3</code>, dark <code>#000000</code>), <code>--ds-bg-sidebar-image</code>, shared macOS glass tint/sheen</td><td>One <code>sidebar-surface</code> material for both navigation columns; content stays opaque</td></tr><tr><td>Settings search</td><td><code>--ds-settings-field-bg</code> (light <code>#ffffff</code>, dark <code>#212121</code>)</td><td>Search pill on the settings rail</td></tr><tr><td>Active settings item</td><td><code>--ds-settings-nav-active</code> (light 12% <code>#1a1c1f</code> mixed over white; dark 10% <code>--gray-0</code> over transparent)</td><td>Selected navigation pill</td></tr><tr><td>Inset search</td><td><code>--ds-field-inset-bg</code>, <code>--ds-field-inset-focus-bg</code> (light <code>#f3f3f3</code> / white; dark 5% / 7% primary-text mix over transparent)</td><td>Plugin search and Agent capability search, including focus</td></tr><tr><td>Prose keycap / thinking code</td><td><code>--ds-prose-kbd-fg</code> (light <code>#303030</code>, dark <code>--ds-text-secondary</code>), <code>--ds-thinking-code-bg</code> (light <code>#f0f0f0</code>, dark 4.5% text mix)</td><td>Keycap ink and the thinking-prose code chip</td></tr><tr><td>Code-card chrome</td><td><code>--ds-code-head-bg</code>, <code>--ds-code-hover-bg</code> (light 3.5% / 6% <code>#1a1c1f</code> over the plate; dark 4% / 8% white)</td><td>The <code>.code-block</code> head band and its hover fills. The Shiki syntax plate and its ink are deliberately not tokens — see below</td></tr><tr><td>Mermaid canvas</td><td><code>--ds-mermaid-canvas</code> (light <code>#ffffff</code>, dark 94% <code>--ds-bg-primary</code> + 6% text mix)</td><td>The diagram body inside <code>.mermaid-block</code></td></tr><tr><td>Scrim / veil</td><td><code>--ds-scrim</code> (dark ~45% black, light ~28% <code>#1a1c1f</code>, D148), <code>--ds-modal-veil</code> (dark 78% <code>--ds-bg-primary</code>, light ~32% <code>#1a1c1f</code>)</td><td>The dialog scrim and the plugin permission backdrop</td></tr><tr><td>Tool output</td><td><code>--ds-tool-row-bg</code> (light 2% <code>#1a1c1f</code>, dark <code>--ds-tile</code>)</td><td>Tool result and error output blocks in the transcript</td></tr><tr><td>Disabled send chip</td><td><code>--ds-send-disabled-bg</code>, <code>--ds-send-disabled-fg</code> (light <code>#8e8e90</code> / <code>#ffffff</code>; dark 18% text mix / 70% <code>--gray-900</code>)</td><td>The composer&#39;s disabled send button</td></tr><tr><td>Composer placeholder</td><td><code>--ds-placeholder-ink</code> (light <code>#4a4c4f</code>, dark 42% white)</td><td>Input and placeholder ink in both composer states</td></tr></tbody></table><p>Main and settings navigation share the same material, not just matching colors. The legacy <code>--ds-settings-rail-bg</code> remains readable with the shared built-in palette and supplies the sidebar color fallback, including theme overrides. An explicit <code>--ds-bg-sidebar</code> override takes precedence. This preserves legacy color reads and inputs without retaining an independent settings-only plate or creating a circular alias. On macOS the shared tint derives from that color; on other platforms the shared plate is opaque. Background imagery uses <code>--ds-bg-sidebar-image</code> for both rails.</p><p>The dark composer shell consumes <code>--ds-bg-elevated-primary</code> directly; light continues to use <code>--ds-bg-composer</code>. Switch on-state knobs consume <code>--ds-switch-knob-on</code> in both palettes. These fills keep their built-in paint, focus rings, and shadows while allowing a contributed stylesheet to override the variables. Theme-specific component rules may retain their existing shadow or layout differences, but must not replace a token-driven fill with a literal.</p><p>Prose and transcript inks ride <code>--ds-text-primary</code>: the light overrides that used to hardcode <code>#1a1c1f</code> now mix the token, so the whole light ink tier moves with the theme (headings 5/6 at 62%, list markers 40%, quote ink 72%, link underlines 30% and 80% on hover, the inline-code chip at 6% with full-strength ink, thinking prose 58% and its code 68%).</p><p><strong>Convention:</strong> a <code>:root[data-theme=&quot;…&quot;]</code> rule must not write a literal colour. Such a rule out-specifies the base token rule <em>and</em> never reads a variable, so it silently pins that surface out of every theme&#39;s reach — see D419. A theme-specific value belongs in the token blocks of <code>styles/tokens.css</code>, where both palettes define the same <code>--ds-*</code> name and the base rule reads it once.</p><p><code>pnpm lint</code> runs <code>scripts/style-surface-tokens.mjs</code>, which enforces that convention mechanically. Rule 1: inside a <code>:root[data-theme]</code> rule every colour declaration — <code>color</code>, <code>background</code>/<code>-color</code>/<code>-image</code>, <code>text-decoration-color</code>, <code>-webkit-text-fill-color</code>, <code>border</code> and its colour longhands, <code>outline</code>, <code>fill</code>, <code>stroke</code>, <code>accent-color</code>, <code>caret-color</code>, and <code>box-shadow</code> — must resolve through a custom property; the token blocks themselves are the place for literals, and a component-local custom property holding a literal colour is a violation too, because it shadows the root token. Rule 2: the base rules of the migrated chrome families — settings rail, search, navigation item, switch thumb, capability search, plugin search, composer shell, composer toolbar/chip/ placeholder/input, plus prose, code-card (<code>code-block</code>, head band, language rail), Mermaid (block, body, head, title, error, source), scrims, tool rows, send button and empty hero — must not paint a literal either.</p><p>Its exemptions are enumerated with reasons rather than left silent:</p><ul><li>the <code>one-dark-pro</code> / <code>one-light</code> Shiki palette (the plate and its ink are authored by the Shiki theme and emitted as inline colours on the highlighted markup, so the pair has to move together — through the Shiki theme, not a CSS token);</li><li>translucent black- or white-alpha shadow values (<code>box-shadow</code>, <code>text-shadow</code>, <code>filter</code>), which only offset darkness; an opaque shadow colour is still checked;</li><li>the ⌘K <code>.search-overlay</code> veil, which keeps the dark 45% black mix in both palettes and so still departs from D148&#39;s lighter light veil. That one is a maintainer decision recorded as a known gap: it is checked by the guard <em>and</em> allowed there, so removing the allowance fails <code>pnpm lint</code>.</li></ul><p>Two boundaries stay open by design. The family rule is a fixed list, not every selector in the renderer, so it catches a regression in a migrated surface but not a brand-new literal on a selector nobody has reviewed. And the guard is static text: it cannot see a cascade conflict between two token-reading rules (#339&#39;s root cause). Both are why the rendered checks in <code>pnpm test:e2e:theme-surfaces</code> remain necessary — that probe compares the built paint of every migrated surface against values sampled from the pre-change app, so a token whose default is not exactly the old literal fails there. Keep the light-qualified <code>.tool-row-content</code> rule that probe pins: at (0,3,0) it out-specifies <code>.tool-row-content.is-error</code> and ties with <code>.tool-block.is-plain .tool-row-content</code>, so dropping it would tint error output and give plain tool blocks a fill in light only.</p><table tabindex="0"><thead><tr><th>Context</th><th>Treatment</th></tr></thead><tbody><tr><td>Row / section separators</td><td>Spacing (4–6px gap between tile rows, 12–24px between sections); never a rule</td></tr><tr><td>Card / panel outlines</td><td><code>--ds-tile</code> fill, no ring, in both themes</td></tr><tr><td>Selection (theme, language, level)</td><td>Deeper tint or raised pill plus the existing check mark; no selected border</td></tr><tr><td>Floating layers (menus, popovers, dialogs, tooltips, toasts, hover cards)</td><td><code>0 0 0 0.5px border-default</code> + shadow on the container; no rules inside</td></tr><tr><td>Focus rings</td><td>accent tint, 2px box-shadow</td></tr><tr><td>Control affordances (switch off-ring, resize handles)</td><td>Allowed; they are the control, not a partition. The sidebar and work-panel dividers paint a 32px centered grip on direct hover/focus, not a full-height rail; keyboard focus and an in-progress drag use the solid accent</td></tr></tbody></table><h2 id="_7-iconography" tabindex="-1">7. Iconography <a class="header-anchor" href="#_7-iconography" aria-label="Permalink to &quot;7. Iconography&quot;">​</a></h2><h3 id="_7-1-icon-set" tabindex="-1">7.1 Icon set <a class="header-anchor" href="#_7-1-icon-set" aria-label="Permalink to &quot;7.1 Icon set&quot;">​</a></h3><ul><li><strong>Primary:</strong> Lucide (SVG, MIT license, 24×24 default grid)</li><li><strong>Alternative:</strong> Heroicons v2 (outline variant)</li><li>Never mix both in the same surface — pick one per implementation file</li><li><strong>Never use emoji as icons</strong> — emoji are text content, not UI affordances</li></ul><h3 id="_7-2-sizing" tabindex="-1">7.2 Sizing <a class="header-anchor" href="#_7-2-sizing" aria-label="Permalink to &quot;7.2 Sizing&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Context</th><th>Size</th><th>Stroke width</th></tr></thead><tbody><tr><td>Inline with text</td><td>16px (1rem)</td><td>1.5px</td></tr><tr><td>Buttons, toolbar</td><td>20px (1.25rem)</td><td>2px</td></tr><tr><td>Empty states</td><td>48px (3rem)</td><td>1.5px</td></tr></tbody></table><h3 id="_7-3-color-rules" tabindex="-1">7.3 Color rules <a class="header-anchor" href="#_7-3-color-rules" aria-label="Permalink to &quot;7.3 Color rules&quot;">​</a></h3><ul><li>Default: <code>text-secondary</code></li><li>Hover/active: <code>text-primary</code></li><li>Accent actions: <code>accent</code> color</li><li>Disabled: <code>text-muted</code></li><li>Never use colored icon backgrounds in buttons (no icon circles/squares)</li></ul><h2 id="_8-motion" tabindex="-1">8. Motion <a class="header-anchor" href="#_8-motion" aria-label="Permalink to &quot;8. Motion&quot;">​</a></h2><h3 id="_8-1-duration-scale" tabindex="-1">8.1 Duration scale <a class="header-anchor" href="#_8-1-duration-scale" aria-label="Permalink to &quot;8.1 Duration scale&quot;">​</a></h3><p>CSS custom properties on <code>:root</code> (D146):</p><table tabindex="0"><thead><tr><th>Token</th><th>CSS variable</th><th>Duration</th><th>Usage</th></tr></thead><tbody><tr><td><code>duration-fast</code></td><td><code>--motion-duration-fast</code></td><td>150ms</td><td>Hover transitions, color changes</td></tr><tr><td><code>duration-normal</code></td><td><code>--motion-duration-normal</code></td><td>200ms</td><td>Expand/collapse, slide-in, toast/dialog enter</td></tr><tr><td><code>duration-slow</code></td><td><code>--motion-duration-slow</code></td><td>300ms</td><td>Panel transitions, boot splash enter</td></tr></tbody></table><p>Interactive surfaces should reference these variables instead of hard-coded millisecond literals when practical.</p><h3 id="_8-2-easing" tabindex="-1">8.2 Easing <a class="header-anchor" href="#_8-2-easing" aria-label="Permalink to &quot;8.2 Easing&quot;">​</a></h3><p>CSS custom properties on <code>:root</code> (D146):</p><table tabindex="0"><thead><tr><th>Token</th><th>CSS variable</th><th>Curve</th><th>Usage</th></tr></thead><tbody><tr><td><code>ease-out</code></td><td><code>--motion-ease-out</code></td><td><code>cubic-bezier(0.22, 1, 0.36, 1)</code></td><td>Default enter / hover / fill transitions</td></tr><tr><td><code>ease-in</code></td><td><code>--motion-ease-in</code></td><td><code>cubic-bezier(0.4, 0, 1, 1)</code></td><td>Exit animations (toast out, splash out)</td></tr><tr><td><code>ease-standard</code></td><td><code>--motion-ease-standard</code></td><td><code>cubic-bezier(0.2, 0, 0, 1)</code></td><td>Continuous progress indicators (boot bar)</td></tr></tbody></table><ul><li>Enter animations: <code>ease-out</code></li><li>Exit animations: <code>ease-in</code></li><li>Spring-like for drag/releases: <strong>not in MVP</strong> (use <code>ease-out</code>)</li></ul><h3 id="_8-3-startup-splash-boot-feedback" tabindex="-1">8.3 Startup splash (boot feedback) <a class="header-anchor" href="#_8-3-startup-splash-boot-feedback" aria-label="Permalink to &quot;8.3 Startup splash (boot feedback)&quot;">​</a></h3><p>While Electron bootstrap is not yet ready, the renderer paints a full-window <strong>startup splash</strong> (<code>StartupSplash</code>, <code>data-testid=&quot;startup-splash&quot;</code>) instead of plain status text:</p><ul><li>Brand mark (<code>BrandLogo</code> 64px), shell name, and tagline from the active catalog</li><li>Accessible status copy via <code>app.starting</code> (screen-reader only) with <code>role=&quot;status&quot;</code> / <code>aria-live=&quot;polite&quot;</code></li><li>Soft indeterminate progress bar as loading feedback (≤1.1s loop)</li><li>Minimum visible time ~420ms on normal motion to avoid a flash on fast boots</li><li>Exit: 280ms opacity fade (<code>startup-splash-out</code>) once <code>ready</code> is true, revealing the already-mounted shell underneath</li><li>Reduced motion: near-zero enter/exit and a static full-width bar</li><li>macOS: the splash is the same glass as the sidebar (D304 / D348) — the <code>--ds-sidebar-glass-tint</code> fill plus top/bottom sheen over the native <code>sidebar</code> vibrancy, so the boot surface never flashes an opaque panel ahead of the translucent sidebar. The mounted shell stays hidden under the glass until the exit fade, then cross-fades in. Other platforms keep the opaque <code>--ds-bg-primary</code> fill</li></ul><p>This is boot-state feedback, not decorative chrome.</p><h3 id="_8-4-overlay-floating-surface-enter" tabindex="-1">8.4 Overlay / floating surface enter <a class="header-anchor" href="#_8-4-overlay-floating-surface-enter" aria-label="Permalink to &quot;8.4 Overlay / floating surface enter&quot;">​</a></h3><p>Dialogs, search spotlight, and modal backdrops use shared enter keyframes:</p><ul><li>Scrim: <code>overlay-in</code> (opacity, <code>--motion-duration-normal</code> / <code>--motion-ease-out</code>)</li><li>Dark theme scrim stays ~45% black; light theme uses ~28% <code>#1a1c1f</code> so white dialogs do not sit under a heavy veil (D148)</li><li>Centered surface: <code>surface-in</code> (fade + 8px rise + slight scale)</li><li>Top-anchored surface (search): <code>surface-in-top</code></li></ul><p>Toast enter/exit keep the existing removal contract (<code>animationend</code> on <code>toast-out</code>) while using the motion tokens and a slightly softer scale.</p><h3 id="_8-5-reduced-motion" tabindex="-1">8.5 Reduced motion <a class="header-anchor" href="#_8-5-reduced-motion" aria-label="Permalink to &quot;8.5 Reduced motion&quot;">​</a></h3><p>All motion tokens must respect <code>prefers-reduced-motion: reduce</code>:</p><div class="language-css vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">css</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">@media</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> (prefers-reduced-motion: reduce) {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  *</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">*</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">::before</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">*</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">::after</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">    animation-duration</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">0.01</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">ms</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> !important</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">    transition-duration</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">0.01</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">ms</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> !important</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><p>Boot splash, overlay/dialog enters, running-status pulses, and continuous bars are also explicitly suppressed or collapsed to a static state.</p><blockquote><p>See also <a href="./09-interaction-patterns">09-interaction-patterns.md</a> §10.</p></blockquote><h3 id="_8-6-prohibited-motion" tabindex="-1">8.6 Prohibited motion <a class="header-anchor" href="#_8-6-prohibited-motion" aria-label="Permalink to &quot;8.6 Prohibited motion&quot;">​</a></h3><ul><li>No parallax</li><li>No continuous background animations (particles, waves); the bounded foreground mascot loop is the explicit empty-home exception</li><li>No shimmer/skeleton animations longer than 1s loop — use simple fade-in for loading states</li><li>No bounce effects</li><li>No multi-second boot theatrical sequences; splash exits as soon as ready (+ min dwell)</li></ul><h3 id="_8-7-responsive-interaction-feedback" tabindex="-1">8.7 Responsive interaction feedback <a class="header-anchor" href="#_8-7-responsive-interaction-feedback" aria-label="Permalink to &quot;8.7 Responsive interaction feedback&quot;">​</a></h3><p>High-frequency workstation feedback must remain compositor-friendly and bounded:</p><ul><li>Destination surfaces, the work panel, jump-to-latest control, and inline error notices enter once with <code>opacity</code> plus a maximum 8px <code>transform</code>/<code>scale</code> offset using <code>--motion-duration-normal</code> and <code>--motion-ease-out</code>.</li><li>Icon, navigation, message-action, tab, sidebar tool, and standard button controls provide a subtle pressed scale while active. The base transition includes <code>transform</code>, so press and release never snap; hover styling never changes element dimensions or surrounding layout.</li><li>Composer focus lifts by 1px with a restrained token-based shadow. Its near-opaque surface must not use backdrop blur: transcript updates beneath a blur layer would force avoidable repaint/compositing work while streaming.</li><li>Inline chat error notices wrap long provider detail and keep their actions reachable without introducing horizontal page overflow.</li><li>Stream-driven updates never restart route or shell animations. Every enter effect is mount/state-transition feedback, not a response to token arrival.</li><li>Sidebar and work-panel dock transitions animate their allocated <code>width</code> and <code>flex-basis</code> together with the bounded opacity/transform offset. MainChat must reflow continuously during the 150–200ms transition, never jump to the final dock width before the first painted frame.</li><li>Replaceable streamed message/tool partials may be coalesced until the next animation frame; terminal, permission, planning, and error states flush first and remain synchronous.</li><li>Reduced-motion mode keeps every state change and scroll destination but uses near-zero animation durations and instant rather than smooth programmatic scrolling.</li></ul><h2 id="_8-0-home-empty-stack-and-bottom-composer-d111-d204-d206" tabindex="-1">8.0 Home empty stack and bottom composer (D111/D204/D206) <a class="header-anchor" href="#_8-0-home-empty-stack-and-bottom-composer-d111-d204-d206" aria-label="Permalink to &quot;8.0 Home empty stack and bottom composer (D111/D204/D206)&quot;">​</a></h2><p>Empty composer placeholder guidance is scoped to the current page and session: home starts with <code>chat.placeholderHome</code> and thread-docked starts with <code>chat.placeholder</code>. Within that context the copy remains stable. Switching between home/session views or active conversations advances through the localized command/file hint (<code>chat.placeholderHomeHint</code> / <code>chat.placeholderHint</code>) and the keyboard hint (<code>chat.placeholderShortcut</code>). It does not rotate on a timer or because focus, draft, or IME state changes. The visible copy uses an opacity fade and remains legible on the light and dark composer plates.</p><p>Empty chat home keeps the content and composer in separate vertical regions inside <code>home-main-content</code> (D111/D204/D206; supersedes the D047 dual-grow portal model):</p><ul><li>Column <code>flex: 1; min-height: 0; overflow: hidden</code></li><li>Inner scroller (<code>.home-scroll</code>) is the only vertical overflow surface for the hero and optional checklist</li><li>Stack (<code>.home-stack-inner</code>) uses content width <strong><code>min(100%, var(--chat-content-max-width))</code></strong> (default 760px, D439), with <strong><code>gap: 16px</code></strong> (workstation ceiling), and auto margins to center the column when the viewport is tall</li><li>The content order is <strong>hero → optional onboarding checklist</strong>. Task entry starts directly in the bottom composer; no starter prompt grid or contextual quick-action row is rendered (D204/D206).</li><li>Short windows (<code>max-height ≤ 760px</code>) top-align the stack and keep every content block reachable by scrolling; the bottom composer remains visible and never covers the checklist</li><li>The home composer is a bottom-reserved sibling of the scroller. Thread mode keeps its absolute bottom dock and reserves its measured height without a full-width fade veil</li><li>Composer radius uses Codex <code>radius-3xl-base</code> (<strong>20px</strong> / <code>1.25rem</code>)</li><li>Empty-home composer height is content-driven: a one-line draft renders the compact shell, grows with the draft through seven visible rows, and then keeps the shell stable while the textarea scrolls internally</li><li>The light sidebar has no standalone New task row; scoped session-creation controls remain icon-only and use the semantic hover wash</li><li>Empty hero title uses <code>var(--ds-text-primary)</code> (light override <code>#1a1c1f</code>); never hardcode light ink for shared hero styles</li><li>Empty-home branding stays quiet: the 100px eight-frame mascot GIF is the sole animated hero mark. Light and dark themes each use a dedicated asset pair. It loops a short wave with an idle hold so the composer remains the primary task surface. Pointer hover does not change the cadence; reduced motion shows the matching still first frame.</li><li>Night home composer plate styles are <strong>dark-scoped only</strong> (elevated-primary <code>#212121f5</code> + standard elevation-prominent)</li><li>Empty draft row keeps <strong>one visible line / 28px optical minimum</strong> so the placeholder remains visible; it auto-grows to seven visual lines and scrolls internally from line eight onward</li><li>Home and thread-docked prompt rows stay free of leading brand icons so the draft aligns directly with the input gutter. Light placeholder ~<code>#525355</code></li><li>Disabled send is a <strong>solid gray chip</strong> (<code>#8e8e90</code> light, white arrow), not opacity-only fade</li><li>Floating composer plates use one solid semantic surface with no internal gradient: <code>--ds-bg-composer</code> in light and elevated-primary in dark. A hairline stroke plus the restrained <code>--elevation-prominent</code> shadow provides separation; the transcript reserves the measured dock height instead of painting a full-width gradient veil.</li><li>In-transcript message-edit uses <code>--ds-tile-deep</code> and <code>--ds-composer-radius</code> with no outer shadow. The row&#39;s paint containment and the transcript scroller would clip a composer lift. Focus uses an inset 2px accent ring so the cue stays inside the plate.</li><li>Dark elevated shell reads as elevated-primary (<code>#212121f5</code> / gray-800 96%) on <code>#181818</code> with standard elevation-prominent</li><li>Starter cards use a two-column grid at workstation widths and collapse to one column below 620px. Each card uses a subtle border, a small icon plate, a localized title/description, and a quiet arrow affordance; hover/focus uses the shared hover fill, border, shadow, and motion tokens.</li></ul><h2 id="_8-1-composer-transient-file-references" tabindex="-1">8.1 Composer transient file references <a class="header-anchor" href="#_8-1-composer-transient-file-references" aria-label="Permalink to &quot;8.1 Composer transient file references&quot;">​</a></h2><p>The passive project / Local / branch rail remains removed under D095. When the draft references files, a transient wrapping row appears inside the composer above the textarea. Each quiet text chip uses the inset surface, a subtle border, a file glyph, an ellipsized leaf name, and a focus-visible remove action. No references means no reserved row or extra composer height. The canonical path is tooltip/accessibility metadata and never textarea body copy, including after unanswered smart-stop restoration. Dispatch and persisted user messages still carry the canonical path required by D124. After send, the transcript paints those same references as composer-matching leaf-name chips rather than full-path text (D320).</p><p>Large text pastes use a second presentation: text-only input at or below the configured <code>largePasteThreshold</code> remains native editor content, while input above it is written as UTF-8 under the active session&#39;s scratch <code>pasted/</code> directory and rendered as one sentinel-backed <code>pasted-text-*.txt</code> chip at the paste caret. The chip remains atomic until the user clicks it or presses Enter/Space; the composer then reads the bounded text file, replaces the sentinel at its current position with editable text, removes the reference, and places the caret after the inserted content. A failed or unsupported read keeps the chip in place. The renderer resolves any remaining sentinel references exactly once immediately before dispatch. The threshold is an AI → Defaults setting, defaults to 600 characters, and applies only to text-only pastes; clipboard files and images retain their chip presentation. Word&#39;s mixed <code>text/plain</code> plus generated <code>image/*</code> copies selects the text representation when the text is not whitespace-only and no file has a native path. That text uses the same threshold. Native files, non-image files, and image-only or whitespace-plus-image pastes retain their chips.</p><h2 id="_8-2-composer-runtime-controls" tabindex="-1">8.2 Composer runtime controls <a class="header-anchor" href="#_8-2-composer-runtime-controls" aria-label="Permalink to &quot;8.2 Composer runtime controls&quot;">​</a></h2><p>The composer renders only controls connected to the active pi session:</p><ul><li>Agent / Plan / Goal updates the durable session mode and changes the next pi toolset. Plan and Goal are contract states of the same Agent.</li><li>The model trigger shows only the active model ID. Its menu selects a configured provider/default-model pair for the active session and links to Agent.</li><li>The right toolbar exposes one combined model × reasoning trigger immediately before the standalone prompt-enhancement Sparkles action and Send/Abort. The trigger shows a Bot icon, the current model, and reasoning level; <code>off</code> omits the level text. Its single <code>role=&quot;menu&quot;</code> popover opens above the trigger at <code>bottom: calc(100% + 8px)</code> and starts with exactly two current-value entries. When the menu lists more than one level (<code>omit</code> plus the binding&#39;s enabled canonical levels), a drag slider sits directly beneath the Reasoning level entry; slider and tick commits apply without leaving the root. Tick labels are not tab stops — the range input is the accessible control. Each entry replaces the menu contents in-place with a back row and its submenu. The Model submenu contains search plus sticky provider groups. Each model row begins at one tab stop beneath its provider heading, making the provider → model hierarchy legible without altering the model label. The Reasoning submenu lists <code>omit</code> then the enabled levels as radio rows with a selected-row check; selecting from the list returns to the root without dismissing the popover.</li><li>While the active session is running, the draft and runtime controls stay editable as next-turn choices; only Send is disabled. Host configuration remains pinned for the in-flight turn and the latest queued choice is persisted after its terminal event. Pending approval still blocks editing.</li><li>File, photo, and appshot controls remain hidden until their payload contracts are implemented end to end.</li><li>The combined model × reasoning menu persists changes to the active session and returns to its root after a selection. It renders the levels enabled by the selected model binding; published catalog levels seed that binding, while Settings can explicitly configure a level for an unknown or proxied model. Unknown models do not gain reasoning automatically. Changing provider clamps or resets the durable session value before the next turn.</li><li>The left-of-input Composer Agent/Plan/Goal chip is the sole active-session mode control and cycles Agent → Plan → Goal → Agent. The Composer-right combined chip owns model and reasoning changes; the top bar has no duplicate mode or model control. The combined Composer picker closes and is disabled while an active <code>pending</code> Plan or Goal approval exists; terminal proposal snapshots do not disable it. The approval selector remembers the last selected mode on this device and uses it for the next pending proposal. Live Host events update the latest checkpoint or execution status retained for the current renderer lifetime. A renderer reload rehydrates only a pending row through <code>plans.pending</code>; terminal cards are not restored.</li></ul><h2 id="_8-3-global-plugin-launcher" tabindex="-1">8.3 Global plugin launcher <a class="header-anchor" href="#_8-3-global-plugin-launcher" aria-label="Permalink to &quot;8.3 Global plugin launcher&quot;">​</a></h2><p><code>Option + Space</code> on macOS and <code>Alt + Space</code> on Windows/Linux opens a centered, frameless 620×440 utility window on the display nearest the pointer. The surface has no close, minimize, maximize, resize, or taskbar controls and dismisses on blur or Escape. Its solid elevated token surface works in light and dark themes without a backdrop blur. Electron starts loading the hidden launcher as soon as the runtime is ready, in parallel with application boot, so the first shortcut invocation reveals an already-rendered surface rather than waiting for a new renderer process and document load.</p><p>The focused search field filters enabled, ready plugins that contribute a panel. Chinese display names match their original characters, tone-free full pinyin, and pinyin initials; plugin IDs, names, and descriptions remain searchable. Arrow keys move the active option, Enter or click opens the existing sandboxed plugin panel, and IME composition keystrokes never navigate or dispatch. The launcher remembers which plugins were opened most recently in renderer-local device storage and shows an empty query in most-recently-used order; a typed query still ranks relevance first and uses recency only as a tiebreaker.</p><ul><li>Local and branch context are non-interactive status labels; the project name remains an action because it opens the project picker.</li><li>Runtime chip labels (Agent/Plan/Goal, Thinking, permission mode, model ID) use <code>--text-sm</code> with <code>--leading-compact</code> inside the 28px hit target. They must not use <code>leading-none</code> with overflow clipping: descenders on glyphs such as <code>g</code>/<code>y</code>/<code>p</code> stay fully visible. Long model IDs still truncate horizontally via ellipsis without crushing the line box (D150).</li></ul><h2 id="_8-3-thinking-disclosure" tabindex="-1">8.3 Thinking disclosure <a class="header-anchor" href="#_8-3-thinking-disclosure" aria-label="Permalink to &quot;8.3 Thinking disclosure&quot;">​</a></h2><ul><li>Assistant thinking renders before the final answer as a lightweight inline disclosure aligned with tool activity rows: transparent transcript surface, Sparkles cue, rotating chevron, secondary text, and a subtle left rule only around expanded reasoning. It uses semantic theme and focus-ring tokens in light and dark modes; it must not introduce a separate inset card.</li><li>The latest thinking disclosure opens while a thinking-only response is streaming and closes when the turn settles if it was not touched. A manual toggle owns the disclosure and remains effective through later deltas and completion.</li><li>The trigger is a button with <code>aria-expanded</code>, <code>aria-controls</code>, and localized Show/Hide labels. Collapsed reasoning is hidden from focus and accessibility traversal; reduced-motion mode disables the running marker pulse and disclosure transitions.</li><li>Thinking never enters the answer bubble, answer copy action, transcript minimap excerpt, or searchable answer text.</li><li>A thinking-only stream opens the transcript surface without an empty answer bubble or a duplicate Working indicator.</li></ul><h2 id="_9-z-index-layers" tabindex="-1">9. Z-index layers <a class="header-anchor" href="#_9-z-index-layers" aria-label="Permalink to &quot;9. Z-index layers&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Layer</th><th>Z-index</th><th>Usage</th></tr></thead><tbody><tr><td><code>z-base</code></td><td>0</td><td>Default content</td></tr><tr><td><code>z-sticky</code></td><td>10</td><td>Sticky headers, topbar</td></tr><tr><td><code>z-dropdown</code></td><td>20</td><td>Dropdown menus, select popovers</td></tr><tr><td><code>z-overlay</code></td><td>30</td><td>Tooltips</td></tr><tr><td><code>z-dialog</code></td><td>40</td><td>Settings and confirmation dialogs</td></tr><tr><td><code>z-toast</code></td><td>50</td><td>Toast notifications</td></tr><tr><td><code>z-command-palette</code></td><td>60</td><td>Command palette overlay, body-portaled menus/popovers</td></tr><tr><td><code>z-devtools</code></td><td>100</td><td>DevTools overlay (non-production)</td></tr></tbody></table><p>Rules:</p><ul><li>Never use <code>z-index: 9999</code> or similar arbitrary high values</li><li>Each layer is a fixed offset; no custom z-index outside these layers</li><li>Stacking within a layer uses DOM order, not higher z-values</li><li>Browser-preview and plugin views are native surfaces composited above every renderer layer, so no <code>z-index</code> in the table above can raise a popover over them. A body-portaled popover clamps to the conversation pane, which ends where the work panel begins, instead of to the viewport.</li><li>A route surface holds no stacking context once its entrance animation finishes, so an overlay authored inside a route page — a modal, a sheet, or their scrims — covers the titlebar band without any <code>z-index</code> juggling. On Windows/Linux the renderer-drawn window controls stay above renderer overlays. Route overlays therefore sit on <code>z-dialog</code> (40): a leaf popup (60) or a toast (50) a dialog raises — portaled to <code>document.body</code>, so in that same stacking context — keeps painting above the dialog&#39;s scrim and keeps taking clicks.</li></ul><h2 id="_10-layout-shell-metrics" tabindex="-1">10. Layout shell metrics <a class="header-anchor" href="#_10-layout-shell-metrics" aria-label="Permalink to &quot;10. Layout shell metrics&quot;">​</a></h2><p>These metrics define the AppShell frame. See <a href="./08-component-spec">08-component-spec.md</a> for component detail. Codex parity decisions (D034/D070) supersede any older value here.</p><table tabindex="0"><thead><tr><th>Metric</th><th>Value</th><th>Notes</th></tr></thead><tbody><tr><td>Titlebar row height</td><td>46px</td><td x:16,y:16="">Codex toolbar rhythm (D034); traffic lights</td></tr><tr><td>Sidebar width (collapsed)</td><td>48px</td><td>Icon-only rail</td></tr><tr><td>Sidebar width (expanded)</td><td>240–520px (default 275px)</td><td>Right-edge handle; drag below 160px collapses (ADR 0141 / ADR 0290)</td></tr><tr><td>Main pane minimum readable width</td><td>450px</td><td>The MainChat hard floor; the sidebar yields before it is breached (ADR 0238)</td></tr><tr><td>Work panel width (closed)</td><td>0px</td><td>Hidden by default</td></tr><tr><td>Work panel width (open)</td><td><code>≥244px</code> (new-profile default 360px), capped by <code>client width - 450px - expanded sidebar</code> with no fixed pixel cap</td><td>the panel is an in-flow column whose width is taken from the existing client area; the renderer owns its divider (ADR 0033 / ADR 0151 / ADR 0238); saved widths remain unchanged</td></tr><tr><td>Composer shell minimum</td><td>~80px</td><td>One-line draft + toolbar padding</td></tr><tr><td>Composer toolbar</td><td>MainChat <code>≥450px</code></td><td>Left/right control groups stay on one row and do not shrink; mode/permission labels stay single-line and ellipsize</td></tr><tr><td>Composer draft height</td><td>1–7 text lines</td><td>Auto-grow; internal scroll beyond line 7</td></tr><tr><td>Chat message max width</td><td>760px default band (user-resizable, min 560px) / 600px user plate</td><td>Band follows <code>min(pane, preferred)</code>; user turns stay compact</td></tr><tr><td>Window min width</td><td>1040px</td><td>Enforced by Electron for the whole app; opening the panel never changes native bounds</td></tr><tr><td>Window min height</td><td>700px</td><td>Enforced by Electron</td></tr></tbody></table><p>An open work panel is a fixed-width in-flow column inside the existing client area (ADR 0033 / ADR 0151). Its flex allocation comes from MainChat, but MainPane retains a 450px hard minimum and the panel&#39;s effective maximum is the remaining client width after the expanded sidebar and that floor (ADR 0238). When the budget is exhausted the expanded sidebar collapses immediately, and the shared budget keeps counting it while <code>sidebar-out</code> occupies flex space. Side-dock allocation therefore cannot paint over or claim MainChat&#39;s floor. The renderer&#39;s measured panel rect continues to position the native Browser view. Opening and collapsing do not request a positive native reservation or change persisted window bounds. Before collapse motion starts, any native Browser preview surface is detached because it cannot participate in renderer CSS animation; macOS, Windows, and Linux retain the fade-and-slide exit.</p><p>Preview mode is a transient shell state: MainChat is unmounted and the work panel occupies the client width beside the sidebar. A window-level 46px chrome row is pointer-transparent outside New Task/sidebar and native window controls; it declares neither drag nor no-drag across the panel. The panel header alone owns the preview pane&#39;s drag area. Its border box, not just its padding, excludes the left action lane plus an 8px gap in both sidebar states on all platforms. The left inset is 8px except for collapsed-sidebar windowed macOS (88px). That macOS reserve is the shared <code>--ds-window-lead-inset</code> token — the traffic-light cluster&#39;s 76px right edge (native geometry from <code>@pi-desktop/shared</code>, the same constants the main process positions the buttons with) plus a 12px gap. The action lane uses the shared 28px control size plus an 8px gap when expanded, and the shared preview action lane (two controls, 4px spacing, 8px gap) when collapsed. The right native-control exclusion remains unchanged. The panel paints the header-height background behind the excluded lane without covering its controls.</p><h3 id="_10-1-responsive-collapse" tabindex="-1">10.1 Responsive collapse <a class="header-anchor" href="#_10-1-responsive-collapse" aria-label="Permalink to &quot;10.1 Responsive collapse&quot;">​</a></h3><ul><li>The work panel never participates in responsive collapse. It keeps its committed preferred width of at least <code>244px</code> (new-profile default 360px) while visible, capped by the shared budget; saved widths remain unchanged.</li><li>The inner panel divider changes the panel width in the renderer. Moving it left takes internal space from MainChat until the 450px floor is reached, at which point the expanded sidebar yields; moving it right returns that space. Native window edges resize only the fixed app window.</li><li>Panel open and collapse change only the in-flow flex allocation. No positive native reservation is requested, and the panel&#39;s preferred width remains a renderer-local setting.</li><li>The outer shell keeps native edge/corner resizing enabled on every platform. Frameless titlebar drag regions never replace the OS resize ownership. A 300ms stable-bounds settle window prevents recovery logic from competing with a slow pointer gesture, and normal base bounds persist 600ms after the last native resize/move event. Width &lt; 1040px or height &lt; 700px is unsupported and prevented by Electron.</li></ul><h2 id="_11-component-foundations" tabindex="-1">11. Component foundations <a class="header-anchor" href="#_11-component-foundations" aria-label="Permalink to &quot;11. Component foundations&quot;">​</a></h2><p>These are <strong>token-level foundations</strong> for common primitives. Detailed component specs are in <a href="./08-component-spec">08-component-spec.md</a>.</p><h3 id="_11-1-button" tabindex="-1">11.1 Button <a class="header-anchor" href="#_11-1-button" aria-label="Permalink to &quot;11.1 Button&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Variant</th><th>Padding</th><th>Height</th><th>Font</th><th>Radius</th><th>Border</th><th>Background</th></tr></thead><tbody><tr><td>Primary</td><td>px-3 py-1.5</td><td>32px</td><td>text-sm 500</td><td>radius-sm</td><td>none</td><td>accent</td></tr><tr><td>Secondary</td><td>px-3 py-1.5</td><td>32px</td><td>text-sm 400</td><td>radius-sm</td><td>none (D297)</td><td><code>--ds-tile</code>, hover <code>--ds-tile-hover</code></td></tr><tr><td>Ghost</td><td>px-2 py-1</td><td>28px</td><td>text-sm 400</td><td>radius-sm</td><td>none</td><td>transparent</td></tr><tr><td>Icon-only</td><td>none</td><td>28px</td><td>—</td><td>radius-full</td><td>none</td><td>transparent; <code>.icon-btn-square</code> pins the width to <code>--ds-control-size</code></td></tr><tr><td>Danger</td><td>px-3 py-1.5</td><td>32px</td><td>text-sm 500</td><td>radius-sm</td><td>none</td><td>error</td></tr></tbody></table><p>An icon-only control states <code>.icon-btn-square</code>. <code>.icon-btn</code> alone takes its width from its content — glyph plus 8px of side padding — which is what a label-driven pill wants and what a control with no label must not inherit. The variant pins both axes to <code>--ds-control-size</code> (28px), keeps <code>flex: 0 0</code> so a crowded toolbar row cannot shrink it back out of square, and drops the side padding that under the global <code>border-box</code> would leave a 12px content box for a 15px glyph.</p><h3 id="_11-2-input-textarea" tabindex="-1">11.2 Input / textarea <a class="header-anchor" href="#_11-2-input-textarea" aria-label="Permalink to &quot;11.2 Input / textarea&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Property</th><th>Value</th></tr></thead><tbody><tr><td>Height (single-line)</td><td>32px</td></tr><tr><td>Padding</td><td>px-3 py-1.5</td></tr><tr><td>Font</td><td>text-sm font-mono (for composer); text-sm font-sans (for settings)</td></tr><tr><td>Border</td><td>none (D297); focus → 2px accent-tinted ring</td></tr><tr><td>Background</td><td><code>--ds-tile</code>; focus lifts to <code>--ds-raised</code></td></tr><tr><td>Radius</td><td>radius-sm</td></tr><tr><td>Text correction (D145)</td><td><code>spellCheck={false}</code>, <code>autoCorrect=&quot;off&quot;</code>, <code>autoCapitalize=&quot;off&quot;</code> on every text input/textarea</td></tr></tbody></table><h3 id="_11-3-card" tabindex="-1">11.3 Card <a class="header-anchor" href="#_11-3-card" aria-label="Permalink to &quot;11.3 Card&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Property</th><th>Value</th></tr></thead><tbody><tr><td>Padding</td><td>p-3</td></tr><tr><td>Border</td><td>none (D297)</td></tr><tr><td>Radius</td><td>radius-lg</td></tr><tr><td>Background</td><td><code>--ds-tile</code></td></tr><tr><td>Hover (interactive)</td><td>bg-tertiary, no shadow change</td></tr></tbody></table><h3 id="_11-4-dialog-modal" tabindex="-1">11.4 Dialog / modal <a class="header-anchor" href="#_11-4-dialog-modal" aria-label="Permalink to &quot;11.4 Dialog / modal&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Property</th><th>Value</th></tr></thead><tbody><tr><td>Max width</td><td>480px</td></tr><tr><td>Padding</td><td>p-6</td></tr><tr><td>Radius</td><td>radius-lg-plus</td></tr><tr><td>Background</td><td>bg-secondary (dark); bg-white (light) + shadow-lg</td></tr><tr><td>Backdrop</td><td>rgba(0,0,0,0.5) with <code>z-dialog</code></td></tr><tr><td>Close</td><td>Escape key + X button top-right</td></tr></tbody></table><h3 id="_11-5-tabs" tabindex="-1">11.5 Tabs <a class="header-anchor" href="#_11-5-tabs" aria-label="Permalink to &quot;11.5 Tabs&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Variant</th><th>Indicator</th></tr></thead><tbody><tr><td>Underline tabs</td><td>2px accent line below active tab</td></tr><tr><td>Padding</td><td>px-3 py-2 text-sm</td></tr><tr><td>Active</td><td>text-primary + accent underline</td></tr><tr><td>Inactive</td><td>text-secondary, hover → text-primary</td></tr></tbody></table><h3 id="_11-6-badge" tabindex="-1">11.6 Badge <a class="header-anchor" href="#_11-6-badge" aria-label="Permalink to &quot;11.6 Badge&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Variant</th><th>Size</th><th>Font</th><th>Radius</th><th>Padding</th></tr></thead><tbody><tr><td>Default</td><td>auto</td><td>text-xs 500</td><td>radius-sm</td><td>px-1.5 py-0.5</td></tr><tr><td>Status dot</td><td>8px circle</td><td>—</td><td>radius-full</td><td>—</td></tr></tbody></table><p>Status badge colors: success (green), warning (amber), error (red), info (indigo), muted (slate).</p><h3 id="_11-7-tooltip" tabindex="-1">11.7 Tooltip <a class="header-anchor" href="#_11-7-tooltip" aria-label="Permalink to &quot;11.7 Tooltip&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Property</th><th>Value</th></tr></thead><tbody><tr><td>Font</td><td>text-xs</td></tr><tr><td>Padding</td><td>px-2 py-1</td></tr><tr><td>Radius</td><td>radius-sm</td></tr><tr><td>Background</td><td>bg-tertiary (dark); bg-slate-800 (light)</td></tr><tr><td>Text</td><td>text-primary</td></tr><tr><td>Delay</td><td>300ms show, 100ms hide</td></tr><tr><td>Max width</td><td>240px</td></tr></tbody></table><h3 id="_11-8-toast" tabindex="-1">11.8 Toast <a class="header-anchor" href="#_11-8-toast" aria-label="Permalink to &quot;11.8 Toast&quot;">​</a></h3><p>Full component contract and usage rules: <a href="./08-component-spec#17-toast">08-component-spec.md §17</a>.</p><table tabindex="0"><thead><tr><th>Property</th><th>Value</th></tr></thead><tbody><tr><td>Position</td><td>top-center viewport, 16px from the top edge, <code>width: min(360px, 100vw − 32px)</code></td></tr><tr><td>Surface</td><td><code>bg-elevated-opaque</code> + 1px <code>border-subtle</code> + <code>shadow-dialog</code> (same family as floating menus)</td></tr><tr><td>Radius</td><td>radius-md-plus</td></tr><tr><td>Font</td><td>text-md, leading-compact-plus</td></tr><tr><td>Variants</td><td><code>info</code> / <code>success</code> / <code>warning</code> / <code>error</code> — 16px Lucide status icon tinted with the semantic token; surface stays neutral (restraint principle)</td></tr><tr><td>Duration</td><td>4s auto-dismiss; error 8s; <code>duration: 0</code> = sticky; hover pauses the timer</td></tr><tr><td>Stack</td><td>vertical, max 4 (oldest dropped), newest nearest the top-center anchor pushing older down; identical message+variant re-raises restart instead of stacking</td></tr><tr><td>Dismiss</td><td>X button on every toast (<code>toast.dismiss</code> i18n label)</td></tr><tr><td>Motion</td><td>enter 200ms ease-out slide-down/fade, exit 150ms ease-in fade; reduced-motion → near-zero duration (not <code>none</code>, removal listens for <code>animationend</code>)</td></tr><tr><td>Z-index</td><td>z-toast (50)</td></tr></tbody></table><h2 id="_12-state-patterns" tabindex="-1">12. State patterns <a class="header-anchor" href="#_12-state-patterns" aria-label="Permalink to &quot;12. State patterns&quot;">​</a></h2><h3 id="_12-1-interactive-states" tabindex="-1">12.1 Interactive states <a class="header-anchor" href="#_12-1-interactive-states" aria-label="Permalink to &quot;12.1 Interactive states&quot;">​</a></h3><table tabindex="0"><thead><tr><th>State</th><th>Background</th><th>Text</th><th>Border</th><th>Cursor</th><th>Motion</th></tr></thead><tbody><tr><td>Default</td><td>per variant</td><td>per variant</td><td>per variant</td><td>default</td><td>—</td></tr><tr><td>Hover</td><td>bg-tertiary or accent-hover</td><td>text-primary</td><td>—</td><td>pointer</td><td>150ms</td></tr><tr><td>Focus</td><td>—</td><td>—</td><td>2px accent ring offset-2</td><td>default</td><td>—</td></tr><tr><td>Focus-visible</td><td>same as focus (only on keyboard focus)</td><td>—</td><td>2px accent ring offset-2</td><td>default</td><td>—</td></tr><tr><td>Active/pressed</td><td>accent bg, text inverted</td><td>text-primary (inverted)</td><td>—</td><td>pointer</td><td>—</td></tr><tr><td>Disabled</td><td>bg-secondary</td><td>text-muted</td><td>border-subtle</td><td>not-allowed</td><td>—</td></tr><tr><td>Loading</td><td>same as default + spinner</td><td>text-secondary</td><td>—</td><td>wait</td><td>spinner 1s rotate</td></tr></tbody></table><h3 id="_12-2-semantic-states" tabindex="-1">12.2 Semantic states <a class="header-anchor" href="#_12-2-semantic-states" aria-label="Permalink to &quot;12.2 Semantic states&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Semantic</th><th>Indicator</th><th>Color</th></tr></thead><tbody><tr><td>Success</td><td>icon ✓ or green dot</td><td>success token</td></tr><tr><td>Error</td><td>icon ✗ or red dot + inline message</td><td>error token</td></tr><tr><td>Warning</td><td>icon ⚠ or amber dot</td><td>warning token</td></tr><tr><td>Running</td><td>spinner (neutral) + pulsing border-left</td><td>accent token</td></tr><tr><td>Pending</td><td>dimmed + clock icon</td><td>muted token</td></tr><tr><td>Denied</td><td>red outline + &quot;Denied&quot; label</td><td>error token</td></tr></tbody></table><h3 id="_12-3-streaming-indicator" tabindex="-1">12.3 Streaming indicator <a class="header-anchor" href="#_12-3-streaming-indicator" aria-label="Permalink to &quot;12.3 Streaming indicator&quot;">​</a></h3><ul><li>Running agent: transcript working feedback + subtle pulse on the left border of the latest assistant message; the topbar remains free of a duplicate status dot</li><li>Completed: spinner replaced by success icon for 2s, then fades</li><li>Error: spinner replaced by error icon, persistent until dismissed</li></ul><h2 id="_13-content-density-rules" tabindex="-1">13. Content density rules <a class="header-anchor" href="#_13-content-density-rules" aria-label="Permalink to &quot;13. Content density rules&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Rule</th><th>Application</th></tr></thead><tbody><tr><td><strong>Base padding 8px (space-2)</strong></td><td>Default inner padding for list items, form groups</td></tr><tr><td><strong>Message gap 10px</strong></td><td>Between chat message rows — denser WorkBuddy-like transcript</td></tr><tr><td><strong>Section gap 16px (space-4)</strong></td><td>Between distinct UI sections (destination pages, settings groups, page-level blocks)</td></tr><tr><td><strong>Panel gap 0px</strong></td><td>Panels touch edge-to-edge with border-subtle separator — no gutters</td></tr><tr><td><strong>Compact list rows 28px height</strong></td><td>Sidebar session items, settings list rows</td></tr><tr><td><strong>Sidebar rhythm 1px / 2px / 8px</strong></td><td>Sidebar lists: 1px between rows, 2px from a group header or section label to its first row, 8px after an expanded project group and between sidebar sections (1px when the preceding group is collapsed)</td></tr><tr><td><strong>Button rows 32px height</strong></td><td>Standard buttons</td></tr><tr><td><strong>Never exceed 24px vertical gap</strong></td><td>Even for &quot;breathing room&quot; — this is a workstation</td></tr><tr><td><strong>Max content width 760px default</strong></td><td>Chat band is user-resizable (min 560px); user plates stay compact</td></tr></tbody></table><h2 id="_14-do-don-t" tabindex="-1">14. Do / Don&#39;t <a class="header-anchor" href="#_14-do-don-t" aria-label="Permalink to &quot;14. Do / Don&#39;t&quot;">​</a></h2><h3 id="do" tabindex="-1">Do <a class="header-anchor" href="#do" aria-label="Permalink to &quot;Do&quot;">​</a></h3><ul><li>Use semantic tokens (<code>text-primary</code>, <code>bg-secondary</code>) — never raw hex in component code</li><li>Use <code>font-mono</code> for all code, file paths, tool arguments, terminal output</li><li>Provide visible focus rings on every interactive element</li><li>Test contrast ratios: <strong>4.5:1 minimum</strong> for normal text, 3:1 for large text</li><li>Keep motion under 300ms and respect <code>prefers-reduced-motion</code></li><li>Collapse long content by default (tool results, long messages) — see <a href="./09-interaction-patterns">09-interaction-patterns.md</a> §4</li><li>Use Lucide/Heroicons SVG icons — never emoji as UI affordances</li><li>Use compact padding and tight spacing — developer density, not consumer spacing</li><li>First launch follows the system theme (see §Theme switching); dark is the primary design target</li></ul><h3 id="don-t" tabindex="-1">Don&#39;t <a class="header-anchor" href="#don-t" aria-label="Permalink to &quot;Don&#39;t&quot;">​</a></h3><ul><li>Don&#39;t hardcode <code>#181818</code> or any hex value in component JSX — use tokens</li><li>Don&#39;t use emoji as icon substitutes (🚀, ✅, ❌ are text, not UI icons)</li><li>Don&#39;t add decorative gradients, glass-morphism, or neon effects</li><li>Don&#39;t use <code>z-index</code> values outside the defined layers</li><li>Don&#39;t animate for decoration — motion is feedback only</li><li>Don&#39;t set <code>font-size: 16px</code> as base — 14px is the workstation default</li><li>Don&#39;t use large hero images or marketing-style empty states</li><li>Don&#39;t apply rounded corners to full-width panels (sidebar, topbar)</li><li>Don&#39;t use <code>border-radius: 0</code> on buttons and inputs (use <code>radius-sm</code> minimum)</li><li>Don&#39;t show raw API keys in any UI surface</li></ul><h2 id="_15-acceptance-criteria" tabindex="-1">15. Acceptance criteria <a class="header-anchor" href="#_15-acceptance-criteria" aria-label="Permalink to &quot;15. Acceptance criteria&quot;">​</a></h2><ol><li>All color tokens defined as CSS custom properties with Tailwind <code>@theme</code> mapping</li><li>Dark and light themes render correctly with ≥4.5:1 contrast on all text/background pairs</li><li><code>system</code> theme follows <code>prefers-color-scheme</code> without white-flash on dark startup</li><li>Typography uses Inter (sans) and JetBrains Mono (mono) with defined fallback stacks</li><li>Base font size is 14px; no component defaults to 16px body text</li><li>All interactive elements have visible <code>focus-visible</code> rings using accent color</li><li>All motion respects <code>prefers-reduced-motion: reduce</code> 7b. Boot shows the branded startup splash until ready, then exits smoothly</li><li>No raw hex color values in React component source (only token references)</li><li>Z-index usage confined to defined layers (no arbitrary values)</li><li>Layout shell metrics (topbar, sidebar, composer) match spec values in CSS</li><li>Icon components use Lucide/Heroicons SVG — no emoji icon affordances</li><li>Spacing values use the defined scale (no arbitrary pixel values in component code)</li><li>Stream updates do not retrigger destination/shell enter motion or a backdrop-filter repaint behind the composer</li><li>Expanded sidebar session titles, project/group titles, and empty-state copy use the 13px compact token without changing the 28–32px row pitch</li></ol><h2 id="dark-floating-surfaces-codex-parity" tabindex="-1">Dark floating surfaces (Codex parity) <a class="header-anchor" href="#dark-floating-surfaces-codex-parity" aria-label="Permalink to &quot;Dark floating surfaces (Codex parity)&quot;">​</a></h2><ul><li>Main surface: <code>#181818</code> (<code>gray-900</code>)</li><li>Sidebar / surface-under: <code>#000000</code></li><li>Floating composer plate: Codex elevated-primary (<code>#212121f5</code> / <code>color-mix(gray-800 96%, transparent)</code>) with standard elevation-prominent (<code>0 0 0 .5px</code> stroke + <code>0 3px 7.5px #0000000a</code> + <code>0 0 20px #0000000d</code>); no heavier night-only lift</li><li>Light workspace chips capsule: elevated gray <code>#f4f4f4</code> (not pure white-on-white)</li><li>Combined workspace chips: elevated translucent plate over main, not flat main gray</li><li>Stage Manager (macOS only): host re-asserts min bounds while collapsed (permanent watchdog). The watchdog does not run on Windows/Linux, so no platform re-layers its own window unprompted (D447)</li></ul><h2 id="destination-pages" tabindex="-1">Destination pages <a class="header-anchor" href="#destination-pages" aria-label="Permalink to &quot;Destination pages&quot;">​</a></h2><ul><li><strong>Project archive</strong>: the D066 Codex index table (search / expand / actions) is embedded in Settings with no duplicate page title or outer page padding; the earlier standalone Projects destination and card grid (D042) are superseded by D133. Per D267 the destination is composed exactly like the agent capability pages (D257): one toolbar (sort segment, search, primary action) and one index whose Pinned / All projects / Archived groups are plain section header lines carrying the only counts on the page. Per D455 it stays one column with no side-by-side pane, and the index is an inset grouped list in the iOS sense: each row reads left to right as identity (glyph, name, status tag, path) and right to left as detail (session count, last active, disclosure indicator), and the selected row is the header of the card that opens under it — so the detail repeats no name, path, or tag. It has no hero block, no decorative gradient, and no page-level counter run</li><li><strong>Settings</strong>: full-page Codex shell per D063/D090/D133/D166 (275px compact navigation rail sharing the main sidebar material, elevated content cards, Back to app); per D092, the content cards fill the pane width available from the current window instead of retaining D070&#39;s fixed 720px cap — the earlier in-shell 200px rail and broad grouped directory are superseded</li><li><strong>Import</strong>: four kinds (sessions / models / skills / MCP) behind one page-scale segmented switcher, composed like the agent capability pages: a quiet pre-scan next-action state per kind, one toolbar per kind (select-all with both counts, the kind&#39;s own option, re-scan, import selected), and one list whose group headers are quiet label lines and whose candidates are individual tiles. No per-kind scan card, no tinted group band, no second copy of the settings row scaffold</li><li>Light destination cards use white elevated plates (not flat gray fills)</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("spec/04-ux/07-ui-design-system.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _07UiDesignSystem = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _07UiDesignSystem as default
};
