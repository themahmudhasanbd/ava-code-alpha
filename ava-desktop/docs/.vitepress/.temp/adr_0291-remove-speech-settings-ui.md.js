import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0291: Remove the speech settings UI","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0291-remove-speech-settings-ui.md","filePath":"adr/0291-remove-speech-settings-ui.md","lastUpdated":1789760880000}');
const _sfc_main = { name: "adr/0291-remove-speech-settings-ui.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0291-remove-the-speech-settings-ui" tabindex="-1">ADR 0291: Remove the speech settings UI <a class="header-anchor" href="#adr-0291-remove-the-speech-settings-ui" aria-label="Permalink to &quot;ADR 0291: Remove the speech settings UI&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-19</li><li>Deciders: PI-Desktop core</li><li>Amends: <a href="./0281-host-speech-capability">ADR 0281</a> (its v1 product entry)</li><li>Related: <a href="./../spec/04-ux/06-settings-ia">04-ux/06-settings-ia.md</a> · <a href="./../spec/04-ux/08-component-spec">04-ux/08-component-spec.md</a> · <a href="./../spec/03-runtime/20-speech">03-runtime/20-speech.md</a> · <a href="./../spec/06-delivery/04-e2e-test-plan">06-delivery/04-e2e-test-plan.md</a></li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>ADR 0281 added the host speech capability: <code>transcribe</code> / <code>synthesize</code> over an optional <code>AppSettings.speech</code>, two built-in protocols, a high-risk <code>speech.adapter.register</code> plugin adapter, and no audio bytes in the renderer. Its item 5 named the v1 product entry as a <strong>Voice</strong> card in Settings → AI plus Composer transcription and draft speech.</p><p>Two renderer surfaces were built for it. The Composer toolbar exposed mic (transcribe) and read-aloud (speak) buttons over a <code>useComposerSpeech</code> hook, and Settings → AI carried the <strong>Voice</strong> card. The Composer controls were removed on 2026-09-18 (<code>344ef4ec2</code>, PR #555) while the host services and the plugin-facing speech API stayed. The card then became the only renderer surface: the only consumer of <code>speech/getStatus</code>, <code>speech/transcribe</code>, and <code>speech/synthesize</code>, and the only reader of the <code>settings.speech*</code> catalog. The copy written for the removed controls — <code>chat.transcribe</code>, <code>chat.transcribing</code>, <code>chat.transcribeFailed</code>, <code>chat.speak</code>, <code>chat.speaking</code>, <code>chat.speakFailed</code>, <code>chat.speakSaved</code> — stayed in all eight catalogs with no caller, and the claims in <code>04-ux/08-component-spec.md</code> §2.5 and ADR 0281 item 5 outlived the controls they described.</p><p>The product does not want a speech configuration surface in Settings. It puts a second provider picker per role, plus Whisper / TTS vocabulary, inside the app shell for a capability whose only customers today are plugins and direct IPC callers.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><strong>Remove the Voice card.</strong> Settings → AI renders no speech surface. <code>apps/desktop/src/features/settings/voice-settings.tsx</code> and its <code>VoiceSettingsCard</code> mount in <code>features/settings/SettingsPage.tsx</code> are deleted.</li><li><strong>Remove the renderer leftovers.</strong> The AI destination in <code>lib/settings-search.ts</code> no longer indexes <code>settings.speechTitle</code>, <code>settings.speechTranscribe</code>, or <code>settings.speechSynthesize</code>; the <code>.settings-row:has(.settings-speech-fields)</code>, <code>.settings-speech-fields</code>, and <code>.settings-speech-lead</code> rules leave <code>styles/settings.css</code>; the thirteen <code>settings.speech*</code> keys leave all eight shipped locale catalogs; and the seven unreferenced <code>chat.transcribe*</code> / <code>chat.speak*</code> keys written for the withdrawn Composer controls leave them too.</li><li><strong>Keep the capability.</strong> ADR 0281 items 1–4 and 6 stay in force: <code>speech/getStatus</code>, <code>speech/transcribe</code>, <code>speech/synthesize</code>, <code>validateSpeechSettings</code>, the built-in <code>openai_audio</code> / <code>openai_chat_audio</code> protocols, the plugin <code>speech.adapter.register</code> permission, and the renderer API bridge in <code>lib/api.ts</code> are unchanged. Audio bytes still never enter the renderer.</li><li><strong>Bindings stay written by a caller.</strong> <code>AppSettings.speech</code> keeps its shape, validation, and persisted semantics; a binding is written through the host settings API (<code>settings/set</code>), and its consumers are plugins and IPC calls. No migration runs and no stored binding is dropped.</li><li><strong>Keep the Composer entry points withdrawn and correct their claims.</strong> ADR 0281 item 5&#39;s Composer transcription and draft-speech actions stay withdrawn: the controls they describe were already removed in <code>344ef4ec2</code>, so their copy is retired and <code>04-ux/08-component-spec.md</code> §2.5 no longer describes them. <code>04-ux/06-settings-ia.md</code> states that speech is not a Settings surface.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A user can no longer configure a transcription or speech provider from the app. The host capability remains reachable for plugins, for IPC callers, and for bindings already stored, but the desktop offers no UI to create one.</li><li>Removing the renderer&#39;s only speech consumer also removes its use of the protocol catalog: <code>speech/getStatus</code> is now called by no renderer module, while the IPC channel and its contract stay.</li><li>The renderer sheds the card, its styles, its search keywords, and its locale keys in eight catalogs; no host, protocol, permission, or schema change accompanies it.</li><li>The user-visible speech vocabulary (Whisper, TTS, voices) no longer appears in the app shell at all, so the chat model picker is the only model surface.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><h3 id="keep-the-card-and-hide-it-behind-a-developer-flag" tabindex="-1">Keep the card and hide it behind a developer flag <a class="header-anchor" href="#keep-the-card-and-hide-it-behind-a-developer-flag" aria-label="Permalink to &quot;Keep the card and hide it behind a developer flag&quot;">​</a></h3><p>Rejected: a hidden surface still owns styles, locale keys, and a provider protocol catalog, and it would keep a second provider picker alive in an app whose speech consumers are plugins.</p><h3 id="remove-the-host-capability-with-the-ui" tabindex="-1">Remove the host capability with the UI <a class="header-anchor" href="#remove-the-host-capability-with-the-ui" aria-label="Permalink to &quot;Remove the host capability with the UI&quot;">​</a></h3><p>Rejected: the capability is a shipped contract with an ADR, an IPC surface, and a high-risk plugin permission. Removing it would break plugin adapters and invalidate stored <code>AppSettings.speech</code> bindings without a migration, for a surface the request only asked to remove from Settings.</p><h3 id="keep-the-card-and-ship-the-composer-actions" tabindex="-1">Keep the card and ship the Composer actions <a class="header-anchor" href="#keep-the-card-and-ship-the-composer-actions" aria-label="Permalink to &quot;Keep the card and ship the Composer actions&quot;">​</a></h3><p>Rejected: the Composer controls were built and deliberately removed in the same release cycle (<code>344ef4ec2</code>, PR #555). Re-shipping them is a product decision that was already reversed, and the request is to remove the settings surface, not to grow the feature. The stale claims are corrected instead, so no spec describes a control that does not exist.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0291-remove-speech-settings-ui.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0291RemoveSpeechSettingsUi = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0291RemoveSpeechSettingsUi as default
};
