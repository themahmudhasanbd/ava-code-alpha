import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"10. 会话、Plan 和 Goal 状态机","description":"","frontmatter":{},"headers":[],"relativePath":"zh-CN/spec/03-runtime/10-session-state-machine.md","filePath":"zh-CN/spec/03-runtime/10-session-state-machine.md","lastUpdated":1789398369000}');
const _sfc_main = { name: "zh-CN/spec/03-runtime/10-session-state-machine.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="_10-会话、plan-和-goal-状态机" tabindex="-1">10. 会话、Plan 和 Goal 状态机 <a class="header-anchor" href="#_10-会话、plan-和-goal-状态机" aria-label="Permalink to &quot;10. 会话、Plan 和 Goal 状态机&quot;">​</a></h1><blockquote><p><strong>翻译说明：</strong> 本页是与 <a href="/spec/03-runtime/10-session-state-machine">英文源规格</a> 一一对应的机器辅助翻译。代码、协议字段和标识符保持原文；如翻译与英文源事实有歧义，以英文版本为准。</p></blockquote><h2 id="_0-持久运行模式与实时规划状态" tabindex="-1">0. 持久运行模式与实时规划状态 <a class="header-anchor" href="#_0-持久运行模式与实时规划状态" aria-label="Permalink to &quot;0. 持久运行模式与实时规划状态&quot;">​</a></h2><p>每个会话仅保留一种操作模式：<code>agent | plan | goal</code>。那里 是一 pi Agent。实时计划状态和执行状态是 host/runtime 投影。 Plan 和 Goal 是<strong>合约模式</strong>：两者在执行之前协商提案 并共享一项预测、一项批准行和一项硬否认 (<strong>D198</strong>)。 <code>kind</code> (<code>plan | goal</code>) 是区分它们的地方，下图读起来是一样的 将 Goal/Plan 替换为 Plan/Goal：</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Agent / inactive</span></span>
<span class="line"><span>  -- user selects Plan while idle OR Agent calls EnterPlanMode --&gt; Plan / planning</span></span>
<span class="line"><span>  -- user selects Goal while idle OR Agent calls EnterGoalMode --&gt; Goal / planning</span></span>
<span class="line"><span>Plan | Goal / planning</span></span>
<span class="line"><span>  -- SubmitPlan | SubmitGoal (title, markdown, question) --&gt; awaiting_approval</span></span>
<span class="line"><span>Plan | Goal / awaiting_approval</span></span>
<span class="line"><span>  -- approve(permission mode) --&gt; Agent / queued, same Agent continues</span></span>
<span class="line"><span>  -- reject | expiry | abort | crash | persistence failure</span></span>
<span class="line"><span>       --&gt; same contract mode / planning</span></span>
<span class="line"><span>Agent / queued</span></span>
<span class="line"><span>  -- dispatcher starts --&gt; Agent / running</span></span>
<span class="line"><span>Agent / running</span></span>
<span class="line"><span>  -- complete | fail | abort --&gt; Agent / inactive</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br></div></div><p>两种合约模式都保留了权限模式选择器。他们的 <code>Bash</code> 政策是 <code>ask</code> 或 <code>accept-edits</code> = 确认，<code>auto</code> = 不确认，所以是合约模式 表达协商意图，但不是严格的只读安全配置文件。 Write/Edit 和 在每个 Plan 或 Goal 权限模式下，插件工具仍被主机策略拒绝。</p><p>这些类型的区别仅在于合同内容和排队执行内容 指令要求：Plan 建议执行有序步骤，而 Goal 提出目标陈述、验收标准和界限，及其 执行持续进行——选择自己的方法——直到每次接受 标准被验证或边界阻止它。</p><p>仅允许通过 UI/session API 进行模式和配置更改 空闲时。批准不是通用工具权限：它是单独的 主机拥有的状态转换。主机重启会中断每个待批准的任务 和 queued/running 执行字段，无重放；一个已经批准的 中断将持久会话保留在 Agent 中。</p><h2 id="_1-会话状态" tabindex="-1">1. 会话状态 <a class="header-anchor" href="#_1-会话状态" aria-label="Permalink to &quot;1. 会话状态&quot;">​</a></h2><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>idle &lt;-&gt; running &lt;-&gt; waiting_permission</span></span>
<span class="line"><span>           \\/ aborted</span></span>
<span class="line"><span>           \\/ error</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><table tabindex="0"><thead><tr><th>状态</th><th>意义</th></tr></thead><tbody><tr><td><code>idle</code></td><td>无主动回合</td></tr><tr><td><code>running</code></td><td>model/tool 激活</td></tr><tr><td><code>waiting_permission</code></td><td>因用户权限决定而被阻止</td></tr><tr><td><code>aborted</code></td><td>当前轮次的终端（然后返回空闲）</td></tr><tr><td><code>error</code></td><td>当前轮次的终端（然后返回空闲）</td></tr></tbody></table><h2 id="_2-转向生命周期" tabindex="-1">2. 转向生命周期 <a class="header-anchor" href="#_2-转向生命周期" aria-label="Permalink to &quot;2. 转向生命周期&quot;">​</a></h2><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>accept_prompt</span></span>
<span class="line"><span> -&gt; turn_start</span></span>
<span class="line"><span> -&gt; streaming</span></span>
<span class="line"><span> -&gt; (optional tool_loop)</span></span>
<span class="line"><span>   -&gt; permission_maybe</span></span>
<span class="line"><span>   -&gt; tool_exec</span></span>
<span class="line"><span> -&gt; turn_end</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p>回合会到达三种终止原因之一 —— <code>completed</code>、<code>aborted</code> 或 <code>error</code> —— 与 第 1 节状态表中 <code>aborted</code> / <code>error</code> 两行一致。终止原因只决定一次： 中止会在取消请求发出之前记录其决定，因此之后的 <code>agent_end</code> 无法把已中止的 回合改述为已完成。终止事件按回合身份归属，而不是按会话：某个终止事件 所属的回合若已不再拥有该会话，它既不改变当前回合的状态，也不释放其资源， 迟到的消息行和工具行仍作为历史记录。宿主在每次已开始的回合中通过 <code>session:turnEnded</code> 插件事件宣告一次终止状态（见 ADR 0252， <code>docs/adr/0252-plugin-host-turn-end-event.md</code>）。</p><p>转向输入按同一身份判定：命名了一个已取消、已开始收尾或已不再拥有该会话的回合的输入， 会以「回合已结束」被拒绝。</p><h2 id="_3-转换规则" tabindex="-1">3. 转换规则 <a class="header-anchor" href="#_3-转换规则" aria-label="Permalink to &quot;3. 转换规则&quot;">​</a></h2><ol><li>每个会话只有一个有效回合</li><li>新提示被 <code>AGENT_BUSY</code> 拒绝，而 running/waiting_permission 期间 renderer 的 运行中发送路径经 <code>agent/queue/push</code> 把下一条 prompt 推入 Host 拥有的回合队列 （架构 v15，D375 / D386 / ADR 0213），并从 <code>agent/event/queueChanged</code> 镜像持久 条目；Agent Host 模块在 <code>agent_end</code> 之后释放一条，恢复的队列挂起到 owner 接入， <code>agent/queue/prioritize</code> 把条目移到队列头部，因此正常的用户发送不会看到 <code>AGENT_BUSY</code>。</li><li>允许中止运行或 waiting_permission。 Renderer 智能停止 删除未应答的 root 用户行并恢复其 session/turn-scoped 预序列化输入框快照；曾经助理文字、思考或任何 工具行开始，中止保留部分转录本并恢复不 草稿。</li><li>权限超时变为工具被拒绝，然后代理可以根据运行时处理继续或结束</li><li>终端回合状态持久后，会话状态返回空闲状态。父级终态错误会中止残留委托，因此“继续”不会变成 <code>AGENT_BUSY</code>（D352）</li><li>更改渲染器的活动 project/session 不会转换或中止 任何后台会话</li><li>工具转换保留原始会话的持久项目根； 它从不采用新活动项目的根</li><li><code>session.endTurn</code> 仅将 <code>running</code> 转至终端。在那个同 事务，未见的 <code>completed</code> 插入 <code>task.completed</code>，未见的 <code>error</code> 插入 <code>task.failed</code>，并且结果已在焦点当前中可见 聊天或任何 <code>aborted</code> 回合不会插入任何通知 (D117)。重复终端 调用是无操作的。</li><li>仅当源空闲时才允许分叉。孩子开始无所事事 没有回合或等待许可状态。 Electron 返回 <code>AGENT_BUSY</code> 的 主动运行时保护并规范主机的持续运行轮流 <code>CONFLICT</code> 回退到相同的 IPC 错误。两条路径均不产生部分 孩子。</li><li>提供 <code>throughMessageId</code> 仅更改快照边界。助理 Fork/Edit 仍然创建一个新的空闲会话 ID，没有共享轮次， 权限等待、运行时或提供商缓存状态 (D134)。</li><li><code>EnterPlanMode</code>、<code>EnterGoalMode</code>、<code>SubmitPlan</code> 和 <code>SubmitGoal</code> 必须是 他们中唯一的工具调用 助理批次。提交工具在新的文件中保留精确的 Markdown 字节 主机拥有的 <code>.pi/&lt;kind&gt;/*.md</code> 工件并创建一个待处理的 <code>plan_approvals</code> 行及其 <code>kind</code> 加上结构化的 title/question 和 神器领域。针对另一种模式调用的提交工具失败 与 <code>PLAN_KIND_MISMATCH</code> 并且什么也不写。</li><li>只有匹配的 <code>plans.resolve</code> 才能解决待处理的提案。批准 以原子方式将持久模式更改为 Agent，存储选定的显式 权限模式，分配执行ID，并更改行的 <code>execution_state</code> 至 <code>queued</code>。</li><li>批准和拒绝是唯一的解决方案操作。拒绝和到期 关闭挂起的行，然后将活动状态返回到可编辑状态 同合同模式规划状态 并且不授予任何执行工具。待处理的中断也会执行相同的操作；一个 批准后 queued/running 中断仍保持 Agent。</li><li>第二次提示，Plan 或 Goal 提交、配置更改或执行 是 在会话处于活动状态、等待批准时被拒绝，或者 queued/running 执行。仅当空闲时才接受配置。</li><li>同一合同模式的后续转变可能会修改 rejected/expired/interrupted 检查点和 必须创建一个新的不可变工件而不是覆盖早期的工件 快照。</li></ol><h2 id="_4-持久化点" tabindex="-1">4. 持久化点 <a class="header-anchor" href="#_4-持久化点" aria-label="Permalink to &quot;4. 持久化点&quot;">​</a></h2><p>消息持久化按照 04-data-storage §5 (D119) 分为两步：fsync&#39;d 转录文件行第一，索引事务第二。</p><ul><li>用户消息：接受时</li><li>转运行行：开始+终端<code>session.endTurn</code>更新</li><li>通知行：与看不见的 completed/error 终端相同的交易 更新；决不会为了可见的当前结果或中止</li><li>assistant/tool 消息：在 message_end/tool_end 上。完成的助手快照在 outbox 追加之前先做检查点；握手在冷启动 <code>session.get</code> 之前等待该 排空（D327）。再验证仍在运行或 刚完成、实时行尚未刷入磁盘的会话时，把有界持久化页按时间顺序缝 到实时快照上，使更早的实时行留在该页之前、进行中或尚未刷入的尾巴 留在该页之后（D317、D324）。只有当该页已包含每一条实时行时才清掉 实时来源标记。</li><li>孤儿会话恢复：活着的 JSONL 若 sessions 行已不在，则在主机启动和 <code>session.appendMessage</code> 时重新插入；排队的 outbox 在恢复后排空， <code>session.delete</code> 丢掉该会话的 outbox 条目（D318）</li><li>未应答的智能停止：标记在现有生命周期中中止的回合， 然后以原子方式将记录重写为其根用户行之前的前缀； 结构化输入框快照仅保留渲染器内存</li><li>mode/project 字段：更改时</li><li>Plan/Goal 提交：将精确的 Markdown 字节写入新的唯一值 <code>.pi/&lt;kind&gt;/*.md</code>， 记录 path/hash/size 加上类型和结构 title/question，然后插入 <code>pending</code> 批准事件之前的 <code>plan_approvals</code> 行</li><li>Plan/Goal审批：审批结果、模式转换、权限模式、 执行 一笔交易中的 ID 和 <code>queued</code> 状态； reject/expiry/interruption 保留 合同模式并将实时规划返回可编辑状态</li><li>启动恢复：以事务方式中断待批准的任务 queued/running 在服务 RPC 之前执行状态；中止关联的运行 轮流工作并且永不重播</li><li>分叉快照：新的转录文件加上一个子 session/index 交易； 源持久性保持不变；消息范围的快照结束 包含在所选消息中</li></ul><h2 id="_5-验收" tabindex="-1">5. 验收 <a class="header-anchor" href="#_5-验收" aria-label="Permalink to &quot;5. 验收&quot;">​</a></h2><ol><li>繁忙会话无法启动第二个并发轮次</li><li>Abort是幂等的</li><li>waiting_permission在UI状态中可见</li><li>两个保留的项目选项卡中的会话可以独立运行，无需 转录事件或工作空间根交叉</li><li>每个未见过的 completed/failed 回合恰好产生一条通知记录 而可见当前结果或中止的回合不会产生任何结果</li><li>空闲分叉作为独立的空闲会话启动；繁忙的信号源无法 生一个孩子</li><li>消息范围的分叉排除后面的行并且从没有源运行时开始 或提供商缓存状态</li><li>Plan、Goal 和 Agent 使用 1 个 pi Agent； Composer-左模式芯片、UI 条目，以及 <code>queued</code>/reject/expiry/interruption 收敛于相同的规划状态，并且 批准恢复 Agent 模式下的 Agent</li><li>合约模式策略仅通过选择的权限模式允许Bash 和 在 Goal 中拒绝 Write/Edit/plugins，无论 <code>auto</code> 或会话授权如何 与 Plan 完全相同</li><li>SubmitPlan/SubmitGoal 使用以下命令写入精确唯一的 <code>.pi/&lt;kind&gt;/*.md</code> 工件 hash/size， 保持 title/question 结构化，并且只有 approve/reject 可以解析其 <code>plan_approvals</code> 行 11.到期使用<code>PLAN_APPROVAL_TIMEOUT</code>；启动中断、shell故障、 进程恢复失败关闭，并且重新启动不会重放挂起， 排队或正在运行的工作</li><li>Goal 执行在结束前报告每个接受标准的结果 转牌圈，并且 scheduled/unattended Goal 跑路被拒绝 <code>PLAN_REQUIRES_INTERACTIVE_SESSION</code> 与 Plan 完全相同</li></ol></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("zh-CN/spec/03-runtime/10-session-state-machine.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _10SessionStateMachine = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _10SessionStateMachine as default
};
