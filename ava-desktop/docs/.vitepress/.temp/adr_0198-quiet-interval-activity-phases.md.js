import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0198: Name every quiet interval on the live activity row","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0198-quiet-interval-activity-phases.md","filePath":"adr/0198-quiet-interval-activity-phases.md","lastUpdated":1789898316000}');
const _sfc_main = { name: "adr/0198-quiet-interval-activity-phases.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0198-name-every-quiet-interval-on-the-live-activity-row" tabindex="-1">ADR 0198: Name every quiet interval on the live activity row <a class="header-anchor" href="#adr-0198-name-every-quiet-interval-on-the-live-activity-row" aria-label="Permalink to &quot;ADR 0198: Name every quiet interval on the live activity row&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-09</li><li>Related: ADR 0175, ADR 0186, D338, D349, D365</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>ADR 0175 added a compact runtime status row for three quiet waits: <code>waiting-model</code>, <code>retrying</code>, and <code>waiting-subagents</code>. Users still saw a long-running turn with no explanation when the runtime was compacting context, recovering a silent response, preparing the next provider request, or waiting on subagents whose child work was invisible on that row. The generic <code>Working…</code> fallback also hid <code>starting</code>. A parent wait that only said &quot;Waiting for 2 subagents&quot; for more than a minute looked stuck even when the delegates were reading and searching.</p><p>The row must stay a single compact inline status. It must not restore the activity-group current-state capsule removed after ADR 0175, and it must not stream raw child events.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Extend <code>AgentActivity</code> so every user-visible quiet interval has a named phase, and enrich <code>waiting-subagents</code> with a live snapshot of running targets:</p><ol><li><code>starting</code> is rendered instead of generic <code>Working…</code>.</li><li><code>preparing</code> covers the gap after a tool batch and before the next provider request.</li><li><code>compacting</code> covers an in-progress checkpoint and carries the reason (<code>manual</code> / <code>threshold</code> / <code>overflow</code>).</li><li><code>recovering</code> covers the silent-turn re-run until the retry request is issued.</li><li><code>waiting-subagents</code> keeps <code>subagentCount</code> as the live running count and adds optional <code>agents[]</code> with <code>name</code>, <code>lastPhase</code> (<code>waiting-model</code> | <code>thinking</code> | <code>tool</code>), and <code>lastToolName</code>. The snapshot updates when a child tool starts/ends or thinking begins; token-level child events do not emit a new status.</li></ol><p>The renderer still draws one compact localized row with a monotonic phase timer. A one-subagent wait names the agent and its latest action; a multi-agent wait lists each running target. The renderer originally hid the row behind thinking, tool, and answer surfaces. Issue #669 demonstrated that those surfaces can remain unchanged while a turn is still running. The tail row now remains visible until completion or pending user interaction, with a Working or Planning/Goal fallback when no runtime phase is known. No second progress card or percentage is added.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Quiet compaction, recovery, and post-tool gaps are distinguishable from a hung turn.</li><li>A parent wait on delegates shows what those delegates are doing without forwarding their event stream.</li><li>Older <code>waiting-subagents</code> payloads without <code>agents</code> still render the count-only label.</li><li>ADR 0175&#39;s coarse-phase rule still holds: child internals stay out of the protocol except as this bounded snapshot.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><h3 id="restore-a-current-state-capsule-on-the-processing-group" tabindex="-1">Restore a current-state capsule on the processing group <a class="header-anchor" href="#restore-a-current-state-capsule-on-the-processing-group" aria-label="Permalink to &quot;Restore a current-state capsule on the processing group&quot;">​</a></h3><p>Rejected because it duplicated the dedicated runtime row and was already removed as redundant chrome.</p><h3 id="forward-raw-subagent-events-onto-the-parent-status-row" tabindex="-1">Forward raw subagent events onto the parent status row <a class="header-anchor" href="#forward-raw-subagent-events-onto-the-parent-status-row" aria-label="Permalink to &quot;Forward raw subagent events onto the parent status row&quot;">​</a></h3><p>Rejected because it would expand the renderer protocol and flicker on every thinking token. A coarse <code>lastPhase</code> / <code>lastToolName</code> snapshot is enough to explain the wait.</p><h3 id="keep-generic-working-for-starting-compaction-and-recovery" tabindex="-1">Keep generic <code>Working…</code> for starting, compaction, and recovery <a class="header-anchor" href="#keep-generic-working-for-starting-compaction-and-recovery" aria-label="Permalink to &quot;Keep generic \`Working…\` for starting, compaction, and recovery&quot;">​</a></h3><p>Rejected because those intervals are the ones users already misread as a stuck turn.</p><h2 id="references" tabindex="-1">References <a class="header-anchor" href="#references" aria-label="Permalink to &quot;References&quot;">​</a></h2><ul><li><code>docs/spec/03-runtime/01-ipc-protocol.md</code></li><li><code>docs/spec/03-runtime/02-agent-runtime.md</code></li><li><code>docs/spec/04-ux/09-interaction-patterns.md</code></li><li><code>docs/spec/06-delivery/04-e2e-test-plan.md</code> (E2E-008c, E2E-094)</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0198-quiet-interval-activity-phases.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0198QuietIntervalActivityPhases = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0198QuietIntervalActivityPhases as default
};
