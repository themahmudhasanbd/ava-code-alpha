import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0065: Smooth shell layout and stream feedback","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0065-smooth-shell-layout-and-stream-feedback.md","filePath":"adr/0065-smooth-shell-layout-and-stream-feedback.md","lastUpdated":1787640443000}');
const _sfc_main = { name: "adr/0065-smooth-shell-layout-and-stream-feedback.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0065-smooth-shell-layout-and-stream-feedback" tabindex="-1">ADR 0065: Smooth shell layout and stream feedback <a class="header-anchor" href="#adr-0065-smooth-shell-layout-and-stream-feedback" aria-label="Permalink to &quot;ADR 0065: Smooth shell layout and stream feedback&quot;">​</a></h1><ul><li>Status: Accepted for implementation</li><li>Date: 2026-08-07</li><li>Deciders: PI-Desktop core</li><li>Related: ADR 0033, ADR 0122, D146, D163, D255</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The shell&#39;s sidebar and work panel already used enter/exit keyframes, but the keyframes changed only opacity and a small transform. Their flex allocations changed to the final width as soon as the element mounted, so MainChat moved before the first visible frame and moved back after the exit animation. This made panel and sidebar actions feel like a jump followed by a cosmetic fade.</p><p>Agent streams had a similar timing problem. Every <code>message_update</code> and <code>tool_update</code> replaced the renderer message array immediately, while the transcript rebuilt its grouped entry projection and follow-scroll work. A fast provider could therefore spend several updates in one frame on historical transcript work instead of presenting the newest tail smoothly.</p><p>The docked composer also painted a full-width gradient veil over the lower transcript. The composer already reserves its measured height and owns an opaque elevated surface, so the veil added visual noise without carrying state.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>Sidebar and work-panel mount/exit animations animate both <code>width</code> and <code>flex-basis</code> from zero to the committed dock width, in addition to the bounded opacity/transform feedback. The panel remains an in-flow fixed-width column and the OS window reservation contract does not change.</li><li>The renderer coalesces high-frequency <code>message_update</code> and <code>tool_update</code> events by stream target until the next animation frame. Only the newest event for each target is applied. Any non-stream control or terminal event flushes the pending batch first, so lifecycle ordering and final states stay synchronous.</li><li><code>ChatTranscript</code> uses React&#39;s deferred value for the heavy grouped-entry projection and minimap input. Immediate controls, permission state, running state, and terminal outcomes remain synchronous.</li><li>The docked composer uses a transparent outer dock with bottom space reserved by the transcript; the composer shell remains the only elevated surface.</li><li>An activity group only receives terminal failure styling after its active run settles. Intermediate error rows remain visible, but a provider/tool retry does not make the whole still-running group look finished.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Opening and closing auxiliary columns now reflows MainChat continuously over the bounded motion duration instead of jumping before the animation.</li><li>A stream may display the newest partial content one paint later when events arrive faster than the display refreshes, but redundant intermediate payloads are discarded and terminal events are never delayed.</li><li>Long transcripts still need a future true virtualization pass; this decision reduces update pressure without changing message persistence or grouping.</li><li>Removing the dock veil makes the transcript surface visually quieter and keeps the composer separation dependent on its measured layout and elevation.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><h3 id="transform-only-dock-animation" tabindex="-1">Transform-only dock animation <a class="header-anchor" href="#transform-only-dock-animation" aria-label="Permalink to &quot;Transform-only dock animation&quot;">​</a></h3><p>Rejected because it leaves the flex allocation at its final value before the first painted frame, which is the source of the visible layout jump.</p><h3 id="overlay-the-work-panel-above-mainchat" tabindex="-1">Overlay the work panel above MainChat <a class="header-anchor" href="#overlay-the-work-panel-above-mainchat" aria-label="Permalink to &quot;Overlay the work panel above MainChat&quot;">​</a></h3><p>Rejected for this change because the current frozen shell contract is a docked third column with a fixed committed width. Overlaying it would hide transcript content and require a broader responsive and accessibility decision.</p><h3 id="debounce-all-agent-events" tabindex="-1">Debounce all agent events <a class="header-anchor" href="#debounce-all-agent-events" aria-label="Permalink to &quot;Debounce all agent events&quot;">​</a></h3><p>Rejected because lifecycle, permission, planning, and terminal events need immediate ordering. Only replaceable partial stream updates are coalesced.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0065-smooth-shell-layout-and-stream-feedback.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0065SmoothShellLayoutAndStreamFeedback = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0065SmoothShellLayoutAndStreamFeedback as default
};
