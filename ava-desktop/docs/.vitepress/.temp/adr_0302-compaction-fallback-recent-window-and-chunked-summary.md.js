import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0302: A failed compaction keeps the recent window, and an oversized summary is chunked","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0302-compaction-fallback-recent-window-and-chunked-summary.md","filePath":"adr/0302-compaction-fallback-recent-window-and-chunked-summary.md","lastUpdated":1790057120000}');
const _sfc_main = { name: "adr/0302-compaction-fallback-recent-window-and-chunked-summary.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0302-a-failed-compaction-keeps-the-recent-window-and-an-oversized-summary-is-chunked" tabindex="-1">ADR 0302: A failed compaction keeps the recent window, and an oversized summary is chunked <a class="header-anchor" href="#adr-0302-a-failed-compaction-keeps-the-recent-window-and-an-oversized-summary-is-chunked" aria-label="Permalink to &quot;ADR 0302: A failed compaction keeps the recent window, and an oversized summary is chunked&quot;">​</a></h1><ul><li>Status: Accepted for implementation</li><li>Date: 2026-09-22</li><li>Amends: ADR 0049 (the retained tail is the recent window, not one user message; a prompt that does not fit is chunked rather than skipped) / ADR 0282 (the reduced pass is no longer the last attempt before the fallback)</li><li>Related: issue #827 · ADR 0064 (the <code>fresh_window</code> family, unchanged) · <a href="./../spec/03-runtime/02-agent-runtime">03-runtime/02-agent-runtime</a></li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>ADR 0049 decided that an automatic compaction failure installs a deterministic retained-tail checkpoint, and described that checkpoint as preserving &quot;the recent provider-valid message tail&quot;. ADR 0282 then made the runtime retry a failed summary and reduce an oversized prompt once before the fallback ran.</p><p>The shipped contract was narrower than either record promised. The Codex-shaped reshape of a preparation folds the split-turn prefix and pi&#39;s recent tail back into <code>messagesToSummarize</code>, then rebuilds the tail as at most the latest user message, and a rebuild of a stored checkpoint narrowed the tail again. On the third automatic compaction of a reported session — whose summary request failed with no provider usage — the checkpoint therefore kept one user sentence, and about 168 messages (3 user / 34 assistant / 131 tool) left the next model request. The model no longer knew the decisions, paths, or unfinished work of the turn it was continuing, while the transcript on disk and the UI row stayed intact: the session looked healthy and behaved as if amnesiac.</p><p>Two further limits produced the same symptom. A summary prompt that still did not fit after the single reduced pass was skipped instead of summarized, so a budget failure cost the range entirely; and the checkpoint recorded <code>usage: null</code> with no reason, leaving the next report unable to distinguish a budget failure from a provider failure.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>A retained-tail fallback retains the real recent window: the newest contiguous messages of the compacted range, every role, bounded by the keep-recent target and by what the safe budget leaves once the carried-forward summary and the recovery notice are paid for. It records <code>details.retainedTailShape</code> as <code>recent_window</code>, and a checkpoint without that marker — every successful checkpoint, and every record written before the marker existed — still normalizes to its latest user message, so a restart cannot restore a sequence of executable-looking old requests. The fallback drops the assistant messages pi drops from the rebuilt context anyway (error, aborted, deferred) and any tool result whose tool call is not in the window, because a provider rejects a result whose call is missing. An <code>active_turn</code> fallback also keeps the active task&#39;s user message ahead of the window when the window itself cannot hold it, so a continuation never loses its goal.</p><p>A budget too small for one prompt no longer skips the model. The range is split into contiguous chunks that each fit the request budget — at most 16 requests, each carrying the previous chunk&#39;s summary through pi&#39;s update-the-summary prompt, with the range&#39;s file list appended on the last one — so the checkpoint still covers every message behind its boundary, and the summary reports the summed usage of the requests that produced it. Only an empty range, or one past that request bound, still falls back on budget grounds.</p><p>The fallback records <code>details.failureReason</code> from a closed vocabulary — <code>no_new_history</code>, <code>summary_budget</code>, <code>summary_provider</code>, <code>checkpoint_oversized</code> — instead of provider error text, which ADR 0049 keeps out of the persisted record.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A failed summary no longer costs the active turn: the degradation is bounded by the same recent-context budget a successful checkpoint respects.</li><li>A fallback checkpoint is larger than it was, and so is the continuation request it authorizes. The window is capped by the keep-recent target and by the room the carried summary leaves, so the guard still applies.</li><li>A summary of a very large range costs several provider requests and reports their summed usage. That is the intent: the alternative was no summary.</li><li>Provider validity is unchanged: a retained tail never carries an orphaned tool result or an unanswered tool call.</li><li>Manual <code>/compact</code> keeps its fail-fast contract, the <code>fresh_window</code> family still carries an empty tail, and no setting, protocol field, or persisted record schema changed.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><h3 id="keep-the-one-user-message-tail-and-rely-on-the-carried-summary" tabindex="-1">Keep the one-user-message tail and rely on the carried summary <a class="header-anchor" href="#keep-the-one-user-message-tail-and-rely-on-the-carried-summary" aria-label="Permalink to &quot;Keep the one-user-message tail and rely on the carried summary&quot;">​</a></h3><p>Rejected: the failed summary is the carried one, so the range after it has no representation at all. That is the reported defect, not a smaller version of it.</p><h3 id="retain-the-window-only-for-active-turn" tabindex="-1">Retain the window only for <code>active_turn</code> <a class="header-anchor" href="#retain-the-window-only-for-active-turn" aria-label="Permalink to &quot;Retain the window only for \`active_turn\`&quot;">​</a></h3><p>Rejected: a completed turn that fails loses the same delta, and the window ends on a finished assistant message, so it reads as history rather than as a pending request — which is what the older normalization guarded against.</p><h3 id="summarize-an-oversized-range-by-concatenating-partial-summaries-with-no" tabindex="-1">Summarize an oversized range by concatenating partial summaries with no <a class="header-anchor" href="#summarize-an-oversized-range-by-concatenating-partial-summaries-with-no" aria-label="Permalink to &quot;Summarize an oversized range by concatenating partial summaries with no&quot;">​</a></h3><p>second request per chunk</p><p>Rejected: each chunk needs its own request regardless, and any merge step either costs another request or leaves several sections that disagree. Chaining pi&#39;s update-the-summary prompt converges on one summary for the whole range.</p><h3 id="raise-the-hard-limit-or-keep-skipping-the-shot-summary" tabindex="-1">Raise the hard limit or keep skipping the shot summary <a class="header-anchor" href="#raise-the-hard-limit-or-keep-skipping-the-shot-summary" aria-label="Permalink to &quot;Raise the hard limit or keep skipping the shot summary&quot;">​</a></h3><p>Rejected: the failure is the summarizer&#39;s, not the window&#39;s, and the guard is what keeps the next request inside the provider limit.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0302-compaction-fallback-recent-window-and-chunked-summary.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0302CompactionFallbackRecentWindowAndChunkedSummary = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0302CompactionFallbackRecentWindowAndChunkedSummary as default
};
