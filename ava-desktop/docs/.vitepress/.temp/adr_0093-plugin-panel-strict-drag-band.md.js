import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0093: Keep a strict 46px plugin drag band with a minimal capsule","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0093-plugin-panel-strict-drag-band.md","filePath":"adr/0093-plugin-panel-strict-drag-band.md","lastUpdated":1787313308000}');
const _sfc_main = { name: "adr/0093-plugin-panel-strict-drag-band.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0093-keep-a-strict-46px-plugin-drag-band-with-a-minimal-capsule" tabindex="-1">ADR 0093: Keep a strict 46px plugin drag band with a minimal capsule <a class="header-anchor" href="#adr-0093-keep-a-strict-46px-plugin-drag-band-with-a-minimal-capsule" aria-label="Permalink to &quot;ADR 0093: Keep a strict 46px plugin drag band with a minimal capsule&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-08-17</li><li>Deciders: PI-Desktop core</li><li>Related: D235, ADR 0092</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The plugin-owned panel surface introduced a compact host capsule, but its layout contract must remain explicit. Frameless windows still need a reliable drag area, and the top band cannot receive plugin clicks. The host must not grow that band for a title, a platform-specific control row, or decorative chrome. Development authors also need a visible reminder of the constraint.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>Every plugin panel reserves exactly 46 CSS px for the transparent host drag band. Normal-flow content is offset by that same value; fixed and sticky plugin UI uses <code>--pi-plugin-titlebar-height: 46px</code>.</li><li>The drag band uses <code>-webkit-app-region: drag</code> and blocks plugin pointer interaction across those 46px. The host capsule opts out with <code>-webkit-app-region: no-drag</code>, so its three buttons remain clickable.</li><li>The host renders no panel title. Development panels show a localized, non-interactive reminder that the top 46px is drag-only outside the capsule; production panels do not show the reminder.</li><li>The capsule stays fixed at the top-right inside the 46px band and remains a minimal closed-Shadow-DOM surface: three controls, a subtle border and page-adaptive background, with no heavy shadow or blur effect. Its private sender-validated window-control channel, localized labels, focus behavior, reduced-motion behavior, and <code>window.pluginBridge</code> remain unchanged.</li></ol><h2 id="paint-through-opt-in" tabindex="-1">Paint-through opt-in <a class="header-anchor" href="#paint-through-opt-in" aria-label="Permalink to &quot;Paint-through opt-in&quot;">​</a></h2><p>Panels that need a full-bleed surface may declare <code>&lt;meta name=&quot;pi-plugin-chrome&quot; content=&quot;v3&quot;&gt;</code>. The host keeps the same 46px geometry and capsule, but replaces one full-width native drag rectangle with empty-space drag segments. Standard controls and elements marked with <code>data-pi-plugin-no-drag</code> create holes in that map, so the page can paint and receive pointer events wherever an element is present while blank space still drags the window. Existing <code>v2</code> panels retain the strict non-clickable band above for compatibility.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Plugin authors get a stable 46px layout and interaction contract on every desktop platform.</li><li>The top band is intentionally unavailable to plugin buttons except for the host capsule; plugin toolbars should use the documented drag/no-drag rules.</li><li>Development authors see the constraint while production panels keep the surface quiet.</li><li>The capsule has less visual weight and cannot extend beyond the drag band.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><h3 id="let-the-drag-band-grow-with-the-capsule" tabindex="-1">Let the drag band grow with the capsule <a class="header-anchor" href="#let-the-drag-band-grow-with-the-capsule" aria-label="Permalink to &quot;Let the drag band grow with the capsule&quot;">​</a></h3><p>Rejected because a variable host band would change plugin layout and make the same panel differ across platforms or future chrome revisions.</p><h3 id="let-every-plugin-content-receive-clicks-in-the-drag-band-by-default" tabindex="-1">Let every plugin content receive clicks in the drag band by default <a class="header-anchor" href="#let-every-plugin-content-receive-clicks-in-the-drag-band-by-default" aria-label="Permalink to &quot;Let every plugin content receive clicks in the drag band by default&quot;">​</a></h3><p>Rejected for the default because frameless window dragging would become unreliable and authors could mistake the top strip for ordinary interactive content. The <code>v3</code> opt-in is intentionally explicit and keeps the original <code>v2</code> behavior unchanged.</p><h3 id="restore-a-host-rendered-panel-title" tabindex="-1">Restore a host-rendered panel title <a class="header-anchor" href="#restore-a-host-rendered-panel-title" aria-label="Permalink to &quot;Restore a host-rendered panel title&quot;">​</a></h3><p>Rejected because the plugin owns the visible panel hierarchy; the host only needs the three native window actions and the development constraint reminder.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0093-plugin-panel-strict-drag-band.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0093PluginPanelStrictDragBand = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0093PluginPanelStrictDragBand as default
};
