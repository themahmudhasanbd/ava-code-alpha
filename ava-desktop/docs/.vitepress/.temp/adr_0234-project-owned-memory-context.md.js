import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0234: Keep project memory host-owned and path-scoped","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0234-project-owned-memory-context.md","filePath":"adr/0234-project-owned-memory-context.md","lastUpdated":1789382919000}');
const _sfc_main = { name: "adr/0234-project-owned-memory-context.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0234-keep-project-memory-host-owned-and-path-scoped" tabindex="-1">ADR 0234: Keep project memory host-owned and path-scoped <a class="header-anchor" href="#adr-0234-keep-project-memory-host-owned-and-path-scoped" aria-label="Permalink to &quot;ADR 0234: Keep project memory host-owned and path-scoped&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-12</li><li>Deciders: PI-Desktop maintainers</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Projects need a durable memory surface similar to ChatGPT Projects. Renderer preferences are not a safe owner for model context: they are presentation state, can be reset independently, and do not provide a host-enforced boundary when a session switches projects.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Store one user-authored memory collection per canonical project path in the existing host <code>kv</code> table under the <code>projectMemory</code> namespace. The visual editor stores normalized entries with an id, optional title, and content using the <code>entries-v1</code> format. The host also derives a readable plain-text <code>content</code> value, limited to 32 KiB, for runtime injection. Electron main loads the value for the session-bound project path and passes it to the agent runtime. The runtime renders it after project instructions as explicitly user-provided context and compares it when deciding whether an idle runtime can be reused.</p><p>The memory editor is exposed from the Project archive row menu. Creating a project keeps the creation surface compact and shows that project memory is available later; it does not add a memory mode selector whose semantics the application does not implement.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Memory survives renderer restarts and is isolated by canonical project path.</li><li>The visual editor can add, edit, and remove memory entries while the runtime consumes the host-derived plain-text projection. Existing legacy records that contain only <code>content</code> remain readable as one untitled entry.</li><li>Changing memory retires the next reusable runtime so a follow-up prompt sees the new value.</li><li>Project memory cannot override runtime safety, tool, or collaboration rules.</li><li>No schema migration is needed because <code>kv</code> is the existing extensibility boundary for new configuration domains.</li><li>ADR 0249 adds an explicit host-owned logical project-group identity and root membership model. Path-scoped records remain the compatibility behavior for legacy single-root projects.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0234-project-owned-memory-context.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0234ProjectOwnedMemoryContext = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0234ProjectOwnedMemoryContext as default
};
