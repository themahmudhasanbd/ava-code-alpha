import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0268: Remove quotes, annotations, and side chats","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0268-remove-quotes-annotations-and-side-chats.md","filePath":"adr/0268-remove-quotes-annotations-and-side-chats.md","lastUpdated":1789547461000}');
const _sfc_main = { name: "adr/0268-remove-quotes-annotations-and-side-chats.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0268-remove-quotes-annotations-and-side-chats" tabindex="-1">ADR 0268: Remove quotes, annotations, and side chats <a class="header-anchor" href="#adr-0268-remove-quotes-annotations-and-side-chats" aria-label="Permalink to &quot;ADR 0268: Remove quotes, annotations, and side chats&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-16</li><li>Deciders: PI-Desktop runtime and desktop UI maintainers</li><li>Supersedes: ADR message-quotes-and-side-chats, ADR response-annotations, ADR floating-annotation-index</li><li>Amends: ADR 0254 (its &quot;Amendment: native side-chat forks&quot;, 2026-09-14)</li><li>Preserves: D097, D128, D134, D209, D254, D261, D301</li><li>Related: <a href="./../spec/04-ux/08-component-spec">04-ux/08-component-spec.md</a> · <a href="./../spec/04-ux/09-interaction-patterns">04-ux/09-interaction-patterns.md</a> · <a href="./../spec/06-delivery/04-e2e-test-plan">06-delivery/04-e2e-test-plan.md</a></li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Three renderer-owned surfaces grew around the transcript and the composer. ADR message-quotes-and-side-chats added a <strong>Quote</strong> action to the message action row, a selected-text overlay, and renderer-owned side chats docked in the work panel. ADR response-annotations turned Add to chat on an assistant turn into a numbered annotation attachment carried to the model in a generated <code># Response annotations:</code> prompt block, and ADR floating-annotation-index added the floating index, source badges and highlights, and the comment editor. ADR 0254&#39;s native side-chat fork amendment made those panels work for <code>native-pi:</code> sessions.</p><p>Together they are more surface than the product uses. The overlay and the action row both had to recover an excerpt from the rendered DOM; annotations added a second prompt serialization that every display surface (transcript, edit seed, queue preview) has to reverse; and a side chat is a second session lifecycle whose only durable artifact — the forked child — <code>session.fork</code> already produces. Copy, edit, fork, and retry cover the rest.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><strong>Message action row.</strong> Remove <strong>Quote</strong>, <strong>Annotate</strong>, and <strong>Open/Ask in side chat</strong> from the hover action row of user messages and assistant turns. Copy, Edit and Delete (user messages), Fork, and Retry/Regenerate stay unchanged in place, behavior, labels, and shortcuts.</li><li><strong>Selection overlay.</strong> Remove the selected-text toolbar entirely: selecting text in the transcript no longer floats an action strip. Delete <code>lib/selection-quote.ts</code>, <code>lib/chat-quotes.ts</code>, the <code>SelectionQuoteButton</code> component and its styles. The composer keeps no quote-prefill path: <code>appendComposerDraftText</code> and <code>quoteMessageIntoComposer</code> and their store action go away.</li><li><strong>Annotations.</strong> Remove annotations as a feature: the comment editor, the floating annotation index, the answer source badges and highlights, the composer annotation attachment chip, the annotation store slice, and the annotation anchor modules are deleted. A <code>:codex-annotation{index=&quot;N&quot;}</code> directive in an answer is no longer recognized; it renders as the ordinary Markdown text it is.</li><li><strong>Prompt assembly.</strong> The send path no longer composes a <code># Response annotations:</code> block: <code>api.prompt</code> and <code>enqueuePrompt</code> use the user&#39;s text as written, with no generated heading, instruction sentence, or <code>&lt;response-annotations&gt;</code> payload. The display-layer reduction <code>requestTextWithoutAnnotations</code> is deleted with it, so an older stored prompt that already contains the generated block is shown verbatim in the transcript, in edit seeds, and in queue previews instead of being reduced back to the request.</li><li><strong>Side chats.</strong> Remove side chats as a feature: the first-Send draft lifecycle that created the child session, the renderer transcript projection (<code>projectSideChatEvent</code>), the <code>sidechat:</code> work-panel tab type and its <code>SideChatTab</code>, the registration / release / cascade-cleanup rules, and the availability blocked-reason reporting are all deleted. <code>session.fork</code> stays as a capability and the Fork action stays as its only entry point.</li><li><strong>Retired identifiers (retired; must never be reused).</strong> Decision IDs <code>D-LOCAL-message-quotes</code>, <code>D-LOCAL-selection-overlay</code>, <code>D-LOCAL-response-annotations</code>, <code>D-native-sidechat-stream</code>, and E2E IDs <code>E2E-CHAT-quote-prefill</code>, <code>E2E-CHAT-selection-markdown</code>, <code>E2E-CHAT-selection-side-chat</code>, <code>E2E-CHAT-annotation-attachments</code>, <code>E2E-CHAT-annotation-session-state</code>, <code>E2E-CHAT-annotation-source-index</code>, <code>E2E-CHAT-annotation-ack-and-steering</code>, <code>E2E-CHAT-side-chat-fork</code>, <code>E2E-CHAT-side-chat-stream</code>, <code>E2E-CHAT-side-chat-add-to-main</code>, <code>E2E-CHAT-side-chat-promote</code>, <code>E2E-CHAT-side-chat-close</code>, and <code>E2E-SESSION-native-side-chat-fork-survives-close</code> are retired and must never be reused for another scenario. The superseded ADR files stay on disk as history.</li><li><strong>Boundaries.</strong> The host protocol stays v11 and the storage schema stays v15: no new IPC channel, no new permission, no protocol or schema bump, and no Rust change. <code>session.fork</code>, the transcript, the work panel, and the rest of the composer keep their existing contracts.</li></ol><h2 id="preserved-behavior" tabindex="-1">Preserved behavior <a class="header-anchor" href="#preserved-behavior" aria-label="Permalink to &quot;Preserved behavior&quot;">​</a></h2><ul><li><strong>D134 / <code>session.fork</code>.</strong> Fork still resolves a durable child from an anchor message and still activates it, including the <code>native-pi:</code> route of ADR 0254; ADR 0023 is unchanged. Side chats were a consumer of it, not a part of it.</li><li><strong>D209 / D301.</strong> Smart Stop still restores the unanswered send&#39;s draft, and per-session draft slots are untouched. With no prefill and no annotation snapshot beside the draft, both paths simply have less to carry.</li><li><strong>D097 / D128 / D254 / D261.</strong> The work panel keeps role, sizing, identity keying, neighbor-close, and per-session switching; losing the <code>sidechat:</code> tab type removes one resource kind, not the model. Transcript reading ownership and the bounded expansion history window are unchanged.</li><li><strong>The rest of the action row.</strong> Copy, Edit, Delete, Retry/Regenerate, and Fork keep their current behavior, tooltips, and accessible names.</li><li><strong>Child sessions already created.</strong> A session forked earlier from a side chat stays an ordinary session in the sidebar, lists, search, and stored history.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A user loses the shortcut of reusing an earlier prompt or answer inside the composer, and loses the panel for asking a side question beside the main conversation; reassembling either means copying text or forking a session and switching to it.</li><li>A stored prompt written while annotations were enabled can show the generated <code># Response annotations:</code> heading and payload as plain text, because every display surface now renders the stored string unchanged.</li><li>The renderer sheds roughly 2000 lines — the overlay, quote recovery, annotation editor, index, badges, and the whole side-chat lifecycle — plus eleven unit-test files and their E2E scenarios.</li><li>Nothing moves across a process boundary: protocol v11, schema v15, the IPC surface, the permission set, and the Rust host are untouched.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><h3 id="hide-the-entry-points-and-keep-the-code" tabindex="-1">Hide the entry points and keep the code <a class="header-anchor" href="#hide-the-entry-points-and-keep-the-code" aria-label="Permalink to &quot;Hide the entry points and keep the code&quot;">​</a></h3><p>Rejected: dead branches would still own store slices, i18n keys, CSS, and the generated-block parser, and every later change to the action row or the send path would have to keep them compiling for nobody.</p><h3 id="keep-a-display-layer-compatibility-downgrade" tabindex="-1">Keep a display-layer compatibility downgrade <a class="header-anchor" href="#keep-a-display-layer-compatibility-downgrade" aria-label="Permalink to &quot;Keep a display-layer compatibility downgrade&quot;">​</a></h3><p>Rejected: keeping <code>requestTextWithoutAnnotations</code> and the directive parser alive only to prettify old prompts would preserve exactly the second serialization this ADR removes, and a stored prompt that is not sent is harmless as text. Old transcripts are readable without it.</p><h3 id="remove-annotations-and-side-chats-keep-quotes-and-the-selection-overlay" tabindex="-1">Remove annotations and side chats, keep quotes and the selection overlay <a class="header-anchor" href="#remove-annotations-and-side-chats-keep-quotes-and-the-selection-overlay" aria-label="Permalink to &quot;Remove annotations and side chats, keep quotes and the selection overlay&quot;">​</a></h3><p>Rejected: the overlay, the quote recovery path, and the composer prefill are the largest single block of renderer-only code here, and quoting a whole message or a sentence has an exact substitute in the OS clipboard and the composer.</p><h3 id="turn-side-chats-into-an-ordinary-child-session-tab-instead-of-deleting-them" tabindex="-1">Turn side chats into an ordinary child-session tab instead of deleting them <a class="header-anchor" href="#turn-side-chats-into-an-ordinary-child-session-tab-instead-of-deleting-them" aria-label="Permalink to &quot;Turn side chats into an ordinary child-session tab instead of deleting them&quot;">​</a></h3><p>Rejected: that is Fork plus the existing session list; a new docked tab kind would keep the registration, projection, and cleanup lifecycle while adding a fourth way to reach the same durable child.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0268-remove-quotes-annotations-and-side-chats.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0268RemoveQuotesAnnotationsAndSideChats = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0268RemoveQuotesAnnotationsAndSideChats as default
};
