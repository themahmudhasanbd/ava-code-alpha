import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0303: A skill package carries resources far above the document cap","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0303-skill-package-resource-limits.md","filePath":"adr/0303-skill-package-resource-limits.md","lastUpdated":1790059889000}');
const _sfc_main = { name: "adr/0303-skill-package-resource-limits.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0303-a-skill-package-carries-resources-far-above-the-document-cap" tabindex="-1">ADR 0303: A skill package carries resources far above the document cap <a class="header-anchor" href="#adr-0303-a-skill-package-carries-resources-far-above-the-document-cap" aria-label="Permalink to &quot;ADR 0303: A skill package carries resources far above the document cap&quot;">​</a></h1><ul><li>Status: Accepted for implementation</li><li>Date: 2026-09-22</li><li>Amends: ADR 0300 (the &quot;bounded Skill package resources&quot; of the configuration sync baseline now have concrete values, separate from the document cap, and a receiver bound that matches the packager)</li><li>Related: <a href="./../spec/03-runtime/22-config-sync">03-runtime/22-config-sync</a> · <code>crates/host-core/src/user_skills.rs</code> · ADR 0243 (the document cap the market path enforces, unchanged)</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>A directory-shaped skill is a <code>SKILL.md</code> document plus sibling resources. One constant bounded both: <code>MAX_SKILL_BYTES</code> (128 KiB) was the document limit and, at the same time, the limit on one resource. The packager added a second pair — at most 64 resources, at most 512 KiB of resources per package — and the receiver kept a third copy of the per-resource number, refusing any resource above 128 KiB when a revision was read back.</p><p>The document limit exists for a prompt-sized reason: a document can be handed to the model. A resource is not prompt content. It is data or a script that stays on disk and is read on demand, so sharing the document&#39;s constant with it made the package bound an accident of the document bound.</p><p>The cost was not a graceful skip. <code>package_files</code> returns <code>Err</code> and the skills capture propagates it, so one oversized skill failed the whole capture and, with it, the sync of every domain. A real skill (a design-reference skill carrying 3.7 MiB of CSV and JSON in 76 files, largest file 808 KiB) reproduced exactly that: config sync reported <code>SKILL_LIMIT_EXCEEDED: skill package is too large</code> and stopped. Raising the packager bound alone would have been worse than the failure it fixed — the revision would upload and then fail on the other device, where the receiver still enforced 128 KiB.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>The document bound and the package bounds are separate:</p><ul><li><code>MAX_SKILL_BYTES</code> (128 KiB) remains the cap on a skill document.</li><li><code>MAX_SKILL_RESOURCE_BYTES</code> (2 MiB) is the new cap on one package resource.</li><li><code>MAX_SKILL_PACKAGE_FILES</code> is 256 and <code>MAX_SKILL_PACKAGE_BYTES</code> is 16 MiB.</li></ul><p>Capture and apply both compare a resource against <code>MAX_SKILL_RESOURCE_BYTES</code>, and <code>read_remote_revision</code> uses that same constant, so an upload the packager accepted is always readable back. Capture compares a resource against its metadata length before reading it, so an oversized file is refused without being loaded into memory first. Package paths, symlinks, collisions, the document cap, and the resource-per-object transport are unchanged.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A package may now be 32× larger than before (512 KiB → 16 MiB) and hold 4× more files. Sync volume, capture memory, and the number of WebDAV objects per revision grow with it; each resource is still one object, so no single request exceeds the per-resource cap.</li><li>Resources share the existing 4096-object ceiling for one revision across all domains; roughly sixteen maximum-size packages fill it, and the existing <code>CONFIG_SYNC_LIMIT_EXCEEDED</code> error is what a user sees beyond that.</li><li>A bound is still all-or-nothing. <code>package_files</code> returns an error that the skills capture propagates, so one resource above 2 MiB, more than 256 resources, or more than 16 MiB of resources in one package still fails the capture, and with it the sync of every domain. This record moves the bound; it does not make an oversized package skippable.</li><li>The descriptor list of a package travels in the entity payload, which keeps its own 512 KiB bound. A package with 256 resource paths long enough to push that payload over the bound is therefore refused at upload (<code>CONFIG_SYNC_LIMIT_EXCEEDED: local entity is too large or unsupported</code>) rather than at capture, which is a sharp edge of the wider file count.</li><li>No migration. The bounds are checked at capture and apply, never stored, so existing packages and revisions keep working; only previously refused packages start being accepted.</li><li>Prompt-facing behavior is unchanged: the editor counter, the market install check, and their copy still describe a 128 KiB document, and ADR 0243 stands.</li><li>Known gaps are not addressed here. Junk directories (<code>__pycache__</code>, <code>.venv</code>, <code>.git</code>) are still packaged and now consume the larger budget, and one symlink anywhere in a package still fails the whole capture rather than being skipped. A device running an older build still enforces 128 KiB per resource on read, so a package accepted here is not readable by it.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><ul><li><strong>Raise the single shared constant.</strong> Rejected: a 2 MiB <code>SKILL.md</code> is a prompt problem, not a package problem, and the market path inlines sibling resources into a document that must stay prompt-sized.</li><li><strong>Raise only the packager bounds.</strong> Rejected: it splits the packager from the receiver and converts a local refusal into a cross-device sync failure.</li><li><strong>Filter junk directories and skip symlinks instead of raising bounds.</strong> Rejected for this record: filtering changes which bytes a package carries, so it is a packaging-semantics decision on its own, not a bound to widen.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0303-skill-package-resource-limits.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0303SkillPackageResourceLimits = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0303SkillPackageResourceLimits as default
};
