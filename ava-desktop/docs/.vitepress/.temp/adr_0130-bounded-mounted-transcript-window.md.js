import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0130: Bounded Mounted Transcript Window","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0130-bounded-mounted-transcript-window.md","filePath":"adr/0130-bounded-mounted-transcript-window.md","lastUpdated":1788013690000}');
const _sfc_main = { name: "adr/0130-bounded-mounted-transcript-window.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0130-bounded-mounted-transcript-window" tabindex="-1">ADR 0130: Bounded Mounted Transcript Window <a class="header-anchor" href="#adr-0130-bounded-mounted-transcript-window" aria-label="Permalink to &quot;ADR 0130: Bounded Mounted Transcript Window&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-08-27</li><li>Deciders: PI-Desktop core</li><li>Related: D261, D269, E2E-159, ADR 0120, ADR 0127, D247, D258, D108, <code>04-ux/08-component-spec.md</code></li><li>Amended by: D269 (history continuation and boundary-driven escalation)</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>ADR 0120 bounded the transcript payload that crosses the IPC boundary, and ADR 0127 / D258 bounded the cost of locating a page inside the JSONL file. Both addressed session <em>activation</em>. Neither bounded what the renderer keeps mounted afterwards.</p><p>The renderer mounted every history row it had paged in and kept it mounted for the life of the session. Scrolling up through a long conversation therefore accumulated, without any ceiling:</p><ul><li>React element trees for every row,</li><li>a parsed Markdown AST per settled block,</li><li>Shiki <code>LineCache</code> token arrays per code fence,</li><li>the DOM nodes themselves.</li></ul><p><code>.message-row</code> and <code>.tool-activity-group</code> already carry <code>content-visibility: auto</code>, which skips layout and paint for far-offscreen rows. That is a CPU optimization and explicitly not a memory one: the retained JavaScript and DOM stay live. On a low-memory Windows machine — where the chat area was reported as getting progressively less responsive as a session grew — retained memory and the GC pressure behind it are the binding constraint.</p><p>A second cost was per-frame. <code>TranscriptHistory</code>&#39;s memo comparator walks every mounted entry, part, and activity item whenever its props array is not reference-equal, and that runs on every streamed token.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>The mounted history is a <strong>trailing window</strong> over the loaded history, not all of it. <code>reduceTranscriptWindow</code> resolves the slice for one render: <code>TRANSCRIPT_INITIAL_MOUNT</code> (15) rows for the first commit after a session switch, <code>TRANSCRIPT_WINDOW_MIN</code> (60) in steady state.</li><li>Reaching the top <strong>escalates in two stages</strong>. <code>growTranscriptWindow</code> adds <code>TRANSCRIPT_WINDOW_STEP</code> (40) rows while the window is partial; only once the window covers all loaded history does the transcript call <code>loadOlder</code> and fetch an older page. Mounting what is already loaded is strictly cheaper than an IPC round trip, so it goes first.</li><li>Window growth and a fetched page take the <strong>same pre-paint scroll anchor</strong>. Both add height above the reading position, so <code>prependHeightRef</code> is captured for both and corrected in the same layout effect.</li><li>The window <strong>resets per session</strong> and is clamped to the loaded history, so a grown budget inherited from the previous session for one frame cannot over-mount.</li><li>The hydration spacer stays <strong>scoped to the first commit</strong>. It exists to make the bounded bottom reachable; kept under the steady-state window it would put a blank viewport between the user and the growth trigger.</li><li>The conversation minimap&#39;s <strong>message dashes</strong> are built from the <strong>mounted entries</strong> (<code>transcriptEntryMessages</code>). It resolves a click by finding the marker&#39;s <code>data-minimap-id</code> node inside the scroller, so message markers for withheld rows would render dashes that jump nowhere. 6a. <strong>Amended by D269.</strong> Withheld history is represented explicitly instead of silently: while loaded rows sit above the window (<code>hiddenAbove &gt; 0</code>) or the host reports an older page (<code>hasMoreBefore</code>), the rail stays mounted and shows one dotted earlier-history continuation, even when the mounted tail has fewer than two message markers or does not overflow one viewport. The continuation runs the same two-stage escalation as clause 2, so it is always actionable; D108&#39;s two-marker-plus-overflow rule resumes once no earlier history remains. 6b. <strong>Amended by D269.</strong> Escalation is driven by the visibility of the transcript&#39;s top loading boundary as well as by the near-top scroll check, using one shared threshold. An underfilled tail page, a fetched page whose rows all fall outside the mounted window, and a window transition can each leave <code>scrollTop</code> unchanged; without an observed boundary those states stalled upward travel and left the outline hidden.</li><li>The entry projection arrays are memoized on <code>entries</code>, so a re-render that changed no message hands <code>TranscriptHistory</code> a reference-equal array and its comparator bails on identity instead of deep-walking the mounted rows.</li></ol><p>No IPC, storage, host protocol, or pagination change. <code>SESSION_TRANSCRIPT_PAGE_SIZE</code> and the <code>session.get</code> window contract are untouched.</p><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Rely on <code>content-visibility</code> alone:</strong> rejected. It was already in place and is what makes the <em>unbounded</em> case survivable at all, but it retains every row&#39;s React tree, Markdown AST, and token arrays. It cannot bound the resource the low-memory report is about.</li><li><strong>Full virtualization with measured row offsets:</strong> rejected for now. Transcript rows have highly variable, content-dependent heights that change after mount (streaming, disclosure toggles, lazy Mermaid), so a measured-offset virtualizer needs a height cache and invalidation for all of it. The trailing window gets the same memory ceiling with a fraction of the moving parts, and the existing pinned-follow and prepend-anchor logic keeps working unchanged.</li><li><strong>Unmount by distance from the viewport in both directions:</strong> rejected. The transcript is read from the bottom and streams at the bottom; withholding rows <em>below</em> the viewport would fight pinned-follow for no benefit.</li><li><strong>Cap the loaded history instead, discarding older pages from the store:</strong> rejected. It would make paging non-monotonic and re-fetch rows the user just scrolled past, and the store&#39;s transcript is also read by the outcome card and full-history rewrite paths.</li><li><strong>Reduce the per-frame comparator cost by making projections referentially stable:</strong> kept only in its cheap form (memoizing the arrays). Reconciling per-turn object identity across rebuilds was rejected as complexity that the bounded window already makes unnecessary — the comparator now walks at most the window, not the history.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Retained renderer memory for a transcript is bounded by the window rather than by how far back the user has scrolled, and per-frame reconciliation during streaming is bounded with it.</li><li>Upward travel through a long session costs one frame per growth step before pagination resumes. Each step is a mount of already-loaded rows, so it does not add IPC.</li><li>The browser&#39;s native find-in-page reaches only mounted rows. This is a real narrowing of an incidental capability; in-app search over the full transcript is unaffected because it does not depend on mounted DOM.</li><li>Newly mounted rows briefly use the <code>contain-intrinsic-size</code> fallback height before their real size is known, so the scrollbar thumb settles as they enter view. This is the pre-existing <code>content-visibility</code> behavior, unchanged in kind by windowing.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0130-bounded-mounted-transcript-window.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0130BoundedMountedTranscriptWindow = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0130BoundedMountedTranscriptWindow as default
};
