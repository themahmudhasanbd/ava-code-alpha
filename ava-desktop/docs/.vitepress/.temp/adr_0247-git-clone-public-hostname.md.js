import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0247: Git clone accepts only syntactically public hosts","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0247-git-clone-public-hostname.md","filePath":"adr/0247-git-clone-public-hostname.md","lastUpdated":1789366655000}');
const _sfc_main = { name: "adr/0247-git-clone-public-hostname.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0247-git-clone-accepts-only-syntactically-public-hosts" tabindex="-1">ADR 0247: Git clone accepts only syntactically public hosts <a class="header-anchor" href="#adr-0247-git-clone-accepts-only-syntactically-public-hosts" aria-label="Permalink to &quot;ADR 0247: Git clone accepts only syntactically public hosts&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-14</li><li>Deciders: PI-Desktop core</li><li>Related: ADR 0243, ADR 0245, D416</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Home &quot;Clone git project&quot; runs <code>git clone</code> with a user-supplied remote (<code>apps/desktop/src/lib/git-clone-url.ts</code>). The parser already rejected <code>file:</code> URLs and embedded passwords so a clone could not read arbitrary local paths or smuggle credentials in the URL.</p><p>It still accepted <code>http://127.0.0.1/...</code>, <code>git@localhost:...</code>, RFC1918, link-local, and IPv6 loopback literals. That is a different hole from the MCP and Skill market fetchers (ADR 0243 / 0245): those pin DNS onto a public HTTPS socket in Electron Main. <code>git clone</code> uses git&#39;s own HTTP and SSH stacks, so a Main-process DNS pin cannot be applied without replacing git.</p><p>A user-initiated clone of GitHub/GitLab over HTTPS or SSH remains a product requirement.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><code>parseGitCloneUrl</code> reuses <code>isPublicHostname</code> from <code>packages/shared/src/public-network.ts</code> for URL hosts and <code>git@host:path</code> hosts. Loopback, unspecified, private, CGNAT, link-local, multicast, reserved, documentation, ULA, site-local, <code>.localhost</code>, <code>.local</code>, and <code>.internal</code> names are rejected the same way market URL guards classify literals.</li><li>Protocols stay <code>https</code>, <code>http</code>, <code>ssh</code>, and <code>git</code>. SSH <code>git@github.com:org/repo.git</code> and <code>https://github.com/org/repo.git</code> remain valid. <code>file:</code> and URL passwords stay rejected.</li><li>Git still resolves DNS and opens sockets itself. This decision does <strong>not</strong> pin clone traffic. Hostname-based rebinding through a public DNS name is accepted residual risk for user-initiated git, documented rather than silently copied from the market HTTPS pin.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Accidental or malicious clones to loopback/LAN IP literals fail in the renderer before <code>git</code> runs.</li><li>Cloning a public git host by DNS name still works, including HTTP remotes.</li><li>Operators who clone from a private git host by literal IP must use an out-of-app git remote; that is an intentional reduction in convenience.</li><li>Residual risk: a public hostname can still resolve to a private address inside git.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Apply market DNS pin to git clone:</strong> rejected; git speaks SSH and its own HTTP, and replacing it is out of scope.</li><li><strong>HTTPS-only remotes:</strong> rejected; SSH <code>git@host:path</code> is the common GitHub path and is not an HTTP SSRF vector in Electron Main.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0247-git-clone-public-hostname.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0247GitClonePublicHostname = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0247GitClonePublicHostname as default
};
