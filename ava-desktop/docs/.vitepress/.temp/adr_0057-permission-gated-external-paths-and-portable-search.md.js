import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0057: Permission-gated external paths and portable native search","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0057-permission-gated-external-paths-and-portable-search.md","filePath":"adr/0057-permission-gated-external-paths-and-portable-search.md","lastUpdated":1788595620000}');
const _sfc_main = { name: "adr/0057-permission-gated-external-paths-and-portable-search.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0057-permission-gated-external-paths-and-portable-native-search" tabindex="-1">ADR 0057: Permission-gated external paths and portable native search <a class="header-anchor" href="#adr-0057-permission-gated-external-paths-and-portable-native-search" aria-label="Permalink to &quot;ADR 0057: Permission-gated external paths and portable native search&quot;">​</a></h1><ul><li>Status: Accepted for implementation (amended by D315)</li><li>Date: 2026-08-05</li><li>Baseline: <code>0.4.14</code></li><li>Protocol: v9</li><li>Storage schema: v10</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Read/search tools were classified as low risk before their path resolver ran. An absolute path outside the session workspace therefore bypassed the normal permission card and failed later as <code>PATH_OUTSIDE_WORKSPACE</code>. That made a legitimate inspection request look like a broken Plan turn and gave the agent an incentive to retry or replace the native tools with shell commands.</p><p>The host already had bounded, cross-platform Rust implementations for <code>Read</code>/<code>Glob</code>/<code>Grep</code>, but the sidecar schemas exposed only a subset of their scoping controls. Models could not reliably provide <code>path</code>, <code>include</code>, <code>outputMode</code>, <code>headLimit</code>, <code>offset</code>, <code>limit</code>, or <code>Glob.limit</code>; some providers also emitted a shell-style <code>files_with_matches</code> value that the host rejected.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><h3 id="_1-explicit-outside-paths-are-permission-gated" tabindex="-1">1. Explicit outside paths are permission-gated <a class="header-anchor" href="#_1-explicit-outside-paths-are-permission-gated" aria-label="Permalink to &quot;1. Explicit outside paths are permission-gated&quot;">​</a></h3><p>For <code>Read</code>, <code>Glob</code>, <code>Grep</code>, <code>Write</code>, and <code>Edit</code>, the host classifies an explicit path against the session workspace and scratch roots before applying the normal risk matrix:</p><ul><li>Auto executes the explicit outside path without a card.</li><li>Ask and Accept edits emit the existing inline permission request.</li><li>Allow once covers only the current call; Allow for session uses the existing tool-name grant scope.</li><li>Deny, timeout, and cancellation return <code>TOOL_DENIED</code> without execution.</li><li>Relative parent traversal and symlink escapes use the same rule as absolute paths.</li></ul><p>After approval, host-core resolves the path with the same canonicalized-ancestor logic used for contained paths. It never turns the external location into a new workspace root, and implicit Bash cwd or recursive walks do not gain this exception. Successful external results report <code>root: &quot;external&quot;</code> where a root field exists and keep absolute canonical paths visible to the model.</p><p>Plan&#39;s existing hard deny for Write/Edit/plugin/unknown tools remains above this path rule.</p><h3 id="_2-native-search-is-the-portable-default" tabindex="-1">2. Native search is the portable default <a class="header-anchor" href="#_2-native-search-is-the-portable-default" aria-label="Permalink to &quot;2. Native search is the portable default&quot;">​</a></h3><p>The runtime exposes the host&#39;s complete bounded search contract:</p><ul><li><code>Read</code>: <code>offset</code> and <code>limit</code>;</li><li><code>Glob</code>: <code>path</code> and <code>limit</code>;</li><li><code>Grep</code>: <code>path</code>, <code>include</code>, <code>outputMode</code>, <code>headLimit</code>, and <code>caseInsensitive</code>.</li></ul><p><code>outputMode</code> exposes exactly <code>content</code>, <code>filesWithMatches</code>, and <code>count</code> in the schema. The host also normalizes <code>files_with_matches</code> and <code>files-with-matches</code> as compatibility aliases before execution. Search guidance prefers workspace-relative paths and the native tools on every platform. Grep may exec a user-installed <code>rg</code> when one is on the process PATH or the Unix login PATH (D181 / D315). That is an implementation backend, not a shell search: stdin is null, arguments are not quoted through a shell, and the host still applies budgets, newest-first order, scoped ignore (<code>--no-ignore-parent</code> when <code>path</code> is explicit), and the same JSON shape. A missing, overridden-invalid, or failing <code>rg</code> (spawn error or exit 2) falls back to the in-process <code>ignore</code> + <code>regex</code> searcher. <code>PI_DESKTOP_RG</code> selects a binary; <code>PI_DESKTOP_DISABLE_RG</code> forces the fallback. Bash search remains a bounded last resort and still does not assume POSIX utilities, PowerShell, or <code>rg</code> availability.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A Plan inspection outside the project can wait for a clear user decision instead of failing at the resolver boundary.</li><li>Auto remains suitable for fully automated sessions, while Ask and Accept edits preserve an explicit consent boundary for external data and mutations.</li><li>Search requests can narrow scope and output before execution, reducing shell fallbacks, invalid output-mode retries, and context growth.</li><li>External reads and searches are visible through absolute paths; external mutations do not create workspace Review or artifact records.</li><li>The security denylist remains the default. This ADR changes the decision point for explicit paths, not the authority of host-core or the Bash cwd.</li></ul><h2 id="alternatives-rejected" tabindex="-1">Alternatives rejected <a class="header-anchor" href="#alternatives-rejected" aria-label="Permalink to &quot;Alternatives rejected&quot;">​</a></h2><h3 id="keep-returning-path-outside-workspace" tabindex="-1">Keep returning <code>PATH_OUTSIDE_WORKSPACE</code> <a class="header-anchor" href="#keep-returning-path-outside-workspace" aria-label="Permalink to &quot;Keep returning \`PATH_OUTSIDE_WORKSPACE\`&quot;">​</a></h3><p>Rejected because the agent cannot distinguish a user-denied request from a missing permission opportunity, and Plan loses a legitimate inspection path.</p><h3 id="treat-all-low-risk-reads-as-auto" tabindex="-1">Treat all low-risk reads as Auto <a class="header-anchor" href="#treat-all-low-risk-reads-as-auto" aria-label="Permalink to &quot;Treat all low-risk reads as Auto&quot;">​</a></h3><p>Rejected because a low-risk operation can still disclose data from an unrelated directory. Scope and user consent must be evaluated separately.</p><h3 id="use-shell-specific-search-commands" tabindex="-1">Use shell-specific search commands <a class="header-anchor" href="#use-shell-specific-search-commands" aria-label="Permalink to &quot;Use shell-specific search commands&quot;">​</a></h3><p>Rejected because command availability and quoting differ across macOS, Linux, and Windows, and unbounded shell output was a measured source of context exhaustion. Execing <code>rg</code> from Grep is not this alternative: the model still calls Grep, and host-core owns the result budget.</p><h2 id="related-docs" tabindex="-1">Related docs <a class="header-anchor" href="#related-docs" aria-label="Permalink to &quot;Related docs&quot;">​</a></h2><ul><li><code>docs/spec/03-runtime/03-tools-and-permissions.md</code></li><li><code>docs/spec/03-runtime/05-host-core-rust.md</code></li><li><code>docs/spec/03-runtime/06-host-rpc-protocol.md</code></li><li><code>docs/spec/03-runtime/08-error-codes.md</code></li><li><code>docs/spec/03-runtime/15-workspace-ignore-rules.md</code></li><li><code>docs/spec/03-runtime/16-tool-result-limits.md</code></li><li><code>docs/spec/04-ux/03-permission-ux.md</code></li><li><code>docs/spec/06-delivery/04-e2e-test-plan.md</code> (E2E-019/E2E-019e)</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0057-permission-gated-external-paths-and-portable-search.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0057PermissionGatedExternalPathsAndPortableSearch = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0057PermissionGatedExternalPathsAndPortableSearch as default
};
