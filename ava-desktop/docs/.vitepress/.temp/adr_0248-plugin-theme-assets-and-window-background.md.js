import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0248 — Package theme assets and contributed window backgrounds","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0248-plugin-theme-assets-and-window-background.md","filePath":"adr/0248-plugin-theme-assets-and-window-background.md","lastUpdated":1789371715000}');
const _sfc_main = { name: "adr/0248-plugin-theme-assets-and-window-background.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0248-—-package-theme-assets-and-contributed-window-backgrounds" tabindex="-1">ADR 0248 — Package theme assets and contributed window backgrounds <a class="header-anchor" href="#adr-0248-—-package-theme-assets-and-contributed-window-backgrounds" aria-label="Permalink to &quot;ADR 0248 — Package theme assets and contributed window backgrounds&quot;">​</a></h1><ul><li><strong>Status</strong>: Accepted for implementation</li><li><strong>Date</strong>: 2026-09-14</li><li><strong>Related</strong>: issue #334, issue #335, <code>07-plugins/04-plugin-security.md</code> §3.1, <code>07-plugins/02-plugin-manifest-schema.md</code> §4, <code>07-plugins/13-plugin-permissions-matrix.md</code></li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>A contributed theme (<code>ui.theme</code>) could only affect the renderer&#39;s CSS, and only within two walls that made real theming impossible:</p><ol><li><strong>References were <code>data:</code>-only.</strong> <code>sanitizeThemeCss</code> refuses every <code>url()</code> target that is not a <code>data:</code> URI, and the sheet itself is capped at 256 KB. Base64 inflates by about a third, so a photographic background is not expressible at all: the usable original is under ~190 KB.</li><li><strong>The native window background ignored plugin themes.</strong> The window is created with the host palette for the resolved light/dark base, so a plugin theme with a dark #0d1424 plate flashed — and kept — the host&#39;s <code>#181818</code>, while the renderer painted the theme&#39;s own colours inside it.</li></ol><p>Both walls were deliberate. The first existed because a relative or remote <code>url()</code> in plugin-authored CSS is a path the renderer would resolve on its own, with no host in the loop to check it. The second existed because only built-in theme ids had a background colour to apply.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Two opt-in additions, both inert when absent.</p><h3 id="_1-contributes-themes-assets-and-the-plugin-asset-scheme" tabindex="-1">1. <code>contributes.themes[].assets</code> and the <code>plugin-asset:</code> scheme <a class="header-anchor" href="#_1-contributes-themes-assets-and-the-plugin-asset-scheme" aria-label="Permalink to &quot;1. \`contributes.themes[].assets\` and the \`plugin-asset:\` scheme&quot;">​</a></h3><p>A theme may declare <code>assets: string[]</code> — package-relative paths whose extension is on a whitelist (<code>png</code>, <code>jpg</code>, <code>jpeg</code>, <code>webp</code>, <code>avif</code>, <code>svg</code>, <code>woff2</code>) and whose summed size is at most 4 MB. The declaration <strong>does not</strong> widen the sheet: validation still refuses the raw text of any reference, and instead rewrites each <code>url()</code> that matches a declared asset to <code>plugin-asset://&lt;pluginId&gt;/&lt;normalizedPath&gt;</code>. Everything else keeps the existing <code>data:</code>-only rule.</p><p>The scheme is host-owned:</p><ul><li>Reserved as privileged (<code>standard</code>, <code>secure</code>, <code>supportFetchAPI</code>, <code>corsEnabled</code>, <code>stream</code>) before the app is ready.</li><li>Handled by a main-process resolver that answers only from the runtime&#39;s per-plugin asset map — populated at theme registration from files that exist inside the plugin package, are not under <code>node_modules</code>, and fit the budget. A path nobody declared has no URL to begin with, and unloading a plugin clears its whole map.</li><li>Served with an explicit MIME type, <code>nosniff</code>, and <code>no-store</code>.</li><li>Admitted by the renderer CSP for <code>img-src</code> and <code>font-src</code>, and by the plugin panel egress policy as a local scheme (it is read-only and package-scoped, so it does not widen panel egress).</li></ul><p>Permission: unchanged. Reading a file the plugin already ships, through a host scheme that resolves only to that plugin&#39;s declared list, is the same capability <code>ui.theme</code> already grants.</p><h3 id="_2-contributes-windowappearance-backgroundcolor-light-dark" tabindex="-1">2. <code>contributes.windowAppearance.backgroundColor.{light,dark}</code> <a class="header-anchor" href="#_2-contributes-windowappearance-backgroundcolor-light-dark" aria-label="Permalink to &quot;2. \`contributes.windowAppearance.backgroundColor.{light,dark}\`&quot;">​</a></h3><p>A theme may name the native window background as <code>#rrggbb</code> or <code>#rrggbbaa</code>, one slot per resolved palette. It is applied only while one of that plugin&#39;s themes is the selected theme, and only for the palette the renderer has resolved — <code>system</code> resolves to light or dark first, exactly as <code>data-theme</code> does.</p><p>Restoration is <strong>derived, not remembered</strong>. The renderer recomputes the colour from the persisted preference and the live theme catalog on every theme, plugin, and OS-appearance change and sends the result (or nothing). A switch to another theme, a plugin disable, and a plugin uninstall all converge on the host palette because the plugin theme is simply gone from the catalog. There is no stored value to leak, and no exit path that can leave a half-applied colour behind — including a crashed renderer, which leaves the host default with no theme applied at all.</p><p>macOS keeps its <code>vibrancy</code> plate and never receives a colour, matching the existing rule that darwin does not download one.</p><p>Permission: new <code>ui.window.appearance</code>, confirmed at install and shown in the Settings permission list. A declaration without the grant is audited and ignored.</p><p>Built-in themes reach the same value through a shared table (<code>packages/shared/src/theme.ts</code>): <code>BUILTIN_THEMES</code> carries each palette&#39;s <code>windowBackground</code> once, and <code>isThemeColorScheme</code> carries the &quot;is this a palette rather than <code>system</code>/a plugin theme&quot; question once. The renderer, main, the plugin panel host, the panel preload, and the theme picker all read it instead of restating the <code>#ffffff</code> / <code>#181818</code> pair or re-listing the built-in ids. The two paths therefore differ only in where the colour is declared, not in how it is applied or restored. <code>system</code> stays resolved locally, because the renderer asks <code>matchMedia</code> and main asks <code>nativeTheme</code>.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>A theme can ship a photographic background or a webfont and can own the native plate behind its own palette, without loosening the sheet&#39;s own rules.</li><li>The renderer only ever sees a host scheme or a <code>data:</code> URI; a relative path still cannot reach the sheet.</li><li><code>contributes.windowAppearance</code> without <code>ui.window.appearance</code> is a manifest error in the SDK validator and in host-core, so the mismatch surfaces at install rather than at load.</li><li>The window is created before any theme is known, so the very first frame of a cold start still uses the host palette. It is replaced as soon as the renderer runs its theme effect, which is the same moment the base palette is applied today.</li><li>The asset handler reads each file synchronously. Within a 4 MB per-plugin budget this is bounded and avoids holding a stream open across a plugin unload, which is the failure mode that would matter more.</li><li>The built-in window palette has one definition. It previously lived in four files, so a palette change could leave one of them behind and split the built-in and contributed paths — the exact divergence this ADR exists to prevent.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><ul><li><strong>Raise the <code>data:</code> size cap.</strong> Keeps one channel, but a 4 MB sheet in a <code>&lt;style&gt;</code> element costs base64 inflation, a parse on every plugin change, and memory in the renderer, and it still cannot express a large image well.</li><li><strong>Allow relative <code>url()</code> and let the renderer resolve it.</strong> Hands path resolution back to plugin-authored content; that is precisely what the <code>data:</code>-only rule exists to prevent.</li><li><strong>Serve assets over <code>file://</code>.</strong> No allowlist, no MIME control, and every plugin would reach the whole filesystem the app can read.</li><li><strong>Remember and restore the native background on the main side.</strong> Requires persisting a derived value and re-deriving it on every exit path; deriving it from live state on each change has no state to restore.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0248-plugin-theme-assets-and-window-background.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0248PluginThemeAssetsAndWindowBackground = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0248PluginThemeAssetsAndWindowBackground as default
};
