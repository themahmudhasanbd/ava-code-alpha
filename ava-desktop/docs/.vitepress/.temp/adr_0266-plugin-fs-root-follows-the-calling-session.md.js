import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0266: Plugin fs Roots Follow the Calling Session","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0266-plugin-fs-root-follows-the-calling-session.md","filePath":"adr/0266-plugin-fs-root-follows-the-calling-session.md","lastUpdated":1789489321000}');
const _sfc_main = { name: "adr/0266-plugin-fs-root-follows-the-calling-session.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0266-plugin-fs-roots-follow-the-calling-session" tabindex="-1">ADR 0266: Plugin fs Roots Follow the Calling Session <a class="header-anchor" href="#adr-0266-plugin-fs-roots-follow-the-calling-session" aria-label="Permalink to &quot;ADR 0266: Plugin fs Roots Follow the Calling Session&quot;">​</a></h1><ul><li><strong>Status</strong>: Accepted</li><li><strong>Date</strong>: 2026-09-16</li><li><strong>Related</strong>: <a href="./0016-sidebar-organization-and-multi-project-tabs">ADR 0016</a> · <a href="./0249-chatgpt-style-logical-project-groups">ADR 0249</a> · <a href="./0263-project-folder-roots-for-plugin-views">ADR 0263</a> · <a href="./0264-host-mediated-actions-follow-the-browsed-folder">ADR 0264</a> · <a href="./../spec/07-plugins/03-plugin-api">07-plugins/03-plugin-api</a> · <a href="./../spec/07-plugins/13-plugin-permissions-matrix">07-plugins/13-plugin-permissions-matrix</a></li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>D093 gives the sidebar several retained project tabs while the shell keeps one selected host workspace, and it fixes the rule this ADR extends: <strong>tool execution resolves its root from the durable session project</strong>, so a background session keeps its own root when another tab becomes active. ADR 0016 and ADR 0249 carry that separation into the project model, and ADR 0263 / ADR 0264 make the folder a view browses plugin-local and per project.</p><p>That rule never reached a plugin. Every <code>pi.fs.*</code> request resolved its root as</p><div class="language-ts vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">rule.root </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">===</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;userSelected&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> ?</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> loaded.userRoot </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> this</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">.services.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">getWorkspacePath</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">()</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>and <code>getWorkspacePath()</code> is the single window-global visible workspace. Two sessions on two projects therefore measured every plugin fs call against whichever project the window happened to show: a plugin agent tool invoked from session B (project B) read project A&#39;s root as soon as the user switched tabs, and that same tool failed <code>NOT_FOUND</code> (&quot;No workspace is open&quot;) for every session at once when no workspace was visible at all (a temporary chat). The four path-safety gates -- permission, realpath containment, the deny-list, and the declared scope/consent -- were correct; only the directory they measured against was wrong.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><strong>The <code>workspace</code> root resolves the project of the tool session that invoked the call.</strong> A new private helper <code>fsRoot(loaded, rule)</code> in <code>plugin-runtime.ts</code> reads the in-flight tool&#39;s <code>sessionId</code>, asks the additive host service <code>getWorkspacePathForSession(sessionId)</code>, and uses the project it answers as the root. The visible workspace is a fallback only, not the primary answer.</li><li><strong>The session is the dimension the root follows; the fallback is the visible workspace.</strong> A panel-bridge call has no tool session, and a session the host does not track -- a runtime that has not launched yet -- resolves the visible workspace exactly as before.</li><li><strong>A <code>userSelected</code> mode is unchanged.</strong> It keeps the directory the user picked through <code>requestDirectory()</code>, with the same memory-only lifetime.</li><li><strong><code>plugin-services.ts</code> wires the service from the <code>sessionProjects</code> map it already keeps</strong> for D093. No new source of truth is introduced, and the plugin runtime caches nothing: each call reads the host&#39;s current answer.</li><li><strong>Not one path-safety gate changes.</strong> Permission, realpath containment, the deny-list, and the declared scope with runtime consent still run, in the same order, over the resolved root. Only <em>which</em> directory the <code>workspace</code> root names changes.</li><li><strong>A session root also resolves when no workspace is visible.</strong> A temporary chat has no visible workspace, so its plugin tools used to fail all at once; with a session project they keep working per session. A panel call in that state still fails closed, because it has nothing else to resolve.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Two sessions on two projects no longer cross-talk: a plugin agent tool reads and writes under the project of the session that invoked it, whatever tab is active.</li><li>Panel-bridge fs calls keep their old meaning: they follow the visible workspace.</li><li>A plugin that only ever ran one session on one project sees no change at all.</li><li><code>NOT_FOUND</code> for a <code>workspace</code> root is now narrower and more precise: neither the invoking session&#39;s project nor a visible workspace resolved.</li><li>The plugin runtime reads session metadata the host already owns, so the change adds no stored state, no migration, and no new plugin permission.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><h3 id="keep-the-visible-workspace-and-let-the-tool-pass-a-project-id" tabindex="-1">Keep the visible workspace and let the tool pass a project id <a class="header-anchor" href="#keep-the-visible-workspace-and-let-the-tool-pass-a-project-id" aria-label="Permalink to &quot;Keep the visible workspace and let the tool pass a project id&quot;">​</a></h3><p>Rejected: the host already knows the invoking session, so the plugin would have to learn and forward a project identifier it does not own, and a plugin-supplied root would be a grant its manifest never declared.</p><h3 id="resolve-the-session-s-project-inside-the-plugin-process" tabindex="-1">Resolve the session&#39;s project inside the plugin process <a class="header-anchor" href="#resolve-the-session-s-project-inside-the-plugin-process" aria-label="Permalink to &quot;Resolve the session&#39;s project inside the plugin process&quot;">​</a></h3><p>Rejected: the session-to-project map is host-owned session metadata (D093), and copying it into a sandboxed plugin process would move it outside the host authority that keeps it current.</p><h3 id="make-the-visible-workspace-follow-the-active-session" tabindex="-1">Make the visible workspace follow the active session <a class="header-anchor" href="#make-the-visible-workspace-follow-the-active-session" aria-label="Permalink to &quot;Make the visible workspace follow the active session&quot;">​</a></h3><p>Rejected: the visible workspace is what the user is looking at, and ADR 0016 with D093 deliberately keep several retained tabs over one selected host workspace. Changing what the user sees in order to fix a plugin lookup inverts the causality.</p><h3 id="fall-back-to-the-last-visible-workspace-for-a-session-the-host-does-not-track" tabindex="-1">Fall back to the last visible workspace for a session the host does not track <a class="header-anchor" href="#fall-back-to-the-last-visible-workspace-for-a-session-the-host-does-not-track" aria-label="Permalink to &quot;Fall back to the last visible workspace for a session the host does not track&quot;">​</a></h3><p>Rejected as no-op: an untracked session plus no visible workspace is exactly the temporary-chat case the session root exists to keep working, so the fallback stays &quot;the visible workspace, when there is one&quot;.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0266-plugin-fs-root-follows-the-calling-session.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0266PluginFsRootFollowsTheCallingSession = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0266PluginFsRootFollowsTheCallingSession as default
};
