import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"04. E2E Test Plan","description":"","frontmatter":{},"headers":[],"relativePath":"spec/06-delivery/04-e2e-test-plan.md","filePath":"spec/06-delivery/04-e2e-test-plan.md","lastUpdated":1790075952000}');
const _sfc_main = { name: "spec/06-delivery/04-e2e-test-plan.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="_04-e2e-test-plan" tabindex="-1">04. E2E Test Plan <a class="header-anchor" href="#_04-e2e-test-plan" aria-label="Permalink to &quot;04. E2E Test Plan&quot;">​</a></h1><blockquote><p>Scope: MVP acceptance scenarios plus current shipped product increments for PI-Desktop Status: Accepted (protocol/Electron automation is active; full desktop Playwright remains planned) Cross-references: <a href="./02-acceptance-criteria">acceptance-criteria</a> · <a href="./01-mvp-milestones">milestones</a> · <a href="./03-ai-development-workflow">ai-development-workflow</a> · <a href="./05-change-checklist">change-checklist</a></p></blockquote><hr><h2 id="_1-goals" tabindex="-1">1. Goals <a class="header-anchor" href="#_1-goals" aria-label="Permalink to &quot;1. Goals&quot;">​</a></h2><h3 id="e2e-images-desktop-conversation" tabindex="-1">E2E-IMAGES-desktop-conversation <a class="header-anchor" href="#e2e-images-desktop-conversation" aria-label="Permalink to &quot;E2E-IMAGES-desktop-conversation&quot;">​</a></h3><ul><li><strong>Preconditions:</strong> Isolated desktop profile and workspace, built image feature, local chat and OpenAI Images HTTP fixtures; no live provider credentials.</li><li><strong>Steps:</strong> Open Models settings with no image model configured; verify the image summary row is absent. Open a provider&#39;s Advanced model settings and verify <strong>Set as image model</strong> is grouped with the image/document attachment capabilities and can be checked for multiple models. Save, use the image summary menu to choose one marked model as the default, then submit a two-image request through the composer and edit the first output through a follow-up message. Collapse tool details.</li><li><strong>Expected:</strong> After saving, the image summary appears with a 12px default-row gap and lists all marked candidates, the selected default is changeable without changing the chat default, decoded image previews stay outside collapsed process details, multipart source upload is used for editing, originals are preserved, and no image HTTP request occurs while unconfigured.</li><li><strong>Settings interactions:</strong> The unconfigured state renders no image summary. Mark candidates in the attachment capability group, save, and select one from the summary menu. An existing missing/disabled candidate shows only Currently unavailable. Covered in <code>scripts/e2e-image-generation-ui.mjs</code>.</li><li><strong>Image model deselection (#826):</strong> With one provider and one model marked for images, uncheck it in the provider editor. Cancel must preserve the image default; Save must clear both its candidate and default binding. Reopen settings and the editor: the image row stays hidden, the checkbox stays unchecked, and the model can be selected as the chat default. Covered in English and Chinese by <code>scripts/e2e-image-generation-ui.mjs</code> with an API-boundary fixture.</li><li><strong>Conversation selection:</strong> Every marked image provider/model is absent from default and Composer candidates. Other providers retain same-ID models. An existing session pinned to any marked image binding is rejected before inference.</li><li><strong>Transport contracts:</strong> Real stdio reverse RPC retains a thrown local image error&#39;s stable code in the production ParentHostProxy. Local HTTP tests check single/multiple binary multipart fields and boundaries, DALL-E <code>b64_json</code> requests, GPT Image parameter omission, bounded responses, and rejection of more than four references before I/O. These are protocol tests, not official provider account/live compatibility certification.</li><li><strong>Status:</strong> Automated in <code>node scripts/e2e-image-chat.mjs</code>; optional screenshots use <code>PI_IMAGE_CHAT_EVIDENCE_DIR</code>. The images are deterministic raster fixtures, not evidence of real-model quality or provider compatibility.</li></ul><h3 id="e2e-scheduled-desktop-automation-lifecycle" tabindex="-1">E2E-SCHEDULED-desktop-automation-lifecycle <a class="header-anchor" href="#e2e-scheduled-desktop-automation-lifecycle" aria-label="Permalink to &quot;E2E-SCHEDULED-desktop-automation-lifecycle&quot;">​</a></h3><ul><li><strong>Preconditions:</strong> Isolated desktop profile, built task candidate, local SSE fixture model; no real provider credentials or paid API.</li><li><strong>Steps:</strong> Open the footer clock; create a daily Morning task at 09:00; edit its name; select a daily time period from four fixed defaults; open the weekday menu, select custom days, save and reopen; reject empty days; verify the four defaults, arrows, Home/End, Enter, Escape/Tab and outside dismissal; select hourly without time fields and verify its first occurrence is one hour away; pause/resume; Run now; open the result conversation; configure a daily task for the next real minute; observe automatic completion; delete the settled task. In a normal Agent conversation, use model tool calls to discover, create, list, update to 15:30 and delete a task. Verify the custom time appears in the form and survives renaming. The model is a local deterministic fixture, not a live provider.</li><li><strong>Expected:</strong> Configuration persists, next time is visible, paused tasks do not dispatch, both execution paths reach the real Agent sidecar, history links to the persisted transcript, and automatic execution does not require a renderer prompt. Host tests additionally prove duplicate admission rejection, stale/missed occurrence handling, invalid input rejection and recovery.</li><li><strong>Specs:</strong> 04-ux/01-ui-ia §3.4; 03-runtime/04-data-storage §4.11; ADR scheduled-desktop-automations.</li><li><strong>Acceptance:</strong> Scheduled task execution and recoverable run history.</li><li><strong>Milestone:</strong> Post-MVP desktop automations.</li><li><strong>Status:</strong> Automated in <code>node scripts/e2e-scheduled.mjs</code>; run against the request candidate per AGENTS.md, with tested head and base recorded.</li></ul><h3 id="e2e-plugin-appearance-extension-lifecycle" tabindex="-1">E2E-PLUGIN-appearance-extension-lifecycle <a class="header-anchor" href="#e2e-plugin-appearance-extension-lifecycle" aria-label="Permalink to &quot;E2E-PLUGIN-appearance-extension-lifecycle&quot;">​</a></h3><ul><li><p><strong>Preconditions:</strong> A fixture plugin declares <code>ui.theme</code>, <code>ui.settings</code>, one asset-backed scenic card with a typed <code>--nexus-backdrop-blur</code> <code>0..20px</code> variable.</p></li><li><p><strong>Steps:</strong> Open Settings, find the final Extensions group by search and keyboard navigation, open the entry, change the variable, select/reselect the theme, restart, then disable, reload and uninstall the plugin while its entry is active.</p></li><li><p><strong>Expected:</strong> The Extensions rail draws the destination&#39;s host icon token (<code>palette</code> / Lucide Palette), not a Skills book glyph and not plugin SVG. Only the declared variable rule changes after Apply; static <code>plugin-asset://</code> URLs remain valid; the value restores after restart; host- rendered cards retain the scenic backdrop behind their translucent content, have no opaque native rectangle, and leave Windows/Linux minimize/maximize controls clickable while Settings is open. Invalid/cross-plugin values are refused. Every lifecycle transition removes the entry and returns the app to General.</p></li><li><p><strong>Status:</strong> Documented; run after integration into main.</p></li><li><p>Document every user-visible and protocol-visible behavior that MVP must verify.</p></li><li><p>Provide a scenario catalog that maps to acceptance criteria (A–H) and milestones (M1–M6).</p></li><li><p>Serve as the traceability backbone: scenario ID ↔ acceptance criterion ↔ spec.</p></li><li><p>Define the required E2E gate for code-bearing task candidates containing the latest <code>origin/main</code>, before the pull request; do not merge into local <code>main</code>.</p></li><li><p>Keep validation evidence tied to the commit the gate ran on, plus any later commit that changes the landed executable content.</p></li></ul><h2 id="_2-non-goals" tabindex="-1">2. Non-goals <a class="header-anchor" href="#_2-non-goals" aria-label="Permalink to &quot;2. Non-goals&quot;">​</a></h2><ul><li>Full UI-driven automated coverage; protocol and source-contract automation is active while the broader desktop suite remains planned.</li><li>General performance / stress testing (post-MVP); bounded regression checks for desktop responsiveness are covered by the relevant functional scenarios.</li><li>Native Windows/Linux release qualification (published artifacts exist; native qualification gaps remain documented).</li><li>Hostile-plugin sandbox scenarios. Publisher provenance and the marketplace download boundary are now in scope (E2E-024R through E2E-024V); basic browse/install/update flows were already part of the catalog.</li><li>Remote Gateway / browser control remains out of scope (ADR 0004 / baseline #20); the bounded local loopback MCP control plane is covered by E2E-220.</li></ul><hr><h2 id="_3-test-pyramid" tabindex="-1">3. Test Pyramid <a class="header-anchor" href="#_3-test-pyramid" aria-label="Permalink to &quot;3. Test Pyramid&quot;">​</a></h2><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>        ╱  E2E  ╲           — few, high-value, cross-system</span></span>
<span class="line"><span>       ╱ Integration ╲      — IPC/RPC contracts, host↔renderer</span></span>
<span class="line"><span>      ╱    Unit       ╲     — per-module, fast, isolated</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><table tabindex="0"><thead><tr><th>Level</th><th>Scope</th><th>Count target</th><th>Tooling</th></tr></thead><tbody><tr><td><strong>Unit</strong></td><td>Single module, no IPC</td><td>Many</td><td>Vitest / Rust #[test]</td></tr><tr><td><strong>Integration</strong></td><td>IPC contract, host↔renderer, host↔sidecar</td><td>Moderate</td><td>Vitest + IPC mocks or live Electron</td></tr><tr><td><strong>E2E</strong></td><td>Full user journey through the desktop app</td><td>100+ functional + US-UI visual catalog</td><td>protocol smoke + Electron probes now; Playwright later</td></tr></tbody></table><p><strong>Strategy</strong>: document all E2E scenarios; add or update unit/integration tests alongside code when change risk makes them necessary; use selective, high-value E2E suites for every code-bearing pull request. Lower-level tests localize correctness, while E2E validates cross-process runtime behavior; passing lower levels does not waive the relevant E2E gate.</p><hr><h2 id="_4-tooling-intent" tabindex="-1">4. Tooling Intent <a class="header-anchor" href="#_4-tooling-intent" aria-label="Permalink to &quot;4. Tooling Intent&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Tool</th><th>Purpose</th><th>Status</th></tr></thead><tbody><tr><td><strong>Vitest</strong></td><td>Unit + integration (TS side)</td><td>Active (<code>pnpm test</code>, shared package)</td></tr><tr><td><strong>Rust #[test]</strong></td><td>Host-core unit tests</td><td>Active (<code>cargo test -p host-core</code>)</td></tr><tr><td><strong>Protocol smoke</strong></td><td>Host RPC + tools + plugins headless</td><td>Active (<code>test:e2e</code>, 22 checks)</td></tr><tr><td><strong>Electron probes</strong></td><td>Boot bridge, session-list responsiveness, and crash supervision</td><td>Active (<code>test:e2e:boot</code>, <code>test:e2e:supervision</code>)</td></tr><tr><td><strong>Playwright</strong></td><td>Full UI-driven journeys</td><td>Planned (post-M5)</td></tr></tbody></table><blockquote><p>Decision: protocol smoke and Electron probes are active validation assets; broader desktop journeys remain scenario-specific and must be recorded as environment-limited when their required platform is unavailable.</p></blockquote><hr><h2 id="_5-environment-requirements" tabindex="-1">5. Environment Requirements <a class="header-anchor" href="#_5-environment-requirements" aria-label="Permalink to &quot;5. Environment Requirements&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Requirement</th><th>Detail</th></tr></thead><tbody><tr><td>Platform</td><td>macOS arm64 and Intel x64, Windows x64, and Linux x64 release targets (D126/D285)</td></tr><tr><td>Profile</td><td>Clean <code>~/.pi-desktop</code> profile (no prior config)</td></tr><tr><td>Fixtures</td><td>Sample project directory (<code>examples/fixtures/sample-project/</code>)</td></tr><tr><td>Sample plugin</td><td><code>examples/plugins/hello</code> loaded from local path</td></tr><tr><td>Provider</td><td>At least one provider with a valid key (test account)</td></tr><tr><td>Display</td><td>Headless-capable Electron or real display</td></tr></tbody></table><hr><h2 id="_6-scenario-template" tabindex="-1">6. Scenario Template <a class="header-anchor" href="#_6-scenario-template" aria-label="Permalink to &quot;6. Scenario Template&quot;">​</a></h2><p>Each scenario is documented in this format:</p><div class="language-markdown vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">markdown</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-light-font-weight": "bold", "--shiki-dark": "#79B8FF", "--shiki-dark-font-weight": "bold" })}">### E2E-&lt;ID&gt;: &lt;title&gt;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">-</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-light-font-weight": "bold", "--shiki-dark": "#E1E4E8", "--shiki-dark-font-weight": "bold" })}"> **Preconditions**</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: what must be true before steps start</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">-</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-light-font-weight": "bold", "--shiki-dark": "#E1E4E8", "--shiki-dark-font-weight": "bold" })}"> **Steps**</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: ordered list of user / system actions</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">-</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-light-font-weight": "bold", "--shiki-dark": "#E1E4E8", "--shiki-dark-font-weight": "bold" })}"> **Expected**</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: observable outcome that proves correctness</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">-</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-light-font-weight": "bold", "--shiki-dark": "#E1E4E8", "--shiki-dark-font-weight": "bold" })}"> **Specs linked**</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: relevant spec file(s)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">-</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-light-font-weight": "bold", "--shiki-dark": "#E1E4E8", "--shiki-dark-font-weight": "bold" })}"> **Acceptance criterion**</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: which A–H letter(s) this verifies</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">-</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-light-font-weight": "bold", "--shiki-dark": "#E1E4E8", "--shiki-dark-font-weight": "bold" })}"> **Milestone**</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: M1–M6 target</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">-</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-light-font-weight": "bold", "--shiki-dark": "#E1E4E8", "--shiki-dark-font-weight": "bold" })}"> **Status**</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: Draft | Documented | Partially automated | Automated | Passed</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br></div></div><hr><h2 id="e2e-main-integration-validation" tabindex="-1">E2E Main Integration Validation <a class="header-anchor" href="#e2e-main-integration-validation" aria-label="Permalink to &quot;E2E Main Integration Validation&quot;">​</a></h2><p>Every code-bearing change must pass the E2E suites relevant to its regression surface on a candidate that already contains the latest <code>origin/main</code>, before the request branch is pushed and the pull request is opened. Code-bearing changes include Renderer, Electron Main, Preload, Agent Runtime, Rust host-core, sessions, transcripts, plans, plugins, MCP, permissions, provider/model runtime, persistence, process lifecycle, packaging/runtime startup, and build or CI behavior that affects application execution. Documentation-only changes are exempt when they do not alter executable behavior.</p><p>Run the selected suites from the request worktree after <code>pnpm check:pr-base</code> passes. An E2E run on a branch that is behind <code>origin/main</code> is exploratory and does not satisfy this gate. Do not merge the task into local <code>main</code> to create the candidate.</p><h3 id="e2e-environment-reuse" tabindex="-1">E2E environment reuse <a class="header-anchor" href="#e2e-environment-reuse" aria-label="Permalink to &quot;E2E environment reuse&quot;">​</a></h3><p>Task-candidate E2E runs from the request worktree after the candidate is prepared, but uses the host development environment already provisioned in the primary checkout. Reuse the compatible Node/pnpm toolchain, <code>node_modules</code>, Electron, Rust/Cargo targets, package-manager stores, build caches, and ignored configuration by reference or link when needed.</p><p>Do not run <code>pnpm install</code>, <code>npm install</code>, or create a second dependency/runtime environment solely for E2E. Isolate only mutable test state: temporary profiles, data directories, sockets, ports, logs, and artifacts. Install or rebuild dependencies only when the host environment is missing or incompatible and record the reason. Clean CI and release runners may install from lockfiles.</p><p>Use the root <code>package.json</code> as the source of truth for executable commands. The minimum selection is:</p><ul><li>Cross-cutting runtime, host, or IPC: <code>pnpm test:e2e</code>.</li><li>Electron startup, preload, or window lifecycle: <code>pnpm test:e2e</code> and <code>pnpm test:e2e:boot</code>.</li><li>Session-list refresh or model capability lookup: <code>pnpm test:e2e</code> and <code>pnpm test:e2e:boot</code>, including the synthetic large-list responsiveness check.</li><li>Settings/composer/plugin search theme surfaces: <code>pnpm build:js</code> followed by <code>pnpm test:e2e:theme-surfaces</code>.</li><li>Composer clipboard representation and text insertion: <code>pnpm test:e2e:composer-paste</code>.</li><li>Composer slash-menu name/description layout: <code>pnpm build:js</code> followed by <code>pnpm test:e2e:composer-autocomplete</code>.</li><li>Transcript render boundaries and cross-part delegation display: <code>pnpm test:e2e:transcript</code>.</li><li>Plan host/runtime behavior: <code>pnpm test:e2e</code> and <code>pnpm test:e2e:plan</code>.</li><li>Plan UI behavior: <code>pnpm test:e2e:plan</code> and <code>pnpm test:e2e:plan-ui</code>.</li><li>Host supervision, crash recovery, or restart behavior: <code>pnpm test:e2e</code> and <code>pnpm test:e2e:supervision</code>.</li><li>Subagent lifecycle: <code>pnpm test:e2e</code> and <code>pnpm test:e2e:subagents</code>.</li><li>Imported-extension dependency installation or registry-boundary changes: <code>pnpm test:e2e:plugin-import-deps</code>.</li><li>Trusted extension or plugin-extension changes: <code>pnpm test:e2e:trusted-extensions</code>.</li><li>Session collaboration / Session Orchestrator: <code>pnpm test:e2e:collaboration</code>.</li><li>Completion-notice silence or the silent-turn contract (D193 / D446): <code>pnpm test:e2e:session-completion</code>.</li><li>Changes spanning multiple surfaces use the union of the applicable suites.</li></ul><p><code>pnpm test:e2e</code> is the default cross-system smoke suite for host RPC, IPC, agent execution, plugins, persistence integration, and shared runtime contracts. A required suite that cannot run because of a missing display, platform, credential, hardware resource, or other environment capability must be recorded as <code>NOT RUN</code> with its reason, alternative validation, and remaining risk. The pull request may still be opened with that record so the change can be validated in a capable environment, but the gate is not satisfied and delivery remains incomplete until the suite passes against the integrated <code>main</code> that carries the change.</p><p>Required results must apply to the executable commit the gate ran on, and any later commit that changes the landed executable content requires a rerun of the affected suites (landing fixes, conflict resolution, or commits added during review). Otherwise the recorded result stands. Report each command, result, tested commit, and any relevant environment limitation; never claim an unexecuted suite passed.</p><h2 id="e2e-failure-policy" tabindex="-1">E2E Failure Policy <a class="header-anchor" href="#e2e-failure-policy" aria-label="Permalink to &quot;E2E Failure Policy&quot;">​</a></h2><p>A failed required E2E blocks the branch push, the pull request, and declaring the integrated delivery complete until the failure is classified as an implementation regression, test regression, environment failure, or known flaky infrastructure. Fix the product or test defect and rerun the affected suite against the integrated <code>main</code>. Do not delete scenarios, weaken assertions, or add retries that hide a deterministic failure. When a scenario is not automated on the required platform, keep its status documented and identify the platform validation still needed.</p><h2 id="_7-mvp-scenario-catalog" tabindex="-1">7. MVP Scenario Catalog <a class="header-anchor" href="#_7-mvp-scenario-catalog" aria-label="Permalink to &quot;7. MVP Scenario Catalog&quot;">​</a></h2><h3 id="runtime-resource-governance" tabindex="-1">Runtime Resource Governance <a class="header-anchor" href="#runtime-resource-governance" aria-label="Permalink to &quot;Runtime Resource Governance&quot;">​</a></h3><h4 id="e2e-097-tool-burst-is-bounded-and-recovers-after-host-restart" tabindex="-1">E2E-097: Tool burst is bounded and recovers after host restart <a class="header-anchor" href="#e2e-097-tool-burst-is-bounded-and-recovers-after-host-restart" aria-label="Permalink to &quot;E2E-097: Tool burst is bounded and recovers after host restart&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Host-core is healthy; one session has a workspace; the supervision probe can terminate the host process.</li><li><strong>Steps</strong>: 1) Dispatch a burst larger than the host tool budget containing read tools and shell commands. 2) Observe <code>app.health</code> while the burst runs. 3) Terminate host-core during active calls. 4) Wait for one supervised restart. 5) Allow the persistence outbox to flush.</li><li><strong>Expected</strong>: Active shell processes never exceed the configured global and per-session limits. Excess work returns <code>HOST_OVERLOADED</code> or waits in the bounded queue. Only one restart loop runs; stale-generation calls fail fast as <code>HOST_UNAVAILABLE</code>; no repeated <code>ERR_STREAM_DESTROYED</code> persistence storm is emitted. Temporary OS thread pressure during the same burst does not terminate host-core through its stdio control path; the host remains on one generation and capacity errors stay structured. Completed assistant/tool messages are persisted once after recovery.</li><li><strong>Specs linked</strong>: <code>03-runtime/06-host-rpc-protocol.md</code>, <code>03-runtime/07-process-model.md</code>, <code>03-runtime/08-error-codes.md</code>, <code>03-runtime/09-logging-and-observability.md</code>, ADR 0051</li><li><strong>Acceptance</strong>: A (runtime health), C (tool execution and recovery)</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Documented; automation pending</li></ul><h3 id="release-packaging" tabindex="-1">Release &amp; Packaging <a class="header-anchor" href="#release-packaging" aria-label="Permalink to &quot;Release &amp; Packaging&quot;">​</a></h3><h4 id="e2e-192-linux-release-publishes-a-system-electron-asar-asset" tabindex="-1">E2E-192: Linux release publishes a system-Electron ASAR asset <a class="header-anchor" href="#e2e-192-linux-release-publishes-a-system-electron-asar-asset" aria-label="Permalink to &quot;E2E-192: Linux release publishes a system-Electron ASAR asset&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A <code>vX.Y.Z</code> tag matches <code>apps/desktop/package.json</code>; the Linux x64 release runner can complete <code>dist:linux</code> and has a system Electron available for repackaging validation.</li><li><strong>Steps</strong>: 1) Run the tag release workflow. 2) Inspect the published GitHub Release assets. 3) Confirm the versioned <code>PI-Desktop-X.Y.Z-linux-x64.asar</code> asset is present. 4) Place that archive in the target Electron resources layout with the target package&#39;s native host and other resources, then launch it with <code>electron &lt;archive&gt;.asar</code>.</li><li><strong>Expected</strong>: The ASAR is copied byte-for-byte from <code>linux-unpacked/resources/app.asar</code>, is uploaded alongside the Linux AppImage, deb, and rpm, and the system Electron opens the PI-Desktop application archive without requiring the bundled Electron executable.</li><li><strong>Specs linked</strong>: <code>06-delivery/06-release-runbook.md</code>, <code>03-runtime/07-process-model.md</code></li><li><strong>Acceptance</strong>: Quality (release artifact and packaging compatibility)</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Documented; artifact export is unit-covered, native system-Electron repackaging remains runner validation</li></ul><h4 id="e2e-200-linux-rpm-preserves-the-wayland-desktop-identity" tabindex="-1">E2E-200: Linux RPM preserves the Wayland desktop identity <a class="header-anchor" href="#e2e-200-linux-rpm-preserves-the-wayland-desktop-identity" aria-label="Permalink to &quot;E2E-200: Linux RPM preserves the Wayland desktop identity&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A Linux x64 package validation run or tag release can complete on Ubuntu 22.04; a clean Fedora 44 KDE/Wayland machine is available for installation and launch.</li><li><strong>Steps</strong>: 1) Build the Linux targets and inspect the RPM with <code>rpm -qip</code> and <code>rpm -qpl</code>. 2) Confirm the package contains the application archive, host-core, <code>pi-desktop.desktop</code>, and the 512px <code>pi-desktop</code> icon. 3) Confirm the RPM has no global <code>/usr/lib/.build-id</code> links. 4) Install the RPM on the Fedora KDE/Wayland machine and launch PI-Desktop from its desktop entry. 5) Inspect the taskbar grouping and the installed desktop entry.</li><li><strong>Expected</strong>: The x64 RPM is produced with the documented name and uploads with the release artifacts. Its desktop entry contains <code>Icon=pi-desktop</code> and <code>StartupWMClass=pi-desktop</code>; the running Wayland window groups with the PI-Desktop launcher and shows its icon instead of a generic Electron icon. The package remains notify-and-link for updates, and the bundled Electron binaries do not create global build-id links.</li><li><strong>Specs linked</strong>: <code>01-product/01-product-scope.md</code>, <code>04-ux/09-interaction-patterns.md</code>, <code>06-delivery/06-release-runbook.md</code></li><li><strong>Acceptance</strong>: Quality (release packaging and desktop integration)</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Unit/source-contract covered (<code>auto-update.test.mjs</code>, <code>development-branding.test.mjs</code>, <code>ci-workflow.test.mjs</code>); Fedora KDE/Wayland installation remains runner validation</li></ul><h4 id="e2e-196a-unsigned-macos-debug-lane" tabindex="-1">E2E-196a: Unsigned macOS debug lane <a class="header-anchor" href="#e2e-196a-unsigned-macos-debug-lane" aria-label="Permalink to &quot;E2E-196a: Unsigned macOS debug lane&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: The Release workflow is manually dispatched with <code>sign_macos: false</code>; Windows and Linux release credentials are not affected. This path must not be used to publish a GitHub Release tag.</li><li><strong>Steps</strong>: 1) Dispatch the Release workflow with <code>sign_macos: false</code>. 2) Confirm both macOS architectures complete ordinary DMG/ZIP packaging without certificate secrets. 3) Inspect the artifacts and workflow steps.</li><li><strong>Expected</strong>: macOS DMG/ZIP artifacts are produced and uploaded without Developer ID signatures or notarization, using explicit <code>-arm64</code> and <code>-x64</code> filename markers for their native architecture; macOS staple and Gatekeeper checks are explicitly skipped. Windows/Linux artifacts and the merged updater feed still publish normally. This exception does not satisfy E2E-196c.</li><li><strong>Specs linked</strong>: <code>06-delivery/06-release-runbook.md</code></li><li><strong>Acceptance</strong>: Quality (debug packaging)</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Opt-in debug lane; tag releases must satisfy E2E-196c.</li></ul><h4 id="e2e-196b-unsigned-macos-packages-expose-first-launch-guidance" tabindex="-1">E2E-196b: Unsigned macOS packages expose first-launch guidance <a class="header-anchor" href="#e2e-196b-unsigned-macos-packages-expose-first-launch-guidance" aria-label="Permalink to &quot;E2E-196b: Unsigned macOS packages expose first-launch guidance&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A default unsigned macOS release has produced both DMG and ZIP artifacts for at least one native architecture; a test macOS account can copy an app into <code>/Applications</code> or <code>~/Applications</code>.</li><li><strong>Steps</strong>: 1) Open the DMG and inspect its root and layout. 2) Confirm the app and Applications link are the only items in the window. 3) Confirm the DMG has no command helper and no <code>If app won&#39;t open, read this.txt</code>. 4) Inspect the ZIP root without extracting the application contents and confirm it has both <code>PI-Desktop-macOS-opening-help.txt</code> and the executable <code>PI-Desktop-macOS-open.command</code>. 5) Read the note, move the app to <code>/Applications</code>, and double-click the ZIP helper.</li><li><strong>Expected</strong>: The DMG contains the branded 720×440 background, the app, and the Applications link only; it does not contain or expose the command helper or the opening-help note. The ZIP contains the helper and the opening note at its root. The note includes <code>xattr -r -d com.apple.quarantine /Applications/PI-Desktop.app</code>, explains that the fallback is only for a trusted unsigned artifact when macOS reports that the app is damaged or does not open, and says signed/notarized builds do not need it. The ZIP helper searches only <code>/Applications/PI-Desktop.app</code> and <code>~/Applications/PI-Desktop.app</code>, removes only <code>com.apple.quarantine</code> when present, and opens the app without <code>sudo</code> or an arbitrary path argument. It validates <code>CFBundleIdentifier=net.aiuo.pi-desktop</code> before changing attributes. The guidance does not claim that an unsigned artifact has passed Gatekeeper qualification.</li><li><strong>Specs linked</strong>: <code>06-delivery/06-release-runbook.md</code>, <code>05-security/01-security.md</code></li><li><strong>Acceptance</strong>: Quality, Security</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Automated by <code>packaging-footprint.test.mjs</code>; native archive inspection remains release-runner validation</li></ul><h4 id="e2e-196c-macos-tag-artifacts-pass-gatekeeper-without-a-quarantine-bypass" tabindex="-1">E2E-196c: macOS tag artifacts pass Gatekeeper without a quarantine bypass <a class="header-anchor" href="#e2e-196c-macos-tag-artifacts-pass-gatekeeper-without-a-quarantine-bypass" aria-label="Permalink to &quot;E2E-196c: macOS tag artifacts pass Gatekeeper without a quarantine bypass&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A <code>vX.Y.Z</code> tag matching <code>apps/desktop/package.json</code> is pushed, or the Release workflow is dispatched with <code>sign_macos: true</code> (default); GitHub Actions has <code>CSC_LINK</code>, <code>CSC_KEY_PASSWORD</code>, <code>APPLE_ID</code>, <code>APPLE_APP_SPECIFIC_PASSWORD</code>, and <code>APPLE_TEAM_ID</code> secrets; both native macOS runners are available.</li><li><strong>Steps</strong>: 1) Run the tag workflow. 2) For each macOS architecture, inspect the unpacked app with <code>codesign -dv --verbose=4</code> and confirm authority <code>Developer ID Application: XingYu Liu (DUV63RKYTW)</code>. 3) Run <code>codesign --verify --deep --strict --verbose=2</code>, <code>spctl --assess --type execute --verbose=4</code>, and <code>xcrun stapler validate</code> against the app, including <code>Contents/Resources/bin/pi-desktop-host-core</code>. 4) Confirm the workflow&#39;s DMG step reported an Apple notary status of <code>Accepted</code> and then run <code>xcrun stapler validate</code> against the matching DMG. 5) Download the DMG on a clean macOS profile, move the app to <code>/Applications</code>, and open it without clearing <code>com.apple.quarantine</code>.</li><li><strong>Expected</strong>: Each macOS app passes signature integrity, Gatekeeper reports <code>source=Notarized Developer ID</code>, and both app and DMG contain valid stapled tickets. The DMG has its own submission: a DMG that was never submitted has no ticket and fails <code>stapler staple</code> with error 65, so a tag build must never reach that state. The app opens normally; no <code>xattr</code> quarantine-removal command or Security &amp; Privacy override is required. Missing secrets, a rejected submission, or an exhausted staple retry fail the job.</li><li><strong>Specs linked</strong>: <code>06-delivery/06-release-runbook.md</code>, <code>05-security/01-security.md</code>, ADR 0289</li><li><strong>Acceptance</strong>: Quality, Security</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Workflow script/unit-covered; clean-machine journey required for each release (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-212-github-release-starts-the-cnb-mirror-pipeline" tabindex="-1">E2E-212: GitHub Release starts the CNB mirror pipeline <a class="header-anchor" href="#e2e-212-github-release-starts-the-cnb-mirror-pipeline" aria-label="Permalink to &quot;E2E-212: GitHub Release starts the CNB mirror pipeline&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Repository secret <code>CNB_MIRROR_TOKEN</code> is configured on <code>vastsa/PI-Desktop</code>; the CNB pipeline at <code>aixk/Pi-Desktop</code> listens for <code>api_trigger_mirror</code>; a GitHub Release tag such as <code>vX.Y.Z</code> exists with uploaded artifacts.</li><li><strong>Steps</strong>: 1) Publish or edit that GitHub Release, or dispatch <code>mirror-to-cnb.yml</code> with the same tag. 2) Inspect the Actions log for the resolved tag and the POST to <code>api.cnb.cool</code>. 3) Confirm the CNB pipeline starts with <code>MIRROR_TAGS</code> equal to that tag.</li><li><strong>Expected</strong>: The job runs only on <code>vastsa/PI-Desktop</code>. Manual dispatch without a <code>vX.Y.Z</code> tag fails before calling CNB. A missing <code>CNB_MIRROR_TOKEN</code> fails closed. The JSON body is built with <code>jq</code> (not YAML string interpolation). GitHub Release artifacts and updater feeds are unchanged; CNB is a mirror of the same tag.</li><li><strong>Specs linked</strong>: <code>06-delivery/06-release-runbook.md</code></li><li><strong>Acceptance</strong>: Quality (release mirroring)</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Source-contract covered (<code>ci-workflow.test.mjs</code>); live CNB start remains operator validation (run only in a capable environment when this surface changes)</li></ul><h3 id="boot-healthcheck" tabindex="-1">Boot &amp; Healthcheck <a class="header-anchor" href="#boot-healthcheck" aria-label="Permalink to &quot;Boot &amp; Healthcheck&quot;">​</a></h3><h4 id="e2e-001-app-launches-and-shows-main-window" tabindex="-1">E2E-001: App launches and shows main window <a class="header-anchor" href="#e2e-001-app-launches-and-shows-main-window" aria-label="Permalink to &quot;E2E-001: App launches and shows main window&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: macOS arm64 or Intel x64; no prior <code>~/.pi-desktop</code> profile. For the development lane, workspace package build outputs are absent or older than their TypeScript sources.</li><li><strong>Steps</strong>: 1) Launch PI-Desktop. In the development lane, use <code>pnpm dev</code>. 2) Observe main window appears.</li><li><strong>Expected</strong>: Development launch rebuilds all workspace dependencies before host-core and Electron startup. Window first shows the branded startup splash while bootstrap runs, then reveals the main shell in English with the current locale catalog; no compile error, missing-menu runtime error, or crash; version info visible. Key lifecycle and error records are written to the categorized logs. GitHub auto-update is not started until after <code>ensureWindow</code>, and a hung feed cannot keep updater status on <code>checking</code> for Chromium&#39;s ~60s timeout.</li><li><strong>Specs linked</strong>: <code>03-runtime/07-process-model.md</code>, <code>04-ux/01-ui-ia.md</code>, <code>03-runtime/09-logging-and-observability.md</code></li><li><strong>Acceptance</strong>: A (app startup)</li><li><strong>Milestone</strong>: M1</li><li><strong>Status</strong>: Partially automated (<code>runtime-build-contract.test.mjs</code> covers the dependency build contract; <code>update-timeout.test.mjs</code> and <code>auto-update.test.mjs</code> cover the bounded auto-check contract; Electron window launch remains Draft)</li></ul><h4 id="e2e-002-ipc-bridge-is-functional" tabindex="-1">E2E-002: IPC bridge is functional <a class="header-anchor" href="#e2e-002-ipc-bridge-is-functional" aria-label="Permalink to &quot;E2E-002: IPC bridge is functional&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: App is running.</li><li><strong>Steps</strong>: 1) Trigger an action that calls preload IPC (e.g. version query). 2) Observe result in renderer.</li><li><strong>Expected</strong>: Main↔renderer IPC returns expected data; no error. The packaged main and plugin-panel sandbox preloads are self-contained and do not require an additional local runtime chunk.</li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code></li><li><strong>Acceptance</strong>: A (bridge normal)</li><li><strong>Milestone</strong>: M1</li><li><strong>Status</strong>: Automated (<code>scripts/e2e-electron-boot.mjs</code> — sandboxed preload bridge + IPC round-trip)</li></ul><h4 id="e2e-003-rust-host-healthcheck-responds" tabindex="-1">E2E-003: Rust host healthcheck responds <a class="header-anchor" href="#e2e-003-rust-host-healthcheck-responds" aria-label="Permalink to &quot;E2E-003: Rust host healthcheck responds&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: App is running; Rust host-core sidecar started.</li><li><strong>Steps</strong>: 1) Electron handshakes with protocol version 11. 2) Call the host healthcheck RPC. 3) Repeat boot with mismatched older and newer protocol fixtures.</li><li><strong>Expected</strong>: The protocol v11 host returns <code>ok</code> and the handshake is logged. Every version other than v11, whether older or newer, is rejected before the conversation surface becomes interactive, so Plan approval/state events and context checkpoints cannot be silently lost.</li><li><strong>Specs linked</strong>: <code>03-runtime/05-host-core-rust.md</code>, <code>03-runtime/06-host-rpc-protocol.md</code></li><li><strong>Acceptance</strong>: A (bridge normal)</li><li><strong>Milestone</strong>: M1</li><li><strong>Status</strong>: Automated (protocol smoke)</li></ul><h4 id="e2e-004-first-run-inline-checklist-appears" tabindex="-1">E2E-004: First-run inline checklist appears <a class="header-anchor" href="#e2e-004-first-run-inline-checklist-appears" aria-label="Permalink to &quot;E2E-004: First-run inline checklist appears&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Fresh profile (no <code>~/.pi-desktop</code>).</li><li><strong>Steps</strong>: 1) Launch app on fresh profile. 2) Observe onboarding checklist.</li><li><strong>Expected</strong>: Inline checklist is displayed; provider/key items open Settings → Agent, and the optional plugin item opens the app-shell Plugins destination.</li><li><strong>Specs linked</strong>: <code>04-ux/05-onboarding.md</code></li><li><strong>Acceptance</strong>: A (first-run checklist)</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Automated (protocol smoke: host onboarding state; UI checklist manual)</li></ul><h3 id="provider-key" tabindex="-1">Provider &amp; Key <a class="header-anchor" href="#provider-key" aria-label="Permalink to &quot;Provider &amp; Key&quot;">​</a></h3><h4 id="e2e-169-default-model-picker-selects-a-configured-model" tabindex="-1">E2E-169: Default model picker selects a configured model <a class="header-anchor" href="#e2e-169-default-model-picker-selects-a-configured-model" aria-label="Permalink to &quot;E2E-169: Default model picker selects a configured model&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Two runnable providers exist; one provider has at least two configured model bindings.</li><li><strong>Steps</strong>: Open Settings → Model configuration → Change default model, then select the second model under the provider.</li><li><strong>Expected</strong>: The Defaults card presents a compact settings row with the provider name and exact model ID beneath the Default model label; its quiet Change action does not repeat the current value. The picker groups model-level entries by provider, marks the exact current model, and a newly created session inherits that exact model and its owning provider. Changing the default later does not rewrite an already created session&#39;s stored provider/model; only a new session or the unpersisted home draft follows the live default. Searching by provider or model filters locally, the result list scrolls without moving the settings card, and an unmatched query shows an empty state.</li><li><strong>Specs linked</strong>: <code>03-runtime/13-model-catalog-and-selection.md</code></li><li><strong>Acceptance</strong>: B (model selection)</li><li><strong>Milestone</strong>: M6</li><li><strong>Status</strong>: Documented; automation pending</li></ul><h4 id="e2e-005-add-a-provider-and-save-api-key" tabindex="-1">E2E-005: Add a provider and save API key <a class="header-anchor" href="#e2e-005-add-a-provider-and-save-api-key" aria-label="Permalink to &quot;E2E-005: Add a provider and save API key&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: App running; no provider configured; the models.dev snapshot ships with the build.</li><li><strong>Steps</strong>: 1) Open Settings → Model configuration and choose Add provider. 2) Confirm the dialog is ONE form with no stepper or Next/Back buttons. The first control is Service — a searchable menu (Choose a service, Custom endpoint, then a flat vendor list from models.dev including Xiaomi), not a native select, region grouping, or vendor-card grid. Open it, type to filter client-side, then choose <strong>Custom endpoint</strong>. Confirm Name and Base URL appear on one row with no helper paragraph under the URL (placeholder only), API Key and API format appear side by side on the next row (not behind Advanced), and that a focused field plus its 2px accent ring stays fully inside the dialog, including on a window narrower than 1040px. 3) Enter a name and a base URL for a service that publishes a <code>/models</code> route, then paste an API key. 4) Confirm the models section fills with the models THAT SERVICE returned, not with every model its vendor publishes; confirm a model the deployment does not host is absent. 5) Type in the filter box and confirm the list narrows client-side with no network request per keystroke. 6) Confirm each row shows the models.dev-derived context/output for models the catalog knows, that its compact text tracks the published value instead of a coarser rounded one (a 1,050,000 window reads <code>1.05M</code>, never <code>1.1M</code>), and that a model with no catalog match still lists with generic defaults. 7) Select two models with the checkboxes. 8) Expand Advanced on one chosen row, override its limits and toggle thinking chips; confirm each numeric field has a five-chip preset ladder for common values, clicking a chip writes the value, hand editing remains possible, and a non-preset value leaves the ladder unselected. Confirm the label and optional hint sit above one compact grouped control and do not force the options onto a second row at normal dialog width; confirm all seven canonical levels are available, that published levels start selected for a known reasoning model, and that a non-reasoning or unknown row shows the same chips unselected with the manual-override hint; enable one level on that row and confirm the other row is unaffected. 9) Open the form-level Advanced and confirm the API format is present but pre-derived. 10) Add a free-form model ID the service did not return; confirm it is added with 128,000 / 8,192 / no-thinking defaults, then enable a thinking level if the endpoint supports it; confirm re-adding the same ID in different letter case is rejected as already added. 11) Save.</li><li><strong>Expected</strong>: The service is asked first and models.dev only enriches the answer and seeds known-model defaults. The settings picker always offers the seven canonical thinking levels, and the Composer later renders the explicit levels saved in the same model binding; an empty or <code>off</code>-only binding resolves to <code>off</code>. Discovery is debounced ~600 ms, does not mark loading until that window elapses, and a slow reply from an earlier keystroke never replaces a newer list; named add-path discovery waits for an API key, while an unsaved custom provider is probed with the typed base URL (and key, if any) before it exists. Preset ladders cover common context/output limits while preserving hand-edited values. Limit text renders through one shared compact formatter, so neighbouring published windows stay distinguishable (<code>1M</code> / <code>1.05M</code> / <code>1.1M</code>) and a compact limit never reads above its published value. Custom endpoint keeps API format beside the key and omits Base URL helper copy; named endpoints do not show format. Point the same custom form at an unreachable or unauthorized URL and confirm the left pane shows a classified error (not a raw JSON/HTML dump and not a second “no models” empty state); with cached rows from a later edit, the same error is a one-line banner above the list. Point a second provider at a base URL with no <code>/models</code> route and confirm the list falls back to the catalog, is labelled as coming from models.dev rather than the service, and still saves. The provider appears as a row with its host, model count and secret badge; the key is stored securely (not in plaintext config); <code>models</code> contains both bindings and <code>models[0]</code> remains the provider default.</li><li><strong>Specs linked</strong>: <code>03-runtime/11-provider-model-system.md</code>, <code>03-runtime/12-provider-config-schema.md</code>, <code>03-runtime/13-model-catalog-and-selection.md</code>, <code>03-runtime/14-secrets-storage.md</code>, <code>04-ux/06-settings-ia.md</code></li><li><strong>Acceptance</strong>: B (multi-model provider configuration, save key)</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Manual UI + automated protocol smoke (provider create + secret, no plaintext echo)</li></ul><h4 id="e2e-provider-defaults-survive-an-added-provider-an-added-provider-leaves-the-app-defaults-alone" tabindex="-1">E2E-PROVIDER-defaults-survive-an-added-provider: An added provider leaves the app defaults alone <a class="header-anchor" href="#e2e-provider-defaults-survive-an-added-provider-an-added-provider-leaves-the-app-defaults-alone" aria-label="Permalink to &quot;E2E-PROVIDER-defaults-survive-an-added-provider: An added provider leaves the app defaults alone&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: App running; provider A saved and set as the app default model; a second provider B serving different models; one image-capable model configured on A and another on a different service.</li><li><strong>Steps</strong>: 1) Open Settings → Model configuration and add provider B; save without touching the Default model row. 2) Confirm the Default model row still names provider A and its exact model, and that a new session starts on it. 3) Set an image model as the default image model, then add a provider that also serves image models; save. 4) Confirm the Default image model row still names the earlier binding while the picker lists the new provider&#39;s image models as candidates. 5) Delete the provider that owned a default, then add a service that serves a model and an image model; save. 6) Confirm both defaults now resolve to that newly added provider.</li><li><strong>Expected</strong>: Saving a new provider never repoints an app default that still resolves: the model default keeps the pairing the Default model row already renders, and the image default keeps its stored binding while its candidate list grows. Only a default that no longer resolves — its provider deleted, or its model removed from the provider — is filled by the newly added provider, so settings are written only when the app would otherwise have nothing to run. The explicit make-default actions, the edit path, and the fallback to the first remaining binding are unchanged.</li><li><strong>Specs linked</strong>: <code>03-runtime/13-model-catalog-and-selection.md</code></li><li><strong>Acceptance</strong>: B (model selection)</li><li><strong>Milestone</strong>: M6</li><li><strong>Status</strong>: Documented; covered by <code>apps/desktop/test/default-model-display.test.mjs</code>, <code>apps/desktop/test/image-generation-default.test.mjs</code>, <code>apps/desktop/test/provider-model-config.test.mjs</code></li></ul><h4 id="e2e-provider-custom-model-limits-a-hand-typed-custom-model-id-takes-its-limits-from-the-model-library" tabindex="-1">E2E-PROVIDER-custom-model-limits: A hand-typed custom model id takes its limits from the model library <a class="header-anchor" href="#e2e-provider-custom-model-limits-a-hand-typed-custom-model-id-takes-its-limits-from-the-model-library" aria-label="Permalink to &quot;E2E-PROVIDER-custom-model-limits: A hand-typed custom model id takes its limits from the model library&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: App running; the models.dev snapshot ships with the build; a provider editor is open.</li><li><strong>Steps</strong>: 1) Type a model id the snapshot publishes into the add-model field and add it; confirm the new row&#39;s context window, max output tokens and thinking levels match the published record instead of 128,000 / 8,192 with none. 2) Type an id the snapshot does not publish and add it; confirm the row keeps the generic 128,000 / 8,192 seed and no thinking levels. 3) Add an id while the service list is unreachable; confirm the row still appears exactly once and stays editable. 4) Add an id and edit its limits immediately, before the lookup answers; confirm the typed values survive.</li><li><strong>Expected</strong>: <code>providers.lookupModel</code> answers a hand-typed id from the local snapshot only — no provider network request and no host call — and a hit seeds the binding the way a picked model is seeded (published context window, max output tokens, thinking levels, <code>contextWindowSource: &quot;catalog&quot;</code>) while the stored id stays exactly what the user typed. A miss, a failed call, or an id the current discovery already described leaves the previous behavior intact: one usable row, the generic seed, and no stalled or duplicated list.</li><li><strong>Specs linked</strong>: <code>03-runtime/12-provider-config-schema.md</code>, <code>03-runtime/13-model-catalog-and-selection.md</code></li><li><strong>Acceptance</strong>: B (multi-model provider configuration)</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Documented; covered by <code>apps/desktop/test/model-custom-lookup.test.mjs</code> and <code>apps/desktop/test/provider-lookup-model-handler.test.mjs</code></li></ul><h4 id="e2e-provider-configured-models-search-the-chosen-pane-s-search-narrows-the-configured-list" tabindex="-1">E2E-PROVIDER-configured-models-search: The chosen pane&#39;s search narrows the configured list <a class="header-anchor" href="#e2e-provider-configured-models-search-the-chosen-pane-s-search-narrows-the-configured-list" aria-label="Permalink to &quot;E2E-PROVIDER-configured-models-search: The chosen pane&#39;s search narrows the configured list&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: App running; one provider saved with at least three model bindings, one of them carrying an alias and another whose catalog display name differs from its id.</li><li><strong>Steps</strong>: 1) Open Settings → Model configuration and edit that provider. 2) Type part of one model id into the search field beside the right pane&#39;s title and confirm the configured list narrows as you type while the count beside the title keeps reporting every configured model. 3) Search another row by its alias, then by its catalog display name, and confirm both find the binding they stand for. 4) Type a query that matches nothing and confirm the right pane shows its own empty state rather than the &quot;nothing chosen yet&quot; one. 5) With that query still in the field, tick a model in the left discovered list and confirm it is added in view — the field is dropped only when the filter would have hidden it — then tick a model whose id matches the query and confirm the filter stays. 6) Clear the filter, remove every configured model, and confirm the field ends up empty instead of holding a query in a box that is now disabled. 7) Save.</li><li><strong>Expected</strong>: The right pane&#39;s search filters the configured list client-side with no host round trip, matching the model id, its alias and the catalog display name case-insensitively. The count badge keeps reporting every configured model, so a filtered view is never mistaken for a provider that offers fewer models. A model added while a filter is active is never added out of view: the field is dropped when the new row would be hidden and kept when it still shows what was added, and an emptied list discards the query so no filter is stranded in a disabled field.</li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code>, <code>03-runtime/13-model-catalog-and-selection.md</code></li><li><strong>Acceptance</strong>: B (multi-model provider configuration)</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Documented; UI automation pending</li></ul><h4 id="e2e-005a-edit-provider-model-bindings-and-migrate-a-legacy-model" tabindex="-1">E2E-005A: Edit provider model bindings and migrate a legacy model <a class="header-anchor" href="#e2e-005a-edit-provider-model-bindings-and-migrate-a-legacy-model" aria-label="Permalink to &quot;E2E-005A: Edit provider model bindings and migrate a legacy model&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: One provider saved with two model bindings; one fixture provider row exists with only the legacy <code>default_model_id</code> and no <code>config_json.models</code>; one fixture row carries an unknown or legacy <code>apiStyle</code> string.</li><li><strong>Steps</strong>: 1) Reopen the saved provider; confirm the single form opens with its cached model list painted immediately, and that the API key field explains an unchanged key is kept. 2) Confirm the live probe then refreshes that list without a retyped key, because the stored secret is reused. 3) Confirm both existing bindings are still chosen and check-marked, with their limits, thinking chips and defaults intact. 4) Enable a level the fixture catalog does not publish, edit one row&#39;s limits and save. 5) Reopen the fixture provider and confirm its legacy model appears as one chosen row with 128,000 context, 8,192 max output and all seven thinking choices available but unselected. 6) Reopen the unknown-style fixture and confirm the editor renders with Chat Completions selected instead of an error boundary; save it and confirm the repaired style is persisted. 7) Save the fixture provider without changing the model. 8) Select the second binding as the app default, reopen the provider and save unchanged; confirm the app default stays on that binding. Save another provider and confirm the app default is unchanged. Reopen the default provider, remove the selected binding, and save; confirm the app default falls back to the first remaining binding. 9) Take the service offline or revoke the key, reopen the provider, and confirm the cached rows stay visible with a compact classified discovery error rather than an empty list or a raw host dump.</li><li><strong>Expected</strong>: Editing never drops an unmodified binding. An explicitly enabled level remains saved even when the catalog does not publish it, so the Composer reads the same binding rather than silently narrowing it; a binding whose model discovery is unavailable keeps its stored levels untouched. Legacy read materializes one binding without losing the old model ID; the subsequent write stores <code>config_json.models</code> and keeps <code>defaultModelId</code> equal to the first binding for older readers. An unknown or legacy <code>apiStyle</code> is treated as a compatibility input: the editor falls back to Chat Completions, remains usable, and repairs the stored value on save. Saving the default provider preserves <code>settings.defaultModelId</code> while the selected model remains configured; only removal of that model falls back to the first remaining binding. Editing another provider never changes the app default. A failed live probe degrades to the cached list plus a compact classified error, never to a blank picker or a raw HTTP/JSON dump, and a catalog fallback is never written into the model cache.</li><li><strong>Specs linked</strong>: <code>03-runtime/11-provider-model-system.md</code>, <code>03-runtime/12-provider-config-schema.md</code>, <code>03-runtime/13-model-catalog-and-selection.md</code>, ADR 0114</li><li><strong>Acceptance</strong>: F (provider persistence and migration)</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Unit-covered host migration; manual UI journey</li></ul><h4 id="e2e-005k-preserve-explicit-extended-thinking-levels-on-the-wire" tabindex="-1">E2E-005K: Preserve explicit extended thinking levels on the wire <a class="header-anchor" href="#e2e-005k-preserve-explicit-extended-thinking-levels-on-the-wire" aria-label="Permalink to &quot;E2E-005K: Preserve explicit extended thinking levels on the wire&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A provider has a selected model binding with reasoning enabled and <code>xhigh</code>/<code>max</code> explicitly selected; the catalog omits or marks those adapter mappings as unsupported; a deterministic OpenAI-compatible capture fixture records request JSON.</li><li><strong>Steps</strong>: 1) Select <code>high</code>, <code>xhigh</code>, and <code>max</code> in separate turns. 2) Capture each request body at the fixture boundary.</li><li><strong>Expected</strong>: The three requests contain <code>reasoning_effort: &quot;high&quot;</code>, <code>reasoning_effort: &quot;xhigh&quot;</code>, and <code>reasoning_effort: &quot;max&quot;</code> respectively. The catalog&#39;s non-null wire mapping remains in force when one is published; an absent or null mapping for an explicitly enabled extended level does not silently downgrade it to <code>high</code>.</li><li><strong>Specs linked</strong>: <code>03-runtime/11-provider-model-system.md</code>, <code>03-runtime/12-provider-config-schema.md</code>, <code>03-runtime/13-model-catalog-and-selection.md</code></li><li><strong>Acceptance</strong>: B (model configuration), F (runtime provider requests)</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Unit-covered; deterministic provider fixture pending</li></ul><h4 id="e2e-005b-configure-the-fixed-opencode-go-api-style-preset" tabindex="-1">E2E-005B: Configure the fixed OpenCode Go API-style preset <a class="header-anchor" href="#e2e-005b-configure-the-fixed-opencode-go-api-style-preset" aria-label="Permalink to &quot;E2E-005B: Configure the fixed OpenCode Go API-style preset&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: App running; no OpenCode Go provider configured; the OpenCode Go endpoint is reachable with a test API key.</li><li><strong>Steps</strong>: 1) Open Settings → Model configuration and open the add-provider dialog. 2) Select <strong>OpenCode Go</strong> in Service. 3) Confirm Name and Base URL are not on the common path, enter an API key, and wait for model discovery. 4) Select a discovered model and save the provider. 5) Reopen the provider and switch Service to Custom endpoint.</li><li><strong>Expected</strong>: Selecting the preset shows Service + API key, a host summary for <code>opencode.ai/zen/go/v1</code>, and focuses the API key field. Discovery requests <code>https://opencode.ai/zen/go/v1/models</code> with <code>Authorization: Bearer &lt;key&gt;</code>; the saved row persists <code>apiStyle: &quot;opencode_go&quot;</code> and the key is stored via the secret store. Reopening preserves the fixed identity. Switching to Custom endpoint reveals editable Name and Base URL.</li><li><strong>Specs linked</strong>: <code>03-runtime/11-provider-model-system.md</code>, <code>03-runtime/12-provider-config-schema.md</code>, <code>04-ux/06-settings-ia.md</code>, ADR 0116</li><li><strong>Acceptance</strong>: B (model configuration and key storage), Security</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Unit-covered (form and discovery contracts); rendered UI scenario Draft</li></ul><h4 id="e2e-205-opencode-go-requests-carry-a-stable-session-header" tabindex="-1">E2E-205: OpenCode Go requests carry a stable session header <a class="header-anchor" href="#e2e-205-opencode-go-requests-carry-a-stable-session-header" aria-label="Permalink to &quot;E2E-205: OpenCode Go requests carry a stable session header&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: An OpenCode Go provider is configured; a deterministic fixture or capture proxy records outbound HTTP headers. A second generic OpenAI-compatible provider is also configured.</li><li><strong>Steps</strong>: 1) Start an Agent turn in a session against OpenCode Go. 2) Capture the provider request headers. 3) Send a follow-up in the same session. 4) Run prompt enhancement and a plugin <code>agent.complete</code> one-shot against the same provider. 5) Run <code>/compact</code> in the same session and capture the summary request. 6) Repeat a turn against the generic OpenAI-compatible provider.</li><li><strong>Expected</strong>: Every OpenCode Go LLM request includes <code>x-opencode-session</code> equal to the conversation id (or a stable per-call id when no session exists), <code>x-opencode-client: pi-desktop</code>, and a <code>User-Agent</code> identifying PI-Desktop. Follow-up turns reuse the same session header, and so does the compaction summary request, which the harness would otherwise send with no headers at all. The generic OpenAI-compatible provider does not receive these headers. The gateway does not return <code>MissingSessionID</code>, and <code>/compact</code> does not fail with a 400.</li><li><strong>Specs linked</strong>: <code>03-runtime/02-agent-runtime.md</code>, <code>03-runtime/11-provider-model-system.md</code>, <code>03-runtime/12-provider-config-schema.md</code>, ADR 0116</li><li><strong>Acceptance</strong>: B (model configuration), F (runtime provider requests)</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Unit-covered (header merge, one-shot stream options, and the compaction summary request)</li></ul><h4 id="e2e-005e-model-level-wire-api-wins-over-the-provider-style" tabindex="-1">E2E-005E: Model-level wire API wins over the provider style <a class="header-anchor" href="#e2e-005e-model-level-wire-api-wins-over-the-provider-style" aria-label="Permalink to &quot;E2E-005E: Model-level wire API wins over the provider style&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: An OpenCode Go provider is configured; a deterministic fixture serves <code>muse-spark-1.3-contributor</code> on <code>/responses</code> and 500s it on <code>/chat/completions</code>. A second generic provider serves the same model id on <code>/chat/completions</code>.</li><li><strong>Steps</strong>: 1) Select the muse model on the OpenCode Go provider and send a turn. 2) Capture the outbound request path. 3) Repeat against the generic provider with the same model id.</li><li><strong>Expected</strong>: The OpenCode Go turn posts to <code>/responses</code> (the model-level <code>api: &quot;openai-responses&quot;</code> pin wins); the generic turn still posts to <code>/chat/completions</code>. Replayed history carries the resolved API.</li><li><strong>Specs linked</strong>: <code>03-runtime/11-provider-model-system.md</code>, <code>03-runtime/12-provider-config-schema.md</code>, ADR 0116</li><li><strong>Acceptance</strong>: F (runtime provider requests)</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Unit-covered (binding resolution and endpoint assertion)</li></ul><h4 id="e2e-005c-openai-compatible-system-role-fallback" tabindex="-1">E2E-005C: OpenAI-compatible system role fallback <a class="header-anchor" href="#e2e-005c-openai-compatible-system-role-fallback" aria-label="Permalink to &quot;E2E-005C: OpenAI-compatible system role fallback&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A deterministic OpenAI-compatible Chat Completions fixture exposes one reasoning-capable model and rejects <code>role: &quot;developer&quot;</code> in favor of <code>role: &quot;system&quot;</code>.</li><li><strong>Steps</strong>: 1) Configure the fixture provider and select its reasoning model. 2) Start an Agent turn with a non-empty system prompt. 3) Capture the JSON request body at the fixture boundary. 4) Repeat with a model record that explicitly sets <code>compat.supportsDeveloperRole: true</code>.</li><li><strong>Expected</strong>: The first request contains the system prompt with <code>role: &quot;system&quot;</code> and the turn succeeds. The explicit model override changes only the second request to <code>role: &quot;developer&quot;</code>; the default remains model-scoped and does not affect other provider adapters.</li><li><strong>Specs linked</strong>: <code>03-runtime/11-provider-model-system.md</code>, <code>03-runtime/12-provider-config-schema.md</code></li><li><strong>Acceptance</strong>: B (OpenAI-compatible provider interoperability)</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Unit-covered (including the #30 GLM gateway regression); deterministic provider fixture pending</li></ul><h4 id="e2e-206-anthropic-messages-endpoint-with-a-v1-base-url" tabindex="-1">E2E-206: Anthropic Messages endpoint with a <code>/v1</code> base URL <a class="header-anchor" href="#e2e-206-anthropic-messages-endpoint-with-a-v1-base-url" aria-label="Permalink to &quot;E2E-206: Anthropic Messages endpoint with a \`/v1\` base URL&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A deterministic Anthropic Messages fixture exposes <code>GET /v1/models</code> and <code>POST /v1/messages</code>; the configured custom endpoint ends in <code>/v1</code> and returns one model such as <code>glm-5.3</code>.</li><li><strong>Steps</strong>: 1) Open Settings → Model configuration and add a Custom endpoint. 2) Enter the fixture URL ending in <code>/v1</code>, choose Anthropic Messages, and wait for model discovery. 3) Select the discovered model and save. 4) Start an Agent turn and capture the fixture request path.</li><li><strong>Expected</strong>: Discovery succeeds against <code>/v1/models</code>, and the Agent turn succeeds against <code>/v1/messages</code>. The runtime does not send the request to a doubled <code>/v1/v1/messages</code> path; a configured Anthropic root without <code>/v1</code> remains equivalent.</li><li><strong>Specs linked</strong>: <code>03-runtime/11-provider-model-system.md</code>, <code>03-runtime/12-provider-config-schema.md</code></li><li><strong>Acceptance</strong>: B (custom provider interoperability), C (chat and stream)</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Unit-covered; deterministic provider fixture pending</li></ul><h4 id="e2e-005f-custom-endpoint-input-guardrails" tabindex="-1">E2E-005F: Custom endpoint input guardrails <a class="header-anchor" href="#e2e-005f-custom-endpoint-input-guardrails" aria-label="Permalink to &quot;E2E-005F: Custom endpoint input guardrails&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: App running; the add-provider dialog is open with Custom endpoint selected.</li><li><strong>Steps</strong>: 1) Enter a valid gateway URL ending in <code>/v1/messages</code>, then leave the Base URL field. 2) Confirm the field keeps the service base URL ending in <code>/v1</code>, and that its helper identifies the API path that will be targeted. 3) Replace the value with <code>ftp://gateway.example.com</code>, then leave the field. 4) Enter a valid URL again and confirm model discovery can run; paste a full <code>/models</code> path and leave the field.</li><li><strong>Expected</strong>: Full operation paths are normalized to the service root on blur, without changing the selected API style. A non-http(s) URL shows an inline, accessible error, does not start discovery, and keeps Save disabled. A valid URL restores discovery; the <code>/models</code> suffix is also removed before the request is made. The long URL field uses a full row on wide dialogs and stacks cleanly with the other credentials at the responsive breakpoint.</li><li><strong>Specs linked</strong>: <code>04-ux/06-settings-ia.md</code>, <code>03-runtime/12-provider-config-schema.md</code></li><li><strong>Acceptance</strong>: B (custom provider configuration)</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Unit-covered; rendered UI scenario pending</li></ul><h4 id="e2e-005g-per-provider-custom-http-headers" tabindex="-1">E2E-005G: Per-provider custom HTTP headers <a class="header-anchor" href="#e2e-005g-per-provider-custom-http-headers" aria-label="Permalink to &quot;E2E-005G: Per-provider custom HTTP headers&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: One API-key AI service (including an OpenCode Go row) and one signed-in vendor (OAuth) account; a capture proxy records outbound HTTP headers, including Codex and Anthropic adapters.</li><li><strong>Steps</strong>: 1) Open the AI service and click the upper-right Advanced settings action. Confirm a separate modal opens without changing the main form layout, and that its header close action is the only close control (no footer action row). Use the common-header preset to add User-Agent, then import a JSON object containing <code>X-Gateway: alpha</code> and enough headers to exceed five visible rows. Copy headers as JSON and confirm the clipboard is the pretty-printed persisted record (blank names omitted, last write wins) with localized success feedback. Confirm the header list scrolls inside the modal while the underlying model panes keep their working area, close the modal, then save. 2) Start an Agent turn, a follow-up, prompt enhancement, and a plugin one-shot. 3) Refresh <code>/models</code> from the form before saving a second change and confirm the unsaved headers are sent. 4) Clear the rows and save; confirm adapter defaults return. 5) Edit the OAuth account Advanced headers, save, then run a turn that refreshes the access token. 6) Repeat against OpenCode Go and confirm <code>x-opencode-session</code> is still present. 7) Repeat against Codex/Anthropic OAuth inference. 8) Attempt <code>Authorization</code> and CR/LF values; save is rejected.</li><li><strong>Expected</strong>: Non-empty custom headers are the last writer on that row&#39;s outbound HTTP (turns, subagents, one-shots, discovery, connection test, OAuth refresh). Empty restores pi-ai / <code>claude-cli</code> / OpenCode defaults. Advanced keeps up to five header rows visible and scrolls additional rows in its own bounded area, so they do not compress or hide the model panes. Escape and outside-click close only the Advanced modal while it is open. The preset adds the expected User-Agent value, Copy JSON serializes the same <code>pairsToRecord</code> map saved on the row, JSON import accepts both supported object shapes, and case-insensitive duplicate keys are merged rather than duplicated. Toolbar actions wrap at the narrow dialog breakpoint instead of overflowing. OpenCode still sends <code>x-opencode-session</code> and <code>x-opencode-client</code>. Codex and Anthropic still send the custom User-Agent despite adapter last-writes. First OAuth login does not collect headers. Reserved keys and CR/LF are rejected. Advanced is a compact key/value editor, not a lone User-Agent field.</li><li><strong>Specs linked</strong>: <code>03-runtime/12-provider-config-schema.md</code>, <code>03-runtime/11-provider-model-system.md</code>, <code>03-runtime/02-agent-runtime.md</code>, <code>04-ux/06-settings-ia.md</code>, ADR 0178</li><li><strong>Acceptance</strong>: B (model configuration), F (runtime provider requests)</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Unit-covered (host persistence, fetch wrapper, discovery, form Advanced); rendered UI scenario pending</li></ul><h4 id="e2e-005j-github-copilot-oauth-requests-carry-native-ide-headers" tabindex="-1">E2E-005J: GitHub Copilot OAuth requests carry native IDE headers <a class="header-anchor" href="#e2e-005j-github-copilot-oauth-requests-carry-native-ide-headers" aria-label="Permalink to &quot;E2E-005J: GitHub Copilot OAuth requests carry native IDE headers&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A signed-in GitHub Copilot OAuth account with a selected model; a deterministic capture proxy records the model request headers.</li><li><strong>Steps</strong>: 1) Start an Agent turn against the account and capture the request headers. 2) Send a follow-up after the assistant response and capture the next request. 3) Repeat with an image attachment when the selected model supports vision. 4) Set a saved custom header with the same name as one of the Copilot defaults and send another turn.</li><li><strong>Expected</strong>: Every Copilot model request includes the pinned pi-ai transport identity headers <code>Editor-Version</code>, <code>Editor-Plugin-Version</code>, and <code>Copilot-Integration-Id</code>. <code>X-Initiator</code> is <code>user</code> for a user-led request and <code>agent</code> for a continuation; <code>Openai-Intent</code> is <code>conversation-edits</code>, and image requests include <code>Copilot-Vision-Request: true</code>. The OAuth row keeps its local provider id for account binding, and a saved custom header remains the final override.</li><li><strong>Specs linked</strong>: <code>03-runtime/11-provider-model-system.md</code>, <code>03-runtime/12-provider-config-schema.md</code>, ADR 0095</li><li><strong>Acceptance</strong>: B (model configuration), F (runtime provider requests)</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Unit-covered (row-scoped model headers and request-context headers); live Copilot account journey pending</li></ul><h4 id="e2e-005h-select-every-visible-model-from-a-long-service-list" tabindex="-1">E2E-005H: Select every visible model from a long service list <a class="header-anchor" href="#e2e-005h-select-every-visible-model-from-a-long-service-list" aria-label="Permalink to &quot;E2E-005H: Select every visible model from a long service list&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: App running; the add-provider or edit-provider dialog is open against a service (or vendor account) that returns a long model list, including at least one model whose id would not match a later search.</li><li><strong>Steps</strong>: 1) Wait until the left pane lists the service&#39;s models. Confirm the list header shows a checkbox beside the pane title, unchecked while no rows are chosen. 2) Tick that header checkbox. Confirm every listed row is checked and the right pane lists a chosen binding for each, keeping any already-configured advanced overrides. 3) Untick one row, then confirm the header checkbox is indeterminate. Tick it again and confirm the remaining visible rows are chosen without duplicating already-chosen ones. 4) Type a filter that matches a subset. Untick the header checkbox and confirm only the matching chosen rows disappear; a model hidden by the filter stays on the right. 5) Tick the header checkbox again and confirm only the matching rows are added back. Clear the filter and confirm the previously hidden chosen model is still present. 6) Save.</li><li><strong>Expected</strong>: One header checkbox selects or clears the currently visible list. A search filter narrows which rows &quot;all&quot; means. Models already chosen outside the filter stay chosen. Newly added rows adopt published limits and thinking levels; existing bindings are not rebuilt. The same control is present in the vendor-account editor because both dialogs render the shared picker.</li><li><strong>Specs linked</strong>: <code>03-runtime/13-model-catalog-and-selection.md</code>, <code>04-ux/06-settings-ia.md</code>, <code>04-ux/08-component-spec.md</code></li><li><strong>Acceptance</strong>: B (multi-model provider configuration)</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Unit-covered (shared picker source contract); rendered UI scenario pending</li></ul><h4 id="e2e-005i-fetch-the-service-model-list-from-the-picker-header" tabindex="-1">E2E-005I: Fetch the service model list from the picker header <a class="header-anchor" href="#e2e-005i-fetch-the-service-model-list-from-the-picker-header" aria-label="Permalink to &quot;E2E-005I: Fetch the service model list from the picker header&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: The add-provider or edit-provider dialog is open against a reachable service (or vendor account) that publishes a <code>/models</code> list.</li><li><strong>Steps</strong>: 1) Confirm the left-pane header shows a Fetch list action beside the title, disabled before a valid endpoint is ready. 2) Enter a valid endpoint. Confirm Fetch list enables during the 600 ms debounce wait. Click it immediately; confirm it does not wait for that window, shows the loading label while the probe is in flight, and then shows rows. 3) Click Fetch list again. Confirm it keeps the current rows on screen and replaces them with the live answer. 4) Take the service offline and click Fetch list; confirm the classified error appears and the previous rows remain. 5) Restore the service, click Fetch list, and confirm the live list returns. 6) Repeat in the vendor-account editor.</li><li><strong>Expected</strong>: The header action probes the service immediately, including during the edit debounce after a URL becomes valid. Automatic discovery on credential edits is unchanged. The same control is present for both credential kinds because both dialogs render the shared picker.</li><li><strong>Specs linked</strong>: <code>03-runtime/13-model-catalog-and-selection.md</code>, <code>04-ux/06-settings-ia.md</code>, <code>04-ux/08-component-spec.md</code></li><li><strong>Acceptance</strong>: B (multi-model provider configuration)</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Unit-covered (discovery hook + picker source contract); rendered UI scenario pending</li></ul><h4 id="e2e-005d-configure-a-zhipu-z-ai-named-endpoint-preset" tabindex="-1">E2E-005D: Configure a Zhipu / Z.AI named endpoint preset <a class="header-anchor" href="#e2e-005d-configure-a-zhipu-z-ai-named-endpoint-preset" aria-label="Permalink to &quot;E2E-005D: Configure a Zhipu / Z.AI named endpoint preset&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: App running; no Zhipu provider configured; the models.dev snapshot ships with <code>zhipuai</code>, <code>zhipuai-coding-plan</code>, <code>zai</code>, and <code>zai-coding-plan</code>.</li><li><strong>Steps</strong>: 1) Open Settings → Model configuration and open the add-provider dialog. 2) Open Service, type to filter, and select <strong>Zhipu AI Coding Plan</strong>. Confirm model discovery does not start until an API key is entered. 3) Confirm the common path is Service + API key with a host summary, enter an API key, and wait for model discovery. 4) Select a discovered model and save. 5) Reopen the provider, switch Service to <strong>Z.AI</strong>, then to <strong>Custom endpoint</strong>.</li><li><strong>Expected</strong>: Coding Plan shows Service + API key, a host summary for <code>open.bigmodel.cn/api/coding/paas/v4</code>, and focuses the API key field. Name and API format are not on the common path. The saved row persists <code>vendorKey: &quot;zhipuai-coding-plan&quot;</code>, <code>apiStyle: &quot;chat_completions&quot;</code>, and the exact Coding Plan URL. Switching to Z.AI replaces the host summary with <code>api.z.ai/api/paas/v4</code> and <code>vendorKey: &quot;zai&quot;</code>. Switching to Custom endpoint reveals editable Name and Base URL without a stepper or vendor-card grid. A later Agent turn against a Zhipu URL sends Completions with Zhipu thinking (<code>thinkingFormat: &quot;zai&quot;</code>) rather than the OpenAI <code>developer</code> role.</li><li><strong>Specs linked</strong>: <code>03-runtime/11-provider-model-system.md</code>, <code>03-runtime/12-provider-config-schema.md</code>, <code>04-ux/06-settings-ia.md</code>, ADR 0155</li><li><strong>Acceptance</strong>: B (model configuration and key storage)</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Unit-covered (preset matching, catalog aliases, Completions compat); rendered UI scenario Draft</li></ul><h4 id="e2e-248-responses-turn-completes-without-waiting-for-the-server-to-close-the-connection" tabindex="-1">E2E-248: Responses turn completes without waiting for the server to close the connection <a class="header-anchor" href="#e2e-248-responses-turn-completes-without-waiting-for-the-server-to-close-the-connection" aria-label="Permalink to &quot;E2E-248: Responses turn completes without waiting for the server to close the connection&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A provider whose model pins <code>api: &quot;openai-responses&quot;</code> (or a provider with <code>apiStyle: &quot;responses&quot;</code>) is configured; the endpoint is fronted by a proxy that holds the HTTP connection open after the final SSE event (a local reverse proxy or a stub server that never sends FIN).</li><li><strong>Steps</strong>: 1) Start a session with that model and send a short prompt. 2) Capture the SSE frames and confirm the server emitted <code>response.completed</code> with <code>status: &quot;completed&quot;</code> and usage. 3) Keep the stub/proxy connection open without sending a TCP FIN. 4) Observe the assistant turn state and send a follow-up prompt.</li><li><strong>Expected</strong>: The turn completes as soon as <code>response.completed</code> is finalized: usage is recorded, <code>stopReason</code> is <code>stop</code>, and the client stops consuming the stream (the underlying request is aborted) instead of blocking on the idle connection. The composer becomes idle immediately and the follow-up turn starts normally. The stream must not hang when the server never closes the connection.</li><li><strong>Specs linked</strong>: <code>03-runtime/11-provider-model-system.md</code> (§16.1)</li><li><strong>Acceptance</strong>: B (provider Responses compatibility)</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Unit-covered (stream processor terminates on the terminal event via the pi-ai patch); live-proxy scenario Draft</li></ul><h4 id="e2e-005e-deepseek-thinking-replay-includes-reasoning-content-on-aggregator-endpoints" tabindex="-1">E2E-005E: DeepSeek thinking replay includes reasoning_content on aggregator endpoints <a class="header-anchor" href="#e2e-005e-deepseek-thinking-replay-includes-reasoning-content-on-aggregator-endpoints" aria-label="Permalink to &quot;E2E-005E: DeepSeek thinking replay includes reasoning_content on aggregator endpoints&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: An OpenAI-compatible provider whose base URL is not <code>deepseek.com</code> is configured with a DeepSeek-family model id (for example SiliconFlow <code>deepseek-ai/DeepSeek-V3.2</code>) and thinking enabled.</li><li><strong>Steps</strong>: 1) Start a session in thinking mode. 2) Complete several turns including at least one assistant reply that produces no thinking text. 3) Send another prompt so the history is replayed to the provider.</li><li><strong>Expected</strong>: The later Completions request includes a reasoning field on every assistant message. Official <code>api.deepseek.com</code> rows may use <code>&quot;&quot;</code> for turns that had no thinking. Non-official DeepSeek-family relays use real retained thinking when available, otherwise the documented non-empty placeholder from ADR 0256 — never a silent empty string after compaction. The request does not switch <code>thinkingFormat</code> to <code>&quot;deepseek&quot;</code> solely because the model id contains <code>&quot;deepseek&quot;</code>. Official <code>api.deepseek.com</code> rows keep URL-based DeepSeek <code>thinkingFormat</code> from pi-ai.</li><li><strong>Specs linked</strong>: <code>03-runtime/11-provider-model-system.md</code>, <code>03-runtime/12-provider-config-schema.md</code></li><li><strong>Acceptance</strong>: B (provider Completions compatibility)</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Unit-covered (compat inject + convertMessages empty/non-empty fill + compaction retained-reasoning wire proof); live OpenCode / aggregator verification deferred; rendered UI scenario pending</li></ul><h4 id="e2e-006-key-survives-restart" tabindex="-1">E2E-006: Key survives restart <a class="header-anchor" href="#e2e-006-key-survives-restart" aria-label="Permalink to &quot;E2E-006: Key survives restart&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Provider + key configured.</li><li><strong>Steps</strong>: 1) Quit app. 2) Relaunch. 3) Open Settings → Agent → Providers.</li><li><strong>Expected</strong>: Provider still listed; key usable (no re-entry needed).</li><li><strong>Specs linked</strong>: <code>03-runtime/14-secrets-storage.md</code></li><li><strong>Acceptance</strong>: B (key survives restart)</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Draft</li></ul><h4 id="e2e-007-no-provider-blocking-prompt" tabindex="-1">E2E-007: No-provider blocking prompt <a class="header-anchor" href="#e2e-007-no-provider-blocking-prompt" aria-label="Permalink to &quot;E2E-007: No-provider blocking prompt&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: App running; no provider configured.</li><li><strong>Steps</strong>: 1) Attempt to start a chat.</li><li><strong>Expected</strong>: Clear blocking prompt explaining that a provider must be configured.</li><li><strong>Specs linked</strong>: <code>04-ux/06-settings-ia.md</code></li><li><strong>Acceptance</strong>: B (blocking prompt)</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Draft</li></ul><h3 id="conversation-stream-abort" tabindex="-1">Conversation Stream &amp; Abort <a class="header-anchor" href="#conversation-stream-abort" aria-label="Permalink to &quot;Conversation Stream &amp; Abort&quot;">​</a></h3><h4 id="e2e-008-new-session-and-send-message" tabindex="-1">E2E-008: New session and send message <a class="header-anchor" href="#e2e-008-new-session-and-send-message" aria-label="Permalink to &quot;E2E-008: New session and send message&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Provider configured.</li><li><strong>Steps</strong>: 1) Create new session. 2) Type a message. 3) Send.</li><li><strong>Expected</strong>: The transcript immediately shows a compact localized <code>Working…</code> status after send, before the first assistant or tool event. It remains visible through thinking/tool/answer output, or identifies a runtime-reported wait. Pending user interaction suppresses it; it disappears when the turn ends. The conversation topbar keeps only the task title and window actions; it does not add a separate running-state indicator.</li><li><strong>Specs linked</strong>: <code>03-runtime/02-agent-runtime.md</code>, <code>03-runtime/10-session-state-machine.md</code></li><li><strong>Acceptance</strong>: C (new session, send message)</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Automated (protocol smoke, live-model lane; requires PI_DESKTOP_TEST_API_KEY)</li></ul><h4 id="e2e-008d-composer-enter-to-send-and-modifier-send" tabindex="-1">E2E-008d: Composer Enter-to-send and modifier send <a class="header-anchor" href="#e2e-008d-composer-enter-to-send-and-modifier-send" aria-label="Permalink to &quot;E2E-008d: Composer Enter-to-send and modifier send&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Provider configured; a session is open.</li><li><strong>Steps</strong>: 1) Leave Enter-to-send enabled. Type a draft and press Enter. 2) Disable Enter-to-send in Settings. 3) Type a second draft and press Cmd/Ctrl+Enter. 4) Press Enter and Shift+Enter in a third draft.</li><li><strong>Expected</strong>: Step 1 sends. After the setting is off, Cmd/Ctrl+Enter sends; plain Enter and Shift+Enter insert newlines and do not send. IME composition and an open autocomplete menu still take precedence over send.</li><li><strong>Specs linked</strong>: <code>04-ux/01-ui-ia.md</code>, <code>04-ux/08-component-spec.md</code> §11.5, <code>04-ux/09-interaction-patterns.md</code> §1.2</li><li><strong>Acceptance</strong>: C (composer send)</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Source-level regression (<code>composer-ime.test.mjs</code>); full UI keyboard journey remains Draft. Protocol smoke does not dispatch key events.</li></ul><h4 id="e2e-008e-host-speech-transcribe-and-speak" tabindex="-1">E2E-008e: Host speech transcribe and speak <a class="header-anchor" href="#e2e-008e-host-speech-transcribe-and-speak" aria-label="Permalink to &quot;E2E-008e: Host speech transcribe and speak&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A session is open. No <code>AppSettings.speech</code> role is bound.</li><li><strong>Steps</strong>: 1) Call <code>speech/transcribe</code> with a scratch wav while unbound. 2) Persist an ASR binding (<code>openai_audio</code> / whisper) and a TTS binding with a host settings write. 3) Transcribe the wav and insert the returned text into a draft. 4) Synthesize that draft.</li><li><strong>Expected</strong>: An unbound role fails <code>SPEECH_NOT_CONFIGURED</code> and never calls the provider. Transcribe returns text. Synthesize writes session-scratch audio and returns a bounded data URL; audio bytes never enter the renderer. Whisper/TTS never appear in the chat model picker.</li><li><strong>Specs linked</strong>: <code>03-runtime/20-speech.md</code>, <code>04-ux/06-settings-ia.md</code></li><li><strong>Acceptance</strong>: C (speech)</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Source-level regression (<code>speech-capability.test.mjs</code>, <code>plugin-speech-adapter.test.mjs</code>); no renderer entry point exists, and the live provider journey remains Draft.</li></ul><h4 id="e2e-008a-first-turn-tools-load-on-demand" tabindex="-1">E2E-008a: First-turn tools load on demand <a class="header-anchor" href="#e2e-008a-first-turn-tools-load-on-demand" aria-label="Permalink to &quot;E2E-008a: First-turn tools load on demand&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Agent mode; provider configured; <code>BrowserPreview</code> or an enabled plugin tool is available; request capture can inspect the first and subsequent provider payloads.</li><li><strong>Steps</strong>: 1) Create a fresh session and send a simple prompt. 2) Inspect the first provider request&#39;s tool list. 3) Ask the agent to create or edit an HTML page and observe the tool activity. 4) Start a second user prompt after the preview task completes.</li><li><strong>Expected</strong>: The first request contains only the mode core tools (Agent: <code>Read</code>/<code>Bash</code>/<code>Edit</code>/<code>Write</code>; Chat: <code>Read</code>/<code>Glob</code>/<code>Grep</code>) and local <code>ToolSearch</code>; deferred schemas are represented only by a bounded <code># On-demand tools</code> catalog. The agent calls <code>ToolSearch</code> before <code>BrowserPreview</code> (or the selected plugin/<code>Skill</code> tool) when the capability is needed, and the matching schema is available on the next model turn. For a user-visible HTML deliverable, <code>BrowserPreview</code> is called once after creation or the first meaningful visual edit, then reused through live reload while the page is refined. Generated, test-only, and non-visual HTML files do not trigger a preview call. At the second prompt, successful activation markers still in the effective context may restore the matching deferred schemas in the first request; failed, interrupted, and missing-result rows do not. Catalog and mode changes also prevent restoration. No host permission or workspace escape is granted by restoration.</li><li><strong>Specs linked</strong>: <code>03-runtime/02-agent-runtime.md</code> §7.1, <code>03-runtime/03-tools-and-permissions.md</code> §2.1, ADR 0048, ADR 0225, <code>08-meta/decisions-log.md</code> (D185, D400)</li><li><strong>Acceptance</strong>: C (first turn and stream) + E (tool execution)</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>agent-runtime</code> deferred-tool tests); live-model request capture and full Electron journey pending</li></ul><h4 id="e2e-008b-bundled-browser-plugin-chrome-and-cdp" tabindex="-1">E2E-008b: Bundled Browser plugin chrome and CDP <a class="header-anchor" href="#e2e-008b-bundled-browser-plugin-chrome-and-cdp" aria-label="Permalink to &quot;E2E-008b: Bundled Browser plugin chrome and CDP&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Packaged or checkout build with bundled plugins; Agent session with a workspace HTML file; Plan session available.</li><li><strong>Steps</strong>: 1) Confirm Plugins lists <code>pi.browser</code>, enabled, not uninstallable. 2) Open the work panel and launch Browser from plugin views. 3) Ask the agent to preview a workspace HTML file (<code>BrowserPreview</code>) then snapshot via ToolSearch <code>cdp</code> / <code>Browser</code>. 4) Switch to Plan and call the plugin Browser tool. 5) Disable <code>pi.browser</code>. 6) Call <code>BrowserPreview</code> and click an http(s) transcript link. 7) From a third-party or test caller, send <code>Network.getAllCookies</code> through <code>pi.browser.cdp</code>.</li><li><strong>Expected</strong>: The launcher has no host Browser row. Preview opens the plugin view and live-reloads the file. Plugin tool <code>plugin_pi_browser_Browser</code> can snapshot after ToolSearch. Plan denies the plugin tool (<code>PLUGIN_DISABLED_IN_PLAN</code>) while <code>BrowserPreview</code> remains callable. Disable hides the view and tools; <code>BrowserPreview</code> errors; http(s) chips use <code>openExternal</code>. Cookie CDP is denied. Guest bounds stay inside the plugin view.</li><li><strong>Specs linked</strong>: ADR 0170, D333, <code>07-plugins/03-plugin-api.md</code>, <code>03-runtime/03-tools-and-permissions.md</code></li><li><strong>Acceptance</strong>: E (plugin view + tool) + security allowlist</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>bundled-plugins</code>, <code>browser-cdp</code>, <code>browser-preview-tool</code>); full Electron journey pending</li></ul><h4 id="e2e-008c-quiet-intervals-explain-active-work" tabindex="-1">E2E-008c: Quiet intervals explain active work <a class="header-anchor" href="#e2e-008c-quiet-intervals-explain-active-work" aria-label="Permalink to &quot;E2E-008c: Quiet intervals explain active work&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A deterministic provider fixture can delay the first response, return one retryable failure with a bounded backoff, and a session can start one delegated task whose completion is controlled by the fixture.</li><li><strong>Steps</strong>: 1) Send a prompt and hold the provider before its first assistant event. 2) Observe the transcript status row. 3) Release a retryable failure and inspect the row during backoff. 4) Start a delegated task and wait for the parent to converge on it. 5) Release the fixture and let the turn end.</li><li><strong>Expected</strong>: The status row names the quiet interval — <code>Starting…</code>, <code>Waiting for model</code>, <code>Preparing next request…</code>, <code>Compacting context…</code>, <code>Recovering empty response…</code>, <code>Retrying model request</code>, or <code>Waiting for</code> a named subagent with its latest coarse action — with a monotonic elapsed time matching the active phase. A multi-subagent wait lists each running target. It uses the same compact inline treatment as <code>Working…</code>, never adds a duplicate progress card, and clears when assistant output or a terminal event arrives. The Stop action remains available throughout.</li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code>, <code>03-runtime/02-agent-runtime.md</code>, <code>04-ux/09-interaction-patterns.md</code>, ADR 0175, ADR 0198</li><li><strong>Acceptance</strong>: C (chat stream), Quality (feedback and accessibility)</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Draft (deterministic fixture pending)</li></ul><h4 id="e2e-009-streamed-tokens-visible-in-ui" tabindex="-1">E2E-009: Streamed tokens visible in UI <a class="header-anchor" href="#e2e-009-streamed-tokens-visible-in-ui" aria-label="Permalink to &quot;E2E-009: Streamed tokens visible in UI&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Session active; message sent.</li><li><strong>Steps</strong>: 1) Request a long answer containing Markdown and inline/display math using both dollar (<code>$…$</code> / <code>$$…$$</code>) and TeX bracket (<code>\\(…\\)</code> / <code>\\[…\\]</code>) delimiters. 2) Observe the assistant response as it streams. 3) Let the answer complete and inspect the renderer console.</li><li><strong>Expected</strong>: Runtime chunks appear progressively through the incremental Markdown renderer and the final response is complete. All four math delimiter forms render with KaTeX, with <code>\\[…\\]</code> using display layout. The renderer does not start a second animation-frame typewriter loop, raise React error 185, or reject Vite-inlined KaTeX fonts under CSP.</li><li><strong>Specs linked</strong>: <code>03-runtime/02-agent-runtime.md</code>, <code>04-ux/08-component-spec.md</code>, <code>04-ux/09-interaction-patterns.md</code>, <code>05-security/01-security.md</code></li><li><strong>Acceptance</strong>: C (streamed output), Quality</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Partially automated (protocol live-model stream, renderer source regression in <code>renderer-stream-safety.test.mjs</code>, and math delimiter rendering in <code>latex-math.test.mjs</code>; full UI observation remains Draft)</li></ul><h4 id="e2e-010-abort-generation" tabindex="-1">E2E-010: Abort generation <a class="header-anchor" href="#e2e-010-abort-generation" aria-label="Permalink to &quot;E2E-010: Abort generation&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A session can produce both a deliberately delayed first token and a streaming response.</li><li><strong>Steps</strong>: 1) Send ordinary text and stop before assistant text, thinking, or a tool row begins. 2) Confirm the user row is undone and the text returns to the composer. 3) Send again, wait for partial output, then stop during the stream. 4) Observe the transcript and composer.</li><li><strong>Expected</strong>: The unanswered send is undone and its draft restored. The streaming send stops with its partial response preserved and no draft restoration or duplicate user turn. The session remains usable.</li><li><strong>Specs linked</strong>: <code>03-runtime/02-agent-runtime.md</code></li><li><strong>Acceptance</strong>: C (abort)</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Draft</li></ul><h4 id="e2e-171-streaming-reply-survives-quit-crash-and-stop" tabindex="-1">E2E-171: Streaming reply survives quit, crash, and stop <a class="header-anchor" href="#e2e-171-streaming-reply-survives-quit-crash-and-stop" aria-label="Permalink to &quot;E2E-171: Streaming reply survives quit, crash, and stop&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A session whose transcript is longer than one renderer page (more than 100 messages) and a model that streams a reply for at least ten seconds before its first tool call.</li><li><strong>Steps</strong>: 1) Send a prompt and let the reply stream for ~5 s. 2) Quit the app (Cmd+Q / tray Quit) mid-stream, relaunch, and open the session. 3) Repeat the send, then kill the agent sidecar process mid-stream and observe the transcript. 4) Repeat the send, press Stop mid-stream, then reopen the session from the sidebar and inspect <code>sessions/&lt;id&gt;.jsonl</code> and <code>sessions/&lt;id&gt;.inflight.json</code>. 5) Repeat the send and let it finish normally.</li><li><strong>Expected</strong>: 2) The session shows the user prompt followed by the streamed text up to at most 1.5 s before the quit, as an <code>aborted</code> assistant row under an <code>aborted</code> turn; nothing earlier in the session is missing or truncated. 3) The streaming row settles to <code>aborted</code> in place with its text, the same row is present after a reload, and no <code>.inflight.json</code> remains. 4) The partial reply is visible immediately after Stop and after reopening; the transcript file was not rewritten (its earlier lines are byte-identical) and the checkpoint file is gone once the aborted final row landed. 5) The completed reply has exactly one row per assistant fragment, no <code>aborted</code> duplicate, and no checkpoint file. A completed reply that had not yet left the outbox at quit is still present after relaunch (promoted <code>complete</code> if recovered from the checkpoint, or drained from the outbox before the first <code>session.get</code>).</li><li><strong>Specs linked</strong>: <code>03-runtime/04-data-storage.md</code>, <code>03-runtime/06-host-rpc-protocol.md</code>, <code>03-runtime/07-process-model.md</code>, <code>03-runtime/01-ipc-protocol.md</code></li><li><strong>Acceptance</strong>: C (abort), F (persistence)</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Draft</li></ul><h4 id="e2e-session-outbox-duplicate-id-does-not-drop-history" tabindex="-1">E2E-SESSION-outbox-duplicate-id-does-not-drop-history <a class="header-anchor" href="#e2e-session-outbox-duplicate-id-does-not-drop-history" aria-label="Permalink to &quot;E2E-SESSION-outbox-duplicate-id-does-not-drop-history&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Two sessions whose provider tool rows reuse the same <code>toolCallId</code> as <code>messages.id</code> (for example <code>call_421522</code>). The first session already persisted that id. The second session then runs several turns so assistant/tool rows queue behind the colliding append.</li><li><strong>Steps</strong>: 1) Complete a tool call in session A with id <code>call_421522</code>. 2) In session B, run the same provider tool id, then continue chatting for several turns. 3) Quit and reopen. 4) Open both sessions.</li><li><strong>Expected</strong>: Session A still has its original tool row. Session B kept its later turns after reopen; the colliding tool row is stored under <code>{sessionB}:{call_421522}</code> (or an equivalent remapped id). The persistence outbox is empty and did not stay paused on <code>UNIQUE constraint failed: messages.id</code>. No later assistant/tool row from either session is missing.</li><li><strong>Specs linked</strong>: <code>03-runtime/04-data-storage.md</code>, <code>03-runtime/06-host-rpc-protocol.md</code>, ADR 0041, D444</li><li><strong>Acceptance</strong>: F (persistence)</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Unit-covered (<code>append_message_remaps_ids_owned_by_another_session</code>, <code>persistence-outbox.test.mjs</code>); desktop journey outstanding</li></ul><h4 id="e2e-session-outbox-poison-does-not-drop-history" tabindex="-1">E2E-SESSION-outbox-poison-does-not-drop-history <a class="header-anchor" href="#e2e-session-outbox-poison-does-not-drop-history" aria-label="Permalink to &quot;E2E-SESSION-outbox-poison-does-not-drop-history&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A session-collaboration delivery turn is running. The user steers with Alt+Enter (content that does not match the delivery). Later assistant and tool rows from this or another session queue behind that append. Optionally the outbox already holds a <code>PERMISSION_DENIED:</code> head from an older host.</li><li><strong>Steps</strong>: 1) Start a collaboration delivery turn. 2) Alt+Enter a steering prompt. 3) Let the turn produce assistant/tool rows, including in another session if convenient. 4) Quit and reopen. 5) Open the affected sessions.</li><li><strong>Expected</strong>: Steering persists as a human user row with no <code>session_message</code> origin. The delivery user row still has host-derived origin. Later assistant/tool rows survive reopen. The outbox is empty and did not stay paused on <code>PERMISSION_DENIED:</code>. A <code>PLUGIN_PERMISSION_DENIED</code> head would still pause rather than drain.</li><li><strong>Specs linked</strong>: <code>03-runtime/04-data-storage.md</code>, <code>03-runtime/06-host-rpc-protocol.md</code>, ADR 0041, ADR 0239, ADR active-turn-steering, D597</li><li><strong>Acceptance</strong>: F (persistence)</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Unit-covered (<code>steering_input_persists_without_inheriting_delivery_origin</code>, <code>persistence-outbox.test.mjs</code>); desktop journey outstanding</li></ul><h4 id="e2e-011-switch-between-project-and-temporary-sessions" tabindex="-1">E2E-011: Switch between project and temporary sessions <a class="header-anchor" href="#e2e-011-switch-between-project-and-temporary-sessions" aria-label="Permalink to &quot;E2E-011: Switch between project and temporary sessions&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: One retained project session and one path-less Temporary session exist. Both transcripts exceed one viewport and have distinct final records.</li><li><strong>Steps</strong>: 1) Open the project session from its exact-path sidebar group. 2) Scroll to an earlier record and confirm the jump-to-latest control appears. 3) Open the Temporary session and observe its first painted frame. 4) Switch rapidly project → Temporary → project while the first two transcript reads are delayed, and observe which row responds and which destination commits. 5) Hover/focus Temporary, switch to it again to exercise the warm cache, then repeat with reduced motion enabled. 6) Observe chat content and workspace chrome.</li><li><strong>Expected</strong>: The sidebar contains no Recents aggregate; retained projects have scoped groups and path-less sessions remain under Temporary; each transcript loads correctly; selecting Temporary clears project context and inherits no workspace access; both sessions remain persisted. Every session first activation paints its distinct final record at the transcript bottom without first exposing the transcript top, another session&#39;s scroll position, or a stale jump-to-latest control. The latest clicked row responds immediately and its transcript request does not wait for superseded reads; only the final project/session/work-panel tuple commits. On a cold switch the currently visible pane keeps showing its own session under a thin progress track until the destination commits, with the composer keeping its settled home/docked shape but inert so no prompt can reach the session being left; no transcript is dimmed at any point. A warm revisit reveals the retained pane immediately at its own content and scroll position, then revalidates in place without a visible change. Reduced motion keeps the progress track static and preserves the same destination without a skeleton remount or animated traversal through history.</li><li><strong>Specs linked</strong>: <code>03-runtime/10-session-state-machine.md</code>, <code>04-ux/01-ui-ia.md</code>, <code>04-ux/08-component-spec.md</code>, <code>04-ux/09-interaction-patterns.md</code></li><li><strong>Acceptance</strong>: C (switch sessions)</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Source-level regression covered; full visual scenario Draft</li></ul><h4 id="e2e-207-open-large-and-long-sessions-through-bounded-transcript-windows" tabindex="-1">E2E-207: Open large and long sessions through bounded transcript windows <a class="header-anchor" href="#e2e-207-open-large-and-long-sessions-through-bounded-transcript-windows" aria-label="Permalink to &quot;E2E-207: Open large and long sessions through bounded transcript windows&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A session contains a message with at least 50 MB of text or tool output and enough additional messages to span several transcript pages; a second session contains a long ordinary conversation.</li><li><strong>Steps</strong>: 1) Open each session from the sidebar while recording the first visible transcript frame and renderer responsiveness. 2) Scroll to the top of the transcript. 3) Repeat the open/close cycle after the session has been cached. 4) Inspect the host request/response or test fixture for the session read window.</li><li><strong>Expected</strong>: Opening paints the newest bounded page without transferring the complete 50 MB value to the renderer. The large value is visibly marked as truncated for display while the transcript and model-facing read remain lossless. Reaching the top loads older pages incrementally and preserves the viewport position; the final page reports no older history. Reopening a long session reuses the bounded cache and does not synchronously construct every historical row before the latest messages become usable. Edit, delete, revision, and Stop operations rehydrate the full transcript before any rewrite, so older messages are never lost because only a window was visible.</li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code>, <code>03-runtime/04-data-storage.md</code>, <code>03-runtime/06-host-rpc-protocol.md</code>, ADR 0120</li><li><strong>Acceptance</strong>: C (session open and scroll), F (persistence), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Source-level regression covered; full large-fixture Electron journey Draft</li></ul><h4 id="e2e-011a-new-session-while-another-session-is-still-streaming" tabindex="-1">E2E-011a: New session while another session is still streaming <a class="header-anchor" href="#e2e-011a-new-session-while-another-session-is-still-streaming" aria-label="Permalink to &quot;E2E-011a: New session while another session is still streaming&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Provider configured; session A is streaming a long response (composer shows the stop/abort control).</li><li><strong>Steps</strong>: 1) While A is still streaming, click New task / New chat. 2) Observe the fresh session&#39;s composer. 3) Type a prompt and send it while A continues streaming in the background. 4) Let A finish and observe the fresh session&#39;s composer again.</li><li><strong>Expected</strong>: The previous streaming transcript leaves the screen on the first frame. The new session immediately shows the empty home and the idle Send control (never a stuck stop/abort control) and its textarea is enabled; the prompt sends and streams normally while A keeps running in the background. When A ends, its cross-session <code>agent_end</code> does not change the new session&#39;s composer state, which remains idle with the Send control.</li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code> (§11.4), <code>04-ux/09-interaction-patterns.md</code> (§1.6, §11)</li><li><strong>Acceptance</strong>: C (session isolation, chat &amp; stream)</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Unit-covered (<code>composer-send-state.test.mjs</code>); pending-admission component/store user path covered (<code>queue-pending-actions.test.mjs</code>); full UI scenario Draft</li></ul><h4 id="e2e-011b-create-a-new-session-from-a-retained-project-group" tabindex="-1">E2E-011b: Create a new session from a retained project group <a class="header-anchor" href="#e2e-011b-create-a-new-session-from-a-retained-project-group" aria-label="Permalink to &quot;E2E-011b: Create a new session from a retained project group&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Provider configured; at least one retained project is visible in the sidebar; the current conversation may be idle or streaming.</li><li><strong>Steps</strong>: 1) Click the project group&#39;s New session control. 2) Wait for the project conversation to load. 3) Type a prompt and inspect the Send control. 4) Send without clicking New session again.</li><li><strong>Expected</strong>: Project activation commits as one renderer navigation flow. Reuse is decided from the in-memory list plus renderer empty signals (<code>messageCount</code>, live rows, running flag, submitted drafts), not a blocking <code>session.list</code>. If the latest session is empty, the existing row is selected on the first frame; if it is non-empty, the empty home replaces the previous transcript immediately and one durable empty session is created from <code>session.create</code>. The composer becomes editable with the Send control enabled as soon as the destination is selected; an earlier project&#39;s background turn cannot leave it disabled. Repeating the click while the slot is empty selects the same row and creates no duplicate.</li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code>, <code>04-ux/09-interaction-patterns.md</code> (§1.6), <code>04-ux/08-component-spec.md</code> (§11.4)</li><li><strong>Acceptance</strong>: C (project session creation and send readiness)</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Source-level regression covered (<code>app-store-sidebar.test.mjs</code>); full UI scenario Draft</li></ul><h4 id="e2e-011c-session-scoped-composer-drafts" tabindex="-1">E2E-011c: Session-scoped composer drafts <a class="header-anchor" href="#e2e-011c-session-scoped-composer-drafts" aria-label="Permalink to &quot;E2E-011c: Session-scoped composer drafts&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Provider configured; sessions A and B exist; A has a transcript so it uses the docked composer; B is an empty session so it uses the home composer; the composer is visible and both sessions are idle.</li><li><strong>Steps</strong>: 1) Select A and type a prompt without sending it. 2) Switch to B and inspect the composer. 3) Type a different prompt in B, then switch back to A. 4) Create a new session and inspect its composer. 5) Return to B and then delete B; revisit the remaining sessions and the home composer if it is available. 6) Type in A, add a workspace file through <code>@</code> and a pasted scratch file, open Settings (or Plugins), then return to chat. Confirm both file chips and the surrounding text remain. Change the workspace of the same draft and confirm only its workspace-relative reference is removed. 7) Hide the app window and show it again with an unsent draft in A.</li><li><strong>Expected</strong>: B initially shows an empty composer, A restores its original unsent prompt, and the new session starts empty rather than inheriting A or B. Each session keeps only its own draft (including file-reference chips) across empty-home ↔ docked remounts. Deleting B removes its cached draft. If a prompt is sent while its request is in flight and the user switches sessions, successful completion clears only the submitting session&#39;s draft and never clears the destination composer. The draft in A is still present after the Settings/Plugins round-trip and after the window is hidden and shown (D301).</li><li><strong>Specs linked</strong>: <code>04-ux/09-interaction-patterns.md</code></li><li><strong>Acceptance</strong>: C (session isolation and composer input)</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Source-level regression covered (<code>composer-draft-cache.test.mjs</code>); real React remount and workspace-change coverage in <code>scripts/e2e/composer-paste.tsx</code> (<code>pnpm test:e2e:composer-paste</code>); full UI scenario Draft</li></ul><h4 id="e2e-011d-new-task-creates-an-immediate-durable-empty-slot" tabindex="-1">E2E-011d: New task creates an immediate durable empty slot <a class="header-anchor" href="#e2e-011d-new-task-creates-an-immediate-durable-empty-slot" aria-label="Permalink to &quot;E2E-011d: New task creates an immediate durable empty slot&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Provider configured; at least one real session exists so the sidebar history is non-empty.</li><li><strong>Steps</strong>: 1) Invoke New Task from the sidebar, the top bar, or Cmd/Ctrl+N inside a retained project and in the temporary scope. 2) Inspect the sidebar history and the composer. 3) Click the same group&#39;s New Task control several times quickly. 4) Type a message and send it. 5) Inspect the sidebar history again and repeat in a different project group.</li><li><strong>Expected</strong>: When the group&#39;s latest session is non-empty, the empty home replaces the previous transcript on the first frame, then one new row is persisted from <code>session.create</code> (without a blocking <code>session.list</code> / <code>session.get</code> round-trip) and selected before the first send. Once that row is the group&#39;s latest empty session, repeated clicks select it (or do nothing when already selected) and create no duplicate. Sending updates the same row&#39;s title and message count; the project and temporary groups keep independent slots.</li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code>, <code>04-ux/08-component-spec.md</code> (§11), <code>04-ux/01-ui-ia.md</code> (§5)</li><li><strong>Acceptance</strong>: C (history integrity and group-scoped creation)</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Source-level regression covered (<code>app-store-sidebar.test.mjs</code>, <code>composer-send-state.test.mjs</code>, <code>session-create.test.mjs</code>); full UI scenario Draft</li></ul><h4 id="e2e-011e-empty-session-reuse-is-scoped-to-the-latest-session" tabindex="-1">E2E-011e: Empty-session reuse is scoped to the latest session <a class="header-anchor" href="#e2e-011e-empty-session-reuse-is-scoped-to-the-latest-session" aria-label="Permalink to &quot;E2E-011e: Empty-session reuse is scoped to the latest session&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: One project group contains an older empty session and a newer session with messages; a separate project and the Temporary group are available.</li><li><strong>Steps</strong>: 1) Click New Task in the first project group. 2) Confirm the new row is created even though the older empty session remains. 3) Click New Task again before sending. 4) Repeat in the second project and Temporary group.</li><li><strong>Expected</strong>: The first click creates a new durable row because only the latest session is considered and it is non-empty. The second click reuses that newly created empty row. The older empty row remains untouched, while each other group gets its own independent empty-slot decision.</li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code>, <code>04-ux/01-ui-ia.md</code>, <code>04-ux/08-component-spec.md</code></li><li><strong>Acceptance</strong>: C (session creation and grouping), F (persistence)</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Source-level regression covered; full UI scenario Draft</li></ul><h4 id="e2e-012b-project-memory-persists-only-within-its-project" tabindex="-1">E2E-012b: Project memory persists only within its project <a class="header-anchor" href="#e2e-012b-project-memory-persists-only-within-its-project" aria-label="Permalink to &quot;E2E-012b: Project memory persists only within its project&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: App running with two retained projects and a configured provider.</li><li><strong>Steps</strong>: 1) Open the first project row menu and choose Project memory. 2) Add a memory card, enter a title and project-specific note, then save. 3) Reopen the editor, edit the note, add a second card, remove the first card, and save. 4) Start or continue a chat in the first project and verify the next runtime receives the saved entries as derived context. 5) Switch to the second project and start a chat. 6) Return to the first project and reopen the editor.</li><li><strong>Expected</strong>: The editor loads the saved cards after reopening. The first project&#39;s runtime receives their readable projection as a labelled user-context block; the second project&#39;s runtime does not. An existing legacy plain-text memory opens as one untitled card. Empty memory is valid, removing all cards clears the projection, saving replaces the prior value, and content above 32 KiB is rejected without a partial save. The create dialog&#39;s memory hint is concise and does not imply that memory is shared across projects.</li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code> (§9), <code>03-runtime/04-data-storage.md</code> (§4.1), <code>03-runtime/06-host-rpc-protocol.md</code> (Projects), <code>04-ux/06-settings-ia.md</code> (Project archive), <code>04-ux/08-component-spec.md</code> (Sidebar interactions)</li><li><strong>Acceptance</strong>: C (chat/stream), D (project UI), F (persistence), Localization</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit/source covered; full provider/UI journey Draft</li></ul><h4 id="e2e-011f-send-while-running-queues-per-session-prompts-and-supports-send-now" tabindex="-1">E2E-011f: Send while running queues per-session prompts and supports Send now <a class="header-anchor" href="#e2e-011f-send-while-running-queues-per-session-prompts-and-supports-send-now" aria-label="Permalink to &quot;E2E-011f: Send while running queues per-session prompts and supports Send now&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Provider configured; session A can produce a delayed response with at least one completed tool batch; session B exists and is idle.</li><li><strong>Steps</strong>: 1) Send a long-running prompt in A. 2) While A is running, verify the composer has exactly one submit button: with an empty draft it is Stop (<code>aria-label=&quot;Stop generating&quot;</code>) and no Send button is present; type a draft and verify the same slot becomes Send (<code>aria-label=&quot;Send&quot;</code>) with no Stop button present. Send two more prompts and inspect the queue above the composer. 3) Remove the second queued row and switch to B. 4) Send a prompt in B, then return to A before either run completes. 5) Choose Send now on A&#39;s remaining queued row, then choose Send now on a second row. 6) Observe A through the current tool/reply boundary and then the next turn. 7) Start another run in A, clear the draft to expose the single Stop button, press Stop, and inspect the queue. 8) Repeat with two queued prompts, let the active turn finish without Send now, and delay its host <code>session.endTurn</code> response until after <code>agent_end</code> is delivered. Release finalization and observe both follow-ups through their turn boundaries. Repeat with a provider error and an immediate abort. 9) With waiting rows, use move up and move down and confirm the persisted order follows. 10) Edit a waiting row while the composer holds text, then with an empty composer. 11) Delay Host queue admission. Attempt to edit/remove the pending row, release admission, then edit/remove the durable row. Repeat with a captured file-reference draft. 12) Withhold a local Host queue-save request until its existing RPC deadline expires, then retry.</li><li><strong>Expected</strong>: The single submit slot contains exactly one button in every state: disabled Send while idle and empty, enabled Send while running with content (which queues the prompt), and Stop while running with an empty draft. A&#39;s two prompts appear in FIFO order, the removed row never sends, and B&#39;s queue remains independent. Send now requests a graceful stop: the current batch completes with a normal <code>agent_end</code>/completed turn, then the promoted rows are delivered in the order they were promoted, before any waiting row, without <code>AGENT_BUSY</code>: the first starts the turn and the rest join it as adjacent user messages, so the model answers once for the whole block. remove, and its Send now button reads as already decided; promotion is one-way. Move up/down swaps only waiting rows, never crosses the promoted block, and persists. Edit is refused with a visible message while the input is non-empty, and otherwise removes the row and returns its text plus its file-reference chips to the composer. Immediate Stop aborts the current reply and preserves A&#39;s queued row; switching sessions preserves both queues. Ordinary completion, provider failure, and abort all resume queued sending automatically after durable finalization releases the session. No queued prompt starts while finalization is pending; each subsequent turn starts once in FIFO order, without another click or session switch. Repeated terminal handling does not double-dispatch. Other sessions&#39; queues remain unchanged, and quitting while finalization is pending preserves queued work without starting another turn. Pending-admission rows disable all five actions, explain Saving in each tooltip, show Saving on Send now, and remain visible without restoring a second draft. Once acknowledged, remove deletes the durable row and edit restores exactly its captured draft; neither row reappears after refresh. A local Host admission timeout clears the pending row, reports the error, and restores the rejected composer draft; retry can queue normally.</li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code> (§5.6), <code>04-ux/08-component-spec.md</code> (§11), <code>04-ux/09-interaction-patterns.md</code> (§3.4), ADR 0118, ADR 0213, ADR 0265</li><li><strong>Acceptance</strong>: C (chat, stream, and session isolation), Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Source-level regression and deterministic desktop finalization / Agent Host integration covered (<code>queued-turn-finalization.test.mjs</code>); promotion, reorder, and edit contracts covered at source level (<code>composer-send-state.test.mjs</code>); full UI scenario Draft</li></ul><h4 id="e2e-queue-promote-orders-delivery-by-click-two-send-now-clicks-deliver-in-click-order" tabindex="-1">E2E-QUEUE-promote-orders-delivery-by-click: Two Send now clicks deliver in click order <a class="header-anchor" href="#e2e-queue-promote-orders-delivery-by-click-two-send-now-clicks-deliver-in-click-order" aria-label="Permalink to &quot;E2E-QUEUE-promote-orders-delivery-by-click: Two Send now clicks deliver in click order&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Provider configured; session A is running a turn with at least one completed tool batch; three prompts are queued behind it. the first queued row. 3) Confirm the promoted block orders third → first, that both rows lock their move/edit/remove actions, and that the remaining row is still editable. 4) Let the boundary pass and observe the transcript.</li><li><strong>Expected</strong>: The first click is delivered first and the second second — the click order is the delivery order, not &quot;last click wins&quot; and not the original queue order. Both rows appear as adjacent user messages in one turn and the model answers once; the queue no longer lists either promoted row. Both promoted rows show as already decided and cannot be edited, removed, or reordered. The waiting row keeps its actions and is not delivered before either promoted row.</li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code> (§5.6), <code>04-ux/08-component-spec.md</code> (§11), ADR 0265</li><li><strong>Acceptance</strong>: C (chat, stream)</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Draft; ordering and the adjacent delivery are covered by <code>turn_queue</code>, <code>turn-queue</code>, and <code>agent-host</code> unit tests</li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code> (§5.6), <code>04-ux/08-component-spec.md</code> (§11), ADR 0265</li><li><strong>Acceptance</strong>: C (chat, stream)</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Draft; underlying ordering covered by <code>turn_queue</code>, <code>turn-queue</code>, and <code>agent-host</code> unit tests</li></ul><h4 id="e2e-queue-reorder-moves-plain-neighbours-move-up-down-reorders-the-waiting-queue" tabindex="-1">E2E-QUEUE-reorder-moves-plain-neighbours: Move up/down reorders the waiting queue <a class="header-anchor" href="#e2e-queue-reorder-moves-plain-neighbours-move-up-down-reorders-the-waiting-queue" aria-label="Permalink to &quot;E2E-QUEUE-reorder-moves-plain-neighbours: Move up/down reorders the waiting queue&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Provider configured; a turn is running with at least three prompts queued and no promoted row.</li><li><strong>Steps</strong>: 1) Move the third row up twice and confirm it becomes first. 2) Move the first row up once and confirm nothing moves. 3) Promote a row, then try to move the adjacent waiting row across it. 4) Reload the renderer and inspect the queue order.</li><li><strong>Expected</strong>: Each move swaps the row with its adjacent waiting neighbour and the Host persists the new order, so a reload reproduces it. The waiting-block boundary and the promoted block are immovable: a move at the block edge is a no-op that never reaches the Host, and no move changes a promoted row&#39;s place.</li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code> (§5.6), ADR 0265</li><li><strong>Acceptance</strong>: C (chat), F (persistence)</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Draft; host-side reorder covered by <code>turn_queue</code> unit tests</li></ul><h4 id="e2e-queue-edit-restores-draft-only-when-input-empty-edit-restores-the-queued-draft" tabindex="-1">E2E-QUEUE-edit-restores-draft-only-when-input-empty: Edit restores the queued draft <a class="header-anchor" href="#e2e-queue-edit-restores-draft-only-when-input-empty-edit-restores-the-queued-draft" aria-label="Permalink to &quot;E2E-QUEUE-edit-restores-draft-only-when-input-empty: Edit restores the queued draft&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Provider configured; a turn is running with a queued prompt that carries text and a file-reference chip; the composer input is empty.</li><li><strong>Steps</strong>: 1) Type a draft, then choose edit on the queued row. 2) Clear the composer and choose edit again. 3) Confirm the queue no longer lists the row and the composer holds the row&#39;s text and its file-reference chip. 4) Send it and compare the transcript with the original queued prompt.</li><li><strong>Expected</strong>: With a non-empty input (or an attachment chip) the edit is refused with a visible message and the row stays queued. With an empty input the row leaves the queue, the Host no longer lists it, and the composer holds the exact text plus the original file-reference chip — not the serialized prompt the Host received. Re-sending produces the same prompt as the queued row would have.</li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code> (§11), ADR 0265</li><li><strong>Acceptance</strong>: C (chat, stream)</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Draft; composer-side contract covered by <code>composer-send-state.test.mjs</code></li></ul><h4 id="e2e-011g-new-task-does-not-leave-the-previous-transcript-on-screen" tabindex="-1">E2E-011g: New Task does not leave the previous transcript on screen <a class="header-anchor" href="#e2e-011g-new-task-does-not-leave-the-previous-transcript-on-screen" aria-label="Permalink to &quot;E2E-011g: New Task does not leave the previous transcript on screen&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Provider configured; session A has a visible transcript (idle or streaming); the group&#39;s latest session is non-empty.</li><li><strong>Steps</strong>: 1) Click New Task (sidebar, top bar, or Cmd/Ctrl+N). 2) Observe the chat surface on the next frame, before the new sidebar row is required to exist. 3) Type in the composer during that interval. 4) Wait for the new row and send.</li><li><strong>Expected</strong>: A&#39;s transcript is gone on the first frame (empty home, idle Send). The composer does not stay on A&#39;s draft. After <code>session.create</code> the same empty session is selected, any text typed during the wait belongs to that session, and sending does not create a second row. Repeating New Task before sending reuses the row. The host is not asked to <code>session.list</code> or <code>session.get</code> before the empty destination is visible.</li><li><strong>Specs linked</strong>: <code>04-ux/09-interaction-patterns.md</code> (§1.6), <code>04-ux/08-component-spec.md</code> (§11), ADR 0154</li><li><strong>Acceptance</strong>: C (session creation), Quality</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Source-level regression covered (<code>session-create.test.mjs</code>, <code>session-switch-performance.test.mjs</code>); full UI scenario Draft</li></ul><h4 id="composer-file-reference-undo-regression" tabindex="-1">Composer file-reference undo regression <a class="header-anchor" href="#composer-file-reference-undo-regression" aria-label="Permalink to &quot;Composer file-reference undo regression&quot;">​</a></h4><ul><li>Add a file reference to an unsent draft, select the chip, delete it, then undo and redo with the native editor shortcuts. Undo must restore both the chip and the file path used by submission; redo must remove both.</li><li>Switch to another chat and back after undo: the other draft stays empty and the source retains the restored reference. Typing a removed chip&#39;s private-use token as plain text must not restore the attachment.</li><li>Automated by <code>pnpm test:e2e:composer-paste</code> (<code>fileReferenceUndoRedo</code>).</li></ul><h3 id="conversation-top-bar" tabindex="-1">Conversation Top Bar <a class="header-anchor" href="#conversation-top-bar" aria-label="Permalink to &quot;Conversation Top Bar&quot;">​</a></h3><h4 id="e2e-087-conversation-top-bar-renders-on-the-chat-route" tabindex="-1">E2E-087: Conversation top bar renders on the chat route <a class="header-anchor" href="#e2e-087-conversation-top-bar-renders-on-the-chat-route" aria-label="Permalink to &quot;E2E-087: Conversation top bar renders on the chat route&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Provider configured; at least one session exists.</li><li><strong>Steps</strong>: 1) Open the chat route. 2) Inspect the 46px bar at the top of the conversation area. 3) Confirm it shows the concise session/task title and the New task / Search action buttons; confirm the sidebar toggle appears <strong>only when the sidebar is collapsed</strong> (when expanded, the sidebar owns that control). 4) Switch to the Pull requests, Scheduled, Plugins, or Settings routes and inspect the same top region.</li><li><strong>Expected</strong>: Every route-owned top region uses the same <code>--ds-toolbar-height</code> (46px), bg-primary surface, and bottom border; Windows/Linux reserve the same 120px native-control band at the right. On the chat route the conversation top bar renders with its title and actions only; it has no model or Agent|Plan|Goal mode control. The left-of-input Composer chip owns the active session&#39;s Agent/Plan/Goal switch, and the Composer-right combined chip owns model and reasoning selection. The task title is the only visible title text and uses the available width, with an ellipsis only on overflow. Check an English title longer than 10 characters in wide and narrow layouts, with the sidebar expanded/collapsed and the work panel open/closed; titles must not overlap the action buttons. The complete title and project scope remain available through its tooltip. The sidebar toggle is present only in the collapsed state (no duplicate of the sidebar&#39;s control). On every other route the frameless drag band renders instead (no chat top-bar controls) while retaining the same surface and alignment. The bar is draggable to move the window; interactive controls do not start a window drag. macOS leaves the left 88px clear for traffic lights only while the sidebar is collapsed (8px in fullscreen); Windows/Linux leave the right 120px clear for native window controls.</li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code> (§2 Topbar)</li><li><strong>Acceptance</strong>: C (send/UI), Quality</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Draft</li></ul><h4 id="e2e-087a-destination-page-headers-clear-the-titlebar-band-on-macos" tabindex="-1">E2E-087a: Destination page headers clear the titlebar band on macOS <a class="header-anchor" href="#e2e-087a-destination-page-headers-clear-the-titlebar-band-on-macos" aria-label="Permalink to &quot;E2E-087a: Destination page headers clear the titlebar band on macOS&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: macOS build; at least one plugin installed.</li><li><strong>Steps</strong>: 1) Open the Plugins route with the window at its default size. 2) Inspect the top of the page: the &quot;Plugins&quot; title row, its primary action, and the overflow menu button. 3) Scroll the page to the top and confirm no page content is hidden behind the 46px band. 4) Repeat on the Scheduled and Pull requests routes. 5) Open a plugin&#39;s detail sheet and inspect its head.</li><li><strong>Expected</strong>: The page header renders fully below the frameless drag band on macOS as it already does on Windows/Linux: the title row is not clipped, and the Installed / Marketplace segmented control and search field sit at their intended offset instead of at the window&#39;s top edge. <code>.page-frame</code> reserves <code>--ds-toolbar-height</code> plus an 8px buffer on darwin, win32, and linux alike. The plugin detail sheet stacks above the band (<code>z-index: 60</code>) and keeps its own head at the top edge, with its close button opting out of the drag rectangle.</li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code> (§2.3 Layout)</li><li><strong>Acceptance</strong>: C (UI), Quality</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Source-level regression covered (<code>apps/desktop/test/plugins-page-style.test.mjs</code>); full UI scenario Draft</li></ul><h4 id="e2e-088-composer-agent-plan-goal-chip-updates-the-session" tabindex="-1">E2E-088: Composer Agent/Plan/Goal chip updates the session <a class="header-anchor" href="#e2e-088-composer-agent-plan-goal-chip-updates-the-session" aria-label="Permalink to &quot;E2E-088: Composer Agent/Plan/Goal chip updates the session&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Chat route active; a session selected.</li><li><strong>Steps</strong>: 1) Click the left-of-input Composer mode chip to enter Plan. 2) Send a prompt that would normally require Write/Edit and observe behavior. 3) Click the same Composer chip to return to Agent. 4) Begin a turn and try to toggle mode mid-run or while a pending Plan approval is visible.</li><li><strong>Expected</strong>: The Composer chip updates the active session <code>mode</code> (Plan and Goal hard-deny Write/Edit and plugin tools while Bash follows the selected permission mode; Agent allows its normal tools per permission settings). The chip is disabled while a turn or active pending approval exists and re-enables after the session returns idle/planning. No top-bar mode control is rendered.</li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code> (§2, §11), <code>03-runtime/03-tools-and-permissions.md</code> (§10), <code>03-runtime/04-data-storage.md</code> (§8)</li><li><strong>Acceptance</strong>: C, E</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Draft</li></ul><h4 id="e2e-088a-composer-configuration-controls-survive-project-session-initialization" tabindex="-1">E2E-088a: Composer configuration controls survive project/session initialization <a class="header-anchor" href="#e2e-088a-composer-configuration-controls-survive-project-session-initialization" aria-label="Permalink to &quot;E2E-088a: Composer configuration controls survive project/session initialization&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Provider configured; a new project or new session flow is visible while the destination <code>activeSessionId</code> is still resolving.</li><li><strong>Steps</strong>: 1) Inspect the Composer mode, model × reasoning, and permission controls during the empty/home transition. 2) Click the mode control and confirm it advances to the next mode. 3) Open the combined chip, enter the Reasoning level submenu, and select a supported level. 4) Open permission mode and select Auto. 5) Inspect the destination session after navigation completes.</li><li><strong>Expected</strong>: None of the idle configuration triggers is disabled merely because the destination session has not been projected yet. New Task has already selected or created the durable empty row, and the first configuration action applies to that session without waiting for a message; no second click is required. Running turns and pending approvals still disable the controls. Behind that transition the chat area follows the cold-switch rule: the currently visible pane keeps showing its own session until the destination commits, the only wait affordance is the thin progress track, nothing is dimmed, and prompt submission stays inert until the visible pane is the active session, so a prompt cannot reach the session being left. In zh-CN/zh-TW, the Composer permission menu renders the Accept edits option as <code>允许编辑</code> / <code>允許編輯</code> and keeps it on one line.</li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code> (§11), <code>04-ux/09-interaction-patterns.md</code> (§5A), ADR 0137</li><li><strong>Acceptance</strong>: C (new project/session composer)</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Draft</li></ul><h4 id="e2e-088b-composer-placeholder-guidance-follows-page-and-session-context" tabindex="-1">E2E-088b: Composer placeholder guidance follows page and session context <a class="header-anchor" href="#e2e-088b-composer-placeholder-guidance-follows-page-and-session-context" aria-label="Permalink to &quot;E2E-088b: Composer placeholder guidance follows page and session context&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: English and zh-CN locales are available; a provider is configured; at least one Skill is active; both an empty home and two conversations can be opened.</li><li><strong>Steps</strong>: 1) On empty home, record the welcome placeholder and wait longer than 4 seconds to confirm it is unchanged. 2) Open conversation A, record its guidance, type and clear text, focus and blur the textarea, and wait; confirm the copy is unchanged. 3) Switch to conversation B and then back to A, recording each guidance change. 4) Switch between home and a conversation and inspect the command/file and keyboard hints. 5) Type <code>/</code> and inspect the slash menu. Include Skills with long English/CJK descriptions, short/no descriptions, a separate title/argument hint, and an exceptionally long slash name; also inspect a long filename in <code>@</code> mode. Repeat at 1040px and 1680px viewport widths with 320px and 640px composer widths. 6) Switch to zh-CN and repeat the context-switch checks.</li><li><strong>Expected</strong>: The initially rendered context starts with its welcome copy and stays stable until the page/session context changes. Each context switch advances to the next localized command/file or keyboard hint with an opacity fade; no timer-driven changes occur. The keyboard hint includes Shift+Enter and a submit hint, while the command/file hint includes <code>/</code> and <code>@</code>. The slash menu still contains <code>/new</code>, <code>/compact</code>, <code>/agent-mode</code>, <code>/plan-mode</code>, and <code>/goal-mode</code>, followed by a Skills group at the bottom. Selecting the Skill inserts its slash id; sending it keeps the typed command chip visible and the model calls <code>Skill</code> with that id before answering. zh-CN shows the matching localized copy, including <code>Shift+Enter for newline · Use Send to submit</code>. Long descriptions use only the space remaining after command names and hints, so short names stay fully visible. Descriptions and oversized names ellipsize within the row without horizontal overflow; file names retain the available row width. Name highlights and click acceptance with input focus retained remain intact.</li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code> (§11), <code>04-ux/04-builtin-commands.md</code> (§7–8)</li><li><strong>Acceptance</strong>: C (send/UI), Localization, Quality</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Source-covered (<code>composer-placeholder-context.test.mjs</code>); slash-menu layout covered by <code>pnpm test:e2e:composer-autocomplete</code> after <code>pnpm build:js</code> (real React/Chromium and production CSS, deterministic command fixtures; no provider required). Full provider/session scenario Draft; branch runs do not replace post-integration E2E</li></ul><h4 id="e2e-089-composer-model-menu-opens-upward-and-switches-model" tabindex="-1">E2E-089: Composer model menu opens upward and switches model <a class="header-anchor" href="#e2e-089-composer-model-menu-opens-upward-and-switches-model" aria-label="Permalink to &quot;E2E-089: Composer model menu opens upward and switches model&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Chat route active; provider configured.</li><li><strong>Steps</strong>: 1) Click the Composer-right model × reasoning chip. 2) Confirm the menu opens upward from the bottom composer. 3) Enter Model, select a different provider/model, and return to the root. 4) Enter Reasoning level and select a supported level. 5) Open Settings from the command palette or application menu.</li><li><strong>Expected</strong>: The trigger uses a Bot icon while retaining the current model and reasoning labels. The root shows only Model and Reasoning level entries. The Model submenu lists enabled runnable providers and only the model bindings saved for each provider, with each model row visibly indented beneath its provider heading. Cached or freshly discovered models may supply display names and metadata for those bindings, but unconfigured discovery results are absent; configured IDs remain available when discovery is unavailable. The Reasoning level submenu lists only the selected model&#39;s published levels. Selecting updates the active session model/reasoning configuration without dismissing the menu; Settings opens from the command palette/menu. The Composer model trigger ellipsizes long IDs. Each option shows one display name only, and hovering a long option exposes its complete display name in the tooltip without changing the menu layout or adding a visible model ID.</li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code> (§11, model menu), <code>03-runtime/13-model-catalog-and-selection.md</code></li><li><strong>Acceptance</strong>: C</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Draft</li></ul><h4 id="e2e-composer-narrow-controls-composer-controls-adapt-to-a-narrow-chat-column" tabindex="-1">E2E-COMPOSER-narrow-controls: Composer controls adapt to a narrow chat column <a class="header-anchor" href="#e2e-composer-narrow-controls-composer-controls-adapt-to-a-narrow-chat-column" aria-label="Permalink to &quot;E2E-COMPOSER-narrow-controls: Composer controls adapt to a narrow chat column&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Chat route active; a configured model is selected; the composer is rendered in both empty-home and thread-docked variants.</li><li><strong>Steps</strong>: 1) Set the Composer container to 560px and inspect the combined model × reasoning trigger. 2) Set it to 480px, then 450px. 3) At each width, inspect the mode, permission, context, enhancement, and Send/Stop controls; open the model menu from the narrow trigger; repeat the visual check in the other Composer variant.</li><li><strong>Expected</strong>: The toolbar remains one non-wrapping row and does not overflow its container. At 560px the reasoning label and separator yield first; at 480px the model label is further capped; at the 450px floor the combined model × reasoning trigger is a 32px icon-only control. The full selection is still available through the trigger&#39;s menu, tooltip, and accessible name. Mode and permission labels remain single-line and ellipsized, the context ring and action controls retain usable hit targets, and the single Send/Stop slot remains reachable. Home and thread-docked composers match.</li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code> (§11)</li><li><strong>Acceptance</strong>: C (send/UI), Quality</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Automated on task candidate <code>737435248ebd32e0b2a406f93b7b245784bc4289</code> (base <code>cea6e02c</code>): <code>pnpm test:e2e:layout</code> 167/167 including the 450px model-chip assertion; <code>pnpm test:e2e:composer-autocomplete</code> and <code>pnpm test:e2e:composer-paste</code> passed. Source-covered by <code>composer-responsive.test.mjs</code>.</li></ul><h4 id="e2e-090-transcript-bottom-reserve-tracks-the-docked-composer-height" tabindex="-1">E2E-090: Transcript bottom reserve tracks the docked composer height <a class="header-anchor" href="#e2e-090-transcript-bottom-reserve-tracks-the-docked-composer-height" aria-label="Permalink to &quot;E2E-090: Transcript bottom reserve tracks the docked composer height&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Chat route active; a session with a transcript that exceeds one viewport so the last message sits near the docked composer.</li><li><strong>Steps</strong>: 1) Scroll the transcript to the latest message. 2) Measure the vertical gap between the last message and the top of the docked composer. 3) Type several lines into the composer so the draft grows multi-line. 4) Re-measure the gap and confirm the last message is still fully visible above the composer (not overlapped). 5) Collapse the draft back to a single line and confirm the gap shrinks back toward the tight ~16px reserve.</li><li><strong>Expected</strong>: The last message sits close above the composer (a small, consistent gap) rather than far below it; the reserve follows the composer&#39;s real height via <code>--composer-dock-height</code> so a taller multi-line draft pushes the transcript up instead of covering it. The jump-to-latest button and the minimap stay anchored just above the composer at every draft height.</li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code> (§4.3 MainChat layout)</li><li><strong>Acceptance</strong>: C (send/UI), Quality</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Draft</li></ul><h4 id="e2e-144-sending-a-prompt-keeps-the-transcript-at-the-latest-turn" tabindex="-1">E2E-144: Sending a prompt keeps the transcript at the latest turn <a class="header-anchor" href="#e2e-144-sending-a-prompt-keeps-the-transcript-at-the-latest-turn" aria-label="Permalink to &quot;E2E-144: Sending a prompt keeps the transcript at the latest turn&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Chat route active; the selected session contains enough history to overflow the transcript viewport; the transcript is at the latest message or has been scrolled upward.</li><li><strong>Steps</strong>: 1) Send a prompt from the bottom of the viewport. 2) Observe the transcript from the first send state through the persisted user-message event and the first streamed row. 3) Repeat with a multi-line draft so the composer collapses when the draft clears, and again after manually scrolling upward before sending.</li><li><strong>Expected</strong>: Send immediately hides the jump control and re-pins the transcript in the layout phase. The historical rows move upward only as the new turn is added; the viewport never flashes to the top of the conversation, and the new user turn plus streamed response remain visible at the bottom. The composer collapse and indicator layout clamps after send never release follow mode (no &quot;↓ Scroll to bottom&quot; button appears while the turn streams unless the user actually scrolled with an input device).</li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code> (§4.3, §4.4), <code>04-ux/09-interaction-patterns.md</code> (§9.1, §10.4)</li><li><strong>Acceptance</strong>: C (send/UI), Quality</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Draft</li></ul><h3 id="workspace-open" tabindex="-1">Workspace Open <a class="header-anchor" href="#workspace-open" aria-label="Permalink to &quot;Workspace Open&quot;">​</a></h3><h4 id="e2e-012-open-a-project-directory" tabindex="-1">E2E-012: Open a project directory <a class="header-anchor" href="#e2e-012-open-a-project-directory" aria-label="Permalink to &quot;E2E-012: Open a project directory&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: App running; no project open.</li><li><strong>Steps</strong>: 1) Open project directory via UI. 2) Select a local folder.</li><li><strong>Expected</strong>: Project path displayed; tool paths resolve relative to project root.</li><li><strong>Specs linked</strong>: <code>03-runtime/15-workspace-ignore-rules.md</code></li><li><strong>Acceptance</strong>: D (open project, show path)</li><li><strong>Milestone</strong>: M3</li><li><strong>Status</strong>: Draft</li></ul><h4 id="e2e-012a-create-a-named-project-from-multiple-folders" tabindex="-1">E2E-012a: Create a named project from multiple folders <a class="header-anchor" href="#e2e-012a-create-a-named-project-from-multiple-folders" aria-label="Permalink to &quot;E2E-012a: Create a named project from multiple folders&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: App running; no project dialog open; at least two local folders are available, including one with a long name or path.</li><li><strong>Steps</strong>: <ol><li>Invoke Add project from Settings → Project archive or the sidebar Projects heading and inspect the empty dialog.</li><li>Add two folders with the folder picker without typing a name. Confirm the name field seeds with the first folder&#39;s name, both rows render, and the first row is marked Primary.</li><li>Remove one row, check the count, and add it again.</li><li>Inspect the empty and populated states in light and dark themes, including a narrow window and reduced-motion settings.</li><li>Use Tab and Shift+Tab to traverse the controls. Close with Escape, then reopen and close by clicking outside; check focus after each close.</li><li>Reopen, enter the name, add the folders, and create the project. Inspect the in-flight controls and the resulting active primary workspace and one grouped project entry with both roots.</li><li>Start a session in the group and ask the agent to read a file using the additional root&#39;s absolute path; then try an unrelated outside path.</li></ol></li><li><strong>Expected</strong>: The dialog traps focus, closes on Escape or outside click while idle, and keeps the name and selected folders visible without horizontal overflow. The native picker allows multiple directories in one selection. Removing a folder updates the count and never removes another row. Create needs one folder and no typed name; an empty name field falls back to the first folder&#39;s name. On creation one logical project group receives the entered display name; its primary folder becomes the active workspace and every selected folder is retained as a group root. The Project archive shows one group row, and its sessions, shared instructions, and shared memory use the group identity. Read/Glob/Grep/ Write/Edit can use an explicitly addressed additional root only after host canonical containment; an unrelated outside path still follows the normal permission flow. The dialog is unavailable while creation is in flight and returns focus to the invoking control after close. The surface follows the shell&#39;s neutral gray theme with a 480px maximum width, 18px tokenized corners, shared dialog elevation, and the global compact type ramp. The title uses the dialog-level heading size, the name field uses the body/input size, and labels/metadata remain on the smaller global steps. The header and action row use the shared 18px dialog gutter; sections use a 16px gap. It shows one Create project title, a filled name field without duplicate placeholder copy, a compact local source chip, and a softly filled Add folder action. It does not add explanatory memory or multi-selection copy. The workspace section keeps the current local folder selection behavior while staying neutral about future remote sources. No outer stroke, section rules, footer divider, or dashed picker border appears. Spacing provides the section hierarchy; long names and paths remain contained, and scrolling content never hides the fixed action row. Both themes keep text readable and keyboard focus visible; reduced motion suppresses the control transitions. The source chip is the extension point for a future remote project source; the current flow remains local-only.</li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code> (§9), <code>04-ux/06-settings-ia.md</code> (Project archive), <code>04-ux/07-ui-design-system.md</code>, <code>04-ux/08-component-spec.md</code> (§3.5)</li><li><strong>Acceptance</strong>: C (project creation UI), D (multi-folder project setup), Accessibility, Localization</li><li><strong>Milestone</strong>: M3</li><li><strong>Status</strong>: Source-level regression covered; full UI scenario Draft</li></ul><h4 id="e2e-013-read-only-tools-work-in-project" tabindex="-1">E2E-013: Read-only tools work in project <a class="header-anchor" href="#e2e-013-read-only-tools-work-in-project" aria-label="Permalink to &quot;E2E-013: Read-only tools work in project&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Project directory open.</li><li><strong>Steps</strong>: 1) Ask agent to read a file in the project. 2) Observe result.</li><li><strong>Expected</strong>: <code>Read</code> returns immediately within project scope. In Agent mode, the agent activates <code>Glob</code> or <code>Grep</code> through <code>ToolSearch</code> before using it; Plan keeps its read/search core available from the first request. All results remain within project scope.</li><li><strong>Specs linked</strong>: <code>03-runtime/03-tools-and-permissions.md</code></li><li><strong>Acceptance</strong>: E (Read/Glob/Grep work), D (tools based on project)</li><li><strong>Milestone</strong>: M3</li><li><strong>Status</strong>: Automated (protocol smoke: Read + Glob in sample project)</li></ul><h3 id="permission-allow-deny-timeout" tabindex="-1">Permission Allow / Deny / Timeout <a class="header-anchor" href="#permission-allow-deny-timeout" aria-label="Permalink to &quot;Permission Allow / Deny / Timeout&quot;">​</a></h3><h4 id="e2e-014-write-edit-bash-triggers-permission-card" tabindex="-1">E2E-014: Write/Edit/Bash triggers permission card <a class="header-anchor" href="#e2e-014-write-edit-bash-triggers-permission-card" aria-label="Permalink to &quot;E2E-014: Write/Edit/Bash triggers permission card&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Agent mode; project open.</li><li><strong>Steps</strong>: 1) Ask agent to write a file. 2) Observe permission card.</li><li><strong>Expected</strong>: Permission card appears inline in the originating transcript with tool name, workspace, arguments preview, countdown, and allow/deny options. It creates no backdrop or modal and does not cover another session.</li><li><strong>Specs linked</strong>: <code>04-ux/03-permission-ux.md</code>, <code>03-runtime/03-tools-and-permissions.md</code></li><li><strong>Acceptance</strong>: E (Write/Edit/Bash trigger confirmation)</li><li><strong>Milestone</strong>: M3</li><li><strong>Status</strong>: Draft</li></ul><h4 id="e2e-015-denied-permission-blocks-execution" tabindex="-1">E2E-015: Denied permission blocks execution <a class="header-anchor" href="#e2e-015-denied-permission-blocks-execution" aria-label="Permalink to &quot;E2E-015: Denied permission blocks execution&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Permission card displayed.</li><li><strong>Steps</strong>: 1) Click deny on permission card. 2) Observe agent response.</li><li><strong>Expected</strong>: Tool not executed; agent receives denied result; no file changed.</li><li><strong>Specs linked</strong>: <code>03-runtime/03-tools-and-permissions.md</code></li><li><strong>Acceptance</strong>: E (denied → not executed)</li><li><strong>Milestone</strong>: M3</li><li><strong>Status</strong>: Draft</li></ul><h4 id="e2e-016-allowed-permission-executes-tool" tabindex="-1">E2E-016: Allowed permission executes tool <a class="header-anchor" href="#e2e-016-allowed-permission-executes-tool" aria-label="Permalink to &quot;E2E-016: Allowed permission executes tool&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Permission card displayed.</li><li><strong>Steps</strong>: 1) Click allow on permission card. 2) Observe agent response and UI.</li><li><strong>Expected</strong>: Tool executed; result returned to model and displayed in UI; file modified.</li><li><strong>Specs linked</strong>: <code>03-runtime/03-tools-and-permissions.md</code></li><li><strong>Acceptance</strong>: E (allowed → result returned)</li><li><strong>Milestone</strong>: M3</li><li><strong>Status</strong>: Draft</li></ul><h4 id="e2e-017-permission-timeout-defaults-to-deny" tabindex="-1">E2E-017: Permission timeout defaults to deny <a class="header-anchor" href="#e2e-017-permission-timeout-defaults-to-deny" aria-label="Permalink to &quot;E2E-017: Permission timeout defaults to deny&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Permission card displayed; no user action.</li><li><strong>Steps</strong>: 1) Wait 120 seconds without responding to permission card. 2) Observe outcome.</li><li><strong>Expected</strong>: Permission auto-denied after timeout; tool not executed.</li><li><strong>Specs linked</strong>: <code>03-runtime/03-tools-and-permissions.md</code></li><li><strong>Acceptance</strong>: E (timeout → deny)</li><li><strong>Milestone</strong>: M3</li><li><strong>Status</strong>: Draft</li></ul><h4 id="e2e-018-plan-denies-workspace-mutation-and-plugin-tools" tabindex="-1">E2E-018: Plan denies workspace mutation and plugin tools <a class="header-anchor" href="#e2e-018-plan-denies-workspace-mutation-and-plugin-tools" aria-label="Permalink to &quot;E2E-018: Plan denies workspace mutation and plugin tools&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Plan mode active with Auto selected and a plugin agent tool registered.</li><li><strong>Steps</strong>: 1) Ask the Agent to call Write, Edit, and the plugin tool. 2) Ask it to run a Bash command that creates a marker file. 3) Repeat the Bash call with Ask selected and inspect the permission card.</li><li><strong>Expected</strong>: Write, Edit, and the plugin tool are not visible and direct attempts return <code>WRITE_DISABLED_IN_PLAN</code>, <code>EDIT_DISABLED_IN_PLAN</code>, or <code>PLUGIN_DISABLED_IN_PLAN</code>; no file is changed by those tools. Bash runs without a confirmation under Auto and may mutate; under Ask it waits for the ordinary permission card. No Chat-mode error or command exists.</li><li><strong>Specs linked</strong>: <code>03-runtime/03-tools-and-permissions.md</code></li><li><strong>Acceptance</strong>: E (Plan policy)</li><li><strong>Milestone</strong>: M3</li><li><strong>Status</strong>: Documented (M6; E2E execution pending)</li></ul><h4 id="e2e-019-workspace-outside-paths-follow-permission-mode" tabindex="-1">E2E-019: Workspace-outside paths follow permission mode <a class="header-anchor" href="#e2e-019-workspace-outside-paths-follow-permission-mode" aria-label="Permalink to &quot;E2E-019: Workspace-outside paths follow permission mode&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Agent or Plan mode; project open; a readable file exists outside both the session project and scratch roots.</li><li><strong>Steps</strong>: 1) With Ask selected, ask the agent to <code>Read</code> the external file and observe the inline permission card. 2) Deny once and verify no content is returned. 3) Repeat and allow once; verify the tool result carries <code>root: &quot;external&quot;</code> and the canonical absolute path. 4) Switch to Auto and repeat with <code>Grep</code> or <code>Glob</code>; verify no card appears and the bounded result returns. 5) Repeat with Accept edits; verify the external read/search still asks for permission.</li><li><strong>Expected</strong>: An explicit outside path never hard-fails before the user can decide. Ask and Accept edits request permission; Auto executes. Denial, timeout, or cancellation returns <code>TOOL_DENIED</code> and performs no operation.</li><li><strong>Specs linked</strong>: <code>03-runtime/03-tools-and-permissions.md</code>, <code>03-runtime/15-workspace-ignore-rules.md</code>, <code>04-ux/03-permission-ux.md</code></li><li><strong>Acceptance</strong>: E (workspace-outside permission policy)</li><li><strong>Milestone</strong>: M3</li><li><strong>Status</strong>: Automated (host-core protocol/unit coverage; desktop journey pending)</li></ul><h4 id="e2e-019e-bounded-search-parameters-stay-portable-across-platforms" tabindex="-1">E2E-019e: Bounded search parameters stay portable across platforms <a class="header-anchor" href="#e2e-019e-bounded-search-parameters-stay-portable-across-platforms" aria-label="Permalink to &quot;E2E-019e: Bounded search parameters stay portable across platforms&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Agent or Plan mode; project open; the host tool catalog is available on macOS, Linux, or Windows.</li><li><strong>Steps</strong>: 1) Activate <code>Glob</code>/<code>Grep</code> when deferred and inspect their schemas. 2) Search with workspace-relative <code>path</code>, <code>include</code>, <code>headLimit</code>, and <code>outputMode: &quot;filesWithMatches&quot;</code> or <code>&quot;count&quot;</code>, first with a directory and then one explicit file. 3) Call <code>Read</code> with a directory and follow its structured Glob suggestion. 4) Repeat with the platform&#39;s native shell selected, without changing the tool arguments.</li><li><strong>Expected</strong>: The schemas expose the same bounded search controls on every platform; <code>Read</code> declares file-only input, <code>Glob</code> declares directory input, and <code>Grep</code> accepts a file or directory. <code>filesWithMatches</code> is accepted as the canonical output mode. A directory Read returns <code>INVALID_ARGUMENT</code> with <code>suggestedTool=Glob</code> and bounded args; the corrected call succeeds. Search results use workspace-relative paths inside the project and absolute paths only for approved external locations. No shell-specific path syntax is required, workspace-relative paths use <code>/</code> for platform separators while literal backslashes in POSIX filenames remain intact, and oversized results remain bounded.</li><li><strong>Specs linked</strong>: <code>03-runtime/03-tools-and-permissions.md</code>, <code>03-runtime/16-tool-result-limits.md</code>, ADR 0057, ADR 0069</li><li><strong>Acceptance</strong>: E (bounded cross-platform search)</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (host-core and agent-runtime); live multi-platform protocol capture pending. The workspace-relative path expectation is covered on Windows by <code>relative_display</code>, which must canonicalize the workspace root with the resolver&#39;s own spelling (<code>simple_canonicalize</code>) — std <code>Path::canonicalize</code> keeps the <code>\\\\?\\</code> prefix there and silently degrades every label to an absolute path.</li></ul><h4 id="e2e-019a-scratch-directory-writes-stay-out-of-the-workspace-d114" tabindex="-1">E2E-019a: Scratch-directory writes stay out of the workspace (D114) <a class="header-anchor" href="#e2e-019a-scratch-directory-writes-stay-out-of-the-workspace-d114" aria-label="Permalink to &quot;E2E-019a: Scratch-directory writes stay out of the workspace (D114)&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Agent mode; project open; session started.</li><li><strong>Steps</strong>: 1) Ask the agent to produce a temporary/intermediate file (e.g. a one-off script). 2) Observe where it writes and whether a permission card appears. 3) Check <code>git status</code> and the work-panel state. 4) Delete the session and check <code>&lt;data_dir&gt;/scratch/</code>.</li><li><strong>Expected</strong>: The file lands under <code>&lt;data_dir&gt;/scratch/&lt;sessionId&gt;/</code> without a permission card; project <code>git status</code> stays clean; no file or Review artifact tab opens for the scratch write; deleting the session removes the scratch directory.</li><li><strong>Specs linked</strong>: <code>03-runtime/03-tools-and-permissions.md §4b</code>, <code>03-runtime/04-data-storage.md</code></li><li><strong>Acceptance</strong>: E (temp files isolated from workspace)</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Partially automated (host-core unit tests: dual-root resolve, scratch write/read, PI_SCRATCH_DIR, sweep)</li></ul><h4 id="e2e-019b-scratch-containment-matches-workspace-defenses-d114" tabindex="-1">E2E-019b: Scratch containment matches workspace defenses (D114) <a class="header-anchor" href="#e2e-019b-scratch-containment-matches-workspace-defenses-d114" aria-label="Permalink to &quot;E2E-019b: Scratch containment matches workspace defenses (D114)&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Agent mode; project open.</li><li><strong>Steps</strong>: 1) Attempt Write with <code>..</code> traversal from the scratch root. 2) Attempt Write through a symlink planted inside scratch pointing outside. 3) Attempt the same Write calls in Plan.</li><li><strong>Expected</strong>: Both escapes return <code>PATH_OUTSIDE_WORKSPACE</code>; Plan returns <code>WRITE_DISABLED_IN_PLAN</code> before any scratch path can make Write available.</li><li><strong>Specs linked</strong>: <code>03-runtime/03-tools-and-permissions.md §4b</code></li><li><strong>Acceptance</strong>: E (scratch root cannot be escaped)</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Automated (host-core unit tests)</li></ul><h4 id="e2e-019c-permission-modes-govern-high-risk-approval-d115-d132" tabindex="-1">E2E-019c: Permission modes govern high-risk approval (D115/D132) <a class="header-anchor" href="#e2e-019c-permission-modes-govern-high-risk-approval-d115-d132" aria-label="Permalink to &quot;E2E-019c: Permission modes govern high-risk approval (D115/D132)&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Agent mode; project open; global default <code>ask</code>.</li><li><strong>Steps</strong>: 1) With a newly inherited session and global default Ask every time, open the composer menu — expect Ask every time to be selected with no global-default/inherit label — then ask the Agent to write a workspace file and expect a permission card. 2) Switch the session chip to Accept edits; repeat — expect no card for Write/Edit but still a card for Bash. 3) Switch to Auto — expect no card for Bash either. 4) Create another inherited session after setting the global default to Accept edits in Settings — expect the composer chip and menu selection to display Accept edits directly and Write/Edit to be auto-allowed. 5) Switch the session to Plan, then Goal, with Auto set — expect Write/Edit/plugin denied but Bash allowed without confirmation.</li><li><strong>Expected</strong>: Effective mode = session override → global default → ask; Plan and Goal Write/Edit/plugin hard denies outrank every mode while their Bash follows the selected mode; the composer chip and menu always display the effective mode without default/inherit provenance.</li><li><strong>Specs linked</strong>: <code>03-runtime/03-tools-and-permissions.md §6</code>, <code>03-runtime/04-data-storage.md</code>, <code>08-meta/decisions-log.md</code> (D115/D132)</li><li><strong>Acceptance</strong>: E (permission modes resolve and enforce host-side)</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Partially automated (host-core unit tests: evaluate matrix, Plan policy precedence, session grants under ask; renderer source test: effective-only composer options and selection)</li></ul><h4 id="e2e-019d-bash-tool-sees-the-user-s-login-shell-toolchain-d181" tabindex="-1">E2E-019d: Bash tool sees the user&#39;s login-shell toolchain (D181) <a class="header-anchor" href="#e2e-019d-bash-tool-sees-the-user-s-login-shell-toolchain-d181" aria-label="Permalink to &quot;E2E-019d: Bash tool sees the user&#39;s login-shell toolchain (D181)&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Agent mode; project open; the OS user has a login shell (default on macOS) whose profile exports at least one tool not on the app&#39;s minimal GUI PATH (e.g. nvm/Homebrew).</li><li><strong>Steps</strong>: 1) Ask the agent to print <code>$PATH</code> and run a toolchain check such as <code>command -v node &amp;&amp; node -v</code>. 2) Compare with the PATH a fresh terminal shows for the same user. 3) Optionally remove <code>~/.bash_profile</code> temporarily and repeat on a machine where only <code>.zshrc</code> initializes the toolchain.</li><li><strong>Expected</strong>: The Bash tool resolves tools the user&#39;s own login shell exports (nvm, pnpm, Homebrew) even though commands run through bash; the probed login PATH is a subset of the effective child PATH (bash profile may prepend/dedupe). A missing or wedged user shell degrades to the host PATH without failing the tool.</li><li><strong>Specs linked</strong>: <code>03-runtime/03-tools-and-permissions.md §5</code>, <code>08-meta/decisions-log.md</code> (D181), ADR 0045</li><li><strong>Acceptance</strong>: E (user toolchain visible in Bash tool)</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Automated (host-core unit tests: login-PATH probe + child-PATH injection)</li></ul><h4 id="e2e-mcp-stdio-login-path-market-stdio-mcp-finds-uvx-npx-after-a-gui-launch-issue-571" tabindex="-1">E2E-MCP-stdio-login-path: Market stdio MCP finds uvx/npx after a GUI launch (issue #571) <a class="header-anchor" href="#e2e-mcp-stdio-login-path-market-stdio-mcp-finds-uvx-npx-after-a-gui-launch-issue-571" aria-label="Permalink to &quot;E2E-MCP-stdio-login-path: Market stdio MCP finds uvx/npx after a GUI launch (issue #571)&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: macOS; the app was started from Finder/Dock so <code>process.env.PATH</code> is the GUI default; <code>uvx</code> or <code>npx</code> exists on the user&#39;s login-shell PATH.</li><li><strong>Steps</strong>: 1) Install Fetch from the MCP market (<code>uvx mcp-server-fetch</code>). 2) Test connection. 3) Repeat with an npx catalog entry.</li><li><strong>Expected</strong>: The stdio child env PATH includes the login-shell PATH (Homebrew / nvm / <code>~/.local/bin</code>) ahead of the GUI PATH, so spawn succeeds. A truly missing binary reports <code>command not found: uvx</code> rather than <code>spawn uvx ENOENT</code>. Windows keeps the inherited PATH.</li><li><strong>Specs linked</strong>: ADR 0045, D600, <code>03-runtime/03-tools-and-permissions.md</code></li><li><strong>Acceptance</strong>: Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Automated (<code>apps/desktop/test/user-login-path.test.mjs</code>, <code>apps/desktop/test/plugin-mcp.test.mjs</code>)</li></ul><h3 id="session-persistence" tabindex="-1">Session Persistence <a class="header-anchor" href="#session-persistence" aria-label="Permalink to &quot;Session Persistence&quot;">​</a></h3><h4 id="e2e-020-session-survives-restart" tabindex="-1">E2E-020: Session survives restart <a class="header-anchor" href="#e2e-020-session-survives-restart" aria-label="Permalink to &quot;E2E-020: Session survives restart&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Session with message history exists.</li><li><strong>Steps</strong>: 1) Quit app. 2) Relaunch. 3) Open session list.</li><li><strong>Expected</strong>: Previous session appears; messages recoverable.</li><li><strong>Specs linked</strong>: <code>03-runtime/04-data-storage.md</code>, <code>03-runtime/10-session-state-machine.md</code></li><li><strong>Acceptance</strong>: F (session survives restart)</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Automated (protocol smoke: host-level persistence; full restart lane manual)</li></ul><h4 id="e2e-021-delete-session-works" tabindex="-1">E2E-021: Delete session works <a class="header-anchor" href="#e2e-021-delete-session-works" aria-label="Permalink to &quot;E2E-021: Delete session works&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Session exists.</li><li><strong>Steps</strong>: 1) Delete a session. 2) Observe session list.</li><li><strong>Expected</strong>: Session removed from list; data gone.</li><li><strong>Specs linked</strong>: <code>03-runtime/04-data-storage.md</code></li><li><strong>Acceptance</strong>: F (delete session)</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Draft</li></ul><h4 id="e2e-021a-rename-session-title-persists-without-changing-activity" tabindex="-1">E2E-021a: Rename session title persists without changing activity <a class="header-anchor" href="#e2e-021a-rename-session-title-persists-without-changing-activity" aria-label="Permalink to &quot;E2E-021a: Rename session title persists without changing activity&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A project-scoped session and a path-less session exist; at least one session has transcript history and one still has the default title.</li><li><strong>Steps</strong>: 1) Open a Sidebar session overflow menu or right-click a session row and choose Rename. 2) Enter a title with surrounding whitespace and save. 3) Verify the title in the Sidebar, topbar, Project archive, and Search. 4) Restart the app and verify the title again. 5) Try an empty and an over-80-Unicode-code-point title. 6) Send the first prompt in the default-title session.</li><li><strong>Expected</strong>: The saved title is trimmed, displayed across every current session-summary surface, and persists after restart. The session stays in the same project or Temporary group, its transcript/message count and recent-activity ordering do not change, and historical notification title snapshots are unchanged. Empty and overlong values are rejected. A custom title is not replaced by first-prompt auto-title; a still-default session continues to receive its automatic title. After its first turn, the default session first shows the prompt fallback and then adopts the concise background LLM summary when the provider returns one.</li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code>, <code>03-runtime/04-data-storage.md</code>, <code>03-runtime/06-host-rpc-protocol.md</code>, <code>04-ux/01-ui-ia.md</code>, <code>04-ux/08-component-spec.md</code>, ADR 0143</li><li><strong>Acceptance</strong>: F (session metadata persistence), Quality (localized task management)</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-021b-developer-mode-copies-a-conversation-id-and-opens-session-scratch" tabindex="-1">E2E-021b: Developer mode copies a conversation id and opens session scratch <a class="header-anchor" href="#e2e-021b-developer-mode-copies-a-conversation-id-and-opens-session-scratch" aria-label="Permalink to &quot;E2E-021b: Developer mode copies a conversation id and opens session scratch&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Developer mode is enabled. A session exists, including one whose scratch directory has not been created yet.</li><li><strong>Steps</strong>: 1) Open a conversation overflow menu. 2) Confirm Copy conversation ID and Open session path appear after Create branch and before Delete, and that Copy session path is absent. 3) Choose Copy conversation ID and paste the clipboard. 4) Choose Open session path. 5) Disable developer mode and reopen the menu.</li><li><strong>Expected</strong>: The clipboard contains the exact session id. The system file manager opens <code>&lt;data_dir&gt;/scratch/&lt;sessionId&gt;/</code>, creating that directory if it was missing. The renderer does not send a filesystem path; Main opens only a resolved scratch directory for that session id. With developer mode off, both actions are absent.</li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code>, <code>04-ux/08-component-spec.md</code>, <code>04-ux/06-settings-ia.md</code></li><li><strong>Acceptance</strong>: C (sessions), Quality (developer tools)</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Unit-covered (<code>session-scratch-path.test.mjs</code>); desktop journey Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-036-localized-import-grouping-starts-collapsed" tabindex="-1">E2E-036: Localized import grouping starts collapsed <a class="header-anchor" href="#e2e-036-localized-import-grouping-starts-collapsed" aria-label="Permalink to &quot;E2E-036: Localized import grouping starts collapsed&quot;">​</a></h4><ul><li><p><strong>Preconditions</strong>: Supported local agent stores contain importable sessions across at least two project paths and two sources, including one session without a project path; the app can be launched once with an English system locale and once with a Simplified Chinese system locale.</p></li><li><p><strong>Steps</strong>: 1) Launch in English and open Settings → Import. 2) Scan for sessions. 3) Inspect the initial source groups. 4) Expand one group and select a session. 5) Change Group by to Project path. 6) Switch back to Source. 7) Repeat the flow after launching with a Simplified Chinese system locale.</p></li><li><p><strong>Expected</strong>: Source/来源 is the initial grouping; all groups are collapsed after the scan and after either grouping change; project-path mode shows exact project paths and a final No project/未关联项目 group; expanding one group leaves the others collapsed; the selected session remains selected across grouping changes; counts, dates, selection labels, accessible names, and the import result use the active locale without raw keys or unresolved double-brace placeholders. A Codex archive above the scan threshold may show an em dash for its unknown message count during review, but it remains selectable and the later import converts the complete transcript. A Codex archive with more than 250 session files shows only the newest 250 by folder date plus a localized cap note; older Codex sessions are absent from that scan.</p></li><li><p><strong>Specs linked</strong>: <code>04-ux/01-ui-ia.md</code>, <code>04-ux/02-i18n-english-first.md</code>, <code>04-ux/08-component-spec.md</code></p></li><li><p><strong>Acceptance</strong>: F (session import review)</p></li><li><p><strong>Milestone</strong>: M2</p></li><li><p><strong>Status</strong>: Draft</p></li></ul><h4 id="e2e-037-import-creates-durable-project-entries" tabindex="-1">E2E-037: Import creates durable project entries <a class="header-anchor" href="#e2e-037-import-creates-durable-project-entries" aria-label="Permalink to &quot;E2E-037: Import creates durable project entries&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Import candidates include two sessions at path A, one at path B, and one without a project path; neither project is the active workspace.</li><li><strong>Steps</strong>: 1) Import all candidates. 2) Open Settings → Project archive. 3) Inspect and expand paths A and B. 4) Return home and inspect Temporary sessions. 5) Repeat the import.</li><li><strong>Expected</strong>: Project archive contains exactly one durable row for A and one for B; the matching imported sessions appear under their exact project rows; the path-less session appears only under Temporary sessions; the active workspace does not change; repeating import duplicates neither sessions nor project rows; no missing filesystem path is created on disk.</li><li><strong>Specs linked</strong>: <code>03-runtime/04-data-storage.md</code>, <code>04-ux/01-ui-ia.md</code>, <code>04-ux/08-component-spec.md</code></li><li><strong>Acceptance</strong>: F (session/project persistence)</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Draft</li></ul><h4 id="e2e-038-settings-owns-the-project-archive-destination" tabindex="-1">E2E-038: Settings owns the project archive destination <a class="header-anchor" href="#e2e-038-settings-owns-the-project-archive-destination" aria-label="Permalink to &quot;E2E-038: Settings owns the project archive destination&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: App running with at least one configured provider, one supported local session store, one retained project, and one archived project.</li><li><strong>Steps</strong>: 1) Open Settings. 2) Inspect the complete settings rail. 3) Open Basics and change the theme in its Appearance card using the searchable theme picker. 4) Open 全局 AI and inspect the Permissions and Defaults cards, including the Command shell row; confirm Context management has no settings card. 5) Open Shortcuts and inspect the Keyboard shortcuts card. 6) Open Instructions and save global instructions. 7) Open Model configuration and inspect the provider studio. 8) Open Import, Project archive, and Info in order. 9) Search Settings for &quot;project&quot; or &quot;archive&quot;. 10) In Project archive, compare each group strip&#39;s count with its rendered rows. 11) Switch the sort control from Recent to Name. 12) Search for a known session title, confirm the owning project is selected and its inspector lists matching sessions, then reveal more than eight sessions; clear the search with the clear affordance. 13) Open the inspector menu, dismiss it with Escape and with an outside press. 14) Restore the archived project, then activate it. 15) Archive one project session, open it from Project archive, and return to Project archive. 16) Return to the app shell and open Plugins.</li><li><strong>Expected</strong>: The rail contains exactly Basics, 全局 AI/AI, Shortcuts, Instructions, Model configuration, Import, Project archive, and Info in that order, each with its semantic Lucide icon (Sliders / Sparkles / Keyboard / FileText / Bot / Download / Archive / Info). The flat directory is visually grouped under four muted, non-interactive headings — Personal / 个人 for Basics, AI, and Shortcuts; Agent / 智能体 for Instructions and Model configuration; Workspace / 工作区 for Import and Project archive; About / 关于 for Info — with whitespace and no divider lines between groups; searching keeps the destination results flat and hides empty groups together with their headings. Appearance remains in Basics, while Permissions, Defaults, and the Command shell row live under 全局 AI; an available selected shell is represented by the selector without a duplicate Configured status, while default, fallback, and no-effective-shell states remain explicit; Context management has no settings card; Keyboard shortcuts and global instructions have their own destinations; Developer lives under Info; Project archive shows active, closed, and archived durable rows without a visibility toggle, grouping them under the always-visible Pinned / All projects / Archived strips (D168/D267/D455) with per-section counts in a one-column workbench. The destination renders no hero block and no page-level counter run: the intro is one quiet description line, and each group strip&#39;s count agrees with its rendered rows; a click selects a row without leaving Settings; sorting by Name reorders rows inside every section without hiding any; search matches project fields and session titles and reports a match count, a session-title result selects its owning project, lists sessions in the inspector by latest activity with relative update times, and reveals history in batches of eight; clearing the search restores the complete index. The inspector menu closes on Escape and on an outside press. Bootstrap completion and background refreshes do not return Settings or Extensions to the chat home; the destination changes only after an explicit navigation action. Restore keeps the archive open and activation returns to chat with the restored project retained in the sidebar. Opening an archived session succeeds before clearing its archived state, returns to chat with that session selected, and makes it visible in the project sidebar; returning to Project archive no longer shows that session as archived. The home sidebar and global page results have no standalone Projects destination; Settings search finds Project archive; Plugins remains an independent app-shell destination.</li><li><strong>Specs linked</strong>: <code>04-ux/06-settings-ia.md</code>, <code>04-ux/01-ui-ia.md</code>, <code>03-runtime/11-provider-model-system.md</code></li><li><strong>Acceptance</strong>: B (model configuration), F (session import)</li><li><strong>Milestone</strong>: M4</li><li><strong>Status</strong>: Unit-covered (<code>settings-project-archive.test.mjs</code>, <code>project-archive.test.mjs</code>, <code>sidebar-navigation.test.mjs</code>); rendered scenario Draft</li></ul><h4 id="e2e-091-appearance-card-selects-searchable-theme-and-language-pickers" tabindex="-1">E2E-091: Appearance card selects searchable theme and language pickers <a class="header-anchor" href="#e2e-091-appearance-card-selects-searchable-theme-and-language-pickers" aria-label="Permalink to &quot;E2E-091: Appearance card selects searchable theme and language pickers&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: App running on macOS; the harness can exercise English, Simplified Chinese, Traditional Chinese, Turkish, German, Spanish, French, and Korean system locales.</li><li><strong>Steps</strong>: <ol><li>Open Settings → General.</li><li>In the Appearance card, open the Theme picker. Confirm System, Light, and Dark are pinned at the top; select Dark and confirm the trigger shows Dark and the UI switches to dark.</li><li>Select Light and confirm the UI switches to light.</li><li>In the Language row, open the searchable picker. Confirm Auto is pinned at the top with the detected native name and that English, 简体中文, 繁體中文, Türkçe, Deutsch, Español, Français, and 한국어 are listed by native name. With Simplified Chinese selected as the OS locale, selecting Auto applies Simplified Chinese; with Traditional Chinese selected, Auto applies Traditional Chinese.</li><li>Select English, 简体中文, 繁體中文, Türkçe, Deutsch, Español, Français, and 한국어 in turn and confirm shell chrome switches to each locale without a reload. Confirm <code>zh-Hant</code> and <code>zh-HK</code> resolve to 繁體中文, <code>de-DE</code> to Deutsch, <code>es-MX</code> to Español, <code>fr-CA</code> to Français, and <code>ko-KR</code> to 한국어.</li><li>Type a native name or English name into the language search and confirm unmatched locales disappear. Type a theme name into the theme search and confirm unmatched options disappear.</li></ol></li><li><strong>Expected</strong>: Theme and Language are searchable picker rows (not a card grid and not a native select); each closed trigger sizes to the current label, capped by the settings control column, without overflowing the row. Theme lists System, Light, and Dark, then any plugin themes after a divider. Auto resolves the OS locale through the main process (<code>app.getLocale()</code>), passes it safely through the sandboxed preload bridge, and reflects the detected native name inline in the menu; zh-TW, Turkish, German, Spanish, French, and Korean are complete shell catalogs, including release-note copy; switching options updates the live UI without a reload.</li><li><strong>Specs linked</strong>: <code>04-ux/06-settings-ia.md</code>, <code>04-ux/02-i18n-english-first.md</code></li><li><strong>Acceptance</strong>: A (core shell), H (localization)</li><li><strong>Milestone</strong>: M4</li><li><strong>Status</strong>: Documented</li></ul><h4 id="e2e-091a-theme-changes-keep-the-windows-frameless-background-aligned" tabindex="-1">E2E-091a: Theme changes keep the Windows frameless background aligned <a class="header-anchor" href="#e2e-091a-theme-changes-keep-the-windows-frameless-background-aligned" aria-label="Permalink to &quot;E2E-091a: Theme changes keep the Windows frameless background aligned&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: App running windowed on Windows with the OS in light mode.</li><li><strong>Steps</strong>: 1) Select Dark in Settings → General → Appearance. 2) Inspect the lower-left, lower-right, and resize edges while the shell settles and while collapsing/expanding the sidebar. 3) Select Light and repeat. 4) Switch the OS color preference and select System; repeat after the app resolves the system theme.</li><li><strong>Expected</strong>: The native BrowserWindow background follows the resolved application theme (<code>#181818</code> for dark and <code>#ffffff</code> for light), so no white strip appears around the frameless renderer during theme changes or shell animations. macOS keeps its transparent vibrancy behavior unchanged.</li><li><strong>Specs linked</strong>: <code>04-ux/06-settings-ia.md</code>, <code>02-architecture/01-architecture.md</code></li><li><strong>Acceptance</strong>: A (core shell)</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Documented; native Windows validation pending</li></ul><h4 id="e2e-settings-ai-tab-pickers-use-in-app-menus" tabindex="-1">E2E-SETTINGS-ai-tab-pickers-use-in-app-menus <a class="header-anchor" href="#e2e-settings-ai-tab-pickers-use-in-app-menus" aria-label="Permalink to &quot;E2E-SETTINGS-ai-tab-pickers-use-in-app-menus&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: App running with a host that reports at least one configured command shell and at least one catalog shell that is unavailable on this platform.</li><li><strong>Steps</strong>: 1) Open Settings → General and open the Theme and Language pickers; note the pill trigger and the opened surface. 2) Open 全局 AI. 3) Open the Permissions card&#39;s permission-mode control and select ask, accept-edits, and auto in turn. 4) Open the Defaults card&#39;s Command shell control; inspect the unavailable entries and select an available shell. 5) Dismiss each open menu with Escape and then with an outside press. 6) Close Settings, reopen it, and read both rows.</li><li><strong>Expected</strong>: Both rows open the same anchored menu surface as the Appearance pickers — the app-drawn frame with the shared radius, elevation, border, and theme tokens, a check mark on the current option, and a hover/keyboard highlight — and never a platform-drawn <code>&lt;select&gt;</code> popup. Closed triggers size to the current label, capped by the settings control column. Unavailable shells stay listed with their suffix, are not selectable, and cannot become the current value. Escape and an outside press close the menu and restore focus to the trigger; arrow keys move between selectable options with wraparound. The selected permission mode and command shell persist across closing and reopening Settings, and the selected shell stays the only configured-state indicator.</li><li><strong>Specs linked</strong>: <code>04-ux/06-settings-ia.md</code>, <code>04-ux/09-interaction-patterns.md</code></li><li><strong>Acceptance</strong>: A (core shell)</li><li><strong>Milestone</strong>: M4</li><li><strong>Status</strong>: Documented</li></ul><h4 id="e2e-039-settings-titlebar-drag-moves-the-window" tabindex="-1">E2E-039: Settings titlebar drag moves the window <a class="header-anchor" href="#e2e-039-settings-titlebar-drag-moves-the-window" aria-label="Permalink to &quot;E2E-039: Settings titlebar drag moves the window&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: App running windowed on macOS with Settings open.</li><li><strong>Steps</strong>: 1) Record the window position. 2) Drag the empty 46px band above the settings rail. 3) Drag the same band above the content pane. 4) Use Back, search, Project archive, and navigation controls. 5) In light and dark themes, sample the top row&#39;s color above the rail and above the content pane.</li><li><strong>Expected</strong>: Either top-band drag moves the native window; Back, search, and navigation remain interactive and never initiate a window drag. The top row above the rail matches the rail surface (<code>#f4f4f4</code> light / <code>#000</code> dark) and the top row above the content pane matches the primary surface, so the band never renders a mismatched color over the rail.</li><li><strong>Specs linked</strong>: <code>04-ux/06-settings-ia.md</code>, <code>04-ux/01-ui-ia.md</code></li><li><strong>Acceptance</strong>: Quality (key operations feel polished)</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Draft</li></ul><h4 id="e2e-043-settings-content-follows-window-width" tabindex="-1">E2E-043: Settings content follows window width <a class="header-anchor" href="#e2e-043-settings-content-follows-window-width" aria-label="Permalink to &quot;E2E-043: Settings content follows window width&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: App running windowed on macOS with Settings open.</li><li><strong>Steps</strong>: 1) Open Basics at the default window width and record the content-card width. 2) Expand the window to 1600px wide. 3) Open Model configuration, Import, and Project archive. 4) Shrink the window to the supported 1040px minimum.</li><li><strong>Expected</strong>: The right-side content cards expand and contract with the available pane at every tested width; the 275px rail and pane gutters remain stable; controls remain visible without clipping or horizontal page scrolling.</li><li><strong>Specs linked</strong>: <code>04-ux/06-settings-ia.md</code>, <code>04-ux/07-ui-design-system.md</code></li><li><strong>Acceptance</strong>: Quality (key operations feel polished)</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>settings-responsive-layout.test.mjs</code>); scenario Documented</li></ul><h4 id="e2e-040-nested-tool-activity-survives-transcript-reload" tabindex="-1">E2E-040: Nested tool activity survives transcript reload <a class="header-anchor" href="#e2e-040-nested-tool-activity-survives-transcript-reload" aria-label="Permalink to &quot;E2E-040: Nested tool activity survives transcript reload&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Provider configured; project open; a turn can contain progress text, multiple search/tool/thinking items, and a failed or denied tool.</li><li><strong>Steps</strong>: 1) In Detailed, stream progress paragraph A, a multi-item search segment, progress paragraph B, and a multi-item command segment. 2) Inspect the open whole process and active group; manually close the active group while more output arrives. 3) Complete the turn and inspect untouched versus user-owned groups. 4) Open one completed group and one item, copy its output, close and reopen the group, then open a sibling group independently. 5) Repeat with a singleton item and with the last activity group&#39;s literal final item set to a tool/search, thinking, failed tool, and denied tool. 6) Switch to Compact and inspect the active-thinking, failure/recovery and completed states. 7) Remount rows within the retained pane, then reload the renderer and reopen the session.</li><li><strong>Expected</strong>: Both modes use one whole-process disclosure and keep the trailing answer outside it. Detailed keeps active and completed processes open; the active multi-item group opens, then closes on completion only if untouched. Compact starts processes/groups and all payloads closed, hides reasoning text, and keeps an untouched active process open after a failed/denied tool through later recovery. A singleton has no group wrapper. Detailed auto-opens a payload only when the literal final item of the last activity group is an eligible tool/search; it never scans backward past thinking, and failure/denial guards keep that leaf closed. Parent, child and sibling choices are independent; closing/reopening a parent preserves descendants, and streaming/completion does not override user-owned choices. Retained-pane remounts preserve choices; renderer restart reapplies defaults while tool names, arguments, results and statuses remain restored. Group/process headers show bounded running and issue summaries without marking the whole turn failed.</li><li><strong>Specs linked</strong>: <code>04-ux/01-ui-ia.md</code>, <code>04-ux/07-ui-design-system.md</code>, <code>04-ux/08-component-spec.md</code>, <code>04-ux/09-interaction-patterns.md</code></li><li><strong>Acceptance</strong>: C (chat stream), E (tools), F (persistence)</li><li><strong>Milestone</strong>: M3</li><li><strong>Status</strong>: Draft. For the 2026-09-20 nested-disclosure change, this scenario is intended behavior for static source/design review only; no unit, component, integration, browser, Electron or E2E test is added or run by that scoped task.</li></ul><h4 id="e2e-041-conversation-minimap-navigates-long-transcripts" tabindex="-1">E2E-041: Conversation minimap navigates long transcripts <a class="header-anchor" href="#e2e-041-conversation-minimap-navigates-long-transcripts" aria-label="Permalink to &quot;E2E-041: Conversation minimap navigates long transcripts&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A session contains enough user and assistant turns to scroll beyond one viewport and densely fill the minimap, including one AI response emitted as multiple assistant fragments around tool activity; a second session has at least two eligible turn markers that still fit in one viewport.</li><li><strong>Steps</strong>: 1) Open the long session. 2) Scroll through the transcript and observe the active minimap marker. 3) Hover a marker and inspect its preview. 4) Use keyboard focus to reach another marker. 5) Activate a marker. 6) Open a session with fewer than two eligible turn markers. 7) Open the multi-message session that still fits one viewport. 8) Resize the long session window taller until content no longer overflows, then shorter again. 9) At a short window height, inspect and activate the first and last markers near the minimap&#39;s vertical bounds, including from the titlebar-facing side.</li><li><strong>Expected</strong>: The rail contains one marker per visible user turn and one per AI response. Multiple assistant fragments between two user messages share a single marker and combined bounded preview, while tool-only rows create no marker and do not split the response. The marker near the upper-third reading anchor exposes <code>aria-current</code>; hover and focus show the same localized sender and preview; nearby markers magnify horizontally without shifting the stack; activation smoothly scrolls to the first contentful message in that response; the rail is absent when fewer than two eligible markers exist <strong>or</strong> when content does not overflow one viewport; the rail reappears once overflow returns after a resize. Dense markers remain centered in the unobstructed span between the 46px titlebar and docked composer, compress uniformly, and remain interactive without entering the native window drag region. The empty-home and docked composer use the same horizontal width envelope, and the composer does not shrink when the minimap appears.</li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code></li><li><strong>Acceptance</strong>: C (chat stream), Quality (keyboard and long-thread navigation)</li><li><strong>Milestone</strong>: M3</li><li><strong>Status</strong>: Draft</li></ul><h4 id="e2e-042-pre-v7-storage-archives-via-breaking-reset-transcripts-live-in-session-files" tabindex="-1">E2E-042: Pre-v7 storage archives via breaking reset; transcripts live in session files <a class="header-anchor" href="#e2e-042-pre-v7-storage-archives-via-breaking-reset-transcripts-live-in-session-files" aria-label="Permalink to &quot;E2E-042: Pre-v7 storage archives via breaking reset; transcripts live in session files&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A fixture data directory contains a <code>pi.sqlite</code> whose <code>PRAGMA user_version</code> is between 1 and 6 (pre-D119 content-in-DB schema) with representative rows.</li><li><strong>Steps</strong>: 1) Start host-core against the fixture. 2) Create a session and append messages through host RPC. 3) Stop and restart host-core. 4) Reload the session through RPC and inspect the data directory.</li><li><strong>Expected</strong>: Host-core renames the legacy file to exactly one <code>pi.sqlite.v6.bak</code>, bootstraps a fresh schema-v7 database (index-only <code>messages</code>), writes <code>sessions/&lt;id&gt;.jsonl</code> with a session-header line plus one line per message, reloads the transcript from the file after restart with identical logical results, and deleting the session removes both the index rows and the session files. No Electron-owned persistence file is authoritative.</li><li><strong>Specs linked</strong>: <code>03-runtime/04-data-storage.md</code>, <code>03-runtime/06-host-rpc-protocol.md</code>, ADR 0014</li><li><strong>Acceptance</strong>: F (persistence), H (reset failures are diagnosable)</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Unit-covered (<code>db::tests::archives_pre_v7_database_and_starts_fresh</code>, <code>sessions::tests::transcript_survives_reopen_from_file</code>, <code>sessions::tests::delete_session_removes_transcript_files</code>); full fixture scenario Draft</li></ul><h4 id="e2e-252-session-and-folder-drag-drop-across-projects" tabindex="-1">E2E-252: Session and folder drag/drop across projects <a class="header-anchor" href="#e2e-252-session-and-folder-drag-drop-across-projects" aria-label="Permalink to &quot;E2E-252: Session and folder drag/drop across projects&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Two projects are open in the sidebar; one holds an idle session, and one session somewhere is running a turn.</li><li><strong>Steps</strong>: 1) Drag the idle session row onto the other project group and drop it. 2) Reopen that session and read its transcript. 3) Attempt to drag the running session and, separately, open its session menu. 4) Drop a folder onto the projects list. 5) Drop a folder on the composer.</li><li><strong>Expected</strong>: The dragged idle session lists under the target project with its transcript, attachments, and tasks unchanged, and the move survives a restart because the session&#39;s project membership is persisted. The running session is not draggable, its context menu has no project-move list, and a move issued after a turn starts is rejected as busy rather than rebinding the agent. Dropping a folder on the projects list adds or switches to that project without creating a duplicate row, and a non-folder drop explains that a folder is required. Dropping a folder on the composer offers an explicit choice and never attaches directory contents: &quot;Open as project&quot; opens the project, and &quot;Reference folder&quot; inserts the literal path. The dragged row paints at reduced opacity and the eligible group shows an accent outline.</li><li><strong>Specs linked</strong>: <code>04-ux/09-interaction-patterns.md</code> §8, <code>03-runtime/06-host-rpc-protocol.md</code> (session.moveProject)</li><li><strong>Acceptance</strong>: D (project membership and workspace switching), Quality (drag/drop feedback and keyboard fallback)</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Documented; source-contract covered (<code>session-project-move.test.mjs</code>); automation pending</li></ul><h3 id="plugin-load-command-disable" tabindex="-1">Plugin Load / Command / Disable <a class="header-anchor" href="#plugin-load-command-disable" aria-label="Permalink to &quot;Plugin Load / Command / Disable&quot;">​</a></h3><h4 id="e2e-022-load-local-plugin" tabindex="-1">E2E-022: Load local plugin <a class="header-anchor" href="#e2e-022-load-local-plugin" aria-label="Permalink to &quot;E2E-022: Load local plugin&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: App running; sample plugin available at local path.</li><li><strong>Steps</strong>: 1) Open Extensions from the sidebar footer Plugins icon. 2) Choose Load local plugin from the header overflow menu. 3) Enable it with the row switch.</li><li><strong>Expected</strong>: Plugin loads; manifest validated; contributions registered; the row appears under Active with a Local tag.</li><li><strong>Specs linked</strong>: <code>07-plugins/01-plugin-system.md</code>, <code>07-plugins/05-plugin-lifecycle.md</code></li><li><strong>Acceptance</strong>: G (load local plugin)</li><li><strong>Milestone</strong>: M4</li><li><strong>Status</strong>: Automated (protocol smoke: plugins.loadDev)</li></ul><h4 id="e2e-022a-create-a-plugin-from-a-template" tabindex="-1">E2E-022A: Create a plugin from a template <a class="header-anchor" href="#e2e-022a-create-a-plugin-from-a-template" aria-label="Permalink to &quot;E2E-022A: Create a plugin from a template&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: App running; an empty folder available.</li><li><strong>Steps</strong>: 1) Open Extensions. 2) Choose New plugin from template in the header overflow menu (or use the empty-state button). 3) Pick each of the four templates in turn and read its description. 4) Choose the folder. 5) Cancel the folder picker on a second attempt.</li><li><strong>Expected</strong>: The picker lists exactly <code>panel-basic</code>, <code>agent-tool-basic</code>, <code>skill-pack</code>, <code>full-demo</code>, each named and described in the active locale; choosing a folder writes the template files, loads the plugin as a development plugin, refreshes the list, then opens that folder as the active project — the app lands on chat with the new folder as the workspace, it appears in the sidebar project list, and the toast reads &quot;&lt;name&gt; created, loaded, and opened for development&quot;; the plugin&#39;s contributions are immediately usable and the built-in plugin-development skill is active in the new workspace. That skill teaches the current global <code>pi</code> API (<code>onLoad()</code> plus <code>pi.commands.register</code>) and the fixed <code>window.pluginBridge</code> boundary, and does not teach the retired <code>onLoad(pi)</code> / <code>pi.registerCommand</code> shape. A canceled folder picker changes nothing and reports no error.</li><li><strong>Specs linked</strong>: <code>07-plugins/10-plugin-devex.md</code>, <code>../../plugin-development.md</code>, ADR 0039</li><li><strong>Acceptance</strong>: G (create plugin from template)</li><li><strong>Milestone</strong>: Post-MVP</li><li><strong>Status</strong>: Automated in part (<code>apps/desktop/test/plugin-template-action.test.mjs</code>: channel, template-id parity with the devkit, locale coverage, canceled-pick ordering, project activation); UI walk-through Documented</li></ul><h4 id="e2e-022b-development-plugin-hot-reload" tabindex="-1">E2E-022B: Development plugin hot reload <a class="header-anchor" href="#e2e-022b-development-plugin-hot-reload" aria-label="Permalink to &quot;E2E-022B: Development plugin hot reload&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A plugin loaded from a local folder and enabled.</li><li><strong>Steps</strong>: 1) Edit <code>main.js</code> to change a command title and save. 2) Save several files at once. 3) Introduce a syntax error and save. 4) Fix the error and save. 5) Add a new permission to <code>manifest.json</code> and save. 6) Open the development-plugin card&#39;s More actions menu and choose Reload. 7) Edit again and restart the app.</li><li><strong>Expected</strong>: The single edit reloads the plugin without re-picking the folder and the command palette shows the new title; a save burst produces one reload, and writes under <code>dist/</code> or <code>node_modules/</code> produce none; the syntax error reports a reload failure without crashing the app, and the fixing save recovers the plugin; the added permission refuses automatic reload with <code>PERMISSION_DENIED</code>, while the explicit Reload action loads the registered folder, refreshes the permission ceiling, and reports success; a later edit uses the refreshed ceiling, and after a restart the folder is still watched.</li><li><strong>Specs linked</strong>: <code>07-plugins/10-plugin-devex.md</code> §7, <code>07-plugins/13-plugin-permissions-matrix.md</code>, ADR 0039, ADR 0075</li><li><strong>Acceptance</strong>: G (hot reload), D (permissions cannot widen without review)</li><li><strong>Milestone</strong>: Post-MVP</li><li><strong>Status</strong>: Automated in part (<code>apps/desktop/test/plugin-hot-reload.test.mjs</code>: debounce, ignore list, permission ceiling, recovery, teardown); manual edit loop Documented</li></ul><h4 id="e2e-022c-check-pack-install-round-trip" tabindex="-1">E2E-022C: Check, pack, install round-trip <a class="header-anchor" href="#e2e-022c-check-pack-install-round-trip" aria-label="Permalink to &quot;E2E-022C: Check, pack, install round-trip&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A scaffolded plugin directory.</li><li><strong>Steps</strong>: 1) <code>pnpm pi-plugin check &lt;dir&gt;</code>. 2) Delete the file named by <code>main</code> and run <code>check</code> again. 3) Restore it, declare <code>contributes.skills</code> without <code>agent.prompt.inject</code>, and run <code>check</code> again. 4) <code>pnpm pi-plugin pack &lt;dir&gt;</code>. 5) Install the resulting <code>.piplug</code> from the plugins page. 6) Ask the agent to run <code>PluginCheck</code> and <code>PluginPack</code> on the same directory.</li><li><strong>Expected</strong>: A scaffolded plugin checks clean and reports its file count and size; the missing <code>main</code> is an error that blocks <code>pack</code>; the inert-skills case is a warning that does not block; <code>pack</code> writes <code>dist/&lt;id&gt;-&lt;version&gt;.piplug</code> with store-only entries and prints its sha256; the package installs through the normal permission review and appears under Active; the agent tools produce the same verdicts and refuse any directory outside the session workspace.</li><li><strong>Specs linked</strong>: <code>07-plugins/10-plugin-devex.md</code> §5–§6, <code>07-plugins/06-plugin-packaging.md</code>, ADR 0039</li><li><strong>Acceptance</strong>: G (local packaging round-trip)</li><li><strong>Milestone</strong>: Post-MVP</li><li><strong>Status</strong>: Automated in part (<code>packages/plugin-devkit</code> vitest: scaffold→check→pack per template, store-method headers, every check rule); install step Documented</li></ul><h4 id="e2e-023-plugin-command-in-global-search-and-executes" tabindex="-1">E2E-023: Plugin command in global search and executes <a class="header-anchor" href="#e2e-023-plugin-command-in-global-search-and-executes" aria-label="Permalink to &quot;E2E-023: Plugin command in global search and executes&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Plugin loaded and enabled.</li><li><strong>Steps</strong>: 1) Open global search (Cmd/Ctrl+K or Cmd/Ctrl+Shift+P). 2) Find the plugin command under the Commands section. 3) Execute.</li><li><strong>Expected</strong>: Command appears in global search results; execution produces expected result.</li><li><strong>Specs linked</strong>: <code>07-plugins/09-plugin-command-palette.md</code></li><li><strong>Acceptance</strong>: G (plugin command appears and executes)</li><li><strong>Milestone</strong>: M4</li><li><strong>Status</strong>: Draft</li></ul><h4 id="e2e-024-plugin-registers-and-calls-agent-tool" tabindex="-1">E2E-024: Plugin registers and calls agent tool <a class="header-anchor" href="#e2e-024-plugin-registers-and-calls-agent-tool" aria-label="Permalink to &quot;E2E-024: Plugin registers and calls agent tool&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Plugin loaded; plugin declares an agent tool.</li><li><strong>Steps</strong>: 1) Ask agent to use the plugin&#39;s tool. 2) Observe permission card if required. 3) Allow.</li><li><strong>Expected</strong>: Tool registered with forced prefix (<code>plugin_&lt;id&gt;_&lt;name&gt;</code>); call succeeds.</li><li><strong>Specs linked</strong>: <code>07-plugins/03-plugin-api.md</code>, <code>07-plugins/13-plugin-permissions-matrix.md</code></li><li><strong>Acceptance</strong>: G (plugin agent tool)</li><li><strong>Milestone</strong>: M4</li><li><strong>Status</strong>: Automated (protocol smoke: dispatch roundtrip host-&gt;runner-&gt;host; in-app JS execution via PluginRuntime)</li></ul><h4 id="e2e-024g-marketplace-detail-sheet-shows-readme-permissions-versions" tabindex="-1">E2E-024G: Marketplace detail sheet shows README, permissions, versions <a class="header-anchor" href="#e2e-024g-marketplace-detail-sheet-shows-readme-permissions-versions" aria-label="Permalink to &quot;E2E-024G: Marketplace detail sheet shows README, permissions, versions&quot;">​</a></h4><ul><li><p><strong>Preconditions</strong>: Official marketplace catalog available.</p></li><li><p><strong>Steps</strong>: 1) Open Extensions → Marketplace. 2) Open details for <code>demo.workspace-summary</code>. 3) Inspect README / risk-grouped permissions / version rows. 4) Pick a version and install after permission review. 5) Dismiss the sheet with Escape and by clicking the scrim.</p></li><li><p><strong>Expected</strong>: Detail sheet loads via <code>market.getDetail</code>; README, safety notes, and per-risk permission explanations render; the picked version drives the sticky install action; Escape and scrim both close the sheet without closing the permission dialog underneath.</p></li><li><p><strong>Specs linked</strong>: <code>07-plugins/07-plugin-marketplace.md</code></p></li><li><p><strong>Acceptance</strong>: G (marketplace detail UX)</p></li><li><p><strong>Status</strong>: Documented</p></li><li><p><strong>Focus regression</strong>: In light and dark themes, open a marketplace card and press Escape. Like pointer dismissal, Escape leaves no line or focus ring on the card. Then use Tab and Shift+Tab: the detail button shows a complete, unclipped ring, and Install remains separately focusable. Automated focus-state regression: <code>node scripts/e2e-plugin-detail-focus.mjs</code>; verify the rendered ring visually.</p></li></ul><h4 id="e2e-024f-refresh-official-remote-marketplace-repository" tabindex="-1">E2E-024F: Refresh official remote marketplace repository <a class="header-anchor" href="#e2e-024f-refresh-official-remote-marketplace-repository" aria-label="Permalink to &quot;E2E-024F: Refresh official remote marketplace repository&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Network available to GitHub raw content.</li><li><strong>Steps</strong>: 1) Open Extensions → Marketplace. 2) Use the header Refresh marketplace action. 3) Confirm the source line points at <code>vastsa/pi-desktop-plugins</code>.</li><li><strong>Expected</strong>: Catalog refreshes from the remote official repo; card grid updates; offline fallback still works if fetch fails.</li><li><strong>Specs linked</strong>: <code>07-plugins/07-plugin-marketplace.md</code></li><li><strong>Acceptance</strong>: G (remote marketplace source)</li><li><strong>Status</strong>: Documented / host-core unit covered</li></ul><h4 id="e2e-024p-switch-the-marketplace-catalog-source" tabindex="-1">E2E-024P: Switch the marketplace catalog source <a class="header-anchor" href="#e2e-024p-switch-the-marketplace-catalog-source" aria-label="Permalink to &quot;E2E-024P: Switch the marketplace catalog source&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Network available to <code>plugins.aiuo.net</code>, <code>raw.githubusercontent.com</code>, and <code>cnb.cool</code>.</li><li><strong>Steps</strong>: 1) Open Extensions → Marketplace on a clean profile and confirm the source line reads Official channel. 2) Switch to GitHub backup, then CNB backup, then Custom with a URL, then back to Official channel. 3) After each switch, confirm the catalog refreshes in the same surface. 4) Install a plugin from the official channel, then one from the CNB backup. 5) Choose Custom URL with an empty value.</li><li><strong>Expected</strong>: A fresh profile opens on the official channel, whose catalog comes from <code>plugins.aiuo.net/catalog.json</code>; the four choices are labelled Official channel / GitHub backup / CNB backup / Custom in that order; switching triggers a refresh and reports the new plugin count; the source selector remains the only source-status control, with no redundant provider explanation or active-source status line; the official install resolves through the platform while the CNB install downloads from the mirror and passes the same shasum verification as before, so the two backup paths are unchanged; switching back to a source reuses its cached snapshot instead of deleting it and never rounds trips; the installed record names the channel the plugin came from; choosing Custom URL with an empty value falls back to the official default rather than an empty endpoint.</li><li><strong>Specs linked</strong>: <code>07-plugins/07-plugin-marketplace.md</code> §2</li><li><strong>Acceptance</strong>: G (remote marketplace source)</li><li><strong>Status</strong>: Documented / host-core unit covered</li></ul><h4 id="e2e-024z-windows-localized-curl-diagnostics-stay-readable" tabindex="-1">E2E-024Z: Windows localized curl diagnostics stay readable <a class="header-anchor" href="#e2e-024z-windows-localized-curl-diagnostics-stay-readable" aria-label="Permalink to &quot;E2E-024Z: Windows localized curl diagnostics stay readable&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Windows x64 host. The official catalog request is forced to fail with a localized, non-UTF-8 curl/Schannel diagnostic (a deterministic fake curl in the test PATH may emit GBK stderr and exit 35).</li><li><strong>Steps</strong>: 1) Select Extensions → Marketplace with GitHub (official) as the source. 2) Refresh the marketplace. 3) Inspect the error toast. 4) Switch to the CNB mirror and refresh again.</li><li><strong>Expected</strong>: The failed request remains a <code>PLUGIN_NETWORK</code> failure and retains the readable localized diagnostic without Unicode replacement characters; switching to the mirror can refresh the catalog normally.</li><li><strong>Specs linked</strong>: <code>07-plugins/07-plugin-marketplace.md</code>, <code>03-runtime/07-process-model.md</code></li><li><strong>Acceptance</strong>: G (remote marketplace source)</li><li><strong>Status</strong>: Documented / host-core unit covered; Windows rendered validation pending</li></ul><h4 id="e2e-024b-marketplace-install-with-permission-review" tabindex="-1">E2E-024B: Marketplace install with permission review <a class="header-anchor" href="#e2e-024b-marketplace-install-with-permission-review" aria-label="Permalink to &quot;E2E-024B: Marketplace install with permission review&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: App running; official market catalog available.</li><li><strong>Steps</strong>: 1) Open Extensions → Marketplace. 2) Install <code>demo.workspace-notes</code>. 3) Read the risk-tiered permission dialog. 4) Accept high-risk permissions.</li><li><strong>Expected</strong>: Permissions are grouped High / Medium / Low with plain-language explanations before any download; the host refreshes marketplace metadata immediately before download so a stale UI cache cannot pair an old checksum with a current package; plugin installed from the marketplace package, checksum verified, permissions granted, panel/tools available; the installed tab and risk-grouped rows reflect the new plugin without a separate overview card row.</li><li><strong>Specs linked</strong>: <code>07-plugins/07-plugin-marketplace.md</code>, <code>07-plugins/13-plugin-permissions-matrix.md</code></li><li><strong>Acceptance</strong>: G (marketplace install + permission review)</li><li><strong>Status</strong>: Documented / host-core covered by unit tests + protocol methods</li></ul><h4 id="e2e-024r-install-a-center-published-plugin-from-the-unchanged-distribution-source" tabindex="-1">E2E-024R: Install a center-published plugin from the unchanged distribution source <a class="header-anchor" href="#e2e-024r-install-a-center-published-plugin-from-the-unchanged-distribution-source" aria-label="Permalink to &quot;E2E-024R: Install a center-published plugin from the unchanged distribution source&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A <code>schemaVersion: 2</code> catalog served from the distribution repository, with one plugin carrying provenance and an approved review verdict, its package under <code>packages/</code>. A second fixture declares <code>artifactBaseUrl</code> for the mirror/enterprise case.</li><li><strong>Steps</strong>: 1) Keep the marketplace source at its default. 2) Open a plugin&#39;s detail sheet. 3) Read the Source section. 4) Install the selected version. 5) Switch to the CNB mirror and repeat the install. 6) Repeat against the fixture that declares a base.</li><li><strong>Expected</strong>: No settings change or client update is needed to see center-published plugins, because the catalog URL is unchanged; the relative package URL resolves against the catalog directory, so GitHub serves it from <code>raw.githubusercontent.com</code> and the mirror from <code>cnb.cool</code> with an identical checksum; a declared <code>artifactBaseUrl</code> takes precedence when present; the detail sheet shows the source repository, commit, and builder before install; the installed record keeps publisher, trust tier, and source pin.</li><li><strong>Specs linked</strong>: <code>07-plugins/07-plugin-marketplace.md</code>, <code>07-plugins/15-plugin-center.md</code></li><li><strong>Acceptance</strong>: G (publisher-owned source distribution)</li><li><strong>Status</strong>: Documented / host-core covered by unit tests</li></ul><h4 id="e2e-024s-package-host-allowlist-refuses-an-untrusted-download" tabindex="-1">E2E-024S: Package host allowlist refuses an untrusted download <a class="header-anchor" href="#e2e-024s-package-host-allowlist-refuses-an-untrusted-download" aria-label="Permalink to &quot;E2E-024S: Package host allowlist refuses an untrusted download&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A catalog fixture whose version URL points at a host outside the allowlist, plus one with embedded credentials and one on plain HTTP.</li><li><strong>Steps</strong>: 1) Refresh the marketplace. 2) Inspect the plugin card and detail sheet. 3) Attempt an install. 4) Repeat with a private catalog whose packages sit on its own host.</li><li><strong>Expected</strong>: The row does not offer an install action for an off-allowlist URL; an attempted install fails with <code>PLUGIN_MARKET_UNTRUSTED_HOST</code> naming the rejected host before any request leaves the machine; a credentialed URL and non-loopback plain HTTP are refused the same way; a private catalog can still serve packages from the host that served it, without widening the allowlist for third-party hosts.</li><li><strong>Specs linked</strong>: <code>07-plugins/07-plugin-marketplace.md</code>, <code>07-plugins/04-plugin-security.md</code></li><li><strong>Acceptance</strong>: G (marketplace download boundary)</li><li><strong>Status</strong>: Documented / host-core covered by unit tests</li></ul><h4 id="e2e-024t-a-withdrawn-version-is-not-offered-installed-or-hidden" tabindex="-1">E2E-024T: A withdrawn version is not offered, installed, or hidden <a class="header-anchor" href="#e2e-024t-a-withdrawn-version-is-not-offered-installed-or-hidden" aria-label="Permalink to &quot;E2E-024T: A withdrawn version is not offered, installed, or hidden&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A catalog where the newest version is <code>yanked</code> with a reason, an older version is live, and the yanked version is installed locally.</li><li><strong>Steps</strong>: 1) Open the plugin&#39;s detail sheet. 2) Inspect version history. 3) Select the withdrawn version. 4) Run Check for updates. 5) Run Apply automatic updates. 6) Inspect the installed row.</li><li><strong>Expected</strong>: The offered latest version is the newest non-yanked one; the withdrawn version stays in history with its reason and a struck-through number; selecting it disables the install action with withdrawn copy rather than not-yet-published copy; an explicit install of it fails with <code>PLUGIN_MARKET_YANKED</code> and the reason; no update is offered when the newest live version is not newer than the installed one, so a downgrade is never presented as an update; the installed row is flagged as needing attention and the plugin keeps running.</li><li><strong>Specs linked</strong>: <code>07-plugins/08-plugin-signing-updates.md</code></li><li><strong>Acceptance</strong>: G (incident response propagation)</li><li><strong>Status</strong>: Documented / host-core covered by unit tests</li></ul><h4 id="e2e-024u-trust-tier-and-host-version-bound-are-enforced-by-the-client" tabindex="-1">E2E-024U: Trust tier and host version bound are enforced by the client <a class="header-anchor" href="#e2e-024u-trust-tier-and-host-version-bound-are-enforced-by-the-client" aria-label="Permalink to &quot;E2E-024U: Trust tier and host version bound are enforced by the client&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A custom-source catalog claiming <code>trust: &quot;verified&quot;</code>, a catalog with an unrecognised tier, and a version whose <code>minPiDesktop</code> exceeds the running host.</li><li><strong>Steps</strong>: 1) Point the marketplace at the custom source. 2) Inspect the card and detail sheet badges. 3) Attempt to install the version pinned to a newer host. 4) Repeat with a <code>minPiDesktop</code> that is a range expression rather than a version.</li><li><strong>Expected</strong>: A <code>verified</code> claim from a non-official source renders as community with no shield; an unrecognised tier renders as unknown; the version requiring a newer host is not offered and an explicit install fails with <code>PLUGIN_HOST_TOO_OLD</code> naming both versions; an unparseable bound is ignored rather than making the plugin uninstallable.</li><li><strong>Specs linked</strong>: <code>07-plugins/07-plugin-marketplace.md</code>, <code>07-plugins/15-plugin-center.md</code></li><li><strong>Acceptance</strong>: G (trust presentation)</li><li><strong>Status</strong>: Documented / host-core covered by unit tests</li></ul><h4 id="e2e-024v-publisher-pins-a-version-with-pi-plugin-publish" tabindex="-1">E2E-024V: Publisher pins a version with pi-plugin publish <a class="header-anchor" href="#e2e-024v-publisher-pins-a-version-with-pi-plugin-publish" aria-label="Permalink to &quot;E2E-024V: Publisher pins a version with pi-plugin publish&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A plugin directory inside a git repository with an https or ssh origin remote.</li><li><strong>Steps</strong>: 1) Run <code>pi-plugin publish</code> with uncommitted changes. 2) Commit and tag, then rerun. 3) Rerun on a commit with no tag. 4) Rerun with a remote containing credentials. 5) Feed the resulting registry entry through the center&#39;s catalog build and the client preflight.</li><li><strong>Expected</strong>: A dirty worktree is refused so the submitted commit describes the packaged bytes; a clean run writes the package, its sha256, the canonical repository URL, the tag ref, the resolved commit, and the plugin subdirectory into a submission payload; an untagged commit is accepted with a warning; a credentialed remote is refused; the generated catalog passes the same preflight the client ships.</li><li><strong>Specs linked</strong>: <code>07-plugins/10-plugin-devex.md</code>, <code>07-plugins/15-plugin-center.md</code></li><li><strong>Acceptance</strong>: G (publisher tooling)</li><li><strong>Status</strong>: Documented / devkit covered by unit tests</li></ul><h4 id="e2e-024c-plugin-package-install-and-auto-update-path" tabindex="-1">E2E-024C: Plugin package install and auto-update path <a class="header-anchor" href="#e2e-024c-plugin-package-install-and-auto-update-path" aria-label="Permalink to &quot;E2E-024C: Plugin package install and auto-update path&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Marketplace catalog has a newer version or local <code>.piplug</code>.</li><li><strong>Steps</strong>: 1) Install package from the header overflow menu. 2) Enable auto-update from the row overflow menu. 3) Reopen Extensions while the marketplace is slow or unavailable and confirm the Installed surface and Marketplace tab remain usable. 4) Confirm the installed list refreshes update metadata from the local catalog. 5) Run Check for updates, then Apply automatic updates.</li><li><strong>Expected</strong>: The Extensions surface does not wait for a remote request during its silent cache-only check; the row moves to Updates available and the update banner reports the count; the highest semantic version is selected even when the catalog version array is not newest-first; the explicit check fetches the current catalog and uses the cached catalog offline; permissions the new version adds are tagged New in the review dialog; auto-update applies only when the permission diff is empty or pre-granted.</li><li><strong>Specs linked</strong>: <code>07-plugins/06-plugin-packaging.md</code>, <code>07-plugins/08-plugin-signing-updates.md</code></li><li><strong>Acceptance</strong>: G (package install + update policy)</li><li><strong>Status</strong>: Documented</li></ul><h4 id="e2e-024q-marketplace-update-diagnosis-and-release-data-gate" tabindex="-1">E2E-024Q: Marketplace update diagnosis and release-data gate <a class="header-anchor" href="#e2e-024q-marketplace-update-diagnosis-and-release-data-gate" aria-label="Permalink to &quot;E2E-024Q: Marketplace update diagnosis and release-data gate&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A catalog fixture contains an installed <code>0.5.0</code> plugin, a <code>0.5.1</code> version, and either unsorted version entries or intentionally missing package metadata.</li><li><strong>Steps</strong>: 1) Record the plugin ID, installed version, displayed latest version, catalog URL, and local cache path. 2) Fetch the live/fixture catalog and inspect the exact entry. 3) Inspect the installed registry separately. 4) Run <code>pnpm check:marketplace -- --url &lt;catalog&gt; --plugin &lt;id&gt;</code>. 5) Run the update check against the fixture.</li><li><strong>Expected</strong>: The failure is classified as catalog data, fetch/cache, host comparison, IPC propagation, or renderer presentation before code is changed; the preflight reports every missing checksum, URL, package size, or permissions field; an incomplete release may be shown for discovery but is not installable; a valid <code>0.5.1</code> is detected regardless of catalog ordering; the final fix includes a regression test for the diagnosed boundary.</li><li><strong>Specs linked</strong>: <code>07-plugins/07-plugin-marketplace.md</code>, <code>07-plugins/08-plugin-signing-updates.md</code>, <code>06-delivery/03-ai-development-workflow.md</code></li><li><strong>Acceptance</strong>: G (marketplace update diagnosis)</li><li><strong>Status</strong>: Documented / preflight script</li></ul><h4 id="e2e-024h-installed-plugins-surface-state-risk-and-failures" tabindex="-1">E2E-024H: Installed plugins surface state, risk, and failures <a class="header-anchor" href="#e2e-024h-installed-plugins-surface-state-risk-and-failures" aria-label="Permalink to &quot;E2E-024H: Installed plugins surface state, risk, and failures&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: At least one enabled plugin, one disabled plugin, and one plugin whose load failed.</li><li><strong>Steps</strong>: 1) Open Extensions → Installed while plugin changed events are arriving in a burst. 2) Read the tab and group counts. 3) Confirm the failed plugin sits under Needs attention with its error message. 4) Search by author and by permission. 5) Clear the search.</li><li><strong>Expected</strong>: The page remains usable during transient host backpressure: identical plugin and extension registry refreshes coalesce, and renderer-facing RPC calls retry explicit <code>HOST_OVERLOADED</code> responses with bounded backoff before surfacing an error. Rows group as Needs attention / Updates available / Active / Turned off with counts; <code>status: &quot;error&quot; | &quot;load_error&quot;</code> renders the error message inline instead of being silent; each row defaults to a two-line name/id/version summary, while a Details disclosure reveals risk-tinted permission chips, capabilities, and resident service status; the single scope trigger opens explained Off / This project / Everywhere choices and the project picker; row icon actions expose hover/focus labels; the result count reflects the filtered subset and clearing restores every group.</li><li><strong>Specs linked</strong>: <code>04-ux/01-ui-ia.md</code>, <code>07-plugins/13-plugin-permissions-matrix.md</code></li><li><strong>Acceptance</strong>: G (installed plugin management)</li><li><strong>Status</strong>: Documented</li></ul><h4 id="e2e-024d-isolated-plugin-panel-host-bridge" tabindex="-1">E2E-024D: Isolated plugin panel host bridge <a class="header-anchor" href="#e2e-024d-isolated-plugin-panel-host-bridge" aria-label="Permalink to &quot;E2E-024D: Isolated plugin panel host bridge&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Plugin with <code>ui.panel</code> enabled.</li><li><strong>Steps</strong>: 1) Set the app language to English and open a panel whose manifest declares localized <code>ui.title.en</code> and <code>ui.title.zh-CN</code>; confirm the native window/launcher identity remains available without a host-rendered title. 2) Set the app language to Simplified Chinese and reopen the panel; confirm the panel content remains plugin-owned. 3) While the panel stays open, switch the app language to Korean and confirm the live <code>appearance:changed</code> event updates the panel controls, safe-area reminder, and accessible labels without reopening it. 4) Open the panel on macOS, Windows, and Linux; confirm the same frameless 46px drag band, fixed top-right capsule fully contained inside that band, and exactly three accessible controls. 5) Exercise minimize, maximize, restore, close, keyboard focus, light/dark themes, page-defined light/dark backgrounds, and reduced motion on every platform. 6) Render a plugin-owned titlebar/toolbar; verify fixed/sticky UI uses <code>--pi-plugin-titlebar-height</code>, its interactive controls use <code>no-drag</code>, and clicks outside the capsule in the top 46px are treated as window dragging. 7) On Windows with classic scrollbars, scroll a panel with content overflow and inspect the right edge. 8) Open a development plugin and confirm the localized reminder explains that the top 46px is not clickable outside the capsule. 9) Reopen a minimized panel. 10) Invoke panel bridge APIs (<code>ui.showToast</code>, optional fs/net with grants). 11) Close the panel from the capsule and by disabling or uninstalling the plugin.</li><li><strong>Expected</strong>: Panel runs in its sandboxed window/partition; all three platforms use one host-owned frameless chrome contract with no native traffic lights, host-rendered title, or application menu; the top drag band is exactly 46px, and the minimal capsule stays fixed at the top-right without exceeding it. The capsule contains minimize/maximize-or-restore/close, follows the plugin page&#39;s surface/text colors, and never forces a black surface onto a light page. A v2 page marked <code>pi-plugin-chrome</code> uses <code>--pi-plugin-titlebar-height</code> and starts its own content directly below the 46px band without an additive duplicate spacer; a legacy page keeps the compatibility offset. A panel&#39;s stable scrollbar gutter is scoped to its actual content scroller; Windows does not show a second root-level empty side rail outside the page surface. The plugin owns its title and toolbar; the host drag strip remains usable, blocks clicks outside the capsule, and development panels alone show the reminder. Reopening restores the existing panel; switching to Korean while the panel remains open updates the host capsule, reminder, and accessible labels in place; bridge calls remain permission-checked and the host remains stable on panel close. Closing a panel must not throw a main-process <code>TypeError: Object has been destroyed</code> or show an uncaught-exception dialog.</li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code>, <code>04-ux/07-ui-design-system.md</code>, <code>07-plugins/01-plugin-system.md</code>, <code>07-plugins/03-plugin-api.md</code>, <code>07-plugins/04-plugin-security.md</code>, <code>07-plugins/12-plugin-ipc-and-host-services.md</code>, ADR 0081, ADR 0082, ADR 0092, ADR 0093</li><li><strong>Acceptance</strong>: G (isolated panel)</li><li><strong>Status</strong>: Documented</li></ul><h4 id="e2e-024aa-plugin-owned-ui-follows-the-host-locale" tabindex="-1">E2E-024AA: Plugin-owned UI follows the host locale <a class="header-anchor" href="#e2e-024aa-plugin-owned-ui-follows-the-host-locale" aria-label="Permalink to &quot;E2E-024AA: Plugin-owned UI follows the host locale&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A loaded plugin with a panel or settings destination, and a plugin process subscribed to <code>pi.events.on(&quot;appearance:changed&quot;)</code>.</li><li><strong>Steps</strong>: 1) Call <code>pi.app.getLocale</code> / <code>app.getAppearance</code> and confirm the tag matches Settings → language. 2) Switch the app language while the plugin stays loaded. 3) Confirm generated <code>contributes.settings</code> titles remain the author-language strings. 4) Confirm the plugin process and any open panel receive <code>appearance:changed</code> with the new <code>locale</code> and restyle their own copy.</li><li><strong>Expected</strong>: The host publishes language only. Plugin-owned copy changes without a reload. The generated settings sheet does not parse <code>{ en, &quot;zh-CN&quot; }</code> maps. Host-owned identity still follows <code>manifest.i18n</code> (ADR 0267).</li><li><strong>Specs linked</strong>: <code>07-plugins/03-plugin-api.md</code>, <code>07-plugins/02-plugin-manifest-schema.md</code>, <code>04-ux/02-i18n-english-first.md</code>, ADR 0280</li><li><strong>Acceptance</strong>: G (plugin i18n)</li><li><strong>Status</strong>: Automated in part (<code>apps/desktop/test/plugin-settings.test.mjs</code>, <code>plugin-work-panel-views.test.mjs</code>); UI walk-through Documented</li></ul><h4 id="e2e-024y-large-file-plugin-reads-and-dropped-file-grants-stay-host-gated" tabindex="-1">E2E-024Y: Large-file plugin reads and dropped-file grants stay host-gated <a class="header-anchor" href="#e2e-024y-large-file-plugin-reads-and-dropped-file-grants-stay-host-gated" aria-label="Permalink to &quot;E2E-024Y: Large-file plugin reads and dropped-file grants stay host-gated&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A test plugin declares <code>fs.read</code>; the workspace contains a readable log and a protected credential fixture; the plugin panel is open.</li><li><strong>Steps</strong>: 1) Pick a directory and open a large log. 2) Confirm initial page, search, follow, and a file-growth poll complete through <code>fs.stat</code> and bounded <code>fs.readRange</code>. 3) Drag a regular file into the panel and open it. 4) Drag a second file or forge an absolute path with the first grant and retry. 5) Reload/unload the plugin and retry the old grant.</li><li><strong>Expected</strong>: Directory reads remain relative to the selected root and are permission/audit checked. Range reads reject invalid values and lengths above 8 MiB, return an empty byte array at EOF, and preserve total size. A real drop creates a one-file, read-only, memory-only grant; the dropped file uses the same paging/search/follow engine, while another path, a protected path, and a grant after unload fail closed with <code>PERMISSION_DENIED</code> or <code>NOT_FOUND</code>. No renderer worker or raw plugin <code>node:fs</code> path is used.</li><li><strong>Specs linked</strong>: <code>07-plugins/03-plugin-api.md</code>, <code>07-plugins/04-plugin-security.md</code>, <code>07-plugins/12-plugin-ipc-and-host-services.md</code>, <code>07-plugins/13-plugin-permissions-matrix.md</code>, ADR 0190</li><li><strong>Acceptance</strong>: Security + G (plugin host services)</li><li><strong>Status</strong>: Unit/integration-covered; real drag gesture remains manual</li></ul><h4 id="e2e-024e-high-risk-plugin-apis-require-grants" tabindex="-1">E2E-024E: High-risk plugin APIs require grants <a class="header-anchor" href="#e2e-024e-high-risk-plugin-apis-require-grants" aria-label="Permalink to &quot;E2E-024E: High-risk plugin APIs require grants&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Notes plugin installed with explicit grants.</li><li><strong>Steps</strong>: 1) Call <code>fs.writeText</code> / <code>net.fetch</code> / <code>shell.openExternal</code> through plugin runtime or panel bridge. 2) Revoke one permission and retry.</li><li><strong>Expected</strong>: Granted calls succeed with audit; revoked/undeclared calls fail with <code>PERMISSION_DENIED</code> and do not crash the app.</li><li><strong>Specs linked</strong>: <code>07-plugins/13-plugin-permissions-matrix.md</code>, <code>07-plugins/04-plugin-security.md</code></li><li><strong>Acceptance</strong>: Security + G</li><li><strong>Status</strong>: Documented</li></ul><h4 id="e2e-024w-plugin-clipboard-history-captures-bounded-text-and-images" tabindex="-1">E2E-024W: Plugin clipboard history captures bounded text and images <a class="header-anchor" href="#e2e-024w-plugin-clipboard-history-captures-bounded-text-and-images" aria-label="Permalink to &quot;E2E-024W: Plugin clipboard history captures bounded text and images&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: The app is running; a test plugin declares and is granted <code>clipboard.read</code>; the Composer can receive one text paste and one image paste.</li><li><strong>Steps</strong>: 1) Paste text into the Composer, paste an image into the Composer, then invoke <code>pi.clipboard.getHistory()</code>. 2) Invoke it again and mutate the returned image bytes. 3) Paste the same text consecutively and invoke the API. 4) Leave the app idle with an image on the OS clipboard and verify no clipboard sampling occurs. 5) Revoke <code>clipboard.read</code> and invoke it again. 6) Add fixtures over the text/image caps and older than the retention window.</li><li><strong>Expected</strong>: The result is newest-first with text and image entries interleaved, ISO timestamps, PNG bytes, and image dimensions; mutating the result does not mutate host state. Consecutive duplicates collapse with a refreshed timestamp. Entries over the per-entry caps and expired entries are absent, and the host total/entry caps are enforced. A paste causes only the event&#39;s already-read content to be recorded; the host does not reread the OS clipboard or sample it while idle. The API reuses the <code>clipboard.read</code> grant, denied calls fail with <code>PERMISSION_DENIED</code>, and a successful call emits an audit entry containing the returned entry count.</li><li><strong>Specs linked</strong>: <code>07-plugins/03-plugin-api.md</code>, <code>07-plugins/04-plugin-security.md</code>, <code>07-plugins/13-plugin-permissions-matrix.md</code>, ADR 0115</li><li><strong>Acceptance</strong>: G (plugin clipboard history) + Security</li><li><strong>Status</strong>: Unit-covered (<code>clipboard-history.test.mjs</code>); Electron clipboard capture remains manual</li></ul><h4 id="e2e-024i-plugin-skills-reach-the-agent-and-load-on-demand" tabindex="-1">E2E-024I: Plugin skills reach the agent and load on demand <a class="header-anchor" href="#e2e-024i-plugin-skills-reach-the-agent-and-load-on-demand" aria-label="Permalink to &quot;E2E-024I: Plugin skills reach the agent and load on demand&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: <code>examples/plugins/hello</code> enabled with <code>agent.prompt.inject</code> granted; a second copy of the manifest without that permission available; one workspace that is a plugin directory and one that is not.</li><li><strong>Steps</strong>: 1) Start a session and ask the agent what skills it has. 2) Ask it to follow the Hello demo skill so it calls the <code>Skill</code> tool. 3) Edit the skill document and repeat step 2. 4) Disable the plugin and start a new turn. 5) Load the variant without <code>agent.prompt.inject</code> and repeat step 1. 6) Declare a document larger than the per-skill cap. 7) Open each of the two workspaces in turn.</li><li><strong>Expected</strong>: The catalog lists the skill id, name, and trimmed description but no body, after the built-in skills and before the project instruction chain; the <code>Skill</code> schema is loaded through <code>ToolSearch</code> only when requested and reads the edited file without a restart; disabling the plugin rebuilds the runtime so the skill disappears from the next turn; the variant without the permission loads normally and contributes no skills; the oversized document is skipped with an audit line rather than clamped into the prompt; the built-in <code>plugin-development</code> skill is catalogued in the plugin workspace and absent in the other, while <code>PluginCheck</code> is listed in the bounded on-demand tool catalog in both.</li><li><strong>Specs linked</strong>: <code>07-plugins/02-plugin-manifest-schema.md</code>, <code>07-plugins/04-plugin-security.md</code> §7.1, <code>07-plugins/10-plugin-devex.md</code>, ADR 0039, ADR 0037, D174</li><li><strong>Acceptance</strong>: G (skill activation) + E (tools &amp; permissions) + D (high-risk permission gating)</li><li><strong>Status</strong>: Unit-covered (<code>plugin-skills.test.mjs</code>, agent-runtime prompt/digest tests); agent-facing scenario Draft</li></ul><h4 id="e2e-024j-plugin-theme-applies-and-falls-back-when-withdrawn" tabindex="-1">E2E-024J: Plugin theme applies and falls back when withdrawn <a class="header-anchor" href="#e2e-024j-plugin-theme-applies-and-falls-back-when-withdrawn" aria-label="Permalink to &quot;E2E-024J: Plugin theme applies and falls back when withdrawn&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: <code>examples/plugins/hello</code> enabled with <code>ui.theme</code> granted; a plugin whose CSS uses <code>@import</code> or a remote <code>url()</code> available for the rejection case, plus a variant of it that only names those tokens inside a comment; a third variant whose theme declares an image asset and a <code>windowAppearance</code> background, with and without <code>ui.window.appearance</code>.</li><li><strong>Steps</strong>: 1) Open Settings → General → Theme and pick <code>Hello Midnight</code>. 2) Restart the app. 3) Disable the providing plugin. 4) Re-enable it, then uninstall it. 5) Load the plugin with unsafe CSS. 6) Load the comment-only variant. 7) Select the asset variant&#39;s theme on Windows/Linux and on macOS, and check the panel opened from that plugin. 8) Deselect its theme after removing <code>ui.window.appearance</code>.</li><li><strong>Expected</strong>: The plugin theme appears in the picker alongside the built-ins and applies immediately; the choice survives restart as <code>plugin:demo.hello:midnight</code>; disabling or uninstalling the provider falls back to <code>system</code> instead of an unstyled shell; unsafe CSS is refused at load with the reason logged and no <code>&lt;style&gt;</code> element injected; the comment-only sheet loads and contributes its theme, because the sanitizer only inspects CSS the browser would apply; the declared asset paints through <code>plugin-asset:</code> in the shell and in the plugin&#39;s own panel, an undeclared reference is refused with the reason logged, the declared background colours the native window on Windows/Linux and is never sent on macOS, and deselecting the theme or dropping the grant returns the window to the host background; the whole shell follows the theme, including the work-panel column, its header, and the browser/file viewer strips, all of which read <code>--ds-bg-dock</code> / <code>--ds-bg-dock-raised</code> rather than a literal.</li><li><strong>Specs linked</strong>: <code>07-plugins/04-plugin-security.md</code> §3.1, <code>04-ux/07-ui-design-system.md</code>, D175</li><li><strong>Acceptance</strong>: G (theme contribution) + Security</li><li><strong>Status</strong>: Unit-covered (<code>plugin-themes.test.mjs</code>, <code>theme-css</code> SDK tests); visual scenario Draft</li></ul><h4 id="e2e-plugin-runtime-theme-apis" tabindex="-1">E2E-PLUGIN-runtime-theme-apis <a class="header-anchor" href="#e2e-plugin-runtime-theme-apis" aria-label="Permalink to &quot;E2E-PLUGIN-runtime-theme-apis&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A plugin with <code>ui.theme</code> that can drive <code>pi.app.setTheme</code> and <code>pi.themes.upsert</code> / <code>remove</code> (panel or command). Settings → General shows the searchable theme picker.</li><li><strong>Steps</strong>: 1) From the plugin UI, call <code>themes.upsert</code> with a new theme id and distinct CSS. 2) Confirm the theme appears in Settings without reload/disable. 3) Call <code>app.setTheme</code> to select it. 4) Call <code>themes.upsert</code> again with different CSS while it is active. 5) Call <code>themes.remove</code> on an inactive theme. 6) Call <code>app.setTheme</code> with an unknown id. 7) Upsert a ninth theme (former hard cap was 8).</li><li><strong>Expected</strong>: The new theme is listed and applies immediately through the same path as Settings; live CSS edits restyle the shell without plugin reload; remove drops the picker row and the event refreshes the list; an unknown id rejects with <code>INVALID_ARGUMENT</code> and leaves the preference unchanged; the ninth theme is accepted. <code>settingsChanged</code> reaches the renderer store; <code>appearance:changed</code> reaches open panels.</li><li><strong>Specs linked</strong>: <code>07-plugins/03-plugin-api.md</code>, <code>07-plugins/12-plugin-ipc-and-host-services.md</code>, <code>07-plugins/13-plugin-permissions-matrix.md</code>, ADR 0260, D417</li><li><strong>Acceptance</strong>: G (theme contribution) + Security</li><li><strong>Status</strong>: Unit-covered (<code>plugin-themes.test.mjs</code>); interactive scenario Draft</li></ul><h4 id="e2e-plugin-sidebar-gradient-token" tabindex="-1">E2E-PLUGIN-sidebar-gradient-token <a class="header-anchor" href="#e2e-plugin-sidebar-gradient-token" aria-label="Permalink to &quot;E2E-PLUGIN-sidebar-gradient-token&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A plugin theme that sets <code>--ds-bg-sidebar</code> to a solid color and <code>--ds-bg-sidebar-image</code> to a <code>linear-gradient(...)</code>; Windows/Linux and macOS shells.</li><li><strong>Steps</strong>: 1) Select the theme. 2) Inspect the sidebar plate and rail. 3) Confirm borders/glass tint still resolve from the color token. 4) Switch back to a built-in theme.</li><li><strong>Expected</strong>: The gradient paints as <code>background-image</code> over the color plate on all platforms; macOS sheen still overlays the image layer; borders and <code>color-mix</code> consumers do not break; clearing the token returns the plain sidebar.</li><li><strong>Specs linked</strong>: <code>04-ux/07-ui-design-system.md</code>, ADR 0260, D417</li><li><strong>Acceptance</strong>: Visual / platform</li><li><strong>Status</strong>: CSS unit contracts in <code>plugin-themes.test.mjs</code>; visual scenario Draft</li></ul><h4 id="e2e-024k-plugin-mcp-server-tools-reach-the-agent" tabindex="-1">E2E-024K: Plugin MCP server tools reach the agent <a class="header-anchor" href="#e2e-024k-plugin-mcp-server-tools-reach-the-agent" aria-label="Permalink to &quot;E2E-024K: Plugin MCP server tools reach the agent&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A plugin declaring one <code>stdio</code> and one non-loopback HTTP MCP server against trusted local-network stubs; <code>mcp.server.local</code> and <code>mcp.server.remote</code> granted; the HTTP host is listed in <code>net.domains</code>; a settings key holding the stub credential.</li><li><strong>Steps</strong>: 1) Enable the plugin and confirm no server process starts yet. 2) Ask the agent to call a discovered tool. 3) Inspect the stub&#39;s received environment/headers. 4) Make the stub fail a call and time one out. 5) Grow the stdio stub&#39;s catalog past the old 64-tool cap and re-discover it. 6) Disable the plugin.</li><li><strong>Expected</strong>: Servers connect lazily on first use; tools appear as <code>plugin_demo_*_&lt;serverId&gt;_&lt;tool&gt;</code> at <code>risk: &quot;medium&quot;</code> with per-call audit; the stdio child receives only the declared <code>env</code> values plus PATH/temp/locale, never host provider keys; the non-loopback HTTP endpoint is accepted only because its host is declared, and its unencrypted transport is visible in review; a redirect to an undeclared host is blocked before the second request; failures and timeouts return tool errors without crashing the plugin or the host; a catalog larger than the old 64-tool cap arrives whole, while a server that breaks a per-server guard (count, pages, cursor, traversal time) is refused instead of contributing a prefix of its catalog; disable disconnects both servers.</li><li><strong>Specs linked</strong>: <code>07-plugins/02-plugin-manifest-schema.md</code>, <code>07-plugins/04-plugin-security.md</code> §8.1, ADR 0038, ADR 0142, D176, D281, D452</li><li><strong>Acceptance</strong>: G (MCP bridge) + E (tools &amp; permissions) + Security</li><li><strong>Status</strong>: Unit-covered (<code>plugin-mcp.test.mjs</code> stdio + HTTP stubs); agent-facing scenario Draft</li></ul><h4 id="e2e-024l-resident-plugin-service-is-supervised-and-visible" tabindex="-1">E2E-024L: Resident plugin service is supervised and visible <a class="header-anchor" href="#e2e-024l-resident-plugin-service-is-supervised-and-visible" aria-label="Permalink to &quot;E2E-024L: Resident plugin service is supervised and visible&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: <code>examples/plugins/hello</code> enabled with <code>background.service</code> granted.</li><li><strong>Steps</strong>: 1) Open Extensions → Installed and expand Details on the plugin row to read the <code>Greeter heartbeat</code> chip. 2) Kill the plugin&#39;s utility process and watch the chip. 3) Kill it repeatedly past the restart ceiling. 4) Disable and re-enable the plugin. 5) Revoke <code>background.service</code> and reload.</li><li><strong>Expected</strong>: The Details disclosure exposes a chip that reports <code>running</code> after load; a kill shows <code>failed</code> then <code>running</code> again with an incremented restart count and backoff between attempts; past five attempts the plugin stays <code>failed</code> and stops retrying; manual disable/enable cancels the pending timer and resets the counter; without the permission the service never starts and the skip is audited.</li><li><strong>Specs linked</strong>: <code>07-plugins/05-plugin-lifecycle.md</code> §3.1, ADR 0040, D177</li><li><strong>Acceptance</strong>: G (resident services)</li><li><strong>Status</strong>: Unit-covered (<code>plugin-services.test.mjs</code> supervision + backoff); manual kill scenario Draft</li></ul><h4 id="e2e-024m-bus-messages-cross-plugins-only-as-declared" tabindex="-1">E2E-024M: Bus messages cross plugins only as declared <a class="header-anchor" href="#e2e-024m-bus-messages-cross-plugins-only-as-declared" aria-label="Permalink to &quot;E2E-024M: Bus messages cross plugins only as declared&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Two plugins enabled — one publishing <code>demo.*</code> topics, one subscribing <code>demo.**</code> — with <code>bus.publish</code> / <code>bus.subscribe</code> granted.</li><li><strong>Steps</strong>: 1) Run the publisher&#39;s command and watch the subscriber. 2) Publish a topic absent from <code>contributes.bus.publish</code>. 3) Subscribe to a pattern absent from <code>contributes.bus.subscribe</code>. 4) Publish a payload over 64KB and exceed 100 publishes in 10s. 5) Unload the subscriber and publish again.</li><li><strong>Expected</strong>: The subscriber receives <code>{ topic, from, payload, at }</code> and the publisher never receives its own message; undeclared publish and subscribe both fail <code>PERMISSION_DENIED</code> with an audit line naming the topic; the oversized payload and the rate burst fail <code>LIMIT_EXCEEDED</code> / <code>RATE_LIMITED</code>; publishing to a departed subscriber succeeds with a smaller fan-out and no host error.</li><li><strong>Specs linked</strong>: <code>07-plugins/02-plugin-manifest-schema.md</code> §5.1, <code>07-plugins/04-plugin-security.md</code> §5.1, ADR 0040, D178</li><li><strong>Acceptance</strong>: G (message bus) + Security</li><li><strong>Status</strong>: Unit-covered (<code>plugin-bus.test.mjs</code> delivery, filtering, caps); two-plugin manual scenario Draft</li></ul><h4 id="e2e-024n-extensions-page-density-and-theme-readable-actions" tabindex="-1">E2E-024N: Extensions page density and theme-readable actions <a class="header-anchor" href="#e2e-024n-extensions-page-density-and-theme-readable-actions" aria-label="Permalink to &quot;E2E-024N: Extensions page density and theme-readable actions&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: App running with at least one installed extension and one available marketplace action; dark and light themes available.</li><li><strong>Steps</strong>: 1) Open Extensions in dark theme. 2) On Windows/Linux, confirm the compact two-tier header presents the extension mark/title and contextual action rail below the native window-control band, then confirm the Installed and Marketplace tabs are the only tabs and both reach the content without a four-card numeric overview band or explanatory header/section paragraphs. 3) Confirm installed rows begin as a quiet two-line summary, then expand Details on one row and inspect its capabilities, service status, and permissions. 4) Use the compact scope control and its explained scope menu, contextual primary action, and a secondary update/action button. 5) Switch to light theme and repeat. 6) Keyboard-focus the Details disclosure, scope states, and each action.</li><li><strong>Expected</strong>: The four numeric overview cards are absent; the header uses a quiet 24px extension mark, 18px title, restrained 30px action controls, and a visible keyboard focus ring; tab counts, installed group counts, and any update alert remain available in their relevant surfaces. The page header, section headers, empty states, and update alert use compact labels and actions; decision-specific explanations remain in disclosures, details, and dialogs. Installed rows keep their default height low while the disclosure exposes the complete secondary readout. The scope trigger stays aligned with the row action rail, its menu explains each state, and icon actions remain visible at rest while showing labels on hover and focus. Primary and secondary buttons keep visible semantic surfaces, text, borders, hover states, and focus rings in both themes, and keyboard focus does not depend on pointer hover.</li><li><strong>Specs linked</strong>: <code>04-ux/01-ui-ia.md</code>, <code>04-ux/07-ui-design-system.md</code>, <code>07-plugins/07-plugin-marketplace.md</code>, ADR 0058, D196</li><li><strong>Acceptance</strong>: G (Extensions page) + Quality</li><li><strong>Status</strong>: Unit-covered (<code>extensions-page.test.mjs</code>, <code>plugins-page-style.test.mjs</code>); visual scenario Draft</li></ul><h4 id="e2e-024o-marketplace-hides-development-only-sample-plugins" tabindex="-1">E2E-024O: Marketplace hides development-only sample plugins <a class="header-anchor" href="#e2e-024o-marketplace-hides-development-only-sample-plugins" aria-label="Permalink to &quot;E2E-024O: Marketplace hides development-only sample plugins&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: App running; the official catalog contains the development fixtures <code>demo.hello</code> or <code>demo.workspace-summary</code> and at least one product plugin.</li><li><strong>Steps</strong>: 1) Open Extensions → Marketplace with an empty search. 2) Search for <code>Hello</code>, <code>Workspace Notes</code>, and <code>Workspace Summary</code> in turn. 3) Inspect the category filters and result cards. 4) Open Installed and verify an already-installed sample remains manageable.</li><li><strong>Expected</strong>: Entries whose IDs begin with <code>demo.</code> never appear in the marketplace cards, categories, or search results; product plugins remain discoverable. An already-installed sample is still listed under Installed so it can be disabled or uninstalled rather than becoming an unmanaged runtime.</li><li><strong>Specs linked</strong>: <code>07-plugins/07-plugin-marketplace.md</code>, <code>04-ux/01-ui-ia.md</code></li><li><strong>Acceptance</strong>: G (Extensions page) + Quality</li><li><strong>Status</strong>: Unit-covered (<code>extensions-page.test.mjs</code>); visual scenario Draft</li></ul><h4 id="e2e-025-disable-plugin-removes-contributions" tabindex="-1">E2E-025: Disable plugin removes contributions <a class="header-anchor" href="#e2e-025-disable-plugin-removes-contributions" aria-label="Permalink to &quot;E2E-025: Disable plugin removes contributions&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Plugin enabled and contributions visible.</li><li><strong>Steps</strong>: 1) Disable the plugin on the Extensions page. 2) Check global search and agent tools.</li><li><strong>Expected</strong>: Commands and tools disappear; no leftover contributions.</li><li><strong>Specs linked</strong>: <code>07-plugins/05-plugin-lifecycle.md</code></li><li><strong>Acceptance</strong>: G (disable removes contributions)</li><li><strong>Milestone</strong>: M4</li><li><strong>Status</strong>: Automated (protocol smoke: disable clears enabled flag; global search removal manual)</li></ul><h4 id="e2e-026-plugin-error-does-not-crash-app" tabindex="-1">E2E-026: Plugin error does not crash app <a class="header-anchor" href="#e2e-026-plugin-error-does-not-crash-app" aria-label="Permalink to &quot;E2E-026: Plugin error does not crash app&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Plugin loaded.</li><li><strong>Steps</strong>: 1) Trigger a scenario where plugin throws an error. 2) Observe app behavior.</li><li><strong>Expected</strong>: App remains running; error is captured and reported; no crash.</li><li><strong>Specs linked</strong>: <code>07-plugins/04-plugin-security.md</code></li><li><strong>Acceptance</strong>: G (plugin error → no crash)</li><li><strong>Milestone</strong>: M4</li><li><strong>Status</strong>: Draft</li></ul><h4 id="e2e-024x-page-copy-stays-concise-in-both-locales" tabindex="-1">E2E-024X: Page copy stays concise in both locales <a class="header-anchor" href="#e2e-024x-page-copy-stays-concise-in-both-locales" aria-label="Permalink to &quot;E2E-024X: Page copy stays concise in both locales&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: App running with English and Simplified Chinese available; project archive, Scheduled, Pull requests, Extensions, and Agent capability destinations are reachable.</li><li><strong>Steps</strong>: 1) Open each destination in English and inspect its header, toolbar, empty state, and primary action. 2) Switch to 简体中文 and repeat. 3) Trigger a permission, validation, destructive-action, or provider-error state.</li><li><strong>Expected</strong>: Page headers do not repeat their title as explanatory subtitles; empty states use a concise title and action, with body text only when context or a required next step is necessary. Settings and capability pages omit prose that only explains obvious controls. Permission, security, validation, destructive-action, keyboard, scope, and error details remain visible in both locales.</li><li><strong>Specs linked</strong>: <code>04-ux/01-ui-ia.md</code>, <code>04-ux/07-ui-design-system.md</code></li><li><strong>Acceptance</strong>: Quality (concise page copy)</li><li><strong>Status</strong>: Unit-covered (<code>packages/i18n/test/user-facing-copy.test.mjs</code>); visual scenario Draft</li></ul><h3 id="security-—-no-secret-leakage" tabindex="-1">Security — No Secret Leakage <a class="header-anchor" href="#security-—-no-secret-leakage" aria-label="Permalink to &quot;Security — No Secret Leakage&quot;">​</a></h3><h4 id="e2e-027-secrets-not-in-logs-for-normal-flows" tabindex="-1">E2E-027: Secrets not in logs for normal flows <a class="header-anchor" href="#e2e-027-secrets-not-in-logs-for-normal-flows" aria-label="Permalink to &quot;E2E-027: Secrets not in logs for normal flows&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Provider configured with API key.</li><li><strong>Steps</strong>: 1) Perform a chat session. 2) Inspect log files.</li><li><strong>Expected</strong>: API keys / tokens not present in any log output for normal flows.</li><li><strong>Specs linked</strong>: <code>05-security/01-security.md</code>, <code>03-runtime/09-logging-and-observability.md</code></li><li><strong>Acceptance</strong>: H (secrets not in logs)</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Automated (protocol smoke: provider list carries no secret material)</li></ul><h4 id="e2e-028-renderer-has-no-node-integration" tabindex="-1">E2E-028: Renderer has no Node integration <a class="header-anchor" href="#e2e-028-renderer-has-no-node-integration" aria-label="Permalink to &quot;E2E-028: Renderer has no Node integration&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: App running.</li><li><strong>Steps</strong>: 1) Inspect renderer process flags.</li><li><strong>Expected</strong>: <code>nodeIntegration: false</code>; <code>contextIsolation: true</code>; preload is the only bridge.</li><li><strong>Specs linked</strong>: <code>05-security/01-security.md</code></li><li><strong>Acceptance</strong>: Security (no Node in renderer)</li><li><strong>Milestone</strong>: M1</li><li><strong>Status</strong>: Draft</li></ul><h4 id="e2e-029-unwhitelisted-ipc-cannot-be-called" tabindex="-1">E2E-029: Unwhitelisted IPC cannot be called <a class="header-anchor" href="#e2e-029-unwhitelisted-ipc-cannot-be-called" aria-label="Permalink to &quot;E2E-029: Unwhitelisted IPC cannot be called&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: App running.</li><li><strong>Steps</strong>: 1) Attempt to invoke an IPC method not on the whitelist from renderer.</li><li><strong>Expected</strong>: Call blocked; no data returned; error or no response.</li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code>, <code>05-security/01-security.md</code></li><li><strong>Acceptance</strong>: Security (IPC whitelist enforced)</li><li><strong>Milestone</strong>: M1</li><li><strong>Status</strong>: Draft</li></ul><h4 id="e2e-030-plugin-cannot-read-api-key" tabindex="-1">E2E-030: Plugin cannot read API key <a class="header-anchor" href="#e2e-030-plugin-cannot-read-api-key" aria-label="Permalink to &quot;E2E-030: Plugin cannot read API key&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Plugin loaded; provider configured.</li><li><strong>Steps</strong>: 1) Plugin attempts to access provider secret via any API. 2) Observe result.</li><li><strong>Expected</strong>: Access denied; no secret data returned to plugin.</li><li><strong>Specs linked</strong>: <code>07-plugins/04-plugin-security.md</code>, <code>03-runtime/14-secrets-storage.md</code></li><li><strong>Acceptance</strong>: Security (plugin cannot read API key)</li><li><strong>Milestone</strong>: M4</li><li><strong>Status</strong>: Draft</li></ul><h4 id="e2e-031-error-codes-are-stable-and-readable" tabindex="-1">E2E-031: Error codes are stable and readable <a class="header-anchor" href="#e2e-031-error-codes-are-stable-and-readable" aria-label="Permalink to &quot;E2E-031: Error codes are stable and readable&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: App launched through the normal desktop development command; provider configured.</li><li><strong>Steps</strong>: 1) Select or enter a model ID that the provider rejects. 2) Send a prompt. 3) Inspect the assistant error message and its detail disclosure. 4) Switch sessions and reload the failed session. 5) Repeat with an invalid provider key.</li><li><strong>Expected</strong>: The run stops and the transcript contains one durable <code>role=assistant</code>, <code>status=error</code> message instead of a toast, floating banner, or blank row. It shows a localized summary and stable <code>MODEL_NOT_CONFIGURED</code> or <code>PROVIDER_UNAUTHORIZED</code> code. Details expose the redacted provider response plus provider/model IDs and can be copied; no API key or Authorization value appears. The configuration failure links to settings, retriable failures offer Retry, the composer becomes usable again, and reload preserves the error message. The development launch executes a sidecar rebuilt from current runtime source.</li><li><strong>Specs linked</strong>: <code>03-runtime/02-agent-runtime.md</code>, <code>03-runtime/07-process-model.md</code>, <code>03-runtime/08-error-codes.md</code></li><li><strong>Acceptance</strong>: C (failed chat settles), H (errors expose stable codes)</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Unit-covered (agent-runtime error message/redaction, host persistence, desktop transcript contract, and predev build contract); full Electron UI scenario Draft</li></ul><h3 id="hardening-m5" tabindex="-1">Hardening (M5) <a class="header-anchor" href="#hardening-m5" aria-label="Permalink to &quot;Hardening (M5)&quot;">​</a></h3><h4 id="e2e-032-backend-crash-triggers-supervised-restart" tabindex="-1">E2E-032: Backend crash triggers supervised restart <a class="header-anchor" href="#e2e-032-backend-crash-triggers-supervised-restart" aria-label="Permalink to &quot;E2E-032: Backend crash triggers supervised restart&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: App running; host-core and sidecar healthy.</li><li><strong>Steps</strong>: 1) Kill the host-core (or sidecar) process externally. 2) Observe app behavior.</li><li><strong>Expected</strong>: In-flight RPCs fail fast (no long hang); <code>hostStatus</code> shows degraded then restored; child restarts with backoff; after 3 failed restarts in 2 minutes the app stays degraded with a visible fatal status. Repeat the kill with the main window closed: the crash is still logged (with the child&#39;s last stderr lines), the child still restarts, and no unhandled renderer-send error appears — supervision is independent of a live window.</li><li><strong>Specs linked</strong>: <code>03-runtime/07-process-model.md</code></li><li><strong>Acceptance</strong>: Quality (main path no crash)</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Automated (<code>scripts/e2e-supervision.mjs</code> — SIGKILL host-core, assert restart + healthy RPC)</li></ul><h4 id="e2e-033-window-bounds-persist-across-restart" tabindex="-1">E2E-033: Window bounds persist across restart <a class="header-anchor" href="#e2e-033-window-bounds-persist-across-restart" aria-label="Permalink to &quot;E2E-033: Window bounds persist across restart&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: App running with default window size.</li><li><strong>Steps</strong>: 1) Resize/move the window to distinct normal bounds A (≥1040×700), maximize before the 600ms save debounce ends, quit, and relaunch. 2) Restore, resize/move to distinct bounds B, quit before the debounce ends, and relaunch again.</li><li><strong>Expected</strong>: Each relaunch restores the latest normal bounds (A, then B), including when quit occurs while maximized or with a pending save. Maximized/fullscreen geometry is never stored as normal bounds; invalid/tiny saved bounds fall back to the 1200×800 default.</li><li><strong>Specs linked</strong>: <code>04-ux/09-interaction-patterns.md</code></li><li><strong>Acceptance</strong>: Quality (key operations feel polished)</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Documented</li></ul><h4 id="e2e-034-ndjson-log-files-are-written-and-redacted" tabindex="-1">E2E-034: NDJSON log files are written and redacted <a class="header-anchor" href="#e2e-034-ndjson-log-files-are-written-and-redacted" aria-label="Permalink to &quot;E2E-034: NDJSON log files are written and redacted&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Fresh profile; provider configured; one chat turn completed.</li><li><strong>Steps</strong>: 1) Run a prompt with a tool call. 2) Open <code>~/.pi-desktop/logs/</code>. 3) Inspect the categorized files under <code>app/</code>, <code>host/</code>, and <code>agent/</code>.</li><li><strong>Expected</strong>: NDJSON records exist with <code>ts/level/channel/category/event/message</code>; a normal tool call produces one completion or failure record carrying <code>sessionId</code>/<code>toolCallId</code>, safe tool metadata, and bounded result/duration information; an interrupted tool remains traceable by the same id; no API key, authorization value, raw command output, or local absolute path appears; each category file rotates at 5 MB; lifecycle, permission, tool, provider, plugin, persistence, updater, and error records remain available without creating dedicated timing category files.</li><li><strong>Specs linked</strong>: <code>03-runtime/09-logging-and-observability.md</code></li><li><strong>Acceptance</strong>: H (diagnostics)</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Documented</li></ul><h4 id="e2e-194-broken-stdout-does-not-crash-the-main-process" tabindex="-1">E2E-194: Broken stdout does not crash the main process <a class="header-anchor" href="#e2e-194-broken-stdout-does-not-crash-the-main-process" aria-label="Permalink to &quot;E2E-194: Broken stdout does not crash the main process&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Packaged or development app; a session can send a prompt.</li><li><strong>Steps</strong>: 1) Launch so stdout/stderr are a closed pipe (Linux AppImage from a desktop entry, or with the stdout reader closed). 2) Send a chat message. 3) Confirm the main process stays up and the uncaught-exception dialog does not appear. 4) Quit and relaunch.</li><li><strong>Expected</strong>: No Electron &quot;A JavaScript error occurred in the main process&quot; dialog with <code>Error: write EPIPE</code> from <code>Logger.log</code>. NDJSON category logs still receive the prompt records. Relaunch still reaches the local host service instead of a fatal &quot;Can&#39;t reach the local service&quot; / “无法连接本地服务” status caused by a previous main-process crash.</li><li><strong>Specs linked</strong>: <code>03-runtime/09-logging-and-observability.md</code>, <code>03-runtime/07-process-model.md</code></li><li><strong>Acceptance</strong>: H (diagnostics), Quality (main path no crash)</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>logger-routing.test.mjs</code>); packaged AppImage journey Documented</li></ul><h4 id="e2e-runtime-non-ascii-http-header-does-not-show-main-exception-dialog" tabindex="-1">E2E-RUNTIME-non-ascii-http-header-does-not-show-main-exception-dialog <a class="header-anchor" href="#e2e-runtime-non-ascii-http-header-does-not-show-main-exception-dialog" aria-label="Permalink to &quot;E2E-RUNTIME-non-ascii-http-header-does-not-show-main-exception-dialog&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Packaged or development app on a machine whose system HTTP proxy or gateway injects a non-Latin-1 response header (for example a value starting with U+661F), or a test that delivers the same <code>TypeError: Cannot convert argument to a ByteString</code> through Electron <code>net</code>.</li><li><strong>Steps</strong>: 1) Launch so the auto-updater check or model discovery issues a main-process <code>net.fetch</code> / Electron-updater request. 2) Confirm the native exception dialog does not appear. 3) Dismiss nothing; wait for a later updater or discovery request. 4) Open <code>~/.pi-desktop/logs/app/runtime.log</code>.</li><li><strong>Expected</strong>: No Electron &quot;A JavaScript error occurred in the main process&quot; dialog. The app stays running and does not quit. <code>runtime.log</code> contains an error record with <code>code: &quot;NON_ASCII_HTTP_HEADER&quot;</code> and <code>recoverable: true</code>. A later main-process HTTP request does not re-open the native dialog.</li><li><strong>Specs linked</strong>: <code>03-runtime/07-process-model.md</code>, <code>03-runtime/09-logging-and-observability.md</code></li><li><strong>Acceptance</strong>: H (diagnostics), Quality (main path no crash)</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>main-process-errors.test.mjs</code>); packaged Windows proxy journey Documented</li></ul><h4 id="e2e-195-linux-glibc-below-2-35-names-supported-distros" tabindex="-1">E2E-195: Linux glibc below 2.35 names supported distros <a class="header-anchor" href="#e2e-195-linux-glibc-below-2-35-names-supported-distros" aria-label="Permalink to &quot;E2E-195: Linux glibc below 2.35 names supported distros&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Linux x64 packaged app; the machine glibc is older than 2.35 (for example Ubuntu 20.04 / Debian 11 / Fedora 35), or a test doubles <code>process.report</code> to <code>2.31</code>.</li><li><strong>Steps</strong>: 1) Launch the AppImage, deb, or rpm. 2) Observe the main window and fatal banner. 3) Confirm host-core is not restarted in a loop.</li><li><strong>Expected</strong>: Electron still opens. There is no uncaught <code>write EPIPE</code> dialog. The fatal banner says the build needs glibc 2.35 or newer and names Ubuntu 22.04, Debian 12, and Fedora 36+. Restart supervision does not spin. A host-core binary whose symbols need glibc 2.39 fails <code>scripts/check-linux-host-glibc.mjs</code>.</li><li><strong>Specs linked</strong>: <code>03-runtime/07-process-model.md</code>, <code>01-product/01-product-scope.md</code>, <code>06-delivery/06-release-runbook.md</code></li><li><strong>Acceptance</strong>: H (diagnostics), Quality (main path no crash)</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>linux-glibc.test.mjs</code>); packaged distro journey Documented</li></ul><h4 id="e2e-035-bash-tool-uses-the-effective-catalog-shell" tabindex="-1">E2E-035: Bash tool uses the effective catalog shell <a class="header-anchor" href="#e2e-035-bash-tool-uses-the-effective-catalog-shell" aria-label="Permalink to &quot;E2E-035: Bash tool uses the effective catalog shell&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Workspace open; agent mode.</li><li><strong>Steps</strong>: 1) Select an available catalog shell and run <code>Bash</code> (e.g. <code>echo ok</code>). 2) Make the persisted selection unavailable and inspect the effective catalog before running the next turn. 3) Run with the previous turn snapshot.</li><li><strong>Expected</strong>: The unchanged <code>Bash</code> protocol call uses the selected catalog entry. A later unavailable persisted choice falls back to the first available platform shell and marks the catalog fallback; the previous turn snapshot is rejected as stale by <code>COMMAND_SHELL_CHANGED</code> rather than silently changing shell. No partial execution occurs; E2E-113 covers the stale identity path.</li><li><strong>Specs linked</strong>: <code>03-runtime/03-tools-and-permissions.md</code>, <code>03-runtime/06-host-rpc-protocol.md</code>, <code>03-runtime/08-error-codes.md</code>, ADR 0054</li><li><strong>Acceptance</strong>: H (errors expose stable codes)</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>tools::shell::tests</code>); scenario Documented</li></ul><h4 id="e2e-044-development-launch-uses-pi-desktop-dock-branding" tabindex="-1">E2E-044: Development launch uses PI-Desktop Dock branding <a class="header-anchor" href="#e2e-044-development-launch-uses-pi-desktop-dock-branding" aria-label="Permalink to &quot;E2E-044: Development launch uses PI-Desktop Dock branding&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: macOS development checkout with canonical <code>build/icon_1024.png</code>.</li><li><strong>Steps</strong>: 1) Run <code>pnpm dev</code>. 2) Inspect the running application&#39;s Dock icon.</li><li><strong>Expected</strong>: The Dock shows the PI-Desktop brand icon, not Electron&#39;s default icon; packaged builds continue to use <code>build/icon.icns</code>.</li><li><strong>Specs linked</strong>: <code>06-delivery/06-release-runbook.md</code></li><li><strong>Acceptance</strong>: Quality (development shell matches release branding)</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>development-branding.test.mjs</code>); visual scenario Documented</li></ul><h4 id="e2e-045-global-text-selection-preserves-editing-and-copying" tabindex="-1">E2E-045: Global text selection preserves editing and copying <a class="header-anchor" href="#e2e-045-global-text-selection-preserves-editing-and-copying" aria-label="Permalink to &quot;E2E-045: Global text selection preserves editing and copying&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: App running with a chat transcript containing a user message, an assistant Markdown response with a code block, and an expanded tool result.</li><li><strong>Steps</strong>: 1) Drag across sidebar/titlebar chrome and a button label. 2) Drag across user/assistant prose, code, and tool output. 3) Focus the composer and a settings/search input, then use <code>Cmd/Ctrl+A</code> and replace the selected text. 4) Copy selected transcript and code text.</li><li><strong>Expected</strong>: Chrome does not leave an accidental text selection; message prose, code, tool input/output, and editable controls remain selectable and copyable; native editing shortcuts, focus-visible rings, and window drag behavior remain intact.</li><li><strong>Specs linked</strong>: <code>04-ux/07-ui-design-system.md</code>, <code>04-ux/09-interaction-patterns.md</code></li><li><strong>Acceptance</strong>: Quality (key operations feel polished)</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>user-select.test.mjs</code>); scenario Documented</li></ul><h4 id="e2e-046-pi-desktop-renderer-branding-and-composer-icon-boundary" tabindex="-1">E2E-046: PI-Desktop renderer branding and composer icon boundary <a class="header-anchor" href="#e2e-046-pi-desktop-renderer-branding-and-composer-icon-boundary" aria-label="Permalink to &quot;E2E-046: PI-Desktop renderer branding and composer icon boundary&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: App running in both English and zh-CN locales, with an empty home and a docked transcript available.</li><li><strong>Steps</strong>: 1) Inspect the expanded and collapsed sidebar. 2) Inspect the empty-home hero and docked composer. 3) Observe the eight-frame mascot GIF looping in place, move the pointer over it, and confirm its cadence and geometry do not change. Enable reduced motion and confirm the still first frame is shown. 4) Focus the footer Settings and Plugins icons, then each project/Temporary session create control. 5) Open Settings and the composer input.</li><li><strong>Expected</strong>: Visible shell identity reads <code>PI-Desktop</code>; the empty-home hero renders the theme-matching 100px <code>HomeMascotLogo</code> GIF with a short idle hold and a looping wave. Pointer hover does not alter the cadence or geometry, and reduced motion shows the matching still first frame. The expanded/collapsed sidebar renders the derived <code>src/assets/brand/logo-*.png</code> asset through <code>BrandLogo</code> and the docked composer prompt row has no leading brand icon or reserved icon slot and its text aligns directly with the input gutter. The right Composer toolbar shows a Bot model × reasoning chip, then a standalone prompt-enhancement Sparkles button, then the single submit slot. The footer Settings and Plugins actions are compact icon buttons; Plugins sits immediately to the right of Settings and exposes a localized accessible name. Every scoped session-creation control uses the dedicated message-plus icon with localized labels and accessible names. <code>Codex</code> remains visible only as the external import-source label or in non-runtime design-reference text.</li><li><strong>Specs linked</strong>: <code>04-ux/01-ui-ia.md</code>, <code>04-ux/07-ui-design-system.md</code>, <code>04-ux/08-component-spec.md</code>, <code>04-ux/09-interaction-patterns.md</code>, <code>08-meta/decisions-log.md</code> (D094/D160/D293), <code>../../adr/0031-icon-free-composer-prompt-row.md</code>, <code>../../adr/0152-eight-frame-empty-home-mascot-gif.md</code></li><li><strong>Acceptance</strong>: Quality (brand consistency and key operations feel polished)</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>renderer-branding.test.mjs</code>); scenario Documented</li></ul><h4 id="e2e-047-retain-collapse-switch-and-close-multiple-project-tabs" tabindex="-1">E2E-047: Retain, collapse, switch, and close multiple project tabs <a class="header-anchor" href="#e2e-047-retain-collapse-switch-and-close-multiple-project-tabs" aria-label="Permalink to &quot;E2E-047: Retain, collapse, switch, and close multiple project tabs&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Projects A and B each have at least one durable session; neither path is archived; a Temporary session also exists.</li><li><strong>Steps</strong>: 1) Open project A from Settings → Project archive. 2) Open project B without closing A. 3) Click A&#39;s directory row on its chevron, folder, label, and trailing disclosure hit area in turn to collapse/expand it; use B&#39;s directory row to activate and collapse B; verify <code>+</code> and overflow do not toggle B. 4) Hover and keyboard-focus A&#39;s project title and confirm the full path is exposed; open A&#39;s project overflow or right-click menu and choose Open folder; confirm conversation overflow no longer offers Open folder. 5) Select A&#39;s conversation. 6) Close B. 7) Restart the app. 8) Reopen B from Settings → Project archive.</li><li><strong>Expected</strong>: A and B render as separate exact-path sidebar groups in a compact continuous list with one keyboard stop per directory disclosure; every non-action point in A&#39;s row toggles only A, project activation is owned by the directory row rather than its overflow menu, and project actions appear on hover/focus without shifting labels, the project title hover/focus path shows A&#39;s full absolute path in a content-sized tooltip (long paths wrap within a 420px maximum), Open folder is a project-menu action only and opens A in the system file manager, and collapse survives restart; activating a group or its conversation clears the previous visible transcript, updates the selected workspace and session binding, and then loads only the selected project&#39;s conversation; Temporary remains separate; closing B removes only its retained tab and deletes neither its project row nor sessions; reopening B restores the same sessions without duplication.</li><li><strong>Specs linked</strong>: <code>04-ux/01-ui-ia.md</code>, <code>04-ux/08-component-spec.md</code>, <code>04-ux/09-interaction-patterns.md</code>, ADR 0016</li><li><strong>Acceptance</strong>: C (switch sessions), D (workspace), F (local presentation persistence)</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>sidebar-preferences.test.mjs</code> for retained paths and collapse persistence); full UI scenario Draft</li></ul><h4 id="e2e-047b-sidebar-session-hover-card-surfaces-rich-metadata" tabindex="-1">E2E-047b: Sidebar session hover card surfaces rich metadata <a class="header-anchor" href="#e2e-047b-sidebar-session-hover-card-surfaces-rich-metadata" aria-label="Permalink to &quot;E2E-047b: Sidebar session hover card surfaces rich metadata&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A retained project with at least two sessions, one of which has a recorded git branch; a Temporary/scratch session also exists.</li><li><strong>Steps</strong>: 1) Hover a session row under the retained project and wait for the card to appear; repeat with keyboard focus on the same row. 2) Change the active project&#39;s Git branch externally, then switch to another conversation in the same directory and hover its row; wait for the card. 3) Move the pointer to a different session row without leaving the sidebar; wait. 4) Hover a Temporary/scratch session row. 5) Resize the sidebar narrower than 320px; hover again. 6) Right-click a session row while the card is visible and open its context menu. 7) Scroll the sidebar body while the card is up.</li><li><strong>Expected</strong>: The card appears after a 500ms dwell, never appears during quick pointer passes, and re-targets to the latest hovered row when the pointer changes. Each card shows: the localized session title, the mode/permission badge, live status, the readable model display name when known, the project name (or &quot;Temporary&quot; / &quot;临时对话&quot; for scratch rows) with the latest Git branch on the same row, and the row&#39;s <code>Updated</code> timestamp formatted by the active locale without seconds. Ordinary sessions do not show a Local task chip, session UUID, separate Provider label, or a status-checked timestamp. Refreshing the branch does not activate a project or change the selected conversation. The session row has no native <code>title</code> tooltip; the hover card is the only full-title surface. The card never widens past 320px, never causes the underlying row to horizontally scroll, and disappears immediately on resize, scroll, or context-menu open.</li><li><strong>Specs linked</strong>: <code>04-ux/09-interaction-patterns.md §9.1b</code></li><li><strong>Acceptance</strong>: F (local presentation)</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>sidebar-navigation.test.mjs</code> and <code>app-store-sidebar.test.mjs</code> for the session hover card, branch refresh, and the absence of a native row <code>title</code>); full UI scenario Draft</li></ul><h4 id="e2e-048-pin-archive-restore-and-sort-project-conversation-rows" tabindex="-1">E2E-048: Pin, archive, restore, and sort project/conversation rows <a class="header-anchor" href="#e2e-048-pin-archive-restore-and-sort-project-conversation-rows" aria-label="Permalink to &quot;E2E-048: Pin, archive, restore, and sort project/conversation rows&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Two retained projects contain conversations with distinct titles and created/updated timestamps; archived view is initially disabled.</li><li><strong>Steps</strong>: 1) Inspect the Sessions and Projects heading actions at rest. 2) Hover each heading and keyboard-focus each action to confirm the controls reveal without moving the labels. 3) Inspect the <code>Sessions</code> toolbar and verify that Sort appears before New Chat. 4) Open Sort and inspect its placement, then select Recently updated, Created date, Oldest first, and Name in turn. 5) Pin one project and one conversation. 6) Archive another conversation and project. 7) Enable Show archived and restore both. 8) Restart the app. 9) Delete a disposable conversation through the distinct Delete action.</li><li><strong>Expected</strong>: Section create and sort controls are visually quiet at rest and reveal on toolbar hover or keyboard focus; project <code>+</code> and overflow actions follow the same rule without shifting labels. Sort precedes New Chat in the Sessions toolbar; the sort menu remains content-sized, opens to the trigger&#39;s right without flipping left, and session/project/section body-level menus use the same right-side rule with a narrow-viewport width cap. Pinned rows remain ahead of unpinned rows under every selected secondary order; in the sidebar, pinned project rows replace the Folder with a filled accent Star while unpinned rows retain Folder. Each sort produces the documented stable order; archived rows disappear from the default view but retain transcripts/project records and reappear in Show archived; restore returns them to the selected order; archiving the active row selects a visible non-archived fallback or creates the documented empty fallback instead of leaving hidden active context; pin/archive/sort choices survive restart; only Delete removes the disposable durable session. A legacy <code>manual</code> preference loads safely without exposing or implying a drag-reorder workflow.</li><li><strong>Specs linked</strong>: <code>03-runtime/04-data-storage.md</code>, <code>04-ux/01-ui-ia.md</code>, <code>04-ux/08-component-spec.md</code>, <code>04-ux/09-interaction-patterns.md</code></li><li><strong>Acceptance</strong>: C (session organization), F (persistence)</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>sidebar-preferences.test.mjs</code> for metadata, filtering, and sort behavior); full UI scenario Draft</li></ul><h4 id="e2e-048b-edit-a-logical-project-name-and-folder-roots" tabindex="-1">E2E-048b: Edit a logical project name and folder roots <a class="header-anchor" href="#e2e-048b-edit-a-logical-project-name-and-folder-roots" aria-label="Permalink to &quot;E2E-048b: Edit a logical project name and folder roots&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: One retained logical project is visible in the sidebar and in Settings → Project archive; it has a primary folder and one additional folder.</li><li><strong>Steps</strong>: 1) Open the project&#39;s overflow menu in the sidebar and choose Edit project. 2) Change the name, remove the additional folder, and add it again with the native folder picker. 3) Confirm the Primary row cannot be removed. 4) Save and inspect the sidebar row, Project archive roots, and active workspace. 5) Restart the app and inspect the group again.</li><li><strong>Background-update regression</strong>: Open Edit project from Settings, change the name and remove the additional folder, then complete a background task before saving. Both draft edits must remain visible and save together. The isolated rendered test is <code>node scripts/e2e-project-edit.mjs</code>; it uses real page/store wiring with a controlled host IPC boundary and does not prove disk persistence.</li><li><strong>Expected</strong>: Both project menus offer Edit project. The editor keeps focus contained, trims the name, limits it to 80 Unicode characters, preserves the Primary folder as the first row, and updates the root count without removing another row. Saving persists one logical group with the adjusted roots; the name survives restart, while normalized paths, workspace identity, sessions, and on-disk folders remain unchanged. A root with existing chats is rejected instead of orphaning those chats.</li><li><strong>Specs linked</strong>: <code>04-ux/01-ui-ia.md</code>, <code>04-ux/08-component-spec.md</code>, <code>04-ux/09-interaction-patterns.md</code></li><li><strong>Acceptance</strong>: D (workspace identity), F (local presentation persistence)</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>project-edit.test.mjs</code>, <code>sidebar-preferences.test.mjs</code>); rendered scenario Draft</li></ul><h4 id="e2e-048a-project-session-lists-fold-after-the-ten-most-recent-rows" tabindex="-1">E2E-048A: Project session lists fold after the ten most recent rows <a class="header-anchor" href="#e2e-048a-project-session-lists-fold-after-the-ten-most-recent-rows" aria-label="Permalink to &quot;E2E-048A: Project session lists fold after the ten most recent rows&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: One retained project contains more than ten durable sessions with distinct updated timestamps; the sidebar uses the default Recently updated sort; another retained project has ten or fewer sessions.</li><li><strong>Steps</strong>: 1) Inspect the large project&#39;s session rows and count them. 2) Select the <strong>Load N more…</strong> control. 3) Switch the sort to Name and inspect the same group before expanding. 4) Restart the app and inspect the group again.</li><li><strong>Expected</strong>: The group shows exactly ten session rows by default plus a <strong>Load N more…</strong> control (N = remaining session count) styled like the time-grouped overflow; the ten rows are the first rows in the active sort order, so pinned rows are never pushed behind unpinned rows and a Name sort folds everything after the first ten alphabetically; selecting Load more expands the full time-grouped list (Yesterday/Previous 7 days/Previous 14 days/Older headers appear as applicable) and the control disappears; expansion is per group and resets on restart (not persisted); the project with ten or fewer sessions shows no fold control.</li><li><strong>Specs linked</strong>: <code>04-ux/01-ui-ia.md</code>, <code>04-ux/09-interaction-patterns.md</code></li><li><strong>Acceptance</strong>: C (session organization)</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Scenario Documented</li></ul><h4 id="e2e-049-background-sessions-keep-their-originating-workspace" tabindex="-1">E2E-049: Background sessions keep their originating workspace <a class="header-anchor" href="#e2e-049-background-sessions-keep-their-originating-workspace" aria-label="Permalink to &quot;E2E-049: Background sessions keep their originating workspace&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Projects A and B are retained; each contains a session in Agent mode; both workspaces contain different marker files with the same relative name.</li><li><strong>Steps</strong>: 1) In session A, start a turn that reads the marker and performs a permission-gated long-running tool. 2) While A is running, activate project B and open session B. 3) Read B&#39;s marker and allow a tool only in B. 4) Wait for both turns to complete. 5) Open a Temporary session and attempt a workspace-required tool.</li><li><strong>Expected</strong>: Switching tabs aborts neither turn; A&#39;s tool cwd/path sandbox remains project A and B&#39;s remains project B; A&#39;s events and grants never appear in B&#39;s transcript/session; each sidebar row reports its own running/completed state; the Temporary session inherits no project and receives <code>WORKSPACE_REQUIRED</code>; returning to A restores A&#39;s completed transcript.</li><li><strong>Specs linked</strong>: <code>02-architecture/01-architecture.md</code>, <code>03-runtime/02-agent-runtime.md</code>, <code>03-runtime/03-tools-and-permissions.md</code>, <code>03-runtime/06-host-rpc-protocol.md</code>, <code>03-runtime/10-session-state-machine.md</code>, ADR 0016</li><li><strong>Acceptance</strong>: C (parallel sessions), D (workspace), E (tool/permission isolation), Security (workspace boundary)</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>rpc::tests</code> for project-bound, Temporary, and missing-session workspace resolution); full multi-turn UI scenario Draft</li></ul><h4 id="e2e-050-composer-model-×-reasoning-menu-follows-exact-capability" tabindex="-1">E2E-050: Composer model × reasoning menu follows exact capability <a class="header-anchor" href="#e2e-050-composer-model-×-reasoning-menu-follows-exact-capability" aria-label="Permalink to &quot;E2E-050: Composer model × reasoning menu follows exact capability&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: One catalogued reasoning model, one non-reasoning model, and one unknown free-form model id.</li><li><strong>Steps</strong>: 1) Open the Composer model × reasoning chip. 2) Confirm the root contains the Model and Reasoning level entries with current values, plus a slider with one track dot per supported level on a rail directly beneath the Reasoning level entry; confirm every level keeps a visible label under the rail, each centered on its track dot. 3) Drag and click the slider across multiple supported levels and click a tick label, confirming the chip updates while the menu stays at the root and the selected track dot sits under the thumb. 4) Open Model, search for a model, and select a model from a provider group; confirm the menu remains open at the root. 5) Open Reasoning level and choose a level from the radio list. 6) Repeat with a non-reasoning provider and an unknown free-form model id; exercise Escape, outside click, Up/Down, Enter, Left, and the slider&#39;s arrow keys. In both roomy and crowded toolbars, switch <code>omit</code>, <code>low</code>, <code>high</code>, <code>max</code>, and <code>off</code> without closing the menu: its horizontal position stays stable, and the trigger stays inside its slot, including with a long model name. Moving the anchor and dispatching a viewport resize must reposition the menu without an event-target type error. Automated geometry coverage: <code>node scripts/e2e-composer-thinking-layout.mjs</code> (isolated Electron fixture, production React picker and compiled styles; no live providers). Repeat native mouse press/release to open, select the first and subsequent reasoning levels, close, and reopen three times with motion enabled. The menu must retain its opening position on every selection; synthetic DOM <code>click()</code> alone does not exercise the trigger&#39;s <code>:active</code> transition. Hover each dot and its label: the corresponding label highlights. Unfilled dots brighten and enlarge slightly; filled dots and the selected thumb stay unchanged without shifting the menu. Move outside: the preview clears and the selected value remains unchanged. Reduced motion disables hover transitions. Click a non-adjacent level and verify the thumb travels through intermediate positions while the selected value already equals the target. Interrupt travel with another click; the thumb retargets without jumping to the old start, and only the clicked levels are submitted. Check save failure, stale completion, remount, and 3/5/7/8-stop alignment. At each stop, verify the fill covers the first dot&#39;s left edge and still ends at the thumb center. Native range dragging follows immediately; arrow keys retain focus and update the selection. With reduced motion enabled, the target is shown without a transition.</li><li><strong>Expected</strong>: The chip is in the right toolbar with a Bot icon, before the standalone prompt-enhancement Sparkles action and Send/Abort; Off omits the level text. The single anchored menu replaces its root with an in-place back row and submenu, never opens tabs or a second popover, and always reopens at the root. Model search filters sticky provider groups; reasoning levels come from <code>omit</code> then the selected model binding&#39;s enabled levels in canonical order. The root carries a drag slider with one labeled stop per level directly beneath the Reasoning level entry when more than one level is listed (a single-level binding hides the slider). Dragging across several stops persists only the last pending level; tick labels are not tab stops. Slider and tick commits update the chip immediately while the menu stays at the root. The Reasoning level entry opens the radio list, which uses radio semantics, a trailing check, and the current model support note. Selecting either value immediately updates the chip and root value, clears model filtering, and keeps the menu open. A non-reasoning or unknown model starts at <code>off</code>, but an explicit Settings binding can make its configured levels available; discovery never promotes it automatically. Refreshing discovered model data cannot overwrite the binding. Before the first message creates a session, the Composer uses the exact model selected in its model menu rather than the provider&#39;s default model; after materialization, the same exact-model capability remains in effect.</li><li><strong>Specs linked</strong>: <code>03-runtime/11-provider-model-system.md</code>, <code>03-runtime/12-provider-config-schema.md</code>, <code>03-runtime/13-model-catalog-and-selection.md</code>, ADR 0018, ADR 0027</li><li><strong>Acceptance</strong>: B (model config), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>thinking-ui.test.mjs</code>, <code>composer-model-thinking-menu.test.mjs</code>, <code>thinking-commit-queue.test.mjs</code>, agent-runtime capability tests); full UI scenario Draft</li></ul><h4 id="e2e-051-thinking-level-persists-with-the-session" tabindex="-1">E2E-051: Thinking level persists with the session <a class="header-anchor" href="#e2e-051-thinking-level-persists-with-the-session" aria-label="Permalink to &quot;E2E-051: Thinking level persists with the session&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A reasoning-capable session is idle.</li><li><strong>Steps</strong>: 1) Select <code>high</code>. 2) Change Plan/Goal/Agent mode without changing the thinking level. 3) Restart the app and reopen the session. 4) Switch to another session and back.</li><li><strong>Expected</strong>: Every configuration update sends the complete session config; <code>high</code> survives the permission-mode change, session switches, host reload, and app restart. A v2 database migrates the same field to <code>off</code> without transcript loss.</li><li><strong>Specs linked</strong>: <code>03-runtime/04-data-storage.md</code>, <code>03-runtime/06-host-rpc-protocol.md</code>, <code>04-ux/08-component-spec.md</code>, ADR 0018</li><li><strong>Acceptance</strong>: F (persistence)</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (host schema/session tests, <code>thinking-ui.test.mjs</code>); full restart scenario Draft</li></ul><h4 id="e2e-052-thinking-level-reaches-the-pi-request" tabindex="-1">E2E-052: Thinking level reaches the pi request <a class="header-anchor" href="#e2e-052-thinking-level-reaches-the-pi-request" aria-label="Permalink to &quot;E2E-052: Thinking level reaches the pi request&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Instrumented reasoning-capable provider with a sparse level set and request capture; one session configured above and below gaps.</li><li><strong>Steps</strong>: 1) Select each enabled level and run a prompt. 2) Seed an explicit binding level the catalog does not publish and run again. 3) Repeat with a pi-catalogued non-reasoning model, first with no enabled level and then after an explicit Settings opt-in.</li><li><strong>Expected</strong>: Main resolves capability using the session&#39;s actual model id; Composer, main, sidecar, and pi use the same upward-first/downward-second clamp over the binding&#39;s enabled set, without intersecting it with the catalog. Pi receives the effective level, an empty or <code>off</code>-only binding receives <code>off</code>, and an explicitly opted-in endpoint receives the configured level. Model-specific request semantics, including adaptive thinking and whether <code>off</code> is expressible, match the pinned pi record without a desktop rewrite.</li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code>, <code>03-runtime/02-agent-runtime.md</code>, <code>03-runtime/13-model-catalog-and-selection.md</code>, ADR 0018, ADR 0027</li><li><strong>Acceptance</strong>: B (model config), C (chat and stream)</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (agent-runtime prompt/clamp tests); integration scenario Draft</li></ul><h4 id="e2e-053-thinking-streams-separately-from-the-answer" tabindex="-1">E2E-053: Thinking streams separately from the answer <a class="header-anchor" href="#e2e-053-thinking-streams-separately-from-the-answer" aria-label="Permalink to &quot;E2E-053: Thinking streams separately from the answer&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Provider emits thinking deltas before and between answer deltas.</li><li><strong>Steps</strong>: 1) Start a turn in both light and dark themes. 2) Observe a thinking-only phase. 3) Let the answer complete. 4) Toggle the disclosure, test keyboard focus, enable reduced motion, and use Copy answer.</li><li><strong>Expected</strong>: The transcript opens during thinking-only streaming; the latest Thinking disclosure opens and updates without an empty answer bubble or duplicate Working indicator. If the user collapses or expands it, that choice remains authoritative through later thinking deltas and completion. The disclosure uses the transcript surface, theme tokens, a Sparkles/chevron trigger, and a left rule instead of an inset card; collapsed content leaves focus traversal and reduced motion disables the running marker pulse and transitions. Final answer markdown renders separately; Copy answer contains no thinking text.</li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code>, <code>04-ux/07-ui-design-system.md</code>, <code>04-ux/08-component-spec.md</code>, ADR 0018</li><li><strong>Acceptance</strong>: C (chat and stream), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>thinking-ui.test.mjs</code>, agent-runtime event tests); full streaming scenario Draft</li></ul><h4 id="e2e-054-stored-thinking-reloads-losslessly" tabindex="-1">E2E-054: Stored thinking reloads losslessly <a class="header-anchor" href="#e2e-054-stored-thinking-reloads-losslessly" aria-label="Permalink to &quot;E2E-054: Stored thinking reloads losslessly&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A completed assistant message contains both reasoning and final answer blocks; another contains reasoning only.</li><li><strong>Steps</strong>: 1) Complete both turns. 2) Restart the host/app. 3) Reopen the session. 4) inspect search results and answer copy.</li><li><strong>Expected</strong>: Host returns the same separate <code>thinking</code> and <code>content</code> values after reload/import/replace round-trips; both messages remain visible; search and answer copy exclude reasoning.</li><li><strong>Specs linked</strong>: <code>03-runtime/04-data-storage.md</code>, <code>03-runtime/06-host-rpc-protocol.md</code>, <code>04-ux/08-component-spec.md</code>, ADR 0018</li><li><strong>Acceptance</strong>: C (chat and stream), F (persistence)</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (host message/import tests, <code>thinking-ui.test.mjs</code>); full reload scenario Draft</li></ul><h4 id="e2e-055-unsupported-provider-transition-clamps-safely" tabindex="-1">E2E-055: Unsupported provider transition clamps safely <a class="header-anchor" href="#e2e-055-unsupported-provider-transition-clamps-safely" aria-label="Permalink to &quot;E2E-055: Unsupported provider transition clamps safely&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Session on a reasoning provider at <code>max</code>; target providers include non-reasoning and sparse-level variants.</li><li><strong>Steps</strong>: 1) Switch to the non-reasoning provider. 2) Run a turn. 3) Switch to sparse variants around the previous level. 4) send malformed/legacy payloads lacking capability or thinking fields.</li><li><strong>Expected</strong>: Non-reasoning persists and sends <code>off</code>; sparse variants choose the same nearest level everywhere; missing fields fall back safely; malformed thinking is not rendered and never contaminates answer content.</li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code>, <code>03-runtime/13-model-catalog-and-selection.md</code>, <code>04-ux/08-component-spec.md</code>, ADR 0018</li><li><strong>Acceptance</strong>: B (model config), C (chat and stream), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>thinking-ui.test.mjs</code>, host validation tests); full UI scenario Draft</li></ul><h4 id="e2e-056-work-panel-shell-docking-and-persistence" tabindex="-1">E2E-056: Work panel shell docking and persistence <a class="header-anchor" href="#e2e-056-work-panel-shell-docking-and-persistence" aria-label="Permalink to &quot;E2E-056: Work panel shell docking and persistence&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: App running with any workspace state.</li><li><strong>Steps</strong>: 1) Relaunch and inspect the titlebar and application menu; confirm the panel starts closed and a viewport-fixed work-panel toggle is present (disabled with no session). Press Cmd/Ctrl+J or click the toggle and inspect the no-resource New launcher, then press/click again to confirm it collapses the panel and no tab is created or deleted; a third press must restore the same context. 2) Open two distinct file artifacts, the same first file again, a URL preview, and a completed Bash row. 3) Verify the header is a tablist: open enough tabs to overflow it, confirm only the strip scrolls and the <code>+</code> trigger stays visible, activate the scrolled-away tab, and close tabs with hover/focus <code>×</code> and middle-click. 4) Click <code>+</code> twice and verify each click creates and activates a separate New launcher tab. Confirm the launcher body contains Review plus each in-scope plugin view exactly once as clickable rows; there is no work-panel dropdown or popup. Click Browser from one New tab and confirm that tab opens the native plugin surface without changing its bounds. 5) Close active middle and edge tabs and verify neighbor selection. Close the final tab and confirm the panel remains open on the New launcher. 6) Use the viewport-fixed work-panel toggle and trigger another artifact. 7) In session A, leave the panel open with multiple tabs and a Browser resource; switch to session B, create a different tab set, then switch repeatedly between A and B and select a project without an active conversation. Generate a background artifact in the non-visible session. 7) Drag the inner left-edge handle left and right across its bounds; verify pointer-down does not jump the divider or resize the native window, cancel one gesture with Escape, then focus the handle and exercise Arrow/Shift+Arrow/Home/End. Commit a different panel width with Browser active, double-click the divider and confirm the default 360px width returns (or the live budget when it is smaller), then commit a different width again. 8) Record MainChat width and native bounds while opening, repeating the same open action, resizing the panel, collapsing, reopening, and closing the final resource. Repeat collapse on Windows while watching the entire frameless window. 9) With the panel open, resize the application from each native edge and confirm only the application bounds change; the panel remains at its renderer-committed width. Resize from the left edge and repeat after toggling the sidebar. 10) Open, resize, and collapse on a small work area, then repeat while maximized and fullscreen. 11) Move the normal window between displays and change the active display&#39;s work-area geometry. 12) Send valid and malformed reservation payloads, including positive values, and confirm the compatibility seam never changes native bounds. 13) Relaunch.</li><li><strong>Expected</strong>: Startup shows no panel, welcome chooser, or fixed tool buttons. A viewport-fixed toggle in the window&#39;s top-right corner is the pointer equivalent of Cmd/Ctrl+J; there is still no application-menu launcher. Cmd/Ctrl+J opens the active session&#39;s panel at its committed width without creating a resource tab and collapses it again on the next press while retaining that context, and the shortcut does nothing without an active session or while Settings is open. Each artifact atomically opens the docked third column and creates or activates one resource; file resources are path-keyed and repeated resources deduplicate. Opening, collapse, and closing animate the panel&#39;s width/flex allocation with its bounded opacity/slide, so MainChat reflows continuously without a pre-animation jump. Opening the panel, collapsing it, or committing a divider resize updates the presentation without a native-window jump. The header is a horizontally scrollable tablist with stable <code>92px–180px</code> tabs, visible spacing, and a fixed <code>+</code>; labels stay readable instead of shrinking into one cluster, the strip alone scrolls, active tabs scroll into view, and close selects the right neighbor then left. New launcher tabs expose Review and in-scope plugin views as body buttons, with no popup to overlap or shift the panel. Clicking a launcher row replaces that New tab with the destination or activates its existing singleton. Closing the last tab leaves the panel open on New. Collapse retains runtime tabs but hides the panel until another artifact reopens it. Width follows the shared three-column budget with no fixed pixel cap and previews its current/minimum/maximum values through the panel separator. The inner divider exposes the panel width to assistive technology and supports the documented keyboard steps. Pointer-down preserves the starting width, movement follows the pointer continuously, and release commits once only when the target changed. Escape or cancellation restores the press-time width, and a double-click restores the 360px default inside the live bounds. Browser preview does not intercept an active divider drag. A and B independently restore their runtime open state, ordered tabs, active tab, and Browser resource; selecting a project without an active conversation hides the panel, and no relative resource crosses session/workspace context. Background artifacts update only their retained context and never change the visible panel or native window geometry. Before exit motion, the native Browser preview detaches from the window; collapse produces no stale preview frame. Only <code>{width}</code> is restored after relaunch; every session&#39;s open state, tabs, active tab, and Browser resource reset. The panel remains exactly at its committed width while open, and sidebar or native window changes do not alter that preferred panel width. The compatibility reservation seam returns <code>{requested: 0, reserved: 0}</code> for every valid request, including positive legacy values, and never changes native bounds. Malformed payloads fail with <code>INVALID_ARGUMENT</code> and never coerce. The former context-panel overlay no longer exists.</li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code>, <code>04-ux/01-ui-ia.md</code>, <code>04-ux/07-ui-design-system.md</code>, <code>04-ux/08-component-spec.md</code>, <code>04-ux/09-interaction-patterns.md</code>, ADR 0068, ADR 0151, ADR 0195, D207, D292, D357</li><li><strong>Acceptance</strong>: F (persistence), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>work-panel-resize.test.mjs</code>, <code>work-panel-window.test.mjs</code>, <code>work-panel-presentation.test.mjs</code>, <code>work-panel.test.mjs</code>); full UI scenario Draft</li></ul><h4 id="e2e-057-message-owned-review-history-survives-commits-and-rolls-back-safely" tabindex="-1">E2E-057: Message-owned review history survives commits and rolls back safely <a class="header-anchor" href="#e2e-057-message-owned-review-history-survives-commits-and-rolls-back-safely" aria-label="Permalink to &quot;E2E-057: Message-owned review history survives commits and rolls back safely&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A project-bound Agent session with a writable workspace; no Git repository is required.</li><li><strong>Steps</strong>: 1) Ask the active agent to edit an existing file and create a new file in session A. 2) Expand the activity group and inspect each review card directly after its corresponding tool row; verify every card starts collapsed with its added/modified status and +/− counts visible in the header, then expand it to verify the exact hunks. 3) Commit the files outside the app, close and reopen the Review panel, then reload session A. 4) Verify the same cards and counts remain because they come from the transcript messages. 5) Use a card&#39;s rollback action and verify the created file is removed or the previous file bytes are restored. 6) Edit that file again outside the app and retry rollback; inspect the conflict result and verify the later bytes remain. 7) Switch to session B and a background project session, then return to A. 8) Repeat with failed, denied, and scratch writes.</li><li><strong>Expected</strong>: Each successful workspace Write/Edit creates one message-owned review record and one adjacent keyboard-accessible card; the card is never a bottom/global entry. The completion opens nothing: the panel keeps whatever the user left it showing, and Review appears only after the user opens it. Every review card, inline and in the Review tab, is collapsed by default and expands on demand. The Review tab lists A&#39;s chronological recorded changes, independent of Git status, repository presence, commit state, focus refresh, or workspace switching. Added, modified, and deleted statuses plus line additions/deletions and hunks are shown when bounded evidence is available. Successful rollback updates the card to Rolled back and survives restart. A post-tool file change returns Conflict and does not overwrite it. Failed, denied, and scratch writes do not create cards; session B cannot inherit A&#39;s records. Binary or oversized snapshots show bounded metadata and disable rollback when the previous bytes were not retained.</li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code> §13a, <code>03-runtime/03-tools-and-permissions.md</code> §4c, <code>04-ux/08-component-spec.md</code> §5, ADR 0043</li><li><strong>Acceptance</strong>: D (workspace), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>chat-review-entry.test.mjs</code>); full UI scenario Draft</li></ul><h4 id="e2e-058-built-in-interactive-terminal-is-absent" tabindex="-1">E2E-058: Built-in interactive terminal is absent <a class="header-anchor" href="#e2e-058-built-in-interactive-terminal-is-absent" aria-label="Permalink to &quot;E2E-058: Built-in interactive terminal is absent&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A workspace is open and the Agent has completed a Bash tool call.</li><li><strong>Steps</strong>: 1) Open the work panel with Cmd/Ctrl+J and inspect the empty state and context menu. 2) Confirm there is no Terminal tab, launcher row, terminal-specific panel copy, or terminal IPC surface. 3) Confirm the completed Bash row still shows its command, output, status, and copy action, and that its <code>IconTerminal</code> presentation remains available. 4) Verify an interactive shell is opened in the user&#39;s external terminal instead of the work panel. 5) Build/package the desktop app and inspect the dependency and unpacked-resource lists.</li><li><strong>Expected</strong>: The work panel offers the Review launcher row plus Browser and in-scope plugin views; Review opens on explicit user action and file resources are transcript-opened; no PTY is created and no terminal tab can be opened. Agent Bash remains non-interactive and fully visible in the transcript. Interactive shell work is performed by the external terminal. Desktop packaging has no PTY/xterm dependency, terminal-specific IPC, or native terminal payload, while generic lifecycle <code>terminal</code> values continue to work.</li><li><strong>Specs linked</strong>: <code>02-architecture/02-tech-stack.md</code>, <code>03-runtime/01-ipc-protocol.md</code> §13a, <code>04-ux/08-component-spec.md</code> §5, ADR 0108</li><li><strong>Acceptance</strong>: D (workspace), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>work-panel.test.mjs</code>, <code>packaging-footprint.test.mjs</code>); full UI scenario Draft</li></ul><h4 id="e2e-059-embedded-browser-preview-isolation-and-overlays" tabindex="-1">E2E-059: Embedded browser preview isolation and overlays <a class="header-anchor" href="#e2e-059-embedded-browser-preview-isolation-and-overlays" aria-label="Permalink to &quot;E2E-059: Embedded browser preview isolation and overlays&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A local dev server is running; a URL or BrowserPreview artifact exists.</li><li><strong>Steps</strong>: 1) Activate the artifact, enter <code>localhost:&lt;port&gt;</code> without a scheme, and submit. 2) Navigate site links; use back/forward/reload/stop. 3) Trigger a <code>window.open</code> popup and a permission-requesting page (e.g. notification prompt). 4) Open global search, then Settings. Return to chat and trigger an inline tool permission card. 5) Switch to another panel tab and back; close the panel. 6) Use open-external.</li><li><strong>Expected</strong>: Scheme-less input normalizes to http; nav state (URL bar, back/forward enablement, load spinner) mirrors the page. Popups open in the default browser (never in-app) only when the URL parses as http(s) or mailto; <code>file:</code>, <code>javascript:</code>, and custom schemes are denied. Permission requests are denied; non-http(s) navigation is blocked except in-root <code>file:</code> siblings. The preview hides under every blocking overlay and while unmounted, reappearing with correct bounds afterwards. An inline permission card does not hide or remount the preview; resize/drag keeps the native view visible and aligned with the placeholder rect without a black flash. Opening the work-panel context dropdown moves beside the native view; the view keeps its full surface rect and the plugin body does not shift down. Open-external launches an http(s) page in the default browser and an in-root file preview via <code>openPath</code>. The view uses an isolated persist partition (no session bleed from the app shell).</li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code> §13a, ADR 0019, ADR 0168</li><li><strong>Acceptance</strong>: Quality, Security</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Draft (manual)</li></ul><h4 id="e2e-browser-session-preview-race-session-switching-does-not-expose-a-stale-preview" tabindex="-1">E2E-BROWSER-session-preview-race: Session switching does not expose a stale preview <a class="header-anchor" href="#e2e-browser-session-preview-race-session-switching-does-not-expose-a-stale-preview" aria-label="Permalink to &quot;E2E-BROWSER-session-preview-race: Session switching does not expose a stale preview&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Two sessions have distinct HTML previews; their Browser panel contexts are retained independently. Directory lookups and document loads can be delayed independently by the fixture.</li><li><strong>Steps</strong>: Preview A; produce B&#39;s preview in the background; collapse A&#39;s panel and open B; delay B&#39;s directory lookup and load, then return to A. Switch rapidly B → C and finish the older B lookup last. Repeat with an already-started old load, an empty session, a failed load, and disposal.</li><li><strong>Expected</strong>: Background B does not navigate A. On switching, no A document appears in B&#39;s panel, even after bounds/visibility updates. Only the latest navigation can reveal the guest; the late B lookup cannot replace C. Returning to A restores A, while an empty session remains empty. Closing or disposing cannot be undone by a delayed completion. Same-session navigation keeps its current page visible. Invalid, failed, or timed-out loads do not report the previous document as the destination being ready. A switch that exceeds the existing 15-second load wait stays hidden until retried; automatic late reveal is not promised.</li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code> §5.3; ADR 0028, ADR 0170.</li><li><strong>Status</strong>: Automated service-path coverage in <code>browser-host-session.test.mjs</code> and <code>browser-pane-navigation.test.mjs</code>: production BrowserHost/BrowserPane, controlled native-browser/Host boundaries, and deterministic timers. Native Electron compositing and the reporter&#39;s live sessions are not covered.</li></ul><h4 id="e2e-browser-in-page-navigation-browser-chrome-follows-same-document-navigation" tabindex="-1">E2E-BROWSER-in-page-navigation: Browser chrome follows same-document navigation <a class="header-anchor" href="#e2e-browser-in-page-navigation-browser-chrome-follows-same-document-navigation" aria-label="Permalink to &quot;E2E-BROWSER-in-page-navigation: Browser chrome follows same-document navigation&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Browser plugin enabled; a local page has a fragment link and a History API route control.</li><li><strong>Steps</strong>: Open the page; click the fragment link; change the route with <code>history.pushState</code>; use Back, Forward, and Reload. Switch to another session and verify late events from the old page cannot update the new preview.</li><li><strong>Expected</strong>: The address matches the main document URL and history buttons reflect its history. Loading settles back to Reload. Subframe, replaced-frame, and invalidated-session events cannot change the toolbar.</li><li><strong>Coverage</strong>: <code>browser-pane-navigation.test.mjs</code> for published state and event isolation; native Electron click-through for actual same-document events.</li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code> §5.3.</li></ul><h4 id="e2e-060-files-tab-browsing-stays-inside-the-workspace" tabindex="-1">E2E-060: Files tab browsing stays inside the workspace <a class="header-anchor" href="#e2e-060-files-tab-browsing-stays-inside-the-workspace" aria-label="Permalink to &quot;E2E-060: Files tab browsing stays inside the workspace&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Workspace with file artifacts for nested source, large (&gt;512KB), image, and binary files.</li><li><strong>Steps</strong>: 1) Activate each file artifact and verify a distinct path-keyed resource in the header switcher; browse the tree, expanding nested folders. 2) Open a source file, the image, the binary, and the large file. 3) Use reveal-in-Finder. 4) Attempt a traversal read (<code>../outside</code>) via devtools IPC. 5) Switch workspaces.</li><li><strong>Expected</strong>: Directories list lazily, folders first, with <code>.git</code> / <code>node_modules</code> / build outputs hidden; text renders with syntax highlight (capped at 5000 lines), images preview inline, binary and oversized files show fallbacks with reveal still available. Traversal attempts are rejected with <code>INVALID_ARGUMENT</code>; no workspace → empty state; switching workspaces resets the tree and viewer.</li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code> §13a, ADR 0019, <code>03-runtime/15-workspace-ignore-rules.md</code></li><li><strong>Acceptance</strong>: D (workspace), Security</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>fs-panel-guard.test.mjs</code>); full UI scenario Draft</li></ul><hr><h4 id="e2e-059a-transcript-message-plates-follow-workbuddy-density" tabindex="-1">E2E-059a: Transcript message plates follow WorkBuddy density <a class="header-anchor" href="#e2e-059a-transcript-message-plates-follow-workbuddy-density" aria-label="Permalink to &quot;E2E-059a: Transcript message plates follow WorkBuddy density&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A session contains at least one short user prompt, one longer user prompt, and a completed assistant answer; light and dark themes available.</li><li><strong>Steps</strong>: 1) Open the session in dark theme. 2) Inspect user and assistant rows at rest and on hover. 3) Start a streaming assistant answer. 4) Switch to light theme and repeat. 5) Focus the copy control with the keyboard.</li><li><strong>Expected</strong>: User turns are right-aligned, theme-neutral soft plates capped near 560px, derived from each theme&#39;s primary text ink rather than an accent tint, with a subtle border; assistant answers remain transparent full-width prose in the 720px content band, including while they stream — no left rail and no whole-turn <code>--ds-tile</code> (D323). The tile belongs only to a subagent/delegation card (D319). Row spacing is denser (~10px). Copy chips are hidden at rest, appear on hover/focus-within, and stay right-aligned under user turns. Both themes keep readable contrast on the user plate.</li><li><strong>Specs linked</strong>: <code>04-ux/07-ui-design-system.md</code>, <code>04-ux/08-component-spec.md</code> §8.3 / §8.4, <code>04-ux/10-workbuddy-benchmark-ux.md</code>, decisions-log D101, D323</li><li><strong>Acceptance</strong>: C (chat stream), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>transcript-style.test.mjs</code>); full visual scenario Draft</li></ul><h4 id="e2e-chat-transcript-context-menu-right-click-a-message-or-the-transcript" tabindex="-1">E2E-CHAT-transcript-context-menu: Right-click a message or the transcript <a class="header-anchor" href="#e2e-chat-transcript-context-menu-right-click-a-message-or-the-transcript" aria-label="Permalink to &quot;E2E-CHAT-transcript-context-menu: Right-click a message or the transcript&quot;">​</a></h4><ul><li><p><strong>Preconditions</strong>: A session contains a completed user prompt and a completed assistant answer; the conversation pane is focused.</p></li><li><p><strong>Steps</strong>: 1) Right-click the user plate. 2) Choose Copy, then Select message text. 3) Select a phrase in the user plate, right-click that plate, and choose Copy; collapse the caret, right-click again, and choose Copy. 4) Right-click the assistant turn and choose Copy. 5) Right-click empty space below the last turn and choose Copy conversation. 6) Press Escape on an open menu, then Tab. 7) Right-click a markdown link in the answer. 8) Edit the user message, replace its content, select a phrase, and right-click Copy. Repeat with a collapsed caret and Select message text, then cancel editing and copy the saved message.</p></li><li><p><strong>Expected</strong>: The user menu lists Copy, Select message text, Edit, and a separated Delete; the assistant menu lists Copy, Select message text, Regenerate, and Branch. Copy writes the live selection in the right-clicked turn when the menu opened over one; a collapsed caret or a selection outside that row falls back to the whole turn. Copy conversation writes the labelled thread. Both show a toast. Select text highlights the bubble. Escape and Tab dismiss the menu without running an item. A link still offers Open in default browser, Open in work panel, and Copy link address. Quote, Annotate, and Open side chat are absent (ADR 0268). The surface is a viewport-fixed body-level layer and does not resize the transcript. During editing, Copy uses the selected draft text or the full draft with a collapsed caret; Select message text selects the draft. Edit, Delete, and revision actions are absent until editing ends. Cancel preserves the original message. Automated Chromium component check: <code>node scripts/e2e-message-edit-copy.mjs</code>.</p></li><li><p><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code> §8.3 / §8.5, <code>04-ux/09-interaction-patterns.md</code> (floating dropdown surfaces), ADR 0268</p></li><li><p><strong>Acceptance</strong>: C (chat stream), Quality</p></li><li><p><strong>Milestone</strong>: M5</p></li><li><p><strong>Status</strong>: Unit-covered (<code>chat-context-menu.test.mjs</code>, <code>chat-context-menu-items.test.mjs</code>, <code>chat-context-menu-surface.test.mjs</code>); full UI scenario Draft</p></li><li><p><strong>Full-history regression</strong>: Open a 140-message session without scrolling back and choose Copy conversation. The clipboard must contain messages 1–140, not only the loaded tail. Repeat from a search context window and with a truncated long message. Preserve visible in-flight text; a failed history read must report an error and leave the clipboard unchanged. Copy must not load history until selected or change the reading position. Run <code>node scripts/e2e-copy-conversation.mjs</code>.</p></li></ul><h4 id="e2e-chat-copy-formula-as-tex-copying-rendered-math-yields-its-source" tabindex="-1">E2E-CHAT-copy-formula-as-tex: Copying rendered math yields its source <a class="header-anchor" href="#e2e-chat-copy-formula-as-tex-copying-rendered-math-yields-its-source" aria-label="Permalink to &quot;E2E-CHAT-copy-formula-as-tex: Copying rendered math yields its source&quot;">​</a></h4><p>Math boundaries remain parseable after copying: touching inline fences get one separator, and every prose dollar in the copied text is escaped, together with backslash runs that would otherwise escape a fence. Annotation whitespace is preserved; widened multiline inline math uses a literal <code>&lt;span&gt;</code> wrapper to prevent a flow opener when pasted at column zero. TeX newlines are not flattened because they can terminate <code>%</code> comments. The wrapper is Markdown source in <code>text/plain</code>, not a <code>text/html</code> payload; compatibility with external editors that disallow inline HTML is not promised. Regression coverage checks both copy entry points and Markdown round trips for adjacent formulas, prose dollars on either side, formatting wrappers, line/block boundaries, padding, and multiline math including TeX comments. Display math containing <code>- x</code>, <code>+ x</code>, <code>* x</code>, <code>&gt; x</code>, or internal blank lines must remain a single math node after copying. The shared remark grammar keeps unclosed math in the streaming tail until its fence closes; subsequent prose and its source offsets remain intact. A footnote whose definition follows its reference — adjacently or streamed in later — renders as a real reference and note section rather than a literal <code>[^1]</code>, which is what block splitting costs when a slice is parsed without the rest of the message. CRLF, ordinary lists, quotes, GFM tables, fenced code and the sources that must keep splitting are covered by <code>markdown-blocks.test.mjs</code>.</p><ul><li><strong>Preconditions</strong>: An assistant answer contains inline math inside a sentence, a display formula on its own lines, a <code>\\[ … \\]</code> formula inside a sentence and another on its own lines, a <code>\\( … \\)</code> formula, a formula carrying a literal <code>$</code>, an inline formula whose value carries a newline and another whose value carries one at each edge, a paragraph with no math, and — each sharing a selection with a formula — a paragraph whose source wrapped across several lines, a bulleted list, a table, a second table whose cell holds a <code>\\[ … \\]</code> formula, a fenced code block that contains blank lines, and a turn whose prose is interrupted by chrome that is unselectable only because it inherits the shell&#39;s <code>user-select: none</code> (a tool row&#39;s section head).</li><li><strong>Steps</strong>: 1) Select the sentence holding the inline formula and press Ctrl/Cmd+C. 2) Select the display formula and copy it. 3) Select from the middle of a formula to the end of its sentence and copy. 4) Select a paragraph, a formula, and another paragraph together and copy. 5) Select prose with no formula in it and copy. 6) Copy each of the wrapped paragraph, the list, both tables, and the code block. 7) Copy each <code>\\[ … \\]</code> formula, the <code>\\( … \\)</code> one, the formula carrying a literal <code>$</code>, and the two whose values carry newlines. 8) Select a formula backwards — from the end of the sentence to its start — and copy. 9) With a formula selected, right-click that turn and choose Copy, then right-click a different turn and choose Copy. 10) Select a turn&#39;s whole contents the way the Select text item does, so the range is anchored on the turn while the copy is raised on a paragraph inside it, and copy. 11) Select without ever leaving one formula — a drag inside its rendering — and copy. 12) Render each result back through the same answer renderer — what pasting it into the composer and sending it produces — and compare the formulas it paints with the ones the selection covered.</li><li><strong>Expected</strong>: Every formula reaches the clipboard as the TeX it was written in — <code>$…$</code> inline, <code>$$…$$</code> on its own lines — instead of the glyphs KaTeX painted, or the same expression twice over from its two trees. A formula carrying a literal <code>$</code> widens its delimiter run past it, as a code span&#39;s fence does, and pastes back as the formula rather than as prose; an inline formula whose value carries a newline keeps the narrow run, because a <code>$$</code> run at the start of a line would open a flow block and swallow the paragraph; one whose value carries a newline at each edge is written with padding of its own, because the grammar drops a line ending there as it drops a space. A cut that starts inside a formula copies the whole formula, and a selection that never leaves one copies that formula alone. Everything else in the selection is byte for byte what the platform already copied: a wrapped paragraph stays one line, a list keeps one line per item, a table keeps tabs between cells and one line per row including the newline the platform closes the last row with, and a code block keeps its blank lines. Chrome the copy leaves behind stays out of the clipboard whether <code>base.css</code> marks it <code>user-select: none</code> by selector (a code block&#39;s language rail) or it is inert only by inheriting the shell&#39;s default (a tool row&#39;s section head, a compact thinking row). A selection with no formula in it is written by the platform unchanged; a selection that holds one reaches the clipboard as its source however deep inside it the copy was raised, so a range anchored on a turn — what Select text produces — copies as TeX even though Chromium raises the event on a paragraph within it. Right-click Copy and Ctrl/Cmd+C produce the same string for one selection, and a right-click on a turn that does not own the selection reads no excerpt from it, so Copy falls back to that turn&#39;s own source. A pasted formula renders as the one it came from — same TeX, and display math still display math — <code>\\[ … \\]</code> included: it is painted inside its sentence&#39;s paragraph, and copies with <code>$$</code> on a line of its own, which is enough to open a display block when it is pasted back. <code>\\( … \\)</code> stays inline. A table cell reaches it too, measured rather than assumed: the <code>.katex-display</code> box breaks the row for the platform&#39;s own reading as well, so no tab lands beside the fence and the formula copies as display math with the neighbouring cell intact on its own line. A backward selection is still backward after the copy. The copy writes <code>text/plain</code> only — no <code>text/html</code> flavour is put on the clipboard.</li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code> §8.7, ADR 0268, decisions-log D619, issue #414</li><li><strong>Acceptance</strong>: C (chat stream), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Automated (<code>pnpm test:e2e:copy-tex</code>, real Chromium) plus <code>selection-tex.test.mjs</code></li></ul><h4 id="e2e-060b-neutral-gray-accent-across-chrome" tabindex="-1">E2E-060b: Neutral gray accent across chrome <a class="header-anchor" href="#e2e-060b-neutral-gray-accent-across-chrome" aria-label="Permalink to &quot;E2E-060b: Neutral gray accent across chrome&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: App running in dark and light themes; plugins page and a chat with markdown links/blockquotes available.</li><li><strong>Steps</strong>: 1) Inspect focus rings, primary buttons, toggles, selected session ring, plugin market primary CTAs. 2) Open an assistant answer with links and a blockquote. 3) Switch theme and re-check.</li><li><strong>Expected</strong>: No blue brand accent remains. Interactive accent, markdown links/rules, and plugin primary actions resolve through the neutral gray accent tokens (<code>white/gray</code> dark, dark-ink light). The plugins installed/market UI (tabs, search, cards, permission modal, and primary / secondary buttons) uses only <code>--ds-*</code> tokens with no blue-slate fallbacks in either theme; button surfaces and ink remain visible in dark mode. Semantic success/warning/error colors are unchanged.</li><li><strong>Specs linked</strong>: <code>04-ux/07-ui-design-system.md</code>, <code>04-ux/08-component-spec.md</code></li><li><strong>Acceptance</strong>: Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>neutral-accent.test.mjs</code>, <code>plugins-page-style.test.mjs</code>); visual scenario Draft</li></ul><h4 id="e2e-060c-assistant-markdown-prose-hierarchy-and-code-chrome" tabindex="-1">E2E-060c: Assistant markdown prose hierarchy and code chrome <a class="header-anchor" href="#e2e-060c-assistant-markdown-prose-hierarchy-and-code-chrome" aria-label="Permalink to &quot;E2E-060c: Assistant markdown prose hierarchy and code chrome&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A completed assistant answer containing headings, a blockquote, a GFM table, a fenced code block with a language tag, inline code, a task list, and a remote image link; light and dark themes available.</li><li><strong>Steps</strong>: 1) Open the session in dark theme and scroll the answer. 2) Hover the code-block copy control and the table rows. 3) Expand a thinking disclosure that contains markdown. 4) Switch to light theme and re-check contrast on inline code, blockquote rule, and code card.</li><li><strong>Expected</strong>: Answer prose uses the <code>.prose-chat</code> hierarchy (h1–h6 ramp, accent-tinted blockquote, hairline-bordered inline code, zebra/hover table shell, inset code card with monospace language tag). A wide GFM table stays inside the transcript column: headers and cells wrap rather than overflowing. Thinking prose stays secondary/smaller and does not merge into the answer. Both themes keep readable contrast; copy still copies raw fence text.</li><li><strong>Specs linked</strong>: <code>04-ux/07-ui-design-system.md</code>, <code>04-ux/08-component-spec.md</code> §8.7</li><li><strong>Acceptance</strong>: C (chat stream), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>user-select.test.mjs</code>, <code>thinking-ui.test.mjs</code>, <code>markdown-prose-style.test.mjs</code>); full visual scenario Draft</li></ul><h4 id="e2e-061-user-message-plaintext-layout-survives-wrapping-and-reload" tabindex="-1">E2E-061: User message plaintext layout survives wrapping and reload <a class="header-anchor" href="#e2e-061-user-message-plaintext-layout-survives-wrapping-and-reload" aria-label="Permalink to &quot;E2E-061: User message plaintext layout survives wrapping and reload&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Provider configured; composer can accept multi-line input via Shift+Enter (or Enter-to-send disabled).</li><li><strong>Steps</strong>: 1) Compose a three-line prompt with two hard newlines and a URL whose encoded path is wider than the user plate. 2) Send. 3) Inspect the user bubble in the transcript. 4) Copy the user message and paste into an external editor. 5) Reload the session.</li><li><strong>Expected</strong>: The user plate shows three distinct lines (not collapsed to a single paragraph). The linked URL wraps inside the plate without horizontal overflow, and every continuation line stays logical-start aligned with the first line instead of being centered. Copied text retains the original newlines. After reload the same line breaks remain.</li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code></li><li><strong>Acceptance</strong>: C (chat stream), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>transcript-style.test.mjs</code>); full visual scenario Draft</li></ul><h4 id="e2e-060d-assistant-meta-chips-compact-context-summary-and-retry-action" tabindex="-1">E2E-060d: Assistant meta chips, compact context summary, and retry action <a class="header-anchor" href="#e2e-060d-assistant-meta-chips-compact-context-summary-and-retry-action" aria-label="Permalink to &quot;E2E-060d: Assistant meta chips, compact context summary, and retry action&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A completed assistant message includes modelId and token usage; another completed assistant message has content but no usage. The selected model has a published 1m-class context window, while its provider binding still contains the legacy 128k generic seed.</li><li><strong>Steps</strong>: 1) Open the session. 2) Confirm the completed turn shows a model badge and no context inspector under the answer. 3) Hover the composer toolbar inspector trigger, confirm the panel stays closed, then click it. 4) Inspect the remaining-token-plus-percentage heading, used/window counts, unboxed turn/speed values, one inline provider-usage summary, and one aggregate tool-usage summary, with no doubled heading rule and no inner section hairlines. 5) Scroll the transcript and resize the window while the panel is open. Toggle and resize the sidebar and work panel while the panel remains open. 6) Move the pointer away from the panel, then dismiss it by clicking the trigger again, clicking outside it, and pressing Escape from the keyboard. 7) Open the work panel on a browser-preview artifact, open the context inspector, and confirm the summary stays inside the conversation pane: fully visible, clear of the panel column, and narrowed rather than clipped on a narrow pane. 8) Click Retry on that turn while idle. 9) Confirm a session without usage still offers Retry on completed turns and omits the composer inspector.</li><li><strong>Expected</strong>: Model badge appears under completed assistant answers when a model id exists. The compact Context inspector appears in the composer right toolbar, left of the model picker, once any usage exists, and always mirrors the newest usage-bearing assistant turn. The trigger shows the ring plus remaining-capacity percentage (no Context label) and low-space warning/error states; click or keyboard activation toggles the same compact summary while pointer hover alone never opens or closes it. An open panel survives the pointer leaving it and closes on a second trigger activation, an outside click, or Escape, which returns focus to the trigger. Provider values remain exact, tool values remain visibly approximate through the <code>~</code> aggregate total, and no per-tool list, source badge, progress bar, or explanatory estimate paragraph is rendered. Occupancy, turn total, and provider cache/input/output/reasoning/hit-rate are the last usage-bearing assistant message, not the summed tool-loop, so cache read stays on the same scale as the context window. The cache hit rate is omitted when cache-read metadata is absent rather than inferred. A published 1m-class limit (for example <code>gpt-5.6-luna</code> at 1,050,000 tokens) is shown instead of 128k, the same effective window is used by the agent runtime, and a non-default Advanced override remains honored. Generation rate remains a completed-turn value and does not update during streaming; Retry re-sends the nearest preceding user prompt and is disabled while a turn is running; the portaled panel stays inside the conversation pane so the work panel&#39;s native browser or plugin surface can never cover it, narrows with a narrow pane instead of crossing that edge, and follows the trigger after scrolling or resize; Copy still excludes thinking text.</li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code>, <code>04-ux/10-workbuddy-benchmark-ux.md</code>, <code>03-runtime/01-ipc-protocol.md</code></li><li><strong>Acceptance</strong>: C (chat stream), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>transcript-style.test.mjs</code>, <code>context-usage.test.mjs</code>, <code>latest-turn-context.test.mjs</code>, runtime usage mapping); full scenario Draft</li></ul><h4 id="e2e-061a-regenerate-replaces-the-current-turn-in-place" tabindex="-1">E2E-061a: Regenerate replaces the current turn in place <a class="header-anchor" href="#e2e-061a-regenerate-replaces-the-current-turn-in-place" aria-label="Permalink to &quot;E2E-061a: Regenerate replaces the current turn in place&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A session has user A → assistant A → user B → assistant B.</li><li><strong>Steps</strong>: 1) Hover assistant A and click Regenerate. 2) Wait for the new turn to complete. 3) Reload the session.</li><li><strong>Expected</strong>: Transcript truncates away assistant A / user B / assistant B before the redo starts; only user A plus the new assistant/tool tail remain. The regenerated answer does not leave the old branch above it. Reload keeps the truncated branch only.</li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code>, <code>03-runtime/01-ipc-protocol.md</code>, <code>03-runtime/04-data-storage.md</code></li><li><strong>Acceptance</strong>: C (chat stream), F (persistence)</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (store/main truncate wiring tests); full scenario Draft</li></ul><h4 id="e2e-062-regenerate-history-pager-restores-prior-variants" tabindex="-1">E2E-062: Regenerate history pager restores prior variants <a class="header-anchor" href="#e2e-062-regenerate-history-pager-restores-prior-variants" aria-label="Permalink to &quot;E2E-062: Regenerate history pager restores prior variants&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A session where an assistant answer was regenerated at least once.</li><li><strong>Steps</strong>: 1) Click Retry/Regenerate on a completed assistant turn. 2) Observe the visible root user bubble while the replacement turn starts and after it completes. 3) Switch to a previous variant. 4) Switch forward again. 5) Reload the session.</li><li><strong>Expected</strong>: The root user bubble remains visible and shows the <code>current / total</code> pager inside its action toolbar once the row is hovered or focused; the toolbar, including the pager, is hidden by default. Retry does not move or detach the selector from that bubble. Switching restores the archived assistant/tool branch in place. Reload preserves the active variant and the full revision set.</li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code>, <code>03-runtime/04-data-storage.md</code></li><li><strong>Acceptance</strong>: C (chat stream), F (persistence)</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>sessions::tests::save_and_activate_message_revision</code>, schema v4 migration); full scenario Draft</li></ul><h4 id="e2e-063-empty-home-keeps-the-primary-task-surface-focused" tabindex="-1">E2E-063: Empty home keeps the primary task surface focused <a class="header-anchor" href="#e2e-063-empty-home-keeps-the-primary-task-surface-focused" aria-label="Permalink to &quot;E2E-063: Empty home keeps the primary task surface focused&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: App running on empty chat home (no transcript) in light and dark themes; window can be resized to ~1200×690 and ~900×640.</li><li><strong>Steps</strong>: 1) Open empty home. 2) Confirm the hero contains the quiet logo, localized title, and short supporting line. 3) Confirm no developer starter cards or contextual quick-action row is rendered. 4) Dismiss onboarding and inspect again. 5) Repeat in the other theme. 6) Resize to a short height and scroll the content region if needed.</li><li><strong>Expected</strong>: The default empty state keeps the hero and bottom composer as the visual anchors, with optional onboarding as the only additional content. Dismissing the checklist leaves no empty spacer. The composer remains at the bottom without covering the hero or checklist, and short windows keep every content block reachable via scroll.</li><li><strong>Specs linked</strong>: <code>04-ux/01-ui-ia.md</code>, <code>04-ux/07-ui-design-system.md</code>, <code>04-ux/08-component-spec.md</code>, <code>08-meta/decisions-log.md</code> (D111/D131/D204/D206)</li><li><strong>Acceptance</strong>: Quality (layout integrity)</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>home-empty-layout.test.mjs</code>); full UI scenario Draft</li></ul><h4 id="e2e-094-active-turns-keep-the-lower-transcript-surface-clear" tabindex="-1">E2E-094: Active turns keep the lower transcript surface clear <a class="header-anchor" href="#e2e-094-active-turns-keep-the-lower-transcript-surface-clear" aria-label="Permalink to &quot;E2E-094: Active turns keep the lower transcript surface clear&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A deterministic agent can emit thinking, tool start/end, streamed answer, permission, and terminal events for a durable session; a second session can run in the background.</li><li><strong>Steps</strong>: 1) Start a turn in the visible session. 2) Observe the transcript while the agent is thinking, using tools, and streaming an answer. 3) Trigger a permission request and inspect the approval card. 4) Switch to a second session while the first continues. 5) Return to the first session after completion.</li><li><strong>Expected</strong>: No generic Understanding, Working, Checking, or completion card appears below the transcript while the turn is active. Assistant and tool rows remain inline. One compact tail status remains visible throughout the running turn, including partial-answer pauses and completed-tool gaps. Runtime phases take precedence over Working or Planning/Goal; pending user interaction suppresses the status. Only an actual permission request renders an actionable permission card. Background activity never changes the visible session, transcript, composer focus, or project.</li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code>, <code>04-ux/09-interaction-patterns.md</code>, <code>03-runtime/10-session-state-machine.md</code></li><li><strong>Acceptance</strong>: C (chat stream), Quality (interaction and accessibility)</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>active-turn-surface.test.mjs</code>); full UI scenario Draft</li></ul><h4 id="e2e-095-terminal-failures-expose-recovery-without-a-success-card" tabindex="-1">E2E-095: Terminal failures expose recovery without a success card <a class="header-anchor" href="#e2e-095-terminal-failures-expose-recovery-without-a-success-card" aria-label="Permalink to &quot;E2E-095: Terminal failures expose recovery without a success card&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A deterministic provider can recover from a failed directory <code>Read</code> with <code>Glob</code>, complete a turn with a workspace edit, and fail another turn with a retriable error; the session has a visible composer.</li><li><strong>Steps</strong>: 1) Run the failed-Read then successful-Glob recovery turn and inspect its activity group. 2) Complete the workspace-edit turn. 3) Confirm that no success outcome card appears and inspect the inline review card immediately after the change tool row. 4) Expand the inline card and verify its hunks. 5) Commit the edited file and confirm the recorded card remains, then use rollback once. 6) Trigger the retriable failure. 7) Inspect the failure card, then choose Retry. 8) Start another new prompt and inspect the old card.</li><li><strong>Expected</strong>: The recovered turn keeps the failed Read visible on its own row, labels the containing group as processed, completes its session outcome, and shows no failure card. Completion uses the transcript and inline review card as its evidence without adding a &quot;Task complete&quot; card. File status, counts, and hunks remain on the adjacent card after commit, and guarded rollback restores the pre-tool state. Failure shows that existing work remains, exposes Retry and Continue, and retry preserves the latest prompt. A new turn clears the previous failure card; an abort creates no failure outcome copy.</li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code>, <code>04-ux/09-interaction-patterns.md</code>, <code>03-runtime/10-session-state-machine.md</code>, ADR 0069</li><li><strong>Acceptance</strong>: C (chat stream), Quality (completion and recovery)</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>assistant-turns.test.mjs</code>, <code>interaction-performance.test.mjs</code>, <code>turn-outcome-card.test.mjs</code>); full UI scenario Draft</li></ul><h4 id="e2e-064-durable-notification-inbox-records-terminal-task-outcomes" tabindex="-1">E2E-064: Durable notification inbox records terminal task outcomes <a class="header-anchor" href="#e2e-064-durable-notification-inbox-records-terminal-task-outcomes" aria-label="Permalink to &quot;E2E-064: Durable notification inbox records terminal task outcomes&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Two durable sessions exist; a deterministic provider can complete one turn, fail one turn with a stable error code, and abort one turn; notification inbox starts empty.</li><li><strong>Steps</strong>: 1) Focus and view session A, then complete a turn in A. 2) While still focused on A, fail a turn in background session B. 3) Unfocus the window and complete another turn in A. 4) Abort a fourth turn. 5) Repeat each terminal RPC. 6) Confirm the main titlebar has no bell, then open the bell in the expanded sidebar footer and switch between All and Unread. 7) Mark one row read and confirm its session has no terminal sidebar mark, then close/reopen the popover and restart the app. 8) Select the other session from its terminal-marked sidebar row. 9) Generate a host fixture with 205 eligible terminal turns. 10) Use Mark all read, then Clear.</li><li><strong>Expected</strong>: A&#39;s visible-current completion creates no row or terminal sidebar mark. Exactly two rows exist, newest first: the unfocused A completion and background B failure, with localized labels, snapshotted session titles, and B&#39;s stable code. Abort/repeated terminal calls create no row. The former footer Help shortcut is absent; the 32px footer bell and its upward-opening popover replace it. Badge and Unread show the exact unread count without opening implicitly reading rows. Read state and both records survive restart. Row selection marks it read and activates its bound project/session. The fixture retains exactly the newest 200 rows. Mark all preserves rows with zero unread. Selecting the other session clears its terminal sidebar mark and marks its task notification read; neither mark returns after refresh or restart. Clear empties only the inbox and leaves sessions, turns, and transcripts intact.</li><li><strong>Specs linked</strong>: <code>03-runtime/04-data-storage.md</code>, <code>03-runtime/06-host-rpc-protocol.md</code>, <code>03-runtime/01-ipc-protocol.md</code>, <code>04-ux/07-ui-design-system.md</code>, <code>04-ux/08-component-spec.md</code>, <code>08-meta/decisions-log.md</code> (D117/D130)</li><li><strong>Acceptance</strong>: C (turn completion), F (persistence), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Draft</li></ul><h4 id="e2e-065-native-task-notifications-are-unfocused-only-and-activate-sessions" tabindex="-1">E2E-065: Native task notifications are unfocused-only and activate sessions <a class="header-anchor" href="#e2e-065-native-task-notifications-are-unfocused-only-and-activate-sessions" aria-label="Permalink to &quot;E2E-065: Native task notifications are unfocused-only and activate sessions&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Native notifications are supported; sessions A and B exist; the main window can be focused, unfocused, hidden, and minimized. A Windows run uses the NSIS-installed app or the standard development command.</li><li><strong>Steps</strong>: 1) Keep the app focused on A and complete a turn in A. 2) While still focused on A, complete a turn in B. 3) Unfocus the app while A remains current and complete another turn in A. 4) Let A&#39;s notification move into the OS notification center, then click it. 5) Minimize the app, fail another turn, and click its native notification. 6) Unfocus the app and abort a turn. 7) Repeat with native delivery suppressed by the OS. 8) On Windows, inspect the native notification attribution, notification-settings entry, taskbar group, installed executable, and Start menu shortcut.</li><li><strong>Expected</strong>: Focused-current A creates neither inbox row, terminal sidebar mark, nor native banner. Focused-background B creates an inbox row without a native banner. Unfocused current A and the minimized failure each create one durable row and one localized native notification. Clicking restores, shows, and focuses the main window before activating the matching session, including after the notification has moved to Windows Action Center; no event opens the wrong currently selected session. Abort shows neither surface. OS suppression does not lose the durable row or surface a misleading app error. Every inspected Windows system surface identifies <code>PI-Desktop</code>; no stock Electron application name or identity is exposed.</li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code>, <code>04-ux/07-ui-design-system.md</code>, <code>04-ux/09-interaction-patterns.md</code>, <code>08-meta/decisions-log.md</code> (D117/D141)</li><li><strong>Acceptance</strong>: C (turn completion), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Source-contract covered (<code>notification-contract.test.mjs</code>); packaged Windows Action Center activation remains runner validation; full UI scenario Draft</li></ul><h4 id="e2e-066-provider-model-catalog-survives-restart-and-offline-refresh" tabindex="-1">E2E-066: Provider model catalog survives restart and offline refresh <a class="header-anchor" href="#e2e-066-provider-model-catalog-survives-restart-and-offline-refresh" aria-label="Permalink to &quot;E2E-066: Provider model catalog survives restart and offline refresh&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A saved provider matches a models.dev provider/API URL and has at least two catalog models; a deterministic fixture can make <code>https://models.dev/api.json</code> unavailable and can also make a provider discovery endpoint unavailable.</li><li><strong>Steps</strong>: 1) Open the provider model picker and confirm models.dev model names, limits, capability badges, and source label appear. 2) Inspect the network fixture and confirm the provider API key is never sent to models.dev. 3) Quit and restart the app. 4) Disconnect models.dev and the provider endpoint. 5) Open the Composer model menu and wait for refresh fallback. 6) Reconnect only the provider endpoint with one custom model, then reopen the picker. 7) Warm lookups for a known and an unknown model, then refresh the catalog fixture with changed metadata and the previously unknown model. 8) Repeat after making that catalog refresh fail.</li><li><strong>Expected</strong>: The first picker open renders the configured/provider cache without starting from an empty list. On restart, the bundled models.dev release snapshot is used without network access. A failed Settings refresh preserves that in-memory snapshot; a custom provider then falls back to its endpoint only for IDs absent from models.dev and finally the configured bindings. Offline refresh preserves every cached/configured entry. A successful provider discovery may persist normalized IDs to Rust-owned SQLite, but it cannot replace models.dev metadata or user-defined bindings. A successful catalog refresh replaces both cached matches and cached misses; a failed refresh keeps the previous results. Editing a model binding or changing the default provider/model takes effect without restarting the app.</li><li><strong>Specs linked</strong>: <code>03-runtime/04-data-storage.md</code>, <code>03-runtime/12-provider-config-schema.md</code>, <code>03-runtime/13-model-catalog-and-selection.md</code>, <code>04-ux/08-component-spec.md</code></li><li><strong>Acceptance</strong>: B (model config), F (persistence), Quality, Security</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>providers::tests</code>, <code>model-cache.test.mjs</code>, <code>models-dev-catalog.test.mjs</code>); full restart/offline UI scenario Draft</li></ul><h4 id="e2e-080-models-dev-metadata-and-generic-unknown-models" tabindex="-1">E2E-080: Models.dev metadata and generic unknown models <a class="header-anchor" href="#e2e-080-models-dev-metadata-and-generic-unknown-models" aria-label="Permalink to &quot;E2E-080: Models.dev metadata and generic unknown models&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A provider/model exists in the models.dev fixture with limits, modalities, and reasoning options. A second fixture contains a provider-discovered model ID absent from models.dev.</li><li><strong>Steps</strong>: 1) Open the provider model picker and select the fixture model. 2) Confirm its models.dev name, context/output limits, capability badges, modalities, cost fields, and thinking levels. 3) Start a short turn and inspect the sidecar model snapshot/request metadata. 4) Use Settings → Model configuration to force a models.dev refresh and confirm the new record is visible without changing the bundled file or writing a user cache. 5) Repeat with an ID absent from models.dev.</li><li><strong>Expected</strong>: The matching models.dev record is authoritative, including its <code>limit</code>, <code>modalities</code>, <code>reasoning_options</code>, <code>tool_call</code>, <code>structured_output</code>, dates, and cost fields; no provider secret is sent to the catalog. Vendor-prefixed records match providers whose configured model ID omits the prefix when the vendor/API identity is unambiguous. A PDF-capable model shows PDF in its modality metadata; PDF attachments remain bounded file references until the selected transport exposes a native PDF block. A provider-discovered or explicitly configured ID absent from models.dev remains runnable with the generic text-only, non-reasoning shape; pi-ai supplies only the selected wire adapter, OAuth flow, and account model availability. A ChatGPT Plus/Pro or GitHub Copilot account lists <code>gpt-6-astra</code> from the pinned pi-ai 0.86.1 catalog; models.dev then supplies its published metadata.</li><li><strong>Specs linked</strong>: <code>02-architecture/02-tech-stack.md</code>, <code>03-runtime/11-provider-model-system.md</code>, <code>03-runtime/13-model-catalog-and-selection.md</code>, ADR 0134</li><li><strong>Acceptance</strong>: B (model config), C (conversation &amp; stream), Security</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>model-capabilities.test.ts</code>, <code>models-dev-catalog.test.mjs</code>); full UI scenario Draft</li></ul><h4 id="e2e-067-platform-application-menus-and-window-chrome" tabindex="-1">E2E-067: Platform application menus and window chrome <a class="header-anchor" href="#e2e-067-platform-application-menus-and-window-chrome" aria-label="Permalink to &quot;E2E-067: Platform application menus and window chrome&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Native macOS, Windows, and Linux runners; built desktop app; English and zh-CN locales available. The Windows/Linux harness can set <code>PI_DESKTOP_START_MAXIMIZED=1</code> before launch so Main maximizes the hidden native window before renderer mount.</li><li><strong>Steps</strong>: 1) On macOS, launch both <code>pnpm dev</code> and a packaged build. Confirm the application-menu title is PI-Desktop, open About PI-Desktop, and inspect its name, version, and icon. Then open every system menu and invoke New Task, Open Project, Settings, global search, sidebar toggle, editing, zoom/fullscreen, Window, Help, Logs, and Check for Updates actions. Verify the update status reports that the current fixture version is up to date. 2) On Windows/Linux, confirm no File/Edit/View/Window/Help menubar appears inside the window and the left-side navigation occupies the reclaimed titlebar space. Verify F10 and Shift+F10 are not consumed by shell chrome; exercise New Task, Open Project, Settings, close-window, zoom, fullscreen, global search (Cmd/Ctrl+K and Cmd/Ctrl+Shift+P), sidebar, and standard editing shortcuts. Invoke Check for Updates from Settings -&gt; Info with the same status result. 3) Close the macOS window, immediately invoke two native menu commands, and acknowledge renderer readiness after the replacement loads. Verify one window and one delivery per command. 4) On Windows/Linux, repeat from the main chat, Settings, and an open work panel. With the work panel open, confirm the viewport-fixed toggle and native window controls stay at the window&#39;s right edge over the panel header, that resource switching plus close actions remain available from the panel menu to their left, and that the native window controls themselves still take hover and click — clicking a control must not move the window. In the main chat, send a first user message and confirm its full bubble starts below the 46px titlebar control band. Open the Extensions page and confirm its header actions, then the detail sheet&#39;s close button, also start below that band and take their own clicks instead of moving the window. Click the center plus the top, bottom, and titlebar-facing edges of each right-side control to minimize, maximize, restore, and close the window. 5) Start the renderer while its native window is already maximized and inspect the initial queried glyph/state. 6) Attempt unknown menu/window IPC actions while a window exists and after it closes. 7) Build each target on its native runner from a clean release-host directory. On Windows, inspect the installed app&#39;s taskbar button and Start menu shortcut icon.</li><li><strong>Expected</strong>: macOS development and packaged launches show PI-Desktop as the native application identity, and the About panel uses the canonical PI-Desktop icon; neither surface exposes the stock Electron name or icon. macOS follows native menu conventions and accelerators. Windows/Linux show no application menu inside the window; navigation and right-side controls do not collide with drag regions, keyboard shortcuts remain operational, and a viewport-fixed work-panel toggle is present on non-Settings routes (not an application-menu command). While the panel is open the native control band and that toggle overlay the panel header; the header ends its box before the band, so the resource tabs and close controls stay clear of the toggle and neither the window controls nor resource close sit under a drag rectangle. Check for Updates invokes the allowlisted update command from the macOS system menu and the Settings surface and shows the resulting up-to-date state. Replacement-window commands wait for renderer readiness without creating duplicate windows or losing events. No Main, Settings, or work-panel drag rectangle overlaps the reserved control zone. The 120px control band is an opaque <code>bg-primary</code> surface in both light and dark themes, with an 8px visual buffer separating it from adjacent work-panel actions, so destination content never bleeds through it. Window controls remain clickable across their full 46px-high hit targets, match native state, and have accessible names; the first user or assistant transcript row never paints beneath them, and neither do the Extensions page header actions or the plugin detail sheet close button. The titlebar and right-side control band share one continuous 1px <code>border-subtle</code> separator; the control band&#39;s leading divider uses the same token and its bottom edge does not disappear under the window buttons. Unknown actions fail closed. The installed Windows taskbar button and Start menu shortcut use the PI-Desktop icon, never Electron&#39;s default icon. Each package contains the target-native host binary (<code>.exe</code> only on Windows). Passing this scenario on Windows/Linux proves shell readiness, not first-release qualification.</li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code>, <code>04-ux/01-ui-ia.md</code>, <code>04-ux/02-i18n-english-first.md</code>, <code>04-ux/07-ui-design-system.md</code>, <code>04-ux/08-component-spec.md</code>, <code>04-ux/09-interaction-patterns.md</code>, <code>06-delivery/06-release-runbook.md</code>, <code>08-meta/decisions-log.md</code> (D118, D121, D129, D357)</li><li><strong>Acceptance</strong>: A (app startup), Quality</li><li><strong>Milestone</strong>: M5 on macOS; post-MVP release qualification on Windows/Linux</li><li><strong>Status</strong>: Unit-covered (<code>window-menu.test.mjs</code>, <code>development-branding.test.mjs</code>); Electron boot probe covers platform bridge, native menu installation, and the pre-render maximize fixture on Windows/Linux; native visual scenario Draft</li></ul><h4 id="e2e-143-close-behavior-is-asked-once-and-stays-configurable-d230" tabindex="-1">E2E-143: Close behavior is asked once and stays configurable (D230) <a class="header-anchor" href="#e2e-143-close-behavior-is-asked-once-and-stays-configurable-d230" aria-label="Permalink to &quot;E2E-143: Close behavior is asked once and stays configurable (D230)&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Windows/Linux run with a clean data dir (no <code>close-behavior.json</code>); the main window is visible; Settings → General is reachable. macOS is excluded: it keeps the native Dock lifecycle.</li><li><strong>Steps</strong>: 1) With the preference unset, click the close button (or press the close-window shortcut) and answer the prompt: Cancel keeps the window open and the preference unset; Close to tray hides the window, shows the tray icon, and the app keeps running (a turn in flight stays live); Quit exits the app. 2) Re-run each choice and verify it is remembered across a full restart and that no second prompt ever appears once a choice exists. 3) With <code>tray</code> set, click the tray icon: the window restores, shows, and focuses; the tray context menu offers Open and Quit, and Quit exits the app. 4) In Settings → General, switch between Close to tray / Quit app and verify the next close follows the new choice, that the D216 tray icon stays resident either way, that an unset preference shows no selection, and that search matches the row. 5) With <code>quit</code> stored, restart and close the window: the app exits even though the tray icon is present. 6) From any setting, use the renderer minimize control on Windows/Linux and verify the native taskbar entry remains available and restores the same window; on macOS verify its native minimize still hides to the tray. The Windows taskbar toggle is covered separately by E2E-124. 7) Invoke unknown values and <code>&quot;ask&quot;</code> on <code>pi-desktop/window/closeBehavior/set</code> and verify they fail closed, and verify the channel is rejected outright on macOS.</li><li><strong>Expected</strong>: The first close prompts exactly once per unset state and Cancel never persists a choice. Tray mode keeps the app alive with a localized tooltip/menu and no data loss; switching to Quit app leaves the tray icon in place, because close-to-tray and macOS minimize still need a restore surface. The preference survives restarts and is honored by both the window-control close button and the close shortcut, and a stored <code>quit</code> exits through the ordered <code>before-quit</code> shutdown rather than depending on <code>window-all-closed</code>. Windows/Linux renderer minimize remains taskbar-native; macOS native minimize remains tray-resident; the bounds watchdog never force-restores a minimized or tray-hidden window. The automated boot probe (<code>app.quit</code>) exits without prompting.</li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code>, <code>04-ux/01-ui-ia.md</code>, <code>04-ux/09-interaction-patterns.md</code>, <code>08-meta/decisions-log.md</code> (D216, D230, D256), ADR 0078, ADR 0090, ADR 0123</li><li><strong>Acceptance</strong>: A (app startup), Quality</li><li><strong>Milestone</strong>: M5 on Windows/Linux (release qualification)</li><li><strong>Status</strong>: Draft</li></ul><h4 id="e2e-204-explicit-quit-confirms-before-shutdown-d363" tabindex="-1">E2E-204: Explicit quit confirms before shutdown (D363) <a class="header-anchor" href="#e2e-204-explicit-quit-confirms-before-shutdown-d363" aria-label="Permalink to &quot;E2E-204: Explicit quit confirms before shutdown (D363)&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A normal interactive session is running (not a boot, supervision, or capture probe). The main window may be visible or tray-hidden.</li><li><strong>Steps</strong>: 1) Choose Quit from the tray menu, or press Cmd+Q / the application-menu Quit item. 2) Cancel the native warning and confirm the window, tray, host-core, and sidecar remain. 3) Repeat and confirm Quit; confirm the ordered shutdown runs. 4) On Windows/Linux with close behavior unset, close the window and choose Quit on the D230 dialog; confirm no second warning. 5) Launch with <code>PI_DESKTOP_BOOT_PROBE=1</code> (and the supervision/capture equivalents) and confirm <code>app.quit()</code> exits without a dialog. 6) On a packaged Windows NSIS or Linux AppImage install, download an update and choose Restart to update; confirm the quit warning never appears and the installer finishes the upgrade and relaunch.</li><li><strong>Expected</strong>: Accidental explicit quit is cancellable. A user who already chose Quit on the close-behavior dialog is not asked again. Probes used by automation never block on the warning. An in-app update restart is never deferred behind the warning either: the platform installer is spawned before <code>app.quit()</code> and aborts once the app outlives its wait window, so the update-triggered quit must run the ordered shutdown immediately — the user already committed to the restart by choosing that action.</li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code>, <code>04-ux/09-interaction-patterns.md</code>, <code>08-meta/decisions-log.md</code> (D216, D230, D363), ADR 0022</li><li><strong>Acceptance</strong>: A (app startup), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>close-behavior-tray.test.mjs</code> asserts the probe and update-restart exemptions; <code>auto-update.test.mjs</code> asserts the install latch is set before <code>quitAndInstall</code>); native dialog journey Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-067a-prerelease-install-discovers-newer-stable-release-d120" tabindex="-1">E2E-067A: Prerelease install discovers newer stable release (D120) <a class="header-anchor" href="#e2e-067a-prerelease-install-discovers-newer-stable-release-d120" aria-label="Permalink to &quot;E2E-067A: Prerelease install discovers newer stable release (D120)&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Packaged build whose embedded version is a prerelease such as <code>0.2.0-rc.6</code>; GitHub Releases latest stable tag is newer (for example <code>0.2.2</code>) with published <code>latest*.yml</code> feeds.</li><li><strong>Steps</strong>: 1) Launch the packaged prerelease install. 2) Wait for the automatic check or invoke Check for Updates from the application menu / Settings → Info.</li><li><strong>Expected</strong>: Update state reports <code>available</code> (manual platforms) or advances through in-app download for packaged macOS, Windows NSIS, and Linux AppImage with <code>availableVersion</code> equal to the newer stable tag. A Windows portable ZIP run stays on the manual notify-and-link path and must not download or run the NSIS installer; a legacy portable executable is likewise manual when <code>PORTABLE_EXECUTABLE_FILE</code> is present. The client must not report up-to-date merely because no newer release shares the same <code>rc</code> prerelease channel.</li><li><strong>Specs linked</strong>: <code>04-ux/09-interaction-patterns.md</code>, <code>05-security/01-security.md</code>, <code>08-meta/decisions-log.md</code> (D120), ADR 0022</li><li><strong>Acceptance</strong>: A (app startup), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>auto-update.test.mjs</code> asserts <code>allowPrerelease = false</code>); packaged discovery scenario Draft</li></ul><h4 id="e2e-067b-shipped-locale-update-notes-and-full-changelog-dialog-d164-d345-d349" tabindex="-1">E2E-067B: Shipped-locale update notes and full changelog dialog (D164/D345/D349) <a class="header-anchor" href="#e2e-067b-shipped-locale-update-notes-and-full-changelog-dialog-d164-d345-d349" aria-label="Permalink to &quot;E2E-067B: Shipped-locale update notes and full changelog dialog (D164/D345/D349)&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: The shipped <code>packages/shared</code> CHANGELOG contains aligned <code>en</code>, <code>zh-CN</code>, <code>zh-TW</code>, and <code>ko</code> stable history; product language can be switched. For the compact update path, use a packaged or fixture updater state with a catalogued <code>availableVersion</code>.</li><li><strong>Steps</strong>: 1) With no available update, open Settings → Info and open Release notes. 2) Inspect the complete history, current-version marker, scrolling, and close behavior by close control, Escape, and backdrop. 3) Force or wait for update discovery so status is manual <code>available</code>, in-app <code>downloading</code>, or <code>downloaded</code>; inspect the ambient banner and Settings Updates row, then reopen Release notes. 4) Switch UI language to zh-CN, then zh-TW, then ko and re-inspect without invoking a new check. 5) Repeat the compact update path with a version absent from the catalog.</li><li><strong>Expected</strong>: <code>UpdateState.releaseNotes</code> is plain multi-line product highlights selected by Main from the shipped-locale catalog — never a renderer-supplied URL. Both surfaces show a localized &quot;What&#39;s new&quot; block when notes exist and hide it when they do not. Locale change refreshes notes for the same version. The Release notes action remains available in every updater state and opens a localized, newest-first modal containing every shipped stable entry, with current and available versions identified when present. The modal traps focus, restores it after close, and does not expose a new IPC domain or feed configuration.</li><li><strong>Specs linked</strong>: <code>04-ux/06-settings-ia.md</code>, <code>04-ux/09-interaction-patterns.md</code>, <code>05-security/01-security.md</code>, <code>06-delivery/06-release-runbook.md</code>, <code>08-meta/decisions-log.md</code> (D164), ADR 0022</li><li><strong>Acceptance</strong>: A (app startup), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>auto-update.test.mjs</code>, <code>changelog.test.ts</code>); packaged UI scenario Draft</li></ul><h4 id="e2e-067c-release-version-surface-preflight-blocks-a-misaligned-tag-d260" tabindex="-1">E2E-067C: Release version-surface preflight blocks a misaligned tag (D260) <a class="header-anchor" href="#e2e-067c-release-version-surface-preflight-blocks-a-misaligned-tag-d260" aria-label="Permalink to &quot;E2E-067C: Release version-surface preflight blocks a misaligned tag (D260)&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A clean worktree at the current stable version. No release tag has been created for the candidate version.</li><li><strong>Steps</strong>: 1) Run <code>node scripts/check-release-docs.mjs</code> on the aligned tree. 2) Regress one surface at a time — remove the newest changelog entry from <code>en</code>, then from <code>zh-CN</code>, then change a highlight count so the locales differ, then set <code>docs/package.json</code> to an older version, then leave the READMEs stating the previous <code>&lt;major&gt;.&lt;minor&gt;.x</code> release line — and rerun the preflight after each. 3) Run <code>node scripts/release.mjs &lt;next-version&gt; --tag</code> with one surface still regressed. 4) Restore every surface, rerun the preflight, and repeat the release command.</li><li><strong>Expected</strong>: The aligned tree reports alignment and exits 0. Each regression is reported by name with the offending file and expected version, and exits non-zero. With a regressed surface, <code>release.mjs</code> bumps files but creates neither the release commit nor the tag, and says the documentation check failed. After restoration the preflight passes and the release command proceeds to commit and tag. <code>--skip-docs-check</code> bypasses only the check and is documented as non-release use.</li><li><strong>Specs linked</strong>: <code>06-delivery/06-release-runbook.md</code> §4.1, <code>06-delivery/05-change-checklist.md</code>, <code>06-delivery/03-ai-development-workflow.md</code>, <code>08-meta/decisions-log.md</code> (D164, D260)</li><li><strong>Acceptance</strong>: Quality (release process)</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Script-covered (<code>scripts/check-release-docs.mjs</code>); manual release rehearsal Draft</li></ul><h4 id="e2e-068-fork-a-conversation-into-an-independent-session" tabindex="-1">E2E-068: Fork a conversation into an independent session <a class="header-anchor" href="#e2e-068-fork-a-conversation-into-an-independent-session" aria-label="Permalink to &quot;E2E-068: Fork a conversation into an independent session&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: An idle project conversation has user, assistant, thinking, and tool history plus at least one regenerate variant. A second source conversation is running. A Temporary conversation and two retained project workspaces contain distinct same-named marker files. The idle source has a session-scoped tool grant.</li><li><strong>Steps</strong>: 1) Open the idle conversation overflow menu with keyboard. 2) Choose Create branch. 3) Append a prompt and change model/mode on the child. 4) Switch the visible workspace, return to the child, and read the marker. 5) Trigger the previously granted tool and verify confirmation is requested. 6) Reopen the source. 7) Restart the app and inspect both sessions. 8) Open the running conversation overflow menu. 9) Fork the Temporary conversation and invoke a workspace-required tool.</li><li><strong>Expected</strong>: A localized branch title appears in the same project group and is activated with composer focus. Its visible active transcript and durable project/provider/model/mode/thinking/permission configuration match the source snapshot, but regenerate pager history is absent. Child messages and later configuration changes do not affect the source; both survive restart. The running source action is disabled. No turns, notifications, artifacts, permission grants, revisions, or scratch files are copied. The marker resolves under the child&#39;s inherited project; the Temporary child remains path-less and returns <code>WORKSPACE_REQUIRED</code>.</li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code>, <code>03-runtime/04-data-storage.md</code>, <code>03-runtime/06-host-rpc-protocol.md</code>, <code>04-ux/01-ui-ia.md</code>, <code>04-ux/08-component-spec.md</code>, <code>04-ux/09-interaction-patterns.md</code></li><li><strong>Acceptance</strong>: C (sessions), D (workspace), F (persistence), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>sessions::tests::fork_session_clones_active_transcript_and_configuration</code>, <code>session-fork.test.mjs</code>); full restart UI scenario Draft</li></ul><h4 id="e2e-071-fork-an-assistant-response-without-changing-its-source" tabindex="-1">E2E-071: Fork an assistant response without changing its source <a class="header-anchor" href="#e2e-071-fork-an-assistant-response-without-changing-its-source" aria-label="Permalink to &quot;E2E-071: Fork an assistant response without changing its source&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: An idle conversation contains two completed user/assistant exchanges and the second assistant response has cache-token usage metadata.</li><li><strong>Steps</strong>: 1) Hover the first assistant response and inspect its toolbar. 2) Click Fork. 3) Confirm the activated child ends at that response and append a prompt. 4) Reopen the source and inspect it. 5) Append a prompt to the child, restart, and inspect source and child. 6) Repeat while the source is running.</li><li><strong>Expected</strong>: The completed-assistant toolbar contains Copy, Fork, and Regenerate only — no Delete and no Edit (D137 moved Edit to user turns). Fork is disabled during a source turn. It activates a separately titled session whose history stops at the selected response; later source turns are absent. Source text, version history, token metadata, later turns, runtime, and cache state remain unchanged. Continuing the child affects only that child and reseeds from its own remapped transcript.</li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code>, <code>03-runtime/04-data-storage.md</code>, <code>03-runtime/06-host-rpc-protocol.md</code>, <code>04-ux/08-component-spec.md</code>, <code>08-meta/decisions-log.md</code> (D134, D137)</li><li><strong>Acceptance</strong>: C (chat stream/sessions), F (persistence), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>sessions::tests::message_scoped_fork_stops_at_selected_assistant_response</code>, <code>session-fork.test.mjs</code>, <code>transcript-style.test.mjs</code>); full restart UI scenario Draft</li></ul><h4 id="e2e-071b-forked-session-retains-messages-after-the-first-ai-turn-completes" tabindex="-1">E2E-071b: Forked session retains messages after the first AI turn completes <a class="header-anchor" href="#e2e-071b-forked-session-retains-messages-after-the-first-ai-turn-completes" aria-label="Permalink to &quot;E2E-071b: Forked session retains messages after the first AI turn completes&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A completed conversation with at least two user/assistant exchanges. The session history window feature is active (bounded loading).</li><li><strong>Steps</strong>: 1) Fork the conversation from the sidebar overflow menu. 2) In the forked session, send a new prompt and wait for the AI to finish responding. 3) Observe the transcript after agent_end fires. 4) Click the forked session entry in the sidebar (re-select). 5) Restart the app and reopen the fork.</li><li><strong>Expected</strong>: All forked messages plus the new user prompt and AI response remain visible after agent_end. Re-selecting the session from the sidebar shows the same messages (no flash of empty state). After restart, messages persist. The session history window is <code>{ messageStart: 0, hasMoreBefore: false }</code>.</li><li><strong>Specs linked</strong>: <code>03-runtime/04-data-storage.md</code>, <code>04-ux/01-ui-ia.md</code></li><li><strong>Acceptance</strong>: C (chat stream), F (persistence), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>session-fork.test.mjs</code><code>fork actions commit the child through one durable helper</code>); UI scenario Draft</li></ul><h4 id="e2e-071c-a-branch-survives-a-navigation-that-lands-during-the-fork" tabindex="-1">E2E-071c: A branch survives a navigation that lands during the fork <a class="header-anchor" href="#e2e-071c-a-branch-survives-a-navigation-that-lands-during-the-fork" aria-label="Permalink to &quot;E2E-071c: A branch survives a navigation that lands during the fork&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A completed conversation with several exchanges, and at least one other session in the sidebar.</li><li><strong>Steps</strong>: 1) Start Branch from here on the source session. 2) While the fork request is in flight, immediately click another session in the sidebar (or press the history-back shortcut). 3) Wait for both to settle. 4) Inspect the sidebar. 5) Open the branch and scroll its transcript.</li><li><strong>Expected</strong>: The later navigation wins the view, and the branch is still listed in the sidebar with its title, with no manual refresh. Opening it shows its complete copied transcript from cache. Nothing is lost and no duplicate branch row appears if the fork is repeated.</li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code>, <code>03-runtime/04-data-storage.md</code></li><li><strong>Acceptance</strong>: C (chat stream), F (persistence), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>session-fork.test.mjs</code><code>a fork is recorded even when a newer navigation took over</code>); UI scenario Draft</li></ul><h4 id="e2e-071d-opening-a-long-session-stays-responsive-and-shows-its-newest-turn" tabindex="-1">E2E-071d: Opening a long session stays responsive and shows its newest turn <a class="header-anchor" href="#e2e-071d-opening-a-long-session-stays-responsive-and-shows-its-newest-turn" aria-label="Permalink to &quot;E2E-071d: Opening a long session stays responsive and shows its newest turn&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: One session whose transcript is far longer than the renderer page size (several hundred messages, including large tool results).</li><li><strong>Steps</strong>: 1) Open the long session from the sidebar. 2) Observe the first painted frame and the scroll position. 3) Scroll to the top edge to page older history, repeatedly, until the first message is reached. 4) Switch to another session and back, recording the frame and offset on return. 5) Scroll up a measured amount, switch away, and switch back again. 6) Send a new prompt and let it complete.</li><li><strong>Expected</strong>: The session opens at its newest turn without a blank or top-of-history frame, and opening it does not visibly slow down as the conversation grows. Each older page prepends without moving the message the user is reading. Paging back reaches the true first message with none skipped or duplicated. Re-selection paints from the retained pane: the first frame back is the same frame that was left, at the same scroll position, with no dim, no blank or skeleton frame, and no rebuild of the rows. A pane the user had scrolled up in returns to that measured offset rather than the bottom, and a pane left pinned re-anchors to the bottom. The new turn appends normally.</li><li><strong>Specs linked</strong>: <code>03-runtime/04-data-storage.md</code>, <code>03-runtime/06-host-rpc-protocol.md</code>, ADR 0137</li><li><strong>Acceptance</strong>: C (chat stream), F (persistence), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>transcripts::tests::layout_window_reads_only_the_requested_tail</code>, <code>sessions::tests::bounded_reads_use_physical_line_positions_not_the_dedup_counter</code>); UI scenario Draft</li></ul><h4 id="e2e-session-list-refresh-keeps-desktop-responsive-large-session-list-refreshes-keep-electron-responsive" tabindex="-1">E2E-SESSION-list-refresh-keeps-desktop-responsive: Large session-list refreshes keep Electron responsive <a class="header-anchor" href="#e2e-session-list-refresh-keeps-desktop-responsive-large-session-list-refreshes-keep-electron-responsive" aria-label="Permalink to &quot;E2E-SESSION-list-refresh-keeps-desktop-responsive: Large session-list refreshes keep Electron responsive&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A built Electron desktop, the bundled models.dev catalog, and a fresh temporary profile with no configured providers. The probe uses only a synthetic <code>authKind: none</code> provider and never starts an Agent turn.</li><li><strong>Steps</strong>: 1) Create 800 empty durable sessions through the Rust Host API, distributed over up to thirteen known model IDs. 2) After fixture creation, request eight session lists concurrently through the renderer preload bridge. 3) Measure Electron Main timer gaps and renderer-to-Main version IPC latency during those reads. 4) Compare all returned session IDs and capability fields.</li><li><strong>Expected</strong>: Every list contains all fixture sessions with stable model and capability fields. Main remains responsive: no measured timer gap or version IPC round trip reaches one second. The probe records individual list/heartbeat durations and the largest Main gap. The ordinary sandboxed boot, platform window, and menu assertions still pass. The profile is discarded afterwards; existing user profiles and running desktop processes are untouched.</li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code>, <code>03-runtime/13-model-catalog-and-selection.md</code>, ADR 0134</li><li><strong>Acceptance</strong>: C (sessions), Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Automated (<code>scripts/e2e-electron-boot.mjs</code> via <code>pnpm test:e2e:boot</code>, using the existing <code>PI_DESKTOP_BOOT_PROBE</code> entry point).</li></ul><h4 id="e2e-071e-regenerating-from-a-paged-back-transcript-replaces-the-right-turn" tabindex="-1">E2E-071e: Regenerating from a paged-back transcript replaces the right turn <a class="header-anchor" href="#e2e-071e-regenerating-from-a-paged-back-transcript-replaces-the-right-turn" aria-label="Permalink to &quot;E2E-071e: Regenerating from a paged-back transcript replaces the right turn&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A session long enough that opening it loads only a bounded window, with several completed exchanges above that window.</li><li><strong>Steps</strong>: 1) Open the session and page older history until an earlier user turn is visible. 2) Regenerate that turn (or edit and resend it). 3) Wait for the new answer. 4) Walk the revision pager back to the original. 5) Reopen the session.</li><li><strong>Expected</strong>: Exactly the selected turn and its answer tail are replaced; no unrelated earlier or later exchange is truncated or archived. The pager restores the original tail. After reopening, the transcript matches what was shown, with no missing messages.</li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code>, <code>03-runtime/04-data-storage.md</code></li><li><strong>Acceptance</strong>: C (chat stream), F (persistence), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>transcript-truncation.test.ts</code>, <code>transcript-style.test.mjs</code>); UI scenario Draft</li></ul><h4 id="e2e-071f-long-transcript-scrolling-and-minimap-hover-stay-smooth" tabindex="-1">E2E-071f: Long-transcript scrolling and minimap hover stay smooth <a class="header-anchor" href="#e2e-071f-long-transcript-scrolling-and-minimap-hover-stay-smooth" aria-label="Permalink to &quot;E2E-071f: Long-transcript scrolling and minimap hover stay smooth&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: One session with several hundred messages, including large tool results and at least one code block, so the minimap rail shows a dense dash stack.</li><li><strong>Steps</strong>: 1) Open the session. 2) Scroll continuously through the whole transcript, up and down. 3) Sweep the cursor slowly along the minimap rail from top to bottom and back. 4) Hover a dash until its preview popover appears, then click it. 5) Resize the window and repeat the rail sweep. 6) Type a long multi-line draft so the composer grows, then sweep the rail again. 7) Switch to another session and back.</li><li><strong>Expected</strong>: Scrolling holds a steady frame rate with no progressive slowdown as more history is traversed. The rail sweep magnifies dashes smoothly and does not degrade as the dash count grows; the dash stack never shifts vertically while magnifying. The popover names the correct turn and clicking scrolls to it. After a window resize, and after the composer grows under a multi-line draft, magnification still tracks the cursor against the dashes&#39; new positions rather than their old ones. Returning to the session reveals its retained pane at the position it was left, with no rebuild of the earlier history and no dim. Switching back and forth repeatedly, the transcript text never jumps or scrolls up and down after the first painted frame. Scrolling up keeps the chosen position across later switches instead of being pulled back to the bottom.</li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code>, ADR 0137</li><li><strong>Acceptance</strong>: C (chat stream), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>interaction-performance.test.mjs</code><code>minimap hover magnification never measures geometry per dash</code>, <code>session switch bounds the first transcript commit instead of rebuilding it</code>, <code>session-switch hydration expands without moving the transcript</code>, <code>minimap re-measures dash centers when the rail&#39;s own box changes</code>); UI scenario Draft</li></ul><h4 id="e2e-071g-retained-session-panes-are-bounded-and-evict-the-oldest" tabindex="-1">E2E-071g: Retained session panes are bounded and evict the oldest <a class="header-anchor" href="#e2e-071g-retained-session-panes-are-bounded-and-evict-the-oldest" aria-label="Permalink to &quot;E2E-071g: Retained session panes are bounded and evict the oldest&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: At least five sessions with distinct transcripts, each longer than one viewport, so a scroll position and a final record identify each one unambiguously.</li><li><strong>Steps</strong>: 1) Open sessions A, B, and C in that order, scrolling each one up a measured amount. 2) Switch back to A, then B, and confirm each returns to its own offset. 3) Open D and then E, so A and B fall outside the retained budget. 4) Return to A. 5) Return to E, then D, and confirm they are still warm. 6) Repeat the whole cycle once more and watch for a stuck progress track, an error toast, or a blank chat area.</li><li><strong>Expected</strong>: Only the visible pane and the two most recently visited ones are retained; visiting beyond that budget evicts the oldest pane. A warm return paints the retained frame and offset immediately. Returning to an evicted session behaves exactly like a cold open: the visible pane stays on its own session under the thin progress track, the composer is inert until the destination commits, and the destination then paints at its newest turn with no error, no empty frame, and no restored offset from before eviction. Nothing is dimmed at any point, hidden panes stay non-interactive and out of the accessibility tree, and repeated cycling neither leaks a growing number of mounted transcripts nor leaves a pane showing another session&#39;s rows.</li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code> §1.6 / §3.5 / §7, <code>04-ux/09-interaction-patterns.md</code> §5, ADR 0130, ADR 0137</li><li><strong>Acceptance</strong>: C (switch sessions), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Draft</li></ul><h4 id="e2e-071h-reopening-a-session-while-its-response-is-streaming" tabindex="-1">E2E-071h: Reopening a session while its response is streaming <a class="header-anchor" href="#e2e-071h-reopening-a-session-while-its-response-is-streaming" aria-label="Permalink to &quot;E2E-071h: Reopening a session while its response is streaming&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Session A has a completed transcript and is producing a long assistant response; at least one other session is available.</li><li><strong>Steps</strong>: 1) While A is streaming, open another session. 2) Wait long enough for A to emit several assistant or tool updates without finishing. 3) Open A again before the response ends. 4) Continue watching A until the turn completes. 5) Switch away and back once more.</li><li><strong>Expected</strong>: A&#39;s retained/live pane is revealed without a blank, skeleton, dim, or top-of-history flash. The first frame includes the latest in-memory assistant/tool tail available before the click. The delayed durable detail response may add completed rows, but it never replaces the partial reply with an older transcript; subsequent stream updates continue from the same row and the completed answer is not duplicated. A&#39;s background events do not change the other session&#39;s transcript, composer, workspace, or focus.</li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code>, <code>04-ux/09-interaction-patterns.md</code>, ADR 0137</li><li><strong>Acceptance</strong>: C (chat stream), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>session-transcript.test.mjs</code>, <code>session-switch-performance.test.mjs</code>); UI scenario Draft</li></ul><h4 id="e2e-073-icon-only-message-toolbars-and-editing-a-user-prompt" tabindex="-1">E2E-073: Icon-only message toolbars and editing a user prompt <a class="header-anchor" href="#e2e-073-icon-only-message-toolbars-and-editing-a-user-prompt" aria-label="Permalink to &quot;E2E-073: Icon-only message toolbars and editing a user prompt&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: An idle conversation contains two completed user/assistant exchanges; one user turn was sent as a slash-template invocation.</li><li><strong>Steps</strong>: 1) Hover a completed assistant row and a user row, then hover and keyboard-focus each action chip. 2) Choose Edit on the first user prompt. 3) Press Escape, reopen Edit, retry the prompt unchanged. 4) Reopen Edit, change the text, and retry with Cmd/Ctrl+Enter. 5) After the new answer completes, use the <code>current / total</code> pager to return to the original exchange and forward again. 6) Reload the session. 7) Choose Edit on the slash-command turn and inspect the seeded text. 8) Start another response and inspect its assistant toolbar while it is streaming and after it completes. 9) Try Edit while a turn is running.</li><li><strong>Expected</strong>: Every toolbar chip shows its glyph only, with the label appearing as a fully visible tooltip 8px above the chip on hover and on keyboard focus (#74); the tooltip remains fully painted when it overlaps the sidebar edge and is never occluded by the sidebar background; no chip renders caption text. Clicking an action dismisses its tooltip immediately; it does not remain visible while the action retains focus. While an assistant response is streaming, its toolbar omits Copy; after the response settles, the assistant toolbar offers Copy, Fork, Regenerate. The user toolbar offers the pager (when variants exist), Copy, Edit, Delete. Edit replaces the prompt bubble with a wider composer-matched inline editor plate filled with <code>--ds-tile-deep</code> (no outer shadow, inset focus ring) so the light plate stays distinct from the white pane without a clipped glow, with Retry and Cancel controls; Escape or Cancel restores the bubble unchanged. Retry truncates the transcript from that prompt and streams a new answer whether or not the text changed, leaving a <code>current / total</code> pager on the user turn that restores the original prompt with its full answer tail in place — surviving reload. The slash turn seeds the typed <code>/command</code> form and re-expands the template on retry. Edit is disabled while a turn is running.</li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code>, <code>03-runtime/01-ipc-protocol.md</code>, <code>03-runtime/04-data-storage.md</code>, <code>08-meta/decisions-log.md</code> (D137, D274)</li><li><strong>Acceptance</strong>: C (chat stream), F (persistence), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>transcript-style.test.mjs</code>); full UI scenario Draft</li></ul><h4 id="e2e-069-platform-specific-sidebar-header-behavior" tabindex="-1">E2E-069: Platform-specific sidebar header behavior <a class="header-anchor" href="#e2e-069-platform-specific-sidebar-header-behavior" aria-label="Permalink to &quot;E2E-069: Platform-specific sidebar header behavior&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: PI-Desktop is open with the expanded sidebar and a chat session is active.</li><li><strong>Steps</strong>: 1) Open Extensions on macOS windowed mode. 2) Inspect the expanded sidebar titlebar. 3) Confirm no PI-Desktop logo/title is visible and Collapse sidebar appears at the right of the traffic lights. 4) Enter fullscreen and inspect the same row. 5) On Windows/Linux, confirm the brand remains visible; activate it with a pointer, then with keyboard focus and Enter/Space.</li><li><strong>Expected</strong>: macOS uses one 46px row with native lights at left, a usable drag region, and an accessible Collapse button at right; the Logo/Home brand is absent in both windowed and fullscreen modes. Global search stays on the conversation topbar, shortcuts, and application menu, not the sidebar header. Windows/Linux render the canonical 20px logo beside the 15px shell name; the complete brand has a localized Home accessible name, visible hover/focus feedback, and returns the main pane to chat without clearing the active conversation or workspace. The logo itself is theme-aware: light mode shows <code>src/assets/brand/logo-light.png</code>, dark mode shows <code>src/assets/brand/logo-dark.png</code>, swapping live with <code>data-theme</code> (no reload).</li><li><strong>Specs linked</strong>: <code>04-ux/01-ui-ia.md</code>, <code>04-ux/07-ui-design-system.md</code>, <code>04-ux/08-component-spec.md</code></li><li><strong>Acceptance</strong>: Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>renderer-branding.test.mjs</code>, <code>sidebar-navigation.test.mjs</code>); rendered interaction scenario Draft</li></ul><h4 id="e2e-098-sidebar-collapse-and-expand-animate-as-a-docked-transition" tabindex="-1">E2E-098: Sidebar collapse and expand animate as a docked transition <a class="header-anchor" href="#e2e-098-sidebar-collapse-and-expand-animate-as-a-docked-transition" aria-label="Permalink to &quot;E2E-098: Sidebar collapse and expand animate as a docked transition&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: PI-Desktop is open with the expanded sidebar and an active chat session; <code>prefers-reduced-motion</code> is off.</li><li><strong>Steps</strong>: 1) Click Collapse sidebar in the expanded sidebar header (or press the sidebar toggle shortcut). 2) Watch the sidebar during collapse. 3) Confirm the main pane expands and the collapsed titlebar now shows an Expand control. 4) Press the sidebar toggle shortcut again from the collapsed state: each press must strictly alternate collapse and expand, so the second press re-expands the sidebar (regression: it must never re-collapse). 5) Collapse and re-expand once more via the shortcut, then repeat the full round trip with the pointer controls. 6) Repeat on Windows/Linux.</li><li><strong>Expected</strong>: Collapse plays the <code>sidebar-out</code> keyframe (opacity + ≤8px slide plus width/flex allocation) while the aside stays in the tree, then unmounts once the animation ends; the main pane fills the freed space continuously. Expand plays the <code>sidebar-in</code> keyframe and the controls return to the expanded header. On Windows the dock stays opaque during exit (<code>sidebar-out-windows</code>), matching the work-panel dock behavior. No layout jump precedes the animation, and focus returns to the sidebar/Expand control predictably.</li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code>, <code>04-ux/07-ui-design-system.md</code></li><li><strong>Acceptance</strong>: Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>sidebar-collapse-animation.test.mjs</code>); rendered interaction scenario Draft</li></ul><h4 id="e2e-208-collapsed-sidebar-does-not-force-a-640px-chat-band" tabindex="-1">E2E-208: Collapsed sidebar does not force a 640px chat band <a class="header-anchor" href="#e2e-208-collapsed-sidebar-does-not-force-a-640px-chat-band" aria-label="Permalink to &quot;E2E-208: Collapsed sidebar does not force a 640px chat band&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: PI-Desktop is open with an active chat session at a viewport wide enough for the default 760px chat content band; reduced motion is off; the user has not resized the band.</li><li><strong>Steps</strong>: 1) Record the width of the centered transcript or empty-home composer band with the sidebar expanded. 2) Collapse the sidebar. 3) Inspect the same content band while the dock transition runs and after it settles. 4) Expand the sidebar and inspect the return transition.</li><li><strong>Expected</strong>: The outer main pane fills the space released by the sidebar. The centered chat content band stays at its preferred 760px ceiling (or <code>min(available, preferred)</code> if the pane is narrower). It does not jump to 640px. The transcript, empty-home stack, and Composer share that envelope.</li><li><strong>Specs linked</strong>: <code>04-ux/01-ui-ia.md</code>, <code>04-ux/07-ui-design-system.md</code>, <code>04-ux/08-component-spec.md</code>, ADR 0277, D439</li><li><strong>Acceptance</strong>: Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>sidebar-collapse-animation.test.mjs</code>, <code>chat-content-width.test.mjs</code>); rendered interaction scenario Draft</li></ul><h4 id="e2e-chat-content-width-handles-dual-edge-handles-resize-the-centered-chat-band" tabindex="-1">E2E-CHAT-content-width-handles: Dual edge handles resize the centered chat band <a class="header-anchor" href="#e2e-chat-content-width-handles-dual-edge-handles-resize-the-centered-chat-band" aria-label="Permalink to &quot;E2E-CHAT-content-width-handles: Dual edge handles resize the centered chat band&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: PI-Desktop is open on chat (empty home or a transcript) at a viewport wider than 760px. Reduced motion off.</li><li><strong>Steps</strong>: 1) Confirm no divider is visible at rest. 2) Hover the left content edge and confirm a short faint capsule, then the right edge. 3) Drag the right handle outward and confirm both edges move, the composer matches, and both capsules lengthen slightly while dragging. 4) Open the work panel or expand the sidebar until the pane is narrower than the new preference; the band compresses without a horizontal scroll. 5) Close the panel / collapse the sidebar and confirm the preference returns. 6) Double-click a handle to restore 760px. 7) Repeat with Arrow keys on a focused handle.</li><li><strong>Expected</strong>: Default 760px. Drag floor 560px (or the pane if smaller). User bubbles stay compact. Handles stay keyboard-accessible (<code>role=&quot;separator&quot;</code>). Preference persists as <code>chatContentMaxWidth</code>.</li><li><strong>Specs linked</strong>: <code>04-ux/01-ui-ia.md</code>, <code>04-ux/08-component-spec.md</code>, ADR 0277, D439</li><li><strong>Acceptance</strong>: C (conversation), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>chat-content-width.test.mjs</code>, <code>packages/shared/src/chat-content-width.test.ts</code>); rendered scenario Draft</li></ul><h4 id="e2e-ui-tooltip-never-outlives-its-trigger-a-themed-tooltip-always-retreats" tabindex="-1">E2E-UI-tooltip-never-outlives-its-trigger: A themed tooltip always retreats <a class="header-anchor" href="#e2e-ui-tooltip-never-outlives-its-trigger-a-themed-tooltip-always-retreats" aria-label="Permalink to &quot;E2E-UI-tooltip-never-outlives-its-trigger: A themed tooltip always retreats&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: PI-Desktop is open on a session with at least one message toolbar, a sidebar with two retained projects, and a window that can lose focus (another application or an OS dialog).</li><li><strong>Steps</strong>: 1) Hover an icon-only action and wait for its tooltip. 2) With the tooltip visible, move the pointer straight out of the window without clicking and then return it. 3) Hover one action, then move the pointer quickly across a row of adjacent actions. 4) Hover an action, then trigger a sidebar re-order or a project expand/collapse that moves its row in the DOM. 5) Hover an action, then press Escape. 6) Hover an action, switch to another application, then return to PI-Desktop. 7) Repeat step 1 for the project path tooltip (long absolute path), a session row&#39;s overflow control, and a message-toolbar chip.</li><li><strong>Expected</strong>: Each tooltip appears after its delay (300ms, 500ms for the project path) and retreats when the pointer leaves. No tooltip survives its trigger unmounting, a window blur, a hidden document, or Escape, and none is left painted after the pointer leaves the window entirely. Crossing adjacent actions shows at most one tooltip at a time. A move that keeps the same row element (list re-order, expand/collapse) keeps the tooltip instead of blinking it; a row that React truly unmounts and replaces drops it. placement near the top edge.</li><li><strong>Specs linked</strong>: <code>04-ux/09-interaction-patterns.md §6.4</code></li><li><strong>Acceptance</strong>: Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>icon-tooltip.test.mjs</code> source contract); rendered pointer/Escape/blur validation Draft</li></ul><h4 id="e2e-ui-row-actions-do-not-swallow-the-row-click-a-hidden-row-action-is-inert" tabindex="-1">E2E-UI-row-actions-do-not-swallow-the-row-click: A hidden row action is inert <a class="header-anchor" href="#e2e-ui-row-actions-do-not-swallow-the-row-click-a-hidden-row-action-is-inert" aria-label="Permalink to &quot;E2E-UI-row-actions-do-not-swallow-the-row-click: A hidden row action is inert&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: PI-Desktop is open with two retained projects, each with at least three sessions, and one active conversation.</li><li><strong>Steps</strong>: 1) Without hovering it, click the right-hand gutter of an idle session row where its overflow control will appear, and note which conversation opens. 2) Repeat on an idle project header row. 3) Hover a row and activate the revealed overflow control on the first click. 4) Tab through the sidebar until a row action receives focus and activate it. 5) Repeat step 1 with an emulated coarse pointer / touch device. 6) With the pointer resting on a row and its action revealed, switch focus to another application, then return without moving the pointer.</li><li><strong>Expected</strong>: The idle gutter belongs to the row — the first click there opens that conversation (a project header gutter activates and toggles that group) instead of an invisible menu, and a coarse pointer never meets a hidden control. The hover-revealed control still opens its menu on the first click, and keyboard focus reveals and keeps it operable. On window blur the row (and the project title tile) drops its hover paint and hides its revealed action; moving the pointer back over the row re-arms it.</li><li><strong>Specs linked</strong>: <code>04-ux/09-interaction-patterns.md §9.1c</code></li><li><strong>Acceptance</strong>: Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Source-contract and style assertions cover the hidden/revealed <code>pointer-events</code> states and the blur release (<code>sidebar-navigation.test.mjs</code>); rendered pointer/touch validation Draft</li><li><strong>Acceptance</strong>: Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>sidebar-navigation.test.mjs</code>, <code>interaction-polish.test.mjs</code>); rendered pointer/touch validation Draft</li></ul><h4 id="e2e-070-native-select-menus-follow-the-windows-theme-across-the-app" tabindex="-1">E2E-070: Native select menus follow the Windows theme across the app <a class="header-anchor" href="#e2e-070-native-select-menus-follow-the-windows-theme-across-the-app" aria-label="Permalink to &quot;E2E-070: Native select menus follow the Windows theme across the app&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: PI-Desktop is running on Windows with light and dark themes available.</li><li><strong>Steps</strong>: 1) In light theme, open remaining native selects (scheduled-task form) and confirm Settings pickers on General, 全局 AI, Model configuration, and Import open as in-app menus rather than platform <code>&lt;select&gt;</code> popups. 2) Repeat every remaining native list in dark theme. 3) Open each list after switching themes without restarting the app.</li><li><strong>Expected</strong>: Settings compact pickers use the shared anchored menu. Every remaining closed native trigger and opened native option list uses the active theme&#39;s readable foreground/background pairing. No dark-theme list falls back to a light Windows surface with light text, no light-theme list uses dark-theme ink, and changing theme updates subsequent openings. The same result holds for native selects outside Settings.</li><li><strong>Specs linked</strong>: <code>04-ux/06-settings-ia.md</code>, <code>04-ux/07-ui-design-system.md</code></li><li><strong>Acceptance</strong>: Quality (cross-platform theme readability)</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>settings-general.test.mjs</code>); Windows rendered scenario Draft</li></ul><h4 id="e2e-071i-long-session-opens-under-a-settle-veil-and-sends-clear-instantly" tabindex="-1">E2E-071i: Long session opens under a settle veil and sends clear instantly <a class="header-anchor" href="#e2e-071i-long-session-opens-under-a-settle-veil-and-sends-clear-instantly" aria-label="Permalink to &quot;E2E-071i: Long session opens under a settle veil and sends clear instantly&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: One session with several hundred messages including code blocks and tool results; a configured model; the host made slow (for example a throttled sidecar or a large pending tool output) so a prompt round trip takes visibly longer than a frame.</li><li><strong>Steps</strong>: 1) From the home surface, open the long session and record the first painted frames. 2) Wait for the transcript to appear. 3) Leave and re-open the same session while its history page is being revalidated. 4) Type a multi-line prompt so the composer grows, then press Enter. 5) While the host is still busy, press Enter again on the now-empty box. 6) Make the host reject a send (for example disable the model&#39;s provider) and press Enter with a new draft. 7) Open a session with fewer than fifteen messages.</li><li><strong>Expected</strong>: The first frame of the long session is an opaque skeleton of alternating user and assistant lines under the composer; no transcript text is visible during the frames in which the history expands or row heights settle, and the skeleton fades out within roughly 600ms onto a transcript already positioned at its newest turn. The rows never move up and down after the reveal. While the multi-line draft grows, the newest turn moves up with the composer instead of disappearing behind it. Re-opening during revalidation and a repeated older-page response leave exactly one row per message id, including the existing user row. Pressing Enter clears the box and shows the user row at the bottom of the transcript in the same frame, before the host has answered; when the host echo arrives the row does not duplicate or jump. The second Enter on the empty box does nothing and queues no duplicate. When the send is rejected, the user row disappears and the draft returns to the box with the caret at its end. The short session shows no skeleton.</li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code>, <code>04-ux/09-interaction-patterns.md</code>, <code>08-meta/decisions-log.md</code> (D287, D288)</li><li><strong>Acceptance</strong>: C (chat stream), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>transcript-settle.test.mjs</code>, <code>composer-send-state.test.mjs</code> <code>send clears the composer before the round trip and restores a rejected draft (D287)</code> and <code>the user row is inserted before the host round trip and echoed under the same id (D288)</code>, <code>session-transcript.test.mjs</code> (<code>repeated transcript rows keep one position and the latest value</code>); UI scenario Draft</li></ul><h4 id="e2e-072-keyboard-shortcut-mappings-persist-and-stay-conflict-safe" tabindex="-1">E2E-072: Keyboard shortcut mappings persist and stay conflict-safe <a class="header-anchor" href="#e2e-072-keyboard-shortcut-mappings-persist-and-stay-conflict-safe" aria-label="Permalink to &quot;E2E-072: Keyboard shortcut mappings persist and stay conflict-safe&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: App running on macOS and on one Windows/Linux target with Settings open; no custom shortcut overrides are stored.</li><li><strong>Steps</strong>: 1) Open Settings → Shortcuts and inspect Keyboard shortcuts. 2) Change Search to an unused modifier chord. 3) Invoke the new chord and then the old chord. 4) Attempt to assign that chord to the command shortcut (now opened via global search). 5) Attempt a bare letter and a reserved editing chord. 6) Disable Search and confirm its row shows Unbound. 7) Confirm neither the default nor custom Search chord invokes Search, then restart and check that it remains disabled. 8) Restore Search individually and confirm its default returns; choose Restore defaults and confirm all rows return to defaults. 9) On macOS inspect the corresponding native application-menu accelerator after each save/reset. 10) On Windows disable the plugin launcher and confirm its old global binding, focused fallback, and Alt+Space host fallback are all inactive. 11) Press and release Ctrl/Command alone, confirm an IME candidate, and hold the back/forward chord long enough to generate repeats. 12) With the main window focused, press the window-visibility chord <code>Alt + Shift + W</code> and confirm the window hides to the tray with no close-behaviour prompt and with the app still running; from another application, press it again and confirm the window returns and focuses. 13) Disable New task, assign its default chord to Search, then reset New task individually. Confirm the conflict error preserves both mappings and Search still opens with that chord. Repeat with New task customized instead of disabled; free the default by resetting Search, then retry New task reset. 14) Seed one profile with a stored customized <code>closeWindow</code> binding and one with a customized <code>summonWindow</code> binding; confirm each profile keeps that binding on the single toggle row after restart and that <code>Cmd/Ctrl + Shift + W</code> registers nothing.</li><li><strong>Expected</strong>: Actions are grouped as Navigation, Agent, and Window with platform-native key labels; recording has visible focus and <code>Escape</code> cancels; the custom Search chord takes effect immediately, replaces the old chord, survives restart, and updates the macOS menu; duplicate, modifier-free, and reserved assignments show an inline error without changing either action; Unbound displays as a localized explicit state, participates in no conflicts, dispatches no old or default chord, persists across restart, removes the macOS accelerator, and disables the Windows launcher fallback layers; individual reset rejects an occupied default without changing either action; conflict-free individual and global reset restore the shared defaults; Keyboard shortcuts is its own Settings destination. Modifier-only and IME keydowns dispatch nothing, and a held history chord traverses only once per physical press. The window-visibility key is one toggle on <code>Alt + Shift + W</code> — a visible, focused window hides to the tray, anything else shows and focuses — and it never enters the close path, so it raises no close-behaviour prompt and never quits; it is globally registered and deliberately avoids <code>Cmd/Ctrl + W</code>, which macOS spends on its own close-window command; the retired <code>Cmd/Ctrl + Shift + W</code> chord registers nothing, and a stored <code>closeWindow</code>/<code>summonWindow</code> override folds into the toggle (D438, D439).</li><li><strong>Specs linked</strong>: <code>04-ux/06-settings-ia.md</code>, <code>04-ux/07-ui-design-system.md</code>, <code>03-runtime/01-ipc-protocol.md</code></li><li><strong>Acceptance</strong>: F (settings persistence), Quality (keyboard accessibility)</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>keyboard-shortcuts.test.ts</code>, <code>settings-keyboard-shortcuts.test.mjs</code>, <code>window-toggle-shortcut.test.mjs</code>, host settings RPC test); rendered scenario Draft</li></ul><h4 id="e2e-073a-developer-mode-gates-the-developer-tools-console" tabindex="-1">E2E-073a: Developer mode gates the developer-tools console <a class="header-anchor" href="#e2e-073a-developer-mode-gates-the-developer-tools-console" aria-label="Permalink to &quot;E2E-073a: Developer mode gates the developer-tools console&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: App running on macOS and on one Windows/Linux target; developer mode is absent or false in persisted settings and Settings -&gt; Info is open.</li><li><strong>Steps</strong>: 1) Find the Developer card through Settings search. 2) Confirm the Open console action is disabled and invoke F12 plus the platform secondary shortcut. 3) Enable developer mode and open the console from Settings. 4) Close it and reopen it with F12; on Windows/Linux repeat with Ctrl+Shift+I, and on macOS inspect and invoke the View-menu developer-tools item. 5) Restart the app and invoke an enabled entry point. 6) Disable developer mode while the console is open. 7) Attempt the console IPC directly while disabled.</li><li><strong>Expected</strong>: No disabled entry point opens developer tools, and macOS omits the View-menu item. Enabling the persisted switch unlocks the localized Settings action and applicable platform shortcuts; each toggles the same window console after restart. Disabling the switch closes the console, disables the Settings action, removes the macOS menu item, and makes direct IPC requests fail closed.</li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code>, <code>04-ux/06-settings-ia.md</code>, <code>04-ux/08-component-spec.md</code>, <code>04-ux/09-interaction-patterns.md</code></li><li><strong>Acceptance</strong>: F (settings persistence), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>settings-general.test.mjs</code>, <code>window-menu.test.mjs</code>); native interaction scenario Draft</li></ul><h4 id="e2e-074-concurrent-session-events-and-permissions-never-steal-focus" tabindex="-1">E2E-074: Concurrent session events and permissions never steal focus <a class="header-anchor" href="#e2e-074-concurrent-session-events-and-permissions-never-steal-focus" aria-label="Permalink to &quot;E2E-074: Concurrent session events and permissions never steal focus&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Sessions A and B exist in Agent mode and can run concurrently; A is visible with a draft in its composer.</li><li><strong>Steps</strong>: 1) Start turns in A and B, then return to A. 2) Let B emit streamed messages, tool activity, completion, and a permission request. 3) Confirm A remains visible and continue editing its draft. 4) Trigger a permission request in A as well. 5) Open B explicitly, resolve only B&#39;s request, then return to A and resolve A&#39;s request. 6) Rapidly select A then B while session details load in opposite completion order. 7) While B is loading, resolve A&#39;s Write/Edit request so its tool completion records an inline review card, then let B emit a BrowserPreview artifact while A is visible. Switch back to each session.</li><li><strong>Expected</strong>: B&#39;s background events update only B&#39;s row and retained state; they do not change A&#39;s active session/project/page, transcript, draft, scroll, or keyboard focus, and no global modal appears. Opening B reveals only B&#39;s inline card with its original countdown. Both requests remain independently actionable, and resolving B does not clear A. The final rapid selection stays on B even when A&#39;s older load finishes later. Only explicit notification or session activation may navigate. A&#39;s post-approval review card is retained only in A without a transient open/close flash in B; B&#39;s BrowserPreview carries B&#39;s session identity, updates only B&#39;s retained Browser resource, and never opens, navigates, focuses, or resizes A&#39;s panel. Explicitly returning to either session restores its own open state, tabs, active tab, and Browser resource.</li><li><strong>Specs linked</strong>: <code>04-ux/01-ui-ia.md</code>, <code>04-ux/03-permission-ux.md</code>, <code>04-ux/08-component-spec.md</code>, <code>04-ux/09-interaction-patterns.md</code></li><li><strong>Acceptance</strong>: C (session isolation), E (permission isolation), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>permission-inline.test.mjs</code> for scoped state, inline rendering contract, absolute countdown, and latest-selection guard; <code>work-panel.test.mjs</code> and <code>browser-preview-tool.test.mjs</code> for session-scoped artifact retention and routing); full UI scenario Draft</li></ul><h4 id="e2e-075-sidebar-section-context-menus-create-sessions-and-projects" tabindex="-1">E2E-075: Sidebar section context menus create sessions and projects <a class="header-anchor" href="#e2e-075-sidebar-section-context-menus-create-sessions-and-projects" aria-label="Permalink to &quot;E2E-075: Sidebar section context menus create sessions and projects&quot;">​</a></h4><ul><li><strong>Status</strong>: Manual</li><li><strong>Priority</strong>: P1</li><li><strong>Covers</strong>: A, C, D / US-UI-57</li><li><strong>Preconditions</strong>: App running with the expanded home sidebar visible.</li><li><strong>Steps</strong>: <ol><li>Right-click the <code>Sessions</code> heading label (not a session row).</li><li>Choose the single create item.</li><li>Right-click empty chrome in the standalone session list.</li><li>Right-click the <code>Projects</code> heading label.</li><li>Choose the single create item and cancel or complete the project picker.</li><li>Right-click empty chrome in the project list (outside any project group).</li></ol></li><li><strong>Expected</strong>: <ul><li>Sessions context menus apply the temporary-group empty-session reuse rule and focus the composer; a new durable row is visible before any message.</li><li>Projects context menus open the same Create project dialog as the heading folder-plus control.</li><li>Existing row context menus and heading glyph buttons remain available; the section menus stay one-item and theme-matched with other sidebar menus.</li><li>Section, session-row, and project-row right-click menus open to the right of the pointer when space permits, and remain fully within the viewport at the right edge.</li><li>Escape and outside click dismiss the menu without creating anything.</li></ul></li></ul><h4 id="e2e-076-startup-splash-appears-then-yields-to-the-main-shell" tabindex="-1">E2E-076: Startup splash appears then yields to the main shell <a class="header-anchor" href="#e2e-076-startup-splash-appears-then-yields-to-the-main-shell" aria-label="Permalink to &quot;E2E-076: Startup splash appears then yields to the main shell&quot;">​</a></h4><ul><li><strong>Status</strong>: Partially automated (<code>startup-splash-motion.test.mjs</code> covers splash markup, motion tokens, reduced-motion, and catalog keys; <code>macos-sidebar-vibrancy.test.mjs</code> covers the darwin glass splash and shell cross-fade; full window timing remains Draft)</li><li><strong>Priority</strong>: P1</li><li><strong>Covers</strong>: A, Quality / US-UI shell polish</li><li><strong>Preconditions</strong>: App launch path available (dev or packaged).</li><li><strong>Steps</strong>: <ol><li>Launch PI-Desktop.</li><li>Observe the first painted renderer surface before bootstrap completes.</li><li>Wait until sessions/settings bootstrap finishes.</li><li>Repeat with OS <code>prefers-reduced-motion: reduce</code> when available.</li><li>On macOS, compare the splash surface with the sidebar glass after the shell appears.</li><li>Repeat with <code>bootstrap</code> held unanswered past the watchdog bounds, and observe the boot surface at 30s and after 180s (issue #831).</li></ol></li><li><strong>Expected</strong>: <ul><li>Before ready: full-window splash with brand mark, shell name, tagline, and accessible starting status (<code>data-testid=&quot;startup-splash&quot;</code>).</li><li>After ready: splash exits with a short fade (or instantly under reduced motion) and the main shell (or settings page) is interactive underneath.</li><li>If the initial state never arrives, the splash is replaced rather than held: the boot surface gains logs, diagnostics, and quit at 30s (<code>STARTUP_SLOW_HINT_MS</code>) without reporting a failure, and at 180s (<code>STARTUP_STALLED_MS</code>) becomes the recovery surface, which also offers a retry (<code>data-testid=&quot;startup-recovery&quot;</code>); every action works without a backend, the renderer-drawn window controls stay above the surface on Windows/Linux, and quit goes through <code>pi-desktop/app/quit</code>.</li><li>On macOS the splash uses the same glass tint and sheen as the sidebar over native <code>sidebar</code> vibrancy; the mounted shell stays hidden until the splash exit fade, then cross-fades in. Other platforms keep the opaque <code>--ds-bg-primary</code> fill.</li><li>No plain unbranded “Starting…” centered text as the only boot UI.</li><li>Overlay/dialog enter motion uses shared tokens; reduced motion keeps state changes without decorative duration.</li><li>If a non-settings bootstrap request fails while the settings read is still in flight, opening Settings still renders the loaded controls. If the settings request itself fails, the active section shows a loading/failure state and retry action rather than a blank content pane; retrying after the local service recovers restores the controls without leaving Settings.</li></ul></li><li><strong>Specs linked</strong>: <code>04-ux/07-ui-design-system.md</code> §8, <code>04-ux/02-i18n-english-first.md</code>, decisions-log D146 / D304 / D348</li><li><strong>Acceptance</strong>: A (app startup), Quality</li><li><strong>Milestone</strong>: M5</li></ul><h4 id="e2e-099-brand-logo-follows-the-active-theme" tabindex="-1">E2E-099: Brand logo follows the active theme <a class="header-anchor" href="#e2e-099-brand-logo-follows-the-active-theme" aria-label="Permalink to &quot;E2E-099: Brand logo follows the active theme&quot;">​</a></h4><ul><li><strong>Status</strong>: Draft</li><li><strong>Priority</strong>: P3</li><li><strong>Covers</strong>: Quality / US-UI shell polish</li><li><strong>Preconditions</strong>: App running; theme can switch between light and dark (and system) without restart.</li><li><strong>Steps</strong>: <ol><li>In light mode, open the app shell, an empty chat home, and the expanded sidebar (Windows/Linux) or startup splash.</li><li>Inspect the rendered <code>BrandLogo</code> source in the sidebar and startup splash, and inspect the light eight-frame <code>HomeMascotLogo</code> GIF in the empty-home hero. Hover the mascot and verify that its cadence does not change.</li><li>Switch the theme to dark (Settings → Basics → Appearance, or system appearance change).</li><li>Re-inspect the same surfaces without reloading.</li><li>Switch back to light and re-inspect.</li></ol></li><li><strong>Expected</strong>: <ul><li>Light and dark mode render <code>src/assets/brand/logo-light.png</code> / <code>src/assets/brand/logo-dark.png</code> live in the sidebar and startup splash without a window reload.</li><li>The empty-home hero renders the 100px eight-frame mascot GIF for the active theme (<code>home-mascot-light.gif</code> / <code>home-mascot-dark.gif</code>) with a short idle hold and a looping wave. Switching theme swaps the pair live without a window reload. Pointer hover does not change the cadence; under reduced motion the matching still first frame remains visible.</li><li>Sizes stay stable across theme changes (sidebar 20px, hero 100px, splash 64px), and the marks stay decorative with no click, keyboard, or focus behavior.</li></ul></li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code> §3.7, <code>04-ux/07-ui-design-system.md</code></li><li><strong>Acceptance</strong>: Quality</li><li><strong>Milestone</strong>: M5</li></ul><h4 id="e2e-077-theme-aware-selection-and-cjk-section-labels" tabindex="-1">E2E-077: Theme-aware selection and CJK section labels <a class="header-anchor" href="#e2e-077-theme-aware-selection-and-cjk-section-labels" aria-label="Permalink to &quot;E2E-077: Theme-aware selection and CJK section labels&quot;">​</a></h4><ul><li><strong>Status</strong>: Partially automated (<code>user-select.test.mjs</code>, <code>interaction-polish.test.mjs</code>)</li><li><strong>Priority</strong>: P2</li><li><strong>Covers</strong>: A, Quality / US-UI shell polish</li><li><strong>Preconditions</strong>: App running with at least one selectable transcript or input; language can be switched to <code>zh-CN</code>.</li><li><strong>Steps</strong>: <ol><li>Select text inside a transcript message or the composer.</li><li>Inspect sidebar Sessions/Projects section labels in English.</li><li>Switch the app language to <code>zh-CN</code> and re-check the same labels.</li><li>Hover jump-latest (when visible), stop, search rows, and profile menu items.</li></ol></li><li><strong>Expected</strong>: <ul><li>Selection highlight uses a neutral text-primary wash (not browser-default blue).</li><li>Caret/form accent colors stay on the monochrome token ramp.</li><li>English section labels may use uppercase + wide tracking; <code>zh-CN</code> labels use normal tracking without forced uppercase.</li><li>Listed chrome controls ease background/color changes via shared motion tokens.</li></ul></li><li><strong>Specs linked</strong>: <code>04-ux/07-ui-design-system.md</code>, <code>04-ux/08-component-spec.md</code></li><li><strong>Acceptance</strong>: D147</li><li><strong>Milestone</strong>: M5</li><li><strong>Status detail</strong>: Source-level coverage for CSS contracts; visual selection paint remains manual.</li></ul><h4 id="e2e-078-work-panel-and-settings-light-surface-polish" tabindex="-1">E2E-078: Work panel and settings light-surface polish <a class="header-anchor" href="#e2e-078-work-panel-and-settings-light-surface-polish" aria-label="Permalink to &quot;E2E-078: Work panel and settings light-surface polish&quot;">​</a></h4><ul><li><strong>Status</strong>: Partially automated (<code>surface-polish.test.mjs</code>)</li><li><strong>Priority</strong>: P2</li><li><strong>Covers</strong>: D, Quality / US-UI shell polish</li><li><strong>Preconditions</strong>: App running; theme can switch to light; a work-panel tab can be opened.</li><li><strong>Steps</strong>: <ol><li>Switch to light theme.</li><li>Open Settings and inspect form fields, toggles, segment controls, and shortcut keycaps.</li><li>Open the work panel (Review / Files / Browser) beside a chat session.</li><li>Hover file-tree rows or diff headers; focus the browser URL field.</li><li>Open a confirmation/provider dialog and inspect the scrim.</li><li>In both light and dark palettes, inspect the settings rail, search, selected item, on-state knob, composer shell, plugin/capability searches, the code card&#39;s head band, the Mermaid canvas, tool output, the composer placeholder and disabled send chip, the dialog scrim and permission backdrop, and the dock question card with its option row. Apply custom surface variables, keyboard-focus both searches, then remove the custom theme.</li></ol></li><li><strong>Expected</strong>: <ul><li>Work panel body reads as quiet <code>#fafafa</code> inset paper with a white header band.</li><li>Settings fields, browser URL, segment tracks, and shortcut keycaps use light inset fills; focused fields lift with a neutral ring.</li><li>Toggle on-state keeps a white knob on the near-black track.</li><li>Hover fills on file-tree/diff/resize ease with shared motion tokens, and the divider matches the sidebar: a 32px centered grip on direct hover/focus, not a full-height hairline; keyboard focus and an in-progress drag use the solid accent.</li><li>Light dialog scrim is softer than the dark 45% veil (~28% ink).</li><li>The dock question card paints the composer plate in both palettes — light <code>#ffffff</code> with the composer shadow, dark 96% <code>#212121</code> — and its option rows are inlaid <code>--ds-tile-deep</code> fills with no raised shadow. A custom <code>--ds-bg-composer</code> / <code>--ds-tile-deep</code> repaints both, and removing it restores the built-in paint.</li><li>Tool output keeps its cascade: light paints the same lighter tile over error output and over plain tool blocks, while dark shows the error tint and leaves plain blocks transparent.</li><li>Custom variables repaint the corresponding fills, keycap ink, and search focus states; removing them restores the built-in 8-bit RGBA paint and the existing shadows/focus rings. Prose ink mixes follow <code>--ds-text-primary</code>, and the <code>one-dark-pro</code> / <code>one-light</code> Shiki plate and its ink stay with the Shiki theme by design. This batch does not change plugin APIs.</li></ul></li><li><strong>Specs linked</strong>: <code>04-ux/07-ui-design-system.md</code>, <code>04-ux/08-component-spec.md</code></li><li><strong>Acceptance</strong>: D148</li><li><strong>Milestone</strong>: M5</li><li><strong>Status detail</strong>: <code>pnpm test:e2e:theme-surfaces</code> exercises these ordinary fills, focus states, and built-in restoration with real Chromium, production CSS, and deterministic DOM fixtures; plugin installation/lifecycle is not covered. Branch runs do not replace post-integration E2E. Other surfaces in this scenario retain manual visual checks.</li></ul><h4 id="e2e-079-user-facing-catalog-copy-in-english-and-chinese" tabindex="-1">E2E-079: User-facing catalog copy in English and Chinese <a class="header-anchor" href="#e2e-079-user-facing-catalog-copy-in-english-and-chinese" aria-label="Permalink to &quot;E2E-079: User-facing catalog copy in English and Chinese&quot;">​</a></h4><ul><li><strong>Status</strong>: Partially automated (<code>packages/i18n/test/user-facing-copy.test.mjs</code>, <code>catalogs.test.mjs</code>)</li><li><strong>Priority</strong>: P2</li><li><strong>Covers</strong>: A, Quality / US-UI copy</li><li><strong>Preconditions</strong>: App running; language can switch between English and zh-CN.</li><li><strong>Steps</strong>: <ol><li>Inspect empty-home hint, sidebar temporary-chat section, and status/connection toasts.</li><li>Open Settings → AI providers and the marketplace refresh action.</li><li>Switch the app language to zh-CN and re-check the same surfaces.</li></ol></li><li><strong>Expected</strong>: <ul><li>Copy explains outcomes in plain product language (AI provider, project, marketplace, connected/limited) rather than host/backend/repo jargon.</li><li>English and zh-CN catalogs keep identical keys and interpolation variables.</li><li>Crash chrome and empty-home titles remain catalog-backed in both locales.</li></ul></li><li><strong>Specs linked</strong>: <code>04-ux/02-i18n-english-first.md</code></li><li><strong>Acceptance</strong>: D149</li><li><strong>Milestone</strong>: M5</li><li><strong>Status detail</strong>: Source-level coverage for catalog contracts; visual wording review remains manual.</li></ul><h4 id="e2e-081-send-re-pins-transcript-and-jumps-to-bottom" tabindex="-1">E2E-081: Send re-pins transcript and jumps to bottom <a class="header-anchor" href="#e2e-081-send-re-pins-transcript-and-jumps-to-bottom" aria-label="Permalink to &quot;E2E-081: Send re-pins transcript and jumps to bottom&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Long transcript that overflows one viewport; provider configured.</li><li><strong>Steps</strong>: <ol><li>Start at the pinned bottom and use a trackpad to scroll upward with a small initial movement; observe the first movement and the jump-to-latest control.</li><li>Type a new prompt in the composer and send it.</li><li>Observe transcript position while the turn starts and streams.</li><li>Scroll upward again with a small trackpad movement while streaming, wait for more content, then click jump-to-latest.</li></ol></li><li><strong>Expected</strong>: <ul><li>The first upward movement immediately releases follow mode and remains stable; it does not snap back, reverse direction, or oscillate while a pending stream or resize follow frame completes.</li><li>New streamed content does not move the manually positioned viewport, and jump-to-latest appears as soon as follow mode is released.</li><li>On send, the transcript re-pins, hides jump-to-latest, and jumps to the bottom so the new user message (and following stream) is visible.</li><li>Streaming continues to follow while pinned.</li><li>Manual scroll mid-stream pauses follow and shows jump-to-latest again; clicking it resumes follow.</li></ul></li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code>, <code>04-ux/09-interaction-patterns.md</code></li><li><strong>Acceptance</strong>: C (chat stream), Quality / D151</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Partially automated (<code>apps/desktop/test/transcript-scroll.test.mjs</code>); full trackpad interaction remains Draft</li></ul><h4 id="e2e-082-new-reasoning-session-defaults-to-the-binding-thinking-level" tabindex="-1">E2E-082: New reasoning session defaults to the binding thinking level <a class="header-anchor" href="#e2e-082-new-reasoning-session-defaults-to-the-binding-thinking-level" aria-label="Permalink to &quot;E2E-082: New reasoning session defaults to the binding thinking level&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: The app default provider/model resolves as reasoning-capable and publishes a sparse thinking-level set with a stored binding default that is not the strongest enabled level; a second default model is non-reasoning.</li><li><strong>Steps</strong>: <ol><li>Set the reasoning-capable model as the app default and create a new session.</li><li>Inspect the Composer model × reasoning chip and the session configuration sent to the host.</li><li>Select a lower level or Off, leave the session, and reopen it.</li><li>Set the non-reasoning model as default and create another new session.</li></ol></li><li><strong>Expected</strong>: <ul><li>The first new session persists and displays the binding&#39;s stored default thinking level, clamped onto the enabled set, even when the provider returns its sparse levels out of order. It does not jump to the strongest enabled level merely because the model supports reasoning.</li><li>Reopening the first session preserves the user&#39;s later explicit selection.</li><li>The non-reasoning session starts at <code>off</code>; its combined chip keeps the Bot icon, omits level text, and its reasoning submenu exposes only Off. Missing capability metadata also falls back to <code>off</code>.</li></ul></li><li><strong>Specs linked</strong>: <code>03-runtime/13-model-catalog-and-selection.md</code>, <code>04-ux/08-component-spec.md</code>, ADR 0018, D303</li><li><strong>Acceptance</strong>: B (model config), F (persistence), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Partially automated (<code>thinking-levels.test.ts</code>, <code>thinking-ui.test.mjs</code>); full UI scenario Draft</li></ul><h4 id="e2e-083-long-streaming-turns-keep-shell-interaction-responsive" tabindex="-1">E2E-083: Long streaming turns keep shell interaction responsive <a class="header-anchor" href="#e2e-083-long-streaming-turns-keep-shell-interaction-responsive" aria-label="Permalink to &quot;E2E-083: Long streaming turns keep shell interaction responsive&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Provider configured; an active session has enough user, assistant, and tool rows to overflow several viewports; work panel can be opened; both normal and reduced-motion preferences are available.</li><li><strong>Steps</strong>: <ol><li>Start a long assistant response that emits frequent streamed updates.</li><li>While it streams, hover and focus sidebar rows, type in the composer when enabled, open/collapse the work panel, and scroll the transcript away from and back to latest.</li><li>Observe the minimap, completed history rows, composer surface, and shell chrome throughout the stream.</li><li>Navigate to Plugins or Settings and back, then repeat with reduced motion.</li></ol></li><li><strong>Expected</strong>: <ul><li>The current assistant row reveals content progressively and pinned follow stays at latest without visible oscillation.</li><li>Replaceable message/tool partials are coalesced to the next paint, while terminal, permission, planning, and error states remain immediate.</li><li>A failed tool row remains error-hued and locally expandable, but never marks the containing activity group as terminally failed. The group reports only processing duration; terminal turn styling comes from the terminal agent outcome surfaces.</li><li>Sidebar, composer, completed message/activity rows, work panel, titlebar, and global overlays do not visibly repaint or lose pointer/keyboard responsiveness for each token update.</li><li>Completed history remains in its stable render boundary while the active tail changes; history stays selectable, copyable, and anchored in the minimap without being rebuilt as a React subtree for every token.</li><li>Within that same active turn, unchanged non-delegation activity groups do not render again just because text updates rebuild turn-wide delegation maps. Tool content changes still render; later TaskWait results update the original Task group&#39;s terminal status and completion duration.</li><li>Pressing and releasing standard, icon, sidebar, send, stop, and message action controls uses one eased transform rather than a snapped scale; active streaming labels keep their readable text while their compact status markers pulse at or below 1 second; loading skeletons retain their pulse.</li><li>Minimap overflow and active-marker state remain correct without marker jitter while streamed content changes height.</li><li>Destination, panel, focus, pressed, jump, and error feedback use one short bounded transition; no composer blur trails the transcript.</li><li>The initial shell does not eagerly evaluate secondary destination modules; first navigation may show a compact localized loading indicator, then preserves normal page interaction after the local chunk resolves.</li><li>Reduced motion preserves every state change and uses instant programmatic scrolling with near-zero transition duration.</li></ul></li><li><strong>Specs linked</strong>: <code>04-ux/07-ui-design-system.md</code>, <code>04-ux/08-component-spec.md</code>, <code>04-ux/09-interaction-patterns.md</code></li><li><strong>Acceptance</strong>: C (chat stream), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>interaction-performance.test.mjs</code>); automated React/Chromium render regression via <code>pnpm test:e2e:transcript</code> (no provider credentials; requires installed Electron and a graphical session, or Xvfb on Linux). It mounts production transcript components, counts ActivityGroup renders across 20 text updates with 100 completed groups, checks changed tool content, and checks cross-part Task terminal status/timing updates. The page links the app&#39;s built stylesheet, which the runtime-status scenario below measures real geometry against; full provider streaming and shell responsiveness remain Draft.</li></ul><h4 id="e2e-chat-running-status-survives-output-pauses" tabindex="-1">E2E-CHAT-running-status-survives-output-pauses <a class="header-anchor" href="#e2e-chat-running-status-survives-output-pauses" aria-label="Permalink to &quot;E2E-CHAT-running-status-survives-output-pauses&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Production <code>ChatTranscript</code>, real store and React DOM in Electron; deterministic messages/status transitions, no provider credentials.</li><li><strong>Steps</strong>: Send a turn, stream partial text, stop delivering deltas, start and finish a tool, clear/set runtime phases, resume text, then complete, stop, or fail. Repeat in detailed and compact modes. Exercise planning, permission, question and proposal waits, history reading, and switching sessions.</li><li><strong>Expected</strong>: Exactly one localized, accessible tail status remains through output pauses and completed-tool gaps. Runtime phases take precedence over Planning/Goal or Working. Pending user interaction suppresses it; terminal turns and history reading remove it. Switching sessions uses that session&#39;s status only. Existing content and permission actions remain available. Active and assistant-error turns mount no empty action toolbar. A partial answer has 24 CSS px between its fragment box and the status label at the default font scale; settled non-error content retains Copy, Fork, and Regenerate. Check detailed and compact modes. History reading and permission cards retain normal message padding. Ending a turn keeps pinned follow at the bottom while restoring the normal toolbar height. A scrolled-up reader keeps the exact text position and scroll offset.</li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code>, <code>04-ux/09-interaction-patterns.md</code></li><li><strong>Acceptance</strong>: C (chat stream), Quality (interaction and accessibility)</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Automated by <code>pnpm test:e2e:transcript</code> (<code>scripts/e2e/transcript-status.tsx</code>). Reproduces issue #669 on upstream <code>ecece8f570fc0c18f21e4941eafe52244a7c6e5f</code>; no real-provider claim.</li></ul><h4 id="e2e-chat-initial-wait-spacing" tabindex="-1">E2E-CHAT-initial-wait-spacing <a class="header-anchor" href="#e2e-chat-initial-wait-spacing" aria-label="Permalink to &quot;E2E-CHAT-initial-wait-spacing&quot;">​</a></h4><ul><li>Send a user message before any assistant output: retain the original user action row and spacing (52px bubble-to-status in the default text fixture).</li><li>At wide and narrow transcript widths, Copy remains focusable and pointer reachable; edit/delete/revision controls retain their disabled running state. Status and action hit targets never overlap, including revision pagers.</li><li>Right-click the status/background for the conversation menu and the message for its own menu. Change runtime phase, receive first output, or stop before output: exactly one status follows the active turn and terminal actions return.</li><li>Coverage: <code>scripts/e2e/transcript-status.tsx</code>, <code>pnpm test:e2e:transcript</code>.</li></ul><h4 id="e2e-chat-runtime-status-keeps-row-position" tabindex="-1">E2E-CHAT-runtime-status-keeps-row-position <a class="header-anchor" href="#e2e-chat-runtime-status-keeps-row-position" aria-label="Permalink to &quot;E2E-CHAT-runtime-status-keeps-row-position&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: An active session whose transcript is taller than the conversation viewport, with a completed tool row owning the tail; the pane is built (<code>pnpm build:js</code>) and Electron is installed.</li><li><strong>Steps</strong>: <ol><li>Mount the production <code>ChatTranscript</code> with fixed messages, the tail owned by a completed activity group, and the session running.</li><li>Record the content height, scroller scroll height/offset, and the first and last rendered row positions with no runtime activity reported.</li><li>Switch the session&#39;s runtime activity to the waiting-for-model phase only; let the layout settle.</li><li>Clear the runtime activity again and let the layout settle.</li><li>Finish the turn (<code>isRunning</code> false) and inspect the tail.</li></ol></li><li><strong>Expected</strong>: <ul><li>The waiting row occupies the reserved status lane: the content height, scroll height, scroll offset, and the position of every already-rendered row are unchanged (within 0.01px) while Working switches to the named wait and back.</li><li>The lane stays reserved and transparent — no background, border, or shadow — in both themes. Clearing a runtime phase restores Working.</li><li>The status row keeps its live-region semantics (<code>role=&quot;status&quot;</code>, <code>aria-live=&quot;polite&quot;</code>); pending user interaction leaves the reserved lane empty.</li><li>An idle finished transcript renders no status lane at all, so its layout is unchanged.</li></ul></li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code></li><li><strong>Acceptance</strong>: C (chat stream), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>active-turn-surface.test.mjs</code>) and automated React/Chromium geometry regression via <code>pnpm test:e2e:transcript</code> (<code>scripts/e2e/transcript-render.tsx</code>, no provider credentials; requires an installed Electron and a graphical session, or Xvfb on Linux). The scenario fails by 40.125px of content height and 40px of row movement when the reserved lane is removed (issue #323).</li></ul><h4 id="e2e-stream-long-turn-keeps-realtime" tabindex="-1">E2E-STREAM-long-turn-keeps-realtime <a class="header-anchor" href="#e2e-stream-long-turn-keeps-realtime" aria-label="Permalink to &quot;E2E-STREAM-long-turn-keeps-realtime&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Provider configured; an Agent session can run a long autonomous turn with thinking, tools, and at least one subagent.</li><li><strong>Steps</strong>: <ol><li>Start a long Agent task that streams thinking and answer text at a high upstream token rate and continues through many tool rounds.</li><li>Observe streaming latency of later short thinking/answer blocks in the same turn, including a nested subagent.</li><li>Stop the turn, send a new prompt in the same session, and compare the new turn&#39;s streaming latency.</li><li>Confirm historical activity rows in the still-running turn do not flash or rebuild as the tail token updates.</li></ol></li><li><strong>Expected</strong>: <ul><li>Upstream 200+ tok/s streams stay visually caught up (batched to the display refresh is allowed; a growing backlog is not).</li><li>Later short chunks in the same long turn do not keep getting slower.</li><li>Stop plus a new prompt is not required to restore speed.</li><li>Parent and subagent streams both stay realtime.</li><li>Transcript text after <code>message_end</code> matches the streamed content.</li></ul></li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code>, <code>03-runtime/02-agent-runtime.md</code>, ADR 0242, D412, issue #299</li><li><strong>Acceptance</strong>: C (chat stream), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>message-stream.test.ts</code>, <code>stream-coalescer.test.ts</code>, <code>streaming-benchmark.test.ts</code>, <code>assistant-turns.test.mjs</code>); rendered long-turn scenario Draft</li></ul><h4 id="e2e-084-long-tool-loop-compacts-before-the-provider-context-limit" tabindex="-1">E2E-084: Long tool loop compacts before the provider context limit <a class="header-anchor" href="#e2e-084-long-tool-loop-compacts-before-the-provider-context-limit" aria-label="Permalink to &quot;E2E-084: Long tool loop compacts before the provider context limit&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Provider configured with known pi-ai context/output limits; a fixture can produce repeated tool turns and large capped tool results without finishing the agent run. Automatic protection is always on and has no settings.</li><li><strong>Steps</strong>: <ol><li>Start one agent task whose tool loop grows past the hard budget.</li><li>Let at least three <code>turn_end</code> events occur before <code>agent_end</code>; observe the composer/session controls, processing rows, transcript, and toasts.</li><li>Continue until a checkpoint is installed, then allow the task to finish.</li><li>Send further prompts until a second checkpoint is installed.</li><li>Repeat the hard-boundary turn with multiple parallel capped tool results in the compacted range.</li><li>Restart the app, reopen the session, and send a follow-up that depends on summarized old work while verifying that completed prompts are not replayed as naked historical user messages.</li><li>Repeat with a provider fixture that returns Bedrock&#39;s <code>prompt is too long: N tokens &gt; M maximum</code> once.</li><li>Run a turn where the model calls <code>new_context</code> well below the hard budget.</li><li>Invoke <code>/compact</code> manually while idle.</li></ol></li><li><strong>Expected</strong>: <ul><li>Each <code>turn_end</code> is evaluated before another provider request and never marks the overall task idle; composer/config controls remain blocked until <code>agent_end</code>, <code>error</code>, or manual-only <code>compaction_end</code>. In-run follow-up assistant turns compact through <code>prepareNextTurn</code> (pi 0.84.4+ skips that hook on a terminating turn); a new user prompt still compacts before its first provider request.</li><li>Every successful compaction adds exactly one divider row to the transcript, positioned immediately after the last message that checkpoint covers, and raises exactly one warning toast. Two checkpoints produce two rows, in order, and neither row replaces or hides a message.</li><li>The <code>new_context</code> call appears as a normal tool activity row, returns immediately, and the checkpoint is created at the following turn boundary rather than mid-turn.</li><li>Opening the context usage inspector after a checkpoint shows one line with the compaction count and the newest summary&#39;s token estimate; before any checkpoint that line is absent.</li><li>The context usage indicator exposes the same localized remaining-context summary on hover and keyboard focus, and its tooltip remains visible above the home/docked composer without clipping.</li><li>At the hard boundary a durable checkpoint is created before the next model request. The complete visible transcript is unchanged, and the continued task stays below the model-aware safe budget.</li><li>After a checkpoint the next provider request contains no assistant or tool message from before the boundary — only the summary and, while an active turn continues, its latest user message, which may carry the checkpoint-truncation marker. A completed-turn checkpoint has an empty retained tail. The request contains no tool call without its result, and expanding the original transcript rows still shows their complete persisted results.</li><li>Restart restores the summary and the recorded active/completed retention mode, and every earlier compaction row is still drawn. A regenerate/fork before a checkpoint boundary drops that record specifically; records anchored on surviving messages are preserved/remapped.</li><li>The exact provider overflow removes only the failed assistant from model context, retries once after compaction, and does not loop on a second overflow.</li><li>If automatic summary generation fails, a durable retained-tail fallback checkpoint is appended, the run stays active, and one warning explains that older model context was reduced; the transcript row for that checkpoint reads &quot;summary generation failed · recent context retained&quot;, never <code>summary ≈N tokens</code> (ADR 0282). Before that fallback, the summary request retries transient provider failures up to three times with 2s/4s/8s backoff, Stop cancels the backoff, deterministic failures do not retry, and an input whose serialized prompt exceeds the window is sent once more with tool results cut to a short prefix rather than skipping the model. If fallback persistence or the safe budget guard fails, <code>CONTEXT_COMPACTION_FAILED</code> is emitted once.</li><li>If the newest checkpoint is already the transcript leaf when a follow-up prompt crosses the hard budget, the runtime rebuilds a smaller tail from the full transcript and carries the existing summary forward instead of reporting that there is no new context to compact.</li><li>The budget reminders appear at most once each per checkpoint window, never in the transcript, and never in a persisted system prompt.</li><li>Idle <code>/compact</code> succeeds and shows its own informational toast on top of the compaction warning, because the user asked for it. Compaction failures surface once through <code>CONTEXT_COMPACTION_FAILED</code> without duplicate error toasts.</li><li>Settings contains no context-management card and Settings search returns no compaction rows.</li></ul></li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code>, <code>03-runtime/02-agent-runtime.md</code>, <code>03-runtime/03-tools-and-permissions.md</code>, <code>03-runtime/04-data-storage.md</code>, <code>03-runtime/06-host-rpc-protocol.md</code>, <code>04-ux/06-settings-ia.md</code>, <code>04-ux/08-component-spec.md</code>, <code>04-ux/09-interaction-patterns.md</code>, ADR 0030, ADR 0049, ADR 0061, ADR 0064, ADR 0136, D158, D203, D275</li><li><strong>Acceptance</strong>: C (chat/stream), F (persistence), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Partially automated (<code>runtime.test.ts</code>, <code>context-compaction.test.mjs</code>, <code>assistant-turns.test.mjs</code>, host-core transcript/session unit tests); full provider/UI journey Draft</li></ul><h4 id="e2e-agents-001-project-instruction-chain-configures-an-agent-session" tabindex="-1">E2E-AGENTS-001: Project instruction chain configures an agent session <a class="header-anchor" href="#e2e-agents-001-project-instruction-chain-configures-an-agent-session" aria-label="Permalink to &quot;E2E-AGENTS-001: Project instruction chain configures an agent session&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A project contains root <code>AGENTS.md</code>, nested <code>packages/api/AGENTS.md</code>, and a provider is configured.</li><li><strong>Steps</strong>: <ol><li>Start an Agent-mode conversation and submit a task covered by the root instruction.</li><li>Let the agent read or edit <code>packages/api/handler.ts</code>.</li><li>Add <code>packages/api/AGENTS.override.md</code>, then have the agent access another file in that directory.</li><li>Edit the root instruction while the session is idle, then submit a follow-up task.</li></ol></li><li><strong>Expected</strong>: The initial runtime receives the root chain. Before the file tool executes, the nested instruction is appended after its root source and therefore takes precedence. In one directory, <code>AGENTS.override.md</code> wins over <code>AGENTS.md</code>; <code>CLAUDE.md</code> and <code>.claude/CLAUDE.md</code> are fallback names. The idle follow-up uses changed root content rather than reusing the prior runtime. Empty, unreadable, oversized, and out-of-root instruction files do not block the turn; combined UTF-8 content is capped at 32 KiB. If path-specific resolution exceeds its two-second deadline or the host is unavailable, the file tool continues with the base chain and does not retain a sibling directory&#39;s rules. Repeated file tools in the same directory during one prompt reuse one path-resolution claim; the next prompt resolves again so changed instruction files are observed. The resolver uses the session-bound project root passed at runtime launch and does not issue a per-file <code>session.get</code> RPC.</li><li><strong>Specs linked</strong>: <code>03-runtime/02-agent-runtime.md</code></li><li><strong>Acceptance</strong>: C (chat/stream), F (persistence)</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Partially automated (<code>project-instructions.test.ts</code>, <code>runtime.test.ts</code>); full provider/UI journey Draft</li></ul><h4 id="e2e-agents-002-global-settings-and-project-menus-manage-instruction-files" tabindex="-1">E2E-AGENTS-002: Global settings and project menus manage instruction files <a class="header-anchor" href="#e2e-agents-002-global-settings-and-project-menus-manage-instruction-files" aria-label="Permalink to &quot;E2E-AGENTS-002: Global settings and project menus manage instruction files&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: PI-Desktop is running; a project can be opened.</li><li><strong>Steps</strong>: <ol><li>Open Settings -&gt; Instructions without an active project and save global content.</li><li>Start a new agent session and verify its instruction context includes the global source.</li><li>Open the Projects view and use a project&#39;s more menu to edit and save its displayed <code>AGENTS.md</code>.</li><li>Submit a prompt in a new or idle session.</li></ol></li><li><strong>Expected</strong>: The global editor targets only <code>~/.pi/agent/AGENTS.md</code>. The project editor is available only from a known project&#39;s Projects-view more menu and targets only that project&#39;s root <code>AGENTS.md</code>. Both editors show their resolved paths, preserve the typed text, and save through the dedicated IPC rather than a general file write API. Global content precedes project content in the next runtime; the saved project content follows it and takes precedence on conflicts. The project editor is a viewport-level dialog.</li><li><strong>Specs linked</strong>: <code>03-runtime/02-agent-runtime.md</code>, ADR 0037</li><li><strong>Acceptance</strong>: C (chat/stream), D (workspace), F (persistence)</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>project-instructions.test.ts</code>); UI journey Draft</li></ul><h4 id="e2e-085-expanded-sidebar-typography-keeps-list-content-compact" tabindex="-1">E2E-085: Expanded sidebar typography keeps list content compact <a class="header-anchor" href="#e2e-085-expanded-sidebar-typography-keeps-list-content-compact" aria-label="Permalink to &quot;E2E-085: Expanded sidebar typography keeps list content compact&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: The expanded sidebar contains at least one standalone session, one retained project with a session, and one empty project group; light and dark themes are available.</li><li><strong>Steps</strong>: <ol><li>Open the app at the default window width and inspect session titles, project/group titles, empty-state copy, section labels, and the footer&#39;s Settings, Plugins, and notification icons.</li><li>Switch between light and dark themes, then narrow the window to the minimum supported expanded-sidebar width.</li><li>Compare the sidebar hierarchy with 14px chat body text and inspect long session/project names.</li></ol></li><li><strong>Expected</strong>: <ul><li>Footer action icons use the shared 32px hit target and compact 14px icon sizing; Plugins sits immediately to the right of Settings.</li><li>Session titles, project/group titles, and empty-state copy use <code>--text-md</code> (13px); section labels and secondary metadata remain at <code>--text-sm</code> (12px).</li><li>The hierarchy remains readable in both themes, row pitch stays compact at approximately 28–32px, and long labels truncate without shell reflow.</li></ul></li><li><strong>Specs linked</strong>: <code>04-ux/07-ui-design-system.md</code>, <code>04-ux/08-component-spec.md</code>, D161</li><li><strong>Acceptance</strong>: Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>sidebar-navigation.test.mjs</code>); rendered visual scenario Draft</li></ul><h4 id="e2e-086-assistant-mermaid-fences-render-safely-without-blocking-streams" tabindex="-1">E2E-086: Assistant Mermaid fences render safely without blocking streams <a class="header-anchor" href="#e2e-086-assistant-mermaid-fences-render-safely-without-blocking-streams" aria-label="Permalink to &quot;E2E-086: Assistant Mermaid fences render safely without blocking streams&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A provider can stream an assistant answer containing a valid Mermaid flowchart, an invalid Mermaid fence, and ordinary fenced code; light and dark themes are available.</li><li><strong>Steps</strong>: <ol><li>Stream a valid <code>mermaid</code> fence slowly and observe it before and after the closing fence arrives.</li><li>Scroll the completed diagram into view, toggle source, copy the source, and switch between light and dark themes.</li><li>Scroll upward so transcript follow is paused, then bring another completed diagram near the viewport.</li><li>Render invalid and over-20,000-character Mermaid sources plus a payload attempting an HTML label, external image, link, or Mermaid config override.</li><li>Expand thinking content containing a <code>mermaid</code> fence.</li></ol></li><li><strong>Expected</strong>: <ul><li>The partial stream remains a normal source code block; only the complete answer fence starts rendering, and only near the viewport. Ordinary code fences and thinking Mermaid fences retain their source presentation.</li><li>The diagram uses bounded card chrome, remains within the transcript width, switches theme without stale colors, and exposes keyboard-accessible diagram/source and copy controls. Copy returns the original fence source.</li><li>Diagram height changes keep a pinned transcript at the bottom but never resume follow after the user scrolls upward.</li><li>Invalid, oversized, or unsafe input cannot fail the assistant turn, execute a link, load embedded media, add foreign HTML, or weaken strict settings; it falls back to readable copyable source when rendering is unavailable.</li></ul></li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code> §8.7, <code>04-ux/09-interaction-patterns.md</code> §2, <code>05-security/01-security.md</code> §2, D165</li><li><strong>Acceptance</strong>: C (chat &amp; stream), Security, Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>mermaid-rendering.test.mjs</code>); rendered security and visual scenario Draft</li></ul><h4 id="e2e-092-packaged-runtime-is-self-contained-without-duplicate-dependencies" tabindex="-1">E2E-092: Packaged runtime is self-contained without duplicate dependencies <a class="header-anchor" href="#e2e-092-packaged-runtime-is-self-contained-without-duplicate-dependencies" aria-label="Permalink to &quot;E2E-092: Packaged runtime is self-contained without duplicate dependencies&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Native macOS arm64 and Intel x64, Windows x64, and Linux x64 packages built from clean release-host directories; a clean application profile; English and zh-CN available; external network access can be disabled while loopback remains available; a deterministic loopback OpenAI-compatible fixture provider returns code, KaTeX, Mermaid, and a Bash command.</li><li><strong>Steps</strong>: <ol><li>Record every compressed artifact format plus the unpacked application, ASAR, Electron runtime, locale, and unpacked-native sizes on each native runner.</li><li>Inspect ASAR and resource inventories for sidecar, host, production modules, source maps, tests/examples/declarations, Chromium locales, and native prebuild targets.</li><li>On each macOS package, run <code>file</code> (or <code>lipo -info</code>) against the app executable and <code>Resources/bin/pi-desktop-host-core</code>; confirm arm64 and x86_64 packages contain only their declared architecture and that the Rust host matches the Electron app. Confirm the shared <code>apps/desktop/package.json</code> macOS configuration produces arm64 assets named <code>PI-Desktop-X.Y.Z-arm64.dmg</code> and <code>PI-Desktop-X.Y.Z-arm64-mac.zip</code>, while the Intel assets use <code>PI-Desktop-X.Y.Z-x64.dmg</code> and <code>PI-Desktop-X.Y.Z-x64-mac.zip</code>; confirm the release directory has both DMG and ZIP artifacts and one merged <code>latest-mac.yml</code> feed whose URLs and checksums match those generated assets.</li><li>Inspect the renderer output for its size controls: emitted JS is minified, no <code>.woff</code> or <code>.ttf</code> files are present, only the KaTeX <code>woff2</code> faces remain (no application font face is emitted any more), and the brand marks are the renderer-sized <code>assets/brand/logo-*.png</code> rather than the 1024px installer icons.</li><li>Configure the loopback fixture provider, disable external egress, and launch from a clean profile. Switch between English and Simplified Chinese, request the deterministic response, render common JavaScript/TypeScript, Python, Rust, shell, Mermaid, and unknown-language fences plus KaTeX and a Mermaid diagram, run the Bash fixture, and verify host and agent-sidecar health.</li><li>Confirm typography and branding survive the stripped font fallbacks: KaTeX math renders with its own faces, Chinese text in both the UI chrome and assistant output stays readable through the system CJK fallback tier, and the sidebar plus startup-splash logos render crisply on a HiDPI display.</li></ol></li><li><strong>Expected</strong>: Each macOS package contains exactly one bundled agent sidecar, one Rust host matching its declared architecture, and only configured Chromium locale packs. The release output contains both native macOS architectures, DMG/ZIP artifacts, and one merged updater feed. Each macOS DMG and ZIP carries its standard <code>-arm64</code> or <code>-x64</code> architecture marker, and no generic macOS DMG, ZIP, or blockmap remains in the release output. The per-architecture updater metadata points to those names without collisions. Renderer dependencies exist through Vite output rather than duplicate raw <code>node_modules</code>; dependency source maps, tests, examples, declarations, a second agent-runtime tree, and reliably excludable non-target native assets are absent. Curated Shiki grammars highlight locally while an unknown fence stays readable as plain text. The renderer ships minified chunks, carries no legacy <code>woff</code>/<code>truetype</code> payload, emits no application font face (only the KaTeX <code>woff2</code> faces remain), and imports only renderer-sized brand marks; math, Chinese text under the CJK fallback tier, and the chrome logos all render correctly. The offline shell starts and all fixture capabilities use local packaged assets; provider/update network failures do not block startup.</li><li><strong>Specs linked</strong>: <code>02-architecture/01-architecture.md</code>, <code>02-architecture/02-tech-stack.md</code>, <code>03-runtime/07-process-model.md</code>, <code>04-ux/02-i18n-english-first.md</code>, <code>05-security/01-security.md</code>, <code>06-delivery/06-release-runbook.md</code>, D008</li><li><strong>Acceptance</strong>: A (app startup), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>packaging-footprint.test.mjs</code> validates static dependency and builder configuration); native inventory and packaged offline launch Draft</li></ul><h4 id="e2e-093-mutating-tools-serialize-and-recover-from-stale-edit-context" tabindex="-1">E2E-093: Mutating tools serialize and recover from stale edit context <a class="header-anchor" href="#e2e-093-mutating-tools-serialize-and-recover-from-stale-edit-context" aria-label="Permalink to &quot;E2E-093: Mutating tools serialize and recover from stale edit context&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A project-bound Agent session has a writable workspace; the provider fixture can emit two same-session <code>Write</code>/<code>Edit</code> calls in one tool batch; a second edit can be given a stale <code>tag</code>; a Bash command can return a non-zero exit code with diagnostics.</li><li><strong>Steps</strong>: <ol><li>Start a task that emits two mutations for the same session while also emitting independent read/search calls.</li><li>Inspect the key tool result and transcript while the first mutation runs.</li><li>Force the second <code>Edit</code> to carry a <code>tag</code> that no longer hashes the file, with anchors that recovery cannot remap, then allow the agent to re-read the file and retry from the current contents.</li><li>Run a Bash command that exits non-zero and inspect its tool result and inline state.</li><li>Repeat with an <code>ops</code> payload whose ranges overlap.</li><li>If the task uses a dedicated worktree outside the advertised workspace, verify its guarded Bash edit and resulting <code>git diff</code>.</li></ol></li><li><strong>Expected</strong>: <ul><li>Read/search calls may overlap, but only one <code>Write</code>/<code>Edit</code> executes for a session at a time; queued mutations do not consume another global mutation slot while waiting.</li><li>The stale-tag edit fails without changing the file and returns <code>EDIT_TAG_MISMATCH</code> carrying the live tag and current content at the anchors; the overlapping-range payload fails with <code>EDIT_RANGE_INVALID</code> before any write.</li><li>The non-zero Bash command is marked failed while retaining its exit code, stdout, and stderr for the agent and diagnostics.</li><li>The retry performs one fresh read and operates on the current file; once that path has spent its recovery graces, the third counted same-path failure — or a third failed shell patch command — returns a terminating tool result plus a visible <code>MUTATION_RETRY_BUDGET_EXHAUSTED</code> row, stops the mutation workflow, and does not repeatedly modify an old patch artifact or its hunk headers.</li><li>The final file contains exactly the intended change, and diff/review data contains no partial or interleaved mutation.</li></ul></li><li><strong>Specs linked</strong>: <code>03-runtime/03-tools-and-permissions.md</code>, <code>03-runtime/06-host-rpc-protocol.md</code>, <code>03-runtime/08-error-codes.md</code></li><li><strong>Acceptance</strong>: E (tools &amp; permissions), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>tool_budget.rs</code>, <code>tools/mod.rs</code>, <code>agent-runtime/runtime.test.ts</code>); full provider/UI journey Draft</li></ul><h4 id="e2e-096-recover-transient-provider-stream-failures-in-place" tabindex="-1">E2E-096: Recover transient provider stream failures in place <a class="header-anchor" href="#e2e-096-recover-transient-provider-stream-failures-in-place" aria-label="Permalink to &quot;E2E-096: Recover transient provider stream failures in place&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A project-bound Agent session uses a deterministic provider fixture that emits a partial assistant stream, terminates once, then succeeds on the next request; a second fixture run can terminate eleven times; a third fixture returns <code>OpenAI API error (502)</code> before headers on one attempt and mid-stream on the next; a fourth fixture returns eleven consecutive 502s; a fifth fixture returns a 503 with <code>Retry-After</code>; a sixth fixture returns a pre-stream opaque 400/422 once and succeeds when the output-limit fields are omitted; the fixture supports both Chat Completions and Responses payloads and aborting after the opaque failure.</li><li><strong>Steps</strong>: <ol><li>Start an Agent turn with the one-termination fixture and observe the partial assistant response.</li><li>Wait for the bounded retry and inspect the transcript, session state, and terminal diagnostics after recovery.</li><li>Repeat with the eleven-termination fixture and inspect the terminal error message/event and its diagnostic details.</li><li>Run the mixed-phase 502 fixture and inspect the request count and terminal diagnostics for both the pre-header and the mid-stream 502.</li><li>Run the persistent eleven-502 fixture and inspect the terminal error.</li><li>Run the 503 <code>Retry-After</code> fixture and inspect the observed wait.</li><li>Run the opaque 400/422 fixture with both API styles and inspect the two request payloads, request count, and terminal diagnostics.</li><li>Abort immediately after the first opaque 400/422 failure and inspect that no repair request starts.</li><li>Reload the session and verify that only the completed response or the single terminal failed assistant remains durable.</li><li>Open Settings → AI → Defaults, enable infinite provider retry, and repeat a network fixture that fails beyond ten attempts before recovering. Stop the turn during backoff and verify no later request starts; disable the setting and verify a fresh persistent outage stops after ten retries.</li><li>Alternate one socket failure and one successful tool-call response for eleven actual Read calls, then fail once and return a final answer. Verify all twelve independent failures recover in the same user turn, each beginning at retry 1, with no repeated tool execution.</li><li>After a recovered tool response, keep the next response failing. Verify it receives ten fresh retries and terminates once with <code>retryAttempt: 10</code>. Restore the fixture, click Continue, and verify completion in the UI.</li></ol></li><li><strong>Expected</strong>: <ul><li><code>terminated</code> is classified as <code>STREAM_FAILED</code>, and an upstream gateway <code>502</code>/<code>503</code>/<code>504</code> as retryable <code>PROVIDER_ERROR</code>.</li><li>Non-429 transient failures share one bounded budget of ten retries after the initial attempt, for eleven provider attempts total, shared by request setup and stream delivery. Each retry waits for an abortable bounded backoff, removes the entire failed assistant suffix from model context, and produces no duplicate assistant bubble or terminal error notification. A retry never calls native continuation while an assistant remains the last model message.</li><li>A mid-stream 502 is retried rather than surfacing immediately. The mixed-phase fixture spends one counter across both phases and makes eleven attempts in total, not one retry per phase. A complete successful response replenishes both retry budgets for the next tool round. Headers and partial output do not. Observed waits without a <code>Retry-After</code> header are 1, 2, 4, then 8 seconds for every later retry, identical in both phases.</li><li>Only the failed request is replayed: the session, its transcript, and any completed tool call are untouched across every retry.</li><li>The recovered turn emits one terminal lifecycle and keeps the same visible assistant message id. Its terminal diagnostics retain the bounded retry outcome and attempt number.</li><li>The eleventh termination emits one terminal <code>STREAM_FAILED</code> assistant error and lifecycle event; the persistent 502 fixture emits one terminal <code>PROVIDER_ERROR</code>. Both carry <code>retryAttempt: 10</code>. Available details include phase, stream timing, and provider status, without credentials or an unrestricted provider body.</li><li>The 503 fixture waits for the server&#39;s <code>Retry-After</code> instead of the client backoff. Non-429 server and fallback waits are capped at 8 seconds.</li><li>A pre-stream 400/422 with the <code>(no body)</code> marker gets one silent repair request with <code>max_tokens</code>, <code>max_completion_tokens</code>, and <code>max_output_tokens</code> removed. The caller&#39;s payload rewrite remains active, the repair consumes no transient retry budget or backoff, and both Chat Completions and Responses fixtures complete on their second request. A second opaque failure remains terminal.</li><li>If the turn is aborted after the first opaque failure, the repair request is not started and the result is <code>aborted</code>.</li><li>A mid-stream HTTP 429 is covered by E2E-149&#39;s separate ten-retry path; the two budgets do not draw from each other.</li><li>Authentication, model-selection, context, and descriptive malformed-request failures do not enter either provider replay path. The opaque empty-body 400/422 case is the bounded repair exception described above. Infinite mode changes no classification and only removes the retry ceiling for admitted network/transient failures; it remains abortable and is visibly marked with an unbounded retry indicator.</li></ul></li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code>, <code>03-runtime/02-agent-runtime.md</code>, <code>03-runtime/08-error-codes.md</code>, <code>08-meta/decisions-log.md</code> (D186, D259, D378), ADR 0050, ADR 0128, ADR 0206</li><li><strong>Acceptance</strong>: C (chat &amp; stream), F (persistence), H (diagnostics), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Retry and successful-response budget boundaries covered by <code>provider-recovery-flow.test.ts</code> through real agent loops. Desktop socket failure, partial-stream recovery, Responses recovery, exhaustion, Continue, and eleven-tool-round recovery run in <code>scripts/e2e-provider-recovery.mjs</code>. Existing classification coverage: <code>agent-errors.test.ts</code>, <code>provider-retry.test.ts</code>, <code>runtime.test.ts</code>, <code>subagent.test.ts</code>. Other scenario variants remain Draft.</li></ul><h4 id="e2e-149-recover-provider-rate-limits-429-silently-in-place" tabindex="-1">E2E-149: Recover provider rate limits (429) silently in place <a class="header-anchor" href="#e2e-149-recover-provider-rate-limits-429-silently-in-place" aria-label="Permalink to &quot;E2E-149: Recover provider rate limits (429) silently in place&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A project-bound Agent session uses deterministic provider fixtures for a setup HTTP 429 and a mid-stream HTTP 429. Each fixture can succeed after a retry and can return eleven consecutive 429 responses. Fixtures cover <code>retry-after-ms</code>, <code>retry-after</code> seconds, and HTTP-date headers, and support aborting during the wait. A builtin subagent uses a fixture with the same responses.</li><li><strong>Steps</strong>: <ol><li>Start an Agent turn with a setup-429 fixture whose next request succeeds.</li><li>Repeat with a mid-stream-429 fixture whose next request succeeds.</li><li>Inspect the transcript, lifecycle events, request count, and terminal diagnostics for both recoveries.</li><li>Repeat with eleven consecutive 429 responses, then inspect the terminal assistant error and diagnostic details.</li><li>Start the subagent fixture, then repeat the persistent eleven-429 case.</li><li>Start another 429 turn and abort while it is waiting; inspect that no later provider request or terminal retry is started.</li><li>Repeat with authentication, model-selection, malformed-request, and context-error fixtures.</li></ol></li><li><strong>Expected</strong>: <ul><li>Every 429 is classified as retryable <code>PROVIDER_RATE_LIMITED</code> in both setup and post-start recovery, including a response body that omits rate-limit wording when the captured HTTP status is 429. Diagnostics retain <code>providerStatus: 429</code>.</li><li>Setup and mid-stream failures share one budget of ten retries after the initial attempt. A persistent fixture therefore makes eleven provider attempts, never multiplies attempts through nested pi-ai retries, and emits no intermediate assistant error, lifecycle <code>error</code>, <code>turn_end</code>, or <code>agent_end</code>. A complete successful response, including a tool-call response, resets both budgets before the next model request. Independent recovered rate limits across more than ten tool rounds must not terminate the user turn or a builtin subagent; partial output alone must not replenish either budget.</li><li>A recovered attempt removes the failed assistant from model context and reuses its visible assistant message id. The transcript has one assistant bubble and one terminal lifecycle; bounded retry diagnostics retain the phase, delay, and attempt number.</li><li>Delay precedence is <code>retry-after-ms</code>, <code>retry-after</code> seconds, HTTP-date, then exponential backoff with positive jitter. Server and fallback waits are capped at 30 seconds and the wait is abortable.</li><li>Exhaustion emits one terminal <code>PROVIDER_RATE_LIMITED</code> assistant error and lifecycle event with <code>retryAttempt: 10</code> and <code>providerStatus: 429</code>; no eleventh retry occurs after the eleven provider attempts. The structured assistant error card remains the only failure surface, exposing one localized <strong>Continue</strong> action and no <strong>Regenerate</strong> action; the generic TurnOutcomeCard is omitted. Activating <strong>Continue</strong> appends the localized continuation prompt (<code>Continue the user&#39;s unfinished task.</code> / <code>继续用户未完成的任务</code>) to the same session and starts the next turn without discarding the failed turn.</li><li>The subagent uses the same ten-retry budget and one visible child bubble; its final report is failed only after the budget is exhausted, while intermediate 429s never become a parent-visible error report.</li><li>Aborting during a backoff cancels the pending timer and starts no later provider request. Authentication, model-selection, malformed-request, and context fixtures make no automatic retry.</li><li>The 429 budget is separate from the non-429 transient budget in E2E-096. A 429 does not consume transient retries and a 502 does not consume 429 retries.</li></ul></li><li><strong>Specs linked</strong>: <code>03-runtime/02-agent-runtime.md</code> (D245, D378), <code>03-runtime/08-error-codes.md</code>, <code>08-meta/decisions-log.md</code> (D245, D378), ADR 0091, ADR 0206</li><li><strong>Acceptance</strong>: C (chat &amp; stream), H (diagnostics), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>provider-retry.test.ts</code>, <code>runtime.test.ts</code>, <code>subagent.test.ts</code>); full provider/UI journey Draft</li></ul><h2 id="_7a-m6-plan-and-shell-scenarios" tabindex="-1">7A. M6 Plan and shell scenarios <a class="header-anchor" href="#_7a-m6-plan-and-shell-scenarios" aria-label="Permalink to &quot;7A. M6 Plan and shell scenarios&quot;">​</a></h2><h4 id="e2e-104-legacy-contract-values-migrate-to-schema-v11" tabindex="-1">E2E-104: Legacy contract values migrate to schema v11 <a class="header-anchor" href="#e2e-104-legacy-contract-values-migrate-to-schema-v11" aria-label="Permalink to &quot;E2E-104: Legacy contract values migrate to schema v11&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Schema-v8 fixtures contain sessions, app defaults, and scheduled records with legacy <code>chat</code> values plus transcripts and permissions; schema-v7 and schema-v9 fixtures cover both guarded entry paths.</li><li><strong>Steps</strong>: 1) Start host-core and allow the guarded migration (v7 first reaches v8). 2) Inspect sessions, settings, scheduled modes, <code>plan_approvals</code> fields/indexes, and the exact readable v8/v9 backup. 3) Restart and inspect the same records. 4) Repeat with malformed app settings, malformed scheduled config, invalid top-level operating modes, and an unknown or wrong-platform default shell. 5) Repeat with a platform-valid persisted shell marked temporarily unavailable and nested extension <code>mode</code> fields.</li><li><strong>Expected</strong>: Every legacy mode is <code>plan</code>, Agent remains the new-session and new-task default, transcripts/permissions survive, <code>plan_approvals</code> retains approval data and has artifact/execution fields, v8→v11 is one atomic transaction after its WAL checkpoint and v8 backup, v9 and v10 create readable backups, and migration failure leaves the source schema authoritative. Every malformed or invalid fixture fails closed before schema promotion. The temporarily unavailable platform-valid shell remains persisted for runtime fallback, and nested extension modes remain unchanged.</li><li><strong>Specs linked</strong>: <code>00-baseline.md</code>, <code>03-runtime/04-data-storage.md</code>, <code>03-runtime/01-ipc-protocol.md</code>, <code>04-ux/06-settings-ia.md</code>, ADR 0053</li><li><strong>Acceptance</strong>: F (persistence), H (diagnostics)</li><li><strong>Milestone</strong>: M6</li><li><strong>Status</strong>: Automated (passed 2026-08-05): host-core 139/139, including 15 focused DB tests, covers schema-v7→v8→v11, v8→v11, v9→v11, and v10→v11 guarded paths, exact readable backups, fail-closed rollback, restart, transcript, settings, scheduled-mode, approval-field, and index tests</li></ul><h4 id="e2e-105-plan-policy-remains-host-authoritative" tabindex="-1">E2E-105: Plan policy remains host-authoritative <a class="header-anchor" href="#e2e-105-plan-policy-remains-host-authoritative" aria-label="Permalink to &quot;E2E-105: Plan policy remains host-authoritative&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A project-bound session is idle in Plan with BrowserPreview, a plugin tool, and a forged <code>requestedMode = &quot;agent&quot;</code> fixture.</li><li><strong>Steps</strong>: 1) Inspect visible Plan tools. 2) Use Read/Glob/Grep and BrowserPreview. 3) Attempt Write, Edit, plugin, and unknown tools through the host with every permission mode. 4) Run Bash under Ask, Accept edits, and Auto.</li><li><strong>Expected</strong>: Plan denies Write/Edit/plugin/unknown tools regardless of the forged mode, grants, or Auto; Bash follows the selected permission mode. The runtime remains one pi Agent and all denials are audited.</li><li><strong>Specs linked</strong>: <code>03-runtime/02-agent-runtime.md</code>, <code>03-runtime/03-tools-and-permissions.md</code>, <code>03-runtime/05-host-core-rust.md</code>, <code>03-runtime/06-host-rpc-protocol.md</code>, <code>05-security/01-security.md</code>, ADR 0053</li><li><strong>Acceptance</strong>: E (tools and permissions), Security</li><li><strong>Milestone</strong>: M6</li><li><strong>Status</strong>: Automated (passed 2026-08-04): <code>test:e2e:plan</code> plus host-core permission/policy and agent-runtime tool-composition tests</li></ul><h4 id="e2e-106-submitplan-rejects-into-editable-planning-and-resubmits-a-new-artifact" tabindex="-1">E2E-106: SubmitPlan rejects into editable planning and resubmits a new artifact <a class="header-anchor" href="#e2e-106-submitplan-rejects-into-editable-planning-and-resubmits-a-new-artifact" aria-label="Permalink to &quot;E2E-106: SubmitPlan rejects into editable planning and resubmits a new artifact&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A project-bound session is idle in Plan with a provider; <code>.pi/plan/</code> is absent or empty and the workspace permits host artifact creation.</li><li><strong>Steps</strong>: 1) Let the Agent call <code>SubmitPlan</code> with fixed title, Markdown, and question. 2) Inspect the new <code>.pi/plan/*.md</code> file byte-for-byte and the <code>plan_approvals</code> row. 3) Inspect the card&#39;s title and artifact opener; confirm the opener uses the bundled file view when it is launchable and the host file tab otherwise (D452), and that the question/description, validity/deadline, and status are absent with only Approve and Reject offered. 4) Open the approval mode menu, choose Auto, and verify the next approval defaults to Auto. 5) Reject the proposal. 6) Confirm durable mode is Plan, live state is editable <code>planning</code>, the approval gate is cleared, and a later prompt is accepted. 7) Let the Agent revise and call <code>SubmitPlan</code> once in that new turn with a complete snapshot. 8) Approve the second proposal with the remembered Auto mode.</li><li><strong>Expected</strong>: Host preserves the exact submitted Markdown bytes in a new unique artifact, records its relative path/hash/size with structured title/question, and never lets the renderer or sidecar write or replace it. The title-derived artifact filename is recognizable from the title, including non-ASCII title characters. The card shows the title and opens the artifact in the bundled file view, falling back to the host file tab when that view is not launchable (D452); it does not require inline question/Markdown/hash/size or a validity/deadline indicator. The selected approval mode is remembered locally for the next approval. Rejection is terminal for the first row, leaves durable mode Plan, and returns live state to editable planning. The later prompt/resubmission creates a second complete Markdown snapshot and a different <code>.pi/plan/*.md</code> artifact; the first artifact bytes remain unchanged. Approving the second proposal with the remembered Auto mode still changes the same Agent to Agent and queues execution.</li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code>, <code>03-runtime/02-agent-runtime.md</code>, <code>03-runtime/04-data-storage.md</code>, <code>03-runtime/06-host-rpc-protocol.md</code>, <code>04-ux/03-permission-ux.md</code>, <code>04-ux/08-component-spec.md</code>, <code>05-security/01-security.md</code>, ADR 0053</li><li><strong>Acceptance</strong>: C (conversation/stream), E (permissions), F (persistence)</li><li><strong>Milestone</strong>: M6</li><li><strong>Status</strong>: Automated (passed 2026-08-05): <code>test:e2e:plan</code> verifies the Host artifact/approval lifecycle. The optional live <code>test:e2e:plan-ui</code> case requires an env-provided OpenAI-compatible provider; the authorized run with model <code>gpt-5.6-luna</code> passed 6/6 with zero console diagnostics. It used the real controlled Composer and Send, the live Agent called <code>EnterPlanMode</code> then <code>SubmitPlan</code>, normal rendered Ask approval resolved through preload/Main, approved execution emitted the exact durable marker, and a private env-gated WeakMap check proved the same <code>DesktopAgentRuntime</code> object before and after approval. Main/Host/sidecar PIDs remained stable; credentials never entered CDP or output. The default no-key run remains 5/5 with the live case explicitly skipped.</li></ul><h4 id="e2e-chat-opaque-floating-decision-and-retry-surfaces-dock-plan-approval-and-retry-surfaces-occlude-transcript-text" tabindex="-1">E2E-CHAT-opaque-floating-decision-and-retry-surfaces: Dock, Plan approval, and retry surfaces occlude transcript text <a class="header-anchor" href="#e2e-chat-opaque-floating-decision-and-retry-surfaces-dock-plan-approval-and-retry-surfaces-occlude-transcript-text" aria-label="Permalink to &quot;E2E-CHAT-opaque-floating-decision-and-retry-surfaces: Dock, Plan approval, and retry surfaces occlude transcript text&quot;">​</a></h4><ul><li><strong>Status</strong>: Automated (<code>pnpm test:e2e:composer-occlusion</code>, <code>pnpm test:e2e:theme-surfaces</code>, <code>apps/desktop/test/plan-mode-source-contract.test.mjs</code>, <code>apps/desktop/test/active-turn-surface.test.mjs</code>)</li><li><strong>Priority</strong>: P2</li><li><strong>Covers</strong>: C, Quality / floating composer and retry surfaces</li><li><strong>Preconditions</strong>: Renderer CSS is the production source under <code>apps/desktop/src/styles</code>.</li><li><strong>Steps</strong>: <ol><li>Inspect the computed background of <code>.composer-dock-docked</code> in both built-in themes and a custom theme.</li><li>Scroll a long transcript until a row passes beneath the floating Composer.</li><li>Inspect <code>.plan-approval-bar</code> in the composer dock styles.</li><li>Inspect <code>.run-activity-error-popover.message-error</code> in the transcript styles.</li><li>Hover or focus a retrying active-turn row in a live session.</li></ol></li><li><strong>Expected</strong>: <ul><li>The dock paints the opaque <code>--ds-bg-primary</code> workspace surface across its full width. Transcript text disappears at the Composer boundary and cannot remain visible below the shell or around its rounded corners.</li><li>The Plan/Goal approval bar paints <code>--ds-bg-composer</code> with <code>--ds-shadow-composer</code> rather than the in-flow <code>--ds-tile</code> wash, so it remains a readable plate over the transparent composer dock.</li><li>The retry hover tooltip mixes the error tint over <code>--ds-bg-elevated-opaque</code>, so transcript text does not show through.</li><li>The retry tooltip is capped to the room above the tail status row and scrolls the remainder, so a long provider message keeps its first lines inside the transcript scrollport and below the conversation bar instead of being hidden by either.</li></ul></li><li><strong>Specs linked</strong>: <code>04-ux/03-permission-ux.md</code>, <code>04-ux/08-component-spec.md</code></li><li><strong>Acceptance</strong>: C, Quality</li><li><strong>Milestone</strong>: M6</li><li><strong>Status detail</strong>: Source contracts assert the CSS tokens. Live hover remains a visual check.</li></ul><h4 id="e2e-107-plan-approval-uses-one-absolute-30-minute-expiry" tabindex="-1">E2E-107: Plan approval uses one absolute 30-minute expiry <a class="header-anchor" href="#e2e-107-plan-approval-uses-one-absolute-30-minute-expiry" aria-label="Permalink to &quot;E2E-107: Plan approval uses one absolute 30-minute expiry&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A pending Plan request exists with a controllable clock.</li><li><strong>Steps</strong>: 1) Record <code>createdAt</code> and <code>expiresAt</code>. 2) Reload the renderer and reopen the request. 3) Advance time to the deadline without resolving. 4) Attempt approval after expiry.</li><li><strong>Expected</strong>: Renderer reload rehydrates only the still-pending row while the host remains alive, and the displayed countdown retains the original absolute deadline; rejected, expired, approved/completed, and interrupted terminal cards are not part of reload hydration. Expiry records <code>expired</code>, leaves the session Plan, returns <code>PLAN_APPROVAL_TIMEOUT</code>, and rejects the late response without changing mode or permission.</li><li><strong>Specs linked</strong>: <code>03-runtime/06-host-rpc-protocol.md</code>, <code>03-runtime/08-error-codes.md</code>, <code>03-runtime/10-session-state-machine.md</code>, <code>04-ux/03-permission-ux.md</code>, ADR 0053</li><li><strong>Acceptance</strong>: E (permissions), H (diagnostics), Security</li><li><strong>Milestone</strong>: M6</li><li><strong>Status</strong>: Automated (passed 2026-08-04): <code>test:e2e:plan-ui</code> covers pending renderer reload; <code>test:e2e:plan</code> and the deterministic host-core late-expiry test cover the absolute deadline, timeout persistence, and fail-closed resolution. No terminal-card reload hydration is claimed.</li></ul><h4 id="e2e-108-startup-fence-interrupts-pending-plan-work" tabindex="-1">E2E-108: Startup fence interrupts pending Plan work <a class="header-anchor" href="#e2e-108-startup-fence-interrupts-pending-plan-work" aria-label="Permalink to &quot;E2E-108: Startup fence interrupts pending Plan work&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A Plan request is pending with a live approval waiter and running planning turn; host and renderer can restart independently.</li><li><strong>Steps</strong>: 1) Reload the renderer and list the live request. 2) Restart the host/app before resolution. 3) Inspect the <code>plan_approvals</code> row, turn, and session after startup. 4) Submit the pre-restart response.</li><li><strong>Expected</strong>: Renderer reload while the host remains alive preserves the still-pending row and original deadline. Full Host/app restart transactionally marks the pending row and turn interrupted/aborted before RPC service, leaves the session Plan, and returns <code>PLAN_APPROVAL_STALE</code> for the old response. No actionable stale card or execution is restored, and the UI is not required to present the interrupted terminal snapshot after restart. No process epoch field is persisted or sent.</li><li><strong>Specs linked</strong>: <code>03-runtime/04-data-storage.md</code>, <code>03-runtime/06-host-rpc-protocol.md</code>, <code>03-runtime/07-process-model.md</code>, <code>03-runtime/10-session-state-machine.md</code>, <code>04-ux/08-component-spec.md</code>, ADR 0053</li><li><strong>Acceptance</strong>: F (persistence), H (diagnostics), Security</li><li><strong>Milestone</strong>: M6</li><li><strong>Status</strong>: Automated (passed 2026-08-04): <code>test:e2e:plan</code> performs a real Host restart and host-core recovery tests verify interrupted durable state; the pending renderer-reload assertion is covered by the E2E-107 UI lane.</li></ul><h4 id="e2e-109-approved-plan-execution-is-not-replayed-after-restart" tabindex="-1">E2E-109: Approved Plan execution is not replayed after restart <a class="header-anchor" href="#e2e-109-approved-plan-execution-is-not-replayed-after-restart" aria-label="Permalink to &quot;E2E-109: Approved Plan execution is not replayed after restart&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A Plan request has been approved with Ask and is captured once in <code>queued</code> and once in <code>running</code> state.</li><li><strong>Steps</strong>: 1) Restart the host during each state. 2) Inspect the <code>plan_approvals.execution_state</code> and turn records after startup. 3) Observe provider/tool invocations and session mode. 4) Start a new user turn explicitly.</li><li><strong>Expected</strong>: Queued/running execution fields become <code>interrupted</code>, associated turns abort, no provider/tool call is replayed, and the session remains Agent because approval already committed. A new turn is accepted only after the user starts it; no interrupted terminal card or stale action is required after restart.</li><li><strong>Specs linked</strong>: <code>03-runtime/04-data-storage.md</code>, <code>03-runtime/06-host-rpc-protocol.md</code>, <code>03-runtime/07-process-model.md</code>, <code>03-runtime/10-session-state-machine.md</code>, ADR 0053</li><li><strong>Acceptance</strong>: C (conversation/stream), F (persistence), H (diagnostics), Security</li><li><strong>Milestone</strong>: M6</li><li><strong>Status</strong>: Automated (passed 2026-08-04): <code>test:e2e:plan</code> restarts real queued and claimed executions and verifies no replay plus Agent retention</li></ul><h4 id="e2e-110-scheduled-plan-is-rejected-before-any-work" tabindex="-1">E2E-110: Scheduled Plan is rejected before any work <a class="header-anchor" href="#e2e-110-scheduled-plan-is-rejected-before-any-work" aria-label="Permalink to &quot;E2E-110: Scheduled Plan is rejected before any work&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A scheduled task is Plan and an unattended runner is available; provider, artifact, and queue writes can be observed.</li><li><strong>Steps</strong>: 1) Trigger the task through the unattended path. 2) Inspect the provider trace, <code>.pi/plan/</code>, and <code>plan_approvals</code> table. 3) Switch the task/session explicitly to Agent and run it again.</li><li><strong>Expected</strong>: Plan is rejected before provider, artifact, approval, or queue work with <code>PLAN_REQUIRES_INTERACTIVE_SESSION</code>; no background auto-approval occurs. Explicit Agent selection permits normal unattended policy.</li><li><strong>Specs linked</strong>: <code>03-runtime/04-data-storage.md</code>, <code>03-runtime/08-error-codes.md</code>, <code>04-ux/01-ui-ia.md</code>, ADR 0053</li><li><strong>Acceptance</strong>: F (persistence), H (diagnostics), Security</li><li><strong>Milestone</strong>: M6</li><li><strong>Status</strong>: Automated (passed 2026-08-04): <code>test:e2e:plan</code> verifies Plan rejection before side effects and explicit Agent execution independent of the global default</li></ul><h4 id="e2e-111-active-turn-pending-approval-and-configuration-boundaries-are-enforced" tabindex="-1">E2E-111: Active-turn, pending-approval, and configuration boundaries are enforced <a class="header-anchor" href="#e2e-111-active-turn-pending-approval-and-configuration-boundaries-are-enforced" aria-label="Permalink to &quot;E2E-111: Active-turn, pending-approval, and configuration boundaries are enforced&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A session has one active Agent turn and another session is idle; a Plan run can be made pending/queued/running.</li><li><strong>Steps</strong>: 1) Attempt a second prompt, mode/provider/model/permission/shell configuration change, and second Plan submission during the active turn. 2) Let the turn become pending approval and repeat the prompt and configuration attempts. 3) Reject the approval. 4) Submit a later prompt and let the Agent create a revised Plan snapshot. 5) Repeat configuration after the session is editable planning.</li><li><strong>Expected</strong>: Active-turn/configuration changes, prompts, and a second Plan submission fail with <code>AGENT_BUSY</code>/<code>CONFLICT</code> while the turn or active pending approval exists; only the originating session is blocked. Reject returns the durable session to Plan and the live state to planning, clears the gate, and permits the later prompt/new artifact. Terminal proposal snapshots do not disable input, the Composer mode chip, or model selection during the current renderer lifetime. Idle/planning configuration succeeds and no cross-session event or workspace root leaks.</li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code>, <code>03-runtime/02-agent-runtime.md</code>, <code>03-runtime/06-host-rpc-protocol.md</code>, <code>03-runtime/10-session-state-machine.md</code>, <code>04-ux/08-component-spec.md</code>, ADR 0053</li><li><strong>Acceptance</strong>: C (conversation/stream), E (permissions), Quality</li><li><strong>Milestone</strong>: M6</li><li><strong>Status</strong>: Automated (passed 2026-08-04): <code>test:e2e:plan</code> verifies Host boundaries and <code>test:e2e:plan-ui</code> verifies pending-only gating plus editable rejected/terminal states during the current renderer lifetime</li></ul><h4 id="e2e-111a-a-staged-mode-switch-does-not-move-the-live-planning-indicator-mid-turn" tabindex="-1">E2E-111a: A staged mode switch does not move the live planning indicator mid-turn <a class="header-anchor" href="#e2e-111a-a-staged-mode-switch-does-not-move-the-live-planning-indicator-mid-turn" aria-label="Permalink to &quot;E2E-111a: A staged mode switch does not move the live planning indicator mid-turn&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A project-bound session is running an Agent turn with a provider; the renderer shows the working indicator and no <code>Plan / planning</code> indicator.</li><li><strong>Steps</strong>: 1) While the Agent turn is still running, switch the Composer mode chip from Agent to Plan. 2) Inspect the transcript status area before the turn ends. 3) Let the turn reach its terminal event. 4) Inspect the status area again while idle, then send a new prompt.</li><li><strong>Expected</strong>: The staged Plan choice updates the chip immediately, but the live planning indicator stays away while the in-flight Agent turn runs; it does not show <code>Plan / planning</code> until the new prompt starts under the staged mode, and the Composer chip does not pulse until that live state projects <code>planning</code>. After the terminal event flushes the configuration the session is durable Plan with editable planning state, and the sent prompt surfaces the <code>Plan / planning</code> indicator in the reserved tail slot while the chip pulses. Tools and answers do not hide it; runtime phases take precedence, and pending user interaction or turn completion suppresses it.</li><li><strong>Specs linked</strong>: <code>03-runtime/02-agent-runtime.md</code>, <code>03-runtime/10-session-state-machine.md</code>, <code>04-ux/08-component-spec.md</code></li><li><strong>Acceptance</strong>: C (conversation/stream), Quality</li><li><strong>Milestone</strong>: M6</li><li><strong>Status</strong>: Draft</li></ul><h4 id="e2e-112-selectable-shell-catalog-persists-the-default" tabindex="-1">E2E-112: Selectable shell catalog persists the default <a class="header-anchor" href="#e2e-112-selectable-shell-catalog-persists-the-default" aria-label="Permalink to &quot;E2E-112: Selectable shell catalog persists the default&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Host has an available platform catalog entry, a fixture can make a persisted choice unavailable, and a project-bound Agent session is idle. The Windows lane exercises the multi-choice ordering.</li><li><strong>Steps</strong>: 1) Inspect the catalog for the platform-valid IDs <code>windows-powershell</code>, <code>windows-pwsh</code>, <code>cmd</code>, <code>git-bash</code>, and <code>bash</code>. 2) Verify settings rejects an unavailable or wrong-platform ID. 3) Select an available shell and persist <code>defaultCommandShell</code>. 4) Make that persisted choice unavailable, restart, and verify the catalog selects the first available platform shell with <code>fallback: true</code>. 5) Execute the unchanged <code>Bash</code> tool.</li><li><strong>Expected</strong>: Settings persists only a valid stable shell ID; unavailable entries remain unavailable with guidance and a later unavailable persisted choice uses the intentional first-available fallback. The host invokes the effective shell while the tool/protocol name stays <code>Bash</code>, and shell selection follows the idle configuration boundary.</li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code>, <code>03-runtime/03-tools-and-permissions.md</code>, <code>03-runtime/06-host-rpc-protocol.md</code>, <code>04-ux/06-settings-ia.md</code>, <code>04-ux/08-component-spec.md</code>, ADR 0054</li><li><strong>Acceptance</strong>: B (model/config), E (tools/permissions), F (persistence)</li><li><strong>Milestone</strong>: M6</li><li><strong>Status</strong>: Automated (passed 2026-08-04): <code>test:e2e:plan</code> verifies catalog, validation, persistence, and restart; the deterministic host-core catalog test verifies first-available fallback when a stored shell becomes unavailable</li></ul><h4 id="e2e-shell-windows-pwsh-msix-store-alias-is-available" tabindex="-1">E2E-SHELL-windows-pwsh-msix-store-alias-is-available <a class="header-anchor" href="#e2e-shell-windows-pwsh-msix-store-alias-is-available" aria-label="Permalink to &quot;E2E-SHELL-windows-pwsh-msix-store-alias-is-available&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Windows host with PowerShell 7 installed via MSIX / Microsoft Store (<code>winget</code> <code>msixbundle</code>). <code>%ProgramFiles%\\PowerShell\\7\\pwsh.exe</code> is absent. <code>pwsh.exe</code> on PATH is the <code>WindowsApps</code> app-execution alias (<code>IO_REPARSE_TAG_APPEXECLINK</code>).</li><li><strong>Steps</strong>: 1) Open Settings → command shell. 2) Inspect <code>windows-pwsh</code> availability. 3) Select PowerShell 7 and persist <code>defaultCommandShell</code>. 4) Run <code>Bash</code>.</li><li><strong>Expected</strong>: <code>windows-pwsh</code> is available and selectable. The host resolves the Store alias without treating it as missing. <code>Bash</code> launches PowerShell 7.</li><li><strong>Specs linked</strong>: <code>03-runtime/03-tools-and-permissions.md</code>, <code>04-ux/06-settings-ia.md</code>, ADR 0054, ADR 0209</li><li><strong>Acceptance</strong>: B (model/config), E (tools/permissions)</li><li><strong>Milestone</strong>: M6</li><li><strong>Status</strong>: Documented; host-core unit tests cover the Win32-attribute decision (alias attrs <code>0x420</code> + tag <code>0x8000001B</code>). Live Store-alias probe is Windows-only and is not run in Linux CI.</li></ul><h4 id="e2e-113-stale-shell-identity-fails-closed" tabindex="-1">E2E-113: Stale shell identity fails closed <a class="header-anchor" href="#e2e-113-stale-shell-identity-fails-closed" aria-label="Permalink to &quot;E2E-113: Stale shell identity fails closed&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A Bash turn has a pinned effective shell ID/dialect; the fixture can change the effective catalog selection before spawn.</li><li><strong>Steps</strong>: 1) Change the effective shell ID or dialect. 2) Execute Bash with the old expected ID. 3) Inspect process creation, fallback attempts, audit, and UI error. 4) launch a fresh turn and retry.</li><li><strong>Expected</strong>: The first call returns <code>COMMAND_SHELL_CHANGED</code>, starts no process, and does not change shell after the turn pin. The audit records the selected ID and dialect. A fresh turn snapshot is required for a later run.</li><li><strong>Specs linked</strong>: <code>03-runtime/03-tools-and-permissions.md</code>, <code>03-runtime/05-host-core-rust.md</code>, <code>03-runtime/06-host-rpc-protocol.md</code>, <code>03-runtime/08-error-codes.md</code>, <code>05-security/01-security.md</code>, ADR 0054</li><li><strong>Acceptance</strong>: E (tools/permissions), H (diagnostics), Security</li><li><strong>Milestone</strong>: M6</li><li><strong>Status</strong>: Automated (passed 2026-08-04): <code>test:e2e:plan</code> verifies stale dialect rejection before marker creation plus host-core stale ID/dialect tests</li></ul><h4 id="e2e-114-bash-streams-stdout-and-stderr-independently" tabindex="-1">E2E-114: Bash streams stdout and stderr independently <a class="header-anchor" href="#e2e-114-bash-streams-stdout-and-stderr-independently" aria-label="Permalink to &quot;E2E-114: Bash streams stdout and stderr independently&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A selected shell is available and a deterministic command writes interleaved stdout and stderr chunks.</li><li><strong>Steps</strong>: 1) Execute the command through <code>Bash</code>. 2) Observe host/RPC/UI output events. 3) Inspect the final bounded result and transcript row.</li><li><strong>Expected</strong>: stdout and stderr remain separate, ordered per tool call, and visible while the process runs. Final output preserves truncation metadata; no chunks cross sessions or turns and the Bash protocol name is unchanged.</li><li><strong>Specs linked</strong>: <code>03-runtime/06-host-rpc-protocol.md</code>, <code>03-runtime/09-logging-and-observability.md</code>, <code>03-runtime/16-tool-result-limits.md</code>, <code>04-ux/09-interaction-patterns.md</code>, ADR 0054</li><li><strong>Acceptance</strong>: C (stream), E (tools), Quality</li><li><strong>Milestone</strong>: M6</li><li><strong>Status</strong>: Automated (passed 2026-08-04): <code>test:e2e:plan</code> verifies distinct stdout/stderr notifications and final tool identity; host/runtime stream tests cover bounded accumulation and session isolation</li></ul><h4 id="e2e-115-bash-timeout-uses-60-seconds-and-a-bounded-override" tabindex="-1">E2E-115: Bash timeout uses 60 seconds and a bounded override <a class="header-anchor" href="#e2e-115-bash-timeout-uses-60-seconds-and-a-bounded-override" aria-label="Permalink to &quot;E2E-115: Bash timeout uses 60 seconds and a bounded override&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A selected shell can run a command longer than 60 seconds; host clock is observable.</li><li><strong>Steps</strong>: 1) Run without a timeout override. 2) Observe the 60-second deadline. 3) Run with an in-range override including values above 300 seconds. 4) Submit zero, negative, and over-21,600-second overrides.</li><li><strong>Expected</strong>: Missing timeout uses exactly 60 seconds and returns <code>TOOL_TIMEOUT</code> after process-tree shutdown. In-range values work within 1–21,600 seconds; out-of-range values fail validation and never spawn.</li><li><strong>Specs linked</strong>: <code>03-runtime/03-tools-and-permissions.md</code>, <code>03-runtime/06-host-rpc-protocol.md</code>, <code>03-runtime/08-error-codes.md</code>, <code>03-runtime/16-tool-result-limits.md</code>, <code>05-security/01-security.md</code>, ADR 0054, ADR 0167</li><li><strong>Acceptance</strong>: E (tools), H (diagnostics), Security</li><li><strong>Milestone</strong>: M6</li><li><strong>Status</strong>: Automated (passed 2026-08-04): long-timeout <code>test:e2e:plan</code> measured the no-override timeout at 60,024 ms and verified in-range plus invalid bounds without delayed marker writes</li></ul><h4 id="e2e-116-bash-abort-shuts-down-the-complete-process-tree" tabindex="-1">E2E-116: Bash abort shuts down the complete process tree <a class="header-anchor" href="#e2e-116-bash-abort-shuts-down-the-complete-process-tree" aria-label="Permalink to &quot;E2E-116: Bash abort shuts down the complete process tree&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A Bash command starts a child and grandchild that emit delayed output; the originating session is running.</li><li><strong>Steps</strong>: 1) Start the command. 2) Abort the active turn. 3) Inspect process descendants, output events, audit, and turn state after the shutdown grace.</li><li><strong>Expected</strong>: The process group/job tree is terminated, no descendant remains, no later output arrives, the turn returns <code>TURN_ABORTED</code>, and the workspace is not rolled back automatically.</li><li><strong>Specs linked</strong>: <code>03-runtime/03-tools-and-permissions.md</code>, <code>03-runtime/07-process-model.md</code>, <code>03-runtime/08-error-codes.md</code>, <code>03-runtime/16-tool-result-limits.md</code>, <code>05-security/01-security.md</code>, ADR 0054</li><li><strong>Acceptance</strong>: C (abort), E (tools), H (diagnostics), Security</li><li><strong>Milestone</strong>: M6</li><li><strong>Status</strong>: Automated (passed 2026-08-04): <code>test:e2e:plan</code> aborts a real descendant process tree and verifies no late marker/output; host-core tests verify cancellation registry cleanup</li></ul><h4 id="e2e-117-agent-plan-goal-ux-and-locales-contain-no-chat-controls" tabindex="-1">E2E-117: Agent/Plan/Goal UX and locales contain no Chat controls <a class="header-anchor" href="#e2e-117-agent-plan-goal-ux-and-locales-contain-no-chat-controls" aria-label="Permalink to &quot;E2E-117: Agent/Plan/Goal UX and locales contain no Chat controls&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: App can run in English and zh-CN with an idle session, Plan artifact fixture, shell settings, and global search&#39;s Commands section available.</li><li><strong>Steps</strong>: 1) Inspect Agent/Plan/Goal, permission, artifact approval, and shell controls in English. 2) Enter Plan and inspect planning/approval/queue/ terminal states while the renderer remains alive. 3) Approve and reject a proposal, confirming that the approval surface disappears after host confirmation. 4) Inspect the Commands section and confirm it contains exactly <code>builtin.session.new</code>, <code>builtin.agent.compact</code>, and the three <code>builtin.mode.*</code> commands, with only <code>/new</code>, <code>/compact</code>, <code>/agent-mode</code>, <code>/plan-mode</code>, and <code>/goal-mode</code> in the builtin <code>/</code> group. Confirm the removed command IDs and <code>newChat</code> / <code>openProject</code> / <code>openSettings</code> dispatch aliases are absent. Use <code>/plan-mode</code> / <code>/agent-mode</code> slash aliases to switch the active idle session, confirming the Composer chip changes immediately. Type a prompt after either alias in the same draft and send it; confirm the mode changes and the prompt remains as a visible user turn. Send an alias alone and confirm it remains a local mode switch without creating an empty transcript turn. If prompt dispatch fails, confirm the complete draft remains editable. 5) Reload after a terminal proposal and inspect the session while asserting that Electron Main and Host process identities did not change. 6) Repeat in zh-CN. 7) Search visible commands for the removed Chat mode and request-changes controls. Host/app restart recovery is exercised separately by E2E-108 and E2E-109.</li><li><strong>Expected</strong>: Agent is the default; the left-of-input Composer chip is the sole active-session Agent/Plan/Goal control; Plan and Goal show Ask/Accept edits/Auto, the submitted title, an artifact opener, remembered approval mode, approve/reject only, shell catalog/fallback status, and localized failed-closed states. No Chat mode, <code>/chat-mode</code>, request-changes action, inline Markdown/hash/size requirement, or stale actionable queue is exposed; terminal checkpoint metadata may remain non-actionable only for the current renderer lifetime, while the composer approval surface is removed after host-confirmed resolution. Renderer reload does not rehydrate rejected, expired, approved/completed, or interrupted terminal cards. Host/app restart does not replay work or restore stale actions, and the UI is not required to present the interrupted terminal snapshot; <code>page = &quot;chat&quot;</code> remains an internal route.</li><li><strong>Specs linked</strong>: <code>01-product/01-product-scope.md</code>, <code>04-ux/01-ui-ia.md</code>, <code>04-ux/04-builtin-commands.md</code>, <code>04-ux/03-permission-ux.md</code>, <code>04-ux/06-settings-ia.md</code>, <code>04-ux/08-component-spec.md</code>, <code>04-ux/02-i18n-english-first.md</code>, ADR 0053, ADR 0054</li><li><strong>Acceptance</strong>: C (conversation), Quality</li><li><strong>Milestone</strong>: M6</li><li><strong>Status</strong>: Automated (passed 2026-08-04): raw-CDP <code>test:e2e:plan-ui</code> uses an env-gated Electron Main probe against the existing Host, asserts stable Electron/Host PIDs, covers pending restore, live terminal controls, rejected and approved/completed terminal-card absence after renderer reload, EN/zh-CN, and 1280×800 / 900×700 rendering. E2E-108/E2E-109 cover Host restart interruption, stale-action rejection, and no replay.</li></ul><h4 id="e2e-118-a-regenerated-turn-keeps-its-final-answer-after-the-branch-archive" tabindex="-1">E2E-118: A regenerated turn keeps its final answer after the branch archive <a class="header-anchor" href="#e2e-118-a-regenerated-turn-keeps-its-final-answer-after-the-branch-archive" aria-label="Permalink to &quot;E2E-118: A regenerated turn keeps its final answer after the branch archive&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A project-bound Agent session whose transcript already has one completed exchange, a provider that streams a multi-tool turn long enough for the final assistant message to land through the persistence outbox, and read access to <code>&lt;data_dir&gt;/sessions/&lt;id&gt;.jsonl</code>, <code>&lt;id&gt;.revisions.jsonl</code>, and the <code>messages</code> index.</li><li><strong>Steps</strong>: 1) Regenerate the assistant answer so the root user turn carries <code>revisionCount</code> / <code>activeRevision</code> and revision 1 is archived. 2) Let the re-run finish a turn that ends with tool calls followed by a final assistant message. 3) Immediately after <code>agent_end</code>, inspect the transcript file, the <code>messages</code> rows, and the archived revision payload. 4) Reload the session. 5) Page the root bubble back to revision 1 and forward again.</li><li><strong>Expected</strong>: The final assistant message is present in the transcript file, in the index, and as the last message of the archived branch. Every message of the turn keeps its owning <code>turn_id</code>. The root carries <code>revisionCount = 2</code> and <code>activeRevision = 2</code>, reload shows the complete turn, and paging restores each branch whole. No <code>session.replaceMessages</code> call is made on the turn-completion path.</li><li><strong>Specs linked</strong>: <code>03-runtime/04-data-storage.md</code> §4.9/§7, <code>03-runtime/06-host-rpc-protocol.md</code> §4, ADR 0041, ADR 0060</li><li><strong>Acceptance</strong>: C (conversation), F (persistence), H (diagnostics), Quality</li><li><strong>Milestone</strong>: M6</li><li><strong>Status</strong>: Covered by host-core unit tests (2026-08-06): <code>save_active_branch_revision_keeps_a_message_appended_after_its_read</code> archives with a message appended after the read and asserts the transcript, the archived payload, and the index <code>(seq, turn_id)</code> rows; <code>replace_messages_preserves_owning_turn_ids</code> covers the remaining rewrite callers. UI paging remains manual.</li></ul><h4 id="e2e-246-retrying-a-large-session-truncates-without-a-whole-transcript-rpc" tabindex="-1">E2E-246: Retrying a large session truncates without a whole-transcript RPC <a class="header-anchor" href="#e2e-246-retrying-a-large-session-truncates-without-a-whole-transcript-rpc" aria-label="Permalink to &quot;E2E-246: Retrying a large session truncates without a whole-transcript RPC&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: An Agent session whose live transcript is thousands of messages (large enough that a <code>session.replaceMessages</code> JSON-RPC line would exceed tens of megabytes), with a completed or failed last user turn.</li><li><strong>Steps</strong>: 1) Retry or regenerate the last user prompt. 2) Inspect host RPC traffic, <code>turns.status</code>, the live jsonl, and <code>message_revisions</code>. 3) Retry again immediately if the first attempt is still on screen as running.</li><li><strong>Expected</strong>: Electron main calls <code>session.truncateFrom</code> with <code>fromMessageId</code> only — no <code>session.replaceMessages</code> and no <code>session.saveRevision</code> of the discarded array. The kept prefix never appears in an NDJSON request. The leftover running turn is <code>aborted</code> before <code>beginTurn</code>. The discarded tail is archived (refresh of the stamped variant, or a new inactive variant). A second retry while a cut is in flight does not ship another full transcript. The UI error is not <code>host RPC timeout: session.replaceMessages</code>.</li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code>, <code>03-runtime/04-data-storage.md</code> §4.9/§7, <code>03-runtime/06-host-rpc-protocol.md</code> §4, ADR 0216, D390</li><li><strong>Acceptance</strong>: C (conversation), F (persistence), Quality</li><li><strong>Milestone</strong>: M6</li><li><strong>Status</strong>: Covered by host-core unit tests (2026-09-10): <code>truncate_from_drops_the_tail_and_archives_the_discarded_branch</code>, <code>truncate_from_rejects_an_unknown_message</code>, <code>truncate_from_refreshes_the_stamped_revision</code>, <code>truncate_from_rpc_cuts_without_shipping_the_kept_prefix</code>. Desktop journey remains Draft.</li></ul><h4 id="e2e-247-windows-host-core-exits-after-stdin-eof-and-oversize-rpcs-fail-immediately" tabindex="-1">E2E-247: Windows host-core exits after stdin EOF and oversize RPCs fail immediately <a class="header-anchor" href="#e2e-247-windows-host-core-exits-after-stdin-eof-and-oversize-rpcs-fail-immediately" aria-label="Permalink to &quot;E2E-247: Windows host-core exits after stdin EOF and oversize RPCs fail immediately&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Windows host-core with the Alt+Space keyboard hook installed; a JSON-RPC request whose NDJSON line exceeds 64 MiB.</li><li><strong>Steps</strong>: 1) Send stdin EOF to a running host-core. 2) From Electron, call a host method whose stringified payload exceeds 64 MiB. 3) If a line still reaches host-core, inspect the <code>LIMIT_EXCEEDED</code> reply id.</li><li><strong>Expected</strong>: After stdin EOF, host-core exits without waiting 130 s. The Windows keyboard hook does not keep the stdout writer alive. Electron rejects the oversize call with <code>LIMIT_EXCEEDED</code> before <code>stdin.write</code>. A host-side oversize reply uses the request id peeked from the prefix, not <code>null</code>. The UI error is not <code>host RPC timeout</code>.</li><li><strong>Specs linked</strong>: <code>03-runtime/07-process-model.md</code>, <code>03-runtime/06-host-rpc-protocol.md</code> §7, ADR 0217, D391</li><li><strong>Acceptance</strong>: Quality</li><li><strong>Milestone</strong>: M6</li><li><strong>Status</strong>: Covered by unit and source-contract tests (2026-09-11): <code>start_does_not_keep_the_stdout_channel_open</code>, <code>peek_jsonrpc_id_reads_a_string_id_from_a_truncated_prefix</code>, <code>apps/desktop/test/windows-host-runtime.test.mjs</code> (weak sender), <code>apps/desktop/test/rpc-lifecycle-contract.test.mjs</code> (client precheck), <code>packages/shared/src/rpc-limits.test.ts</code>. Desktop journey remains Draft.</li></ul><h4 id="e2e-119-parallel-subagents-report-back-without-entering-the-parent-s-context" tabindex="-1">E2E-119: Parallel subagents report back without entering the parent&#39;s context <a class="header-anchor" href="#e2e-119-parallel-subagents-report-back-without-entering-the-parent-s-context" aria-label="Permalink to &quot;E2E-119: Parallel subagents report back without entering the parent&#39;s context&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A project-bound Agent session with the user home containing <code>~/.agents/subagents/scout.md</code> (read-only, no <code>tools</code> key), <code>~/.agents/subagents/fixer.md</code> (<code>tools: Read, Edit</code>), <code>~/.agents/subagents/pinned.md</code> (<code>model:</code> naming a second configured provider) and <code>~/.agents/subagents/broken.md</code> (missing <code>name</code>); a provider whose stream can be driven to emit two <code>Task</code> calls in one assistant message; permission mode <code>ask</code> so a delegate&#39;s <code>Edit</code> is gated; read access to <code>&lt;data_dir&gt;/sessions/&lt;id&gt;.jsonl</code> and the <code>messages</code> index.</li><li><strong>Steps</strong>: <ol><li>Prompt a turn in which the assistant emits two <code>Task</code> calls — <code>scout</code> and <code>pinned</code> — in one message. Observe the delegation card while both run and after each one settles; collapse it, then expand each node.</li><li>Prompt a turn whose assistant message emits a single <code>Task</code> call — <code>scout</code> — and compare its presentation with step 1&#39;s.</li><li>Prompt a turn in which two <code>fixer</code> delegates each edit a different file, and answer only the first permission card.</li><li>Answer the second card, then prompt a third turn where two <code>fixer</code> delegates edit the <strong>same</strong> file.</li><li>Start a fan-out and press Stop while one card is on screen and another is queued.</li><li>Prompt a <code>Task</code> call naming <code>broken</code>, then one naming an agent that does not exist, then one whose definition pins an unconfigured provider.</li><li>Switch the session to Plan, then to Goal, and inspect the tool catalog.</li><li>Reload the session and re-expand the delegation card and every <code>Task</code> node.</li></ol></li><li><strong>Expected</strong>: <ul><li>Both delegates in step 1 run concurrently, and <code>pinned</code> streams on its own provider/model while the parent keeps the session&#39;s.</li><li>The two <code>Task</code> calls in step 1 form one full-width delegation card. While active it opens once and its header updates the subagent and settled counts; after settlement it keeps the user&#39;s expansion choice and reports aggregate success, warning, or issue state plus elapsed time.</li><li>The lone <code>Task</code> in step 2 draws the same card with a single delegate node — same root, connector, outcome, runtime and step count — and never the compact one-line tool row (D265). Its aggregate line is worded for one subagent, so no locale reads &quot;1 个 Subagents&quot; or &quot;Subagents working&quot;.</li><li>The expanded card shows one main-agent root connected to <code>scout</code> and <code>pinned</code> in parent-row order, with no invented edge between delegates. Each node shows its agent, short description, explicit outcome, duration and step count. Expanding a node shows the brief, report exactly once, and <code>status</code>/<code>turns</code>/<code>toolCalls</code>. Delegate rows appear only inside that node, never in the turn stream or the minimap.</li><li>If the parent keeps working after those <code>Task</code> calls — thinking, <code>Read</code>, <code>Grep</code>, or a lifecycle row — that work is a separate processing group, not rows inside the delegation card (D319). The card&#39;s tile, “Subagent working” header, and topology canvas contain only the <code>Task</code> nodes.</li><li>The parent&#39;s next request contains the reports and <strong>no</strong> delegate message or tool row; the rows are nonetheless present in the transcript file and the index with <code>meta.parentToolCallId</code> and <code>meta.agentName</code>.</li><li>Only the head permission card is rendered; it names the asking delegate and the number waiting behind it. Answering it reveals the next card, and neither answer resolves the other request.</li><li><code>scout</code> cannot call <code>Edit</code> or <code>Write</code> at all; <code>fixer</code> can. Same-file edits in step 4 apply in a defined order and neither loses the other&#39;s write.</li><li>Stop denies both the shown and the queued request, and both delegates end <code>aborted</code> in text and icon inside their own <code>Task</code> nodes — the parent turn ends once and the aggregate card settles with a warning.</li><li><code>broken</code> is absent from the catalog with a launch diagnostic and the session keeps its other three delegates; an unknown agent and an unresolvable model pin each fail as a <code>Task</code> tool error naming the cause, with no fallback to the session provider and no turn failure.</li><li><code>Task</code> is absent from the catalog in Plan and Goal.</li><li>After reload the card is collapsed by default; re-expanding preserves node order, attribution, outcome and nested content exactly as they appeared live.</li></ul></li><li><strong>Specs linked</strong>: <code>03-runtime/02-agent-runtime.md</code> §5f/§7.2b/§8, <code>03-runtime/03-tools-and-permissions.md</code> §10.2, <code>03-runtime/04-data-storage.md</code> §4.7a, <code>04-ux/03-permission-ux.md</code> §6a, <code>04-ux/08-component-spec.md</code> §9.9, ADR 0062, decisions-log D201, D265, D319</li><li><strong>Acceptance</strong>: C (conversation), E (tools &amp; permissions), F (persistence), Security, Quality</li><li><strong>Milestone</strong>: M6</li><li><strong>Status</strong>: Covered by unit tests (2026-08-06): <code>packages/shared</code><code>subagent-definition.test.ts</code> and <code>packages/agent-runtime</code><code>subagent-definitions.test.ts</code> (frontmatter, tool filtering, malformed documents, global-user-shadows-builtin, legacy-<code>maxTurns</code> ignored); <code>subagent.test.ts</code> (report bounding, abort, event attribution, prompt framing) and <code>path-lock.test.ts</code> (same-path ordering, concurrency cap); desktop <code>permission-inline.test.mjs</code> (queue order, id-matched removal, tool-call removal, abort denying the queue, card copy), <code>subagent-wiring.test.mjs</code> (main-process discovery and model pins) and <code>subagent-transcript.test.mjs</code> + <code>assistant-turns.test.mjs</code> (nesting, single report print, memoization, the card gate that admits a lone delegation and the count-aware aggregate copy in both locales), plus <code>subagent-topology.test.mjs</code> (delegate detection, structured outcomes and aggregate counts). Full multi-provider fan-out and rendered topology interaction remain manual.</li></ul><h4 id="e2e-subagent-legacy-turn-limit-frontmatter-is-ignored" tabindex="-1">E2E-SUBAGENT-legacy-turn-limit-frontmatter-is-ignored <a class="header-anchor" href="#e2e-subagent-legacy-turn-limit-frontmatter-is-ignored" aria-label="Permalink to &quot;E2E-SUBAGENT-legacy-turn-limit-frontmatter-is-ignored&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Agent mode. A user document <code>~/.agents/subagents/legacy-worker.md</code> whose frontmatter declares <code>maxTurns: 2</code> next to a valid <code>description</code> and <code>tools</code>, a second document that spells the same key <code>max-turns: 2</code>, and a third that never mentions it.</li><li><strong>Steps</strong>: 1) Open Settings → Subagents and confirm every row in the Built-in and Global groups renders with its tool grant and that no turn-limit field exists anywhere in the page or the editor&#39;s Advanced disclosure. 2) Delegate to <code>legacy-worker</code> and let it make more than two tool-calling turns. 3) Read the document back through the settings API and again as a raw file. 4) Open the editor on that row, save it without changing anything, and re-read the file.</li><li><strong>Expected</strong>: Both spellings load as valid definitions. The declared key is ignored exactly like any other unrecognized frontmatter key: no parse error, no warning or diagnostic naming it, the definition still resolves, and the file is not rewritten. The delegate is never stopped at two turns and never reports <code>truncated</code>; it ends only when it finishes or is <code>TaskStop</code>&#39;d. No surface in the app, and no locale&#39;s <code>chat.subagentStatus</code> copy, reports a turn limit or a “Turn limit reached” status.</li><li><strong>Specs linked</strong>: <code>03-runtime/02-agent-runtime.md</code> §5f, <code>04-ux/06-settings-ia.md</code> §7, ADR 0253, decisions-log D423</li><li><strong>Acceptance</strong>: C (conversation), Quality</li><li><strong>Milestone</strong>: M6</li><li><strong>Status</strong>: Draft — unit covered in <code>packages/shared</code>, <code>packages/agent-runtime</code>, and the host-core <code>user_subagents</code> regression test; the full UI journey needs a capable environment.</li></ul><h4 id="e2e-subagent-inherit-parent-tools" tabindex="-1">E2E-SUBAGENT-inherit-parent-tools <a class="header-anchor" href="#e2e-subagent-inherit-parent-tools" aria-label="Permalink to &quot;E2E-SUBAGENT-inherit-parent-tools&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Agent mode. A user document <code>~/.agents/subagents/worker.md</code> with <code>tools: inherit</code> (no assignable extras), at least one plugin or MCP tool in the parent catalog, and a non-empty Skill catalog. Built-in <code>explorer</code> remains on its whitelist.</li><li><strong>Steps</strong>: <ol><li>Confirm Settings → Agent → Subagents lists <code>worker</code> and that Edit shows Inherit parent tools on. Save without changing tools and reopen the file.</li><li>Delegate <code>Task</code> to <code>worker</code> with a brief that needs a Skill id and a plugin tool the parent already had.</li><li>Delegate <code>Task</code> to builtin <code>explorer</code> in the same session.</li><li>Confirm <code>worker</code> cannot call <code>Task</code>, <code>ToolSearch</code>, <code>asktool</code>, or <code>new_context</code>.</li></ol></li><li><strong>Expected</strong>: <ul><li>Step 1 round-trips <code>tools: inherit</code>; the document does not disappear from <code>agents.active</code> and save does not rewrite it to <code>Read, Glob, Grep</code>.</li><li><code>worker</code> receives Skill, the plugin/MCP tool, and the parent builtins minus the deny list. Its system prompt lists those names, includes the <code># Skills</code> catalog, and says it may change files when the parent catalog includes Bash/Edit/Write.</li><li><code>explorer</code> still has only <code>Read, Glob, Grep, Bash</code> and cannot call Skill.</li><li>The Task catalog line for <code>worker</code> reads <code>(tools: inherit)</code>.</li></ul></li><li><strong>Specs linked</strong>: <code>03-runtime/03-tools-and-permissions.md</code> §10.2, <code>03-runtime/02-agent-runtime.md</code> §5f/§7.2b, ADR 0246, issue #215</li><li><strong>Acceptance</strong>: E (tools &amp; permissions), Security</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Automated by <code>test:e2e:subagents</code> (host-core create/read/on-disk/active/loader inherit round-trip) and <code>test:e2e:subagent-models</code> (real sidecar/local transport Task spawn, inherited Skill/plugin catalog minus the deny list, and builtin explorer isolation). Unit coverage remains in <code>packages/shared</code>, <code>packages/agent-runtime</code>, and host-core <code>user_subagents</code>; the UI inherit-checkbox journey remains Draft. Required suites: <code>test:e2e</code>, <code>test:e2e:subagents</code>, <code>test:e2e:subagent-models</code>.</li></ul><h4 id="e2e-145-tool-results-read-as-structured-blocks-never-json" tabindex="-1">E2E-145: Tool results read as structured blocks, never JSON <a class="header-anchor" href="#e2e-145-tool-results-read-as-structured-blocks-never-json" aria-label="Permalink to &quot;E2E-145: Tool results read as structured blocks, never JSON&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A project-bound Agent session with permissions allowed for the turn; a plugin tool whose result is an arbitrary record is installed; the workspace contains a file large enough to trip host truncation.</li><li><strong>Steps</strong>: <ol><li>Run one turn that reads a source file, globs a directory, greps a token, greps again with <code>outputMode: filesWithMatches</code> and <code>count</code>, edits a workspace file, edits a scratch-root file, runs a failing shell command, and calls the plugin tool.</li><li>Inspect each collapsed activity row, then expand every row in light and dark.</li><li>Click a Glob path and a Grep hit heading.</li><li>Trigger a high-risk tool so the inline permission card appears.</li><li>Read a truncated file and copy each block.</li></ol></li><li><strong>Expected</strong>: <ul><li>No expanded row shows escaped JSON, and no payload appears twice.</li><li>Read/Write show highlighted content; Bash shows command, output, and error-hued stderr as separate blocks with empty channels omitted; Glob shows a path list; Grep shows hits grouped per file with line numbers in <code>content</code> mode, a path list in <code>filesWithMatches</code>, and per-file totals in <code>count</code>; the failing command carries an <code>exit 1</code> chip.</li><li>A Read row&#39;s collapsed chip shows the returned window as <code>{lineCount},L{offset+1}-L{offset+lineCount}</code> (for example, <code>50,L16-L65</code>), never <code>fileBytes</code>; a Read result without valid window metadata has no size chip. Write continues to show its byte-size chip.</li><li>Each Glob/Grep path-list row is start-aligned with natural character spacing; glyphs are not distributed across the block width.</li><li>The workspace edit shows no inline diff (its ReviewChangeCard owns it); the scratch edit shows a compact diff and a <code>scratch</code> chip.</li><li>The plugin result renders label/value fields and labeled blocks, not a blob.</li><li>Clicking a path opens it in the work panel; paths outside the workspace root are not clickable.</li><li>The permission card&#39;s args preview uses the same blocks.</li><li>Host truncation markers stay visible, a <code>truncated</code> chip appears, a host <code>notice</code> renders as a neutral note under the block it qualifies, capped lists report the hidden remainder, and copy yields the full payload.</li></ul></li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code> §9, §10.2, <code>08-meta/decisions-log.md</code> (D192)</li><li><strong>Acceptance</strong>: C (chat &amp; stream), E (tools &amp; permissions), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>tool-presentation.test.mjs</code>, <code>transcript-style.test.mjs</code>); full UI journey Draft</li></ul><h4 id="e2e-146-a-turn-that-produces-no-visible-text-re-runs-once" tabindex="-1">E2E-146: A turn that produces no visible text re-runs once <a class="header-anchor" href="#e2e-146-a-turn-that-produces-no-visible-text-re-runs-once" aria-label="Permalink to &quot;E2E-146: A turn that produces no visible text re-runs once&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A project-bound Agent session uses a deterministic provider fixture that ends one turn with no tool call and no text — once with reasoning content present, once with nothing at all; a second fixture run ends both the first turn and the re-run that way.</li><li><strong>Steps</strong>: <ol><li>Start an Agent turn with the reasoning-only fixture and watch the transcript while the runtime recovers.</li><li>Inspect the transcript, session state, and terminal diagnostics afterwards.</li><li>Repeat with the nothing-at-all fixture.</li><li>Repeat with the twice-silent fixture and inspect the terminal error message, its details disclosure, and its action button.</li><li>Click the error&#39;s retry action.</li><li>Reload the session and verify what stayed durable.</li></ol></li><li><strong>Expected</strong>: <ul><li>The recovered turn keeps the same visible assistant message id, emits one terminal lifecycle, and shows no error. The user sees only the answer.</li><li>The empty assistant is removed from model context before the re-run, so the provider never receives two assistant messages in a row, and it is never appended to the durable transcript.</li><li>The terminal diagnostics identify the empty-response recovery and the re-run&#39;s outcome.</li><li>The second silence emits one terminal retriable <code>EMPTY_MODEL_RESPONSE</code> assistant error and lifecycle event; the message names both attempts, and the retry action re-sends the last prompt.</li><li>A turn whose text is empty because it requested tools is untouched, and so is an aborted or already-failed turn.</li><li>Only one re-run happens per prompt, including after context-overflow recovery within the same prompt.</li></ul></li><li><strong>Specs linked</strong>: <code>03-runtime/02-agent-runtime.md</code> §5e, §7, <code>03-runtime/08-error-codes.md</code> §3.2, <code>08-meta/decisions-log.md</code> (D193)</li><li><strong>Acceptance</strong>: C (chat &amp; stream), F (persistence), H (diagnostics), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>runtime.test.ts</code>); full provider/UI journey Draft</li></ul><h4 id="e2e-146a-approved-plan-goal-progress-text-continues-once" tabindex="-1">E2E-146a: Approved Plan/Goal progress text continues once <a class="header-anchor" href="#e2e-146a-approved-plan-goal-progress-text-continues-once" aria-label="Permalink to &quot;E2E-146a: Approved Plan/Goal progress text continues once&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A project-bound session has an approved Plan or Goal and a deterministic provider fixture. One fixture ends with short forward-looking progress text and no tool call; a second ends with a normal final report;</li><li><strong>Steps</strong>: <ol><li>Start the approved execution with the progress-only fixture and inspect the transcript while the runtime recovers.</li><li>Inspect the model request context, lifecycle events, and visible message ids after the continuation completes.</li><li>Repeat with the normal final-report fixture.</li><li>Repeat the progress fixture with a continuation that emits a real tool call, then inspect the tool result and final report.</li></ol></li><li><strong>Expected</strong>: <ul><li>The progress text remains in one assistant bubble and causes exactly one continuation. The provider receives the user/execution context without the prior assistant message at the end, and the continuation carries the progress nudge.</li><li>The first attempt&#39;s <code>agent_start</code>, <code>turn_start</code>, <code>turn_end</code>, and <code>agent_end</code> are suppressed; the reused bubble id and one terminal lifecycle are visible to the UI. The nudge is removed after the run.</li><li>A continuation tool call proceeds through the ordinary autonomous loop; it does not create a duplicate assistant bubble or lifecycle.</li><li>A normal final report, including one recovered after silent-turn recovery, does not cause an unnecessary progress continuation. Ordinary Agent text answers remain unchanged.</li></ul></li><li><strong>Specs linked</strong>: <code>03-runtime/02-agent-runtime.md</code> §5e/§5e.1/§7, <code>03-runtime/08-error-codes.md</code> §3.2</li><li><strong>Acceptance</strong>: C (chat &amp; stream), F (persistence), H (diagnostics), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>runtime.test.ts</code>, <code>progress-turn.test.ts</code>); full provider/UI journey Draft</li></ul><h4 id="e2e-147-scoped-search-stays-inside-its-budget-and-the-agent-narrates" tabindex="-1">E2E-147: Scoped search stays inside its budget and the agent narrates <a class="header-anchor" href="#e2e-147-scoped-search-stays-inside-its-budget-and-the-agent-narrates" aria-label="Permalink to &quot;E2E-147: Scoped search stays inside its budget and the agent narrates&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A project-bound Agent session; the workspace contains a multi-megabyte source file, a minified bundle with a <code>.map</code> sibling (one line, megabytes long), a binary file, and a dependency tree excluded by <code>.gitignore</code>.</li><li><strong>Steps</strong>: <ol><li>Inspect <code>tools.list</code> for <code>Read</code>, <code>Glob</code>, and <code>Grep</code>.</li><li>Read the multi-megabyte file, then read it again from the reported next offset.</li><li>Grep a token that hits the minified bundle and its <code>.map</code>.</li><li>Grep the same token with <code>path</code> pointing into the ignored dependency tree, then with <code>include</code> narrowing to one extension, then with <code>outputMode: filesWithMatches</code> and <code>count</code>.</li><li>Glob a broad pattern, and Glob with <code>path</code> and <code>limit</code>.</li><li>Read the binary file.</li><li>Run a shell command that prints far past the shell budget on stdout, then one that fails after printing progress noise on stderr, and open the spill file named in each marker.</li><li>Ask a question that needs several tool batches, and watch the transcript between batches.</li></ol></li><li><strong>Expected</strong>: <ul><li>Every description carries its parameters and the real limit numbers.</li><li>No single tool result exceeds its budget: 128 KB for Read/Glob/Grep, 96 KB for Bash. Read reports <code>offset</code>, <code>lineCount</code>, <code>fileBytes</code>, and a next-offset <code>notice</code>; the second read continues without overlap; <code>totalLines</code> is always reported from the first read so the model knows the file scale upfront. A filled default or requested window reports <code>truncated: false</code> even when the file continues.</li><li>No file size is ever refused. Lines from the bundle and the <code>.map</code> arrive clipped at 16,384 chars and the clip count appears in <code>notice</code>, so one line cannot consume the result.</li><li>An explicit <code>path</code> reaches into the ignored tree; without it the same search returns nothing from there. <code>include</code>, <code>outputMode</code>, and <code>headLimit</code> each shrink the payload, and results order newest-modified first. When <code>rg</code> is on PATH, Grep uses it and still matches that contract; when it is missing or exits 2, Grep falls back in-process (D315).</li><li>The binary read fails with <code>TOOL_BINARY_CONTENT</code> and no binary reaches the model; Grep skips it silently.</li><li>Bash stdout keeps its head, stderr keeps its tail, both markers name which end survived and the spill path, and each spill file opens with the fuller output.</li><li>The agent answers in the language the user wrote in, precedes each tool batch with a sentence about what it is doing, never leaves more than one batch without new visible text, and ends with a self-contained result.</li></ul></li><li><strong>Specs linked</strong>: <code>03-runtime/16-tool-result-limits.md</code>, <code>03-runtime/02-agent-runtime.md</code> §7, <code>08-meta/decisions-log.md</code> (D194, D306, D315)</li><li><strong>Acceptance</strong>: C (chat &amp; stream), E (tools &amp; permissions), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (host-core <code>tools</code> tests, <code>runtime.test.ts</code> prompt assertions); full provider/UI journey Draft</li></ul><h4 id="e2e-100-a-pasted-mcp-server-runs-and-only-where-it-is-scoped" tabindex="-1">E2E-100: A pasted MCP server runs, and only where it is scoped <a class="header-anchor" href="#e2e-100-a-pasted-mcp-server-runs-and-only-where-it-is-scoped" aria-label="Permalink to &quot;E2E-100: A pasted MCP server runs, and only where it is scoped&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Two projects on disk, <code>~/work/api</code> and <code>~/personal/site</code>. A local stdio MCP server available on PATH. An Agent session per project.</li><li><strong>Steps</strong>: <ol><li>Extensions → MCP → Import from JSON. Paste a <code>mcpServers</code> document holding three servers: one valid stdio entry, one remote HTTP entry at a trusted LAN address such as <code>http://192.168.1.20:8080/mcp</code> with no <code>type</code>, and one stdio entry with no <code>command</code>.</li><li>Confirm the import, then open the imported stdio server and press Test connection.</li><li>Leave the server at <strong>Everywhere</strong> and ask the agent in each project to list its available tools.</li><li>Set the server to <strong>These projects</strong>, with only <code>~/work/api</code> picked.</li><li>Ask again in each project.</li><li>In the already-open <code>~/personal/site</code> session — assembled while the server was global — ask the agent to call one of the server&#39;s tools by name.</li><li>Edit the server&#39;s <code>env</code> and save; ask in <code>~/work/api</code> again.</li><li>Rename the server and re-scope it; ask once more.</li><li>Point the server&#39;s command at a binary that does not exist, save, and open a new session.</li><li>Restore the valid command and Test connection. Activate a tool through <code>ToolSearch</code>, then terminate the stub server process between calls. Call the same tool in the existing session without searching again.</li><li>Repeat with the restarted stub omitting that tool, and with the server disabled or scoped away before recovery. Also try concurrent calls after a disconnect and a server that fails its recovery handshake.</li></ol></li><li><strong>Expected</strong>: <ul><li>The activated tool works after a transport restart without a second search; concurrent calls share one handshake. The fresh server list must still advertise the tool. Removed tools and inactive servers are refused without executing a call; inactive servers are not reconnected. Recovery failure returns <code>UNAVAILABLE</code> and does not trigger repeated handshake attempts until an edit or Test connection. A failed tool execution is never replayed.</li><li>Two servers import; the third is listed as skipped with &quot;a stdio server requires command&quot;. The LAN HTTP entry lands as <code>http</code> with its url intact, and the editor shows the unencrypted-connection warning.</li><li>Test reports connected with the tool names it found, and the row&#39;s glyph turns from connecting to ready.</li><li>While global, both sessions see <code>mcp_&lt;serverId&gt;_&lt;tool&gt;</code> names.</li><li>After narrowing, only the <code>~/work/api</code> session sees them; the summary chip reads &quot;1 project&quot; and names it.</li><li>The stale call from step 6 fails with <code>TOOL_NOT_FOUND</code> and &quot;not active for this session&quot; — scope holds at dispatch, not only in the catalog.</li><li>The <code>env</code> edit drops the connection: the next assembly or call re-handshakes, and the tool&#39;s behaviour reflects the new value. The rename in step 8 does not reconnect anything.</li><li>The broken command records <code>failed</code> with a message, contributes no tools, and is not re-dialled on the following session assembly; pressing Test retries it.</li></ul></li><li><strong>Specs linked</strong>: <code>07-plugins/01-plugin-system.md</code> §12, <code>03-runtime/01-ipc-protocol.md</code> §12a, <code>08-meta/decisions-log.md</code> (D192, D193)</li><li><strong>Acceptance</strong>: E (tools &amp; permissions), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>apps/desktop/test/user-mcp.test.mjs</code>, <code>packages/shared/src/mcp-import.test.ts</code>, host-core <code>mcp_servers</code> tests); full UI journey Draft</li></ul><h4 id="e2e-100b-remote-http-mcp-server-oauth-2-1-authorization-and-token-lifecycle" tabindex="-1">E2E-100B: Remote HTTP MCP server OAuth 2.1 authorization and token lifecycle <a class="header-anchor" href="#e2e-100b-remote-http-mcp-server-oauth-2-1-authorization-and-token-lifecycle" aria-label="Permalink to &quot;E2E-100B: Remote HTTP MCP server OAuth 2.1 authorization and token lifecycle&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: An HTTP MCP server endpoint configured requiring OAuth 2.1 authentication (RFC 9728 discovery and PKCE S256).</li><li><strong>Steps</strong>: <ol><li>Open Settings &gt; Agent &gt; MCP. Add an HTTP MCP server URL.</li><li>The server status displays <code>Authorization required</code>.</li><li>Click <code>Authorize</code>. Main spins up loopback on <code>127.0.0.1</code>, launches external browser to the authorization endpoint with RFC 8707 <code>resource</code>.</li><li>Complete login in browser, redirecting to <code>http://127.0.0.1:&lt;port&gt;/callback</code>.</li><li>The loopback callback validates state/code, completes token exchange with PKCE verifier, saves token to encrypted secret <code>secret:mcp:&lt;id&gt;:oauth</code>, renders escaped success page, and emits <code>done</code> event.</li><li>Settings UI updates status to <code>Ready</code> with discovered tools, shows localized success toast, and displays <code>OAuth</code> badge.</li><li>When access token expires, <code>UserMcpRuntime</code> transparently uses refresh token to obtain a fresh access token without user prompt.</li><li>Moving the server via <code>mcp.transfer</code> preserves and re-keys the OAuth token secret under the destination ID.</li></ol></li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code>, ADR 0283, ADR 0142</li><li><strong>Acceptance</strong>: E (tools &amp; permissions), Security</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>apps/desktop/test/mcp-oauth.test.mjs</code>, <code>apps/desktop/test/user-mcp.test.mjs</code>); full UI journey Draft</li></ul><h4 id="e2e-101-a-user-skill-is-written-once-and-scoped-per-project" tabindex="-1">E2E-101: A user skill is written once and scoped per project <a class="header-anchor" href="#e2e-101-a-user-skill-is-written-once-and-scoped-per-project" aria-label="Permalink to &quot;E2E-101: A user skill is written once and scoped per project&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Two projects on disk. An Agent session in each.</li><li><strong>Steps</strong>: <ol><li>Extensions → Skills → New. Save with an empty description.</li><li>Fill in a description, write a body, and save.</li><li>Ask the agent in each project to use the skill by name.</li><li>Set the skill to <strong>These projects</strong> with only the first project picked, then switch it Off and back to <strong>These projects</strong>.</li><li>Ask again in each project.</li><li>In the first project&#39;s session, narrow the skill to the <em>second</em> project and immediately ask the agent to invoke it.</li><li>Paste a body over 128 KB.</li><li>Switch the app language to 中文 and revisit every surface above.</li></ol></li><li><strong>Expected</strong>: <ul><li>Saving without a description is refused with a message naming the field: the description is the only part that enters the prompt.</li><li>The base prompt carries the skill&#39;s id, name and trimmed description and not its body; the body arrives only through the <code>Skill</code> tool.</li><li>Toggling Off and back restores the picked project without re-picking it.</li><li>After narrowing, only the scoped project&#39;s session can invoke it; the other gets &quot;not enabled for this project&quot;.</li><li>Step 6 fails in the already-open session too — the scope is re-read when the body is loaded, not trusted from the catalog that listed it.</li><li>The byte counter warns before the 128 KB cap and the save is refused past it.</li><li>Every label, empty state, error and count renders in Chinese, with counts reading naturally at 0, 1 and many.</li></ul></li><li><strong>Specs linked</strong>: <code>07-plugins/01-plugin-system.md</code> §12.3, <code>03-runtime/01-ipc-protocol.md</code> §12b, <code>08-meta/decisions-log.md</code> (D174, D192, D194)</li><li><strong>Acceptance</strong>: E (tools &amp; permissions), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (host-core <code>user_skills</code> tests, <code>apps/desktop/test/extensions-page.test.mjs</code>); full UI journey Draft</li></ul><h4 id="e2e-102-composer-file-and-image-paste-keeps-structured-attachment-metadata" tabindex="-1">E2E-102: Composer file and image paste keeps structured attachment metadata <a class="header-anchor" href="#e2e-102-composer-file-and-image-paste-keeps-structured-attachment-metadata" aria-label="Permalink to &quot;E2E-102: Composer file and image paste keeps structured attachment metadata&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: The app is running with an Agent session in a project and a home composer available. The OS clipboard contains a text snippet no longer than the configured large-paste threshold, one or more local files (including a filename with whitespace), and an image in separate paste attempts. Record the app data directory and the session id.</li><li><strong>Steps</strong>: <ol><li>Paste text-only within the configured threshold and confirm editable text. Copy a Word selection with text plus a generated image representation; confirm text wins, including multiline/CRLF, blank/trailing lines, literal <code>&lt;&gt;&amp;</code> and quotes, replacing selections across line breaks/attachment chips, caret position, and native undo/redo. Repeat above the threshold and confirm a TXT chip rather than an image. Paste a real native image file with accompanying filename text and confirm it remains an image attachment.</li><li>Paste one local file, then paste multiple files including a spaced name.</li><li>Paste an image from the OS screenshot/clipboard provider.</li><li>Inspect the draft before sending: confirm each materialized item is a removable leaf-name chip and no scratch absolute path occupies the textarea. Hover/focus chips to inspect their full paths, remove one, then send the prompt and inspect the session message&#39;s attachment metadata. Before sending, verify image thumbnails sit left-aligned above and outside the input shell. Replacing/undoing all text must retain the images; image-only drafts must survive session switching and retain submission metadata. Mount the complete Composer: click Send for image-only input, verify attachment delivery and successful clearing, and immediately reject a text-plus-image send to verify full draft restoration. In a narrow pane, add 20 images, scroll to the last one and remove it without losing others. Inspect the thumbnail and open it with click, Enter, and Space. Confirm a centered modal preview opens, the work panel stays unchanged, and the draft is neither edited nor sent. Check small images stay at natural size, wide images fit without distortion, zoom/reset and download target the selected original, and multiple images support button and arrow-key navigation with zoom reset. Drag at fit size and after zoom, including releasing outside the image; check cancellation and ordinary wheel panning. Fit and image navigation must reset position; dragging must not dismiss the preview or leave capture active after close/session changes. Close with Escape, the close button, and blank space; verify focus/caret restoration and modal focus containment. Native plugin surfaces must hide while the preview is open. Repeat without a project. Remove an attachment; it must neither preview nor send. Switch session/project or remove the selected attachment during a delayed read; the old preview must close and late results must not overwrite a newer image. Missing/undecodable images show retry and keep the draft. Text chips must still expand as editable text.</li><li>Inspect <code>&lt;data_dir&gt;/scratch/&lt;sessionId&gt;/pasted/</code> and compare the saved bytes with the source files/image. Check the project <code>git status</code>.</li><li>Delete the session, then confirm its scratch directory and pasted files are removed.</li></ol></li><li><strong>Expected</strong>: <ul><li>Text-only paste within the configured threshold remains native and is not routed through the file bridge. Oversized text behavior is covered by E2E-102g.</li><li>Each file/image is saved with a sanitized, UUID-backed unique name under the session scratch root, while its chip shows only the sanitized original leaf name. Duplicate leaf names remain separate references.</li><li>The dispatched prompt carries ordinary files through the existing path reference flow and carries pasted files/images as structured attachments; the persisted user message contains refs and metadata, never binary bytes. The agent can use its normal file tools to read the materialized files.</li><li>A home paste creates or reuses a durable session before writing. The workspace remains clean and no workspace artifact row is created.</li><li>Image preview preserves the draft and attachment metadata, uses <code>fs/readImageDataUrl</code> with bounded reads and path containment, and works without a workspace for allowed scratch files. Preview preserves the draft attachment format and leaves work-panel tabs unchanged. Closing/unmounting releases modal/native-view blocking.</li><li>Deleting the session removes the pasted files with the rest of scratch.</li><li>Deleting text immediately before an inline file chip must not insert a blank line or move the chip to the next line; native undo/redo stay intact.</li></ul></li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code> §11.7–11.8, <code>03-runtime/01-ipc-protocol.md</code> §13c, <code>03-runtime/03-tools-and-permissions.md</code> §4b, <code>03-runtime/04-data-storage.md</code>, <code>08-meta/decisions-log.md</code> (D197, D209, D243, D392), ADR 0059, ADR 0070, ADR 0101, ADR 0218</li><li><strong>Acceptance</strong>: C (conversation &amp; stream), E (tools &amp; permissions), F (persistence), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>composer-paste-files.test.mjs</code>, <code>composer-clipboard.test.mjs</code>, <code>composer-native-deletion.test.mjs</code>); <code>pnpm test:e2e:composer-paste</code> mounts the real ComposerInput, draft/paste hooks, file viewer, production CSS and sandboxed preload. It dispatches Chromium ClipboardEvents with synthetic mixed data and native File objects, exercises the real scratch writer and contained file reader, compares saved bytes and decodes the preview image. Requires a desktop build, installed Electron and a graphical session (Xvfb on Linux). It does not modify the OS clipboard or automate Word; Word/platform journeys and full provider dispatch remain manual. Run against the refreshed request candidate and record its revision; PR integration validation follows the authoritative <code>AGENTS.md</code> workflow without merging into local main first.</li></ul><h4 id="e2e-102h-composer-picker-imports-files-into-session-scratch" tabindex="-1">E2E-102h: Composer picker imports files into session scratch <a class="header-anchor" href="#e2e-102h-composer-picker-imports-files-into-session-scratch" aria-label="Permalink to &quot;E2E-102h: Composer picker imports files into session scratch&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: The app is running with a home or Agent composer and a durable session. The native picker can select a text file and an image outside the active workspace.</li><li><strong>Steps</strong>: 1) Click the Composer <code>+</code> button twice in quick succession; confirm that only one native file picker opens, directly without an intermediate type-choice menu. 2) Select both fixtures. 3) Inspect the draft chips and send a prompt asking the agent to read the text fixture and identify the image marker. 4) Inspect the renderer request, session transcript, and the session scratch directory.</li><li><strong>Expected</strong>: While the native picker or its renderer import is in flight, repeated <code>+</code> clicks are ignored. While the native picker is open, the main process rejects duplicate picker requests instead of opening another dialog. The native picker returns a short-lived one-shot token, never a source absolute path, and the selections are copied into <code>&lt;data_dir&gt;/scratch/&lt;sessionId&gt;/pasted/</code> before they enter the draft. The app classifies each selected item from its MIME/extension metadata, so the same picker handles both regular files and images. Chips show sanitized leaf names while prompt attachments reference only the copied paths; the workspace is unchanged. The agent can call <code>Read</code> on the text fixture, and a vision-capable model receives the image as an image block. Durable messages retain metadata and refs only, never the source absolute path or binary bytes. The file picker does not offer directory selection; missing files, expired/replayed tokens, and oversized files return a visible IPC error and write nothing.</li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code> §13c, <code>03-runtime/04-data-storage.md</code>, <code>04-ux/08-component-spec.md</code> §11.7–11.8, ADR 0059, ADR 0101, ADR 0218</li><li><strong>Acceptance</strong>: B (model config), C (conversation &amp; stream), E (tools &amp; permissions), F (persistence), Security, Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>apps/desktop/test/composer-paste-files.test.mjs</code>); provider/UI journey Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-102i-composer-accepts-native-file-and-folder-drops" tabindex="-1">E2E-102i: Composer accepts native file and folder drops <a class="header-anchor" href="#e2e-102i-composer-accepts-native-file-and-folder-drops" aria-label="Permalink to &quot;E2E-102i: Composer accepts native file and folder drops&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: The app is running with an Agent session and a visible Composer. The OS file manager exposes one regular file, one folder, and a mixed multi-selection; place the caret in the middle of a non-empty draft.</li><li><strong>Steps</strong>: 1) Drag the regular file over the Composer and observe the target outline, then drop it. 2) Drag the folder into the same draft. 3) Repeat with a mixed file/folder selection while the caret is between existing text. 4) Inspect the draft, remove the file chip, and send the prompt. 5) Inspect the saved scratch file and the persisted user message.</li><li><strong>Expected</strong>: File-system drag-over prevents the browser default and marks the whole Composer shell without layout movement. Regular files are saved through the existing bounded session-scratch bridge and appear as removable leaf-name chips; folders are not read, copied, or traversed, and their full native paths appear at the caret as <code>@&lt;path&gt;/</code> literal directory references. Mixed drops preserve OS order, retain surrounding text, and restore the caret after asynchronous file saving. Sending keeps the file&#39;s existing attachment metadata/path behavior and the folder path as prompt text; no workspace files are created.</li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code> §11.5–11.8, <code>04-ux/09-interaction-patterns.md</code> §8.2/§8a.2, <code>03-runtime/01-ipc-protocol.md</code> §13c, ADR 0059, ADR 0070, ADR 0222</li><li><strong>Acceptance</strong>: C (conversation &amp; stream), F (persistence), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>apps/desktop/test/composer-drag-drop.test.mjs</code>); full desktop gesture journey Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-102a-composer-file-reference-results-use-compact-leaf-names" tabindex="-1">E2E-102a: Composer file reference results use compact leaf names <a class="header-anchor" href="#e2e-102a-composer-file-reference-results-use-compact-leaf-names" aria-label="Permalink to &quot;E2E-102a: Composer file reference results use compact leaf names&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: The app is running with an Agent session in a workspace containing nested files, duplicate leaf names in different directories, and a directory whose name contains whitespace.</li><li><strong>Steps</strong>: 1) Type <code>@</code> and filter to the nested and duplicate entries. 2) Inspect the visible rows, then hover for full-path tooltips and inspect their accessible names. 3) Accept a file with Enter and confirm a leaf-name chip remains in the draft at the caret while the <code>@</code> token and full path stay hidden. Accept a second file with Tab or click. Accept a directory result and continue to a child file. 4) Send the completed references and inspect the persisted user message.</li><li><strong>Expected</strong>: <ul><li>Each result persistently renders only its leaf name; directories retain a trailing <code>/</code>, and no parent path consumes horizontal row space.</li><li>The tooltip and accessible name retain the complete relative path so duplicate leaf names remain distinguishable.</li><li>Enter/Tab/click on a file replaces the <code>@</code> token with an inline chip that stays in the draft; that key does not send. File acceptance retains the original complete <code>entry.path</code> behind the chip; directory acceptance retains literal path continuation. At dispatch the sent and persisted prompt contains each complete path with existing whitespace quoting, and the agent can read both selected files normally.</li></ul></li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code> §11.8, <code>04-ux/09-interaction-patterns.md</code> §8a, <code>03-runtime/01-ipc-protocol.md</code> §13c, <code>08-meta/decisions-log.md</code> (D124, D209, D362), ADR 0024, ADR 0070</li><li><strong>Acceptance</strong>: C (conversation &amp; stream), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>apps/desktop/test/composer-file-reference-display.test.mjs</code>); full UI journey Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-102b-unanswered-stop-restores-compact-file-reference-drafts" tabindex="-1">E2E-102b: Unanswered Stop restores compact file-reference drafts <a class="header-anchor" href="#e2e-102b-unanswered-stop-restores-compact-file-reference-drafts" aria-label="Permalink to &quot;E2E-102b: Unanswered Stop restores compact file-reference drafts&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: An Agent session can delay its first assistant event. The draft contains ordinary text, one workspace reference, two pasted references with duplicate leaf names, and a canonical path containing whitespace.</li><li><strong>Steps</strong>: 1) Send the mixed draft and stop before assistant text, thinking, or any tool row begins. 2) Inspect the restored composer and transcript. 3) Send the restored draft again and inspect the persisted user message. 4) Repeat, allow partial assistant output to begin, then stop.</li><li><strong>Expected</strong>: <ul><li>Unanswered Stop removes the just-sent user row and restores the original ordinary text plus leaf-name chips in stable order.</li><li>Relative and scratch absolute paths never appear in the restored textarea; duplicate labels remain distinct references.</li><li>Resending serializes each exact canonical path once with existing whitespace quoting.</li><li>Stop after reply start preserves the partial aborted transcript and does not restore or duplicate text or chips.</li><li>Scratch bytes remain under the existing session lifecycle.</li></ul></li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code> §11.5/§11.8, <code>04-ux/09-interaction-patterns.md</code> §3.2/§8a.2, <code>03-runtime/10-session-state-machine.md</code>, <code>08-meta/decisions-log.md</code> (D209), ADR 0070</li><li><strong>Acceptance</strong>: C (conversation &amp; stream), F (persistence), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>composer-file-reference-display.test.mjs</code>, <code>transcript-style.test.mjs</code>); full UI journey Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-102c-vision-capable-models-receive-pasted-images-as-image-input" tabindex="-1">E2E-102c: Vision-capable models receive pasted images as image input <a class="header-anchor" href="#e2e-102c-vision-capable-models-receive-pasted-images-as-image-input" aria-label="Permalink to &quot;E2E-102c: Vision-capable models receive pasted images as image input&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A deterministic vision-capable models.dev model whose resolved record contains <code>modalities.input: [&quot;text&quot;, &quot;image&quot;]</code>; an Agent session; one pasted PNG and one text prompt. Capture the renderer request, main-to-sidecar payload, provider request, durable transcript, and <code>&lt;data_dir&gt;/attachments/</code>.</li><li><strong>Steps</strong>: <ol><li>Select the vision-capable model and paste the PNG into Composer.</li><li>Confirm the PNG appears as a removable image chip above the textarea and no separate explanatory vision-status row is rendered.</li><li>Send a prompt asking the model to identify one visible detail.</li><li>Inspect the provider request and durable session message after completion.</li><li>Reload the session and ask a follow-up about the same image.</li></ol></li><li><strong>Expected</strong>: <ul><li>The model picker/session capability state comes from the exact models.dev record, and Composer shows the image as a removable chip without a separate explanatory status row. If a remote refresh fails, the bundled release snapshot remains authoritative for the process.</li><li>Main writes one content-addressed image blob and sends the sidecar a transient image attachment; the provider adapter emits an image content block/data URL, not only <code>@&lt;scratch-path&gt;</code> text.</li><li>The durable message contains <code>kind</code>, display <code>name</code>, MIME/size, and the <code>attachments/&lt;sha256&gt;</code> ref, but no base64 or image bytes.</li><li>After reload, history hydration restores the image block from the bounded attachment root and the follow-up still has image context.</li></ul></li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code> §5.1/§13c, <code>03-runtime/02-agent-runtime.md</code> §5c, <code>03-runtime/04-data-storage.md</code>, <code>03-runtime/13-model-catalog-and-selection.md</code> §11.2, <code>04-ux/08-component-spec.md</code> §11.7–11.8, <code>08-meta/decisions-log.md</code> (D243), ADR 0101</li><li><strong>Acceptance</strong>: B (model config), C (conversation &amp; stream), F (persistence), Quality, Security</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>model-capabilities.test.ts</code>, host-core attachment roundtrip); provider/UI journey Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-102d-non-vision-and-oversized-images-use-the-path-fallback" tabindex="-1">E2E-102d: Non-vision and oversized images use the path fallback <a class="header-anchor" href="#e2e-102d-non-vision-and-oversized-images-use-the-path-fallback" aria-label="Permalink to &quot;E2E-102d: Non-vision and oversized images use the path fallback&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: One known non-vision model and one known vision-capable model; an Agent session; a normal PNG and a deterministic image just above the 10 MB inline bound.</li><li><strong>Steps</strong>: <ol><li>Select the non-vision model, paste the normal PNG, and inspect Composer&#39;s removable image chip.</li><li>Send the prompt and inspect the sidecar/provider request.</li><li>Select the vision model, paste the oversized image, and send it.</li><li>Retry each turn after disposing/recreating the runtime.</li></ol></li><li><strong>Expected</strong>: <ul><li>Composer shows the pasted image as a removable chip without a separate vision-status explanation. Both cases show a safe <code>@path</code> fallback and no image block/base64 payload.</li><li>The non-vision request references the session scratch file; the oversized vision request references a safe path while the image remains available to the normal file tools.</li><li>Retries and runtime recreation use the content-addressed image ref and a session <code>replayed/</code> path where needed; no duplicate binary blobs are made.</li><li>The transcript still stores attachment metadata/ref and the UI does not claim that the image was sent visually.</li></ul></li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code> §5.1, <code>03-runtime/03-tools-and-permissions.md</code> §4b, <code>03-runtime/02-agent-runtime.md</code> §5c, <code>03-runtime/04-data-storage.md</code>, ADR 0101</li><li><strong>Acceptance</strong>: B (model config), C (conversation &amp; stream), E (tools &amp; permissions), F (persistence), Security</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-102e-unknown-model-ids-fail-closed-for-vision-transport" tabindex="-1">E2E-102e: Unknown model ids fail closed for vision transport <a class="header-anchor" href="#e2e-102e-unknown-model-ids-fail-closed-for-vision-transport" aria-label="Permalink to &quot;E2E-102e: Unknown model ids fail closed for vision transport&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A custom provider/model id absent from the pi-ai catalog, discovery data that incorrectly labels it <code>vision</code>, no explicit <code>supportsImages</code> binding override, and a pasted PNG.</li><li><strong>Steps</strong>: <ol><li>Refresh the provider model list and select the discovered custom id.</li><li>Paste the PNG and inspect the model picker and Composer image chip.</li><li>Send the prompt and inspect the sidecar/provider payload.</li></ol></li><li><strong>Expected</strong>: <ul><li>The unknown model is runnable as a generic text model but is not promoted to <code>vision</code> by discovery/cache metadata.</li><li>Composer keeps the image chip without rendering a model-dependent status message, and the provider receives no image block or base64 value; the safe file path remains available.</li><li>The durable attachment ref is still recorded so a later known vision model can replay the image correctly.</li></ul></li><li><strong>Specs linked</strong>: <code>03-runtime/11-provider-model-system.md</code> §6.2/§11, <code>03-runtime/13-model-catalog-and-selection.md</code> §11.2, <code>04-ux/08-component-spec.md</code> §11.8, ADR 0101, ADR 0218</li><li><strong>Acceptance</strong>: B (model config), C (conversation &amp; stream), E (tools &amp; permissions), Security</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>model-capabilities.test.ts</code>); provider/UI journey Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-102f-oversized-image-references-do-not-block-session-startup" tabindex="-1">E2E-102f: Oversized image references do not block session startup <a class="header-anchor" href="#e2e-102f-oversized-image-references-do-not-block-session-startup" aria-label="Permalink to &quot;E2E-102f: Oversized image references do not block session startup&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A project contains an image larger than the 10 MB inline bound; a known vision-capable model is selected; the session has a valid provider and workspace.</li><li><strong>Steps</strong>: 1. Reference the oversized image from the Composer and send a prompt. 2. Observe the turn lifecycle while the attachment is prepared. 3. Dispose/recreate the runtime and send a follow-up about the image. 4. Inspect the session transcript, the content-addressed attachment blob, and the session <code>replayed/</code> path.</li><li><strong>Expected</strong>: The session accepts the prompt and reaches the provider without hanging the Electron main process or crashing the sidecar. Main hashes and copies the image without constructing a whole-file in-memory buffer. The provider receives the safe <code>@path</code> fallback rather than an oversized image block/base64 payload. Runtime recreation copies the stored blob into the scratch fallback path without reading the whole blob into memory. The durable message retains only attachment metadata and the content-addressed ref.</li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code> §5.1, <code>03-runtime/02-agent-runtime.md</code> §5, <code>03-runtime/04-data-storage.md</code>, ADR 0101</li><li><strong>Acceptance</strong>: C (conversation &amp; stream), E (tools &amp; permissions), F (persistence), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered; full UI journey Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-102g-large-text-paste-becomes-an-inline-session-scratch-reference" tabindex="-1">E2E-102g: Large text paste becomes an inline session-scratch reference <a class="header-anchor" href="#e2e-102g-large-text-paste-becomes-an-inline-session-scratch-reference" aria-label="Permalink to &quot;E2E-102g: Large text paste becomes an inline session-scratch reference&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: The app is running with an Agent session in a project and a home composer available. The large paste threshold is first left at its default, then changed to a small test value. Record the app data directory, session id, and a multiline Unicode text fixture.</li><li><strong>Steps</strong>: <ol><li>Open Settings → AI → Defaults and confirm the large paste threshold is 600. Set it to a deterministic lower value, save it, then return to the composer.</li><li>Paste text exactly at the threshold and confirm native textarea behavior; paste text one character above it at the beginning, middle, and end of drafts, including multiline and Unicode content.</li><li>Inspect the draft after each oversized paste: confirm the exact prefix and suffix remain, a generated <code>pasted-text-*.txt</code> chip appears at the original selection, the editor does not contain the scratch absolute path, and the composer reports its busy/error state correctly during the transfer.</li><li>Click the generated TXT chip and repeat with keyboard focus plus Enter and Space. Confirm the exact UTF-8 contents replace the chip at its position, the text is editable, the caret lands after it, and subsequent send uses the edited text. While a read is pending, switch drafts or remove the chip and confirm a stale response does not change the current draft.</li><li>Inspect the session <code>scratch/&lt;sessionId&gt;/pasted/</code> file bytes, send a mixed draft that still contains a chip, and inspect the renderer request, persisted user message, and agent-readable path. Switch projects and sessions before sending a cached draft, then remove the chip and confirm it is no longer dispatched.</li><li>Delete the owning session and confirm its temporary paste files are removed.</li></ol></li><li><strong>Expected</strong>: <ul><li>The threshold is persisted as an AI default, defaults to 600 on older settings, and accepts only integer values from 1 through 1,000,000.</li><li>Text at or below the threshold remains native. Text above it is saved byte-for-byte as UTF-8 <code>text/plain</code> under the owning session&#39;s scratch <code>pasted/</code> directory, without changing the project or creating an artifact.</li><li>The sentinel-backed TXT chip is inserted at the exact paste selection, including in the middle of a multiline draft. Clicking or pressing Enter / Space expands it to editable exact text and removes its reference; edits made afterward are what dispatch sends. A failed, binary, image, or oversized read leaves the chip and mapping intact.</li><li>Any remaining chip is resolved to its canonical path in place exactly once; it is neither appended as a basename nor duplicated as an attachment. Removing the chip removes that mapping.</li><li>Session switching, project switching, unanswered Stop restoration, and session deletion respect the existing session ownership and cleanup rules.</li></ul></li><li><strong>Specs linked</strong>: <code>04-ux/06-settings-ia.md</code>, <code>04-ux/07-ui-design-system.md</code> §8.1, <code>04-ux/08-component-spec.md</code> §11.7–11.8, <code>04-ux/09-interaction-patterns.md</code> §8a, <code>03-runtime/01-ipc-protocol.md</code> §8, <code>03-runtime/04-data-storage.md</code> §7, <code>08-meta/decisions-log.md</code> (D262), ADR 0059, ADR 0070, ADR 0131</li><li><strong>Acceptance</strong>: C (conversation &amp; stream), E (tools &amp; permissions), F (persistence), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>composer-trigger.test.ts</code>, <code>apps/desktop/test/composer-paste-files.test.mjs</code>); full UI journey Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-103-settings-agent-pages-manage-file-backed-capabilities" tabindex="-1">E2E-103: Settings Agent pages manage file-backed capabilities <a class="header-anchor" href="#e2e-103-settings-agent-pages-manage-file-backed-capabilities" aria-label="Permalink to &quot;E2E-103: Settings Agent pages manage file-backed capabilities&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: The app is running with two registered projects, A and B, and an Agent session available in each. The fixtures use only <code>~/.agents/skills</code>, <code>~/.agents/servers</code>, <code>~/.agents/subagents</code>, and the two projects&#39; <code>.agents</code> directories; no <code>.pi</code> capability directory exists.</li><li><strong>Steps</strong>: <ol><li>Open Settings &gt; Agent and verify Skills, MCP, and Subagents are three independent navigation destinations. Open Extensions and verify that only Installed and Marketplace tabs are present.</li><li>Open Skills. Confirm one toolbar sits above one panel, the panel shows a global group header rooted at <code>~/.agents/skills</code> and a project group header rooted at project A&#39;s <code>.agents/skills</code>, both flow in one column at natural page height, and the project picker changes the selected project. Confirm the Skills toolbar has the same single primary action shape as MCP, with the concise New label; the level-specific Import action is available in each Skills group header. On first paint confirm skeleton rows appear; on a later refresh confirm the rows already on screen stay and the list dims instead.</li><li>Exercise the level filter and the search field. Confirm All / Global / Project carry counts that agree with the rendered rows, that selecting a level hides the other group without hiding the toolbar or its actions, that search narrows both groups and its counts, that clearing the search restores every row, and that a search with no matches reports it and suggests widening the filter.</li><li>Toggle a global skill off while project A is selected. Confirm the switch flips immediately without the list reverting to skeletons, the row is dimmed, a toast names the skill, every other row stays interactive while the request is in flight, and the global document remains unchanged. Switch to project B and confirm the global skill remains enabled there. Force a host rejection and confirm the switch returns to its previous position.</li><li>Create a skill from the page. With the filter on Global confirm the primary action names the global destination and the new document lands in <code>~/.agents/skills</code>; with the filter on Project confirm it names the project and lands in project A. With Project selected and no project chosen, confirm the attempt reports it instead of failing silently. Edit the new skill, save, and confirm the body round-trips.</li><li>From a project skill&#39;s overflow menu choose Reveal and confirm the project file is revealed, not a global file of the same id. Choose Remove and confirm the first press only arms and relabels the item, the second press deletes, and dismissing the menu disarms it.</li><li>Put a same-name project skill in A and disable it. Confirm the effective runtime catalog does not fall back to the global skill; the project record shadows first and filtering happens second.</li><li>Use the Import action to choose exactly one Markdown file. Confirm it is physically copied to the level the filter points at, appears immediately, and a second file cannot be selected in the same picker invocation.</li><li>Open MCP. Add a project server and a global server, edit the project server, and test an existing connection from the row&#39;s overflow menu. Confirm the modal locks the id while editing, rejects a same-level duplicate id or label, and reports ready or failed through the row badge and a toast. Delete a server through the same two-press menu action and confirm its file is gone.</li><li>Delete a skill or MCP file outside the app, reload its page, and confirm the row disappears and its local state has no orphaned entry. Confirm deleting a global file also removes its project overrides.</li><li>Open Subagents. Confirm it is one global-only panel rooted at <code>~/.agents/subagents</code>, with no level filter, no project picker, and no project-level controls. Confirm a Built-in group lists the five shipped defaults (<code>explorer</code>, <code>code-reviewer</code>, <code>test-runner</code>, <code>fixer</code>, <code>ui-designer</code>) even when the user directory is empty, each with a Built-in badge, its tool grant, and an enablement switch, but no reveal or delete. Confirm the Global group header carries the global level label and item count, that create/edit/delete/reveal all work from the page for user-owned rows, that leaving the output limit empty writes a definition with no <code>maxTokens</code>, and that an empty user directory still resolves <code>settings.subagentsEmpty</code> to localized empty-state copy under the Global group rather than displaying a raw translation key. Open New subagent and confirm the Model field is a select of the same configured, runnable models as the Composer, grouped by provider, with an inherit-session option, not a free-typed <code>provider/model</code> input. Pin a configured model, save, and confirm the document&#39;s <code>model:</code> frontmatter is <code>vendorKey-or-name/modelId</code>. When two configured providers share a generic or vendor key, confirm each provider remains a separate group and its model pin uses a unique display name (or provider id when the names also collide). Edit a definition whose pin is no longer configured and confirm that pin remains selected instead of snapping to inherit.</li><li>Narrow the window to the toolbar&#39;s stacking breakpoint. Confirm the segmented control spans the width, search moves below it, actions wrap left-aligned, group headers drop the resolved path, and the page gains no horizontal overflow. With a pointer that cannot hover, confirm the row&#39;s edit and overflow controls are visible without hovering.</li></ol></li><li><strong>Expected</strong>: <ul><li>The three Settings pages use no tabs for switching capabilities, flow at natural page height for empty and populated states, support dark and light themes, and begin with quiet page-specific descriptions plus project-over-global scope copy where relevant. Each page is one toolbar above one panel: the toolbar carries the level filter with live counts, one search field with a clear affordance, the project picker, and the primary actions; the panel divides levels with group headers naming the level, its resolved <code>.agents</code> path, and a localized count. Rows use quiet capability icons, a level badge plus localized source/transport badges, descriptions, and persistent enablement switches; edit and the overflow menu stay quiet until the row is hovered, focused, or has its menu open, and are always visible where hover is unavailable. Skeleton rows appear on first paint only, later refreshes dim the rows already on screen and announce the refresh, busy state is confined to the row with the in-flight request, and counts are exposed to assistive technology. Empty states stay centered inside the panel without a decorative frame, show a host Lucide icon in a chip wrapper (not a padded raw SVG), and offer the page&#39;s primary action, and no capability-specific color system is introduced.</li><li>Create, edit, and delete are available for all three capabilities without leaving Settings. New capabilities land at the level the filter points at, destructive actions require two presses of the same relabelled menu item, and revealing a project-level skill opens that project&#39;s file rather than a global file sharing its id. The Subagents editor Model field is a grouped select of configured runnable models plus inherit-session, not a free-typed id; saving writes <code>vendorKey-or-name/modelId</code> (using a unique provider name or id when aliases collide), and an unconfigured existing pin remains selected. The Advanced disclosure also carries the delegate&#39;s own output limit: it starts empty, shows the model-default placeholder rather than an unlimited one, and a value round-trips through the document&#39;s <code>maxTokens</code> frontmatter and back into the field — while clearing it removes the key so the delegate follows the model again.</li><li>Capability files contain configuration/frontmatter only; enablement is persisted in the app-local <code>agent-capabilities</code> state files.</li><li>Project records shadow global records by id or name even when disabled, and the next runtime activation reflects the same result as the UI.</li><li>Physical import is single-file and level-specific, and disk deletion is removed by scanning rather than represented as a pending row.</li></ul></li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code> §12a–§12d, <code>03-runtime/02-agent-runtime.md</code> §5f, <code>03-runtime/13-model-catalog-and-selection.md</code> §2 (Subagent editor), <code>04-ux/01-ui-ia.md</code> §3.5–§3.6, <code>04-ux/06-settings-ia.md</code> §2 (Agent capability destinations), §4.21–§4.25, <code>07-plugins/01-plugin-system.md</code> §12.2–§12.3, <code>08-meta/decisions-log.md</code> (D193, D194, D202, D257), ADR 0112, ADR 0126</li><li><strong>Acceptance</strong>: D (workspace), E (tools &amp; permissions), F (persistence), Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Source/unit covered by <code>apps/desktop/test/agent-capability-settings.test.mjs</code>, <code>apps/desktop/test/extensions-page.test.mjs</code>, <code>apps/desktop/test/subagent-models.test.mjs</code>, <code>apps/desktop/test/subagent-output-limit.test.mjs</code> (the output cap&#39;s path from the editor draft through host-core to the built delegate model), and host-core capability tests; full native-picker, rendered modal, project-switch, and runtime journey remain Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-capability-move-across-levels-mcp-servers-and-skills-move-between-the-global-and-project-agents-levels" tabindex="-1">E2E-CAPABILITY-move-across-levels: MCP servers and skills move between the global and project <code>.agents</code> levels <a class="header-anchor" href="#e2e-capability-move-across-levels-mcp-servers-and-skills-move-between-the-global-and-project-agents-levels" aria-label="Permalink to &quot;E2E-CAPABILITY-move-across-levels: MCP servers and skills move between the global and project \`.agents\` levels&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: The app runs against a disposable global <code>.agents</code> root (an isolated HOME, or <code>PI_DESKTOP_AGENTS_DIR</code> when the bare host is driven directly). Projects A and B are registered. The global root owns <code>~/.agents/servers/echo.json</code> (id <code>echo</code>, label <code>Echo</code>), <code>~/.agents/skills/review.md</code> (frontmatter name <code>Review</code>), and nothing else that uses the names used below. Project A owns no <code>echo</code> server and no <code>review</code> skill, and owns <code>&lt;project A&gt;/.agents/skills/with-resources/</code> holding <code>SKILL.md</code> plus <code>scripts/run.sh</code> and <code>templates/report.md</code>. The MCP and Skills pages show the project picker described in the Settings IA spec.</li><li><strong>Steps</strong>: <ol><li>Open Settings &gt; Agent &gt; MCP with project A selected. Disable the global <code>echo</code> row, then choose Move into A. Confirm the row leaves the global group, appears under project A, and stays disabled there.</li><li>Confirm <code>~/.agents/servers/echo.json</code> no longer exists and <code>&lt;project A&gt;/.agents/servers/echo.json</code> does, byte-identical. Read <code>&lt;data&gt;/agent-capabilities/mcp.json</code> and confirm it holds no global <code>echo</code> entry and no project-A override for the old id, and that project A&#39;s entry carries the disabled value.</li><li>Create a new global <code>echo</code> server again (id <code>echo</code>, label <code>Echo</code>). With project A selected choose Move into A and confirm the arriving row is <code>echo-2</code> with label <code>Echo (2)</code>, that project A&#39;s original <code>echo.json</code> is byte-identical to before the move, and that both project rows list.</li><li>Open Skills with project A selected. Create a project <code>review.md</code> whose frontmatter carries <code>name: Review</code>, a description, and an extra custom field, plus a body; disable it, then choose Move to Global. Confirm it appears globally as <code>review-2</code>, arrives disabled, and leaves the global <code>review.md</code> unchanged.</li><li>Diff the moved document against the project original: only the frontmatter <code>name</code> line differs (<code>name: Review (2)</code>); the description, the custom field, blank lines, line endings, and every body byte are identical.</li><li>Move project A&#39;s <code>with-resources</code> skill to Global. Confirm <code>&lt;project A&gt;/.agents/skills/with-resources/</code> is gone and <code>~/.agents/skills/with-resources/</code> holds <code>SKILL.md</code>, <code>scripts/run.sh</code>, and <code>templates/report.md</code> with identical bytes, and that the Skill tool still resolves the document&#39;s resources.</li><li>Reload the global and project pages. Confirm the moved rows keep the enablement they had across the reload, that project B lists none of the project-A capabilities, and that the moved global <code>review-2</code> applies to project B with the value it had after the move.</li><li>Clear the project selection in the toolbar. Confirm Move into &lt;project&gt; is no longer offered on the remaining global rows and the project group asks for a project selection instead; re-select project B and confirm the action returns.</li><li>Drive <code>mcp.transfer</code> and <code>skills.transfer</code> with <code>from</code> and <code>to</code> naming the same directory and confirm the response returns the current record and neither directory changes.</li></ol></li><li><strong>Expected</strong>: <ul><li>A move relocates the document: the destination directory gains the file, the source level stops listing the entry, and no copy is left behind.</li><li>Enablement follows the document. A global row disabled for project A arrives in project A disabled; a project row arrives globally with its visible value as the new global default. The source level keeps no state entry — including project overrides — for the old id.</li><li>A destination collision is resolved by renaming only the colliding name: an id collision gives the arriving id a <code>-2</code>/<code>-3</code> suffix, a case-insensitive display-name / label collision gives the display name a <code> (2)</code>/<code> (3)</code> suffix, and the existing entry is never overwritten or merged. Renaming a skill rewrites only the frontmatter <code>name</code> line and preserves every other byte.</li><li>A directory-shaped skill moves as one unit with its sibling resources, and a transfer whose two targets name the same directory is a no-op.</li><li>Without a selected project the move action is unavailable with an explicit explanation, and no capability file or state entry changes.</li></ul></li><li><strong>Specs linked</strong>: <code>03-runtime/06-host-rpc-protocol.md</code> §4 (Agent capabilities), <code>03-runtime/01-ipc-protocol.md</code> §12a–§12b, <code>04-ux/06-settings-ia.md</code> §2 (Agent capability destinations), ADR 0112, ADR 0269</li><li><strong>Acceptance</strong>: E (tools &amp; permissions), F (persistence), Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Host-covered (<code>pnpm test:e2e:capability-move</code> drives the same RPC surface Electron main calls against a real host-core and an isolated global root, asserting the documents and the state file on disk; host-core registry/state tests cover the rename, the id the scan derives, and directory resources; the source contract is pinned by <code>apps/desktop/test/agent-capability-settings.test.mjs</code>). The rendered Settings journey — row menu, toggle, toast — remains Draft.</li></ul><h4 id="e2e-120-global-plugin-launch-next-turn-editing-and-stopped-throughput" tabindex="-1">E2E-120: Global plugin launch, next-turn editing, and stopped throughput <a class="header-anchor" href="#e2e-120-global-plugin-launch-next-turn-editing-and-stopped-throughput" aria-label="Permalink to &quot;E2E-120: Global plugin launch, next-turn editing, and stopped throughput&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Install and enable a panel plugin whose Chinese display name is <code>无限画布</code>, plus a second panel plugin (for example <code>邮件助手</code>). Configure a provider that streams slowly enough to stop a partial answer. Run once on macOS and once on Windows.</li><li><strong>Steps</strong>: <ol><li>Immediately after PI-Desktop finishes booting, leave it unfocused and press Option+Space on macOS or Alt+Space on Windows while another application owns the foreground window. Confirm the first invocation promptly reveals a fully rendered, centered launcher on the pointer&#39;s display without a blank initialization frame, native close, minimize, maximize, resize, or taskbar controls. Confirm Windows does not show the active application&#39;s system menu.</li><li>Search separately for <code>无限</code>, <code>wuxianhuabu</code>, and <code>wxhb</code>. Use Up/Down and Enter for one run and click for another; confirm the existing plugin panel opens. Confirm Chinese IME candidate Enter does not open a result.</li><li>Open the launcher again with an empty query and confirm the plugin opened in step 2 is the first result. Open the other panel plugin, then reopen the launcher and confirm the two plugins appear in reverse opening order. Restart the application and confirm the same most-recently-used order survives the restart.</li><li>Start an Agent answer. While it streams, type the next draft and change Thinking, permission mode, and Agent/Plan/Goal. Confirm every selection is editable, Stop remains present, and Send cannot dispatch.</li><li>Stop after partial output. Confirm the partial answer remains, the queued configuration becomes durable only after termination, and the next turn uses the final selection rather than any intermediate selection.</li><li>Inspect the stopped answer&#39;s conversation statistics, reload the session, and inspect again.</li></ol></li><li><strong>Expected</strong>: <ul><li>Early warm-up starts before backend boot completes and removes BrowserWindow and renderer loading from the first shortcut&#39;s visible path. On macOS the panel is activated once without a second app/window-stack hop, so the reveal does not visibly stutter. Launcher search returns only enabled, ready panel plugins and every invocation starts with an empty, focused query ordered by most-recently-used history across restarts; typing a query still ranks relevance first. Escape and focus loss hide it without closing the main application.</li><li>No running turn observes the staged mode/model/thinking/permission change, and a second prompt cannot be sent concurrently.</li><li>Changing Thinking during a stream keeps the selected model&#39;s enabled levels visible. The submenu never collapses to Off-only, including for unpinned sessions that inherit the app default model.</li><li>Stopped throughput is present before and after reload. It uses exact output usage when the provider supplied it; otherwise the UI labels the persisted four-code-point estimate as approximate.</li></ul></li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code>, <code>03-runtime/02-agent-runtime.md</code> §5b/§9, <code>04-ux/07-ui-design-system.md</code> §8.2–8.3, <code>04-ux/08-component-spec.md</code> §11, <code>04-ux/09-interaction-patterns.md</code> §1/§3, D211, D212, D219, ADR 0072, ADR 0073</li><li><strong>Acceptance</strong>: C (conversation &amp; stream), F (persistence), G (plugins), Quality</li><li><strong>Milestone</strong>: M6</li><li><strong>Status</strong>: Unit/source-contract covered; full cross-platform UI journey Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-121-goal-approval-resumes-autonomous-acceptance-criteria-execution" tabindex="-1">E2E-121: Goal approval resumes autonomous acceptance-criteria execution <a class="header-anchor" href="#e2e-121-goal-approval-resumes-autonomous-acceptance-criteria-execution" aria-label="Permalink to &quot;E2E-121: Goal approval resumes autonomous acceptance-criteria execution&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A project-bound session has a configured provider and is idle in Agent mode; the workspace permits host artifact creation and has no prior test goal artifact.</li><li><strong>Steps</strong>: 1) Switch the session to Goal and let the Agent call <code>EnterGoalMode</code>, then <code>SubmitGoal(title, markdown, question)</code>. 2) Inspect the exact Markdown bytes in the new <code>.pi/goal/*.md</code> artifact and the matching <code>plan_approvals</code> row. 3) Confirm the shared approval card exposes only Approve/Reject and that Goal denies Write/Edit/plugin tools while Bash follows the selected permission mode. 4) Approve with Ask and observe the same Agent resume in Agent mode. 5) Inspect the final response for criterion-by-criterion verification or an explicit boundary, then reload the session.</li><li><strong>Expected</strong>: Goal uses the Plan approval pipeline without a second planner; the artifact is immutable and the row records <code>kind = goal</code>, path, hash, size, and execution state. Approval is a separate user decision, transitions the session to Agent, and starts autonomous work only after approval. The transcript remains reviewable after reload and no scheduled/unattended Goal run can bypass the approval boundary.</li><li><strong>Specs linked</strong>: <code>03-runtime/02-agent-runtime.md</code>, <code>03-runtime/03-tools-and-permissions.md</code>, <code>03-runtime/04-data-storage.md</code>, <code>03-runtime/06-host-rpc-protocol.md</code>, <code>03-runtime/10-session-state-machine.md</code>, <code>04-ux/01-ui-ia.md</code>, <code>04-ux/08-component-spec.md</code>, D198</li><li><strong>Acceptance</strong>: C (conversation &amp; stream), E (tools &amp; permissions), F (persistence), H (diagnostics), Security</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Unit/source-contract covered (<code>packages/agent-runtime</code> and host-core Goal tests); full UI journey Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-122-plugins-request-and-deliver-native-notifications" tabindex="-1">E2E-122: Plugins request and deliver native notifications <a class="header-anchor" href="#e2e-122-plugins-request-and-deliver-native-notifications" aria-label="Permalink to &quot;E2E-122: Plugins request and deliver native notifications&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: An installed plugin declares and is granted <code>notify</code>; the desktop platform supports Electron native notifications; the OS notification permission is in its initial or previously denied state.</li><li><strong>Steps</strong>: 1) From the plugin process, call <code>getNotificationPermission()</code>. 2) Call <code>requestNotificationPermission()</code> and observe the native permission/probe result. 3) Call <code>showNativeNotification({ title, body })</code>. 4) Repeat with a plugin that lacks <code>notify</code>, and on a platform where native notifications are unsupported.</li><li><strong>Expected</strong>: The first status is <code>unknown</code>, <code>denied</code>, or <code>unsupported</code>; the request returns a best-effort <code>granted</code>, <code>denied</code>, or <code>unsupported</code> result; a granted plugin receives <code>{ shown: true, permission: &quot;granted&quot; }</code> for native delivery, while denied/unsupported delivery returns <code>shown: false</code> without crashing the plugin. Missing <code>notify</code> fails with <code>PERMISSION_DENIED</code>. Clicking a delivered native plugin notification restores and focuses the main window, but native plugin notifications do not add durable task inbox rows or activate a chat session.</li><li><strong>Specs linked</strong>: <code>07-plugins/01-plugin-system.md</code>, <code>07-plugins/03-plugin-api.md</code>, <code>07-plugins/13-plugin-permissions-matrix.md</code>, ADR 0074</li><li><strong>Acceptance</strong>: E (tools &amp; permissions), G (plugins), Security, Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Unit/source-contract covered; full cross-platform OS permission journey Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-148-plugin-settings-and-local-shortcuts-are-editable" tabindex="-1">E2E-148: Plugin settings and local shortcuts are editable <a class="header-anchor" href="#e2e-148-plugin-settings-and-local-shortcuts-are-editable" aria-label="Permalink to &quot;E2E-148: Plugin settings and local shortcuts are editable&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: An enabled plugin declares string, boolean, JSON, and <code>shortcut</code> settings; the shortcut points to a declared plugin command.</li><li><strong>Steps</strong>: 1) Open Plugins and open the plugin settings surface. 2) Change a normal field and the shortcut, then save. 3) Reload the plugin list and press the new shortcut while the app window is focused. 4) Try a reserved app shortcut and repeat with the plugin scoped away from the current project.</li><li><strong>Expected</strong>: Controls are generated from the manifest, values persist in the plugin-private settings file, and <code>plugin:settingsChanged</code> is delivered. The new shortcut invokes the plugin command only in the focused app window and matching activation scope. Reserved/conflicting bindings are rejected; no OS global shortcut is registered.</li><li><strong>Specs linked</strong>: <code>07-plugins/02-plugin-manifest-schema.md</code>, <code>07-plugins/03-plugin-api.md</code>, <code>07-plugins/11-plugin-storage-isolation.md</code>, ADR 0159</li><li><strong>Acceptance</strong>: F (persistence), G (plugins), Security, Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Source-contract and focused integration coverage; full desktop journey Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-oauth-anthropic-rate-limit-retry-bounded-token-retries-preserve-the-account" tabindex="-1">E2E-OAUTH-anthropic-rate-limit-retry: Bounded token retries preserve the account <a class="header-anchor" href="#e2e-oauth-anthropic-rate-limit-retry-bounded-token-retries-preserve-the-account" aria-label="Permalink to &quot;E2E-OAUTH-anthropic-rate-limit-retry: Bounded token retries preserve the account&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A local HTTP fixture intercepts only Anthropic&#39;s token URL; the production pi-ai flow and Desktop <code>VendorOAuth</code> run with an in-memory Host RPC fixture. No real account, browser authorization or remote endpoint is used.</li><li><strong>Steps</strong>: Run <code>pnpm test:e2e:oauth-retry</code>. Exercise exchange and refresh 429 → success, repeated 429, seconds/date/malformed/over-budget <code>Retry-After</code>, invalid grants (including after 429), 5xx, socket disconnect, invalid success JSON, cancellation during request/body/wait, and an earlier caller deadline. Resolve the same account concurrently and retry after a failed refresh.</li><li><strong>Expected</strong>: At most three requests share one deadline and original signal; no retry precedes a server hint. Only explicit 429 is retried; grant rejection and ambiguous failures stop. Request grant fields and custom headers remain unchanged. Failed refresh retains the old credential and releases its lock; concurrent resolves rotate/write once. Failed/cancelled login removes only its newly created row. HTTP/token-JSON errors contain no token-body canary, URL or embedded stack and provide recovery guidance without claiming every 429 consumes code. Existing network-error diagnostics are unchanged.</li><li><strong>Specs linked</strong>: <code>03-runtime/11-provider-model-system.md</code> §8a; ADR 0095.</li><li><strong>Acceptance</strong>: B (vendor accounts), Security, Quality.</li><li><strong>Status</strong>: All 20 local HTTP scenarios passed against the installed patch; the existing 20 login/session regressions also passed. This is a pre-merge transport and orchestration integration test, not live OAuth, visual UI or Host persistence validation. Post-integration main E2E is NOT RUN.</li></ul><h4 id="e2e-151-multiple-vendor-accounts-stay-isolated-through-login-use-and-removal" tabindex="-1">E2E-151: Multiple vendor accounts stay isolated through login, use, and removal <a class="header-anchor" href="#e2e-151-multiple-vendor-accounts-stay-isolated-through-login-use-and-removal" aria-label="Permalink to &quot;E2E-151: Multiple vendor accounts stay isolated through login, use, and removal&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A build with <code>registerBunOAuthFlows()</code> running at startup and a real subscription for at least one PKCE vendor (Anthropic) and one device-code vendor (xAI, GitHub Copilot, or Meta/Muse). No provider row exists yet for either vendor.</li><li><strong>Steps</strong>: 1) Open Settings -&gt; Model configuration, confirm the Vendor accounts card starts empty, and open Add account — the picker lists every <code>models.getProviders().filter(p =&gt; p.auth.oauth)</code> vendor, including vendors with existing accounts; when accounts exist, confirm their rows use the same single-level list surface as AI services and that Add account matches the primary button treatment of Add provider. 2) Pick Anthropic and complete the browser login; confirm one connected account row and one OAuth provider row appear. 3) Use Add account again, pick Anthropic again, and complete a second login with a different account; confirm two account rows and two provider ids. 4) Edit the first account, change its display name, select multiple catalog/custom models, configure context window, max output tokens, and thinking levels for one model, then save; confirm the row shows the account label once while the Defaults selector reflects the edited model and each option shows only one provider name, without an appended account label or model ID. Before saving, focus the default-model field and confirm the authenticated suggestions open as an app-styled list with aligned model IDs/display names, that typing filters it, ArrowDown/ArrowUp plus Enter selects an option, Escape closes it, and the fixed list is portaled above the dialog without changing dialog height or being clipped by the dialog&#39;s overflow. Confirm a custom model ID can still be entered. Press Test connection and confirm the result resolves the edited account. Open the Composer model menu and confirm the edited account label is used as the OAuth provider group heading, while the configured model alias is shown on its model row. With both Anthropic accounts configured, open the Defaults picker and confirm the one provider name each group shows is that account&#39;s own label, so the two accounts are not two identical <code>Anthropic</code> groups, that the summary line above the Change action names the account holding the current default, and that typing an account label filters to that account&#39;s models while typing the vendor name still reaches both. Repeat the same check on the Settings prompt-enhancement model picker. 5) Resolve and use each account separately, including model discovery and one streamed turn per account. 6) Start the device-code login on a second vendor, including Meta/Muse when available, then press Cancel while the dialog is polling; confirm no row or credential is left. 7) Remove the first Anthropic account, then confirm its provider row and OAuth secret are gone while the second Anthropic account remains usable. 8) If the removed account was default, confirm Defaults points to another ready provider or shows no default. 9) Grep sidecar and renderer logs for token material.</li><li><strong>Expected</strong>: Each successful login creates a distinct row with <code>authKind: &quot;oauth&quot;</code>, <code>hasSecret</code> and <code>hasOauth</code> both true, a non-secret account label, and <code>baseUrl</code>/<code>apiStyle</code>/<code>defaultModelId</code> filled from that account&#39;s own catalog. Each row has its own <code>secret:provider:&lt;providerId&gt;:oauth</code> ref and row-scoped pi-ai collection; resolving one account never returns the other account&#39;s token. The model list is the authenticated catalog (a Copilot account lists only what its subscription includes; a Meta account lists Muse Spark models), not a <code>/models</code> probe. Matching models.dev metadata supplies each newly logged-in binding&#39;s limits, modalities, and thinking levels; an ID missing from models.dev uses the conservative generic text-only/non-reasoning shape. label/model fields and the full per-model bindings, and Test connection resolves that exact account. Both turns run without a pasted key and reuse the same warm runtime — the launch payload carries <code>apiKey: &quot;&quot;</code> and each request resolves auth through <code>provider.resolveAuth</code>, which Electron main answers locally and refuses with <code>PROVIDER_NOT_BOUND</code> for an unbound provider id. Cancel aborts the local callback server / device-code polling and leaves no row and no credential. The connection test proves the account by resolving auth rather than probing with a key. Remove account delegates to <code>providers.delete</code>, clearing both secret refs and metadata for exactly that row. No log, event, or IPC payload contains an access token, refresh token, or authorization code.</li><li><strong>Specs linked</strong>: <code>03-runtime/11-provider-model-system.md</code> §8a, <code>03-runtime/12-provider-config-schema.md</code> §3, <code>03-runtime/14-secrets-storage.md</code> §10, <code>03-runtime/01-ipc-protocol.md</code> §8, <code>04-ux/06-settings-ia.md</code>, <code>08-meta/decisions-log.md</code> (D237/D240), ADR 0095, ADR 0098</li><li><strong>Acceptance</strong>: B (model config), C (conversation &amp; stream), F (persistence), Security, Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Unit coverage in <code>packages/agent-runtime</code> (auth resolution and runtime reuse) and host-core secret-ref tests; full desktop journey Draft and needs live vendor accounts (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-197-github-copilot-accepts-the-default-enterprise-domain" tabindex="-1">E2E-197: GitHub Copilot accepts the default Enterprise domain <a class="header-anchor" href="#e2e-197-github-copilot-accepts-the-default-enterprise-domain" aria-label="Permalink to &quot;E2E-197: GitHub Copilot accepts the default Enterprise domain&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A build with the GitHub Copilot OAuth flow registered; the device-code login can reach the GitHub endpoint.</li><li><strong>Steps</strong>: 1) Open Settings -&gt; Model configuration -&gt; Add account and choose GitHub Copilot. 2) Leave the Enterprise URL/domain field empty. 3) Confirm Continue is enabled and submit the empty value. 4) Complete the device-code login and inspect the resulting account row.</li><li><strong>Expected</strong>: The empty text prompt is submitted as an empty string, the provider uses github.com, and the device-code flow completes normally. Secret and manual-code prompts remain disabled while empty. No OAuth token or device credential is rendered or logged.</li><li><strong>Specs linked</strong>: <code>04-ux/06-settings-ia.md</code>, <code>04-ux/08-component-spec.md</code> §19, <code>03-runtime/12-provider-config-schema.md</code> §3, <code>08-meta/decisions-log.md</code> (D237/D240)</li><li><strong>Acceptance</strong>: B (model config), Security, Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Unit-covered (<code>apps/desktop/test/oauth-login-prompt.test.mjs</code>); live device-code journey Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-152-a-plugin-contributes-a-work-panel-view" tabindex="-1">E2E-152: A plugin contributes a work panel view <a class="header-anchor" href="#e2e-152-a-plugin-contributes-a-work-panel-view" aria-label="Permalink to &quot;E2E-152: A plugin contributes a work panel view&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A development plugin declaring <code>ui.view</code> and one <code>contributes.views</code> entry whose <code>entry</code> is a small HTML page calling <code>window.pluginBridge.invoke(&quot;ui.showToast&quot;, …)</code>. Two projects open, with the plugin&#39;s activation scope limited to the first.</li><li><strong>Steps</strong>: <ol><li>Load the plugin as a development plugin. Confirm the Plugins page shows a work-panel-views capability badge.</li><li>Press <code>Cmd/Ctrl + J</code> to reveal the work panel, then click <code>+</code> to create a New launcher tab. Confirm the fixed <code>+</code> trigger, the maximize control, and the viewport-fixed work-panel toggle resolve to one button group — one 4px control gap, no divider between maximize and the toggle — while the <code>+</code> trigger and the toggle stay separate, non-overlapping hit regions with more than 24px of visual gap. Confirm the launcher lists the built-in Review row and the plugin view&#39;s localized title and icon (or a lettered tile if the manifest names an unknown token).</li><li>Activate the plugin row. Confirm the plugin&#39;s page renders inside the panel body with no window-control capsule and no reserved 46px band, and that its button reaches the host toast.</li><li>Drag the inner panel divider and resize the conversation area. Confirm the page tracks the panel rect without lag or tearing, then drag the outer right window edge and confirm the panel width changes while the base chat width stays fixed.</li><li>Open global search, then Settings. Confirm the page is hidden while each overlay is up and returns when it closes.</li><li>Click <code>+</code> again, then choose the same view from the new launcher. Confirm it returns to the live page — same scroll position, no reload — rather than stacking a second tab.</li><li>Switch to the second project. Confirm the view disappears from the New launcher and from the no-tab entry list.</li><li>Switch back, reopen the view, then disable the plugin. Confirm the tab closes and the view&#39;s renderer process exits (Activity Monitor / Task Manager).</li><li>Re-enable, reopen, then edit the plugin&#39;s HTML on disk to trigger a development reload. Confirm the view reloads rather than going blank.</li></ol></li><li><strong>Expected</strong>: A plugin view is reachable, isolated, correctly positioned, and bounded by the plugin&#39;s lifecycle and activation scope. The panel remains an in-flow internal column; its renderer-owned divider resizes the panel and native window edges never change that target. It never renders while a blocking overlay is open, and it never obtains window controls.</li><li><strong>Specs linked</strong>: <code>07-plugins/02-plugin-manifest-schema.md</code> §4/§5, <code>07-plugins/13-plugin-permissions-matrix.md</code> §2, <code>04-ux/08-component-spec.md</code> §5, ADR 0104, ADR 0092</li><li><strong>Acceptance</strong>: G (plugins), Security, Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Unit coverage in <code>apps/desktop/test/plugin-work-panel-views.test.mjs</code> (addressing, launcher grouping, isolation parity, scope filtering, lifecycle teardown), <code>packages/plugin-sdk</code> and host-core manifest validation; the desktop journey is Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-153-the-vendored-file-manager-replaces-the-built-in-files-tool" tabindex="-1">E2E-153: The vendored file manager replaces the built-in Files tool <a class="header-anchor" href="#e2e-153-the-vendored-file-manager-replaces-the-built-in-files-tool" aria-label="Permalink to &quot;E2E-153: The vendored file manager replaces the built-in Files tool&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A packaged build (so <code>resources/plugins</code> is copied outside the asar) and a project with nested directories, a <code>node_modules</code>, a <code>.env</code>, a binary file, an image, a CSV, and a Markdown file. The project is a group whose second folder is registered as another root (ADR 0249).</li><li><strong>Steps</strong>: <ol><li>Open the Plugins page. Confirm <strong>File Manager</strong> is listed as a bundled plugin, enabled, showing a work-panel-views capability, and that it offers no Uninstall action.</li><li>Reveal the work panel and click <code>+</code> to create a New launcher tab. Confirm its rows include Review and the plugin-contributed File Manager and Browser views. Trigger an agent edit and confirm the panel does not open itself: Review appears under Open resources only after the user selects its row.</li><li>Open the File Manager view. Confirm the tree lists the project, expands directories lazily, and omits <code>node_modules</code>, <code>.git</code>, and <code>.env</code>. Switch the view&#39;s top-left folder control to the project&#39;s second folder and confirm the tree follows it while the app&#39;s visible workspace does not, then switch back to the project&#39;s primary folder.</li><li>Right-click a file and confirm <strong>Open with default app</strong> and <strong>Show in folder</strong> are offered and work; right-click a directory and confirm they are not offered, because the host refuses the action for directories.</li><li>Open a text file, edit it, and save. Confirm the file on disk changed and the editor keeps the saved content. Change the same file outside the app, edit and save again, and confirm the conflict is reported instead of the external change being overwritten. Click the binary file and confirm it reports as unsupported rather than printing replacement characters; open the image, the CSV, and the Markdown file and confirm each gets its own viewer. Switch the app to Simplified Chinese and confirm the tree, viewer, and context menu are localized. Switch projects and confirm the tree updates without waiting on a poll.</li><li>Click a project file path in the conversation. Confirm it opens in this view on that file — a chat click now prefers the file view over the host <code>file:</code> tab.</li><li>Disable the File Manager plugin. Confirm the view disappears from the menu and the panel, and that a clicked conversation file path falls back to the host <code>file:&lt;path&gt;</code> tab under Open resources.</li><li>Re-enable it, then restart the app. Confirm the enabled state and the tree return, and that the registry did not gain a duplicate row.</li></ol></li><li><strong>Expected</strong>: A panel surface runs entirely on the public plugin contribution channel, is user-disableable, cannot be uninstalled, and survives restart. Its host-mediated actions obey the declared <code>fs.read</code> scope, and its own reads and writes stay inside the jail of the one project folder it is browsing (ADR 0241, ADR 0263).</li><li><strong>Specs linked</strong>: <code>07-plugins/03-plugin-api.md</code> §3, <code>07-plugins/13-plugin-permissions-matrix.md</code> §2, <code>04-ux/08-component-spec.md</code> §5, ADR 0104, ADR 0109, ADR 0111, ADR 0169, ADR 0241, ADR 0249, ADR 0263</li><li><strong>Acceptance</strong>: G (plugins), D (workspace), Security, Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Unit coverage in <code>apps/desktop/test/bundled-plugins.test.mjs</code> (manifest contract, public-bridge-only page, vendored checksums), <code>apps/desktop/test/plugin-fs-scope.test.mjs</code> (<code>fs.openDefault</code> and <code>fs.reveal</code> guards), <code>apps/desktop/test/plugin-work-panel-views.test.mjs</code> (docked-view event broadcast), and host-core <code>bundled_plugins_refresh_from_disk_but_keep_user_state</code>; the packaged journey is Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-plugin-file-view-collapse-persists" tabindex="-1">E2E-PLUGIN-file-view-collapse-persists <a class="header-anchor" href="#e2e-plugin-file-view-collapse-persists" aria-label="Permalink to &quot;E2E-PLUGIN-file-view-collapse-persists&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: The bundled File Manager view is open on a project with nested folders and a text file, and the work panel is wide enough for the two-pane layout.</li><li><strong>Steps</strong>: <ol><li>Drag the divider between the file list and the content pane to a non-default width, then activate the toolbar&#39;s left-most toggle from the keyboard alone (Tab to it, then Enter or Space).</li><li>Click a file reference in the conversation.</li><li>Activate the same toggle again.</li><li>Collapse the file list, then close and reopen the view, and finally restart the app.</li><li>Expand the file list by hand and click another chat file reference.</li></ol></li><li><strong>Expected</strong>: The toggle hides the view&#39;s own left file list and gives the content pane the full width, and it stays keyboard-reachable with an accessible name that swaps between <code>Hide file list</code> and <code>Show file list</code>. The open request that follows a chat click shows the requested file with its ancestor folders expanded and the file list collapsed, whether the view was already open or was opened by that click. Expanding again restores the previously dragged split width together with the expanded folders and the selected file, not the default split or the project root. The collapsed state is persisted: it survives closing and reopening the view and a full app restart, and a manual expansion holds until the next host open request collapses it again.</li><li><strong>Specs linked</strong>: <code>07-plugins/02-plugin-manifest-schema.md</code> §4/§5, <code>04-ux/08-component-spec.md</code> §5.2.2, ADR 0104, ADR 0241, ADR 0262</li><li><strong>Acceptance</strong>: G (plugins), Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: The bundle&#39;s manifest, entry page, and upstream checksum are unit-covered (<code>apps/desktop/test/bundled-plugins.test.mjs</code>); the plugin-side journey is Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-plugin-file-view-switches-folder-per-project" tabindex="-1">E2E-PLUGIN-file-view-switches-folder-per-project <a class="header-anchor" href="#e2e-plugin-file-view-switches-folder-per-project" aria-label="Permalink to &quot;E2E-PLUGIN-file-view-switches-folder-per-project&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A project group whose two folders each hold a text file the other folder does not, and a second, single-folder project. The work panel is open on the bundled File Manager view, browsing the first project&#39;s primary folder.</li><li><strong>Steps</strong>: <ol><li>Switch the view&#39;s top-left folder control to the project&#39;s second folder. Confirm the tree, the filename search, and open/save all follow it.</li><li>Save an edit in the text file only that second folder holds.</li><li>Close and reopen the view, then restart the app. Confirm the second folder is still the one being browsed.</li><li>Switch to the other project and back. Confirm each project remembers its own folder.</li><li>Click a chat reference that resolves in the project&#39;s second folder, then one that resolves in its primary folder.</li><li>With the second folder selected, search the tree for the primary folder&#39;s own file name, and try to open a <code>.env</code> and a symlink or junction inside the second folder that points outside it. 7) With the second folder selected, right-click the text file only that folder holds and use <strong>Open with default app</strong>, then <strong>Show in folder</strong>; switch to the primary folder and do the same for a file only <em>it</em> holds. 8) Back in the second folder, use the same two actions on a file whose name both folders contain.</li></ol></li><li><strong>Expected</strong>: <ul><li>The control lists the project&#39;s folders in group order, primary first, and names the one being browsed; the tree, search, and editing all work inside that one folder, and a single-folder project offers just its one folder.</li><li>Switching folders changes only what this view browses: the app&#39;s visible workspace, the agent&#39;s tool roots, the session&#39;s primary path, project instructions, and project memory are all unchanged (ADR 0263).</li><li>The choice is remembered per project: it survives closing and reopening the view and a full app restart, and the other project keeps its own folder.</li><li>Both chat references open in this view on the file they name — the one from the second folder included, in that folder — with no host <code>file:</code> tab.</li><li>The selected folder is the jail, not the group: the filename search does not reach a file that only another project folder holds, and the credential path and the symlink/junction escape are still refused (ADR 0241).</li><li>The two system actions reach the file that was clicked, in the folder being browsed: a file only the second folder holds opens or reveals its real self instead of reporting &quot;not found&quot;, and the file whose name both folders share opens the second folder&#39;s copy, not the primary folder&#39;s (ADR 0264).</li></ul></li><li><strong>Specs linked</strong>: <code>07-plugins/03-plugin-api.md</code> §3, <code>04-ux/08-component-spec.md</code> §5.2.2, ADR 0241, ADR 0249, ADR 0263, ADR 0264</li><li><strong>Acceptance</strong>: G (plugins), Security, Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: The host-side resolution and addressing are unit-covered (<code>apps/desktop/test/chat-ref-resolve.test.mjs</code>, <code>apps/desktop/test/transcript-file-chips.test.mjs</code>); the plugin-side journey is Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-plugin-bundled-plugin-keeps-a-marketplace-update" tabindex="-1">E2E-PLUGIN-bundled-plugin-keeps-a-marketplace-update <a class="header-anchor" href="#e2e-plugin-bundled-plugin-keeps-a-marketplace-update" aria-label="Permalink to &quot;E2E-PLUGIN-bundled-plugin-keeps-a-marketplace-update&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A build that ships a bundled plugin whose marketplace entry offers a newer version, and a data directory the user can install into.</li><li><strong>Steps</strong>: <ol><li>Open the Plugins page. Confirm the plugin is listed as bundled and enabled, and that it offers no Uninstall action.</li><li>Update it from the marketplace. Confirm the row moves to the catalog version and still offers no Uninstall action.</li><li>Restart the app. Confirm the updated version is still the installed one, the plugin is still enabled, and the registry holds exactly one row for it.</li><li>Disable the plugin, restart again, and confirm it stays disabled at the updated version.</li></ol></li><li><strong>Expected</strong>: Bundled means default and non-removable, not frozen. A user&#39;s update outlives the launch that reconciles the shipped copy, an app that ships a strictly newer version still wins, and a catalog version that is not newer is never offered as an update.</li><li><strong>Specs linked</strong>: <code>07-plugins/07-plugin-marketplace.md</code>, ADR 0104, ADR 0241</li><li><strong>Acceptance</strong>: G (plugins), Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Unit coverage in host-core <code>a_bundled_plugin_keeps_the_update_the_user_installed</code>, <code>a_newer_shipped_version_replaces_an_older_user_install</code>, <code>a_plugin_a_build_stops_shipping_is_no_longer_bundled</code>, and <code>market_entry_offers_an_update_only_when_the_catalog_is_newer</code>; the packaged journey is Draft</li></ul><h4 id="e2e-154-model-additions-use-models-dev-metadata-and-generic-unknown-ids" tabindex="-1">E2E-154: Model additions use models.dev metadata and generic unknown IDs <a class="header-anchor" href="#e2e-154-model-additions-use-models-dev-metadata-and-generic-unknown-ids" aria-label="Permalink to &quot;E2E-154: Model additions use models.dev metadata and generic unknown IDs&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A custom provider dialog matches a models.dev provider/API URL and exposes at least two model records with limits, modalities, and reasoning options. A deterministic fixture also exposes a provider-discovered ID absent from models.dev.</li><li><strong>Steps</strong>: <ol><li>Select two models from the models.dev list and inspect their names, context/output limits, capability badges, source labels, and thinking chips. 2. Save the provider and start a session; confirm the request keeps the provider&#39;s configured base URL/API style. 3. Force a Settings catalog refresh and confirm it refetches models.dev without changing the bundled release file or writing a user cache. 4. Add an ID absent from models.dev and inspect its generic fallback card.</li></ol></li><li><strong>Expected</strong>: models.dev fields prefill known model bindings and remain the sole metadata source. Provider keys are never included in the fixed models.dev request. Provider discovery remains available only to supply custom/account-specific IDs; those IDs receive the generic text-only, non-reasoning defaults. pi-ai supplies the selected transport and OAuth/account availability, not model metadata.</li><li><strong>Specs linked</strong>: <code>03-runtime/11-provider-model-system.md</code> §6.2, <code>03-runtime/13-model-catalog-and-selection.md</code> §11.1–§12, ADR 0134</li><li><strong>Acceptance</strong>: B (model config), C (conversation &amp; stream), Security</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Unit/source-contract covered; full provider-dialog journey Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-nav-plugins-button-goes-back-plugins-footer-reuses-navigation-history" tabindex="-1">E2E-NAV-plugins-button-goes-back: Plugins footer reuses navigation history <a class="header-anchor" href="#e2e-nav-plugins-button-goes-back-plugins-footer-reuses-navigation-history" aria-label="Permalink to &quot;E2E-NAV-plugins-button-goes-back: Plugins footer reuses navigation history&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: An isolated profile has a named conversation and visible messages. No model credentials or external marketplace access are required.</li><li><strong>Steps</strong>: 1) Select the conversation, type an unsent draft, enter Plugins from the footer, and click the same button again. 2) Reopen Plugins, type a search in Installed, return and reopen. 3) In the navigation fixture, repeat from <code>pulls</code>, <code>scheduled</code>, and Settings; test a history containing both Scheduled and Settings before Plugins. 4) Exercise Forward then the Plugins button again. 5) Open Plugins with no previous history entry and click it.</li><li><strong>Expected</strong>: The second click performs the existing Back action exactly once; it does not append a return entry or skip Settings. A chat history entry uses existing session selection/loading behavior. The conversation and unsent draft remain usable on return. If Back is unavailable, the button opens chat. Its pressed state reflects whether Plugins is active. Plugin browsing tab, search fields, and category survive route unmounts; dialogs and listeners are released rather than kept hidden. Settings navigation is unchanged.</li><li><strong>Specs linked</strong>: <code>04-ux/01-ui-ia.md</code> §2/§5, <code>04-ux/08-component-spec.md</code> §3</li><li><strong>Acceptance</strong>: C (session navigation), G (plugin browsing), Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Actual footer-handler/history-slice and browse-state regression tests passed. Real macOS UI validation of the rebuilt history-based revision passed two open/back cycles: the same two conversation messages and unsent draft remain, the button clears its active state on return, and reopening retains the Installed search filter. Non-chat history entries, Forward, and no-history fallback are covered by tests, not native UI. The layout journey also activates the footer button a second time and asserts the previous destination; it passed 37/37 checks from integrated main <code>d6ffaa3b</code>. No external marketplace or live model was required.</li></ul><h4 id="e2e-provider-custom-form-excludes-account-formats-keep-account-apis-out-of-new-custom-services" tabindex="-1">E2E-PROVIDER-custom-form-excludes-account-formats: Keep account APIs out of new custom services <a class="header-anchor" href="#e2e-provider-custom-form-excludes-account-formats-keep-account-apis-out-of-new-custom-services" aria-label="Permalink to &quot;E2E-PROVIDER-custom-form-excludes-account-formats: Keep account APIs out of new custom services&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Isolated profile, English and Simplified Chinese. Seed non-OAuth legacy rows using Codex and Pi account formats, including an OpenAI preset URL; use synthetic models and no real credentials.</li><li><strong>Steps</strong>: 1) Add a Custom service and inspect its format choices. 2) Edit each legacy row and save unchanged, then explicitly select Responses and save. 3) Copy each original legacy row, inspect the current format and explanatory hint, wait past discovery debounce, and cancel. 4) Copy again, choose Anthropic Messages and save. 5) Check saved payloads and the unchanged source row.</li><li><strong>Expected</strong>: New custom choices are the four general protocols; Codex and Pi are available through vendor accounts, not as new API-key choices. Legacy editing preserves format, name, URL and authentication unless explicitly changed. A copied account format is visible but cannot be newly selected; saving and discovery are blocked until an explicit supported choice. The explanation is localized. Cancel performs no create; a valid copy never uses the source id or credentials. Named OpenCode Go and OAuth account flows remain unchanged.</li><li><strong>Automation</strong>: <code>pnpm test:e2e:provider-api-style</code> renders the production React form in Electron/Chromium with a stubbed API boundary and checks exact create, update and discovery payloads. This does not verify Host storage or live OAuth.</li><li><strong>Specs linked</strong>: <code>03-runtime/12-provider-config-schema.md</code>, ADR 0095.</li><li><strong>Acceptance</strong>: B (model configuration), Security.</li><li><strong>Status</strong>: Helper/copy regressions passed. The branch Electron/React scenario passed in English and Simplified Chinese: six scenario groups, four creates and eight updates through the stubbed API. Host persistence, live OAuth/model calls and visual layout were not exercised. Post-integration main E2E is NOT RUN.</li></ul><h4 id="e2e-provider-copy-config-without-credentials-copy-configuration-into-an-independent-provider" tabindex="-1">E2E-PROVIDER-copy-config-without-credentials: Copy configuration into an independent provider <a class="header-anchor" href="#e2e-provider-copy-config-without-credentials-copy-configuration-into-an-independent-provider" aria-label="Permalink to &quot;E2E-PROVIDER-copy-config-without-credentials: Copy configuration into an independent provider&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Settings contains an ordinary provider with a saved API key, custom headers, and two model bindings with distinct aliases, limits, thinking levels, and modality overrides; an OAuth account also exists. Record the source configuration and global default provider/model. Use a deterministic endpoint to capture discovery requests without real secrets.</li><li><strong>Steps</strong>: 1) Copy the ordinary provider. 2) Confirm the custom-service draft retains the name, URL, API format, and model bindings while the key and custom headers are blank and an omission notice is visible. 3) Change the API format, a model alias/limit, and its thinking levels; cancel. 4) Confirm provider count, source data, and global defaults are unchanged. 5) Copy again and trigger discovery before entering a new key, then with a distinct fixture key. 6) Save under a distinct name with the changed API format. 7) Reopen both providers and edit the copy. 8) Inspect the OAuth account row for absence of Copy. 9) In the draft-construction fixture, add unknown source/model fields and verify they are not copied. 10) Copy an OpenCode Go provider, confirm its named service and fixed format are kept, then select Custom service and choose another ordinary API format.</li><li><strong>Expected</strong>: Cancel creates no provider or secret. Draft model objects and thinking arrays do not share references with the source. Discovery and connection testing do not use the source provider id or stored credential; authenticated discovery uses only the new draft key. Copy never reads the secret store. Save creates a distinct provider through the existing create path with independent model bindings and credentials, while the source and global defaults remain unchanged. Custom headers and unknown fields are omitted even if they contain credential-like values. OAuth accounts cannot be copied through this action.</li><li><strong>Specs linked</strong>: <code>03-runtime/12-provider-config-schema.md</code>, <code>03-runtime/14-secrets-storage.md</code></li><li><strong>Acceptance</strong>: B (model configuration), F (independent persistence), Security</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Real Host/helper fixture verified independent creation, edits, deletion, retained source credentials/defaults, and restart persistence. Actual UI validation on an isolated no-secret profile confirmed cancel leaves one provider; saving a copy with Responses changed to Anthropic Messages and a changed name/alias creates a second provider without changing the global default. Reopening both rows confirmed the source retained Responses and its original alias, while the copy retained Anthropic Messages and its edited alias. Credential-bearing network discovery, external-model calls, and the OpenCode Go UI variant were not exercised.</li></ul><h4 id="e2e-plugin-global-shortcut-owns-only-its-own-command" tabindex="-1">E2E-PLUGIN-global-shortcut-owns-only-its-own-command <a class="header-anchor" href="#e2e-plugin-global-shortcut-owns-only-its-own-command" aria-label="Permalink to &quot;E2E-PLUGIN-global-shortcut-owns-only-its-own-command&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Two local fixture plugins are installed in an isolated profile. Plugin A declares <code>keyboard.globalShortcut</code>, <code>contributes.commands</code> with one command, and <code>contributes.globalShortcuts</code> mapping <code>Alt+Shift+V</code> to it. Plugin B declares the same permission and asks for <code>Alt+Shift+V</code> while A holds it. A second application is available for a cross-application keypress.</li><li><strong>Steps</strong>: 1) Install and load A and confirm the accelerator is held through <code>listGlobalShortcuts</code>. 2) Focus another application, press <code>Alt+Shift+V</code>, and confirm A&#39;s command runs; drive the registered handler through the host&#39;s test seam when no cross-application input is available. 3) Attempt a shortcut whose <code>command</code> belongs to another plugin. 4) Attempt B&#39;s registration of <code>Alt+Shift+V</code> and inspect the answer. 5) Disable and uninstall A and confirm the accelerator becomes free and B can take it; repeat after terminating A&#39;s runtime (crash) and while A&#39;s panel is closed. 6) Attempt the app&#39;s own launcher accelerator <code>Alt+Space</code>, the <code>Alt+Shift+W</code> window toggle, a reserved binding such as <code>Mod+C</code>, an invalid accelerator, and a ninth shortcut for one plugin.</li><li><strong>Expected</strong>: Only A&#39;s own command runs for the accelerator; a shortcut whose <code>command</code> is not registered by the plugin is refused with <code>INVALID_ARGUMENT</code>. B receives a refusal (<code>registered: false</code>, code <code>SHORTCUT_CONFLICT</code>) and keeps no accelerator while A holds it. The host&#39;s own <code>Alt+Space</code> launcher and <code>Alt+Shift+W</code> window-toggle shortcuts and OS-reserved bindings are refused with <code>SHORTCUT_CONFLICT</code> or <code>SHORTCUT_UNAVAILABLE</code>; an invalid accelerator is refused with <code>INVALID_ACCELERATOR</code> and the ninth per-plugin shortcut with <code>LIMIT_EXCEEDED</code>. Disabling, unloading, or crashing a plugin releases every accelerator it held, after which another plugin can take it, and app quit releases all shortcuts. Audit records each register/unregister attempt with the plugin id and result, and never records key contents.</li><li><strong>Specs linked</strong>: <code>07-plugins/03-plugin-api.md</code>, <code>07-plugins/04-plugin-security.md</code>, <code>07-plugins/13-plugin-permissions-matrix.md</code>, ADR 0257</li><li><strong>Acceptance</strong>: G (plugins), Security, Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Registry, host wiring, and manifest validation are unit-covered (<code>apps/desktop/test/plugin-shortcuts.test.mjs</code>, <code>packages/plugin-sdk/src/index.test.ts</code>, <code>crates/host-core/src/plugins/tests.rs</code>); the cross-application keypress journey is Draft and needs a capable desktop environment.</li></ul><h4 id="e2e-plugin-permission-gate-for-real-time-capabilities" tabindex="-1">E2E-PLUGIN-permission-gate-for-real-time-capabilities <a class="header-anchor" href="#e2e-plugin-permission-gate-for-real-time-capabilities" aria-label="Permalink to &quot;E2E-PLUGIN-permission-gate-for-real-time-capabilities&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A fixture plugin whose manifest can be changed between runs, and a data directory in which the user can re-grant permissions.</li><li><strong>Steps</strong>: 1) Install a build that requires <code>keyboard.globalShortcut</code> and inspect the install dialog. 2) Load a plugin that calls <code>pi.keyboard.listGlobalShortcuts()</code> without declaring the permission. 3) Declare but do not grant the permission, then call the same API. 4) Grant the permission and call the same API again. 5) Revoke the permission with the plugin loaded and press the accelerator. 6) Repeat steps 2–5 for <code>audio.capture.background</code>, <code>audio.playback.background</code>, and <code>net.websocket</code>.</li><li><strong>Expected</strong>: Undeclared and declared-but-ungranted calls are refused with <code>PERMISSION_DENIED</code> and an audit entry, so an ungranted capability fails closed instead of degrading. After a grant the same call is allowed — for <code>net.websocket</code> that is a connect that proceeds past the gate — while the callable audio methods answer a coded <code>UNSUPPORTED</code> because the host has no device backend yet, audited as <code>{ api: &quot;audio.&lt;method&gt;&quot;, ok: false, errorCode: &quot;UNSUPPORTED&quot; }</code>, never a silent success. Revoking the permission with the plugin loaded stops the accelerator immediately and the host releases it. The four permissions appear with their risk tiers (high for <code>audio.capture.background</code> and <code>net.websocket</code>, medium for <code>audio.playback.background</code> and <code>keyboard.globalShortcut</code>) and localized copy in the install dialog and in the plugin detail sheet, ordered by descending risk.</li><li><strong>Specs linked</strong>: <code>07-plugins/04-plugin-security.md</code>, <code>07-plugins/13-plugin-permissions-matrix.md</code>, ADR 0257</li><li><strong>Acceptance</strong>: G (plugins), Security</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: The gates and validator behaviour are unit-covered (<code>packages/plugin-sdk/src/index.test.ts</code>, <code>packages/plugin-devkit/src/check.test.ts</code>, <code>crates/host-core/src/plugins/tests.rs</code>); the dialog journey is Draft.</li></ul><h4 id="e2e-plugin-background-audio-and-realtime-connection" tabindex="-1">E2E-PLUGIN-background-audio-and-realtime-connection <a class="header-anchor" href="#e2e-plugin-background-audio-and-realtime-connection" aria-label="Permalink to &quot;E2E-PLUGIN-background-audio-and-realtime-connection&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A voice-assistant fixture plugin holding <code>background.service</code>, <code>audio.capture.background</code>, <code>audio.playback.background</code>, <code>keyboard.globalShortcut</code>, <code>net.websocket</code>, and <code>desktop.control</code>, with a mock audio backend and a local mock WebSocket server so CI never needs a real microphone.</li><li><strong>Steps</strong>: 1) Load the plugin with no panel open and confirm that its background service starts. 2) Register the push-to-talk accelerator. 3) Open the input device and receive PCM frames. 4) Connect the mock server and send PCM. 5) Receive the response audio and play it. 6) Interrupt playback with <code>stopOutput</code>. 7) Call <code>pi.desktop.invoke({ operation: &quot;session/create&quot; })</code> and then <code>agent/prompt</code>. 8) Unload the plugin.</li><li><strong>Expected</strong>: Frames flow with bounded buffering and no unbounded queue growth, and <code>stopOutput</code> clears queued audio immediately. Egress to a host outside <code>manifest.net.domains</code> is refused. On unload the microphone, the audio output, the socket, and the accelerator are all released with no orphan process, listener, or timer. <code>session/delete</code>-class dangerous desktop operations still require the host&#39;s native confirmation even when the plugin passes <code>confirm: true</code>.</li><li><strong>Specs linked</strong>: <code>07-plugins/03-plugin-api.md</code>, <code>07-plugins/04-plugin-security.md</code>, <code>07-plugins/12-plugin-ipc-and-host-services.md</code>, ADR 0257</li><li><strong>Acceptance</strong>: G (plugins), Security, Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Partially implemented — the socket half is implemented and unit-covered by <code>apps/desktop/test/plugin-websocket.test.mjs</code> (allowlist, bounds, lifecycle); the background-audio half answers a coded <code>UNSUPPORTED</code> because the host has no device backend yet, so this scenario stays Draft until audio lands.</li></ul><h3 id="e2e-config-sync-webdav-portable-configuration" tabindex="-1">E2E-CONFIG-SYNC-webdav-portable-configuration <a class="header-anchor" href="#e2e-config-sync-webdav-portable-configuration" aria-label="Permalink to &quot;E2E-CONFIG-SYNC-webdav-portable-configuration&quot;">​</a></h3><ul><li><strong>Preconditions:</strong> A built task candidate, isolated host profile, and a local WebDAV fixture that supports strong ETags and conditional PUT, plus a fixture variant that ignores conditional headers but supports <code>PROPFIND</code> directory listing. No real WebDAV account, provider, or production desktop.</li><li><strong>Steps:</strong> 1) Open Settings → Cloud sync and enter the fixture URL, device label, directory, and backup password. 2) Run the capability test and confirm it uses temporary objects. 3) Select provider/MCP/skill categories while leaving credentials and memory off; enable credentials in a second preview and verify only redacted counts are shown. 4) Configure device A, create a user provider and MCP definition, and sync. 5) Configure device B against the same vault, sync, inspect pending activation/mapping, and verify no command or task runs before approval. 6) Approve a changed safe entity, reject one staged entity, edit disjoint settings on both devices, and sync again. 7) Exercise a concurrent head writer, wrong password, weak ETag, ciphertext corruption, redirect, archive traversal, and network interruption. 8) On the ignored-precondition fixture, select explicit compatibility mode, confirm the warning, and configure two devices. Verify each device publishes its own encrypted head under the heads collection, a concurrent update is merged from both tips, and compatibility mode does not delete immutable history. Cancel the confirmation once and verify configuration is not saved. 9) Use the explicit LAN HTTP acknowledgement with a loopback/private fixture, verify the setting survives a state refresh, and confirm a public HTTP endpoint is rejected even when the checkbox is selected.</li><li><strong>Expected:</strong> Strict mode refuses unreliable conditional writes. The explicit compatibility mode accepts only a server that proves bounded directory listing, explains that it is not atomic CAS, and retains per-device tips for merge/recovery. A server that ignores conditional headers is never accepted without that user selection. HTTP remains opt-in and is accepted only for localhost, <code>.local</code>, or private/link-local addresses; public HTTP endpoints are rejected and the UI explains the credential exposure risk. A fixture that reports a missing object as 502 is accepted only after the capability probe observes that endpoint-specific behavior. WebDAV sees only authenticated ciphertext and opaque object names; raw secrets never appear in renderer state or logs. Identical and disjoint edits converge, conflicts remain reviewable, explicit deletions use tombstones, category opt-out is not deletion, and executable imports remain inactive until local approval and mapping. Recovery never exposes a partial local apply.</li><li><strong>Specs:</strong> <code>03-runtime/22-config-sync.md</code>, <code>03-runtime/14-secrets-storage.md</code>, <code>05-security/01-security.md</code>, ADR 0300, ADR 0301.</li><li><strong>Acceptance:</strong> F (persistence), Security, Quality.</li><li><strong>Milestone:</strong> M6+.</li><li><strong>Status:</strong> Draft; merge/crypto and in-process WebDAV conditional-write coverage exists. The remaining automation is the full two-device process path and checkpoint-level local recovery fault injection.</li></ul><h2 id="_8-traceability-matrix" tabindex="-1">8. Traceability Matrix <a class="header-anchor" href="#_8-traceability-matrix" aria-label="Permalink to &quot;8. Traceability Matrix&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Acceptance</th><th>Scenarios</th></tr></thead><tbody><tr><td>C / F — Hourly task updates</td><td>E2E-SCHEDULED-manual-to-hourly</td></tr><tr><td>C / F / Quality — Saved project isolation</td><td>E2E-SCHEDULED-manual-workspace-binding</td></tr><tr><td>C / F / Quality — Desktop automations</td><td>E2E-SCHEDULED-desktop-automation-lifecycle</td></tr><tr><td>F / Security / Quality — Portable configuration sync</td><td>E2E-CONFIG-SYNC-webdav-portable-configuration</td></tr><tr><td>A / C — Unicode stdio framing</td><td>E2E-RPC-unicode-separators</td></tr><tr><td>C / G / Quality — Plugins navigation</td><td>E2E-NAV-plugins-button-goes-back</td></tr><tr><td>C / D / Quality — Sidebar row states</td><td>E2E-LAYOUT-sidebar-row-states</td></tr><tr><td>A / C / Quality — Sidebar material and settings return</td><td>E2E-LAYOUT-sidebar-settings</td></tr><tr><td>B / F / Security — Provider copy</td><td>E2E-PROVIDER-copy-config-without-credentials</td></tr><tr><td>B / F / Quality — Selected model order</td><td>E2E-MODEL-selected-order-persists</td></tr><tr><td>A — App startup</td><td>E2E-001, E2E-002, E2E-003, E2E-004, E2E-067, E2E-076, E2E-079, E2E-092, E2E-097, E2E-143, E2E-150, E2E-168, E2E-204</td></tr><tr><td>A / C / F / Quality — Tray session navigation</td><td>E2E-TRAY-bounded-session-navigation</td></tr><tr><td>B — Model config</td><td>E2E-005, E2E-006, E2E-007, E2E-038, E2E-050, E2E-052, E2E-055, E2E-066, E2E-080, E2E-082, E2E-102c, E2E-102d, E2E-102e, E2E-151, E2E-154, E2E-163, E2E-166, E2E-172, E2E-174, E2E-197, E2E-005G, E2E-005J, E2E-199, E2E-201, E2E-202, E2E-203, E2E-205, E2E-206, E2E-209</td></tr><tr><td>C — Conversation &amp; stream</td><td>E2E-CHAT-running-status-survives-output-pauses, E2E-008, E2E-008d, E2E-008e, E2E-008a, E2E-009, E2E-010, E2E-011, E2E-011a, E2E-011b, E2E-011d, E2E-011e, E2E-011g, E2E-031, E2E-040, E2E-047, E2E-048, E2E-048A, E2E-049, E2E-052, E2E-053, E2E-054, E2E-055, E2E-059, E2E-059a, E2E-060c, E2E-060d, E2E-061, E2E-061a, E2E-062, E2E-064, E2E-065, E2E-068, E2E-071, E2E-073, E2E-074, E2E-075, E2E-081, E2E-083, E2E-084, E2E-086, E2E-087, E2E-088, E2E-088b, E2E-089, E2E-090, E2E-COMPOSER-narrow-controls, E2E-094, E2E-095, E2E-096, E2E-097, E2E-098, E2E-099, E2E-102, E2E-102a, E2E-102b, E2E-102c, E2E-102d, E2E-102g, E2E-106, E2E-109, E2E-111, E2E-114, E2E-116, E2E-117, E2E-118, E2E-119, E2E-120, E2E-121, E2E-218, E2E-259, E2E-219, E2E-AGENTS-001, E2E-142, E2E-144, E2E-145, E2E-146, E2E-146a, E2E-147, E2E-151, E2E-154, E2E-155, E2E-158, E2E-159, E2E-161, E2E-162, E2E-166, E2E-172, E2E-173, E2E-174, E2E-177, E2E-178, E2E-179, E2E-180, E2E-182, E2E-183, E2E-187, E2E-198, E2E-199, E2E-202, E2E-203, E2E-207, E2E-208, E2E-CHAT-content-width-handles, E2E-250, E2E-102i, E2E-PLUGIN-session-orchestrator-real-workers, E2E-SUBAGENT-settlement-updates-before-parent-poll, E2E-SUBAGENT-resume-a-settled-delegation</td></tr><tr><td>D — Workspace</td><td>E2E-012, E2E-013, E2E-022B, E2E-024I, E2E-047, E2E-049, E2E-057, E2E-058, E2E-060, E2E-068, E2E-075, E2E-078, E2E-153, E2E-158, E2E-182, E2E-187, E2E-252</td></tr><tr><td>D — Workspace (project ordering)</td><td>E2E-253</td></tr><tr><td>E — Tools &amp; permissions</td><td>E2E-008a, E2E-014, E2E-015, E2E-016, E2E-017, E2E-018, E2E-019, E2E-024I, E2E-024K, E2E-040, E2E-049, E2E-074, E2E-093, E2E-097, E2E-099, E2E-100, E2E-101, E2E-102, E2E-102d, E2E-102e, E2E-102g, E2E-103, E2E-105, E2E-106, E2E-107, E2E-111, E2E-112, E2E-113, E2E-114, E2E-115, E2E-116, E2E-119, E2E-121, E2E-122, E2E-142, E2E-145, E2E-147, E2E-155, E2E-158, E2E-166, E2E-181, E2E-PLUGIN-imported-pi-package-skills</td></tr><tr><td>F — Persistence</td><td>E2E-020, E2E-021, E2E-021a, E2E-036, E2E-037, E2E-038, E2E-040, E2E-042, E2E-047, E2E-048, E2E-051, E2E-054, E2E-056, E2E-061, E2E-062, E2E-064, E2E-066, E2E-068, E2E-071, E2E-072, E2E-073, E2E-082, E2E-084, E2E-096, E2E-098, E2E-102, E2E-102b, E2E-102c, E2E-102d, E2E-102g, E2E-102i, E2E-103, E2E-AGENTS-001, E2E-061a, E2E-073a, E2E-104, E2E-106, E2E-107, E2E-108, E2E-109, E2E-110, E2E-112, E2E-118, E2E-119, E2E-120, E2E-121, E2E-123, E2E-142, E2E-146, E2E-146a, E2E-148, E2E-151, E2E-158, E2E-160, E2E-168, E2E-171, E2E-177, E2E-178, E2E-183, E2E-186, E2E-005J, E2E-PLUGIN-session-orchestrator-real-workers</td></tr><tr><td>F — Persistence (project ordering)</td><td>E2E-251</td></tr><tr><td>G — Plugins</td><td>E2E-022, E2E-022A, E2E-022B, E2E-022C, E2E-023, E2E-024, E2E-024B, E2E-024C, E2E-024D, E2E-024AA, E2E-024E, E2E-024W, E2E-024F, E2E-024G, E2E-024H, E2E-024I, E2E-024J, E2E-024K, E2E-024L, E2E-024M, E2E-024N, E2E-024O, E2E-024P, E2E-025, E2E-026, E2E-105, E2E-117, E2E-120, E2E-122, E2E-123, E2E-024Q, E2E-148, E2E-152, E2E-153, E2E-PLUGIN-imported-pi-package-skills, E2E-PLUGIN-imported-pi-package-wrapper, E2E-PLUGIN-import-extension-installs-dependencies, E2E-PLUGIN-import-extension-reports-missing-dependency, E2E-PLUGIN-global-shortcut-owns-only-its-own-command, E2E-PLUGIN-permission-gate-for-real-time-capabilities, E2E-PLUGIN-background-audio-and-realtime-connection, E2E-PLUGIN-fs-root-follows-the-calling-session</td></tr><tr><td>H — Diagnostics</td><td>E2E-027, E2E-031, E2E-034, E2E-042, E2E-096, E2E-098, E2E-104, E2E-107, E2E-108, E2E-109, E2E-110, E2E-113, E2E-115, E2E-116, E2E-118, E2E-121, E2E-146, E2E-146a, E2E-155, E2E-159, E2E-176, E2E-194, E2E-195</td></tr><tr><td>Security</td><td>E2E-028, E2E-029, E2E-030, E2E-024J, E2E-024K, E2E-024M, E2E-049, E2E-068, E2E-086, E2E-102c, E2E-102d, E2E-102e, E2E-105, E2E-106, E2E-107, E2E-108, E2E-109, E2E-110, E2E-112, E2E-113, E2E-115, E2E-116, E2E-117, E2E-119, E2E-121, E2E-122, E2E-123, E2E-142, E2E-148, E2E-151, E2E-153, E2E-158, E2E-187, E2E-196c, E2E-196b, E2E-196, E2E-PLUGIN-fs-root-follows-the-calling-session</td></tr><tr><td>Quality</td><td>E2E-CHAT-running-status-survives-output-pauses, E2E-032, E2E-033, E2E-039, E2E-043, E2E-044, E2E-045, E2E-046, E2E-047, E2E-048, E2E-048A, E2E-049, E2E-050, E2E-053, E2E-055, E2E-056, E2E-057, E2E-058, E2E-059, E2E-060, E2E-061, E2E-062, E2E-063, E2E-064, E2E-065, E2E-066, E2E-067, E2E-068, E2E-069, E2E-070, E2E-071, E2E-072, E2E-073, E2E-074, E2E-075, E2E-076, E2E-077, E2E-078, E2E-079, E2E-080, E2E-081, E2E-082, E2E-083, E2E-084, E2E-085, E2E-086, E2E-092, E2E-093, E2E-094, E2E-095, E2E-096, E2E-097, E2E-098, E2E-099, E2E-100, E2E-101, E2E-102, E2E-102a, E2E-102b, E2E-102c, E2E-102d, E2E-102e, E2E-103, E2E-AGENTS-001, E2E-021a, E2E-024N, E2E-059a, E2E-060b, E2E-060c, E2E-061a, E2E-073a, E2E-111, E2E-114, E2E-117, E2E-118, E2E-119, E2E-120, E2E-122, E2E-123, E2E-142, E2E-143, E2E-144, E2E-145, E2E-146, E2E-147, E2E-148, E2E-150, E2E-151, E2E-153, E2E-155, E2E-158, E2E-159, E2E-160, E2E-161, E2E-162, E2E-163, E2E-168, E2E-172, E2E-173, E2E-174, E2E-011g, E2E-176, E2E-177, E2E-178, E2E-179, E2E-180, E2E-181, E2E-182, E2E-183, E2E-186, E2E-187, E2E-194, E2E-195, E2E-196a, E2E-196b, E2E-196c, E2E-198, E2E-199, E2E-200, E2E-196, E2E-201, E2E-204, E2E-202, E2E-203, E2E-205, E2E-206, E2E-207, E2E-208, E2E-209, E2E-210, E2E-218, E2E-259, E2E-219, E2E-250, E2E-252, E2E-102i, E2E-SUBAGENT-settlement-updates-before-parent-poll, E2E-PLUGIN-imported-pi-package-skills, E2E-PLUGIN-fs-root-follows-the-calling-session, E2E-SUBAGENT-resume-a-settled-delegation</td></tr><tr><td>Quality (project ordering)</td><td>E2E-253</td></tr><tr><td>C — Conversation &amp; stream (IME slash alias)</td><td>E2E-255</td></tr><tr><td>E — Tools &amp; permissions (Skill residency)</td><td>E2E-254</td></tr><tr><td>Quality (Skill residency and IME slash alias)</td><td>E2E-254, E2E-255</td></tr><tr><td>C — Conversation &amp; stream (import visibility)</td><td>E2E-257</td></tr><tr><td>F — Persistence (import visibility)</td><td>E2E-257</td></tr><tr><td>G — Plugins (import visibility)</td><td>E2E-257</td></tr><tr><td>Quality (import visibility)</td><td>E2E-257</td></tr><tr><td>G — Plugins (Session Orchestrator)</td><td>E2E-PLUGIN-session-orchestrator-real-workers</td></tr><tr><td>Security (Session Orchestrator)</td><td>E2E-PLUGIN-session-orchestrator-real-workers</td></tr><tr><td>Quality (Session Orchestrator)</td><td>E2E-PLUGIN-session-orchestrator-real-workers</td></tr><tr><td>C — Conversation &amp; stream (Session list responsiveness)</td><td>E2E-SESSION-list-refresh-keeps-desktop-responsive</td></tr><tr><td>Quality (Session list responsiveness)</td><td>E2E-SESSION-list-refresh-keeps-desktop-responsive</td></tr><tr><td>Security (imported extension dependencies)</td><td>E2E-PLUGIN-import-extension-installs-dependencies, E2E-PLUGIN-import-extension-reports-missing-dependency</td></tr><tr><td>Quality (imported extension dependencies)</td><td>E2E-PLUGIN-import-extension-installs-dependencies, E2E-PLUGIN-import-extension-reports-missing-dependency</td></tr><tr><td>F / G / Security / Quality — Imported extension npm recovery</td><td>E2E-PLUGIN-import-extension-recovers-missing-npm</td></tr><tr><td>G — Plugins (desktop tool dispatch budget)</td><td>E2E-PLUGIN-slow-tool-is-not-cut-off-by-host-dispatch</td></tr><tr><td>C — Conversation &amp; stream (Independent session communication)</td><td>E2E-SESSION-independent-top-level-communication</td></tr><tr><td>D — Plugin security (Independent session communication)</td><td>E2E-SESSION-independent-top-level-communication</td></tr><tr><td>G — Plugins (Independent session communication)</td><td>E2E-SESSION-independent-top-level-communication</td></tr><tr><td>Quality (Independent session communication)</td><td>E2E-SESSION-independent-top-level-communication, E2E-SESSION-hover-card-model-and-links</td></tr><tr><td>C — Conversation &amp; stream (Hover card model and links)</td><td>E2E-SESSION-hover-card-model-and-links</td></tr><tr><td>C — Conversation &amp; stream (Chat file references)</td><td>E2E-CHAT-shorthand-file-ref-opens-the-matching-file, E2E-CHAT-file-ref-opens-the-surface-that-owns-it</td></tr><tr><td>G — Plugins (Chat file references)</td><td>E2E-CHAT-file-ref-opens-the-surface-that-owns-it, E2E-PLUGIN-file-view-collapse-persists</td></tr><tr><td>Quality (Chat file references)</td><td>E2E-CHAT-shorthand-file-ref-opens-the-matching-file, E2E-CHAT-file-ref-opens-the-surface-that-owns-it, E2E-PLUGIN-file-view-collapse-persists</td></tr><tr><td>G — Plugins (project folder roots)</td><td>E2E-PLUGIN-file-view-switches-folder-per-project</td></tr><tr><td>Security (project folder roots)</td><td>E2E-PLUGIN-file-view-switches-folder-per-project</td></tr><tr><td>Quality (project folder roots)</td><td>E2E-PLUGIN-file-view-switches-folder-per-project</td></tr><tr><td>D — Workspace (project delete)</td><td>E2E-PROJECT-delete-removes-project-and-owned-sessions</td></tr><tr><td>F — Persistence (project delete)</td><td>E2E-PROJECT-delete-removes-project-and-owned-sessions</td></tr><tr><td>Quality (project delete)</td><td>E2E-PROJECT-delete-removes-project-and-owned-sessions</td></tr><tr><td>Quality (two-click delete)</td><td>E2E-SESSION-two-click-delete-arms-first</td></tr><tr><td>Security (plugin real-time capabilities)</td><td>E2E-PLUGIN-global-shortcut-owns-only-its-own-command, E2E-PLUGIN-permission-gate-for-real-time-capabilities, E2E-PLUGIN-background-audio-and-realtime-connection</td></tr><tr><td>C — Conversation &amp; stream (disclosure reading position)</td><td>E2E-CHAT-disclosure-toggle-keeps-reading-position</td></tr><tr><td>E — Tools &amp; permissions (disclosure reading position)</td><td>E2E-CHAT-disclosure-toggle-keeps-reading-position</td></tr><tr><td>E — Tools &amp; permissions (capability level move)</td><td>E2E-CAPABILITY-move-across-levels</td></tr><tr><td>F — Persistence (capability level move)</td><td>E2E-CAPABILITY-move-across-levels</td></tr><tr><td>Quality (capability level move)</td><td>E2E-CAPABILITY-move-across-levels</td></tr></tbody></table><table tabindex="0"><thead><tr><th>Milestone</th><th>Scenarios</th></tr></thead><tbody><tr><td>M1</td><td>E2E-001, E2E-002, E2E-003, E2E-028, E2E-029</td></tr><tr><td>M2</td><td>E2E-004, E2E-005, E2E-006, E2E-007, E2E-008, E2E-008d, E2E-008e, E2E-009, E2E-010, E2E-011, E2E-011a, E2E-011b, E2E-011d, E2E-011e, E2E-011g, E2E-020, E2E-021, E2E-021a, E2E-027, E2E-031, E2E-036, E2E-037, E2E-042, E2E-087, E2E-088, E2E-088b, E2E-089, E2E-090, E2E-COMPOSER-narrow-controls, E2E-144, E2E-005J, E2E-201, E2E-202, E2E-207, E2E-206</td></tr><tr><td>M3</td><td>E2E-012, E2E-013, E2E-014, E2E-015, E2E-016, E2E-017, E2E-018, E2E-019, E2E-040</td></tr><tr><td>M4</td><td>E2E-022, E2E-023, E2E-024, E2E-025, E2E-026, E2E-030, E2E-038</td></tr><tr><td>M5</td><td>E2E-CHAT-running-status-survives-output-pauses, E2E-008a, E2E-032, E2E-033, E2E-034, E2E-039, E2E-043, E2E-044, E2E-045, E2E-046, E2E-047, E2E-048, E2E-048A, E2E-049, E2E-050, E2E-051, E2E-052, E2E-053, E2E-054, E2E-055, E2E-056, E2E-057, E2E-058, E2E-059, E2E-060, E2E-061, E2E-062, E2E-063, E2E-064, E2E-065, E2E-066, E2E-067, E2E-068, E2E-069, E2E-070, E2E-071, E2E-072, E2E-073, E2E-074, E2E-075, E2E-076, E2E-077, E2E-078, E2E-079, E2E-080, E2E-081, E2E-082, E2E-083, E2E-084, E2E-085, E2E-086, E2E-092, E2E-093, E2E-096, E2E-097, E2E-098, E2E-099, E2E-100, E2E-101, E2E-102, E2E-102a, E2E-102b, E2E-102c, E2E-102d, E2E-102e, E2E-AGENTS-001, E2E-059a, E2E-060b, E2E-060c, E2E-061a, E2E-073a, E2E-094, E2E-095, E2E-143, E2E-145, E2E-146, E2E-146a, E2E-147, E2E-177, E2E-178, E2E-180, E2E-181, E2E-182, E2E-183, E2E-186, E2E-187, E2E-194, E2E-195, E2E-204, E2E-208, E2E-CHAT-content-width-handles, E2E-250, E2E-252, E2E-102i</td></tr><tr><td>M5 (project ordering)</td><td>E2E-253</td></tr><tr><td>M2 (IME slash alias)</td><td>E2E-255</td></tr><tr><td>M5 (Skill residency)</td><td>E2E-254</td></tr><tr><td>M6</td><td>E2E-104, E2E-105, E2E-106, E2E-107, E2E-108, E2E-109, E2E-110, E2E-111, E2E-112, E2E-113, E2E-114, E2E-115, E2E-116, E2E-117, E2E-118, E2E-119, E2E-120, E2E-103, E2E-172</td></tr><tr><td>M6+</td><td>E2E-121, E2E-122, E2E-148, E2E-150, E2E-151, E2E-154, E2E-155, E2E-158, E2E-159, E2E-160, E2E-161, E2E-162, E2E-163, E2E-166, E2E-168, E2E-173, E2E-174, E2E-176, E2E-179, E2E-196a, E2E-196b, E2E-196c, E2E-198, E2E-199, E2E-200, E2E-202, E2E-203, E2E-205, E2E-209, E2E-210, E2E-212, E2E-213, E2E-214, E2E-215, E2E-216, E2E-217, E2E-218, E2E-259, E2E-219, E2E-257, E2E-SUBAGENT-settlement-updates-before-parent-poll, E2E-PLUGIN-fs-root-follows-the-calling-session, E2E-SUBAGENT-resume-a-settled-delegation</td></tr><tr><td>M6+ (Session Orchestrator)</td><td>E2E-PLUGIN-session-orchestrator-real-workers</td></tr><tr><td>M6+ (Selected model order)</td><td>E2E-MODEL-selected-order-persists</td></tr><tr><td>M6+ (Session list responsiveness)</td><td>E2E-SESSION-list-refresh-keeps-desktop-responsive</td></tr><tr><td>M6+ (Independent session communication)</td><td>E2E-SESSION-independent-top-level-communication, E2E-SESSION-hover-card-model-and-links</td></tr><tr><td>M5 (Chat file references)</td><td>E2E-CHAT-shorthand-file-ref-opens-the-matching-file, E2E-CHAT-file-ref-opens-the-surface-that-owns-it</td></tr><tr><td>M6+ (Chat file references)</td><td>E2E-PLUGIN-file-view-collapse-persists</td></tr><tr><td>M6+ (project folder roots)</td><td>E2E-PLUGIN-file-view-switches-folder-per-project</td></tr><tr><td>Post-MVP</td><td>E2E-022A, E2E-022B, E2E-022C, E2E-024I, E2E-024J, E2E-024K, E2E-024L, E2E-024M (plugin roadmap R2/R3/R6)</td></tr><tr><td>Post-baseline local automation</td><td>E2E-220</td></tr><tr><td>Post-MVP remote control</td><td>E2E-221, E2E-222, E2E-223, E2E-224, E2E-225, E2E-226, E2E-227, E2E-228, E2E-229, E2E-230, E2E-231, E2E-232</td></tr><tr><td>Trusted extensions (R7 v1)</td><td>E2E-DIALOG-long-text-boundaries, E2E-241, E2E-242, E2E-HOOKS-cancel-and-dispose, E2E-TRUSTED-EXTENSION-custom-agent-stream-and-binding, E2E-243, E2E-244, E2E-245, E2E-PLUGIN-imported-pi-package-skills, E2E-PLUGIN-import-extension-installs-dependencies, E2E-PLUGIN-import-extension-reports-missing-dependency, E2E-PLUGIN-declared-provider-appears-in-the-native-provider-list</td></tr><tr><td>Trusted extensions (R7 v1 npm recovery)</td><td>E2E-PLUGIN-import-extension-recovers-missing-npm</td></tr><tr><td>Post-MVP regression coverage (plugin tool dispatch)</td><td>E2E-PLUGIN-slow-tool-is-not-cut-off-by-host-dispatch</td></tr><tr><td>M6+ (Project delete)</td><td>E2E-PROJECT-delete-removes-project-and-owned-sessions</td></tr><tr><td>M6+ (Two-click delete)</td><td>E2E-SESSION-two-click-delete-arms-first</td></tr><tr><td>C — Conversation &amp; stream (model fallback)</td><td>E2E-SUBAGENT-ordered-model-fallback-preserves-work</td></tr><tr><td>Quality (model fallback isolation)</td><td>E2E-SUBAGENT-ordered-model-fallback-preserves-work</td></tr><tr><td>C — Conversation &amp; stream (legacy subagent turn limit)</td><td>E2E-SUBAGENT-legacy-turn-limit-frontmatter-is-ignored</td></tr><tr><td>Quality (legacy subagent turn limit)</td><td>E2E-SUBAGENT-legacy-turn-limit-frontmatter-is-ignored</td></tr><tr><td>M6+ (disclosure reading position)</td><td>E2E-CHAT-disclosure-toggle-keeps-reading-position</td></tr><tr><td>M6+ (capability level move)</td><td>E2E-CAPABILITY-move-across-levels</td></tr><tr><td>E — Tools &amp; permissions (builtin subagent defaults)</td><td>E2E-SUBAGENT-settings-lists-builtin-defaults</td></tr><tr><td>Quality (builtin subagent defaults)</td><td>E2E-SUBAGENT-settings-lists-builtin-defaults</td></tr><tr><td>C — Conversation &amp; stream (opaque floating surfaces)</td><td>E2E-CHAT-opaque-floating-decision-and-retry-surfaces</td></tr><tr><td>Quality (opaque floating surfaces)</td><td>E2E-CHAT-opaque-floating-decision-and-retry-surfaces</td></tr><tr><td>M6 (opaque floating surfaces)</td><td>E2E-CHAT-opaque-floating-decision-and-retry-surfaces</td></tr><tr><td>B — Model config (catalog window provenance)</td><td>E2E-MODEL-catalog-window-correction-reaches-saved-bindings</td></tr><tr><td>F — Persistence (catalog window provenance)</td><td>E2E-MODEL-catalog-window-correction-reaches-saved-bindings</td></tr><tr><td>Quality (catalog window provenance)</td><td>E2E-MODEL-catalog-window-correction-reaches-saved-bindings</td></tr><tr><td>M6+ (catalog window provenance)</td><td>E2E-MODEL-catalog-window-correction-reaches-saved-bindings</td></tr><tr><td>C — Conversation &amp; stream (delegate context budget)</td><td>E2E-SUBAGENT-context-overflow-compacts-before-failing, E2E-SUBAGENT-context-overflow-reports-actionable-failure, E2E-SUBAGENT-resume-seeds-within-context-budget</td></tr><tr><td>Quality (delegate context budget)</td><td>E2E-SUBAGENT-context-overflow-compacts-before-failing, E2E-SUBAGENT-context-overflow-reports-actionable-failure, E2E-SUBAGENT-resume-seeds-within-context-budget</td></tr><tr><td>M6+ (delegate context budget)</td><td>E2E-SUBAGENT-context-overflow-compacts-before-failing, E2E-SUBAGENT-context-overflow-reports-actionable-failure, E2E-SUBAGENT-resume-seeds-within-context-budget</td></tr><tr><td>C / F / Quality — The context estimate stays safe (calibration)</td><td>E2E-CONTEXT-estimate-calibration-stays-safe</td></tr><tr><td>C — Conversation &amp; stream (unique tool-call ids)</td><td>E2E-RUNTIME-unique-tool-call-ids-per-request</td></tr><tr><td>F — Persistence (stored model binding array)</td><td>E2E-PROVIDER-stored-binding-array-reads-entry-by-entry</td></tr><tr><td>Quality (stored model binding array)</td><td>E2E-PROVIDER-stored-binding-array-reads-entry-by-entry</td></tr><tr><td>Quality (unique tool-call ids)</td><td>E2E-RUNTIME-unique-tool-call-ids-per-request</td></tr><tr><td>C — Conversation &amp; stream (loop context ownership)</td><td>E2E-RUNTIME-loop-context-ownership</td></tr><tr><td>Quality (loop context ownership)</td><td>E2E-RUNTIME-loop-context-ownership</td></tr><tr><td>G — Plugin host lifecycle (crash report)</td><td>E2E-PLUGIN-crash-report-names-the-exit-code</td></tr><tr><td>Quality (crash report)</td><td>E2E-PLUGIN-crash-report-names-the-exit-code</td></tr></tbody></table><p>The <code>US-UI-*</code> visual scenarios (§UI shell visual scenarios) trace to the Codex parity decisions in <a href="./../08-meta/decisions-log">decisions-log §D</a> rather than the A–H criteria; their gold source is the capture suite.</p><p>The release artifact paths are covered by E2E-192, E2E-196a, E2E-196b, E2E-196c, and E2E-200 (Quality, M6+).</p><hr><h2 id="_9-how-ai-must-update-this-doc" tabindex="-1">9. How AI Must Update This Doc <a class="header-anchor" href="#_9-how-ai-must-update-this-doc" aria-label="Permalink to &quot;9. How AI Must Update This Doc&quot;">​</a></h2><p>When adding or changing a feature that affects user-visible or protocol-visible behavior:</p><ol><li><strong>Add a new scenario</strong> using the template in §6. Assign the next available ID (<code>E2E-&lt;N&gt;</code>). IDs retired by ADR 0268 are never reused.</li><li><strong>Link it</strong> to the relevant acceptance criterion (A–H) and milestone (M1–M6 or M6+ for the current product increment).</li><li><strong>Set status</strong> to <code>Draft</code> unless an automated test already exists.</li><li><strong>Update the traceability matrix</strong> in §8.</li><li><strong>Commit</strong> the update as part of the change (per <a href="./03-ai-development-workflow">ai-development-workflow</a> R3).</li></ol><hr><h2 id="_10-future-automation-mapping" tabindex="-1">10. Future Automation Mapping <a class="header-anchor" href="#_10-future-automation-mapping" aria-label="Permalink to &quot;10. Future Automation Mapping&quot;">​</a></h2><p>When E2E automation is implemented (post-M5):</p><ul><li>Each <code>Draft</code> scenario → Playwright test file.</li><li>Scenario ID becomes test case name: <code>e2e-001-app-launches</code>.</li><li>Fixtures and test data paths defined in a <code>tests/e2e/fixtures/</code> directory.</li><li>CI gate: all E2E scenarios must pass before release.</li></ul><p>Automation section will be expanded in a future ADR when the tooling decision is finalized.</p><hr><h2 id="_11-acceptance-criteria" tabindex="-1">11. Acceptance Criteria <a class="header-anchor" href="#_11-acceptance-criteria" aria-label="Permalink to &quot;11. Acceptance Criteria&quot;">​</a></h2><p>This test plan spec is accepted when:</p><ul><li>[ ] All MVP acceptance criteria (A–H) have at least one E2E scenario.</li><li>[ ] All security acceptance items have at least one E2E scenario.</li><li>[ ] Every scenario links to at least one spec document.</li><li>[ ] Traceability matrix is complete (scenarios ↔ acceptance ↔ milestones).</li><li>[ ] Scenario template is defined and all entries follow it.</li><li>[ ] AI update rules are documented and cross-linked to workflow spec.</li><li>[ ] Environment requirements match baseline (native macOS arm64/Intel x64, clean profile).</li></ul><h2 id="ui-shell-visual-scenarios" tabindex="-1">UI shell visual scenarios <a class="header-anchor" href="#ui-shell-visual-scenarios" aria-label="Permalink to &quot;UI shell visual scenarios&quot;">​</a></h2><h3 id="us-ui-01-codex-aligned-shell-chrome" tabindex="-1">US-UI-01 Codex-aligned shell chrome <a class="header-anchor" href="#us-ui-01-codex-aligned-shell-chrome" aria-label="Permalink to &quot;US-UI-01 Codex-aligned shell chrome&quot;">​</a></h3><ul><li>Open the desktop app on macOS dark theme.</li><li>Expect charcoal main surface (<code>#181818</code>), left sidebar with current-project and Temporary session groups, and a floating bottom composer with mode/model controls and no workspace rail.</li><li>Expect no blue-slate marketing chrome; primary send control is a circular inverted button.</li></ul><h3 id="us-ui-02-empty-thread-hero" tabindex="-1">US-UI-02 Empty thread hero <a class="header-anchor" href="#us-ui-02-empty-thread-hero" aria-label="Permalink to &quot;US-UI-02 Empty thread hero&quot;">​</a></h3><ul><li>Open or create a thread with zero messages.</li><li>Expect the centered hero copy &quot;What can I help you build?&quot;, a short muted supporting line, with no developer starter cards. The optional project name remains a dotted-underline action when a workspace is open.</li></ul><h3 id="us-ui-03-sidebar-destinations" tabindex="-1">US-UI-03 Sidebar destinations <a class="header-anchor" href="#us-ui-03-sidebar-destinations" aria-label="Permalink to &quot;US-UI-03 Sidebar destinations&quot;">​</a></h3><ul><li>Expect the expanded home sidebar to show Sessions and Projects without standalone Plugins, Pull requests, or Scheduled rows.</li><li>Click the plug-shaped Plugins icon in the sidebar footer, immediately to the right of Settings, and expect it to replace the main pane with a dedicated page.</li><li>Open Settings → Project archive and use it to open, switch, and close a local folder workspace.</li></ul><h3 id="us-ui-04-composer-without-workspace-context" tabindex="-1">US-UI-04 Composer without workspace context <a class="header-anchor" href="#us-ui-04-composer-without-workspace-context" aria-label="Permalink to &quot;US-UI-04 Composer without workspace context&quot;">​</a></h3><ul><li>With a git workspace open, composer does not show project, Local, or branch labels above the prompt surface.</li><li>Operating-mode selector switches between Agent, Plan, and Goal; both contract modes keep the permission-mode chip and explain their Bash tradeoff.</li></ul><h3 id="us-ui-05-locale-chrome" tabindex="-1">US-UI-05 Locale chrome <a class="header-anchor" href="#us-ui-05-locale-chrome" aria-label="Permalink to &quot;US-UI-05 Locale chrome&quot;">​</a></h3><ul><li>On a zh-CN system locale, sidebar labels render in Chinese (项目 / 临时会话), without 拉取请求 or 已安排 entries. The footer plug-shaped Plugins icon exposes the localized accessible name 插件.</li><li>Empty-thread hero and supporting line are localized Chinese copy; project name remains a dotted-underline action when a workspace is open.</li><li>Composer omits the 本地 workspace label and shows Agent/Plan/Goal plus the active model ID; both locales expose the Plan and Goal approval copy.</li></ul><h3 id="us-ui-06-session-auto-title" tabindex="-1">US-UI-06 Session auto-title <a class="header-anchor" href="#us-ui-06-session-auto-title" aria-label="Permalink to &quot;US-UI-06 Session auto-title&quot;">​</a></h3><ul><li>Create a new task and send a first prompt such as &quot;同步代码&quot;.</li><li>Expect its project or temporary session row to show the normalized prompt fallback immediately, then adopt a concise LLM summary after the first turn.</li><li>Restart before/after the summary and confirm the current title is retained; rename a task from its session menu and send a first prompt if it was still using a default title. Expect the custom label to remain unchanged while the default-title task receives the normal first-prompt title.</li></ul><h3 id="us-ui-08-shortcut-only-destination-history" tabindex="-1">US-UI-08 Shortcut-only destination history <a class="header-anchor" href="#us-ui-08-shortcut-only-destination-history" aria-label="Permalink to &quot;US-UI-08 Shortcut-only destination history&quot;">​</a></h3><ul><li>Navigate Settings → Project archive → a project session → Plugins.</li><li>Expect no back/forward buttons in the expanded sidebar or main titlebar.</li><li>Press <code>Cmd/Ctrl+[</code> and <code>Cmd/Ctrl+]</code>; expect them to traverse that history.</li></ul><h3 id="us-ui-09-grouped-session-title-backfill" tabindex="-1">US-UI-09 Grouped session title backfill <a class="header-anchor" href="#us-ui-09-grouped-session-title-backfill" aria-label="Permalink to &quot;US-UI-09 Grouped session title backfill&quot;">​</a></h3><ul><li>Open an older session that previously showed &quot;New task&quot;/&quot;New chat&quot; but has a first user message.</li><li>Expect its scoped sidebar row to display a truncated first-user-message title after session list load.</li></ul><h3 id="us-ui-11-empty-draft-reuse" tabindex="-1">US-UI-11 Empty draft reuse <a class="header-anchor" href="#us-ui-11-empty-draft-reuse" aria-label="Permalink to &quot;US-UI-11 Empty draft reuse&quot;">​</a></h3><ul><li>Click New task twice.</li><li>Expect only one empty &quot;New task&quot; draft in the current project or Temporary group and the home hero remains visible. Empty drafts in another scope are not reused.</li></ul><h3 id="us-ui-12-composer-without-workspace-rail" tabindex="-1">US-UI-12 Composer without workspace rail <a class="header-anchor" href="#us-ui-12-composer-without-workspace-rail" aria-label="Permalink to &quot;US-UI-12 Composer without workspace rail&quot;">​</a></h3><ul><li>On empty home, project home, and in a thread, expect no project / Local / branch context rail above the composer.</li><li>The prompt shell remains one uninterrupted rounded surface with no reserved rail height, attached top lip, rail shadow, bottom seam, or separators.</li></ul><h3 id="us-ui-13-light-theme-shell-parity" tabindex="-1">US-UI-13 Light theme shell parity <a class="header-anchor" href="#us-ui-13-light-theme-shell-parity" aria-label="Permalink to &quot;US-UI-13 Light theme shell parity&quot;">​</a></h3><ul><li>Set theme to system/light on a light macOS appearance.</li><li>Expect sidebar <code>#f3f3f3</code>, main <code>#ffffff</code>, text <code>#1a1c1f</code>, white floating composer, and home hero with project underline.</li><li>Sidebar project/session labels, footer Settings/Plugins/notification icons, current-project identity, thread titles, and composer controls must remain readable dark-on-light (≥4.5:1). Never white/translucent text on the light sidebar.</li><li>The macOS traffic-light row keeps Collapse sidebar readable at the right on light chrome without rendering the Logo/Home brand.</li></ul><h3 id="us-ui-14-semantic-chrome-tokens" tabindex="-1">US-UI-14 Semantic chrome tokens <a class="header-anchor" href="#us-ui-14-semantic-chrome-tokens" aria-label="Permalink to &quot;US-UI-14 Semantic chrome tokens&quot;">​</a></h3><ul><li>Toggle theme system → light → dark without restart.</li><li>Shell chrome (sidebar items, composer runtime controls, icon buttons) follows semantic <code>--ds-text-*</code> / <code>--ds-bg-*</code> tokens in both themes; no hard-coded white (<code>gray-0</code>) text on light surfaces.</li></ul><h3 id="us-ui-15-codex-density-elevation" tabindex="-1">US-UI-15 Codex density + elevation <a class="header-anchor" href="#us-ui-15-codex-density-elevation" aria-label="Permalink to &quot;US-UI-15 Codex density + elevation&quot;">​</a></h3><ul><li>Sidebar rows use a compact ~28–32px pitch with the 12–14px hierarchy from US-UI-69 and 8px horizontal padding (Codex <code>radius-token-row</code> 10px).</li><li>Floating composer uses Codex elevation-prominent: 0.5px stroke + soft 3px/20px shadow (not heavy 10–30px drop).</li><li>Empty hero title is 28px / 34px line-height, weight 400.</li><li>Window restores ≥1000×700 (target 1200×800) if Stage Manager collapses it.</li></ul><h3 id="us-ui-16-sidebar-footer-utility-layout" tabindex="-1">US-UI-16 Sidebar footer utility layout <a class="header-anchor" href="#us-ui-16-sidebar-footer-utility-layout" aria-label="Permalink to &quot;US-UI-16 Sidebar footer utility layout&quot;">​</a></h3><ul><li>On the light/dark home shell, the sidebar footer is a transparent utility band with no separator. Settings, Plugins, and notification actions are grouped on the left, while the build/version chip is right-aligned.</li><li>The notification Bell remains visible in the left action group with its unread badge and opens the inbox above the footer; the main titlebar has no duplicate Bell.</li><li>Clicking the build/version chip checks for updates when current, or opens Settings → Info when an actionable update is available.</li><li>Traffic lights sit at Codex <code>{x:16,y:16}</code> with a 46px toolbar; the expanded macOS sidebar places Collapse sidebar at the right in that same row, with no Logo/Home brand or back/forward buttons.</li></ul><h3 id="us-ui-17-pi-desktop-home-hero-logo" tabindex="-1">US-UI-17 PI-Desktop home hero logo <a class="header-anchor" href="#us-ui-17-pi-desktop-home-hero-logo" aria-label="Permalink to &quot;US-UI-17 PI-Desktop home hero logo&quot;">​</a></h3><ul><li>On empty chat home, the 100px <code>HomeMascotLogo</code> GIF renders above the title as an eight-frame waving mascot with a short idle hold. Light and dark themes each use a dedicated GIF and still PNG.</li><li>Pointer hover does not change the cadence or geometry; reduced motion shows the matching still first frame. The mascot remains decorative.</li><li>Title is 28px / weight 400; active project name uses dotted underline (1px, offset 4px).</li><li>Composer does not render attachment or appshot controls before their payload reaches pi end to end.</li></ul><h3 id="us-ui-18-composer-has-no-inert-actions" tabindex="-1">US-UI-18 Composer has no inert actions <a class="header-anchor" href="#us-ui-18-composer-has-no-inert-actions" aria-label="Permalink to &quot;US-UI-18 Composer has no inert actions&quot;">​</a></h3><ul><li>On chat home and a docked thread, inspect every composer control.</li><li>Expect no file, photo, or appshot controls while those payloads are unsupported by the pi runtime. Exact reasoning-capable models expose the current Thinking level immediately to the right of Agent / Plan / Goal; unsupported models show no trigger. Unknown compatible models can explicitly enable thinking from the model menu, and changes update the durable session.</li><li>Expect no project, Local, or branch context labels in the composer.</li><li>Every visible composer control changes the active session, opens its menu, or submits/aborts the current turn.</li></ul><h3 id="us-ui-19-permanent-stage-manager-bounds-restore-macos-only" tabindex="-1">US-UI-19 Permanent Stage Manager bounds restore (macOS only) <a class="header-anchor" href="#us-ui-19-permanent-stage-manager-bounds-restore-macos-only" aria-label="Permalink to &quot;US-UI-19 Permanent Stage Manager bounds restore (macOS only)&quot;">​</a></h3><ul><li>On macOS with Stage Manager, shrink or unfocus the PI window until width &lt; 1040 or height &lt; 700.</li><li>Expect the shell to re-assert a Codex-like footprint (~1200×800, min 1040×700) and keep restoring while still collapsed (not only during the first 20s after launch).</li><li>The recovery watchdog is macOS-only (D447). On Windows/Linux it must not run at all: the app must never re-layer or re-raise its own window unprompted. Focus another window, then confirm PI-Desktop stays behind it instead of jumping back to the top of the stack, and that a stacking check (<code>xprop -root _NET_CLIENT_LIST_STACKING</code>) never shows it returning to the top periodically.</li></ul><h3 id="us-ui-20-dark-floating-composer-box" tabindex="-1">US-UI-20 Dark floating composer box <a class="header-anchor" href="#us-ui-20-dark-floating-composer-box" aria-label="Permalink to &quot;US-UI-20 Dark floating composer box&quot;">​</a></h3><ul><li>Switch to dark theme on chat home.</li><li>Expect main <code>#181818</code>, sidebar <code>#000000</code>, and the floating composer plate at elevated-primary (<code>#212121f5</code> / gray-800 96%) with elevation-prominent stroke + soft lift so the box reads against the main surface.</li></ul><h3 id="us-ui-21-composer-model-menu-configures-pi" tabindex="-1">US-UI-21 Composer model menu configures pi <a class="header-anchor" href="#us-ui-21-composer-model-menu-configures-pi" aria-label="Permalink to &quot;US-UI-21 Composer model menu configures pi&quot;">​</a></h3><ul><li>Create a session with provider A/model A, then open the Composer-right model × reasoning menu.</li><li>Expect the top bar to show only the task title and window actions. The Composer-right model × reasoning chip shows provider A/model A and the current reasoning level. Its menu starts with only Model and Reasoning level entries; Model opens the searchable provider-group list and Reasoning level opens the capability-filtered radio list in the same popover.</li><li>In the searchable provider-group list, expect each provider heading to read as the visual parent: its <code>--text-md</code> treatment is stronger than the indented model rows&#39; normal-weight <code>--text-sm</code> treatment. In zh-CN, headings must not force uppercase or wide Latin tracking.</li><li>Select provider B/model B, send a prompt, and expect the main-to-sidecar <code>agent.prompt</code> payload and pi runtime to use B for that session.</li><li>Switch away and back; expect B and its clamped reasoning level to remain selected. Selecting a model or reasoning level returns to the root without closing the Composer menu; outside click and Escape close it. While a turn runs, expect the combined control to remain available only for next-turn configuration unless a pending approval gates it.</li></ul><h3 id="us-ui-22-profile-footer-menu" tabindex="-1">US-UI-22 Profile footer menu <a class="header-anchor" href="#us-ui-22-profile-footer-menu" aria-label="Permalink to &quot;US-UI-22 Profile footer menu&quot;">​</a></h3><ul><li>On the sidebar footer, click the <code>Custom</code> / <code>Local profile</code> trigger.</li><li>Expect a 280px opaque elevated menu 8px above the footer. It repeats the local identity in a non-interactive header, then shows a divider and Settings, Logs, and Theme actions in that order.</li><li>Arrow keys wrap through the three actions; Home/End jump to the boundary. Escape closes the menu and restores trigger focus. An outside pointer press closes it without stealing target focus.</li><li>Settings navigates to the settings page, Logs opens local logs, and Theme cycles the current theme after closing the menu.</li></ul><h3 id="us-ui-23-project-archive-index" tabindex="-1">US-UI-23 Project archive index <a class="header-anchor" href="#us-ui-23-project-archive-index" aria-label="Permalink to &quot;US-UI-23 Project archive index&quot;">​</a></h3><ul><li>Open Settings → Project archive.</li><li>Expect the Settings title &quot;Project archive&quot; plus the one-workbench composition (D168/D267): a quiet intro line carrying the page description only, with no hero block, gradient banner, or page-level counter run; one toolbar with a Recent / Name sort segmented control, a search field, its clear affordance, the live match count, and the primary &quot;Add project&quot;; and one panel whose sections run Pinned, All projects, Archived as in-panel header strips, each present strip showing its label and row count. With no project at all the panel shows one quiet empty state with its own primary action.</li><li>Expect each row to carry a colored glyph, the project name with its Active / Open / pinned / Archived tags, one meta line with the shortened monospace path, branch, and session count, a relative last-active time, and the hover-revealed New task and row-menu actions. Ordinary rows use a Folder glyph; pinned rows use a filled Star glyph and retain the pinned text tag. Archived rows stay listed and softened rather than hidden.</li><li>Expand a non-active project and open one of its sessions; expect the app to activate that project before selecting the session, so workspace tools and session scope use the same project.</li><li>Switch the sort to Name and expect rows to reorder inside every section with no row hidden; clear the search and expect the complete index back.</li></ul><h3 id="us-ui-24-settings-full-page-shell" tabindex="-1">US-UI-24 Settings full-page shell <a class="header-anchor" href="#us-ui-24-settings-full-page-shell" aria-label="Permalink to &quot;US-UI-24 Settings full-page shell&quot;">​</a></h3><ul><li>Open Settings (footer profile → Settings).</li><li>Expect <strong>full-page</strong> Codex settings (no app sidebar/nav). Left rail has Back to app, search, and exactly Basics / 全局 AI / Shortcuts / Model configuration / Import / Project archive / Info in that order; content pane shows section title and the destination&#39;s settings or archive content.</li><li>Return to the app shell and expect Plugins to remain an independent sidebar-footer destination.</li><li>Drag the empty 46px top band over either the rail or content pane; the native window moves while Back, search, and navigation remain clickable.</li></ul><h3 id="us-ui-27-dark-destination-pages" tabindex="-1">US-UI-27 Dark destination pages <a class="header-anchor" href="#us-ui-27-dark-destination-pages" aria-label="Permalink to &quot;US-UI-27 Dark destination pages&quot;">​</a></h3><ul><li>Force dark theme and open Plugins and Settings → Project archive.</li><li>Expect black sidebar, main <code>#181818</code>, and destination cards/rows readable on elevated dark plates (not flat same-gray).</li></ul><h3 id="us-ui-28-home-empty-composer-association" tabindex="-1">US-UI-28 Home empty composer association <a class="header-anchor" href="#us-ui-28-home-empty-composer-association" aria-label="Permalink to &quot;US-UI-28 Home empty composer association&quot;">​</a></h3><ul><li>On empty chat home (light + dark), expect the hero, optional onboarding checklist, and home composer in one scrollable vertical flow (D111/D204/D206), without a large empty gap or starter-card layer.</li><li>The composer remains a standalone plate without an attached workspace rail.</li><li>Starting a transcript restores the bottom-docked composer with fade veil.</li></ul><h3 id="us-ui-29-light-composer-plate-legibility" tabindex="-1">US-UI-29 Light composer plate legibility <a class="header-anchor" href="#us-ui-29-light-composer-plate-legibility" aria-label="Permalink to &quot;US-UI-29 Light composer plate legibility&quot;">​</a></h3><ul><li>On light theme empty home, the white composer shell uses one uniform solid fill with no internal gradient or background image.</li><li>The shell still reads as an elevated box through a hairline stroke and restrained soft shadow against the <code>#ffffff</code> main surface.</li><li>Toolbar controls and placeholder remain legible (not pure white-on-white).</li></ul><h3 id="us-ui-30-composer-placeholder-copy" tabindex="-1">US-UI-30 Composer placeholder copy <a class="header-anchor" href="#us-ui-30-composer-placeholder-copy" aria-label="Permalink to &quot;US-UI-30 Composer placeholder copy&quot;">​</a></h3><ul><li>Empty home and session composers start with their localized welcome copy: <code>chat.placeholderHome</code> / <code>chat.placeholder</code>.</li><li>The selected guidance stays unchanged until the page/session context changes; switching context advances to localized <code>/</code>/<code>@</code> command/file guidance and the keyboard hint <code>Shift+Enter for newline · Use Send to submit</code>, with an opacity fade.</li><li>Waiting, focusing, editing, clearing, or composing does not change the copy.</li><li>Placeholder ink is legible on light and dark floating plates.</li></ul><h3 id="us-ui-31-home-empty-vertical-stack-d111-d204-d206" tabindex="-1">US-UI-31 Home empty vertical stack (D111/D204/D206) <a class="header-anchor" href="#us-ui-31-home-empty-vertical-stack-d111-d204-d206" aria-label="Permalink to &quot;US-UI-31 Home empty vertical stack (D111/D204/D206)&quot;">​</a></h3><ul><li>Given empty chat home, when the window is ~1200×690, the hero and optional onboarding checklist render in a centered scrollable content stack above a bottom-reserved home composer (not dual-grow absolute portal regions).</li><li>No starter cards or absolute overlay are present; the onboarding checklist remains actionable and the composer is directly available.</li></ul><h3 id="us-ui-32-dark-floating-box-elevation" tabindex="-1">US-UI-32 Dark floating box elevation <a class="header-anchor" href="#us-ui-32-dark-floating-box-elevation" aria-label="Permalink to &quot;US-UI-32 Dark floating box elevation&quot;">​</a></h3><ul><li>Given dark theme empty home, when the composer shell is painted, it uses elevated-primary <code>#212121</code> on <code>#181818</code> with elevation-prominent stroke+lift identical to light (no heavier custom dark shadow).</li></ul><h3 id="us-ui-33-scoped-sidebar-session-groups" tabindex="-1">US-UI-33 Scoped sidebar session groups <a class="header-anchor" href="#us-ui-33-scoped-sidebar-session-groups" aria-label="Permalink to &quot;US-UI-33 Scoped sidebar session groups&quot;">​</a></h3><ul><li>The home sidebar has no Recents aggregate.</li><li>It shows one independently collapsible header per retained project path with nested sessions and one <code>Temporary sessions</code> / <code>临时会话</code> header for path-less sessions.</li><li>Project and Temporary headers expose compact scope-specific <code>+</code> controls; project/session overflow menus expose pin/archive actions; nav row pitch remains ~32px and session row pitch ~28–31px.</li></ul><h3 id="us-ui-34-home-has-no-developer-starter-cards-d206" tabindex="-1">US-UI-34 Home has no developer starter cards (D206) <a class="header-anchor" href="#us-ui-34-home-has-no-developer-starter-cards-d206" aria-label="Permalink to &quot;US-UI-34 Home has no developer starter cards (D206)&quot;">​</a></h3><ul><li>On empty chat home (light + dark), no developer starter grid, card, or contextual quick-action row renders between the hero and composer.</li><li>Task entry starts directly in the bottom composer, while the optional onboarding checklist remains actionable when present.</li></ul><h3 id="us-ui-35-empty-composer-plate-density" tabindex="-1">US-UI-35 Empty composer plate density <a class="header-anchor" href="#us-ui-35-empty-composer-plate-density" aria-label="Permalink to &quot;US-UI-35 Empty composer plate density&quot;">​</a></h3><ul><li>Empty-home composer is compact and content-driven with an empty or one-line draft; it does not reserve the former fixed ~148px empty plate.</li></ul><h3 id="us-ui-36-hero-y-night-box-elevation" tabindex="-1">US-UI-36 Hero Y + night box elevation <a class="header-anchor" href="#us-ui-36-hero-y-night-box-elevation" aria-label="Permalink to &quot;US-UI-36 Hero Y + night box elevation&quot;">​</a></h3><ul><li>At ~1200×690 light home, the hero forms one centered block; the home scroller does not clip its top or overlap the bottom composer, and no starter grid is rendered.</li><li>Dark home composer plate reads as elevated-primary <code>#212121f5</code> with elevation-prominent against <code>#181818</code> (not flat same-surface).</li><li>Light composer renders as one uninterrupted solid surface with no context rail or independent top elevation.</li><li>Model chip shows the active model ID; its menu contains only runnable provider/model choices and Agent.</li><li>Placeholder and approval chip remain legible on light and dark plates.</li></ul><h3 id="us-ui-39-home-mark-hero-title-optical" tabindex="-1">US-UI-39 Home mark + hero title optical <a class="header-anchor" href="#us-ui-39-home-mark-hero-title-optical" aria-label="Permalink to &quot;US-UI-39 Home mark + hero title optical&quot;">​</a></h3><ul><li>Empty-home PI-Desktop mark is visible (not near-invisible); stroke density remains readable without a decorative ghost effect.</li><li>Empty-home title with a project uses a readable project label span (short basenames may display as <code>PI-Desktop</code> for optical parity).</li></ul><h3 id="us-ui-40-home-content-width-vs-rem-root" tabindex="-1">US-UI-40 Home content width vs rem root <a class="header-anchor" href="#us-ui-40-home-content-width-vs-rem-root" aria-label="Permalink to &quot;US-UI-40 Home content width vs rem root&quot;">​</a></h3><ul><li>At 1200×690 light empty home, composer plate outer width is ~744–760px (not ~640px).</li><li>Home suggestion grid spans the same content column as the composer plate.</li></ul><h3 id="us-ui-41-dark-hero-night-box-readability" tabindex="-1">US-UI-41 Dark hero + night box readability <a class="header-anchor" href="#us-ui-41-dark-hero-night-box-readability" aria-label="Permalink to &quot;US-UI-41 Dark hero + night box readability&quot;">​</a></h3><ul><li>Dark empty home hero title ink is light-on-dark (<code>--ds-text-primary</code> / near white), not hardcoded <code>#1a1c1f</code>.</li><li>Night composer plate is elevated-primary <code>#212121f5</code> on main <code>#181818</code> with elevation-prominent; light theme is not forced to the night plate fill.</li></ul><h3 id="us-ui-42-light-scoped-session-creation-chrome" tabindex="-1">US-UI-42 Light scoped session creation chrome <a class="header-anchor" href="#us-ui-42-light-scoped-session-creation-chrome" aria-label="Permalink to &quot;US-UI-42 Light scoped session creation chrome&quot;">​</a></h3><ul><li>On the light sidebar, the Sessions and Projects scoped create controls remain icon-only with semantic hover wash; no standalone New task row is rendered.</li></ul><h3 id="us-ui-43-empty-home-plate-y-night-elevated-primary" tabindex="-1">US-UI-43 Empty home plate Y + night elevated-primary <a class="header-anchor" href="#us-ui-43-empty-home-plate-y-night-elevated-primary" aria-label="Permalink to &quot;US-UI-43 Empty home plate Y + night elevated-primary&quot;">​</a></h3><ul><li>Open empty home at ~1200×690 light theme.</li><li>Composer plate is bottom-aligned and content-driven: an empty or one-line draft uses the compact shell rather than a fixed ~140px minimum; the surface remains uniformly solid with no decorative wash.</li><li>Switch dark theme: night plate is elevated-primary (<code>#212121f5</code> / gray-800 96%) with the same restrained elevation and no internal gradient.</li></ul><h3 id="us-ui-44-settings-compact-directory-merged-sections" tabindex="-1">US-UI-44 Settings compact directory + merged sections <a class="header-anchor" href="#us-ui-44-settings-compact-directory-merged-sections" aria-label="Permalink to &quot;US-UI-44 Settings compact directory + merged sections&quot;">​</a></h3><ul><li>Open Settings light theme at ~1200×690.</li><li>Full-page shell: rail ~260px on <code>#f3f3f3</code>, main <code>#fff</code>; search pill at the rail top; Back to app pinned at the rail foot and vertically centred on the main shell&#39;s sidebar footer icon line; General active pill with icon.</li><li>Rail order is exactly General / 常规, AI, Shortcuts / 快捷键, Instructions / 指令, Models / 模型, Skills / 技能, MCP, Subagents / 子智能体, Import / 导入, Projects / 项目, and Info / 信息; the rows are grouped under muted Preferences / 偏好, Agent / 智能体, Workspace / 工作区, and System / 系统 headings without divider lines, duplicate destinations, or placeholder rows. The selected page keeps its descriptive title, such as Model configuration or Project archive.</li><li>General content: large title and an <strong>Appearance</strong> card with working system/light/dark controls. 全局 AI holds Permissions, Defaults, and the Command shell row; Context management has no settings card. Shortcuts holds the Keyboard shortcuts card. File-open target, language override, menu-bar behavior, and bottom-panel behavior are absent until host-backed implementations exist.</li><li>Model configuration contains the default model selector, separate vendor account management with add/edit dialogs, and card-based AI service management with an add-provider dialog. The Defaults card reuses the compact geometry of every other settings row: the &quot;Default model&quot; label sits above the provider - model id line, and the quiet Change action remains separate from the current value. Change opens a floating listbox anchored to it: the card&#39;s height never changes, the model-level list is grouped by provider and scrolls once the configured models exceed its bounded height, it flips above the trigger near the viewport bottom, it is not clipped by the settings panel, and Escape, an outside press, or scrolling the trigger out of view closes it while focus returns to Change.</li><li>Plugin load/enable/disable/uninstall remains available from the app shell&#39;s independent Extensions destination; its Marketplace tab also owns the official/mirror/custom catalog source picker, so Settings has no duplicate Extensions destination.</li><li>Dark: rail <code>#000</code>, main <code>#181818</code>, cards elevated <code>#212121</code>.</li></ul><h3 id="us-ui-38-composer-workspace-context-omitted" tabindex="-1">US-UI-38 Composer workspace context omitted <a class="header-anchor" href="#us-ui-38-composer-workspace-context-omitted" aria-label="Permalink to &quot;US-UI-38 Composer workspace context omitted&quot;">​</a></h3><ul><li>On empty home, project home, and after starting a transcript, the composer never renders a project / Local / branch capsule.</li><li>Workspace identity remains visible through the home hero or sidebar rather than being duplicated above the prompt.</li></ul><h3 id="us-ui-37-empty-draft-row-resize" tabindex="-1">US-UI-37 Empty draft row + resize <a class="header-anchor" href="#us-ui-37-empty-draft-row-resize" aria-label="Permalink to &quot;US-UI-37 Empty draft row + resize&quot;">​</a></h3><ul><li>Empty composer prompt rows show no leading brand icon and retain visible placeholder ink (not a blank white/night hole).</li><li>Auto-resize never collapses empty textarea below ~28px.</li><li>Disabled send control is a solid gray chip on light (<code>#8e8e90</code>), full opacity with white arrow.</li><li>Dark night plate remains elevated-primary <code>#212121f5</code> with readable elevation-prominent on <code>#181818</code>.</li></ul><h3 id="us-ui-31b-superseded" tabindex="-1">US-UI-31b (superseded) <a class="header-anchor" href="#us-ui-31b-superseded" aria-label="Permalink to &quot;US-UI-31b (superseded)&quot;">​</a></h3><ul><li>Superseded by US-UI-31 home empty vertical stack (D111).</li></ul><h3 id="us-ui-45-composer-width-remains-stable-when-the-minimap-appears" tabindex="-1">US-UI-45 Composer width remains stable when the minimap appears <a class="header-anchor" href="#us-ui-45-composer-width-remains-stable-when-the-minimap-appears" aria-label="Permalink to &quot;US-UI-45 Composer width remains stable when the minimap appears&quot;">​</a></h3><ul><li>Open empty home and record the composer plate width. Open a short transcript that fits one viewport, then grow it until the left-edge conversation minimap appears.</li><li>The empty-home and docked composer plates use the same horizontal gutter and maximum content width. Their shell, textarea, placeholder alignment, toolbar padding, minimum input height, theme fill, and shadow are visually identical; only the parent placement and localized welcome copy differ. The minimap remains out of flow on the transcript&#39;s left edge and does not squeeze or resize the composer.</li><li>When transcript content first overflows, the native scrollbar gutter is already reserved on both sides, so the transcript and composer keep one horizontal center instead of jumping left.</li></ul><h3 id="us-ui-46-home-with-project-composer-chrome" tabindex="-1">US-UI-46 Home-with-project composer chrome <a class="header-anchor" href="#us-ui-46-home-with-project-composer-chrome" aria-label="Permalink to &quot;US-UI-46 Home-with-project composer chrome&quot;">​</a></h3><ul><li>Open a project on empty home (no transcript).</li><li>Expect no workspace controls attached to the plate; there is no legacy draft mark, and the placeholder uses the PI-Desktop copy.</li><li>Model chip shows the active model ID; the footer uses the circular local-user glyph, two-line Custom / Local profile identity, disclosure chevron, and separate Help → Settings Info control.</li></ul><h3 id="us-ui-47-projects-index-parity" tabindex="-1">US-UI-47 Projects index parity <a class="header-anchor" href="#us-ui-47-projects-index-parity" aria-label="Permalink to &quot;US-UI-47 Projects index parity&quot;">​</a></h3><ul><li>Open Settings → Project archive.</li><li>Expect the Settings section title, search pill, Add project button, and the complete durable project list including archived rows.</li><li>Rows expand for recent tasks; activating a project or one of its sessions uses <code>setProject</code> without re-picking via dialog and keeps session/workspace context synchronized. Sidebar pin, archive, and close metadata remains local to the renderer; only the explicit row-menu Delete project action removes a durable Project-archive row, and it removes that project&#39;s sessions with it (ADR 0251).</li></ul><h3 id="us-ui-48-home-starter-glyphs-and-labels-are-absent-d206" tabindex="-1">US-UI-48 Home starter glyphs and labels are absent (D206) <a class="header-anchor" href="#us-ui-48-home-starter-glyphs-and-labels-are-absent-d206" aria-label="Permalink to &quot;US-UI-48 Home starter glyphs and labels are absent (D206)&quot;">​</a></h3><ul><li>On empty home, no developer starter icon plate, title/description, or starter-card glyph renders in light or dark themes.</li><li>The hero, optional onboarding checklist, and bottom composer remain the only empty-home task-entry surfaces.</li></ul><h3 id="us-ui-49-scoped-sidebar-row-chrome" tabindex="-1">US-UI-49 Scoped sidebar row chrome <a class="header-anchor" href="#us-ui-49-scoped-sidebar-row-chrome" aria-label="Permalink to &quot;US-UI-49 Scoped sidebar row chrome&quot;">​</a></h3><ul><li>Hover or select a project or temporary session row.</li><li>Expect restrained title rows with active/hover background and compact overflow actions for pin/archive (not a Recents aggregate).</li><li>Multiple retained project groups may be visible at once; sessions remain under exact-path groups, while closed-project sessions remain available in Settings → Project archive.</li></ul><h3 id="us-ui-50-destination-title-scale" tabindex="-1">US-UI-50 Destination title scale <a class="header-anchor" href="#us-ui-50-destination-title-scale" aria-label="Permalink to &quot;US-UI-50 Destination title scale&quot;">​</a></h3><ul><li>Open Settings → Project archive and Plugins.</li><li>Expect large section titles (~28px) consistent with Codex destination/index pages.</li><li>Dark home scoped session-creation controls remain quiet icon actions without a standalone New task row.</li></ul><h3 id="us-ui-52-settings-gold-chrome-metrics-d070" tabindex="-1">US-UI-52 Settings gold chrome metrics (D070) <a class="header-anchor" href="#us-ui-52-settings-gold-chrome-metrics-d070" aria-label="Permalink to &quot;US-UI-52 Settings gold chrome metrics (D070)&quot;">​</a></h3><ul><li>Open Settings light Basics at ~1200×690.</li><li>Expect ~275px <code>#f4f4f4</code> rail, single active Basics pill, Back + search.</li><li>Expect the working theme selector without inert toggle or open-target rows.</li><li>Expect Permissions + Basics + Appearance elevated cards; Agent, Import, and Info remain the only other destinations.</li><li>Resize between 1040px, 1200px, and 1600px widths; the content cards fill the available right pane at each size without changing the rail or introducing horizontal scrolling.</li></ul><h3 id="us-ui-53-settings-dark-shell-d070" tabindex="-1">US-UI-53 Settings dark shell (D070) <a class="header-anchor" href="#us-ui-53-settings-dark-shell-d070" aria-label="Permalink to &quot;US-UI-53 Settings dark shell (D070)&quot;">​</a></h3><ul><li>Dark theme Settings Basics: black rail, elevated cards, blue on-toggles, Back returns to chat.</li><li>Row descriptions use theme-aware secondary text and remain clearly readable on the <code>#212121</code> card surface; they must not fall back to low-contrast muted ink.</li></ul><h3 id="us-ui-54-toast-variants-lifecycle-d085" tabindex="-1">US-UI-54 Toast variants + lifecycle (D085) <a class="header-anchor" href="#us-ui-54-toast-variants-lifecycle-d085" aria-label="Permalink to &quot;US-UI-54 Toast variants + lifecycle (D085)&quot;">​</a></h3><ul><li>Trigger a success (save provider), an error (run with an invalid key), and an info toast from a test plugin.</li><li>Expect a top-center stack on an elevated plate with a tinted variant icon (green ✓ / red ! / info) and an X dismiss per card; newest enters at the top-center anchor and pushes older cards down.</li><li>Success/info auto-dismiss ~4s, error lingers ~8s; hovering a card pauses its countdown; X removes it immediately.</li><li>With the stack overlapping the frameless titlebar band, hover still pauses the countdown and every X remains clickable instead of dragging the window.</li><li>Repeating the same action restarts the existing toast instead of stacking a duplicate; stack never exceeds 4.</li><li>Toast message text is selectable: dragging across a card highlights only its message text and <code>Cmd/Ctrl+C</code> copies it, while the card icon and its X dismiss control expose no selection.</li><li>Capture rig scenes <code>pi-toasts-light</code> / <code>pi-toasts-dark</code> show the stack in both themes.</li></ul><h3 id="us-ui-55-composer-textarea-growth-d089" tabindex="-1">US-UI-55 Composer textarea growth (D089) <a class="header-anchor" href="#us-ui-55-composer-textarea-growth-d089" aria-label="Permalink to &quot;US-UI-55 Composer textarea growth (D089)&quot;">​</a></h3><ul><li>In both home and thread-docked composers, an empty or single-line draft displays one visible text line.</li><li>Enter or paste two through seven visual lines; the textarea grows with the wrapped content without manual resizing.</li><li>Add an eighth visual line; the textarea stays at seven visible lines and scrolls internally instead of growing the composer further.</li><li>Delete back to one line or submit the draft; the textarea contracts to its one-line default.</li><li>With a large workspace open and the <code>@</code> file menu filtering, type sustained text; the caret keeps up with typing without visible stalls and the menu&#39;s row order is unchanged. Growth through seven lines, internal scrolling past the seventh, and contraction on delete or submit behave as above (D264).</li></ul><h3 id="us-ui-56-codex-transcript-tool-activity" tabindex="-1">US-UI-56 Codex transcript tool activity <a class="header-anchor" href="#us-ui-56-codex-transcript-tool-activity" aria-label="Permalink to &quot;US-UI-56 Codex transcript tool activity&quot;">​</a></h3><ul><li>In light and dark themes, tool calls use transparent compact activity rows, not elevated cards or colored success rails.</li><li>Historical consecutive calls appear inside a default-collapsed processing group. During a live turn, the latest group opens automatically so its process list is visible, but tool-call details stay collapsed. The latest thinking step opens automatically; when the turn settles, only that automatic thinking disclosure closes. A group or row touched by the user keeps its chosen state.</li><li>Its active header shows <code>Processing · {elapsed}</code> and a localized current action/phase capsule such as <code>Editing</code>, <code>Thinking</code>, or <code>Waiting for model</code>; its completed header shows <code>Processed for {elapsed}</code>, plus a localized step count.</li><li>The row shows a semantic 15–16px icon, progressive/past-tense action, ellipsized monospace argument hint, quiet disclosure chevron, and localized running/error/denied state.</li><li>Fork-family tools show the GitFork branch icon instead of the generic tool glyph.</li><li>Expanding a completed call reveals output before input. Both sections are independently copyable and capped with internal scrolling.</li><li>Reloading the session preserves the action label and argument hint instead of degrading the row to a generic <code>Tool</code>.</li><li>Run a turn that emits assistant text, calls multiple tools, and resumes with more assistant text. During streaming and after session reload, expect one assistant article for the whole user turn, with fragments and activity in original order but only one trailing model/usage row and one Copy/Fork/Retry toolbar. Copy includes all assistant text fragments in order.</li></ul><h3 id="us-ui-57-multi-project-sidebar-groups" tabindex="-1">US-UI-57 Multi-project sidebar groups <a class="header-anchor" href="#us-ui-57-multi-project-sidebar-groups" aria-label="Permalink to &quot;US-UI-57 Multi-project sidebar groups&quot;">​</a></h3><ul><li>Open projects A and B without closing either.</li><li>Expect a <code>Sessions</code> heading above <code>Projects</code>, containing path-less conversations plus new-session and sort actions. With more than five standalone conversations, expect a five-row-high list that scrolls to every remaining row without growing further.</li><li>Right-click the <code>Sessions</code> heading or empty standalone list chrome and expect a one-item create menu that creates/reuses a path-less temporary session.</li><li>Expect the following <code>Projects</code> heading to retain its new-project folder action, one path-keyed group per retained project, and an active-state marker on exactly one group. Its list consumes the remaining height and scrolls independently. Adjacent project groups read as a compact continuous tree without detached card spacing.</li><li>Right-click the <code>Projects</code> heading or empty project-list chrome and expect a one-item create menu that opens the same project picker as the folder-plus action.</li><li>Expect project and session lists to scroll inside the sidebar body without clipping behind the footer; sidebar Collapse remains in the sidebar header. When the work panel is open, expect its sole collapse control to be the viewport-fixed toggle in the window&#39;s top-right corner rather than a chevron in the work-panel content header.</li><li>Collapse A by clicking its directory label, expand it from the chevron area, then activate B and return to A. Only A&#39;s child rows collapse; project <code>+</code> and overflow actions do not toggle it; the active project, topbar path, and transcript switch together; the composer remains free of workspace identity chrome.</li><li>Close B and reopen it from Settings → Project archive. Closing removes only the sidebar tab; durable project/session rows remain available.</li></ul><h3 id="us-ui-58-sidebar-organization-actions" tabindex="-1">US-UI-58 Sidebar organization actions <a class="header-anchor" href="#us-ui-58-sidebar-organization-actions" aria-label="Permalink to &quot;US-UI-58 Sidebar organization actions&quot;">​</a></h3><ul><li>Open a project and conversation overflow menu.</li><li>Expect localized Pin/Unpin, Archive/Restore, and (for conversations) Delete actions with keyboard-reachable menu semantics.</li><li>Open the sort menu from the standalone <code>Sessions</code> heading, pin one project/session, and choose each user-facing sort mode (Recently updated, Created date, Oldest first, Name). Pinned projects remain first in Projects; pinned conversations appear once in the global Pinned section above Sessions and Projects, with the selected session sort and no date headers.</li><li>Archive a row, verify it is absent by default, enable Show archived, and restore it. The transcript and project binding remain unchanged.</li><li>A legacy <code>manual</code> preference loads without presenting a drag-reorder affordance.</li></ul><h3 id="e2e-sidebar-global-pinned-conversations" tabindex="-1">E2E-SIDEBAR-global-pinned-conversations <a class="header-anchor" href="#e2e-sidebar-global-pinned-conversations" aria-label="Permalink to &quot;E2E-SIDEBAR-global-pinned-conversations&quot;">​</a></h3><ul><li>Seed an old pinned conversation in project A, today&#39;s unpinned conversation and eleven other normal rows in A, a pinned conversation in collapsed project B, one in closed project C, and a pinned Temporary conversation.</li><li>Expect one Pinned section above Sessions and Projects, containing all four pins with their project names or Temporary space label. Pins have no date headers and do not appear a second time in ordinary history. A still shows ten normal rows initially, with its remaining rows behind Load more.</li><li>Change the date across midnight and select each session sort. Pins remain above history; sorting changes only their internal order. Selecting B or C&#39;s pin activates its original conversation and project; selecting the Temporary pin clears workspace context. Running and unread states remain visible.</li><li>Pin and unpin through the keyboard menu. The row moves immediately and focus follows its overflow control, or returns to the Sessions sort control if the row is now folded or belongs to a closed project. Unpin the last pin and expect no empty Pinned section.</li><li>Archive a pin and a pinned conversation&#39;s project. Both disappear by default; Show archived reveals them and Restore preserves the pin. Delete a pinned conversation and expect no stale row. Reload and expect saved pins to return.</li><li>With enough pins to overflow, scroll within Pinned and verify that Sessions, Projects, and the footer remain reachable in light/dark themes at minimum supported window size. Existing hover cards, context menus, drag/drop, and project pinning retain their normal behavior.</li></ul><h3 id="e2e-project-delete-removes-project-and-owned-sessions" tabindex="-1">E2E-PROJECT-delete-removes-project-and-owned-sessions <a class="header-anchor" href="#e2e-project-delete-removes-project-and-owned-sessions" aria-label="Permalink to &quot;E2E-PROJECT-delete-removes-project-and-owned-sessions&quot;">​</a></h3><ul><li><strong>Preconditions</strong>: three durable projects A, B, and C, each with at least one session that has a transcript; A archived and retained as a sidebar tab; B the active workspace; C a root of a stored two-folder project group.</li><li><strong>Steps</strong>: open Settings → Project archive, open A&#39;s row menu, choose Delete project, and confirm in the dialog. Then repeat the same action from the sidebar project menu for B while B is the active workspace: the item arms on the first click and only the second click removes B. Then attempt the same action for C, then for a path the host no longer knows, and finally for a fourth project D while one of D&#39;s tasks is still running.</li><li><strong>Expected</strong>: the dialog names the project, states that the project and its sessions with their transcripts are removed permanently, and states that the folder on disk is not deleted; nothing is removed before the confirmation, and an armed item that is left alone disarms itself and removes nothing. After confirming, the durable project row, that project&#39;s sessions, their transcripts, scratch and review files, and its durable project memory are gone, while the folder on disk is untouched. The deleted project disappears from Settings → Project archive and from the sidebar immediately and again after a reload: no retained tab, no recent-project entry, no session-derived row, and no stale pin, archive, or order preference. Sessions and transcripts of every other project are untouched. When the deleted project was the active workspace, the workspace falls back to another open project or to Temporary, and the next launch does not reopen the deleted path. A project whose folder was moved or deleted on disk is still removable. Deleting C is refused with a message and the group is unchanged; a path the host has no durable row for is removed from the archive and the sidebar anyway, without a missing-project error. Deleting D while its task runs opens the confirmation dialog instead of a warning that disappears with its toast; the dialog names the running sessions and its confirm button reads as stopping them, cancelling removes nothing, and confirming stops exactly those turns and then deletes D (see E2E-PROJECT-delete-running-sessions-are-named-and-stopped).</li><li><strong>Specs linked</strong>: <code>03-runtime/06-host-rpc-protocol.md</code> §Projects, <code>03-runtime/04-data-storage.md</code>, <code>04-ux/08-component-spec.md</code> §3.9, ADR 0251</li><li><strong>Acceptance criterion</strong>: D (workspace), F (persistence), Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Partially automated — <code>pnpm test:e2e</code> covers the host contract (project row, owned sessions, on-disk transcripts and scratch, project memory, isolation from other projects, the running-task refusal, the group-root refusal, and the idempotent unknown path), and <code>pnpm test:e2e:boot</code> round-trips <code>pi-desktop/project/remove</code> through the sandboxed preload; the Settings archive → dialog → sidebar journey remains Draft</li></ul><h3 id="e2e-project-delete-running-sessions-are-named-and-stopped" tabindex="-1">E2E-PROJECT-delete-running-sessions-are-named-and-stopped <a class="header-anchor" href="#e2e-project-delete-running-sessions-are-named-and-stopped" aria-label="Permalink to &quot;E2E-PROJECT-delete-running-sessions-are-named-and-stopped&quot;">​</a></h3><ul><li><strong>Preconditions</strong>: a durable project D with one session whose turn is still streaming, reachable both from the sidebar project menu and from Settings → Project archive.</li><li><strong>Steps</strong>: from each surface, open D&#39;s row menu and choose Delete project without stopping the turn. Expect the confirmation dialog with a running-session line and a stop-and-delete confirm label; press Cancel and expect nothing to change. Open the dialog again and confirm.</li><li><strong>Expected</strong>: the menu never replaces the dialog with a bare warning, so the action stays reachable while a task runs. The dialog keeps naming the project, its session count, and the untouched folder; while a turn is live it also names how many sessions are still running, its confirm button reads as stopping them, and that line joins the dialog&#39;s <code>aria-describedby</code> only while it is rendered. Cancelling deletes nothing and leaves the turn streaming. Confirming stops exactly the listed sessions and only then removes the project, its sessions, their transcripts, and its durable memory, leaving the folder on disk. A turn that starts between the dialog opening and the confirmation is still refused by the host, and the dialog reports that refusal with <code>project.deleteRunningBlocked</code> while removing nothing.</li><li><strong>Specs linked</strong>: <code>03-runtime/06-host-rpc-protocol.md</code> §Projects, <code>04-ux/08-component-spec.md</code> §3.9, ADR 0251, D421, D431</li><li><strong>Acceptance criterion</strong>: D (workspace), Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Partially automated — <code>apps/desktop/test/project-delete.test.mjs</code> pins both menus reaching the dialog with the project&#39;s live running session ids, the dialog&#39;s running-session line and stop-and-delete label, the abort loop running before <code>deleteProject</code>, the <code>CONFLICT</code> fallback, and the new copy in every shipped catalog; the end-to-end journey remains Draft</li></ul><h3 id="e2e-session-two-click-delete-arms-first" tabindex="-1">E2E-SESSION-two-click-delete-arms-first <a class="header-anchor" href="#e2e-session-two-click-delete-arms-first" aria-label="Permalink to &quot;E2E-SESSION-two-click-delete-arms-first&quot;">​</a></h3><ul><li><strong>Preconditions</strong>: a project with one idle session and one running session, both reachable from the sidebar session menu, the sidebar project menu, and the Projects index.</li><li><strong>Steps</strong>: open the session menu of the idle session, press Delete once, and leave the item armed until the arm expires before pressing it again to confirm the removal. Repeat for a project row from the sidebar menu and from the Projects index.</li><li><strong>Expected</strong>: the first press removes nothing and relabels the item to <code>nav.deleteTaskConfirm</code> / <code>project.deleteMenuConfirm</code> (&quot;Delete?&quot; / &quot;确认删除？&quot;) with <code>data-armed=&quot;true&quot;</code>; the menu stays open, and an outside press, Escape, or the expiry clears the arm without removing anything. Only the second press removes the session with its transcript and its row, and only the second press on a project row removes an idle project. A session and a project never share an arm. Deleting a project whose turn is live still opens the dialog that names those sessions and stops them (see E2E-PROJECT-delete-running-sessions-are-named-and-stopped).</li><li><strong>Specs linked</strong>: <code>04-ux/09-interaction-patterns.md</code> §1.6, D421, D431, D441</li><li><strong>Acceptance criterion</strong>: Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Partially automated — <code>apps/desktop/test/two-step-delete.test.mjs</code> pins the shared arm and its expiry, both labels in every shipped catalog, and that the first press only arms; the end-to-end journey remains Draft</li></ul><h3 id="us-ui-59-session-rooted-background-tools" tabindex="-1">US-UI-59 Session-rooted background tools <a class="header-anchor" href="#us-ui-59-session-rooted-background-tools" aria-label="Permalink to &quot;US-UI-59 Session-rooted background tools&quot;">​</a></h3><ul><li>Start a visible turn in project A, switch to project B while it runs, and inspect both sidebar status indicators.</li><li>Expect A&#39;s turn to continue in the background, B&#39;s composer/context to show only B, and tool output/artifacts from A to remain rooted in A without opening or activating a work-panel tab over B.</li><li>Open a Temporary session and invoke a workspace-required tool; expect the normal <code>WORKSPACE_REQUIRED</code> result rather than inheritance from B.</li></ul><h3 id="us-ui-60-workbuddy-transcript-plates-d101" tabindex="-1">US-UI-60 WorkBuddy transcript plates (D101) <a class="header-anchor" href="#us-ui-60-workbuddy-transcript-plates-d101" aria-label="Permalink to &quot;US-UI-60 WorkBuddy transcript plates (D101)&quot;">​</a></h3><ul><li>Open a mixed transcript in light and dark themes.</li><li>Expect right-aligned compact user plates, transparent full-width assistant prose, denser row spacing, and hover-only copy chips under each turn.</li><li>While an assistant answer streams, expect the same transparent full-width prose as a completed turn — no left rail and no whole-turn tile. The tile belongs only to a subagent/delegation card (D319, D323).</li></ul><h3 id="us-ui-60b-assistant-markdown-prose-redesign" tabindex="-1">US-UI-60b Assistant markdown prose redesign <a class="header-anchor" href="#us-ui-60b-assistant-markdown-prose-redesign" aria-label="Permalink to &quot;US-UI-60b Assistant markdown prose redesign&quot;">​</a></h3><ul><li>Open an assistant answer with headings, table, code fence, blockquote, and task list.</li><li>Expect the refined <code>.prose-chat</code> hierarchy and inset code chrome in both themes.</li><li>Expand thinking markdown and confirm it stays visually subordinate to the answer.</li></ul><h3 id="us-ui-60c-compact-assistant-error-card" tabindex="-1">US-UI-60c Compact assistant error card <a class="header-anchor" href="#us-ui-60c-compact-assistant-error-card" aria-label="Permalink to &quot;US-UI-60c Compact assistant error card&quot;">​</a></h3><ul><li>Trigger a retriable provider/model failure in the transcript in light and dark themes.</li><li>Expect the assistant error to use a restrained inline surface with a thin error rail. The localized summary, stable code, and details disclosure share one compact header; the card does not render a second bottom action row.</li><li>Confirm the details remain expanded on first render, keep the redacted provider response and provider/model IDs, and expose an icon-only copy control with an accessible label/tooltip. On a narrow window, the header actions wrap without horizontal overflow.</li><li>Expect the compact assistant error card itself to expose one localized <strong>Continue</strong> action beside the details disclosure. Click it and expect the app to append the localized continuation prompt (<code>Continue the current task</code> / <code>继续当前任务</code>) to the same session and start the next turn without truncating the failed turn.</li><li>For a terminal <code>PROVIDER_RATE_LIMITED</code> (including HTTP 429), expect the structured assistant error card to remain the only failure surface: it exposes exactly one localized <strong>Continue</strong> action and no <strong>Regenerate</strong> action, while the generic TurnOutcomeCard is omitted.</li><li>Click <strong>Continue</strong> and expect the app to append the localized continuation prompt (<code>Continue the user&#39;s unfinished task.</code> / <code>继续用户未完成的任务</code>) to the same session and start the next turn without truncating the failed turn.</li></ul><h3 id="us-ui-61-assistant-context-summary-retry-d103-d184-d244-d347" tabindex="-1">US-UI-61 Assistant context summary + retry (D103, D184, D244, D347) <a class="header-anchor" href="#us-ui-61-assistant-context-summary-retry-d103-d184-d244-d347" aria-label="Permalink to &quot;US-UI-61 Assistant context summary + retry (D103, D184, D244, D347)&quot;">​</a></h3><ul><li>Complete an assistant turn that reports usage.</li><li>Expect a model badge under the answer and the compact Context inspector in the composer toolbar, left of the model picker. The trigger shows the remaining-capacity ring and percentage; clicking it (or activating it from the keyboard) shows remaining tokens plus percentage, used/window counts, two unboxed turn/speed values, one inline exact provider-usage summary, and one aggregate tool-usage summary with types, calls, and approximate tokens. Per-tool rows, bars, badges, explanatory estimate copy, and inner section hairlines are not shown.</li><li>Hovering the trigger changes nothing; the open panel closes on a second activation, an outside click, or Escape.</li><li>Scroll or resize while the panel is open; expect the body-level overlay to flip, clamp, and remain fully visible instead of being clipped by the composer or transcript.</li><li>Hover the action row and click Retry; the nearest preceding user prompt is re-sent.</li></ul><h3 id="us-ui-62-in-place-regenerate-d105" tabindex="-1">US-UI-62 In-place regenerate (D105) <a class="header-anchor" href="#us-ui-62-in-place-regenerate-d105" aria-label="Permalink to &quot;US-UI-62 In-place regenerate (D105)&quot;">​</a></h3><ul><li>On a multi-turn transcript, regenerate an earlier assistant answer.</li><li>Expect the later turns to disappear and the chosen user prompt to re-run in place, without stacking a second copy of the prompt.</li></ul><h3 id="us-ui-63-regenerate-history-pager-d109" tabindex="-1">US-UI-63 Regenerate history pager (D109) <a class="header-anchor" href="#us-ui-63-regenerate-history-pager-d109" aria-label="Permalink to &quot;US-UI-63 Regenerate history pager (D109)&quot;">​</a></h3><ul><li>Regenerate an assistant answer twice.</li><li>After each retry, hover or focus the root user bubble and expect its action toolbar to expose a <code>1/N</code> pager for restoring earlier variants.</li></ul><h3 id="us-ui-64-empty-home-no-composer-overlap-d111-d204-d206" tabindex="-1">US-UI-64 Empty home no composer overlap (D111/D204/D206) <a class="header-anchor" href="#us-ui-64-empty-home-no-composer-overlap-d111-d204-d206" aria-label="Permalink to &quot;US-UI-64 Empty home no composer overlap (D111/D204/D206)&quot;">​</a></h3><ul><li>Open empty home at ~1200×690 and at a shorter height (~900×640).</li><li>Expect the hero and optional onboarding checklist in a scrollable content region, with the home composer visibly reserved at the bottom and no starter cards.</li><li>Short windows scroll the content region rather than stacking the composer over the checklist; when the checklist is absent, no empty spacer remains.</li></ul><h3 id="us-ui-65-durable-notification-inbox-d117-d130" tabindex="-1">US-UI-65 Durable notification inbox (D117/D130) <a class="header-anchor" href="#us-ui-65-durable-notification-inbox-d117-d130" aria-label="Permalink to &quot;US-UI-65 Durable notification inbox (D117/D130)&quot;">​</a></h3><ul><li>Verify a focused-current completion leaves the inbox unchanged, then populate it through background/unfocused completed and failed task rows, including one long session title. Inspect the expanded sidebar footer and popover in light/dark themes at default and narrow supported widths.</li><li>Expect no titlebar bell, a stable 32px footer bell in the former Help position, a non-overlapping <code>1</code>–<code>99</code> / <code>99+</code> badge, dense 360px-or-narrower list, localized kind/session/time/error content, and distinct text/icon/unread-dot semantics without nested cards or clipped text.</li><li>Switch All/Unread; use Tab, arrow keys, Home/End, Enter/Space, Escape, and outside click. Focus order remains predictable, row activation opens the correct session, and Escape restores focus to the bell.</li><li>Mark all read and Clear expose icon tooltips/accessible names, disabled and empty states remain understandable, and reduced-motion mode changes the popover instantly without suppressing focus or unread state.</li></ul><h3 id="us-ui-66-application-update-notice-layout" tabindex="-1">US-UI-66 Application update notice layout <a class="header-anchor" href="#us-ui-66-application-update-notice-layout" aria-label="Permalink to &quot;US-UI-66 Application update notice layout&quot;">​</a></h3><ul><li>In a conversation with the docked composer visible, exercise manual <code>available</code>, in-app <code>downloading</code>, and <code>downloaded</code> update fixtures in light and dark themes at default and minimum supported window sizes. Grow the composer draft to its maximum visible height.</li><li>Expect one compact update notice below the titlebar in the main pane&#39;s top-right safe area. It never intersects the composer, including while the draft grows, and it does not cover an open work panel.</li><li>Expect a stable update icon/title/message hierarchy, determinate progress for <code>downloading</code>, the applicable View release or Restart to update action, and a 24px dismiss control with an accessible name. Dismissing one status stage does not suppress a later stage for the same version.</li><li>When the fixture includes <code>releaseNotes</code>, expect a &quot;What&#39;s new&quot; section under the status message on both the banner and Settings → Info Updates row, using the product UI locale (EN or zh-CN). A fixture without notes omits the section. Switching language re-resolves the same version&#39;s notes without a new check.</li></ul><h3 id="us-ui-67-distinct-sidebar-task-status-indicators-d135" tabindex="-1">US-UI-67 Distinct sidebar task status indicators (D135) <a class="header-anchor" href="#us-ui-67-distinct-sidebar-task-status-indicators-d135" aria-label="Permalink to &quot;US-UI-67 Distinct sidebar task status indicators (D135)&quot;">​</a></h3><ul><li>In light and dark themes, keep session B selected while session A progresses through in-progress, completed, a new in-progress turn, failed, and aborted states. Repeat with reduced motion enabled and inspect keyboard focus.</li><li>Expect A to show an orange breathing dot while in progress, a green check on completion, and a red circled alert on failure. Starting a new turn clears A&#39;s earlier terminal mark; abort leaves no completed or failed mark.</li><li>Expect selected idle B to show a static accent-blue outlined ring and active row background. If selected B starts work, its orange in-progress dot takes precedence until the turn settles; its latest terminal result remains hidden behind the selected ring while selected.</li><li>Every indicator exposes localized In progress / Selected / Completed / Failed text through its accessible name and tooltip. Reduced motion makes the orange dot static without changing its color or meaning. Row height, title truncation, pin icon, hover actions, and focus ring remain stable in both themes.</li><li>Open a conversation with a completed or failed mark and expect that terminal mark to clear immediately while its durable task notification becomes read. Refresh notifications and restart the app; the acknowledged mark must not return. A terminal notification marked read from the inbox likewise produces no sidebar terminal mark.</li></ul><h3 id="us-ui-68-session-scoped-inline-permissions-and-artifacts-d138-d142" tabindex="-1">US-UI-68 Session-scoped inline permissions and artifacts (D138/D142) <a class="header-anchor" href="#us-ui-68-session-scoped-inline-permissions-and-artifacts-d138-d142" aria-label="Permalink to &quot;US-UI-68 Session-scoped inline permissions and artifacts (D138/D142)&quot;">​</a></h3><ul><li>Run two sessions concurrently and keep A visible while B reaches a tool approval request. Inspect light/dark themes at default and narrow widths.</li><li>Expect no backdrop, modal, page/session switch, work-panel hide, transcript replacement, or composer-focus change in A. B retains its pending state.</li><li>Open B explicitly and expect one inline permission card after B&#39;s latest activity, with readable risk, args, workspace, countdown, and wrapping action controls. Switching away and back preserves the absolute deadline.</li><li>Make A and B pending together, resolve each independently, and confirm neither action removes or changes the other card.</li><li>Resolve A&#39;s Write/Edit permission and switch to B before completion. Expect no transient Review panel in B and no panel/window flash; returning to A restores A&#39;s prior panel selection with the inline review card in its transcript — the edit opened no tab — while B&#39;s tabs and Browser resource remain unchanged.</li></ul><h3 id="us-ui-69-sidebar-type-balance-d144-d161" tabindex="-1">US-UI-69 Sidebar type balance (D144/D161) <a class="header-anchor" href="#us-ui-69-sidebar-type-balance-d144-d161" aria-label="Permalink to &quot;US-UI-69 Sidebar type balance (D144/D161)&quot;">​</a></h3><ul><li>Open the expanded sidebar in light and dark themes at default and minimum supported widths with at least one session, one project group, and the local profile footer visible.</li><li>Expect Plugins, footer profile name, and profile menu actions to render at the body chrome size (<code>--text-base</code> / 14px).</li><li>Expect session/thread titles, project/group titles, and empty-state copy at <code>--text-md</code> / 13px, with uppercase section labels (<code>SESSIONS</code> / <code>PROJECTS</code>) at <code>--text-sm</code> / 12px — never below <code>--text-md</code> for primary list content.</li><li>Confirm row pitch remains compact (≈28–32px), titles still truncate cleanly, and collapsed icon-rail controls stay legible without reflowing the shell.</li></ul><h3 id="us-ui-70-disable-text-correction-on-editable-fields-d145" tabindex="-1">US-UI-70 Disable text correction on editable fields (D145) <a class="header-anchor" href="#us-ui-70-disable-text-correction-on-editable-fields-d145" aria-label="Permalink to &quot;US-UI-70 Disable text correction on editable fields (D145)&quot;">​</a></h3><ul><li>Open empty home, a docked transcript, Settings search, Plugins market search, Projects archive search, global search (which now includes commands), provider model combo, message-edit textarea, and the work-panel browser URL bar in light and dark.</li><li>Expect every text <code>input</code>/<code>textarea</code> to expose <code>spellcheck=&quot;false&quot;</code> (React <code>spellCheck={false}</code>) plus <code>autocorrect=&quot;off&quot;</code> and <code>autocapitalize=&quot;off&quot;</code>.</li><li>Expect no red spelling underlines while typing code-like tokens, paths, model ids, or URLs; checkboxes and non-text controls remain unchanged.</li></ul><h3 id="us-ui-71-composer-runtime-chip-descenders-d150" tabindex="-1">US-UI-71 Composer runtime chip descenders (D150) <a class="header-anchor" href="#us-ui-71-composer-runtime-chip-descenders-d150" aria-label="Permalink to &quot;US-UI-71 Composer runtime chip descenders (D150)&quot;">​</a></h3><ul><li>Open empty home and a docked thread with a model ID that contains descenders (for example <code>gpt</code>, <code>gemini</code>, or any id with <code>g</code>/<code>y</code>/<code>p</code>/<code>q</code>/<code>j</code>).</li><li>Inspect Agent/Plan/Goal, Thinking (when present), permission mode, and the model chip in light and dark.</li><li>Expect every chip label to show full glyph ink — bottoms of <code>g</code>/<code>y</code>/<code>p</code> are not clipped by the 28px capsule — while long model IDs still ellipsize horizontally.</li><li><strong>Specs linked</strong>: <code>04-ux/07-ui-design-system.md</code> §8.2, <code>04-ux/08-component-spec.md</code> §11.5, decisions-log D150</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Partially automated (renderer source test: chip line-height + no leading-none)</li></ul><h3 id="us-ui-72-apple-inspired-global-corner-hierarchy-d210" tabindex="-1">US-UI-72 Apple-inspired global corner hierarchy (D210) <a class="header-anchor" href="#us-ui-72-apple-inspired-global-corner-hierarchy-d210" aria-label="Permalink to &quot;US-UI-72 Apple-inspired global corner hierarchy (D210)&quot;">​</a></h3><ul><li>Open the empty home, a populated transcript, Settings, Plugins, Project archive, a menu, and a dialog at the default supported desktop size in light and dark themes.</li><li>Expect fixed corners to follow the global 4/6/8/10/12/14/16/18/20/24px ladder, with visually larger or more elevated surfaces receiving the larger radii.</li><li>Standard compact and medium buttons and fields remain rounded rectangles, not capsules. Pills, segmented selections, status labels, progress tracks, switches, equal-width circular icon controls, and dots retain their explicit capsule or circle shape.</li><li>Where a rounded child sits against a rounded parent corner, expect the radii to read concentrically with the intervening inset. Full-width sidebar, titlebar, and work-panel edges remain square rather than becoming floating cards.</li><li>Resize to the minimum supported window and inspect menus/dialogs near each edge. Rounded surfaces must not clip text, focus rings, actions, or scrollable content.</li><li><strong>Specs linked</strong>: <code>04-ux/07-ui-design-system.md</code> §6.2, ADR 0071</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Partially automated (radius token and shared-control source test)</li></ul><h3 id="us-ui-73-composer-mode-selector-has-stable-width" tabindex="-1">US-UI-73 Composer mode selector has stable width <a class="header-anchor" href="#us-ui-73-composer-mode-selector-has-stable-width" aria-label="Permalink to &quot;US-UI-73 Composer mode selector has stable width&quot;">​</a></h3><ul><li>Open the empty home and a docked thread in both English and zh-CN.</li><li>Switch the Composer mode chip through Agent/Plan/Goal several times.</li><li>Expect the mode chip to keep one fixed width sized for the longest built-in label (English &quot;Agent&quot; / zh-CN &quot;智能体&quot;); the adjacent Thinking and permission controls, send button, and composer shell do not move or resize.</li><li>In Goal, expect the permission chip to remain visible with the same geometry, show Full auto / 全自动, and stay disabled without opening a permission menu. The Plan/Goal approval card remains the separate execution-policy control.</li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code> §11.3</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Partially automated (renderer style/source contract)</li></ul><h3 id="us-ui-74-macos-native-sidebar-vibrancy" tabindex="-1">US-UI-74 macOS native sidebar vibrancy <a class="header-anchor" href="#us-ui-74-macos-native-sidebar-vibrancy" aria-label="Permalink to &quot;US-UI-74 macOS native sidebar vibrancy&quot;">​</a></h3><ul><li>Open the desktop app on macOS in both light and dark appearances with the sidebar expanded, then exercise the existing collapse/expand path.</li><li>Expect the main window to use native <code>sidebar</code> vibrancy with a thin theme tint behind <code>.sidebar</code> and any rendered <code>.sidebar-rail</code>: the material follows the app theme (<code>nativeTheme.themeSource</code>), so a dark shell stays on a dark plate and a light shell stays on a light plate. Desktop content stays perceptible through the material and the surface carries a top-to-bottom sheen rather than a flat fill. The dock carries no seam or hairline — the glass meets the opaque main pane flush, so no hard divider separates the two panes. Switching language or other non-theme settings does not rebuild the glass. Disabling or uninstalling a selected plugin theme returns native chrome to <code>system</code>.</li><li>Expect <code>.main-pane</code>, <code>.main-titlebar</code>, and <code>.conversation-topbar</code> to remain solid theme surfaces without whole-window transparency or a strong artificial blur/card treatment. Sidebar collapse/expand, resize, traffic-light placement, and drag/no-drag hit regions remain unchanged.</li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code> §1.7, §3.4; decisions-log D304 / D348</li><li><strong>Milestone</strong>: M6</li><li><strong>Status</strong>: Partially automated (<code>macos-sidebar-vibrancy.test.mjs</code> source contract); native visual verification Draft</li></ul><h3 id="us-ui-75-light-message-edit-plate" tabindex="-1">US-UI-75 Light message-edit plate <a class="header-anchor" href="#us-ui-75-light-message-edit-plate" aria-label="Permalink to &quot;US-UI-75 Light message-edit plate&quot;">​</a></h3><ul><li>Open a populated transcript in light and dark themes and choose Edit on a user prompt.</li><li>Expect the inline editor to be a <code>--ds-tile-deep</code> plate at <code>--ds-composer-radius</code> with no outer shadow and no hairline stroke. On light theme the 8% ink wash must remain distinct from the <code>#ffffff</code> chat surface; the composer glow must not appear or get clipped at the plate edges. Retry and Cancel stay legible in the footer.</li><li>Focusing the textarea paints an inset 2px accent ring inside the plate; Escape or Cancel restores the bubble.</li></ul><h4 id="e2e-123-asktool-collects-multiple-answers-and-returns-skipped-placeholders" tabindex="-1">E2E-123: asktool collects multiple answers and returns skipped placeholders <a class="header-anchor" href="#e2e-123-asktool-collects-multiple-answers-and-returns-skipped-placeholders" aria-label="Permalink to &quot;E2E-123: asktool collects multiple answers and returns skipped placeholders&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Agent, Plan, or Goal mode; a configured provider; a session with an active transcript.</li><li><strong>Steps</strong>: 1) Ask the agent to call <code>asktool</code> with a single-select question, a multi-select question, and an option list for each. 2) Confirm each card shows the fixed custom-input choice. 3) Answer the first question, click Next, and select two answers on the multi-select question. 4) Skip the final question without entering text. 5) Inspect the completed tool row and the next model response.</li><li><strong>Expected</strong>: One question is visible at a time; the small indicators show answered, current, and skipped states in the composer approval area, at the same dock position used by Plan and Goal approval. The request has no countdown. Question text and options render at the compact card body size (<code>--text-md</code>, one step below the surrounding chat body), matching the permission card scale in the same dock area. The card shell uses the slim rail (2 px accent, 14 px × 16 px padding). Option rows and action buttons use the app&#39;s compact control density (30 px rows, 15 px marks, 8 px row gaps, compact buttons), and the card keeps a relaxed internal rhythm (12/14 px indicator margins, <code>--leading-normal</code> question line-height). The question&#39;s option list is bounded to the available viewport height; when a fixture contains enough options to overflow, only that list scrolls and the question header, custom-answer input, and Skip / Next / Submit actions remain reachable. Scrolling the list does not move the conversation page or hide the action row. The tool output is ordered as <code>question：answer</code>, uses <code>、</code> between multiple answers and <code>\\n---\\n</code> between questions, and keeps <code>question：</code> for the skipped question. Decline all produces empty placeholders for every question and still completes the tool call.</li><li><strong>Specs linked</strong>: <code>03-runtime/17-asktool-questions.md</code>, <code>04-ux/11-asktool-question-card.md</code>, ADR 0077</li><li><strong>Acceptance</strong>: E (interactive tool output), C (inline card)</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Draft (unit coverage active; desktop journey pending)</li></ul><h4 id="e2e-124-window-controls-minimize-to-the-taskbar-and-close-to-the-chosen-surface" tabindex="-1">E2E-124: Window controls minimize to the taskbar and close to the chosen surface <a class="header-anchor" href="#e2e-124-window-controls-minimize-to-the-taskbar-and-close-to-the-chosen-surface" aria-label="Permalink to &quot;E2E-124: Window controls minimize to the taskbar and close to the chosen surface&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Built desktop app on macOS, Windows, and Linux; English and zh-CN locales are available; a normal main window is open.</li><li><strong>Steps</strong>: 1) On Windows, leave the focused main window visible and click its taskbar button; confirm it minimizes while the PI-Desktop taskbar entry remains. Click the same taskbar button again and confirm the window restores and focuses. Cover the window with another app, click the PI-Desktop taskbar entry, and confirm it comes to the front without entering the tray. 2) On</li><li>macOS, click the traffic-light minimize control and confirm it hides the window to the tray. On Windows/Linux, use the renderer minimize control and confirm the native taskbar entry remains available; click it again and confirm the same window restores and focuses. 3) Select Close to tray and close the Windows/Linux window; confirm it leaves the taskbar and is restored by the tray icon. Select Quit and repeat; confirm the process exits.</li></ul><ol start="4"><li>Open the tray menu and choose Show, then repeat with Quit. 5) Repeat in zh-CN and invoke macOS app activation while the window is tray-hidden.</li></ol><ul><li><strong>Expected</strong>: Windows native taskbar toggling and the Windows/Linux renderer minimize controls keep the taskbar entry and round-trip through native minimize/restore; a taskbar click on a covered window brings it to the front. Close to tray is the only Windows/Linux close path that hides the window, while Quit exits. On macOS the menu bar icon is a readable transparent monochrome PI mark without the rounded application tile, and native minimize remains tray-resident. Show/click/double-click/app activation restores the existing window; the localized menu contains Show PI-Desktop and Quit PI-Desktop. Quit runs the normal shutdown sequence and leaves no orphan host, sidecar, or tray process.</li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code>, <code>03-runtime/07-process-model.md</code>, <code>04-ux/08-component-spec.md</code>, <code>04-ux/09-interaction-patterns.md</code>, <code>08-meta/decisions-log.md</code> (D216, D230, D252, D256), ADR 0078, ADR 0090, ADR 0117, ADR 0123</li><li><strong>Acceptance</strong>: A (app lifecycle), Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Unit/source-contract covered; native cross-platform tray journey Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-150-a-second-launch-surfaces-the-running-app-instead-of-starting-one" tabindex="-1">E2E-150: A second launch surfaces the running app instead of starting one <a class="header-anchor" href="#e2e-150-a-second-launch-surfaces-the-running-app-instead-of-starting-one" aria-label="Permalink to &quot;E2E-150: A second launch surfaces the running app instead of starting one&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Built desktop app on macOS, Windows, and Linux, installed with the default data directory; one instance is running with at least one session in its sidebar.</li><li><strong>Steps</strong>: 1) Launch the app again from the platform&#39;s normal entry point (Start menu/desktop shortcut, <code>.AppImage</code>, <code>open -n</code> on macOS) and observe both the window and the process list. 2) Minimize the window into the tray, then launch again. 3) On Windows/Linux with close behavior <code>tray</code>, close the window, then launch again. 4) While the app is running, launch a build with <code>PI_DESKTOP_DATA_DIR</code> set to an empty directory. 5) Still while the packaged app is running, start a development build (the <code>dev</code> script, with no <code>PI_DESKTOP_DATA_DIR</code>). 6) Quit the app, confirm no process remains, and launch once more.</li><li><strong>Expected</strong>: Steps 1–3 never create a second window, tray icon, host-core, agent sidecar, or log file: the existing window is restored and focused, the duplicate process exits, and the running instance&#39;s session list, in-flight turn, and <code>pi.sqlite</code> are untouched. Step 4 starts normally as an independent instance against its own data directory. Step 5 also starts normally: the development build takes its own <code>userData</code> and <code>~/.pi-desktop-dev</code>, so it neither waits for nor disturbs the running packaged app. Step 6 starts a clean single instance, proving the lock is released on exit and never leaves a stale block.</li><li><strong>Specs linked</strong>: <code>03-runtime/07-process-model.md</code>, <code>08-meta/decisions-log.md</code> (D236, D599, D002), ADR 0094</li><li><strong>Acceptance</strong>: A (app lifecycle), Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Unit/source-contract covered; native cross-platform relaunch journey Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-125-complete-bilingual-vitepress-documentation-remains-usable" tabindex="-1">E2E-125: Complete bilingual VitePress documentation remains usable <a class="header-anchor" href="#e2e-125-complete-bilingual-vitepress-documentation-remains-usable" aria-label="Permalink to &quot;E2E-125: Complete bilingual VitePress documentation remains usable&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Docs dependencies are installed and the VitePress preview server is running from the repository.</li><li><strong>Steps</strong>: 1) Open <code>/</code> at 1440×900 and verify the English landing page, system map, read-by-intent journeys, reference shelf, global search, Guide/Specs/ADRs navigation, and language selector. 2) Switch to <code>/zh-CN/</code> and verify the translated hero, mirrored topic map, and Chinese specification links. 3) Open <code>/spec/03-runtime/01-ipc-protocol</code>, switch languages, and verify that <code>/zh-CN/spec/03-runtime/01-ipc-protocol</code> contains translated prose, preserved code identifiers, and a link back to the English source. 4) Search in each locale and open a matching result. 5) Repeat the homepage and a long table-heavy specification at 390×844 in light and dark mode.</li><li><strong>Expected</strong>: Both locale entry points and every English/Chinese specification pair render without broken links or page-level horizontal overflow. Landing and reading columns are visually centered within their available layout; the mobile hero presents text before the system visual. Search returns local results. The mobile navigation opens and closes without shifting or obscuring the page. Code blocks and tables remain readable through contained scrolling, theme contrast stays clear, and every Chinese spec identifies the English page as its canonical source. Directly refresh <code>/zh-CN/spec/README</code>, <code>/zh-CN/spec/03-runtime/01-ipc-protocol</code>, <code>/zh-CN/adr/</code>, <code>/spec/README</code>, and <code>/adr/README</code> on Vercel; each route resolves through the documented <code>cleanUrls</code> configuration instead of returning 404.</li><li><strong>Specs linked</strong>: <code>02-architecture/04-documentation-site.md</code>, ADR 0079</li><li><strong>Acceptance</strong>: Quality, documentation discoverability, responsive layout</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Browser-rendered desktop/mobile verification is authorized for this documentation redesign; remote deployment refresh checks remain Draft.</li></ul><h4 id="e2e-126-appearance-card-selects-a-global-ui-font" tabindex="-1">E2E-126: Appearance card selects a global UI font <a class="header-anchor" href="#e2e-126-appearance-card-selects-a-global-ui-font" aria-label="Permalink to &quot;E2E-126: Appearance card selects a global UI font&quot;">​</a></h4><ul><li><p><strong>Preconditions</strong>: App running on macOS with an installed system font distinct from the built-in token stack (for example PingFang SC); a clean <code>~/.pi-desktop</code> profile.</p></li><li><p><strong>Steps</strong>:</p><ol><li>Open Settings → Basics and confirm the Appearance card shows a Font row below Theme and Language with a trigger labeled &quot;System default&quot;.</li><li>Open the Font picker and confirm it lists System default followed by installed system families only — no bundled open-licensed families and no license badge on any row; confirm the search input filters families and the current selection shows a check badge; confirm the menu opens as a floating layer above the card (not clipped or squeezed inside it) and stays readable when the card is near the bottom edge of the window; with many installed families, confirm the list opens without an input stall and scrolls immediately (only the visible rows are rendered, with an overscan buffer).</li><li>Select an installed system family (for example PingFang SC) and confirm the trigger label and the whole UI re-render in that family without a reload, including CJK fallback rendering for Chinese text.</li><li>Reopen the picker and confirm the family is still selected, then select System default and confirm the UI returns to the built-in token stack immediately.</li><li>Restart the app and reopen Settings and confirm the default is still applied (the override is cleared, persisting an empty <code>AppSettings.fontFamily</code>).</li><li>With a profile whose stored stack names a formerly bundled family (for example <code>&quot;Geist&quot;, &quot;PingFang SC&quot;, &quot;Hiragino Sans GB&quot;, &quot;Microsoft YaHei&quot;, sans-serif</code>), confirm the Font row shows that family under the Saved group, that no font file is loaded from the app, and that the UI renders through the system CJK fallback tier.</li></ol></li><li><p><strong>Expected</strong>: The Font row is a searchable picker whose trigger previews the current family in that face; options are System default and the installed system families enumerated by Electron main via <code>pi-desktop/app/systemFonts</code> (cached 60 s, hidden <code>.</code>-prefixed families excluded); the app ships no font files, so no bundled group and no license badge appear, and a stored stack that matches no option stays listed first under Saved; selection persists as a CSS stack in <code>AppSettings.fontFamily</code> and overrides <code>--font-sans</code> live; Chinese text stays readable through the system-only CJK fallback tier; the menu is a body-level floating layer that is never clipped by the settings card; the option list is windowed with fixed row heights and an overscan buffer so only the visible slice is in the DOM, keeping opening, scrolling, and typing responsive regardless of how many families are installed; System default clears the override by persisting an empty stack.</p></li><li><p><strong>Specs linked</strong>: <code>04-ux/06-settings-ia.md</code>, <code>04-ux/07-ui-design-system.md</code>, <code>03-runtime/01-ipc-protocol.md</code>, ADR 0083, ADR 0298</p></li><li><p><strong>Acceptance</strong>: A (core shell), H (localization)</p></li><li><p><strong>Milestone</strong>: M5+</p></li><li><p><strong>Status</strong>: Documented</p></li></ul><h4 id="e2e-193-appearance-card-sets-a-global-type-scale" tabindex="-1">E2E-193: Appearance card sets a global type scale <a class="header-anchor" href="#e2e-193-appearance-card-sets-a-global-type-scale" aria-label="Permalink to &quot;E2E-193: Appearance card sets a global type scale&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: App running with a clean <code>~/.pi-desktop</code> profile and an open conversation that shows transcript text, the composer, and the sidebar.</li><li><strong>Steps</strong>: <ol><li>Open Settings → General and confirm the Appearance card shows a Font size row below Font, with Grande selected and the slider at 100%.</li><li>Choose Venti. Confirm chat transcript, composer, settings labels, sidebar session titles, and Lucide chrome icons all enlarge without a reload, keeping their relative steps, and that the control shows 115%.</li><li>Drag the slider to 125%. Confirm Trenta becomes selected and every <code>--text-*</code> surface and icon grows further. Confirm the UI never shows a px field.</li><li>Use Zoom In, then Reset Zoom. Confirm window zoom still scales chrome and that the type scale remains 125% after reset.</li><li>Restart the app and confirm the 125% scale is still applied (<code>AppSettings.fontScale</code> = 1.25).</li><li>Choose Grande. Confirm the whole UI returns to 100% immediately; restart and confirm the default remains.</li></ol></li><li><strong>Expected</strong>: Font size is Starbucks-style cup presets Tall / Grande / Venti / Trenta plus a percentage slider 80%–150% in 2.5% steps. Selection persists as <code>AppSettings.fontScale</code> (absent means 1) and sets <code>--font-scale</code>, multiplying every <code>--text-*</code> token and shared Lucide icon. Window Zoom In/Out/Reset remains independent. Invalid values are rejected or clamped. No protocol or schema version bump.</li><li><strong>Specs linked</strong>: <code>04-ux/06-settings-ia.md</code>, <code>04-ux/07-ui-design-system.md</code>, ADR 0180, D343</li><li><strong>Acceptance</strong>: A (core shell), B (settings), H (localization)</li><li><strong>Milestone</strong>: M5+</li><li><strong>Status</strong>: Unit-covered (<code>packages/shared/src/font-size.test.ts</code>, <code>apps/desktop/test/settings-font-size.test.mjs</code>); full UI journey Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-127-macos-keeps-the-app-in-the-dock-and-cmd-tab" tabindex="-1">E2E-127: macOS keeps the app in the Dock and Cmd+Tab <a class="header-anchor" href="#e2e-127-macos-keeps-the-app-in-the-dock-and-cmd-tab" aria-label="Permalink to &quot;E2E-127: macOS keeps the app in the Dock and Cmd+Tab&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Built desktop app on macOS; the plugin launcher shortcut (Option+Space) is registered; at least two Spaces and one other application running fullscreen.</li><li><strong>Steps</strong>: <ol><li>Launch the app without opening the launcher and confirm it appears in the Dock and in the Cmd+Tab switcher (<code>lsappinfo list</code> reports <code>type=&quot;Foreground&quot;</code>, not <code>type=&quot;UIElement&quot;</code>).</li><li>Press Option+Space, confirm the launcher panel appears focused with a typable input, dismiss it, and confirm the app is still in Cmd+Tab.</li><li>Put the main window in fullscreen, press Option+Space, and confirm the panel floats above it.</li><li>Switch to a second regular Space and confirm Option+Space shows the panel there.</li><li>Minimize the main window into the tray, switch to another app, then Cmd+Tab back to PI-Desktop and confirm the window returns focused; repeat with a Dock click and with the tray Show item.</li><li>With the main window hidden, press Option+Space and confirm only the launcher appears — the main window stays hidden until it is restored.</li></ol></li><li><strong>Expected</strong>: The process never adopts the accessory activation policy, so Dock and Cmd+Tab presence survives launcher warm-up and every launcher invocation; the launcher stays focusable, covers all regular Spaces and the app&#39;s own fullscreen window (overlaying another app&#39;s fullscreen Space is out of scope and activates PI-Desktop instead); activation from Cmd+Tab, App Exposé, the Dock, or the tray restores a tray-hidden window, while launcher and plugin-panel activation leaves it hidden.</li><li><strong>Specs linked</strong>: <code>03-runtime/07-process-model.md</code>, ADR 0086, ADR 0078, ADR 0080</li><li><strong>Acceptance</strong>: A (core shell)</li><li><strong>Milestone</strong>: M5+</li><li><strong>Status</strong>: Documented</li></ul><h4 id="e2e-128-revealed-work-panel-with-no-resource-offers-available-views" tabindex="-1">E2E-128: Revealed work panel with no resource offers available views <a class="header-anchor" href="#e2e-128-revealed-work-panel-with-no-resource-offers-available-views" aria-label="Permalink to &quot;E2E-128: Revealed work panel with no resource offers available views&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: App running with a project open and an active conversation that has produced no file, URL, or review artifact, so the session&#39;s work panel context holds no tabs.</li><li><strong>Steps</strong>: <ol><li>Press <code>Cmd/Ctrl + J</code> and confirm the panel appears with an empty body that shows the title &quot;New&quot; and Review plus Browser/in-scope plugin-view rows — not a blank area below the title bar.</li><li>Tab into the available rows and confirm each takes a visible focus ring and that hovering a row shows only a background fill.</li><li>Activate Browser or a plugin view and confirm its singleton tab is created and selected; the empty body and its view list disappear.</li><li>Click <code>+</code> to create a New launcher tab, activate the same view from its body, and confirm it selects the existing tab rather than creating a second one.</li><li>Close the view tab and confirm the panel remains open on the New launcher when it was the last tab; press <code>Cmd/Ctrl + J</code> again and confirm it hides.</li><li>Repeat step 1 in Chinese and in both light and dark themes, and at the 244px panel minimum, confirming the copy wraps rather than clipping.</li></ol></li><li><strong>Expected</strong>: <code>Cmd/Ctrl + J</code> reveals the panel without creating a tab, and the New launcher lists the same Review/plugin-view entries as its <code>+</code>-created page; a row creates or selects that singleton view. Each <code>+</code> click creates a separate closable New tab. Closing the final tab leaves the panel open on New. The no-tab empty body is not exposed as a <code>tabpanel</code>; explicit New tabs are labelled tabpanels. Their rows are buttons in a <code>role=&quot;group&quot;</code> labelled Tools. Panel empty states share the app&#39;s empty-state proportions with no action button in the &quot;open a project&quot; states.</li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code> §5.2, §5.2.1, §5.3, §5.4, §5.5, <code>04-ux/07-ui-design-system.md</code>, ADR 0108</li><li><strong>Acceptance</strong>: A (core shell), H (localization)</li><li><strong>Milestone</strong>: M5+</li><li><strong>Status</strong>: Documented</li></ul><h4 id="e2e-129-a-run-row-shows-its-command-once-and-copies-it-from-the-head" tabindex="-1">E2E-129: A run row shows its command once and copies it from the head <a class="header-anchor" href="#e2e-129-a-run-row-shows-its-command-once-and-copies-it-from-the-head" aria-label="Permalink to &quot;E2E-129: A run row shows its command once and copies it from the head&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: App running with a project open and a conversation that has produced at least three command rows: one that succeeded with output, one that failed with both output and errors, and one still running.</li><li><strong>Steps</strong>: <ol><li>Leave the running row collapsed while it produces output and confirm the row remains a compact one-line status; then click its disclosure while the command is still running and confirm the current output appears immediately. 1a) Keep the command running through enough output to exceed the output area&#39;s visible height and confirm the output stays inside its capped scroll region, rather than pushing the transcript out of view. Confirm later output replaces the same body in place while the row remains open. 1b) Expand the successful row and confirm the body shows the command&#39;s output as plain text — no <code>Output</code> heading, no bordered card, no per-block copy button — and that the command is not repeated inside the body, with no argument list in its place.</li><li>Confirm each row&#39;s head states its outcome to the right of the summary: <code>Done</code>, <code>Failed</code>, <code>Denied</code>, or <code>Working…</code>, each with a dot in the matching tint, and that the running row shows the pulsing dot instead of the row spinner. 2a) Run a command that exits non-zero (<code>pnpm test</code> on a failing suite, or <code>false</code>) and confirm the row reads <code>Failed</code> with the error dot and opens itself, even though the tool call completed. Confirm the exit-code chip and the worded outcome agree. 2b) Interrupt a long command so the shell is killed with no exit code, and confirm the row reads <code>Failed</code> rather than <code>Done</code>.</li><li>Hover the successful row and confirm a copy button and the chevron appear between the summary and the row&#39;s right edge; move the pointer away and confirm both fade while the status label stays visible.</li><li>Activate the copy button and confirm the clipboard holds the command as it was issued — a multi-line command keeps its line breaks, unlike the single-line summary in the head — and that the button acknowledges the copy before returning to its idle icon.</li><li>Confirm the hover fill covers the whole head, including the copy button and chevron, and that a row with nothing to expand takes no fill at all.</li><li>Tab through the row and confirm the header is the only stop that toggles the body, the copy button is reachable and takes a visible focus ring (revealing itself on focus), and the chevron is never a tab stop.</li><li>With a screen reader, expand and collapse the row and confirm the outcome is announced once, not twice.</li><li>Ask the agent to run a command that requires approval and confirm the permission card still shows the command it is asking about.</li><li>Enable &quot;reduce motion&quot; and confirm the running dot holds still and the copy button appears without a fade.</li><li>Repeat steps 1–3 in Chinese and in both light and dark themes.</li></ol></li><li><strong>Expected</strong>: A command appears exactly once per row, in the head, alongside a copy control that yields it verbatim and a worded outcome that does not rely on the dot&#39;s color. The outcome reports what the command did — a non-zero or missing exit code reads <code>Failed</code> regardless of the tool call&#39;s own status — and a row with nothing to report states nothing instead of claiming <code>Done</code>. The running row is collapsed by default, and expanding it during execution shows the cumulative <code>details.output</code> stream in the stdout channel. The body keeps the live output in a capped internal scroll region and updates in place without rerendering unrelated rows; the completed <code>details.stdout</code> value wins over any older partial snapshot. The expanded body holds only what the command printed, as bare text, and a command that printed nothing opens empty rather than falling back to its arguments. Approval cards are unaffected, since they have no head of their own.</li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code> §9.2, §9.3, §9.5, §9.10</li><li><strong>Acceptance</strong>: E (tools &amp; permissions), H (localization)</li><li><strong>Milestone</strong>: M5+</li><li><strong>Status</strong>: Documented</li></ul><h4 id="e2e-130-read-mints-a-tag-that-an-edit-consumes-without-re-reading" tabindex="-1">E2E-130: Read mints a tag that an Edit consumes without re-reading <a class="header-anchor" href="#e2e-130-read-mints-a-tag-that-an-edit-consumes-without-re-reading" aria-label="Permalink to &quot;E2E-130: Read mints a tag that an Edit consumes without re-reading&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A project-bound Agent session with a writable workspace and a source file of at least 300 lines. The provider fixture can emit an exact <code>Edit</code> payload.</li><li><strong>Steps</strong>: <ol><li><code>Read</code> the file with no <code>offset</code> and record the <code>[path#TAG]</code> header, the <code>tag</code> field, and the <code>N:</code> prefixes on the returned lines.</li><li>Emit <code>Edit</code> with that <code>tag</code> and a single <code>PUT N.=M:</code> whose body replaces two lines inside the read window.</li><li>Confirm the successful result reports a new <code>tag</code>, then emit a second <code>Edit</code> with the returned tag and a <code>PUT &gt;$:</code> append, with no intervening <code>Read</code>.</li><li><code>Read</code> a 200-line window at an <code>offset</code>, then <code>Edit</code> a line inside that window using the tag from the windowed read.</li><li>Reopen the file on disk and compare it to the intended content byte for byte.</li></ol></li><li><strong>Expected</strong>: The header tag is the whole-file tag, not the window&#39;s, so a windowed read anchors correctly; line numbers are absolute and unaffected by <code>offset</code>. Both edits apply, the second without any re-read, and each success returns the post-write tag. The file on disk matches the intended content exactly, with its original line endings and BOM state preserved.</li><li><strong>Specs linked</strong>: <code>03-runtime/18-line-anchored-edit-contract.md</code> §3, §4.2, §5, §6, §9, <code>03-runtime/16-tool-result-limits.md</code> §5, ADR 0087</li><li><strong>Acceptance</strong>: E (tools &amp; permissions)</li><li><strong>Milestone</strong>: M5+</li><li><strong>Status</strong>: Documented</li></ul><h4 id="e2e-131-an-edit-on-never-displayed-lines-is-rejected-and-the-retry-succeeds" tabindex="-1">E2E-131: An edit on never-displayed lines is rejected and the retry succeeds <a class="header-anchor" href="#e2e-131-an-edit-on-never-displayed-lines-is-rejected-and-the-retry-succeeds" aria-label="Permalink to &quot;E2E-131: An edit on never-displayed lines is rejected and the retry succeeds&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A session that has read only lines 1–50 of a 400-line file. A second fixture file has one line longer than 16,384 characters.</li><li><strong>Steps</strong>: <ol><li>Emit <code>Edit</code> with the correct <code>tag</code> and a <code>PUT 300.=301:</code> op.</li><li>Inspect the error code and confirm the message inlines the current content of lines 300 and 301.</li><li>Retry the identical <code>Edit</code> payload, unchanged, including the same <code>tag</code>.</li><li>Emit <code>Edit</code> with the correct tag and a <code>PUT 5.=60:</code> op spanning 56 unseen lines, and inspect the reveal.</li><li>Retry that identical payload unchanged.</li><li><code>Read</code> the second fixture, confirm the long line is clipped and counted in <code>notice</code>, then <code>Edit</code> that clipped line.</li></ol></li><li><strong>Expected</strong>: Step 1 fails with <code>EDIT_LINES_UNSEEN</code> and the file is unchanged. Step 3 applies, because the complete reveal merged those lines into the session&#39;s provenance. Step 4 fails with a reveal truncated at 40 lines that says to re-read the range, and step 5 fails again — a truncated reveal merges nothing, so the guard cannot be walked past in under-cap slices. Step 6 fails with <code>EDIT_LINES_UNSEEN</code>: a clipped line was never displayed.</li><li><strong>Specs linked</strong>: <code>03-runtime/18-line-anchored-edit-contract.md</code> §4.3, §9.1, §11, §12, <code>03-runtime/16-tool-result-limits.md</code> §2, ADR 0087</li><li><strong>Acceptance</strong>: E (tools &amp; permissions), Quality</li><li><strong>Milestone</strong>: M5+</li><li><strong>Status</strong>: Documented</li></ul><h4 id="e2e-132-gap-inserts-deletes-and-multi-op-payloads-apply-against-one-snapshot" tabindex="-1">E2E-132: Gap inserts, deletes, and multi-op payloads apply against one snapshot <a class="header-anchor" href="#e2e-132-gap-inserts-deletes-and-multi-op-payloads-apply-against-one-snapshot" aria-label="Permalink to &quot;E2E-132: Gap inserts, deletes, and multi-op payloads apply against one snapshot&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A read file whose content is known line by line.</li><li><strong>Steps</strong>: <ol><li>Emit one <code>Edit</code> combining <code>PUT &lt;1:</code>, <code>PUT &gt;40:</code>, <code>CUT 12.=14</code>, and <code>PUT 80.=80:</code> in a single <code>ops</code> payload.</li><li>Compare the result against the same four changes computed against the original line numbering.</li><li>Emit <code>PUT &gt;$:</code> on the same file and confirm the append lands after the final line with exactly one terminating newline.</li><li>Emit an <code>ops</code> payload whose body row starts with a literal <code>-</code> written as <code>+- item</code>, and one with a literal <code>+</code> written as <code>++ item</code>.</li><li>Emit an <code>Edit</code> whose <code>PUT</code> body exactly reproduces the range&#39;s current content.</li></ol></li><li><strong>Expected</strong>: Every anchor indexes the tagged snapshot, so no op shifts another and the combined result equals the four independent changes. <code>+-</code> and <code>++</code> write a single leading <code>-</code> and <code>+</code>. Step 5 returns <code>EDIT_NO_CHANGE</code> rather than reporting a successful write of nothing, and leaves no review record.</li><li><strong>Specs linked</strong>: <code>03-runtime/18-line-anchored-edit-contract.md</code> §7.2, §7.3, §7.4, §8.1, §9.3, ADR 0087</li><li><strong>Acceptance</strong>: E (tools &amp; permissions)</li><li><strong>Milestone</strong>: M5+</li><li><strong>Status</strong>: Documented</li></ul><h4 id="e2e-133-block-ops-resolve-echo-their-span-and-decline-instead-of-guessing" tabindex="-1">E2E-133: Block ops resolve, echo their span, and decline instead of guessing <a class="header-anchor" href="#e2e-133-block-ops-resolve-echo-their-span-and-decline-instead-of-guessing" aria-label="Permalink to &quot;E2E-133: Block ops resolve, echo their span, and decline instead of guessing&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Read fixtures in a supported grammar (a Rust file with a decorated/attributed function), a Markdown file with nested headings, and a file in a language outside the supported grammar list.</li><li><strong>Steps</strong>: <ol><li>Emit <code>PUT N*:</code> anchored on the <code>fn</code> line of a function whose declaration carries an <code>#[attribute]</code> line above it, and inspect the echoed <code>{anchorLine, start, end, op}</code>.</li><li>Repeat anchored on the attribute line and compare the echoed span.</li><li>Emit <code>PUT &gt;N*:</code> on the same opener and confirm the insertion lands after the block&#39;s last line at sibling indentation.</li><li>Emit <code>CUT N*</code> anchored on a lone <code>}</code> closer.</li><li>Emit <code>PUT N*:</code> on a <code>##</code> heading in the Markdown fixture and confirm the span reaches the next same-or-higher heading, not the next deeper one.</li><li>Emit <code>PUT N*:</code> in the unsupported-language file.</li><li>Introduce a syntax error into the Rust fixture, re-read, and emit a block op.</li></ol></li><li><strong>Expected</strong>: Step 1&#39;s span starts at the <code>fn</code> line and excludes the attribute; step 2&#39;s includes both — the difference is visible in the echo before the model has to infer it. Steps 4, 6, and 7 fail with <code>EDIT_BLOCK_UNRESOLVED</code> and a message naming the plain-range alternative; neither approximates a span. Range and gap ops still work in the unsupported language.</li><li><strong>Specs linked</strong>: <code>03-runtime/18-line-anchored-edit-contract.md</code> §8.2, §11, §12, ADR 0087 §4</li><li><strong>Acceptance</strong>: E (tools &amp; permissions), Quality</li><li><strong>Milestone</strong>: M5+</li><li><strong>Status</strong>: Documented</li></ul><h4 id="e2e-134-registers-move-code-within-a-call-and-across-calls" tabindex="-1">E2E-134: Registers move code within a call and across calls <a class="header-anchor" href="#e2e-134-registers-move-code-within-a-call-and-across-calls" aria-label="Permalink to &quot;E2E-134: Registers move code within a call and across calls&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Two read files in the same session.</li><li><strong>Steps</strong>: <ol><li>In one <code>Edit</code>, emit <code>CUT 20.=30</code> followed by <code>PUT &lt;5 </code> with no register label, and confirm the lines moved within the file.</li><li>In one <code>Edit</code>, emit two <code>CUT</code> ops with no labels followed by one unlabeled paste.</li><li>Emit <code>CUT 40* @fn</code> on the first file, then in a separate <code>Edit</code> call emit <code>PUT &lt;10 @fn</code> on the second file.</li><li>Emit <code>PUT &lt;10 @missing</code> for a register that was never set.</li><li>Emit <code>PUT 10.=12 @fn</code> with a body row attached.</li><li>Emit <code>PUT &lt;1 </code> with no label in a fresh <code>Edit</code> call that performed no capture.</li><li>Delete the source file, then paste <code>@fn</code> again in a later call.</li></ol></li><li><strong>Expected</strong>: Step 1 applies as one move with no duplicated or orphaned lines. Step 2 fails with <code>EDIT_REGISTER_AMBIGUOUS</code> instead of using the most recent capture. Step 3 completes the cross-file move across two calls, each with its own permission gate, review record, and artifacts row. Steps 4 and 6 fail with <code>EDIT_REGISTER_EMPTY</code> — the anonymous register did not survive the earlier call. Step 5 fails with <code>EDIT_PARSE_FAILED</code>. Step 7 still pastes: a register holds captured content, not a live reference.</li><li><strong>Specs linked</strong>: <code>03-runtime/18-line-anchored-edit-contract.md</code> §7.5, §8.3, §11, §13.2, ADR 0087 §5</li><li><strong>Acceptance</strong>: E (tools &amp; permissions)</li><li><strong>Milestone</strong>: M5+</li><li><strong>Status</strong>: Documented</li></ul><h4 id="e2e-135-a-drifted-file-recovers-when-remapping-is-provable-and-fails-when-it-is-not" tabindex="-1">E2E-135: A drifted file recovers when remapping is provable and fails when it is not <a class="header-anchor" href="#e2e-135-a-drifted-file-recovers-when-remapping-is-provable-and-fails-when-it-is-not" aria-label="Permalink to &quot;E2E-135: A drifted file recovers when remapping is provable and fails when it is not&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A read file whose tag is recorded. An external process can modify the file between the read and the edit.</li><li><strong>Steps</strong>: <ol><li>Insert 10 unrelated lines above the edit target from outside the session, then emit the original <code>Edit</code> with the stale <code>tag</code>.</li><li>Inspect the warning on the successful result and confirm the change landed at the shifted location, not at the original line numbers.</li><li>Repeat with a change that modifies one of the anchor lines themselves.</li><li>Repeat with a change that inserts lines <em>between</em> two anchors of a multi-op payload, so the anchors would move by different offsets.</li><li>Repeat with a <code>CUT</code> whose captured interior lines were externally edited.</li><li>Repeat after the session itself has written the file twice, using the tag from the first write.</li><li>Repeat with a duplicated anchor line whose one neighboring context line matches but whose other does not.</li></ol></li><li><strong>Expected</strong>: Step 1 applies with a line-remap plus external-change warning. Steps 3, 4, 5, and 7 fail closed with <code>EDIT_TAG_MISMATCH</code> and current content; none of them writes. Step 6 applies with a session-chain warning instead of an external-change warning, because the corrective advice differs. No recovery path ever writes the tagged snapshot&#39;s content over the live file.</li><li><strong>Specs linked</strong>: <code>03-runtime/18-line-anchored-edit-contract.md</code> §9, §10, §11, ADR 0087 §6</li><li><strong>Acceptance</strong>: E (tools &amp; permissions), Quality</li><li><strong>Milestone</strong>: M5+</li><li><strong>Status</strong>: Documented</li></ul><h4 id="e2e-136-a-stale-tag-still-applies-for-head-and-tail-inserts" tabindex="-1">E2E-136: A stale tag still applies for head and tail inserts <a class="header-anchor" href="#e2e-136-a-stale-tag-still-applies-for-head-and-tail-inserts" aria-label="Permalink to &quot;E2E-136: A stale tag still applies for head and tail inserts&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A read file whose tag is recorded, plus an external writer.</li><li><strong>Steps</strong>: <ol><li>Modify the middle of the file externally, then emit <code>PUT &gt;$:</code> with the stale tag.</li><li>Repeat with <code>PUT &lt;1:</code> and the same stale tag.</li><li>Repeat with a payload that mixes <code>PUT &gt;$:</code> and an anchored <code>PUT 50.=50:</code>.</li><li>Emit an <code>Edit</code> whose <code>tag</code> is well formed but was never recorded for that path in this session.</li><li>Emit an <code>Edit</code> with a <code>tag</code> that is not four hex digits.</li></ol></li><li><strong>Expected</strong>: Steps 1 and 2 apply with a drift warning, because neither anchor can be moved by content drift. Step 3 does not take the position-stable path: it goes to recovery and, failing that, to <code>EDIT_TAG_MISMATCH</code>. Step 4 returns <code>EDIT_TAG_UNKNOWN</code> and step 5 returns <code>EDIT_TAG_REQUIRED</code>; neither is reported as a generic <code>TOOL_FAILED</code>.</li><li><strong>Specs linked</strong>: <code>03-runtime/18-line-anchored-edit-contract.md</code> §9, §11, <code>03-runtime/08-error-codes.md</code> §3.4</li><li><strong>Acceptance</strong>: E (tools &amp; permissions)</li><li><strong>Milestone</strong>: M5+</li><li><strong>Status</strong>: Documented</li></ul><h4 id="e2e-137-boundary-repair-fixes-an-off-by-one-edge-and-refuses-a-tie" tabindex="-1">E2E-137: Boundary repair fixes an off-by-one edge and refuses a tie <a class="header-anchor" href="#e2e-137-boundary-repair-fixes-an-off-by-one-edge-and-refuses-a-tie" aria-label="Permalink to &quot;E2E-137: Boundary repair fixes an off-by-one edge and refuses a tie&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A read source file with nested closing delimiters.</li><li><strong>Steps</strong>: <ol><li>Emit a <code>PUT N.=M:</code> whose range includes one trailing <code>}</code> that the body does not restate, and inspect the result and its warning.</li><li>Emit a <code>PUT N.=M:</code> whose body restates a line that sits immediately outside the range.</li><li>Emit a payload constructed so that two distinct repaired texts tie at the minimum repair cost.</li><li>Emit a payload against a file that already fails to parse, and confirm the repair does not retain a row on parse-success evidence alone.</li></ol></li><li><strong>Expected</strong>: Steps 1 and 2 apply with a warning naming exactly what was repaired, so a silent structural change is impossible. Step 3 returns <code>EDIT_REPAIR_AMBIGUOUS</code> rather than choosing; step 4 does not invent a retention. In every case the file either contains the repaired result described in the warning or is untouched.</li><li><strong>Specs linked</strong>: <code>03-runtime/18-line-anchored-edit-contract.md</code> §8.4, §11, ADR 0087</li><li><strong>Acceptance</strong>: E (tools &amp; permissions), Quality</li><li><strong>Milestone</strong>: M5+</li><li><strong>Status</strong>: Documented</li></ul><h4 id="e2e-138-move-remove-and-rollback-keep-review-evidence-honest" tabindex="-1">E2E-138: Move, remove, and rollback keep review evidence honest <a class="header-anchor" href="#e2e-138-move-remove-and-rollback-keep-review-evidence-honest" aria-label="Permalink to &quot;E2E-138: Move, remove, and rollback keep review evidence honest&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A project-bound session with Review visible and a read file inside the workspace root.</li><li><strong>Steps</strong>: <ol><li>Emit <code>Edit</code> with a <code>PUT</code> op plus <code>MV DEST</code> in the same <code>ops</code> payload.</li><li>Inspect the review records for the tool call and the Review panel rows.</li><li>Roll the change back and confirm both the source and the destination return to their pre-call state.</li><li>Emit <code>Edit</code> with <code>REM</code>, then roll it back.</li><li>After a rollback, emit an <code>Edit</code> using the tag the session held before the rollback.</li><li>Emit <code>Edit</code> on a path that does not exist but whose basename and tag match exactly one file this session recorded, and inspect the warning.</li><li>Repeat step 6 with two recorded candidates sharing that basename and tag.</li></ol></li><li><strong>Expected</strong>: Step 1 records a source deletion and a destination creation under one tool call; step 3 restores both or neither. Step 4&#39;s rollback restores the captured bytes, hash-guarded on the full digest rather than the 16-bit tag. Step 5 fails rather than editing against content the rollback replaced. Step 6 rebinds to the real file with a warning, and the write-permission gate is evaluated against the rebound path; step 7 declines instead of picking one.</li><li><strong>Specs linked</strong>: <code>03-runtime/18-line-anchored-edit-contract.md</code> §9.2, §13.1, <code>03-runtime/03-tools-and-permissions.md</code> §4c, ADR 0043, ADR 0087</li><li><strong>Acceptance</strong>: E (tools &amp; permissions), Quality</li><li><strong>Milestone</strong>: M5+</li><li><strong>Status</strong>: Documented</li></ul><h4 id="e2e-139-snapshot-provenance-is-per-session-and-bounded" tabindex="-1">E2E-139: Snapshot provenance is per session and bounded <a class="header-anchor" href="#e2e-139-snapshot-provenance-is-per-session-and-bounded" aria-label="Permalink to &quot;E2E-139: Snapshot provenance is per session and bounded&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A subagent-capable session (§5f), a second session on the same workspace, and a fixture with more files than the snapshot store&#39;s path bound.</li><li><strong>Steps</strong>: <ol><li>Read a file in the parent session, then have a delegate <code>Edit</code> that file using the parent&#39;s tag without reading it first.</li><li>Read a file in session A and emit the same <code>Edit</code> payload from session B.</li><li>Read more distinct paths than the store retains, then edit the first-read path with its original tag.</li><li>Read one path five times with changing content between reads, then edit using the tag from the first read.</li><li>Read a file, then read the identical unchanged file twice more at different offsets, and confirm one tag covers all three windows.</li><li>Restart the app, then emit an <code>Edit</code> with a tag from before the restart.</li><li>Write a file through a path that a save hook reformats, then <code>Edit</code> using the tag the write returned.</li></ol></li><li><strong>Expected</strong>: Steps 1 and 2 fail — provenance is per reader, and no session hands another its tags. Steps 3, 4, and 6 fail with <code>EDIT_TAG_UNKNOWN</code> and an instruction to re-read, never with a wrong write. Step 5 applies anywhere in the union of the three windows without a fourth read. Step 7 applies, because the recorded tag describes the bytes that actually landed, and the drift is reported as a one-line warning rather than a whole-file diff.</li><li><strong>Specs linked</strong>: <code>03-runtime/18-line-anchored-edit-contract.md</code> §4.2, §4.4, §5.4, §13.5, <code>03-runtime/02-agent-runtime.md</code> §5f, ADR 0087 §3</li><li><strong>Acceptance</strong>: E (tools &amp; permissions), Quality</li><li><strong>Milestone</strong>: M5+</li><li><strong>Status</strong>: Documented</li></ul><h4 id="e2e-140-recoverable-edit-failures-each-get-one-retry-before-the-guard-counts-three" tabindex="-1">E2E-140: Recoverable edit failures each get one retry before the guard counts three <a class="header-anchor" href="#e2e-140-recoverable-edit-failures-each-get-one-retry-before-the-guard-counts-three" aria-label="Permalink to &quot;E2E-140: Recoverable edit failures each get one retry before the guard counts three&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A session with one file read, and a way to make the file drift on disk between calls.</li><li><strong>Steps</strong>: <ol><li>Let the file drift, then emit an <code>Edit</code> with the now-stale tag whose anchors cannot be remapped, so it fails with <code>EDIT_TAG_MISMATCH</code>.</li><li>Re-read, then emit an <code>Edit</code> anchored on lines the session never displayed, so it fails with <code>EDIT_LINES_UNSEEN</code> and a truncated reveal.</li><li>Emit an <code>Edit</code> on that same path with a malformed op header.</li><li>Emit a second <code>Edit</code> with a malformed op header.</li><li>Emit a third <code>Edit</code> with a malformed op header.</li></ol></li><li><strong>Expected</strong>: Steps 1 and 2 return their own codes with no <code>terminate</code> hint — each recoverable code spends its single grace on that path, and the turn keeps going, so the agent can act on what the error handed it. Steps 3 and 4 count as attempts 1 and 2 and still do not terminate. Step 5 terminates. A successful <code>Edit</code> inserted anywhere before step 5 resets the count, so the following failure is attempt 1 again.</li><li><strong>Specs linked</strong>: <code>03-runtime/18-line-anchored-edit-contract.md</code> §9.3, §11, <code>03-runtime/03-tools-and-permissions.md</code> §4d, ADR 0087, ADR 0207</li><li><strong>Acceptance</strong>: E (tools &amp; permissions), Quality</li><li><strong>Milestone</strong>: M5+</li><li><strong>Status</strong>: Documented</li></ul><h4 id="e2e-141-an-exhausted-retry-budget-ends-the-turn-with-a-visible-retriable-row" tabindex="-1">E2E-141: An exhausted retry budget ends the turn with a visible, retriable row <a class="header-anchor" href="#e2e-141-an-exhausted-retry-budget-ends-the-turn-with-a-visible-retriable-row" aria-label="Permalink to &quot;E2E-141: An exhausted retry budget ends the turn with a visible, retriable row&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A session where <code>Edit</code> on one path fails with a non-recoverable code every time.</li><li><strong>Steps</strong>: <ol><li>Emit three failing <code>Edit</code> calls on the same path within one prompt.</li><li>Observe the transcript after the agent loop stops.</li><li>Send a follow-up prompt in the same session.</li><li>Repeat with three failing <code>apply_patch</code> shell commands instead of <code>Edit</code>.</li></ol></li><li><strong>Expected</strong>: The third call carries the termination hint and the loop stops, but the turn does not merely complete: the transcript ends on an assistant error row with <code>MUTATION_RETRY_BUDGET_EXHAUSTED</code>, marked retriable, naming the path and the next action, and the same code arrives as an error event. When the last error is <code>EDIT_PARSE_FAILED</code>, the row&#39;s recovery hint explains the syntax correction — for example, a body-bearing <code>PUT 48.=48</code> must be written as <code>PUT 48.=48:</code> — and does not tell the agent to re-read solely to repair the malformed payload. The turn is recorded as failed rather than completed with no final message. Step 3 proceeds normally — the guard&#39;s counters are per prompt. Step 4 produces the same row with <code>details.kind</code> of <code>patch-command</code>.</li><li><strong>Specs linked</strong>: <code>03-runtime/18-line-anchored-edit-contract.md</code> §9.3, <code>03-runtime/03-tools-and-permissions.md</code> §4d, <code>03-runtime/08-error-codes.md</code> §3.3, ADR 0087</li><li><strong>Acceptance</strong>: E (tools &amp; permissions), C (chat &amp; stream)</li><li><strong>Milestone</strong>: M5+</li><li><strong>Status</strong>: Documented</li></ul><h4 id="e2e-156-edit-tool-succeeds-on-files-with-crlf-line-endings" tabindex="-1">E2E-156: Edit tool succeeds on files with CRLF line endings <a class="header-anchor" href="#e2e-156-edit-tool-succeeds-on-files-with-crlf-line-endings" aria-label="Permalink to &quot;E2E-156: Edit tool succeeds on files with CRLF line endings&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A workspace containing a file with Windows-style CRLF (<code>\\r\\n</code>) line endings.</li><li><strong>Steps</strong>: <ol><li>Use Read to display the file content and record the whole-file <code>tag</code>.</li><li>Issue an Edit with that <code>tag</code> and a <code>PUT N.=N:</code> whose body uses LF-only endings (as the model always produces from Read output).</li><li>Inspect the file on disk after the edit.</li></ol></li><li><strong>Expected</strong>: The Edit succeeds and returns a new <code>tag</code>. The written file preserves CRLF line endings throughout — both in modified and unmodified lines. No <code>MUTATION_RETRY_BUDGET_EXHAUSTED</code> error occurs.</li><li><strong>Specs linked</strong>: <code>03-runtime/18-line-anchored-edit-contract.md</code> §3.1, <code>03-runtime/03-tools-and-permissions.md</code></li><li><strong>Acceptance</strong>: E (tools &amp; permissions)</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Documented</li></ul><h4 id="e2e-142-background-delegation-converges-through-taskwait-and-honors-permission-scopes" tabindex="-1">E2E-142: Background delegation converges through TaskWait and honors permission scopes <a class="header-anchor" href="#e2e-142-background-delegation-converges-through-taskwait-and-honors-permission-scopes" aria-label="Permalink to &quot;E2E-142: Background delegation converges through TaskWait and honors permission scopes&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A project-bound Agent session whose permission mode can be switched between <code>ask</code>, <code>accept-edits</code>, and <code>auto</code>, with a provider whose stream can be driven; the five builtin subagents (<code>explorer</code>, <code>code-reviewer</code>, <code>test-runner</code>, <code>fixer</code>, <code>ui-designer</code>) and a global <code>~/.agents/subagents/readonly.md</code> definition. Builtins use the default <code>permission: inherit</code> behavior.</li><li><strong>Steps</strong>: <ol><li>Prompt a turn in which the assistant emits two <code>Task</code> calls — <code>explorer</code> on one direction and a second <code>explorer</code> on another — in one assistant message, then, without ending the turn, continues its own tool calls and converges with <code>TaskWait</code>.</li><li>Confirm the parent&#39;s visible text keeps streaming between <code>Task</code> and <code>TaskWait</code> (no dead turn), that the two <code>Task</code> rows form one delegation card that opens once, and that <code>TaskWait</code>&#39;s row shows both reports.</li><li>With the session in <code>ask</code>, prompt a turn that delegates to <code>fixer</code> with a multi-file spec. Confirm its <code>Write</code>/<code>Edit</code>, <code>Bash</code>, and external-path calls each render a card naming <code>fixer</code>. Switch the session to <code>accept-edits</code> and confirm only <code>Write</code>/<code>Edit</code> inside the workspace are auto-allowed. Switch it to <code>auto</code> and confirm the same delegate&#39;s <code>Write</code>/<code>Edit</code>, <code>Bash</code>, and external <code>Glob</code>/<code>Write</code> calls all resolve without a second authorization card.</li><li>Prompt a turn that starts three delegates and then calls <code>TaskWait</code> with <code>mode: &quot;any&quot;</code>, <code>minCompleted: 1</code>; confirm it returns as soon as the first settles and that the still-running delegates keep running.</li><li>Prompt a turn that starts a delegate and then ends the turn without <code>TaskWait</code>/<code>TaskStop</code>; confirm the delegate is stopped at run end and its node reads <code>aborted</code>, and that the next turn&#39;s model context contains no delegate rows.</li><li>Prompt ten <code>Task</code> calls in one turn and one more; confirm the eleventh fails as a tool error naming the 10-delegate cap, and that <code>TaskStop</code> frees a slot so an eleventh delegation can start.</li><li>Reload the session; confirm the delegation card, its nodes, and the <code>TaskWait</code> rows persist and re-render collapsed, and that <code>TaskWait</code> re-reads a settled delegation&#39;s report by id without re-running it.</li><li>Prompt a turn where the agent starts two Task calls and then emits visible text before calling TaskWait (so Task and TaskWait land in different activity parts); confirm the topology card shows &quot;completed&quot; status on both nodes once TaskWait returns, not stuck at &quot;running&quot;, and that each node shows a non-zero runtime duration derived from the delegation lifecycle timestamps rather than the immediate <code>Task</code> start call.</li><li>Edit <code>~/.agents/subagents/readonly.md</code> to declare <code>permission: auto</code> and reload the catalog; confirm the definition still loads but carries a warning, and that its delegate still resolves under the session&#39;s effective mode (a <code>Write</code> inside the workspace still raises a permission card).</li><li>Prompt a turn that starts a delegate, lets <code>TaskWait</code> time out so the node still says running, then calls <code>TaskStop</code>; confirm the topology node and the <code>TaskStop</code> row both read <code>stopped</code> (not <code>running</code>). End the turn and reload the session; confirm the card is not labelled working and does not keep ticking elapsed.</li></ol></li><li><strong>Expected</strong>: <code>Task</code> returns immediately with a <code>delegationId</code> and the parent keeps working; <code>TaskWait</code> converges with per-delegation reports and statuses; <code>TaskList</code>/<code>TaskStop</code> drive the lifecycle; a <code>TaskStop</code> result and a finished turn never leave a live “Subagent working” card; builtin <code>fixer</code> inherits the selected session permission mode, so <code>auto</code> also covers explicit external paths without a duplicate authorization prompt while <code>ask</code> and <code>accept-edits</code> retain their approval boundaries; a global definition&#39;s declared scope is dropped; the per-session running cap of 10 is enforced; no delegate outlives its turn; reloaded transcripts keep their delegation topology.</li><li><strong>Specs linked</strong>: <code>03-runtime/02-agent-runtime.md</code> §5f/§5f.1/§7.1, <code>03-runtime/03-tools-and-permissions.md</code> §10.2, <code>08-meta/decisions-log.md</code> (D242 amends D231), ADR 0089 and ADR 0100</li><li><strong>Acceptance</strong>: C (conversation), E (tools &amp; permissions), F (persistence), Security, Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Draft (unit coverage in <code>packages/agent-runtime</code><code>runtime.test.ts</code> subagent suite and host-core <code>rpc/mod.rs</code> delegate-scope tests; desktop journey pending)</li></ul><h4 id="e2e-162-e2e-173-delegate-workflow-scrolling" tabindex="-1">E2E-162 / E2E-173: Delegate workflow scrolling <a class="header-anchor" href="#e2e-162-e2e-173-delegate-workflow-scrolling" aria-label="Permalink to &quot;E2E-162 / E2E-173: Delegate workflow scrolling&quot;">​</a></h4><ul><li><strong>Status</strong>: Superseded by the single-scroll live process behavior in E2E-198.</li></ul><h4 id="e2e-199-subagent-editor-offers-preset-templates-and-a-provider-bounded-model-picker" tabindex="-1">E2E-199: Subagent editor offers preset templates and a provider-bounded model picker <a class="header-anchor" href="#e2e-199-subagent-editor-offers-preset-templates-and-a-provider-bounded-model-picker" aria-label="Permalink to &quot;E2E-199: Subagent editor offers preset templates and a provider-bounded model picker&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A project-bound Agent session. At least one configured, runnable provider with model bindings exists in Settings. The <code>~/.agents/subagents</code> directory is empty. Builtins are present but no project subagent file overrides them.</li><li><strong>Steps</strong>: <ol><li>Open Settings → Agent → Subagents, click <strong>New subagent</strong>, and confirm the sheet opens with a &quot;Start from template&quot; row of compact name chips (Explorer, Code reviewer, Test runner, Fixer, plus a blank chip). Chips show names only; the selected chip&#39;s one-line caption appears once under the row. Confirm there is no long subtitle, no per-chip Apply label, and that Advanced starts collapsed. Confirm hyphenated ids (<code>code-reviewer</code>, <code>test-runner</code>) render catalog names, not raw keys such as <code>presetCode-reviewerName</code>. At normal desktop width, confirm the name/description/select controls read as compact filled wells, the prompt editor is the only tall field, and the Save/Cancel actions stay visually subordinate to the form. Focus a field and confirm its accent ring remains visible without a permanent divider.</li><li>Click the <strong>Explorer</strong> chip without touching any field. Confirm the form is pre-filled: name <code>Explorer</code>, the description from the builtin, the <code>Read / Glob / Grep / Bash</code> tool grant, and the full Explorer system prompt. Expand Advanced and confirm the model field is unchanged (still inherit).</li><li>Reopen the sheet, click <strong>Fixer</strong>, and confirm the grant expands to <code>Read / Glob / Grep / Edit / Write / Bash</code> and the Fixer body. The mutating-hint line appears under the tools row. Expand Advanced and confirm the output limit starts empty.</li><li>Expand Advanced. Open the model picker. Confirm the picker lists every model of every configured, runnable provider, grouped by provider name. Choose one and confirm the draft&#39;s <code>model</code> field becomes <code>&lt;vendorKey-or-name&gt;/&lt;modelId&gt;</code> (matches what the runtime resolver accepts in <code>BUILTIN_SUBAGENT_DOCUMENTS</code>). 4a. Configure a custom endpoint whose display name contains a space (for example <strong>My Gateway</strong>). Confirm the picker offers it, select it, and confirm the sheet saves with the pin <code>&lt;display name&gt;/&lt;modelId&gt;</code> and the save button enabled. The picker and the draft check must never disagree about what is saveable.</li><li>Confirm the picker offers no <strong>Custom (provider/model)…</strong> entry and the field renders no free-text input, so a model id can only come from the configured catalog. Switch the picker to <strong>Inherit session model</strong>, save, and confirm the draft&#39;s <code>model</code> field is empty and the sidecar falls back to the session model. 5a. In the open model menu, confirm the list scrolls inside the menu and never runs past the window edge, and that typing in the filter field narrows the rows (including by provider name). Confirm the menu is not an OS-drawn select popup: it stays inside the sheet&#39;s own layer.</li><li>Disable every provider that contributes a model. Reopen the editor, expand Advanced, and confirm the model field renders an empty state with an action button instead of a free-text input, and that the action opens Models.</li><li>Switch the locale to Simplified Chinese. Confirm the preset chips render the translated names (<code>探索者</code>, <code>代码审查员</code>, <code>测试执行者</code>, <code>修复者</code>, <code>空白开始</code>) and the picker labels (<code>沿用当前会话的模型</code>, <code>前往模型设置</code>) resolve; no raw i18n keys appear in either locale.</li></ol></li><li><strong>Expected</strong>: The editor offers only models the user already configured, so the picker is the only way to set a model and every saved pin is resolvable. No free-text model id is accepted. When no provider offers a runnable model the field explains that and links to Models instead of asking the user to type an id the runtime could not resolve. Picking a preset overwrites the draft wholesale (description, tools, body) but never silently clears the user&#39;s other choices (model, thinking level, scope).</li><li><strong>Specs linked</strong>: <code>04-ux/06-settings-ia.md</code> §7, <code>03-runtime/13-model-catalog-and-selection.md</code> §11, <code>03-runtime/11-provider-model-system.md</code> §6.4, ADR 0062, ADR 0089</li><li><strong>Acceptance</strong>: B (model config), C (conversation &amp; stream), Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Unit/source-contract covered (<code>packages/shared/src/subagent-presets.test.ts</code>, <code>apps/desktop/test/subagent-editor-presets.test.mjs</code>); full UI journey Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-subagent-settings-lists-builtin-defaults" tabindex="-1">E2E-SUBAGENT-settings-lists-builtin-defaults <a class="header-anchor" href="#e2e-subagent-settings-lists-builtin-defaults" aria-label="Permalink to &quot;E2E-SUBAGENT-settings-lists-builtin-defaults&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A running app. <code>~/.agents/subagents</code> is empty. The five shipped builtins are present, none is switched off, and no user document shadows them.</li><li><strong>Steps</strong>: <ol><li>Open Settings → Agent → Subagents. Confirm a Built-in group lists <code>explorer</code>, <code>code-reviewer</code>, <code>test-runner</code>, <code>fixer</code>, and <code>ui-designer</code> with localized names, <code>Task(&lt;handle&gt;)</code> copy, tool grants, a Built-in badge, <strong>Copy as mine</strong>, and an enablement switch in the on position. Confirm none of those rows has Reveal or Delete.</li><li>Confirm the Global group still shows localized <code>settings.subagentsEmpty</code> copy and the New subagent action.</li><li>Switch <code>fixer</code> off from its Built-in row. Confirm the row dims with its switch off, the toast names it, nothing appears in <code>~/.agents/subagents</code>, and the row stays listed, because that switch is the way back on.</li><li>Send a prompt with <code>fixer</code> switched off. Confirm the Task catalog does not offer it while the other four remain, then switch it back on and confirm the next prompt offers it again.</li><li>Choose <strong>Copy as mine</strong> on explorer. Confirm the create sheet opens pre-filled from that definition (name, description, tools, body) with the Explorer template chip selected, not Blank. Save. Confirm explorer now appears only as a user-owned Global row and is omitted from Built-in, and the next prompt&#39;s Task catalog uses the user document.</li><li>Disable the user explorer and reload the page. Confirm the user row is off and explorer reappears under Built-in (disabled user documents do not reach the loader, so the shipped definition wins again).</li></ol></li><li><strong>Expected</strong>: Settings shows the defaults the agent can actually delegate to, and every one of them can be turned off from its own row. Builtin activation is app-local state rather than a document, so a switched-off default keeps its row; copying a builtin stays the way to retune one, and reveal and delete remain file-backed actions on user-owned rows only.</li><li><strong>Specs linked</strong>: <code>04-ux/06-settings-ia.md</code> §2, <code>03-runtime/01-ipc-protocol.md</code> §12c, <code>03-runtime/02-agent-runtime.md</code> §5f, ADR 0062, ADR 0063, ADR 0270</li><li><strong>Acceptance</strong>: E (tools &amp; permissions), Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Source/unit covered (<code>apps/desktop/test/agent-capability-settings.test.mjs</code>, <code>apps/desktop/test/subagent-wiring.test.mjs</code>, <code>packages/agent-runtime/src/subagent-definitions.test.ts</code>, <code>packages/shared/src/subagent-presets.test.ts</code>); full UI journey Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-198-a-subagent-task-opens-with-a-live-conversation-process" tabindex="-1">E2E-198: A subagent task opens with a live conversation process <a class="header-anchor" href="#e2e-198-a-subagent-task-opens-with-a-live-conversation-process" aria-label="Permalink to &quot;E2E-198: A subagent task opens with a live conversation process&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A project-bound Agent session with a mocked provider stream where one <code>explorer</code> delegate has a Task description and emits thinking, tool, and answer rows over time. The work panel is initially closed.</li><li><strong>Steps</strong>: 1) Expand the activity group if needed and click the <code>explorer</code> topology node. 2) Click the selected <code>explorer</code> node again and confirm the right-side dock closes, then click it once more to reopen it. 3) Observe the right-side dock while the delegate streams. 4) Scroll the task/process conversation upward and then return to the latest output. 5) Switch sessions and return to the original session. 6) Let a delegate start and then fail, open its node, and read the foot of the dock; repeat with a delegate that completes and one that is still running.</li><li><strong>Expected</strong>: The right dock shows a sticky identity header (avatar, name, and model caption on the left; status capsule and elapsed time trailing on the same row without wrapping), the Task call&#39;s description as a full-width inset grouped card under a Task section label, capped at four lines with an inline Show more / Show less control for longer tasks, and the delegate&#39;s live thinking/tool/answer process under an Activity section on one subtle vertical timeline using the same row components as the main conversation. The selected topology node is a full-row toggle without an extra disclosure chevron: its first click opens the dock and its second click closes it. New rows appear without a reload and follow the bottom while pinned. The panel has one body scrollbar; the process does not create a nested scrollbar or a second elevated card. At the minimum supported panel width, long commands, paths, and tool summaries remain contained within the dock without horizontal page overflow. A real upward gesture pauses follow and exposes jump-to-latest. The transcript remains the same height and keeps its own scroll state. Session switching hides the selection and returning never shows another session&#39;s task. In step 6, a delegate that starts and then fails closes the dock with an error card rather than a bare <code>Failed</code> capsule: the localized summary (the registered <code>errors.&lt;code&gt;</code> sentence when the runtime reported a known code, the localized <code>chat.subagentStatus.*</code> outcome otherwise), the stable code, the raw provider message behind a Show details / Hide details disclosure, and a copy control. The disclosure control stays reachable while the details are collapsed, and the delegate that completed or is still running shows no card at all.</li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code> §5.7, <code>04-ux/09-interaction-patterns.md</code> §9.1</li><li><strong>Acceptance</strong>: C (conversation), Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Documented; desktop journey pending. The failure card&#39;s data source is unit-tested in <code>subagent-topology.test.mjs</code>: a settled delegation&#39;s <code>error: { code, message }</code> read from <code>TaskWait</code> <code>delegations[]</code> and <code>TaskStop</code><code>stopped[]</code>, lifecycle row ordering, entries without an error, and the terminal Task snapshot that carries a failure before parent polling.</li></ul><h4 id="e2e-subagent-settlement-updates-before-parent-poll" tabindex="-1">E2E-SUBAGENT-settlement-updates-before-parent-poll <a class="header-anchor" href="#e2e-subagent-settlement-updates-before-parent-poll" aria-label="Permalink to &quot;E2E-SUBAGENT-settlement-updates-before-parent-poll&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: An Agent session with two parallel delegates; one can finish while the other continues and the parent does not poll lifecycle tools.</li><li><strong>Steps</strong>: 1) Start both delegates and open the first delegate&#39;s detail dock. 2) Optionally let TaskList report both as running. 3) Complete only the first delegate while the parent turn remains live. 4) Switch sessions and return, then reload history after the turn finishes. 5) Repeat with failed and stopped delegates, and with a delegate finishing before its Task result arrives.</li><li><strong>Expected</strong>: The settled node stops spinning immediately, its status is completed (green) or the actual failure/stop outcome, and its elapsed time stops increasing. The sibling remains running; the aggregate reads one of two settled. The open dock updates with the same status and failure details. A stale running TaskList snapshot cannot undo settlement. Reload preserves the actual terminal outcome and original Task identity, arguments, and usage.</li><li><strong>Specs linked</strong>: <code>03-runtime/02-agent-runtime.md</code> §Subagents, <code>04-ux/08-component-spec.md</code> delegation topology</li><li><strong>Acceptance criterion</strong>: C, Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Runtime event-order and renderer projection regressions automated; desktop journey documented. Required suites: <code>test:e2e</code>, <code>test:e2e:subagents</code>.</li></ul><h4 id="e2e-subagent-resume-a-settled-delegation" tabindex="-1">E2E-SUBAGENT-resume-a-settled-delegation <a class="header-anchor" href="#e2e-subagent-resume-a-settled-delegation" aria-label="Permalink to &quot;E2E-SUBAGENT-resume-a-settled-delegation&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: An Agent session on a deterministic local transport that can complete, fail, stop, and hold a delegate open on request. Two user definitions, <code>~/.agents/subagents/scout.md</code> (read-only) and <code>~/.agents/subagents/fixer.md</code>; a workspace file <code>src/report.ts</code> whose conclusion is easy to locate by file and line; and a definition model binding that can be removed from the configuration. The parent catalog offers <code>Task</code>, <code>TaskWait</code>, <code>TaskList</code>, and <code>TaskStop</code>.</li><li><strong>Steps</strong>: <ol><li>Delegate <code>scout</code> a brief that must end by naming one conclusion with its file and line number, and let it settle <code>completed</code>. Note the <code>delegationId</code> the Task result returns.</li><li>Send a turn that passes that id as <code>Task.resume</code> and asks for the same conclusion plus a second occurrence the first run never reached. Expand the delegation card, then inspect what the parent itself received.</li><li>Send a turn that only says &quot;reuse what the scout already found&quot;, with no <code>resume</code> value.</li><li>Start a long-running <code>scout</code> delegation and call <code>Task.resume</code> for it while it works; then, while a resumed run of another chain is still working, call <code>Task.resume</code> for that chain again.</li><li><code>TaskStop</code> one delegation, abort a second, and close the app while a third is still working. After relaunch, call <code>Task.resume</code> for each of those three ids as reported by <code>TaskWait</code>.</li><li>Let a delegation fail after one successful read, then call <code>Task.resume</code> for it.</li><li>Send one <code>Task</code> call that carries both <code>resume</code> and <code>model</code>.</li><li>Remove the chain&#39;s recorded model from the configuration, then <code>Task.resume</code> the chain and read its delegation lifecycle details and card.</li><li>Settle two <code>scout</code> chains, <code>resume</code> the older one, settle a third chain for the same definition, and read the reusable list the next prompt offers; then keep one chain running while two others settle for that definition.</li><li>Accumulate more than 50,000 lines of read-only tool output in one chain, then call <code>Task.resume</code> with its id.</li><li>Resume a <code>scout</code> chain while spelling <code>Task.agent</code> as <code>Explorer</code>, then as <code>explorer.md</code>, then as another definition&#39;s name.</li><li>Let one chain read more than eight files and send two further prompts in the same session, comparing the reusable list each prompt composes.</li><li>Relaunch the app and, without changing anything else, <code>Task.resume</code> a <code>completed</code> chain from before the restart.</li><li>From a fixture session that resolves a chain but no longer holds any of its delegate rows, call <code>Task.resume</code> with that id.</li></ol></li><li><strong>Expected</strong>: <ul><li>Step 2 resumes from the chain: the new run answers with the earlier conclusion&#39;s exact file and line, and its rows contain no fresh full read of that file. <code>Task</code> returns a new <code>delegationId</code>, and the parent&#39;s own context still holds one final report for that delegation and no delegate tool rows.</li><li>Step 3 is a cold start: a brand-new delegate with no earlier conclusion in its rows, a fresh <code>delegationId</code>, and no link to the earlier chain. Leaving the id out never inherits context, whatever the prompt says.</li><li>Step 4 fails twice as tool errors. The running delegation is reported as still running, with the instruction to converge through <code>TaskWait</code> first; nothing is started, nothing is queued, and the running delegate keeps working.</li><li>Step 5 refuses <code>stopped</code>, <code>aborted</code>, and the run the app closed while it was still working (which reads as <code>interrupted</code> after relaunch) as not resumable, each naming that reason and the fresh-delegation path instead. No run starts.</li><li>Step 6 resumes the failed chain like a completed one: the reads it already made seed the new run, and its failed assistant row is not replayed.</li><li>Step 7 is refused as a tool error: a resumed run keeps the chain&#39;s model, and the message points at starting a new delegation to change models.</li><li>Step 8 still resumes, on the binding the definition resolves to now, and the delegation&#39;s lifecycle details carry the previous model id as <code>modelChangedFrom</code>. The parent sees it through the lifecycle snapshot it polls and the card shows it with the run, so the switch is never silent.</li><li>Step 9 keeps at most two reusable chains per definition: the least recently active settled chain is evicted whole, so every id on it answers as an unknown delegation with the current reusable list, while a chain whose latest run is still working is never evicted — the group may sit over the bound rather than strand a live delegate.</li><li>Step 10 drops the over-budget chain from the reusable list without trimming its history: no later prompt offers it and a <code>resume</code> for it is refused as having read too much to resume cheaply, while an ordinary delegation for the same work still succeeds from cold.</li><li>Step 11 matches the definition name case-insensitively and with the document suffix — <code>Explorer</code> and <code>explorer.md</code> both resume the chain — while a different definition&#39;s name is a name mismatch that lists the agents that do have reusable chains.</li><li>Step 12 offers each chain&#39;s latest <code>delegationId</code>, its objective, and at most eight of the files it read, with a <code>(+N more)</code> suffix past that. A chain that just settled is already there in the same session, with no app restart and no new session; running, non-resumable, over-budget, and evicted chains never appear.</li><li>Step 13 rebuilds the chain relationship from the transcript, so the <code>completed</code> chain is offered again after relaunch and resumes as before.</li><li>Step 14 fails as having no recorded history, and that same id is not listed among the reusable ids in the error or in any later prompt: the chain leaves the reusable list.</li><li>Throughout, the transcript shows the chain as one continuous multi-turn conversation under its newest Task card, with no &quot;resumed&quot; marker, and the resumed run&#39;s counters start at 0 so its turn, tool, and usage numbers describe the new run while the earlier rounds stay readable above it.</li></ul></li><li><strong>Specs linked</strong>: <code>03-runtime/02-agent-runtime.md</code> §5f, ADR 0279</li><li><strong>Acceptance</strong>: C (conversation), Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Not covered (二期)</strong>: reviving a <code>stopped</code>/<code>aborted</code> delegation, in-chain compaction, task queuing, and cross-session resumption.</li><li><strong>Status</strong>: Draft — chain resolution, resume validation, the reusable list, and transcript chain grouping are unit/regression covered (<code>packages/agent-runtime/src/delegation-chain.test.ts</code>, <code>delegation-history.test.ts</code>, <code>runtime.test.ts</code>, <code>apps/desktop/test/assistant-turns.test.mjs</code>); the desktop journey needs a capable environment. Required suites: <code>test:e2e</code>, <code>test:e2e:subagents</code>, <code>test:e2e:transcript</code>.</li></ul><h4 id="e2e-subagent-context-overflow-compacts-before-failing" tabindex="-1">E2E-SUBAGENT-context-overflow-compacts-before-failing <a class="header-anchor" href="#e2e-subagent-context-overflow-compacts-before-failing" aria-label="Permalink to &quot;E2E-SUBAGENT-context-overflow-compacts-before-failing&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: An Agent session on a deterministic local transport whose model metadata declares a small context window (for example 16,000 tokens) and whose replies are scripted. The small window is injected through that fake provider, never a real one: real providers and paid APIs are not default test environments in this repository. A user definition <code>~/.agents/subagents/reader.md</code> declares <code>Read</code>, <code>Glob</code>, and <code>Grep</code>, and the workspace holds files large enough that two or three reads cross the delegate&#39;s hard limit.</li><li><strong>Steps</strong>: <ol><li>Delegate a brief that requires reading those files in sequence and record every request the transport receives, with its estimated size.</li><li>Read the request that follows the boundary at which the delegate crosses its hard limit.</li><li>Repeat with the crossing landing while a tool result is still pending, then with it landing on a completed turn.</li><li>Repeat with the summary request scripted to fail.</li><li>Run the same brief as the session Agent on the same fixture and compare its requests and transcript rows with a run recorded before this change.</li><li>Inspect the delegation card, the transcript, the context inspector, and the parent&#39;s own model context after the delegate settles.</li></ol></li><li><strong>Expected</strong>: The delegate keeps working instead of failing. The request after the crossing is below the hard limit and carries a summary plus the applicable retained tail; no request is sent above the window. A pending tool result retains as an active turn (latest user message only), a completed turn retains none. A failed summary degrades to the original task brief plus the most recent message(s), the run still completes, and the report and lifecycle details say it was degraded rather than presenting a partial answer as complete. Delegate compaction adds no transcript row, no host-core checkpoint, no warning toast, and no context-inspector line; the delegate&#39;s own rows stay complete and the parent&#39;s model context still holds only the report. The session Agent behaves exactly as it did before.</li><li><strong>Specs linked</strong>: <code>03-runtime/02-agent-runtime.md</code> §5.1, §5f, <code>03-runtime/08-error-codes.md</code> §3.2, ADR 0299, ADR 0064, ADR 0136</li><li><strong>Acceptance criterion</strong>: C — Conversation &amp; stream; Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Draft. Required suites: <code>test:e2e</code>, <code>test:e2e:subagents</code>.</li></ul><h4 id="e2e-subagent-context-overflow-reports-actionable-failure" tabindex="-1">E2E-SUBAGENT-context-overflow-reports-actionable-failure <a class="header-anchor" href="#e2e-subagent-context-overflow-reports-actionable-failure" aria-label="Permalink to &quot;E2E-SUBAGENT-context-overflow-reports-actionable-failure&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: The same injected small-window fake provider, sized so even the degraded context cannot fit. One definition declares two ordered <code>fallbackModels</code>: one whose window is no larger than the primary&#39;s and one that is larger. A second definition declares none.</li><li><strong>Steps</strong>: <ol><li>Delegate a brief that overflows past both compaction and degradation, and read the tool result the parent receives plus the lifecycle details.</li><li>Repeat for the definition that declares the two alternatives, recording which alternatives the transport is actually asked for.</li><li>Repeat with every alternative at the same small window.</li><li>Read the delegation card and the report in English and in Chinese.</li><li>Continue the parent turn, then send a new prompt.</li></ol></li><li><strong>Expected</strong>: The run fails with <code>SUBAGENT_CONTEXT_OVERFLOW</code>, not retriable, and what the parent reads names what it can change — narrow the task, delegate to a model with a larger context window, read less at once. The provider&#39;s raw overflow sentence is not what the parent receives. An alternative whose own budget cannot hold the carried context is never requested and appears in <code>modelFailures</code> with that reason; the larger alternative is attempted and can succeed. With no alternative that fits, the outcome stays <code>SUBAGENT_CONTEXT_OVERFLOW</code> rather than the final provider error. The delegate&#39;s rows stay durable and visible, the session returns to idle, and the next prompt is not <code>AGENT_BUSY</code>.</li><li><strong>Specs linked</strong>: <code>03-runtime/02-agent-runtime.md</code> §5f, <code>03-runtime/08-error-codes.md</code> §3.2, ADR 0299, ADR subagent-model-fallback</li><li><strong>Acceptance criterion</strong>: C — Conversation &amp; stream; Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Draft. Required suites: <code>test:e2e</code>, <code>test:e2e:subagents</code>, <code>test:e2e:subagent-models</code>.</li></ul><h4 id="e2e-subagent-resume-seeds-within-context-budget" tabindex="-1">E2E-SUBAGENT-resume-seeds-within-context-budget <a class="header-anchor" href="#e2e-subagent-resume-seeds-within-context-budget" aria-label="Permalink to &quot;E2E-SUBAGENT-resume-seeds-within-context-budget&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: The same injected small-window fake provider. One settled <code>reader</code> chain read enough to exceed the delegate hard limit while staying under <code>MAX_RESUMABLE_READ_LINES</code>; a second settled chain fits well inside the budget.</li><li><strong>Steps</strong>: <ol><li><code>Task.resume</code> the over-budget chain and capture its first provider request in full.</li><li><code>Task.resume</code> the chain that fits and capture the same request.</li><li>Ask the resumed run for a conclusion the chain reached in its most recent round, and for one it reached in its first round.</li><li>Relaunch the app, rebuild the chain index from the transcript, and resume the over-budget chain again.</li><li>Accumulate more than <code>MAX_RESUMABLE_READ_LINES</code> of read-only output in a chain and read the reusable list the next prompt offers.</li></ol></li><li><strong>Expected</strong>: The first request of a resumed run is below the hard limit. It opens with the original task brief and holds the most recent turns; the oldest tool results are dropped first, and dropping an assistant message drops its tool calls with it, so no orphaned tool call reaches the provider. A chain that fits is seeded whole, exactly as before. The most recent conclusion is answered from the seeded context; the first round&#39;s may be gone, and the run says so rather than inventing it. A resume never fails with <code>CONTEXT_TOO_LARGE</code> or <code>SUBAGENT_CONTEXT_OVERFLOW</code> on its first request. <code>MAX_RESUMABLE_READ_LINES</code> still removes an over-read chain from the reusable list; truncation does not make it resumable again.</li><li><strong>Specs linked</strong>: <code>03-runtime/02-agent-runtime.md</code> §5f, ADR 0299, ADR 0279</li><li><strong>Acceptance criterion</strong>: C — Conversation &amp; stream; Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Draft. Required suites: <code>test:e2e</code>, <code>test:e2e:subagents</code>.</li></ul><h4 id="e2e-161-a-delegation-lifecycle-row-reads-as-a-subagent-row" tabindex="-1">E2E-161: A delegation lifecycle row reads as a subagent row <a class="header-anchor" href="#e2e-161-a-delegation-lifecycle-row-reads-as-a-subagent-row" aria-label="Permalink to &quot;E2E-161: A delegation lifecycle row reads as a subagent row&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A project-bound Agent session with a mocked provider stream that starts two delegates (<code>explorer</code>, <code>fixer</code>) with <code>Task</code>, then calls <code>TaskList</code>, <code>TaskWait</code> and <code>TaskStop</code>.</li><li><strong>Steps</strong>: 1) Start both delegates and inspect the collapsed <code>TaskList</code> row while both are running. 2) Let <code>explorer</code> complete and <code>fixer</code> fail, then inspect the <code>TaskWait</code> row&#39;s summary, badge and expanded body. 3) Start a third delegate reusing the <code>explorer</code> definition, call <code>TaskList</code>, and read the summary. 4) Stop a running delegate and inspect the <code>TaskStop</code> row. 5) Confirm the delegation card&#39;s own subagent count is unchanged by all three lifecycle rows. 6) Repeat in Chinese.</li><li><strong>Expected</strong>: Every lifecycle row summarizes by agent name — never by its <code>delegationIds</code> argument, and no bare UUID appears in a collapsed row. Its badge uses the shared subagent status vocabulary: running while any member runs, <code>Failed</code> / “失败” once a member failed even though a sibling completed, and <code>Stopped by request</code> / “已按请求停止” for the stopped delegate — including when the persisted <code>TaskStop</code> snapshot still says <code>running</code>. The topology card&#39;s node matches that stopped outcome and is not labelled working after the turn ends. A repeated definition is counted (<code>explorer ×2</code>) rather than listed twice. The expanded body shows the joined reports as a notice followed by one named row per subagent with status, runtime and turns, and contains no pretty-printed <code>delegations[]</code> JSON. The delegation card still reports the number of <code>Task</code> calls only, so lifecycle rows never inflate the topology counts.</li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code> §9.9, <code>03-runtime/02-agent-runtime.md</code> §5f, ADR 0062, ADR 0089, decisions-log D269</li><li><strong>Acceptance</strong>: C (conversation), Quality</li></ul><h4 id="e2e-155-subagent-lifetime-is-parent-judged-runtime-delivers-reports" tabindex="-1">E2E-155: Subagent lifetime is parent-judged; runtime delivers reports <a class="header-anchor" href="#e2e-155-subagent-lifetime-is-parent-judged-runtime-delivers-reports" aria-label="Permalink to &quot;E2E-155: Subagent lifetime is parent-judged; runtime delivers reports&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A project-bound Agent session with a Bash-capable <code>explorer</code> definition and a mocked provider stream.</li><li><strong>Steps</strong>: 1) Start a delegate and let the parent stop calling tools while it still runs; confirm the durable turn stays open and the delegate is not aborted. 2) Let the delegate finish and confirm the parent is prompted with its report without the user sending “continue”. 3) Let a <code>TaskWait</code> expire while the delegate is still running and read the heartbeat the parent receives. 4) <code>TaskList</code> a running delegate and confirm elapsed / last-tool fields. 5) <code>TaskStop</code> and user Stop still abort. 6) Run a delegate whose document declares <code>maxTurns: 2</code> and let it pass two tool-calling turns; confirm the key is ignored and the delegate keeps running. 7) Start a delegate on another model, exhaust the parent HTTP 429 budget, and click Continue; confirm leftover delegates abort, the session is idle, Continue is accepted, and the failed assistant error surface stays visible. 8) Define a delegate with an explicit <code>maxTokens</code> and one without, run both, and read the two outgoing provider requests. 9) Start enough delegates for their combined reports to exceed the bounded <code>TaskWait</code> result, then let the parent idle.</li><li><strong>Expected</strong>: Idle and duration watchdogs never fire. Parent idle does not abort delegates. Completion reports are delivered into the same durable turn. <code>TaskWait</code> expiry reports “Still running after Ns”, includes a heartbeat, and states that this is not a failure. No turn count ends a delegate, and no status reports one. Explorer&#39;s catalog includes <code>Bash</code> while code-reviewer remains read-only. A terminal parent 429 aborts leftover delegates and Continue is not <code>AGENT_BUSY</code> (D352). In step 8 the capped delegate&#39;s request carries the declared output limit and the uncapped one carries the model&#39;s published limit, so the cap overrides the derived <code>max_tokens</code> / <code>max_completion_tokens</code> / <code>max_output_tokens</code> without disturbing the session&#39;s own requests (D383). In step 9, the reports omitted from the bounded <code>TaskWait</code> content are delivered once by the idle resume and are not replayed after they reach the parent.</li><li><strong>Specs linked</strong>: <code>03-runtime/02-agent-runtime.md</code> §5f, <code>03-runtime/08-error-codes.md</code>, <code>03-runtime/09-logging-and-observability.md</code>, ADR 0166, ADR 0189, decisions-log D328 / D352 / D383</li><li><strong>Acceptance</strong>: C (conversation), E (tools &amp; permissions), H (diagnostics), Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Covered by unit tests; full desktop journey pending. The output cap&#39;s parse and clamp are covered in <code>packages/shared</code><code>subagent-definition.test.ts</code>, and its document round-trip in host-core <code>user_subagents</code> tests; the request-level assertion in step 8 stays manual.</li></ul><h4 id="e2e-157-global-scrollbars-stay-quiet-while-remaining-discoverable" tabindex="-1">E2E-157: Global scrollbars stay quiet while remaining discoverable <a class="header-anchor" href="#e2e-157-global-scrollbars-stay-quiet-while-remaining-discoverable" aria-label="Permalink to &quot;E2E-157: Global scrollbars stay quiet while remaining discoverable&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: PI-Desktop is open with the expanded sidebar, more temporary sessions than the five-row cap, and enough retained project sessions to overflow the Projects region.</li><li><strong>Steps</strong>: 1) Inspect the idle Sessions and Projects scrollbars in light and dark themes. 2) Move the pointer into each list and then away from it, confirming that only the hovered list&#39;s thumb appears. 3) Move the pointer back into each list, drag its thumb through the region, and keyboard-focus a row to confirm the focused list keeps its thumb available. 4) Scroll the list with the wheel or trackpad after moving the pointer away from the thumb. 5) Open a long conversation and a long right-side work-panel view, including the file manager view when the bundled plugin is enabled, and compare their idle, hovered, focused, and scrolling states on Windows.</li><li><strong>Expected</strong>: Both regions remain independently scrollable and the footer stays fixed. Every in-app scrollbar is trackless, 6px wide, and transparent at rest; hovering or focusing the owning scroller reveals only its thumb, while scrolling reveals it for 300ms after the last scroll event. Dragging keeps it visible without changing the scroll region&#39;s width. Chat, code, Settings, and the right-side work-panel scrollbars all match this rule. A docked or detached plugin panel receives the same host-injected rule; an external page inside Browser keeps its own page-owned scrollbar.</li><li><strong>Specs linked</strong>: <code>04-ux/07-ui-design-system.md</code>, <code>04-ux/08-component-spec.md</code>, <code>04-ux/09-interaction-patterns.md</code></li><li><strong>Acceptance</strong>: Quality (sidebar polish and independent navigation)</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Unit-covered (<code>interaction-polish.test.mjs</code>); rendered scenario pending</li></ul><h4 id="e2e-158-temporary-sessions-use-isolated-scratch-workspaces" tabindex="-1">E2E-158: Temporary sessions use isolated scratch workspaces <a class="header-anchor" href="#e2e-158-temporary-sessions-use-isolated-scratch-workspaces" aria-label="Permalink to &quot;E2E-158: Temporary sessions use isolated scratch workspaces&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: PI-Desktop has a project open, a temporary session can be created, and the host data directory is known. The temporary session starts with an empty transcript.</li><li><strong>Steps</strong>: <ol><li>Create or select a temporary session while the project remains recently active, then inspect the empty-home hero. 2. Confirm the hero uses the temporary-session copy and has no project underline or project switcher; confirm a project session and no active session still use their own hero states.</li><li>In the temporary session, use Read/Glob/Grep on a file under its <code>&lt;data_dir&gt;/scratch/&lt;sessionId&gt;</code> root, then Write/Edit a file with a workspace-relative path and run a bounded Bash command.</li><li>Inspect the tool results and filesystem, then switch back to the project and confirm the project root and git status are unchanged.</li><li>Enter Plan or Goal in the temporary session and confirm submission still fails with the existing project-root requirement.</li></ol></li><li><strong>Expected</strong>: A path-less session binds every native tool to its own <code>scratch/&lt;sessionId&gt;</code> directory, never to the visible or recently active project. Relative paths work inside that scratch root, containment and permission rules remain active, and no project artifact is created. The temporary hero is localized and has no project switcher; project and no-session hero states remain unchanged. Plan/Goal retain their project-root boundary.</li><li><strong>Specs linked</strong>: <code>03-runtime/03-tools-and-permissions.md</code> §4/§4b, <code>03-runtime/10-session-state-machine.md</code>, <code>04-ux/01-ui-ia.md</code>, <code>04-ux/02-i18n-english-first.md</code>, ADR 0124</li><li><strong>Acceptance</strong>: C (conversation), D (workspace), E (tools &amp; permissions), F (persistence), Security, Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Unit-covered (<code>crates/host-core/src/rpc/mod.rs</code>, <code>temporary-session-workspace.test.mjs</code>); rendered desktop journey pending</li></ul><h4 id="e2e-159-a-long-transcript-keeps-a-bounded-mounted-window" tabindex="-1">E2E-159: A long transcript keeps a bounded mounted window <a class="header-anchor" href="#e2e-159-a-long-transcript-keeps-a-bounded-mounted-window" aria-label="Permalink to &quot;E2E-159: A long transcript keeps a bounded mounted window&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: PI-Desktop is open on a session whose transcript is substantially longer than one <code>session.get</code> page (several hundred messages, including fenced code blocks and expanded tool activity), on a memory- constrained Windows machine where the regression was reported.</li><li><strong>Steps</strong>: <ol><li>Activate the session and confirm the first paint lands at the newest message without a top-of-transcript flash.</li><li>Record the renderer&#39;s transcript row count and heap usage, then scroll upward continuously to the oldest loaded message and past it so older pages are fetched.</li><li>At each point where the view stops advancing, confirm it resumes: earlier rows appear without the rows under the cursor being pushed down, and the earlier-messages indicator appears only when a page is actually fetched.</li><li>Re-record the row count and heap usage after paging far back, then scroll back to the bottom and send a new prompt.</li><li>While the answer streams, confirm following stays pinned and typing in the composer stays responsive. Scroll up mid-stream and confirm follow releases and the jump-to-latest pill appears.</li><li>Hover and click conversation minimap dashes at several heights, then switch to another session and back.</li><li>Activate a long session whose newest page collapses to less than one viewport (one tool-heavy turn) and confirm the outline is present with its earlier-history continuation, that clicking it reveals earlier turns, and that it keeps advancing without a manual scroll gesture until the whole history is loaded and mounted.</li><li>Confirm the continuation disappears once nothing earlier remains, and that a short completed conversation that fits one viewport still shows no rail.</li></ol></li><li><strong>Expected</strong>: Mounted transcript rows stay bounded by the window rather than growing with how far back the user scrolled, so the recorded row count and heap usage after paging far back stay close to the values recorded before it. Upward travel is continuous: growing the window and fetching a page both keep the viewport anchored to the row being read. Every minimap message dash jumps to a real row. Streaming stays smooth with the composer responsive, pinned-follow and jump-to-latest behave as specified, and switching away and back paints the retained pane at the position it was left. Earlier history is never stranded: with rows withheld or an older page pending, the outline stays available with an actionable earlier-history continuation and keeps advancing when the top boundary remains visible, even when the mounted tail does not overflow one viewport (D269). The continuation and the rail disappear once the full history is loaded and mounted and the transcript fits one page.</li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code> §7 and §8, <code>03-runtime/04-data-storage.md</code>, ADR 0120, ADR 0127, ADR 0130, D108, D269</li><li><strong>Acceptance</strong>: C (conversation), H (diagnostics), Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Unit-covered (<code>transcript-window.test.mjs</code>, <code>conversation-minimap.test.mjs</code>, <code>interaction-performance.test.mjs</code>); rendered desktop journey and the low-memory Windows measurement pending</li></ul><h4 id="e2e-160-dragging-the-window-across-displays-keeps-the-dropped-position" tabindex="-1">E2E-160: Dragging the window across displays keeps the dropped position <a class="header-anchor" href="#e2e-160-dragging-the-window-across-displays-keeps-the-dropped-position" aria-label="Permalink to &quot;E2E-160: Dragging the window across displays keeps the dropped position&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: PI-Desktop is open on a machine with two displays arranged side by side, ideally with different work areas (a menu bar or taskbar on one only, or different resolutions). Run the case once with the work panel closed and once with it open at a committed width.</li><li><strong>Steps</strong>: <ol><li>Note the window position on the first display, then drag it by its titlebar onto the second display and release the pointer.</li><li>Confirm the window stays where it was released: no jump, no snap to the display edge, and no vertical shift inherited from the first display.</li><li>Drag the window so it straddles the boundary between the two displays and release it, then confirm it settles fully inside one display&#39;s work area without changing size.</li><li>With the panel open, repeat the cross-display drag and confirm the panel remains an internal column at its committed renderer width; no reservation is re-planned and no panel-specific native geometry is applied.</li><li>Drag the window back to the first display and confirm the application bounds continue to follow the dropped position without panel expansion.</li><li>Leave the window on the second display, quit, and relaunch.</li><li>Disconnect the second display while the window is on it, then reconnect it.</li></ol></li><li><strong>Expected</strong>: Every pointer release leaves the window at the position the user dropped it on the display they dropped it on. A straddling drop is normalized into one work area without a resize. Relaunch reopens the window on the display it was last used on rather than the one it started on. Removing the display the window occupied still relocates it to a live display, and reconnecting preserves the same application bounds contract; no work-panel reservation is restored.</li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code>, <code>04-ux/09-interaction-patterns.md</code> §8, ADR 0132, ADR 0151</li><li><strong>Acceptance</strong>: F (persistence), Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Unit-covered (<code>work-panel-window.test.mjs</code>: cross-display drag adoption, previous-display replan regression, work-area clamping); the two-display desktop journey and the relaunch/hotplug legs are pending</li></ul><h4 id="e2e-167-native-edge-resize-stays-smooth-and-persists-the-settled-bounds" tabindex="-1">E2E-167: Native edge resize stays smooth and persists the settled bounds <a class="header-anchor" href="#e2e-167-native-edge-resize-stays-smooth-and-persists-the-settled-bounds" aria-label="Permalink to &quot;E2E-167: Native edge resize stays smooth and persists the settled bounds&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: PI-Desktop is open in a normal, non-maximized window on macOS, Windows, or Linux. Run the case with the work panel closed and once with it open at a committed width.</li><li><strong>Steps</strong>: <ol><li>Drag each reachable window edge and one corner slowly, including a brief pause during the gesture, then release.</li><li>Confirm the window follows the pointer continuously and does not jump to the default size or display edge while the pointer is down.</li><li>With the work panel open, drag its inner divider slowly in both directions and confirm the panel width changes inside the existing window while the native bounds stay fixed. Repeat below the panel minimum and above its maximum, then verify the target follows the live budget (<code>client width - 360px - expanded sidebar</code>) instead of a fixed cap.</li><li>Close and relaunch the app after the resize settles.</li></ol></li><li><strong>Expected</strong>: Native edge and corner hit regions remain available in frameless chrome, the minimum size remains 1040×700, and the recovery watchdog does not compete with a slow resize stream. The renderer-owned divider updates the bounded panel target without changing native bounds; the last settled window bounds and the committed panel width reopen after relaunch. No temporary work-panel reservation width is persisted or restored.</li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code>, <code>04-ux/01-ui-ia.md</code>, <code>04-ux/07-ui-design-system.md</code>, <code>04-ux/08-component-spec.md</code>, <code>04-ux/09-interaction-patterns.md</code>, ADR 0029 / ADR 0151</li><li><strong>Acceptance</strong>: A (app shell), F (persistence), Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Unit/source-contract covered; native desktop edge/corner journey remains pending</li></ul><h4 id="e2e-168-expanded-sidebar-width-follows-an-anchored-resize-gesture" tabindex="-1">E2E-168: Expanded sidebar width follows an anchored resize gesture <a class="header-anchor" href="#e2e-168-expanded-sidebar-width-follows-an-anchored-resize-gesture" aria-label="Permalink to &quot;E2E-168: Expanded sidebar width follows an anchored resize gesture&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: PI-Desktop is open in the chat shell with the sidebar expanded and a retained project/session visible.</li><li><strong>Steps</strong>: <ol><li>Drag the sidebar&#39;s right-edge handle from its default width toward both directions, including a slow drag with a brief pause, then release.</li><li>Confirm the main pane reflows continuously and the sidebar does not jump when the pointer is pressed.</li><li>With the work panel open or on a small supported window, continue the resize toward the maximum; inspect the composer toolbar while the main pane reflows.</li><li>Repeat with a target below 240px and above the live maximum; release and confirm the sidebar width stops at 240px and the live cap (at most 520px) while MainChat stays at or above 450px. Continue the drag below 160px and confirm the sidebar collapses immediately without saving the in-progress width.</li><li>Focus the edge handle and press ArrowLeft/ArrowRight, Home, and End; inspect the separator&#39;s current ARIA value. ArrowLeft at 240px must not collapse the sidebar.</li><li>Start a resize, press Escape or cancel the pointer, then restart the app. Collapse and re-expand the sidebar as a separate check; the preferred expanded width must return.</li><li>Drag the handle wide, double-click it, and confirm the width returns to the default 275px. Repeat after a minimum-width drag and on a squeezed window where the live cap is the binding bound.</li></ol></li><li><strong>Expected</strong>: The handle is discoverable on direct hover/focus without a full-height white/accent rail when the sidebar body is hovered, has no native window drag or text-selection side effect, and remains anchored to the press point. MainChat follows the live width until its 450px floor. Pointer release saves one clamped preferred width; Escape/cancellation restores the starting width without saving it. Double-clicking the handle restores the default 275px width, clamped to the live budget so the reset never breaches the 450px MainChat floor. A pointer width below 160px collapses the sidebar as a user action and restores the preferred expanded width on reopen. Keyboard changes commit immediately and expose localized width semantics. The saved width survives relaunch and is restored after sidebar collapse; collapse does not convert the preferred width into the icon-rail width. MainChat never falls below 450px, and the composer toolbar keeps its left and right groups on one row without squeezed buttons. Mode/permission labels remain single-line and ellipsized; no toolbar text is vertically split or overlapped.</li><li><strong>Specs linked</strong>: <code>04-ux/01-ui-ia.md</code>, <code>04-ux/07-ui-design-system.md</code>, <code>04-ux/08-component-spec.md</code>, <code>04-ux/09-interaction-patterns.md</code>, ADR 0141, ADR 0238, ADR 0290, D280, D408, D459</li><li><strong>Acceptance</strong>: A (app shell), F (persistence), Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Unit/source-contract covered (<code>sidebar-preferences.test.mjs</code>, <code>sidebar-resize.test.mjs</code>, <code>sidebar-resize-math.test.mjs</code>); rendered desktop drag and relaunch journey remains pending</li></ul><h4 id="e2e-162-a-vendor-account-and-an-ai-service-offer-the-same-model-picker" tabindex="-1">E2E-162: A vendor account and an AI service offer the same model picker <a class="header-anchor" href="#e2e-162-a-vendor-account-and-an-ai-service-offer-the-same-model-picker" aria-label="Permalink to &quot;E2E-162: A vendor account and an AI service offer the same model picker&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: One signed-in vendor (OAuth) account and one API-key AI service, both with discoverable models, and at least one of them exposing a reasoning-capable model.</li><li><strong>Steps</strong>: 1) Open Settings → Model configuration. 2) Edit the AI service, expand a chosen model&#39;s Advanced disclosure, and note its context window, max output and thinking chips; cancel. 3) Edit the vendor account and do the same on one of its chosen models. 4) Toggle a thinking level on a reasoning-capable account model and save. 5) Reopen the account editor and read that model&#39;s chips. 6) For an OpenAI Codex account, inspect <code>gpt-6-astra</code> (or another account model also published under models.dev&#39;s <code>openai</code> provider) and confirm its published context/output limits and reasoning levels are present. 7) In the account editor, hand-type a custom model ID the catalog does not publish, enable a thinking level on it, and save.</li><li><strong>Expected</strong>: Both dialogs render the same picker — the same discovered list, the same search, the same free-form custom-model entry, the same chosen pane, the same Advanced disclosure and the same chips — so the account editor is no longer missing the advanced controls. A level enabled on an account model persists and reappears when the editor is reopened, including a level the catalog does not publish. OpenAI Codex&#39;s <code>openai-codex</code> adapter key resolves the matching <code>openai</code> models.dev record, so <code>gpt-6-astra</code> is not shown with generic 128,000 / 8,192 / no-reasoning defaults. The authenticated ChatGPT list itself comes from the pinned pi-ai catalog (0.86.1 includes <code>gpt-6-astra</code>); models.dev cannot add a missing OAuth ID. A model with no published record keeps its explicit levels and starts with all choices available for manual opt-in. The account&#39;s default model stays the head binding.</li><li><strong>Specs linked</strong>: <code>04-ux/06-settings-ia.md</code>, <code>04-ux/08-component-spec.md</code> §19, <code>03-runtime/11-provider-model-system.md</code> §10, <code>08-meta/decisions-log.md</code> (D270 refines D237/D240)</li><li><strong>Acceptance</strong>: B (model config), Quality</li></ul><h4 id="e2e-163-advanced-model-settings-own-the-default-thinking-level-and-attachment-capabilities" tabindex="-1">E2E-163: Advanced model settings own the default thinking level and attachment capabilities <a class="header-anchor" href="#e2e-163-advanced-model-settings-own-the-default-thinking-level-and-attachment-capabilities" aria-label="Permalink to &quot;E2E-163: Advanced model settings own the default thinking level and attachment capabilities&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: One AI service with discoverable models, including a reasoning-capable model that publishes at least three thinking levels, a vision-capable model, and a model models.dev describes as text-only. The service must persist model-local <code>supportsImages</code> overrides.</li><li><strong>Steps</strong>: 1) Open Settings → Model configuration, edit the service and expand a reasoning-capable model&#39;s Advanced disclosure. 2) Enable at least three thinking levels and pick a default that is not the lowest enabled level, then save. 3) Reopen the editor and read the default selector. 4) Disable the level currently chosen as default and read the selector again. 5) Start a new session on that model and open the composer reasoning menu. 6) Back in Advanced, on the text-only model, turn Image input on, save, reopen and confirm the switch reports itself as overridden. 7) Open the Composer model menu and confirm that this model shows the vision badge. 8) On the published vision model, turn Image input off and confirm its Composer row no longer shows the vision badge. 9) Attach an image in a session on the text-only model. 10) Tick the same box back to the value models.dev publishes, save, and reopen. 11) Turn PDF input on for a model whose catalog entry omits it, save, and attach a PDF. 12) Configure a model, then point the service at an endpoint that no longer lists it, reopen the editor and read that model&#39;s capability boxes.</li><li><strong>Expected</strong>: The default thinking level is selectable among the levels the binding enables and nothing else; it persists across reopen and is the level the home draft chip and a newly persisted session start at, not the strongest enabled level. Disabling the chosen default moves it to a still-enabled level rather than leaving a level the runtime would clamp away, and the selector is absent when a binding enables one level or none. An answered Image input switch overrides the published capability in both directions and survives reopen. The Composer model menu shows the vision badge for the effective <code>true</code> override on the published text-only model, hides it for an explicit <code>false</code> override on the published vision model, and follows the published value when the override is reset to <code>null</code>. The text-only model transports the attached image as an image content block. Ticking a box back to the published value stores &quot;follow the catalog&quot; rather than an equal-valued override, so a later catalog correction still reaches the binding without any separate reset control. All seven canonical thinking chips remain available even when the catalog publishes no reasoning support, so an endpoint can be opted in explicitly. PDF input records the capability without changing transport: the PDF stays a bounded file reference the model reads with its file tools. Each capability is one checkbox with a short label and no per-row explanatory copy. The Advanced body is a compact sheet: the alias hint is a title tooltip, limit fields hide native spinners, thinking chips span the pane with the default selector on the label row, and attachment plus delegation checkboxes share one wrapping row. The first chosen row starts expanded. A configured model absent from live discovery still shows its published capabilities rather than reading as undescribed.</li><li><strong>Specs linked</strong>: <code>03-runtime/11-provider-model-system.md</code> §6.2, <code>03-runtime/12-provider-config-schema.md</code>, <code>03-runtime/13-model-catalog-and-selection.md</code> §11.2, <code>04-ux/08-component-spec.md</code> §11.7–11.8, <code>04-ux/06-settings-ia.md</code>, ADR 0218</li><li><strong>Acceptance</strong>: B (model config), Quality</li></ul><h4 id="e2e-164-context-compaction-preserves-the-active-task-boundary" tabindex="-1">E2E-164: Context compaction preserves the active task boundary <a class="header-anchor" href="#e2e-164-context-compaction-preserves-the-active-task-boundary" aria-label="Permalink to &quot;E2E-164: Context compaction preserves the active task boundary&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A provider fixture can complete multiple sequential tasks, trigger an automatic checkpoint at a terminal boundary, trigger an active-turn checkpoint during a tool loop, and restart a session.</li><li><strong>Steps</strong>: <ol><li>Complete task A and task B in one session with distinct instructions and visible completion replies.</li><li>Trigger a checkpoint after a completed turn, then send task C and capture the next provider request context.</li><li>Trigger compaction while task D still has tool results or <code>toolUse</code> pending, and capture the next provider request.</li><li>Restart and reopen the session, then send another prompt.</li></ol></li><li><strong>Expected</strong>: A completed-turn checkpoint has an empty retained tail; the next request contains its summary plus task C and no bare A/B prompts. An active checkpoint retains exactly the latest active user prompt, with no older user prompts or pre-boundary assistant/tool messages. Restart honors <code>retainedTailMode</code>, and legacy multi-user tails normalize to the latest user message. If automatic summary generation fails, the fallback retains a bounded recent user tail even after a completed turn, and a later retry removes only the synthetic recovery notice while preserving any carried summary. The visible transcript remains complete and checkpoint rows remain.</li><li><strong>Specs linked</strong>: <code>03-runtime/02-agent-runtime.md</code>, <code>03-runtime/04-data-storage.md</code>, <code>03-runtime/16-tool-result-limits.md</code>, <code>08-meta/decisions-log.md</code> (D275), ADR 0136</li><li><strong>Acceptance</strong>: C (chat/stream), F (persistence), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>packages/agent-runtime/src/runtime.test.ts</code>, <code>context-compaction.test.mjs</code>); provider/UI journey Draft</li></ul><h4 id="e2e-165-a2a-and-peer-tools-are-withdrawn" tabindex="-1">E2E-165: A2A and Peer tools are withdrawn <a class="header-anchor" href="#e2e-165-a2a-and-peer-tools-are-withdrawn" aria-label="Permalink to &quot;E2E-165: A2A and Peer tools are withdrawn&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Agent mode; protocol v11 host. A user subagent definition lists <code>A2A</code> (or <code>Peer</code>) among its tools. Two Agent-mode sessions are open.</li><li><strong>Steps</strong>: 1) Handshake and inspect host capabilities. 2) Inspect the parent Agent tool list and <code>ToolSearch</code> results. 3) Load the definition that names <code>A2A</code>/<code>Peer</code>. 4) Start two concurrent working-tool delegates. 5) Ask whether one conversation can see the other.</li><li><strong>Expected</strong>: Handshake succeeds at protocol v11 and does not advertise <code>&quot;a2a&quot;</code>. <code>A2A</code> and <code>Peer</code> are absent from the parent catalog, deferred tools, and <code>ToolSearch</code>. The unknown tool names are dropped with a parse warning; remaining working tools still spawn. Concurrent delegates report only through <code>Task*</code> — there is no sibling or parent-to-parent channel. <code>a2a.*</code> RPC methods return method-not-found. Schema v14 databases have no <code>a2a_*</code> tables.</li><li><strong>Specs linked</strong>: <code>03-runtime/02-agent-runtime.md</code> §5f.2, <code>03-runtime/03-tools-and-permissions.md</code> §10.2, <code>03-runtime/06-host-rpc-protocol.md</code> §3–§4, <code>08-meta/decisions-log.md</code> (D326), ADR 0165</li><li><strong>Acceptance</strong>: E (tools &amp; permissions) + C (chat/stream) + Security</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Draft</li></ul><p>E2E-165b, E2E-165c, and E2E-165d (A2A push, cross-session A2A, parent A2A) are withdrawn with ADR 0165.</p><h4 id="e2e-166-subagent-model-selection" tabindex="-1">E2E-166: Subagent model selection <a class="header-anchor" href="#e2e-166-subagent-model-selection" aria-label="Permalink to &quot;E2E-166: Subagent model selection&quot;">​</a></h4><ul><li><p><strong>Preconditions</strong>: Agent mode; at least one provider has two configured model bindings; a builtin subagent definition is available.</p></li><li><p><strong>Steps</strong>: 1) Enable the delegation checkbox for one model binding, save the provider, reopen it, and confirm the checkbox remains enabled; restart the app, reopen the provider again, and confirm it is still enabled. 2) Start a session and inspect the parent agent&#39;s system prompt for the delegation model summary. 3) Delegate a Task with <code>model: &quot;provider/modelId&quot;</code> pointing to the enabled binding. 4) Delegate a Task with <code>model:</code> pointing to a binding that is not enabled for subagents. 5) Delegate a Task with <code>model:</code> pointing to a model that is not configured at all. 6) Delegate a Task with no <code>model:</code> parameter and a definition that has a frontmatter model pin. 7) Delegate a Task with no <code>model:</code> parameter and a definition that has no frontmatter model pin. 8) With no enabled delegation models, delegate a Task once with no <code>model:</code> and once with <code>model:</code> repeating the current session&#39;s <code>provider/modelId</code>. 9) Pin an unselected model to one definition; attempt to use it as another definition&#39;s explicit override, then invoke its owner with no override. 10) Remove a previously enabled override key and start another prompt in the same idle session.</p></li><li><p><strong>Expected</strong>:</p><ol><li>Saving and reopening the provider preserves the <code>availableForSubagents</code> opt-in, including after an application restart.</li><li>The delegation model summary appears in the parent&#39;s system prompt listing every successfully resolved model marked <code>availableForSubagents</code>, with no definition-only pins. The Task definition catalog displays each default model and recommends omitting <code>model</code> to preserve it.</li><li>The Task tool accepts the <code>model</code> parameter and the delegate runs on the specified model, not the session model; its delegation node shows the effective model id immediately after the subagent name.</li><li>If the model is not configured or not enabled for delegation, the Task returns a tool error listing available models.</li><li>Resolution priority is Task.model parameter → definition frontmatter pin → session model.</li><li>On-demand resolution succeeds for models enabled in provider settings via the <code>provider.resolveSubagentModel</code> RPC.</li><li>When no delegation model is configured, omitting <code>model:</code> and explicitly repeating the current session <code>provider/modelId</code> both start the delegate on the session model; the latter is not reported as an unavailable model.</li><li>A private pin remains usable by its definition when <code>model</code> is omitted or when <code>Task.model</code> repeats that definition&#39;s own pin key, but cannot be selected for another definition without opt-in. Rejection issues no child provider request. On-demand authorization does not retire an idle runtime; changed launch opt-in does, so a stale cached binding grants no selection rights.</li></ol></li><li><p><strong>Specs linked</strong>: <code>03-runtime/02-agent-runtime.md</code> §5f, <code>03-runtime/11-provider-model-system.md</code> §7, <code>03-runtime/12-provider-config-schema.md</code> §2, <code>08-meta/decisions-log.md</code> (D278)</p></li><li><p><strong>Acceptance</strong>: C (chat/stream) + B (model configuration) + E (tools)</p></li><li><p><strong>Milestone</strong>: M6+</p></li><li><p><strong>Status</strong>: Partially automated. <code>pnpm test:e2e:subagent-models</code> drives the built sidecar over real NDJSON and a local deterministic SSE model fixture: private cross-definition rejection, own-pin echo, normal pin use, allowed override priority, on-demand authorization without runtime rebuild, exact-session inheritance, and revocation across two prompts all pass. Runtime unit tests cover the same selection gates and the desktop launch test exercises independent opt-in, revocation, and accounts sharing a vendor alias; the wiring test checks unique on-demand matching. The settings checkbox UI/persistence journey and live external provider execution remain manual; this fixture does not claim a complete native UI journey.</p></li><li><p><strong>Authorization regression coverage (#841)</strong>: <code>pnpm test:e2e:subagent-models</code> grants a model on demand, reuses the runtime, then revokes it without changing the launch catalog. The next prompt must reauthorize and issue no child request. A transcript-restored resume with a colliding model id must use the session binding rather than another definition&#39;s private account. Runtime tests also cover own pins/fallbacks, opted-in bindings, visible fallback metadata, live-key reauthorization, and late RPC responses crossing parent turns.</p></li></ul><h4 id="e2e-170-shell-titlebars-use-borderless-chrome" tabindex="-1">E2E-170: Shell titlebars use borderless chrome <a class="header-anchor" href="#e2e-170-shell-titlebars-use-borderless-chrome" aria-label="Permalink to &quot;E2E-170: Shell titlebars use borderless chrome&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: PI-Desktop is open in chat, at least one destination page, and Settings on a supported light or dark theme. On Windows/Linux, renderer- drawn window controls are visible.</li><li><strong>Steps</strong>: 1) Inspect the top band on the chat, destination, and Settings surfaces. 2) Switch between light and dark themes and repeat. 3) On Windows/Linux, inspect the window-control band and its boundary with the adjacent surface. 4) Drag the titlebar and activate each window-control button.</li><li><strong>Expected</strong>: The shared 46px top band remains stable and draggable, but its lower edge has no visible border line in either theme or route. The Windows/Linux control band has no bottom line; only its existing faint side seam separates the controls from the adjacent surface. Focus rings, hover states, window actions, and content clearance remain unchanged.</li><li><strong>Specs linked</strong>: <code>04-ux/01-ui-ia.md</code>, <code>04-ux/07-ui-design-system.md</code>, <code>04-ux/08-component-spec.md</code></li><li><strong>Acceptance</strong>: A (app shell), Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Unit/source-contract covered (<code>topbar-consistency.test.mjs</code>, <code>settings-drag-region.test.mjs</code>, <code>window-menu.test.mjs</code>); rendered light/dark desktop journey remains pending</li></ul><h4 id="e2e-172-mid-turn-thinking-pick-does-not-collapse-an-unpinned-session-menu" tabindex="-1">E2E-172: Mid-turn thinking pick does not collapse an unpinned session menu <a class="header-anchor" href="#e2e-172-mid-turn-thinking-pick-does-not-collapse-an-unpinned-session-menu" aria-label="Permalink to &quot;E2E-172: Mid-turn thinking pick does not collapse an unpinned session menu&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: The app default is a reasoning-capable custom provider/model whose binding publishes a sparse set such as <code>low</code>/<code>high</code>/<code>max</code>. A session exists on the default create path (<code>provider_id</code>/<code>model_id</code> NULL).</li><li><strong>Steps</strong>: 1) Send a prompt so the session is running. 2) Open the Composer model × reasoning menu and select a different enabled thinking level. 3) Reopen the thinking submenu without waiting for the turn to finish. 4) Switch to another session and back. 5) Let the turn finish.</li><li><strong>Expected</strong>: The chip shows the selected level (not Off). The submenu still lists every enabled binding level. Switching sessions does not collapse the menu. After <code>agent_end</code>, the queued configuration is durable. Re-saving a provider is not required to recover the menu.</li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code>, <code>03-runtime/13-model-catalog-and-selection.md</code>, <code>04-ux/08-component-spec.md</code> §11</li><li><strong>Acceptance</strong>: B (model config), C (chat and stream), Quality</li><li><strong>Milestone</strong>: M6</li><li><strong>Status</strong>: Unit-covered (<code>session-thinking.test.mjs</code>, <code>thinking-ui.test.mjs</code>, <code>composer-send-state.test.mjs</code>); full UI scenario Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-174-binding-default-thinking-level-seeds-drafts-and-new-sessions" tabindex="-1">E2E-174: Binding default thinking level seeds drafts and new sessions <a class="header-anchor" href="#e2e-174-binding-default-thinking-level-seeds-drafts-and-new-sessions" aria-label="Permalink to &quot;E2E-174: Binding default thinking level seeds drafts and new sessions&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: One AI service with a reasoning model whose published levels omit <code>off</code> (for example <code>low</code> / <code>high</code> / <code>max</code>) and whose Advanced default thinking level is a non-max enabled level such as <code>low</code>.</li><li><strong>Steps</strong>: 1) Open the home composer with no active session and read the model × reasoning chip. 2) Create a new task without opening the reasoning menu, then read the chip and the session&#39;s stored <code>thinkingLevel</code>. 3) On the home draft, switch to that model from the model menu and read the chip before sending. 4) Change the binding default to another enabled level, save, and repeat steps 1–2 on a fresh draft.</li><li><strong>Expected</strong>: The home draft chip, a draft model switch, and the newly persisted session all start at the binding&#39;s stored default, not the strongest published or enabled level. Changing the default in Settings changes the next draft and new session and does not rewrite existing sessions.</li><li><strong>Specs linked</strong>: <code>03-runtime/11-provider-model-system.md</code> §6.2, <code>03-runtime/13-model-catalog-and-selection.md</code> §4, <code>04-ux/08-component-spec.md</code> §11.4 / §11.5, <code>08-meta/decisions-log.md</code> (D303)</li><li><strong>Acceptance</strong>: B (model config), C (chat/stream), Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Unit/source-contract covered (<code>thinking-levels.test.ts</code>, <code>thinking-ui.test.mjs</code>); rendered desktop journey remains pending</li></ul><h4 id="e2e-175-a-paged-read-is-not-shown-as-truncated" tabindex="-1">E2E-175: A paged Read is not shown as truncated <a class="header-anchor" href="#e2e-175-a-paged-read-is-not-shown-as-truncated" aria-label="Permalink to &quot;E2E-175: A paged Read is not shown as truncated&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A project-bound Agent session; the workspace contains a text file of at least 3000 lines whose lines are shorter than 16,384 characters, plus a fixture whose first line exceeds that cap.</li><li><strong>Steps</strong>: <ol><li><code>Read</code> the long file with no <code>offset</code>/<code>limit</code>.</li><li><code>Read</code> the same file with <code>offset</code> equal to the reported next offset and a modest <code>limit</code>.</li><li><code>Read</code> the over-long-line fixture.</li><li>Grep a token that matches more than the default <code>headLimit</code>.</li></ol></li><li><strong>Expected</strong>: <ul><li>Step 1 returns the default 2000-line window, <code>truncated: false</code>, no truncated chip, <code>totalLines</code> of the whole file, and a <code>notice</code> naming the next offset. It does not tell the model to Grep.</li><li>Step 2 continues without overlap and stays <code>truncated: false</code>.</li><li>Step 3 sets <code>truncated: true</code>, counts the clipped line in <code>notice</code>, and shows the truncated chip.</li><li>Step 4 sets <code>truncated: true</code> because remaining matches exist, and shows the chip.</li></ul></li><li><strong>Specs linked</strong>: <code>03-runtime/16-tool-result-limits.md</code> §2 / §5, <code>04-ux/08-component-spec.md</code> §9.2, <code>08-meta/decisions-log.md</code> (D306)</li><li><strong>Acceptance</strong>: C (chat &amp; stream), E (tools &amp; permissions)</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (host-core <code>tools</code> tests)</li></ul><h4 id="e2e-176-settings-info-opens-a-prefilled-github-bug-form" tabindex="-1">E2E-176: Settings Info opens a prefilled GitHub bug form <a class="header-anchor" href="#e2e-176-settings-info-opens-a-prefilled-github-bug-form" aria-label="Permalink to &quot;E2E-176: Settings Info opens a prefilled GitHub bug form&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Settings can be opened; the machine can launch a system browser.</li><li><strong>Steps</strong>: 1) Open Settings → Info. 2) Confirm the Application row shows the current app version. 3) Search settings for the Report a problem label. 4) Activate Open GitHub.</li><li><strong>Expected</strong>: The row is indexed by Settings search and stays on Info. The action calls <code>pi-desktop/app/openFeedback</code> with no URL from the renderer. Main opens <code>https://github.com/vastsa/PI-Desktop/issues/new</code> with <code>template=bug_report.yml</code> and prefills <code>app-version</code>, <code>os</code>, and <code>environment</code>. The GitHub bug form still requires description, reproduction steps, expected, actual, version, and OS; blank issues remain disabled.</li><li><strong>Specs linked</strong>: <code>04-ux/06-settings-ia.md</code>, <code>03-runtime/01-ipc-protocol.md</code>, <code>06-delivery/03-ai-development-workflow.md</code>, <code>08-meta/decisions-log.md</code> (D313), ADR 0157</li><li><strong>Acceptance</strong>: H (diagnostics), Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Unit/source-contract covered (<code>github-feedback.test.ts</code>, <code>feedback.test.mjs</code>); rendered desktop journey remains pending (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-177-switching-a-long-running-session-keeps-the-newest-prompt-in-view" tabindex="-1">E2E-177: Switching a long running session keeps the newest prompt in view <a class="header-anchor" href="#e2e-177-switching-a-long-running-session-keeps-the-newest-prompt-in-view" aria-label="Permalink to &quot;E2E-177: Switching a long running session keeps the newest prompt in view&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A session has stayed open long enough that the renderer holds more than the newest-100 durable page (hundreds of user/assistant/tool rows); a second session exists so a switch is possible.</li><li><strong>Steps</strong>: 1) Interrupt an in-flight reply if needed, then send a new prompt. 2) While that turn is still running, switch to the other session and back. 3) Confirm the newest user row (and any streaming tail) is at the bottom of the transcript and the session still shows as running. 4) Optional: Stop, switch away and back; the same newest rows remain in chronological order.</li><li><strong>Expected</strong>: Revalidation does not append older live history after the bounded durable page. The mounted trailing window still shows the just-sent prompt and the live tail. The turn continues in the background across the switch. Stop is not required to make the prompt visible again.</li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code> §1.6 / §3.5, <code>04-ux/09-interaction-patterns.md</code> (session isolation), ADR 0120, ADR 0137, <code>08-meta/decisions-log.md</code> (D261, D317)</li><li><strong>Acceptance</strong>: C (conversation &amp; stream), F (persistence), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>session-transcript.test.mjs</code> D317 cases); full desktop journey Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-183-switching-an-idle-session-keeps-a-completed-reply-that-is-not-on-disk-yet" tabindex="-1">E2E-183: Switching an idle session keeps a completed reply that is not on disk yet <a class="header-anchor" href="#e2e-183-switching-an-idle-session-keeps-a-completed-reply-that-is-not-on-disk-yet" aria-label="Permalink to &quot;E2E-183: Switching an idle session keeps a completed reply that is not on disk yet&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Two conversations exist. The source has a completed user prompt and an assistant reply still on screen. The durable <code>session.get</code> page for that session still has only the user row (the persistence outbox has not flushed the assistant line).</li><li><strong>Steps</strong>: 1) Wait until the turn is no longer running. 2) Switch to the other session and back. 3) Confirm the assistant reply is still visible. 4) Optional: wait until the outbox drains, switch away and back again; the reply remains and now also exists in the JSONL.</li><li><strong>Expected</strong>: Idle revalidation stitches the live snapshot onto the durable page. A completed live-only assistant/tool row is not replaced by the user-only durable page. Live provenance is not cleared until that page contains every live id.</li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code> §1.6 / §3.5, <code>04-ux/09-interaction-patterns.md</code> (session isolation), ADR 0137, <code>08-meta/decisions-log.md</code> (D317, D324)</li><li><strong>Acceptance</strong>: C (conversation &amp; stream), F (persistence), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>session-transcript.test.mjs</code> D324 case, <code>session-switch-performance.test.mjs</code>); full desktop journey Draft (do not run E2E locally unless explicitly requested)</li></ul><h4 id="e2e-184-completed-ai-replies-survive-closing-and-reopening-the-app" tabindex="-1">E2E-184: Completed AI replies survive closing and reopening the app <a class="header-anchor" href="#e2e-184-completed-ai-replies-survive-closing-and-reopening-the-app" aria-label="Permalink to &quot;E2E-184: Completed AI replies survive closing and reopening the app&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A session with at least one finished user prompt and assistant reply. The reply may still be in the persistence outbox or only in <code>sessions/&lt;id&gt;.inflight.json</code> when the process exits.</li><li><strong>Steps</strong>: 1) Send a prompt and wait until the assistant reply is complete and the session is idle. 2) Quit the app (window close / tray Quit) and relaunch. 3) Open the same session. 4) Repeat with a hard kill of the process immediately after the reply appears on screen. 5) Repeat with several completed turns, then quit and reopen.</li><li><strong>Expected</strong>: Every user prompt and every completed assistant reply is visible after relaunch. No session shows user rows with empty gaps where the answers were. A reply recovered from a leftover checkpoint of a <code>completed</code> turn is <code>complete</code>, not <code>aborted</code>. Tool rows that had reached <code>tool_end</code> are also present. Earlier turns already on disk are unchanged.</li><li><strong>Specs linked</strong>: <code>03-runtime/04-data-storage.md</code>, <code>03-runtime/07-process-model.md</code>, <code>03-runtime/10-session-state-machine.md</code>, ADR 0041, ADR 0153, <code>08-meta/decisions-log.md</code> (D327)</li><li><strong>Acceptance</strong>: C (conversation &amp; stream), F (persistence)</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>sessions.rs</code> D327 inflight tests, <code>persistence-outbox.test.mjs</code>, <code>inflight-checkpoint.test.mjs</code>); protocol reproduction in the issue-42 host+outbox harness; full desktop journey Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-178-a-missing-sessions-row-is-restored-so-the-outbox-can-drain" tabindex="-1">E2E-178: A missing sessions row is restored so the outbox can drain <a class="header-anchor" href="#e2e-178-a-missing-sessions-row-is-restored-so-the-outbox-can-drain" aria-label="Permalink to &quot;E2E-178: A missing sessions row is restored so the outbox can drain&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A session has a live <code>sessions/&lt;id&gt;.jsonl</code> and queued turns in <code>session-message-outbox.json</code>, but its row is gone from <code>pi.sqlite</code> <code>sessions</code> (WAL/index loss).</li><li><strong>Steps</strong>: 1) Confirm the sidebar no longer lists the session and <code>session.appendMessage</code> would fail <code>session not found</code>. 2) Restart the app (or otherwise complete a host handshake that flushes the outbox). 3) Optional: delete the session and confirm its outbox entries are dropped rather than resurrected.</li><li><strong>Expected</strong>: Host boot reinserts the sessions row from the JSONL and rebuilds the search index. The outbox drains without pausing at the head. The conversation returns to the sidebar with its messages. A user-deleted session is not recreated from leftover outbox entries.</li><li><strong>Specs linked</strong>: <code>03-runtime/04-data-storage.md</code>, <code>03-runtime/06-host-rpc-protocol.md</code>, <code>03-runtime/07-process-model.md</code>, ADR 0041, <code>08-meta/decisions-log.md</code> (D318)</li><li><strong>Acceptance</strong>: C (conversation &amp; stream), F (persistence)</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (host-core orphaned-session restore tests, <code>persistence-outbox.test.mjs</code>); full desktop journey Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-179-parent-tools-after-a-task-fan-out-stay-outside-the-delegation-card" tabindex="-1">E2E-179: Parent tools after a Task fan-out stay outside the delegation card <a class="header-anchor" href="#e2e-179-parent-tools-after-a-task-fan-out-stay-outside-the-delegation-card" aria-label="Permalink to &quot;E2E-179: Parent tools after a Task fan-out stay outside the delegation card&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A project-bound Agent session whose provider stream can emit two <code>Task</code> calls in one assistant message and then keep working — think, <code>Read</code>, <code>Grep</code> — before a <code>TaskWait</code>.</li><li><strong>Steps</strong>: 1) Prompt a turn that fans out two delegates, then continues with parent thinking and workspace reads while at least one delegate is still running. 2) Inspect the expanded delegation card and the rows below it. 3) Let the delegates settle and inspect elapsed time on the card versus the parent processing group. 4) Reload the session and re-expand the card.</li><li><strong>Expected</strong>: The delegation card contains only the main-agent root and the two <code>Task</code> nodes. Parent thinking, <code>Read</code>, <code>Grep</code>, and <code>TaskWait</code> render in a separate processing group, not flush against the subagent tile and not under a “Subagent working” header. The card keeps inset from its tile edge. While a delegate is still running the card stays labelled working, remains open, and ticks elapsed from that fan-out&#39;s own timestamps even after the parent has moved on. Reload preserves the same split.</li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code> §9.9, ADR 0062, decisions-log D265, D319</li><li><strong>Acceptance</strong>: C (conversation), Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Unit-covered (<code>assistant-turns.test.mjs</code>, <code>subagent-topology.test.mjs</code>, <code>subagent-transcript.test.mjs</code>); desktop journey pending (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-180-sent-file-references-stay-chips-and-open-on-click" tabindex="-1">E2E-180: Sent file references stay chips and open on click <a class="header-anchor" href="#e2e-180-sent-file-references-stay-chips-and-open-on-click" aria-label="Permalink to &quot;E2E-180: Sent file references stay chips and open on click&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: An Agent session in a workspace that contains a nested source file, an HTML file, and a file whose name contains whitespace, and a project group whose second folder holds a source file of its own. The composer can also paste an OS file into session scratch. The bundled File Manager plugin is loaded.</li><li><strong>Steps</strong>: 1) Attach a workspace source file, a workspace HTML file, a whitespace-named file, and a pasted scratch file via composer chips, then send. 2) Inspect the user bubble. 3) Click the HTML chip, then the workspace source chip, then the scratch chip. 4) Attach the file that lives in the project&#39;s second folder and click its chip.</li><li><strong>Expected</strong>: <ul><li>Each sent reference renders as a compact leaf-name chip (icon + name), not as a full <code>@path</code>. The tooltip and accessible name keep the canonical path. Quoted and scratch-absolute paths are included.</li><li>A chip plus a short prompt keeps the user plate content-sized; it does not stretch to the <code>min(82%, 600px)</code> ceiling.</li><li>Clicking the HTML chip opens the work-panel browser on that file.</li><li>Clicking the workspace source chip opens it in the File Manager work-panel view on that file; the host <code>file:</code> tab and the OS default application are no longer what a chip click opens.</li><li>Clicking the scratch chip opens the host <code>file:</code> tab on that file&#39;s absolute path, because it lives outside the File Manager&#39;s project root.</li><li>Clicking the chip of the file in the project&#39;s second folder opens it in the File Manager view on that file: completion searches the whole project group, primary folder first, and a sibling-folder file is addressed by absolute path because a relative path always means the primary folder (ADR 0263).</li><li>The persisted user message still contains the canonical <code>@path</code> text for the agent.</li></ul></li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code> §8.3 / §11.8, <code>04-ux/09-interaction-patterns.md</code> §8a.2, <code>03-runtime/01-ipc-protocol.md</code>, ADR 0163, ADR 0241, ADR 0262, ADR 0263, <code>08-meta/decisions-log.md</code> (D320)</li><li><strong>Acceptance</strong>: C (conversation &amp; stream), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>chat-links.test.mjs</code>, <code>transcript-file-chips.test.mjs</code>, <code>fs-panel-guard.test.mjs</code>, <code>transcript-style.test.mjs</code>); full UI journey Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-chat-shorthand-file-ref-opens-the-matching-file" tabindex="-1">E2E-CHAT-shorthand-file-ref-opens-the-matching-file <a class="header-anchor" href="#e2e-chat-shorthand-file-ref-opens-the-matching-file" aria-label="Permalink to &quot;E2E-CHAT-shorthand-file-ref-opens-the-matching-file&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A project group whose primary folder holds <code>img/openimage.js</code>, <code>src/dir/a.ts</code>, and a deeper second <code>dir/a.ts</code> such as <code>packages/app/dir/a.ts</code>, and whose second folder holds <code>lib/only-here.ts</code>. The open session&#39;s scratch store holds a file with the same leaf name <code>openimage.js</code> and one file no project folder has; the attachment store holds an <code>attachments/&lt;sha256&gt;</code> blob. The transcript renders assistant markdown.</li><li><strong>Steps</strong>: 1) Prompt a turn whose reply names <code>openimage.js</code> as inline code and click it. 2) Prompt a turn that names <code>dir/a.ts</code> and click it. 3) Prompt a turn that names the scratch-only file, click it, then do the same for the <code>attachments/&lt;sha256&gt;</code> ref. 4) Prompt a turn that names an absolute path to a project file whose leaf name also exists in scratch, and click it. 5) Prompt a turn that names <code>missing-helper.js</code> and click it. 6) Prompt a turn that names <code>only-here.ts</code> and click it.</li><li><strong>Expected</strong>: <ul><li>Clicking <code>openimage.js</code> opens the project&#39;s <code>img/openimage.js</code>, although the scratch store holds a file with that same leaf name: the project is searched to exhaustion before the scratch store is considered at all.</li><li><code>dir/a.ts</code> opens <code>src/dir/a.ts</code>: an exact path beats a shorthand, a longer matching tail beats a bare leaf name, and the shallowest candidate wins a tail-length tie, so the deeper <code>packages/app/dir/a.ts</code> is never the one that opens.</li><li>A reference the project cannot answer resolves in the session scratch store; one that neither answers resolves in the attachment store, and an <code>attachments/&lt;sha256&gt;</code> ref opens that stored blob.</li><li>An absolute reference that names a real file inside a known root wins outright, whichever shorthand would otherwise have matched.</li><li><code>only-here.ts</code> opens the second folder&#39;s <code>lib/only-here.ts</code>: the primary folder is searched first and to exhaustion, then the project group&#39;s other folders in the group&#39;s own order, and the match names the folder that answered (ADR 0263).</li><li>A file that answered from a sibling folder is addressed to the work panel by its absolute path, while a primary-folder file stays project-relative (ADR 0263).</li><li>A reference that matches nothing raises an error toast reading <code>No file matches missing-helper.js</code> and opens nothing: no new work-panel tab, no empty panel, no blank side-browser page, and the work panel and transcript keep the content they already had.</li></ul></li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code> § fs, <code>04-ux/09-interaction-patterns.md</code> §8a.2, ADR 0124, ADR 0163, ADR 0249, ADR 0262, ADR 0263</li><li><strong>Acceptance</strong>: C (conversation &amp; stream), D (workspace), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>apps/desktop/test/chat-ref-resolve.test.mjs</code>); full UI journey Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-chat-file-ref-opens-the-surface-that-owns-it" tabindex="-1">E2E-CHAT-file-ref-opens-the-surface-that-owns-it <a class="header-anchor" href="#e2e-chat-file-ref-opens-the-surface-that-owns-it" aria-label="Permalink to &quot;E2E-CHAT-file-ref-opens-the-surface-that-owns-it&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: The bundled File Manager plugin is loaded and enabled and the work panel is closed. The open session&#39;s scratch store holds one file and the attachment store one blob. The project is a group whose primary folder contains an <code>.html</code> page and a text file, and whose second folder holds a text file of its own. The same session&#39;s transcript carries a <code>Read</code> row whose summary names a project file, a <code>Glob</code> result listing project paths, and a <code>Grep</code> result grouping its hits by file.</li><li><strong>Steps</strong>: 1) Click a project file reference in an assistant reply. 2) Type an unsaved edit into that view and click the same reference again. 3) Click a reference that resolves into the session scratch store, then the <code>attachments/&lt;sha256&gt;</code> reference. 4) Click a workspace <code>.html</code> reference in an assistant reply and the same reference as a sent user chip. 5) Click the file path in the tool row&#39;s summary, then a path in the <code>Glob</code> result&#39;s file list and a path heading of the <code>Grep</code> result. 6) Disable the File Manager plugin, click a project file reference and the tool row summary again, then re-enable it and click both once more. 7) Click a reference that resolves in the project&#39;s second folder, then one that resolves in its primary folder.</li><li><strong>Expected</strong>: <ul><li>A project file opens in the File Manager work-panel view on that file, with its ancestor folders expanded and the file selected; no host <code>file:</code> tab is added for it.</li><li>Clicking the same reference again does not reload the view: the unsaved edit is still in the editor and no second tab appears.</li><li>A scratch or attachment file opens in the host <code>file:</code> tab under Open resources, addressed by its absolute path, never in the File Manager view.</li><li>A <code>.html</code> / <code>.htm</code> page of the project&#39;s primary folder opens in the work-panel side browser, from the assistant reply and the user chip alike; a page in a sibling folder is a project file like any other and opens in the File Manager view, because the side browser is rooted at the primary folder (ADR 0263).</li><li>A tool surface reaches the destination of the file it names, not one of its own: the tool row&#39;s summary path and the paths of the <code>Glob</code> file list and the <code>Grep</code> path headings open the same project file in the File Manager view. A summary path that is a link opens the file without expanding the row, and only a summary without a resolvable target falls through to the row&#39;s own disclosure.</li><li>A reference that resolved in the project&#39;s second folder opens in the File Manager view on that file, reached by its absolute path, with no host <code>file:</code> tab; the reference from the primary folder opens in that same view addressed project-relative (ADR 0263).</li><li>With the plugin disabled, a project file reference — from the reply and from a tool row or result list alike — falls back to the host <code>file:</code> tab, the surface those clicks used before, which now also reaches the project&#39;s other folders; re-enabling the plugin restores the File Manager destination.</li></ul></li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code> §8.3, §9.6, <code>04-ux/09-interaction-patterns.md</code> §8a.2, ADR 0104, ADR 0163, ADR 0241, ADR 0249, ADR 0262, ADR 0263</li><li><strong>Acceptance</strong>: C (conversation &amp; stream), G (plugins), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>apps/desktop/test/transcript-file-chips.test.mjs</code> for the wiring and <code>apps/desktop/test/tool-row-file-refs.test.mjs</code> for the work-panel entry each shape of resolution produces); full UI journey Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-181-an-imported-skill-is-listed-in-the-next-session-catalog" tabindex="-1">E2E-181: An imported skill is listed in the next session catalog <a class="header-anchor" href="#e2e-181-an-imported-skill-is-listed-in-the-next-session-catalog" aria-label="Permalink to &quot;E2E-181: An imported skill is listed in the next session catalog&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Settings &gt; Agent &gt; Skills is open. A conventional <code>&lt;skill&gt;/SKILL.md</code> document has a non-ASCII frontmatter name and a folded YAML description. An empty Agent session is available on the same project the skill will be imported into.</li><li><strong>Steps</strong>: <ol><li>Import the <code>SKILL.md</code> into Global, then into the selected project.</li><li>Confirm the Skills page shows the display name, the ASCII id (directory name, not <code>skill</code>), and the flattened description.</li><li>Start a new Agent session on that project and ask the agent to use the skill by display name.</li><li>Repeat with a second directory skill that also lacks an ASCII name, and with a skill whose description is a <code>|</code> block.</li></ol></li><li><strong>Expected</strong>: <ul><li>Import succeeds. The catalog lists both skills with distinct ids.</li><li>The next session&#39;s system prompt includes each skill&#39;s id, name, and flattened description. The <code>Skill</code> tool loads the body by that id.</li><li>A misspelled <code>Skill</code> id lists user skill ids among the available skills, not only plugin ids.</li><li>Neither document is dropped because its title is non-ASCII or because both files are named <code>SKILL.md</code>.</li></ul></li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code> §12b, <code>07-plugins/01-plugin-system.md</code> §12.3, <code>08-meta/decisions-log.md</code> (D174, D194)</li><li><strong>Acceptance</strong>: E (tools &amp; permissions), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (host-core <code>user_skills</code> / <code>agent_capabilities</code> tests, <code>apps/desktop/test/plugin-skills.test.mjs</code>); full UI journey Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-182-relative-file-paths-in-chat-and-markdown-preview-open" tabindex="-1">E2E-182: Relative file paths in chat and markdown preview open <a class="header-anchor" href="#e2e-182-relative-file-paths-in-chat-and-markdown-preview-open" aria-label="Permalink to &quot;E2E-182: Relative file paths in chat and markdown preview open&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: An Agent session in a workspace that contains <code>apps/desktop/src/App.tsx</code>, <code>docs/adr/0163-transcript-file-reference-chips.md</code>, <code>docs/spec/00-baseline.md</code>, and a Unicode-named file such as <code>报告.pdf</code>.</li><li><strong>Steps</strong>: 1) Open an existing session whose transcript already contains assistant markdown. 2) Prompt a turn whose assistant reply mentions <code>apps/desktop/src/App.tsx</code> as a bare path, as inline code, a Unicode path such as <code>报告.pdf</code>, and as a markdown link. 3) Click each. 4) Open the ADR markdown file in the work-panel files viewer and click a <code>../spec/00-baseline.md</code> link. 5) Include an absolute path under the workspace, an outside absolute path, and a <code>~/</code> path in chat; confirm only the under-root path becomes a target. 6) Send a user message <code>使用llama.cpp，给我迁移步骤，只读。</code>, then a user message that names the real <code>apps/desktop/src/App.tsx</code> as a bare path and as <code>@apps/desktop/src/App.tsx</code>.</li><li><strong>Expected</strong>: <ul><li>Opening the session paints the transcript without throwing.</li><li>Each chat path opens <code>apps/desktop/src/App.tsx</code> in the File Manager work-panel view — the file view a chat click prefers — not a host <code>file:</code> tab.</li><li>Unicode filenames and multi-segment paths inside the workspace become targets, while an outside absolute path and a <code>~/</code> path stay plain text.</li><li>An absolute path under the workspace resolves to its workspace-relative target.</li><li>The markdown-file <code>../</code> link opens <code>docs/spec/00-baseline.md</code>, not a workspace-root <code>spec/00-baseline.md</code>.</li><li>A <code>../../../outside.ts</code> link from <code>docs/adr</code> stays inert.</li><li>The user sentence containing <code>使用llama.cpp</code> stays exact original text, not a file chip, when that path does not exist.</li><li>A user-message bare path becomes a chip only after <code>fs/resolveRef</code> confirms a real file; an explicit <code>@path</code> chips immediately.</li></ul></li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code> §8.3, <code>08-meta/decisions-log.md</code> (D322)</li><li><strong>Acceptance</strong>: C (conversation &amp; stream), D (workspace), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>chat-links.test.mjs</code>, <code>verified-chat-files.test.mjs</code>, <code>transcript-file-chips.test.mjs</code>, <code>markdown-prose-style.test.mjs</code>); full UI journey Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-185-external-url-opens-stay-on-http-s-and-mailto" tabindex="-1">E2E-185: External URL opens stay on http(s) and mailto <a class="header-anchor" href="#e2e-185-external-url-opens-stay-on-http-s-and-mailto" aria-label="Permalink to &quot;E2E-185: External URL opens stay on http(s) and mailto&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A chat transcript can render markdown links. A plugin is granted <code>shell.openExternal</code>. The work-panel preview can load an http page and a workspace HTML file.</li><li><strong>Steps</strong>: 1) Modified-click https, mailto, <code>file:</code>, <code>javascript:</code>, <code>ms-msdt:</code>, and a custom-scheme markdown link (<code>target=&quot;_blank&quot;</code>). 2) From the plugin, call <code>pi.shell.openExternal</code> with https, mailto, and <code>file:</code>. 3) In the embedded preview, <code>window.open</code> an https URL and a <code>file:</code> URL; use Open in browser on the http page and on the workspace HTML file.</li><li><strong>Expected</strong>: <ul><li>https and mailto open in the OS handler. <code>file:</code>, <code>javascript:</code>, <code>data:</code>, <code>ms-msdt:</code>, and custom schemes do not.</li><li>Plugin <code>file:</code> fails with <code>INVALID_ARGUMENT</code>; mailto succeeds.</li><li>Preview <code>window.open</code> of a non-allowlisted scheme is denied in-app and does not call <code>openExternal</code>.</li><li>Open in browser for http(s) uses <code>openExternal</code>; for an in-root file preview it uses <code>openPath</code>, not a <code>file:</code> URL through <code>openExternal</code>.</li></ul></li><li><strong>Specs linked</strong>: <code>05-security/01-security.md</code>, <code>07-plugins/04-plugin-security.md</code> §8, <code>07-plugins/03-plugin-api.md</code>, ADR 0109, ADR 0168, <code>08-meta/decisions-log.md</code> (D330)</li><li><strong>Acceptance</strong>: Security</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>safe-open-external.test.mjs</code>, <code>feedback.test.mjs</code>); full UI journey Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-186-token-usage-dashboard-is-plugin-owned-host-still-stores-turn-totals" tabindex="-1">E2E-186: Token usage dashboard is plugin-owned; host still stores turn totals <a class="header-anchor" href="#e2e-186-token-usage-dashboard-is-plugin-owned-host-still-stores-turn-totals" aria-label="Permalink to &quot;E2E-186: Token usage dashboard is plugin-owned; host still stores turn totals&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A profile with at least one completed Agent turn that reported provider usage after this build. Settings is reachable. Plugin <code>pi.token-insights</code> may be installed.</li><li><strong>Steps</strong>: 1) Complete a turn that also settled a subagent. 2) Open Settings. 3) Search settings for &quot;tokens&quot; / &quot;用量&quot;. 4) Open Token Insights from the command palette (<code>usage</code> / <code>用量</code>). 5) Confirm the transcript assistant chip.</li><li><strong>Expected</strong>: <ul><li>The rail has no Usage / 用量 destination. Preferences is General, AI, Shortcuts.</li><li>Settings search does not surface a usage tab.</li><li><code>stats.getTokenUsageHistory</code> still returns completed-turn totals that include subagent spend.</li><li>The assistant chip under the transcript still shows parent-only provider usage.</li><li>Token Insights is the heatmap / KPI dashboard. When the plugin is installed, PI-Desktop remainders from the host turns table appear there without rewriting <code>message.usage</code>.</li></ul></li><li><strong>Specs linked</strong>: <code>04-ux/06-settings-ia.md</code>, <code>03-runtime/01-ipc-protocol.md</code>, <code>03-runtime/06-host-rpc-protocol.md</code>, ADR 0171, ADR 0173, <code>08-meta/decisions-log.md</code> (D331, D335)</li><li><strong>Acceptance</strong>: F (persistence), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (agent-runtime usage split, host-core history aggregation, settings-search / i18n catalogs); plugin remainder merge covered in <code>pi-desktop-plugins</code>; full UI journey Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-187-history-attachments-and-local-markdown-images-render-inline" tabindex="-1">E2E-187: History attachments and local markdown images render inline <a class="header-anchor" href="#e2e-187-history-attachments-and-local-markdown-images-render-inline" aria-label="Permalink to &quot;E2E-187: History attachments and local markdown images render inline&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: An Agent session in a workspace that contains <code>docs/pixel.png</code>. The user has previously pasted an image so the session JSONL stores an <code>attachments/&lt;sha256&gt;</code> image ref with a stored mimeType.</li><li><strong>Steps</strong>: <ol><li>Reopen the session. Confirm the pasted image renders as a thumbnail, not only a file chip.</li><li>Click the thumbnail. Confirm the host files viewer opens on that attachment ref and shows the image.</li><li>Send a turn whose assistant markdown includes <code>![](docs/pixel.png)</code> and <code>![](/etc/passwd)</code>. Confirm the workspace image renders inline and the outside-root path does not load file bytes.</li><li>Confirm <code>fs/readImageDataUrl</code> with <code>ref: &quot;/etc/passwd&quot;</code> and <code>mimeType: &quot;image/png&quot;</code> returns <code>missing</code>, not a data URL.</li></ol></li><li><strong>Expected</strong>: <ul><li>Contained image refs display inline within the 5MB cap.</li><li>Outside-root paths and mime spoofing of non-image extensions stay closed.</li><li>Clicking a resolved thumbnail opens the host <code>file:</code> tab, not the OS handler.</li></ul></li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code>, <code>04-ux/08-component-spec.md</code> §8.3, ADR 0172, <code>08-meta/decisions-log.md</code> (D334)</li><li><strong>Acceptance</strong>: C (conversation &amp; stream), D (workspace), Security, Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>fs-panel-guard.test.mjs</code>, <code>message-image-display.test.mjs</code>); full UI journey Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-188-plugin-host-apis-list-models-read-in-flight-context-and-complete" tabindex="-1">E2E-188: Plugin host APIs list models, read in-flight context, and complete <a class="header-anchor" href="#e2e-188-plugin-host-apis-list-models-read-in-flight-context-and-complete" aria-label="Permalink to &quot;E2E-188: Plugin host APIs list models, read in-flight context, and complete&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: At least one authenticated provider; a development plugin granted <code>models.list</code>, <code>session.read</code>, and <code>agent.complete</code>; an Agent session with a prior user turn.</li><li><strong>Steps</strong>: <ol><li>From the plugin process call <code>pi.models.list()</code>. Confirm only ready models are returned and no secret fields appear.</li><li>Call <code>pi.session.getLlmContext()</code> outside a tool execution. Confirm <code>INVALID_ARGUMENT</code>.</li><li>Ask the Agent to call the plugin tool. Inside <code>execute</code>, call <code>getLlmContext()</code> then <code>agent.complete({ modelKey, includeSessionContext: true })</code>. Confirm the tool result contains reviewer text and usage, not a secret.</li><li>Repeat <code>agent.complete</code> until the eighth call in 60s succeeds and the ninth returns <code>RATE_LIMITED</code>.</li><li>Make the completion fail at the provider (for example a model whose credential the user has revoked). Confirm the plugin reads the classified code — <code>PROVIDER_UNAUTHORIZED</code> — rather than a generic failure.</li><li>Stop host-core and call <code>pi.models.list()</code>. Confirm <code>[]</code> and no warn line.</li><li>Stop host-core and call <code>pi.models.list()</code>. Confirm <code>[]</code> and no warn line.</li></ol></li><li><strong>Expected</strong>: Credentials never leave Electron main. Audit lines record plugin id, model key, sizes, and usage — not transcript or completion text. Plan still returns <code>PLUGIN_DISABLED_IN_PLAN</code> for the plugin tool. A dead host transport returns an empty model list without a warning (D080).</li><li><strong>Specs linked</strong>: <code>07-plugins/03-plugin-api.md</code>, <code>07-plugins/13-plugin-permissions-matrix.md</code>, ADR 0174, D336</li><li><strong>Acceptance</strong>: G (plugin agent tool), Security</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>plugin-complete.test.mjs</code>, <code>plugin-session-context.test.ts</code>, <code>subagent-wiring.test.mjs</code>); full UI journey Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-189-bundled-advisor-plugin-is-temporarily-unavailable" tabindex="-1">E2E-189: Bundled Advisor plugin is temporarily unavailable <a class="header-anchor" href="#e2e-189-bundled-advisor-plugin-is-temporarily-unavailable" aria-label="Permalink to &quot;E2E-189: Bundled Advisor plugin is temporarily unavailable&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A packaged or development build of PI-Desktop.</li><li><strong>Steps</strong>: <ol><li>Inspect the bundled plugin resources and confirm <code>pi.advisor</code> is absent.</li><li>Open the command palette and plugin settings. Confirm <code>/advisor</code>, the Advisor plugin, and its <code>advisor</code> tool are absent.</li></ol></li><li><strong>Expected</strong>: The temporary removal does not expose an Advisor command, plugin, panel, skill, or agent tool. The general host-owned plugin completion APIs remain available to explicitly installed plugins.</li><li><strong>Specs linked</strong>: <code>07-plugins/03-plugin-api.md</code>, ADR 0174, D336</li><li><strong>Acceptance</strong>: G (plugin agent tool), C (conversation)</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>bundled-plugins.test.mjs</code>); full UI journey Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-190-settings-network-proxy-applies-to-app-owned-http" tabindex="-1">E2E-190: Settings Network proxy applies to app-owned HTTP <a class="header-anchor" href="#e2e-190-settings-network-proxy-applies-to-app-owned-http" aria-label="Permalink to &quot;E2E-190: Settings Network proxy applies to app-owned HTTP&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A reachable local HTTP or SOCKS5 proxy, or a known-bad port for the failure path. A configured provider/model whose endpoint is reachable through the proxy is available for the model-request step.</li><li><strong>Steps</strong>: <ol><li>Open Settings → General. Confirm a Network card with Proxy modes System, Direct, and Custom. System is selected on a profile that never set a proxy.</li><li>Choose Custom. Confirm a Proxy URL field, Bypass defaulting to localhost / 127.0.0.1 / ::1 / <code>&lt;local&gt;</code>, and Test. Enter <code>not-a-proxy</code> and blur. Confirm an inline invalid-URL error and that settings are not saved.</li><li>Enter <code>socks5://127.0.0.1:1080</code> or <code>http://127.0.0.1:7890</code> and blur. Confirm <code>AppSettings.networkProxy.mode</code> is <code>custom</code> after <code>settings.get</code>.</li><li>Click Test against a listening proxy. Confirm a Connected status. Click Test against a closed port. Confirm a failure status without changing the saved URL. Enter <code>http://user:pass@127.0.0.1:&lt;auth-port&gt;</code> and <code>socks5://user:pass@127.0.0.1:&lt;auth-port&gt;</code> against proxies that require those credentials. Confirm Test reports Connected rather than <code>net::ERR_NO_SUPPORTED_PROXIES</code> (issue #490).</li><li>With Custom saved, send a short prompt through the configured provider. Confirm the provider request and response pass through the proxy, including when a SOCKS5 proxy returns the complete bind response in one TCP chunk. Confirm a subsequent marketplace refresh and a models.dev catalog refresh use the proxy (host-core curl <code>--proxy</code>, Electron <code>net.fetch</code>), and confirm a loopback URL in Bypass is not proxied.</li><li>Switch to Direct, then System. Confirm Chromium returns to <code>mode: &quot;direct&quot;</code> then <code>mode: &quot;system&quot;</code>, and the sidecar is reconfigured without an app restart.</li></ol></li><li><strong>Expected</strong>: Custom covers model calls, marketplace, updates, plugin <code>net.fetch</code>, and the in-app browser. Workspace Bash <code>env</code> does not show <code>HTTP_PROXY</code> / <code>ALL_PROXY</code> from the setting. OAuth still opens the system browser. Invalid schemes (<code>file:</code>, <code>ftp:</code>, and SOCKS4) and malformed percent-encoded credentials are rejected. Authenticated HTTP and SOCKS5 URLs Test and apply without <code>net::ERR_NO_SUPPORTED_PROXIES</code> (issue #490). No protocol or schema version bump.</li><li><strong>Specs linked</strong>: <code>04-ux/06-settings-ia.md</code>, <code>03-runtime/07-process-model.md</code>, ADR 0177, D340</li><li><strong>Acceptance</strong>: B (settings), F (providers), Security</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>network-proxy.test.ts</code>, <code>node-proxy.test.ts</code>, <code>authenticated-proxy-relay.test.ts</code>, <code>settings-general.test.mjs</code>, host-core <code>network_proxy</code> tests); malformed credentials and unsupported SOCKS4 schemes are covered by the shared parser tests; full UI journey Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-191-newly-emitted-apperror-codes-stay-registered" tabindex="-1">E2E-191: Newly emitted AppError codes stay registered <a class="header-anchor" href="#e2e-191-newly-emitted-apperror-codes-stay-registered" aria-label="Permalink to &quot;E2E-191: Newly emitted AppError codes stay registered&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: The shared package test suite is available.</li><li><strong>Steps</strong>: <ol><li>Run the shared error helper tests.</li><li>Inspect the newly emitted runtime and Edit code list used by the test.</li></ol></li><li><strong>Expected</strong>: Every code introduced by this update, including context-compaction, empty-response, and Edit recovery failures, exists in <code>packages/shared/src/errors.ts</code> with an identical key and value. Reserved codes remain excluded until they are emitted.</li><li><strong>Specs linked</strong>: <code>03-runtime/08-error-codes.md</code></li><li><strong>Acceptance</strong>: Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>packages/shared/src/errors.test.ts</code>); full UI journey not applicable</li></ul><h4 id="e2e-209-import-copies-model-configuration-from-local-agent-stores" tabindex="-1">E2E-209: Import copies model configuration from local agent stores <a class="header-anchor" href="#e2e-209-import-copies-model-configuration-from-local-agent-stores" aria-label="Permalink to &quot;E2E-209: Import copies model configuration from local agent stores&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: At least one supported local config exists among <code>~/.claude/settings.json</code>, <code>~/.codex/config.toml</code> <code>[model_providers.*]</code>, <code>~/.config/opencode/opencode.json</code>, <code>~/.pi/agent/models.json</code>, or <code>~/.cc-switch/cc-switch.db</code>, including two API-key profiles with the same endpoint and different keys, and optionally one OAuth-only vendor. PI-Desktop may already have an equivalent provider.</li><li><strong>Steps</strong>: <ol><li>Open Settings → Import. Confirm a Sessions card and a Model configuration card, each with its own Scan.</li><li>Scan model configuration. Confirm groups start collapsed, rows show name, model count, host, and an API key / No API key badge, and that no secret value appears in the UI or in the scan IPC payload.</li><li>Import the selected providers. Confirm both same-endpoint profiles appear as separate rows under Settings → Models and remain selectable in the Composer model menu. Re-import the same selection and confirm those unchanged credentials are skipped.</li><li>If the app had no default model, confirm the first imported provider becomes the default. If a default already existed, confirm it is unchanged.</li><li>Confirm an OAuth-only source account is absent from the candidate list and that session import still works independently.</li></ol></li><li><strong>Expected</strong>: Explicit scan only (D007). Stored API keys land in the host secret store. Only equivalent providers (normalized URL + API style + same credential) skip; different credentials at one endpoint remain separate. No protocol or schema version bump.</li><li><strong>Specs linked</strong>: <code>04-ux/06-settings-ia.md</code>, <code>04-ux/08-component-spec.md</code> §18.5, <code>03-runtime/01-ipc-protocol.md</code>, <code>03-runtime/11-provider-model-system.md</code>, ADR 0179, D342</li><li><strong>Acceptance</strong>: B (model configuration), F (session import)</li><li><strong>Milestone</strong>: M4</li><li><strong>Status</strong>: Unit-covered (<code>packages/shared/src/model-config-import.test.ts</code>, <code>apps/desktop/test/model-config-import.test.mjs</code>); full UI journey Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-210-documentation-screenshots-resolve-from-github-and-vitepress" tabindex="-1">E2E-210: Documentation screenshots resolve from GitHub and VitePress <a class="header-anchor" href="#e2e-210-documentation-screenshots-resolve-from-github-and-vitepress" aria-label="Permalink to &quot;E2E-210: Documentation screenshots resolve from GitHub and VitePress&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: The repository contains the gallery assets under <code>docs/public/screenshots/app/</code>; documentation dependencies are installed.</li><li><strong>Steps</strong>: <ol><li>Open <code>docs/guide/screenshots.md</code> and <code>docs/zh-CN/guide/screenshots.md</code> from the GitHub file view. Confirm the gallery images resolve to files under <code>docs/public/screenshots/</code>.</li><li>Open the English and Chinese screenshot pages in the VitePress preview. Confirm representative images from the home, panel, and settings sections render.</li><li>Confirm the navigation logo loads <code>docs/public/app-icon.png</code> on both locale pages.</li><li>Confirm each locale footer includes an <code>AIUO.NET</code> link to <code>https://aiuo.net</code>.</li><li>Run <code>pnpm docs:build</code> and inspect the generated pages for image load failures.</li></ol></li><li><strong>Expected</strong>: GitHub renders every gallery image instead of requesting a repository-root <code>/screenshots/</code> path; both VitePress locale pages continue to render the gallery from the same checked-in assets. The navigation uses the app icon, both footers expose the <code>https://aiuo.net</code> link, and the docs build succeeds.</li><li><strong>Specs linked</strong>: ADR 0079, <code>docs/README.md</code>, <code>docs/guide/screenshots.md</code>, <code>docs/zh-CN/guide/screenshots.md</code></li><li><strong>Acceptance</strong>: Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Static/documentation check covered (<code>pnpm docs:build</code> and path audit); remote GitHub and browser journey pending</li></ul><h4 id="e2e-196-chat-links-honor-destination-settings-and-context-menu-actions" tabindex="-1">E2E-196: Chat links honor destination settings and context menu actions <a class="header-anchor" href="#e2e-196-chat-links-honor-destination-settings-and-context-menu-actions" aria-label="Permalink to &quot;E2E-196: Chat links honor destination settings and context menu actions&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A chat transcript can render an HTTP(S) Markdown link. The work-panel Browser view and the system browser opener are available. The clipboard can be observed or stubbed for the copy action.</li><li><strong>Steps</strong>: <ol><li>In Settings → AI → Defaults, select <strong>Work panel browser</strong> and click the link from a chat reply.</li><li>Select <strong>Default OS browser</strong> and click the same link again.</li><li>Right-click the link and activate each context-menu item with the pointer: Open in default browser, Open in work panel, and Copy link address. Repeat the menu actions with keyboard focus and Arrow/Home/End navigation.</li><li>Repeat a link click with Ctrl/Cmd, Shift, and Alt held.</li></ol></li><li><strong>Expected</strong>: <ul><li>The Work panel browser is the default plain-click destination.</li><li>The Default OS browser setting routes chat, transcript, and plugin HTTP(S) clicks through the main-owned external opener, including markdown links, autolinked URLs, and inline-code URLs in assistant replies; changing the setting persists after reload.</li><li>The body-level context menu remains interactive when clicked. Its external and work-panel actions open the requested destination, and Copy link address updates the clipboard before showing the success toast. A rejected clipboard write shows an error toast instead of a success toast.</li><li>Modifier clicks continue to open links externally regardless of the setting.</li><li>Plugin/settings clicks that want the work panel return to chat so the dock is visible, without recording a navigation hop. A missing session falls back to the OS browser.</li><li>Workspace HTML preview, BrowserPreview, OAuth, and Feedback keep their existing destinations.</li></ul></li><li><strong>Specs linked</strong>: <code>04-ux/06-settings-ia.md</code>, <code>04-ux/08-component-spec.md</code> §8.3, <code>03-runtime/01-ipc-protocol.md</code>, <code>08-meta/decisions-log.md</code> (D330)</li><li><strong>Acceptance</strong>: B (settings), C (conversation &amp; stream), Security, Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>apps/desktop/test/markdown-link-menu.test.mjs</code>, locale catalog tests, <code>apps/desktop/test/open-http-url.test.mjs</code>); full UI journey Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-201-alias-a-configured-model-and-copy-a-model-id" tabindex="-1">E2E-201: Alias a configured model and copy a model id <a class="header-anchor" href="#e2e-201-alias-a-configured-model-and-copy-a-model-id" aria-label="Permalink to &quot;E2E-201: Alias a configured model and copy a model id&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: One provider saved with at least two model bindings, at least one of which the catalog publishes with a display name.</li><li><strong>Steps</strong>: 1) Open Settings → Model configuration and reopen the provider. 2) Drag-select a model id in the live model list and copy it; confirm the clipboard holds the id and the row&#39;s checkbox did not toggle. 3) Click the same row&#39;s checkbox without selecting text; confirm it still toggles. 4) Expand Advanced on a chosen row and type a short alias. 5) Save and open the Composer model picker; confirm the alias names that model while the other row keeps its published name. 6) Search the picker by the alias and by the real id; both reach the row. 7) Clear the alias, save, and confirm the published display name returns. 8) Re-enter the alias, save, reopen the provider, restart the app, and confirm the alias is still there. 9) Type more than 60 characters into the alias input; confirm the field keeps only the first 60, then send a direct <code>providers.update</code> RPC with a 61-character alias and confirm it fails with <code>MODEL_ALIAS_TOO_LONG</code>.</li><li><strong>Expected</strong>: The alias is a display label only — the provider request still carries <code>models[].id</code>, and the configuration row keeps showing the real id beside the alias chip. Model ids and names are selectable inside the non-selectable shell, and a click that carries a selection never toggles the row checkbox. A blank or cleared alias falls back to the catalog display name.</li><li><strong>Specs linked</strong>: <code>03-runtime/12-provider-config-schema.md</code>, <code>04-ux/08-component-spec.md</code>, ADR 0192</li><li><strong>Acceptance</strong>: B (model configuration), Quality</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Unit-covered (<code>composer-models.test.mjs</code>, <code>provider-model-config.test.mjs</code>); rendered UI journey Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-model-selected-order-persists-selected-model-order-survives-saving" tabindex="-1">E2E-MODEL-selected-order-persists: Selected model order survives saving <a class="header-anchor" href="#e2e-model-selected-order-persists-selected-model-order-survives-saving" aria-label="Permalink to &quot;E2E-MODEL-selected-order-persists: Selected model order survives saving&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: An AI service and an OAuth vendor account each have at least three selected models. Include a model with an alias and non-default Advanced settings. Record their binding values, the app-level default provider/model, and an explicitly bound session&#39;s model choice. A controllable save response is available to hold either editor busy.</li><li><strong>Steps</strong>: <ol><li>Open Settings → Model configuration and edit the AI service. Drag the last selected model by its handle before the first row, then drag it after the last row. Confirm the insertion location and resulting order.</li><li>Configure the selected IDs in this order: <code>shown-a</code>, <code>hidden-a</code>, <code>shown-b</code>, <code>hidden-b</code>, <code>shown-c</code>. Filter by <code>shown-</code>, then drag <code>shown-c</code> before <code>shown-a</code>. Clear the filter and inspect all five bindings.</li><li>Apply the filter again. Focus <code>shown-c</code>&#39;s reorder handle and press Down, then Up. Confirm it moves past the neighboring visible row each time and focus remains on the moved model&#39;s handle. Try Up on the first visible model and Down on the last; neither changes the order.</li><li>Start a drag and cancel it, then start another and drop outside the selected rows. Neither action changes the draft order.</li><li>Drag-select and copy a model ID in the discovery list and in the selected pane. Confirm no reorder or checkbox toggle occurs. Click a discovery checkbox, toggle Advanced, edit an alias, and remove a selected model; each action retains its existing behavior without starting a drag.</li><li>Save the reordered bindings, reopen the editor, then restart the app and reopen it again. Check order, aliases, model IDs, and Advanced settings. Check the provider default and existing session. Repeat with the service set as the app&#39;s default provider and with a different default provider.</li><li>Change the order again, cancel the editor, and reopen it. The last saved order remains.</li><li>Repeat the drag, keyboard, save/reopen, and cancel checks in the vendor account editor, including a filtered list with hidden selected bindings.</li><li>Hold a save in flight and try drag and keyboard reordering. Release the save, then filter down to one visible selected row and try again.</li></ol></li><li><strong>Expected</strong>: Both editors persist the complete ordered binding array. In step 2 the full order becomes <code>shown-c</code>, <code>shown-a</code>, <code>hidden-a</code>, <code>shown-b</code>, <code>hidden-b</code>; the hidden bindings remain present and retain their relative order. Moving a binding never resets its ID, alias, or Advanced overrides. Saving keeps the provider&#39;s compatibility default at the head binding. When that service or account is the app&#39;s default provider, saving also updates the app&#39;s default model to the head, preserving E2E-005A&#39;s existing behavior. Editing another provider leaves the app default unchanged. An explicitly bound session keeps its stored model choice. Canceled/outside drags leave the draft unchanged, editor cancellation discards unsaved moves, and busy or single-visible-row forms disable the reorder handles. Text copying and existing row actions remain independent.</li><li><strong>Specs linked</strong>: <code>03-runtime/13-model-catalog-and-selection.md</code> §2, <code>03-runtime/12-provider-config-schema.md</code>; ADR 0114, ADR 0192</li><li><strong>Acceptance</strong>: B (model configuration), F (persistence), Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Documented; full desktop journey has not been run.</li></ul><h4 id="e2e-202-subagent-thinking-follows-its-exact-model-binding" tabindex="-1">E2E-202: Subagent thinking follows its exact model binding <a class="header-anchor" href="#e2e-202-subagent-thinking-follows-its-exact-model-binding" aria-label="Permalink to &quot;E2E-202: Subagent thinking follows its exact model binding&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A configured provider has a model binding marked <code>availableForSubagents</code>. The catalog either reports that model as non-reasoning or omits one of the levels the binding explicitly enables. A user subagent definition and the builtin <code>Task</code> catalog are available.</li><li><strong>Steps</strong>: 1) In Settings → Model configuration, enable <code>medium</code> and <code>high</code> for the delegation model and mark it available for subagents. 2) Set the subagent definition&#39;s thinking level to <code>high</code>, save, and restart the app. 3) Run the definition with its frontmatter model pin. 4) Run a builtin with <code>Task.model</code> selecting the same binding, including the on-demand resolution path. 5) Run a definition with no model pin while the parent session is set to <code>medium</code>.</li><li><strong>Expected</strong>: The pinned and explicitly selected delegates retain the binding&#39;s enabled thinking levels and send the selected non-<code>off</code> level even when models.dev says reasoning is unavailable or publishes a sparse set. The unpinned delegate inherits the parent&#39;s effective level. A binding with no non-<code>off</code> level still resolves to <code>off</code>.</li><li><strong>Specs linked</strong>: <code>03-runtime/02-agent-runtime.md</code>, <code>03-runtime/11-provider-model-system.md</code>, ADR 0144 / D283</li><li><strong>Acceptance</strong>: B (model configuration) + C (chat/stream) + Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Unit/wiring-covered (<code>model-capabilities.test.ts</code>, <code>apps/desktop/test/subagent-wiring.test.mjs</code>); full UI journey Draft (do not run E2E locally unless explicitly requested)</li></ul><h4 id="e2e-203-omit-subagent-thinking-override-and-read-selected-levels-in-dark-mode" tabindex="-1">E2E-203: Omit subagent thinking override and read selected levels in dark mode <a class="header-anchor" href="#e2e-203-omit-subagent-thinking-override-and-read-selected-levels-in-dark-mode" aria-label="Permalink to &quot;E2E-203: Omit subagent thinking override and read selected levels in dark mode&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A configured subagent model supports reasoning, and the application has both light and dark themes available.</li><li><strong>Steps</strong>: 1) Open Settings → Agent → Subagents and inspect the thinking selector. 2) Confirm it contains inherit-session, do-not-send, <code>off</code>, and the canonical levels. 3) Choose do-not-send, save, and confirm the document contains <code>thinkingLevel: omit</code>. 4) Run the subagent through a provider with a meaningful adapter default and inspect the outbound request. 5) Switch to dark mode, open Settings → Model configuration, expand a model&#39;s Advanced section, and select multiple thinking-level chips.</li><li><strong>Expected</strong>: Inherit continues to use the parent level; do-not-send persists and sends no provider thinking override; explicit <code>off</code> remains an explicit disable. Selected thinking chips have a solid high-contrast fill and readable text in both themes, and each selected level is visually distinguishable from the track.</li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code>, <code>03-runtime/02-agent-runtime.md</code>, <code>03-runtime/13-model-catalog-and-selection.md</code>, <code>04-ux/06-settings-ia.md</code>, ADR 0194 / D356</li><li><strong>Acceptance</strong>: B (model configuration) + C (chat/stream) + Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Unit/source-contract-covered; full UI journey Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-203a-session-thinking-omit-sends-no-provider-override" tabindex="-1">E2E-203a: Session thinking omit sends no provider override <a class="header-anchor" href="#e2e-203a-session-thinking-omit-sends-no-provider-override" aria-label="Permalink to &quot;E2E-203a: Session thinking omit sends no provider override&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A configured reasoning model is selected in Composer.</li><li><strong>Steps</strong>: 1) Open the model × reasoning menu and confirm <code>omit</code> is the first reasoning option, followed by the binding&#39;s enabled canonical levels. 2) Choose <code>omit</code> and confirm the chip shows <code>omit</code> and the session stores <code>thinkingLevel: omit</code>. 3) Send a turn and inspect the outbound request. 4) Choose explicit <code>off</code> and send again. 5) In Settings → Model configuration, expand the same model&#39;s Advanced row and confirm the default picker lists <code>omit</code> first; save <code>omit</code> as the default and start a new session.</li><li><strong>Expected</strong>: <code>omit</code> persists and the request has no thinking/reasoning field. Explicit <code>off</code> still serializes a disable. A non-reasoning model keeps an <code>off</code>-only menu. A new session whose binding default is <code>omit</code> starts at <code>omit</code>.</li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code>, <code>03-runtime/02-agent-runtime.md</code>, <code>03-runtime/13-model-catalog-and-selection.md</code>, ADR 0295 / D456</li><li><strong>Acceptance</strong>: C (chat/stream) + Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Unit/source-contract-covered; full UI journey Draft</li></ul><h4 id="e2e-211-windows-portable-zip-launches-after-extraction-d603" tabindex="-1">E2E-211: Windows portable ZIP launches after extraction (D603) <a class="header-anchor" href="#e2e-211-windows-portable-zip-launches-after-extraction-d603" aria-label="Permalink to &quot;E2E-211: Windows portable ZIP launches after extraction (D603)&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A Windows x64 tag or <code>dist:win</code> package has produced both <code>PI-Desktop-Setup-&lt;version&gt;.exe</code> and <code>PI-Desktop-Portable-&lt;version&gt;.zip</code> from the shared electron-builder config; a clean user profile is available; the account is a standard user without administrator elevation.</li><li><strong>Steps</strong>: 1) Inspect the release directory and <code>latest.yml</code>. 2) Extract the portable ZIP to a user-writable directory without running the NSIS installer. 3) Launch the extracted <code>PI-Desktop.exe</code>. 4) Confirm there is no administrator prompt and that the running app has the PI-Desktop icon and taskbar entry. 5) Invoke Check for Updates. 6) Confirm Settings → Info offers the releases page rather than Restart to update. 7) Quit and relaunch the extracted executable.</li><li><strong>Expected</strong>: Both Windows artifacts are space-free and uploaded. <code>latest.yml</code> points at the NSIS installer only. The extracted ZIP app starts without a setup wizard or administrator prompt, keeps the normal PI-Desktop taskbar identity/icon, uses the existing application data directory, and reports update mode <code>manual</code>. An available update does not download or run <code>PI-Desktop-Setup-&lt;version&gt;.exe</code>. Relaunch restores sessions from that same profile.</li><li><strong>Specs linked</strong>: <code>01-product/01-product-scope.md</code>, <code>06-delivery/06-release-runbook.md</code>, <code>03-runtime/07-process-model.md</code>, ADR 0197 / D603</li><li><strong>Acceptance</strong>: Quality (release packaging)</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Unit/source-contract covered (<code>auto-update.test.mjs</code>); native Windows launch remains runner validation (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-213-the-first-composer-model-menu-paint-keeps-configured-aliases" tabindex="-1">E2E-213: The first Composer model menu paint keeps configured aliases <a class="header-anchor" href="#e2e-213-the-first-composer-model-menu-paint-keeps-configured-aliases" aria-label="Permalink to &quot;E2E-213: The first Composer model menu paint keeps configured aliases&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A runnable provider has at least one configured model with a non-empty alias and a cached or discoverable model record for that ID. The app has restarted, or the provider model cache has been invalidated.</li><li><strong>Steps</strong>: 1) Open the Composer model × reasoning chip. 2) Enter the Model submenu immediately and observe the first visible frame. 3) Keep the menu open until the cached/live model refresh completes. 4) Close and reopen the menu.</li><li><strong>Expected</strong>: The first visible row already uses the configured alias (or the configured ID when no alias exists), and the list never flashes empty or replaces the alias with a catalog name/wire ID during hydration. The same single display name remains after refresh and on the next open; selecting the row still sends the configured model ID.</li><li><strong>Specs linked</strong>: <code>03-runtime/13-model-catalog-and-selection.md</code>, <code>04-ux/08-component-spec.md</code></li><li><strong>Acceptance</strong>: Quality (stable first paint), B (model selection)</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Unit/source-contract covered (<code>composer-models.test.mjs</code>); full UI journey remains runner validation (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-214-plugin-session-import-and-ownership-boundary" tabindex="-1">E2E-214: Plugin session import and ownership boundary <a class="header-anchor" href="#e2e-214-plugin-session-import-and-ownership-boundary" aria-label="Permalink to &quot;E2E-214: Plugin session import and ownership boundary&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A test plugin declares two <code>contributes.sessionSources</code> entries and receives only the P0/P1 session permissions. A second plugin has no access to the first plugin&#39;s sessions.</li><li><strong>Steps</strong>: 1) Import a session with external project/provider/model history, user, assistant, and tool messages. 2) Repeat the same import and verify idempotent skip with the same host-generated id. 3) List, get, and page messages, including descending order and content truncation. 4) Attempt an undeclared source, a system/running message role, invalid timestamps, an oversized/deep tool value, and each missing permission. 5) Repeat list/get/ messages/rename/delete from the second plugin.</li><li><strong>Expected</strong>: Source declarations and permissions are enforced in Electron before host dispatch; ids are generated by host-core; original bindings are history-only; returned messages are marked external; reserved tool metadata is removed; invalid inputs fail with stable error codes; the second plugin sees neither the row nor its transcript.</li><li><strong>Specs linked</strong>: <code>07-plugins/02-plugin-manifest-schema.md</code>, <code>07-plugins/03-plugin-api.md</code>, <code>07-plugins/13-plugin-permissions-matrix.md</code>, ADR 0200, D367</li><li><strong>Acceptance</strong>: Security, Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Unit/RPC/wiring-covered; full UI journey Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-215-plugin-batch-and-delete-lifecycle" tabindex="-1">E2E-215: Plugin batch and delete lifecycle <a class="header-anchor" href="#e2e-215-plugin-batch-and-delete-lifecycle" aria-label="Permalink to &quot;E2E-215: Plugin batch and delete lifecycle&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: The test plugin can call <code>session.importBatch</code>, rename, and delete. Host-core starts with an empty v14 database.</li><li><strong>Steps</strong>: 1) Run a <code>skip</code> batch containing valid, duplicate, and invalid items. 2) Run a <code>fail</code> batch containing one invalid or conflicting item and verify no item from that batch lands. 3) Rename an owned session and verify list ordering/metadata. 4) Trash it, verify normal core/plugin reads omit it, then purge it and import the same external id again. 5) Exercise page-size, payload, batch-size, and rolling import/delete limits. 6) Restart on a migrated v13 database and verify existing core sessions remain active.</li><li><strong>Expected</strong>: Skip is partial and fail is atomic; list/get/message operations stay ownership-scoped; trash is recoverable only through the owner purge; purge removes transcript files and permits re-import; bounds return <code>LIMIT_EXCEEDED</code> and rolling limits return <code>RATE_LIMITED</code>; migration produces schema v14 without project rows for plugin history.</li><li><strong>Specs linked</strong>: <code>03-runtime/04-data-storage.md</code>, <code>03-runtime/06-host-rpc-protocol.md</code>, ADR 0200, D367</li><li><strong>Acceptance</strong>: Security, Quality, Recovery</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Host/RPC/unit-covered; full UI journey Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-plugin-usage-listturns-plugin-usage-fact-listing" tabindex="-1">E2E-PLUGIN-usage-listTurns: Plugin usage fact listing <a class="header-anchor" href="#e2e-plugin-usage-listturns-plugin-usage-fact-listing" aria-label="Permalink to &quot;E2E-PLUGIN-usage-listTurns: Plugin usage fact listing&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A test plugin is granted <code>usage.read</code>. The host database has completed turns across live and soft-deleted sessions.</li><li><strong>Steps</strong>: 1) Call <code>pi.usage.listTurns</code> without the permission. 2) Call it with the permission, page by cursor, and filter by session/project/window. 3) Pass inverted bounds, a window longer than 365 days, and a malformed cursor. 4) Confirm rows include token counters and titles but no message bodies, and that trashed sessions are absent.</li><li><strong>Expected</strong>: Missing permission returns <code>PERMISSION_DENIED</code> and does not hit the host. Valid calls return keyset pages of completed-turn facts. Invalid params return <code>INVALID_PARAMS</code>. Empty titles are <code>null</code>.</li><li><strong>Specs linked</strong>: <code>07-plugins/03-plugin-api.md</code>, <code>07-plugins/13-plugin-permissions-matrix.md</code>, <code>03-runtime/06-host-rpc-protocol.md</code>, ADR 0173, D335</li><li><strong>Acceptance</strong>: Security, Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Unit/RPC/wiring-covered (<code>plugin-session-api.test.mjs</code>, host-core <code>plugin_usage</code>); full UI journey Draft</li></ul><h4 id="e2e-216-explicit-plugin-project-binding-and-host-owned-sidebar-refresh" tabindex="-1">E2E-216: Explicit plugin project binding and host-owned sidebar refresh <a class="header-anchor" href="#e2e-216-explicit-plugin-project-binding-and-host-owned-sidebar-refresh" aria-label="Permalink to &quot;E2E-216: Explicit plugin project binding and host-owned sidebar refresh&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A test plugin has <code>project.create</code> and <code>session.import</code> permissions, declares a session source, and the renderer is showing the existing sidebar. A project path is available without changing the active workspace.</li><li><strong>Steps</strong>: 1) Call <code>pi.project.create({ path })</code> and record the returned <code>projectId</code>. 2) Import one session with that id and one session without it. 3) Observe the renderer while the plugin call completes. 4) Repeat the import with the same external id and then rename/delete an owned session.</li><li><strong>Expected</strong>: Project creation returns a durable id without activating or replacing the current workspace. Only the import with an explicit id has an active project binding; an omitted id stays unbound and its <code>projectPath</code> is history only. Each successful write causes one host-owned <code>pi-desktop/session/event/changed</code>, the renderer refreshes through <code>refreshSessions()</code>, and the sidebar does not require a plugin-emitted event. Skipped imports do not trigger a redundant refresh, and closed project tabs are not reopened merely because their session list was refreshed.</li><li><strong>Specs linked</strong>: <code>07-plugins/03-plugin-api.md</code>, <code>07-plugins/13-plugin-permissions-matrix.md</code>, <code>03-runtime/01-ipc-protocol.md</code>, <code>03-runtime/06-host-rpc-protocol.md</code>, ADR 0201, D368</li><li><strong>Acceptance</strong>: C (sessions), Security, Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Unit/source-contract-covered; full UI journey Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-217-windows-host-starts-on-a-clean-x64-emulated-arm64-install" tabindex="-1">E2E-217: Windows host starts on a clean x64-emulated ARM64 install <a class="header-anchor" href="#e2e-217-windows-host-starts-on-a-clean-x64-emulated-arm64-install" aria-label="Permalink to &quot;E2E-217: Windows host starts on a clean x64-emulated ARM64 install&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A clean Windows 11 x64 or ARM64 machine/profile without a separately installed Visual C++ Redistributable, Node.js, or another local agent runtime; the x64 NSIS installer is available.</li><li><strong>Steps</strong>: 1) Install PI-Desktop. 2) Launch it for the first time. 3) Wait for the startup splash to yield to the main shell. 4) Inspect the runtime logs, then open Settings → Info.</li><li><strong>Expected</strong>: The bundled x64 <code>pi-desktop-host-core.exe</code> starts and completes <code>app.handshake</code> without <code>0xC0000135</code> (<code>STATUS_DLL_NOT_FOUND</code>), the shell does not remain on “Can&#39;t reach the local service”, host status is healthy, and Settings → Info reports the host version instead of <code>host unknown</code>. The package uses the statically linked MSVC CRT; no separate runtime installer is required. Native Windows ARM64 artifacts remain out of scope.</li><li><strong>Specs linked</strong>: <code>03-runtime/07-process-model.md</code>, <code>06-delivery/06-release-runbook.md</code></li><li><strong>Acceptance</strong>: A (app startup), Quality (clean-install packaging)</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Source-contract-covered; clean-machine Windows x64 and ARM64 qualification remains runner validation (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-218-prompt-enhancement-preserves-pasted-image-chips" tabindex="-1">E2E-218: Prompt enhancement preserves pasted image chips <a class="header-anchor" href="#e2e-218-prompt-enhancement-preserves-pasted-image-chips" aria-label="Permalink to &quot;E2E-218: Prompt enhancement preserves pasted image chips&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A configured, authenticated model is available; an Agent session has a Composer draft containing one pasted image chip followed by ordinary prompt text.</li><li><strong>Steps</strong>: 1) Paste the image into the Composer and type a prompt after the chip. 2) Click <code>Enhance prompt</code>. 3) Observe the request and the updated Composer draft. 4) Send the enhanced draft and inspect the dispatched attachment metadata.</li><li><strong>Expected</strong>: The Sparkles action is enabled with the image chip present. The one-shot request contains only the visible prompt text, completes successfully, and rewrites that text. The image chip remains at the front of the draft, remains removable, and is dispatched exactly once with the enhanced prompt. Enhancement does not create a transcript row or alter the attachment bytes. The request is built from the built-in system prompt and the effective user template: with no saved override the built-in template applies; with an override saved in Settings, that text applies instead (E2E-259).</li><li><strong>Specs linked</strong>: <code>04-ux/12-prompt-enhancement.md</code>, <code>04-ux/08-component-spec.md</code> §11.3/§11.7–11.8, <code>03-runtime/01-ipc-protocol.md</code> §13, <code>03-runtime/02-agent-runtime.md</code></li><li><strong>Acceptance</strong>: C (conversation &amp; stream), Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Unit/source-contract-covered; full UI journey Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-259-prompt-enhancement-honors-the-configurable-user-template" tabindex="-1">E2E-259: Prompt enhancement honors the configurable user template <a class="header-anchor" href="#e2e-259-prompt-enhancement-honors-the-configurable-user-template" aria-label="Permalink to &quot;E2E-259: Prompt enhancement honors the configurable user template&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A configured, authenticated model is available; an Agent session has an empty Composer draft; Settings -&gt; AI is reachable.</li><li><strong>Steps</strong>: 1) Open Settings -&gt; AI and inspect the Prompt enhancement card with no saved override: the custom-template switch is off, disabled, and explains that saving a template unlocks it, and the row offers only the edit icon button. 2) Confirm no field for the system prompt is offered anywhere on the card or in the editor. 3) Open the editor; while the sheet is open, clear the draft variable token out of the user template and attempt to save. 4) Use the insert action to put the draft variable back, save, and confirm the sheet closes and the switch is now enabled and on. 5) Enhance a Chinese draft that also names a file such as <code>prompt-templates.ts</code>. 6) Enhance a mixed-language draft. 7) Resolve a model that returns the rewritten draft wrapped in quotation marks. 8) Reopen the editor, press <code>Escape</code>, and confirm the edit was abandoned. 9) Reopen the editor, edit the template, and close it by clicking the backdrop. 10) Turn the switch off and enhance again, then turn it back on and confirm the user&#39;s text is still there. 11) Pin an enhancement model, disable that provider, and enhance once more. 12) Confirm the reasoning row defaults to <code>Off (no reasoning)</code> and offers no follow-the-session entry, then raise it and enhance again to see the difference.</li><li><strong>Expected</strong>: With no override the editor opens on the built-in default text, so the displayed value equals the value in force, and the card shows no system-prompt field at all. Saving a user template without the draft variable is refused locally with a message, and no write reaches host-core. With no saved template the switch is disabled; after saving one it is enabled and on without a separate toggle. <code>Escape</code> and a backdrop click abandon the edit, leaving the stored value unchanged. With the switch off, enhancement uses the built-in template even though a custom one is stored; with it on, the stored template applies. Either way the request&#39;s system prompt is the built-in one and its user message contains the draft inside <code>&lt;draft&gt;</code> tags with the placeholder substituted. The rewritten draft keeps the draft&#39;s language, carries no language meta note, keeps <code>prompt-templates.ts</code> byte-identical, and has the wrapping quotation pair removed. Turning the switch off and on again leaves the user&#39;s stored text intact. A disabled pinned enhancement provider falls back to the Composer&#39;s current model, the enhancement still succeeds, and the fallback is logged as a warning. The reasoning row defaults to <code>Off (no reasoning)</code>, offers every canonical level plus <code>Off</code>, has no follow-the-session entry, and a level the model cannot honour is clamped rather than rejected. An enhancement that receives no provider response fails within about 60 seconds with <code>TIMEOUT</code> and a message naming the budget and the setting to change; it does not hang and does not silently retry on the session model.</li><li><strong>Specs linked</strong>: <code>04-ux/12-prompt-enhancement.md</code> §3/§5, <code>04-ux/06-settings-ia.md</code>, <code>03-runtime/01-ipc-protocol.md</code> §13, <code>03-runtime/04-data-storage.md</code>, ADR 0121, D447</li><li><strong>Acceptance</strong>: C (conversation &amp; stream), Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Unit/RPC/source-contract-covered for the template resolution, validation, and quote stripping; full UI journey Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-220-local-mcp-control-drives-a-running-desktop" tabindex="-1">E2E-220: Local MCP control drives a running desktop <a class="header-anchor" href="#e2e-220-local-mcp-control-drives-a-running-desktop" aria-label="Permalink to &quot;E2E-220: Local MCP control drives a running desktop&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Start PI-Desktop with <code>PI_DESKTOP_MCP_CONTROL=1</code> and a clean profile. A local project directory is available, the Electron user-data directory is writable, and the desktop has completed backend boot.</li><li><strong>Steps</strong>: 1) Read <code>mcp-control.json</code> and use its URL and bearer token. 2) Call <code>initialize</code>, <code>tools/list</code>, and <code>pi_control_describe</code>. 3) Call <code>pi_project_open</code> with the fixture project. 4) Call <code>pi_session_create</code>, <code>pi_session_get</code>, and <code>pi_agent_status</code>. 5) Call <code>pi_agent_prompt</code> and observe the existing desktop session-change event select the target session. 6) Call <code>pi_desktop_invoke</code> for a reviewed read operation. 7) Attempt <code>pi_session_delete</code> and <code>pi_session_configure</code> without confirmation, then repeat with <code>confirm: true</code>. 8) Stop the app and inspect the manifest.</li><li><strong>Expected</strong>: An unauthenticated request receives 401; <code>initialize</code> with an unsupported protocol version negotiates <code>2025-06-18</code>; the authenticated MCP handshake and tool catalog succeed; project/session/Agent operations use the same IPC validation and host permission boundaries as the renderer; mutating calls refresh/select the visible project and session while <code>pi_session_get</code> does not; destructive operations and <code>pi_session_configure</code> fail with a confirmation error until acknowledged; secret writes, <code>plugin/loadDev</code>, and native-picker channels are absent from <code>pi_control_describe</code>; secret-shaped fields are stripped; the endpoint binds loopback only; disallowed Origins and unsupported protocol version headers are rejected; and the manifest changes to <code>active: false</code> on shutdown.</li><li><strong>Specs linked</strong>: <code>02-architecture/01-architecture.md</code>, <code>03-runtime/01-ipc-protocol.md</code>, <code>05-security/01-security.md</code>, ADR 0203, D370, D372</li><li><strong>Acceptance</strong>: A (app control), C (sessions), Security, Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: MCP protocol/unit-covered by <code>apps/desktop/test/mcp-control.test.mjs</code>; full Electron journey documented and remains deferred by the no-local-E2E policy</li></ul><h4 id="e2e-234-workspace-security-denylist-and-ignore-layers" tabindex="-1">E2E-234: Workspace security denylist and ignore layers <a class="header-anchor" href="#e2e-234-workspace-security-denylist-and-ignore-layers" aria-label="Permalink to &quot;E2E-234: Workspace security denylist and ignore layers&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A project containing <code>.env</code>, <code>.env.example</code>, <code>server.pem</code>, <code>keys/id_rsa</code>, <code>notes.txt</code>, <code>node_modules/pkg/index.js</code>, <code>generated/out.txt</code>, <code>debug.log</code>, and a root <code>.pi-desktopignore</code> with <code>generated/</code>. Every file contains the word <code>needle</code>. The session is Agent in <code>auto</code> permission mode.</li><li><strong>Steps</strong>: 1) Ask for <code>Read</code> of <code>.env</code>, then of <code>.env.example</code>. 2) Ask for <code>Write</code> to <code>keys/id_rsa</code>. 3) Run an unscoped <code>Grep</code> and <code>Glob</code> for <code>needle</code>. 4) Run <code>Grep</code> with <code>path: node_modules/pkg</code> and with <code>path: generated</code>. 5) Repeat step 1 with a system <code>rg</code> installed and with <code>PI_DESKTOP_DISABLE_RG=1</code>.</li><li><strong>Expected</strong>: Steps 1 and 2 fail with <code>WORKSPACE_PATH_DENIED</code>, the <code>.env.example</code> read succeeds, and no <code>keys/id_rsa</code> file is created. The unscoped search lists <code>notes.txt</code> and <code>.env.example</code> only: <code>.env</code>, <code>server.pem</code>, <code>node_modules</code>, <code>generated</code>, and <code>debug.log</code> are absent. The explicit-path searches return one hit each. The in-process walker and the <code>rg</code> fast path produce the same file set.</li><li><strong>Specs linked</strong>: <code>03-runtime/15-workspace-ignore-rules.md</code>, <code>03-runtime/08-error-codes.md</code> §3.3, D032</li><li><strong>Acceptance</strong>: B (workspace tools), Security</li><li><strong>Milestone</strong>: M3+</li><li><strong>Status</strong>: unit-covered by <code>crates/host-core/src/tools/mod.rs</code> (<code>security_denylist_blocks_read_write_edit_and_hides_search_results</code>, <code>default_ignores_and_workspace_ignore_file_hide_unscoped_walks_only</code>) and <code>tools/ignore_rules.rs</code>; the Electron journey is documented and deferred by the no-local-E2E policy</li></ul><h4 id="e2e-235-dangling-symlinks-cannot-write-outside-the-workspace" tabindex="-1">E2E-235: Dangling symlinks cannot write outside the workspace <a class="header-anchor" href="#e2e-235-dangling-symlinks-cannot-write-outside-the-workspace" aria-label="Permalink to &quot;E2E-235: Dangling symlinks cannot write outside the workspace&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A project containing <code>dangling -&gt; /tmp/outside/planted.txt</code> where the target does not exist, and <code>inner -&gt; ./not-yet.txt</code>. Agent mode, <code>auto</code> permission.</li><li><strong>Steps</strong>: 1) Ask for <code>Write</code> to <code>dangling</code>. 2) Ask for <code>Write</code> to <code>dangling-dir/new.txt</code> where <code>dangling-dir -&gt; /tmp/outside/dir</code>. 3) Ask for <code>Write</code> to <code>inner</code>.</li><li><strong>Expected</strong>: Steps 1 and 2 fail with <code>PATH_OUTSIDE_WORKSPACE</code> and nothing appears under <code>/tmp/outside</code>. Step 3 creates <code>not-yet.txt</code> inside the project. A symlink loop fails with a canonicalize error rather than hanging.</li><li><strong>Specs linked</strong>: <code>03-runtime/03-tools-and-permissions.md</code>, <code>03-runtime/15-workspace-ignore-rules.md</code> §3</li><li><strong>Acceptance</strong>: B, Security</li><li><strong>Milestone</strong>: M3+</li><li><strong>Status</strong>: unit-covered by <code>crates/host-core/src/workspace.rs</code> (<code>blocks_dangling_symlink_escape</code>, <code>dangling_symlink_inside_workspace_resolves_to_its_target</code>, <code>dangling_symlink_loop_is_rejected</code>)</li></ul><h4 id="e2e-236-plugin-desktop-control-needs-the-user-s-native-consent" tabindex="-1">E2E-236: Plugin desktop control needs the user&#39;s native consent <a class="header-anchor" href="#e2e-236-plugin-desktop-control-needs-the-user-s-native-consent" aria-label="Permalink to &quot;E2E-236: Plugin desktop control needs the user&#39;s native consent&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A dev plugin granted <code>desktop.control</code> whose panel calls <code>pi.desktop.invoke({ operation: &quot;session/delete&quot;, args: [id], confirm })</code>. One disposable session exists.</li><li><strong>Steps</strong>: 1) Invoke with <code>confirm: false</code>. 2) Invoke with <code>confirm: true</code> and press Escape on the dialog. 3) Invoke with <code>confirm: true</code> and click Deny. 4) Invoke with <code>confirm: true</code> and click Allow once. 5) Invoke a <code>read</code> operation.</li><li><strong>Expected</strong>: Step 1 fails with <code>CONFIRMATION_REQUIRED</code> and no dialog appears. Steps 2 and 3 fail with <code>PERMISSION_DENIED</code>; the session still exists. The dialog names <code>session/delete</code> and the catalog description, never panel-authored text. Step 4 deletes the session and the sidebar refreshes. Step 5 shows no dialog. Every invocation is audited with plugin id, operation, and risk.</li><li><strong>Specs linked</strong>: <code>07-plugins/03-plugin-api.md</code> (desktop control), <code>07-plugins/04-plugin-security.md</code> §8.2, <code>07-plugins/13-plugin-permissions-matrix.md</code>, ADR 0203, ADR 0208, D370, D372, D377</li><li><strong>Acceptance</strong>: D (plugins), Security</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: runtime-covered by <code>apps/desktop/test/plugin-desktop-control.test.mjs</code>; the native dialog journey is documented and deferred by the no-local-E2E policy</li></ul><h4 id="e2e-plugin-session-orchestrator-real-workers-session-orchestrator-creates-and-coordinates-durable-sessions" tabindex="-1">E2E-PLUGIN-session-orchestrator-real-workers: Session Orchestrator creates and coordinates durable sessions <a class="header-anchor" href="#e2e-plugin-session-orchestrator-real-workers-session-orchestrator-creates-and-coordinates-durable-sessions" aria-label="Permalink to &quot;E2E-PLUGIN-session-orchestrator-real-workers: Session Orchestrator creates and coordinates durable sessions&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: The marketplace <code>pi.session-orchestrator</code> plugin is installed and enabled; the parent Agent session has a configured authenticated provider/model and a project path. The parent is in Agent mode.</li><li><strong>Steps</strong>: 1) Ask the parent to review Frontend, Electron, and Rust in parallel. 2) Confirm that <code>SessionTask.spawn</code> returns three distinct real durable <code>sessionId</code> values and one host <code>messageId</code> per delivery; each worker is visible in the normal session list. 3) Confirm all three workers receive prompts without using <code>session/fork</code> and can run concurrently. 4) While a worker is busy, send a follow-up to that exact Session ID and verify it is queued against the target inbox rather than starting a second turn. 5) Inspect status from the Agents panel and a sidebar hover card; confirm both use bounded host projections and do not fetch a complete worker transcript. 6) Wait for one worker, query <code>result</code> by its exact <code>messageId</code> and <code>turnId</code>, and inspect the parent transcript for one host-generated completion message. 7) Re-read the result and repeat the settlement notification path; confirm the callback and transcript row are not duplicated. 8) Send a parent-to-worker and worker-to-parent message in separate active plugin Agent tool turns; verify source/target provenance and real target turn binding in both transcripts. 9) Open one worker from the Agents panel, send it a follow-up using the exact returned <code>sessionId</code>, and stop another worker. 10) Restart the host/plugin with a queued delivery and confirm it remains held, while an interrupted turn is not replayed. Repeat the parallel creation step with a large existing session list while keeping the parent visible. 11) With one model&#39;s «Available for AI delegation» left off, ask the parent to spawn a worker on it by <code>modelKey</code>, then spawn again with no <code>modelKey</code>.</li><li><strong>Expected</strong>: Each worker is a real durable session with the parent&#39;s project/model/thinking/permission ceiling and an independent empty transcript at creation. The host ledger binds every delivery to the actual target durable turn; result text and error status derive from that turn&#39;s terminal state, not from polling assistant text. The parent receives only bounded, at-most-once completion messages; full worker transcripts remain inspectable in their own sessions. <code>sessionId</code> is the only canonical worker identity and follow-up <code>send</code> reuses that same session and context without creating a replacement. A session-message row is visibly distinct from human input and its provenance survives reload. <code>wait</code> returns <code>timedOut</code> within its bound instead of occupying the host tool deadline. Cancel interrupts only the selected delivery/turn without deleting the session. Sends without an active plugin tool invocation, forged source ids, targets above the source permission ceiling, worker fan-out overflow, inbox overflow, and autonomous callback loops fail closed. A <code>spawn</code> naming a model the user has not enabled for AI delegation is refused with <code>PERMISSION_DENIED</code> before a worker exists, while omitting <code>modelKey</code> — or naming the default model&#39;s own key — still inherits. Unrelated sessions and the existing Task family are unchanged, and no localhost MCP call or token access occurs. Bursts of worker notifications serialize and coalesce session-list refreshes while preserving the final worker list and the foreground session.</li><li><strong>Specs linked</strong>: <code>07-plugins/03-plugin-api.md</code>, <code>07-plugins/04-plugin-security.md</code>, <code>07-plugins/11-plugin-storage-isolation.md</code>, <code>03-runtime/01-ipc-protocol.md</code>, <code>03-runtime/06-host-rpc-protocol.md</code>, <code>03-runtime/04-data-storage.md</code>, <code>03-runtime/11-provider-model-system.md</code>, ADR 0237, ADR 0239, ADR subagent-model-opt-in</li><li><strong>Acceptance</strong>: C (parallel durable sessions), D (plugin security), Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: host ledger coverage is automated by <code>pnpm test:e2e:collaboration</code>; marketplace plugin tests cover the plugin runtime, and host-core/desktop unit tests cover the additive host primitives. The full live provider/Electron journey remains runner validation under the no-local-E2E policy</li></ul><h4 id="e2e-session-independent-top-level-communication-sessiontask-discovers-and-communicates-with-existing-sessions" tabindex="-1">E2E-SESSION-independent-top-level-communication: SessionTask discovers and communicates with existing sessions <a class="header-anchor" href="#e2e-session-independent-top-level-communication-sessiontask-discovers-and-communicates-with-existing-sessions" aria-label="Permalink to &quot;E2E-SESSION-independent-top-level-communication: SessionTask discovers and communicates with existing sessions&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: The marketplace <code>pi.session-orchestrator</code> plugin is installed and enabled. Two existing Agent sessions were created from the normal New Task flow and are not linked as Session Orchestrator workers. The caller is an active Agent session with a configured authenticated provider.</li><li><strong>Steps</strong>: 1) Call <code>SessionTask</code> with <code>action: &quot;list&quot;</code> and identify both existing sessions by their durable <code>sessionId</code>. 2) Send a message to one independent session and verify it is admitted against that session&#39;s inbox. 3) From the target session, send a reply to the original session. 4) Call <code>status</code> and <code>result</code> with the returned IDs and inspect the two transcripts.</li><li><strong>Expected</strong>: <code>list</code> includes bounded references to existing communicable Agent sessions without requiring plugin-owned history or treating them as workers. <code>send</code> works in either direction using the real target Session ID, preserves each target&#39;s existing model/project/context/permissions, and never creates a replacement session. The host records source and target provenance, results remain bound to the actual durable turns, and the list response contains no transcript, project path, credentials, or message previews. Non-Agent sessions remain rejected by the existing host policy.</li><li><strong>Specs linked</strong>: <code>07-plugins/03-plugin-api.md</code>, <code>07-plugins/04-plugin-security.md</code>, <code>03-runtime/01-ipc-protocol.md</code>, <code>03-runtime/04-data-storage.md</code>, ADR 0239, ADR 0240</li><li><strong>Acceptance</strong>: C (conversation &amp; stream), D (plugin security), G (plugins), Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: host discovery and bidirectional delivery are automated by <code>pnpm test:e2e:collaboration</code>; plugin and host-core regression coverage is automated. The live multi-session provider/Electron journey remains runner validation under the no-local-E2E policy</li></ul><h4 id="e2e-session-completion-notice-allows-silence-a-trusted-completion-notice-may-finish-without-an-acknowledgement" tabindex="-1">E2E-SESSION-completion-notice-allows-silence: A trusted completion notice may finish without an acknowledgement <a class="header-anchor" href="#e2e-session-completion-notice-allows-silence-a-trusted-completion-notice-may-finish-without-an-acknowledgement" aria-label="Permalink to &quot;E2E-SESSION-completion-notice-allows-silence: A trusted completion notice may finish without an acknowledgement&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A candidate commit has its own built host-core and runtime sidecar. The local SSE provider deterministically returns visible text or a successful empty response; no live credentials are required.</li><li><strong>Steps</strong>: 1) Deliver a task through the real Host collaboration ledger and sidecar, read its successful result, and complete the coordinator summary. 2) Resolve the queued completion callback through the production Main input resolver and run the recipient against an empty SSE response. 3) Run another human request, copied completion framing, a ledger task, and a ledger message against empty responses on the same recipient runtime.</li><li><strong>Expected</strong>: The original result remains unchanged. The completion has one provider request, no error, one terminal lifecycle, completed ledger status, and no acknowledgement callback. Each ordinary input still retries once and ends with <code>EMPTY_MODEL_RESPONSE</code>, and no request the recipient sends after the silent notice carries an empty assistant message. Unit coverage additionally rejects missing reply-to IDs/wrong targets, spends the exception on a tool batch, keeps it across a provider retry, revokes it once accepted user steering enters the context, and keeps the accepted silence out of the runtime entries and pi transcript state.</li><li><strong>Specs linked</strong>: <code>03-runtime/02-agent-runtime.md</code> §5e, <code>03-runtime/08-error-codes.md</code>, ADR 0239 (D446 amendment)</li><li><strong>Acceptance</strong>: C (conversation &amp; stream), D (provenance), Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Automated by <code>pnpm test:e2e:session-completion</code> on the committed, rebased candidate in its dedicated worktree. The harness drives real Host RPCs, the production provenance resolver, sidecar, and local SSE, and persists runtime messages before Host settlement. It does not exercise Electron&#39;s queue/outbox UI or a live provider. Candidate/base SHAs and results belong in the validation report; existing ledger coverage runs separately through <code>pnpm test:e2e:collaboration</code>.</li></ul><h4 id="e2e-session-hover-card-model-and-links-session-hover-cards-expose-readable-model-and-creation-navigation" tabindex="-1">E2E-SESSION-hover-card-model-and-links: Session hover cards expose readable model and creation navigation <a class="header-anchor" href="#e2e-session-hover-card-model-and-links-session-hover-cards-expose-readable-model-and-creation-navigation" aria-label="Permalink to &quot;E2E-SESSION-hover-card-model-and-links: Session hover cards expose readable model and creation navigation&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: The app has one collaboration-created session, one independent session, and a configured provider/model with readable catalog names. The sidebar contains both sessions.</li><li><strong>Steps</strong>: 1) Hover or keyboard-focus the collaboration-created session. 2) Inspect the model metadata, creator reference, and created-session list. 3) Activate the creator and one created-session reference with the keyboard. 4) Delete (or otherwise remove) one referenced session, or use a session whose reference is already stale, then revisit the card. 5) Inspect an independent session&#39;s card as well.</li><li><strong>Expected</strong>: The card shows the model&#39;s display name, falling back to the provider&#39;s readable name, instead of the provider ID. A collaboration-created session shows its creator, and a creator shows its bounded created-session list. Each live reference is a native keyboard-focusable button with an accessible open-session name; activating it opens that durable session and focuses the Composer. A reference whose session no longer exists is presented as text with an &quot;unavailable&quot; indication and is not a keyboard-focusable navigation control; activating a session that no longer exists (for example a session deleted between the snapshot and the click) surfaces a visible error instead of switching to an empty transcript. An independent session remains a valid local session without a fabricated creator link. Hover polling remains bounded and does not load a transcript: a read that exceeds its deadline is abandoned, and an unfocused window keeps a slower idle poll instead of stopping or overrunning.</li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code> §5.7, <code>03-runtime/04-data-storage.md</code>, <code>04-ux/08-component-spec.md</code>, <code>04-ux/09-interaction-patterns.md</code>, ADR 0240</li><li><strong>Acceptance</strong>: C (conversation &amp; stream), Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: source-contract and projection tests are automated; rendered pointer/keyboard validation remains runner validation</li></ul><h4 id="e2e-237-plugin-fetch-re-checks-egress-on-every-redirect" tabindex="-1">E2E-237: Plugin fetch re-checks egress on every redirect <a class="header-anchor" href="#e2e-237-plugin-fetch-re-checks-egress-on-every-redirect" aria-label="Permalink to &quot;E2E-237: Plugin fetch re-checks egress on every redirect&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A dev plugin with <code>net.domains: [&quot;allowed.test&quot;]</code> and <code>net.fetch</code>. A local server on <code>allowed.test</code> answers <code>/hop</code> with a 302 to <code>http://undeclared.test/leak</code>, <code>/ok</code> with 200, and <code>/limited</code> with 429 and <code>Retry-After: 2</code>.</li><li><strong>Steps</strong>: 1) Call <code>pi.net.fetch({ url: &quot;https://allowed.test/ok&quot; })</code>. 2) Call <code>pi.net.fetch({ url: &quot;https://allowed.test/hop&quot; })</code>. 3) Call <code>pi.net.fetch({ url: &quot;https://allowed.test/limited&quot; })</code>.</li><li><strong>Expected</strong>: Step 1 returns 200. Step 2 fails with <code>PERMISSION_DENIED</code> naming <code>undeclared.test</code>, and the undeclared server records no request. The audit log shows the denied hop. Step 3 returns 429 with its <code>Retry-After</code> header intact, the server records exactly one request for it, and the audit log shows <code>ok: false</code> with the advertised <code>retryAfter</code> — the host retries nothing.</li><li><strong>Specs linked</strong>: <code>07-plugins/04-plugin-security.md</code> §8.0, <code>07-plugins/03-plugin-api.md</code> §7</li><li><strong>Acceptance</strong>: D, Security</li><li><strong>Milestone</strong>: M4+</li><li><strong>Status</strong>: runtime-covered by <code>apps/desktop/test/plugin-egress.test.mjs</code> (per-hop egress and failed-call audit)</li></ul><h4 id="e2e-238-tool-requests-for-an-unknown-session-do-not-fall-back" tabindex="-1">E2E-238: Tool requests for an unknown session do not fall back <a class="header-anchor" href="#e2e-238-tool-requests-for-an-unknown-session-do-not-fall-back" aria-label="Permalink to &quot;E2E-238: Tool requests for an unknown session do not fall back&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: host-core running; a JSON-RPC probe attached to its stdio.</li><li><strong>Steps</strong>: 1) Send <code>tools.execute</code> with <code>sessionId: &quot;missing&quot;</code> and a <code>Read</code> of <code>README.md</code>. 2) Send <code>plans.enter</code> with the same id.</li><li><strong>Expected</strong>: Both fail with <code>SESSION_NOT_FOUND</code> (numeric <code>1007</code> and <code>PLAN_SESSION_NOT_FOUND</code> respectively); no file under the last-opened workspace is read.</li><li><strong>Specs linked</strong>: <code>03-runtime/06-host-rpc-protocol.md</code> §7, <code>03-runtime/08-error-codes.md</code> §3.1</li><li><strong>Acceptance</strong>: B, Security</li><li><strong>Milestone</strong>: M3+</li><li><strong>Status</strong>: unit-covered by <code>crates/host-core/src/rpc/mod.rs</code> (<code>temporary_session_uses_its_own_scratch_workspace</code>)</li></ul><h4 id="e2e-239-an-older-build-names-the-newer-data-schema-instead-of-looping" tabindex="-1">E2E-239: An older build names the newer data schema instead of looping <a class="header-anchor" href="#e2e-239-an-older-build-names-the-newer-data-schema-instead-of-looping" aria-label="Permalink to &quot;E2E-239: An older build names the newer data schema instead of looping&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: a data directory last opened by a newer PI-Desktop whose host-core migrated it past the schema this build supports.</li><li><strong>Steps</strong>: 1) Launch the older packaged app on that data directory. 2) Observe the banner and <code>logs/app/runtime.log</code>.</li><li><strong>Expected</strong>: host-core exits once; no further restart attempts are logged. The fatal banner says this PI-Desktop is older than the local data, shows both schema numbers, and tells the user to install the newer version. The data directory is not modified.</li><li><strong>Specs linked</strong>: <code>03-runtime/07-process-model.md</code> (boot outcomes)</li><li><strong>Acceptance</strong>: B</li><li><strong>Milestone</strong>: M3+</li><li><strong>Status</strong>: source-contract covered by <code>apps/desktop/test/host-boot-diagnostics.test.mjs</code></li></ul><h4 id="e2e-240-an-intel-macos-build-on-apple-silicon-points-at-the-native-download" tabindex="-1">E2E-240: An Intel macOS build on Apple Silicon points at the native download <a class="header-anchor" href="#e2e-240-an-intel-macos-build-on-apple-silicon-points-at-the-native-download" aria-label="Permalink to &quot;E2E-240: An Intel macOS build on Apple Silicon points at the native download&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Apple Silicon Mac; the x64 macOS package installed and running under Rosetta 2.</li><li><strong>Steps</strong>: 1) Launch the app. 2) Read the banner under the title bar. 3) Click its dismiss action.</li><li><strong>Expected</strong>: The app boots normally. A dismissible hint says this is the Intel build on an Apple Silicon machine and asks the user to install the Apple Silicon build. Dismissing hides it for the session; the native arm64 package shows no hint.</li><li><strong>Specs linked</strong>: <code>03-runtime/07-process-model.md</code> (boot outcomes)</li><li><strong>Acceptance</strong>: B</li><li><strong>Milestone</strong>: M3+</li><li><strong>Status</strong>: source-contract covered by <code>apps/desktop/test/host-boot-diagnostics.test.mjs</code></li></ul><h2 id="remote-agent-control-target-scenarios-post-mvp" tabindex="-1">Remote Agent Control target scenarios (post-MVP) <a class="header-anchor" href="#remote-agent-control-target-scenarios-post-mvp" aria-label="Permalink to &quot;Remote Agent Control target scenarios (post-MVP)&quot;">​</a></h2><p>The following scenarios require the approved remote harness. They are documented now so the protocol and security work has an explicit acceptance target. Do not run them against a local desktop or a production Gateway unless the request explicitly authorizes that environment. D374 amended every scenario in this section to the revised contract: <code>{ epoch, sequence }</code> cursors, ephemeral deltas, the Host-owned turn queue, the full local approval vocabulary, the remote permission ceiling, the Host link relay, and the browser cookie profile. D375 re-sequenced the milestones: E2E-231 and E2E-232 are the acceptance targets of the scheduled SSH-tunnel and integration milestones, while E2E-227 and E2E-228 run when the Gateway and browser milestones are scheduled.</p><h4 id="e2e-221-racp-initialization-negotiates-capabilities-and-policy" tabindex="-1">E2E-221: RACP initialization negotiates capabilities and policy <a class="header-anchor" href="#e2e-221-racp-initialization-negotiates-capabilities-and-policy" aria-label="Permalink to &quot;E2E-221: RACP initialization negotiates capabilities and policy&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A test Agent Host and an authenticated client support <code>RACP-WS</code> v1. The Host has one visible idle Session and a configured <code>remoteMaxPermissionMode</code> and <code>approvalLifetimeMs</code>.</li><li><strong>Steps</strong>: 1) Connect without initialization and send a request. 2) Send <code>connection/initialize</code> with supported bindings and the <code>turnQueue</code>, <code>hostEvents</code>, and <code>history</code> capabilities. 3) Send <code>notifications/initialized</code>. 4) Repeat with an unsupported major version. 5) Call <code>host/list</code> on the direct Host connection.</li><li><strong>Expected</strong>: Pre-initialization requests are rejected; the valid handshake returns the negotiated protocol, connection id, principal roles, limits including <code>maxQueuedTurnsPerSession</code> and <code>replayWindowEvents</code>, the advertised capabilities, and the informational <code>policy</code> block; the unsupported major version returns <code>PROTOCOL_MISMATCH</code>; <code>host/list</code> returns <code>METHOD_NOT_FOUND</code> because no Gateway is present.</li><li><strong>Specs linked</strong>: <code>03-runtime/19-remote-agent-control-protocol.md</code> §3 and §7.7, <code>05-security/02-remote-control-security.md</code> §3</li><li><strong>Acceptance</strong>: Security, Quality</li><li><strong>Milestone</strong>: Post-MVP</li><li><strong>Status</strong>: Draft; remote harness required</li></ul><h4 id="e2e-222-remote-turn-streams-ordered-durable-events-and-live-deltas" tabindex="-1">E2E-222: Remote turn streams ordered durable events and live deltas <a class="header-anchor" href="#e2e-222-remote-turn-streams-ordered-durable-events-and-live-deltas" aria-label="Permalink to &quot;E2E-222: Remote turn streams ordered durable events and live deltas&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: An authenticated controller is attached to an idle Session with a deterministic fixture Agent whose turn has two model rounds, one tool call, and one compaction.</li><li><strong>Steps</strong>: 1) Attach and subscribe from the current cursor. 2) Call <code>turn/start</code>. 3) Collect every event envelope until the terminal event, separating durable events (with <code>sequence</code>) from ephemeral events (with <code>afterSequence</code>). 4) Compare the final snapshot with the state rebuilt from durable events alone. 5) Compare each <code>payload.event</code> with the local <code>AgentEvent</code> recorded by the desktop for the same turn.</li><li><strong>Expected</strong>: <code>turn/start</code> returns quickly with a <code>turnId</code>; durable sequences are strictly increasing inside one epoch; <code>item.delta</code>, <code>tool.progress</code>, and <code>turn.activity</code> carry <code>afterSequence</code> and never a <code>sequence</code>; exactly one <code>turn.completed</code> is emitted, at the local <code>agent_end</code>, and none at the intermediate <code>turn_end</code>; the compaction appears as an <code>item</code> with <code>itemType: &quot;compaction&quot;</code>; the terminal state is immutable; and the final snapshot equals the state reconstructed from durable events plus the content of <code>item.completed</code> payloads.</li><li><strong>Specs linked</strong>: <code>03-runtime/19-remote-agent-control-protocol.md</code> §§5–8, <code>03-runtime/10-session-state-machine.md</code>, <code>02-architecture/05-remote-agent-control.md</code> §8</li><li><strong>Acceptance</strong>: C (conversation &amp; stream), Quality</li><li><strong>Milestone</strong>: Post-MVP</li><li><strong>Status</strong>: Draft; remote harness required</li></ul><h4 id="e2e-223-reconnect-replays-durable-events-or-resynchronizes-by-epoch" tabindex="-1">E2E-223: Reconnect replays durable events or resynchronizes by epoch <a class="header-anchor" href="#e2e-223-reconnect-replays-durable-events-or-resynchronizes-by-epoch" aria-label="Permalink to &quot;E2E-223: Reconnect replays durable events or resynchronizes by epoch&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A turn is producing at least five durable events and a long streaming assistant message; the harness can close and reopen the client connection and restart the Host.</li><li><strong>Steps</strong>: 1) Record the last applied cursor. 2) Disconnect in the middle of a streaming message. 3) Reconnect and subscribe with <code>after</code>. 4) Repeat after evicting the cursor from the bounded replay window. 5) Restart the Host during an idle period and reconnect with the old cursor. 6) Repeat step 3 over <code>RACP-HTTP</code> using <code>Last-Event-ID</code> in the <code>epoch:sequence</code> form.</li><li><strong>Expected</strong>: A retained cursor replays every later durable event exactly once and no delta; the snapshot&#39;s <code>activeItems</code> carry the text streamed before the disconnect; an evicted cursor returns <code>resync.required</code> with a complete snapshot; a cursor from the previous epoch returns <code>CURSOR_EXPIRED</code> and a snapshot with a new epoch; a durable gap never causes the client to guess or apply out-of-order state; and the SSE path behaves identically.</li><li><strong>Specs linked</strong>: <code>03-runtime/19-remote-agent-control-protocol.md</code> §§5.3, 5.4, 7.2, and 8, <code>02-architecture/05-remote-agent-control.md</code> §8</li><li><strong>Acceptance</strong>: Recovery, Quality</li><li><strong>Milestone</strong>: Post-MVP</li><li><strong>Status</strong>: Draft; remote harness required</li></ul><h4 id="e2e-224-remote-approvals-carry-the-local-vocabulary-and-stay-host-owned" tabindex="-1">E2E-224: Remote approvals carry the local vocabulary and stay host-owned <a class="header-anchor" href="#e2e-224-remote-approvals-carry-the-local-vocabulary-and-stay-host-owned" aria-label="Permalink to &quot;E2E-224: Remote approvals carry the local vocabulary and stay host-owned&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A Session policy requires a tool approval; the fixture Agent submits a Plan and asks one multi-select asktool question in the same run; Host policy allows remote session grants; a second Session is configured with <code>permissionMode: &quot;auto&quot;</code> and the Host ceiling is <code>ask</code>.</li><li><strong>Steps</strong>: 1) Start the turn from a controller. 2) Observe the tool approval request. 3) Attempt a decision from a viewer, then with a stale revision. 4) Resolve from an authorized approver with <code>allow-session</code>. 5) Attach a third client after the Plan approval is raised and read its snapshot. 6) Resolve the Plan with <code>approve</code> and no <code>permissionMode</code>, then with <code>permissionMode: &quot;accept-edits&quot;</code>. 7) Answer the input request with one multi-select answer and one <code>null</code>. 8) Repeat step 4 after expiry. 9) Start a turn on the <code>auto</code> Session from a controller without <code>approver</code>.</li><li><strong>Expected</strong>: The viewer and stale responses fail closed with <code>FORBIDDEN</code> and <code>APPROVAL_STALE</code>; <code>allow-session</code> resumes the turn and a later call of the same tool in that Session needs no approval; the late client&#39;s snapshot lists the pending Plan approval; the mode-less approve is rejected and the explicit one queues execution in Agent mode with <code>accept-edits</code>; the desktop&#39;s own approval card closes when the remote decision lands; the asktool answers reach the Agent as the local <code>AskToolResolution</code>; expiry returns <code>APPROVAL_EXPIRED</code> and never executes the tool; and the <code>auto</code> Session turn reports <code>effectivePermissionMode: &quot;ask&quot;</code> and raises an approval while the durable session mode stays <code>auto</code>.</li><li><strong>Specs linked</strong>: <code>03-runtime/19-remote-agent-control-protocol.md</code> §§5.5, 7.3, 7.5, and 9, <code>05-security/02-remote-control-security.md</code> §§4 and 7, <code>03-runtime/10-session-state-machine.md</code></li><li><strong>Acceptance</strong>: E (tools &amp; permissions), Security, Recovery</li><li><strong>Milestone</strong>: Post-MVP</li><li><strong>Status</strong>: Draft; remote harness required</li></ul><h4 id="e2e-225-a-turn-continues-when-its-client-disconnects-and-the-queue-is-host-owned" tabindex="-1">E2E-225: A turn continues when its client disconnects and the queue is host-owned <a class="header-anchor" href="#e2e-225-a-turn-continues-when-its-client-disconnects-and-the-queue-is-host-owned" aria-label="Permalink to &quot;E2E-225: A turn continues when its client disconnects and the queue is host-owned&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Two authenticated controllers can access the same Session; the fixture Agent has a delayed deterministic turn; the local desktop renderer is open on the same Session.</li><li><strong>Steps</strong>: 1) Client A starts the turn. 2) Disconnect A while it is running. 3) Observe the Session from client B. 4) Reconnect A as a viewer. 5) From B, call <code>turn/start</code> with <code>admission: &quot;reject_if_busy&quot;</code>, then with <code>admission: &quot;queue&quot;</code> twice. 6) Cancel the second queued turn from B. 7) Call <code>turn/stop</code> from B. 8) Fill the queue to <code>maxQueuedTurnsPerSession</code> and start once more.</li><li><strong>Expected</strong>: The turn continues after A disconnects; B observes the same turn and durable sequence; A catches up by cursor; the first start returns <code>AGENT_BUSY</code>; the queued starts return <code>turn.queued</code> with positions and the desktop renderer shows the same queued prompts; the cancel emits <code>turn.canceled</code>; <code>turn/stop</code> ends the active turn as <code>completed</code> at the next boundary and the remaining queued turn starts; the overflowing start returns <code>AGENT_BUSY</code> with <code>details.queueFull</code>.</li><li><strong>Specs linked</strong>: <code>02-architecture/05-remote-agent-control.md</code> §§6, 9, and 10, <code>03-runtime/19-remote-agent-control-protocol.md</code> §§7.3–7.4 and 8</li><li><strong>Acceptance</strong>: C (conversation &amp; stream), Recovery</li><li><strong>Milestone</strong>: Post-MVP</li><li><strong>Status</strong>: Draft; remote harness required</li></ul><h4 id="e2e-226-roles-scopes-and-revocation-are-enforced" tabindex="-1">E2E-226: Roles, scopes, and revocation are enforced <a class="header-anchor" href="#e2e-226-roles-scopes-and-revocation-are-enforced" aria-label="Permalink to &quot;E2E-226: Roles, scopes, and revocation are enforced&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: One tenant, two Hosts, two Sessions, and principals with viewer, controller, approver, and owner roles exist. A multi-tenant harness profile, when available, adds a second tenant.</li><li><strong>Steps</strong>: 1) Try every catalog operation, including <code>session/history</code>, <code>turn/cancel</code>, and host-scope <code>events/subscribe</code>, with each role. 2) Substitute a Session, Host, and approval id from the other Host, and from the other tenant when the multi-tenant profile is active. 3) Queue a turn as a controller, then revoke that principal and retry its existing connection.</li><li><strong>Expected</strong>: The role matrix is enforced, including the <code>allow-session</code> policy row; foreign identifiers return <code>FORBIDDEN</code> or <code>NOT_FOUND</code> without existence leakage; revocation closes the connection, blocks new mutations, and cancels the revoked principal&#39;s queued turn; and audit records identify the denied principal without recording prompt or secret content. The cross-tenant cases are required only under the multi-tenant profile.</li><li><strong>Specs linked</strong>: <code>05-security/02-remote-control-security.md</code> §§3–9 and §11, <code>03-runtime/19-remote-agent-control-protocol.md</code> §§6 and 13</li><li><strong>Acceptance</strong>: Security, Quality</li><li><strong>Milestone</strong>: Post-MVP</li><li><strong>Status</strong>: Draft; remote harness required</li></ul><h4 id="e2e-227-the-host-link-relays-clients-behind-inbound-firewall-nat" tabindex="-1">E2E-227: The Host link relays clients behind inbound firewall/NAT <a class="header-anchor" href="#e2e-227-the-host-link-relays-clients-behind-inbound-firewall-nat" aria-label="Permalink to &quot;E2E-227: The Host link relays clients behind inbound firewall/NAT&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: The Agent Host runs behind a test firewall that rejects inbound connections. The Gateway is publicly reachable by two test clients.</li><li><strong>Steps</strong>: 1) Enroll the Host with a one-time credential. 2) Establish the outbound mTLS Host link. 3) Attach both clients and run a turn that raises a tool approval. 4) Answer the relayed server request from the client that received it, then attempt to answer it again from the other client. 5) Upload an attachment through the Gateway upload target and reference it from a turn. 6) Drop the link and allow it to reconnect. 7) Revoke the Host and attempt re-enrollment with the old credential.</li><li><strong>Expected</strong>: No inbound desktop port is opened; the Gateway routes only to the authenticated Host; the approval request is delivered on exactly one logical connection and the second answer returns the stored result with <code>alreadyResolved</code>; attachment bytes cross the link in bounded chunks, the Host verifies the hash, and the Gateway&#39;s copy is gone after <code>attachment/complete</code>; local execution continues across the link interruption and both clients resume from resumable cursors; reconnect does not duplicate the turn; and the revoked credential cannot re-enroll.</li><li><strong>Specs linked</strong>: <code>02-architecture/05-remote-agent-control.md</code> §5.3, <code>03-runtime/19-remote-agent-control-protocol.md</code> §§10 and 11.4, <code>05-security/02-remote-control-security.md</code> §§3.2 and 6, <code>06-delivery/07-remote-control-rollout.md</code> §2</li><li><strong>Acceptance</strong>: Security, Recovery, Quality</li><li><strong>Milestone</strong>: Post-MVP</li><li><strong>Status</strong>: Draft; unscheduled until the Gateway milestone is scheduled (D375)</li></ul><h4 id="e2e-228-shipped-bindings-and-browser-profiles-preserve-semantic-behavior" tabindex="-1">E2E-228: Shipped bindings and browser profiles preserve semantic behavior <a class="header-anchor" href="#e2e-228-shipped-bindings-and-browser-profiles-preserve-semantic-behavior" aria-label="Permalink to &quot;E2E-228: Shipped bindings and browser profiles preserve semantic behavior&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: The same deterministic command/event fixture is available through <code>RACP-WS</code> and <code>RACP-HTTP</code>, and through <code>RACP-GRPC</code> only if that reserved binding has shipped. A browser harness can hold a Gateway session cookie.</li><li><strong>Steps</strong>: 1) Run the fixture through each shipped binding on the header profile. 2) Disconnect during the event stream. 3) Retry a mutation with the same idempotency key. 4) Compare normalized responses, errors, durable sequences, and final snapshots. 5) From the browser harness, open the WebSocket and the SSE stream on the cookie profile with an allowed Origin, then with a disallowed Origin, then with a token in the URL, then send a mutation without a CSRF token.</li><li><strong>Expected</strong>: All shipped bindings accept and reject the same operations, preserve durable event order and terminal state, return the same semantic error codes, and produce one mutation result despite retries; the cookie profile succeeds only with an allowed Origin; the disallowed Origin, the URL token, and the CSRF-less mutation are rejected.</li><li><strong>Specs linked</strong>: <code>03-runtime/19-remote-agent-control-protocol.md</code> §§11 and 14, <code>05-security/02-remote-control-security.md</code> §§3.1 and 5, <code>06-delivery/07-remote-control-rollout.md</code> §§3–4</li><li><strong>Acceptance</strong>: Quality, Recovery, Security</li><li><strong>Milestone</strong>: Post-MVP</li><li><strong>Status</strong>: Draft; the browser-profile steps are unscheduled until the browser milestone is scheduled (D375); the parity steps run when a second binding ships</li></ul><h4 id="e2e-229-attachment-and-workspace-boundaries-are-enforced-remotely" tabindex="-1">E2E-229: Attachment and workspace boundaries are enforced remotely <a class="header-anchor" href="#e2e-229-attachment-and-workspace-boundaries-are-enforced-remotely" aria-label="Permalink to &quot;E2E-229: Attachment and workspace boundaries are enforced remotely&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A Session has a project root and private scratch root. A controller can upload one valid fixture and one invalid fixture, directly and through a Gateway relay.</li><li><strong>Steps</strong>: 1) Upload bytes with a correct hash and reference the attachment from a turn. 2) Repeat with a wrong hash, oversized body, local absolute path, <code>file://</code> URL, and an expired upload target. 3) Repeat step 2 through the Gateway relay. 4) Attempt a tool path outside the Session root. 5) Call <code>project/list</code> and <code>session/create</code> without a path.</li><li><strong>Expected</strong>: Only the verified attachment is accepted; invalid uploads fail before turn admission on both paths; no local client path reaches the Host; <code>project/list</code> returns labels and ids without absolute paths; <code>session/create</code> binds the project by id; and the existing <code>PATH_OUTSIDE_WORKSPACE</code> boundary remains authoritative.</li><li><strong>Specs linked</strong>: <code>03-runtime/19-remote-agent-control-protocol.md</code> §§5.7, 7.7, and 10, <code>05-security/02-remote-control-security.md</code> §6, <code>03-runtime/03-tools-and-permissions.md</code></li><li><strong>Acceptance</strong>: E (tools &amp; permissions), Security</li><li><strong>Milestone</strong>: Post-MVP</li><li><strong>Status</strong>: Draft; remote harness required</li></ul><h4 id="e2e-230-failure-recovery-does-not-replay-admitted-work" tabindex="-1">E2E-230: Failure recovery does not replay admitted work <a class="header-anchor" href="#e2e-230-failure-recovery-does-not-replay-admitted-work" aria-label="Permalink to &quot;E2E-230: Failure recovery does not replay admitted work&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A deterministic Host, Gateway, and client can inject process, link, and response-loss failures; one turn is running and two are queued.</li><li><strong>Steps</strong>: 1) Drop the response after <code>turn/start</code> is admitted. 2) Retry with the same idempotency key. 3) Crash the Host during the running turn. 4) Restart the Host and reconnect from the last cursor. 5) Drop an approval response after it is accepted and retry it.</li><li><strong>Expected</strong>: The first retry returns the original turn; execution occurs once; Host recovery marks the interrupted turn according to the local recovery policy; the reconnect receives a new epoch and a snapshot whose queue is empty, and neither queued turn is started or replayed; no completed item is replayed as a new turn; and the retried approval returns the stored decision with <code>alreadyResolved</code> without executing twice.</li><li><strong>Specs linked</strong>: <code>02-architecture/05-remote-agent-control.md</code> §10, <code>03-runtime/19-remote-agent-control-protocol.md</code> §§7–8 and 9.3, <code>06-delivery/07-remote-control-rollout.md</code> §4</li><li><strong>Acceptance</strong>: Recovery, Security, Quality</li><li><strong>Milestone</strong>: Post-MVP</li><li><strong>Status</strong>: Draft; remote harness required</li></ul><h4 id="e2e-231-the-desktop-drives-a-remote-host-over-an-ssh-tunnel" tabindex="-1">E2E-231: The desktop drives a remote Host over an SSH tunnel <a class="header-anchor" href="#e2e-231-the-desktop-drives-a-remote-host-over-an-ssh-tunnel" aria-label="Permalink to &quot;E2E-231: The desktop drives a remote Host over an SSH tunnel&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A Linux test machine runs <code>sshd</code> and holds a project the desktop can reach with the user&#39;s SSH key. A GitHub Releases fixture serves the <code>pi-host</code> bundle for that platform at the desktop&#39;s version, a bundle at another version, and a tampered bundle with a wrong checksum. The desktop has one local session open, one user MCP server configured, and one installed plugin whose tool requires workspace access.</li><li><strong>Steps</strong>: 1) Add the remote machine from the desktop and let the uploaded bootstrap script download, verify, and start <code>pi-host</code> over SSH. 2) Observe the pairing exchange and the resulting device token. 3) Create a session under a remote project through <code>project/list</code> and <code>session/create</code>. 4) Start a turn whose fixture reads, edits, and runs a command in the remote project, and approve the command from the desktop card. 5) Switch the session to Plan mode and back with <code>session/configure</code> while idle, then attempt it while a turn runs. 6) Open the files tab and the diff tab for the remote session. 7) Advertise relay from the desktop, run a turn that calls the desktop MCP tool, then close the desktop during a second call. 8) Open a terminal on the remote session and run a command. 9) Kill the SSH session mid-turn with the terminal open, restore it, and let the desktop reconnect. 10) Inspect the remote tool catalog. 11) Attempt to connect from a non-loopback address on the remote machine, then with a reused pairing token. 12) Point the bootstrap at the tampered bundle, then at the other version, and reconnect.</li><li><strong>Expected</strong>: Files change only on the remote machine and the command runs there; the approval card appears in the desktop with the local vocabulary; the remote host-core binds loopback only; <code>session/configure</code> succeeds while idle and returns <code>CONFLICT</code> while running; files and diff come from the remote session root and a path outside it returns <code>PATH_OUTSIDE_WORKSPACE</code>; the desktop MCP tool executes on the desktop and its result reaches the remote transcript, while the second call fails with <code>TOOL_FAILED</code> and the turn continues; the terminal runs on the remote machine inside the session root; the turn continues through the SSH drop, the desktop resumes by cursor without a duplicate, and the terminal output resumes from the replay ring; the remote catalog lists the relayed MCP tool but not the workspace-requiring plugin tool; the non-loopback peer and the reused pairing token are rejected; the tampered bundle is refused before start with a Settings toast that names the checksum failure rather than a bare SSH exit code; a refused SSH login surfaces ssh&#39;s last stderr line in that toast; the version mismatch returns <code>PROTOCOL_MISMATCH</code> and offers the re-download; and the local session is untouched throughout.</li><li><strong>Specs linked</strong>: <code>02-architecture/05-remote-agent-control.md</code> §§5.2 and 6.3, <code>03-runtime/19-remote-agent-control-protocol.md</code> §§6.2, 9.4, and 11.1, <code>05-security/02-remote-control-security.md</code> §§3.4, 4.3, 5.1, and 7, <code>06-delivery/07-remote-control-rollout.md</code> §2</li><li><strong>Acceptance</strong>: E (tools &amp; permissions), Security, Recovery, Quality</li><li><strong>Milestone</strong>: Post-MVP (rollout R2)</li><li><strong>Status</strong>: Draft; remote harness with a Linux SSH target required</li></ul><h4 id="e2e-remote-host-ssh-password-authentication" tabindex="-1">E2E-REMOTE-HOST-ssh-password-authentication <a class="header-anchor" href="#e2e-remote-host-ssh-password-authentication" aria-label="Permalink to &quot;E2E-REMOTE-HOST-ssh-password-authentication&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Developer mode is enabled (Settings → Info → Developer), so the Remote Hosts destination exists. A Linux test machine runs <code>sshd</code> with <code>PasswordAuthentication yes</code> and <code>PubkeyAuthentication no</code>, so the login is only reachable with a password; the user&#39;s local SSH agent holds no usable key for it. A GitHub Releases fixture serves the <code>pi-host</code> bundle for that platform at the desktop&#39;s version. A second machine accepts the user&#39;s key only.</li><li><strong>Steps</strong>: 1) In Settings → Remote Hosts, open the Add form&#39;s SSH tab and install over SSH with key authentication and confirm the form shows the identity-file field. 2) Switch the authentication mode to password, confirm the identity-file field is replaced by a masked password field, and reveal it once. 3) Try to submit an empty password. 4) Point the form at the password-only machine, enter the password, and bootstrap it. 5) Quit the desktop, relaunch it, and let the host reconnect. 6) Change the saved password to a wrong value through a re-pair, then force a reconnect. 7) Bootstrap the key-only machine with a password supplied. 8) Inspect the desktop&#39;s process arguments and <code>remote-hosts.json</code> while a password-authenticated host is connected. 9) Attempt to submit a password containing a newline through the IPC channel directly.</li><li><strong>Expected</strong>: Key mode is byte-for-byte today&#39;s behaviour — <code>BatchMode=yes</code> and no credential file anywhere. In password mode the password field replaces the identity-file field, the reveal toggle shows and re-hides the value, and an empty password keeps the submit disabled. The password-only machine installs and pairs. After the relaunch the host reconnects with no prompt, proving the saved credential is used. The wrong password yields exactly one failed attempt per connect (no helper-driven retry storm) and the host shows as offline rather than crashing the app or showing a rejected-login dialog. The Settings toast carries ssh&#39;s last stderr line (for example <code>Permission denied</code>), not a bare exit code 255. The key-only machine fails under password mode because that mode sets <code>PubkeyAuthentication=no</code>; a user with a key picks key mode. No <code>ssh</code> argument contains the password, no askpass file or directory remains after the connect completes, and the on-disk record stores the password only as keychain ciphertext — never in cleartext. A password containing a newline is refused with <code>INVALID_ARGUMENT</code> before any remote command runs.</li><li><strong>Specs linked</strong>: <code>05-security/02-remote-control-security.md</code> §§3.4 and 13 (gate 21), <code>02-architecture/05-remote-agent-control.md</code> §5.2, <code>06-delivery/07-remote-control-rollout.md</code> §2</li><li><strong>Acceptance</strong>: Security, D (surfaces), Quality</li><li><strong>Milestone</strong>: Post-MVP (rollout R2b)</li><li><strong>Status</strong>: Draft; remote harness with a password-only Linux SSH target required. The credential seam itself is covered offline by <code>apps/desktop/test/remote-host-ssh-password.test.mjs</code>.</li></ul><h4 id="e2e-remote-host-settings-compact-inventory" tabindex="-1">E2E-REMOTE-HOST-settings-compact-inventory <a class="header-anchor" href="#e2e-remote-host-settings-compact-inventory" aria-label="Permalink to &quot;E2E-REMOTE-HOST-settings-compact-inventory&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: The desktop Settings window can open. No paired remote host is required. Developer mode is off in persisted settings.</li><li><strong>Steps</strong>: 1) Open Settings and confirm the rail has no Remote Hosts row and that searching settings for it returns nothing. 2) Enable developer mode in Info → Developer and confirm Remote Hosts appears on the rail between Projects and Info with an Experimental badge. 3) Open it and confirm the page title carries the same badge, with a host inventory and one Add form (SSH / Pair) and no instructional copy. 4) Switch Add to Pair and back to SSH; confirm both forms stay filled. 5) Disable developer mode while the destination is open and confirm the page returns to General and the rail row is gone.</li><li><strong>Expected</strong>: The whole destination is marked Experimental and is reachable only while developer mode is on; the rail row, the page, and settings search add and drop it together. Field labels and placeholders remain; overview, body, and hint copy are absent. There is no Experimental switch list. SSH stays the default Add tab. Pairing and SSH bootstrap keep their existing success and failure toasts.</li><li><strong>Specs linked</strong>: <code>04-ux/06-settings-ia.md</code> §1, §3, <code>06-delivery/07-remote-control-rollout.md</code> §2 R2, <code>02-architecture/05-remote-agent-control.md</code> §5.2</li><li><strong>Acceptance</strong>: D (surfaces), Quality</li><li><strong>Milestone</strong>: Post-MVP (rollout R2b)</li><li><strong>Status</strong>: Draft; covered offline by <code>apps/desktop/test/settings-remote-hosts.test.mjs</code> and <code>apps/desktop/test/settings-developer-only-destinations.test.mjs</code>.</li></ul><h4 id="e2e-232-the-outbound-messaging-integration-relays-events-and-commands" tabindex="-1">E2E-232: The outbound messaging integration relays events and commands <a class="header-anchor" href="#e2e-232-the-outbound-messaging-integration-relays-events-and-commands" aria-label="Permalink to &quot;E2E-232: The outbound messaging integration relays events and commands&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A Host runs with the integration adapter configured against a webhook sink and a long-polling bot fixture, with one linked chat and one unlinked chat. No inbound port is open on the Host machine.</li><li><strong>Steps</strong>: 1) Run a turn to completion. 2) Start a turn that raises a tool approval and leave it pending. 3) Send the stop command from the linked chat during a running turn. 4) Send the same command from the unlinked chat. 5) Make the webhook sink return errors for one minute, then recover. 6) Inspect every payload delivered.</li><li><strong>Expected</strong>: The sink and the bot receive redacted summaries of <code>turn.completed</code> and <code>approval.requested</code> with ids and bounded summary text only; the linked chat&#39;s stop maps to <code>turn/stop</code> under the linked principal&#39;s roles and the turn ends at the next boundary; the unlinked chat&#39;s command has no effect and is audited; delivery failures are retried within the bound and never delay or block the turn; and the Host opens no listener.</li><li><strong>Specs linked</strong>: <code>02-architecture/05-remote-agent-control.md</code> §4, <code>06-delivery/07-remote-control-rollout.md</code> §2 (R3), <code>05-security/02-remote-control-security.md</code> §10</li><li><strong>Acceptance</strong>: Security, Quality</li><li><strong>Milestone</strong>: Post-MVP (rollout R3)</li><li><strong>Status</strong>: Draft; integration fixture required</li></ul><h2 id="trusted-extension-scenarios-r7-v1" tabindex="-1">Trusted extension scenarios (R7 v1) <a class="header-anchor" href="#trusted-extension-scenarios-r7-v1" aria-label="Permalink to &quot;Trusted extension scenarios (R7 v1)&quot;">​</a></h2><p>The following scenarios are the acceptance targets of D387 / ADR 0214 and <code>07-plugins/16-trusted-extensions.md</code>. The headless runner generates its six plugin-form fixtures in an isolated temporary directory at runtime.</p><h4 id="e2e-241-discovery-lists-trusted-extensions-and-enablement-is-explicit" tabindex="-1">E2E-241: Discovery lists trusted extensions and enablement is explicit <a class="header-anchor" href="#e2e-241-discovery-lists-trusted-extensions-and-enablement-is-explicit" aria-label="Permalink to &quot;E2E-241: Discovery lists trusted extensions and enablement is explicit&quot;">​</a></h4><ul><li><strong>Preconditions</strong> (D388): a pi extension directory <code>hello/</code> with <code>index.ts</code>, and a plugin package declaring <code>contributes.agentExtensions</code> with <code>agent.extension</code> whose activation scope is limited to the fixture project; a second project outside that scope.</li><li><strong>Steps</strong>: 1) Plugins → Import pi extension, accept the confirm, pick <code>hello/</code>. 2) Send a prompt in the fixture project. 3) Open the imported plugin&#39;s row details. 4) Disable the plugin and send a prompt. 5) Switch to the second project and send a prompt. 6) Load a plugin whose manifest lists <code>agentExtensions</code> without the permission.</li><li><strong>Expected</strong>: The import creates <code>plugins/imported/hello</code> with a manifest holding <code>agent.extension</code> and lists the plugin with the <code>agentExtension</code> capability and the permission chip; the next turn lists its tools and the row shows <code>loaded</code> with the registered names; disabling the plugin retires the runtime and the next turn has no extension tools; the project-scoped plugin contributes nothing outside its projects; the manifest without the permission is rejected as <code>PLUGIN_INVALID</code>; <code>~/.pi/agent/settings.json</code> is never written.</li><li><strong>Specs linked</strong>: <code>07-plugins/16-trusted-extensions.md</code> §2, §3, §11; D007; D387</li><li><strong>Acceptance</strong>: Security, Quality</li><li><strong>Milestone</strong>: Post-MVP (R7 v1)</li><li><strong>Status</strong>: Partially automated (<code>pnpm test:e2e:trusted-extensions</code>); the headless journey covers plugin discovery/projection, project scope, load state, and diagnostics, while native picker import and explicit enablement remain renderer/platform validation.</li></ul><h4 id="e2e-plugin-imported-pi-package-wrapper-imported-package-module-types-preserve-plugin-initialization" tabindex="-1">E2E-PLUGIN-imported-pi-package-wrapper: Imported package module types preserve plugin initialization <a class="header-anchor" href="#e2e-plugin-imported-pi-package-wrapper-imported-package-module-types-preserve-plugin-initialization" aria-label="Permalink to &quot;E2E-PLUGIN-imported-pi-package-wrapper: Imported package module types preserve plugin initialization&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Isolated local Pi packages declare <code>type: module</code>, <code>type: commonjs</code>, or no <code>type</code>; each has an extension and skill contributions. A fourth fixture represents an older imported ESM package with the generated CommonJS <code>main.js</code> and matching manifest.</li><li><strong>Steps</strong>: Run <code>node --test apps/desktop/test/imported-package-skills-runtime.test.mjs</code>. Generate each plugin through the production importer, load it through <code>PluginRuntime</code> and the real child-process plugin host, read its skill catalog and bodies, and inspect its declared extension. Re-import the older fixture without loading it, then load the older fixture itself.</li><li><strong>Expected</strong>: Each new manifest points to an existing <code>main.cjs</code>; initialization succeeds for all three package types. Source and both copied <code>package.json</code> files retain the same bytes. The repeated import has a distinct path/id and loads successfully; generating it leaves the older manifest, wrapper, and package files unchanged. Loading the older fixture rewrites its generated <code>main.js</code> in place to <code>main.cjs</code>, keeps the copied <code>package.json</code> bytes, and initializes. A customized <code>main.js</code> is not rewritten.</li><li><strong>Specs linked</strong>: <code>07-plugins/16-trusted-extensions.md</code> §3.2; ADR 0215.</li><li><strong>Acceptance</strong>: Quality</li><li><strong>Status</strong>: Automated import-to-plugin-host fixture. Run against the committed task candidate after incorporating the latest <code>origin/main</code>; record the tested candidate, base, result, and environment in delivery evidence. Native picker, npm dependency installation, Windows execution, and third-party extension execution during a provider turn are not covered by this fixture.</li></ul><h4 id="e2e-plugin-imported-pi-package-skills-explicit-package-import-exposes-skills-through-plugin-grants" tabindex="-1">E2E-PLUGIN-imported-pi-package-skills: Explicit package import exposes skills through plugin grants <a class="header-anchor" href="#e2e-plugin-imported-pi-package-skills-explicit-package-import-exposes-skills-through-plugin-grants" aria-label="Permalink to &quot;E2E-PLUGIN-imported-pi-package-skills: Explicit package import exposes skills through plugin grants&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A local fixture package under an npm-style <code>node_modules/@fixture/package-skills</code> path declares <code>pi.extensions</code> and <code>pi.skills</code>. Its skills include a direct Markdown file, a directory with <code>SKILL.md</code>, and a collection containing two different <code>SKILL.md</code> files. Reference files, assets, a <code>node_modules-note.txt</code> resource, and an internal <code>node_modules</code> dependency are present. A second fixture declares only <code>pi.skills</code>. No downloaded third-party code or dependency installation is needed.</li><li><strong>Steps</strong>: <ol><li>Choose the mixed package through Plugins → Import pi extension and inspect the generated manifest and copied resources. For headless validation, pass the explicit selected path to the same importer.</li><li>Load the generated directory in the real <code>PluginRuntime</code>; inspect <code>getSkills()</code> and read every document with <code>loadSkillBody(id)</code>.</li><li>Reload with only <code>agent.extension</code> granted, then reload with only <code>agent.prompt.inject</code> granted. Unload the plugin and try the old skill IDs.</li><li>Import the skill-only package; ensure a helper <code>index.js</code> is not treated as an extension. Install a generated two-skill fixture through the real Host <code>plugins.installFromPath</code>, list it, read its installed manifest and skill files, then uninstall it.</li><li>Attempt declarations with absolute paths, <code>..</code>, missing or unsupported files, an internal dependency path, or descendant symbolic links. Exceed the 32-skill or 256-directory scan limit and inspect failure cleanup.</li></ol></li><li><strong>Expected</strong>: All four declared skills appear with independent stable IDs, including the repeated <code>SKILL.md</code> basenames, and loading them returns the correct body with frontmatter removed. The imported extension remains a separate contribution. Resources and their relative paths survive copying; npm installation ancestors do not suppress the package, and only dependency directory segments within the selection are excluded. Missing skill grants leave no skill catalog entries and produce the existing permission audit; grants can be restored without changing IDs. Unload removes both catalogs, and old skill IDs return <code>NOT_FOUND</code>. Skill-only imports declare only <code>agent.prompt.inject</code>, and Host reports the <code>skills</code> capability while retaining both skill documents. Invalid declarations fail without importing outside data or retaining a partial copied plugin. Nothing automatically imports <code>~/.pi</code> or runs npm lifecycle scripts; the explicit dependency path may run the bounded npm installer described in E2E-PLUGIN-import-extension-installs-dependencies.</li><li><strong>Specs linked</strong>: <code>07-plugins/16-trusted-extensions.md</code> §3.2; <code>07-plugins/02-plugin-manifest-schema.md</code>; D007</li><li><strong>Acceptance</strong>: E (tools &amp; permissions), G (plugins), Quality</li><li><strong>Milestone</strong>: Post-MVP (R7 v1)</li><li><strong>Status</strong>: Partially automated. <code>imported-package-skills.test.mjs</code> covers import discovery, resource copying, grants, and invalid-path handling. <code>imported-package-skills-runtime.test.mjs</code> drives the generated no-op plugin through the real plugin host subprocess and verifies catalog/body loading, grant removal/restoration, and unload. On 2026-09-13, a separate temporary two-skill fixture passed real Host <code>plugins.installFromPath</code> → <code>plugins.list</code> → installed manifest/body reads → <code>plugins.uninstall</code>; it reported only <code>agent.prompt.inject</code> and the <code>skills</code> capability. The native picker, rendered plugin row, and a provider turn invoking the imported Skill have not been executed for this scenario; no full desktop journey is claimed.</li></ul><h4 id="e2e-242-extension-tools-and-hooks-take-effect-in-a-turn" tabindex="-1">E2E-242: Extension tools and hooks take effect in a turn <a class="header-anchor" href="#e2e-242-extension-tools-and-hooks-take-effect-in-a-turn" aria-label="Permalink to &quot;E2E-242: Extension tools and hooks take effect in a turn&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: An enabled fixture extension that registers tool <code>fx_add</code>, handles <code>before_agent_start</code> by appending a marker to the system prompt, <code>tool_call</code> by blocking <code>bash</code> with a reason, and <code>tool_result</code> by replacing <code>fx_add</code> output.</li><li><strong>Steps</strong>: 1) Start a turn in Agent mode whose fixture model calls <code>fx_add</code> then <code>bash</code>. 2) Inspect the provider request. 3) Inspect the tool results. 4) Switch to Plan mode and repeat. 5) Register a second extension declaring a tool named <code>read</code>.</li><li><strong>Expected</strong>: The system prompt carries the marker; <code>fx_add</code> executes in the sidecar with no permission prompt and its result is the replaced value; <code>bash</code> is blocked with the extension&#39;s reason and the block is visible in the transcript; an audit line records extension id, tool name, and duration and no parameters; in Plan mode <code>fx_add</code> follows the non-core mode gate; the <code>read</code> collision is rejected with a diagnostic and the core tool is unchanged.</li><li><strong>Specs linked</strong>: <code>07-plugins/16-trusted-extensions.md</code> §6, §7; ADR 0214</li><li><strong>Acceptance</strong>: B (agent), Security, Quality</li><li><strong>Milestone</strong>: Post-MVP (R7 v1)</li><li><strong>Status</strong>: Partially automated (<code>pnpm test:e2e:trusted-extensions</code>); Agent-mode tool dispatch, ToolSearch deferral, hooks, blocking, and result replacement pass, while Plan-mode gating and core-tool collision remain additional validation.</li></ul><h4 id="e2e-trusted-extension-custom-agent-stream-and-binding-plugin-owned-agent-streams-and-session-binding" tabindex="-1">E2E-TRUSTED-EXTENSION-custom-agent-stream-and-binding: Plugin-owned agent streams and session binding <a class="header-anchor" href="#e2e-trusted-extension-custom-agent-stream-and-binding-plugin-owned-agent-streams-and-session-binding" aria-label="Permalink to &quot;E2E-TRUSTED-EXTENSION-custom-agent-stream-and-binding: Plugin-owned agent streams and session binding&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: An enabled trusted extension calls <code>registerAgent</code> with one model and a fixture <code>stream</code>/<code>complete</code> implementation. The fixture provider has no Host provider row or Host secret.</li><li><strong>Steps</strong>: 1) Load the extension and inspect <code>ctx.modelRegistry</code> for the redacted model. 2) Call <code>pi.setModel(model)</code> while idle. 3) Run a turn and inspect the callback&#39;s model/context/options. 4) Restart or create the next turn. 5) Attempt to read Host provider keys/secret refs through the registry.</li><li><strong>Expected</strong>: <code>registerAgent</code> appears in the loaded contract and the model is selectable; <code>setModel</code> persists only the current session binding under an <code>extension-agent:</code> provider id; the plugin callback streams the assistant response and receives cancellation; the next turn reloads the extension and restores the same agent implementation; the registry exposes model metadata and auth availability but no Host key, secret ref, OAuth token or arbitrary Host headers. <code>registerProvider</code> with the same plugin-owned stream shape has equivalent behavior.</li><li><strong>Specs linked</strong>: <code>07-plugins/16-trusted-extensions.md</code> §5, §10; ADR 0258; D426</li><li><strong>Acceptance</strong>: B (agent), C (conversation &amp; stream), Security, Quality</li><li><strong>Milestone</strong>: Post-MVP (R7 v1)</li><li><strong>Status</strong>: Partially automated (<code>pnpm test:e2e:trusted-extensions</code>): the loaded contract, both registration forms (<code>registerAgent</code> and the <code>registerProvider</code> alias, in each call form), the custom-agent names on the plugin row, the redacted registry, idle <code>setModel</code> persistence under the <code>extension-agent:</code> id, and the next turn restored through the plugin&#39;s own transport pass. Cancellation through <code>options.signal</code>, the <code>stream</code>-form callback&#39;s model/context/options inspection, and cross-session module sharing remain additional validation.</li></ul><h4 id="e2e-243-extension-commands-and-ui-prompts-round-trip-through-the-renderer" tabindex="-1">E2E-243: Extension commands and UI prompts round-trip through the renderer <a class="header-anchor" href="#e2e-243-extension-commands-and-ui-prompts-round-trip-through-the-renderer" aria-label="Permalink to &quot;E2E-243: Extension commands and UI prompts round-trip through the renderer&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: An enabled fixture extension registering command <code>greet</code> that calls <code>ui.input</code>, then <code>ui.select</code>, then <code>ui.confirm</code>, then <code>ui.notify</code>, and renames the session.</li><li><strong>Steps</strong>: 1) Open global search and inspect the Commands section. 2) Run <code>/greet</code> from the composer. 3) Answer each prompt. 4) Run <code>/greet</code> again and abort the turn while the input prompt is open. 5) Run <code>/greet</code> with no active session. 6) Attach a remote controller (fixture) and run <code>/greet</code>.</li><li><strong>Expected</strong>: <code>greet</code> is listed after built-in and plugin commands with the extension label; each prompt shows the extension label and path; answers reach the extension in order; the toast appears; the session is renamed and <code>session_info_changed</code> fires; the aborted prompt resolves <code>undefined</code> and the command ends; with no session the entry is disabled with a tooltip; under remote control the prompt fails with <code>UNSUPPORTED</code> and the command reports it.</li><li><strong>Specs linked</strong>: <code>07-plugins/16-trusted-extensions.md</code> §8, §9, §10; <code>07-plugins/09-plugin-command-palette.md</code></li><li><strong>Acceptance</strong>: A (app control), Quality</li><li><strong>Milestone</strong>: Post-MVP (R7 v1)</li><li><strong>Status</strong>: Partially automated (<code>pnpm test:e2e:trusted-extensions</code>); global/composer command discovery, prompt broker round-trip, abort, session rename, exec, and Host-owned queue pass, while no-session and remote-control cases remain additional validation.</li></ul><h4 id="e2e-244-unsupported-apis-load-errors-and-handler-timeouts-degrade-to-diagnostics" tabindex="-1">E2E-244: Unsupported APIs, load errors, and handler timeouts degrade to diagnostics <a class="header-anchor" href="#e2e-244-unsupported-apis-load-errors-and-handler-timeouts-degrade-to-diagnostics" aria-label="Permalink to &quot;E2E-244: Unsupported APIs, load errors, and handler timeouts degrade to diagnostics&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Three enabled fixture extensions: one importing <code>@earendil-works/pi-tui</code> at top level and calling <code>ui.setWidget</code>; one whose module throws at load; one whose <code>context</code> handler never resolves.</li><li><strong>Steps</strong>: 1) Start a turn. 2) Open the diagnostics drawer for each entry. 3) Wait past the 30 s handler limit. 4) Disable the throwing extension and start another turn.</li><li><strong>Expected</strong>: The pi-tui import succeeds, <code>setWidget</code> returns an inert <code>dispose</code>, and one diagnostic per member is recorded; the throwing extension shows <code>error</code> with message and stack, the composer shows a one-line notice, and the other extensions still load; the stalled handler is abandoned after 30 s with a diagnostic and the turn completes with the unmodified context; after disabling, the notice disappears at the next turn boundary and no running turn was interrupted.</li><li><strong>Specs linked</strong>: <code>07-plugins/16-trusted-extensions.md</code> §4.2, §4.4, §5, §6</li><li><strong>Acceptance</strong>: Quality</li><li><strong>Milestone</strong>: Post-MVP (R7 v1)</li><li><strong>Status</strong>: Partially automated (<code>pnpm test:e2e:trusted-extensions</code>); load errors and inert terminal-UI APIs degrade to diagnostics, while the stalled-handler timeout and disable-at-boundary journey remain additional validation.</li></ul><h4 id="e2e-245-the-packaged-sidecar-loads-a-typescript-extension-through-jiti" tabindex="-1">E2E-245: The packaged sidecar loads a TypeScript extension through jiti <a class="header-anchor" href="#e2e-245-the-packaged-sidecar-loads-a-typescript-extension-through-jiti" aria-label="Permalink to &quot;E2E-245: The packaged sidecar loads a TypeScript extension through jiti&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A packaged build of the desktop app; a fixture <code>~/.pi/agent/extensions/typed.ts</code> that uses TypeScript syntax, imports <code>@earendil-works/pi-coding-agent</code> and <code>typebox</code>, and registers a tool.</li><li><strong>Steps</strong>: 1) Launch the packaged app. 2) Enable <code>typed.ts</code>. 3) Start a turn whose fixture model calls the tool. 4) Inspect the sidecar bundle manifest for the three pi package versions.</li><li><strong>Expected</strong>: The extension loads without a transpile or resolution error; the aliased imports resolve to the sidecar&#39;s copies; the tool executes; the three pi package versions are identical and the CI version-lock check passes.</li><li><strong>Specs linked</strong>: <code>07-plugins/16-trusted-extensions.md</code> §4.2, §13; ADR 0214</li><li><strong>Acceptance</strong>: Quality, Release</li><li><strong>Milestone</strong>: Post-MVP (R7 v1, delivered first as the bundling spike)</li><li><strong>Status</strong>: Unit-covered by <code>packages/agent-runtime/src/extensions/bundle.test.ts</code>: an esbuild bundle run from a temp directory, plus a kernel-loader case that loads a <code>typebox</code>-importing extension through <code>@earendil-works/pi-coding-agent</code>&#39;s own loader from a bundle with no <code>node_modules</code> above it; the packaged-app jiti journey remains Draft and is not faked by the headless runner.</li></ul><h4 id="e2e-plugin-import-extension-installs-dependencies-importing-an-extension-with-npm-dependencies-installs-them-before-first-load" tabindex="-1">E2E-PLUGIN-import-extension-installs-dependencies: Importing an extension with npm dependencies installs them before first load <a class="header-anchor" href="#e2e-plugin-import-extension-installs-dependencies-importing-an-extension-with-npm-dependencies-installs-them-before-first-load" aria-label="Permalink to &quot;E2E-PLUGIN-import-extension-installs-dependencies: Importing an extension with npm dependencies installs them before first load&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A local pi extension package with <code>package.json</code>, <code>pi.extensions</code>, a pinned pure-JavaScript <code>is-number@7.0.0</code> dependency, a <code>workspaces</code> field, and no <code>node_modules</code>; the built workspace packages and npm are available.</li><li><strong>Steps</strong>: 1) Generate the imported plugin from the local directory. 2) Run the real bounded installer. 3) Inspect the copied package, lockfile, installed module, lifecycle marker, and trusted-extension load report.</li><li><strong>Expected</strong>: The plugin root holds the copied <code>package.json</code> with <code>workspaces</code> stripped. The installer runs the two registry-only, <code>--ignore-scripts</code> npm steps; every lockfile <code>resolved</code> URL is registry-only, <code>node_modules/is-number</code> exists, no lifecycle marker is written, and the trusted-extension runner reports <code>loaded</code> with the dependency-backed command registered.</li><li><strong>Specs linked</strong>: <code>07-plugins/16-trusted-extensions.md</code> §3.2, §10.2; ADR 0244</li><li><strong>Acceptance</strong>: Security, Quality</li><li><strong>Milestone</strong>: Post-MVP (R7 v1)</li><li><strong>Status</strong>: Automated by <code>pnpm test:e2e:plugin-import-deps</code> for the deterministic installer boundary; the full picker/renderer/turn journey remains a separate validation surface.</li></ul><h4 id="e2e-plugin-import-extension-reports-missing-dependency-a-failed-dependency-install-or-unloadable-dependency-is-surfaced-never-silent" tabindex="-1">E2E-PLUGIN-import-extension-reports-missing-dependency: A failed dependency install or unloadable dependency is surfaced, never silent <a class="header-anchor" href="#e2e-plugin-import-extension-reports-missing-dependency-a-failed-dependency-install-or-unloadable-dependency-is-surfaced-never-silent" aria-label="Permalink to &quot;E2E-PLUGIN-import-extension-reports-missing-dependency: A failed dependency install or unloadable dependency is surfaced, never silent&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Three local pi extension packages whose <code>package.json</code> dependencies use unsupported <code>file:</code>, git, and HTTP tarball sources; none has <code>node_modules</code> or a lockfile.</li><li><strong>Steps</strong>: 1) Generate each imported plugin. 2) Invoke the real dependency installer. 3) Inspect the returned error and the generated plugin directory.</li><li><strong>Expected</strong>: Each failure is explicit and occurs before npm starts; the imported plugin and manifest remain registered, while no loadable <code>node_modules</code> or generated lockfile remains. The renderer toast/load-error journey is covered separately.</li><li><strong>Specs linked</strong>: <code>07-plugins/16-trusted-extensions.md</code> §3.2, §4.4, §10.2; ADR 0244</li><li><strong>Acceptance</strong>: Security, Quality</li><li><strong>Milestone</strong>: Post-MVP (R7 v1)</li><li><strong>Status</strong>: Automated by <code>pnpm test:e2e:plugin-import-deps</code> for the deterministic registry-source rejection boundary; renderer warning-toast and <code>load_error</code> behavior remains a separate validation surface.</li></ul><h4 id="e2e-plugin-import-extension-recovers-missing-npm-choose-a-trusted-npm-installation-and-resume-the-same-import" tabindex="-1">E2E-PLUGIN-import-extension-recovers-missing-npm: Choose a trusted npm installation and resume the same import <a class="header-anchor" href="#e2e-plugin-import-extension-recovers-missing-npm-choose-a-trusted-npm-installation-and-resume-the-same-import" aria-label="Permalink to &quot;E2E-PLUGIN-import-extension-recovers-missing-npm: Choose a trusted npm installation and resume the same import&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A desktop build with an isolated data directory; npm/Node.js unavailable on its inherited <code>PATH</code>; a local extension declaring a pinned registry dependency and an install-script marker; a trusted Node.js/npm installation outside <code>PATH</code> (on macOS, include a hidden <code>~/.nvm</code> directory).</li><li><strong>Steps</strong>: 1) Import the package through Plugins and accept the trust confirm. 2) In the native missing-npm message choose &quot;Choose npm&quot;. Select an invalid executable, then an npm without usable Node.js; choose again after each warning. 3) Select working npm alongside <code>node</code>. Inspect the resulting plugin and saved path. 4) Restart the app and import another package; then remove or move the saved executable and repeat. 5) Repeat with Cancel in the message box, picker, and invalid-selection warning. 6) Make saving the path fail while keeping the plugin destination writable and retry with a valid selection. 7) With working npm, force an ordinary registry/network failure. Repeat dialog checks in all eight shipped locales.</li><li><strong>Expected</strong>: Only structured npm/Node unavailability prompts for a path. Native copy explains trusted Node.js, installing Node.js if absent, and remembering the selection; hidden directories are visible. Invalid selections are rejected by bounded version checks and never saved; choose-again/cancel stays available. A valid choice resumes installation in the original generated directory with the original plugin id and writes <code>&lt;dataDir&gt;/npm-path.json</code> atomically. Restart reuses the validated path without prompting; a stale saved path prompts again. Save failure warns but the current install still uses the valid choice. Every completion, cancellation, and dependency error loads/registers that generated plugin exactly once without recopying it; dependency failures remain visible. Network/registry failures never open the npm picker. Only the install child gains the selected directory in <code>PATH</code>; shell probing, global environment changes, credential inheritance, and lifecycle marker execution do not occur. The registry-only proxy and two-step dependency validation remain enforced. Dialogs use localized standalone bodies and existing localized Cancel, with dynamic diagnostics appended on a new line.</li><li><strong>Specs linked</strong>: <code>07-plugins/16-trusted-extensions.md</code> §3.2; ADR 0244</li><li><strong>Acceptance</strong>: F, G, Security, Quality</li><li><strong>Milestone</strong>: Post-MVP (R7 v1)</li><li><strong>Status</strong>: Automated at the native-dialog I/O boundary by <code>pnpm test:e2e:plugin-import-deps</code>: the real import handler and npm installer recover from an empty PATH, keep registry-only resolution and disabled scripts, persist the selected path, and register the same plugin once. The desktop <code>npm-install-recovery.test.mjs</code> suite covers cancellation, invalid/stale paths, preference reload, save failure, localization, and ordinary network errors. Physical native-picker interaction (including macOS symlink selection), full desktop restart, and visual checks in every locale remain manual/not run.</li></ul><hr><h4 id="e2e-233-icon-only-actions-explain-their-purpose-in-the-active-language" tabindex="-1">E2E-233: Icon-only actions explain their purpose in the active language <a class="header-anchor" href="#e2e-233-icon-only-actions-explain-their-purpose-in-the-active-language" aria-label="Permalink to &quot;E2E-233: Icon-only actions explain their purpose in the active language&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: The desktop app is running with a project, a chat error, toast, update notice, dialog, sidebar row, pull request, and work-panel file available as applicable; the UI language can be changed between English and Simplified Chinese.</li><li><strong>Steps</strong>: 1) Hover each icon-only action in the error, toast/update, dialog, capability search, sidebar, pull-request, and file-viewer surfaces. 2) Focus the same controls with the keyboard. 3) Repeat after switching the UI language to Simplified Chinese.</li><li><strong>Expected</strong>: Each control exposes a localized action purpose on hover and focus, has the same localized accessible name, and does not expose a raw icon name or URL as its action label. Decorative icons remain silent to assistive technology. English and Simplified Chinese show different catalog values.</li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code>, <code>04-ux/09-interaction-patterns.md</code></li><li><strong>Acceptance</strong>: Accessibility, Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Source-contract covered; desktop hover/focus automation pending</li></ul><h4 id="e2e-plan-005-plan-mode-plugin-tools-with-plansafeactions-are-read-only-d384" tabindex="-1">E2E-PLAN-005: Plan-mode plugin tools with <code>planSafeActions</code> are read-only (D384) <a class="header-anchor" href="#e2e-plan-005-plan-mode-plugin-tools-with-plansafeactions-are-read-only-d384" aria-label="Permalink to &quot;E2E-PLAN-005: Plan-mode plugin tools with \`planSafeActions\` are read-only (D384)&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: PI-Desktop is built with the bundled Browser plugin (<code>pi.browser</code>) enabled and a workspace that exposes one http(s) URL the planner can reach. The catalog list is the default bundled one; no third-party plugin needs to be installed for this scenario.</li><li><strong>Steps</strong>: 1) Create a new session and switch it to Plan mode from the mode selector. 2) Send the prompt &quot;Use the browser plugin to read <code>https://example.com</code>, summarize the page, and tell me what to change.&quot; 3) Wait for the planner to call <code>plugin_pi_browser_Browser</code> with <code>action=&quot;navigate&quot;</code> followed by <code>action=&quot;snapshot&quot;</code>, and to submit a plan with the requested summary. 4) Approve the plan and confirm the Agent run completes. 5) Reject the plan, re-send the same prompt in Plan mode, and confirm the planner can still call <code>navigate</code> + <code>snapshot</code>. 6) Ask the planner to &quot;click the sign-in button&quot; through the browser plugin and confirm the call is rejected with <code>PERMISSION_DENIED</code> (a Plan call can never click). 7) Inspect the active session tool list and confirm it shows the Browser plugin with the description suffix <code>Plan mode: only navigate, snapshot, screenshot, console actions</code>. 8) Switch back to Agent mode and confirm the same prompt lets the model call <code>click</code> and <code>fill</code> without the suffix.</li><li><strong>Expected</strong>: Plan mode can drive the Browser plugin for the four declared read-only actions, the description tells the model which actions are allowed, and any mutating action is denied with a structured <code>PERMISSION_DENIED</code> error before the plugin sees the call. Agent mode keeps the full plugin surface.</li><li><strong>Specs linked</strong>: <code>03-runtime/02-agent-runtime.md</code>, <code>03-runtime/03-tools-and-permissions.md</code>, <code>07-plugins/README.md</code>, ADR 0211</li><li><strong>Acceptance</strong>: Functional, Quality</li><li><strong>Milestone</strong>: M6</li><li><strong>Status</strong>: Partially automated: <code>test:e2e:plan</code> covers Plan-mode host admission, durable-mode/action-list forwarding, and fixture-boundary mutation denial; the full Electron Browser journey remains Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-250-context-usage-display-preference-switches-the-inspector-s-leading-figure" tabindex="-1">E2E-250: Context usage display preference switches the inspector&#39;s leading figure <a class="header-anchor" href="#e2e-250-context-usage-display-preference-switches-the-inspector-s-leading-figure" aria-label="Permalink to &quot;E2E-250: Context usage display preference switches the inspector&#39;s leading figure&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: An Agent session has completed at least one turn that reported token usage. Settings → AI → Defaults is reachable.</li><li><strong>Steps</strong>: <ol><li>Confirm the composer toolbar context ring shows remaining capacity (ring nearly full, percentage ≈ remaining %, tooltip and aria-label use remaining vocabulary).</li><li>Open Settings → AI → Defaults and switch the Context usage display segmented control from Remaining to Used.</li><li>Return to the chat and inspect the context ring: the ring arc now fills by <code>usedRatio</code> (nearly empty at low occupancy), the percentage shows ≈ used %, the popover heading shows used tokens + percentage, and the tooltip/aria-label use used-capacity vocabulary.</li><li>Confirm warning/critical ring colors still follow remaining capacity: at remaining &gt; 25 % the ring stays neutral even when used % is high.</li><li>Switch back to Remaining and confirm the original display returns.</li></ol></li><li><strong>Expected</strong>: The display mode flips the ring arc, percentage, token count, heading, tooltip, and aria-label consistently. Color thresholds remain based on remaining capacity in both modes. The default for a fresh profile is Remaining.</li><li><strong>Specs linked</strong>: <code>04-ux/06-settings-ia.md</code>, <code>04-ux/08-component-spec.md</code>, ADR 0223, <code>08-meta/decisions-log.md</code> (D398)</li><li><strong>Acceptance</strong>: C (conversation &amp; stream), Quality (preference)</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>context-usage.test.mjs</code>, <code>settings-general.test.mjs</code>); full scenario Draft</li></ul><h4 id="e2e-251-custom-dropdowns-float-without-changing-page-layout" tabindex="-1">E2E-251: Custom dropdowns float without changing page layout <a class="header-anchor" href="#e2e-251-custom-dropdowns-float-without-changing-page-layout" aria-label="Permalink to &quot;E2E-251: Custom dropdowns float without changing page layout&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A desktop build with a configured provider, at least one project/session, and the default dark theme. The Settings, Plugins, Projects, chat composer, work panel, and sidebar surfaces are reachable.</li><li><strong>Steps</strong>: 1) Open each available custom dropdown/menu from Settings, Projects, Plugins, the sidebar, the composer, Plan approval, and Scope. 2) Repeat with the trigger near the bottom and right edges of the window, and while the surrounding page/card has scrollable content. 3) Scroll the owning pane and resize the window while a menu remains open. 4) Close each menu with Escape and by pressing outside it.</li><li><strong>Expected</strong>: Every custom dropdown is a body-level fixed layer that overlays content without increasing row/card height or changing page/sidebar/work-panel allocation. It stays within the viewport, flips or clamps when space is tight, follows its trigger after scroll/resize, is not clipped by settings cards or page overflow, and restores focus to its trigger on close. Native <code>&lt;select&gt;</code> popups are excluded because they are rendered by the operating system.</li><li><strong>Specs linked</strong>: <code>04-ux/07-ui-design-system.md</code>, <code>04-ux/09-interaction-patterns.md</code></li><li><strong>Acceptance</strong>: Quality, responsive layout, Accessibility</li><li><strong>Milestone</strong>: M5+</li><li><strong>Status</strong>: Source-contract covered (<code>fixed-dropdown-surfaces.test.mjs</code>); desktop journey Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-253-project-groups-support-manual-drag-and-keyboard-reordering" tabindex="-1">E2E-253: Project groups support manual drag and keyboard reordering <a class="header-anchor" href="#e2e-253-project-groups-support-manual-drag-and-keyboard-reordering" aria-label="Permalink to &quot;E2E-253: Project groups support manual drag and keyboard reordering&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: The sidebar contains at least three project groups, including one pinned or archived project, and each project has a stable host workspace/path/directory.</li><li><strong>Steps</strong>: <ol><li>Press a project title, move it above or below another project group, inspect the insertion line, and release.</li><li>Click a project title and confirm it still selects the project and toggles collapse without changing order.</li><li>Focus the same title and press <code>ArrowUp</code> or <code>ArrowDown</code>; repeat once in each direction.</li><li>Restart the app and inspect the project order.</li><li>Open a session from a reordered project and confirm its host workspace, path, and directory are unchanged.</li></ol></li><li><strong>Expected</strong>: The project groups render in the released order and the manual order survives restart. There is no reorder grip and no 400ms delay. An insertion line shows drop placement. The title exposes a keyboard-accessible reorder action, a short click does not reorder, <code>Escape</code> cancels an active drag, and the existing pinned / archived priority rules remain intact. Reordering never changes a project&#39;s host workspace, path, directory, or session sort.</li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code>, <code>04-ux/09-interaction-patterns.md</code>, <code>03-runtime/04-data-storage.md</code>, <code>08-meta/decisions-log.md</code> (D399, D402, D403)</li><li><strong>Acceptance</strong>: D (workspace), F (persistence), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Source-contract covered (<code>app-store-sidebar.test.mjs</code>, <code>sidebar-preferences.test.mjs</code>, <code>sidebar-project-reorder.test.mjs</code>); rendered desktop journey Draft</li></ul><h4 id="e2e-254-a-skill-loads-on-the-first-agent-turn" tabindex="-1">E2E-254: A skill loads on the first Agent turn <a class="header-anchor" href="#e2e-254-a-skill-loads-on-the-first-agent-turn" aria-label="Permalink to &quot;E2E-254: A skill loads on the first Agent turn&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: At least one Skill is active for the current project, a provider is configured, and the session runs in Agent mode with another on-demand capability present (for example <code>BrowserPreview</code> or a plugin tool).</li><li><strong>Steps</strong>: <ol><li>Open a new Agent conversation and send a prompt that matches the active Skill&#39;s description.</li><li>Inspect the first provider request and its tool list.</li><li>Confirm the model calls <code>Skill</code> with the exact id without calling <code>ToolSearch</code> first, and that the returned document is the skill body.</li><li>Send <code>/&lt;skill-id&gt;</code> from the composer and inspect the following turn.</li><li>Switch the session to Plan mode and inspect the tool list again.</li><li>Disable or remove every Skill and start another Agent turn.</li></ol></li><li><strong>Expected</strong>: Whenever the skill catalog is non-empty, <code>Skill</code> ships with the first request and never appears under <code># On-demand tools</code>, so both a matching task and a <code>/skill-id</code> invocation load the body without a discovery round trip. <code>ToolSearch</code> still exists for the other on-demand capabilities and never returns <code>Skill</code>. Plan mode omits the tool and the <code># Skills</code> section, and an empty catalog registers no <code>Skill</code> tool at all.</li><li><strong>Specs linked</strong>: <code>03-runtime/02-agent-runtime.md</code> (§7.1), <code>03-runtime/03-tools-and-permissions.md</code> (§2.1), <code>04-ux/04-builtin-commands.md</code> (§8), <code>08-meta/decisions-log.md</code> (D404), ADR 0048, ADR 0219, ADR 0230</li><li><strong>Acceptance</strong>: C (conversation &amp; stream), E (tools &amp; permissions), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>packages/agent-runtime/src/runtime.test.ts</code>); rendered desktop journey Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-255-an-ideographic-comma-opens-the-slash-menu" tabindex="-1">E2E-255: An ideographic comma opens the slash menu <a class="header-anchor" href="#e2e-255-an-ideographic-comma-opens-the-slash-menu" aria-label="Permalink to &quot;E2E-255: An ideographic comma opens the slash menu&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A Chinese IME is available, the composer draft is empty, and at least one slash entry exists (builtin alias, template, plugin command, or Skill).</li><li><strong>Steps</strong>: <ol><li>With the draft empty, type <code>、</code> and inspect the composer.</li><li>Continue typing a command name and accept the highlighted row.</li><li>Type a draft that contains <code>、</code> between other characters.</li><li>Send a draft whose first character was typed as <code>、</code> without accepting any row.</li></ol></li><li><strong>Expected</strong>: The committed <code>、</code> is rewritten to <code>/</code> in place, the ordinary slash menu opens with the same filtering and keyboard behavior as a typed <code>/</code>, and the caret stays after the substituted character. A <code>、</code> anywhere later in the draft stays untouched text, and the <code>@</code> file menu never reacts to the mark.</li><li><strong>Specs linked</strong>: <code>04-ux/04-builtin-commands.md</code> (§9), <code>04-ux/08-component-spec.md</code> (§11), <code>08-meta/decisions-log.md</code> (D405), ADR 0024, ADR 0231</li><li><strong>Acceptance</strong>: C (conversation &amp; stream), Localization, Quality</li><li><strong>Milestone</strong>: M2</li><li><strong>Status</strong>: Unit-covered (<code>packages/shared/src/composer-trigger.test.ts</code>, <code>apps/desktop/test/composer-ime.test.mjs</code>); rendered desktop journey Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-256-empty-home-project-name-switches-among-sidebar-projects" tabindex="-1">E2E-256: Empty-home project name switches among sidebar projects <a class="header-anchor" href="#e2e-256-empty-home-project-name-switches-among-sidebar-projects" aria-label="Permalink to &quot;E2E-256: Empty-home project name switches among sidebar projects&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: At least two local projects are open in the sidebar; the visible chat is an empty project-bound session.</li><li><strong>Steps</strong>: <ol><li>Confirm the hero title underlines the current project name.</li><li>Click the underlined name and inspect the menu.</li><li>Search for a sidebar project, select a different one, and inspect the hero and sidebar.</li><li>Reopen the menu and choose Open project, then pick a folder or cancel.</li><li>Reopen the menu, choose Clone git project, paste a repository URL, then pick a parent folder or cancel.</li><li>Open a temporary empty session and confirm the underline is absent.</li></ol></li><li><strong>Expected</strong>: The click opens a searchable, fixed switcher of the sidebar&#39;s open projects instead of the folder picker. Choosing another project activates it and lands on that project&#39;s empty home (reusing an empty session when one exists). Open project still uses the folder picker. Clone git project asks for a URL, then a folder, runs <code>git clone</code>, and opens the cloned project. Temporary and no-session heroes stay without the switcher. Escape and outside click dismiss the menu.</li><li><strong>Specs linked</strong>: <code>04-ux/01-ui-ia.md</code>, <code>04-ux/08-component-spec.md</code></li><li><strong>Acceptance</strong>: Quality (navigation and accessibility)</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>home-project-switcher.test.mjs</code>, <code>git-clone.test.mjs</code>, <code>sidebar-preferences.test.mjs</code>); full UI scenario Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-clone-public-hostname-rejects-private" tabindex="-1">E2E-CLONE-public-hostname-rejects-private <a class="header-anchor" href="#e2e-clone-public-hostname-rejects-private" aria-label="Permalink to &quot;E2E-CLONE-public-hostname-rejects-private&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: The home project switcher Clone git project action is available.</li><li><strong>Steps</strong>: 1) Enter <code>https://127.0.0.1/org/repo.git</code>, <code>http://localhost/org/repo.git</code>, <code>https://10.0.0.5/org/repo.git</code>, and <code>git@127.0.0.1:org/repo.git</code>. 2) Enter <code>https://github.com/org/repo.git</code> and <code>git@github.com:org/repo.git</code>.</li><li><strong>Expected</strong>: Private, loopback, and link-local remotes are rejected before <code>git clone</code> runs. Public GitHub HTTPS and SSH remotes still parse to a folder name. <code>file:</code> and password-bearing URLs remain rejected.</li><li><strong>Specs linked</strong>: <code>04-ux/01-ui-ia.md</code>, ADR 0247, D416</li><li><strong>Acceptance</strong>: Security, D (workspace)</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>apps/desktop/test/git-clone.test.mjs</code>)</li></ul><h4 id="e2e-258-create-project-dialog-can-start-from-a-git-repository" tabindex="-1">E2E-258: Create project dialog can start from a git repository <a class="header-anchor" href="#e2e-258-create-project-dialog-can-start-from-a-git-repository" aria-label="Permalink to &quot;E2E-258: Create project dialog can start from a git repository&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: The Create project dialog opens from the Projects heading (no existing project is required); <code>git</code> is installed.</li><li><strong>Steps</strong>: <ol><li>Open the Create project dialog and click Add folder twice quickly; confirm that only one native folder picker opens, then cancel it.</li><li>Switch the source selector to Git repository.</li><li>Paste <code>https://github.com/octocat/Hello-World.git</code> and confirm the project name field is seeded with <code>Hello-World</code>, then type a custom name.</li><li>Choose a clone destination folder and confirm the destination row shows it.</li><li>Confirm Create and inspect the workspace, sidebar, and project archive.</li><li>Reopen the dialog, switch to Git repository, and paste a private or malformed remote.</li></ol></li><li><strong>Expected</strong>: Repeated folder-picker clicks are ignored while the native picker is open. The dialog swaps the folder list for a repository URL field plus a clone destination row and keeps one project name field. Source options and fields are filled tiles without strokes; keyboard focus uses the shared accent-tinted ring. Create stays disabled until the URL parses and a folder is chosen; a name left empty falls back to the parsed repository name. Confirming runs <code>git clone</code> into the chosen folder with the renderer still owning project creation: the checkout becomes the primary root and the entered name names the group. Private, loopback, link-local, credential-bearing, and malformed remotes leave Create disabled (ADR 0247) and no folder is written.</li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code> §9, <code>04-ux/08-component-spec.md</code>, ADR 0273, ADR 0233, ADR 0247</li><li><strong>Acceptance</strong>: Quality (project entry), D (workspace)</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Unit-covered (<code>apps/desktop/test/project-create-dialog.test.mjs</code>, <code>apps/desktop/test/git-clone.test.mjs</code>); full UI scenario Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-257-importing-into-an-archived-project-restores-its-visibility" tabindex="-1">E2E-257: Importing into an archived project restores its visibility <a class="header-anchor" href="#e2e-257-importing-into-an-archived-project-restores-its-visibility" aria-label="Permalink to &quot;E2E-257: Importing into an archived project restores its visibility&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A durable project has been archived in the renderer sidebar preferences and is hidden from the default sidebar. A core import candidate has that project&#39;s path, and a test plugin can import a session with an explicit host project id.</li><li><strong>Steps</strong>: <ol><li>Open Settings → Project archive and confirm the archived project remains available there while the default sidebar omits it.</li><li>Scan and import the core candidate whose project path belongs to the archived project.</li><li>Confirm the project and its imported session appear in the default sidebar, then archive the project again.</li><li>Use the plugin&#39;s <code>session.importBatch</code> with the existing project&#39;s id and inspect the sidebar after the host refresh event.</li><li>Refresh sessions without importing anything, import a pathless session, and repeat an already imported session.</li></ol></li><li><strong>Expected</strong>: Each successful import that adds a project-bound session clears the archived presentation state for that exact normalized project path and makes the project/session discoverable. Ordinary refreshes, pathless sessions, skipped imports, and plugin history paths without an explicit project binding leave archive state unchanged; no host project row or transcript is deleted or recreated.</li><li><strong>Specs linked</strong>: <code>04-ux/06-settings-ia.md</code>, <code>04-ux/08-component-spec.md</code>, <code>03-runtime/04-data-storage.md</code>, ADR 0236, D407</li><li><strong>Acceptance</strong>: C (conversation &amp; stream), F (persistence), G (plugins), Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Unit/source-contract covered (<code>sidebar-session-groups.test.mjs</code>, <code>project-import-archive.test.mjs</code>, <code>plugin-session-refresh.test.mjs</code>); rendered desktop journey Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-import-codex-scan-filters-synthetic-titles" tabindex="-1">E2E-IMPORT-codex-scan-filters-synthetic-titles <a class="header-anchor" href="#e2e-import-codex-scan-filters-synthetic-titles" aria-label="Permalink to &quot;E2E-IMPORT-codex-scan-filters-synthetic-titles&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A Codex archive whose sessions open with synthetic injections (<code># Context from my IDE setup:</code>, <code># In app browser:</code>, <code># Browser comments:</code>, <code># Files mentioned by the user:</code>, <code># Diff comments:</code>, <code># Selected text:</code>, <code># Review findings:</code>, <code># AGENTS.md</code>, <code>You are Codex</code>, <code>&lt;environment&gt;</code>) and at least one session whose stored timestamps are corrupt or out of range.</li><li><strong>Steps</strong>: <ol><li>Run Settings → Session import → Scan over the archive.</li><li>Inspect candidate titles and the createdAt/updatedAt shown per session.</li><li>Import a session whose first real user message follows synthetic injections.</li></ol></li><li><strong>Expected</strong>: Candidate titles come from the first real user message — synthetic injections never surface as titles, while genuinely pasted markdown that starts with <code>#</code> (for example <code># Role: …</code>) is kept. Sessions whose user messages are all synthetic do not appear as candidates. A corrupt or out-of-range stored timestamp falls back to the source file&#39;s mtime, never to the import moment.</li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code>, <code>04-ux/06-settings-ia.md</code>, D320</li><li><strong>Acceptance</strong>: C (conversation &amp; stream), F (persistence), Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Unit-covered (<code>importer-codex-scan.test.mjs</code>); UI journey Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-layout-three-column-width-priority" tabindex="-1">E2E-LAYOUT-three-column-width-priority <a class="header-anchor" href="#e2e-layout-three-column-width-priority" aria-label="Permalink to &quot;E2E-LAYOUT-three-column-width-priority&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A desktop session is open in a non-Settings route with a persisted preferred work-panel width, on a window wide enough for the three columns.</li><li><strong>Steps</strong>: <ol><li>Open the work panel and request the user&#39;s preferred width.</li><li>Drag the inner divider toward MainChat&#39;s left edge, including during pointer preview, then release.</li><li>Manually reopen the sidebar after the layout collapsed it.</li><li>Close the work panel and confirm the sidebar returns; repeat after manually collapsing the sidebar.</li><li>Repeat divider changes with <code>ArrowLeft</code>, <code>ArrowRight</code>, <code>Home</code>, and <code>End</code>.</li><li>Navigate to the real Plugins, Pull requests, and Scheduled routes with the work panel closed, then collapse the sidebar. In light and dark themes, measure both titlebar actions and compare their rest/hover styling with the shared work-panel toggle. Reopen the sidebar, collapse it again, and use New Task to return to an editable chat composer on each route.</li></ol></li><li><strong>Expected</strong>: The ordinary <code>.main-titlebar</code> actions (without a preview chrome ancestor) render as centered 28px square targets, with the shared transparent rest surface, secondary ink, radius, and semantic hover wash/primary ink. Hover does not change geometry; sidebar and New Task actions remain usable. The native window width never changes. MainChat never measures below 450px — including mid-drag and while <code>sidebar-out</code> still occupies flex space. The effective panel maximum is the client width minus the 450px MainChat floor and the expanded sidebar width, with no fixed pixel cap. When that budget is exhausted the expanded sidebar collapses immediately, and the panel may keep growing afterwards. A manual reopen spends panel width first; MainChat is preserved where possible and otherwise lands on the 460px reopen target. Closing the panel restores only a sidebar the layout collapsed. The separator&#39;s ARIA minimum/maximum follow the same dynamic budget. The panel header&#39;s <code>+</code>, maximize, and viewport-fixed collapse toggle resolve to a single control gap (<code>--ds-work-panel-control-gap</code>) with no divider, inset, or margin of the action group&#39;s own, and all three are the shared chrome icon control: 28px square on a transparent seat with only a semantic hover wash, so the header shows quiet icons rather than filled or raised squares. The collapse toggle&#39;s open state changes its glyph and ink only.</li><li><strong>Overlay coverage measured in the running app</strong>: on the Plugins route a plugin modal opens; the modal veil is the topmost hit at the work-panel toggle inside the titlebar band; the titlebar band is not the topmost hit there, so it does not paint over the modal veil; and closing the modal leaves the route clean. <code>pnpm test:e2e:layout</code> asserts those four hits as renderer DOM/CDP evidence, not as native hit-test proof.</li><li><strong>Specs linked</strong>: <code>04-ux/01-ui-ia.md</code>, <code>04-ux/07-ui-design-system.md</code> §10, <code>04-ux/08-component-spec.md</code> §1 and §5, <code>04-ux/09-interaction-patterns.md</code> §8, ADR 0238</li><li><strong>Acceptance</strong>: F (persistence), Quality</li><li><strong>Milestone</strong>: Post-M6 desktop shell maintenance</li><li><strong>Status</strong>: Automated (<code>scripts/e2e-three-column-layout.mjs</code> via <code>pnpm test:e2e:layout</code> — fixed-window width invariance, the 450px floor across a pointer drag, one non-wrapping composer toolbar at that floor with the model chip collapsed to its 32px icon, sidebar yield/restore, the 460px reopen target, the panel action group&#39;s shared control gap, preview mode, and ordinary Plugins/Pull requests/Scheduled titlebar Source contracts in <code>chrome-control-geometry.test.mjs</code> also cover the shared disabled state and panel controls&#39; transparent seat; the panel surface still needs the eyes-on pass above. DOM/CDP checks establish renderer behavior, not native Windows/Linux hit testing; native platform checks remain separate. Unit coverage in <code>work-panel-resize.test.mjs</code></li></ul><h4 id="e2e-layout-work-panel-maximize" tabindex="-1">E2E-LAYOUT-work-panel-maximize <a class="header-anchor" href="#e2e-layout-work-panel-maximize" aria-label="Permalink to &quot;E2E-LAYOUT-work-panel-maximize&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A desktop session is open with the work panel visible. <ol><li>Note the current panel width. If the sidebar is expanded, collapse it; then click the panel header&#39;s <code>+</code> action to open a real work-panel tab.</li><li>Click the panel header&#39;s preview toggle.</li><li>Inspect the header border box and tab alignment with the sidebar collapsed and expanded on macOS windowed/fullscreen and Windows/Linux. Confirm the overlay row and spacer declare neither drag nor no-drag and do not cover the panel controls with opaque paint.</li><li>Using native pointer input, click the centers and edges of sidebar and New Task controls; confirm sidebar toggles and New Task exits preview to an editable composer. Reenter preview, operate tabs, close, add, restore, panel toggle and native controls, then drag empty header space and confirm the native window moves. Repeat in light/dark themes and both sidebar states.</li></ol></li><li><strong>Expected</strong>: Entering preview mode stops rendering MainChat and hands its width to the panel, so the panel spans the client area minus the expanded sidebar (the whole client area when the sidebar is collapsed). The native window never changes size. The divider is inert while preview mode is on (<code>aria-disabled</code>). Leaving preview mode restores the previous panel width and keeps whatever sidebar state the user chose last. The mode is transient: it is not persisted and ends when the panel closes. Preview mode keeps the shell&#39;s new-task, sidebar, and system-window actions reachable while MainChat is absent. The panel header is the sole drag owner in the preview pane. Its actual border box and first tab start at least 8px after the shell actions, including expanded-sidebar New Task, on every platform. Left inset is 8px except collapsed-sidebar windowed macOS (88px). The preview row renders the shared <code>--ds-window-lead-inset</code> (76px native cluster edge plus a 12px gap) as its own left padding; the check reads the resolved padding rather than restating the number. Fullscreen retains the action lane with an 8px inset. The right native-control border exclusion remains intact. Controls receive native clicks without moving the window; empty header space still drags it. Header-height background paint fills the excluded lane without hiding controls.</li><li><strong>Specs linked</strong>: <code>04-ux/01-ui-ia.md</code>, <code>04-ux/07-ui-design-system.md</code> §10, <code>04-ux/08-component-spec.md</code> §5, <code>04-ux/09-interaction-patterns.md</code> §8, ADR 0238 §6, issue #289</li><li><strong>Acceptance</strong>: F (persistence), Quality</li><li><strong>Milestone</strong>: Post-M6 desktop shell maintenance</li><li><strong>Status</strong>: Partially automated (<code>scripts/e2e-three-column-layout.mjs</code> — preview entry/exit, DOM action behavior, header border-box exclusion, row/spacer drag ownership, and all-platform/fullscreen CSS fixtures in both sidebar states). DOM clicks and CDP input are not native hit-test proof; native pointer, window-drag and visual checks remain required on each platform. Branch runs are exploratory and do not satisfy the integrated-main gate.</li></ul><h4 id="e2e-layout-sidebar-project-group-fold" tabindex="-1">E2E-LAYOUT-sidebar-project-group-fold <a class="header-anchor" href="#e2e-layout-sidebar-project-group-fold" aria-label="Permalink to &quot;E2E-LAYOUT-sidebar-project-group-fold&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Four retained sidebar project groups, seeded through the host: one holding five sessions across four date buckets, one holding a single session, one holding none, and one holding ten pinned sessions. <code>prefers-reduced-motion</code> is unset.</li><li><strong>Steps</strong>: <ol><li>Inspect the groups: body layering, row and date-label counts, the empty state, the tail each expanded group contributes to the next one, and the non-project lists&#39; budgets.</li><li>Collapse the multi-row group with a real pointer click on its directory row — scrolled into view and confirmed to hit that button — and read the group body&#39;s height, opacity, resolved <code>grid-template-rows</code>, and the distance to the next group on every frame for about half a second, while recording the fold&#39;s own <code>transitionrun</code> / <code>transitionend</code>.</li><li>Expand it again and confirm the open geometry returns.</li><li>Collapse and re-expand it within the same motion.</li><li>Repeat the collapse with <code>prefers-reduced-motion: reduce</code> emulated.</li><li>Scroll the pinned list to its last row.</li></ol></li><li><strong>Expected</strong>: A project group is one grid row (<code>grid-template-rows: 1fr</code>) that animates to <code>0fr</code> over the 200ms normal duration — no <code>max-height</code> clamp, no opacity transition — so the fold is a single continuous height ramp with no plateau followed by a snap, and <code>opacity</code> stays 1 on every frame: the rows are clipped, never faded. The fold fires one transition and its own event reports the 200ms normal duration. The rows are clipped by an inner <code>min-height: 0</code> box, and the 1px row gap plus the group&#39;s 2px / 7px inset sit on the list inside that clip, so the inset travels with the rows. An expanded group&#39;s 7px inset plus the 1px scroller gap read as an 8px tail to the group below it; the last group in the list has no neighbour, so it is checked against its own inset and clip instead. A folded group&#39;s tail leaves with its rows, its section is its header plus the 1px scroller gap, and its rows stay mounted past the clipped edge while the group is <code>aria-hidden</code> and <code>inert</code>. A reversal mid-flight turns on the frame it reached and settles back on the open height without overshooting, and the empty group folds its empty state the same way. Under reduced motion both endpoints are kept and the travel is dropped. The pinned list holds eight rows inside <code>min(233px, 30vh)</code> and scrolls to its remaining rows, and the standalone list keeps its flex column and 146px budget. The group indent, ordering, and workspace state are unchanged.</li><li><strong>Specs linked</strong>: <code>04-ux/01-ui-ia.md</code>, <code>04-ux/07-ui-design-system.md</code> §6.1 and §13, <code>04-ux/08-component-spec.md</code> §6.2, <code>08-meta/decisions-log.md</code> (2026-09-16, sidebar list rhythm and project-group fold)</li><li><strong>Acceptance</strong>: Quality</li><li><strong>Milestone</strong>: Post-M6 desktop shell maintenance</li><li><strong>Status</strong>: Automated (<code>scripts/e2e-three-column-layout.mjs</code> via <code>pnpm test:e2e:layout</code> — host-seeded groups and pins, a hit-tested CDP pointer click, per-frame height and opacity sampling across the real fold, the transition&#39;s own reported duration, mid-flight reversal, and reduced-motion emulation). Unit coverage in <code>apps/desktop/test/sidebar-collapse-animation.test.mjs</code> and <code>apps/desktop/test/sidebar-pinned-rendering.test.mjs</code>. The sampled values are renderer geometry, not an eyes-on visual pass.</li></ul><h4 id="e2e-layout-sidebar-row-states" tabindex="-1">E2E-LAYOUT-sidebar-row-states <a class="header-anchor" href="#e2e-layout-sidebar-row-states" aria-label="Permalink to &quot;E2E-LAYOUT-sidebar-row-states&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Host-seeded project, pinned and standalone conversations; a current workspace; built desktop with isolated data and profile directories.</li><li><strong>Steps</strong>: In dark and light themes, select a project conversation, hover its project title and an unselected conversation, then hover the selected row. Exercise the window-blur handler, drop-target styling, project action hover and keyboard Tab/Shift+Tab focus. Fold and reopen the selected conversation&#39;s group. Select pinned and standalone conversations. Open Settings and return. Enable reduced motion and inspect both row transition durations.</li><li><strong>Expected</strong>: Project and conversation hover backgrounds, radii and transitions match. The title button stays transparent. Only a conversation uses selected fill, which wins over hover; workspace identity remains a separate dot with no persistent header fill. Folding never promotes the project to selected. Pinned and standalone rows use the same selected surface. Keyboard focus retains an outline, action buttons retain local feedback, drop-target paint wins over hover, and blur releases hover without clearing selection. Settings replaces sidebar navigation, and returning restores conversation and workspace context without a second selected row. Rendered component tests additionally cover no selected session, a pending destination and non-chat page state.</li><li><strong>Specs linked</strong>: <code>04-ux/01-ui-ia.md</code>, <code>04-ux/08-component-spec.md</code>, <code>04-ux/09-interaction-patterns.md</code> §9.1c</li><li><strong>Acceptance</strong>: C, D, Quality</li><li><strong>Milestone</strong>: Post-M6 desktop shell maintenance</li><li><strong>Status</strong>: Automated via <code>pnpm test:e2e:layout</code> and <code>scripts/e2e/sidebar-row-states.mjs</code>: real CDP pointer/keyboard input and computed-style assertions. Window blur/focus events and the drop-target class are injected for those styling checks; this is not a native focus/drag test. Unit coverage: <code>sidebar-navigation.test.mjs</code>, <code>sidebar-pinned-rendering.test.mjs</code>.</li></ul><h4 id="e2e-layout-sidebar-settings" tabindex="-1">E2E-LAYOUT-sidebar-settings <a class="header-anchor" href="#e2e-layout-sidebar-settings" aria-label="Permalink to &quot;E2E-LAYOUT-sidebar-settings&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Built desktop, isolated host/profile, visible chat sidebar.</li><li><strong>Steps</strong>: <ol><li>In dark/light palettes and darwin/win32/linux CSS branches, compare the home sidebar and Settings rail color, image layers, size and position. Inspect transparent ancestors and opaque settings content/titlebar.</li><li>Return through Back to app while tracing sidebar insertion, width and animationstart events. Repeat quick round trips, a previously collapsed sidebar, and settings navigation interrupting an entrance.</li><li>Explicitly reopen a collapsed sidebar, then repeat settings return with reduced motion. Repeat material comparisons with legacy and canonical theme color overrides and a sidebar background image.</li></ol></li><li><strong>Expected</strong>: Both navigation surfaces share one material. Settings navigation and shell have no entrance animation; only a nested content-enter wrapper inside its opaque pane animates, and that motion is opacity-only so it cannot trap <code>position: fixed</code> overlays. Settings dialogs cover the full window, including the rail. On macOS all ancestors behind the rail are transparent, while right content and titlebar stay opaque. Returning to an expanded sidebar starts and stays at 275px without sidebar-in events; a collapsed sidebar stays absent. A real reopen still produces sidebar-in and a width ramp. Legacy theme color input remains supported for both rails, and a canonical override wins.</li><li><strong>Specs linked</strong>: <code>04-ux/06-settings-ia.md</code>, <code>04-ux/07-ui-design-system.md</code>, <code>04-ux/08-component-spec.md</code> §1.4 and §1.7</li><li><strong>Acceptance</strong>: A, C, Quality</li><li><strong>Milestone</strong>: Post-M6 desktop shell maintenance</li><li><strong>Status</strong>: Automated via <code>pnpm test:e2e:layout</code> and <code>scripts/e2e/sidebar-settings.mjs</code>, with trusted CDP pointer/keyboard input, mutation-time and subsequent geometry samples, animation events and computed styles. CDP focus emulation keeps the isolated page painting while its native window is occluded; otherwise Chromium can freeze CSS motion and hover input. Platform branches and palettes are renderer emulation, not native Windows/Linux or OS material/theme validation. Optional <code>PI_DESKTOP_LAYOUT_ARTIFACT_DIR</code> captures renderer screenshots. State tests in <code>sidebar-settings-return.test.mjs</code> cover initial presentation, both interrupted phases, hidden-state changes and reversals. <code>settings-dialog-overlay.test.mjs</code> covers the full-window overlay contract. <code>pnpm test:e2e:theme-surfaces</code> verifies the opaque fallback and legacy theme override in real Chromium.</li></ul><h4 id="e2e-agent-alt-enter-steers-active-turn-enter-follows-up-and-alt-enter-steers-the-active-turn" tabindex="-1">E2E-AGENT-alt-enter-steers-active-turn: Enter follows up and Alt+Enter steers the active turn <a class="header-anchor" href="#e2e-agent-alt-enter-steers-active-turn-enter-follows-up-and-alt-enter-steers-the-active-turn" aria-label="Permalink to &quot;E2E-AGENT-alt-enter-steers-active-turn: Enter follows up and Alt+Enter steers the active turn&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A session with a configured model and a controllable streaming response/tool; an image-capable model for the attachment case.</li><li><strong>Steps</strong>: <ol><li>Start a prompt, then type a follow-up and press Enter. Confirm a FIFO row.</li><li>During the same turn, type a correction and press Alt+Enter. Repeat with an image chip and with two corrections before the current request ends.</li><li>Finish the current response/tool batch and inspect the next model input, transcript and durable turn id. Let the turn finish and observe follow-up.</li><li>Repeat with Enter-to-send off, an open autocomplete menu, Shift+Enter, Alt+Shift+Enter and a Chinese IME candidate confirmation. Inspect the Send tooltip on macOS (<code>⌥+Enter</code>) and Windows/Linux (<code>Alt+Enter</code>).</li><li>Race steering against turn completion, Stop, and a pending plan approval; switch sessions while a rejected request is pending.</li><li>Change the next-turn model while running, then steer. Verify the active model and permission configuration remain unchanged.</li><li>Steer while the parent waits for background delegates; leave them running and verify the parent receives the correction before their reports finish.</li><li>Reload after completion and simulate a crash after a streaming reply was reserved by steering. Inspect row order, recovered text and owning turn.</li><li>Reload the renderer after steering is accepted but before its reply starts, then press Stop and inspect the persisted transcript.</li></ol></li><li><strong>Expected</strong>: Enter queues an ordinary follow-up. Alt+Enter creates a user row in the current turn with no queue row or new public <code>agent_start</code>. Started tools finish, then the next request contains the corrections/images. The ordinary FIFO starts only after durable turn finalization. IME and newline actions never submit; idle Alt+Enter sends normally. A stale/closed target keeps the draft in its own session and never fails the active turn. Accepted input is not replayed independently after Stop. Completed replies and accepted steering input remain in history after renderer reload and Stop. Terminal assistant snapshots replace provisional snapshots in place; crash recovery preserves the latest checkpoint and adjacent steering rows without duplicates.</li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code> (§5.1a), <code>03-runtime/02-agent-runtime.md</code> (§4.0), <code>03-runtime/04-data-storage.md</code>, <code>04-ux/09-interaction-patterns.md</code> (§3.5), ADR active-turn-steering</li><li><strong>Acceptance</strong>: C (conversation &amp; stream), E (tools &amp; permissions), Quality</li><li><strong>Milestone</strong>: M5</li><li><strong>Status</strong>: Draft. Existing regression suites cover surrounding behavior; the rendered steering journey has not been run (do not run E2E locally unless explicitly requested).</li></ul><h4 id="e2e-session-content-search-and-message-navigation" tabindex="-1">E2E-SESSION-content-search-and-message-navigation <a class="header-anchor" href="#e2e-session-content-search-and-message-navigation" aria-label="Permalink to &quot;E2E-SESSION-content-search-and-message-navigation&quot;">​</a></h4><ul><li><strong>Scope</strong>: Desktop global search, host search projections, and original conversation navigation (issue #270, ADR session-content-search).</li><li><strong>Preconditions</strong>: At least 65 visible sessions with a shared body keyword; one session has 125 matching user/assistant messages. Include a body-only keyword, a metadata-only match, an archived session, a soft-deleted session, one/two-character CJK terms, literal <code>%</code>, <code>_</code>, quotes, and a path. Include a long session with a match beyond the latest 100 messages and a message whose matching text follows 100,000 characters. Include an actively streaming conversation and a fixture with repeated physical message lines. Include a nested assistant answer whose Task is outside its 60-line page, a later terminal copy of that Task, collapsed activity, hidden Markdown link destinations, emphasis delimiters, and file-chip directories. Add short matching sentences surrounded by unrelated Chinese/English sentences, multiple short lines before a match, a quoted sentence, a file path containing periods, and a matching sentence longer than 180 characters.</li><li><strong>Steps</strong>: Search body-only user and assistant terms, then rename the owning session and repeat. Check aggregated counts and sender/time/snippet labels. Load every result page. Open a session heading and each of its two snippets; verify that each snippet closes search, opens the same original conversation, and scrolls to its own matching text. The heading selects its first snippet. Check normal Markdown, message actions, and composer. Repeat with two assistant fragments in one turn, an old target outside the latest page, and a match after 100,000 characters. Verify a visible highlight and that layout settling does not pull the target away. Read upward and load later messages without gaps. Use the latest-message control to resume the live transcript. Scroll within the active conversation, reopen search, and select its own result; locate the target inside the same pane. Edit, retry, branch, and delete an old message. Reopen search and check the retained query. Repeat with CJK and symbols. Verify that a contiguous Chinese phrase matches, while inserting a space between its words only matches text with that space. Change queries rapidly while delayed first-page and later-page requests resolve out of order. Close/reopen during loading. Retry after a transient search error. Open the nested answer and verify that the owning Task is revealed and its existing dock scrolls to the answer. Search the hidden URL, delimiters, and chip directory after a long prefix; verify the corresponding visible element is highlighted. Interrupt layout correction with a wheel/key gesture. Page ordinary history while output streams, then interrupt a pending page with search, another result, a new turn, and return to latest. Change a revision without changing its message ID. Edit/retry a visible but display-capped message and verify that the canonical full text becomes the action input. Search a running conversation and return to its live stream, then switch conversations. Use arrows, Enter, Escape, Tab, and CJK IME confirmation, and exercise page/settings/plugin-command results. Search a word in the middle sentence and verify that the result shows that sentence with a background highlight on the literal match. Repeat after multiple line breaks and at narrow window widths in light and dark themes. Long-sentence truncation must keep the whole matching query visible; short previews must omit unrelated adjacent sentences.</li><li><strong>Expected</strong>: Every matching visible session is reachable; counts cover all 125 messages, with no duplicate session rows. Archived visibility follows the existing explicit-search rule and deleted sessions never appear. Each snippet opens its owning conversation directly without an intermediate context reader, plain-text replacement, or Back to conversation action. Each selected snippet lands at its exact message and matching rendered text, including old history and individual assistant fragments. Reading-window loads do not overwrite live output. New turns return to the live transcript. Actions on historical messages work normally and do not leave obsolete rows. Nested answers land in the original Task dock; source-only matches land on their rendered owner. Ordinary paging retains current streamed content. Interrupted reads cannot reopen a dock or restore stale/loading views, and same-ID edits replace obsolete displayed content. Message actions never use clipped text as input. Rapidly selecting another result rejects stale target/page completions. Deleting a result before selection reports failure instead of landing at the tail. Later query ownership wins over stale results/errors. IME Enter does not execute an action. Search transport failures are explicit. Existing commands, pages, settings, and keyboard navigation still work.</li><li><strong>Status</strong>: Draft; Rust and renderer unit regressions cover the data/query boundaries. Full rendered E2E requires an explicitly authorized run.</li></ul><h3 id="mcp-market-scenarios-pnpm-test-e2e-mcp-market-headless-protocol-level" tabindex="-1">MCP market scenarios (<code>pnpm test:e2e:mcp-market</code>, headless protocol-level) <a class="header-anchor" href="#mcp-market-scenarios-pnpm-test-e2e-mcp-market-headless-protocol-level" aria-label="Permalink to &quot;MCP market scenarios (\`pnpm test:e2e:mcp-market\`, headless protocol-level)&quot;">​</a></h3><table tabindex="0"><thead><tr><th>ID</th><th>Scenario</th><th>Verification</th></tr></thead><tbody><tr><td>E2E-MCP-MARKET-NET-BOUNDARY</td><td>URL guard rejects credentials, loopback, private, special-use IPv4, v4-mapped, ULA, site-local and link-local bypass forms (trailing dot included); direct/unknown routes pin the checked public address by default, while a fully proxied route uses the session transport and an explicit <code>allowFakeIp</code> opt-in can cover transparent-router fake-IP sources without allowing real private answers</td><td>deterministic guard assertions; source-contract coverage for DNS pin, proxy route selection, explicit fake-IP scope and bounded responses</td></tr><tr><td>E2E-MCP-MARKET-SEMANTICS</td><td>Registry records map to install templates preserving package versions, named/positional runtime/package arguments and required/optional env variables; a remote header variable is recognized in both the registry&#39;s <code>{name}</code> and the catalog&#39;s <code>\${NAME}</code> spelling, prompts for declared editable values, preserves unbound brace literals, and resolves header-local defaults, fixed values, and optional flags without merging same-named inputs across headers (ADR registry-header-variable-spelling)</td><td>deterministic mapping assertions</td></tr><tr><td>E2E-MCP-MARKET-INSTALL</td><td>Builtin catalog entry resolves through <code>resolveCatalogEntry</code> and installs via the host <code>mcp.upsert</code> RPC; record lands in <code>~/.agents/servers/</code></td><td>real host binary, isolated temp HOME</td></tr><tr><td>E2E-MCP-MARKET-HEADER-SCOPE</td><td>Registry header-local <code>{token}</code> resolves only in its header; same-named URL path/query tokens remain literal through mapping, resolution, host upsert/list and persistence. URL templates retain only legacy <code>\${NAME}</code> substitution. When <code>headerBindings</code> exists (even empty or partial), unbound tokens in every header stay literal and never consume another header&#39;s input or default</td><td>shared regressions plus real host binary with isolated temporary storage; remote entry disabled, no network call</td></tr><tr><td>E2E-MCP-MARKET-partial-header-bindings-stay-literal</td><td>Resolve a catalog with only Authorization bound and another header using the same <code>{token}</code> / <code>\${token}</code>; an undeclared <code>\${UNBOUND}</code> in a third header also remains literal through host upsert/list and disk persistence</td><td>real host binary, disabled remote entry, synthetic input and isolated temporary storage; no network call</td></tr></tbody></table><h4 id="e2e-skill-market-net-boundary-public-https-skill-sources-reject-private-and-loopback-urls" tabindex="-1">E2E-SKILL-MARKET-NET-BOUNDARY: Public-HTTPS skill sources reject private and loopback URLs <a class="header-anchor" href="#e2e-skill-market-net-boundary-public-https-skill-sources-reject-private-and-loopback-urls" aria-label="Permalink to &quot;E2E-SKILL-MARKET-NET-BOUNDARY: Public-HTTPS skill sources reject private and loopback URLs&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Shared public-network helpers and the main-process public-HTTPS client with injectable fetch/DNS/route.</li><li><strong>Steps</strong>: 1) Classify trailing-dot localhost, IPv4 loopback, IPv4-mapped IPv6, ULA, link-local, RFC1918, and <code>http://</code> URLs. 2) Resolve a public hostname to a private A record. 3) Follow a 302 whose Location is <code>https://127.0.0.1/</code>. 4) Report a proxied route and a TUN fake-IP answer (<code>198.18.0.1</code>), the same answer on a <code>DIRECT</code> route, on an unreadable route, and on a route list that offers <code>DIRECT</code>. 5) Let a first hop be proxied and its redirect target direct.</li><li><strong>Expected</strong>: Every bypass form is rejected. A public CDN URL is accepted. DNS that yields a private address and a redirect onto loopback both throw a policy error without fetching the private target. A judged refusal is not retried; a local resolver that answered nothing is, and is reported as <code>NETWORK_RESOLVE_FAILED</code> (<code>kind</code> <code>unresolved</code>) rather than as an address-check refusal — the guard reached no verdict, so nothing may claim it did. An address in a proxy&#39;s fake-IP range (<code>198.18.0.0/15</code>, Clash&#39;s default) is refused and not retried where the guard judged it — a direct or unreadable route — and is accepted on the proxied one, and is reported as <code>kind</code> <code>fake-ip</code> with <code>addressKind</code> <code>benchmark</code> and <code>reason</code> <code>non-public-address</code> — distinct from a real private target (<code>kind</code> <code>policy</code>, <code>addressKind</code> <code>private</code>), because the guard judged the target in the second case and only the proxy&#39;s placeholder in the first. Every other refusal carries <code>NETWORK_POLICY_BLOCKED</code> (spec 08 §3.1) with its <code>reason</code>, the address it resolved to, the class of that address, and the route it was judged on, so the install sheet can name the reason and offer a retry instead of leaving the install button disabled with no explanation, and the market list can tell a refused source apart from a merely unreachable one. Every other non-public class still refuses on all routes, and each redirect hop is judged on its own route (ADR 0272).</li><li><strong>Specs linked</strong>: <code>05-security/01-security.md</code>, ADR 0243, ADR 0272, <code>03-runtime/01-ipc-protocol.md</code> §12b</li><li><strong>Acceptance</strong>: Security, Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Automated (<code>pnpm test:e2e:skill-market</code>, <code>apps/desktop/test/public-https-fetch.test.mjs</code>, <code>apps/desktop/test/public-https-fetch-route.test.mjs</code>, <code>apps/desktop/test/skill-market-scan.test.mjs</code>, <code>apps/desktop/test/skill-market-failure.test.mjs</code>, <code>apps/desktop/test/skill-market-policy-refusal.test.mjs</code>, <code>packages/shared/src/public-network.test.ts</code>)</li></ul><h4 id="e2e-skill-market-expansion-adjacent-markdown-resources-inline-before-install" tabindex="-1">E2E-SKILL-MARKET-EXPANSION: Adjacent markdown resources inline before install <a class="header-anchor" href="#e2e-skill-market-expansion-adjacent-markdown-resources-inline-before-install" aria-label="Permalink to &quot;E2E-SKILL-MARKET-EXPANSION: Adjacent markdown resources inline before install&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A jsDelivr skill document whose directory lists FORMS.md and REFERENCE.md (mocked listing in unit tests; expansion helper in E2E).</li><li><strong>Steps</strong>: Split SKILL.md, expand listed sibling markdown files as fenced appendices, and confirm a document over 128 KiB is flagged too large.</li><li><strong>Expected</strong>: The preview/install body contains the skill text plus <code># Attached resource:</code> appendices. Non-markdown siblings are omitted. A body that would exceed host <code>MAX_SKILL_BYTES</code> is not written.</li><li><strong>Specs linked</strong>: <code>04-ux/06-settings-ia.md</code>, ADR 0243</li><li><strong>Acceptance</strong>: Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Automated (<code>pnpm test:e2e:skill-market</code>, <code>apps/desktop/test/skill-market-scan.test.mjs</code>)</li></ul><h4 id="e2e-skill-market-install-market-install-writes-a-user-skill-through-skills-create" tabindex="-1">E2E-SKILL-MARKET-INSTALL: Market install writes a user skill through skills.create <a class="header-anchor" href="#e2e-skill-market-install-market-install-writes-a-user-skill-through-skills-create" aria-label="Permalink to &quot;E2E-SKILL-MARKET-INSTALL: Market install writes a user skill through skills.create&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Host binary; isolated HOME. A builtin catalog entry with an assembled markdown body.</li><li><strong>Steps</strong>: Handshake; <code>skills.create</code> with the assembled name/description/body; read <code>~/.agents/skills/pdf.md</code>; <code>skills.list</code>.</li><li><strong>Expected</strong>: The file has rendered frontmatter and the instruction body. The skill appears in <code>skills.list</code>. No path besides <code>skills.create</code> is used.</li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code> §12b, ADR 0243</li><li><strong>Acceptance</strong>: Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Automated (<code>pnpm test:e2e:skill-market</code>)</li></ul><h4 id="e2e-skill-market-id-align-scanned-skill-ids-match-host-valid-capability-id" tabindex="-1">E2E-SKILL-MARKET-ID-ALIGN: Scanned skill ids match host valid_capability_id <a class="header-anchor" href="#e2e-skill-market-id-align-scanned-skill-ids-match-host-valid-capability-id" aria-label="Permalink to &quot;E2E-SKILL-MARKET-ID-ALIGN: Scanned skill ids match host valid_capability_id&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Shared <code>sanitizeSkillCatalogId</code>.</li><li><strong>Steps</strong>: Sanitize <code>Frontend_Design</code>, <code>1-pdf</code>, and an empty remainder.</li><li><strong>Expected</strong>: Host-legal slugs (<code>frontend-design</code>, <code>1-pdf</code>, <code>skill-7</code>) so <code>installedIds</code> matches the created record.</li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code> §12b</li><li><strong>Acceptance</strong>: Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Automated (<code>pnpm test:e2e:skill-market</code>)</li></ul><h4 id="e2e-tray-bounded-session-navigation" tabindex="-1">E2E-TRAY-bounded-session-navigation <a class="header-anchor" href="#e2e-tray-bounded-session-navigation" aria-label="Permalink to &quot;E2E-TRAY-bounded-session-navigation&quot;">​</a></h4><ul><li><strong>Scope</strong>: Native tray groups, hidden/recreated window activation, and unread semantics (issue #293, ADR tray-session-shortcuts).</li><li><strong>Preconditions</strong>: At least four running, four unread, and four pinned sessions across two projects; include overlaps, read-latest/older-unread notifications, archived/deleted sessions, an archived project, empty titles, multiline titles, long CJK/emoji titles, and literal ampersands. Also cover a single populated group of seven and one of more than nine, with the other two groups empty, to exercise reclaimed share. Use an isolated profile. Repeat native activation on macOS and Windows/Linux.</li><li><strong>Steps</strong>: Hide the main window and open the tray menu. Inspect group order, counts, duplicates, titles, and unchanged unread records. Choose the third row from another project, then View more from Settings with a collapsed sidebar and a retained search query. Finish/abort tasks while hidden; read a result, pin/unpin, rename, archive/restore, and delete a session. Close the macOS window while a task runs and let it finish, then activate its tray row while the new renderer bootstraps a pending plan. Delay a Host read while a newer preference update, delete, or Host restart arrives. Retry a transient read failure by hovering/right-clicking the tray on Windows/Linux; macOS retries from the next session or inbox event instead of mouse-enter. Repeat after clearing all group memberships and changing shipped locales. Choose Quit then Cancel, then Quit and confirm.</li><li><strong>Expected</strong>: Running → Unread → Pinned; at most nine rows in total. Each non-empty group keeps up to three rows and overflowing groups reclaim the share smaller groups leave unused, in priority order: seven running sessions with no unread or pinned show all seven, and a single group holding more than nine shows nine behind View more. Deduplicate before limits, so hidden Running overflow cannot appear as Unread/Pinned. Empty groups and stale shortcuts disappear. Unread uses the latest terminal result per session, newest first. Titles remain one line within the 32-column cap, half as many characters for CJK/emoji, including literal ampersands. Opening the macOS menu leaves the window hidden and records unread. A row opens exactly that session/project, acknowledges it normally, and wins over startup navigation. View more returns from Settings, closes search, and expands session navigation. Hidden/closed windows receive fresh groups; stale reads, archived/deleted targets, and a failed backend cannot restore stale shortcuts. Open, localization, quit cancellation, and shutdown work.</li><li><strong>Specs linked</strong>: <code>03-runtime/01-ipc-protocol.md</code> §13b, <code>03-runtime/07-process-model.md</code>, <code>04-ux/08-component-spec.md</code>, <code>04-ux/09-interaction-patterns.md</code>, ADR tray-session-shortcuts.</li><li><strong>Acceptance</strong>: A (app/window lifecycle), C (conversation navigation), F (unread persistence), Quality (bounded localized menu).</li><li><strong>Milestone</strong>: Post-M6 desktop shell maintenance.</li><li><strong>Status</strong>: Draft; native E2E requires an explicitly authorized run.</li></ul><h4 id="e2e-plugin-turn-ended-once-per-host-turn-a-plugin-observes-exactly-one-turn-end-event-per-host-turn" tabindex="-1">E2E-PLUGIN-turn-ended-once-per-host-turn: A plugin observes exactly one turn-end event per host turn <a class="header-anchor" href="#e2e-plugin-turn-ended-once-per-host-turn-a-plugin-observes-exactly-one-turn-end-event-per-host-turn" aria-label="Permalink to &quot;E2E-PLUGIN-turn-ended-once-per-host-turn: A plugin observes exactly one turn-end event per host turn&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A plugin with a tool and an event listener for <code>session:turnEnded</code> is loaded and enabled; its panel records each received payload and the <code>turnId</code> its tool receives through the tool context.</li><li><strong>Steps</strong>: <ol><li>Submit a prompt whose reply issues three tool calls in one turn.</li><li>Record the number of <code>session:turnEnded</code> payloads the plugin receives and compare the <code>turnId</code> with the one the plugin&#39;s tool saw.</li><li>Submit another prompt, then stop it with <code>Cmd/Ctrl + .</code>.</li><li>Submit a third prompt that fails, so the turn ends with an error.</li><li>Inspect the Plugins settings page for a new permission review.</li></ol></li><li><strong>Expected</strong>: Step 2 receives exactly one <code>session:turnEnded</code> whose <code>reason</code> is <code>completed</code>, and its <code>turnId</code> equals the tool context&#39;s <code>turnId</code>. Step 3 receives exactly one event with <code>reason</code> <code>aborted</code> — never a second <code>completed</code> after the abort. Step 4 receives exactly one event with <code>reason</code><code>error</code>. A turn that never started emits nothing, and no plugin receives two events for one turn even when the terminal event arrives more than once. Step 5 shows no new permission review, and subscribing to an unknown event name does not surface an error.</li><li><strong>Specs linked</strong>: <code>07-plugins/03-plugin-api.md</code>, <code>07-plugins/13-plugin-permissions-matrix.md</code>, ADR 0252</li><li><strong>Acceptance</strong>: Quality (protocol and plugin contract)</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Module-covered (<code>apps/desktop/test/session-turn-ended.test.mjs</code>, <code>apps/desktop/test/queued-turn-finalization.test.mjs</code>); desktop journey Draft (run only in a capable environment when this surface changes)</li></ul><h3 id="e2e-session-native-pi-continue-appends-original-jsonl" tabindex="-1">E2E-SESSION-native-pi-continue-appends-original-jsonl <a class="header-anchor" href="#e2e-session-native-pi-continue-appends-original-jsonl" aria-label="Permalink to &quot;E2E-SESSION-native-pi-continue-appends-original-jsonl&quot;">​</a></h3><ul><li><strong>Preconditions:</strong> A synthetic Pi v3 session contains branches, compaction metadata (including <code>retainedTail</code>), model/thinking changes, custom and context-bearing custom-message entries, and a valid project cwd. A faux local model/auth binding is configured in the fixture Pi agent directory; Desktop provider secrets are absent. A fixture extension restores custom state in <code>session_start</code> and contributes a skill through <code>resources_discover</code>.</li><li><strong>Steps:</strong> Start PI-Desktop with fixture-only agent/session directories; refresh sessions; open the native row beside a Desktop row; submit one text prompt; stop or let the faux response settle; send the same text again with a retryable response before success; reselect/refresh repeatedly; reopen through a fresh Pi <code>SessionManager</code>; invoke native compact and queue push/list.</li><li><strong>Expected:</strong> The original fixture JSONL receives native SDK entries whose parent starts at the previous current leaf; all prior bytes/unknown entries remain unchanged; the new leaf is visible after refresh; no Desktop SQLite session or Desktop transcript copy is created; the Desktop-owned row still follows its existing runtime and storage path. Native requests expose zero built-in or extension model tools; startup and discovered skill are effective. Exactly two durable SDK-ID user rows remain, including after abort; smart-stop never rewrites native history. Retries/compaction remain running and stoppable until one terminal completion; the owned idle lease accepts the second send. Compact and queue push/list reject before host access. Opaque host-turn queue remove/prioritize remain Desktop-only because native paths create no entries.</li><li><strong>Specs:</strong> runtime §12; storage §12; security §12; ADR 0254.</li><li><strong>Status:</strong> Documented; run after integration into main.</li></ul><h3 id="e2e-session-native-pi-external-change-fails-closed" tabindex="-1">E2E-SESSION-native-pi-external-change-fails-closed <a class="header-anchor" href="#e2e-session-native-pi-external-change-fails-closed" aria-label="Permalink to &quot;E2E-SESSION-native-pi-external-change-fails-closed&quot;">​</a></h3><ul><li><strong>Preconditions:</strong> A writable synthetic native v3 fixture is listed.</li><li><strong>Steps:</strong> Acquire Desktop continuation ownership, then simulate a foreign append/replacement before the next SDK append; also attempt a second Desktop lease and stale leases whose owners are live, remote, malformed, or uncertain. Refresh list/detail with a dead same-host owner and unchanged or complete append-only extended bytes, then prompt through normal UI capabilities.</li><li><strong>Expected:</strong> The second writer is refused; external divergence tears down continuation with a visible read-only/error reason; no requested entry is appended and no bytes are truncated or rewritten; a lease releases on normal dispose and is reclaimed after crash only with a dead same-host owner and an unchanged target or same-file prefix-preserving complete parent-chain extension. Uncooperative Pi clients can still race the OS append; no shared-lock guarantee is claimed.</li><li><strong>Specs:</strong> runtime §12; storage §12; security §12; ADR 0254.</li><li><strong>Status:</strong> Documented; run after integration into main.</li></ul><h3 id="e2e-session-native-pi-incompatible-session-is-read-only" tabindex="-1">E2E-SESSION-native-pi-incompatible-session-is-read-only <a class="header-anchor" href="#e2e-session-native-pi-incompatible-session-is-read-only" aria-label="Permalink to &quot;E2E-SESSION-native-pi-incompatible-session-is-read-only&quot;">​</a></h3><ul><li><strong>Preconditions:</strong> Fixtures cover missing cwd, missing trailing newline, v1/v2 header, unavailable saved provider/auth, and untrusted project resources.</li><li><strong>Steps:</strong> Refresh and open each fixture.</li><li><strong>Expected:</strong> Each remains browseable with a specific reason, the composer is disabled, no migration/repair/fallback occurs, and source bytes and mtimes are unchanged.</li><li><strong>Specs:</strong> IPC native routing; runtime §12; storage §12; security §12.</li><li><strong>Status:</strong> Documented; run after integration into main.</li></ul><h3 id="e2e-subagent-ordered-model-fallback-preserves-work" tabindex="-1">E2E-SUBAGENT-ordered-model-fallback-preserves-work <a class="header-anchor" href="#e2e-subagent-ordered-model-fallback-preserves-work" aria-label="Permalink to &quot;E2E-SUBAGENT-ordered-model-fallback-preserves-work&quot;">​</a></h3><ul><li><strong>Preconditions</strong>: A configured primary and at least three alternatives use deterministic local transports; one definition declares the ordered alternatives. A second definition has no alternatives, and none are opted in for Task overrides.</li><li><strong>Steps</strong>: Add two alternatives in Settings, reorder them, save and reopen; change another field without changing the list. Run the child, complete one tool call, then fail the primary request. Fail the first alternative and complete the second. Run a matrix with zero, one, two, and three unavailable models before a successful model, leaving an unused model after success. Repeat with all four models unavailable, mixed 401/403/404 failures, exhausted transient/429 retries on two consecutive models, unavailable/duplicate pins, an explicit authorized Task primary, and Stop during recovery. Remove all alternatives, save, and reopen.</li><li><strong>Expected</strong>: List order and clear/preserve semantics round-trip. Provider retry budgets precede fallback; each distinct configured binding is used once. The original task and completed tool results reach the next model; completed tools are not replayed. Adapter/auth/headers and thinking match each selected model. Failure diagnostics and total usage survive settlement; effective model/thinking survive reload. Stop cancels the chain; host/tool errors do not switch models. Exhaustion fails explicitly. Alternatives never authorize a Task override for the second definition. No alternatives retains the existing single-model behavior.</li><li><strong>Specs linked</strong>: <code>03-runtime/02-agent-runtime.md</code> §5f, <code>03-runtime/13-model-catalog-and-selection.md</code> §Subagent editor, ADR subagent-model-fallback.</li><li><strong>Acceptance criterion</strong>: C — Conversation &amp; stream; Quality (compatibility and permission isolation).</li><li><strong>Milestone</strong>: M6+.</li><li><strong>Status</strong>: Automated registry and runtime coverage through shared/runtime regression tests and <code>test:e2e:subagents</code> / <code>test:e2e:subagent-models</code>. The sidecar suite checks the zero-to-three failure matrix and four-model exhaustion against actual HTTP request order, live/settled Task metadata, ordered failure diagnostics, and the successful child report. Completed-tool preservation and independent retry budgets use real-transport runtime tests. The configuration-editor journey passed under WSL; task-transcript reload acceptance remains outstanding. Required post-integration suites: <code>test:e2e</code>, <code>test:e2e:subagents</code>, <code>test:e2e:subagent-models</code>.</li></ul><h4 id="e2e-plugin-declared-provider-appears-in-the-native-provider-list-a-plugin-declared-provider-is-a-host-owned-row-with-thinking-controls" tabindex="-1">E2E-PLUGIN-declared-provider-appears-in-the-native-provider-list: A plugin-declared provider is a Host-owned row with thinking controls <a class="header-anchor" href="#e2e-plugin-declared-provider-appears-in-the-native-provider-list-a-plugin-declared-provider-is-a-host-owned-row-with-thinking-controls" aria-label="Permalink to &quot;E2E-PLUGIN-declared-provider-appears-in-the-native-provider-list: A plugin-declared provider is a Host-owned row with thinking controls&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: An installed local plugin declares one provider in <code>contributes.providers</code> with the <code>provider.register</code> permission, one model, a fixture <code>baseUrl</code>, <code>thinkingLevels: [&quot;off&quot;, &quot;low&quot;, &quot;high&quot;]</code>, and <code>defaultThinkingLevel: &quot;high&quot;</code>; a key is stored once through Settings.</li><li><strong>Steps</strong>: 1) Enable the plugin and open Settings → Providers. 2) Inspect the model binding and Composer thinking selector, then select the row as the session model and run a turn. 3) Change the session thinking level and run a second turn. 4) Try to edit the provider, then delete it, through the user path. 5) Disable the plugin, inspect the list and stored credential, and re-enable it. 6) Uninstall the plugin; reinstall and enable it, then remove the declaration from its manifest and reload. 7) Load a manifest that declares providers without <code>provider.register</code>. 8) Load a manifest that declares an <code>oauth</code> block and <code>authKind: &quot;oauth&quot;</code>. 9) Load manifests whose model uses a non-array <code>thinkingLevels</code>, a non-string entry, or a non-string <code>defaultThinkingLevel</code>.</li><li><strong>Expected</strong>: Step 1 shows one row in the native provider list with <code>ownerPluginId</code> set to the plugin and the row id <code>plugin:&lt;pluginId&gt;:&lt;declaredId&gt;</code>. Its model binding contains the normalized levels <code>off</code>, <code>low</code>, and <code>high</code> in declaration order, with <code>high</code> as the default. Step 2 shows those declared levels in the thinking selector and starts the session at <code>high</code>; the turn uses that choice. Step 3 persists and uses the changed session level without changing the provider declaration. Step 4 refuses both actions with an error whose message begins <code>PROVIDER_OWNED_BY_PLUGIN</code> and leaves the row unchanged. Step 5 keeps the row and sets <code>enabled = 0</code> while <code>secret:provider:&lt;id&gt;:api_key</code> stays stored, so re-enabling restores the credential and thinking binding. Step 6 deletes the row and both credential refs (<code>:api_key</code> and <code>:oauth</code>) in both orders — uninstall, and a manifest that no longer declares the provider. Steps 7 and 8 fail manifest validation as <code>PLUGIN_INVALID</code> — the missing-permission message and <code>plugin OAuth providers are not supported in this release</code> / <code>unsupported authKind oauth</code> — and neither failure changes plugin enablement. Step 9 rejects each malformed thinking-level field with <code>PLUGIN_INVALID</code> and leaves plugin enablement unchanged.</li><li><strong>Specs linked</strong>: <code>07-plugins/02-plugin-manifest-schema.md</code> §4, §5.4, §7; <code>07-plugins/13-plugin-permissions-matrix.md</code>; <code>03-runtime/04-data-storage.md</code> §4.3, §7; <code>03-runtime/11-provider-model-system.md</code> §6.2; <code>03-runtime/12-provider-config-schema.md</code> §2, §9; <code>03-runtime/06-host-rpc-protocol.md</code>; ADR 0259; D427</li><li><strong>Acceptance</strong>: B (model config), E (tools &amp; permissions), F (persistence), G (plugins), Security, Quality</li><li><strong>Milestone</strong>: Post-MVP (R7 v1)</li><li><strong>Status</strong>: Partially automated (<code>pnpm test:e2e:trusted-extensions</code>): the declared row materializing as <code>plugin:&lt;pluginId&gt;:&lt;declaredId&gt;</code> in the native provider list with its <code>ownerPluginId</code>, endpoint, models, and thinking binding passes. Ownership refusal (<code>PROVIDER_OWNED_BY_PLUGIN</code>), disable/enable, undeclare and uninstall cleanup, and the manifest refusals are covered by host-core unit tests; the renderer&#39;s read-only row and thinking-selector presentation remain additional validation.</li></ul><h4 id="e2e-chat-disclosure-toggle-keeps-reading-position" tabindex="-1">E2E-CHAT-disclosure-toggle-keeps-reading-position <a class="header-anchor" href="#e2e-chat-disclosure-toggle-keeps-reading-position" aria-label="Permalink to &quot;E2E-CHAT-disclosure-toggle-keeps-reading-position&quot;">​</a></h4><ul><li><strong>Scope</strong>: Manual disclosure of a tool, thinking or activity title in a transcript or a delegate run dock while the scroller is pinned to the bottom (issue #324).</li><li><strong>Preconditions</strong>: A session longer than one screen, sitting at the bottom with follow mode on, holding a tool row, a thinking row and an activity group whose expanded detail is taller than its header, plus one expanded delegate run that owns a nested scroller. Repeat with a finished turn (<code>isRunning</code> false) and while the turn streams.</li><li><strong>Steps</strong>: Click the title of a tool row, a thinking row and an activity group while pinned at the bottom, and again after the turn has finished. Repeat with the viewport parked in the middle of the transcript, with a keyboard activation (Enter, then Space, on a focused title), and from the collapse rail. Expand a tool row inside an expanded delegate dock. Then scroll with the wheel, press ArrowUp with a title focused, click the jump-to-latest control, and send a new prompt.</li><li><strong>Expected</strong>: The clicked title keeps its on-screen position while the detail animates open and closed, and the transcript never re-bottoms underneath it; follow mode is left and the newest-message control appears. The held position survives every frame of an animated activity group and also covers a simultaneous height change above the title. A nested dock holds its own position and the transcript behind it does not re-bottom either. Real scroll input, the jump control, a new turn and every navigation release the hold and follow the live tail again, while a reader who scrolled up keeps their place. Space still activates a focused title and arrow keys still scroll.</li><li><strong>Specs linked</strong>: <code>04-ux/09-interaction-patterns.md</code> §9.1; ADR transcript-reading-ownership; D287, D302, D430</li><li><strong>Acceptance</strong>: C (conversation &amp; stream), E (tools &amp; permissions), Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Partially automated. <code>pnpm test:e2e:transcript-disclosure</code> mounts the real transcript scroll hook and a real tool row in a real 600 CSS px Electron viewport and clicks the title with a real DOM click, asserting the title&#39;s offset from the scroller top and the scroll offset for the transcript and for a nested follow scroller; without the fix the same fixture reports the title moving by the full height of the opened detail. The app stylesheet is not linked, so the heights come from inline filler and the components&#39; own intrinsic size. <code>disclosure-anchor.test.mjs</code> covers the pure anchor and input math, and <code>transcript-disclosure-reading.test.mjs</code> covers the wiring. Keyboard, wheel and animated-activity-group paths remain additional validation.</li></ul><h4 id="e2e-plugin-fs-root-follows-the-calling-session-a-plugin-tool-s-fs-root-follows-its-own-session-not-the-visible-workspace" tabindex="-1">E2E-PLUGIN-fs-root-follows-the-calling-session: A plugin tool&#39;s fs root follows its own session, not the visible workspace <a class="header-anchor" href="#e2e-plugin-fs-root-follows-the-calling-session-a-plugin-tool-s-fs-root-follows-its-own-session-not-the-visible-workspace" aria-label="Permalink to &quot;E2E-PLUGIN-fs-root-follows-the-calling-session: A plugin tool&#39;s fs root follows its own session, not the visible workspace&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: Two projects, A and B, each holding a text file the other does not, and one enabled plugin that registers an agent tool whose execute calls <code>pi.fs.readText</code> and <code>pi.fs.glob</code> on a root-relative path and declares <code>fs.read</code> with the <code>workspace</code> root. Both projects have a live session with the plugin tool available, and the window shows project A.</li><li><strong>Steps</strong>: <ol><li>Invoke the tool from session A on the file only project A holds, then from session B (project B) on the file only project B holds, without switching the window&#39;s visible workspace.</li><li>Switch the visible workspace to project B and repeat both calls.</li><li>With the visible workspace on project B, ask session A&#39;s tool for a path that exists only under project A, and session B&#39;s tool for the same path.</li><li>Open the plugin&#39;s panel and call the same read over the panel bridge with project A visible, then with project B visible.</li><li>Move one session to a temporary chat so no workspace is visible, invoke the tool from that session, then call the panel bridge in the same state.</li></ol></li><li><strong>Expected</strong>: Steps 1 and 2 answer from the invoking session&#39;s own project every time: session A reads and globs project A&#39;s file, session B project B&#39;s file, and neither answer changes when the visible workspace moves between the two projects. Step 3 returns project A&#39;s file for session A and <code>NOT_FOUND</code> for session B, so a session can never reach the other project&#39;s root. Step 4 is unchanged from before: each panel call resolves the visible workspace, so its answer follows the project on screen rather than either session. Step 5 keeps working per session -- the session&#39;s plugin tool resolves its own project -- and the panel call still fails with <code>NOT_FOUND</code> (&quot;No workspace is open&quot;). No gate is relaxed anywhere: a denied name, a <code>..</code> or absolute path, a symlink out of the root, and a path outside the declared scope behave as they do for a single visible workspace.</li><li><strong>Specs linked</strong>: <code>07-plugins/03-plugin-api.md</code> §3, <code>07-plugins/13-plugin-permissions-matrix.md</code> §6, ADR 0016, ADR 0249, ADR 0266, D093</li><li><strong>Acceptance</strong>: G (plugins), Security, Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Partially automated (<code>apps/desktop/test/plugin-fs-session-root.test.mjs</code>): a tool call writes and reads under the project of the invoking session, a panel call and an untracked session fall back to the visible workspace, a <code>userSelected</code> mode keeps the picked directory, and a session root stands in when the window shows no project. The two-live-sessions desktop journey and the panel step are Draft (run only in a capable environment when this surface changes)</li></ul><h4 id="e2e-model-catalog-window-correction-reaches-saved-bindings" tabindex="-1">E2E-MODEL-catalog-window-correction-reaches-saved-bindings <a class="header-anchor" href="#e2e-model-catalog-window-correction-reaches-saved-bindings" aria-label="Permalink to &quot;E2E-MODEL-catalog-window-correction-reaches-saved-bindings&quot;">​</a></h4><ul><li><strong>Goal</strong>: a models.dev limit correction reaches an already saved binding without deleting and re-adding the model, while a number the user entered in Settings is never overwritten.</li><li><strong>Steps</strong>: <ol><li>Configure a provider, select a model models.dev publishes a <code>limit.context</code> for, and save. Open the row&#39;s Advanced body and read the context-window field and its hint.</li><li>Serve a corrected catalog record for that model (a different published window), reopen Settings, and read the row, the context inspector, and the window a new session launches with.</li><li>Type a window in the Advanced field — the preset ladder once and a hand-typed <code>128000</code> once — save, then serve another catalog correction and reopen Settings and the inspector.</li><li>Save and reopen a provider row whose binding carries no <code>contextWindowSource</code>: once with the generic <code>128000</code> seed, once with any other stored value.</li></ol></li><li><strong>Expected</strong>: Step 1 shows the published number with the &quot;follows models.dev&quot; hint. Step 2 shows the corrected number everywhere the effective window is used (settings row, context inspector, session launch) with no delete and re-add. Step 3 keeps the entered number in the settings row, in the inspector, and in the launched request, including a hand-typed <code>128000</code> for a model whose published window is larger, and the hint is gone. Step 4 resolves deterministically: the <code>128000</code> seed follows the catalog, every other value stays as stored. Every step keeps the marker across the save/read round trip of the provider row, and a config written before the marker stays readable.</li><li><strong>Specs linked</strong>: <code>03-runtime/13-model-catalog-and-selection.md</code> §9.1, <code>03-runtime/12-provider-config-schema.md</code> §2, <code>03-runtime/11-provider-model-system.md</code> §2, <code>04-ux/06-settings-ia.md</code> §2</li><li><strong>Acceptance</strong>: B (model config), F (persistence), Quality</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Partially automated: <code>apps/desktop/test/model-binding-catalog-source.test.mjs</code> drives the main-process resolver (catalog-sourced correction reaches the exposed row, a user value survives it, the generic seed still follows the catalog, an inherited value stays marked); <code>packages/shared/src/model-catalog.test.ts</code> covers the four source rules; <code>crates/host-core/src/providers/catalog.rs</code> covers the config round trip, the unmarked record, and the dropped unknown marker. The end-to-end settings journey</li></ul><h4 id="e2e-plugin-official-channel-resolves-through-the-platform-an-official-channel-install-resolves-through-the-platform-and-installs-from-the-first-working-mirror" tabindex="-1">E2E-PLUGIN-official-channel-resolves-through-the-platform: An official-channel install resolves through the platform and installs from the first working mirror <a class="header-anchor" href="#e2e-plugin-official-channel-resolves-through-the-platform-an-official-channel-install-resolves-through-the-platform-and-installs-from-the-first-working-mirror" aria-label="Permalink to &quot;E2E-PLUGIN-official-channel-resolves-through-the-platform: An official-channel install resolves through the platform and installs from the first working mirror&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A clean profile on the official channel, a plugin present in <code>plugins.aiuo.net/catalog.json</code>, and a request log for the platform and both mirror hosts (a local stub may stand in for each).</li><li><strong>Steps</strong>: 1) Open Extensions → Marketplace and confirm the source line reads Official channel and that the catalog came from <code>plugins.aiuo.net</code>. 2) Install the plugin. 3) Capture the request the platform received. 4) Inspect which mirror served the package. 5) Install a second plugin, then install the same version of the first one again.</li><li><strong>Expected</strong>: Exactly one <code>POST /api/v1/download/resolve</code> is sent per install or update, with a JSON body carrying <code>deviceId</code>, <code>pluginId</code>, and the version when one was picked; the package comes from the first entry in <code>downloads</code> that answers, and its bytes match the returned <code>sha256</code> and <code>sizeBytes</code> before anything is extracted; a mirror that is unreachable or fails is abandoned and the next one is used without user interaction; reinstalling the same version issues a fresh resolve call rather than reusing the earlier answer, because the response is never cached; the installed plugin passes the ordinary permission review and its record names the official channel as its provider.</li><li><strong>Specs linked</strong>: <code>07-plugins/07-plugin-marketplace.md</code> §2, <code>07-plugins/15-plugin-center.md</code> §10</li><li><strong>Acceptance</strong>: G (remote marketplace source)</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Draft</li></ul><h4 id="e2e-plugin-mirror-digest-mismatch-falls-through-to-the-next-mirror-a-mirror-whose-bytes-fail-the-digest-is-rejected-before-extraction" tabindex="-1">E2E-PLUGIN-mirror-digest-mismatch-falls-through-to-the-next-mirror: A mirror whose bytes fail the digest is rejected before extraction <a class="header-anchor" href="#e2e-plugin-mirror-digest-mismatch-falls-through-to-the-next-mirror-a-mirror-whose-bytes-fail-the-digest-is-rejected-before-extraction" aria-label="Permalink to &quot;E2E-PLUGIN-mirror-digest-mismatch-falls-through-to-the-next-mirror: A mirror whose bytes fail the digest is rejected before extraction&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: An official-channel install whose <code>downloads</code> list has at least two entries, with the first mirror serving bytes that do not match the returned <code>sha256</code> (a stale distribution, or a stub that serves the CNB-era bytes for <code>pi.todo-0.6.5</code>), plus a view of the install cache and the plugin directory.</li><li><strong>Steps</strong>: 1) Start the install. 2) Watch the first mirror&#39;s download and the digest check. 3) Inspect the install cache and the plugin directory before the install finishes. 4) Let the install continue. 5) Repeat with a stub whose first mirror fails only the announced <code>sizeBytes</code>.</li><li><strong>Expected</strong>: The mismatching bytes are discarded without being extracted or handed to the installer, nothing lands in the plugin directory, and the rejection is reported in the install progress instead of being swallowed; the next mirror&#39;s bytes are verified against the same digest and the install completes from there; the size-mismatch case behaves identically; when every entry fails, the install ends as a reported failure rather than a partially installed plugin.</li><li><strong>Specs linked</strong>: <code>07-plugins/07-plugin-marketplace.md</code> §2</li><li><strong>Acceptance</strong>: G (remote marketplace source) + Security</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Draft</li></ul><h4 id="e2e-plugin-platform-unreachable-install-falls-back-to-the-catalog-url-an-install-falls-back-to-the-catalog-url-when-the-platform-cannot-be-reached" tabindex="-1">E2E-PLUGIN-platform-unreachable-install-falls-back-to-the-catalog-url: An install falls back to the catalog URL when the platform cannot be reached <a class="header-anchor" href="#e2e-plugin-platform-unreachable-install-falls-back-to-the-catalog-url-an-install-falls-back-to-the-catalog-url-when-the-platform-cannot-be-reached" aria-label="Permalink to &quot;E2E-PLUGIN-platform-unreachable-install-falls-back-to-the-catalog-url: An install falls back to the catalog URL when the platform cannot be reached&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: The official catalog is already cached from a successful refresh, and <code>plugins.aiuo.net</code> becomes unreachable for the install (a blocked stub, or a refused DNS/proxy route).</li><li><strong>Steps</strong>: 1) Refresh the catalog while the platform is reachable, then make it unreachable. 2) Install a plugin whose catalog entry carries a relative <code>url</code>. 3) Confirm which host served the package and whether the platform received a resolve request. 4) Restore reachability and install a version the platform refuses in turn with <code>403 NOT_PUBLISHED</code>, <code>403 PLUGIN_ARCHIVED</code>, <code>404</code>, <code>429</code>, and <code>503</code>.</li><li><strong>Expected</strong>: The install resolves the package from the catalog&#39;s own URL — <code>artifactBaseUrl</code> plus the relative <code>url</code> — and completes after the same shasum verification; no resolve request reaches the platform for that install and the fallback install is not counted; the failed resolve call is visible in the install log instead of being hidden; once the platform answers again each refusal produces its own message — not-published with no retry, archived hiding the plugin from install and update selection, not-found, one <code>Retry-After</code> wait for the rate limit, and a deployment error for <code>503</code> — and no refusal silently switches to another channel or another version.</li><li><strong>Specs linked</strong>: <code>07-plugins/07-plugin-marketplace.md</code> §2</li><li><strong>Acceptance</strong>: G (remote marketplace source)</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Draft</li></ul><h4 id="e2e-plugin-device-identifier-is-stable-and-never-the-machine-code-the-device-identifier-is-stable-across-launches-and-is-not-the-raw-machine-code" tabindex="-1">E2E-PLUGIN-device-identifier-is-stable-and-never-the-machine-code: The device identifier is stable across launches and is not the raw machine code <a class="header-anchor" href="#e2e-plugin-device-identifier-is-stable-and-never-the-machine-code-the-device-identifier-is-stable-across-launches-and-is-not-the-raw-machine-code" aria-label="Permalink to &quot;E2E-PLUGIN-device-identifier-is-stable-and-never-the-machine-code: The device identifier is stable across launches and is not the raw machine code&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: A host whose machine identifier is readable (Windows <code>MachineGuid</code>, the macOS platform UUID, or <code>/etc/machine-id</code>), a second environment where it is not readable, and a request-logging stub for <code>POST /api/v1/download/resolve</code>.</li><li><strong>Steps</strong>: 1) Trigger two installs in one session and compare the recorded <code>deviceId</code> values. 2) Restart the app and trigger a third install. 3) Compare the value with the raw machine identifier of the host. 4) Search the settings, the Marketplace surface, and the installed-plugin detail view for the value. 5) Repeat steps 1 and 2 where no machine identifier is readable, then inspect the application data directory.</li><li><strong>Expected</strong>: Every resolve request from one installation carries the same 64-character lowercase hex value, including after a restart and after an app reinstall while the machine identifier is unchanged; the value is neither the machine code nor a prefix of it, and it equals <code>sha256(&quot;pi-desktop.device.v1:&quot; + &lt;machine identifier&gt;)</code>; the identifier never appears in the UI and no setting can reveal or reset it; in the unreadable case the value is a different 64-hex string that is generated once and persisted under <code>plugins/market/device.json</code>, then repeated across restarts.</li><li><strong>Specs linked</strong>: <code>07-plugins/07-plugin-marketplace.md</code> §2, ADR 0276</li><li><strong>Acceptance</strong>: G (remote marketplace source) + Security</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Draft</li></ul><h4 id="e2e-plugin-install-progress-shows-phases-and-mirror-an-official-channel-install-reports-its-phases-and-the-mirror-it-is-using-then-completes" tabindex="-1">E2E-PLUGIN-install-progress-shows-phases-and-mirror: An official-channel install reports its phases and the mirror it is using, then completes <a class="header-anchor" href="#e2e-plugin-install-progress-shows-phases-and-mirror-an-official-channel-install-reports-its-phases-and-the-mirror-it-is-using-then-completes" aria-label="Permalink to &quot;E2E-PLUGIN-install-progress-shows-phases-and-mirror: An official-channel install reports its phases and the mirror it is using, then completes&quot;">​</a></h4><ul><li><p><strong>Preconditions</strong>: A clean profile on the official channel, a plugin whose resolve answer lists at least two entries, a first mirror that fails or is slow so a second attempt is observable, and a renderer subscribed to <code>plugin.installProgress</code>.</p></li><li><p><strong>Steps</strong>: 1) Start a manual install from the marketplace detail sheet. 2) Record the reports that arrive while it runs. 3) Hover the dialog after the install succeeds. 4) Look at the installed plugin once the install ends. 5) Install again with a large package and count the reports over a window of at least one second.</p></li><li><p><strong>Expected</strong>: The dialog shows the phases in order — <code>resolve</code>, <code>download</code>, <code>verify</code>, <code>install</code>, <code>enable</code> — with <code>mirror n/N · name</code> and a determinate bar from <code>receivedBytes</code> / <code>totalBytes</code>; every report carries <code>pluginId</code> and <code>version</code>, only the report that names a mirror carries <code>source</code>, and <code>attempt</code> counts 1-based within <code>attempts</code> while a mirror switch increments <code>attempt</code> without changing <code>attempts</code>; byte reports arrive at most once per 200 ms, with one extra report per phase change and one terminal report; the install ends with no <code>error</code> and the plugin is installed and enabled after the ordinary permission review; the dialog closes about two seconds after success, that countdown pauses while it is hovered, and a background auto-update installs the same way without opening the dialog at all.</p></li><li><p><strong>Specs linked</strong>: <code>07-plugins/07-plugin-marketplace.md</code> §2, <code>07-plugins/15-plugin-center.md</code> §10, ADR 0276 §7</p></li><li><p><strong>Acceptance</strong>: G (remote marketplace source)</p></li><li><p><strong>Milestone</strong>: M6+</p></li><li><p><strong>Status</strong>: Draft</p></li><li><p><strong>Local fallback regression</strong>: Set the process default data directory to a different temporary root, then create a plugin manager with its own root. All bundled fallback package URLs must stay beneath the manager&#39;s root; installing the bundled package must still report byte progress, validate its size and checksum, and preserve cancellation behavior.</p></li></ul><h4 id="e2e-plugin-cancel-during-download-installs-nothing-cancelling-during-the-download-stops-the-install-leaves-nothing-installed-and-closes-the-dialog-without-an-error" tabindex="-1">E2E-PLUGIN-cancel-during-download-installs-nothing: Cancelling during the download stops the install, leaves nothing installed, and closes the dialog without an error <a class="header-anchor" href="#e2e-plugin-cancel-during-download-installs-nothing-cancelling-during-the-download-stops-the-install-leaves-nothing-installed-and-closes-the-dialog-without-an-error" aria-label="Permalink to &quot;E2E-PLUGIN-cancel-during-download-installs-nothing: Cancelling during the download stops the install, leaves nothing installed, and closes the dialog without an error&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: An official-channel install of a package large enough or a mirror slow enough that the download phase lasts, a way to answer <code>market.cancelInstall</code>, and a view of the plugin directory, the install cache, and the Installed list.</li><li><strong>Steps</strong>: 1) Start the install and wait for the download phase. 2) Press the cancel action in the dialog. 3) Watch the dialog and capture the RPC answer. 4) Inspect the plugin directory, the install cache, and the Installed list after the install ends. 5) Send <code>market.cancelInstall</code> again for the same id, for an install that is not running, and once after the download has finished.</li><li><strong>Expected</strong>: The cancel call answers <code>{ cancelled: true, id }</code> for the running install, and the install fails with <code>PLUGIN_CANCELLED</code> (JSON-RPC code 1019); the dialog closes without reporting an error; nothing is installed — no plugin directory, no installed row, no enabled plugin — and no partial package survives in the cache; the second call for the same id, the call for an install that is not running, and a call after the download finished answer <code>{ cancelled: false, id }</code> and change nothing, so a cancel can never interrupt the write of the plugin directory.</li><li><strong>Specs linked</strong>: <code>07-plugins/07-plugin-marketplace.md</code> §2, ADR 0276 §7</li><li><strong>Acceptance</strong>: G (remote marketplace source) + Security</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Draft</li></ul><h4 id="e2e-plugin-failed-install-lists-tried-mirrors-a-failed-install-keeps-the-dialog-open-and-lists-the-mirrors-it-tried-with-a-copy-action" tabindex="-1">E2E-PLUGIN-failed-install-lists-tried-mirrors: A failed install keeps the dialog open and lists the mirrors it tried with a copy action <a class="header-anchor" href="#e2e-plugin-failed-install-lists-tried-mirrors-a-failed-install-keeps-the-dialog-open-and-lists-the-mirrors-it-tried-with-a-copy-action" aria-label="Permalink to &quot;E2E-PLUGIN-failed-install-lists-tried-mirrors: A failed install keeps the dialog open and lists the mirrors it tried with a copy action&quot;">​</a></h4><ul><li><strong>Preconditions</strong>: An official-channel install whose every mirror fails — for example a digest mismatch on the first and a network error on the second — with a renderer subscribed to <code>plugin.installProgress</code> and a clipboard readback.</li><li><strong>Steps</strong>: 1) Start the install. 2) Let every mirror fail. 3) Read the terminal report and the dialog. 4) Use the copy action, then the retry action once the mirrors serve valid bytes again.</li><li><strong>Expected</strong>: The terminal report carries <code>error</code> and a <code>tried[]</code> entry per mirror in the order they were tried, each naming its <code>source</code>, <code>url</code>, and the error that mirror answered; the dialog stays open and shows the readable error plus that list; the copy action puts the tried mirrors on the clipboard; the retry action starts a new install of the same version and completes it when the mirrors answer, without reusing the failed attempt&#39;s partial state; nothing was installed by the failed attempt.</li><li><strong>Specs linked</strong>: <code>07-plugins/07-plugin-marketplace.md</code> §2, ADR 0276 §7</li><li><strong>Acceptance</strong>: G (remote marketplace source)</li><li><strong>Milestone</strong>: M6+</li><li><strong>Status</strong>: Draft</li></ul><h3 id="e2e-chat-turn-process-and-thinking-display" tabindex="-1">E2E-CHAT-turn-process-and-thinking-display <a class="header-anchor" href="#e2e-chat-turn-process-and-thinking-display" aria-label="Permalink to &quot;E2E-CHAT-turn-process-and-thinking-display&quot;">​</a></h3><ul><li><strong>Preconditions:</strong> A turn with progress paragraph A, multiple searches plus thinking, progress paragraph B, multiple commands plus thinking, and a final answer; Detailed and Compact display modes; legacy message-level transcript search targets.</li><li><strong>Steps:</strong> Review the nested disclosure path in Detailed, including independent group/item toggles, parent close/reopen, a singleton segment, literal-final-item leaf selection, failure/denial/recovery, retained-pane remounts and a legacy search reveal. Repeat in Compact and with permission/question/plan/goal action cards, a stopped partial answer, an assistant error and delegated child work.</li><li><strong>Expected:</strong> Both modes use one whole-process disclosure and leave the final answer, assistant errors, stopped trailing text and pending actions outside it. Detailed starts active/completed processes open; the active multi-item group is open and an untouched group closes on completion. Compact starts processes and groups closed, hides reasoning, and keeps payloads closed; an untouched active process with a recorded failed/denied tool stays open through recovery and closes on completion. Singletons have no group. Detailed auto-opens only an eligible literal final tool/search item of the last activity group; it does not scan past thinking, and failed/denied leaves stay closed. Parent/child/sibling states remain independent, pane-owned user choices survive updates, mode changes and remounts, and renderer restart reapplies defaults. Search reveals the process and activity group that own the named message once per request; item-level targeting is not part of this change, and Compact reasoning requires an explicit switch to Detailed. Saved mode survives restart and a missing/unknown setting resolves to Detailed.</li><li><strong>Validation scope for the 2026-09-20 change:</strong> Nested disclosure and activity group presentation only; precise item-level transcript search targeting is out of scope and keeps the existing message-level search behavior.</li><li><strong>Specs:</strong> 04-ux/06-settings-ia, 04-ux/08-component-spec, 04-ux/09-interaction-patterns; ADR turn-process-and-thinking-display.</li></ul><h3 id="e2e-rpc-unicode-separators" tabindex="-1">E2E-RPC-unicode-separators <a class="header-anchor" href="#e2e-rpc-unicode-separators" aria-label="Permalink to &quot;E2E-RPC-unicode-separators&quot;">​</a></h3><ul><li><strong>Preconditions:</strong> Built host-core, shared package and agent runtime; isolated temporary data directory; loopback-only fixture provider. No live credentials.</li><li><strong>Steps:</strong> Append a user message containing U+2028/U+2029, CJK text, emoji and escaped CR/LF; read it, restart the host, and read it again. Send a Unicode prompt through AgentSidecar; restore history through the parent host proxy; stream and persist a Unicode answer. Send an unknown method containing the same characters, then a health request.</li><li><strong>Expected:</strong> Text survives unchanged across persistence and all stdio directions. Requests settle without RPC timeouts; error replies and subsequent requests remain usable. No migration of existing sessions is needed.</li><li><strong>Automation:</strong> <code>pnpm test:e2e:rpc-unicode</code>; <code>packages/shared/src/ndjson.test.ts</code> additionally checks every UTF-8 split boundary, consecutive frames, CRLF, EOF and disposal.</li><li><strong>Specs:</strong> 03-runtime/06-host-rpc-protocol §2.</li><li><strong>Acceptance:</strong> A (runtime), C (sessions).</li><li><strong>Milestone:</strong> M6+.</li><li><strong>Status:</strong> Automated; run against the task/PR integration candidate.</li></ul><h3 id="e2e-provider-order-reorder-configured-ai-services" tabindex="-1">E2E-PROVIDER-ORDER: Reorder configured AI services <a class="header-anchor" href="#e2e-provider-order-reorder-configured-ai-services" aria-label="Permalink to &quot;E2E-PROVIDER-ORDER: Reorder configured AI services&quot;">​</a></h3><ul><li>Open Model configuration with three providers and drag the last before the first, then after the last; verify pointer-following cards, live slot preview, and saved order. Check movement threshold, action-button exclusion, edge scrolling, Escape cancellation, and automatic-scroll cleanup.</li><li>Cancel a drag and use Up/Down on a focused provider card. Verify no configuration or default-model changes. Failed saves report an error and retain accepted order; overlapping moves are blocked and late catalog reads cannot undo a move.</li><li>Reopen the Composer model menu: groups follow the provider order. Restart the host and renderer and verify persistence. Check English and Chinese labels.</li><li>Automated: <code>pnpm test:e2e:provider-order</code> runs the real settings component, sandboxed preload, provider IPC registrar, and an isolated Rust host. It uses synthetic Chromium pointer/key events and test providers without credentials; model-catalog/OAuth discovery is stubbed and no provider requests are sent.</li><li>Host unit tests cover no metadata, disabled/plugin rows, new/deleted IDs, stale moves, no-op moves, preserved configuration, and reopening the database.</li></ul><p>The provider-order suite also runs a 200-card fixture under a controlled animation clock. A burst of 200 pointer events may arm React state once, but stationary frames must produce no further renders or pending callbacks. Card geometry is read at press time, and move/release/cancel/unmount paths must clear transient transforms and queued frames. Release before the scheduled frame must still save the latest destination. These assertions measure work counts, not device FPS.</p><h3 id="e2e-chrome-window-controls-survive-work-panel" tabindex="-1">E2E-CHROME-window-controls-survive-work-panel <a class="header-anchor" href="#e2e-chrome-window-controls-survive-work-panel" aria-label="Permalink to &quot;E2E-CHROME-window-controls-survive-work-panel&quot;">​</a></h3><ul><li><strong>Preconditions:</strong> A built desktop and matching host-core binary; isolated profile and data directory with one local session; no real provider calls.</li><li><strong>Steps:</strong> Open the work panel from the titlebar, toggle the sidebar, maximize and restore the panel, maximize and restore the native window, close the panel, visit Settings and return, reopen the panel, minimize/restore the window, then click Close with close-to-tray configured in the disposable profile. Also enter and leave native fullscreen with the panel open. Check the three controls with Chromium hit testing throughout, including light/dark Windows/Linux CSS.</li><li><strong>Expected:</strong> Windows/Linux minimize, maximize/restore, and close stay visible and hit-testable at the window edge. Native actions work with the panel open. Preview sidebar navigation remains clickable. On macOS no duplicate renderer window controls appear; the native traffic-light and fullscreen contracts stay unchanged. CSS emulation does not qualify another operating system&#39;s native UI.</li><li><strong>Specs:</strong> <code>04-ux/01-ui-ia.md</code> titlebar; ADR 0021 and ADR 0025.</li><li><strong>Acceptance:</strong> Window actions remain accessible independently of pane state.</li><li><strong>Milestone:</strong> Post-MVP regression coverage.</li><li><strong>Automation:</strong> <code>pnpm test:e2e:window-controls</code>; macOS traffic-light geometry also has <code>apps/desktop/test/traffic-light-reserve.test.mjs</code> contract coverage.</li><li><strong>Status:</strong> Automated for the executing native platform; run on macOS/Linux runners for native qualification. Optional screenshots are written only to <code>PI_DESKTOP_CHROME_ARTIFACT_DIR</code>.</li></ul><h3 id="e2e-plugin-slow-tool-is-not-cut-off-by-host-dispatch" tabindex="-1">E2E-PLUGIN-slow-tool-is-not-cut-off-by-host-dispatch <a class="header-anchor" href="#e2e-plugin-slow-tool-is-not-cut-off-by-host-dispatch" aria-label="Permalink to &quot;E2E-PLUGIN-slow-tool-is-not-cut-off-by-host-dispatch&quot;">​</a></h3><ul><li><strong>Preconditions:</strong> A loaded plugin that registers an agent tool; the tool awaits work longer than 60s but shorter than the 110s plugin tool budget (for example an <code>agent.complete</code> call that takes ~70s).</li><li><strong>Steps:</strong> Ask the agent to use the tool, approve it if prompted, and wait for the call to finish. Repeat with the permission card left open for a while before approving.</li><li><strong>Expected:</strong> The call returns the plugin&#39;s result. host-core does not answer <code>TOOL_TIMEOUT</code> before Electron&#39;s budget expires, and no transport layer reports <code>host RPC timeout</code>, <code>sidecar RPC timeout</code>, or <code>parent host proxy timeout</code> for the call. Budgets nest from the inside out: plugin <code>agent.complete</code> 90s &lt; MCP call 100s &lt; plugin tool 110s &lt; host-core dispatch 150s, while the widest MCP leg (a lazy handshake, then a <code>tools/list</code> traversal, then the call) is 140s. The transport deadline covers the permission wait, the admission queue wait, and that dispatch, so a call that waited for a saturated plugin class before it was dispatched is still inside it.</li><li><strong>Specs:</strong> <code>07-plugins/12-plugin-ipc-and-host-services.md</code> agent-tool dispatch; ADR 0038; ADR 0174.</li><li><strong>Acceptance:</strong> G (plugin agent tool).</li><li><strong>Milestone:</strong> Post-MVP regression coverage.</li><li><strong>Automation:</strong> <code>apps/desktop/test/plugin-timeout-budgets.test.mjs</code> checks the budget order and the dispatch call site across the TypeScript and Rust sources; <code>packages/shared/src/protocol.test.ts</code> covers the transport deadline; host-core <code>desktop_dispatch_outlasts_every_electron_budget_it_wraps</code> covers the dispatch default.</li><li><strong>Status:</strong> Contract-covered; no end-to-end driver waits out a real 70s call.</li></ul><h3 id="e2e-plugin-crash-report-names-the-exit-code" tabindex="-1">E2E-PLUGIN-crash-report-names-the-exit-code <a class="header-anchor" href="#e2e-plugin-crash-report-names-the-exit-code" aria-label="Permalink to &quot;E2E-PLUGIN-crash-report-names-the-exit-code&quot;">​</a></h3><ul><li><strong>Preconditions:</strong> A loaded plugin whose host process dies on its own — <code>process.exit(7)</code> after writing one line to stderr is the fixture — with a resident service so the supervisor path is exercised too. Isolated desktop profile; no marketplace or network access.</li><li><strong>Steps:</strong> Load the plugin and let the host process die. Read the load error, the <code>failed</code> service state, the <code>plugin.crash</code> audit record and the <code>plugin</code> log channel. Repeat with a hard fault (a Windows <code>0xC0000005</code>-class exit) if one is available, and then quit the app while a plugin host is alive.</li><li><strong>Expected:</strong> Every one of those surfaces names the exit code (<code>exit code 7</code>, and <code>exit code 3221225477 (0xC0000005)</code> for the fault), while the fixture&#39;s stderr line is absent from the load error and crash audit record. A clean quit reports no crash at all: quitting is a shutdown, not a crash.</li><li><strong>Specs:</strong> <code>07-plugins/05-plugin-lifecycle.md</code> §3.1 / §8, <code>08-meta/decisions-log.md</code> D607.</li><li><strong>Acceptance:</strong> G (plugin host lifecycle), Quality (diagnosability).</li><li><strong>Milestone:</strong> Post-MVP regression coverage.</li><li><strong>Automation:</strong> <code>apps/desktop/test/plugin-services.test.mjs</code> forks a real host process, kills it with a fixture exit code and asserts the code plus the absence of raw stderr in the crash audit record; <code>plugin-isolation.test.mjs</code> and the shutdown cases cover the &quot;quit is not a crash&quot; half.</li><li><strong>Status:</strong> Automated at the runtime level; no UI driver reads the plugin page&#39;s error text.</li></ul><h3 id="e2e-dialog-long-text-boundaries" tabindex="-1">E2E-DIALOG-long-text-boundaries <a class="header-anchor" href="#e2e-dialog-long-text-boundaries" aria-label="Permalink to &quot;E2E-DIALOG-long-text-boundaries&quot;">​</a></h3><ul><li><strong>Preconditions:</strong> Built renderer assets and an isolated Electron component fixture; production components, store, translations and stylesheet are used. Only the preload IPC boundary is replaced with deterministic host replies.</li><li><strong>Steps:</strong> Open an extension input prompt with a long Windows source path; type, submit, reopen and dismiss. Repeat long title/message/option prompts in light/dark themes and Chinese at 520×480. Inspect long project/plugin names in rename, instructions, memory, delete, install, settings, permission-review and OAuth dialogs; exercise dismissal controls.</li><li><strong>Expected:</strong> Full source paths and other long text wrap without clipping; horizontal overflow is absent, and the 420px prompt width is unchanged. Tall prompts scroll inside the viewport. Close, Escape, typing and radio submission work.</li><li><strong>Specs:</strong> <code>04-ux/08-component-spec.md</code>, Dialog long-text containment.</li><li><strong>Acceptance:</strong> Bounded text and reachable modal controls.</li><li><strong>Milestone:</strong> Post-MVP maintenance.</li><li><strong>Automation:</strong> <code>pnpm test:e2e:dialog-overflow</code>; source/contract dialog suites supplement, but do not replace, real Chromium geometry and pointer checks.</li><li><strong>Status:</strong> Implemented. Native Windows evidence; macOS/Linux not qualified.</li></ul><h3 id="e2e-output-cap-716-keep-model-requests-inside-the-published-context-window" tabindex="-1">E2E-OUTPUT-CAP-716: Keep model requests inside the published context window <a class="header-anchor" href="#e2e-output-cap-716-keep-model-requests-inside-the-published-context-window" aria-label="Permalink to &quot;E2E-OUTPUT-CAP-716: Keep model requests inside the published context window&quot;">​</a></h3><ul><li><strong>Preconditions:</strong> A deterministic provider fixture exposes a published context window, supports parent and subagent requests, and records request payloads.</li><li><strong>Steps:</strong> Configure a user context override larger than the published window; send a CJK-heavy parent prompt, invoke a subagent, and run a one-shot completion. Repeat the parent request with <code>thinkingLevel: &quot;omit&quot;</code>.</li><li><strong>Expected:</strong> Each request carries a concrete output budget no larger than the remaining published window after input estimation and safety reserve. The runtime compacts against the published safety ceiling, and no request is sent with <code>input + output</code> beyond that ceiling. Small ASCII requests retain the configured budget when it fits.</li><li><strong>Specs linked:</strong> <code>03-runtime/02-agent-runtime.md</code>, <code>03-runtime/13-model-catalog-and-selection.md</code></li><li><strong>Acceptance:</strong> F (runtime provider requests), C (chat and stream)</li><li><strong>Status:</strong> Unit-covered; deterministic provider fixture pending</li></ul><h3 id="e2e-provider-certificate-trust-and-terminal-errors" tabindex="-1">E2E-PROVIDER-certificate-trust-and-terminal-errors <a class="header-anchor" href="#e2e-provider-certificate-trust-and-terminal-errors" aria-label="Permalink to &quot;E2E-PROVIDER-certificate-trust-and-terminal-errors&quot;">​</a></h3><ul><li><strong>Preconditions:</strong> Built request candidate incorporating current <code>origin/main</code>, Electron installed, isolated test process/profile and loopback HTTPS fixture. No real provider, credentials, user profile, or OS certificate-store writes.</li><li><strong>Steps:</strong> Run <code>node scripts/e2e-provider-certificates.mjs</code>. Launch the actual desktop sidecar and submit a chat prompt against an untrusted localhost certificate. Relaunch with its CA in <code>NODE_EXTRA_CA_CERTS</code>, then request the same certificate through a hostname absent from its SAN.</li><li><strong>Expected:</strong> The child&#39;s default CA set includes system roots and extra CAs. The first request fails once with a non-retriable certificate error and no retry status; the trusted request returns text; the hostname mismatch still fails once. TLS and hostname verification remain enabled.</li><li><strong>UI:</strong> Run <code>node scripts/e2e-provider-certificate-ui.mjs</code> for the real error component in isolated Chromium. Certificate errors get localized guidance; DNS and protocol errors retain the generic summary. Errno/raw details remain visible, and details can be closed and reopened. Optional <code>PI_CERTIFICATE_EVIDENCE_DIR</code> records a screenshot; <code>--baseline</code> uses the upstream error component with the same fixture and stylesheet.</li><li><strong>Lower-level coverage:</strong> <code>provider-certificate-flow.test.ts</code> enters main session <code>prompt()</code> and delegate <code>run()</code> through real Agent/pi-ai wiring, with only the external fetch mocked. Both stop after one request and retain the certificate cause. Error classification and recovery suites cover direct, nested, flattened, non-certificate and wrapped certificate failures.</li><li><strong>Limits:</strong> OS-root inclusion is checked without installing a root. The TLS success fixture uses a child-only extra CA; it does not reproduce a specific antivirus installation or claim native macOS/Linux verification.</li></ul><h3 id="e2e-provider-stored-binding-array-reads-entry-by-entry" tabindex="-1">E2E-PROVIDER-stored-binding-array-reads-entry-by-entry <a class="header-anchor" href="#e2e-provider-stored-binding-array-reads-entry-by-entry" aria-label="Permalink to &quot;E2E-PROVIDER-stored-binding-array-reads-entry-by-entry&quot;">​</a></h3><ul><li><strong>Preconditions:</strong> A throwaway data directory and the host-core binary; a provider created through <code>providers.create</code> with at least three complete bindings; no real provider or credentials.</li><li><strong>Steps:</strong> Call <code>providers.list</code> and confirm every binding returns. Edit the stored <code>config_json</code> to delete one binding&#39;s <code>maxTokens</code> and list again. Restore the field and list again. Then set one binding&#39;s <code>contextWindow</code> to a string and list once more. Attempt <code>providers.update</code> with the readable subset and an unrelated provider change.</li><li><strong>Expected:</strong> All three bindings return in every case except the malformed entry, where the readable bindings return in their stored order. The binding that lost <code>maxTokens</code> reads with the generic default output cap, and restoring the field restores the value. The host log names the provider id, the entry&#39;s index and the reason for the entry that could not be decoded. The explicit model-array update is rejected with <code>MODEL_BINDINGS_DEGRADED</code>, while the stored <code>config_json</code> remains unchanged.</li><li><strong>Specs linked:</strong> <code>03-runtime/12-provider-config-schema.md</code> §2, <code>08-meta/decisions-log.md</code> D610</li><li><strong>Acceptance:</strong> F (persistence), Quality</li><li><strong>Status:</strong> Unit-covered (<code>providers::catalog::tests</code>, <code>providers::tests::a_stored_array_survives_an_entry_that_lost_a_field</code>); the host RPC path is exercised by <code>scripts/e2e-smoke.mjs</code> for provider create and list, but no suite drives a hand-edited <code>config_json</code>.</li></ul><h3 id="e2e-context-estimate-calibration-stays-safe" tabindex="-1">E2E-CONTEXT-estimate-calibration-stays-safe <a class="header-anchor" href="#e2e-context-estimate-calibration-stays-safe" aria-label="Permalink to &quot;E2E-CONTEXT-estimate-calibration-stays-safe&quot;">​</a></h3><ul><li><strong>Preconditions:</strong> Deterministic provider fixture whose reported usage can be scripted; a session whose occupancy is close to the hard boundary; no real provider credentials.</li><li><strong>Steps:</strong> Report usage below the prediction repeatedly and confirm the displayed occupancy never falls below the floor and compaction still fires; report usage far outside the plausible band and confirm the value does not move; run a CJK-heavy session and compare the displayed occupancy with the reported usage.</li><li><strong>Expected:</strong> The calibrated occupancy stays within 0.85×–6× of the raw estimate, moves up as soon as there is evidence, moves down only after agreeing samples, and a projection at 1.18× the hard limit still compacts. Misreports move it in neither direction. CJK text no longer reads at a quarter of its measured cost.</li><li><strong>Specs linked:</strong> <code>03-runtime/02-agent-runtime.md</code> §5.1, <code>08-meta/decisions-log.md</code> D606</li><li><strong>Acceptance:</strong> C (chat and stream), F (persistence), Quality</li><li><strong>Status:</strong> Unit-covered (<code>context-calibration.test.ts</code>, including the CJK and boundary cases); desktop E2E pending</li></ul><h3 id="e2e-runtime-unique-tool-call-ids-per-request" tabindex="-1">E2E-RUNTIME-unique-tool-call-ids-per-request <a class="header-anchor" href="#e2e-runtime-unique-tool-call-ids-per-request" aria-label="Permalink to &quot;E2E-RUNTIME-unique-tool-call-ids-per-request&quot;">​</a></h3><ul><li><p><strong>Preconditions:</strong> A session transcript that carries the same tool call twice (a retried append leaves the call under a second row id), loaded into a recreated runtime against a deterministic provider fixture. No real provider credentials.</p></li><li><p><strong>Steps:</strong> Send one prompt so the runtime assembles and issues a request. Read the outgoing request the fixture received, and the <code>agent</code> log channel. Repeat with a transcript whose calls are already unique.</p></li><li><p><strong>Expected:</strong> The outgoing request carries exactly one <code>toolCall</code> for that id and exactly one matching result, so no provider can answer <code>tool_use ids must be unique</code>. The log channel gets one line naming the session and the ids. The unique transcript&#39;s request is unchanged, byte for byte, and logs nothing.</p></li><li><p><strong>Specs:</strong> <code>03-runtime/02-agent-runtime.md</code> §5, <code>08-meta/decisions-log.md</code> D608.</p></li><li><p><strong>Acceptance:</strong> C (conversation and stream), Quality.</p></li><li><p><strong>Milestone:</strong> Post-MVP regression coverage.</p></li><li><p><strong>Automation:</strong> <code>packages/agent-runtime/src/runtime.test.ts</code> covers both halves against a real runtime built from a duplicated history (drop + one log line) and a unique one (identity, no log line).</p></li><li><p><strong>Status:</strong> Unit-covered; no end-to-end driver issues a real provider request against a duplicated transcript.</p></li></ul><h3 id="e2e-runtime-loop-context-ownership" tabindex="-1">E2E-RUNTIME-loop-context-ownership <a class="header-anchor" href="#e2e-runtime-loop-context-ownership" aria-label="Permalink to &quot;E2E-RUNTIME-loop-context-ownership&quot;">​</a></h3><ul><li><strong>Preconditions:</strong> A deterministic provider fixture that answers the first two rounds with one tool call each and stops the turn after the second round the way a user Stop does. No real provider credentials.</li><li><strong>Steps:</strong> Send one prompt so the runtime runs both tool rounds and ends the turn on a tool round. Send a second prompt. Read the state the runtime kept and the outgoing request the fixture received.</li><li><strong>Expected:</strong> The kept state holds each streamed assistant message and each tool result exactly once; no result is separated from the call it answers; the fixture&#39;s request carries exactly one result per call. No duplicate-drop line is logged, because the request guard had nothing to drop.</li><li><strong>Specs:</strong> <code>03-runtime/02-agent-runtime.md</code> §5c, <code>08-meta/decisions-log.md</code> D620.</li><li><strong>Acceptance:</strong> C (conversation and stream), Quality.</li><li><strong>Milestone:</strong> Post-MVP regression coverage.</li><li><strong>Automation:</strong> <code>packages/agent-runtime/src/runtime.test.ts</code> (loop context ownership) drives the real pi loop through <code>runtime.prompt()</code> and reads the view <code>convertToLlm</code> handed the provider; <code>subagent-loop-context.test.ts</code> covers the delegate&#39;s turn boundary. The adapter&#39;s own output is out of reach of both, so <code>tool-call-dedupe.test.ts</code> (request wire contract) drives pi-ai&#39;s message transform directly to pin the two outputs one call id receives when a request separates a call from its result.</li><li><strong>Status:</strong> Unit-covered; no end-to-end driver issues a real provider request from a context the loop appended to.</li></ul><h3 id="e2e-mcp-http-ack-—-http-acknowledgement-and-authorization-status" tabindex="-1">E2E-MCP-HTTP-ACK — HTTP acknowledgement and authorization status <a class="header-anchor" href="#e2e-mcp-http-ack-—-http-acknowledgement-and-authorization-status" aria-label="Permalink to &quot;E2E-MCP-HTTP-ACK — HTTP acknowledgement and authorization status&quot;">​</a></h3><ul><li><strong>Preconditions</strong>: A local mock Streamable HTTP server returns JSON for initialize, <code>202</code> with plain-text <code>Accepted</code> for notifications/initialized, SSE for tools/list, and JSON for tools/call. No provider credentials needed.</li><li><strong>Steps</strong>: Connect, discover a tool, and call it. Render the MCP settings row with idle, connecting, ready, non-authentication failure, and authentication failure statuses, including an expired stored OAuth credential.</li><li><strong>Expected</strong>: The acknowledgement does not enter the JSON parser; discovery, calls, and session headers remain functional. Only explicit authRequired status shows the authorization-required badge. Non-authentication failures retain their original error. Authentication failures remain actionable.</li><li><strong>Specs</strong>: 07-plugins/01-plugin-system §12.2; ADR 0038; ADR 0283.</li><li><strong>Acceptance</strong>: HTTP client integration and settings component rendering.</li><li><strong>Milestone</strong>: Maintenance.</li><li><strong>Status</strong>: Covered by the existing HTTP client integration fixture and a focused component-render validation; no live IDA process required.</li></ul><h3 id="e2e-image-generation-and-editing" tabindex="-1">E2E-IMAGE-generation-and-editing <a class="header-anchor" href="#e2e-image-generation-and-editing" aria-label="Permalink to &quot;E2E-IMAGE-generation-and-editing&quot;">​</a></h3><ul><li><strong>Preconditions:</strong> Built task candidate containing latest origin/main; isolated host data directory, local image HTTP fixture, no production credentials.</li><li><strong>Steps:</strong> Choose a model in Advanced, cancel and verify no change; save and replace it through another provider&#39;s Advanced settings. Clear the binding via the test settings API to verify recovery. Generate same-prompt variants and distinct images, edit a generated image, inspect partial failures, then restart the host/session. Attempt the tool in a durable Plan session and verify no HTTP request occurs.</li><li><strong>Expected:</strong> One image binding persists without changing the chat default; generated files, edit sources and transcript references survive restart. Images render in chat; unconfigured errors navigate to Models settings.</li><li><strong>Specs:</strong> 03-runtime/21-image-generation; 03-runtime/13-model-catalog-and-selection.</li><li><strong>Acceptance:</strong> Configured image generation/editing, safe cancellation and persistence.</li><li><strong>Milestone:</strong> Post-MVP.</li><li><strong>Status:</strong> Automated by <code>scripts/e2e-image-generation.mjs</code> and <code>scripts/e2e-image-generation-ui.mjs</code>; backend test uses real Rust/stdio/HTTP/files, UI test uses real Chromium and production components with API-boundary fixtures.</li></ul><table tabindex="0"><thead><tr><th>Scenario</th><th>Acceptance</th><th>Specification</th><th>Automation</th></tr></thead><tbody><tr><td>E2E-IMAGE-generation-and-editing</td><td>Image capability and recovery</td><td>03-runtime/21-image-generation</td><td>Host and UI suites above</td></tr></tbody></table><h4 id="e2e-chat-parenthesized-url-complete-urls-in-user-messages" tabindex="-1">E2E-CHAT-parenthesized-url: Complete URLs in user messages <a class="header-anchor" href="#e2e-chat-parenthesized-url-complete-urls-in-user-messages" aria-label="Permalink to &quot;E2E-CHAT-parenthesized-url: Complete URLs in user messages&quot;">​</a></h4><ul><li><strong>Steps</strong>: Send <code>https://en.wikipedia.org/wiki/React_(software)</code> in a user message and click its link. Repeat with the URL wrapped in prose parentheses, followed by a sentence-ending period, and followed immediately by another link or file reference.</li><li><strong>Expected</strong>: The complete URL, including <code>(software)</code>, opens the React software article. The outer prose closing parenthesis and sentence-ending punctuation after the URL parenthesis are not part of the link. Adjacent references remain independently clickable. Nested parentheses, query/fragment parentheses and percent-encoded parentheses remain intact.</li><li><strong>Coverage</strong>: <code>chat-links.test.mjs</code>; native desktop click-through with the normal browser destination.</li><li><strong>Specs linked</strong>: <code>04-ux/08-component-spec.md</code> §8.3.</li></ul><h4 id="e2e-ime-escape-composition-cancellation-preserves-drafts" tabindex="-1">E2E-IME-escape: Composition cancellation preserves drafts <a class="header-anchor" href="#e2e-ime-escape-composition-cancellation-preserves-drafts" aria-label="Permalink to &quot;E2E-IME-escape: Composition cancellation preserves drafts&quot;">​</a></h4><ul><li><strong>Steps</strong>: Edit a user message; type a draft; dispatch composing Escape and Cmd/Ctrl+Enter. Open global search and dispatch composing Escape from its input. Repeat with legacy keyCode 229, then ordinary Escape and retry shortcuts.</li><li><strong>Expected</strong>: Composition events neither discard the edit nor close search nor submit a retry. Ordinary Escape still cancels/closes, including search focus outside its input; ordinary Cmd/Ctrl+Enter still retries.</li><li><strong>Coverage</strong>: <code>node scripts/e2e-ime-escape.mjs</code> exercises production components with bubbling DOM keyboard events and host-boundary fixtures. This does not verify an operating system IME candidate window.</li></ul><h3 id="e2e-scheduled-manual-to-hourly" tabindex="-1">E2E-SCHEDULED-manual-to-hourly <a class="header-anchor" href="#e2e-scheduled-manual-to-hourly" aria-label="Permalink to &quot;E2E-SCHEDULED-manual-to-hourly&quot;">​</a></h3><ul><li><strong>Preconditions:</strong> Built host candidate, isolated data directory, no provider.</li><li><strong>Steps:</strong> Through <code>tools.execute</code>, create a paused Manual task, update only its id and cadence to Hourly, rename it, restart, and list tasks again.</li><li><strong>Expected:</strong> Update succeeds without calendar fields; the next occurrence is one hour away. Prompt, paused state and saved configuration survive. Rename does not reset the interval. RPC tests also cover required Daily/Weekly times and retention of an existing custom schedule.</li><li><strong>Specs:</strong> 04-ux/01-ui-ia §3.4.</li><li><strong>Acceptance:</strong> C / F — task configuration and persistence.</li><li><strong>Milestone:</strong> Maintenance.</li><li><strong>Status:</strong> <code>node scripts/e2e-scheduled-hourly-update.mjs</code> exercises the real Rust host, permission path, stdio and SQLite; no model inference or desktop UI.</li></ul><h3 id="e2e-scheduled-manual-workspace-binding" tabindex="-1">E2E-SCHEDULED-manual-workspace-binding <a class="header-anchor" href="#e2e-scheduled-manual-workspace-binding" aria-label="Permalink to &quot;E2E-SCHEDULED-manual-workspace-binding&quot;">​</a></h3><ul><li><strong>Preconditions:</strong> Built request candidate; isolated host data and two project directories. No provider credentials or paid API.</li><li><strong>Steps:</strong> Create Manual and Hourly tasks in project A and without a project; restart the host, switch to B, Run now, rename through the editor payload, and Run now again.</li><li><strong>Expected:</strong> Each result session and edited task retain the original binding, including no-project tasks. Legacy cadence-only tasks keep their previous fallback until explicitly configured (covered by host RPC tests).</li><li><strong>Specs:</strong> 04-ux/01-ui-ia §3.4.</li><li><strong>Acceptance:</strong> Saved workspace binding across run, edit and restart.</li><li><strong>Milestone:</strong> Maintenance.</li><li><strong>Status:</strong> Automated by <code>node --experimental-strip-types scripts/e2e-scheduled-workspace.mjs</code>, using production Electron dispatch and real Rust/stdio/SQLite. Only external inference is replaced with an observer.</li></ul><h3 id="e2e-session-temporary-attachment-fork-preview-and-independent-branch-inputs" tabindex="-1">E2E-SESSION-temporary-attachment-fork: Preview and independent branch inputs <a class="header-anchor" href="#e2e-session-temporary-attachment-fork-preview-and-independent-branch-inputs" aria-label="Permalink to &quot;E2E-SESSION-temporary-attachment-fork: Preview and independent branch inputs&quot;">​</a></h3><ul><li><strong>Steps</strong>: In a task without a project, paste text above the long-paste threshold, send it, and click its transcript attachment. Return from the preview, create a branch, and open the same attachment in the child. Delete the source task and reopen the child attachment; branch the child again.</li><li><strong>Expected</strong>: Every preview shows the original saved bytes. Each branch path belongs to that branch&#39;s scratch input directory. No project is required, and no access to another task&#39;s scratch directory is granted. A bounded fork excludes later-only and unreferenced inputs.</li><li><strong>Automation</strong>: <code>scripts/e2e-composer-paste.mjs</code> covers long-text save, real Electron file read, temporary-task preview, and back navigation. Host tests <code>fork_preserves_referenced_pasted_files_independently</code> and <code>sessions::fork_files</code> cover ownership, deletion, repeated/bounded forks, retained checkpoint paths, expired inputs, rollback, and symlink rejection.</li></ul><h3 id="e2e-scheduled-dispatch" tabindex="-1">E2E-SCHEDULED-dispatch <a class="header-anchor" href="#e2e-scheduled-dispatch" aria-label="Permalink to &quot;E2E-SCHEDULED-dispatch&quot;">​</a></h3><ul><li><strong>Scenario:</strong> Independent task dispatch.</li><li><strong>Expected:</strong> The Electron runner admits independent due tasks without awaiting another task&#39;s prompt setup. Local in-flight ownership is keyed by task ID and Host instance until setup settles; Host remains authoritative for enabled, due and overlap checks. A replaced Host&#39;s completion cannot clear its successor&#39;s local ownership. Stop prevents new polls; admitted work keeps the existing execution/failure lifecycle. Failures remain observable and the 90-second late policy is unchanged.</li><li><strong>Automation:</strong> <code>node --experimental-strip-types scripts/e2e-scheduled-dispatch.mjs</code> uses an isolated real Host and SQLite profile. Inference is not sent to a live provider.</li></ul><h3 id="e2e-scheduled-project-removal" tabindex="-1">E2E-SCHEDULED-project-removal <a class="header-anchor" href="#e2e-scheduled-project-removal" aria-label="Permalink to &quot;E2E-SCHEDULED-project-removal&quot;">​</a></h3><ul><li><strong>Scenario:</strong> Project removal and automations.</li><li><strong>Expected:</strong> Removing a project pauses its bound scheduled tasks without deleting their definitions, schedule, workspace binding or run history. Session references in history may become null when the project&#39;s conversations are removed. Already admitted task runs block removal even before a conversation turn starts. Tasks belonging to other projects and unbound legacy tasks are unaffected. Explicit resume or Run now may recreate a project from the preserved path; automatic polling cannot do so while paused.</li><li><strong>Automation:</strong> <code>node --experimental-strip-types scripts/e2e-scheduled-project-removal.mjs</code> uses an isolated real Host and SQLite profile. Inference is not sent to a live provider.</li></ul><h3 id="e2e-scheduled-legacy-pause" tabindex="-1">E2E-SCHEDULED-legacy-pause <a class="header-anchor" href="#e2e-scheduled-legacy-pause" aria-label="Permalink to &quot;E2E-SCHEDULED-legacy-pause&quot;">​</a></h3><ul><li><strong>Scenario:</strong> Legacy task maintenance.</li><li><strong>Expected:</strong> Agent tools allow title, prompt and pause updates on legacy automatic tasks without a schedule, including an echoed unchanged cadence. These edits do not arm the task or capture the foreground workspace. Explicit enabling, a cadence change or a supplied schedule still follows schedule validation. Resume requires an explicit valid schedule; Manual-to-Hourly retains its existing default interval behavior.</li><li><strong>Automation:</strong> <code>node --experimental-strip-types scripts/e2e-scheduled-legacy-maintenance.mjs</code> uses an isolated real Host and SQLite profile. Inference is not sent to a live provider.</li></ul><h3 id="e2e-scheduled-calendar-intent" tabindex="-1">E2E-SCHEDULED-calendar-intent <a class="header-anchor" href="#e2e-scheduled-calendar-intent" aria-label="Permalink to &quot;E2E-SCHEDULED-calendar-intent&quot;">​</a></h3><ul><li><strong>Scenario:</strong> Calendar intent.</li><li><strong>Expected:</strong> The optional config_json.calendarConfigured boolean distinguishes an explicitly configured Daily/Weekly calendar from Hourly&#39;s internal schedule placeholder. Without the key, legacy Daily/Weekly schedules are treated as configured; legacy Hourly schedules retain their values but require an explicit schedule when converting to Daily/Weekly. Known calendar intent survives Hourly and restart, including midnight. Clearing or replacing the calendar with a different non-calendar placeholder clears intent. This additive extension needs no table/schema migration; older versions ignore it and cannot enforce the new conversion guard. Metadata-only edits and Manual-to-Hourly remain unchanged.</li><li><strong>Automation:</strong> <code>node --experimental-strip-types scripts/e2e-scheduled-calendar-intent.mjs</code> uses an isolated real Host and SQLite profile. Inference is not sent to a live provider.</li></ul><h3 id="e2e-scheduled-paths" tabindex="-1">E2E-SCHEDULED-paths <a class="header-anchor" href="#e2e-scheduled-paths" aria-label="Permalink to &quot;E2E-SCHEDULED-paths&quot;">​</a></h3><ul><li><strong>Scenario:</strong> Workspace identity.</li><li><strong>Expected:</strong> Stored workspace bindings use the existing project canonicalization contract on both write and read. On Windows, slash direction, case, trailing separators and extended path prefixes do not hide a task from its own project&#39;s conversation. The distinction between missing legacy bindings and explicit null remains unchanged. Foreign-project tools cannot list or mutate bound tasks.</li><li><strong>Automation:</strong> <code>node --experimental-strip-types scripts/e2e-scheduled-paths.mjs</code> uses an isolated real Host and SQLite profile. Inference is not sent to a live provider.</li></ul><h2 id="hosted-search-continuation-contract-offline-sidecar" tabindex="-1">Hosted-search continuation contract (offline sidecar) <a class="header-anchor" href="#hosted-search-continuation-contract-offline-sidecar" aria-label="Permalink to &quot;Hosted-search continuation contract (offline sidecar)&quot;">​</a></h2><p><strong>Scope:</strong> ADR 0297; provider-hosted search content, estimation, local tools, Task delegation, and saved-history recovery. No real model or search service.</p><p><strong>Entry:</strong> <code>pnpm test:e2e:hosted-search</code>, which rebuilds the shared package and <code>@pi-desktop/agent-runtime</code> sidecar bundle. The test starts that bundle over its ordinary stdio RPC interface, with an isolated data root, a synthetic Host and a loopback-only Responses endpoint. Source-only mocks or an already installed desktop application do not satisfy this test.</p><table tabindex="0"><thead><tr><th>Case</th><th>Required observation</th></tr></thead><tbody><tr><td>Search then new user prompt</td><td>Valid search item is replayed and the second turn completes.</td></tr><tr><td>Search then ordinary local tool</td><td>A real tool round completes and the next provider request is prepared without type/estimation failure.</td></tr><tr><td>Search then Task</td><td>Actual delegation orchestration runs against a synthetic provider; parent continuation finishes without repeated local errors.</td></tr><tr><td>Saved search history restored</td><td>A fresh runtime preserves search replay without adding fake tool fields or rewriting the original record.</td></tr><tr><td>Usage invalidated by a real prefix change</td><td>The isolated sidecar changes instructions during a real Read round; the prior nonzero usage no longer covers that prefix, and continuation succeeds.</td></tr><tr><td>Invalid saved replay container</td><td>Restore returns safe INTERNAL/context-validation, sends no provider request or persistence Host RPC, and keeps the process healthy. In-memory immutability is checked by the replay unit tests.</td></tr><tr><td>Unknown saved search phase</td><td>The same explicit rejection is observed without silently discarding the search record.</td></tr></tbody></table><p><strong>Companion gates:</strong> Responses/Azure/Anthropic adapter contracts; typed search phases; zero/valid/stale usage; changed/unchanged system prefix and tool ledger; search content growth and CJK output budgets; structured local preparation failures without retry/fetch, ordinary transport retry and cancellation. Dependency upgrades must run these gates against the locked dependency version and record the tested bundle.</p><p><strong>Evidence:</strong> record build and test exit codes, baseline SHA, dependency versions, artifact identity and independent review in <code>docs/project/hosted-search-contract-verification.md</code>. Never record private conversation content or credentials. A skipped test remains NOT RUN, not PASS.</p><h3 id="e2e-hooks-cancel-and-dispose" tabindex="-1">E2E-HOOKS-cancel-and-dispose <a class="header-anchor" href="#e2e-hooks-cancel-and-dispose" aria-label="Permalink to &quot;E2E-HOOKS-cancel-and-dispose&quot;">​</a></h3><ul><li><p><strong>Issue #816 extension:</strong> Cancel a command while its prompt is visible and verify the real renderer removes the dialog, no later prompts or exec occur, and the next command/turn succeeds. The trusted-extension Electron driver checks the DOM via CDP. Runner tests cover long commands, stopped context waits, tool cancellation and late updates, detached header payloads and recovery; process tests use a real parent/child tree with readiness synchronization.</p></li><li><p><strong>Preconditions:</strong> An isolated Desktop profile, deterministic local provider, and a trusted plugin with a waiting preflight handler.</p></li><li><p><strong>Steps:</strong> Start a prompt; stop while the handler waits; release the old handler; send another prompt. Repeat with runtime disposal. Load another fixture with stalled startup/shutdown handlers and a deferred event.</p></li><li><p><strong>Expected:</strong> No provider request starts for the stopped/disposed prompt; the next prompt completes. Late results cannot restart work. Shutdown runs once, waits time out per handler, and deferred registrations are diagnosed without disabling supported handlers.</p></li><li><p><strong>Specs:</strong> 07-plugins/16 §6.</p></li><li><p><strong>Acceptance:</strong> Responsive cancellation and bounded extension lifecycle waits.</p></li><li><p><strong>Milestone:</strong> Hooks P0.</p></li><li><p><strong>Status:</strong> Runner and real Runtime/local-HTTP integration automated in <code>extensions/runner.test.ts</code> and <code>extensions/runtime-lifecycle.test.ts</code>. The trusted-extension Electron driver exercises the Stop path through the real renderer via CDP and records the visible and retired dialog states.</p></li></ul><h2 id="composer-command-source-manual-compaction-and-empty-transcript-reads-795" tabindex="-1">Composer command source, manual compaction, and empty transcript reads (#795) <a class="header-anchor" href="#composer-command-source-manual-compaction-and-empty-transcript-reads-795" aria-label="Permalink to &quot;Composer command source, manual compaction, and empty transcript reads (#795)&quot;">​</a></h2><p><strong>Scope:</strong> the composer&#39;s slash dispatch, the manual compaction RPC, and the renderer&#39;s durable transcript reads. No real model or provider is contacted.</p><table tabindex="0"><thead><tr><th>Scenario</th><th>Expected</th></tr></thead><tbody><tr><td><code>/compact</code> typed while <code>composer/commands</code> fails</td><td>The submission is refused with <code>chat.slashCommandSourceUnavailable</code>, the draft survives, and no prompt reaches the session. The failed read is not cached, so the next submit retries it.</td></tr><tr><td>A warm command source</td><td>A builtin still dispatches locally, while a template and an unknown alias still travel as prompt text.</td></tr><tr><td>A manual compaction that outlives its transport deadline</td><td>The host re-reads the durable compaction record and reports success when a new checkpoint landed, logs the mismatch, and rethrows the timeout when none did. A verdict the sidecar reported itself is never reconciled.</td></tr><tr><td>A transcript window that reads empty for a session with history</td><td>The selection re-reads once, keeps the snapshot the user already has, and otherwise reports <code>chat.sessionTranscriptEmpty</code>; the empty page is never cached, so hover prefetch cannot re-serve it.</td></tr></tbody></table><p><strong>Automation:</strong> <code>node --test apps/desktop/test/slash-command-source.test.mjs</code>, <code>node --test apps/desktop/test/session-transcript-empty-read.test.mjs</code>, <code>node --test apps/desktop/test/plugin-timeout-budgets.test.mjs</code>, <code>pnpm --filter @pi-desktop/shared test</code>, and <code>pnpm --filter @pi-desktop/host-runtime test</code>.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("spec/06-delivery/04-e2e-test-plan.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _04E2eTestPlan = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _04E2eTestPlan as default
};
