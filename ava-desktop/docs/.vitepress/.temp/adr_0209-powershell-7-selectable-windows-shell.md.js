import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0209: PowerShell 7 as a selectable Windows command shell","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0209-powershell-7-selectable-windows-shell.md","filePath":"adr/0209-powershell-7-selectable-windows-shell.md","lastUpdated":1789024894000}');
const _sfc_main = { name: "adr/0209-powershell-7-selectable-windows-shell.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0209-powershell-7-as-a-selectable-windows-command-shell" tabindex="-1">ADR 0209: PowerShell 7 as a selectable Windows command shell <a class="header-anchor" href="#adr-0209-powershell-7-as-a-selectable-windows-command-shell" aria-label="Permalink to &quot;ADR 0209: PowerShell 7 as a selectable Windows command shell&quot;">​</a></h1><ul><li>Status: Accepted (issue #151, merged in #191)</li><li>Date: 2026-09-10</li><li>Baseline: <code>0.14.6</code></li><li>Protocol: v11 (unchanged; additive catalog ID)</li><li>Storage schema: v14 (unchanged; no migration)</li><li>Decision: D381</li><li>Amends: ADR 0054 §1 (Windows catalog table and Windows catalog list)</li><li>Related: <a href="./../spec/03-runtime/03-tools-and-permissions">03-runtime/03-tools-and-permissions.md</a>, <a href="./../spec/03-runtime/01-ipc-protocol">03-runtime/01-ipc-protocol.md</a>, <a href="./../spec/03-runtime/06-host-rpc-protocol">03-runtime/06-host-rpc-protocol.md</a>, <a href="./../spec/04-ux/06-settings-ia">04-ux/06-settings-ia.md</a>, <a href="./../spec/05-security/01-security">05-security/01-security.md</a>, <a href="./../spec/06-delivery/04-e2e-test-plan">06-delivery/04-e2e-test-plan.md</a> (E2E-112)</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>ADR 0054 froze a Windows catalog of <code>windows-powershell</code>, <code>cmd</code>, and <code>git-bash</code>. The <code>windows-powershell</code> entry resolves the in-box Windows PowerShell 5.1 (<code>%SystemRoot%\\System32\\WindowsPowerShell\\v1.0\\powershell.exe</code>).</p><p>PowerShell 7 (<code>pwsh.exe</code>) is a separate product that installs side by side with 5.1 and never replaces it. A user who wants PowerShell 7 therefore had no way to select it: the catalog exposed no entry, and <code>Bash</code> kept launching 5.1 even after the user installed 7. Issue #151 reports exactly that, and the codebase confirms the gap — <code>crates/host-core/src/tools/shell.rs</code> contains no <code>pwsh.exe</code> resolution path, and <code>packages/shared/src/command-shells.ts</code> lists four stable IDs only.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><h3 id="_1-a-fifth-stable-id-windows-pwsh" tabindex="-1">1. A fifth stable ID: <code>windows-pwsh</code> <a class="header-anchor" href="#_1-a-fifth-stable-id-windows-pwsh" aria-label="Permalink to &quot;1. A fifth stable ID: \`windows-pwsh\`&quot;">​</a></h3><p>The Windows catalog gains <code>windows-pwsh</code> (<code>PowerShell 7</code>), selectable from Settings and persisted in <code>defaultCommandShell</code> like every other entry. The ID is stable and platform-scoped, and joins the <code>CommandShellId</code> protocol union.</p><h3 id="_2-resolution-order-and-failure-guidance" tabindex="-1">2. Resolution order and failure guidance <a class="header-anchor" href="#_2-resolution-order-and-failure-guidance" aria-label="Permalink to &quot;2. Resolution order and failure guidance&quot;">​</a></h3><p><code>windows-pwsh</code> resolves, in order:</p><ol><li><code>%ProgramFiles%\\PowerShell\\7\\pwsh.exe</code>, which is where a machine-scope MSI or <code>winget --scope machine</code> install lands. <code>%ProgramW6432%</code> is probed as well, so a 32-bit host process on 64-bit Windows — where <code>ProgramFiles</code> points at the x86 tree — still finds a real 64-bit install.</li><li><code>pwsh.exe</code> on PATH, which covers Store, user-scope, and portable installs.</li></ol><p>Neither location is discoverable by guessing, so a miss returns <code>SHELL_NOT_FOUND</code> with guidance that names both searched locations and points at installing PowerShell 7. The wording lives in <code>PWSH_MISSING_GUIDANCE</code> next to the resolver.</p><h3 id="_3-one-shared-invocation-contract" tabindex="-1">3. One shared invocation contract <a class="header-anchor" href="#_3-one-shared-invocation-contract" aria-label="Permalink to &quot;3. One shared invocation contract&quot;">​</a></h3><p>PowerShell 7 accepts the same non-interactive flags as 5.1 (<code>-NoLogo -NoProfile -NonInteractive -InputFormat Text -OutputFormat Text -Command</code>), so both IDs keep the <code>powershell</code> dialect and share the existing pinned script, including the UTF-8 output setup and <code>$LASTEXITCODE</code> propagation. The catalog ID stays distinct so the pinned turn identity, the folded settings write path, and <code>COMMAND_SHELL_CHANGED</code> still compare exactly.</p><h3 id="_4-the-platform-default-does-not-change" tabindex="-1">4. The platform default does not change <a class="header-anchor" href="#_4-the-platform-default-does-not-change" aria-label="Permalink to &quot;4. The platform default does not change&quot;">​</a></h3><p><code>windows-powershell</code> remains the default for a fresh Windows install, keeps <code>is_default</code>, and stays the value <code>defaultCommandShell</code> falls back to when unset. PowerShell 7 is never chosen for a user who did not ask for it: selecting it is an explicit Settings action.</p><p>Catalog order still decides the first-available fallback (ADR 0054 §1), and <code>windows-pwsh</code> sits directly after the in-box entry, so a persisted <code>windows-powershell</code> that later becomes unavailable now resolves to <code>windows-pwsh</code> before <code>cmd</code>. This follows the existing fallback rule instead of adding a new one, and it cannot change the command language — both entries share the <code>powershell</code> dialect. On Windows the in-box 5.1 entry is effectively always present, so the reordering is not reachable for a normal install; the catalog test covers both steps of the order so the behavior is pinned either way.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Windows users can run agent commands under PowerShell 7 while keeping 5.1 available as a separate, still-default choice.</li><li>The protocol gains one ID, so the folded settings validation, the catalog E2E lane (E2E-112), <code>scripts/e2e-plan.mjs</code>, and the protocol type unions move with it. No new RPC method, no new tool name, and no storage migration are needed: an unknown ID is rejected exactly as before, and a persisted unavailable <code>windows-pwsh</code> recovers through the first-available fallback.</li><li>One existing behavior moves: a persisted <code>windows-powershell</code> that becomes unavailable now falls back to <code>windows-pwsh</code> instead of <code>cmd</code>, because the new entry sits earlier in catalog order. The dialect is unchanged, and the case is unreachable on a normal Windows install, but the existing catalog test is updated to pin both steps of the order rather than only its first result.</li><li>A machine without PowerShell 7 keeps the entry visible and unavailable, the same presentation <code>git-bash</code> already uses.</li></ul><h2 id="alternatives-rejected" tabindex="-1">Alternatives rejected <a class="header-anchor" href="#alternatives-rejected" aria-label="Permalink to &quot;Alternatives rejected&quot;">​</a></h2><h3 id="make-windows-powershell-prefer-pwsh-exe-when-present" tabindex="-1">Make <code>windows-powershell</code> prefer <code>pwsh.exe</code> when present <a class="header-anchor" href="#make-windows-powershell-prefer-pwsh-exe-when-present" aria-label="Permalink to &quot;Make \`windows-powershell\` prefer \`pwsh.exe\` when present&quot;">​</a></h3><p>Rejected: it silently changes the shell an existing user already runs, and it makes the pinned turn identity ambiguous between two different executables that share a dialect. It also cannot express &quot;use 5.1&quot; once 7 is installed, which is the choice ADR 0054 §1 deliberately granted by freezing the catalog.</p><h3 id="let-the-user-configure-an-arbitrary-pwsh-exe-path" tabindex="-1">Let the user configure an arbitrary <code>pwsh.exe</code> path <a class="header-anchor" href="#let-the-user-configure-an-arbitrary-pwsh-exe-path" aria-label="Permalink to &quot;Let the user configure an arbitrary \`pwsh.exe\` path&quot;">​</a></h3><p>Rejected: ADR 0054 already rejected renderer- or sidecar-supplied executable paths, because catalog policy and identity validation depend on a closed ID set. A <code>PI_DESKTOP_*</code> env override could be added later for unusual installs, but it is not needed for the install layouts above and is deliberately left out of this change.</p><h3 id="add-a-powershell7-tool-name" tabindex="-1">Add a <code>PowerShell7</code> tool name <a class="header-anchor" href="#add-a-powershell7-tool-name" aria-label="Permalink to &quot;Add a \`PowerShell7\` tool name&quot;">​</a></h3><p>Rejected: ADR 0054 §3 keeps <code>Bash</code> as the tool and protocol name and treats shell choice as request data. A second tool name would multiply permission and audit surfaces without adding authority.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0209-powershell-7-selectable-windows-shell.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0209Powershell7SelectableWindowsShell = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0209Powershell7SelectableWindowsShell as default
};
