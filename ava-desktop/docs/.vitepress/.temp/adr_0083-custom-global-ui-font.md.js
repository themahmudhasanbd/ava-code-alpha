import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0083: Custom global UI font","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0083-custom-global-ui-font.md","filePath":"adr/0083-custom-global-ui-font.md","lastUpdated":1789839347000}');
const _sfc_main = { name: "adr/0083-custom-global-ui-font.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0083-custom-global-ui-font" tabindex="-1">ADR 0083: Custom global UI font <a class="header-anchor" href="#adr-0083-custom-global-ui-font" aria-label="Permalink to &quot;ADR 0083: Custom global UI font&quot;">​</a></h1><ul><li>Status: Superseded in part by ADR 0298</li><li>Date: 2026-08-14</li><li>Baseline: <code>0.4.16</code></li><li>Protocol: v9 (one additive Electron-main IPC channel; host RPC unchanged)</li><li>Storage schema: v10 (optional <code>AppSettings.fontFamily</code> JSON field)</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The shell typography is a single hardcoded token stack (<code>--font-sans</code>). Users want to choose a global UI font from Settings — including open-licensed fonts that are safe for commercial use — similar to the font picker in the dbx desktop client, which enumerates system fonts and bundles an OFL family.</p><p>The renderer is sandboxed behind the preload IPC allowlist, and the app ships a bundled renderer, so the font source must be local (no CDN/CSS font services, per <a href="./../spec/04-ux/07-ui-design-system">07-ui-design-system §2</a>).</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><h3 id="_1-settings-picker-and-persistence" tabindex="-1">1. Settings picker and persistence <a class="header-anchor" href="#_1-settings-picker-and-persistence" aria-label="Permalink to &quot;1. Settings picker and persistence&quot;">​</a></h3><p>Settings → Basics → Appearance gains a <strong>Font</strong> row with a searchable picker. Selections persist as <code>AppSettings.fontFamily</code>, a CSS <code>font-family</code> stack string. An absent or empty value means the built-in token stack; the picker always offers <strong>System default</strong> first. Selecting System default persists an empty stack (<code>fontFamily: &quot;&quot;</code>) rather than removing the key: <code>settings.set</code> merges supplied fields into stored settings and JSON serialization drops <code>undefined</code>, so an omitted key cannot clear a stored override.</p><p>The picker menu portals to <code>document.body</code> as a fixed body-level floating layer (measured against the trigger and clamped to the viewport), so the settings card&#39;s <code>overflow</code> cannot clip or squeeze it; it follows the body-level floating-layer contract in the component spec.</p><h3 id="_2-bundled-open-licensed-families-superseded-by-adr-0298" tabindex="-1">2. Bundled open-licensed families (superseded by ADR 0298) <a class="header-anchor" href="#_2-bundled-open-licensed-families-superseded-by-adr-0298" aria-label="Permalink to &quot;2. Bundled open-licensed families (superseded by ADR 0298)&quot;">​</a></h3><p><em>(Historical. <a href="./0298-remove-bundled-fonts">ADR 0298</a> removed every bundled family, so the picker offers System default plus installed system families and nothing is loaded from disk.)</em></p><p>Four families shipped with the app as <code>woff2</code>, all under the SIL Open Font License 1.1 (free for commercial use and redistribution; license texts were shipped under <code>apps/desktop/src/assets/fonts/licenses/</code>):</p><table tabindex="0"><thead><tr><th>Family</th><th>Script coverage</th><th>Source</th></tr></thead><tbody><tr><td>Geist</td><td>Latin (variable)</td><td>vercel/geist-font</td></tr><tr><td>Inter</td><td>Latin (variable)</td><td>rsms/inter</td></tr><tr><td>Noto Sans SC</td><td>CJK (variable)</td><td>google/fonts (Source Han Sans lineage)</td></tr><tr><td>LXGW WenKai</td><td>CJK kai (regular)</td><td>lxgw/LxgwWenKai</td></tr></tbody></table><p>Every stack appends a CJK fallback tier so Chinese text stays readable when the selected family has no CJK glyphs. The tier was (<code>Noto Sans SC</code>, <code>PingFang SC</code>, <code>Hiragino Sans GB</code>, <code>Microsoft YaHei</code>, <code>sans-serif</code>) and is now the system-only (<code>PingFang SC</code>, <code>Hiragino Sans GB</code>, <code>Microsoft YaHei</code>, <code>sans-serif</code>) after ADR 0298. The mono stack (<code>--font-mono</code>) is unchanged.</p><h3 id="_3-system-font-enumeration-in-electron-main" tabindex="-1">3. System font enumeration in Electron main <a class="header-anchor" href="#_3-system-font-enumeration-in-electron-main" aria-label="Permalink to &quot;3. System font enumeration in Electron main&quot;">​</a></h3><p>Electron main resolves installed system font families using platform tooling only (no native modules), so the main bundle stays self-contained:</p><ul><li>macOS: <code>osascript</code> JXA bridging <code>CTFontManagerCopyAvailableFontFamilyNames</code> — the same CoreText query <code>font_kit::all_families()</code> uses, returning canonical CSS family names in tens of milliseconds — with <code>system_profiler SPFontsDataType -json</code> kept as a fallback when osascript is unavailable</li><li>Windows: PowerShell <code>[Windows.Media.Fonts]::SystemFontFamilies</code></li><li>Linux: <code>fc-list -f &quot;%{family[0]}\\n&quot;</code></li></ul><p>Results are deduplicated, filtered (hidden <code>.</code>-prefixed families excluded), sorted, and cached for 60 seconds per process. The renderer reaches them through one new allowlisted IPC channel, <code>pi-desktop/app/systemFonts</code>.</p><h3 id="_4-application" tabindex="-1">4. Application <a class="header-anchor" href="#_4-application" aria-label="Permalink to &quot;4. Application&quot;">​</a></h3><p>The renderer overrides <code>--font-sans</code> on <code>document.documentElement</code> from <code>AppSettings.fontFamily</code>; <code>body</code> and every <code>var(--font-sans)</code> consumer pick it up without a reload. <em>(Superseded by <a href="./0298-remove-bundled-fonts">ADR 0298</a>: the <code>@font-face</code> rules, <code>apps/desktop/src/styles/fonts.css</code>, and its <code>@import</code> in <code>globals.css</code> are deleted, so a stored stack resolves through installed families.)</em></p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Users pick a global UI font once; it persists across restarts and renders offline from installed families (<a href="./0298-remove-bundled-fonts">ADR 0298</a> ships no font files).</li><li>CJK coverage stays correct for every option via the appended fallback tier.</li><li>macOS enumeration resolves through the fast CoreText path in tens of milliseconds (the previous <code>system_profiler</code> path took 2–5 s and is now only a fallback); the result is cached 60 s per process and returns canonical family names such as <code>PingFang SC</code> rather than system_profiler&#39;s localized aliases such as <code>苹方-简</code>.</li><li>The installer carried roughly 16 MB of bundled font files; ADR 0298 removed them, so <code>out/renderer</code> emits no application font face and only KaTeX&#39;s <code>woff2</code> glyphs remain (see <code>06-delivery/06-release-runbook.md</code>).</li><li><code>@font-face</code> <code>font-weight</code> descriptors are exempted from the style-token guard because they describe font files, not UI typography.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><ul><li><strong><code>font_kit</code> in host-core</strong> (dbx&#39;s approach): fast native enumeration, but adds a host RPC method and widens the host protocol surface for a renderer-only preference; Electron main reaches the same CoreText family list through an <code>osascript</code> JXA bridge without widening the protocol.</li><li><strong><code>font-list</code> npm package</strong>: clean API, but its internal directory <code>require(&quot;./libs/core&quot;)</code> does not survive electron-vite main bundling, and its macOS helper is a prebuilt binary that would need asar unpacking.</li><li><strong><code>queryLocalFonts()</code> (Local Font Access API)</strong>: permission-gated in the sandboxed renderer and still experimental in Electron.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0083-custom-global-ui-font.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0083CustomGlobalUiFont = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0083CustomGlobalUiFont as default
};
