import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0238: Prioritize MainChat in the three-column shell","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0238-three-column-width-priority.md","filePath":"adr/0238-three-column-width-priority.md","lastUpdated":1789752642000}');
const _sfc_main = { name: "adr/0238-three-column-width-priority.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0238-prioritize-mainchat-in-the-three-column-shell" tabindex="-1">ADR 0238: Prioritize MainChat in the three-column shell <a class="header-anchor" href="#adr-0238-prioritize-mainchat-in-the-three-column-shell" aria-label="Permalink to &quot;ADR 0238: Prioritize MainChat in the three-column shell&quot;">​</a></h1><ul><li>Status: Accepted (sidebar width clamp amended by <a href="./0290-resizable-sidebar-collapse-threshold">ADR 0290</a>)</li><li>Date: 2026-09-13</li><li>Amends: <a href="./0226-reserve-chat-width-for-composer-controls">ADR 0226</a> · <a href="./0151-internal-work-panel-dock">ADR 0151</a> · <a href="./0033-internal-dock-work-panel">ADR 0033</a> — the fixed <code>244..720px</code> clamp from D167/ADR 0033/ADR 0151 is replaced by the live budget below</li><li>Related: <a href="./0033-internal-dock-work-panel">ADR 0033</a> · <a href="./0151-internal-work-panel-dock">ADR 0151</a> · <a href="./../spec/04-ux/01-ui-ia">01-ui-ia</a> · <a href="./../spec/04-ux/07-ui-design-system">07-ui-design-system §10</a> · <a href="./../spec/04-ux/08-component-spec">08-component-spec §1 and §5</a> · <a href="./../spec/04-ux/09-interaction-patterns">09-interaction-patterns §8</a> · E2E-LAYOUT-three-column-width-priority</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The renderer shell has three in-flow columns inside a fixed client area: the expanded sidebar, MainChat and the work panel. ADR 0033 and ADR 0151 keep native window bounds out of the panel&#39;s reach, but the shell still has no explicit width priority. The 515px chat reservation of ADR 0226 and the fixed <code>244px–720px</code> panel range left pointer resizing, keyboard resizing, sidebar toggles and window resizing with different rules, and a narrow window could pin MainChat to its floor while the expanded sidebar kept its full width.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>MainChat has a hard <code>450px</code> minimum, derived from the composer toolbar&#39;s unfolded row (plus button, mode and permission chips, model/thinking chip, enhance and send buttons) plus its margins. The work-panel maximum is the remaining client width after that floor and the expanded sidebar (<code>clientWidth - mainChatMinimum - expandedSidebarWidth</code>); there is no fixed pixel cap, so a wide window keeps spending width on the panel until MainChat reaches its floor. The shared renderer budget function is used by pointer preview, keyboard resize, panel presentation, sidebar changes and shell resize observation.</li><li>When the expanded sidebar would make MainChat reach the 450px floor, the renderer immediately collapses the sidebar through the existing mounted <code>sidebar-out</code> animation. While that animation still occupies flex space, the shared budget continues to count the sidebar so MainChat stays at or above 450px. The preferred work-panel width remains the user&#39;s persisted target, so the panel can continue growing after the sidebar has yielded.</li><li>A manual sidebar reopen spends work-panel width first. It preserves the current MainChat width where possible; if the 450px floor would be crossed, it targets <code>460px</code>. This reopen path may persist a positive compact panel width below the ordinary <code>244px</code> presentation minimum.</li><li>Automatic sidebar collapse is remembered only until the work panel closes. Closing the panel restores a sidebar collapsed by the layout mechanism. Manual sidebar collapse, manual reopen and a subsequent manual collapse clear that record.</li><li>The window itself stays fixed. No panel action requests a positive native reservation: the renderer keeps <code>window/setWorkPanelReservation</code> at zero and Main normalizes every valid request to <code>{ requested: 0, reserved: 0 }</code> without applying panel width or x-offset geometry (ADR 0033 / ADR 0151). The supported window minimum stays <code>1040×700</code>, which already exceeds <code>mainChatMinimum + panelMinimum</code>, so the panel minimum is satisfiable at every supported window size.</li><li>The panel header exposes a preview (maximize) toggle. While preview mode is on, MainChat is not rendered at all and the panel takes the whole client area beside the sidebar (<code>clientWidth - expandedSidebarWidth</code>); the 450px MainChat floor is therefore suspended by design, because there is no chat column to protect. Leaving preview mode restores the previous panel width and keeps whatever sidebar state the user chose last; the mode is transient (never persisted, ends with the panel) and never changes native bounds. Because MainChat is absent in this mode, AppShell supplies a window-level 46px chrome row with New Task, sidebar, and native window controls. Collapsed-sidebar preview reserves the shared <code>--ds-window-lead-inset</code> on the left: 88px in windowed macOS and 8px in fullscreen (D433). Hit-region amendment (2026-09-16): the chrome row and its spacer declare neither drag nor no-drag, and pass pointer events through outside controls. The panel header is the sole drag owner within the preview pane. Its actual border box excludes the left shell-action lane plus an 8px gap in both sidebar states, including expanded-sidebar New Task. All platforms use an 8px left inset except collapsed-sidebar windowed macOS (88px). The existing right native-control border exclusion remains unchanged. Header-height background paint fills the excluded lane without an opaque control overlay. Native pointer clicks and window dragging require native validation; renderer geometry and DOM/CDP event dispatch are not native hit-test proof.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>MainChat cannot be compressed below 450px by any supported shell width change; the sidebar is the column that yields.</li><li>A constrained window shows a narrower work panel during the current layout, while the persisted preferred width remains available when space returns.</li><li>The composer toolbar now handles widths between 450px and its comfortable layout instead of relying on a 515px reservation, so its control groups ellipsize or reflow below that width.</li><li>No host protocol, SQLite schema, plugin contract, security boundary or native window geometry changes.</li></ul><h2 id="alternatives-rejected" tabindex="-1">Alternatives rejected <a class="header-anchor" href="#alternatives-rejected" aria-label="Permalink to &quot;Alternatives rejected&quot;">​</a></h2><h3 id="keep-the-515px-chat-reservation-of-adr-0226" tabindex="-1">Keep the 515px chat reservation of ADR 0226 <a class="header-anchor" href="#keep-the-515px-chat-reservation-of-adr-0226" aria-label="Permalink to &quot;Keep the 515px chat reservation of ADR 0226&quot;">​</a></h3><p>Rejected because it leaves side-dock priority implicit and cannot satisfy the 450px hard floor with automatic sidebar-yield behaviour, which is what keeps MainChat usable in the fixed window.</p><h3 id="let-the-sidebar-resize-continuously-to-preserve-every-column" tabindex="-1">Let the sidebar resize continuously to preserve every column <a class="header-anchor" href="#let-the-sidebar-resize-continuously-to-preserve-every-column" aria-label="Permalink to &quot;Let the sidebar resize continuously to preserve every column&quot;">​</a></h3><p>Rejected because the sidebar remains a discrete expanded/collapsed column for this interaction. Its user-selected preferred width is not silently mutated by window pressure.</p><h3 id="mirror-the-committed-panel-width-into-native-window-bounds" tabindex="-1">Mirror the committed panel width into native window bounds <a class="header-anchor" href="#mirror-the-committed-panel-width-into-native-window-bounds" aria-label="Permalink to &quot;Mirror the committed panel width into native window bounds&quot;">​</a></h3><p>Rejected because expanding the application window is the outward behaviour ADR 0033 and ADR 0151 removed. The window minimum already guarantees <code>mainChatMinimum + panelMinimum</code>, so the budget is always satisfiable without touching native bounds.</p><h2 id="references" tabindex="-1">References <a class="header-anchor" href="#references" aria-label="Permalink to &quot;References&quot;">​</a></h2><ul><li><code>apps/desktop/src/lib/work-panel-resize.ts</code></li><li><code>apps/desktop/src/components/workpanel/WorkPanel.tsx</code></li><li><code>apps/desktop/src/features/app/useAppShellRuntime.tsx</code></li><li><code>apps/desktop/src/features/app/AppShell.tsx</code></li><li><code>apps/desktop/src/styles/chat-shell.css</code></li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0238-three-column-width-priority.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0238ThreeColumnWidthPriority = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0238ThreeColumnWidthPriority as default
};
