import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0146: Assign outer and inner work-panel resize ownership by boundary","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0146-separate-work-panel-and-chat-resize-ownership.md","filePath":"adr/0146-separate-work-panel-and-chat-resize-ownership.md","lastUpdated":1788491820000}');
const _sfc_main = { name: "adr/0146-separate-work-panel-and-chat-resize-ownership.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0146-assign-outer-and-inner-work-panel-resize-ownership-by-boundary" tabindex="-1">ADR 0146: Assign outer and inner work-panel resize ownership by boundary <a class="header-anchor" href="#adr-0146-assign-outer-and-inner-work-panel-resize-ownership-by-boundary" aria-label="Permalink to &quot;ADR 0146: Assign outer and inner work-panel resize ownership by boundary&quot;">​</a></h1><ul><li>Status: Superseded by ADR 0151</li><li>Date: 2026-09-02</li><li>Related: ADR 0122, ADR 0132</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The visible work panel is an in-flow right column backed by a native width reservation. Its renderer divider and the native window&#39;s right edge are both resize affordances, but assigning both gestures to the same dimension makes it impossible to adjust the panel without changing the conversation width, or to adjust the conversation without changing the panel preference.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>When the work panel is open:</p><ul><li>The outer native right edge, including right corners where Electron reports them, owns the panel target. Main keeps the base conversation width fixed, previews the bounded <code>244..720px</code> panel target to the renderer, and commits the target after the native resize stream settles. The native minimum is temporarily lowered to allow the panel target to reach its minimum.</li><li>The inner renderer divider owns the base conversation width. Pointer and keyboard changes use the bounded <code>window/setWorkPanelChatWidth({width})</code> target-state channel (<code>1040..10000px</code>) and preserve the currently active panel reservation. Requests are serialized so a fast pointer stream cannot reorder native bounds updates; when the work area is too tight, the conversation target stops before it would narrow the panel. Cancellation sends the press-time target again.</li><li>Left and non-right native edges retain their existing base-window behavior. Display transitions, maximized/fullscreen deferral, persisted base bounds, and work-area reservation clamping continue to follow ADR 0122 and ADR 0132.</li></ul><p>The event is Electron-local and does not change the host protocol version.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><p>The two resize gestures have an unambiguous dimension owner. The renderer must temporarily block composited Browser and plugin views during either gesture, and the panel separator&#39;s accessible value describes the conversation target, not the panel target. Linux uses the main-process right-edge cursor position as the fallback because Electron&#39;s <code>will-resize</code> edge detail is not available on that platform.</p><h2 id="alternatives-rejected" tabindex="-1">Alternatives rejected <a class="header-anchor" href="#alternatives-rejected" aria-label="Permalink to &quot;Alternatives rejected&quot;">​</a></h2><ul><li>Keep the old renderer divider as the panel resizer and let native edges resize the chat: this does not satisfy the requested boundary ownership.</li><li>Expose an unrestricted BrowserWindow resize IPC: this would weaken the target-state and validation boundary without being needed for either gesture.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0146-separate-work-panel-and-chat-resize-ownership.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0146SeparateWorkPanelAndChatResizeOwnership = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0146SeparateWorkPanelAndChatResizeOwnership as default
};
