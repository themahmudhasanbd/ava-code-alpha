import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0295: Session thinking-parameter omission","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0295-session-thinking-parameter-omission.md","filePath":"adr/0295-session-thinking-parameter-omission.md","lastUpdated":1789824109000}');
const _sfc_main = { name: "adr/0295-session-thinking-parameter-omission.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0295-session-thinking-parameter-omission" tabindex="-1">ADR 0295: Session thinking-parameter omission <a class="header-anchor" href="#adr-0295-session-thinking-parameter-omission" aria-label="Permalink to &quot;ADR 0295: Session thinking-parameter omission&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-20</li><li>Deciders: PI-Desktop runtime and UX maintainers</li><li>Amends: ADR 0194, ADR 0144, ADR 0221</li><li>Related: D456, E2E-203a</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Subagents already offer <code>thinkingLevel: omit</code> (ADR 0194): the agent bookkeeping state stays <code>off</code>, but the request uses the low-level provider stream so no thinking override is synthesized. Sessions only accepted the seven canonical levels. Explicit <code>off</code> is not equivalent to omitting the parameter; adapters often serialize <code>off</code> as <code>reasoning_effort: &quot;none&quot;</code> or <code>thinking: disabled</code>. Users need that third choice on the Composer model × reasoning menu.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>Persist <code>omit</code> as a session <code>thinkingLevel</code> alongside the seven canonical levels. Model-binding <code>thinkingLevels</code> and catalog capability lists remain the seven canonical values; <code>omit</code> is a client selector, not a published capability.</li><li>The Composer reasoning menu prepends <code>omit</code> whenever the selected model exposes at least one enabled canonical level. The chip renders the canonical string <code>omit</code> (ADR 0221). Non-reasoning models keep an <code>off</code>-only menu.</li><li>Runtime clamping preserves <code>omit</code> on a reasoning model and maps it to <code>off</code> otherwise. Agent bookkeeping stays <code>off</code>; the parent stream uses the same low-level omit path as subagents (<code>thinkingLevelMap.off = null</code>).</li><li>Schema v19 rebuilds <code>sessions</code> so the CHECK includes <code>omit</code>. Handshake protocol version is unchanged.</li><li>Settings <code>defaultThinkingLevel</code> accepts <code>omit</code> when the binding enables any canonical reasoning level, so new sessions can start without a thinking override. Capability chips stay canonical; an off-only binding still rejects <code>omit</code>.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Composer can leave the provider adapter&#39;s default thinking behavior in control without disabling thinking.</li><li>Existing sessions keep their stored canonical levels.</li><li>Subagent inherit of a parent <code>omit</code> continues to omit.</li><li>A reasoning model&#39;s Settings default can be <code>omit</code>; new sessions inherit it.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0295-session-thinking-parameter-omission.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0295SessionThinkingParameterOmission = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0295SessionThinkingParameterOmission as default
};
