import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0170: Ship the work-panel browser as a bundled plugin over public CDP","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0170-work-panel-browser-as-bundled-plugin.md","filePath":"adr/0170-work-panel-browser-as-bundled-plugin.md","lastUpdated":1789030011000}');
const _sfc_main = { name: "adr/0170-work-panel-browser-as-bundled-plugin.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0170-ship-the-work-panel-browser-as-a-bundled-plugin-over-public-cdp" tabindex="-1">ADR 0170: Ship the work-panel browser as a bundled plugin over public CDP <a class="header-anchor" href="#adr-0170-ship-the-work-panel-browser-as-a-bundled-plugin-over-public-cdp" aria-label="Permalink to &quot;ADR 0170: Ship the work-panel browser as a bundled plugin over public CDP&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-06</li><li>Deciders: PI-Desktop core</li><li>Related: <a href="./0019-work-panel-subsystems">ADR 0019</a> · <a href="./0104-plugin-contributed-work-panel-views">ADR 0104</a> · <a href="./0105-files-as-a-bundled-plugin">ADR 0105</a> · <a href="./../spec/07-plugins/README">07-plugins</a></li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The work-panel browser was a host-built launcher row (<code>HEADER_TOOLS</code>) plus a Main-owned <code>WebContentsView</code> guest. Files already proved that a first-party surface can ship as a bundled plugin on the public <code>contributes.views</code> channel. Users also need the whole browser — chrome, preview, and agent CDP — to be optional the same way: enable/disable on the Plugins page, never uninstallable.</p><p>The plugin page cannot <em>be</em> the browser. Plugin views are sandboxed, have <code>webviewTag: false</code>, and are confined by <code>net.domains</code>. Arbitrary http(s) and workspace files, plus Chrome DevTools Protocol, have to stay on a host-owned guest.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><strong><code>pi.browser</code> is a bundled plugin</strong> in <code>apps/desktop/resources/plugins/</code>, enabled by default, disableable, not uninstallable. It contributes view <code>browser</code> (icon token <code>browser</code>, order 10) and registers the agent tool <code>Browser</code>.</li><li><strong>The guest <code>WebContentsView</code> (<code>persist:work-browser</code>) and debugger stay host-owned.</strong> Plugin chrome is a normal plugin view. The guest is stacked on a content-relative hole the chrome reports through <code>pi.browser.setBounds</code>.</li><li><strong><code>browser.cdp</code> is a public permission</strong> (high). <code>pi.browser.*</code> is a public host API. Any plugin that declares the permission may call it. Bounds are clamped to the calling plugin view so a guest cannot cover chat/composer.</li><li><strong>Raw CDP is allowlisted and deny-by-default.</strong> Cookie, storage, target, and network-interception methods are refused. No DevTools websocket is exposed.</li><li><strong>Host <code>BrowserPreview</code> remains a thin facade</strong> (Plan/subagent name stability). It errors if <code>pi.browser</code> is disabled; otherwise it loads the workspace file into the guest when that session&#39;s chrome is visible and reveals the plugin view. Plugin CDP stays Agent-only (<code>plugin_*</code>).</li><li><strong>v1 is a singleton guest.</strong> Session locations are remembered and rebound when the originating conversation&#39;s plugin tab is shown (D142). Background sessions do not steal the visible guest.</li></ol><p>This supersedes ADR 0105 clause 4 (Browser remains a host-built launcher) and the host-launcher clause of ADR 0019. Guest ownership and navigation policy are unchanged. ADR 0108&#39;s &quot;Browser as the host-built tool&quot; clause is likewise superseded: the panel&#39;s launchable surfaces are plugin views.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Disabling <code>pi.browser</code> removes the launcher row, agent CDP, and guest. URL chips fall back to <code>openExternal</code>. <code>BrowserPreview</code> fails closed.</li><li>Third-party plugins with <code>browser.cdp</code> share the same guest; last chrome <code>setBounds</code> wins.</li><li>Plan still sees <code>BrowserPreview</code>. The Browser plugin tool is also visible in Plan/Goal for the four <code>planSafeActions</code> (<code>navigate</code>, <code>snapshot</code>, <code>screenshot</code>, <code>console</code>); click/fill/evaluate/cdp stay Agent-only (ADR 0211).</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><h3 id="load-the-web-page-inside-the-plugin-view" tabindex="-1">Load the web page inside the plugin view <a class="header-anchor" href="#load-the-web-page-inside-the-plugin-view" aria-label="Permalink to &quot;Load the web page inside the plugin view&quot;">​</a></h3><p>Rejected: would require <code>webviewTag</code>, a wildcard <code>net.domains</code>, and attaching CDP to the wrong WebContents.</p><h3 id="private-host-apis-only-pi-browser-may-call" tabindex="-1">Private host APIs only <code>pi.browser</code> may call <a class="header-anchor" href="#private-host-apis-only-pi-browser-may-call" aria-label="Permalink to &quot;Private host APIs only \`pi.browser\` may call&quot;">​</a></h3><p>Rejected: the Files bar is that first-party features prove the public channel.</p><h3 id="keep-react-browsertab-chrome-and-only-plugin-ize-the-launcher" tabindex="-1">Keep React <code>BrowserTab</code> chrome and only plugin-ize the launcher <a class="header-anchor" href="#keep-react-browsertab-chrome-and-only-plugin-ize-the-launcher" aria-label="Permalink to &quot;Keep React \`BrowserTab\` chrome and only plugin-ize the launcher&quot;">​</a></h3><p>Rejected: that is not “the whole default browser is a plugin.”</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0170-work-panel-browser-as-bundled-plugin.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0170WorkPanelBrowserAsBundledPlugin = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0170WorkPanelBrowserAsBundledPlugin as default
};
