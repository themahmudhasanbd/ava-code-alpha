import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0154: Reveal the New Task empty destination before host IO","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0154-reveal-new-task-empty-destination.md","filePath":"adr/0154-reveal-new-task-empty-destination.md","lastUpdated":1788557630000}');
const _sfc_main = { name: "adr/0154-reveal-new-task-empty-destination.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0154-reveal-the-new-task-empty-destination-before-host-io" tabindex="-1">ADR 0154: Reveal the New Task empty destination before host IO <a class="header-anchor" href="#adr-0154-reveal-the-new-task-empty-destination-before-host-io" aria-label="Permalink to &quot;ADR 0154: Reveal the New Task empty destination before host IO&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-05</li><li>Deciders: PI-Desktop core</li><li>Related: D252, D305, ADR 0113, ADR 0137, E2E-011a, E2E-011d, E2E-011g</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>ADR 0113 made New Task persist a durable empty session (or reuse the group&#39;s latest empty row). The renderer implemented that by awaiting <code>session.list</code>, then <code>session.create</code>, then another <code>session.list</code>, then <code>session.get</code> before changing the visible transcript. During that chain the previous conversation stayed on screen: a cold-switch rule (ADR 0137) is correct for opening an existing session, but for a brand-new empty destination it felt like the click did nothing, then the chat jumped.</p><p><code>session.list</code> was there so a first message sent moments earlier would not be mistaken for an empty slot while <code>messageCount</code> was still 0. The renderer already knows that case: the session is running, has live rows, or has a submitted composer draft.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><strong>First-frame empty reveal.</strong> Creating a session clears the previous transcript and retained panes synchronously, before <code>session.create</code>. The empty home composer is the destination. Reusing an empty slot commits an empty transcript on the same frame rather than waiting for <code>session.get</code>.</li><li><strong>Reuse without a list refresh.</strong> New Task decides from the in-memory session list plus renderer signals (<code>runningSessions</code>, live/cached rows, submitted drafts). Host <code>messageCount</code> remains the durable empty predicate; the in-memory signals only prevent treating a just-started turn as empty.</li><li><strong>Commit from <code>session.create</code>.</strong> The created summary is inserted into the sidebar list the way a fork child is. There is no follow-up <code>session.list</code> or <code>session.get</code> for a new empty session: its transcript is known to be empty. A superseded navigation still records the row.</li><li><strong>One in-flight create per group.</strong> Send and paste wait for the same-scope New Task promise instead of materializing a second session. Keystrokes typed on the empty home during create move onto the new session id.</li></ol><p>Protocol, storage, and host RPC are unchanged. <code>session.create</code> remains the source of the durable id.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>New Task no longer leaves the previous conversation on screen.</li><li>Rapid New Task clicks and first-message races stay serialized per group.</li><li>An empty reuse still revalidates through <code>selectSession</code> after the empty pane is already visible.</li><li>Spec <code>04-ux/09-interaction-patterns.md</code> §1.6, <code>04-ux/08-component-spec.md</code> §11, E2E-011a / E2E-011d / E2E-011g; D305 amends D252&#39;s refresh-then-select timing, not the reuse rule.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li>Client-generated session ids on <code>session.create</code>: first-frame id without a home-draft interval, but an additive protocol field and remapping if an older host ignores it.</li><li>Keep the previous pane with a progress track: that is the cold-switch rule for existing transcripts and is the hitch this request removes.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0154-reveal-new-task-empty-destination.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0154RevealNewTaskEmptyDestination = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0154RevealNewTaskEmptyDestination as default
};
