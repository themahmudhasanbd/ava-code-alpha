import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0193: Last-request occupancy in the context inspector","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0193-last-request-context-occupancy.md","filePath":"adr/0193-last-request-context-occupancy.md","lastUpdated":1788937388000}');
const _sfc_main = { name: "adr/0193-last-request-context-occupancy.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0193-last-request-occupancy-in-the-context-inspector" tabindex="-1">ADR 0193: Last-request occupancy in the context inspector <a class="header-anchor" href="#adr-0193-last-request-occupancy-in-the-context-inspector" aria-label="Permalink to &quot;ADR 0193: Last-request occupancy in the context inspector&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-09</li><li>Deciders: PI-Desktop renderer and UX maintainers</li><li>Amends: D103, D184, D244, D347, ADR 0047, ADR 0103, ADR 0184</li><li>Related: D355, E2E-060d, US-UI-61</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>The composer inspector mixed two token scopes. Remaining capacity and the used/window counts came from the newest assistant <strong>message</strong>. Turn total, input, output, cache read/write, reasoning, and cache hit rate came from the summed visual <strong>turn</strong> — every model call in a tool loop.</p><p>That made a 55k window sit next to a 367k cache-read total and a 377k turn total after nine tool calls. The numbers were billed correctly and still looked like a leak. OpenCode&#39;s context widget and compaction check use only the last assistant message:</p><p><code>input + output + reasoning + cache.read + cache.write</code></p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>Occupancy, remaining capacity, used/window counts, turn total, provider input/output/cache/reasoning, and cache hit rate are the newest usage-bearing assistant message (the last model request). Occupancy is <code>input + output + reasoning + cacheRead + cacheWrite</code> on that message.</li><li>Completed-turn generation speed and the aggregate tool row still describe the visual turn (every fragment and tool call between the surrounding user messages).</li><li>Host completed-turn rollups, <code>addUsage</code>, and Token Insights stay additive billing. This change is renderer presentation of parent <code>message.usage</code>.</li><li>A later streaming turn without totals still does not steal the previous usage-bearing turn. Delegate rows still do not drive the ring.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Cache read stays on the same scale as the context window.</li><li>A tool-heavy turn no longer looks like it overflowed the model window.</li><li>Turn-summed cache and cost remain available to the usage plugin, not the occupancy ring.</li></ul><h2 id="rejected-alternatives" tabindex="-1">Rejected alternatives <a class="header-anchor" href="#rejected-alternatives" aria-label="Permalink to &quot;Rejected alternatives&quot;">​</a></h2><ul><li><strong>Keep the turn sum and only relabel it:</strong> users still compare 367k cache read to a 55k window in the same panel.</li><li><strong>Show both last-request occupancy and turn-summed cache:</strong> two totals in a compact inspector recreate the original contradiction.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0193-last-request-context-occupancy.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0193LastRequestContextOccupancy = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0193LastRequestContextOccupancy as default
};
