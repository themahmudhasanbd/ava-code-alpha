import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"13. Model Catalog & Selection","description":"","frontmatter":{},"headers":[],"relativePath":"spec/03-runtime/13-model-catalog-and-selection.md","filePath":"spec/03-runtime/13-model-catalog-and-selection.md","lastUpdated":1790067213000}');
const _sfc_main = { name: "spec/03-runtime/13-model-catalog-and-selection.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="_13-model-catalog-selection" tabindex="-1">13. Model Catalog &amp; Selection <a class="header-anchor" href="#_13-model-catalog-selection" aria-label="Permalink to &quot;13. Model Catalog &amp; Selection&quot;">​</a></h1><h2 id="_1-product-rule" tabindex="-1">1. Product rule <a class="header-anchor" href="#_1-product-rule" aria-label="Permalink to &quot;1. Product rule&quot;">​</a></h2><p>Users must be able to use <strong>market-available models broadly</strong>, not only a curated demo subset.</p><p>Therefore:</p><ol><li>Catalog is refreshable</li><li>Custom model IDs are always allowed</li><li>OpenAI-compatible gateways are first-class</li><li>Search is global across enabled providers</li></ol><h2 id="_2-selection-ux" tabindex="-1">2. Selection UX <a class="header-anchor" href="#_2-selection-ux" aria-label="Permalink to &quot;2. Selection UX&quot;">​</a></h2><p>Model configuration is <strong>discovery-first</strong>: the AI service is the authority on which models it serves, so the service&#39;s own endpoint is asked first and models.dev is used only to enrich what came back. The bundled catalog is never presented as a browsable list of every published model — a deployment does not necessarily host everything its vendor publishes, and a key is not necessarily entitled to it.</p><h3 id="settings-one-provider-form" tabindex="-1">Settings: one provider form <a class="header-anchor" href="#settings-one-provider-form" aria-label="Permalink to &quot;Settings: one provider form&quot;">​</a></h3><p><code>ProviderSetupDialog</code> is a single form, not a wizard:</p><ul><li><strong>Name</strong>, <strong>Base URL</strong> and <strong>API Key</strong> are all on screen at once. An empty key when editing means “keep the stored one”; <code>secretValue</code> is only sent when the user typed something.</li><li>The models section lists what the service returned. <code>useProviderModels</code> debounces edits by 600 ms, guards against out-of-order replies with a monotonic request sequence, and needs no API key so local and no-auth gateways still resolve. A saved provider paints its cached list immediately and then replaces it with the live answer.</li><li>Filtering that list is client-side: it is a short live list, not a catalog, so no host search is involved.</li><li>The list header has a checkbox that selects or clears every currently visible row in one step, so a long service list does not have to be ticked one by one. While a search filter is active, &quot;all&quot; means the matching rows only; models already chosen outside the filter stay chosen. Newly added rows adopt <code>bindingFromModelInfo</code> (or the custom-model defaults); existing bindings keep their advanced overrides. The checkbox is checked when every visible row is chosen, unchecked when none are, and indeterminate when the visible set is mixed.</li><li>The same header has a Fetch list action that probes the service immediately, skipping the 600 ms edit debounce and the cache-first paint. Existing rows stay visible while it loads. Automatic discovery on credential edits is unchanged. The control is disabled when no discoverable endpoint is ready, while a probe is in flight, or while the form is saving. Idle-with-a-valid-URL (the edit debounce) stays enabled so Fetch list can skip that window.</li><li>Context window, output limit and initial thinking levels come from <code>bindingFromModelInfo</code> over the enriched record; per-model overrides live behind a per-row <strong>Advanced</strong> disclosure. <code>publishedThinkingLevels</code> describes the catalog baseline and seeds known-model bindings. The dialog always offers the seven canonical levels, including for unknown or non-reasoning records, and the runtime uses the binding&#39;s explicit set. A model that publishes no level list and no level map but does claim reasoning still seeds <code>low</code>/<code>medium</code>/<code>high</code>.</li><li>Limit values render through one shared compact formatter (<code>formatCompactTokenCount</code>): up to two decimals at the <code>M</code> scale and one at the <code>K</code> scale, trailing zeros dropped, and a <code>K</code> mantissa that would round up to 1000 promoted to the <code>M</code> scale. Published windows on the 1M line therefore stay distinguishable — 1000000 reads <code>1M</code>, 1048576 and 1050000 read <code>1.05M</code>, 1100000 reads <code>1.1M</code> — instead of collapsing into one rounded <code>1M</code>/<code>1.1M</code>, and a limit the service never published reads as an em dash. The settings rows, the Composer picker, the context inspector and the transcript all call this one implementation, while usage counters keep a real <code>0</code> instead of the dash.</li><li>When an explicit binding enables <code>xhigh</code> or <code>max</code> without a catalog wire mapping, the runtime sends that canonical value through to the adapter rather than letting the adapter clamp it to <code>high</code>. Existing non-null catalog mappings remain authoritative for providers that translate the level.</li><li>The wire API is derived from the provider&#39;s published <code>npm</code> adapter (<code>apiStyleForAdapter</code>) and is only editable inside <strong>Advanced</strong>.</li><li>A custom model ID is always accepted, so a gateway without a <code>/models</code> route stays usable.</li></ul><h3 id="settings-selected-model-order" tabindex="-1">Settings: selected model order <a class="header-anchor" href="#settings-selected-model-order" aria-label="Permalink to &quot;Settings: selected model order&quot;">​</a></h3><p>The AI service and OAuth vendor-account editors share the selected-model pane. Each selected row has a dedicated reorder handle: drag it before or after another visible row, or focus it and press the Up or Down arrow key to move it past the neighboring visible row. Reordering is disabled while the form is busy or fewer than two selected rows are visible. Dragging text still selects it for copying; checkbox, Advanced, and Remove actions keep their existing behavior and do not start a reorder.</p><p>The complete <code>models</code> binding array owns the order. Filtering only hides rows: a move inserts the existing binding before or after the visible target in that complete array, preserving hidden bindings and their relative order. Model IDs, aliases, and advanced overrides travel with their bindings. A canceled drag or a drop outside a selected row does not change the draft.</p><p>Saving persists the new order through the existing provider update flow, and reopening either editor displays it again. Canceling the editor discards its unsaved order. The provider&#39;s compatibility <code>defaultModelId</code> still mirrors the first binding on save, so moving a model to the head changes that provider default. When the edited service or account is the app&#39;s default provider, saving also synchronizes the app-level default model to that first binding, as the existing save flow does. The app default is unchanged when editing another provider, and an explicitly bound session keeps its stored model choice. Adding a provider is not a way to change either app default: the default model, and the default image model when the new service brings image models, move to it only while nothing resolves for the app — an empty selection, or one whose provider or model is gone. A default the user can still run stays where it is until they repoint it. No storage schema or IPC contract changes are required.</p><h3 id="discovery-precedence" tabindex="-1">Discovery precedence <a class="header-anchor" href="#discovery-precedence" aria-label="Permalink to &quot;Discovery precedence&quot;">​</a></h3><p><code>providers.listModels</code> resolves in this order, and the order is load-bearing:</p><ol><li>The stored secret is resolved, so an edit needs no retyped key.</li><li><code>discoverProviderModels</code> asks the service (<code>/models</code> or the per-style equivalent). A non-empty answer wins, is enriched per model through <code>modelsDevCatalog.findModel</code>, is written back to the model cache, and is reported as <code>source: &quot;remote&quot;</code>.</li><li>Only if the endpoint published nothing usable —no route, an auth error, or an empty list— does <code>modelsForProvider</code> supply the vendor&#39;s published models, reported as <code>source: &quot;catalog&quot;</code> together with any discovery error so the UI can say the service did not answer. This result is <strong>not</strong> cached, so a catalog guess never becomes indistinguishable from a real probe.</li><li>Last resort: the provider&#39;s configured model, <code>source: &quot;fallback&quot;</code>.</li></ol><p>An OAuth vendor account skips step 2 — it has no key to probe with, and pi-ai already knows which models the subscription allows.</p><p>The picker never dumps the raw host error into the model list. A failed probe with no rows shows a classified one-line summary (auth, missing list, rate limit, timeout, network, invalid response, or HTTP status) plus a short hint to add an ID manually. A failed probe that still has cached rows keeps those rows and shows the same summary as a compact banner.</p><h3 id="subagent-editor" tabindex="-1">Subagent editor <a class="header-anchor" href="#subagent-editor" aria-label="Permalink to &quot;Subagent editor&quot;">​</a></h3><p>The Subagents create/edit sheet reuses the configured, runnable models the Composer already offers (enabled providers with a credential or <code>authKind: none</code>). The control is a searchable, provider-grouped menu anchored to its trigger rather than a native <code>&lt;select&gt;</code>: a definition may pin any configured model, so the list can run to dozens of rows, and only an anchored surface scrolls inside itself and accepts a filter. Inherit-session is the empty value, options are <code>vendorKey-or-name/modelId</code> grouped by provider display name, and a pin that is no longer configured stays as an extra row so an edit cannot silently drop it. Every option comes from the configured provider catalog, so a saved pin is always resolvable; the sheet deliberately offers no free-text model id, and when no provider has a runnable model it shows an empty state with an action that opens Models instead of a hand-typed field. Only the slash in a pin is structural: the provider half is matched by a normalized alias, and a custom endpoint&#39;s display name may contain spaces, so the picker and the draft check share one splitter and can never disagree about what is saveable. The thinking selector offers inherit-session (empty), do-not-send, and the seven canonical levels; inherit keeps the session level, while do-not-send leaves the provider adapter&#39;s own default untouched. When a generic or duplicate vendor key would be ambiguous, the option uses a unique provider display name; if the names also collide, it uses the stored provider id so no configured provider disappears from the picker.</p><p>The sheet also offers an ordered <strong>Fallback models</strong> list using that same configured-model picker. Users can add, move up/down, or remove alternatives. Already-selected models are excluded from the add menu. Saved pins that become unavailable stay visible and removable; reopening or editing another field must not drop them. Clearing the list saves <code>fallbackModels: []</code>. Inherit-session remains a primary-only choice. The hint explains that alternatives run after model retries fail, completed tool results are kept, and Stop cancels the whole task. See runtime §5f and ADR subagent-model-fallback.</p><h3 id="advanced" tabindex="-1">Advanced <a class="header-anchor" href="#advanced" aria-label="Permalink to &quot;Advanced&quot;">​</a></h3><ul><li>“Use custom model ID”</li><li>“Refresh catalog”</li><li>per-provider wire API override</li><li>per-model context window, output limit and thinking-level overrides</li></ul><h2 id="_3-recent-models" tabindex="-1">3. Recent models <a class="header-anchor" href="#_3-recent-models" aria-label="Permalink to &quot;3. Recent models&quot;">​</a></h2><p>Persist recent selected model refs:</p><div class="language-ts vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">type</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> RecentModelRef</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  providerId</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  modelId</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  usedAt</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>Show top N in picker.</p><h2 id="_4-session-model-binding" tabindex="-1">4. Session model binding <a class="header-anchor" href="#_4-session-model-binding" aria-label="Permalink to &quot;4. Session model binding&quot;">​</a></h2><p>Each session stores:</p><ul><li><code>providerId</code></li><li><code>modelId</code></li><li><code>thinkingLevel</code> (<code>off|minimal|low|medium|high|xhigh|max|omit</code>)</li></ul><p>Changing model or thinking level mid-session affects subsequent turns only. The stored thinking preference survives restart; the effective request level is clamped against the selected model binding&#39;s enabled levels at execution time, except <code>omit</code>, which is preserved on a reasoning model and sends no thinking override. An empty binding or a binding containing only <code>off</code> resolves to <code>off</code>.</p><p>For a newly created session, the renderer resolves the selected (or app-default) model&#39;s <code>ModelBinding</code>. A reasoning model starts at that binding&#39;s <code>defaultThinkingLevel</code> (<code>omit</code> is preserved; other values are clamped onto the enabled levels). When the default is unset it falls back to the highest enabled level seeded from published <code>supportedThinkingLevels</code>. A non-reasoning or unknown model starts at <code>off</code> until the user enables a non-<code>off</code> level. This is a creation default only and never rewrites an existing session&#39;s stored choice.</p><p>Unpinned sessions still advertise that inherited default model&#39;s reasoning capability on session list/get/create/fork/configure. Enrichment does not pin <code>providerId</code>/<code>modelId</code>; desktop session create does, by writing the then-current app default (or Composer draft override) into the durable ids. Later Settings default-model changes do not rewrite an already created session. Opening a legacy row whose ids are still empty snapshots the last used turn, else the current default, so it stops following Settings. The Composer never treats a <code>supportsReasoning: false</code> or empty thinking-level snapshot as authoritative when the selected catalog/binding model exposes levels, so a mid-turn thinking or model pick cannot collapse the menu to Off-only.</p><h2 id="_5-capability-warnings" tabindex="-1">5. Capability warnings <a class="header-anchor" href="#_5-capability-warnings" aria-label="Permalink to &quot;5. Capability warnings&quot;">​</a></h2><p>If user selects model tagged without tools while in Agent mode:</p><ul><li>show non-blocking warning</li><li>do not hard-block (vendor tags may be incomplete)</li></ul><h2 id="_6-refresh-behavior" tabindex="-1">6. Refresh behavior <a class="header-anchor" href="#_6-refresh-behavior" aria-label="Permalink to &quot;6. Refresh behavior&quot;">​</a></h2><p>The bundled <code>apps/desktop/resources/models.dev/api.json</code> snapshot is the startup baseline. It is refreshed by <code>scripts/release.mjs</code> before a release tag is created; application startup does not fetch or write a catalog. Settings invokes the Electron-only <code>providers.refreshModelCatalog</code> channel to refetch <code>https://models.dev/api.json</code>; a successful response replaces only the current process&#39;s in-memory models.dev catalog and never writes user data.</p><p>Repeated metadata lookups use a bounded process-local cache keyed by the configured vendor key, base URL, and case-insensitive, trimmed model ID. Both matches and misses are cached; the original provider preference, alias matching, and candidate ranking remain unchanged. Replacing the catalog after a successful bundled load or Settings refresh invalidates the cache. A failed refresh preserves the previous catalog and its results. Session capability enrichment resolves a matching catalog record once per session and then applies the current provider/model binding and session defaults, so user overrides are never retained as stale cached capabilities. Refreshing a large session list must not repeat a full catalog scan for every occurrence of the same lookup.</p><p>Provider model loading remains stale-while-revalidate:</p><ol><li><code>source: &quot;cache&quot;</code> hydrates a saved provider&#39;s normalized discovery rows from Rust-owned SQLite without provider network access.</li><li>The renderer can show those rows immediately in the Composer and provider dialog.</li><li><code>source: &quot;refresh&quot;</code> uses the bundled/in-memory models.dev catalog first and probes a provider endpoint only to discover IDs that models.dev does not expose.</li><li>Successful provider discovery may update the Rust-owned normalized cache; it cannot replace a matching models.dev record or its metadata.</li><li>Configured <code>ModelBinding</code> IDs are retained when discovery is partial or unavailable, and an ID absent from models.dev receives generic metadata.</li></ol><h2 id="_7-offline-behavior" tabindex="-1">7. Offline behavior <a class="header-anchor" href="#_7-offline-behavior" aria-label="Permalink to &quot;7. Offline behavior&quot;">​</a></h2><p>If refresh fails / offline:</p><ul><li>use cached catalog</li><li>never clear an already-rendered cached list or flash an empty picker</li><li>allow custom model id</li><li>still allow providers with known model ids</li><li>when a saved provider cache is empty or partial, append every configured model binding before applying models.dev metadata decoration, so multi-model settings remain editable and per-model capability state stays aligned</li><li>if the bundled release snapshot is absent or invalid during an offline release, keep the configured IDs visible with generic text-only metadata</li></ul><h2 id="_8-catalog-item-schema" tabindex="-1">8. Catalog item schema <a class="header-anchor" href="#_8-catalog-item-schema" aria-label="Permalink to &quot;8. Catalog item schema&quot;">​</a></h2><div class="language-ts vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">type</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> ModelCatalogItem</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  providerId</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  vendorKey</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  modelId</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  displayName</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  source</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;bundled&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;discovered&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;user&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;recent&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  capabilities</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> Array</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&lt;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">    |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;tools&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">    |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;vision&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">    |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;reasoning&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">    |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;streaming&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">    |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;json&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">    |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;long_context&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  &gt;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  contextWindow</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> number</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  maxOutputTokens</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> number</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  deprecated</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> boolean</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  notes</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  supportedThinkingLevels</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> Array</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&lt;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">    &quot;off&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;minimal&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;low&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;medium&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;high&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;xhigh&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;max&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  &gt;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">  /** Which known catalog supplied metadata for this row. */</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  catalogSource</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;models.dev&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br><span class="line-number">19</span><br><span class="line-number">20</span><br><span class="line-number">21</span><br><span class="line-number">22</span><br><span class="line-number">23</span><br><span class="line-number">24</span><br></div></div><h2 id="_9-selection-resolution-order" tabindex="-1">9. Selection resolution order <a class="header-anchor" href="#_9-selection-resolution-order" aria-label="Permalink to &quot;9. Selection resolution order&quot;">​</a></h2><p>When UI/search requests models for picker:</p><ol><li>recent models for enabled providers</li><li>user-defined models</li><li>models.dev records for the matching provider/API URL</li><li>provider discovery/cache for custom or account-specific models</li><li>always include &quot;custom model id&quot; entry action</li></ol><p>Deduplicate by <code>(providerId, modelId)</code> with priority: <code>user &gt; models.dev &gt; provider-discovered &gt; recent-only</code>. The <code>catalogSource</code> field records a models.dev match; a provider cache stores only normalized selection fields and is re-decorated from the local raw catalog on the next read.</p><h3 id="_9-1-effective-context-window" tabindex="-1">9.1 Effective context window <a class="header-anchor" href="#_9-1-effective-context-window" aria-label="Permalink to &quot;9.1 Effective context window&quot;">​</a></h3><p>The runtime, the context inspector and the settings surface resolve one effective model window. Every binding records where its <code>contextWindow</code> came from (<code>contextWindowSource</code>):</p><ul><li><code>catalog</code> — the number is a models.dev snapshot, so a later correction to the published <code>limit.context</code> replaces it. A refreshed record such as <code>gpt-5.6-luna</code> (<code>1,050,000</code> tokens) stops appearing as a 128k model, and a limit that models.dev corrects reaches the binding without deleting and re-adding the model.</li><li><code>user</code> — the number was entered through the per-model Advanced control (or the preset ladder in it) and is never replaced by the catalog, including the <code>128,000</code> value that is otherwise the generic seed.</li></ul><p>Bindings written before the marker name no source. They keep the historical rule, deterministically: a published <code>limit.context</code> replaces exactly the generic <code>128,000</code> seed, and every other stored value stays the explicit value. Unknown models still use the conservative 128k generic window and are never promoted from an ID pattern alone. The marker is optional in the persisted record, so a config written by an older version stays readable and a downgrade ignores it.</p><p>The configured user value remains persisted and visible in Advanced settings, but provider safety does not trust an enlarged override beyond a known published window. Outbound output caps, automatic compaction, and overflow classification use the smaller of the configured and published windows; a smaller user value continues to narrow the runtime budget.</p><h3 id="_9-2-conversation-composer-scope" tabindex="-1">9.2 Conversation Composer scope <a class="header-anchor" href="#_9-2-conversation-composer-scope" aria-label="Permalink to &quot;9.2 Conversation Composer scope&quot;">​</a></h3><p>The conversation Composer is a configured-model picker, not a raw discovery catalog. For each enabled, runnable provider it renders only the model IDs in that provider&#39;s persisted <code>models</code> bindings (or the legacy <code>defaultModelId</code> fallback). Cached or freshly discovered records may enrich those rows with display names and metadata, but a discovered model that is not configured is not shown in the conversation list. When discovery is missing, the configured IDs remain visible by themselves.</p><p>The Settings provider dialog continues to use discovery to add and configure models; saving a model binding is what makes it eligible for the Composer.</p><p>When the combined Composer menu opens, the renderer starts provider-model hydration before the Model submenu is entered. The first visible rows therefore come from the cached catalog or configured bindings; live discovery remains a background update. A configured non-empty alias is resolved from the binding for every equivalent model ID and remains the sole visible model name while the catalog is refreshed.</p><p>Vision badges in the Composer use the effective image-input capability for the exact provider/model binding. An explicit <code>supportsImages: true</code> or <code>false</code> wins over the published record; an absent or <code>null</code> value follows it. This lets a configured custom or proxied model show the capability the endpoint was explicitly configured to use without shaping the published <code>ModelInfo</code>. An OAuth provider heading uses its non-secret account label when present, so duplicate accounts from one vendor remain distinguishable; model rows still use the configured model alias or published model name.</p><h2 id="_10-default-model-policy" tabindex="-1">10. Default model policy <a class="header-anchor" href="#_10-default-model-policy" aria-label="Permalink to &quot;10. Default model policy&quot;">​</a></h2><p>App-level default:</p><ul><li>first successfully tested provider + its default/recommended model</li><li>the Settings default-model picker lists every configured model under its provider; selecting an entry persists both the owning provider and that exact model ID</li><li>saving that provider preserves the selected app-default model while it remains configured; removing it falls back to the first remaining binding</li><li>the picker supports local search across provider name and model ID; its result list scrolls within the floating surface and shows an explicit empty state when no model matches</li><li>the picker uses concise settings-specific search copy; each result gives visual priority to the model ID and keeps the provider as secondary metadata</li><li>results are grouped by provider so a provider name is shown once per group rather than repeated on every model row</li><li>a provider is named the same way here as in the Composer menu: an OAuth row uses its non-secret account label when present, so two accounts of one vendor stay distinguishable in the group heading, in the summary line that reports the current default, and in each option&#39;s accessible name. Search matches the account label and the vendor name, so either spelling reaches the row</li><li>the Settings prompt-enhancement model picker reuses this menu and resolves its provider names the same way</li><li>if none configured, onboarding checklist requires provider setup before first agent run</li></ul><p>Session-level:</p><ul><li>inherits app default at creation and stores that <code>providerId</code>/<code>modelId</code> pair</li><li>later Settings default-model changes apply only to new sessions and the unpersisted home draft, not to already created sessions</li><li>initializes thinking to the highest level enabled by the inherited model&#39;s binding; published levels seed a new binding, while an empty or <code>off</code>-only binding starts at <code>off</code></li><li>can override independently</li></ul><h2 id="_11-capability-gating" tabindex="-1">11. Capability gating <a class="header-anchor" href="#_11-capability-gating" aria-label="Permalink to &quot;11. Capability gating&quot;">​</a></h2><table tabindex="0"><thead><tr><th>mode/feature</th><th>required capability</th></tr></thead><tbody><tr><td>Agent mode tools</td><td><code>tools</code> (warn if missing; hard-block only if runtime cannot function)</td></tr><tr><td>image input</td><td><code>vision</code></td></tr><tr><td>reasoning UI affordances</td><td><code>reasoning</code></td></tr><tr><td>structured repair helpers</td><td><code>json</code> optional</td></tr></tbody></table><p>Warnings are non-blocking unless execution is impossible.</p><h3 id="_11-1-reasoning-capability-resolution" tabindex="-1">11.1 Reasoning capability resolution <a class="header-anchor" href="#_11-1-reasoning-capability-resolution" aria-label="Permalink to &quot;11.1 Reasoning capability resolution&quot;">​</a></h3><ol><li>Resolve the models.dev metadata for the matching provider/API URL and exact <code>modelId</code>. Matching also accepts a catalog vendor prefix when the configured provider uses an unprefixed ID (for example <code>deepseek-v4</code> matches <code>deepseek/deepseek-v4</code> only under the matching provider identity).</li><li>The models.dev record is authoritative for published <code>reasoning</code> and <code>reasoning_options</code>; cached/provider capability claims cannot replace it.</li><li>The provider&#39;s exact <code>ModelBinding.thinkingLevels</code> is authoritative for the user&#39;s effective selection. It may explicitly enable a canonical level that the catalog does not publish.</li><li>A free-form ID absent from models.dev starts as an unknown generic model and exposes only <code>off</code>; Settings can promote it only after an explicit binding selection, never through discovery or an automatic inference.</li><li>The Composer renders the effective binding levels in canonical order. If no binding exists, it falls back to the published model levels and provider defaults.</li><li>If a stored/requested level is unavailable, choose the nearest enabled binding level by scanning upward first and then downward. A binding with no non-<code>off</code> level resolves to <code>off</code>.</li><li>Changing to a provider/model with no enabled reasoning level persists <code>off</code>; no unconfigured level leaks into the next request.</li><li>For explicitly enabled <code>xhigh</code>/<code>max</code>, an absent or null catalog mapping is materialized as an identity adapter mapping; a non-null catalog mapping is preserved.</li></ol><h3 id="_11-2-vision-capability-resolution" tabindex="-1">11.2 Vision capability resolution <a class="header-anchor" href="#_11-2-vision-capability-resolution" aria-label="Permalink to &quot;11.2 Vision capability resolution&quot;">​</a></h3><ol><li>Resolve the published image-input baseline from the matching model record.</li><li>Apply the exact configured binding&#39;s <code>supportsImages</code> value to that baseline. An absent or <code>null</code> value follows the published capability; <code>true</code> enables image input for a configured endpoint even when its published record is text-only, and <code>false</code> disables a published image capability.</li><li>The Composer model-row vision badge and the main attachment transport gate use this same effective result. An unknown or custom model without an explicit binding override remains on the conservative path-fallback route; discovery or cache metadata alone cannot promote it to image transport.</li><li>The main process prepares pasted images as content-addressed refs. A vision-capable model receives images within the 10 MB app-side inline bound as transient image blocks; other cases receive a safe <code>@path</code>.</li></ol><h3 id="_11-3-settings-model-add-metadata" tabindex="-1">11.3 Settings model-add metadata <a class="header-anchor" href="#_11-3-settings-model-add-metadata" aria-label="Permalink to &quot;11.3 Settings model-add metadata&quot;">​</a></h3><p>When the setup form adds a model the service returned, its initial context window, output limit, capability badges and thinking defaults come from <code>bindingFromModelInfo</code> over the enriched record, so the common path needs no manual token entry. The enrichment lookup is:</p><ol><li>A matching models.dev provider is preferred by <code>vendorKey</code>, then by normalized provider API URL, including explicit native-adapter aliases such as <code>openai-codex</code> → <code>openai</code>; its exact model record supplies the fields.</li><li>A provider endpoint may add custom/account-specific IDs, but cannot replace models.dev metadata. A free-form miss receives the fixed generic defaults from <code>bindingForCustomModel</code>.</li><li>The lookup does not send API keys to models.dev. Runtime model resolution uses the same models.dev record and the selected pi-ai transport adapter.</li></ol><h2 id="_12-refresh-strategy" tabindex="-1">12. Refresh strategy <a class="header-anchor" href="#_12-refresh-strategy" aria-label="Permalink to &quot;12. Refresh strategy&quot;">​</a></h2><ul><li>manual refresh button in settings/model picker</li><li>optional refresh on provider create/test success</li><li>no aggressive background polling in MVP</li><li>refresh failures keep previous cache and surface non-fatal error</li></ul><p>Electron decorates cached and freshly returned model rows from the local models.dev snapshot. Runtime model resolution passes the same full models.dev configuration to the selected pi-ai transport adapter. Provider discovery remains an ID-only fallback for custom/account-specific models absent from the snapshot.</p><h2 id="_13-search-behavior" tabindex="-1">13. Search behavior <a class="header-anchor" href="#_13-search-behavior" aria-label="Permalink to &quot;13. Search behavior&quot;">​</a></h2><p>There is no catalog search channel. The models a user chooses from are the ones their service returned, and that list is short enough to filter in the renderer: the provider form matches the typed text against model id and display name with a plain case-insensitive substring test.</p><p>The Composer picker likewise searches the <strong>configured</strong> models only, matching model id, display name, published family and the account-aware provider display name (<code>composerModelMatchesQuery</code>).</p><p>Model ids are compared case-insensitively wherever a chosen model is matched against a returned one, so a hand-typed <code>GPT-5</code> and a published <code>gpt-5</code> are the same model to the check mark, the toggle and the duplicate guard.</p><h2 id="_14-acceptance-criteria" tabindex="-1">14. Acceptance criteria <a class="header-anchor" href="#_14-acceptance-criteria" aria-label="Permalink to &quot;14. Acceptance criteria&quot;">​</a></h2><ul><li>[ ] the model list a user chooses from is the one their service returned; the bundled catalog is never offered as a browsable list of every published model</li><li>[ ] the catalog is consulted only when the endpoint publishes nothing usable, and that result is reported as <code>catalog</code>, is not cached, and carries the discovery error</li><li>[ ] adding an AI service is one form, with no stages to step through, and the wire API is derived from the published adapter instead of being asked for</li><li>[ ] adding a discovered model requires no manual context-window or output-token entry; overrides stay behind a per-model Advanced disclosure</li><li>[ ] the API-key path and the OAuth vendor-account path use the same live model list and the same binding shape</li><li>[ ] selected models can be reordered by drag handle or Up/Down arrow keys in both editors; saving and reopening preserves the order, aliases, and overrides, and a filtered move preserves hidden bindings and their order</li><li>[ ] canceling a drag or the editor preserves the previous applicable order; busy forms disable reordering, and text-copy and row actions still work</li><li>[ ] an unsaved provider can be probed from the form before it is persisted, and a saved one reuses its stored secret without a retyped key</li><li>[ ] custom model id path works without catalog hit</li><li>[ ] recent models surface in the picker</li><li>[ ] refresh merges into cache and picker (never destructively replaces)</li><li>[ ] restart hydrates the prior catalog before live refresh, and offline refresh keeps the cached picker populated</li><li>[ ] capability badges visible</li><li>[ ] session model change applies to next turn only</li><li>[ ] a newly created session stores the then-current default provider/model, and later default-model changes do not rewrite that session</li><li>[ ] a new session defaults a reasoning-capable inherited model to that binding&#39;s stored default thinking level (clamped onto the enabled set; strongest-enabled only when unset) and otherwise defaults to <code>off</code></li><li>[ ] the settings picker always exposes the canonical thinking ladder; published levels seed known models and explicit binding levels clamp the same way in Composer, Electron main, and the pi sidecar</li><li>[ ] models.dev metadata wins for a matching provider/model; an ID absent from it uses the generic shape while pi-ai supplies only transport/OAuth</li><li>[ ] provider settings and cached discovery cannot replace known catalog capabilities; explicit binding edits remain persisted configuration</li><li>[ ] a models.dev limit correction reaches an already saved <code>catalog</code> binding without deleting and re-adding the model, while a number the user entered in Advanced (<code>user</code>) survives every correction, a hand-entered <code>128,000</code> included</li><li>[ ] a binding saved before the provenance marker resolves deterministically: the generic 128k seed follows the catalog and every other value stays as stored</li><li>[ ] the provenance marker survives a provider save/read round trip and an unmarked record keeps working</li><li>[ ] unknown free-form models remain runnable without invented capabilities</li><li>[ ] a models.dev record and an unknown generic record resolve through the same selected transport without sending provider credentials to the remote catalog</li><li>[ ] compact limit text never reads above the published value, keeps the neighbouring 1M-line windows apart (<code>1M</code> / <code>1.05M</code> / <code>1.1M</code>), and never renders a <code>K</code> mantissa at or above 1000</li></ul><h2 id="image-model-binding" tabindex="-1">Image model binding <a class="header-anchor" href="#image-model-binding" aria-label="Permalink to &quot;Image model binding&quot;">​</a></h2><p>The default conversation model has a separate <strong>Image generation model</strong> row below it. Model Advanced can select that unique binding; provider form Save commits it, Cancel discards it, and replacing it leaves the conversation default unchanged. See <a href="./21-image-generation">image generation and editing</a> for the tool and batch contract.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("spec/03-runtime/13-model-catalog-and-selection.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _13ModelCatalogAndSelection = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _13ModelCatalogAndSelection as default
};
