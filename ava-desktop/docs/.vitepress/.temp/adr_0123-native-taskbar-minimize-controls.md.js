import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0123: Use native taskbar minimize for Windows/Linux window controls","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0123-native-taskbar-minimize-controls.md","filePath":"adr/0123-native-taskbar-minimize-controls.md","lastUpdated":1787654633000}');
const _sfc_main = { name: "adr/0123-native-taskbar-minimize-controls.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0123-use-native-taskbar-minimize-for-windows-linux-window-controls" tabindex="-1">ADR 0123: Use native taskbar minimize for Windows/Linux window controls <a class="header-anchor" href="#adr-0123-use-native-taskbar-minimize-for-windows-linux-window-controls" aria-label="Permalink to &quot;ADR 0123: Use native taskbar minimize for Windows/Linux window controls&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-08-25</li><li>Deciders: PI-Desktop core</li><li>Related: D216, D230, D252, D256, E2E-124, ADR 0078, ADR 0090, ADR 0117</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Windows/Linux use a frameless window with renderer-drawn minimize, maximize, and close controls. The minimize action was calling <code>window.hide()</code>, which removed the window from the taskbar and made the action behave like a close-to-tray operation. The close button already has an independent, persisted <code>tray</code>/<code>quit</code> choice, so two different controls were unexpectedly using the same hidden-window behavior.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>The Windows/Linux renderer window-control <code>minimize</code> action calls Electron&#39;s native <code>BrowserWindow.minimize()</code> transition. The Windows/Linux native-menu minimize action uses the same transition.</li><li>The main-window <code>minimize</code> event does not convert Windows/Linux native minimization into <code>hide()</code>. The operating system keeps the taskbar entry so the user can restore the same window normally.</li><li>macOS keeps its existing traffic-light tray-resident minimize behavior. Windows/Linux close behavior remains owned by ADR 0090: the close button hides to the tray for <code>tray</code> and exits for <code>quit</code>.</li><li>The resident tray, close-behavior persistence, IPC action allowlists, host protocol, and background-process lifetime do not change.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>The three Windows/Linux window buttons now match the expected desktop model: minimize goes to the taskbar, maximize toggles the window, and close follows the user&#39;s remembered tray/quit choice.</li><li>The tray remains available for close-to-tray and for macOS&#39;s existing minimize behavior; no second tray lifecycle is introduced.</li><li>No new privileged bridge or protocol version is required because the existing <code>minimize</code> action changes only its Main-owned Electron effect.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Keep explicit minimize as hide-to-tray:</strong> rejected because it makes the minimize and close controls indistinguishable when close-to-tray is selected and removes the standard taskbar restore path.</li><li><strong>Remove the resident tray:</strong> rejected because close-to-tray and macOS minimize still need a reliable restore surface.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0123-native-taskbar-minimize-controls.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0123NativeTaskbarMinimizeControls = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0123NativeTaskbarMinimizeControls as default
};
