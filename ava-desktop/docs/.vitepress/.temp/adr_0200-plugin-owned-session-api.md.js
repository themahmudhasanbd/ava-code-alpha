import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0200: Host-Owned Plugin Session Import and Ownership API","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0200-plugin-owned-session-api.md","filePath":"adr/0200-plugin-owned-session-api.md","lastUpdated":1788959642000}');
const _sfc_main = { name: "adr/0200-plugin-owned-session-api.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0200-host-owned-plugin-session-import-and-ownership-api" tabindex="-1">ADR 0200: Host-Owned Plugin Session Import and Ownership API <a class="header-anchor" href="#adr-0200-host-owned-plugin-session-import-and-ownership-api" aria-label="Permalink to &quot;ADR 0200: Host-Owned Plugin Session Import and Ownership API&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-09</li><li>Decision: D367</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Plugins need to migrate and inspect conversation history from external tools. The existing <code>session.getLlmContext</code> API is intentionally limited to the in-flight tool call, while the core <code>session.import</code> boundary accepts a host session summary and can bind a project. Reusing either shape would let a plugin choose a core session identity or create project records outside its ownership boundary.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Add a separate P0/P1 <code>pi.session</code> domain for plugin-owned imported sessions:</p><ul><li><code>import</code>, <code>importBatch</code>, <code>list</code>, <code>get</code>, <code>listMessages</code>, <code>rename</code>, and <code>delete</code> are exposed through new plugin permissions and additive host RPC methods.</li><li>A manifest declares <code>contributes.sessionSources</code>. The Electron permission gateway rejects undeclared sources before host dispatch.</li><li>Host-core generates session, message, and internal tool-call ids. The idempotency key is <code>(pluginId, source, externalId)</code> in <code>session_import_origins</code>; a plugin can only query or mutate its own rows.</li><li>Imported sessions default to no active project/provider/model binding. ADR 0201 adds an explicit host-created project-id opt-in; original values are retained as history JSON for inspection, not used as live authorization or execution configuration.</li><li><code>trash</code> soft-deletes and hides a row while preserving its transcript and origin; <code>purge</code> cascades the row and removes transcript files. Purging a trashed row remains available to its owning plugin.</li><li>The host and Electron runtime enforce bounded content, tool values, payload depth, message/batch sizes, strict timestamps, reserved-key scrubbing, and per-plugin rolling import/delete limits.</li><li>P2/P3 operations (session creation, message mutation, binding, batch delete, and tags) remain deferred and are not represented as permissions.</li></ul><p>Schema v14 adds <code>sessions.deleted_at</code> and the origin sidecar. The JSON-RPC protocol remains v11 because these methods are additive and only Electron main can provide the trusted plugin id.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><p>The plugin API can safely support external-history migration without exposing SQLite, transcripts, host ids, or another plugin&#39;s sessions. The storage layer gains one migration and a soft-delete state that normal core session lists must exclude. Future P2/P3 work must extend this ownership model rather than reuse the core session importer.</p><h2 id="verification" tabindex="-1">Verification <a class="header-anchor" href="#verification" aria-label="Permalink to &quot;Verification&quot;">​</a></h2><p>Coverage includes manifest/source validation, runtime permission and payload checks, host idempotency, batch skip/rollback semantics, ownership filtering, list/get/rename, message paging/truncation, tool reserved-key scrubbing, trash-to-purge lifecycle, migration-compatible schema creation, and rate limits. Full UI E2E execution remains deferred per repository policy; scenarios E2E-214 and E2E-215 are recorded in the E2E plan.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0200-plugin-owned-session-api.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0200PluginOwnedSessionApi = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0200PluginOwnedSessionApi as default
};
