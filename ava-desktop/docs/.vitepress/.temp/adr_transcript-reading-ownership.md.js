import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR transcript-reading-ownership: Share renderer history and search views","description":"","frontmatter":{},"headers":[],"relativePath":"adr/transcript-reading-ownership.md","filePath":"adr/transcript-reading-ownership.md","lastUpdated":1789300134000}');
const _sfc_main = { name: "adr/transcript-reading-ownership.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-transcript-reading-ownership-share-renderer-history-and-search-views" tabindex="-1">ADR transcript-reading-ownership: Share renderer history and search views <a class="header-anchor" href="#adr-transcript-reading-ownership-share-renderer-history-and-search-views" aria-label="Permalink to &quot;ADR transcript-reading-ownership: Share renderer history and search views&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-13</li><li>Amends: <a href="/adr/session-content-search">ADR session-content-search</a></li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Search navigation added a pane-local controller alongside ordinary history paging, a global target handoff, and live/model caches. These independent paths needed separate cancellation and rewrite handling. Nested assistant answers were searchable but had no top-level transcript row, while Markdown could hide the source characters that matched a query.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>The renderer store owns one transient <code>TranscriptView</code> per retained session. Ordinary paging and search use the same read actions and physical cursors; MainChat and the subagent dock use the same projection. The pending view&#39;s identity owns an asynchronous read. Replacement, eviction, return to latest, canonical edits, and a new turn invalidate stale target or page completions. Ordinary history always uses the current canonical value for overlapping rows, including streaming-to-complete transitions. Explicit search retains its selected historical snapshot until the user leaves it or starts a turn.</p><p>Reading views never populate runtime/model caches. A message action hydrates canonical input when history is partial or display-limited, then rechecks the session, run, navigation, and message-snapshot ownership before publishing it.</p><p>The additive <code>SessionDetail.navigationParent</code> field carries a nested target&#39;s latest owning Task as capped display context. It does not widen the physical page or change its cursors. Existing uncapped and ordinary reads omit it. The renderer reveals the Task and locates the answer in the existing details dock; missing parent context is an explicit navigation failure.</p><p>Parser-owned source offsets map hidden Markdown syntax and destinations to visible elements. User text and file chips preserve equivalent source offsets. Literal rendered matches use CSS ranges; source-only matches highlight their owning element. The shared browser focus effect releases bottom following and bounds layout correction by a deadline or the first reading gesture.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><p>This removes the global target transfer, pane-local navigation controller, and separate ordinary-history load lock. Live/model cache ownership, transcript storage, IPC allowlisting, and protocol version remain unchanged. The optional parent projection is backward compatible for existing session readers.</p><p>Navigation does not load an entire transcript into the renderer. Stable-ID and parent lookup still scan canonical physical identities; this decision does not introduce a new search or message-offset index. Complete selected message text can still be large, by design. Source-only matches identify the visible owner rather than reproducing hidden Markdown as extra UI copy.</p><h2 id="validation" tabindex="-1">Validation <a class="header-anchor" href="#validation" aria-label="Permalink to &quot;Validation&quot;">​</a></h2><p>Renderer state tests cover competing reads, streaming during ordinary paging, new turns, pane eviction, same-ID rewrites, and canonical action preparation. Server rendering tests exercise nested answers, collapsed Task disclosure, Markdown destinations and delimiters, file chips, long messages, and CRLF source offsets. Browser-effect unit tests cover geometry, gesture interruption, and StrictMode-style cleanup/replay. Host tests cover bounded physical pages and latest parent context outside the page. The interaction contract is <code>E2E-SESSION-content-search-and-message-navigation</code>; these unit/rendering checks do not claim a completed native-app E2E run.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/transcript-reading-ownership.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const transcriptReadingOwnership = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  transcriptReadingOwnership as default
};
