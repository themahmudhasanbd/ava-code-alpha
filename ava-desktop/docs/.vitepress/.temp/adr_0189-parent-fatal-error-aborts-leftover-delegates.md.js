import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0189: Parent fatal error aborts leftover delegates","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0189-parent-fatal-error-aborts-leftover-delegates.md","filePath":"adr/0189-parent-fatal-error-aborts-leftover-delegates.md","lastUpdated":1788884981000}');
const _sfc_main = { name: "adr/0189-parent-fatal-error-aborts-leftover-delegates.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0189-parent-fatal-error-aborts-leftover-delegates" tabindex="-1">ADR 0189: Parent fatal error aborts leftover delegates <a class="header-anchor" href="#adr-0189-parent-fatal-error-aborts-leftover-delegates" aria-label="Permalink to &quot;ADR 0189: Parent fatal error aborts leftover delegates&quot;">​</a></h1><ul><li>Status: Accepted for implementation</li><li>Date: 2026-09-08</li><li>Deciders: PI-Desktop core</li><li>Related: D352, D328, ADR 0166, ADR 0089, E2E-155</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>D328 / ADR 0166 keep a durable turn open when the parent <em>idles</em> with running delegates, then resume the parent with their reports. That path is correct for a parent that stopped calling tools.</p><p>A terminal parent provider error is different. After a 429 budget is exhausted, Electron already finishes the durable turn as <code>error</code> and the renderer shows Continue. Leftover delegates — often on another model that is not rate-limited — kept <code>isRunning</code> true, so Continue was rejected as <code>AGENT_BUSY</code> (<code>session already has an active turn</code>). The main conversation looked finished while the sidecar was still busy.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li><strong>Parent idle still does not abort delegates.</strong> D328 is unchanged for a parent that stops calling tools without a fatal error.</li><li><strong>A terminal parent error does abort leftover delegates.</strong> Exhausted rate limits, other terminal provider/stream errors, overflow-recovery failure, mutation-budget termination, and rejected-prompt failures abort running delegates, skip the resume prompt, and emit <code>agent_end</code>.</li><li><strong>The session becomes idle for Continue.</strong> <code>getStatus().isRunning</code> does not count leftover delegates after <code>turnHadError</code>. The failed TurnOutcomeCard stays visible; a later <code>agent_end</code> must not overwrite it with <code>completed</code>.</li><li><strong>A later parent prompt does not inherit previous-turn delegates.</strong> Each <code>prompt()</code> / <code>executeApprovedPlan()</code> bumps a turn epoch, aborts leftover runs from earlier epochs, and only auto-resumes delegates started in the current turn.</li></ol><p>No IPC, storage, or host-protocol change.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Continue after a 429 (or other terminal parent error) is accepted even if a Gemini (or other) subagent was still running.</li><li>Work those leftover delegates had not yet reported is aborted rather than written into a dead parent turn.</li><li>Parent idle with live delegates still shows a live turn until reports are delivered.</li></ul><h2 id="alternatives-rejected" tabindex="-1">Alternatives rejected <a class="header-anchor" href="#alternatives-rejected" aria-label="Permalink to &quot;Alternatives rejected&quot;">​</a></h2><ul><li><strong>Keep leftover delegates running after parent error.</strong> The session stays busy and Continue is <code>AGENT_BUSY</code>.</li><li><strong>Detach leftover delegates from <code>isRunning</code> without aborting.</strong> A later Continue turn would race them on the workspace.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0189-parent-fatal-error-aborts-leftover-delegates.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0189ParentFatalErrorAbortsLeftoverDelegates = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0189ParentFatalErrorAbortsLeftoverDelegates as default
};
