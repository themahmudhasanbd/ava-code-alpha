import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0197: Publish a Windows Portable Package","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0197-windows-portable-exe.md","filePath":"adr/0197-windows-portable-exe.md","lastUpdated":1790060879000}');
const _sfc_main = { name: "adr/0197-windows-portable-exe.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0197-publish-a-windows-portable-package" tabindex="-1">ADR 0197: Publish a Windows Portable Package <a class="header-anchor" href="#adr-0197-publish-a-windows-portable-package" aria-label="Permalink to &quot;ADR 0197: Publish a Windows Portable Package&quot;">​</a></h1><ul><li>Status: Accepted (amended by D603)</li><li>Date: 2026-09-22</li><li>Deciders: PI-Desktop core</li><li>Related: D120, D126, D364, D603, ADR 0022, E2E-211</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Windows tag releases publish an NSIS installer (<code>PI-Desktop-Setup-&lt;version&gt;.exe</code>). Company environments that whitelist a single executable, or that block installers, also need a no-install package. The previous electron-builder <code>portable</code> target produced a self-extracting executable. In some Windows environments that wrapper could trigger an administrator prompt and did not provide a reliable normal executable identity for the taskbar.</p><p>The no-install lane must not change data ownership or the NSIS in-app update lane. A normal ZIP lets the user choose the extraction directory and launches the packaged <code>PI-Desktop.exe</code> directly.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><ol><li>The Windows x64 release lane publishes both NSIS and ZIP targets.</li><li>The no-install artifact name is space-free: <code>PI-Desktop-Portable-\${version}.zip</code>.</li><li>The Windows release helper builds NSIS and ZIP in separate electron-builder invocations. The ZIP app metadata contains <code>piDistribution = &quot;zip&quot;</code>; installed builds contain <code>piDistribution = &quot;installed&quot;</code>.</li><li>The ZIP target does not write <code>latest.yml</code>. Packaged ZIP runs use notify-and-link delivery, while NSIS installs keep in-app download and quit-and-install.</li><li>The updater continues to recognize <code>PORTABLE_EXECUTABLE_FILE</code> for older portable executables, but ordinary ZIP launches use the packaged metadata marker instead.</li><li>User data, logs, and secrets stay in the existing application data directory. This decision does not introduce a beside-the-exe profile.</li></ol><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Windows users who cannot run an installer can download, extract, and launch <code>PI-Desktop.exe</code> from the ZIP without a self-extracting wrapper.</li><li>The extracted executable keeps the normal PI-Desktop Windows identity for taskbar grouping and icon display.</li><li>ZIP users discover updates in-app and open the releases page; they replace the extracted application themselves.</li><li>NSIS in-app updates, hashes, and feed ownership are unchanged.</li><li>The ZIP package is larger on disk after extraction than the old temporary self-extracting wrapper, but it avoids runtime extraction and its related execution-policy prompts.</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li>Keep electron-builder&#39;s <code>portable</code> target: rejected because its self-extracting wrapper is the source of the administrator-prompt and taskbar-identity problems this amendment addresses.</li><li>In-app update of ZIP via the NSIS installer: rejected because it would install the application and require a whitelisted installer.</li><li>Beside-the-exe data directory: rejected as an unrelated data-ownership change; <code>PI_DESKTOP_DATA_DIR</code> already relocates the profile when needed.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0197-windows-portable-exe.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0197WindowsPortableExe = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0197WindowsPortableExe as default
};
