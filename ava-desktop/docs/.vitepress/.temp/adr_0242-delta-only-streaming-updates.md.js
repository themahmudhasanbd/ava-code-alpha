import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"ADR 0242: Delta-only coalesced streaming updates","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0242-delta-only-streaming-updates.md","filePath":"adr/0242-delta-only-streaming-updates.md","lastUpdated":1789321937000}');
const _sfc_main = { name: "adr/0242-delta-only-streaming-updates.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="adr-0242-delta-only-coalesced-streaming-updates" tabindex="-1">ADR 0242: Delta-only coalesced streaming updates <a class="header-anchor" href="#adr-0242-delta-only-coalesced-streaming-updates" aria-label="Permalink to &quot;ADR 0242: Delta-only coalesced streaming updates&quot;">​</a></h1><ul><li>Status: Accepted</li><li>Date: 2026-09-14</li><li>Decision: D412</li><li>Amends: ADR 0127, ADR 0130, ADR 0149, ADR 0153</li><li>Issue: #299</li></ul><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><p>Long Agent turns streamed at 200+ tokens/s from the provider, but PI-Desktop displayed them at a much lower rate that got worse the longer the turn ran. Stop plus a new prompt restored speed; the same turn&#39;s later short thinking blocks stayed slow. Subagents showed the same lag. TTFT was fine.</p><p>Each <code>message_update</code> flattened the full assistant snapshot, compared it with <code>startsWith</code> against the accumulated string, <code>JSON.stringify</code>&#39;d the whole <code>UiMessage</code> onto sidecar stdout, and AgentHost <code>boundPayload()</code> stringified it again to check frame size. Renderer batching only ran after that work. A length-n stream therefore cost about O(n²) bytes and CPU on the hot path and backlogged the pipeline.</p><p>The renderer already split history from the active tail, but the active <code>AssistantTurn</code> rebuilt every activity part on each token and handed new delegation <code>Map</code>s to every <code>ActivityGroup</code>, so historical groups in the same turn re-rendered with the tail.</p><p>RACP already documented <code>item.delta</code> as non-durable delta text, with the full row completed in <code>item.completed</code>. The local event still carried a full snapshot on every update.</p><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><p>Keep <code>currentAssistant</code> as the in-memory authority in the runtime. On the append-only streaming path, emit <code>message_update</code> with:</p><ul><li><code>stream: &quot;delta&quot;</code></li><li><code>deltaText</code> / <code>deltaThinking</code></li><li><code>resetText</code> / <code>resetThinking</code> when the provider replaced rather than appended</li><li>a slim <code>message</code> identity (id, role, status, createdAt, model/provider, parentToolCallId, agentName) without growing <code>content</code> / <code>thinking</code></li></ul><p>A 16ms coalescer in <code>DesktopAgentRuntime</code> concatenates pending deltas and flushes immediately before <code>tool_start</code>, <code>tool_end</code>, <code>message_end</code>, <code>error</code>, <code>abort</code>, retry snapshots, and other semantic events so order is preserved.</p><p><code>message_start</code> and <code>message_end</code> still carry a full <code>UiMessage</code>. Retry and other snapshot replacements omit <code>stream</code> and still replace the live row.</p><p>First-party consumers apply deltas:</p><ul><li>AgentHost patches <code>activeItems</code> and skips <code>JSON.stringify</code> for small delta frames</li><li>Electron main reconstructs the inflight checkpoint snapshot from deltas</li><li>the renderer concatenates same-frame deltas, then patches the live <code>UiMessage</code></li></ul><p>The transcript projection reuses unchanged activity parts and keeps delegation status/timing <code>Map</code>s stable when their contents did not change. Only the live tail ActivityGroup receives <code>runtimeActivity</code>.</p><p>Protocol version remains 11. The new fields are additive. Consumers that ignore <code>stream</code> and replace with <code>message</code> still receive <code>message_end</code> as the authoritative row.</p><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Cross-process streaming bytes grow with the new chunk, not with the cumulative snapshot.</li><li>A long turn no longer forms a serialization backlog that later short chunks wait behind.</li><li>Historical activity groups in the active turn do not re-render for each tail token.</li><li>Crash-recovery checkpoints still see a reconstructed full snapshot.</li><li>Remote RACP <code>item.delta</code> payloads match the documented delta contract.</li></ul><h2 id="alternatives" tabindex="-1">Alternatives <a class="header-anchor" href="#alternatives" aria-label="Permalink to &quot;Alternatives&quot;">​</a></h2><ul><li>Coalesce only in the renderer: rejected; the O(n²) stringify already happened in the sidecar and main.</li><li>Drop tokens or raise the debounce to 50ms: rejected; that hides backlog rather than removing it.</li><li>New <code>message_delta</code> event type: unnecessary; additive fields on the existing <code>message_update</code> keep old consumers compiling.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("adr/0242-delta-only-streaming-updates.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _0242DeltaOnlyStreamingUpdates = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _0242DeltaOnlyStreamingUpdates as default
};
